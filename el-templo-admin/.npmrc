# pnpm configuration
engine-strict=true
shamefully-hoist=true
# Supply chain attack mitigation: reject packages published less than 7 days ago.
# Aplica al resolver deps (pnpm add/install sin frozen-lockfile). Precedente: axios.
minimum-release-age=10080
