/**
 * Autotest del timer sobre el hardware real: `/tv/?selftest=1`.
 *
 * STUB — TODO(plan 164-07): bajar `/tv/timer-vectors.json` (los 6 vectores dorados que
 * emite el test del API, copiados por scripts/build-tv.mjs), correrlos contra el `phaseAt`
 * del kiosco e imprimir PASS/FAIL en pantalla. Es la unica verificacion automatica que
 * puede correr en el televisor de una sede.
 */

export function runSelfTest(): void {
  // Sin implementacion todavia.
}
