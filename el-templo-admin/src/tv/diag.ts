/**
 * Pantalla de diagnostico `/tv/?diag=1`.
 *
 * STUB — TODO(plan 164-06): imprimir user agent, feature-detects (container queries,
 * aspect-ratio, gap en flex, fetch), `window.__TV_VERSION__`, latencia del poll y el
 * buffer de `logger.ts`. Es el unico instrumento posible en un televisor sin devtools.
 */

export function renderDiag(): void {
  // Sin implementacion todavia.
}
