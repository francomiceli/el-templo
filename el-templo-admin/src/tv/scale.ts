/**
 * Escalado del marco del kiosco: 16:9 exacto + tamaño de fuente raiz derivado del ancho.
 *
 * STUB — TODO(plan 164-06): calcular el marco y el font-size raiz por JS para reemplazar
 * `container-type: size` + `cqw` del mockup (Chromium 105) por `rem` (universal).
 * Se llama desde `main.ts` al arrancar y en cada `resize`, asi que ya esta cableado.
 */

export function scaleTv(): void {
  // Sin implementacion todavia: el CSS del mockup sigue usando cqw hasta el plan 164-06.
}
