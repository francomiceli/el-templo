name: Build iOS Staging (TestFlight)

# Manual trigger only — macOS runners are expensive (~10x Linux).
#
# PREREQUISITES: Set up the following GitHub Secrets before first run:
#
#   Apple Developer Account (requires $99/year Apple Developer Program membership):
#     IOS_BUILD_CERTIFICATE_BASE64  — Distribution certificate (.p12) encoded as base64
#     IOS_P12_PASSWORD              — Password for the .p12 certificate
#     IOS_PROVISION_PROFILE_BASE64  — Provisioning profile (.mobileprovision) encoded as base64
#
#   App Store Connect API (for TestFlight upload):
#     APPLE_API_KEY_ID              — App Store Connect API key ID
#     APPLE_API_ISSUER_ID           — App Store Connect API issuer ID
#     APPLE_API_KEY_BASE64          — App Store Connect API private key (.p8) encoded as base64
#
#   Staging Environment:
#     STAGING_VITE_API_URL          — Staging API URL (e.g., https://api-staging.eltemplo.org/api)
#     STAGING_VITE_SENTRY_DSN       — Staging Sentry DSN for frontend error tracking
#
# SETUP STEPS:
#   1. Register staging app (com.eltemplo.app.staging) in App Store Connect
#   2. Create a Distribution certificate in Apple Developer portal
#   3. Create a Provisioning Profile for com.eltemplo.app.staging
#   4. Create an App Store Connect API key with Developer role
#   5. Base64-encode all certificate/key files: base64 -i file.p12 | pbcopy
#   6. Add all secrets to GitHub repository settings
#
# If altool doesn't work at runtime (Apple has partially deprecated it),
# switch to the apple-actions/upload-testflight-build GitHub Action
# or use xcrun notarytool for the upload step.
on:
  workflow_dispatch:

env:
  NODE_VERSION: '22'
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    name: Build & Upload to TestFlight
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install dependencies
        working-directory: el-templo-app
        run: pnpm install --frozen-lockfile

      - name: Build web assets for staging
        working-directory: el-templo-app
        run: pnpm run build
        env:
          VITE_API_URL: ${{ secrets.STAGING_VITE_API_URL }}
          VITE_APP_NAME: "El Templo (Staging)"
          VITE_SENTRY_DSN: ${{ secrets.STAGING_VITE_SENTRY_DSN }}
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          SENTRY_ORG: ${{ secrets.SENTRY_ORG }}
          SENTRY_PROJECT: ${{ secrets.SENTRY_PROJECT }}

      - name: Install Capacitor dependencies
        working-directory: el-templo-app/src-capacitor
        run: pnpm install --frozen-lockfile

      - name: Sync web assets and native plugins
        working-directory: el-templo-app/src-capacitor
        env:
          STAGING: 'true'
        run: |
          mkdir -p www
          cp -r ../dist/spa/* www/
          npx cap sync ios

      - name: Install CocoaPods
        working-directory: el-templo-app/src-capacitor/ios/App
        run: pod install

      - name: Import signing certificate
        env:
          BUILD_CERTIFICATE_BASE64: ${{ secrets.IOS_BUILD_CERTIFICATE_BASE64 }}
          P12_PASSWORD: ${{ secrets.IOS_P12_PASSWORD }}
        run: |
          CERTIFICATE_PATH=$RUNNER_TEMP/build_certificate.p12
          KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db

          echo -n "$BUILD_CERTIFICATE_BASE64" | base64 --decode -o $CERTIFICATE_PATH

          security create-keychain -p "" $KEYCHAIN_PATH
          security set-keychain-settings -lut 21600 $KEYCHAIN_PATH
          security unlock-keychain -p "" $KEYCHAIN_PATH
          security import $CERTIFICATE_PATH -P "$P12_PASSWORD" -A -t cert -f pkcs12 -k $KEYCHAIN_PATH
          security list-keychain -d user -s $KEYCHAIN_PATH

      - name: Import provisioning profile
        env:
          BUILD_PROVISION_PROFILE_BASE64: ${{ secrets.IOS_PROVISION_PROFILE_BASE64 }}
        run: |
          PROFILE_PATH=$RUNNER_TEMP/build_pp.mobileprovision
          echo -n "$BUILD_PROVISION_PROFILE_BASE64" | base64 --decode -o $PROFILE_PATH
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          cp $PROFILE_PATH ~/Library/MobileDevice/Provisioning\ Profiles

      - name: Build and archive
        working-directory: el-templo-app/src-capacitor/ios/App
        run: |
          xcodebuild -workspace App.xcworkspace \
            -scheme App \
            -sdk iphoneos \
            -configuration Release \
            -archivePath $RUNNER_TEMP/app.xcarchive \
            archive \
            CODE_SIGN_STYLE=Manual

      - name: Create ExportOptions.plist
        run: |
          cat > $RUNNER_TEMP/ExportOptions.plist << 'PLIST'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>method</key>
            <string>app-store</string>
            <key>destination</key>
            <string>upload</string>
          </dict>
          </plist>
          PLIST

      - name: Export IPA
        run: |
          xcodebuild -exportArchive \
            -archivePath $RUNNER_TEMP/app.xcarchive \
            -exportOptionsPlist $RUNNER_TEMP/ExportOptions.plist \
            -exportPath $RUNNER_TEMP/export

      - name: Upload to TestFlight
        env:
          APPLE_API_KEY_ID: ${{ secrets.APPLE_API_KEY_ID }}
          APPLE_API_ISSUER_ID: ${{ secrets.APPLE_API_ISSUER_ID }}
          APPLE_API_KEY_BASE64: ${{ secrets.APPLE_API_KEY_BASE64 }}
        run: |
          # Decode API key
          mkdir -p ~/private_keys
          echo -n "$APPLE_API_KEY_BASE64" | base64 --decode > ~/private_keys/AuthKey_${APPLE_API_KEY_ID}.p8

          # Upload to TestFlight via altool
          # Note: If altool is removed in a future Xcode, use apple-actions/upload-testflight-build
          # or the App Store Connect API directly.
          xcrun altool --upload-app \
            -f $RUNNER_TEMP/export/*.ipa \
            --apiKey "$APPLE_API_KEY_ID" \
            --apiIssuer "$APPLE_API_ISSUER_ID" \
            -t ios

      - name: Upload IPA as artifact (backup)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: staging-ipa-${{ github.sha }}
          path: ${{ runner.temp }}/export/*.ipa
          retention-days: 30
          if-no-files-found: ignore

      - name: Cleanup keychain
        if: always()
        run: |
          security delete-keychain $RUNNER_TEMP/app-signing.keychain-db 2>/dev/null || true
