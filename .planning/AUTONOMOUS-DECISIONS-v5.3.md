# Autónomo v5.3 — Log de decisiones (para revisar con Franco a la mañana)

Run desatendido del milestone v5.3 (fases 145-147). Reglas acordadas:

- **Ante ambigüedad/bloqueo NO frenar:** tomar la decisión más razonable según el
  brief (`BRIEF-FEEDBACK-V52-CAJA.md`), registrarla acá y seguir.
- **No pushear nada** (regla de Franco "siempre preguntar antes de pushear").
  Todo queda commiteado local en `staging`. Franco revisa y pushea a la mañana.
- Saltear `discuss-phase`. Cadena `plan-phase → execute-phase` por fase, secuencial.
- Auto-aceptar checkpoints del executor. Correr los tests de cada fase.

## Decisiones tomadas (cada una a confirmar)

_(Se completa durante el run. Formato: Fase NNN — decisión — por qué — alternativa.)_

### Fase 145 — PoS del profe

- **Saltear discuss/CONTEXT.md** — uso el brief como fuente de decisiones (ya está todo cerrado). Alternativa: correr discuss (innecesario, perdería tiempo).
- **Saltear research** — son cambios puntuales sobre código conocido (CargarPagoPage.vue, coach-load-routes.ts, BandejaPendientesTab.vue). Alternativa: research (no aporta).
- **Saltear UI-SPEC** (gate UI) — el brief y los requirements ya tienen el detalle visual (aviso de deuda, dropdown Motivo, chip). Alternativa: generar UI-SPEC formal (overhead innecesario para 3 cambios chicos de UI).
- **Saltear plan-checker formal en 145** (fase chica, bajo riesgo: UI + 1 columna enum) para reservar contexto a la fase 146 (pesada). El planner ya hizo coverage audit y el executor verifica acceptance_criteria + corre tests. El plan-checker SÍ se corre en 146. Alternativa: correrlo igual (insurance barato pero gasta contexto en el run nocturno).
- Migración 0159 = columna `misc_reason` enum `('sin_plan','otro')` NULL en `financial_transactions`.

#### 145-01 (ejecutado)

- **Moneda del cobro suelto ahora sigue al plan del socio** — al cargar `autocompletar` también en modo misc (necesario para el banner de deuda POS-01), `currencySymbol` y el `currency` que se manda en `miscCharge` pasan a reflejar la moneda del plan vigente (`autocompletar.currency`, fallback `'ARS'`). Antes en misc siempre era ARS porque `autocompletar` quedaba `null`. **Por qué:** es más correcto (un socio con plan EUR cobra en EUR) y reusa el mismo patrón que renew. Socio sin plan → `hasRenewable=false`, `currency=null` → sigue ARS (sin cambio). **Alternativa descartada:** desacoplar la moneda de misc de `autocompletar` para preservar el ARS fijo (más código, y dejaría el símbolo del monto inconsistente con el banner). A confirmar con Franco si se prefiere forzar ARS en cobro suelto.
- **Default del dropdown Motivo = 'sin_plan'** (caso operativo principal según el brief). El ref es nullable (`'sin_plan' | 'otro' | null`) por pedido del plan, pero arranca y se resetea a `'sin_plan'`; `canConfirm` igual exige que esté seteado. **Alternativa:** arrancar sin selección (forzaría un tap extra al profe en el caso más común).
- **Banner de deuda ubicado debajo del q-select de socio, fuera de los bloques de modo** — depende sólo de `autocompletar?.outstanding > 0`, así aplica idéntico a renew y misc sin duplicar markup. Sobrevive el cambio de modo porque `onModeChange` recarga `autocompletar` en ambos modos.

#### 145-02 (ejecutado)

- **Chip "Sin plan — asignar" dentro del slot `#body-cell-socio`, no como columna nueva** — el plan lo prefería explícitamente; renderizado en un `<div>` debajo del nombre del socio (`q-chip` dense, color `warning`, ícono `person_add`, tamaño `sm`). Reusa `goToMember(row.memberId)` (sin segundo `router.push`). **Alternativa descartada:** columna propia en la grilla (agregaría ancho a una tabla ya densa).
- **Condición de visibilidad del chip:** `row.miscReason === 'sin_plan' && row.memberId`. El guard de `memberId` evita un chip que navegaría a `/alumnos/null`. Filas con `miscReason` null/'otro' no muestran chip (por diseño del requirement).
- **Sin nueva ambigüedad de scope** — todo el contrato (ruta `/alumnos/:userId`, líneas de `select`/`map`, mirror manual del tipo) estaba cerrado en el plan; no hubo desviaciones. Error pre-existente `onExportBandeja` (TS2339, línea 38) en `BandejaPendientesTab.vue` queda fuera de scope (ya documentado en 145-01, no introducido por este plan).

### Fase 146 — Caja: validación e imputación fundacional

#### 146-01 (ejecutado) — CAJA-01 + CAJA-04: caja sugerida = sede del profe

- **Fallback amplio en `resolveSuggestedCaja`** — el `try/catch` alrededor de `resolveCashRegister` captura CUALQUIER error (sin caja efectivo en la sede del profe Y moneda inconsistente), no sólo el caso "branchId no resolvible" del behavior. Devuelve `undefined` → `create` resuelve por la sede del socio (comportamiento previo). **Por qué:** es estrictamente más seguro ("no romper" el cobro del profe); si la resolución por sede del socio también falla, el error original de `create` sí surfacea. **Alternativa descartada:** catch acotado sólo a `BadRequestError` de "no caja" (dejaría que un mismatch de moneda del profe rompa un cobro que hoy funciona por sede del socio).
- **DRY: `resolveUserBranchId` compartido** — extraje la resolución `users.branchId` + fallback "Templo Online" a un helper único; `resolveMemberBranchId` (sede del socio, ledger) y `resolveRecorderBranchId` (sede del profe, caja) lo delegan. **Alternativa descartada:** duplicar el cuerpo (viola DRY, CLAUDE.md pide flaggearlo agresivamente).
- **`TransactionService.resolveCashRegister` público** — en vez de agregar un `CashRegisterService` al constructor de `SubscriptionService` (afecta todos los sitios de instanciación), expuse un método público que delega a `cashRegisterService.resolveCashRegister`. `SubscriptionService` ya tiene un `TransactionService` inyectado → cero DI nueva. **Alternativa descartada:** DI extra de `CashRegisterService` (más superficie, más sitios a tocar).
- **Pre-resolución del caja en renew guardada por `renewalPrice > 0`** — sólo se pre-resuelve la caja sugerida si habrá charge (precio > 0); una renovación gratis no crea transacción, así que evitamos un throw innecesario. **Alternativa:** resolver siempre (arriesga un error en un path no-op).
- **`recordAssignmentCharge.cashRegisterId` opcional reutilizable por plan 03** — el param queda genérico (override de caja); los 4 callers internos admin no lo pasan → `undefined` → caja por sede del socio (sin regresión, verificado con `charge-on-assign.test.ts` 14/14 verdes). El plan 03 lo reutiliza para imputar el anticipo.
- **Tests de Task 1 y Task 2 en un mismo `describe` ("caja sugerida por sede del profe")** — crean sede B + socio B + caja banco ARS una vez en `beforeAll`; cubren misc cash, misc transfer, settle cash y renew cash. CAJA-04 no necesitó cambio de UI (la PoS ya no expone caja/sede); sólo se agregó un comentario marcando CAJA-04 en el schema.

#### 146-02 (ejecutado) — CAJA-02 + CAJA-03 + COBRO-05 + primitivos plan 03

- **Migración 0160 idempotente con derived tables (esquiva error 1093)** — el seed de Galicia/Mercado Pago hace `INSERT ... SELECT` que referencia `cash_registers` tanto para el `cutoff_date` (`MIN(cutoff_date)`) como para el guard `NOT EXISTS`. MySQL tira error 1093 ("can't specify target table in FROM") al referenciar la tabla destino en un subquery del propio INSERT. Lo resolví materializando ambos subqueries en derived tables: el del cutoff con agregación, el del NOT EXISTS con `LIMIT 1` (fuerza materialización en MySQL 8, que si no mergea el derived table y re-dispara 1093). **Por qué:** el test setup aplica migraciones con un catch que NO tolera el mensaje de 1093 → rompería todo el suite. **Alternativa descartada:** hardcodear el cutoff (perdería el "valor global existente" que pide el plan) o no poner guard de idempotencia (el plan lo exige por nombre).
- **cutoff_date de las cajas nuevas = MIN(cutoff_date) global** — la columna es NOT NULL; copio el valor global existente en vez de `CURDATE()` para mantener la convención del seed 0154 (un único cutoff global). En la DB local quedó `2026-06-24`.
- **Casi meto un `;` en un comentario SQL** — la primera corrida de `db:migrate` falló con ER_PARSE_ERROR porque escribí literalmente "splittea por ;" en un comentario del .sql (el runner splittea por `;` antes de strippear `--`). Reescribí el comentario sin el carácter. Registrado como recordatorio: el lint del runner es real.
- **Body de validate con `type: ["object", "null"]`** — agregar un body schema al endpoint que antes NO tenía body rompió el test existente `validation-state.test.ts` (validate REST sin payload → `request.body=null` → 400 con `type:"object"`). Incluir `"null"` mantiene la retrocompat (validar sin elegir caja = conservar la sugerida). **Alternativa descartada:** tocar el test existente (el plan exige que siga pasando sin cambios).
- **RBAC pending-misc = FINANCE_VOID_ROLES per-handler (LOW 2)** — el endpoint devuelve datos financieros del socio (cobros sueltos). El guard de módulo es FINANCE_READ (incluye recepción); el per-handler estricto FINANCE_VOID (owner/admin/gestion) 403'a a recepción. NO usé SUBSCRIPTION_ROLES (que incluye coach/recepción) aunque "asignar plan" lo sugería. Test "recepción → 403" incluido. **Alternativa descartada:** SUBSCRIPTION_ROLES (filtraría datos financieros a recepción/coach).
- **LOW 1 resolver estable** — `orderBy(cashRegisters.id)` en la query banco del resolver; con 3 cajas banco ARS (Banco ARS + Galicia + Mercado Pago) la sugerida queda fija en la más antigua (Banco ARS). La caja REAL la elige gestión al validar, así que esto es sólo la sugerencia inicial.
- **Tasks 2 y 3 commiteadas juntas (e6fa89f3)** — comparten los mismos 4 archivos (transaction-service, schemas, routes, test). Separarlas por archivo era imposible sin staging parcial de hunks; el unit cohesivo es un solo commit. Task 1 (migración) sí quedó en su commit propio (46c47146).

#### 146-03 (ejecutado) — COBRO-03 imputación atómica + COBRO-04 excedente

- **`recordAssignmentCharge` default vs imputación = if/else mutuamente excluyente (corrección MEDIUM del plan-checker)** — la llamada incondicional original a `recordAssignmentCharge(tx, ...)` se convirtió en `if (appliedMiscChargeId) { void + recreate } else { default }`. Ejecutar ambos crearía un segundo `plan_charge` (doble ingreso, crédito fantasma). El test cuenta filas `plan_charge` del socio = exactamente 1 (no sólo "existe una"). **Alternativa descartada:** dejar el default y sumar la variante (= doble cobro, el bug que el checker señaló).
- **Lectura del advance + validaciones DENTRO de la `db.transaction` existente** — no se abrió tx nueva; la lectura, los guards (T-146-08), el check de excedente (COBRO-04), el `voidInTx` y el `recordAssignmentCharge` de imputación comparten el handle `tx` de `assignPlan`. Atomicidad total: si algo posterior revienta, el void rollbackea y el advance NO queda anulado. **Verificado** con un test que mockea `BalanceService.applyDelta` para reventar SÓLO en el recreate (`row.kind === 'plan_charge'`), dejando pasar la reversa del void → rollback total, advance sigue `pendiente`.
- **COBRO-04 rechaza ANTES de anular** — el guard `advance.amount > pricePaid → BadRequestError` corre antes del `voidInTx`. Como además está dentro de la tx, aunque tirara después igual rollbackearía; pero lo ordené antes para que ni siquiera se intente anular. Test: anticipo 60000 vs plan 50000 → 400 "excede el precio del plan", advance sigue pendiente, sin sub ni plan_charge.
- **Guards T-146-08 colapsados en un solo `if`** — `!advance || memberId!==userId || kind!=='advance_payment' || validationStatus!=='pendiente' || voidedAt!==null` → 400. Cubre anticipo de otro socio, ya validado, ya anulado o id inexistente. Tests para "otro socio" y "ya validado".
- **`paymentMethod`/`cashRegisterId` del anticipo overridean el payload** — en la imputación se pasa `paymentMethod: advance.paymentMethod` y `cashRegisterId: advance.cashRegisterId` a `recordAssignmentCharge`; el `paymentMethod: 'cash'` que mande el dialog se ignora (el test lo verifica: anticipo transfer → plan_charge nace transfer). El plan_charge nace `validado` (camino admin, sin `recorderRole`).
- **audit `plan_assigned` lleva `imputedFromMiscChargeId` (T-146-11)** — flag en el payload (null cuando no hay imputación). El `voidInTx` deja su propio audit `transaction_voided`, así que el rastro es doble.
- **UI: selector sólo en modo 'assign'** — `changePlan` NO implementa `appliedMiscChargeId` en el backend, así que el selector se muestra sólo si `props.mode !== 'change'`. `loadPendingMisc()` retorna temprano en modo change. **Alternativa descartada:** ofrecerlo también en cambio de plan (el backend lo ignoraría → confusión).
- **getPendingMisc en `useTransactionsApi` (no en `useSubscriptionsApi`)** — el endpoint vive bajo `/admin/finance/transactions/pending-misc`, pertenece al dominio finance. La falla de la lectura no bloquea el alta (`catch` → selector no se muestra, flujo normal intacto).
- **`isCobroInvalid` bloquea Confirmar sólo si `miscExceedsPrice`** — con un anticipo imputado no se valida `amountReceived` (lo aporta el anticipo); el único caso que bloquea es excedente (que el backend rechazaría con 400). Anticipo menor al precio se permite (socio queda deudor, con banner amarillo).

#### 146-04 (ejecutado) — CAJA-02/CAJA-03 (FE) selector de caja al validar + COBRO-05 (FE) bloqueo del Validar

- **Filtro de cajas por medio de pago en el selector** — `cajaOptions` filtra primero por `currency` de la fila (nunca cross-currency), luego acota por medio: `transfer`/`card` → sólo `type==='banco'` (cuentas Galicia / Mercado Pago); `cash` → sólo `type==='efectivo'`; otros medios (`aura_credit`/`internal`) → todas las cajas de la moneda. El plan sólo exigía explícitamente `transfer → banco`; extendí `card → banco` (las tarjetas liquidan en cuenta banco) por coherencia. El backend (plan 02) valida igual la coherencia con 400 → defensa en profundidad, el filtro sólo guía la elección. **Alternativa descartada:** mostrar todas las cajas siempre (confunde a gestión y deja elegir cajas incoherentes que el server rechazaría).
- **Selector dialog propio en vez del `$q.dialog` programático** — el confirm simple de `onValidar` no admite un `q-select`; lo reemplacé por un `q-dialog` template (mismo patrón que Observar/Corregir/Anular), reusando `actionRow`/`dialogLoading` compartidos. `selectedCajaId` arranca pre-seleccionado con `row.cashRegisterId` (la caja sugerida del resolver del plan 02). Validar queda `:disable` si no hay caja elegida.
- **Mensaje de error del backend vía `transactionsApi.error.value`** — el plan pide mostrar el 400 del server (moneda incoherente / sin_plan) con notify negative. El `err` que sube el composable es el axios error crudo (`err.message` = "Request failed with status code 400"), pero `extractError` ya volcó el mensaje real del server en `transactionsApi.error.value`; uso ese ref en el catch. **Alternativa descartada:** `err.message` (mostraría el texto genérico de axios, no el del backend).
- **Doble barrera COBRO-05** — botón Validar `:disable` para `miscReason==='sin_plan'` + `q-tooltip` "Asigná un plan para imputar este cobro", y guard temprano `if (row.miscReason === 'sin_plan') return` al inicio de `onValidar` por si el click se dispara igual. El chip "Sin plan — asignar" (fase 145) sigue visible y navega a la ficha. El server ya rechaza (plan 02) → defensa en profundidad pura.
- **`loadCashRegisters` se carga en mount y al cambiar de país** — reusa `getCashRegisterBalances` (params `country` cuando isOwner, igual que `loadBandeja`). Si falla, sólo loguea (no bloquea la bandeja); el selector queda vacío con hint "No hay cajas activas para esta moneda".

#### 146-05 (ejecutado) — ARQUEO-01/02/04 listMovEgresos pasa a arqueo por caja

- **Task 2 (exponer validationStatus en schema/route) = sin cambio de código, sólo docs** — la respuesta de `GET /movements-history` es passthrough (el `movementsHistorySchema.response` sólo registra 401/403/500, no un schema de fila), y la route devuelve `listMovEgresos(filters)` directo sin mapeo que descarte campos. Por eso el `validationStatus` agregado al service shape (task 1) fluye solo; el test ARQUEO-02 confirma que el endpoint lo devuelve. Sólo actualicé comentarios de route/schema. **Alternativa descartada:** tipar explícitamente la fila en el response schema (over-engineering; el resto del módulo confía en MovEgresoItem sin tipar la respuesta).
- **`conditions` puede quedar vacío para owner sin filtros** — al quitar el `inArray(kind, ...)` el array de condiciones arranca vacío; owner global sin caja/país → `.where(and())` = `.where(undefined)` (sin WHERE, ve todo, correcto). Non-owner sin país sigue forzando `sql\`1 = 0\`` (defensa en profundidad intacta). **Verificado** con tsc + tests (67/67).
- **`list()` (Transacciones) NO se tocó (ARQUEO-04)** — es una función separada con INNER JOIN users/branches; su criterio quedó idéntico. Agregué un test que inserta una expense NULL-member en la caja y verifica que aparece en el arqueo pero NO en `GET /transactions`, mientras un plan_charge de socio sí sigue en list().
- **El export REP-04 (`/movements-history/export`) hereda el arqueo por caja** — reusa `listMovEgresos`, así que ahora exporta todos los kinds de la caja (consistente con el arqueo). No requirió cambios; el workbook sólo itera filas.

#### 146-06 (ejecutado) — ARQUEO-02/03 (FE) filtro Cobros + marca de estado en MovEgresosTab

- **FE `MovEgresoItem` sincronizado con el backend del plan 05** — el type de `src/types/transaction.ts` seguía describiendo el shape viejo (kind IN cash_transfer/expense/adjustment, sin `validationStatus`, sin `memberId`). Lo actualicé a espejo del API: `kind` string ensanchado (suma plan_charge/debt_settlement/advance_payment/refund), `memberId: number | null` y `validationStatus: string`. **Alternativa descartada:** castear ad-hoc en el componente (dejaría el type mintiendo sobre la respuesta real).
- **`validationStatus` tipado como `string` en el FE (no union)** — el enum del schema es `pendiente|observado|corregido|validado`, pero el resto del FE ya tipa este campo como `string` (TransactionListItem.validationStatus línea 335). Mantuve la coherencia con un helper `validationLabel`/`validationColor` que mapea por valor con fallback al raw. **Alternativa descartada:** introducir un `type ValidationStatus` nuevo (inconsistente con el resto del módulo, fuera de scope).
- **`corregido` mapeado a `grey-6`** — el plan sólo especificó pendiente=warning, validado=positive, observado=info. El enum tiene un cuarto estado `corregido` (post void+recreate) no mencionado; le asigné `grey-6` (neutro, "ya resuelto/archivado") y label "Corregido". **Alternativa descartada:** omitirlo (caería al fallback mostrando el string crudo sin color coherente).
- **`kindColor`: cobros de socio = positive, refund = negative** — plan_charge/debt_settlement/advance_payment (ingresos) en verde; `refund` (reintegro, sale plata) en rojo junto a expense. **Alternativa descartada:** todos los cobros en un solo color (refund es egreso de dinero, agruparlo con los ingresos confunde el arqueo).
- **`isEgreso` ahora incluye `refund`** — el "−" rojo del monto se muestra para expense Y refund (ambos restan de la caja). Dejé `cash_transfer` fuera (su narrativa origen→destino va en Concepto, sin signo, para no romper el display de movimientos ya consolidado del plan 04). **Alternativa descartada:** basar el signo en `direction==='outflow'` (la leg de origen de un traspaso pasaría a mostrar "−", cambiando el display ya establecido).
- **Columna "Estado" entre Caja y Registrado por + badge en el detail dialog (146-06)** — el badge de validación se muestra tanto en la fila (columna nueva) como en el dialog de detalles, reusando el patrón q-badge existente.

### Fase 147 — Centros de costo de egresos

#### 147-01 (ejecutado) — EGR-01/02/03 (backend) catálogo + columna + endpoint + validación + arqueo

- **`cost_centers.country` = `varchar(2)` (NO enum)** — modelado igual que `branches.country` (decisión 7 del CONTEXT), para no introducir un enum nuevo que requiera mantener orden byte-for-byte con la migración. Default `'AR'`, notNull. Seed AR: Alquiler Constitución / Librería / Viáticos profes / Varios ("Varios" = escape obligatorio). **Alternativa descartada:** `mysqlEnum('country', ['AR','ES'])` (más rígido, exige cuidar enum drift y no aporta sobre varchar para un catálogo chico).
- **Migración 0161 hand-written + seed idempotente `INSERT … SELECT … FROM DUAL WHERE NOT EXISTS`** — `db:generate` sigue roto por el drift pre-existente (`sessions.goal_plan_type`), igual que 0154/0158. Seed keyed on `(name, country)` con `SELECT … FROM DUAL` (no subquery sobre `cost_centers` en el mismo INSERT…SELECT) para evitar error 1093. Aplicada local con `pnpm db:migrate` (4 filas AR, re-run = "No new migrations"). **Trampa que mordió:** dos comentarios `--` contenían un `;` (uno irónicamente describiendo la regla); el runner splittea por `;` antes de strippear `--` → parse error. Reescritos sin `;`.
- **`cost_center_id` NULLABLE a nivel DB, obligatorio a nivel app SOLO para `expense`** — FK nullable tras `cash_register_id` (mismo patrón). Los 10 create paths dejan `costCenterId` undefined → NULL; únicamente `registerExpense` lo setea. `CreateTransactionInput.costCenterId?` es server-derived (NUNCA del body de `/transactions`), espejo de `miscReason`.
- **Validación del egreso = exists + active (NO match de país centro↔caja)** — `registerExpense` hace un solo `SELECT … WHERE id = ? AND is_active = true`; si falta o no existe/está inactivo → `BadRequestError`. NO se valida que el país del centro coincida con el de la caja: el selector del front ya filtra por país y el server solo garantiza un centro real y activo (T-147-01). El schema además exige `costCenterId` (`required`, `minimum:1`) → doble barrera (400 de schema + 400 de servicio).
- **`listActiveCostCenters` vive en `CashRegisterService` (no un service nuevo)** — `cost_centers` es un catálogo análogo a `cash_registers`, así que el método de listado va en el "service de catálogos de caja" en vez de crear un `CostCenterService` de un solo método (sería over-engineering dado que el ABM está diferido). **Alternativa descartada:** service dedicado (premature abstraction).
- **`GET /cost-centers` gatea `FINANCE_VOID_ROLES` en-handler** — mismo conjunto que registrar egresos (decisión 3 del CONTEXT), por encima del `FINANCE_READ_ROLES` del module hook. Country scope owner-aware = copia EXACTA de `GET /cash-registers/balances` (owner → `?country?.toUpperCase()`; non-owner → `scope.country`). Coach → 403 (test lo cubre).
- **`costCenterName` en `listMovEgresos` vía LEFT JOIN `cost_centers`** — agregado SOLO a la query de rows (la de count no lo necesita), `r.costCenterName ?? null` en el map. Solo las filas `expense` traen nombre; el resto de kinds queda null (arqueo por caja trae todos los kinds).
- **Tests existentes de `movement-service.test.ts` ajustados (Rule 1, blast radius del cambio)** — `registerExpense` ahora exige `costCenterId`; los 3 calls del servicio + el payload del inject RBAC (schema valida antes del handler → sin `costCenterId` daría 400 en vez del 403 esperado) se actualizaron con un centro AR resuelto en `beforeAll` desde el seed 0161. Verificado: movement-service 10/10, cost-centers 8/8.
- **`pnpm test -- cost-centers` corre el suite COMPLETO (no filtra)** — con esta config de pnpm/vitest el `-- cost-centers` no actúa de filtro; corrió 2129 tests (511s, 119 fallos pre-existentes ajenos, p.ej. `user-status-transitions` FK `audit_log`). La invocación correcta para un archivo es `pnpm test test/finance/cost-centers.test.ts`. Usé esa para validar puntual (8/8) sin atribuirme los fallos del suite global.ación se muestra tanto en la fila (columna nueva) como en el dialog de detalles, reusando el patrón q-badge existente. No toqué los filtros de caja/período ni la pestaña "Transacciones" (MovimientosTab), per scope.
