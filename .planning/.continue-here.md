---
context: default
phase: milestone-v5.4-completo
task: revision-final
status: paused
last_updated: 2026-07-05T02:07:45Z
---

# Handoff — Milestone v5.4 (Reforma del Admin) COMPLETO, pendiente full revisión

> Corrida autónoma overnight 2026-07-04/05 (autorizada por Franco). Fases 153-156 ejecutadas de punta a punta.
> El milestone quedó 8/8 (149-152 venían de antes). **NADA pusheado** — todo local en `feat/roster-effective-dated` (82 commits de la sesión, tip `9aeb0797`).

## Critical Anti-Patterns

| Pattern                     | Description                                                                                                                                | Severity | Prevention Mechanism                                                     |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | -------- | ------------------------------------------------------------------------ |
| Shell cwd drift             | Un `cd el-templo-api && ...` dejó el shell parado ahí y los checks siguientes con rutas relativas dieron falsos negativos ("no such file") | advisory | Usar rutas absolutas o re-`cd` al root en cada comando compuesto         |
| Todo falso-positivo crónico | `v51-milestone-data-rollout.md` matchea TODAS las fases por keywords genéricas (score 0.6); ya fue revisado y descartado 8 veces           | advisory | Cerrarlo, retaggearlo o subir el umbral — no volver a evaluarlo por fase |

<current_state>
Milestone v5.4 completo: ROADMAP/STATE/REQUIREMENTS al 100% (33/33 reqs), PROJECT.md evolucionado. Las 4 fases nuevas (153-156) están `human_needed`: código verificado sin gaps, falta SOLO la verificación humana. Working tree limpio salvo los archivos PRE-EXISTENTES de antes de la sesión (segmentation, session-data-transformer, docs v5.3/130/131) que NO se tocaron ni deben barrerse.
</current_state>

<completed_work>

- **Fase 153 Mejoras de Deudas** (única con discusión interactiva): hub 3 tabs en `/deudas` (Por socio intacta default / Por deuda = DeudasReport mudado desde Reportes con motivo derivado + período + fecha registro + nota tooltip / Vencidos = leads 60d sin monto). Endpoint `GET /admin/reports/expired-members` (coach 403). Sin migración. Review: 8 warnings → 8 fixes.
- **Fase 154 Alumnos**: crear alumno prominente; cobro desde la fila → `/cobros?memberId=`; módulo `settings` nuevo (`pricing.card_surcharge_enabled`, GET staff/PUT owner) + **migración 0166** (ON El Templo); gate server-side en `resolvePriceType` (assign/change/renew/preview + PoS); export con `includeGreekLevel`; Avatar→"Categoría"; niveles griegos gateados (`TEMPLO_GREEK_LEVELS`); página `/configuracion/precios`. Review: 4 warnings → 4 fixes.
- **Fase 155 Horarios**: solape re-scopeado a (sucursal, día, actividad) en create/update/**toggle** (`findOverlappingSchedule`); **migración 0167** `activities.max_capacity`; cupo efectivo = actividad ?? sucursal (helper único `scheduling/capacity.ts`, usado también por analytics `getSlotOccupancy`); crear clase/actividad desde celda vacía (CreateSlotDialog `initial` + actividad inline con Cupo); grilla N slots por celda + click slot-aware desktop Y mobile. Review: 4 warnings → 4 fixes.
- **Fase 156 Planes de pago vs Rutinas**: labels "Planes de pago"/"Rutinas de entrenamiento" + flag `TEMPLO_TRAINING_ROUTINES`; Zero a config (`pricing.zero_price_enabled`, **migración 0168**, gate en resolvePriceType incl. boarding pass); **migración 0169** `plan_programs` + `programIds` en CRUD + acceso all→lista→nada con filtro anti-Foundation + multi-select en PlanFormDialog; test de regresión PLAN-04 (precio editable sin tocar históricos). Review: **1 crítico (CR-01 boarding pass en changePlanAfterCurrent) + 5 warnings → corregidos**; WR-06 diferido.
  </completed_work>

<remaining_work>

1. **UAT visual — 28 ítems** repartidos en `153/154/155/156-HUMAN-UAT.md` (aparecen en `/gsd:progress` y `/gsd:audit-uat`).
2. **Decisión WR-04 (154):** con regla de recargo OFF, las renovaciones normalizan tarjeta→regular (cambia cuánto se cobra en white-label; El Templo ON intacto). Confirmar o revertir `6cb6860b`.
3. **Decisión WR-06 (156):** `changePlanNow` ignora `boardingPass` (bug PRE-existente, fuera del diff, diferido con nota en `156-REVIEW.md`). Decidir fix aparte o aceptar.
4. **Push a staging** (siempre pedir OK): dispara CI con ~15 archivos de tests nuevos y arrastra las migraciones 0166-0169 al tren.
5. **Full revisión** (lo que Franco quiere hacer mañana): opciones — `/gsd:audit-milestone` (auditoría del milestone contra la intención original), `/gsd:audit-uat` (cross-fase de UAT pendientes), leer los 4 `*-REVIEW.md` (todos con resoluciones commiteadas).
   </remaining_work>

<decisions_made>

- **153 (interactiva con Franco):** 3 tabs; DeudasReport SALE de Reportes; vencidos = ventana fija 60d, sin monto, en ambos tabs si hay deuda+vencido; motivo derivado (sin campo nuevo); fecha = carga; nota en tooltip; profe solo ve "Por socio"; solo lectura.
- **154 (auto):** config de precios en `system_settings` (no per-plan); "Categoría" y NO "Segmento" (colisión con member_segment de 136); gate de niveles por superficie (no `canAccessTraining`).
- **155 (auto):** solape solo misma actividad; cupo POR SLOT sin techo agregado de sucursal; crear-desde-slot reusa dialog existente con prefill.
- **156 (auto):** réplica del patrón settings de 154 para Zero; `plan_programs` join conservando `grants_all_programs` (all>lista); renovaciones heredan `pricePaid` POR DISEÑO (assertado, no corregido); filtro Foundation heredado por la lista.
  </decisions_made>

<blockers>
- **Migraciones 0168/0169 NO aplicadas en la DB local** (0166/0167 sí): antes de probar Zero/multi-programa visualmente en local → `cd el-templo-api && pnpm db:migrate`.
- **⚠️ ORDEN DE MIGRACIONES vs master — RECONCILIAR ANTES DE PUSHEAR A STAGING/MASTER (detectado 2026-07-06).** Mientras el milestone se cocinaba, un PR aparte (ratings/roster) aterrizó en `master` con la **migración 0164**. Este branch trae `0163` + `0165–0169` pero **NO tiene 0164**, y además quedó **6 commits atrás de master** (hotfix renovación `1708312a`, ratings `a70a5296`/`fbb110e3`, bump 1.5.7 `8fd67005`, segmentation `9f20efe7`, merge PR#3 `44905fbd`).
  - **Riesgo:** en prod/staging el `0164` YA está aplicado, así que al deployar el milestone el `0163` (bank_accounts_and_retiros) se aplicaría *después* del `0164`; pero en una DB fresca (test DB de CI) el orden es `0163→0164`. Si `0163` y `0164` NO son independientes, el comportamiento diverge entre CI y prod (bug que `tsc` no atrapa). El runner (`run-migrations.ts`) NO tiene guard de orden — aplica por nombre ordenado salteando los ya-aplicados, sin avisar.
  - **Acción obligatoria antes del push:** (1) `git merge origin/master` dentro del branch (traer los 6 commits); (2) verificar que `0163` sea independiente de `0164` (probable: 0163 = caja/bancos, 0164 = ratings/roster) y **aceptar conscientemente** el orden divergente, **o** renumerar `0163→0170` (por encima del máx de master) para un orden limpio; (3) re-chequear que `0165–0169` no dependan de nada de master fuera de `0164`.
  - No se agregó guard de CI (decisión 2026-07-06: solo dejar esta nota). Si vuelve a pasar, considerar el guard mecánico en `ci.yml`.
</blockers>

## Required Reading (in order)

1. `.planning/phases/153-mejoras-de-deudas/153-HUMAN-UAT.md` → `154-*/154-HUMAN-UAT.md` → `155-horarios/155-HUMAN-UAT.md` → `156-*/156-HUMAN-UAT.md` — los 28 ítems de la revisión.
2. `.planning/phases/154-*/154-REVIEW.md` (WR-04) y `.planning/phases/156-*/156-REVIEW.md` (WR-06) — las 2 decisiones de negocio pendientes, con contexto completo.
3. `.planning/HANDOFF.json` — estado estructurado de esta pausa.

## Infrastructure State

- Branch: `feat/roster-effective-dated`, tip `57b674c9` (257 commits sobre master). **Pusheada a `origin/feat/roster-effective-dated` como BACKUP el 2026-07-06** (rama feature, NO dispara deploy ni migraciones). Quedó 6 commits atrás de master (ver blocker de orden de migraciones). Contiene también el WIP previo de la fase 151 v5.4 — esta branch ES la de trabajo del milestone.
- DB local: migraciones aplicadas hasta 0167; 0168/0169 pendientes de `pnpm db:migrate`.
- Gates verdes al pausar: `tsc --noEmit` API exit 0; `pnpm lint` admin 0 errores (9 warnings pre-existentes ajenos).
- Tests: NUNCA corridos localmente (regla del proyecto) — corren en CI al pushear.
- Archivos pre-existentes modificados SIN tocar (no barrer en commits): `.planning/AUTONOMOUS-DECISIONS-v5.3.md`, `130-*`/`131-CONTEXT`, `session-data-transformer.ts`, `segmentation/*`.

<context>
Franco pidió cerrar el milestone autónomamente tomando siempre las opciones recomendadas; mañana hace la full revisión. El pipeline por fase fue: discuss(--auto) → pattern-mapper → planner → plan-checker (hallazgos aplicados al plan ANTES de ejecutar) → executors secuenciales (worktrees off) → code-review → code-fixer (todo critical+warning corregido, info diferido) → verifier. Los verifiers dieron `human_needed` con 0 gaps de código en las 4 fases — el único delta a `passed` es la verificación visual/decisiones humanas.
</context>

<next_action>
Start with: leer los 4 HUMAN-UAT.md (o `/gsd:audit-uat` para verlos consolidados), decidir WR-04 y WR-06, y con el visto bueno pedir el OK de push a staging.
</next_action>
