---
phase: 14-admin-session-review-ui
plan: 08
type: execute
wave: 5
depends_on: ["14-04", "14-05", "14-06", "14-07"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Admin can complete full approval workflow (approve/revert, no discard)"
    - "Member app shows approved sessions only"
    - "Regeneration permanently deletes old sessions with confirmation dialog"
    - "Block pool swap lets coaches replace blocks from approved session pool"
    - "Pool blocks deduplicated by exercise fingerprint"
    - "Generation creates sessions for selected week"
---

<objective>
Human verification of complete admin session review workflow

Purpose: Verify end-to-end functionality of the admin session review system before phase completion.
Output: Confirmed working approval workflow, member visibility, and generation features
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-admin-session-review-ui/14-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete admin session review system including:
    - Admin Quasar app at el-templo-admin/ (port 9100)
    - Database schema with session status columns (discarded columns removed via migration 0007)
    - Admin API endpoints for session management (discard/restore removed)
    - SessionsPage with week/day navigation and filtering (no discard button)
    - SessionDetailPage with block cards, algorithm details, and block swap dialog
    - GeneratePage with permanent deletion confirmation dialog on regeneration
    - Block pool swap: coaches can replace any block with one from approved sessions pool
    - Pool blocks deduplicated by exercise fingerprint (sorted exercise names)
    - API endpoints: GET /admin/blocks/pool, POST /admin/sessions/:id/blocks/:blockId/swap
    - Member endpoint filtering (approved-only)
    - Pending count badge in admin drawer
    - Low sessions alert
    - DiscardedPage and /discarded route removed entirely
  </what-built>

  <how-to-verify>
    1. **Start both apps:**
       ```bash
       # Terminal 1: API
       cd el-templo-api && npm run dev

       # Terminal 2: Admin app
       cd el-templo-admin && npm run dev

       # Terminal 3: Member app
       cd el-templo-app && npm run dev
       ```

    2. **Admin Login Test:**
       - Open http://localhost:9100
       - Should redirect to /login
       - Try logging in with a member account - should see "Acceso denegado"
       - Log in with admin/coach account - should redirect to /sessions
       - Verify drawer shows navigation with pending badge

    3. **Session Generation Test:**
       - Navigate to /generate
       - Select a week (e.g., week 2)
       - Click "Generar Semana Completa"
       - Should see "Generadas X sesiones" message
       - Week summary should show pending icons

    4. **Session List Test:**
       - Navigate to /sessions
       - Should see sessions grouped by week
       - Use day tabs (Lun, Mar, etc.) to filter
       - Status filter should work
       - Level group filter should work
       - Pending sessions should sort first

    5. **Approval Workflow Test:**
       - Click approve (checkmark) on a pending session
       - Status should change to "Aprobada" (green badge)
       - Pending count in drawer should decrease
       - Click revert on approved session
       - Status should change back to "Pendiente"
       - No discard button should exist anywhere

    6. **Regeneration Deletion Test:**
       - Navigate to /generate
       - Select a week that already has sessions
       - Check "Regenerar" checkbox - label should mention permanent deletion
       - Click generate - confirmation dialog should warn about permanent deletion
       - Confirm and verify old sessions are deleted, new ones created

    7. **Bulk Approve Test:**
       - Navigate to a day with multiple pending sessions
       - Click "Aprobar todas (N)"
       - Confirm in dialog
       - All sessions should become approved

    8. **Session Detail Test:**
       - Click "Ver detalles" on any session
       - Should show session header with status
       - Should show all blocks with colored cards
       - Each block should show format, intensity, rep budget
       - Exercises should show name, contraction badge, reps
       - Toggle "Mostrar detalles del algoritmo"
       - Should show difficulty values
       - Approve/revert buttons should work from detail view (no discard)

    9. **Block Swap Test:**
       - On session detail page, click swap icon on any block
       - Swap dialog should open showing pool blocks for same route + memberLevel
       - Pool blocks should show format, exercise count, intensity, reps, source week/day
       - Blocks should be deduplicated (no two blocks with same exercise set)
       - Click a pool block to swap - confirm dialog should appear
       - After swap, block should update with new exercises/format
       - Verify pool excludes blocks from the current session

    9. **Member Visibility Test:**
       - Open http://localhost:9000 (member app)
       - Log in as a member
       - Navigate to Weekly View
       - Days with pending sessions should show "Sesion no disponible"
       - Days with approved sessions should show full session
       - Confirm members cannot see pending/discarded sessions

    10. **Low Sessions Alert Test:**
       - In admin app, approve only current week sessions
       - Warning banner should appear in drawer area
       - Generate and approve next week sessions
       - Warning should disappear

    **Expected behavior summary:**
    - Admin sees all sessions, can approve/revert (no discard)
    - Coaches can swap individual blocks from approved session pool
    - Regeneration permanently deletes old sessions (with confirmation)
    - Members only see approved sessions
    - Pending count badge updates live
    - Spanish labels throughout admin app
    - Clean neutral admin design (no navy/bronze)
    - No /discarded route or "Descartadas" nav item
  </how-to-verify>

  <resume-signal>Type "approved" if all flows work correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human confirms:
1. Admin app runs and restricts to admin/coach roles
2. Session generation creates pending sessions
3. Approval workflow updates status correctly (approve/revert only)
4. Regeneration permanently deletes old sessions with confirmation dialog
5. Block swap dialog shows deduplicated pool blocks filtered by route + memberLevel
6. Block swap replaces block content correctly
7. Member app shows only approved sessions
8. Badge and alerts update correctly
9. No references to discard/restore in UI or navigation
</verification>

<success_criteria>
- All 10 test scenarios pass (including block swap and regeneration deletion)
- No console errors in admin or member apps
- Spanish labels display correctly
- Responsive design works on mobile viewport
- Migration 0007 applied (discard columns dropped)
</success_criteria>

<output>
After completion, create `.planning/phases/14-admin-session-review-ui/14-08-SUMMARY.md`
</output>
