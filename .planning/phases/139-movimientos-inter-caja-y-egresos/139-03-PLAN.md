---
phase: 139-movimientos-inter-caja-y-egresos
plan: 03
type: execute
wave: 2
depends_on: [139-01]
files_modified:
  - el-templo-api/src/modules/finance/movement-service.ts
  - el-templo-api/src/modules/finance/types.ts
  - el-templo-api/src/modules/finance/routes.ts
  - el-templo-api/test/finance/movement-service.test.ts
autonomous: true
requirements: [MOV-01, MOV-02, MOV-03, MOV-04]

must_haves:
  truths:
    - "Registering a movement inserts 2 cash_transfer rows (outflow origen + inflow destino) linked via transaction_links, in one db.transaction, net 0"
    - "A movement between cajas of different currency is rejected (same-currency guard)"
    - "Reconciliation: when counted != expected, an adjustment row corrects origen saldo to the physical count and leaves a 'reconciliation' audit trail"
    - "Registering an expense inserts 1 cash_transfer-less expense outflow row that subtracts from its caja saldo"
    - "Voiding a movement voids BOTH legs (and the reconciliation adjustment if any) atomically; voiding an expense voids the single row"
    - "Only admin/FINANCE_VOID_ROLES may register/void movements and expenses (server-side role check, not from body)"
  artifacts:
    - path: "el-templo-api/src/modules/finance/movement-service.ts"
      provides: "MovementService facade: registerMovement, registerExpense, voidMovement, voidExpense"
      min_lines: 80
    - path: "el-templo-api/src/modules/finance/routes.ts"
      provides: "POST /movements, /expenses, /movements/:id/void, /expenses/:id/void with RBAC + country scope"
      contains: "/movements"
    - path: "el-templo-api/test/finance/movement-service.test.ts"
      provides: "MOV-01..04 integration tests"
      min_lines: 60
  key_links:
    - from: "el-templo-api/src/modules/finance/movement-service.ts"
      to: "TransactionService.create (both legs) + transaction_links"
      via: "two create() calls inside one db.transaction + tx.insert(transactionLinks)"
      pattern: "cash_transfer"
    - from: "el-templo-api/src/modules/finance/movement-service.ts"
      to: "TransactionService.voidPair"
      via: "void both legs atomically"
      pattern: "voidPair"
    - from: "el-templo-api/src/modules/finance/routes.ts"
      to: "FINANCE_VOID_ROLES gate"
      via: "server-side role check on /movements and /expenses"
      pattern: "FINANCE_VOID_ROLES"
---

<objective>
Build `MovementService` — the facade that composes the 137/138 primitives into
the two new finance operations: inter-caja movements (2-row double-entry asiento
with reconciliation + same-currency guard) and expenses (1 outflow row), plus
their orthogonal void (void-the-pair for a movement, single void for an expense).
Wire the REST routes (admin-only) and the full MOV-01..04 integration test suite.

Purpose: This is the only genuinely-new code in phase 139 (RESEARCH: "almost
entirely composition"). It delivers all four requirements end to end on top of
Plan 01's schema/type/primitive foundation and Plan 02's outflow-aware getBalance.

Output: movement-service.ts, 4 REST routes, input/output types, and integration
tests proving net-0, same-currency rejection, reconciliation trail, expense
subtraction, and atomic void-the-pair.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-movimientos-inter-caja-y-egresos/139-CONTEXT.md
@.planning/phases/139-movimientos-inter-caja-y-egresos/139-RESEARCH.md
@.planning/phases/139-movimientos-inter-caja-y-egresos/139-01-SUMMARY.md
@el-templo-api/src/modules/finance/transaction-service.ts
@el-templo-api/src/modules/finance/cash-register-service.ts

<interfaces>
<!-- Extracted from the codebase + Plan 01 outputs. -->

TransactionService (after Plan 01):
async create(input: CreateTransactionInput, recordedBy: number, tx?: TxHandle): Promise<TransactionDetail>
// memberId now number | null; cashRegisterId?: number | null override (input.cashRegisterId !== undefined
// bypasses resolveCashRegister); links: [] OK for cash_transfer/expense/adjustment.
async voidPair(ids: number[], voidedBy: number, input: VoidTransactionInput): Promise<void> // NEW in Plan 01 — one db.transaction
private \_void(...) // do not call directly from MovementService — use voidPair

CashRegisterService:
async getBalance(cashRegisterId): Promise<{ cashRegisterId, currency, firmeBalance, pendienteAmount }> // outflow-aware after Plan 02
// load each caja's currency via a direct select on schema.cashRegisters (id, currency) for the guard.

schema.transactionLinks: { transactionId, targetKind: 'transaction', targetId, allocatedAmount } — both-ways links A↔B.
schema.cashRegisters: { id, currency, ... }
auditLog.write(tx, { actorId, action: 'reconciliation', targetKind: 'transaction', targetId, payload, reason })

routes.ts wiring (lines ~55-95): transactionService + cashRegisterService + balanceService are already
constructed; the module onRequest hook authenticates + attachCountryScope. FINANCE_VOID_ROLES =
["owner","admin","gestion"]. Void route precedent (lines 206-275): RBAC check + non-owner country scope
via INNER JOIN branches on the target tx. Movements/expenses have memberId NULL but still carry branchId
(the caja's branch context) — pass a branchId for the create() rows.

RESEARCH Pattern 1 (2-row + both-ways links), Pattern 2 (expense), Pattern 3 (reconciliation as a
separate adjustment row, emitted ONLY when counted != expected; always record expected+counted in the
audit payload), Code Example "Reusing \_void for both legs".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MovementService — registerMovement + registerExpense + reconciliation + same-currency guard</name>
  <files>el-templo-api/src/modules/finance/movement-service.ts</files>
  <read_first>
    el-templo-api/src/modules/finance/transaction-service.ts (create() lines 121-301, voidPair from Plan 01),
    el-templo-api/src/modules/finance/cash-register-service.ts (getBalance, resolveCashRegister guard lines 94-100),
    el-templo-api/src/modules/finance/balance-service.ts,
    el-templo-api/src/modules/shared/audit-log.ts (write signature + 'reconciliation' action),
    el-templo-api/src/db/schema/transaction-links.ts,
    el-templo-api/src/modules/finance/types.ts (CreateTransactionInput, CashRegisterBalance),
    .planning/phases/139-movimientos-inter-caja-y-egresos/139-RESEARCH.md (Patterns 1/2/3, Recommended Project Structure),
    .planning/phases/139-movimientos-inter-caja-y-egresos/139-CONTEXT.md (D-01..D-05)
  </read_first>
  <behavior>
    - registerMovement(origenCajaId, destinoCajaId, amount, counted?, adminId, notes): inserts 2 cash_transfer rows (outflow at origen, inflow at destino), both validado, both memberId null, paymentMethod 'internal', linked both-ways via transaction_links (target_kind='transaction', allocatedAmount 0), all in ONE db.transaction. System net = 0.
    - same-currency guard: if origen.currency !== destino.currency → BadRequestError "Moneda inconsistente: ..." BEFORE any write.
    - reconciliation: capture expected = getBalance(origen).firmeBalance at the moment + counted (physical). When counted !== expected, insert ONE kind='adjustment' row at origen (direction inflow if counted>expected, outflow if counted<expected, amount = |diff|), linked to the origen movement row, and auditLog.write 'reconciliation' with payload { movementId, adjustmentId, expected, counted, diff }. When counted === expected (or counted omitted), emit NO adjustment row but still record expected (and counted if given) in the audit payload (cheap clean trail).
    - registerExpense(cajaId, amount, adminId, notes): inserts 1 kind='expense', direction='outflow', cashRegisterId=cajaId, memberId null, paymentMethod 'internal', links [] row.
  </behavior>
  <action>
    Create el-templo-api/src/modules/finance/movement-service.ts as a facade class (precedent:
    edit-service.ts domain facades) whose constructor takes (db, log, txnService: TransactionService,
    cashRegisterService: CashRegisterService) — mirror the existing finance service constructor shape.
    No new dependencies.

    registerMovement: load origen + destino caja currency via a direct select on schema.cashRegisters;
    apply the same-currency guard (mirror cash-register-service.ts:94-100 BadRequestError wording).
    Snapshot expected = (await cashRegisterService.getBalance(origenCajaId)).firmeBalance BEFORE opening
    the tx. Then this.db.transaction: call txnService.create({ memberId: null, kind: 'cash_transfer',
    direction: 'outflow', cashRegisterId: origenCajaId, currency: origen.currency, paymentMethod:
    'internal', amount, branchId: <derive>, notes, links: [] }, adminId, tx) for row A; same for row B
    with direction 'inflow' + cashRegisterId destino; insert the two both-ways transaction_links; then
    the reconciliation block per the behavior. Derive a branchId for the rows (a caja may have NULL
    branch_id — use the caja's branch_id when present, otherwise a sentinel branch consistent with how
    138 handles central/banco cajas; confirm against cash-registers schema during implementation —
    branchId on financial_transactions is NOT NULL, so pick the caja's branch or a documented default).

    registerExpense: single txnService.create({ kind: 'expense', direction: 'outflow', ... links: [] }).

    Use Pino (this.log), no console.log. catch (err: unknown) + instanceof Error where you handle errors.
    No `any`. Define a private helper for the same-currency guard if it reduces duplication (DRY).

  </action>
  <acceptance_criteria>
    - registerMovement inserts exactly 2 cash_transfer rows linked via transaction_links, net 0, in one db.transaction.
    - Cross-currency origen/destino throws BadRequestError before any row is written (no partial state).
    - counted != expected emits one adjustment row at origen with the correct direction/amount + a 'reconciliation' audit row; counted == expected emits no adjustment row.
    - registerExpense inserts 1 expense outflow row at the caja.
    - `cd el-templo-api && npx tsc --noEmit` passes (no `any`).
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && grep -q 'cash_transfer' src/modules/finance/movement-service.ts && grep -q "'reconciliation'\|\"reconciliation\"" src/modules/finance/movement-service.ts && npx tsc --noEmit</automated>
  </verify>
  <done>MovementService.registerMovement (2-row + reconciliation + same-currency guard) and registerExpense exist, atomic, no `any`, Pino logging; tsc green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: voidMovement + voidExpense (void-the-pair) + REST routes</name>
  <files>el-templo-api/src/modules/finance/movement-service.ts, el-templo-api/src/modules/finance/routes.ts, el-templo-api/src/modules/finance/types.ts</files>
  <read_first>
    el-templo-api/src/modules/finance/movement-service.ts (Task 1 output),
    el-templo-api/src/modules/finance/transaction-service.ts (voidPair from Plan 01, _void lines 348-430),
    el-templo-api/src/modules/finance/routes.ts (lines 40-95 wiring, 130-275 create/void precedent),
    el-templo-api/src/modules/shared/permissions.ts (FINANCE_VOID_ROLES line 112),
    .planning/phases/139-movimientos-inter-caja-y-egresos/139-RESEARCH.md (Pitfall 3, "Reusing _void for both legs", Security Domain),
    .planning/phases/139-movimientos-inter-caja-y-egresos/139-CONTEXT.md (D-08, D-10)
  </read_first>
  <behavior>
    - voidMovement(movementRowId, voidedBy, reason): finds the sibling leg via transaction_links (target_kind='transaction') AND any linked reconciliation adjustment row, then voids ALL of them via txnService.voidPair([...ids], voidedBy, { reason }) in one db.transaction. After void: both legs' voided_at set; both cajas' firmeBalance return to pre-movement values.
    - voidExpense(expenseRowId, voidedBy, reason): single void (no sibling) via voidPair([id], ...) or void().
    - Routes POST /movements, POST /expenses, POST /movements/:id/void, POST /expenses/:id/void: each gated to FINANCE_VOID_ROLES server-side (coach/recepcion 403); non-owner country scope enforced via the target caja/branch (mirror lines 227-258); reason mandatory + amount > 0 + valid caja ids validated via Fastify JSON schema (mirror voidTransactionSchema); cashRegisterId/memberId NEVER read from a member-keyed body path (D-03).
  </behavior>
  <action>
    Add voidMovement + voidExpense to movement-service.ts. voidMovement: in one db.transaction, select
    transaction_links WHERE transaction_id = id AND target_kind='transaction' to collect the sibling leg
    id(s) and the reconciliation adjustment id (the adjustment links to the origen row too), de-dup the
    id set, then call txnService.voidPair([id, ...siblings], voidedBy, { reason }). voidExpense calls
    voidPair([id], ...). Reject empty reason with BadRequestError (voidPair/_void already do, but validate
    early for a clean 400).

    Wire MovementService into routes.ts: construct it after transactionService/cashRegisterService (reuse
    the already-built instances). Add the 4 routes following the existing precedent: a server-side
    FINANCE_VOID_ROLES check returning 403 for non-privileged roles (NOT trusting the body), the non-owner
    country-scope guard (resolve the caja's branch country, 404 cross-country to avoid existence leak),
    and Fastify JSON schemas validating params/body (amount > 0, reason non-empty). Define
    RegisterMovementInput / RegisterExpenseInput / MovementDetail in types.ts. Pino logging on success
    (request.log), catch (err: unknown) + instanceof Error mapping to the standard error envelope used by
    the existing routes.

  </action>
  <acceptance_criteria>
    - voidMovement voids both legs + the reconciliation adjustment (if any) atomically via voidPair; voidExpense voids the single row.
    - All 4 routes reject coach/recepcion with 403 via a server-side FINANCE_VOID_ROLES check (not from body).
    - Non-owner cross-country caja access returns 404 (existence not leaked); reason mandatory; amount > 0 enforced by schema.
    - `cd el-templo-api && npx tsc --noEmit` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && grep -q 'voidPair' src/modules/finance/movement-service.ts && grep -q '/movements' src/modules/finance/routes.ts && grep -q 'FINANCE_VOID_ROLES' src/modules/finance/routes.ts && npx tsc --noEmit</automated>
  </verify>
  <done>void-the-pair atomic; 4 admin-only routes wired with RBAC + country scope + schema validation; types defined; tsc green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MOV-01..04 integration test suite</name>
  <files>el-templo-api/test/finance/movement-service.test.ts</files>
  <read_first>
    el-templo-api/test/finance/cash-register-service.test.ts (createTestApp + caja/branch setup pattern),
    el-templo-api/test/finance/transaction-service.test.ts (create()/void() test patterns),
    el-templo-api/test/helpers.ts (createTestApp, registerUser, auth),
    el-templo-api/src/modules/finance/movement-service.ts (Task 1/2 output),
    .planning/phases/139-movimientos-inter-caja-y-egresos/139-RESEARCH.md (Phase Requirements -> Test Map),
    .planning/phases/139-movimientos-inter-caja-y-egresos/139-CONTEXT.md (D-01..D-08)
  </read_first>
  <behavior>
    - MOV-01: a movement creates 2 linked cash_transfer rows, net 0; Σ firmeBalance of both same-currency cajas unchanged.
    - MOV-01 guard: a movement between an ARS caja and a EUR caja is rejected (BadRequestError), no rows written.
    - MOV-02: with counted < expected, origen firmeBalance equals counted after the movement, an adjustment row exists, and a 'reconciliation' audit_log row exists; with counted == expected, no adjustment row.
    - MOV-03: an expense reduces its caja firmeBalance by the amount; the row is kind='expense', memberId null, no links.
    - MOV-03 (D-07): creating a movement + an expense does NOT change the `balances` table (row count unchanged).
    - MOV-04: voidMovement sets voided_at on BOTH legs (and the adjustment) and restores both cajas' firmeBalance; voidExpense voids the single row and restores the caja saldo.
    - RBAC: a coach/recepcion token gets 403 on POST /movements and POST /expenses (route-level, if exercised via inject).
  </behavior>
  <action>
    Create el-templo-api/test/finance/movement-service.test.ts. Reuse the createTestApp + branch/caja
    bootstrapping from cash-register-service.test.ts (set up two same-currency cajas + one EUR caja for
    the guard test). Drive registerMovement/registerExpense/voidMovement/voidExpense directly through the
    service for the data-integrity assertions, and (optionally) exercise the routes via app.inject for the
    403 RBAC case. Assert: row counts + kinds + links via direct schema selects; firmeBalance via
    getBalance; audit trail via a select on audit_log for action='reconciliation'; balances row count
    unchanged for the no-op case. Cover every Test Map row for MOV-01..04. No `any`; catch (err: unknown).
  </action>
  <acceptance_criteria>
    - Tests cover MOV-01 (2-row net-0 + same-currency rejection), MOV-02 (reconciliation saldo+trail, and no-diff no-adjustment), MOV-03 (expense subtracts + applyDelta no-op), MOV-04 (void-the-pair + void expense restore saldo), and the 403 RBAC case.
    - `cd el-templo-api && npx tsc --noEmit` passes for the test file.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && test -f test/finance/movement-service.test.ts && grep -qi 'reconcil' test/finance/movement-service.test.ts && grep -q 'voidMovement' test/finance/movement-service.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>movement-service.test.ts covers MOV-01..04 + applyDelta no-op + RBAC 403; tsc green. (Full suite executes in CI on push to staging.)</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                           | Description                                            |
| -------------------------------------------------- | ------------------------------------------------------ |
| client → POST /movements,/expenses,/void           | untrusted role + caja ids + amount + reason cross here |
| MovementService → ledger (2 legs + reconciliation) | money integrity: all-or-nothing in one db.transaction  |
| movement rows → member `balances`                  | must NEVER touch member balances                       |

## STRIDE Threat Register

| Threat ID | Category    | Component                                         | Disposition | Mitigation Plan                                                                                                    |
| --------- | ----------- | ------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------ |
| T-139-06  | Elevation   | POST /movements,/expenses,/void                   | mitigate    | Server-side FINANCE_VOID_ROLES check (owner/admin/gestion); coach/recepcion 403; role NOT read from body           |
| T-139-07  | Tampering   | cross-country caja manipulation                   | mitigate    | Non-owner country scope via caja's branch; 404 cross-country (no existence leak), mirror routes.ts:227-258         |
| T-139-08  | Integrity   | orphan half-movement (one leg voids, other lives) | mitigate    | Both legs + reconciliation row in ONE db.transaction (registerMovement); voidPair voids all atomically (Pitfall 3) |
| T-139-09  | Integrity   | currency-mixing movement                          | mitigate    | Same-currency guard rejects before any write (BadRequestError)                                                     |
| T-139-10  | Repudiation | phantom saldo / silent reconciliation adjust      | mitigate    | Reconciliation leaves named trail (expected+counted+motivo+autor+fecha) via audit_log 'reconciliation' (D-04)      |
| T-139-11  | Tampering   | movement/expense touching member balances         | mitigate    | links:[] + tx-to-tx links only → applyDelta no-op (Plan 01 MUST-FIX B); asserted by test                           |
| T-139-SC  | Tampering   | npm/pip/cargo installs                            | accept      | Zero packages installed this phase (RESEARCH: Package Legitimacy Audit N/A)                                        |

</threat_model>

<verification>
- registerMovement = 2 linked cash_transfer rows, net 0, same-currency guard, atomic.
- Reconciliation adjustment only on diff + 'reconciliation' audit trail; expected/counted always recorded.
- registerExpense = 1 expense outflow row; applyDelta no-op proven by test.
- voidMovement voids both legs + adjustment atomically; voidExpense single void; saldos restored.
- 4 routes admin-only (FINANCE_VOID_ROLES) + country scope + schema validation.
- MOV-01..04 integration tests + RBAC 403; `npx tsc --noEmit` green.
</verification>

<success_criteria>

- [ ] MovementService facade: registerMovement, registerExpense, voidMovement, voidExpense
- [ ] 2-row double-entry, net 0, both-ways links, one db.transaction (MOV-01)
- [ ] same-currency guard rejects cross-currency before any write (MOV-01)
- [ ] reconciliation: adjustment row + 'reconciliation' audit only on diff; saldo reflects counted (MOV-02)
- [ ] expense = 1 outflow row subtracting from caja saldo; applyDelta no-op (MOV-03)
- [ ] void-the-pair atomic; void expense single; saldos restored (MOV-04)
- [ ] 4 routes admin-only + country scope + JSON schema; role server-side not from body
- [ ] MOV-01..04 integration tests + RBAC 403; `npx tsc --noEmit` green
      </success_criteria>

<output>
Create `.planning/phases/139-movimientos-inter-caja-y-egresos/139-03-SUMMARY.md` when done.
Flag for phase 141 in the SUMMARY: caja history must LEFT JOIN users to SHOW the new NULL-member
movement/expense rows (every current finance list/export INNER JOINs users and drops them).
</output>
