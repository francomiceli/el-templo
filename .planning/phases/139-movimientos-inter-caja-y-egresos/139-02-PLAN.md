---
phase: 139-movimientos-inter-caja-y-egresos
plan: 02
type: execute
wave: 2
depends_on: [139-01]
files_modified:
  - el-templo-api/src/modules/finance/cash-register-service.ts
  - el-templo-api/test/finance/cash-register-service.test.ts
autonomous: true
requirements: [MOV-01, MOV-03]

must_haves:
  truths:
    - "getBalance firm balance = opening + Σ(inflow validados) − Σ(outflow validados) since cutoff"
    - "The sum of all same-currency cajas' firmeBalance is unchanged after an inter-caja movement (net-0 invariant)"
    - "An expense outflow reduces its caja's firmeBalance by the expense amount"
    - "The 138 inflow-only balance tests stay green after the outflow extension"
  artifacts:
    - path: "el-templo-api/src/modules/finance/cash-register-service.ts"
      provides: "getBalance signed (inflow − outflow) via firmMoneyConditions()"
      contains: "outflow"
    - path: "el-templo-api/test/finance/cash-register-service.test.ts"
      provides: "net-0 invariant + expense-subtracts + refund-outflow behavior tests"
      contains: "net-0"
  key_links:
    - from: "el-templo-api/src/modules/finance/cash-register-service.ts"
      to: "financial_transactions outflow SUM"
      via: "direction='outflow' + firmMoneyConditions() + gte(transactionDate, cutoff)"
      pattern: "outflow"
---

<objective>
Complete the `// TODO 139` that phase 138 left in `getBalance`: subtract validado
outflow rows from a caja's firm balance, so expenses and the outflow leg of an
inter-caja movement actually reduce the caja's saldo. The signature is unchanged
(D-08); only the body gains the outflow term.

Purpose: Without this, an expense or a movement-outflow leg inserts a row that is
invisible to the caja saldo — the whole point of MOV-01/MOV-03 is that the saldo
reflects them. The net-0 invariant (a movement leaves the total of same-currency
cajas unchanged) is the test that proves the double-entry model is balanced.

Output: signed getBalance + integration tests (net-0 invariant, expense subtracts,
refund-outflow behavior pinned, 138 tests still green).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-movimientos-inter-caja-y-egresos/139-CONTEXT.md
@.planning/phases/139-movimientos-inter-caja-y-egresos/139-RESEARCH.md
@.planning/phases/139-movimientos-inter-caja-y-egresos/139-01-SUMMARY.md

<interfaces>
<!-- Extracted from cash-register-service.ts (lines 104-185). -->

async getBalance(cashRegisterId: number): Promise<CashRegisterBalance>
// currently: firmeBalance = caja.openingBalance + Σ(direction='inflow' + firmMoneyConditions()
// + gte(transactionDate, caja.cutoffDate)) [the firm inflow SUM, lines 143-156]
// pendienteAmount is a SEPARATE SUM (lines 160-174), born inflow/pendiente — DO NOT touch it.
// firmMoneyConditions() is imported from ./firm-money — NEVER inline `validado`/`voided_at IS NULL`.

CashRegisterBalance: { cashRegisterId, currency, firmeBalance, pendienteAmount } // signature stable

RESEARCH Pattern 4: add a symmetric outflow SUM (direction='outflow' + firmMoneyConditions()

- same cutoff gate); firmeBalance = opening + inflowTotal − outflowTotal. The outflow SUM must be
  direction-generic enough that an 'adjustment' reconciliation row (which can be inflow OR outflow)
  is summed with the correct sign by its own direction.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: getBalance outflow extension (the // TODO 139)</name>
  <files>el-templo-api/src/modules/finance/cash-register-service.ts</files>
  <read_first>
    el-templo-api/src/modules/finance/cash-register-service.ts (lines 102-185),
    el-templo-api/src/modules/finance/firm-money.ts,
    .planning/phases/139-movimientos-inter-caja-y-egresos/139-RESEARCH.md (Pattern 4, Pitfall 4),
    .planning/phases/139-movimientos-inter-caja-y-egresos/139-CONTEXT.md (D-09)
  </read_first>
  <action>
    Edit getBalance: add a second SUM mirroring the existing firm inflow SUM but with
    direction='outflow' (same firmMoneyConditions() spread, same gte(transactionDate, caja.cutoffDate)
    gate, same cash_register_id scope). Compute
    firmeBalance = caja.openingBalance + Number(inflowTotal) − Number(outflowTotal).
    Reuse firmMoneyConditions() — NEVER inline validado/voided_at (D-08/T-138-09). Remove/replace the
    `// TODO 139` comment with a note that the body is now signed. Leave the pendienteAmount SUM
    (inflow/pendiente) exactly as-is — pendientes are a separate axis and never net against firme.

    Note (D-09 resolved): the outflow term subtracts ALL validado outflow rows since cutoff with this
    cash_register_id — that intentionally includes kind='expense', the 'cash_transfer' outflow leg,
    AND any kind='refund' outflow (a cash refund genuinely leaves the caja, so subtracting it is
    correct; the cutoff_date excludes historical noise). Do NOT filter by kind in this SUM —
    direction='outflow' + firm + cutoff is the whole predicate.

  </action>
  <acceptance_criteria>
    - getBalance has two SUMs (inflow and outflow), each using firmMoneyConditions() + the cutoff gate, no inlined firm filter.
    - firmeBalance arithmetic is opening + inflow − outflow; pendienteAmount SUM is unchanged.
    - No kind filter on the outflow SUM (refund/expense/cash_transfer-out all subtract).
    - `cd el-templo-api && npx tsc --noEmit` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && grep -q '"outflow"' src/modules/finance/cash-register-service.ts && grep -q 'firmMoneyConditions' src/modules/finance/cash-register-service.ts && npx tsc --noEmit</automated>
  </verify>
  <done>getBalance subtracts validado outflows since cutoff via firmMoneyConditions(); signature unchanged; pendiente SUM untouched; tsc green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: net-0 invariant + expense-subtracts + refund-outflow tests</name>
  <files>el-templo-api/test/finance/cash-register-service.test.ts</files>
  <read_first>
    el-templo-api/test/finance/cash-register-service.test.ts (full),
    el-templo-api/src/modules/finance/cash-register-service.ts (getBalance, post-edit),
    .planning/phases/139-movimientos-inter-caja-y-egresos/139-RESEARCH.md (Pattern 4 net-0 invariant, Pitfall 4, Test Map),
    .planning/phases/139-movimientos-inter-caja-y-egresos/139-CONTEXT.md (D-09)
  </read_first>
  <behavior>
    - net-0 invariant: insert a cash_transfer outflow (−N) at origen caja + a cash_transfer inflow (+N) at destino caja (same currency, both validado). (getBalance(origen).firmeBalance + getBalance(destino).firmeBalance) is unchanged vs before the pair.
    - expense subtracts: insert a single expense outflow (amount E) at a caja → that caja's firmeBalance drops by exactly E.
    - refund-outflow behavior pinned: a kind='refund' outflow row WITH a cash_register_id since cutoff reduces the caja's firmeBalance by its amount (D-09 resolved: a cash refund leaves the caja).
    - 138 regression: the pre-existing inflow-only getBalance tests in this file still pass (the firme inflow SUM math is unchanged).
  </behavior>
  <action>
    Extend cash-register-service.test.ts with the four cases above using the existing createTestApp +
    branch/caja setup pattern already in the file. Insert ledger rows via TransactionService.create()
    with explicit cashRegisterId override, memberId: null, links: [], validationStatus default
    (validado), transactionDate >= cutoff. For net-0, assert the SUM across origen+destino firmeBalance
    equals the pre-pair SUM. For refund, create a refund outflow with a cash_register_id and assert the
    saldo dropped by the refund amount (this PINS the resolved D-09 behavior so a future change is caught).
    Do not duplicate the 138 cases — only add the new outflow-related ones.
  </action>
  <acceptance_criteria>
    - Test file contains a net-0 invariant assertion (origen+destino firmeBalance sum unchanged after a movement pair).
    - Test asserts an expense outflow reduces its caja firmeBalance by exactly the amount.
    - Test pins refund-outflow behavior (saldo reduced).
    - `cd el-templo-api && npx tsc --noEmit` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && grep -qi 'net-0\|net 0\|invariant' test/finance/cash-register-service.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Tests cover net-0 invariant, expense subtraction, refund-outflow behavior, and the 138 cases stay green; tsc compiles. (Full test run is CI on push.)</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                          |
| ---------------------------------- | ------------------------------------------------------------------------------------ |
| caja saldo derivation (getBalance) | the firm balance must reflect ALL validado outflows since cutoff, no silent omission |

## STRIDE Threat Register

| Threat ID | Category    | Component                          | Disposition | Mitigation Plan                                                                                                |
| --------- | ----------- | ---------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------- |
| T-139-04  | Integrity   | getBalance net-0                   | mitigate    | net-0 invariant test proves a movement leaves total same-currency saldo unchanged (no money created/destroyed) |
| T-139-05  | Repudiation | refund outflow suddenly subtracted | accept      | D-09 resolved: refund outflow SHOULD reduce caja; behavior pinned by an explicit test so a regression is loud  |
| T-139-SC  | Tampering   | npm/pip/cargo installs             | accept      | Zero packages installed this phase                                                                             |

</threat_model>

<verification>
- getBalance = opening + Σinflow − Σoutflow via firmMoneyConditions() + cutoff gate.
- net-0 invariant holds; expense subtracts; refund-outflow pinned; 138 tests green.
- `npx tsc --noEmit` green.
</verification>

<success_criteria>

- [ ] getBalance subtracts validado outflows since cutoff (no kind filter)
- [ ] firmMoneyConditions() reused, never inlined; pendiente SUM untouched
- [ ] net-0 invariant test passes
- [ ] expense-subtracts + refund-outflow tests pass
- [ ] 138 inflow-only tests still green; `npx tsc --noEmit` green
      </success_criteria>

<output>
Create `.planning/phases/139-movimientos-inter-caja-y-egresos/139-02-SUMMARY.md` when done.
</output>
