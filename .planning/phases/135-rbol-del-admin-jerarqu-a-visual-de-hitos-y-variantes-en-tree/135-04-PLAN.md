---
phase: 135-rbol-del-admin-jerarqu-a-visual-de-hitos-y-variantes-en-tree
plan: 04
type: execute
wave: 2
depends_on: [135-03]
files_modified:
  - el-templo-admin/src/types/tree-editor.ts
  - el-templo-admin/src/components/treemap/ExerciseFlowNode.vue
  - el-templo-admin/src/pages/TreeMapPage.vue
autonomous: false
requirements: [B-RENDER]
must_haves:
  truths:
    - "Hito nodes with variants render a chevron toggle + a '+N variantes' counter and start COLLAPSED by default"
    - "Clicking the toggle expands the hito to show its variants hanging below, each with its dl band (color by level) and dl number, ordered by dl ascending"
    - "Expansion state lives in the page (expandedMilestones), mirroring expandedRoutes — not in the node component"
    - "Front Lever route collapses from a flat ~70-node row to canonical hitos with variants tucked under them"
  artifacts:
    - path: "el-templo-admin/src/types/tree-editor.ts"
      provides: "variants: MilestoneVariant[] on TreeNode (1:1 mirror of backend)"
      contains: "variants"
    - path: "el-templo-admin/src/components/treemap/ExerciseFlowNode.vue"
      provides: "chevron toggle + '+N variantes' chip; emits toggleVariants(exerciseId)"
      contains: "toggleVariants"
    - path: "el-templo-admin/src/pages/TreeMapPage.vue"
      provides: "expandedMilestones state + toggleMilestone + variant sub-node/edge render in the chain loop with band layout accounting"
      contains: "expandedMilestones"
  key_links:
    - from: "ExerciseFlowNode chevron"
      to: "TreeMapPage.toggleMilestone"
      via: "@toggle-variants emit wired in the #node-exercise slot"
      pattern: "toggleMilestone"
    - from: "TreeMapPage chain loop"
      to: "expandedMilestones / variant sub-nodes"
      via: "variantsExpanded flag drives sub-node + edge push"
      pattern: "expandedMilestones"
---

<objective>
Block B frontend (D-09/D-10). Render the hito→variante hierarchy on the existing Vue Flow band canvas: each hito node with variants gains a chevron toggle and a "+N variantes" counter (chevron/badge patterns copied from `RouteFlowNode.vue`), starts COLLAPSED by default (D-10), and expands to show its variants hanging below — each variant drawn with its dl band (color by kairos→spartan level via `dlBand()`) and dl number, ordered by dl ascending. Expansion state lives in the page (`expandedMilestones`, mirroring `expandedRoutes`), not in the node component (D-10). This collapses the Front Lever flat ~70-node row into canonical hitos with variants tucked under them — the original problem.

Purpose: the canvas never drew the hito→variant level. With Plan 02 populating the data and Plan 03 embedding variants[] in /tree, this plan draws the collapsible hierarchy.
Output: extended types + node component + page, plus a human-verify checkpoint on the live canvas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/135-rbol-del-admin-jerarqu-a-visual-de-hitos-y-variantes-en-tree/135-CONTEXT.md
@.planning/phases/135-rbol-del-admin-jerarqu-a-visual-de-hitos-y-variantes-en-tree/135-PATTERNS.md
@.planning/phases/135-rbol-del-admin-jerarqu-a-visual-de-hitos-y-variantes-en-tree/135-03-SUMMARY.md

<interfaces>
<!-- Use these directly. The backend variants[] shape comes from Plan 03's SUMMARY. -->

MilestoneVariant (types/tree-editor.ts:141-145, already exists): { id: number; name: string; dl: number }.
TreeNode (types/tree-editor.ts:12-18) — add `variants: MilestoneVariant[]` (mirror backend EditableNode 1:1). fetchTree (useTreeEditorApi.ts:32-47) does NOT change — payload grows, axios call identical.

RouteFlowNode.vue chevron to replicate (RouteFlowNode.vue:35-39):
<q-icon :name="data.expanded ? 'unfold_less' : 'unfold_more'" size="18px" class="route-flow-node__chevron" />
RouteFlowNode.vue badge to template the counter (RouteFlowNode.vue:61-69): a clickable q-badge with tooltip — copy the structure for "+N variantes".

ExerciseFlowNode.vue:

- ExerciseNodeData (:9-21) — add `variants: MilestoneVariant[]` and `variantsExpanded: boolean` (page sets the latter, like RouteNodeData.expanded).
- defineEmits → add `toggleVariants: [exerciseId: number]`.
- dlBand()/bandTextClass() already imported (:7); stripeStyle/badgeColor pattern (:30-42) reused for each variant's band.

TreeMapPage.vue:

- expandedRoutes (:93) ref<Set<string>>; toggleRoute (:449-455) → mirror as expandedMilestones ref<Set<number>> + toggleMilestone(exerciseId). Both START EMPTY (collapsed default, D-10).
- post-refetch cleanup mirrors :153-155 (filter expandedRoutes to existing) → filter expandedMilestones to exerciseIds that still have variants.
- chain loop (:315-353): node `data` built at :321-334 → add `variants: ex.variants ?? []`, `variantsExpanded: expandedMilestones.value.has(ex.exerciseId)`. If expanded, push variant sub-nodes (position pattern { x, y: chainY0 + i\*stepY } + offset) and hito→variante edges (dashed/tenue, distinct from backbone chain; see existing XRUTA_EDGE_STYLE/AUTO_COLOR/MANUAL_COLOR).
- band height: maxChainLen / bandY (:281, :356-360) must account for expanded variant rows or bands overlap.
- slot wiring (:1337-1339 #node-exercise) → add @toggle-variants="toggleMilestone" (mirror #node-route @review at :1334-1336).
- onNodeClick (:437-447) distinguishes route(toggle) vs exercise(select) — keep variant toggle on the chevron emit (@click.stop), NOT the node click, so the side-panel selection still works.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Mirror variants[] in TreeNode + extend ExerciseFlowNode (toggle + counter) (B4/B5)</name>
  <read_first>
    - el-templo-admin/src/types/tree-editor.ts (the file being modified — TreeNode :12-18, MilestoneVariant :141-145)
    - el-templo-admin/src/components/treemap/ExerciseFlowNode.vue (the file being modified — ExerciseNodeData :9-21, dlBand import :7, stripeStyle/badgeColor :30-42)
    - el-templo-admin/src/components/treemap/RouteFlowNode.vue:35-39 and :61-69 (chevron + badge patterns to COPY, not modify)
    - el-templo-admin/src/constants/levels.ts:42-68 (dlBand/bandTextClass/DL_BANDS — reuse as-is)
    - .planning/phases/135-rbol-del-admin-jerarqu-a-visual-de-hitos-y-variantes-en-tree/135-03-SUMMARY.md (exact backend variants[] field shape)
  </read_first>
  <action>
    Extend `TreeNode` (types/tree-editor.ts:12-18) with `variants: MilestoneVariant[]` — 1:1 mirror of the backend EditableNode shape from Plan 03's SUMMARY (MilestoneVariant already exists at :141-145). In `ExerciseFlowNode.vue`: extend `ExerciseNodeData` (:9-21) with `variants: MilestoneVariant[]` and `variantsExpanded: boolean`; add `defineEmits<{ toggleVariants: [exerciseId: number] }>()`. Render a chevron (copy `unfold_more`/`unfold_less` from RouteFlowNode.vue:35-39) shown ONLY when `data.variants.length > 0`; on chevron `@click.stop` emit `toggleVariants(data.exerciseId)`. Render a "+N variantes" chip (template from RouteFlowNode.vue:61-69 badge, neutral color e.g. grey-7) shown when `data.variants.length > 0` and the node is collapsed (`!data.variantsExpanded`). Use `createLogger()` if any logging is needed — never `console.*`. No `any`. The node component holds NO expansion state — it only emits (state lives in the page, D-10).
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm exec vue-tsc --noEmit 2>&1 | grep -iE "tree-editor.ts|ExerciseFlowNode" || echo "no typecheck errors in changed files"</automated>
  </verify>
  <acceptance_criteria>
    - TreeNode has `variants: MilestoneVariant[]` mirroring the backend shape.
    - ExerciseFlowNode shows chevron + "+N variantes" chip only when variants.length > 0; chip hidden when expanded.
    - Chevron emits `toggleVariants(exerciseId)` on @click.stop; component holds no expansion state.
    - No `console.*`, no `any`; vue-tsc clean for the two files.
  </acceptance_criteria>
  <done>Hito node shows a toggle + variant counter wired to an emit; types mirror the backend.</done>
</task>

<task type="auto">
  <name>Task 2: Page expansion state + variant sub-node/edge render + band layout (B6)</name>
  <read_first>
    - el-templo-admin/src/pages/TreeMapPage.vue (the file being modified — expandedRoutes :93, post-refetch cleanup :153-155, chain loop :315-353, node data :321-334, band height maxChainLen/bandY :281 + :356-360, onNodeClick :437-447, toggleRoute :449-455, #node-exercise slot :1337-1339, #node-route @review :1334-1336)
    - el-templo-admin/src/components/treemap/ExerciseFlowNode.vue (the node it renders + the toggleVariants emit from Task 1)
    - el-templo-admin/src/constants/levels.ts:42-68 (dlBand for variant bands)
  </read_first>
  <action>
    In TreeMapPage.vue: add `const expandedMilestones = ref<Set<number>>(new Set())` (starts EMPTY = collapsed default, D-10) and `toggleMilestone(exerciseId: number)` mirroring `toggleRoute` (:449-455) — clone the Set, toggle, reassign, `rebuildGraph()`. Mirror the post-refetch cleanup (:153-155): filter `expandedMilestones` to exerciseIds that still have variants. In the chain loop (:315-353), at the node `data` build (:321-334), add `variants: ex.variants ?? []` and `variantsExpanded: expandedMilestones.value.has(ex.exerciseId)`. When a hito is expanded, push its variant sub-nodes below it (position pattern `{ x, y: chainY0 + i*stepY }` with an extra offset so they hang under the hito; reuse `dlBand()` for each variant's band color and show its dl) and hito→variante edges styled distinctly from the backbone chain (dashed/tenue — reuse an existing edge style constant like XRUTA_EDGE_STYLE/AUTO_COLOR/MANUAL_COLOR). **Account for expanded variant rows in the band-height calc** (`maxChainLen`/`bandY` at :281, :356-360) so bands do not overlap when milestones are expanded — extend the same mechanism that already adapts height to the longest expanded chain. Wire the slot (:1337-1339 #node-exercise) with `@toggle-variants="toggleMilestone"` (mirror #node-route @review at :1334-1336). Keep the variant toggle on the chevron emit, NOT on `onNodeClick` (:437-447) — the node click must still select for the side panel. No `console.*`, no `any`.
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm exec vue-tsc --noEmit 2>&1 | grep -iE "TreeMapPage" || echo "no typecheck errors in TreeMapPage"</automated>
  </verify>
  <acceptance_criteria>
    - `expandedMilestones` ref (Set<number>) starts empty; `toggleMilestone` mirrors toggleRoute and calls rebuildGraph.
    - Post-refetch, expandedMilestones is filtered to surviving variant-bearing exerciseIds.
    - Chain loop sets variants + variantsExpanded on node data; expanded hitos push variant sub-nodes (dl band via dlBand) + distinct hito→variante edges.
    - Band-height calc accounts for expanded variant rows (no overlap).
    - @toggle-variants wired to toggleMilestone on the #node-exercise slot; node click still selects for the side panel.
    - No `console.*`, no `any`; vue-tsc clean for TreeMapPage.vue.
  </acceptance_criteria>
  <done>The canvas renders collapsed hitos with a counter; expanding shows banded variants hanging below without band overlap.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The hierarchical hito→variante render on the admin /tree-map canvas: hito nodes with variants now show a chevron + "+N variantes" chip and start collapsed; clicking the chevron expands variants below with their dl bands.</what-built>
  <how-to-verify>
    1. Run the admin app locally (`cd el-templo-admin && pnpm dev`) and open `/tree-map`.
    2. Find the Front Lever route (the witness case that used to render as a flat ~70-node row). Confirm it now shows canonical hito nodes, each collapsed by default, with a "+N variantes" chip where it has variants.
    3. Click a hito's chevron — its variants should hang below, each with the correct dl band color (kairos→spartan) and dl number, ordered by dl ascending. The band should not overlap neighboring bands when expanded.
    4. Click the chevron again to collapse — the chip returns, the canvas is clean.
    5. Click the node body (not the chevron) — the side panel selection should still work (unaffected by the toggle).
    6. Confirm variant-less hitos show no chevron/chip.
  </how-to-verify>
  <resume-signal>Type "approved" if the hierarchy renders correctly, or describe the visual issues.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                                                                                                                                        |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| admin browser → admin app | Web-only admin/coach UI consuming the already-authorized /tree payload. No new network surface; purely client-side rendering of data the endpoint already returns. |

## STRIDE Threat Register

| Threat ID | Category               | Component                         | Disposition | Mitigation Plan                                                                                                               |
| --------- | ---------------------- | --------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------- |
| T-135-10  | Information Disclosure | variant render                    | accept      | Renders only data the /tree endpoint already returns to the same authorized admin role; no new data fetched, no new endpoint. |
| T-135-11  | Tampering              | client state (expandedMilestones) | accept      | Pure UI expansion state; no mutation of server data from this plan (read-only render).                                        |
| T-135-SC  | Tampering              | npm/pip/cargo installs            | mitigate    | No new packages (reuses existing q-icon/q-badge/dlBand). CLAUDE.md no-auto-install rule honored.                              |

</threat_model>

<verification>
- `vue-tsc --noEmit` clean for tree-editor.ts, ExerciseFlowNode.vue, TreeMapPage.vue.
- No `console.*`, no `any` in changed files.
- Human-verify checkpoint confirms the Front Lever route collapses correctly and expansion shows banded variants without overlap.
</verification>

<success_criteria>

- B-RENDER: hito nodes collapsible with "+N variantes" counter, collapsed by default (D-10), variants banded + dl-ordered on expand (D-09).
- Expansion state in the page (expandedMilestones), not the component (D-10).
- Side-panel selection unaffected; variant-less hitos show no toggle.
  </success_criteria>

<output>
Create `.planning/phases/135-rbol-del-admin-jerarqu-a-visual-de-hitos-y-variantes-en-tree/135-04-SUMMARY.md` when done.
</output>
