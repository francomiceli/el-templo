---
phase: 135-rbol-del-admin-jerarqu-a-visual-de-hitos-y-variantes-en-tree
plan: 02
type: execute
wave: 2
depends_on: [135-01]
files_modified:
  - el-templo-api/src/db/migrations/0146_populate_milestone_exercise_id.sql
autonomous: false
requirements: [A-POBLADO]
must_haves:
  truths:
    - "A hand-written DATA migration 0146 ships the local --apply result to prod via the pipeline (UPDATE statements, no DDL)"
    - "The keying strategy (by exercise_id vs by natural key) is chosen by an explicit local↔prod catalog-parity check (D-07), not assumed"
    - "The migration is reversible: documented rollback SQL sets milestone_exercise_id = NULL"
    - "No `;` appears inside any SQL comment (runner splits by `;` before stripping `--`)"
  artifacts:
    - path: "el-templo-api/src/db/migrations/0146_populate_milestone_exercise_id.sql"
      provides: "DATA migration: UPDATE exercises SET milestone_exercise_id from the local apply result, keyed per the verified strategy, with comment-safety header and rollback doc"
      contains: "milestone_exercise_id"
  key_links:
    - from: "0146 migration"
      to: "exercises.milestone_exercise_id"
      via: "UPDATE statements generated from local apply (Plan 01)"
      pattern: "UPDATE exercises"
    - from: "0146 migration"
      to: "_migrations table / pipeline"
      via: "pnpm db:migrate runner (never drizzle-kit migrate)"
      pattern: "milestone"
---

<objective>
Block A → prod. Ship the local `--apply` result (Plan 01) to production as a hand-written DATA migration `0146` (D-06): a set of `UPDATE exercises SET milestone_exercise_id = ...` statements, applied via the project pipeline (`pnpm db:migrate`, `_migrations` table is source of truth — never CLI on prod, never seed re-run). The keying strategy is decided by an explicit local↔prod catalog-parity check (D-07): identical autoincrement IDs → key by `exercise_id`; divergent → fallback to natural key (exercise name + route JOIN). Header follows the comment-safety discipline of 0145; the rollback command is documented alongside (D-05).

Purpose: prod data changes go through migrations + pipeline + staging-first STRICT. This migration is the auditable, reversible vehicle for the milestone population.
Output: `0146_populate_milestone_exercise_id.sql` generated from the local post-apply state, with a documented rollback and a parity-justified keying choice.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/135-rbol-del-admin-jerarqu-a-visual-de-hitos-y-variantes-en-tree/135-CONTEXT.md
@.planning/phases/135-rbol-del-admin-jerarqu-a-visual-de-hitos-y-variantes-en-tree/135-PATTERNS.md
@.planning/phases/135-rbol-del-admin-jerarqu-a-visual-de-hitos-y-variantes-en-tree/135-01-SUMMARY.md

<interfaces>
<!-- Migration discipline (CLAUDE.md + user memory [[feedback_no_semicolon_in_sql_comments]]) -->

Header to copy VERBATIM from 0145:38-43 (comment-safety + hand-written note). NUNCA un `;` dentro de un comentario.

Latest migration number = 0145 (verified: 0145_milestone_exercise_id.sql is the tail). New = 0146.
Confirm no other branch took 0146 before generating.

Keying strategies (D-06/D-07):

- Parity holds (same autoincrement IDs local↔prod):
  UPDATE exercises SET milestone_exercise_id = <hito_id> WHERE id = <variante_id>;
- Parity fails (IDs diverge) — natural-key fallback (analog 0089 / 0064 / 0067 key by name):
  UPDATE exercises v
  JOIN exercises h ON h.exercise = '<NOMBRE HITO>' AND h.route = '<RUTA>'
  SET v.milestone_exercise_id = h.id
  WHERE v.exercise = '<NOMBRE VARIANTE>' AND v.route = '<RUTA>';
  (JOIN-by-name assumes (exercise, route) uniqueness — VERIFY before emitting or the UPDATE over-matches.)

Rollback doc (D-05), placed in a comment block / adjacent note (NOT executed by the runner):
UPDATE exercises SET milestone_exercise_id = NULL WHERE milestone_exercise_id IS NOT NULL;
(optionally scoped to heuristic-accepted-not-manually-corrected rows — D-05 leaves scoped version optional.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify local↔prod exercise-catalog parity and choose keying strategy (D-07)</name>
  <read_first>
    - el-templo-api/src/db/migrations/0145_milestone_exercise_id.sql (header discipline + the column DDL)
    - el-templo-api/src/db/migrations/0089_tren_inferior_replicate_weeks_11_12.sql (natural-key DATA-migration analog)
    - el-templo-api/src/db/schema/exercises.ts (exercises columns: id, exercise (name), route, milestone_exercise_id)
    - el-templo-api/.planning/phases/135-rbol-del-admin-jerarqu-a-visual-de-hitos-y-variantes-en-tree/135-01-SUMMARY.md (the local assignment set produced by --apply)
  </read_first>
  <action>
    Determine whether the exercise catalog has identical autoincrement IDs in local and prod (D-07). Run a parity comparison: produce a fingerprint of the catalog (e.g. `SELECT id, exercise, route FROM exercises ORDER BY id` from the local DB) and compare against prod's catalog. Since prod access is via the pipeline/staging-first (never direct CLI on prod — D-06), use the available source of truth: compare the local catalog against the seed/migration history that produced prod, OR if a read-only parity snapshot is available, diff the (id, exercise, route) tuples. ALSO verify `(exercise, route)` uniqueness in the catalog (required if the fallback natural-key JOIN is used — a non-unique pair would over-match). Record the decision in the SUMMARY: if every (id ↔ exercise+route) tuple matches between local and prod → choose **key-by-exercise_id**; if any divergence → choose **natural-key fallback** and confirm (exercise, route) uniqueness. This task PRODUCES the keying decision that Task 2 consumes — do not emit the migration until the strategy is fixed.
  </action>
  <verify>
    <automated>cd el-templo-api && node -e "const {execSync}=require('child_process'); console.log('parity check is a documented decision task; verify the chosen strategy is recorded in SUMMARY')" && grep -ci "uniqueness\|parity\|exercise_id\|natural" /home/franco/projects/el-templo/.planning/phases/135-rbol-del-admin-jerarqu-a-visual-de-hitos-y-variantes-en-tree/135-02-SUMMARY.md 2>/dev/null || echo "record decision in SUMMARY"</automated>
  </verify>
  <acceptance_criteria>
    - A documented parity finding exists: either "catalog identical local↔prod → key by exercise_id" or "catalog diverges → natural-key fallback".
    - If natural-key fallback is chosen, `(exercise, route)` uniqueness is explicitly verified (no duplicate pairs).
    - The chosen strategy is recorded so Task 2 emits the correct statement form.
  </acceptance_criteria>
  <done>Keying strategy chosen and justified by an explicit catalog-parity check; uniqueness verified if natural-key path.</done>
</task>

<task type="auto">
  <name>Task 2: Emit 0146 DATA migration from local apply result (D-06/D-08) + document rollback (D-05)</name>
  <read_first>
    - el-templo-api/src/db/migrations/0145_milestone_exercise_id.sql (copy the header block verbatim)
    - el-templo-api/src/db/migrations/0089_tren_inferior_replicate_weeks_11_12.sql (statement/comment shape for keyed DATA migration)
    - el-templo-api/.planning/phases/135-rbol-del-admin-jerarqu-a-visual-de-hitos-y-variantes-en-tree/135-01-SUMMARY.md (assignment set exercise_id → milestone_exercise_id)
    - el-templo-api/src/db/migrations/0146_populate_milestone_exercise_id.sql (the file being created)
  </read_first>
  <action>
    Create `el-templo-api/src/db/migrations/0146_populate_milestone_exercise_id.sql`. Copy the comment-safety + hand-written header VERBATIM from 0145:38-43. GENERATE the `UPDATE exercises SET milestone_exercise_id = ...` statements from the local post-apply assignment set (captured in Plan 01's SUMMARY — exercise_id → milestone_exercise_id for every variante; hitos stay NULL so emit nothing for them). Use the keying strategy chosen in Task 1: by `id` if parity holds, else the natural-key JOIN form (per the <interfaces> block). One statement per terminator, each `;` on its own line. **NEVER place a `;` inside any `--` comment** — hard runner rule (the runner splits by `;` before stripping comments; a comment-semicolon breaks the entire migration). Prefer EMITTING the statements programmatically from the apply state (the apply function exported in Plan 01) rather than hand-typing 70 lines, to avoid transcription errors. After the executable statements, add a documented ROLLBACK note (D-05) in a comment block — the `UPDATE exercises SET milestone_exercise_id = NULL WHERE milestone_exercise_id IS NOT NULL;` form (or the scoped variant) — clearly marked as NOT auto-run. Do NOT use `drizzle-kit generate`/`migrate` for this (DML, not schema; the `_migrations` table is the source of truth and the migration ships via `pnpm db:migrate` in the pipeline). Confirm 0146 is the next free number.
  </action>
  <verify>
    <automated>F=/home/franco/projects/el-templo/el-templo-api/src/db/migrations/0146_populate_milestone_exercise_id.sql; test -f "$F" && ! grep -n '^--.*;' "$F" && grep -cq "UPDATE exercises" "$F" && echo "OK: file exists, no semicolons in line-comments, has UPDATEs" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File 0146_populate_milestone_exercise_id.sql exists with the 0145 comment-safety header.
    - Contains UPDATE exercises ... milestone_exercise_id statements generated from the local apply result (one per variante).
    - NO `;` inside any `--` comment line (grep `'^--.*;'` returns nothing).
    - One statement per terminator; each `;` on its own line.
    - Rollback SQL is documented in a clearly-marked, non-executed comment block (D-05).
    - Keying form matches Task 1's decision (id vs natural-key JOIN).
  </acceptance_criteria>
  <done>0146 migration emitted from the local apply result with verified keying, comment-safe, reversible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The 0146 DATA migration generated from the local --apply result: UPDATE statements that populate exercises.milestone_exercise_id, keyed per the verified parity strategy, with a documented rollback.</what-built>
  <how-to-verify>
    1. Open `el-templo-api/src/db/migrations/0146_populate_milestone_exercise_id.sql` and review the UPDATE statements against the dry-run plan you approved in Plan 01 (especially the Front Lever / TTB witness route — its variantes should point at the correct hito).
    2. Confirm the keying strategy matches the parity finding recorded in 135-02-SUMMARY.md (by exercise_id, or natural-key JOIN if catalogs diverge).
    3. Confirm there is no `;` inside any comment line and the rollback note is present.
    4. This migration touches PROD data on the next staging→pipeline run. Approve only if the assignment plan is correct (it is reversible via the documented rollback, but staging-first STRICT applies — it ships via the pipeline, never CLI on prod).
  </how-to-verify>
  <resume-signal>Type "approved" to accept the migration, or describe corrections needed.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary            | Description                                                                                                                                                                                                      |
| ------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| migration → prod DB | The 0146 migration mutates prod exercises rows via the pipeline. A wrong key or a comment-semicolon would corrupt or abort the migration; the member tree backbone depends on milestone_exercise_id correctness. |

## STRIDE Threat Register

| Threat ID | Category          | Component                | Disposition | Mitigation Plan                                                                                                                                                                              |
| --------- | ----------------- | ------------------------ | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-135-04  | Tampering         | 0146 keying strategy     | mitigate    | D-07 parity check (Task 1) selects key-by-id only when catalogs provably match; natural-key fallback otherwise (survives ID drift). Human checkpoint reviews the assignment before it ships. |
| T-135-05  | Denial of Service | runner abort on bad SQL  | mitigate    | Comment-safety header + grep gate proving no `;` inside `--` comments; one statement per terminator. Prevents the runner's split-by-`;`-before-strip from breaking the whole migration.      |
| T-135-06  | Repudiation       | irreversible prod change | mitigate    | D-05 rollback SQL documented adjacent to the migration; ships via pipeline + \_migrations tracking (auditable diff), never direct CLI on prod (D-06, staging-first STRICT).                  |
| T-135-SC  | Tampering         | npm/pip/cargo installs   | mitigate    | No package installs in this plan.                                                                                                                                                            |

</threat_model>

<verification>
- 0146 file exists, header copied from 0145, no `;` in comments, one statement per terminator.
- UPDATEs generated from the local apply result; keying matches the parity decision.
- Rollback documented; migration ships via pipeline (`pnpm db:migrate`), never `drizzle-kit migrate`.
- Human checkpoint approves the assignment plan before it touches prod.
</verification>

<success_criteria>

- D-06: deterministic DATA migration shipping the local apply result via the pipeline.
- D-07: keying strategy chosen by explicit parity check (uniqueness verified if natural-key).
- D-08: comment-safe, hand-written, one-statement-per-terminator.
- D-05: rollback documented.
  </success_criteria>

<output>
Create `.planning/phases/135-rbol-del-admin-jerarqu-a-visual-de-hitos-y-variantes-en-tree/135-02-SUMMARY.md`. Record: the parity finding + chosen keying strategy, the number of variante UPDATEs emitted, and the exact rollback command.
</output>
