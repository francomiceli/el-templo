---
phase: 135-rbol-del-admin-jerarqu-a-visual-de-hitos-y-variantes-en-tree
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/bootstrap-milestones.ts
  - el-templo-api/test/exercises/milestone-apply.test.ts
autonomous: true
requirements: [A-POBLADO, A-CORRECCION]
must_haves:
  truths:
    - "`bootstrap-milestones.ts --dry-run` prints the full hito→variantes plan (confidence/movementToken/stepRank per row) for every long route WITHOUT writing to the DB"
    - "The dry-run output surfaces the Front Lever / TTB witness route so the user can review before applying"
    - "The dry-run output ALSO reports the count of pending DIMENSION proposals that `--apply` would accept as a side-effect (acceptMilestoneReview accepts a pending dimension proposal for the same exercise in the same tx, service.ts:1027-1049), so the operator sees it before approving"
    - "`bootstrap-milestones.ts --apply` writes exercises.milestone_exercise_id (NULL for hitos, hito id for variantes) by accepting pending proposals transactionally"
    - "`--apply` is milestone-only BY CONTRACT: it asserts zero pending DIMENSION proposals exist (v5.1 sequence: 124+133 already settled dimensions) and ABORTS with a clear message if any remain, so it never silently mass-mutates habilidad/step or prunes off-backbone nodes"
    - "Apply processes all role='hito' proposals before role='variante' proposals"
    - "Re-running --apply only touches status='pending' proposals; accepted/rejected proposals are never overwritten (idempotency / coach corrections preserved)"
  artifacts:
    - path: "el-templo-api/bootstrap-milestones.ts"
      provides: "--dry-run plan printer (milestone plan + pending-dimension side-effect count) + --apply transactional bulk accept guarded to milestone-only"
      contains: "dry-run"
    - path: "el-templo-api/test/exercises/milestone-apply.test.ts"
      provides: "Integration test: apply writes truth, idempotent re-run, accepted/rejected untouched, aborts on pending dimension proposals"
      contains: "milestone_exercise_id"
  key_links:
    - from: "bootstrap-milestones.ts --apply"
      to: "TreeEditorService.acceptMilestoneReview"
      via: "per-pending-proposal call inside per-row isolated transaction"
      pattern: "acceptMilestoneReview"
    - from: "bootstrap-milestones.ts --apply"
      to: "exercise_milestone_proposals (status='pending')"
      via: "SELECT only pending proposals to iterate"
      pattern: "status.*pending"
    - from: "bootstrap-milestones.ts --apply pre-apply guard"
      to: "exercise_dimension_proposals (status='pending')"
      via: "count + assert-and-abort before any accept (milestone-only contract)"
      pattern: "exercise_dimension_proposals"
---

<objective>
Block A — Data population engine. Extend the existing `bootstrap-milestones.ts` CLI with a READ-ONLY `--dry-run` mode (D-01) that prints the full hito→variantes plan for user review (Front Lever witness case), and an `--apply` mode (D-02/D-03) that persists the heuristic by transactionally accepting all `pending` milestone proposals — reusing `TreeEditorService.acceptMilestoneReview` (the only path that writes `milestone_exercise_id`). Apply is idempotent (D-04): re-runs touch only `pending` proposals, leaving coach-corrected `accepted`/`rejected` rows intact.

Purpose: Today `milestone_exercise_id` is NULL on every exercise, so `backboneNodeConditions()` treats all ~70 exercises as hitos and the Front Lever route renders flat. Populating the column collapses the row. This plan builds the population mechanism (the prod migration that ships the result is Plan 02).
Output: Extended CLI with dry-run + apply, plus an integration test proving truth-write + idempotency.

**Coupling note (acceptMilestoneReview side-effect).** `acceptMilestoneReview` (service.ts:1027-1049) selects and accepts ANY pending DIMENSION proposal for the same exercise inside the same `acceptInTransaction`, writing `habilidad`/`step` and potentially pruning the node off-backbone (WR-01 path). The bulk `--apply` reuses this accept, so if any dimension proposals are still `pending`, `--apply` would silently mass-accept dimensions too — a backbone mutation beyond `milestone_exercise_id` that the D-01 dry-run printer (the human safety gate) would NOT show. This plan neutralizes that risk two ways: (1) `--dry-run` reports the pending-dimension count so the operator sees it before approving; (2) `--apply` is milestone-only BY CONTRACT — it asserts zero pending dimension proposals (the v5.1 sequence guarantees 124+133 already settled them) and ABORTS with a clear message otherwise (aligns with D-01 "no poblar masivamente mal").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/135-rbol-del-admin-jerarqu-a-visual-de-hitos-y-variantes-en-tree/135-CONTEXT.md
@.planning/phases/135-rbol-del-admin-jerarqu-a-visual-de-hitos-y-variantes-en-tree/135-PATTERNS.md

<interfaces>
<!-- The executor uses these directly — no codebase exploration needed beyond <read_first>. -->

bootstrap-milestones.ts (existing, to extend):

- `export async function runBootstrapMilestones<...>(db)` — the proposal-writer. READ catalog → `proposeMilestones(catalog)` (pure) → idempotent INSERT into exercise_milestone_proposals (status='pending', engine='milestone-heuristic-v1'). console.log is documented-legitimate here (header :27).
- entrypoint guard at :209 — `if (process.argv[1] && process.argv[1].endsWith("bootstrap-milestones.ts"))`. Parse `process.argv` flags here.
- narrowers: `readCatalogRows` (:145), `readExerciseIdSet` (:183) — reuse / mirror for any new raw SELECT, no `any`.

proposeMilestones() (milestone-heuristic.ts, pure, already invoked at bootstrap :103):

- returns one proposal per exercise: `{ exerciseId, proposedMilestoneExerciseId (NULL=hito / NOT NULL=variante), movementToken, stepRank, confidence (80|60|40) }`.

TreeEditorService.acceptMilestoneReview (service.ts:982-1151) — REUSE, do not reimplement:

```typescript
acceptMilestoneReview(input: AcceptMilestoneReviewInput): Promise<MutationResult>
// input: { exerciseId, role: 'hito' } | { exerciseId, role: 'variante', milestoneExerciseId: number }
// In a db.transaction: sets exercises.milestone_exercise_id, prunes degraded variant edges,
// flips the matching PENDING milestone proposal to 'accepted' (WHERE status='pending', service.ts:1139-1147).
// SIDE-EFFECT (service.ts:1027-1049): in the SAME tx it ALSO selects the first pending
// exercise_dimension_proposals row for the same exercise and accepts it (acceptInTransaction),
// writing habilidad/step and possibly pruning the node off-backbone (WR-01). The bulk --apply
// must guarantee zero pending dimension proposals BEFORE calling this (milestone-only contract).
```

exercise_dimension_proposals (schema/exercises.ts) — the side-effect source: columns include exercise_id, status ('pending'|'accepted'|'rejected'). The pre-apply guard counts WHERE status='pending'.

ProposalService.bulkAccept (proposal-service.ts:275-298) — per-row isolation pattern to imitate:
each accept in its own try/catch; on error log.warn + continue; return acceptedCount.

Proposal→input mapping (proposal carries proposed_milestone_exercise_id):

- proposed_milestone_exercise_id IS NULL → { exerciseId, role: 'hito' }
- proposed_milestone_exercise_id = H → { exerciseId, role: 'variante', milestoneExerciseId: H }
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Add --dry-run plan printer to bootstrap-milestones.ts (D-01)</name>
  <read_first>
    - el-templo-api/bootstrap-milestones.ts (the file being modified — full read: structure, runBootstrapMilestones, narrowers, entrypoint guard at :209, header :27)
    - el-templo-api/src/modules/exercises/milestone-heuristic.ts (proposeMilestones output shape — read the exported function + return type)
    - el-templo-api/src/modules/tree-editor/service.ts:1027-1049 (the dimension-accept side-effect inside acceptMilestoneReview — to understand what the dry-run must surface)
    - el-templo-api/src/db/schema/exercises.ts (exercise_dimension_proposals columns: exercise_id, status)
  </read_first>
  <action>
    In the entrypoint guard (bootstrap-milestones.ts:209), parse `process.argv` for `--dry-run`. Implement a READ-ONLY dry-run path: read the catalog rows (reuse the existing catalog read at :66 / `readCatalogRows` :145), run `proposeMilestones(catalog)` (the same pure call already at :103), and group the proposals by route. For each route, print the hito (proposedMilestoneExerciseId IS NULL) followed by its variantes (proposedMilestoneExerciseId = hito id), each line showing exercise name, movementToken, stepRank, and confidence. Filter/highlight the Front Lever / TTB witness route per D-01 (CONTEXT specifics). **ALSO read and print the count of pending DIMENSION proposals** (`SELECT COUNT(*) FROM exercise_dimension_proposals WHERE status = 'pending'` — write a narrower in the style of `readExerciseIdSet` :183, no `any`): label it clearly as the number of dimension proposals `--apply` would accept as a side-effect of `acceptMilestoneReview` (service.ts:1027-1049), warning the operator that a non-zero count means `--apply` would mutate habilidad/step / prune off-backbone beyond milestone assignment. The dry-run path MUST NOT open any write transaction and MUST NOT call acceptMilestoneReview — it only reads (catalog + the dimension count) and formats. Use the CLI's documented-legitimate `console.log` (header :27); do NOT introduce `request.log`/`app.log` (this is a standalone CLI). No `any` — reuse `readCatalogRows` narrowing; if a new SELECT is needed, write a narrower in the style of `readExerciseIdSet` (:183). Default behavior (no flag) MUST remain the existing proposal-writer (runBootstrapMilestones) unchanged.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit 2>&1 | grep -i "bootstrap-milestones" || echo "no bootstrap typecheck errors"</automated>
  </verify>
  <acceptance_criteria>
    - `bootstrap-milestones.ts --dry-run` reads the catalog, runs `proposeMilestones`, and prints a grouped-by-route hito→variantes plan with movementToken/stepRank/confidence per row.
    - The Front Lever / TTB route is surfaced in the output (filtered or highlighted).
    - The dry-run output ALSO surfaces the count of pending DIMENSION proposals that `--apply` would accept as a side-effect, labeled as such (grep confirms the dry-run branch reads `exercise_dimension_proposals` with `status = 'pending'` and prints a count). On the expected v5.1 state this count is 0.
    - Dry-run opens NO write transaction and calls NO accept method (grep the dry-run branch confirms no `acceptMilestoneReview`/`transaction` inside it).
    - No new `any` types; narrowers used for any raw SELECT.
    - `tsc --noEmit` reports no errors in bootstrap-milestones.ts.
  </acceptance_criteria>
  <done>Running with --dry-run prints the full milestone plan (Front Lever visible) AND the pending-dimension side-effect count, and writes nothing to the DB.</done>
</task>

<task type="auto">
  <name>Task 2: Add --apply transactional bulk accept, milestone-only by contract (D-02/D-03/D-04)</name>
  <read_first>
    - el-templo-api/bootstrap-milestones.ts (the file being modified)
    - el-templo-api/src/modules/tree-editor/service.ts:982-1151 (acceptMilestoneReview — the reused truth-writer; :1027-1049 the pending-DIMENSION-proposal accept side-effect; :1139-1147 the pending-milestone flip)
    - el-templo-api/src/modules/admin/proposal-service.ts:275-298 (bulkAccept — per-row isolation pattern)
    - el-templo-api/src/db/schema/exercises.ts (exercise_milestone_proposals + exercise_dimension_proposals columns: exercise_id, proposed_milestone_exercise_id, status, engine)
  </read_first>
  <action>
    Add an `--apply` branch to the entrypoint guard. **FIRST, before any accept, enforce the milestone-only contract:** count pending DIMENSION proposals (`SELECT COUNT(*) FROM exercise_dimension_proposals WHERE status = 'pending'` via a narrower, no `any`). If the count is > 0, ABORT — print a clear error explaining that `acceptMilestoneReview` (service.ts:1027-1049) would accept these dimension proposals as a side-effect (mutating habilidad/step and possibly pruning nodes off-backbone), that the v5.1 sequence (phases 124+133) is supposed to have settled all dimensions first, and that the operator must resolve/clear pending dimension proposals (or run the dimension review) before re-running `--apply`. Do NOT proceed to accept any milestone proposal when the guard trips (assert-and-abort, per D-01 "no poblar masivamente mal"; this keeps `--apply` milestone-only by contract). Then, with the guard passed, SELECT only `pending` MILESTONE proposals: `SELECT exercise_id, proposed_milestone_exercise_id FROM exercise_milestone_proposals WHERE status = 'pending'` (narrower in the style of `readExerciseIdSet` :183 — no `any`). Instantiate `TreeEditorService` (constructor DI, same as production). **Process all `role='hito'` rows (proposed_milestone_exercise_id IS NULL) FIRST, then all `role='variante'` rows** — this ordering is CRITICAL (PATTERNS.md A1): acceptMilestoneReview with role='variante' validates the destination hito is not itself a variante (service.ts:1073-1077), which only holds if every hito was accepted first. For each row call `acceptMilestoneReview` with the mapped input (NULL→{exerciseId, role:'hito'}; H→{exerciseId, role:'variante', milestoneExerciseId:H}). Wrap each call in its own try/catch imitating `bulkAccept` (proposal-service.ts:280-296): on error `console.warn`/log skip + continue, increment an acceptedCount; one bad proposal must NOT rollback the others (acceptMilestoneReview already wraps each in its own db.transaction). Idempotency (D-04) is satisfied because the SELECT filters to `pending` and acceptMilestoneReview only flips `pending` proposals — accepted/rejected (coach-corrected) rows are never re-touched. Print a summary (counts of hitos accepted, variantes accepted, skipped). Do NOT write `milestone_exercise_id` directly and do NOT add a `milestone_source` column (D-03 anti-patterns).
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit 2>&1 | grep -i "bootstrap-milestones" || echo "no bootstrap typecheck errors"</automated>
  </verify>
  <acceptance_criteria>
    - `--apply` FIRST counts pending DIMENSION proposals and ABORTS (no milestone accept performed) with a clear message if any exist (grep confirms a guard reading `exercise_dimension_proposals` with `status = 'pending'` that returns/exits before the accept loop).
    - With the guard passed, `--apply` selects ONLY `status='pending'` MILESTONE proposals (grep confirms `pending` in the WHERE on exercise_milestone_proposals).
    - role='hito' rows are processed before role='variante' rows (ordering visible in code: partition then iterate hitos then variantes).
    - Each accept is `acceptMilestoneReview` (no direct UPDATE of `milestone_exercise_id`; grep confirms no `set({ milestoneExerciseId` in bootstrap).
    - Per-row try/catch with log-and-continue (a failing proposal does not abort the run).
    - No `milestone_source` column introduced anywhere.
    - `tsc --noEmit` clean for bootstrap-milestones.ts.
  </acceptance_criteria>
  <done>--apply aborts if any pending dimension proposals remain (milestone-only contract); otherwise populates milestone_exercise_id via accepted proposals; hitos first then variantes; re-run touches only pending rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration test — apply truth-write + idempotency + milestone-only guard (A3)</name>
  <read_first>
    - el-templo-api/test/exercises/proposal-review.test.ts (accept-in-tx / idempotency / member→403 pattern)
    - el-templo-api/test/exercises/bootstrap-dimensions.test.ts (driver pattern: import the runner, run against per-worker DB, no process spawn)
    - el-templo-api/test/tree-editor/tree-editor.test.ts:36-155 (createRoute/createExercise seed helpers; createExercise already supports milestoneExerciseId since 133)
    - el-templo-api/bootstrap-milestones.ts (the apply function under test, exported)
  </read_first>
  <behavior>
    - After seeding a backbone route with several exercises and running the proposal-writer (all milestone proposals 'pending', and NO pending dimension proposals), calling --apply writes exercises.milestone_exercise_id: NULL for hitos, the hito id for variantes (matching the proposals).
    - All applied milestone proposals flip to status='accepted'.
    - Re-running --apply is a no-op for already-accepted proposals: counts of accepted-this-run = 0, and no milestone_exercise_id changes.
    - A milestone proposal pre-set to status='accepted' or 'rejected' (simulating a coach correction) is NOT re-touched by --apply: its milestone_exercise_id and status are unchanged after a run.
    - Milestone-only guard: when at least one pending exercise_dimension_proposals row exists for a seeded exercise, --apply ABORTS without accepting any milestone proposal (milestone_exercise_id unchanged, milestone proposals still 'pending'), and signals the abort (throws / non-zero result / documented sentinel).
  </behavior>
  <action>
    Create `el-templo-api/test/exercises/milestone-apply.test.ts`. Import `runBootstrapMilestones` (already exported) and the new apply function (export it from bootstrap-milestones.ts so the test can call it without spawning a process — mirror how bootstrap-dimensions.test.ts imports its runner). Seed via the `createRoute`/`createExercise` helpers from tree-editor.test.ts. Assert the five behaviors above against the per-worker MySQL test DB. For the coach-correction case, manually UPDATE one milestone proposal to 'rejected' (or 'accepted') with a known milestone_exercise_id before the apply, then assert it is untouched after. For the milestone-only guard case, INSERT a pending exercise_dimension_proposals row for a seeded exercise and assert --apply aborts and leaves milestone_exercise_id + milestone proposal status unchanged. Follow CLAUDE.md: no `any` (use `instanceof Error` in catches), real-MySQL integration style. Do NOT run the suite locally (user rule [[feedback_tests_run_in_ci_not_local]]) — author the test; CI runs it on staging push.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit 2>&1 | grep -i "milestone-apply.test" || echo "no test typecheck errors"</automated>
  </verify>
  <acceptance_criteria>
    - File el-templo-api/test/exercises/milestone-apply.test.ts exists with the five assertions above.
    - Apply function is exported from bootstrap-milestones.ts and imported by the test (no child_process spawn).
    - Test asserts: variantes get hito id, hitos stay NULL, milestone proposals flip to 'accepted', re-run is no-op, accepted/rejected rows untouched, AND --apply aborts (no milestone write) when a pending dimension proposal exists.
    - `tsc --noEmit` clean for the new test file. (Suite execution deferred to CI per user rule.)
  </acceptance_criteria>
  <done>Integration test authored covering truth-write, idempotency, coach-correction preservation, and the milestone-only abort guard; typecheck clean.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary          | Description                                                                                                                                                                                                                 |
| ----------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CLI operator → DB | The apply CLI mutates exercises.milestone_exercise_id and proposal status. Operator is a trusted maintainer (no public surface), but a partial/non-transactional write would corrupt the backbone the member tree consumes. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                    | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --------- | ---------------------- | ------------------------------------------------------------ | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-135-01  | Tampering              | --apply bulk write                                           | mitigate    | Each accept runs in its own db.transaction via acceptMilestoneReview; per-row try/catch isolates failures (no partial cross-row corruption). hitos-before-variantes ordering prevents invalid intermediate states.                                                                                                                                                                                                                                       |
| T-135-02  | Information Disclosure | --dry-run output                                             | accept      | Output is exercise names/tokens + a pending-dimension count printed to operator stdout only; no PII, internal CLI.                                                                                                                                                                                                                                                                                                                                       |
| T-135-03  | Denial of Service      | re-run pisando trabajo manual                                | mitigate    | SELECT filters to status='pending' and acceptMilestoneReview flips only pending; coach-corrected accepted/rejected rows are provably untouched (Task 3 asserts this).                                                                                                                                                                                                                                                                                    |
| T-135-04  | Tampering              | hidden dimension-accept side-effect of acceptMilestoneReview | mitigate    | acceptMilestoneReview (service.ts:1027-1049) accepts a pending DIMENSION proposal in the same tx (writes habilidad/step, may prune off-backbone). --apply is milestone-only BY CONTRACT: it asserts zero pending dimension proposals and ABORTS otherwise (Task 2); --dry-run reports the pending-dimension count for operator visibility (Task 1); Task 3 asserts the abort. So --apply never silently mutates dimensions beyond milestone_exercise_id. |
| T-135-SC  | Tampering              | npm/pip/cargo installs                                       | mitigate    | No new packages installed in this plan (CLAUDE.md no-auto-install rule). slopcheck N/A.                                                                                                                                                                                                                                                                                                                                                                  |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit` clean in el-templo-api for bootstrap-milestones.ts and the new test.
- `--dry-run` writes nothing (no transaction/accept in the dry-run branch) and reports the pending-dimension side-effect count.
- `--apply` aborts if any pending dimension proposals remain (milestone-only contract); otherwise writes truth only via acceptMilestoneReview, hitos before variantes, pending-only.
- Test file authored (suite run deferred to CI on staging push).
</verification>

<success_criteria>

- D-01 dry-run plan printer present and write-free (Front Lever surfaced; pending-dimension side-effect count surfaced).
- D-02/D-03 apply persists all pending milestone proposals transactionally via acceptMilestoneReview, milestone-only by contract (aborts on pending dimension proposals), no direct column write, no milestone_source column.
- D-04 idempotency: pending-only iteration; accepted/rejected preserved (test asserts).
- Integration test authored (incl. milestone-only abort guard).
  </success_criteria>

<output>
Create `.planning/phases/135-rbol-del-admin-jerarqu-a-visual-de-hitos-y-variantes-en-tree/135-01-SUMMARY.md` when done. Record: how the apply function is exported (for Plan 02 to import when emitting the migration), the milestone-only contract (apply aborts on pending dimension proposals), and the exact assignment-capture shape (exercise_id → milestone_exercise_id) the apply produces.
</output>
