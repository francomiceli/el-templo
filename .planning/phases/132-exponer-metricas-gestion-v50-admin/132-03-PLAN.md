---
phase: 132-exponer-metricas-gestion-v50-admin
plan: 03
type: execute
wave: 3
depends_on: ["132-01", "132-02"]
files_modified:
  - el-templo-admin/src/types/analytics.ts
  - el-templo-admin/src/composables/useAnalyticsApi.ts
autonomous: true
requirements: [SC-1, D-10, D-12]
must_haves:
  truths:
    - "useAnalyticsApi exposes getTicket, getChurn, getRenewal, getLtv, getFrequency, getTrialFunnel hitting the 6 endpoints"
    - "types/analytics.ts has TS interfaces mirroring all 6 backend output shapes including FrequencyCoolingRow.name/phone"
    - "AnalyticsFilters carries planId, turno ('manana'|'tarde'), and window so buildParams serializes them"
  artifacts:
    - path: "el-templo-admin/src/composables/useAnalyticsApi.ts"
      provides: "6 new analytics fetch methods + buildParams turno/window"
      contains: "getTrialFunnel"
    - path: "el-templo-admin/src/types/analytics.ts"
      provides: "TicketAnalytics/ChurnAnalytics/RenewalAnalytics/LtvAnalytics/FrequencyAnalytics/TrialFunnelAnalytics + MetricShape"
      contains: "TrialFunnelAnalytics"
  key_links:
    - from: "useAnalyticsApi.getTrialFunnel"
      to: "/admin/analytics/trial-funnel"
      via: "api.get with buildParams(filters)"
      pattern: "admin/analytics/trial-funnel"
    - from: "buildParams"
      to: "request query"
      via: "turno + window serialized when defined"
      pattern: "filters.turno"
---

<objective>
Build the frontend contract layer (SC-1): mirror the 6 backend output shapes into `types/analytics.ts` and add the 6 fetch methods to `useAnalyticsApi.ts`, plus extend `AnalyticsFilters`/`buildParams` with `turno` and `window` (planId already present). This is the interface-first layer the 4 new tab components (plan 132-04) consume — they receive typed contracts, no codebase exploration needed.

Purpose: Decouple the typing + API wiring from the visual components so plan 04 builds against stable contracts. Mirrors the backend types from `el-templo-api/.../analytics/types.ts` exactly so the wire shapes match.
Output: 6 typed methods registered in the composable; 6 analytics interfaces (+ their nested row types + the shared `MetricShape`) in the frontend types file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/132-exponer-metricas-gestion-v50-admin/132-PATTERNS.md
@.planning/phases/132-exponer-metricas-gestion-v50-admin/132-UI-SPEC.md
@.planning/phases/132-02-PLAN.md

<interfaces>
<!-- Backend source-of-truth shapes to MIRROR (el-templo-api/src/modules/analytics/types.ts) -->
TicketAnalytics (line 498): { byCurrency: { ARS: TicketCurrencyBlock; EUR: TicketCurrencyBlock }; historicalFallbackCount; excludedNoLink }
  TicketCurrencyBlock (455): { global: MetricShape; globalCohorts: TicketCohortSplit; zeroCount; zeroPct; discountMean|null; discountMedian|null; perPlan: TicketPlanRow[]; byBranch: TicketBranchRow[]; byDuration: TicketDurationRow[] }
  TicketCohortSplit (394): { listPrice: TicketCohortAverage; discounted: TicketCohortAverage }  TicketCohortAverage (376): { average: number|null; n }
  TicketPlanRow (403) / TicketBranchRow (432) / TicketDurationRow (444)
ChurnAnalytics (568): { window: ChurnWindowResult; comparison: ChurnWindowResult[]; enGracia; series: ChurnSeriesPoint[]; breakdowns: ChurnSegmentRow[] }
  ChurnWindowResult (539): { windowDays; churn: MetricShape }   ChurnSeriesPoint (552): { bucket; churn: MetricShape; provisional }   ChurnSegmentRow (524): { axis: ChurnRenewalAxis; key; churn: MetricShape }
  ChurnRenewalAxis (517) = "branch"|"country"|"duration"|"plan"
RenewalAnalytics (611): { windowDays; renewal: MetricShape; enGracia; breakdowns: RenewalSegmentRow[] }   RenewalSegmentRow (596): { axis; key; renewal: MetricShape }
LtvAnalytics (717): { lifetimeHeadlineMonths: number|null; survivalMedianMonths: number|null; monetary: LtvMonetary; breakdowns: LtvSegmentRow[]; n }
  LtvMonetary (666): { ARS: LtvCurrencyBlock; EUR: LtvCurrencyBlock }   LtvCurrencyBlock (640): { projected: number|null; observed: number|null; monthlyRealRevenue: number|null; n }   LtvSegmentRow (679)
FrequencyAnalytics (808): { distribution: FrequencyDistributionRow[]; coolingDown: FrequencyCoolingRow[]; checkInAdoption: CheckInAdoptionRow[]; breakdowns: FrequencySegmentRow[] }
  FrequencyBand (745) = "inactivo"|"bajo"|"medio"|"alto"
  FrequencyDistributionRow (760): { band: FrequencyBand; count: MetricShape }
  FrequencyCoolingRow (774, ENRICHED by 132-02): { userId; currentBand: FrequencyBand; priorBand: FrequencyBand; pctVariacion: number|null; name: string; phone: string|null }
  FrequencySegmentRow (790): { axis; key; band: FrequencyBand; count: MetricShape }
TrialFunnelAnalytics (901): { counts: TrialFunnelStageCounts; rates: TrialFunnelRates; series: TrialFunnelSeriesRow[]; breakdowns: TrialFunnelBreakdownRow[]; attributionWindowDays }
  TrialFunnelStageCounts (836): { reservaron; asistieron; compraron }
  TrialFunnelRates (852): { tasaShow: MetricShape; tasaCierre: MetricShape; puntaAPunta: MetricShape }
  TrialFunnelSeriesRow (867): { bucket; rates: TrialFunnelRates; counts: TrialFunnelStageCounts; provisional }
  TrialFunnelBreakdownRow (883): { axis: TrialFunnelAxis; key; rates; counts }   TrialFunnelAxis (828) = "branch"|"country"|"turno"|"plan"

<!-- MetricShape envelope { nominal; percentage; n } — pervasive; NOT yet in frontend types/analytics.ts; add it. -->
<!-- CheckInAdoptionRow already exists in frontend types/analytics.ts (imported by composable). -->

<!-- useAnalyticsApi.ts method template (getRetention, lines 177-191) + registration block (217-231) + buildParams (24-33, already serializes planId). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mirror the 6 backend output shapes into types/analytics.ts</name>
  <files>el-templo-admin/src/types/analytics.ts</files>
  <read_first>
    - el-templo-admin/src/types/analytics.ts (existing FunnelAnalytics line 268, AdvancedFinanceAnalytics line 353, AnalyticsFilters lines 362-378, CheckInAdoptionRow)
    - el-templo-api/src/modules/analytics/types.ts (the 6 analytics interfaces + nested rows enumerated in <interfaces>)
    - 132-PATTERNS.md "types/analytics.ts" section (the interface→backend-line mapping table)
  </read_first>
  <behavior>
    - A `vue-tsc` typecheck (next task / plan 04) compiles against these types with no `any`.
    - FrequencyCoolingRow includes `name: string` and `phone: string | null` (matching the 132-02 enrichment).
    - AnalyticsFilters gains `turno?: 'manana' | 'tarde'` and `window?: number` (planId already present).
  </behavior>
  <action>
    Add a `MetricShape` interface `{ nominal: number; percentage: number; n: number }` (shared envelope) near the top of the analytics types. Then add the 6 top-level interfaces and ALL their nested row interfaces exactly mirroring the backend shapes listed in <interfaces> — TicketAnalytics (+ TicketCurrencyBlock, TicketCohortSplit, TicketCohortAverage, TicketPlanRow, TicketBranchRow, TicketDurationRow), ChurnAnalytics (+ ChurnWindowResult, ChurnSeriesPoint, ChurnSegmentRow, ChurnRenewalAxis), RenewalAnalytics (+ RenewalSegmentRow), LtvAnalytics (+ LtvMonetary, LtvCurrencyBlock, LtvSegmentRow), FrequencyAnalytics (+ FrequencyBand, FrequencyDistributionRow, FrequencyCoolingRow with name+phone, FrequencySegmentRow), TrialFunnelAnalytics (+ TrialFunnelStageCounts, TrialFunnelRates, TrialFunnelSeriesRow, TrialFunnelBreakdownRow, TrialFunnelAxis). Reuse the existing `CheckInAdoptionRow`. Use `number | null` (not `number`) wherever the backend declares `["number","null"]`. No `any` (CLAUDE.md).
    Extend `AnalyticsFilters` with `turno?: 'manana' | 'tarde'` and `window?: number` (keep existing planId/entryOrigin). Add doc comments noting turno applies only to funnel + frecuencia.
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm exec vue-tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>vue-tsc passes; all 6 analytics interfaces + nested rows + MetricShape exported; FrequencyCoolingRow has name+phone; AnalyticsFilters has turno+window; no `any`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: add 6 fetch methods to useAnalyticsApi.ts + serialize turno/window</name>
  <files>el-templo-admin/src/composables/useAnalyticsApi.ts</files>
  <read_first>
    - el-templo-admin/src/composables/useAnalyticsApi.ts (getRetention lines 177-191 = method template; buildParams lines 24-33; the returned-object registration lines 217-231; the import block lines 10-22)
    - el-templo-admin/src/types/analytics.ts (the 6 interfaces added in Task 1 — import them)
  </read_first>
  <behavior>
    - getTrialFunnel({ turno: 'manana' }) issues GET /admin/analytics/trial-funnel with `turno=manana` in params.
    - getFrequency() resolves to FrequencyAnalytics typed; getTicket()/getLtv() return their typed shapes.
    - buildParams serializes turno and window only when defined (undefined params omitted).
  </behavior>
  <action>
    In buildParams, add `if (filters.turno !== undefined) params.turno = filters.turno;` and `if (filters.window !== undefined) params.window = filters.window;` (planId line already present).
    Add 6 methods copying the getRetention body verbatim, each with its endpoint + return type + error message:
      getTicket → GET '/admin/analytics/ticket' → TicketAnalytics ("Error cargando ticket promedio")
      getChurn → '/admin/analytics/churn' → ChurnAnalytics ("Error cargando churn")
      getRenewal → '/admin/analytics/renewal' → RenewalAnalytics ("Error cargando renovación")
      getLtv → '/admin/analytics/ltv' → LtvAnalytics ("Error cargando LTV")
      getFrequency → '/admin/analytics/frequency' → FrequencyAnalytics ("Error cargando frecuencia")
      getTrialFunnel → '/admin/analytics/trial-funnel' → TrialFunnelAnalytics ("Error cargando funnel de prueba")
    Import the 6 types from 'src/types/analytics'. Register all 6 in the returned object (lines 217-231). Use `extractError` + `loading`/`error` refs exactly like the analogs. Never console.* (use the composable's existing error handling; tabs log via createLogger).
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm exec vue-tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>vue-tsc passes; 6 methods exist + registered; buildParams serializes turno+window; each method hits the correct endpoint with typed return.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                        |
| ------------------------------- | ------------------------------------------------------------------ |
| admin SPA → /admin/analytics/\* | Client passes filters; backend (plans 01/02) enforces auth + scope |

## STRIDE Threat Register

| Threat ID | Category               | Component               | Disposition | Mitigation Plan                                                                                                                                                        |
| --------- | ---------------------- | ----------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| T-132-08  | Information disclosure | client trusting payload | accept      | This layer is a typed client over already-authorized endpoints; auth/scope/PII gating is enforced server-side (plans 01/02). No new trust surface, no secret handling. |
| T-132-09  | Tampering              | filter serialization    | mitigate    | buildParams only serializes known typed filter fields; turno is the 'manana'                                                                                           | 'tarde' union — no free-form injection from this layer. Backend re-validates via JSON schema. |
| T-132-SC  | Tampering              | npm installs            | mitigate    | No new dependencies (uses existing axios `api`, types, extractError).                                                                                                  |

</threat_model>

<verification>
- `cd el-templo-admin && pnpm exec vue-tsc --noEmit` clean.
- Grep: 6 methods registered in the returned object; turno+window in buildParams.
</verification>

<success_criteria>

- SC-1 met: 6 typed methods + 6 mirrored interfaces.
- No `any`; nullable fields typed `| null`; FrequencyCoolingRow carries name/phone.
- AnalyticsFilters supports planId/turno/window for the page-level filters.
  </success_criteria>

<output>
Create `.planning/phases/132-exponer-metricas-gestion-v50-admin/132-03-SUMMARY.md` when done.
</output>
