---
phase: 132-exponer-metricas-gestion-v50-admin
plan: 04
type: execute
wave: 4
depends_on: ["132-03"]
files_modified:
  - el-templo-admin/src/components/analytics/ConversionTab.vue
  - el-templo-admin/src/components/analytics/IngresosTab.vue
autonomous: true
requirements: [SC-2, D-01, D-05, D-06]
must_haves:
  truths:
    - "ConversionTab renders the trial funnel as a descending-width embudo with tasaCierre as the dominant star rate"
    - "IngresosTab renders Ticket (double headline: general + list-price) and LTV (months + $) with ARS/EUR in separate blocks, never summed"
  artifacts:
    - path: "el-templo-admin/src/components/analytics/ConversionTab.vue"
      provides: "Funnel de prueba tab consuming TrialFunnelAnalytics"
      contains: "tasaCierre"
    - path: "el-templo-admin/src/components/analytics/IngresosTab.vue"
      provides: "Ticket + LTV tab, per-currency"
      contains: "renderedCurrencies"
  key_links:
    - from: "ConversionTab.vue"
      to: "props.data: TrialFunnelAnalytics | null"
      via: "rates.tasaCierre headline + counts embudo"
      pattern: "tasaCierre"
    - from: "IngresosTab.vue"
      to: "props.ticket.byCurrency / props.ltv.monetary"
      via: "per-currency v-for, never sum"
      pattern: "renderedCurrencies"
---

<objective>
Create two of the four new thematic tab components (SC-2): **ConversionTab** (Funnel de prueba — D-06) and **IngresosTab** (Ticket + LTV — D-01/D-05). Both are presentational: they receive typed props from the page (plan 132-06 wires them) and render per the LOCKED visualization in DECISIONES-VISUALIZACION.md and the UI-SPEC. No fetching here — the page owns fetch.

Purpose: Render the Conversión and Ingresos groups. These two are grouped together in this plan because both are chart/stat tabs with no CSV/table-interaction complexity (that lives in FrecuenciaTab, plan 132-05).
Output: 2 Vue tab components with skeleton/empty states, chart.js wiring from chart-colors, per-currency isolation for Ingresos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/132-exponer-metricas-gestion-v50-admin/132-PATTERNS.md
@.planning/phases/132-exponer-metricas-gestion-v50-admin/132-UI-SPEC.md
@.planning/phases/132-exponer-metricas-gestion-v50-admin/DECISIONES-VISUALIZACION.md
@.planning/phases/132-03-PLAN.md

<interfaces>
<!-- Types from plan 132-03 (src/types/analytics.ts) -->
TrialFunnelAnalytics: { counts: {reservaron;asistieron;compraron}; rates: {tasaShow;tasaCierre;puntaAPunta} (each MetricShape {nominal;percentage;n}); series; breakdowns: {axis:'branch'|'country'|'turno'|'plan';key;rates;counts}[]; attributionWindowDays }
TicketAnalytics: { byCurrency: { ARS: TicketCurrencyBlock; EUR: TicketCurrencyBlock }; historicalFallbackCount; excludedNoLink }
  TicketCurrencyBlock: { global: MetricShape; globalCohorts: {listPrice:{average:number|null;n}; discounted:{average:number|null;n}}; zeroCount; zeroPct; discountMean|null; discountMedian|null; perPlan; byBranch; byDuration }
LtvAnalytics: { lifetimeHeadlineMonths: number|null; survivalMedianMonths: number|null; monetary: { ARS: LtvCurrencyBlock; EUR: LtvCurrencyBlock }; breakdowns; n }
  LtvCurrencyBlock: { projected: number|null; observed: number|null; monthlyRealRevenue: number|null; n }

<!-- Analog patterns (read these files) -->

- FunnelTab.vue (BEING DELETED in 132-06): chart.js registration (86-116), loading/empty skeleton (46-55), horizontal bar indexAxis:'y' (58-65, 200-223), star-rate text-h4 card (71-78). COPY the wiring, NOT the freemium cohort math.
- FinanzasAvanzadasTab.vue: per-currency isolation — currencyHasData + renderedCurrencies computed (11-37, 93-110), per-currency v-for grid (col-md-6 when 2 currencies), formatPrice import (line 75), the ARPU text-h4+text-caption stat block (49-55) = the double-headline layout template.
- src/utils/chart-colors.ts (COLORS, chartColors) — never inline hex.
- src/utils/format-price.ts (formatPrice).

<!-- UI-SPEC binding copy: empty funnel "No hay sesiones de prueba en el periodo seleccionado"; currency tag "(ARS)"/"(EUR)" as text-caption text-grey-6; null money → "—". -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ConversionTab.vue — Funnel de prueba (D-06)</name>
  <files>el-templo-admin/src/components/analytics/ConversionTab.vue</files>
  <read_first>
    - el-templo-admin/src/components/analytics/FunnelTab.vue (chart.js registration + Bar import lines 86-116; loading/empty skeleton 46-55; horizontal bar funnelOptions indexAxis:'y' 200-223; star-rate text-h4 card 71-78; q-banner dense rounded 21-28 for any warning)
    - el-templo-admin/src/types/analytics.ts (TrialFunnelAnalytics + nested types)
    - el-templo-admin/src/utils/chart-colors.ts
    - 132-UI-SPEC.md "Conversión — Funnel de prueba (D-06)" + Copywriting Contract
  </read_first>
  <behavior>
    - With props.data null → renders the q-skeleton loading block.
    - With props.data where counts are all 0 → renders empty state "No hay sesiones de prueba en el periodo seleccionado".
    - With data → renders 3 descending-width bars (reservaron ≥ asistieron ≥ compraron) and tasaCierre.percentage as a text-h3/h4 text-primary dominant headline; tasaShow + puntaAPunta as smaller text-h6 + text-caption.
  </behavior>
  <action>
    Create ConversionTab.vue (script setup lang="ts"). Props: `data: TrialFunnelAnalytics | null` + `loading: boolean`. Register chart.js (CategoryScale, LinearScale, BarElement, Title, Tooltip, Legend) and import Bar from vue-chartjs, COLORS/chartColors from chart-colors — copy the registration block from FunnelTab.vue. Render the embudo as a horizontal Bar (`indexAxis: 'y'`) over [reservaron, asistieron, compraron] from props.data.counts, descending width, in a `<q-card flat bordered><q-card-section>` with `style="height: 300px; position: relative"`. Render `props.data.rates.tasaCierre.percentage` as the dominant star rate (`text-h3`/`text-h4 text-primary`, label "Tasa de cierre (compró sobre asistió)") above/beside the secondary `tasaShow` and `puntaAPunta` (`text-h6` + `text-caption`). Reuse the loading skeleton + empty-state pattern verbatim, swapping empty copy to "No hay sesiones de prueba en el periodo seleccionado". The turno×sucursal cross (D-11) is achieved by the page-level turno filter + the existing sucursal breakdown — render the `breakdowns` filtered to `axis==='branch'` as a small q-table/list under the funnel; do NOT build a 2D component. Use createLogger for any error path; never console.*. No `any`.
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm exec vue-tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>vue-tsc passes; ConversionTab consumes TrialFunnelAnalytics; tasaCierre is the dominant headline; descending embudo; skeleton + correct empty copy; sucursal breakdown shown; no 2D aggregation code; no inline hex.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IngresosTab.vue — Ticket + LTV (D-01 / D-05)</name>
  <files>el-templo-admin/src/components/analytics/IngresosTab.vue</files>
  <read_first>
    - el-templo-admin/src/components/analytics/FinanzasAvanzadasTab.vue (currencyHasData + renderedCurrencies computed 11-37, 93-110; per-currency v-for grid; formatPrice import line 75; ARPU text-h4+text-caption stat block 49-55 = double-headline template)
    - el-templo-admin/src/types/analytics.ts (TicketAnalytics, LtvAnalytics + nested)
    - el-templo-admin/src/utils/format-price.ts
    - 132-UI-SPEC.md "Ingresos — Ticket (D-01)" + "Ingresos — LTV (D-05)"
    - DECISIONES-VISUALIZACION.md sections 1 (Ticket) + 5 (LTV)
  </read_first>
  <behavior>
    - Ticket: per currency, renders a double headline — `global` (promedio general) + `globalCohorts.listPrice.average` (precio de lista) side by side (col-6 each); `% con descuento` and `% a $0` shown as TWO separate stats (never merged).
    - LTV: per currency, double headline — `lifetimeHeadlineMonths` + per-currency `$` ; shows BOTH month estimates (lifetimeHeadlineMonths simple + survivalMedianMonths supervivencia) labeled distinctly; `projected` vs `observed` side by side, null → "—".
    - ARS and EUR render in SEPARATE blocks; no figure sums across currencies.
    - props null → skeleton; empty (n===0 both currencies) → "No hay datos para el periodo seleccionado".
  </behavior>
  <action>
    Create IngresosTab.vue (script setup lang="ts"). Props: `ticket: TicketAnalytics | null`, `ltv: LtvAnalytics | null`, `loading: boolean`. Reuse the per-currency isolation pattern from FinanzasAvanzadasTab.vue: a `renderedCurrencies` computed driven by `currencyHasData` checking `ticket.byCurrency[cur].global.n` / `ltv.monetary[cur].n`; render a per-currency `v-for` grid (`col-md-6` when 2 currencies, else `col-md-12`). For each currency:
      Ticket block: two `text-h4` headlines side by side — `formatPrice(block.global.nominal, cur)` (promedio general) and `formatPrice(block.globalCohorts.listPrice.average, cur)` (precio de lista, null → "—"); below, two separate stats — `% con descuento` (from discountMean*100) and `% a $0` (block.zeroPct) — explicitly NOT merged. The per-plan/per-branch desglose is an expandable detail (`q-expansion-item`) listing `perPlan`/`byBranch` rows; not always visible.
      LTV block: two `text-h4` headlines — meses (`lifetimeHeadlineMonths`, null → "—") + `$` (`formatPrice(monetary[cur].observed ?? monetary[cur].projected, cur)`); show BOTH month estimates labeled "estimación simple" (`lifetimeHeadlineMonths`) and "supervivencia" (`survivalMedianMonths`); show `projected` vs `observed` side by side per currency, null → "—".
    Add the "(ARS)"/"(EUR)" currency tag as `text-caption text-grey-6`. Use `<q-card flat bordered>`. NEVER sum ARS+EUR. Skeleton + empty copy per UI-SPEC. createLogger for errors; no console.*; no `any`.
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm exec vue-tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>vue-tsc passes; IngresosTab consumes TicketAnalytics+LtvAnalytics; double headlines; % descuento and % $0 separated; both LTV month estimates + projected/observed shown null-safe; per-currency blocks never summed; skeleton + empty state.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                | Description                                           |
| ----------------------- | ----------------------------------------------------- |
| page props → tab render | Already-authorized analytics data rendered in the DOM |

## STRIDE Threat Register

| Threat ID | Category               | Component                    | Disposition | Mitigation Plan                                                                                                                                                  |
| --------- | ---------------------- | ---------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-132-10  | Information disclosure | currency cross-contamination | mitigate    | Per-currency `renderedCurrencies` isolation reused from FinanzasAvanzadasTab; ARS/EUR never summed — a code-review check confirms no `+` across currency blocks. |
| T-132-11  | Tampering              | null/NaN figures rendered    | mitigate    | All nullable figures (listPrice.average, projected, observed, lifetimeHeadlineMonths) render "—" when null; no NaN reaches the DOM.                              |
| T-132-SC  | Tampering              | npm installs                 | mitigate    | No new deps — chart.js/vue-chartjs already installed; no install task.                                                                                           |

</threat_model>

<verification>
- `cd el-templo-admin && pnpm exec vue-tsc --noEmit` clean.
- Grep: no raw hex in either component (colors via chart-colors); no `any`.
- Manual: ConversionTab has no 2D-cross code; IngresosTab never adds ARS+EUR.
</verification>

<success_criteria>

- ConversionTab + IngresosTab render per the locked D-06/D-01/D-05 contracts.
- Per-currency isolation honored; null-safe rendering; skeleton + empty states present.
- Components are pure props-in (page wires fetch in plan 132-06).
  </success_criteria>

<output>
Create `.planning/phases/132-exponer-metricas-gestion-v50-admin/132-04-SUMMARY.md` when done.
</output>
