---
phase: 132-exponer-metricas-gestion-v50-admin
plan: 06
type: execute
wave: 5
depends_on: ["132-03", "132-04", "132-05"]
files_modified:
  - el-templo-admin/src/pages/AnaliticasPage.vue
  - el-templo-admin/src/components/analytics/MiembrosTab.vue
  - el-templo-admin/src/components/analytics/FinanzasAvanzadasTab.vue
  - el-templo-admin/src/components/analytics/FunnelTab.vue
autonomous: true
requirements: [SC-2, SC-3, SC-4, D-07, D-08, D-09, D-15, D-16, D-17, D-18]
must_haves:
  truths:
    - "AnaliticasPage shows 4 new tabs (Conversión, Retención, Asistencia, Ingresos) in the D-07 thematic grouping and D-08 order, each fetching its endpoint(s) with the global filters"
    - "The page has Plan (all 6 tabs) and Turno (only Conversión + Asistencia) filters that flow into the fetches (D-09)"
    - "Deprecated content is physically deleted: Renovación 7/14/30 cards (D-15) + Tasa de retención card (D-18) in MiembrosTab, ARPU card (D-16) in FinanzasAvanzadasTab, and FunnelTab.vue + its tab/panel/state/fetch (D-17)"
    - "Conserved per D-20 and D-21: RetencionTab.vue, all of FinanzasTab.vue, the Caja vs Devengado chart, and the operational MiembrosTab counts (Nuevos/Bajas/Distribución/lista) are NOT deleted"
    - "No dead imports, orphan components, or dead fetch calls remain (AsistenciaTab.vue is NOT touched; getMemberAnalytics is PRESERVED)"
  artifacts:
    - path: "el-templo-admin/src/pages/AnaliticasPage.vue"
      provides: "4 new tabs + plan/turno filters wired; old FunnelTab removed"
      contains: "ConversionTab"
    - path: "el-templo-admin/src/components/analytics/MiembrosTab.vue"
      provides: "renovación + tasa-de-retención cards removed; operational counts conserved"
  key_links:
    - from: "AnaliticasPage fetchTabData"
      to: "the 4 new tab fetches (getTrialFunnel/getChurn+getRenewal/getFrequency/getTicket+getLtv)"
      via: "switch cases on activeTab passing currentFilters (+turno where applicable)"
      pattern: "getTrialFunnel"
    - from: "currentFilters computed"
      to: "selectedPlanId / selectedTurno"
      via: "planId spread on all; turno spread only by conversión+frecuencia fetches"
      pattern: "selectedPlanId"
---

<objective>
Wire the 4 new tab components into `AnaliticasPage.vue` in the D-07/D-08 grouping+order, add the Plan (all 6) and Turno (funnel+frecuencia only) global filters (D-09), and physically delete the deprecated screens (D-15/D-16/D-17/D-18) — leaving no dead imports, orphan components, or dead fetch calls (SC-3/SC-4).

Purpose: This is the integration + cleanup plan. It depends on plans 03/04/05 (types/composable + the 4 components). It is the ONLY plan that touches AnaliticasPage.vue, so the new tab wiring and the FunnelTab deletion happen together (no file contention).
Output: Analíticas page renders the 6 new metrics with their filters; deprecated cards/tab/file gone; AsistenciaTab.vue untouched (D-19 CANCELLED); getMemberAnalytics preserved (feeds conserved counts).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/132-exponer-metricas-gestion-v50-admin/132-CONTEXT.md
@.planning/phases/132-exponer-metricas-gestion-v50-admin/132-PATTERNS.md
@.planning/phases/132-exponer-metricas-gestion-v50-admin/132-UI-SPEC.md
@.planning/phases/132-04-PLAN.md
@.planning/phases/132-05-PLAN.md

<interfaces>
<!-- AnaliticasPage.vue current wiring (the modify targets) -->
- <q-tabs> tabs block lines 136-149 (miembros/finanzas/programas/funnel/retencion). <q-tab-panels> 151-239.
- Filter row: country select 17-27 (isOwner), branch select 30-42, date dropdown 44-90. onFilterChange (630-632).
- currentFilters computed (409-414): { branchId, country: isOwner?selectedCountry:undefined, dateFrom, dateTo }.  ← add planId; turno added per-fetch.
- Per-tab refs (433-447): funnelData/retentionData/advancedFinanceData + loading + funnelEntryOrigin/retentionPlanId.
- Fetch methods (554-597) + fetchTabData switch (609-628) + watch(activeTab) (636-638).
- Imports (251-266): FunnelTab import line 253; FunnelEntryOrigin type line 265; funnelData/loadingFunnel refs 433/437; fetchFunnelData 554-568; onFunnelFilterChange 605-607; case 'funnel' 621-623.
- membersApi.getPlans(includeArchived=false, { branchId?, country? }) => PlanOption[]  ← plan-list source for the new Plan select (useMembersApi).

<!-- New components (plans 04/05) -->

ConversionTab.vue props: { data: TrialFunnelAnalytics|null; loading }
IngresosTab.vue props: { ticket: TicketAnalytics|null; ltv: LtvAnalytics|null; loading }
RetencionGestionTab.vue props: { churn: ChurnAnalytics|null; renewal: RenewalAnalytics|null; loading }
FrecuenciaTab.vue props: { data: FrequencyAnalytics|null; loading }

<!-- composable (plan 03): getTicket/getChurn/getRenewal/getLtv/getFrequency/getTrialFunnel(filters) -->

<!-- DELETION MAP (132-UI-SPEC Deprecation Map + 132-PATTERNS CONTRADICTIONS) -->

D-15 MiembrosTab.vue: delete Renovación 7/14/30 cards (template ~49-66) + renewalRateCards computed (~251-259). KEEP getMemberAnalytics (AnaliticasPage:521 feeds conserved counts). renewalRate field stays unless fully orphaned.
D-16 FinanzasAvanzadasTab.vue: delete ARPU mensual q-card (~40-57) + arpuEntries computed (~163-173). CONSERVE "Caja vs Devengado" chart + its code.
D-17 AnaliticasPage.vue: remove <q-tab name="funnel"> (147), <q-tab-panel name="funnel"> (220-228), FunnelTab import (253), FunnelEntryOrigin import (265), funnelData/loadingFunnel/funnelEntryOrigin refs, fetchFunnelData + onFunnelFilterChange, case 'funnel'. Then DELETE the FunnelTab.vue file.
D-18 MiembrosTab.vue: delete the "Tasa de retención" card (~28-46, third stat in Nuevos/Bajas row). KEEP Nuevos + Bajas cards.
D-19 CANCELLED: do NOT touch AsistenciaTab.vue.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plan + Turno global filters + mount the 4 new tabs (D-07/D-08/D-09/SC-2)</name>
  <files>el-templo-admin/src/pages/AnaliticasPage.vue</files>
  <read_first>
    - el-templo-admin/src/pages/AnaliticasPage.vue (filter row 16-90; q-tabs 136-149; q-tab-panels 151-239; imports 251-268; refs 425-447; currentFilters 409-414; fetch methods 554-597; fetchTabData 609-628; onMounted/onUnmounted 642-650)
    - el-templo-admin/src/composables/useMembersApi.ts (getPlans lines 397-418 + PlanOption type)
    - el-templo-admin/src/composables/useAnalyticsApi.ts (the 6 new methods from plan 132-03)
    - 132-UI-SPEC.md "Layout & Panel Order" + "Global filters" tables
    - 132-PATTERNS.md "pages/AnaliticasPage.vue" section
  </read_first>
  <action>
    Import ConversionTab, RetencionGestionTab, FrecuenciaTab, IngresosTab and the 6 analytics types (TicketAnalytics, ChurnAnalytics, RenewalAnalytics, LtvAnalytics, FrequencyAnalytics, TrialFunnelAnalytics) from src/types/analytics.
    Add a Plan filter: a `q-select` in the filter row (`dense outlined emit-value map-options`, match branch styling) bound to `selectedPlanId = ref<number | null>(null)` with options from a new `planOptions` ref populated by `membersApi.getPlans(false, { branchId: selectedBranchId.value, country: isOwner.value ? selectedCountry.value : undefined })` (first option "Todos los planes" = null); fetch it in onMounted alongside fetchBranches and refetch on country/branch change. `@update:model-value="onFilterChange"`.
    Add a Turno filter: a `q-select` (options Mañana='manana', Tarde='tarde', plus "Todos los turnos"=null) bound to `selectedTurno = ref<'manana'|'tarde'|null>(null)`, shown ONLY when `activeTab` is 'conversion' or 'frecuencia' (v-if), `@update:model-value="onFilterChange"`.
    Extend `currentFilters` to spread `planId: selectedPlanId.value ?? undefined` (applies to all 6). Do NOT put turno in currentFilters — pass it per-fetch only for conversión + frecuencia.
    Add the 4 tabs to `<q-tabs>` in D-08 order AFTER the existing tabs (the existing miembros/finanzas/programas/retencion REMAIN; the old 'funnel' tab is removed in Task 3): `<q-tab name="conversion" label="Conversión" icon="filter_alt" />`, `<q-tab name="retencion-gestion" label="Retención" icon="replay" />`, `<q-tab name="frecuencia" label="Asistencia" icon="event_available" />`, `<q-tab name="ingresos" label="Ingresos" icon="payments" />`. Add matching `<q-tab-panel>`s mounting each component with its props + loading ref.
    Add per-tab data refs + loading refs + fetch functions (copy the fetchRetentionData pattern): fetchConversion (getTrialFunnel with `{ ...currentFilters.value, turno: selectedTurno.value ?? undefined }`), fetchRetencionGestion (Promise.all getChurn + getRenewal → set churnData + renewalData), fetchFrecuencia (getFrequency with `{ ...currentFilters.value, turno: selectedTurno.value ?? undefined }`), fetchIngresos (Promise.all getTicket + getLtv → ticketData + ltvData). Each wrapped in try/catch with log.error + null-on-error per the existing convention.
    Add the 4 cases to `fetchTabData` switch.
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm exec vue-tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>vue-tsc passes; 4 tabs mounted in D-08 order; Plan select (all tabs) + Turno select (conversión+frecuencia only) wired into fetches; planId in currentFilters; turno passed per-fetch; fetchTabData has the 4 new cases.</done>
</task>

<task type="auto">
  <name>Task 2: Delete FunnelTab wiring in the page + delete FunnelTab.vue (D-17/SC-3/SC-4)</name>
  <files>el-templo-admin/src/pages/AnaliticasPage.vue, el-templo-admin/src/components/analytics/FunnelTab.vue</files>
  <read_first>
    - el-templo-admin/src/pages/AnaliticasPage.vue (FunnelTab import 253; FunnelEntryOrigin import 265; <q-tab name="funnel"> 147; <q-tab-panel name="funnel"> 220-228; funnelData/loadingFunnel/funnelEntryOrigin refs 433/437/447; fetchFunnelData 554-568; onFunnelFilterChange 605-607; case 'funnel' 621-623)
    - el-templo-admin/src/components/analytics/FunnelTab.vue (confirm it has no other importers)
  </read_first>
  <action>
    In AnaliticasPage.vue remove the OLD funnel wiring entirely: the `<q-tab name="funnel">`, the `<q-tab-panel name="funnel">` block, the `import FunnelTab from ...` line, the `FunnelEntryOrigin` type import, the `funnelData`/`loadingFunnel`/`funnelEntryOrigin` refs, the `fetchFunnelData` and `onFunnelFilterChange` functions, and the `case 'funnel'` in fetchTabData. Confirm `FunnelAnalytics` import is also removed if now unused (the NEW funnel uses TrialFunnelAnalytics). Do not touch the new 'conversion' tab added in Task 1.
    Then delete the file el-templo-admin/src/components/analytics/FunnelTab.vue.
    Verify no remaining references: grep the admin src for `FunnelTab` and `FunnelEntryOrigin`/`funnelEntryOrigin` — there must be zero matches outside types/analytics.ts (the FunnelEntryOrigin TYPE may remain defined in types if the freemium funnel endpoint type is still used elsewhere; remove the import in the page only).
  </action>
  <verify>
    <automated>cd el-templo-admin && grep -rn "FunnelTab\|funnelEntryOrigin\|fetchFunnelData\|loadingFunnel\b" src/ ; echo "exit grep (expect no matches in page/components):" ; pnpm exec vue-tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>vue-tsc passes; FunnelTab.vue deleted; no FunnelTab import/refs/fetch/tab/panel remain; grep shows no orphan references to the old funnel wiring; the new 'conversion' tab is intact.</done>
</task>

<task type="auto">
  <name>Task 3: Delete deprecated cards in MiembrosTab + FinanzasAvanzadasTab (D-15/D-16/D-18/SC-3/SC-4)</name>
  <files>el-templo-admin/src/components/analytics/MiembrosTab.vue, el-templo-admin/src/components/analytics/FinanzasAvanzadasTab.vue</files>
  <read_first>
    - el-templo-admin/src/components/analytics/MiembrosTab.vue (Renovación 7/14/30 cards template ~49-66; renewalRateCards computed ~251-259; "Tasa de retención" card ~28-46; Nuevos ~12-19 + Bajas ~20-27 cards which STAY; the conserved Distribución/lista "requieren atención" sections)
    - el-templo-admin/src/components/analytics/FinanzasAvanzadasTab.vue (ARPU mensual q-card ~40-57; arpuEntries computed ~163-173; "Caja vs Devengado" chart ~11-37 + cashVsAccruedData/chartOptions/renderedCurrencies/currencyHasData which STAY)
    - 132-PATTERNS.md CONTRADICTIONS (getMemberAnalytics is PRESERVED; renewalRate consumption removed)
  </read_first>
  <action>
    In MiembrosTab.vue: delete the Renovación (7/14/30 días) cards block from the template (D-15) and its `renewalRateCards` computed; delete the simple "Tasa de retención" stat card (D-18). KEEP the Nuevos and Bajas cards, the Distribución por plan, and the lista "requieren atención" (all conserved per D-21). If `props.data.renewalRate` and any imported `RenewalRate`/related type become unused after removing the cards, remove the now-dead local usages/imports too — but do NOT change the `getMemberAnalytics` call in AnaliticasPage.vue (it still feeds Nuevos/Bajas/Distribución counts). Removing the `renewalRate` field from the backend MemberAnalytics is OUT OF SCOPE for this phase (leave the endpoint shape intact).
    In FinanzasAvanzadasTab.vue: delete the "ARPU mensual" q-card (D-16) and the `arpuEntries` computed. CONSERVE the "Caja vs Devengado por mes" chart and ALL its supporting code (cashVsAccruedData, chartOptions, renderedCurrencies, currencyHasData, formatPrice). The new Ticket lives in IngresosTab.vue, not here.
    Do NOT touch AsistenciaTab.vue (D-19 cancelled).
    After edits, grep MiembrosTab + FinanzasAvanzadasTab for `renewalRateCards`, `arpuEntries`, and any now-unused refs/computeds to confirm no dead code remains.
  </action>
  <verify>
    <automated>cd el-templo-admin && grep -rn "renewalRateCards\|arpuEntries" src/components/analytics/ ; echo "(expect no matches)" ; pnpm exec vue-tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>vue-tsc passes; Renovación 7/14/30 + Tasa de retención cards gone from MiembrosTab; ARPU card gone from FinanzasAvanzadasTab; Caja vs Devengado conserved; Nuevos/Bajas/Distribución/lista conserved; getMemberAnalytics + AsistenciaTab.vue untouched; no dead computeds.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                       |
| ---------------------------------- | --------------------------------------------------------------------------------- |
| page filter selects → fetch params | Plan/turno selections become query params (re-validated server-side, plans 01/02) |

## STRIDE Threat Register

| Threat ID | Category          | Component                             | Disposition | Mitigation Plan                                                                                                                                                                                    |
| --------- | ----------------- | ------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-132-15  | Elevation         | plan/turno filters bypassing scope    | mitigate    | The page only passes typed filter values; country/branch scope + planId/turno AND-ing is enforced server-side (plans 01/02). The page never sends country for non-owners (isOwner gate preserved). |
| T-132-16  | Denial of service | orphan fetch after deletion           | mitigate    | Task 2 grep-gate confirms no dead fetchFunnelData/refs remain → no stale calls to a removed endpoint path.                                                                                         |
| T-132-17  | Tampering         | accidental deletion of conserved view | mitigate    | Done-criteria + grep gates assert AsistenciaTab.vue untouched, getMemberAnalytics + Caja vs Devengado + operational counts conserved.                                                              |
| T-132-SC  | Tampering         | npm installs                          | mitigate    | No new deps; file deletion + wiring only.                                                                                                                                                          |

</threat_model>

<verification>
- `cd el-templo-admin && pnpm exec vue-tsc --noEmit` clean after all 3 tasks.
- Grep gates: zero `FunnelTab`/`funnelEntryOrigin`/`fetchFunnelData` orphans; zero `renewalRateCards`/`arpuEntries`.
- Manual: AsistenciaTab.vue NOT in the diff; getMemberAnalytics call unchanged; ReportesPage.vue not touched.
- Visual (deferred to phase human-verify): open Analíticas → 4 new tabs present in order, plan/turno filters behave, no deprecated cards.
</verification>

<success_criteria>

- SC-2: 6 metrics render in AnaliticasPage across the 4 grouped tabs, honoring country/branch scope + admin roles + the plan/turno filters.
- SC-3: deprecated content physically deleted (not hidden): Renovación 7/14/30 + Tasa de retención + ARPU + FunnelTab.vue.
- SC-4: no dead imports, orphan components, or dead fetch calls; AsistenciaTab.vue and getMemberAnalytics preserved.
  </success_criteria>

<output>
Create `.planning/phases/132-exponer-metricas-gestion-v50-admin/132-06-SUMMARY.md` when done.
</output>
