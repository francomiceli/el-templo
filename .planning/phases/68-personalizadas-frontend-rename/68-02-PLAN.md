---
phase: 68-personalizadas-frontend-rename
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-app/src/modules/personalizada/types.ts
  - el-templo-app/src/modules/personalizada/index.ts
  - el-templo-app/src/modules/personalizada/routes.ts
  - el-templo-app/src/modules/personalizada/stores/personalizadaStore.ts
  - el-templo-app/src/modules/personalizada/composables/usePersonalizadaApi.ts
  - el-templo-app/src/modules/personalizada/composables/usePersonalizadaSession.ts
  - el-templo-app/src/modules/personalizada/components/PersonalizadaProgressBar.vue
  - el-templo-app/src/modules/personalizada/components/PersonalizadaProgressIndicator.vue
  - el-templo-app/src/modules/personalizada/pages/PersonalizadaSelection.vue
  - el-templo-app/src/modules/personalizada/pages/PersonalizadaOverview.vue
  - el-templo-app/src/modules/personalizada/pages/PersonalizadaSession.vue
  - el-templo-app/src/modules/personalizada/pages/DurationPicker.vue
  - el-templo-app/src/modules/progression/components/PersonalizadaSection.vue
  - el-templo-app/src/modules/progression/composables/usePersonalizadaProgress.ts
  - el-templo-app/src/modules/progression/pages/MiCamino.vue
  - el-templo-app/src/boot/modules.ts
autonomous: true
requirements: [PERS-09, PERS-10, PERS-11, PERS-12]

must_haves:
  truths:
    - "Member app module folder is src/modules/personalizada/ with all files renamed"
    - "All stores, composables, pages, and components use personalizada naming"
    - "Route paths are /personalizada/* instead of /journey/*"
    - "UI text shows Clase Personalizada / Personalizadas in Spanish"
    - "Progression module references point to personalizada module"
    - "boot/modules.ts references personalizada paths (still commented out)"
    - "Zero remaining journey/Journey references in el-templo-app/src/"
    - "vue-tsc --noEmit passes on member app"
  artifacts:
    - path: "el-templo-app/src/modules/personalizada/index.ts"
      provides: "Module manifest with personalizada naming"
      contains: "name: 'personalizada'"
    - path: "el-templo-app/src/modules/personalizada/routes.ts"
      provides: "Routes under /personalizada/*"
      contains: "path: 'personalizada'"
    - path: "el-templo-app/src/modules/personalizada/stores/personalizadaStore.ts"
      provides: "Pinia store with personalizada naming"
      contains: "usePersonalizadaStore"
  key_links:
    - from: "el-templo-app/src/modules/personalizada/composables/usePersonalizadaApi.ts"
      to: "/personalizadas/*"
      via: "API endpoint paths"
      pattern: "/personalizadas/"
    - from: "el-templo-app/src/modules/progression/composables/usePersonalizadaProgress.ts"
      to: "el-templo-app/src/modules/personalizada/composables/usePersonalizadaApi.ts"
      via: "import"
      pattern: "from 'src/modules/personalizada/composables/usePersonalizadaApi'"
    - from: "el-templo-app/src/boot/modules.ts"
      to: "el-templo-app/src/modules/personalizada/index.ts"
      via: "commented import"
      pattern: "src/modules/personalizada"
---

<objective>
Rename the entire member app journey module to personalizada: folder, all internal files (types, store, composables, components, pages, routes), progression module references, and boot/modules.ts imports.

Purpose: Member app must match the renamed API endpoints from Phase 67 and align with the admin app rename. All code identifiers and UI text switch from Journey to Personalizada/Clase Personalizada.
Output: Module folder renamed from journey/ to personalizada/ with all 16 files renamed or updated, zero remaining journey references.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-personalizadas-frontend-rename/68-CONTEXT.md
@.planning/phases/67-personalizadas-backend-rename/67-02-SUMMARY.md

<interfaces>
<!-- API endpoints (renamed in Phase 67) that the member app calls: -->
GET  /personalizadas/metadata     (response: { personalizadas: PersonalizadaMetadata[] })
GET  /personalizadas/active       (response: PersonalizadaProgress | null)
POST /personalizadas/select       (body: { personalizadaType }, response: PersonalizadaProgress)
GET  /personalizadas/archived     (response: ArchivedPersonalizada[])
GET  /personalizadas/session      (params: week, day, duration, response: PersonalizadaSessionResponse)
POST /personalizadas/complete     (body: SessionCompleteData, response: { success, progress })

<!-- Note: API response keys changed in Phase 67: -->
<!-- - "journeys" -> "personalizadas" in metadata response -->
<!-- - "journey" -> "personalizada" in single-item responses -->

<!-- Progression module types that reference journey types (to be updated): -->

From src/modules/journey/types.ts:
JourneyProgress, ArchivedJourney, JourneyMetadata, JourneyType, JourneyTier
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename member app journey module folder and all internal files</name>
  <files>
    el-templo-app/src/modules/personalizada/types.ts,
    el-templo-app/src/modules/personalizada/index.ts,
    el-templo-app/src/modules/personalizada/routes.ts,
    el-templo-app/src/modules/personalizada/stores/personalizadaStore.ts,
    el-templo-app/src/modules/personalizada/composables/usePersonalizadaApi.ts,
    el-templo-app/src/modules/personalizada/composables/usePersonalizadaSession.ts,
    el-templo-app/src/modules/personalizada/components/PersonalizadaProgressBar.vue,
    el-templo-app/src/modules/personalizada/components/PersonalizadaProgressIndicator.vue,
    el-templo-app/src/modules/personalizada/pages/PersonalizadaSelection.vue,
    el-templo-app/src/modules/personalizada/pages/PersonalizadaOverview.vue,
    el-templo-app/src/modules/personalizada/pages/PersonalizadaSession.vue,
    el-templo-app/src/modules/personalizada/pages/DurationPicker.vue
  </files>
  <read_first>
    el-templo-app/src/modules/journey/types.ts,
    el-templo-app/src/modules/journey/index.ts,
    el-templo-app/src/modules/journey/routes.ts,
    el-templo-app/src/modules/journey/stores/journeyStore.ts,
    el-templo-app/src/modules/journey/composables/useJourneyApi.ts,
    el-templo-app/src/modules/journey/composables/useJourneySession.ts,
    el-templo-app/src/modules/journey/components/JourneyProgressBar.vue,
    el-templo-app/src/modules/journey/components/JourneyProgressIndicator.vue,
    el-templo-app/src/modules/journey/pages/JourneySelection.vue,
    el-templo-app/src/modules/journey/pages/JourneyOverview.vue,
    el-templo-app/src/modules/journey/pages/JourneySession.vue,
    el-templo-app/src/modules/journey/pages/DurationPicker.vue
  </read_first>
  <action>
**Strategy:** Create new `personalizada/` folder with all renamed files, then `git rm -r` the old `journey/` folder. This is cleaner than `git mv` on the whole folder because every file inside changes content too.

**Step 1: Create folder structure**

```
mkdir -p el-templo-app/src/modules/personalizada/{stores,composables,components,pages}
```

**Step 2: Create `types.ts`**
Copy from journey/types.ts with renames:

- Comment line 1: "Personalizada types for the member app"
- Comment line 3: "Mirrors API types from el-templo-api/src/modules/personalizadas/types.ts"
- `JourneyType` -> `PersonalizadaType`
- `JourneyTier` -> `PersonalizadaTier`
- `JourneyDuration` -> `PersonalizadaDuration`
- `JourneyProgress` -> `PersonalizadaProgress`
  - field `journeyType` -> `personalizadaType`
- `ArchivedJourney` -> `ArchivedPersonalizada`
  - field `journeyType` -> `personalizadaType`
- `JourneyMetadata` -> `PersonalizadaMetadata`
- `JourneySessionResponse` -> `PersonalizadaSessionResponse`
  - field `journeyType` -> `personalizadaType`
- Comment on line ~50: "Personalizada session response from API"

**Step 3: Create `index.ts`**
Copy from journey/index.ts with renames:

- Comments: "Personalizada module manifest" and "personalized session selection"
- `manifest.name: 'personalizada'` (was 'journey')
- `manifest.label: 'Personalizada'` (was 'Journey')
- `manifest.basePath: '/personalizada'` (was '/journey')
- Function: `registerModule` stays same name
- Comments: "Register personalizada module routes"

**Step 4: Create `routes.ts`**
Copy from journey/routes.ts with renames:

- All path strings: `'journey'` -> `'personalizada'`, `'journey/overview/:type'` -> `'personalizada/overview/:type'`, etc.
- Route names: `'personalizada-selection'` (was journey-selection), `'personalizada-overview'`, `'personalizada-duration'`, `'personalizada-session'`
- Component imports: `'./pages/PersonalizadaSelection.vue'`, `'./pages/PersonalizadaOverview.vue'`, `'./pages/DurationPicker.vue'` (same name), `'./pages/PersonalizadaSession.vue'`
- Comments: "Personalizada module route configuration"

**Step 5: Create `stores/personalizadaStore.ts`**
Copy from journey/stores/journeyStore.ts with renames:

- Import: `usePersonalizadaApi` from `'../composables/usePersonalizadaApi'`
- Type imports: `PersonalizadaType, PersonalizadaDuration, PersonalizadaProgress, PersonalizadaMetadata, PersonalizadaSessionResponse`
- Logger: `createLogger('PersonalizadaStore')`
- Store: `usePersonalizadaStore = defineStore('personalizada', ...)` (was 'journey')
- Variable: `personalizadaMetadata` (was journeyMetadata)
- Computed: `hasActivePersonalizada` (was hasActiveJourney), `activePersonalizadaName` (was activeJourneyName), `activePersonalizadaTier` (was activeJourneyTier)
- Functions: `fetchMetadata`, `fetchActivePersonalizada` (was fetchActiveJourney), `selectPersonalizada` (was selectJourney), `fetchSession`, `completePersonalizadaSession` (was completeJourneySession)
- All `.journeyType` property accesses -> `.personalizadaType`
- Error messages: "Error cargando personalizadas" (was "Error cargando journeys"), "Error cargando tu personalizada", "Error seleccionando personalizada"
- Log messages: all "journey" -> "personalizada"
- `activeJourney` ref stays as `activePersonalizada`... Actually, keep variable names for internal refs short: `activePersonalizada` (was activeJourney)

**Step 6: Create `composables/usePersonalizadaApi.ts`**
Copy from journey/composables/useJourneyApi.ts with renames:

- Logger: `createLogger('PersonalizadaApi')`
- Interface: `SessionCompleteData.personalizadaType` field (was journeyType), typed as `PersonalizadaType`
- `SessionCompleteResponse.progress` typed as `PersonalizadaProgress`
- Function: `usePersonalizadaApi` (was useJourneyApi)
- API paths: ALL `/journeys/` -> `/personalizadas/`
  - `'/personalizadas/metadata'` response type: `{ personalizadas: PersonalizadaMetadata[] }` (was `{ journeys: ... }`)
  - Return: `response.data.personalizadas` (was `response.data.journeys`)
  - `'/personalizadas/active'`
  - `'/personalizadas/select'`
  - `'/personalizadas/archived'`
  - `'/personalizadas/session'`
  - `'/personalizadas/complete'`
- Function names: `getActivePersonalizada` (was getActiveJourney), `selectPersonalizada` (was selectJourney), `getArchivedPersonalizadas` (was getArchivedJourneys)
- Log messages: all "journey" -> "personalizada"

**Step 7: Create `composables/usePersonalizadaSession.ts`**
Copy from journey/composables/useJourneySession.ts with renames:

- Type imports from `'../types'`: `PersonalizadaType, PersonalizadaDuration, PersonalizadaSessionResponse`
- Logger: `createLogger('PersonalizadaSession')`
- Exported function: `getBlocksForDuration` stays same name (generic utility)
- Exported function: `usePersonalizadaSession` (was useJourneySession)
- Parameter type: `session: PersonalizadaSessionResponse`
- Comments: all "journey" -> "personalizada"
- Log message: "Personalizada session initialized"

**Step 8: Create `components/PersonalizadaProgressBar.vue`**
Copy from journey/components/JourneyProgressBar.vue with renames:

- CSS class prefix: `personalizada-progress` (was `journey-progress`) throughout template and style
- No script changes needed (no journey references in script)

**Step 9: Create `components/PersonalizadaProgressIndicator.vue`**
Copy from journey/components/JourneyProgressIndicator.vue with renames:

- Type imports: `PersonalizadaDuration, PersonalizadaProgress` from `'../types'`
- CSS class prefix: `personalizada-progress-indicator` (was `journey-progress-indicator`) throughout template and style
- Props interface: `PersonalizadaProgress` type for `progress` prop
- Comment: "Personalizada display name"

**Step 10: Create `pages/PersonalizadaSelection.vue`**
Copy from journey/pages/JourneySelection.vue with renames:

- CSS class: `personalizada-selection-page` (was `journey-selection-page`)
- Logger: `createLogger('PersonalizadaSelection')`
- Store: `usePersonalizadaStore` from `'../stores/personalizadaStore'`
- Type imports: `PersonalizadaType, PersonalizadaTier` from `'../types'`
- Store usage: `personalizadaStore.hasActivePersonalizada`, `.activePersonalizadaName`, `.personalizadaMetadata`, `.activePersonalizada`, `.loading`, `.error`, `.fetchMetadata()`, `.fetchActivePersonalizada()`
- UI text: `"Elige tu Clase Personalizada"` (was "Elige tu Journey")
- Active banner: `"Tu Personalizada Actual"` (was "Tu Journey Actual")
- `onContinueActivePersonalizada` (was onContinueActiveJourney) -> navigates to `'/personalizada/duration'`
- `onSelectPersonalizada` (was onSelectJourney) -> navigates to `` `/personalizada/overview/${type}` ``
- `isActivePersonalizada` (was isActiveJourney) checks `.personalizadaType`
- `getPersonalizadasByTier` (was getJourneysByTier)
- CSS: `.journey-card` -> `.personalizada-card`, `.tier-journeys` -> `.tier-personalizadas`

**Step 11: Create `pages/PersonalizadaOverview.vue`**
Copy from journey/pages/JourneyOverview.vue with renames:

- CSS class: `personalizada-overview-page`
- Logger: `createLogger('PersonalizadaOverview')`
- Store: `usePersonalizadaStore`
- Types: `PersonalizadaMetadata, PersonalizadaType, PersonalizadaTier`
- Store usage: `.personalizadaMetadata`, `.activePersonalizada`, `.selectPersonalizada()`, `.fetchMetadata()`, `.fetchActivePersonalizada()`
- UI text: "Personalizada no encontrada" (was "Journey no encontrado"), "Volver" links to `'/personalizada'`
- Confirm button: `"Elegir esta Personalizada"` (was "Elegir este Journey")
- Notify: `Personalizada "${name}" seleccionada` (was `Journey "${name}" seleccionado`)
- Error notify: `"Error al seleccionar la personalizada"` (was `"el journey"`)
- All router.push paths: `/journey/` -> `/personalizada/`
- CSS class names: `.journey-name` -> `.personalizada-name`, `.journey-description` -> `.personalizada-description`, `.journey-overview-page` -> `.personalizada-overview-page`

**Step 12: Create `pages/PersonalizadaSession.vue`**
Copy from journey/pages/JourneySession.vue with renames:

- CSS class: `personalizada-session` (was `journey-session`)
- Logger: `createLogger('PersonalizadaSession')`
- Component import: `PersonalizadaProgressIndicator` from `'../components/PersonalizadaProgressIndicator.vue'`
- Composable: `usePersonalizadaSession` from `'../composables/usePersonalizadaSession'`
- Store: `usePersonalizadaStore` from `'../stores/personalizadaStore'`
- Type: `PersonalizadaSessionResponse` from `'../types'`
- Store usage: `.hasActivePersonalizada`, `.activePersonalizada`, `.activePersonalizadaName`, `.selectedDuration`, `.fetchActivePersonalizada()`, `.currentSession`, `.currentWeek`, `.setCurrentWeek()`, `.personalizadaMetadata`, `.fetchMetadata()`, `.fetchSession()`, `.completePersonalizadaSession()`
- Ref: `session` typed as `PersonalizadaSessionResponse | null`
- Player: `usePersonalizadaSession(newSession, newDuration)`
- Route names: all `'journey-'` -> `'personalizada-'` (selection, duration, session)
- Log messages: all "journey" -> "personalizada"
- CSS: all `.journey-session` -> `.personalizada-session`

**Step 13: Create `pages/DurationPicker.vue`**
Copy from journey/pages/DurationPicker.vue with renames:

- Logger: `createLogger('DurationPicker')` (stays same)
- Store: `usePersonalizadaStore` from `'../stores/personalizadaStore'`
- Type: `PersonalizadaDuration` from `'../types'`
- Store usage: `.hasActivePersonalizada`, `.activePersonalizada`, `.activePersonalizadaName`, `.fetchActivePersonalizada()`, `.personalizadaMetadata`, `.fetchMetadata()`, `.setDuration()`
- UI text: `"Tu Personalizada"` (was "Tu Journey")
- Route paths: `'/personalizada'` (was '/journey'), `'/personalizada/session'` (was '/journey/session')
- Log: "No active personalizada" (was "No active journey")

**Step 14: Delete old folder**

```
git rm -r el-templo-app/src/modules/journey/
```

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-app && ls src/modules/personalizada/stores/personalizadaStore.ts src/modules/personalizada/composables/usePersonalizadaApi.ts src/modules/personalizada/routes.ts && echo "--- Files exist ---" && grep -r "journey\|Journey" src/modules/personalizada/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Directory `el-templo-app/src/modules/personalizada/` exists with stores/, composables/, components/, pages/ subdirectories
    - Directory `el-templo-app/src/modules/journey/` does NOT exist
    - `personalizada/types.ts` contains `export type PersonalizadaType =` and `PersonalizadaSessionResponse`
    - `personalizada/index.ts` contains `name: 'personalizada'` and `basePath: '/personalizada'`
    - `personalizada/routes.ts` contains `path: 'personalizada'` and `name: 'personalizada-selection'`
    - `personalizada/stores/personalizadaStore.ts` contains `usePersonalizadaStore` and `defineStore('personalizada'`
    - `personalizada/composables/usePersonalizadaApi.ts` contains `'/personalizadas/metadata'` and `response.data.personalizadas`
    - `personalizada/composables/usePersonalizadaSession.ts` contains `usePersonalizadaSession`
    - `personalizada/pages/PersonalizadaSelection.vue` contains `Elige tu Clase Personalizada`
    - `personalizada/pages/PersonalizadaOverview.vue` contains `Elegir esta Personalizada`
    - `personalizada/pages/PersonalizadaSession.vue` contains `personalizada-session` class
    - `personalizada/pages/DurationPicker.vue` contains `'/personalizada/session'`
    - `grep -r "journey\|Journey" el-templo-app/src/modules/personalizada/` returns 0 results
  </acceptance_criteria>
  <done>Member app module folder is src/modules/personalizada/ with all 12 internal files renamed, route paths are /personalizada/*, UI text shows Spanish "Clase Personalizada", zero journey references in the module</done>
</task>

<task type="auto">
  <name>Task 2: Update progression module and boot/modules.ts references</name>
  <files>
    el-templo-app/src/modules/progression/components/PersonalizadaSection.vue,
    el-templo-app/src/modules/progression/composables/usePersonalizadaProgress.ts,
    el-templo-app/src/modules/progression/pages/MiCamino.vue,
    el-templo-app/src/boot/modules.ts
  </files>
  <read_first>
    el-templo-app/src/modules/progression/components/JourneySection.vue,
    el-templo-app/src/modules/progression/composables/useJourneyProgress.ts,
    el-templo-app/src/modules/progression/pages/MiCamino.vue,
    el-templo-app/src/boot/modules.ts
  </read_first>
  <action>
**Step 1: Create `progression/components/PersonalizadaSection.vue`** (new file, delete old)

Copy from JourneySection.vue with renames:

- Type imports: `PersonalizadaProgress, ArchivedPersonalizada, PersonalizadaMetadata, PersonalizadaTier` from `'src/modules/personalizada/types'` (was `src/modules/journey/types`)
- Logger: `createLogger('PersonalizadaSection')`
- Props interface: `activePersonalizada: PersonalizadaProgress | null`, `archivedPersonalizadas: ArchivedPersonalizada[]`, `allMetadata: PersonalizadaMetadata[]`
- Computed: `activePersonalizadaName`, `activePersonalizadaTierLabel`
- Functions: `getPersonalizadaName`, `getPersonalizadaTierLabel`
- `.journeyType` accesses -> `.personalizadaType`
- `onConfirmChange` log: "User confirmed personalizada change"
- Router push: `'/personalizada'` (was '/journey')

Template UI text:

- "Comienza tu Clase Personalizada" (was "Comienza tu Journey personalizado")
- "Elige una ruta de entrenamiento..." (stays same -- it's a description)
- "Elegir Personalizada" (was "Elegir Journey")
- Button `to="/personalizada"` (was "/journey")
- "Cambiar Personalizada" (was "Cambiar Journey")
- "Historial de Personalizadas" (was "Historial de Journeys")
- Dialog title: "Cambiar Personalizada"
- Dialog text: "Cambiar de Personalizada reiniciara tu progreso actual. Tu historial sera guardado."

CSS class prefix: `personalizada-section` (was `journey-section`) throughout all template class references AND style block. This includes:

- `.personalizada-section` (root)
- `.personalizada-section__loading`, `__error`, `__error-text`
- `.personalizada-section__active-card`, `__active-header`
- `.personalizada-section__personalizada-name` (was `__journey-name`)
- `.personalizada-section__tier-badge`, `__icon`, `__since`
- `.personalizada-section__semanas`, `__semana-row`, etc.
- `.personalizada-section__change-btn`
- `.personalizada-section__prompt-card`, `__prompt-content`, etc.
- `.personalizada-section__archived`, `__archived-title`, `__archived-card`, etc.
- `.personalizada-section__dialog`, `__dialog-title`, `__dialog-text`, `__dialog-confirm`

Then `git rm el-templo-app/src/modules/progression/components/JourneySection.vue`.

**Step 2: Create `progression/composables/usePersonalizadaProgress.ts`** (new file, delete old)

Copy from useJourneyProgress.ts with renames:

- Import: `usePersonalizadaApi` from `'src/modules/personalizada/composables/usePersonalizadaApi'`
- Types: `PersonalizadaProgress, ArchivedPersonalizada, PersonalizadaMetadata` from `'src/modules/personalizada/types'`
- Logger: `createLogger('PersonalizadaProgress')`
- Interface: `PersonalizadaProgressDisplay` (was JourneyProgressDisplay)
  - `activePersonalizada: PersonalizadaProgress | null` (was activeJourney)
  - `archivedPersonalizadas: ArchivedPersonalizada[]` (was archivedJourneys)
  - `allMetadata: PersonalizadaMetadata[]` (stays same)
- Function: `usePersonalizadaProgress` (was useJourneyProgress)
- Refs: `activePersonalizada` (was activeJourney), `archivedPersonalizadas` (was archivedJourneys), `allMetadata`
- `apiInstance` typed as `ReturnType<typeof usePersonalizadaApi> | null`
- `fetchPersonalizadaData` (was fetchJourneyData)
  - `const api = usePersonalizadaApi()`
  - `api.getActivePersonalizada()` (was getActiveJourney)
  - `api.getArchivedPersonalizadas()` (was getArchivedJourneys)
  - `api.getMetadata()`
- Log: "Personalizada progress loaded"
- Error: "Error cargando datos de personalizada"
- `getMetadataForType` function parameter name: `personalizadaType` (was journeyType)

Then `git rm el-templo-app/src/modules/progression/composables/useJourneyProgress.ts`.

**Step 3: Update `progression/pages/MiCamino.vue`**

Update commented-out imports (these stay commented but reference new paths):

- Line ~130 template comment: "Personalizada section hidden from this release" (was "Journey section")
- Line ~144 script comment: "Personalizada progress and archived history" (was "Journey progress")
- Line ~152: `// import { usePersonalizadaProgress } from '../composables/usePersonalizadaProgress'` (was useJourneyProgress)
- Line ~158: `// import PersonalizadaSection from '../components/PersonalizadaSection.vue'` (was JourneySection)

**Step 4: Update `boot/modules.ts`**

Update commented-out imports to reference personalizada paths:

- Line ~10 comment: "Personalizada module hidden from this release" (was "Journey module")
- Line ~11: `// import { manifest as personalizadaManifest, registerModule as registerPersonalizada } from 'src/modules/personalizada'` (was journey)
- Line ~25: `// personalizadaManifest, // Hidden from this release` (was journeyManifest)
- Line ~35: `// registerPersonalizada(router) // Hidden from this release` (was registerJourney)

**Step 5: Delete old files**

```
git rm el-templo-app/src/modules/progression/components/JourneySection.vue
git rm el-templo-app/src/modules/progression/composables/useJourneyProgress.ts
```

**Step 6: Run full type check**

```
cd el-templo-app && npx vue-tsc --noEmit
```

**Step 7: Final grep sweep**

```
grep -rn "journey\|Journey\|JOURNEY" el-templo-app/src/ --include="*.ts" --include="*.vue"
```

Must return 0 results. If any remain, fix them.
</action>
<verify>
<automated>cd /home/franco/projects/el-templo/el-templo-app && npx vue-tsc --noEmit 2>&1 | tail -20 && echo "---" && grep -rn "journey\|Journey\|JOURNEY" src/ --include="_.ts" --include="_.vue" | wc -l</automated>
</verify>
<acceptance_criteria> - File `progression/components/PersonalizadaSection.vue` exists and contains `Comienza tu Clase Personalizada` - File `progression/components/PersonalizadaSection.vue` contains `from 'src/modules/personalizada/types'` - File `progression/components/PersonalizadaSection.vue` contains `to="/personalizada"` - File `progression/components/JourneySection.vue` does NOT exist - File `progression/composables/usePersonalizadaProgress.ts` exists and contains `usePersonalizadaProgress` - File `progression/composables/usePersonalizadaProgress.ts` contains `from 'src/modules/personalizada/composables/usePersonalizadaApi'` - File `progression/composables/useJourneyProgress.ts` does NOT exist - File `progression/pages/MiCamino.vue` contains `usePersonalizadaProgress` (commented) - File `progression/pages/MiCamino.vue` contains `PersonalizadaSection` (commented) - File `boot/modules.ts` contains `src/modules/personalizada` (commented import) - File `boot/modules.ts` contains `personalizadaManifest` (commented) - `grep -rn "journey\|Journey\|JOURNEY" el-templo-app/src/ --include="*.ts" --include="*.vue"` returns 0 results - `vue-tsc --noEmit` exits with code 0
</acceptance_criteria>
<done>Progression module references point to personalizada module, boot/modules.ts references personalizada paths (commented), vue-tsc passes, zero journey references in member app src/</done>
</task>

</tasks>

<verification>
1. `grep -rn "journey\|Journey\|JOURNEY" el-templo-app/src/ --include="*.ts" --include="*.vue"` returns 0 results
2. `cd el-templo-app && npx vue-tsc --noEmit` passes
3. Old folder `src/modules/journey/` does not exist
4. Old files `progression/components/JourneySection.vue` and `progression/composables/useJourneyProgress.ts` do not exist
5. Route paths are all `/personalizada/*`
6. API calls target `/personalizadas/*` endpoints
</verification>

<success_criteria>

- Member app module folder is src/modules/personalizada/ with all files renamed
- All route paths are /personalizada/\*
- All API calls target /personalizadas/\* endpoints
- UI text shows "Clase Personalizada" / "Personalizadas" in Spanish
- Progression module references point to personalizada module
- boot/modules.ts references personalizada paths (still commented)
- TypeScript compilation passes
- Zero journey references in el-templo-app/src/
  </success_criteria>

<output>
After completion, create `.planning/phases/68-personalizadas-frontend-rename/68-02-SUMMARY.md`
</output>
