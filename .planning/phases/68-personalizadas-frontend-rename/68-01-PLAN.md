---
phase: 68-personalizadas-frontend-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-admin/src/types/personalizada.ts
  - el-templo-admin/src/composables/usePersonalizadasAdminApi.ts
  - el-templo-admin/src/types/session.ts
  - el-templo-admin/src/composables/useSessionsApi.ts
  - el-templo-admin/src/pages/GeneratePage.vue
  - el-templo-admin/src/pages/SessionsPage.vue
  - el-templo-admin/src/pages/SessionEditPage.vue
  - el-templo-admin/src/pages/AlumnoDetailPage.vue
autonomous: true
requirements: [PERS-08, PERS-10, PERS-12]

must_haves:
  truths:
    - "Admin type file is named personalizada.ts with PersonalizadaType, PersonalizadaMetadata, MemberPersonalizadaInfo, etc."
    - "Admin composable is named usePersonalizadasAdminApi.ts hitting /admin/personalizadas/* endpoints"
    - "All admin pages reference personalizada types and composable, with Spanish UI text"
    - "Zero remaining journey/Journey references in el-templo-admin/src/"
    - "vue-tsc --noEmit passes on admin app"
  artifacts:
    - path: "el-templo-admin/src/types/personalizada.ts"
      provides: "Renamed type definitions"
      contains: "PersonalizadaType"
    - path: "el-templo-admin/src/composables/usePersonalizadasAdminApi.ts"
      provides: "Renamed admin API composable"
      contains: "usePersonalizadasAdminApi"
  key_links:
    - from: "el-templo-admin/src/pages/GeneratePage.vue"
      to: "el-templo-admin/src/composables/usePersonalizadasAdminApi.ts"
      via: "import usePersonalizadasAdminApi"
      pattern: "from 'src/composables/usePersonalizadasAdminApi'"
    - from: "el-templo-admin/src/pages/GeneratePage.vue"
      to: "el-templo-admin/src/types/personalizada.ts"
      via: "import types"
      pattern: "from 'src/types/personalizada'"
    - from: "el-templo-admin/src/composables/usePersonalizadasAdminApi.ts"
      to: "/admin/personalizadas/*"
      via: "API endpoint paths"
      pattern: "/admin/personalizadas/"
---

<objective>
Rename all journey references to personalizada in the admin app (el-templo-admin): type file, composable, session types, and all pages that reference them.

Purpose: Admin app must match the renamed API endpoints from Phase 67. All type names, composable names, variable names, and UI-visible text switch from Journey to Personalizada (Spanish).
Output: 8 files renamed or updated in el-templo-admin/src/ with zero remaining journey references.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-personalizadas-frontend-rename/68-CONTEXT.md
@.planning/phases/67-personalizadas-backend-rename/67-02-SUMMARY.md

<interfaces>
<!-- API endpoints are already renamed in Phase 67. Admin composable must hit these paths: -->
POST /admin/personalizadas/generate
GET  /admin/personalizadas/members
GET  /admin/personalizadas/members/:userId

<!-- Session types that reference personalizada (already renamed in API): -->

- SessionFilter.personalizadaType (was journeyType)
- AdminSessionSummary.personalizadaType (was journeyType)
- Route query param: personalizadaType (was journeyType)
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Rename admin type file and composable</name>
  <files>
    el-templo-admin/src/types/personalizada.ts,
    el-templo-admin/src/composables/usePersonalizadasAdminApi.ts,
    el-templo-admin/src/types/session.ts,
    el-templo-admin/src/composables/useSessionsApi.ts
  </files>
  <read_first>
    el-templo-admin/src/types/journey.ts,
    el-templo-admin/src/composables/useJourneyAdminApi.ts,
    el-templo-admin/src/types/session.ts,
    el-templo-admin/src/composables/useSessionsApi.ts
  </read_first>
  <action>
**Step 1: Create `el-templo-admin/src/types/personalizada.ts`** (new file, then delete old)

Copy content from `journey.ts` with these renames:

- `JourneyType` -> `PersonalizadaType`
- `JourneyTier` -> `PersonalizadaTier`
- `JourneyDuration` -> `PersonalizadaDuration`
- `JourneyMetadata` -> `PersonalizadaMetadata`
- `MemberJourneyInfo` -> `MemberPersonalizadaInfo`
  - field `journeyType` -> `personalizadaType`
  - field `journeyName` -> `personalizadaName`
- `MemberJourneyDetail` -> `MemberPersonalizadaDetail`
  - nested `active.journeyType` -> `active.personalizadaType`
  - nested `archived[].journeyType` -> `archived[].personalizadaType`
  - `journeyStats` -> `personalizadaStats`
  - nested `completions[].journeyType` -> `completions[].personalizadaType`
- `JourneyGenerateResult` -> `PersonalizadaGenerateResult`
- `ALL_JOURNEY_TYPES` -> `ALL_PERSONALIZADA_TYPES`
- `JOURNEY_TIER_MAP` -> `PERSONALIZADA_TIER_MAP`
- `JOURNEY_TYPE_LABELS` -> `PERSONALIZADA_TYPE_LABELS`
- `JOURNEY_TIER_LABELS` -> `PERSONALIZADA_TIER_LABELS`
- `JOURNEY_TIER_COLORS` -> `PERSONALIZADA_TIER_COLORS`
- Comment on line 122 "Color for journey type badges" -> "Color for personalizada type badges"

Then `git rm el-templo-admin/src/types/journey.ts`.

**Step 2: Create `el-templo-admin/src/composables/usePersonalizadasAdminApi.ts`** (new file, then delete old)

Copy content from `useJourneyAdminApi.ts` with these renames:

- Import types from `'src/types/personalizada'` (not `journey`)
- `PersonalizadaType` (was JourneyType)
- `PersonalizadaGenerateResult` (was JourneyGenerateResult)
- `MemberPersonalizadaInfo` (was MemberJourneyInfo)
- `MemberPersonalizadaDetail` (was MemberJourneyDetail)
- `usePersonalizadasAdminApi` (was useJourneyAdminApi)
- `generatePersonalizadaSessions` (was generateJourneySessions)
- `personalizadaType` parameter (was journeyType)
- API paths: `'/admin/personalizadas/generate'` (was `/admin/journeys/generate`)
- API paths: `'/admin/personalizadas/members'` (was `/admin/journeys/members`)
- API paths: `` `/admin/personalizadas/members/${userId}` `` (was `/admin/journeys/members/${userId}`)
- Error message: `'Error generando sesiones de personalizada'` (was `'Error generando sesiones de journey'`)

Then `git rm el-templo-admin/src/composables/useJourneyAdminApi.ts`.

**Step 3: Update `el-templo-admin/src/types/session.ts`**

Line 14: `journeyType: string | null` -> `personalizadaType: string | null`
Lines 27-28: Comment about journey -> personalizada, field name `journeyType` -> `personalizadaType`

**Step 4: Update `el-templo-admin/src/composables/useSessionsApi.ts`**

Line 48: `journeyType?: string` -> `personalizadaType?: string`
Line 53: The params variable destructuring/assignment: `journeyType` -> `personalizadaType`
Line 54: `if (journeyType) params.journeyType = journeyType;` -> `if (personalizadaType) params.personalizadaType = personalizadaType;`
</action>
<verify>
<automated>cd /home/franco/projects/el-templo/el-templo-admin && grep -r "journey\|Journey\|JOURNEY" src/types/ src/composables/ --include="_.ts" | grep -v node_modules | wc -l</automated>
</verify>
<acceptance_criteria> - File `el-templo-admin/src/types/personalizada.ts` exists and contains `export type PersonalizadaType =` - File `el-templo-admin/src/types/personalizada.ts` contains `ALL_PERSONALIZADA_TYPES` - File `el-templo-admin/src/types/personalizada.ts` contains `MemberPersonalizadaDetail` - File `el-templo-admin/src/composables/usePersonalizadasAdminApi.ts` exists and contains `export function usePersonalizadasAdminApi()` - File `el-templo-admin/src/composables/usePersonalizadasAdminApi.ts` contains `'/admin/personalizadas/generate'` - File `el-templo-admin/src/composables/usePersonalizadasAdminApi.ts` contains `from 'src/types/personalizada'` - File `el-templo-admin/src/types/session.ts` contains `personalizadaType: string | null` - File `el-templo-admin/src/composables/useSessionsApi.ts` contains `personalizadaType` (not journeyType) - Old files `src/types/journey.ts` and `src/composables/useJourneyAdminApi.ts` do NOT exist - `grep -r "journey\|Journey" el-templo-admin/src/types/ el-templo-admin/src/composables/ --include="_.ts"` returns 0 results
</acceptance_criteria>
<done>Admin type file is personalizada.ts with all types renamed, composable is usePersonalizadasAdminApi.ts hitting /admin/personalizadas/\* endpoints, session types use personalizadaType field</done>
</task>

<task type="auto">
  <name>Task 2: Update all admin pages from journey to personalizada</name>
  <files>
    el-templo-admin/src/pages/GeneratePage.vue,
    el-templo-admin/src/pages/SessionsPage.vue,
    el-templo-admin/src/pages/SessionEditPage.vue,
    el-templo-admin/src/pages/AlumnoDetailPage.vue
  </files>
  <read_first>
    el-templo-admin/src/pages/GeneratePage.vue,
    el-templo-admin/src/pages/SessionsPage.vue,
    el-templo-admin/src/pages/SessionEditPage.vue,
    el-templo-admin/src/pages/AlumnoDetailPage.vue
  </read_first>
  <action>
**GeneratePage.vue** -- Update all journey references:

Imports (around line 288-298):

- `import { usePersonalizadasAdminApi } from 'src/composables/usePersonalizadasAdminApi'` (was useJourneyAdminApi)
- `type PersonalizadaType, type PersonalizadaTier, type PersonalizadaGenerateResult` from `'src/types/personalizada'` (was journey)
- `ALL_PERSONALIZADA_TYPES, PERSONALIZADA_TYPE_LABELS, PERSONALIZADA_TIER_LABELS, PERSONALIZADA_TIER_COLORS, PERSONALIZADA_TIER_MAP` (was ALL_JOURNEY_TYPES, etc.)

Variable names (lines ~303-652):

- `personalizadaApi` (was journeyApi) = `usePersonalizadasAdminApi()`
- `personalizadaWeek` (was journeyWeek)
- `selectedPersonalizadaTypes` (was selectedJourneyTypes), typed as `PersonalizadaType[]`, initialized with `ALL_PERSONALIZADA_TYPES`
- `personalizadaRegenerate` (was journeyRegenerate)
- `personalizadaGenerating` (was journeyGenerating)
- `personalizadaTypeLoading` (was journeyTypeLoading), typed as `PersonalizadaType | null`
- `personalizadaResults` (was journeyResults), typed as `Record<string, PersonalizadaGenerateResult>`
- `isPersonalizadaFutureWeek` (was isJourneyFutureWeek)

Interface rename:

- `PersonalizadaTierGroup` (was JourneyTierGroup) with fields typed as `PersonalizadaTier` and `PersonalizadaType`

Computed/functions:

- `personalizadaTiers` (was journeyTiers), typed as `PersonalizadaTierGroup[]`
- `allPersonalizadaTypesList` (was allJourneyTypesList)
- `togglePersonalizadaType` (was toggleJourneyType)
- `handlePersonalizadaGenerateAll` (was handleJourneyGenerateAll)
- `handlePersonalizadaGenerateSingle` (was handleJourneyGenerateSingle)

All internal references to these variables in function bodies must update:

- `journeyApi.generateJourneySessions` -> `personalizadaApi.generatePersonalizadaSessions`
- Error log strings: "journey" -> "personalizada"
- Notify messages: "sesiones de journey" -> "sesiones de personalizada"

Template references (lines ~148-266):

- All `v-model`, `v-if`, `v-for`, `@click`, `:loading`, `:disable` bindings reference new variable names
- Line ~175 UI text: "Tipos de Journey" -> "Tipos de Personalizada"

**SessionsPage.vue** -- Update all journey references:

Imports (around line 321):

- `type PersonalizadaType` from `'src/types/personalizada'` (was journey)
- `ALL_PERSONALIZADA_TYPES, PERSONALIZADA_TYPE_LABELS, PERSONALIZADA_TIER_MAP, PERSONALIZADA_TIER_COLORS` (was JOURNEY\_\*)

Variable names (lines ~532-650):

- `personalizadaWeek` (was journeyWeek)
- `selectedPersonalizadaTab` (was selectedJourneyTab), typed as `PersonalizadaType`
- `personalizadaSessions` (was journeySessions)
- `personalizadaLoading` (was journeyLoading)
- `showPersonalizadaDatePicker` (was showJourneyDatePicker)

Interface renames:

- `PersonalizadaDayLevelStatus` (was JourneyDayLevelStatus)
- `PersonalizadaDayGroup` (was JourneyDayGroup)

Computed/functions:

- `personalizadaDayGroups` (was journeyDayGroups)
- `loadPersonalizadaSessions` (was loadJourneySessions)
- `editPersonalizadaDay` (was editJourneyDay)
- `handleBulkApprovePersonalizadaDay` (was handleBulkApproveJourneyDay)
- `prevPersonalizadaWeek` (was prevJourneyWeek)
- `nextPersonalizadaWeek` (was nextJourneyWeek)
- `onPersonalizadaDatePicked` (was onJourneyDatePicked)
- `getPersonalizadaBadgeColor` (was getJourneyBadgeColor)
- `getPersonalizadaLabel` (was getJourneyLabel)

Session filter params:

- Line ~399: `journeyType: 'null'` -> `personalizadaType: 'null'`

Query params in route pushes:

- `journeyType: selectedJourneyTab.value` -> `personalizadaType: selectedPersonalizadaTab.value`
- `personalizadaType` in editPersonalizadaDay

Watcher (lines ~702-714):

- Tab watch: `personalizadaSessions.value.length === 0` check and `loadPersonalizadaSessions()` calls

Template: All `v-model`, `v-for`, `v-if`, `@click`, `:color`, `:label` bindings reference new variable names. Line ~280-281: UI text `getJourneyLabel(selectedJourneyTab)` -> `getPersonalizadaLabel(selectedPersonalizadaTab)`.

**SessionEditPage.vue** -- Update journey references:

Imports (around line 139-140):

- `type PersonalizadaType` from `'src/types/personalizada'` (was journey)
- `PERSONALIZADA_TIER_COLORS, PERSONALIZADA_TIER_MAP, PERSONALIZADA_TYPE_LABELS` (was JOURNEY\_\*)

Variable (line 179):

- `personalizadaType` (was journeyType), computed from `route.query.personalizadaType` (was `route.query.journeyType`)

Template (lines 24-26):

- `:color="PERSONALIZADA_TIER_COLORS[PERSONALIZADA_TIER_MAP[personalizadaType as PersonalizadaType]]"`
- `:label="PERSONALIZADA_TYPE_LABELS[personalizadaType as PersonalizadaType]"`

Back navigation query params (~line 334):

- `if (personalizadaType.value) query.tab = 'personalizadas'`

All internal references to `journeyType.value` -> `personalizadaType.value`.

**AlumnoDetailPage.vue** -- Update journey references:

Imports (around line 375-392):

- `import { usePersonalizadasAdminApi } from 'src/composables/usePersonalizadasAdminApi'` (was useJourneyAdminApi)
- `type PersonalizadaType, type PersonalizadaTier, type MemberPersonalizadaDetail` from `'src/types/personalizada'` (was journey)
- `PERSONALIZADA_TYPE_LABELS, PERSONALIZADA_TIER_MAP, PERSONALIZADA_TIER_COLORS, PERSONALIZADA_TIER_LABELS` (was JOURNEY\_\*)

Variable names (lines ~400-520):

- `personalizadaApi` (was journeyApi) = `usePersonalizadasAdminApi()`
- `personalizadaDetail` (was journeyDetail), typed as `MemberPersonalizadaDetail | null`
- `personalizadaLoading` (was journeyLoading)

Functions:

- `personalizadaLabel` (was journeyLabel)
- `personalizadaBadgeColor` (was journeyBadgeColor)
- `tierLabel` stays same but uses `PERSONALIZADA_TIER_MAP` and `PERSONALIZADA_TIER_LABELS`
- `loadPersonalizadaDetail` (was loadJourneyDetail)
- Error log: "journey detail" -> "personalizada detail"

Template (lines ~113-325):

- All `v-if="personalizadaLoading"`, `v-else-if="personalizadaDetail"`, etc.
- UI text: "Journey Activo" -> "Personalizada Activa"
- "Sin journey activo" -> "Sin personalizada activa"
- "Progreso Journey" -> "Progreso Personalizada"
- "Sesiones journey" -> "Sesiones personalizada"
- "Journeys Anteriores" -> "Personalizadas Anteriores"
- All `.journeyType` accesses -> `.personalizadaType`
- All `journeyDetail.` -> `personalizadaDetail.`
- `personalizadaDetail.personalizadaStats` (was journeyStats)
- `journeyLabel(...)` -> `personalizadaLabel(...)`, `journeyBadgeColor(...)` -> `personalizadaBadgeColor(...)`
  </action>
  <verify>
  <automated>cd /home/franco/projects/el-templo/el-templo-admin && npx vue-tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria> - `grep -r "journey\|Journey\|JOURNEY" el-templo-admin/src/ --include="*.vue" --include="*.ts" | grep -v node_modules` returns 0 results - `el-templo-admin/src/pages/GeneratePage.vue` contains `usePersonalizadasAdminApi` and `from 'src/types/personalizada'` - `el-templo-admin/src/pages/GeneratePage.vue` contains `personalizadaWeek` (not journeyWeek) - `el-templo-admin/src/pages/GeneratePage.vue` contains `Tipos de Personalizada` (not Journey) - `el-templo-admin/src/pages/SessionsPage.vue` contains `personalizadaSessions` (not journeySessions) - `el-templo-admin/src/pages/SessionsPage.vue` contains `personalizadaType: 'null'` (not journeyType) - `el-templo-admin/src/pages/SessionEditPage.vue` contains `PERSONALIZADA_TIER_COLORS` (not JOURNEY_TIER_COLORS) - `el-templo-admin/src/pages/SessionEditPage.vue` contains `route.query.personalizadaType` (not journeyType) - `el-templo-admin/src/pages/AlumnoDetailPage.vue` contains `Personalizada Activa` (not Journey Activo) - `el-templo-admin/src/pages/AlumnoDetailPage.vue` contains `personalizadaDetail` (not journeyDetail) - `el-templo-admin/src/pages/AlumnoDetailPage.vue` contains `Personalizadas Anteriores` (not Journeys Anteriores) - `vue-tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>All 4 admin pages updated: GeneratePage, SessionsPage, SessionEditPage, AlumnoDetailPage use personalizada naming and Spanish UI text. Zero journey references in admin src/.</done>
  </task>

</tasks>

<verification>
1. `grep -rn "journey\|Journey\|JOURNEY" el-templo-admin/src/ --include="*.ts" --include="*.vue" | grep -v node_modules` returns 0 results
2. `cd el-templo-admin && npx vue-tsc --noEmit` passes
3. Old files `src/types/journey.ts` and `src/composables/useJourneyAdminApi.ts` do not exist
</verification>

<success_criteria>

- All admin types, composables, and pages use personalizada naming
- API endpoint paths point to /admin/personalizadas/\*
- UI text shows Spanish "Personalizada" instead of English "Journey"
- TypeScript compilation passes
- Zero journey references in el-templo-admin/src/
  </success_criteria>

<output>
After completion, create `.planning/phases/68-personalizadas-frontend-rename/68-01-SUMMARY.md`
</output>
