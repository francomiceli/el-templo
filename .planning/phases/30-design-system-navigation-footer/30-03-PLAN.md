---
phase: 30-design-system-navigation-footer
plan: 03
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - el-templo-web/components/AppFooter.vue
  - el-templo-web/components/AppPrefooter.vue
autonomous: true
requirements:
  - FOOT-01
  - FOOT-02
  - FOOT-03
  - FOOT-04
  - FOOT-05

must_haves:
  truths:
    - "Pre-footer CTA zone displays 'TU CUERPO ES TU TEMPLO.' with Terracotta CTA on Deep Charcoal background"
    - "Footer has 4 navigation columns: Entrená, Ecosistema, Empresa, Legal"
    - "Footer contact zone shows logo text, email, phone, and 3 social media icons"
    - "Footer legal zone shows copyright 2026 and terms/privacy/cookies links"
    - "Footer link hover transitions from Sandy Beige to Marble Cream in 200ms"
    - "Non-existent page links are dimmed in Olive Stone with no hover effect and no pointer cursor"
    - "Social media icons are disabled with same dimmed treatment as coming-soon links"
  artifacts:
    - path: "el-templo-web/components/AppPrefooter.vue"
      provides: "Pre-footer CTA section with Deep Charcoal bg, hero echo copy, and Terracotta CTA button"
      contains: "prefooter"
    - path: "el-templo-web/components/AppFooter.vue"
      provides: "Full footer with 4-col nav, contact zone, social icons, and legal zone"
      contains: "footer__nav"
  key_links:
    - from: "el-templo-web/components/AppFooter.vue"
      to: "el-templo-web/assets/css/tokens.css"
      via: "CSS custom property usage"
      pattern: "var\\(--color-"
---

<objective>
Build the pre-footer CTA section and complete footer component — 4-column navigation, contact/social zone, and legal zone — with proper handling of non-existent page links and disabled social icons.

Purpose: The footer appears on every page and provides institutional navigation, contact info, and a final conversion CTA. Non-existent page links must be visually dimmed (not broken links) per CONTEXT.md decisions.
Output: 2 Vue components (AppPrefooter.vue, AppFooter.vue).
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-design-system-navigation-footer/30-CONTEXT.md
@.planning/phases/30-design-system-navigation-footer/30-01-SUMMARY.md

**Footer spec from `.docs/brand-landing/spec10-pt3.md` and `spec10-pt4.md`:**

The HTML structure and CSS are fully specified in the spec files. Key details:

**Pre-footer (Zone 1):**

- Background: Deep Charcoal
- Title: "TU CUERPO ES TU TEMPLO." — Montserrat ExtraBold 28px (22px mobile), Marble Cream, uppercase
- Subtitle: "La primera sesión es sin cargo. Vení a sentir el método." — Geologica Regular italic, 16px, Olive Stone
- CTA: "COMENZÁ TU CAMINO" — Primary Terracotta button (same global `.btn .btn--primary`)
- Section ID: `#sesion-prueba` (anchor target for nav CTA)
- CTA href: `https://wa.me/54223XXXXXXX` (placeholder WhatsApp link)
- max-width: 600px, centered, padding 64px 0

**Footer Navigation (Zone 2):**

4 columns with these links:

Column 1 — Entrená:

- El Método → `/#metodo` (anchor, works)
- Sistema de Niveles → `/#niveles` (anchor, works)
- Los 5 Enfoques → `/#enfoques` (anchor, works)
- Descubrí tu Nivel → `/#descubri-nivel` (anchor, works)
- Reservar Sesión → `/#sesion-prueba` (anchor, works)

Column 2 — Ecosistema:

- Templo Online → `/app` (COMING SOON — dimmed)
- Academy → `/academy` (COMING SOON — dimmed)
- Franquicias → `/franquicias` (works)
- Gladius → `/gladius` (works)
- AURA CLUB → `/aura-club` (COMING SOON — dimmed)

Column 3 — Empresa:

- Nuestra Filosofía → `/filosofia` (COMING SOON — dimmed)
- Ignacio Bordón → `/ignacio-bordon` (COMING SOON — dimmed)
- Sedes → `/#sedes` (anchor, works)
- Contacto → `/contacto` (COMING SOON — dimmed)

Column 4 — Legal:

- Términos → `/terminos` (COMING SOON — dimmed)
- Privacidad → `/privacidad` (COMING SOON — dimmed)
- Cookies → `/cookies` (COMING SOON — dimmed)

**CONTEXT.md Decisions (locked) — Coming-soon link treatment:**

- Dimmed text in Olive Stone (#8A8472)
- No hover effect
- No pointer cursor (`cursor: default`)
- Visually present but clearly inactive
- Render as `<span>` not `<a>` to prevent accidental navigation

**Contact Zone (Zone 3):**

- Logo: text "El Templo" in Montserrat Bold, Marble Cream (placeholder per CONTEXT.md)
- Email: info@eltemplo.com (placeholder)
- Phone: +54 223 XXX XXXX (placeholder)
- Social icons: Instagram, YouTube, TikTok — ALL DISABLED per CONTEXT.md (dimmed treatment, no links)
- Layout: flex, logo left, contact center, social right. Mobile: stack centered.
- border-top: 1px solid rgba(242,237,229,0.1)

**Legal Zone (Zone 4):**

- "© 2026 El Templo Calistenia. Todos los derechos reservados."
- Geologica Regular, 12px, Olive Stone, centered
- border-top: 1px solid rgba(242,237,229,0.08)

**CSS from spec (key rules):**

```css
.footer { background: var(--color-deep-charcoal); }
.footer__nav { grid-template-columns: repeat(4, 1fr); gap: 32px; padding: 48px 0; }
.footer__col-title { Montserrat Bold, 12px, uppercase, tracking 0.1em, Terracotta }
.footer__col a { Geologica Regular, 14px, Sandy Beige, padding 4px 0, transition color 200ms }
.footer__col a:hover { color: Marble Cream }
```

Responsive:

- Tablet (768px): nav grid → 2 columns
- Mobile (480px): nav grid → 2 columns, contact stacks centered
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Build AppPrefooter component</name>
  <files>el-templo-web/components/AppPrefooter.vue</files>
  <action>
Create the pre-footer CTA section that echoes the hero copy as a final conversion push.

**Template:**

```html
<section class="prefooter" id="sesion-prueba">
  <div class="prefooter__container">
    <h2 class="prefooter__title">Tu cuerpo es tu templo.</h2>
    <p class="prefooter__sub">
      La primera sesión es sin cargo. Vení a sentir el método.
    </p>
    <a
      href="https://wa.me/54223XXXXXXX"
      class="btn btn--primary prefooter__cta"
      target="_blank"
      rel="noopener"
    >
      Comenzá tu camino
    </a>
  </div>
</section>
```

**Scoped styles following spec exactly:**

```css
.prefooter {
  background: var(--color-deep-charcoal);
  padding: var(--space-large) 0; /* 64px */
  text-align: center;
}

.prefooter__container {
  max-width: 600px;
  margin: 0 auto;
  padding: 0 5%;
}

.prefooter__title {
  font-family: var(--font-authority);
  font-weight: 800;
  font-size: 28px;
  text-transform: uppercase;
  letter-spacing: 0.04em;
  color: var(--color-marble-cream);
  margin: 0 0 var(--space-base) 0;
}

.prefooter__sub {
  font-family: var(--font-clarity);
  font-style: italic;
  font-size: 16px;
  color: var(--color-olive-stone);
  margin: 0 0 var(--space-comfortable) 0;
}

.prefooter__cta {
  /* Inherits from .btn .btn--primary global styles */
}
```

Responsive:

- Tablet (max-width 768px): `.prefooter__title { font-size: 22px; }`
- Mobile (max-width 480px): `.prefooter__title { font-size: 20px; }`

CRITICAL: No `#000000` or `#FFFFFF`. All colors via CSS custom properties.
</action>
<verify>
<automated>cd /home/franco/projects/el-templo/el-templo-web && grep -q 'prefooter\_\_title' components/AppPrefooter.vue && grep -q 'sesion-prueba' components/AppPrefooter.vue && grep -q 'btn--primary' components/AppPrefooter.vue && echo "PASS" || echo "FAIL"</automated>
</verify>
<done> - AppPrefooter renders Deep Charcoal section with centered title, subtitle, and Terracotta CTA - Section has id="sesion-prueba" (anchor target for nav/hero CTAs) - CTA links to WhatsApp placeholder - Responsive text sizing for tablet/mobile - No hardcoded colors — all via CSS custom properties
</done>
</task>

<task type="auto">
  <name>Task 2: Build AppFooter component with nav columns, contact zone, and legal</name>
  <files>el-templo-web/components/AppFooter.vue</files>
  <action>
Create the full footer component with all 4 zones.

**Script setup:**

Define the footer data structure for DRY rendering. Use a `disabled` flag on links that don't have pages yet:

```typescript
interface FooterLink {
  label: string;
  href: string;
  disabled: boolean;
}

interface FooterColumn {
  title: string;
  links: FooterLink[];
}

const columns: FooterColumn[] = [
  {
    title: "Entrená",
    links: [
      { label: "El Método", href: "/#metodo", disabled: false },
      { label: "Sistema de Niveles", href: "/#niveles", disabled: false },
      { label: "Los 5 Enfoques", href: "/#enfoques", disabled: false },
      { label: "Descubrí tu Nivel", href: "/#descubri-nivel", disabled: false },
      { label: "Reservar Sesión", href: "/#sesion-prueba", disabled: false },
    ],
  },
  {
    title: "Ecosistema",
    links: [
      { label: "Templo Online", href: "/app", disabled: true },
      { label: "Academy", href: "/academy", disabled: true },
      { label: "Franquicias", href: "/franquicias", disabled: false },
      { label: "Gladius", href: "/gladius", disabled: false },
      { label: "AURA CLUB", href: "/aura-club", disabled: true },
    ],
  },
  {
    title: "Empresa",
    links: [
      { label: "Nuestra Filosofía", href: "/filosofia", disabled: true },
      { label: "Ignacio Bordón", href: "/ignacio-bordon", disabled: true },
      { label: "Sedes", href: "/#sedes", disabled: false },
      { label: "Contacto", href: "/contacto", disabled: true },
    ],
  },
  {
    title: "Legal",
    links: [
      { label: "Términos", href: "/terminos", disabled: true },
      { label: "Privacidad", href: "/privacidad", disabled: true },
      { label: "Cookies", href: "/cookies", disabled: true },
    ],
  },
];
```

**Template:**

```html
<footer class="footer">
  <div class="footer__container">
    <!-- Zone 2: Nav Columns -->
    <div class="footer__nav">
      <div v-for="col in columns" :key="col.title" class="footer__col">
        <h4 class="footer__col-title">{{ col.title }}</h4>
        <template v-for="link in col.links" :key="link.label">
          <NuxtLink v-if="!link.disabled" :to="link.href" class="footer__link">
            {{ link.label }}
          </NuxtLink>
          <span v-else class="footer__link footer__link--disabled">
            {{ link.label }}
          </span>
        </template>
      </div>
    </div>

    <!-- Zone 3: Contact + Social -->
    <div class="footer__contact">
      <div class="footer__logo">
        <NuxtLink to="/" class="footer__logo-text">El Templo</NuxtLink>
      </div>
      <div class="footer__info">
        <a href="mailto:info@eltemplo.com" class="footer__info-link">
          info@eltemplo.com
        </a>
        <a href="tel:+54223XXXXXXX" class="footer__info-link">
          +54 223 XXX XXXX
        </a>
      </div>
      <div class="footer__social">
        <!-- All disabled per CONTEXT.md -->
        <span
          class="footer__social-icon footer__social-icon--disabled"
          aria-label="Instagram"
        >
          <!-- Inline SVG: Instagram icon -->
        </span>
        <span
          class="footer__social-icon footer__social-icon--disabled"
          aria-label="YouTube"
        >
          <!-- Inline SVG: YouTube icon -->
        </span>
        <span
          class="footer__social-icon footer__social-icon--disabled"
          aria-label="TikTok"
        >
          <!-- Inline SVG: TikTok icon -->
        </span>
      </div>
    </div>

    <!-- Zone 4: Legal -->
    <div class="footer__legal">
      <p>&copy; 2026 El Templo Calistenia. Todos los derechos reservados.</p>
    </div>
  </div>
</footer>
```

For the social media SVG icons, use minimal inline SVGs (Instagram camera, YouTube play triangle, TikTok music note). They should be simple 20x20 viewbox paths. Keep them minimal — they're disabled anyway but should look recognizable.

**Scoped styles following the spec CSS:**

Key classes:

```css
.footer {
  background: var(--color-deep-charcoal);
}

.footer__container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 5%;
}

/* Nav columns */
.footer__nav {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 32px;
  padding: 48px 0;
  border-top: 1px solid rgba(242, 237, 229, 0.1);
}

.footer__col-title {
  font-family: var(--font-authority);
  font-weight: 700;
  font-size: 12px;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: var(--color-terracotta);
  margin: 0 0 16px 0;
}

.footer__link {
  display: block;
  font-family: var(--font-clarity);
  font-weight: 400;
  font-size: 14px;
  color: var(--color-sandy-beige);
  text-decoration: none;
  padding: 4px 0;
  transition: color 200ms ease;
}

.footer__link:hover {
  color: var(--color-marble-cream);
}

/* DISABLED links — CONTEXT.md decision */
.footer__link--disabled {
  color: var(--color-olive-stone);
  cursor: default;
}
.footer__link--disabled:hover {
  color: var(--color-olive-stone); /* No hover change */
}

/* Contact zone */
.footer__contact {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 32px 0;
  border-top: 1px solid rgba(242, 237, 229, 0.1);
}

.footer__logo-text {
  font-family: var(--font-authority);
  font-weight: 700;
  font-size: 18px;
  color: var(--color-marble-cream);
  text-transform: uppercase;
  letter-spacing: 0.08em;
  text-decoration: none;
}

.footer__info-link {
  display: block;
  font-family: var(--font-clarity);
  font-size: 14px;
  color: var(--color-sandy-beige);
  text-decoration: none;
  transition: color 200ms ease;
}
.footer__info-link:hover {
  color: var(--color-marble-cream);
}

/* Social icons */
.footer__social {
  display: flex;
  gap: 16px;
}

.footer__social-icon {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 40px;
  height: 40px;
  color: var(--color-sandy-beige);
  transition: color 200ms ease;
}

.footer__social-icon--disabled {
  color: var(--color-olive-stone);
  cursor: default;
}

.footer__social-icon svg {
  width: 20px;
  height: 20px;
}

/* Legal zone */
.footer__legal {
  padding: 24px 0;
  border-top: 1px solid rgba(242, 237, 229, 0.08);
}

.footer__legal p {
  font-family: var(--font-clarity);
  font-size: 12px;
  color: var(--color-olive-stone);
  text-align: center;
  margin: 0;
}
```

**Responsive:**

```css
@media (max-width: 768px) {
  .footer__nav {
    grid-template-columns: 1fr 1fr;
  }
  .footer__contact {
    flex-direction: column;
    gap: 24px;
    text-align: center;
  }
}

@media (max-width: 480px) {
  .footer__nav {
    grid-template-columns: 1fr 1fr;
    gap: 24px;
  }
}
```

CRITICAL: No `#000000` or `#FFFFFF`. Disabled links are `<span>` not `<a>` to prevent navigation. All social icons are disabled per CONTEXT.md.
</action>
<verify>
<automated>cd /home/franco/projects/el-templo/el-templo-web && grep -q 'footer**nav' components/AppFooter.vue && grep -q 'footer**link--disabled' components/AppFooter.vue && grep -q 'footer**social-icon--disabled' components/AppFooter.vue && grep -q 'footer**legal' components/AppFooter.vue && grep -q '2026' components/AppFooter.vue && echo "PASS" || echo "FAIL"</automated>
</verify>
<done> - AppFooter renders 4 navigation columns with correct links and titles - Disabled links (coming-soon pages) render as dimmed Olive Stone spans with no hover/pointer - Contact zone shows logo text, email, phone with Sandy Beige → Marble Cream hover - Social icons are inline SVGs, all disabled per CONTEXT.md - Legal zone shows © 2026 text - Footer is responsive: 4-col → 2-col on tablet/mobile, contact stacks on mobile - No hardcoded colors, no pure black/white
</done>
</task>

</tasks>

<verification>
1. `cd el-templo-web && npx nuxi typecheck` — passes
2. `pnpm --filter el-templo-web build` — SSG build succeeds
3. Visual: Pre-footer shows Deep Charcoal bg, centered copy echoing hero, Terracotta CTA button
4. Visual: Footer has 4 nav columns, disabled links are visibly dimmed, Sandy Beige links turn Marble Cream on hover
5. Visual: Social icons visible but dimmed, contact info present, copyright shows 2026
6. Visual: At 768px, footer grid becomes 2-column, contact stacks
7. Functional: Active links (/#metodo, /franquicias, etc.) navigate correctly. Disabled links (spans) are not clickable.
</verification>

<success_criteria>

- Pre-footer CTA with Deep Charcoal bg, hero echo copy, Terracotta button linking to WhatsApp
- Pre-footer has id="sesion-prueba" as anchor target
- 4-column footer nav with correct link groupings
- Coming-soon links dimmed in Olive Stone with no hover/cursor
- Contact zone with logo text, email, phone, disabled social icons
- Legal zone with © 2026 copyright
- Link hover: Sandy Beige → Marble Cream (200ms)
- Responsive: 4-col → 2-col on tablet/mobile
- Type check passes, build succeeds
  </success_criteria>

<output>
After completion, create `.planning/phases/30-design-system-navigation-footer/30-03-SUMMARY.md`
</output>
