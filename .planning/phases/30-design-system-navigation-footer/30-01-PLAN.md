---
phase: 30-design-system-navigation-footer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-web/nuxt.config.ts
  - el-templo-web/assets/css/tokens.css
  - el-templo-web/assets/css/base.css
  - el-templo-web/assets/css/buttons.css
  - el-templo-web/assets/css/layout.css
  - el-templo-web/components/PlaceholderBox.vue
autonomous: true
requirements:
  - DS-01
  - DS-02
  - DS-03
  - DS-04
  - DS-05
  - DS-06
  - DS-07
  - DS-08

must_haves:
  truths:
    - "All CSS custom properties from the canonical token registry are defined in :root and available globally"
    - "Montserrat, Cormorant Garamond, and Geologica load at the correct weights across all pages"
    - "No pure black (#000000) or pure white (#FFFFFF) exists anywhere in any CSS file"
    - "Three responsive breakpoints (desktop 1200px+, tablet 768-1199px, mobile <768px) are usable via media queries"
    - "Primary, Ghost, and Secondary button variants render correctly with hover/active/focus states"
    - "Placeholder boxes render with Deep Charcoal background, Warm Stone border, and centered Olive Stone text label"
  artifacts:
    - path: "el-templo-web/assets/css/tokens.css"
      provides: "Complete :root token registry — colors, fonts, spacing, radius, shadows, transitions"
      contains: "--color-marble-cream"
    - path: "el-templo-web/assets/css/base.css"
      provides: "CSS reset, global typography, html scroll-behavior, body defaults"
      contains: "scroll-behavior: smooth"
    - path: "el-templo-web/assets/css/buttons.css"
      provides: "BEM button component classes — .btn--primary, .btn--ghost, .btn--secondary-gold, .btn--secondary-azul"
      contains: ".btn--primary"
    - path: "el-templo-web/assets/css/layout.css"
      provides: "Section wrapper and page container utility classes"
      contains: ".section"
    - path: "el-templo-web/components/PlaceholderBox.vue"
      provides: "Reusable placeholder/skeleton component for pending assets"
      contains: "PlaceholderBox"
  key_links:
    - from: "el-templo-web/nuxt.config.ts"
      to: "el-templo-web/assets/css/tokens.css"
      via: "css array in nuxt config"
      pattern: "css:.*tokens"
---

<objective>
Create the complete CSS design system foundation for el-templo-web — custom property tokens, Google Fonts loading, global base styles, responsive breakpoints, reusable BEM button components, section/container layout utilities, and a placeholder/skeleton component for pending assets.

Purpose: Every subsequent phase builds on this visual foundation. Without tokens, fonts, and base components, section phases would hardcode values and diverge from the brand spec.
Output: 6 files — 4 CSS files (tokens, base, buttons, layout), 1 Vue component (PlaceholderBox), nuxt.config.ts updated to load global CSS + fonts.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-design-system-navigation-footer/30-CONTEXT.md

The canonical token registry is spread across spec files in `.docs/brand-landing/`. The most complete :root listing is in spec1-pt2.md (hero section CSS). Additional tokens discovered across specs:

**Complete color palette (from specs):**

- `--color-marble-cream: #F2EDE5` (primary light background, text on dark)
- `--color-terracotta: #C07A56` (primary CTA, tags, accents)
- `--color-clay: #A0755A` (hover state for Terracotta)
- `--color-aged-gold: #B89B5E` (franchise CTA, premium accents)
- `--color-deep-charcoal: #3D3732` (primary dark, overlays, text on light)
- `--color-warm-stone: #D9CFC1` (borders, alternating section backgrounds)
- `--color-sandy-beige: #E5D9C8` (footer links, subtle borders)
- `--color-olive-stone: #8A8472` (small/meta text, disabled states)
- `--color-charcoal-mist: #5C554E` (secondary body text, subtitles)
- `--color-azul-noche: #2B3A4E` (app CTA only — one usage on entire site)
- `--color-warm-linen: #EDE4D6` (card backgrounds — spec3/spec7)

**Fonts:**

- `--font-authority: 'Montserrat', sans-serif` (headings, labels, tags)
- `--font-elegance: 'Cormorant Garamond', serif` (subtitles, quotes, italic accents)
- `--font-clarity: 'Geologica', sans-serif` (body, buttons, functional text)

**Spacing:**

- `--space-base: 16px`
- `--space-comfortable: 24px`
- `--space-spacious: 32px`
- `--space-section: 48px`
- `--space-large: 64px`
- `--space-hero: 96px`

**Radius:**

- `--radius-base: 6px` (buttons)
- `--radius-card: 8px` (cards, images)

**Shadows (Deep Charcoal rgba, NEVER black):**

- `--shadow-subtle: 0 2px 8px rgba(61, 55, 50, 0.10)`
- `--shadow-medium: 0 4px 16px rgba(61, 55, 50, 0.15)`
- `--shadow-prominent: 0 8px 30px rgba(61, 55, 50, 0.15)`

**Transitions:**

- `--transition-base: 300ms ease`
- `--transition-fast: 150ms ease`

**Google Fonts link (from spec1-pt3):**

```html
<link
  href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;600;700;800&family=Cormorant+Garamond:ital,wght@0,400;0,500;0,600;1,400&family=Geologica:wght@400;500;600&display=swap"
  rel="stylesheet"
/>
```

**Breakpoints (from specs):**

- Desktop: 1200px+ (no media query, default)
- Tablet: max-width 768px
- Mobile: max-width 480px

**CONTEXT.md Decisions (locked):**

- BEM naming convention throughout
- No pure black (#000000) or pure white (#FFFFFF) — hard brand rule
- All shadows use Deep Charcoal rgba(61,55,50,x), never black rgba(0,0,0,x)
- Placeholder system: Deep Charcoal bg, Warm Stone border, centered Olive Stone text label, no icons
- Logo placeholder: styled text "El Templo" in Montserrat Bold
  </context>

<tasks>

<task type="auto">
  <name>Task 1: CSS token registry, base reset, and nuxt.config integration</name>
  <files>el-templo-web/assets/css/tokens.css, el-templo-web/assets/css/base.css, el-templo-web/nuxt.config.ts</files>
  <action>
**Create `el-templo-web/assets/css/tokens.css`:**

Define the complete `:root` block with ALL tokens from the canonical registry listed in the context section above. Include every color, font, spacing, radius, shadow, and transition variable. This is the single source of truth for the entire design system.

**Create `el-templo-web/assets/css/base.css`:**

1. Minimal CSS reset (box-sizing border-box on \*, margin/padding 0 on body, img max-width 100%).
2. `html { scroll-behavior: smooth; }` for anchor navigation.
3. Body defaults: `font-family: var(--font-clarity)`, `color: var(--color-deep-charcoal)`, `background: var(--color-marble-cream)`, `line-height: 1.6`, `-webkit-font-smoothing: antialiased`.
4. Heading defaults (h1-h6): `font-family: var(--font-authority)`, `font-weight: 800`, `color: var(--color-deep-charcoal)`, `line-height: 1.2`.
5. Link reset: `color: inherit`, `text-decoration: none`.
6. CRITICAL: Verify NO instance of `#000000` or `#FFFFFF` exists in any value. Use `--color-deep-charcoal` and `--color-marble-cream` respectively.

**Update `el-templo-web/nuxt.config.ts`:**

1. Add `css` array importing tokens.css and base.css (tokens first so variables are available):

   ```ts
   css: [
     '~/assets/css/tokens.css',
     '~/assets/css/base.css',
     '~/assets/css/buttons.css',
     '~/assets/css/layout.css',
   ],
   ```

2. Add Google Fonts via `app.head.link` in nuxt config:
   ```ts
   app: {
     head: {
       link: [
         { rel: 'preconnect', href: 'https://fonts.googleapis.com' },
         { rel: 'preconnect', href: 'https://fonts.gstatic.com', crossorigin: '' },
         {
           rel: 'stylesheet',
           href: 'https://fonts.googleapis.com/css2?family=Montserrat:wght@300;600;700;800&family=Cormorant+Garamond:ital,wght@0,400;0,500;0,600;1,400&family=Geologica:wght@400;500;600&display=swap',
         },
       ],
     },
   },
   ```

Note: The `css` array entries for buttons.css and layout.css will exist once Task 2 creates them. Nuxt will error during dev if files are missing, but since tasks execute sequentially within a plan, this is fine — just ensure Task 2 creates these files before running dev.
</action>
<verify>
<automated>cd /home/franco/projects/el-templo/el-templo-web && grep -c '#000000\|#FFFFFF\|rgb(0,\s*0,\s*0\|rgb(255,\s*255,\s*255' assets/css/tokens.css assets/css/base.css; echo "Exit: $?" && grep -q 'color-marble-cream' assets/css/tokens.css && grep -q 'scroll-behavior' assets/css/base.css && grep -q 'fonts.googleapis.com' nuxt.config.ts && echo "PASS" || echo "FAIL"</automated>
</verify>
<done> - tokens.css contains complete :root with all 11 colors, 3 fonts, 6 spacings, 2 radii, 3 shadows, 2 transitions - base.css has CSS reset, body defaults using token variables, heading defaults, link reset, scroll-behavior smooth - nuxt.config.ts loads all 4 CSS files and Google Fonts via head links - Zero instances of #000000 or #FFFFFF anywhere
</done>
</task>

<task type="auto">
  <name>Task 2: BEM button components, layout utilities, and placeholder component</name>
  <files>el-templo-web/assets/css/buttons.css, el-templo-web/assets/css/layout.css, el-templo-web/components/PlaceholderBox.vue</files>
  <action>
**Create `el-templo-web/assets/css/buttons.css`:**

Implement BEM button classes following the spec exactly:

1. `.btn` base class: `display: inline-block`, `font-family: var(--font-clarity)`, `text-transform: uppercase`, `letter-spacing: 0.08em`, `border-radius: var(--radius-base)`, `text-decoration: none`, `cursor: pointer`, `border: none`, `transition: var(--transition-base)`.

2. `.btn--primary` (Terracotta): `font-weight: 600`, `font-size: 15px`, `background: var(--color-terracotta)`, `color: var(--color-marble-cream)`, `padding: 16px 40px`, `box-shadow: var(--shadow-subtle)`.
   - Hover: `background: var(--color-clay)`, `transform: translateY(-1px)`, `box-shadow: var(--shadow-medium)`.
   - Active: `transform: translateY(0)`, `box-shadow: var(--shadow-subtle)`.
   - Focus-visible: `outline: 2px solid var(--color-terracotta)`, `outline-offset: 3px`.

3. `.btn--ghost` (Ghost/Tertiary — no bg, no border, text link style):
   - `font-weight: 500`, `font-size: 14px`, `background: transparent`, `color: var(--color-terracotta)`, `padding: 0`, `letter-spacing: 0.06em`.
   - Hover: `color: var(--color-clay)`, `text-decoration: underline`.
   - No box-shadow, no border.

4. `.btn--secondary-gold` (Aged Gold bordered):
   - `font-weight: 600`, `font-size: 13px`, `background: transparent`, `color: var(--color-aged-gold)`, `border: 1.5px solid rgba(184, 155, 94, 0.6)`, `padding: 12px 32px`.
   - Hover: `border-color: var(--color-aged-gold)`, `background: rgba(184, 155, 94, 0.1)`, `color: var(--color-marble-cream)`.
   - Active: `background: rgba(184, 155, 94, 0.15)`.

5. `.btn--secondary-azul` (Azul Noche — only usage is app download CTA):
   - `font-weight: 600`, `font-size: 15px`, `background: var(--color-azul-noche)`, `color: var(--color-marble-cream)`, `padding: 16px 40px`.
   - Hover: `background: #3D4E62` (slightly lighter), `transform: translateY(-1px)`.
   - Active: `transform: translateY(0)`.
   - Focus-visible: `outline: 2px solid var(--color-azul-noche)`, `outline-offset: 3px`.

6. Mobile responsive (max-width 480px): `.btn--primary`, `.btn--secondary-azul`: `padding: 14px 32px`, `font-size: 14px`. `.btn--secondary-gold`: `padding: 10px 28px`, `font-size: 12px`.

**Create `el-templo-web/assets/css/layout.css`:**

1. `.section` — Section wrapper: `padding: var(--space-hero) 0` (96px vertical). Tablet: `padding: var(--space-large) 0` (64px). Mobile: same as tablet.

2. `.section--warm-stone` — Alternating background: `background: var(--color-warm-stone)`.

3. `.section--deep-charcoal` — Dark background: `background: var(--color-deep-charcoal)`.

4. `.section__container` — Content container: `max-width: 1200px`, `margin: 0 auto`, `padding: 0 5%`.

5. `.section__tag` — Reusable tag/label pattern (appears in nearly every section): `display: block`, `font-family: var(--font-authority)`, `font-weight: 700`, `font-size: 11px`, `text-transform: uppercase`, `letter-spacing: 0.15em`, `color: var(--color-terracotta)`, `margin-bottom: var(--space-base)`.

6. `.section__title` — Reusable H2 pattern: `font-family: var(--font-authority)`, `font-weight: 800`, `text-transform: uppercase`, `letter-spacing: 0.04em`, `color: var(--color-deep-charcoal)`, `line-height: 1.2`. Desktop: `font-size: 36px`, `margin-bottom: var(--space-spacious)`. Tablet: `font-size: 28px`. Mobile: `font-size: 24px`, `letter-spacing: 0.03em`.

7. `.section__subtitle` — Reusable subtitle pattern (Cormorant Garamond italic): `font-family: var(--font-elegance)`, `font-weight: 400`, `font-style: italic`, `color: var(--color-charcoal-mist)`, `line-height: 1.5`. Desktop: `font-size: 20px`. Tablet: `font-size: 17px`. Mobile: `font-size: 16px`.

**Create `el-templo-web/components/PlaceholderBox.vue`:**

A Vue component for pending assets (images, videos) per CONTEXT.md decisions:

Props:

- `label` (string, default: `'Imagen pendiente'`) — centered text hint
- `aspectRatio` (string, optional, e.g., `'16/9'`, `'4/5'`) — CSS aspect-ratio
- `height` (string, optional, e.g., `'400px'`) — explicit height if no aspect ratio

Template: A `<div>` with class `placeholder-box`. Contains a `<span>` with class `placeholder-box__label` showing the label text.

Scoped styles (BEM):

- `.placeholder-box`: `background: var(--color-deep-charcoal)`, `border: 1px solid var(--color-warm-stone)`, `border-radius: var(--radius-card)`, `display: flex`, `align-items: center`, `justify-content: center`, `width: 100%`, `overflow: hidden`. Apply `aspect-ratio` from prop if provided, else `height` from prop.
- `.placeholder-box__label`: `font-family: var(--font-clarity)`, `font-size: 14px`, `color: var(--color-olive-stone)`, `text-align: center`, `padding: var(--space-base)`.

No icons per CONTEXT.md decision. Text hint only.
</action>
<verify>
<automated>cd /home/franco/projects/el-templo/el-templo-web && grep -q 'btn--primary' assets/css/buttons.css && grep -q 'btn--ghost' assets/css/buttons.css && grep -q 'btn--secondary-gold' assets/css/buttons.css && grep -q 'btn--secondary-azul' assets/css/buttons.css && grep -q 'section\_\_container' assets/css/layout.css && grep -q 'placeholder-box' components/PlaceholderBox.vue && echo "PASS" || echo "FAIL"</automated>
</verify>
<done> - buttons.css has 4 button variants (primary, ghost, secondary-gold, secondary-azul) with hover/active/focus states and mobile responsive adjustments - layout.css has section wrapper, container, tag, title, and subtitle utility classes with responsive breakpoints - PlaceholderBox.vue renders a dark box with Warm Stone border and centered Olive Stone text label, accepts label/aspectRatio/height props - All files use token variables exclusively — no hardcoded colors
</done>
</task>

<task type="auto">
  <name>Task 3: Update index page to validate design system renders correctly</name>
  <files>el-templo-web/pages/index.vue</files>
  <action>
Replace the current placeholder index page with a minimal design system validation page that demonstrates tokens, fonts, buttons, layout utilities, and the placeholder component are all working. This page is temporary (will be replaced in Phase 31 with the real hero section) but serves as visual proof the foundation is correct.

Content of the validation page:

```vue
<script setup lang="ts">
useHead({
  title: "El Templo | Escuela de Movimiento",
});
</script>

<template>
  <main>
    <!-- Section: Design System Validation -->
    <section class="section">
      <div class="section__container">
        <span class="section__tag">Sistema de Diseño</span>
        <h2 class="section__title">Validación visual del design system.</h2>
        <p class="section__subtitle">
          Esta página valida que todos los tokens, tipografías y componentes del
          design system están cargados correctamente.
        </p>
      </div>
    </section>

    <!-- Section: Typography -->
    <section class="section section--warm-stone">
      <div class="section__container">
        <span class="section__tag">Tipografía</span>
        <h2 class="section__title">Montserrat ExtraBold — Títulos.</h2>
        <p
          style="font-family: var(--font-elegance); font-style: italic; font-size: 20px; color: var(--color-charcoal-mist); margin-bottom: var(--space-spacious);"
        >
          Cormorant Garamond Italic — Subtítulos y frases destacadas.
        </p>
        <p
          style="font-family: var(--font-clarity); font-size: 16px; color: var(--color-deep-charcoal); line-height: 1.6;"
        >
          Geologica Regular — Texto de cuerpo, botones y texto funcional. Esta
          fuente se usa para la mayoría del contenido de la página.
        </p>
      </div>
    </section>

    <!-- Section: Buttons -->
    <section class="section">
      <div class="section__container">
        <span class="section__tag">Botones</span>
        <h2 class="section__title">Variantes de botones.</h2>
        <div
          style="display: flex; flex-wrap: wrap; gap: var(--space-comfortable); align-items: center; margin-top: var(--space-spacious);"
        >
          <a href="#" class="btn btn--primary">Primario Terracotta</a>
          <a href="#" class="btn btn--ghost">Ghost Terracotta →</a>
          <a href="#" class="btn btn--secondary-gold">Secundario Gold</a>
          <a href="#" class="btn btn--secondary-azul">Secundario Azul</a>
        </div>
      </div>
    </section>

    <!-- Section: Placeholder -->
    <section class="section section--warm-stone">
      <div class="section__container">
        <span class="section__tag">Placeholders</span>
        <h2 class="section__title">Componente placeholder.</h2>
        <div
          style="display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: var(--space-spacious); margin-top: var(--space-spacious);"
        >
          <PlaceholderBox label="Video pendiente" aspect-ratio="16/9" />
          <PlaceholderBox label="Imagen pendiente" aspect-ratio="4/5" />
          <PlaceholderBox label="Logo pendiente" height="80px" />
        </div>
      </div>
    </section>
  </main>
</template>
```

This provides a visual smoke test for all DS requirements without requiring real content. Phase 31 will replace this page entirely with the hero section.
</action>
<verify>
<automated>cd /home/franco/projects/el-templo/el-templo-web && npx nuxi typecheck 2>&1 | tail -5 && echo "TYPE_CHECK_DONE"</automated>
</verify>
<done> - Index page renders sections demonstrating all 3 fonts, 4 button variants, section layout utilities, and PlaceholderBox component - Type check passes with no errors - Page is visually verifiable in browser at localhost
</done>
</task>

</tasks>

<verification>
1. `cd el-templo-web && npx nuxi typecheck` — passes with no TypeScript errors
2. `grep -rn '#000000\|#FFFFFF' el-templo-web/assets/` — returns zero matches (no pure black/white)
3. `pnpm --filter el-templo-web build` — SSG build completes successfully
4. Visual: `pnpm --filter el-templo-web dev` → open localhost → all 3 fonts render, buttons have correct colors/hover states, placeholder boxes display correctly, responsive breakpoints work at 1200px, 768px, and 480px widths
</verification>

<success_criteria>

- Complete CSS token registry defined in :root with all 11 colors, 3 font families, 6 spacings, 2 radii, 3 shadows, 2 transitions
- Google Fonts loading Montserrat (300,600,700,800), Cormorant Garamond (400,500,600+italic), Geologica (400,500,600)
- 4 BEM button variants with correct hover/active/focus-visible states
- Section/container layout utilities with responsive breakpoints
- PlaceholderBox Vue component rendering correctly
- Zero instances of #000000 or #FFFFFF in any CSS file
- TypeScript type check passes, SSG build succeeds
  </success_criteria>

<output>
After completion, create `.planning/phases/30-design-system-navigation-footer/30-01-SUMMARY.md`
</output>
