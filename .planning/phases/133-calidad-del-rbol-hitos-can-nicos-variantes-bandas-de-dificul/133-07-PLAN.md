---
phase: 133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul
plan: 07
type: execute
wave: 5
depends_on: ["133-04", "133-06"]
files_modified:
  - el-templo-admin/src/types/tree-editor.ts
  - el-templo-admin/src/components/treemap/SubgroupFlowNode.vue
  - el-templo-admin/src/components/treemap/RouteFlowNode.vue
  - el-templo-admin/src/pages/TreeMapPage.vue
autonomous: true
requirements: [R3-SUBGRP, R4-XRUTA]
must_haves:
  truths:
    - "Las rutas se ordenan y rotulan por sub-grupo de category fina dentro de su banda de categoría, con filtro 'Sub-grupo' clearable en la toolbar"
    - "Los prerequisitos cross-ruta se renderizan gris punteado (dash 8 4, grey-8, ArrowClosed, sin animación) — distinguibles de las precedencias manuales terracotta y del dashed de inicio de cadena"
    - "Con un extremo colapsado, la arista R4 se agrega a nivel ruta→ruta y la ruta élite muestra el badge 'prereq' — el prerequisito nunca queda invisible (Pitfall 7)"
  artifacts:
    - path: "el-templo-admin/src/components/treemap/SubgroupFlowNode.vue"
      provides: "Nodo-label de sub-grupo (caption uppercase, sin caja)"
    - path: "el-templo-admin/src/components/treemap/RouteFlowNode.vue"
      provides: "Badge prereq junto al pendingCount"
      contains: "prereq"
  key_links:
    - from: "el-templo-admin/src/pages/TreeMapPage.vue"
      to: "subGroup del payload EditableTree"
      via: "orden de rutas + labels + filtro en rebuildGraph"
      pattern: "subGroup"
    - from: "el-templo-admin/src/pages/TreeMapPage.vue"
      to: "precedenceEdges cross-ruta"
      via: "estilo R4 + arista agregada ruta→ruta cuando hay extremo colapsado"
      pattern: "8 4"
---

<objective>
R3 UI + R4 render: sub-grupos por category fina como agrupador visual y filtro (locked decision 5 — "la riqueza queda en las rutas, no convertirla en eje de navegación principal"), y prerequisitos cross-ruta en gris punteado visibles incluso con rutas colapsadas (locked decision 6 + resolución del Pitfall 7 vía badge + arista agregada, opción a+b recomendada).

Purpose: cerrar la capa visual de la fase — dificultad (plan 02), revisión (plan 06) y ahora estructura (sub-grupos) y dependencias entre rutas (R4).
Output: layout con sub-grupos + filtro, jerarquía de aristas completa, badge prereq.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul/133-UI-SPEC.md (C3, C6, Capa semántica 2 de Color — LOCKED)
@.planning/phases/133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul/133-PATTERNS.md (secciones 12, 13, No Analog Found)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sub-grupos R3 — orden, labels y filtro</name>
  <files>el-templo-admin/src/types/tree-editor.ts, el-templo-admin/src/components/treemap/SubgroupFlowNode.vue, el-templo-admin/src/pages/TreeMapPage.vue</files>
  <read_first>
    - el-templo-admin/src/pages/TreeMapPage.vue (rebuildGraph líneas 150-272 con constantes LAYOUT líneas 48-56 — orden de columnas de ruta dentro de la banda; toolbar con búsqueda líneas 703-725 — ahí va el q-select)
    - el-templo-admin/src/components/treemap/CategoryFlowNode.vue (24 líneas — plantilla del nodo label-only: draggable false, selectable false)
    - el-templo-admin/src/types/tree-editor.ts (TreeRoute — agregar subGroup, espejo del DTO del plan 04)
    - .planning/phases/133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul/133-UI-SPEC.md (C3 — display names es-AR LOCKED)
  </read_first>
  <action>
    types/tree-editor.ts: `subGroup: string` en TreeRoute (espejo del payload del plan 04, valor UPPERCASE de la DB).

    Constante de display names (en el mismo módulo de tipos o junto a las constantes del treemap): mapa UPPERCASE→es-AR title case con la lista LOCKED del UI-SPEC C3: PULL VERTICAL→Pull Vertical, PULL HORIZONTAL→Pull Horizontal, PUSH VERTICAL→Push Vertical, PUSH HORIZONTAL→Push Horizontal, KNEE DOMINANT→Knee Dominant, HIP DOMINANT→Hip Dominant, CORE ANTERIOR→Core Anterior, CORE POSTERIOR→Core Posterior, CORE LATERAL→Core Lateral, OBLICUOS→Oblicuos; fallback: title-case genérico para valores fuera de la lista.

    SubgroupFlowNode.vue: copiar CategoryFlowNode.vue (nodo label-only, draggable:false, selectable:false) con el estilo C3/Typography: caption 12px, peso 600, text-uppercase, color grey-7, letter-spacing 0.5px. Sin caja ni borde (agrupador visual, NO eje de navegación — locked).

    TreeMapPage.rebuildGraph: ordenar las rutas por subGroup (alfabético) dentro de su banda de categoría y posicionar un SubgroupFlowNode encima de la primera columna de cada grupo. Filtro: `q-select` denso "Sub-grupo", clearable, en la toolbar junto a la búsqueda, opciones = sub-grupos presentes en el payload (display name como label, UPPERCASE como value); al filtrar, ocultar rutas de otros sub-grupos (mismo mecanismo que la búsqueda existente). Empty state con copy LOCKED: "No hay rutas en este sub-grupo."
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && pnpm exec vue-tsc --noEmit -p tsconfig.json && grep -c "subGroup" src/types/tree-editor.ts src/pages/TreeMapPage.vue</automated>
    <human-check>UAT de fase: las rutas aparecen agrupadas con labels de sub-grupo; el filtro reduce el canvas; limpiar el filtro restaura todo.</human-check>
  </verify>
  <acceptance_criteria>
    - TreeRoute tiene subGroup tipado (vue-tsc falla si el payload no lo trae — espejo 1:1)
    - SubgroupFlowNode.vue existe, registrado como nodeType en TreeMapPage
    - El q-select es clearable y sus opciones se derivan del payload (no hardcodeadas)
    - Copy del empty state EXACTO: "No hay rutas en este sub-grupo."
    - vue-tsc pasa
  </acceptance_criteria>
  <done>Sub-grupos visibles como agrupador + filtro, sin convertirse en eje de navegación.</done>
</task>

<task type="auto">
  <name>Task 2: Aristas R4 gris punteado + arista agregada ruta→ruta + badge prereq</name>
  <files>el-templo-admin/src/pages/TreeMapPage.vue, el-templo-admin/src/components/treemap/RouteFlowNode.vue</files>
  <read_first>
    - el-templo-admin/src/pages/TreeMapPage.vue (rebuildGraph líneas 247-268 — render actual de precedenceEdges: terracotta animada si manual, #9e9e9e si auto, gate visibleExercises.has(...) líneas 248-252 que es el gap del Pitfall 7; dashed "start of chain" línea 224 con strokeDasharray 6 4 #bdbdbd que NO debe confundirse)
    - el-templo-admin/src/components/treemap/RouteFlowNode.vue (badge pendingCount líneas 46-54 + estilo __pending líneas 120-126 — plantilla EXACTA del badge prereq; RouteNodeData líneas 5-18)
    - .planning/phases/133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul/133-UI-SPEC.md (C6 + tabla "Capa semántica 2" — estilos LOCKED)
  </read_first>
  <action>
    TreeMapPage.rebuildGraph — clasificar cada precedenceEdge: construir un mapa exerciseId→routeId desde el payload; si fromRoute !== toRoute es arista CROSS-RUTA (R4). Render según la tabla LOCKED del UI-SPEC:
    - Ambos extremos expandidos: arista nodo→nodo con style stroke '#757575' (grey-8), strokeDasharray '8 4', strokeWidth 2, markerEnd ArrowClosed, animated: false. Las precedencias manuales SAME-route (cross-partición de esfuerzo) conservan su render terracotta animado actual — solo cambian las cross-ruta.
    - Algún extremo colapsado: en lugar de omitirla (gate visibleExercises actual), emitir UNA arista agregada RouteFlowNode→RouteFlowNode por par de rutas (id `prereq-agg-{fromRouteId}-{toRouteId}`, deduplicada si hay varias aristas entre el mismo par) con el mismo estilo R4.
    - Click en arista R4 (nodo→nodo o agregada): seleccionar y mostrar tooltip con copy LOCKED `Prerequisito: {ejercicio origen} ({ruta}) → {ejercicio destino} ({ruta})` (para la agregada, listar los pares que resume).
    - Alta/baja de aristas: mecanismo existente sin UI nueva (arrastrar handle / POST /precedence) — no tocar.

    RouteFlowNode — badge prereq (plantilla del badge pendingCount): `q-badge outline color="grey-8" label="prereq"`, posicionado a la IZQUIERDA del badge pendingCount cuando coexisten (pending mantiene prioridad visual). Data nueva `hasPrereq: boolean` en RouteNodeData, computada en rebuildGraph: true si la ruta tiene aristas de precedencia cross-ruta ENTRANTES (es la ruta élite). Tooltip con copy LOCKED: "Esta ruta tiene prerequisitos en otra ruta. Expandí ambas para ver las aristas."
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && pnpm exec vue-tsc --noEmit -p tsconfig.json && grep -c "8 4" src/pages/TreeMapPage.vue && grep -c "prereq" src/components/treemap/RouteFlowNode.vue</automated>
    <human-check>UAT de fase (con aristas FLR→FL seedeadas por bootstrap-elite-prereqs o creadas a mano): ambas rutas expandidas → arista gris punteada con flecha; colapsar FL → arista agregada ruta→ruta + badge prereq en FLR; el dashed de inicio de cadena se distingue claramente.</human-check>
  </verify>
  <acceptance_criteria>
    - Estilo R4 EXACTO a la tabla locked: stroke '#757575', dash '8 4', width 2, ArrowClosed, animated false (grep de los valores en TreeMapPage.vue)
    - Las precedencias manuales same-route conservan terracotta animado (el bloque existente líneas 253-266 sigue aplicando a non-cross-ruta)
    - Arista agregada con id `prereq-agg-` presente en el código y deduplicada por par de rutas
    - Badge prereq en RouteFlowNode a la izquierda del pendingCount, con el tooltip LOCKED
    - vue-tsc pasa
  </acceptance_criteria>
  <done>R4 legible en todos los estados del canvas: nunca invisible, nunca confundible con las otras 3 clases de arista.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ninguna nueva | Render puro de datos ya autorizados; alta/baja de aristas usa el endpoint existente /precedence (guard ya testeado) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-60 | Information Disclosure | sub-grupos/aristas en canvas admin | accept | Página detrás de login coach/owner; datos ya presentes en el payload autorizado |
| T-133-SC | Tampering | npm/pip/cargo installs | accept | Cero instalaciones en esta fase |
</threat_model>

<verification>
- `cd el-templo-admin && pnpm exec vue-tsc --noEmit -p tsconfig.json` limpio.
- R3-SUBGRP UI y R4-XRUTA render son manual-only (sin suite de componentes): UAT visual al cierre de fase; la parte automatizable (subGroup en payload, precedencias con nuevo scope) quedó testeada en el plan 04.
</verification>

<success_criteria>
- Sub-grupos como agrupador visual + filtro (no navegación), jerarquía de 4 clases de arista inconfundible, prerequisitos de rutas de élite siempre visibles (expandido, colapsado o vía badge).
</success_criteria>

<output>
Create `.planning/phases/133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul/133-07-SUMMARY.md` when done
</output>
