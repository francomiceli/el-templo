---
phase: 133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul
plan: 06
type: execute
wave: 4
depends_on: ["133-02", "133-05"]
files_modified:
  - el-templo-admin/src/types/tree-editor.ts
  - el-templo-admin/src/composables/useTreeEditorApi.ts
  - el-templo-admin/src/components/treemap/MilestoneReviewList.vue
  - el-templo-admin/src/pages/TreeMapPage.vue
autonomous: true
requirements: [R1-REV, TTB-SIG]
must_haves:
  truths:
    - "El drawer de revisión agrupa propuestas por movimiento, cada fila tiene toggle Hito|Variante con select de hito dependiente, y Aceptar escribe ambos ejes en un solo click"
    - "En la ruta TTB el drawer muestra el banner persistente de señal de split (TTB/Windshield/ATW) — no descartable"
    - "El panel lateral muestra el rol del nodo (Hito / Variante de: {nombre}), lista las variantes del hito con 'Marcar como hito' y permite 'Promover a hito' con confirmación"
    - "Aceptar una variante dentro de una cadena ordenada a mano pide confirmación explícita antes de reconectar vecinos"
  artifacts:
    - path: "el-templo-admin/src/components/treemap/MilestoneReviewList.vue"
      provides: "Lista del drawer extraída como componente (TreeMapPage ya tiene 1.041 líneas)"
      min_lines: 80
    - path: "el-templo-admin/src/composables/useTreeEditorApi.ts"
      provides: "getMilestoneReview/getVariants/acceptMilestoneReview/rejectMilestoneReview/promoteMilestone"
      contains: "milestone-review"
  key_links:
    - from: "el-templo-admin/src/composables/useTreeEditorApi.ts"
      to: "/admin/tree-editor/milestone-review"
      via: "api.get/api.post"
      pattern: "milestone-review"
    - from: "el-templo-admin/src/pages/TreeMapPage.vue"
      to: "el-templo-admin/src/components/treemap/MilestoneReviewList.vue"
      via: "import + render en el drawer tree-map-review"
      pattern: "MilestoneReviewList"
---

<objective>
R1-REV UI + TTB-SIG: extender el drawer de revisión de /tree-map con el eje hito/variante (una sola pasada del profe — locked decision 2), la señal persistente de split TTB (locked decision 4), y el panel lateral con rol del nodo, variantes y promoción. Consume los endpoints del plan 05 y las constantes de banda del plan 02.

Purpose: la revisión del profe es el ÚNICO camino que escribe truth — esta UI es la puerta.
Output: composable + tipos espejados, componente de lista extraído, drawer y panel extendidos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul/133-UI-SPEC.md (C1 badge variante, C4, C5, Copywriting Contract — LOCKED)
@.planning/phases/133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul/133-PATTERNS.md (secciones 13, 14)
@.planning/phases/133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul/133-05-PLAN.md (bloque interfaces — contratos HTTP a espejar 1:1)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tipos + composable (espejo 1:1 de los endpoints del plan 05)</name>
  <files>el-templo-admin/src/types/tree-editor.ts, el-templo-admin/src/composables/useTreeEditorApi.ts</files>
  <read_first>
    - el-templo-admin/src/composables/useTreeEditorApi.ts (idiom exacto: setPrecedence líneas 63-79 — api.post tipado, extractError, error.value, log.error, Notify negative, rethrow; contrato cleanup() líneas 98-105, sin onUnmounted)
    - el-templo-admin/src/types/tree-editor.ts (espejo DTO backend 1:1)
    - .planning/phases/133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul/133-05-PLAN.md (bloque interfaces — shapes exactos)
  </read_first>
  <action>
    types/tree-editor.ts: agregar `MilestoneReviewRow` (exerciseId, name, dl, effort, movementToken: string | null, stepRank: number | null, proposedMilestoneExerciseId: number | null, status, confidence: number | null), `MilestoneVariant` (id, name, dl), `AcceptMilestoneBody` (exerciseId, role: 'hito' | 'variante', milestoneExerciseId?, dimensionOverrides?) — espejo 1:1 del backend, sin any.

    useTreeEditorApi.ts: 5 métodos nuevos con el idiom de setPrecedence — `getMilestoneReview(route)` GET /admin/tree-editor/milestone-review?route=, `getVariants(exerciseId)` GET /admin/tree-editor/milestone/{id}/variants, `acceptMilestoneReview(body)`, `rejectMilestoneReview(exerciseId)`, `promoteMilestone(exerciseId)` con los POSTs correspondientes. Mensajes de error en es-AR del Copywriting Contract (genérico mutaciones: "No se pudo guardar el cambio. Reintentá; si persiste, recargá la página."). createLogger, nunca console.log; cleanup() se mantiene no-op-safe.
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && pnpm exec vue-tsc --noEmit -p tsconfig.json && grep -c "milestone-review" src/composables/useTreeEditorApi.ts</automated>
  </verify>
  <acceptance_criteria>
    - 5 métodos nuevos en el composable, cada uno con try/catch (err: unknown) + extractError + Notify + rethrow
    - `grep -c "console.log" src/composables/useTreeEditorApi.ts` = 0
    - vue-tsc pasa; los tipos espejan el contrato del plan 05 (mismos nombres de campo)
  </acceptance_criteria>
  <done>Capa de datos lista para drawer y panel.</done>
</task>

<task type="auto">
  <name>Task 2: Drawer — agrupación por movimiento, toggle Hito|Variante, accept combinado y banner TTB</name>
  <files>el-templo-admin/src/components/treemap/MilestoneReviewList.vue, el-templo-admin/src/pages/TreeMapPage.vue</files>
  <read_first>
    - el-templo-admin/src/pages/TreeMapPage.vue (drawer tree-map-review template líneas 781-892: filas con selects in-place row.proposedStep/row.proposedHabilidad líneas 857-888; acceptOne/rejectOne/bulkAcceptRoute líneas 508-556 con proposalBusyId/bulkBusy; diálogo de confirmación patrón onConnect líneas 399-418; payload del árbol con partitions[].overridden para detectar cadenas locked)
    - .planning/phases/133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul/133-UI-SPEC.md (C4 + Copywriting — copy LOCKED, usar textual)
    - el-templo-admin/src/constants/levels.ts (dlBand para mostrar dl en el select de candidatos)
  </read_first>
  <action>
    Extraer la lista del drawer a `components/treemap/MilestoneReviewList.vue`: recibe por props las filas mergeadas (propuesta de dimensión existente + MilestoneReviewRow matcheada por exerciseId) y emite eventos accept/reject por fila; TreeMapPage conserva estado, fetching y mutaciones (extracción recomendada por RESEARCH/PATTERNS para frenar el crecimiento del archivo de 1.041 líneas).

    Contrato C4 del UI-SPEC, al pie de la letra:
    - Agrupación visual por movementToken: header de grupo caption 12px/600/uppercase grey-7 con el token (ej. WINDSHIELD) + contador (N); filas con movementToken null al final bajo header "Sin movimiento detectado".
    - Por fila: `q-btn-toggle` denso Hito|Variante pre-poblado por la heurística (proposedMilestoneExerciseId null → Hito); si Variante → `q-select` denso label "Hito" con candidatos del MISMO grupo (movementToken × stepRank), formato `nombre — dl N`, pre-seleccionado el propuesto. Badges: `q-badge color="primary" outline label="Hito"` / `q-badge color="grey-7" outline label="Variante"`; el badge existente orange-8 "sin escalón" se conserva.
    - Accept por fila: UN click → si la fila tiene MilestoneReviewRow, llamar acceptMilestoneReview con role + milestoneExerciseId + dimensionOverrides tomados de los selects de dimensión de la fila (el backend escribe ambos ejes en una transacción); si la fila NO tiene propuesta de hito, mantener el flujo existente acceptOne sin cambios. Reject análogo. Sin optimistic updates; loading con proposalBusyId; refetch tras éxito. Notif éxito (copy LOCKED): "Propuesta aceptada: {nombre} → {Hito|Variante de {hito}}."
    - Confirmación cadena locked: si el ejercicio a aceptar como variante pertenece a una partición con overridden=true → diálogo ANTES del POST con copy LOCKED: título "La cadena se va a editar", cuerpo «"{nombre}" está en una cadena ordenada a mano. Al marcarlo como variante sale de la cadena y sus vecinos se reconectan.», botones "Aceptar igual" (primary) / "Cancelar" (flat). Accept/reject individual normal sigue SIN confirmación.
    - "Aceptar todas" conserva su diálogo existente y ahora acepta ambos ejes por fila.
    - Banner TTB (TTB-SIG): cuando reviewRoute === 'TTB', `q-banner dense class="bg-warning text-white"` icon="call_split" arriba de la lista, NO accionable ni descartable. Copy LOCKED: título "Posible split de ruta", cuerpo "Los movimientos TTB, Windshield y ATW podrían ser rutas separadas. La decisión se toma con los profes — esta agrupación es la referencia."
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && pnpm exec vue-tsc --noEmit -p tsconfig.json && grep -rc "Posible split de ruta" src/components/treemap/ src/pages/TreeMapPage.vue</automated>
    <human-check>UAT de fase: abrir drawer en TTB → banner gold visible; filas agrupadas por movimiento; toggle + select dependiente funcionan; aceptar variante en cadena manual pide confirmación.</human-check>
  </verify>
  <acceptance_criteria>
    - MilestoneReviewList.vue existe y TreeMapPage lo importa (`grep -c "MilestoneReviewList" src/pages/TreeMapPage.vue` ≥ 2: import + template)
    - Copy del banner TTB y del diálogo de cadena locked EXACTOS al Copywriting Contract (grep de los títulos "Posible split de ruta" y "La cadena se va a editar" = 1 cada uno)
    - Header "Sin movimiento detectado" presente para el grupo null
    - vue-tsc pasa; `grep -c "console.log" src/components/treemap/MilestoneReviewList.vue` = 0
  </acceptance_criteria>
  <done>Drawer con revisión de dos ejes en una pasada, señal TTB persistente, confirmación en cadenas locked.</done>
</task>

<task type="auto">
  <name>Task 3: Panel lateral — rol del nodo, variantes del hito y promoción</name>
  <files>el-templo-admin/src/pages/TreeMapPage.vue</files>
  <read_first>
    - el-templo-admin/src/pages/TreeMapPage.vue (panel tree-map-panel líneas 895-960: badge Manual/Auto líneas 903-907 como plantilla del badge de rol; q-btn dense outline size="sm" de "Reasignar ruta" líneas 944-954 como plantilla de acciones; patrón de diálogo onConnect líneas 399-418)
    - .planning/phases/133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul/133-UI-SPEC.md (C5 + Copywriting — copy LOCKED)
    - el-templo-admin/src/constants/levels.ts (dlBand para el badge dl de cada variante listada)
  </read_first>
  <action>
    Contrato C5 del UI-SPEC sobre el panel existente:
    - Línea de metadatos gana el badge de rol: los nodos del canvas son siempre backbone → badge "Hito" (primary outline). Si la entidad mostrada es una variante (seleccionada desde la lista de variantes), badge "Variante de: {nombre}" (grey-7 outline) con el nombre del hito linkeable — click selecciona el hito en el canvas.
    - Sección "Variantes (N)": `q-expansion-item` denso colapsado por defecto; al expandir, fetch lazy con getVariants(exerciseId); cada variante en `q-item` denso con nombre + badge dl de banda (dlBand) + botón `q-btn flat dense size="sm"` "Marcar como hito". Empty state (copy LOCKED): "Este hito no tiene variantes asignadas."
    - "Marcar como hito" / "Promover a hito": diálogo de confirmación con copy LOCKED — título "Promover a hito", cuerpo «"{variante}" pasa a ser el hito y "{hito actual}" pasa a variante. Las cadenas del árbol se recalculan.», botones "Promover" (primary) / "Cancelar" (flat). On OK → promoteMilestone(variantId) → notif éxito (copy LOCKED): "{nombre} ahora es el hito; {nombre anterior} pasó a variante." → refetch del árbol completo (las cadenas cambiaron). Botón disabled mientras otra mutación está en vuelo.
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && pnpm exec vue-tsc --noEmit -p tsconfig.json && grep -c "Promover a hito\|Este hito no tiene variantes" src/pages/TreeMapPage.vue</automated>
    <human-check>UAT de fase: seleccionar un hito con variantes aceptadas → expansión lista variantes; "Marcar como hito" pide confirmación, ejecuta el swap y el canvas se actualiza.</human-check>
  </verify>
  <acceptance_criteria>
    - Badge de rol presente en la línea de metadatos del panel
    - Expansión "Variantes" con fetch lazy (getVariants se llama al expandir, no al seleccionar)
    - Copy del diálogo y las notifs EXACTOS al Copywriting Contract
    - vue-tsc pasa
  </acceptance_criteria>
  <done>Panel con rol, variantes y swap funcionales contra los endpoints del plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin UI → endpoints milestone-* | La UI solo consume; la autorización y validación viven en el backend (plan 05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-50 | Tampering | mutaciones disparadas desde el drawer/panel | mitigate | Sin optimistic updates (estado previo intacto ante error — contrato UI-SPEC); confirmaciones en las 3 acciones destructivas; backend valida todo (T-133-41) |
| T-133-51 | Repudiation | accepts masivos accidentales | mitigate | "Aceptar todas" mantiene diálogo de confirmación; accept individual reversible vía promote/re-clasificación |
| T-133-SC | Tampering | npm/pip/cargo installs | accept | Cero instalaciones en esta fase |
</threat_model>

<verification>
- `cd el-templo-admin && pnpm exec vue-tsc --noEmit -p tsconfig.json` limpio.
- Copys LOCKED verificados por grep textual.
- R1-REV UI y TTB-SIG son manual-only (el admin no tiene suite de componentes — justificado en RESEARCH): UAT visual al cierre de fase con los pasos de los human-check.
</verification>

<success_criteria>
- El profe revisa dimensión + hito/variante en una sola pasada desde el drawer, la señal TTB no se pierde, el panel permite inspeccionar y promover variantes — todo con el copy y los estados del UI-SPEC.
</success_criteria>

<output>
Create `.planning/phases/133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul/133-06-SUMMARY.md` when done
</output>
