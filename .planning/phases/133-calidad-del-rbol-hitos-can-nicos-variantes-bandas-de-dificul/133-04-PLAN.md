---
phase: 133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul
plan: 04
type: execute
wave: 2
depends_on: ["133-01"]
files_modified:
  - el-templo-api/src/modules/exercises/backbone-scope.ts
  - el-templo-api/src/modules/tree-editor/service.ts
  - el-templo-api/src/modules/tree-editor/schemas.ts
  - el-templo-api/src/modules/tree-progress/service.ts
  - el-templo-api/rebuild-progression-graph.ts
  - el-templo-api/test/exercises/rebuild-progression-graph.test.ts
  - el-templo-api/test/exercises/exercise-progression-service.test.ts
  - el-templo-api/test/tree-progress/member-tree.test.ts
  - el-templo-api/test/tree-editor/tree-editor.test.ts
autonomous: true
requirements: [R1-FILTER, R3-SUBGRP]
must_haves:
  truths:
    - "Un ejercicio con milestone_exercise_id NOT NULL desaparece del backbone en los 4 caminos: rebuild (2 SELECTs), GET /tree del admin, árbol del miembro (tree-progress) y getNeighbor (vía aristas tras rebuild)"
    - "El predicado de backbone existe UNA vez como helper Drizzle compartido (tree-editor + tree-progress) — fin de las copias VERBATIM"
    - "GET /admin/tree-editor/tree expone subGroup (category fina dominante) por ruta"
    - "Efecto member-visible declarado: Mi Árbol pierde nodos a medida que los profes aceptan variantes — es el objetivo, no un bug (Pitfall 5)"
  artifacts:
    - path: "el-templo-api/src/modules/exercises/backbone-scope.ts"
      provides: "Helper backboneNodeConditions() compartido"
      exports: ["backboneNodeConditions", "VALID_EFFORTS"]
    - path: "el-templo-api/rebuild-progression-graph.ts"
      provides: "Scope con milestone_exercise_id IS NULL en los DOS SELECTs"
      contains: "milestone_exercise_id IS NULL"
  key_links:
    - from: "el-templo-api/src/modules/tree-editor/service.ts"
      to: "el-templo-api/src/modules/exercises/backbone-scope.ts"
      via: "import backboneNodeConditions"
      pattern: "backboneNodeConditions"
    - from: "el-templo-api/src/modules/tree-progress/service.ts"
      to: "el-templo-api/src/modules/exercises/backbone-scope.ts"
      via: "import backboneNodeConditions"
      pattern: "backboneNodeConditions"
    - from: "el-templo-api/rebuild-progression-graph.ts"
      to: "predicado compartido"
      via: "SQL crudo con comentario cruzado + test de consistencia de node-set"
      pattern: "milestone_exercise_id IS NULL"
---

<objective>
R1-FILTER + R3 server-side: extraer el predicado de backbone (hoy duplicado VERBATIM en 5 apariciones) a un helper compartido ANTES de extenderlo (refactor locked por las constraints), agregar la 5ª condición `milestone_exercise_id IS NULL` en los 4 caminos de lectura, y exponer la `category` fina dominante por ruta en `EditableTree` para los sub-grupos R3.

Purpose: las variantes aceptadas salen del backbone en TODOS los consumidores a la vez (admin, miembro, rebuild, player) — un node-set desincronizado crea árboles fantasma.
Output: helper compartido, filtro en 4 sitios, subGroup en el payload, tests extendidos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul/133-RESEARCH.md (secciones Predicado de backbone, Pattern 4, Pitfalls 2/3/5, Code Examples)
@.planning/phases/133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul/133-PATTERNS.md (secciones 5, 7, 8, Shared Patterns)
</context>

<interfaces>
Contrato que este plan CREA en `el-templo-api/src/modules/exercises/backbone-scope.ts` (consumido por tree-editor, tree-progress y plan 05):

- `export const VALID_EFFORTS = ["CON", "EXC", "ISO"] as const` (movido desde tree-editor/service.ts línea 38; re-exportar desde tree-editor para no romper imports existentes — `export type Effort` se mantiene).
- `export function backboneNodeConditions()` → array de condiciones Drizzle: `isNull(exercises.canonicalExerciseId)`, `inArray(exercises.effort, [...VALID_EFFORTS])`, `isNull(exercises.habilidad)`, `isNull(exercises.milestoneExerciseId)`, `eq(routes.excludedFromTree, false)` — para spread dentro de `and(...)`.
- Docblock con el contrato del embudo (canonical → habilidad → milestone → ruta no excluida) y referencia cruzada a los 2 sitios de SQL crudo del rebuild que deben mantenerse espejados a mano.

Contrato R3 que este plan AGREGA al payload de `GET /admin/tree-editor/tree` (consumido por plan 07):
- Cada objeto route de `EditableTree.categories[].routes[]` gana `subGroup: string` — valor UPPERCASE de la DB (p.ej. "PULL VERTICAL"), calculado server-side como la category más frecuente entre los nodos backbone de la ruta (tie-break: orden alfabético). El title-case es-AR es responsabilidad del frontend (UI-SPEC C3).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helper backbone-scope + filtro milestone en los 4 caminos de lectura</name>
  <files>el-templo-api/src/modules/exercises/backbone-scope.ts, el-templo-api/src/modules/tree-editor/service.ts, el-templo-api/src/modules/tree-progress/service.ts, el-templo-api/rebuild-progression-graph.ts, el-templo-api/test/exercises/rebuild-progression-graph.test.ts, el-templo-api/test/tree-progress/member-tree.test.ts, el-templo-api/test/exercises/exercise-progression-service.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/tree-editor/service.ts (loadGraphNodes líneas 161-194, predicado líneas 175-182, VALID_EFFORTS línea 38)
    - el-templo-api/src/modules/tree-progress/service.ts (loadGraphNodes líneas 252-290, predicado VERBATIM líneas 267-278)
    - el-templo-api/rebuild-progression-graph.ts (SELECT backbone líneas 77-89; readManualEdgePartitions líneas 155-167 — el filtro va sobre AMBOS endpoints ef./et.; docblock de scope líneas 22-26)
    - el-templo-api/test/exercises/rebuild-progression-graph.test.ts (caso E existente para habilidad — plantilla del caso nuevo de variante; importa runRebuildProgressionGraph contra DB per-worker)
    - el-templo-api/test/tree-progress/member-tree.test.ts y el-templo-api/test/exercises/exercise-progression-service.test.ts (seeds y shape de asserts existentes)
  </read_first>
  <behavior>
    - Test 1 (rebuild): seed con un ejercicio variante (milestoneExerciseId apuntando a un hito de la misma partición) → tras runRebuildProgressionGraph, la variante no tiene aristas incidentes y la cadena conecta hito-anterior→hito-siguiente (plantilla: caso E de habilidad).
    - Test 2 (member): el árbol de tree-progress no incluye la variante en sus nodos ni en los conteos de porcentaje.
    - Test 3 (getNeighbor): tras el rebuild del seed anterior, getNeighbor desde el vecino del hito nunca devuelve la variante (membresía por aristas — quedó sin aristas).
    - Test 4 (consistencia de node-set, Pattern 4): sobre un mismo seed, el node-set que devuelve el helper Drizzle (vía tree-editor loadGraphNodes) y el SELECT crudo del rebuild son idénticos (mismos exercise ids).
  </behavior>
  <action>
    Crear `src/modules/exercises/backbone-scope.ts` con el contrato del bloque <interfaces>. Mover `VALID_EFFORTS` ahí y re-exportar desde tree-editor/service.ts (los imports existentes de `Effort`/`isValidEffort` no se rompen).

    tree-editor/service.ts y tree-progress/service.ts: reemplazar el predicado inline de loadGraphNodes por `and(...backboneNodeConditions())` — borrar las copias VERBATIM y sus comentarios "copiado EXACTLY (D-06)", reemplazándolos por referencia al helper.

    rebuild-progression-graph.ts: agregar `AND e.milestone_exercise_id IS NULL` al SELECT de nodos (líneas 77-89) y `AND ef.milestone_exercise_id IS NULL AND et.milestone_exercise_id IS NULL` en readManualEdgePartitions (líneas 155-167). Actualizar el docblock del scope (líneas 22-26) y agregar comentario cruzado: "espejo manual de backboneNodeConditions() en src/modules/exercises/backbone-scope.ts — mantener sincronizado (hay test de consistencia)". El SQL crudo solo interpola via `${}`.

    Extender los 3 tests existentes con los casos del bloque behavior (helper createExercise del seed gana el campo opcional `milestoneExerciseId`). El test de consistencia (Test 4) va en rebuild-progression-graph.test.ts.
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-api && pnpm exec vitest run test/exercises/rebuild-progression-graph.test.ts test/tree-progress/member-tree.test.ts test/exercises/exercise-progression-service.test.ts && pnpm exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Los 4 tests del behavior pasan
    - `grep -c "backboneNodeConditions" src/modules/tree-editor/service.ts src/modules/tree-progress/service.ts` ≥ 1 en cada archivo
    - `grep -v '^\s*--' rebuild-progression-graph.ts | grep -c "milestone_exercise_id IS NULL"` ≥ 3 (1 SELECT de nodos + 2 endpoints de readManualEdgePartitions)
    - Predicado inline eliminado: `grep -c "isNull(schema.exercises.habilidad)" src/modules/tree-progress/service.ts` = 0 (la condición vive solo en el helper)
  </acceptance_criteria>
  <done>Embudo de backbone de 3 capas (canonical → habilidad → milestone) con una sola fuente de verdad Drizzle + espejo crudo testeado por consistencia; las variantes salen de admin, miembro, rebuild y player.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: subGroup (category fina dominante) en EditableTree</name>
  <files>el-templo-api/src/modules/tree-editor/service.ts, el-templo-api/src/modules/tree-editor/schemas.ts, el-templo-api/test/tree-editor/tree-editor.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/tree-editor/service.ts (SELECT de loadGraphNodes líneas 163-173 — ya trae pattern; DTO EditableRoute líneas 80-89; armado de categories/routes)
    - el-templo-api/src/modules/tree-editor/schemas.ts (response schema de GET /tree — agregar subGroup)
    - el-templo-api/test/tree-editor/tree-editor.test.ts (seedGraph con particiones PULLR/PUSHR + asserts de shape del response)
  </read_first>
  <behavior>
    - Test 1 (shape): GET /tree devuelve `subGroup` string en cada route del payload; con seed donde la ruta tiene category mayoritaria "PULL VERTICAL" (3 nodos) y minoritaria "CORE ANTERIOR" (1 nodo) → subGroup = "PULL VERTICAL".
    - Test 2 (variantes no votan): un nodo variante (milestone_exercise_id NOT NULL) no participa del cómputo de dominante (queda fuera del node-set por Task 1).
    - Test 3 (tie-break): empate de conteo → gana la category alfabéticamente menor (determinístico).
  </behavior>
  <action>
    En loadGraphNodes: agregar `category: schema.exercises.category` al SELECT. En el armado de EditableTree: computar la category dominante por ruta EN MEMORIA (conteo por route sobre los nodos ya cargados — sin subqueries correlacionadas, Pitfall 3), tie-break alfabético, y asignarla como `subGroup` del DTO EditableRoute. Si una ruta queda sin nodos con category, subGroup = "" (string vacío, nunca undefined — additionalProperties false en schemas).

    schemas.ts: agregar `subGroup: { type: "string" }` al objeto route del response schema de GET /tree (required).

    Extender tree-editor.test.ts: el helper createExercise ya recibe pattern — agregar parámetro category (default existente del helper o "PULL VERTICAL") y los 3 asserts del behavior.
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-api && pnpm exec vitest run test/tree-editor/tree-editor.test.ts && pnpm exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - GET /tree responde subGroup por ruta (test de shape verde)
    - `grep -c "subGroup" src/modules/tree-editor/schemas.ts` ≥ 1
    - El cómputo es en memoria: cero subqueries nuevas con sql`` en .select() (`grep -n "sql\`" src/modules/tree-editor/service.ts` no suma ocurrencias nuevas respecto al estado pre-task)
  </acceptance_criteria>
  <done>R3 server-side completo: el frontend (plan 07) recibe el agrupador por ruta sin computar nada; el cómputo excluye variantes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin → GET /tree | Lectura autenticada existente (guard TRAINING_ROLES heredado, sin endpoint nuevo) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-30 | Tampering | drift entre helper Drizzle y SQL crudo del rebuild | mitigate | Test de consistencia de node-set sobre seed compartido + comentario cruzado en ambos archivos |
| T-133-31 | Tampering | SQL injection en SELECTs crudos del rebuild | mitigate | Interpolación SOLO vía `${}` (parametriza) — patrón existente sin cambios |
| T-133-32 | Information Disclosure | category fina en payload admin | accept | GET /tree ya requiere coach/owner; category no es dato sensible |
| T-133-SC | Tampering | npm/pip/cargo installs | accept | Cero instalaciones en esta fase |
</threat_model>

<verification>
- Tests puntuales de los 4 archivos de test tocados verdes (NO suite completa local).
- `pnpm exec tsc --noEmit -p tsconfig.json` limpio.
- Declarar en el SUMMARY el efecto member-visible (Pitfall 5): Mi Árbol del miembro pierde nodos cuando los profes acepten variantes — para VERIFICATION/UAT de fase y release notes. Registrar el deferred-item de fase 134: "¿dominado sobre una variante ilumina su hito en el % del miembro?".
</verification>

<success_criteria>
- Filtro de variantes activo en los 4 caminos con una sola fuente de verdad, subGroup servido por ruta, tests extendidos verdes, efecto member-visible documentado.
</success_criteria>

<output>
Create `.planning/phases/133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul/133-04-SUMMARY.md` when done
</output>
