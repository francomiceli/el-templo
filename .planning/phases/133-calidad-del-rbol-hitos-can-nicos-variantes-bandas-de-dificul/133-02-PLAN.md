---
phase: 133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-admin/src/constants/levels.ts
  - el-templo-admin/src/pages/AlumnosPage.vue
  - el-templo-admin/src/pages/AlumnoDetailPage.vue
  - el-templo-admin/src/pages/SessionsPage.vue
  - el-templo-admin/src/components/sessions/EditableBlockCard.vue
  - el-templo-admin/src/components/treemap/ExerciseFlowNode.vue
  - el-templo-admin/src/pages/TreeMapPage.vue
autonomous: true
requirements: [R2-BANDS]
must_haves:
  truths:
    - "Cada nodo de ejercicio del mapa muestra su banda de dificultad: stripe de color de 4px + badge dl coloreado con tooltip {banda} (dl {min}–{max})"
    - "La toolbar de /tree-map tiene la leyenda de las 6 bandas (kairos 1–2 … spartan 11–12)"
    - "levelColor() vive UNA sola vez en constants/levels.ts y las 4 superficies que la duplicaban la importan"
  artifacts:
    - path: "el-templo-admin/src/constants/levels.ts"
      provides: "levelColor() extraída + DL_BANDS locked + helper dlBand()"
      exports: ["levelColor", "DL_BANDS", "dlBand"]
    - path: "el-templo-admin/src/components/treemap/ExerciseFlowNode.vue"
      provides: "Stripe de banda + q-badge dl"
      contains: "DL_BANDS"
  key_links:
    - from: "el-templo-admin/src/components/treemap/ExerciseFlowNode.vue"
      to: "el-templo-admin/src/constants/levels.ts"
      via: "import de DL_BANDS/dlBand"
      pattern: "from ['\"].*constants/levels"
    - from: "el-templo-admin/src/pages/AlumnosPage.vue"
      to: "el-templo-admin/src/constants/levels.ts"
      via: "import de levelColor (definición local eliminada)"
      pattern: "levelColor"
---

<objective>
R2 completo (admin, UI pura): bandas de dificultad por nodo con el mapeo LOCKED kairos 1-2 / alfa 3 / delta 4-6 / sigma 7-8 / omega 9-10 / spartan 11-12, color + dl numérico + leyenda; y el refactor DRY locked por UI-SPEC: extraer `levelColor()` (hoy duplicada VERBATIM en 4 archivos) a `constants/levels.ts` ANTES de agregarle consumidores nuevos.

Purpose: hacer la dificultad visible en el mapa — `dl` ya viaja en el payload del árbol editable; este plan solo agrega la capa visual. Independiente del backend (wave 1, paralelo al plan 01).
Output: constantes compartidas + nodo con banda + leyenda en toolbar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul/133-UI-SPEC.md (secciones Color, C1, C2 — contrato LOCKED)
@.planning/phases/133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul/133-PATTERNS.md (secciones 10-11)
</context>

<interfaces>
Contrato que este plan CREA en `el-templo-admin/src/constants/levels.ts` (consumido por planes 06/07 y por las 4 páginas refactorizadas):

- `levelColor(level: string): string` — switch case-insensitive: kairos→'amber-6', alfa→'amber-8', delta→'deep-orange-7', sigma→'brown-8', omega→'red-9', spartan→'grey-9', default→'grey' (extracción VERBATIM de AlumnosPage.vue líneas 591-608).
- `DL_BANDS` (as const): [{level:'kairos',min:1,max:2,color:'amber-6'},{level:'alfa',min:3,max:3,color:'amber-8'},{level:'delta',min:4,max:6,color:'deep-orange-7'},{level:'sigma',min:7,max:8,color:'brown-8'},{level:'omega',min:9,max:10,color:'red-9'},{level:'spartan',min:11,max:12,color:'grey-9'}] — mapeo LOCKED (tree-quality-research.md §4.3), NO derivar de LEVEL_LINEAR_MIN del API.
- `dlBand(dl: number | null | undefined)` → la banda correspondiente o `null` si dl es null/fuera de 1-12.
- Regla de texto sobre color: kairos/alfa (amber) → texto charcoal `$accent` / clase `text-grey-10`; resto → texto blanco. Exportar helper o constante (p.ej. `bandTextClass(band)`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extraer levelColor a constants/levels.ts + DL_BANDS + refactor DRY de las 4 superficies</name>
  <files>el-templo-admin/src/constants/levels.ts, el-templo-admin/src/pages/AlumnosPage.vue, el-templo-admin/src/pages/AlumnoDetailPage.vue, el-templo-admin/src/pages/SessionsPage.vue, el-templo-admin/src/components/sessions/EditableBlockCard.vue</files>
  <read_first>
    - el-templo-admin/src/constants/levels.ts (hoy solo LEVEL_ORDER, 7 líneas — se extiende)
    - el-templo-admin/src/pages/AlumnosPage.vue (levelColor líneas 591-608 — fuente de la extracción VERBATIM, incluye spartan 'grey-9')
    - el-templo-admin/src/pages/AlumnoDetailPage.vue (duplicado en línea ~1084), el-templo-admin/src/pages/SessionsPage.vue (~751), el-templo-admin/src/components/sessions/EditableBlockCard.vue (~616)
  </read_first>
  <action>
    Extender `constants/levels.ts` con los exports del bloque <interfaces> (levelColor, DL_BANDS, dlBand, bandTextClass). Docblock en DL_BANDS: mapeo de UI deliberado y LOCKED — NO coincide con LEVEL_LINEAR_MIN del API (alfa arranca en dl 1 allá) y NO debe "corregirse".

    Refactor DRY: eliminar la definición local de `levelColor` en AlumnosPage.vue, AlumnoDetailPage.vue, SessionsPage.vue y EditableBlockCard.vue; las 4 pasan a importarla desde constants/levels. No cambiar ningún call-site ni comportamiento (extracción pura).
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && pnpm exec vue-tsc --noEmit -p tsconfig.json && grep -rln "function levelColor" src/pages src/components | grep -cv constants | grep -q '^0$' && echo DRY-OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "function levelColor" el-templo-admin/src/` matchea SOLO en constants/levels.ts (0 definiciones locales restantes)
    - Los 4 archivos importan desde constants/levels: `grep -l "constants/levels" AlumnosPage.vue AlumnoDetailPage.vue SessionsPage.vue EditableBlockCard.vue` = 4 archivos
    - `DL_BANDS` exporta exactamente 6 bandas con los rangos locked (1-2, 3, 4-6, 7-8, 9-10, 11-12)
    - vue-tsc pasa
  </acceptance_criteria>
  <done>Paleta única en constants/levels.ts; 4 superficies refactorizadas sin cambio de comportamiento; DL_BANDS disponible para los planes 06/07.</done>
</task>

<task type="auto">
  <name>Task 2: ExerciseFlowNode con stripe + badge dl, y leyenda de bandas en la toolbar</name>
  <files>el-templo-admin/src/components/treemap/ExerciseFlowNode.vue, el-templo-admin/src/pages/TreeMapPage.vue</files>
  <read_first>
    - el-templo-admin/src/components/treemap/ExerciseFlowNode.vue (interfaz ExerciseNodeData líneas 6-17 ya trae dl; texto plano `dl {{ data.dl ?? '—' }}` línea 37; badge Manual líneas 38-43 como plantilla de q-badge; borde $grey-5 línea 57; estados --manual línea 64 y --selected líneas 67-69 que NO cambian)
    - el-templo-admin/src/pages/TreeMapPage.vue (toolbar con búsqueda líneas ~703-725 y badges Auto/Manual líneas ~729-738 — ahí va la leyenda)
    - el-templo-admin/src/constants/levels.ts (recién creado en Task 1)
  </read_first>
  <action>
    ExerciseFlowNode (contrato C1 del UI-SPEC):
    - Stripe: `border-left: 4px solid <color de banda>` aplicado vía style binding usando `dlBand(data.dl)`; convive sobre el borde `$grey-5` existente. Los estados `--manual` (borde completo $primary) y `--selected` (outline $primary) NO se modifican — el stripe se superpone solo en el lado izquierdo.
    - Badge dl: reemplazar el texto plano `dl N` por `q-badge` con color de banda (prop `color` con el token Quasar), label `dl N`, clase de texto según bandTextClass (charcoal para kairos/alfa, blanco para el resto). Tooltip: `{banda} (dl {min}–{max})` (ej. `delta (dl 4–6)`).
    - dl null o fuera de 1-12: sin stripe, badge `dl —` color `grey-6`.

    TreeMapPage (contrato C2): leyenda en la toolbar existente, alineada a la derecha — 6 chips `q-badge` compactos en fila con label `kairos 1–2`, `alfa 3`, `delta 4–6`, `sigma 7–8`, `omega 9–10`, `spartan 11–12`, color de banda + texto según contraste, iterando DL_BANDS (no hardcodear). En viewport <1100px colapsa a un único `q-btn flat icon="palette"` con la leyenda dentro de un `q-menu` (usar `$q.screen.width` o clase responsive). Regla de marca: ningún azul.
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && pnpm exec vue-tsc --noEmit -p tsconfig.json && grep -c "DL_BANDS" src/components/treemap/ExerciseFlowNode.vue src/pages/TreeMapPage.vue</automated>
    <human-check>UAT visual de fase: abrir /tree-map, expandir una ruta — cada nodo muestra stripe + badge coloreado coherente con su dl; la leyenda lista las 6 bandas; en ventana angosta colapsa al botón palette.</human-check>
  </verify>
  <acceptance_criteria>
    - ExerciseFlowNode importa dlBand/DL_BANDS desde constants/levels (grep `constants/levels` = 1 match en el archivo)
    - El texto plano `dl {{` ya no existe en el template (reemplazado por q-badge): `grep -c 'dl {{' src/components/treemap/ExerciseFlowNode.vue` = 0
    - TreeMapPage renderiza la leyenda iterando DL_BANDS (grep `DL_BANDS` ≥ 1 en TreeMapPage.vue)
    - vue-tsc pasa; ningún hex azul nuevo (`grep -iE '#((00|1?[0-9a-f])[0-9a-f]ff|0000ff)' ` no aplica — verificar visualmente que solo se usan los 6 tokens de DL_BANDS)
  </acceptance_criteria>
  <done>Bandas visibles en cada nodo (stripe + badge con tooltip), leyenda en toolbar con colapso responsive, estados manual/selected intactos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ninguna nueva | UI pura sobre datos ya servidos al admin autenticado; sin input nuevo hacia el API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-10 | Information Disclosure | leyenda/badges en /tree-map | accept | La página ya está detrás del login admin; no se expone dato nuevo (dl ya viajaba en el payload) |
| T-133-SC | Tampering | npm/pip/cargo installs | accept | Cero instalaciones en esta fase |
</threat_model>

<verification>
- `cd el-templo-admin && pnpm exec vue-tsc --noEmit -p tsconfig.json` limpio.
- Grep DRY: una sola definición de levelColor en todo src/.
- UAT visual diferido al cierre de fase (R2-BANDS es manual-only — el admin no tiene suite de componentes, justificado en RESEARCH Validation).
</verification>

<success_criteria>
- R2 completo: banda visible (color + dl) en cada nodo con el mapeo locked, leyenda presente, paleta consolidada en un único módulo importado por 6 superficies (4 refactorizadas + 2 nuevas).
</success_criteria>

<output>
Create `.planning/phases/133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul/133-02-SUMMARY.md` when done
</output>
