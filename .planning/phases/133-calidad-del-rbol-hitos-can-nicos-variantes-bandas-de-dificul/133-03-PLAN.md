---
phase: 133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul
plan: 03
type: execute
wave: 2
depends_on: ["133-01"]
files_modified:
  - el-templo-api/src/modules/exercises/milestone-heuristic.ts
  - el-templo-api/src/modules/exercises/route-progression-map.ts
  - el-templo-api/bootstrap-milestones.ts
  - el-templo-api/bootstrap-elite-prereqs.ts
  - el-templo-api/test/exercises/milestone-heuristic.test.ts
  - el-templo-api/test/exercises/bootstrap-milestones.test.ts
autonomous: true
requirements: [R1-HEUR, R4-XRUTA]
must_haves:
  truths:
    - "La heurística agrupa una partición (ruta × esfuerzo) por (movimiento × escalón) en forma determinística y propone UN hito por grupo; sobre un seed tipo TTB produce ≤13 hitos"
    - "bootstrap-milestones inserta SOLO propuestas pending (nunca toca exercises) y es idempotente: correrlo dos veces no duplica filas"
    - "bootstrap-elite-prereqs inserta las aristas manual cross-ruta FLR→FL y PLPU→PL en forma idempotente (maquinaria lista; ejecución en prod queda manual post-deploy)"
  artifacts:
    - path: "el-templo-api/src/modules/exercises/milestone-heuristic.ts"
      provides: "Motor puro de agrupamiento movimiento × escalón"
      exports: ["proposeMilestones"]
      min_lines: 80
    - path: "el-templo-api/bootstrap-milestones.ts"
      provides: "CLI idempotente que inserta propuestas de hito pending"
      exports: ["runBootstrapMilestones"]
    - path: "el-templo-api/bootstrap-elite-prereqs.ts"
      provides: "CLI idempotente que seedea aristas manual cross-ruta para rutas de élite"
      exports: ["runBootstrapElitePrereqs"]
  key_links:
    - from: "el-templo-api/src/modules/exercises/milestone-heuristic.ts"
      to: "el-templo-api/src/modules/exercises/route-progression-map.ts"
      via: "import de normalizeWords/classify/phraseAppears (Don't Hand-Roll)"
      pattern: "from ['\"]\\./route-progression-map"
    - from: "el-templo-api/bootstrap-milestones.ts"
      to: "exercise_milestone_proposals"
      via: "INSERT ... WHERE NOT EXISTS (idempotente)"
      pattern: "NOT EXISTS"
---

<objective>
R1-HEUR: motor determinístico (sin LLM) que propone hitos por (movimiento × escalón) por partición, y su CLI idempotente que inserta propuestas pending. Más la maquinaria R4 decidida por las constraints: CLI idempotente que seedea las aristas manual cross-ruta de las rutas de élite (FLR dl 5, PLPU dl 4) — patrón de fases 125/126: código + CLI mergeado, ejecución manual post-deploy.

Purpose: la heurística propone, el profe confirma — el truth (`milestone_exercise_id`) NUNCA lo escribe este plan (frontera de fase 125).
Output: módulo puro testeable + 2 CLIs idempotentes + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul/133-RESEARCH.md (secciones R1, Pattern 2, Pitfall 6, Don't Hand-Roll)
@.planning/phases/133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul/133-PATTERNS.md (sección 4)
</context>

<interfaces>
Contrato que este plan CREA (consumido por el plan 05 vía datos en `exercise_milestone_proposals`, y por tests):

`milestone-heuristic.ts`:
- `proposeMilestones(rows: CatalogRow[]): MilestoneProposal[]` — función PURA. CatalogRow = { id, name, route, effort, dificultadLineal, acceptedStep: number | null } (acceptedStep = proposed_step de la propuesta de dimensión SOLO si status='accepted', sino null).
- MilestoneProposal = { exerciseId, proposedMilestoneExerciseId: number | null, movementToken: string | null, stepRank: number | null, confidence: number }.
- `MOVEMENT_VOCAB: Readonly<Record<string, readonly string[]>>` — vocabulario declarativo de familias de movimiento por ruta, most-specific-first.

`bootstrap-milestones.ts` (raíz del paquete): `export async function runBootstrapMilestones<TSchema extends Record<string, unknown>>(db: MySql2Database<TSchema>): Promise<{ proposed: number; skipped: number }>` + guard de entrypoint CLI.

`bootstrap-elite-prereqs.ts` (raíz del paquete): `export async function runBootstrapElitePrereqs<TSchema extends Record<string, unknown>>(db: MySql2Database<TSchema>): Promise<{ inserted: number; skipped: number }>` + guard de entrypoint CLI. Constante `ELITE_PREREQS = [{ elite: 'FLR', base: 'FL' }, { elite: 'PLPU', base: 'PL' }]`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Módulo milestone-heuristic.ts (motor puro movimiento × escalón)</name>
  <files>el-templo-api/src/modules/exercises/milestone-heuristic.ts, el-templo-api/src/modules/exercises/route-progression-map.ts, el-templo-api/test/exercises/milestone-heuristic.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/exercises/route-progression-map.ts (REUSAR, no duplicar: normalizeWords líneas 310-320, classify líneas 416-437, phraseAppears líneas 369-382 — hoy NO exportada, exportarla; estructura COMPILED/bySpecificity líneas 346-393 como patrón; ROUTE_PROGRESSION_MAP para tokens de escalón)
    - el-templo-api/test/exercises/bootstrap-dimensions.test.ts (analog de estilo de test sobre motor puro + seeds)
    - el-templo-api/test/tree-editor/tree-editor.test.ts (helpers createRoute/createExercise si el test necesita seeds de integración)
  </read_first>
  <behavior>
    - Test 1 (agrupamiento): un seed estilo TTB CON con ~20 nombres reales (familias TTB/ATW/WINDSHIELD/BENT ARM/OA/90 × escalones TUCK/STRADDLE/FULL) produce grupos (movimiento × escalón); cada grupo tiene exactamente 1 propuesta con proposedMilestoneExerciseId=null (el hito) y el resto apunta al id del hito.
    - Test 2 (cota): el seed produce ≤13 hitos propuestos (criterio del goal: 8-13 hitos para escaleras largas).
    - Test 3 (determinismo): dos corridas sobre el mismo input devuelven exactamente el mismo output (mismo hito por grupo).
    - Test 4 (sin movimiento): ejercicio cuyo nombre no matchea ningún token del vocab → movementToken=null, propuesto como hito (proposedMilestoneExerciseId=null) con confidence baja.
    - Test 5 (escalón en vivo): el escalón se deriva de classify() sobre el nombre — el test NO puebla progression_step (truth vacío, Pitfall 6); si acceptedStep viene poblado, tiene prioridad sobre classify().
    - Test 6 (selección de hito): dentro de un grupo, gana el nombre más canónico — menor cantidad de tokens normalizados; empate → menor dificultad_lineal; empate → menor id.
  </behavior>
  <action>
    En `route-progression-map.ts`: exportar `phraseAppears` (o un wrapper `matchMostSpecific(words, tokens)` equivalente) — cambio mínimo, sin tocar lógica existente.

    Crear `milestone-heuristic.ts` con el contrato del bloque <interfaces>:
    - `MOVEMENT_VOCAB` declarativo por ruta, compilado a module-load (patrón COMPILED del analog), seedeado para las particiones largas verificadas en RESEARCH: TTB (tokens ATW, WINDSHIELD, BENT ARM, OA, 90, TTB — most-specific-first), FL, OAP, OAPU como mínimo. Rutas sin vocabulario → movement null para todos sus ejercicios (van al grupo "sin movimiento", el profe decide). Documentar en docblock que ampliar el vocab es barato y esperado.
    - Pipeline puro por partición (ruta × esfuerzo): (1) movimiento = primer token del vocab que aparece en normalizeWords(name), most-specific-first; (2) escalón = acceptedStep ?? classify(name, route).step (en vivo — el truth progression_step tiene 0 filas, Pitfall 6); (3) agrupar por (movimiento, escalón); (4) hito del grupo = nombre con menos tokens normalizados, tie-break menor dl, luego menor id; (5) emitir propuestas: hito → proposedMilestoneExerciseId null; resto → id del hito; movementToken y stepRank en todas; confidence 80 si movimiento detectado y grupo >1, 60 si grupo singleton con movimiento, 40 si movimiento null.
    - Sin acceso a DB en este módulo (función pura — la carga de catálogo vive en el CLI). Sin `any`; tipos explícitos.
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-api && pnpm exec vitest run test/exercises/milestone-heuristic.test.ts && pnpm exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Los 6 tests del bloque behavior pasan
    - `grep -c "from \"./route-progression-map\"" src/modules/exercises/milestone-heuristic.ts` ≥ 1 (reusa, no duplica normalización)
    - `grep -c "export function proposeMilestones\|export const proposeMilestones" src/modules/exercises/milestone-heuristic.ts` = 1
    - El módulo no importa drizzle ni db (puro): `grep -c "from \"drizzle-orm\"\|/db/" src/modules/exercises/milestone-heuristic.ts` = 0
  </acceptance_criteria>
  <done>Motor puro determinístico con vocab declarativo, tests verdes, cota ≤13 hitos sobre seed TTB-like.</done>
</task>

<task type="auto">
  <name>Task 2: CLI bootstrap-milestones.ts (propuestas pending, idempotente)</name>
  <files>el-templo-api/bootstrap-milestones.ts, el-templo-api/test/exercises/bootstrap-milestones.test.ts</files>
  <read_first>
    - el-templo-api/bootstrap-dimensions.ts (analog COMPLETO: header "console.log is acceptable here: standalone CLI" líneas 27-28; runBootstrap genérico línea 98; INSERT idempotente WHERE NOT EXISTS líneas 185-192; narrower readCatalogRows líneas 205-229; guard de entrypoint líneas 261-267; constantes ENGINE/CONFIDENCE líneas 40-43)
    - el-templo-api/test/exercises/bootstrap-dimensions.test.ts (analog del test: importa runBootstrap y lo corre contra la DB per-worker sin spawnear proceso)
    - el-templo-api/src/modules/exercises/milestone-heuristic.ts (Task 1)
  </read_first>
  <action>
    Copiar la estructura completa de `bootstrap-dimensions.ts`:
    - READ: catálogo en scope candidato a hito — `canonical_exercise_id IS NULL AND effort IN ('CON','EXC','ISO') AND habilidad IS NULL AND routes.excluded_from_tree = 0` (mismo scope del backbone actual; el filtro por milestone aún no aplica porque se está proponiendo). LEFT JOIN a `exercise_dimension_proposals` para traer `proposed_step` cuando `status='accepted'` (→ acceptedStep del contrato del motor). Narrower sin `any` para el resultado de db.execute.
    - TRANSFORM: `proposeMilestones()` por partición (agrupar filas por route+effort en memoria — catálogo ~1.5k filas, evita subqueries correlacionadas, Pitfall 3).
    - WRITE: INSERT idempotente en `exercise_milestone_proposals` con `WHERE NOT EXISTS (SELECT 1 ... WHERE exercise_id = X)` — espejo de las líneas 185-192 del analog. `ENGINE = "milestone-heuristic-v1"`. Status siempre 'pending'. PROHIBIDO tocar `exercises` (anti-patrón explícito del RESEARCH).
    - Export genérico `runBootstrapMilestones<TSchema>(db)` + guard de entrypoint con `process.argv[1].endsWith("bootstrap-milestones.ts")`. console.log legítimo (CLI standalone, documentar en header).

    Test (analog bootstrap-dimensions.test.ts): seed de 2 particiones con helpers, correr runBootstrapMilestones dos veces → (a) propuestas pending creadas con movementToken/stepRank/engine; (b) segunda corrida no duplica (UNIQUE(exercise_id) + NOT EXISTS); (c) cero filas de exercises modificadas; (d) un ejercicio con propuesta preexistente se saltea (skipped).
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-api && pnpm exec vitest run test/exercises/bootstrap-milestones.test.ts && pnpm exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Test de idempotencia pasa (2 corridas → mismo conteo de filas en exercise_milestone_proposals)
    - `grep -c "NOT EXISTS" bootstrap-milestones.ts` ≥ 1
    - `grep -c "UPDATE exercises\|update(schema.exercises)\|update(exercises)" bootstrap-milestones.ts` = 0 (nunca escribe truth)
    - `grep -c "export async function runBootstrapMilestones" bootstrap-milestones.ts` = 1
  </acceptance_criteria>
  <done>CLI idempotente listo para correr post-deploy (manual); solo inserta pending; test verde.</done>
</task>

<task type="auto">
  <name>Task 3: CLI bootstrap-elite-prereqs.ts (aristas manual cross-ruta R4, idempotente)</name>
  <files>el-templo-api/bootstrap-elite-prereqs.ts, el-templo-api/test/exercises/bootstrap-milestones.test.ts</files>
  <read_first>
    - el-templo-api/bootstrap-dimensions.ts (misma estructura de CLI: header, export genérico, narrowers, guard)
    - el-templo-api/src/db/schema/exercise-progressions.ts (aristas: from_exercise_id, to_exercise_id, source enum auto|manual, UNIQUE(from,to))
    - el-templo-api/src/modules/tree-editor/service.ts (setPrecedenceEdge líneas 542-567 — semántica de arista cross-partición que este CLI reproduce por datos)
  </read_first>
  <action>
    Crear `bootstrap-elite-prereqs.ts` (raíz del paquete, misma estructura CLI que Task 2) con `ELITE_PREREQS = [{ elite: 'FLR', base: 'FL' }, { elite: 'PLPU', base: 'PL' }]` y regla determinística documentada:
    - Para cada par y para effort 'CON': target = primer ejercicio backbone de la ruta élite por orden (dificultad_lineal ASC, id ASC) dentro del scope backbone (canonical IS NULL, habilidad IS NULL, milestone_exercise_id IS NULL, route no excluida); source = ejercicio backbone de la ruta base con el mayor dificultad_lineal estrictamente menor que target.dificultad_lineal (tie-break id ASC). Pisos verificados en RESEARCH: FLR arranca en dl 5, PLPU en dl 4 — la regla los respeta sin hardcodear los valores.
    - INSERT en `exercise_progressions (from_exercise_id, to_exercise_id, source)` VALUES (source, target, 'manual') con WHERE NOT EXISTS sobre (from,to). Si ya existe CUALQUIER arista manual entrante cross-ruta hacia la ruta élite, saltear el par entero (los profes pueden haberla ajustado — no pisar criterio humano).
    - Si una ruta del par no existe o no tiene ejercicios en scope → skip con log, nunca throw (CLI resumible).

    Test: agregar describe block en `test/exercises/bootstrap-milestones.test.ts` (o archivo propio si queda más claro): seed FL con dl 1-6 y FLR con dl 5-8 → corre el CLI → existe exactamente 1 arista manual FL(dl<5 máx)→FLR(primer dl 5); segunda corrida → 0 insertadas; con arista manual preexistente hacia FLR → skip.
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-api && pnpm exec vitest run test/exercises/bootstrap-milestones.test.ts && pnpm exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Test crea la arista manual cross-ruta esperada y la segunda corrida inserta 0
    - `grep -c "'manual'" bootstrap-elite-prereqs.ts` ≥ 1 y `grep -c "NOT EXISTS" bootstrap-elite-prereqs.ts` ≥ 1
    - `grep -c "export async function runBootstrapElitePrereqs" bootstrap-elite-prereqs.ts` = 1
    - El CLI no borra ni actualiza aristas existentes: `grep -c "DELETE FROM exercise_progressions\|UPDATE exercise_progressions" bootstrap-elite-prereqs.ts` = 0
  </acceptance_criteria>
  <done>Maquinaria R4 lista (open question 2 resuelta según constraint): CLI idempotente mergeado; la ejecución en prod queda como paso manual post-deploy documentado en el SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → DB | Los CLIs corren solo por operador (post-deploy manual); no hay input de red |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-20 | Tampering | bootstrap-milestones escribe truth sin revisión | mitigate | El CLI inserta SOLO filas pending en exercise_milestone_proposals; acceptance criteria con grep gate prohíbe UPDATE exercises; frontera documentada en docblock |
| T-133-21 | Tampering | SQL injection en sql`` crudo de los CLIs | mitigate | Interpolar SOLO vía `${}` (parametriza — patrón rebuild existente); agregaciones en memoria, sin subqueries correlacionadas (Pitfall 3) |
| T-133-22 | Tampering | bootstrap-elite-prereqs pisa criterio humano | mitigate | Skip del par si ya existe arista manual entrante cross-ruta; NOT EXISTS sobre (from,to); nunca DELETE/UPDATE |
| T-133-SC | Tampering | npm/pip/cargo installs | accept | Cero instalaciones en esta fase |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit -p tsconfig.json` limpio en el-templo-api.
- Tests puntuales de los 2 archivos nuevos verdes (NO suite completa local).
- Ejecución real de los CLIs contra la DB local de staging: opcional en este plan (puede correrse para inspección manual del usuario); la ejecución en prod es paso post-deploy manual — documentarlo en el SUMMARY.
</verification>

<success_criteria>
- Heurística determinística con cota ≤13 hitos sobre partición larga, CLIs idempotentes que nunca escriben truth ni pisan trabajo manual, todo testeado y tipado sin `any`.
</success_criteria>

<output>
Create `.planning/phases/133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul/133-03-SUMMARY.md` when done
</output>
