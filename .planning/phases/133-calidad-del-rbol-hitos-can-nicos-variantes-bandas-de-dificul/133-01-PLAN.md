---
phase: 133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/schema/exercises.ts
  - el-templo-api/src/db/schema/exercise-milestone-proposals.ts
  - el-templo-api/src/db/schema/index.ts
  - el-templo-api/src/db/migrations/0145_milestone_exercise_id.sql
  - el-templo-api/test/migrations/0145-milestone-exercise-id.test.ts
autonomous: true
requirements: [R1-MIG]
must_haves:
  truths:
    - "La columna exercises.milestone_exercise_id existe (INT NULL, self-FK SET NULL, indexada) y la tabla exercise_milestone_proposals existe con UNIQUE(exercise_id)"
    - "La migración 0145 aplicó vía el runner custom y quedó registrada en _migrations"
    - "Nada cambia en el backbone hasta que un profe acepte: el default NULL no altera ningún node-set"
  artifacts:
    - path: "el-templo-api/src/db/migrations/0145_milestone_exercise_id.sql"
      provides: "DDL de la columna milestone_exercise_id + tabla exercise_milestone_proposals"
      contains: "milestone_exercise_id"
    - path: "el-templo-api/src/db/schema/exercise-milestone-proposals.ts"
      provides: "Modelo Drizzle de propuestas de hito"
      exports: ["exerciseMilestoneProposals"]
    - path: "el-templo-api/test/migrations/0145-milestone-exercise-id.test.ts"
      provides: "Test de integración de la migración (information_schema + round-trip)"
  key_links:
    - from: "el-templo-api/src/db/schema/exercises.ts"
      to: "el-templo-api/src/db/migrations/0145_milestone_exercise_id.sql"
      via: "nombre físico de columna idéntico en int() 1er-arg y ALTER TABLE"
      pattern: "milestone_exercise_id"
    - from: "el-templo-api/src/db/schema/index.ts"
      to: "el-templo-api/src/db/schema/exercise-milestone-proposals.ts"
      via: "export * from"
      pattern: "exercise-milestone-proposals"
---

<objective>
Crear la base de datos de R1: columna `milestone_exercise_id` en `exercises` (NULL = hito o sin clasificar → backbone; NOT NULL = variante colgando del hito apuntado) y tabla `exercise_milestone_proposals` (opción A del RESEARCH — espejo de la migración 0138, decisión del planner confirmada por las constraints de la fase).

Purpose: sin la columna y la tabla de propuestas no existe el eje hito/variante; todo el resto de la fase depende de este schema.
Output: migración 0145 aplicada localmente, schema Drizzle espejado, test de migración verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul/133-RESEARCH.md
@.planning/phases/133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul/133-PATTERNS.md (secciones 1-3)
</context>

<interfaces>
Contratos que este plan CREA y que los planes 03/04/05 consumen:

Schema `exercises` (nuevo campo Drizzle):
- `milestoneExerciseId: int("milestone_exercise_id")` — self-FK a `exercises.id`, `onDelete: "set null"`, índice `exercises_milestone_idx`.

Schema `exerciseMilestoneProposals` (tabla nueva `exercise_milestone_proposals`):
- `id` INT AI PK
- `exerciseId` → `exercise_id` INT NOT NULL, FK exercises(id) ON DELETE CASCADE, UNIQUE
- `proposedMilestoneExerciseId` → `proposed_milestone_exercise_id` INT NULL, FK exercises(id) ON DELETE SET NULL (NULL = propuesto como HITO)
- `movementToken` → `movement_token` VARCHAR(100) NULL (NULL = sin movimiento detectado)
- `stepRank` → `step_rank` INT NULL (escalón usado para agrupar)
- `status` ENUM('pending','accepted','rejected') NOT NULL DEFAULT 'pending'
- `engine` VARCHAR(30) NOT NULL
- `confidence` INT NULL
- `createdAt` → `created_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
- Índices: UNIQUE `exercise_milestone_proposals_exercise_uq` (exercise_id), `exercise_milestone_proposals_status_idx` (status)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Schema Drizzle — columna milestone_exercise_id + tabla exercise_milestone_proposals</name>
  <files>el-templo-api/src/db/schema/exercises.ts, el-templo-api/src/db/schema/exercise-milestone-proposals.ts, el-templo-api/src/db/schema/index.ts</files>
  <read_first>
    - el-templo-api/src/db/schema/exercises.ts (analog exacto: `canonicalExerciseId` líneas 55-58 + índice línea 82; `AnyMySqlColumn` ya importado en línea 8)
    - el-templo-api/src/db/schema/exercise-dimension-proposals.ts (espejo completo para la tabla nueva: enum de status líneas 22-26, docblock de frontera líneas 28-51, tabla + índices líneas 52-76)
    - el-templo-api/src/db/schema/index.ts (estilo de export)
  </read_first>
  <action>
    En `exercises.ts`: agregar `milestoneExerciseId: int("milestone_exercise_id").references((): AnyMySqlColumn => exercises.id, { onDelete: "set null" })` inmediatamente después de `canonicalExerciseId`, con docblock que documente la semántica (NULL = hito o sin clasificar → entra al backbone; NOT NULL = variante colgando del hito apuntado → fuera del backbone; truth escrito SOLO por el accept transaccional del profe, nunca por la heurística). Agregar `index("exercises_milestone_idx").on(table.milestoneExerciseId)` en el array de índices de la tabla, espejo del índice de canonical.

    Crear `exercise-milestone-proposals.ts` como espejo estructural de `exercise-dimension-proposals.ts`, con las columnas del bloque <interfaces> de este plan (nombres físicos EXACTOS — Pitfall 1 del RESEARCH: el 1er arg de int()/varchar()/mysqlEnum ES el nombre físico). Definir el enum de status propio (`exercise_milestone_proposal_status` o reusar patrón mysqlEnum inline como el analog) con valores pending/accepted/rejected. Replicar el docblock de frontera del analog: las propuestas NUNCA escriben en exercises; el truth se escribe únicamente en el accept.

    En `index.ts`: agregar `export * from "./exercise-milestone-proposals";` junto al export de exercise-dimension-proposals (mantener orden del archivo).
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-api && pnpm exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'milestone_exercise_id' src/db/schema/exercises.ts` ≥ 1 y el literal aparece como 1er arg de `int()`
    - `grep -c 'exercises_milestone_idx' src/db/schema/exercises.ts` = 1
    - `grep -c 'export \* from "./exercise-milestone-proposals"' src/db/schema/index.ts` = 1
    - `exercise-milestone-proposals.ts` exporta `exerciseMilestoneProposals` con UNIQUE sobre exercise_id (`uniqueIndex(...).on(table.exerciseId)`) e índice de status
    - tsc pasa sin errores
  </acceptance_criteria>
  <done>Schema Drizzle compila con la columna nueva en exercises y la tabla de propuestas de hito exportada desde index.ts.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Migración 0145 — escribir SQL, aplicar con el runner custom y testear</name>
  <files>el-templo-api/src/db/migrations/0145_milestone_exercise_id.sql, el-templo-api/test/migrations/0145-milestone-exercise-id.test.ts</files>
  <read_first>
    - el-templo-api/src/db/migrations/0143_route_progression_model.sql (plantilla de header: comment safety + reversibilidad + hand-written; estilo ALTER líneas 39-45)
    - el-templo-api/src/db/migrations/0138_create_exercise_dimension_proposals.sql (plantilla CREATE TABLE + convención de naming de FK `<tabla>_<col>_<reftabla>_<refcol>_fk` líneas 62-63)
    - el-templo-api/test/migrations/0121-users-lead-fields.test.ts (analog del test: helper getColumn vía INFORMATION_SCHEMA líneas 54-68, asserts de FK/DELETE_RULE/índices, round-trip Drizzle, fila en _migrations)
    - el-templo-api/src/db/schema/exercises.ts y exercise-milestone-proposals.ts (recién modificados en Task 1 — los nombres físicos del SQL deben coincidir EXACTO)
  </read_first>
  <action>
    Escribir `0145_milestone_exercise_id.sql` a mano (NO drizzle-kit generate — journal desincronizado) con el header de disciplina de 0143 (comment safety: el runner splittea por punto y coma ANTES de strippear los comentarios — PROHIBIDO cualquier `;` dentro de un comentario; documentar reversibilidad: DROP de FK/índice/columna y DROP TABLE). Statements, cada uno con un único terminador en su propia línea:
    1. `ALTER TABLE exercises ADD COLUMN milestone_exercise_id INT NULL DEFAULT NULL AFTER canonical_exercise_id`
    2. `CREATE INDEX exercises_milestone_idx ON exercises(milestone_exercise_id)`
    3. `ALTER TABLE exercises ADD CONSTRAINT exercises_milestone_exercise_id_exercises_id_fk FOREIGN KEY (milestone_exercise_id) REFERENCES exercises(id) ON DELETE SET NULL`
    4. `CREATE TABLE exercise_milestone_proposals` con las columnas exactas del bloque <interfaces> del plan: id INT AUTO_INCREMENT PRIMARY KEY, exercise_id INT NOT NULL, proposed_milestone_exercise_id INT NULL, movement_token VARCHAR(100) NULL, step_rank INT NULL, status ENUM('pending','accepted','rejected') NOT NULL DEFAULT 'pending', engine VARCHAR(30) NOT NULL, confidence INT NULL, created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, UNIQUE KEY exercise_milestone_proposals_exercise_uq (exercise_id), KEY exercise_milestone_proposals_status_idx (status), CONSTRAINT exercise_milestone_proposals_exercise_id_exercises_id_fk FOREIGN KEY (exercise_id) REFERENCES exercises(id) ON DELETE CASCADE, CONSTRAINT exercise_milestone_proposals_proposed_milestone_exercise_id_fk FOREIGN KEY (proposed_milestone_exercise_id) REFERENCES exercises(id) ON DELETE SET NULL

    Nota MySQL: el nombre de constraint tiene límite de 64 chars — el naming Drizzle-convergente completo para proposed_milestone excede el patrón; usar el nombre acortado indicado arriba y dejar comentario.

    Aplicar localmente: `cd el-templo-api && pnpm db:migrate` (runner custom — NUNCA drizzle-kit migrate ni db:push). Verificar salida: 0145 aplicada y registrada en `_migrations`.

    Escribir el test `0145-milestone-exercise-id.test.ts` (analog 0121): (a) INFORMATION_SCHEMA.COLUMNS confirma `exercises.milestone_exercise_id` INT nullable; (b) INFORMATION_SCHEMA.REFERENTIAL_CONSTRAINTS/KEY_COLUMN_USAGE confirma la FK self con DELETE_RULE 'SET NULL' y la FK CASCADE de la tabla de propuestas; (c) INFORMATION_SCHEMA.STATISTICS confirma `exercises_milestone_idx` y el UNIQUE de exercise_id en proposals; (d) round-trip Drizzle: insertar un ejercicio hito + uno variante apuntándolo + una propuesta pending y leerlos; (e) `_migrations` contiene exactamente una fila para 0145. Usar los helpers existentes del directorio test/migrations.
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-api && pnpm exec vitest run test/migrations/0145-milestone-exercise-id.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm db:migrate` corrió sin error y `grep -v '^--' src/db/migrations/0145_milestone_exercise_id.sql | grep -c 'milestone_exercise_id'` ≥ 3
    - Ningún comentario del SQL contiene punto y coma: `grep '^--' src/db/migrations/0145_milestone_exercise_id.sql | grep -c ';'` = 0
    - El test 0145 pasa (vitest run del archivo puntual — NO correr la suite completa, regla del proyecto)
    - Nombre físico idéntico entre ALTER y schema: `grep -c '"milestone_exercise_id"' src/db/schema/exercises.ts` = 1
  </acceptance_criteria>
  <done>Migración 0145 aplicada localmente vía runner custom, registrada en _migrations, SQL commiteado junto al schema, test de migración verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ninguna nueva | Este plan es DDL + schema; no expone superficie de entrada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-01 | Tampering | migración 0145 / runner custom | mitigate | SQL manuscrito sin `;` en comentarios (el runner splittea por `;` antes de strippear `--`); test de migración valida columna/FK/índices vía INFORMATION_SCHEMA |
| T-133-02 | Tampering | integridad referencial hito↔variante | mitigate | FK self con ON DELETE SET NULL (variante huérfana vuelve a hito, nunca referencia colgante); FK CASCADE en proposals |
| T-133-SC | Tampering | npm/pip/cargo installs | accept | La fase no instala ningún paquete (Package Legitimacy Audit del RESEARCH: cero cambios en package.json) |
</threat_model>

<verification>
- `cd el-templo-api && pnpm exec tsc --noEmit -p tsconfig.json` limpio.
- `pnpm exec vitest run test/migrations/0145-milestone-exercise-id.test.ts` verde.
- `_migrations` contiene 0145 (el test lo asserta).
- NO correr la suite completa local; CI la corre al pushear (con confirmación del usuario).
</verification>

<success_criteria>
- Columna + tabla existen en la DB local, schema Drizzle espejado exacto (nombres físicos idénticos), migración trackeada por el runner custom, test puntual verde. Default NULL garantiza cero cambio de comportamiento hasta que un profe acepte.
</success_criteria>

<output>
Create `.planning/phases/133-calidad-del-rbol-hitos-can-nicos-variantes-bandas-de-dificul/133-01-SUMMARY.md` when done
</output>
