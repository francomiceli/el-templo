---
phase: 40-day-player-redesign
plan: 05
type: execute
wave: 2
depends_on: ["40-04"]
files_modified:
  - el-templo-app/src/modules/training/components/player/StoryExerciseCard.vue
  - el-templo-app/src/modules/training/components/BlockProgressionView.vue
  - el-templo-app/src/modules/training/pages/DayPlayer.vue
autonomous: false
requirements: []
must_haves:
  truths:
    - "App builds successfully with zero TypeScript errors"
    - "DayPlayer renders without runtime errors when navigating to a session"
    - "All CONTEXT.md decisions are implemented as specified"
  artifacts:
    - path: "el-templo-app/src/modules/training/pages/DayPlayer.vue"
      provides: "Complete redesigned DayPlayer passing build verification"
  key_links:
    - from: "DayPlayer.vue"
      to: "all player components"
      via: "component imports and event wiring"
      pattern: "import.*player/"
---

<objective>
Build verification and visual inspection checkpoint for the complete DayPlayer redesign. Verify the app builds, run a type check, fix any remaining integration issues, and present the redesigned player for user visual verification.

Purpose: Ensure everything from Plans 01-04 integrates correctly, the app compiles, and the user can visually verify the redesigned experience matches their vision from the CONTEXT.md discussion.

Output: A verified, building DayPlayer redesign ready for staging deployment.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/40-day-player-redesign/40-CONTEXT.md
@.planning/phases/40-day-player-redesign/40-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and integration fixes</name>
  <files>el-templo-app/src/modules/training/components/player/StoryExerciseCard.vue, el-templo-app/src/modules/training/components/BlockProgressionView.vue, el-templo-app/src/modules/training/pages/DayPlayer.vue</files>
  <action>
Run the full verification suite:

1. **TypeScript check**: `cd el-templo-app && npx vue-tsc --noEmit`
   - Fix any type errors across the player module
   - Common issues: prop type mismatches between parent/child, missing imports, ref/computed unwrapping

2. **Build check**: `cd el-templo-app && pnpm build`
   - Fix any build errors (missing modules, circular imports, Vite resolution issues)

3. **Lint check**: `cd el-templo-app && pnpm lint`
   - Fix any ESLint/Prettier issues

4. **CONTEXT.md decision audit** — systematically verify each locked decision:
   - [ ] Tap zones: right=next, left=prev (navigation only, NOT completion)
   - [ ] Segmented progress bars at top with filled/empty/active states
   - [ ] Header overlay on video (back, block name, timer) with semi-transparent gradient
   - [ ] Split card: ~70% video/name hero, ~30% exercise detail
   - [ ] No-video fallback: exercise name hero on dark background with contraction badge
   - [ ] Bottom detail shows: name, Cantidad (not Dosis), contraction, notes, position
   - [ ] Completion ONLY via "Completar" button
   - [ ] No checkboxes anywhere in the player
   - [ ] Compact list: no left icons, green check on right for completed
   - [ ] Tapping compact list row navigates to that story slide
   - [ ] Mobility: own story slide + in compact list + name-only in transition
   - [ ] Between-block: card overlay on blur, mobility + quote, button only
   - [ ] Splash: card overlay on blur, Comenzar button, no auto-advance
   - [ ] Celebration: flame icon, card overlay on blur, Ver Resumen button, no auto-advance
   - [ ] "Dosis" → "Cantidad" renamed throughout

5. **Fix any gaps** found during the audit. Common issues:
   - Missing `Cantidad` rename in a template string
   - Missing mobility slide in segmented bar count
   - Incorrect prop wiring between BlockProgressionView and DayPlayer

If the build and type check pass with zero errors, proceed to the checkpoint.
</action>
<verify>
<automated>cd /home/franco/projects/el-templo/el-templo-app && npx vue-tsc --noEmit --pretty 2>&1 | tail -5 && pnpm build 2>&1 | tail -10</automated>
</verify>
<done>App passes TypeScript check and build with zero errors. All CONTEXT.md decisions verified as implemented. No remaining integration issues.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of redesigned DayPlayer</name>
  <files>el-templo-app/src/modules/training/pages/DayPlayer.vue</files>
  <action>
CHECKPOINT: Visual verification of the complete DayPlayer redesign.

What was built:

- Segmented progress bar at top of story area
- Story exercise cards with video/name hero (70%) + exercise detail (30%)
- Tap zones for navigation (right=next, left=prev)
- "Completar" button as sole completion mechanism
- Compact exercise list below story card
- Card-overlay-on-blur screens for splash, between-block transition, and celebration
- Flame icon on celebration (not trophy)
- Motivational quotes on transition and celebration screens
- "Dosis" renamed to "Cantidad" throughout

How to verify:

1. Start dev server: `cd el-templo-app && pnpm dev`
2. Login and navigate to a session (training/session/{date})
3. Verify splash screen: card overlay on blur, Comenzar button, no auto-advance
4. Verify Stories interface: segmented bar, tap right=next / left=prev, glow on active, gold on completed
5. Verify exercise detail (bottom 30%): name, Cantidad (not Dosis), contraction, notes, Completar button
6. Verify compact list: no left icons, green check right, tap navigates
7. Verify mobility slide: last story slide in non-INITIUM blocks, also in compact list
8. Complete block → transition screen: blur overlay, mobility reminder, quote, button-only dismissal
9. Complete all blocks → celebration: flame icon, quote, Ver Resumen button, no auto-advance

Resume signal: Type "approved" or describe issues to fix
</action>
<verify>
<automated>cd /home/franco/projects/el-templo/el-templo-app && npx vue-tsc --noEmit --pretty 2>&1 | tail -3</automated>
</verify>
<done>User has visually verified the redesigned DayPlayer and approved or issues have been resolved.</done>
</task>

</tasks>

<verification>
1. `vue-tsc --noEmit` passes with zero errors
2. `pnpm build` succeeds
3. All 16 CONTEXT.md locked decisions verified as implemented
4. User visually approves the redesigned DayPlayer experience
</verification>

<success_criteria>

- Zero TypeScript errors, zero build errors
- User confirms the Stories-style player matches their vision
- All CONTEXT.md decisions implemented without exception
  </success_criteria>

<output>
After completion, create `.planning/phases/40-day-player-redesign/40-05-SUMMARY.md`
</output>
