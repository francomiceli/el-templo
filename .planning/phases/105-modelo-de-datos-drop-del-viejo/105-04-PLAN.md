---
phase: 105-modelo-de-datos-drop-del-viejo
plan: 04
type: execute
wave: 3
depends_on:
  - "105-02"
files_modified:
  - el-templo-api/src/modules/analytics/service.ts
  - el-templo-api/src/modules/reports/service.ts
autonomous: true
requirements:
  - TXN-03
user_setup: []
must_haves:
  truths:
    - "analytics/service.ts queries de revenue corren contra financial_transactions con kind IN ('plan_charge','debt_settlement') AND voided_at IS NULL AND direction = 'inflow'"
    - "reports/service.ts queries (Drizzle y SQL raw) reescritas contra financial_transactions con join a transaction_links y subscriptions cuando aplica"
    - "ningún query en analytics o reports referencia las tablas payments o debts después de este plan"
    - "pnpm typecheck pasa para los 2 archivos modificados"
  artifacts:
    - path: el-templo-api/src/modules/analytics/service.ts
      provides: "Revenue queries (monthlyRevenue, revenueByMethod, revenueByBranch) sobre financial_transactions"
      contains: "financialTransactions"
    - path: el-templo-api/src/modules/reports/service.ts
      provides: "Reports (Drizzle y raw SQL) sobre financial_transactions con joins via transaction_links"
      contains: "financial_transactions"
  key_links:
    - from: el-templo-api/src/modules/analytics/service.ts
      to: el-templo-api/src/db/schema/financial-transactions.ts
      via: "schema.financialTransactions in queries"
      pattern: "schema\\.financialTransactions"
    - from: el-templo-api/src/modules/reports/service.ts
      to: el-templo-api/src/db/schema
      via: "join financial_transactions ft + transaction_links tl + subscriptions s"
      pattern: "transaction_links|financialTransactions"
---

<objective>
Reescribir los queries de revenue/reports en `analytics/service.ts` (L780-L870+) y `reports/service.ts` (L160-L200, L434, L755+) contra `financial_transactions` (con joins a `transaction_links` y `subscriptions` cuando se necesita info del plan), sustituyendo los filtros por `WHERE kind IN ('plan_charge','debt_settlement') AND voided_at IS NULL AND direction = 'inflow'` per D-01. NO stub, NO defer — al final de Phase 105 los dashboards de admin deben funcionar end-to-end con la nueva tabla.

Purpose: D-01 lockeó que la fase no se entrega con dashboards rotos. Sin este plan, admin pierde visibilidad de revenue mientras Phase 106-109 entregan la UX nueva.
Output: 2 archivos con queries reescritas, mismas firmas públicas, mismas response shapes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/105-modelo-de-datos-drop-del-viejo/105-SPEC.md
@.planning/phases/105-modelo-de-datos-drop-del-viejo/105-CONTEXT.md
@.planning/phases/105-modelo-de-datos-drop-del-viejo/105-PATTERNS.md
@.planning/phases/105-modelo-de-datos-drop-del-viejo/105-02-SUMMARY.md
@CLAUDE.md
@el-templo-api/src/modules/analytics/service.ts
@el-templo-api/src/modules/reports/service.ts
@el-templo-api/src/db/schema/financial-transactions.ts
@el-templo-api/src/db/schema/transaction-links.ts

<interfaces>
Current analytics filter (L788 area uses schema.payments + paymentDate). Replacement uses schema.financialTransactions, transactionDate, plus added inArray(kind, ['plan_charge','debt_settlement']) and eq(direction,'inflow').

Current reports raw SQL (L179) joins payments p → users → branches → subscriptions s ON s.id = p.subscription_id → subscription_plans → users r. Replacement joins financial_transactions ft → users m → branches b ON b.id = ft.branch_id (branch is now first-class on ft) → transaction_links tl ON tl.transaction_id = ft.id AND tl.target_kind = 'subscription' → subscriptions s ON s.id = tl.target_id → subscription_plans sp → users r. WHERE adds ft.kind IN ('plan_charge','debt_settlement') AND ft.voided_at IS NULL AND ft.direction = 'inflow'.

Field rename note: paymentDate (in old payments.ts) → transactionDate (in financial_transactions). Keep the alias in raw SQL (`SELECT ft.transaction_date AS paymentDate`) and Drizzle (`.as("paymentDate")`) so consumer types/mappers don't change.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite analytics/service.ts revenue queries</name>
  <read_first>
    - el-templo-api/src/modules/analytics/service.ts (read full file or at least L750-L900 — capture all 3 query methods + their imports)
    - el-templo-api/src/db/schema/financial-transactions.ts (column names: transactionDate, kind, direction, voidedAt, paymentMethod, branchId)
    - el-templo-api/src/db/schema/index.ts (just updated — confirm financialTransactions is exported)
    - .planning/phases/105-modelo-de-datos-drop-del-viejo/105-CONTEXT.md D-01
    - .planning/phases/105-modelo-de-datos-drop-del-viejo/105-PATTERNS.md "analytics/service.ts rewrite"
  </read_first>
  <action>
**Imports update:** verify `inArray` is imported from `drizzle-orm`. If not, add it: `import { ..., inArray } from "drizzle-orm";`.

For each revenue method (the actual names per current file — read them; CONTEXT mentions `monthlyRevenue, revenueByMethod`, PATTERNS mentions `getRevenueTrend, getRevenueByMethod, getRevenueByBranch`):

1. Replace every `schema.payments` reference with `schema.financialTransactions`.
2. Replace `schema.payments.paymentDate` → `schema.financialTransactions.transactionDate`.
3. Replace `schema.payments.memberId` → `schema.financialTransactions.memberId`.
4. Replace `schema.payments.paymentMethod` → `schema.financialTransactions.paymentMethod`.
5. Replace `schema.payments.amount` → `schema.financialTransactions.amount`.
6. Replace `schema.payments.currency` → `schema.financialTransactions.currency`.
7. **Add 2 new conditions** to the WHERE clause of each method (D-01):
   - `inArray(schema.financialTransactions.kind, ["plan_charge", "debt_settlement"])`
   - `eq(schema.financialTransactions.direction, "inflow")`
8. Keep `isNull(schema.financialTransactions.voidedAt)`.

**For revenueByBranch specifically:** branch is now first-class on financial_transactions. Group by `schema.financialTransactions.branchId` directly — no JOIN to users for branch resolution.

**Type fixes:** literal arrays may need `as const` or `satisfies TransactionKind[]` if `inArray`'s generic constraint complains.

DO NOT touch code that doesn't query payments. Public return types UNCHANGED.
</action>
<verify>
<automated>cd el-templo-api &amp;&amp; grep -c "schema\.payments" src/modules/analytics/service.ts</automated>
</verify>
<acceptance_criteria> - `grep -c "schema\\.payments" el-templo-api/src/modules/analytics/service.ts` returns `0`. - `grep -c "schema\\.financialTransactions" el-templo-api/src/modules/analytics/service.ts` returns ≥6. - `grep -cE "inArray\\(schema\\.financialTransactions\\.kind" el-templo-api/src/modules/analytics/service.ts` returns ≥3. - `grep -cE "eq\\(schema\\.financialTransactions\\.direction" el-templo-api/src/modules/analytics/service.ts` returns ≥3. - `grep -c "isNull(schema\\.financialTransactions\\.voidedAt)" el-templo-api/src/modules/analytics/service.ts` returns ≥3. - `cd el-templo-api && pnpm tsc --noEmit 2>&1 | grep "analytics/service.ts" | wc -l` returns `0`.
</acceptance_criteria>
<done>3 revenue methods rewritten; D-01 filter applied; typecheck clean for this file.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite reports/service.ts queries (Drizzle + raw SQL)</name>
  <read_first>
    - el-templo-api/src/modules/reports/service.ts (read L150-L250, L420-L460, L745-L800)
    - el-templo-api/src/modules/analytics/service.ts (just modified — same patterns)
    - el-templo-api/src/db/schema/transaction-links.ts (UNIQUE on tuple; join key)
    - .planning/phases/105-modelo-de-datos-drop-del-viejo/105-PATTERNS.md "reports/service.ts rewrite"
  </read_first>
  <action>
**Block A — Drizzle query L160-L200 area:** Replace `schema.payments` with `schema.financialTransactions`. ADD a join via transaction_links because `financial_transactions` has NO `subscriptionId` column:

```typescript
.innerJoin(schema.transactionLinks, and(
  eq(schema.transactionLinks.transactionId, schema.financialTransactions.id),
  eq(schema.transactionLinks.targetKind, "subscription"),
))
.innerJoin(schema.subscriptions, eq(schema.subscriptions.id, schema.transactionLinks.targetId))
```

Add the same WHERE filters as analytics (`kind IN`, `direction = inflow`, `voidedAt IS NULL`). Preserve GROUP BY / ORDER BY / LIMIT verbatim. Keep `paymentDate` as output alias.

**Block B — Raw SQL block (~L179):** Replace SELECT/FROM block per the interfaces section. Pattern:

```typescript
const rows = await this.db.execute(sql`
  SELECT ft.id, ft.transaction_date AS paymentDate, ft.amount, ft.currency,
         ft.payment_method AS paymentMethod, ft.voided_at AS voidedAt, ...
  FROM financial_transactions ft
  INNER JOIN users m ON m.id = ft.member_id
  INNER JOIN branches b ON b.id = ft.branch_id
  INNER JOIN transaction_links tl ON tl.transaction_id = ft.id AND tl.target_kind = 'subscription'
  INNER JOIN subscriptions s ON s.id = tl.target_id
  INNER JOIN subscription_plans sp ON sp.id = s.plan_id
  INNER JOIN users r ON r.id = ft.recorded_by
  WHERE ft.kind IN ('plan_charge', 'debt_settlement')
    AND ft.voided_at IS NULL
    AND ft.direction = 'inflow'
    AND ${conditionsConcat}
`);
```

Preserve every existing parameter (`${dateFrom}`, `${branchId}`, etc.) at its identical position. Preserve every column alias so the row mapper at L204+ stays unchanged.

**Block C — L434 area:** apply the same rules. If it's a separate aggregate (`COUNT(*)`, `SUM(amount)`), swap table + add kind/direction/voided filters.

**Block D — L755+ area:** same.

**Critical for raw SQL:** Drizzle's `sql` template is parameterized via `${var}`. NEVER concatenate user input. All variables go through `${...}`.

After all 4 blocks, run `pnpm tsc --noEmit` to confirm types resolve. Match the existing destructuring/casting pattern for `this.db.execute(sql\`...\`)`results.
  </action>
  <verify>
    <automated>cd el-templo-api &amp;&amp; grep -nE "(FROM|from)\s+payments\b|schema\.payments|p\.payment_date" src/modules/reports/service.ts || echo "NONE"</automated>
  </verify>
  <acceptance_criteria>
    -`grep -cE "FROM\\s+payments\\b" el-templo-api/src/modules/reports/service.ts`returns`0`(raw SQL).
    -`grep -c "schema\\.payments" el-templo-api/src/modules/reports/service.ts`returns`0`(Drizzle).
    -`grep -c "schema\\.financialTransactions\\|financial_transactions" el-templo-api/src/modules/reports/service.ts`returns ≥4.
    -`grep -c "transaction_links\\|schema\\.transactionLinks" el-templo-api/src/modules/reports/service.ts`returns ≥1 (at least one query joins via the pivot).
    -`grep -cE "ft\\.kind IN \\('plan_charge', 'debt_settlement'\\)|kind:\\s*\\[.*plan_charge.\*debt_settlement" el-templo-api/src/modules/reports/service.ts`returns ≥1.
    -`cd el-templo-api && pnpm tsc --noEmit 2>&1 | grep "reports/service.ts" | wc -l`returns`0`.
    - Public method signatures UNCHANGED (verify by `git diff` — only body lines change).
</acceptance_criteria>
<done>4 query blocks rewritten to financial_transactions with transaction_links join; D-01 filter applied; raw SQL stays parameterized; typecheck clean.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                                                                 |
| ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| Admin → analytics/reports endpoints | These read-only endpoints already have RBAC (Phase 65 ReportesPage). This plan does NOT change auth — only the data source. |
| Raw SQL templates → MySQL           | All variables interpolated via Drizzle's `sql` template tag (parameterized). No concatenation.                              |

## STRIDE Threat Register

| Threat ID | Category                  | Component                                       | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                             |
| --------- | ------------------------- | ----------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-105-15  | Tampering / SQL Injection | Raw SQL in reports/service.ts                   | mitigate    | All `${var}` placeholders flow through Drizzle's `sql` tag (parameterized). Verified by code review + grep for any string concatenation in the templates (`"... " + var + " ..."` pattern).                                                                                                 |
| T-105-16  | Information Disclosure    | Revenue queries cross-branch leakage            | accept      | Pre-existing branch-scope guard (Phase 66 PAYMENT_READ_ROLES) lives in routes, not service. This plan preserves the existing routes; no new exposure.                                                                                                                                       |
| T-105-17  | Tampering                 | Filter regression — admin sees inflated revenue | mitigate    | The added `direction = 'inflow'` clause prevents counting outflows (refunds) as revenue. The `kind IN ('plan_charge','debt_settlement')` clause excludes `adjustment` (could be admin tweaks, NOT real revenue) and `advance_payment` (not yet earned). D-01 explicitly locked this filter. |

</threat_model>

<verification>
- `cd el-templo-api && pnpm tsc --noEmit` shows 0 errors in analytics/service.ts and reports/service.ts.
- Pre-existing typecheck errors elsewhere (members service still imports debtService, payments module still imports schema.payments) are EXPECTED and resolved in Plans 05-06.
- Spot-check that revenue queries return ARS-currency rows when seeded with a `plan_charge inflow` transaction (manual sanity check during execution).
</verification>

<success_criteria>

- analytics/service.ts: 3 revenue methods rewritten with D-01 filter; no `schema.payments` references.
- reports/service.ts: 4 query blocks (Drizzle + 3 raw SQL) rewritten with D-01 filter and `transaction_links` JOIN; no `schema.payments` or `FROM payments` references.
- Both files typecheck clean.
- Public method signatures unchanged.
  </success_criteria>

<output>
After completion, create `.planning/phases/105-modelo-de-datos-drop-del-viejo/105-04-SUMMARY.md` documenting:
- Exact method names rewritten (since CONTEXT and PATTERNS doc differed).
- Whether `paymentDate` alias was preserved or renamed in each block.
- Any consumer code paths that broke and were updated (if the column rename forced a downstream type change).
</output>
