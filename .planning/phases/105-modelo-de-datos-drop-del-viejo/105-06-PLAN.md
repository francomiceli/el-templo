---
phase: 105-modelo-de-datos-drop-del-viejo
plan: 06
type: execute
wave: 4
depends_on:
  - "105-03"
  - "105-04"
  - "105-05"
files_modified:
  - el-templo-api/src/app.ts
  - el-templo-api/src/db/schema/payments.ts
  - el-templo-api/src/db/schema/debts.ts
  - el-templo-api/src/modules/payments/index.ts
  - el-templo-api/src/modules/payments/types.ts
  - el-templo-api/src/modules/payments/schemas.ts
  - el-templo-api/src/modules/payments/service.ts
  - el-templo-api/src/modules/payments/routes.ts
  - el-templo-api/src/modules/members/debts-service.ts
  - el-templo-api/test/payments/payments.test.ts
  - el-templo-api/test/members/debts.test.ts
  - el-templo-admin/src/composables/usePaymentsApi.ts
autonomous: true
requirements:
  - TXN-03
  - TXN-04
user_setup: []
must_haves:
  truths:
    - "el-templo-api/src/modules/payments/ no existe en el filesystem"
    - "el-templo-api/src/modules/members/debts-service.ts no existe en el filesystem"
    - "el-templo-api/src/db/schema/payments.ts no existe en el filesystem"
    - "el-templo-api/src/db/schema/debts.ts no existe en el filesystem"
    - "test/payments/payments.test.ts no existe (replaced por test/finance/transaction-service.test.ts)"
    - "test/members/debts.test.ts no existe"
    - "el-templo-api/src/app.ts ya no registra paymentRoutes"
    - "grep -rE \"from ['\\\"].*\\\\b(payments|debts)\\\\b\" el-templo-api/src returns 0 matches"
    - "pnpm typecheck pasa limpio en el-templo-api (TXN-03 + TXN-04 backend complete)"
  artifacts:
    - path: el-templo-api/src/app.ts
      provides: "App bootstrap sin paymentRoutes register"
      contains: "buildApp"
  key_links:
    - from: el-templo-api/src/app.ts
      to: nothing-payments
      via: "no import or register of paymentRoutes"
      pattern: "^(?!.*paymentRoutes).*$"
---

<objective>
Borrar quirúrgicamente todo el código viejo del modelo de payments + debts en el backend: el módulo `el-templo-api/src/modules/payments/` completo (5 archivos), `el-templo-api/src/modules/members/debts-service.ts` (149 LOC), los schema files `payments.ts` y `debts.ts`, las suites de tests viejos `test/payments/payments.test.ts` y `test/members/debts.test.ts`, el composable frontend dead-code `el-templo-admin/src/composables/usePaymentsApi.ts` (si existe), y el `app.register(paymentRoutes, ...)` de `app.ts`. Después de este plan, `pnpm typecheck` debe pasar limpio en el-templo-api (acceptance criterion del SPEC §4).

Purpose: TXN-03 + TXN-04 backend — el "drop completo" del modelo viejo. Plans 03/04/05 dejaron a los importadores libres de la dependencia; ahora se eliminan los archivos.
Output: 12 archivos eliminados / modificados; grep gates verdes; typecheck verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/105-modelo-de-datos-drop-del-viejo/105-SPEC.md
@.planning/phases/105-modelo-de-datos-drop-del-viejo/105-CONTEXT.md
@.planning/phases/105-modelo-de-datos-drop-del-viejo/105-PATTERNS.md
@.planning/phases/105-modelo-de-datos-drop-del-viejo/105-03-SUMMARY.md
@.planning/phases/105-modelo-de-datos-drop-del-viejo/105-04-SUMMARY.md
@.planning/phases/105-modelo-de-datos-drop-del-viejo/105-05-SUMMARY.md
@CLAUDE.md
@el-templo-api/src/app.ts

<interfaces>
Files to delete (verified in PATTERNS.md "Files deleted entirely"):
1. el-templo-api/src/db/schema/payments.ts (replaced by financial-transactions.ts)
2. el-templo-api/src/db/schema/debts.ts (replaced by balances.ts)
3. el-templo-api/src/modules/payments/index.ts
4. el-templo-api/src/modules/payments/types.ts
5. el-templo-api/src/modules/payments/schemas.ts
6. el-templo-api/src/modules/payments/service.ts
7. el-templo-api/src/modules/payments/routes.ts
   (the entire `el-templo-api/src/modules/payments/` directory)
8. el-templo-api/src/modules/members/debts-service.ts
9. el-templo-api/test/payments/payments.test.ts (replaced by test/finance/transaction-service.test.ts)
10. el-templo-api/test/members/debts.test.ts
11. el-templo-admin/src/composables/usePaymentsApi.ts (if exists; PATTERNS.md says verify in planning)

File to modify:

- el-templo-api/src/app.ts L24, L120 — remove `import { paymentRoutes }` and `app.register(paymentRoutes, ...)`.

Grep gates from SPEC §4 / acceptance criteria (must all return 0):

- `grep -rE "from ['\"].*\\b(payments|debts)\\b['\"]" el-templo-api/src`
- `grep -rE "isDebtor|debtAmount|debtCurrency|debtNote" el-templo-admin/src` (Plan 07 covers admin; this plan handles backend grep only)

Note: comments/migrations referencing "payments" or "debts" historically are OK per SPEC. The grep targets `from '...'` import statements specifically.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update app.ts to drop paymentRoutes registration</name>
  <read_first>
    - el-templo-api/src/app.ts (read full file — capture import block + register block)
    - .planning/phases/105-modelo-de-datos-drop-del-viejo/105-PATTERNS.md "app.ts" (L24, L120)
  </read_first>
  <action>
Open `el-templo-api/src/app.ts`. Find:
- Line ~24: `import { paymentRoutes } from "./modules/payments";`. DELETE this entire line.
- Line ~120: `await app.register(paymentRoutes, { prefix: "/api/admin/payments" });`. DELETE this entire line.

Do NOT register `financeRoutes` — Phase 106 adds the new endpoints. For Phase 105, the finance module exposes the service classes only (per Plan 02 barrel: no `routes` export).

Other code in `app.ts` is untouched.
</action>
<verify>
<automated>cd el-templo-api &amp;&amp; grep -nE "paymentRoutes|payment_routes" src/app.ts || echo "NONE"</automated>
</verify>
<acceptance_criteria> - `grep -c "paymentRoutes" el-templo-api/src/app.ts` returns `0`. - `grep -c "from \"./modules/payments\"" el-templo-api/src/app.ts` returns `0`. - `cd el-templo-api && pnpm tsc --noEmit 2>&1 | grep "app.ts" | wc -l` returns `0`.
</acceptance_criteria>
<done>app.ts no longer imports or registers paymentRoutes; typecheck clean for this file.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete payments module + debts-service + schema files + dead tests + dead composable</name>
  <read_first>
    - el-templo-api/src/modules/payments/ (verify directory structure: index.ts / types.ts / schemas.ts / service.ts / routes.ts)
    - el-templo-api/src/modules/members/debts-service.ts (verify the file exists and is 149 LOC)
    - el-templo-api/src/db/schema/payments.ts and debts.ts (still on disk after Plan 01 — Plan 01 only updated the barrel)
    - el-templo-api/test/payments/payments.test.ts and test/members/debts.test.ts (verify they exist)
    - el-templo-admin/src/composables/usePaymentsApi.ts (PATTERNS.md says verify; may not exist)
    - .planning/phases/105-modelo-de-datos-drop-del-viejo/105-PATTERNS.md "Files deleted entirely"
  </read_first>
  <action>
Run a series of `rm` commands. Each is gated by `[ -e PATH ] && rm -v PATH` so missing files don't fail the task. List each command separately so the executor can audit:

```bash
# Schema files (already orphaned by Plan 01 barrel update)
rm el-templo-api/src/db/schema/payments.ts
rm el-templo-api/src/db/schema/debts.ts

# Payments module — delete entire directory
rm -rf el-templo-api/src/modules/payments

# Debts service file
rm el-templo-api/src/modules/members/debts-service.ts

# Old test suites (replaced by test/finance/transaction-service.test.ts)
rm el-templo-api/test/payments/payments.test.ts
rmdir el-templo-api/test/payments 2>/dev/null || true
rm el-templo-api/test/members/debts.test.ts
# do NOT rmdir test/members — other tests live there (members.test.ts, members-leads-filter.test.ts, etc.)

# Dead admin composable (verify existence first)
[ -f el-templo-admin/src/composables/usePaymentsApi.ts ] && rm -v el-templo-admin/src/composables/usePaymentsApi.ts || echo "usePaymentsApi.ts not present (OK)"
```

After deletions, run `cd el-templo-api && pnpm tsc --noEmit` and check for any residual import errors. If any file still imports from `./payments` or `./debts-service` or `../../db/schema/payments` etc., that import was missed in Plans 03-05 — fix it inline:

- If it's a JSDoc/comment reference, leave it.
- If it's a real import statement that survived, find what it was using (likely a type or service the previous plans should have removed) and remove it.

**Common surprises to handle:**

- `el-templo-api/src/db/schema/index.ts` — Plan 01 already removed the `export * from "./payments"` and `export * from "./debts"` lines. Verify they're still gone (`grep -c "from \"./payments\\|from \"./debts" el-templo-api/src/db/schema/index.ts` returns 0).
- Any test helper that imported from payments — check `test/helpers.ts` for stale imports.
- The migration `0096_debts_table.sql` etc. — leave intact (historical record).

Final grep gate per SPEC §4 acceptance criterion:

```bash
grep -rE "from ['\"].*/(payments|debts)/?['\"]" el-templo-api/src
```

This must return 0 matches in `src/` (test/ and migrations/ are excluded by the `src/` scope; raw word "payments" inside migration filenames is fine).
</action>
<verify>
<automated>cd el-templo-api &amp;&amp; ls src/modules/payments 2&gt;/dev/null; ls src/db/schema/payments.ts src/db/schema/debts.ts src/modules/members/debts-service.ts 2&gt;/dev/null; grep -rE "from ['\"]._/(payments|debts)/?['\"]" src 2&gt;/dev/null | wc -l</automated>
</verify>
<acceptance_criteria> - `[ ! -d el-templo-api/src/modules/payments ]` — directory does NOT exist. - `[ ! -f el-templo-api/src/modules/members/debts-service.ts ]` — file does NOT exist. - `[ ! -f el-templo-api/src/db/schema/payments.ts ]` — file does NOT exist. - `[ ! -f el-templo-api/src/db/schema/debts.ts ]` — file does NOT exist. - `[ ! -f el-templo-api/test/payments/payments.test.ts ]` — file does NOT exist. - `[ ! -f el-templo-api/test/members/debts.test.ts ]` — file does NOT exist. - `grep -rE "from ['\"]._/(payments|debts)['\"]|from ['\"]._/payments/|from ['\"]._/debts-service" el-templo-api/src | wc -l`returns`0`.
    - `grep -c "DebtService\\|PaymentService" el-templo-api/src/\*_/_.ts 2>/dev/null | grep -v ":0$" | wc -l`returns`0`(zero files have non-zero matches).
    -`cd el-templo-api && pnpm tsc --noEmit`exits 0 (FULL typecheck of el-templo-api passes — this is the SPEC §4 gate).
    -`cd el-templo-api && pnpm lint 2>&1 | tail -5` shows no errors related to deleted files.
</acceptance_criteria>
<done>All 6 backend file groups deleted (payments dir, debts-service, 2 schema files, 2 test files, optional admin composable); full el-templo-api typecheck passes; SPEC §4 grep gate verde.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                      | Description                                                                                                                                                                                   |
| --------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Filesystem (deletions) → git history          | Deleted files remain in git history (recoverable). No data destruction beyond what the migration already did in Plan 01.                                                                      |
| Stale clients hitting `/api/admin/payments/*` | After deploy, those routes return 404 (route not registered). Phase 106 adds `/api/admin/transactions/*` as the replacement; Plan 07 frontend cleanup is the corresponding admin-app refresh. |

## STRIDE Threat Register

| Threat ID | Category          | Component                                         | Disposition | Mitigation Plan                                                                                                                                                                                      |
| --------- | ----------------- | ------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| T-105-21  | Denial of Service | Stale admin clients call `/api/admin/payments/*`  | accept      | The admin app is push-deployed; users get the new bundle on next load. Phase 105 SPEC explicitly accepts that stale clients break — D-11 documents the same posture for the form payload.            |
| T-105-22  | Tampering         | Accidental delete of an unrelated file            | mitigate    | Each `rm` is explicit and listed by name. The grep gate (`grep -rE "from .../payments                                                                                                                | debts"`) catches any orphaned import the deletions broke. Typecheck on `pnpm tsc --noEmit` is the final blocker. |
| T-105-23  | Repudiation       | Lost test coverage from deleting payments.test.ts | mitigate    | `test/finance/transaction-service.test.ts` (Plan 02) covers the equivalent service-layer behavior with the new model. The deleted test cannot be re-run because the schema it depends on is dropped. |

</threat_model>

<verification>
- `cd el-templo-api && pnpm tsc --noEmit` exits 0.
- `cd el-templo-api && pnpm lint` exits 0 (no orphan-import warnings).
- `grep -rE "from ['\"].*/(payments|debts)['\"]" el-templo-api/src | wc -l` returns 0.
- `cd el-templo-api && pnpm test 2>&1 | tail -20` shows the test suite running (deleted files are gone from the runner).
</verification>

<success_criteria>

- 6 file/directory deletions performed (or verified-absent).
- app.ts no longer references paymentRoutes.
- Full el-templo-api typecheck passes (SPEC §4 gate).
- Grep gate for `from .../payments|debts` returns 0.
  </success_criteria>

<output>
After completion, create `.planning/phases/105-modelo-de-datos-drop-del-viejo/105-06-SUMMARY.md` documenting:
- Exact list of files deleted (with sizes if interesting).
- Whether `usePaymentsApi.ts` existed and was deleted, or was already absent.
- Any orphan imports that surfaced and how they were resolved.
- Confirmation that `pnpm tsc --noEmit` exits 0.
</output>
