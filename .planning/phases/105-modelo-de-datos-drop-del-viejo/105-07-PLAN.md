---
phase: 105-modelo-de-datos-drop-del-viejo
plan: 07
type: execute
wave: 5
depends_on:
  - "105-06"
files_modified:
  - el-templo-admin/src/components/MemberFormDialog.vue
  - el-templo-admin/src/pages/AlumnosPage.vue
  - el-templo-admin/src/types/member.ts
  - el-templo-admin/src/composables/useMembersApi.ts
autonomous: true
requirements:
  - TXN-04
user_setup: []
must_haves:
  truths:
    - "MemberFormDialog.vue NO contiene la sección 'Deuda' (q-toggle 'Deudor', amount/currency/note bloque condicional)"
    - "MemberFormDialog.vue NO contiene los campos isDebtor/debtAmount/debtCurrency/debtNote en form state, defaults, reset, ni submit payload"
    - "el-templo-admin/src/types/member.ts NO contiene DebtUpsertInput ni el campo debt en UpdateMemberInput"
    - "AlumnosPage.vue filtro 'Solo deudores' (línea 55), banner total deuda (línea 149), columna deuda (línea 538) siguen funcionando con la nueva data flow desde GET /admin/members"
    - 'grep -rE "isDebtor|debtAmount|debtCurrency|debtNote" el-templo-admin/src returns 0 matches'
    - "pnpm typecheck pasa en el-templo-admin"
    - "pnpm lint pasa en el-templo-admin"
  artifacts:
    - path: el-templo-admin/src/components/MemberFormDialog.vue
      provides: "Form member sin sección Deuda"
      contains: "MemberFormDialog"
    - path: el-templo-admin/src/pages/AlumnosPage.vue
      provides: "AlumnosPage con filtro/banner/columna funcionales sobre balances data"
      contains: "AlumnosPage"
    - path: el-templo-admin/src/types/member.ts
      provides: "Types sin DebtUpsertInput / debt field"
      contains: "MemberListItem"
  key_links:
    - from: el-templo-admin/src/components/MemberFormDialog.vue
      to: el-templo-admin/src/composables/useMembersApi.ts
      via: "submit payload sin debt field"
      pattern: "updateMember"
    - from: el-templo-admin/src/pages/AlumnosPage.vue
      to: el-templo-admin/src/composables/useMembersApi.ts
      via: "fetch members con onlyDebtors filter; banner reads totalDebtByCurrency"
      pattern: "totalDebtByCurrency"
---

<objective>
Eliminar quirúrgicamente la sección "Deuda" del `MemberFormDialog.vue` (líneas 420-464) junto con los campos `isDebtor`/`debtAmount`/`debtCurrency`/`debtNote` del form state, defaults, reset, y submit payload (D-11). Asegurar que `AlumnosPage.vue` (líneas 55/149/538) sigue funcionando con la nueva forma del endpoint (per Plan 05 `MemberListItem.debt` field changed/removed). Limpiar tipos viejos (`DebtUpsertInput`, posibly `ActiveDebt`, `DebtCurrency`) de `el-templo-admin/src/types/member.ts`. El composable `useMembersApi.ts` es generic y no debería necesitar cambios (per PATTERNS.md), pero verificar.

Purpose: TXN-04 frontend — el cierre simbólico del modelo viejo. El placeholder de la nota de deuda (la "prueba autoejecutable de que el modelo es insuficiente" — CONTEXT specifics) desaparece visualmente del admin.
Output: 4 archivos modificados; admin app compila + linta limpio; el filtro "Solo deudores" sigue mostrando la lista correcta de members; grep gate de campos viejos verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/105-modelo-de-datos-drop-del-viejo/105-SPEC.md
@.planning/phases/105-modelo-de-datos-drop-del-viejo/105-CONTEXT.md
@.planning/phases/105-modelo-de-datos-drop-del-viejo/105-PATTERNS.md
@.planning/phases/105-modelo-de-datos-drop-del-viejo/105-05-SUMMARY.md
@.planning/phases/105-modelo-de-datos-drop-del-viejo/105-06-SUMMARY.md
@CLAUDE.md
@el-templo-admin/src/components/MemberFormDialog.vue
@el-templo-admin/src/pages/AlumnosPage.vue
@el-templo-admin/src/types/member.ts
@el-templo-admin/src/composables/useMembersApi.ts

<interfaces>
Sección a borrar en MemberFormDialog.vue (L420-464):
```vue
<!-- ─── Deuda (Phase 101) ─────────────────────────────────────── -->
<q-separator class="q-my-md" />
<div class="text-subtitle2 text-weight-bold">Deuda</div>
<q-toggle v-model="form.isDebtor" label="Deudor" color="negative" class="q-mt-sm" />
<div v-if="form.isDebtor" class="q-gutter-sm q-mt-sm">
  <q-input v-model="form.debtAmount" ... />
  <q-select v-model="form.debtCurrency" ... :options="debtCurrencyOptions" />
  <q-input v-model="form.debtNote" type="textarea"
    placeholder="Aclarar de qué suscripción es la deuda..." />
</div>
```

Form state fields to remove (in `<script setup>`): `form.isDebtor`, `form.debtAmount`, `form.debtCurrency`, `form.debtNote`. Plus the `debtCurrencyOptions` constant and any `import { DEBT_CURRENCY_OPTIONS, type DebtCurrency } from '...'`.

Submit payload: in the function that calls `useMembersApi().updateMember(...)`, remove the `debt` field from the body.

types/member.ts (L60-200): per PATTERNS.md

- Remove `DebtUpsertInput`, `ActiveDebt`, `DebtCurrency` types
- `UpdateMemberInput`: remove `debt?: DebtUpsertInput` field
- `MemberListItem`: per Plan 05 SUMMARY — depends on whether the field was renamed or removed in the backend response. If backend removed the per-row `debt` field, REMOVE `debt` from MemberListItem. If renamed to `outstandingBalance`, RENAME accordingly.

AlumnosPage.vue per PATTERNS.md: "no structural changes — toggle, banner, and column stay. The data flowing in changes shape only at the type level (debt source = balances)." Verify column at L538/L545 and banner at L149 still bind to whatever field the backend now sends. If the backend renamed `debt` → `outstandingBalance` (per Plan 05 SUMMARY), update bindings here.

useMembersApi.ts: per PATTERNS.md "generic over MembersListResponse — no changes needed". Verify by reading the file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete Deuda section + form fields from MemberFormDialog.vue</name>
  <read_first>
    - el-templo-admin/src/components/MemberFormDialog.vue (read full file or at least template L380-L500 + script setup with form state + submit fn)
    - el-templo-admin/src/types/member.ts (current shape of UpdateMemberInput so the submit payload type matches after the edit)
    - el-templo-admin/src/composables/useMembersApi.ts (full file — check if updateMember body has explicit type or accepts loose object)
    - .planning/phases/105-modelo-de-datos-drop-del-viejo/105-CONTEXT.md D-11
    - .planning/phases/105-modelo-de-datos-drop-del-viejo/105-PATTERNS.md "MemberFormDialog.vue (L420-464)"
  </read_first>
  <action>
**Template edits (the `<template>` block):**
- Locate the Deuda section: `<!-- ─── Deuda` comment marker around L420. DELETE from that comment through the closing `</div>` of the conditional block (~L464). This removes:
  - The `<q-separator>` immediately preceding the Deuda heading.
  - The `<div class="text-subtitle2 text-weight-bold">Deuda</div>` heading.
  - The `<q-toggle v-model="form.isDebtor" ...>` toggle.
  - The `<div v-if="form.isDebtor" ...>` conditional block containing the amount input, currency select, and note textarea.

If the L420-L464 range is approximate (the file may have shifted), use the marker comment `<!-- ─── Deuda` as the anchor and `<q-separator class="q-my-md" />` BEFORE the next non-debt section as the closing anchor. Verify by checking that NO references to `isDebtor`, `debtAmount`, `debtCurrency`, `debtNote`, or `debtCurrencyOptions` remain in the template.

**`<script setup>` edits:**

- Locate the reactive form ref (e.g. `const form = ref<...>({ ... })` or `reactive({ ... })`). REMOVE the keys: `isDebtor`, `debtAmount`, `debtCurrency`, `debtNote`. If there are TypeScript types annotating the form, update them to drop these fields.
- Locate the form-reset / form-default logic (often a `defaultForm()` factory function or a `resetForm()` callback). REMOVE the same 4 keys from the defaults.
- Locate the watcher / props handler that initializes the form from a `props.member` (if editing an existing member). REMOVE the assignments of those 4 fields. If there's an explicit assertion `form.isDebtor = props.member.isDebtor ?? false`, delete it.
- Locate the submit function (e.g. `submit()` or `onSubmit()`). It calls `useMembersApi().updateMember(...)` with a body. REMOVE the `debt` field (or `isDebtor: form.isDebtor` etc.) from the body. The body should now contain only legitimate member fields.
- REMOVE the `debtCurrencyOptions` constant declaration if present.
- REMOVE `import { DEBT_CURRENCY_OPTIONS, type DebtCurrency } from '...'` if present.
- REMOVE any `import { type ActiveDebt } from '...'` if present.

**Logging:** if the file uses `createLogger()` from `src/utils/logger.ts`, preserve those calls. NEVER use `console.log/warn/error` (CLAUDE.md).

After the edits, the form should still have all NON-debt fields working (name, email, phone, branch, plan, status, etc.). Only the debt UI is gone.
</action>
<verify>
<automated>grep -cE "isDebtor|debtAmount|debtCurrency|debtNote|debtCurrencyOptions|DEBT_CURRENCY_OPTIONS" el-templo-admin/src/components/MemberFormDialog.vue</automated>
</verify>
<acceptance_criteria> - `grep -c "Deuda" el-templo-admin/src/components/MemberFormDialog.vue` returns `0` (the heading and section comment both gone). Note: if "Deuda" appears in unrelated copy (e.g. a tooltip), keep that match — but the heading must be gone. - `grep -cE "isDebtor|debtAmount|debtCurrency|debtNote" el-templo-admin/src/components/MemberFormDialog.vue` returns `0`. - `grep -c "debtCurrencyOptions\\|DEBT_CURRENCY_OPTIONS" el-templo-admin/src/components/MemberFormDialog.vue` returns `0`. - `grep -c "ActiveDebt\\|DebtUpsertInput\\|DebtCurrency" el-templo-admin/src/components/MemberFormDialog.vue` returns `0`. - `grep -c "console\\." el-templo-admin/src/components/MemberFormDialog.vue` returns `0`. - `cd el-templo-admin && pnpm tsc --noEmit 2>&1 | grep "MemberFormDialog.vue" | wc -l` returns `0` (verify via vue-tsc).
</acceptance_criteria>
<done>MemberFormDialog.vue has NO Deuda section, NO debt fields in form state/defaults/reset/submit, NO debt-related imports.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update types/member.ts + verify AlumnosPage + useMembersApi</name>
  <read_first>
    - el-templo-admin/src/types/member.ts (full file)
    - el-templo-admin/src/pages/AlumnosPage.vue (read L40-L70 for filter, L140-L170 for banner, L520-L560 for column)
    - el-templo-admin/src/composables/useMembersApi.ts (full file)
    - .planning/phases/105-modelo-de-datos-drop-del-viejo/105-05-SUMMARY.md (CRITICAL: states whether MemberListItem.debt was REMOVED or RENAMED — backend behavior dictates frontend choice)
    - .planning/phases/105-modelo-de-datos-drop-del-viejo/105-PATTERNS.md "AlumnosPage.vue" + "types/member.ts"
  </read_first>
  <action>
**File 1: `el-templo-admin/src/types/member.ts`**

- DELETE the `DebtUpsertInput` interface (around L165-191 area per PATTERNS.md).
- DELETE the `ActiveDebt` interface.
- DELETE the `DebtCurrency` type alias.
- DELETE the `debt?: DebtUpsertInput` field from `UpdateMemberInput` (around L144).
- For `MemberListItem.debt` (around L70): consult `105-05-SUMMARY.md` for the backend decision. Two cases:
  - **Case A (debt field REMOVED in backend):** delete the `debt` field from `MemberListItem`.
  - **Case B (renamed to `outstandingBalance` in backend):** rename the field accordingly and update the type to whatever `BalanceRow` shape the backend now returns (likely `Array<{ currency: string; amount: number }> | null` or just a number).
- Verify any other `Debt*` types or related constants and remove if dead code.

**File 2: `el-templo-admin/src/pages/AlumnosPage.vue`**

- Line ~55 (filter toggle): the toggle's label and v-model probably reference `onlyDebtors` or similar. If the prop name or the API query param changed in Plan 05 (read 105-05-SUMMARY.md), update accordingly. If the contract preserved the same query param (e.g. `?debtorOnly=true`), no change.
- Line ~149 (banner): the banner reads from a list/aggregate (likely `members.totalDebtByCurrency` or similar — verify). Per Plan 05 the response field is preserved as `TotalDebtRow[]`. Verify the binding still resolves; no rewrite needed unless the field name changed.
- Line ~538 (column): the column likely binds to `row.debt` or `row.debt?.amount`. Per Case A above, if `debt` was removed from `MemberListItem`, this column either:
  - Goes away (delete the column block in QTable's columns definition + the format callback).
  - Or shows a placeholder ("—" or similar) since per-row detail is gone (per Plan 05 PATTERNS.md "AlumnosPage's column at L538 just shows aggregate from the banner").

  Per CONTEXT D-10, "Funcionalidad equivalente al admin, sin downtime de UX" — the user expects a working column. If we DELETE the column entirely, the table loses a piece of UX. Recommendation: KEEP the column but bind it to a flag like `row.hasOutstandingBalance: boolean` (a server-side projection added in Plan 05) — but Plan 05 did NOT add this field. So the simplest path: DELETE the column. Document this in 105-07-SUMMARY.md so Phase 108 can add the per-row "saldo actual" detail back.

  **Action:** delete the `debt` column from the QTable `columns` array AND remove any `format` callback referencing `row.debt`. Adjust the `colspan` of any banner row if the column count changes.

**File 3: `el-templo-admin/src/composables/useMembersApi.ts`**

- Read the file. Per PATTERNS.md it's "generic over MembersListResponse — no changes needed". Confirm: the composable's TypeScript types reference `MemberListItem` from `types/member.ts`; since that type just shed a field, the composable adapts automatically. NO code changes needed.
- HOWEVER: if the composable has an explicit `updateMember` method that types its body as `UpdateMemberInput`, that type was just shrunk. Verify no callers (other than MemberFormDialog) pass legacy fields. If there are stale callers, fix them inline.
- If the composable has a `usePaymentsApi` analog (which Plan 06 deleted), confirm there's no broken import here.

After all 3 files, run `cd el-templo-admin && pnpm tsc --noEmit && pnpm lint`. Both must pass.
</action>
<verify>
<automated>cd el-templo-admin &amp;&amp; grep -rE "DebtUpsertInput|ActiveDebt|DebtCurrency|isDebtor|debtAmount|debtCurrency|debtNote" src 2&gt;/dev/null | wc -l; pnpm tsc --noEmit 2&gt;&amp;1 | tail -10</automated>
</verify>
<acceptance_criteria> - `grep -c "DebtUpsertInput\\|ActiveDebt" el-templo-admin/src/types/member.ts` returns `0`. - `grep -c "DebtCurrency" el-templo-admin/src/types/member.ts` returns `0`. - `grep -cE "debt\\?:|debt:\\s*DebtUpsertInput" el-templo-admin/src/types/member.ts` returns `0`. - `grep -rE "isDebtor|debtAmount|debtCurrency|debtNote" el-templo-admin/src` returns 0 matches (SPEC §5 acceptance criterion). - `grep -rE "DebtUpsertInput|ActiveDebt|DebtCurrency" el-templo-admin/src` returns 0 matches. - `grep -c "console\\." el-templo-admin/src/pages/AlumnosPage.vue el-templo-admin/src/components/MemberFormDialog.vue el-templo-admin/src/types/member.ts el-templo-admin/src/composables/useMembersApi.ts` returns `0`. - `cd el-templo-admin && pnpm tsc --noEmit` exits 0. - `cd el-templo-admin && pnpm lint` exits 0.
</acceptance_criteria>
<done>types/member.ts purged of Debt types; AlumnosPage.vue updated to match new MemberListItem shape (column deleted or rebound); admin app typechecks + lints clean; SPEC §5 grep gate verde.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                                                                                                           |
| --------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| MemberFormDialog → PATCH /members/:id   | After this plan, the body emitted by the form contains zero debt-related fields. The backend Zod `.strict()` from Plan 05 backs this with HTTP 400 on legacy clients. |
| AlumnosPage filter → GET /admin/members | The filter sends the same query param shape; the backend now resolves it against `balances`. No client-side change.                                                   |

## STRIDE Threat Register

| Threat ID | Category               | Component                                    | Disposition | Mitigation Plan                                                                                                                                                                                  |
| --------- | ---------------------- | -------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-105-24  | Tampering              | Stale form bundle on user's browser          | accept      | Admin app is push-deployed; Vite hot-reload + service worker refresh ensures users get the new bundle on next load. Backend `.strict()` rejects legacy payloads in the meantime (D-11).          |
| T-105-25  | Information Disclosure | Per-row debt detail removed from AlumnosPage | accept      | Per Plan 05 SUMMARY decision (Case A: delete the per-row field). Banner aggregate still shows total debt. Phase 108 adds the dedicated per-member saldo detail. Documented in 105-07-SUMMARY.md. |
| T-105-26  | Tampering              | XSS in any new template binding              | mitigate    | No new template bindings are added in this plan — only deletions and (possibly) renaming. Vue's interpolation is safe by default for the unchanged bindings.                                     |

</threat_model>

<verification>
- `cd el-templo-admin && pnpm tsc --noEmit` exits 0.
- `cd el-templo-admin && pnpm lint` exits 0.
- `grep -rE "isDebtor|debtAmount|debtCurrency|debtNote" el-templo-admin/src | wc -l` returns 0.
- `grep -rE "DebtUpsertInput|ActiveDebt|DebtCurrency" el-templo-admin/src | wc -l` returns 0.
- Manual smoke (Plan 08 covers): open MemberFormDialog → no Deuda section visible. Toggle "Solo deudores" on AlumnosPage → only members with `balances.amount > 0` show.
</verification>

<success_criteria>

- MemberFormDialog.vue: Deuda section + form fields + imports gone.
- types/member.ts: Debt types deleted; UpdateMemberInput cleaned; MemberListItem updated per backend response shape.
- AlumnosPage.vue: column rebound or deleted; filter and banner still functional.
- useMembersApi.ts: verified clean (likely no changes).
- Admin app typechecks + lints.
- SPEC §5 grep gate (no isDebtor/debtAmount/debtCurrency/debtNote in admin src) is green.
  </success_criteria>

<output>
After completion, create `.planning/phases/105-modelo-de-datos-drop-del-viejo/105-07-SUMMARY.md` documenting:
- Whether the per-row debt column in AlumnosPage was deleted or rebound (Phase 108 needs to know to bring it back).
- Whether useMembersApi.ts needed any changes.
- Any leftover styling/copy quirks (e.g. an extra `<q-separator>` to clean up).
</output>
