---
phase: 105-modelo-de-datos-drop-del-viejo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/schema/financial-transactions.ts
  - el-templo-api/src/db/schema/transaction-links.ts
  - el-templo-api/src/db/schema/balances.ts
  - el-templo-api/src/db/schema/index.ts
  - el-templo-api/src/db/migrations/0106_finance_model_replace_payments_debts.sql
autonomous: true
requirements:
  - TXN-01
  - TXN-02
  - TXN-03
  - TXN-04
user_setup: []
must_haves:
  truths:
    - "Tablas financial_transactions, transaction_links, balances existen en MySQL local con todos los campos del SPEC"
    - "Tablas payments y debts NO existen en MySQL local después de aplicar la migration"
    - "pnpm typecheck verde en el-templo-api con los nuevos schema files"
    - "El runner de migrations registra 0106 en _migrations"
  artifacts:
    - path: el-templo-api/src/db/schema/financial-transactions.ts
      provides: "Drizzle table definition financialTransactions + relations + enums"
      contains: "export const financialTransactions = mysqlTable"
    - path: el-templo-api/src/db/schema/transaction-links.ts
      provides: "Drizzle pivot table transactionLinks con UNIQUE composite"
      contains: "export const transactionLinks = mysqlTable"
    - path: el-templo-api/src/db/schema/balances.ts
      provides: "Cache table balances con UNIQUE(member_id, target_kind, target_id, currency)"
      contains: "export const balances = mysqlTable"
    - path: el-templo-api/src/db/migrations/0106_finance_model_replace_payments_debts.sql
      provides: "Single migration: CREATE 3 nuevas → DROP payments → DROP debts"
      contains: "CREATE TABLE financial_transactions"
  key_links:
    - from: el-templo-api/src/db/schema/index.ts
      to: el-templo-api/src/db/schema/financial-transactions.ts
      via: "export * from './financial-transactions'"
      pattern: "export \\* from \\\"./financial-transactions\\\""
    - from: el-templo-api/src/db/schema/index.ts
      to: el-templo-api/src/db/schema/balances.ts
      via: "export * from './balances'"
      pattern: "export \\* from \\\"./balances\\\""
---

<objective>
Crear los 3 Drizzle schema files (`financial-transactions.ts`, `transaction-links.ts`, `balances.ts`), actualizar el barrel `schema/index.ts` (quitar payments + debts, agregar 3 nuevas), escribir la migration manual `0106_finance_model_replace_payments_debts.sql` con CREATE de las 3 nuevas tablas seguido de DROP de payments y debts en ese orden exacto, y aplicarla con el custom migration runner. Esta es la base sobre la que se montan todos los waves siguientes.

Purpose: Schema + migration es la única dependency hard del resto del phase. El [BLOCKING] db:migrate corre acá para que cuando Wave 2 ejecute integration tests las tablas ya existan.
Output: 3 schema files nuevos, index.ts actualizado, archivo SQL de migration aplicado en MySQL local.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/105-modelo-de-datos-drop-del-viejo/105-SPEC.md
@.planning/phases/105-modelo-de-datos-drop-del-viejo/105-CONTEXT.md
@.planning/phases/105-modelo-de-datos-drop-del-viejo/105-PATTERNS.md
@CLAUDE.md
@el-templo-api/src/db/schema/payments.ts
@el-templo-api/src/db/schema/debts.ts
@el-templo-api/src/db/schema/aura-transactions.ts
@el-templo-api/src/db/schema/aura-balances.ts
@el-templo-api/src/db/schema/format-compatibility.ts
@el-templo-api/src/db/schema/index.ts
@el-templo-api/src/db/migrations/0096_debts_table.sql
@el-templo-api/src/db/migrations/0070_rename_programs_merge_goal_plans.sql

<interfaces>
<!-- Anchor patterns for schema/migration. Executor uses these directly — no codebase exploration required. -->

From el-templo-api/src/db/schema/payments.ts (analog for financial_transactions — soft-void columns + relations shape):

```typescript
voidedAt: timestamp("voided_at"),
voidedBy: int("voided_by").references(() => users.id),
voidReason: text("void_reason"),
createdAt: timestamp("created_at").defaultNow().notNull(),
// indices
index("idx_payments_member_id").on(table.memberId),
index("idx_payments_subscription_id").on(table.subscriptionId),
```

From el-templo-api/src/db/schema/aura-balances.ts (analog for balances — cache mirrored from a ledger):

```typescript
export const auraBalances = mysqlTable("aura_balances", {
  id: int("id").primaryKey().autoincrement(),
  userId: int("user_id")
    .references(() => users.id)
    .notNull()
    .unique(),
  balance: int("balance").default(0).notNull(),
  updatedAt: timestamp("updated_at").defaultNow().onUpdateNow().notNull(),
});
```

From el-templo-api/src/db/schema/format-compatibility.ts (analog for transaction_links — small pivot with composite uniqueIndex):

```typescript
import { mysqlTable, int, mysqlEnum, uniqueIndex } from 'drizzle-orm/mysql-core';
// ...
}, (table) => [
  uniqueIndex('format_compat_lookup_idx').on(table.formatId, table.block, table.level, table.intensity),
]);
```

Existing schema barrel lines to remove (el-templo-api/src/db/schema/index.ts L36-37):

```typescript
export * from "./payments";
export * from "./debts";
```

Migration runner contract (el-templo-api/src/db/run-migrations.ts):

- Picks up files matching `el-templo-api/src/db/migrations/*.sql` in lexicographic order.
- Splits SQL on `;` BEFORE stripping `--` comments → no inline `;` allowed inside `--` comments.
- Tracks applied filenames in MySQL table `_migrations`.
- Invocation: `cd el-templo-api && pnpm db:migrate`. Never `drizzle-kit migrate` or `drizzle-kit push`.
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create 3 Drizzle schema files + update barrel</name>
  <read_first>
    - el-templo-api/src/db/schema/payments.ts (full file — analog for financial-transactions.ts)
    - el-templo-api/src/db/schema/aura-balances.ts (full file — analog for balances.ts cache shape)
    - el-templo-api/src/db/schema/format-compatibility.ts (full file — pivot uniqueIndex pattern)
    - el-templo-api/src/db/schema/aura-transactions.ts (relations + mysqlEnum patterns)
    - el-templo-api/src/db/schema/index.ts (barrel to update)
    - el-templo-api/src/db/schema/branches.ts (FK target — confirm export name `branches`)
    - el-templo-api/src/db/schema/users.ts (FK target — confirm export name `users`)
    - .planning/phases/105-modelo-de-datos-drop-del-viejo/105-SPEC.md §1, §2, §3
    - .planning/phases/105-modelo-de-datos-drop-del-viejo/105-PATTERNS.md sections "financial-transactions.ts", "transaction-links.ts", "balances.ts"
    - .planning/phases/105-modelo-de-datos-drop-del-viejo/105-CONTEXT.md D-05, D-06, D-07, D-08
  </read_first>
  <action>
Create three new files in `el-templo-api/src/db/schema/` following the analog patterns. Use `mysqlEnum` declarations inline per D-05 (no separate enum exports if not needed downstream). Drizzle column names use snake_case in the SQL identifier; TypeScript field names use camelCase.

**File 1: `el-templo-api/src/db/schema/financial-transactions.ts`** — analog: `payments.ts`. Required columns per SPEC §1:

- `id: int("id").primaryKey().autoincrement()`
- `memberId: int("member_id").references(() => users.id).notNull()`
- `kind: mysqlEnum("kind", ["plan_charge", "debt_settlement", "refund", "adjustment", "advance_payment"]).notNull()`
- `direction: mysqlEnum("direction", ["inflow", "outflow"]).notNull()`
- `amount: int("amount").notNull()`
- `currency: varchar("currency", { length: 3 }).default("ARS").notNull()`
- `paymentMethod: mysqlEnum("payment_method", ["cash", "transfer", "card", "aura_credit", "internal"]).notNull()`
- `transactionDate: date("transaction_date", { mode: "string" }).notNull()`
- `effectiveDate: date("effective_date", { mode: "string" }).notNull()`
- `branchId: int("branch_id").references(() => branches.id).notNull()` (NOT NULL per SPEC; online uses virtual "Templo Online" branch — D-Migration Constraints in CONTEXT)
- `recordedBy: int("recorded_by").references(() => users.id).notNull()`
- `voidedAt: timestamp("voided_at")`
- `voidedBy: int("voided_by").references(() => users.id)` (nullable)
- `voidReason: text("void_reason")` (nullable)
- `notes: text("notes")` (nullable)
- `createdAt: timestamp("created_at").defaultNow().notNull()`
- `updatedAt: timestamp("updated_at").defaultNow().onUpdateNow().notNull()`

Indices:

- `index("idx_financial_tx_member_id").on(table.memberId)`
- `index("idx_financial_tx_transaction_date").on(table.transactionDate)`
- `index("idx_financial_tx_branch_date").on(table.branchId, table.transactionDate)`
- `index("idx_financial_tx_kind_voided").on(table.kind, table.voidedAt)`

Relations export `financialTransactionsRelations` with: `member` (users), `recorder` (users), `voider` (users), `branch` (branches), `links: many(transactionLinks)`. Use distinct `relationName` strings per relation (e.g. `"financialTxMember"`, `"financialTxRecorder"`, `"financialTxVoider"`) to avoid Drizzle ambiguity (mirrors `paymentsRelations` shape).

Import `transactionLinks` from `./transaction-links` for the relation. Header comment: `// Module: finance — phase 105`.

**File 2: `el-templo-api/src/db/schema/transaction-links.ts`** — analog: `format-compatibility.ts` (pivot) + `aura-transactions.ts:36-49` (discriminated target). Required columns per SPEC §2:

- `id: int("id").primaryKey().autoincrement()`
- `transactionId: int("transaction_id").references(() => financialTransactions.id).notNull()`
- `targetKind: mysqlEnum("target_kind", ["subscription", "debt_balance", "transaction"]).notNull()`
- `targetId: int("target_id").notNull()` — **no `.references(...)`** (heterogeneous FK target by `target_kind`; service layer enforces integrity per SPEC §7).
- `allocatedAmount: int("allocated_amount").notNull()`
- `createdAt: timestamp("created_at").defaultNow().notNull()`

Indices (passed as second arg to `mysqlTable`):

- `uniqueIndex("uniq_tx_target").on(table.transactionId, table.targetKind, table.targetId)` — SPEC §2 UNIQUE constraint.
- `index("idx_tx_links_target").on(table.targetKind, table.targetId)` — for "qué transacciones afectan al concepto X" lookups.

Export `transactionLinksRelations` with `transaction: one(financialTransactions, { fields: [transactionLinks.transactionId], references: [financialTransactions.id] })`.

**File 3: `el-templo-api/src/db/schema/balances.ts`** — analog: `aura-balances.ts`. Required columns per SPEC §3:

- `id: int("id").primaryKey().autoincrement()`
- `memberId: int("member_id").references(() => users.id).notNull()`
- `targetKind: mysqlEnum("target_kind", ["subscription", "debt_balance"]).notNull()` (NOTE: smaller enum than transaction_links — no `transaction` value)
- `targetId: int("target_id").notNull()`
- `currency: varchar("currency", { length: 3 }).notNull()` — no default per D-08; caller chooses.
- `amount: int("amount").notNull()` — signed; D-08 explicitly allows negatives (saldo a favor).
- `lastRecomputedAt: timestamp("last_recomputed_at").defaultNow().onUpdateNow().notNull()`
- `createdAt: timestamp("created_at").defaultNow().notNull()`

Indices:

- `uniqueIndex("uniq_balance_target").on(table.memberId, table.targetKind, table.targetId, table.currency)`
- `index("idx_balances_member").on(table.memberId)`
- `index("idx_balances_amount_member").on(table.amount, table.memberId)` — backs "Solo deudores" lookup.

Export `balancesRelations` with `member: one(users, { fields: [balances.memberId], references: [users.id] })`.

**File 4: update `el-templo-api/src/db/schema/index.ts`** — at lines 36-37, REMOVE:

```typescript
export * from "./payments";
export * from "./debts";
```

ADD in their place (preserving alphabetical ordering near siblings):

```typescript
export * from "./financial-transactions";
export * from "./transaction-links";
export * from "./balances";
```

DO NOT delete the schema files `payments.ts` / `debts.ts` in this task — they get deleted in Wave 4 / Plan 06 once nothing imports from them anymore. After this barrel edit `pnpm typecheck` will fail on transitive importers; that is expected and gets resolved as later waves swap callers. **Verify behavior:** run `cd el-templo-api && pnpm tsc --noEmit 2>&1 | head -5` to confirm the barrel itself parses (the errors will be from downstream callers, not from this file).
</action>
<verify>
<automated>cd el-templo-api &amp;&amp; node -e "const s=require('./src/db/schema'); console.log(typeof s.financialTransactions, typeof s.transactionLinks, typeof s.balances); console.log('payments?', typeof s.payments)"</automated>
<manual>Inspect the 3 new files exist and `index.ts` no longer references payments/debts</manual>
</verify>
<acceptance_criteria> - File `el-templo-api/src/db/schema/financial-transactions.ts` exists. - `grep -c "export const financialTransactions = mysqlTable" el-templo-api/src/db/schema/financial-transactions.ts` returns `1`. - `grep -c "export const financialTransactionsRelations" el-templo-api/src/db/schema/financial-transactions.ts` returns `1`. - `grep -E "branchId.*notNull\\(\\)" el-templo-api/src/db/schema/financial-transactions.ts` returns ≥1 match (branch_id is NOT NULL). - `grep -c "voidedAt\|voidedBy\|voidReason" el-templo-api/src/db/schema/financial-transactions.ts` returns ≥3. - File `el-templo-api/src/db/schema/transaction-links.ts` exists with `uniqueIndex("uniq_tx_target")` AND `index("idx_tx_links_target")`. - `grep "references(() => financialTransactions" el-templo-api/src/db/schema/transaction-links.ts` returns ≥1 match (transactionId FK). - `grep -E "targetId: int\\(\"target_id\"\\)\\.notNull" el-templo-api/src/db/schema/transaction-links.ts` returns 1 match AND that line does NOT contain `references(`. - File `el-templo-api/src/db/schema/balances.ts` exists with `uniqueIndex("uniq_balance_target")` and `index("idx_balances_amount_member")`. - `grep -c "export \\* from \"./payments\"" el-templo-api/src/db/schema/index.ts` returns `0`. - `grep -c "export \\* from \"./debts\"" el-templo-api/src/db/schema/index.ts` returns `0`. - `grep -c "export \\* from \"./financial-transactions\"" el-templo-api/src/db/schema/index.ts` returns `1`. - `grep -c "export \\* from \"./transaction-links\"" el-templo-api/src/db/schema/index.ts` returns `1`. - `grep -c "export \\* from \"./balances\"" el-templo-api/src/db/schema/index.ts` returns `1`.
</acceptance_criteria>
<done>3 schema files committed with all SPEC §1-§3 columns and indices; barrel updated; payments.ts and debts.ts schema files NOT yet deleted (handled in Plan 06).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write manual migration SQL 0106</name>
  <read_first>
    - el-templo-api/src/db/migrations/0096_debts_table.sql (full file — CREATE TABLE shape with FKs and indices)
    - el-templo-api/src/db/migrations/0070_rename_programs_merge_goal_plans.sql (full file — DROP TABLE shape)
    - el-templo-api/src/db/migrations/0100_user_status_enum.sql (precedent for hand-written migration with comments — see STATE.md "SQL comments must not contain inline `;`")
    - el-templo-api/src/db/run-migrations.ts (read first 60 lines — confirm split-on-`;` behavior + _migrations table contract)
    - .planning/phases/105-modelo-de-datos-drop-del-viejo/105-SPEC.md §1-§4 + Constraints
    - .planning/phases/105-modelo-de-datos-drop-del-viejo/105-CONTEXT.md "Migration Constraints"
    - el-templo-api/src/db/schema/financial-transactions.ts (just created — keep field names + types in lockstep)
  </read_first>
  <action>
Create file `el-templo-api/src/db/migrations/0106_finance_model_replace_payments_debts.sql`. **Order of statements is critical** (CONTEXT D-Migration Constraints): CREATE the 3 new tables FIRST, then DROP the 2 old ones. MySQL does not support transactional DDL for CREATE/DROP TABLE — the order is the only protection against partial-failure data loss.

**Header comment block** (use only `--` line comments; NEVER use inline `;` inside a `--` comment per Plan 103-01 precedent in STATE.md, because the runner splits on `;` before stripping comments):

```sql
-- Phase 105: Finance model replace payments and debts with transactional 3-table model
-- Created 2026-04-28
-- CREATE order financial_transactions then transaction_links then balances then DROP payments then DROP debts
-- No backfill per SPEC payments and debts are dropped without data migration
-- Manual not drizzle-kit generate per CLAUDE.md and CONTEXT D-Migration Constraints
```

**Statement 1: `CREATE TABLE financial_transactions`** — must mirror the Drizzle schema 1:1 in column names, types, defaults, nullability:

- `id INT NOT NULL AUTO_INCREMENT`
- `member_id INT NOT NULL`
- `kind ENUM('plan_charge','debt_settlement','refund','adjustment','advance_payment') NOT NULL`
- `direction ENUM('inflow','outflow') NOT NULL`
- `amount INT NOT NULL`
- `currency VARCHAR(3) NOT NULL DEFAULT 'ARS'`
- `payment_method ENUM('cash','transfer','card','aura_credit','internal') NOT NULL`
- `transaction_date DATE NOT NULL`
- `effective_date DATE NOT NULL`
- `branch_id INT NOT NULL`
- `recorded_by INT NOT NULL`
- `voided_at TIMESTAMP NULL`
- `voided_by INT NULL`
- `void_reason TEXT NULL`
- `notes TEXT NULL`
- `created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP`
- `updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
- `PRIMARY KEY (id)`
- `CONSTRAINT fk_financial_tx_member FOREIGN KEY (member_id) REFERENCES users(id)`
- `CONSTRAINT fk_financial_tx_branch FOREIGN KEY (branch_id) REFERENCES branches(id)`
- `CONSTRAINT fk_financial_tx_recorder FOREIGN KEY (recorded_by) REFERENCES users(id)`
- `CONSTRAINT fk_financial_tx_voider FOREIGN KEY (voided_by) REFERENCES users(id)`
- `INDEX idx_financial_tx_member_id (member_id)`
- `INDEX idx_financial_tx_transaction_date (transaction_date)`
- `INDEX idx_financial_tx_branch_date (branch_id, transaction_date)`
- `INDEX idx_financial_tx_kind_voided (kind, voided_at)`

Add `ENGINE=InnoDB DEFAULT CHARSET=utf8mb4` if 0096 uses it (match precedent).

**Statement 2: `CREATE TABLE transaction_links`** — mirror `transaction-links.ts`:

- `id INT NOT NULL AUTO_INCREMENT`
- `transaction_id INT NOT NULL`
- `target_kind ENUM('subscription','debt_balance','transaction') NOT NULL`
- `target_id INT NOT NULL`
- `allocated_amount INT NOT NULL`
- `created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP`
- `PRIMARY KEY (id)`
- `CONSTRAINT fk_tx_links_transaction FOREIGN KEY (transaction_id) REFERENCES financial_transactions(id)`
- `UNIQUE KEY uniq_tx_target (transaction_id, target_kind, target_id)`
- `INDEX idx_tx_links_target (target_kind, target_id)`

**No FK on `target_id`** — heterogeneous target enforced at service layer per SPEC §7.

**Statement 3: `CREATE TABLE balances`** — mirror `balances.ts`:

- `id INT NOT NULL AUTO_INCREMENT`
- `member_id INT NOT NULL`
- `target_kind ENUM('subscription','debt_balance') NOT NULL`
- `target_id INT NOT NULL`
- `currency VARCHAR(3) NOT NULL`
- `amount INT NOT NULL`
- `last_recomputed_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
- `created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP`
- `PRIMARY KEY (id)`
- `CONSTRAINT fk_balances_member FOREIGN KEY (member_id) REFERENCES users(id)`
- `UNIQUE KEY uniq_balance_target (member_id, target_kind, target_id, currency)`
- `INDEX idx_balances_member (member_id)`
- `INDEX idx_balances_amount_member (amount, member_id)`

**Statement 4 + 5: DROP**

```sql
DROP TABLE `payments`;
DROP TABLE `debts`;
```

Each statement ends with `;` on its own line. No inline `;` inside any `--` comment.
</action>
<verify>
<automated>grep -E "^CREATE TABLE financial_transactions|^CREATE TABLE transaction_links|^CREATE TABLE balances|^DROP TABLE \`payments\`|^DROP TABLE \`debts\`" el-templo-api/src/db/migrations/0106_finance_model_replace_payments_debts.sql | wc -l</automated>
</verify>
<acceptance_criteria> - File `el-templo-api/src/db/migrations/0106_finance_model_replace_payments_debts.sql` exists. - `grep -c "^CREATE TABLE financial_transactions" el-templo-api/src/db/migrations/0106_finance_model_replace_payments_debts.sql` returns `1`. - `grep -c "^CREATE TABLE transaction_links" el-templo-api/src/db/migrations/0106_finance_model_replace_payments_debts.sql` returns `1`. - `grep -c "^CREATE TABLE balances" el-templo-api/src/db/migrations/0106_finance_model_replace_payments_debts.sql` returns `1`. - `grep -c "^DROP TABLE \`payments\`" el-templo-api/src/db/migrations/0106_finance_model_replace_payments_debts.sql`returns`1`.
    - `grep -c "^DROP TABLE \`debts\`" el-templo-api/src/db/migrations/0106_finance_model_replace_payments_debts.sql`returns`1`.
    - The line number of `^CREATE TABLE financial_transactions`is LESS THAN the line number of`^DROP TABLE \`payments\``(verify with`grep -n`).
    - `grep -nE "^\\s*--.*;" el-templo-api/src/db/migrations/0106_finance_model_replace_payments_debts.sql`returns 0 matches (no`;`inside`--`comments).
    -`grep -c "ENUM('plan_charge','debt_settlement','refund','adjustment','advance_payment')" el-templo-api/src/db/migrations/0106_finance_model_replace_payments_debts.sql`returns`1`.
    - `grep -c "UNIQUE KEY uniq_tx_target" el-templo-api/src/db/migrations/0106_finance_model_replace_payments_debts.sql`returns`1`.
    - `grep -c "UNIQUE KEY uniq_balance_target" el-templo-api/src/db/migrations/0106_finance_model_replace_payments_debts.sql`returns`1`.
</acceptance_criteria>
<done>SQL file committed; columns + enums + indices + FKs match the Drizzle schema 1:1; CREATE precedes DROP; comments are safe for the splitter.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: [BLOCKING] Apply migration 0106 to local MySQL</name>
  <read_first>
    - el-templo-api/src/db/run-migrations.ts (full file — confirm pnpm db:migrate runs this script and tracks via _migrations table)
    - CLAUDE.md "Database Changes" section (the runner is source of truth, never drizzle-kit migrate)
    - .planning/phases/105-modelo-de-datos-drop-del-viejo/105-SPEC.md "Acceptance Criteria" (first three checkboxes)
  </read_first>
  <action>
[BLOCKING] Apply the migration locally so all subsequent waves run against the new schema. **NEVER use `drizzle-kit migrate` or `drizzle-kit push`** (CLAUDE.md is explicit on this).

Run: `cd el-templo-api && pnpm db:migrate`

After it completes, verify in MySQL:

1. `_migrations` table contains a row with filename `0106_finance_model_replace_payments_debts.sql`.
2. `SHOW TABLES` lists `financial_transactions`, `transaction_links`, `balances`.
3. `SHOW TABLES` does NOT list `payments` or `debts`.

If the migration fails partway, the order (CREATE first, DROP last) ensures `payments` + `debts` still exist. Investigate the error, fix the SQL file, re-run. Do NOT mark this task done unless all 3 verifications pass.

**This task is the gate for Wave 2.** Plan 02 integration tests cannot run until this completes successfully.
</action>
<verify>
<automated>cd el-templo-api &amp;&amp; mysql -u root eltemplo -N -e "SELECT COUNT(_) FROM \_migrations WHERE filename = '0106_finance_model_replace_payments_debts.sql'" 2&gt;/dev/null | tr -d ' '</automated>
<manual>Run `cd el-templo-api &amp;&amp; mysql -u root eltemplo -e "SHOW TABLES LIKE 'payments'; SHOW TABLES LIKE 'debts'; SHOW TABLES LIKE 'financial_transactions';"` and confirm only the third returns a row.</manual>
</verify>
<acceptance_criteria> - `cd el-templo-api && pnpm db:migrate` exits 0. - `mysql -u root eltemplo -N -e "SELECT COUNT(_) FROM \_migrations WHERE filename = '0106_finance_model_replace_payments_debts.sql'"`returns`1`.
    - `mysql -u root eltemplo -N -e "SHOW TABLES LIKE 'financial_transactions'"`returns 1 row.
    -`mysql -u root eltemplo -N -e "SHOW TABLES LIKE 'transaction_links'"`returns 1 row.
    -`mysql -u root eltemplo -N -e "SHOW TABLES LIKE 'balances'"`returns 1 row.
    -`mysql -u root eltemplo -N -e "SHOW TABLES LIKE 'payments'"`returns 0 rows.
    -`mysql -u root eltemplo -N -e "SHOW TABLES LIKE 'debts'"`returns 0 rows.
    - Same checks pass against`eltemplo_test`database (so the integration test suite in Plan 02 can run):`mysql -u root eltemplo_test -N -e "SHOW TABLES LIKE 'financial_transactions'"`returns 1 row. (If`pnpm db:migrate`only targets eltemplo, run it again with`DATABASE_URL`pointing at`eltemplo_test`or follow the project's test-DB migration script.)
  </acceptance_criteria>
  <done>Local MySQL`eltemplo`AND`eltemplo_test` reflect the new schema; runner has tracked the migration; subsequent plans can read/write the new tables.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                 | Description                                                                                                                 |
| ------------------------ | --------------------------------------------------------------------------------------------------------------------------- |
| migration runner → MySQL | DDL is irreversible (DROP TABLE); the runner is the only authorized executor.                                               |
| schema barrel → app code | A typo in `index.ts` exports leaks `undefined` to importers (caught by typecheck downstream, but worth a manual grep here). |

## STRIDE Threat Register

| Threat ID | Category                  | Component                                                  | Disposition                     | Mitigation Plan                                                                                                                                                                                                                                     |
| --------- | ------------------------- | ---------------------------------------------------------- | ------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-105-01  | Tampering / Repudiation   | Drop migration (irreversible)                              | accept                          | No code-level rollback path. Recovery posture: restore from latest MySQL backup. CONTEXT and SPEC explicitly accept this — "no backfill, no migración de datos históricos". CREATE-before-DROP order limits blast radius if migration fails midway. |
| T-105-02  | Information Disclosure    | New `financial_transactions` table                         | mitigate                        | All `*_by` and `member_id` columns are FKs to `users(id)`. No PII directly stored in this table — names/emails come via JOIN gated by existing routes RBAC (Phase 106 enforces). 105 adds no endpoints.                                             |
| T-105-03  | Denial of Service         | Missing index on hot lookup paths                          | mitigate                        | Indices defined per SPEC §1-§3 cover the known query shapes (`(branch_id, transaction_date)`, `(kind, voided_at)`, `(amount, member_id)` for "Solo deudores").                                                                                      |
| T-105-04  | Tampering                 | Service-layer-enforced FK on `transaction_links.target_id` | mitigate (deferred enforcement) | Heterogeneous target_id has no DB-level FK. Service layer in Plan 02 validates existence before insert (SPEC §7). Documented + tested in Plan 02 integration tests.                                                                                 |
| T-105-05  | Tampering / SQL Injection | Manual migration SQL                                       | mitigate                        | File is fully literal SQL — no string interpolation, no user input. Reviewed by hand.                                                                                                                                                               |

</threat_model>

<verification>
- `pnpm db:migrate` completes successfully against `eltemplo` and `eltemplo_test`.
- `SHOW CREATE TABLE financial_transactions` matches the Drizzle schema field-by-field.
- `pnpm typecheck` in `el-templo-api` will FAIL because Wave 3+ callers still import `paymentService`/`DebtService` — that is expected and resolved by later plans. The barrel itself parses without errors.
</verification>

<success_criteria>

- 3 schema files exist with all required columns, FKs, indices.
- Schema barrel updated to remove payments+debts and add 3 new exports.
- Migration SQL file 0106 exists with CREATE-then-DROP ordering and no inline `;` in comments.
- Local MySQL has the new tables; payments + debts dropped; runner tracked the migration.
- Test DB `eltemplo_test` also migrated (so Plan 02 tests can run).
  </success_criteria>

<output>
After completion, create `.planning/phases/105-modelo-de-datos-drop-del-viejo/105-01-SUMMARY.md` documenting:
- New schema files created and key columns/indices.
- Migration filename + applied state.
- Any deviations from PATTERNS.md (e.g., field name choices).
- Pre-existing typecheck errors that Wave 3+ resolves (so the next executor knows what to expect).
</output>
