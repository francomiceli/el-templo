---
phase: 44-app-landing-page-app
plan: 4
title: "Forms + WhatsApp + Cross-Site Integration + SEO + Analytics"
type: execute
wave: 2
depends_on: ["44-01", "44-02", "44-03"]
autonomous: false
requirements:
  - APP-19
  - APP-20
  - APP-21
  - APP-22
  - APP-23
  - APP-24
  - APP-26
  - APP-27
files_modified:
  - el-templo-web/components/AppLandingFormWaitlist.vue
  - el-templo-web/components/AppLandingFormLabs.vue
  - el-templo-web/components/AppLandingWhatsApp.vue
  - el-templo-web/components/SectionConversion.vue
  - el-templo-web/components/AppNav.vue
  - el-templo-web/components/AppFooter.vue
  - el-templo-web/pages/app.vue
must_haves:
  truths:
    - "Form A submits to POST /api/app/waitlist and shows confirmation with WhatsApp link"
    - "Form B submits to POST /api/app/labs-inquiry and shows confirmation with WhatsApp link"
    - "Floating WhatsApp button visible on /app"
    - "SectionConversion app CTA links to /app instead of app.eltemplo.org"
    - "AppNav includes App/Templo Online link"
    - "AppFooter Templo Online link is enabled"
    - "/app has correct SEO meta tags (title, description, OG)"
    - "GA4 events fire on download CTA click and both form submissions"
  artifacts:
    - path: "el-templo-web/components/AppLandingFormWaitlist.vue"
      provides: "4-field notification waitlist form"
    - path: "el-templo-web/components/AppLandingFormLabs.vue"
      provides: "8-field Labs external gym inquiry form"
    - path: "el-templo-web/components/AppLandingWhatsApp.vue"
      provides: "Floating WhatsApp button for /app"
    - path: "el-templo-web/pages/app.vue"
      provides: "Complete page with all sections, SEO, analytics"
  key_links:
    - from: "el-templo-web/components/AppLandingFormWaitlist.vue"
      to: "POST /api/app/waitlist"
      via: "$fetch API call on submit"
    - from: "el-templo-web/components/AppLandingFormLabs.vue"
      to: "POST /api/app/labs-inquiry"
      via: "$fetch API call on submit"
    - from: "el-templo-web/components/SectionConversion.vue"
      to: "/app"
      via: "ctaHref changed from app.eltemplo.org"
---

# Plan 44-04: Forms + WhatsApp + Cross-Site Integration + SEO + Analytics

<objective>
Complete the /app page: build both forms with API integration, add floating WhatsApp, wire all sections into app.vue, update cross-site links (SectionConversion, AppNav, AppFooter), and add SEO meta tags + analytics events.

Purpose: Final assembly. Forms are the conversion endpoints. Cross-site integration ensures /app is discoverable from all other pages.
Output: Fully functional /app page with working forms, analytics, and site-wide link integration.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/44-app-landing-page-app/44-CONTEXT.md
@.docs/brand-landing/spec12-pt1.md
@.docs/brand-landing/spec12-pt2.md

Reference patterns (read these for exact implementation details):

- el-templo-web/components/FranForm.vue (form pattern: reactive state, validation, $fetch, loading/submitted states, WhatsApp confirmation)
- el-templo-web/components/FranWhatsApp.vue (floating WhatsApp button pattern)
- el-templo-web/components/AppNav.vue (nav links array)
- el-templo-web/components/AppFooter.vue (footer ecosystem links with disabled flag)
- el-templo-web/components/SectionConversion.vue (app CTA href to update)
- el-templo-web/composables/useAnalytics.ts (trackEvent, trackLead)
- el-templo-web/pages/franquicias.vue (SEO meta pattern with useSeoMeta)

Dependencies (from earlier plans in this phase):

- 44-01-SUMMARY.md (API endpoints: POST /api/app/waitlist, POST /api/app/labs-inquiry)
- 44-02-SUMMARY.md (data file, page shell, hero/ecosystem/module components)
- 44-03-SUMMARY.md (academy/labs/flywheel/download components)
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Build Form A (waitlist) + Form B (Labs) + WhatsApp button</name>
  <files>
    el-templo-web/components/AppLandingFormWaitlist.vue,
    el-templo-web/components/AppLandingFormLabs.vue,
    el-templo-web/components/AppLandingWhatsApp.vue
  </files>
  <action>
  **Component: `el-templo-web/components/AppLandingFormWaitlist.vue`**

BEM prefix: `app-landing-form`. Background: Warm Stone #D9CFC1 (shared with Labs form in same section). id="formulario-waitlist".

Follow FranForm.vue pattern exactly for reactive state, validation, $fetch, loading/submitted states.

H3: "¿Queres saber cuando se activan?" — Montserrat SemiBold.
Subtitle: "Dejanos tus datos y te avisamos cuando Olympic Academy y Labs esten disponibles online."

4 fields:

1. Nombre — text input, required, maxLength 255
2. Email — email input, required, format validation
3. Que modulo te interesa? — Multi-select checkboxes (NOT native select). Options: "Olympic Academy", "Labs", "Ambos". Store as comma-separated string: "olympic-academy", "labs", "olympic-academy,labs". Required (at least one checked).
4. Ciudad / Pais — text input, optional, maxLength 255

CTA: "AVISAME CUANDO ESTE DISPONIBLE" (btn--primary Terracotta).

Submit: `$fetch` POST to `/api/app/waitlist` with `{ nombre, email, moduloInteres, ciudadPais }`. Use runtime config for API base URL (same pattern as FranForm).

On success: Show confirmation message with WhatsApp link (same wa.link/ci8dpl URL). Track GA4 event: `trackEvent("form_submit_app_waitlist")`.

Inline validation: Show error messages below each field on blur. Clear on focus.

Loading state: Disable submit button, show spinner text.
Error state: Show banner message if API call fails.

**Component: `el-templo-web/components/AppLandingFormLabs.vue`**

BEM prefix: `app-landing-form`. Same section background. id="formulario-labs" (anchor target for Labs dual CTA).

H3: "QUIERO LABS PARA MI GIMNASIO" — Montserrat SemiBold.
Subtitle: "Contanos sobre tu gimnasio y te contactamos."

8 fields:

1. Nombre — text, required
2. Email — email, required, format validation
3. Telefono / WhatsApp — tel, required
4. Nombre del gimnasio — text, required
5. Ciudad / Pais — text, required
6. Cantidad de socios — native select, required. Options: "Menos de 50", "50-200", "200-500", "Mas de 500". Values: "menos-de-50", "50-200", "200-500", "mas-de-500".
7. Que sistema usas actualmente? — native select, required. Options: "Ninguno", "Excel", "Fitco", "Crosshero", "Otro". Values: "ninguno", "excel", "fitco", "crosshero", "otro".
8. Mensaje — textarea, optional, maxLength 500

CTA: "QUIERO LABS PARA MI GIMNASIO" (btn--secondary-azul, Azul Noche).

Submit: `$fetch` POST to `/api/app/labs-inquiry`. On success: confirmation + WhatsApp link. Track: `trackEvent("form_submit_labs_inquiry")` + `trackLead()` (Meta Pixel).

Both forms use native select elements for mobile UX (consistent with FranForm pattern per Phase 34 decisions). Both forms side-by-side on desktop (50/50), stacked on mobile (waitlist first).

**Component: `el-templo-web/components/AppLandingWhatsApp.vue`**

Clone FranWhatsApp.vue exactly. Change only:

- BEM prefix: `app-landing-whatsapp` (not `fran-whatsapp`)
- Analytics event: `trackEvent("click_whatsapp_app")` (not franchise)
- Same URL: `https://wa.link/ci8dpl`
- Same animation, same styling, same reduced motion handling
  </action>
  <verify>
  <automated>cd /home/franco/projects/el-templo/el-templo-web && npx nuxi typecheck 2>&1 | tail -20</automated>
  </verify>
  <done>Form A has 4 fields with multi-select checkboxes for modules, submits to /api/app/waitlist. Form B has 8 fields with native selects, submits to /api/app/labs-inquiry. Both show confirmation + WhatsApp on success. WhatsApp floating button renders with correct BEM prefix and analytics event.</done>
  </task>

<task type="auto">
  <name>Task 2: Wire app.vue (all sections) + cross-site updates + SEO + analytics</name>
  <files>
    el-templo-web/pages/app.vue,
    el-templo-web/components/SectionConversion.vue,
    el-templo-web/components/AppNav.vue,
    el-templo-web/components/AppFooter.vue
  </files>
  <action>
  **Complete page: `el-templo-web/pages/app.vue`**

Wire all 10 section components + WhatsApp in template order matching spec flow:

```vue
<template>
  <div class="app-landing">
    <AppLandingHero />
    <AppLandingEcosystem />
    <AppLandingModuleArete />
    <AppLandingModuleTemplo />
    <AppLandingModuleAcademy />
    <AppLandingModuleLabs />
    <AppLandingFlywheel />
    <AppLandingDownload />
    <div class="app-landing__forms" id="formularios">
      <div class="section-container">
        <h2 class="app-landing__forms-title">CONTACTO</h2>
        <div class="app-landing__forms-grid">
          <AppLandingFormWaitlist />
          <AppLandingFormLabs />
        </div>
      </div>
    </div>
    <AppLandingWhatsApp />
  </div>
</template>
```

Forms section wrapper: Background Warm Stone #D9CFC1. Forms grid: 2-column on desktop (50/50 with gap), stacked on mobile.

**SEO meta tags** (in script setup):

```typescript
useSeoMeta({
  title:
    "El Templo Online — App de Entrenamiento, H&aacute;bitos y Gesti&oacute;n",
  description:
    "Ecosistema digital de El Templo Calistenia. 4 m&oacute;dulos: Aret&ecirc; (h&aacute;bitos), El Templo (entrenamiento), Olympic Academy (formaci&oacute;n) y Labs (gesti&oacute;n de gimnasios).",
  ogTitle: "El Templo Online — El Templo en tu Bolsillo",
  ogDescription:
    "Una plataforma, cuatro m&oacute;dulos. H&aacute;bitos, entrenamiento, formaci&oacute;n y gesti&oacute;n.",
  ogUrl: "https://eltemplo.org/app",
  ogType: "website",
});

useHead({
  link: [{ rel: "canonical", href: "https://eltemplo.org/app" }],
});
```

Add scoped CSS for `.app-landing__forms` wrapper and grid layout.

**Cross-site update: `el-templo-web/components/SectionConversion.vue`**

Change the app conversion path CTA href from `"https://app.eltemplo.org"` to `"/app"`. This is the critical funnel update per CONTEXT.md: home -> /app -> store (instead of home -> app.eltemplo.org directly).

Also update the `target` attribute: remove `target="_blank"` and `rel="noopener noreferrer"` since it's now an internal link. Change the `<a>` to a `<NuxtLink>` for the app path only (presencial stays as external `<a>` to WhatsApp).

**Cross-site update: `el-templo-web/components/AppNav.vue`**

Add "App" or "Templo Online" to the navLinks array. Insert after "Blog" (6th position), before the CTA:

```typescript
{ label: "App", href: "/app", sectionId: null },
```

Use "App" (short) rather than "Templo Online" to prevent nav overflow on narrow desktop. This is the 7th non-CTA link.

Check if narrow desktop nav spacing still works with 7 links (Phase 30 has a narrow breakpoint rule reducing gap/font). If 7 links overflow, consider hiding "App" label in mobile drawer only or using "Templo" as the short label.

**Cross-site update: `el-templo-web/components/AppFooter.vue`**

Change the "Templo Online" ecosystem link from `disabled: true` to `disabled: false`:

Current: `{ label: "Templo Online", href: "/app", disabled: true }`
Change to: `{ label: "Templo Online", href: "/app", disabled: false }`

This enables the footer link that was previously stubbed as coming-soon.
</action>
<verify>
<automated>cd /home/franco/projects/el-templo/el-templo-web && npx nuxi typecheck 2>&1 | tail -20</automated>
</verify>
<done>app.vue wires all 10 sections + WhatsApp in correct order. SEO meta tags render for /app. SectionConversion app CTA links to /app (not app.eltemplo.org). AppNav includes App link. AppFooter Templo Online link is enabled. GA4 events configured for download CTA, both form submissions. Meta Pixel trackLead fires on Labs form submit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual and functional verification of complete /app page</name>
  <action>Human verifies the complete /app page visually and functionally.</action>
  <what-built>Complete /app landing page with 10 sections, 2 working forms, floating WhatsApp, cross-site integration (AppNav, AppFooter, SectionConversion), and SEO meta tags</what-built>
  <how-to-verify>
    1. Start dev server: `cd el-templo-web && pnpm dev`
    2. Navigate to http://localhost:9200/app
    3. Verify hero: H1 "EL TEMPLO EN TU BOLSILLO.", store badges, dual CTAs
    4. Click "CONOCE LOS MODULOS" — should scroll to ecosystem section
    5. Verify ecosystem: 4 module cards with progressive unlock flow
    6. Verify Arete and El Templo sections: full opacity, ACTIVO badges, features, properties
    7. Verify Academy: 0.6 opacity, Aged Gold PROXIMAMENTE badge, /academy link
    8. Verify Labs: 0.6 opacity, Azul Noche PROXIMAMENTE badge, dual CTA (franquicias + formulario)
    9. Click "VER FRANQUICIAS" in Labs — should navigate to /franquicias
    10. Click "QUIERO LABS PARA MI GIMNASIO" in Labs — should scroll to form B
    11. Verify Flywheel: vertical flow with 4 stages and connecting arrows on dark background
    12. Verify Download: store badges, phone mockup placeholder, DESCARGA LA APP CTA
    13. Fill and submit Form A (waitlist) — confirm validation + submission works
    14. Fill and submit Form B (Labs) — confirm validation + submission works
    15. Check floating WhatsApp button (bottom-right)
    16. Navigate to home page: verify SectionConversion app CTA links to /app
    17. Verify AppNav includes "App" link
    18. Verify AppFooter "Templo Online" link is clickable (not disabled)
    19. Resize to mobile: verify all sections stack properly, CTAs are full-width
    20. Check page source: verify SEO meta tags (title, description, OG)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `cd el-templo-web && npx nuxi typecheck` — no TypeScript errors
2. Form A submits to /api/app/waitlist and shows confirmation
3. Form B submits to /api/app/labs-inquiry and shows confirmation
4. Floating WhatsApp renders correctly with entrance animation
5. SectionConversion app CTA links to /app (internal, not external)
6. AppNav shows "App" link
7. AppFooter "Templo Online" link is enabled
8. Page has correct SEO meta tags
9. DESCARGA LA APP CTA appears exactly 3 times (hero, post-El Templo, download)
10. No prices visible anywhere
11. "Sesion" used throughout, never "clase"
12. No #000000 or #FFFFFF in CSS
</verification>

<success_criteria>

- Both forms work end-to-end (validation, submit, confirmation, WhatsApp)
- Cross-site links updated: SectionConversion, AppNav, AppFooter
- SEO meta tags present on /app
- GA4 events fire on all conversion points
- Meta Pixel trackLead fires on Labs form submission
- Visual verification confirms all 10 sections render correctly
- Page communicates ONE platform with modules, not 4 separate apps
  </success_criteria>

<output>
After completion, create `.planning/phases/44-app-landing-page-app/44-04-SUMMARY.md`
</output>
