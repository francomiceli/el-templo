---
phase: 44-app-landing-page-app
plan: 3
title: "Academy + Labs Modules + Flywheel Digital + Download Section"
type: execute
wave: 2
depends_on: ["44-02"]
autonomous: true
requirements:
  - APP-15
  - APP-16
  - APP-17
  - APP-18
files_modified:
  - el-templo-web/components/AppLandingModuleAcademy.vue
  - el-templo-web/components/AppLandingModuleLabs.vue
  - el-templo-web/components/AppLandingFlywheel.vue
  - el-templo-web/components/AppLandingDownload.vue
must_haves:
  truths:
    - "Academy section renders at opacity 0.6 with Aged Gold PROXIMAMENTE badge"
    - "Labs section renders at opacity 0.6 with Azul Noche PROXIMAMENTE badge"
    - "Labs section has dual CTA: franchisee link to /franquicias + external gym link to #formulario-labs"
    - "Flywheel shows vertical top-to-bottom flow: Arete -> El Templo -> Academy -> Labs with arrows"
    - "Download section shows store badges + phone mockup placeholder + DESCARGA LA APP CTA"
    - "No module shows prices"
  artifacts:
    - path: "el-templo-web/components/AppLandingModuleAcademy.vue"
      provides: "Olympic Academy module detail section"
    - path: "el-templo-web/components/AppLandingModuleLabs.vue"
      provides: "Labs module detail section with dual CTA"
    - path: "el-templo-web/components/AppLandingFlywheel.vue"
      provides: "Vertical flywheel digital flow"
    - path: "el-templo-web/components/AppLandingDownload.vue"
      provides: "Download section with store badges"
  key_links:
    - from: "el-templo-web/components/AppLandingModuleLabs.vue"
      to: "/franquicias"
      via: "NuxtLink for franchisee CTA"
    - from: "el-templo-web/components/AppLandingModuleLabs.vue"
      to: "#formulario-labs"
      via: "anchor scroll for external gym CTA"
    - from: "el-templo-web/components/AppLandingDownload.vue"
      to: "storeLinks from data file"
      via: "import { storeLinks } from ~/data/app-landing"
---

# Plan 44-03: Academy Module + Labs Module + Flywheel Digital + Download Section

<objective>
Build the remaining 4 content sections of /app: Olympic Academy module detail (proximamente), Labs module detail (proximamente with dual CTA), the Flywheel Digital vertical flow visualization, and the Download section with store badges.

Purpose: Completes the module showcase and conversion sections. Labs dual CTA is critical — different messaging for franchisees vs external gyms.
Output: 4 components ready to be wired into app.vue by Plan 44-04.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/44-app-landing-page-app/44-CONTEXT.md
@.docs/brand-landing/spec12-pt1.md
@.docs/brand-landing/spec12-pt2.md

Reference patterns:

- el-templo-web/data/app-landing.ts (module data — created by Plan 44-02)
- el-templo-web/components/AppLandingModuleArete.vue (active module pattern — created by Plan 44-02)
- el-templo-web/components/FranExpansion.vue (visual section with animations)
- el-templo-web/composables/useScrollReveal.ts (scroll-triggered animation)
- el-templo-web/components/PlaceholderBox.vue (pending asset placeholders)
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Build Academy + Labs module detail sections</name>
  <files>
    el-templo-web/components/AppLandingModuleAcademy.vue,
    el-templo-web/components/AppLandingModuleLabs.vue
  </files>
  <action>
  **Component: `el-templo-web/components/AppLandingModuleAcademy.vue`**

BEM prefix: `app-landing-module`. Background: Warm Stone #D9CFC1. id="modulo-academy".

Same section layout pattern as Arete/El Templo but with PROXIMAMENTE treatment:

- H2: "OLYMPIC ACADEMY" — Montserrat Bold, Aged Gold color.
- Tagline: "Forma entrenadores. Desde la app." — Cormorant Garamond italic.
- Description from data file.
- Feature list: 4 items with Aged Gold bullet/checkmark icons.
- Properties table from data file. Estado shows Aged Gold "PROXIMAMENTE" badge.
- Proximamente treatment: entire section content wrapper at opacity 0.6 (but section background stays full opacity). Badge is fully opaque.
- Note linking to /academy: "Conoce el programa completo en" + NuxtLink to /academy. This note renders at full opacity (outside the dimmed wrapper).
- NO download CTA (module is not active yet).

Desktop: Split layout 55% text / 45% visual (PlaceholderBox). Mobile: stacked.

Import from app-landing.ts `modules[2]`. Conditional rendering based on `module.isActive` flag:

- If active: same treatment as Arete/El Templo (full opacity, accent border, hover)
- If !active: opacity 0.6 wrapper, no hover effects, PROXIMAMENTE badge

Use `useScrollReveal` for entrance.

**Component: `el-templo-web/components/AppLandingModuleLabs.vue`**

BEM prefix: `app-landing-module`. Background: Marble Cream #F2EDE5. id="modulo-labs".

Labs has DUAL AUDIENCE — this is the unique section. Same layout structure but:

- H2: "LABS" — Montserrat Bold, Azul Noche color.
- Tagline: "Gestiona tu gimnasio. Simple." — Cormorant Garamond italic.
- Description from data file.
- Feature list: 6 items (Turnos, Socios, Finanzas, Mensajeria, CRM, Metricas) with Azul Noche icons.
- Properties table from data. Estado shows Azul Noche "PROXIMAMENTE" badge.
- Proximamente treatment: opacity 0.6 on content (same as Academy).

**Dual CTA zone** (renders at full opacity, outside dimmed wrapper):

- Left card: "Para Franquiciados" — "Incluido en tu franquicia" text + ghost CTA "VER FRANQUICIAS" linking to /franquicias via NuxtLink.
- Right card: "Para Gimnasios Externos" — "Quiero Labs para mi gimnasio" text + Azul Noche CTA "QUIERO LABS PARA MI GIMNASIO" scrolling to #formulario-labs.
- Cards display side-by-side on desktop (50/50), stack on mobile.

Positioning statement below features (inside dimmed wrapper): "No compite con enterprise (Mindbody). Nacio de anos de resolver problemas reales: de un garage a una cadena internacional."

Import from app-landing.ts `modules[3]`. Same isActive conditional as Academy.
</action>
<verify>
<automated>cd /home/franco/projects/el-templo/el-templo-web && npx nuxi typecheck 2>&1 | tail -20</automated>
</verify>
<done>Academy renders at 0.6 opacity with Aged Gold badge and /academy link. Labs renders at 0.6 opacity with Azul Noche badge and dual CTA cards (franchisee -> /franquicias, external -> #formulario-labs). No prices shown for any module.</done>
</task>

<task type="auto">
  <name>Task 2: Build Flywheel Digital + Download section</name>
  <files>
    el-templo-web/components/AppLandingFlywheel.vue,
    el-templo-web/components/AppLandingDownload.vue
  </files>
  <action>
  **Component: `el-templo-web/components/AppLandingFlywheel.vue`**

BEM prefix: `app-landing-flywheel`. Background: Deep Charcoal #3D3732. id="flywheel-digital".

**IMPORTANT per CONTEXT.md:** This is a VERTICAL top-to-bottom flow, NOT the circular FlywheelDiagram from /academy. Different visual treatment.

H2: "EL CICLO COMPLETO. EN DIGITAL." — Montserrat Bold, Marble Cream color.
Subtitle: "Cada modulo te lleva al siguiente. Cada paso te acerca mas al dominio completo."

Vertical flow layout:

- 4 stages stacked vertically, centered:
  1. ARETE (alinea) — Terracotta accent circle/box
  2. EL TEMPLO (entrena) — Terracotta accent
  3. OLYMPIC ACADEMY (formate) — Aged Gold accent
  4. LABS (gestiona) — Azul Noche accent
- Between each stage: downward arrow (SVG) or connecting line in Sandy Beige
- Each stage shows: accent-colored circle with module icon + module name (Montserrat SemiBold, Marble Cream) + verb (Geologica, Sandy Beige)
- Scroll-triggered reveal: stages appear one by one with 0.2s stagger delay

Connection text at bottom: "El mismo camino que vivis en nuestras sedes — ahora tiene su version digital." — Cormorant Garamond italic, Sandy Beige color.

Desktop: Stages centered in column, max-width 600px. Arrows ~40px tall.
Mobile: Same vertical layout, slightly smaller stage circles.

Use `useScrollReveal` with staggered animation (inline transition-delay per stage, consistent with Phase 30 drawer link pattern).

Import `flywheelContent` from app-landing.ts.

**Component: `el-templo-web/components/AppLandingDownload.vue`**

BEM prefix: `app-landing-download`. Background: Sandy Beige #E5D9C8. id="descarga".

H2: "EMPEZA AHORA." — Montserrat Bold.
Subtitle: "Descarga El Templo Online y comenza con Arete — gratis."

Layout:

- Desktop: Two columns — left: text + store badges + CTA, right: phone mockup (PlaceholderBox, 280x560, portrait aspect ratio).
- Mobile: Stacked — text + badges on top, mockup below (centered).

Store badges: App Store + Google Play official badge SVGs (or PlaceholderBox at 135x40 each if SVGs not available). Badges horizontal. Minimum 44px touch target.

CTA: "DESCARGA LA APP" (btn--primary, Terracotta) — this is the 3rd and final appearance per spec. Links to storeLinks.appStore.

Additional text: "Disponible en iOS y Android. Empeza con Arete gratis." — Geologica, Olive Stone.

Import `downloadContent` and `storeLinks` from app-landing.ts.

Use `useScrollReveal` for entrance animation.
</action>
<verify>
<automated>cd /home/franco/projects/el-templo/el-templo-web && npx nuxi typecheck 2>&1 | tail -20</automated>
</verify>
<done>Flywheel shows vertical flow with 4 stages, connecting arrows, and staggered scroll animation on Deep Charcoal background. Download section shows store badges, phone mockup placeholder, and 3rd DESCARGA LA APP CTA on Sandy Beige.</done>
</task>

</tasks>

<verification>
1. `cd el-templo-web && npx nuxi typecheck` — no TypeScript errors
2. Academy section renders with Aged Gold badge and 0.6 opacity content
3. Labs section renders with Azul Noche badge and dual CTA cards
4. Labs franchisee CTA links to /franquicias, external CTA scrolls to #formulario-labs
5. Flywheel shows vertical Arete -> El Templo -> Academy -> Labs flow with arrows
6. Flywheel stages animate in with stagger on scroll
7. Download section shows store badges and phone mockup placeholder
8. DESCARGA LA APP appears 3 times total (hero, post-El Templo, download)
9. No prices visible for any module
10. No #000000 or #FFFFFF in CSS
</verification>

<success_criteria>

- Academy and Labs sections render with correct proximamente treatment (opacity 0.6 + badge)
- Labs dual CTA distinguishes franchisee vs external gym paths
- Flywheel Digital visualizes the progressive unlock narrative vertically
- Download section provides the final conversion point with store badges
- All 4 components are ready to be wired into app.vue
- All typography uses brand fonts (Montserrat, Cormorant Garamond, Geologica)
  </success_criteria>

<output>
After completion, create `.planning/phases/44-app-landing-page-app/44-03-SUMMARY.md`
</output>
