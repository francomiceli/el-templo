---
phase: 32-levels-approaches-conversion-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-web/components/SectionLevels.vue
autonomous: true
requirements:
  - NIV-01
  - NIV-02
  - NIV-03
  - NIV-04
  - NIV-05
  - NIV-06
  - NIV-07

must_haves:
  truths:
    - 6 level tabs (Alfa through Olympic) render with Alfa active by default
    - Clicking a tab shows that level's content with a fade-in animation and hides the previous
    - Arrow keys navigate between tabs, wrapping at the ends
    - Each level panel shows split content (text left / PlaceholderBox right on desktop, stacked on mobile with visual first)
    - Each level has a per-level ghost CTA that links to the conversion section
    - On mobile, tabs scroll horizontally with snap alignment and hidden scrollbar
    - A closing CTA "COMENZA TU CAMINO" scrolls to #descubri-nivel
    - Tab underline slides smoothly between tabs (animated, not instant swap)
    - ARIA roles tablist/tab/tabpanel are present and aria-selected updates dynamically
  artifacts:
    - el-templo-web/components/SectionLevels.vue
  key_links:
    - Tab change updates aria-selected on all tabs and shows correct panel
    - Per-level CTA scrolls to #descubri-nivel (conversion section)
    - Section footer CTA scrolls to #descubri-nivel
    - Section uses id="niveles" for nav anchor wiring
---

<objective>
Build the interactive 6-level tab system (Alfa through Olympic) as a self-contained Vue component.

Purpose: Let visitors identify with a training level ("Where am I in this journey?") — the bridge between method understanding and conversion action.
Output: SectionLevels.vue component with full tab interaction, keyboard navigation, ARIA accessibility, responsive behavior, and all spec copy.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-levels-approaches-conversion-sections/32-CONTEXT.md

# Spec source

@.docs/brand-landing/spec4-pt1.md
@.docs/brand-landing/spec4-pt2.md
@.docs/brand-landing/spec4-pt3.md
@.docs/brand-landing/spec4-pt4.md

# Existing patterns to follow

@el-templo-web/components/SectionMethod.vue
@el-templo-web/composables/useScrollReveal.ts
@el-templo-web/assets/css/tokens.css
@el-templo-web/assets/css/buttons.css

<interfaces>
<!-- Existing composable -->
From el-templo-web/composables/useScrollReveal.ts:
```typescript
export function useScrollReveal(options?: {
  threshold?: number;
  rootMargin?: string;
  once?: boolean;
}): {
  revealed: Ref<boolean>;
  elementRef: Ref<HTMLElement | null>;
  cleanup: () => void;
};
```

<!-- Existing buttons -->

From el-templo-web/assets/css/buttons.css:

- .btn.btn--primary — Terracotta solid button
- .btn.btn--ghost — Ghost text link style (Terracotta)

<!-- Existing component -->

From el-templo-web/components/PlaceholderBox.vue:

- Props: label (string), aspectRatio (string, e.g. "4/5")

<!-- CSS tokens available -->

From el-templo-web/assets/css/tokens.css:

- All color, font, spacing, radius, shadow, transition tokens
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Build SectionLevels.vue with interactive tab system and all spec copy</name>
  <files>el-templo-web/components/SectionLevels.vue</files>
  <action>
Create `SectionLevels.vue` following the established BEM + scoped style pattern from SectionMethod.vue.

**Script section:**

- Define `Level` interface: `{ id: string; name: string; number: number; headline: string; description: string; mirror: string }`.
- Create `levels` array with all 6 levels (Alfa, Delta, Sigma, Omega, Spartan, Olympic) using the EXACT spec copy from spec4 (use HTML entities for accented characters like `\u00E1`, `\u00E9`, `\u00ED`, `\u00F3`, `\u00FA`, `\u00F1`):
  - Alfa (Nivel 1): headline "Donde todo empieza.", description as spec, mirror as spec
  - Delta (Nivel 2): headline "El cambio se siente.", description as spec, mirror as spec
  - Sigma (Nivel 3): headline "La fuerza se vuelve lenguaje.", description as spec, mirror as spec
  - Omega (Nivel 4): headline "Donde los l\u00EDmites se reescriben.", description as spec, mirror as spec
  - Spartan (Nivel 5): headline "Tu ejemplo es tu ense\u00F1anza.", description as spec, mirror as spec
  - Olympic (Nivel 6): headline "Aret\u00E9. Excelencia en todo sentido.", description as spec, mirror as spec
- `activeLevel` ref initialized to `'alfa'`.
- `setActiveLevel(id: string)` function that updates activeLevel and triggers animation by toggling an `animating` ref (set true, then false after 10ms via nextTick to retrigger CSS animation).
- `handleKeydown(event: KeyboardEvent)` function for arrow key navigation:
  - ArrowRight: next tab (wraps to first)
  - ArrowLeft: previous tab (wraps to last)
  - Calls setActiveLevel and focuses the new tab button via template ref
- `tabRefs` as a record of template refs for each tab button (use `ref` on each button with `:ref="el => { if (el) tabRefs[level.id] = el as HTMLElement }"`).
- Use `useScrollReveal()` for section entrance animation. Call `cleanup()` in `onBeforeUnmount`.
- Computed `activeData` that returns the current level object from the array.

**Template section:**

- `<section id="niveles" class="levels">` wrapping everything.
- Section header: tag "Progresi\u00F3n", H2 "No ven\u00EDs solo a entrenar. Ven\u00EDs a recorrer un camino.", subtitle "Cada nivel es una fase real de evoluci\u00F3n. No se salta. No se simula. Se entrena, se prueba, se conquista." (use HTML entities).
- Tabs bar: `<div class="levels__tabs" role="tablist">` containing 6 `<button>` elements, each with:
  - `role="tab"`
  - `:aria-selected="activeLevel === level.id ? 'true' : 'false'"`
  - `:tabindex="activeLevel === level.id ? 0 : -1"`
  - `@click="setActiveLevel(level.id)"`
  - `@keydown="handleKeydown($event)"`
  - Class binding: `levels__tab` + `levels__tab--active` when active
  - `:id="'tab-' + level.id"` and `:aria-controls="'panel-' + level.id"`
- Sliding underline indicator: a `<div class="levels__tabs-indicator">` positioned absolutely inside the tabs bar, using a computed style for `left` and `width` based on the active tab's position. Calculate using the index of the active tab and the known tab widths (use a computed or method that reads DOM positions on mount and on resize).
  - Alternative simpler approach: Use CSS transform on the indicator div. Track active tab index. On desktop, indicator is `calc(100% / 6)` wide and `translateX(${activeIndex * 100}%)`. On mobile (where tabs scroll), fall back to per-tab border-bottom (no sliding indicator).
- Tab panel: `<div>` for the active level only (use `v-if` or `v-show` with animation class):
  - `role="tabpanel"`
  - `:id="'panel-' + activeData.id"`
  - `:aria-labelledby="'tab-' + activeData.id"`
  - `:key="activeData.id"` for transition trigger
  - Apply `levels__panel` class + animation class
  - Two columns: `.levels__panel-text` (name, "Nivel X", headline, description, mirror quote, per-level ghost CTA) and `.levels__panel-visual` (PlaceholderBox with label per level, aspect-ratio="4/5").
- Per-level CTA: `<a href="#descubri-nivel" class="levels__level-cta">` with text "\u00BFEste sos vos? Reserv\u00E1 tu sesi\u00F3n" + arrow span. Per CONTEXT.md discretion: link to #descubri-nivel (the conversion section).
- Section footer: `.levels__footer` with text "No importa tu nivel. Lo importante es empezar." + `<a href="#descubri-nivel" class="btn btn--primary levels__footer-cta">Comenz\u00E1 tu camino</a>`.

**Style section (scoped):**
Follow spec4 CSS exactly with these specifications:

- `.levels` background: Warm Stone, padding: space-hero (96px) 0.
- `.levels__container` max-width: 1100px, centered, padding 0 5%.
- Header typography: tag (Montserrat Bold 11px uppercase Terracotta), title (Montserrat ExtraBold 36px uppercase Deep Charcoal), subtitle (Cormorant Garamond italic 20px Charcoal Mist, max-width 700px centered).
- Tabs bar: flex, justify-content center, border-bottom 1px Warm Stone, overflow-x auto, scrollbar hidden. `position: relative` for indicator positioning.
- Tab button: Montserrat 600 13px uppercase, letter-spacing 0.08em, padding 14px 24px, color Olive Stone (inactive) / Deep Charcoal (active), border-bottom 2px transparent, hover bg rgba(192,122,86,0.06), transition base, white-space nowrap, cursor pointer, no border/background defaults. Focus-visible: outline 2px Terracotta.
- Tab active: no per-tab border-bottom (handled by sliding indicator on desktop).
- Sliding indicator: absolute bottom 0, height 2px, bg Terracotta, transition transform 300ms ease + width 300ms ease.
- Panel: grid 2-col 1fr 1fr, gap 48px, bg Marble Cream, border-radius 8px, padding 48px, margin-top 32px, box-shadow 0 4px 20px rgba(61,55,50,0.08). Animation: `levelFadeIn` keyframe (opacity 0 + translateY(8px) -> 1 + 0) 300ms.
- Panel text typography per spec: level-name (Montserrat Bold 28px uppercase), level-number (Geologica 13px Olive Stone), headline (Cormorant Garamond italic 20px), description (Geologica 15px line-height 1.6), mirror (Geologica italic 14px Olive Stone, border-left 2px Terracotta, padding-left 16px).
- Level CTA: inline-flex, gap 8px, Geologica 500 14px uppercase, Terracotta, hover Clay + underline + arrow translateX(4px).
- Visual: PlaceholderBox with aspect-ratio 4:5, max-height 450px, border-radius 8px.
- Footer: text-align center, padding-top 48px, margin-top 48px, border-top 1px rgba(61,55,50,0.1). Text: Montserrat Bold 20px uppercase Deep Charcoal.

**Responsive breakpoints:**

- `@media (max-width: 768px)`: title 28px, subtitle 17px, tabs justify-content flex-start (scroll), tab padding 12px 16px / font-size 12px, panel single column, visual order -1 (goes first), visual aspect-ratio 16/9 max-height 250px, panel padding 32px. Hide sliding indicator, use per-tab border-bottom instead.
  - Add `scroll-snap-type: x mandatory` on tabs, `scroll-snap-align: start` on each tab.
- `@media (max-width: 480px)`: section padding space-large, title 24px, level-name 22px, headline 17px.

**Reduced motion:**

- `@media (prefers-reduced-motion: reduce)`: disable panel animation (opacity 1, transform none), disable indicator transition, disable hover transforms.

**Scroll reveal:**

- Use `useScrollReveal()` on the section element. Apply `.is-visible` class to trigger entrance animation (same pattern as SectionIdentity — fade in the whole container).
  </action>
  <verify>
  <automated>cd /home/franco/projects/el-templo/el-templo-web && npx nuxi typecheck 2>&1 | tail -20</automated>
  </verify>
  <done> - SectionLevels.vue exists and type-checks without errors - Component renders 6 tabs with Alfa active by default - Keyboard arrow navigation cycles through all 6 tabs - ARIA attributes (role="tablist/tab/tabpanel", aria-selected, aria-controls) are correct - Panel content shows split text/visual with fade-in animation on tab change - Per-level ghost CTA links to #descubri-nivel - Footer CTA (primary Terracotta) links to #descubri-nivel - Mobile: tabs scroll horizontally with snap, panel stacks with visual first - Tab underline indicator slides smoothly between tabs on desktop - All 6 levels have exact spec copy (headlines, descriptions, mirror phrases)
  </done>
  </task>

</tasks>

<verification>
- `npx nuxi typecheck` passes with no errors
- Visual: Warm Stone background, Marble Cream panel, all typography per spec
- Interaction: tabs switch panels with fade animation, keyboard nav works, sliding underline animates
- Accessibility: ARIA roles present, focus-visible on tabs, tabindex management
- Responsive: 768px tabs scroll + panel stacks, 480px tighter sizing
- Copy: all 6 levels match spec exactly, no "clase" anywhere
</verification>

<success_criteria>
SectionLevels.vue is a complete, self-contained component implementing all 7 NIV requirements. The 6-tab system is keyboard-navigable, ARIA-accessible, responsive (horizontal scroll on mobile), and uses exact brand copy from spec4. Ready for integration into index.vue in Plan 03.
</success_criteria>

<output>
After completion, create `.planning/phases/32-levels-approaches-conversion-sections/32-01-SUMMARY.md`
</output>
