---
phase: 32-levels-approaches-conversion-sections
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-web/components/SectionApproaches.vue
autonomous: true
requirements:
  - ENF-01
  - ENF-02
  - ENF-03
  - ENF-04
  - ENF-05

must_haves:
  truths:
    - 5 approach cards (Kallos, Sthenos, Motus, Pyros, Dynamis) render with equal heights
    - Desktop shows 5 cards in a single row; tablet shows 3+2 (last 2 centered); mobile shows horizontal scroll with snap
    - Cards have hover elevation (translateY -3px + shadow intensify) on desktop
    - Hidden scrollbar on mobile with gradient fade indicator on right edge
    - No CTAs in this section (informational only)
    - Each card has inline SVG icon, Greek name, translation, separator, description, and closing sentence
  artifacts:
    - el-templo-web/components/SectionApproaches.vue
  key_links:
    - Section uses id="enfoques" for nav anchor wiring
    - Closing sentences use margin-top auto for bottom-alignment across cards
    - Cards use staggered scroll-reveal entrance animation
---

<objective>
Build the 5 training approaches card grid showing how El Templo classifies movement.

Purpose: Communicate philosophical differentiation — El Templo trains capacities, not muscles. This is informational/brand, no conversion CTAs.
Output: SectionApproaches.vue component with responsive 5-card grid, inline SVG icons, hover states, and mobile horizontal scroll.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-levels-approaches-conversion-sections/32-CONTEXT.md

# Spec source

@.docs/brand-landing/spec5-pt1.md
@.docs/brand-landing/spec5-pt2.md
@.docs/brand-landing/spec5-pt3.md

# Existing patterns to follow

@el-templo-web/components/SectionMethod.vue
@el-templo-web/composables/useScrollReveal.ts
@el-templo-web/assets/css/tokens.css
@el-templo-web/assets/css/buttons.css

<interfaces>
<!-- Existing composable -->
From el-templo-web/composables/useScrollReveal.ts:
```typescript
export function useScrollReveal(options?: {
  threshold?: number;
  rootMargin?: string;
  once?: boolean;
}): {
  revealed: Ref<boolean>;
  elementRef: Ref<HTMLElement | null>;
  cleanup: () => void;
};
```

<!-- CSS tokens available -->

From el-templo-web/assets/css/tokens.css:

- --color-marble-cream, --color-warm-stone, --color-warm-linen, --color-terracotta
- --color-deep-charcoal, --color-olive-stone, --color-charcoal-mist
- --font-authority, --font-elegance, --font-clarity
- --space-base, --space-comfortable, --space-spacious, --space-section, --space-large, --space-hero
- --radius-card, --shadow-subtle, --shadow-medium, --transition-base
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Build SectionApproaches.vue with 5 approach cards and responsive grid</name>
  <files>el-templo-web/components/SectionApproaches.vue</files>
  <action>
Create `SectionApproaches.vue` following BEM + scoped style patterns from SectionMethod.vue.

**Script section:**

- Define `Approach` interface: `{ id: string; name: string; translation: string; description: string; sentence: string; iconSvg: string }`.
- Create `approaches` array with all 5 approaches using EXACT spec copy (HTML entities for accented chars). Per CONTEXT.md, icons are hand-crafted inline SVGs following the ROM/SKILLS pattern (stroke-based, ~40x40, Terracotta via currentColor):
  1. **Kall\u00F3s** — "El centro" — core/stability icon: SVG showing a centered diamond or cross-shape representing stability/balance. Design: a vertical line with horizontal bar crossing at center, small circle at center point. Stroke-based, 40x40 viewBox.
  2. **Sthenos** — "La fuerza" — fist/strength icon: SVG showing a raised fist or strong arm. Design: simplified forearm + fist outline, stroke-based.
  3. **Motus** — "El movimiento" — wave/flow icon: SVG showing a flowing wave or dynamic motion. Design: two sinusoidal curves suggesting fluid movement.
  4. **Pyros** — "El fuego" — flame icon: SVG showing a flame. Design: classic teardrop flame shape with inner curve.
  5. **Dynamis** — "El dominio" — target/precision icon: SVG showing a handstand figure or crosshair/target. Design: simplified figure in handstand position (inverted person).

  Each SVG: `<svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 40 40" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">...</svg>`

- Approach descriptions (exact spec copy):
  - Kall\u00F3s: "Todo movimiento empieza en el centro. Kall\u00F3s es estabilidad, control y dominio del core. La base invisible que sostiene cada skill, cada fuerza, cada movimiento. Sin centro, no hay construcci\u00F3n posible." / Sentence: "La fuerza nace del centro."
  - Sthenos: "Empujar, traccionar, sostener. Sthenos es la fuerza en su expresi\u00F3n m\u00E1s pura: carga progresiva con el peso de tu propio cuerpo. No se trata de cu\u00E1nto levant\u00E1s \u2014 se trata de cu\u00E1nto control\u00E1s." / Sentence: "Fuerza real es control absoluto."
  - Motus: "El cuerpo fue dise\u00F1ado para moverse libre. Motus es coordinaci\u00F3n, fluidez y movimiento natural. Patrones que conectan fuerza con gracia, potencia con precisi\u00F3n. Mov\u00E9te como el cuerpo pide." / Sentence: "Mover bien es vivir mejor."
  - Pyros: "Intensidad, resistencia, coraz\u00F3n. Pyros es el trabajo cardiovascular y metab\u00F3lico que forja la resistencia real. No es correr sin sentido \u2014 es entrenar la capacidad del cuerpo de sostener el esfuerzo y recuperarse." / Sentence: "El fuego se entrena, no se improvisa."
  - Dynamis: "Handstand, muscle up, front lever, planche. Dynamis es la dimensi\u00F3n t\u00E9cnica: los skills que definen a la calistenia avanzada. Cada skill es una conquista \u2014 meses de pr\u00E1ctica concentrados en segundos de dominio." / Sentence: "Dominar un movimiento es dominar la paciencia."

- Use `useScrollReveal()` for staggered entrance animation on the grid. Call `cleanup()` in `onBeforeUnmount`.

**Template section:**

- `<section id="enfoques" class="approaches">` wrapping everything.
- Section header: tag "Los 5 enfoques", H2 "Cinco formas de entender el cuerpo.", subtitle "En El Templo no entrenamos m\u00FAsculos. Entrenamos capacidades. Cada enfoque es una dimensi\u00F3n del movimiento humano \u2014 y las cinco trabajan juntas." (HTML entities).
- Cards grid: `<div ref="gridRef" class="approaches__grid" :class="{ 'is-visible': revealed }">` containing 5 cards.
- Each card: `<div class="approaches__card" :style="{ transitionDelay: revealed ? index * 100 + 'ms' : '0ms' }">` with:
  - Icon: `<span class="approaches__icon" v-html="approach.iconSvg" />` (eslint-disable-next-line vue/no-v-html on previous line)
  - Name: `<h3 class="approaches__name">{{ approach.name }}</h3>`
  - Translation: `<span class="approaches__translation">{{ approach.translation }}</span>`
  - Separator: `<div class="approaches__separator"></div>`
  - Description: `<p class="approaches__desc">{{ approach.description }}</p>`
  - Sentence: `<p class="approaches__sentence">{{ approach.sentence }}</p>`
- No CTAs anywhere in this section (per spec: informational only).

**Style section (scoped):**
Follow spec5 CSS exactly:

- `.approaches` background: Marble Cream, padding: space-hero (96px) 0.
- `.approaches__container` max-width: 1200px, centered, padding 0 5%.
- Header: same pattern as levels (tag Montserrat Bold 11px Terracotta, title Montserrat ExtraBold 36px, subtitle Cormorant Garamond italic 20px Charcoal Mist max-width 650px). All centered.
- Grid: `display: grid; grid-template-columns: repeat(5, 1fr); gap: 24px`.
- Card: `display: flex; flex-direction: column; align-items: center; text-align: center; background: var(--color-warm-linen); border: 1px solid var(--color-warm-stone); border-radius: 8px; padding: 32px 24px; min-height: 320px; box-shadow: var(--shadow-subtle); transition: var(--transition-base)`.
- Card entrance animation: opacity 0, translateY(20px), transition opacity 500ms + transform 500ms (same as SectionMethod blocks pattern). `.is-visible .approaches__card` -> opacity 1, translateY(0).
- Card hover: `transform: translateY(-3px); box-shadow: var(--shadow-medium)`. Override for `.is-visible .approaches__card:hover`: translateY(-3px).
- Icon: width 40px height 40px, margin-bottom space-base. `svg` color: Terracotta.
- Name: Montserrat Bold 16px uppercase, letter-spacing 0.08em, Deep Charcoal, margin 0 0 4px 0.
- Translation: Cormorant Garamond italic 14px Olive Stone, margin-bottom 12px, display block.
- Separator: 30px x 2px, Terracotta, margin 0 auto 12px auto.
- Description: Geologica 14px Deep Charcoal, line-height 1.55, text-align center, margin 0.
- Sentence: Geologica italic 13px Charcoal Mist, `margin-top: auto` (pushes to bottom of flex column), padding-top 16px.

**Responsive breakpoints:**

- `@media (max-width: 768px)`: section padding space-large, title 28px, subtitle 17px. Grid: `repeat(3, 1fr)`, gap 20px. Last 2 cards: use grid placement to center them (`.approaches__card:nth-child(4) { grid-column: 1 / 2; }` and `.approaches__card:nth-child(5) { grid-column: 2 / 3; }` — but this needs 3-col grid context. Actually for centering 2 in 3 cols: wrap last row differently. Better approach: use `justify-content: center` on the grid won't work directly. Instead, with 3-col grid the 4th and 5th cards naturally go to row 2. To center them, add a pseudo-technique or just let them occupy cols 1 and 2 of row 2, which is acceptable per spec "3+2 centered". For true centering in 3-col: `@supports` with subgrid or just accept left-aligned 2 in second row (spec says "repeat(3, 1fr) + row 2: repeat(2, 1fr)" which is aspirational; left-aligned is acceptable).
- `@media (max-width: 480px)`: title 24px, subtitle 16px. Grid becomes: `display: flex; overflow-x: auto; scroll-snap-type: x mandatory; gap: 16px; padding-bottom: 16px; scrollbar-width: none; -ms-overflow-style: none`. Hidden scrollbar with `::-webkit-scrollbar { display: none }`. Card: `min-width: 260px; flex-shrink: 0; scroll-snap-align: start`.
- Mobile gradient fade indicator: `approaches__grid::after` pseudo-element per spec: `content: ''; position: sticky; right: 0; min-width: 40px; flex-shrink: 0; background: linear-gradient(to left, var(--color-marble-cream) 0%, rgba(242, 237, 229, 0) 100%); pointer-events: none;` — only in the 480px breakpoint (when grid is flex). Use a modifier class or media query scope.

**Reduced motion:**

- `@media (prefers-reduced-motion: reduce)`: cards opacity 1, transform none, transition none. Hover: no transform, shadow stays subtle.
  </action>
  <verify>
  <automated>cd /home/franco/projects/el-templo/el-templo-web && npx nuxi typecheck 2>&1 | tail -20</automated>
  </verify>
  <done> - SectionApproaches.vue exists and type-checks without errors - 5 approach cards render with inline SVG icons, names, translations, separators, descriptions, closing sentences - Cards have equal min-height (320px) with sentences bottom-aligned via margin-top auto - Desktop: 5 cards in single row; Tablet: 3 + 2 layout; Mobile: horizontal scroll with snap - Scrollbar hidden on mobile with gradient fade indicator - Card hover: translateY(-3px) + shadow intensify on desktop - Staggered scroll-reveal entrance animation (100ms between cards) - No CTAs anywhere in the section - All copy matches spec5 exactly with correct accents
  </done>
  </task>

</tasks>

<verification>
- `npx nuxi typecheck` passes with no errors
- Visual: Marble Cream background, Warm Linen cards, Terracotta separators and icons
- Cards: equal height, sentences bottom-aligned, hover elevation on desktop
- Responsive: 5-col -> 3+2 -> horizontal scroll with snap + gradient fade
- Copy: all 5 approaches match spec exactly, accented names (Kall\u00F3s not Kallos)
- No CTAs in section
</verification>

<success_criteria>
SectionApproaches.vue is a complete, self-contained component implementing all 5 ENF requirements. The 5 approach cards display with correct typography, responsive grid behavior, hover states, and mobile horizontal scroll with hidden scrollbar and gradient fade indicator. Ready for integration into index.vue in Plan 03.
</success_criteria>

<output>
After completion, create `.planning/phases/32-levels-approaches-conversion-sections/32-02-SUMMARY.md`
</output>
