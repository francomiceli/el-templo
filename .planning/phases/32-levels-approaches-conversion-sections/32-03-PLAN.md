---
phase: 32-levels-approaches-conversion-sections
plan: 03
type: execute
wave: 2
depends_on: ["32-01", "32-02"]
files_modified:
  - el-templo-web/components/SectionConversion.vue
  - el-templo-web/pages/index.vue
autonomous: true
requirements:
  - DESC-01
  - DESC-02
  - DESC-03
  - DESC-04
  - DESC-05

must_haves:
  truths:
    - Two conversion cards (Presencial + App) display in 50/50 grid on desktop, stacked on mobile with Presencial first
    - Presencial CTA is Terracotta primary button linking to WhatsApp
    - App CTA is Azul Noche button linking to app.eltemplo.org with a "coming soon native apps" text note below
    - Section anchor is #descubri-nivel and is the target of Hero and Levels CTAs
    - Card hover produces translateY(-2px) + shadow elevation
    - Both cards have inline SVG icons (Terracotta for presencial, Azul Noche for app)
    - Complementary note "Tu primera sesion es sin cargo. La app es gratuita." appears below the cards
    - All 3 new section components replace their stub placeholders in index.vue
  artifacts:
    - el-templo-web/components/SectionConversion.vue
    - el-templo-web/pages/index.vue (updated)
  key_links:
    - index.vue imports and renders SectionLevels, SectionApproaches, SectionConversion in correct order
    - SectionConversion uses id="descubri-nivel" which is the target of SectionHero and SectionLevels CTAs
    - Presencial CTA links to WhatsApp (+54 9 2235 82-0521)
    - App CTA links to app.eltemplo.org
    - Alternating backgrounds maintained: Levels=Warm Stone, Approaches=Marble Cream, Conversion=Warm Stone
---

<objective>
Build the dual-path conversion section and wire all 3 Phase 32 components into the home page.

Purpose: Convert interested visitors with two clear paths (try in person or try the app), then integrate all Phase 32 sections into the live page replacing stub placeholders.
Output: SectionConversion.vue component + updated index.vue with all 3 new sections.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-levels-approaches-conversion-sections/32-CONTEXT.md

# Spec source

@.docs/brand-landing/spec6-pt1.md
@.docs/brand-landing/spec6-pt2.md
@.docs/brand-landing/spec6-pt3.md

# Existing page to modify

@el-templo-web/pages/index.vue

# Existing patterns

@el-templo-web/components/SectionMethod.vue
@el-templo-web/composables/useScrollReveal.ts
@el-templo-web/assets/css/tokens.css
@el-templo-web/assets/css/buttons.css

<interfaces>
<!-- Existing composable -->
From el-templo-web/composables/useScrollReveal.ts:
```typescript
export function useScrollReveal(options?: {
  threshold?: number;
  rootMargin?: string;
  once?: boolean;
}): {
  revealed: Ref<boolean>;
  elementRef: Ref<HTMLElement | null>;
  cleanup: () => void;
};
```

<!-- Existing buttons from buttons.css -->

- .btn.btn--primary — Terracotta solid button (for presencial CTA)
- .btn.btn--secondary-azul — Azul Noche solid button (for app CTA — already exists!)

<!-- CSS tokens -->

From el-templo-web/assets/css/tokens.css:

- --color-warm-stone, --color-marble-cream, --color-sandy-beige, --color-terracotta
- --color-azul-noche, --color-deep-charcoal, --color-charcoal-mist, --color-olive-stone, --color-clay
- --font-authority, --font-elegance, --font-clarity
- All spacing, radius, shadow, transition tokens

<!-- Current index.vue structure (stubs to replace) -->

SectionHero -> SectionIdentity -> SectionMethod -> [#niveles stub] -> [#enfoques stub] -> [#descubri-nivel stub] -> [#sedes stub]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build SectionConversion.vue with dual-path conversion cards</name>
  <files>el-templo-web/components/SectionConversion.vue</files>
  <action>
Create `SectionConversion.vue` following BEM + scoped style patterns.

**Script section:**

- Define `ConversionPath` interface: `{ id: string; title: string; description: string; sentence: string; ctaText: string; ctaHref: string; ctaClass: string; iconSvg: string }`.
- Create `paths` array with 2 entries using EXACT spec copy (HTML entities for accented chars):
  1. **Presencial**: title "Ven\u00ED a sentirlo en persona", description "Reserv\u00E1 tu primera sesi\u00F3n sin cargo en cualquiera de nuestras 8 sedes. Un entrenador te eval\u00FAa, te asigna tu nivel y te muestra c\u00F3mo funciona el m\u00E9todo en vivo. No hay mejor forma de conocer El Templo que entrenando.", sentence "Tu cuerpo lo entiende m\u00E1s r\u00E1pido que tu cabeza.", CTA text "Reserv\u00E1 tu sesi\u00F3n", href "https://wa.me/5492235820521?text=Hola%21%20Quiero%20reservar%20mi%20primera%20sesi%C3%B3n%20de%20prueba", ctaClass "btn btn--primary", icon: temple/people SVG (Terracotta via currentColor).
  2. **App**: title "Prob\u00E1 tu nivel desde donde est\u00E9s", description "Descarg\u00E1 Templo Online y el sistema te gu\u00EDa. Te propone ejercicios por nivel \u2014 si los super\u00E1s, sube la dificultad. Si te cuestan, ah\u00ED est\u00E1 tu punto de partida. En minutos sab\u00E9s d\u00F3nde est\u00E1s y hacia d\u00F3nde vas.", sentence "No necesit\u00E1s llegar sabiendo. Solo necesit\u00E1s empezar.", CTA text "Descarg\u00E1 la app", href "https://app.eltemplo.org", ctaClass "btn btn--secondary-azul", icon: phone/app SVG (Azul Noche via currentColor).

- Design inline SVG icons (48x48, stroke-based, similar approach to Method icons):
  - **Presencial icon (temple/people)**: A simplified temple/building shape with 2-3 small person figures below. Stroke-based, viewBox 0 0 48 48. Could be: triangle roof peak + two columns + 2 stick figures inside.
  - **App icon (phone)**: A smartphone outline with a small app-like element on screen. Stroke-based, viewBox 0 0 48 48. Simple rectangle with rounded corners + inner screen area + small arrow or play symbol.

- Use `useScrollReveal()` for staggered entrance (card left 0ms, card right +150ms). Call `cleanup()` in `onBeforeUnmount`.

**Template section:**

- `<section id="descubri-nivel" class="discover">` — this anchor is the target of Hero CTA and Levels footer CTA.
- Section header: tag "Tu nivel", H2 "\u00BFEn qu\u00E9 punto del camino est\u00E1s?", subtitle "Descubr\u00ED tu nivel. Pod\u00E9s probarlo en nuestra app desde donde est\u00E9s, o ven\u00ED a una sesi\u00F3n y un entrenador te eval\u00FAa en persona." (HTML entities).
- Cards grid: `<div ref="gridRef" class="discover__grid" :class="{ 'is-visible': revealed }">`.
- Render the presencial card FIRST in the HTML (source order). On desktop, CSS grid puts them side by side. On mobile, presencial naturally appears first (source order = visual order).
- Each card: `<div class="discover__card discover__card--{id}" :style="{ transitionDelay: revealed ? index * 150 + 'ms' : '0ms' }">` with:
  - Icon: `<span class="discover__icon" v-html="path.iconSvg" />` (eslint-disable on line above)
  - Title: `<h3 class="discover__card-title">{{ path.title }}</h3>`
  - Description: `<p class="discover__card-desc">{{ path.description }}</p>`
  - Sentence: `<p class="discover__card-sentence">{{ path.sentence }}</p>`
  - CTA: `<a :href="path.ctaHref" :class="path.ctaClass + ' discover__cta'" :target="path.id === 'app' ? '_blank' : undefined" :rel="path.id === 'app' ? 'noopener noreferrer' : undefined">{{ path.ctaText }}</a>`
- Below the app CTA (inside the app card), add a "coming soon" note: `<p class="discover__app-note">Pr\u00F3ximamente tambi\u00E9n en Google Play y App Store.</p>`. Per CONTEXT.md: "NOT a modal, just a text note with greyed-out or subtle store references".
- Complementary note below both cards: `<p class="discover__note">Tu primera sesi\u00F3n es sin cargo. La app es gratuita.</p>`.

**Style section (scoped):**
Follow spec6 CSS exactly:

- `.discover` background: Warm Stone, padding: space-hero (96px) 0.
- `.discover__container` max-width: 1000px, centered, padding 0 5%.
- Header: tag (Montserrat Bold 11px Terracotta), title (Montserrat ExtraBold 36px uppercase), subtitle (Cormorant Garamond italic 20px Charcoal Mist max-width 600px centered).
- Grid: `display: grid; grid-template-columns: 1fr 1fr; gap: 32px`.
- Card: `display: flex; flex-direction: column; background: var(--color-marble-cream); border: 1px solid var(--color-sandy-beige); border-radius: 8px; padding: 40px 32px; box-shadow: 0 4px 20px rgba(61,55,50,0.08); transition: var(--transition-base)`.
- Card entrance animation: opacity 0, translateY(20px), transition opacity 500ms + transform 500ms. `.is-visible .discover__card` -> opacity 1, translateY(0).
- Card hover: `transform: translateY(-2px); box-shadow: 0 6px 24px rgba(61,55,50,0.12)`. Override for `.is-visible .discover__card:hover`: translateY(-2px).
- Icon: width 48px height 48px, margin 0 auto 24px auto, display block. `.discover__card--presencial .discover__icon svg { color: var(--color-terracotta); }` `.discover__card--app .discover__icon svg { color: var(--color-azul-noche); }`.
- Card title: Montserrat Bold 20px uppercase, tracking 0.04em, Deep Charcoal, text-align center, margin 0.
- Card desc: Geologica 15px Deep Charcoal, line-height 1.6, margin-top 20px.
- Card sentence: Geologica italic 14px Charcoal Mist, border-left 2px Terracotta, padding-left 16px, margin-top 20px.
- CTAs: use existing `.btn.btn--primary` and `.btn.btn--secondary-azul` classes. Add `.discover__cta` for shared spacing: `display: block; text-align: center; margin-top: auto; padding-top: 24px` (margin-top auto pushes CTA to bottom of flex column). Width: auto on desktop, full-width on mobile.
- App note: Geologica 13px Olive Stone, text-align center, margin-top 12px, font-style italic.
- Complementary note: Geologica italic 14px Olive Stone, text-align center, margin-top 32px.

**Responsive breakpoints:**

- `@media (max-width: 768px)`: section padding space-large, title 28px, subtitle 17px, grid gap 24px, card padding 32px 24px, card-title 18px.
- `@media (max-width: 480px)`: title 24px, subtitle 16px. Grid: `grid-template-columns: 1fr; gap: 24px`. Presencial card already first in source order (no CSS order needed). CTAs: `width: 100%; max-width: 300px; margin-left: auto; margin-right: auto;`.

**Reduced motion:**

- `@media (prefers-reduced-motion: reduce)`: cards opacity 1, transform none, transition none. Hover: no transform, keep base shadow.
  </action>
  <verify>
  <automated>cd /home/franco/projects/el-templo/el-templo-web && npx nuxi typecheck 2>&1 | tail -20</automated>
  </verify>
  <done> - SectionConversion.vue exists and type-checks - Two cards render: Presencial (Terracotta CTA to WhatsApp) and App (Azul Noche CTA to app.eltemplo.org) - App card has "coming soon" native apps note below CTA - Section uses id="descubri-nivel" anchor - Cards hover with translateY(-2px) + shadow elevation - Complementary note appears below cards - All copy matches spec6 exactly
  </done>
  </task>

<task type="auto">
  <name>Task 2: Wire all 3 sections into index.vue replacing stubs</name>
  <files>el-templo-web/pages/index.vue</files>
  <action>
Update `index.vue` to replace the 3 Phase 32 stub sections with the real components.

**Changes to make:**

1. Remove the `<!-- Niveles anchor stub (Phase 32) -->` section block entirely (lines ~13-24 in current file) and replace with `<SectionLevels />`.
2. Remove the `<!-- Enfoques anchor stub (Phase 32) -->` section block entirely (lines ~26-37) and replace with `<SectionApproaches />`.
3. Remove the `<!-- Descubri tu Nivel anchor stub (Phase 32) -->` section block entirely (lines ~39-50) and replace with `<SectionConversion />`.

**Resulting template order in `<main>`:**

```html
<SectionHero />
<SectionIdentity />
<SectionMethod />
<SectionLevels />
<SectionApproaches />
<SectionConversion />

<!-- Sedes anchor stub (Phase 33) -->
<section id="sedes" class="section">...existing sedes stub stays...</section>
```

**Background alternation verification:**

- SectionHero: Deep Charcoal (video bg)
- SectionIdentity: Marble Cream
- SectionMethod: Warm Stone / Marble Cream / Warm Stone / Marble Cream (4 zones)
- SectionLevels: Warm Stone (correct - follows Method's last zone Marble Cream)
- SectionApproaches: Marble Cream (correct - alternates from Warm Stone)
- SectionConversion: Warm Stone (correct - alternates from Marble Cream)
- Sedes stub: Marble Cream (uses default `.section` class, no --warm-stone modifier — check this is correct and update if needed)

Nuxt auto-imports components from `components/` directory, so no explicit import statements needed.
</action>
<verify>
<automated>cd /home/franco/projects/el-templo/el-templo-web && npx nuxi typecheck 2>&1 | tail -20</automated>
</verify>
<done> - index.vue no longer contains any Phase 32 stub sections - SectionLevels, SectionApproaches, SectionConversion render in correct order - Background alternation is correct (Warm Stone, Marble Cream, Warm Stone) - Sedes stub (Phase 33) still present and correctly positioned after SectionConversion - Page type-checks successfully - Nav anchor links (#niveles, #enfoques) still work because new components have the correct IDs - Hero CTA "COMENZA TU CAMINO" scrolls to #descubri-nivel which now targets SectionConversion
</done>
</task>

</tasks>

<verification>
- `npx nuxi typecheck` passes with no errors
- `cd /home/franco/projects/el-templo/el-templo-web && npx nuxi generate 2>&1 | tail -30` — SSG build succeeds
- Visual: Conversion section on Warm Stone bg with Marble Cream cards, Terracotta + Azul Noche CTAs
- Background alternation: Levels(WarmStone) -> Approaches(MarbleCream) -> Conversion(WarmStone) -> Sedes(MarbleCream)
- Anchor wiring: #niveles -> SectionLevels, #enfoques -> SectionApproaches, #descubri-nivel -> SectionConversion
- WhatsApp link correctly formatted: wa.me/5492235820521 with greeting text
- App link goes to app.eltemplo.org, opens in new tab
- No Phase 32 stubs remain in index.vue
</verification>

<success_criteria>
SectionConversion.vue implements all 5 DESC requirements with dual-path cards, correct CTA colors and destinations, and "coming soon" native app note. index.vue is updated to render all 3 Phase 32 components in the correct order, replacing all stub sections. The full home page flow from Hero through Conversion is complete with proper background alternation and anchor wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/32-levels-approaches-conversion-sections/32-03-SUMMARY.md`
</output>
