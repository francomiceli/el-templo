---
phase: 156-planes-de-pago-vs-rutinas-de-entrenamiento
plan: 03
type: execute
wave: 3
depends_on: ["156-02"]
files_modified:
  - el-templo-api/src/modules/programs/enrollment-service.ts
  - el-templo-api/src/modules/subscriptions/service.ts
  - el-templo-api/test/subscriptions/plan-programs-access.test.ts
autonomous: true
requirements: [PLAN-03]

must_haves:
  truths:
    - "grantsAllPrograms=true → enrola en todos los programas (lista ignorada), como hoy"
    - "grantsAllPrograms=false + lista no vacía → enrola exactamente en los programas de la lista"
    - "grantsAllPrograms=false + lista vacía → no enrola en ninguno (comportamiento actual)"
    - "La lista NUNCA otorga Foundation (goalPlanType IS NULL): mismo filtro anti-piratería que el bundle"
    - "El teardown protege programas cubiertos por otra sub con lista y limpia los de la lista"
    - "Los 4 call sites de enrollFromPlan proyectan la lista y el guard incluye lista no vacía"
  artifacts:
    - path: "el-templo-api/src/modules/programs/enrollment-service.ts"
      provides: "Resolución all → lista → nada + filtro Foundation + teardown"
      contains: "programIds"
    - path: "el-templo-api/test/subscriptions/plan-programs-access.test.ts"
      provides: "Casos multi-programa: all/lista/nada, Foundation, teardown, protección"
  key_links:
    - from: "el-templo-api/src/modules/subscriptions/service.ts"
      to: "enrollFromPlan"
      via: "guard con programIds.length > 0 + proyección de la lista"
      pattern: "enrollFromPlan"
    - from: "el-templo-api/src/modules/programs/enrollment-service.ts"
      to: "program_enrollments"
      via: "insert source plan_bundle con filtro goalPlanType IS NOT NULL"
      pattern: "goalPlanType|plan_bundle"
---

<objective>
Lógica de acceso multi-programa (PLAN-03, D-07). El "acceso" de un socio = filas en `program_enrollments`; la app de miembros consulta enrollments, no el plan → toda la resolución vive en `enrollFromPlan` + `tearDownForSubscription` y la app de miembros funciona sin cambios. Resolver en un criterio único: **all → lista → nada**. La lista HEREDA el filtro anti-piratería del bundle (`goalPlanType IS NOT NULL`) — un plan NO puede otorgar Foundation por lista (fase 104 R7). Los 4 callers proyectan la lista y amplían el guard con `|| programIds.length > 0`. El teardown suma los `plan_programs` de los protectores al protected set.

Purpose: un plan con lista da/quita acceso a los programas seleccionados, igual que hoy con "todos".
Output: resolución all→lista→nada + filtro Foundation + teardown + 4 callers + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-planes-de-pago-vs-rutinas-de-entrenamiento/156-CONTEXT.md
@.planning/phases/156-planes-de-pago-vs-rutinas-de-entrenamiento/156-PATTERNS.md

<interfaces>
<!-- Líneas exactas del PATTERNS — no explorar. -->

programs/enrollment-service.ts:
EnrollFromPlanInput (44-48): { id, linkedProgramId, grantsAllPrograms } → agregar programIds: number[]
enrollFromPlan rama bundle (197-254): if grantsAllPrograms → bulk-enroll programas activos con goalPlanType IS NOT NULL (exclusión Foundation), dedupe contra activos (213-228), insert source "plan_bundle" (231-242)
tearDownForSubscription (492-694): - protected set (523-552): programas cubiertos por OTRA sub activa/pausada (linkedProgramId + anyProtectorIsBundle) → un protector con lista aporta sus plan_programs - legacy fallback (577-613): enrollments subscription_id IS NULL

subscriptions/service.ts — 4 callers de enrollFromPlan, todos guardados por if (plan.linkedProgramId || plan.grantsAllPrograms):
1317-1329 (assignPlan), 2997-3008 (changePlanNow targetPlan), 3722-3743 (activateScheduledSub), 4162-4190 (renew newPlan)
guard anti-doble-bundle (963-995): 409 si ya hay bundle — NO aplica a listas
assertPlanInvariants (228-245): ya cubierto en 156-02

test/subscriptions/bundle-todos-los-programas.test.ts (analog R3/R4: auto-enroll, idempotencia, teardown, protección)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolución all→lista→nada + filtro Foundation + teardown</name>
  <read_first>
    - el-templo-api/src/modules/programs/enrollment-service.ts (44-48 input; 197-254 rama bundle; 492-694 teardown; 523-552 protected set; 577-613 legacy fallback)
    - el-templo-api/src/db/schema/plan-programs.ts (156-02)
  </read_first>
  <action>
    Extender `EnrollFromPlanInput` con `programIds: number[]`. En `enrollFromPlan` reemplazar el `if (plan.grantsAllPrograms)` por la resolución única: all=true → query actual (203-211: isActive + goalPlanType IS NOT NULL); all=false y lista no vacía → mismos filtros restringidos con `inArray(schema.programs.id, input.programIds)` — el filtro `goalPlanType IS NOT NULL` SE MANTIENE en la rama lista (anti-piratería 104 R7: la lista no puede otorgar Foundation); all=false y lista vacía → nada. Dedupe idéntico (213-228) + insert con `source: "plan_bundle"` (reutilizar el source evita migrar el enum). En `tearDownForSubscription`: el protected set (523-552) debe joinear `plan_programs` de los protectores con lista y sumar esos programIds al set; el legacy fallback (577-613) puede quedar como está (filas pre-fase-112, bajo riesgo — documentar la decisión en el SUMMARY).
  </action>
  <verify>
    <automated>cd el-templo-api && grep -c "programIds" src/modules/programs/enrollment-service.ts && grep -q "goalPlanType" src/modules/programs/enrollment-service.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - EnrollFromPlanInput incluye programIds:number[].
    - enrollFromPlan resuelve all → lista → nada en un criterio único.
    - La rama lista mantiene goalPlanType IS NOT NULL (Foundation excluida).
    - Insert con source "plan_bundle" (sin migrar el enum).
    - Protected set del teardown incorpora los plan_programs de protectores con lista.
    - tsc verde.
  </acceptance_criteria>
  <done>Resolución de acceso multi-programa + teardown con filtro anti-piratería.</done>
</task>

<task type="auto">
  <name>Task 2: 4 callers proyectan la lista + amplían el guard</name>
  <read_first>
    - el-templo-api/src/modules/subscriptions/service.ts (1317-1329, 2997-3008, 3722-3743, 4162-4190 callers; guard if plan.linkedProgramId || plan.grantsAllPrograms; 963-995 anti-doble-bundle)
  </read_first>
  <action>
    En los 4 call sites de `enrollFromPlan` (assignPlan 1317, changePlanNow 2997, activateScheduledSub 3722, renew 4162): (a) cargar los `programIds` del plan (subquery/join a `plan_programs` por el plan involucrado) y pasarlos en el input; (b) ampliar el guard `if (plan.linkedProgramId || plan.grantsAllPrograms)` a `|| programIds.length > 0` para que un plan solo-lista no saltee el enrolamiento. El guard anti-doble-bundle (963-995) NO se toca — no aplica a listas (dos planes con listas coexisten; el dedupe ya evita duplicados).
  </action>
  <verify>
    <automated>cd el-templo-api && grep -c "programIds" src/modules/subscriptions/service.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Los 4 callers proyectan programIds y los pasan a enrollFromPlan.
    - El guard incluye programIds.length > 0 (un plan solo-lista enrola).
    - El guard anti-doble-bundle intacto.
    - tsc verde.
  </acceptance_criteria>
  <done>Todos los flujos de asignación/cambio/activación/renovación respetan la lista.</done>
</task>

<task type="auto">
  <name>Task 3: Test de acceso multi-programa</name>
  <read_first>
    - el-templo-api/test/subscriptions/bundle-todos-los-programas.test.ts (analog 806 líneas; fixtures createPlan/createMember/assignPlan de _helpers.ts; asserts sobre programEnrollments)
    - el-templo-api/test/subscriptions/_helpers.ts (fixtures)
  </read_first>
  <action>
    Crear `test/subscriptions/plan-programs-access.test.ts` replicando el esqueleto del bundle-test para un plan con lista: (1) all=true enrola en todos (regresión); (2) lista no vacía enrola SOLO en los programas de la lista; (3) lista vacía no enrola en ninguno; (4) la lista con un programa Foundation (goalPlanType IS NULL) NO lo enrola (filtro anti-piratería); (5) idempotencia (re-assign no duplica); (6) teardown: al anular la sub se quitan los enrollments de la lista salvo los protegidos por otra sub activa. Usar los fixtures de _helpers.ts y asserts directos sobre `program_enrollments`.
  </action>
  <verify>
    <automated>cd el-templo-api && test -f test/subscriptions/plan-programs-access.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Cubre all/lista/nada, exclusión Foundation, idempotencia y teardown con protección.
    - Asserts directos sobre program_enrollments.
    - tsc verde (los tests corren en CI).
  </acceptance_criteria>
  <done>Acceso multi-programa garantizado con test de integración.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                                                          |
| ------------------------- | ------------------------------------------------------------------------------------ |
| plan config → enrollment  | un plan (owner-editable) determina a qué programas accede el socio                   |
| member app → /sessions/\* | el gating de sesiones se apoya en enrollments; Foundation reusa contenido presencial |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                | Disposition | Mitigation Plan                                                                                |
| --------- | ---------------------- | -------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------- |
| T-156-07  | Elevation of Privilege | lista otorga Foundation (piratería contenido presencial) | mitigate    | filtro goalPlanType IS NOT NULL en la rama lista (mismo que el bundle, 104 R7) + test dedicado |
| T-156-08  | Tampering              | plan solo-lista saltea enrolamiento por guard incompleto | mitigate    | ampliar el guard con programIds.length > 0 en los 4 callers                                    |
| T-156-09  | Information Disclosure | teardown deja acceso residual a programas de la lista    | mitigate    | protected set incorpora plan_programs de protectores; test de teardown                         |
| T-156-SC  | Tampering              | npm/pip/cargo installs                                   | accept      | no se instalan paquetes nuevos                                                                 |

</threat_model>

<verification>
- tsc verde.
- Tests en CI: plan-programs-access (all/lista/nada + Foundation + teardown).
- La app de miembros no requiere cambios (consulta program_enrollments, no el plan).
</verification>

<success_criteria>
Un plan resuelve acceso all → lista → nada en un criterio único, la lista nunca otorga Foundation, el teardown protege/limpia correctamente, y todo queda garantizado con test.
</success_criteria>

<output>
Create `.planning/phases/156-planes-de-pago-vs-rutinas-de-entrenamiento/156-03-SUMMARY.md` when done
</output>
