---
phase: 156-planes-de-pago-vs-rutinas-de-entrenamiento
verified: 2026-07-05T00:00:00Z
status: human_needed
score: 6/6 must-haves verified
overrides_applied: 0
human_verification:
  - test: "En AssignPlanDialog, modo 'Cambiar ahora' (changePlanNow) con boarding pass activo y regla Zero ON"
    expected: "Decisión de negocio pendiente (WR-06, diferido intencionalmente): hoy changePlanNow cobra priceZero − prorrateo pero NO marca boardingPassUsed=true — el pase queda reutilizable indefinidamente vía cambios de plan. Owner debe decidir: (a) consumir el pass en cambio-ahora igual que en assign/changePlanAfterCurrent, o (b) rechazar boardingPass:true en change-now con 400 y ocultar el toggle en ese modo."
    why_human: "Requiere decisión de negocio del owner (Franco/Nacho), no es verificable por código — el REVIEW.md documenta la asimetría como bug pre-existente fuera de scope de esta fase, explícitamente diferido a criterio humano."
  - test: "Verificación visual de nav ('Planes de pago' / 'Rutinas de entrenamiento'), segundo toggle 'Precio Zero' en /configuracion/precios, multi-select de programas en PlanFormDialog (visible con grantsAllPrograms OFF + superficie de rutinas ON, excluye Foundation), gate de boarding pass/opción Zero en AssignPlanDialog y CobrosPage."
    expected: "Los elementos se ven y se comportan según lo descrito en cada plan (labels, toggles, multi-select con chips, ocultamiento condicional)."
    why_human: "Comportamiento visual/UX — no verificable por grep/tsc; los 5 planes documentan explícitamente 'UAT de fase, no bloqueante aquí'."
---

# Phase 156: Planes de pago vs Rutinas de entrenamiento Verification Report

**Phase Goal:** Se separan "Planes de pago" de "Rutinas de entrenamiento", el precio "Zero" pasa a configuración, un plan puede dar acceso a varios programas seleccionados, y actualizar el precio por inflación no crea un plan nuevo ni altera históricos (garantizado con test).
**Verified:** 2026-07-05
**Status:** human_needed
**Re-verification:** No — initial verification

## Context: Post-Fix State

`156-REVIEW.md` (code review, depth standard, 24 files) found 1 Critical + 6 Warnings + 2 Info. Resolution status: `resolved`. CR-01 and WR-01..WR-05 were fixed in 6 atomic `fix(156):` commits (verified present in `git log`: `55b890c5`, `2b846458`, `5805141b`, `3ce82485`, `6442f746`, `a9cb472b`). WR-06 was explicitly deferred (pre-existing bug in `changePlanNow`, requires an owner business decision — not a phase-156 regression). IN-01/IN-02 are informational, not fixed, not blocking. This verification checks the CURRENT codebase state, i.e., post-fix.

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                  | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| --- | ---------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Se separan "Planes de pago" de "Rutinas de entrenamiento" en el nav/UI, sin tocar rutas/ids (PLAN-01)                  | ✓ VERIFIED | `templo-config.ts:166` label `'Planes de pago'`, `:175` label `'Rutinas de entrenamiento'`, paths `/planes`/`/programas` unchanged (grep confirms roles/paths intact). `routines?: boolean` gate (`:123`) + `isNavItemVisible` check (`:244`) gates the item as a per-installation Templo surface (`TEMPLO_TRAINING_ROUTINES = TEMPLO_ENABLED`, `:47`). `PlanesPage.vue:6` and `ProgramasPage.vue:7` titles updated.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2   | El precio "Zero" pasa a configuración: default OFF, El Templo ON, gate server-side en el punto único (PLAN-02)         | ✓ VERIFIED | Key `pricing.zero_price_enabled` (`keys.ts`), `GET`/`PUT /pricing/zero-price` (staff/owner, `routes.ts:111-145`), migration `0168_seed_pricing_zero_price.sql` (idempotent seed `'on'`). `resolvePriceType` (`service.ts:4467-4476`) normalizes `zero`→`regular` when OFF; covers assign (`:1194`), changePlanNow (`:2938`), changePlanAfterCurrent boarding pass (`:3402`, CR-01 fix confirmed in code), renew (`:3686`), preview (`:4102`). Tests: `zero-price-setting.test.ts` + `zero-price-gate.test.ts` (9 cases incl. CR-01 case at line 193). Admin UI gates confirmed: `ConfiguracionPreciosPage.vue` 2nd toggle, `PlanFormDialog.vue` `zeroPriceEnabled` gate, `AssignPlanDialog.vue` gated option + boarding-pass toggle (WR-05 fix confirmed), `CobrosPage.vue` gated toggle.                                                                                                                                                                                                  |
| 3   | Un plan puede dar acceso a varios programas seleccionados, además de "todos" (PLAN-03)                                 | ✓ VERIFIED | Join table `plan_programs` (schema + migration `0169`, UNIQUE compuesto + FKs). `createPlan`/`updatePlan` persist `programIds` transactionally with server-side existence+active+`goalPlanType IS NOT NULL` validation (`assertProgramsExist`, WR-02 fix confirmed at `service.ts:273-296`). `enrollFromPlan` resolves all→lista→nada, list branch inherits the anti-piracy Foundation filter. 4 callers (assign/changeNow/renew/activateScheduled) project the list and widen the guard; WR-01 fix confirmed (`service.ts:3895-3899`, `4351-4355` — `enrollFromPlan` is always called, only the linked-program grant is nulled when in-flight). Teardown protects programs still covered by another sub's list. Admin: `PlanFormDialog.vue` multi-select wired to `form.programIds`, gated by `grantsAllPrograms` OFF + routines surface ON; WR-04 fix confirmed (presencial toggle no longer wipes the persisted list). Test: `plan-programs-access.test.ts` (8 cases incl. WR-01 case). |
| 4   | Actualizar el precio por inflación no crea un plan nuevo ni altera históricos, garantizado con test (PLAN-04)          | ✓ VERIFIED | `updatePlan` only touches `subscription_plans`/`plan_programs` (never `subscriptions`/`financial_transactions`). Dedicated regression test in `plans-crud.test.ts:597-678` asserts: same `plan.id` after price bump, `subscriptions.pricePaid` and `financial_transactions.amount` unchanged for a pre-existing sub, a NEW assignment to a different member uses the NEW price, and a renewal of the original member explicitly inherits the OLD `pricePaid` (documented by-design, asserted not "corrected").                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 5   | El código no introduce anti-patrones de deuda (TODO/FIXME/console.log/stubs) en los archivos tocados                   | ✓ VERIFIED | Grep for `TBD/FIXME/XXX/TODO/HACK/console.*` across all 20 phase-modified files: 0 real matches (2 false positives: "TODOS" substring in a label, an input `placeholder` attribute).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 6   | CR-01/WR-01..WR-05 del code review están efectivamente resueltos en el código actual (no solo declarados en REVIEW.md) | ✓ VERIFIED | Each fix independently re-derived from source, not from REVIEW.md/SUMMARY claims: CR-01 (`service.ts:3402`), WR-01 (`service.ts:3895-3899`/`4351-4355`), WR-02 (`service.ts:284` `isNotNull(goalPlanType)`), WR-03 (`PlanFormDialog.vue:660-661` `extractError`/`isExpectedClientError`), WR-04 (`PlanFormDialog.vue:512-526` category-gated watcher), WR-05 (`AssignPlanDialog.vue:239` `v-if="zeroPriceEnabled"`). Matching test coverage confirmed for CR-01/WR-01/WR-02.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact                                                           | Expected                                      | Status     | Details                                                                                                              |
| ------------------------------------------------------------------ | --------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------- |
| `el-templo-api/src/modules/settings/keys.ts`                       | Key `pricing.zero_price_enabled`              | ✓ VERIFIED | `zeroPrice: "pricing.zero_price_enabled"` present                                                                    |
| `el-templo-api/src/modules/settings/service.ts`                    | get/set Zero flag                             | ✓ VERIFIED | `getZeroPriceEnabled`/`setZeroPriceEnabled` via shared `getFlag`/`setFlag`                                           |
| `el-templo-api/src/modules/settings/routes.ts`                     | GET staff / PUT owner                         | ✓ VERIFIED | `/pricing/zero-price` GET+PUT, `OWNER_ROLES` preHandler                                                              |
| `el-templo-api/src/db/migrations/0168_seed_pricing_zero_price.sql` | Idempotent seed 'on'                          | ✓ VERIFIED | `INSERT ... SELECT ... WHERE NOT EXISTS`, no `;` in comments                                                         |
| `el-templo-api/test/settings/zero-price-setting.test.ts`           | CRUD + guards                                 | ✓ VERIFIED | File exists, exercised via grep of describe/it                                                                       |
| `el-templo-api/test/subscriptions/zero-price-gate.test.ts`         | Gate end-to-end + CR-01                       | ✓ VERIFIED | 9 `it()` cases incl. CR-01 changePlanAfterCurrent boarding pass                                                      |
| `el-templo-api/src/db/schema/plan-programs.ts`                     | Join table schema                             | ✓ VERIFIED | `mysqlTable("plan_programs", ...)` w/ UNIQUE compuesto + FKs                                                         |
| `el-templo-api/src/db/migrations/0169_plan_programs.sql`           | Additive CREATE TABLE                         | ✓ VERIFIED | `CREATE TABLE plan_programs`, UNIQUE, FKs RESTRICT, no `;` in comments                                               |
| `el-templo-api/test/subscriptions/plans-crud.test.ts`              | PLAN-04 regression + programIds               | ✓ VERIFIED | PLAN-03 (multi-program) + PLAN-04 (price history) describe blocks present, incl. WR-02 Foundation-only-rejected case |
| `el-templo-api/src/modules/programs/enrollment-service.ts`         | all→lista→nada + Foundation filter + teardown | ✓ VERIFIED | `programIds` (7 occurrences), `goalPlanType` filter present                                                          |
| `el-templo-api/test/subscriptions/plan-programs-access.test.ts`    | Multi-program access cases                    | ✓ VERIFIED | 8 cases incl. WR-01 in-flight-linked case                                                                            |
| `el-templo-admin/src/config/templo-config.ts`                      | Nav labels + surface flag                     | ✓ VERIFIED | Labels + `TEMPLO_TRAINING_ROUTINES` + `routines` gate in `isNavItemVisible`                                          |
| `el-templo-admin/src/pages/PlanesPage.vue` / `ProgramasPage.vue`   | Titles renamed                                | ✓ VERIFIED | Titles updated, routes/components untouched                                                                          |
| `el-templo-admin/src/pages/ConfiguracionPreciosPage.vue`           | Second Zero toggle                            | ✓ VERIFIED | `zeroPriceEnabled` toggle + banner + optimistic revert                                                               |
| `el-templo-admin/src/components/PlanFormDialog.vue`                | priceZero gate + multi-select                 | ✓ VERIFIED | `zeroPriceEnabled` gate, `form.programIds` multi-select, WR-04 fix present                                           |
| `el-templo-admin/src/types/subscription.ts`                        | `programIds` in payload shapes                | ✓ VERIFIED | `programIds?: number[]` present                                                                                      |

### Key Link Verification

| From                       | To                                    | Via                                                              | Status  | Details                                                                                                                             |
| -------------------------- | ------------------------------------- | ---------------------------------------------------------------- | ------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| `subscriptions/service.ts` | `SettingsService.getZeroPriceEnabled` | `resolvePriceType` zero branch                                   | ✓ WIRED | Confirmed at `service.ts:4467-4476`, covers all 5+ call sites incl. boarding pass in both `assignPlan` and `changePlanAfterCurrent` |
| `settings/routes.ts`       | `OWNER_ROLES` preHandler              | `PUT /pricing/zero-price`                                        | ✓ WIRED | `routes.ts:145`                                                                                                                     |
| `subscriptions/service.ts` | `plan_programs`                       | `createPlan/updatePlan` persist `programIds`                     | ✓ WIRED | `db.transaction`-wrapped delete+insert, validated via `assertProgramsExist`                                                         |
| `subscriptions/service.ts` | `mapPlanRow programIds`               | payload embedded                                                 | ✓ WIRED | `programIds` exposed in `planSchema`/`PlanListItem`                                                                                 |
| `subscriptions/service.ts` | `enrollFromPlan`                      | guard `programIds.length > 0` + list projection                  | ✓ WIRED | 4 callers confirmed; WR-01 fix confirmed (always calls `enrollFromPlan`, only linked grant is nulled)                               |
| `enrollment-service.ts`    | `program_enrollments`                 | insert source `plan_bundle` w/ `goalPlanType IS NOT NULL` filter | ✓ WIRED | Filter present in list branch (anti-piracy inherited from bundle)                                                                   |
| `usePricingSettingsApi.ts` | `/admin/settings/pricing/zero-price`  | get/set                                                          | ✓ WIRED | Confirmed in composable                                                                                                             |
| `PlanFormDialog.vue`       | `form.programIds`                     | multi-select q-select                                            | ✓ WIRED | Bound, populated in edit-mode, cleared on `grantsAllPrograms` toggle (non-presencial only per WR-04)                                |

### Data-Flow Trace (Level 4)

Not applicable in the classic dashboard sense — this phase is server-side gating + CRUD + nav labels, not a data-rendering dashboard. The one data-flow-relevant check (PLAN-04: does `updatePlan` actually leave historical `subscriptions.pricePaid`/`financial_transactions` untouched) is covered by the dedicated integration test in `plans-crud.test.ts:597-678`, traced above.

### Behavioral Spot-Checks

| Behavior                                          | Command                                                                               | Result                                                                                                                                                            | Status                              |
| ------------------------------------------------- | ------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- |
| API type-checks cleanly                           | `cd el-templo-api && npx tsc --noEmit`                                                | No output (clean exit)                                                                                                                                            | ✓ PASS                              |
| Admin lint on phase-touched files                 | `cd el-templo-admin && npx eslint <8 files>`                                          | 2 pre-existing unrelated warnings (unused vars in `CobrosPage.vue`, phase 146 code)                                                                               | ✓ PASS                              |
| Admin type-check (vue-tsc) on phase-touched files | `cd el-templo-admin && npx vue-tsc --noEmit -p tsconfig.json` filtered to phase files | 1 error in `AssignPlanDialog.vue:1080` — pre-existing, introduced in phase 146 (`git log -S` confirms), unrelated to boarding-pass/zero-price code touched by 156 | ✓ PASS (pre-existing, out of scope) |
| Migration numbering sequential, no collisions     | `ls migrations/`                                                                      | 0167 (155) → 0168 → 0169 (156), no gaps/dupes                                                                                                                     | ✓ PASS                              |

### Probe Execution

No `scripts/*/tests/probe-*.sh` declared by this phase's PLAN/SUMMARY files and none found under conventional paths. SKIPPED (no probes for this phase).

### Requirements Coverage

| Requirement | Source Plan            | Description                                                          | Status      | Evidence                                                                            |
| ----------- | ---------------------- | -------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------- |
| PLAN-01     | 156-04                 | Separación de superficie "Planes de pago"/"Rutinas de entrenamiento" | ✓ SATISFIED | Nav labels + surface gate + page titles confirmed in code                           |
| PLAN-02     | 156-01, 156-05         | Precio "Zero" a configuración                                        | ✓ SATISFIED | Setting + gate + UI confirmed, incl. CR-01/WR-05 fixes                              |
| PLAN-03     | 156-02, 156-03, 156-05 | Multi-programa por plan                                              | ✓ SATISFIED | Join table + CRUD + access resolution + UI confirmed, incl. WR-01/WR-02/WR-04 fixes |
| PLAN-04     | 156-02                 | Precio actualizable sin romper históricos                            | ✓ SATISFIED | Dedicated regression test confirmed present and substantive                         |

No orphaned requirements found: REQUIREMENTS.md traceability table maps PLAN-01..04 exclusively to Phase 156, and all 4 are claimed by plan frontmatter (`156-01`: PLAN-02; `156-02`: PLAN-03, PLAN-04; `156-03`: PLAN-03; `156-04`: PLAN-01; `156-05`: PLAN-02, PLAN-03).

### Anti-Patterns Found

None (blocker or warning level). Grep across all 20 phase-modified files for `TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER/console.*` produced only 2 false positives (a label substring "TODOS" and a legitimate `placeholder` HTML attribute on an input). No unresolved debt markers.

### Human Verification Required

### 1. WR-06 business decision — `changePlanNow` boarding pass asymmetry

**Test:** Review `el-templo-api/src/modules/subscriptions/service.ts:2920-2960` (`changePlanNow`). Confirm it still has no boarding-pass branch (verified in this pass: `resolvePriceType(input.priceTypeApplied)` is called but there is no `boardingPassUsed` validation/marking, unlike `assignPlan` and `changePlanAfterCurrent`).
**Expected:** Owner decides: (a) add symmetric boarding-pass handling to `changePlanNow` (validate `boardingPassUsed`, mark it used), or (b) reject `boardingPass:true` in change-now with 400 and hide the toggle in that UI mode.
**Why human:** This is a business-rule decision (whether "cambiar ahora, reiniciando" should consume the one-shot boarding pass), explicitly deferred in `156-REVIEW.md` as pre-existing and out of scope for this fix pass — not resolvable by grep/code inspection alone.

### 2. Visual/UX confirmation of admin surface changes

**Test:** Load `/planes` and `/programas` in the admin (as owner, El Templo installation) and confirm: nav shows "Planes de pago" / "Rutinas de entrenamiento"; `/configuracion/precios` shows two toggles (tarjeta + Zero) with correct copy; `PlanFormDialog` shows the programs multi-select (chips) when `grantsAllPrograms` is OFF and hides it when ON; toggling "Da acceso a TODOS" on a plan presencial no longer wipes a previously-set program list (WR-04); `AssignPlanDialog`/`CobrosPage` hide the Zero option/toggle and the boarding-pass toggle when the Zero rule is OFF (WR-05).
**Expected:** All UI elements render and behave per the description above.
**Why human:** Visual rendering and interactive toggle behavior are not verifiable via static code inspection alone; all 5 plans explicitly flag this as "UAT de fase, no bloqueante aquí."

### Gaps Summary

No gaps found. All 4 requirement IDs (PLAN-01..04) are backed by concrete, wired, tested code in the current codebase — not just SUMMARY claims. The code-review cycle (156-REVIEW.md) found 1 Critical + 6 Warnings; this verification independently re-derived from source (not from REVIEW.md's own "RESUELTO" annotations) that CR-01 and WR-01 through WR-05 are genuinely fixed in the code, each with matching test coverage where applicable. WR-06 is a pre-existing, out-of-scope bug correctly left unresolved pending an explicit owner decision — it is surfaced here as a human-verification item, not a gap, per the phase's own instructions. IN-01/IN-02 are low-severity info items, correctly left unaddressed, not blocking. Status is `human_needed` (not `passed`) solely because of the two human-verification items above (business decision + visual UAT) — no code-level deficiency exists.

---

_Verified: 2026-07-05_
_Verifier: Claude (gsd-verifier)_
