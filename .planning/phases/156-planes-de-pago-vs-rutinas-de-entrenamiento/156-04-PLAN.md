---
phase: 156-planes-de-pago-vs-rutinas-de-entrenamiento
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-admin/src/config/templo-config.ts
  - el-templo-admin/src/pages/PlanesPage.vue
  - el-templo-admin/src/pages/ProgramasPage.vue
autonomous: true
requirements: [PLAN-01]

must_haves:
  truths:
    - "El nav muestra 'Planes de pago' (path /planes) y 'Rutinas de entrenamiento' (path /programas)"
    - "El ítem 'Rutinas de entrenamiento' está gateado como superficie Templo (no aparece en white-label default)"
    - "Los títulos de PlanesPage y ProgramasPage reflejan los nombres nuevos"
    - "Los paths y los identificadores de código (planes/programas) NO cambian"
  artifacts:
    - path: "el-templo-admin/src/config/templo-config.ts"
      provides: "Labels nav + flag de superficie de rutinas"
      contains: "Rutinas de entrenamiento"
    - path: "el-templo-admin/src/pages/PlanesPage.vue"
      provides: "Título 'Planes de pago'"
    - path: "el-templo-admin/src/pages/ProgramasPage.vue"
      provides: "Título 'Rutinas de entrenamiento'"
  key_links:
    - from: "el-templo-admin/src/config/templo-config.ts"
      to: "isNavItemVisible"
      via: "flag de superficie gatea el ítem /programas"
      pattern: "TEMPLO_TRAINING_ROUTINES|routines"
---

<objective>
Separación de superficie "Planes de pago" vs "Rutinas de entrenamiento" (PLAN-01, D-01/D-02). Renames de LABEL y títulos, sin tocar rutas ni identificadores de código. El ítem "Rutinas de entrenamiento" (`/programas`) se gatea como superficie Templo (patrón `TEMPLO_GREEK_LEVELS`, per-instalación, no per-user), además del dueño-only existente (149 D-15): white-label default no lo ve, El Templo sí. Nada se borra (NAV-04).

Purpose: nombres claros para Nacho; de-Templo-ficación de la superficie MVP.
Output: labels nav + flag de superficie + títulos de página.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-planes-de-pago-vs-rutinas-de-entrenamiento/156-CONTEXT.md
@.planning/phases/156-planes-de-pago-vs-rutinas-de-entrenamiento/156-PATTERNS.md

<interfaces>
<!-- Líneas exactas del PATTERNS — no explorar. -->

templo-config.ts:
TEMPLO_GREEK_LEVELS = TEMPLO_ENABLED (25-34) — analog del flag per-instalación
NAV_MODEL (147-152): header 'Planes' con items { path:'/planes', label:'Planes', roles:PLANES_READ_ROLES } y { path:'/programas', label:'Programas', roles:DUENO_ROLES }
isNavItemVisible (214-221): if (item.templo && !TEMPLO_ENABLED) return false — mecanismo de gating de superficie

PlanesPage.vue: título text-h5 línea 6 ('Planes')
ProgramasPage.vue: título text-h5 línea 7 ('Programas')
Consumo del flag en páginas (analog AlumnosPage.vue:356-360)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Labels nav + flag de superficie de rutinas</name>
  <read_first>
    - el-templo-admin/src/config/templo-config.ts (25-34 TEMPLO_GREEK_LEVELS; 147-152 NAV_MODEL; 214-221 isNavItemVisible)
  </read_first>
  <action>
    Agregar `export const TEMPLO_TRAINING_ROUTINES = TEMPLO_ENABLED;` con docblock equivalente a TEMPLO_GREEK_LEVELS (per-instalación, NO per-user, NO reutilizar canAccessTraining). Cambiar los labels del NAV_MODEL: 'Planes' → 'Planes de pago' y 'Programas' → 'Rutinas de entrenamiento' (paths `/planes` y `/programas` intactos, roles intactos). Gatear el ítem `/programas` por la superficie de rutinas: agregar una prop análoga a `templo` en NavItem (p.ej. `routines?: boolean`) chequeada en isNavItemVisible contra `TEMPLO_TRAINING_ROUTINES` (o marcar el ítem `templo: true` si se prefiere reutilizar el knob existente — a discreción, pero mantener el gating per-instalación). El header/categoría 'Planes' de nivel superior conserva su label si es la categoría; solo cambian los labels de ítems según D-01.
  </action>
  <verify>
    <automated>cd el-templo-admin && grep -q "Planes de pago" src/config/templo-config.ts && grep -q "Rutinas de entrenamiento" src/config/templo-config.ts && grep -q "TEMPLO_TRAINING_ROUTINES\|routines" src/config/templo-config.ts && npx vue-tsc --noEmit -p tsconfig.json 2>/dev/null || npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Labels 'Planes de pago' y 'Rutinas de entrenamiento' en el nav; paths y roles intactos.
    - Flag de superficie de rutinas (per-instalación) que gatea el ítem /programas en isNavItemVisible.
    - White-label default no muestra Rutinas; El Templo sí.
    - Type check verde.
  </acceptance_criteria>
  <done>Nav renombrado y superficie de rutinas gateada.</done>
</task>

<task type="auto">
  <name>Task 2: Títulos de PlanesPage y ProgramasPage</name>
  <read_first>
    - el-templo-admin/src/pages/PlanesPage.vue (título text-h5 línea 6)
    - el-templo-admin/src/pages/ProgramasPage.vue (título text-h5 línea 7)
  </read_first>
  <action>
    Actualizar el título visible de PlanesPage a "Planes de pago" y el de ProgramasPage a "Rutinas de entrenamiento" (y cualquier texto visible coherente en esas cabeceras). NO renombrar rutas, componentes, ni identificadores de código. NO tocar la lógica de la tabla ni las columnas de precio.
  </action>
  <verify>
    <automated>cd el-templo-admin && grep -q "Planes de pago" src/pages/PlanesPage.vue && grep -q "Rutinas de entrenamiento" src/pages/ProgramasPage.vue</automated>
  </verify>
  <acceptance_criteria>
    - PlanesPage titula "Planes de pago"; ProgramasPage titula "Rutinas de entrenamiento".
    - Sin cambios de ruta ni de identificadores.
  </acceptance_criteria>
  <done>Títulos de página coherentes con los nombres nuevos.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                          |
| --------------------------- | ---------------------------------------------------- |
| nav visibility → superficie | el nav solo OCULTA ítems; no es control de seguridad |

## STRIDE Threat Register

| Threat ID | Category               | Component                                   | Disposition | Mitigation Plan                                                                                                                                                                   |
| --------- | ---------------------- | ------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-156-10  | Elevation of Privilege | ocultar /programas por flag no es seguridad | accept      | el gating real es dueño-only por rol en la API (149 D-15); el flag de superficie solo esconde en UI (comentado en templo-config: "The nav only HIDES items — it is NOT security") |
| T-156-SC  | Tampering              | npm/pip/cargo installs                      | accept      | no se instalan paquetes nuevos                                                                                                                                                    |

</threat_model>

<verification>
- Type check verde en el-templo-admin.
- Verificación visual del nav y títulos → cubierta por UAT de fase (no bloqueante aquí).
</verification>

<success_criteria>
Nav y páginas muestran "Planes de pago" y "Rutinas de entrenamiento"; la subcategoría de rutinas queda gateada como superficie Templo; rutas e identificadores intactos.
</success_criteria>

<output>
Create `.planning/phases/156-planes-de-pago-vs-rutinas-de-entrenamiento/156-04-SUMMARY.md` when done
</output>
