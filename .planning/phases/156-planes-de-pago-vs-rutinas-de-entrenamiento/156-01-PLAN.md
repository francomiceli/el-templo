---
phase: 156-planes-de-pago-vs-rutinas-de-entrenamiento
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/settings/keys.ts
  - el-templo-api/src/modules/settings/service.ts
  - el-templo-api/src/modules/settings/routes.ts
  - el-templo-api/src/modules/subscriptions/service.ts
  - el-templo-api/src/db/migrations/0168_seed_pricing_zero_price.sql
  - el-templo-api/test/settings/zero-price-setting.test.ts
  - el-templo-api/test/subscriptions/zero-price-gate.test.ts
autonomous: true
requirements: [PLAN-02]

must_haves:
  truths:
    - "GET /api/admin/settings/pricing/zero-price devuelve el estado de la regla Zero para staff"
    - "PUT /api/admin/settings/pricing/zero-price solo lo puede cambiar el dueño (403 para el resto)"
    - "En instalación default la regla Zero está OFF; en El Templo (seed) está ON"
    - "Con Zero OFF, asignar/renovar con priceType='zero' persiste 'regular' y cobra priceRegular"
    - "Con Zero OFF, un boarding pass NO fuerza 'zero' — se normaliza vía resolvePriceType"
  artifacts:
    - path: "el-templo-api/src/modules/settings/keys.ts"
      provides: "Key pricing.zero_price_enabled"
      contains: "zeroPrice"
    - path: "el-templo-api/src/db/migrations/0168_seed_pricing_zero_price.sql"
      provides: "Seed idempotente 'on' para El Templo"
      contains: "pricing.zero_price_enabled"
    - path: "el-templo-api/test/settings/zero-price-setting.test.ts"
      provides: "CRUD + guards de la setting Zero"
    - path: "el-templo-api/test/subscriptions/zero-price-gate.test.ts"
      provides: "Gate end-to-end assign/renew/boarding pass"
  key_links:
    - from: "el-templo-api/src/modules/subscriptions/service.ts"
      to: "SettingsService.getZeroPriceEnabled"
      via: "resolvePriceType rama zero"
      pattern: "resolvePriceType"
    - from: "el-templo-api/src/modules/settings/routes.ts"
      to: "OWNER_ROLES preHandler"
      via: "PUT /pricing/zero-price"
      pattern: "pricing/zero-price"
---

<objective>
Mover el precio "Zero" del default a configuración (PLAN-02, D-03/D-04). Réplica exacta del par `card_surcharge` de la fase 154: nueva key `pricing.zero_price_enabled` en el módulo `settings` (GET staff / PUT owner-only), default OFF, seed ON para El Templo (migración 0168). El gate vive en el punto único `resolvePriceType`: con la regla OFF normaliza `zero`→`regular`, cubriendo assign/change/renew/preview/PoS sin duplicar lógica. Se resuelve además el edge del boarding pass (service.ts:1074-1080) ruteándolo también por `resolvePriceType`.

Purpose: white-label sin "Zero"; El Templo conserva comportamiento de prod intacto.
Output: settings key + endpoints + migración 0168 + gate en resolvePriceType + boarding pass + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-planes-de-pago-vs-rutinas-de-entrenamiento/156-CONTEXT.md
@.planning/phases/156-planes-de-pago-vs-rutinas-de-entrenamiento/156-PATTERNS.md

<interfaces>
<!-- Analogs card_surcharge — el executor los replica literalmente, sin explorar. -->

settings/keys.ts (9-12):
PRICING_SETTINGS_KEYS = { cardSurcharge: "pricing.card_surcharge_enabled" }
→ agregar zeroPrice: "pricing.zero_price_enabled"

settings/service.ts (33-62):
getCardSurchargeEnabled(): Promise<boolean> // select settingKey, row?.settingValue === ON, default false
setCardSurchargeEnabled(enabled): insert(...).onDuplicateKeyUpdate(...) + log.info
→ duplicar como getZeroPriceEnabled/setZeroPriceEnabled (o extraer getFlag/setFlag privados; DRY a discreción)

settings/routes.ts (29-110):
onRequest exige staff (29-37); GET /pricing/card-surcharge (40-66); PUT (69-109) con preHandler OWNER_ROLES (72-79); schema { enabled: boolean }

subscriptions/service.ts resolvePriceType (4276-4288):
private async resolvePriceType(priceType: PriceType): Promise<PriceType>
// hoy: if (priceType !== "credit_card") return priceType; ... surchargeEnabled ? priceType : "regular"
// los 5 call sites: 1052 (assignPlan), 2813 (changePlan), 3238, 3545 (renew WR-04), 3945 (preview)

subscriptions/service.ts boarding pass (1074-1080):
input.boardingPass fuerza priceTypeApplied = "zero" DESPUÉS de resolvePriceType (1052)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Key + service + routes + migración 0168 de la setting Zero</name>
  <read_first>
    - el-templo-api/src/modules/settings/keys.ts (fuente: PRICING_SETTINGS_KEYS)
    - el-templo-api/src/modules/settings/service.ts (33-62: par card-surcharge)
    - el-templo-api/src/modules/settings/routes.ts (29-110: GET staff / PUT owner)
    - el-templo-api/src/db/migrations/0166_seed_pricing_card_surcharge.sql (analog completo)
    - CLAUDE.md (migraciones a mano, nunca `;` en comentarios SQL)
  </read_first>
  <action>
    Agregar la key `zeroPrice: "pricing.zero_price_enabled"` a PRICING_SETTINGS_KEYS. En settings/service.ts agregar getter/setter para la key Zero replicando el par card-surcharge (default false; setter con `onDuplicateKeyUpdate` + `this.log.info`); si preferís DRY, extraé `getFlag(key)`/`setFlag(key, enabled)` privados y expresá ambos pares en términos de ellos. En settings/routes.ts agregar GET `/pricing/zero-price` (staff, copia de 40-66) y PUT `/pricing/zero-price` (preHandler OWNER_ROLES, copia de 69-109, schema `{ enabled: boolean }`, `handleServiceError` en el catch). Crear migración `0168_seed_pricing_zero_price.sql` como copia de 0166 con key `pricing.zero_price_enabled` seed `'on'` (INSERT ... SELECT ... WHERE NOT EXISTS, idempotente). NUNCA `;` dentro de comentarios `--`.
  </action>
  <verify>
    <automated>cd el-templo-api && grep -q "zero_price_enabled" src/modules/settings/keys.ts && grep -q "pricing/zero-price" src/modules/settings/routes.ts && grep -q "pricing.zero_price_enabled" src/db/migrations/0168_seed_pricing_zero_price.sql && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - keys.ts exporta `zeroPrice: "pricing.zero_price_enabled"`.
    - service.ts expone getter/setter de la regla Zero (default false).
    - routes.ts registra GET `/pricing/zero-price` (staff) y PUT (owner-only vía preHandler).
    - Migración 0168 idempotente, seed `'on'`, sin `;` en comentarios.
    - tsc verde.
  </acceptance_criteria>
  <done>Setting Zero disponible en API con seed El Templo ON, default OFF.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate zero en resolvePriceType + boarding pass + tests</name>
  <read_first>
    - el-templo-api/src/modules/subscriptions/service.ts (4276-4288 resolvePriceType; 1052/1074-1080 assignPlan+boardingPass; 3542-3551 renew WR-04)
    - el-templo-api/test/settings/pricing-setting.test.ts (analog CRUD setting)
    - el-templo-api/test/finance/coach-load-pricing-gate.test.ts (analog gate end-to-end)
  </read_first>
  <behavior>
    - Con Zero OFF: assignPlan con priceType 'zero' persiste priceTypeApplied 'regular' y cobra priceRegular.
    - Con Zero ON: assignPlan con 'zero' persiste 'zero' y cobra priceZero (comportamiento actual intacto).
    - Con Zero OFF: renovación heredada de una sub 'zero' renueva a 'regular' (WR-04).
    - Con Zero OFF: assignPlan con boardingPass=true NO persiste 'zero' — se normaliza a 'regular'.
    - Setting: default OFF; PUT owner→200; PUT no-owner→403; GET staff→200; GET member→403.
  </behavior>
  <action>
    En `resolvePriceType` agregar la rama simétrica a card-surcharge: `if (priceType === "zero")` → si `getZeroPriceEnabled()` devuelve true retornar `"zero"`, si no loguear (`{ rule: PRICING_SETTINGS_KEYS.zeroPrice }`, "zero-price rule OFF — normalizing to regular") y retornar `"regular"`. Esto cubre los 5 call sites (1052/2813/3238/3545/3945) incluida la herencia de renovaciones. Para el boarding pass (1074-1080): en lugar de forzar `priceTypeApplied = "zero"` directo, rutear ese valor por `resolvePriceType` (segundo pase) para que con Zero OFF quede 'regular'; con Zero ON el resultado sigue siendo 'zero' (idéntico a hoy). NO recortar los enums de schemas.ts (precedente 154: el gate es la normalización server-side). Crear `test/settings/zero-price-setting.test.ts` (copia de pricing-setting.test.ts con URL `/api/admin/settings/pricing/zero-price` y key aserted) y `test/subscriptions/zero-price-gate.test.ts` (esqueleto de coach-load-pricing-gate.test.ts: describe ON / OFF, con assign, renovación WR-04 y boarding pass).
  </action>
  <verify>
    <automated>cd el-templo-api && grep -vn '^\s*//' src/modules/subscriptions/service.ts | grep -c 'resolvePriceType' && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - resolvePriceType normaliza 'zero'→'regular' cuando la regla está OFF y persiste el valor normalizado.
    - Boarding pass ruteado por resolvePriceType (no fuerza 'zero' con regla OFF).
    - test/settings/zero-price-setting.test.ts cubre default-OFF, PUT owner/no-owner, GET staff/member.
    - test/subscriptions/zero-price-gate.test.ts cubre assign OFF→regular, renovación WR-04 y boarding pass OFF→regular.
    - tsc verde.
  </acceptance_criteria>
  <done>Gate Zero server-side operativo en el punto único; boarding pass cubierto; tests que lo garantizan.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                  |
| ----------------------------- | ------------------------------------------------------------ |
| admin client → PUT settings   | staff autenticado intenta cambiar una regla owner-only       |
| admin/PoS client → assignPlan | payload con priceType='zero' cruza el borde con la regla OFF |

## STRIDE Threat Register

| Threat ID | Category               | Component                                 | Disposition | Mitigation Plan                                                                                                                         |
| --------- | ---------------------- | ----------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| T-156-01  | Elevation of Privilege | PUT /pricing/zero-price                   | mitigate    | preHandler OWNER_ROLES (copia de card-surcharge 72-79); test 403 no-owner                                                               |
| T-156-02  | Tampering              | assignPlan/renew/PoS con priceType='zero' | mitigate    | normalización server-side en resolvePriceType (punto único); el valor persistido queda 'regular' con regla OFF, sin importar el payload |
| T-156-03  | Tampering              | boardingPass fuerza 'zero' (bypass)       | mitigate    | rutear boarding pass por resolvePriceType (segundo pase)                                                                                |
| T-156-SC  | Tampering              | npm/pip/cargo installs                    | accept      | esta fase no instala paquetes nuevos (solo código sobre módulos existentes)                                                             |

</threat_model>

<verification>
- tsc verde en el-templo-api.
- Los tests corren en CI (no localmente): zero-price-setting + zero-price-gate.
- Migración 0168 idempotente y sin `;` en comentarios.
</verification>

<success_criteria>
Regla Zero configurable (GET staff/PUT owner), default OFF con seed El Templo ON, y normalización server-side en resolvePriceType que cubre assign/change/renew/preview/PoS y el boarding pass, garantizado con tests.
</success_criteria>

<output>
Create `.planning/phases/156-planes-de-pago-vs-rutinas-de-entrenamiento/156-01-SUMMARY.md` when done
</output>
