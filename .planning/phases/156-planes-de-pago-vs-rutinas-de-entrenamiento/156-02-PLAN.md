---
phase: 156-planes-de-pago-vs-rutinas-de-entrenamiento
plan: 02
type: execute
wave: 2
depends_on: ["156-01"]
files_modified:
  - el-templo-api/src/db/schema/plan-programs.ts
  - el-templo-api/src/db/schema/index.ts
  - el-templo-api/src/db/migrations/0169_plan_programs.sql
  - el-templo-api/src/modules/subscriptions/service.ts
  - el-templo-api/src/modules/subscriptions/schemas.ts
  - el-templo-api/src/modules/subscriptions/types.ts
  - el-templo-api/test/subscriptions/plans-crud.test.ts
autonomous: true
requirements: [PLAN-03, PLAN-04]

must_haves:
  truths:
    - "Existe la tabla join plan_programs (subscription_plan_id, program_id, UNIQUE compuesto)"
    - "El create/update de planes acepta programIds:number[] y lo persiste (delete+insert por planId)"
    - "El payload de planes (planSchema/mapPlanRow/PlanListItem) expone programIds"
    - "Un plan online con lista no vacía satisface assertPlanInvariants"
    - "Actualizar precio de un plan NO crea plan nuevo (mismo id) ni altera pricePaid/transacciones históricas"
    - "Una asignación NUEVA posterior al update usa el precio nuevo; la renovación hereda pricePaid por diseño"
  artifacts:
    - path: "el-templo-api/src/db/schema/plan-programs.ts"
      provides: "Drizzle schema join plan_programs"
      contains: "plan_programs"
    - path: "el-templo-api/src/db/migrations/0169_plan_programs.sql"
      provides: "CREATE TABLE aditivo + UNIQUE compuesto"
      contains: "plan_programs"
    - path: "el-templo-api/test/subscriptions/plans-crud.test.ts"
      provides: "Regresión PLAN-04 + persistencia programIds"
  key_links:
    - from: "el-templo-api/src/modules/subscriptions/service.ts"
      to: "plan_programs"
      via: "createPlan/updatePlan persist programIds"
      pattern: "plan_programs|planPrograms"
    - from: "el-templo-api/src/modules/subscriptions/service.ts"
      to: "mapPlanRow programIds"
      via: "payload embebido"
      pattern: "programIds"
---

<objective>
Base de datos + CRUD de la selección múltiple de programas por plan (PLAN-03, D-06/D-08) y test de regresión de precios (PLAN-04, D-09). Tabla join `plan_programs` (analog `blog_post_tags`) + migración 0169. `grants_all_programs` se CONSERVA con prioridad: true → todos (lista ignorada); false → la lista de `plan_programs`. El create/update de planes acepta `programIds:number[]` embebido en el payload y lo persiste (delete+insert por planId). El payload de respuesta expone `programIds`. PLAN-04 se garantiza con test: `updatePlan` solo toca `subscription_plans` (ya verificado en código), nunca `subscriptions`/`financial_transactions`.

Purpose: un plan puede dar acceso a varios programas seleccionados; subir precio por inflación no rompe históricos.
Output: schema plan-programs + migración 0169 + CRUD programIds + payload + test PLAN-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-planes-de-pago-vs-rutinas-de-entrenamiento/156-CONTEXT.md
@.planning/phases/156-planes-de-pago-vs-rutinas-de-entrenamiento/156-PATTERNS.md

<interfaces>
<!-- Analogs con línea exacta — no explorar. -->

db/schema/blog-tags.ts (17-29): blogPostTags = mysqlTable("blog_post_tags", { id, postId, tagId }, uniqueIndex + index por col)
subscription-plans.ts:36: patrón .references(() => programs.id) para FK real (D-06)
⚠️ MEMORY: 1er arg de columna = snake_case EXACTO de la migración, o CI revienta con Unknown column.

subscriptions/service.ts:
createPlan (501-540): insert directo + getPlanById
updatePlan (545-606): patch campo-a-campo if (input.X !== undefined) + assertPlanInvariants + UPDATE + re-fetch
⚠️ SOLO toca subscription_plans (598-603) — nunca subscriptions/financial_transactions (base de PLAN-04)
assertPlanInvariants (228-245): planes online exigen linkedProgramId O grantsAllPrograms → agregar "O lista no vacía"
mapPlanRow (~4351): fila → PlanListItem
pricePaid copiado al asignar (1229 vía getBasePrice 1090-1091); renew hereda currentSub.pricePaid (3517, DELIBERADO)

subscriptions/schemas.ts: createPlanSchema/updatePlanSchema (vecino grantsAllPrograms:{type:"boolean"}); planSchema response (20-48)
subscriptions/types.ts: CreatePlanInput/UpdatePlanInput (99-137); PlanListItem (60-82)

test/subscriptions/plans-crud.test.ts:30-59 (PUT mismo id, preserva campos); test/subscriptions/charge-on-assign.test.ts (asserts transacciones)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema plan_programs + migración 0169</name>
  <read_first>
    - el-templo-api/src/db/schema/blog-tags.ts (17-29 analog join)
    - el-templo-api/src/db/schema/subscription-plans.ts (36 patrón .references)
    - el-templo-api/src/db/schema/index.ts (registro de exports)
    - el-templo-api/src/db/migrations/0152_class_coach_roster_and_ratings.sql (analog DDL aditivo + UNIQUE compuesto)
    - CLAUDE.md (migraciones a mano, nunca `;` en comentarios)
  </read_first>
  <action>
    Crear `src/db/schema/plan-programs.ts` con `planPrograms = mysqlTable("plan_programs", { id autoincrement, subscriptionPlanId int("subscription_plan_id").notNull().references(() => subscriptionPlans.id), programId int("program_id").notNull().references(() => programs.id) }, uniqueIndex compuesto ("plan_program_unique").on(subscriptionPlanId, programId) + index por cada columna)`. Nombres de columna snake_case EXACTOS iguales a la migración. Registrar el export en `src/db/schema/index.ts`. Crear `0169_plan_programs.sql` (analog 0152): encabezado con rationale de idempotencia/reversibilidad, `CREATE TABLE plan_programs` aditivo, UNIQUE KEY compuesto, FKs RESTRICT. Sin `;` en comentarios.
  </action>
  <verify>
    <automated>cd el-templo-api && test -f src/db/schema/plan-programs.ts && grep -q "plan_programs" src/db/migrations/0169_plan_programs.sql && grep -q "plan-programs\|planPrograms" src/db/schema/index.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - plan-programs.ts define planPrograms con UNIQUE compuesto + FKs a subscription_plans y programs.
    - Columnas snake_case coinciden con la migración.
    - index.ts re-exporta el schema.
    - 0169 aditivo, UNIQUE compuesto, sin `;` en comentarios.
    - tsc verde.
  </acceptance_criteria>
  <done>Tabla join plan_programs disponible en schema y migración.</done>
</task>

<task type="auto">
  <name>Task 2: CRUD programIds embebido + payload + assertPlanInvariants + test PLAN-04</name>
  <read_first>
    - el-templo-api/src/modules/subscriptions/service.ts (501-540 createPlan; 545-606 updatePlan; 228-245 assertPlanInvariants; ~4351 mapPlanRow; 598-603 confirma PLAN-04)
    - el-templo-api/src/modules/subscriptions/schemas.ts (createPlanSchema/updatePlanSchema/planSchema 20-48)
    - el-templo-api/src/modules/subscriptions/types.ts (CreatePlanInput/UpdatePlanInput 99-137; PlanListItem 60-82)
    - el-templo-api/test/subscriptions/plans-crud.test.ts (30-59)
    - el-templo-api/test/subscriptions/charge-on-assign.test.ts (asserts transacciones)
  </read_first>
  <action>
    En schemas.ts agregar `programIds: { type: "array", items: { type: "integer" } }` a createPlanSchema/updatePlanSchema y a planSchema (response). En types.ts agregar `programIds?: number[]` a CreatePlanInput/UpdatePlanInput y `programIds: number[]` a PlanListItem. En service.ts: (a) `createPlan`/`updatePlan` persisten la lista con delete-por-planId + insert de las filas de `plan_programs` dentro del mismo flujo — envolver create+programIds y update+programIds en `db.transaction` para atomicidad; validar que cada programId exista (query contra `programs` activos) y rechazar ids inválidos (400) — NO confiar en el payload; (b) `mapPlanRow` incluye `programIds` (subquery/join a plan_programs); (c) `assertPlanInvariants` acepta lista no vacía como binding válido para planes online (además de linkedProgramId/grantsAllPrograms). En plans-crud.test.ts agregar: persistencia de programIds en create/update; **caso explícito de rechazo: POST/PUT /plans con un `programId` inexistente → 400 y NO se persiste ninguna fila en plan_programs** (warning 2 del plan-checker); y la regresión PLAN-04: assign a un socio → update de priceRegular/priceCreditCard/priceZero → assert (mismo plan id, `subscriptions.pricePaid` intacto, `financial_transactions` intactos) → nuevo assign usa el precio nuevo; documentar con un assert que la renovación hereda pricePaid por diseño (NO corregir la herencia).
  </action>
  <verify>
    <automated>cd el-templo-api && grep -q "programIds" src/modules/subscriptions/schemas.ts && grep -q "programIds" src/modules/subscriptions/types.ts && grep -c "programIds" src/modules/subscriptions/service.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - create/update de planes aceptan y persisten programIds (delete+insert por planId, atómico).
    - programIds inválidos (programa inexistente) → 400, no se persisten.
    - planSchema/mapPlanRow/PlanListItem exponen programIds.
    - assertPlanInvariants acepta lista no vacía en planes online.
    - Test PLAN-04: update de precio no crea plan nuevo, pricePaid/transacciones intactos, assign posterior usa precio nuevo, renovación hereda (documentado).
    - tsc verde.
  </acceptance_criteria>
  <done>CRUD multi-programa embebido + payload + regresión de precios garantizada con test.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                       |
| ------------------------------ | ------------------------------------------------- |
| admin client → POST/PUT /plans | payload con programIds arbitrarios cruza el borde |

## STRIDE Threat Register

| Threat ID | Category               | Component                        | Disposition | Mitigation Plan                                                                            |
| --------- | ---------------------- | -------------------------------- | ----------- | ------------------------------------------------------------------------------------------ |
| T-156-04  | Tampering              | createPlan/updatePlan programIds | mitigate    | validar cada programId contra `programs` existentes antes de insertar; ids inválidos → 400 |
| T-156-05  | Information Disclosure | mapPlanRow expone programIds     | accept      | los ids de programa no son sensibles; el endpoint ya es staff/owner-gated                  |
| T-156-06  | Denial of Service      | delete+insert por planId         | accept      | lista acotada (programas activos del gym); volumen trivial, dentro de transacción          |
| T-156-SC  | Tampering              | npm/pip/cargo installs           | accept      | no se instalan paquetes nuevos                                                             |

</threat_model>

<verification>
- tsc verde.
- Tests en CI: plans-crud (persistencia programIds + PLAN-04).
- Migración 0169 aditiva, idempotente, sin `;` en comentarios.
- La exclusión Foundation en la resolución de acceso se cubre en 156-03 (este plan solo persiste la lista).
</verification>

<success_criteria>
Tabla join + CRUD embebido de programIds con validación server-side, payload que expone la lista, invariantes actualizados, y test que garantiza que subir precio no crea plan nuevo ni altera históricos.
</success_criteria>

<output>
Create `.planning/phases/156-planes-de-pago-vs-rutinas-de-entrenamiento/156-02-SUMMARY.md` when done
</output>
