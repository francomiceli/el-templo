---
phase: 156-planes-de-pago-vs-rutinas-de-entrenamiento
plan: 05
type: execute
wave: 3
depends_on: ["156-01", "156-02", "156-04"]
files_modified:
  - el-templo-admin/src/composables/usePricingSettingsApi.ts
  - el-templo-admin/src/pages/ConfiguracionPreciosPage.vue
  - el-templo-admin/src/components/PlanFormDialog.vue
  - el-templo-admin/src/components/AssignPlanDialog.vue
  - el-templo-admin/src/pages/CobrosPage.vue
  - el-templo-admin/src/types/subscription.ts
autonomous: true
requirements: [PLAN-02, PLAN-03]

must_haves:
  truths:
    - "La página /configuracion/precios tiene un segundo toggle 'Precio Zero' con copy explicativo"
    - "Con Zero OFF: CobrosPage esconde el toggle Zero, AssignPlanDialog no ofrece la opción, PlanFormDialog esconde priceZero"
    - "PlanFormDialog ofrece un multi-select de programas visible cuando grantsAllPrograms está OFF y la superficie de rutinas está ON"
    - "El watch de grantsAllPrograms limpia el multi-select al prender 'todos'"
    - "El payload de create/update de planes viaja con programIds"
  artifacts:
    - path: "el-templo-admin/src/pages/ConfiguracionPreciosPage.vue"
      provides: "Segundo toggle Zero"
      contains: "Zero"
    - path: "el-templo-admin/src/components/PlanFormDialog.vue"
      provides: "Gate priceZero + multi-select programas"
      contains: "programIds"
    - path: "el-templo-admin/src/types/subscription.ts"
      provides: "programIds en payloads y PlanListItem"
      contains: "programIds"
  key_links:
    - from: "el-templo-admin/src/composables/usePricingSettingsApi.ts"
      to: "/admin/settings/pricing/zero-price"
      via: "get/set zero-price"
      pattern: "zero-price"
    - from: "el-templo-admin/src/components/PlanFormDialog.vue"
      to: "form.programIds"
      via: "multi-select q-select"
      pattern: "programIds"
---

<objective>
Superficie admin de PLAN-02 (Zero UI, D-05) y PLAN-03 (multi-select de programas, D-08). Segundo toggle "Precio Zero" en `/configuracion/precios` (analog card-surcharge); con Zero OFF se esconden todos los controles Zero (CobrosPage, AssignPlanDialog, PlanFormDialog) — la UI esconde, el gate real ya vive server-side (156-01). PlanFormDialog gana un multi-select de programas visible cuando `grantsAllPrograms` está OFF y la superficie de rutinas está ON; el payload viaja con `programIds` (156-02).

Purpose: coach/admin configura Zero y arma planes multi-programa desde la UI.
Output: métodos zero en el composable + toggle + gates Zero + multi-select + tipos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-planes-de-pago-vs-rutinas-de-entrenamiento/156-CONTEXT.md
@.planning/phases/156-planes-de-pago-vs-rutinas-de-entrenamiento/156-PATTERNS.md

<interfaces>
<!-- Líneas exactas del PATTERNS — no explorar. -->

usePricingSettingsApi.ts: getCardSurchargeEnabled (36-50) / setCardSurchargeEnabled (57-68) → api.get/put<{enabled:boolean}>('/admin/settings/pricing/card-surcharge') con loading/error/extractError/cleanup → duplicar para '/admin/settings/pricing/zero-price'

ConfiguracionPreciosPage.vue (84 líneas): q-card + q-banner (12-19) + q-toggle @update:model-value=onToggle (21-27), loadSetting() onMounted (52-63), onToggle con revert optimista (65-79)

PlanFormDialog.vue:
campo priceZero (87-97, required rule 95); campo Tarjeta gateado v-if="cardSurchargeEnabled" (98-107) — analog exacto
loadCardSurchargeRule (308-320, conservador OFF ante error) + invocación al abrir (477); onSubmit defaultea (546)
q-select linkedProgramId (178-196) + programOptions (359-366) — base del multi-select
toggle grantsAllPrograms (205); watch (456-463) limpia linkedProgramId al prender
poblar edit-mode (479-499); payload (535-562)
⚠️ priceZero es required en form Y en el POST del API → con el campo oculto, defaultear en onSubmit (p.ej. priceZero = form.priceRegular o 0)

AssignPlanDialog.vue: priceTypeOptions computed (1225-1238), zero SIEMPRE en opts (1228); cardSurchargeEnabled ref (901) + loadCardSurchargeRule (903-908) + llamada al abrir (1736); boarding pass fuerza 'zero' (1617)

CobrosPage.vue: q-toggle zeroPrice (398-403); cardSurchargeEnabled ref (1262) + loadCardSurchargeRule (1617-1622) + onMounted (1658); zeroPrice resetea a false (844,1171) → payload zero:false (1486) queda correcto

types/subscription.ts: grantsAllPrograms en PlanListItem (117), payload create (150), payload update (169) → agregar programIds?: number[] en los 3
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Métodos zero en el composable + segundo toggle en ConfiguracionPreciosPage</name>
  <read_first>
    - el-templo-admin/src/composables/usePricingSettingsApi.ts (36-68 par card-surcharge)
    - el-templo-admin/src/pages/ConfiguracionPreciosPage.vue (84 líneas completas)
  </read_first>
  <action>
    En usePricingSettingsApi.ts duplicar `getCardSurchargeEnabled`/`setCardSurchargeEnabled` como `getZeroPriceEnabled`/`setZeroPriceEnabled` apuntando a `/admin/settings/pricing/zero-price` (mismo loading/error/extractError/cleanup). En ConfiguracionPreciosPage.vue agregar una segunda q-card-section idéntica a la de card-surcharge con su q-banner explicativo (copy: "Con la regla apagada, la opción de Precio Zero no se ofrece al cobrar ni al armar planes"), q-toggle con onToggle de revert optimista ante error, y carga en onMounted. Orden/agrupación de los dos toggles a discreción. Logging con createLogger, nunca console.
  </action>
  <verify>
    <automated>cd el-templo-admin && grep -q "zero-price" src/composables/usePricingSettingsApi.ts && grep -qi "zero" src/pages/ConfiguracionPreciosPage.vue && (npx vue-tsc --noEmit -p tsconfig.json 2>/dev/null || npx tsc --noEmit)</automated>
  </verify>
  <acceptance_criteria>
    - Composable expone get/set zero-price contra el endpoint de 156-01.
    - ConfiguracionPreciosPage muestra el toggle Zero con copy y revert optimista.
    - Type check verde.
  </acceptance_criteria>
  <done>Toggle Zero configurable desde /configuracion/precios.</done>
</task>

<task type="auto">
  <name>Task 2: PlanFormDialog — gate priceZero + multi-select programas + tipos</name>
  <read_first>
    - el-templo-admin/src/components/PlanFormDialog.vue (87-107 priceZero/Tarjeta; 178-196 q-select; 205 toggle; 308-320 loadRule; 359-366 programOptions; 456-463 watch; 477/479-499 open+edit; 535-562 payload)
    - el-templo-admin/src/types/subscription.ts (117/150/169)
    - el-templo-admin/src/config/templo-config.ts (flag TEMPLO_TRAINING_ROUTINES de 156-04)
  </read_first>
  <action>
    En types/subscription.ts agregar `programIds?: number[]` a PlanListItem (117), payload create (150) y payload update (169). En PlanFormDialog.vue: (a) gatear el campo priceZero con `v-if="zeroPriceEnabled"` (analog exacto de Tarjeta 98) + ref `zeroPriceEnabled` cargado con un `loadZeroPriceRule` conservador (OFF ante error) invocado al abrir; en onSubmit defaultear `priceZero` cuando el campo está oculto (usar `form.priceRegular` o 0, ya que el POST del API lo exige). (b) Agregar un q-select `multiple use-chips` de programas (base: el q-select de linkedProgramId + programOptions) bindeado a `form.programIds`, visible solo cuando `!form.grantsAllPrograms` Y la superficie de rutinas está ON (import `TEMPLO_TRAINING_ROUTINES` de templo-config); excluir Foundation de las opciones si `programOptions` lo incluye (coherencia con el filtro server-side de 156-03). Extender el watch de grantsAllPrograms (456-463) para limpiar también `form.programIds` al prender 'todos'. Poblar `form.programIds` en edit-mode desde `props.plan.programIds` y agregarlo al payload.
  </action>
  <verify>
    <automated>cd el-templo-admin && grep -q "programIds" src/types/subscription.ts && grep -q "programIds" src/components/PlanFormDialog.vue && grep -q "zeroPriceEnabled" src/components/PlanFormDialog.vue && (npx vue-tsc --noEmit -p tsconfig.json 2>/dev/null || npx tsc --noEmit)</automated>
  </verify>
  <acceptance_criteria>
    - Campo priceZero gateado por zeroPriceEnabled; onSubmit defaultea priceZero cuando está oculto.
    - Multi-select de programas visible solo con grantsAllPrograms OFF y superficie de rutinas ON; Foundation excluida de las opciones.
    - Watch de grantsAllPrograms limpia programIds al prender 'todos'.
    - Edit-mode puebla programIds desde props.plan; payload incluye programIds.
    - types/subscription.ts expone programIds en los 3 shapes.
    - Type check verde.
  </acceptance_criteria>
  <done>Form de planes con gate Zero y multi-select de programas cableado al payload.</done>
</task>

<task type="auto">
  <name>Task 3: Gate de la opción Zero en AssignPlanDialog + CobrosPage</name>
  <read_first>
    - el-templo-admin/src/components/AssignPlanDialog.vue (1225-1238 priceTypeOptions; 901/903-908/1736 carga flag; 1617 boarding pass)
    - el-templo-admin/src/pages/CobrosPage.vue (398-403 toggle zeroPrice; 1262/1617-1622/1658 carga flag; 844/1171 reset; 1486 payload)
  </read_first>
  <action>
    AssignPlanDialog.vue: agregar ref `zeroPriceEnabled` + `loadZeroPriceRule` (conservador OFF) invocado al abrir (analog 903-908/1736), y mover el push de `{ value: 'zero' }` en `priceTypeOptions` (1228) a un push condicional por `zeroPriceEnabled` (simétrico al de credit_card). El boarding pass (1617) queda como mecanismo independiente en la UI — el server ya lo normaliza (156-01), no hace falta tocarlo aquí. CobrosPage.vue: esconder el q-toggle zeroPrice (398-403) con `v-if="zeroPriceEnabled"` + ref/carga análogos (1262/1617-1622/1658). El payload `zero:false` (1486) queda correcto sin más cambios porque `zeroPrice` ya resetea a false. Fallback conservador OFF ante error; logging con createLogger.
  </action>
  <verify>
    <automated>cd el-templo-admin && grep -q "zeroPriceEnabled" src/components/AssignPlanDialog.vue && grep -q "zeroPriceEnabled" src/pages/CobrosPage.vue && (npx vue-tsc --noEmit -p tsconfig.json 2>/dev/null || npx tsc --noEmit)</automated>
  </verify>
  <acceptance_criteria>
    - AssignPlanDialog no ofrece la opción Zero cuando la regla está OFF.
    - CobrosPage esconde el toggle Zero cuando la regla está OFF (payload zero:false intacto).
    - Ambos cargan el flag con fallback conservador OFF.
    - Type check verde.
  </acceptance_criteria>
  <done>La opción/toggle Zero desaparece de la UI de cobro cuando la regla está apagada.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary         | Description                                              |
| ---------------- | -------------------------------------------------------- |
| admin UI → cobro | los controles Zero son conveniencia visual, no seguridad |

## STRIDE Threat Register

| Threat ID | Category               | Component                                       | Disposition | Mitigation Plan                                                                                                                     |
| --------- | ---------------------- | ----------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| T-156-11  | Spoofing               | UI esconde Zero pero el payload podría forzarlo | mitigate    | el gate real es server-side (resolvePriceType, 156-01); la UI solo esconde. Bypass por payload directo cerrado en 156-01 (T-156-02) |
| T-156-12  | Information Disclosure | multi-select expone programas gateados          | accept      | los programas visibles ya requieren superficie de rutinas ON + owner; sin dato sensible                                             |
| T-156-SC  | Tampering              | npm/pip/cargo installs                          | accept      | no se instalan paquetes nuevos                                                                                                      |

</threat_model>

<verification>
- Type check verde en el-templo-admin.
- Verificación visual (toggle Zero, gates, multi-select) → UAT de fase, no bloqueante aquí.
- El gate efectivo lo garantizan los tests de 156-01 (server-side); esta UI solo esconde.
</verification>

<success_criteria>
La UI admin refleja PLAN-02 (toggle Zero + esconder controles con regla OFF) y PLAN-03 (multi-select de programas cableado al payload), apoyada en el gate server-side de 156-01/156-02.
</success_criteria>

<output>
Create `.planning/phases/156-planes-de-pago-vs-rutinas-de-entrenamiento/156-05-SUMMARY.md` when done
</output>
