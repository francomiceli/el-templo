---
phase: 107-cobro-al-asignar-plan
plan: 03
type: execute
wave: 3
depends_on: [107-01, 107-02]
files_modified:
  - el-templo-api/test/subscriptions/charge-on-assign.test.ts
autonomous: true
requirements:
  - CHARGE-03
must_haves:
  truths:
    - "Test happy: assignPlan con amountReceived = pricePaid → balance row = 0"
    - "Test happy: assignPlan SIN amountReceived (backward compat) → idéntico al anterior"
    - "Test happy: assignPlan con amountReceived = pricePaid - 10000 → balance row positivo en 10000"
    - "Test happy: assignPlan con pricePaid = 0 (boarding pass) → no transaction, no balance row"
    - "Test sad: amountReceived > pricePaid → 400 con mensaje claro"
    - "Test sad: amountReceived < 0 → 400 (cubre schema-layer guard; service-layer guard es defense-in-depth)"
    - "Test atomicity: balanceService.applyDelta mockeado para tirar → ni subscription ni transaction persisten en DB (rollback)"
    - "Mismo set para changePlan modo now (proration / netAmount como cap) y modo after_current (pricePaid como cap)"
    - "Mismo set para renewSubscription (renewalPrice como cap)"
    - "Tests verifican notes flow-aware: regex /Cobro al (asignar|cambiar a|cambio programado a|renovar) plan/ matchea financial_transactions.notes según flow"
  artifacts:
    - path: "el-templo-api/test/subscriptions/charge-on-assign.test.ts"
      provides: "Integration tests covering D-17 matrix + D-11 atomicity + flow-aware notes"
      min_lines: 250
  key_links:
    - from: "test file"
      to: "assignPlan/changePlan/renewSubscription endpoints"
      via: "app.inject + assignPlan helper from _helpers.ts"
      pattern: "POST /subscriptions/.*/assign"
    - from: "atomicity test"
      to: "SubscriptionService directo + mocked BalanceService"
      via: "DI injection failingBalance"
      pattern: "applyDelta = async .* throw new Error"
---

<objective>
Crear suite de integration tests cubriendo el matrix completo de D-17 (happy paths + sad paths) y D-11 (atomicity rollback) para el cobro al asignar/cambiar/renovar plan. Tests corren contra MySQL real (`eltemplo_test_<POOL_ID>` per-worker). Cubre CHARGE-03 verification + valida que el refactor de Plan 02 cumple los invariantes de SPEC §7-§8.

Purpose: La verificación automatizada de CHARGE-03 (atomicidad). Sin este plan, no hay forma de saber si el refactor de Plan 02 realmente garantiza el rollback unificado. También valida que el `flowLabelMap` de Plan 02 produce las notes correctas para CajaPage.
Output: 1 archivo de test nuevo con cobertura completa, ejecutable como `pnpm --filter el-templo-api test charge-on-assign`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/107-cobro-al-asignar-plan/107-CONTEXT.md
@.planning/phases/107-cobro-al-asignar-plan/107-PATTERNS.md
@.planning/phases/107-01-SUMMARY.md
@.planning/phases/107-02-SUMMARY.md
@.planning/phases/105-modelo-de-datos-drop-del-viejo/105-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Test infrastructure existente -->

From el-templo-api/test/helpers.ts:

```ts
export async function createTestApp(): Promise<FastifyInstance>;
export async function cleanAllTestData(app: FastifyInstance): Promise<void>;
export async function createStaffUser(app, role): Promise<{ userId; token }>;
export async function getAuthToken(app, email, password): Promise<string>;
```

From el-templo-api/test/subscriptions/\_helpers.ts (line 74-94):

```ts
export async function assignPlan(
  app: FastifyInstance,
  adminToken: string,
  userId: number,
  overrides: Record<string, unknown> = {},
): Promise<{ statusCode: number; body: Record<string, unknown> }>;
```

El parámetro `overrides` ya pasa a través `amountReceived` sin necesidad de cambiar el helper.

From el-templo-api/src/modules/finance/balance-service.ts:

```ts
export class BalanceService {
  async applyDelta(tx: TxHandle, transaction, links, sign): Promise<void>;
}
```

From el-templo-api/src/modules/finance/transaction-service.ts (post Plan 01):

```ts
export class TransactionService {
  constructor(db, log, balanceService);
  async create(input, recordedBy, tx?): Promise<TransactionDetail>;
}
```

From el-templo-api/src/modules/subscriptions/service.ts (post Plan 02):

```ts
export class SubscriptionService {
  // Constructor signature exacta TBD por executor — leer service.ts antes de instanciar.
  // Posibles deps: db, log, auraSvc, transactionService?, bookingService?, settingsService?, ...
  async assignPlan(userId, input, adminId): Promise<...>;
  // recordAssignmentCharge es privado, no se invoca directo en tests
}
```

DB schema relevante (Phase 105):

- `schema.subscriptions` — userId, planId, branchId, status, ...
- `schema.financialTransactions` — memberId, kind, amount, notes, voidedAt, ...
- `schema.transactionLinks` — transactionId, targetKind, targetId, allocatedAmount
- `schema.balances` — memberId, targetKind, targetId, amount (signed; >0 = debe)
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear test suite charge-on-assign.test.ts (happy + sad paths para assignPlan)</name>
  <files>el-templo-api/test/subscriptions/charge-on-assign.test.ts</files>

  <behavior>
    - **Happy 1** — `assignPlan` con `amountReceived = pricePaid`: response 201, balance row para (memberId, subscription, subId) tiene `amount === 0`, financial_transactions.notes matchea `/Cobro al asignar plan/`.
    - **Happy 2** — `assignPlan` SIN `amountReceived` (campo omitido en body): response 201, balance row idéntico al Happy 1 (default a pricePaid).
    - **Happy 3** — `assignPlan` con `amountReceived = pricePaid - 10000`: response 201, balance row tiene `amount === 10000` (positivo = miembro debe).
    - **Happy 4** — `assignPlan` con plan de `pricePaid = 0` (boarding pass o priceTypeApplied='zero'): response 201, NO existe row en `financial_transactions` para ese member, NO existe row en `balances` para esa subscription.
    - **Sad 1** — `amountReceived = pricePaid + 1`: response 400, `body.message` matches `/no puede exceder/i`.
    - **Sad 2** — `amountReceived = -100`: response 400 (rechazado por JSON Schema layer `minimum: 0` antes de llegar al service). El service-layer guard del helper es defense-in-depth y queda unreachable desde HTTP — se documenta con comentario in-line; no se exercita por test runtime acá.
    - Subscription row se persiste en Happy paths; en Sad paths NO existe subscription row para ese user (en Sad 1 el throw del service ocurre dentro del db.transaction, antes del commit, por lo que rollback aplica; en Sad 2 el schema rechaza antes de invocar el service).
  </behavior>

<read_first> - el-templo-api/test/finance/transactions-api.test.ts (estructura de import + setup pattern, líneas 1-60) - el-templo-api/test/subscriptions/\_helpers.ts (assignPlan helper signature) - el-templo-api/test/helpers.ts (createTestApp, cleanAllTestData, createStaffUser signatures) - el-templo-api/test/finance/transaction-service.test.ts (patrón de assert sobre balance row, líneas 65-118) - el-templo-api/src/db/schema/index.ts (exports financialTransactions, transactionLinks, balances, subscriptions) - .planning/phases/107-cobro-al-asignar-plan/107-PATTERNS.md sección "charge-on-assign.test.ts (NEW integration test)" - .planning/phases/107-cobro-al-asignar-plan/107-CONTEXT.md decisión D-17
</read_first>

  <action>
Crear el archivo `el-templo-api/test/subscriptions/charge-on-assign.test.ts` con la siguiente estructura:

```ts
import { describe, it, expect, beforeAll, afterAll, beforeEach } from "vitest";
import { and, eq } from "drizzle-orm";
import type { FastifyInstance } from "fastify";

import {
  createTestApp,
  cleanAllTestData,
  createStaffUser,
  // ... cualquier otro helper necesario para crear branch/plan/member
} from "../helpers";
import { assignPlan as assignPlanHelper } from "./_helpers";
import * as schema from "../../src/db/schema";

describe("Phase 107 — Charge on assign", () => {
  let app: FastifyInstance;
  let adminToken: string;
  let adminId: number;
  let memberId: number;
  let branchId: number;
  let planId: number; // plan con pricePaid > 0 (ej. 100000)
  let zeroPlanId: number; // plan con pricePaid = 0 (boarding pass)

  beforeAll(async () => {
    app = await createTestApp();
  });

  afterAll(async () => {
    await app.close();
  });

  beforeEach(async () => {
    await cleanAllTestData(app);
    // Setup fixtures: admin, member, branch, plan (priceRegular = 100000), zeroPlan
    // ... (usar patrones existentes de _helpers o de transactions-api.test.ts)
    // Asegurar que adminId, memberId, branchId, planId, zeroPlanId quedan asignados
  });

  describe("assignPlan", () => {
    it("Happy 1 — amountReceived === pricePaid → balance = 0 + notes 'Cobro al asignar'", async () => {
      const res = await assignPlanHelper(app, adminToken, memberId, {
        planId,
        branchId,
        amountReceived: 100000, // explícito = pricePaid
      });
      expect(res.statusCode).toBe(201);

      const subId = (res.body as { subscriptionId: number }).subscriptionId;

      const [bal] = await app.db
        .select()
        .from(schema.balances)
        .where(
          and(
            eq(schema.balances.memberId, memberId),
            eq(schema.balances.targetKind, "subscription"),
            eq(schema.balances.targetId, subId),
          ),
        )
        .limit(1);
      expect(bal?.amount ?? 0).toBe(0);

      // Notes flow-aware (Plan 02 flowLabelMap): "Cobro al asignar plan X"
      const [tx] = await app.db
        .select()
        .from(schema.financialTransactions)
        .where(eq(schema.financialTransactions.memberId, memberId))
        .limit(1);
      expect(tx?.notes ?? "").toMatch(/Cobro al asignar plan/);
    });

    it("Happy 2 — amountReceived omitido → backward compat (default pricePaid) → balance = 0", async () => {
      const res = await assignPlanHelper(app, adminToken, memberId, {
        planId,
        branchId,
        // amountReceived no se pasa
      });
      expect(res.statusCode).toBe(201);

      const subId = (res.body as { subscriptionId: number }).subscriptionId;
      const [bal] = await app.db
        .select()
        .from(schema.balances)
        .where(
          and(
            eq(schema.balances.memberId, memberId),
            eq(schema.balances.targetKind, "subscription"),
            eq(schema.balances.targetId, subId),
          ),
        )
        .limit(1);
      expect(bal?.amount ?? 0).toBe(0);
    });

    it("Happy 3 — cobro parcial pricePaid - 10000 → balance row = 10000", async () => {
      const res = await assignPlanHelper(app, adminToken, memberId, {
        planId,
        branchId,
        amountReceived: 90000, // pricePaid 100000 - 10000
      });
      expect(res.statusCode).toBe(201);

      const subId = (res.body as { subscriptionId: number }).subscriptionId;
      const [bal] = await app.db
        .select()
        .from(schema.balances)
        .where(
          and(
            eq(schema.balances.memberId, memberId),
            eq(schema.balances.targetKind, "subscription"),
            eq(schema.balances.targetId, subId),
          ),
        )
        .limit(1);
      expect(bal?.amount).toBe(10000);
    });

    it("Happy 4 — pricePaid = 0 (boarding pass) → no transaction, no balance row", async () => {
      const res = await assignPlanHelper(app, adminToken, memberId, {
        planId: zeroPlanId,
        branchId,
        priceTypeApplied: "zero",
        // amountReceived no relevante porque chargeBase = 0
      });
      expect(res.statusCode).toBe(201);

      const txs = await app.db
        .select()
        .from(schema.financialTransactions)
        .where(eq(schema.financialTransactions.memberId, memberId));
      expect(txs).toHaveLength(0);

      const balances = await app.db
        .select()
        .from(schema.balances)
        .where(eq(schema.balances.memberId, memberId));
      expect(balances).toHaveLength(0);
    });

    it("Sad 1 — amountReceived > pricePaid → 400 con mensaje 'no puede exceder' + rollback", async () => {
      const res = await assignPlanHelper(app, adminToken, memberId, {
        planId,
        branchId,
        amountReceived: 100001, // pricePaid + 1
      });
      expect(res.statusCode).toBe(400);
      const body = res.body as { message?: string };
      expect(body.message ?? "").toMatch(/no puede exceder/i);

      // Rollback verificado: no subscription persistida (cap-violation throw dentro del db.transaction)
      const subs = await app.db
        .select()
        .from(schema.subscriptions)
        .where(eq(schema.subscriptions.userId, memberId));
      expect(subs).toHaveLength(0);
    });

    it("Sad 2 — amountReceived < 0 → 400 (rechazado por JSON Schema layer)", async () => {
      // NOTE (defense-in-depth): JSON Schema (`minimum: 0`) en assignPlanSchema rechaza amountReceived
      // negativo en el route boundary, antes de invocar SubscriptionService.assignPlan. Por lo tanto
      // el service-layer guard `if (amountReceived < 0) throw BadRequestError("...")` del helper
      // recordAssignmentCharge queda UNREACHABLE desde HTTP cuando el schema está montado — es
      // defense-in-depth puro (cubierto por typecheck + revisión manual, NO por este test runtime).
      // Para exercitar el guard service-layer directamente, ver alternativa en este mismo test
      // (instanciar SubscriptionService y llamar assignPlan con amountReceived: -1) — opcional,
      // no mandatorio porque el cap superior (Sad 1) ya prueba que el helper sí valida.
      const res = await assignPlanHelper(app, adminToken, memberId, {
        planId,
        branchId,
        amountReceived: -100,
      });
      expect(res.statusCode).toBe(400);

      // El subscription nunca se intentó crear porque el schema rechazó el body antes de invocar
      // el service — este assert es trivial pero valida la postcondición esperada (no orphans).
      const subs = await app.db
        .select()
        .from(schema.subscriptions)
        .where(eq(schema.subscriptions.userId, memberId));
      expect(subs).toHaveLength(0);
    });
  });
});
```

**Notas implementation:**

- Para los planes y branches, reusar helpers existentes de `_helpers.ts` o crear inline siguiendo patrones de `transactions-api.test.ts`. Si un plan default ya viene seedeado con `pricePaid = 100000`, usarlo. Si no, crear uno con `app.db.insert(schema.subscriptionPlans).values({...})`.
- Para `zeroPlanId`: crear un plan con `priceRegular = 0`, `priceCreditCard = 0`. O reusar el plan de boarding pass si existe. Verificar que `assignPlan` con `priceTypeApplied: "zero"` produce `pricePaid = 0` en el service layer.
- El `assignPlan` helper devuelve `body` parseado; el campo `subscriptionId` debe estar disponible (ver implementation actual de assignPlan response shape — si el campo se llama `id` o `subscriptionId` adaptar el cast).
- Si la response de assignPlan no incluye el `subscriptionId` directamente, query la subscription via `db.select().from(schema.subscriptions).where(eq(userId, memberId))` y tomar el último.
- Los assertions sobre `bal.amount` usan `??` para tolerar `undefined` cuando el row no existe (Happy 1 y 2 — sí existe, amount=0).
  </action>

    <verify>
      <automated>cd el-templo-api && pnpm test --run test/subscriptions/charge-on-assign.test.ts</automated>
    </verify>

  <acceptance_criteria> - Archivo `el-templo-api/test/subscriptions/charge-on-assign.test.ts` existe. - `cd el-templo-api && pnpm test --run test/subscriptions/charge-on-assign.test.ts` exits 0 con 6 tests describe("assignPlan") pasando (Happy 1-4 + Sad 1-2). - `grep -cE "expect\\(.*statusCode\\).toBe\\(400\\)" el-templo-api/test/subscriptions/charge-on-assign.test.ts` returns ≥2 (sad paths). - `grep -cE "expect\\(bal\\?\\.amount" el-templo-api/test/subscriptions/charge-on-assign.test.ts` returns ≥3 (happy 1, 2, 3 verifican balance row). - `grep -cE "no puede exceder" el-templo-api/test/subscriptions/charge-on-assign.test.ts` returns ≥1. - `grep -cE "Cobro al (asignar|cambiar a|cambio programado a|renovar) plan" el-templo-api/test/subscriptions/charge-on-assign.test.ts` returns ≥1 (notes flow-aware verificada al menos una vez). - `grep -cE "defense-in-depth|UNREACHABLE|JSON Schema layer" el-templo-api/test/subscriptions/charge-on-assign.test.ts` returns ≥1 (comentario explicativo del Sad 2 presente).
  </acceptance_criteria>

  <done>Suite happy + sad paths de assignPlan verde contra MySQL real, cubre D-17 + valida CHARGE-03 a nivel HTTP (rollback de subscription en sad-path, balance correcto en happy paths). Sad 2 documenta explícitamente la naturaleza defense-in-depth del guard service-layer.</done>
  </task>

<task type="auto" tdd="true">
  <name>Task 2: Atomicity test (mocked BalanceService failure) + changePlan/renewSubscription matrix</name>
  <files>el-templo-api/test/subscriptions/charge-on-assign.test.ts</files>

  <behavior>
    - **Atomicity** — instanciar SubscriptionService directo con un BalanceService mockeado cuyo `applyDelta` tira `Error("simulated balance failure")`. Llamar `subSvc.assignPlan(memberId, validInput, adminId)` → debe rejectar. Después: NO existe subscription para memberId, NO existe financial_transaction para memberId, NO existe balance row.
    - **changePlan/now** — happy + sad similares a assignPlan, pero `chargeBase = netAmount` (después de proration credit). Asume que el preview computa el netAmount; el test puede pre-computarlo manualmente sabiendo el plan actual + plan nuevo + tiempo restante. Notes esperado: `/Cobro al cambiar a plan/`.
    - **changePlan/after_current** — happy + sad con `chargeBase = pricePaid` del plan nuevo (sin proration). `startMode: "after_current"`. Notes esperado: `/Cobro al cambio programado a plan/`.
    - **renewSubscription** — happy + sad con `chargeBase = renewalPrice`. Notes esperado: `/Cobro al renovar plan/`.
    - Granularidad pragmática: para change-now, change-after-current y renew basta con (Happy — parcial → balance correcto + notes correcto) + (Sad — cap exceeded → 400 + rollback). NO repetir Happy 1/2/4 si ya están cubiertos por assignPlan (los validators son los mismos, viven en el helper compartido `recordAssignmentCharge`).
  </behavior>

<read_first>
**CRITICAL — leer estos archivos ANTES de escribir el atomicity test, en este orden:**

    1. **Constructor signature de SubscriptionService.** El executor DEBE leer las primeras ~80 líneas de `el-templo-api/src/modules/subscriptions/service.ts` para identificar:
       - Argumentos exactos del constructor (qué dependencias inyectadas: db, log, auraService, transactionService, bookingService, settingsService, etc.).
       - Si hay setters post-construcción (ej. `setBookingService(...)`, `setX(...)`) — el patrón de circular DI que el codebase usa cuando dos services se referencian.
       - El orden de los parámetros (positional vs object). Replicar EXACTAMENTE.

    2. **Tests existentes que instancian SubscriptionService directamente.** Buscar con:
       ```bash
       grep -rn "new SubscriptionService" el-templo-api/test/
       grep -rn "new SubscriptionService" el-templo-api/src/
       ```
       Si existe algún test con el patrón, copiarlo como base. Si NO existe ningún test directo (solo factories del app), entonces:
       - **Estrategia A (preferida si el patrón es complejo):** usar `app = await createTestApp()` y monkey-patch el balanceService EXISTENTE: `(app.balanceService as { applyDelta: typeof BalanceService.prototype.applyDelta }).applyDelta = async () => { throw new Error("simulated balance failure"); };` antes de llamar al endpoint. Verificar primero que `app.balanceService` está expuesto en el FastifyInstance (mirar `el-templo-api/src/app.ts` o `index.ts`).
       - **Estrategia B (si A no es viable):** instanciar manualmente con todos los DI args; usar `app.log` como logger y `app.db` como conexión.

    3. **Cómo se usa change/renew en tests existentes.** Buscar:
       ```bash
       grep -rn "changePlan\\|/change\\b" el-templo-api/test/subscriptions/
       grep -rn "renewSubscription\\|/renew" el-templo-api/test/subscriptions/
       ```
       Reusar URL paths exactos y body shapes; no inventar.

    Otros archivos:
    - el-templo-api/test/finance/transaction-service.test.ts (patrón de DI directo + mocked dependency, beforeAll setup)
    - el-templo-api/src/modules/subscriptions/service.ts (post Plan 02, especialmente recordAssignmentCharge — para entender qué argumentos espera el constructor de SubscriptionService Y cómo se invoca el flowLabelMap)
    - el-templo-api/src/modules/subscriptions/routes.ts (para ver cómo se instancia SubscriptionService normalmente — DI pattern de producción)
    - .planning/phases/107-cobro-al-asignar-plan/107-PATTERNS.md sección "Atomicity test (D-11) — pattern to copy"

</read_first>

  <action>
**A. Atomicity test (en el mismo archivo, dentro del describe principal o en un describe nuevo):**

Pseudo-skeleton (el executor adapta a la signature exacta encontrada en read_first):

```ts
describe("Atomicity (D-11)", () => {
  it("balanceService.applyDelta throws → subscription + transaction rollback", async () => {
    // ESTRATEGIA A — monkey-patch sobre app existente (preferida si app expone balanceService)
    const originalApplyDelta = app.balanceService.applyDelta.bind(
      app.balanceService,
    );
    app.balanceService.applyDelta = async () => {
      throw new Error("simulated balance failure");
    };

    try {
      const res = await assignPlanHelper(app, adminToken, memberId, {
        planId,
        branchId,
        amountReceived: 50000,
      });
      // Endpoint debe devolver 5xx (Internal) porque el throw NO es BadRequestError.
      expect(res.statusCode).toBeGreaterThanOrEqual(500);

      // Verifica rollback completo
      const subs = await app.db
        .select()
        .from(schema.subscriptions)
        .where(eq(schema.subscriptions.userId, memberId));
      expect(subs).toHaveLength(0);

      const txs = await app.db
        .select()
        .from(schema.financialTransactions)
        .where(eq(schema.financialTransactions.memberId, memberId));
      expect(txs).toHaveLength(0);

      const balances = await app.db
        .select()
        .from(schema.balances)
        .where(eq(schema.balances.memberId, memberId));
      expect(balances).toHaveLength(0);
    } finally {
      // Restaurar para no contaminar tests siguientes
      app.balanceService.applyDelta = originalApplyDelta;
    }

    // ALTERNATIVA — ESTRATEGIA B (si app NO expone balanceService, descomentar y adaptar):
    //
    // const { TransactionService } = await import("../../src/modules/finance/transaction-service");
    // const { BalanceService } = await import("../../src/modules/finance/balance-service");
    // const { SubscriptionService } = await import("../../src/modules/subscriptions/service");
    //
    // const failingBalance = new BalanceService(app.db, app.log);
    // failingBalance.applyDelta = async () => {
    //   throw new Error("simulated balance failure");
    // };
    // const txSvc = new TransactionService(app.db, app.log, failingBalance);
    //
    // // CRITICAL: signature exacta del constructor — leer service.ts primeras 80 líneas + grep
    // // por "new SubscriptionService" en src/ para encontrar el call site canónico.
    // // Si requiere bookingService / settingsService / auraService, instanciar mínimos o reusar
    // // los del app: const subSvc = new SubscriptionService(app.db, app.log, app.auraService, txSvc);
    // // Aplicar setters si los hay: subSvc.setBookingService?.(app.bookingService);
    //
    // await expect(
    //   subSvc.assignPlan(memberId, validInput, adminId),
    // ).rejects.toThrow(/simulated balance failure/);
  });
});
```

**Importante:** la elección entre A y B depende de lo que `read_first` revele. Si `app.balanceService` está expuesto y es el mismo singleton que usa SubscriptionService (camino normal del bootstrap), A es muchísimo más simple. Si no, B requiere replicar el DI exactamente.

**B. changePlan/now coverage (proration / netAmount):**

```ts
describe("changePlan (now / proration)", () => {
  it("Happy — amountReceived parcial → balance row con netAmount - amountReceived + notes 'cambiar a'", async () => {
    // 1. Asignar plan inicial (assignPlan happy con full payment).
    // 2. Calcular netAmount esperado:
    //    Alternativa pragmática: GET /change-plan-preview para obtener netAmount, luego POST /change
    //    con amountReceived = netAmount - 5000.
    // 3. Assertir balance row sobre la NUEVA subscriptionId con amount === 5000.
    // 4. Assertir financial_transactions.notes matches /Cobro al cambiar a plan/.
  });

  it("Sad — amountReceived > netAmount → 400 + rollback", async () => {
    // Mismo setup; POST /change con amountReceived = netAmount + 1 → 400.
    // Assertir que el plan original sigue activo (no se cambió) y NO hay financial_transaction nueva.
  });
});
```

Helper para change-plan: usar `app.inject` directamente con la URL exacta encontrada en `read_first` (paso 3). Verificar URL exacta en `el-templo-api/src/modules/subscriptions/routes.ts`.

**C. changePlan/after_current coverage (sin proration):**

OBLIGATORIO — sección con su propio `describe` y al menos 2 `it` (Happy + Sad). NO mezclar con change-now.

```ts
describe("changePlan (after_current / sin proration)", () => {
  it("Happy — startMode='after_current' con amountReceived parcial → balance row positivo + notes 'cambio programado a'", async () => {
    // 1. Asignar plan inicial (assignPlan happy con full payment).
    // 2. POST /change con body:
    //    {
    //      planId: nuevoPlanId,                // plan distinto, priceRegular = ej. 120000
    //      branchId,
    //      startDate: <fecha post-período-actual>,
    //      priceTypeApplied: "regular",
    //      paymentMethod: "cash",
    //      startMode: "after_current",
    //      amountReceived: 115000,             // priceNuevoPlan (120000) - 5000
    //    }
    // 3. Response 201 (o el statusCode que el endpoint devuelve para change). Capturar new subId.
    // 4. Assertir balance row sobre la nueva subscription:
    //    expect(bal?.amount).toBe(5000);
    // 5. Assertir notes:
    //    const [tx] = await app.db.select().from(schema.financialTransactions)
    //      .where(eq(schema.financialTransactions.memberId, memberId)).orderBy(desc(...)).limit(1);
    //    expect(tx?.notes ?? "").toMatch(/Cobro al cambio programado a plan/);
  });

  it("Sad — startMode='after_current' con amountReceived > pricePaid → 400 + rollback", async () => {
    // Mismo setup; POST /change con startMode: "after_current" + amountReceived: priceNuevoPlan + 1
    //   → 400 con /no puede exceder/i.
    // Assertir que NO se creó la nueva subscription (rollback) y NO hay financial_transaction nueva.
    // El plan original sigue activo, sin nueva financial_transaction asociada al cambio.
  });
});
```

**D. renewSubscription coverage:**

```ts
describe("renewSubscription", () => {
  it("Happy — amountReceived parcial → balance row positivo + notes 'renovar'", async () => {
    // 1. Asignar plan + cobrar full.
    // 2. Forzar la subscription a estado renewable (manipular endsAt con
    //    app.db.update(schema.subscriptions).set({ endsAt: <ayer> }).where(...) ).
    // 3. POST /renew con amountReceived = renewalPrice - 7000 → 201.
    // 4. Assertir balance row con amount === 7000 sobre la subscription renovada.
    // 5. Assertir financial_transactions.notes matches /Cobro al renovar plan/.
  });

  it("Sad — amountReceived > renewalPrice → 400 + rollback", async () => {
    // Mismo setup; POST /renew con amountReceived = renewalPrice + 1 → 400 con /no puede exceder/i.
    // Assertir que la subscription sigue en estado pre-renew (no se extendió).
  });
});
```

**E. Verificación final del archivo completo:**

`pnpm test --run test/subscriptions/charge-on-assign.test.ts` debe ejecutar todos los `describe` blocks y pasar todos los `it`. El total esperado: **≥11 tests** (assignPlan: 6 + atomicity: 1 + changePlan/now: 2 + changePlan/after_current: 2 + renewSubscription: 2 = 13 mínimo, ajustable según cobertura efectiva).

Si algún test de change/renew requiere fixtures complicadas (ej. plan que se vence en 7 días para forzar el state renewable), considerar manipular DB directamente con `app.db.update(schema.subscriptions).set({ endsAt: ... })` — no requiere endpoint.
</action>

  <verify>
    <automated>cd el-templo-api && pnpm test --run test/subscriptions/charge-on-assign.test.ts && pnpm test --run test/finance/ test/subscriptions/</automated>
  </verify>

<acceptance_criteria> - `cd el-templo-api && pnpm test --run test/subscriptions/charge-on-assign.test.ts` exits 0. - El test atomicity está presente: `grep -cE "simulated balance failure" el-templo-api/test/subscriptions/charge-on-assign.test.ts` returns ≥1. - El test atomicity verifica los 3 invariantes: `grep -cE "expect\\(subs\\)\\.toHaveLength\\(0\\)|expect\\(txs\\)\\.toHaveLength\\(0\\)|expect\\(balances\\)\\.toHaveLength\\(0\\)" el-templo-api/test/subscriptions/charge-on-assign.test.ts` returns ≥3. - El executor leyó la signature del SubscriptionService (constructor o monkey-patch sobre app.balanceService): `grep -cE "new SubscriptionService\\(|app\\.balanceService" el-templo-api/test/subscriptions/charge-on-assign.test.ts` returns ≥1. - Coverage explícita de change-now: `grep -cE "describe\\(\"changePlan \\(now" el-templo-api/test/subscriptions/charge-on-assign.test.ts` returns ≥1. - **Coverage explícita de change-after_current** (D-17 exige ambos modos): `grep -cE "after_current|after-current" el-templo-api/test/subscriptions/charge-on-assign.test.ts` returns ≥1. - **Coverage explícita de renewSubscription:** `grep -cE "renewSubscription|describe\\(\"renew" el-templo-api/test/subscriptions/charge-on-assign.test.ts` returns ≥1. - Notes flow-aware verificada para los 3 flows: `grep -cE "Cobro al (asignar|cambiar a|cambio programado a|renovar) plan" el-templo-api/test/subscriptions/charge-on-assign.test.ts` returns ≥3 (mínimo: assign + change + renew; idealmente 4 con change-after_current explícito). - Total tests ejecutados ≥11 (verificable por output de vitest). - Suite completa de regresión sigue verde: `cd el-templo-api && pnpm test --run test/finance/ test/subscriptions/` exits 0.
</acceptance_criteria>

  <done>
    - Atomicity verificada por test directo (mock applyDelta throws → no orphans en DB).
    - changePlan/now, changePlan/after_current, renewSubscription cubiertos con happy + sad mínimos.
    - Notes flow-aware verificada por regex contra el `notes` real persistido en financial_transactions.
    - Suite estable y reproducible contra MySQL real (eltemplo_test).
    - Cero regresión en suites pre-existentes.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                |
| --------------------------------- | ---------------------------------------------------------- |
| test → MySQL real (eltemplo_test) | Cada test seedea fixtures + cleanAllTestData en beforeEach |

## STRIDE Threat Register

| Threat ID | Category    | Component           | Disposition | Mitigation Plan                                                                                                                                                                                                                                                  |
| --------- | ----------- | ------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-107-10  | Tampering   | atomicity test      | mitigate    | El test mockea `applyDelta` antes de DI, garantizando que el bug "transactionService.create fuera del db.transaction" rompe el test (el rollback no aplicaría a la subscription si el callsite estuviera fuera de la tx). Esto es el contract test de CHARGE-03. |
| T-107-11  | Repudiation | test data isolation | accept      | Per-worker DB (eltemplo*test*<POOL_ID>) + cleanAllTestData en beforeEach asegura que tests no contaminan otros runs. Patrón existente de Phase 105/106.                                                                                                          |

</threat_model>

<verification>
1. Suite `charge-on-assign.test.ts` verde standalone: `pnpm test --run test/subscriptions/charge-on-assign.test.ts`.
2. Suite completa verde: `pnpm test --run test/`.
3. Inspección manual: el test atomicity falla deliberadamente si revertimos el refactor de Plan 02 (probar localmente extrayendo el `recordAssignmentCharge` fuera del `db.transaction` → test rojo confirma cobertura efectiva).
4. Inspección manual: las 4 notes esperadas (`Cobro al asignar/cambiar a/cambio programado a/renovar plan`) aparecen al menos una vez cada una en assertions.
</verification>

<success_criteria>

- CHARGE-03 verificado por automation: rollback unificado existe.
- D-17 matrix completo cubierto, incluyendo change-after_current explícito.
- Notes flow-aware (Plan 02 flowLabelMap) verificada para los 4 flows.
- Test suite ejecutable en CI sin flakiness (per-worker DB).
- Cero regresión en tests existentes de Phase 105/106.
  </success_criteria>

<output>
After completion, create `.planning/phases/107-cobro-al-asignar-plan/107-03-SUMMARY.md`
</output>
</content>
