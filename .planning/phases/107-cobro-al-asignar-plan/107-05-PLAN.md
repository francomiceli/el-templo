---
phase: 107-cobro-al-asignar-plan
plan: 05
type: execute
wave: 2
depends_on: [107-04]
files_modified:
  - el-templo-admin/src/components/AssignPlanDialog.vue
  - el-templo-admin/src/composables/useSubscriptionsApi.ts
autonomous: false
requirements:
  - CHARGE-01
  - CHARGE-02
must_haves:
  truths:
    - "El step Confirmar de AssignPlanDialog tiene un bloque 'Cobro' con q-input de monto recibido (default = chargeBase) + q-select de payment method"
    - "El q-select de paymentMethod ya NO existe en el step 2 (Configurar) — fue movido al bloque Cobro"
    - "Cuando amountReceived < chargeBase: banner amarillo bg-yellow-1 visible above-the-fold con mensaje en español incluyendo el monto pendiente formateado"
    - "Cuando amountReceived > chargeBase o amountReceived < 0: botón Confirmar disabled"
    - "Cuando chargeBase === 0 (boarding pass / override 0 / plan zero): bloque Cobro visible pero deshabilitado, leyenda 'Plan gratuito - sin cobro'"
    - "En mode='change' + startMode='now': el desglose Plan / Crédito proration / Neto a cobrar ya se muestra en el step Confirmar (existente, L298-371) y chargeBase = netAmount"
    - "Watch on chargeBase pre-llena amountReceived con chargeBase cuando el usuario entra al step Confirmar"
    - "Watch on props.modelValue resetea amountReceived al abrir el dialog"
    - "executeConfirm envía amountReceived al payload con la lógica chargeBase===0 ? undefined : amountReceived ?? undefined"
    - "useSubscriptionsApi.ts forwarding genérico verificado (sin cambio o cambio mínimo)"
  artifacts:
    - path: "el-templo-admin/src/components/AssignPlanDialog.vue"
      provides: "Bloque Cobro completo en step Confirmar + banner amarillo + disabled state + payload extension"
      contains: "amountReceived"
    - path: "el-templo-admin/src/composables/useSubscriptionsApi.ts"
      provides: "(verify-only) forwarding de amountReceived via input genérico"
  key_links:
    - from: "AssignPlanDialog amountReceived ref"
      to: "executeConfirm payload"
      via: "amountReceived: chargeBase === 0 ? undefined : (amountReceived.value ?? undefined)"
      pattern: "amountReceived:\\s*chargeBase"
    - from: "isPartialCharge computed"
      to: "yellow banner v-if"
      via: "bg-yellow-1 q-card"
      pattern: "bg-yellow-1"
    - from: "useSubscriptionsApi.assignPlan"
      to: "POST /admin/subscriptions/.../assign"
      via: "axios body forwarding (input genérico)"
      pattern: "subscription/assign"
---

<objective>
Refactorizar `AssignPlanDialog.vue` (~1079 LOC) implementando el bloque Cobro completo en el step Confirmar (D-01..D-07):

1. Mover el `q-select` de `paymentMethod` del step 2 (Configurar, líneas ~481-490) al nuevo bloque Cobro del step Confirmar.
2. Agregar `q-input` numérico de `amountReceived` con prefix="$", default = `chargeBase` (computed: `netAmount` para change/now, `finalPrice` el resto).
3. Preview en vivo del saldo pendiente (CHARGE-02).
4. Banner amarillo above-the-fold cuando cobro parcial (texto en español, monto formateado con `formatPrice`).
5. Botón Confirmar disabled cuando inválido (`> chargeBase` o `< 0`).
6. Bloque deshabilitado con leyenda "Plan gratuito - sin cobro" cuando `chargeBase === 0`.
7. En mode='change' + startMode='now': el desglose proration ya existe (L298-371) — sólo wire `chargeBase = netAmount`.
8. Watches para resetear amountReceived al abrir dialog y pre-llenar al entrar al step Confirmar.
9. Extender el payload de `executeConfirm` (L985-1008) con `amountReceived`.
10. Verificar que `useSubscriptionsApi.ts` no requiere cambios (forwarding genérico).

Plan marcado `autonomous: false` porque incluye un checkpoint humano de verificación visual al final (la única manera de validar branding/i18n/UX consistencia es ojo humano).

Purpose: CHARGE-01 (UI con sección Cobro al asignar/renovar) + CHARGE-02 (preview en vivo del saldo). Cara visible de toda la Phase 107.
Output: AssignPlanDialog actualizado, smoke checklist documentado para que el checkpoint humano pueda validar visualmente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/107-cobro-al-asignar-plan/107-CONTEXT.md
@.planning/phases/107-cobro-al-asignar-plan/107-PATTERNS.md
@.planning/phases/107-04-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Tipos frontend (Plan 04) y building blocks existentes en AssignPlanDialog.vue -->

From el-templo-admin/src/types/subscription.ts (post Plan 04):

```ts
export interface AssignPlanInput {
  // ... campos existentes ...
  amountReceived?: number;
}
export interface RenewSubscriptionInput {
  paymentMethod: PaymentMethod;
  amountReceived?: number;
}
```

From el-templo-admin/src/utils/format-price.ts:

```ts
export function formatPrice(amount: number, currency: string): string;
```

Ya importado en AssignPlanDialog.vue:555.

From el-templo-admin/src/types/transaction.ts:78:

```ts
export const PAYMENT_METHOD_OPTIONS = PAYMENT_METHOD_FILTER_OPTIONS;
```

Ya importado en AssignPlanDialog.vue:569 (junto con `type PaymentMethod`).

From el-templo-admin/src/utils/logger.ts:

```ts
export function createLogger(name: string): Logger;
```

Ya en uso: `const log = createLogger('AssignPlanDialog');` en línea ~573.

Existing computed `pricingDisplay` (AssignPlanDialog.vue:747-765) calcula `finalPrice` (override / AURA / boardingPass / plan price). Reusar.

Existing `changePlanPreviewData` (AssignPlanDialog.vue:611) expone `netAmount` para mode='change' (verificar shape — debería tener `netAmount`, `proration.remainingValue`, `currentPlan`, `allowed`, `reason`).

Existing q-input numérico patrón (líneas 158-168, priceOverrideAmount):

```vue
<q-input
  v-model.number="assignForm.priceOverrideAmount"
  label="Monto"
  type="number"
  dense
  outlined
  prefix="$"
/>
```

Existing q-select payment patrón (líneas 481-490, A MOVER):

```vue
<q-select
  v-model="assignForm.paymentMethod"
  :options="paymentMethodOptions"
  label="Metodo de pago *"
  dense
  outlined
  emit-value
  map-options
  class="q-mb-md"
/>
```

Existing yellow-bg analog: el bg-red-1 banner del L270-296 ("Cambio no permitido") — reaplicar con bg-yellow-1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor AssignPlanDialog.vue — bloque Cobro + banner + disabled + payload extension</name>
  <files>el-templo-admin/src/components/AssignPlanDialog.vue</files>

<read_first> - el-templo-admin/src/components/AssignPlanDialog.vue (archivo completo, especialmente: stepper structure, step 2 ~líneas 478-510, step Confirmar ~líneas 437-540, watch L1049-1078, executeConfirm L985-1008, pricingDisplay L747-765, changePlanPreviewData L611, imports L553-575) - el-templo-admin/src/types/subscription.ts (post Plan 04, AssignPlanInput.amountReceived) - el-templo-admin/src/utils/format-price.ts (signature de formatPrice) - .planning/phases/107-cobro-al-asignar-plan/107-PATTERNS.md sección "AssignPlanDialog.vue (Vue component, extend)" — todos los analogs y snippets - .planning/phases/107-cobro-al-asignar-plan/107-CONTEXT.md decisiones D-01..D-07, D-18
</read_first>

  <action>
**A. Refs y computeds nuevos en `<script setup>` (debajo de los refs existentes, cerca de L575).**

```ts
// Phase 107 - Cobro al asignar (D-01..D-07)
const amountReceived = ref<number | null>(null);

const chargeBase = computed<number>(() => {
  // mode='change' + startMode='now': proration neto
  if (
    props.mode === "change" &&
    startMode.value === "now" &&
    changePlanPreviewData.value?.netAmount != null
  ) {
    return changePlanPreviewData.value.netAmount;
  }
  // resto (assign / change-after_current): finalPrice
  return pricingDisplay.value.finalPrice ?? 0;
});

const isPartialCharge = computed<boolean>(
  () =>
    chargeBase.value > 0 &&
    amountReceived.value !== null &&
    amountReceived.value < chargeBase.value,
);

const pendingBalance = computed<number>(() =>
  Math.max(0, chargeBase.value - (amountReceived.value ?? 0)),
);

const isCobroInvalid = computed<boolean>(() => {
  if (chargeBase.value === 0) return false; // bloque deshabilitado, no bloquea Confirmar
  if (amountReceived.value === null) return true;
  if (amountReceived.value < 0) return true;
  if (amountReceived.value > chargeBase.value) return true;
  return false;
});
```

**B. Watches: reset y pre-fill de amountReceived.**

Localizar el `watch(() => props.modelValue, ...)` existente (~L1049-1078). Dentro del callback que resetea `assignForm`, agregar:

```ts
amountReceived.value = null;
```

Agregar un watch nuevo dedicado (debajo del existente):

```ts
// Pre-llenar amountReceived cuando el usuario entra al step Confirmar (D-02).
watch(
  [() => step.value, chargeBase],
  ([newStep, base]) => {
    if (newStep === confirmStep.value && amountReceived.value === null) {
      amountReceived.value = base;
    }
  },
  { immediate: false },
);
```

(`confirmStep` y `step` ya existen como ref/computed según PATTERNS — verificar nombre exacto al leer el archivo.)

**C. Eliminar el q-select de paymentMethod del step 2 (líneas 481-490 actuales).**

Cortar ese bloque entero (`<q-select v-model="assignForm.paymentMethod" .../>` y su contenedor de layout inmediato si solo aloja ese campo, sin tocar otros campos del step). El step 2 queda solo con plan/precio (priceTypeApplied, startDate, boardingPass, AURA, override) — D-01.

**D. Construir el bloque Cobro en el step Confirmar.**

Localizar el step Confirmar (~líneas 437-540, después del summary del precio y antes de `q-stepper-navigation`). Insertar el bloque Cobro **debajo del summary**, **antes** de los buttons de navegación. Estructura completa:

```vue
<!-- BLOQUE COBRO (Phase 107 D-01..D-07) -->
<q-card flat bordered class="q-mb-md">
  <q-card-section>
    <div class="text-subtitle1 text-weight-bold q-mb-md">Cobro</div>

    <!-- Plan gratuito: leyenda deshabilitada -->
    <div v-if="chargeBase === 0" class="bg-grey-2 q-pa-md rounded-borders q-mb-md">
      <div class="row items-center q-gutter-sm">
        <q-icon name="info" size="sm" />
        <div class="text-body2">Plan gratuito - sin cobro</div>
      </div>
    </div>

    <!-- Inputs (siempre visibles, deshabilitados si chargeBase=0) -->
    <div class="row q-col-gutter-md">
      <div class="col-12 col-sm-6">
        <q-input
          v-model.number="amountReceived"
          label="Monto recibido"
          type="number"
          dense
          outlined
          prefix="$"
          :max="chargeBase"
          :min="0"
          :disable="chargeBase === 0"
          hint="Por defecto se cobra el total. Modificá si el cobro es parcial."
        />
      </div>
      <div class="col-12 col-sm-6">
        <q-select
          v-model="assignForm.paymentMethod"
          :options="paymentMethodOptions"
          label="Metodo de pago *"
          dense
          outlined
          emit-value
          map-options
          :disable="chargeBase === 0"
        />
      </div>
    </div>

    <!-- Preview saldo pendiente (CHARGE-02) -->
    <div
      v-if="chargeBase > 0 && amountReceived !== null"
      class="q-mt-md text-body2"
    >
      <span class="text-weight-medium">Saldo pendiente:</span>
      {{ formatPrice(pendingBalance, displayCurrency) }}
    </div>
  </q-card-section>
</q-card>

<!-- Banner amarillo cobro parcial (D-03) -->
<q-card v-if="isPartialCharge" flat bordered class="q-mb-md bg-yellow-1">
  <q-card-section>
    <div class="row items-center q-gutter-sm q-mb-sm">
      <q-icon name="warning" color="warning" size="sm" />
      <div class="text-subtitle2 text-weight-bold">Cobro parcial</div>
    </div>
    <div class="text-body2">
      El plan se asigna con saldo pendiente. El miembro quedará como deudor por
      {{ formatPrice(pendingBalance, displayCurrency) }}.
    </div>
  </q-card-section>
</q-card>
```

**Importante sobre orden visual (above-the-fold):** colocar el bloque Cobro PRIMERO (admin tipea ahí) y el banner amarillo INMEDIATAMENTE después (visible sin scroll en pantallas estándar) — cumple D-03.

**E. Disable expression del botón Confirmar.**

Localizar la expresión actual (~L538-542):

```vue
:disable=" props.mode === 'change' && startMode === 'now' &&
changePlanPreviewData?.allowed === false "
```

Reemplazar por:

```vue
:disable=" (props.mode === 'change' && startMode === 'now' &&
changePlanPreviewData?.allowed === false) || isCobroInvalid "
```

**F. Extender executeConfirm con el payload field (~L985-1008).**

En la construcción del payload `AssignPlanInput`, agregar la nueva propiedad después de `notes` o `startMode`:

```ts
const payload: AssignPlanInput = {
  // ... campos existentes ...
  notes: assignForm.value.notes.trim() || undefined,
  startMode: props.mode === "change" ? startMode.value : undefined,
  amountReceived:
    chargeBase.value === 0 ? undefined : (amountReceived.value ?? undefined),
};
```

Si la función maneja también el caso change y/o renew con payloads distintos, replicar la lógica en cada branch.

**G. Verificar imports.**

- `formatPrice` ya importado en L555.
- `createLogger` ya importado en L553.
- `PAYMENT_METHOD_OPTIONS` y `PaymentMethod` ya importados en L569.
- `AssignPlanInput` ya importado (Plan 04 lo extendió, no requiere cambio en el import).
- `computed`, `ref`, `watch` ya importados.

**H. Restricciones de estilo.**

- NO usar `console.log/warn/error` (CLAUDE.md). Si se necesita logging, usar `log.info/warn/error`.
- NO usar `any` types (CLAUDE.md).
- Texto en **español** en todos los labels, hints, banners, leyendas.
- Reusar `formatPrice(amount, currency)` para todos los montos visibles.
- `displayCurrency` ya existe en el componente — usar la variable existente.
  </action>

    <verify>
      <automated>cd el-templo-admin && grep -c "amountReceived" src/components/AssignPlanDialog.vue && grep -c "bg-yellow-1" src/components/AssignPlanDialog.vue && grep -c "Plan gratuito - sin cobro" src/components/AssignPlanDialog.vue && grep -c "Cobro parcial" src/components/AssignPlanDialog.vue && pnpm typecheck && pnpm lint</automated>
    </verify>

  <acceptance_criteria> - `grep -c "amountReceived" el-templo-admin/src/components/AssignPlanDialog.vue` returns ≥5 (ref, watch, computeds, q-input v-model, payload field). - `grep -c "isPartialCharge\|pendingBalance\|chargeBase" el-templo-admin/src/components/AssignPlanDialog.vue` returns ≥6 (computed defs + usages en template y disable expr). - `grep -c "bg-yellow-1" el-templo-admin/src/components/AssignPlanDialog.vue` returns 1 (sólo el banner Cobro parcial). - `grep -c "Plan gratuito - sin cobro" el-templo-admin/src/components/AssignPlanDialog.vue` returns 1. - `grep -c "Cobro parcial" el-templo-admin/src/components/AssignPlanDialog.vue` returns 1. - El q-select de paymentMethod NO existe ya en el step 2 — verificable revisando que la única ocurrencia de `<q-select v-model="assignForm.paymentMethod"` está dentro del bloque Cobro del step Confirmar (manual visual). `grep -c "v-model=\"assignForm.paymentMethod\"" el-templo-admin/src/components/AssignPlanDialog.vue` returns 1. - `cd el-templo-admin && pnpm typecheck` exits 0. - `cd el-templo-admin && pnpm lint` exits 0.
  </acceptance_criteria>

  <done>AssignPlanDialog refactorizado con bloque Cobro funcional en step Confirmar; default, banner, disabled, leyenda y payload extension implementados; sin regresión de typecheck/lint.</done>
  </task>

<task type="auto">
  <name>Task 2: Verify-only useSubscriptionsApi.ts forward genérico</name>
  <files>el-templo-admin/src/composables/useSubscriptionsApi.ts</files>

<read_first> - el-templo-admin/src/composables/useSubscriptionsApi.ts (líneas 169-184 assignPlan; 186-220 changePlan; 240-260 renewSubscription) - .planning/phases/107-cobro-al-asignar-plan/107-PATTERNS.md sección "useSubscriptionsApi.ts"
</read_first>

  <action>
Inspeccionar `useSubscriptionsApi.ts`:
- `assignPlan(userId, input: AssignPlanInput)` debe hacer `api.post(url, input)` con el input completo (forwarding genérico) — ver L169-184.
- `changePlan(userId, input: AssignPlanInput | ChangePlanInput)` ídem — ver L186-220.
- `renewSubscription(userId, input: RenewSubscriptionInput)` ídem — ver L240-260.

**Si los 3 métodos forward el `input` completo al body de axios** (sin desestructurar campos), NO requiere cambio. El nuevo `amountReceived` flue automáticamente porque está tipado en `AssignPlanInput` (Plan 04) y `RenewSubscriptionInput` (Plan 04).

**Si algún método desestructura campos** (ej. `api.post(url, { planId: input.planId, ...})`), agregar `amountReceived: input.amountReceived` al objeto literal. Eliminar la desestructuración si no agrega valor (mejor forwarding genérico).

Ejecutar typecheck para confirmar que el campo nuevo no introduce errores.

**Importante:** NO renombrar funciones, NO cambiar signatures de retorno, NO romper callers existentes.
</action>

  <verify>
    <automated>cd el-templo-admin && pnpm typecheck && pnpm lint</automated>
  </verify>

<acceptance_criteria> - `cd el-templo-admin && pnpm typecheck` exits 0. - `cd el-templo-admin && pnpm lint` exits 0. - Si NO se modificó el archivo: agregar nota en SUMMARY confirmando que el forwarding es genérico y no requiere cambio (verify-only). - Si SÍ se modificó: el cambio es mínimo (1-3 líneas agregando `amountReceived: input.amountReceived` al body literal) y no rompe consumidores.
</acceptance_criteria>

<done>El composable está listo para mandar `amountReceived` en el payload de assign/change/renew sin cambios estructurales.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Smoke visual del bloque Cobro en staging admin</name>

<files>n/a (checkpoint task)</files>
<action>Pause for human verification per how-to-verify steps.</action>
<verify>Human approval (resume-signal).</verify>
<done>User types "approved" or describes issues.</done>
<what-built>
Bloque "Cobro" en el step Confirmar de AssignPlanDialog con:

- q-input "Monto recibido" (default = chargeBase)
- q-select "Método de pago" (movido desde step 2)
- Preview en vivo del saldo pendiente
- Banner amarillo "Cobro parcial" cuando amountReceived < chargeBase
- Bloque deshabilitado con "Plan gratuito - sin cobro" cuando chargeBase = 0
- Botón Confirmar disabled cuando inválido
  </what-built>

  <how-to-verify>

1. **Build local del admin app:**
   - `cd el-templo-admin && pnpm dev` (o el comando dev habitual del proyecto)
   - Abrir admin en localhost (URL típica: http://localhost:9000 o el puerto configurado)
   - Loguearse como admin/owner contra DB local o staging.

2. **Verificación 1 — Asignar con cobro completo (default backward compat):**
   - Ir a AlumnosPage → seleccionar miembro → "Gestionar Plan" → "Asignar plan"
   - Step 1: elegir plan con `priceRegular > 0` (ej. cualquier plan presencial)
   - Step 2: confirmar precio (sin override). Confirmar que **YA NO** está el q-select de Método de pago acá.
   - Step Confirmar: aparece el bloque "Cobro" con monto pre-llenado = finalPrice. Cambiar payment method a Efectivo. Click Confirmar.
   - **Esperado:** asignación exitosa, saldo pendiente = $0 visible momentáneamente, alumno NO aparece como deudor.

3. **Verificación 2 — Asignar con cobro parcial:**
   - Mismo flujo. En step Confirmar, modificar "Monto recibido" a un valor menor (ej. 50% del finalPrice).
   - **Esperado:** debajo del bloque Cobro aparece banner amarillo "Cobro parcial" con texto "El plan se asigna con saldo pendiente. El miembro quedará como deudor por $X ARS." (o equivalente con la moneda del plan).
   - "Saldo pendiente:" arriba del banner muestra el mismo monto.
   - Click Confirmar → asignación exitosa.
   - Volver a AlumnosPage → el alumno aparece con saldo pendiente reflejado en el banner / columna de saldo (según UI Phase 105).

4. **Verificación 3 — Cambio con proration (mode='change' + startMode='now'):**
   - Asignar primero un plan A. Luego "Cambiar plan" a un plan B con startMode='now'.
   - **Esperado:** el step Confirmar muestra desglose Plan / Crédito proration / Neto a cobrar (esto YA existía pre-Phase-107).
   - El "Monto recibido" del bloque Cobro pre-llena con el **netAmount** (no el priceRegular del plan B).
   - Modificar a un valor menor → banner amarillo aparece, saldo pendiente refleja `netAmount - amountReceived`.

5. **Verificación 4 — Boarding pass (chargeBase = 0):**
   - Asignar plan con boarding pass marcado, o con `priceTypeApplied='zero'`, o con override = 0.
   - **Esperado:** el bloque Cobro se ve, pero los inputs (monto + método) están grises/deshabilitados. Aparece la leyenda "Plan gratuito - sin cobro".
   - Botón Confirmar habilitado. Click → asignación exitosa, NO se crea financial_transaction (ver verificación 5 de Plan 06).

6. **Verificación 5 — Inválido (cap-exceeded):**
   - Asignar plan. En step Confirmar, modificar "Monto recibido" a un valor MAYOR que chargeBase.
   - **Esperado:** botón Confirmar disabled. No es posible avanzar.

7. **Verificación 6 — Texto en español:**
   - Confirmar que TODOS los labels, hints, banners y leyendas están en español. Sin "Amount received", "Cobro partial", etc.

**Buscar regresiones:** verificar que el flujo de cambio de plan after_current sigue funcionando (sin proration), que renewSubscription sigue funcionando (si se gatilla desde otro componente, no necesariamente desde AssignPlanDialog), y que el step 2 (Configurar) no rompe por la eliminación del q-select de paymentMethod.
</how-to-verify>

<resume-signal>Type "approved" si las 6 verificaciones pasan, o describir cualquier issue (ej. "el banner aparece dos veces", "el monto pre-llenado es 0 en lugar de finalPrice", "boarding pass muestra inputs habilitados").</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                                                 |
| --------------------------- | --------------------------------------------------------------------------- |
| admin user → frontend → API | El admin manipula `amountReceived` en el q-input antes de enviar al backend |

## STRIDE Threat Register

| Threat ID | Category               | Component                                | Disposition | Mitigation Plan                                                                                                               |
| --------- | ---------------------- | ---------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------- |
| T-107-13  | Tampering              | client-side validation only              | mitigate    | Frontend valida cap pero **no es la única defensa** — backend (Plan 02) revalida y devuelve 400. T-107-04 cubre el otro lado. |
| T-107-14  | Information Disclosure | banner amarillo expone "saldo pendiente" | accept      | Es info que el admin necesita ver para decidir cobrar parcial. Sin leak hacia el miembro (admin app, RBAC enforced).          |
| T-107-15  | Repudiation            | logger frontend                          | accept      | log existente (createLogger) ya en uso en errors. Sin necesidad de tracking adicional en este plan.                           |

</threat_model>

<verification>
1. `cd el-templo-admin && pnpm typecheck` exits 0.
2. `cd el-templo-admin && pnpm lint` exits 0.
3. Smoke checkpoint humano (Task 3) approved → confirmación visual de los 6 escenarios.
</verification>

<success_criteria>

- CHARGE-01 frontend cubierto: bloque Cobro funcional en step Confirmar.
- CHARGE-02 cubierto: preview en vivo del saldo + banner amarillo para cobro parcial.
- Backward compat visual: assignar/cambiar/renovar full payment es 1 click más simple que antes (paymentMethod ya no salta de step).
- UX: bloque deshabilitado cuando boarding pass / chargeBase=0 con leyenda clara.
- Disable robusto del Confirmar para cap-violations.
- Smoke approved por humano (autonomous: false intencional).
  </success_criteria>

<output>
After completion, create `.planning/phases/107-cobro-al-asignar-plan/107-05-SUMMARY.md`
</output>
