---
phase: 107-cobro-al-asignar-plan
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-admin/src/types/subscription.ts
autonomous: true
requirements:
  - CHARGE-01
must_haves:
  truths:
    - "AssignPlanInput tiene campo opcional amountReceived?: number"
    - "RenewSubscriptionInput tiene campo opcional amountReceived?: number"
    - "ChangePlanInput (si existe como tipo separado) o el alias de AssignPlanInput tiene el campo"
    - "Comentario JSDoc en español documenta semántica backward-compat"
  artifacts:
    - path: "el-templo-admin/src/types/subscription.ts"
      provides: "Type contracts del payload de assign/change/renew con amountReceived opcional"
      contains: "amountReceived?: number"
  key_links:
    - from: "el-templo-admin/src/types/subscription.ts"
      to: "el-templo-admin/src/components/AssignPlanDialog.vue (Plan 05 consumer)"
      via: "import type { AssignPlanInput }"
      pattern: "amountReceived\\?: number"
---

<objective>
Extender los tipos del frontend admin con `amountReceived?: number` para que `AssignPlanDialog.vue` (Plan 05) y futuros consumers (CajaPage, MemberFormDialog si se reusa) puedan tipar el payload sin `any`.

Este plan está en Wave 1 porque sólo agrega campos opcionales al type — es totalmente independiente del backend (Plan 01/02). Permite que Plan 05 (UI) corra en paralelo a Plan 02 (backend) si la planificación de ejecución lo permite.

Purpose: Habilitar TypeScript-safe payload extension en frontend (CLAUDE.md no `any` rule). Prepara el consumo del nuevo campo por la UI en Plan 05.
Output: Types actualizados, `pnpm typecheck` verde sin nuevos errores.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/107-cobro-al-asignar-plan/107-CONTEXT.md
@.planning/phases/107-cobro-al-asignar-plan/107-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Tipos actuales en frontend admin (espejo del backend) -->

From el-templo-admin/src/types/subscription.ts (line 228-241 actual):

```ts
export interface AssignPlanInput {
  planId: number;
  branchId: number;
  startDate: string;
  priceTypeApplied: PriceType;
  paymentMethod: PaymentMethod;
  scheduleIds?: number[];
  auraSpend?: number;
  priceOverrideAmount?: number;
  priceOverrideReason?: string;
  boardingPass?: boolean;
  notes?: string;
  startMode?: "now" | "after_current";
}

export interface RenewSubscriptionInput {
  paymentMethod: PaymentMethod;
}
```

Verificar también si existe `ChangePlanInput` separado o si reusa `AssignPlanInput` (en el backend reusa; en frontend hay que confirmar leyendo el archivo).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar amountReceived a AssignPlanInput, RenewSubscriptionInput, ChangePlanInput (si existe)</name>
  <files>el-templo-admin/src/types/subscription.ts</files>

<read_first> - el-templo-admin/src/types/subscription.ts (estado completo — buscar AssignPlanInput, RenewSubscriptionInput, y eventual ChangePlanInput) - .planning/phases/107-cobro-al-asignar-plan/107-PATTERNS.md sección "el-templo-admin/src/types/subscription.ts (type extension)" - .planning/phases/107-cobro-al-asignar-plan/107-CONTEXT.md decisión D-12 + D-13
</read_first>

  <action>
**A. Agregar campo a AssignPlanInput.**

Después de `startMode?: 'now' | 'after_current';` (línea 240 actual), insertar:

```ts
  /**
   * Monto efectivamente recibido al asignar plan (Phase 107 D-12, D-13).
   * Backward-compat: undefined → backend defaults to pricePaid.
   * Validación frontend: 0 <= amountReceived <= chargeBase (boton Confirmar
   * deshabilitado si excede). Backend revalida y devuelve 400 en cap-violation.
   */
  amountReceived?: number;
```

**B. Agregar campo a RenewSubscriptionInput.**

Después de `paymentMethod: PaymentMethod;`, insertar:

```ts
  /** Cobro al renovar (Phase 107). Backward-compat: undefined → renewalPrice. */
  amountReceived?: number;
```

**C. Si existe `ChangePlanInput` como tipo separado, agregar el mismo campo.**

Buscar en el archivo: `grep -E "^export interface ChangePlanInput" el-templo-admin/src/types/subscription.ts`. Si existe, agregar `amountReceived?: number;` con el mismo JSDoc del A. Si NO existe, ChangePlanInput es alias de AssignPlanInput o se construye en el callsite — sin acción adicional.

**Restricciones:**

- NO renombrar campos existentes.
- NO marcar amountReceived como required (es opcional para backward compat).
- NO agregar otras interfaces (PaymentMethod, PriceType, etc. ya están definidas; reusar imports existentes).
  </action>

    <verify>
      <automated>cd el-templo-admin && grep -cE "amountReceived\?: number" src/types/subscription.ts | grep -E "^[2-9]$|^1[0-9]$" && pnpm typecheck</automated>
    </verify>

  <acceptance_criteria> - `grep -cE "amountReceived\?: number" el-templo-admin/src/types/subscription.ts` returns ≥2 (AssignPlanInput + RenewSubscriptionInput; potencialmente 3 si existe ChangePlanInput separado). - `cd el-templo-admin && pnpm typecheck` exits 0 (sin errores nuevos). - El campo es opcional (`?:`) — `grep -E "amountReceived: number" src/types/subscription.ts` (sin `?`) returns 0 matches.
  </acceptance_criteria>

  <done>Frontend types listos para que Plan 05 (AssignPlanDialog.vue) consuma `amountReceived` con tipado completo, sin `any`.</done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description |
| ---------------------------------------- | ----------- |
| n/a (sólo type definitions, sin runtime) | —           |

## STRIDE Threat Register

| Threat ID | Category  | Component           | Disposition | Mitigation Plan                                                                                                                                                    |
| --------- | --------- | ------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-107-12  | Tampering | type contract drift | mitigate    | El campo en frontend es estructuralmente idéntico al backend (`amountReceived?: number`). JSDoc cita la decisión D-12/D-13 para evitar drift en futuros refactors. |

</threat_model>

<verification>
1. `cd el-templo-admin && pnpm typecheck` exits 0.
2. `cd el-templo-admin && pnpm lint` exits 0.
3. Plan 05 podrá importar `AssignPlanInput` y asignar `amountReceived` sin TS error.
</verification>

<success_criteria>

- Tipos frontend espejo del backend.
- Cero ruido en typecheck/lint.
- Independencia total del backend (este plan corre antes de Plan 01/02 si la ejecución es paralela).
  </success_criteria>

<output>
After completion, create `.planning/phases/107-cobro-al-asignar-plan/107-04-SUMMARY.md`
</output>
