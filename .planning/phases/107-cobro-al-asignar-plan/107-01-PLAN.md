---
phase: 107-cobro-al-asignar-plan
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/finance/transaction-service.ts
  - el-templo-api/src/modules/finance/balance-service.ts
autonomous: true
requirements:
  - CHARGE-03
must_haves:
  truths:
    - "TransactionService.create() acepta parámetro opcional tx?: TxHandle"
    - "Cuando se pasa tx, todas las queries internas (financialTransactions insert, transactionLinks insert, balanceService.applyDelta) corren contra esa conexión"
    - "Cuando NO se pasa tx, abre db.transaction propia (backward compat — el endpoint REST POST /api/admin/transactions sigue funcionando idéntico)"
    - "TxHandle exportado desde balance-service.ts (o duplicado en transaction-service.ts) y reutilizable por subscriptions/service.ts"
  artifacts:
    - path: "el-templo-api/src/modules/finance/transaction-service.ts"
      provides: "create() con tx? opcional via runner pattern"
      contains: "tx?: TxHandle"
    - path: "el-templo-api/src/modules/finance/balance-service.ts"
      provides: "TxHandle export (si no estaba)"
      contains: "export type TxHandle"
  key_links:
    - from: "transaction-service.ts create()"
      to: "balanceService.applyDelta"
      via: "txHandle pass-through"
      pattern: "applyDelta\\(txHandle"
    - from: "transaction-service.ts create()"
      to: "this.db.transaction"
      via: "fallback runner cuando tx undefined"
      pattern: "this\\.db\\.transaction"
---

<objective>
Refactorizar `TransactionService.create()` para aceptar parámetro opcional `tx?: TxHandle`. Cuando se provee, reusa la conexión externa (permitiendo que `subscriptions/service.ts` envuelva el create en su propia `db.transaction` para atomicidad real). Cuando NO se provee, abre una `db.transaction` propia (preserva el comportamiento actual del endpoint REST `POST /api/admin/transactions`).

Verificar que `BalanceService.applyDelta` ya acepta `tx` (lo hace — Phase 105 lo dejó así). Exportar `TxHandle` desde `balance-service.ts` como tipo público para que `transaction-service.ts` y, en Plan 02, `subscriptions/service.ts` puedan importarlo.

Purpose: Habilitar atomicidad real (D-09 / CHARGE-03) sin romper el endpoint REST existente. Es la fundación backend del refactor de Plan 02.
Output: TransactionService refactorizado, todos los tests existentes (el-templo-api/test/finance/\*) verdes con cero cambios en sus archivos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/107-cobro-al-asignar-plan/107-CONTEXT.md
@.planning/phases/107-cobro-al-asignar-plan/107-PATTERNS.md
@.planning/phases/105-modelo-de-datos-drop-del-viejo/105-SPEC.md
@CLAUDE.md

<interfaces>
<!-- TxHandle ya existe en balance-service.ts:32-46. Patrón canónico a exportar. -->
<!-- Drizzle tx-handle pattern (project-wide) -->

From el-templo-api/src/modules/finance/balance-service.ts (current — line 32-46):

```ts
type DbInstance = MySql2Database<typeof schema>;

/**
 * The Drizzle `tx` handle passed to the `db.transaction(async (tx) => ...)`
 * callback. Pragmatic typing: same shape as the parent `MySql2Database`,
 * scoped to the running transaction.
 */
type TxHandle = Parameters<Parameters<DbInstance["transaction"]>[0]>[0];
```

From el-templo-api/src/modules/finance/balance-service.ts (current — line 67-71):

```ts
async applyDelta(
  tx: TxHandle,
  transaction: FinancialTransactionRow,
  links: TransactionLinkRow[],
  sign: 1 | -1,
): Promise<void>
```

applyDelta YA acepta tx requerido. No requiere cambio de signature, solo asegurar que TxHandle sea exportable.

From el-templo-api/src/modules/finance/transaction-service.ts (current — line 66-69):

```ts
async create(
  input: CreateTransactionInput,
  recordedBy: number,
): Promise<TransactionDetail>
```

Body actual abre `await this.db.transaction(async (tx) => { ... })` internamente (líneas 70-202).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export TxHandle desde balance-service.ts</name>
  <files>el-templo-api/src/modules/finance/balance-service.ts</files>

<read_first> - el-templo-api/src/modules/finance/balance-service.ts (estado actual completo, especialmente líneas 32-46 y 67-71) - .planning/phases/107-cobro-al-asignar-plan/107-PATTERNS.md sección "balance-service.ts (service, refactor — minimal)"
</read_first>

  <action>
Cambiar la declaración del tipo `TxHandle` de privado a `export`. La línea actual:
```ts
type TxHandle = Parameters<Parameters<DbInstance["transaction"]>[0]>[0];
```
debe quedar:
```ts
export type TxHandle = Parameters<Parameters<DbInstance["transaction"]>[0]>[0];
```

NO modificar el body de `applyDelta` ni ninguna otra función. La signature `applyDelta(tx: TxHandle, ...)` ya es la correcta para Phase 107 (Plan 02 le pasará el `tx` desde `subscriptions/service.ts` via `transactionService.create`).

Agregar un comentario JSDoc de invariante encima de `applyDelta` (línea 67) defendiendo que **todas** las queries (`tx.insert(schema.balances)` L147 y `tx.update(schema.balances)` L100) DEBEN seguir usando el `tx` recibido — nunca `this.db`. Si un futuro refactor accidentalmente swappea, atomicidad rompe silenciosamente.

Texto exacto del comentario:

```ts
/**
 * Mantiene atómicamente la cache `balances` aplicando el delta de una transacción.
 *
 * INVARIANTE (no romper): todas las queries del body usan el `tx` recibido,
 * nunca `this.db`. Si un caller envuelve `applyDelta` en una `db.transaction`
 * externa (ej. `subscriptions/service.ts` en Phase 107), `tx` ES la conexión
 * externa y `this.db` rompería el rollback unificado. Ver SPEC §7-§8.
 */
```

  </action>

  <verify>
    <automated>cd el-templo-api && grep -E "^export type TxHandle" src/modules/finance/balance-service.ts && grep -c "this\.db\.(insert|update|delete|select)" src/modules/finance/balance-service.ts | grep -E "^0$" && pnpm typecheck</automated>
  </verify>

<acceptance_criteria> - `grep -E "^export type TxHandle" el-templo-api/src/modules/finance/balance-service.ts` matches exactly 1 line. - `grep -c "this\.db\.(insert|update|delete|select)" el-templo-api/src/modules/finance/balance-service.ts` returns `0` (todas las queries usan tx). - `cd el-templo-api && pnpm typecheck` exits 0.
</acceptance_criteria>

<done>TxHandle es importable desde otros archivos del módulo finance. Comentario de invariante presente.</done>
</task>

<task type="auto">
  <name>Task 2: TransactionService.create acepta tx? opcional con runner pattern</name>
  <files>el-templo-api/src/modules/finance/transaction-service.ts</files>

<read_first> - el-templo-api/src/modules/finance/transaction-service.ts (estado actual completo, especialmente create() líneas 66-202) - el-templo-api/src/modules/finance/balance-service.ts (post-Task 1, para confirmar export TxHandle) - .planning/phases/107-cobro-al-asignar-plan/107-PATTERNS.md sección "transaction-service.ts" (snippet del runner pattern, líneas 47-63 del PATTERNS doc) - .planning/phases/105-modelo-de-datos-drop-del-viejo/105-SPEC.md §7-§8 (atomicity invariants TXN-05/06/07)
</read_first>

  <action>
Importar `TxHandle` al tope del archivo:
```ts
import { BalanceService, type TxHandle } from "./balance-service";
```
(Si ya importa BalanceService, agregar `type TxHandle` al import existente. Si no, agregar import nuevo. NO duplicar el tipo localmente — single source of truth en balance-service.ts.)

Cambiar la signature de `create()` (línea 66-69 actual):

```ts
async create(
  input: CreateTransactionInput,
  recordedBy: number,
  tx?: TxHandle,            // ← NEW (D-09)
): Promise<TransactionDetail> {
```

Reemplazar la apertura del `await this.db.transaction(async (tx) => { ... })` (línea 70 actual) por el runner pattern. Patrón exacto del PATTERNS doc (líneas 47-63):

```ts
async create(
  input: CreateTransactionInput,
  recordedBy: number,
  tx?: TxHandle,
): Promise<TransactionDetail> {
  const runner = tx
    ? <T,>(cb: (h: TxHandle) => Promise<T>) => cb(tx)
    : <T,>(cb: (h: TxHandle) => Promise<T>) => this.db.transaction(cb);

  return await runner<TransactionDetail>(async (txHandle) => {
    // ... entire existing body of create() — every reference to the
    // previous local `tx` is renamed to `txHandle` ...
  });
}
```

Renombrar **todas** las referencias a la variable local `tx` dentro del body (que viene del callback del `db.transaction`) a `txHandle`. Importante: NO confundir con el parámetro `tx` (que es el opcional externo). Después del refactor el body ya no referencia el parámetro `tx` directamente — solo `txHandle` (el resuelto por el runner).

Mantener intactos:

- TODOS los validations (Σ allocated_amount, member exists, branch exists, target_id exists per target_kind) — siguen DENTRO del runner.
- La llamada `this.balanceService.applyDelta(txHandle, ...)` (línea 187 actual) — debe seguir pasando `txHandle`, nunca el parámetro externo `tx`.
- El log estructurado de éxito (líneas 189-199) — sigue DENTRO del runner, antes del `return`.
- El return value y la shape de TransactionDetail.

Backward-compat: callers del REST endpoint (`finance/routes.ts:119`) no pasan `tx`, así que el runner cae al `this.db.transaction(cb)` y el comportamiento es **idéntico** al pre-refactor.
</action>

  <verify>
    <automated>cd el-templo-api && grep -E "tx\?: TxHandle" src/modules/finance/transaction-service.ts && grep -E "const runner = tx" src/modules/finance/transaction-service.ts && pnpm typecheck && pnpm test --run test/finance/transaction-service.test.ts test/finance/transactions-api.test.ts</automated>
  </verify>

<acceptance_criteria> - `grep -E "tx\?: TxHandle" el-templo-api/src/modules/finance/transaction-service.ts` returns ≥1 match (signature). - `grep -E "const runner = tx" el-templo-api/src/modules/finance/transaction-service.ts` returns 1 match. - `grep -E "import .*TxHandle.*from .\\./balance-service" el-templo-api/src/modules/finance/transaction-service.ts` returns 1 match (no duplicate type). - `cd el-templo-api && pnpm typecheck` exits 0. - `cd el-templo-api && pnpm test --run test/finance/transaction-service.test.ts test/finance/transactions-api.test.ts` exits 0 (tests existentes de Phase 105/106 verdes — backward compat verificado sin pasar tx).
</acceptance_criteria>

<done>TransactionService.create acepta tx? opcional. Tests existentes verdes (no pasan tx → fallback runner abre db.transaction propia). El módulo finance compila sin errores.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary          | Description                                                                                                           |
| ----------------- | --------------------------------------------------------------------------------------------------------------------- |
| internal API → DB | `transactionService.create()` orquesta inserts en `financial_transactions`, `transaction_links` y delta en `balances` |

## STRIDE Threat Register

| Threat ID | Category    | Component                                 | Disposition | Mitigation Plan                                                                                                                                                                                              |
| --------- | ----------- | ----------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-107-01  | Tampering   | `applyDelta`                              | mitigate    | Comentario de invariante en balance-service.ts: todas las queries usan `tx` recibido; runtime check vía test de atomicidad de Plan 03                                                                        |
| T-107-02  | Repudiation | `transaction-service.create` log de éxito | accept      | El log estructurado existente (líneas 189-199) ya escribe userId/transactionId/recordedBy. Sin cambio en surface logging.                                                                                    |
| T-107-03  | Denial      | runner pattern (recursive db.transaction) | mitigate    | Si caller pasa `tx` y `create()` intentara abrir otra `db.transaction(cb)`, MySQL fallaría con "transaction already started". Runner explícito evita el bug: cuando `tx` está presente, NO abrimos nueva tx. |

</threat_model>

<verification>
Smoke verification post-implementación:
1. `cd el-templo-api && pnpm typecheck` → 0 errors.
2. `cd el-templo-api && pnpm test --run test/finance/` → todos los tests del módulo finance verdes (Phase 105/106 sin regresión).
3. `grep -rE "this\\.db\\.transaction" el-templo-api/src/modules/finance/transaction-service.ts` → ahora aparece **dentro** del fallback del runner, no como única ruta.
4. Manual: revisar que `finance/routes.ts:119` (`POST /api/admin/transactions`) NO requiera cambio — verifica backward compat.
</verification>

<success_criteria>

- TxHandle exportado desde balance-service.ts y reutilizado por transaction-service.ts (no duplicación).
- TransactionService.create() acepta `tx?: TxHandle` y reusa esa conexión cuando se provee.
- Cuando NO se provee tx, abre `this.db.transaction(cb)` (idéntico al pre-refactor).
- Cero regresiones: tests del módulo finance verdes sin modificación.
- Código preparado para Plan 02 (subscriptions/service.ts puede ahora envolver `transactionService.create(input, adminId, outerTx)` para atomicidad real).
  </success_criteria>

<output>
After completion, create `.planning/phases/107-cobro-al-asignar-plan/107-01-SUMMARY.md`
</output>
