---
phase: 107-cobro-al-asignar-plan
plan: 02
type: execute
wave: 2
depends_on: [107-01]
files_modified:
  - el-templo-api/src/modules/subscriptions/service.ts
  - el-templo-api/src/modules/subscriptions/types.ts
  - el-templo-api/src/modules/subscriptions/schemas.ts
autonomous: true
requirements:
  - CHARGE-01
  - CHARGE-03
must_haves:
  truths:
    - "AssignPlanInput, ChangePlanInput (alias de AssignPlanInput), RenewSubscriptionInput tienen campo opcional amountReceived?: number"
    - "Body schemas Fastify (assignPlanSchema, changePlanSchema, renewSubscriptionSchema) declaran amountReceived: { type: integer, minimum: 0 } como propiedad opcional"
    - "Los 4 callsites (assignPlan L1117, changePlanNow L2271, changePlanAfterCurrent L2641, renewSubscription L2904-2925) llaman a transactionService.create DENTRO del db.transaction existente, pasando el outer tx"
    - "Backward compat: cuando amountReceived es undefined, el callsite usa pricePaid/netAmount/renewalPrice (chargeBase) como default — comportamiento idéntico al pre-refactor"
    - "Validación: amountReceived < 0 → BadRequestError; amountReceived > chargeBase → BadRequestError con mensaje 'amountReceived no puede exceder el monto a cobrar ($X)'"
    - "Cuando amountReceived < chargeBase: log estructurado info con campos del D-16, mensaje 'Plan asignado con cobro parcial'"
    - "renewSubscription: renewBranchId resuelto ANTES o DENTRO del db.transaction (no después como hoy)"
    - "Helper privado recordAssignmentCharge(tx, params) extraído y reusado por los 4 callsites (DRY)"
    - "Notes del transaction es flow-aware: 'Cobro al asignar plan X' / 'Cobro al cambiar a plan X' / 'Cobro al cambio programado a plan X' / 'Cobro al renovar plan X' — leíble por operaciones en CajaPage"
  artifacts:
    - path: "el-templo-api/src/modules/subscriptions/types.ts"
      provides: "AssignPlanInput.amountReceived, RenewSubscriptionInput.amountReceived"
      contains: "amountReceived?: number"
    - path: "el-templo-api/src/modules/subscriptions/schemas.ts"
      provides: "JSON Schema validation amountReceived integer minimum 0"
      contains: 'amountReceived: { type: "integer", minimum: 0 }'
    - path: "el-templo-api/src/modules/subscriptions/service.ts"
      provides: "Helper recordAssignmentCharge + 4 callsites refactorizados con tx pass-through"
      contains: "private async recordAssignmentCharge"
  key_links:
    - from: "subscriptions/service.ts callsites"
      to: "transactionService.create"
      via: "pasa outer tx (3er parámetro) para atomicidad"
      pattern: "transactionService\\.create\\([^)]+,\\s*adminId,\\s*tx\\)"
    - from: "subscriptions/service.ts cap validation"
      to: "BadRequestError"
      via: "throw antes de cualquier write"
      pattern: "throw new BadRequestError"
    - from: "subscriptions/service.ts log partial"
      to: "this.log.info"
      via: "structured log D-16"
      pattern: "Plan asignado con cobro parcial"
    - from: "subscriptions/service.ts notes"
      to: "financial_transactions.notes"
      via: "flowLabelMap[flow] genera label en español"
      pattern: "flowLabelMap"
---

<objective>
Refactorizar los 4 callsites de `subscriptions/service.ts` que hoy llaman a `transactionService.create` DESPUÉS de cerrar la `db.transaction` de la subscription. Mover esas llamadas DENTRO del bloque, pasando el outer `tx` para atomicidad real (D-10 / CHARGE-03). En el mismo refactor:

1. Extender `AssignPlanInput` y `RenewSubscriptionInput` con `amountReceived?: number` (D-13).
2. Extender los body JSON Schemas de assign/change/renew con la propiedad opcional (D-15 — Fastify JSON Schema, NOT Zod).
3. Validar el cap (`amountReceived <= chargeBase`) y el floor (`>= 0`) lanzando `BadRequestError` (D-14 → 400 via handleServiceError).
4. Computar `chargeBase` por flow: `pricePaid` para assign + change-after-current, `netAmount` para change-now (proration), `renewalPrice` para renew.
5. Usar `amountReceived` como `amount` Y `allocatedAmount` del único link (D-13: si parcial, balance row queda con `chargeBase - amountReceived`).
6. Para renewSubscription: hoist `renewBranchId` antes/dentro del db.transaction (PATTERNS L206 — hoy se resuelve después).
7. Cuando `amountReceived < chargeBase` (cobro parcial real), emitir log estructurado info con campos D-16.
8. Extraer helper privado `recordAssignmentCharge(tx, params)` para DRY los 4 callsites.
9. Notes del transaction flow-aware: en español operativo, distinguir asignar / cambiar / cambio programado / renovar para que la columna "Concepto" de CajaPage (Barcelona y Chapadmalal) sea legible diariamente.

Purpose: Cumple CHARGE-01 (payload accepts amountReceived) + CHARGE-03 (atomicidad subscription + transaction + balance). Es el corazón del cambio backend de Phase 107.
Output: 4 callsites atómicos, helper compartido, schemas + types extendidos, todos los tests existentes verdes (los nuevos van en Plan 03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/107-cobro-al-asignar-plan/107-CONTEXT.md
@.planning/phases/107-cobro-al-asignar-plan/107-PATTERNS.md
@.planning/phases/107-01-SUMMARY.md
@.planning/phases/105-modelo-de-datos-drop-del-viejo/105-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Contracts del Plan 01 (TxHandle export, transactionService.create con tx? opcional) -->

From el-templo-api/src/modules/finance/balance-service.ts (post Plan 01):

```ts
export type TxHandle = Parameters<Parameters<DbInstance["transaction"]>[0]>[0];
```

From el-templo-api/src/modules/finance/transaction-service.ts (post Plan 01):

```ts
async create(
  input: CreateTransactionInput,
  recordedBy: number,
  tx?: TxHandle,
): Promise<TransactionDetail>;
```

Cuando se pasa `tx`, todas las queries internas usan esa conexión. Cuando no, abre db.transaction propia.

From el-templo-api/src/modules/shared/errors.ts:

```ts
export class BadRequestError extends Error {
  /* ... */
}
```

handleServiceError mapea BadRequestError → HTTP 400.

From el-templo-api/src/modules/subscriptions/types.ts (line 218-241 actual):

```ts
export interface AssignPlanInput {
  planId: number;
  branchId: number;
  startDate: string;
  priceTypeApplied: PriceType;
  paymentMethod: PaymentMethod;
  scheduleIds?: number[];
  auraSpend?: number;
  priceOverrideAmount?: number;
  priceOverrideReason?: string;
  boardingPass?: boolean;
  notes?: string;
  startMode?: "now" | "after_current";
}

export interface RenewSubscriptionInput {
  paymentMethod: PaymentMethod;
}
```

ChangePlanInput es alias de AssignPlanInput (ver routes.ts:292) — un solo cambio cubre ambos.

From el-templo-api/src/modules/subscriptions/schemas.ts (line 296-311 actual, assignPlanSchema body):

```ts
properties: {
  planId: { type: "integer" },
  branchId: { type: "integer" },
  startDate: { type: "string", pattern: "^\\d{4}-\\d{2}-\\d{2}$" },
  priceTypeApplied: { type: "string", enum: ["regular", "zero", "credit_card"] },
  paymentMethod: { type: "string", enum: ["cash", "transfer", "card"] },
  scheduleIds: { type: "array", items: { type: "integer" }, minItems: 1 },
  auraSpend: { type: "integer", minimum: 0 },
  priceOverrideAmount: { type: "integer", minimum: 0 },   // ← análogo numérico canónico
  priceOverrideReason: { type: "string" },
  boardingPass: { type: "boolean" },
  notes: { type: "string" },
},
```

NO hay `additionalProperties: false` — clientes viejos mandando otros campos siguen validando (backward compat D-19).

From el-templo-api/src/modules/subscriptions/service.ts (callsite shape, líneas 1117-1140 actuales):

```ts
if (this.transactionService && pricePaid > 0) {
  await this.transactionService.create(
    {
      memberId: userId,
      kind: "plan_charge" as const,
      direction: "inflow" as const,
      amount: pricePaid,
      currency: plan.currency,
      paymentMethod: input.paymentMethod,
      transactionDate: input.startDate,
      effectiveDate: input.startDate,
      branchId: input.branchId,
      notes: input.notes ?? null,
      links: [
        {
          targetKind: "subscription" as const,
          targetId: subscriptionId,
          allocatedAmount: pricePaid,
        },
      ],
    },
    adminId,
  );
}
```

Crítico: este bloque está HOY **fuera** del `db.transaction(async (tx) => ...)` que persiste la subscription. Phase 107 lo mueve adentro y le pasa el `tx`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender types y JSON Schemas con amountReceived</name>
  <files>
    el-templo-api/src/modules/subscriptions/types.ts,
    el-templo-api/src/modules/subscriptions/schemas.ts
  </files>

<read_first> - el-templo-api/src/modules/subscriptions/types.ts (estado completo, especialmente AssignPlanInput L218-231 y RenewSubscriptionInput L233-241) - el-templo-api/src/modules/subscriptions/schemas.ts (estado completo — assignPlanSchema, changePlanSchema, renewSubscriptionSchema) - .planning/phases/107-cobro-al-asignar-plan/107-PATTERNS.md sección "subscriptions/types.ts" + sección "subscriptions/schemas.ts" - .planning/phases/107-cobro-al-asignar-plan/107-CONTEXT.md decisiones D-12, D-13, D-14, D-15
</read_first>

  <action>
**A. types.ts — agregar campo opcional amountReceived a 2 interfaces.**

En `AssignPlanInput` (línea 218 actual, después de `startMode?: "now" | "after_current";`), agregar:

```ts
  /**
   * Monto efectivamente recibido al asignar (D-12, D-13).
   * Backward-compat: undefined → defaults to pricePaid en service layer.
   * Validación: 0 <= amountReceived <= pricePaid (cap-violación = 400).
   */
  amountReceived?: number;
```

En `RenewSubscriptionInput` (línea 233 actual), agregar:

```ts
  /**
   * Monto efectivamente recibido al renovar (D-12, D-13).
   * Backward-compat: undefined → defaults to renewalPrice en service layer.
   */
  amountReceived?: number;
```

`ChangePlanInput` se hereda automáticamente porque routes.ts:292 reusa `AssignPlanInput` para change. NO hay tipo separado a tocar.

**B. schemas.ts — agregar propiedad amountReceived a 3 body schemas.**

Patrón canónico a copiar (línea 307 actual): `priceOverrideAmount: { type: "integer", minimum: 0 },`

En `assignPlanSchema.body.properties` (línea 296-311 actual), agregar después de `priceOverrideAmount`:

```ts
      amountReceived: { type: "integer", minimum: 0 },
```

En `changePlanSchema.body.properties` (línea 321-365 actual): localizar el bloque properties (sigue el mismo patrón que assignPlanSchema) y agregar la misma línea `amountReceived: { type: "integer", minimum: 0 },` después de la propiedad numérica integer existente más cercana semánticamente (priceOverrideAmount si está, sino al final del bloque properties antes del cierre).

En `renewSubscriptionSchema.body.properties` (línea 367-388 actual): agregar la misma línea `amountReceived: { type: "integer", minimum: 0 },` al bloque properties.

**Importante:**

- NO agregar `amountReceived` a `required`. Es opcional (D-13).
- NO setear `additionalProperties: false` en ningún schema (backward compat D-19 — clientes viejos siguen validando aunque omitan el campo).
- NO usar Zod (D-15 — el codebase usa Fastify JSON Schema con `as const`).
- Mantener el tipo `as const` ya presente en el archivo.

**C. Validar lectura de schemas en routes.ts.**
Confirmar (sin modificar) que `routes.ts` registra `{ schema: assignPlanSchema }`, `{ schema: changePlanSchema }`, `{ schema: renewSubscriptionSchema }` (líneas ~252, ~292, etc.). El schema actualizado se aplica automáticamente.
</action>

  <verify>
    <automated>cd el-templo-api && grep -E "amountReceived\?: number" src/modules/subscriptions/types.ts | wc -l | grep -E "^2$" && grep -cE "amountReceived: \{ type: \"integer\", minimum: 0 \}" src/modules/subscriptions/schemas.ts | grep -E "^3$" && pnpm typecheck</automated>
  </verify>

<acceptance_criteria> - `grep -cE "amountReceived\?: number" el-templo-api/src/modules/subscriptions/types.ts` returns `2` (AssignPlanInput + RenewSubscriptionInput). - `grep -cE "amountReceived: \\{ type: \"integer\", minimum: 0 \\}" el-templo-api/src/modules/subscriptions/schemas.ts` returns `3` (assign + change + renew schemas). - `grep -cE "additionalProperties: false" el-templo-api/src/modules/subscriptions/schemas.ts` returns `0` (no se agregó por accidente). - `cd el-templo-api && pnpm typecheck` exits 0.
</acceptance_criteria>

<done>Los 3 endpoints REST de subscriptions ahora aceptan `amountReceived` en el body sin error 400 de validación. Backward compat preservado. Tipos exportados para que routes.ts y tests puedan referenciar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor 4 callsites + helper recordAssignmentCharge + log estructurado D-16</name>
  <files>el-templo-api/src/modules/subscriptions/service.ts</files>

  <behavior>
    - Helper `recordAssignmentCharge(tx, params)` privado en SubscriptionService que centraliza: cap validation, `transactionService.create(...)` con tx pass-through, log estructurado partial, notes flow-aware via `flowLabelMap`.
    - 4 callsites (assignPlan, changePlanNow, changePlanAfterCurrent, renew) llaman al helper DENTRO del `db.transaction` que crea la subscription.
    - Helper recibe params con `chargeBase` (cap superior) + `flow` label + datos para construir CreateTransactionInput.
    - Cuando `amountReceived < 0` → BadRequestError "amountReceived no puede ser negativo".
    - Cuando `amountReceived > chargeBase` → BadRequestError "amountReceived no puede exceder el monto a cobrar ($chargeBase)".
    - Cuando `amountReceived === undefined` → default a `chargeBase` (backward compat).
    - Cuando `amountReceived === 0` o `chargeBase === 0` → no se crea transaction (guard `if (amountReceived > 0)`).
    - Cuando `amountReceived < chargeBase` (parcial real, > 0) → log info estructurado con campos D-16.
    - El link único pasado a transactionService.create tiene `allocatedAmount === amount === amountReceived`.
    - Para renew: renewBranchId se resuelve ANTES o DENTRO del db.transaction (no después como hoy — PATTERNS L206).
    - Notes del transaction generado por `flowLabelMap[flow]`: "Cobro al asignar plan X" / "Cobro al cambiar a plan X" / "Cobro al cambio programado a plan X" / "Cobro al renovar plan X".
  </behavior>

<read_first> - el-templo-api/src/modules/subscriptions/service.ts líneas alrededor de 1080-1145 (assignPlan callsite + db.transaction) - el-templo-api/src/modules/subscriptions/service.ts líneas alrededor de 2240-2300 (changePlanNow callsite con netAmount/proration) - el-templo-api/src/modules/subscriptions/service.ts líneas alrededor de 2620-2680 (changePlanAfterCurrent / scheduled callsite) - el-templo-api/src/modules/subscriptions/service.ts líneas alrededor de 2880-2940 (renewSubscription callsite + renewBranchId resolution) - el-templo-api/src/modules/finance/transaction-service.ts (post Plan 01 — confirma signature `create(input, recordedBy, tx?)`) - el-templo-api/src/modules/finance/balance-service.ts (post Plan 01 — confirma `export type TxHandle`) - el-templo-api/src/modules/shared/errors.ts (BadRequestError class) - el-templo-api/src/modules/shared/error-handler.ts (handleServiceError → 400 mapping) - .planning/phases/107-cobro-al-asignar-plan/107-PATTERNS.md sección "subscriptions/service.ts (service, refactor 4 callsites)" — snippet target completo y tabla de 4 sites con cap base + flow label - .planning/phases/107-cobro-al-asignar-plan/107-CONTEXT.md decisiones D-08, D-09, D-10, D-13, D-14, D-16
</read_first>

  <action>
**A. Imports al tope del archivo.**

Asegurar que estos imports existan (agregar los faltantes, NO duplicar):

```ts
import { type TxHandle } from "../finance/balance-service";
import { BadRequestError } from "../shared/errors";
```

**B. Definir el tipo ChargeFlow + flowLabelMap (módulo-level, fuera de la clase).**

Justo arriba de la clase `SubscriptionService` (o en un bloque privado de tipos al tope del archivo si ya existe), agregar:

```ts
type ChargeFlow = "assign" | "change-now" | "change-after-current" | "renew";

/**
 * Etiquetas en español operativo para la columna "Concepto" de CajaPage (Barcelona y Chapadmalal).
 * Distinguir el flujo es importante: ops ve diariamente estas notes para conciliar.
 *
 * - assign                   → "Cobro al asignar plan X"
 * - change-now               → "Cobro al cambiar a plan X" (proration / inmediato)
 * - change-after-current     → "Cobro al cambio programado a plan X" (toma efecto post-período actual)
 * - renew                    → "Cobro al renovar plan X"
 */
const flowLabelMap: Record<ChargeFlow, string> = {
  assign: "asignar",
  "change-now": "cambiar a",
  "change-after-current": "cambio programado a",
  renew: "renovar",
};
```

**C. Agregar helper privado en la clase SubscriptionService.**

Ubicación: como método privado de la clase (debajo de los métodos privados existentes). Signature exacta:

```ts
/**
 * Centraliza el registro del cobro al asignar/cambiar/renovar plan (Phase 107 D-08/D-13/D-14/D-16).
 *
 * Validación cap (lanzada antes de cualquier write):
 *   - amountReceived < 0 → BadRequestError
 *   - amountReceived > chargeBase → BadRequestError ("no puede exceder…")
 *
 * Default backward-compat: si params.amountReceived es undefined, se usa chargeBase.
 *
 * Cuando el cobro efectivo es > 0 invoca transactionService.create con el outer `tx` (D-10 atomicidad).
 * Cuando es 0 (boarding pass / chargeBase=0 / explícito 0), no crea transaction (preserva el guard
 * `pricePaid > 0` actual del codebase).
 *
 * Cuando el cobro es parcial (0 < amountReceived < chargeBase), emite log estructurado info con
 * los campos del D-16 — observabilidad operativa sin queries adhoc a balances.
 *
 * El `notes` de la financial_transaction se genera con `flowLabelMap[flow]` para que ops vea en
 * CajaPage textos como "Cobro al asignar plan Performance Mensual", "Cobro al cambiar a plan X",
 * "Cobro al cambio programado a plan X", "Cobro al renovar plan Y".
 *
 * INVARIANTE: el método NO abre db.transaction propia. Recibe tx del caller; el caller debe estar
 * dentro de un `this.db.transaction(async (tx) => { ... })` para que la atomicidad subscription +
 * transaction + balance funcione como CHARGE-03 exige.
 */
private async recordAssignmentCharge(
  tx: TxHandle,
  params: {
    userId: number;
    subscriptionId: number;
    planId: number;
    planName: string;
    planCurrency: string;
    chargeBase: number;
    amountReceived: number | undefined;
    paymentMethod: PaymentMethod;
    branchId: number;
    effectiveDate: string;
    adminId: number;
    flow: ChargeFlow;
  },
): Promise<void> {
  if (!this.transactionService) return;

  const amountReceived = params.amountReceived ?? params.chargeBase;

  if (amountReceived < 0) {
    throw new BadRequestError("amountReceived no puede ser negativo");
  }
  if (amountReceived > params.chargeBase) {
    throw new BadRequestError(
      `amountReceived no puede exceder el monto a cobrar ($${params.chargeBase})`,
    );
  }

  if (amountReceived > 0) {
    const flowLabel = flowLabelMap[params.flow];
    await this.transactionService.create(
      {
        memberId: params.userId,
        kind: "plan_charge" as const,
        direction: "inflow" as const,
        amount: amountReceived,
        currency: params.planCurrency,
        paymentMethod: params.paymentMethod,
        transactionDate: params.effectiveDate,
        effectiveDate: params.effectiveDate,
        branchId: params.branchId,
        notes: `Cobro al ${flowLabel} plan ${params.planName}`,
        links: [
          {
            targetKind: "subscription" as const,
            targetId: params.subscriptionId,
            allocatedAmount: amountReceived,
          },
        ],
      },
      params.adminId,
      tx,
    );
  }

  if (amountReceived < params.chargeBase) {
    this.log.info(
      {
        userId: params.userId,
        subscriptionId: params.subscriptionId,
        planId: params.planId,
        pricePaid: params.chargeBase,
        amountReceived,
        pendingBalance: params.chargeBase - amountReceived,
        paymentMethod: params.paymentMethod,
        branchId: params.branchId,
        recordedBy: params.adminId,
        flow: params.flow,
      },
      "Plan asignado con cobro parcial",
    );
  }
}
```

**D. Refactor callsite 1 — assignPlan (alrededor línea 1117).**

1. Localizar el `db.transaction(async (tx) => { ... })` que persiste la subscription en assignPlan (típicamente abre cerca de L1080 y cierra cerca de L1107).
2. **Eliminar** el bloque actual `if (this.transactionService && pricePaid > 0) { await this.transactionService.create(...) }` (líneas 1117-1140 aprox) que vive **fuera** del `db.transaction`.
3. **Agregar** dentro del `db.transaction` (justo antes del `return { subscriptionId, ... }` interno, después del `await this.recomputeUserStatus(userId, tx)`):

```ts
await this.recordAssignmentCharge(tx, {
  userId,
  subscriptionId,
  planId: plan.id,
  planName: plan.name,
  planCurrency: plan.currency,
  chargeBase: pricePaid,
  amountReceived: input.amountReceived,
  paymentMethod: input.paymentMethod,
  branchId: input.branchId,
  effectiveDate: input.startDate,
  adminId,
  flow: "assign",
});
```

**E. Refactor callsite 2 — changePlanNow (alrededor línea 2271).**

Mismo patrón. `chargeBase = netAmount` (el resultado de proration). `flow: "change-now"`. `effectiveDate` = la fecha de cambio (verificar variable existente, típicamente `input.startDate` o equivalente). Mover la llamada DENTRO del `db.transaction` que crea la nueva subscription, antes del return interno.

**F. Refactor callsite 3 — changePlanAfterCurrent / scheduled (alrededor línea 2641).**

`chargeBase = pricePaid` (sin proration). `flow: "change-after-current"`. Mover dentro del db.transaction.

**G. Refactor callsite 4 — renewSubscription (alrededor línea 2904-2925).**

`chargeBase = renewalPrice`. `flow: "renew"`. **Importante (PATTERNS L206):** la resolución de `renewBranchId` hoy ocurre DESPUÉS del db.transaction. Hay que hoist:

- Opción A (preferida): mover el lookup de `renewBranchId` (incluyendo el fallback a 'Templo Online' virtual branch — ver decisión Plan 105-03 en STATE.md) ANTES de abrir el `db.transaction`. La consulta a users.branchId no necesita estar dentro de la tx.
- Opción B: meter el lookup DENTRO de la tx, antes del helper call.

Elegir A (más simple, menos surface dentro de la tx). Asegurar que si `renewBranchId` no se resuelve (ni del user, ni del fallback), el código sigue lanzando el error que ya lanza hoy (mantener semántica original).

Después del hoist, el helper recibe `branchId: renewBranchId`.

**H. Verificar invariantes preservados.**

- `recomputeUserStatus(userId, tx)` debe seguir corriendo ANTES del `recordAssignmentCharge` dentro de cada tx (PATTERNS L205).
- El `notes` del form (`input.notes`) sigue persistiéndose en la subscription (no en la transaction). El helper auto-genera el notes de la transaction usando `flowLabelMap[flow]` + planName.
- El return value de `assignPlan/changePlan/renewSubscription` no cambia (mismo shape).
- handleServiceError en routes.ts mapea BadRequestError → 400. NO modificar routes.ts.
  </action>

    <verify>
      <automated>cd el-templo-api && grep -cE "private async recordAssignmentCharge" src/modules/subscriptions/service.ts | grep -E "^1$" && grep -cE "this\.recordAssignmentCharge\(tx" src/modules/subscriptions/service.ts | grep -E "^4$" && grep -cE "Plan asignado con cobro parcial" src/modules/subscriptions/service.ts | grep -E "^1$" && grep -cE "flowLabelMap" src/modules/subscriptions/service.ts | grep -E "^[2-9]$|^[1-9][0-9]+$" && pnpm typecheck && pnpm lint && pnpm test --run test/finance/ test/subscriptions/</automated>
    </verify>

  <acceptance_criteria> - `grep -cE "private async recordAssignmentCharge" el-templo-api/src/modules/subscriptions/service.ts` returns `1` (helper extraído). - `grep -cE "this\\.recordAssignmentCharge\\(tx" el-templo-api/src/modules/subscriptions/service.ts` returns `4` (los 4 callsites usan el helper). - `grep -cE "Plan asignado con cobro parcial" el-templo-api/src/modules/subscriptions/service.ts` returns `1` (log estructurado, una sola vez en el helper). - `grep -cE "this\\.transactionService\\.create" el-templo-api/src/modules/subscriptions/service.ts` returns `1` (única invocación, dentro del helper — los 4 callsites antiguos eliminados). - `grep -cE "amountReceived no puede" el-templo-api/src/modules/subscriptions/service.ts` returns `2` (negativo + cap). - `grep -cE "flowLabelMap|cambiar a|renovar" el-templo-api/src/modules/subscriptions/service.ts` returns ≥3 (mapa definido + al menos 2 etiquetas presentes en el source). - `grep -cE "type ChargeFlow|ChargeFlow =" el-templo-api/src/modules/subscriptions/service.ts` returns ≥1 (tipo definido). - `grep -cE "Cobro al \\\$\\{flowLabel\\} plan" el-templo-api/src/modules/subscriptions/service.ts` returns `1` — notes generado dinámicamente vía template literal con `flowLabel`. - `cd el-templo-api && pnpm typecheck` exits 0. - `cd el-templo-api && pnpm lint` exits 0. - `cd el-templo-api && pnpm test --run test/finance/ test/subscriptions/` exits 0 — los tests existentes de Phase 105/106 verdes; backward compat verificada (assignPlan sin amountReceived sigue funcionando con default = pricePaid).
  </acceptance_criteria>

    <done>
      - 4 callsites llaman a `recordAssignmentCharge(tx, ...)` DENTRO de su respectivo `db.transaction`.
      - `transactionService.create` se invoca con outer `tx` (3er param), por lo que rollback de la subscription también rollbackea la transaction y el balance row.
      - amountReceived undefined → default chargeBase (backward compat).
      - amountReceived inválido → BadRequestError → 400.
      - Cobro parcial real emite log info estructurado.
      - Helper extraído (DRY, PATTERNS recommendation).
      - renewBranchId resuelto antes/dentro de la tx, sin orphan post-tx.
      - Notes flow-aware: CajaPage muestra "Cobro al asignar/cambiar a/cambio programado a/renovar plan X" según flujo.
    </done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary          | Description                                                                                                |
| ----------------- | ---------------------------------------------------------------------------------------------------------- |
| client → REST API | Body con `amountReceived` (cliente puede enviar negativo, mayor que pricePaid, no integer, no enviar nada) |
| service → DB      | Subscription + transaction + balance writes (deben ser atómicos)                                           |

## STRIDE Threat Register

| Threat ID | Category               | Component                             | Disposition | Mitigation Plan                                                                                                                                                                                                                      |
| --------- | ---------------------- | ------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-107-04  | Tampering              | `amountReceived` body field           | mitigate    | Doble validación: JSON Schema (`type: integer, minimum: 0`) en route layer + cap superior `<= chargeBase` en service layer (BadRequestError). Defensa en profundidad — el schema rechaza floats/negativos antes de tocar el service. |
| T-107-05  | Repudiation            | partial-charge log                    | mitigate    | Log estructurado D-16 incluye `recordedBy: adminId` + `branchId` + `userId` + `subscriptionId` + `paymentMethod`. Trazabilidad completa por miembro/admin/sucursal sin queries adhoc a balances.                                     |
| T-107-06  | Information Disclosure | error message "no puede exceder ($X)" | accept      | El mensaje revela el chargeBase que el admin YA conoce (lo está mostrando el dialog). Sin leak adicional.                                                                                                                            |
| T-107-07  | Denial of Service      | renewBranchId hoist                   | mitigate    | El lookup se hace antes de la tx — query rápida con index en users.branchId. NO mantiene la tx abierta esperando IO innecesario.                                                                                                     |
| T-107-08  | Elevation              | adminId pasado al helper              | accept      | adminId proviene de `request.user.userId` post-auth-hook. Routes layer no toca request.body para esto — sin surface de abuse.                                                                                                        |
| T-107-09  | Tampering              | atomic rollback                       | mitigate    | Test de Plan 03 (mock balance failure) verifica que ni subscription ni transaction quedan persistidas si applyDelta tira. El uso del outer `tx` es la mitigation técnica; el test la valida.                                         |

</threat_model>

<verification>
1. `pnpm typecheck` y `pnpm lint` verde en el-templo-api.
2. `pnpm test --run test/finance/ test/subscriptions/` verde — tests de Phase 105/106 sin regresión.
3. Inspección manual: el bloque `await this.transactionService.create(...)` original (4 ocurrencias) ya NO existe fuera del helper. `grep -nE "transactionService\\.create" el-templo-api/src/modules/subscriptions/service.ts` devuelve solo la línea dentro del helper.
4. Inspección manual: cada `db.transaction(async (tx) => { ... })` de los 4 flows ahora cierra con un `await this.recordAssignmentCharge(tx, ...)` antes del return interno.
5. Inspección manual: `flowLabelMap` definido al tope con 4 entradas; el template literal `Cobro al ${flowLabel} plan ${planName}` genera la etiqueta correcta para cada flow.
6. Plan 03 (siguiente) cubre los nuevos behaviors con integration tests reales, incluido el chequeo de regex `/Cobro al (asignar|cambiar a|cambio programado a|renovar) plan/` sobre los rows de financial_transactions.
</verification>

<success_criteria>

- CHARGE-01 cubierto a nivel backend: el endpoint REST acepta `amountReceived` en el body de assign/change/renew.
- CHARGE-03 cubierto: subscription + financial_transaction + transaction_link + balance row se escriben en una sola db.transaction. Cualquier fallo (validation, balance, connection drop) revierte todo.
- Backward compat: clientes que no pasan amountReceived siguen funcionando idénticos al pre-Phase-107.
- Cobro parcial genera log estructurado observable.
- Cap-violations devuelven 400 con mensaje claro en español.
- Helper DRY: zero duplicación del bloque create + log entre los 4 callsites.
- Notes flow-aware: CajaPage refleja el flujo real (asignar / cambiar / cambio programado / renovar) — no todo aparece como "Cobro al asignar".
- Cero regresión en tests existentes.
  </success_criteria>

<output>
After completion, create `.planning/phases/107-cobro-al-asignar-plan/107-02-SUMMARY.md`
</output>
</content>
</invoke>
