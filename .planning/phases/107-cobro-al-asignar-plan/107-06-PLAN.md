---
phase: 107-cobro-al-asignar-plan
plan: 06
type: execute
wave: 3
depends_on: [107-02, 107-03, 107-05]
files_modified:
  - .planning/phases/107-cobro-al-asignar-plan/107-VERIFICATION.md
autonomous: false
requirements:
  - CHARGE-01
  - CHARGE-02
  - CHARGE-03
must_haves:
  truths:
    - "VERIFICATION.md documenta resultado de los 5 escenarios de smoke test D-20 contra staging"
    - "Cada escenario tiene status PASS/FAIL/PENDING + evidencia (screenshot ref, query SQL ejecutada, log line, etc.)"
    - "Escenario 1: asignar plan con cobro completo (default backward compat) -> caja muestra el cobro"
    - "Escenario 2: asignar plan con cobro parcial -> balance row aparece, AlumnosPage muestra deudor"
    - "Escenario 3: cambio con proration y cobro parcial -> desglose visible, balance correcto"
    - "Escenario 4: asignar con boarding pass -> bloque deshabilitado, no transaction"
    - "Escenario 5: logs estructurados en cobros parciales aparecen en server logs/Sentry"
    - "Trazabilidad CHARGE-01/02/03 cumplida (un escenario o test cubre cada requirement)"
    - "Sign-off del usuario para deploy a producción (regla operativa: no deploy de viernes — D-21)"
  artifacts:
    - path: ".planning/phases/107-cobro-al-asignar-plan/107-VERIFICATION.md"
      provides: "Smoke test results + traceability matrix + production deploy sign-off"
      min_lines: 80
  key_links:
    - from: "VERIFICATION.md"
      to: "CHARGE-01, CHARGE-02, CHARGE-03"
      via: "traceability matrix"
      pattern: "CHARGE-0[123]"
---

<objective>
Producir el documento `107-VERIFICATION.md` que captura el resultado del smoke test manual contra staging (D-20) y la matriz de trazabilidad de los requirements CHARGE-01/02/03 contra los plans 01-05. Cubre los 5 escenarios obligatorios del D-20:

1. Cobro completo (backward compat).
2. Cobro parcial.
3. Cambio con proration y cobro parcial.
4. Boarding pass / chargeBase=0.
5. Logs estructurados visibles.

Este plan es checkpoint-driven (autonomous: false): el flujo es Claude prepara la checklist + ejecuta lo que pueda automatizar (queries SQL en staging vía CLI si está disponible, lectura de logs), y el usuario ejecuta los pasos manuales restantes y aprueba el deploy a producción (D-21 — no viernes).

Purpose: Cierre formal de Phase 107 con evidencia. Garantiza que los 3 requirements quedan demostrablemente cumplidos antes de prod.
Output: VERIFICATION.md con matriz + escenarios + sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/107-cobro-al-asignar-plan/107-CONTEXT.md
@.planning/phases/107-01-SUMMARY.md
@.planning/phases/107-02-SUMMARY.md
@.planning/phases/107-03-SUMMARY.md
@.planning/phases/107-04-SUMMARY.md
@.planning/phases/107-05-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear VERIFICATION.md con checklist + traceability matrix</name>
  <files>.planning/phases/107-cobro-al-asignar-plan/107-VERIFICATION.md</files>

<read_first> - .planning/phases/107-cobro-al-asignar-plan/107-CONTEXT.md decisión D-20 (5 escenarios) y D-21 (rollout policy) - .planning/phases/107-01-SUMMARY.md, 107-02-SUMMARY.md, 107-03-SUMMARY.md, 107-04-SUMMARY.md, 107-05-SUMMARY.md (estado real implementado) - .planning/REQUIREMENTS.md sección CHARGE-01/02/03 (texto literal) - Estructura de VERIFICATION.md de phases anteriores: `cat .planning/phases/106-endpoints-transaccionales/106-VERIFICATION.md` o `.planning/phases/106-06-VERIFICATION.md` si existe
</read_first>

  <action>
Crear `.planning/phases/107-cobro-al-asignar-plan/107-VERIFICATION.md` con la siguiente estructura:

```markdown
# Phase 107: Cobro al Asignar Plan — Verification

**Date:** YYYY-MM-DD (fecha real al momento de ejecutar este plan)
**Phase status:** Ready for production deploy / Awaiting sign-off / Issues found
**Verified by:** Claude (automated checks) + ignaciobordon@eltemplo.org (manual smoke)

---

## Traceability Matrix

| Requirement | Description                                                                                         | Covered by                                                                                                                                                            | Status |
| ----------- | --------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| CHARGE-01   | `AssignPlanDialog` incluye sección "Cobro" con monto, método, fecha al asignar/renovar              | Plan 02 (backend payload accept) + Plan 04 (types) + Plan 05 Task 1 (UI bloque Cobro)                                                                                 | ✅/❌  |
| CHARGE-02   | Preview en vivo del saldo cuando monto < pricePaid                                                  | Plan 05 Task 1 (computed pendingBalance + banner amarillo)                                                                                                            | ✅/❌  |
| CHARGE-03   | Asignar plan + crear financial_transaction + crear transaction_link es atómico; fallo revierte todo | Plan 01 (TransactionService.create acepta tx?) + Plan 02 (helper recordAssignmentCharge dentro del db.transaction) + Plan 03 atomicity test (mock applyDelta failure) | ✅/❌  |

---

## Plans Status

| Plan   | Status       | Tests                                     | Notes                                       |
| ------ | ------------ | ----------------------------------------- | ------------------------------------------- |
| 107-01 | done/blocked | finance/\* verde                          | TxHandle exportado, runner pattern aplicado |
| 107-02 | done/blocked | finance/_ + subscriptions/_ verde         | Helper extraído, 4 callsites refactorizados |
| 107-03 | done/blocked | charge-on-assign.test.ts: N tests passing | Atomicity probada                           |
| 107-04 | done/blocked | typecheck verde                           | amountReceived agregado a 2 interfaces      |
| 107-05 | done/blocked | typecheck + lint verde + smoke approved   | UI funcional                                |

---

## Smoke Test (D-20) — Staging

**Environment:** staging.admin.eltemplo.org (URL real) + staging API
**Tester:** ignaciobordon@eltemplo.org
**Date:** YYYY-MM-DD

### Escenario 1 — Cobro completo (backward compat)

**Steps:**

1. Login admin staging.
2. AlumnosPage -> seleccionar miembro test -> Gestionar Plan -> Asignar plan.
3. Plan: cualquiera con `priceRegular > 0` (ej. presencial mensual).
4. Step Confirmar: monto recibido pre-llenado = finalPrice. NO modificar.
5. Confirmar.

**Expected:**

- Asignación 201 OK.
- CajaPage muestra el cobro (kind=plan_charge, amount=pricePaid, paymentMethod elegido).
- AlumnosPage NO marca al miembro como deudor.
- DB: `SELECT * FROM balances WHERE member_id = X AND target_kind = 'subscription'` -> 1 row con `amount = 0` (o ningún row si Phase 105 no seedea para zero balance — verificar contra SPEC).

**Result:** ✅/❌
**Evidence:** [screenshot CajaPage | query SQL output | network request body | log line]

---

### Escenario 2 — Cobro parcial

**Steps:**

1. Login admin staging.
2. AlumnosPage -> otro miembro test -> Asignar plan.
3. Plan con priceRegular = 100000 (o equivalente).
4. Step Confirmar: modificar monto recibido a 60000.
5. Esperado en UI: banner amarillo "Cobro parcial. El miembro quedará como deudor por $40.000 ARS."
6. Confirmar.

**Expected:**

- Asignación 201 OK.
- DB: `SELECT amount FROM balances WHERE member_id = X AND target_kind = 'subscription' AND target_id = subId` -> `40000`.
- AlumnosPage marca al miembro como deudor (banner agregado o tag visual según UI Phase 105).
- CajaPage muestra el cobro de 60000.

**Result:** ✅/❌
**Evidence:** [...]

---

### Escenario 3 — Cambio con proration + cobro parcial

**Steps:**

1. Asignar plan A al miembro test (ej. priceRegular 80000), cobro full.
2. A las pocas horas: "Cambiar plan" -> plan B (priceRegular 120000), startMode='now'.
3. Step Confirmar: ver desglose Plan / Crédito proration / Neto a cobrar.
4. Asumir netAmount = 100000 (ejemplo). Modificar monto recibido a 70000.
5. Confirmar.

**Expected:**

- Cambio 201 OK, vieja subscription cancelada.
- Banner amarillo muestra "deudor por $30.000 ARS" (netAmount 100000 - 70000 = 30000).
- DB: balance row sobre la NUEVA subscriptionId tiene `amount = 30000`.

**Result:** ✅/❌
**Evidence:** [...]

---

### Escenario 4 — Boarding pass (chargeBase = 0)

**Steps:**

1. Asignar plan con boarding pass activado (o priceTypeApplied='zero', o override = 0).
2. Step Confirmar: bloque Cobro visible, inputs deshabilitados, leyenda "Plan gratuito - sin cobro".
3. Confirmar.

**Expected:**

- Asignación 201 OK.
- DB: `SELECT * FROM financial_transactions WHERE member_id = X` -> no row creada por este assign.
- DB: `SELECT * FROM balances WHERE member_id = X AND target_kind = 'subscription' AND target_id = subId` -> no row.

**Result:** ✅/❌
**Evidence:** [...]

---

### Escenario 5 — Logs estructurados visibles

**Steps:**

1. Verificar logs server (Sentry / CloudWatch / docker logs según el setup de staging).
2. Buscar: `"Plan asignado con cobro parcial"` (texto exacto del log de Plan 02).

**Expected:**

- ≥1 log line con ese mensaje.
- Campos del JSON estructurado presentes: `userId`, `subscriptionId`, `planId`, `pricePaid`, `amountReceived`, `pendingBalance`, `paymentMethod`, `branchId`, `recordedBy`, `flow`.
- Para cada escenario 2 y 3, el log debe tener flow="assign" y "change-now" respectivamente.

**Result:** ✅/❌
**Evidence:** [grep result | log excerpt | Sentry event link]

---

## Atomicity (CHARGE-03) — Automated Verification

Plan 03 incluye test directo de atomicidad (mock balanceService.applyDelta failure):

- File: `el-templo-api/test/subscriptions/charge-on-assign.test.ts` describe "Atomicity (D-11)".
- Run: `cd el-templo-api && pnpm test --run test/subscriptions/charge-on-assign.test.ts`.
- Expected: PASS — assertions verifican que ni subscription ni transaction ni balance row persisten cuando applyDelta tira.

**Status:** ✅/❌
**Last run:** YYYY-MM-DD HH:MM (output del comando)

---

## Production Deploy Sign-Off (D-21)

**Policy:** No deploy de viernes (regla operativa estándar).

**Pre-flight checklist:**

- [ ] Todos los plans 107-01..107-05 con SUMMARY ✅
- [ ] `pnpm test` verde en api (no solo subscriptions/finance)
- [ ] `pnpm typecheck` y `pnpm lint` verde en api y admin
- [ ] Smoke staging escenarios 1-5 ✅
- [ ] Logs estructurados verificados
- [ ] Atomicity test verde
- [ ] Hoy NO es viernes (validar fecha al momento de deploy)

**Approval:** ☐ ignaciobordon@eltemplo.org — fecha + nota

**Production deploy:** ejecutar pipeline de deploy estándar (master push -> CI -> deploy automatizado) SOLO después del approval explícito.

---

## Issues Found (if any)

[Si algún escenario falla, listar issue + plan de remediation aquí. Si todo verde, dejar "None."]

---

_Phase: 107-cobro-al-asignar-plan_
_Verification: YYYY-MM-DD_
```

Reemplazar los placeholders YYYY-MM-DD con la fecha real al momento de ejecución (typing literal o `date +%Y-%m-%d` via Bash).

Para los `Result: ✅/❌` y `Status:` campos, dejarlos como `PENDING` cuando se crea el documento; el operador (Claude + usuario en el checkpoint Task 2) los va llenando.

**Importante:**

- NO ejecutar el deploy a producción dentro de este plan (regla del usuario "Always ask before pushing", "No unsolicited manual frontend deploys"). El plan termina con el sign-off del documento, NO con un push.
- NO usar `git push` salvo confirmación explícita (regla feedback_ask_before_push).
  </action>

    <verify>
      <automated>test -f .planning/phases/107-cobro-al-asignar-plan/107-VERIFICATION.md && grep -c "Escenario [1-5]" .planning/phases/107-cobro-al-asignar-plan/107-VERIFICATION.md && grep -c "CHARGE-0[123]" .planning/phases/107-cobro-al-asignar-plan/107-VERIFICATION.md && grep -c "Sign-Off" .planning/phases/107-cobro-al-asignar-plan/107-VERIFICATION.md</automated>
    </verify>

  <acceptance_criteria> - El archivo `.planning/phases/107-cobro-al-asignar-plan/107-VERIFICATION.md` existe. - `grep -c "Escenario [1-5]" .planning/phases/107-cobro-al-asignar-plan/107-VERIFICATION.md` returns ≥5 (los 5 escenarios D-20). - `grep -c "CHARGE-0[123]" .planning/phases/107-cobro-al-asignar-plan/107-VERIFICATION.md` returns ≥3 (matriz de trazabilidad cubre los 3 requirements). - `grep -c "Sign-Off" .planning/phases/107-cobro-al-asignar-plan/107-VERIFICATION.md` returns ≥1 (sección de production approval presente). - `grep -c "PENDING\|✅\|❌" .planning/phases/107-cobro-al-asignar-plan/107-VERIFICATION.md` returns ≥10 (status placeholders en escenarios + traceability matrix).
  </acceptance_criteria>

  <done>VERIFICATION.md scaffold completo con 5 escenarios + matriz + sign-off section, listo para que el checkpoint humano lo complete con evidencia real.</done>
  </task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke staging + sign-off para deploy a producción</name>

<files>n/a (checkpoint task)</files>
<action>Pause for human verification per how-to-verify steps.</action>
<verify>Human approval (resume-signal).</verify>
<done>User types "approved" or describes issues.</done>
<what-built>

- Backend Phase 107 deployado en staging (Plans 01-03).
- Frontend admin Phase 107 deployado en staging (Plans 04-05).
- VERIFICATION.md scaffold creado en Task 1.

Verificación que falta: ejecutar los 5 escenarios D-20 contra staging real, capturar evidencia, y firmar el deploy a producción (D-21).
</what-built>

  <how-to-verify>
1. **Confirmar staging deploy:**
   - Backend staging tiene los cambios de Plans 01-03 (revisar último commit de master / staging branch).
   - Frontend admin staging tiene los cambios de Plans 04-05.
   - Si NO está deployado a staging, abortar y deployar primero (siguiendo regla feedback_staging_first_strict).

2. **Ejecutar Escenario 1 — Cobro completo:**
   - Seguir los steps del VERIFICATION.md sección "Escenario 1".
   - Capturar evidencia: screenshot del banner CajaPage del día + query SQL `SELECT * FROM financial_transactions WHERE member_id = X` mostrando el cobro.
   - Marcar Result en VERIFICATION.md como ✅ o ❌.

3. **Ejecutar Escenario 2 — Cobro parcial:**
   - Seguir los steps. Capturar:
     - Screenshot del banner amarillo en el dialog ("deudor por $X").
     - Screenshot de AlumnosPage mostrando el deudor.
     - Query: `SELECT amount FROM balances WHERE member_id = X AND target_kind = 'subscription'`.
   - Marcar Result.

4. **Ejecutar Escenario 3 — Cambio con proration:**
   - Seguir los steps. Capturar el desglose Plan/Crédito/Neto + balance row.
   - Marcar Result.

5. **Ejecutar Escenario 4 — Boarding pass:**
   - Seguir los steps. Capturar bloque Cobro deshabilitado + queries que confirman no transaction/balance.
   - Marcar Result.

6. **Ejecutar Escenario 5 — Logs estructurados:**
   - SSH al server staging (con permission del usuario — regla feedback_always_ask_before_ssh) o consultar Sentry.
   - `grep "Plan asignado con cobro parcial" /var/log/... ` (o equivalente Sentry query).
   - Confirmar que el log estructurado aparece después de los Escenarios 2 y 3.
   - Marcar Result.

7. **Atomicity automated re-run:**
   - `cd el-templo-api && pnpm test --run test/subscriptions/charge-on-assign.test.ts`
   - Confirmar 0 fallos.
   - Marcar Status en VERIFICATION.md.

8. **Sign-off para producción:**
   - Si TODOS los escenarios + atomicity test = ✅:
     - Verificar fecha actual: `date +%A` -> debe NO ser "Friday" (D-21).
     - Marcar approval en VERIFICATION.md con fecha + email del usuario.
     - El usuario puede entonces autorizar `git push origin master` para gatillar el pipeline (Claude NO pushea sin confirmación explícita).
   - Si algún escenario falla:
     - Documentar el issue en sección "Issues Found".
     - NO firmar deploy. Abrir tarea correctiva (puede ser fix directo en master local antes de merge a staging, o nuevo plan en Phase 107.X si el issue es estructural).
       </how-to-verify>

  <resume-signal>
Type "approved" si los 5 escenarios + atomicity test pasan y el documento VERIFICATION.md quedó completo con evidencia + sign-off para deploy.
Si hay issues, describir cuáles y si requieren fix antes de prod o pueden diferirse a una phase posterior.
NO ejecutar deploy a producción en este task — solo firmar la decisión.
  </resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                |
| --------------------------------- | -------------------------------------------------------------------------- |
| staging environment -> producción | El deploy a prod ocurre solo después de smoke staging ✅ + sign-off humano |

## STRIDE Threat Register

| Threat ID | Category    | Component                 | Disposition | Mitigation Plan                                                                                                             |
| --------- | ----------- | ------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------- |
| T-107-16  | Denial      | viernes deploy            | mitigate    | D-21 política operativa: NO deploy de viernes. Verificación explícita en pre-flight checklist.                              |
| T-107-17  | Tampering   | smoke verde sin atomicity | mitigate    | El sign-off requiere atomicity test (Plan 03) verde como condición separada — smoke manual no es suficiente para CHARGE-03. |
| T-107-18  | Repudiation | sign-off sin evidencia    | mitigate    | Cada escenario requiere evidence (screenshot/query/log) registrada en VERIFICATION.md antes del approval.                   |

</threat_model>

<verification>
1. VERIFICATION.md existe y tiene la estructura correcta (Task 1 acceptance criteria).
2. Smoke approved por humano con todos los Result fields llenos (Task 2 resume-signal).
3. Plan 03 atomicity test pasando en último run.
</verification>

<success_criteria>

- VERIFICATION.md completo con evidencia real para los 5 escenarios.
- CHARGE-01, CHARGE-02, CHARGE-03 marked como cubiertos en la matriz.
- Sign-off explícito del usuario para deploy producción (NO viernes).
- Cero issues open al cerrar Phase 107.
  </success_criteria>

<output>
After completion, create `.planning/phases/107-cobro-al-asignar-plan/107-06-SUMMARY.md`
</output>
