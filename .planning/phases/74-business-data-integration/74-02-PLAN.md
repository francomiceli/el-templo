---
phase: 74-business-data-integration
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - el-templo-bot/src/ai/tools.ts
  - el-templo-bot/test/knowledge.test.ts
autonomous: true
requirements:
  - BIZ-02

must_haves:
  truths:
    - "Bot returns correct addresses for all 5 branches when get_location is called"
    - "Bot returns working Google Maps short links (goo.gl) for all 5 branches"
    - "Bot answers location questions for Moreno and Mario Bravo branches (previously missing)"
    - "Knowledge data contains correct pricing numbers verified by automated test"
  artifacts:
    - path: "el-templo-bot/src/ai/tools.ts"
      provides: "get_location tool with real addresses and Maps links for all 5 branches"
      contains: "Mario Bravo"
    - path: "el-templo-bot/test/knowledge.test.ts"
      provides: "Tests verifying business data accuracy"
      min_lines: 40
  key_links:
    - from: "el-templo-bot/src/ai/tools.ts"
      to: "BRANCH_ADDRESSES and BRANCH_MAPS_LINKS"
      via: "exported lookup maps used by get_location tool and imported by tests"
      pattern: "export.*BRANCH_ADDRESSES|export.*BRANCH_MAPS"
---

<objective>
Fix the get_location tool to return correct addresses and real Google Maps links for all 5 El Templo branches, and add automated tests to verify business data accuracy.

Purpose: The current get_location tool has only 3 branches with WRONG addresses (says "San Miguel de Tucuman" instead of Mar del Plata), and generates generic Maps search links instead of the real goo.gl short links. Users asking for directions get incorrect information.

Output: Updated tools.ts with all 5 real branch addresses and Maps links, knowledge.test.ts validating data accuracy.
</objective>

<execution_context>
@/Users/bores/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bores/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@contexto/el-templo-business-data.md
@el-templo-bot/src/ai/tools.ts
@.planning/phases/74-business-data-integration/74-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix get_location tool with real addresses and Maps links</name>
  <files>el-templo-bot/src/ai/tools.ts</files>
  <action>
Update the `BRANCH_ADDRESSES` map and add a `BRANCH_MAPS_LINKS` map in `el-templo-bot/src/ai/tools.ts` with the real El Templo data. All branches are in Mar del Plata, Argentina.

**IMPORTANT: Both `BRANCH_ADDRESSES` and `BRANCH_MAPS_LINKS` MUST be exported** (use `export const`) so that tests in 74-02 Task 2 can import and assert on them directly.

**Replace `BRANCH_ADDRESSES` with correct data (exported):**

```typescript
export const BRANCH_ADDRESSES: Record<string, string> = {
  constitucion: "Av. Constitucion 6745, Mar del Plata",
  jujuy: "Jujuy 3761, Mar del Plata",
  alem: "Alem 3958, Mar del Plata",
  moreno: "Moreno 3751, Mar del Plata",
  "mario bravo": "Mario Bravo 618, Mar del Plata",
};
```

**Add `BRANCH_MAPS_LINKS` (exported) with the real Google Maps short links from the business data doc:**

```typescript
export const BRANCH_MAPS_LINKS: Record<string, string> = {
  constitucion: "https://maps.app.goo.gl/vi9c8ErtHr7RpQxD6",
  jujuy: "https://maps.app.goo.gl/EFEVhYhphKKaZqF5A",
  alem: "https://maps.app.goo.gl/KiyqJQJYG1LbUsAL6",
  moreno: "https://maps.app.goo.gl/EFEVhYhphKKaZqF5A",
  "mario bravo": "https://maps.app.goo.gl/DciqhK6yc3dhpmTn6",
};
```

Note: Jujuy and Moreno share the same Maps link in the source doc -- this appears intentional (same area).

**Update `formatBranchLocations` function:**

- Use `BRANCH_MAPS_LINKS[codeKey]` for the Maps link instead of generating a search URL
- Fall back to the generated search URL only if no entry exists in `BRANCH_MAPS_LINKS`
- Keep using `BRANCH_ADDRESSES[codeKey]` for the address text

**Update key matching logic in BRANCH_ADDRESSES and BRANCH_MAPS_LINKS:**
The `codeKey` is derived from `row.code.toLowerCase()`. The branch codes in the database may not match the new keys exactly (e.g., DB might have "mario_bravo" or "mariobravo"). To handle this robustly:

- Add a `normalizeBranchCode(code: string): string` helper that maps common variations to the canonical keys (e.g., "mario_bravo" -> "mario bravo", "mariobravo" -> "mario bravo")
- Also try matching by checking if the `row.name.toLowerCase()` contains any of the canonical keys as a fallback
- This ensures get_location works regardless of exact DB branch code format
  </action>
  <verify>
  <automated>cd /Users/bores/el-templo/el-templo-bot && npx tsc --noEmit src/ai/tools.ts</automated>
  <manual>Verify all 5 branches have correct Mar del Plata addresses and real goo.gl Maps links</manual>
  </verify>
  <done>get_location tool returns correct addresses and real Google Maps short links for all 5 branches including Moreno and Mario Bravo which were previously missing. BRANCH_ADDRESSES and BRANCH_MAPS_LINKS are exported for test access.</done>
  </task>

<task type="auto">
  <name>Task 2: Add knowledge data accuracy tests</name>
  <files>el-templo-bot/test/knowledge.test.ts</files>
  <action>
Create `el-templo-bot/test/knowledge.test.ts` using vitest (matching existing test pattern in el-templo-bot/test/).

Tests should verify:

**1. Knowledge file data accuracy (import getBusinessKnowledge from ../src/ai/knowledge.js):**

- Output contains correct Flex price "$80.000" (or "$80,000" depending on format)
- Output contains correct Foundation price "$250.000"
- Output contains correct Performance price "$600.000"
- Output contains Zero prices for all plan types
- Output contains credit card prices ($670,000 Performance TC, $370,000 Foundation+ TC, $280,000 Foundation TC)
- Output contains all 5 branch names (Constitucion, Jujuy, Alem, Moreno, Mario Bravo)
- Output contains schedule data for each branch
- Output mentions ROM (Range of Motion / Rango Organico de Movilidad)
- Output mentions Boarding Pass
- Output mentions trial class info (sesion de prueba, $20.000 value, bonificada)
- Output mentions app download links
- Output mentions password recovery steps

**2. Branch address data accuracy (import BRANCH_ADDRESSES and BRANCH_MAPS_LINKS from ../src/ai/tools.js):**

Both constants are exported from tools.ts (ensured in Task 1). Import them directly:

```typescript
import { BRANCH_ADDRESSES, BRANCH_MAPS_LINKS } from "../src/ai/tools.js";
```

- BRANCH_ADDRESSES has exactly 5 entries (constitucion, jujuy, alem, moreno, mario bravo)
- All addresses contain "Mar del Plata" (NOT "Tucuman")
- BRANCH_MAPS_LINKS has exactly 5 entries matching the same keys
- All Maps links contain "goo.gl" domain (real short links, not generated search URLs)

**3. System prompt integration (import getSystemPrompt from ../src/ai/system-prompt.js):**

- getSystemPrompt() output contains "Conocimiento del negocio" section
- getSystemPrompt() output contains pricing data
- getSystemPrompt() output length is reasonable (> 2000 chars, indicating knowledge was injected)

Use `describe` and `it` blocks. Pattern: `import { describe, it, expect } from "vitest";`
</action>
<verify>
<automated>cd /Users/bores/el-templo/el-templo-bot && npx vitest run test/knowledge.test.ts</automated>
</verify>
<done>All knowledge accuracy tests pass, verifying correct pricing numbers, branch addresses, Maps links, ROM data, trial flow info, and app help data match the source business document</done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes: `cd el-templo-bot && npx tsc --noEmit`
2. Knowledge tests pass: `cd el-templo-bot && npx vitest run test/knowledge.test.ts`
3. All existing bot tests still pass: `cd el-templo-bot && npx vitest run`
4. get_location returns "Mar del Plata" addresses (not Tucuman)
5. get_location returns goo.gl Maps links for all 5 branches
</verification>

<success_criteria>

- All 5 branches have correct Mar del Plata addresses in the get_location tool
- All 5 branches have real Google Maps short links (not generated search URLs)
- BRANCH_ADDRESSES and BRANCH_MAPS_LINKS are exported from tools.ts for direct test import
- Automated tests verify pricing numbers, branch data, and prompt integration match the source document
- All existing tests continue to pass
  </success_criteria>

<output>
After completion, create `.planning/phases/74-business-data-integration/74-02-SUMMARY.md`
</output>
