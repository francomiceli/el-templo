---
phase: 74-business-data-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-bot/src/ai/knowledge.ts
  - el-templo-bot/src/ai/system-prompt.ts
autonomous: true
requirements:
  - BIZ-01
  - BIZ-03
  - BIZ-04
  - BIZ-05
  - BIZ-06
  - BIZ-07
  - BIZ-08

must_haves:
  truths:
    - "System prompt includes accurate pricing for Flex, Flex+, Foundation, Foundation+, Performance plans (regular and Zero prices)"
    - "System prompt includes credit card pricing with 3-installment surcharges"
    - "System prompt includes class schedules per branch with correct days and times"
    - "System prompt explains Zero pricing rules (Boarding Pass first month, upgrade to long-term plan)"
    - "System prompt explains ROM (Range of Motion) classes and what they include"
    - "System prompt explains trial class flow (free first class, Boarding Pass, 24h advance booking, what to bring)"
    - "System prompt includes app troubleshooting info (download links, password recovery, booking/cancellation steps)"
    - "Knowledge data lives in a separate structured file, not hardcoded in the prompt string"
  artifacts:
    - path: "el-templo-bot/src/ai/knowledge.ts"
      provides: "Structured business knowledge data exported as typed objects/strings"
      min_lines: 80
    - path: "el-templo-bot/src/ai/system-prompt.ts"
      provides: "System prompt that imports and injects knowledge data"
      contains: "import.*knowledge"
  key_links:
    - from: "el-templo-bot/src/ai/system-prompt.ts"
      to: "el-templo-bot/src/ai/knowledge.ts"
      via: "import and string interpolation into system prompt"
      pattern: "import.*knowledge"
---

<objective>
Create a structured, maintainable knowledge file containing all El Templo business data (pricing, schedules, Zero rules, ROM, trial flow, app help) and wire it into the system prompt so the bot can answer accurately about all business topics.

Purpose: The bot currently has NO business data in its system prompt. Users asking about pricing, schedules, or procedures get generic responses or the bot has to guess. This plan gives the bot accurate, comprehensive knowledge.

Output: `knowledge.ts` with structured business data, updated `system-prompt.ts` that imports and injects this data.
</objective>

<execution_context>
@/Users/bores/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bores/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@contexto/el-templo-business-data.md
@el-templo-bot/src/ai/system-prompt.ts
@el-templo-bot/src/ai/tools.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create structured knowledge file with all business data</name>
  <files>el-templo-bot/src/ai/knowledge.ts</files>
  <action>
Create `el-templo-bot/src/ai/knowledge.ts` that exports a `getBusinessKnowledge(): string` function returning a formatted markdown string suitable for injection into the system prompt. The function should compose the following sections from structured data constants:

**1. Pricing section (`PRICING_DATA`):**

- Flex plans (1 month): Flex $80,000 regular / $65,000 Zero, Flex+ $100,000 regular / $80,000 Zero
- Foundation plans (4 months): Foundation $250,000 regular / $220,000 Zero, Foundation+ $350,000 regular / $315,000 Zero
- Performance (8 months): $600,000 regular / $560,000 Zero
- Credit card plans (3 cuotas sin interes, tarjeta fisica, miercoles y sabados): Performance $670,000 (Zero $625,000), Foundation+ $370,000 (Zero $340,000), Foundation $280,000 (Zero $245,000)
- For each plan include: sessions per week, modality (turnos fijos/libres), benefits (sesiones de regalo, ROM access, multisede, eventos), sede access (unica/multisede)

**2. Zero pricing rules (`ZERO_RULES`):**

- Boarding Pass (first month at El Templo): unlocks Zero prices on any plan, one-time benefit
- Conversion to long-term: members starting with Flex in first month can get Zero prices exclusively on Foundation/Performance when renewing
- Outside these cases, regular prices apply
- Payment aliases: eltemplomdp.sa (Post SP), eltemplo.mdp (Renovaciones), Cardio number: 432 2555 / 432 2222

**3. Class schedules per branch (`SCHEDULES_DATA`):**

- Constitucion 6745: L-V 7/8/9 // 17/18/19/20 (no 10am morning slot)
- Jujuy 3761: L-V 7/8/9/10 // 17/18/19/20
- Moreno 3751: L-V 7/8/9/10 // 17/18/19/20 + Sab 8/9/10/11
- Alem 3958: L-V 7/8/9/10 // 17/18/19/20 + Sab 8/9/10/11
- Mario Bravo 618: L-V 7/8/9/10 // 17/18/19/20
- Activity type: Calistenia (all branches). ROM classes on Saturdays at select branches.
- Classes are 60 minutes each, guided by professors, 4 training blocks

**4. ROM section (`ROM_DATA`):**

- ROM = Range of Motion (Rango Organico de Movilidad)
- Complementary class focused on mobility, control, range of motion expansion
- Works: articular mobility, body control, active flexibility, recovery and movement quality
- Available in Flex+, Foundation+, Performance plans
- Basic plan members can use their 2 gift sessions for ROM classes

**5. Trial class flow (`TRIAL_FLOW`):**

- First class is free (valued at $20,000, 100% bonificada with Boarding Pass)
- Boarding Pass unlocks exclusive discounts on first membership
- Coordinate minimum 24h in advance
- User selects preferred branch, then chooses day/time
- What to bring: comfortable clothes, no shoes (barefoot training), hand towel optional, water bottle (no glass), dispenser available
- After booking: admin sends Boarding Pass with session details, reconfirmation required before class

**6. App troubleshooting (`APP_HELP`):**

- App download: Android app link (https://kommo.cc/K/U8OEZV/U7CGMD), Web/iPhone/PC link (https://kommo.cc/K/U8OEZX/U7CGMF)
- Account activation: tap "Olvidaste tu contrasena?", enter email, click "Enviar email de recuperacion", create password, select "Unite ahora"
- Booking a class (plus plans): Turnos online > select branch, membership, activity (Calistenia) > choose time slot. Available from 48h before to 5 min before class.
- Canceling a class: Turnos online > select branch, membership, activity > select reserved class > confirm cancellation. Cancel up to 20 min before class.
- Viewing membership: Profile > Mis servicios/membresias > select branch > filter dates > tap blue magnifying glass
- Wait list: If slot shows red (full), join wait list. System sends email alert if spot opens.
- Admin support hours: Monday to Friday, 7 to 21hs

**7. Plan upgrade paths (`UPGRADE_PATHS`):**

- Flex (2x/week) -> Flex+ (6x/week, +ROM) for more flexibility
- Flex/Flex+ (1 month) -> Foundation (4 months) to lock in price and get Zero discounts
- Foundation -> Foundation+ for multisede + ROM access
- Any plan -> Performance (8 months) for maximum benefits (multisede, ROM, events, vacation freeze)
- Long-term plans (Foundation, Performance) can be paid with credit card in 3 installments

Use TypeScript constants (not classes) for the data. Export `getBusinessKnowledge()` that returns a single formatted markdown string. No `any` types. Use Pino-compatible approach (no console.log).
</action>
<verify>
<automated>cd /Users/bores/el-templo/el-templo-bot && npx tsc --noEmit src/ai/knowledge.ts</automated>
<manual>Check that knowledge.ts contains all 7 data sections with correct numbers from the business data doc</manual>
</verify>
<done>knowledge.ts exports getBusinessKnowledge() returning a markdown string with all 7 business data sections, all prices match the source document exactly, all branch schedules are accurate</done>
</task>

<task type="auto">
  <name>Task 2: Wire knowledge file into system prompt</name>
  <files>el-templo-bot/src/ai/system-prompt.ts</files>
  <action>
Update `el-templo-bot/src/ai/system-prompt.ts` to import `getBusinessKnowledge` from `./knowledge.js` and inject the business knowledge into the system prompt.

Specifically:

1. Add `import { getBusinessKnowledge } from "./knowledge.js";` at the top
2. In `getSystemPrompt()`, after the "Limites" section of the base prompt, append a new section:

```
## Conocimiento del negocio

A continuacion tenes la informacion actualizada de El Templo. Usa estos datos para responder consultas de precios, horarios, sedes, planes, clases de prueba y uso de la app. No inventes datos -- si la informacion no esta aca, admitilo y ofrecé escalar a un humano.

${getBusinessKnowledge()}
```

3. The knowledge section MUST be part of the base prompt string (not a conditional append like state/profile sections). It should always be present.
4. Keep the existing state-specific and profile context append logic unchanged.
5. Do NOT modify the tone, tools, booking, escalation, or limits sections of the existing prompt.
   </action>
   <verify>
   <automated>cd /Users/bores/el-templo/el-templo-bot && npx tsc --noEmit src/ai/system-prompt.ts</automated>
   <manual>Verify the prompt structure: base instructions -> business knowledge -> state context -> profile context</manual>
   </verify>
   <done>system-prompt.ts imports knowledge.ts and injects business data into every system prompt. Calling getSystemPrompt() returns a prompt containing accurate pricing, schedules, ROM info, trial flow, and app help data.</done>
   </task>

</tasks>

<verification>
1. TypeScript compilation passes: `cd el-templo-bot && npx tsc --noEmit`
2. knowledge.ts contains exact prices: $80,000 (Flex regular), $65,000 (Flex Zero), $100,000 (Flex+ regular), $250,000 (Foundation regular), $350,000 (Foundation+ regular), $600,000 (Performance regular)
3. knowledge.ts contains all 5 branch schedules with correct hours
4. getSystemPrompt() output contains "Conocimiento del negocio" section
5. getSystemPrompt() output contains pricing, schedule, ROM, trial, and app help data
</verification>

<success_criteria>

- knowledge.ts exists with structured business data matching el-templo-business-data.md exactly
- system-prompt.ts imports and injects knowledge into every prompt
- TypeScript compiles without errors
- All 7 business data sections are present and accurate
  </success_criteria>

<output>
After completion, create `.planning/phases/74-business-data-integration/74-01-SUMMARY.md`
</output>
