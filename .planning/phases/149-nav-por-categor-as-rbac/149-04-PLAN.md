---
phase: 149-nav-por-categor-as-rbac
plan: 04
type: execute
wave: 2
depends_on: [149-03]
files_modified:
  - el-templo-admin/src/router/routes.ts
  - el-templo-admin/src/router/index.ts
  - el-templo-admin/src/pages/PlanesPage.vue
  - el-templo-admin/src/pages/ConfiguracionCajaPage.vue
  - el-templo-admin/src/composables/useFinanceConfigApi.ts
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, NAV-04]
must_haves:
  truths:
    - "Al entrar a '/', el empleado aterriza en /pagos, el dueño en /alumnos, y Fran Scaine en /sessions"
    - "El empleado (coach/gestion/recepcion) puede abrir /planes en modo lectura (ve qué incluye + precios) pero no ve botones de crear/editar/archivar planes ni promos"
    - "admin y owner siguen viendo todos los controles de edición de Planes"
    - "Un gestion que navega a /programas por URL directa es rebotado (router guard dueño-only: meta.allowedRoles = ['admin','owner']) — sin puerta trasera de nav (D-15)"
    - "La ruta /configuracion-caja y la página ConfiguracionCajaPage ya no existen"
  artifacts:
    - path: "el-templo-admin/src/router/routes.ts"
      provides: "Landing por rol + /planes y /pagos abiertos a empleado + /programas dueño-only + sin ruta config-caja"
      contains: "PLANES_READ_ROLES"
    - path: "el-templo-admin/src/pages/PlanesPage.vue"
      provides: "Controles de escritura gateados por canEditPlans (dueño-only)"
      contains: "canEditPlans"
  key_links:
    - from: "el-templo-admin/src/router/routes.ts"
      to: "el-templo-admin/src/config/templo-config.ts"
      via: "import de PLANES_READ_ROLES / PAGOS_ROLES para meta.allowedRoles"
      pattern: "templo-config"
    - from: "el-templo-admin/src/router/routes.ts"
      to: "useAuthStore"
      via: "redirect por rol en '/'"
      pattern: "redirect"
---

<objective>
Completar la reforma de nav del lado del ruteo y de la lectura de Planes: (1) landing por rol en `/` (D-14), (2) ampliar `/planes` y `/pagos` para que el empleado entre (Planes en lectura, Pagos su PoS), (3) angostar el router guard de `/programas` a dueño-only (D-15, cierra el lado frontend de la puerta trasera de Programas), (4) ocultar los controles de escritura de Planes para el empleado en `PlanesPage.vue` (D-09/D-10), y (5) borrar la ruta + página + composable de Configuración de Caja (D-13, lado frontend).

Purpose: Cierra la consistencia frontend↔API (D-04): el empleado gana lectura de Planes en el nav (Plan 03) y la página oculta la edición, mientras la API bloquea la escritura (Plan 01). Programas queda dueño-only en los TRES niveles — nav (Plan 03), router guard (este plan) y API (Plan 01) — sin puertas traseras. La landing por rol evita que el empleado caiga en pantallas que no le sirven.
Output: `routes.ts` con landing por rol + roles ampliados en /planes-/pagos + /programas dueño-only + sin ruta config-caja; `router/index.ts` con `defaultPages` alineado a D-14; `PlanesPage.vue` con `canEditPlans`; página y composable de config-caja borrados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-nav-por-categor-as-rbac/149-CONTEXT.md
@.planning/phases/149-nav-por-categor-as-rbac/149-RESEARCH.md
@.planning/phases/149-nav-por-categor-as-rbac/149-PATTERNS.md

<interfaces>
<!-- Estado actual (file:line) -->
// routes.ts
line 14   → { path: '', redirect: '/sessions' }             // REEMPLAZAR por landing por rol
line 62   → /planes  meta.allowedRoles: ['gestion','admin','owner']      // AMPLIAR a todo staff (PLANES_READ_ROLES)
line 67   → /programas meta.allowedRoles: ['gestion','admin','owner']    // ANGOSTAR a ['admin','owner'] (dueño-only, D-15)
line 81   → /pagos   meta.allowedRoles: ['coach','gestion','admin','owner']  // AMPLIAR: incluir recepcion (PAGOS_ROLES)
line 172-179 → ruta 'configuracion-caja'                    // BORRAR

// router/index.ts:44-51 — defaultPages (fallback cuando allowedRoles bloquea)
{ owner:'/sessions', admin:'/alumnos', coach:'/sessions', gestion:'/alumnos', recepcion:'/alumnos' }
// Target D-14: owner→/alumnos, admin→/alumnos, coach→/pagos, gestion→/pagos, recepcion→/pagos
// El bloque trainingOnly (líneas 59-61, bounce a /alumnos) SE CONSERVA.

// templo-config.ts (creado en Plan 03) exporta: PLANES_READ_ROLES, PAGOS_ROLES (AdminRole[])
// utils/trainingAccess.ts → canAccessTraining(user)

// PlanesPage.vue
line 389 → const isOwner = computed(() => authStore.user?.role === 'owner'); // controla selector de país (línea 29/602) — SE CONSERVA
controles de escritura a ocultar: btn "Nuevo Plan" (47), editar/desactivar fila tabla 1 (140,151),
editar/desactivar fila tabla 2 (240,251), "Nueva Promo" (276), desactivar promo (343)
</interfaces>

<landing_order>

<!-- Orden CRÍTICO (Pitfall 2): canAccessTraining devuelve true para CUALQUIER owner. -->

1. if (user.role === 'coach' && canAccessTraining(user)) → '/sessions' // sólo el coach exclusivo Fran, NO el owner
2. else if (['owner','admin'].includes(user.role)) → '/alumnos' // dueño
3. else → '/pagos' // empleado (coach no-Fran, gestion, recepcion)
   </landing_order>
   </context>

<tasks>

<task type="auto">
  <name>Task 1: Landing por rol + roles ampliados + /programas dueño-only + borrar config-caja (routes.ts + index.ts + páginas)</name>
  <read_first>
    - el-templo-admin/src/router/routes.ts (redirect línea 14; /planes 62; /programas 65-67; /pagos 81; ruta config-caja 172-179)
    - el-templo-admin/src/router/index.ts (beforeEach + defaultPages 44-54; trainingOnly 59-61)
    - el-templo-admin/src/config/templo-config.ts (PLANES_READ_ROLES, PAGOS_ROLES — creados en Plan 03)
    - el-templo-admin/src/utils/trainingAccess.ts (canAccessTraining)
    - el-templo-admin/src/stores/useAuthStore.ts (user.role/email)
    - .planning/phases/149-nav-por-categor-as-rbac/149-RESEARCH.md (§Router Guard y Landing, Pitfall 2)
  </read_first>
  <action>
    En `routes.ts`:
    - Importar `PLANES_READ_ROLES` y `PAGOS_ROLES` desde `src/config/templo-config` y usarlos en `meta.allowedRoles` de `/planes` (línea 62) y `/pagos` (línea 81) respectivamente (widening: `/planes` a todo staff; `/pagos` incluye recepcion). Una sola fuente de verdad de roles (DRY).
    - ANGOSTAR el `meta.allowedRoles` de `/programas` (línea 67) de `['gestion','admin','owner']` a `['admin','owner']` (dueño-only, D-15). Preferir el set `DUENO_ROLES` exportado por `templo-config.ts` (mismo espejo que la API `PROGRAMAS_ROLES`) para no hardcodear roles inline; si `DUENO_ROLES` no está exportado en templo-config, usar el literal `['admin','owner'] as AdminRole[]` y dejar un TODO citando D-06. Esto cierra el lado router de la puerta trasera de Programas (el nav ya lo oculta en Plan 03; la API lo bloquea en Plan 01).
    - Reemplazar `{ path: '', redirect: '/sessions' }` (línea 14) por `{ path: '', redirect: () => { ... } }` que lea el `useAuthStore` y devuelva el destino según <landing_order> EXACTAMENTE en ese orden (no usar `canAccessTraining` a secas — mandaría al owner a /sessions).
    - Borrar la ruta `configuracion-caja` completa (líneas 172-179).

    En `router/index.ts`: actualizar el mapa `defaultPages` (líneas 44-51) a los valores target de <interfaces> (owner→/alumnos, admin→/alumnos, coach→/pagos, gestion→/pagos, recepcion→/pagos). CONSERVAR intacto el bloque `trainingOnly` con bounce a `/alumnos` (líneas 59-61).

    Borrar los archivos `src/pages/ConfiguracionCajaPage.vue` y `src/composables/useFinanceConfigApi.ts` (único consumidor de este composable es esa página — verificado). Grep final: cero referencias a `ConfiguracionCajaPage`, `useFinanceConfigApi` y `configuracion-caja` en `src/`.

  </action>
  <verify>
    <automated>cd el-templo-admin && ! grep -rn "configuracion-caja\|ConfiguracionCajaPage\|useFinanceConfigApi" src/ && pnpm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PLANES_READ_ROLES\|PAGOS_ROLES" el-templo-admin/src/router/routes.ts` ≥ 2 (roles importados de templo-config)
    - `/pagos` allowedRoles incluye `recepcion` (grep del bloque /pagos)
    - El bloque `/programas` ya NO incluye `gestion` en `meta.allowedRoles` (grep del bloque programas muestra sólo admin/owner)
    - `routes.ts` ya no contiene `redirect: '/sessions'` estático en la ruta raíz; hay un `redirect:` función con la lógica por rol
    - `grep -rn "configuracion-caja\|ConfiguracionCajaPage\|useFinanceConfigApi" el-templo-admin/src` no devuelve resultados
    - `router/index.ts` `defaultPages`: coach/gestion/recepcion → '/pagos', owner/admin → '/alumnos'
    - `ls el-templo-admin/src/pages/ConfiguracionCajaPage.vue` y `.../composables/useFinanceConfigApi.ts` fallan (borrados)
    - `pnpm run build` compila el admin sin errores
  </acceptance_criteria>
  <done>'/' redirige por rol (empleado→/pagos, dueño→/alumnos, Fran→/sessions); /planes y /pagos abiertos al empleado; /programas dueño-only en el router guard; ruta/página/composable de config-caja borrados; defaultPages alineado a D-14.</done>
</task>

<task type="auto">
  <name>Task 2: Planes en modo lectura para el empleado (canEditPlans en PlanesPage.vue)</name>
  <read_first>
    - el-templo-admin/src/pages/PlanesPage.vue (computed isOwner línea 389 + uso 29/602; controles de escritura 47,140,151,240,251,276,343; dialogs 260,351)
    - .planning/phases/149-nav-por-categor-as-rbac/149-RESEARCH.md (§PlanesPage.vue — tabla de controles, Pitfall "no reusar isOwner")
    - .planning/phases/149-nav-por-categor-as-rbac/149-CONTEXT.md (D-09/D-10)
  </read_first>
  <action>
    En `PlanesPage.vue` agregar un computed NUEVO `canEditPlans = computed(() => ['owner','admin'].includes(authStore.user?.role ?? ''))`. NO reusar `isOwner` (excluiría a admin, que es dueño — D-01); `isOwner` se conserva sólo para el selector de país (líneas 29/602).

    Aplicar `v-if="canEditPlans"` a los controles de escritura: botón "Nuevo Plan" (línea 47), botones editar/desactivar por fila de la primera tabla (140,151), editar/desactivar de la segunda tabla (240,251), botón "Nueva Promo" (276) y desactivar promo (343). El listado de planes y el tab de Promos quedan VISIBLES en modo lectura (D-10: el empleado ve qué incluye + precios, planes de pago + promos vigentes). Los dialogs `PlanFormDialog` (260) y `PromoFormDialog` (351) no se abren si no hay trigger — no requieren cambios adicionales.

    No tocar la ruta `/programas` ni ProgramasPage aquí: su gating dueño-only ya queda cubierto en el nav (Plan 03), el router guard (Task 1 de este plan) y la API (Plan 01). Programas no forma parte de PlanesPage.

  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm run lint src/pages/PlanesPage.vue && pnpm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "canEditPlans" el-templo-admin/src/pages/PlanesPage.vue` ≥ 6 (definición + 5 controles gateados, o más)
    - `isOwner` sigue existiendo y se usa sólo para el país (líneas 29/602 sin cambio de semántica)
    - Los botones "Nuevo Plan", "Nueva Promo", editar y desactivar (planes y promos) están envueltos en `v-if="canEditPlans"`
    - `pnpm run build` compila el admin sin errores
  </acceptance_criteria>
  <done>El empleado ve Planes/Promos en modo lectura sin botones de escritura; admin/owner ven todos los controles; el selector de país sigue gateado por isOwner.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                                                                                          |
| ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| rol del usuario → ruteo + controles de UI | El ruteo y el ocultamiento de controles son UX; la escritura de planes y el CRUD de programs los bloquea la API (Plan 01, D-11/D-15) |

## STRIDE Threat Register

| Threat ID | Category                          | Component                                                                      | Disposition | Mitigation Plan                                                                                                                                                  |
| --------- | --------------------------------- | ------------------------------------------------------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-149-10  | Denial of Service (UX)            | Landing loop: coach cae en /sessions (trainingOnly) y rebota                   | mitigate    | Orden de <landing_order>: coach no-Fran → /pagos; el bounce trainingOnly a /alumnos se conserva                                                                  |
| T-149-11  | Elevation of Privilege (aparente) | Ocultar botones de edición no protege el endpoint                              | accept      | La API bloquea los writes (Plan 01 PLANES_WRITE_ROLES); PlanesPage sólo esconde controles                                                                        |
| T-149-12  | Information Disclosure            | Widening de /planes expone datos sensibles al empleado                         | accept      | D-10 acota la lectura a planes de pago + promos vigentes (qué incluye + precios), que el profe necesita en mostrador; sin superficie de entrenamiento            |
| T-149-14  | Elevation of Privilege            | gestion navega a /programas por URL directa (router guard aún permite gestion) | mitigate    | `meta.allowedRoles` de `/programas` angostado a `['admin','owner']`; el router rebota a gestion. Cierra el lado frontend; la API lo cierra en Plan 01 (T-149-13) |
| T-149-SC  | Tampering                         | Instalación de paquetes                                                        | accept      | Esta fase NO instala paquetes — sin superficie de supply chain                                                                                                   |

</threat_model>

<verification>
- `cd el-templo-admin && pnpm run build` compila sin errores.
- `pnpm run lint` sin errores nuevos en los archivos tocados.
- **UAT visual (D-14 + D-09/D-10 + D-15):** (1) loguearse como owner→aterriza en /alumnos; admin→/alumnos; gestion/recepcion→/pagos; coach genérico→/pagos; Fran Scaine→/sessions. (2) Como empleado, abrir /planes: se ven planes + promos con precios/inclusiones, SIN botones de crear/editar/archivar. (3) Como admin, /planes muestra todos los controles. (4) Como gestion, navegar a mano a /programas → rebote (no entra). (5) Confirmar que /configuracion-caja ya no existe (404/redirect).
- **UAT / paso manual D-05:** antes de shippear, verificar (pedir OK; nunca SSH autónomo) los usuarios reales con rol gestion/recepcion en prod (impacto operativo del landing/roles). Para El Templo el impacto esperado es nulo.
</verification>

<success_criteria>

- Landing por rol funcionando con el orden correcto (NAV-01, D-14).
- Empleado con Planes en modo lectura + Pagos; edición sólo dueño (NAV-02/NAV-03).
- /programas dueño-only en el router guard (D-15) — consistente con nav (Plan 03) y API (Plan 01).
- Ruta, página y composable de config-caja eliminados (NAV-04, D-13).
  </success_criteria>

<output>
Create `.planning/phases/149-nav-por-categor-as-rbac/149-04-SUMMARY.md` when done
</output>
</content>
</invoke>
