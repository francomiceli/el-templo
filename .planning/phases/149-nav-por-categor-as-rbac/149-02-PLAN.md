---
phase: 149-nav-por-categor-as-rbac
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/finance/transaction-service.ts
  - el-templo-api/src/modules/finance/routes.ts
  - el-templo-api/src/modules/finance/config-service.ts
  - el-templo-api/src/modules/finance/schemas.ts
  - el-templo-api/src/modules/finance/types.ts
  - el-templo-api/src/modules/finance/index.ts
  - el-templo-api/test/finance-config.test.ts
autonomous: true
requirements: [NAV-01]
must_haves:
  truths:
    - "El umbral de pendientes de la bandeja sigue siendo 3 días independientemente del estado de system_settings en la DB (una fila finance.pending_overdue_days con valor ≠ 3 ya no altera el resultado)"
    - "GET/PUT /api/finance/config/overdue-threshold responden 404 (los endpoints ya no existen)"
    - "La API arranca y la bandeja de pendientes (listPendingTray) sigue respondiendo tras eliminar FinanceConfigService, sin superficie de Configuración de Caja"
  artifacts:
    - path: "el-templo-api/src/modules/finance/transaction-service.ts"
      provides: "Read-path del umbral vía constante OVERDUE_DAYS (Opción A)"
      contains: "OVERDUE_DAYS"
  key_links:
    - from: "el-templo-api/src/modules/finance/transaction-service.ts"
      to: "OVERDUE_DAYS"
      via: "import desde ./constants"
      pattern: "OVERDUE_DAYS"
---

<objective>
Eliminar por completo la perilla de "Configuración de Caja" (D-13): los endpoints `GET`/`PUT /config/overdue-threshold`, el `FinanceConfigService`, sus schemas/types y el test asociado. El umbral de pendientes queda hardcodeado en la constante `OVERDUE_DAYS=3` (Opción A del research — sin depender del estado de `system_settings` en prod, sin migración).

Purpose: El usuario decidió borrar la perilla de la fase 142. Opción A garantiza "default hardcodeado" leyendo la constante directamente, sin dejar código muerto ni depender de si prod tiene una fila `finance.pending_overdue_days` con valor ≠ 3.
Output: Read-path a constante en `transaction-service.ts`, endpoints y servicio de config borrados, tsc verde sin imports colgados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-nav-por-categor-as-rbac/149-CONTEXT.md
@.planning/phases/149-nav-por-categor-as-rbac/149-RESEARCH.md
@.planning/phases/149-nav-por-categor-as-rbac/149-PATTERNS.md

<interfaces>
<!-- Estado actual verificado (file:line) -->
finance/constants.ts:16                → export const OVERDUE_DAYS = 3;
transaction-service.ts:39              → import { FinanceConfigService } from "./config-service";
transaction-service.ts:123             → private readonly financeConfig: FinanceConfigService;
transaction-service.ts:130-132         → constructor param opcional `financeConfig?` con default `new FinanceConfigService(db, log)`
transaction-service.ts:1384            → const threshold = await this.financeConfig.getOverdueThreshold();  // en listPendingTray
finance/routes.ts:85-94                → instancia financeConfigService y la pasa a new TransactionService(...)
finance/routes.ts:1043-1085            → endpoints GET/PUT /config/overdue-threshold
finance/index.ts:7                     → export { FinanceConfigService, FINANCE_SETTINGS_KEYS } from "./config-service";
finance/schemas.ts:825                 → getOverdueThresholdSchema / putOverdueThresholdSchema
finance/types.ts:550-554               → OverdueThresholdBody
<!-- Consumidores de FinanceConfigService/FINANCE_SETTINGS_KEYS: SÓLO config-service.ts, finance/index.ts, finance/routes.ts, transaction-service.ts y test/finance-config.test.ts (verificado por grep). Ningún otro módulo. -->
<!-- La constante OVERDUE_DAYS no tiene otros consumidores fuera de finance. El constructor param financeConfig es OPCIONAL: la mayoría de los ~9 call-sites de `new TransactionService` NO lo pasan; sólo finance/routes.ts lo pasa. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read-path del umbral a la constante OVERDUE_DAYS (Opción A)</name>
  <read_first>
    - el-templo-api/src/modules/finance/transaction-service.ts (imports 39-41, campo 123, constructor 130-132, uso 1384, comentario obsoleto 40-41)
    - el-templo-api/src/modules/finance/constants.ts (OVERDUE_DAYS línea 16)
    - el-templo-api/src/modules/finance/routes.ts (instanciación financeConfigService + TransactionService líneas 85-94)
    - .planning/phases/149-nav-por-categor-as-rbac/149-RESEARCH.md (§ConfiguracionCajaPage Opción A, Pitfall 5)
  </read_first>
  <action>
    En `transaction-service.ts`: eliminar el import de `FinanceConfigService` (línea 39), el campo `financeConfig` (123) y el parámetro opcional `financeConfig?` del constructor (130-132). Importar `OVERDUE_DAYS` desde `./constants`. En `listPendingTray` (línea 1384) reemplazar `await this.financeConfig.getOverdueThreshold()` por la constante `OVERDUE_DAYS` directamente. Actualizar el comentario obsoleto de las líneas 40-41 (ya no fluye por FinanceConfigService).

    En `finance/routes.ts`: eliminar la instanciación de `financeConfigService` (líneas 85-94) y dejar de pasarla a `new TransactionService(...)` (línea 89). Verificar que el resto de call-sites de `new TransactionService` no pasaban `financeConfig` (eran opcionales) — no requieren cambio.

    NO borrar todavía los endpoints ni el archivo config-service (eso es Task 2); esta task sólo corta la dependencia del read-path para que el borrado de Task 2 no rompa listPendingTray.

  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit && pnpm test finance</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "FinanceConfigService\|financeConfig" el-templo-api/src/modules/finance/transaction-service.ts` == 0
    - `grep -c "OVERDUE_DAYS" el-templo-api/src/modules/finance/transaction-service.ts` ≥ 1
    - `pnpm exec tsc --noEmit` sin errores (constructor y call-sites consistentes)
    - Los tests de finance que ejercen listPendingTray siguen en verde (umbral efectivo = 3)
  </acceptance_criteria>
  <done>listPendingTray usa OVERDUE_DAYS directo; transaction-service ya no depende de FinanceConfigService; tsc verde.</done>
</task>

<task type="auto">
  <name>Task 2: Borrar endpoints /config/overdue-threshold + FinanceConfigService + schemas/types/test</name>
  <read_first>
    - el-templo-api/src/modules/finance/routes.ts (endpoints 1043-1085; imports 23 FinanceConfigService, 49-50 schemas, 76 OverdueThresholdBody)
    - el-templo-api/src/modules/finance/config-service.ts (archivo completo a borrar; exporta FinanceConfigService + FINANCE_SETTINGS_KEYS)
    - el-templo-api/src/modules/finance/index.ts (línea 7 export a borrar)
    - el-templo-api/src/modules/finance/schemas.ts (línea 825 getOverdueThresholdSchema/putOverdueThresholdSchema)
    - el-templo-api/src/modules/finance/types.ts (líneas 550-554 OverdueThresholdBody)
    - el-templo-api/test/finance-config.test.ts (archivo completo a borrar)
  </read_first>
  <action>
    Borrar del código, en este orden, todo lo que quede huérfano tras Task 1:
    - `finance/routes.ts`: borrar ambos endpoints `GET`/`PUT /config/overdue-threshold` (bloque 1043-1085) y sus imports (FinanceConfigService línea 23, getOverdueThresholdSchema/putOverdueThresholdSchema líneas 49-50, OverdueThresholdBody línea 76).
    - Borrar el archivo `finance/config-service.ts` por completo (incluye `FinanceConfigService` y `FINANCE_SETTINGS_KEYS` — sin otros consumidores).
    - `finance/index.ts`: borrar la línea `export { FinanceConfigService, FINANCE_SETTINGS_KEYS } from "./config-service";`
    - `finance/schemas.ts`: borrar `getOverdueThresholdSchema` y `putOverdueThresholdSchema` (línea 825).
    - `finance/types.ts`: borrar `OverdueThresholdBody` (líneas 550-554).
    - Borrar el archivo de test `el-templo-api/test/finance-config.test.ts`.
    Correr un grep final para confirmar cero referencias residuales a `FinanceConfigService`, `FINANCE_SETTINGS_KEYS`, `OverdueThreshold` y `config/overdue-threshold` en `src/` y `test/`.
  </action>
  <verify>
    <automated>cd el-templo-api && ! grep -rn "FinanceConfigService\|FINANCE_SETTINGS_KEYS\|OverdueThreshold\|config/overdue-threshold" src/ test/ && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "FinanceConfigService\|FINANCE_SETTINGS_KEYS\|OverdueThreshold\|config/overdue-threshold" el-templo-api/src el-templo-api/test` no devuelve resultados
    - `ls el-templo-api/src/modules/finance/config-service.ts` falla (archivo borrado)
    - `ls el-templo-api/test/finance-config.test.ts` falla (archivo borrado)
    - `pnpm exec tsc --noEmit` sin errores
  </acceptance_criteria>
  <done>Perilla de config-caja eliminada de la API por completo (endpoints, servicio, schemas, types, test); tsc verde; sin referencias colgadas.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                                                                 |
| --------------------------- | ------------------------------------------------------------------------------------------- |
| cliente admin → API Fastify | Los endpoints eliminados eran owner/admin-only; su desaparición reduce superficie de ataque |

## STRIDE Threat Register

| Threat ID | Category          | Component                                                                                      | Disposition | Mitigation Plan                                                                                                                     |
| --------- | ----------------- | ---------------------------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| T-149-05  | Tampering         | Fila `finance.pending_overdue_days` en system_settings de prod con valor ≠ 3 quedaría ignorada | accept      | Opción A lee la constante directo → la fila es irrelevante, sin migración; el umbral efectivo es siempre 3 (default esperado, D-13) |
| T-149-06  | Denial of Service | Imports/constructor colgados tras el borrado rompen el arranque de la API                      | mitigate    | Task 1 corta la dependencia antes de borrar; grep final + tsc como gate                                                             |
| T-149-SC  | Tampering         | Instalación de paquetes                                                                        | accept      | Esta fase NO instala paquetes — sin superficie de supply chain                                                                      |

</threat_model>

<verification>
- `cd el-templo-api && pnpm exec tsc --noEmit` sin errores.
- `cd el-templo-api && pnpm test finance` en verde (listPendingTray con umbral 3).
- Grep final sin referencias a FinanceConfigService / config/overdue-threshold.
- Suite completa en CI al pushear a staging.
</verification>

<success_criteria>

- El umbral de pendientes queda hardcodeado en OVERDUE_DAYS=3 (D-13, Opción A).
- Endpoints, servicio, schemas, types y test de config-caja eliminados sin dejar código muerto.
- Sin migración necesaria (la fila de system_settings queda irrelevante).
  </success_criteria>

<output>
Create `.planning/phases/149-nav-por-categor-as-rbac/149-02-SUMMARY.md` when done
</output>
</content>
</invoke>
