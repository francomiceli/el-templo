---
phase: 149-nav-por-categor-as-rbac
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - el-templo-api/src/modules/shared/permissions.ts
  - el-templo-api/src/modules/programs/routes.ts
  - el-templo-api/test/programs.test.ts
  - el-templo-api/test/rbac-sets.test.ts
  - el-templo-admin/src/pages/PlanesPage.vue
autonomous: true
requirements: [NAV-03]

must_haves:
  truths:
    - "Un usuario gestion/recepcion recibe 200 (no 403) al listar programas vía GET /admin/programs"
    - "Un usuario coach recibe 403 en GET /admin/programs — Programas NO se le muestra al empleado-profe (D-10)"
    - "La columna 'Programa' en /planes muestra el programa vinculado real para gestion/recepcion (no '—' en todos los planes)"
    - "Para coach, PlanesPage NO dispara el fetch de programas (sin 403, sin evento de Sentry); la columna degrada a '—'"
    - "El diálogo 'Asignar programa adicional' carga las opciones de programa para gestion (select poblado, no error)"
    - "Los 6 handlers restantes del CRUD admin de programs (create, detail, analytics, update, content, deactivate) siguen dueño-only: gestion recibe 403"
  artifacts:
    - path: "el-templo-api/src/modules/shared/permissions.ts"
      provides: "PROGRAMAS_LIST_ROLES = ['owner','admin','gestion','recepcion'] — catálogo de programas legible por staff administrativo, SIN coach (D-10)"
      contains: "PROGRAMAS_LIST_ROLES"
    - path: "el-templo-api/src/modules/programs/routes.ts"
      provides: "GET /admin/programs (list) gateado por PROGRAMAS_LIST_ROLES; los otros 6 handlers admin siguen con PROGRAMAS_ROLES"
      contains: "PROGRAMAS_LIST_ROLES"
    - path: "el-templo-api/test/programs.test.ts"
      provides: "Contrato fijado: gestion → 200 en GET /admin/programs (list); coach → 403 en el mismo GET; gestion → 403 en los 6 handlers restantes"
      contains: "PROGRAMAS_LIST_ROLES"
  key_links:
    - from: "el-templo-admin/src/pages/PlanesPage.vue (loadPrograms)"
      to: "GET /admin/programs"
      via: "programsApi.getPrograms() — condicional al rol (no-coach)"
      pattern: "programsApi\\.getPrograms"
    - from: "el-templo-admin/src/components/AssignProgramAddonDialog.vue (listActivePrograms)"
      to: "GET /admin/programs"
      via: "useProgramsApi().listActivePrograms()"
      pattern: "listActivePrograms"
---

<objective>
Cierra la brecha NAV-03 (CR-01 del REVIEW / truth 3 de la verificación): el angostamiento de `GET /admin/programs` a dueño-only (Plan 01) rompió dos consumidores frontend vivos que corren para roles no-dueño abiertos en esta misma fase — la columna "Programa" de `/planes` (que esta fase abrió a todo el staff en modo lectura) y el diálogo "Asignar programa adicional" (cuyo POST de asignación sigue permitido a gestion/recepcion vía FINANCE_WRITE_ROLES).

La corrección separa **lectura de escritura** en el módulo programs, espejando el patrón ya aplicado a Planes en el Plan 01 (`PLANES_WRITE_ROLES` vs `PLANES_READ_ROLES`), pero **respetando D-10**: Programas (rutinas) NO se le muestra al profe (coach) — la opción "todo incluido programas" fue explícitamente rechazada en la discusión de fase. El GET de listado se reabre sólo a `['owner','admin','gestion','recepcion']` (`PROGRAMAS_LIST_ROLES`, espejo de `FINANCE_WRITE_ROLES` — coach tampoco puede asignar add-ons, así que no tiene necesidad funcional del catálogo). Coach sigue recibiendo 403 en el GET, y por eso `loadPrograms()` en PlanesPage se gatea por rol para que coach no dispare el 403 ni el evento de Sentry. Los otros 6 handlers del CRUD admin siguen dueño-only (`PROGRAMAS_ROLES`, D-15). El diálogo de add-ons queda consistente con su POST ya abierto: gestion/recepcion listan y asignan; coach no ve ni lo uno ni lo otro.

Purpose: dejar "Planes en modo lectura" funcional para el staff administrativo (columna Programa real para gestion/recepcion) y eliminar el spam a Sentry en cada visita de empleado, sin exponer Programas al coach (D-10).
Output: PROGRAMAS_LIST_ROLES (sin coach) + GET list reabierto a staff administrativo + test de contrato por rol (gestion 200 / coach 403) + loadPrograms gateado por rol y sin reporte a Sentry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-nav-por-categor-as-rbac/149-VERIFICATION.md
@.planning/phases/149-nav-por-categor-as-rbac/149-REVIEW.md
@.planning/phases/149-nav-por-categor-as-rbac/149-01-SUMMARY.md
@.planning/phases/149-nav-por-categor-as-rbac/149-CONTEXT.md
@./CLAUDE.md

<interfaces>
<!-- Extraído del código. El executor NO necesita explorar el codebase. -->

Sets de rol actuales en `el-templo-api/src/modules/shared/permissions.ts`:

- `ALL_STAFF_ROLES = ["coach","admin","owner","gestion","recepcion"]` (todo el staff)
- `ADMIN_ROLES = ["admin","owner"]` (Dueño)
- `PROGRAMAS_ROLES = ADMIN_ROLES` (dueño-only, aplicado hoy a los 7 handlers admin de programs)
- `FINANCE_WRITE_ROLES = ["owner","admin","gestion","recepcion"]` (SIN coach — gatea el POST de add-ons, D-22) ← PROGRAMAS_LIST_ROLES lo espeja
- `PLANES_WRITE_ROLES = ADMIN_ROLES` / `PLANES_READ_ROLES = SUBSCRIPTION_ROLES` (precedente lectura/escritura a espejar en forma, no en valor)

`el-templo-api/src/modules/programs/routes.ts` — guards actuales (todos `PROGRAMAS_ROLES`, expresión `(PROGRAMAS_ROLES as readonly string[]).includes(role)`):

- L191 `POST /admin/programs` (create) — guard en L196
- L212 `GET /admin/programs` (list) — guard en L217 ← ÚNICO que se reabre
- L235 `GET /admin/programs/analytics` — guard en L240
- L280 `GET /admin/programs/:programId` (detail) — guard en L285
- L307 `PUT /admin/programs/:programId` — guard en L315
- L331 `POST /admin/programs/:programId/content` — guard en L342
- L361 `POST /admin/programs/:programId/deactivate` — guard en L366
- Enrollments (`COACH_ROLES`, D-35) y program-addons POST (`FINANCE_WRITE_ROLES`, D-22) NO se tocan.
  Import actual (L23-27): `{ PROGRAMAS_ROLES, COACH_ROLES, FINANCE_WRITE_ROLES }` desde `../shared/permissions`.
  OJO grep: "PROGRAMAS_ROLES" también aparece en comentarios (header L7, cabecera de bloque L185) — los criterios de aceptación cuentan la expresión de guard, no el token pelado.

Consumidores frontend:

- `el-templo-admin/src/composables/useProgramsApi.ts:26 getPrograms()` → `GET /admin/programs`; `:161 listActivePrograms()` llama a `getPrograms()`.
- `PlanesPage.vue:639-646 loadPrograms()` → `getPrograms()` (poblado de la columna "Programa"), invocada en onMounted L775. PlanesPage ya tiene `authStore` (usa `isOwner` y `canEditPlans = ['owner','admin'].includes(authStore.user?.role ?? '')` — patrón a replicar para el gating por rol).
- `AssignProgramAddonDialog.vue:109-121 loadPrograms()` → `listActivePrograms()`. Se abre desde MemberProgramsTab; su POST de asignación usa FINANCE_WRITE_ROLES (sin coach), así que con PROGRAMAS_LIST_ROLES = espejo de FINANCE_WRITE_ROLES el flujo queda consistente sin tocar el diálogo.

Test D-15 existente en `el-templo-api/test/programs.test.ts` (bloque `describe("RBAC Dueño-only (D-15)")`, L442-512): crea `gestionToken` en beforeEach (patrón `createStaffUser` + `getAuthToken`, L445-459) y recorre un array de endpoints afirmando gestion → 403 para TODOS, incluido `{ method: "GET", url: "/api/admin/programs" }` (L468). Ese GET debe SALIR del loop de 403 (para gestion) y pasar a dos casos propios: gestion → 200 y coach → 403.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Separar lectura/escritura en programs (PROGRAMAS_LIST_ROLES sin coach, D-10) + fijar contrato por rol</name>
  <files>el-templo-api/src/modules/shared/permissions.ts, el-templo-api/src/modules/programs/routes.ts, el-templo-api/test/programs.test.ts, el-templo-api/test/rbac-sets.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/shared/permissions.ts (ver PROGRAMAS_ROLES L186, FINANCE_WRITE_ROLES L192 — el valor a espejar; PLANES_READ_ROLES L173 — el patrón lectura/escritura)
    - el-templo-api/src/modules/programs/routes.ts (L184-377: los 7 handlers admin; el GET list está en L212-228, guard en L217; import en L23-27; comentarios con "PROGRAMAS_ROLES" en L7 y L185)
    - el-templo-api/test/programs.test.ts (bloque D-15 L442-512: beforeEach con gestionToken L445-459, array de endpoints L461-497, assert de 403 L499-510; patrón createStaffUser/getAuthToken para crear el coachToken nuevo)
    - el-templo-api/test/rbac-sets.test.ts (patrón de deep-equal de sets efectivos, para agregar la aserción de PROGRAMAS_LIST_ROLES)
    - .planning/phases/149-nav-por-categor-as-rbac/149-CONTEXT.md (D-10: Programas NO se le muestra al empleado-profe — fundamento de excluir coach del set)
  </read_first>
  <behavior>
    - GET /api/admin/programs con token gestion → 200 (recepcion también está en el set)
    - GET /api/admin/programs con token coach → 403 (D-10: Programas no es superficie del profe)
    - POST /api/admin/programs con token gestion → 403 (sin cambios)
    - GET /api/admin/programs/analytics, GET /api/admin/programs/:id, PUT, POST /:id/content, POST /:id/deactivate con token gestion → 403 (sin cambios)
    - PROGRAMAS_LIST_ROLES es deep-equal a ["owner","admin","gestion","recepcion"]
  </behavior>
  <action>
    En `permissions.ts`, agregar un export nuevo `PROGRAMAS_LIST_ROLES = FINANCE_WRITE_ROLES` (valor efectivo `["owner","admin","gestion","recepcion"]` — SIN coach) inmediatamente después de `PROGRAMAS_ROLES` (L186), con JSDoc que explique: el catálogo de programas (GET /admin/programs list) es legible por el staff administrativo porque `PlanesPage.vue` (columna Programa) y `AssignProgramAddonDialog.vue` (cuyo POST de asignación usa FINANCE_WRITE_ROLES, D-22) dependen de él; coach queda EXCLUIDO per D-10 (Programas es superficie de entrenamiento que no se le muestra al profe — y coach tampoco puede asignar add-ons, así que no tiene uso funcional del catálogo); la escritura/detalle/analytics sigue dueño-only vía `PROGRAMAS_ROLES` (D-15). Documentar que espeja en forma el par `PLANES_WRITE_ROLES`/`PLANES_READ_ROLES` y en valor a `FINANCE_WRITE_ROLES`. Si reusar la referencia `FINANCE_WRITE_ROLES` genera problema de orden de declaración en el archivo, declarar el literal `["owner","admin","gestion","recepcion"] as const` y fijar la equivalencia en el test. NO modificar `PROGRAMAS_ROLES`.

    En `programs/routes.ts`: agregar `PROGRAMAS_LIST_ROLES` al import desde `../shared/permissions` (L23-27). En el handler `GET /admin/programs` (list, L212-228), reemplazar la única referencia `PROGRAMAS_ROLES` de su guard (L217) por `PROGRAMAS_LIST_ROLES`. NO tocar los otros 6 guards `PROGRAMAS_ROLES` (create L196, analytics L240, detail L285, put L315, content L342, deactivate L366). Actualizar los comentarios de cabecera (L7 y L185) para reflejar "list = PROGRAMAS_LIST_ROLES (staff administrativo, sin coach — D-10); resto dueño-only (PROGRAMAS_ROLES, D-15)".

    En `programs.test.ts` (bloque D-15, L461-511): quitar `{ method: "GET", url: "/api/admin/programs" }` (L468) del array `endpoints` que afirma gestion → 403. Agregar dos casos nuevos: (a) `it("gestion puede LISTAR programas — GET /admin/programs → 200 (CR-01)")` que inyecte GET `/api/admin/programs` con el `gestionToken` y afirme `res.statusCode === 200` y que el body tenga la propiedad `programs` (array); (b) `it("coach NO puede listar programas — GET /admin/programs → 403 (D-10)")` creando un usuario coach con el mismo patrón `createStaffUser`/`getAuthToken` del beforeEach (email único con timestamp, role "coach") y afirmando 403. Dejar el resto del loop de 403 intacto (create/analytics/detail/put/content/deactivate).

    En `rbac-sets.test.ts`: agregar un caso que afirme `PROGRAMAS_LIST_ROLES` deep-equal a `["owner","admin","gestion","recepcion"]` (siguiendo el estilo de los deep-equal existentes de no-regresión), importando el nuevo set.

  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit && pnpm test test/programs.test.ts && pnpm test rbac-sets</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "PROGRAMAS_LIST_ROLES" el-templo-api/src/modules/shared/permissions.ts` devuelve el export nuevo, y su valor efectivo NO incluye "coach"
    - `grep -c "PROGRAMAS_LIST_ROLES as readonly" el-templo-api/src/modules/programs/routes.ts` == 1 (sólo el guard del GET list)
    - `grep -c "PROGRAMAS_ROLES as readonly" el-templo-api/src/modules/programs/routes.ts` == 6 (los 6 guards restantes; se cuenta la expresión de guard porque el token pelado también aparece en comentarios e import)
    - El import de programs/routes.ts contiene `PROGRAMAS_LIST_ROLES`
    - En `programs.test.ts`, el array de endpoints del test de 403 ya NO contiene `url: "/api/admin/programs"` con method GET; existen dos `it` nuevos: gestion → 200 y coach → 403 en GET /admin/programs
    - `pnpm test test/programs.test.ts` pasa (gestion 200 en list, coach 403 en list, gestion 403 en los 6 restantes)
    - `rbac-sets.test.ts` afirma PROGRAMAS_LIST_ROLES deep-equal `["owner","admin","gestion","recepcion"]` y pasa
    - `pnpm exec tsc --noEmit` sin errores
  </acceptance_criteria>
  <done>GET /admin/programs (list) legible por owner/admin/gestion/recepcion; coach → 403 (D-10); los 6 handlers admin restantes siguen dueño-only; contrato fijado por tests verdes.</done>
</task>

<task type="auto">
  <name>Task 2: loadPrograms() en PlanesPage.vue gateado por rol (coach no fetchea) y sin spam a Sentry</name>
  <files>el-templo-admin/src/pages/PlanesPage.vue</files>
  <read_first>
    - el-templo-admin/src/pages/PlanesPage.vue (L639-646 loadPrograms; onMounted L775 donde se invoca; L415 aprox. el computed canEditPlans `['owner','admin'].includes(authStore.user?.role ?? '')` — patrón a replicar; L624-637 loadPlans como referencia de estilo)
    - el-templo-admin/src/utils/logger.ts (confirmar que `log.warn` NO reporta a Sentry y `log.error` SÍ — L12: "error() also sends to Sentry when initialized")
  </read_first>
  <action>
    En `PlanesPage.vue`:

    1. Agregar un computed `canViewPrograms = computed(() => ['owner','admin','gestion','recepcion'].includes(authStore.user?.role ?? ''))` junto a `canEditPlans` (mismo patrón, mismo estilo), con comentario que referencie D-10 (coach no ve Programas) y que el valor espeja `PROGRAMAS_LIST_ROLES` de la API.
    2. Gatear el fetch: en `onMounted` (L775) — o al inicio de `loadPrograms()` con early-return — invocar el fetch de programas SOLO si `canViewPrograms.value` es true. Para coach, `programs.value` queda `[]` y la columna "Programa" degrada naturalmente a "—" sin request, sin 403 y sin evento de log.
    3. En el `catch` de `loadPrograms()` (L642-645): cambiar `log.error('Error loading programs', ...)` por `log.warn('Error loading programs', { error: message })` — la columna es decoración no crítica; un fallo de red no debe generar un evento de Sentry.

    NO tocar `loadPlans()` (L624-637) ni `loadPromos()` (L692-702): sus catches quedan como están. NO ocultar la columna del template (degradar a "—" es suficiente y evita tocar el layout de la tabla para un solo rol).

  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm exec vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -v "session-pdf-builder\|session-data-transformer" | grep -c "PlanesPage" ; pnpm exec eslint src/pages/PlanesPage.vue</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "canViewPrograms" el-templo-admin/src/pages/PlanesPage.vue` >= 2 (declaración + uso en el gating del fetch)
    - El array del computed `canViewPrograms` NO incluye 'coach'
    - `grep -n "log.error" el-templo-admin/src/pages/PlanesPage.vue` NO aparece dentro del bloque `loadPrograms`
    - `grep -A6 "async function loadPrograms" el-templo-admin/src/pages/PlanesPage.vue` contiene `log.warn`
    - `loadPlans` sigue usando `log.error` + `$q.notify` (sin cambios)
    - `eslint src/pages/PlanesPage.vue` → 0 errores
  </acceptance_criteria>
  <done>Coach no dispara el fetch de programas (columna "—", cero Sentry); gestion/recepcion/dueño ven la columna Programa real; fallo de red degrada a warn.</done>
</task>

</tasks>

<verification>
- API: `pnpm exec tsc --noEmit` verde; `pnpm test test/programs.test.ts` verde (gestion 200 en list, coach 403 en list, gestion 403 en los 6 handlers restantes); `pnpm test rbac-sets` verde.
- Admin: vue-tsc/eslint de PlanesPage.vue sin errores.
- Grep: `PROGRAMAS_LIST_ROLES as readonly` == 1 en programs/routes.ts; `PROGRAMAS_ROLES as readonly` == 6; `canViewPrograms` en PlanesPage sin 'coach'.

**UAT visual (pendiente, no bloquea el plan — requiere sesión real por rol):**

1. Como gestion/recepcion, abrir `/planes`: la columna "Programa" muestra el programa vinculado real (no "—" en todos los planes online).
2. Como coach, abrir `/planes`: la columna "Programa" muestra "—" y NO hay request a /admin/programs (verificar en Network) ni evento en Sentry.
3. Como gestion, abrir la tab Programas de un alumno → "Asignar programa adicional": el select carga las opciones de programa (no "No se pudieron cargar los programas").
4. Confirmar en Sentry que dejó de llegar el evento "Error loading programs" en visitas de empleado a /planes.
   </verification>

<success_criteria>

- GET /admin/programs (list) devuelve 200 para owner/admin/gestion/recepcion y 403 para coach (contrato D-10 fijado por test de integración).
- Los 6 handlers admin restantes siguen dueño-only (gestion→403).
- Coach no dispara fetch/403/Sentry en /planes; gestion/recepcion ven la columna Programa real.
- NAV-03 ("Planes en modo lectura") queda funcional para el empleado sin exponer Programas al profe.
  </success_criteria>

<output>
Create `.planning/phases/149-nav-por-categor-as-rbac/149-05-SUMMARY.md` when done
</output>
