---
phase: 149-nav-por-categor-as-rbac
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-admin/src/config/templo-config.ts
  - el-templo-admin/src/layouts/AdminLayout.vue
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, NAV-04]
must_haves:
  truths:
    - "El drawer del admin se agrupa en Finanzas / Alumnos / Horarios / Planes + Configuración + Templo"
    - "El empleado (coach/gestion/recepcion) ve el header Finanzas con al menos Pagos dentro; Caja y Analíticas no aparecen para él"
    - "Entrenamiento (Sesiones/Programador/Ejercicios/Árbol), Campañas, Profes y landing viven en la sección Templo al final, gateadas por el flag Templo + rol"
    - "El header de una categoría se muestra sólo si tiene ≥1 item visible para el rol actual"
    - "No queda ningún item de Configuración de Caja en el drawer"
  artifacts:
    - path: "el-templo-admin/src/config/templo-config.ts"
      provides: "Nav-model declarativo (categorías→items→roles) + flag Templo + sets de rol espejo del backend"
      min_lines: 60
      contains: "NAV_MODEL"
    - path: "el-templo-admin/src/layouts/AdminLayout.vue"
      provides: "Drawer derivado del nav-model; sin los 7 computed ad-hoc"
      contains: "NAV_MODEL"
  key_links:
    - from: "el-templo-admin/src/layouts/AdminLayout.vue"
      to: "el-templo-admin/src/config/templo-config.ts"
      via: "import de NAV_MODEL + helpers de visibilidad"
      pattern: "templo-config"
    - from: "el-templo-admin/src/config/templo-config.ts"
      to: "el-templo-admin/src/utils/trainingAccess.ts"
      via: "canAccessTraining para items trainingOnly"
      pattern: "canAccessTraining"
---

<objective>
Crear el modelo de navegación declarativo único del admin (`templo-config.ts`) y refactorizar el drawer de `AdminLayout.vue` para derivarse de él, agrupando el nav plano actual en las categorías MVP **Finanzas / Alumnos / Horarios / Planes** + una sección **Configuración** + una sección **Templo** al final (D-07/D-08/D-12). Elimina los 7 computed ad-hoc que hoy duplican los `allowedRoles` con comentarios "keep in sync" (la deuda DRY que CONTEXT pide resolver).

Purpose: Una sola fuente de verdad para la visibilidad del drawer. El nav esconde (no es seguridad — la API es, D-04). Deja las features Templo/marketing fuera del nav MVP sin borrarlas, gateadas por un flag central que la tenancy futura convierte en config por tenant (D-06).
Output: `templo-config.ts` con NAV_MODEL + sets de rol espejo + helpers de visibilidad; drawer derivado, con secciones Configuración y Templo, sin el item de Configuración de Caja.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-nav-por-categor-as-rbac/149-CONTEXT.md
@.planning/phases/149-nav-por-categor-as-rbac/149-RESEARCH.md
@.planning/phases/149-nav-por-categor-as-rbac/149-PATTERNS.md

<interfaces>
<!-- Tipos existentes (extraídos del código) -->
// src/types/admin.ts
type AdminRole = 'gestion' | 'coach' | 'admin' | 'owner' | 'recepcion';
interface AdminUser { id: number; email: string; firstName: string; lastName: string; role: AdminRole; branchId: number; }

// src/utils/trainingAccess.ts (espejo del backend, JSDoc que cita permissions.ts)
canAccessTraining(user: Pick<AdminUser,'role'|'email'> | null | undefined): boolean // true para owner o el email de Fran Scaine

// src/stores/useAdminStore.ts
adminStore.pendingCount: number // badge en Sesiones
adminStore.lowSessionsAlert: boolean // banner de sesiones bajas

<!-- Íconos existentes a reusar (del drawer actual) -->

people(Alumnos) calendar_month(Horarios) point_of_sale(Pagos/Caja) request_quote(Deudas)
card_membership(Planes) school(Programas/Academy) analytics(Analíticas) summarize(Reportes)
campaign(Campañas) groups(Profes) fitness_center(Sesiones/Gladius) auto_awesome(Programador)
sports_gymnastics(Ejercicios) hub(Árbol) manage_accounts(Usuarios) notifications(Notificaciones)
article(Blog) notifications_active(App Waitlist) business(Labs) store(Franquicias)
</interfaces>

<rbac_target>

<!-- Matriz de roles por item (D-01/D-02/D-03/D-15). "Dueño" = admin+owner; "Empleado" = coach+gestion+recepcion. -->

Finanzas:

- Pagos → todo staff [coach,gestion,recepcion,admin,owner] (espeja FINANCE_LOAD_ROLES; recepcion INCLUIDA)
- Deudas → [coach,gestion,admin,owner] (dueño + override Templo coach+gestion — espeja COACH_DEBTS_ROLES)
- Reportes → [gestion,admin,owner] (dueño + override Templo gestion — espeja CAJA_ROLES)
- Caja → [admin,owner] (dueño-only)
- Analíticas → [admin,owner] (dueño-only)
  Alumnos:
- Alumnos → todo staff
  Horarios:
- Horarios → todo staff
  Planes:
- Planes → todo staff (read-only para empleado; edición se oculta en PlanesPage, Plan 04)
- Programas → [admin,owner] (dueño-only, D-15)
  Configuración:
- Usuarios → [owner]
- Notificaciones → [admin,owner]
  Templo (flag TEMPLO_ENABLED):
- Sesiones/Programador/Ejercicios/Árbol → trainingOnly (canAccessTraining, D-08)
- Campañas → [admin,owner]
- Profes (/puntuaciones) → [owner]
- Blog / Gladius / Academy / App Waitlist / Labs Inquiries / Franquicias → [owner]
  </rbac_target>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Crear templo-config.ts (nav-model + sets de rol espejo + helpers)</name>
  <read_first>
    - el-templo-admin/src/layouts/AdminLayout.vue (drawer actual líneas 17-186, 7 computed 228-259 — la duplicación a reemplazar; badge de Sesiones línea 27, banner 190-202)
    - el-templo-admin/src/router/routes.ts (allowedRoles por ruta — valores de referencia)
    - el-templo-admin/src/types/admin.ts (AdminRole, AdminUser)
    - el-templo-admin/src/utils/trainingAccess.ts (patrón espejo con JSDoc que cita el backend)
    - el-templo-api/src/modules/shared/permissions.ts (sets a espejar: ADMIN_ROLES, CAJA_ROLES, COACH_DEBTS_ROLES, FINANCE_LOAD_ROLES, SUBSCRIPTION_ROLES)
    - .planning/phases/149-nav-por-categor-as-rbac/149-RESEARCH.md (Pattern 1, §Router/Landing, Pitfall 4)
  </read_first>
  <action>
    Crear `el-templo-admin/src/config/templo-config.ts`. Regla de dirección de imports: este config importa de `types/admin.ts` y `utils/trainingAccess.ts`; NADA del core importa este config.

    (a) Exportar `TEMPLO_ENABLED = true` con JSDoc: gate central de features Templo; cuando llegue la tenancy pasa a ser config por tenant (cita D-06 + `.docs/saas-multitenancy/04-mecanismo-modulos.md`).

    (b) Exportar sets de rol `AdminRole[]` como constantes, cada uno con JSDoc que cite el set espejo de `shared/permissions.ts`, usando exactamente los valores de <rbac_target>: `DUENO_ROLES` (['admin','owner']), `ALL_STAFF_ROLES`, `PAGOS_ROLES` (todo staff, incluye recepcion — espeja FINANCE_LOAD_ROLES), `DEUDAS_ROLES` (['coach','gestion','admin','owner']), `REPORTES_ROLES` (['gestion','admin','owner']), `PLANES_READ_ROLES` (todo staff), `CAJA_SALDOS_ROLES` (['admin','owner']), `ANALITICAS_ROLES` (['admin','owner']).

    (c) Exportar interfaces `NavItem { path: string; label: string; icon: string; roles?: AdminRole[]; trainingOnly?: boolean; templo?: boolean; badge?: 'pending' }` y `NavCategory { header: string; items: NavItem[]; templo?: boolean }`.

    (d) Exportar `NAV_MODEL: NavCategory[]` con las 6 categorías y sus items exactamente como en <rbac_target>, reusando los íconos de <interfaces>. La categoría Templo lleva `templo: true`; sus items de Entrenamiento llevan `trainingOnly: true` (sin `roles`), y Sesiones lleva `badge: 'pending'`. Los paths coinciden con las rutas actuales (`/pagos`, `/deudas`, `/reportes`, `/caja`, `/analiticas`, `/alumnos`, `/horarios`, `/planes`, `/programas`, `/usuarios`, `/notificaciones`, `/sessions`, `/generate`, `/exercises`, `/tree-map`, `/campanias`, `/puntuaciones`, `/blog`, `/gladius`, `/academy`, `/app-waitlist`, `/labs-inquiries`, `/franquicias`). NO incluir `/configuracion-caja` (D-13).

    (e) Exportar helpers puros: `isNavItemVisible(item: NavItem, user: AdminUser | null): boolean` = (si `item.trainingOnly` → `canAccessTraining(user)`; sino → `!!user && item.roles.includes(user.role)`); un item de categoría templo además requiere `TEMPLO_ENABLED`. Y `isNavCategoryVisible(cat: NavCategory, user): boolean` = (si `cat.templo` y no `TEMPLO_ENABLED` → false) y `cat.items.some(i => isNavItemVisible(i, user))` (Pitfall 4: header visible sólo si ≥1 item visible).

  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm run lint src/config/templo-config.ts && pnpm run build</automated>
  </verify>
  <acceptance_criteria>
    - `el-templo-admin/src/config/templo-config.ts` existe y exporta `NAV_MODEL`, `TEMPLO_ENABLED`, `isNavItemVisible`, `isNavCategoryVisible`
    - `grep -c "header:" el-templo-admin/src/config/templo-config.ts` == 6 (Finanzas, Alumnos, Horarios, Planes, Configuración, Templo)
    - `grep -c "configuracion-caja" el-templo-admin/src/config/templo-config.ts` == 0
    - `grep -c "trainingOnly" el-templo-admin/src/config/templo-config.ts` ≥ 4 (los 4 items de Entrenamiento)
    - `pnpm run build` compila el admin sin errores de tipos
  </acceptance_criteria>
  <done>templo-config.ts declara las 6 categorías con sus roles/gates y helpers de visibilidad; compila; sin item de config-caja.</done>
</task>

<task type="auto">
  <name>Task 2: Refactorizar el drawer de AdminLayout.vue para derivar del nav-model</name>
  <read_first>
    - el-templo-admin/src/layouts/AdminLayout.vue (archivo completo: template drawer 17-186, script 209-283, los 7 computed 228-259)
    - el-templo-admin/src/config/templo-config.ts (NAV_MODEL + helpers creados en Task 1)
    - el-templo-admin/src/utils/trainingAccess.ts (canSeeTraining base)
    - .planning/phases/149-nav-por-categor-as-rbac/149-PATTERNS.md (§AdminLayout.vue — patrón visual a mantener)
  </read_first>
  <action>
    Reescribir el `<q-list>` del drawer para iterar `NAV_MODEL` filtrado por `isNavCategoryVisible(cat, authStore.user)`; por cada categoría visible renderizar un `<q-item-label header>{{ cat.header }}</q-item-label>` (precedido de `<q-separator>` salvo la primera categoría visible) y, dentro, los items con `isNavItemVisible(item, authStore.user)` como `<q-item clickable v-ripple :to="item.path">` con `<q-icon :name="item.icon" />` y el label. MANTENER el patrón visual D-12 (headers + items planos, `q-item-label header` + `q-item`, sin expansión).

    Casos especiales a preservar: el item con `badge: 'pending'` (Sesiones) muestra el `<q-badge color="negative" :label="adminStore.pendingCount" />` cuando `adminStore.pendingCount > 0` (como hoy, líneas 27-29). El banner de "sesiones bajas" (líneas 190-202) y los `onMounted`/`watch` que hacen fetch sólo para viewers de training (`canSeeTraining`) se conservan intactos.

    ELIMINAR los 7 computed ad-hoc (`isAdminRole`, `isCajaRole`, `isCoachDebtsRole`, `isOwnerRole`, `isPagosVisible`, `isCajaSaldosRole` — líneas 235-259). Conservar `canSeeTraining` (línea 232) sólo si aún se usa para los fetch/banner de training; la visibilidad de los items de training ahora viene de `isNavItemVisible`. Borrar el `<q-item to="/configuracion-caja">` (líneas 179-184) — ya no está en el modelo. El resultado: el drawer ya no declara roles inline, todo deriva de `templo-config.ts`.

  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm run lint src/layouts/AdminLayout.vue && pnpm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isAdminRole\|isCajaRole\|isCoachDebtsRole\|isOwnerRole\|isPagosVisible\|isCajaSaldosRole" el-templo-admin/src/layouts/AdminLayout.vue` == 0 (los 7 computed eliminados)
    - `grep -c "configuracion-caja" el-templo-admin/src/layouts/AdminLayout.vue` == 0
    - `grep -c "NAV_MODEL\|isNavCategoryVisible\|isNavItemVisible" el-templo-admin/src/layouts/AdminLayout.vue` ≥ 2 (el drawer deriva del modelo)
    - `grep -c "adminStore.pendingCount" el-templo-admin/src/layouts/AdminLayout.vue` ≥ 1 (badge de Sesiones preservado)
    - `pnpm run build` compila el admin sin errores
  </acceptance_criteria>
  <done>El drawer itera NAV_MODEL con headers por categoría visible (≥1 item), preserva el badge de Sesiones y el banner; sin los 7 computed ni el item de config-caja.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                                                      |
| ---------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| rol del usuario → visibilidad del drawer | El nav SÓLO esconde items; NO es seguridad. La seguridad real vive en la API (D-04, Plan 01 + guards existentes) |

## STRIDE Threat Register

| Threat ID | Category                          | Component                                                                            | Disposition | Mitigation Plan                                                                                                                      |
| --------- | --------------------------------- | ------------------------------------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| T-149-07  | Information Disclosure (UX)       | Header de categoría vacío para el empleado (Pitfall 4)                               | mitigate    | `isNavCategoryVisible` exige ≥1 item visible; el empleado ve "Finanzas" con al menos Pagos                                           |
| T-149-08  | Elevation of Privilege (aparente) | Confiar en el nav para seguridad                                                     | accept      | El drawer sólo esconde; los 403 los da la API (Plan 01). Documentado en JSDoc del config                                             |
| T-149-09  | Elevation of Privilege            | Programas oculto en nav a gestion pero API programs aún permite CAJA_ROLES (gestion) | accept      | Diferido a fase 156 (D-10/D-15): el gating final de Programas como Templo se decide allí; en 149 sólo cambia la visibilidad del item |
| T-149-SC  | Tampering                         | Instalación de paquetes                                                              | accept      | Esta fase NO instala paquetes — sin superficie de supply chain                                                                       |

</threat_model>

<verification>
- `cd el-templo-admin && pnpm run build` compila sin errores de tipos.
- `pnpm run lint` sin errores nuevos en los archivos tocados.
- **UAT visual (frontend sin test runner):** loguearse con roles owner, admin, gestion, coach, recepcion y confirmar: (1) las 6 categorías aparecen para dueño; (2) el empleado ve Finanzas con Pagos (coach/gestion también Deudas; gestion también Reportes), Alumnos, Horarios, Planes; (3) Caja/Analíticas ocultas para empleado; (4) sección Templo al final con Entrenamiento sólo para owner/Fran; (5) ningún item de Configuración de Caja.
</verification>

<success_criteria>

- Drawer agrupado en Finanzas / Alumnos / Horarios / Planes + Configuración + Templo (NAV-01/NAV-04).
- Empleado ve Finanzas→Pagos + Planes; dueño ve todo (NAV-02/NAV-03).
- Los 7 computed ad-hoc eliminados; visibilidad derivada de una única fuente (DRY resuelta).
  </success_criteria>

<output>
Create `.planning/phases/149-nav-por-categor-as-rbac/149-03-SUMMARY.md` when done
</output>
