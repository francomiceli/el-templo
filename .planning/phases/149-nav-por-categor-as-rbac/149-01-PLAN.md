---
phase: 149-nav-por-categor-as-rbac
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/shared/permissions.ts
  - el-templo-api/src/modules/subscriptions/routes.ts
  - el-templo-api/src/modules/programs/routes.ts
  - el-templo-api/test/rbac-sets.test.ts
  - el-templo-api/test/subscriptions/plans-crud.test.ts
  - el-templo-api/test/programs.test.ts
autonomous: true
requirements: [NAV-02, NAV-03]
must_haves:
  truths:
    - "Un coach que llama por API a POST/PUT/PATCH de plans o promo-plans recibe 403"
    - "Un coach que llama a GET /plans sigue recibiendo 200 (lectura abierta a staff)"
    - "admin y owner (dueño) pueden crear/editar/archivar planes por API"
    - "Un gestion que llama al CRUD de admin de programs (crear/editar/listar/detalle/analytics/content/desactivar) recibe 403 — Programas es dueño-only por API (D-15/D-04, sin puerta trasera)"
    - "admin y owner siguen operando el CRUD de admin de programs (200/201)"
    - "Los sets efectivos de Reportes y Deudas para El Templo quedan byte-idénticos a hoy (gestion y coach no pierden acceso)"
    - "El set CAJA_ROLES en sí NO cambia (reports/leads/coach lo siguen consumiendo con el mismo valor)"
  artifacts:
    - path: "el-templo-api/src/modules/shared/permissions.ts"
      provides: "Sets core white-label + TEMPLO_RBAC_OVERRIDES + PLANES_WRITE_ROLES/PLANES_READ_ROLES + PROGRAMAS_ROLES"
      contains: "TEMPLO_RBAC_OVERRIDES"
    - path: "el-templo-api/src/modules/subscriptions/routes.ts"
      provides: "Guards per-handler dueño-only en los 7 writes de plans/promo-plans"
      contains: "PLANES_WRITE_ROLES"
    - path: "el-templo-api/src/modules/programs/routes.ts"
      provides: "CRUD de admin de programs cerrado dueño-only (7 handlers que hoy usan CAJA_ROLES pasan a PROGRAMAS_ROLES)"
      contains: "PROGRAMAS_ROLES"
    - path: "el-templo-api/test/rbac-sets.test.ts"
      provides: "No-regresión de sets efectivos + verificación de sets nuevos"
    - path: "el-templo-api/test/subscriptions/plans-crud.test.ts"
      provides: "Casos coach 403 en writes + coach 200 en GET /plans (D-11)"
    - path: "el-templo-api/test/programs.test.ts"
      provides: "Caso gestion→403 en el CRUD de admin de programs (D-15) + dueño→200 (no regresión)"
  key_links:
    - from: "el-templo-api/src/modules/subscriptions/routes.ts"
      to: "PLANES_WRITE_ROLES"
      via: "import desde ../shared/permissions"
      pattern: "PLANES_WRITE_ROLES"
    - from: "el-templo-api/src/modules/programs/routes.ts"
      to: "PROGRAMAS_ROLES"
      via: "import desde ../shared/permissions (reemplaza CAJA_ROLES en los 7 handlers de CRUD admin)"
      pattern: "PROGRAMAS_ROLES"
    - from: "el-templo-api/src/modules/shared/permissions.ts"
      to: "TEMPLO_RBAC_OVERRIDES"
      via: "composición core + override para CAJA_ROLES/COACH_DEBTS_ROLES"
      pattern: "TEMPLO_RBAC_OVERRIDES"
---

<objective>
Endurecer el RBAC de la API para la reforma del admin: (1) re-expresar los sets de roles como "core white-label (2 niveles) + overrides Templo" en un solo lugar (D-01/D-02/D-03/D-06), (2) cerrar el bug de escalación de privilegios confirmado en D-11 — hoy un coach puede crear/editar/archivar planes y promos por API porque los writes no tienen guard per-handler, y (3) cerrar la puerta trasera de Programas por API (D-15/D-04): el CRUD de admin de programs hoy usa `CAJA_ROLES` (incluye gestion), pero Programas es dueño-only — hay que angostarlo a owner/admin.

Purpose: La seguridad real vive en la API (D-04). El nav esconde; la API bloquea. Sin este endurecimiento, ocultar la edición de Planes y el item de Programas en el frontend deja puertas traseras por API (coach escribe planes; gestion opera Programas por URL directa).
Output: `permissions.ts` con los sets nuevos, guards per-handler en los 7 writes de subscriptions, CRUD de programs cerrado dueño-only, y tests que prueban 403 para el rol degradado / 200 para el dueño, más una no-regresión de sets efectivos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-nav-por-categor-as-rbac/149-CONTEXT.md
@.planning/phases/149-nav-por-categor-as-rbac/149-RESEARCH.md
@.planning/phases/149-nav-por-categor-as-rbac/149-PATTERNS.md

<interfaces>
<!-- Sets actuales relevantes en shared/permissions.ts (extraídos del código). El executor los re-expresa; el valor efectivo de los EXISTENTES NO debe cambiar. -->
ADMIN_ROLES        = ["admin", "owner"]                              // = "Dueño" (D-01)
CAJA_ROLES         = ["gestion", "admin", "owner"]                   // consumido por reports/routes.ts:55, members/leads-routes.ts Y HOY por programs/routes.ts (CRUD admin) — este plan saca a programs de CAJA_ROLES; NO cambia el valor del set
COACH_DEBTS_ROLES  = ["coach", "gestion", "admin", "owner"]          // consumido por coach/routes.ts:26 (tab Deudas)
COACH_ROLES        = ["coach", "admin", "owner"]                     // consumido por programs enrollment (línea 262) — NO tocar
FINANCE_WRITE_ROLES= ["owner","admin","gestion","recepcion"]         // consumido por programs program-addons (línea 420, PoS) — NO tocar
SUBSCRIPTION_ROLES = ["coach", "admin", "owner", "gestion", "recepcion"]  // guard module-wide subscriptions (NO tocar)
FINANCE_LOAD_ROLES = [...FINANCE_WRITE_ROLES, "coach"]               // patrón de composición por spread (imitar)

<!-- Patrón per-handler 403 a copiar (finance/routes.ts:1047) -->

if (!(ADMIN_ROLES as readonly string[]).includes(request.user.role)) {
return reply.code(403).send({ error: "Acceso denegado", message: "Solo owner/admin" });
}

<!-- programs/routes.ts: 7 handlers de CRUD admin que HOY chequean CAJA_ROLES (líneas 196, 217, 240, 285, 315, 342, 366): POST /admin/programs, GET /admin/programs, GET /admin/programs/analytics, GET /admin/programs/:programId, PUT /admin/programs/:programId, POST /admin/programs/:programId/content, POST /admin/programs/:programId/deactivate. Los OTROS handlers del módulo NO son dueño-only y NO se tocan: enrollments/user/:userId (línea 262, COACH_ROLES, D-35) y users/:userId/program-addons (línea 420, FINANCE_WRITE_ROLES, PoS D-22). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sets core + overrides Templo + PROGRAMAS_ROLES en permissions.ts + test de no-regresión</name>
  <read_first>
    - el-templo-api/src/modules/shared/permissions.ts (sets actuales, patrón `as const` + JSDoc + spread-compose de FINANCE_LOAD_ROLES)
    - el-templo-api/src/modules/reports/routes.ts (consumidor de CAJA_ROLES línea 55 — NO se toca)
    - el-templo-api/src/modules/coach/routes.ts (consumidor de COACH_DEBTS_ROLES línea 26 — NO se toca)
    - .planning/phases/149-nav-por-categor-as-rbac/149-RESEARCH.md (§RBAC Sets, Pitfall 1, Code Examples "Set core + override Templo")
  </read_first>
  <action>
    En `permissions.ts` agregar el objeto `TEMPLO_RBAC_OVERRIDES = { reportes: ["gestion"], deudas: ["coach", "gestion"] } as const` con JSDoc que documente la regla de dirección override→core (nunca core→Templo, cita D-06 y `.docs/saas-multitenancy/04-mecanismo-modulos.md`).

    Re-expresar los dos sets efectivos como composición core + override, PRESERVANDO EL ORDEN EXACTO para que el valor quede byte-idéntico:
    - `CAJA_ROLES = [...TEMPLO_RBAC_OVERRIDES.reportes, ...ADMIN_ROLES] as const` → resultado `["gestion","admin","owner"]` (idéntico al actual)
    - `COACH_DEBTS_ROLES = [...TEMPLO_RBAC_OVERRIDES.deudas, ...ADMIN_ROLES] as const` → resultado `["coach","gestion","admin","owner"]` (idéntico al actual)
    Mantener los nombres `CAJA_ROLES` / `COACH_DEBTS_ROLES` intactos para no tocar a sus consumidores (reports, coach, leads) — el cambio es sólo en CÓMO se definen, no en el valor. IMPORTANTE (checker BLOCKER 1): NO cambiar el VALOR de `CAJA_ROLES` — programs deja de consumirlo (Task 3), pero el set sigue siendo `["gestion","admin","owner"]` para sus otros consumidores.

    Agregar tres sets nuevos con JSDoc citando D-01/D-11/D-15:
    - `PLANES_WRITE_ROLES = ADMIN_ROLES` (dueño = owner+admin — cierra D-11; NO usar solo owner: admin es dueño)
    - `PLANES_READ_ROLES = SUBSCRIPTION_ROLES` (todo staff, Planes read-only para empleado)
    - `PROGRAMAS_ROLES = ADMIN_ROLES` (dueño-only — cierra D-15; Programas es superficie de entrenamiento que la fase 156 moverá a Templo; hoy owner+admin. JSDoc: reemplaza el uso de CAJA_ROLES en el CRUD admin de programs para que gestion no acceda)

    Crear `el-templo-api/test/rbac-sets.test.ts`: test unitario (sin app/MySQL, sólo importa las constantes) que asserta con deep-equal:
    - `CAJA_ROLES` == `["gestion","admin","owner"]`
    - `COACH_DEBTS_ROLES` == `["coach","gestion","admin","owner"]`
    - `PLANES_WRITE_ROLES` == `["admin","owner"]`
    - `PLANES_READ_ROLES` == `["coach","admin","owner","gestion","recepcion"]`
    - `PROGRAMAS_ROLES` == `["admin","owner"]`
    - `TEMPLO_RBAC_OVERRIDES.reportes` == `["gestion"]` y `.deudas` == `["coach","gestion"]`
    No modificar reports/routes.ts, coach/routes.ts ni leads-routes.ts (byte-identidad garantiza no-regresión de esos consumidores).

  </action>
  <verify>
    <automated>cd el-templo-api && pnpm test rbac-sets && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TEMPLO_RBAC_OVERRIDES" el-templo-api/src/modules/shared/permissions.ts` ≥ 1
    - `grep -c "PLANES_WRITE_ROLES\|PLANES_READ_ROLES\|PROGRAMAS_ROLES" el-templo-api/src/modules/shared/permissions.ts` ≥ 3
    - `pnpm test rbac-sets` pasa con los 7 assertions de deep-equal en verde
    - `pnpm exec tsc --noEmit` sin errores en el paquete API
    - `git diff` NO muestra cambios en reports/routes.ts, coach/routes.ts, members/leads-routes.ts
  </acceptance_criteria>
  <done>permissions.ts expone TEMPLO_RBAC_OVERRIDES + PLANES_WRITE_ROLES + PLANES_READ_ROLES + PROGRAMAS_ROLES; CAJA_ROLES y COACH_DEBTS_ROLES quedan compuestos core+override con valor byte-idéntico; rbac-sets.test.ts en verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guards per-handler dueño-only en los 7 writes de subscriptions (D-11) + tests</name>
  <read_first>
    - el-templo-api/src/modules/subscriptions/routes.ts (guard module-wide líneas 94-105; handlers de escritura líneas ~153, ~163, ~181, ~198, ~583, ~597, ~614; GET ~127, ~138, ~576)
    - el-templo-api/src/modules/finance/routes.ts (patrón per-handler 403 línea 1047)
    - el-templo-api/test/subscriptions/plans-crud.test.ts (test existente "non-admin user gets 403" línea 224; usa registerUser/inject)
    - el-templo-api/test/helpers.ts (createStaffUser línea 418, getAuthToken línea 59)
    - el-templo-api/src/modules/shared/permissions.ts (PLANES_WRITE_ROLES creado en Task 1)
  </read_first>
  <behavior>
    - coach token → 403 en POST /plans
    - coach token → 403 en PUT /plans/:planId
    - coach token → 403 en PATCH /plans/:planId/deactivate
    - coach token → 403 en POST /plans/bulk-migrate
    - coach token → 403 en POST /promo-plans
    - coach token → 403 en PATCH /promo-plans/:promoId
    - coach token → 403 en PATCH /promo-plans/:promoId/deactivate
    - coach token → 200 en GET /plans (lectura sigue abierta a staff)
    - admin token → sigue creando/actualizando planes (200/201, no regresión)
  </behavior>
  <action>
    En `subscriptions/routes.ts` importar `PLANES_WRITE_ROLES` desde `../shared/permissions`. Insertar como PRIMERA línea de cada uno de los 7 handlers de escritura (POST /plans, PUT /plans/:planId, PATCH /plans/:planId/deactivate, POST /bulk-migrate, POST /promo-plans, PATCH /promo-plans/:promoId, PATCH /promo-plans/:promoId/deactivate) el check per-handler: si `request.user.role` no está en `PLANES_WRITE_ROLES` (cast `as readonly string[]`), responder `reply.code(403).send({ error: "Acceso denegado", message: "Solo owner/admin" })`. Usar exactamente el patrón de finance/routes.ts:1047.

    NO tocar el hook `onRequest` module-wide con `SUBSCRIPTION_ROLES` (línea 94) — lo usan assign/renew/pause del PoS profe con rol coach (fase 148/140). NO agregar el check a los GET (`/plans`, `/plans/:planId`, `/promo-plans`) ni a los handlers de assign/renew/pause/cancel.

    Escribir primero los casos de test (RED) en `plans-crud.test.ts`: crear un staff user coach vía `createStaffUser({ role: "coach", ... })` + token vía `getAuthToken`; assertar 403 en los 7 writes y 200 en `GET /plans`. Mantener verde el flujo admin existente. Luego implementar los guards (GREEN).

  </action>
  <verify>
    <automated>cd el-templo-api && pnpm test subscriptions/plans-crud && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Los 7 handlers de escritura contienen un check contra `PLANES_WRITE_ROLES` (verificable: `grep -c "PLANES_WRITE_ROLES" el-templo-api/src/modules/subscriptions/routes.ts` ≥ 8 contando el import)
    - El hook module-wide sigue usando `SUBSCRIPTION_ROLES` (grep muestra `SUBSCRIPTION_ROLES` aún presente en el `onRequest`)
    - `pnpm test subscriptions/plans-crud`: coach recibe 403 en los 7 writes y 200 en GET /plans; admin sigue creando planes
    - `pnpm exec tsc --noEmit` sin errores
  </acceptance_criteria>
  <done>Los 7 writes de plans/promo-plans rechazan a coach con 403; GET /plans sigue abierto a staff; admin/owner escriben normalmente; tests en verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cerrar el CRUD de admin de programs a dueño-only (D-15/D-04) + test gestion→403</name>
  <read_first>
    - el-templo-api/src/modules/programs/routes.ts (imports línea 24 CAJA_ROLES; 7 handlers CRUD admin con check CAJA_ROLES líneas 196, 217, 240, 285, 315, 342, 366; enrollment COACH_ROLES línea 262 — NO tocar; program-addons FINANCE_WRITE_ROLES línea 420 — NO tocar)
    - el-templo-api/src/modules/shared/permissions.ts (PROGRAMAS_ROLES creado en Task 1)
    - el-templo-api/test/programs.test.ts (describe "Program CRUD (admin)" línea 91, "Auth/permissions" línea 412; usa createStaffUser + getAuthToken)
    - .planning/phases/149-nav-por-categor-as-rbac/149-CONTEXT.md (D-15, D-04)
  </read_first>
  <behavior>
    - gestion token → 403 en POST /admin/programs
    - gestion token → 403 en GET /admin/programs (listar)
    - gestion token → 403 en GET /admin/programs/analytics
    - gestion token → 403 en PUT /admin/programs/:programId
    - gestion token → 403 en POST /admin/programs/:programId/deactivate
    - admin token → sigue operando el CRUD de admin de programs (200/201, no regresión)
  </behavior>
  <action>
    En `programs/routes.ts` importar `PROGRAMAS_ROLES` desde `../shared/permissions` y reemplazar `CAJA_ROLES` por `PROGRAMAS_ROLES` en LOS 7 handlers de CRUD admin (líneas 196, 217, 240, 285, 315, 342, 366). Si tras el reemplazo `CAJA_ROLES` ya no se usa en el archivo, quitar su import (verificar con grep). NO tocar el check `COACH_ROLES` del handler de enrollments (línea 262, D-35) ni el `FINANCE_WRITE_ROLES` del handler de program-addons (línea 420, PoS D-22) — esos NO son dueño-only. NO tocar la definición del set `CAJA_ROLES` en permissions.ts (otros consumidores dependen del valor).

    Escribir primero los casos de test (RED) en `programs.test.ts` (dentro del describe "Auth/permissions" o uno nuevo "RBAC dueño-only"): crear un staff user gestion vía `createStaffUser(app, { role: "gestion", ... })` + token; assertar 403 en los handlers de CRUD admin (al menos POST create, GET list, GET analytics, PUT update, POST deactivate). Mantener verde el flujo admin existente (dueño → 200/201). Luego implementar el swap de set (GREEN).

    Actualizar el comentario de cabecera del archivo (línea 7 "Admin CRUD: CAJA_ROLES (gestion, admin, owner)") a "Admin CRUD: PROGRAMAS_ROLES (admin, owner) — dueño-only, D-15".

  </action>
  <verify>
    <automated>cd el-templo-api && pnpm test programs && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PROGRAMAS_ROLES" el-templo-api/src/modules/programs/routes.ts` ≥ 8 (import + 7 handlers)
    - `grep -c "CAJA_ROLES" el-templo-api/src/modules/programs/routes.ts` == 0 (ya no se consume; import removido)
    - El handler de enrollments sigue con `COACH_ROLES` y el de program-addons con `FINANCE_WRITE_ROLES` (grep muestra ambos presentes)
    - `pnpm test programs`: gestion recibe 403 en el CRUD admin; admin/owner siguen operando (200/201)
    - `pnpm exec tsc --noEmit` sin errores
  </acceptance_criteria>
  <done>Los 7 handlers de CRUD admin de programs rechazan a gestion con 403 (dueño-only, D-15); enrollments y program-addons intactos; CAJA_ROLES sin cambios de valor; tests en verde.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                                                                     |
| --------------------------- | ----------------------------------------------------------------------------------------------- |
| cliente admin → API Fastify | El rol viaja en el JWT; el cliente puede llamar cualquier endpoint con un token válido de staff |

## STRIDE Threat Register

| Threat ID | Category                       | Component                                                                                                    | Disposition | Mitigation Plan                                                                                                                                                                           |
| --------- | ------------------------------ | ------------------------------------------------------------------------------------------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-149-01  | Elevation of Privilege         | subscriptions writes de plans/promo-plans (coach puede escribir hoy, D-11)                                   | mitigate    | Check per-handler `PLANES_WRITE_ROLES` (owner+admin) en los 7 writes; GET quedan abiertos a staff. Test coach→403                                                                         |
| T-149-02  | Elevation / DoS (403 indebido) | Re-expresión de CAJA_ROLES/COACH_DEBTS_ROLES como core+override                                              | mitigate    | No-regresión byte-idéntica en rbac-sets.test.ts; consumidores intactos                                                                                                                    |
| T-149-03  | Elevation of Privilege         | Rotura accidental del PoS al tocar el guard module-wide                                                      | accept      | NO se toca el `onRequest` SUBSCRIPTION_ROLES; el write-guard es sólo per-handler en los 7 CRUD                                                                                            |
| T-149-13  | Elevation of Privilege         | CRUD de admin de programs abierto a gestion vía CAJA_ROLES (puerta trasera API a Programas dueño-only, D-15) | mitigate    | Los 7 handlers de CRUD admin pasan de `CAJA_ROLES` a `PROGRAMAS_ROLES` (owner+admin); test gestion→403. Cierra el lado API de la puerta trasera; el router del admin lo cierra en Plan 04 |
| T-149-SC  | Tampering                      | Instalación de paquetes                                                                                      | accept      | Esta fase NO instala paquetes (refactor sobre código existente) — sin superficie de supply chain                                                                                          |

</threat_model>

<verification>
- `cd el-templo-api && pnpm test rbac-sets subscriptions/plans-crud programs` en verde.
- `cd el-templo-api && pnpm exec tsc --noEmit` sin errores.
- Suite completa corre en CI al pushear a staging (no correr suite completa local).
- **UAT / paso manual D-05:** antes de shippear, verificar (pedir OK para SSH; nunca autónomo) qué usuarios reales tienen rol `gestion`/`recepcion` en prod. Para El Templo los sets efectivos de Reportes/Deudas NO cambian (override); el único cambio de acceso real es que gestion pierde el CRUD de Programas (dueño-only, D-15) — confirmar impacto operativo (esperado: nulo, gestion no gestiona rutinas).
</verification>

<success_criteria>

- coach recibe 403 en los 7 writes de plans/promo-plans y 200 en GET /plans (NAV-02/NAV-03, D-11).
- gestion recibe 403 en el CRUD de admin de programs; admin/owner operan (NAV-02/NAV-03, D-15/D-04).
- Sets efectivos de Reportes/Deudas byte-idénticos a hoy; el valor de CAJA_ROLES no cambia (no-regresión).
- PLANES_WRITE_ROLES / PLANES_READ_ROLES / PROGRAMAS_ROLES / TEMPLO_RBAC_OVERRIDES disponibles para el resto de la fase.
  </success_criteria>

<output>
Create `.planning/phases/149-nav-por-categor-as-rbac/149-01-SUMMARY.md` when done
</output>
</content>
</invoke>
