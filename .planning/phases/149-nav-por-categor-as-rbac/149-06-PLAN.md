---
phase: 149-nav-por-categor-as-rbac
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - el-templo-admin/src/pages/LoginPage.vue
  - el-templo-admin/src/router/routes.ts
  - el-templo-admin/src/router/index.ts
autonomous: true
# Este plan no cierra un REQ-ID de REQUIREMENTS.md: cierra D-14 (decisión locked de
# 149-CONTEXT.md, truth 6 de 149-VERIFICATION.md). NAV-01..04 ya están cubiertos por
# los planes 149-01..05; no re-declarar acá para no confundir auditorías de cobertura.
requirements: []

must_haves:
  truths:
    - "Tras un login exitoso, el owner/admin aterriza en /alumnos, el empleado (coach no-Fran/gestion/recepcion) en /pagos, y Fran Scaine en /sessions"
    - "En carga fría (refresh/bookmark) de '/', el destino se resuelve por rol DESPUÉS de checkAuth (no todos los roles caen en /pagos)"
    - "landingForRole() se ejecuta en el flujo real de entrada (login), no sólo cuando se tipea la URL raíz a mano"
  artifacts:
    - path: "el-templo-admin/src/pages/LoginPage.vue"
      provides: "Post-login navega a '/' (delega el destino en el landing por rol)"
      contains: "router.push('/')"
    - path: "el-templo-admin/src/router/routes.ts"
      provides: "landingForRole exportado + índice '/' con fallback estático seguro"
      contains: "export function landingForRole"
    - path: "el-templo-admin/src/router/index.ts"
      provides: "Landing por rol evaluado en beforeEach DESPUÉS de checkAuth (user cargado)"
      contains: "landingForRole"
  key_links:
    - from: "el-templo-admin/src/pages/LoginPage.vue"
      to: "landingForRole (router)"
      via: "router.push('/') → beforeEach(checkAuth) → landingForRole()"
      pattern: "router.push\\('/'\\)"
    - from: "el-templo-admin/src/router/index.ts (beforeEach)"
      to: "landingForRole (routes.ts)"
      via: "import + return landingForRole() cuando la navegación nace en '/'"
      pattern: "landingForRole\\(\\)"
---

<objective>
Cierra la brecha D-14 (CR-02 del REVIEW / truth 6 de la verificación): el landing por rol nunca corre en el camino principal porque `LoginPage.vue:68` conserva `router.push('/sessions')` hardcodeado — el owner cae en /sessions post-login (exactamente lo que D-14 elimina), y el coach no-Fran rebota a /alumnos en vez de /pagos. Además (WR-01), el `redirect` de route-record del índice `/` se resuelve durante el matching ANTES de que `checkAuth()` corra, por lo que en carga fría de `/` todos los roles caen en /pagos (user aún null).

La corrección: (1) el login navega a `/` y delega el destino en el landing por rol; (2) el landing se evalúa en el guard `beforeEach` DESPUÉS de `checkAuth()` (user ya cargado), no en el redirect de route-record; (3) el índice `/` queda con un fallback estático seguro para eliminar la evaluación pre-auth.

Purpose: que "el dueño aterriza en Alumnos, el empleado en Pagos, Fran en Sesiones" funcione end-to-end desde el login real y en refresh de la raíz.
Output: LoginPage → '/', landingForRole exportado + evaluado en el guard post-checkAuth, índice con fallback estático.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-nav-por-categor-as-rbac/149-VERIFICATION.md
@.planning/phases/149-nav-por-categor-as-rbac/149-REVIEW.md
@.planning/phases/149-nav-por-categor-as-rbac/149-04-SUMMARY.md
@./CLAUDE.md

<interfaces>
<!-- Extraído del código. El executor NO necesita explorar el codebase. -->

`el-templo-admin/src/router/routes.ts` (estado actual):

- L1-5 imports; L5 importa `PLANES_READ_ROLES, PAGOS_ROLES, DUENO_ROLES` de `src/config/templo-config`.
- L16-26 `function landingForRole(): string` (privada, NO exportada). Lee `useAuthStore().user` y resuelve, EN ESTE ORDEN CRÍTICO (Pitfall 2 — canAccessTraining es true para cualquier owner):
  1. `user?.role === 'coach' && canAccessTraining(user)` → `/sessions` (sólo Fran)
  2. `user && DUENO_ROLES.includes(user.role)` → `/alumnos` (dueño)
  3. resto → `/pagos` (empleado)
- L38 índice: `{ path: '', redirect: () => landingForRole() }` ← este redirect se resuelve durante el matching, ANTES del beforeEach (bug WR-01).

`el-templo-admin/src/router/index.ts` (estado actual):

- L6 importa `canAccessTraining` de `src/utils/trainingAccess`.
- L25 `Router.beforeEach(async (to) => { ... })`:
  - L27-29 si `to.meta.public` → true.
  - L34 `const isValid = await authStore.checkAuth()` (acá se carga `authStore.user`); si !isValid → '/login'.
  - L40-55 chequeo de `allowedRoles` con `defaultPages` (owner/admin→/alumnos, coach/gestion/recepcion→/pagos).
  - L62-64 bounce `trainingOnly`: si `!canAccessTraining(user)` → '/alumnos'.

`el-templo-admin/src/pages/LoginPage.vue`:

- L62 `const router = useRouter()`, L63 `const authStore = useAuthStore()`.
- L65-72 `handleLogin()`: `await authStore.login(...)` y luego L68 `router.push('/sessions')` ← hardcodeado, se cambia.

Nota de vue-router 4: los `redirect` de route-record se resuelven durante el matching; el `beforeEach` corre sobre la ubicación YA redirigida, exponiendo el origen en `to.redirectedFrom`. Por eso el landing debe evaluarse en el guard (post-checkAuth), no en el redirect del índice.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Login navega a '/' + landingForRole evaluado en el guard post-checkAuth</name>
  <files>el-templo-admin/src/pages/LoginPage.vue, el-templo-admin/src/router/routes.ts, el-templo-admin/src/router/index.ts</files>
  <read_first>
    - el-templo-admin/src/pages/LoginPage.vue (L54-73: handleLogin, L68 el push hardcodeado)
    - el-templo-admin/src/router/routes.ts (L7-26 landingForRole + su JSDoc de orden crítico; L38 índice con redirect)
    - el-templo-admin/src/router/index.ts (L25-67 beforeEach completo: checkAuth L34, allowedRoles L40-55, trainingOnly L62-64)
    - el-templo-admin/src/config/templo-config.ts (L36 DUENO_ROLES, L45 PAGOS_ROLES — para entender los sets que usa landingForRole)
  </read_first>
  <action>
    **LoginPage.vue:** cambiar L68 `router.push('/sessions')` por `router.push('/')`. Así el destino post-login lo resuelve el landing por rol en el guard, no un literal. Mantener el `try/catch` intacto.

    **routes.ts:** exportar `landingForRole` — cambiar `function landingForRole()` (L16) por `export function landingForRole()`. Conservar íntegro su cuerpo y el orden crítico documentado (coach+Fran → /sessions ANTES del bloque dueño). Cambiar el índice L38 de `{ path: '', redirect: () => landingForRole() }` a un fallback ESTÁTICO seguro: `{ path: '', redirect: '/pagos' }`. Motivo: el redirect de route-record se resuelve antes de checkAuth (user null → landing incorrecto, WR-01); con un destino estático `/pagos` (accesible a todo el staff) el guard toma el control y re-resuelve por rol. Añadir un comentario breve en el índice explicando que el destino real lo decide `landingForRole()` en el beforeEach post-checkAuth y que este redirect es sólo un fallback pre-auth.

    **index.ts:** importar `landingForRole` desde `./routes` (junto al import existente de `routes` en L3). Dentro de `beforeEach`, DESPUÉS de `checkAuth` (tras L37, con `authStore.user` ya cargado) y ANTES del chequeo de `allowedRoles` (L40), agregar: si la navegación nace en la raíz — condición `to.path === '/' || to.redirectedFrom?.path === '/'` — calcular `const dest = landingForRole()` y, sólo si `dest !== to.path`, `return dest`. El guard `dest !== to.path` previene loop infinito (cuando el destino ya coincide, deja pasar). Esto garantiza que el landing use el user real (post-checkAuth), corrigiendo tanto CR-02 (login → '/') como WR-01 (carga fría de '/'). NO tocar el bloque `trainingOnly` (L62-64) ni `defaultPages` (L47-54).

  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm exec vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -v "session-pdf-builder\|session-data-transformer" | grep -Ec "LoginPage|router/index|router/routes" ; pnpm exec eslint src/pages/LoginPage.vue src/router/index.ts src/router/routes.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "router.push('/')" el-templo-admin/src/pages/LoginPage.vue` presente; `grep -c "router.push('/sessions')" el-templo-admin/src/pages/LoginPage.vue` == 0
    - `grep -n "export function landingForRole" el-templo-admin/src/router/routes.ts` presente
    - El índice en routes.ts usa `redirect: '/pagos'` (string estático), ya NO `redirect: () => landingForRole()`
    - `grep -c "landingForRole" el-templo-admin/src/router/index.ts` >= 2 (import + uso en el guard)
    - En index.ts, el uso de `landingForRole()` está DESPUÉS de `await authStore.checkAuth()` y protegido por `dest !== to.path` (sin loop)
    - `eslint` de los 3 archivos → 0 errores; `vue-tsc` sin errores nuevos en esos archivos
  </acceptance_criteria>
  <done>El landing por rol corre en el flujo real de login y en refresh de '/', usando el user cargado por checkAuth; sin loops de navegación.</done>
</task>

</tasks>

<verification>
- `vue-tsc --noEmit` sin errores nuevos en LoginPage/router; `eslint` de los 3 archivos limpio.
- Grep: LoginPage usa `router.push('/')`; `landingForRole` exportado; índice con fallback estático; `landingForRole` importado y usado en el guard post-checkAuth.

**UAT visual (pendiente, no bloquea el plan — requiere sesión real por rol):**

1. Login con **owner** → aterriza en `/alumnos` (NO /sessions).
2. Login con **admin** → `/alumnos`.
3. Login con **gestion** y **recepcion** → `/pagos`.
4. Login con **coach genérico** (no Fran) → `/pagos` (NO /alumnos).
5. Login con **Fran Scaine** (Scaine7@hotmail.com) → `/sessions`.
6. Estando logueado como owner, refrescar (F5) sobre `/` → cae en `/alumnos` (NO /pagos) — confirma el fix de WR-01 (carga fría).
   </verification>

<success_criteria>

- Post-login, cada rol aterriza en su destino D-14 (owner/admin→/alumnos, empleado→/pagos, Fran→/sessions).
- El landing se evalúa después de checkAuth, también en carga fría de '/'.
- Sin route-record redirect dependiente del user pre-auth; sin loops de navegación.
  </success_criteria>

<output>
Create `.planning/phases/149-nav-por-categor-as-rbac/149-06-SUMMARY.md` when done
</output>
