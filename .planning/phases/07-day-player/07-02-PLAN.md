---
phase: 07-day-player
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - el-templo-app/src/modules/training/components/player/VideoPlaceholder.vue
  - el-templo-app/src/modules/training/components/player/ProgressBar.vue
  - el-templo-app/src/modules/training/components/player/ExerciseCard.vue
autonomous: true

must_haves:
  truths:
    - "Video area displays placeholder when no video URL"
    - "Video auto-loops with iOS-compatible attributes"
    - "Progress bar shows 4-block completion (25% per block)"
    - "Exercise card shows full detail including reps, rest, contraction"
  artifacts:
    - path: "el-templo-app/src/modules/training/components/player/VideoPlaceholder.vue"
      provides: "Video display area with placeholder fallback"
      min_lines: 50
    - path: "el-templo-app/src/modules/training/components/player/ProgressBar.vue"
      provides: "4-block progress indicator"
      min_lines: 40
    - path: "el-templo-app/src/modules/training/components/player/ExerciseCard.vue"
      provides: "Expanded exercise detail component"
      min_lines: 60
  key_links:
    - from: "ProgressBar.vue"
      to: "blockColors.ts"
      via: "getBlockAccentColor import"
      pattern: "getBlockAccentColor"
    - from: "VideoPlaceholder.vue"
      to: "video element"
      via: "HTML5 video with autoplay loop muted playsinline"
      pattern: "autoplay.*loop.*muted.*playsinline"
---

<objective>
Create core display components for Day Player: video placeholder with iOS compatibility, 4-block progress bar, and exercise detail card.

Purpose: Provide visual building blocks for the Day Player page - video area at top, progress indicator, and exercise details.
Output: 3 Vue components in new player/ subdirectory.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-day-player/CONTEXT.md
@.planning/phases/07-day-player/07-RESEARCH.md
@el-templo-app/src/modules/training/types/session.ts
@el-templo-app/src/modules/training/utils/blockColors.ts
@el-templo-app/src/modules/training/components/BlockCard.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VideoPlaceholder component</name>
  <files>el-templo-app/src/modules/training/components/player/VideoPlaceholder.vue</files>
  <action>
Create player/ subdirectory and VideoPlaceholder.vue component.

Props:
- videoUrl?: string | null - URL for video (null shows placeholder)
- posterUrl?: string - Poster image during loading

Template structure:
1. Container div at ~40vh height with black background
2. When no videoUrl: Show placeholder with videocam icon + "Video proximamente" text
   - Use gradient background (#1a1a1a to #2d2d2d)
   - Center icon (48px) and caption text
3. When videoUrl provided: Show HTML5 video with ALL FOUR attributes:
   - autoplay loop muted playsinline (required for iOS Safari)
   - Use poster prop for loading state
   - object-fit: contain

Script (setup):
- videoRef for programmatic play control
- attemptAutoplay() function for fallback
- Watch videoUrl changes: load() then attemptAutoplay()
- onMounted: attemptAutoplay()

Reference RESEARCH.md "Pattern 4: Video Placeholder with iOS Compatibility".

Style (scoped):
- .video-container: width 100%, height 40vh, position relative, black background
- .video-placeholder: absolute inset 0, flexbox centered, gradient background
- .video-player: width/height 100%, object-fit contain
  </action>
  <verify>TypeScript compiles: `cd el-templo-app && npx tsc --noEmit`</verify>
  <done>VideoPlaceholder shows icon placeholder or loops video with iOS compatibility</done>
</task>

<task type="auto">
  <name>Task 2: Create ProgressBar component</name>
  <files>el-templo-app/src/modules/training/components/player/ProgressBar.vue</files>
  <action>
Create 4-block progress indicator using Quasar's QLinearProgress.

Props:
- completedBlocks: BlockRole[] - Array of completed block roles
- currentBlock: BlockRole - Currently active block

Template structure:
1. Container with horizontal padding
2. QLinearProgress with:
   - :value="progress" (0-1 scale, completedBlocks.length / 4)
   - :color="currentBlockColor" (from getBlockAccentColor)
   - track-color="grey-3"
   - size="8px"
   - rounded
3. Labels row below bar with 4 labels:
   - ['Initium', 'Nucleus', 'Deuteros', 'Athlos']
   - Completed labels get positive color + bold
   - Use justify-between for spacing

Script (setup):
- Import getBlockAccentColor from utils/blockColors
- Computed completedCount = completedBlocks.length
- Computed progress = completedCount / 4
- Computed currentBlockColor = getBlockAccentColor(currentBlock)

Reference RESEARCH.md "Progress Bar Component" code example.

Style (scoped, SCSS):
- .session-progress: padding 8px 16px
- .session-progress__labels: flex, justify-between, margin-top 4px
- .session-progress__label: font-size 10px, color grey, uppercase, letter-spacing
- .session-progress__label--completed: positive color, font-weight 600
  </action>
  <verify>TypeScript compiles: `cd el-templo-app && npx tsc --noEmit`</verify>
  <done>ProgressBar shows 4-step progress with block-specific accent colors</done>
</task>

<task type="auto">
  <name>Task 3: Create ExerciseCard component</name>
  <files>el-templo-app/src/modules/training/components/player/ExerciseCard.vue</files>
  <action>
Create expanded exercise detail card showing full prescription info.

Props:
- exercise: Prescription (from session.ts)
- accentColor: string - Quasar color name for accents (from block)
- isActive?: boolean - Whether this exercise is currently selected (default false)

Template structure:
1. Card container with subtle elevation, rounded corners
2. Header row:
   - Exercise name (text-h6, bold)
   - Badge with contraction type (CON/EXC/ISO) using accent color
3. Main metrics row (flex with gaps):
   - If exercise.reps: show "X reps" with number highlighted
   - If exercise.seconds: show "X seg" with number highlighted
   - Always show rest: "Descanso: Xs"
4. Notes section (if exercise.notes):
   - text-caption, grey, italic
   - Show notes content

Script (setup):
- Format reps/seconds for display
- Computed hasReps = exercise.reps !== null
- Computed hasTime = exercise.seconds !== null

Style (scoped, SCSS):
- Card padding 16px, margin-bottom 8px
- Active state: left border 4px with accent color, slightly elevated
- Metrics: flex row with gap 24px
- Metric values: text-weight-bold, larger size
- Metric labels: text-caption, grey
  </action>
  <verify>TypeScript compiles: `cd el-templo-app && npx tsc --noEmit`</verify>
  <done>ExerciseCard displays full exercise details with contraction badge and metrics</done>
</task>

</tasks>

<verification>
1. player/ subdirectory created with 3 components
2. VideoPlaceholder has all 4 iOS-required video attributes
3. ProgressBar correctly divides progress into 4 blocks (not 5)
4. ExerciseCard shows reps OR seconds (not both)
5. All components use TypeScript and composition API
6. Accent colors imported from updated blockColors.ts
</verification>

<success_criteria>
- TypeScript compilation succeeds with no errors
- VideoPlaceholder handles null videoUrl gracefully
- ProgressBar calculates percentage based on 4 blocks
- ExerciseCard displays all Prescription fields appropriately
- Components follow existing codebase patterns (SCSS, Quasar)
</success_criteria>

<output>
After completion, create `.planning/phases/07-day-player/07-02-SUMMARY.md`
</output>
