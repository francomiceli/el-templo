---
phase: 07-day-player
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - el-templo-app/src/modules/training/components/player/BlockHeader.vue
  - el-templo-app/src/modules/training/components/player/ExerciseList.vue
  - el-templo-app/src/modules/training/components/player/DeuterosChoice.vue
  - el-templo-app/src/modules/training/components/player/SplashScreen.vue
autonomous: true

must_haves:
  truths:
    - "Block header shows block name with accent color"
    - "Exercise list uses accordion behavior (one expanded at a time)"
    - "Deuteros choice presents 2 swipeable options"
    - "Splash screen shows session info and auto-proceeds after 3-4 seconds"
  artifacts:
    - path: "el-templo-app/src/modules/training/components/player/BlockHeader.vue"
      provides: "Block title with accent color styling"
      min_lines: 25
    - path: "el-templo-app/src/modules/training/components/player/ExerciseList.vue"
      provides: "Collapsible exercise list with accordion"
      min_lines: 60
    - path: "el-templo-app/src/modules/training/components/player/DeuterosChoice.vue"
      provides: "Swipeable 2-option Deuteros selector"
      min_lines: 80
    - path: "el-templo-app/src/modules/training/components/player/SplashScreen.vue"
      provides: "Motivational splash with auto-proceed"
      min_lines: 40
  key_links:
    - from: "DeuterosChoice.vue"
      to: "blockColors.ts"
      via: "getBlockColorClass import"
      pattern: "getBlockColorClass"
    - from: "ExerciseList.vue"
      to: "ExerciseCard.vue"
      via: "component import"
      pattern: "import.*ExerciseCard"
---

<objective>
Create block interaction components for Day Player: block header, exercise list with accordion, Deuteros choice selector, and splash screen.

Purpose: Enable block navigation UI with visual identity, exercise expansion, Deuteros choice workflow, and session entry experience.
Output: 4 Vue components completing the player component library.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-day-player/CONTEXT.md
@.planning/phases/07-day-player/07-RESEARCH.md
@el-templo-app/src/modules/training/types/session.ts
@el-templo-app/src/modules/training/utils/blockColors.ts
@el-templo-app/src/modules/training/utils/routeNames.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BlockHeader and SplashScreen components</name>
  <files>
    el-templo-app/src/modules/training/components/player/BlockHeader.vue
    el-templo-app/src/modules/training/components/player/SplashScreen.vue
  </files>
  <action>
**BlockHeader.vue:**
Simple header component showing block name with accent styling.

Props:
- blockName: string - Display name (e.g., "NUCLEUS", "INITIUM")
- accentColor: string - Quasar color name for accent
- route?: string - Optional route name to show below block name

Template:
1. Container with left border (4px) in accent color
2. Block name as text-h5, uppercase, bold
3. If route provided: show as text-caption below name (grey)

Style (scoped):
- Padding 16px left (after border), 8px vertical
- Left border uses dynamic accent color
- Subtle background using accent color at 10% opacity

**SplashScreen.vue:**
Motivational entry splash that auto-proceeds.

Props:
- sessionInfo: { day: string, levelGroup: string } - Session metadata
- onComplete: () => void - Callback when splash ends

Template:
1. Full-screen overlay (fixed, inset 0, high z-index)
2. Dark gradient background
3. Center content:
   - El Templo logo or icon placeholder (large)
   - Session info: "Lunes · ALFA_DELTA" format
   - Motivational message: "Vamos a entrenar!"
   - Loading dots or subtle animation (optional)
4. Auto-fade after 3 seconds, then call onComplete

Script (setup):
- onMounted: setTimeout to call onComplete after 3000ms
- Optional: ref for fade-out animation class

Style (scoped):
- Full viewport coverage
- Dark gradient (#1a1a2e to #16213e)
- Centered flexbox
- Fade-out transition (opacity 1 to 0)
  </action>
  <verify>TypeScript compiles: `cd el-templo-app && npx tsc --noEmit`</verify>
  <done>BlockHeader shows accent-styled block name, SplashScreen auto-proceeds after timeout</done>
</task>

<task type="auto">
  <name>Task 2: Create ExerciseList component with accordion behavior</name>
  <files>el-templo-app/src/modules/training/components/player/ExerciseList.vue</files>
  <action>
Create collapsible exercise list where only one exercise is expanded at a time.

Props:
- exercises: Prescription[] - Array of exercises to display
- accentColor: string - Quasar color for active exercise accent
- selectedIndex: number - Currently selected exercise index (v-model)

Emits:
- update:selectedIndex - When user taps different exercise

Template structure:
1. Container for exercise list
2. For each exercise in exercises:
   - QExpansionItem with:
     - :model-value="index === selectedIndex"
     - @update:model-value to emit update:selectedIndex
     - header showing exercise name + contraction badge
     - content slot with ExerciseCard (detailed view)
3. First exercise auto-expanded (index 0)

Script (setup):
- Import ExerciseCard component
- Handle expansion changes to implement accordion:
  - When item expands, emit new selectedIndex
  - Only one can be expanded at a time

Accordion behavior:
- Tapping exercise name expands it and collapses current
- Emit selectedIndex change so parent can update video

Style (scoped):
- Remove default QExpansionItem borders for cleaner look
- Add subtle dividers between items
- Expanded item gets left accent border
  </action>
  <verify>TypeScript compiles: `cd el-templo-app && npx tsc --noEmit`</verify>
  <done>ExerciseList shows collapsible exercises with accordion behavior and emits selection changes</done>
</task>

<task type="auto">
  <name>Task 3: Create DeuterosChoice component</name>
  <files>el-templo-app/src/modules/training/components/player/DeuterosChoice.vue</files>
  <action>
Create swipeable 2-option selector for Deuteros 1 vs Deuteros 2 choice.

Props:
- deuteros1: Block - First Deuteros option
- deuteros2: Block - Second Deuteros option

Emits:
- select: [choice: 'DEUTEROS_1' | 'DEUTEROS_2'] - When user confirms choice

Template structure:
1. Header section:
   - Title: "Elige tu bloque Deuteros"
   - Subtitle: "Desliza para ver las opciones"
2. Swipeable cards container (CSS scroll-snap):
   - Two cards at 85% width each
   - Each card shows:
     - Header with block role label + route name (use getRouteName)
     - Color-coded header background (getBlockColorClass)
     - Exercise preview (first 3 exercise names)
     - "+N mas" if more than 3 exercises
3. Selected state indicator on tapped card
4. Bottom action button:
   - "Comenzar Deuteros 1" or "Comenzar Deuteros 2"
   - Disabled until selection made
   - On click: emit('select', selectedChoice)

Script (setup):
- ref selected: 'DEUTEROS_1' | 'DEUTEROS_2' | null
- Computed options array with label, route, exercises for each
- selectOption(role): set selected
- confirm(): emit select event

Reference RESEARCH.md "Pattern 5: Deuteros Choice with CSS Scroll-Snap".

Style (scoped, SCSS):
- Use CSS scroll-snap-type: x mandatory on container
- Cards: scroll-snap-align: center, 85% width
- Selected card: scale(1.02), deeper shadow
- Smooth transitions on selection
  </action>
  <verify>TypeScript compiles: `cd el-templo-app && npx tsc --noEmit`</verify>
  <done>DeuterosChoice presents swipeable cards and emits selection on confirm</done>
</task>

</tasks>

<verification>
1. All 4 components exist in player/ subdirectory
2. BlockHeader uses dynamic accent color for border
3. ExerciseList implements accordion (one expanded at a time)
4. ExerciseList emits selectedIndex changes for parent handling
5. DeuterosChoice uses CSS scroll-snap for smooth swiping
6. SplashScreen auto-proceeds after 3 seconds
7. All components use TypeScript and composition API
</verification>

<success_criteria>
- TypeScript compilation succeeds with no errors
- BlockHeader renders block name with accent styling
- ExerciseList properly manages accordion expansion state
- DeuterosChoice clearly shows both options with exercise previews
- SplashScreen provides smooth entry experience with auto-proceed
</success_criteria>

<output>
After completion, create `.planning/phases/07-day-player/07-03-SUMMARY.md`
</output>
