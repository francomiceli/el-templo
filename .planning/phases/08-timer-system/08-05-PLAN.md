---
phase: 08-timer-system
plan: 05
type: execute
wave: 4
depends_on: ["08-04"]
files_modified:
  - el-templo-app/src/modules/training/pages/DayPlayer.vue
  - el-templo-app/src/modules/training/components/player/TimerControls.vue
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Action bar content (TimerControls + Listo button) never overlaps exercise list on mobile"
    - "Buttons in action bar have reasonable max-width on desktop/tablet viewports"
  artifacts:
    - path: "el-templo-app/src/modules/training/pages/DayPlayer.vue"
      provides: "Dynamic padding-bottom and constrained action bar"
      contains: "max-width"
    - path: "el-templo-app/src/modules/training/components/player/TimerControls.vue"
      provides: "Timer controls without full-width stretch on desktop"
  key_links:
    - from: ".day-player__content padding-bottom"
      to: ".day-player__action actual height"
      via: "padding must exceed action bar height in all states"
---

<objective>
Fix two layout issues found during UAT Test 7 (full session flow):

1. On mobile, stacked TimerControls + "Listo!" button exceed the 80px padding-bottom, hiding exercise content behind the fixed action bar.
2. On desktop, buttons in .day-player__action stretch full viewport width with no max-width constraint.

Purpose: Ensure the Day Player layout works correctly across all viewport sizes and action bar content states.
Output: Patched styles in DayPlayer.vue and TimerControls.vue.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-timer-system/08-UAT.md
@el-templo-app/src/modules/training/pages/DayPlayer.vue
@el-templo-app/src/modules/training/components/player/TimerControls.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix action bar padding and max-width in DayPlayer.vue</name>
  <files>el-templo-app/src/modules/training/pages/DayPlayer.vue</files>
  <action>
    In the `<style scoped>` section of DayPlayer.vue, make two changes:

    1. **Increase padding-bottom on `.day-player__content`:**
       Change `padding-bottom: 80px` to `padding-bottom: 160px`. The worst-case action bar height is: TimerControls padding (16px top) + "Iniciar Timer" button (~48px) + "Listo!" button gap (8px) + "Listo!" button (~48px) + bottom padding (16px) + safe-area-inset (~34px on iPhone) = ~170px. Using 160px as padding-bottom provides sufficient clearance for the vast majority of cases. This is the simple, reliable approach — no JS measurement needed.

    2. **Add max-width and centering to `.day-player__action`:**
       Add `max-width: 500px` and `margin-left: auto; margin-right: auto;` to `.day-player__action`. This constrains buttons to a reasonable width on desktop/tablet while remaining full-width on mobile (where viewport < 500px).

    Do NOT change the template or script sections. This is a CSS-only fix.
  </action>
  <verify>
    Run `cd /home/franco/projects/el-templo/el-templo-app && npx vue-tsc --noEmit` to confirm no type errors introduced (style-only change should pass trivially).
    Visual: Open browser dev tools, toggle mobile viewport (375px width) — exercise list should scroll fully without content hidden behind action bar. Toggle desktop viewport (1200px width) — action buttons should be centered and max 500px wide.
  </verify>
  <done>
    - .day-player__content has padding-bottom: 160px
    - .day-player__action has max-width: 500px with auto horizontal margins
    - No template or script changes
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove full-width stretch from TimerControls initial button</name>
  <files>el-templo-app/src/modules/training/components/player/TimerControls.vue</files>
  <action>
    The "Iniciar Timer" q-btn (line ~11) has `class="full-width"`. This is fine within the action bar now that the action bar has max-width, but the TimerControls component itself should not force full-width on its container.

    Keep the `full-width` class on the "Iniciar Timer" button — it should fill the action bar width, which is now properly constrained by the parent's max-width. No changes needed here if the parent constraint is sufficient.

    However, review the `.timer-controls` wrapper div: it has `padding: 16px 0` which adds vertical space. Remove this padding since the parent `.day-player__action` already provides 16px padding. The double padding contributes to the action bar exceeding 80px. Change:
    ```
    .timer-controls {
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 16px 0;
    }
    ```
    to:
    ```
    .timer-controls {
      display: flex;
      justify-content: center;
      align-items: center;
    }
    ```

    This removes ~32px of unnecessary vertical space from the action bar.
  </action>
  <verify>
    Run `cd /home/franco/projects/el-templo/el-templo-app && npx vue-tsc --noEmit` to confirm no type errors.
    Visual: In mobile viewport, the action bar should be more compact — timer controls sit flush within the parent padding.
  </verify>
  <done>
    - TimerControls .timer-controls padding removed (parent provides padding)
    - Action bar total height reduced by ~32px
  </done>
</task>

</tasks>

<verification>
1. Mobile viewport (375px): Navigate to a timed block (Nucleus). Start timer. Verify "Listo!" button (if For Time) does not overlap exercise list. Scroll exercise list fully — all content visible above action bar.
2. Desktop viewport (1200px+): Action buttons are centered with max-width ~500px, not stretching full viewport width.
3. Straight Sets blocks: "Complete Block" button still renders correctly in constrained action bar.
4. No visual regressions in header, progress bar, or exercise list.
</verification>

<success_criteria>
- Exercise list scrolls fully on mobile without content hidden behind action bar
- Action bar buttons have reasonable width on desktop (max 500px, centered)
- Timer controls are vertically compact (no double padding)
- All block types (straight sets + timed) render correctly in action area
</success_criteria>

<output>
After completion, create `.planning/phases/08-timer-system/08-05-SUMMARY.md`
</output>
