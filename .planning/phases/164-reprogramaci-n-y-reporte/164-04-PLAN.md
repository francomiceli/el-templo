---
phase: 164-reprogramaci-n-y-reporte
plan: 04
type: execute
wave: 2
depends_on: [164-03]
files_modified:
  - el-templo-admin/src/composables/useReportsApi.ts
  - el-templo-admin/src/components/reports/TrialSessionsReport.vue
autonomous: true
requirements: [REPRO-02, REPRO-03]

must_haves:
  truths:
    - "El reporte de Sesiones de Prueba muestra una columna 'Reprogramaciones' con el contador por lead"
    - "El header de la columna documenta que cuenta TODAS las pruebas canceladas del lead (incl. self-service)"
    - "Junto al estado del lead aparece un indicador discreto (ícono+tooltip 'Estado puesto a mano') cuando leadStatusSource==='manual'"
    - "Un select de origen (Automático/Manual, clearable) filtra el reporte via el query param leadStatusSource"
  artifacts:
    - path: "el-templo-admin/src/composables/useReportsApi.ts"
      provides: "reschedules + leadStatusSource en row/filters client types"
      contains: "leadStatusSource"
    - path: "el-templo-admin/src/components/reports/TrialSessionsReport.vue"
      provides: "columna + indicador + filtro"
      contains: "reschedules"
  key_links:
    - from: "TrialSessionsReport.vue select de origen"
      to: "buildServerFilters → backend leadStatusSource"
      via: "filtro server-side"
      pattern: "leadStatusSource"
    - from: "TrialSessionsReport.vue columna Reprogramaciones"
      to: "row.reschedules"
      via: "field binding"
      pattern: "reschedules"
---

<objective>
UI del reporte de Sesiones de Prueba (REPRO-02, REPRO-03, lado frontend): columna "Reprogramaciones", indicador discreto auto/manual junto al estado, y select de filtro por origen. Todo consume lo que expone el backend del Plan 164-03.

Purpose: Que gestión vea el "ruido" de reprogramaciones por lead y distinga/filtre estados puestos a mano vs automáticos, directo en el reporte.
Output: tipos cliente en `useReportsApi`, y columna + chip/ícono + select de filtro en `TrialSessionsReport.vue`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164-reprogramaci-n-y-reporte/164-CONTEXT.md
@.planning/phases/164-reprogramaci-n-y-reporte/164-PATTERNS.md
@.planning/phases/164-reprogramaci-n-y-reporte/164-03-PLAN.md
@./CLAUDE.md

Repo constraints (deep work): Spanish user-facing strings; NO new dependencies; never push; stage explicit paths. Sin colores nuevos fuera de la paleta terracotta. Gate local OBLIGATORIO (CI NO typechequea el admin): `pnpm exec vue-tsc --noEmit` desde `el-templo-admin`.

<interfaces>
Backend del Plan 164-03: cada fila del reporte trae `reschedules: number` y `leadStatusSource: 'auto'|'manual'|null` (null = automático/histórico). El endpoint acepta el query param `leadStatusSource=auto|manual` (auto incluye NULL). El CSV ya incluye "Reprogramaciones" y "Origen estado" (server-side; no hay mapeo CSV cliente que tocar).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tipos cliente + columna + indicador + filtro</name>
  <files>el-templo-admin/src/composables/useReportsApi.ts, el-templo-admin/src/components/reports/TrialSessionsReport.vue</files>
  <read_first>
    - el-templo-admin/src/composables/useReportsApi.ts — `TrialSessionsRowClient` (~48-69) y `TrialSessionsFiltersClient` (~31-46); `buildTrialSessionsParams` (~297-306, ya strippea undefined/null genéricamente → NO tocar).
    - el-templo-admin/src/components/reports/TrialSessionsReport.vue — array `columns` (~545-639, columna plain-field `period` ~625-631 como espejo), body-slot `#body-cell-leadStatus` (~148-179, usa `leadStatusEffective` para el chip), selects de filtro (~15-74, `shift`/`leadStatus`), `filters` reactive (~367/375-376), `buildServerFilters` (~643-659).
  </read_first>
  <action>
    En `useReportsApi.ts`: agregar `reschedules: number;` y `leadStatusSource: 'auto' | 'manual' | null;` a `TrialSessionsRowClient`; agregar `leadStatusSource?: 'auto' | 'manual';` a `TrialSessionsFiltersClient`. NO tocar `buildTrialSessionsParams` (ya strippea genéricamente).
    En `TrialSessionsReport.vue`:
    - Columna (D-04): append a `columns` (después de ~638) espejando `period`: `{ name:'reschedules', label:'Reprogramaciones', field:'reschedules', align:'center', sortable:false }`. Header tooltip documentando "cuenta TODAS las pruebas canceladas del lead (incl. self-service)" (specifics.67) via slot `#header-cell-reschedules` con `q-tooltip` (o `headerClasses`+tooltip).
    - Indicador auto/manual (D-05): extender el body-slot `#body-cell-leadStatus` (~148-179) — DESPUÉS del `q-chip` existente, renderizar condicionalmente un ícono pequeño discreto (p.ej. `edit`/`person`) con `<q-tooltip>Estado puesto a mano</q-tooltip>` SOLO cuando `props.row.leadStatusSource === 'manual'`. `null`/`'auto'` → nada (histórico = automático). Sin colores nuevos.
    - Filtro (D-06): agregar un `q-select` en la fila de filtros espejando `shift` (~45-54): opciones `[{label:'Automático',value:'auto'},{label:'Manual',value:'manual'}]`, `clearable`, `emit-value`, `map-options`, ligado a `filters.leadStatusSource`. Agregar `leadStatusSource` al `filters` reactive y a `buildServerFilters` (una línea: `leadStatusSource: filters.leadStatusSource ?? undefined`).
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm exec vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: `useReportsApi.ts` incluye `reschedules`/`leadStatusSource` en el row client y `leadStatusSource?` en filters client; `buildTrialSessionsParams` sin cambios.
    - Source: `columns` tiene la entrada `reschedules` con label 'Reprogramaciones' y hay un tooltip de header documentando el conteo total de canceladas.
    - Source: el slot `#body-cell-leadStatus` renderiza el indicador SOLO para `leadStatusSource === 'manual'` con tooltip "Estado puesto a mano".
    - Source: existe el `q-select` de origen (clearable, opciones Automático/Manual) y `buildServerFilters` pasa `leadStatusSource`.
    - Source: sin colores fuera de la paleta ni `console.*`.
    - Behavior: `pnpm exec vue-tsc --noEmit` (admin) sin errores nuevos.
    - No new dependencies.
  </acceptance_criteria>
  <done>El reporte muestra la columna, el indicador manual discreto y el filtro por origen, todo cableado al backend y compilando con vue-tsc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| report UI → GET /reports/trial-sessions | staff con CAJA_ROLES agrega `leadStatusSource` a la query (la autorización/scope la aplica el backend) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-10 | Information Disclosure | el filtro/columna nuevos no deben ampliar el scope de datos | mitigate | La UI sólo agrega un query param opcional; el guard CAJA_ROLES + country-scope y el enum del filtro viven en el backend (Plan 164-03) |
| T-164-11 | Repudiation / integridad de reporte | el indicador manual induce a error si mapea mal null/auto | mitigate | Sólo `'manual'` muestra indicador; `null`/`'auto'` = automático (histórico), consistente con la semántica de 163 y el filtro backend |
| T-164-SC | Tampering | npm installs | accept | No se agregan dependencias en esta fase |
</threat_model>

<verification>
- `cd el-templo-admin && pnpm exec vue-tsc --noEmit` sin errores nuevos.
- `grep -n "reschedules\|leadStatusSource" el-templo-admin/src/components/reports/TrialSessionsReport.vue` muestra columna + indicador + filtro.
- Verificación visual (UAT de milestone): abrir el reporte de Sesiones de Prueba → ver columna Reprogramaciones con conteos, ícono "puesto a mano" en leads de origen manual, y el select Automático/Manual filtrando (auto incluye históricos sin origen).
</verification>

<success_criteria>
El reporte de Sesiones de Prueba del admin muestra el contador de reprogramaciones por lead, un indicador discreto auto/manual junto al estado, y un filtro por origen cableado al backend, compilando con vue-tsc. (REPRO-02, REPRO-03, lado frontend)
</success_criteria>

<output>
Create `.planning/phases/164-reprogramaci-n-y-reporte/164-04-SUMMARY.md` when done
</output>
