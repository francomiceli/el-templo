---
phase: 164-reprogramaci-n-y-reporte
plan: 02
type: execute
wave: 2
depends_on: [164-01]
files_modified:
  - el-templo-admin/src/composables/useSchedulingApi.ts
  - el-templo-admin/src/components/scheduling/RescheduleTrialDialog.vue
  - el-templo-admin/src/components/scheduling/SesionesDePruebaDialog.vue
autonomous: true
requirements: [REPRO-01]

must_haves:
  truths:
    - "Cada fila de trial en SesionesDePruebaDialog tiene un botón 'Reprogramar' junto a 'quitar' (el flujo viejo NO se elimina)"
    - "Al reprogramar, un picker de fecha+slot llama a POST /trials/:bookingId/reschedule y refresca la lista"
    - "El feedback usa $q.notify (positivo/negativo) con extractError, igual que removeTrial"
  artifacts:
    - path: "el-templo-admin/src/composables/useSchedulingApi.ts"
      provides: "cliente rescheduleTrial(bookingId, {scheduleId,date,branchId})"
      contains: "rescheduleTrial"
    - path: "el-templo-admin/src/components/scheduling/RescheduleTrialDialog.vue"
      provides: "picker de fecha+slot para reprogramar"
      contains: "reschedule"
    - path: "el-templo-admin/src/components/scheduling/SesionesDePruebaDialog.vue"
      provides: "acción Reprogramar por fila"
      contains: "reschedul"
  key_links:
    - from: "SesionesDePruebaDialog.vue botón Reprogramar"
      to: "RescheduleTrialDialog.vue"
      via: "apertura del diálogo por fila"
      pattern: "Reschedul"
    - from: "RescheduleTrialDialog.vue submit"
      to: "useSchedulingApi.rescheduleTrial"
      via: "cliente API → POST reschedule"
      pattern: "rescheduleTrial"
---

<objective>
UI del admin para la acción "Reprogramar" de una sesión de prueba (REPRO-01, lado frontend). Botón por fila en `SesionesDePruebaDialog.vue` que abre un picker de fecha+slot y llama al endpoint transaccional del Plan 164-01. El flujo viejo (quitar + volver a cargar) se mantiene intacto.

Purpose: Dar a gestión la reprogramación de un solo paso desde el operativo diario de trials.
Output: cliente `rescheduleTrial` en `useSchedulingApi`, `RescheduleTrialDialog.vue` (nuevo), y la acción en `SesionesDePruebaDialog.vue`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164-reprogramaci-n-y-reporte/164-CONTEXT.md
@.planning/phases/164-reprogramaci-n-y-reporte/164-PATTERNS.md
@.planning/phases/164-reprogramaci-n-y-reporte/164-01-PLAN.md
@./CLAUDE.md

Repo constraints (deep work): Spanish user-facing strings; NO new dependencies; never push; stage explicit paths. Frontend logging via `createLogger()` — nunca `console.*`. Sin colores nuevos fuera de la paleta terracotta (`quasar.variables.scss`). Gate local OBLIGATORIO (CI NO typechequea el admin): `pnpm exec vue-tsc --noEmit` desde `el-templo-admin`.

<interfaces>
Endpoint del Plan 164-01: `POST /api/admin/scheduling/trials/:bookingId/reschedule`, body `{ scheduleId: number; date: string; branchId: number }`, respuesta `{ bookingId: number }`. Guard ALL_STAFF_ROLES + country-scope (heredado). El fila de trial es `TrialListItem` (import desde `src/types/scheduling`): expone `bookingId`, `userId`, `scheduleId`, `startTime`, nombres, `phone`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: cliente rescheduleTrial + RescheduleTrialDialog.vue</name>
  <files>el-templo-admin/src/composables/useSchedulingApi.ts, el-templo-admin/src/components/scheduling/RescheduleTrialDialog.vue</files>
  <read_first>
    - el-templo-admin/src/composables/useSchedulingApi.ts — `bookTrial` (~382-401) para el envelope loading/error/try-finally + `extractError` (import ~8); el bloque `return {}` (~512-533) donde registrar; los clientes de slots `getWeeklyGrid`/`getSlotDetail` ya existentes (para poblar el select de slots).
    - el-templo-admin/src/components/scheduling/SlotDetailDialog.vue — sub-flujo de trial-booking: bloque de select elegibles (~459-500) y `onBookTrial` (~1287-1309) para la forma de éxito/error con `$q.notify`.
    - el-templo-admin/src/components/scheduling/SesionesDePruebaDialog.vue — patrón `q-date` + `q-popup-proxy` (~28-42) y `onDatePicked` (~228-234, normaliza `YYYY/MM/DD` → `YYYY-MM-DD`).
  </read_first>
  <action>
    En `useSchedulingApi.ts`: agregar `async function rescheduleTrial(bookingId: number, data: { scheduleId: number; date: string; branchId: number }): Promise<{ bookingId: number }>` copiando verbatim el envelope de `bookTrial` (loading/error/try-catch-finally, `error.value = extractError(err, 'Error reprogramando sesión de prueba')`, `throw err`) y `api.post(\`/admin/scheduling/trials/${bookingId}/reschedule\`, data)`. Registrarlo en el `return {}` block.
    Crear `RescheduleTrialDialog.vue`: diálogo Quasar que recibe (props) la fila de trial (`bookingId`, `userId`, branch/sede) y expone un `q-date` (patrón + normalizador `onDatePicked` de SesionesDePruebaDialog) + un `q-select` de slots disponibles para la sede (poblado con `getWeeklyGrid`/`getSlotDetail`). Al confirmar: llamar `rescheduleTrial(bookingId, { scheduleId, date, branchId })`, `$q.notify({ type:'positive', message:'Sesión de prueba reprogramada' })`, emitir un evento de éxito y cerrar; en error `const msg = schedulingApi.error.value ?? '<fallback es>'; $q.notify({ type:'negative', message: msg, timeout: 5000 })`. Sin colores nuevos (paleta terracotta). Logging via `createLogger()` si hace falta, nunca `console.*`. Detalles visuales = Claude's Discretion (D-03).
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm exec vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n "rescheduleTrial" useSchedulingApi.ts` muestra la función Y su registro en el `return {}`.
    - Source: el cliente POSTea a `/admin/scheduling/trials/${bookingId}/reschedule` con body `{scheduleId,date,branchId}` y usa `extractError`.
    - Source: existe `RescheduleTrialDialog.vue` con `q-date` (normalizado a `YYYY-MM-DD`), un select de slot, y feedback `$q.notify` positivo/negativo.
    - Source: sin `console.*` ni colores fuera de la paleta.
    - Behavior: `pnpm exec vue-tsc --noEmit` (admin) sin errores nuevos.
    - No new dependencies.
  </acceptance_criteria>
  <done>Cliente + diálogo compilan con vue-tsc; el diálogo llama al endpoint y da feedback estándar.</done>
</task>

<task type="auto">
  <name>Task 2: acción "Reprogramar" por fila en SesionesDePruebaDialog</name>
  <files>el-templo-admin/src/components/scheduling/SesionesDePruebaDialog.vue</files>
  <read_first>
    - el-templo-admin/src/components/scheduling/SesionesDePruebaDialog.vue — botón "quitar" (~124-138), `confirmRemoveTrial`/`removeTrial` (~268-294), ref `removingBookingId` (~266), `load()` refresh (usado en removeTrial ~286), import `TrialListItem` (~153), y `$q.notify`+`extractError` (~285-290).
    - el-templo-admin/src/components/scheduling/RescheduleTrialDialog.vue — la interfaz de props/eventos del diálogo del Task 1.
  </read_first>
  <action>
    Agregar un `q-btn` de acción "Reprogramar" AL LADO del botón "quitar" existente (copiar la forma del `q-btn` side-section ~124-138) con un ícono nuevo (`event_repeat` o `edit_calendar`) y una ref `reschedulingBookingId` espejo de `removingBookingId`. Al click, abrir `RescheduleTrialDialog.vue` sembrado con `trial.bookingId` + sede/branch de la fila. En el evento de éxito del diálogo, llamar `load()` para refrescar la lista (igual que `removeTrial` ~286). NO eliminar ni tocar el flujo "quitar" (D-03: coexisten). Strings en español. Sin colores nuevos.
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm exec vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -niE "reprogram|reschedul" SesionesDePruebaDialog.vue` muestra el botón nuevo y el uso del diálogo.
    - Source: el botón "quitar" y `confirmRemoveTrial`/`removeTrial` siguen presentes sin cambios de comportamiento.
    - Source: el éxito del diálogo dispara `load()` para refrescar.
    - Behavior: `pnpm exec vue-tsc --noEmit` (admin) sin errores nuevos.
    - No new dependencies.
  </acceptance_criteria>
  <done>Cada fila de trial ofrece "Reprogramar" junto a "quitar"; ambos flujos funcionan; la lista se refresca tras reprogramar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin UI → POST /trials/:bookingId/reschedule | staff dispara la reprogramación (la autorización real la aplica el backend) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-08 | Elevation of Privilege | acción Reprogramar en el admin | mitigate | La UI NO relaja nada: el guard ALL_STAFF_ROLES + country-scope vive en el endpoint (Plan 164-01); el cliente sólo POSTea |
| T-164-09 | Repudiation / UX | feedback ambiguo ante fallo | mitigate | `$q.notify` positivo/negativo con `extractError`, espejando `removeTrial`; refresco de lista tras éxito |
| T-164-SC | Tampering | npm installs | accept | No se agregan dependencias en esta fase |
</threat_model>

<verification>
- `cd el-templo-admin && pnpm exec vue-tsc --noEmit` sin errores nuevos.
- `grep -n "rescheduleTrial" el-templo-admin/src/composables/useSchedulingApi.ts` muestra el cliente.
- Verificación visual (UAT de milestone): abrir el diálogo de Sesiones de Prueba del día, "Reprogramar" en una fila, elegir fecha+slot, confirmar → notificación positiva + la fila se mueve al nuevo turno.
</verification>

<success_criteria>
Gestión reprograma una sesión de prueba en un solo paso desde `SesionesDePruebaDialog.vue` (picker de fecha+slot → endpoint transaccional), con feedback estándar y el flujo viejo intacto, compilando con vue-tsc. (REPRO-01, lado frontend)
</success_criteria>

<output>
Create `.planning/phases/164-reprogramaci-n-y-reporte/164-02-SUMMARY.md` when done
</output>
