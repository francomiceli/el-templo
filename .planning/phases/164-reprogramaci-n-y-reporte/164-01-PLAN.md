---
phase: 164-reprogramaci-n-y-reporte
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/scheduling/trials-service.ts
  - el-templo-api/src/modules/scheduling/schemas.ts
  - el-templo-api/src/modules/scheduling/routes.ts
  - el-templo-api/test/scheduling/reschedule-trial.test.ts
autonomous: true
requirements: [REPRO-01]

must_haves:
  truths:
    - "Gestión reprograma una prueba en un solo paso: el turno viejo queda cancelado y el nuevo creado dentro de la MISMA transacción DB"
    - "Reprogramar dispara el reset Perdido → En seguimiento con source 'auto' (reusa el snippet de 163)"
    - "La regla una-prueba-por-vida NO bloquea la reprogramación (la vieja se cancela in-tx)"
    - "El endpoint valida slot existente, fecha válida y coherencia sede↔schedule igual que bookTrial"
    - "El endpoint exige rol ALL_STAFF_ROLES y respeta country-scope como el resto de /trials"
  artifacts:
    - path: "el-templo-api/src/modules/scheduling/trials-service.ts"
      provides: "rescheduleTrial(input) transaccional"
      contains: "async rescheduleTrial"
    - path: "el-templo-api/src/modules/scheduling/schemas.ts"
      provides: "rescheduleTrialSchema (params.bookingId + body {scheduleId,date,branchId})"
      contains: "rescheduleTrialSchema"
    - path: "el-templo-api/src/modules/scheduling/routes.ts"
      provides: "POST /trials/:bookingId/reschedule handler"
      contains: "/trials/:bookingId/reschedule"
    - path: "el-templo-api/test/scheduling/reschedule-trial.test.ts"
      provides: "integration tests del endpoint"
      contains: "reschedule"
  key_links:
    - from: "routes.ts POST /trials/:bookingId/reschedule"
      to: "trialService.rescheduleTrial"
      via: "handler call"
      pattern: "rescheduleTrial"
    - from: "trials-service.ts rescheduleTrial"
      to: "schema.users leadStatusSource='auto'"
      via: "reset dentro de la tx"
      pattern: "leadStatusSource"
---

<objective>
Backend transaccional de la acción "Reprogramar" de una sesión de prueba (REPRO-01). Un endpoint admin nuevo cancela la booking de prueba vieja y crea la nueva en UNA sola transacción DB, disparando el reset de estado de la fase 163, con las mismas validaciones que `bookTrial`.

Purpose: Convertir "quitar turno + volver a cargar" en una operación atómica de primera clase, base sobre la que se monta la UI del admin (Plan 164-02).
Output: `rescheduleTrial` en el service, `rescheduleTrialSchema`, ruta `POST /trials/:bookingId/reschedule`, y tests de integración.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164-reprogramaci-n-y-reporte/164-CONTEXT.md
@.planning/phases/164-reprogramaci-n-y-reporte/164-PATTERNS.md
@.planning/phases/163-m-quina-de-estados-autom-tica-del-lead/163-03-SUMMARY.md
@./CLAUDE.md

Repo constraints (deep work): Spanish user-facing strings; NO new dependencies; never push; stage explicit paths. Local gate: `pnpm tsc --noEmit` (api). Tests corren en CI — localmente sólo se corre el archivo nuevo/cambiado con `pnpm vitest run <file>`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rescheduleTrial (service + schema + route)</name>
  <files>el-templo-api/src/modules/scheduling/trials-service.ts, el-templo-api/src/modules/scheduling/schemas.ts, el-templo-api/src/modules/scheduling/routes.ts</files>
  <read_first>
    - el-templo-api/src/modules/scheduling/trials-service.ts — `bookTrial` (~586-730): validación de schedule (~590-602), coherencia sede `userRow.branchId !== scheduleRow.branchId` (~620-625), guard una-prueba-por-vida PENDING (~627-651, DEBE saltearse/scoparse), tx skeleton (~659-717) con rama reactivate-or-insert (~684-716) para el UNIQUE `(member_id, schedule_id, booking_date)`, y el reset `leadStatusSource:"auto"` (~680). También `reserveTrialSelfService` (~284-349) como segundo template del reset (~299) y forma de body `{scheduleId, date, branchId}` (~70-74).
    - el-templo-api/src/modules/scheduling/booking-service.ts — `adminRemoveBooking` (~753-795): soft-cancel `status='cancelado', cancelledAt, waitlistPosition:null` (~777-791) y `promoteWaitlist` (privado, abre su PROPIA tx ~1698). Cabecera trials (~20-22): las pruebas `is_trial=1` NO ocupan capacidad → la promoción de waitlist sobre una prueba cancelada es casi siempre no-op.
    - el-templo-api/src/modules/scheduling/schemas.ts — `errorSchema` (~9), `bookTrialSchema` (~548-571), `adminRemoveBookingSchema` (~682-699).
    - el-templo-api/src/modules/scheduling/routes.ts — hook onRequest guard ALL_STAFF_ROLES + attachCountryScope (~116-125), `POST /trials` handler (~597-610), `DELETE /bookings/:bookingId` (~577-588), `handleServiceError` uso (imports ~32).
  </read_first>
  <behavior>
    - rescheduleTrial({bookingId, scheduleId, date, branchId}) abre UNA `this.db.transaction`: (1) soft-cancel de la booking vieja (`status='cancelado', cancelledAt=new Date(), waitlistPosition=null`), (2) reset del lead `leadStatus:'en_seguimiento', leadStatusSource:'auto'` (snippet verbatim de bookTrial ~680, D-02), (3) crear la booking nueva con la rama reactivate-or-insert de bookTrial (respeta el UNIQUE). Retorna `{ bookingId }` de la nueva.
    - Validaciones reusadas de bookTrial: schedule existe → NotFoundError (404); coherencia sede↔schedule (`branchId`/`userRow.branchId` vs `scheduleRow.branchId`) → ConflictError (409). La regla una-prueba-por-vida NO debe bloquear (la vieja se canceló in-tx: cancelar primero dentro de la tx, o excluir la bookingId reprogramada del pending-check).
    - promoteWaitlist NO se llama dentro de la tx (evitar tx anidada): correr después del commit o omitir (prueba no ocupa capacidad → no-op). Elegir la opción (a) de PATTERNS §trials-service para mantener la forma single-tx.
  </behavior>
  <action>
    En `schemas.ts`: exportar `rescheduleTrialSchema` componiendo `params:{bookingId:integer}` (de adminRemoveBookingSchema) + `body:{scheduleId:integer≥1, date:string pattern ^\d{4}-\d{2}-\d{2}$, branchId:integer≥1, additionalProperties:false}` (nombre de campo `date`, NO `bookingDate`, para matchear el body de D-01/CONTEXT) + `response:{200:{bookingId:integer},400/404/409:errorSchema}`.
    En `trials-service.ts`: agregar `async rescheduleTrial(input: RescheduleTrialInput): Promise<{ bookingId: number }>` (definir el input type junto a `BookTrialInput`). Reusar imports existentes (`and, desc, eq, inArray, ne, sql`, error classes, `todayInTz`, `BookingService`) — no agregar nada. NO duplicar la lógica de reset (D-02): copiar el mismo `tx.update(schema.users).set({ leadStatus:'en_seguimiento', leadStatusSource:'auto' })` de bookTrial. Loggear con `this.log.info` al final como bookTrial. La validación de ventana de fecha es Claude's Discretion (bookTrial no la re-valida; `reserveTrialSelfService` sí via `validateTrialBookingDate`) — elegir paridad con bookTrial salvo que la coherencia de sede lo exija.
    En `routes.ts`: agregar `fastify.post<{Params:{bookingId:number}; Body:{scheduleId:number; date:string; branchId:number}}>("/trials/:bookingId/reschedule", { schema: rescheduleTrialSchema }, ...)`. Devolver `reply.code(200)` (mutación de un lead existente, no 201). Error path: `handleServiceError(err, reply, request.log, "reschedule trial")` — NO hand-roll de try/catch. NO agregar guard per-route: el hook onRequest del plugin ya aplica ALL_STAFF_ROLES + attachCountryScope.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: `schemas.ts` exporta `rescheduleTrialSchema` con `params.bookingId`, body `{scheduleId,date,branchId}` (`additionalProperties:false`) y `response.200 = {bookingId}`.
    - Source: `trials-service.ts` contiene `async rescheduleTrial` que abre exactamente UNA `this.db.transaction` conteniendo el soft-cancel de la vieja, el reset `leadStatusSource:'auto'`, y la creación de la nueva.
    - Source: `grep -n "leadStatusSource" trials-service.ts` muestra el reset dentro de rescheduleTrial (reuso, no lógica nueva).
    - Source: `routes.ts` contiene la ruta `/trials/:bookingId/reschedule` con `handleServiceError(...)` y SIN guard per-route adicional.
    - Behavior: `pnpm tsc --noEmit` (api) limpio, sin `any` nuevos.
    - No new dependencies added to package.json.
  </acceptance_criteria>
  <done>rescheduleTrial + schema + ruta compilan; el reset y la validación reusan el código de bookTrial; guard heredado del hook del plugin.</done>
</task>

<task type="auto">
  <name>Task 2: Tests de integración de reschedule</name>
  <files>el-templo-api/test/scheduling/reschedule-trial.test.ts</files>
  <read_first>
    - el-templo-api/test/scheduling/trials.test.ts — happy path POST /trials (~181-229); helpers `getAuthToken` (~32/57-58), `adminToken`, `createActivity` (~78), `createScheduleSlot` (~89), `createPruebaUser` (~145), `getFutureSlot` (~124), y el patrón `app.inject`.
    - el-templo-api/test/expire-lost-leads.test.ts — `dateDaysAgo` LOCAL-date (~53-59, NO usar variante UTC por el bug ART/CURDATE), `seedLead({leadStatus, leadStatusSource})` (~76-100), `seedTrialBooking(userId, daysAgo, status)` (~102-118), `leadStatusOf(userId)` (~136-147) que devuelve `{leadStatus, leadStatusSource}`.
    - el-templo-api/src/modules/scheduling/trials-service.ts — la firma final de rescheduleTrial (del Task 1) para conocer el body.
  </read_first>
  <action>
    Crear `test/scheduling/reschedule-trial.test.ts` siguiendo la estructura de `trials.test.ts` (setup app + adminToken, helpers de seed). Cubrir los 4 casos de specifics.68:
    (1) reprogramar cancela la booking vieja y crea una nueva activa en una tx — assert vieja `status='cancelado'`, nueva `status='reservado'`, ambas `is_trial=1`;
    (2) dispara el reset Perdido → En seguimiento con source 'auto' — sembrar el lead como `leadStatus:'perdido', leadStatusSource:'manual'` (caso más exigente, como hizo 163-03) y assert `leadStatusOf` → `{leadStatus:'en_seguimiento', leadStatusSource:'auto'}`;
    (3) respeta validaciones de slot — slot inexistente → 404; cross-branch (schedule de otra sede) → 409;
    (4) NO tropieza con la regla una-prueba-por-vida (la vieja quedó cancelada in-tx) — reprogramar dos veces seguidas funciona.
    Autenticar con el `adminToken` del helper. NO correr el suite completo (regla del repo). Correr sólo este archivo con `pnpm vitest run test/scheduling/reschedule-trial.test.ts`.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm vitest run test/scheduling/reschedule-trial.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: existe `test/scheduling/reschedule-trial.test.ts` con ≥4 `it(`/`test(` cubriendo los casos (1)-(4).
    - Behavior: `pnpm vitest run test/scheduling/reschedule-trial.test.ts` pasa (verde contra `eltemplo_test`).
    - Behavior: el caso (2) siembra `leadStatusSource:'manual'` y afirma que el reset lo pisa a `'auto'`.
    - Behavior: el caso (3) afirma 404 (slot inexistente) y 409 (cross-branch).
    - No new dependencies.
  </acceptance_criteria>
  <done>Los 4 casos verdes; el archivo de test citado corre aislado sin el resto del suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin client → POST /trials/:bookingId/reschedule | staff autenticado envía bookingId/scheduleId/branchId/date (input no confiable) |
| service → DB (bookings, users) | mutación transaccional de estado de booking y lead |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-01 | Elevation of Privilege | POST /trials/:bookingId/reschedule | mitigate | Reusar el hook onRequest del plugin (ALL_STAFF_ROLES + `fastify.authenticate`); NO agregar ruta fuera del plugin ni guard más laxo |
| T-164-02 | Information Disclosure / Tampering | reschedule de una booking de otra sede | mitigate | `attachCountryScope` del hook + coherencia sede↔schedule de bookTrial (409 cross-branch); test (3) lo cubre |
| T-164-03 | Tampering | body con campos extra / tipos inválidos | mitigate | `rescheduleTrialSchema` con `additionalProperties:false` + tipos/pattern de fecha; AJV valida antes del handler |
| T-164-04 | Denial of Service (integridad) | fallo parcial deja lead sin turno o con dos activos | mitigate | Cancel-old + reset + create-new en UNA `db.transaction` (rollback atómico); test (1) afirma el estado final |
| T-164-SC | Tampering | npm installs | accept | No se agregan dependencias en esta fase |
</threat_model>

<verification>
- `cd el-templo-api && pnpm tsc --noEmit` limpio.
- `cd el-templo-api && pnpm vitest run test/scheduling/reschedule-trial.test.ts` verde.
- `grep -n "rescheduleTrial" el-templo-api/src/modules/scheduling/routes.ts` muestra la ruta con `handleServiceError`.
</verification>

<success_criteria>
Existe un endpoint admin `POST /api/admin/scheduling/trials/:bookingId/reschedule` que, en una sola transacción, cancela la prueba vieja, crea la nueva y resetea el lead a En seguimiento (source auto), con las validaciones de bookTrial y el guard ALL_STAFF_ROLES, cubierto por tests de integración verdes. (REPRO-01, lado backend)
</success_criteria>

<output>
Create `.planning/phases/164-reprogramaci-n-y-reporte/164-01-SUMMARY.md` when done
</output>
