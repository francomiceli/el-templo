---
phase: 164-reprogramaci-n-y-reporte
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/reports/service.ts
  - el-templo-api/src/modules/reports/types.ts
  - el-templo-api/src/modules/reports/schemas.ts
  - el-templo-api/src/modules/reports/routes.ts
  - el-templo-api/test/reports-trial-sessions.test.ts
autonomous: true
requirements: [REPRO-02, REPRO-03]

must_haves:
  truths:
    - "Cada fila del reporte de Sesiones de Prueba trae `reschedules`: COUNT de las bookings de prueba canceladas del lead (retroactivo, sin schema nuevo)"
    - "Cada fila trae `leadStatusSource` ('auto'|'manual'|null); null se trata como automático/histórico"
    - "El endpoint filtra por `leadStatusSource=auto|manual`; auto incluye las filas NULL"
    - "El CSV exporta 'Reprogramaciones' y 'Origen estado' al final de las columnas existentes"
    - "El filtro respeta CAJA_ROLES + country-scope existentes y no filtra por owner (disponible a todos los report-viewers)"
  artifacts:
    - path: "el-templo-api/src/modules/reports/service.ts"
      provides: "COUNT correlacionado de canceladas + lead_status_source en el SELECT + filtro + CSV"
      contains: "AS reschedules"
    - path: "el-templo-api/src/modules/reports/types.ts"
      provides: "reschedules + leadStatusSource en row/filters"
      contains: "leadStatusSource"
    - path: "el-templo-api/src/modules/reports/schemas.ts"
      provides: "querystring + row props para reschedules/leadStatusSource"
      contains: "leadStatusSource"
    - path: "el-templo-api/src/modules/reports/routes.ts"
      provides: "passthrough del filtro leadStatusSource"
      contains: "leadStatusSource"
    - path: "el-templo-api/test/reports-trial-sessions.test.ts"
      provides: "tests de count + source + filtro"
      contains: "reschedules"
  key_links:
    - from: "service.ts getTrialSessionsReport SELECT"
      to: "bookings canceladas del member"
      via: "correlated COUNT subquery aliaseada"
      pattern: "AS reschedules"
    - from: "routes.ts buildTrialSessionsFilters"
      to: "service.ts buildTrialSessionsConditions"
      via: "filters.leadStatusSource"
      pattern: "leadStatusSource"
---

<objective>
Derivar en el reporte de Sesiones de Prueba (a) el contador de reprogramaciones por lead (REPRO-02) y (b) el origen del estado auto/manual con filtro (REPRO-03). Todo derivado de datos existentes: contador = COUNT de bookings de prueba canceladas del member; origen = `lead_status_source` de la fase 163. Sin schema nuevo, retroactivo gratis.

Purpose: Exponer el historial de "ruido del lead" y la trazabilidad auto/manual en el reporte y el CSV, base sobre la que se monta la UI del reporte (Plan 164-04).
Output: COUNT correlacionado + `lead_status_source` en `getTrialSessionsReport`, filtro `leadStatusSource`, columnas CSV, tipos/schemas, y tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164-reprogramaci-n-y-reporte/164-CONTEXT.md
@.planning/phases/164-reprogramaci-n-y-reporte/164-PATTERNS.md
@.planning/phases/163-m-quina-de-estados-autom-tica-del-lead/163-03-SUMMARY.md
@./CLAUDE.md

Repo constraints (deep work): Spanish user-facing strings; NO new dependencies; never push; stage explicit paths. Local gate: `pnpm tsc --noEmit` (api). Tests corren en CI — localmente sólo el archivo cambiado con `pnpm vitest run <file>`.

<interfaces>
Gotcha OBLIGATORIO (MEMORY reference_drizzle_select_unqualified_columns): el gotcha muerde al QUERY BUILDER `.select()`. `getTrialSessionsReport` usa RAW `this.db.execute<T>(sql\`...\`)` con aliases explícitos (`b,u,s,br,a,creator,pp` + derived `latest_trial`) → el COUNT correlacionado se escribe con alias explícito (`rc.member_id = u.id`) y es SEGURO. Bindear todo valor con `${...}`, NUNCA `sql.raw`/concat (nota de SQL-injection ~1468-1473).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: reschedule count + lead_status_source + filtro + CSV</name>
  <files>el-templo-api/src/modules/reports/service.ts, el-templo-api/src/modules/reports/types.ts, el-templo-api/src/modules/reports/schemas.ts, el-templo-api/src/modules/reports/routes.ts</files>
  <read_first>
    - el-templo-api/src/modules/reports/service.ts — `getTrialSessionsReport` (~1475-1606): derived `latest_trial` (~1456), page query (~1537-1578) con el LEFT JOIN `attended` a espejar (~1503-1507/1567-1571), los dos row-type generics inline duplicados (~1518-1536 y ~1580-1599), count query (~1487-1511, NO tocar la lógica de conteo de leads); `mapTrialSessionRow` (~1813-1897, estilo null-coalescing `createdBy` ~1867-1873); `buildTrialSessionsConditions` (~1690-1804, predicado simple `gestionaUserId` ~1754-1759); `exportTrialSessions` (~1624-1678, arrays `headers` ~1633-1647 + `cells` ~1658-1672, consts `asistidoLabel`/`estadoLabel` ~1653-1657).
    - el-templo-api/src/modules/reports/types.ts — `TrialSessionsRow` (~207) y `TrialSessionsFilters` (~179), convención JSDoc-por-campo.
    - el-templo-api/src/modules/reports/schemas.ts — `trialSessionsQuerystringProps` (~468-490, enum style `shift`/`attended` ~485-486) y `trialSessionsRowSchema` (~492-541, nullable-enum style `leadStatus` ~506-514).
    - el-templo-api/src/modules/reports/routes.ts — `buildTrialSessionsFilters` (~882-942): request generic querystring (~884-901), objeto filters retornado (~928-941), owner-strip de `gestionaUserId` (~907-918, NO replicar), `country` de `request.scope.country` (~930). Guard CAJA_ROLES plugin-level (~58).
  </read_first>
  <action>
    En `service.ts` → `getTrialSessionsReport` (page query): agregar en la SELECT list un COUNT correlacionado aliaseado que cuenta TODAS las pruebas canceladas del lead (proxy de ruido del lead, incluye self-service canceladas — specifics.67): `(SELECT COUNT(*) FROM bookings AS rc WHERE rc.member_id = u.id AND rc.is_trial = 1 AND rc.booking_status = 'cancelado') AS reschedules`. Verificar el nombre real de la columna de estado de booking (`booking_status` vs `status`) contra el schema antes de escribir. Agregar también `u.lead_status_source AS lead_status_source` a la SELECT list. Actualizar AMBOS row-type generics inline duplicados (~1518-1536 y ~1580-1599) con `reschedules` y `lead_status_source`. NO modificar la count query de leads (~1487-1511).
    En `mapTrialSessionRow`: agregar `reschedules: Number(r.reschedules)` y `leadStatusSource: r.lead_status_source ?? null` al input type y al objeto retornado (estilo `createdBy`).
    En `buildTrialSessionsConditions`: agregar el filtro `leadStatusSource` espejando el predicado `gestionaUserId` — si `=== 'auto'` push `sql\`(u.lead_status_source = 'auto' OR u.lead_status_source IS NULL)\`` (auto incluye NULL = histórico/desconocido, D-05); si no, `sql\`u.lead_status_source = ${filters.leadStatusSource}\``. Bindear con `${...}`.
    En `exportTrialSessions`: agregar `"Reprogramaciones"` y `"Origen estado"` al FINAL del array `headers` y las celdas matcheantes al FINAL de `cells` — reschedules → `String(row.reschedules)`; origen → label ES via const local (`manual` → "Manual", else "Automático"), estilo `asistidoLabel`/`estadoLabel`.
    En `types.ts`: `TrialSessionsRow` += `reschedules: number;` y `leadStatusSource: "auto" | "manual" | null;`; `TrialSessionsFilters` += `leadStatusSource?: "auto" | "manual";` (JSDoc por campo).
    En `schemas.ts`: `trialSessionsQuerystringProps` += `leadStatusSource: { type: "string", enum: ["auto","manual"] }`; `trialSessionsRowSchema` += `reschedules: { type: "integer" }` y `leadStatusSource: { anyOf: [{ type:"string", enum:["auto","manual"] }, { type:"null" }] }`.
    En `routes.ts` → `buildTrialSessionsFilters`: agregar `leadStatusSource?: "auto"|"manual"` al request generic querystring y `leadStatusSource: q.leadStatusSource` (una línea) al objeto retornado. SIN owner-strip (a diferencia de `gestionaUserId`): el filtro está disponible a todos los report-viewers; `country` sigue viniendo de `request.scope.country`.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n "AS reschedules" service.ts` muestra el COUNT correlacionado con alias `rc` (`rc.member_id = u.id AND rc.is_trial = 1 AND rc.<estado> = 'cancelado'`).
    - Source: `grep -n "lead_status_source" service.ts` muestra `u.lead_status_source AS lead_status_source` en la SELECT y el predicado de filtro con la rama auto-incluye-NULL.
    - Source: `buildTrialSessionsConditions` bindea el valor del filtro con `${...}` (nunca `sql.raw`/concat).
    - Source: `exportTrialSessions` tiene "Reprogramaciones" y "Origen estado" como ÚLTIMAS entradas de `headers` y celdas correspondientes al final de `cells`.
    - Source: `types.ts`, `schemas.ts` (querystring + row) y `routes.ts` incluyen `leadStatusSource`/`reschedules`; `routes.ts` NO agrega owner-strip para `leadStatusSource`.
    - Behavior: `pnpm tsc --noEmit` (api) limpio; sin `any` nuevos.
  </acceptance_criteria>
  <done>El reporte y el export exponen reschedules + leadStatusSource; el filtro auto incluye NULL; sin schema nuevo ni owner-strip del nuevo filtro.</done>
</task>

<task type="auto">
  <name>Task 2: Tests de reschedules + source + filtro</name>
  <files>el-templo-api/test/reports-trial-sessions.test.ts</files>
  <read_first>
    - el-templo-api/test/reports-trial-sessions.test.ts — `SeedLeadOpts` (~158) + `seedLead` (~169-207, agregar `leadStatusSource` a las opciones/insert), `seedBooking` (~225-238, ya soporta `status:'cancelado'` + `isTrial`), `ctx.ownerToken`/`ctx.adminArToken` (~45/55/252/272), `REPORTS_URL` (~41), patrón `ctx.app.inject` a `${REPORTS_URL}/trial-sessions` (~340).
    - el-templo-api/src/modules/reports/service.ts — la forma final de la fila (del Task 1) para conocer `reschedules`/`leadStatusSource`.
  </read_first>
  <action>
    Extender `test/reports-trial-sessions.test.ts` (agregar `leadStatusSource` a `SeedLeadOpts`/`seedLead` si falta). Cubrir:
    (1) contador: sembrar un lead con N bookings de prueba canceladas + 1 activa → assert `row.reschedules === N`;
    (2) source: sembrar un lead con `leadStatusSource:'manual'` y otro con `null` → assert `row.leadStatusSource` respectivo;
    (3) filtro: pegar `?leadStatusSource=manual` (sólo manual) y `?leadStatusSource=auto` (auto DEBE incluir las filas NULL) → assert el conjunto filtrado.
    Autenticar con `ctx.ownerToken`. Correr sólo este archivo con `pnpm vitest run test/reports-trial-sessions.test.ts` (regla del repo: no correr el suite completo).
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm vitest run test/reports-trial-sessions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: existen casos que afirman `reschedules === N`, `leadStatusSource` correcto, y el filtrado por `?leadStatusSource=auto|manual`.
    - Behavior: el caso de filtro `auto` afirma que las filas con `leadStatusSource=null` SÍ aparecen.
    - Behavior: `pnpm vitest run test/reports-trial-sessions.test.ts` verde contra `eltemplo_test`.
    - No new dependencies.
  </acceptance_criteria>
  <done>Los casos de count/source/filtro verdes en el archivo citado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| report-viewer → GET /reports/trial-sessions (+/export) | staff con CAJA_ROLES envía querystring incl. `leadStatusSource` (input no confiable) |
| service → DB (bookings, users) | agregación read-only con subquery correlacionada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-05 | Tampering / Injection | filtro `leadStatusSource` en SQL raw | mitigate | Enum AJV `["auto","manual"]` en el schema + binding `${...}` en `buildTrialSessionsConditions`; nunca `sql.raw`/concat |
| T-164-06 | Information Disclosure | fuga de datos cross-country/cross-scope vía el nuevo filtro | mitigate | El filtro NO altera el scope: `country` sigue de `request.scope.country` y el guard CAJA_ROLES plugin-level se mantiene; sin owner-strip especial porque el campo no expone datos de otro scope |
| T-164-07 | Information Disclosure | el COUNT correlacionado cuenta bookings de otros members | mitigate | Subquery scopeada por `rc.member_id = u.id`; el driver `latest_trial` ya aplica las condiciones de sede/estado del reporte |
| T-164-SC | Tampering | npm installs | accept | No se agregan dependencias en esta fase |
</threat_model>

<verification>
- `cd el-templo-api && pnpm tsc --noEmit` limpio.
- `cd el-templo-api && pnpm vitest run test/reports-trial-sessions.test.ts` verde.
- `grep -n "AS reschedules\|lead_status_source" el-templo-api/src/modules/reports/service.ts` muestra el COUNT y la columna source.
</verification>

<success_criteria>
El reporte de Sesiones de Prueba (JSON + CSV) expone el contador de reprogramaciones por lead (derivado, retroactivo) y el origen del estado auto/manual, con filtro `leadStatusSource=auto|manual` (auto incluye NULL), respetando CAJA_ROLES + country-scope, cubierto por tests verdes. (REPRO-02, REPRO-03, lado backend)
</success_criteria>

<output>
Create `.planning/phases/164-reprogramaci-n-y-reporte/164-03-SUMMARY.md` when done
</output>
