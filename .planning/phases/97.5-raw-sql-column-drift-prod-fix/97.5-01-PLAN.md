---
phase: 97.5-raw-sql-column-drift-prod-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-bot/src/ai/tools.ts
  - el-templo-bot/src/state/machine.ts
  - el-templo-api/test/whatsapp/ai-tools-membership-drift.test.ts
  - el-templo-api/test/lint/raw-sql-column-drift.test.ts
autonomous: true
requirements: [DRIFT-01, DRIFT-02]

must_haves:
  truths:
    - "1. RED integration test exists at el-templo-api/test/whatsapp/ai-tools-membership-drift.test.ts and reproduces `Unknown column 'sub.status'` / `'s.status'` (MySQL errno 1054 / sqlState 42S22) on master HEAD."
    - "2. GREEN applied at all 3 drift sites via Option B (rename `status` -> `subscription_status` in both SubscriptionRow interfaces, in 3 raw-SQL references, and in 3 JS-side reads); the RED integration tests turn GREEN."
    - "3. Sweep-lint at el-templo-api/test/lint/raw-sql-column-drift.test.ts runs under `pnpm test` (auto-included by vitest glob `test/**/*.test.ts`); asserts zero drift across el-templo-bot/src/** and el-templo-api/src/**; the synthetic-drift positive-control fixture passes (and stays PERMANENT)."
    - "4. Zero modifications outside the 4 prod surfaces (tools.ts, machine.ts, ai-tools-membership-drift.test.ts, raw-sql-column-drift.test.ts) plus the 2 inline SubscriptionRow interface declarations contained within tools.ts and machine.ts."
    - "5. Phase 98 unblocked: cherry-picking `phase-98-preserve/task-1-green-baseline` + `98-TASK-2-WIP.patch` after Phase 97.5 GREEN no longer surfaces `Unknown column 'sub.status'`."
    - "6. Sweep-lint live-introspection rename map (Drizzle `getTableColumns`) covers all 10 high-risk plain-word->prefixed renames: subscriptions.status->subscription_status, bookings.status->booking_status, attendance.status->attendance_status, attendance.source->attendance_source, whatsapp_conversations.status->conversation_status, whatsapp_messages.direction->message_direction, whatsapp_messages.messageType->wa_message_type, aura_config.sourceType->aura_config_source_type, exercises.level->exercise_level, format_compatibility.level->compat_level (subset-not-equality assertion)."
  artifacts:
    - path: "el-templo-api/test/whatsapp/ai-tools-membership-drift.test.ts"
      provides: "RED integration test reproducing sub.status / s.status drift via real MySQL"
      contains: 'describe("DRIFT-01'
    - path: "el-templo-api/test/lint/raw-sql-column-drift.test.ts"
      provides: "Permanent sweep-lint guardrail with synthetic-drift positive control + must-include subset coverage"
      contains: "raw SQL column drift"
    - path: "el-templo-bot/src/ai/tools.ts"
      provides: "checkMembership query using subscription_status (Option B rename)"
      contains: "sub.subscription_status"
    - path: "el-templo-bot/src/state/machine.ts"
      provides: "determineClientState query using subscription_status (Option B rename)"
      contains: "s.subscription_status"
  key_links:
    - from: "el-templo-bot/src/ai/tools.ts:495,500"
      to: "el-templo-api/src/db/schema/subscriptions.ts (mysqlEnum subscription_status)"
      via: "raw SQL column reference matches Drizzle column.name"
      pattern: "sub\\.subscription_status"
    - from: "el-templo-bot/src/state/machine.ts:77"
      to: "el-templo-api/src/db/schema/subscriptions.ts"
      via: "raw SQL column reference matches Drizzle column.name"
      pattern: "s\\.subscription_status"
    - from: "el-templo-api/test/lint/raw-sql-column-drift.test.ts"
      to: "el-templo-api/src/db/schema/*.ts"
      via: "getTableColumns() walk of schema exports at test boot"
      pattern: "getTableColumns"
---

<objective>
Eliminate the raw-SQL <-> Drizzle column-name drift class in `el-templo-bot` by fixing the 3 confirmed `subscriptions.status` drift sites via the Option B inline-rename pattern (D-04), proving the bug exists on master via a RED MySQL-errno integration test (D-01), and shipping a permanent sweep-lint guardrail (D-02 + D-03) that detects future drift across all bot + api raw SQL templates.

Purpose: Unblock Phase 98 retry (cherry-pick can land without surfacing `Unknown column 'sub.status'`) and Phase 97 RGUARD-01 (green-on-real-MySQL baseline for v5.3.3 regression suite). Generalize the Phase 95 (vi) per-table fix into a class-level guard so this drift class cannot regress without a loud test failure.

Output:

- 2 new test files: `el-templo-api/test/whatsapp/ai-tools-membership-drift.test.ts` (RED integration), `el-templo-api/test/lint/raw-sql-column-drift.test.ts` (permanent sweep-lint).
- 2 modified prod files: `el-templo-bot/src/ai/tools.ts` (4 edits: 1 interface, 2 SQL, 1 JS read), `el-templo-bot/src/state/machine.ts` (4 edits: 1 interface, 1 SQL, 2 JS reads).
- 3 atomic commits per D-05: (1) RED (both test files, both failing on master), (2) GREEN (Option B rename across both prod files), (3) SUMMARY.
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97.5-raw-sql-column-drift-prod-fix/97.5-CONTEXT.md
@.planning/phases/97.5-raw-sql-column-drift-prod-fix/97.5-PATTERNS.md
@CLAUDE.md

<interfaces>
Key interfaces extracted from authoritative sources so the executor does not need to re-explore.

From `el-templo-bot/src/ai/tools.ts:452-460` (current — BEFORE rename), the `SubscriptionRow` interface contains 7 fields: `plan_name: string`, `status: string` (becomes `subscription_status`), `start_date: string`, `end_date: string | null`, `price_paid: number`, `classes_per_week: number | null`, `multi_branch: number`.

From `el-templo-bot/src/state/machine.ts:39-43` (current — BEFORE rename), the `SubscriptionRow` interface contains 3 fields: `status: string` (becomes `subscription_status`), `end_date: string | null`, `is_trial: number`.

From `el-templo-api/src/db/schema/subscriptions.ts:18-23, :51` (source of truth) — `mysqlEnum("subscription_status", ["active","paused","cancelled","expired"])`. The SQL column name is `subscription_status`; the JS property name is `status` (intentional Drizzle rename).

From `el-templo-api/node_modules/drizzle-orm/utils.d.ts:37` — `getTableColumns<T>(table: T): T['_']['columns']`. Documented, stable accessor. Each column object exposes a runtime `.name` property holding the SQL column name.

From `el-templo-api/test/whatsapp/ai-tools.test.ts:1-43` (canonical bot-tool integration test boot — DO NOT use `createTestApp` from `test/helpers.ts`; bot-tool tests pool+drizzle directly): `mysql.createPool({ host, port, user, password, database: "eltemplo_test" })`, then `drizzle(pool, { schema, mode: "default" })`, then `executeTool("check_membership", { phone }, db)`.

From `el-templo-bot/src/state/machine.ts` (export shape): `determineClientState(phone: string, db: DB): Promise<{ state: string; userId: number | null }>` — swallows errors and returns `{ state: "lead" }` (see `machine.ts:130-134` catch block); the RED assertion is on the RESULT shape (assert `result?.state === "active_member"`), NOT on a thrown error.

From `el-templo-api/vitest.config.ts:13` — `include: ['test/**/*.test.ts']` auto-includes the new `test/lint/` directory; no config change.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED): Add failing integration test for sub.status / s.status drift against real MySQL</name>
  <files>el-templo-api/test/whatsapp/ai-tools-membership-drift.test.ts</files>
  <read_first>
    - .planning/phases/97.5-raw-sql-column-drift-prod-fix/97.5-CONTEXT.md (D-01 + canonical_refs drift sites)
    - .planning/phases/97.5-raw-sql-column-drift-prod-fix/97.5-PATTERNS.md (Surface 3: integration-test boot, seed shape, RED assertion shape)
    - el-templo-api/test/whatsapp/ai-tools.test.ts (canonical boot pattern lines 1-43; cleanup-then-seed lines 45-100)
    - el-templo-bot/test/v5-3-3-booking.integration.test.ts (lines 486-549 — only existing MySQL-errno catch+assert precedent in repo)
    - el-templo-bot/src/ai/tools.ts (read :440-555 to confirm `executeTool("check_membership", ...)` call signature and `SubscriptionRow` shape)
    - el-templo-bot/src/state/machine.ts (read :30-135 to confirm `determineClientState` signature + the swallow-errors catch at :130-134)
    - el-templo-api/src/db/schema/branches.ts (confirm `branches.code` is at least varchar(10) so 'TSTMBR' fits — per PATTERNS drift item 4)
    - el-templo-api/src/db/schema/subscriptions.ts (:18-23, :51 — source of truth for `subscription_status` mysqlEnum)
  </read_first>
  <action>
    Create new file `el-templo-api/test/whatsapp/ai-tools-membership-drift.test.ts`. Follow the canonical bot-tool integration-test shape (per D-01 + PATTERNS Surface 3): import `describe, it, expect, beforeAll, afterAll, beforeEach` from "vitest"; import `drizzle` from "drizzle-orm/mysql2"; import `mysql` from "mysql2/promise"; import `* as schema` from "../../src/db/schema/index"; import `executeTool` from "../../../el-templo-bot/src/ai/tools"; import `determineClientState` from "../../../el-templo-bot/src/state/machine". DO NOT import `createTestApp` from `test/helpers.ts` (that is for Fastify-routes tests — flagged in PATTERNS drift item 1). Declare `let pool: mysql.Pool` and `let db: ReturnType<typeof drizzle<typeof schema>>` at module scope. In `beforeAll`, build the pool from env vars (`DB_HOST`/`DB_PORT`/`DB_USER`/`DB_PASSWORD` with the same fallbacks as `ai-tools.test.ts`) targeting database `eltemplo_test`, then `db = drizzle(pool, { schema, mode: "default" })`. In `afterAll`, `await pool.end()`. In `beforeEach`, run reverse-FK-order DELETEs (bookings, subscriptions, schedules, then `subscription_plans WHERE name LIKE 'Test%'`, then `users WHERE email LIKE 'test-mbr-drift-%'`, then `activities WHERE name LIKE 'Test%'`, then `branches WHERE code LIKE 'TST%'`) and seed: a branch with `code='TSTMBR'` (5 chars; per D-01 Claude's-Discretion + PATTERNS drift item 4 — confirm via Read against branches.ts schema), a user with phone `'5491100099001'` and first_name `'DriftJuan'`, a `subscription_plans` row with `name='Test MembershipDrift Plan'`, and an ACTIVE `subscriptions` row using the prod SQL-column name (i.e. the INSERT writes `subscription_status='active'` — the seed must succeed regardless of whether the bot SELECT is fixed). Use the exact seed shape sketched in PATTERNS Surface 3 (lines 340-381). Then write TWO `describe` blocks tied to DRIFT-01: (a) `"DRIFT-01 — checkMembership SQL references sub.status (must be sub.subscription_status)"` containing two `it()` cases — the first wraps `executeTool("check_membership", { phone: "5491100099001" }, db)` in try/catch, asserts `caught === undefined`, `typeof result === "string"`, `result` contains `"DriftJuan"` and `"Activa"`; the second wraps the same call, captures `err.message`, asserts `.not.toMatch(/sub\.status|Unknown column/i)` (verbatim Phase 95 (vi) shape from `v5-3-3-booking.integration.test.ts:521-547`). (b) `"DRIFT-01 — determineClientState SQL references s.status (must be s.subscription_status)"` containing one `it()` case wrapping `determineClientState("5491100099001", db)` in try/catch and asserting `caught === undefined && result?.state === "active_member"` (RED on master because `machine.ts:130-134` swallows the SQL error and returns `{ state: "lead" }` — assertion is on result shape, NOT on a thrown error; this divergence from check_membership is intentional and called out in PATTERNS Surface 3). Use `unknown` for caught variables (`let caught: unknown`) and narrow with `err instanceof Error ? err.message : String(err)`. No `any`. Write clear JSDoc above each describe explaining: (1) this is RED on master per Phase 97.5 D-01, (2) what the Option B fix will be, (3) which prod surface line ranges are responsible. This file must FAIL on master HEAD `3f330787`; it will turn GREEN after Task 3 (per D-05 commit chain).
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm vitest run test/whatsapp/ai-tools-membership-drift.test.ts 2>&amp;1 | tee /tmp/97.5-task1-red.log; grep -E "Unknown column|sub\.status|s\.status" /tmp/97.5-task1-red.log</automated>
  </verify>
  <acceptance_criteria>
    - File `el-templo-api/test/whatsapp/ai-tools-membership-drift.test.ts` exists.
    - `cd el-templo-api && pnpm vitest run test/whatsapp/ai-tools-membership-drift.test.ts` exits NON-ZERO (test failure / errors).
    - Vitest stderr or stdout contains the literal substring `Unknown column 'sub.status'` (errno 1054 / sqlState 42S22) — from the `check_membership` describe block.
    - `grep -c "createTestApp" el-templo-api/test/whatsapp/ai-tools-membership-drift.test.ts` returns 0 (negative check — must NOT use the Fastify helper; per PATTERNS drift item 1).
    - `grep -c "TSTMBR" el-templo-api/test/whatsapp/ai-tools-membership-drift.test.ts` returns at least 1 (seed branch code).
    - `grep -cE 'describe\("DRIFT-01' el-templo-api/test/whatsapp/ai-tools-membership-drift.test.ts` returns at least 2 (one for `checkMembership`, one for `determineClientState`).
    - `cd el-templo-api && pnpm tsc --noEmit` exits 0 (test file type-checks even on master, since SubscriptionRow types remain valid; the runtime failure is the SQL column error).
    - No `any` type used: `grep -vE '^\s*(#|//|\*)' el-templo-api/test/whatsapp/ai-tools-membership-drift.test.ts | grep -cE ':\s*any\b|<any>'` returns 0.
  </acceptance_criteria>
  <done>
    The RED integration test file exists, fails on master HEAD with the literal MySQL error `Unknown column 'sub.status' in 'field list'`, type-checks under `pnpm tsc --noEmit`, follows the pool+drizzle pattern (not `createTestApp`), and uses the `TSTMBR` namespace per Phase 98 D-05 precedent. Ready for commit (1) of the D-05 chain.
  </done>
</task>

<task type="auto">
  <name>Task 2 (RED): Add failing sweep-lint test that detects the 3 real drift sites + synthetic-drift positive control + Drizzle introspection rename map with must-include subset</name>
  <files>el-templo-api/test/lint/raw-sql-column-drift.test.ts</files>
  <read_first>
    - .planning/phases/97.5-raw-sql-column-drift-prod-fix/97.5-CONTEXT.md (D-02 sub-rules i/ii/iii, D-03 must-include list, D-05 RED-FIRST ordering)
    - .planning/phases/97.5-raw-sql-column-drift-prod-fix/97.5-PATTERNS.md (Surface 4 + sub-patterns a/b/c/d + "No Analog Found" + drift items 3 and 5)
    - el-templo-api/test/setup.ts (lines 31-38 — file-walking precedent: readdirSync + readFileSync)
    - el-templo-api/src/db/schema/subscriptions.ts (canonical `mysqlEnum("subscription_status", ...)` pattern — read full file ~70 lines)
    - el-templo-api/src/db/schema/bookings.ts (`mysqlEnum("booking_status", ...)`)
    - el-templo-api/src/db/schema/attendance.ts (`attendance_status`, `attendance_source`)
    - el-templo-api/src/db/schema/whatsapp.ts (`conversation_status`, `message_direction`, `wa_message_type`)
    - el-templo-api/src/db/schema/index.ts (the barrel that re-exports all tables — confirm enumeration shape)
    - el-templo-api/node_modules/drizzle-orm/utils.d.ts (confirm `getTableColumns<T>(table: T): T['_']['columns']` signature; locate `Table` class export for instanceof check; locate `getTableName` if present)
    - el-templo-api/vitest.config.ts (confirm glob `test/**/*.test.ts` auto-includes `test/lint/`)
    - el-templo-bot/src/ai/tools.ts (the file the scanner must flag at :495 and :500)
    - el-templo-bot/src/state/machine.ts (the file the scanner must flag at :77)
  </read_first>
  <action>
    Create new file `el-templo-api/test/lint/raw-sql-column-drift.test.ts`. The `test/lint/` directory does not yet exist; creating this file auto-creates it. Per vitest glob `test/**/*.test.ts`, it is auto-included by `pnpm test` (no config change). The scanner uses ONLY Node stdlib + `drizzle-orm` + vitest — ZERO new deps per D-02.

    Implement four logical sections inside the test file:

    (1) Drizzle introspection rename map builder. Import `getTableColumns` and `Table` from `"drizzle-orm"` (NOT `"drizzle-orm/utils"` — the function re-exports from the main entry per `utils.d.ts:37`; confirm during Read). Also attempt to import `getTableName` from `"drizzle-orm"` — confirm during Read. Import `* as schema` from `"../../src/db/schema/index"`. Define `function buildRenameMap(): Map&lt;string, Map&lt;string, string&gt;&gt;` (returns `tableName -> Map&lt;jsProperty, sqlColumn&gt;` containing ONLY entries where `jsProperty !== sqlColumn`). For each `[exportName, exportValue]` of `Object.entries(schema)`, skip if `!(exportValue instanceof Table)`. Read the SQL table name via `getTableName(exportValue)` (documented stable API). For columns, call `getTableColumns(exportValue)` and iterate; for each column, read its `.name` runtime property. Use `unknown`-cast narrowing (e.g., `(col as { name: string }).name`) — no `any`. Per CONTEXT.md Claude's-Discretion bullet 2 + PATTERNS drift item 3: if `getTableName` is unavailable on the installed drizzle-orm version, fall back to `(exportValue as { _: { name: string } })._.name` — but prefer the documented API.

    (2) Sweep-lint scanner — file walker + sql-block extractor + drift detector. Implement `function walkSourceFiles(roots: string[]): string[]` using `fs.readdirSync(dir, { recursive: true, withFileTypes: true })` (Node ≥ 20.1 — confirmed in PATTERNS Surface 4). Filter `.ts` extension; skip `.d.ts`, `node_modules`, `dist`. Roots are absolute paths derived from `path.resolve(__dirname, "../../../el-templo-bot/src")` and `path.resolve(__dirname, "../../src")`. Implement `function findRawSqlSites(files: string[]): Array&lt;{ file: string; line: number; sql: string }&gt;` matching the stateful line-by-line accumulator sketched in PATTERNS Surface 4 sub-pattern (d) (the inline sketch from `debug/bot-raw-sql-status-column-drift.md:227-290`): detect `sql\`` opens via `line.search(/\bsql`/)`, capture contents across lines until the closing backtick, handle the single-line case where open and close are on the same line. Implement `function scanSqlForDrift(site, renameMap): Array&lt;{ file, line, alias, column, expectedSqlColumn, table? }&gt;` that: (a) strips `${...}` Drizzle interpolations per D-02 (iii) via `site.sql.replace(/\$\{[^}]*\}/g, "PLACEHOLDER")`; (b) parses `FROM &lt;table&gt; [AS] &lt;alias&gt;` and `JOIN &lt;table&gt; [AS] &lt;alias&gt;` via regex per D-02 (i) — keep tables-in-scope, mapping alias -> table (default alias = table name when omitted); (c) scans for `&lt;alias&gt;.&lt;col&gt;` patterns and flags if the alias resolves to a known table AND `renameMap.get(table)?.get(col)` returns a different SQL column name; (d) MANDATORY D-02 (i) bare-column detection: in single-table blocks (exactly one table in scope, no JOIN), additionally tokenize bare identifiers (whitespace-delimited, comma-delimited, parenthesized) and cross-check unqualified columns against the FROM-table's rename map — flag if a bare token equals a JS-property key in the rename map. This catches a hypothetical future `SELECT status FROM subscriptions` even without alias.

    (3) D-02 (ii) MANDATORY synthetic-drift positive-control fixture (PERMANENT). Add `describe("raw SQL column drift — sweep-lint", () => ...)` containing `it("scanner detects a synthetic drift fixture (self-test — PERMANENT, survives real-drift GREEN)", ...)`. Construct a known-bad fixture string for the alias-qualified case: `const syntheticDrift = "SELECT sub.status FROM subscriptions sub WHERE sub.user_id = 1"`. Call `scanSqlForDrift({ file: "&lt;synthetic&gt;", line: 1, sql: syntheticDrift }, buildRenameMap())` and assert the result has length >= 1, with `result[0].column === "status"` and `result[0].expectedSqlColumn === "subscription_status"`. Add a JSDoc comment block above this `it()` explicitly stating: PERMANENT — this fixture must NOT be deleted after real drift sites are GREEN; it is the self-test that catches a scanner-becomes-no-op regression (per PATTERNS drift item 5). Add a second `it()` for the bare-column case: feed `"SELECT status FROM subscriptions"` (single-table, no alias) and assert at least one flag — proves D-02 (i) bare-column detection works.

    (4) D-05 RED-FIRST real-drift detection + D-03 must-include subset coverage. Add `it("scanner detects the 3 real drift sites on master (RED on master, GREEN post-fix)", ...)`. Walk `el-templo-bot/src/**/*.ts` AND `el-templo-api/src/**/*.ts` via the two helpers above. Pass each captured `sql` site through `scanSqlForDrift`. Assert `expect(drift).toEqual([])`. On master HEAD `3f330787` this MUST fail (3 entries: `tools.ts:495`, `tools.ts:500`, `machine.ts:77`); post Task 3 GREEN this turns green. Also add `it("Drizzle-discovered rename map ⊇ the high-risk must-include subset (D-03)", ...)`. Define `const HIGH_RISK_RENAMES_MUST_INCLUDE: Record&lt;string, Record&lt;string, string&gt;&gt;` with EXACTLY the 10 entries from D-03 (subscriptions.status->subscription_status, bookings.status->booking_status, attendance.status->attendance_status, attendance.source->attendance_source, whatsapp_conversations.status->conversation_status, whatsapp_messages.direction->message_direction, whatsapp_messages.messageType->wa_message_type, aura_config.sourceType->aura_config_source_type, exercises.level->exercise_level, format_compatibility.level->compat_level). Iterate; for each table, assert `discovered.get(table)` is defined; for each `(jsProp, sqlCol)` entry, assert `discovered.get(table)!.get(jsProp) === sqlCol`. Failure messages must name the missing table/column so a schema drift produces a LOUD locate-able failure (per D-03 guard-the-guard rationale). This is SUBSET (`discovered ⊇ must-include`), NOT equality — `discovered` may legitimately contain more renames (e.g., medium camelCase->snake) without breaking the test.

    Use `unknown` everywhere for narrowing. No `any` types (CLAUDE.md). No `console.log` (use the test runner's assertion output). Keep helpers inline in the file (no extraction into `src/`); the test is self-contained per PATTERNS Surface 4 framing. Add a top-of-file JSDoc explaining: this is the PERMANENT sweep-lint for raw-SQL <-> Drizzle column-name drift; scan scope is `el-templo-bot/src/**/*.ts` + `el-templo-api/src/**/*.ts`; the synthetic-drift fixture is permanent; new schema renames are auto-discovered via Drizzle introspection (no hand-maintenance).

  </action>
  <verify>
    <automated>cd el-templo-api && pnpm vitest run test/lint/raw-sql-column-drift.test.ts 2>&amp;1 | tee /tmp/97.5-task2-red.log; grep -E "tools\.ts|machine\.ts|sub\.status|s\.status" /tmp/97.5-task2-red.log</automated>
  </verify>
  <acceptance_criteria>
    - File `el-templo-api/test/lint/raw-sql-column-drift.test.ts` exists; directory `el-templo-api/test/lint/` exists.
    - `cd el-templo-api && pnpm vitest run test/lint/raw-sql-column-drift.test.ts` exits NON-ZERO on master HEAD `3f330787`.
    - Failure output mentions BOTH `tools.ts` AND `machine.ts` (the real drift sites — confirms RED-FIRST per D-05).
    - The synthetic-drift positive-control `it()` PASSES even on master (independent of real-source scan).
    - The D-03 must-include subset `it()` PASSES even on master (asserts schema introspection only, independent of bot raw-SQL state).
    - `cd el-templo-api && pnpm tsc --noEmit` exits 0.
    - `grep -c "PERMANENT" el-templo-api/test/lint/raw-sql-column-drift.test.ts` returns at least 1 (the synthetic-drift fixture is explicitly marked PERMANENT per D-02 ii + PATTERNS drift item 5).
    - `grep -cE "HIGH_RISK_RENAMES_MUST_INCLUDE" el-templo-api/test/lint/raw-sql-column-drift.test.ts` returns at least 1.
    - `grep -v '^[[:space:]]*//' el-templo-api/test/lint/raw-sql-column-drift.test.ts | grep -cE 'subscriptions|bookings|attendance|whatsapp_conversations|whatsapp_messages|aura_config|exercises|format_compatibility'` returns at least 8 (must-include subset covers all 10 high-risk entries across 8 tables).
    - `grep -c "getTableColumns" el-templo-api/test/lint/raw-sql-column-drift.test.ts` returns at least 1 (Drizzle introspection per D-03).
    - No new deps added: `git diff el-templo-api/package.json` shows no changes; `grep -cE '"glob"|"ts-morph"' el-templo-api/package.json` returns 0.
    - No `any` type: `grep -vE '^\s*(#|//|\*)' el-templo-api/test/lint/raw-sql-column-drift.test.ts | grep -cE ':\s*any\b|<any>'` returns 0.
  </acceptance_criteria>
  <done>
    The sweep-lint file exists, FAILS on master HEAD with the 3 real drift sites detected (D-05 RED-FIRST satisfied), passes its synthetic-drift positive-control (D-02 ii — PERMANENT), passes its D-03 must-include subset coverage, uses live Drizzle introspection via `getTableColumns` (no hardcoded rename map), adds zero new deps, and type-checks. Ready for commit (1) of the D-05 chain (paired with Task 1's RED file in the SAME commit per D-05 canonical chain, or as a separate RED-B commit if the executor opts for the 4-commit variant — in either case BOTH RED files land before any GREEN edit).
  </done>
</task>

<task type="auto">
  <name>Task 3 (GREEN): Apply Option B rename in tools.ts and machine.ts — both inline SubscriptionRow interfaces, 3 SQL drift sites, 3 JS-side reads</name>
  <files>el-templo-bot/src/ai/tools.ts, el-templo-bot/src/state/machine.ts</files>
  <read_first>
    - .planning/phases/97.5-raw-sql-column-drift-prod-fix/97.5-CONTEXT.md (D-04 fix shape, canonical_refs drift sites, deferred Option A/C rejection)
    - .planning/phases/97.5-raw-sql-column-drift-prod-fix/97.5-PATTERNS.md (Surfaces 1 + 2 with verbatim before/after; drift items 2 and 6)
    - el-templo-bot/src/ai/tools.ts (read :440-555 to see current SubscriptionRow + checkMembership query + JS-side reads; confirm `:504` is a bind and `:534`/`:537` reference unaffected fields `classes_per_week` and `multi_branch`)
    - el-templo-bot/src/state/machine.ts (read :30-135 to see current SubscriptionRow + determineClientState query + JS-side reads; confirm `:83` is a bind, `:90` and `:116` are the substantive accesses, `:130-134` is the swallow-errors catch)
    - el-templo-bot/src/schedulers/trial-followup.ts (line 106 — pre-existing CORRECT `s.subscription_status` usage; confirms the post-fix shape is already canonical elsewhere — read as orientation, do NOT modify)
  </read_first>
  <action>
    Apply the Option B rename per D-04 + PATTERNS Surface 1 + Surface 2. EXACTLY these 8 edits, no more, no less.

    File `el-templo-bot/src/ai/tools.ts`:
    1. At line 452-460, inside the `interface SubscriptionRow { ... }` declaration (8 fields total; confirm exact field count during Read), rename the field `status: string;` to `subscription_status: string;`. Do NOT touch any other field. Do NOT add comments or reformat unrelated lines.
    2. At line 495 (SELECT list), replace `sub.status` with `sub.subscription_status`. Adjacent SELECT-list tokens (`sp.name AS plan_name`, `sub.start_date`, `sub.end_date`, `sub.price_paid`, `sp.classes_per_week`, `sp.multi_branch`) MUST be preserved verbatim — only the `sub.status` token changes. Do NOT add an `AS status` alias (Option A is explicitly REJECTED per CONTEXT.md `<deferred>` and D-04 rationale).
    3. At line 500 (WHERE clause), replace `sub.status IN ('active', 'paused')` with `sub.subscription_status IN ('active', 'paused')`. The `IN ('active', 'paused')` literal MUST be preserved verbatim.
    4. At line 538 (JS-side, inside the `statusText` ternary), replace `sub.status === "active"` with `sub.subscription_status === "active"`. The ternary branches `"Activa" : "Pausada"` MUST be preserved verbatim. DO NOT touch line 504 (`const sub = subs[0]` — variable binding, no `.status` access — per PATTERNS drift item 2). DO NOT touch lines 534 or 537 (these read `sub.classes_per_week` and `sub.multi_branch` — unaffected fields).

    File `el-templo-bot/src/state/machine.ts`:
    5. At line 39-43, inside the `interface SubscriptionRow { ... }` declaration (3 fields: `status`, `end_date`, `is_trial`), rename `status: string;` to `subscription_status: string;`. Do NOT touch the `is_trial` comment "MySQL boolean from joined plan" if present.
    6. At line 77 (SELECT list of the determineClientState query), replace `s.status, s.end_date, sp.is_trial` with `s.subscription_status, s.end_date, sp.is_trial`. Adjacent SQL (FROM, JOIN, WHERE, ORDER BY) MUST be preserved verbatim.
    7. At line 90 (JS-side, inside `determineClientState` active-subscription lookup), replace `subRows.find((s) => s.status === "active")` with `subRows.find((s) => s.subscription_status === "active")`. The arrow-function shape MUST be preserved verbatim.
    8. At line 116 (JS-side, paused-subscription lookup), replace `subRows.find((s) => s.status === "paused")` with `subRows.find((s) => s.subscription_status === "paused")`. The arrow-function shape MUST be preserved verbatim. DO NOT touch line 83 (`const subRows = subResult[0]` — variable binding, no `.status` access — per PATTERNS drift item 2). DO NOT touch the swallow-errors catch at :130-134.

    DO NOT extract a shared `SubscriptionRow` type (D-04 explicitly rejects this; the two shapes are intentionally different). DO NOT modify any other file. After edits: both `pnpm tsc --noEmit` passes (both inline interface renames + 3 JS reads stay type-consistent), the RED integration test from Task 1 turns GREEN, and the sweep-lint from Task 2 turns GREEN (zero real drift detected; synthetic-drift fixture still passes because it operates on an in-test string, not on the source files).

  </action>
  <verify>
    <automated>cd el-templo-bot && pnpm tsc --noEmit 2>&amp;1 | tee /tmp/97.5-task3-bot-tsc.log; cd ../el-templo-api && pnpm tsc --noEmit 2>&amp;1 | tee /tmp/97.5-task3-api-tsc.log && pnpm vitest run test/whatsapp/ai-tools-membership-drift.test.ts test/lint/raw-sql-column-drift.test.ts 2>&amp;1 | tee /tmp/97.5-task3-green.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '\bsub\.status\b|\bs\.status\b' el-templo-bot/src/ai/tools.ts el-templo-bot/src/state/machine.ts` returns NO matches (zero remaining `sub.status` or `s.status` references in raw SQL OR JS reads).
    - `grep -nE 'sub\.subscription_status' el-templo-bot/src/ai/tools.ts` returns at least 3 matches (line 495 SELECT, line 500 WHERE, line 538 JS read).
    - `grep -nE 's\.subscription_status' el-templo-bot/src/state/machine.ts` returns at least 3 matches (line 77 SELECT, line 90 JS read, line 116 JS read).
    - `grep -nE 'subscription_status: string' el-templo-bot/src/ai/tools.ts el-templo-bot/src/state/machine.ts` returns exactly 2 matches (one per inline interface).
    - `grep -cE 'AS status\b' el-templo-bot/src/ai/tools.ts el-templo-bot/src/state/machine.ts` returns 0 (Option A aliasing explicitly NOT used — D-04 rejection).
    - `cd el-templo-bot && pnpm tsc --noEmit` exits 0.
    - `cd el-templo-api && pnpm tsc --noEmit` exits 0.
    - `cd el-templo-api && pnpm vitest run test/whatsapp/ai-tools-membership-drift.test.ts` exits 0 (RED integration test turns GREEN).
    - `cd el-templo-api && pnpm vitest run test/lint/raw-sql-column-drift.test.ts` exits 0 (sweep-lint turns GREEN: zero real-drift entries detected; synthetic-drift positive control still passes; D-03 subset coverage still passes).
    - `cd el-templo-api && pnpm test` exits 0 (full test suite passes; no regression introduced).
    - Git diff against master HEAD `3f330787` touches EXACTLY 4 files (2 prod files modified + 2 new test files from Tasks 1-2): `git diff --name-only master..HEAD` lists `el-templo-bot/src/ai/tools.ts`, `el-templo-bot/src/state/machine.ts`, `el-templo-api/test/whatsapp/ai-tools-membership-drift.test.ts`, `el-templo-api/test/lint/raw-sql-column-drift.test.ts` and NO OTHER FILES (SC#4 enforced).
    - No `any` introduced: `grep -vE '^\s*(#|//|\*)' el-templo-bot/src/ai/tools.ts el-templo-bot/src/state/machine.ts | grep -cE ':\s*any\b|<any>'` matches the same count as on master (no NEW `any` introduced).
  </acceptance_criteria>
  <done>
    Option B rename applied at all 8 sites (2 interfaces + 3 SQL + 3 JS reads); both bot and api packages type-check; both new test files pass; full `pnpm test` in el-templo-api stays green; exactly 4 files touched (SC#4). Ready for commit (2) of the D-05 chain (GREEN). After this commit lands on master, cherry-picking `phase-98-preserve/task-1-green-baseline` + `98-TASK-2-WIP.patch` no longer surfaces `Unknown column 'sub.status'` (SC#5 unblocked).
  </done>
</task>

<task type="auto">
  <name>Task 4 (SUMMARY): Write Phase 97.5 SUMMARY and finalize the D-05 commit chain</name>
  <files>.planning/phases/97.5-raw-sql-column-drift-prod-fix/97.5-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/97.5-raw-sql-column-drift-prod-fix/97.5-CONTEXT.md (D-01..D-05 + canonical_refs — the SUMMARY restates which decisions shipped vs which deferred)
    - .planning/phases/97.5-raw-sql-column-drift-prod-fix/97.5-PATTERNS.md (Shared Patterns S-1..S-5 — the SUMMARY references which were applied)
    - $HOME/.claude/get-shit-done/templates/summary.md (canonical SUMMARY shape)
    - .planning/phases/96.5-*/96.5-01-SUMMARY.md if present (atomic RED -> GREEN -> SUMMARY exemplar — read for shape match)
    - .planning/ROADMAP.md (Phase 97.5 SC list — the SUMMARY ticks each SC against verified evidence)
  </read_first>
  <action>
    Create `.planning/phases/97.5-raw-sql-column-drift-prod-fix/97.5-01-SUMMARY.md` following the standard SUMMARY template. Required sections:

    (1) Frontmatter with `phase: 97.5-raw-sql-column-drift-prod-fix`, `plan: 01`, `status: shipped`, `commits` array listing the RED and GREEN shas (SUMMARY sha pending), `requirements: [DRIFT-01, DRIFT-02]`, `files_modified` listing the 4 surfaces.

    (2) What shipped — bullet list of: (a) RED integration test reproducing `Unknown column 'sub.status'`/`'s.status'` against real `eltemplo_test` MySQL; (b) Option B rename applied at 8 sites; (c) permanent sweep-lint guardrail with live Drizzle introspection + synthetic-drift positive control + D-03 subset coverage covering 10 high-risk renames.

    (3) Decisions honored — explicit cross-reference: D-01 (separate RED file at `test/whatsapp/ai-tools-membership-drift.test.ts` — avoids Phase 98 cherry-pick conflict), D-02 (regex + accumulator scanner, zero new deps; bare-column detection implemented; synthetic-drift fixture marked PERMANENT; Drizzle interpolations skipped), D-03 (live `getTableColumns` introspection; SUBSET coverage on 10 high-risk renames; NOT equality), D-04 (inline rename in both SubscriptionRow declarations + JS-side reads; NO shared type extraction; NO `AS status` aliasing; NO hardcoded unit pin), D-05 (RED-FIRST sweep-lint commit ordering — both RED files landed before any GREEN edit).

    (4) Verification evidence — paste the relevant grep/vitest exit codes that closed each SC item: SC#1 (RED reproduces errno 1054), SC#2 (GREEN at 8 sites + RED turns green), SC#3 (sweep-lint passes; synthetic fixture passes), SC#4 (exactly 4 files touched — paste `git diff --name-only master..HEAD` output), SC#5 (Phase 98 cherry-pick unblocked — note the verification step), SC#6 (subset coverage assertion green for all 10 renames).

    (5) Deferred — bullet list mirroring CONTEXT.md `<deferred>`: Option C Drizzle typed-query-builder migration (v5.4.0+), `formatBranchLocations` accent matching (v5.4.0+), Phase 98 retry `check_schedule` test-side date mismatch (Phase 98 Task 2 expansion), sweep-lint expansion to `el-templo-app/src/**` and `el-templo-admin/src/**` (no raw SQL there — out of scope until raw SQL is introduced).

    (6) Threat model conclusion — RECONFIRM the planning-time conclusion (low/low): SQL column-name rename in already-parameterized `db.execute(sql\`...\`)` calls; no new user input surfaces; no auth changes; no new endpoints. Only meaningful threat tracked: the seeded RED fixture cleans up after itself in `beforeEach` (DELETE WHERE code LIKE 'TST%' etc.) — confirmed by re-reading the file post-Task-1.

    (7) Unblocks — Phase 98 retry (cherry-pick `phase-98-preserve/task-1-green-baseline` + `98-TASK-2-WIP.patch` no longer surfaces `Unknown column 'sub.status'`); Phase 97 RGUARD-01 (green-on-real-MySQL baseline for v5.3.3 regression suite).

    (8) Patterns established — first codebase precedent for `sql\`...\`` template-literal block extraction (PATTERNS "No Analog Found" sub-pattern d); reusable for future column-name lint expansions. Reuses S-1 (inline raw-SQL row-type), S-2 (Option B explicit rename), S-3 (pool+drizzle integration-test boot), S-4 (TST-prefixed test namespace via `TSTMBR`), S-5 (vitest auto-discovery).

    Do NOT write code in the SUMMARY. Do NOT introduce new prose decisions — only restate what shipped vs CONTEXT.md/PATTERNS.md/ROADMAP.md. After writing, this becomes commit (3) of the D-05 chain.

  </action>
  <verify>
    <automated>ls .planning/phases/97.5-raw-sql-column-drift-prod-fix/97.5-01-SUMMARY.md &amp;&amp; grep -cE 'DRIFT-01|DRIFT-02|D-01|D-02|D-03|D-04|D-05' .planning/phases/97.5-raw-sql-column-drift-prod-fix/97.5-01-SUMMARY.md &amp;&amp; grep -cE 'SC#1|SC#2|SC#3|SC#4|SC#5|SC#6' .planning/phases/97.5-raw-sql-column-drift-prod-fix/97.5-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/97.5-raw-sql-column-drift-prod-fix/97.5-01-SUMMARY.md` exists.
    - SUMMARY references all 5 decisions (D-01..D-05) — grep returns at least 5 matches across D-NN tokens.
    - SUMMARY ticks all 6 success criteria (SC#1..SC#6) — grep returns at least 6 matches.
    - SUMMARY frontmatter `status: shipped` and `requirements: [DRIFT-01, DRIFT-02]` both present (`grep -cE 'status:\s+shipped' .planning/phases/97.5-raw-sql-column-drift-prod-fix/97.5-01-SUMMARY.md` returns 1; `grep -cE 'DRIFT-01.*DRIFT-02|DRIFT-02.*DRIFT-01' .planning/phases/97.5-raw-sql-column-drift-prod-fix/97.5-01-SUMMARY.md` returns at least 1).
    - SUMMARY lists exactly the 4 files_modified surfaces (no fifth surface).
    - No code blocks in the SUMMARY (no triple-backtick fenced blocks): `grep -cE '^```' .planning/phases/97.5-raw-sql-column-drift-prod-fix/97.5-01-SUMMARY.md` returns 0.
  </acceptance_criteria>
  <done>
    SUMMARY exists at the expected path; restates D-01..D-05 + SC#1..SC#6 + deferred items + threat-model conclusion + unblocks; references no new decisions; ready for commit (3) of the D-05 chain. Phase 97.5 closed.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                                                                                                                                                                                                                                                                           |
| -------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| test seed -> eltemplo_test MySQL | Seeded RED fixture writes rows in `beforeEach` (branches/users/subscription_plans/subscriptions). Must clean up on each run via `WHERE code LIKE 'TST%'` etc. so a partial-run failure does not leave orphaned rows. Cleanup runs FIRST in `beforeEach` (reverse-FK-order DELETEs) per PATTERNS Surface 3 seed shape. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                      | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                        |
| --------- | ---------------------- | ------------------------------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-97.5-01 | Tampering              | Raw SQL column references in `tools.ts:495,500,538` and `machine.ts:77,90,116` | mitigate    | D-04 Option B rename ensures the JS-side type and the raw SQL string use the same identifier `subscription_status`; Task 2 sweep-lint guards future regressions across all `sql\`...\``blocks in bot+api source. Already-parameterized`db.execute(sql\`...${userId}...\`)` calls remain parameterized; rename does not introduce string concatenation. |
| T-97.5-02 | Information Disclosure | Seeded RED fixture rows in `eltemplo_test`                                     | accept      | Test database is isolated (`eltemplo_test`, not `eltemplo` prod); seed values are synthetic (`DriftJuan`, `test-mbr-drift-1@test.com`, `TSTMBR`). No PII. `beforeEach` cleanup uses `WHERE code LIKE 'TST%'` to prevent test-data accumulation.                                                                                                        |
| T-97.5-SC | Tampering              | npm/pip/cargo installs                                                         | mitigate    | Zero new packages installed in this phase. Plan-time verification: `grep -cE '"glob"\|"ts-morph"' el-templo-api/package.json` returns 0 (D-02 "zero new deps"). No legitimacy audit needed because no new packages.                                                                                                                                    |

**Realistic conclusion (per planning-time guidance):** NONE OF INTEREST (low / low). SQL column-name rename in already-parameterized `db.execute(sql\`...\`)`calls; no new user input surfaces; no auth changes; no new endpoints; no new packages. Only meaningful tracked threat is fixture cleanup (T-97.5-02), already mitigated by the reverse-FK-order`beforeEach` DELETEs.
</threat_model>

<verification>
Phase-wide automated checks after Task 4 (run by execute-phase verifier):

1. `cd el-templo-bot && pnpm tsc --noEmit` exits 0.
2. `cd el-templo-api && pnpm tsc --noEmit` exits 0.
3. `cd el-templo-api && pnpm test` exits 0 (full test suite passes; both new files and entire pre-existing suite green).
4. `git diff --name-only master..HEAD` lists EXACTLY: `el-templo-bot/src/ai/tools.ts`, `el-templo-bot/src/state/machine.ts`, `el-templo-api/test/whatsapp/ai-tools-membership-drift.test.ts`, `el-templo-api/test/lint/raw-sql-column-drift.test.ts`, `.planning/phases/97.5-raw-sql-column-drift-prod-fix/97.5-01-SUMMARY.md` (and the 97.5-01-PLAN.md / 97.5-CONTEXT.md / 97.5-PATTERNS.md authoring commits from earlier). NO OTHER prod files modified (SC#4).
5. `grep -nE '\bsub\.status\b|\bs\.status\b' el-templo-bot/src/ai/tools.ts el-templo-bot/src/state/machine.ts` returns no matches.
6. Git log shows the D-05 commit chain (`type: tdd` execution would write 3 commits: RED, GREEN, SUMMARY; the 4-commit RED-A / RED-B / GREEN / SUMMARY variant is also acceptable per D-05). Both RED commits land BEFORE the GREEN commit — verifiable by `git log --oneline master..HEAD` ordering.
   </verification>

<success_criteria>
Phase 97.5 ROADMAP success criteria (must all be ticked in the SUMMARY):

- SC#1: RED integration test exists at `el-templo-api/test/whatsapp/ai-tools-membership-drift.test.ts` and reproduces `Unknown column 'sub.status'` / `'s.status'` (errno 1054 / sqlState 42S22) on master HEAD `3f330787`.
- SC#2: GREEN applied at all 3 SQL drift sites via Option B (rename `status` -> `subscription_status` in both `SubscriptionRow` interfaces, in 3 raw-SQL references, and in 3 JS-side reads); RED integration tests turn GREEN.
- SC#3: Sweep-lint at `el-templo-api/test/lint/raw-sql-column-drift.test.ts` runs under `pnpm test` (vitest auto-glob); asserts zero drift across `el-templo-bot/src/**` and `el-templo-api/src/**`; synthetic-drift positive-control fixture passes (and remains PERMANENT post-GREEN).
- SC#4: Zero modifications outside the 4 prod surfaces (`tools.ts`, `machine.ts`, `ai-tools-membership-drift.test.ts`, `raw-sql-column-drift.test.ts`) plus the 2 inline `SubscriptionRow` declarations contained within `tools.ts` and `machine.ts`.
- SC#5: Phase 98 unblocked — cherry-picking `phase-98-preserve/task-1-green-baseline` + `98-TASK-2-WIP.patch` after Phase 97.5 GREEN no longer surfaces `Unknown column 'sub.status'`.
- SC#6: Sweep-lint live-introspection rename map covers all 10 high-risk plain-word->prefixed renames (subset-not-equality assertion green).
  </success_criteria>

<output>
Create `.planning/phases/97.5-raw-sql-column-drift-prod-fix/97.5-01-SUMMARY.md` when Task 4 completes (per execute-plan workflow).
</output>
