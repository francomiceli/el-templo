---
phase: 98-test-hygiene-98-a-b-c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/test/helpers.ts
  - el-templo-api/test/subscriptions/subscriptions.test.ts
  - el-templo-api/test/whatsapp/ai-tools.test.ts
  - el-templo-api/test/whatsapp/webhook.test.ts
autonomous: false
requirements: [HYG-01]

must_haves:
  truths:
    - "SC#1 (D-01..D-10): cd el-templo-api && pnpm test --run exits 511 passed / 1 failed / 512 total. The one failure is BUG-03 (i) LIKE-search RED at el-templo-bot/src/ai/tools.ts:455 (Phase 95-deferred, STAYS RED)."
    - "SC#2 (D-01/D-02/D-03/D-04): subscriptions.test.ts replaces 6 stale startDate literals with futureDateISO(...); the 3 leave-alone sites (:537/:721/:733) and the :606 lower-bound assertion remain UNCHANGED. The :366 test rewrites :374 and :375 to compare against the dynamic start/computed endDate (non-tautological)."
    - "SC#3 (D-05/D-06): ai-tools.test.ts INSERT at :60 renames code='alem' to code='TSTA'. The cleanup filter LIKE 'TST%' (line 55) is UNCHANGED. Wording assertions at :112, :174, :225 update to 'cupos disponibles' with each site preserving its own spot count (20/2/20). Display name 'Test Alem' UNCHANGED."
    - "SC#4 (D-07/D-08/D-09/D-10): webhook.test.ts adds vi.mock for el-templo-bot/src/ai/provider stubbing createAiProvider().chat to return a canned Spanish greeting with toolCalls:[]. Both echo literals at :292 and :298 replace with the canned reply. The image test at :388 rewrites to assert the post-quick-16-fix-3 store+reply behavior using waitForHandler() instead of setTimeout(100)."
    - "SC#5 HARD GUARD: git diff across all phase commits shows ZERO changes to el-templo-api/src/** and el-templo-bot/src/**. Negative-assertion gate."
    - "SC#6: cd el-templo-api && pnpm tsc --noEmit AND cd el-templo-bot && pnpm tsc --noEmit both exit 0 post-fix."
    - "6-pair sha256 canonical-invariant hash 67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344 UNCHANGED post-phase (Phase 98 modifies zero terms in the DEBOUNCE_TTL_SECONDS block)."
    - "D-11 commit cadence: 3 atomic GREEN commits (98-A → 98-B → 98-C, smallest → largest) + 1 SUMMARY commit. No combined RED/GREEN; each fix-zone GREEN is independently revertible. STOP-and-reclassify guard honored: if any failure unexpectedly reveals a production bug, STOP."
  artifacts:
    - path: "el-templo-api/test/helpers.ts"
      provides: "futureDateISO(daysFromToday: number): string named export"
      contains: "export function futureDateISO"
    - path: "el-templo-api/test/subscriptions/subscriptions.test.ts"
      provides: "6 startDate literals replaced with futureDateISO; :374/:375 dynamic-ized per D-03"
    - path: "el-templo-api/test/whatsapp/ai-tools.test.ts"
      provides: "Seed rename alem→TSTA at :60; wording updates at :112/:174/:225"
    - path: "el-templo-api/test/whatsapp/webhook.test.ts"
      provides: "vi.mock createAiProvider; echo asserts updated; image test rewritten for store+reply"
  key_links:
    - from: "el-templo-api/test/subscriptions/subscriptions.test.ts"
      to: "el-templo-api/test/helpers.ts"
      via: "import futureDateISO"
      pattern: "import \\{[^}]*futureDateISO[^}]*\\} from \"\\.\\./helpers\""
    - from: "el-templo-api/test/subscriptions/subscriptions.test.ts"
      to: "el-templo-api/src/modules/shared/date-utils.ts"
      via: "import addDays (READ-ONLY, helpers reuse — D-02)"
      pattern: "import \\{[^}]*addDays[^}]*\\}"
    - from: "el-templo-api/test/whatsapp/webhook.test.ts"
      to: "el-templo-bot/src/ai/provider.ts"
      via: "vi.mock interception at createAiProvider factory"
      pattern: "vi\\.mock\\(\"\\.\\./\\.\\./\\.\\./el-templo-bot/src/ai/provider\""
    - from: "el-templo-api/test/whatsapp/webhook.test.ts"
      to: "waitForHandler()"
      via: "image test awaits handler async work (replaces setTimeout(100))"
      pattern: "await waitForHandler\\(\\)"
---

<objective>
Restore green baseline on the `el-templo-api` test suite. Close 29 of 30 currently-failing tests across three independent fix-zones (98-A subscriptions, 98-B ai-tools, 98-C webhook) by editing TEST FILES ONLY. The 30th failure (BUG-03 (i) LIKE-search RED at `el-templo-bot/src/ai/tools.ts:455`) is Phase 95-owned and STAYS RED.

Purpose: Phase 97 RGUARD-01 ("milestone-scoped regression suite") cannot lock a regression baseline on top of a 30-red API suite — new regressions during Phase 97 + Manual UAT Round 2 would be indistinguishable from existing noise. Phase 98 unblocks the v5.4.0 path step 3.

Output: 4 files modified (1 helper export + 3 test files). Three atomic GREEN sub-commits + 1 SUMMARY commit. Zero production source modifications (SC#5 HARD GUARD).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-test-hygiene-98-a-b-c/98-CONTEXT.md
@.planning/phases/98-test-hygiene-98-a-b-c/98-PATTERNS.md
@CLAUDE.md

<!-- Test files being modified (read in full before touching) -->

@el-templo-api/test/helpers.ts
@el-templo-api/test/subscriptions/subscriptions.test.ts
@el-templo-api/test/whatsapp/ai-tools.test.ts
@el-templo-api/test/whatsapp/webhook.test.ts

<!-- READ-ONLY anchors (production source — SC#5 HARD GUARD: never modified) -->

@el-templo-api/src/modules/shared/date-utils.ts
@el-templo-bot/src/ai/provider.ts

<interfaces>
<!-- Mock target shape — verified against el-templo-bot/src/ai/provider.ts -->

`AiResponse` interface (provider.ts:33-36):

- `content` is string or null
- `toolCalls` is array of ToolCall

`AiProvider` interface (provider.ts:38-45):

- `chat(messages, tools)` returns Promise of AiResponse

`createAiProvider` factory (provider.ts:57-69):

- Exported function — returns object implementing `AiProvider`. The 98-C `vi.mock` intercepts at this factory.

Handler call sites (READ-ONLY — verified, NOT modified):

- `el-templo-bot/src/webhook/handler.ts:700` — `createAiProvider()` invocation
- `el-templo-bot/src/webhook/handler.ts:708` — first `provider.chat(messages, BOT_TOOLS)`
- `el-templo-bot/src/webhook/handler.ts:846` — second `provider.chat` (skipped when `toolCalls: []`)

Date helper (production source — IMPORTED by test, never modified):

- `el-templo-api/src/modules/shared/date-utils.ts` exports `addDays(dateStr: string, days: number): string` — noon-UTC pure helper.

Existing helpers.ts named exports (the style template for `futureDateISO`):

- `createTestApp()` returns Promise of FastifyInstance
- `getAuthToken(app, email, password)` returns Promise of string
- `registerUser(app, email, password, ...)` returns Promise of an object

waitForHandler primitives in `webhook.test.ts` (existing, lines 161-198):

- `function waitForHandler()` returns Promise of void (returns `handlerPromise`)
- `function resetHandlerPromise()` returns void (called in `beforeEach`)
- `onMessageHandled: () => handlerResolve()` callback wired at `:196-198`
  </interfaces>

<canned_reply_constant>

<!-- D-07 plan-phase lock (per "Claude's Discretion" in CONTEXT.md). Used in both 98-C mock AND 98-C echo assertions at :292/:298. -->

Canned mock chat reply: `"Hola, soy Mica."` (15 chars, plain ASCII greeting, valid Spanish, doesn't collide with any other assertion in the file).
</canned_reply_constant>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (98-A GREEN): Add futureDateISO helper and rewrite 6 stale startDate sites in subscriptions.test.ts</name>
  <files>el-templo-api/test/helpers.ts, el-templo-api/test/subscriptions/subscriptions.test.ts</files>
  <read_first>
    - el-templo-api/test/helpers.ts (full file — 80 lines; see existing `createTestApp`, `getAuthToken`, `registerUser` named-export style)
    - el-templo-api/test/subscriptions/subscriptions.test.ts (full file — line numbers per CONTEXT.md D-01/D-04 + PATTERNS.md drift report)
    - el-templo-api/src/modules/shared/date-utils.ts (READ-ONLY — `addDays(dateStr, days): string` noon-UTC helper signature, per D-02)
    - .planning/phases/98-test-hygiene-98-a-b-c/98-CONTEXT.md sections D-01, D-02, D-03, D-04 (rule-driven classification — NOT find-replace; respect `:537`/`:721`/`:733` LEAVE-ALONE and `:606` lower-bound LEAVE-ALONE)
    - .planning/phases/98-test-hygiene-98-a-b-c/98-PATTERNS.md "Surface 1" + "Surface 2" (style template for futureDateISO; coupled-assertion rewrite pattern for `:374/:375`)
  </read_first>
  <action>
    Step 1 — Add `futureDateISO` to `el-templo-api/test/helpers.ts` per D-01. Implement as a named export `export function futureDateISO(daysFromToday: number): string` returning `"YYYY-MM-DD"`. Use noon-UTC internally to avoid DST/day-boundary drift (mirror the noon-UTC pattern from `src/modules/shared/date-utils.ts:15-19` — DO NOT import from src in helpers.ts; inline the same shape). Include a JSDoc comment matching the style of the existing exports (one sentence return description, one sentence context). Place the export at the end of the file after `registerUser`.

    Step 2 — Update `el-templo-api/test/subscriptions/subscriptions.test.ts` imports per D-02 + PATTERNS.md "Surface 2". Fold `futureDateISO` into the existing helpers import line: change `import { createTestApp, getAuthToken, registerUser } from "../helpers";` to `import { createTestApp, getAuthToken, registerUser, futureDateISO } from "../helpers";`. Add an `addDays` import: `import { addDays } from "../../src/modules/shared/date-utils";` (positioned with the other imports, alphabetical or grouped per the file's existing order).

    Step 3 — Replace the 6 STALE `startDate` literals using `futureDateISO(...)`. Apply the D-01 rule per site (NOT a literal find-replace of `"2026-03-01"` — `:423` is `"2026-04-01"` and would be missed). The exact 6 sites and the per-site rewrite shape:

    - Site `:132` (`startDate: "2026-03-01"`): replace with `startDate: futureDateISO(7)` (sub must be active throughout the test run — pick 7 days as a safe minimum since `plan.durationDays` is 30 by default, so endDate = today+37, comfortably future).
    - Site `:366` (`startDate: "2026-03-01"`): introduce `const start = futureDateISO(7);` immediately before the `assignPlan` call. Use `startDate: start` in the payload. Then rewrite the two coupled assertions per D-03:
      - `:374` `expect(body.startDate).toBe("2026-03-01")` → `expect(body.startDate).toBe(start)` (echo-of-input — D-03 explicitly OK).
      - `:375` `expect(body.endDate).toBe("2026-03-31")` → `expect(new Date(body.endDate).getTime()).toBeGreaterThan(Date.now())` (D-03 non-tautological inequality). Preserve the `// 30 days from Mar 1` comment removed and replace with `// endDate is in the future`.
    - Site `:388` (`startDate: "2026-03-01"`): replace with `startDate: futureDateISO(7)`.
    - Site `:406` (`startDate: "2026-03-01"`): replace with `startDate: futureDateISO(7)`.
    - Site `:423` (`startDate: "2026-04-01"` — NOT 2026-03-01): replace with `startDate: futureDateISO(7)`. This is the site a naive find-replace would miss.
    - Site `:584` (`startDate: "2026-03-01"`): replace with `startDate: futureDateISO(7)`.

    Step 4 — VERIFY LEAVE-ALONE sites are UNCHANGED (per D-04):
    - Line `:537` `startDate: "2025-01-01"` — intentional past (auto-expire test). DO NOT TOUCH.
    - Line `:721` `startDate: "2026-06-01"` — future, coupled to history-ordering assertions at `:749-758` which echo the exact string. DO NOT TOUCH.
    - Line `:733` `startDate: "2026-07-01"` — future, coupled to history-ordering assertions at `:749-758`. DO NOT TOUCH.
    - Line `:606` `expect(body.endDate >= "2026-03-31").toBe(true)` — resilient lower-bound (future endDate still satisfies). DO NOT TOUCH per D-01 LEAVE-ALONE.

    Step 5 — Type-check then run only the subscriptions file:
    - `cd el-templo-api && pnpm tsc --noEmit` must exit 0.
    - `cd el-templo-api && pnpm test --run -- test/subscriptions/subscriptions.test.ts` must show all-pass with no failures.

    Step 6 — Commit atomically: `git add el-templo-api/test/helpers.ts el-templo-api/test/subscriptions/subscriptions.test.ts && git commit -m "test(98-A): rewrite 6 stale startDate sites + add futureDateISO helper"`. Per D-11 atomic GREEN cadence.

    Naming/style constraints (per CLAUDE.md):
    - No `console.log` anywhere in new code.
    - No `any` types — use `unknown` + narrowing if needed. (`futureDateISO` is `: string`, no narrowing required.)
    - JSDoc on the new export.
    - Pre-commit hook (Husky + lint-staged + Prettier) will auto-format — accept its output; do NOT fight it.

    Do NOT modify any other file. Do NOT add new exports beyond `futureDateISO`. Do NOT touch `:537`/`:606`/`:721`/`:733`.

  </action>
  <behavior>
    - `futureDateISO(daysFromToday)` returns an ISO-format `"YYYY-MM-DD"` string representing today + N days (noon UTC).
    - `futureDateISO(0)` returns today's ISO date.
    - `futureDateISO(7)` returns 7 days from today.
    - The 6 STALE sub-tests (at lines :132, :366, :388, :406, :423, :584 pre-edit) now create subscriptions whose `endDate` (computed by `autoExpireSubscriptions` boundary as `startDate + plan.durationDays`) lies in the future, so `autoExpireSubscriptions` does NOT flip them to `expired` before the test's downstream assertions run.
    - The `:366` test's assertions verify lifecycle contract (active sub, echo of literal startDate input, endDate is in the future) WITHOUT re-computing endDate through `addDays` on both sides of an equality (non-tautological per D-03).
    - The 3 LEAVE-ALONE sites continue to pass unchanged.
  </behavior>
  <verify>
    <automated>cd el-templo-api &amp;&amp; pnpm tsc --noEmit &amp;&amp; pnpm test --run -- test/subscriptions/subscriptions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `el-templo-api/test/helpers.ts` contains `export function futureDateISO(daysFromToday: number): string` (substantive verify gate (f) — code-discipline grep on new export).
    - `grep -nE "export function futureDateISO" el-templo-api/test/helpers.ts` returns exactly one line.
    - `grep -c "startDate: \"2026-03-01\"" el-templo-api/test/subscriptions/subscriptions.test.ts` returns `0` (all 5 occurrences replaced; verify by negative count).
    - `grep -c "startDate: \"2026-04-01\"" el-templo-api/test/subscriptions/subscriptions.test.ts` returns `0` (the :423 site replaced).
    - `grep -c "startDate: \"2025-01-01\"" el-templo-api/test/subscriptions/subscriptions.test.ts` returns `1` (LEAVE-ALONE site :537 preserved).
    - `grep -c "startDate: \"2026-06-01\"" el-templo-api/test/subscriptions/subscriptions.test.ts` returns `1` (LEAVE-ALONE site :721 preserved).
    - `grep -c "startDate: \"2026-07-01\"" el-templo-api/test/subscriptions/subscriptions.test.ts` returns `1` (LEAVE-ALONE site :733 preserved).
    - `grep -c "body.endDate >= \"2026-03-31\"" el-templo-api/test/subscriptions/subscriptions.test.ts` returns `1` (LEAVE-ALONE :606 lower-bound preserved).
    - `grep -cE "console\\.(log|warn|error)" el-templo-api/test/helpers.ts` returns `0` (no new console usage).
    - `grep -cE ": any\\b" el-templo-api/test/helpers.ts` returns `0` (no `any` types in new code).
    - `cd el-templo-api && pnpm tsc --noEmit` exits with code 0 (substantive verify gate (b)).
    - `cd el-templo-api && pnpm test --run -- test/subscriptions/subscriptions.test.ts` shows all-pass (the 6 previously-failing tests in this file now pass; no new failures introduced).
    - `git diff HEAD~1 -- 'el-templo-api/src/**' 'el-templo-bot/src/**' | wc -l` returns `0` (SC#5 negative-assertion gate (e)).
    - `git show --stat HEAD | grep -cE 'el-templo-api/test/(helpers\\.ts|subscriptions/subscriptions\\.test\\.ts)$'` returns `2` (exact-file-count assertion (c) — exactly 2 test files modified in this commit).
    - `git log -1 --format=%s | grep -qE '^test\\(98-A\\): '` (commit subject regex (d)).
  </acceptance_criteria>
  <done>
    - `futureDateISO` exported from helpers.ts with JSDoc + explicit string return type.
    - 6 STALE startDate sites in subscriptions.test.ts use `futureDateISO(...)` instead of past literals.
    - 3 LEAVE-ALONE sites unchanged.
    - `:366` test's `:374` echoes the dynamic `start` variable; `:375` asserts non-tautological future endDate per D-03.
    - tsc clean. Subscriptions file pnpm test all-pass.
    - Atomic GREEN commit landed with subject `test(98-A): ...`.
  </done>
</task>

<task type="auto">
  <name>Task 2 (98-B GREEN): Rename seed code alem→TSTA and update 3 wording assertions in ai-tools.test.ts</name>
  <files>el-templo-api/test/whatsapp/ai-tools.test.ts</files>
  <read_first>
    - el-templo-api/test/whatsapp/ai-tools.test.ts (full file — focus on `:55` cleanup filter, `:58-61` INSERT, `:117-120` second-branch INSERT as TSTB analog, and the 3 wording-assertion sites at `:112`, `:174`, `:225`)
    - .planning/phases/98-test-hygiene-98-a-b-c/98-CONTEXT.md sections D-05, D-06 (and PATTERNS.md "Drift report" — D-06 covers 3 sites, NOT 1 as CONTEXT.md initially stated)
    - .planning/phases/98-test-hygiene-98-a-b-c/98-PATTERNS.md "Surface 3" (rename rationale + TST-prefix convention)
  </read_first>
  <action>
    Step 1 — Per D-05, rename the seed `code` at the INSERT statement near `:60` (currently `code='alem'`) to `code='TSTA'`. The surrounding INSERT statement is:
      `VALUES ('Test Alem', 'alem', 20, true, NOW(), NOW())`
    Change ONLY the `'alem'` literal to `'TSTA'`. The display name `'Test Alem'` STAYS UNCHANGED (the test assertion at `:109` checks `result.toContain("Test Alem")` — display name, not internal code).

    DO NOT touch:
    - Line `:55` cleanup filter `DELETE FROM branches WHERE code LIKE 'TST%'` (will now correctly match `'TSTA'` after this rename — that IS the fix).
    - Line `:119` `'TSTB'` (already correct; the rename brings `:60` into line with this convention).

    Step 2 — Per D-06 (corrected via PATTERNS.md drift report — 3 sites, not 1), update the 3 stale wording assertions. Each site preserves its own NUMBER (do NOT blind-replace `"20 lugares"`):
    - Site `:112` `expect(result).toContain("20 lugares");` → `expect(result).toContain("20 cupos disponibles");`
    - Site `:174` `expect(result).toContain("2 lugares");` → `expect(result).toContain("2 cupos disponibles");` (NUMBER is `2`, not `20`)
    - Site `:225` `expect(result).toContain("20 lugares");` → `expect(result).toContain("20 cupos disponibles");`

    Wording anchor (READ-ONLY — DO NOT MODIFY): `el-templo-bot/src/ai/tools.ts:389` emits `${spotsRemaining} cupos disponibles`. The wording is intentional production state per ROADMAP §Notes.

    Step 3 — Type-check then run only the ai-tools file:
    - `cd el-templo-api && pnpm tsc --noEmit` exits 0.
    - `cd el-templo-api && pnpm test --run -- test/whatsapp/ai-tools.test.ts` shows all-pass (20 previously-failing tests now pass; cleanup-cascade closed by seed rename + wording aligned).

    Step 4 — Commit atomically: `git add el-templo-api/test/whatsapp/ai-tools.test.ts && git commit -m "test(98-B): rename seed alem→TSTA and align wording to 'cupos disponibles' (3 sites)"`.

    Rejected approaches (DO NOT IMPLEMENT):
    - Extending the LIKE filter to `LIKE 'TST%' OR code = 'alem'` — D-05 explicit reject.
    - Seed-registry-by-ID pattern — D-05 explicit reject (over-engineered for one-line bug).
    - Modifying `el-templo-bot/src/ai/tools.ts:389` wording — violates SC#5 + D-06 intentional prod state.

    Style constraints (per CLAUDE.md): no `console.log`, no `any` types added.

  </action>
  <behavior>
    - The INSERT at `:60` seeds a branch with `code='TSTA'`. Cleanup filter `LIKE 'TST%'` now matches the seeded row on subsequent runs → no `Duplicate entry 'alem'` cascade across tests 2-20.
    - The 3 wording assertions match current production output `${N} cupos disponibles`. Number preserved per-site (20/2/20).
    - Display-name assertion at `:109` (`result.toContain("Test Alem")`) continues to pass — display name unchanged.
  </behavior>
  <verify>
    <automated>cd el-templo-api &amp;&amp; pnpm tsc --noEmit &amp;&amp; pnpm test --run -- test/whatsapp/ai-tools.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'alem'" el-templo-api/test/whatsapp/ai-tools.test.ts` returns `0` (the seed literal renamed).
    - `grep -c "'TSTA'" el-templo-api/test/whatsapp/ai-tools.test.ts` returns `1` (the new code).
    - `grep -c "'TSTB'" el-templo-api/test/whatsapp/ai-tools.test.ts` returns `1` (line :119 unchanged).
    - `grep -c "'Test Alem'" el-templo-api/test/whatsapp/ai-tools.test.ts` returns `1` (display name unchanged at :60).
    - `grep -c "LIKE 'TST%'" el-templo-api/test/whatsapp/ai-tools.test.ts` returns `1` (cleanup filter at :55 unchanged).
    - `grep -cE "[0-9]+ lugares" el-templo-api/test/whatsapp/ai-tools.test.ts` returns `0` (all 3 stale wording occurrences replaced).
    - `grep -cE "[0-9]+ cupos disponibles" el-templo-api/test/whatsapp/ai-tools.test.ts` returns `3` (the 3 new assertions).
    - `grep -c "\"20 cupos disponibles\"" el-templo-api/test/whatsapp/ai-tools.test.ts` returns `2` (sites :112 + :225).
    - `grep -c "\"2 cupos disponibles\"" el-templo-api/test/whatsapp/ai-tools.test.ts` returns `1` (site :174 — number preserved).
    - `cd el-templo-api && pnpm tsc --noEmit` exits with code 0.
    - `cd el-templo-api && pnpm test --run -- test/whatsapp/ai-tools.test.ts` shows all-pass.
    - `git diff HEAD~1 -- 'el-templo-api/src/**' 'el-templo-bot/src/**' | wc -l` returns `0` (SC#5).
    - `git show --stat HEAD | grep -cE 'el-templo-api/test/whatsapp/ai-tools\\.test\\.ts$'` returns `1` (exactly 1 file modified in this commit).
    - `git log -1 --format=%s | grep -qE '^test\\(98-B\\): '` (commit subject regex).
  </acceptance_criteria>
  <done>
    - Seed renamed `alem`→`TSTA` at `:60`.
    - 3 wording assertions updated (`:112`, `:174`, `:225`).
    - tsc clean. ai-tools file pnpm test all-pass (20 previously-red tests now green).
    - Atomic GREEN commit landed with subject `test(98-B): ...`.
  </done>
</task>

<task type="auto">
  <name>Task 3 (98-C GREEN): Add vi.mock for AI provider, update echo asserts, rewrite image test for post-quick-16-fix-3 behavior</name>
  <files>el-templo-api/test/whatsapp/webhook.test.ts</files>
  <read_first>
    - el-templo-api/test/whatsapp/webhook.test.ts (FULL file — focus on :27-39 existing `sendTextMessage` vi.mock pattern, :161-198 `waitForHandler`/`resetHandlerPromise`/`onMessageHandled` infra, :286-300 new-sender text test, :388-418 image test, :402 the `setTimeout(100)` to remove)
    - el-templo-bot/src/ai/provider.ts (READ-ONLY — verify `createAiProvider` factory at `:57-69` + `AiProvider` interface `:38-45` + `AiResponse` shape `:33-36`)
    - el-templo-bot/src/webhook/handler.ts lines 173-188 + 323-358 + 700 + 708 + 846 (READ-ONLY — verify mock target + `getNonTextFallback("image")` text containing "imagen")
    - .planning/phases/98-test-hygiene-98-a-b-c/98-CONTEXT.md sections D-07, D-08, D-09, D-10
    - .planning/phases/98-test-hygiene-98-a-b-c/98-PATTERNS.md "Surface 4" (mock-shape template, image-test rewrite skeleton, drift on D-08 covering 2 lines :292 + :298)
  </read_first>
  <action>
    Step 1 — Add the `vi.mock` block for `el-templo-bot/src/ai/provider` immediately AFTER the existing `vi.mock('../../../el-templo-bot/src/whatsapp/client', ...)` block (`:27-39`) and BEFORE the route import at `:42`. Mirror the EXACT shape of the existing mock per PATTERNS.md S-1:

    Use `vi.mock` with the provider path `../../../el-templo-bot/src/ai/provider` and an async factory taking `importOriginal`. Inside the factory, await `importOriginal` invocation typed against the provider module (mirror the typing pattern at the existing :27-39 client mock), then return an object that spreads the original module and overrides `createAiProvider` with a `vi.fn` returning an object whose `chat` is `vi.fn().mockResolvedValue` of `{ content: "Hola, soy Mica.", toolCalls: [] }`.

    Why `toolCalls: []`: handler at `:708` checks `if (response.toolCalls.length > 0)` for the tool loop; empty array skips the tool round-trip entirely. The mock returns a single canned text reply via `provider.chat`, which the handler then sends via the already-mocked `sendTextMessage`.

    The canned reply CONSTANT is the literal string `"Hola, soy Mica."` (locked plan-phase value per D-07 Discretion; 15 chars; valid Spanish greeting; doesn't collide with any other assertion). Same literal is used in Step 2 echo-assertion rewrites.

    Step 2 — Per D-08 + PATTERNS.md drift, rewrite the TWO echo-text literals in the new-sender text test (both sites use the canned reply):
    - Site `:292` `expect(echoMessages[0].content).toBe("Echo: Hello from WhatsApp!")` → `expect(echoMessages[0].content).toBe("Hola, soy Mica.")`.
    - Site `:298` `"Echo: Hello from WhatsApp!"` (second arg of `expect(sendTextMessage).toHaveBeenCalledWith("5491100000001", "Echo: Hello from WhatsApp!")`) → `"Hola, soy Mica."`.

    DO NOT touch:
    - Inbound message content at `:251`/`:284` (`"Hello from WhatsApp!"` — that's the user input, not the bot reply).
    - The existing-sender count assertion at `:339-340` (`expect(messages).toHaveLength(2)` + `expect(sendTextMessage).toHaveBeenCalledOnce()`) — count is invariant under this change.

    Step 3 — Per D-09 + D-10, rewrite the image test at `:388-418`. Replace the stale "returns 200 but does not store or reply" body with post-quick-16-fix-3 behavior assertions. The new test:
    - Rename description from `"returns 200 but does not store or reply"` to `"returns 200, stores inbound, and sends non-text fallback"`.
    - Keep the `app.inject` POST and the status/body assertions.
    - Remove the `await new Promise((r) => setTimeout(r, 100));` at `:402`.
    - Replace with `await waitForHandler();` (handler now fires async work — store + reply per `el-templo-bot/src/webhook/handler.ts:323-358` "quick-16 fix 3" anchor).
    - Replace the stale assertions:
      - `expect(messages).toHaveLength(0)` → `expect(messages).toHaveLength(2)` (1 inbound + 1 outbound fallback).
      - `expect(sendTextMessage).not.toHaveBeenCalled()` → `expect(sendTextMessage).toHaveBeenCalledOnce()`.
      - `expect(conversations).toHaveLength(0)` → `expect(conversations).toHaveLength(1)` (inbound INSERT triggered creation).
    - Add a new assertion `expect(messages[1].content).toContain("imagen")` to anchor the fallback variant (per D-09 semantic substring; matches `getNonTextFallback("image")` which returns `"Recibí tu imagen, pero ..."`).
    - Use `ORDER BY id` on the messages SELECT so `messages[1]` is the outbound fallback deterministically.

    Step 4 — Type-check then run only the webhook file:
    - `cd el-templo-api && pnpm tsc --noEmit` exits 0.
    - `cd el-templo-bot && pnpm tsc --noEmit` exits 0 (verify SC#6 across both packages).
    - `cd el-templo-api && pnpm test --run -- test/whatsapp/webhook.test.ts` shows all-pass (3 previously-failing tests now pass).

    Step 5 — Run the FULL `el-templo-api` test suite to confirm SC#1:
    - `cd el-templo-api && pnpm test --run` should show `511 passed / 1 failed / 512 total`. The 1 failure must be BUG-03 (i) LIKE-search at `el-templo-bot/src/ai/tools.ts:455` in `test/whatsapp/v5-3-3-booking.integration.test.ts` (Phase 95-deferred RED — STAYS RED per CONTEXT.md deferred section).

    Step 6 — Re-verify the 6-pair sha256 canonical-invariant block is UNCHANGED. The hash must remain `67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344` at all 6 anchors. Run a `git diff HEAD~3 -- '.planning/STATE.md' '.planning/ROADMAP.md' 'el-templo-bot/src/**' 'el-templo-api/src/**'` and confirm zero changes to any DEBOUNCE_TTL_SECONDS canonical-block site. (Phase 98 modifies zero terms by construction; this is a defensive re-verification.)

    Step 7 — Commit atomically: `git add el-templo-api/test/whatsapp/webhook.test.ts && git commit -m "test(98-C): add AI provider mock + align echo asserts + rewrite image test for store+reply behavior"`.

    Rejected approaches (DO NOT IMPLEMENT — D-07 rejects):
    - Mocking the OpenAI SDK directly (brittle to SDK shape).
    - Setting a real `OPENAI_API_KEY` in CI (cost + flakiness + non-determinism).
    - Adding a `MockAiProvider` class in `el-templo-bot/src/ai/` (violates SC#5).

    SC#5 HARD GUARD reminder: this task touches ONLY `el-templo-api/test/whatsapp/webhook.test.ts`. If the executor sees ANY need to modify `el-templo-api/src/**` or `el-templo-bot/src/**`, STOP and re-classify per `/gsd-debug` (a/b/c) framework. Do NOT silently absorb.

    Style constraints (per CLAUDE.md): no `console.log`, no `any` types added.

  </action>
  <behavior>
    - `vi.mock("../../../el-templo-bot/src/ai/provider", ...)` intercepts `createAiProvider` at module load. Tests that hit the handler's AI path now use the stubbed provider whose `chat()` returns `{ content: "Hola, soy Mica.", toolCalls: [] }` — no real OpenAI API call, no 401 on placeholder `sk-xxxxxxxx`.
    - The new-sender text test's outbound message content + `sendTextMessage` invocation both verify the canned reply `"Hola, soy Mica."` (DB row content + mock-spy second arg).
    - The image test verifies the store+reply behavior anchored to `handler.ts:323-358` "quick-16 fix 3": 1 inbound row + 1 outbound row, 1 `sendTextMessage` call, fallback content contains `"imagen"`, 1 conversation row created.
    - `await waitForHandler()` replaces the brittle `setTimeout(100)` race condition; the test waits deterministically for the handler's async work to complete.
  </behavior>
  <verify>
    <automated>cd el-templo-api &amp;&amp; pnpm tsc --noEmit &amp;&amp; pnpm test --run -- test/whatsapp/webhook.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "vi.mock(\"../../../el-templo-bot/src/ai/provider\"" el-templo-api/test/whatsapp/webhook.test.ts` returns `1` (new mock added).
    - `grep -c "vi.mock(\"../../../el-templo-bot/src/whatsapp/client\"" el-templo-api/test/whatsapp/webhook.test.ts` returns `1` (existing mock untouched).
    - `grep -c "Hola, soy Mica\\." el-templo-api/test/whatsapp/webhook.test.ts` returns `3` (canned reply appears once in mock factory + twice in echo asserts at :292 + :298).
    - `grep -c "Echo: Hello from WhatsApp!" el-templo-api/test/whatsapp/webhook.test.ts` returns `0` (both stale echo literals replaced).
    - `grep -c "setTimeout(r, 100)" el-templo-api/test/whatsapp/webhook.test.ts | head -1` — count of `setTimeout(r, 100)` occurrences must be at most the pre-edit count minus 1 (the `:402` site removed). Specifically: `grep -nE "setTimeout\\(r, 100\\)" el-templo-api/test/whatsapp/webhook.test.ts` MUST NOT show a hit between lines 388 and 420 (the image test region).
    - `grep -cE "await waitForHandler\\(\\)" el-templo-api/test/whatsapp/webhook.test.ts` returns a count strictly greater than the pre-edit count (image test now awaits handler).
    - `grep -c "returns 200, stores inbound, and sends non-text fallback" el-templo-api/test/whatsapp/webhook.test.ts` returns `1` (new description landed).
    - `grep -c "returns 200 but does not store or reply" el-templo-api/test/whatsapp/webhook.test.ts` returns `0` (stale description removed).
    - `grep -c 'toContain("imagen")' el-templo-api/test/whatsapp/webhook.test.ts` returns `1` (semantic substring assertion landed).
    - `cd el-templo-api && pnpm tsc --noEmit` exits with code 0 (substantive verify gate (b)).
    - `cd el-templo-bot && pnpm tsc --noEmit` exits with code 0 (substantive verify gate (b), both packages — SC#6).
    - `cd el-templo-api && pnpm test --run -- test/whatsapp/webhook.test.ts` shows all-pass (3 previously-failing webhook tests now green).
    - **PHASE-WIDE SC#1 GATE:** `cd el-templo-api && pnpm test --run` exit status reports `511 passed | 1 failed | 512 total`. The 1 failure is in `test/whatsapp/v5-3-3-booking.integration.test.ts` (BUG-03 (i) LIKE-search at `el-templo-bot/src/ai/tools.ts:455`, Phase 95-deferred — STAYS RED).
    - **SC#5 HARD GUARD:** `git diff HEAD~3 HEAD -- 'el-templo-api/src/**' 'el-templo-bot/src/**' | wc -l` returns `0` across all 3 Phase 98 GREEN commits (substantive verify gate (e) — negative assertion).
    - `git show --stat HEAD | grep -cE 'el-templo-api/test/whatsapp/webhook\\.test\\.ts$'` returns `1` (exactly 1 file in this commit).
    - `git log -1 --format=%s | grep -qE '^test\\(98-C\\): '` (commit subject regex (d)).
    - `git log HEAD~2..HEAD --format=%s | grep -cE '^test\\(98-[ABC]\\): '` returns `3` (all 3 atomic GREEN commits follow the naming convention).
  </acceptance_criteria>
  <done>
    - `vi.mock` block for AI provider added with canned `"Hola, soy Mica."` reply + empty toolCalls.
    - Echo literal updated at both `:292` and `:298`.
    - Image test rewritten: description renamed, `setTimeout(100)` removed, `waitForHandler()` integrated, assertions match post-quick-16-fix-3 store+reply behavior, `"imagen"` semantic substring asserted.
    - `tsc --noEmit` clean on BOTH `el-templo-api` AND `el-templo-bot` (SC#6).
    - Full `el-templo-api` test suite: 511 passed / 1 failed / 512 total (SC#1 met).
    - SC#5 verified: zero `src/**` changes across all 3 Phase 98 commits.
    - 6-pair sha256 invariant `67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344` re-verified UNCHANGED.
    - Atomic GREEN commit landed with subject `test(98-C): ...`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 (Phase 98 sign-off): Human review of phase-wide SC#1..SC#6 + commit chain</name>
  <files>(none — verification-only checkpoint; no files modified at this step)</files>
  <action>Pause execution. Surface the verification checklist below to the operator. The operator runs each step and confirms or rejects. Do NOT proceed to SUMMARY commit until "approved" is received. If the operator reports any failure, halt and do NOT modify any files — return control with the rejection details so the executor can determine whether to revert, retry, or escalate.</action>
  <what-built>
    Three atomic GREEN test-infra commits closing 29 of 30 pre-existing `el-templo-api` failures. One Phase-95-deferred RED preserved. Zero production source modifications.
  </what-built>
  <how-to-verify>
    1. Confirm the commit chain is exactly 3 GREEN commits with correct subjects:
       `git log HEAD~2..HEAD --format='%h %s'` should show three commits whose subjects start with `test(98-A): `, `test(98-B): `, `test(98-C): ` respectively (oldest → newest).
    2. Confirm SC#1: `cd el-templo-api && pnpm test --run` exits with the test runner reporting `511 passed | 1 failed | 512 total`. Inspect the failed-test list — the SINGLE failure MUST be in `test/whatsapp/v5-3-3-booking.integration.test.ts` with description containing "BUG-03 candidate (i)" or "LIKE-search". If any OTHER test fails, STOP and re-investigate (likely SC#3 wording site missed, or 98-A LEAVE-ALONE site accidentally modified).
    3. Confirm SC#5 (HARD GUARD): `git diff HEAD~3 HEAD -- 'el-templo-api/src/**' 'el-templo-bot/src/**'` outputs ZERO lines. If non-empty, STOP and revert the offending change — Phase 98 must NOT touch production source.
    4. Confirm SC#6: `cd el-templo-api && pnpm tsc --noEmit` exits 0 AND `cd el-templo-bot && pnpm tsc --noEmit` exits 0.
    5. Confirm 6-pair sha256 invariant UNCHANGED: extract the canonical `DEBOUNCE_TTL_SECONDS` block from any one of the 6 anchors (`.planning/STATE.md`, `.planning/ROADMAP.md` Phase 93 Notes, `.planning/ROADMAP.md` Phase 94 SC#1, `.planning/MACRO-ROADMAP.md` constraint #6, `93-CONTEXT.md` Cross-Phase Invariant, `94-02-SUMMARY.md`), pipe through `shasum -a 256`, and confirm output begins with `67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344`.
    6. Confirm exact file-count: `git diff --name-only HEAD~3 HEAD` should list exactly 4 files: `el-templo-api/test/helpers.ts`, `el-templo-api/test/subscriptions/subscriptions.test.ts`, `el-templo-api/test/whatsapp/ai-tools.test.ts`, `el-templo-api/test/whatsapp/webhook.test.ts`.
    7. Confirm BUG-03 (i) preserved RED: the failure-output line for the single failing test must show "FAILS on master" or equivalent — this is the intentional Phase 95-deferred deferred-scope marker. Phase 98 does NOT close it.
    8. Spot-check leave-alone sites in `subscriptions.test.ts` are unchanged:
       - `:537` `startDate: "2025-01-01"` present.
       - `:721` `startDate: "2026-06-01"` present.
       - `:733` `startDate: "2026-07-01"` present.
       - `:606` `expect(body.endDate >= "2026-03-31").toBe(true)` present.

    If ALL eight checks pass, the phase is ready for SUMMARY. The execute-phase orchestrator will create `98-01-SUMMARY.md` next.

  </how-to-verify>
  <verify>
    <human-check>Operator confirms all eight checklist steps above pass and replies "approved". If any step fails, operator reports which one and the actual output so executor can halt without modifying files.</human-check>
  </verify>
  <done>
    Operator has replied "approved" after walking the eight verification steps. Phase 98 is cleared for the SUMMARY commit (created by execute-phase orchestrator after this checkpoint resolves).
  </done>
  <resume-signal>Type "approved" to proceed to SUMMARY commit, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                                                                                                                                                              |
| ------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Test code → production runtime | Tests EXECUTE production handlers/services via `app.inject`, but Phase 98 modifies ONLY test files. No new attack surface introduced.                                                                    |
| Mocked external API (OpenAI)   | The `vi.mock` in 98-C interposes a canned-text provider, eliminating accidental real-API calls during test runs (placeholder `sk-xxxxxxxx` would otherwise 401). Improves isolation, not a new boundary. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                          | Disposition | Mitigation Plan                                                                                                                                               |
| --------- | ---------------------- | -------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-98-01   | Tampering              | Production source via accidental test-helper drift | mitigate    | SC#5 HARD GUARD: `git diff HEAD~3 HEAD -- 'el-templo-api/src/**' 'el-templo-bot/src/**'                                                                       | wc -l == 0` (negative-assertion gate (e)) verified in Task 3 acceptance + Task 4 human-verify checklist. STOP-and-reclassify guard documented in CONTEXT.md domain section. |
| T-98-02   | Information Disclosure | Real `OPENAI_API_KEY` leaking via test-run logs    | accept      | Test env uses placeholder `sk-xxxxxxxx`; no real key in CI by design. `vi.mock` in 98-C eliminates outbound API calls regardless. No PII or secrets touched.  |
| T-98-03   | Denial of Service      | Brittle `setTimeout(100)` race in image test       | mitigate    | D-10 replaces `setTimeout(100)` with deterministic `waitForHandler()`. Removes a flake source under load.                                                     |
| T-98-04   | Repudiation            | Atomic commits enabling per-zone revert            | mitigate    | D-11 atomic GREEN-A/B/C cadence; each fix-zone is independently revertible. Commit subject regex `^test\(98-[ABC]\): ` verified by gate (d) for auditability. |

**Phase 98 attack-surface delta: ZERO.** Test-infra-only. SC#5 HARD GUARD is the structural containment.
</threat_model>

<verification>

## Phase-wide checks (executed by execute-phase + Task 4 human-verify)

| Gate                     | Command                                                                                   | Pass                                                                           |
| ------------------------ | ----------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ | -------- | ---------------------------------------------------------- |
| SC#1                     | `cd el-templo-api && pnpm test --run`                                                     | `511 passed                                                                    | 1 failed | 512 total`; single failure is BUG-03 (i) at `tools.ts:455` |
| SC#5                     | `git diff HEAD~3 HEAD -- 'el-templo-api/src/**' 'el-templo-bot/src/**'                    | wc -l`                                                                         | `0`      |
| SC#6a                    | `cd el-templo-api && pnpm tsc --noEmit`                                                   | exit 0                                                                         |
| SC#6b                    | `cd el-templo-bot && pnpm tsc --noEmit`                                                   | exit 0                                                                         |
| 6-pair invariant         | `shasum -a 256` of the canonical `DEBOUNCE_TTL_SECONDS` block at any anchor               | hash begins `67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344` |
| File-count               | `git diff --name-only HEAD~3 HEAD`                                                        | exactly 4 files (helpers.ts + 3 test files)                                    |
| Commit naming            | `git log HEAD~2..HEAD --format=%s                                                         | grep -cE '^test\(98-[ABC]\): '`                                                | `3`      |
| BUG-03 (i) RED preserved | `cd el-templo-api && pnpm test --run -- test/whatsapp/v5-3-3-booking.integration.test.ts` | exit non-zero; description references "LIKE-search" / "BUG-03 candidate (i)"   |

</verification>

<success_criteria>

Phase 98 ships when:

- [ ] 3 atomic GREEN commits landed with subjects `test(98-A): ...`, `test(98-B): ...`, `test(98-C): ...` (D-11 cadence).
- [ ] `cd el-templo-api && pnpm test --run` → `511 passed | 1 failed | 512 total` (SC#1).
- [ ] The single failure is BUG-03 (i) LIKE-search RED at `el-templo-bot/src/ai/tools.ts:455` (Phase 95-deferred — STAYS RED).
- [ ] `cd el-templo-api && pnpm tsc --noEmit` exits 0 AND `cd el-templo-bot && pnpm tsc --noEmit` exits 0 (SC#6).
- [ ] `git diff HEAD~3 HEAD -- 'el-templo-api/src/**' 'el-templo-bot/src/**'` produces zero output (SC#5 HARD GUARD).
- [ ] 6-pair sha256 `67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344` re-verified UNCHANGED at all 6 anchors.
- [ ] Exactly 4 files modified across the 3 commits: `el-templo-api/test/helpers.ts`, `el-templo-api/test/subscriptions/subscriptions.test.ts`, `el-templo-api/test/whatsapp/ai-tools.test.ts`, `el-templo-api/test/whatsapp/webhook.test.ts`.
- [ ] Human sign-off received via Task 4 checkpoint.
- [ ] Phase 97 RGUARD-01 unblocked (green baseline established; regression-suite lock can proceed on top).

</success_criteria>

<output>
Create `.planning/phases/98-test-hygiene-98-a-b-c/98-01-SUMMARY.md` after Task 4 sign-off.

Summary should document:

- The 3 atomic GREEN commit hashes + subjects + per-zone fail-count deltas (98-A: 6→0, 98-B: 20→0, 98-C: 3→0; total 29→0 newly-green plus 1 deferred RED preserved).
- SC#1..SC#6 verification outputs (the verification-checks table filled in with actual outputs).
- BUG-03 (i) STAYS RED confirmation (Phase 95-deferred marker preserved).
- 6-pair sha256 invariant re-verified UNCHANGED.
- Next step in v5.4.0 path: Phase 97 RGUARD-01 unblocked.
  </output>
