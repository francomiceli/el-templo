---
phase: 46-lifestyle-content-extraction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/lifestyle/seed/factos.seed.ts
  - el-templo-api/src/modules/lifestyle/seed/tools.seed.ts
  - el-templo-api/src/modules/lifestyle/seed/DEFERRED-CONTENT.md
  - el-templo-api/src/modules/lifestyle/index.ts
autonomous: true
requirements: [RSTRC-05]

must_haves:
  truths:
    - "~40 factos are curated from the 60 universal ones, prioritized by brand fit (stoic warriors, classical philosophy)"
    - "All 5 philosophical tool definitions are extracted with names, descriptions, and question frameworks"
    - "Deferred content catalog documents all Level 3+ content with counts, types, and source file locations"
    - "Lifestyle module has a barrel export re-exporting all seed types"
  artifacts:
    - path: "el-templo-api/src/modules/lifestyle/seed/factos.seed.ts"
      provides: "FactoSeed type + FACTO_SEEDS array with ~40 curated stoic/warrior factos"
      exports: ["FactoSeed", "FACTO_SEEDS"]
    - path: "el-templo-api/src/modules/lifestyle/seed/tools.seed.ts"
      provides: "PhilosophicalToolSeed type + TOOL_SEEDS array with 5 tool definitions"
      exports: ["PhilosophicalToolSeed", "TOOL_SEEDS"]
    - path: "el-templo-api/src/modules/lifestyle/seed/DEFERRED-CONTENT.md"
      provides: "Inventory of all deferred Arete content for v5.0 reference"
    - path: "el-templo-api/src/modules/lifestyle/index.ts"
      provides: "Module barrel export for lifestyle seed types"
  key_links:
    - from: "el-templo-api/src/modules/lifestyle/seed/factos.seed.ts"
      to: "arete-app/constants/factos.ts"
      via: "content curation from universal (brand: 'both') factos"
      pattern: "FactoSeed\\[\\]"
    - from: "el-templo-api/src/modules/lifestyle/seed/tools.seed.ts"
      to: "arete-app/features/tools/tools-index.tsx"
      via: "tool definition extraction"
      pattern: "PhilosophicalToolSeed\\[\\]"
    - from: "el-templo-api/src/modules/lifestyle/index.ts"
      to: "el-templo-api/src/modules/lifestyle/seed/*.ts"
      via: "barrel re-export"
      pattern: "export.*from.*seed"
---

<objective>
Curate ~40 factos from Arete's universal catalog, extract all 5 philosophical tool definitions, create a deferred content inventory, and wire up the lifestyle module barrel export.

Purpose: Factos and tools complete the starter content set. The deferred catalog ensures nothing is lost for v5.0. The barrel export follows the module convention established in Phase 45.

Output: Two seed data files, one deferred content inventory document, and a module barrel export.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-lifestyle-content-extraction/46-CONTEXT.md
@arete-app/constants/factos.ts
@arete-app/features/tools/tools-index.tsx
@arete-app/constants/challenges.ts
@arete-app/constants/revelations.ts
@el-templo-api/src/modules/aura/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Curate ~40 factos and extract 5 philosophical tool definitions into seed files</name>
  <files>el-templo-api/src/modules/lifestyle/seed/factos.seed.ts, el-templo-api/src/modules/lifestyle/seed/tools.seed.ts</files>
  <action>
Create the lifestyle seed directory if not already created by Plan 01: `el-templo-api/src/modules/lifestyle/seed/`

**Factos (factos.seed.ts):**

Read `arete-app/constants/factos.ts`. Select ~40 factos from the 60 universal ones (f01-f60, brand: 'both'). EXCLUDE all 20 Arete-specific factos (f61-f80, brand: 'arete') — those are Mediterranean wellness / feminine focus, not El Templo's brand.

Curation criteria — prioritize by El Templo brand fit (temple/warrior/stoic identity):

- INCLUDE ALL 10 Marco Aurelio factos (f01-f10) — core stoic emperor, perfect fit
- INCLUDE ALL 8 Seneca factos (f11-f18) — core stoic philosopher, perfect fit
- INCLUDE ALL 6 Epicteto factos (f19-f24) — core stoic, perfect fit
- INCLUDE ALL 5 Socrates factos (f25-f29) — foundation of Western philosophy
- INCLUDE 2 of 3 Platon factos — f30 (Academia), f31 (wrestler/philosopher) — skip f32 (geometry inscription, less engaging)
- INCLUDE 3 of 4 Aristoteles factos — f33 (peripatetics), f34 (habits quote), f36 (virtue/mean) — skip f35 (animal classification, less relevant)
- INCLUDE 2 of 3 Alejandro Magno factos — f37 (Iliada), f38 (water in desert) — skip f39 (Diogenes encounter, already covered by f44-f46)
- INCLUDE ALL 4 Leonidas/Esparta factos (f40-f43) — warrior ethos, core brand
- INCLUDE ALL 3 Diogenes factos (f44-f46) — radical simplicity, strong brand fit
- INCLUDE 1 of 2 Heraclito — f47 (river/change, iconic) — skip f48 (rejected governance, weaker)
- INCLUDE 1 of 2 Pitagoras — f49 (5 years silence) — skip f50 (music/math, less engaging)
- INCLUDE BOTH Hipatia factos — f51, f52 — science/wisdom, good diversity
- INCLUDE ALL 3 Zenon de Citio factos (f53-f55) — founder of Stoicism
- INCLUDE ALL 3 Caton el Joven factos (f56-f58) — stoic politician/warrior
- INCLUDE Tales de Mileto f59 — knowledge as weapon
- INCLUDE Cineas f60 — great philosophical question

This gives ~40-42 factos. Claude has discretion on exact selection — aim for ~40.

Define the interface:

- `FactoSeed`: id (string), text (string), source (string), figure (string), era (string), category (union matching Arete's categories minus 'bienestar': 'filosofia' | 'guerra' | 'politica' | 'ciencia' | 'arte' | 'deporte')

No brand field needed (El Templo is one brand). No minLevel needed (all shown to Level 2+ per Arete's design, but level gating is a v5.0 concern).

The universal factos are already brand-neutral — no adaptation needed beyond removing the `brand` field. The text is clean stoic content.

Export `FACTO_SEEDS` as `readonly FactoSeed[]`.

**Tools (tools.seed.ts):**

Read `arete-app/features/tools/tools-index.tsx` to extract the 5 tool definitions. This file has the TOOLS array with id, name, description, minLevel for each tool.

Also read the individual tool component files to extract the question frameworks (the criteria/dimensions each tool uses):

- `arete-app/features/tools/components/las-4-pruebas.tsx` — Las 4 Pruebas: examines a belief against 4 criteria
- `arete-app/features/tools/components/mapa-de-friccion.tsx` — Mapa de Friccion: identifies friction sources and patterns
- `arete-app/features/tools/components/tabla-de-poder.tsx` — Tabla de Poder: evaluates a decision from 4 dimensions
- `arete-app/features/tools/components/tabla-del-estratega.tsx` — Tabla del Estratega: compares two options dimension by dimension
- `arete-app/features/tools/components/test-de-virtud.tsx` — Test de Virtud: examines motivation behind habits

For each tool, extract:

- Name and description from TOOLS array
- The framework structure: what questions it asks, what dimensions/criteria it uses
- Do NOT extract React Native UI code — only the conceptual framework

Define the interface:

- `PhilosophicalToolSeed`: id (string), name (string), description (string), framework (object describing the tool's approach — questions asked, dimensions evaluated, etc.)

The framework field should be a structured object that captures the tool's intellectual model without UI concerns. Use a flexible but typed structure. Read each component file to understand what questions/criteria each tool uses, then represent that as data.

Export `TOOL_SEEDS` as `readonly PhilosophicalToolSeed[]`.

Use `as const satisfies` pattern for type safety on both exports.
</action>
<verify>
<automated>cd /home/franco/projects/el-templo/el-templo-api && npx tsc --noEmit src/modules/lifestyle/seed/factos.seed.ts src/modules/lifestyle/seed/tools.seed.ts 2>&1 | head -20</automated>
</verify>
<done>factos.seed.ts contains ~40 curated universal factos with FactoSeed type, tools.seed.ts contains 5 philosophical tool definitions with frameworks, both compile cleanly with no `any` types</done>
</task>

<task type="auto">
  <name>Task 2: Create deferred content catalog and lifestyle module barrel export</name>
  <files>el-templo-api/src/modules/lifestyle/seed/DEFERRED-CONTENT.md, el-templo-api/src/modules/lifestyle/index.ts</files>
  <action>
**Deferred Content Catalog (DEFERRED-CONTENT.md):**

Create a reference document cataloging all Arete content NOT included in the starter set. This is v5.0's "shopping list" — raw inventory with source locations, not adapted content.

Structure the catalog with these sections:

1. **Habits (Level 3-5)** — List all 24 habits with minLevel >= 3. Include: code, name, area, minLevel. Source: `arete-app/constants/habits.ts` and `arete-app/constants/habit-details.ts`

2. **Journal Questions (Deep + Philosophical)** — List all Level 3+ questions (7 deep + 4 philosophical from 'both', 3 deep + 2 philosophical from 'arete'). Include: id, text, category, minLevel. Source: `arete-app/constants/journal-questions.ts`

3. **Challenges** — All 36 challenges (24 universal + 12 Arete-specific), all Level 3+. Include: count by area and brand. Source: `arete-app/constants/challenges.ts`

4. **Revelations** — All 23 revelations (Level 3+). Include: count by category and brand. Source: `arete-app/constants/revelations.ts`

5. **Gamification Config** — AURUM earn rates, rank thresholds, quests, variable rewards. Note: deferred to v5.0 AURA economy. Source: `arete-app/constants/aurum.ts`, `arete-app/constants/gamification.ts`

6. **Skipped Content** — Monthly badges (12, brand: 'arete' mythology), level naming system. Note: El Templo will design its own. Source: `arete-app/constants/badges.ts`, `arete-app/constants/levels.ts`

For each section include:

- Total count
- Content types and their shapes
- Level requirements
- Brand distribution (how many 'both' vs 'arete' vs 'aurea')
- Exact source file path in the Arete codebase
- Brief note on adaptation needed for El Templo

**Lifestyle Module Barrel Export (index.ts):**

Create `el-templo-api/src/modules/lifestyle/index.ts` following the barrel export pattern from Phase 45 (see `el-templo-api/src/modules/aura/index.ts` for the established pattern).

Re-export all seed types and data arrays:

```typescript
// Module: lifestyle
export type { HabitSeed } from "./seed/habits.seed";
export type { JournalQuestionSeed } from "./seed/journal-questions.seed";
export type { FactoSeed } from "./seed/factos.seed";
export type { PhilosophicalToolSeed } from "./seed/tools.seed";

export { HABIT_SEEDS } from "./seed/habits.seed";
export { JOURNAL_QUESTION_SEEDS } from "./seed/journal-questions.seed";
export { FACTO_SEEDS } from "./seed/factos.seed";
export { TOOL_SEEDS } from "./seed/tools.seed";
```

Note: If Plan 01 has not run yet (parallel execution), the barrel export may fail tsc initially. The executor should create the barrel export regardless and note that full compilation requires both plans complete. If Plan 01 has already run, the barrel export will compile fully.
</action>
<verify>
<automated>cd /home/franco/projects/el-templo/el-templo-api && ls -la src/modules/lifestyle/seed/DEFERRED-CONTENT.md src/modules/lifestyle/index.ts && wc -l src/modules/lifestyle/seed/DEFERRED-CONTENT.md</automated>
</verify>
<done>DEFERRED-CONTENT.md exists with inventory of all deferred content (habits L3+, journal deep/philosophical, 36 challenges, 23 revelations, gamification config, skipped content) with counts, types, and source paths. index.ts barrel export re-exports all seed types following the aura module pattern.</done>
</task>

</tasks>

<verification>
- factos.seed.ts and tools.seed.ts compile with `npx tsc --noEmit`
- DEFERRED-CONTENT.md has sections for all 6 deferred content categories
- index.ts follows the barrel export pattern from the aura module
- No `any` types in any TypeScript file
- No Arete/Aurea Virtus brand references in seed files (factos are already brand-neutral)
- Facto count is approximately 40 (not more than 45, not fewer than 35)
- All 5 philosophical tools are represented in tools.seed.ts
</verification>

<success_criteria>

1. ~40 curated factos focusing on stoic warriors and classical philosophy figures
2. All 5 philosophical tools extracted with framework definitions
3. Deferred content catalog is a complete inventory for v5.0 planning
4. Module barrel export re-exports all seed types
5. All TypeScript files compile cleanly
   </success_criteria>

<output>
After completion, create `.planning/phases/46-lifestyle-content-extraction/46-02-SUMMARY.md`
</output>
