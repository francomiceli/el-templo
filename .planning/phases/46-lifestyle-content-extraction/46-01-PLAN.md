---
phase: 46-lifestyle-content-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/lifestyle/seed/habits.seed.ts
  - el-templo-api/src/modules/lifestyle/seed/journal-questions.seed.ts
autonomous: true
requirements: [RSTRC-05]

must_haves:
  truths:
    - "~15 Level 1-2 habits are extracted from Arete with full details (howTo, whyItMatters, tips) and adapted to El Templo brand"
    - "~14 simple-tier journal questions are extracted and Arete-specific ones adapted to neutral voice"
    - "All brand references (Arete, Aurea Virtus, AURUM, alchemical terms, gendered language) are removed or replaced"
    - "Argentine Spanish (rioplatense) tone is preserved"
  artifacts:
    - path: "el-templo-api/src/modules/lifestyle/seed/habits.seed.ts"
      provides: "HabitSeed type + HabitDetailSeed type + HABITS array with ~15 Level 1-2 habits including full details"
      exports: ["HabitSeed", "HabitDetailSeed", "HABIT_SEEDS"]
    - path: "el-templo-api/src/modules/lifestyle/seed/journal-questions.seed.ts"
      provides: "JournalQuestionSeed type + JOURNAL_QUESTION_SEEDS array with ~14 simple-tier questions"
      exports: ["JournalQuestionSeed", "JOURNAL_QUESTION_SEEDS"]
  key_links:
    - from: "el-templo-api/src/modules/lifestyle/seed/habits.seed.ts"
      to: "arete-app/constants/habits.ts + habit-details.ts"
      via: "content extraction and brand adaptation"
      pattern: "HabitSeed\\[\\]"
    - from: "el-templo-api/src/modules/lifestyle/seed/journal-questions.seed.ts"
      to: "arete-app/constants/journal-questions.ts"
      via: "content extraction and brand adaptation"
      pattern: "JournalQuestionSeed\\[\\]"
---

<objective>
Extract and adapt Level 1-2 habits (with full details) and simple-tier journal questions from the Arete codebase into typed TypeScript seed files for El Templo's lifestyle module.

Purpose: These seed files become the content foundation for v5.0's lifestyle module. Habits and journal questions need the most brand adaptation work (removing Arete/Aurea Virtus references, adapting gendered language, replacing AURUM with AURA).

Output: Two seed data files in el-templo-api/src/modules/lifestyle/seed/ with typed arrays ready for future DB seeding.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-lifestyle-content-extraction/46-CONTEXT.md
@arete-app/constants/habits.ts
@arete-app/constants/habit-details.ts
@arete-app/constants/journal-questions.ts
@el-templo-api/src/modules/aura/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create habits seed file with ~15 Level 1-2 habits and full details</name>
  <files>el-templo-api/src/modules/lifestyle/seed/habits.seed.ts</files>
  <action>
Create the lifestyle seed directory: `el-templo-api/src/modules/lifestyle/seed/`

Read `arete-app/constants/habits.ts` and `arete-app/constants/habit-details.ts` to extract all habits where minLevel <= 2. There are approximately 15 of these (Level 1: MEN-01, CUE-01, COH-01, ACC-01, REF-01 = 5 habits; Level 2: MEN-02, MEN-03, MEN-04, CUE-02, CUE-03, CUE-04, COH-02, ACC-02, VIN-01, VIN-02, REF-02, REF-03 = 12 habits; total ~17 — include all minLevel 1-2).

Define two TypeScript interfaces in the file:

- `HabitSeed`: id (string), area (union of 6 areas), code (string), name (string), description (string), moment (union: 'manana' | 'dia' | 'noche' | 'semanal' | 'libre'), minLevel (number), durationMinutes (number | null), howTo (string), whyItMatters (string), tips (string[])
- Combine habit + habit details into a single flat structure per entry (no separate details lookup needed)

Export `HABIT_SEEDS` as a typed `readonly HabitSeed[]` array.

Brand adaptation rules (apply to ALL text fields — name, description, howTo, whyItMatters, tips):

- Replace 'Aurea Virtus' with 'El Templo' wherever it appears
- Replace 'Arete' (as brand reference) with 'El Templo'
- Replace 'AURUM' with 'AURA'
- Remove alchemical references (alquimia, alquimico, Nigredo, Albedo, Citrinitas, Rubedo)
- Make gendered language neutral where applicable
- Remove level names (Iniciado, Adepto, Semilla, Raiz, etc.)
- Preserve Argentine Spanish (rioplatense) — vos forms, informal conjugations
- Keep the original tone: stoic content stays stoic, contemplative stays contemplative
- This is LIGHT editing — keep original style, just remove brand references

The existing Arete content is already very clean and brand-neutral for the 'both' brand habits. Most Level 1-2 habits have brand 'both' so minimal changes needed. Focus adaptation on the howTo/whyItMatters/tips text which may reference the Arete app itself.

Use `as const satisfies readonly HabitSeed[]` for type safety.
</action>
<verify>
<automated>cd /home/franco/projects/el-templo/el-templo-api && npx tsc --noEmit src/modules/lifestyle/seed/habits.seed.ts 2>&1 | head -20</automated>
</verify>
<done>habits.seed.ts exists with HabitSeed type and HABIT_SEEDS array containing all Level 1-2 habits (~15-17) with full howTo/whyItMatters/tips details, all brand references adapted, TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Create journal questions seed file with ~14 simple-tier questions</name>
  <files>el-templo-api/src/modules/lifestyle/seed/journal-questions.seed.ts</files>
  <action>
Read `arete-app/constants/journal-questions.ts` to extract all simple-tier (category: 'simple') questions.

There are 14 questions with brand 'both' (s01-s14) and 5 with brand 'arete' (a01-a05). Per CONTEXT.md, include the ~14 simple-tier questions including the Arete-specific ones adapted to neutral voice.

Include all 14 'both' questions (s01-s14) as-is (they're already brand-neutral). For the 5 Arete-specific simple questions (a01-a05), adapt them:

- a01 'Que le pidio tu cuerpo hoy que realmente le diste?' — keep as-is (already neutral)
- a02 'Que gesto de cuidado tuviste con vos misma hoy...' — change 'vos misma' to 'vos mismo/a' or neutral 'contigo' while keeping rioplatense feel. Per brand term mappings, make gendered terms neutral. Use 'vos' form naturally: 'Que gesto de cuidado tuviste hoy, por chiquito que sea?'
- a03 'Si tuvieras que agradecerle algo a tu cuerpo esta noche...' — keep as-is (neutral)
- a04 'Que ritual de tu dia te dio mas calma o placer?' — keep as-is (neutral)
- a05 'Que sabor, aroma o textura del dia te gustaria guardar en la memoria?' — keep as-is (neutral)

So include all 19 simple-tier questions (14 'both' + 5 'arete' adapted) in the seed. The CONTEXT.md says "~14" but also says "including Arete-specific ones adapted to neutral voice" — include all simple-tier ones.

Define the interface:

- `JournalQuestionSeed`: id (string), text (string), category ('simple')
  - No minLevel field needed since all are Level 1 simple
  - No brand field needed since El Templo is one unified brand

Export `JOURNAL_QUESTION_SEEDS` as `readonly JournalQuestionSeed[]`.

Use `as const satisfies readonly JournalQuestionSeed[]` for type safety.
</action>
<verify>
<automated>cd /home/franco/projects/el-templo/el-templo-api && npx tsc --noEmit src/modules/lifestyle/seed/journal-questions.seed.ts 2>&1 | head -20</automated>
</verify>
<done>journal-questions.seed.ts exists with JournalQuestionSeed type and JOURNAL_QUESTION_SEEDS array containing all simple-tier journal questions with Arete-specific ones adapted to neutral language, TypeScript compiles cleanly</done>
</task>

</tasks>

<verification>
- Both files compile with `npx tsc --noEmit`
- No `any` types used
- No brand references remain: grep for 'Aurea Virtus', 'Arete' (as brand, not person name), 'AURUM', 'alquimi' in the seed files should return no results
- Exported arrays have correct lengths (~15-17 habits, ~14-19 journal questions)
</verification>

<success_criteria>

1. habits.seed.ts contains all Level 1-2 habits with full details, brand-adapted
2. journal-questions.seed.ts contains all simple-tier questions, brand-adapted
3. Both files use strict TypeScript types with no `any`
4. All brand terms mapped per CONTEXT.md decisions
5. Argentine Spanish preserved throughout
   </success_criteria>

<output>
After completion, create `.planning/phases/46-lifestyle-content-extraction/46-01-SUMMARY.md`
</output>
