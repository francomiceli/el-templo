---
phase: 178-bloque-alternativo-en-t-cnica-combos-dos-deuteros-en-pantall
plan: 05
type: execute
wave: 2
depends_on: ["178-02"]
files_modified:
  - el-templo-admin/src/utils/pdf/session-data-transformer.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "El PDF de planis de un día combos incluye una página del bloque COMBOS_II_ALT después de COMBOS_II"
    - "El PDF de planis de un día técnica incluye una página del bloque TECNICA_II_ALT después de TECNICA_II"
    - "vue-tsc del admin compila limpio"
  artifacts:
    - path: "el-templo-admin/src/utils/pdf/session-data-transformer.ts"
      provides: "Página del bloque alt en la rama isCombosTecnica"
      contains: "COMBOS_II_ALT"
  key_links:
    - from: "el-templo-admin/src/utils/pdf/session-data-transformer.ts"
      to: "el-templo-admin/src/constants/roleLabels.ts"
      via: "ROLE_LABELS.COMBOS_II_ALT / ROLE_LABELS.TECNICA_II_ALT"
      pattern: "ROLE_LABELS.COMBOS_II_ALT"
---

<objective>
Agregar la página del bloque alternativo al PDF de planis de los días técnica/combos. El editor y las cards NO cambian (agrupan por `role+sortOrder`, el alt aparece como card propia gratis); sólo el generador de PDF necesita emitir la página nueva, porque lista los roles hardcodeados uno por uno.

Purpose: el bloque alt es un bloque físico real → tiene que imprimirse en el plani que usan los coaches, igual que COMBOS_II/TECNICA_II.
Output: el PDF de un día combos/técnica trae la página del alt entre el II y el bloque de cierre.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/franco/projects/et-bloque-alt/.planning/phases/178-bloque-alternativo-en-t-cnica-combos-dos-deuteros-en-pantall/178-DESIGN-SEED.md
@/home/franco/projects/et-bloque-alt/CLAUDE.md

<interfaces>
- `session-data-transformer.ts` rama `isCombosTecnica` (~L464-527): pushea `buildGridPage(role, label, sessionsByLevel)` por cada rol hardcodeado. Ejemplo actual:
  `const combosII = buildGridPage('COMBOS_II', ROLE_LABELS.COMBOS_II, sessionsByLevel); if (combosII) blocks.push(combosII);`
  `buildGridPage(role: string, label: string, sessionsByLevel): PdfBlockPage | null` devuelve null si no hay bloques de ese rol (día combos O técnica, nunca ambos → sólo se pushea el par presente).
- `ROLE_LABELS.COMBOS_II_ALT` / `ROLE_LABELS.TECNICA_II_ALT` existen tras el plan 02 (admin roleLabels).
- Orden de páginas actual: INITIUM → COMBOS_I → COMBOS_II → TECNICA_I → TECNICA_II → cierre FB (EPIKOS/ATHLOS) → STRETCHING.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Página del bloque alt en el PDF combos/técnica</name>
  <files>el-templo-admin/src/utils/pdf/session-data-transformer.ts</files>
  <read_first>
    - /home/franco/projects/et-bloque-alt/el-templo-admin/src/utils/pdf/session-data-transformer.ts (líneas 352-527: buildGridPage y rama isCombosTecnica)
    - /home/franco/projects/et-bloque-alt/el-templo-admin/src/constants/roleLabels.ts (labels admin tras plan 02)
    - /home/franco/projects/et-bloque-alt/.planning/phases/178-bloque-alternativo-en-t-cnica-combos-dos-deuteros-en-pantall/178-DESIGN-SEED.md (sección 4)
  </read_first>
  <action>
    En la rama `isCombosTecnica`: inmediatamente DESPUÉS del push de `combosII`, agregar `const combosIIAlt = buildGridPage('COMBOS_II_ALT', ROLE_LABELS.COMBOS_II_ALT, sessionsByLevel); if (combosIIAlt) blocks.push(combosIIAlt);`. Inmediatamente DESPUÉS del push de `tecnicaII`, agregar `const tecnicaIIAlt = buildGridPage('TECNICA_II_ALT', ROLE_LABELS.TECNICA_II_ALT, sessionsByLevel); if (tecnicaIIAlt) blocks.push(tecnicaIIAlt);`. Así el orden queda INITIUM → COMBOS_I → COMBOS_II → COMBOS_II_ALT → TECNICA_I → TECNICA_II → TECNICA_II_ALT → cierre → STRETCHING (en la práctica sólo se pushean las del modo del día porque `buildGridPage` devuelve null para el otro par). NO tocar el editor ni las cards.
  </action>
  <acceptance_criteria>
    - `grep -c "buildGridPage('COMBOS_II_ALT'" el-templo-admin/src/utils/pdf/session-data-transformer.ts` == 1 y `grep -c "buildGridPage('TECNICA_II_ALT'" ...` == 1.
    - El push del alt de combos aparece DESPUÉS del de `combosII` y ANTES de `tecnicaI` (verificar por número de línea con grep -n).
    - `cd el-templo-admin && npx vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -c 'error TS'` == 0 (CI NO typechequea el admin — correr local, foreground, timeout amplio).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Ninguna nueva. Transformación de datos client-side para el PDF.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-06 | Information Disclosure | Página nueva del PDF | accept | Mismo dato de sesión ya visible en el editor; sin exposición nueva. |
| T-178-SC | Tampering | Instalación de paquetes | accept | Este plan NO instala paquetes. No aplica gate de legitimidad. |
</threat_model>

<verification>
- `cd el-templo-admin && npx vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -c 'error TS'` == 0.
- Verificación manual (checkpoint del plan 07): generar un PDF de un día combos y uno técnica y confirmar que aparece la página del alt.
</verification>

<success_criteria>
- El PDF combos/técnica emite la página del bloque alt en el orden correcto.
- vue-tsc del admin limpio.
</success_criteria>

<output>
Crear `.planning/phases/178-bloque-alternativo-en-t-cnica-combos-dos-deuteros-en-pantall/178-05-SUMMARY.md` al terminar.
</output>
