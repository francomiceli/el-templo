---
phase: 178-bloque-alternativo-en-t-cnica-combos-dos-deuteros-en-pantall
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/migrations/0206_tv_show_alternative.sql
  - el-templo-api/src/db/schema/tv.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "La tabla tv_class_state ya no tiene las columnas deuteros_auto_rotate ni deuteros_pinned_at"
    - "La tabla tv_class_state tiene una columna show_alternative BOOLEAN NOT NULL DEFAULT FALSE"
    - "El schema Drizzle (tv.ts) refleja exactamente el estado post-migración de la tabla"
  artifacts:
    - path: "el-templo-api/src/db/migrations/0206_tv_show_alternative.sql"
      provides: "DROP de columnas de rotación + ADD de show_alternative sobre tv_class_state"
      contains: "show_alternative"
    - path: "el-templo-api/src/db/schema/tv.ts"
      provides: "tvClassState sin columnas de rotación, con showAlternative"
      contains: "showAlternative"
  key_links:
    - from: "el-templo-api/src/db/schema/tv.ts"
      to: "el-templo-api/src/db/migrations/0206_tv_show_alternative.sql"
      via: "columna show_alternative espejada SQL↔schema"
      pattern: "show_alternative"
---

<objective>
Migración de la tabla `tv_class_state`: bajar las dos columnas de la rotación automática de deuteros (`deuteros_auto_rotate`, `deuteros_pinned_at`) y agregar `show_alternative` (boolean, default false) que persiste el toggle "Ver alternativo" por sede. Reflejar el cambio en el schema Drizzle (`db/schema/tv.ts`).

Purpose: la rotación se elimina (acoplada con el 2×2) y el toggle del bloque alternativo necesita estado persistido por sede (una fila por sede, espejado por N televisores). El drop y el add van en la MISMA migración (decisión cerrada).
Output: la tabla y el schema quedan en el estado final; los planes 06 (TV backend) y 07 (TV frontend) construyen sobre `show_alternative`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/franco/projects/et-bloque-alt/.planning/phases/178-bloque-alternativo-en-t-cnica-combos-dos-deuteros-en-pantall/178-DESIGN-SEED.md
@/home/franco/projects/et-bloque-alt/CLAUDE.md
@/home/franco/projects/et-bloque-alt/.claude/skills/el-templo-db-migrations

<interfaces>
- `tv_class_state` (schema en tv.ts ~L115-168) hoy incluye:
  - `deuterosAutoRotate: boolean("deuteros_auto_rotate").default(true).notNull()` (~L159)
  - `deuterosPinnedAt: timestamp("deuteros_pinned_at", { fsp: 3 })` (~L162)
- Última migración en master/worktree: `0205_tv_deuteros_auto_rotate.sql` (la que AGREGÓ estas columnas). Formato de migración: SQL plano, sin bloque down, comentarios con `--` y SIN `;` dentro de comentarios.
- Runner: `pnpm db:migrate` (custom, trackea en tabla `_migrations`). NUNCA `drizzle-kit migrate`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar numeración y escribir la migración</name>
  <files>el-templo-api/src/db/migrations/0206_tv_show_alternative.sql</files>
  <read_first>
    - /home/franco/projects/et-bloque-alt/el-templo-api/src/db/migrations/0205_tv_deuteros_auto_rotate.sql (formato de referencia)
    - /home/franco/projects/et-bloque-alt/.claude/skills/el-templo-db-migrations (numeración a mano, trampa del `;` en comentarios, host compartido staging/prod)
    - /home/franco/projects/et-bloque-alt/.planning/phases/178-bloque-alternativo-en-t-cnica-combos-dos-deuteros-en-pantall/178-DESIGN-SEED.md (sección 6)
  </read_first>
  <action>
    PRIMERO verificar colisión de numeración AL MOMENTO DE EJECUTAR: listar `el-templo-api/src/db/migrations/` y confirmar que `0206` no está tomado por ningún tren pendiente (177/aniversarios/alta-prorrateada). Si `0206` ya existe, usar el siguiente libre y renombrar el archivo en consecuencia (ajustar `files_modified` mentalmente). El nombre esperado: `0206_tv_show_alternative.sql`.
    Contenido del SQL sobre `tv_class_state`, en un solo `ALTER TABLE`: `DROP COLUMN deuteros_auto_rotate`, `DROP COLUMN deuteros_pinned_at`, `ADD COLUMN show_alternative BOOLEAN NOT NULL DEFAULT FALSE`. Encabezado con comentarios `--` explicando: baja de la rotación automática (feature 0205 revertida por fase 178) y el nuevo toggle "Ver alternativo" persistido por sede. PROHIBIDO usar `;` dentro de los comentarios (trampa del skill de migraciones). NO agregar `session_blocks.role` a nada: es varchar(20) sin enum, el rol alt NO necesita migración de schema (es dato de runtime del generador).
  </action>
  <acceptance_criteria>
    - El archivo existe con el número libre verificado y contiene los tres cambios: `grep -c 'DROP COLUMN deuteros_auto_rotate' <archivo>` == 1, `grep -c 'DROP COLUMN deuteros_pinned_at' <archivo>` == 1, `grep -c 'show_alternative BOOLEAN NOT NULL DEFAULT FALSE' <archivo>` == 1.
    - Ningún comentario del archivo contiene `;`: `grep -n '^--.*;' <archivo>` no devuelve líneas.
    - El SQL NO menciona `session_blocks`: `grep -c 'session_blocks' <archivo>` == 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Reflejar el cambio en el schema Drizzle</name>
  <files>el-templo-api/src/db/schema/tv.ts</files>
  <read_first>
    - /home/franco/projects/et-bloque-alt/el-templo-api/src/db/schema/tv.ts (líneas 104-168: tvClassState)
  </read_first>
  <action>
    En `tvClassState`: BORRAR las columnas `deuterosAutoRotate` (~L159) y `deuterosPinnedAt` (~L162) junto con sus comentarios. AGREGAR `showAlternative: boolean("show_alternative").default(false).notNull()` con comentario que explique que persiste el toggle "Ver alternativo" del 2º bloque en días técnica/combos (una fila por sede, espejada por N televisores). Mantener `boolean` importado (ya lo está). NO correr `db:generate` (la migración se escribió a mano en la Task 1, que es la fuente de verdad — el schema sólo tiene que espejar el estado final).
  </action>
  <acceptance_criteria>
    - `grep -c 'deuteros_auto_rotate\|deuterosAutoRotate\|deuteros_pinned_at\|deuterosPinnedAt' el-templo-api/src/db/schema/tv.ts` == 0.
    - `grep -c 'show_alternative' el-templo-api/src/db/schema/tv.ts` ≥ 1 y `grep -c 'showAlternative' el-templo-api/src/db/schema/tv.ts` ≥ 1.
    - `cd el-templo-api && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c 'error TS'` — NOTA: el service.ts todavía referencia las columnas viejas hasta el plan 06, así que este typecheck puede reportar errores en `tv/service.ts`/`tv/types.ts`. Verificar que los ÚNICOS errores TS residuales sean por referencias a `deuterosAutoRotate`/`deuterosPinnedAt`/`showAlternative` en el módulo tv (se resuelven en el plan 06). Cualquier OTRO error es un fallo de este plan.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Ninguna nueva. Cambio de esquema DDL sobre una tabla interna existente.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-04 | Denial of Service | Colisión de número de migración con tren pendiente | mitigate | Verificar el máximo aplicado y trenes pendientes ANTES de fijar el número (skill de migraciones). |
| T-178-05 | Tampering | `;` en comentarios SQL rompe el runner | mitigate | Prohibido `;` en comentarios; gate de grep en acceptance. |
| T-178-SC | Tampering | Instalación de paquetes | accept | Este plan NO instala paquetes. No aplica gate de legitimidad. |
</threat_model>

<verification>
- Los gates de grep del SQL y del schema pasan.
- NO aplicar la migración contra prod/staging (host compartido). La aplicación real ocurre en el pipeline de deploy; local sólo si el ejecutor corre contra `eltemplo_test` para los tests del plan 06.
</verification>

<success_criteria>
- Migración `0206` (o siguiente libre verificado) con drop de rotación + add de show_alternative, sin `;` en comentarios.
- Schema Drizzle espejado.
</success_criteria>

<output>
Crear `.planning/phases/178-bloque-alternativo-en-t-cnica-combos-dos-deuteros-en-pantall/178-04-SUMMARY.md` al terminar.
</output>
