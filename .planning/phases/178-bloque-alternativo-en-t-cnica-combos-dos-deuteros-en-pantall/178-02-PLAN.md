---
phase: 178-bloque-alternativo-en-t-cnica-combos-dos-deuteros-en-pantall
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/shared/role-labels.ts
  - el-templo-admin/src/constants/roleLabels.ts
  - el-templo-app/src/constants/roleLabels.ts
  - el-templo-app/src/modules/training/types/session.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "El TV rotula el bloque alt como 'COMBOS II ALT' / 'TÉCNICA II ALT' (etiqueta larga)"
    - "El listado admin muestra un badge corto para el bloque alt"
    - "La member app tiene etiqueta para el bloque alt y su BlockRole incluye los roles nuevos (vue-tsc app limpio)"
  artifacts:
    - path: "el-templo-api/src/modules/shared/role-labels.ts"
      provides: "ROLE_LABELS y ROLE_BADGE_LABELS con los roles alt"
      contains: "COMBOS II ALT"
    - path: "el-templo-admin/src/constants/roleLabels.ts"
      provides: "ROLE_LABELS admin con los roles alt"
      contains: "COMBOS_II_ALT"
    - path: "el-templo-app/src/constants/roleLabels.ts"
      provides: "ROLE_LABELS app con los roles alt"
      contains: "COMBOS_II_ALT"
    - path: "el-templo-app/src/modules/training/types/session.ts"
      provides: "BlockRole del app extendido con los roles alt"
      contains: "COMBOS_II_ALT"
  key_links:
    - from: "el-templo-app/src/constants/roleLabels.ts"
      to: "el-templo-app/src/modules/training/types/session.ts"
      via: "Record<BlockRole, string>"
      pattern: "Record<BlockRole"
---

<objective>
Espejar la etiqueta del bloque alternativo en los TRES diccionarios de labels (no hay propagación automática cross-app): api (TV + badge admin), admin (PDF/editor) y member app. La app además tipa su diccionario como `Record<BlockRole, string>`, así que hay que extender también el `BlockRole` del app o vue-tsc rompe.

Purpose: los tres runtimes tienen bundles separados sin paquete compartido; una etiqueta agregada en un solo dict deja los otros dos mostrando el rol crudo. Etiqueta canónica (D-CONTEXT): "COMBOS II ALT" / "TÉCNICA II ALT" (larga TV) + badge corto admin.
Output: el bloque alt se rotula correctamente en TV, PDF, editor y member app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/franco/projects/et-bloque-alt/.planning/phases/178-bloque-alternativo-en-t-cnica-combos-dos-deuteros-en-pantall/178-DESIGN-SEED.md
@/home/franco/projects/et-bloque-alt/CLAUDE.md

<interfaces>
- api `role-labels.ts`: `ROLE_LABELS: Record<string, string>` (larga TV) y `ROLE_BADGE_LABELS: Record<string, string>` (badge corto admin). COMBOS_II → "COMBOS II"; badge COMBOS_II → "II", TECNICA_II → "II".
- admin `roleLabels.ts`: `ROLE_LABELS: Record<string, string>`. COMBOS_II → "COMBOS II", TECNICA_II → "TÉCNICA II".
- app `roleLabels.ts`: `ROLE_LABELS: Record<BlockRole, string>` — EXHAUSTIVO. `import type { BlockRole } from '../modules/training/types/session'`. COMBOS_II → "Combos II", TECNICA_II → "Técnica II".
- app `session.ts`: `export type BlockRole = ...` unión cerrada propia del app (separada de la del api).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Labels api (TV larga + badge admin)</name>
  <files>el-templo-api/src/modules/shared/role-labels.ts</files>
  <read_first>
    - /home/franco/projects/et-bloque-alt/el-templo-api/src/modules/shared/role-labels.ts (completo, 73 líneas)
    - /home/franco/projects/et-bloque-alt/.planning/phases/178-bloque-alternativo-en-t-cnica-combos-dos-deuteros-en-pantall/178-DESIGN-SEED.md (sección 3)
  </read_first>
  <action>
    En `ROLE_LABELS` (larga, TV): agregar `COMBOS_II_ALT: "COMBOS II ALT"` y `TECNICA_II_ALT: "TÉCNICA II ALT"` (junto a COMBOS_II/TECNICA_II). Respetar los acentos exactos ("TÉCNICA").
    En `ROLE_BADGE_LABELS` (corta, badge admin): agregar `COMBOS_II_ALT: "II·A"` y `TECNICA_II_ALT: "II·A"` (badge corto que distingue el alt del II; usar el punto medio "·" que ya se usa como separador en el repo, NO ";").
  </action>
  <acceptance_criteria>
    - `grep -c 'COMBOS II ALT' el-templo-api/src/modules/shared/role-labels.ts` ≥ 1 y `grep -c 'TÉCNICA II ALT'` ≥ 1.
    - `grep -c 'COMBOS_II_ALT' el-templo-api/src/modules/shared/role-labels.ts` ≥ 2 (una en ROLE_LABELS, otra en ROLE_BADGE_LABELS).
    - `cd el-templo-api && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c 'error TS'` == 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Labels admin y member app + extender BlockRole del app</name>
  <files>el-templo-admin/src/constants/roleLabels.ts, el-templo-app/src/constants/roleLabels.ts, el-templo-app/src/modules/training/types/session.ts</files>
  <read_first>
    - /home/franco/projects/et-bloque-alt/el-templo-admin/src/constants/roleLabels.ts (completo, 56 líneas)
    - /home/franco/projects/et-bloque-alt/el-templo-app/src/constants/roleLabels.ts (completo)
    - /home/franco/projects/et-bloque-alt/el-templo-app/src/modules/training/types/session.ts (líneas 12-27: union BlockRole del app)
  </read_first>
  <action>
    En admin `roleLabels.ts` `ROLE_LABELS`: agregar `COMBOS_II_ALT: 'COMBOS II ALT'` y `TECNICA_II_ALT: 'TÉCNICA II ALT'`.
    En app `session.ts`: agregar `| 'COMBOS_II_ALT'` y `| 'TECNICA_II_ALT'` a la unión `BlockRole` del app (con comentario "Phase 178: bloque alternativo del 2º bloque").
    En app `roleLabels.ts` `ROLE_LABELS` (`Record<BlockRole, string>`): agregar `COMBOS_II_ALT: 'Combos II Alt'` y `TECNICA_II_ALT: 'Técnica II Alt'` — obligatorio porque el Record es exhaustivo sobre el BlockRole del app.
  </action>
  <acceptance_criteria>
    - `grep -c 'COMBOS_II_ALT' el-templo-admin/src/constants/roleLabels.ts` ≥ 1 y `grep -c 'TECNICA_II_ALT'` ≥ 1.
    - `grep -c 'COMBOS_II_ALT' el-templo-app/src/modules/training/types/session.ts` ≥ 1.
    - `grep -c 'COMBOS_II_ALT' el-templo-app/src/constants/roleLabels.ts` ≥ 1.
    - `cd el-templo-admin && npx vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -c 'error TS'` == 0 (CI NO typechequea el admin — correr local, foreground, timeout amplio).
    - `cd el-templo-app && npx vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -c 'error TS'` == 0 (CI NO typechequea el app — correr local).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Ninguna nueva. Diccionarios de presentación estáticos.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-02 | Information Disclosure | Etiquetas visibles del bloque | accept | Sólo texto de UI, sin datos sensibles. |
| T-178-SC | Tampering | Instalación de paquetes | accept | Este plan NO instala paquetes. No aplica gate de legitimidad. |
</threat_model>

<verification>
- `cd el-templo-api && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c 'error TS'` == 0.
- `cd el-templo-admin && npx vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -c 'error TS'` == 0.
- `cd el-templo-app && npx vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -c 'error TS'` == 0.
</verification>

<success_criteria>
- Los tres diccionarios rotulan el bloque alt; el app además tiene el rol en su `BlockRole`.
- Los tres typechecks (api tsc, admin vue-tsc, app vue-tsc) pasan limpios.
</success_criteria>

<output>
Crear `.planning/phases/178-bloque-alternativo-en-t-cnica-combos-dos-deuteros-en-pantall/178-02-SUMMARY.md` al terminar.
</output>
