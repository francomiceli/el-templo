---
phase: 178-bloque-alternativo-en-t-cnica-combos-dos-deuteros-en-pantall
plan: 03
type: execute
wave: 2
depends_on: ["178-01"]
files_modified:
  - el-templo-api/src/modules/sessions/combos-generator.ts
  - el-templo-api/src/modules/sessions/tecnica-generator.ts
  - el-templo-api/test/unit/combos-generator.test.ts
  - el-templo-api/test/unit/tecnica-generator.test.ts
  - el-templo-api/test/sessions/generate-modes.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Toda sesión combos generada trae 5 bloques en orden INITIUM → COMBOS_I → COMBOS_II → COMBOS_II_ALT → (cierre FB ATHLOS/EPIKOS)"
    - "Toda sesión técnica generada trae 5 bloques en orden INITIUM → TECNICA_I → TECNICA_II → TECNICA_II_ALT → STRETCHING"
    - "El bloque alt usa el mismo pool y formato forzado que el II, pero resuelve una ruta distinta (hash incluye el rol) → sus ejercicios difieren del II"
    - "validateSession sobre una sesión combos/técnica de 5 bloques devuelve valid:true (sin errores de conteo)"
  artifacts:
    - path: "el-templo-api/src/modules/sessions/combos-generator.ts"
      provides: "assembleFixedStructureSession emite el bloque alt; generateCombosSession resuelve su ruta"
      contains: "COMBOS_II_ALT"
    - path: "el-templo-api/src/modules/sessions/tecnica-generator.ts"
      provides: "generateTecnicaSession pasa el altSpec con ruta role-inclusive"
      contains: "TECNICA_II_ALT"
  key_links:
    - from: "el-templo-api/src/modules/sessions/combos-generator.ts"
      to: "el-templo-api/src/modules/sessions/pipeline/semana-nueva-pipeline.ts"
      via: "runSemanaNuevaBlockPipeline con route del altSpec + forcedFormat"
      pattern: "runSemanaNuevaBlockPipeline"
---

<objective>
Que los generadores de combos y técnica emitan SIEMPRE un 5º bloque físico: el alternativo del 2º bloque (`COMBOS_II_ALT` / `TECNICA_II_ALT`). Reusa la misma estructura del II (mismo pool, mismo formato forzado), pero resuelve una ruta distinta vía hash que incluye el rol, de modo que sus ejercicios difieran del II. Es un bloque real, editable después.

Purpose: el alt "generado desde el arranque" (decisión cerrada con Nacho) es un bloque físico → reusa todo el editor, el PDF y el roster de la TV sin infraestructura nueva. Estructura fija técnica/combos pasa de 4 a 5 bloques.
Output: cada `generateCombosSession`/`generateTecnicaSession` devuelve una `DaySession` de 5 bloques con el alt insertado entre el II y el bloque de cierre.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/franco/projects/et-bloque-alt/.planning/phases/178-bloque-alternativo-en-t-cnica-combos-dos-deuteros-en-pantall/178-DESIGN-SEED.md
@/home/franco/projects/et-bloque-alt/CLAUDE.md

<interfaces>
- `assembleFixedStructureSession(db, week, day, levelGroup, memberLevel, sessionMode, blockSpecs: readonly [FixedStructureBlockSpec, FixedStructureBlockSpec], forcedFormat)` (combos-generator.ts ~L102-270). Hoy: INITIUM → 2 role blocks (loop) → bloque de cierre (combos: circuito FB ATHLOS/EPIKOS con `blocks[blocks.length-1]` para intensity/repsBudget; técnica: STRETCHING).
- `FixedStructureBlockSpec { readonly role: BlockRole; readonly route: string }`.
- `resolveRoutePool(pool: readonly string[], hashInput: string): string` (semana-nueva-pipeline.ts L66) — `simpleHash(hashInput) % pool.length`. La SELECCIÓN de ejercicios (stage-6) es determinista sobre la RUTA (ordena por id, no usa el rol como semilla) → para que el alt tenga ejercicios distintos, tiene que caer en una RUTA distinta a la del II.
- combos: `COMBOS_ROUTE_POOLS.COMBOS_II = GOAL_PLAN_ROUTE_MAP.tren_inferior`; routeComboII = `resolveRoutePool(COMBOS_ROUTE_POOLS.COMBOS_II, '${week}-${day}-COMBOS_II')`.
- técnica: `TECNICA_ROUTE_POOL` (unión superior+inferior deduped); sharedRoute = `resolveRoutePool(TECNICA_ROUTE_POOL, '${week}-${day}')` (rol EXCLUIDO a propósito para I y II).
- `validateSession` (session-validator.ts): duplicados cross-block y "todas las rutas iguales" son WARNINGS (no rompen `valid`); el conteo de bloques SÍ es error (arreglado a 5 en plan 01).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: assembleFixedStructureSession emite el bloque alt</name>
  <files>el-templo-api/src/modules/sessions/combos-generator.ts, el-templo-api/src/modules/sessions/tecnica-generator.ts</files>
  <read_first>
    - /home/franco/projects/et-bloque-alt/el-templo-api/src/modules/sessions/combos-generator.ts (completo, 342 líneas)
    - /home/franco/projects/et-bloque-alt/el-templo-api/src/modules/sessions/tecnica-generator.ts (completo, 92 líneas)
    - /home/franco/projects/et-bloque-alt/el-templo-api/src/modules/sessions/pipeline/semana-nueva-pipeline.ts (líneas 44-171: opciones, resolveRoutePool, runSemanaNuevaBlockPipeline)
    - /home/franco/projects/et-bloque-alt/.planning/phases/178-bloque-alternativo-en-t-cnica-combos-dos-deuteros-en-pantall/178-DESIGN-SEED.md (sección 1)
  </read_first>
  <behavior>
    - Una sesión combos generada tiene exactamente 5 bloques: [INITIUM, COMBOS_I, COMBOS_II, COMBOS_II_ALT, ATHLOS|EPIKOS].
    - Una sesión técnica generada tiene exactamente 5 bloques: [INITIUM, TECNICA_I, TECNICA_II, TECNICA_II_ALT, STRETCHING].
    - El alt aparece INMEDIATAMENTE después del II y ANTES del bloque de cierre.
    - `altBlock.route !== IIblock.route` (cuando el pool tiene ≥2 rutas) y el set de `exerciseId` del alt NO es idéntico al del II.
    - El cierre FB de combos conserva sus números actuales: intensity/repsBudget salen del bloque II (el 2º role block), NO del alt.
  </behavior>
  <action>
    Extender la firma de `assembleFixedStructureSession` con un parámetro nuevo `altSpec: FixedStructureBlockSpec` (rol alt + su ruta ya resuelta por el caller). Antes de la rama de cierre (`if (sessionMode === "combos")`): capturar la referencia al 2º role block (el II, hoy `blocks[blocks.length - 1]`) en una variable `secondRoleBlock` para preservar el cálculo del cierre FB. Luego generar el bloque alt con `runSemanaNuevaBlockPipeline(createInitialContext(week, day, levelGroup, memberLevel, altSpec.role), spomService, db, { route: altSpec.route, forcedFormat })`, pushearlo a `blocks`, y emitir una traza `${traceCodePrefix}_ALT_GENERATED` análoga a `${traceCodePrefix}_BLOCK_GENERATED`. En la rama de cierre de combos, usar `secondRoleBlock.intensity`/`secondRoleBlock.repsBudget` en lugar de `blocks[blocks.length - 1]` (que ahora es el alt).
    En `generateCombosSession`: resolver la ruta del alt desde `COMBOS_ROUTE_POOLS.COMBOS_II` con hash que INCLUYE el rol: `resolveRoutePool(COMBOS_ROUTE_POOLS.COMBOS_II, '${week}-${day}-COMBOS_II_ALT')`. Si esa ruta coincide con `routeComboII` y el pool tiene ≥2 rutas, correrla determinísticamente al siguiente índice del pool (garantiza ejercicios distintos). Pasar `altSpec = { role: "COMBOS_II_ALT", route: <ruta alt> }` como nuevo argumento.
    En `generateTecnicaSession`: resolver la ruta del alt desde `TECNICA_ROUTE_POOL` con hash que INCLUYE el rol: `resolveRoutePool(TECNICA_ROUTE_POOL, '${week}-${day}-TECNICA_II_ALT')`. Si coincide con `sharedRoute` y el pool tiene ≥2 rutas, correrla al siguiente índice. Pasar `altSpec = { role: "TECNICA_II_ALT", route: <ruta alt> }`. Documentar en comentario que el alt SÍ incluye el rol en el hash (a diferencia de I/II que lo excluyen) para caer en ruta distinta.
    Actualizar los docblocks de ambos generadores para reflejar la estructura de 5 bloques.
  </action>
  <acceptance_criteria>
    - `grep -c 'COMBOS_II_ALT' el-templo-api/src/modules/sessions/combos-generator.ts` ≥ 1 y `grep -c 'TECNICA_II_ALT' el-templo-api/src/modules/sessions/tecnica-generator.ts` ≥ 1.
    - `grep -n 'altSpec' el-templo-api/src/modules/sessions/combos-generator.ts` devuelve ≥ 1 línea (parámetro nuevo presente).
    - `grep -n 'secondRoleBlock' el-templo-api/src/modules/sessions/combos-generator.ts` devuelve ≥ 1 línea (cierre FB preserva números del II).
    - `cd el-templo-api && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c 'error TS'` == 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Tests de generadores (unit + integración de modos)</name>
  <files>el-templo-api/test/unit/combos-generator.test.ts, el-templo-api/test/unit/tecnica-generator.test.ts, el-templo-api/test/sessions/generate-modes.test.ts</files>
  <read_first>
    - /home/franco/projects/et-bloque-alt/el-templo-api/test/unit/combos-generator.test.ts (patrón de setup + aserciones existentes)
    - /home/franco/projects/et-bloque-alt/el-templo-api/test/unit/tecnica-generator.test.ts
    - /home/franco/projects/et-bloque-alt/el-templo-api/test/sessions/generate-modes.test.ts
    - /home/franco/projects/et-bloque-alt/el-templo-api/test/helpers.ts (utilidades de auth/DB de test)
  </read_first>
  <action>
    Extender los tests siguiendo el patrón existente de cada archivo (no reinventar el setup de DB). En los unit tests de combos y técnica: agregar aserciones de que la sesión generada tiene 5 bloques; que el orden de roles es INITIUM → *_I → *_II → *_II_ALT → cierre; que el bloque alt existe con el rol correcto; que `altBlock.route !== secondBlock.route` (cuando aplica) y que el set de `exerciseId` del alt NO es idéntico al del II (comparar como sets). En `generate-modes.test.ts` (integración): que un día combos y un día técnica traen el bloque alt en su respectivo modo, y que `validateSession(session).valid === true`.
    NUNCA hardcodear ids de la tabla `users` — resolver por email (`admin@test.com`) si hace falta auth (regla del repo).
  </action>
  <acceptance_criteria>
    - `cd el-templo-api && npx vitest run test/unit/combos-generator.test.ts test/unit/tecnica-generator.test.ts --reporter=dot` pasa (foreground, timeout amplio, PROHIBIDO run_in_background).
    - `cd el-templo-api && npx vitest run test/sessions/generate-modes.test.ts --reporter=dot` pasa.
    - Los tests nuevos asertan explícitamente `blocks.length === 5` para combos y técnica (`grep -c '5' ...` no aplica; verificar leyendo que la aserción existe).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Ninguna nueva. Generación determinista server-side; sin entrada externa nueva.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-03 | Tampering | Colisión de ruta alt vs II | mitigate | Shift determinista al siguiente índice del pool si la ruta del alt coincide con la del II. |
| T-178-SC | Tampering | Instalación de paquetes | accept | Este plan NO instala paquetes. No aplica gate de legitimidad. |
</threat_model>

<verification>
- `cd el-templo-api && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c 'error TS'` == 0.
- Tests de generadores y de modos en foreground, verdes.
- Generar (o testear) una semana combos y una técnica → 5 bloques con el alt y ejercicios distintos del II.
</verification>

<success_criteria>
- Ambos generadores emiten el 5º bloque alt con ruta/ejercicios distintos del II.
- `validateSession` acepta las sesiones de 5 bloques.
- Suite de generadores y modos verde.
</success_criteria>

<output>
Crear `.planning/phases/178-bloque-alternativo-en-t-cnica-combos-dos-deuteros-en-pantall/178-03-SUMMARY.md` al terminar.
</output>
