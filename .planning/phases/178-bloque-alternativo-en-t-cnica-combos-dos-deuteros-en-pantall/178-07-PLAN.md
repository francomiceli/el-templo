---
phase: 178-bloque-alternativo-en-t-cnica-combos-dos-deuteros-en-pantall
plan: 07
type: execute
wave: 4
depends_on: ["178-06"]
files_modified:
  - el-templo-admin/src/composables/useTvApi.ts
  - el-templo-admin/src/pages/TvControlPage.vue
  - el-templo-admin/src/pages/TvScreenPage.vue
  - el-templo-admin/src/tv/render.ts
autonomous: false
requirements: []

must_haves:
  truths:
    - "La pantalla del TV muestra los deuteros regulares como grilla 2×2 legible (4 listas) en vez de rotarlos"
    - "El control del profe tiene un botón 'Ver alternativo' (en días técnica/combos) que swapea la pantalla al bloque alt compartiendo cronómetro"
    - "El toggle de rotación de deuteros ya no existe en el control ni en los tipos del front"
    - "vue-tsc del admin compila limpio"
  artifacts:
    - path: "el-templo-admin/src/composables/useTvApi.ts"
      provides: "TvControlState/TvStateWrite con showAlternative, sin campos de rotación"
      contains: "showAlternative"
    - path: "el-templo-admin/src/pages/TvControlPage.vue"
      provides: "Botón 'Ver alternativo', sin toggle de rotación"
      contains: "showAlternative"
    - path: "el-templo-admin/src/pages/TvScreenPage.vue"
      provides: "Grilla 2×2 para 4 columnas de deuteros + escala/tipografía"
      contains: "stage"
    - path: "el-templo-admin/src/tv/render.ts"
      provides: "paintList marca el layout según cantidad de columnas"
      contains: "columns.length"
  key_links:
    - from: "el-templo-admin/src/pages/TvControlPage.vue"
      to: "el-templo-admin/src/composables/useTvApi.ts"
      via: "send({ showAlternative })"
      pattern: "showAlternative"
---

<objective>
Front de la TV: (1) pintar los cuatro listados de deuteros como grilla 2×2 legible en el TV real (rediseño de escala/tipografía); (2) reemplazar el toggle de rotación por el botón "Ver alternativo" en el control del profe; (3) sacar del front todo rastro de la rotación (tipos + botón). El backend (plan 06) ya emite hasta 4 columnas y el swap del alt; acá sólo se pinta y se manda el comando.

Purpose: cerrar la feature en la superficie que ven el profe (control) y la sucursal (TV). El 2×2 necesita layout y tipografía nuevos porque hasta hoy la zona era de 1-2 columnas.
Output: TV con deuteros 2×2, control con "Ver alternativo", cero rotación.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/franco/projects/et-bloque-alt/.planning/phases/178-bloque-alternativo-en-t-cnica-combos-dos-deuteros-en-pantall/178-DESIGN-SEED.md
@/home/franco/projects/et-bloque-alt/CLAUDE.md

<interfaces>
- `useTvApi.ts` (espejo del contrato del API): `TvControlState` (~L78-87) con `deuterosAutoRotate`/`deuterosPinnedAt`; `TvStateWrite` (~L114-123) con `deuterosAutoRotate?`. `TvControlContext` expone `mode: TvClassMode` y `blocks`.
- `TvControlPage.vue`: botón de rotación (~L211-227) con `@click="onToggleDeuteros"`; computed `deuterosAutoRotate` (~L662-664); `onToggleDeuteros` (~L882-884) hace `send({ deuterosAutoRotate: !... })`. `currentBlock` (role/shared) y `canControl` disponibles.
- `TvScreenPage.vue`: `#tvScreenRoot .stage` (~L771-777) es `display: flex` (1-2 columnas 50/50); `.lista-col` (~L784-786) `flex: 1 1 0`. `.cabCol` (~L790+) header nivel/ruta.
- `render.ts` `paintList` (~L460-527): crea N `.lista-col` dentro de `n.stage` a partir de `c.columns` (ya soporta N genérico); `COMPACT_OVER = 5` (~L53) achica la tipografía de listas largas. `n.stage` es el contenedor.
- El backend emite 4 columnas de deuteros con headers prefijados por "DEUTEROS I/II".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contrato del front + botón "Ver alternativo" (sacar rotación)</name>
  <files>el-templo-admin/src/composables/useTvApi.ts, el-templo-admin/src/pages/TvControlPage.vue</files>
  <read_first>
    - /home/franco/projects/et-bloque-alt/el-templo-admin/src/composables/useTvApi.ts (líneas 70-125)
    - /home/franco/projects/et-bloque-alt/el-templo-admin/src/pages/TvControlPage.vue (líneas 205-230; 655-666; 872-889)
    - /home/franco/projects/et-bloque-alt/.planning/phases/178-bloque-alternativo-en-t-cnica-combos-dos-deuteros-en-pantall/178-DESIGN-SEED.md (sección 5)
  </read_first>
  <action>
    En `useTvApi.ts`: en `TvControlState` reemplazar `deuterosAutoRotate`/`deuterosPinnedAt` por `showAlternative: boolean`. En `TvStateWrite` reemplazar `deuterosAutoRotate?` por `showAlternative?: boolean`.
    En `TvControlPage.vue`: reemplazar el computed `deuterosAutoRotate` por `showAlternative` (`() => context.value?.state?.showAlternative === true`). Reemplazar `onToggleDeuteros` por `onToggleAlternative` que hace `void send({ showAlternative: !showAlternative.value })`. Reemplazar el bloque del botón (~L214-227): botón "Ver alternativo" que se muestra SÓLO en días técnica/combos (`context.value?.mode === 'combos' || context.value?.mode === 'tecnica'`) y `:disable="!canControl"`; label `showAlternative ? 'VIENDO ALTERNATIVO' : 'VER ALTERNATIVO'`, icono `swap_horiz`, color según estado (mismo patrón visual que el botón viejo). Comentario nuevo explicando que swapea la vista del 2º bloque compartiendo cronómetro (manejado por el profe).
  </action>
  <acceptance_criteria>
    - `grep -rc 'deuterosAutoRotate\|deuterosPinnedAt\|onToggleDeuteros\|ROTACIÓN DEUTEROS' el-templo-admin/src/composables/useTvApi.ts el-templo-admin/src/pages/TvControlPage.vue` == 0 en ambos.
    - `grep -c 'showAlternative' el-templo-admin/src/composables/useTvApi.ts` ≥ 2 y `grep -c 'showAlternative' el-templo-admin/src/pages/TvControlPage.vue` ≥ 2.
    - `grep -c 'VER ALTERNATIVO' el-templo-admin/src/pages/TvControlPage.vue` ≥ 1.
    - `cd el-templo-admin && npx vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -c 'error TS'` == 0 (con Task 1 y 2 completas).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Grilla 2×2 de deuteros en la pantalla del TV</name>
  <files>el-templo-admin/src/tv/render.ts, el-templo-admin/src/pages/TvScreenPage.vue</files>
  <read_first>
    - /home/franco/projects/et-bloque-alt/el-templo-admin/src/tv/render.ts (líneas 50-55; 456-527: paintList)
    - /home/franco/projects/et-bloque-alt/el-templo-admin/src/pages/TvScreenPage.vue (líneas 766-800; y 1170-1200 para el media query .item)
    - /home/franco/projects/et-bloque-alt/.planning/phases/178-bloque-alternativo-en-t-cnica-combos-dos-deuteros-en-pantall/178-DESIGN-SEED.md (sección 5)
  </read_first>
  <action>
    En `render.ts` `paintList`: después de `clear(n.stage)`, marcar el layout según la cantidad de columnas — setear un atributo/clase en `n.stage` que refleje `c.columns.length` (ej. `n.stage.setAttribute('data-cols', String(c.columns.length))`). No cambiar la generación de `.lista-col` (ya es genérica para N).
    En `TvScreenPage.vue`: cambiar `#tvScreenRoot .stage` para soportar 1, 2 y 4 columnas. Con 4 columnas → grilla 2×2 (`display: grid; grid-template-columns: repeat(2, 1fr); grid-auto-rows: 1fr; gap`), con 1-2 columnas conservar el layout actual (fila). Usar el selector `#tvScreenRoot .stage[data-cols="4"]` (y opcionalmente `[data-cols="3"]`) para el modo grilla; el default (1-2) queda flex como hoy. Ajustar escala/tipografía para el caso 2×2: reducir el `font-size` de `.cabCol` y de los items (`.lista-col .item`) en el modo `[data-cols="4"]` para que las 4 listas entren legibles en el TV (objetivo: legible en el televisor real; el checkpoint humano lo valida). Considerar bajar el umbral efectivo de compactado en 2×2 si hiciera falta. NO tocar la lógica de `render.ts` más allá del atributo de layout.
  </action>
  <acceptance_criteria>
    - `grep -c "data-cols" el-templo-admin/src/tv/render.ts` ≥ 1 y `grep -c "data-cols" el-templo-admin/src/pages/TvScreenPage.vue` ≥ 1.
    - `grep -c "grid-template-columns: repeat(2" el-templo-admin/src/pages/TvScreenPage.vue` ≥ 1 (modo 2×2).
    - `cd el-templo-admin && npx vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -c 'error TS'` == 0.
    - `cd el-templo-admin && npx eslint -c ./eslint.config.js src/tv/render.ts src/pages/TvScreenPage.vue src/pages/TvControlPage.vue src/composables/useTvApi.ts 2>&1 | grep -c 'error'` == 0.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Backend (plan 06) + front (este plan) del bloque alternativo, el 2×2 de deuteros y la baja de la rotación. Todo automatizado y typecheckeado; falta la validación visual en el TV real y el flujo end-to-end, que sólo un humano puede confirmar.
  </what-built>
  <how-to-verify>
    1. Levantar api + admin localmente (o apuntar a un entorno con datos). Generar una semana combos y una técnica → confirmar en el editor que aparece la card del bloque alt (COMBOS II ALT / TÉCNICA II ALT) con ejercicios distintos del II, y que se puede editar/swappear como cualquier bloque.
    2. Generar el PDF de un día combos y uno técnica → confirmar que trae la página del bloque alt.
    3. Abrir la pantalla del TV en un día REGULAR con bloque deuteros activo → confirmar que se ven los DOS deuteros (I y II) en grilla 2×2, legibles en el televisor.
    4. En un día combos/técnica, desde el control del profe: tocar "Ver alternativo" con el 2º bloque activo → la pantalla cambia al bloque alt SIN reiniciar el cronómetro; volver a tocar → vuelve al principal, mismo timer.
    5. Confirmar que ya NO existe el botón de rotación de deuteros y que la pantalla no rota sola.
  </how-to-verify>
  <resume-signal>Escribí "aprobado" o describí los problemas visuales/funcionales encontrados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Ninguna nueva del lado del front (consume el mismo endpoint de control ya autenticado).

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-09 | Tampering | Comando showAlternative desde el control | mitigate | El backend valida el campo (schema, plan 06); el front sólo envía booleano. |
| T-178-SC | Tampering | Instalación de paquetes | accept | Este plan NO instala paquetes. No aplica gate de legitimidad. |
</threat_model>

<verification>
- `cd el-templo-admin && npx vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -c 'error TS'` == 0 (CI NO typechequea el admin — correr local, foreground).
- `cd el-templo-admin && npx eslint -c ./eslint.config.js src/tv/render.ts src/pages/TvScreenPage.vue src/pages/TvControlPage.vue src/composables/useTvApi.ts` sin errores.
- Checkpoint humano aprobado (visual TV + flujo end-to-end).
</verification>

<success_criteria>
- Deuteros regular en 2×2 legible; botón "Ver alternativo" funcional con cronómetro compartido; rotación erradicada del front.
- vue-tsc y eslint del admin limpios; checkpoint humano aprobado.
</success_criteria>

<output>
Crear `.planning/phases/178-bloque-alternativo-en-t-cnica-combos-dos-deuteros-en-pantall/178-07-SUMMARY.md` al terminar.
</output>
