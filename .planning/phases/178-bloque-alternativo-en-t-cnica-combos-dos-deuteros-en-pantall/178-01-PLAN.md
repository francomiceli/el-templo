---
phase: 178-bloque-alternativo-en-t-cnica-combos-dos-deuteros-en-pantall
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/sessions/types.ts
  - el-templo-api/src/modules/sessions/validators/block-validator.ts
  - el-templo-api/src/modules/sessions/validators/session-validator.ts
  - el-templo-api/src/modules/sessions/pipeline/stage-5-format.ts
  - el-templo-api/src/modules/admin/edit-service.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "El tipo BlockRole del api incluye COMBOS_II_ALT y TECNICA_II_ALT"
    - "el-templo-api typecheckea limpio con TODOS los mapped types exhaustivos completos para los dos roles nuevos"
    - "validateSession acepta una sesion combos/tecnica de 5 bloques sin error de conteo, y sigue exigiendo 4 en ROM"
    - "El editor de sesiones ofrece formatos compatibles para el bloque alt (blockMap → 'nucleus', igual que II)"
  artifacts:
    - path: "el-templo-api/src/modules/sessions/types.ts"
      provides: "BlockRole union con COMBOS_II_ALT y TECNICA_II_ALT"
      contains: "COMBOS_II_ALT"
    - path: "el-templo-api/src/modules/sessions/validators/block-validator.ts"
      provides: "FORMAT_COMPATIBILITY con entradas para los roles alt"
      contains: "COMBOS_II_ALT"
    - path: "el-templo-api/src/modules/sessions/validators/session-validator.ts"
      provides: "INTENSITY_RANGES con roles alt + conteo de bloques 4→5 para combos/tecnica"
      contains: "TECNICA_II_ALT"
    - path: "el-templo-api/src/modules/sessions/pipeline/stage-5-format.ts"
      provides: "roleToBlock exhaustivo para los roles alt (fallback muerto)"
      contains: "COMBOS_II_ALT"
    - path: "el-templo-api/src/modules/admin/edit-service.ts"
      provides: "blockMap de getCompatibleFormats mapea alt → nucleus"
      contains: "COMBOS_II_ALT"
  key_links: []
---

<objective>
Fundación de tipos y validadores del api para el bloque alternativo. Agregar los dos roles nuevos (`COMBOS_II_ALT`, `TECNICA_II_ALT`) a la unión cerrada `BlockRole` y completar TODOS los mapped types que TypeScript obliga (compatibilidad de formato, rangos de intensidad, `roleToBlock`, `blockMap` del editor), más ajustar el conteo de bloques fijos de 4 a 5 para combos/técnica.

Purpose: sin esta base ningún otro plan del api compila — la unión cerrada `BlockRole` fuerza a completar los diccionarios exhaustivos, y el generador (plan 03) y la TV (plan 06) no pueden referenciar los roles nuevos hasta que existan en el tipo.
Output: `el-templo-api` typecheckea limpio con los dos roles nuevos completamente integrados en el sistema de validación.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/franco/projects/et-bloque-alt/.planning/phases/178-bloque-alternativo-en-t-cnica-combos-dos-deuteros-en-pantall/178-DESIGN-SEED.md
@/home/franco/projects/et-bloque-alt/.planning/phases/178-bloque-alternativo-en-t-cnica-combos-dos-deuteros-en-pantall/178-CONTEXT.md
@/home/franco/projects/et-bloque-alt/CLAUDE.md

<interfaces>
BlockRole hoy (types.ts ~L43-58) es una unión cerrada de 14 strings; termina en "STRETCHING".
Los diccionarios exhaustivos que la referencian y que TS obliga a completar:
- block-validator.ts: `const FORMAT_COMPATIBILITY: Record<BlockRole, readonly string[]>` (~L20-57).
- session-validator.ts: `const INTENSITY_RANGES: Record<BlockRole, { min: number; max: number }>` (~L25-43), y `isFixedStructureSession`/conteo (~L66-87), hoy exige `session.blocks.length !== 4` para fixed-structure.
- stage-5-format.ts: `function roleToBlock(role: BlockRole): "initium"|"nucleus"|"deuteros"|"athlos"|"epikos"` con `switch` exhaustivo (~L25-54).
edit-service.ts `getCompatibleFormats`: `const blockMap: Record<string, string>` (~L661-676) — NO es exhaustivo (Record<string,string>), pero hay que agregar las entradas a mano.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar los dos roles a BlockRole y completar compatibilidad/intensidad</name>
  <files>el-templo-api/src/modules/sessions/types.ts, el-templo-api/src/modules/sessions/validators/block-validator.ts, el-templo-api/src/modules/sessions/validators/session-validator.ts</files>
  <read_first>
    - /home/franco/projects/et-bloque-alt/el-templo-api/src/modules/sessions/types.ts (líneas 37-58: docblock + union BlockRole)
    - /home/franco/projects/et-bloque-alt/el-templo-api/src/modules/sessions/validators/block-validator.ts (líneas 19-57: FORMAT_COMPATIBILITY)
    - /home/franco/projects/et-bloque-alt/el-templo-api/src/modules/sessions/validators/session-validator.ts (líneas 20-87: MIN/MAX_BLOCKS, INTENSITY_RANGES, isFixedStructureSession y Check 1)
    - /home/franco/projects/et-bloque-alt/.planning/phases/178-bloque-alternativo-en-t-cnica-combos-dos-deuteros-en-pantall/178-DESIGN-SEED.md (sección 2)
  </read_first>
  <action>
    En `types.ts` agregar a la unión `BlockRole` las dos entradas `"COMBOS_II_ALT"` y `"TECNICA_II_ALT"` (ubicarlas junto a `COMBOS_II` y `TECNICA_II` respectivamente), con comentario "Phase 178: variante alternativa del 2º bloque, mismo momento/ventana". Actualizar el docblock de arriba del union para reflejar que combos/técnica ahora son 5 bloques.
    En `block-validator.ts` `FORMAT_COMPATIBILITY`: agregar `COMBOS_II_ALT` con el MISMO array que `COMBOS_II` (`["Combos"]`) y `TECNICA_II_ALT` con el MISMO que `TECNICA_II` (`["For Quality", "Cluster", "Accumulate X"]`).
    En `session-validator.ts` `INTENSITY_RANGES`: agregar `COMBOS_II_ALT` y `TECNICA_II_ALT` con `{ min: 30, max: 70 }` (igual que II). Además, en `isFixedStructureSession`/Check 1 (~L66-87): hoy toda sesión fixed-structure exige exactamente 4 bloques. Cambiarlo para que ROM (mode 'rom' o roles `ROM_`) siga exigiendo 4, pero combos/técnica exijan 5. Concretamente: derivar `expectedFixedBlocks = (session.sessionMode === "rom" || session.blocks.some((b) => b.role.startsWith("ROM_"))) ? 4 : 5` y comparar contra ese valor en la rama fixed-structure. Actualizar el mensaje de error para que reporte el número esperado dinámico. NO tocar `MIN_BLOCKS`/`MAX_BLOCKS` (regular sigue 4-5).
  </action>
  <acceptance_criteria>
    - `grep -c 'COMBOS_II_ALT' el-templo-api/src/modules/sessions/types.ts` ≥ 1 y `grep -c 'TECNICA_II_ALT' .../types.ts` ≥ 1.
    - `grep -v '^\s*//' el-templo-api/src/modules/sessions/validators/block-validator.ts | grep -c 'COMBOS_II_ALT'` ≥ 1 (entrada real, no comentario).
    - `grep -v '^\s*//' el-templo-api/src/modules/sessions/validators/session-validator.ts | grep -c 'II_ALT'` ≥ 2.
    - En session-validator la rama fixed-structure ya NO contiene el literal `!== 4` sin ramificar por ROM: `grep -n 'expectedFixedBlocks' session-validator.ts` devuelve ≥ 1 línea.
    - `cd el-templo-api && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c 'error TS'` == 0 (con las tareas 1 y 2 completas).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Completar roleToBlock (stage 5) y blockMap del editor</name>
  <files>el-templo-api/src/modules/sessions/pipeline/stage-5-format.ts, el-templo-api/src/modules/admin/edit-service.ts</files>
  <read_first>
    - /home/franco/projects/et-bloque-alt/el-templo-api/src/modules/sessions/pipeline/stage-5-format.ts (líneas 24-54: roleToBlock switch exhaustivo)
    - /home/franco/projects/et-bloque-alt/el-templo-api/src/modules/admin/edit-service.ts (líneas 657-676: getCompatibleFormats blockMap)
    - /home/franco/projects/et-bloque-alt/.planning/phases/178-bloque-alternativo-en-t-cnica-combos-dos-deuteros-en-pantall/178-DESIGN-SEED.md (sección 2 y 4)
  </read_first>
  <action>
    En `stage-5-format.ts` `roleToBlock`: agregar los `case "COMBOS_II_ALT":` y `case "TECNICA_II_ALT":` al bloque de casos que hoy agrupa `COMBOS_I/II`, `TECNICA_I/II`, `STRETCHING` y retorna `"initium"` como fallback muerto (los roles alt bypassean stage 5 vía `forcedFormat` igual que el resto de combos/técnica, pero el `switch` exhaustivo lo exige para compilar). Mantener el comentario que explica que nunca se ejecuta para estos roles.
    En `edit-service.ts` `getCompatibleFormats` `blockMap`: agregar `COMBOS_II_ALT: "nucleus"` y `TECNICA_II_ALT: "nucleus"` junto a las entradas existentes de combos/técnica (mismo mapeo a la familia 'nucleus' de format_compatibility que II — así el selector de formato del editor funciona para el bloque alt).
  </action>
  <acceptance_criteria>
    - `grep -c 'COMBOS_II_ALT' el-templo-api/src/modules/sessions/pipeline/stage-5-format.ts` ≥ 1 y `grep -c 'TECNICA_II_ALT'` ≥ 1.
    - `grep -v '^\s*//' el-templo-api/src/modules/admin/edit-service.ts | grep -c 'II_ALT.*nucleus\|II_ALT"'` ≥ 1 (ambas entradas presentes en blockMap).
    - `cd el-templo-api && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c 'error TS'` == 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Ninguna nueva. Este plan es puramente de tipos/validación interna del api; no cruza cliente→API ni agrega entrada externa.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-01 | Tampering | Conteo de bloques en validateSession | accept | El conteo es defensivo (warning/error de coherencia), no un control de seguridad; entrada ya autenticada (staff). |
| T-178-SC | Tampering | Instalación de paquetes | accept | Este plan NO instala paquetes (solo edita fuentes existentes). No aplica gate de legitimidad. |
</threat_model>

<verification>
- `cd el-templo-api && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c 'error TS'` == 0.
- Correr los tests de validadores existentes en foreground con timeout amplio: `cd el-templo-api && npx vitest run test/unit --reporter=dot` (si el directorio tiene tests de validadores) — deben pasar. PROHIBIDO `run_in_background`.
</verification>

<success_criteria>
- Los dos roles alt existen en `BlockRole` y en los cuatro diccionarios exhaustivos/semiexhaustivos.
- `el-templo-api` typecheckea sin errores.
- Una sesión combos/técnica de 5 bloques pasa el conteo de `validateSession`; ROM de 4 sigue pasando.
</success_criteria>

<output>
Crear `.planning/phases/178-bloque-alternativo-en-t-cnica-combos-dos-deuteros-en-pantall/178-01-SUMMARY.md` al terminar.
</output>
