---
phase: 182-plataforma-rol-super-owner-wizard-de-alta-de-tenant
plan: 07
type: execute
wave: 5
depends_on: [182-04, 182-05]
files_modified:
  - el-templo-api/test/tenancy/iso-04-platform.test.ts
  - el-templo-api/test/tenancy/con-04-crons-per-tenant.test.ts
  - el-templo-api/test/fixtures/platform.ts
autonomous: true
requirements: [PLAT-01, PLAT-02, PLAT-03]
must_haves:
  truths:
    - "Un gimnasio creado por `provisionTenant()` real queda aislado: su owner no ve datos de El Templo y El Templo no ve los suyos (D-16, criterio de éxito 4)"
    - "El super-owner recibe 401/403 en rutas de tenant y un usuario de tenant recibe 401 en `/api/platform/*` (D-09, D-16)"
    - "El `Origin` del slug nuevo resuelve el tenant y un slug desconocido devuelve 404 `TENANT_NOT_FOUND` (D-04, D-16)"
    - "Las baterías iso-01, iso-02 e iso-03 siguen verdes (D-16)"
    - "Un tenant recién aprovisionado no produce efectos en los crons `forEachActiveTenant` (D-17, Pitfall 9 del research)"
  artifacts:
    - path: "el-templo-api/test/tenancy/iso-04-platform.test.ts"
      provides: "Batería de aislamiento de la capa de plataforma, con caso y control por aserción"
      min_lines: 200
  key_links:
    - from: "el-templo-api/test/tenancy/iso-04-platform.test.ts"
      to: "provisionTenant"
      via: "creación del gimnasio de prueba con el servicio real, no con seedSecondTenant"
      pattern: "provisionTenant\\("
---

<objective>
Escribir la batería `iso-04` que prueba el aislamiento de la capa de plataforma usando un gimnasio
creado por el `provisionTenant()` **real** (no el fixture manual `seedSecondTenant`), y agregar a
`con-04` el caso que verifica que un tenant recién aprovisionado no produce efectos en los crons.

Purpose: es el criterio de éxito 4 de la fase y D-16. Sin esta batería, "el tenant nuevo queda
aislado desde el minuto cero" es una afirmación, no un hecho verificado.
Output: `iso-04-platform.test.ts` con caso + control por aserción, y un caso nuevo en `con-04`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/182-plataforma-rol-super-owner-wizard-de-alta-de-tenant/182-CONTEXT.md
@.planning/phases/182-plataforma-rol-super-owner-wizard-de-alta-de-tenant/182-RESEARCH.md
@.planning/phases/182-plataforma-rol-super-owner-wizard-de-alta-de-tenant/182-PATTERNS.md
@.planning/phases/182-plataforma-rol-super-owner-wizard-de-alta-de-tenant/182-04-SUMMARY.md
@.planning/phases/182-plataforma-rol-super-owner-wizard-de-alta-de-tenant/182-05-SUMMARY.md
@CLAUDE.md
</context>

<constraints>
- **Tests SOLO en foreground**, timeout 600000 ms. PROHIBIDO `run_in_background` y loops de `pgrep`.
- `git add` explícito por ruta. Nunca `git push`.
- Sin `any`; usuarios de test resueltos por email, **jamás ids de `users` hardcodeados**.
- **No modificar `test/helpers.ts`** (`TABLES_TO_CLEAN` incluido): la limpieza de las tablas de
  plataforma vive en `test/fixtures/platform.ts`.
- El gimnasio de prueba de `iso-04` se crea con `provisionTenant()` **real** (D-16 lo exige
  literalmente): no reutilizar `seedSecondTenant`/`TENANT_DOS`.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Batería `iso-04-platform` — aislamiento de datos y de sesiones</name>
  <files>
    el-templo-api/test/tenancy/iso-04-platform.test.ts,
    el-templo-api/test/fixtures/platform.ts
  </files>
  <read_first>
    - el-templo-api/test/tenancy/iso-03-finance-coach-load.test.ts líneas 1-120 (docblock y patrón caso+control por aserción)
    - el-templo-api/test/fixtures/second-tenant.ts (estructura del fixture, limpieza por FKs)
    - el-templo-api/test/fixtures/platform.ts (creado en 182-02)
    - el-templo-api/src/modules/platform/provisioning-service.ts (creado en 182-05)
    - el-templo-api/test/helpers.ts (`createTestApp`, `getAuthToken`)
  </read_first>
  <behavior>
    Setup: un `platform_users` (super-owner), y un gimnasio nuevo creado llamando a
    `provisionTenant()` real con slug `iso04-<sufijo aleatorio>`; el owner tiene contraseña
    conocida y se loguea por `POST /api/auth/login`.

    **Aislamiento de datos (caso + control por cada uno):**
    - El owner nuevo pide un listado de alumnos (`GET /api/admin/members`, o la ruta de listado que
      exista) ⇒ 200 y **cero** filas de El Templo. Control: el mismo endpoint con el token de
      `admin@test.com` ⇒ 200 y **ninguna** fila del gimnasio nuevo.
    - El owner nuevo pide el listado de sedes disponible (`GET /api/admin/members/branches`) ⇒ 200
      con **solo** sus sedes (la virtual y, si existe, la física). Control: con el token de El
      Templo, ninguna de las sedes del gimnasio nuevo aparece.
    - El owner nuevo pide un recurso por id que pertenece a El Templo (p. ej. la ficha de un socio
      existente de El Templo, resuelto por email) ⇒ 404 o 403, **nunca** 200. Control: la ficha de
      su propio owner ⇒ 200 (distingue "aislamiento correcto" de "siembra rota").

    **Aislamiento de sesiones (D-09/D-16):**
    - Token de plataforma contra `GET /api/auth/me` ⇒ 401.
    - Token de plataforma contra una ruta de tenant con datos (`GET /api/admin/members`) ⇒ 401.
    - Token del owner nuevo contra `POST /api/platform/tenants` ⇒ 401. Control: el mismo POST con
      token de plataforma ⇒ 201.
    - Token del owner nuevo contra `GET /api/platform/auth/me` ⇒ 401.

    **Resolución pre-login (D-04/D-16):**
    - Request con `origin: https://<slug del gimnasio nuevo>.localtest.me` ⇒ no da 404.
    - Request con `origin: https://iso04-inexistente.localtest.me` ⇒ 404 con `code:
      "TENANT_NOT_FOUND"`.

    **Aprovisionamiento verificado en la base:**
    - El gimnasio nuevo tiene exactamente 5 filas `tenant_settings` con `setting_key LIKE
      'module.%'`, con `module.gimnasio.enabled = "true"`.
    - Ninguna fila del gimnasio nuevo (branches/users/tenant_settings) tiene `tenant_id = 1`.
  </behavior>
  <action>
Extender `el-templo-api/test/fixtures/platform.ts` con
`export async function seedGimnasioAprovisionado(app, opts?: { slug?: string; conSedeFisica?:
boolean }): Promise<{ tenantId: number; slug: string; ownerEmail: string; ownerPassword: string;
ownerToken: string; virtualBranchId: number; physicalBranchId: number | null }>` — llama a
`provisionTenant(app.db, app.log, {...})` con un `actorPlatformUserId` de un super-owner sembrado,
y después obtiene el token del owner con `getAuthToken`. Y
`export async function limpiarGimnasioAprovisionado(app, tenantId): Promise<void>` que borre en
orden de FKs `tenant_settings`, `users`, `branches` y `tenants` de ese `tenantId`, más
`platform_audit_log` de ese target.

Crear `el-templo-api/test/tenancy/iso-04-platform.test.ts` con:
- Docblock al estilo de `iso-03-finance-coach-load.test.ts`: qué prueba, por qué el gimnasio se
  crea con `provisionTenant()` real (D-16), y la regla de caso + control.
- `describe` por bloque de `<behavior>`, con un `it` por línea y su control donde corresponda.
- `beforeAll`: `createTestApp()`, `seedPlatformUser`, `seedGimnasioAprovisionado`.
- `afterAll`: `limpiarGimnasioAprovisionado`, `limpiarPlataforma`, `app.close()`.
- Antes de escribir los `it` de aislamiento de datos, **verificar en el código** qué rutas de
  listado existen realmente (`grep -n "admin/members" el-templo-api/test/tenant-manifest.ts`) y
  usar las rutas reales; no inventar endpoints.
- Ningún `it` debe distinguir "recurso ajeno" de "recurso inexistente" por el status: si el
  endpoint devuelve 404 para ambos, afirmarlo así y dejarlo comentado (mismo criterio D-09 de las
  iso-03).
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm test -- test/tenancy/iso-04-platform.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd el-templo-api && pnpm test -- test/tenancy/iso-04-platform.test.ts` sale con código 0 y ejecuta al menos 14 tests.
    - `grep -c 'provisionTenant(' el-templo-api/test/fixtures/platform.ts` >= 1 y `grep -c 'seedSecondTenant' el-templo-api/test/tenancy/iso-04-platform.test.ts` == 0.
    - El archivo contiene una aserción `expect(res.statusCode).toBe(401)` para el token de plataforma sobre `/api/admin/members`.
    - El archivo contiene una aserción `expect(body.code).toBe("TENANT_NOT_FOUND")`.
    - El archivo contiene una aserción de que ninguna fila del gimnasio nuevo tiene `tenant_id = 1`.
    - Cada `describe` de aislamiento de datos tiene al menos un `it` de control positivo sobre el recurso propio.
    - `git diff --name-only` NO incluye `el-templo-api/test/helpers.ts`.
  </acceptance_criteria>
  <done>Existe evidencia automatizada de que un gimnasio creado por el wizard queda aislado en datos y en sesiones desde el minuto cero.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Caso de crons sobre un tenant recién aprovisionado en `con-04`</name>
  <files>el-templo-api/test/tenancy/con-04-crons-per-tenant.test.ts</files>
  <read_first>
    - el-templo-api/test/tenancy/con-04-crons-per-tenant.test.ts (archivo completo: cómo invoca los jobs y cómo verifica efectos)
    - el-templo-api/src/modules/shared/tenant.ts (`forEachActiveTenant`, su try/catch por tenant)
    - el-templo-api/test/fixtures/platform.ts (`seedGimnasioAprovisionado`, de la Task 1)
    - .planning/phases/182-.../182-RESEARCH.md §Pitfall 9 (los crons NO están gateados por módulo; son no-op por ausencia de datos)
  </read_first>
  <behavior>
    - Con un gimnasio aprovisionado por `provisionTenant()` y **sin ningún dato** (0 socios, 0 reservas, 0 leads), correr los jobs que ya ejercita `con-04` ⇒ ninguno lanza y ninguno escribe filas para ese `tenantId`.
    - El gimnasio nuevo **sí** es iterado por `forEachActiveTenant` (no se lo saltea silenciosamente): verificarlo por el efecto observable que ya use el archivo (contador, log o spy), no por asunción.
    - Los efectos de los jobs sobre El Templo (tenant 1) siguen siendo los mismos que sin el gimnasio nuevo.
  </behavior>
  <action>
Agregar a `el-templo-api/test/tenancy/con-04-crons-per-tenant.test.ts` un `describe("tenant recien
aprovisionado (fase 182, D-17)", …)` que use `seedGimnasioAprovisionado` y corra los mismos jobs
que el resto del archivo, afirmando los tres puntos de `<behavior>`.

Escribir en un comentario del `describe` el hecho verificado por el research, para que nadie lo
"arregle" por error: **ningún job de `src/jobs/` consulta flags de módulo** — el tenant nuevo es
no-op por **ausencia de datos**, no por el flag `module.templo-*.enabled = "false"`. Y
`forEachActiveTenant` aísla errores por tenant con try/catch, así que un gimnasio roto no frena a
los demás.

Limpiar el gimnasio de prueba en el `afterAll` del `describe` con
`limpiarGimnasioAprovisionado`.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm test -- test/tenancy/con-04-crons-per-tenant.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd el-templo-api && pnpm test -- test/tenancy/con-04-crons-per-tenant.test.ts` sale con código 0.
    - El archivo contiene el `describe` nuevo con la referencia a D-17 y el comentario sobre "no-op por ausencia de datos, no por el flag".
    - El nuevo bloque usa `seedGimnasioAprovisionado` y limpia con `limpiarGimnasioAprovisionado`.
    - Los `it` preexistentes del archivo siguen pasando (el conteo total de tests del archivo sube, ninguno baja).
  </acceptance_criteria>
  <done>Queda verificado —no supuesto— que un gimnasio recién dado de alta no genera efectos en los 7 crons por tenant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant nuevo ↔ El Templo | Frontera de aislamiento de datos que esta batería verifica |
| sesión de plataforma ↔ sesión de tenant | Frontera de privilegio verificada en ambos sentidos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-06b | Information Disclosure | Fuga de datos entre gimnasios | mitigate | `iso-04` con caso **y** control por aserción: el control positivo sobre el recurso propio impide que un fixture roto se lea como "aislamiento correcto" |
| T-182-01b | Elevation of Privilege | Cruce de sesiones plataforma↔tenant | mitigate | Cuatro aserciones cruzadas (token de plataforma en 2 rutas de tenant, token de tenant en 2 rutas de plataforma), todas esperando 401 |
| T-182-04b | Information Disclosure | Enumeración de gimnasios por `Origin` | mitigate | Aserción de 404 `TENANT_NOT_FOUND` con un slug inexistente |
| T-182-05b | Tampering | Filas del alta estampadas en El Templo | mitigate | Aserción explícita de que ninguna fila del gimnasio nuevo tiene `tenant_id = 1` |
| T-182-25 | Denial of Service | Un gimnasio vacío rompiendo los crons de todos | mitigate | Caso en `con-04` que corre los jobs con el tenant nuevo presente y verifica que ninguno lanza y que los efectos sobre El Templo no cambian |
</threat_model>

<verification>
- `cd el-templo-api && pnpm test -- test/tenancy/` → 0 (iso-01..04, con-01..06, mod-01/02)
- `cd el-templo-api && pnpm test -- test/platform/` → 0
- `cd el-templo-api && pnpm exec tsc --noEmit` → 0
- `cd el-templo-api && pnpm lint:tenant` → 0
</verification>

<success_criteria>
1. `iso-04-platform.test.ts` existe, usa `provisionTenant()` real y pasa con caso + control.
2. Los cuatro cruces de sesión dan 401.
3. Slug desconocido ⇒ 404 `TENANT_NOT_FOUND`; slug del gimnasio nuevo resuelve.
4. `iso-01`, `iso-02` e `iso-03` siguen verdes.
5. `con-04` verifica que el tenant nuevo no produce efectos en los crons.
</success_criteria>

<output>
Crear `.planning/phases/182-plataforma-rol-super-owner-wizard-de-alta-de-tenant/182-07-SUMMARY.md` al terminar.
</output>
