---
phase: 182-plataforma-rol-super-owner-wizard-de-alta-de-tenant
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/schema/platform-users.ts
  - el-templo-api/src/db/schema/platform-audit-log.ts
  - el-templo-api/src/db/schema/index.ts
  - el-templo-api/src/db/migrations/0216_platform_core.sql
  - el-templo-api/src/db/tenant-tables.ts
  - el-templo-api/test/db/tenant-tables.test.ts
  - el-templo-api/src/modules/shared/modules.ts
  - el-templo-api/src/modules/shared/branch-names.ts
  - el-templo-api/.env.example
  - el-templo-api/vitest.config.ts
  - el-templo-admin/.env.example
autonomous: true
requirements: [PLAT-01, PLAT-03]
must_haves:
  truths:
    - "Las tablas `platform_users` y `platform_audit_log` existen en la base y no llevan `tenant_id` (D-06, Opción B del doc 08)"
    - "El registro de clasificación de tablas reconoce las dos tablas nuevas como exentas, con motivo escrito, y sus gates de conteo quedan verdes"
    - "`PLATFORM_DOMAIN` y `PLATFORM_JWT_SECRET` están documentadas como variables de entorno y disponibles en el entorno de tests (D-01, D-03)"
    - "Existe una constante única para la key del flag de gimnasio y otra para el nombre de la sede virtual, sin tocar `MODULE_NAMES` (D-15, research C-1)"
  artifacts:
    - path: "el-templo-api/src/db/schema/platform-users.ts"
      provides: "Tabla Drizzle `platform_users` sin tenant_id"
      contains: "mysqlTable(\"platform_users\""
    - path: "el-templo-api/src/db/schema/platform-audit-log.ts"
      provides: "Tabla Drizzle `platform_audit_log` sin tenant_id"
      contains: "mysqlTable(\"platform_audit_log\""
    - path: "el-templo-api/src/db/migrations/0216_platform_core.sql"
      provides: "DDL hand-written de las dos tablas"
      contains: "CREATE TABLE IF NOT EXISTS `platform_users`"
    - path: "el-templo-api/src/modules/shared/branch-names.ts"
      provides: "Constante única VIRTUAL_BRANCH_NAME"
      contains: "VIRTUAL_BRANCH_NAME"
  key_links:
    - from: "el-templo-api/src/db/tenant-tables.ts"
      to: "platform_users / platform_audit_log"
      via: "TENANT_EXEMPT_TABLES"
      pattern: "\"platform_users\""
    - from: "el-templo-api/vitest.config.ts"
      to: "entorno de tests"
      via: "env block"
      pattern: "PLATFORM_JWT_SECRET"
---

<objective>
Poner el cimiento de datos y de configuración de la capa de plataforma: las dos tablas globales
(`platform_users`, `platform_audit_log`), su migración hand-written, su clasificación en el
registro de tenancy con los conteos actualizados, las variables de entorno nuevas y las dos
constantes que el resto de la fase consume (`GIMNASIO_FLAG_KEY`, `VIRTUAL_BRANCH_NAME`).

Purpose: sin estas tablas y sin las variables no hay login de plataforma ni aprovisionamiento;
y sin actualizar los gates de tenancy (conteo de tablas) CI queda rojo desde el primer commit.
Output: schema Drizzle + migración 0216 aplicada + `tenant-tables.ts` actualizado + `.env.example`
de API y admin + entorno de tests con las variables de plataforma.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/182-plataforma-rol-super-owner-wizard-de-alta-de-tenant/182-CONTEXT.md
@.planning/phases/182-plataforma-rol-super-owner-wizard-de-alta-de-tenant/182-RESEARCH.md
@.planning/phases/182-plataforma-rol-super-owner-wizard-de-alta-de-tenant/182-PATTERNS.md
@CLAUDE.md
@.claude/skills/el-templo-db-migrations/SKILL.md
</context>

<constraints>
Reglas de ejecución de esta fase (valen para TODOS los planes 182-XX):

- **Tests SOLO en foreground**, con timeout amplio (600000 ms). PROHIBIDO `run_in_background`.
  PROHIBIDO esperar con loops de `pgrep`/`sleep` (se matchean a sí mismos y cuelgan la sesión).
- **`git add` explícito por ruta.** Nunca `git add -A` ni `git add .`. Nunca `git push`.
- **No instalar dependencias nuevas** (`pnpm add`) sin un checkpoint humano. `pnpm install
  --frozen-lockfile` (instalar lo que el lockfile ya declara) NO es una dependencia nueva y sí
  está permitido si el worktree no tiene `node_modules`.
- **Sin `any`.** `catch (err: unknown)` + `instanceof Error`. Logging con `request.log`/`app.log`
  (Pino) en la API; nunca `console.log`.
- **Migraciones:** solo el runner custom (`pnpm db:migrate`). PROHIBIDO `drizzle-kit
  migrate`/`push` en trabajo commiteado. **Sin `;` dentro de comentarios `--`** del SQL.
- No commitear documentación fuera de `.planning/` y de los archivos que el plan lista.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Schema Drizzle de las dos tablas de plataforma + constantes compartidas</name>
  <files>
    el-templo-api/src/db/schema/platform-users.ts,
    el-templo-api/src/db/schema/platform-audit-log.ts,
    el-templo-api/src/db/schema/index.ts,
    el-templo-api/src/modules/shared/modules.ts,
    el-templo-api/src/modules/shared/branch-names.ts
  </files>
  <read_first>
    - el-templo-api/src/db/schema/tenants.ts (forma de tabla raíz sin `tenant_id`, docblock, trampa de `mysqlEnum`)
    - el-templo-api/src/db/schema/audit-log.ts (forma de la tabla de auditoría existente)
    - el-templo-api/src/db/schema/index.ts (patrón de `export * from "./x"`)
    - el-templo-api/src/modules/shared/modules.ts (MODULE_NAMES, moduleFlagKey, MODULE_FLAG_ON/OFF)
    - .planning/phases/182-.../182-PATTERNS.md (sección de `platform-users.ts` / `platform-audit-log.ts`)
    - .planning/phases/182-.../182-RESEARCH.md (§"Recomendación firme sobre D-15", §Conflicto C-1)
  </read_first>
  <action>
Crear `el-templo-api/src/db/schema/platform-users.ts` con la tabla `platform_users`
(`mysqlTable("platform_users", …)`), columnas: `id` int PK autoincrement; `email`
varchar(255) `.notNull().unique()`; `passwordHash` varchar("password_hash", 255) `.notNull()`;
`isActive` boolean("is_active") `.default(true).notNull()`; `lastLoginAt`
timestamp("last_login_at") nullable; `createdAt` timestamp("created_at")
`.defaultNow().notNull()`; `updatedAt` timestamp("updated_at")
`.defaultNow().onUpdateNow().notNull()`. **Sin columna `tenantId` y sin `role`**: hay un solo
actor de plataforma (D-06, Opción B del doc 08). Docblock arriba explicando por qué la tabla no
lleva `tenant_id` (el super-owner está POR ENCIMA de los tenants) y que va a
`TENANT_EXEMPT_TABLES`.

Crear `el-templo-api/src/db/schema/platform-audit-log.ts` con la tabla `platform_audit_log`:
`id` int PK autoincrement; `actorId` int("actor_id") `.notNull().references(() =>
platformUsers.id)`; `action` varchar(64) `.notNull()`; `targetTenantId` int("target_tenant_id")
**nullable** (`.references(() => tenants.id)`) — nullable a propósito: crear un tenant es la
acción que todavía no tiene tenant al momento de empezar; `payload` text `.notNull()` (JSON
serializado); `createdAt` timestamp("created_at") `.defaultNow().notNull()`; índice
`idx_platform_audit_actor` sobre `actorId` e `idx_platform_audit_target` sobre `targetTenantId`.
Docblock: es write-only por diseño, `target_tenant_id` es columna de NEGOCIO (auditar sobre qué
gimnasio), NO de aislamiento.

Agregar en `el-templo-api/src/db/schema/index.ts` las dos líneas `export * from
"./platform-users";` y `export * from "./platform-audit-log";` al final del archivo, siguiendo el
patrón existente.

En `el-templo-api/src/modules/shared/modules.ts`, agregar (sin tocar `MODULE_NAMES`,
`moduleFlagKey` ni `parseModuleFlagKey`):
`export const GIMNASIO_FLAG_KEY = "module.gimnasio.enabled";` con un docblock que diga
literalmente que **la key existe antes que el módulo**: la fase 184 mete `"gimnasio"` en
`MODULE_NAMES` junto con el rename `templo-module`→`feature-module` (checklist H-2 del doc 08), y
hasta entonces la fila es INERTE para `parseModuleFlagKey` a propósito (auditable con un SELECT,
D-15). Agregar `"gimnasio"` a `MODULE_NAMES` en esta fase está PROHIBIDO: rompe
`test/tenancy/mod-01-flags.test.ts` y enciende gimnasio para El Templo en `restoreTemploFlags`.

Crear `el-templo-api/src/modules/shared/branch-names.ts` exportando
`export const VIRTUAL_BRANCH_NAME = "Templo Online";` con docblock: es el nombre literal que
busca por igualdad el fallback de `resolveUserBranchId` (`finance/coach-load-routes.ts`) y
`subscriptions/service.ts`; renombrar el VALOR está prohibido en esta fase (rompe dos caminos de
producción de El Templo, D-05). En este plan solo se crea la constante — la sustitución de los
dos literales existentes por el import la hace el plan 182-05.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `el-templo-api/src/db/schema/platform-users.ts` contiene `mysqlTable("platform_users"` y NO contiene la cadena `tenant_id` ni `tenantId` fuera de comentarios.
    - `el-templo-api/src/db/schema/platform-audit-log.ts` contiene `mysqlTable("platform_audit_log"` y `target_tenant_id`.
    - `grep -c 'platform-users' el-templo-api/src/db/schema/index.ts` devuelve 1 y `grep -c 'platform-audit-log' el-templo-api/src/db/schema/index.ts` devuelve 1.
    - `grep -n 'GIMNASIO_FLAG_KEY' el-templo-api/src/modules/shared/modules.ts` encuentra la constante con el valor `"module.gimnasio.enabled"`.
    - `grep -vE '^\s*(//|\*|/\*)' el-templo-api/src/modules/shared/modules.ts | grep -c '"gimnasio"'` devuelve 0 dentro del array `MODULE_NAMES` (es decir: `MODULE_NAMES` sigue teniendo exactamente los 4 `templo-*`).
    - `el-templo-api/src/modules/shared/branch-names.ts` exporta `VIRTUAL_BRANCH_NAME = "Templo Online"`.
    - `cd el-templo-api && pnpm exec tsc --noEmit` sale con código 0.
  </acceptance_criteria>
  <done>Las dos tablas están declaradas en Drizzle, exportadas desde el barrel, y existen las dos constantes compartidas; el typecheck de la API pasa.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Migración 0216 + clasificación de tablas + conteos de los gates</name>
  <files>
    el-templo-api/src/db/migrations/0216_platform_core.sql,
    el-templo-api/src/db/tenant-tables.ts,
    el-templo-api/test/db/tenant-tables.test.ts
  </files>
  <read_first>
    - el-templo-api/src/db/migrations/0215_referral_partners.sql (header completo: estilo, motivo, verificación de numeración)
    - el-templo-api/src/db/migrations/0209_seed_module_flags.sql (estilo idempotente)
    - el-templo-api/src/db/run-migrations.ts (por qué no puede haber `;` en comentarios)
    - el-templo-api/src/db/tenant-tables.ts líneas 160-190 (bloque `TENANT_EXEMPT_TABLES` con motivos)
    - el-templo-api/test/db/tenant-tables.test.ts (el `it` de conteos: 91 gym-owned + 4 exentas = 95)
    - .claude/skills/el-templo-db-migrations/SKILL.md
  </read_first>
  <action>
**Paso 0 — re-verificar la numeración.** Correr
`git fetch origin && ls el-templo-api/src/db/migrations/ | tail -5` y
`git ls-tree --name-only origin/master el-templo-api/src/db/migrations/ | tail -5` y
`git ls-tree --name-only origin/staging el-templo-api/src/db/migrations/ | tail -5`.
Si el número más alto en cualquiera de las dos ramas es >= 0216, usar el siguiente libre y
renombrar el archivo (dejar constancia del corrimiento en el header, como hace la 0215).

Escribir `el-templo-api/src/db/migrations/0216_platform_core.sql` (o el número resuelto) con:
- Header `--` explicando: fase 182 (PLAT-01), las dos tablas son GLOBALES sin `tenant_id` a
  propósito (doc 08 Opción B, doc 05 §3 "club GLOBAL"), van a `TENANT_EXEMPT_TABLES`.
  **Sin ningún `;` dentro de las líneas de comentario** — el runner parte por `;` ANTES de limpiar
  comentarios.
- `CREATE TABLE IF NOT EXISTS \`platform_users\`` byte-a-byte coherente con el schema Drizzle:
  `id INT AUTO_INCREMENT PRIMARY KEY`, `email VARCHAR(255) NOT NULL`, `password_hash VARCHAR(255)
  NOT NULL`, `is_active TINYINT(1) NOT NULL DEFAULT 1`, `last_login_at TIMESTAMP NULL`,
  `created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP`, `updated_at TIMESTAMP NOT NULL
  DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`, `UNIQUE KEY
  \`platform_users_email_unique\` (\`email\`)`.
- `CREATE TABLE IF NOT EXISTS \`platform_audit_log\``: `id INT AUTO_INCREMENT PRIMARY KEY`,
  `actor_id INT NOT NULL`, `action VARCHAR(64) NOT NULL`, `target_tenant_id INT NULL`,
  `payload TEXT NOT NULL`, `created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP`,
  índices `idx_platform_audit_actor (actor_id)` e `idx_platform_audit_target (target_tenant_id)`,
  FK `fk_platform_audit_actor` → `platform_users(id)` y FK `fk_platform_audit_tenant` →
  `tenants(id)` (esta última con `ON DELETE SET NULL`).
- **Ningún hash de contraseña ni fila de `platform_users` en la migración** (D-07: el bootstrap es
  por CLI, ningún hash entra a git).

Aplicar la migración contra la base local: `cd el-templo-api && pnpm db:migrate` (runner custom,
foreground). Si el worktree no tiene `node_modules`, correr antes
`cd el-templo-api && pnpm install --frozen-lockfile`.

En `el-templo-api/src/db/tenant-tables.ts`: agregar `"platform_users"` y `"platform_audit_log"`
al array `TENANT_EXEMPT_TABLES` y sumar dos viñetas al docblock de arriba con el motivo en prosa,
en el mismo estilo que las cuatro existentes (ej.: `platform_users` — el actor de plataforma está
POR ENCIMA de los tenants; darle `tenant_id` sería afirmar que pertenece a un gimnasio, que es
justo lo contrario de lo que es). Corregir en ese docblock la frase "Son cuatro y no hay una
quinta por descuido" para que diga seis.

En `el-templo-api/test/db/tenant-tables.test.ts`, en el `it("los conteos son …")`: actualizar el
título a "91 gym-owned + 6 exentas y cubren las 97 tablas del schema",
`expect(TENANT_EXEMPT_TABLES.length).toBe(6)` y `expect(schemaTables.size).toBe(97)`, y agregar
un comentario "Fase 182 sumó `platform_users` y `platform_audit_log` a las exentas: 4 -> 6,
95 -> 97" en el mismo estilo que los comentarios de las fases 159 y 179 que ya están ahí.

Correr además `cd el-templo-api && pnpm db:verify-tenant` para confirmar que el verificador de
schema físico no tiene su propia lista que haya que actualizar (asunción A6 del research). Si
falla por las tablas nuevas, actualizar lo que pida y documentarlo en el SUMMARY.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm test -- test/db/tenant-tables.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - El archivo de migración existe con el número más bajo libre >= 0216 verificado contra `origin/master` Y `origin/staging`.
    - `grep -n '^--' el-templo-api/src/db/migrations/0216_platform_core.sql | grep -c ';'` devuelve 0 (ningún punto y coma dentro de comentarios).
    - `pnpm db:migrate` termina con código 0 y `SELECT name FROM _migrations ORDER BY id DESC LIMIT 1` devuelve el nombre del archivo nuevo.
    - `SHOW COLUMNS FROM platform_users LIKE 'tenant_id'` devuelve 0 filas.
    - `el-templo-api/src/db/tenant-tables.ts` contiene `"platform_users"` y `"platform_audit_log"` dentro de `TENANT_EXEMPT_TABLES`, cada una con motivo en el docblock.
    - `cd el-templo-api && pnpm test -- test/db/tenant-tables.test.ts` sale con código 0 (los conteos 91+6=97 y `.toBe(6)` pasan).
    - `cd el-templo-api && pnpm db:verify-tenant` sale con código 0 (o el SUMMARY documenta qué pidió y qué se cambió).
  </acceptance_criteria>
  <done>La migración está aplicada en la base local, las dos tablas están clasificadas como exentas con motivo, y el gate de conteo de tablas está verde.</done>
</task>

<task type="auto">
  <name>Task 3: Variables de entorno de plataforma (API, admin y entorno de tests)</name>
  <files>
    el-templo-api/.env.example,
    el-templo-api/vitest.config.ts,
    el-templo-admin/.env.example
  </files>
  <read_first>
    - el-templo-api/.env.example (bloque Wellhub y bloque del sentinel: estilo de comentario)
    - el-templo-api/vitest.config.ts (bloque `env` con `JWT_SECRET: "test-secret-for-testing"`)
    - el-templo-admin/.env.example
    - .planning/phases/182-.../182-CONTEXT.md (D-01, D-03, D-08, D-18)
  </read_first>
  <action>
En `el-templo-api/.env.example`, agregar un bloque nuevo al final con comentarios en el estilo del
archivo:
- `PLATFORM_DOMAIN=""` — dominio de la plataforma (D-01). NUNCA hardcodeado en código. Local:
  `localtest.me` (resuelve a 127.0.0.1 sin tocar `/etc/hosts`). Staging:
  `staging.<dominio>`. Prod: el dominio definitivo (`kaia.eltemplo.org` es el valor provisorio
  aceptado). Vacío ⇒ la resolución pre-login por `Origin` queda apagada y la API se comporta
  exactamente como hoy (D-05).
- `PLATFORM_JWT_SECRET=""` — **valor DISTINTO de `JWT_SECRET`, no opcional**. Con un secreto
  compartido, un token de plataforma pasaría `request.jwtVerify()` de cualquier ruta de tenant
  (`plugins/auth.ts` no valida audience) y `attachScope` resolvería el `platformUserId` como un
  `users.id` ajeno: escalada de privilegios estructural.
- `# PLATFORM_JWT_EXPIRES_IN=8h` — comentada, default en código `8h` (D-08: expiración corta, sin
  refresh largo).

En `el-templo-api/vitest.config.ts`, dentro del bloque `test.env`, agregar:
`PLATFORM_JWT_SECRET: "test-platform-secret-distinto-del-de-tenant"`,
`PLATFORM_DOMAIN: "localtest.me"` y `PLATFORM_JWT_EXPIRES_IN: "8h"`, con un comentario que
explique que el valor de `PLATFORM_JWT_SECRET` tiene que ser distinto de `JWT_SECRET` porque hay
un test que prueba justamente que un token no verifica contra el otro secreto.

En `el-templo-admin/.env.example`, agregar
`VITE_PLATFORM_DOMAIN=localtest.me` con un comentario: se usa SOLO para el preview de la URL
`slug.<dominio>` en el wizard (D-10); se hornea en build (cambiarla exige rebuild del admin, no
solo restart).
  </action>
  <verify>
    <automated>cd el-templo-api && grep -c 'PLATFORM_JWT_SECRET' .env.example vitest.config.ts && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'PLATFORM_DOMAIN' el-templo-api/.env.example` >= 1 y `grep -c 'PLATFORM_JWT_SECRET' el-templo-api/.env.example` >= 1.
    - `grep -n 'PLATFORM_JWT_SECRET' el-templo-api/vitest.config.ts` muestra un valor literal distinto de `test-secret-for-testing`.
    - `grep -n 'PLATFORM_DOMAIN' el-templo-api/vitest.config.ts` muestra el valor `localtest.me`.
    - `grep -c 'VITE_PLATFORM_DOMAIN' el-templo-admin/.env.example` == 1.
    - `cd el-templo-api && pnpm exec tsc --noEmit` sale con código 0.
  </acceptance_criteria>
  <done>Las tres variables nuevas están documentadas en los `.env.example` correspondientes y disponibles en el entorno de tests de la API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → base de datos | La migración crea estructura persistente en staging y prod |
| entorno → proceso de la API | Secretos por variables de entorno (`PLATFORM_JWT_SECRET`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-01 | Elevation of Privilege | JWT de plataforma vs JWT de tenant | mitigate | `PLATFORM_JWT_SECRET` documentado como variable propia y con valor distinto en `vitest.config.ts`; el enforcement se implementa en 182-02 |
| T-182-10 | Information Disclosure | `platform_users` | mitigate | La migración no siembra NINGÚN hash de contraseña (D-07: bootstrap por CLI, ningún hash en git) |
| T-182-11 | Tampering | Clasificación de tenancy | mitigate | Las tablas nuevas entran a `TENANT_EXEMPT_TABLES` con motivo escrito; el gate de conteo queda verde en el mismo commit, así que ninguna tabla queda sin clasificar |
| T-182-SC | Tampering | npm/pnpm installs | accept | Este plan no instala nada nuevo: `pnpm install --frozen-lockfile` solo materializa el lockfile existente. Cualquier `pnpm add` es gate humano (182-03) |
</threat_model>

<verification>
- `cd el-templo-api && pnpm exec tsc --noEmit` → 0
- `cd el-templo-api && pnpm test -- test/db/tenant-tables.test.ts` → 0
- `cd el-templo-api && pnpm lint:tenant` → 0
- `cd el-templo-api && pnpm db:verify-tenant` → 0
</verification>

<success_criteria>
1. `platform_users` y `platform_audit_log` existen en la base local, sin columna `tenant_id`.
2. La migración está numerada por encima del tope real de `origin/master` y `origin/staging`, aplicada por el runner custom, y sin `;` en comentarios.
3. `TENANT_EXEMPT_TABLES` tiene 6 entradas con motivo y el test de conteos (91+6=97) pasa.
4. `PLATFORM_DOMAIN`, `PLATFORM_JWT_SECRET` (+ `PLATFORM_JWT_EXPIRES_IN`) y `VITE_PLATFORM_DOMAIN` están documentadas; el entorno de tests las tiene.
5. `MODULE_NAMES` sigue teniendo exactamente 4 entradas; `GIMNASIO_FLAG_KEY` existe como constante aparte.
</success_criteria>

<output>
Crear `.planning/phases/182-plataforma-rol-super-owner-wizard-de-alta-de-tenant/182-01-SUMMARY.md` al terminar.
</output>
