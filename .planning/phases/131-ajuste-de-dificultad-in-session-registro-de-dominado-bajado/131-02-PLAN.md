---
phase: 131-ajuste-de-dificultad-in-session-registro-de-dominado-bajado
plan: 02
type: execute
wave: 2
depends_on: ["131-01"]
files_modified:
  - el-templo-api/src/modules/tree-progress/service.ts
  - el-templo-api/src/modules/exercise-adjustments/coach-service.ts
  - el-templo-api/src/modules/exercise-adjustments/routes.ts
  - el-templo-api/src/modules/exercise-adjustments/schemas.ts
  - el-templo-api/test/tree-progress.test.ts
  - el-templo-api/test/exercise-adjustments-coach.test.ts
  - el-templo-admin/src/composables/useExerciseAdjustmentsApi.ts
  - el-templo-admin/src/types/exercise-adjustments.ts
  - el-templo-admin/src/pages/AlumnoDetailPage.vue
autonomous: true
requirements: [ADJUST-04]
must_haves:
  truths:
    - "A node with a member's latest `dominado` record counts as reached in the Phase 127 % (in addition to the existing level+session proxies) — latest record per node wins (D-05, ADJUST-04)"
    - "A coach/owner can view a given member's dominado/bajado records via an admin/coach-gated endpoint; a member can NEVER reach it (D-05)"
    - "The coach view renders the member's dominado/bajado records in the admin alumno detail (ADJUST-04)"
    - "The enrichment never alters how level or SPOM is computed — only the 'reached' definition is widened (D-06)"
  artifacts:
    - path: "el-templo-api/src/modules/tree-progress/service.ts"
      provides: "buildMemberTree reached includes latest-dominado per node"
      contains: "exerciseAdjustments"
    - path: "el-templo-api/src/modules/exercise-adjustments/coach-service.ts"
      provides: "listMemberAdjustments(memberId)"
      exports: ["ExerciseAdjustmentCoachService"]
    - path: "el-templo-admin/src/pages/AlumnoDetailPage.vue"
      provides: "Coach-facing list of a member's dominado/bajado records"
      contains: "dominado"
  key_links:
    - from: "el-templo-api/src/modules/tree-progress/service.ts"
      to: "exercise_adjustments latest-dominado set"
      via: "query joined into the reached computation"
      pattern: "exerciseAdjustments|exercise_adjustments"
    - from: "el-templo-api/src/modules/exercise-adjustments/routes.ts"
      to: "TRAINING_ROLES gate"
      via: "onRequest authenticate + role check"
      pattern: "TRAINING_ROLES"
    - from: "el-templo-admin/src/pages/AlumnoDetailPage.vue"
      to: "GET /admin/exercise-adjustments/:memberId"
      via: "useExerciseAdjustmentsApi composable"
      pattern: "exercise-adjustments"
---

<objective>
Close ADJUST-04: feed the new `dominado` record into the Phase 127 tree-progress % (a node with the member's latest `dominado` record counts as reached, on top of the existing level+session proxies), and expose a coach/owner-gated view of a member's dominado/bajado records (endpoint + admin alumno-detail UI).

Purpose: The in-session adjustment must be visible — both in the member's % of the tree (127) and to the coach who decides level/SPOM changes (which this milestone deliberately does NOT automate, D-06).
Output: Enriched `buildMemberTree`, a coach read endpoint on the exercise-adjustments module, admin composable + types + a section on AlumnoDetailPage, integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/131-ajuste-de-dificultad-in-session-registro-de-dominado-bajado/131-CONTEXT.md
@.planning/phases/131-ajuste-de-dificultad-in-session-registro-de-dominado-bajado/131-01-PLAN.md

<read_first>
- el-templo-api/src/modules/tree-progress/service.ts — THE SEAM (header lines ~16-31; reached computed at lines ~277-279). `reached = node.dificultadLineal <= ceiling || completedExerciseIds.has(node.exerciseId)`. This plan adds a third OR branch: latest-`dominado` per node. `loadCompletedExerciseIds` (lines ~143-177) is the analog loader to mirror.
- el-templo-api/src/db/schema/exercise-adjustments.ts — the table from Plan 01 (member_id, exercise_id, status, created_at).
- el-templo-api/src/modules/exercise-adjustments/routes.ts + schemas.ts — Plan 01's module; this plan ADDS a coach GET route (do not break the member POST).
- el-templo-api/src/modules/tree-editor/routes.ts — TRAINING_ROLES onRequest gate pattern for admin/coach scope (403 for members).
- el-templo-api/src/modules/shared/permissions.ts — `TRAINING_ROLES` (coach/owner).
- el-templo-admin/src/composables/useTreeEditorApi.ts — admin composable pattern (api, extractError, Notify, createLogger, cleanup(), no onUnmounted, no console.log, no any).
- el-templo-admin/src/pages/AlumnoDetailPage.vue — the host page for the coach view section.
- el-templo-api/test/tree-progress.test.ts — existing 127 test to extend with the dominado-reached case.
- el-templo-api/test/helpers.ts — auth/request utilities, eltemplo_test.
</read_first>

<interfaces>
<!-- The 127 reached seam (current). -->
From tree-progress/service.ts:
```typescript
// per node:
const reached = node.dificultadLineal <= ceiling || completedExerciseIds.has(node.exerciseId);
// loadCompletedExerciseIds(db, userId): Promise<Set<number>>  — analog loader to mirror
```

<!-- exercise_adjustments row (from Plan 01). status: "dominado" | "bajado". -->
Latest-per-node rule (D-05): for each exercise_id, take the row with MAX(created_at). A node counts toward the new reached branch iff its latest record status = 'dominado'. A latest 'bajado' does NOT mark it reached.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enrich Phase 127 reached with latest-dominado per node</name>
  <files>el-templo-api/src/modules/tree-progress/service.ts, el-templo-api/test/tree-progress.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/tree-progress/service.ts (the seam header + reached line + loadCompletedExerciseIds loader to mirror)
    - el-templo-api/src/db/schema/exercise-adjustments.ts (the table)
    - el-templo-api/test/tree-progress.test.ts (existing reached-proxy tests to extend)
  </read_first>
  <behavior>
    - Test A (dominado marks reached): a member whose level ceiling and completed sessions do NOT reach node X, but who has a latest `dominado` record for X → buildMemberTree reports X.reached = true and the subfamily/category counts include it.
    - Test B (latest wins, bajado does not mark): a member with an earlier `dominado` then a later `bajado` for node Y (Y above ceiling, not in completed) → Y.reached = false (latest record is bajado).
    - Test C (no regression): the existing level-ceiling and completed-session reached proxies still mark nodes reached (existing tests stay green).
  </behavior>
  <action>
    Add a loader `loadDominatedExerciseIds(db, userId): Promise<Set<number>>` mirroring `loadCompletedExerciseIds`: read this member's `exercise_adjustments` rows, compute the latest record per `exercise_id` (MAX created_at; in JS reduce or a grouped query — keep it simple and deterministic, tie-break by id), and return the set of exercise_ids whose latest status is `dominado`. Wire it into the existing `Promise.all` next to `loadCompletedExerciseIds`, and extend the per-node reached expression to a THIRD OR branch: `node.dificultadLineal <= ceiling || completedExerciseIds.has(node.exerciseId) || dominatedExerciseIds.has(node.exerciseId)`. Update the seam header comment (lines ~16-31) to state that phase 131's dominado registry now AUGMENTS (not replaces) the proxies, latest-per-node wins. Do NOT change levelCeiling or any level/SPOM logic (D-06). No `any`; structured logging only.

    Extend `test/tree-progress.test.ts` with Tests A/B/C above, seeding exercise_adjustments rows directly (member_id + exercise_id + status + created_at ordering for the latest-wins case).
  </action>
  <acceptance_criteria>
    - source: el-templo-api/src/modules/tree-progress/service.ts | behavior: reached has a third OR branch fed by latest-dominado exercise_adjustments; latest-per-node wins | test-command: `cd el-templo-api && grep -n "dominated\|exerciseAdjustments\|exercise_adjustments" src/modules/tree-progress/service.ts`
    - source: el-templo-api/src/modules/tree-progress/service.ts | behavior: levelCeiling / level / SPOM logic unchanged (no users.level write) | test-command: `cd el-templo-api && ! grep -n "update(schema.users)\|users.level =" src/modules/tree-progress/service.ts`
    - source: el-templo-api | behavior: typecheck passes; tests written for CI | test-command: `cd el-templo-api && pnpm typecheck`
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm typecheck && grep -n "dominated\|exerciseAdjustments" src/modules/tree-progress/service.ts</automated>
    <human-check>Run tree-progress tests in CI (not locally per project workflow).</human-check>
  </verify>
  <done>buildMemberTree marks a node reached when the member's latest record for it is dominado; latest-per-node wins; level/SPOM untouched; tests written; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Coach read endpoint + admin alumno-detail view</name>
  <files>el-templo-api/src/modules/exercise-adjustments/coach-service.ts, el-templo-api/src/modules/exercise-adjustments/routes.ts, el-templo-api/src/modules/exercise-adjustments/schemas.ts, el-templo-api/test/exercise-adjustments-coach.test.ts, el-templo-admin/src/composables/useExerciseAdjustmentsApi.ts, el-templo-admin/src/types/exercise-adjustments.ts, el-templo-admin/src/pages/AlumnoDetailPage.vue</files>
  <read_first>
    - el-templo-api/src/modules/tree-editor/routes.ts (TRAINING_ROLES onRequest gate; 403 for members)
    - el-templo-api/src/modules/exercise-adjustments/routes.ts (Plan 01 member POST — add the coach GET alongside, do not regress it)
    - el-templo-admin/src/composables/useTreeEditorApi.ts (admin composable conventions)
    - el-templo-admin/src/pages/AlumnoDetailPage.vue (host page; find a sensible section to mount the list)
    - el-templo-api/test/helpers.ts (auth utilities incl. role-scoped tokens)
  </read_first>
  <action>
    API: add `ExerciseAdjustmentCoachService` (constructor DI db) with `listMemberAdjustments(memberId: number)` returning the member's adjustment rows (join `to_exercise_id`/`exercise_id` to `exercises.exercise` for human-readable names; newest first). Add a route `GET /:memberId` to the exercise-adjustments routes file, gated so ONLY TRAINING_ROLES (coach/owner) pass — mirror the tree-editor `onRequest` role check (authenticate then 403 if role not in TRAINING_ROLES). The member POST from Plan 01 stays member-scoped (authenticate only); apply the role gate per-route, not as a plugin-wide hook, so the member POST is not locked out — read tree-editor for the hook pattern and apply it at the coach route level. Add the GET response schema to schemas.ts (plain-const). memberId here is a coach LOOKING UP a member, which is legitimate for TRAINING_ROLES (distinct from the member self-scope of Plan 01).

    Admin app: `useExerciseAdjustmentsApi.ts` composable (mirror useTreeEditorApi — api.get(`/admin/exercise-adjustments/${memberId}`), extractError, Notify, createLogger, expose cleanup(), no onUnmounted, no console.log, no any) + `types/exercise-adjustments.ts` (row shape: id, exerciseName, toExerciseName, status, dayId, date, createdAt). Add a section to `AlumnoDetailPage.vue` that fetches and lists the member's dominado/bajado records (status badge, exercise name, served neighbor, date). Use the warm brand palette (no blue) consistent with existing admin pages.
  </action>
  <acceptance_criteria>
    - source: el-templo-api/src/modules/exercise-adjustments/routes.ts | behavior: GET /:memberId is gated by TRAINING_ROLES (403 for members); member POST still authenticate-only | test-command: `cd el-templo-api && grep -n "TRAINING_ROLES" src/modules/exercise-adjustments/routes.ts`
    - source: el-templo-api/test/exercise-adjustments-coach.test.ts | behavior: coach/owner gets 200 with the member's records; a member token gets 403 | test-command: `cd el-templo-api && pnpm typecheck`
    - source: el-templo-admin/src/pages/AlumnoDetailPage.vue | behavior: renders the member's dominado/bajado records via the composable | test-command: `cd el-templo-admin && grep -n "useExerciseAdjustmentsApi\|dominado" src/pages/AlumnoDetailPage.vue && pnpm typecheck`
    - source: el-templo-admin/src/composables/useExerciseAdjustmentsApi.ts | behavior: exposes cleanup(), no onUnmounted, no console.log | test-command: `cd el-templo-admin && grep -n "cleanup" src/composables/useExerciseAdjustmentsApi.ts && ! grep -n "onUnmounted\|console\." src/composables/useExerciseAdjustmentsApi.ts`
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm typecheck && grep -n "TRAINING_ROLES" src/modules/exercise-adjustments/routes.ts</automated>
    <human-check>Run coach-view integration tests in CI (not locally). Visually confirm the AlumnoDetailPage section after CI/staging deploy.</human-check>
  </verify>
  <done>Coach/owner-gated GET returns a member's dominado/bajado records (403 for members); admin alumno detail lists them; member POST unaffected; typecheck green both apps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin app → GET /api/admin/exercise-adjustments/:memberId | coach/owner reads a member's records (privileged) |
| member tree view → buildMemberTree | reads only request.user.userId's own records (Plan 01/127 contract) |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-05 | Elevation of Privilege | coach read endpoint | mitigate | GET /:memberId gated by TRAINING_ROLES onRequest (authenticate then 403 for non-coach/owner), mirroring tree-editor; test asserts a member token → 403 |
| T-131-06 | Information Disclosure | member self % | mitigate | buildMemberTree still keyed on the request.user.userId passed by the route (T-127-01); the new dominated loader is scoped by that userId, never a body id |
| T-131-07 | Elevation of Privilege | level/SPOM escalation | mitigate | the % enrichment only widens the 'reached' read; no write to users.level or SPOM (D-06); grep gate asserts no users.level write |
| T-131-SC | Tampering | npm installs | mitigate | NO new dependencies; reuses drizzle/fastify/quasar. Any install proposal → STOP and ask the user (memory) |
</threat_model>

<verification>
- `pnpm typecheck` green in el-templo-api AND el-templo-admin.
- tree-progress reached now includes latest-dominado per node; level/SPOM untouched.
- Coach GET gated by TRAINING_ROLES (403 for members); admin alumno detail lists records.
- Tests written for CI; confirm with the user before pushing to staging.
</verification>

<success_criteria>
- ADJUST-04: the dominado record feeds the Phase 127 % (latest-per-node wins) AND is visible to the coach.
- D-06 honored: no automatic level/SPOM change.
</success_criteria>

<output>
Create `.planning/phases/131-ajuste-de-dificultad-in-session-registro-de-dominado-bajado/131-02-SUMMARY.md` when done.
</output>
