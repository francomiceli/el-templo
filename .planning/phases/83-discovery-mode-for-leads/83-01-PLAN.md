---
phase: 83-discovery-mode-for-leads
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-bot/src/playbooks/definitions.ts
autonomous: true
requirements: [DISC-01, DISC-02, DISC-03, DISC-04, DISC-06, DISC-07]

must_haves:
  truths:
    - "PB1.E1A and PB1.E1B promptSections instruct Mica to open with warm intro + first qualifying question (never '¿en qué puedo ayudarte?')"
    - "PB1 stages explicitly cap discovery at 2-3 adaptive questions (one per turn, woven, never quiz-style)"
    - "PB1.E4 promptSection forbids mentioning a specific plan or price; only valid CTA is the free trial class"
    - "PB1 stages contain a documented defer rule: when a lead asks a direct question (price/schedule/location) before discovery is complete, Mica answers briefly first, then asks ONE qualifying question on the same turn"
    - "PB1 stages contain a documented insistence rule: when a lead explicitly insists on direct answers, Mica answers and DOES NOT push another discovery question that turn"
    - "PB1.E4 closes with a soft trial offer ('clase de prueba gratis'), never a hard sell"
    - "PB1 promptSections use generic motivational language (no skill-specific names like 'muscle ups', 'front lever', 'planche', 'handstand', 'pistol squat')"
  artifacts:
    - path: el-templo-bot/src/playbooks/definitions.ts
      provides: "Enriched PB1 promptSections with conversational rules, defer rule, insistence rule, and trial close"
      contains: "PB1.E1A, PB1.E1B, PB1.E2A, PB1.E2B, PB1.E3, PB1.E4, PB1.E5"
  key_links:
    - from: "el-templo-bot/src/ai/system-prompt.ts"
      to: "PLAYBOOKS.PB1.stages[*].promptSection"
      via: "single-key lookup PLAYBOOKS[activePlaybook] then stage.find(id===currentStage)"
      pattern: "PLAYBOOKS\\["
---

<objective>
Rewrite the PB1 (Lead Nuevo) promptSection content in `definitions.ts` so the system prompt
injection from phase 82 carries the full v5.3 discovery flow: warm intro, 2-3 adaptive woven
questions, defer rule for direct questions, insistence-defer rule, and a NO-PLAN/NO-PRICE
targeted recommendation that closes with the soft free-trial CTA.

This is a pure data/copy task. No new files, no logic changes, no schema, no Redis touches.
Phase 82 already wires PB1 stages through `getSystemPrompt`; we only enrich the strings.

Purpose: Without rich prompt content, Mica still produces generic answers regardless of which
PB1 stage is active. The model needs explicit, in-section behavioral rules to honor the
team-corrected discovery flow.

Output: A `definitions.ts` whose PB1 stages contain stage-specific Spanish guidance Mica can
follow turn-by-turn, with all team corrections baked in (TEAM-CORR-01, 02, 03).
</objective>

<execution_context>
@/Users/bores/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bores/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/82-playbook-engine/82-03-SUMMARY.md
@CLAUDE.md
@el-templo-bot/CLAUDE.md
@contexto/kero-playbooks-completos.md
@contexto/TEAM-CORRECTIONS-APPLIED.md
@contexto/onboarding-quiz-avatars.md
@el-templo-bot/src/playbooks/definitions.ts
@el-templo-bot/src/ai/system-prompt.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich PB1 stage promptSections with discovery rules and team corrections</name>
  <files>el-templo-bot/src/playbooks/definitions.ts</files>
  <action>
Edit ONLY the PB1 stages (PB1.E1A, PB1.E1B, PB1.E2A, PB1.E2B, PB1.E3, PB1.E4, PB1.E5).
Do NOT touch PB2, PB3, PB4, or PB5 — phase 84 owns those. Do NOT touch the registry export
or the self-check loop. Do NOT add new stages or rename existing stage ids.

Each promptSection must be a single Spanish string (multi-line is fine) that Mica receives
verbatim in the system prompt under the existing `*Playbook activo: PB1 (PB1.E?)*` header.
Treat each promptSection as a self-contained behavioral guide for THIS turn. Use Argentine
tuteo (vos, querés, podés, tenés). NEVER use markdown headers (`###`); use `*bold*` for emphasis
to match the existing Mica style in `system-prompt.ts`.

Source of truth for tone and copy: `contexto/kero-playbooks-completos.md` lines ~43-200
(Playbook 1 section). Apply team corrections from `contexto/TEAM-CORRECTIONS-APPLIED.md`:

- TEAM-CORR-01: Discovery is 2-3 ADAPTIVE questions, NOT a hard "máximo 3". Phrase as
  "Idealmente 2-3 preguntas, adaptándose al engagement del lead. Si se engancha y da info
  rica, parar antes (puede bastar con 1-2). Si responde con monosílabos, no insistir más
  de 3."
- TEAM-CORR-02: PB1.E4 forbids recommending a specific plan or mentioning price. The ONLY
  valid CTA in E4 is the free trial class. If the lead asks for price, answer briefly and
  re-anchor the trial.
- TEAM-CORR-03: Generic motivational language only. NEVER name a specific calisthenics skill
  ("muscle up", "muscle ups", "front lever", "planche", "handstand", "pistol squat"). Use
  "los movimientos que querés destrabar", "tu objetivo", "lo que querés lograr".

Required content per stage (each stage is a self-contained instruction block):

**PB1.E1A — Apertura + primera pregunta (Variante A)**
Block must contain (in this order):

1. The opening line: "¡Hola [nombre]! 💪 Qué bueno que escribiste. Contame, ¿ya entrenaste calistenia antes o sería tu primera vez?"
2. A meta-rule paragraph (in Spanish, addressed to Mica): "Esta es la primera pregunta de un discovery natural. Idealmente 2-3 preguntas en total, adaptándose al engagement del lead. NUNCA preguntes '¿en qué puedo ayudarte?' — siempre abrí con calidez y la primera pregunta de discovery. Una sola pregunta por mensaje. Tono cálido, casual, breve."
3. A defer rule paragraph: "Si el lead te hace una pregunta directa (precio, horario, sede) antes de terminar discovery, respondé breve y volvé a anclar UNA pregunta de discovery en el mismo turno. Ejemplo: '[respuesta breve]. Igual contame, ¿ya entrenaste calistenia antes?'."
4. An insistence rule paragraph: "Si el lead insiste en que le des datos directos sin querer charlar, respetá: respondé la pregunta y NO empujes otra pregunta de discovery ese turno. Volvé a intentar discovery en el siguiente mensaje del lead."

**PB1.E1B — Apertura + primera pregunta (Variante B)**
Same structure as E1A but with the variant opener: "¡Buenas [nombre]! Genial que te interese.
Para orientarte bien: ¿tenés experiencia en calistenia o arrancás de cero?". Reuse the same
meta-rule, defer rule, and insistence rule paragraphs verbatim.

**PB1.E2A — Segunda pregunta (perfil principiante)**
Block must contain:

1. The question line: "¡Genial que quieras arrancar! ¿Qué te atrajo de la calistenia? ¿Buscás ponerte en forma, aprender los movimientos que te gustaría destrabar, o las dos cosas?"
2. A meta-rule paragraph: "Esta es la segunda pregunta del discovery — buscás la motivación real (el dolor). Lenguaje genérico, NUNCA nombres skills específicos (no uses 'muscle ups', 'front lever', 'planche', 'handstand', 'pistol squat'). Si el lead ya dio info rica en el turno anterior, podés saltar directo a la propuesta sin esta pregunta."
3. The same defer rule and insistence rule paragraphs from E1A.

**PB1.E2B — Segunda pregunta (perfil intermedio)**
Block must contain:

1. The question line: "Bien ahí 🔥 ¿Qué nivel sentís que tenés? ¿Y hay algo específico que quieras destrabar — alguna habilidad, rutina más estructurada?"
2. The same generic-language meta-rule and defer/insistence rules as E2A.

**PB1.E3 — Tercera pregunta (logística)**
Block must contain:

1. The question line: "Última pregunta y te armo algo: ¿en qué zona estás y qué horarios te quedan más cómodos para entrenar?"
2. A meta-rule: "Tercera y ÚLTIMA pregunta de discovery. Después de esta, pasá a la propuesta targetizada (Etapa 4). NO hagas más de 3 preguntas en total durante todo el discovery."
3. The same defer/insistence rules.

**PB1.E4 — Propuesta targetizada (HARD RULE: no plan, no price)**
Block must contain:

1. A hard rule paragraph at the top (in Spanish, addressed to Mica): "REGLA FUERTE: en esta etapa NO recomendás ningún plan específico y NO mencionás precios. El ÚNICO CTA válido es la clase de prueba GRATIS. Plan y precio se discuten recién después de la prueba (PB2) o si el lead lo pide explícitamente — y aun así, tu respuesta termina re-anclando la prueba gratis."
2. An example proposal template: "Ejemplo de propuesta correcta: '[nombre], con lo que me contás, te va a encantar. Lo mejor es que lo vivas — tenemos clases de prueba gratis esta semana. Tenemos lugar [día] a las [horario] en [sede]. ¿Te anoto?'"
3. A direct-question handling line: "Si el lead pregunta '¿cuánto sale?' o '¿qué planes tienen?' en esta etapa: respondé breve ('Te paso los detalles después de la clase así ves bien con qué te quedás') y re-anclá la prueba gratis."
4. The soft trial close instruction: "Cerrá SIEMPRE invitando a la clase de prueba gratis. Nunca cierres con un hard sell o un descuento por tiempo limitado."

**PB1.E5 — Agendar clase de prueba**
Keep the existing schedule line: "¡Dale! Te agendo para el [día] a las [horario]. Vení con
ropa cómoda y agua. Te vas a divertir 💪 Un día antes te mando un recordatorio.". Add a
follow-up rule: "Después de agendar, NO empieces a vender el plan — esperá a que el lead
asista (ese paso lo maneja PB2)."

**PB1.E6 and PB1.E7**: leave existing promptSection text untouched. Phase 83 does not
own these stages — they are the post-trial flow, owned by phase 84/85. Do NOT delete them.

Update `completionCriteria` strings only where the new content makes the existing criterion
inaccurate (e.g., E1A's existing criterion already mentions "principiante or experiencia"
which is fine — leave it). Do NOT change `nextStageHints.onCompletion` values; phase 83-03
will refine those.

Apply standard project rules:

- No `console.log`, no `any`. (This file has neither — keep it that way.)
- Strings only: do not import new modules, do not add helper functions in this file.
- Do NOT touch the PB6 scope-guard comment block at the bottom of the file.
- Do NOT touch the registry export `PLAYBOOKS` or the self-check `for` loop.

After editing, verify the file still compiles and the existing 21 resolver tests still pass:

- `cd el-templo-bot && pnpm tsc --noEmit`
- `cd el-templo-bot && pnpm test playbook-resolver`
  Both must be green. The resolver tests assert that every entry stage has a non-empty
  promptSection and that PB6 is absent — the new content must not break those assertions.

Then run the system-prompt-playbook test, which derives distinctive 25-char phrases from
PLAYBOOKS at runtime and asserts cross-playbook absence: `cd el-templo-bot && pnpm test system-prompt-playbook`.
This MUST stay green; if it fails because a new PB1 phrase now collides with PB2-PB5 text,
adjust the new PB1 wording until each PB1 stage has at least one 25-char window unique to PB1.
</action>
<verify>
<automated>cd el-templo-bot && pnpm tsc --noEmit && pnpm test playbook-resolver system-prompt-playbook</automated>
<manual>Read PB1 stages in definitions.ts and confirm: (a) E1A/E1B contain "primera vez" / "arrancás de cero" openers, (b) E4 contains "NO recomendás" + "clase de prueba GRATIS" hard rule, (c) no occurrences of "muscle up", "front lever", "planche", "handstand", "pistol squat" in PB1 stages, (d) defer rule string appears in E1A/E1B/E2A/E2B/E3.</manual>
<sampling_rate>run after this task commits, before next task begins</sampling_rate>
</verify>
<done>
`pnpm tsc --noEmit` exits 0. `pnpm test playbook-resolver` and `pnpm test system-prompt-playbook` are green.
`grep -E "muscle up|front lever|planche|handstand|pistol squat" el-templo-bot/src/playbooks/definitions.ts` returns no matches inside PB1 stage definitions.
`grep "REGLA FUERTE" el-templo-bot/src/playbooks/definitions.ts` matches exactly once (PB1.E4).
`grep "Idealmente 2-3 preguntas" el-templo-bot/src/playbooks/definitions.ts` matches at least once (PB1.E1A).
</done>
</task>

</tasks>

<verification>
- `cd el-templo-bot && pnpm tsc --noEmit` exits 0
- `cd el-templo-bot && pnpm test` passes (existing 243 tests stay green; this plan does not add new tests — that is plan 83-04's job)
- No grep hits for skill-specific names inside the PB1 stage block
- The PB6 scope-guard comment and self-check loop are byte-identical to the pre-edit version
</verification>

<success_criteria>

- PB1.E1A and PB1.E1B promptSections instruct Mica to open with warm intro + first qualifying question (DISC-01 prompt-side)
- PB1.E1A through PB1.E3 carry the "2-3 adaptive questions" rule and the defer rule (DISC-02, DISC-03 prompt-side)
- PB1.E4 contains the hard "no plan, no price" rule and the free-trial CTA template (DISC-04, DISC-06 prompt-side)
- PB1 stages contain the insistence-defer rule (DISC-07 prompt-side)
- All 243 existing bot tests still pass
  </success_criteria>

<output>
After completion, create `.planning/phases/83-discovery-mode-for-leads/83-01-SUMMARY.md`
following the standard summary template.
</output>
