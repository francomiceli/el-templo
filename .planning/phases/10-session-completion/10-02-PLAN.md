---
phase: 10-session-completion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-app/src/modules/training/components/player/CelebrationScreen.vue
autonomous: true

must_haves:
  truths:
    - "Celebration screen shows trophy icon with congratulations message"
    - "Celebration auto-advances after 3.5 seconds"
  artifacts:
    - path: "el-templo-app/src/modules/training/components/player/CelebrationScreen.vue"
      provides: "Celebratory completion screen"
      contains: "emoji_events"
---

<objective>
Create celebration screen component for session completion.

Purpose: Celebration provides motivational moment after completing all blocks before showing summary.
Output: CelebrationScreen.vue component that auto-advances after 3.5 seconds.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-session-completion/10-CONTEXT.md

# Existing patterns
@el-templo-app/src/modules/training/components/player/SplashScreen.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CelebrationScreen component</name>
  <files>
    el-templo-app/src/modules/training/components/player/CelebrationScreen.vue
  </files>
  <action>
Create new component for the celebratory moment after completing all blocks. Per CONTEXT.md: 3-4 seconds duration, auto-advances, no user tap required.

```vue
<template>
  <div
    class="celebration-screen fixed-full column items-center justify-center"
    :class="{ 'fade-out': isFading }"
  >
    <div class="celebration-content column items-center q-gutter-y-md">
      <!-- Animated trophy icon -->
      <div class="icon-container" :class="{ 'pulse-animation': !isFading }">
        <q-icon name="emoji_events" size="100px" color="amber" />
      </div>

      <!-- Congratulations text -->
      <div class="text-h4 text-white text-weight-bold text-center">
        Sesion Completada!
      </div>

      <!-- Subtitle -->
      <div class="text-subtitle1 text-white-70 text-center">
        Excelente trabajo
      </div>

      <!-- Loading indicator for transition -->
      <div class="loading-dots q-mt-lg">
        <q-spinner-dots color="white" size="40px" />
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted, onUnmounted } from 'vue';

interface Props {
  /** Duration before auto-advancing (default: 3500ms per CONTEXT.md) */
  duration?: number;
}

interface Emits {
  (e: 'complete'): void;
}

const props = withDefaults(defineProps<Props>(), {
  duration: 3500,
});
const emit = defineEmits<Emits>();

/** Fade-out animation state */
const isFading = ref(false);

/** Timer references for cleanup */
let displayTimer: ReturnType<typeof setTimeout> | null = null;
let fadeTimer: ReturnType<typeof setTimeout> | null = null;

onMounted(() => {
  // Display for duration, then start fade
  displayTimer = setTimeout(() => {
    isFading.value = true;
    // Complete after fade animation (0.5s)
    fadeTimer = setTimeout(() => {
      emit('complete');
    }, 500);
  }, props.duration);
});

onUnmounted(() => {
  if (displayTimer) clearTimeout(displayTimer);
  if (fadeTimer) clearTimeout(fadeTimer);
});
</script>

<style scoped lang="scss">
.celebration-screen {
  z-index: 9999;
  background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
  transition: opacity 0.5s ease-out;

  &.fade-out {
    opacity: 0;
    pointer-events: none;
  }
}

.celebration-content {
  padding: 24px;
}

.icon-container {
  width: 140px;
  height: 140px;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.1);
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 24px;
}

.pulse-animation {
  animation: pulse 1s ease-in-out infinite;
}

@keyframes pulse {
  0%, 100% {
    transform: scale(1);
  }
  50% {
    transform: scale(1.1);
  }
}

.text-white-70 {
  color: rgba(255, 255, 255, 0.7);
}

.loading-dots {
  opacity: 0.8;
}
</style>
```

Key design decisions:
- Trophy icon (emoji_events) - universally recognized achievement symbol
- Pulse animation on icon - draws attention, celebrates achievement
- 3.5s display + 0.5s fade = 4s total (within 3-4 second target)
- Spinner dots indicate transition to summary (user knows something is coming)
- Same dark gradient as SplashScreen for visual consistency
  </action>
  <verify>
1. Component imports without errors: Add temporary import in DayPlayer.vue
2. Renders correctly: Show component conditionally to verify layout
3. Auto-advances: Verify 'complete' event fires after ~4 seconds
  </verify>
  <done>
CelebrationScreen.vue shows trophy icon, "Sesion Completada!" message, and auto-advances after 3.5 seconds.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npm run build` in el-templo-app
2. CelebrationScreen renders with animation
3. CelebrationScreen emits 'complete' after timeout
</verification>

<success_criteria>
- CelebrationScreen shows trophy icon with pulse animation
- CelebrationScreen auto-advances after 3.5 seconds (emits 'complete')
- Visual consistency with existing SplashScreen gradient
</success_criteria>

<output>
After completion, create `.planning/phases/10-session-completion/10-02-SUMMARY.md`
</output>
