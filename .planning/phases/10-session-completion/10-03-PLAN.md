---
phase: 10-session-completion
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - el-templo-app/src/modules/training/components/player/RpeSlider.vue
  - el-templo-app/src/modules/training/components/player/SessionSummary.vue
autonomous: true

must_haves:
  truths:
    - "RPE slider shows 1-10 scale with labels at 2, 4, 6, 8, 10"
    - "Selected RPE value displays description text below slider"
    - "Summary shows total duration, blocks completed, exercises count"
    - "Summary shows per-block breakdown with duration and exercise count"
    - "Done button is tappable and emits finish event"
  artifacts:
    - path: "el-templo-app/src/modules/training/components/player/RpeSlider.vue"
      provides: "RPE input slider with labels"
      contains: "q-slider"
    - path: "el-templo-app/src/modules/training/components/player/SessionSummary.vue"
      provides: "Session summary screen with stats and RPE"
      contains: "Resumen"
  key_links:
    - from: "SessionSummary.vue"
      to: "RpeSlider.vue"
      via: "component import and v-model binding"
      pattern: "RpeSlider.*v-model"
---

<objective>
Create session summary UI components: RPE slider for effort input and summary screen displaying session statistics with per-block breakdown.

Purpose: Members review their workout and optionally rate perceived exertion before finishing.
Output: RpeSlider.vue component and SessionSummary.vue component.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-session-completion/10-CONTEXT.md
@.planning/phases/10-session-completion/10-RESEARCH.md

# Existing patterns
@el-templo-app/src/modules/training/components/player/SplashScreen.vue
@el-templo-app/src/modules/training/components/player/BlockHeader.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RpeSlider component</name>
  <files>
    el-templo-app/src/modules/training/components/player/RpeSlider.vue
  </files>
  <action>
Create the RPE input slider per CONTEXT.md requirements:
- 1-10 scale
- Labels at intervals (2, 4, 6, 8, 10)
- Selected value's label shown below dynamically
- Optional (user can skip)

```vue
<template>
  <div class="rpe-slider">
    <div class="rpe-slider__label text-subtitle2 text-grey-7 q-mb-sm">
      Esfuerzo Percibido (RPE)
    </div>

    <q-slider
      v-model="internalValue"
      :min="1"
      :max="10"
      :step="1"
      :markers="true"
      :marker-labels="markerLabels"
      label
      label-always
      color="primary"
      thumb-size="24px"
      class="q-px-md"
    />

    <div class="rpe-slider__description text-center q-mt-md text-body2">
      <template v-if="internalValue !== null">
        <span class="text-weight-medium">{{ internalValue }}</span>
        <span class="text-grey-7"> - {{ rpeDescriptions[internalValue] }}</span>
      </template>
      <template v-else>
        <span class="text-grey-6">Desliza para indicar tu esfuerzo</span>
      </template>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, watch } from 'vue';

interface Props {
  /** Current RPE value (1-10 or null) */
  modelValue: number | null;
}

interface Emits {
  (e: 'update:modelValue', value: number | null): void;
}

const props = defineProps<Props>();
const emit = defineEmits<Emits>();

// Internal value starts at 5 if null (center of slider) but doesn't emit until user interacts
const internalValue = ref<number>(props.modelValue ?? 5);
const hasInteracted = ref(props.modelValue !== null);

// Marker labels per CONTEXT.md: every 2 numbers (2, 4, 6, 8, 10)
const markerLabels: Record<number, string> = {
  2: 'Facil',
  4: 'Moderado',
  6: 'Duro',
  8: 'Muy Duro',
  10: 'Maximo',
};

// Full descriptions for all values
const rpeDescriptions: Record<number, string> = {
  1: 'Muy facil, apenas esfuerzo',
  2: 'Facil',
  3: 'Facil a moderado',
  4: 'Moderado',
  5: 'Moderado a duro',
  6: 'Duro',
  7: 'Duro a muy duro',
  8: 'Muy duro',
  9: 'Extremadamente duro',
  10: 'Esfuerzo maximo',
};

// Watch for user interaction and emit
watch(internalValue, (newValue) => {
  hasInteracted.value = true;
  emit('update:modelValue', newValue);
});

// Sync from parent if changed externally
watch(() => props.modelValue, (newValue) => {
  if (newValue !== null) {
    internalValue.value = newValue;
    hasInteracted.value = true;
  }
});
</script>

<style scoped lang="scss">
.rpe-slider {
  padding: 16px 8px;
}

.rpe-slider__description {
  min-height: 24px;
}
</style>
```

Design decisions:
- Slider starts at 5 (middle) visually but doesn't emit until user interacts
- Labels at 2, 4, 6, 8, 10 per CONTEXT.md
- Full descriptions for all 10 values
- Thumb size 24px for touch-friendliness (research recommends minimum 20px)
  </action>
  <verify>
Component renders in isolation. Test with:
```vue
<RpeSlider v-model="rpeValue" />
```
Verify slider moves, labels appear, description updates.
  </verify>
  <done>
RpeSlider shows 1-10 scale with labels, displays dynamic description for selected value.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create SessionSummary component</name>
  <files>
    el-templo-app/src/modules/training/components/player/SessionSummary.vue
  </files>
  <action>
Create the session summary screen per CONTEXT.md requirements:
- Total duration, blocks completed, exercise count
- Per-block breakdown
- RPE slider (directly visible, not in modal)
- Done button to finish

```vue
<template>
  <div class="session-summary fixed-full column">
    <!-- Header -->
    <div class="session-summary__header q-pa-md">
      <div class="text-h5 text-weight-bold">Resumen de Sesion</div>
      <div class="text-subtitle2 text-grey-7">{{ formattedDate }}</div>
    </div>

    <!-- Scrollable Content -->
    <div class="session-summary__content col q-pa-md">
      <!-- Key Stats -->
      <div class="stats-grid q-mb-lg">
        <div class="stat-card">
          <q-icon name="timer" size="28px" color="primary" />
          <div class="stat-value">{{ formattedDuration }}</div>
          <div class="stat-label">Duracion</div>
        </div>
        <div class="stat-card">
          <q-icon name="view_module" size="28px" color="secondary" />
          <div class="stat-value">{{ blocksCompleted.length }}</div>
          <div class="stat-label">Bloques</div>
        </div>
        <div class="stat-card">
          <q-icon name="fitness_center" size="28px" color="accent" />
          <div class="stat-value">{{ totalExercises }}</div>
          <div class="stat-label">Ejercicios</div>
        </div>
      </div>

      <!-- Total Days Trained -->
      <div class="days-trained q-mb-lg text-center">
        <div class="text-h4 text-weight-bold text-primary">{{ totalDaysTrained }}</div>
        <div class="text-body2 text-grey-7">dias entrenados</div>
      </div>

      <!-- Per-Block Breakdown -->
      <div class="blocks-breakdown q-mb-lg">
        <div class="text-subtitle1 text-weight-medium q-mb-sm">Detalle por Bloque</div>
        <q-list separator>
          <q-item v-for="block in blocksCompleted" :key="block.blockRole" dense>
            <q-item-section avatar>
              <q-icon
                :name="getBlockIcon(block.blockRole)"
                :color="getBlockColor(block.blockRole)"
              />
            </q-item-section>
            <q-item-section>
              <q-item-label>{{ getBlockName(block.blockRole) }}</q-item-label>
              <q-item-label caption>
                {{ formatBlockDuration(block) }} · {{ block.exerciseCount }} ejercicios
              </q-item-label>
            </q-item-section>
          </q-item>
        </q-list>
      </div>

      <!-- RPE Input -->
      <div class="rpe-section q-mb-lg">
        <RpeSlider v-model="rpeValue" />
      </div>

      <!-- Notes (optional) -->
      <div class="notes-section">
        <q-input
          v-model="notesValue"
          type="textarea"
          label="Notas (opcional)"
          outlined
          :rows="2"
          maxlength="500"
          counter
        />
      </div>
    </div>

    <!-- Fixed Footer -->
    <div class="session-summary__footer q-pa-md">
      <q-btn
        color="primary"
        unelevated
        label="Terminar Sesion"
        class="full-width"
        size="lg"
        :loading="isSubmitting"
        @click="onFinish"
      />
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue';
import RpeSlider from './RpeSlider.vue';

interface BlockSummary {
  blockRole: string;
  startedAt: string;
  completedAt: string;
  exerciseCount: number;
  skipped: boolean;
}

interface Props {
  /** Date of the session (YYYY-MM-DD) */
  date: string;
  /** Total session duration in seconds */
  totalDurationSeconds: number;
  /** Array of completed blocks with timing */
  blocksCompleted: BlockSummary[];
  /** Total exercises across all blocks */
  totalExercises: number;
  /** Cumulative days trained (from API response) */
  totalDaysTrained: number;
}

interface Emits {
  (e: 'finish', data: { rpe: number | null; notes: string | null }): void;
}

const props = defineProps<Props>();
const emit = defineEmits<Emits>();

// Form state
const rpeValue = ref<number | null>(null);
const notesValue = ref<string>('');
const isSubmitting = ref(false);

// Computed
const formattedDate = computed(() => {
  const d = new Date(props.date + 'T00:00:00');
  return d.toLocaleDateString('es-ES', {
    weekday: 'long',
    day: 'numeric',
    month: 'long',
  });
});

const formattedDuration = computed(() => {
  const mins = Math.floor(props.totalDurationSeconds / 60);
  const secs = props.totalDurationSeconds % 60;
  if (mins >= 60) {
    const hours = Math.floor(mins / 60);
    const remainingMins = mins % 60;
    return `${hours}h ${remainingMins}m`;
  }
  return `${mins}:${secs.toString().padStart(2, '0')}`;
});

// Block display helpers
function getBlockName(role: string): string {
  const names: Record<string, string> = {
    INITIUM: 'Initium',
    NUCLEUS: 'Nucleus',
    DEUTEROS_1: 'Deuteros',
    DEUTEROS_2: 'Deuteros',
    ATHLOS_EPIKOS: 'Athlos',
  };
  return names[role] || role;
}

function getBlockIcon(role: string): string {
  const icons: Record<string, string> = {
    INITIUM: 'wb_sunny',
    NUCLEUS: 'star',
    DEUTEROS_1: 'layers',
    DEUTEROS_2: 'layers',
    ATHLOS_EPIKOS: 'local_fire_department',
  };
  return icons[role] || 'view_module';
}

function getBlockColor(role: string): string {
  const colors: Record<string, string> = {
    INITIUM: 'light-blue',
    NUCLEUS: 'purple',
    DEUTEROS_1: 'teal',
    DEUTEROS_2: 'deep-orange',
    ATHLOS_EPIKOS: 'amber',
  };
  return colors[role] || 'grey';
}

function formatBlockDuration(block: BlockSummary): string {
  const start = new Date(block.startedAt).getTime();
  const end = new Date(block.completedAt).getTime();
  const seconds = Math.round((end - start) / 1000);
  const mins = Math.floor(seconds / 60);
  const secs = seconds % 60;
  return `${mins}:${secs.toString().padStart(2, '0')}`;
}

// Actions
function onFinish(): void {
  isSubmitting.value = true;
  emit('finish', {
    rpe: rpeValue.value,
    notes: notesValue.value.trim() || null,
  });
}
</script>

<style scoped lang="scss">
.session-summary {
  background: white;
  z-index: 9998;
}

.session-summary__header {
  background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
  color: white;
}

.session-summary__content {
  overflow-y: auto;
  padding-bottom: 100px; // Space for fixed footer
}

.session-summary__footer {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  background: white;
  border-top: 1px solid rgba(0, 0, 0, 0.08);
  padding-bottom: calc(16px + env(safe-area-inset-bottom, 0px));
}

.stats-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 12px;
}

.stat-card {
  background: #f5f5f5;
  border-radius: 12px;
  padding: 16px 8px;
  text-align: center;

  .stat-value {
    font-size: 1.5rem;
    font-weight: 700;
    margin: 8px 0 4px;
  }

  .stat-label {
    font-size: 0.75rem;
    color: #757575;
    text-transform: uppercase;
  }
}

.days-trained {
  background: linear-gradient(135deg, rgba(25, 118, 210, 0.1) 0%, rgba(25, 118, 210, 0.05) 100%);
  border-radius: 12px;
  padding: 20px;
}

.blocks-breakdown {
  background: #fafafa;
  border-radius: 12px;
  padding: 16px;
}
</style>
```

Design decisions:
- Fixed header with dark gradient (matches app theme)
- 3-column stat grid for key metrics
- Days trained highlighted prominently (per CONTEXT.md)
- Per-block breakdown with icons and colors matching block identity from Phase 7
- RPE slider directly visible (not in modal, per CONTEXT.md)
- Optional notes field with character limit
- Fixed footer with "Terminar Sesion" button
  </action>
  <verify>
Component renders with mock data:
```vue
<SessionSummary
  date="2026-01-28"
  :total-duration-seconds="3600"
  :blocks-completed="[...]"
  :total-exercises="20"
  :total-days-trained="5"
  @finish="handleFinish"
/>
```
Verify layout, RPE slider works, Done button emits event.
  </verify>
  <done>
SessionSummary shows stats grid, days trained, per-block breakdown, RPE slider, notes field, and Done button.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npm run build` in el-templo-app
2. RpeSlider renders with markers and labels
3. RpeSlider emits update:modelValue on interaction
4. SessionSummary displays all sections correctly
5. SessionSummary emits finish event with RPE and notes
6. Layout is scrollable with fixed footer
</verification>

<success_criteria>
- RpeSlider has 1-10 scale with labels at 2, 4, 6, 8, 10
- RpeSlider shows dynamic description for selected value
- SessionSummary displays: duration, blocks, exercises, days trained
- SessionSummary shows per-block breakdown with timing
- Done button emits finish event with { rpe, notes }
</success_criteria>

<output>
After completion, create `.planning/phases/10-session-completion/10-03-SUMMARY.md`
</output>
