---
phase: 28-r2-video-upload-infrastructure
plan: 03
type: execute
wave: 3
depends_on: [28-02]
files_modified:
  - el-templo-admin/src/components/exercises/BulkUploadDialog.vue
  - el-templo-admin/src/pages/ExercisesPage.vue
autonomous: false
requirements: [SC-02, SC-04]

must_haves:
  truths:
    - "Coach can select multiple MP4 files for bulk upload"
    - "Filenames are parsed to auto-match exercises by ID prefix"
    - "Preview table shows matched file-to-exercise mappings before upload"
    - "Unmatched files are flagged with option for manual exercise assignment"
    - "Coach confirms matches before uploads begin"
    - "Bulk upload shows per-file progress and overall progress"
    - "Exercises without video show in member app VideoPlaceholder (placeholder state)"
    - "Exercises with uploaded video display video in member app DayPlayer"
    - "Admin exercise list shows correct video status badges"
  artifacts:
    - path: "el-templo-admin/src/components/exercises/BulkUploadDialog.vue"
      provides: "Bulk upload dialog with file matching, preview, and batch upload"
      contains: "BulkUploadDialog"
  key_links:
    - from: "el-templo-admin/src/components/exercises/BulkUploadDialog.vue"
      to: "el-templo-admin/src/composables/useVideoUpload.ts"
      via: "reuses uploadVideo for each matched file"
      pattern: "uploadVideo"
    - from: "el-templo-admin/src/pages/ExercisesPage.vue"
      to: "el-templo-admin/src/components/exercises/BulkUploadDialog.vue"
      via: "dialog opened from Subida Masiva button"
      pattern: "BulkUploadDialog"
---

<objective>
Build the bulk upload dialog for batch video uploads with filename-to-exercise matching, preview/validation table, manual assignment for unmatched files, and batch progress tracking. Then verify the full end-to-end flow from upload to member app video playback.

Purpose: Enable coaches to upload hundreds of videos efficiently by selecting multiple files, reviewing auto-matched exercises, and uploading in batch. This is the primary workflow for the initial 300-video upload and ongoing 1500-video goal.

Output: BulkUploadDialog.vue component, wired into ExercisesPage "Subida Masiva" button, plus end-to-end verification.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-r2-video-upload-infrastructure/28-RESEARCH.md
@.planning/phases/28-r2-video-upload-infrastructure/28-01-SUMMARY.md
@.planning/phases/28-r2-video-upload-infrastructure/28-02-SUMMARY.md

Key existing files to reference:
@el-templo-admin/src/pages/ExercisesPage.vue (parent page to wire dialog into)
@el-templo-admin/src/composables/useVideoUpload.ts (reuse uploadVideo method)
@el-templo-admin/src/composables/useExercisesApi.ts (fetch exercises for manual matching)
@el-templo-admin/src/types/exercise.ts (Exercise type)
</context>

<tasks>

<task type="auto">
  <name>Task 1: BulkUploadDialog component with filename matching, preview, manual assignment, and batch upload</name>
  <files>
    el-templo-admin/src/components/exercises/BulkUploadDialog.vue
    el-templo-admin/src/pages/ExercisesPage.vue
  </files>
  <action>
**1. Create BulkUploadDialog.vue** (`src/components/exercises/BulkUploadDialog.vue`):

Props:

- `modelValue: boolean` (v-model for dialog visibility)
- `exercises: Exercise[]` (full exercise list for matching — fetched by parent or passed down)

Emits:

- `update:modelValue`
- `upload-complete` (when all uploads finish, parent refreshes list)

**Dialog structure (QDialog, persistent when uploading):**

**Phase 1 — File Selection:**

- Large drop area or QFile with `multiple` and `accept=".mp4"` for selecting files
- "Seleccionar Archivos" button
- After files selected, parse each filename using the convention `{exerciseId}-{name-slug}.mp4`:
  - Extract the numeric prefix before the first `-` as exerciseId
  - Look up exerciseId in the `exercises` prop array
  - If found: status = "matched", show exercise name
  - If not found (bad ID, no ID prefix, non-numeric prefix): status = "unmatched"
- Client-side validation per file: check size <= 20MB, check extension is .mp4. Flag violations.
- Transition to Phase 2

**Phase 2 — Preview / Validation Table:**

- QTable showing all selected files with columns:
  - Archivo (filename)
  - Estado (matched / unmatched / error)
  - Ejercicio (matched exercise name, or manual assignment dropdown for unmatched)
  - Tamano (file size in MB, formatted)
- Matched files show green check icon + exercise name
- Unmatched files show orange warning icon + QSelect dropdown for manual exercise assignment:
  - QSelect with `use-input` for searching exercises by name
  - `filter` function searches exercise list client-side
  - Once exercise selected, status changes to "matched (manual)"
- Files with validation errors (>20MB) show red error icon and are excluded from upload
- Summary line: "X archivos listos, Y sin asignar, Z con errores"
- "Subir X Videos" button (primary, enabled only when at least 1 file is matched)
- "Cancelar" button
- Option to remove individual files from the list (X button per row)

**Phase 3 — Upload Progress:**

- Replace preview table with progress view
- Per-file progress: filename, progress bar (QLinearProgress), status icon (uploading/done/failed)
- Overall progress: "Subiendo X de Y videos" with QLinearProgress for overall percentage
- Each matched file uploads sequentially (to avoid overwhelming R2 / API):
  1. Call `useVideoUpload.uploadVideo(exerciseId, file)` for each file
  2. Update per-file status as each completes
  3. On error for a file: mark it failed, continue with next (don't abort batch)
- "Cerrar" button enabled only when all uploads complete (or all remaining are failed)
- After close, emit `upload-complete` so parent refreshes exercise list

**Filename parsing function:**

```typescript
function parseFilename(filename: string): {
  exerciseId: number | null;
  slug: string;
} {
  const match = filename.match(/^(\d+)-(.+)\.mp4$/i);
  if (!match) return { exerciseId: null, slug: filename };
  return { exerciseId: parseInt(match[1], 10), slug: match[2] };
}
```

**2. Wire into ExercisesPage.vue:**

- Import BulkUploadDialog
- Add `showBulkUpload` ref (boolean)
- Replace the placeholder "Subida Masiva" button/toast with actual functionality:
  - Button opens dialog: `showBulkUpload = true`
  - Before opening: fetch full exercise list (all pages, no filters) if not already cached. Pass to dialog as prop. This is needed for filename matching and manual assignment dropdown.
  - OR: fetch all exercises in a single API call with high limit (the exercises table has ~1500 rows max, which is manageable as a single response for matching purposes). Add a `fetchAllExercises()` method to useExercisesApi that calls with `limit: 2000` to get all exercises at once.
- Add `<BulkUploadDialog v-model="showBulkUpload" :exercises="allExercises" @upload-complete="refreshList" />`
- On `upload-complete` event, refresh the current page of the exercise list

**Add `fetchAllExercises()` to useExercisesApi composable:**
Calls GET `/admin/exercises?limit=2000` to fetch all exercises in one call for bulk matching. Cache the result so repeated dialog opens don't re-fetch. Only include id, exercise (name), videoUrl fields needed for matching.
</action>
<verify>
TypeScript compiles: `cd el-templo-admin && npx tsc --noEmit`
"Subida Masiva" button opens dialog.
Selecting multiple MP4 files shows preview table with matched/unmatched status.
Unmatched files allow manual exercise assignment via searchable dropdown.
</verify>
<done>
BulkUploadDialog parses filenames, shows preview table with auto-matched and manually-assignable exercises, validates file sizes, uploads batch with per-file progress, handles errors per-file without aborting batch. Wired into ExercisesPage "Subida Masiva" button.
</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end verification of video upload infrastructure</name>
  <action>
Human verification of the complete R2 video upload infrastructure: R2 Fastify plugin, presigned URL upload flow, post-processing (thumbnail + conditional compression), exercise listing API, admin Exercises page with search/filter/single upload/bulk upload, assembleVideoUrl wiring to session and admin APIs. Member app VideoPlaceholder displays videos from R2 URLs.

**Prerequisites:**

- R2 bucket created and configured (CORS, public URL enabled)
- R2 env vars set in el-templo-api .env
- FFmpeg installed on server (or local machine for testing)

**Test 1: Exercise Listing**

1. Navigate to admin app -> Ejercicios
2. Verify exercise list loads with pagination
3. Test search: type an exercise name, verify filtering
4. Test filters: select a category, level, route, contraction type
5. Test video status filter: "Sin Video" should show exercises without videos
6. Verify "Crear Ejercicio" button is visible but disabled

**Test 2: Single Video Upload**

1. Find an exercise without video (grey videocam icon)
2. Click "Subir" button -> file picker opens
3. Select a test MP4 file (< 20MB, < 20s)
4. Verify upload progress shown
5. After completion: row updates to show green videocam icon
6. Click "Ver" -> video opens in new tab from R2 URL

**Test 3: Video Replace**

1. On an exercise with video, click "Reemplazar"
2. Select a different MP4 file
3. Verify old video replaced, new video plays

**Test 4: Video Delete**

1. On an exercise with video, click "Eliminar"
2. Confirm deletion dialog
3. Verify icon changes back to grey (no video)

**Test 5: Bulk Upload**

1. Click "Subida Masiva" button
2. Select 3+ MP4 files with naming convention `{id}-{slug}.mp4`
3. Include 1 file with invalid name (no numeric prefix)
4. Verify preview table: matched files show exercise names, unmatched shows dropdown
5. Manually assign the unmatched file to an exercise
6. Click "Subir" -> verify per-file progress
7. After completion, close dialog -> exercise list refreshes with updated video statuses

**Test 6: Member App Video Playback**

1. Open member app -> navigate to training
2. Select a day with sessions containing exercises that now have videos
3. Verify VideoPlaceholder shows actual video (autoplay, loop, muted)
4. Navigate between exercises -> video updates to match selected exercise

**Test 7: Validation**

1. Try uploading a file > 20MB -> should be rejected with error message
2. Try uploading a non-MP4 file -> should be rejected
3. Try uploading a video > 20s -> should be rejected
   </action>
   <verify>User executes tests 1-7 above and reports results. Type "approved" or describe any issues found.</verify>
   <done>All 7 test scenarios pass. Videos upload from admin, play in member app, and status badges are correct.</done>
   </task>

</tasks>

<verification>
1. `cd el-templo-admin && npx tsc --noEmit` — zero TypeScript errors
2. Bulk upload dialog parses filenames correctly
3. Preview table shows matched/unmatched status
4. Manual exercise assignment works for unmatched files
5. Batch upload proceeds sequentially with per-file progress
6. Failed uploads don't abort the batch
7. Member app displays uploaded videos correctly
8. Admin exercise list reflects current video status
</verification>

<success_criteria>

- Bulk upload dialog fully functional with filename matching and preview
- Manual assignment available for unmatched files
- Batch upload with progress tracking and error resilience
- Full end-to-end flow verified: upload via admin -> video plays in member app
- All 6 success criteria from roadmap verified:
  1. R2 bucket configured for public read
  2. Upload mechanism exists (admin UI with single + bulk)
  3. CDN URL pattern defined (R2 key + R2_PUBLIC_URL assembly)
  4. exercises.video_url populated for uploaded exercises
  5. Videos play in member app DayPlayer
  6. Admin shows video status badges
     </success_criteria>

<output>
After completion, create `.planning/phases/28-r2-video-upload-infrastructure/28-03-SUMMARY.md`
</output>
