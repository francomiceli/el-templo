---
phase: 169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc
plan: 03
type: execute
wave: 3
depends_on: ["169-01", "169-02"]
files_modified:
  - el-templo-api/src/jobs/auto-approve.ts
  - el-templo-api/src/jobs/auto-resume-pauses.ts
  - el-templo-api/src/jobs/notification-cron.ts
autonomous: true
requirements: [CON-04]

must_haves:
  truths:
    - "auto-approve y auto-resume-pauses exponen una función pura runX(db) testeable, extraída del cuerpo del cron.schedule"
    - "Los 3 jobs restantes corren su barrido dentro de forEachActiveTenant, completando los 7 de D-01"
    - "Los 4 schedules de notification-cron pasan por el sweep sin duplicar el envoltorio en cada uno"
    - "seedTemplates() queda FUERA del sweep con anotación /* tenant-safe: … */ y motivo escrito, porque correrlo por tenant duplicaría filas del tenant 1 mientras el service siga siendo global (D-02)"
    - "Ninguna firma de service cambia y el comportamiento con un solo tenant activo es idéntico al actual"
  artifacts:
    - path: "el-templo-api/src/jobs/auto-approve.ts"
      provides: "runAutoApprove(db) exportada + sweep por tenant"
      exports: ["runAutoApprove", "startAutoApproveJob"]
    - path: "el-templo-api/src/jobs/auto-resume-pauses.ts"
      provides: "runAutoResumePauses(db) exportada con los 3 barridos + sweep por tenant"
      exports: ["runAutoResumePauses", "startAutoResumePausesJob"]
    - path: "el-templo-api/src/jobs/notification-cron.ts"
      provides: "los 4 schedules con sweep por tenant y las dos extracciones que faltaban"
      contains: "forEachActiveTenant"
  key_links:
    - from: "el-templo-api/src/jobs/notification-cron.ts"
      to: "el-templo-api/src/modules/shared/tenant.ts"
      via: "import de forEachActiveTenant"
      pattern: 'from "../modules/shared/tenant"'
---

<objective>
Cerrar D-01 con los 3 crons que faltan, que son los que necesitan refactor antes del sweep:
`auto-approve` y `auto-resume-pauses` no tienen función pura exportada (todo vive dentro del
`cron.schedule`, así que hoy son intesteables), y `notification-cron` son 4 `cron.schedule`
dentro de una sola función.

Purpose: es la mayor asimetría entre los 7 jobs (§2b de PATTERNS). Sin la extracción, el test
del criterio 3 del ROADMAP no puede ejercitar estos jobs y quedarían como el hueco que la fase
justamente promete no dejar.
Output: los 7 crons de D-01 completos, con `runX(db)` exportado en los 3 de este plan.

Nota de secuenciación: la dependencia lógica de este plan es solo 169-01. La fase corre
SERIALIZADA —un plan por wave— porque los 9 planes comparten un único worktree y sus tests
son MySQL-backed: dos archivos de vitest a la vez revientan el timeout de 120 s del
provisioning por worker en esta máquina. Por eso `depends_on` suma a 169-02 como dependencia
OPERATIVA, no lógica.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc/169-CONTEXT.md
@.planning/phases/169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc/169-PATTERNS.md
@.planning/phases/169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc/169-01-SUMMARY.md
@.claude/skills/el-templo-change-control/SKILL.md
@CLAUDE.md

<interfaces>
<!-- Contratos que el executor NO tiene que salir a buscar. -->

**API disponible desde `src/modules/shared/tenant.ts` (plan 169-01):**
`forEachActiveTenant(db, log, jobName, run) → { tenants, ok, failed }`, `TenantContext = { tenantId: number }`.

**Extracciones exigidas (patrón de `expire-lost-leads.ts:73-138`: lógica pura exportada + scheduler):**

| Job                            | Estado actual                                                                                                                                          | Funciones que deben existir después                                                                                                                                                                                              |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `auto-approve.ts:20-42`        | todo inline en `cron.schedule`; el `AdminSessionService` se instancia en `:21`                                                                         | `export async function runAutoApprove(db): Promise<{ approved: number }>` (acumulador con sweep) + `runAutoApproveForTenant(db, ctx)` + `startAutoApproveJob(db)` que solo programa y loguea                                     |
| `auto-resume-pauses.ts:29-90`  | inline, 3 barridos con try/catch separados, 6 services construidos en `:30-46`                                                                         | `export async function runAutoResumePauses(db): Promise<{ resumed: number; activated: number; expired: number }>` + `runAutoResumePausesForTenant(db, ctx)` (mantiene los 3 try/catch internos) + `startAutoResumePausesJob(db)` |
| `notification-cron.ts:274-560` | 4 `cron.schedule` en `startNotificationJobs`; `runPlanRenewalWarnings` (:213), `runMorningEnergyForTz` y `runWeeklySummaryForTz` (:509-548) ya existen | `export async function runNotificationQueueTick(db)` (schedule 1) y `export async function runBatchSegmentRecalculation(db)` (schedule 2), extraídos del cuerpo de sus `cron.schedule`; los 4 caminos pasan por el sweep         |

**Dónde va el sweep en `notification-cron` (una sola forma, no cuatro copias):** el envoltorio
`forEachActiveTenant` va DENTRO de cada función `runX` extraída/existente, no en el callback del
`cron.schedule`. Así los 4 schedules quedan idénticos a lo que son hoy (llamar a su `runX` con
try/catch) y el sweep se prueba llamando a la función pura. Para los dos `…ForTz`, el sweep va
dentro de la función `…ForTz`, con el loop de tenant por fuera del cuerpo — mismo criterio que
`mark-no-shows` en el plan 169-02.

**Excepción explícita: `seedTemplates()` (`notification-cron.ts:552-557`).** NO se envuelve en
el sweep. `notification_templates` es gym-owned y su unique pasó a ser compuesta
`(tenant_id, template_key)` en la 168, pero el service sigue insertando global (D-02): correrlo
una vez por tenant activo insertaría N veces las filas del tenant 1. Queda fuera del sweep con
la anotación grepeable `/* tenant-safe: seed de templates global hasta la adopción de
notifications (fase 175) */` y un comentario de una línea explicando el porqué. Formato de la
anotación: `/* tenant-safe: <motivo> */`, motivo obligatorio, nunca la anotación pelada
(doc 03 §3 capa 3 + `src/db/schema/tv.ts:80`).

**Comentario obligatorio en cada job** (mismo bloque que el plan 169-02): Fase 169 (CON-04,
D-01) el barrido corre una vez por tenant ACTIVO; D-02 el `ctx` no baja a los services; y la
advertencia de vencimiento (mientras el cuerpo siga global, más de un tenant activo repetiría
el barrido N veces — por eso el tenant 2 no se onboardea hasta ISO-03 verde).

**Tests existentes a no romper:** `test/notification-plan-renewal.test.ts` y
`test/notifications.test.ts` ejercitan estos caminos. Si alguno se pone rojo, el bug está en la
extracción, no en el test.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extraer runAutoApprove y runAutoResumePauses, y envolverlos en el sweep por tenant</name>
  <files>el-templo-api/src/jobs/auto-approve.ts, el-templo-api/src/jobs/auto-resume-pauses.ts</files>
  <read_first>
    - el-templo-api/src/jobs/auto-approve.ts (worktree: los 42 líneas completas)
    - el-templo-api/src/jobs/auto-resume-pauses.ts (las 90 líneas completas: los 6 services de :30-46 y los 3 try/catch de :52-82)
    - el-templo-api/src/jobs/expire-lost-leads.ts (el patrón `runX` exportada + `startXJob` a imitar, ya con sweep del plan 169-02)
    - el-templo-api/src/modules/shared/tenant.ts (firma de `forEachActiveTenant`)
  </read_first>
  <action>
    **auto-approve.ts** — extraer el cuerpo del `cron.schedule` a
    `runAutoApproveForTenant(db, ctx)`, que instancia el `AdminSessionService` y llama
    `autoApprovePendingSessions()` (misma lógica, mismos logs, ahora con `tenantId` como campo
    estructurado). Exportar `runAutoApprove(db): Promise<{ approved: number }>` que acumula
    sobre `forEachActiveTenant(db, log, "auto-approve", …)`. `startAutoApproveJob(db)` queda
    solo con el `cron.schedule` (`'59 23 * * *'`, timezone `America/Argentina/Buenos_Aires`)
    llamando a `runAutoApprove(db)` con `catch (err: unknown)` + `log.error({ err }, …)`, más
    el `log.info` de "cron programado" que ya tiene.

    **auto-resume-pauses.ts** — extraer a `runAutoResumePausesForTenant(db, ctx)` los 3 barridos
    con SUS TRES try/catch actuales intactos (`autoResumeDuePauses`, `activateDueScheduledSubs`,
    `autoExpireDueSubscriptions`): que un barrido falle no debe impedir los otros dos, igual que
    hoy. Los 6 services (`AuraService`, `BalanceService`, `CashRegisterService`,
    `TransactionService`, `EnrollmentService`, `SubscriptionService`) se construyen dentro de
    esa función (no en `startAutoResumePausesJob`) para que cada vuelta arranque limpia.
    Devolver `{ resumed, activated, expired }`, con 0 en el barrido que haya fallado.
    Exportar `runAutoResumePauses(db)` acumulando sobre
    `forEachActiveTenant(db, log, "auto-resume-pauses", …)`.
    `startAutoResumePausesJob(db)` queda con el `cron.schedule` (`'5 0 * * *'`, timezone AR)
    llamando a la función pura, con su propio `catch`.

    Ojo con el doble nivel de contención: los 3 try/catch internos ya tragan sus errores, así
    que el `catch` de `forEachActiveTenant` casi nunca se va a disparar en este job. Es correcto
    y hay que dejarlo escrito en un comentario para que nadie lo "arregle" después.

    En los dos archivos: bloque de comentario obligatorio de `<interfaces>`, import desde
    `"../modules/shared/tenant"`, cero cambios en firmas de service, cero `any`, cero `console.*`.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-169-tenant-layer/el-templo-api && npx tsc --noEmit && grep -q 'export async function runAutoApprove' src/jobs/auto-approve.ts && grep -q 'export async function runAutoResumePauses' src/jobs/auto-resume-pauses.ts && test "$(grep -v '^\s*[/*]' src/jobs/auto-approve.ts | grep -c 'forEachActiveTenant')" -ge 1 && test "$(grep -v '^\s*[/*]' src/jobs/auto-resume-pauses.ts | grep -c 'forEachActiveTenant')" -ge 1 && test "$(grep -v '^\s*[/*]' src/jobs/auto-resume-pauses.ts | grep -c 'catch')" -ge 3 && test "$(grep -v '^\s*[/*]' src/jobs/auto-approve.ts | grep -c 'cron.schedule')" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `auto-approve.ts` exporta `runAutoApprove` y `startAutoApproveJob`; el `cron.schedule` aparece una sola vez y su callback no contiene lógica de negocio (solo llama a `runAutoApprove` y loguea)
    - `auto-resume-pauses.ts` exporta `runAutoResumePauses` y devuelve `{ resumed, activated, expired }`, conservando los 3 `try/catch` independientes de los 3 barridos
    - Los dos archivos llaman a `forEachActiveTenant` fuera de comentarios y definen su `…ForTenant`
    - Los 6 services de `auto-resume-pauses` se construyen dentro del cuerpo por tenant, no en la función de scheduling
    - Los logs por tenant incluyen `tenantId` como campo del objeto estructurado
    - Hay un comentario que explica por qué el `catch` del sweep casi nunca se dispara en `auto-resume-pauses` (los 3 try/catch internos ya contienen)
    - `npx tsc --noEmit` sale 0 y `startAutoApproveJob` / `startAutoResumePausesJob` conservan su firma `(db)` (`src/index.ts` no cambia)
  </acceptance_criteria>
  <done>Los dos jobs asimétricos tienen función pura exportada y barren por tenant activo, con el timing y los try/catch originales intactos.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep por tenant en los 4 schedules de notification-cron, con seedTemplates exento y anotado</name>
  <files>el-templo-api/src/jobs/notification-cron.ts</files>
  <read_first>
    - el-templo-api/src/jobs/notification-cron.ts (worktree: `runPlanRenewalWarnings` :213-272, `startNotificationJobs` :274-560 completa — los 4 `cron.schedule`, el loop por tz :506-550 y el `seedTemplates` :552-557)
    - el-templo-api/src/modules/shared/tenant.ts (firma de `forEachActiveTenant`)
    - el-templo-api/src/db/schema/tv.ts (:80 — idioma de la anotación `/* tenant-safe: <motivo> */` que dejó escrita la 168)
    - el-templo-api/src/db/tenant-tables.ts (los motivos M8 y la lista de tablas gym-owned: `notification_templates` está entre las convertidas por CON-01)
    - el-templo-api/test/notification-plan-renewal.test.ts (qué afirma hoy sobre `runPlanRenewalWarnings`)
  </read_first>
  <action>
    Extraer dos funciones que hoy viven inline y exportarlas, con el idioma
    `runX(db)` de `expire-lost-leads`:
    - `runNotificationQueueTick(db)` — el cuerpo del schedule 1 (`'*/15 * * * *'`):
      `initFirebase()`, `processQueue()` y `purgeOldNotifications()`, devolviendo lo que hoy
      loguea (`{ sent, failed, purged }`).
    - `runBatchSegmentRecalculation(db)` — el cuerpo del schedule 2 (`'0 3 * * *'`): el barrido
      de `member_profiles` con su loop `for (const profile of profiles)` y su try/catch por
      perfil intactos, más la llamada a `runPlanRenewalWarnings` que hoy hace al final,
      devolviendo `{ transitionsFound, notificationsQueued, ghostReattempts }`.

    Envolver el cuerpo de las CUATRO funciones puras en `forEachActiveTenant`:
    `runNotificationQueueTick`, `runBatchSegmentRecalculation`, `runMorningEnergyForTz` y
    `runWeeklySummaryForTz`, cada una con su `…ForTenant` como cuerpo y el acumulador afuera
    (mismo patrón que los planes 169-02 y esta Task 1). `jobName` distinto por cada una
    (`"notification-queue"`, `"notification-segments"`, `"notification-morning-energy"`,
    `"notification-weekly-summary"`) para que el log diga cuál falló.
    `runPlanRenewalWarnings` NO recibe su propio sweep: se la llama desde dentro del cuerpo por
    tenant de `runBatchSegmentRecalculation`, así que ya corre una vez por tenant. Dejarlo
    escrito en un comentario para que no se agregue un segundo sweep anidado.

    Los 4 `cron.schedule` de `startNotificationJobs` quedan reducidos a: llamar a su función
    pura, loguear el resultado y `catch (err: unknown)` + `log.error`. Los dos que dependen de
    timezone siguen programándose dentro del `for (const tz of tzs)` con
    `getDistinctBranchTimezones(db)` resuelto en el boot, como hoy.

    `seedTemplates()` (:552-557) queda FUERA del sweep, con la anotación
    `/* tenant-safe: seed de templates global hasta la adopción de notifications (fase 175) */`
    en la línea de la llamada y un comentario arriba explicando que envolverla insertaría N
    veces las filas del tenant 1 mientras el service siga siendo global (D-02), y que la unique
    de `notification_templates` ya es compuesta `(tenant_id, template_key)` desde la 168.

    NO cambiar la lista de tenants por tick a una lista resuelta en el boot: `startNotificationJobs`
    sigue siendo `async` solo por `getDistinctBranchTimezones`, y el sweep de tenants se resuelve
    en cada corrida para que activar un gimnasio no exija reiniciar el proceso (mismo espíritu
    que "el tenant no viaja en el JWT", `country-scope.ts:30-31`). `src/index.ts` no cambia.

    Bloque de comentario obligatorio de `<interfaces>` una sola vez, arriba del archivo. Cero
    `any`, cero `console.*`, ninguna firma de service tocada.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-169-tenant-layer/el-templo-api && npx tsc --noEmit && grep -q 'export async function runNotificationQueueTick' src/jobs/notification-cron.ts && grep -q 'export async function runBatchSegmentRecalculation' src/jobs/notification-cron.ts && test "$(grep -v '^\s*[/*]' src/jobs/notification-cron.ts | grep -c 'forEachActiveTenant')" -ge 4 && test "$(grep -c 'tenant-safe: seed de templates' src/jobs/notification-cron.ts)" -ge 1 && test "$(grep -v '^\s*[/*]' src/jobs/notification-cron.ts | grep -c 'cron.schedule')" -eq 4 && npx vitest run test/notification-plan-renewal.test.ts --no-file-parallelism && bash -c 'for f in src/jobs/auto-approve.ts src/jobs/auto-resume-pauses.ts src/jobs/notification-cron.ts; do grep -v "^\s*[/*]" "$f" | grep -qE ":\s*any\b|as any|<any>" && { echo "any explicito en $f"; exit 1; }; done; exit 0'</automated>
  </verify>
  <acceptance_criteria>
    - `notification-cron.ts` exporta `runNotificationQueueTick` y `runBatchSegmentRecalculation`, y sigue exportando `runPlanRenewalWarnings` y `startNotificationJobs`
    - Hay al menos 4 llamadas a `forEachActiveTenant` fuera de comentarios, una por cada camino (`queue`, `segments`, `morning-energy`, `weekly-summary`), cada una con un `jobName` distinto
    - `runPlanRenewalWarnings` NO tiene su propio sweep y un comentario explica que ya corre dentro del cuerpo por tenant de `runBatchSegmentRecalculation`
    - Siguen existiendo exactamente 4 `cron.schedule` y ninguno contiene lógica de negocio: solo llamada a la función pura + log + `catch`
    - La llamada a `seedTemplates()` lleva la anotación `/* tenant-safe: seed de templates global hasta la adopción de notifications (fase 175) */` con el motivo escrito, y un comentario explica el riesgo de duplicación si se la envolviera
    - `startNotificationJobs` conserva su firma `(db): Promise<void>` y `src/index.ts` no aparece en `git diff --stat`
    - `npx vitest run test/notification-plan-renewal.test.ts --no-file-parallelism` pasa sin haber tocado el archivo de test
    - `npx tsc --noEmit` sale 0
  </acceptance_criteria>
  <done>Los 4 schedules de notificaciones barren por tenant activo con funciones puras testeables, y la única excepción (seedTemplates) está anotada con motivo.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                     | Description                                            |
| -------------------------------------------- | ------------------------------------------------------ |
| tabla `tenants` → cuerpo de los 3 jobs       | decide si se procesan los datos de un gimnasio         |
| `seedTemplates()` → `notification_templates` | escritura sobre tabla gym-owned sin contexto de tenant |
| cuerpo del job → services de notificaciones  | el `ctx` se detiene acá (D-02) hasta la fase 175       |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                     | Disposition | Mitigation Plan                                                                                                                                                   |
| --------- | ---------------------- | ----------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-169-12  | Information Disclosure | notificaciones enviadas a socios de un tenant suspendido                      | mitigate    | Los 4 caminos salen de `listActiveTenants`; un tenant no activo no entra al sweep                                                                                 |
| T-169-13  | Tampering              | `seedTemplates` duplicando filas del tenant 1 si se lo envolviera en el sweep | mitigate    | Queda deliberadamente fuera del sweep con anotación `/* tenant-safe: … */` y motivo, más el comentario que explica el porqué y la fase que lo cierra (175)        |
| T-169-14  | Denial of Service      | error en un tenant cortando la cola de notificaciones de los demás            | mitigate    | `forEachActiveTenant` contiene por iteración; además los try/catch por perfil de `runBatchSegmentRecalculation` siguen intactos                                   |
| T-169-15  | Tampering              | regresión de timing al extraer las funciones de los `cron.schedule`           | mitigate    | Las 4 expresiones cron y sus timezones se conservan literales; el conteo de `cron.schedule` se verifica en `<verify>` y el test de plan-renewal corre sin ajustes |
| T-169-16  | Elevation of Privilege | barrido global repetido N veces cuando exista un segundo tenant               | accept      | Idéntico con un solo tenant. Anotado en el comentario del archivo; el gate del milestone impide onboardear el tenant 2 antes de ISO-03                            |
| T-169-SC  | Tampering              | instalación de paquetes npm                                                   | mitigate    | Cero dependencias nuevas; `node_modules` por symlink verificado con `cmp` en 169-01                                                                               |

</threat_model>

<verification>
- `npx tsc --noEmit` verde.
- `npx vitest run test/notification-plan-renewal.test.ts --no-file-parallelism` verde sin editar tests.
- `src/index.ts` sin cambios (las firmas `startXJob(db)` se conservan).
- Commit con `git add` por ruta explícita de los 3 archivos, nunca `git add -A`.
</verification>

<success_criteria>

- `auto-approve` y `auto-resume-pauses` tienen función pura exportada y sweep por tenant.
- Los 4 schedules de `notification-cron` pasan por el sweep, con `runNotificationQueueTick` y `runBatchSegmentRecalculation` extraídos y exportados.
- `seedTemplates()` exento con anotación grepeable y motivo.
- Con estos 3, los 7 jobs de D-01 están cubiertos (el gate de cobertura lo verifica 169-04).
  </success_criteria>

<output>
Create `.planning/phases/169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc/169-03-SUMMARY.md` when done
</output>
</content>
