---
phase: 169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc
plan: 07
type: execute
wave: 4
depends_on: ["169-01"]
files_modified:
  - el-templo-api/src/db/scripts/require-tenant.ts
  - el-templo-api/test/unit/require-tenant.test.ts
  - el-templo-api/scripts/seed-onboarding-aura.ts
  - el-templo-api/src/db/run-migrations.ts
  - el-templo-api/src/db/scripts/verify-tenant-backfill.ts
  - el-templo-api/src/db/scripts/verify-tenant-uniques.ts
  - el-templo-api/src/db/seed.ts
  - el-templo-api/src/db/seed-spom.ts
  - el-templo-api/scripts/wellhub-sandbox.ts
autonomous: true
requirements: [CON-04]

must_haves:
  truths:
    - "Existe un helper compartido que parsea --tenant=<id> y aborta con exit code 2 cuando falta (D-06)"
    - "El helper valida contra la DB que el tenant EXISTA antes de escribir nada, y NO exige status active (D-07)"
    - "seed-onboarding-aura.ts exige --tenant y usa tenantWhere en su SELECT y tenantValues en su INSERT: es el ejemplar de los dos helpers en un camino sin request"
    - "Los 6 scripts exentos llevan /* tenant-safe: <motivo> */ con motivo escrito y grepeable"
    - "El parser y la validación están cubiertos por un test unitario que no necesita MySQL"
  artifacts:
    - path: "el-templo-api/src/db/scripts/require-tenant.ts"
      provides: "parseTenantArg, requireTenant, queryFnFromConnection, TenantArgError, failTenantArg"
      exports:
        [
          "parseTenantArg",
          "requireTenant",
          "queryFnFromConnection",
          "TenantArgError",
          "failTenantArg",
        ]
    - path: "el-templo-api/test/unit/require-tenant.test.ts"
      provides: "Cobertura del parser y de la validación contra un query fn falso"
      min_lines: 90
  key_links:
    - from: "el-templo-api/scripts/seed-onboarding-aura.ts"
      to: "el-templo-api/src/db/scripts/require-tenant.ts"
      via: "requireTenant(queryFnFromConnection(connection)) antes de cualquier escritura"
      pattern: "requireTenant"
---

<objective>
Cerrar la última superficie sin request de CON-04: los scripts CLI que escriben tablas
gym-owned. Un helper compartido exige `--tenant=<id>`, aborta sin él, valida contra la DB que el
tenant exista (typo = corte antes de escribir nada) y devuelve el `TenantContext`. El retrofit
de `seed-onboarding-aura.ts` es el ejemplar que demuestra `tenantWhere` + `tenantValues` fuera
de un request, y los 6 scripts de plataforma quedan exentos con motivo escrito.

Purpose: hoy `seed-onboarding-aura.ts` escribe `aura_config` cayendo en el `DEFAULT 1` sin
declararlo. Con dos gimnasios, ese script escribe en el gimnasio equivocado sin decir una
palabra.
Output: `require-tenant.ts`, el retrofit, las 6 anotaciones y el test unitario del parser.

Nota de secuenciación: la dependencia lógica es solo 169-01; la wave 4 es serialización
operativa del worktree compartido (archivos disjuntos de los del plan 169-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc/169-CONTEXT.md
@.planning/phases/169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc/169-PATTERNS.md
@.planning/phases/169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc/169-01-SUMMARY.md
@.claude/skills/el-templo-change-control/SKILL.md
@CLAUDE.md

<interfaces>
<!-- Contratos que el executor NO tiene que salir a buscar. -->

**Superficie pública de `src/db/scripts/require-tenant.ts`:**

| Export                                                                         | Forma                                                                                                                      | Semántica                                                                                           |
| ------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| `TenantQueryFn`                                                                | `(sql: string, params?: unknown[]) => Promise<Array<Record<string, unknown>>>`                                             | mismo idioma de `QueryFn` que ya usa `verify-tenant-uniques.ts`: hace testeable el helper sin MySQL |
| `queryFnFromConnection(connection)`                                            | adapta una `Connection` de `mysql2/promise` a `TenantQueryFn`                                                              | el `createSingleConnection()` de `src/db/index.ts:23-30` devuelve esa `connection`                  |
| `TenantArgError`                                                               | `class ... extends Error` con `readonly exitCode = 2`                                                                      | error de USO, no de datos                                                                           |
| `parseTenantArg(argv: string[]): number`                                       | acepta `--tenant=<id>` y `--tenant <id>`; lanza `TenantArgError` si falta, si no es entero o si es `<= 0`                  |                                                                                                     |
| `requireTenant(query: TenantQueryFn, argv?: string[]): Promise<TenantContext>` | parsea, hace `SELECT id, status FROM tenants WHERE id = ?`, lanza `TenantArgError` si no existe, y devuelve `{ tenantId }` | si el status no es `active`, **avisa por stderr y sigue** (D-07)                                    |
| `failTenantArg(err: unknown, scriptName: string): never`                       | imprime a stderr y `process.exit(err instanceof TenantArgError ? err.exitCode : 1)`                                        | wrapper fino para el `main().catch()`                                                               |

**Códigos de salida ya convencionados en el repo** (`verify-tenant-uniques.ts:47-48`):
**0** OK, **1** discrepancias/fallo de datos, **2** error de conexión o de USO. Falta de
`--tenant` y tenant inexistente son error de uso → **2**, nunca 1.

**D-07, contrato deliberadamente distinto al de crons y webhook:** el helper valida que el
tenant EXISTA (un typo de id corta antes de escribir nada) pero **NO exige `status = 'active'`**.
El CLI es tooling de operador y puede necesitar tocar un gimnasio suspendido (export, limpieza).
Escribirlo en el docblock para que nadie lo "unifique" después con el criterio de los crons.

**Los 6 scripts EXENTOS (solo anotación, cero cambios de comportamiento)** — formato
`/* tenant-safe: <motivo> */`, motivo obligatorio, nunca la anotación pelada:

| Archivo                                    | Motivo a escribir                                                                                        |
| ------------------------------------------ | -------------------------------------------------------------------------------------------------------- |
| `src/db/run-migrations.ts`                 | herramienta de plataforma: aplica DDL a la base entera, no escribe filas de negocio                      |
| `src/db/scripts/verify-tenant-backfill.ts` | verificador de plataforma: escanea TODOS los tenants por diseño, corre en CI/deploy y es de solo lectura |
| `src/db/scripts/verify-tenant-uniques.ts`  | ídem                                                                                                     |
| `src/db/seed.ts`                           | provisioning local/de test: construye la base desde cero, incluido el tenant 1                           |
| `src/db/seed-spom.ts`                      | ídem                                                                                                     |
| `scripts/wellhub-sandbox.ts`               | no toca la DB: postea al webhook local, que resuelve el tenant por su propio camino (fase 169, plan 05)  |

Van en el docblock de cabecera de cada archivo (una línea, arriba), no adentro de una función.
Registrados en `package.json:12-20` como `db:migrate`, `db:verify-tenant`, `db:verify-uniques`,
`db:seed`, `seed:spom`.

**Retrofit de `scripts/seed-onboarding-aura.ts` (43 líneas, el ejemplar de D-06).** Hoy:
`createSingleConnection()` → `select ... where eq(auraConfig.sourceType, "onboarding_completion")`
→ `insert(auraConfig).values({...})`, todo cayendo en el `DEFAULT 1` sin declararlo. Después:
`requireTenant(queryFnFromConnection(connection))` antes de tocar nada,
`and(tenantWhere(auraConfig, ctx), eq(auraConfig.sourceType, …))` en el SELECT —con
`tenantWhere` como PRIMER término, que es la convención— y `tenantValues(ctx, {...})` en el
INSERT. Actualizar la línea de uso del docblock a
`npx tsx scripts/seed-onboarding-aura.ts --tenant=1`. No se agrega script nuevo a `package.json`.

⚠️ **`createSingleConnection()` devuelve `drizzle(connection)` SIN el schema tipado**
(`src/db/index.ts:23-30`), distinto de `app.db`. `tenantWhere` y `tenantValues` son funciones
puras que reciben el objeto de tabla importado, así que funcionan igual — pero el executor tiene
que confirmarlo con `npx tsc --noEmit` y no asumirlo.

**`console.*` en scripts standalone está permitido** (precedente: `verify-tenant-uniques.ts:464-472`).
La prohibición de CLAUDE.md es para el runtime de Fastify y los frontends, no para el CLI.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helper require-tenant.ts + test unitario del parser y de la validación</name>
  <files>el-templo-api/src/db/scripts/require-tenant.ts, el-templo-api/test/unit/require-tenant.test.ts</files>
  <read_first>
    - el-templo-api/src/db/scripts/verify-tenant-uniques.ts (worktree: el idioma de script standalone — `QueryFn`, `main()` con carga de env :580-620, `stepDatabaseGuard`, los códigos de salida de :47-48 y el `require.main === module` de :464-472)
    - el-templo-api/src/db/index.ts (`createSingleConnection` :23-30: qué devuelve exactamente)
    - el-templo-api/src/modules/shared/tenant.ts (`TenantContext`)
    - el-templo-api/src/db/schema/tenants.ts (columnas `id` y `status`)
    - el-templo-api/test/unit/ (idioma de los tests unitarios sin DB del repo)
  </read_first>
  <behavior>
    - `parseTenantArg(["--tenant=7"])` y `parseTenantArg(["--tenant", "7"])` devuelven `7`.
    - `parseTenantArg([])`, `["--tenant"]`, `["--tenant=abc"]`, `["--tenant=0"]` y `["--tenant=-3"]` lanzan `TenantArgError` con `exitCode` 2.
    - `requireTenant(query, ["--tenant=1"])` con un query fn que devuelve `[{ id: 1, status: "active" }]` resuelve `{ tenantId: 1 }`.
    - `requireTenant(query, ["--tenant=42"])` con un query fn que devuelve `[]` lanza `TenantArgError` y el mensaje nombra el id inexistente.
    - `requireTenant(query, ["--tenant=9"])` con `status: "suspended"` resuelve igual `{ tenantId: 9 }` y avisa por stderr (D-07).
    - El SQL que `requireTenant` le pasa al query fn es parametrizado (el id viaja en `params`, no interpolado en el string).
  </behavior>
  <action>
    Crear `src/db/scripts/require-tenant.ts` con la superficie exacta de la tabla de
    `<interfaces>`. Docblock de cabecera con el idioma del milestone (secciones en MAYÚSCULA
    citando fase y decisión) que explique: QUÉ resuelve (D-06: los scripts que escriben tablas
    gym-owned exigen el tenant o abortan), POR QUÉ no exige `active` (D-07, contrato distinto al
    de crons/webhook, con el motivo de operador escrito), y LA LISTA de scripts exentos con su
    motivo, apuntando a que la anotación `/* tenant-safe: … */` es lo que el sentinel de la
    fase 170 va a leer.

    `parseTenantArg` soporta las dos formas (`--tenant=<id>` y `--tenant <id>`) y valida con
    `Number.isInteger` + `> 0`. Los mensajes de error tienen que ser accionables: decir el uso
    correcto (`--tenant=<id>`) y, cuando el tenant no existe, nombrar el id que se pidió.

    `requireTenant` hace `SELECT id, status FROM tenants WHERE id = ?` PARAMETRIZADO (el id nunca
    interpolado en el string: es entrada de línea de comandos). Si no hay fila → `TenantArgError`.
    Si hay fila con status distinto de `active` → `console.warn` a stderr con el status y seguir.

    `queryFnFromConnection` adapta la `Connection` de `mysql2/promise` sin cambiarle nada al
    llamador. `failTenantArg(err, scriptName)` imprime a stderr con el nombre del script y sale
    con 2 para `TenantArgError`, 1 para cualquier otro error.

    Crear `test/unit/require-tenant.test.ts` cubriendo los 6 puntos de `<behavior>` con un
    `TenantQueryFn` falso (una función que registra el SQL y los params recibidos y devuelve lo
    que el test le indique). Sin MySQL, sin `createTestApp`: es un test unitario puro y tiene que
    correr en menos de un segundo. Espiar `console.warn` para afirmar el aviso del tenant no
    activo, y afirmar sobre los `params` capturados que el id viajó parametrizado.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-169-tenant-layer/el-templo-api && npx tsc --noEmit && npx vitest run test/unit/require-tenant.test.ts --no-file-parallelism && test "$(grep -v '^\s*[/*]' src/db/scripts/require-tenant.ts | grep -c 'exitCode = 2')" -ge 1 && test "$(grep -v '^\s*[/*]' src/db/scripts/require-tenant.ts | grep -cE 'status.*!==.*active.*throw|throw.*status')" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/scripts/require-tenant.ts` exporta `TenantQueryFn`, `queryFnFromConnection`, `TenantArgError`, `parseTenantArg`, `requireTenant` y `failTenantArg`
    - `TenantArgError` lleva `exitCode = 2` y el helper NUNCA usa 1 para un error de uso
    - `requireTenant` NO lanza cuando el status no es `active`: avisa y devuelve el contexto (D-07), y el docblock explica por qué el contrato difiere del de crons/webhook
    - El SELECT de `tenants` es parametrizado (el test afirma sobre los `params` capturados, no sobre el string)
    - `npx vitest run test/unit/require-tenant.test.ts --no-file-parallelism` pasa con al menos 8 tests y sin tocar MySQL
    - El docblock lista los 6 scripts exentos con su motivo y menciona que la fase 170 lee las anotaciones
    - `npx tsc --noEmit` sale 0
  </acceptance_criteria>
  <done>El helper existe con el contrato de D-06/D-07, aborta con exit 2 y está cubierto por tests sin dependencia de DB.</done>
</task>

<task type="auto">
  <name>Task 2: Retrofit de seed-onboarding-aura y anotación de los 6 scripts exentos</name>
  <files>el-templo-api/scripts/seed-onboarding-aura.ts, el-templo-api/src/db/run-migrations.ts, el-templo-api/src/db/scripts/verify-tenant-backfill.ts, el-templo-api/src/db/scripts/verify-tenant-uniques.ts, el-templo-api/src/db/seed.ts, el-templo-api/src/db/seed-spom.ts, el-templo-api/scripts/wellhub-sandbox.ts</files>
  <read_first>
    - el-templo-api/scripts/seed-onboarding-aura.ts (worktree: las 43 líneas completas)
    - el-templo-api/src/db/scripts/require-tenant.ts (lo escrito en Task 1)
    - el-templo-api/src/modules/shared/tenant.ts (`tenantWhere`, `tenantValues`, convención de `and(tenantWhere(...), ...resto)`)
    - el-templo-api/src/db/index.ts (`createSingleConnection`: el `db` viene SIN schema tipado)
    - el-templo-api/src/db/schema/tv.ts (:80 — idioma de la anotación `/* tenant-safe: <motivo> */`)
    - el-templo-api/src/db/run-migrations.ts, src/db/seed.ts, src/db/seed-spom.ts, scripts/wellhub-sandbox.ts, src/db/scripts/verify-tenant-backfill.ts, src/db/scripts/verify-tenant-uniques.ts (los docblocks de cabecera de los 6, para insertar la anotación sin romper el estilo de cada uno)
  </read_first>
  <action>
    **Retrofit de `scripts/seed-onboarding-aura.ts`:** después de `createSingleConnection()` y
    ANTES de cualquier query, llamar
    `const ctx = await requireTenant(queryFnFromConnection(connection));`. El SELECT pasa a
    `where(and(tenantWhere(auraConfig, ctx), eq(auraConfig.sourceType, "onboarding_completion")))`
    con `tenantWhere` como PRIMER término (convención del doc 03 §3). El INSERT pasa a
    `values(tenantValues(ctx, { sourceType: …, defaultAmount: 50, description: …, isActive: true }))`.
    Los mensajes de `console.log` suman el `tenantId` para que el operador vea en qué gimnasio
    escribió. El `main().catch(...)` pasa por `failTenantArg(err, "seed-onboarding-aura")` para
    que la falta de `--tenant` salga con 2 y no con 1. Actualizar la línea de uso del docblock a
    `npx tsx scripts/seed-onboarding-aura.ts --tenant=1` y agregar una línea explicando que el
    script es idempotente POR TENANT.

    **Los 6 exentos:** agregar en el docblock de cabecera de cada archivo la anotación
    `/* tenant-safe: <motivo> */` con el motivo EXACTO de la tabla de `<interfaces>`, más una
    línea que aclare que la fase 170 (sentinel + lint) es la que las lee. Cero cambios de
    comportamiento en estos 6 archivos: solo comentarios. Verificar que `db:migrate`,
    `db:verify-tenant` y `db:verify-uniques` sigan corriendo (los dos verificadores en modo
    lectura contra la base local).

    No agregar scripts nuevos a `package.json`.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-169-tenant-layer/el-templo-api && npx tsc --noEmit && for f in src/db/run-migrations.ts src/db/scripts/verify-tenant-backfill.ts src/db/scripts/verify-tenant-uniques.ts src/db/seed.ts src/db/seed-spom.ts scripts/wellhub-sandbox.ts; do grep -q 'tenant-safe:' "$f" || { echo "FALTA anotacion en $f"; exit 1; }; done && grep -q 'requireTenant' scripts/seed-onboarding-aura.ts && grep -q 'tenantValues' scripts/seed-onboarding-aura.ts && grep -q 'tenantWhere' scripts/seed-onboarding-aura.ts && test "$(git -C /home/franco/projects/et-169-tenant-layer diff --numstat -- el-templo-api/src/db/run-migrations.ts el-templo-api/src/db/seed.ts el-templo-api/src/db/seed-spom.ts | awk '{s+=$2} END {print s+0}')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - Los 6 scripts exentos contienen `/* tenant-safe: <motivo> */` en su docblock de cabecera, cada uno con el motivo de la tabla de `<interfaces>` y ninguno con la anotación pelada
    - `git diff --numstat` sobre `run-migrations.ts`, `seed.ts` y `seed-spom.ts` muestra 0 líneas borradas (solo se agregaron comentarios)
    - `scripts/seed-onboarding-aura.ts` llama a `requireTenant` ANTES de la primera query (verificable por orden de líneas) y usa `tenantWhere` en el SELECT y `tenantValues` en el INSERT
    - En el SELECT del retrofit, `tenantWhere` es el PRIMER término del `and(...)`
    - Correr `npx tsx scripts/seed-onboarding-aura.ts` SIN `--tenant` termina con exit code 2 y un mensaje que indica el uso correcto; con `--tenant=999999` (inexistente) también sale 2 nombrando el id
    - Correr `npx tsx scripts/seed-onboarding-aura.ts --tenant=1` contra la base local escribe o saltea la fila de `aura_config` y el mensaje incluye el `tenantId`
    - `npx tsc --noEmit` sale 0 (el `db` sin schema tipado de `createSingleConnection` compila con los helpers)
  </acceptance_criteria>
  <done>El ejemplar de D-06 exige el tenant y usa los dos helpers, y las 6 exenciones quedan grepeables con motivo.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                       | Description                                                                                        |
| ---------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| línea de comandos del operador → MySQL         | argumento humano que decide en qué gimnasio se escribe                                             |
| script CLI → tablas gym-owned                  | escritura sin request, sin scope y sin auditoría de sesión                                         |
| base local vs `eltemplo_staging` vs `eltemplo` | staging y prod comparten host MySQL: un script apuntado a la base equivocada escribe en producción |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                               | Disposition | Mitigation Plan                                                                                                                                                                                                                               |
| --------- | ---------------------- | ----------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-169-32  | Elevation of Privilege | script escribiendo en el gimnasio equivocado por el `DEFAULT 1`         | mitigate    | `--tenant` obligatorio con corte en exit 2 antes de cualquier query de escritura, más `tenantWhere`/`tenantValues` en el retrofit                                                                                                             |
| T-169-33  | Tampering              | typo en el id de tenant escribiendo en un gimnasio real ajeno           | mitigate    | `requireTenant` valida contra la DB que el tenant EXISTA antes de escribir nada, y el mensaje nombra el id pedido                                                                                                                             |
| T-169-34  | Tampering              | SQL injection por el argumento de línea de comandos                     | mitigate    | El SELECT de `tenants` es parametrizado y el test afirma sobre los `params` capturados, no sobre el string armado                                                                                                                             |
| T-169-35  | Denial of Service      | operador bloqueado para tocar un gimnasio suspendido (export, limpieza) | accept      | D-07: el helper NO exige `active`, solo avisa. Contrato deliberadamente distinto al de crons y webhook, escrito en el docblock                                                                                                                |
| T-169-36  | Repudiation            | exención de script indistinguible de un olvido                          | mitigate    | Formato `/* tenant-safe: <motivo> */` con motivo obligatorio en los 6, verificado archivo por archivo en `<verify>`                                                                                                                           |
| T-169-37  | Tampering              | script corrido contra la base equivocada del host compartido            | accept      | Fuera del alcance de esta fase: los scripts ya cargan env por `NODE_ENV` como `run-migrations.ts`. El guard de base (`SELECT DATABASE()`) queda como precedente de `verify-tenant-uniques.ts` para quien agregue un script de escritura nuevo |
| T-169-SC  | Tampering              | instalación de paquetes npm                                             | mitigate    | Cero dependencias nuevas; `node_modules` por symlink verificado en 169-01                                                                                                                                                                     |

</threat_model>

<verification>
- `npx tsc --noEmit` verde.
- `npx vitest run test/unit/require-tenant.test.ts --no-file-parallelism` verde.
- Las 3 corridas manuales de `seed-onboarding-aura.ts` (sin flag, id inexistente, `--tenant=1`) con los exit codes esperados, registradas en el SUMMARY.
- Los 6 exentos con la anotación y sin cambios de comportamiento.
- Commit con `git add` por ruta explícita de los 9 archivos, nunca `git add -A`.
</verification>

<success_criteria>

- `require-tenant.ts` con `--tenant` obligatorio, exit 2 por error de uso y validación de existencia sin exigir `active` (D-06, D-07).
- `seed-onboarding-aura.ts` retrofiteado como ejemplar de `tenantWhere` + `tenantValues` sin request.
- Los 6 scripts de plataforma exentos con motivo grepeable.
- Test unitario del parser y de la validación corriendo sin MySQL.
  </success_criteria>

<output>
Create `.planning/phases/169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc/169-07-SUMMARY.md` when done
</output>
</content>
