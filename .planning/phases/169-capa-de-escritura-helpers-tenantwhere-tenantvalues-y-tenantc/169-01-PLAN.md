---
phase: 169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/shared/tenant.ts
  - el-templo-api/test/tenancy/tenant-helpers.test.ts
autonomous: true
requirements: [CON-03, CON-04]

must_haves:
  truths:
    - "Existe un worktree propio basado en origin/master (1200b8af) con node_modules resuelto sin haber corrido ningún install"
    - "tenantWhere y tenantValues viven en src/modules/shared/tenant.ts con la firma EXACTA del doc 03 §3 y aceptan indistintamente el scope de un request y un TenantContext (CON-03)"
    - "Un scope con tenantId null NO produce una query sin filtro: assertTenant corta con 403 TENANT_UNRESOLVED antes de tocar datos"
    - "listActiveTenants devuelve solo los tenants con status 'active' (comparación contra 'active', no contra la lista de estados malos)"
    - "forEachActiveTenant sigue con el próximo tenant cuando uno falla y reporta cuántos fallaron (D-03)"
  artifacts:
    - path: "el-templo-api/src/modules/shared/tenant.ts"
      provides: "tenantWhere, tenantValues, assertTenant, TenantContext, listActiveTenants, forEachActiveTenant"
      exports:
        [
          "tenantWhere",
          "tenantValues",
          "assertTenant",
          "listActiveTenants",
          "forEachActiveTenant",
          "TenantContext",
          "TenantId",
          "TENANT_UNRESOLVED",
        ]
      min_lines: 120
    - path: "el-templo-api/test/tenancy/tenant-helpers.test.ts"
      provides: "Cobertura unitaria de los 5 exports + iterador contra MySQL real con 2 tenants"
      min_lines: 180
  key_links:
    - from: "el-templo-api/src/modules/shared/tenant.ts"
      to: "el-templo-api/src/modules/shared/country-scope.ts"
      via: "assertTenant narrowing de CountryScope.tenantId (number | null) a TenantId (number)"
      pattern: "assertTenant"
    - from: "el-templo-api/src/modules/shared/tenant.ts"
      to: "tabla tenants"
      via: "listActiveTenants: SELECT id FROM tenants WHERE status = 'active'"
      pattern: "schema\\.tenants\\.status"
---

<objective>
Crear la base de la fase: el worktree propio sobre `origin/master` y el archivo
`src/modules/shared/tenant.ts` — la ÚNICA API de lectura/escritura con tenant del sistema,
con la firma exacta que ya fijó el doc 03 §3, más el narrowing fail-closed que resuelve la
fricción entre `CountryScope.tenantId: number | null` y la firma lockeada `{ tenantId: number }`,
más el iterador de tenants activos que los 7 crons van a consumir.

Purpose: los 8 planes siguientes importan de este archivo. Si la firma o el fail-closed
salen mal acá, salen mal en los crons, en el webhook, en el CLI y en el TV a la vez.
Output: worktree `et-169-tenant-layer` en la rama `feat/169-capa-escritura`,
`src/modules/shared/tenant.ts` y `test/tenancy/tenant-helpers.test.ts` en verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc/169-CONTEXT.md
@.planning/phases/169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc/169-PATTERNS.md
@.claude/skills/el-templo-change-control/SKILL.md
@CLAUDE.md

<interfaces>
<!-- Contrato que el executor NO tiene que salir a buscar ni re-diseñar. -->

**Superficie pública EXACTA de `src/modules/shared/tenant.ts`** (nombres no negociables):

| Export                                                                                                                                | Forma                                                                                  | Origen del contrato                                                                                   |
| ------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| `TenantId`                                                                                                                            | alias de tipo = `number`                                                               | doc 03 §3 (código propuesto)                                                                          |
| `TenantContext`                                                                                                                       | `{ tenantId: TenantId }`                                                               | CONTEXT: `{ tenantId }` plano, una sola API                                                           |
| `TENANT_UNRESOLVED`                                                                                                                   | constante string `"TENANT_UNRESOLVED"`                                                 | hermano de `TENANT_SUSPENDED` (country-scope.ts:20)                                                   |
| `tenantWhere<T extends { tenantId: AnyMySqlColumn }>(table: T, scope: { tenantId: TenantId })`                                        | devuelve `eq(table.tenantId, scope.tenantId)`                                          | doc 03 §3, LOCKEADA                                                                                   |
| `tenantValues<V extends Record<string, unknown>>(scope: { tenantId: TenantId }, values: V)`                                           | devuelve `{ ...values, tenantId: scope.tenantId }` tipado `V & { tenantId: TenantId }` | doc 03 §3, LOCKEADA                                                                                   |
| `assertTenant(scope: { tenantId: number \| null }, where: string): TenantContext`                                                     | narrowing fail-closed; lanza si es null                                                | resolución del riesgo 1 de PATTERNS                                                                   |
| `TenantLogger`                                                                                                                        | `{ info(obj, msg): void; warn(obj, msg): void; error(obj, msg): void }`                | interfaz mínima estructural: la satisfacen `pino()` de los jobs y `FastifyBaseLogger` de los services |
| `listActiveTenants(db): Promise<TenantContext[]>`                                                                                     | `SELECT id FROM tenants WHERE status = 'active' ORDER BY id`                           | D-01/D-05                                                                                             |
| `forEachActiveTenant(db, log: TenantLogger, jobName: string, run: (ctx: TenantContext) => Promise<void>): Promise<TenantSweepResult>` | loop secuencial con try/catch por tenant                                               | D-01/D-03                                                                                             |
| `TenantSweepResult`                                                                                                                   | `{ tenants: number; ok: number; failed: number }`                                      | acumulador del sweep                                                                                  |

`AnyMySqlColumn` se importa de `drizzle-orm/mysql-core` (ya usado en
`src/db/schema/users.ts:12`, `referrals.ts:10`, `refresh-tokens.ts:8`).
`eq` de `drizzle-orm`. `MySql2Database` de `drizzle-orm/mysql2`. `AppError` de `./errors`.

**Decisiones de este plan (documentarlas en el docblock del archivo):**

1. `assertTenant` lanza `new AppError("No se pudo resolver el gimnasio", 403, TENANT_UNRESOLVED)`.
   403 y no 500: `tenantId = null` es corrupción de datos y el criterio heredado de la 166 es
   denegar aguas abajo, no caerse. NUNCA `!`, NUNCA `?? 1`.
2. Los helpers reciben `{ tenantId: number }` (firma lockeada). Un call site con
   `request.scope` DEBE llamar `assertTenant(request.scope, "<dónde>")` primero — el narrowing
   es explícito y visible en el diff, no escondido dentro del helper.
3. `listActiveTenants` compara `eq(schema.tenants.status, "active")`. Un estado futuro del enum
   (`src/db/schema/tenants.ts:45-49` tiene hoy active/suspended/archived) queda DENEGADO por
   default en vez de colarse — mismo criterio que `country-scope.ts:180-184`.
4. `forEachActiveTenant` NO propaga el error de un tenant: `catch` + `log.error({ err, tenantId, job })`
   - sigue. Pino manda `log.error` a Sentry vía `instrument.ts` (CLAUDE.md), así que D-03 queda
     cubierto sin importar `@sentry/node` acá.
5. Si `listActiveTenants` devuelve vacío, `log.warn({ job })` y el sweep termina con
   `{ tenants: 0, ok: 0, failed: 0 }` (no es un error, pero no puede pasar en silencio).

**Ids de tenant de test de la fase** (cada archivo usa el suyo — `isolate: false` en
`vitest.config.ts:33-40` hace que dos archivos con el mismo id se pisen dentro del worker):

| Archivo                                                      | Id        |
| ------------------------------------------------------------ | --------- |
| `test/tenancy/tenant-helpers.test.ts` (este plan)            | **90169** |
| `test/tenancy/con-04-crons-per-tenant.test.ts` (169-04)      | 90269     |
| `test/tenancy/con-03-write-paths-tenant-id.test.ts` (169-08) | 90369     |
| `test/wellhub/webhook-tenant-derivation.test.ts` (169-05)    | 90469     |

El tenant 1 (El Templo, sembrado por la 0190) existe siempre y ningún test lo borra.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Worktree de la fase desde origin/master, sin instalar nada</name>
  <files>(sin archivos de código — crea el worktree /home/franco/projects/et-169-tenant-layer)</files>
  <read_first>
    - .claude/skills/el-templo-change-control/SKILL.md (secciones 1 "staging-first", 3 "worktree aislado", 4 "git add por ruta explícita", 6 "nunca instalar dependencias")
    - .planning/phases/169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc/169-PATTERNS.md (el bloque "⚠️ Base de lectura" del encabezado)
    - .planning/STATE.md (sección del worktree de la 168 y el estado de origin/master)
  </read_first>
  <action>
    NO tocar el checkout principal `/home/franco/projects/el-templo`: está en
    `fix/referral-preview-y-refresh-ficha`, 244 commits atrás, y es compartido con otras
    sesiones. Cero `git checkout`, cero `git stash`, cero edición ahí.

    Desde el checkout principal correr `git fetch origin` y crear el worktree
    `/home/franco/projects/et-169-tenant-layer` con rama nueva `feat/169-capa-escritura`
    basada en `origin/master`.

    Verificar antes de escribir una línea, y abortar reportando si algo falla:
    (a) `el-templo-api/src/modules/shared/country-scope.ts` exporta `attachScope` y su
    `CountryScope` tiene el campo `tenantId` (fase 166 presente);
    (b) `el-templo-api/src/db/tenant-tables.ts` existe (fases 167/168 presentes);
    (c) `el-templo-api/src/modules/shared/tenant.ts` NO existe todavía;
    (d) el `.sql` de número más alto en `src/db/migrations/` es el `0196_*` (esta fase no
    agrega migraciones; si hiciera falta una, se reserva desde 0197 y se lee el skill
    `el-templo-db-migrations` antes de escribirla).

    Copiar `.env` y `.env.development` desde `/home/franco/projects/et-168-contratos/el-templo-api/`
    (no están versionados y los tests los necesitan).

    Resolver `node_modules` SIN instalar (regla dura del skill §6): buscar en orden
    `/home/franco/projects/et-168-contratos`, `/home/franco/projects/et-167-columnas`,
    `/home/franco/projects/et-166-tenancy` el primero cuyo `el-templo-api/pnpm-lock.yaml` sea
    byte-idéntico al del worktree nuevo (`cmp`), y symlinkear
    `el-templo-api/node_modules` al destino real de su `node_modules` (`readlink -f`, para no
    encadenar symlinks). Si ninguno coincide, NO instalar: cortar y reportar.
    Recordar que la regla `node_modules/` del `.gitignore` NO matchea un symlink, así que el
    symlink ensucia `git status` — borrarlo antes de cualquier verificación de árbol limpio y
    antes de commitear.

  </action>
  <verify>
    <automated>test "$(git -C /home/franco/projects/et-169-tenant-layer rev-parse --abbrev-ref HEAD)" = "feat/169-capa-escritura" && git -C /home/franco/projects/et-169-tenant-layer merge-base --is-ancestor origin/master HEAD && test -f /home/franco/projects/et-169-tenant-layer/el-templo-api/src/db/tenant-tables.ts && test ! -f /home/franco/projects/et-169-tenant-layer/el-templo-api/src/modules/shared/tenant.ts && test -e /home/franco/projects/et-169-tenant-layer/el-templo-api/node_modules && test -f /home/franco/projects/et-169-tenant-layer/el-templo-api/.env.development</automated>
  </verify>
  <acceptance_criteria>
    - `git -C /home/franco/projects/et-169-tenant-layer rev-parse --abbrev-ref HEAD` imprime `feat/169-capa-escritura`
    - `git -C /home/franco/projects/et-169-tenant-layer merge-base --is-ancestor origin/master HEAD` sale 0
    - `src/modules/shared/country-scope.ts` del worktree contiene la cadena `tenantId: number | null` y `export async function attachScope`
    - `src/db/tenant-tables.ts` existe y `src/modules/shared/tenant.ts` todavía no
    - `el-templo-api/node_modules` existe y no se ejecutó ningún `pnpm install`/`npm install`/`pnpm add` (el SUMMARY nombra el worktree fuente y el resultado del `cmp`)
    - `git -C /home/franco/projects/el-templo rev-parse --abbrev-ref HEAD` sigue imprimiendo `fix/referral-preview-y-refresh-ficha` y su working tree quedó sin tocar
  </acceptance_criteria>
  <done>El worktree existe sobre origin/master con env y node_modules resueltos, sin haber instalado nada y sin haber tocado el checkout principal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Escribir src/modules/shared/tenant.ts — la única API de tenancy</name>
  <files>el-templo-api/src/modules/shared/tenant.ts</files>
  <read_first>
    - el-templo-api/src/modules/shared/country-scope.ts (worktree: el tipo `CountryScope`, el docblock de `tenantId` que ordena tratar `null` como deny, y el patrón fail-closed de :169-194)
    - el-templo-api/src/modules/shared/member-search.ts (idioma del módulo de funciones puras en shared/: sin clase, sin `db`, docblock con las reglas arriba)
    - el-templo-api/src/db/schema/tenants.ts (valores del enum `status`: active/suspended/archived)
    - el-templo-api/src/db/schema/tenant-column.ts (el texto canónico "el valor SALE SIEMPRE DEL SERVIDOR … JAMÁS de un payload" y la mención existente de `TenantContext`, que este archivo llena)
    - el-templo-api/src/jobs/mark-no-shows.ts (analog del iterador de dimensión: `getDistinctBranchTimezones` + el loop agregador de `runMarkNoShows`)
    - el-templo-api/src/db/tenant-tables.ts (docblock del milestone: secciones en MAYÚSCULA que citan fase y decisión)
    - el-templo-api/src/modules/shared/errors.ts (firma de `AppError`)
  </read_first>
  <behavior>
    - `tenantWhere(schema.auraConfig, { tenantId: 1 })` produce una condición cuyo SQL nombra `tenant_id` y cuyo parámetro es 1.
    - `tenantValues({ tenantId: 7 }, { sourceType: "x" })` devuelve `{ sourceType: "x", tenantId: 7 }` y NO muta el objeto de entrada.
    - `tenantValues({ tenantId: 7 }, { tenantId: 99, a: 1 })` devuelve `tenantId: 7` — el scope gana siempre sobre lo que venga en `values`.
    - `assertTenant({ tenantId: 5 }, "x")` devuelve `{ tenantId: 5 }`; `assertTenant({ tenantId: null }, "x")` lanza `AppError` con `statusCode 403` y `code === TENANT_UNRESOLVED`.
    - `listActiveTenants(db)` con el tenant 1 activo y un segundo tenant activo devuelve los dos ordenados por id; con el segundo en `suspended` o `archived` devuelve solo el 1.
    - `forEachActiveTenant` invoca el callback una vez por tenant activo y devuelve `{ tenants, ok, failed }`; si el callback lanza para un tenant, loguea con `tenantId` y sigue con el siguiente (`failed` cuenta el fallo, `ok` cuenta los demás).
  </behavior>
  <action>
    Crear `src/modules/shared/tenant.ts` con EXACTAMENTE la superficie pública de la tabla de
    `<interfaces>`. Las firmas de `tenantWhere` y `tenantValues` son las del doc 03 §3 y NO se
    re-diseñan: `tenantWhere` devuelve `eq(table.tenantId, scope.tenantId)` y `tenantValues`
    devuelve el spread con `tenantId` del scope AL FINAL (para que el scope pise cualquier
    `tenantId` que viniera en `values` — es la mitigación de mass-assignment a nivel tipo).

    Docblock de cabecera con el idioma del milestone (secciones en MAYÚSCULA citando fase y
    decisión, como `tenant-tables.ts:1-51`), cubriendo:
    - QUÉ ES: la única API de tenancy; scope de request y `TenantContext` son estructuralmente
      compatibles porque el contrato es `{ tenantId }` plano — no hay dos APIs (CON-03).
    - CONVENCIONES DE USO: `and(tenantWhere(table, scope), ...resto)` como PRIMER término de
      todo WHERE sobre tabla gym-owned; en ``sql`` `` crudos, `WHERE tenant_id = ${scope.tenantId}`.
    - DE DÓNDE SALE EL TENANT: server-side siempre (`scope.tenantId` resuelto por `attachScope`
      leyendo `users.tenant_id`, o un `TenantContext` construido por el loop de crons / la
      derivación del webhook / el `--tenant` del CLI). JAMÁS de un payload, de una query string
      ni del JWT. Citar el texto canónico de `src/db/schema/tenant-column.ts:11-16`.
    - POR QUÉ HAY UN GUARD: `CountryScope.tenantId` es `number | null` y la firma lockeada pide
      `number`. `null` es corrupción de datos y significa DENY, nunca "todos los gimnasios"
      (orden explícita de `country-scope.ts:33-37`). Prohibido `!` y `?? 1`.
    - ALCANCE DE LA 169 (D-02): el `TenantContext` llega hasta el cuerpo de los jobs y hasta el
      punto de derivación del webhook. Los services MANTIENEN su firma actual hasta su fase de
      adopción (172-175). Con un solo tenant activo el resultado es idéntico al de hoy; el
      cuerpo de cada job sigue siendo global, y por eso el gate del milestone es que el tenant 2
      no se onboardea hasta que ISO-03 esté verde.

    `listActiveTenants` selecciona `schema.tenants.id` con
    `where(eq(schema.tenants.status, "active"))` y `orderBy(schema.tenants.id)`, y mapea a
    `TenantContext[]`. Comentar por qué la comparación es contra `'active'` y no contra la lista
    de estados malos.

    `forEachActiveTenant(db, log, jobName, run)`: pide la lista, si viene vacía `log.warn` y
    devuelve el resultado en cero; si no, loop `for...of` SECUENCIAL (con un solo tenant no hay
    nada que paralelizar y el orden secuencial mantiene el comportamiento actual de los jobs),
    `try { await run(ctx); ok++ } catch (err: unknown) { failed++; log.error({ err, tenantId: ctx.tenantId, job: jobName }, "<mensaje>") }`.
    Nunca `console.*` (CLAUDE.md). El `tenantId` va como campo estructurado del primer argumento,
    jamás interpolado en el mensaje.

    Sin `any` en todo el archivo. No agregar el archivo al barrel `src/modules/shared/index.ts`:
    `country-scope.ts` tampoco está y se importa por path directo desde sus 22 call sites —
    esta fase es consistente con eso y todos los consumidores importan `"../shared/tenant"`
    (o el path relativo que corresponda).

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-169-tenant-layer/el-templo-api && npx tsc --noEmit && test "$(grep -v '^\s*[/*]' src/modules/shared/tenant.ts | grep -c 'export ')" -ge 8 && grep -q 'eq(table.tenantId, scope.tenantId)' src/modules/shared/tenant.ts && grep -q 'eq(schema.tenants.status, "active")' src/modules/shared/tenant.ts && test "$(grep -v '^\s*[/*]' src/modules/shared/tenant.ts | grep -cE '\?\? 1|scope\.tenantId!|tenantId!')" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `src/modules/shared/tenant.ts` exporta los 10 nombres de la tabla de `<interfaces>`: `TenantId`, `TenantContext`, `TenantLogger`, `TenantSweepResult`, `TENANT_UNRESOLVED`, `tenantWhere`, `tenantValues`, `assertTenant`, `listActiveTenants`, `forEachActiveTenant`
    - El cuerpo de `tenantWhere` es literalmente `eq(table.tenantId, scope.tenantId)` y su parámetro `scope` está tipado `{ tenantId: TenantId }` (no `number | null`)
    - En `tenantValues` el `tenantId` del scope aparece DESPUÉS del spread de `values` (verificable leyendo el return: `{ ...values, tenantId: scope.tenantId }`)
    - `assertTenant` lanza `AppError` con status 403 y `TENANT_UNRESOLVED` cuando `tenantId` es `null`, y el archivo no contiene ni `?? 1` ni un non-null assertion sobre `tenantId` fuera de comentarios
    - `listActiveTenants` filtra con `eq(schema.tenants.status, "active")` (comparación positiva, no exclusión de estados malos)
    - `forEachActiveTenant` tiene el `try/catch` DENTRO del loop y el `catch` loguea `tenantId` como campo estructurado; no hay `throw` que corte el sweep
    - `npx tsc --noEmit` sale 0 y no hay ningún `any` en el archivo
    - El docblock cita explícitamente CON-03, D-02, D-03 y la regla "el tenant JAMÁS viene del payload/JWT"
  </acceptance_criteria>
  <done>El archivo existe, typechequea, tiene la firma lockeada del doc 03 §3 y resuelve el narrowing `number | null` con un guard fail-closed exportado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests de tenant.ts — pureza, fail-closed, filtro de activos y aislamiento de errores</name>
  <files>el-templo-api/test/tenancy/tenant-helpers.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/shared/tenant.ts (lo escrito en Task 2)
    - el-templo-api/test/tenancy/con-01-uniques-cross-tenant.test.ts (patrón del 2º tenant ad-hoc: constantes de tenant :54-63, declaración de alcance :38-42, trampa del DEFAULT 1 :28-32 y :166-173, limpieza `limpiarTenantSegundo` :270-289)
    - el-templo-api/test/shared/tenant-scope.test.ts (unit de un helper de tenancy sin pasar por ruta, y el `afterEach` incondicional que devuelve el tenant 1 a 'active' :105-119)
    - el-templo-api/test/helpers.ts (createTestApp, cleanAllTestData)
    - el-templo-api/src/db/schema/aura.ts (columnas de `aura_config`, la tabla gym-owned de pocas FK que usan las aserciones de comportamiento)
  </read_first>
  <behavior>
    - Pureza: `tenantValues` no muta su entrada y el `tenantId` del scope pisa un `tenantId` presente en `values`.
    - Comportamiento real contra MySQL: dos filas de `aura_config` insertadas con `tenantValues` para el tenant 1 y para el 90169; un SELECT con `tenantWhere` devuelve solo la del tenant pedido.
    - `assertTenant` devuelve el contexto con un `tenantId` numérico y lanza 403 `TENANT_UNRESOLVED` con `null`.
    - `listActiveTenants` incluye el 90169 mientras está `active` y lo excluye con `suspended` y con `archived`.
    - `forEachActiveTenant` corre el callback una vez por tenant activo; con un callback que lanza para el 90169, `failed` es 1, `ok` es 1 y el tenant 1 igual fue procesado.
  </behavior>
  <action>
    Crear `test/tenancy/tenant-helpers.test.ts` siguiendo el idioma del `con-01`:
    constantes `TENANT_TEMPLO = 1` y `TENANT_SEGUNDO = 90169` arriba con el comentario de por
    qué el id es alto y fijo, y una declaración de alcance que repita textualmente que las
    fixtures 2-tenant completas son de la fase 171 (ISO-03) — este archivo NO adelanta esa API
    ni agrega nada a `test/helpers.ts`.

    Trampa obligatoria del DEFAULT 1 (T-168-15): TODO insert del archivo pasa `tenantId`
    explícito, incluso los del tenant 1. Un insert que se olvide de estamparlo cae en el tenant 1
    sin avisar y el test pasaría en verde probando exactamente nada.

    `beforeAll`: `createTestApp()` y sembrar el tenant 90169 (`INSERT INTO tenants (id, slug,
    name, status)` con slug propio del archivo, p. ej. `test-169-helpers`) de forma defensiva
    (borrarlo antes por si quedó de una corrida anterior).
    `afterEach` incondicional: devolver el tenant 1 a `status='active'` y el 90169 a `'active'`
    — si un test deja el tenant 1 suspendido, rompe TODOS los tests siguientes del worker
    (`isolate: false`).
    `afterAll`: borrar las filas de `aura_config` sembradas y el tenant 90169, en orden seguro
    de FKs. NO usar `cleanAllTestData` en este archivo: es admin-global y no está scopeada por
    tenant (warning heredado del 168-REVIEW) — la limpieza es local y explícita.

    Para la aserción de pureza de `tenantWhere` sin tocar la DB, usar
    `db.select().from(schema.auraConfig).where(tenantWhere(schema.auraConfig, ctx)).toSQL()` y
    verificar que el SQL nombra `tenant_id` y que los params contienen el id esperado.

    Para el filtro de estados, recorrer los TRES valores del enum (`active`, `suspended`,
    `archived`) sobre el 90169 y afirmar presencia/ausencia en `listActiveTenants`.

    Para el aislamiento de errores, pasar a `forEachActiveTenant` un callback que lance solo
    cuando `ctx.tenantId === TENANT_SEGUNDO`, y un logger fake que capture las llamadas a
    `error` para afirmar que el objeto logueado incluye `tenantId: 90169` y `job`.

    Correr el archivo SOLO (`pnpm vitest run test/tenancy/tenant-helpers.test.ts
    --no-file-parallelism`): más de un archivo a la vez revienta el timeout de 120 s del
    provisioning en esta máquina.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-169-tenant-layer/el-templo-api && npx vitest run test/tenancy/tenant-helpers.test.ts --no-file-parallelism && bash -c 'for f in src/modules/shared/tenant.ts test/tenancy/tenant-helpers.test.ts; do grep -v "^\s*[/*]" "$f" | grep -qE ":\s*any\b|as any|<any>" && { echo "any explicito en $f"; exit 1; }; done; exit 0'</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/tenancy/tenant-helpers.test.ts --no-file-parallelism` termina con 0 tests fallidos y al menos 8 tests
    - El archivo declara `const TENANT_SEGUNDO = 90169` y ningún otro archivo de la fase usa ese id
    - Todo `insert` del archivo pasa `tenantId` explícito (incluidos los del tenant 1) y el archivo lo justifica en un comentario citando T-168-15
    - Existe un `afterEach` incondicional que devuelve el tenant 1 a `status='active'`
    - El archivo NO llama a `cleanAllTestData` y su `afterAll` borra el tenant 90169 y sus filas de `aura_config`
    - Hay un test que afirma que `listActiveTenants` excluye el 90169 con `suspended` Y otro con `archived`
    - Hay un test que afirma `{ tenants: 2, ok: 1, failed: 1 }` (o equivalente) cuando el callback lanza para un solo tenant, y que el `log.error` capturado incluye `tenantId: 90169`
    - Después de la corrida, `SELECT COUNT(*) FROM tenants WHERE id = 90169` da 0 en la base del worker
  </acceptance_criteria>
  <done>El comportamiento de los 5 exports está probado contra MySQL real, incluido el fail-closed y el aislamiento de errores, sin dejar residuo de tenant.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                              | Description                                                                         |
| ----------------------------------------------------- | ----------------------------------------------------------------------------------- |
| `scope.tenantId` (`number \| null`) → helper de query | un `null` que se cuele produce una query SIN filtro de tenant sobre tabla gym-owned |
| tabla `tenants` → loop de jobs                        | el estado comercial decide si los datos de un gimnasio se tocan o no                |
| worktree nuevo → checkout principal compartido        | operaciones de git que podrían pisar el trabajo de otra sesión                      |
| lockfile → `node_modules`                             | dependencias que entrarían al árbol si se corriera un install                       |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                     | Disposition | Mitigation Plan                                                                                                                                                                               |
| --------- | ---------------------- | ----------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-169-01  | Information Disclosure | `tenantWhere` invocado con un scope de `tenantId` null                        | mitigate    | La firma pide `{ tenantId: number }`: un `CountryScope` no compila sin pasar por `assertTenant`, que lanza 403 `TENANT_UNRESOLVED`. Prohibido `!` y `?? 1`, verificado por grep en `<verify>` |
| T-169-02  | Elevation of Privilege | `tenantValues` recibiendo un `values` con `tenantId` propio (mass-assignment) | mitigate    | El `tenantId` del scope va DESPUÉS del spread y siempre pisa. Test dedicado en Task 3                                                                                                         |
| T-169-03  | Information Disclosure | `listActiveTenants` devolviendo un tenant suspendido/archivado                | mitigate    | Comparación positiva contra `'active'`; un estado futuro del enum deniega por default. Probado sobre los 3 valores del enum                                                                   |
| T-169-04  | Denial of Service      | un tenant roto abortando el sweep de todos los demás                          | mitigate    | `try/catch` por iteración con `log.error` estructurado (D-03), probado con un callback que lanza                                                                                              |
| T-169-05  | Tampering              | worktree creado sobre el checkout principal atrasado y compartido             | mitigate    | `git worktree add` desde `origin/master` tras `git fetch`; cero checkout/stash en `/home/franco/projects/el-templo`; assert de que su rama sigue intacta                                      |
| T-169-06  | Repudiation            | tests que pasan en verde probando nada por el `DEFAULT 1` de `tenant_id`      | mitigate    | Todo insert del test estampa `tenantId` explícito y hay aserciones de exclusión (el tenant 90169 NO aparece), no solo de inclusión                                                            |
| T-169-SC  | Tampering              | instalación de paquetes npm                                                   | mitigate    | Esta fase no agrega ninguna dependencia. `node_modules` se resuelve por symlink tras verificar el lockfile con `cmp`; si difiere se corta y se pregunta. Cero `pnpm install`                  |

</threat_model>

<verification>
- `npx tsc --noEmit` verde en el worktree.
- `npx vitest run test/tenancy/tenant-helpers.test.ts --no-file-parallelism` verde.
- El checkout principal `/home/franco/projects/el-templo` sin modificar.
- El symlink de `node_modules` borrado antes de verificar el árbol y antes de commitear.
- Commit con `git add` por ruta explícita (`src/modules/shared/tenant.ts`, `test/tenancy/tenant-helpers.test.ts`), nunca `git add -A`.
</verification>

<success_criteria>

- Worktree `et-169-tenant-layer` en `feat/169-capa-escritura` sobre `origin/master`, sin installs.
- `src/modules/shared/tenant.ts` con la superficie pública exacta de `<interfaces>` y la firma lockeada del doc 03 §3.
- Narrowing `number | null` resuelto con `assertTenant` fail-closed (403 `TENANT_UNRESOLVED`).
- `listActiveTenants` + `forEachActiveTenant` listos para los 7 crons, con aislamiento de errores.
- `test/tenancy/tenant-helpers.test.ts` en verde y sin residuo del tenant 90169.
  </success_criteria>

<output>
Create `.planning/phases/169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc/169-01-SUMMARY.md` when done
</output>
</content>
</invoke>
