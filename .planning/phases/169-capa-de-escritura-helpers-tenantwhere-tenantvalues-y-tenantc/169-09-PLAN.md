---
phase: 169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc
plan: 09
type: execute
wave: 6
depends_on:
  [
    "169-01",
    "169-02",
    "169-03",
    "169-04",
    "169-05",
    "169-06",
    "169-07",
    "169-08",
  ]
files_modified: []
autonomous: false
requirements: [CON-03, CON-04]

must_haves:
  truths:
    - "Los 5 criterios de éxito del ROADMAP para la fase 169 tienen evidencia nombrada: archivo, test y salida"
    - "El inventario completo de exenciones tenant-safe cabe en una sola búsqueda y cada entrada tiene motivo escrito"
    - "El typecheck y los 11 archivos de test (6 nuevos + 5 de regresión) corren en verde en el worktree antes de pedir cualquier push"
    - "Cada push ocurrió después de una señal explícita de Franco en la conversación, y ninguna señal habilitó más de su etapa"
    - "Esta fase no agrega migraciones: el tope aplicado en producción sigue siendo 0196"
  artifacts:
    - path: ".planning/phases/169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc/169-09-SUMMARY.md"
      provides: "Evidencia del gate: mapa criterio→prueba, inventario de exenciones, SHAs por entorno y las señales humanas con su alcance"
  key_links:
    - from: "el-templo-api/src/modules/shared/tenant.ts"
      to: "producción (rama master)"
      via: "staging-first: push a staging, CI verde, después master"
      pattern: "feat/169-capa-escritura"
---

<objective>
Cerrar la fase con dos cosas que ningún plan anterior puede dar solo: un gate consolidado que
mapee los 5 criterios del ROADMAP a evidencia concreta (incluido el inventario grepeable de
exenciones que la fase 170 va a consumir), y el rollout staging-first detrás de señales humanas
explícitas.

Purpose: los 8 planes anteriores dejan piezas verdes por separado. Acá se prueba que juntas
compilan, que la suite relevante pasa entera y que no quedó ninguna exención sin motivo.
Output: SUMMARY con el mapa criterio→prueba y el inventario de exenciones, y la rama de la fase
en `staging` y después en `master`.

Sin migraciones: la fase es 100% código. El tope aplicado en producción sigue siendo 0196 y las
fases siguientes reservan desde 0197.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc/169-CONTEXT.md
@.claude/skills/el-templo-change-control/SKILL.md
@CLAUDE.md

<interfaces>
<!-- Contratos que el executor NO tiene que salir a buscar. -->

**Mapa criterio del ROADMAP → evidencia exigida (va tal cual al SUMMARY):**

| #   | Criterio (ROADMAP fase 169)                                                                                                                                        | Evidencia                                                                                                                                                                            |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | `tenantWhere`/`tenantValues` en `shared/tenant.ts` aceptan indistintamente scope de request y `TenantContext`, cubiertos por unit tests (CON-03)                   | `src/modules/shared/tenant.ts` + `test/tenancy/tenant-helpers.test.ts` verde                                                                                                         |
| 2   | Mandar `tenantId: 2` en el body de rutas de escritura y la fila nace con `tenant_id = 1` (CON-03)                                                                  | `test/tenancy/con-03-write-paths-tenant-id.test.ts` verde, 5 rutas × 2 casos                                                                                                         |
| 3   | Los crons iteran tenants ACTIVOS con un `TenantContext` por tenant; un suspendido no se procesa (CON-04)                                                           | `test/tenancy/con-04-crons-per-tenant.test.ts` verde, incluido el gate de cobertura de los 7 jobs                                                                                    |
| 4   | Webhook Wellhub deriva por `gym.id` → `branches.wellhub_gym_id` → `branches.tenant_id` y falla cerrado; los scripts CLI exigen el tenant y abortan sin él (CON-04) | `test/wellhub/webhook-tenant-derivation.test.ts` + `test/unit/require-tenant.test.ts` verdes + las 3 corridas manuales de `seed-onboarding-aura.ts` registradas en el 169-07-SUMMARY |
| 5   | `tv_pairings` pre-claim anotado con la exención y el claim estampa el tenant (CON-04)                                                                              | `src/modules/tv/pairing.ts` + `test/tv/tv-pairing-tenant.test.ts` verde                                                                                                              |

**Los 11 archivos de test del gate.** Correr de a UNO, siempre con `--no-file-parallelism`: más
de un archivo a la vez revienta el timeout de 120 s del provisioning en esta máquina.

Nuevos de la fase:
`test/tenancy/tenant-helpers.test.ts`, `test/tenancy/con-04-crons-per-tenant.test.ts`,
`test/tenancy/con-03-write-paths-tenant-id.test.ts`,
`test/wellhub/webhook-tenant-derivation.test.ts`, `test/tv/tv-pairing-tenant.test.ts`,
`test/unit/require-tenant.test.ts`.

De regresión (tocan código modificado por la fase):
`test/expire-lost-leads.test.ts`, `test/jobs/reassign-multibranch.test.ts`,
`test/notification-plan-renewal.test.ts`, `test/wellhub/webhook-checkin.test.ts`,
`test/tv/tv-pairing.test.ts`.

**Inventario de exenciones esperado — 9 archivos, cada uno con una anotación `tenant-safe:`
seguida de su motivo:** `src/modules/tv/pairing.ts` (pairing pre-claim),
`src/modules/wellhub/service.ts` (idempotencia global previa a la derivación),
`src/jobs/notification-cron.ts` (seed de templates), `src/db/run-migrations.ts`,
`src/db/scripts/verify-tenant-backfill.ts`, `src/db/scripts/verify-tenant-uniques.ts`,
`src/db/seed.ts`, `src/db/seed-spom.ts`, `scripts/wellhub-sandbox.ts`.
Si aparece una exención en un archivo que no está en esta lista, NO se borra: se reporta y se
decide, porque puede ser legítima y solo faltar en el inventario.

**Reglas de rollout (skill `el-templo-change-control`, no re-litigar):**

- Todo push va detrás de una señal explícita de Franco EN ESTA conversación, por push. Una señal
  previa —de esta fase o de otra— no autoriza el siguiente.
- Staging PRIMERO, master después. Todo push a `master` es un deploy a producción.
- `git add` siempre por ruta explícita. Nunca `git add -A` ni `git add .`.
- Antes de cada push: `git fetch origin`, verificar que la rama contenga SOLO archivos de la fase,
  y `npx tsc --noEmit` sobre el árbol resultante.
- Para el typecheck sobre el árbol mergeado, symlinkear `node_modules` como en 169-01 y BORRAR el
  symlink antes de mirar `git status` y antes de commitear (la regla `node_modules/` del
  `.gitignore` no matchea un symlink).
- **Sin SSH en esta fase:** no hay migración ni verificación contra las bases reales. Si algún
  diagnóstico lo requiriera, es un gate humano aparte y explícito.
- **Sin bump de versión:** los cambios son solo de `el-templo-api`; `el-templo-app/version.txt`
  no se toca y no hay build de tiendas.
- Si `origin/staging` está adelantado y el push no es fast-forward, usar una rama descartable
  basada en `origin/staging` con merge `--no-ff` (patrón del 168-06), verificando antes que los
  archivos que difieren entre master y staging no intersecten con los de la fase.

**Archivos que la fase debería haber tocado (lista de chequeo del árbol):**
`src/modules/shared/tenant.ts`; los 7 de `src/jobs/`; `src/index.ts`;
`src/modules/wellhub/service.ts`; `src/db/schema/wellhub.ts`; `src/modules/tv/pairing.ts`;
`src/modules/tv/control-routes.ts`; `src/modules/tv/schemas.ts`; `src/db/schema/tv.ts`;
`src/db/scripts/require-tenant.ts`; `scripts/seed-onboarding-aura.ts`; los 6 exentos;
`src/modules/gladius/routes.ts`; los `schemas.ts` auditados de members/finance/campaigns/scheduling;
y los 6 archivos de test nuevos. Cualquier archivo fuera de esa lista se reporta antes de pushear.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate local consolidado — typecheck, 11 archivos de test, inventario de exenciones y mapa criterio→prueba</name>
  <files>(sin archivos de código — produce la evidencia del SUMMARY)</files>
  <read_first>
    - .planning/ROADMAP.md (la sección "Phase 169" completa: los 5 criterios de éxito textuales)
    - .planning/phases/169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc/169-01-SUMMARY.md a 169-08-SUMMARY.md (qué quedó hecho y qué desviaciones se registraron)
    - .claude/skills/el-templo-change-control/SKILL.md (sección 10: los tests corren en CI, local solo archivos puntuales)
  </read_first>
  <action>
    En el worktree `/home/franco/projects/et-169-tenant-layer`, sobre `el-templo-api`:

    1. `npx tsc --noEmit`. Tiene que salir 0.
    2. Correr los 11 archivos de test de la lista de `<interfaces>` DE A UNO, cada uno con
       `npx vitest run <archivo> --no-file-parallelism`, y registrar el resultado de cada uno con
       su conteo de tests. Si alguno queda rojo, NO avanzar al Task 2: reportar archivo, test y
       causa.
    3. Inventario de exenciones: correr
       `grep -rn "tenant-safe:" --include=*.ts src/ scripts/` y verificar que aparezcan los 9
       archivos de la lista, cada uno con un motivo escrito después de los dos puntos (nunca la
       anotación pelada). Copiar la salida completa al SUMMARY: ese es el inventario que la fase
       170 va a consumir para el sentinel y el lint.
    4. Verificar que la fase NO agregó migraciones: el `.sql` de mayor número en
       `src/db/migrations/` sigue empezando con `0196`, y `git diff --name-only origin/master...HEAD`
       no lista ningún archivo bajo `src/db/migrations/`.
    5. Chequeo de árbol: borrar el symlink de `node_modules`, correr `git status --porcelain`
       (tiene que quedar vacío) y `git diff --name-only origin/master...HEAD`, y comparar contra
       la lista de archivos esperados de `<interfaces>`. Reportar cualquier archivo fuera de la
       lista antes de seguir.
    6. Armar el mapa criterio→prueba con la salida REAL de cada test (no la esperada) y dejarlo
       en el SUMMARY junto al inventario de exenciones.

    Nota de tiempo: son 11 corridas secuenciales contra MySQL real y van a tardar. Es deliberado
    — CI corre la suite completa recién después del push, y este gate es lo único que separa un
    push rojo de uno verde.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-169-tenant-layer/el-templo-api && npx tsc --noEmit && bash -c 'for f in src/modules/tv/pairing.ts src/modules/wellhub/service.ts src/jobs/notification-cron.ts src/db/run-migrations.ts src/db/scripts/verify-tenant-backfill.ts src/db/scripts/verify-tenant-uniques.ts src/db/seed.ts src/db/seed-spom.ts scripts/wellhub-sandbox.ts; do grep -qE "tenant-safe: +[A-Za-z]" "$f" || exit 1; done' && test "$(ls src/db/migrations/*.sql | sort | tail -1 | xargs basename | cut -c1-4)" = "0196"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sale 0 en el worktree
    - Los 11 archivos de test corrieron de a uno y los 11 terminaron con 0 fallos; el SUMMARY lista archivo por archivo con su conteo de tests
    - Los 9 archivos del inventario contienen una anotación `tenant-safe:` seguida de un motivo en texto (no la anotación pelada), y la salida completa del grep está copiada en el SUMMARY
    - `git diff --name-only origin/master...HEAD` no lista ningún archivo bajo `src/db/migrations/` y el `.sql` de mayor número sigue empezando con `0196`
    - `git status --porcelain` queda vacío con el symlink de `node_modules` borrado
    - Todo archivo modificado en la rama está en la lista de archivos esperados de `<interfaces>`, o el SUMMARY explica por qué no
    - El SUMMARY contiene el mapa criterio→prueba con los 5 criterios del ROADMAP y su evidencia real
  </acceptance_criteria>
  <done>La fase compila entera, los 11 archivos de test pasan, el inventario de exenciones está completo con motivos y el árbol solo contiene archivos de la fase.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Rollout staging-first con dos señales humanas separadas</name>
  <what-built>
    La capa de escritura con tenant completa y verde en local: `src/modules/shared/tenant.ts`
    (helpers + `TenantContext` + iterador de tenants activos con aislamiento de errores), los 7
    crons barriendo por tenant activo, el webhook de Wellhub derivando el tenant desde
    `branches.wellhub_gym_id` y cortando fail-closed por estado comercial, el helper CLI
    `--tenant` con el retrofit de `seed-onboarding-aura`, la exención anotada del pre-claim de
    `tv_pairings` con el claim estampando el tenant del staff, y la batería que prueba que
    mandar `tenantId` en el body no cambia el `tenant_id` de la fila.

    Cero migraciones, cero dependencias nuevas, cero cambios de frontend. El gate local del
    Task 1 dejó `tsc` y los 11 archivos de test en verde.

  </what-built>
  <read_first>
    - .claude/skills/el-templo-change-control/SKILL.md (staging-first estricto, human gates, "todo push a master es un deploy a prod", `git add` por ruta explícita)
    - .planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-06-SUMMARY.md (el precedente del rollout de la fase anterior: etapas, poller de CI y cómo se registraron las señales)
    - .planning/phases/169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc/169-09-PLAN.md (la sección `<interfaces>` de este plan: reglas de rollout y lista de archivos esperados)
  </read_first>
  <action>
    Ejecutar la secuencia de abajo avanzando SOLO hasta donde llegue la señal recibida, y volver a
    pedir la siguiente antes de seguir. Ninguna señal previa —de esta fase o de otra— autoriza una
    etapa que no nombra.

    Antes de cada push: `git fetch origin`, verificar que el árbol contenga solo archivos de la
    fase, y `npx tsc --noEmit` sobre el árbol resultante (symlinkeando `node_modules` como en
    169-01 y borrando el symlink antes del push).

    Antes del push a master, además: comprobar si `origin/staging` tiene commits ajenos parados
    (`git merge-base --is-ancestor origin/staging HEAD`) y reportar el resultado, para que ningún
    trabajo de terceros viaje a producción de polizón en este push.

    No hay SSH en esta fase y no hay bump de versión: si algo pareciera pedirlo, cortar y
    preguntar.

  </action>
  <how-to-verify>
    Este checkpoint tiene DOS señales separadas, en este orden. Cada una habilita solo su etapa.

    **Señal 1 — "aprobado staging"** habilita la etapa A:
    1. Push de `feat/169-capa-escritura` a `origin` (respaldo de la rama) y a `staging` — directo
       si es fast-forward, o vía rama descartable basada en `origin/staging` con merge `--no-ff`
       si staging está adelantado.
    2. Seguimiento de CI y del deploy de staging con un poller cada 90 s. Se te reporta el número
       de run y el resultado por job.
    Qué mirar vos: que CI quede verde, y en particular el job de integration tests, que es donde
    corren por primera vez en CI los 6 archivos de test nuevos y los 5 de regresión, con la suite
    completa alrededor (el gate local corrió los 11 aislados; CI es lo que prueba que no rompimos
    nada del resto).

    **Señal 2 — "aprobado prod"** habilita la etapa B:
    3. Merge `--no-ff` de `origin/master` dentro de `feat/169-capa-escritura`, verificación del
       árbol resultante y `npx tsc --noEmit` sobre el merge.
    4. Push a `master` (deploy a producción) y seguimiento del pipeline hasta el smoke test.
    Qué mirar vos: que el deploy termine verde y que la API siga respondiendo. Esta fase no
    cambia comportamiento con un solo tenant activo, así que el staff no debería notar
    absolutamente nada: los crons hacen lo mismo, el webhook responde lo mismo y el TV se
    empareja igual.

    Si alguna etapa sale mal, se corta ahí y se reporta: nada se "arregla hacia adelante" sin una
    señal nueva.

  </how-to-verify>
  <resume-signal>Escribí "aprobado staging" para la etapa A. Después de ver CI verde, escribí "aprobado prod" para la etapa B. Cualquier otra respuesta se toma como no autorizado.</resume-signal>
  <acceptance_criteria>
    - Ningún `git push` se ejecutó antes de su señal correspondiente, y el SUMMARY registra cada señal con su texto literal, su hora y el alcance que habilitó
    - `git add` se usó siempre por ruta explícita; el SUMMARY lista los paths stageados
    - El SUMMARY registra el SHA de `origin/staging` y el de `origin/master` después de cada etapa, y el número de run de CI con su resultado por job
    - Antes del push a master se reportó el resultado de `git merge-base --is-ancestor origin/staging HEAD` y, si había commits ajenos parados en staging, se dijo cuáles son y que no viajan
    - No se ejecutó ningún comando SSH ni ningún `pnpm install` durante el rollout
    - `el-templo-app/version.txt` no fue modificado
  </acceptance_criteria>
  <done>La fase 169 está en staging con CI verde y en master con deploy verde, cada push respaldado por una señal humana explícita registrada en el SUMMARY.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                | Description                                               |
| ------------------------------------------------------- | --------------------------------------------------------- |
| rama local → `origin/staging`                           | primer entorno compartido; CI corre la suite completa acá |
| `origin/staging` → `origin/master`                      | todo push a master es un deploy a producción              |
| worktree de la fase → commits ajenos parados en staging | riesgo de que trabajo de terceros viaje de polizón        |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                           | Disposition | Mitigation Plan                                                                                                                                                                       |
| --------- | ---------------------- | ------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-169-43  | Repudiation            | push sin consentimiento explícito                                   | mitigate    | Dos señales separadas, cada una habilitando solo su etapa; el checkpoint es bloqueante y no auto-aprobable, y ninguna señal previa ni contexto de memoria cuenta como autorización    |
| T-169-44  | Tampering              | trabajo ajeno viajando a producción en este push                    | mitigate    | `git merge-base --is-ancestor origin/staging HEAD` reportado antes del push a master, y chequeo de árbol contra la lista de archivos esperados de la fase                             |
| T-169-45  | Tampering              | archivos peligrosos del working tree entrando por un `git add -A`   | mitigate    | `git add` siempre por ruta explícita; el SUMMARY lista los paths stageados (el checkout del repo contiene keystores, credenciales de Firebase y repos embebidos sin trackear)         |
| T-169-46  | Denial of Service      | regresión de comportamiento en producción por el sweep de los crons | mitigate    | Con un solo tenant activo el resultado es idéntico; los 5 tests de regresión corrieron en el gate local y la suite completa en CI antes de master                                     |
| T-169-47  | Elevation of Privilege | exención sin motivo que la fase 170 termine autorizando a ciegas    | mitigate    | El inventario de exenciones se verifica archivo por archivo en el Task 1 y se copia entero al SUMMARY                                                                                 |
| T-169-SC  | Tampering              | instalación de paquetes npm durante el rollout                      | mitigate    | Cero dependencias nuevas en toda la fase. Para el typecheck del árbol mergeado se symlinkea `node_modules` tras verificar el lockfile con `cmp`, y el symlink se borra antes del push |

</threat_model>

<verification>
- `npx tsc --noEmit` verde y los 11 archivos de test verdes en el gate local (Task 1).
- Inventario de exenciones completo, con motivo en los 9 archivos, copiado al SUMMARY.
- Sin archivos bajo `src/db/migrations/` en el diff de la fase.
- Dos señales humanas registradas con texto literal, hora y alcance.
- SHAs de `origin/staging` y `origin/master` registrados, y run de CI con resultado por job.
</verification>

<success_criteria>

- Los 5 criterios del ROADMAP mapeados a evidencia real en el SUMMARY.
- Inventario de exenciones `tenant-safe` completo y grepeable, listo para la fase 170.
- Rama de la fase en `staging` con CI verde y en `master` con deploy verde.
- Cada push detrás de su señal humana; cero SSH, cero installs, cero bump de versión.
  </success_criteria>

<output>
Create `.planning/phases/169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc/169-09-SUMMARY.md` when done
</output>
</content>
