---
phase: 169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc
plan: 08
type: execute
wave: 5
depends_on: ["169-01"]
files_modified:
  - el-templo-api/test/tenancy/con-03-write-paths-tenant-id.test.ts
  - el-templo-api/src/modules/gladius/routes.ts
  - el-templo-api/src/modules/members/schemas.ts
  - el-templo-api/src/modules/finance/schemas.ts
  - el-templo-api/src/modules/campaigns/schemas.ts
  - el-templo-api/src/modules/scheduling/schemas.ts
autonomous: true
requirements: [CON-03]

must_haves:
  truths:
    - "Mandar tenantId en el body de una ruta de escritura clave no cambia el tenant_id de la fila creada: nace con 1 (criterio 2 del ROADMAP)"
    - "El tenant sembrado que se manda en el body EXISTE en la DB, así que el test distingue 'lo ignoró' de 'la FK lo rechazó'"
    - "Los 6 sitios que spreadean request.body están auditados y sus 6 body-schemas declaran additionalProperties: false, verificado por import y no por grep"
    - "Si alguien afloja el additionalProperties de cualquiera de los 6, la suite queda en rojo"
  artifacts:
    - path: "el-templo-api/test/tenancy/con-03-write-paths-tenant-id.test.ts"
      provides: "Batería representativa de D-09 + guard de los 6 body-schemas del inventario D-08"
      min_lines: 220
  key_links:
    - from: "el-templo-api/test/tenancy/con-03-write-paths-tenant-id.test.ts"
      to: "rutas de escritura de members / subscriptions / finance / scheduling"
      via: "app.inject con tenantId spoofeado en el payload y SELECT del tenant_id resultante"
      pattern: "tenant_id"
---

<objective>
Probar el criterio 2 del ROADMAP: mandar `tenantId` en el body de rutas de escritura clave y ver
que la fila nace igual con `tenant_id = 1`, porque el tenant sale exclusivamente de contexto
server-side. Y cerrar la auditoría de D-08: los 6 sitios que spreadean `request.body` quedan
inventariados y con un guard que se pone rojo si alguien afloja su `additionalProperties`.

Purpose: con el `DEFAULT 1` de la 167, el riesgo real de la fase no son columnas faltantes sino
mass-assignment. Este plan es el que convierte "el repo ya enumera campos" en una invariante
verificada en vez de una costumbre.
Output: `test/tenancy/con-03-write-paths-tenant-id.test.ts` en verde, con la batería y el guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc/169-CONTEXT.md
@.planning/phases/169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc/169-PATTERNS.md
@.planning/phases/169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc/169-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contratos que el executor NO tiene que salir a buscar. -->

**Inventario D-08 — los 6 (y únicos) sitios de `src/` que spreadean el body de un request**,
verificados sobre `origin/master` (`grep -rn "\.\.\.request\.body" --include=*.ts src/` da
exactamente estos 6, ni uno más):

| #   | Sitio                              | Línea                                                                   | Body-schema que lo protege                            | Export                                |
| --- | ---------------------------------- | ----------------------------------------------------------------------- | ----------------------------------------------------- | ------------------------------------- |
| 1   | `src/modules/members/routes.ts`    | :650-655 `createMember({ ...request.body, createdBy, referredBy })`     | `createMemberSchema` (`members/schemas.ts:472`)       | exportado                             |
| 2   | `src/modules/members/routes.ts`    | :765-768 `createTrialMember({ ...request.body, createdBy })`            | `createTrialMemberSchema` (`members/schemas.ts:315`)  | exportado                             |
| 3   | `src/modules/scheduling/routes.ts` | :635-638 `rescheduleTrial({ bookingId, ...request.body })`              | `rescheduleTrialSchema` (`scheduling/schemas.ts:582`) | exportado                             |
| 4   | `src/modules/finance/routes.ts`    | :310 `transactionService.create({ ...request.body, validationStatus })` | `createTransactionSchema` (`finance/schemas.ts:52`)   | exportado                             |
| 5   | `src/modules/campaigns/routes.ts`  | :187 `service.create({ ...request.body, country })`                     | `createCampaignSchema` (`campaigns/schemas.ts:22`)    | exportado                             |
| 6   | `src/modules/gladius/routes.ts`    | :185-188 `createProduct({ ...request.body, country })`                  | `createProductSchema` (`gladius/routes.ts:54`)        | **NO exportado — hay que exportarlo** |

**Hallazgo del sondeo previo (confirmarlo, no asumirlo):** los 6 body-schemas YA declaran
`additionalProperties: false`, y ningún service del repo hace `.values({ ...body })` — todos
enumeran campos (`finance/transaction-service.ts:280-300`, `members/service.ts:691-716`). O sea
que la auditoría de D-08 probablemente no encuentre nada que arreglar. Eso NO reduce el alcance
del plan: lo que falta no es el arreglo, es el **guard** que impida la regresión, y el registro
del inventario en el SUMMARY. Si el executor SÍ encuentra un `additionalProperties` faltante, lo
agrega con un comentario que cite la regla del milestone y el precedente
`members/routes.ts:766` ("Phase 114 D-31: createdBy comes from the JWT, never the request body")
— `tenant_id` es exactamente el mismo contrato, un escalón más arriba.

**Guard por import, no por grep.** Los 6 schemas se importan como objetos y se afirma
`schema.body.additionalProperties === false`. Un grep sobre el archivo contaría la palabra en un
comentario o en un sub-schema anidado y daría verde de mentira. Para el #6 hay que agregarle
`export` a `const createProductSchema` en `src/modules/gladius/routes.ts` (un cambio de una
palabra; no mover el schema de archivo).

**Batería representativa D-09 — las 5 rutas** (una por módulo crítico, cubriendo los 3 sitios de
spread que crean filas):

| Ruta                               | Método y URL                                                                                           | Tabla cuya fila se inspecciona |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------ | ------------------------------ |
| alta de socio asistida (spread #1) | `POST /api/admin/members`                                                                              | `users`                        |
| alta de lead de prueba (spread #2) | `POST /api/admin/members/trial`                                                                        | `users`                        |
| cobro (spread #4)                  | `POST /api/admin/finance/transactions`                                                                 | `financial_transactions`       |
| asignación de plan                 | `POST /api/admin/subscriptions/members/:userId/subscription/assign`                                    | `subscriptions`                |
| reserva                            | la ruta `POST` de `src/modules/scheduling/routes.ts:850` (`/reserve`); el prefijo sale de `src/app.ts` | `bookings`                     |

`test/helpers.ts` ya trae `createStaffUser`, `createTestPlan`, `createTestMember`,
`assignTestPlan` (que acepta `overrides`, así que meter `tenantId` en el payload es directo) y
`ensureEfectivoCaja` (necesario para el cobro).

**La trampa que hace que este test valga algo (T-168-15 + la del tenant inexistente):**

1. El `tenantId` spoofeado tiene que ser el de un tenant que EXISTE (`90369`, sembrado por el
   archivo). Si se mandara un id inexistente, la fila nacería en 1 igual pero por la FK, no
   porque el código lo ignore: el test pasaría en verde probando nada.
2. Las aserciones van por `SELECT tenant_id FROM <tabla> WHERE id = ?` contra la fila realmente
   creada, no contra el body de la respuesta.
3. Cada ruta se afirma DOS veces: con `tenantId` spoofeado y sin él, y las dos filas tienen que
   nacer con el mismo `tenant_id = 1`.

**Id de tenant de este archivo: `90369`.** Ningún otro archivo de la fase lo usa.

**Alcance declarado:** esta batería es representativa por decisión de D-09. El barrido del 100%
de las rutas llega con el manifiesto de la fase 171 y la batería ISO-03 de la 172. Escribirlo en
el docblock para que nadie lea el archivo como una promesa de cobertura total.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditoría de los 6 sitios de mass-assignment y guard fail-closed de sus body-schemas</name>
  <files>el-templo-api/test/tenancy/con-03-write-paths-tenant-id.test.ts, el-templo-api/src/modules/gladius/routes.ts, el-templo-api/src/modules/members/schemas.ts, el-templo-api/src/modules/finance/schemas.ts, el-templo-api/src/modules/campaigns/schemas.ts, el-templo-api/src/modules/scheduling/schemas.ts</files>
  <read_first>
    - el-templo-api/src/modules/members/schemas.ts (worktree: `createTrialMemberSchema` :315-400 y `createMemberSchema` :472-540, incluidos los comentarios de spoof-guard de :339 y :382)
    - el-templo-api/src/modules/finance/schemas.ts (`createTransactionSchema` :52-95)
    - el-templo-api/src/modules/campaigns/schemas.ts (`createCampaignSchema` :22-45)
    - el-templo-api/src/modules/scheduling/schemas.ts (`rescheduleTrialSchema` :582-605)
    - el-templo-api/src/modules/gladius/routes.ts (`createProductSchema` :54-68 y el spread de :185-188)
    - el-templo-api/src/db/schema/tenant-column.ts (:11-16 — el texto canónico "el valor SALE SIEMPRE DEL SERVIDOR … JAMÁS de un payload" a citar en el docblock del guard)
    - el-templo-api/test/db/tenant-tables.test.ts (idioma de los gates estructurales que dejó la 168, con mensaje que lista los incumplidores)
  </read_first>
  <action>
    Re-correr la auditoría desde cero antes de escribir nada:
    `grep -rn "\.\.\.request\.body\|\.\.\.req\.body" --include=*.ts src/` y
    `grep -rn "values({ \.\.\." --include=*.ts src/`. Si aparece un sitio que no está en la tabla
    de `<interfaces>`, sumarlo al guard y reportarlo en el SUMMARY — la tabla es el inventario
    conocido, no un límite.

    Para cada uno de los 6, verificar que su body-schema declare `additionalProperties: false` en
    el nivel raíz del `body`. Donde falte, agregarlo con un comentario que cite la regla del
    milestone (`tenant_id` jamás del borde, `src/db/schema/tenant-column.ts:11-16`) y el
    precedente `members/routes.ts:766`. Donde ya esté, no tocar nada.

    Agregar `export` a `const createProductSchema` en `src/modules/gladius/routes.ts` para poder
    importarlo desde el test (único cambio de código previsto en ese archivo).

    Crear `test/tenancy/con-03-write-paths-tenant-id.test.ts` con un primer `describe`,
    "guard de mass-assignment (D-08)", que NO necesita DB: importa los 6 objetos de schema y
    afirma `body.additionalProperties === false` para cada uno, con el mensaje del `expect`
    nombrando el schema y el sitio de spread que protege. Documentar arriba por qué el guard es
    por import y no por grep (un grep contaría la palabra en un comentario o en un sub-schema
    anidado), y qué hacer si se cae: no borrar la aserción, decidir conscientemente si esa ruta
    puede aceptar propiedades libres y, si puede, dejar de spreadear su body.

    Registrar en el SUMMARY el inventario completo con el estado encontrado de cada uno de los 6
    (ya cerrado / se cerró en este plan), que es el entregable documental de D-08.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-169-tenant-layer/el-templo-api && npx tsc --noEmit && test "$(grep -rn '\.\.\.request\.body' --include=*.ts src/ | wc -l)" -eq 6 && grep -q 'export const createProductSchema' src/modules/gladius/routes.ts && npx vitest run test/tenancy/con-03-write-paths-tenant-id.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - El grep de `...request.body` sobre `src/` sigue dando exactamente 6 resultados y el SUMMARY los lista con archivo, línea y estado
    - Los 6 body-schemas declaran `additionalProperties: false` en el nivel raíz del `body`; el SUMMARY dice cuáles ya lo tenían y cuáles se cerraron acá
    - `createProductSchema` está exportado desde `src/modules/gladius/routes.ts`
    - El primer `describe` del archivo de test importa los 6 objetos de schema y afirma `body.additionalProperties === false` para cada uno, con el nombre del schema y su sitio de spread en el mensaje del `expect`
    - El docblock del guard explica por qué la verificación es por import y no por grep, y qué hacer cuando se caiga
    - `npx tsc --noEmit` sale 0
  </acceptance_criteria>
  <done>El inventario D-08 está cerrado y verificado, y una regresión del `additionalProperties` de cualquiera de los 6 pone la suite en rojo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Batería D-09 — tenantId en el body no cambia el tenant_id de la fila</name>
  <files>el-templo-api/test/tenancy/con-03-write-paths-tenant-id.test.ts</files>
  <read_first>
    - el-templo-api/test/helpers.ts (`createTestApp`, `createStaffUser` :443, `createTestPlan` :328, `createTestMember` :352, `assignTestPlan` :392 con su parámetro `overrides`, `ensureEfectivoCaja` :299)
    - el-templo-api/test/tenancy/con-01-uniques-cross-tenant.test.ts (:38-63 constantes y alcance, :109-164 helpers de aserción con mensaje explicativo, :270-289 limpieza del segundo tenant)
    - el-templo-api/test/shared/tenant-scope.test.ts (:105-119 — `afterEach` incondicional que devuelve el tenant 1 a 'active')
    - el-templo-api/src/app.ts (:177-262 — los prefijos reales: `/api/admin/members`, `/api/admin/subscriptions`, `/api/admin/finance`; y el prefijo bajo el que se registra scheduling)
    - el-templo-api/src/modules/finance/schemas.ts (campos obligatorios de `createTransactionSchema`, para armar un cobro válido)
    - el-templo-api/src/modules/scheduling/routes.ts (:850 — la ruta `/reserve` y su body)
  </read_first>
  <behavior>
    - `POST /api/admin/members` con `tenantId: 90369` en el payload crea el socio y la fila de `users` tiene `tenant_id = 1`.
    - `POST /api/admin/members/trial` con `tenantId: 90369` idem.
    - `POST /api/admin/finance/transactions` con `tenantId: 90369` crea la transacción con `tenant_id = 1`.
    - `POST /api/admin/subscriptions/members/:userId/subscription/assign` con `tenantId: 90369` crea la suscripción con `tenant_id = 1`.
    - La reserva creada por la ruta `/reserve` con `tenantId: 90369` en el body nace con `tenant_id = 1`.
    - Para cada ruta, la fila creada SIN el campo spoofeado también nace con `tenant_id = 1` (control).
  </behavior>
  <action>
    Agregar al archivo un segundo `describe`, "batería D-09 — el tenant no viene del borde", con
    las 5 rutas de la tabla de `<interfaces>`.

    `beforeAll`: `createTestApp()` y sembrar el tenant `90369` (borrado defensivo previo, slug
    propio del archivo). **El tenant tiene que EXISTIR**: sin eso, la fila nacería en 1 por la FK
    y no por el código, y el test probaría nada. Escribir ese razonamiento en un comentario
    arriba del `beforeAll`, citando T-168-15.
    `afterEach`: devolver el tenant 1 a `active` incondicionalmente.
    `afterAll`: borrar el tenant 90369 y cerrar la app.

    Escribir un helper local de aserción —siguiendo el idioma de `con-01` :109-164— que reciba la
    tabla y el id de la fila y afirme `tenant_id === 1`, con un mensaje que explique qué
    significa el fallo ("la fila nació en el tenant X: el `tenant_id` se está tomando del body").

    Cada ruta se ejercita DOS veces: con el campo `tenantId` spoofeado en el payload y sin él, y
    las dos filas se afirman en 1. La versión sin spoof es el control que distingue "lo ignoró"
    de "esta ruta nunca escribe tenant".

    Los fixtures mínimos (staff autenticado, plan, sede, caja para el cobro, horario para la
    reserva) salen de `test/helpers.ts`; no agregar helpers nuevos a ese archivo (las fixtures
    2-tenant formales son de la fase 171). Si una ruta necesita demasiado andamiaje, reutilizar
    el setup del test existente de ese módulo copiándolo localmente, nunca importándolo de otro
    archivo de test.

    Documentar en el docblock que la batería es representativa por D-09 y que el barrido del 100%
    llega con el manifiesto de la 171 / ISO-03 de la 172.

    Correr el archivo SOLO: `npx vitest run test/tenancy/con-03-write-paths-tenant-id.test.ts
    --no-file-parallelism`.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-169-tenant-layer/el-templo-api && npx vitest run test/tenancy/con-03-write-paths-tenant-id.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - Hay un test por cada una de las 5 rutas de la tabla de `<interfaces>`, cada uno con su par de control sin `tenantId` spoofeado (10 casos en total)
    - El tenant 90369 se siembra en `beforeAll` y existe al momento de cada request (verificable porque un `SELECT` sobre `tenants` dentro del test lo encuentra), con el comentario que explica por qué tiene que existir
    - Las aserciones son `SELECT tenant_id FROM <tabla> WHERE id = ?` sobre la fila creada, no sobre el body de la respuesta
    - El helper de aserción produce un mensaje que nombra el tenant encontrado y explica qué significa el fallo
    - `afterEach` devuelve el tenant 1 a `active` y `afterAll` borra el 90369; después de la corrida `SELECT COUNT(*) FROM tenants WHERE id = 90369` da 0
    - El docblock declara que la batería es representativa (D-09) y nombra la fase 171/172 como la del barrido completo
    - `npx vitest run test/tenancy/con-03-write-paths-tenant-id.test.ts --no-file-parallelism` termina con 0 fallos
  </acceptance_criteria>
  <done>El criterio 2 del ROADMAP está probado por comportamiento en 5 rutas de escritura clave, con un tenant spoofeado que existe de verdad.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                            | Description                                                                                              |
| --------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| cliente autenticado → body de una ruta de escritura | el borde donde un `tenant_id` spoofeado entraría al sistema                                              |
| body validado → service                             | los 6 sitios que spreadean el body son el punto donde una propiedad extra podría llegar a un `.values()` |
| JSON-Schema de Fastify → ajv `removeAdditional`     | `additionalProperties: false` es lo que strippea las propiedades desconocidas                            |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                       | Disposition | Mitigation Plan                                                                                                                                                               |
| --------- | ---------------------- | ------------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-169-38  | Elevation of Privilege | `tenantId` spoofeado en el body llegando a un INSERT                            | mitigate    | Batería de 5 rutas que afirma `tenant_id = 1` por SELECT sobre la fila creada, con un tenant spoofeado que EXISTE (si no, la FK haría pasar el test por el motivo equivocado) |
| T-169-39  | Tampering              | regresión futura: alguien afloja `additionalProperties` en uno de los 6 schemas | mitigate    | Guard por import de los 6 objetos de schema, con mensaje que nombra el sitio de spread que protege. Falla ruidosa, no silenciosa                                              |
| T-169-40  | Tampering              | sitio de spread NUEVO agregado en otro módulo                                   | mitigate    | La auditoría se re-corre en el plan (`grep` de `...request.body`) y `<verify>` exige que el conteo siga siendo 6; un sitio nuevo rompe el plan y obliga a decidir             |
| T-169-41  | Repudiation            | test verde que en realidad no prueba nada por el `DEFAULT 1` o por la FK        | mitigate    | Tenant spoofeado existente + par de control sin spoof + aserciones por SELECT sobre la fila creada, no sobre la respuesta HTTP                                                |
| T-169-42  | Information Disclosure | leer la batería como cobertura total de rutas de escritura                      | accept      | El docblock declara que es representativa por D-09 y nombra la fase 171/172 como la del barrido completo                                                                      |
| T-169-SC  | Tampering              | instalación de paquetes npm                                                     | mitigate    | Cero dependencias nuevas; `node_modules` por symlink verificado en 169-01                                                                                                     |

</threat_model>

<verification>
- `npx tsc --noEmit` verde.
- `npx vitest run test/tenancy/con-03-write-paths-tenant-id.test.ts --no-file-parallelism` verde con los 10 casos de la batería más el guard de los 6 schemas.
- El grep de `...request.body` sigue dando 6.
- Base del worker sin residuo del tenant 90369.
- Commit con `git add` por ruta explícita, nunca `git add -A`.
</verification>

<success_criteria>

- Criterio 2 del ROADMAP probado en 5 rutas de escritura clave con un tenant spoofeado existente.
- Inventario D-08 cerrado y registrado en el SUMMARY con el estado de los 6 sitios.
- Guard por import que deja la suite en rojo si se afloja el `additionalProperties` de cualquiera de los 6.
- `createProductSchema` exportado para que el guard pueda alcanzarlo.
  </success_criteria>

<output>
Create `.planning/phases/169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc/169-08-SUMMARY.md` when done
</output>
</content>
