---
phase: 169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc
plan: 04
type: execute
wave: 3
depends_on: ["169-02", "169-03"]
files_modified:
  - el-templo-api/test/tenancy/con-04-crons-per-tenant.test.ts
  - el-templo-api/src/index.ts
autonomous: true
requirements: [CON-04]

must_haves:
  truths:
    - "Con dos tenants activos sembrados, el cuerpo de un cron real corre exactamente dos veces, una por tenant (criterio 3 del ROADMAP)"
    - "Con el segundo tenant en suspended y en archived, el cuerpo corre una sola vez: el tenant no activo NO se procesa"
    - "Si el cuerpo falla para un tenant, el otro igual se procesa y runX resuelve sin lanzar (D-03 probado sobre un job real, no sobre el helper)"
    - "Un job nuevo agregado a src/jobs/ sin pasar por forEachActiveTenant deja la suite en rojo (gate fail-closed de cobertura de D-01)"
  artifacts:
    - path: "el-templo-api/test/tenancy/con-04-crons-per-tenant.test.ts"
      provides: "Prueba del criterio 3 sobre crons reales + gate de cobertura de los 7 jobs"
      min_lines: 200
  key_links:
    - from: "el-templo-api/test/tenancy/con-04-crons-per-tenant.test.ts"
      to: "el-templo-api/src/jobs/"
      via: "lectura del fuente de los 7 jobs y assert de que cada uno referencia forEachActiveTenant"
      pattern: "forEachActiveTenant"
---

<objective>
Probar el criterio 3 del ROADMAP contra crons REALES (no contra el helper): con dos tenants
sembrados, el cuerpo del job corre una vez por tenant activo, un tenant suspendido o archivado
no se procesa, y un tenant que falla no frena al otro. Y dejar un gate fail-closed que ponga en
rojo cualquier job futuro que se agregue sin el sweep.

Purpose: sin el gate, D-01 ("ningún cron queda para acordarse después") se cumple hoy y se
rompe en la próxima fase que agregue un job. El gate convierte la decisión en una invariante.
Output: `test/tenancy/con-04-crons-per-tenant.test.ts` en verde y el comentario de arranque de
`src/index.ts` actualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc/169-CONTEXT.md
@.planning/phases/169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc/169-PATTERNS.md
@.planning/phases/169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc/169-02-SUMMARY.md
@.planning/phases/169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc/169-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contratos que el executor NO tiene que salir a buscar. -->

**Id de tenant de este archivo: `90269`.** Ningún otro archivo de la fase lo usa
(`isolate: false` en `vitest.config.ts:33-40` hace que dos archivos con el mismo id se pisen
dentro del worker).

**Cómo se observa "el cuerpo corrió una vez por tenant" sin fixtures pesadas:** los jobs
instancian sus services DENTRO del cuerpo por tenant (planes 169-02/169-03), así que un spy
sobre el método del service cuenta exactamente las vueltas del sweep, sin mockear el sweep ni
la DB. Camino real de punta a punta salvo el método espiado.

| Job representativo                                         | Seam a espiar                                                                                                    | Por qué este                                                                                              |
| ---------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| `runAutoApprove(db)` (`src/jobs/auto-approve.ts`)          | `vi.spyOn(AdminSessionService.prototype, "autoApprovePendingSessions")` con `mockResolvedValue({ approved: 0 })` | el service se construye dentro del cuerpo por tenant; una sola llamada por vuelta                         |
| `runExpireLostLeads(db)` (`src/jobs/expire-lost-leads.ts`) | sin spy: corre de verdad contra MySQL                                                                            | prueba que un job con `sql` `` crudo sobrevive al sweep con 2 tenants y devuelve el mismo tipo de siempre |

**Estados a recorrer sobre el tenant 90269:** `active` (2 vueltas), `suspended` (1 vuelta),
`archived` (1 vuelta). La comparación del helper es contra `'active'`, así que los tres valores
del enum de `src/db/schema/tenants.ts:45-49` quedan cubiertos.

**Aislamiento de errores sobre job real (D-03):** con los dos tenants activos, hacer que el spy
lance en su SEGUNDA invocación (`mockRejectedValueOnce` después de un `mockResolvedValueOnce`,
o un contador). `runAutoApprove(db)` debe RESOLVER (no lanzar) y el spy debe haber sido llamado
2 veces.

**Gate de cobertura de D-01 (fail-closed):** leer con `fs.readFileSync` los archivos de
`src/jobs/*.ts`, filtrar las líneas de comentario antes de contar (`grep`-en-JS: descartar las
que empiezan con `//`, `/*` o `*`), y afirmar dos cosas:

1. la cantidad de archivos de job es exactamente **7** — si aparece uno nuevo, el test se cae y
   obliga a decidir conscientemente (mismo espíritu que el `onRoute` fail-closed de la 171);
2. todo archivo que contenga `cron.schedule` contiene también `forEachActiveTenant`.
   El mensaje del `expect` tiene que listar los archivos incumplidores por nombre, no solo fallar.

**Los 7 jobs esperados:** `auto-approve.ts`, `auto-resume-pauses.ts`, `expire-lost-leads.ts`,
`mark-no-shows.ts`, `notification-cron.ts`, `reassign-multibranch.ts`, `wellhub-sync.ts`.

**Higiene obligatoria del archivo:**

- Todo insert estampa `tenantId` explícito (trampa del `DEFAULT 1`, T-168-15).
- `afterEach` incondicional que devuelve el tenant 1 a `status='active'` — si el archivo deja el
  tenant 1 suspendido rompe todos los tests siguientes del worker.
- `afterAll` que borra el tenant 90269 en orden seguro de FKs.
- NO usar `cleanAllTestData` en los tests que dependen del segundo tenant: es admin-global y no
  está scopeada por tenant (warning heredado del 168-REVIEW).
- `vi.restoreAllMocks()` en `afterEach` para que el spy no se filtre a otros archivos del worker.
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test de criterio 3 — el cuerpo del cron corre una vez por tenant activo</name>
  <files>el-templo-api/test/tenancy/con-04-crons-per-tenant.test.ts</files>
  <read_first>
    - el-templo-api/test/jobs/reassign-multibranch.test.ts (:11-92 — único test de job del repo: `createTestApp`, reloj pinneado con `vi.useFakeTimers`, ciclo de vida beforeAll/afterAll)
    - el-templo-api/test/tenancy/con-01-uniques-cross-tenant.test.ts (:38-63 declaración de alcance y constantes de tenant, :166-173 la trampa del DEFAULT 1, :270-289 la limpieza del segundo tenant)
    - el-templo-api/test/shared/tenant-scope.test.ts (:105-119 — el `afterEach` incondicional que devuelve el tenant 1 a 'active')
    - el-templo-api/src/jobs/auto-approve.ts (worktree, ya con sweep: dónde se instancia `AdminSessionService`)
    - el-templo-api/src/jobs/expire-lost-leads.ts (worktree, ya con sweep: el tipo de retorno y qué necesita en la DB para expirar un lead)
    - el-templo-api/src/modules/shared/tenant.ts (semántica de `forEachActiveTenant`)
  </read_first>
  <behavior>
    - Con el tenant 1 y el 90269 en `active`, `runAutoApprove(db)` invoca `autoApprovePendingSessions` exactamente 2 veces.
    - Con el 90269 en `suspended`, exactamente 1 vez. Con el 90269 en `archived`, exactamente 1 vez.
    - Con los dos activos y el spy lanzando en la segunda invocación, `runAutoApprove(db)` resuelve sin lanzar y el spy fue llamado 2 veces.
    - `runExpireLostLeads(db)` con los dos tenants activos devuelve `{ expired, skippedManual }` numéricos y no lanza (job con SQL crudo bajo el sweep).
  </behavior>
  <action>
    Crear `test/tenancy/con-04-crons-per-tenant.test.ts` con el idioma del `con-01`: constantes
    `TENANT_TEMPLO = 1` y `TENANT_SEGUNDO = 90269` arriba, con el comentario de por qué el id es
    alto y fijo, y la declaración de alcance que repita que las fixtures 2-tenant completas son
    de la fase 171 (ISO-03) — este archivo no adelanta esa API ni agrega nada a `test/helpers.ts`.

    `beforeAll`: `createTestApp()` y sembrar el tenant 90269 (borrado defensivo previo) con slug
    propio del archivo. `beforeEach`: dejar el 90269 en `active` y `vi.restoreAllMocks()`.
    `afterEach`: devolver el tenant 1 a `active` incondicionalmente. `afterAll`: borrar el 90269
    y cerrar la app.

    Escribir los cuatro tests de `<behavior>` sobre `runAutoApprove`, más el smoke de
    `runExpireLostLeads`. En cada uno, el número esperado de invocaciones va como aserción
    exacta (`toHaveBeenCalledTimes`), no como "al menos": el punto del test es distinguir 2 de 1.

    Documentar arriba del archivo por qué se espía el método del service y no se mockea
    `forEachActiveTenant`: un mock del sweep probaría el mock. El spy sobre el service deja vivo
    el camino real (`listActiveTenants` contra MySQL, el loop, el try/catch) y solo corta en el
    punto donde empezaría la lógica de negocio, que en esta fase no cambia (D-02).

    Correr el archivo SOLO: `npx vitest run test/tenancy/con-04-crons-per-tenant.test.ts
    --no-file-parallelism` (más de un archivo a la vez revienta el timeout de 120 s del
    provisioning en esta máquina).

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-169-tenant-layer/el-templo-api && npx vitest run test/tenancy/con-04-crons-per-tenant.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - El archivo declara `const TENANT_SEGUNDO = 90269` y ningún otro archivo de la fase usa ese id
    - Existe un test que afirma `toHaveBeenCalledTimes(2)` con los dos tenants activos y otros dos que afirman `toHaveBeenCalledTimes(1)` con `suspended` y con `archived`
    - Existe un test donde el spy lanza en la segunda invocación, `runAutoApprove(db)` resuelve sin lanzar y el spy fue llamado 2 veces
    - Existe un smoke de `runExpireLostLeads(db)` con dos tenants activos que afirma el tipo de retorno y que no lanza
    - Hay `afterEach` incondicional que devuelve el tenant 1 a `status='active'` y `vi.restoreAllMocks()`
    - El archivo NO llama a `cleanAllTestData` en los tests que dependen del tenant 90269, y su `afterAll` lo borra
    - Después de la corrida, `SELECT COUNT(*) FROM tenants WHERE id = 90269` da 0 en la base del worker
    - `npx vitest run test/tenancy/con-04-crons-per-tenant.test.ts --no-file-parallelism` termina con 0 fallos
  </acceptance_criteria>
  <done>El criterio 3 del ROADMAP está probado sobre crons reales, incluido el tenant no activo y el aislamiento de errores.</done>
</task>

<task type="auto">
  <name>Task 2: Gate fail-closed de cobertura de los 7 jobs + comentario de arranque en index.ts</name>
  <files>el-templo-api/test/tenancy/con-04-crons-per-tenant.test.ts, el-templo-api/src/index.ts</files>
  <read_first>
    - el-templo-api/test/tenancy/con-04-crons-per-tenant.test.ts (lo escrito en Task 1)
    - el-templo-api/src/index.ts (worktree: el bloque de arranque de crons :37-45 y su comentario)
    - el-templo-api/test/db/tenant-tables.test.ts (worktree: los gates de forma sobre registros que dejó la 168 — el idioma de "assert estructural con mensaje que lista los incumplidores")
    - el-templo-api/src/jobs/ (los 7 archivos, para confirmar el inventario)
  </read_first>
  <action>
    Agregar al archivo de Task 1 un `describe` aparte, "cobertura de D-01", que NO necesita DB:
    lee `src/jobs/` con `fs.readdirSync`, filtra `.ts`, y afirma
    (a) que hay exactamente 7 archivos, listándolos en el mensaje del `expect` si difiere, y
    (b) que todo archivo cuyo contenido —descartadas las líneas de comentario (`//`, `/*`, `*`)—
    contenga `cron.schedule` contenga también `forEachActiveTenant`, con el mensaje del `expect`
    enumerando los archivos incumplidores por nombre.
    El filtro de comentarios no es cosmético: sin él, la prosa de un docblock que mencione
    `forEachActiveTenant` haría pasar un job que no lo llama.

    Escribir arriba del `describe` por qué el gate es fail-closed y qué hacer cuando se caiga:
    si el job nuevo debe barrer por tenant, envolverlo; si es genuinamente global, anotarlo con
    `/* tenant-safe: <motivo> */` y sumarlo a la excepción explícita del test (que debe ser una
    lista con motivo, nunca un `skip`).

    En `src/index.ts`, actualizar el comentario de `:37-38`. Hoy dice que mark-no-shows y
    notifications son `async` porque descubren timezones en el boot; ahora tiene que decir además
    que la lista de tenants activos se resuelve **en cada corrida del job, no en el boot**, para
    que activar o suspender un gimnasio aplique sin reiniciar el proceso (mismo espíritu que "el
    tenant no viaja en el JWT", `country-scope.ts:30-31`), citando la fase 169 y D-01. NO cambiar
    ninguna llamada ni el orden de arranque: las 7 firmas `startXJob(db)` se conservan.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-169-tenant-layer/el-templo-api && npx tsc --noEmit && npx vitest run test/tenancy/con-04-crons-per-tenant.test.ts --no-file-parallelism && test "$(git -C /home/franco/projects/et-169-tenant-layer diff --numstat -- el-templo-api/src/index.ts | awk '{print $2}')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - El archivo de test contiene un `describe` de cobertura que afirma `7` archivos en `src/jobs/` y que cada uno con `cron.schedule` referencia `forEachActiveTenant`, con las líneas de comentario descartadas antes de buscar
    - El mensaje del `expect` del gate enumera los archivos incumplidores por nombre (verificable rompiendo temporalmente un job y viendo el mensaje; revertir después)
    - `src/index.ts` solo cambió líneas de comentario: `git diff --numstat` para ese archivo muestra 0 deleciones de código (las 7 llamadas `startXJob(app.db)` siguen idénticas)
    - El comentario nuevo de `src/index.ts` menciona la fase 169, D-01 y que la lista de tenants se resuelve por corrida y no en el boot
    - `npx tsc --noEmit` sale 0 y el archivo de test sigue en verde
  </acceptance_criteria>
  <done>Un job nuevo sin sweep deja la suite en rojo, y el comentario de arranque explica dónde se resuelve la lista de tenants.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                   | Description                                                                                 |
| ---------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| `src/jobs/` → sweep por tenant                             | un job nuevo que no pase por el helper escribe/lee sin contexto de tenant y nadie se entera |
| test que siembra un 2º tenant → resto del worker de vitest | `isolate: false` comparte proceso: residuo de tenant o de spy contamina archivos siguientes |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                            | Disposition | Mitigation Plan                                                                                                                                                |
| --------- | ---------------------- | ------------------------------------------------------------------------------------ | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-169-17  | Information Disclosure | cron procesando un tenant suspendido o archivado                                     | mitigate    | Tests con aserción exacta de invocaciones (`toHaveBeenCalledTimes(1)`) sobre los dos estados no activos, no solo sobre `active`                                |
| T-169-18  | Elevation of Privilege | job futuro agregado sin sweep por tenant                                             | mitigate    | Gate fail-closed que exige 7 archivos y `forEachActiveTenant` en todo archivo con `cron.schedule`, con filtro de comentarios para que la prosa no lo satisfaga |
| T-169-19  | Repudiation            | test que pasa en verde probando el mock en vez del código                            | mitigate    | Se espía el método del service, no el sweep: `listActiveTenants`, el loop y el try/catch corren de verdad contra MySQL. Documentado en el docblock del archivo |
| T-169-20  | Denial of Service      | el archivo deja el tenant 1 suspendido y rompe todos los tests siguientes del worker | mitigate    | `afterEach` incondicional que lo devuelve a `active` + `afterAll` que borra el 90269 + `vi.restoreAllMocks()`                                                  |
| T-169-SC  | Tampering              | instalación de paquetes npm                                                          | mitigate    | Cero dependencias nuevas; `node_modules` por symlink verificado en 169-01                                                                                      |

</threat_model>

<verification>
- `npx vitest run test/tenancy/con-04-crons-per-tenant.test.ts --no-file-parallelism` verde.
- `npx tsc --noEmit` verde.
- `src/index.ts` con cambios solo de comentario.
- Base del worker sin residuo del tenant 90269.
- Commit con `git add` por ruta explícita de los 2 archivos, nunca `git add -A`.
</verification>

<success_criteria>

- Criterio 3 del ROADMAP probado sobre crons reales: 2 tenants activos = 2 vueltas, suspendido/archivado = 1.
- Aislamiento de errores (D-03) probado sobre un job real, no solo sobre el helper.
- Gate fail-closed de los 7 jobs en la suite.
- `src/index.ts` documenta dónde se resuelve la lista de tenants activos.
  </success_criteria>

<output>
Create `.planning/phases/169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc/169-04-SUMMARY.md` when done
</output>
</content>
