---
phase: 42-blog-internal-linking-system-tags-related-posts-cross-page-ctas
plan: 04
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - el-templo-web/components/BlogRelatedPosts.vue
  - el-templo-web/components/BlogPostCta.vue
  - el-templo-web/pages/blog/[slug].vue
  - el-templo-web/server/api/__sitemap__/blog.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Individual blog post page shows 'Posts Relacionados' section with up to 3 related posts"
    - "Related posts section appears after the article content, before sidebar stacks on mobile"
    - "Related post cards show thumbnail, title, and tag pills"
    - "A CTA banner appears between article end and related posts section"
    - "CTA banner type matches the post's ctaType field (trial, franchise, or app)"
    - "Tag pages are included in the sitemap"
    - "Default CTA (trial session) renders correctly when no ctaType is set"
  artifacts:
    - path: "el-templo-web/components/BlogRelatedPosts.vue"
      provides: "Related posts section with horizontal card layout"
    - path: "el-templo-web/components/BlogPostCta.vue"
      provides: "Cross-page CTA banner with 3 variants (trial, franchise, app)"
    - path: "el-templo-web/pages/blog/[slug].vue"
      provides: "Extended post page with CTA banner and related posts"
    - path: "el-templo-web/server/api/__sitemap__/blog.ts"
      provides: "Extended sitemap source including tag page URLs"
  key_links:
    - from: "el-templo-web/pages/blog/[slug].vue"
      to: "/api/blog/posts/:slug/related"
      via: "useFetch for related posts data"
      pattern: "useFetch.*related"
    - from: "el-templo-web/components/BlogPostCta.vue"
      to: "/franquicias, WhatsApp, /gladius"
      via: "NuxtLink/anchor based on ctaType prop"
      pattern: "ctaType.*trial|franchise|app"
    - from: "el-templo-web/server/api/__sitemap__/blog.ts"
      to: "/api/blog/tags"
      via: "$fetch for tag slugs"
      pattern: "fetch.*blog/tags"
---

<objective>
Add related posts section, cross-page CTA banners, and tag sitemap entries to complete the blog internal linking system.

Purpose: Related posts reduce bounce rate and increase pages/session (positive SEO signals). Cross-page CTAs drive traffic from informational content (blog) to conversion pages (franquicias, gladius, trial session). Tag pages in sitemap ensure Google discovers and indexes topical clusters.

Output: BlogRelatedPosts.vue, BlogPostCta.vue, extended [slug].vue page, extended sitemap source.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/42-blog-internal-linking-system-tags-related-posts-cross-page-ctas/42-CONTEXT.md
@.planning/phases/42-blog-internal-linking-system-tags-related-posts-cross-page-ctas/42-01-SUMMARY.md

@el-templo-web/pages/blog/[slug].vue
@el-templo-web/components/BlogSidebar.vue
@el-templo-web/server/api/**sitemap**/blog.ts

<interfaces>
<!-- API endpoints from Plan 01 -->

```
GET /api/blog/posts/:slug/related?limit=3 → { posts: [{ id, title, slug, excerpt, coverImage, publishedAt, readingTime, tags }] }
GET /api/blog/tags → { tags: [{ id, name, slug, postCount }] }
GET /api/blog/posts/:slug → { ..., ctaType: 'trial' | 'franchise' | 'app', tags: [...] }
```

Existing blog post page layout:

```
<main class="blog-post">
  <div class="blog-post__hero"> ... cover, title, meta ... </div>
  <div class="blog-post__grid">
    <article class="blog-post__article"> ... body HTML ... </article>
    <BlogSidebar :current-slug="slug" />
  </div>
</main>
```

Grid: article 70% + sidebar 30% on desktop, single column on mobile (sidebar below).

BlogSidebar contains: recent posts, brand CTA (trial session WhatsApp), social share. Per CONTEXT.md: "Sidebar CTA remains as-is (always shows trial session)".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BlogPostCta banner and BlogRelatedPosts section</name>
  <files>el-templo-web/components/BlogPostCta.vue, el-templo-web/components/BlogRelatedPosts.vue</files>
  <action>
  **1. Create BlogPostCta.vue** — Full-width CTA banner between article end and related posts:

Props:

```typescript
defineProps<{
  ctaType?: "trial" | "franchise" | "app";
}>();
```

Default ctaType to `'trial'` if not provided.

Three CTA variants (per CONTEXT.md):

**Trial session (default):**

- Heading: "Reserv&aacute; tu Sesi&oacute;n de Prueba"
- Subtext: "Descubr&iacute; tu nivel y empez&aacute; a entrenar con el M&eacute;todo El Templo."
- CTA button: "RESERVAR SESI&Oacute;N" → WhatsApp link (same URL as sidebar: `https://wa.me/5492235820521?text=...`)
- Button color: Terracotta (primary brand CTA)

**Franchise:**

- Heading: "Abr&iacute; tu Templo"
- Subtext: "Conoce nuestro modelo de franquicia y sum&aacute;te al movimiento."
- CTA button: "CONOCER FRANQUICIA" → NuxtLink to /franquicias
- Button color: Aged Gold

**App/Gladius:**

- Heading: "Entren&aacute; con la App"
- Subtext: "Sesiones personalizadas, progresiones y seguimiento de tu entrenamiento."
- CTA button: "DESCUBRIR M&Aacute;S" → NuxtLink to /gladius
- Button color: Terracotta

**Styles (BEM prefix: `blog-cta`):**

- Full width within the article column
- Background: `var(--color-sandy-beige)` with subtle border-top/bottom in `var(--color-warm-stone)`
- Padding: `var(--space-spacious)` vertical, `var(--space-comfortable)` horizontal
- Border-radius: `var(--radius-card)`
- Heading: `var(--font-authority)`, 600 weight, 22px, `var(--color-deep-charcoal)`
- Subtext: `var(--font-clarity)`, 400 weight, 15px, `var(--color-olive-stone)`, max-width 500px
- Button: Solid filled button matching brand CTA styles (see nav/hero patterns), centered
- Layout: text-align center, flex column, items center, gap 12px
- Margin: `var(--space-spacious) 0` top and bottom

**2. Create BlogRelatedPosts.vue** — "Posts Relacionados" section:

Props:

```typescript
interface RelatedPost {
  id: number;
  title: string;
  slug: string;
  excerpt: string;
  coverImage: string | null;
  publishedAt: string | null;
  readingTime: number;
  tags: Array<{ id: number; name: string; slug: string }>;
}

defineProps<{
  posts: RelatedPost[];
}>();
```

Template:

```html
<section v-if="posts.length > 0" class="blog-related">
  <h3 class="blog-related__heading">Posts Relacionados</h3>
  <div class="blog-related__grid">
    <NuxtLink
      v-for="post in posts"
      :key="post.id"
      :to="`/blog/${post.slug}`"
      class="blog-related__card"
    >
      <!-- Thumbnail -->
      <div class="blog-related__image-wrapper">
        <img
          v-if="post.coverImage"
          :src="post.coverImage"
          :alt="post.title"
          class="blog-related__image"
          loading="lazy"
        />
        <PlaceholderBox v-else aspect-ratio="16 / 9" label="" />
      </div>
      <!-- Content -->
      <div class="blog-related__content">
        <h4 class="blog-related__title">{{ post.title }}</h4>
        <div v-if="post.tags?.length" class="blog-related__tags">
          <!-- Show tag names as plain text (not links, since card is already a link) -->
          <span
            v-for="tag in post.tags.slice(0, 3)"
            :key="tag.id"
            class="blog-related__tag"
          >
            {{ tag.name }}
          </span>
        </div>
      </div>
    </NuxtLink>
  </div>
</section>
```

**Styles (BEM prefix: `blog-related`):**

- Section heading: `var(--font-authority)`, 600 weight, 18px uppercase, letter-spacing 0.08em, `var(--color-deep-charcoal)`, margin-bottom `var(--space-comfortable)`
- Grid: `display: grid; grid-template-columns: repeat(3, 1fr); gap: var(--space-comfortable);`
- Tablet: `grid-template-columns: repeat(3, 1fr)` (keep 3 columns, they're small cards)
- Mobile: `grid-template-columns: 1fr;` (stack)
- Card: `text-decoration: none; transition: transform var(--transition-base);`
- Card hover: `transform: translateY(-2px)`
- Image wrapper: `aspect-ratio: 16/9; overflow: hidden; border-radius: var(--radius-base);`
- Image: `width: 100%; height: 100%; object-fit: cover;`
- Title: `var(--font-authority)`, 500 weight, 15px, `var(--color-deep-charcoal)`, line-height 1.3, margin-top 8px
- Tags: `display: flex; flex-wrap: wrap; gap: 4px; margin-top: 6px;`
- Tag spans: `font-size: 11px; color: var(--color-olive-stone); font-family: var(--font-clarity);` (plain text, not pills, to avoid nested links)
- Margin-top on the section: `var(--space-section)`
  </action>
  <verify>
  <automated>cd /home/franco/projects/el-templo/el-templo-web && npx nuxi typecheck 2>&1 | tail -20</automated>
  </verify>
  <done> - BlogPostCta renders 3 CTA variants correctly based on ctaType prop - BlogRelatedPosts renders up to 3 cards in a responsive grid - Related post cards show thumbnail, title, and tag names - CTA buttons link to correct destinations (WhatsApp, /franquicias, /gladius) - Components type-check clean
  </done>
  </task>

<task type="auto">
  <name>Task 2: Wire CTA and related posts into post page + tag sitemap entries</name>
  <files>el-templo-web/pages/blog/[slug].vue, el-templo-web/server/api/__sitemap__/blog.ts</files>
  <action>
  **1. Extend blog/[slug].vue** to fetch and display related posts and CTA:

Add the BlogPost interface `ctaType` field:

```typescript
interface BlogPost {
  // ... existing fields
  ctaType?: "trial" | "franchise" | "app";
  tags: Array<{ id: number; name: string; slug: string }>;
}
```

Add a second `useFetch` for related posts:

```typescript
const { data: relatedData } = useFetch<{ posts: RelatedPost[] }>(
  () => `${config.public.apiUrl}/blog/posts/${slug.value}/related?limit=3`,
);
const relatedPosts = computed(() => relatedData.value?.posts ?? []);
```

**Update the template layout.** Currently the structure is:

```html
<div class="blog-post__grid">
  <article class="blog-post__article">
    <div class="blog-post__body" v-html="renderedBody" />
  </article>
  <BlogSidebar :current-slug="slug" />
</div>
```

Add CTA and related posts **inside the article column**, after the body HTML and before the grid ends. This ensures they're within the 70% article column on desktop:

```html
<div class="blog-post__grid">
  <article class="blog-post__article">
    <div class="blog-post__body" v-html="renderedBody" />

    <!-- Cross-page CTA banner (between article end and related posts) -->
    <BlogPostCta :cta-type="post.ctaType || 'trial'" />

    <!-- Related posts section -->
    <BlogRelatedPosts :posts="relatedPosts" />
  </article>
  <BlogSidebar :current-slug="slug" />
</div>
```

Per CONTEXT.md: "Bottom-of-post CTA banner: full-width styled block between article end and related posts" and "Show 3 related posts in a Posts Relacionados section at the bottom of the article, before the sidebar stacks on mobile."

**2. Extend sitemap source** `server/api/__sitemap__/blog.ts`:

Add tag page URLs alongside blog post URLs. After fetching posts, also fetch tags:

```typescript
export default defineSitemapEventHandler(async () => {
  const config = useRuntimeConfig();
  const entries = [];

  try {
    // Blog post URLs (existing)
    const postsResponse = await $fetch<{
      posts: Array<{ slug: string; updatedAt: string }>;
    }>(`${config.public.apiUrl}/blog/posts?limit=1000`);

    for (const post of postsResponse.posts) {
      entries.push({
        loc: `/blog/${post.slug}`,
        lastmod: post.updatedAt,
      });
    }

    // Tag page URLs (NEW)
    const tagsResponse = await $fetch<{
      tags: Array<{ slug: string }>;
    }>(`${config.public.apiUrl}/blog/tags`);

    for (const tag of tagsResponse.tags) {
      entries.push({
        loc: `/blog/tag/${tag.slug}`,
        changefreq: "weekly",
      });
    }
  } catch {
    // Gracefully return whatever we have
  }

  return entries;
});
```

**3. Verify no layout issues:** The CTA banner and related posts must work within the grid article column. On mobile, when the grid becomes single-column, the CTA and related posts should flow naturally before the sidebar. Test this by checking the CSS: `grid-template-columns: 1fr` at `max-width: 768px` ensures proper stacking.
</action>
<verify>
<automated>cd /home/franco/projects/el-templo/el-templo-web && npx nuxi typecheck 2>&1 | tail -20</automated>
</verify>
<done> - Blog post page shows CTA banner after article body - CTA type matches the post's ctaType (defaulting to trial) - Related posts section shows up to 3 posts below the CTA - Sidebar remains in its 30% column on desktop, below everything on mobile - Sitemap includes /blog/tag/{slug} entries for all active tags - Layout works correctly at all 3 breakpoints - Type check passes
</done>
</task>

</tasks>

<verification>
1. Type check: `cd el-templo-web && npx nuxi typecheck`
2. Visit a blog post — CTA banner visible after article content
3. Related posts section shows up to 3 posts below CTA
4. Click a related post — navigates to that post
5. Verify CTA variants: a post with ctaType 'franchise' shows franchise CTA
6. Default (no ctaType) shows trial session CTA with WhatsApp link
7. Build and check sitemap: tag page URLs included
8. Mobile view: CTA → related posts → sidebar stack correctly
</verification>

<success_criteria>

- CTA banner renders correctly with all 3 variants
- Related posts section shows tag-ranked posts with fallback to recent
- Sitemap includes tag page URLs for SEO indexing
- Layout integrity maintained across desktop/tablet/mobile
- No regressions on existing blog post page functionality
- All type checks pass clean
  </success_criteria>

<output>
After completion, create `.planning/phases/42-blog-internal-linking-system-tags-related-posts-cross-page-ctas/42-04-SUMMARY.md`
</output>
