---
phase: 125-bootstrap-llm-revisi-n-de-profes-de-la-descomposici-n
plan: 03
type: execute
wave: 3
depends_on: ["125-02"]
files_modified:
  - el-templo-admin/src/composables/useProposalsApi.ts
  - el-templo-admin/src/types/proposal.ts
  - el-templo-admin/src/pages/ProposalReviewPage.vue
  - el-templo-admin/src/router/routes.ts
  - el-templo-admin/src/layouts/AdminLayout.vue
autonomous: true
requirements: [TREE-03]
must_haves:
  truths:
    - "A profe opens a new admin page showing pending proposals in a filterable table grouped by route"
    - "Each proposal row's sub-family / leverage / route is inline-editable before accepting"
    - "A profe can accept-group (all proposals in a route group), accept a single row (with overrides), or reject a single row"
    - "Accepting/rejecting refreshes the list so the row leaves the pending view"
  artifacts:
    - path: "el-templo-admin/src/composables/useProposalsApi.ts"
      provides: "fetchProposals / acceptProposal / rejectProposal / bulkAccept against /admin/exercises/proposals*"
      exports: ["useProposalsApi"]
    - path: "el-templo-admin/src/pages/ProposalReviewPage.vue"
      provides: "Filterable route-grouped q-table with inline edit + accept/reject/accept-group"
      contains: "useProposalsApi"
    - path: "el-templo-admin/src/router/routes.ts"
      provides: "proposals route as a child of AdminLayout"
      contains: "ProposalReviewPage"
  key_links:
    - from: "el-templo-admin/src/pages/ProposalReviewPage.vue"
      to: "/admin/exercises/proposals"
      via: "useProposalsApi composable"
      pattern: "useProposalsApi"
    - from: "el-templo-admin/src/layouts/AdminLayout.vue"
      to: "/proposals"
      via: "q-item nav link"
      pattern: "/proposals"
---

<objective>
Build the profe review screen (TREE-03, frontend): a new admin page `ProposalReviewPage.vue` with a filterable, route-grouped q-table of pending proposals, inline-editable sub-family / leverage / route per row, plus accept-group, individual accept (with overrides), and reject — backed by a `useProposalsApi` composable hitting the Plan 02 endpoints. Registered as a child of AdminLayout with a nav link.

Purpose: Lets profes sweep ~1.493 exercises fast (D-07): group by route, bulk-accept a clean group, override/reject the rest, before the dimensions are fixed as truth.
Output: composable, types, page, router entry, nav link.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/125-bootstrap-llm-revisi-n-de-profes-de-la-descomposici-n/125-CONTEXT.md
@.planning/phases/125-bootstrap-llm-revisi-n-de-profes-de-la-descomposici-n/125-PATTERNS.md
@.planning/phases/125-bootstrap-llm-revisi-n-de-profes-de-la-descomposici-n/125-02-SUMMARY.md

<interfaces>
<!-- Admin frontend analogs. Reuse these shapes directly; never console.log (CLAUDE.md → createLogger). -->

From el-templo-admin/src/composables/useExercisesApi.ts (copy structure):

- `import { Notify } from 'quasar'`, `import { createLogger } from 'src/utils/logger'`, `import { extractError } from 'src/utils/extract-error'`, `api` from src/boot/axios
- `const log = createLogger('useProposalsApi')`
- error idiom: `catch (err: unknown) { const message = extractError(err, '...'); log.error('...', { error: message }); Notify.create({ type: 'negative', message }); throw err; }`
- `export function useProposalsApi() { ... return { loading, error, fetchProposals, acceptProposal, rejectProposal, bulkAccept } }`

From el-templo-admin/src/pages/ExercisesPage.vue (copy table/filter/inline-edit shape):

- filter bar: q-input search + q-select route/status with `@update:model-value="onFilterChange"`
- q-table with `body-cell-<col>` slots; name-cell click-to-edit + inline q-select for category (calls `onInline...Change(props.row.id, val)`) — these are the patterns for inline-editing proposed_subfamily / proposed_leverage / proposed_route
- script imports: `import { ref, reactive, computed, onMounted } from 'vue'`, `import { useQuasar } from 'quasar'`, `import type { QTableProps } from 'quasar'`

From el-templo-admin/src/router/routes.ts (AdminLayout children, line 36-38):

- `{ path: 'exercises', component: () => import('pages/ExercisesPage.vue') }` — add a sibling `{ path: 'proposals', component: () => import('pages/ProposalReviewPage.vue') }`

From el-templo-admin/src/layouts/AdminLayout.vue (nav, line 37-42):

- nav link pattern: `<q-item clickable v-ripple to="/exercises"> ... <q-item-section>Ejercicios</q-item-section> </q-item>` — add a sibling pointing to `/proposals` (e.g. label "Revisión de dimensiones") in the Entrenamiento group

Plan 02 API surface (from /admin/exercises/proposals\*):

- GET /admin/exercises/proposals?route=&status= → { rows, total } where each row has proposal id, exercise id, exercise name, current route, proposedSubfamily, proposedLeverage, proposedRoute, routePending, status
- POST /admin/exercises/proposals/:id/accept (optional body: proposedSubfamily?, proposedLeverage?, proposedRoute?)
- POST /admin/exercises/proposals/:id/reject
- POST /admin/exercises/proposals/bulk-accept (body: { ids: number[] })
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: useProposalsApi composable + proposal types</name>
  <files>el-templo-admin/src/composables/useProposalsApi.ts, el-templo-admin/src/types/proposal.ts</files>
  <read_first>
    - el-templo-admin/src/composables/useExercisesApi.ts (composable structure, axios + extractError + Notify + createLogger error idiom)
    - el-templo-admin/src/types/exercise.ts (type-file shape to mirror)
  </read_first>
  <action>
    Create `el-templo-admin/src/types/proposal.ts` mirroring exercise.ts: a `Proposal` interface (id, exerciseId, exerciseName, route, proposedSubfamily: string | null, proposedLeverage: string | null, proposedRoute: string | null, routePending: boolean, status: 'pending' | 'accepted' | 'rejected') and a `ProposalListResponse` ({ rows: Proposal[]; total: number }). No `any` (CLAUDE.md).

    Create `el-templo-admin/src/composables/useProposalsApi.ts` copying useExercisesApi.ts structure: `const log = createLogger('useProposalsApi')`, `loading`/`error` refs, `api` from src/boot/axios, the extractError + Notify + log.error error idiom (NEVER console.log). Export `useProposalsApi()` returning:
    - `fetchProposals(filters: { route?: string; status?: string })` → `api.get<ProposalListResponse>('/admin/exercises/proposals', { params })` (axios serializer omits null/undefined keys — keep default status undefined → backend defaults to pending).
    - `acceptProposal(id, overrides?: { proposedSubfamily?; proposedLeverage?; proposedRoute? })` → `api.post('/admin/exercises/proposals/${id}/accept', overrides ?? {})`.
    - `rejectProposal(id)` → `api.post('/admin/exercises/proposals/${id}/reject', {})`.
    - `bulkAccept(ids: number[])` → `api.post('/admin/exercises/proposals/bulk-accept', { ids })`.
    Each method wraps in the standard try/catch error idiom and rethrows.

  </action>
  <acceptance_criteria>
    - `cd el-templo-admin && npx vue-tsc --noEmit` (or the project's typecheck script) exits 0
    - useProposalsApi.ts exports `useProposalsApi` with fetchProposals/acceptProposal/rejectProposal/bulkAccept
    - No `console.log/warn/error` anywhere in the new files (uses createLogger)
    - No `any` types
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit && grep -c "useProposalsApi" src/composables/useProposalsApi.ts && ! grep -q "console\." src/composables/useProposalsApi.ts</automated>
  </verify>
  <done>Typed composable hits all four proposal endpoints with the standard error idiom; typecheck passes; no console.log; no any.</done>
</task>

<task type="auto">
  <name>Task 2: ProposalReviewPage + router entry + nav link</name>
  <files>el-templo-admin/src/pages/ProposalReviewPage.vue, el-templo-admin/src/router/routes.ts, el-templo-admin/src/layouts/AdminLayout.vue</files>
  <read_first>
    - el-templo-admin/src/pages/ExercisesPage.vue (filter bar + q-table + body-cell inline-edit slots — copy shape)
    - el-templo-admin/src/composables/useProposalsApi.ts (Task 1)
    - el-templo-admin/src/router/routes.ts (AdminLayout children, exercises entry line 36-38)
    - el-templo-admin/src/layouts/AdminLayout.vue (nav q-item pattern line 37-42)
  </read_first>
  <action>
    Create `el-templo-admin/src/pages/ProposalReviewPage.vue` reusing ExercisesPage.vue's table/filter shape:
    - Filter bar: q-select for route (drives grouping per D-07) + q-select for status (default pending). On change, call `fetchProposals`.
    - q-table of proposals ordered/grouped by route. Use `body-cell-proposedSubfamily`, `body-cell-proposedLeverage`, `body-cell-proposedRoute` slots with click-to-edit / inline q-input or q-select (mirror ExercisesPage name-cell + category inline edit) so the profe can override before accepting (inline-edit, D-07). Edits update a local row model; they are sent as overrides on accept.
    - Per-row actions: Accept button → `acceptProposal(row.id, editedFieldsIfAny)`; Reject button → `rejectProposal(row.id)`. After each, refresh the list (the row leaves the pending view).
    - Group-level "Aceptar grupo" button → `bulkAccept(idsInCurrentRouteGroup)` then refresh (D-07). (No per-row override is applied in bulk-accept — bulk uses the proposed values; overrides are for individual accept.)
    - Loading/empty states; `onMounted` initial fetch. Script imports per ExercisesPage (ref/reactive/computed/onMounted, useQuasar, QTableProps, useProposalsApi). NEVER console.log — the composable logs via createLogger.

    Modify `el-templo-admin/src/router/routes.ts`: add `{ path: 'proposals', component: () => import('pages/ProposalReviewPage.vue') }` as a child of AdminLayout, sibling to the exercises entry.

    Modify `el-templo-admin/src/layouts/AdminLayout.vue`: add a nav `q-item` in the Entrenamiento group pointing `to="/proposals"` with label e.g. "Revisión de dimensiones" (mirror the Ejercicios q-item shape line 37-42).

  </action>
  <acceptance_criteria>
    - `cd el-templo-admin && npx vue-tsc --noEmit` exits 0
    - ProposalReviewPage.vue imports useProposalsApi and renders a filterable q-table with inline-edit slots + accept/reject + accept-group
    - routes.ts registers the proposals route under AdminLayout; AdminLayout.vue has a /proposals nav link
    - No console.* and no `any` in the new/modified frontend code
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit && grep -c "useProposalsApi" src/pages/ProposalReviewPage.vue && grep -c "ProposalReviewPage" src/router/routes.ts && grep -c "/proposals" src/layouts/AdminLayout.vue</automated>
  </verify>
  <done>Profe review page is reachable from the admin nav, lists pending proposals grouped/filterable by route with inline edit + accept/reject/accept-group; typecheck passes; no console.log; no any.</done>
</task>

</tasks>

<verification>
- vue-tsc --noEmit clean for el-templo-admin
- Page reachable via /proposals nav link; composable hits all four endpoints; inline edit + accept/reject/accept-group wired
- No console.log (createLogger only); no any types (CLAUDE.md)
- This is a UI verification candidate — manual UAT (load page, group by route, accept/reject/override) deferred to phase verification
</verification>

<success_criteria>

- Profes have a working filterable, route-grouped review screen with inline override + accept/reject + accept-group
- TREE-03 satisfied end-to-end (API + UI)
  </success_criteria>

<output>
Create `.planning/phases/125-bootstrap-llm-revisi-n-de-profes-de-la-descomposici-n/125-03-SUMMARY.md` when done
</output>
