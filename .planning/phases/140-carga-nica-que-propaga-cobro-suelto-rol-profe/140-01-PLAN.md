---
phase: 140-carga-nica-que-propaga-cobro-suelto-rol-profe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/migrations/0156_idempotency_key.sql
  - el-templo-api/src/db/schema/financial-transactions.ts
  - el-templo-api/src/modules/shared/permissions.ts
  - el-templo-api/src/modules/finance/types.ts
  - el-templo-api/src/modules/finance/transaction-service.ts
  - el-templo-api/src/modules/subscriptions/types.ts
  - el-templo-api/src/modules/subscriptions/service.ts
autonomous: true
requirements: [CARGA-02, CARGA-03, CARGA-04]
must_haves:
  truths:
    - "A coach-initiated renewal charge is born validation_status='pendiente' (recorderRole threaded through renewSubscription)"
    - "Persisting two financial_transactions with the same idempotency_key is rejected by a UNIQUE constraint at the DB level"
    - "transactionService.create persists input.idempotencyKey on the inserted row"
    - "A new FINANCE_LOAD_ROLES exists = FINANCE_WRITE_ROLES + coach, and coach remains absent from FINANCE_VOID/ADJUSTMENT/READ"
  artifacts:
    - path: "el-templo-api/src/db/migrations/0156_idempotency_key.sql"
      provides: "nullable UNIQUE idempotency_key column on financial_transactions"
      contains: "idempotency_key"
    - path: "el-templo-api/src/db/schema/financial-transactions.ts"
      provides: "idempotencyKey column + uniqueIndex"
      contains: "idempotencyKey"
    - path: "el-templo-api/src/modules/shared/permissions.ts"
      provides: "FINANCE_LOAD_ROLES export"
      contains: "FINANCE_LOAD_ROLES"
  key_links:
    - from: "el-templo-api/src/modules/subscriptions/service.ts"
      to: "recordAssignmentCharge recorderRole"
      via: "renewSubscription forwards input.recorderRole"
      pattern: "recorderRole"
    - from: "el-templo-api/src/modules/finance/transaction-service.ts"
      to: "financial_transactions.idempotency_key"
      via: "INSERT persists input.idempotencyKey"
      pattern: "idempotencyKey"
---

<objective>
Backend foundation for the coach PoS load: the schema, permission, and service plumbing every downstream task depends on. Adds the idempotency column (migration 0156 + schema), the `FINANCE_LOAD_ROLES` permission, threads `recorderRole`/`idempotencyKey` through the renewal chain so a coach renewal is born PENDIENTE, and makes `transactionService.create` persist the idempotency key. No HTTP surface and no frontend yet — those are Waves 2 and 3.

Purpose: Phases 137/138 pre-wired role→status and caja resolution; this plan wires the last two missing service-level seams (recorderRole forwarding — Pitfall 1 — and idempotency persistence) plus the permission constant, so Wave 2 endpoints are pure thin handlers.
Output: migration 0156, schema column, FINANCE_LOAD_ROLES, idempotencyKey on CreateTransactionInput + persisted in create(), recorderRole+idempotencyKey on RenewSubscriptionInput forwarded into recordAssignmentCharge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140-carga-nica-que-propaga-cobro-suelto-rol-profe/140-CONTEXT.md
@.planning/phases/140-carga-nica-que-propaga-cobro-suelto-rol-profe/140-RESEARCH.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

From el-templo-api/src/modules/shared/permissions.ts:

- `FINANCE_WRITE_ROLES = ["owner","admin","gestion","recepcion"] as const`
- `FINANCE_ADJUSTMENT_ROLES = ["owner","admin","gestion"] as const`
- `FINANCE_VOID_ROLES = ["owner","admin","gestion"] as const`
- `FINANCE_READ_ROLES = ["owner","admin","gestion","recepcion"] as const` (coach excluded — leave untouched)
- `export type AdminRole = (typeof ALL_STAFF_ROLES)[number]`

From el-templo-api/src/modules/finance/transaction-service.ts:

- `async create(input: CreateTransactionInput, recordedBy: number, tx?: TxHandle): Promise<TransactionDetail>` — single INSERT site; wraps work in a `runner` (uses passed `tx` or opens its own `db.transaction`).
- `KINDS_ALLOWED_WITHOUT_LINKS = ["advance_payment","adjustment","cash_transfer","expense"]` — `advance_payment` already accepts empty links.

From el-templo-api/src/modules/finance/types.ts:

- `interface CreateTransactionInput` (~line 57) — already has `validationStatus?: InitialValidationStatus` (~86) and `cashRegisterId?: number | null` (~98), both DERIVED server-side, never from body.

From el-templo-api/src/db/schema/financial-transactions.ts:

- already imports `index` (line 10) from drizzle-orm/mysql-core; `notes: text("notes")` column present; table-extras array holds the existing `index(...)` entries (lines 88-102).

From el-templo-api/src/modules/subscriptions/types.ts:

- `interface RenewSubscriptionInput { paymentMethod; amountReceived?; priceOverrideAmount?; priceOverrideReason? }` (~260)

From el-templo-api/src/modules/subscriptions/service.ts:

- `import type { AdminRole } from "../shared/permissions"` (line 59 — already imported)
- `private async recordAssignmentCharge(tx, params)` (~249) — `params.recorderRole?: AdminRole` already exists; `validationStatus = params.recorderRole === "coach" ? "pendiente" : "validado"` (~281). It calls `transactionService.create(input, adminId, tx)`.
- `renewSubscription(userId, input: RenewSubscriptionInput, adminId)` (~3189) — calls `recordAssignmentCharge(tx, {...flow:'renew'})` at ~3462 WITHOUT passing `recorderRole`.
- `assignPlan(...)` charge call at ~1211 / ~2798 / ~3141 (other recordAssignmentCharge call sites) — leave admin behavior unchanged (do not add recorderRole there).
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0156 + schema column for idempotency_key</name>
  <read_first>
    - el-templo-api/src/db/schema/financial-transactions.ts (column list + table-extras index array + drizzle-orm/mysql-core imports)
    - el-templo-api/src/db/migrations/0155_movement_expense_kinds.sql (hand-written header style precedent)
    - .planning/phases/140-carga-nica-que-propaga-cobro-suelto-rol-profe/140-RESEARCH.md (Migration 0156 + schema delta code examples; Pitfall 5)
  </read_first>
  <action>
    Hand-write `el-templo-api/src/db/migrations/0156_idempotency_key.sql` (do NOT run `pnpm db:generate` — it hangs on the pre-existing `sessions.goal_plan_type` drift, same reason 0153/0154/0155 were hand-written). Two statements: `ALTER TABLE financial_transactions ADD COLUMN idempotency_key varchar(64) NULL AFTER notes;` then `ALTER TABLE financial_transactions ADD UNIQUE INDEX uq_financial_tx_idempotency_key (idempotency_key);`. Header comments explain it is additive/nullable and that MySQL permits unlimited NULLs in a UNIQUE index so historical/admin rows never collide. NEVER use `;` inside any `--` comment line (the runner splits on `;` before stripping comments). In `financial-transactions.ts`: add `idempotencyKey: varchar("idempotency_key", { length: 64 })` after the `notes` column, import `varchar` and `uniqueIndex` from `drizzle-orm/mysql-core` if not already imported, and add `uniqueIndex("uq_financial_tx_idempotency_key").on(table.idempotencyKey)` to the table-extras array. Apply with `pnpm db:migrate` and commit the SQL file.
  </action>
  <acceptance_criteria>
    - `el-templo-api/src/db/migrations/0156_idempotency_key.sql` exists with the column + unique index ALTERs.
    - No `;` appears inside any SQL comment line: `! grep -nE '^\s*--.*;' el-templo-api/src/db/migrations/0156_idempotency_key.sql` (exit 0 = clean).
    - `financial-transactions.ts` exports `idempotencyKey` column and a `uq_financial_tx_idempotency_key` uniqueIndex; `cd el-templo-api && npx tsc --noEmit` passes.
    - `pnpm db:migrate` applies 0156 (row recorded in `_migrations`); a second INSERT into `financial_transactions` with a duplicate non-null `idempotency_key` raises a duplicate-key error.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit && ! grep -nE '^\s*--.*;' src/db/migrations/0156_idempotency_key.sql</automated>
  </verify>
  <done>0156 migration applied + committed; schema has nullable UNIQUE idempotencyKey column; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: FINANCE_LOAD_ROLES + idempotencyKey on CreateTransactionInput + persist in create()</name>
  <read_first>
    - el-templo-api/src/modules/shared/permissions.ts (FINANCE_WRITE_ROLES + the other FINANCE_* sets)
    - el-templo-api/src/modules/finance/types.ts (CreateTransactionInput ~57, validationStatus ~86, cashRegisterId ~98)
    - el-templo-api/src/modules/finance/transaction-service.ts (create() ~127, the INSERT into financial_transactions inside the runner)
    - .planning/phases/140-carga-nica-que-propaga-cobro-suelto-rol-profe/140-RESEARCH.md (FINANCE_LOAD_ROLES + idempotency code examples; Pitfall 3 atomicity note)
  </read_first>
  <action>
    In `shared/permissions.ts` add `export const FINANCE_LOAD_ROLES = [...FINANCE_WRITE_ROLES, "coach"] as const;` with a docstring stating it gates ONLY the coach load endpoints (carga → PENDIENTE) per D-06, and that coach stays out of FINANCE_VOID/ADJUSTMENT/READ. Do NOT modify FINANCE_VOID_ROLES, FINANCE_ADJUSTMENT_ROLES, or FINANCE_READ_ROLES. In `finance/types.ts` add `idempotencyKey?: string | null` to `CreateTransactionInput` (document it as a client-generated opaque ticket key, NOT secret). In `transaction-service.ts create()`, persist `input.idempotencyKey ?? null` on the INSERT into `financial_transactions` (map to the new `idempotencyKey` schema column). Do NOT add the ER_DUP_ENTRY catch here — endpoint-level dedup lands in Wave 2 (plan 02) because the renewal tx must roll back fully before re-reading on a fresh connection (Pitfall 3). This task only writes the key.
  </action>
  <acceptance_criteria>
    - `grep -c "FINANCE_LOAD_ROLES" el-templo-api/src/modules/shared/permissions.ts` >= 1; the array literal includes `"coach"` and spreads `FINANCE_WRITE_ROLES`.
    - FINANCE_VOID_ROLES / FINANCE_ADJUSTMENT_ROLES / FINANCE_READ_ROLES unchanged: `! grep -E "FINANCE_(VOID|ADJUSTMENT|READ)_ROLES = \[[^]]*coach" el-templo-api/src/modules/shared/permissions.ts`.
    - `CreateTransactionInput` declares `idempotencyKey?`; create() writes it on the INSERT row.
    - `cd el-templo-api && npx tsc --noEmit` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit && grep -q "FINANCE_LOAD_ROLES" src/modules/shared/permissions.ts</automated>
  </verify>
  <done>FINANCE_LOAD_ROLES exported (coach in load only, not void/adjustment/read); idempotencyKey accepted + persisted in create(); typecheck green.</done>
</task>

<task type="auto">
  <name>Task 3: Thread recorderRole + idempotencyKey through renewSubscription (Pitfall 1)</name>
  <read_first>
    - el-templo-api/src/modules/subscriptions/types.ts (RenewSubscriptionInput ~260)
    - el-templo-api/src/modules/subscriptions/service.ts (renewSubscription ~3189; the recordAssignmentCharge call at ~3462; recordAssignmentCharge def ~249 incl. recorderRole? ~273 and the validationStatus derivation ~281; AdminRole import line 59)
    - .planning/phases/140-carga-nica-que-propaga-cobro-suelto-rol-profe/140-RESEARCH.md (Pitfall 1; renovar-plan architecture diagram)
  </read_first>
  <action>
    Add two optional fields to `RenewSubscriptionInput` in subscriptions/types.ts: `recorderRole?: AdminRole` (import the type from `../shared/permissions`) and `idempotencyKey?: string`. In `renewSubscription`, forward both into the `recordAssignmentCharge(tx, {...})` call at ~3462: add `recorderRole: input.recorderRole` and `idempotencyKey: input.idempotencyKey`. In `recordAssignmentCharge`, accept `idempotencyKey?: string` on its params object and pass it through to `transactionService.create` input (recorderRole is already consumed there for validationStatus). Leave the assignPlan call sites (~1211/~2798/~3141) untouched so admin assign/change keep `recorderRole` undefined → `validado` (backward-compatible). Result: a renewal with `recorderRole:'coach'` produces a charge with `validation_status='pendiente'`; without it, `'validado'` as before.
  </action>
  <acceptance_criteria>
    - `RenewSubscriptionInput` declares `recorderRole?: AdminRole` and `idempotencyKey?: string`.
    - The `renewSubscription` → `recordAssignmentCharge` call forwards both fields: `grep -n "recorderRole: input.recorderRole" el-templo-api/src/modules/subscriptions/service.ts` returns a match in the renew path.
    - `recordAssignmentCharge` passes `idempotencyKey` into its `transactionService.create` input.
    - Behavior: a renewSubscription call with `recorderRole:'coach'` yields a financial_transaction with `validation_status='pendiente'`; with no recorderRole, `'validado'` (admin path unchanged).
    - `cd el-templo-api && npx tsc --noEmit` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit && grep -q "recorderRole: input.recorderRole" src/modules/subscriptions/service.ts</automated>
  </verify>
  <done>renewSubscription forwards recorderRole+idempotencyKey; coach renewal born PENDIENTE; admin path unchanged; typecheck green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                                                    |
| ------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| client → API (future coach endpoints) | Untrusted body; validation_status / cashRegisterId / recorderRole MUST be server-derived, never read from body |
| service → DB                          | financial_transactions INSERT; idempotency UNIQUE constraint enforces dedup at the storage layer               |

## STRIDE Threat Register

| Threat ID | Category               | Component                              | Disposition | Mitigation Plan                                                                                                                                                          |
| --------- | ---------------------- | -------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-140-01  | Tampering              | renewSubscription recorderRole         | mitigate    | recorderRole derives validation_status server-side (137); this plan only forwards it from the service caller — Wave 2 routes set it from `request.user.role`, never body |
| T-140-02  | Tampering              | financial_transactions idempotency_key | mitigate    | nullable UNIQUE index makes duplicate-key dedup atomic at the DB; key is an opaque client UUID, not a secret                                                             |
| T-140-03  | Elevation of Privilege | FINANCE_LOAD_ROLES                     | mitigate    | new constant adds coach to LOAD only; void/adjustment/read sets left unchanged (asserted in Task 2 acceptance)                                                           |
| T-140-SC  | Tampering              | npm/pip/cargo installs                 | accept      | No packages installed this phase (RESEARCH Package Legitimacy Audit: zero new deps)                                                                                      |

</threat_model>

<verification>
- `cd el-templo-api && npx tsc --noEmit` passes after all three tasks.
- Migration 0156 applied to local + committed; no `;` in SQL comments.
- FINANCE_LOAD_ROLES exists; coach absent from VOID/ADJUSTMENT/READ.
- renewSubscription forwards recorderRole → coach renewal born PENDIENTE (proven end-to-end by the Wave 2 integration test).
</verification>

<success_criteria>

- idempotency_key nullable UNIQUE column exists (migration + schema).
- FINANCE_LOAD_ROLES = FINANCE_WRITE_ROLES + coach; no widening of void/adjustment/read.
- CreateTransactionInput accepts + create() persists idempotencyKey.
- RenewSubscriptionInput carries recorderRole + idempotencyKey, forwarded into recordAssignmentCharge (Pitfall 1 fixed).
  </success_criteria>

<output>
Create `.planning/phases/140-carga-nica-que-propaga-cobro-suelto-rol-profe/140-01-SUMMARY.md` when done.
</output>
