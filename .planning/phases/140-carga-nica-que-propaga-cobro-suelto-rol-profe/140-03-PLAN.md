---
phase: 140-carga-nica-que-propaga-cobro-suelto-rol-profe
plan: 03
type: execute
wave: 3
depends_on: ["140-02"]
files_modified:
  - el-templo-admin/src/composables/useFinanceLoadApi.ts
  - el-templo-admin/src/pages/CargarPagoPage.vue
  - el-templo-admin/src/router/routes.ts
  - el-templo-admin/src/layouts/AdminLayout.vue
autonomous: false
requirements: [CARGA-01, CARGA-03]
must_haves:
  truths:
    - "A coach opens /cargar on their phone and sees a mobile PoS screen with two modes (Renovar plan / Cobro suelto)"
    - "Typeahead finds a socio; picking one in Renovar mode pre-fills plan vigente + monto (editable)"
    - "Confirmar sends one client-generated idempotency key per attempt; the disabled-button guard plus the key prevent double charges"
    - "Cobro suelto mode collects monto + concepto libre and posts a non-plan charge"
    - "Mis cargas de hoy lists the coach's own pending tickets"
    - "The Cargar pago nav item shows only for load-roles; the route allowedRoles includes coach"
  artifacts:
    - path: "el-templo-admin/src/pages/CargarPagoPage.vue"
      provides: "coach PoS Cargar pago screen (two modes, sticky Confirmar, mis cargas)"
      min_lines: 120
      contains: "Cargar pago"
    - path: "el-templo-admin/src/composables/useFinanceLoadApi.ts"
      provides: "renewLoad / miscCharge / getAutocompletar / listMyLoads + cleanup()"
      contains: "cleanup"
  key_links:
    - from: "el-templo-admin/src/composables/useFinanceLoadApi.ts"
      to: "/api/admin/finance/coach-load"
      via: "api.post / api.get over boot/axios"
      pattern: "coach-load"
    - from: "el-templo-admin/src/router/routes.ts"
      to: "CargarPagoPage.vue"
      via: "route /cargar with meta.allowedRoles incl coach"
      pattern: "cargar"
    - from: "el-templo-admin/src/layouts/AdminLayout.vue"
      to: "/cargar"
      via: "nav q-item gated by isLoadRole"
      pattern: "isLoadRole"
---

<objective>
The coach-facing PoS screen. A new `/cargar` route → `CargarPagoPage.vue` (mobile-web, big buttons, warm palette, pending=gold) honoring 140-UI-SPEC: two modes (Renovar plan autocompletar / Cobro suelto), socio typeahead, server-resolved caja (never shown), client-generated idempotency key per Confirmar with a disabled-button double-submit guard, and a "Mis cargas de hoy" ticket list. Backed by a new `useFinanceLoadApi` composable over `boot/axios` exposing `cleanup()`. Nav entry gated by `isLoadRole`.

Purpose: First real UI of the milestone — the profe carga una vez y la admin deja de re-tipear. This plan consumes the Wave 2 endpoints; no backend changes here.
Output: useFinanceLoadApi composable, CargarPagoPage.vue, route, nav item. This plan is NON-autonomous: Task 3 is a human-verify checkpoint (mobile PoS visual/functional check; Franco was away during the UI-SPEC).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140-carga-nica-que-propaga-cobro-suelto-rol-profe/140-CONTEXT.md
@.planning/phases/140-carga-nica-que-propaga-cobro-suelto-rol-profe/140-RESEARCH.md
@.planning/phases/140-carga-nica-que-propaga-cobro-suelto-rol-profe/140-UI-SPEC.md
@.planning/phases/140-carga-nica-que-propaga-cobro-suelto-rol-profe/140-02-SUMMARY.md

<interfaces>
<!-- Wave 2 endpoints (plan 02) + existing admin patterns. -->

Wave 2 endpoints (prefix /api/admin/finance/coach-load):

- `GET /autocompletar/:userId` → `{ planName, amount, currency, hasRenewable }`
- `POST /renew` body `{ userId, amountReceived?, paymentMethod, idempotencyKey }` → 201 (200 on idempotent replay)
- `POST /misc` body `{ memberId, amount, concepto, paymentMethod, currency, idempotencyKey }` → 201 (200 on replay)
- `GET /mis-cargas` → coach's own recent loads (forced recordedBy=self server-side)

From el-templo-admin/src/composables/useTransactionsApi.ts (mirror this shape):

- `export function useTransactionsApi()` → `{ loading, error, ...methods }` over `api` from `src/boot/axios`; uses `extractError(err, 'msg')` from `src/utils/extract-error`. Composables expose `cleanup()`, NO `onUnmounted` inside (CLAUDE.md).

From el-templo-admin/src/composables/useMembersApi.ts:

- `searchMembers(search: string, limit = 10): Promise<MemberSearchResult[]>` — reuse for the socio typeahead (coach already authorized).

From el-templo-admin/src/layouts/AdminLayout.vue (script):

- `const userRole = computed(() => authStore.user?.role ?? '')` (~215)
- existing role computeds: `isCoachRole` (~218), `isAdminRole` (~221), `isCajaRole = ['gestion','admin','owner']` (~224), `isOwnerRole` (~233). Add `isLoadRole = computed(() => ['coach','gestion','admin','owner'].includes(userRole.value))` mirroring backend FINANCE_LOAD_ROLES. Nav "Gestion" section header is at ~53 (renders for all roles).

From el-templo-admin/src/router/routes.ts:

- AdminLayout children use `meta: { allowedRoles: [...] as AdminRole[] }` (e.g. line 49: `['coach','admin','owner','gestion','recepcion']`). Router `beforeEach` reads `to.meta.allowedRoles`.

UI-SPEC contract (140-UI-SPEC.md) — authoritative for visuals; A1-A9 assumptions are overridable:

- Route `/cargar`; component `CargarPagoPage.vue`; mode toggle = `q-btn-toggle` (spread, toggle-color primary); typeahead = `q-select use-input @filter` (debounce 300, copy `SlotAttendancePanel.vue` pattern); payment buttons = 3× `q-btn size=lg full-width`, selected=solid `$primary`, unselected=outline; sticky full-width Confirmar (≥56px) showing the amount; pending badge = `q-badge color="warning"` "Pendiente" (gold, never red); caja never shown; Spanish copy table; idempotency key = `crypto.randomUUID()` once per confirmation attempt, regenerated only after acknowledged success; disabled-button guard during submit; duplicate-submit no-op de-dupes the list prepend by ticket id.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: useFinanceLoadApi composable</name>
  <read_first>
    - el-templo-admin/src/composables/useTransactionsApi.ts (loading/error refs, api over boot/axios, extractError, cleanup)
    - el-templo-admin/src/composables/useMembersApi.ts (searchMembers signature)
    - el-templo-admin/src/utils/extract-error.ts
    - .planning/phases/140-carga-nica-que-propaga-cobro-suelto-rol-profe/140-RESEARCH.md (frontend composable code example)
    - .planning/phases/140-carga-nica-que-propaga-cobro-suelto-rol-profe/140-UI-SPEC.md (Component Inventory → composable: 3 capabilities)
  </read_first>
  <action>
    Create `el-templo-admin/src/composables/useFinanceLoadApi.ts` mirroring `useTransactionsApi`: `loading`/`error` refs, `api` from `src/boot/axios`, `extractError` for messages, and a `cleanup()` that resets loading/error (NO `onUnmounted` inside — CLAUDE.md). Expose: `getAutocompletar(userId)` → `api.get('/admin/finance/coach-load/autocompletar/' + userId)`; `renewLoad(body)` → `api.post('/admin/finance/coach-load/renew', body)`; `miscCharge(body)` → `api.post('/admin/finance/coach-load/misc', body)`; `listMyLoads()` → `api.get('/admin/finance/coach-load/mis-cargas')`. Define TS interfaces for the request bodies (CoachRenewLoadInput, CoachMiscChargeInput) and the autocompletar response — no `any`. The composable does NOT generate the idempotency key (the page owns the key lifecycle); it only forwards the `idempotencyKey` field passed in each body. Use `createLogger()` only if logging is needed; never console.
  </action>
  <acceptance_criteria>
    - File exists; exports `useFinanceLoadApi` returning `{ loading, error, getAutocompletar, renewLoad, miscCharge, listMyLoads, cleanup }`.
    - No `onUnmounted` inside the composable: `! grep -q "onUnmounted" el-templo-admin/src/composables/useFinanceLoadApi.ts`.
    - No `any` types; bodies typed.
    - `cd el-templo-admin && npx tsc --noEmit` (or `npx vue-tsc --noEmit` if that is the project's typecheck) passes.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit 2>/dev/null || npx tsc --noEmit</automated>
  </verify>
  <done>useFinanceLoadApi composable with cleanup(), typed bodies, four methods over coach-load endpoints; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: CargarPagoPage.vue + route + nav (honor UI-SPEC)</name>
  <read_first>
    - .planning/phases/140-carga-nica-que-propaga-cobro-suelto-rol-profe/140-UI-SPEC.md (FULL — Layout, Component Inventory, Interaction Flows, States, Copywriting, Idempotency lifecycle, Route Placement, Nav entry)
    - el-templo-admin/src/components/SlotAttendancePanel.vue (~106-138 typeahead q-select use-input @filter pattern to copy)
    - el-templo-admin/src/pages/CajaPage.vue (warm palette finance UI reference, methodColor/methodLabel ~259-266, skeleton ~35-37 — DO NOT modify CajaPage)
    - el-templo-admin/src/composables/useFinanceLoadApi.ts (Task 1) + useMembersApi.searchMembers
    - el-templo-admin/src/layouts/AdminLayout.vue (role computeds ~215-233, Gestion header ~53)
    - el-templo-admin/src/router/routes.ts (AdminLayout children, meta.allowedRoles pattern)
  </read_first>
  <action>
    Create `el-templo-admin/src/pages/CargarPagoPage.vue`: single-column `q-page q-pa-md`, `max-width:480px; margin:0 auto`. Title `text-h5` "Cargar pago". A `q-btn-toggle` (spread, toggle-color="primary", full-width) for the two modes — "Renovar plan" (default) and "Cobro suelto". Bordered `q-card` form: socio typeahead (`q-select use-input @filter` debounce 300 calling `searchMembers`, option slot shows name + status); MODE A renders read-only Plan vigente + editable Monto (populated via `getAutocompletar` on pick, with `q-skeleton` while loading and the "no plan vigente → usá Cobro suelto" inline warning when `hasRenewable=false`); MODE B renders Monto + required Concepto (`q-input type=textarea autogrow`). Three big payment-method `q-btn` (size=lg full-width): Efectivo/Transferencia/Tarjeta = cash/transfer/card, selected=solid `$primary`, others outline. `text-caption` hint "Queda pendiente de validación." Sticky full-width Confirmar `q-btn color=primary size=lg` showing the amount; enabled only per the UI-SPEC per-mode rules. "Mis cargas de hoy" `q-list` of ticket items (time, member, amount, concept/plan, method badge keeping existing color semantics, gold "Pendiente" badge) loaded via `listMyLoads`, with empty + skeleton states.
    Idempotency: generate ONE `crypto.randomUUID()` when a confirmation attempt begins; reuse it on retries of the same attempt; regenerate only after an acknowledged success (form reset). On submit set Confirmar `:loading` + `:disable` (double-submit guard). On success `$q.notify({type:'positive'})`, reset form, prepend the new ticket (de-dupe by ticket id so an idempotent no-op does not add a second row). On error `$q.notify({type:'negative'})` and RE-ENABLE Confirmar (retry reuses the same key). All copy in Spanish per the UI-SPEC table. Use `createLogger()` not console.
    Wiring: add the route to `router/routes.ts` under AdminLayout children — `{ path: 'cargar', component: () => import('pages/CargarPagoPage.vue'), meta: { allowedRoles: ['coach','gestion','admin','owner'] as AdminRole[] } }`. In `AdminLayout.vue` add `const isLoadRole = computed(() => ['coach','gestion','admin','owner'].includes(userRole.value))` (mirror existing computeds) and a `q-item v-if="isLoadRole" to="/cargar"` with icon `point_of_sale` and label "Cargar pago" under the Gestion section. Do NOT grant /caja (saldos) to coach.
  </action>
  <acceptance_criteria>
    - `CargarPagoPage.vue` exists (≥120 lines), renders the two-mode PoS layout per UI-SPEC; caja is never rendered/picked.
    - Idempotency key generated once per confirmation attempt via `crypto.randomUUID()`; Confirmar disabled during submit (`grep -q "crypto.randomUUID" el-templo-admin/src/pages/CargarPagoPage.vue`).
    - Route `/cargar` present with `allowedRoles` including `'coach'`; `grep -q "cargar" el-templo-admin/src/router/routes.ts`.
    - `AdminLayout.vue` has `isLoadRole` computed and a `to="/cargar"` nav item gated by it; `grep -q "isLoadRole" el-templo-admin/src/layouts/AdminLayout.vue`.
    - No `any`; no console.* (uses createLogger if needed); Spanish copy matches the UI-SPEC table.
    - Typecheck + lint pass: `cd el-templo-admin && npx vue-tsc --noEmit` (or `npx tsc --noEmit`).
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-admin && (npx vue-tsc --noEmit 2>/dev/null || npx tsc --noEmit) && grep -q "crypto.randomUUID" src/pages/CargarPagoPage.vue && grep -q "isLoadRole" src/layouts/AdminLayout.vue</automated>
  </verify>
  <done>CargarPagoPage.vue honors UI-SPEC (two modes, sticky Confirmar, idempotency guard, mis cargas); route + nav gated by load-role; typecheck green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The coach PoS "Cargar pago" screen at /cargar (two modes, socio typeahead, server-resolved caja, idempotent Confirmar, mis cargas list) wired to the Wave 2 endpoints. Franco was away during the UI-SPEC, so the visuals/flow need a real-device sanity check.</what-built>
  <how-to-verify>
    1. Run the admin dev server (`cd el-templo-admin && pnpm dev` or the project's run command) and log in as a `coach` user.
    2. Open `/cargar` on a phone (or a narrow ~375px browser window). Confirm: big buttons, warm palette (no blue), single column, sticky Confirmar reachable one-handed.
    3. Renovar plan: search a socio with an active plan → plan vigente + monto pre-fill; edit monto; pick Efectivo; Confirmar. Expect a positive notify "pendiente de validación" and a new gold "Pendiente" ticket in "Mis cargas de hoy".
    4. Double-tap Confirmar / retry on a slow connection → still exactly ONE ticket (idempotency).
    5. Cobro suelto: switch mode, pick a socio, enter monto + concepto libre, pick Transferencia, Confirmar → ticket shows the concepto, no plan touched.
    6. Confirm a coach does NOT see /caja (saldos) in the nav and cannot reach it.
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual/flow issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                                |
| ------------------------------------ | ------------------------------------------------------------------------------------------ |
| coach browser → coach-load endpoints | Client guard (allowedRoles) is UX convenience only; server is the source of truth (Wave 2) |

## STRIDE Threat Register

| Threat ID | Category               | Component                         | Disposition | Mitigation Plan                                                                                                 |
| --------- | ---------------------- | --------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------- |
| T-140-09  | Elevation of Privilege | client route guard bypass         | mitigate    | allowedRoles guard is UX only; backend FINANCE_LOAD_ROLES + plugin 403 enforce real access (Wave 2 test-proven) |
| T-140-10  | Tampering              | client sets validationStatus/caja | mitigate    | page never sends validationStatus/cashRegisterId; server derives both (137/138)                                 |
| T-140-11  | Tampering              | double-tap duplicate charge       | mitigate    | client idempotency key per attempt + disabled-button guard; backend UNIQUE dedup is the backstop                |
| T-140-SC  | Tampering              | npm/pip/cargo installs            | accept      | No packages installed (RESEARCH audit: zero new deps; reuses Quasar + boot/axios)                               |

</threat_model>

<verification>
- `cd el-templo-admin && npx vue-tsc --noEmit` (or tsc) passes.
- /cargar route + nav gated by isLoadRole; coach sees Cargar pago, not /caja.
- Idempotency key generated client-side per attempt; Confirmar disabled on submit.
- Human-verify checkpoint approved (mobile PoS visual + the 6 flow steps).
</verification>

<success_criteria>

- Coach PoS screen live at /cargar honoring 140-UI-SPEC (two modes, sticky Confirmar, mis cargas, pending=gold).
- useFinanceLoadApi composable with cleanup() over the coach-load endpoints.
- Idempotent double-submit guard tied to a per-attempt client key.
- Route + nav restricted to load-roles; saldos remain out of coach reach.
  </success_criteria>

<output>
Create `.planning/phases/140-carga-nica-que-propaga-cobro-suelto-rol-profe/140-03-SUMMARY.md` when done.
</output>
