---
phase: 33-locations-community-ecosystem-faq
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-web/data/sedes.ts
  - el-templo-web/components/SectionLocations.vue
  - el-templo-web/data/ecosystem.ts
  - el-templo-web/components/SectionEcosystem.vue
autonomous: true
requirements:
  - SED-01
  - SED-02
  - SED-03
  - SED-04
  - SED-05
  - ECO-01
  - ECO-02
  - ECO-03
  - ECO-04

must_haves:
  truths:
    - 8 sede cards display with name, address, badge (if applicable), and two micro-CTAs (Como llegar, Reservar sesion)
    - Sedes grouped by city with subtitle headers showing Mar del Plata (7) and Barcelona (1)
    - Desktop shows 4-column grid for MDP, tablet 3-column, mobile horizontal scroll with snap
    - Park badge AL AIRE LIBRE (Olive Stone), Chapadmalal RETIRO (Aged Gold), Barcelona INTERNACIONAL (Azul Noche)
    - International fallback CTA at bottom with Azul Noche button linking to app
    - 4 ecosystem pathway cards in 2x2 grid with colored left-border accents
    - Ecosystem cards link to /app, /academy, /franquicias, /gladius
    - Ecosystem grid responsive 2x2 desktop/tablet to 1-column mobile
  artifacts:
    - el-templo-web/data/sedes.ts
    - el-templo-web/components/SectionLocations.vue
    - el-templo-web/data/ecosystem.ts
    - el-templo-web/components/SectionEcosystem.vue
  key_links:
    - SectionLocations imports sedes data from data/sedes.ts
    - SectionEcosystem imports pathways data from data/ecosystem.ts
    - SectionLocations uses useScrollReveal for entrance animation
    - SectionEcosystem uses useScrollReveal for staggered card entrance
---

<objective>
Build the SectionLocations and SectionEcosystem components with their data files.

Purpose: SectionLocations anchors visitors to physical reality (8 real sedes with addresses and CTAs). SectionEcosystem shows the broader El Templo growth pathways (train, certify, invest, equip). Both are data-driven card sections following established BEM patterns.

Output: 2 Vue components + 2 TypeScript data files, ready for page integration in Plan 03.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/33-locations-community-ecosystem-faq/33-CONTEXT.md
@.docs/brand-landing/spec7-pt1.md
@.docs/brand-landing/spec7-pt2.md
@.docs/brand-landing/spec7-pt3.md
@.docs/brand-landing/spec9-pt1.md
@.docs/brand-landing/spec9-pt2.md
@.docs/brand-landing/spec9-pt3.md
@el-templo-web/components/SectionConversion.vue (reference pattern for data-driven card section)
@el-templo-web/composables/useScrollReveal.ts (reuse for scroll-triggered entrance)
@el-templo-web/components/PlaceholderBox.vue (reuse for sede photo placeholders)

<interfaces>
<!-- Existing composable used by both new sections -->
From el-templo-web/composables/useScrollReveal.ts:
```typescript
interface UseScrollRevealOptions {
  threshold?: number;
  rootMargin?: string;
  once?: boolean;
}
interface UseScrollRevealReturn {
  revealed: Ref<boolean>;
  elementRef: Ref<HTMLElement | null>;
  cleanup: () => void;
}
export function useScrollReveal(options?: UseScrollRevealOptions): UseScrollRevealReturn;
```

<!-- Existing pattern from SectionConversion.vue -->

From el-templo-web/components/SectionConversion.vue:

- Data defined as typed array in `<script setup>`
- useScrollReveal for staggered entrance
- BEM scoped CSS with tokens.css variables
- HTML entities for accented characters
- `onBeforeUnmount(() => { cleanup(); })` pattern
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Sede data file + SectionLocations component</name>
  <files>el-templo-web/data/sedes.ts, el-templo-web/components/SectionLocations.vue</files>
  <action>
**Create `el-templo-web/data/sedes.ts`:**

Define and export a typed `Sede` interface and `sedes` array:

```typescript
interface Sede {
  id: string; // e.g. 'constitucion'
  name: string; // e.g. 'Constitucion'
  address: string; // e.g. 'Constitucion 6745'
  city: "Mar del Plata" | "Barcelona";
  mapsUrl: string; // Google Maps URL
  whatsappUrl: string; // WhatsApp deep link
  badge?: {
    text: string; // 'AL AIRE LIBRE' | 'RETIRO' | 'INTERNACIONAL'
    variant: "outdoor" | "special" | "intl";
  };
}
```

Populate with all 8 sedes per spec7:

1. Constitucion - Constitucion 6745, MDP
2. Jujuy - Jujuy 3761, MDP
3. Moreno - Moreno 3751, MDP
4. Alem - Alem 3958, MDP
5. Mario Bravo - Mario Bravo 618, MDP
6. Park - Parque Primavesi, MDP, badge: "AL AIRE LIBRE" / outdoor
7. Chapadmalal - Los Lobos, Chapadmalal, badge: "RETIRO" / special (per CONTEXT.md)
8. Barcelona - Av. Diagonal 368, Barcelona, badge: "INTERNACIONAL" / intl

Use a single shared WhatsApp link for all "Reservar sesion" CTAs per CONTEXT.md decision:
`https://wa.me/5492235820521?text=Hola%21%20Quiero%20reservar%20mi%20primera%20sesi%C3%B3n%20de%20prueba`

Maps URLs: `https://maps.google.com/?q={address}+{city}` for each sede.

Export a `sedesByCity` computed grouping helper or separate the arrays by city in the data file for easy rendering.

**Create `el-templo-web/components/SectionLocations.vue`:**

Follow SectionConversion.vue pattern -- BEM scoped CSS, useScrollReveal, HTML entities for accents.

Structure per spec7:

- Section wrapper: `<section id="sedes" class="locations">` with Marble Cream background
- Header: tag "Sedes", H2 "ENCONTRA TU TEMPLO.", subtitle about 8 sedes
- City group headers: flex row with city name (left) + count (right), border-bottom Warm Stone
- MDP group: grid of 7 sede cards (4-col desktop, 3-col tablet, horizontal scroll mobile)
- Barcelona group: single card, max-width 280px, flex-start
- Each card: PlaceholderBox (3:2 aspect ratio, sede name as label) for photo area + card body (name, address, badge if applicable, separator, CTAs)
- Badges positioned over the image area (absolute, bottom-left): AL AIRE LIBRE (Olive Stone bg), RETIRO (Aged Gold bg), INTERNACIONAL (Azul Noche bg)
- CTAs: "Como llegar" (maps link, new tab) + "Reservar sesion" (WhatsApp link)
- International fallback CTA at bottom: "No estas en Argentina ni en Espana?" + "Proba la app" (Azul Noche button)

CSS per spec7-pt2/pt3 (full specs provided):

- Card: Warm Linen bg, Warm Stone border, 8px radius, shadow-subtle, hover translateY(-2px) + shadow-medium
- MDP grid: `grid-template-columns: repeat(4, 1fr)` desktop, `repeat(3, 1fr)` tablet, flex overflow-x scroll with snap mobile
- Row 2 (3 cards) centering: use natural grid flow (3 cards in 4-col grid left-aligns, which is acceptable per Phase 32 approach pattern)
- Mobile: `min-width: 240px; flex-shrink: 0; scroll-snap-align: start` per card
- Barcelona single card: `max-width: 280px` desktop, `width: 100%` mobile

Use `useScrollReveal` for section entrance with stagger on cards via `transitionDelay` (same pattern as SectionConversion).

Use HTML entities for accented chars: `&aacute;`, `&eacute;`, `&iacute;`, `&oacute;`, `&uacute;`, `&iquest;`.

IMPORTANT: Use PlaceholderBox component for sede images (not `<img>` tags). Per CONTEXT.md: "Placeholder boxes (using existing PlaceholderBox component with sede name overlay) for images until real photos arrive."
</action>
<verify>
<automated>cd /home/franco/projects/el-templo/el-templo-web && npx nuxi typecheck 2>&1 | tail -5</automated>
</verify>
<done>

- data/sedes.ts exports typed Sede interface and sedes array with all 8 locations
- SectionLocations.vue renders 8 sede cards grouped by city with badges, CTAs, responsive grid, and international fallback
- PlaceholderBox used for all sede images
- All CSS uses design tokens (no hardcoded colors, no pure black/white)
- Scroll reveal animation on section entrance
  </done>
  </task>

<task type="auto">
  <name>Task 2: Ecosystem data file + SectionEcosystem component</name>
  <files>el-templo-web/data/ecosystem.ts, el-templo-web/components/SectionEcosystem.vue</files>
  <action>
**Create `el-templo-web/data/ecosystem.ts`:**

Define and export a typed `EcosystemPathway` interface and `pathways` array:

```typescript
interface EcosystemPathway {
  id: string; // 'online' | 'academy' | 'franchise' | 'gladius'
  tag: string; // 'Entrena' | 'Formate' | 'Inverti' | 'Equipate'
  title: string; // Card title
  description: string; // Card description
  ctaText: string; // CTA label
  ctaHref: string; // /app, /academy, /franquicias, /gladius
  variant: string; // CSS modifier class suffix
}
```

Populate with all 4 pathways per spec9 copy (use HTML entities for accents in the component, not in the data file -- data file uses plain unicode):

1. online: "Entrena" / "El metodo, donde estes." / description / "Descarga la app" / /app
2. academy: "Formate" / "Convertite en entrenador certificado." / description / "Conoce Academy" / /academy
3. franchise: "Inverti" / "Se dueno de tu propio Templo." / description / "Explorar franquicias" / /franquicias
4. gladius: "Equipate" / "Las barras que usamos, ahora en tu espacio." / description / "Ver Gladius" / /gladius

**Create `el-templo-web/components/SectionEcosystem.vue`:**

Follow same BEM pattern:

- Section: `<section id="ecosistema" class="ecosystem">` with Warm Stone background
- Header: tag "Ecosistema", H2 "EL TEMPLO CRECE CON VOS.", subtitle with 4 imperative verbs
- Grid: 2x2 desktop/tablet, 1-column mobile
- Each card: Marble Cream bg, Sandy Beige border, 8px radius, 32px 28px padding
- 3px left-border accent per variant: Azul Noche (online), Terracotta (academy), Aged Gold (franchise + gladius)
- Card content: tag/verb (color matches variant), title, description, ghost CTA with arrow
- Hover: translateY(-2px), shadow-medium, full border takes variant color
- CTA: inline-flex with arrow span, hover arrow translateX(4px)
- max-width: 1000px (slightly narrower than other sections -- spec says "compact bridge section")

CSS per spec9-pt2/pt3 (full specs provided):

- Section padding: 80px desktop, 64px mobile
- Title: 32px / 26px / 22px across breakpoints (slightly smaller than main sections per spec)
- Subtitle: 18px / 16px, max-width 600px
- Card tag: 12px uppercase with variant color
- Card title: 18px / 16px mobile, normal case (NOT uppercase)
- Card desc: 14px, line-height 1.55
- CTA: 13px, uppercase, variant color with darkened hover

Use `useScrollReveal` for section entrance with stagger on cards (100ms between each per spec9).
</action>
<verify>
<automated>cd /home/franco/projects/el-templo/el-templo-web && npx nuxi typecheck 2>&1 | tail -5</automated>
</verify>
<done>

- data/ecosystem.ts exports typed EcosystemPathway interface and 4 pathways array
- SectionEcosystem.vue renders 4 pathway cards in 2x2 grid with colored left-border accents
- Each card has tag, title, description, and ghost CTA linking to respective page
- Hover changes full border to variant color with elevation
- Responsive: 2x2 to 1-column at mobile breakpoint
- All CSS uses design tokens
  </done>
  </task>

</tasks>

<verification>
1. `cd el-templo-web && npx nuxi typecheck` passes with no errors
2. `data/sedes.ts` exports 8 sedes with correct addresses, Maps URLs, WhatsApp links
3. `data/ecosystem.ts` exports 4 pathways with correct copy, links, variants
4. SectionLocations renders cards grouped by city with badge variants
5. SectionEcosystem renders 2x2 grid with colored border accents
6. Both components use useScrollReveal for entrance animation
7. Both components use BEM scoped CSS with design token variables only
</verification>

<success_criteria>

- 8 sede cards with real location data, Maps/WhatsApp CTAs, and special badges render correctly
- 4 ecosystem pathway cards with colored left-border accents and ghost CTAs render correctly
- Both sections are responsive across 3 breakpoints
- No hardcoded colors, no pure black or white in CSS
- TypeScript compiles without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/33-locations-community-ecosystem-faq/33-01-SUMMARY.md`
</output>
