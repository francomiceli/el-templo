---
phase: 104
plan: 06
type: execute
wave: 3
depends_on: [104-05]
files_modified:
  - el-templo-app/src/pages/ReservasPage.vue
autonomous: false
requirements: [R11]

must_haves:
  truths:
    - "ReservasPage gates on hasPresencialPlan (NOT on branchIsVirtual or hasActiveSubscription)"
    - "Users without subscription → blocked with 'Activá Tu Plan' state"
    - "Users with presencial plan → can access reservation grid"
    - "Users with online_regular/online_goal/online_coach plan → blocked"
    - "Users with the 'Todos los Programas' bundle → blocked"
  artifacts:
    - path: "el-templo-app/src/pages/ReservasPage.vue"
      provides: "Updated gating logic using hasPresencialPlan from useUserStore"
      contains: "hasPresencialPlan"
  key_links:
    - from: "ReservasPage.vue v-else-if"
      to: "useUserStore.hasPresencialPlan (added in Plan 05)"
      via: "computed import"
      pattern: "hasPresencialPlan"
---

<objective>
Replace the proxy-based gating in `ReservasPage.vue` (which currently uses `!hasActiveSubscription || isOnlineUser` where `isOnlineUser = profile?.branchIsVirtual ?? false`) with the explicit capability `!hasPresencialPlan` exposed by `useUserStore` (added in Plan 05). The new condition correctly blocks users with bundle plans (online_regular planCategory) and online plans, while admitting only members with an active presencial subscription.

Purpose: Implements R11 — closes the gating loophole where a member with an online plan but a non-virtual branch could see the reservation grid.
Output: Single-file edit to `ReservasPage.vue`. Includes a UAT checkpoint for the 4 R11 user profiles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/104-planes-vs-programas-bundle/104-SPEC.md
@.planning/phases/104-planes-vs-programas-bundle/104-05-SUMMARY.md
@el-templo-app/src/pages/ReservasPage.vue
@el-templo-app/src/stores/useUserStore.ts

<interfaces>
<!-- ReservasPage.vue current shape (extracted):
  Line 9:  <div v-else-if="!hasActiveSubscription || isOnlineUser" class="reservas__empty">
  Line 11:   <h2 class="reservas__empty-title">Activá Tu Plan</h2>
  Line 399: const isOnlineUser = computed(() => userStore.profile?.branchIsVirtual ?? false)

Plan 05 added to useUserStore:
hasPresencialPlan: computed(() => {
const sub = subscription.value
if (!sub) return false
if (sub.status !== 'active' && sub.status !== 'paused') return false
return sub.planCategory === 'presencial'
})
-->

The change is one line (the v-else-if condition) plus removing the now-unused `isOnlineUser` computed. The "Activá Tu Plan" copy stays — it remains the right blocked-state for online and no-sub users alike.

Optional copy improvement (recommended but Claude's discretion per SPEC R11): if `hasActiveSubscription && !hasPresencialPlan`, surface a slightly different message like `"Las reservas son para planes presenciales. Tu plan no las incluye."` instead of the generic "Activá Tu Plan". This is more informative for bundle/online users who DO have a plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace gating condition in ReservasPage.vue with !hasPresencialPlan; refine empty-state copy</name>
  <read_first>
    - el-templo-app/src/pages/ReservasPage.vue (full file — confirm there are no other usages of isOnlineUser besides line 399)
    - el-templo-app/src/stores/useUserStore.ts (confirm hasPresencialPlan exists and is exported — it should be after Plan 05)
  </read_first>
  <action>
    1. In `el-templo-app/src/pages/ReservasPage.vue`:

       a. Add a computed to read the new capability (place near the existing `isOnlineUser` definition around line 399):
       ```typescript
       const hasPresencialPlan = computed(() => userStore.hasPresencialPlan)
       const hasActiveButNotPresencial = computed(
         () => userStore.hasActiveSubscription && !userStore.hasPresencialPlan
       )
       ```

       b. Remove the `isOnlineUser` computed at line 399 entirely (no other consumers — confirmed by grep before edit).

       c. Change the v-else-if at line 9 from:
       ```vue
       <div v-else-if="!hasActiveSubscription || isOnlineUser" class="reservas__empty">
       ```
       to:
       ```vue
       <div v-else-if="!hasPresencialPlan" class="reservas__empty">
       ```

       d. Inside the empty-state block, refine the copy to differentiate "no plan at all" from "plan but not presencial":
       ```vue
       <q-icon name="event_available" size="64px" color="grey-5" />
       <h2 class="reservas__empty-title">{{ emptyTitle }}</h2>
       <p class="reservas__empty-text">{{ emptyText }}</p>
       ```
       Add the computeds:
       ```typescript
       const emptyTitle = computed(() =>
         hasActiveButNotPresencial.value ? 'Reservas presenciales' : 'Activá Tu Plan'
       )
       const emptyText = computed(() =>
         hasActiveButNotPresencial.value
           ? 'Las reservas son para planes presenciales. Tu plan actual no las incluye.'
           : 'No tenés un plan activo. Consultá por tu plan para comenzar a entrenar.'
       )
       ```

       e. Verify the WhatsApp button still makes sense in both copies — keep it; it's the right CTA for both "no plan" and "online plan, want presencial" cases.

    2. After the edit, run `grep -n 'isOnlineUser\|branchIsVirtual' el-templo-app/src/pages/ReservasPage.vue` to confirm both are GONE from this file (other files like `MiTemplo.vue`, `MainLayout.vue`, `ReservasPageLegacy.vue` still use them — those are out of scope for R11 per the SPEC).

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-app &amp;&amp; pnpm typecheck 2>&amp;1 | tail -5 &amp;&amp; pnpm lint 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'hasPresencialPlan' el-templo-app/src/pages/ReservasPage.vue` returns ≥ 2 (import + template usage).
    - `grep -c 'isOnlineUser\|branchIsVirtual' el-templo-app/src/pages/ReservasPage.vue` returns 0.
    - The v-else-if condition is exactly `!hasPresencialPlan` (single-source check).
    - `pnpm typecheck` and `pnpm lint` pass.
    - `el-templo-app/src/pages/ReservasPageLegacy.vue` is NOT touched (out of scope).
  </acceptance_criteria>
  <done>ReservasPage gates on the new capability; copy differentiates "no plan" from "wrong plan type".</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT — verify all 4 R11 profiles on local dev build</name>
  <what-built>
    ReservasPage now gates on `hasPresencialPlan` only. The empty state shows differentiated copy depending on whether the user has no subscription vs. has an online/bundle subscription.
  </what-built>
  <how-to-verify>
    Run the member app locally: `cd el-templo-app && pnpm dev`. Walk through the 4 R11 profiles by logging in as different test users (or assign different plans via the admin app to one test user between runs).

    **Profile (i) — No subscription:**
    Log in as user with NO active subscription. Tap "Reservas" tab.
    Expect: "Activá Tu Plan" title, "No tenés un plan activo..." text, WhatsApp button.

    **Profile (ii) — Active presencial plan:**
    Log in as user with active presencial plan (any tier).
    Expect: full reservation grid visible (NOT the empty state). Branch selector and bonus banner present per existing logic.

    **Profile (iii) — Active online plan (online_regular, online_goal, or online_coach):**
    Log in as user with active online plan.
    Expect: empty state visible with title "Reservas presenciales" and the differentiated copy mentioning "Las reservas son para planes presenciales..."

    **Profile (iv) — Active "Todos los Programas" bundle:**
    Log in as user with the bundle plan assigned (assign via admin first).
    Expect: empty state with "Reservas presenciales" title (same as profile iii — bundle is online_regular planCategory).

    **Sanity:**
    - Use browser dev tools → Vue devtools → confirm `userStore.hasPresencialPlan` is `true` only in profile (ii) and `false` in (i), (iii), (iv).
    - Confirm `userStore.subscription` is non-null in (ii), (iii), (iv) and null in (i).

    Report: pass/fail per profile, screenshots of (iii) and (iv) showing the differentiated copy.

  </how-to-verify>
  <resume-signal>Type "approved" if all 4 profiles behave correctly, or describe failures with the profile number and a screenshot.</resume-signal>
</task>

</tasks>

<verification>
1. `cd el-templo-app && pnpm typecheck && pnpm lint` green.
2. UAT checkpoint passed.
</verification>

<success_criteria>

- ReservasPage's gating expression is exactly `!hasPresencialPlan`.
- `isOnlineUser` and `branchIsVirtual` references are gone from this file.
- Differentiated copy improves UX for bundle/online users.
- All 4 R11 profiles behave per the SPEC.
  </success_criteria>

<output>
After completion, create `.planning/phases/104-planes-vs-programas-bundle/104-06-SUMMARY.md` capturing: the chosen copy strings (Spanish), confirmation of UAT pass for all 4 profiles, and a note about the legacy file (ReservasPageLegacy.vue) deliberately not touched.
</output>
