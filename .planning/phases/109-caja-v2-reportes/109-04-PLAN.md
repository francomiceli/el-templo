---
phase: 109-caja-v2-reportes
plan: 04
type: execute
wave: 2
depends_on:
  - 109-02
files_modified:
  - el-templo-admin/src/types/transaction.ts
  - el-templo-admin/src/composables/useTransactionsApi.ts
  - el-templo-admin/src/components/DeudasReport.vue
  - el-templo-admin/src/pages/ReportesPage.vue
autonomous: true
requirements:
  - CAJA-03
  - CAJA-04
requirements_addressed:
  - CAJA-03
  - CAJA-04
tags: [phase-109, finance, frontend, reports, deudas, aging, excel]
must_haves:
  truths:
    - "ReportesPage tiene un 5to tab/sección 'Deudas' al lado de los 4 reportes existentes (accesos / cobros / vencimientos / inactivos)"
    - "El reporte 'Deudas' muestra cards de totales por bucket arriba (Hasta 30 / 31-60 / 61-90 / 90+ días). Owner ve sets de 4 cards × N currencies; non-owner ve un solo set"
    - "Tabla detallada abajo con columnas: Miembro, Plan/Concepto, Sucursal, Monto, Antigüedad (días), Bucket, Moneda. Default sort: antigüedad DESC"
    - "Filtros server-side: sucursal (q-select), moneda (solo owner), búsqueda por nombre (q-input)"
    - "Paginación 'Cargar más' (default page=1, pageSize=50) — match con FinancialHistoryTab pattern de Phase 108"
    - "Excel export: una row por concepto pendiente con 9 columnas: Miembro, Plan/Concepto, Sucursal, Monto, Moneda, Antigüedad (días), Bucket, Fecha devengo, Tipo (subscription/debt_balance). Filename: deudas-<YYYY-MM-DD>.xlsx"
    - "UI 100% español. Internal naming en código: 'aging' / 'outstanding-balances' (D-01 — UI siempre 'Deudas' / 'Antigüedad')"
    - "NUNCA suma currencies distintas en un total. Owner ve cards separadas; non-owner solo ve su currency"
  artifacts:
    - path: "el-templo-admin/src/types/transaction.ts"
      provides: "DebtBucket, OutstandingBalanceRow, BucketTotals, OutstandingBalancesResult, OutstandingBalancesFilters types"
      contains: "OutstandingBalanceRow"
    - path: "el-templo-admin/src/composables/useTransactionsApi.ts"
      provides: "getOutstandingBalances method"
      contains: "getOutstandingBalances"
    - path: "el-templo-admin/src/components/DeudasReport.vue"
      provides: "Encapsulated report section: cards + filters + table + export"
      contains: "DeudasReport"
    - path: "el-templo-admin/src/pages/ReportesPage.vue"
      provides: "5to q-tab 'Deudas' mounting DeudasReport"
      contains: "Deudas"
  key_links:
    - from: "DeudasReport.vue"
      to: "useTransactionsApi.getOutstandingBalances"
      via: "composable call with filters"
      pattern: "getOutstandingBalances"
    - from: "ReportesPage.vue"
      to: "DeudasReport component"
      via: "import + 5to q-tab + q-tab-panel"
      pattern: "DeudasReport"
---

<objective>
Crear el reporte "Deudas" como 5ta sección en `ReportesPage` (D-02, D-03): cards de totales por bucket de antigüedad arriba, tabla detallada con filtros server-side abajo, paginación "Cargar más", y Excel export con 9 columnas. Cubre CAJA-03 y la parte de CAJA-04 correspondiente al export de deudas.

Purpose: Permitir al admin ver y exportar el detalle de saldos pendientes de toda la red, con visión ejecutiva (cards) y operativa (tabla + filtros).

Output: ReportesPage extendido con 5to tab. Componente `DeudasReport.vue` reutilizable que encapsula cards + filtros + tabla + export. UI 100% español, sin "aging" en strings UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/109-caja-v2-reportes/109-CONTEXT.md
@.planning/phases/108-pago-de-saldo-historial-financiero/108-05-SUMMARY.md
@el-templo-admin/src/pages/ReportesPage.vue
@el-templo-admin/src/composables/useTransactionsApi.ts
@el-templo-admin/src/types/transaction.ts
@el-templo-admin/src/types/report.ts
@el-templo-admin/src/components/FinancialHistoryTab.vue
@el-templo-admin/src/utils/format-price.ts
@el-templo-admin/src/utils/format-date.ts

<interfaces>
Source-of-truth contracts the executor MUST use directly.

1. Backend endpoint (defined in Plan 109-02): `GET /api/admin/reports/outstanding-balances`. Response shape (from Plan 109-02 types):

```typescript
type DebtBucket = "0-30" | "31-60" | "61-90" | "90+";

interface OutstandingBalanceRow {
  memberId: number;
  memberName: string;
  branchId: number | null;
  branchName: string | null;
  targetKind: "subscription" | "debt_balance";
  targetId: number;
  conceptLabel: string;
  amount: number;
  currency: string; // 'ARS' | 'EUR'
  effectiveDate: string; // YYYY-MM-DD
  ageInDays: number;
  bucket: DebtBucket;
}

type BucketTotals = Record<DebtBucket, number>;

interface OutstandingBalancesResult {
  rows: OutstandingBalanceRow[];
  total: number;
  page: number;
  limit: number;
  bucketTotals: BucketTotals | Record<string, BucketTotals>;
}

interface OutstandingBalancesFilters {
  branchId?: number;
  currency?: string;
  search?: string;
  page?: number;
  limit?: number;
}
```

2. Bucket display labels (UI español, D-01: NEVER "aging"):

```typescript
const BUCKET_LABELS_ES: Record<DebtBucket, string> = {
  "0-30": "Hasta 30 días",
  "31-60": "31-60 días",
  "61-90": "61-90 días",
  "90+": "90+ días",
};
```

3. Pattern from Phase 108 FinancialHistoryTab — paginación "Cargar más" with PaginatedResult shape `{ rows, total, page, limit }`:

```typescript
const PAGE_SIZE = 50;
const items = ref<OutstandingBalanceRow[]>([]);
const total = ref(0);
const currentPage = ref(1);
const hasMore = computed(() => items.value.length < total.value);
async function loadMore() {
  const next = currentPage.value + 1;
  const res = await api.getOutstandingBalances({
    ...currentFilters(),
    page: next,
    limit: PAGE_SIZE,
  });
  items.value.push(...res.rows);
  currentPage.value = res.page;
  total.value = res.total;
}
```

4. ReportesPage existing tab pattern (verbatim from `el-templo-admin/src/pages/ReportesPage.vue`):

```vue
<q-tab name="accesos" label="Accesos" icon="login" />
<q-tab name="cobros" label="Cobros" icon="payments" />
<q-tab name="vencimientos" label="Vencimientos" icon="event_busy" />
<q-tab name="inactivos" label="Inactivos" icon="person_off" />
<!-- ADD: -->
<q-tab name="deudas" label="Deudas" icon="warning" />
```

5. xlsx + downloadBlob (already used in ReportesPage, lines 856 + 979/1103/1172/1238).

6. Auth scope (already in ReportesPage): use `authStore.user?.role` to decide owner-only currency selector visibility.
   </interfaces>
   </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add types + composable method getOutstandingBalances</name>
  <files>el-templo-admin/src/types/transaction.ts, el-templo-admin/src/composables/useTransactionsApi.ts</files>
  <behavior>
    - Types `DebtBucket`, `OutstandingBalanceRow`, `BucketTotals`, `OutstandingBalancesResult`, `OutstandingBalancesFilters` exported from transaction.ts (mirror backend types from Plan 109-02 verbatim).
    - `useTransactionsApi.getOutstandingBalances(filters)` method added, returns `OutstandingBalancesResult`.
    - Method uses GET against `/admin/reports/outstanding-balances` with query params.
    - All filter values undefined-safe (axios will omit undefined params automatically when passed via `params:` config).
  </behavior>
  <action>
    1. Read `el-templo-admin/src/types/transaction.ts` to find the appropriate location (after the existing Phase 108 outstanding-concepts types).
    2. Add types at the bottom of `transaction.ts`:
       ```typescript
       // -- Phase 109 CAJA-03 — Outstanding balances (Deudas report) ----------
       // Internal naming: aging / outstanding-balances. UI label always "Deudas"
       // (D-01 — never expose "aging" string to the user).

       export type DebtBucket = '0-30' | '31-60' | '61-90' | '90+';

       export interface OutstandingBalanceRow {
         memberId: number;
         memberName: string;
         branchId: number | null;
         branchName: string | null;
         targetKind: 'subscription' | 'debt_balance';
         targetId: number;
         conceptLabel: string;
         amount: number;
         currency: string;
         effectiveDate: string;
         ageInDays: number;
         bucket: DebtBucket;
       }

       export type BucketTotals = Record<DebtBucket, number>;

       export interface OutstandingBalancesResult {
         rows: OutstandingBalanceRow[];
         total: number;
         page: number;
         limit: number;
         // Owner: keyed by currency. Non-owner: flat.
         bucketTotals: BucketTotals | Record<string, BucketTotals>;
       }

       export interface OutstandingBalancesFilters {
         branchId?: number;
         currency?: string;
         search?: string;
         page?: number;
         limit?: number;
       }
       ```
    3. Read `el-templo-admin/src/composables/useTransactionsApi.ts` lines ~60-100 to see the existing pattern for getOutstandingConcepts.
    4. Add a new method to `useTransactionsApi.ts` mirroring the existing pattern:
       ```typescript
       async function getOutstandingBalances(
         filters: OutstandingBalancesFilters = {}
       ): Promise<OutstandingBalancesResult> {
         try {
           const { data } = await api.get<OutstandingBalancesResult>(
             '/admin/reports/outstanding-balances',
             { params: filters }
           );
           return data;
         } catch (err: unknown) {
           const message = err instanceof Error ? err.message : 'unknown error';
           log.error('Failed to load outstanding balances', { error: message });
           throw err;
         }
       }
       ```
    5. Add `getOutstandingBalances` to the returned object of the composable + import the new types.
    6. NO `any` types. NO `console.*`.

  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm exec tsc --noEmit -p tsconfig.json && grep -nc "OutstandingBalanceRow\|DebtBucket\|getOutstandingBalances" src/types/transaction.ts src/composables/useTransactionsApi.ts</automated>
  </verify>
  <done>
    - 5 new types exported from transaction.ts (grep ≥5 matches)
    - `getOutstandingBalances` method added to composable (grep ≥2: definition + return)
    - tsc clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create DeudasReport.vue component (cards + filters + table + load more)</name>
  <files>el-templo-admin/src/components/DeudasReport.vue</files>
  <behavior>
    - Self-contained component. Renders:
      - Filters row at top: branch q-select, currency q-select (only visible if owner), search q-input.
      - Cards row(s): bucket totals. Non-owner = single row of 4 cards (Hasta 30, 31-60, 61-90, 90+) using flat `bucketTotals`. Owner = one row of 4 cards per currency (label "Totales por antigüedad (ARS)", "Totales por antigüedad (EUR)", etc.) using nested `bucketTotals`.
      - Tabla detallada: q-table with columns Miembro, Plan/Concepto, Sucursal, Monto, Antigüedad (días), Bucket, Moneda. Default sort: antigüedad DESC (rows arrive sorted from backend).
      - "Cargar más" button below table when `hasMore`.
    - On mount: fetch page 1.
    - Filter changes: reset to page 1, refetch.
    - Loading state: q-spinner + disable filters/buttons.
    - Empty state: "No hay deudas pendientes" when total === 0.
    - UI 100% español. Internal variable/function names CAN use "aging" / "outstandingBalances" (D-01 internal-only).
  </behavior>
  <action>
    1. Read `el-templo-admin/src/components/FinancialHistoryTab.vue` end-to-end to copy:
       - PaginatedResult handling pattern (`items` ref, `total` ref, `currentPage`, `hasMore` computed)
       - "Cargar más" button + loading guard
       - q-list/q-table layout idioms
    2. Read `el-templo-admin/src/pages/ReportesPage.vue` lines ~186-310 (the "cobros" tab — closest analog: filters + table + export button).
    3. Create `el-templo-admin/src/components/DeudasReport.vue` with this structure:
       ```vue
       <template>
         <div>
           <!-- Filters -->
           <div class="row q-gutter-sm q-mb-md">
             <q-select
               v-model="filters.branchId"
               :options="branchOptions"
               option-value="id"
               option-label="name"
               emit-value
               map-options
               label="Sucursal"
               outlined
               dense
               clearable
               class="col-3"
             />
             <q-select
               v-if="isOwner"
               v-model="filters.currency"
               :options="CURRENCY_OPTIONS"
               label="Moneda"
               outlined
               dense
               clearable
               class="col-2"
             />
             <q-input
               v-model="filters.search"
               label="Buscar miembro"
               outlined
               dense
               clearable
               debounce="400"
               class="col-3"
             />
             <q-space />
             <q-btn
               label="Exportar Excel"
               color="primary"
               icon="file_download"
               @click="onExport"
               :loading="exporting"
             />
           </div>

           <!-- Cards: bucket totals -->
           <template v-if="isOwner && isMultiCurrency">
             <div v-for="(cur, idx) in currencyKeys" :key="cur" :class="idx > 0 ? 'q-mt-md' : ''">
               <div class="text-subtitle1 q-mb-sm">Totales por antigüedad ({{ cur }})</div>
               <div class="row q-gutter-sm">
                 <q-card v-for="bucket in BUCKETS" :key="bucket" flat bordered class="col">
                   <q-card-section>
                     <div class="text-caption text-grey-7">{{ BUCKET_LABELS_ES[bucket] }}</div>
                     <div class="text-h6">
                       {{ formatPrice((bucketTotalsByCurrency[cur] ?? emptyBucketTotals())[bucket], cur) }}
                     </div>
                   </q-card-section>
                 </q-card>
               </div>
             </div>
           </template>
           <template v-else>
             <div class="text-subtitle1 q-mb-sm">Totales por antigüedad</div>
             <div class="row q-gutter-sm q-mb-md">
               <q-card v-for="bucket in BUCKETS" :key="bucket" flat bordered class="col">
                 <q-card-section>
                   <div class="text-caption text-grey-7">{{ BUCKET_LABELS_ES[bucket] }}</div>
                   <div class="text-h6">
                     {{ formatPrice(bucketTotalsFlat[bucket], displayCurrency) }}
                   </div>
                 </q-card-section>
               </q-card>
             </div>
           </template>

           <!-- Table -->
           <q-table
             :rows="items"
             :columns="columns"
             row-key="rowKey"
             :loading="loading"
             flat
             bordered
             :pagination="{ rowsPerPage: 0 }"
             hide-pagination
           >
             <template v-slot:body-cell-monto="props">
               <q-td :props="props">{{ formatPrice(props.row.amount, props.row.currency) }}</q-td>
             </template>
             <template v-slot:body-cell-bucket="props">
               <q-td :props="props">
                 <q-badge :color="bucketColor(props.row.bucket)">
                   {{ BUCKET_LABELS_ES[props.row.bucket as DebtBucket] }}
                 </q-badge>
               </q-td>
             </template>
             <template v-slot:no-data>
               <div class="full-width row q-py-md justify-center text-grey-6">
                 No hay deudas pendientes
               </div>
             </template>
           </q-table>

           <div v-if="hasMore" class="row justify-center q-mt-md">
             <q-btn
               label="Cargar más"
               color="primary"
               flat
               :loading="loading"
               @click="loadMore"
             />
           </div>
         </div>
       </template>

       <script setup lang="ts">
       import { ref, reactive, computed, onMounted, watch } from 'vue';
       import * as XLSX from 'xlsx';
       import { useTransactionsApi } from 'src/composables/useTransactionsApi';
       import { useAuthStore } from 'src/stores/auth';
       import { formatPrice } from 'src/utils/format-price';
       import { formatDate } from 'src/utils/format-date';
       import { createLogger } from 'src/utils/logger';
       import type {
         DebtBucket,
         OutstandingBalanceRow,
         BucketTotals,
         OutstandingBalancesFilters,
       } from 'src/types/transaction';

       interface Props {
         branchOptions: Array<{ id: number; name: string }>;
       }
       const props = defineProps<Props>();

       const log = createLogger('DeudasReport');
       const api = useTransactionsApi();
       const authStore = useAuthStore();

       const BUCKETS: DebtBucket[] = ['0-30', '31-60', '61-90', '90+'];
       const BUCKET_LABELS_ES: Record<DebtBucket, string> = {
         '0-30': 'Hasta 30 días',
         '31-60': '31-60 días',
         '61-90': '61-90 días',
         '90+': '90+ días',
       };
       const CURRENCY_OPTIONS = ['ARS', 'EUR'];
       const PAGE_SIZE = 50;

       const isOwner = computed(() => authStore.user?.role === 'owner');
       const displayCurrency = computed(() => /* derive from country scope, e.g. 'ARS' */ 'ARS');

       const filters = reactive<{
         branchId: number | null;
         currency: string | null;
         search: string;
       }>({ branchId: null, currency: null, search: '' });

       const items = ref<OutstandingBalanceRow[]>([]);
       const total = ref(0);
       const currentPage = ref(1);
       const loading = ref(false);
       const exporting = ref(false);
       const bucketTotalsFlat = ref<BucketTotals>({ '0-30': 0, '31-60': 0, '61-90': 0, '90+': 0 });
       const bucketTotalsByCurrency = ref<Record<string, BucketTotals>>({});
       const isMultiCurrency = computed(() => Object.keys(bucketTotalsByCurrency.value).length > 1);
       const currencyKeys = computed(() => Object.keys(bucketTotalsByCurrency.value).sort());

       const hasMore = computed(() => items.value.length < total.value);

       const columns = [
         { name: 'miembro', label: 'Miembro', field: 'memberName', align: 'left', sortable: false },
         { name: 'concepto', label: 'Plan/Concepto', field: 'conceptLabel', align: 'left', sortable: false },
         { name: 'sucursal', label: 'Sucursal', field: (r: OutstandingBalanceRow) => r.branchName ?? '—', align: 'left', sortable: false },
         { name: 'monto', label: 'Monto', field: 'amount', align: 'right', sortable: false },
         { name: 'antiguedad', label: 'Antigüedad (días)', field: 'ageInDays', align: 'right', sortable: false },
         { name: 'bucket', label: 'Bucket', field: 'bucket', align: 'center', sortable: false },
         { name: 'moneda', label: 'Moneda', field: 'currency', align: 'center', sortable: false },
       ];

       function bucketColor(b: DebtBucket): string {
         return b === '0-30' ? 'positive'
           : b === '31-60' ? 'warning'
           : b === '61-90' ? 'orange'
           : 'negative';
       }
       function emptyBucketTotals(): BucketTotals {
         return { '0-30': 0, '31-60': 0, '61-90': 0, '90+': 0 };
       }
       function rowKey(r: OutstandingBalanceRow): string {
         return `${r.targetKind}:${r.targetId}`;
       }

       function currentFilters(): OutstandingBalancesFilters {
         return {
           branchId: filters.branchId ?? undefined,
           currency: filters.currency ?? undefined,
           search: filters.search.trim() ? filters.search.trim() : undefined,
         };
       }

       async function load(reset = true): Promise<void> {
         loading.value = true;
         try {
           const page = reset ? 1 : currentPage.value + 1;
           const res = await api.getOutstandingBalances({
             ...currentFilters(),
             page,
             limit: PAGE_SIZE,
           });
           if (reset) {
             items.value = res.rows;
           } else {
             items.value.push(...res.rows);
           }
           currentPage.value = res.page;
           total.value = res.total;
           // Discriminate flat vs nested bucketTotals
           const bt = res.bucketTotals;
           const looksFlat = '0-30' in bt;
           if (looksFlat) {
             bucketTotalsFlat.value = bt as BucketTotals;
             bucketTotalsByCurrency.value = {};
           } else {
             bucketTotalsByCurrency.value = bt as Record<string, BucketTotals>;
             bucketTotalsFlat.value = emptyBucketTotals();
           }
         } catch (err: unknown) {
           const message = err instanceof Error ? err.message : 'unknown error';
           log.error('Failed to load Deudas report', { error: message });
         } finally {
           loading.value = false;
         }
       }

       function loadMore(): Promise<void> {
         return load(false);
       }

       // Watch filters to reset and refetch
       watch(
         () => [filters.branchId, filters.currency, filters.search],
         () => { void load(true); }
       );

       onMounted(() => { void load(true); });

       async function onExport(): Promise<void> {
         exporting.value = true;
         try {
           // Fetch full filtered set, no pagination, single high-limit call
           const res = await api.getOutstandingBalances({
             ...currentFilters(),
             page: 1,
             limit: 5000,
           });
           if (res.total > 1000) {
             log.warn('Deudas export > 1000 rows', { total: res.total });
           }
           const rows = res.rows.map((r) => ({
             Miembro: r.memberName,
             'Plan/Concepto': r.conceptLabel,
             Sucursal: r.branchName ?? '',
             Monto: r.amount,
             Moneda: r.currency,
             'Antigüedad (días)': r.ageInDays,
             Bucket: BUCKET_LABELS_ES[r.bucket],
             'Fecha devengo': formatDate(r.effectiveDate),
             Tipo: r.targetKind,
           }));
           const ws = XLSX.utils.json_to_sheet(rows);
           const wb = XLSX.utils.book_new();
           XLSX.utils.book_append_sheet(wb, ws, 'Deudas');
           const blob = new Blob(
             [XLSX.write(wb, { type: 'array', bookType: 'xlsx' })],
             { type: 'application/octet-stream' }
           );
           const today = new Date().toISOString().slice(0, 10);
           downloadBlob(blob, `deudas-${today}.xlsx`);
         } catch (err: unknown) {
           const message = err instanceof Error ? err.message : 'unknown error';
           log.error('Deudas export failed', { error: message });
         } finally {
           exporting.value = false;
         }
       }

       function downloadBlob(blob: Blob, filename: string): void {
         const url = URL.createObjectURL(blob);
         const a = document.createElement('a');
         a.href = url;
         a.download = filename;
         document.body.appendChild(a);
         a.click();
         document.body.removeChild(a);
         URL.revokeObjectURL(url);
       }
       </script>
       ```
    4. The `displayCurrency` computed should derive from the user's country scope (AR → ARS, ES → EUR). Read how ReportesPage or CajaPage derives currency from authStore — mirror that exact pattern. If unclear, default to 'ARS' with a `// TODO: derive from auth scope` comment.
    5. The `branchOptions` prop is supplied by the parent (ReportesPage) — uses the same branch list the other reports use.
    6. NO `any`. NO `console.*`. Logger via createLogger. UI strings 100% español.

  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm exec tsc --noEmit -p tsconfig.json && wc -l src/components/DeudasReport.vue && grep -nc "getOutstandingBalances\|BUCKET_LABELS_ES\|deudas-" src/components/DeudasReport.vue</automated>
  </verify>
  <done>
    - DeudasReport.vue created (≥250 lines)
    - Cards render flat OR nested by currency depending on isOwner + bucketTotals shape
    - Tabla con 7 columnas (Miembro, Plan/Concepto, Sucursal, Monto, Antigüedad, Bucket, Moneda)
    - "Cargar más" button gated by `hasMore` computed
    - Export filename `deudas-${today}.xlsx`
    - 0 matches for `console\.`
    - 0 matches for the literal string "aging" in template/UI strings (internal var names like `aging` ARE allowed; but check via `grep -i "aging" template-section`) — D-01 guard; manual review of template section
    - tsc clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire DeudasReport as 5to tab in ReportesPage</name>
  <files>el-templo-admin/src/pages/ReportesPage.vue</files>
  <behavior>
    - 5to q-tab `name="deudas" label="Deudas"` added next to the existing 4.
    - 5to q-tab-panel mounting `<DeudasReport :branch-options="branches">`.
    - Active tab default behavior preserved (whatever the page does today).
    - No changes to the 4 existing tabs.
  </behavior>
  <action>
    1. Read `el-templo-admin/src/pages/ReportesPage.vue` lines 50-70 (q-tabs declaration) and lines 60-470 (tab panels) to confirm the exact pattern.
    2. Add the new q-tab declaration alongside the existing 4 (line ~58):
       ```vue
       <q-tab name="deudas" label="Deudas" icon="warning" />
       ```
    3. Add the new q-tab-panel after the "inactivos" panel:
       ```vue
       <q-tab-panel name="deudas">
         <DeudasReport :branch-options="branches" />
       </q-tab-panel>
       ```
       (Confirm the exact prop name for branches list — ReportesPage already loads them for filtering existing reports; reuse the existing reactive ref.)
    4. Add import: `import DeudasReport from 'src/components/DeudasReport.vue';`
    5. If there is a `switch (activeTab)` for export button or anything similar (line ~1426-1438), add `case 'deudas':` — but since DeudasReport encapsulates its own export button, the parent switch likely needs no entry (or just `case 'deudas': /* handled inside component */ break;`). Verify.
    6. NO `any`, NO `console.*`, UI 100% español.
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm exec tsc --noEmit -p tsconfig.json && grep -c 'name="deudas"' src/pages/ReportesPage.vue && grep -c "DeudasReport" src/pages/ReportesPage.vue</automated>
  </verify>
  <done>
    - `name="deudas"` appears in q-tab + q-tab-panel (≥2 matches)
    - `DeudasReport` imported + used (≥2 matches)
    - tsc clean
    - Existing 4 reports unchanged (manual diff review — Plan 109-05 smoke verifies)
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                                                                                                    |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| admin browser → admin API | Authenticated session; reuses backend endpoint from Plan 109-02 (CAJA_ROLES guard + attachCountryScope). No new write surface. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                              | Disposition | Mitigation Plan                                                                                                                                                                                                                   |
| --------- | ---------------------- | ---------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-109-09  | Information Disclosure | Currency selector visible to non-owner could leak EUR data to AR scope | mitigate    | Selector gated by `v-if="isOwner"`. Backend also enforces country scope: a non-owner request with `?currency=EUR` would still be filtered to AR-country branches → effectively empty result. Defense-in-depth.                    |
| T-109-10  | DoS                    | Excel export with extreme dataset                                      | mitigate    | `limit: 5000` cap; `log.warn` if >1000; backend also caps via schema (Plan 109-02 set max=200, but export uses higher path; verify or cap export at 5000 + add warn). Acceptable for v1; pivot table requested in deferred ideas. |
| T-109-11  | Tampering              | Untyped bucketTotals shape                                             | mitigate    | Discriminator check (`'0-30' in bt`) before narrowing. If shape unexpected, falls back to flat empty totals (graceful degradation).                                                                                               |

</threat_model>

<verification>
After all 3 tasks:
- `pnpm exec tsc --noEmit` clean (admin)
- `pnpm exec eslint src/components/DeudasReport.vue src/pages/ReportesPage.vue` clean
- `grep -c "DeudasReport" src/pages/ReportesPage.vue` ≥2
- `grep -c 'name="deudas"' src/pages/ReportesPage.vue` ≥2
- D-01 guard: scan template section of DeudasReport.vue for "aging" — must be 0 matches in any UI-rendered string (script-internal var names are OK)
- Manual smoke (Plan 109-05): ReportesPage shows 5 tabs, click Deudas tab, cards render with bucket totals, table renders with rows sorted by ageInDays DESC, filter by branch/currency/search works, "Cargar más" loads next page, Excel export downloads `deudas-YYYY-MM-DD.xlsx` with 9 columns.
</verification>

<success_criteria>

1. ReportesPage muestra 5to tab "Deudas" (D-02, D-03)
2. Cards bucket totals arriba: 4 cards non-owner, sets de 4 × N currencies para owner (D-05, D-06)
3. Tabla detallada con 7 columnas, sort por antigüedad DESC, paginación "Cargar más" (D-05, D-07)
4. Filtros server-side: sucursal, moneda (owner only), search nombre (D-05)
5. Excel export `deudas-<YYYY-MM-DD>.xlsx` con 9 columnas (D-16, D-17)
6. UI 100% español, "aging" no aparece en strings rendered (D-01)
7. NUNCA suma currencies distintas en un total (D-06)
   </success_criteria>

<output>
After completion, create `.planning/phases/109-caja-v2-reportes/109-04-SUMMARY.md` with:
- Files modified + line counts (DeudasReport.vue ≥250 lines)
- Verification grep outputs (especially D-01 guard, D-06 multi-currency)
- Decisions/deviations (especially around displayCurrency derivation)
</output>
