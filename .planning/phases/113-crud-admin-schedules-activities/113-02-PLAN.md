---
phase: 113-crud-admin-schedules-activities
plan: 02
type: execute
wave: 2
depends_on:
  - 113-01
files_modified:
  - el-templo-admin/src/components/scheduling/CreateSlotDialog.vue
  - el-templo-admin/src/components/scheduling/ActivitiesDialog.vue
  - el-templo-admin/src/pages/HorariosPage.vue
  - el-templo-admin/src/types/scheduling.ts
  - el-templo-admin/src/composables/useSchedulingApi.ts
autonomous: true
requirements:
  - D-17
  - D-18
  - D-19
  - D-20
  - D-13
  - D-16
must_haves:
  truths:
    - "Admin ve un boton 'Crear horario' en HorariosPage que abre un modal con sede pre-seleccionada (filtro actual), día, start, end, activity (dropdown solo activas)"
    - "Tras crear con exito, el grid se refresca y aparece el slot nuevo en la celda correspondiente"
    - "Si el backend devuelve 409 por overlap, el admin ve un toast/banner en español accionable mencionando el horario en conflicto (HH:MM-HH:MM)"
    - "HorariosPage tiene tabs 'Horarios' y 'Actividades'; el contenido del grid actual queda en la tab Horarios; tab Actividades muestra la lista CRUD"
    - "El boton 'Actividades' del header se elimina (queda como tab) — el dialog se reusa montado dentro de la tab via slot/embed o se reemplaza por un panel inline equivalente"
    - "Al desactivar una activity con schedules activos referenciando, el admin ve un toast con la lista de slots afectados (`Lun 10:00-11:00 (Constitución)`, etc.) y la activity NO se desactiva"
    - "Al crear una activity con nombre duplicado, el admin ve toast 'Ya existe una actividad activa con ese nombre' (no genérico)"
  artifacts:
    - path: "el-templo-admin/src/components/scheduling/CreateSlotDialog.vue"
      provides: "Modal con form de creacion de slot"
      min_lines: 120
    - path: "el-templo-admin/src/pages/HorariosPage.vue"
      provides: "Tabs Horarios/Actividades + boton Crear horario"
      contains: "q-tabs"
    - path: "el-templo-admin/src/components/scheduling/ActivitiesDialog.vue"
      provides: "Cascade-error UX y toast con nombres específicos"
      contains: "affectedSchedules"
  key_links:
    - from: "CreateSlotDialog"
      to: "useSchedulingApi.createSchedule"
      via: "form submit calls composable"
      pattern: "createSchedule\\("
    - from: "HorariosPage 'Crear horario' button"
      to: "CreateSlotDialog v-model:show"
      via: "props"
      pattern: "v-model:show"
    - from: "ActivitiesDialog cascade error"
      to: "API 409 affectedSchedules payload"
      via: "axios error.response.data.affectedSchedules"
      pattern: "affectedSchedules"
---

<objective>
Frontend admin para Phase 113:
1. Modal de creación de slot (`CreateSlotDialog.vue`) + botón "Crear horario" en `HorariosPage` (D-17).
2. Refactor de `HorariosPage` a layout con tabs "Horarios" / "Actividades" (D-18). La tab Actividades reusa el mismo componente CRUD existente (`ActivitiesDialog.vue`) — para minimizar churn lo embebemos como panel embedded (no como dialog) o lo invocamos desde la tab. Mantener `SlotDetailDialog.vue` y `HolidaysDialog.vue`/`SesionesDePruebaDialog.vue` intactos (D-19).
3. UX accionable de errores: overlap 409 al crear slot, name duplicate 409 al crear/renombrar activity, cascade-block 409 al desactivar activity (D-13/D-16/D-20).

Purpose: Cerrar el loop UX para que el admin no tenga que pedir migraciones SQL (ej: el caso Constitución 10am que disparó esta fase). Hoy `ActivitiesDialog` ya tiene CRUD básico funcional pero no surfacea cascade error; el botón de crear horario directamente no existe.
Output: 1 nuevo componente, 2 archivos modificados, 1 type-defs ampliado, 1 composable extension trivial.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/113-crud-admin-schedules-activities/113-CONTEXT.md
@.planning/phases/113-crud-admin-schedules-activities/113-01-SUMMARY.md
@el-templo-admin/src/pages/HorariosPage.vue
@el-templo-admin/src/components/scheduling/ActivitiesDialog.vue
@el-templo-admin/src/components/scheduling/HolidaysDialog.vue
@el-templo-admin/src/components/scheduling/SlotDetailDialog.vue
@el-templo-admin/src/composables/useSchedulingApi.ts
@el-templo-admin/src/types/scheduling.ts
@el-templo-admin/src/utils/extract-error.ts
@el-templo-admin/src/utils/logger.ts

<interfaces>
<!-- Existing frontend surface this plan extends. -->

From el-templo-admin/src/composables/useSchedulingApi.ts (already exposes):

```typescript
async function createSchedule(data: {
  branchId: number;
  activityId: number;
  dayOfWeek: number;
  startTime: string;
  endTime: string;
}): Promise<ScheduleSlot>;

async function listActivities(): Promise<ActivityRecord[]>;
async function createActivity(data: {
  name;
  description?;
}): Promise<ActivityRecord>;
async function updateActivity(activityId, data): Promise<ActivityRecord>;
```

From el-templo-admin/src/types/scheduling.ts (existing):

```typescript
export type ActivityRecord = { id, name, description, isActive, createdAt, updatedAt };
export type ScheduleSlot = { id, branchId, branchName, activityId, activityName,
                              dayOfWeek, startTime, endTime, isActive, ... };
export const DAY_SHORT_LABELS: Record<DayOfWeek, string>; // 'Lun'..'Sab'
```

From el-templo-admin/src/utils/extract-error.ts:

```typescript
export function extractError(err: unknown, fallback: string): string;
// Used pattern: err.response?.data?.message ?? err.message ?? fallback
```

From el-templo-admin/src/components/scheduling/ActivitiesDialog.vue (current):

- defineProps<{ show: boolean }>(); defineEmits<{ 'update:show' }>();
- Catches errors with `$q.notify({ type: 'negative', message: 'Error guardando actividad' })` — generic message.
- Needs to surface specific 409 messages + cascade `affectedSchedules` payload (Plan 01 backend contract).

Quasar 2 patterns to use: q-tabs/q-tab/q-tab-panels/q-tab-panel, q-dialog, q-form, q-select with emit-value+map-options, q-input.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Nuevo CreateSlotDialog.vue + tipo de error con affectedSchedules</name>
  <files>
    el-templo-admin/src/components/scheduling/CreateSlotDialog.vue,
    el-templo-admin/src/types/scheduling.ts,
    el-templo-admin/src/composables/useSchedulingApi.ts
  </files>
  <read_first>
    - el-templo-admin/src/components/scheduling/HolidaysDialog.vue (patrón referencia: dialog con form, validación, notify success/error, emit `holidays-changed`)
    - el-templo-admin/src/components/scheduling/ActivitiesDialog.vue (patrón referencia: defineProps show + emit update:show + watch(props.show) para reset)
    - el-templo-admin/src/composables/useSchedulingApi.ts (líneas 79-99 — `createSchedule` ya existe; sólo agregar tipo `AffectedScheduleRef`)
    - el-templo-admin/src/types/scheduling.ts (entero — agregar export `AffectedScheduleRef`)
    - el-templo-admin/src/utils/logger.ts y extract-error.ts (patrón de uso)
    - .planning/phases/113-crud-admin-schedules-activities/113-CONTEXT.md (D-17, D-20)
  </read_first>
  <action>
    **A. types/scheduling.ts** — Agregar:
    ```ts
    export type AffectedScheduleRef = {
      id: number;
      dayOfWeek: number;
      startTime: string;
      endTime: string;
      branchName: string;
    };
    ```

    **B. composables/useSchedulingApi.ts** — No agregar métodos nuevos. Sí re-exportar el tipo si ayuda (opcional). Verificar que el catch de `createSchedule` propaga el error completo (sí — `throw err` está al final del catch).

    **C. components/scheduling/CreateSlotDialog.vue** — Crear nuevo componente Quasar/Vue 3 (composition API). Estructura:

    ```vue
    <template>
      <q-dialog :model-value="show" @update:model-value="$emit('update:show', $event)">
        <q-card style="min-width: 480px; max-width: 560px">
          <q-card-section>
            <div class="text-h6">Crear horario</div>
            <div class="text-caption text-grey-7">
              Para cambiar el horario de un slot existente, desactivalo y crea uno nuevo.
            </div>
          </q-card-section>
          <q-separator />
          <q-card-section>
            <q-form @submit.prevent="onSubmit" class="q-gutter-md">
              <q-select
                v-model="form.branchId"
                :options="branchOptions"
                label="Sucursal"
                outlined
                emit-value
                map-options
                :rules="[(v) => v != null || 'Seleccioná una sucursal']"
              />
              <q-select
                v-model="form.dayOfWeek"
                :options="dayOptions"
                label="Día"
                outlined
                emit-value
                map-options
                :rules="[(v) => v != null || 'Seleccioná un día']"
              />
              <div class="row q-gutter-sm">
                <q-input
                  v-model="form.startTime"
                  label="Inicio (HH:MM)"
                  outlined
                  mask="##:##"
                  class="col"
                  :rules="[validateTime, validateRange]"
                />
                <q-input
                  v-model="form.endTime"
                  label="Fin (HH:MM)"
                  outlined
                  mask="##:##"
                  class="col"
                  :rules="[validateTime, validateRange]"
                />
              </div>
              <q-select
                v-model="form.activityId"
                :options="activeActivityOptions"
                label="Actividad"
                outlined
                emit-value
                map-options
                :loading="loadingActivities"
                :rules="[(v) => v != null || 'Seleccioná una actividad']"
              />
              <div v-if="errorMessage" class="text-negative text-caption q-mt-sm">
                {{ errorMessage }}
              </div>
              <div class="row justify-end q-gutter-sm">
                <q-btn flat label="Cancelar" @click="onCancel" />
                <q-btn type="submit" color="primary" label="Crear" :loading="submitting" />
              </div>
            </q-form>
          </q-card-section>
        </q-card>
      </q-dialog>
    </template>

    <script setup lang="ts">
    import { ref, computed, watch } from 'vue';
    import { useQuasar } from 'quasar';
    import { createLogger } from 'src/utils/logger';
    import { extractError } from 'src/utils/extract-error';
    import { useSchedulingApi } from 'src/composables/useSchedulingApi';
    import type { ActivityRecord, DayOfWeek } from 'src/types/scheduling';
    import { DAY_SHORT_LABELS } from 'src/types/scheduling';
    import type { BranchOption } from 'src/types/member';

    const log = createLogger('CreateSlotDialog');
    const $q = useQuasar();
    const schedulingApi = useSchedulingApi();

    const props = defineProps<{
      show: boolean;
      branches: BranchOption[];
      defaultBranchId: number | null;
    }>();
    const emit = defineEmits<{
      'update:show': [value: boolean];
      'created': [];
    }>();

    const form = ref({
      branchId: null as number | null,
      dayOfWeek: null as DayOfWeek | null,
      startTime: '',
      endTime: '',
      activityId: null as number | null,
    });
    const submitting = ref(false);
    const errorMessage = ref<string | null>(null);
    const activities = ref<ActivityRecord[]>([]);
    const loadingActivities = ref(false);

    const branchOptions = computed(() =>
      props.branches.map((b) => ({ label: b.name, value: b.id }))
    );
    const dayOptions = [
      { label: 'Lunes', value: 1 },
      { label: 'Martes', value: 2 },
      { label: 'Miércoles', value: 3 },
      { label: 'Jueves', value: 4 },
      { label: 'Viernes', value: 5 },
      { label: 'Sábado', value: 6 },
    ];
    const activeActivityOptions = computed(() =>
      activities.value
        .filter((a) => a.isActive)
        .map((a) => ({ label: a.name, value: a.id }))
    );

    function validateTime(v: string): true | string {
      return /^\d{2}:\d{2}$/.test(v) || 'Formato HH:MM';
    }
    function validateRange(): true | string {
      if (!form.value.startTime || !form.value.endTime) return true;
      return form.value.endTime > form.value.startTime ||
        'La hora de fin debe ser posterior al inicio';
    }

    async function loadActivities() {
      loadingActivities.value = true;
      try {
        activities.value = await schedulingApi.listActivities();
      } catch (err: unknown) {
        log.error('Error loading activities for slot creation', {
          error: err instanceof Error ? err.message : 'Error desconocido',
        });
      } finally {
        loadingActivities.value = false;
      }
    }

    function resetForm() {
      form.value = {
        branchId: props.defaultBranchId,
        dayOfWeek: null,
        startTime: '',
        endTime: '',
        activityId: null,
      };
      errorMessage.value = null;
    }

    function onCancel() {
      emit('update:show', false);
    }

    async function onSubmit() {
      if (
        form.value.branchId == null ||
        form.value.dayOfWeek == null ||
        form.value.activityId == null
      )
        return;
      submitting.value = true;
      errorMessage.value = null;
      try {
        await schedulingApi.createSchedule({
          branchId: form.value.branchId,
          activityId: form.value.activityId,
          dayOfWeek: form.value.dayOfWeek,
          startTime: form.value.startTime,
          endTime: form.value.endTime,
        });
        $q.notify({ type: 'positive', message: 'Horario creado' });
        emit('created');
        emit('update:show', false);
      } catch (err: unknown) {
        // Backend (Plan 01) devuelve 409 con message accionable:
        // "Ya existe un horario activo HH:MM-HH:MM que se solapa..."
        // o 400 "La hora de fin debe ser posterior al inicio".
        // Mostrar inline en el form (no toast genérico) para que el admin pueda corregir sin perder contexto.
        errorMessage.value = extractError(err, 'Error creando horario');
        log.error('Create slot failed', { error: errorMessage.value });
      } finally {
        submitting.value = false;
      }
    }

    watch(
      () => props.show,
      (val) => {
        if (val) {
          resetForm();
          loadActivities();
        }
      }
    );
    </script>
    ```

    Notas:
    - NO `any`. Usar `unknown` con narrowing en catches.
    - NO `console.*`. Usar `createLogger('CreateSlotDialog')`.
    - DAY_SHORT_LABELS no se usa en este dialog (los labels son los nombres completos para claridad de creación). El tipo `DayOfWeek` SÍ — se importa para tipar `form.dayOfWeek`.
    - `BranchOption` importado de `src/types/member` igual que HorariosPage.vue (línea 242).

  </action>
  <acceptance_criteria>
    - `el-templo-admin/src/components/scheduling/CreateSlotDialog.vue` exists
    - `wc -l el-templo-admin/src/components/scheduling/CreateSlotDialog.vue` returns >= 120
    - `grep -n "createSchedule(" el-templo-admin/src/components/scheduling/CreateSlotDialog.vue` returns at least 1 line
    - `grep -n "extractError" el-templo-admin/src/components/scheduling/CreateSlotDialog.vue` returns at least 1 line
    - `grep -n "any" el-templo-admin/src/components/scheduling/CreateSlotDialog.vue` returns 0 lines (or only inside strings/comments — verify by reading the file)
    - `grep -n "AffectedScheduleRef" el-templo-admin/src/types/scheduling.ts` returns 1 line (export)
    - `cd el-templo-admin && pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-admin && pnpm tsc --noEmit</automated>
  </verify>
  <done>
    Componente nuevo creado, tipo `AffectedScheduleRef` exportado, tsc clean. El dialog está aislado — la integración en `HorariosPage` ocurre en Task 2.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Integrar tabs (Horarios/Actividades) + botón "Crear horario" en HorariosPage</name>
  <files>
    el-templo-admin/src/pages/HorariosPage.vue
  </files>
  <read_first>
    - el-templo-admin/src/pages/HorariosPage.vue (entero — entender estructura controles row, grid, dialogs montados)
    - el-templo-admin/src/components/scheduling/ActivitiesDialog.vue (verificar que sigue siendo dialog auto-contenido — vamos a invocarlo igual desde la tab Actividades como dialog "siempre abierto" o convertirlo en panel)
    - el-templo-admin/src/pages/AlumnosPage.vue or any page using q-tabs/q-tab-panels in admin (patrón de tab panels — buscar con `grep -rn "q-tabs" el-templo-admin/src/pages | head -5`)
  </read_first>
  <action>
    Modificar `HorariosPage.vue`:

    **1. Importar el nuevo dialog:**
    Agregar al bloque de imports (~línea 243):
    ```ts
    import CreateSlotDialog from 'src/components/scheduling/CreateSlotDialog.vue';
    ```

    **2. Agregar state:**
    ```ts
    const activeTab = ref<'horarios' | 'actividades'>('horarios');
    const showCreateSlotDialog = ref(false);
    const branchesRaw = ref<BranchOption[]>([]); // populated alongside branchOptions
    ```
    Modificar `loadBranches()` para guardar también `branchesRaw.value = branches;` (no solo el `.map` para `branchOptions`). El tipo `BranchOption` ya está importado.

    **3. Reemplazar el botón "Actividades" del header por botón "Crear horario":**
    Quitar el `<q-btn>` con icon `sports_gymnastics` label "Actividades" (líneas 62-68) y reemplazarlo por:
    ```html
    <q-btn
      icon="add"
      label="Crear horario"
      color="primary"
      @click="showCreateSlotDialog = true"
      :disable="!selectedBranchId"
      v-if="activeTab === 'horarios'"
    />
    ```
    Mantener los botones "Sesiones de Prueba" y "Feriados" (relacionados pero ortogonales a esta fase).

    **4. Envolver el contenido del grid en una tab + agregar tab Actividades:**
    Después de la fila de controles (controls row terminating ~línea 77), antes del grid (~línea 79), insertar:
    ```html
    <q-tabs
      v-model="activeTab"
      dense
      align="left"
      class="text-primary q-mb-md"
      indicator-color="primary"
    >
      <q-tab name="horarios" label="Horarios" icon="schedule" />
      <q-tab name="actividades" label="Actividades" icon="sports_gymnastics" />
    </q-tabs>
    <q-tab-panels v-model="activeTab" animated keep-alive>
      <q-tab-panel name="horarios" class="q-pa-none">
        <!-- Aquí va TODO el bloque actual del grid (loadingGrid, !selectedBranchId,
             timeSlots.length===0, isMobile branch, desktop branch) — mover dentro -->
      </q-tab-panel>
      <q-tab-panel name="actividades" class="q-pa-none">
        <!-- Embed: usar el ActivitiesDialog como dialog estable o, mejor, mostrar un panel inline
             que invoque el mismo dialog automáticamente al estar la tab activa.
             Decisión: dejar el ActivitiesDialog como `show=true` cuando activeTab==='actividades',
             pero como dialog flotante es UX raro adentro de una tab.

             Solución pragmática: mostrar un wrapper inline que renderiza el contenido del dialog.
             Como el dialog actual tiene markup auto-contenido, en lugar de refactor profundo,
             reemplazamos el wrapper q-dialog por una variante "embedded" via prop. Ver Task 3. -->
        <ActivitiesPanel
          :active="activeTab === 'actividades'"
          @cascade-error="onCascadeError"
        />
      </q-tab-panel>
    </q-tab-panels>
    ```

    **NOTA:** En lugar de extraer un nuevo `ActivitiesPanel.vue`, una alternativa más simple es: dejar `ActivitiesDialog` siendo un dialog **y** auto-abrirlo cuando la tab Actividades esté activa, mostrando un placeholder mínimo en la tab que diga "Gestión abierta en ventana modal — cerrar volverá a Horarios". Eso es UX peor pero requiere menos refactor.

    **Decisión final:** Convertir `ActivitiesDialog.vue` en `ActivitiesPanel.vue` (Task 3) — un panel embeddable. Aquí en HorariosPage usar `<ActivitiesPanel>` directamente (no como dialog).

    Cambiar import:
    ```ts
    // Antes:
    // import ActivitiesDialog from 'src/components/scheduling/ActivitiesDialog.vue';
    // Después:
    import ActivitiesPanel from 'src/components/scheduling/ActivitiesDialog.vue';
    ```
    (Mantener el filename `ActivitiesDialog.vue` para no romper git history; sólo se renombra el import alias.) **O** renombrar el archivo si se prefiere (Task 3 detalla).

    **5. Eliminar `showActivitiesDialog`:** ya no se necesita (la tab reemplaza el dialog flotante).

    **6. Montar `<CreateSlotDialog>`:**
    Junto a los otros dialogs (~línea 221-230, después de `</q-page>` template el bloque es dentro del template pero después del grid; revisar — los dialogs están dentro del `<q-page>` cerca del final del template). Agregar:
    ```html
    <CreateSlotDialog
      v-model:show="showCreateSlotDialog"
      :branches="branchesRaw"
      :default-branch-id="selectedBranchId"
      @created="loadWeeklyGrid"
    />
    ```

    **7. Quitar el SHM `<ActivitiesDialog v-model:show="showActivitiesDialog" />`** del template (línea 228) — ya no se usa.

    **8. Cascade error handler:**
    ```ts
    function onCascadeError(payload: {
      message: string;
      affectedSchedules: AffectedScheduleRef[];
    }) {
      const list = payload.affectedSchedules
        .slice(0, 5)
        .map(
          (s) => `${DAY_SHORT_LABELS[s.dayOfWeek as DayOfWeek]} ${s.startTime}-${s.endTime} (${s.branchName})`
        )
        .join(', ');
      const more =
        payload.affectedSchedules.length > 5
          ? ` y ${payload.affectedSchedules.length - 5} más`
          : '';
      $q.notify({
        type: 'negative',
        message: `${payload.message} Afectados: ${list}${more}`,
        timeout: 8000,
        multiLine: true,
      });
    }
    ```
    Importar `AffectedScheduleRef` desde `src/types/scheduling`.

    No `any` en ningún punto. No `console.*`.

  </action>
  <acceptance_criteria>
    - `grep -n "q-tabs" el-templo-admin/src/pages/HorariosPage.vue` returns at least 1 line
    - `grep -nE "name=\"horarios\"" el-templo-admin/src/pages/HorariosPage.vue` returns at least 1 line
    - `grep -nE "name=\"actividades\"" el-templo-admin/src/pages/HorariosPage.vue` returns at least 1 line
    - `grep -n "CreateSlotDialog" el-templo-admin/src/pages/HorariosPage.vue` returns at least 2 lines (import + usage)
    - `grep -n "Crear horario" el-templo-admin/src/pages/HorariosPage.vue` returns at least 1 line
    - `grep -n "showActivitiesDialog" el-templo-admin/src/pages/HorariosPage.vue` returns 0 lines (eliminado)
    - `grep -n "AffectedScheduleRef" el-templo-admin/src/pages/HorariosPage.vue` returns at least 1 line
    - `cd el-templo-admin && pnpm tsc --noEmit` exits 0
    - `cd el-templo-admin && pnpm lint` exits 0 (or unchanged warning count)
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-admin && pnpm tsc --noEmit</automated>
  </verify>
  <done>
    HorariosPage tiene tabs Horarios/Actividades, botón "Crear horario" abre `CreateSlotDialog`, dialog `ActivitiesDialog` reemplazado por panel embebido (`ActivitiesPanel`), refresh post-create funciona.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Convertir ActivitiesDialog → panel embebido + cascade error UX (D-13)</name>
  <files>
    el-templo-admin/src/components/scheduling/ActivitiesDialog.vue
  </files>
  <read_first>
    - el-templo-admin/src/components/scheduling/ActivitiesDialog.vue (entero, 219 líneas)
    - el-templo-admin/src/types/scheduling.ts (verificar export `AffectedScheduleRef` añadido en Task 1)
    - el-templo-admin/src/utils/extract-error.ts
    - .planning/phases/113-crud-admin-schedules-activities/113-01-SUMMARY.md (shape del 409 body — `{ error, message, affectedSchedules }`)
  </read_first>
  <action>
    Refactor de `ActivitiesDialog.vue` en dos planos:

    **A. Convertir de dialog → panel embebido**

    1. Cambiar el `<template>`: eliminar el `<q-dialog>` wrapper y `<q-card>`/`<q-card-actions align="right">` con botón Cerrar. El contenido pasa a ser un `<div class="q-pa-md">` directo.

    2. Cambiar `defineProps` y emits:
    ```ts
    const props = defineProps<{
      active: boolean;  // true cuando la tab Actividades está visible
    }>();
    const emit = defineEmits<{
      'cascade-error': [payload: { message: string; affectedSchedules: AffectedScheduleRef[] }];
    }>();
    ```
    Eliminar `'update:show'` y `show`.

    3. Cambiar el watcher: en vez de `watch(() => props.show, ...)`, usar `watch(() => props.active, (val) => { if (val) { loadActivities(); cancelEditActivity(); } });` y también ejecutar inicial-load con `onMounted` si `props.active === true` desde el primer render.

    **B. Cascade error UX en deactivation flow**

    1. Importar:
    ```ts
    import type { AffectedScheduleRef } from 'src/types/scheduling';
    import { extractError } from 'src/utils/extract-error';
    import axios from 'axios';
    ```

    2. En `onToggleActivity` (línea 176), cambiar el catch:
    ```ts
    } catch (err: unknown) {
      const message = err instanceof Error ? err.message : 'Error desconocido';
      log.error('Error toggling activity', { error: message });

      // Detectar cascade-block 409 con affectedSchedules y emitir al padre
      if (axios.isAxiosError(err) && err.response?.status === 409) {
        const data = err.response.data as {
          message?: string;
          affectedSchedules?: AffectedScheduleRef[];
        };
        if (Array.isArray(data.affectedSchedules) && data.affectedSchedules.length > 0) {
          emit('cascade-error', {
            message: data.message ?? 'No se puede desactivar la actividad',
            affectedSchedules: data.affectedSchedules,
          });
          return; // No mostrar toast genérico
        }
      }
      $q.notify({ type: 'negative', message: extractError(err, 'Error actualizando actividad') });
    }
    ```

    3. En `onSaveActivity` (línea 150), cambiar el catch genérico para surfacear 409 de uniqueness:
    ```ts
    } catch (err: unknown) {
      const message = err instanceof Error ? err.message : 'Error desconocido';
      log.error('Error saving activity', { error: message });
      // 409 de uniqueness ("Ya existe una actividad activa con ese nombre") debe verse tal cual
      $q.notify({ type: 'negative', message: extractError(err, 'Error guardando actividad') });
    }
    ```
    (Sólo cambia el fallback genérico por `extractError` para que el message del backend llegue al usuario.)

    **C. Confirmación más explícita en `confirmDeactivate`**

    Actualizar el message en el `$q.dialog` (línea 199) para reflejar que ahora puede bloquear:
    ```ts
    message: `Desactivar "${act.name}"? Si hay horarios activos usandola, vas a tener que cambiarles la actividad o desactivarlos primero.`
    ```

    **D. NOTA importante:** El alias de import en `HorariosPage.vue` (Task 2) es `import ActivitiesPanel from 'src/components/scheduling/ActivitiesDialog.vue';` — el archivo se queda con el nombre `ActivitiesDialog.vue` por minimizar churn de git. El alias del import lo usa como `ActivitiesPanel`. Documentar esto con un comentario al tope del archivo:
    ```vue
    <!--
      Phase 113: este componente fue refactor desde dialog flotante a panel embebido,
      consumido como tab Actividades en HorariosPage. El nombre del archivo se mantiene
      por compatibilidad de git history; el alias del import en HorariosPage es
      `ActivitiesPanel`.
    -->
    ```

    No `any`, no `console.*`. Logger ya está usado.

  </action>
  <acceptance_criteria>
    - `grep -nE "<q-dialog" el-templo-admin/src/components/scheduling/ActivitiesDialog.vue` returns 0 lines (dialog wrapper eliminado)
    - `grep -n "active: boolean" el-templo-admin/src/components/scheduling/ActivitiesDialog.vue` returns at least 1 line
    - `grep -n "cascade-error" el-templo-admin/src/components/scheduling/ActivitiesDialog.vue` returns at least 2 lines (defineEmits + emit call)
    - `grep -n "axios.isAxiosError" el-templo-admin/src/components/scheduling/ActivitiesDialog.vue` returns at least 1 line
    - `grep -n "AffectedScheduleRef" el-templo-admin/src/components/scheduling/ActivitiesDialog.vue` returns at least 1 line
    - `grep -n "update:show" el-templo-admin/src/components/scheduling/ActivitiesDialog.vue` returns 0 lines (emit eliminado)
    - `cd el-templo-admin && pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-admin && pnpm tsc --noEmit && cd ../el-templo-admin && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>
    `ActivitiesDialog.vue` ahora es un panel embebido (sin q-dialog wrapper); deactivation 409 con `affectedSchedules` emite `cascade-error` al padre que lo surfacea con toast accionable; uniqueness 409 muestra mensaje del backend en el toast (no genérico); HorariosPage build queda verde.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                                                                                          |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| admin user → admin app | Admin user inputs in CreateSlotDialog form (HH:MM strings, dropdown ids); validated client-side then re-validated by Plan-01 backend |
| admin app → API        | Axios calls with JWT bearer; same auth surface as existing endpoints (no new attack surface)                                         |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                | Disposition | Mitigation Plan                                                                                          |
| ----------- | ---------------------- | ---------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------- |
| T-113-FE-01 | Tampering              | Client-only time validation              | accept      | Backend (Plan 01) re-validates `endTime > startTime` and overlap; client `validateRange` is UX hint only |
| T-113-FE-02 | Information Disclosure | affectedSchedules toast                  | accept      | Same data already visible in weekly grid; admin is the only viewer                                       |
| T-113-FE-03 | DoS                    | listActivities call on every dialog open | accept      | Activities table small (<50 rows); cache invalidation on tab switch acceptable                           |

</threat_model>

<verification>
1. `cd el-templo-admin && pnpm tsc --noEmit` clean.
2. `cd el-templo-admin && pnpm build` exits 0 (build catches template/import errors).
3. Manual smoke (NOT a checkpoint — admin is local, autonomous mode): N/A — defer to user post-deploy.
4. `grep -c "console\\." el-templo-admin/src/components/scheduling/CreateSlotDialog.vue el-templo-admin/src/components/scheduling/ActivitiesDialog.vue` returns 0.
5. `grep -nE ":\s*any[\s,;)]" el-templo-admin/src/components/scheduling/CreateSlotDialog.vue el-templo-admin/src/components/scheduling/ActivitiesDialog.vue el-templo-admin/src/pages/HorariosPage.vue` returns 0 lines.
6. `grep -n "PUT.*toggle\\|PATCH.*activity" el-templo-admin/src/components/scheduling/SlotDetailDialog.vue` UNCHANGED (verify SlotDetailDialog.vue not modified — D-19).
</verification>

<success_criteria>

- `CreateSlotDialog.vue` existe (≥120 líneas), invoca `useSchedulingApi.createSchedule`, usa `extractError` para 409/400.
- `HorariosPage.vue` con tabs Horarios/Actividades, botón "Crear horario" en header (sólo visible en tab Horarios), `<CreateSlotDialog>` montado, panel `ActivitiesPanel` (alias) en tab Actividades.
- `ActivitiesDialog.vue` refactor a panel embebido (sin q-dialog), emite `cascade-error` con `affectedSchedules` al recibir 409 backend; uniqueness 409 surfacea message backend.
- `tsc --noEmit` y `pnpm build` ambos clean en admin.
- Sin instalación/actualización de dependencias.
- `SlotDetailDialog.vue` intacto (D-19).
- Endpoints existentes en backend (`PUT /toggle`, `PATCH /activity`) sin tocar.
  </success_criteria>

<output>
After completion, create `.planning/phases/113-crud-admin-schedules-activities/113-02-SUMMARY.md` documentando:
- Líneas exactas modificadas en HorariosPage.vue (tab markup, button removed/added, dialog mount).
- Cambios estructurales en ActivitiesDialog.vue (dialog → panel + emit cascade-error).
- Decisión sobre filename de ActivitiesDialog.vue (mantenido vs renombrado).
- Cualquier issue encontrado en `pnpm build` y cómo se resolvió.
</output>
