---
phase: 154-alumnos-de-templo-ficaci-n-accesos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/settings/keys.ts
  - el-templo-api/src/modules/settings/service.ts
  - el-templo-api/src/modules/settings/routes.ts
  - el-templo-api/src/modules/settings/index.ts
  - el-templo-api/src/app.ts
  - el-templo-api/src/db/migrations/0166_seed_pricing_card_surcharge.sql
  - el-templo-api/test/settings/pricing-setting.test.ts
autonomous: true
requirements: [ALUM-03]
must_haves:
  truths:
    - "El owner puede leer y cambiar la regla de recargo por tarjeta vía API"
    - "Cualquier staff (incl. profe) puede leer el valor de la regla (lo necesita la UI de cobro)"
    - "Un staff no-owner NO puede cambiar la regla (403)"
    - "La instalación El Templo arranca con la regla ON (comportamiento actual intacto)"
    - "Una instalación white-label nueva (sin la key) arranca con la regla OFF"
  artifacts:
    - path: "el-templo-api/src/modules/settings/keys.ts"
      provides: "Constante única de la setting key (pricing.card_surcharge_enabled)"
      contains: "pricing.card_surcharge_enabled"
    - path: "el-templo-api/src/modules/settings/service.ts"
      provides: "Read/upsert de la setting con default OFF"
    - path: "el-templo-api/src/modules/settings/routes.ts"
      provides: "GET (staff) + PUT (owner-only) /api/admin/settings/pricing/card-surcharge"
    - path: "el-templo-api/src/db/migrations/0166_seed_pricing_card_surcharge.sql"
      provides: "Seed idempotente ON para El Templo"
      contains: "pricing.card_surcharge_enabled"
    - path: "el-templo-api/test/settings/pricing-setting.test.ts"
      provides: "Cobertura CRUD + guard owner"
  key_links:
    - from: "el-templo-api/src/app.ts"
      to: "settingsRoutes"
      via: "app.register(prefix /api/admin/settings)"
      pattern: "settingsRoutes"
    - from: "el-templo-api/src/modules/settings/service.ts"
      to: "system_settings"
      via: "drizzle onDuplicateKeyUpdate + select"
      pattern: "onDuplicateKeyUpdate"
---

<objective>
Crear la infraestructura server-side de la setting de recargo por medio de pago (ALUM-03): un módulo `settings` con la key canónica, un servicio de lectura/upsert con default OFF, endpoints (GET staff-readable + PUT owner-only) y la migración idempotente 0166 que deja la regla ON para la instalación El Templo.

Purpose: Es el punto único de verdad de la regla. Los planes 02 (gate server-side del precio) y 03/04 (UI del admin) la consumen. Sin esto, la de-Templo-ficación del recargo por tarjeta no tiene dónde vivir.
Output: Módulo `settings` registrado + migración 0166 + test de integración.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/154-alumnos-de-templo-ficaci-n-accesos/154-CONTEXT.md
@.planning/phases/154-alumnos-de-templo-ficaci-n-accesos/154-PATTERNS.md

<interfaces>
<!-- Contratos que el executor debe respetar. Extraídos del codebase. -->

Schema (NO se toca) — el-templo-api/src/db/schema/system-settings.ts:
systemSettings: { id, settingKey: varchar(100) UNIQUE, settingValue: text, updatedAt }

Guard owner-only — el-templo-api/src/modules/shared/permissions.ts:20:
export const OWNER_ROLES = ["owner"] as const;

Registro de plugins — el-templo-api/src/app.ts (analog líneas 43 import, 156 register):
import { userRoutes } from "./modules/users";
await app.register(memberRoutes, { prefix: "/api/admin/members" });

Read analog — streaks/service.ts:273-293 (select + Map + fallback a default cuando la key no existe)
Write analog — ratings/service.ts:172 (.onDuplicateKeyUpdate({ set: { ... } }))
Error handling — modules/shared/error-handler: handleServiceError(err, reply, request.log, "context")
Seed idempotente analog — db/migrations/0157_seed_finance_overdue_threshold.sql
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Módulo settings — keys + service + routes owner-gated, registrado en app.ts</name>
  <files>el-templo-api/src/modules/settings/keys.ts, el-templo-api/src/modules/settings/service.ts, el-templo-api/src/modules/settings/routes.ts, el-templo-api/src/modules/settings/index.ts, el-templo-api/src/app.ts</files>
  <read_first>
    - el-templo-api/src/modules/users/routes.ts (plugin owner-gated: hook onRequest líneas 20-34, handleServiceError línea 79, schema por ruta)
    - el-templo-api/src/modules/users/index.ts (patrón de re-export)
    - el-templo-api/src/modules/streaks/service.ts (líneas 1-20 imports/constructor, 273-293 read de system_settings con fallback a default)
    - el-templo-api/src/modules/ratings/service.ts (línea 172 onDuplicateKeyUpdate)
    - el-templo-api/src/modules/shared/permissions.ts (OWNER_ROLES línea 20)
    - el-templo-api/src/app.ts (líneas 43 y 156 como analog de import + register)
    - el-templo-api/src/db/schema/system-settings.ts (schema completo)
    - ./CLAUDE.md (logging: request.log/app.log nunca console; no any)
  </read_first>
  <behavior>
    - GET /api/admin/settings/pricing/card-surcharge autenticado (cualquier staff) devuelve { enabled: boolean }
    - GET con la key ausente devuelve { enabled: false } (default OFF)
    - PUT /api/admin/settings/pricing/card-surcharge con role owner y body { enabled: true } persiste 'on' y devuelve { enabled: true }
    - PUT con role NO-owner devuelve 403
    - Un segundo PUT { enabled: false } sobre una key existente actualiza a 'off' (upsert, no duplica fila)
  </behavior>
  <action>
    Crear `keys.ts`: `export const PRICING_SETTINGS_KEYS = { cardSurcharge: "pricing.card_surcharge_enabled" } as const;` — ésta es la ÚNICA definición de la key en todo el repo (el plan 02 la importa desde acá).

    Crear `service.ts`: clase `SettingsService` con constructor `(db: MySql2Database<typeof schema>, log: FastifyBaseLogger)` (mismo shape que streaks/service). Método `getCardSurchargeEnabled(): Promise<boolean>` = select de `systemSettings` where `settingKey = PRICING_SETTINGS_KEYS.cardSurcharge`, devuelve `row?.settingValue === "on"` (default `false` si no hay fila — este es el default OFF de D-03). Método `setCardSurchargeEnabled(enabled: boolean): Promise<void>` = `db.insert(systemSettings).values({ settingKey: PRICING_SETTINGS_KEYS.cardSurcharge, settingValue: enabled ? "on" : "off" }).onDuplicateKeyUpdate({ set: { settingValue: enabled ? "on" : "off" } })`. Serializar como `'on'`/`'off'` (string) — es la discreción de shape autorizada en CONTEXT.

    Crear `routes.ts`: `export const settingsRoutes: FastifyPluginAsync`. Hook `onRequest` a nivel de plugin que llama `fastify.authenticate` (staff cualquiera puede leer — NO owner-gate a nivel plugin, porque el GET lo consume la UI de cobro del profe, ver PATTERNS hallazgo/nota D-04). GET `/pricing/card-surcharge` con response schema `{ enabled: boolean }` → llama `getCardSurchargeEnabled`. PUT `/pricing/card-surcharge` con `preHandler` propio que verifica `OWNER_ROLES.includes(request.user.role)` y si no, `reply.code(403).send({ error: "Acceso denegado", message: "Solo el propietario puede cambiar reglas de precio" })`; body schema `{ type: "object", required: ["enabled"], properties: { enabled: { type: "boolean" } } }` → llama `setCardSurchargeEnabled` y devuelve `{ enabled }`. Instanciar el service con `fastify.db` y `fastify.log`. Errores con `handleServiceError` en el catch. Nunca console.* (usar request.log). Sin `any`.

    Crear `index.ts`: `export { settingsRoutes } from "./routes";`.

    Registrar en `app.ts`: import `{ settingsRoutes } from "./modules/settings"` junto a los otros imports de módulos, y `await app.register(settingsRoutes, { prefix: "/api/admin/settings" });` junto a los otros register (analog memberRoutes línea 156).

  </action>
  <acceptance_criteria>
    - `el-templo-api/src/modules/settings/keys.ts` exporta `PRICING_SETTINGS_KEYS.cardSurcharge === "pricing.card_surcharge_enabled"` — grep: `grep -c "pricing.card_surcharge_enabled" el-templo-api/src/modules/settings/keys.ts` == 1
    - `settingsRoutes` registrado en app.ts con prefix `/api/admin/settings` — grep: `grep -c "settingsRoutes" el-templo-api/src/app.ts` >= 2 (import + register)
    - El PUT tiene un guard que referencia `OWNER_ROLES` — grep: `grep -c "OWNER_ROLES" el-templo-api/src/modules/settings/routes.ts` >= 1
    - Sin `console.` en los archivos nuevos — `grep -rc "console\." el-templo-api/src/modules/settings/` == 0
    - Sin `: any` en los archivos nuevos — `grep -rn ": any" el-templo-api/src/modules/settings/` sin resultados
    - `cd el-templo-api && pnpm exec tsc --noEmit` verde
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>El módulo settings existe, está registrado en app.ts, GET/PUT tipados con guard owner en el PUT, tsc verde.</done>
</task>

<task type="auto">
  <name>Task 2: Migración 0166 idempotente (ON para El Templo) + test de integración</name>
  <files>el-templo-api/src/db/migrations/0166_seed_pricing_card_surcharge.sql, el-templo-api/test/settings/pricing-setting.test.ts</files>
  <read_first>
    - el-templo-api/src/db/migrations/0157_seed_finance_overdue_threshold.sql (analog EXACTO: seed idempotente de system_settings, WHERE NOT EXISTS, reglas de comentarios)
    - el-templo-api/test/finance/coach-load-alta.test.ts (estructura de test: createTestApp, createStaffUser, getAuthToken, app.inject, header con inventario de escenarios)
    - el-templo-api/test/helpers.ts (línea 208: `schema.systemSettings` se limpia entre tests → sembrar la setting en cada caso; helpers de auth)
    - .planning/phases/154-alumnos-de-templo-ficaci-n-accesos/154-PATTERNS.md (sección Migración 0166 + gotcha del cleanup)
    - ./CLAUDE.md (migraciones a mano con el runner propio; NUNCA `;` dentro de comentarios SQL; commitear el SQL junto al código)
  </read_first>
  <action>
    Crear `0166_seed_pricing_card_surcharge.sql` copiando la forma de 0157: `INSERT INTO system_settings (setting_key, setting_value) SELECT 'pricing.card_surcharge_enabled', 'on' WHERE NOT EXISTS (SELECT 1 FROM system_settings WHERE setting_key = 'pricing.card_surcharge_enabled')`. Deja la regla ON para El Templo (comportamiento actual intacto, D-03) y respeta un valor previo seteado por PUT (semántica "no clobberear"). Comentarios `--` sin ningún `;` adentro (el runner splittea por `;` antes de strippear comentarios; ver MEMORY). NO usar `db:generate` (roto); escribir el archivo a mano con el número 0166 (la última es 0165).

    Crear `test/settings/pricing-setting.test.ts` (Vitest, estructura de coach-load-alta.test.ts). Escenarios: (a) crear owner y staff no-owner con `createStaffUser`/`getAuthToken`; (b) GET sin sembrar la key → `{ enabled: false }` (default OFF); (c) PUT owner `{ enabled: true }` → 200 `{ enabled: true }`, luego GET → `{ enabled: true }`; (d) PUT owner `{ enabled: false }` sobre la key existente → 200 y GET → `{ enabled: false }` (upsert, verificar que no explota por duplicado); (e) PUT con token no-owner → 403. Sembrar/limpiar según helpers (recordar el cleanup de systemSettings). No correr el suite localmente — corre en CI.

  </action>
  <acceptance_criteria>
    - `0166_seed_pricing_card_surcharge.sql` existe y contiene `pricing.card_surcharge_enabled` y `WHERE NOT EXISTS` — `grep -c "WHERE NOT EXISTS" el-templo-api/src/db/migrations/0166_seed_pricing_card_surcharge.sql` == 1
    - Ninguna línea de comentario del SQL contiene `;` — `grep -n "^--" el-templo-api/src/db/migrations/0166_seed_pricing_card_surcharge.sql | grep ";"` sin resultados
    - El número de migración es exactamente 0166 (siguiente a 0165) — el archivo `0166_*.sql` existe y no hay colisión
    - El test cubre owner-200 y no-owner-403 y default-OFF — grep en el test: contiene `403` y `enabled` y referencia `card-surcharge`
    - `cd el-templo-api && pnpm exec tsc --noEmit` verde (el test tipa)
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Migración 0166 escrita a mano (idempotente, ON para Templo, sin `;` en comentarios), test de integración cubre CRUD + guard owner + default OFF, tsc verde. El test corre en CI al pushear.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                                                  |
| -------------------------- | -------------------------------------------------------------------------------------------- |
| admin staff → API settings | Staff autenticado (incl. profe) puede leer; solo owner debe poder escribir                   |
| migración → DB prod        | Seed viaja a prod con el tren; debe ser idempotente y no clobberear valores seteados por PUT |

## STRIDE Threat Register

| Threat ID | Category               | Component                                      | Disposition | Mitigation Plan                                                                     |
| --------- | ---------------------- | ---------------------------------------------- | ----------- | ----------------------------------------------------------------------------------- |
| T-154-01  | Elevation of Privilege | PUT /api/admin/settings/pricing/card-surcharge | mitigate    | preHandler owner-only sobre el PUT (OWNER_ROLES → 403); test no-owner=403           |
| T-154-02  | Information Disclosure | GET setting (staff-readable)                   | accept      | el valor es un flag de precio no sensible; el staff lo necesita para la UI de cobro |
| T-154-03  | Tampering              | migración 0166 re-run / valor previo por PUT   | mitigate    | INSERT ... WHERE NOT EXISTS (idempotente, no clobberea)                             |
| T-154-SC  | Tampering              | npm installs                                   | accept      | esta fase NO instala ni actualiza dependencias (usa drizzle/fastify existentes)     |

</threat_model>

<verification>
- `cd el-templo-api && pnpm exec tsc --noEmit` verde
- `settingsRoutes` registrado en app.ts con prefix `/api/admin/settings`
- Migración 0166 idempotente, sin `;` en comentarios
- Test de integración cubre GET default OFF, PUT owner 200, PUT no-owner 403, upsert idempotente
</verification>

<success_criteria>
La regla de recargo por tarjeta tiene un hogar server-side canónico: key única, servicio con default OFF, endpoints (GET staff / PUT owner-only) y seed idempotente que deja El Templo en ON. Consumible por los planes 02, 03 y 04.
</success_criteria>

<output>
Create `.planning/phases/154-alumnos-de-templo-ficaci-n-accesos/154-01-SUMMARY.md` when done
</output>
