---
phase: 154-alumnos-de-templo-ficaci-n-accesos
plan: 05
type: execute
wave: 3
depends_on: [154-02, 154-03]
files_modified:
  - el-templo-admin/src/pages/AlumnosPage.vue
  - el-templo-admin/src/pages/AlumnoDetailPage.vue
  - el-templo-admin/src/types/member.ts
autonomous: true
requirements: [ALUM-01, ALUM-02, ALUM-04, ALUM-05]
must_haves:
  truths:
    - "'Crear alumno' es la acción primaria prominente del header de Alumnos; 'Nuevo en Prueba' y export quedan secundarios"
    - "Cada fila de alumno tiene un botón de cobro junto al lápiz que navega a /cobros?memberId={id}"
    - "En toda la UI de Alumnos, 'Avatar' se llama 'Categoría' (columna, filtro, ficha) sin cambiar el mecanismo"
    - "Con TEMPLO_GREEK_LEVELS off, la columna Nivel, su filtro y el badge griego de la ficha quedan ocultos; con on, visibles como hoy"
    - "El export Excel omite la columna Nivel cuando los niveles griegos están gateados off (consistencia)"
  artifacts:
    - path: "el-templo-admin/src/pages/AlumnosPage.vue"
      provides: "Header prominente, botón de cobro en fila, renames avatar→categoría, gating de niveles + export"
    - path: "el-templo-admin/src/pages/AlumnoDetailPage.vue"
      provides: "Labels avatar→categoría + gating del badge/subtítulo de nivel griego"
    - path: "el-templo-admin/src/types/member.ts"
      provides: "Campo includeGreekLevel en MemberListParams para tipar el export gateado"
  key_links:
    - from: "el-templo-admin/src/pages/AlumnosPage.vue"
      to: "/cobros?memberId="
      via: "router.push query en la acción de fila"
      pattern: "memberId"
    - from: "el-templo-admin/src/pages/AlumnosPage.vue"
      to: "TEMPLO_GREEK_LEVELS"
      via: "import desde config/templo-config + gate de columna/filtro/export"
      pattern: "TEMPLO_GREEK_LEVELS"
---

<objective>
Rehacer la UX de la página de Alumnos (ALUM-01, ALUM-02, ALUM-04, ALUM-05): "Crear alumno" como acción primaria prominente y degradar las secundarias (D-01); botón de cobro en cada fila junto al lápiz que navega a `/cobros?memberId={id}` (D-02); renombrar "Avatar"→"Categoría" en toda la UI conservando el mecanismo (D-06); y gatear los niveles griegos (columna, filtro, badge de la ficha y columna del export) por el flag de superficie `TEMPLO_GREEK_LEVELS` (D-07/D-08).

Purpose: Es la cara visible de la de-Templo-ficación de Alumnos. El consumo de `?memberId=` (plan 04) y el flag + param de export (planes 03/02) ya existen; acá se wirea la UI que los dispara.
Output: AlumnosPage + AlumnoDetailPage reformadas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/154-alumnos-de-templo-ficaci-n-accesos/154-CONTEXT.md
@.planning/phases/154-alumnos-de-templo-ficaci-n-accesos/154-PATTERNS.md

<interfaces>
<!-- Contratos existentes + de planes 02/03. -->

Del plan 03 (dependencia) — config:
export const TEMPLO_GREEK_LEVELS: boolean; // superficie Templo, gate por instalación (no por usuario)

Del plan 02 (dependencia) — API export:
GET /export acepta query param includeGreekLevel (boolean, default true → columna presente)

Admin export composable — src/composables/useMembersApi.ts:503:
async function exportMembers(params?: Omit<MemberListParams, 'page' | 'limit'>): Promise<Blob>
// pasa `params` DIRECTO al query string de GET /admin/members/export → basta agregar el campo al tipo
interface MemberListParams // src/types/member.ts:231

AlumnosPage.vue (a modificar):

- Header 20-44: export (flat round), "Nuevo en Prueba" (warning dense), "Nuevo" (primary dense, @click showCreateDialog=true línea 42)
- Flujo alta existente: showCreateDialog → MemberFormDialog (291) → onMemberSaved (813-828)
- Fila acciones 280-287: q-btn edit icon @click viewMember (viewMember 791-793 = router.push(`/alumnos/${id}`))
- Columna acciones width 583; Renames avatar: filtro label línea 150, avatarFilterOptions 467-481 ('Sin avatar' 480), columna avatar 555
- Niveles: columna nivel 545-552 + template #body-cell-nivel 241-248; filtro nivel 110-121 + levelFilterOptions 440-448; visibleColumns computed 592; greekLevel/LEVEL_GREEK_MAP 598-609
- Export onExport 749-783 (membersApi.exportMembers manda filtros incl. level)
- createLogger('AlumnosPage') 342; catch pattern 666-669

AlumnoDetailPage.vue (a modificar):

- Badge avatar 77-85 (tooltip "Avatar: ...")
- Nivel: badge griego flotante 32-39 (greekLevel), subtítulo levelDisplayName(level) 46
- Helpers locales LEVEL_GREEK_MAP/LEVEL_NAMES/greekLevel/levelDisplayName 1055-1083
- AVATAR_LABELS en src/types/member.ts:122 (valores A-K, NO contienen "Avatar" → sin cambios)

NOTA (PATTERNS hallazgo 1): MemberFormDialog.vue NO tiene campo avatar → NO se toca.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AlumnosPage — header prominente, botón de cobro en fila, renames, gating de niveles + export</name>
  <files>el-templo-admin/src/pages/AlumnosPage.vue, el-templo-admin/src/types/member.ts</files>
  <read_first>
    - el-templo-admin/src/pages/AlumnosPage.vue (header 20-44; fila acciones 280-287; viewMember 791-793; filtro avatar 148-150; avatarFilterOptions 467-481; columna avatar 555; columna/template nivel 241-248 y 545-552; filtro nivel 110-121 y levelFilterOptions 440-448; visibleColumns 592; onExport 749-783; createLogger 342)
    - el-templo-admin/src/composables/useMembersApi.ts (exportMembers línea 503: forwardea params al query string)
    - el-templo-admin/src/types/member.ts (MemberListParams línea 231; AVATAR_LABELS línea 122 — no se toca)
    - el-templo-admin/src/config/templo-config.ts (TEMPLO_GREEK_LEVELS del plan 03; precedente de import page→config: DeudasPage.vue:73)
    - .planning/phases/154-alumnos-de-templo-ficaci-n-accesos/154-CONTEXT.md (D-01, D-02, D-06, D-07)
    - .planning/phases/154-alumnos-de-templo-ficaci-n-accesos/154-PATTERNS.md (sección AlumnosPage: ocurrencias exactas de "Avatar", superficies de nivel, gate en visibleColumns)
    - ./CLAUDE.md (createLogger nunca console; no any)
  </read_first>
  <action>
    ALUM-01 (D-01) header: convertir "Crear alumno" en la acción primaria — el botón que hoy es "Nuevo" (primary dense, @click showCreateDialog=true, línea 42) pasa a label completo "Crear alumno", `color="primary"`, sin `dense` (botón grande). Degradar "Nuevo en Prueba" y el export a acciones secundarias (visualmente menores: dense/flat/outline, orden a discreción del CONTEXT). NO crear flujo de alta nuevo: reusar `showCreateDialog`/MemberFormDialog/onMemberSaved.

    ALUM-02 (D-02) botón de cobro en fila: en el template `#body-cell-acciones` (280-287), agregar un segundo `q-btn flat dense round` con ícono de pago (p.ej. `payments` o `attach_money`, discreción) y `<q-tooltip>Registrar cobro</q-tooltip>`, cuyo `@click` haga `router.push({ path: '/cobros', query: { memberId: String(props.row.id) } })`. Ampliar el `style: 'width: ...'` de la columna `acciones` (583) para dos botones (~80px). El lápiz (editar) queda.

    ALUM-04 (D-06) renames Avatar→Categoría (label only, mecanismo intacto): `label="Avatar"` del q-select de filtro (150) → "Categoría"; `avatarFilterOptions` opción `{ label: 'Sin avatar', value: 'none' }` (480) → "Sin categoría"; columna `{ name: 'avatar', label: 'Avatar', ... }` (555) → "Categoría". NO cambiar `filters.avatarType`, `props.row.avatarType`, ni el param de API. NO usar "Segmento" (colisión member_segment fase 136).

    ALUM-05 (D-07/D-08) gating de niveles por `TEMPLO_GREEK_LEVELS` (import desde `src/config/templo-config`): (a) columna Nivel — filtrar la entrada `nivel` en el `computed visibleColumns` (592) cuando el flag esté off (punto natural, ya es computed); (b) filtro de nivel — el q-select (110-121) y su render se muestran solo si el flag on (envolver con `v-if`); (c) export — en `onExport` (749-783), pasar `includeGreekLevel: TEMPLO_GREEK_LEVELS` a `membersApi.exportMembers(...)` para que la columna Nivel del Excel siga el flag (consistencia, discreción recomendada del CONTEXT). Los helpers `greekLevel`/`LEVEL_GREEK_MAP` (598-609) quedan (no se borra lógica, NAV-04). Con el flag on, todo se ve como hoy.

    Tipado del export: agregar `includeGreekLevel?: boolean;` a la interface `MemberListParams` (`src/types/member.ts:231`). Como `exportMembers` forwardea `params` al query string sin transformarlos, esto basta para que el llamado tipe y el param viaje al backend (plan 02). Nunca console.*; createLogger; sin `any`.

  </action>
  <acceptance_criteria>
    - Header: existe un botón "Crear alumno" primario no-dense — `grep -c "Crear alumno" el-templo-admin/src/pages/AlumnosPage.vue` >= 1
    - Fila: hay un botón de cobro que navega a /cobros con memberId — `grep -c "memberId" el-templo-admin/src/pages/AlumnosPage.vue` >= 1 y `grep -c "'/cobros'" el-templo-admin/src/pages/AlumnosPage.vue` >= 1
    - Renames: no queda ningún label visible "Avatar"/"Sin avatar" — `grep -n "label.*'Avatar'\|label=\"Avatar\"\|Sin avatar" el-templo-admin/src/pages/AlumnosPage.vue` sin resultados; y `filters.avatarType`/`avatarType` siguen presentes (mecanismo intacto)
    - Gating: la página importa TEMPLO_GREEK_LEVELS y lo usa en visibleColumns/filtro/export — `grep -c "TEMPLO_GREEK_LEVELS" el-templo-admin/src/pages/AlumnosPage.vue` >= 2
    - Export pasa includeGreekLevel y el tipo lo acepta — `grep -c "includeGreekLevel" el-templo-admin/src/pages/AlumnosPage.vue` >= 1 y `grep -c "includeGreekLevel" el-templo-admin/src/types/member.ts` == 1
    - Sin `console.` nuevo — `grep -c "console\." el-templo-admin/src/pages/AlumnosPage.vue` no aumenta respecto de HEAD
    - `cd el-templo-admin && pnpm lint` verde
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-admin && pnpm lint</automated>
  </verify>
  <done>Header con "Crear alumno" primario + secundarias degradadas, botón de cobro por fila → /cobros?memberId=, "Avatar" renombrado a "Categoría", niveles griegos (columna/filtro/export) gateados por TEMPLO_GREEK_LEVELS; lint verde.</done>
</task>

<task type="auto">
  <name>Task 2: AlumnoDetailPage — labels avatar→categoría + gating del badge/subtítulo de nivel griego</name>
  <files>el-templo-admin/src/pages/AlumnoDetailPage.vue</files>
  <read_first>
    - el-templo-admin/src/pages/AlumnoDetailPage.vue (badge avatar 77-85 tooltip "Avatar:"; badge griego flotante 32-39; subtítulo levelDisplayName 46; helpers 1055-1083)
    - el-templo-admin/src/config/templo-config.ts (TEMPLO_GREEK_LEVELS del plan 03)
    - .planning/phases/154-alumnos-de-templo-ficaci-n-accesos/154-PATTERNS.md (sección AlumnoDetailPage)
    - ./CLAUDE.md (no console, no any)
  </read_first>
  <action>
    ALUM-04 (D-06): en el tooltip del badge de avatar (77-85), cambiar el texto "Avatar:" por "Categoría:". Los valores (`AVATAR_LABELS`, `memberProfile.avatarType`) quedan intactos. `AVATAR_LABELS` en `src/types/member.ts` NO se toca (sus valores A-K no contienen la palabra "Avatar").

    ALUM-05 (D-07): importar `TEMPLO_GREEK_LEVELS` de `src/config/templo-config` y gatear las dos superficies de nivel: el badge griego flotante sobre la foto (32-39, usa `greekLevel`) y el subtítulo `levelDisplayName(memberProfile.level)` (46) se muestran solo con el flag on (envolver con `v-if="TEMPLO_GREEK_LEVELS"` o equivalente). Los helpers locales (1055-1083) quedan (gate only, no se borra lógica). Con el flag on, se ve como hoy. Sin console; sin `any`.

  </action>
  <acceptance_criteria>
    - No queda el label "Avatar:" en el tooltip — `grep -c "Avatar:" el-templo-admin/src/pages/AlumnoDetailPage.vue` == 0 y `grep -c "Categoría:" el-templo-admin/src/pages/AlumnoDetailPage.vue` >= 1
    - El badge/subtítulo de nivel están gateados — `grep -c "TEMPLO_GREEK_LEVELS" el-templo-admin/src/pages/AlumnoDetailPage.vue` >= 1
    - Los helpers de nivel siguen definidos (no se borraron) — greekLevel/levelDisplayName siguen presentes
    - Sin `console.` nuevo; sin `: any` nuevo
    - `cd el-templo-admin && pnpm lint` verde
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-admin && pnpm lint</automated>
  </verify>
  <done>La ficha del alumno dice "Categoría" en vez de "Avatar" y oculta el nivel griego cuando TEMPLO_GREEK_LEVELS está off; lint verde.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary         | Description                                                                   |
| ---------------- | ----------------------------------------------------------------------------- |
| admin UI Alumnos | Cambios de presentación/nav; sin nuevos endpoints ni datos sensibles cruzando |

## STRIDE Threat Register

| Threat ID | Category               | Component                             | Disposition | Mitigation Plan                                                                                                        |
| --------- | ---------------------- | ------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------- |
| T-154-12  | Information Disclosure | columna/badge de nivel griego         | mitigate    | gating por TEMPLO_GREEK_LEVELS en UI + includeGreekLevel en export (server, plan 02) → consistente en pantalla y Excel |
| T-154-13  | Tampering              | botón de cobro por fila (query param) | accept      | solo navega con un id de fila ya scoped por el listado server-side; el consumo valida el id (plan 04)                  |
| T-154-SC  | Tampering              | npm installs                          | accept      | esta fase NO instala ni actualiza dependencias                                                                         |

</threat_model>

<verification>
- `cd el-templo-admin && pnpm lint` verde
- "Crear alumno" primario; secundarias degradadas
- Botón de cobro por fila → /cobros?memberId=
- "Avatar"→"Categoría" en columna/filtro/ficha; mecanismo intacto
- Niveles griegos gateados por TEMPLO_GREEK_LEVELS en columna/filtro/badge/subtítulo/export
</verification>

<success_criteria>
La página de Alumnos prioriza crear y cobrar, llama "Categoría" al avatar y esconde los niveles griegos fuera del default white-label — todo consistente entre pantalla y export, y sin tocar los mecanismos subyacentes (avatarType, users.level, columnas de precio).
</success_criteria>

<output>
Create `.planning/phases/154-alumnos-de-templo-ficaci-n-accesos/154-05-SUMMARY.md` when done
</output>
