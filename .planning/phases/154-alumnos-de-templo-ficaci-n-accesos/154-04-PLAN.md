---
phase: 154-alumnos-de-templo-ficaci-n-accesos
plan: 04
type: execute
wave: 3
depends_on: [154-01, 154-03]
files_modified:
  - el-templo-admin/src/pages/CobrosPage.vue
  - el-templo-admin/src/components/AssignPlanDialog.vue
  - el-templo-admin/src/components/PlanFormDialog.vue
autonomous: true
requirements: [ALUM-02, ALUM-03]
must_haves:
  truths:
    - "Llegar a /cobros?memberId={id} preselecciona ese socio y entra al paso de socio del wizard"
    - "Un memberId inexistente muestra un aviso y cae al flujo normal (no rompe la página)"
    - "Con la regla de recargo OFF, CobrosPage no aplica el precio de tarjeta (usa priceRegular/priceZero)"
    - "Con la regla OFF, AssignPlanDialog no ofrece la opción 'Tarjeta' en el selector de tipo de precio"
    - "Con la regla OFF, PlanFormDialog esconde el campo de precio 'Tarjeta' (el valor persistido no se toca)"
  artifacts:
    - path: "el-templo-admin/src/pages/CobrosPage.vue"
      provides: "Consumo de ?memberId= (deep-link) + gate de getBasePriceFor por la regla"
    - path: "el-templo-admin/src/components/AssignPlanDialog.vue"
      provides: "Opción credit_card condicionada a la regla"
    - path: "el-templo-admin/src/components/PlanFormDialog.vue"
      provides: "Campo precio-tarjeta condicionado a la regla"
  key_links:
    - from: "el-templo-admin/src/pages/CobrosPage.vue"
      to: "route.query.memberId"
      via: "useRoute + onMounted → prefill selectedMember"
      pattern: "memberId"
    - from: "el-templo-admin/src/pages/CobrosPage.vue"
      to: "usePricingSettingsApi"
      via: "getCardSurchargeEnabled gate en getBasePriceFor"
      pattern: "getCardSurchargeEnabled"
---

<objective>
Cerrar el lado admin de ALUM-02 (deep-link de cobro) y ALUM-03 (gating de UI del recargo por tarjeta) en las tres superficies de precio-por-medio del admin: CobrosPage (consumo de `?memberId=` + gate de `getBasePriceFor`), AssignPlanDialog (opción `credit_card` condicional) y PlanFormDialog (campo precio-tarjeta condicional). El backend ya garantiza la regla (plan 02); acá la UI deja de ofrecer/aplicar el recargo cuando está OFF y se preselecciona el socio al llegar por query param.

Purpose: Sin el consumo de `?memberId=`, el botón de cobro en la fila (plan 05) navega a un wizard vacío. Sin gatear la UI, el admin sigue viendo "Tarjeta" aunque la regla esté OFF (inconsistente con el server, que ya lo normaliza).
Output: CobrosPage + AssignPlanDialog + PlanFormDialog gateados; deep-link funcional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/154-alumnos-de-templo-ficaci-n-accesos/154-CONTEXT.md
@.planning/phases/154-alumnos-de-templo-ficaci-n-accesos/154-PATTERNS.md

<interfaces>
<!-- Contratos existentes + del plan 03. -->

Del plan 03 (dependencia) — composable:
usePricingSettingsApi(): { getCardSurchargeEnabled(): Promise<boolean>, setCardSurchargeEnabled(enabled): Promise<void>, loading, error, cleanup }

CobrosPage.vue (a modificar):

- NO importa useRoute ni tiene onMounted hoy (imports 667-689; usa onBeforeRouteLeave)
- getBasePriceFor(plan, method, zero) líneas 1255-1258: if (method === 'card') return plan.priceCreditCard ?? plan.priceRegular; return zero ? plan.priceZero : plan.priceRegular;
- onUsarExistente() 1197-1211: patrón de prefill de selectedMember desde un id + loadAutocompletar(id)
- MemberSearchOption { id, displayLabel, statusLabel, statusColor } (703-708)
- onMemberSearch 1331-1350 (mapeo label/status sobre membersApi.searchMembers); loadAutocompletar 1383-1399 ($q.notify en error)
- wizard arranca en currentStep = 0 (712); el prefill debe entrar al paso Socio
  Analog deep-link: HorariosPage.vue:911-922 (onMounted + route.query)

AssignPlanDialog.vue:

- priceTypeOptions computed 1206-1218: agrega credit_card al array si selectedPlan.priceCreditCard != null
- resolver local getBasePrice() 1428-1438; submit manda priceTypeApplied (1593)

PlanFormDialog.vue:

- campo priceCreditCard q-input label "Tarjeta" líneas 98-107; form state 317/467/488/526 (NO se toca — D-04)
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: CobrosPage — consumir ?memberId= (ALUM-02) + gatear getBasePriceFor (ALUM-03)</name>
  <files>el-templo-admin/src/pages/CobrosPage.vue</files>
  <read_first>
    - el-templo-admin/src/pages/CobrosPage.vue (imports 667-689; getBasePriceFor 1255-1258; onUsarExistente 1197-1211; MemberSearchOption 703-708; onMemberSearch 1331-1350; loadAutocompletar 1383-1399; currentStep 712)
    - el-templo-admin/src/pages/HorariosPage.vue (911-922: analog de onMounted + route.query deep-link)
    - el-templo-admin/src/composables/usePricingSettingsApi.ts (del plan 03: getCardSurchargeEnabled)
    - .planning/phases/154-alumnos-de-templo-ficaci-n-accesos/154-PATTERNS.md (sección CobrosPage: prefill + gate, gotcha de imports faltantes)
    - ./CLAUDE.md (createLogger nunca console; no any; catch err unknown)
  </read_first>
  <action>
    Deep-link (ALUM-02, D-02): importar `useRoute` de vue-router (hoy NO está) y agregar un `onMounted`. En mount, leer `route.query.memberId`; si es un string no vacío, resolver el socio y preseleccionarlo replicando `onUsarExistente`: setear `selectedMember.value` con el shape `MemberSearchOption { id, displayLabel, statusLabel, statusColor }`, hacer `resetAltaFields()` si aplica, `void loadAutocompletar(id)`, y avanzar el wizard al paso Socio (setear `currentStep` al paso correspondiente, no dejarlo en la portada 0). Para resolver id→datos, reutilizar el mapeo de `onMemberSearch` sobre `membersApi.searchMembers` (o el fetch por id equivalente que ya use la página); si el `memberId` no existe o falla, `$q.notify` (mismo patrón que loadAutocompletar) y continuar en el flujo normal (no romper). Limpiar el query param no es obligatorio (discreción; si se hace, `router.replace` como CajaPage/DeudasPage).

    Gate de precio (ALUM-03, D-03/D-04): usar `usePricingSettingsApi` y cargar `getCardSurchargeEnabled()` en el mismo `onMounted` a un `ref<boolean>` (default conservador: si falla la carga, tratar como OFF para no aplicar recargo indebido — o ON para no romper; elegir OFF por seguridad de no cobrar de más, documentar). En `getBasePriceFor`, cuando la regla esté OFF, la rama `method === 'card'` debe caer a `zero ? plan.priceZero : plan.priceRegular` (o sea, ignorar `priceCreditCard`). Con la regla ON, comportamiento actual. NO cambiar la firma de `getBasePriceFor` si se puede leer el ref del scope; mantener la función pura respecto de sus args salvo el flag del scope.

    Nunca console.*; `createLogger('CobrosPage')` si hace falta; sin `any`; `catch (err: unknown)`.

  </action>
  <acceptance_criteria>
    - CobrosPage importa useRoute y tiene onMounted — `grep -c "useRoute" el-templo-admin/src/pages/CobrosPage.vue` >= 1 y `grep -c "onMounted" el-templo-admin/src/pages/CobrosPage.vue` >= 1
    - Consume el query param memberId — `grep -c "memberId" el-templo-admin/src/pages/CobrosPage.vue` >= 1
    - getBasePriceFor está gateado por la regla — `grep -c "getCardSurchargeEnabled\|cardSurcharge" el-templo-admin/src/pages/CobrosPage.vue` >= 1
    - El caso memberId inexistente usa $q.notify (no rompe) — hay una rama de error con notify en el bloque de mount
    - Sin `console.` ni `: any` nuevos — `grep -c "console\." el-templo-admin/src/pages/CobrosPage.vue` no aumenta respecto de HEAD
    - `cd el-templo-admin && pnpm lint` verde
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-admin && pnpm lint</automated>
  </verify>
  <done>CobrosPage preselecciona el socio al llegar por ?memberId= (con fallback a toast + flujo normal si no existe) y no aplica el precio de tarjeta cuando la regla está OFF; lint verde.</done>
</task>

<task type="auto">
  <name>Task 2: AssignPlanDialog (opción credit_card condicional) + PlanFormDialog (campo Tarjeta condicional)</name>
  <files>el-templo-admin/src/components/AssignPlanDialog.vue, el-templo-admin/src/components/PlanFormDialog.vue</files>
  <read_first>
    - el-templo-admin/src/components/AssignPlanDialog.vue (priceTypeOptions computed 1206-1218; getBasePrice local 1428-1438; submit priceTypeApplied 1593)
    - el-templo-admin/src/components/PlanFormDialog.vue (campo priceCreditCard 98-107; form state 317/467/488/526)
    - el-templo-admin/src/composables/usePricingSettingsApi.ts (del plan 03)
    - .planning/phases/154-alumnos-de-templo-ficaci-n-accesos/154-PATTERNS.md (secciones AssignPlanDialog + PlanFormDialog; hallazgo 2)
    - ./CLAUDE.md (no console, no any)
  </read_first>
  <action>
    AssignPlanDialog (D-04): cargar `getCardSurchargeEnabled()` (usePricingSettingsApi) a un `ref<boolean>` al montar el dialog (o al abrirse). En `priceTypeOptions` (computed 1206-1218), agregar `&& <reglaOn>` a la condición que empuja la opción `credit_card`, de modo que con la regla OFF la opción "Tarjeta" NO aparece. El resolver local `getBasePrice()` (1428-1438) debe también degradar `credit_card`→regular cuando la regla esté OFF, por consistencia con lo que muestra la UI (aunque el server ya lo normaliza — plan 02 — la UI no debe mostrar un monto con recargo). El default conservador ante fallo de carga = OFF (no ofrecer recargo). Sin `any`; no console.

    PlanFormDialog (D-05): cargar la regla igual (ref boolean). Envolver el `div.col-12.col-sm-4` del `q-input` de "Tarjeta" (98-107) con `v-if="<reglaOn>"` de modo que el campo se esconda cuando la regla esté OFF. El form state (`form.priceCreditCard`, líneas 317/467/488/526) queda INTACTO — solo se esconde el input, no se borra ni resetea el valor (D-04: las columnas priceRegular/priceCreditCard/priceZero se conservan).

  </action>
  <acceptance_criteria>
    - AssignPlanDialog condiciona la opción credit_card por la regla — `grep -c "getCardSurchargeEnabled\|cardSurcharge" el-templo-admin/src/components/AssignPlanDialog.vue` >= 1
    - PlanFormDialog esconde el campo Tarjeta por la regla — `grep -c "getCardSurchargeEnabled\|cardSurcharge" el-templo-admin/src/components/PlanFormDialog.vue` >= 1 y el q-input "Tarjeta" queda bajo un v-if
    - El form state priceCreditCard NO se borra ni resetea (solo se esconde el input) — el bloque de reset del form no toca priceCreditCard de forma nueva
    - Sin `console.` ni `: any` nuevos en ambos archivos
    - `cd el-templo-admin && pnpm lint` verde
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-admin && pnpm lint</automated>
  </verify>
  <done>AssignPlanDialog no ofrece "Tarjeta" y PlanFormDialog esconde el campo precio-tarjeta cuando la regla está OFF, sin tocar el estado/columnas persistidos; lint verde.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                                                                |
| ------------------------- | ------------------------------------------------------------------------------------------ |
| admin UI → API assignPlan | La UI esconde la opción tarjeta; la defensa real es la normalización server-side (plan 02) |
| deep-link ?memberId=      | Query param no confiable: un id inexistente/ajeno no debe romper ni exponer datos          |

## STRIDE Threat Register

| Threat ID | Category               | Component                          | Disposition | Mitigation Plan                                                                                            |
| --------- | ---------------------- | ---------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------- |
| T-154-09  | Tampering              | AssignPlanDialog priceTypeApplied  | mitigate    | UI esconde credit_card cuando OFF; el server ya normaliza (plan 02) aunque el cliente lo fuerce            |
| T-154-10  | Information Disclosure | ?memberId= arbitrario              | accept      | searchMembers ya está scoped por rol/sucursal server-side; id inexistente → toast + flujo normal, sin fuga |
| T-154-11  | Denial of Service      | memberId inválido rompe CobrosPage | mitigate    | fallback con $q.notify + continuar en flujo normal (no throw sin catch)                                    |
| T-154-SC  | Tampering              | npm installs                       | accept      | esta fase NO instala ni actualiza dependencias                                                             |

</threat_model>

<verification>
- `cd el-templo-admin && pnpm lint` verde
- /cobros?memberId= preselecciona socio; id inexistente → toast + flujo normal
- getBasePriceFor / AssignPlanDialog / PlanFormDialog respetan la regla OFF
- El estado persistido de priceCreditCard no se toca
</verification>

<success_criteria>
El deep-link de cobro funciona end-to-end (con el botón del plan 05) y las tres superficies de precio del admin dejan de ofrecer/aplicar el recargo por tarjeta cuando la regla está OFF, consistente con el server.
</success_criteria>

<output>
Create `.planning/phases/154-alumnos-de-templo-ficaci-n-accesos/154-04-SUMMARY.md` when done
</output>
