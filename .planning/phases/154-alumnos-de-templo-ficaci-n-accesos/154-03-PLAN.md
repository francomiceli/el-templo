---
phase: 154-alumnos-de-templo-ficaci-n-accesos
plan: 03
type: execute
wave: 2
depends_on: [154-01]
files_modified:
  - el-templo-admin/src/config/templo-config.ts
  - el-templo-admin/src/composables/usePricingSettingsApi.ts
  - el-templo-admin/src/pages/ConfiguracionPreciosPage.vue
  - el-templo-admin/src/router/routes.ts
autonomous: true
requirements: [ALUM-03, ALUM-05]
must_haves:
  truths:
    - "El owner ve en la categoría Configuración una entrada para las reglas de precio y puede prender/apagar el recargo por tarjeta"
    - "Un empleado (no owner) no ve la entrada ni puede entrar a la página"
    - "Existe un flag de superficie TEMPLO_GREEK_LEVELS que los consumidores (plan 05) usan para gatear los niveles griegos"
    - "El composable de settings expone lectura/escritura tipada del recargo por tarjeta para todo el admin"
  artifacts:
    - path: "el-templo-admin/src/config/templo-config.ts"
      provides: "Flag TEMPLO_GREEK_LEVELS + entrada de nav owner-only de config de precios"
      contains: "TEMPLO_GREEK_LEVELS"
    - path: "el-templo-admin/src/composables/usePricingSettingsApi.ts"
      provides: "getCardSurchargeEnabled / setCardSurchargeEnabled tipados"
    - path: "el-templo-admin/src/pages/ConfiguracionPreciosPage.vue"
      provides: "Toggle owner-only del recargo por tarjeta"
    - path: "el-templo-admin/src/router/routes.ts"
      provides: "Ruta owner-only a la página de config de precios"
  key_links:
    - from: "el-templo-admin/src/pages/ConfiguracionPreciosPage.vue"
      to: "/api/admin/settings/pricing/card-surcharge"
      via: "usePricingSettingsApi"
      pattern: "usePricingSettingsApi"
    - from: "el-templo-admin/src/config/templo-config.ts"
      to: "NAV_MODEL Configuración"
      via: "item roles ['owner'] path /configuracion/precios"
      pattern: "/configuracion/precios"
---

<objective>
Construir la superficie de configuración del admin para la regla de precio (ALUM-03, D-05) y declarar el flag de superficie de niveles griegos (ALUM-05, D-07/D-08) que consume el plan 05. Incluye: el composable tipado que lee/escribe la setting del plan 01, una página owner-only con un toggle, su ruta y entrada de nav, y el nuevo flag `TEMPLO_GREEK_LEVELS` en `templo-config.ts`.

Purpose: Este plan define contratos consumidos por los planes 04 (composable) y 05 (flag de niveles). Es "interface-first": los flags/composable existen antes de que los consumidores los usen.
Output: Página de config de precios + composable + ruta/nav + flag de niveles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/154-alumnos-de-templo-ficaci-n-accesos/154-CONTEXT.md
@.planning/phases/154-alumnos-de-templo-ficaci-n-accesos/154-PATTERNS.md

<interfaces>
<!-- Contratos existentes + del plan 01. -->

Del plan 01 (dependencia) — API:
GET /api/admin/settings/pricing/card-surcharge → { enabled: boolean } (staff)
PUT /api/admin/settings/pricing/card-surcharge body { enabled: boolean } → { enabled: boolean } (owner-only, 403 si no)

templo-config.ts (a extender):
export const TEMPLO_ENABLED = true; // línea 23, patrón del flag de superficie
interface NavItem { path; label; icon; roles; templo?: boolean } // líneas 88-98
NAV_MODEL categoría { header: 'Configuración', items: [ { path:'/usuarios', ..., roles:['owner'] }, { path:'/notificaciones', ... } ] } // líneas 143-154

routes.ts (analog usuarios líneas 197-201):
{ path: 'usuarios', component: () => import('pages/UsuariosPage.vue'), meta: { allowedRoles: ['owner'] as AdminRole[] } }

Composable analog — src/composables/useFinanceLoadApi.ts (refs loading/error, api de boot/axios, extractError, cleanup(), sin onUnmounted, sin console, sin any)
Notify/loading analog — AlumnosPage.vue onExport (749-783)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flag TEMPLO_GREEK_LEVELS + entrada de nav de config + composable de settings</name>
  <files>el-templo-admin/src/config/templo-config.ts, el-templo-admin/src/composables/usePricingSettingsApi.ts</files>
  <read_first>
    - el-templo-admin/src/config/templo-config.ts (TEMPLO_ENABLED línea 23; NavItem líneas 88-98; categoría Configuración 143-154; isNavItemVisible 197)
    - el-templo-admin/src/composables/useFinanceLoadApi.ts (patrón de composable: refs, api, extractError, cleanup, sin onUnmounted)
    - el-templo-admin/src/boot/axios.ts (export `api`)
    - el-templo-admin/src/utils/extract-error.ts (extractError)
    - ./CLAUDE.md (composables exponen cleanup y NO usan onUnmounted; createLogger nunca console; no any)
  </read_first>
  <action>
    En `templo-config.ts`: agregar `export const TEMPLO_GREEK_LEVELS = TEMPLO_ENABLED;` con un doc-comment breve (misma naturaleza que TEMPLO_ENABLED: gate de superficie por instalación, NO por usuario — D-08, no reusar canAccessTraining). Default El Templo = visible (true); un white-label nuevo lo pondría en false. Esta constante la consume el plan 05 (AlumnosPage/AlumnoDetailPage). Además, agregar a la categoría `Configuración` de `NAV_MODEL` (después del item `/usuarios`) un `NavItem`: `{ path: '/configuracion/precios', label: 'Reglas de precio', icon: 'sell', roles: ['owner'] }`. Respetar la regla de dirección de imports del header del archivo (este config importa desde types/utils, nadie del core lo importa).

    Crear `usePricingSettingsApi.ts` siguiendo el patrón de `useFinanceLoadApi`: refs `loading`/`error`, usa `api` de `src/boot/axios`, `extractError` para mensajes, `cleanup()` que resetea estado, SIN `onUnmounted`. Exponer `getCardSurchargeEnabled(): Promise<boolean>` (GET `/admin/settings/pricing/card-surcharge`, devuelve `data.enabled`) y `setCardSurchargeEnabled(enabled: boolean): Promise<void>` (PUT mismo path, body `{ enabled }`). Tipar la respuesta con una interface local `{ enabled: boolean }`. Nunca console.* (usar createLogger si hace falta loguear un error); sin `any`; `catch (err: unknown)` + `instanceof Error`.

  </action>
  <acceptance_criteria>
    - `TEMPLO_GREEK_LEVELS` exportado — `grep -c "export const TEMPLO_GREEK_LEVELS" el-templo-admin/src/config/templo-config.ts` == 1
    - Entrada de nav de config de precios owner-only — `grep -c "/configuracion/precios" el-templo-admin/src/config/templo-config.ts` >= 1
    - El composable expone ambas funciones — `grep -c "getCardSurchargeEnabled\|setCardSurchargeEnabled" el-templo-admin/src/composables/usePricingSettingsApi.ts` >= 2
    - Sin `onUnmounted` en el composable — `grep -c "onUnmounted" el-templo-admin/src/composables/usePricingSettingsApi.ts` == 0
    - Sin `console.` ni `: any` en los archivos nuevos — `grep -rn "console\.\|: any" el-templo-admin/src/composables/usePricingSettingsApi.ts` sin resultados
    - `cd el-templo-admin && pnpm lint` verde
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-admin && pnpm lint</automated>
  </verify>
  <done>Flag TEMPLO_GREEK_LEVELS + item de nav owner-only + composable tipado con cleanup, lint verde.</done>
</task>

<task type="auto">
  <name>Task 2: Página ConfiguracionPreciosPage (toggle owner-only) + ruta</name>
  <files>el-templo-admin/src/pages/ConfiguracionPreciosPage.vue, el-templo-admin/src/router/routes.ts</files>
  <read_first>
    - el-templo-admin/src/pages/UsuariosPage.vue (analog de page owner-only bajo Configuración: estructura de page, uso de composable, notify)
    - el-templo-admin/src/composables/usePricingSettingsApi.ts (creado en Task 1)
    - el-templo-admin/src/router/routes.ts (bloque usuarios 197-201: meta allowedRoles ['owner'] as AdminRole[])
    - el-templo-admin/src/pages/AlumnosPage.vue (líneas 749-783 onExport: patrón loading ref + $q.notify positivo/negativo; línea 342 createLogger)
    - ./CLAUDE.md (createLogger nunca console; no any)
  </read_first>
  <action>
    Crear `ConfiguracionPreciosPage.vue` (Quasar, Pinia composition/setup no requerido — es una page simple). Contenido: un `q-toggle` con label "Aplicar recargo por pago con tarjeta" (o similar) bindeado a un `ref<boolean>`. En `onMounted`, llamar `getCardSurchargeEnabled()` del composable para inicializar el toggle (con loading ref y manejo de error vía `$q.notify` + createLogger). Al cambiar el toggle (o con un botón "Guardar", a discreción), llamar `setCardSurchargeEnabled(value)` y notificar éxito/error con `$q.notify` (patrón de onExport de AlumnosPage). Copy en español. Explicar en un `q-banner`/caption breve que con la regla apagada todos los medios de pago cobran el precio regular (sin recargo por tarjeta). Usar `createLogger('ConfiguracionPreciosPage')` para errores; nunca console; sin `any`; `catch (err: unknown)`.

    En `routes.ts`, agregar un child route bajo el mismo layout que `usuarios`: `{ path: 'configuracion/precios', component: () => import('pages/ConfiguracionPreciosPage.vue'), meta: { allowedRoles: ['owner'] as AdminRole[] } }`. El path debe matchear el `/configuracion/precios` de la entrada de nav del Task 1.

  </action>
  <acceptance_criteria>
    - La page usa el composable — `grep -c "usePricingSettingsApi" el-templo-admin/src/pages/ConfiguracionPreciosPage.vue` >= 1
    - La page tiene un q-toggle — `grep -c "q-toggle" el-templo-admin/src/pages/ConfiguracionPreciosPage.vue` >= 1
    - La ruta existe y es owner-only — `grep -c "configuracion/precios" el-templo-admin/src/router/routes.ts` >= 1 y la línea vecina contiene `allowedRoles: ['owner']`
    - Sin `console.` ni `: any` — `grep -rn "console\.\|: any" el-templo-admin/src/pages/ConfiguracionPreciosPage.vue` sin resultados
    - `cd el-templo-admin && pnpm lint` verde
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-admin && pnpm lint</automated>
  </verify>
  <done>Página owner-only con toggle que lee/escribe la regla vía composable, ruta owner-only registrada, lint verde.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                | Description                                                                          |
| ----------------------- | ------------------------------------------------------------------------------------ |
| admin UI → API settings | La UI esconde/gatea, pero la seguridad real es el guard owner-only del PUT (plan 01) |

## STRIDE Threat Register

| Threat ID | Category               | Component                   | Disposition | Mitigation Plan                                                                                                                           |
| --------- | ---------------------- | --------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| T-154-07  | Elevation of Privilege | ruta /configuracion/precios | mitigate    | route meta allowedRoles ['owner'] + nav roles ['owner']; el PUT ya es owner-only server-side (plan 01) — la UI escondida no es la defensa |
| T-154-08  | Spoofing               | toggle refleja estado ajeno | accept      | el GET es staff-readable por diseño; el toggle solo lo puede persistir un owner (403 si no)                                               |
| T-154-SC  | Tampering              | npm installs                | accept      | esta fase NO instala ni actualiza dependencias (usa Quasar/axios existentes)                                                              |

</threat_model>

<verification>
- `cd el-templo-admin && pnpm lint` verde
- TEMPLO_GREEK_LEVELS exportado y consumible por el plan 05
- Item de nav + ruta owner-only para /configuracion/precios
- Composable lee/escribe la setting del plan 01
</verification>

<success_criteria>
El owner puede prender/apagar el recargo por tarjeta desde una página owner-only en Configuración; el flag de niveles griegos existe como contrato para el plan 05; el composable de settings está disponible para el plan 04.
</success_criteria>

<output>
Create `.planning/phases/154-alumnos-de-templo-ficaci-n-accesos/154-03-SUMMARY.md` when done
</output>
