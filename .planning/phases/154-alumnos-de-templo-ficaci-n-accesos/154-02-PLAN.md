---
phase: 154-alumnos-de-templo-ficaci-n-accesos
plan: 02
type: execute
wave: 2
depends_on: [154-01]
files_modified:
  - el-templo-api/src/modules/subscriptions/service.ts
  - el-templo-api/src/modules/members/routes.ts
  - el-templo-api/src/modules/members/schemas.ts
  - el-templo-api/test/finance/coach-load-pricing-gate.test.ts
autonomous: true
requirements: [ALUM-03, ALUM-05]
must_haves:
  truths:
    - "Con la regla OFF, un cobro por tarjeta (coach PoS) cobra priceRegular, no priceCreditCard"
    - "Con la regla OFF, un assignPlan del admin con priceTypeApplied 'credit_card' NO aplica recargo aunque el cliente lo mande"
    - "Con la regla ON, el recargo por tarjeta se aplica igual que hoy (comportamiento intacto)"
    - "El priceType persistido en la subscription queda normalizado a 'regular' cuando la regla está OFF (no queda 'credit_card' con monto regular)"
    - "El export Excel de Alumnos puede omitir la columna Nivel cuando el llamador lo pide (gating de niveles griegos consistente)"
  artifacts:
    - path: "el-templo-api/src/modules/subscriptions/service.ts"
      provides: "Normalización server-side de priceType credit_card→regular cuando la regla está OFF, en el punto único"
    - path: "el-templo-api/src/modules/members/routes.ts"
      provides: "Param includeGreekLevel en /export que condiciona la columna Nivel"
    - path: "el-templo-api/test/finance/coach-load-pricing-gate.test.ts"
      provides: "Cobertura del gate on/off en ambos flujos (coach PoS + admin assign)"
  key_links:
    - from: "el-templo-api/src/modules/subscriptions/service.ts"
      to: "PRICING_SETTINGS_KEYS.cardSurcharge"
      via: "import desde modules/settings/keys"
      pattern: "PRICING_SETTINGS_KEYS"
    - from: "el-templo-api/src/modules/members/routes.ts"
      to: "sheet.columns Nivel"
      via: "condicional includeGreekLevel"
      pattern: "includeGreekLevel"
---

<objective>
Cerrar el gate de precio server-side (ALUM-03, D-04): en el punto único de verdad (`subscriptions/service.ts`), cuando la regla `pricing.card_surcharge_enabled` está OFF, normalizar `priceType` `credit_card`→`regular` ANTES de calcular el monto y ANTES de persistirlo. Esto cubre los dos flujos que resuelven precio (coach PoS de la fase 148 y el `assignPlan` del admin desde AssignPlanDialog) con un solo cambio, cerrando el bypass del cliente. Además, gatear server-side la columna "Nivel" del export Excel de Alumnos (ALUM-05), que se arma en la API y no la ve el flag front-only de `templo-config.ts`.

Purpose: La UI puede esconder la opción de tarjeta (planes 03/04), pero el cliente puede mandar `priceTypeApplied: "credit_card"` igual (PATTERNS hallazgo 2). Sin el gate server-side, el recargo se aplica. La seguridad real vive en la API (149 D-04).
Output: Gate en `getBasePrice`/entry points + param de export + test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/154-alumnos-de-templo-ficaci-n-accesos/154-CONTEXT.md
@.planning/phases/154-alumnos-de-templo-ficaci-n-accesos/154-PATTERNS.md

<interfaces>
<!-- Contratos ya existentes en el codebase. -->

Del plan 01 (dependencia) — el-templo-api/src/modules/settings:
export const PRICING_SETTINGS_KEYS = { cardSurcharge: "pricing.card_surcharge_enabled" };
class SettingsService { getCardSurchargeEnabled(): Promise<boolean> } // default false = OFF

Punto único de precio — subscriptions/service.ts:4230-4239:
private getBasePrice(plan: PlanDetail, priceType: PriceType): number {
switch (priceType) { case "regular": ...; case "zero": ...; case "credit_card": return plan.priceCreditCard ?? plan.priceRegular; }
}
// Call sites de getBasePrice: 1085 (assignPlan), 2824 (changePlanNow), 3261 (changePlanAfterCurrent), 3915 (helper)
// priceTypeApplied se RESUELVE en: 1047 (assignPlan), 2807 (changePlanNow), 3229 (changePlanAfterCurrent)
// priceTypeApplied se PERSISTE en la subscription (líneas 1225, 2936, 3311)

Coach PoS (fase 148) — finance/coach-load-routes.ts:771-778:
const priceTypeApplied: PriceType = body.paymentMethod === "card" ? "credit_card" : body.zero ? "zero" : "regular";
// luego llama subscriptions.assignPlan(...) → resuelve monto en getBasePrice

Admin — subscriptions/schemas.ts:301-303: priceTypeApplied enum ["regular","zero","credit_card"] llega directo del cliente.

Export — members/routes.ts:249-273 (sheet.columns estático incl. { header: "Nivel", key: "nivel" }; for row of rows → sheet.addRow(row))
Export schema — members/schemas.ts:618 exportMembersSchema.querystring.properties
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate server-side del precio en el punto único (subscriptions/service.ts) + test on/off</name>
  <files>el-templo-api/src/modules/subscriptions/service.ts, el-templo-api/test/finance/coach-load-pricing-gate.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/subscriptions/service.ts (getBasePrice 4230-4239; resolución de priceTypeApplied en 1047/2807/3229; persistencia en 1225/2936/3311; constructor 189 para ver this.db/this.log)
    - el-templo-api/src/modules/settings/keys.ts (creado en plan 01: PRICING_SETTINGS_KEYS)
    - el-templo-api/src/modules/settings/service.ts (creado en plan 01: getCardSurchargeEnabled)
    - el-templo-api/src/modules/streaks/service.ts (273-293: patrón de read de system_settings con this.db si se prefiere inline en vez de instanciar SettingsService)
    - el-templo-api/src/modules/finance/coach-load-routes.ts (771-778: mapeo medio→priceType; body.paymentMethod)
    - el-templo-api/test/finance/coach-load-alta.test.ts (estructura del test del mismo endpoint)
    - el-templo-api/test/helpers.ts (cleanup de systemSettings línea 208 → sembrar la setting en cada caso; createTestApp, createStaffUser, registerUser, ensureEfectivoCaja)
    - .planning/phases/154-alumnos-de-templo-ficaci-n-accesos/154-PATTERNS.md (sección "API — gate server-side del precio")
    - ./CLAUDE.md (no console en API → request.log/this.log; no any; tests de integración obligatorios para rutas modificadas)
  </read_first>
  <behavior>
    - assignPlan con priceTypeApplied "credit_card" + regla OFF → monto cobrado = priceRegular Y priceType persistido = "regular"
    - assignPlan con priceTypeApplied "credit_card" + regla ON → monto = priceCreditCard (comportamiento actual)
    - Coach PoS (paymentMethod "card") + regla OFF → alta cobra priceRegular
    - Coach PoS (paymentMethod "card") + regla ON → alta cobra priceCreditCard
    - priceType "zero" y "regular" no se ven afectados por la regla en ningún caso
  </behavior>
  <action>
    Agregar un helper privado async `resolvePriceType(priceType: PriceType): Promise<PriceType>` en `SubscriptionService` que lea la regla REUSANDO el servicio del plan 154-01: importar `SettingsService` desde `../settings/service` (o el método/función expuesta) e invocar `getCardSurchargeEnabled()` — NO reimplementar el select inline (DRY: un solo punto de lectura de la key en todo el backend; el patrón de streaks queda solo como referencia histórica). Si `priceType === "credit_card"` y la regla está OFF, devolver `"regular"`; en cualquier otro caso devolver el `priceType` recibido sin cambios. NO reimplementar `getBasePrice` (queda igual: sigue siendo el switch puro).

    En los TRES puntos donde se resuelve `priceTypeApplied` antes de `getBasePrice` (assignPlan ~1047, changePlanNow ~2807, changePlanAfterCurrent ~3229), reemplazar el valor por `await this.resolvePriceType(<valorActual>)` de modo que la variable normalizada se use TANTO para `getBasePrice` COMO para lo que se persiste en la subscription (columnas priceTypeApplied 1225/2936/3311). Así, con la regla OFF, nunca queda grabado `credit_card` con monto regular (D-04, evitar el drift persistido que marca PATTERNS). Preferir el helper por sobre gatear dentro de `getBasePrice` (que es sync y se llama en 4 sitios) — normalizar en la resolución del priceType es el punto correcto.

    Esto cubre automáticamente el coach PoS de la fase 148 (coach-load-routes mapea a `credit_card` y llama `assignPlan`) y el admin (que manda `priceTypeApplied` directo). NO tocar coach-load-routes.ts (su mapeo queda; la normalización sucede aguas abajo en assignPlan). Nunca console.*; usar this.log si hace falta loguear. Sin `any`.

    Test `test/finance/coach-load-pricing-gate.test.ts` (Vitest, estructura de coach-load-alta.test.ts): sembrar la setting en cada caso (recordar cleanup de systemSettings). Casos: (a) alta coach `paymentMethod:"card"` con regla ON → monto = priceCreditCard del plan; (b) mismo con regla OFF → monto = priceRegular; (c) verificar que el priceTypeApplied persistido en la subscription resultante es "regular" cuando OFF. Si es más directo, testear `assignPlan` con `priceTypeApplied:"credit_card"` regla OFF vía el endpoint admin correspondiente. Reusar `ensureEfectivoCaja`/`createStaffUser`/`registerUser`.

  </action>
  <acceptance_criteria>
    - `resolvePriceType` existe y referencia la key canónica — `grep -c "PRICING_SETTINGS_KEYS" el-templo-api/src/modules/subscriptions/service.ts` >= 1 y `grep -c "resolvePriceType" el-templo-api/src/modules/subscriptions/service.ts` >= 4 (definición + 3 call sites)
    - No se reimplementó getBasePrice como async (sigue sync) — el switch de getBasePrice queda intacto
    - Sin `console.` agregado — `grep -c "console\." el-templo-api/src/modules/subscriptions/service.ts` no aumenta respecto de HEAD
    - El test cubre ON→priceCreditCard y OFF→priceRegular y priceType persistido "regular" cuando OFF — grep en el test contiene `priceCreditCard`/`priceRegular` y `regular`
    - `cd el-templo-api && pnpm exec tsc --noEmit` verde
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>El gate normaliza credit_card→regular cuando la regla está OFF en el punto único, cubriendo coach PoS + admin, con el priceType persistido consistente; test on/off verde en CI; tsc verde.</done>
</task>

<task type="auto">
  <name>Task 2: Gate server-side de la columna Nivel en el export Excel (ALUM-05)</name>
  <files>el-templo-api/src/modules/members/routes.ts, el-templo-api/src/modules/members/schemas.ts</files>
  <read_first>
    - el-templo-api/src/modules/members/routes.ts (líneas 210-286: querystring del export, exportMembers call, sheet.columns con "Nivel" línea 255, loop addRow 272-273)
    - el-templo-api/src/modules/members/schemas.ts (exportMembersSchema línea 618: querystring.properties)
    - .planning/phases/154-alumnos-de-templo-ficaci-n-accesos/154-PATTERNS.md (hallazgo 5: la columna Nivel se arma server-side; decisión query param desde el admin)
    - .planning/phases/154-alumnos-de-templo-ficaci-n-accesos/154-CONTEXT.md (discreción: gatear también la columna del export = recomendado sí, por consistencia)
  </read_first>
  <action>
    Agregar un query param `includeGreekLevel` (boolean) al export. En `exportMembersSchema.querystring.properties` (schemas.ts:618) agregar `includeGreekLevel: { type: "boolean" }`. En el handler del export (routes.ts) leer `request.query.includeGreekLevel`; tratar `undefined` como `true` (default = incluir, preserva el comportamiento actual de El Templo y de cualquier llamador que no pase el param). Construir `sheet.columns` condicionalmente: cuando `includeGreekLevel === false`, OMITIR el objeto `{ header: "Nivel", key: "nivel", width: 12 }` del array (exceljs ignora la key `nivel` de cada row si no hay columna con ese key, así que el loop `addRow(row)` no necesita cambios). Cuando es true, dejar el array como está hoy. Tipar el query param en el `Querystring` genérico del `fastify.get` del export. Sin `any`; logging con request.log si hiciera falta.

    NOTA: la fuente de verdad del flag es front-only (`templo-config.ts` `TEMPLO_GREEK_LEVELS`, plan 03). El admin pasa `includeGreekLevel=<valor del flag>` desde AlumnosPage en el plan 05; este endpoint solo respeta el param. NO duplicar una constante Templo en la API (evitar Templo-ismos nuevos en core, constraint SaaS).

  </action>
  <acceptance_criteria>
    - `includeGreekLevel` está en el schema del export — `grep -c "includeGreekLevel" el-templo-api/src/modules/members/schemas.ts` >= 1
    - El handler condiciona la columna Nivel por el param — `grep -c "includeGreekLevel" el-templo-api/src/modules/members/routes.ts` >= 1
    - Existe un test de integración del export (en `el-templo-api/test/members/` o suite existente de members) con 3 casos: `includeGreekLevel=false` → el workbook NO contiene el header "Nivel"; `includeGreekLevel=true` → lo contiene; sin el param → lo contiene (default preservado). El test parsea el buffer xlsx de la respuesta (exceljs `workbook.xlsx.load`) y asserta sobre `sheet.columns`/headers. NO se ejecuta localmente (corre en CI); el archivo debe compilar bajo tsc.
    - Default sin el param = columna presente (retrocompatible): la lógica trata undefined como true
    - No hay constante Templo hardcodeada nueva en la API para niveles — `grep -in "greek" el-templo-api/src/modules/members/routes.ts` solo aparece en el nombre del param, no en una lista de valores Templo
    - `cd el-templo-api && pnpm exec tsc --noEmit` verde
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>El export omite la columna Nivel cuando el llamador pasa includeGreekLevel=false; default (sin param) mantiene la columna; tsc verde.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                                      |
| ------------------------------ | -------------------------------------------------------------------------------- |
| admin cliente → API assignPlan | El cliente manda priceTypeApplied; no se debe confiar en él para aplicar recargo |
| coach PoS → API                | El profe carga cobros; el monto debe recalcularse server-side                    |

## STRIDE Threat Register

| Threat ID | Category               | Component                             | Disposition | Mitigation Plan                                                                                                        |
| --------- | ---------------------- | ------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------- |
| T-154-04  | Tampering              | assignPlan priceTypeApplied (cliente) | mitigate    | normalización server-side credit_card→regular cuando la regla OFF, en el punto único; test admin OFF no aplica recargo |
| T-154-05  | Tampering              | precio persistido en subscription     | mitigate    | normalizar el priceType ANTES de persistir (no queda credit_card con monto regular)                                    |
| T-154-06  | Information Disclosure | export Excel columna Nivel            | accept      | gating de superficie (white-label), no es dato sensible; el param solo omite una columna                               |
| T-154-SC  | Tampering              | npm installs                          | accept      | esta fase NO instala ni actualiza dependencias                                                                         |

</threat_model>

<verification>
- `cd el-templo-api && pnpm exec tsc --noEmit` verde
- resolvePriceType aplicado en assignPlan + changePlanNow + changePlanAfterCurrent
- Test coach-load pricing gate cubre ON/OFF y priceType persistido
- Export respeta includeGreekLevel con default true
</verification>

<success_criteria>
El recargo por tarjeta deja de aplicarse cuando la regla está OFF, en el único punto server-side, cubriendo coach PoS y admin y sin dejar priceType inconsistente persistido. La columna Nivel del export se puede gatear desde el llamador. La regla ON mantiene el comportamiento actual de El Templo.
</success_criteria>

<output>
Create `.planning/phases/154-alumnos-de-templo-ficaci-n-accesos/154-02-SUMMARY.md` when done
</output>
