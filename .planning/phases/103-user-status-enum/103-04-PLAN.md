---
phase: 103
plan: 04
type: execute
wave: 4
depends_on: [103-01, 103-02, 103-03]
files_modified:
  - el-templo-api/src/modules/members/service.ts
  - el-templo-api/src/modules/members/schemas.ts
  - el-templo-api/src/modules/members/types.ts
  - el-templo-api/src/modules/analytics/service.ts
  - el-templo-admin/src/types/member.ts
  - el-templo-admin/src/composables/useMembersApi.ts
  - el-templo-admin/src/components/SlotAttendancePanel.vue
  - el-templo-api/test/members/members-status-filter.test.ts
autonomous: true
requirements: [R8, R10]
must_haves:
  truths:
    - "GET /api/admin/members?status=freemium returns only users with users.status='freemium' (same for prueba/activo/inactivo)"
    - "API response payload includes status field (not isActive)"
    - "members/service.ts no longer uses isActiveSubquery for users — reads users.status directly"
    - "members/service.ts createMember insert sets status='prueba' explicitly (single-owner of this file's status edits)"
    - "analytics/service.ts countActiveMembers uses status='activo' instead of isActive=true"
    - "members export ('Estado' column) shows the 4-value label (Freemium/En Prueba/Activo/Inactivo) not the old 2-value (Activo/Inactivo)"
    - "Admin types (MemberListItem, MemberProfile, MemberListParams) replace isActive with status"
    - "SlotAttendancePanel reads status (not isActive) for member badge in search results"
  artifacts:
    - path: "el-templo-api/src/modules/members/service.ts"
      provides: "List/detail/export queries read users.status; createMember sets status='prueba'"
    - path: "el-templo-api/src/modules/members/schemas.ts"
      provides: "JSON schema with 5-value status enum filter and status in response"
    - path: "el-templo-admin/src/types/member.ts"
      provides: "TypeScript types updated"
    - path: "el-templo-admin/src/composables/useMembersApi.ts"
      provides: "Composable params/response use status (not isActive)"
  key_links:
    - from: "members/service.ts listMembers/getMemberProfile"
      to: "users.status column (Plan 01)"
      via: "Direct SELECT users.status — no subquery derivation"
      pattern: "users\\.status"
    - from: "members/service.ts createMember insert (planId path)"
      to: "subscriptions/service.ts assignPlan (Plan 02 wraps in tx + recomputeUserStatus)"
      via: "createMember writes status='prueba', then assignPlan flips to 'activo' inside Plan 02's tx"
      pattern: "status: 'prueba'"
    - from: "el-templo-admin SlotAttendancePanel"
      to: "members API status field"
      via: "Read m.status === 'activo' instead of m.isActive"
      pattern: "status === 'activo'"
---

<objective>
Migrate the Members API from the derived `isActiveSubquery` model to first-class `users.status` reads (R8, R10) and take single-owner responsibility for ALL status edits in `members/service.ts`, including the `createMember` insert (status='prueba' per R7/D-12). Update the JSON schema enum from `['todos','alumnos','leads']` to `['todos','freemium','prueba','activo','inactivo']`. Update API response payload to include `status` (drop `isActive`). Update admin TypeScript types, composables, and the hidden `SlotAttendancePanel` consumer. Migrate `analytics/service.ts:205` to `users.status='activo'`. Migrate the export 'Estado' column to surface the 4-value label.

Purpose: Replace runtime derivation with stored fact. Enables Plan 05 (AlumnosPage UI) by exposing `status` in the API response. Owning the createMember insert here keeps `members/service.ts` mutated by exactly one plan in the wave structure (avoids file-conflict per BLOCKER 3).

Output: API contract migrated, createMember writes status='prueba', admin types/composables/components migrated, integration tests prove all 4 enum filter values + status presence in response.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/103-user-status-enum/103-SPEC.md
@.planning/phases/103-user-status-enum/103-CONTEXT.md
@.planning/phases/103-user-status-enum/103-RESEARCH.md
@el-templo-api/src/modules/members/service.ts
@el-templo-api/src/modules/members/schemas.ts
@el-templo-api/src/modules/members/types.ts
@el-templo-api/src/modules/analytics/service.ts
@el-templo-admin/src/types/member.ts
@el-templo-admin/src/composables/useMembersApi.ts
@el-templo-admin/src/components/SlotAttendancePanel.vue
@el-templo-api/test/members/members-leads-filter.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate API: members/service.ts (incl. createMember status='prueba'), schemas.ts, types.ts, analytics/service.ts</name>
  <files>el-templo-api/src/modules/members/service.ts, el-templo-api/src/modules/members/schemas.ts, el-templo-api/src/modules/members/types.ts, el-templo-api/src/modules/analytics/service.ts</files>
  <read_first>
    - el-templo-api/src/modules/members/service.ts FULL FILE — focus on lines 53, 114-124, 196-220, 247-294, 348, 366-373, 404, 434, 473, 545, 591, 656, 709-718, 775-793, 812 (per RESEARCH §4)
    - el-templo-api/src/modules/members/schemas.ts lines 27, 33, 80, 111, 130, 147, 320 (per RESEARCH §4)
    - el-templo-api/src/modules/members/types.ts (full file — small)
    - el-templo-api/src/modules/analytics/service.ts:205 (single hidden reference to users.isActive)
    - .planning/phases/103-user-status-enum/103-SPEC.md R7, R8, R10 acceptance lines (69-91)
    - .planning/phases/103-user-status-enum/103-CONTEXT.md D-09, D-11, D-12 (D-12: createMember writes status='prueba')
    - .planning/phases/103-user-status-enum/103-RESEARCH.md §4 (Members API Migration Map — exhaustive line list)
  </read_first>
  <behavior>
    - `listMembers` accepts `status: 'todos' | 'freemium' | 'prueba' | 'activo' | 'inactivo'` (5 values, default 'todos').
    - When `status !== 'todos'`, the SQL filter is `eq(users.status, value)` — direct column read, no `isActiveSubquery`.
    - The legacy `'alumnos'` and `'leads'` values are NO LONGER accepted (admin app updated in Task 2 of this plan to use the new enum).
    - `getMemberProfile` returns `status` field in the response (string enum), drops `isActive`.
    - The `isActiveSubquery` helper used to project `isActive: bool` in list/detail responses → replace with direct projection of `users.status`.
    - **`createMember` insert (~line 473): adds `status: 'prueba' as const` to the values map** (per R7/D-12 — admin enrolling someone who walked into a sede). Drop any legacy `isActive: true` field. If the same handler also calls `subscriptionService.assignPlan` when planId is provided, Plan 02's recomputeUserStatus flips status to 'activo' inside that tx — no extra wiring needed in createMember itself.
    - `softDeleteMember` no longer sets `isActive: false` — only writes `deletedAt`. Read site already filters by `deletedAt IS NULL`.
    - `exportMembers`: the 'Estado' column now reads from `r.status` and maps to one of `Freemium / En Prueba / Activo / Inactivo` (D-09 labels).
    - `analytics/service.ts:205` `eq(schema.users.isActive, true)` → `eq(schema.users.status, 'activo')`.
    - JSON schema query enum extended from 3 to 5 values; response schema swaps `isActive: boolean` → `status: { enum: [...4 values] }`.
    - TypeScript types: `MemberListItem.isActive` removed, `status` added; `MemberListParams.isActive` removed, `status` extended to 5-value enum; `MemberProfile.isActive` removed, `status` added.
  </behavior>
  <action>
    **Part A — `el-templo-api/src/modules/members/service.ts`:**
    1. Find `isActiveSubquery` definition (line ~115 per RESEARCH; the comment "Derive active status from subscriptions (not the stale users.is_active column)"). DELETE the subquery declaration and ALL its usages.
    2. In every `listMembers` / `getMemberProfile` / equivalent select, replace projection `isActive: isActiveSubquery` (or similar) with `status: users.status`.
    3. Replace the filter logic in `listMembers` (around lines 196-220):
       - OLD: `status === 'leads' ? notExistsActiveSubAndExistsTrialBooking : status === 'alumnos' ? existsActiveSub : undefined` plus `isActive` boolean filter
       - NEW: `status === 'todos' ? undefined : eq(users.status, status)` (single condition for the 4 enum values)
    4. **`createMember` (~line 473) — single-owner status='prueba' edit (BLOCKER 3 fix):**
       The insert that today writes the new user — replace any `isActive: true` field with `status: 'prueba' as const` as a literal string value. Example shape:
       ```ts
       // OLD
       await this.db.insert(users).values({
         email,
         /* ... */,
         isActive: true,
       });
       // NEW
       await this.db.insert(users).values({
         email,
         /* ... */,
         status: 'prueba' as const,
       });
       ```
       Acceptance: `grep "status: 'prueba'" members/service.ts` returns 1+ match.
       Note: Plan 03 explicitly does NOT touch this file — this plan owns ALL members/service.ts edits per the wave-conflict resolution.
    5. In `softDeleteMember` (~line 591), confirm `isActive: false` is gone from the UPDATE (only `deletedAt: now`).
    6. In `exportMembers` (~line 812), the 'Estado' column code likely reads `r.isActive ? "Activo" : "Inactivo"`. Replace with a small map:
       ```ts
       const STATUS_LABELS: Record<string, string> = {
         freemium: 'Freemium', prueba: 'En Prueba', activo: 'Activo', inactivo: 'Inactivo',
       };
       // ...
       estado: r.status ? STATUS_LABELS[r.status] : '',
       ```
    7. Run `grep -n "isActive" el-templo-api/src/modules/members/service.ts`. Expected: 0 matches that refer to `users.isActive`. (Matches like `subscription.isActive` or `plan.isActive` are unrelated — verify each.)

    **Part B — `el-templo-api/src/modules/members/schemas.ts`:**
    1. Locate the query schema for the list endpoint (~line 147). Old enum: `['todos', 'alumnos', 'leads']`. Replace with `['todos', 'freemium', 'prueba', 'activo', 'inactivo']`.
    2. Locate response schemas (~lines 33, 80, 130, 320 per RESEARCH §4). For each:
       - Remove the `isActive: { type: 'boolean' }` property (and from required array if listed).
       - Add `status: { type: 'string', enum: ['freemium','prueba','activo','inactivo'], nullable: true }` (nullable because staff rows have NULL — though staff don't appear in member list, the type matches the column).

    **Part C — `el-templo-api/src/modules/members/types.ts`:**
    1. `MemberListItem`: remove `isActive: boolean`; add `status: 'freemium' | 'prueba' | 'activo' | 'inactivo' | null`.
    2. `MemberProfile`: same change.
    3. `MemberListParams`: remove `isActive?: boolean`; replace `status?: 'todos' | 'alumnos' | 'leads'` with `status?: 'todos' | 'freemium' | 'prueba' | 'activo' | 'inactivo'`.

    **Part D — `el-templo-api/src/modules/analytics/service.ts:205`:**
    1. Locate the line `eq(schema.users.isActive, true)` (or similar). It's used in `countActiveMembers` per RESEARCH §1.
    2. Replace with `eq(schema.users.status, 'activo')`.
    3. `grep -n "users.isActive\\|users\\.is_active" el-templo-api/src/modules/analytics/`. Expected: 0 matches.

    **Part E — Verify TS compiles:**
    ```bash
    cd el-templo-api && pnpm tsc --noEmit
    ```

  </action>
  <verify>
    <automated>cd el-templo-api && pnpm tsc --noEmit &amp;&amp; ! grep -rn "isActiveSubquery" src/modules/members/ &amp;&amp; ! grep -rn "users\\.isActive\\|users\\.is_active" src/modules/members/ src/modules/analytics/ &amp;&amp; grep -n "status: 'prueba'" src/modules/members/service.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "isActiveSubquery" el-templo-api/src/modules/members/` returns 0 matches
    - `grep -rn "users\\.isActive\\|users\\.is_active" el-templo-api/src/modules/members/ el-templo-api/src/modules/analytics/` returns 0 matches
    - `grep -n "status: 'prueba'" el-templo-api/src/modules/members/service.ts` returns 1+ matches (createMember insert)
    - `grep -n "'freemium'\\|'prueba'\\|'activo'\\|'inactivo'" el-templo-api/src/modules/members/schemas.ts` returns 4+ matches in the schema enum
    - `grep -n "isActive" el-templo-api/src/modules/members/types.ts` returns 0 matches
    - `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Members API and analytics read users.status directly; createMember writes status='prueba'; isActiveSubquery is gone; TypeScript compiles cleanly.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate admin types/composables + SlotAttendancePanel + add API integration tests</name>
  <files>el-templo-admin/src/types/member.ts, el-templo-admin/src/composables/useMembersApi.ts, el-templo-admin/src/components/SlotAttendancePanel.vue, el-templo-api/test/members/members-status-filter.test.ts</files>
  <read_first>
    - el-templo-admin/src/types/member.ts FULL FILE
    - el-templo-admin/src/composables/useMembersApi.ts (focus on getMembers, getMember, exportMembers — params and return types)
    - el-templo-admin/src/components/SlotAttendancePanel.vue:310-330 (search result badge — `m.isActive` reference at line ~318)
    - el-templo-admin/src/composables/useMembersApi.ts:235 (this is `plans.isActive` per RESEARCH §4 — DO NOT MIGRATE; verify by reading)
    - el-templo-api/test/members/members-leads-filter.test.ts FULL FILE (closest analog — extend pattern from 3 to 5 enum values)
    - .planning/phases/103-user-status-enum/103-SPEC.md R8, R10
    - .planning/phases/103-user-status-enum/103-RESEARCH.md §4 (admin migration map) and §6 (test patterns)
  </read_first>
  <behavior>
    - Admin TypeScript types (`types/member.ts`) mirror the API contract: `MemberListItem.status`, `MemberProfile.status`, `MemberListParams.status` extended to 5 values.
    - `useMembersApi.ts` getMembers param type = `{ status?: 'todos' | 'freemium' | 'prueba' | 'activo' | 'inactivo' }` and the response type has `status` (no `isActive`).
    - `SlotAttendancePanel.vue:318` reads `m.status === 'activo'` instead of `m.isActive` for the "Activa/Inactiva" badge in member search results.
    - **NOT TOUCHED:** `composables/useMembersApi.ts:235` (which references `plans.isActive` per RESEARCH §4 — confirmed false-alarm).
    - **NOT TOUCHED:** `pages/PlanesPage.vue`, `pages/ProgramasPage.vue` (those are `plan.isActive` / `program.isActive`, different models).
    - Integration tests cover: all 5 enum filter values return correct counts; response includes `status` not `isActive`.
  </behavior>
  <action>
    **Part A — `el-templo-admin/src/types/member.ts`:**
    1. Around line 55: `MemberListItem` — remove `isActive: boolean`, add `status: 'freemium' | 'prueba' | 'activo' | 'inactivo' | null`.
    2. Around line 145: `MemberListParams` — remove `isActive?: boolean`, add `status?: 'todos' | 'freemium' | 'prueba' | 'activo' | 'inactivo'`.
    3. Around line 151: replace existing `status?: 'todos' | 'alumnos' | 'leads'` line with the 5-value enum (it may overlap with the change in step 2 — consolidate).
    4. `MemberProfile` interface (find by `interface MemberProfile`): remove `isActive`, add `status`.

    **Part B — `el-templo-admin/src/composables/useMembersApi.ts`:**
    1. Find `getMembers` and `exportMembers` functions. Their params type comes from `MemberListParams` (Part A) — no manual change needed if the import is already there. Verify by reading.
    2. Find any explicit `isActive` references in the file outside of line 235 (which is plans-related per RESEARCH — leave alone). Replace with `status`.
    3. The PATCH-style member update functions (if any take `isActive` in their payload) — read carefully. R7's locked decision is auto-status, not manual override; member edit endpoints should NOT accept `status` from the client. If existing edit endpoints had `isActive`, that field should be REMOVED from the type/payload.

    **Part C — `el-templo-admin/src/components/SlotAttendancePanel.vue`:**
    1. Line ~318: change `m.isActive` to `m.status === 'activo'` in the badge condition. The label text ('Activa'/'Inactiva') stays the same — it's a degraded representation but acceptable per RESEARCH §1.
    2. If the search results API (`searchMembers` or similar) doesn't currently return `status`, verify the API response shape. If the panel calls `useMembersApi.getMembers`, then Task 1 already exposed `status` and this works.

    **Part D — Integration test `el-templo-api/test/members/members-status-filter.test.ts`:**
    Create a new test file. Test cases:
    1. `?status=freemium` returns only freemium users; count matches `SELECT COUNT(*) FROM users WHERE status='freemium' AND deleted_at IS NULL`
    2. `?status=prueba` — same shape
    3. `?status=activo` — same shape
    4. `?status=inactivo` — same shape
    5. `?status=todos` (or no status param) returns all members regardless of status
    6. Response payload for each member includes `status` field; no `isActive` key in the JSON body

    Use existing fixture pattern from `members-leads-filter.test.ts:1-80`.

    Skeleton:
    ```ts
    import { describe, it, expect, beforeEach, afterAll } from 'vitest';
    import { createTestApp, cleanAllTestData, getAuthToken } from '../helpers';
    import { eq, and, isNull, count } from 'drizzle-orm';
    import * as schema from '../../src/db/schema';
    import argon2 from 'argon2';

    describe('Phase 103 — GET /api/admin/members ?status filter (R8, R10)', () => {
      let app: Awaited<ReturnType<typeof createTestApp>>;
      let adminToken: string;

      beforeEach(async () => {
        app = await createTestApp();
        await cleanAllTestData(app);
        adminToken = await getAuthToken(app, /* admin creds */);
        // Seed: 1 user per status value (freemium, prueba, activo, inactivo)
      });

      afterAll(async () => app.close());

      const STATUSES = ['freemium', 'prueba', 'activo', 'inactivo'] as const;
      for (const s of STATUSES) {
        it(`?status=${s} returns only users with status=${s}`, async () => {
          const res = await app.inject({
            method: 'GET', url: `/api/admin/members?status=${s}`,
            headers: { Authorization: `Bearer ${adminToken}` },
          });
          expect(res.statusCode).toBe(200);
          const body = res.json();
          for (const row of body.rows) {
            expect(row.status).toBe(s);
            expect(row).not.toHaveProperty('isActive');
          }
          const [{ value }] = await app.db
            .select({ value: count() })
            .from(schema.users)
            .where(and(eq(schema.users.status, s), isNull(schema.users.deletedAt)));
          expect(body.total).toBe(value);
        });
      }

      it('?status=todos returns all members regardless of status', async () => { /* ... */ });
      it('Response payload includes status field, not isActive', async () => { /* ... */ });
    });
    ```

  </action>
  <verify>
    <automated>cd el-templo-api && pnpm test test/members/members-status-filter.test.ts &amp;&amp; cd ../el-templo-admin && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "isActive" el-templo-admin/src/types/member.ts` returns 0 matches (only `status` remains)
    - `grep -n "m\\.isActive" el-templo-admin/src/components/SlotAttendancePanel.vue` returns 0 matches
    - `grep -n "status === 'activo'" el-templo-admin/src/components/SlotAttendancePanel.vue` returns 1+ match
    - `cd el-templo-admin && pnpm tsc --noEmit` exits 0
    - Integration test file exists and `pnpm test test/members/members-status-filter.test.ts` exits 0 with all 4+ enum cases passing
    - `cd el-templo-admin && grep -rn "members?.isActive\\|memberProfile?.isActive\\|m\\.isActive" src/` returns 0 matches outside of this plan's expected files (Plan 05 will handle AlumnosPage/AlumnoDetailPage)
  </acceptance_criteria>
  <done>
    Admin types and the SlotAttendancePanel hidden consumer migrated; API contract proven by integration tests; admin TS compiles clean.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                                      |
| ------------------------------ | -------------------------------------------------------------------------------- |
| admin client→API list endpoint | admin auth gate upstream; status filter values are validated by JSON schema enum |

## STRIDE Threat Register

| Threat ID | Category               | Component                                 | Disposition | Mitigation Plan                                                                         |
| --------- | ---------------------- | ----------------------------------------- | ----------- | --------------------------------------------------------------------------------------- |
| T-103-06  | Information Disclosure | API status field exposes commercial state | accept      | Already exposed in admin-only endpoints; admin role required upstream                   |
| T-103-07  | Tampering              | client-supplied status filter             | mitigate    | JSON schema enum validates the 5 allowed values; unknown values rejected at route entry |

</threat_model>

<verification>
- API filter accepts 5 values, rejects unknown
- API response includes status, drops isActive
- createMember writes status='prueba' (single-owner of members/service.ts edits)
- Hidden refs (analytics, SlotAttendancePanel) migrated
- Admin types align with API contract
- Both API and admin compile cleanly
</verification>

<success_criteria>

- [ ] members/service.ts reads users.status directly (no isActiveSubquery)
- [ ] members/service.ts createMember writes status='prueba'
- [ ] schemas.ts query enum has 5 values; response schema has status not isActive
- [ ] types.ts (API) and types/member.ts (admin) aligned
- [ ] analytics/service.ts:205 migrated
- [ ] SlotAttendancePanel migrated
- [ ] members export 'Estado' column shows 4-value labels
- [ ] Integration tests prove all 4 enum filter values
- [ ] Both apps compile cleanly
      </success_criteria>

<output>
After completion, create `.planning/phases/103-user-status-enum/103-04-SUMMARY.md` summarizing:
- All grep results post-migration (no isActive in members/analytics modules; admin types clean)
- Confirmation that createMember insert now contains `status: 'prueba'`
- Whether you extended `members-leads-filter.test.ts` or created a fresh `members-status-filter.test.ts`
- Confirmation that SlotAttendancePanel's data source returns `status`
- Note any consumers found during execution that weren't in the original RESEARCH map (return as discovery for Plan 07 grep gate to verify)
</output>
</output>
