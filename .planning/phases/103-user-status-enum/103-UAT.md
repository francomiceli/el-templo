---
status: complete
phase: 103-user-status-enum
source:
  - 103-01-SUMMARY.md
  - 103-02-SUMMARY.md
  - 103-03-SUMMARY.md
  - 103-04-SUMMARY.md
  - 103-05-SUMMARY.md
  - 103-06-SUMMARY.md
  - 103-07-SUMMARY.md
started: 2026-04-25T16:00:00Z
updated: 2026-04-25T20:00:00Z
---

## Current Test

All 8 tests passed. UAT complete.

## Tests

### 1. Cold start smoke

expected: |
Kill API + admin processes. Start fresh: `cd el-templo-api && pnpm dev` and `cd el-templo-admin && pnpm dev`.
Acceptance: API boots without errors; migration runner reports `0100_user_status_enum` already applied (or applies clean if running against a fresh DB); admin login succeeds; AlumnosPage loads.
result: pass

### 2. Self-register lands as `freemium`

expected: |
Create a new user via the member app's signup flow (or `POST /register` directly with no `promoCode`).
Open the admin AlumnosPage and find the new user. Their badge reads "Freemium" with info color (blue/cyan).
Verifies: R7 (default for self-register), R10 (badge), Plan 03 + 04 + 05.
result: pass
side_fixes:

- "fix(103) c7d9f537 — embed trial counter into 'En Prueba' badge"
- "fix(103) 60e50617 — allow null dateOfBirth + flatten optional sections"
- "fix(103) 3136cb05 — drop 'Datos Adicionales' header, mark dateOfBirth required"
- "fix(103) 34e5a2df — caption layout, row spacing, action bar restructure"
- "fix(103) 35e55887 — DNI status icon inside input + Genero required"

### 3. Trial booking flow with eligible-picker

expected: |
Refactored flow (no separate "Nueva Sesión de Prueba" route or 3-input form):
(a) Empty state path: Horarios → click any future slot → toggle "Agregar sesión de prueba" inside the SlotDetailDialog. If branch has no eligible prueba alumnos, the section shows "No hay alumnos en prueba…" + button "Crear alumno en prueba". Click → router pushes /alumnos?nuevo=prueba and the create dialog auto-opens. Fill the form, submit. AlumnosPage row shows badge "En Prueba (0/1)" (warning color).
(b) Booking path: re-open the same slot → toggle "Agregar sesión de prueba" → q-select lists the freshly-created alumno (search by nombre/DNI/teléfono works) → select + "Reservar". Slot detail's "Sesiones de Prueba" group lists the alumno; badge flips to "En Prueba (1/1)" once a non-cancelled trial booking exists.
Verifies: R7 (status='prueba' default for /admin/members), R10 (badge), Plan 03 + 04 + 05, plus the Phase 103 inline refactor (bookTrial + listEligibleTrials).
result: pass
side_fixes:

- "fix(103) — bookTrial reactivates cancelled (member,slot,date) row instead of 500 on UNIQUE collision"

### 4. Auto-transition `prueba` → `activo` on plan assign

expected: |
Take the trial user from Test 3 (or any existing En Prueba user). Open their detail page → Gestionar Plan → assign any plan.
After assignment, the alumno detail header badge changes from "En Prueba" to "Activo" (positive/green).
Going back to AlumnosPage and filtering by "Activos" includes this user.
Verifies: R5 (auto-transition), Plan 02's recomputeUserStatus working end-to-end.
result: pass
side_fixes:

- "fix(103) — assignPlan + changeFixedSchedules: reject duplicate day-of-week in scheduleIds"
- "fix(103) — FixedSchedulePicker: grey out other slots once a day is picked, outline-based selection to avoid layout shift"
- "fix(103) — AssignPlanDialog: unified action bar (Cerrar left, Volver/Continuar/Confirmar right) across all steps"
- "fix(103) — MemberFormDialog: Deuda toggle no longer auto-activates when MemberProfile lacks the debt field"

### 5. Auto-transition `activo` → `inactivo` on cancel

expected: |
Take any "Activo" user. Open their detail → cancel their active subscription.
Badge changes to "Inactivo" (grey). AlumnosPage filter "Inactivos" includes this user.
IMPORTANT: if the same user later gets a new sub assigned, status flips back to `Activo` (not `freemium` — they were a paying member before).
Verifies: R6 (cancel transition), D-04 (never back to freemium after cancel).
result: pass

### 6. AlumnosPage filter dropdown — 5 options + 4-color badges

expected: |
Open AlumnosPage. Verify all of these in one pass:

- The "Estado" dropdown has exactly 5 options: Todos, Freemium, En Prueba, Activos, Inactivos
- The "Solo Leads" toggle is GONE from the filter row (the "Solo deudores" toggle stays)
- Selecting each option filters the list correctly
- Row badges show 4 distinct colors: Freemium=blue, En Prueba=orange/yellow, Activo=green, Inactivo=grey
- Selecting "Todos" mixes all 4
  Verifies: R9 (dropdown), R10 (badges), Plan 04 + 05 + D-15.
  result: pass

### 7. Staff disable + new login gate

expected: |
In admin → UsuariosPage, find a coach (any non-member role). Click "Desactivar usuario" → confirm.
Try to log in as that coach (use their credentials in another browser/incognito) → login is REJECTED with an appropriate error.
Back in admin, click "Activar usuario" on the same coach → login succeeds again.
Verifies: R11 (staff toggle writes staff_disabled), R12 (NEW login gate that closes pre-existing loophole).
result: pass

### 8. CSV export `Estado` column shows 4 labels

expected: |
In AlumnosPage, click "Exportar" → download the Excel.
Open it. The "Estado" column shows one of: Freemium, En Prueba, Activo, Inactivo (matching what the badge shows for each row).
Verifies: members/service.ts:812 export label migration.
result: pass

## Summary

total: 8
passed: 8
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

<!-- Empty until issues are reported -->
