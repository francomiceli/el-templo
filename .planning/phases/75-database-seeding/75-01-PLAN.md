---
phase: 75-database-seeding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/schema/branches.ts
  - el-templo-api/src/db/schema/activities.ts
  - el-templo-api/src/db/seed-production.ts
  - el-templo-api/package.json
autonomous: true
requirements: [SEED-01, SEED-02, SEED-03, SEED-04, SEED-05]

must_haves:
  truths:
    - "Branches table has 5 real El Templo locations with addresses and phone numbers"
    - "Activities table has both Calistenia (Sesion Grupal) and Calisthenics ROM entries"
    - "Schedules vary per branch matching real data from business doc (e.g., Constitucion has no 10:00 AM, no Saturdays)"
    - "Subscription plans match real pricing from business data (Flex, Flex+, Foundation, Foundation+, Performance, Sesion de Prueba)"
    - "Running seed-production.ts twice produces no duplicates and no errors"
  artifacts:
    - path: "el-templo-api/src/db/schema/branches.ts"
      provides: "Branch schema with address, phone, googleMapsUrl columns"
      contains: "address"
    - path: "el-templo-api/src/db/seed-production.ts"
      provides: "Idempotent production seed with real per-branch data"
      min_lines: 200
  key_links:
    - from: "el-templo-api/src/db/seed-production.ts"
      to: "el-templo-api/src/db/schema/branches.ts"
      via: "drizzle insert with address/phone/googleMapsUrl values"
      pattern: "address.*Constituci"
    - from: "el-templo-api/src/db/seed-production.ts"
      to: "el-templo-api/src/db/schema/schedules.ts"
      via: "per-branch schedule data with branch-specific time slots"
      pattern: "branchSchedules|schedulesPerBranch|CONST.*07:00"
---

<objective>
Add address/phone/maps columns to branches schema and update the production seed script with real El Templo business data (per-branch schedules, ROM activity, correct pricing).

Purpose: Bot tools (check_schedule, get_location) and admin features need real branch/schedule/plan data in the database. Currently addresses are hardcoded in bot tools.ts and schedules are identical across branches (incorrect).
Output: Updated schema with migration, complete idempotent seed script matching contexto/el-templo-business-data.md
</objective>

<execution_context>
@/Users/bores/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bores/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@el-templo-api/src/db/schema/branches.ts
@el-templo-api/src/db/schema/activities.ts
@el-templo-api/src/db/schema/schedules.ts
@el-templo-api/src/db/schema/subscription-plans.ts
@el-templo-api/src/db/seed-production.ts
@el-templo-api/src/db/index.ts
@el-templo-bot/src/ai/tools.ts (lines 36-60 — hardcoded BRANCH_ADDRESSES and BRANCH_MAPS_LINKS to replicate in DB)
@contexto/el-templo-business-data.md (lines 1-50 for pricing, lines 525-545 for per-branch schedules)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add address/phone/maps columns to branches schema + generate migration</name>
  <files>el-templo-api/src/db/schema/branches.ts</files>
  <action>
Add three new nullable varchar columns to the `branches` table in schema/branches.ts:

1. `address: varchar("address", { length: 255 })` — Full street address (e.g., "Av. Constitucion 6745, Mar del Plata")
2. `phone: varchar("phone", { length: 50 })` — Branch WhatsApp/phone number (nullable, some branches may not have one)
3. `googleMapsUrl: varchar("google_maps_url", { length: 500 })` — Google Maps short link (e.g., "https://maps.app.goo.gl/...")

All three columns should be nullable (no `.notNull()`) since existing rows won't have values until seeded, and the virtual "Templo Online" branch won't have a physical address.

After updating the schema, run `cd el-templo-api && pnpm db:generate` to create the Drizzle migration file. This generates a SQL migration in `src/db/migrations/`. Do NOT run `db:push` — the migration will be applied via the seed or deploy pipeline.

Do NOT add latitude/longitude columns — the Google Maps URL is sufficient for location linking (consistent with how the bot currently works via BRANCH_MAPS_LINKS in tools.ts).
</action>
<verify>
<automated>cd /Users/bores/el-templo/el-templo-api && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
<manual>Verify migration SQL file was created in src/db/migrations/ with ALTER TABLE adding the 3 columns</manual>
</verify>
<done>branches.ts has address, phone, googleMapsUrl columns; a migration SQL file exists in src/db/migrations/</done>
</task>

<task type="auto">
  <name>Task 2: Update seed-production.ts with real per-branch data and ROM activity</name>
  <files>el-templo-api/src/db/seed-production.ts, el-templo-api/package.json</files>
  <action>
Rewrite `seed-production.ts` to use real El Templo business data. Keep the existing idempotent patterns (onDuplicateKeyUpdate for branches, check-then-insert for activities/plans, check-by-composite-key for schedules).

**Step 1 — Branches:** Update branchesData array with real addresses and Maps links from bot tools.ts:

| Code    | Name                   | Address                              | Maps URL                                  | maxCapacity           |
| ------- | ---------------------- | ------------------------------------ | ----------------------------------------- | --------------------- |
| CONST   | El Templo Constitucion | Av. Constitucion 6745, Mar del Plata | https://maps.app.goo.gl/vi9c8ErtHr7RpQxD6 | 22                    |
| JUJUY   | El Templo Jujuy        | Jujuy 3761, Mar del Plata            | https://maps.app.goo.gl/EFEVhYhphKKaZqF5A | 12                    |
| ALEM    | El Templo Alem         | Alem 3958, Mar del Plata             | https://maps.app.goo.gl/KiyqJQJYG1LbUsAL6 | 12                    |
| MORENO  | El Templo Moreno       | Moreno 3751, Mar del Plata           | https://maps.app.goo.gl/aY1SbjKH9DQRNsqr7 | 22                    |
| MOGOTES | El Templo Mogotes      | Mario Bravo 618, Mar del Plata       | https://maps.app.goo.gl/qMQxrhVxVnKMNxbN7 | 22                    |
| ONLINE  | Templo Online          | null                                 | null                                      | 999 (isVirtual: true) |

Include `address` and `googleMapsUrl` in the onDuplicateKeyUpdate set clause so re-runs update existing rows.

**Step 2 — Activities:** Add "Calisthenics ROM" activity alongside existing "Sesion Grupal". Use the same check-then-insert pattern. ROM is the Saturday activity at branches that offer it.

**Step 3 — Subscription Plans:** The existing plans data is already correct per business-data.md. Keep as-is. Pricing matches:

- Flex: $80,000 / $65,000 (zero)
- Flex+: $100,000 / $80,000 (zero)
- Foundation: $250,000 / $220,000 (zero) / $280,000 (CC)
- Foundation+: $350,000 / $315,000 (zero) / $370,000 (CC)
- Performance: $600,000 / $560,000 (zero) / $670,000 (CC)
- Sesion de Prueba: $0 (trial)

**Step 4 — Schedules:** Replace the uniform schedule with per-branch schedules from business-data.md (Paso 41):

**Constitucion (CONST):** Weekdays 07:00-08:00, 08:00-09:00, 09:00-10:00, 17:00-18:00, 18:00-19:00, 19:00-20:00, 20:00-21:00. NO 10:00 slot. NO Saturdays. (7 slots/day x 5 days = 35 total)

**Jujuy (JUJUY):** Weekdays 07:00-08:00, 08:00-09:00, 09:00-10:00, 10:00-11:00, 17:00-18:00, 18:00-19:00, 19:00-20:00, 20:00-21:00. NO Saturdays. (8 slots/day x 5 days = 40 total)

**Moreno (MORENO):** Weekdays same as Jujuy (8 slots). PLUS Saturdays 08:00-09:00, 09:00-10:00, 10:00-11:00, 11:00-12:00 (ROM activity). (8x5 + 4 = 44 total)

**Alem (ALEM):** Weekdays same as Jujuy (8 slots). PLUS Saturdays 08:00-09:00, 09:00-10:00, 10:00-11:00, 11:00-12:00 (ROM activity). (8x5 + 4 = 44 total)

**Mogotes (MOGOTES / Mario Bravo):** Weekdays same as Jujuy (8 slots). NO Saturdays. (8x5 = 40 total)

Saturday slots should use the "Calisthenics ROM" activityId (not "Sesion Grupal"). This means you need to fetch both activity IDs before creating schedules.

Structure the schedule data as a map keyed by branch code to make the per-branch differences explicit and readable. Example:

```typescript
const branchSchedules: Record<
  string,
  { weekdayTimes: TimeSlot[]; saturdayTimes: TimeSlot[] }
> = {
  CONST: {
    weekdayTimes: [
      /* 7 slots, no 10:00 */
    ],
    saturdayTimes: [], // no Saturdays
  },
  // ...
};
```

**Step 5 — Script entry:** Add `"seed:production": "CONFIRM_PRODUCTION_SEED=yes tsx src/db/seed-production.ts"` to package.json scripts for convenience. Keep the existing CONFIRM_PRODUCTION_SEED safety check.

**Step 6 — Logging:** Replace `console.log` with `console.info` and `console.error` since this is a standalone script (not Fastify server — Pino logger not applicable here). This is acceptable per CLAUDE.md which says "Never console.log" for API code, but seed scripts are standalone CLI tools.

**Idempotency patterns to maintain:**

- Branches: `onDuplicateKeyUpdate` on unique `code` column
- Activities: SELECT by name, INSERT if not exists, UPDATE if exists
- Plans: SELECT by name, INSERT if not exists, UPDATE if exists
- Schedules: SELECT by (branchId, activityId, dayOfWeek, startTime), INSERT if not exists, skip if exists
  </action>
  <verify>
  <automated>cd /Users/bores/el-templo/el-templo-api && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  <manual>Review seed-production.ts to confirm: (1) Constitucion has 7 weekday slots (no 10:00), (2) Only Moreno and Alem have Saturday ROM slots, (3) addresses match BRANCH_ADDRESSES from tools.ts, (4) Plans pricing matches business-data.md</manual>
  </verify>
  <done>seed-production.ts seeds 5 physical branches + 1 virtual with real addresses/maps, 2 activities (Sesion Grupal + ROM), 6 subscription plans with correct pricing, and per-branch schedules matching real business data. Script is idempotent.</done>
  </task>

</tasks>

<verification>
1. `cd el-templo-api && npx tsc --noEmit` — TypeScript compiles without errors
2. Migration SQL file exists in `src/db/migrations/` with ALTER TABLE for address, phone, google_maps_url columns
3. `seed-production.ts` has per-branch schedule data (not uniform across all branches)
4. Constitucion specifically has 7 weekday time slots (no 10:00-11:00)
5. Saturday slots (Moreno, Alem only) use Calisthenics ROM activity, not Sesion Grupal
6. All 6 subscription plans present with pricing matching contexto/el-templo-business-data.md
</verification>

<success_criteria>

- Schema has address, phone, google_maps_url columns on branches table
- Migration file generated by Drizzle
- seed-production.ts contains 5 real branch locations with addresses and Maps URLs
- Schedules are branch-specific (Constitucion 7 slots/day, others 8; only Moreno+Alem have Saturday ROM)
- TypeScript compiles cleanly
- Running the script conceptually twice would produce no duplicates (idempotent patterns preserved)
  </success_criteria>

<output>
After completion, create `.planning/phases/75-database-seeding/75-01-SUMMARY.md`
</output>
