---
phase: 128-editor-de-rbol-en-el-admin
plan: 03
type: execute
wave: 2
depends_on: ["128-02"]
files_modified:
  - el-templo-admin/src/types/tree-editor.ts
  - el-templo-admin/src/composables/useTreeEditorApi.ts
  - el-templo-admin/src/pages/TreeEditorPage.vue
  - el-templo-admin/src/router/routes.ts
  - el-templo-admin/src/layouts/AdminLayout.vue
autonomous: false
requirements: [TREE-07]

must_haves:
  truths:
    - "A new admin section 'Editor de árbol' exists, reachable from the sidebar for coach/owner only (SC1, D-06)."
    - "A profe can reorder exercises within a subfamily×effort partition (up/down buttons, not fragile drag-and-drop) and the change persists + reflects on reload (SC2, D-03)."
    - "A profe can group/split subfamilies and add/remove precedence cross-edges from the UI (SC3, D-04, D-05)."
    - "The UI badges every order/edge as auto (SPOM default) vs manual (profe override) so the profe knows what they are overriding (Specific Idea)."
    - "All persistence goes through the /api/admin/tree-editor endpoints (no direct DB, no member endpoints) (D-06)."
  artifacts:
    - path: "el-templo-admin/src/composables/useTreeEditorApi.ts"
      provides: "fetchTree/reorderPartition/setPrecedence/regroup calling /admin/tree-editor"
      contains: "/admin/tree-editor"
    - path: "el-templo-admin/src/pages/TreeEditorPage.vue"
      provides: "Editable tree view: expandable categories/subfamilies, up/down reorder, auto/manual badges, precedence + regroup actions"
      min_lines: 80
    - path: "el-templo-admin/src/router/routes.ts"
      provides: "Route /tree-editor gated allowedRoles ['coach','owner']"
      contains: "tree-editor"
    - path: "el-templo-admin/src/types/tree-editor.ts"
      provides: "TS types mirroring the editable-tree response + request bodies"
      min_lines: 15
  key_links:
    - from: "TreeEditorPage.vue"
      to: "useTreeEditorApi"
      via: "composable methods"
      pattern: "useTreeEditorApi"
    - from: "useTreeEditorApi.ts"
      to: "/api/admin/tree-editor/*"
      via: "axios api instance"
      pattern: "/admin/tree-editor"
    - from: "AdminLayout.vue"
      to: "/tree-editor route"
      via: "sidebar q-item in the isCoachRole block"
      pattern: "tree-editor"
---

<objective>
Build the admin "Editor de árbol" section (SC1) that consumes the Plan-02 endpoints so profes can reorder, regroup, and set precedences (SC2/SC3) with changes persisting and prevailing over the auto order (SC4). UX is SIMPLE + ROBUST: expandable categories → subfamilies → ordered nodes, with up/down order buttons (NOT drag-and-drop) and explicit action buttons. Every order/edge shows an auto (SPOM default) vs manual (profe override) badge.

Purpose: closes TREE-07 end-to-end — the profe-facing surface that refines the auto-built tree.
Output: a new admin page, composable, types, route, and sidebar link.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-editor-de-rbol-en-el-admin/128-CONTEXT.md
@.planning/phases/128-editor-de-rbol-en-el-admin/128-02-SUMMARY.md

@el-templo-admin/src/composables/useProposalsApi.ts
@el-templo-admin/src/types/proposal.ts
@el-templo-admin/src/pages/ProposalReviewPage.vue
@el-templo-admin/src/pages/ExercisesPage.vue
@el-templo-admin/src/router/routes.ts
@el-templo-admin/src/layouts/AdminLayout.vue

<interfaces>
<!-- The composable + page mirror the 125 proposals slice (same admin coach surface). -->

Backend contract (from Plan 02, /api/admin/tree-editor, axios api already prefixes /api):

- GET /admin/tree-editor/tree → editable tree (category → subfamily → ordered nodes; each order/edge tagged auto|manual)
- POST /admin/tree-editor/reorder body { subfamilyId, effort:'CON'|'EXC'|'ISO', orderedExerciseIds:number[] }
- POST /admin/tree-editor/precedence body { fromExerciseId, toExerciseId, op:'add'|'remove' }
- POST /admin/tree-editor/regroup body { exerciseIds:number[], targetSubfamilyId }
  (Confirm the EXACT response shape + field names from 128-02-SUMMARY.md before typing.)

Composable pattern to MIRROR (useProposalsApi.ts): `ref` loading/error, `api.get/post`, `extractError` + `Notify.create({type:'negative'})` + `createLogger('useTreeEditorApi').error(...)`, rethrow. NEVER console.log. Composable must expose a `cleanup()` and NOT use onUnmounted internally (CLAUDE.md frontend pattern).

Route pattern to MIRROR (router/routes.ts): add a child of AdminLayout `{ path:'tree-editor', component: ()=>import('pages/TreeEditorPage.vue'), meta:{ allowedRoles:['coach','owner'] as AdminRole[] } }`.

Sidebar pattern to MIRROR (AdminLayout.vue ~lines 37-48, inside the `isCoachRole` training block): add a `<q-item to="/tree-editor">` with a warm-palette icon (e.g. `account_tree`) and label "Editor de árbol".

Brand: use existing Quasar components + the warm token palette (q-badge color="primary"/"grey" etc.) — NO new colors, NO blue (brand palette: terracotta/cream/clay/charcoal/aged-gold; quasar.variables.scss is source of truth).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types + composable + route + sidebar link</name>
  <files>el-templo-admin/src/types/tree-editor.ts, el-templo-admin/src/composables/useTreeEditorApi.ts, el-templo-admin/src/router/routes.ts, el-templo-admin/src/layouts/AdminLayout.vue</files>
  <read_first>
    - .planning/phases/128-editor-de-rbol-en-el-admin/128-02-SUMMARY.md (the EXACT response/request shapes — do not guess field names)
    - el-templo-admin/src/composables/useProposalsApi.ts (composable idiom to mirror)
    - el-templo-admin/src/types/proposal.ts (types file style)
    - el-templo-admin/src/router/routes.ts (training child routes + allowedRoles)
    - el-templo-admin/src/layouts/AdminLayout.vue lines 37-48 + 217-218 (isCoachRole training nav block)
    - el-templo-admin/src/boot/axios.ts (the `api` instance; /api prefix), src/utils/extract-error.ts, src/utils/logger.ts
  </read_first>
  <action>
    Create `types/tree-editor.ts` mirroring the Plan-02 response/request shapes verbatim from the summary: an `EditableTree` (categories → subfamilies → nodes with an `orderSource: 'auto'|'manual'` per partition and per-edge `source`), plus request body types `ReorderBody`, `PrecedenceBody`, `RegroupBody`. No `any`.

    Create `composables/useTreeEditorApi.ts` exporting `useTreeEditorApi()` with `loading`/`error` refs and `fetchTree()`, `reorderPartition(body)`, `setPrecedence(body)`, `regroup(body)`, each calling the corresponding `/admin/tree-editor/*` endpoint via the `api` axios instance, wrapped in `extractError` + `Notify` + `createLogger('useTreeEditorApi').error` + rethrow (copy the useProposalsApi idiom). Expose a no-op-safe `cleanup()`; do NOT call onUnmounted inside the composable.

    Add the `/tree-editor` route as a child of AdminLayout in router/routes.ts with `meta.allowedRoles: ['coach','owner'] as AdminRole[]` (matching TRAINING_ROLES and the other training pages).

    Add the sidebar `<q-item to="/tree-editor">` inside the existing training block (the same template guarded by `isCoachRole`, next to "Revisión de dimensiones"), icon `account_tree`, label "Editor de árbol".

  </action>
  <acceptance_criteria>
    - source: types/tree-editor.ts has no `any` and mirrors the summary's field names; useTreeEditorApi.ts calls only `/admin/tree-editor/*` endpoints and uses extractError+Notify+logger (no console.log); exposes cleanup().
    - source: router/routes.ts has a `/tree-editor` child with allowedRoles ['coach','owner']; AdminLayout.vue has a "/tree-editor" q-item in the isCoachRole block.
    - CLI: `cd el-templo-admin && pnpm vue-tsc --noEmit` exits 0 (or the project's typecheck script).
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-admin && pnpm vue-tsc --noEmit 2>/dev/null || cd el-templo-admin && pnpm run typecheck</automated>
  </verify>
  <done>Types, composable (cleanup, no console.log), route, and sidebar link added; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: TreeEditorPage.vue — expandable tree, up/down reorder, auto/manual badges, precedence + regroup actions</name>
  <files>el-templo-admin/src/pages/TreeEditorPage.vue</files>
  <read_first>
    - el-templo-admin/src/pages/ProposalReviewPage.vue (page layout, q-expansion, action buttons, loading/empty states to mirror)
    - el-templo-admin/src/pages/ExercisesPage.vue (q-page header + banner + dialog idiom)
    - el-templo-admin/src/composables/useTreeEditorApi.ts (the composable from Task 1)
    - el-templo-admin/src/types/tree-editor.ts (the types from Task 1)
    - el-templo-admin/quasar.variables.scss (warm palette tokens — do not introduce new colors / blue)
  </read_first>
  <action>
    Build `TreeEditorPage.vue` (Quasar q-page, Composition API `<script setup lang="ts">`) that on mount calls `fetchTree()` and renders expandable categories → subfamilies → ordered node lists. For each node row inside a partition show up/down q-btns (disabled at the ends) that reorder the LOCAL list and POST the new order via `reorderPartition` (optimistic or refetch on success — pick refetch for simplicity/robustness). Show a q-badge per partition/edge: "Auto" (color grey, SPOM default) vs "Manual" (color primary, profe override) so the profe sees what they are overriding.

    Add explicit actions (buttons, NOT drag-and-drop): a "Precedencia" action opening a small q-dialog to add/remove a cross-edge between two selected nodes (`setPrecedence`), and a "Reagrupar" action opening a q-dialog to move selected exercise(s) to another subfamily (`regroup`). After any successful mutation, refetch the tree and Notify positive. Handle loading (skeleton/spinner) and empty states. Use only existing Quasar components + warm tokens.

    No `any`; type all refs/handlers against types/tree-editor.ts. Use createLogger for any error path (composable already notifies). No console.log.

  </action>
  <acceptance_criteria>
    - source: TreeEditorPage.vue renders expandable categories→subfamilies→nodes, up/down reorder buttons calling reorderPartition, an auto/manual badge per partition/edge, and dialogs invoking setPrecedence + regroup; all data via useTreeEditorApi (no direct api calls, no member endpoints).
    - behavior: reordering a node up/down persists (POST reorder) and the reloaded tree reflects the new order with a "Manual" badge; precedence/regroup dialogs call their endpoints and refetch.
    - CLI: `cd el-templo-admin && pnpm vue-tsc --noEmit` exits 0 (or project typecheck script); `pnpm lint` clean on the new file.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-admin && pnpm vue-tsc --noEmit 2>/dev/null || cd el-templo-admin && pnpm run typecheck</automated>
  </verify>
  <done>Editor page renders the tree with up/down reorder, auto/manual badges, and precedence + regroup actions, all via the composable; typecheck clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The new admin "Editor de árbol" section: sidebar link (coach/owner), expandable tree, up/down reorder with auto/manual badges, and precedence + regroup dialogs — all persisting via /api/admin/tree-editor.</what-built>
  <how-to-verify>
    1. Run the admin app locally (`cd el-templo-admin && pnpm dev`) and the API; log in as a coach or owner.
    2. Confirm "Editor de árbol" appears in the sidebar (training block) and opens the page; confirm a member/gestion/recepcion role does NOT see/reach it.
    3. Expand a category → subfamily; reorder two exercises with the up/down buttons; confirm the partition now shows a "Manual" badge and the new order persists after a page reload.
    4. Open the Precedencia dialog, add a cross-edge between two nodes, confirm it appears as Manual; remove it, confirm it disappears.
    5. Open the Reagrupar dialog, move an exercise to another subfamily, confirm it re-buckets and persists on reload.
    6. Confirm the colors are warm/brand (no blue) and the layout is not broken.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. badge wrong, reorder doesn't persist, role leak).</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                                                                                                                                              |
| -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| admin browser → /api/admin/tree-editor | The UI only ever calls the staff-scoped editor endpoints; the route is gated allowedRoles ['coach','owner'] client-side, but the server (Plan 02) is the real authority. |

## STRIDE Threat Register

| Threat ID | Category               | Component                           | Disposition | Mitigation Plan                                                                                                                                                                                                          |
| --------- | ---------------------- | ----------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-128-06  | Elevation of Privilege | client route guard                  | mitigate    | Route meta.allowedRoles ['coach','owner'] + sidebar shown only in isCoachRole block; the authoritative check is the server-side TRAINING_ROLES guard (Plan 02 T-128-03). Client guard is defense-in-depth, not the gate. |
| T-128-07  | Tampering              | reorder/precedence/regroup requests | accept      | The UI sends only ids from the fetched tree; the server validates FK existence + partition membership (Plan 02 T-128-04). No client-side trust needed.                                                                   |
| T-128-SC  | Tampering              | npm installs                        | accept      | No new packages — uses existing Quasar/Vue/axios. No legitimacy gate required.                                                                                                                                           |

</threat_model>

<verification>
- `vue-tsc --noEmit` (or project typecheck) clean in el-templo-admin; lint clean on new files.
- Human checkpoint: sidebar visibility by role, reorder persistence + Manual badge, precedence add/remove, regroup, warm palette, no layout break.
</verification>

<success_criteria>

- New admin "Editor de árbol" section exists and is coach/owner-only (SC1).
- Reorder within a subfamily persists and reflects on reload (SC2).
- Group/split subfamilies + precedence edits work from the UI (SC3).
- All edits persist via the editor endpoints and prevail over the auto order (SC4); auto vs manual is always badged.
  </success_criteria>

<output>
Create `.planning/phases/128-editor-de-rbol-en-el-admin/128-03-SUMMARY.md` when done.
</output>
