---
phase: 35-gladius-blog
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - el-templo-web/pages/gladius.vue
  - el-templo-web/data/gladius.ts
  - el-templo-web/components/GladHero.vue
  - el-templo-web/components/GladPhilosophy.vue
  - el-templo-web/components/GladCatalog.vue
  - el-templo-web/components/GladInAction.vue
  - el-templo-web/components/GladContact.vue
  - el-templo-web/components/GladWhatsApp.vue
autonomous: true
requirements:
  - GLAD-01
  - GLAD-02
  - GLAD-04
  - GLAD-05
  - GLAD-06

must_haves:
  truths:
    - "/gladius renders a full-viewport hero with Gladius branding and 'FORJADO PARA LOS QUE ENTRENAN EN SERIO.' H1"
    - "A product philosophy section explains why Gladius exists and its connection to El Templo"
    - "Product catalog displays cards fetched from API with photo placeholder, name, description, and 'Consultar' CTA"
    - "An 'En Accion' section shows real-use photo gallery with social proof copy"
    - "Contact section provides WhatsApp CTA and inquiry form (nombre, email, producto de interes)"
    - "A floating WhatsApp button is always visible on the page"
    - "The page shares the same header and footer as the home page (default layout)"
  artifacts:
    - path: "el-templo-web/pages/gladius.vue"
      provides: "Page composition and SEO meta"
      min_lines: 20
    - path: "el-templo-web/data/gladius.ts"
      provides: "Static content data (hero copy, philosophy text, section config)"
      exports: ["gladiusConfig"]
    - path: "el-templo-web/components/GladHero.vue"
      provides: "Full-viewport hero with parallax and entrance animation"
      min_lines: 50
    - path: "el-templo-web/components/GladCatalog.vue"
      provides: "Product catalog cards fetched from API"
      min_lines: 40
    - path: "el-templo-web/components/GladContact.vue"
      provides: "Inquiry form with validation and WhatsApp CTA"
      min_lines: 60
  key_links:
    - from: "el-templo-web/components/GladCatalog.vue"
      to: "/api/gladius/products"
      via: "useFetch or $fetch"
      pattern: "fetch.*gladius.*products"
    - from: "el-templo-web/components/GladContact.vue"
      to: "/api/gladius/inquire"
      via: "$fetch POST"
      pattern: "fetch.*gladius.*inquire"
---

<objective>
Build the complete /gladius equipment showcase page with 5 sections following the spec-gladius-pt1/pt2 structure, matching the brand voice and visual patterns established in Phases 30-34.

Purpose: Gladius is a revenue-generating product line. The page drives WhatsApp inquiries and positions Gladius as a premium equipment brand within the El Templo ecosystem.

Output: Full /gladius page with Hero, Philosophy, Catalog (API-backed), En Accion gallery, Contact form + WhatsApp, floating WhatsApp button.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/35-gladius-blog/35-CONTEXT.md
@.planning/phases/35-gladius-blog/35-01-SUMMARY.md

# Template patterns from Phase 34 (Franquicias)

@el-templo-web/pages/franquicias.vue
@el-templo-web/components/FranHero.vue
@el-templo-web/components/FranForm.vue
@el-templo-web/components/FranWhatsApp.vue
@el-templo-web/data/franquicias.ts

# Shared composables

@el-templo-web/composables/useScrollReveal.ts

# Gladius spec

@.docs/brand-landing/spec-gladius-pt1.md
@.docs/brand-landing/spec-gladius-pt2.md

<interfaces>
<!-- From Plan 01 API (to be read from SUMMARY) -->
From Gladius API routes:
```typescript
// GET /api/gladius/products -> GladiusProduct[]
// POST /api/gladius/inquire -> { message: string, whatsappUrl: string }
interface GladiusProduct {
  id: number;
  name: string;
  slug: string;
  description: string;
  photo: string | null;
  status: string;
  sortOrder: number;
}
```

From existing components:

```typescript
// PlaceholderBox.vue — image placeholder with aspect-ratio and label
// useScrollReveal() -> { revealed, elementRef, cleanup }
// FranHero pattern: parallax, staggered entrance, scroll indicator
// FranForm pattern: reactive form state, inline validation, $fetch submit
// FranWhatsApp pattern: floating fixed-position WhatsApp button
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Data file and Hero + Philosophy + En Accion sections</name>
  <files>
    el-templo-web/data/gladius.ts,
    el-templo-web/components/GladHero.vue,
    el-templo-web/components/GladPhilosophy.vue,
    el-templo-web/components/GladInAction.vue
  </files>
  <action>
    1. **`data/gladius.ts`** — Centralized data file (same pattern as franquicias.ts):
       ```typescript
       export const gladiusConfig = {
         whatsappUrl: 'https://wa.link/ci8dpl', // Same Gladius contact
         hero: {
           title: 'FORJADO PARA LOS QUE ENTRENAN EN SERIO.',
           subtitle: 'Barras de calistenia dise\u00f1adas en El Templo. Probadas en 8 sedes. Construidas para durar.',
           cta: 'VER PRODUCTOS',
           ctaAnchor: '#catalogo-gladius',
         },
         philosophy: {
           title: 'EL ARMA DEL CALISTENISTA',
           body: 'Gladius naci\u00f3 dentro de El Templo. Las mismas barras con las que entrenan m\u00e1s de 1000 alumnos cada semana, ahora en tu casa, tu parque, tu espacio. No es equipamiento gen\u00e9rico. Es la herramienta que us\u00e1s cuando entren\u00e1s con tu propio peso y quer\u00e9s que el fierro est\u00e9 a la altura.',
           features: [
             { title: 'Dise\u00f1o Propio', text: 'Cada barra est\u00e1 pensada para calistenia real, no fitness gen\u00e9rico.' },
             { title: 'Probadas en 8 Sedes', text: 'El mismo equipamiento que usan m\u00e1s de 1000 alumnos cada semana.' },
             { title: 'Construidas para Durar', text: 'Materiales y terminaciones pensados para uso intensivo diario.' },
           ],
         },
         inAction: {
           title: 'EN ACCI\u00d3N',
           subtitle: 'El mismo equipamiento que us\u00e1s en El Templo. En cada sede, cada sesi\u00f3n, cada d\u00eda.',
           // Placeholder photos until real photography arrives
           photos: Array.from({ length: 6 }, (_, i) => ({
             id: i + 1,
             alt: `Gladius en uso - foto ${i + 1}`,
           })),
         },
         contact: {
           title: 'CONSULT\u00c1 POR TU GLADIUS',
           subtitle: 'Escribinos para conocer disponibilidad, precios y opciones de env\u00edo.',
           whatsappCta: 'CONSULTAR POR WHATSAPP',
         },
       };
       ```
       Adjust copy using exact spec text from spec-gladius-pt1.md. Use HTML entities for accented characters.

    2. **`GladHero.vue`** — Full-viewport hero following FranHero.vue pattern:
       - Deep Charcoal background with PlaceholderBox for product hero image
       - Overlay gradient (0.20/0.65 opacity, same as franchise hero)
       - H1: "FORJADO PARA LOS QUE ENTRENAN EN SERIO." in Montserrat ExtraBold
       - Subtitle in Cormorant Garamond italic
       - Primary CTA "VER PRODUCTOS" scrolls to #catalogo-gladius (Terracotta button)
       - Secondary CTA "CONSULTAR" scrolls to #contacto-gladius (Aged Gold ghost button)
       - Staggered entrance animation (same CSS pattern as FranHero)
       - Parallax on desktop (same pattern: requestAnimationFrame + matchMedia guard)
       - Scroll indicator arrow with bounce animation
       - BEM prefix: `glad-hero`
       - CRITICAL: Escape nav with negative margin-top (same as SectionHero pattern)

    3. **`GladPhilosophy.vue`** — Philosophy section:
       - Warm Stone background
       - Split layout: text (60%) + PlaceholderBox image (40%) on desktop, stacked on mobile
       - H2 section title "EL ARMA DEL CALISTENISTA"
       - Narrative body text in Geologica
       - 3 feature items below text (icon/title/description) — use inline SVG stroke icons
       - useScrollReveal for entrance animation (slide-in from sides, same as SectionIdentity)
       - BEM prefix: `glad-philosophy`

    4. **`GladInAction.vue`** — "En Accion" real-use photo gallery:
       - Marble Cream background
       - H2 "EN ACCI&Oacute;N" + subtitle
       - 6 PlaceholderBox photos in mosaic grid (similar to SectionCommunity gallery pattern)
         - Desktop: 3-column grid with mixed aspect ratios (2:3, 3:2, 1:1)
         - Tablet: 2-column
         - Mobile: horizontal scroll with snap
       - Social proof text below: "Las mismas barras que usan +1000 alumnos en 8 sedes."
       - useScrollReveal for grid entrance
       - BEM prefix: `glad-action`

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-web && npx nuxi typecheck 2>&1 | tail -20</automated>
  </verify>
  <done>Data file exports gladiusConfig, Hero renders with parallax and CTAs, Philosophy shows split layout with features, InAction shows photo gallery grid</done>
</task>

<task type="auto">
  <name>Task 2: Catalog + Contact form + WhatsApp + page composition</name>
  <files>
    el-templo-web/components/GladCatalog.vue,
    el-templo-web/components/GladContact.vue,
    el-templo-web/components/GladWhatsApp.vue,
    el-templo-web/pages/gladius.vue
  </files>
  <action>
    1. **`GladCatalog.vue`** — Product catalog section:
       - Anchor: `id="catalogo-gladius"` (target of hero CTA)
       - Warm Stone background
       - H2 "NUESTROS PRODUCTOS" or similar (follow spec brand voice)
       - Fetch products from API: `useFetch('/api/gladius/products')` for SSG data fetching
       - Product cards on Warm Linen (#EDE4D6) background:
         - PlaceholderBox for product photo (aspect-ratio 4:3)
         - Product name in Montserrat SemiBold
         - Description in Geologica Regular
         - NO VISIBLE PRICE — "Consultar" ghost button (Aged Gold) per CONTEXT.md decision
         - "Consultar" scrolls to #contacto-gladius (pre-fills product name if possible via query/hash)
       - Grid: 3 columns desktop, 2 tablet, 1 mobile
       - Card hover: translateY(-3px) + shadow intensifies (300ms) — same as approach cards
       - Cards have border: 1px solid Warm Stone, border-radius 8px
       - Empty state: if no products from API, show "Pr&oacute;ximamente" message
       - BEM prefix: `glad-catalog`
       - IMPORTANT: Use `useRuntimeConfig().public.apiUrl` for the API base URL (or configure Nuxt proxy). Follow the same pattern used in FranForm.vue for API calls.

    2. **`GladContact.vue`** — Contact/inquiry form section:
       - Anchor: `id="contacto-gladius"` (target of Consultar CTAs)
       - Sandy Beige background (same as FranForm)
       - H2 "CONSULT&Aacute; POR TU GLADIUS"
       - Subtitle text
       - Two-column layout on desktop: form (60%) + WhatsApp CTA (40%). Stack on mobile.
       - Form fields (following FranForm pattern exactly):
         - nombre (text, required)
         - email (email, required)
         - productoInteres (text, required — can be pre-populated from catalog card click)
         - mensaje (textarea, optional, maxLength 1000)
       - Inline validation (same pattern as FranForm: requiredFields array, emailRegex, validateField, validateAll)
       - Submit via `$fetch` (NOT useFetch) to POST /api/gladius/inquire
       - Loading state during submission
       - Post-submit: confirmation message + WhatsApp link (same UX as FranForm)
       - Error state for submission failure
       - BEM prefix: `glad-contact`
       - Gladius brand voice: "Consultar", "Solicitar" — never "compra ahora"

    3. **`GladWhatsApp.vue`** — Floating WhatsApp button:
       - EXACT COPY of FranWhatsApp.vue but with `glad-whatsapp` BEM prefix
       - Same fixed bottom-right position, WhatsApp green, scale entrance
       - Same href to WhatsApp link from gladiusConfig
       - Consider if FranWhatsApp can be reused via prop (if FranWhatsApp accepts a configurable URL, just reuse it; if hardcoded, create separate component). Read FranWhatsApp.vue first to decide.

    4. **`pages/gladius.vue`** — Replace stub with full page:
       - Same pattern as franquicias.vue: useHead for SEO + compose sections
       - SEO meta:
         - title: "Gladius — Equipamiento de Calistenia por El Templo"
         - description: "Barras de calistenia y paralelas dise&ntilde;adas en El Templo. Probadas en 8 sedes por +1000 alumnos. Consult&aacute; disponibilidad."
         - og:url: "https://eltemplo.org/gladius"
         - og:type: "website"
       - Compose: GladHero, GladPhilosophy, GladCatalog, GladInAction, GladContact, GladWhatsApp
       - Uses default layout (shares nav + footer with home page) — GLAD-06 satisfied automatically

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-web && npx nuxi typecheck 2>&1 | tail -20</automated>
  </verify>
  <done>/gladius page renders all 5 sections + WhatsApp, catalog fetches from API, inquiry form submits correctly, SEO meta tags set, page shares header/footer via default layout</done>
</task>

</tasks>

<verification>
```bash
# Type check passes
cd /home/franco/projects/el-templo/el-templo-web && npx nuxi typecheck

# Page renders without errors

cd /home/franco/projects/el-templo/el-templo-web && npx nuxi build 2>&1 | tail -20

# All Gladius components exist

ls el-templo-web/components/Glad\*.vue

# Data file exports

grep "export" el-templo-web/data/gladius.ts

```
</verification>

<success_criteria>
1. /gladius renders full-viewport hero with "FORJADO PARA LOS QUE ENTRENAN EN SERIO." H1 and dual CTAs
2. Philosophy section shows split text/image layout with 3 feature items
3. Product catalog displays cards from API with PlaceholderBox photos and "Consultar" CTAs
4. "En Accion" section shows mosaic photo gallery
5. Contact section has working inquiry form (nombre, email, producto de interes) that submits to API
6. Floating WhatsApp button visible on the page
7. Page shares same header/footer as home page (default layout)
8. All text uses brand voice — no "compra ahora", only "Consultar", "Solicitar", "Adquirir"
</success_criteria>

<output>
After completion, create `.planning/phases/35-gladius-blog/35-02-SUMMARY.md`
</output>
```
