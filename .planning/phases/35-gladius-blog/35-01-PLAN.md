---
phase: 35-gladius-blog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/schema/gladius-products.ts
  - el-templo-api/src/db/schema/gladius-inquiries.ts
  - el-templo-api/src/db/schema/blog-posts.ts
  - el-templo-api/src/db/schema/index.ts
  - el-templo-api/src/db/migrations/0019_gladius_blog_tables.sql
  - el-templo-api/src/modules/gladius/service.ts
  - el-templo-api/src/modules/gladius/routes.ts
  - el-templo-api/src/modules/blog/service.ts
  - el-templo-api/src/modules/blog/image-service.ts
  - el-templo-api/src/modules/blog/routes.ts
  - el-templo-api/src/app.ts
  - el-templo-api/.env.example
  - el-templo-api/test/gladius/gladius.test.ts
  - el-templo-api/test/blog/blog.test.ts
autonomous: true
requirements:
  - GLAD-03
  - GLAD-05
  - BLOG-01

must_haves:
  truths:
    - "GET /api/gladius/products returns an array of published products with name, description, photo, slug"
    - "POST /api/gladius/inquire persists inquiry to DB and returns whatsappUrl"
    - "Admin-authenticated POST/PUT/DELETE /api/gladius/products manages product catalog"
    - "Admin-authenticated POST /api/blog/posts creates a draft blog post"
    - "Admin-authenticated PUT /api/blog/posts/:id updates post fields including status (draft/published)"
    - "GET /api/blog/posts returns paginated published posts with title, excerpt, coverImage, slug, publishedAt"
    - "GET /api/blog/posts/:slug returns full post with markdown body"
    - "POST /api/blog/upload-image returns an R2 URL for uploaded image"
    - "All Gladius and Blog integration tests pass"
  artifacts:
    - path: "el-templo-api/src/db/schema/gladius-products.ts"
      provides: "gladius_products table schema"
      contains: "gladiusProducts"
    - path: "el-templo-api/src/db/schema/gladius-inquiries.ts"
      provides: "gladius_inquiries table schema"
      contains: "gladiusInquiries"
    - path: "el-templo-api/src/db/schema/blog-posts.ts"
      provides: "blog_posts table schema"
      contains: "blogPosts"
    - path: "el-templo-api/src/modules/gladius/routes.ts"
      provides: "Gladius API routes"
      exports: ["gladiusRoutes"]
    - path: "el-templo-api/src/modules/blog/routes.ts"
      provides: "Blog API routes"
      exports: ["blogRoutes"]
  key_links:
    - from: "el-templo-api/src/app.ts"
      to: "gladius/routes.ts + blog/routes.ts"
      via: "fastify.register"
      pattern: "register.*gladiusRoutes|blogRoutes"
    - from: "el-templo-api/src/modules/blog/image-service.ts"
      to: "el-templo-api/src/plugins/r2.ts"
      via: "S3Client presigned URL"
      pattern: "PutObjectCommand|getSignedUrl"
---

<objective>
Create all API backend infrastructure for Gladius product catalog + inquiry form and Blog post management + image upload.

Purpose: Both Gladius frontend (/gladius page) and Blog frontend (/blog pages) need API endpoints and database tables before any UI can be built. This plan establishes the full backend layer for both features.

Output: 3 new DB tables (gladius_products, gladius_inquiries, blog_posts), Gladius public + admin routes, Blog CRUD + public routes, blog image upload via R2, integration tests for both.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/35-gladius-blog/35-CONTEXT.md

# Franchise pattern (template for Gladius inquiry)

@el-templo-api/src/db/schema/franchise-applications.ts
@el-templo-api/src/modules/franchise/routes.ts
@el-templo-api/src/modules/franchise/service.ts
@el-templo-api/test/franchise/franchise-application.test.ts

# R2 upload pattern (reuse for blog images)

@el-templo-api/src/plugins/r2.ts
@el-templo-api/src/modules/admin/video-service.ts
@el-templo-api/src/modules/admin/video-schemas.ts

# App registration pattern

@el-templo-api/src/app.ts
@el-templo-api/src/db/schema/index.ts

# Test helpers

@el-templo-api/test/helpers.ts

<interfaces>
<!-- Existing Fastify decorators from plugins -->
From el-templo-api/src/plugins/r2.ts:
```typescript
// FastifyInstance.r2: S3Client
// FastifyInstance.r2Bucket: string
```

From el-templo-api/src/plugins/auth.ts:

```typescript
// FastifyInstance.authenticate: preHandler hook
// request.user: { userId: number; email: string; role: string }
```

From el-templo-api/src/db/schema/franchise-applications.ts (template pattern):

```typescript
export const franchiseApplications = mysqlTable("franchise_applications", {
  id: int("id").primaryKey().autoincrement(),
  nombre: varchar("nombre", { length: 255 }).notNull(),
  email: varchar("email", { length: 255 }).notNull(),
  // ...
  createdAt: timestamp("created_at").defaultNow().notNull(),
  status: varchar("status", { length: 50 }).default("new").notNull(),
});
```

From el-templo-api/src/modules/franchise/routes.ts (public route pattern):

```typescript
export const franchiseRoutes: FastifyPluginAsync = async (fastify) => {
  const service = new FranchiseService(fastify.db, fastify.log);
  fastify.post<{ Body: ApplyBody }>("/apply", { schema: applySchema }, async (request, reply) => { ... });
};
```

From el-templo-api/src/modules/admin/routes.ts (admin auth pattern):

```typescript
const ADMIN_ROLES = ["coach", "admin", "superadmin"];
// Uses fastify.authenticate preHandler + role check in route handler
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Database schemas and migration for Gladius + Blog tables</name>
  <files>
    el-templo-api/src/db/schema/gladius-products.ts,
    el-templo-api/src/db/schema/gladius-inquiries.ts,
    el-templo-api/src/db/schema/blog-posts.ts,
    el-templo-api/src/db/schema/index.ts,
    el-templo-api/src/db/migrations/0019_gladius_blog_tables.sql
  </files>
  <behavior>
    - gladius_products table has: id (PK auto), name (varchar 255), slug (varchar 255 unique), description (text), photo (varchar 500, nullable), status (varchar 20, default 'published'), sort_order (int, default 0), created_at, updated_at
    - gladius_inquiries table has: id (PK auto), nombre (varchar 255), email (varchar 255), producto_interes (varchar 255), mensaje (text nullable), created_at, status (varchar 50, default 'new')
    - blog_posts table has: id (PK auto), title (varchar 255), slug (varchar 255 unique), excerpt (text), cover_image (varchar 500 nullable), body (text -- Markdown content), status (varchar 20, default 'draft'), created_at, updated_at, published_at (timestamp nullable)
    - All three tables export from schema/index.ts
  </behavior>
  <action>
    Create 3 Drizzle schema files following the franchise-applications.ts pattern:

    1. `gladius-products.ts`: Table `gladius_products` with fields listed in behavior. Use `mysqlTable` from drizzle-orm/mysql-core. `slug` has unique constraint. `status` enum-like via varchar with default 'published'. `sort_order` for admin-controlled ordering. `updated_at` uses `.defaultNow().onUpdateNow()`.

    2. `gladius-inquiries.ts`: Table `gladius_inquiries` following the franchise_applications pattern exactly. Simpler fields: nombre, email, producto_interes (what product they're asking about), mensaje (optional). Same status + created_at pattern.

    3. `blog-posts.ts`: Table `blog_posts`. `body` stores raw Markdown. `status` is 'draft' or 'published'. `published_at` is nullable (set when status changes to 'published'). `slug` has unique constraint.

    4. Update `schema/index.ts` to add 3 new exports: `export * from "./gladius-products"`, `export * from "./gladius-inquiries"`, `export * from "./blog-posts"`.

    5. Create manual SQL migration `0019_gladius_blog_tables.sql` (same pattern as 0018_franchise_applications.sql -- manual SQL because drizzle-kit generate has interactive prompt conflicts). Create all 3 tables in one migration file.

    Use `int`, `varchar`, `text`, `timestamp` from `drizzle-orm/mysql-core`. No `any` types.

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-api && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>3 schema files type-check, index.ts re-exports all three, migration SQL creates all tables</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gladius API routes, service, and integration tests</name>
  <files>
    el-templo-api/src/modules/gladius/service.ts,
    el-templo-api/src/modules/gladius/routes.ts,
    el-templo-api/src/app.ts,
    el-templo-api/test/gladius/gladius.test.ts
  </files>
  <behavior>
    - GET /api/gladius/products returns array of published products ordered by sort_order
    - GET /api/gladius/products/:slug returns single product by slug (404 if not found)
    - POST /api/gladius/inquire with {nombre, email, productoInteres, mensaje?} returns 201 + whatsappUrl
    - POST /api/gladius/inquire with missing required fields returns 400
    - POST /api/admin/gladius/products (admin-only) creates product, returns 201
    - PUT /api/admin/gladius/products/:id (admin-only) updates product
    - DELETE /api/admin/gladius/products/:id (admin-only) deletes product
    - GET /api/admin/gladius/products (admin-only) returns all products including unpublished
    - Non-admin users get 403 on admin routes
    - Inquiry persists to gladius_inquiries table
  </behavior>
  <action>
    Create Gladius module following the franchise module pattern:

    1. `gladius/service.ts` — GladiusService class:
       - Constructor takes `db` (MySql2Database) and `log` (FastifyBaseLogger)
       - `listPublishedProducts()`: SELECT * FROM gladius_products WHERE status='published' ORDER BY sort_order ASC
       - `getProductBySlug(slug)`: SELECT by slug, return null if not found
       - `listAllProducts()`: SELECT * ORDER BY sort_order (admin — includes unpublished)
       - `createProduct(data)`: INSERT INTO gladius_products, return inserted product. Auto-generate slug from name if not provided (lowercase, replace spaces with hyphens, strip special chars)
       - `updateProduct(id, data)`: UPDATE by id, return updated product
       - `deleteProduct(id)`: DELETE by id
       - `submitInquiry(data)`: INSERT INTO gladius_inquiries, send notification email via Resend (same graceful skip pattern as franchise), return { whatsappUrl }
       - Use the same WHATSAPP_URL as franchise ("https://wa.link/ci8dpl") or a Gladius-specific one
       - Email notification: subject "Nueva consulta Gladius: {nombre} — {productoInteres}"

    2. `gladius/routes.ts` — gladiusRoutes FastifyPluginAsync:
       - Public routes (no auth):
         - GET `/products` — list published products
         - GET `/products/:slug` — get product by slug (return 404 if not found)
         - POST `/inquire` — submit inquiry (JSON Schema validation: nombre required maxLength 255, email required format email, productoInteres required maxLength 255, mensaje optional maxLength 1000)
       - Admin routes (require authenticate preHandler + role check for admin/superadmin ONLY — NOT coach):
         - GET `/admin/products` — list all products
         - POST `/admin/products` — create product
         - PUT `/admin/products/:id` — update product
         - DELETE `/admin/products/:id` — delete product
       - Role check: `if (!['admin', 'superadmin'].includes(request.user.role))` return 403

    3. Update `app.ts`:
       - Import `gladiusRoutes` from `./modules/gladius/routes`
       - Register: `await app.register(gladiusRoutes, { prefix: "/api/gladius" })`

    4. `test/gladius/gladius.test.ts` — Integration tests following franchise-application.test.ts pattern:
       - Test GET /api/gladius/products returns empty array initially
       - Test POST /api/gladius/inquire with valid data returns 201 + whatsappUrl
       - Test POST /api/gladius/inquire with missing nombre returns 400
       - Test POST /api/gladius/inquire with invalid email returns 400
       - Test inquiry persists to DB
       - Clean up test data in afterEach
       - NOTE: Admin CRUD routes tested via direct inject with auth token (use getAuthToken from helpers if admin user exists in test DB, otherwise test the public routes only and note admin routes need seed data)

    Use Pino logger (fastify.log / this.log). No console.log. No `any` types. Catch errors as `unknown`.

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-api && npx vitest run test/gladius/gladius.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All Gladius API routes respond correctly, inquiry persists to DB, integration tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Blog API routes, service, image upload, and integration tests</name>
  <files>
    el-templo-api/src/modules/blog/service.ts,
    el-templo-api/src/modules/blog/image-service.ts,
    el-templo-api/src/modules/blog/routes.ts,
    el-templo-api/.env.example,
    el-templo-api/test/blog/blog.test.ts
  </files>
  <behavior>
    - GET /api/blog/posts?page=1&limit=10 returns paginated published posts (title, slug, excerpt, coverImage, publishedAt, readingTime computed)
    - GET /api/blog/posts/:slug returns full published post with body (Markdown)
    - GET /api/blog/posts/:slug returns 404 for draft posts (public can't see drafts)
    - POST /api/admin/blog/posts (admin-only) creates draft post, returns 201
    - PUT /api/admin/blog/posts/:id (admin-only) updates post fields
    - PUT /api/admin/blog/posts/:id with status='published' sets published_at if not already set
    - DELETE /api/admin/blog/posts/:id (admin-only) deletes post
    - GET /api/admin/blog/posts (admin-only) returns all posts with status filter (?status=draft|published|all)
    - POST /api/admin/blog/upload-image (admin-only) returns presigned R2 URL for blog image upload
    - Non-admin users get 403 on admin routes
    - Reading time auto-calculated from word count (~200 words/min)
  </behavior>
  <action>
    Create Blog module:

    1. `blog/service.ts` — BlogService class:
       - Constructor takes `db` and `log`
       - `listPublishedPosts(page, limit)`: SELECT published posts ORDER BY published_at DESC. Return `{ posts, total, page, totalPages }`. Each post includes computed `readingTime` (Math.ceil(wordCount / 200)). Don't return body in list (only excerpt).
       - `getPublishedPostBySlug(slug)`: SELECT by slug WHERE status='published'. Return full post with body + computed readingTime. Return null if not found or draft.
       - `listAllPosts(page, limit, statusFilter?)`: Admin — all posts, optional status filter. ORDER BY updated_at DESC.
       - `createPost(data)`: INSERT with status='draft'. Auto-generate slug from title if not provided (same slugify as Gladius). Return created post.
       - `updatePost(id, data)`: UPDATE by id. If status changes to 'published' and published_at is null, set published_at=NOW(). If status changes back to 'draft', keep published_at (allows re-publish). Return updated post.
       - `deletePost(id)`: DELETE by id.
       - Helper: `computeReadingTime(body: string): number` — split body by whitespace, count words, Math.ceil(count / 200), minimum 1.

    2. `blog/image-service.ts` — BlogImageService class:
       - Constructor takes `r2` (S3Client), `bucket` (string), `log`
       - `generateUploadUrl(filename: string)`: Generate presigned PUT URL for R2.
         - R2 key: `blog/images/${Date.now()}-${sanitizedFilename}`
         - Content type inferred from extension (image/jpeg, image/png, image/webp, image/avif)
         - Presigned URL expiry: 15 minutes
         - Return `{ uploadUrl, publicUrl }` where publicUrl = `${R2_PUBLIC_URL}/${key}`
       - Reuse the presigned URL pattern from VideoService but simplified (no post-processing needed for images)

    3. `blog/routes.ts` — blogRoutes FastifyPluginAsync:
       - Public routes (no auth):
         - GET `/posts` — paginated published posts (query: page int default 1, limit int default 10 max 50)
         - GET `/posts/:slug` — single published post by slug
       - Admin routes (admin/superadmin only — NOT coach):
         - GET `/admin/posts` — all posts with status filter
         - POST `/admin/posts` — create post (body: title required, excerpt, coverImage, body, slug optional)
         - PUT `/admin/posts/:id` — update post (body: partial fields including status)
         - DELETE `/admin/posts/:id` — delete post
         - POST `/admin/upload-image` — generate presigned R2 URL for blog image (body: filename required). Guard: if !fastify.r2, return 503 "Image storage not configured"
       - Same role restriction pattern as Gladius admin routes

    4. Update `app.ts`:
       - Import `blogRoutes` from `./modules/blog/routes`
       - Register: `await app.register(blogRoutes, { prefix: "/api/blog" })`

    5. Update `.env.example` to document new env vars needed:
       - Add comment section for Gladius/Blog (GLADIUS_NOTIFICATION_EMAIL, R2_PUBLIC_URL if not already documented)

    6. `test/blog/blog.test.ts` — Integration tests:
       - Test GET /api/blog/posts returns empty array + pagination metadata
       - Test GET /api/blog/posts/:slug returns 404 for non-existent slug
       - Test public routes don't expose draft posts
       - Test pagination parameters work correctly
       - Clean up test data in afterEach
       - NOTE: Admin CRUD tested if admin auth available in test DB, otherwise test public routes

    Use Pino logger. No console.log. No `any` types. Error handling with `catch (err: unknown)`.

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-api && npx vitest run test/blog/blog.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All Blog API routes respond correctly, image upload returns presigned URL (or 503 if R2 not configured), integration tests pass</done>
</task>

</tasks>

<verification>
```bash
# All API tests pass
cd /home/franco/projects/el-templo/el-templo-api && pnpm test

# Type check passes

cd /home/franco/projects/el-templo/el-templo-api && npx tsc --noEmit

# Schema files exist and export correctly

grep -l "gladiusProducts\|gladiusInquiries\|blogPosts" el-templo-api/src/db/schema/\*.ts

# Routes registered in app.ts

grep "gladiusRoutes\|blogRoutes" el-templo-api/src/app.ts

# Migration file exists

ls el-templo-api/src/db/migrations/0019\_\*.sql

```
</verification>

<success_criteria>
1. Three new DB tables (gladius_products, gladius_inquiries, blog_posts) with correct schemas
2. Gladius public API: list products, get by slug, submit inquiry
3. Gladius admin API: CRUD products (admin/superadmin only)
4. Blog public API: paginated published posts, get by slug with body
5. Blog admin API: CRUD posts with draft/publish workflow, image upload via R2
6. All integration tests pass
7. TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/35-gladius-blog/35-01-SUMMARY.md`
</output>
```
