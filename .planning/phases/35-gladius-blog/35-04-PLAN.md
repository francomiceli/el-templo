---
phase: 35-gladius-blog
plan: 04
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - el-templo-web/pages/blog/index.vue
  - el-templo-web/pages/blog/[slug].vue
  - el-templo-web/components/BlogPostCard.vue
  - el-templo-web/components/BlogSidebar.vue
  - el-templo-web/components/BlogPagination.vue
autonomous: true
requirements:
  - BLOG-03
  - BLOG-04
  - BLOG-05
  - BLOG-06

must_haves:
  truths:
    - "/blog renders an index page with post cards showing title, excerpt, cover image, date, and reading time"
    - "/blog supports pagination with page numbers and unique URLs (/blog?page=2)"
    - "/blog/[slug] renders individual post with full Markdown-rendered content, cover image hero, and brand typography"
    - "Individual post pages have sidebar with recent posts, brand CTA, and social share buttons"
    - "Each blog post page has unique SEO meta tags (title, description, Open Graph)"
    - "Blog index page has SEO meta tags for the blog section"
    - "Empty blog state shows a friendly message when no posts exist"
  artifacts:
    - path: "el-templo-web/pages/blog/index.vue"
      provides: "Blog index with post list and pagination"
      min_lines: 50
    - path: "el-templo-web/pages/blog/[slug].vue"
      provides: "Individual blog post page with sidebar"
      min_lines: 80
    - path: "el-templo-web/components/BlogPostCard.vue"
      provides: "Post card component for index listing"
      min_lines: 30
    - path: "el-templo-web/components/BlogSidebar.vue"
      provides: "Sidebar with recent posts, CTA, share buttons"
      min_lines: 50
    - path: "el-templo-web/components/BlogPagination.vue"
      provides: "Page number navigation component"
      min_lines: 20
  key_links:
    - from: "el-templo-web/pages/blog/index.vue"
      to: "/api/blog/posts"
      via: "useFetch for SSG"
      pattern: "useFetch.*blog.*posts"
    - from: "el-templo-web/pages/blog/[slug].vue"
      to: "/api/blog/posts/:slug"
      via: "useFetch for SSG"
      pattern: "useFetch.*blog.*posts"
    - from: "el-templo-web/pages/blog/[slug].vue"
      to: "useHead"
      via: "dynamic SEO meta tags per post"
      pattern: "useHead|useSeoMeta"
---

<objective>
Build the public-facing blog pages in el-templo-web: a paginated index page with post cards and individual post pages with sidebar layout, social sharing, and per-post SEO meta tags.

Purpose: The blog establishes El Templo's authority in the calistenia/movement space, drives organic SEO traffic through long-form content targeting keywords like "calistenia", "entrenamiento peso corporal", and provides evergreen content for social sharing.

Output: /blog index page with pagination, /blog/[slug] post page with sidebar (recent posts + CTA + share), BlogPostCard component, per-post SEO meta tags.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/35-gladius-blog/35-CONTEXT.md
@.planning/phases/35-gladius-blog/35-01-SUMMARY.md

# Nuxt page patterns

@el-templo-web/pages/franquicias.vue
@el-templo-web/pages/index.vue

# Design system

@el-templo-web/assets/css/tokens.css

# Composables

@el-templo-web/composables/useScrollReveal.ts

<interfaces>
<!-- Blog public API from Plan 01 -->
From Blog API:
```typescript
// GET /api/blog/posts?page=1&limit=10
// Response: { posts: BlogPost[], total: number, page: number, totalPages: number }
interface BlogPost {
  id: number;
  title: string;
  slug: string;
  excerpt: string;
  coverImage: string | null;
  body: string; // Only in single post response
  status: 'draft' | 'published';
  createdAt: string;
  updatedAt: string;
  publishedAt: string | null;
  readingTime: number; // Computed: minutes
}

// GET /api/blog/posts/:slug
// Response: BlogPost (full, with body)

````

<!-- Nuxt patterns -->
```typescript
// useFetch for SSG data fetching (reactive, SSR-compatible)
// useHead / useSeoMeta for SEO meta tags
// useRuntimeConfig().public.apiUrl for API base URL
// NuxtLink for internal navigation
````

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Blog index page with post cards and pagination</name>
  <files>
    el-templo-web/pages/blog/index.vue,
    el-templo-web/components/BlogPostCard.vue,
    el-templo-web/components/BlogPagination.vue
  </files>
  <action>
    1. **`components/BlogPostCard.vue`** — Post card for index listing:
       - Props: `post` (BlogPost interface — title, slug, excerpt, coverImage, publishedAt, readingTime)
       - Layout: vertical card (full width of parent, stacked vertically per CONTEXT.md "vertical list")
         - Cover image area: PlaceholderBox (16:9 aspect ratio) or `<img>` if coverImage exists. Use lazy loading.
         - Content area:
           - Date formatted as "DD de Mes, YYYY" in Spanish (e.g., "01 de Marzo, 2026") using Intl.DateTimeFormat or manual formatting
           - Reading time: "{N} min de lectura"
           - Title: h2 in Montserrat SemiBold, linked to /blog/{slug}
           - Excerpt: Geologica Regular, Deep Charcoal, max 3 lines with text-overflow ellipsis
           - "Leer m&aacute;s" ghost CTA (Terracotta hover, arrow +4px on hover — same as IDEN-03 pattern)
       - Card styling: Marble Cream background, Warm Stone border-bottom, padding comfortable
       - Hover: subtle shadow elevation on entire card (300ms)
       - NuxtLink wrapping entire card for accessibility
       - BEM prefix: `blog-card`

    2. **`components/BlogPagination.vue`** — Page number navigation:
       - Props: `currentPage` (number), `totalPages` (number)
       - Emits: `page-change` with page number
       - Renders page numbers as NuxtLink to /blog?page={N} (SEO-friendly URLs, each page pre-renderable)
       - Show: First, Previous, current range (max 5 visible), Next, Last
       - Active page highlighted with Terracotta background
       - Disabled prev/next when at boundaries
       - BEM prefix: `blog-pagination`
       - Styling: centered, Geologica Medium, gap between buttons

    3. **`pages/blog/index.vue`** — Blog index page:
       - SEO meta via useHead:
         - title: "Blog El Templo Calistenia \u2014 Entrenamiento, M\u00E9todo y Movimiento"
         - description: "Art\u00EDculos sobre calistenia, entrenamiento con peso corporal, nutrici\u00F3n, movilidad y el m\u00E9todo El Templo."
         - og:url: "https://eltemplo.org/blog"
         - og:type: "website"
       - Read page from query params: `const page = computed(() => Number(route.query.page) || 1)`
       - Fetch posts: `useFetch` to GET /api/blog/posts?page={page}&limit=10
         - Use `useRuntimeConfig().public.apiUrl` as base URL
         - Watch page ref to refetch on page change
       - Layout:
         - Section header: H1 "BLOG" in Montserrat Bold + subtitle in Cormorant Garamond
         - Marble Cream background for consistency
         - Post list: vertical stack of BlogPostCard components
         - Pagination below post list
       - Empty state: when no posts, show friendly message "Pr\u00F3ximamente publicaremos contenido. Volv\u00E9 pronto." centered with brand typography
       - Loading state: skeleton placeholders while fetching (3 placeholder cards)
       - BEM prefix: `blog-index`
       - Uses default layout (same header/footer as all pages)

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-web && npx nuxi typecheck 2>&1 | tail -20</automated>
  </verify>
  <done>Blog index page renders post cards in vertical list, pagination works with page numbers and URL params, empty state displays correctly, SEO meta tags set</done>
</task>

<task type="auto">
  <name>Task 2: Individual blog post page with sidebar and per-post SEO</name>
  <files>
    el-templo-web/pages/blog/[slug].vue,
    el-templo-web/components/BlogSidebar.vue
  </files>
  <action>
    1. **`components/BlogSidebar.vue`** — Post page sidebar:
       - Props: `currentSlug` (string, to exclude current post from recent list)
       - **Recent posts section:**
         - H3 "Posts Recientes"
         - Fetch 5 most recent published posts: `useFetch` to /api/blog/posts?limit=5
         - Filter out current post by slug
         - Each item: NuxtLink with title + date, compact styling
         - BEM: `blog-sidebar__recent`
       - **Brand CTA section:**
         - "Reserv&aacute; tu Sesi&oacute;n de Prueba" Terracotta button linking to /#descubri-nivel
         - Small brand tagline below
         - BEM: `blog-sidebar__cta`
       - **Social share section:**
         - H3 "Compartir"
         - 3 share buttons:
           - WhatsApp: `https://wa.me/?text={encodedUrl}` (inline SVG icon, WhatsApp green)
           - Twitter/X: `https://twitter.com/intent/tweet?url={encodedUrl}&text={encodedTitle}` (inline SVG)
           - Copy link: button that copies current URL to clipboard (navigator.clipboard.writeText), shows "Copiado!" feedback via ref toggle
         - Icons: inline SVGs, 24x24, stroke-based
         - BEM: `blog-sidebar__share`
       - Sidebar styling: sticky top (offset for nav height: top: 80px), max-width 300px
       - BEM prefix: `blog-sidebar`

    2. **`pages/blog/[slug].vue`** — Individual post page:
       - Fetch post: `useFetch` to GET /api/blog/posts/{slug} where slug from route params
       - Handle 404: if post not found, show error page (use `showError` or custom 404 component)
       - **Per-post SEO meta** via `useSeoMeta` (dynamic, from fetched post data):
         - title: `{post.title} — Blog El Templo`
         - description: post.excerpt (truncated to 160 chars)
         - ogTitle: post.title
         - ogDescription: post.excerpt
         - ogImage: post.coverImage (if exists)
         - ogUrl: `https://eltemplo.org/blog/${post.slug}`
         - ogType: "article"
         - articlePublishedTime: post.publishedAt
         - This satisfies BLOG-05 (per-post meta tags, Open Graph, structured Article data)
       - **Layout: article (~70%) + sidebar (~30%)** per CONTEXT.md decision:
         - Desktop: CSS grid `grid-template-columns: 1fr 300px` with gap
         - Tablet: same grid but sidebar narrows
         - Mobile: single column, sidebar below article
       - **Post hero:**
         - Full-width cover image (or PlaceholderBox if no coverImage)
         - Below image: post title in Montserrat Bold (36px desktop, 28px mobile)
         - Date + reading time line: "01 de Marzo, 2026 \u2022 5 min de lectura"
       - **Article body:**
         - Render Markdown as HTML. Install `marked` in el-templo-web: `pnpm add marked` (or check if already available)
         - Use `marked.parse(post.body)` and render via `v-html`
         - Style the rendered HTML with brand typography:
           - h2: Montserrat SemiBold, 28px, Deep Charcoal, margin-top spacious
           - h3: Montserrat Medium, 22px
           - p: Geologica Regular, 17px, line-height 1.8, Deep Charcoal
           - a: Terracotta color, underline on hover
           - ul/ol: left padding, Geologica
           - img: max-width 100%, border-radius base, margin vertical comfortable
           - blockquote: left border 3px Terracotta, italic, padding-left comfortable, Cormorant Garamond
           - code: monospace, Sandy Beige background, padding 2px 6px, border-radius
           - pre > code: block, Sandy Beige background, padding comfortable, overflow-x auto
         - BEM scoping: `.blog-post__body` contains all these element styles
       - **Sidebar:** BlogSidebar component with currentSlug prop
       - BEM prefix: `blog-post`

    **Markdown rendering security note:** Since blog content is authored by admin users (not public), XSS risk is minimal. Still, consider `marked` with `sanitize: false` (default) since admin-authored content is trusted. If concerned, add DOMPurify later.

    **Reading time format:** "{N} min de lectura" — use the readingTime from API response.

    **Date formatting:** Use Spanish locale formatting. Create a small utility function `formatDate(dateString: string): string` that returns "DD de Mes, YYYY" format. Can use `new Date(dateString).toLocaleDateString('es-AR', { day: 'numeric', month: 'long', year: 'numeric' })`.

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-web && npx nuxi typecheck 2>&1 | tail -20</automated>
  </verify>
  <done>Individual post page renders with cover image hero, Markdown body as styled HTML, sidebar with recent posts + CTA + share buttons, per-post SEO meta tags, responsive sidebar layout</done>
</task>

</tasks>

<verification>
```bash
# Type check passes
cd /home/franco/projects/el-templo/el-templo-web && npx nuxi typecheck

# Build succeeds (checks SSG page generation)

cd /home/franco/projects/el-templo/el-templo-web && npx nuxi build 2>&1 | tail -20

# Blog pages exist

ls el-templo-web/pages/blog/index.vue el-templo-web/pages/blog/\\[slug\\].vue

# Blog components exist

ls el-templo-web/components/Blog\*.vue

# Markdown renderer available

grep "marked" el-templo-web/package.json

```
</verification>

<success_criteria>
1. /blog renders index page with post cards (title, excerpt, cover image, date, reading time) in vertical list
2. Pagination with page numbers generates SEO-friendly URLs (/blog?page=2)
3. /blog/[slug] renders individual post with full Markdown content styled in brand typography
4. Post page has sidebar with recent posts (3-5), brand CTA, and social share buttons (WhatsApp, Twitter/X, copy link)
5. Each post page has unique SEO meta tags (title, description, og:title, og:description, og:image, og:type: article)
6. Blog index has SEO meta tags for the blog section
7. Empty blog state shows friendly "Proximamente" message
8. Responsive: sidebar layout on desktop, stacked on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/35-gladius-blog/35-04-SUMMARY.md`
</output>
```
