---
phase: 110-admin-users-by-country-multi-branch-staff
plan: 09
type: execute
wave: 7
depends_on: [110-07, 110-08]
files_modified:
  - .planning/phases/110-admin-users-by-country-multi-branch-staff/110-VERIFICATION.md
autonomous: true
requirements_addressed: [REQ-8, REQ-11, REQ-12]

must_haves:
  truths:
    - "VERIFICATION.md exists with the 16 SPEC acceptance criteria as a pass/fail checklist"
    - "Smoke scenarios cover REQ-8 booking bypass HTTP-level (the it.todo from Plan 07 — heavyweight scenarios deferred to UAT; service-level minimal test is automated per Warning 3)"
    - "UAT prompts cover REQ-11 form per role (4 scenarios — admin / gestion / coach / recepción)"
    - "UAT prompts cover REQ-12 selector filtering (3 scenarios — admin AR / admin ES / coach)"
    - "Sign-off gate explicit: NO friday deploy reminder, all smoke scenarios PASS, decision IDs covered"
    - "§6 documents Blocker 4 dismissal: Nyquist VALIDATION.md not generated for this run because workflow gate Step 5.5 disabled it (research_enabled=false, has_research=false, no --research flag)"
  artifacts:
    - path: .planning/phases/110-admin-users-by-country-multi-branch-staff/110-VERIFICATION.md
      provides: "smoke + UAT checklist + decision coverage matrix + sign-off + Nyquist gate documentation"
      contains: "Acceptance Criteria"
  key_links:
    - from: .planning/phases/110-admin-users-by-country-multi-branch-staff/110-VERIFICATION.md
      to: SPEC.md acceptance criteria + CONTEXT.md decisions D-01..D-13
      via: "checklist mapping"
      pattern: "REQ-|D-"
---

<objective>
Create the phase-level VERIFICATION.md scaffold that the operator runs through manually before deploying to staging/production. Mirrors the Phase 109 P05 idiom (skip_checkpoints + handoff to operator).

Includes:

- 16 SPEC acceptance criteria as pass/fail checklist (status: PENDIENTE)
- Smoke scenarios for REQ-8 HTTP-level (the it.todo from Plan 07 — heavyweight scenarios; service-level was automated per Warning 3)
- UAT prompts for REQ-11 (form per role) + REQ-12 (selector filtering)
- Decision coverage matrix (D-01..D-13)
- Sign-off gate with NO friday deploy reminder
- §6 explicit documentation of Nyquist VALIDATION.md gate (Blocker 4 dismissal)

Purpose: Phase 110 closeout document. SPEC requires manual UAT for REQ-11/REQ-12 visual verification.
Output: 1 markdown file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/110-admin-users-by-country-multi-branch-staff/110-CONTEXT.md
@.planning/phases/110-admin-users-by-country-multi-branch-staff/110-SPEC.md
@.planning/phases/110-admin-users-by-country-multi-branch-staff/110-07-SUMMARY.md
@.planning/phases/110-admin-users-by-country-multi-branch-staff/110-08-SUMMARY.md
@.planning/phases/109-caja-v2-reportes/109-VERIFICATION.md

<interfaces>
Phase 109 P05 VERIFICATION.md format (precedent — STATE.md note):
  - Top section: "Smoke Pendiente — Handoff al Operador" (prominent block)
  - 16 SPEC acceptance criteria as `- [ ] REQ-X: <criterion>` checkboxes (PENDIENTE)
  - Smoke scenarios with reproducible steps + expected outcome
  - Decision coverage matrix: D-XX → "Covered by Plan NN Task M"
  - Sign-off section with NO friday deploy reminder ("NO viernes" appears 4x in Phase 109 idiom)

User memory (MEMORY.md):

- "Always ask before pushing"
- "Hotfixes go to staging AND master"
- "Staging-first workflow — STRICT"
- "App version bumps on production builds"

Phase 110 acceptance criteria (from SPEC.md §Acceptance Criteria — copy verbatim):

1. Migration SQL aplicada en local y staging sin errores
2. SELECT COUNT(\*) FROM users WHERE role IN ('admin','gestion') AND country IS NULL = 0
3. Cada coach/recepción tiene ≥ 1 fila en user_branches
4. Owners siguen accesibles con country IS NULL
5. request.scope incluye country, branchIds, isOwner, role (and userBranchId per Blocker 2 fix)
6. canAccessBranch admin AR pidiendo sede ES retorna false
7. canAccessBranch sede isVirtual=true retorna true
8. GET /api/admin/members?branchId=X con admin de país distinto retorna 403 BRANCH_OUT_OF_SCOPE
9. Endpoint para sede de Templo Online retorna 200 desde admin AR y desde admin ES
10. Coach con 2 user_branches puede operar sobre esas 2 + 403 sobre tercera
11. Staff (no-member) reserva en sede distinta sin plan multiBranch
12. Service rechaza con 400 al crear admin sin country
13. Service rechaza con 400 al crear coach con 0 user_branches
    (Plan 07 also covers REQ-9 5th case — member with branchIds → 400)
14. UsuariosPage muestra país para admin/gestion y multi-select para coach/recepción
15. Selector de sede en CajaPage muestra solo sedes accesibles (admin AR vs admin ES)
16. Tests integración pasan en el-templo-api/test/

Blocker 4 dismissal (per checker review):
Workflow plan-phase.md Step 5.5: "Nyquist is not applicable for this run when
all of the following are true: research_enabled is false, has_research is false,
no --research flag was provided. In that case: skip validation-strategy creation
entirely."
Phase 110 satisfies all three conditions: - research_enabled = false (init config for this run) - has_research = false (no RESEARCH.md in the phase dir) - --research flag not provided
Therefore VALIDATION.md is NOT required. The plan-checker dimension 8e flag
was a false positive for this run; documented in §6.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 110-VERIFICATION.md with smoke + UAT scenarios + decision matrix + Nyquist gate documentation</name>
  <files>.planning/phases/110-admin-users-by-country-multi-branch-staff/110-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/110-admin-users-by-country-multi-branch-staff/110-SPEC.md (acceptance criteria — copy verbatim)
    - .planning/phases/110-admin-users-by-country-multi-branch-staff/110-CONTEXT.md (D-01..D-13 to map in coverage matrix)
    - .planning/phases/110-admin-users-by-country-multi-branch-staff/110-07-SUMMARY.md (test count + REQ-8 it.todo split: service-level automated vs HTTP-level UAT)
    - .planning/phases/110-admin-users-by-country-multi-branch-staff/110-08-SUMMARY.md (UI single-seam audit results)
    - .planning/phases/109-caja-v2-reportes/109-VERIFICATION.md (format precedent)
  </read_first>
  <action>
**Create `.planning/phases/110-admin-users-by-country-multi-branch-staff/110-VERIFICATION.md`** with the following structure. Adapt to the actual file/path conventions used by Phase 109's VERIFICATION.md.

```markdown
# Phase 110 — VERIFICATION

**Status:** PENDIENTE  
**Smoke Pendiente — Handoff al Operador**

This document is the manual sign-off gate for Phase 110 (Admin users por país + multi-sede staff). All sections must be PASS before staging deploy. **NO viernes** — do NOT deploy this phase to production on a Friday.

## 1. Smoke Pendiente — Handoff al Operador

> **Read first:** Plan 07 (`110-07-PLAN.md`) covers REQ-5/6/7/9/10/12 in automated tests. REQ-8 (booking multibranch staff bypass) has TWO layers: (a) service-level minimal coverage IS automated in Plan 07 per Warning 3 (BookingService.reserve() called directly with a small seed); (b) HTTP-level heavyweight scenarios (POST /api/scheduling/bookings with full booking flow) are surfaced here as `it.todo` and verified manually in §3 Smoke Scenarios. REQ-11 (form per role) requires visual UAT — covered in §4.

### 1.1 Pre-flight checklist

- [ ] On a non-master local branch (NOT viernes)
- [ ] Migration 0107 already applied locally (Plan 02 PASS)
- [ ] `cd el-templo-api && pnpm test test/branch-access.test.ts` exits 0 (Plan 07 — incl. REQ-8 service-level + REQ-9 5th case)
- [ ] `cd el-templo-api && pnpm test test/country-scope.test.ts` exits 0 (regression — Phase 98 D-18 owner-without-toggle still resolves to own branch country)
- [ ] `cd el-templo-admin && pnpm tsc --noEmit && pnpm build` exits 0 (Plan 08)
- [ ] `cd el-templo-app && pnpm tsc --noEmit` exits 0 (no member-app changes expected — sanity)

## 2. Acceptance Criteria (from SPEC.md)

Copy of the 16 SPEC checkboxes — track PASS / FAIL here.

- [ ] **AC-1**: Migration SQL aplicada sin errores (`pnpm db:migrate` exit 0)
- [ ] **AC-2**: `SELECT COUNT(*) FROM users WHERE role IN ('admin','gestion') AND country IS NULL` = 0
- [ ] **AC-3**: Cada coach/recepción tiene ≥ 1 fila en `user_branches`
- [ ] **AC-4**: Owners con `country IS NULL`
- [ ] **AC-5**: `request.scope` incluye `country, branchIds, isOwner, role, userBranchId`
- [ ] **AC-6**: `canAccessBranch` admin AR → sede ES retorna `false`
- [ ] **AC-7**: `canAccessBranch` sede `isVirtual=true` retorna `true` para cualquier rol
- [ ] **AC-8**: `GET /api/admin/members?branchId=<ES>` con admin AR retorna 403 + `code: BRANCH_OUT_OF_SCOPE`
- [ ] **AC-9**: Endpoint con sede virtual retorna 200 desde admin AR Y desde admin ES
- [ ] **AC-10**: Coach con `user_branches=[A,B]` opera sobre A → 200; sobre B → 200; sobre C (otra AR) → 403
- [ ] **AC-11**: Staff (rol != member) reserva en sede distinta a `branch_id` sin plan `multiBranch` → 200 (service-level automated; HTTP-level §3 S-1)
- [ ] **AC-12**: Service rechaza con 400 al crear admin sin `country`
- [ ] **AC-13**: Service rechaza con 400 al crear coach con `branchIds=[]`
- [ ] **AC-13b** (REQ-9 4th rule, Blocker 1 fix): Service rechaza con 400 al crear member con `branchIds=[X]`
- [ ] **AC-14**: UsuariosPage muestra país para admin/gestion + multi-select para coach/recepción
- [ ] **AC-15**: Selector de sede en `CajaPage.vue` muestra solo sedes accesibles (admin AR vs admin ES)
- [ ] **AC-16**: Tests integración nuevos pasan en `el-templo-api/test/`

## 3. Smoke Scenarios (manual)

### S-1: REQ-8 booking multibranch staff bypass — HTTP-level (covers AC-11 heavyweight path)

**Goal:** confirm at the HTTP layer that a coach (or any staff role) using the member app can reservar bonus en otra sede sin plan multiBranch — and that members are still rejected. The service-level invariant is already automated in Plan 07 per Warning 3; this scenario validates the full POST /api/scheduling/bookings flow end-to-end.

**Setup:**

1. In local DB, find or create a coach user with role=coach, branchId=<sede A>.
2. Confirm the coach has NO active subscription with `multiBranch=true`. If they have any subscription, ensure plan.multiBranch=false.
3. Find or create a schedule slot in <sede B> ≠ <sede A>.

**Steps (member app, logged in as the coach):**

1. Open the bookings page.
2. Switch to <sede B> view.
3. Reserve a class in that sede.

**Expected:** 200 + booking row created.

**Regression check:**

1. Repeat with a member user (role=member) on a non-multiBranch plan.
2. Reserve the same way.

**Expected:** 400 with message "No podes reservar clases bonus en otra sucursal con tu plan actual".

- [ ] S-1 PASS

### S-2: AC-15 CajaPage selector — admin AR vs admin ES

**Setup:** ensure local DB has at least 1 AR sede + 1 ES sede + 1 virtual (Templo Online).

**Steps (admin app, run twice):**

Round A — log in as admin user with `country='AR'`:

1. Open CajaPage.
2. Click the sede selector dropdown.

**Expected:** the dropdown shows only AR sedes + Templo Online. NO ES sedes.

Round B — log in as admin user with `country='ES'`:

1. Same as above.

**Expected:** ES sedes + Templo Online. NO AR sedes.

- [ ] S-2 Round A PASS
- [ ] S-2 Round B PASS

### S-3: AC-15 same check, coach with limited user_branches

**Setup:** find or create a coach user with `user_branches = [<AR sede 1>, <virtual>]` (NOT <AR sede 2>).

**Steps (admin app, logged in as that coach):**

1. Open CajaPage.
2. Click the sede selector.

**Expected:** dropdown shows only <AR sede 1> + <virtual>. NOT <AR sede 2>. NOT ES sedes.

- [ ] S-3 PASS

## 4. UAT — REQ-11 form per role (covers AC-14)

Logged in as an owner user, open UsuariosPage and click "Nuevo usuario".

### 4.1 Admin role

1. Select role = "Admin".

**Expected:** form shows País (select AR/ES) + Sede (single-select). Does NOT show Sedes operativas (multi-select).

- [ ] 4.1 PASS

### 4.2 Gestion role

1. Select role = "Gestion".

**Expected:** form shows País + Sede. Does NOT show Sedes operativas.

- [ ] 4.2 PASS

### 4.3 Coach role

1. Select role = "Coach".

**Expected:** form shows Sede (single-select for sede personal de entrenamiento) + Sedes operativas (multi-select with chips). Does NOT show País.

- [ ] 4.3 PASS

### 4.4 Recepcion role

1. Select role = "Recepcion".

**Expected:** same as coach — Sede + Sedes operativas. NOT País.

- [ ] 4.4 PASS

### 4.5 Owner role

1. Select role = "Owner".

**Expected:** form shows Sede only. NOT País. NOT Sedes operativas (D-12: owner.country=NULL).

- [ ] 4.5 PASS

### 4.6 Validation — submit empty

1. Select role = "Admin", leave País empty, click Guardar.

**Expected:** inline error on País field. Form does NOT submit.

- [ ] 4.6 PASS

### 4.7 Validation — coach without sedes operativas

1. Select role = "Coach", pick Sede, leave Sedes operativas empty, click Guardar.

**Expected:** inline error "Requerido al menos una sede". Form does NOT submit.

- [ ] 4.7 PASS

### 4.8 End-to-end create — coach with 2 sedes

1. Create coach with branchId=<sede A>, branchIds=[<sede A>, <sede B>].
2. Confirm 201 response.
3. Run SQL: `SELECT user_id, branch_id FROM user_branches WHERE user_id = <id>`.

**Expected:** 2 rows.

- [ ] 4.8 PASS

## 5. Decision Coverage Matrix

Confirms each CONTEXT.md decision is implemented somewhere in the phase.

| Decision                                                        | Implementation                                                                                                             | Plan                      | Status |
| --------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- | ------------------------- | ------ |
| D-01 canAccessBranch helper                                     | `el-templo-api/src/modules/shared/branch-access.ts`                                                                        | 03 Task 2                 | [ ]    |
| D-02 requireBranchAccess({ from, optional? }) per route         | `requireBranchAccess` factory + per-route registration                                                                     | 03 Task 2 + 06 Tasks 1-3  | [ ]    |
| D-03 preHandler (403) + service (400) coexistence               | preserved inline 400 guards in finance/members/scheduling                                                                  | 06 Tasks 1-3              | [ ]    |
| D-04 403 (permission) + 400 (data) coexistence                  | both code paths exist                                                                                                      | 03 Task 2 + Phase 98 D-03 | [ ]    |
| D-05 body shape `{ error, message, code: BRANCH_OUT_OF_SCOPE }` | exact constant exported + sent (incl. inline GET /:userId guard harmonized — Warning 2)                                    | 03 Task 2 + 06 Task 1     | [ ]    |
| D-06 structured warn log                                        | `request.log.warn({...}, BRANCH_OUT_OF_SCOPE)`                                                                             | 03 Task 2                 | [ ]    |
| D-07 GET /admin/members/branches scope filter                   | rewrite with virtual concat                                                                                                | 06 Task 1                 | [ ]    |
| D-08 owner ?country= toggle                                     | preserved Phase 98 D-02 (incl. owner-without-toggle resolves to own branch country — Blocker 3 fix) + applied to /branches | 03 Task 1 + 06 Task 1     | [ ]    |
| D-09 virtual sedes always included                              | `b.isVirtual` short-circuits filter                                                                                        | 03 Task 2 + 06 Task 1     | [ ]    |
| D-10 staff CRUD owner-only                                      | OWNER_ROLES guard preserved                                                                                                | 05 Task 3                 | [ ]    |
| D-11 form pide rol→país→multi-select                            | UsuariosPage conditional fields                                                                                            | 08 Task 2                 | [ ]    |
| D-12 owner.country=NULL                                         | validateStaffCardinality + form hides País for owner                                                                       | 05 Task 2 + 08 Task 2     | [ ]    |
| D-13 user_branches IS security                                  | canAccessBranch Rule 4 returns false outside scope                                                                         | 03 Task 2 + 07 tests      | [ ]    |

## 6. SPEC R2 deviation + Nyquist gate documentation

### 6.1 SPEC R2 — composite PK vs autoincrement on user_branches

Plan 01 chose codebase convention (`id` autoincrement PK + unique index) over SPEC R2 literal text (composite PK on `(user_id, branch_id)`).

- [ ] Operator acknowledges deviation — uniqueness invariant preserved; production migration consistent with codebase patterns.

### 6.2 Nyquist VALIDATION.md not generated (Blocker 4 dismissal — false positive)

**Status:** N/A for this phase.

The plan-checker initially flagged this phase as missing `VALIDATION.md`. Per the GSD workflow `plan-phase.md` Step 5.5:

> Nyquist is not applicable for this run when all of the following are true:
>
> - research_enabled is false
> - has_research is false
> - no `--research` flag was provided
>   In that case: skip validation-strategy creation entirely.

Phase 110 satisfies all three conditions (init config disabled research; no RESEARCH.md exists in the phase dir; planning was invoked without `--research`). Therefore **VALIDATION.md is intentionally absent** and the plan-checker dimension 8e flag was a false positive for this run. No revision needed.

- [ ] Operator acknowledges that VALIDATION.md is N/A per the workflow gate (NOT a missing artifact).

### 6.3 REQ-8 HTTP-level coverage gap

REQ-8 has TWO test layers (Plan 07 SUMMARY documents this split per Warning 3):

- **Service-level (automated):** `BookingService.reserve()` called directly with minimal seed — verifies the role-based bypass at booking-service.ts:142.
- **HTTP-level (manual UAT):** full POST /api/scheduling/bookings flow with all the seed prerequisites (capacity, holds, attendance windows, etc.) — covered in §3 S-1.

- [ ] Operator confirms §3 S-1 PASS (HTTP-level scenario completes the REQ-8 coverage).

## 7. Sign-off

> Reminder: **NO viernes** deploy. Staging-first. Hotfixes go to staging AND master (memory rule). Always ask before pushing.

- [ ] All §2 acceptance criteria PASS
- [ ] All §3 smoke scenarios PASS
- [ ] All §4 UAT prompts PASS
- [ ] §5 decision coverage matrix complete
- [ ] §6.1 SPEC R2 deviation acknowledged
- [ ] §6.2 Nyquist VALIDATION.md N/A acknowledged (Blocker 4 dismissal)
- [ ] §6.3 REQ-8 HTTP-level scenario PASS
- [ ] **NO viernes**: today's date is NOT Friday
- [ ] STATE.md updated with Phase 110 entry
- [ ] CHANGELOG / RETROSPECTIVE entry drafted (if convention applies)

**Operator signature:** ************\_************  
**Date (NOT viernes):** ************\_************

---

_Phase: 110-admin-users-by-country-multi-branch-staff_  
_Verification scaffolded: <ISO date at task run>_
```

**Notes:**

- Replace `<ISO date at task run>` with the actual date when writing.
- The format is intentionally heavy on checkboxes — Phase 109 precedent (operator sign-off doc, not narrative).
- "NO viernes" appears multiple times deliberately (precedent: "NO viernes" appears 4× in Phase 109 P05 per STATE.md).
- §6.2 explicitly addresses Blocker 4 dismissal — the plan-checker false positive on Nyquist VALIDATION.md.
- §6.3 explicitly captures the REQ-8 two-layer split (service-level automated + HTTP-level UAT).
- Operator runs §3 smoke + §4 UAT; tests in Plan 07 cover §2 AC-5/6/7/8/9/10/11(service-level)/12/13/13b/16 automatically.
  </action>
  <verify>
  <automated>test -f .planning/phases/110-admin-users-by-country-multi-branch-staff/110-VERIFICATION.md && grep -c "NO viernes" .planning/phases/110-admin-users-by-country-multi-branch-staff/110-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria> - `test -f .planning/phases/110-admin-users-by-country-multi-branch-staff/110-VERIFICATION.md` exits 0 - `grep -c "AC-" .planning/phases/110-admin-users-by-country-multi-branch-staff/110-VERIFICATION.md` returns ≥ 16 (all SPEC criteria + AC-13b for REQ-9 4th rule) - `grep -c "S-1\|S-2\|S-3" .planning/phases/110-admin-users-by-country-multi-branch-staff/110-VERIFICATION.md` returns ≥ 3 (3 smoke scenarios) - `grep -c "4.1\|4.2\|4.3\|4.4\|4.5\|4.6\|4.7\|4.8" .planning/phases/110-admin-users-by-country-multi-branch-staff/110-VERIFICATION.md` returns ≥ 8 (8 UAT prompts) - `grep -c "D-0[1-9]\|D-1[0-3]" .planning/phases/110-admin-users-by-country-multi-branch-staff/110-VERIFICATION.md` returns ≥ 13 (all 13 decisions in matrix) - `grep -c "NO viernes" .planning/phases/110-admin-users-by-country-multi-branch-staff/110-VERIFICATION.md` returns ≥ 3 (precedent: appears multiple times) - `grep -c "BRANCH_OUT_OF_SCOPE" .planning/phases/110-admin-users-by-country-multi-branch-staff/110-VERIFICATION.md` returns ≥ 2 - `grep -c "Nyquist\|VALIDATION.md" .planning/phases/110-admin-users-by-country-multi-branch-staff/110-VERIFICATION.md` returns ≥ 2 (Blocker 4 dismissal documented in §6.2) - `grep -c "Step 5.5\|workflow gate" .planning/phases/110-admin-users-by-country-multi-branch-staff/110-VERIFICATION.md` returns ≥ 1 (workflow gate citation in §6.2) - `grep -c "AC-13b" .planning/phases/110-admin-users-by-country-multi-branch-staff/110-VERIFICATION.md` returns 1 (Blocker 1 — REQ-9 4th rule)
  </acceptance_criteria>
  <done>VERIFICATION.md scaffold complete; smoke + UAT + decision matrix in place; Nyquist gate dismissal documented in §6.2; REQ-8 two-layer split documented in §6.3; ready for operator sign-off.</done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                      |
| --------------------- | ------------------------------------------------ |
| operator → production | Operator signs off VERIFICATION.md before deploy |

## STRIDE Threat Register

| Threat ID   | Category               | Component                            | Disposition | Mitigation Plan                                                                                                                         |
| ----------- | ---------------------- | ------------------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| T-110-09-01 | Repudiation            | deploy without sign-off              | mitigate    | VERIFICATION.md is the audit trail; "NO viernes" prompt appears 3x; staging-first workflow per memory                                   |
| T-110-09-02 | Tampering              | smoke scenarios skipped              | mitigate    | §1 pre-flight + §7 sign-off explicitly require all sections PASS                                                                        |
| T-110-09-03 | Information Disclosure | smoke scenario reveals admin paths   | accept      | Doc lives in .planning/ (project-internal); not exposed externally                                                                      |
| T-110-09-04 | Repudiation            | VALIDATION.md absence misread as gap | mitigate    | §6.2 explicitly cites the workflow gate (Step 5.5) and the three preconditions for N/A status; Blocker 4 dismissal documented for audit |

</threat_model>

<verification>
- 16+ acceptance criteria + 3 smoke + 8 UAT + 13 decisions covered.
- "NO viernes" reminder appears multiple times.
- SPEC R2 deviation surfaced for explicit operator acknowledgment.
- §6.2 documents Nyquist VALIDATION.md gate (Blocker 4 dismissal — workflow Step 5.5).
- §6.3 documents REQ-8 two-layer test split (service-level automated, HTTP-level UAT).
</verification>

<success_criteria>

- VERIFICATION.md is the single document the operator runs through before staging deploy.
- Plan 07 it.todo (REQ-8 HTTP-level) is folded into §3 S-1 manual smoke; service-level is automated.
- All decisions trace to a plan + task.
- §6.2 makes the Nyquist gate decision auditable.
  </success_criteria>

<output>
After completion, create `.planning/phases/110-admin-users-by-country-multi-branch-staff/110-09-SUMMARY.md` and update `.planning/STATE.md` to reflect Phase 110 plans completed (orchestrator may handle the STATE.md update separately).
</output>
</content>
