---
phase: 170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci
plan: 03
type: execute
wave: 3
depends_on: [170-01, 170-02]
files_modified:
  - el-templo-api/src/db/scripts/lint-tenant.ts
  - el-templo-api/test/tenancy/con-06-lint.test.ts
  - el-templo-api/test/tenancy/__fixtures__/lint/
autonomous: true
requirements: [CON-06]

must_haves:
  truths:
    - "El lint resuelve el nombre de tabla fisica de un acceso escrito como schema.<identificador>, por AST con el compiler API de TypeScript y no por regex (D-10)"
    - "Un sql crudo y un .from()/.insert()/.update()/.delete() sobre tabla gym-owned sin tenant_id se detectan como acceso no cumplidor"
    - "tenantWhere y tenantValues cuentan como cumplimiento"
    - "El tag tenant-safe escrito en prosa (comentario // o dentro de un JSDoc) NO exime"
    - "Una exencion con motivo vacio NO exime"
    - "El lint valida tambien las exenciones existentes y emite su inventario completo en una salida revisable de una sola pasada (D-12)"
    - "El canal de exencion del lint es el codigo fuente: comentario de bloque anclado al call site por AST, sin exigir que viaje embebido en el SQL (D-17)"
  artifacts:
    - path: "el-templo-api/src/db/scripts/lint-tenant.ts"
      provides: "buildSchemaTableMap + lintTenantSources (motor AST, sin CLI todavia)"
      exports:
        [
          "buildSchemaTableMap",
          "lintTenantSources",
          "TenantAccess",
          "LintSourceResult",
        ]
      min_lines: 200
    - path: "el-templo-api/test/tenancy/con-06-lint.test.ts"
      provides: "Bateria del motor: mapa de schema, deteccion, cumplimiento, anclaje de exenciones"
      min_lines: 150
  key_links:
    - from: "el-templo-api/src/db/scripts/lint-tenant.ts"
      to: "el-templo-api/src/db/tenant-tables.ts"
      via: "import de isGymOwnedTable (nunca duplicar la lista de 87)"
      pattern: "isGymOwnedTable"
    - from: "el-templo-api/src/db/scripts/lint-tenant.ts"
      to: "typescript compiler API"
      via: "ts.createSourceFile + getLeadingCommentRanges / getTrailingCommentRanges"
      pattern: "createSourceFile"
---

<objective>
Construir el motor de analisis estatico del lint: el pase AST que (1) arma el mapa
identificador de schema a tabla fisica, (2) encuentra todo acceso a una tabla gym-owned
(template `sql` crudo y query builder), (3) decide si el acceso cumple (`tenant_id`,
`tenantWhere`, `tenantValues`) y (4) ancla las exenciones `/* tenant-safe: <motivo> */` al
sitio real del acceso por AST — cerrando el hallazgo 169-09 (el grep crudo autoriza 11
archivos cuando hay 9 exenciones reales).

Purpose: es la mitad "que esta mal" del lint. La mitad "esto rompe el build" (allowlist,
ratchet, CLI, exit codes) es el plan 05, que extiende este mismo archivo.
Output: `src/db/scripts/lint-tenant.ts` con `lintTenantSources()` exportada + su bateria
en `test/tenancy/con-06-lint.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-CONTEXT.md
@.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-RESEARCH.md
@.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-PATTERNS.md
@CLAUDE.md

**Trabajar en el worktree `/home/franco/projects/et-170-deteccion`** (creado en 170-01).

<interfaces>

```ts
Contrato que este plan crea (el plan 05 lo consume y lo extiende con la CLI):

  export interface TenantAccess {
    file: string;   // ruta RELATIVA a la raiz del repo, separadores "/". Clave de la allowlist (D-13)
    line: number;   // linea 1-based del acceso. Va en el reporte, NUNCA en la allowlist (D-13: sin lineas)
    table: string;  // nombre de tabla FISICA (el de GYM_OWNED_TABLES)
    kind: "sql-template" | "query-builder";
    compliant: boolean;   // usa tenant_id / tenantWhere / tenantValues
    exemption?: { motive: string; scope: "file" | "site"; line: number };
  }

  export interface LintSourceResult {
    filesScanned: number;
    accesses: TenantAccess[];
    violations: TenantAccess[];   // !compliant && sin exemption (la allowlist los filtra en el plan 05)
    exemptions: TenantAccess[];   // con exencion anclada, inventario de D-12
  }

  export function buildSchemaTableMap(schemaDir: string): ReadonlyMap<string, string>;

  export function lintTenantSources(opts: {
    rootDir: string;                        // raiz del repo
    scopeDirs?: string[];                   // default: alcance de la fase (ver Task 2)
    schemaMap?: ReadonlyMap<string, string>;
  }): LintSourceResult;

De src/db/tenant-tables.ts: isGymOwnedTable(name: string): boolean, GYM_OWNED_TABLES.
De src/modules/shared/tenant.ts: tenantWhere(table, scope) y tenantValues(scope, values),
la forma correcta que el lint premia (fase 169, firmas LOCKEADAS por el doc 03).
```

</interfaces>

<exemption_ground_truth>

```text
Sitios reales de origin/master contra los que el matcher se valida (RESEARCH, verificado
ejecutando el matcher sobre esos archivos):

ACEPTA:
  src/db/seed.ts                            file-level
  src/jobs/notification-cron.ts:754         leading de un ExpressionStatement
                                            (matchea DOBLE: statement + CallExpression interno,
                                             hace falta dedup por range.pos, Pitfall 7)
  src/modules/tv/pairing.ts:145             comentario TRAILING sobre .insert(schema.tvPairings)
  src/modules/wellhub/service.ts:135        leading de un VariableStatement
  src/db/scripts/verify-tenant-uniques.ts   file-level (debajo del docblock, comentario aparte)

RECHAZA (son los 2 falsos positivos del grep crudo, hallazgo 169-09):
  src/db/schema/tv.ts:81                    es un comentario de linea, no un bloque
  src/db/scripts/require-tenant.ts:44       el tag vive DENTRO de un JSDoc, no pegado a la apertura

Regla que separa exencion de prosa, con sus DOS condiciones necesarias:
  (a) range.kind === ts.SyntaxKind.MultiLineCommentTrivia
  (b) el tag arranca el comentario (pegado a la apertura del bloque), con motivo
      obligatorio despues de los dos puntos
```

</exemption_ground_truth>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mapa identificador a tabla fisica por AST (buildSchemaTableMap)</name>
  <files>el-templo-api/src/db/scripts/lint-tenant.ts</files>
  <read_first>
    - el-templo-api/src/db/scripts/verify-tenant-uniques.ts — COMPLETO. Molde del idioma de
      scripts del repo: docblock QUE PRUEBA / POR QUE NO ... / SOLO LECTURA / COMO SE CORRE
      + codigos de salida, la exencion tenant-safe propia del archivo (~:61-66), los imports
      desde ../tenant-tables, y el TENANT_COLUMN como unico literal del archivo.
    - el-templo-api/src/db/schema/index.ts y dos archivos de src/db/schema/ (por ejemplo tv.ts
      y uno grande) — para ver las formas reales de declaracion de mysqlTable.
    - .planning/phases/170-.../170-RESEARCH.md (Hallazgo 5 y Anti-patterns: el regex de una
      linea resuelve 21 de 92 declaraciones y levanta una tabla fantasma "foo" que vive
      dentro de un JSDoc — por eso es AST y no regex).
  </read_first>
  <action>
    Crear `src/db/scripts/lint-tenant.ts` con el docblock del molde de
    `verify-tenant-uniques.ts`, incluyendo: su propia exencion de archivo
    `/* tenant-safe: <motivo> */` como comentario de bloque APARTE debajo del docblock (el
    lint es tooling de plataforma, vive bajo src/ y se auto-analiza), un
    POR QUE NO ES UNA REGLA ESLINT (D-09: el API no tiene config ESLint y armarla es la
    pieza mas cara) y un POR QUE NO USA EL TYPE CHECKER (D-10: el pase sintactico sobre 382
    archivos tarda 606 ms medidos; ts.createProgram costaria lo mismo que un tsc --noEmit).

    Implementar `buildSchemaTableMap(schemaDir)`:
    - Listar recursivamente los .ts de `src/db/schema/`, excluyendo .d.ts.
    - Por archivo: ts.createSourceFile(path, text, ts.ScriptTarget.Latest, true) y recorrido
      con ts.forEachChild.
    - Reconocer las declaraciones de tabla como nodos: VariableDeclaration cuyo inicializador
      es un CallExpression cuya expresion se llama `mysqlTable` y cuyo primer argumento es un
      StringLiteral. Mapear el nombre del binding (identificador exportado) al literal de
      tabla fisica.
    - Dejar escrito en un comentario que el AST ignora gratis los `mysqlTable(` que viven
      dentro de comentarios/JSDoc, y que por eso este pase NO puede degradarse a un grep.
    - Devolver un ReadonlyMap<string, string>.

    Restricciones: `console.*` esta permitido en este archivo (es tooling CLI, precedente
    escrito en require-tenant.ts:70-72), pero este task todavia no imprime nada. Sin `any`.
    Manejo de errores con `catch (err: unknown)` + `instanceof Error` (CLAUDE.md).

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-170-deteccion/el-templo-api && pnpm exec tsc --noEmit && pnpm exec tsx -e 'import { buildSchemaTableMap } from "./src/db/scripts/lint-tenant"; const m = buildSchemaTableMap("src/db/schema"); if (m.size < 85 || m.get("tvPairings") !== "tv_pairings" || m.has("foo")) { console.error("MAP_FAIL size=" + m.size); process.exit(1); } console.log("MAP_OK size=" + m.size);'</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/scripts/lint-tenant.ts` exporta `buildSchemaTableMap`
    - El mapa construido sobre `src/db/schema` tiene al menos 85 entradas (hay 92 declaraciones de mysqlTable en origin/master)
    - `buildSchemaTableMap` mapea `tvPairings` a `tv_pairings` (caso camelCase a snake_case comprobado)
    - El mapa NO contiene la entrada fantasma `foo` que un regex de una linea levanta desde un JSDoc
    - El archivo importa `isGymOwnedTable` desde `../tenant-tables` y no declara ninguna lista de tablas propia
    - `pnpm exec tsc --noEmit` sale 0; cero `any` en el archivo
  </acceptance_criteria>
  <done>El lint sabe que `schema.tvPairings` es la tabla fisica `tv_pairings`, resolviendo las 92 declaraciones y no solo las 21 que un regex alcanza.</done>
</task>

<task type="auto">
  <name>Task 2: Deteccion de accesos, cumplimiento y anclaje de exenciones (lintTenantSources)</name>
  <files>el-templo-api/src/db/scripts/lint-tenant.ts</files>
  <read_first>
    - el-templo-api/src/db/scripts/lint-tenant.ts (lo escrito en el Task 1)
    - el-templo-api/src/modules/shared/tenant.ts (~:100-180) — `TenantContext`, `tenantWhere`,
      `tenantValues`: las firmas exactas que cuentan como cumplimiento
    - el-templo-api/src/modules/tv/pairing.ts (~:140-150) — exencion TRAILING real
    - el-templo-api/src/modules/wellhub/service.ts (~:130-140) — exencion leading de VariableStatement
    - el-templo-api/src/jobs/notification-cron.ts (~:748-760) — exencion que matchea DOBLE
    - el-templo-api/src/db/schema/tv.ts (~:75-85) y el-templo-api/src/db/scripts/require-tenant.ts
      (~:38-50) — los DOS casos de prosa que el matcher debe rechazar
    - .planning/phases/170-.../170-RESEARCH.md (Pattern 3 y Pitfall 7)
  </read_first>
  <action>
    Extender `lint-tenant.ts` con el pase de deteccion, exportando `lintTenantSources(opts)`
    con la forma declarada en `<interfaces>`.

    ALCANCE DE ARCHIVOS (decision explicita de la fase, D-16 + Pitfall 9 — escribirla en el
    docblock con el motivo): el lint analiza `el-templo-api/src/**/*.ts` y
    `el-templo-api/scripts/**/*.ts`, excluyendo `*.d.ts`. EXCLUYE `test/**` a proposito: hay
    228 archivos de test que escriben tablas gym-owned adrede para armar fixtures, y meterlos
    haria la allowlist inicial irrevisable; el ruido de los tests es problema de los fixtures
    2-tenant de la fase 171, que es el gate correcto para eso. Como D-16 hace el baseline
    one-shot, este alcance queda FIJADO: cambiarlo despues obliga a regenerar, que es
    justamente la puerta trasera que D-16 prohibe.

    DETECCION (dos formas de acceso):
    1. `kind: "query-builder"` — CallExpression cuyo nombre de metodo es `from`, `insert`,
       `update` o `delete` y cuyo primer argumento resuelve a una tabla del mapa de schema:
       o bien un PropertyAccessExpression sobre el identificador del namespace importado de
       `db/schema` (92 archivos usan `import * as schema`), o bien un Identifier que provenga
       de un import nombrado desde `db/schema` (4 archivos). Quedarse solo con las tablas que
       `isGymOwnedTable` acepta.
    2. `kind: "sql-template"` — TaggedTemplateExpression cuyo tag se llama `sql`. Tablas
       referenciadas = union de (a) los nombres que matchea, sobre la concatenacion de los
       spans literales del template, el mismo patron `from|join|into|update` seguido de
       identificador que usa el sentinel, filtrados por `isGymOwnedTable`; y (b) toda tabla
       del mapa de schema interpolada dentro del template. La union es fail-closed a
       proposito (sobre-reporta antes que sub-reportar) y ese motivo va escrito.

    CUMPLIMIENTO (`compliant`): el sitio cumple si el texto del statement que CONTIENE el
    acceso (el ancestro Statement mas cercano) menciona `tenantWhere(`, `tenantValues(`,
    `.tenantId` o el literal `tenant_id`. Documentar que `tenantWhere`/`tenantValues` son la
    forma canonica premiada (discrecion de Claude segun CONTEXT) y que el chequeo es de
    PRESENCIA, no de correccion — igual limitacion asumida que el sentinel.

    ANCLAJE DE EXENCIONES (Pattern 3, la parte que cierra el hallazgo 169-09):
    - Un comentario cuenta solo si `range.kind === ts.SyntaxKind.MultiLineCommentTrivia` Y el
      tag `tenant-safe:` arranca el comentario pegado al `/*`, con motivo no vacio despues de
      los dos puntos. Las dos condiciones son necesarias: (a) rechaza `schema/tv.ts:81`, que
      es un `//`; (b) rechaza `require-tenant.ts:44`, donde el tag esta dentro de un JSDoc.
    - Posiciones aceptadas: `ts.getLeadingCommentRanges(text, node.pos)` y
      `ts.getTrailingCommentRanges(text, node.end)` del statement que contiene el acceso y del
      propio CallExpression (el caso trailing de `pairing.ts:145` solo aparece asi).
    - Deduplicar por `range.pos` y exigir que el comentario caiga dentro del span del
      statement que contiene el acceso: sin eso, el mismo comentario matchea dos veces en
      `notification-cron.ts:754` (ExpressionStatement + CallExpression interno) y una exencion
      puede autorizar el call equivocado (Pitfall 7).
    - Exencion FILE-LEVEL: comentario de bloque con el tag valido en la trivia previa al
      primer statement del archivo. Exime todos los accesos del archivo con `scope: "file"`.
      El motivo escrito en el codigo debe registrar que file-level es de ALCANCE MAYOR (cubre
      codigo que todavia no se escribio) y que por eso el inventario lo reporta aparte (D-12,
      mitigacion de T-170-06).

    Devolver `LintSourceResult` con `accesses`, `violations` (no cumplidores sin exencion) y
    `exemptions` (con exencion), y `filesScanned`. Sin `any`; sin ejecutar ni importar
    dinamicamente el codigo analizado (el AST se lee, nunca se evalua).

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-170-deteccion/el-templo-api && pnpm exec tsc --noEmit && pnpm exec vitest run test/tenancy/con-06-lint.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lintTenantSources({ rootDir })` corrido sobre el repo devuelve `filesScanned` mayor a 350 y termina en menos de 10 s
    - Sobre el repo real, `exemptions` contiene entradas para `src/db/seed.ts`, `src/jobs/notification-cron.ts`, `src/modules/tv/pairing.ts` y `src/modules/wellhub/service.ts`
    - Sobre el repo real, NINGUNA exencion se atribuye a `src/db/schema/tv.ts` ni a `src/db/scripts/require-tenant.ts` (los 2 casos de prosa)
    - Ninguna exencion aparece duplicada para el mismo acceso (dedup por `range.pos` demostrado con `notification-cron.ts`)
    - El docblock del archivo contiene el alcance escrito (src y scripts, excluye test) con su motivo
    - `grep -c "eval\|import(" src/db/scripts/lint-tenant.ts` devuelve 0 (el lint no ejecuta lo que analiza)
    - `pnpm exec tsc --noEmit` sale 0
  </acceptance_criteria>
  <done>`lintTenantSources` devuelve, para el repo entero, la lista de accesos a tablas gym-owned con su cumplimiento y su exencion anclada — sin ejecutar una linea del codigo analizado.</done>
</task>

<task type="auto">
  <name>Task 3: Bateria del motor con fixtures + archivos reales (con-06-lint.test.ts)</name>
  <files>el-templo-api/test/tenancy/con-06-lint.test.ts, el-templo-api/test/tenancy/__fixtures__/lint/</files>
  <read_first>
    - el-templo-api/test/tenancy/con-03-write-paths-tenant-id.test.ts — idioma de test/tenancy:
      imports, docblock con QUE HACER CUANDO ESTE GUARD SE CAIGA, declaracion de "este
      describe no toca la DB", y la estructura de dos describe por archivo
    - el-templo-api/test/db/tenant-tables.test.ts (~:249-256) — precedente de "los helpers se
      prueban contra datos vivos, no contra un fixture inventado"
    - el-templo-api/src/db/scripts/lint-tenant.ts (Tasks 1 y 2)
  </read_first>
  <action>
    Crear los fixtures y la bateria del motor. DOS describe en el archivo:

    `describe("lint-tenant — motor sobre fixtures")`: un directorio
    `test/tenancy/__fixtures__/lint/` (mismo naming que `test/tv/__fixtures__/`), FUERA del
    alcance real del lint para que no contamine la allowlist (Pitfall 9). Extension de los
    fixtures: usar `.ts.txt` o un subdirectorio que el `tsconfig` no compile, de modo que
    `tsc --noEmit` no intente typechequear codigo deliberadamente roto; si se usan `.ts`
    reales, deben compilar limpio. Escribir en el fixture, como minimo:
      - un `.from(schema.<tabla gym-owned>)` sin filtro de tenant (violacion)
      - el mismo acceso con `tenantWhere(...)` (cumple)
      - un `.insert(schema.<tabla>).values(tenantValues(ctx, {...}))` (cumple)
      - un `sql` crudo con `from <tabla gym-owned>` sin tenant_id (violacion)
      - un `sql` crudo con `where tenant_id = ?` (cumple)
      - un acceso con exencion valida inline y otro con `/* tenant-safe: */` de motivo vacio
        (NO exime, sigue siendo violacion)
      - un acceso a una tabla NO gym-owned (no genera acceso)
    Afirmar el conteo y el contenido de `violations` y `exemptions` para ese rootDir de
    fixture — no solo el largo.

    `describe("lint-tenant — anclaje de exenciones contra los archivos reales")`: correr el
    matcher sobre los 6 archivos reales listados en `<exemption_ground_truth>` y afirmar las
    4 aceptaciones y los 2 rechazos por nombre de archivo. Comentar en el test que estos 2
    rechazos SON el hallazgo 169-09 (el grep crudo autoriza 11 archivos cuando hay 9
    exenciones) y que borrarlos reabre el agujero.

    Un `it` cuyo nombre contenga la frase "mapa de schema": al menos 85 entradas, `tvPairings`
    a `tv_pairings`, y ausencia de la entrada fantasma `foo`. Es la regresion permanente del
    chequeo que el Task 1 hizo en linea con `tsx -e`.

    Docblock del archivo con `QUE HACER CUANDO ESTE GUARD SE CAIGA`: "no borres la entrada de
    la allowlist ni desactives el lint — migra el modulo al patron o escribi la exencion con
    motivo". Declarar que estos describe NO tocan la base de datos (el setupFiles provisiona
    MySQL igual para todo archivo, ~96 s, hallazgo 169-07).

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-170-deteccion/el-templo-api && pnpm exec vitest run test/tenancy/con-06-lint.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run test/tenancy/con-06-lint.test.ts` sale 0
    - Existe un `it` cuyo nombre contiene "mapa de schema"
    - Existen aserciones nominales para las 4 aceptaciones y los 2 rechazos de exencion
    - Los fixtures viven bajo `test/tenancy/__fixtures__/lint/` y `lintTenantSources` sobre el rootDir real NO reporta accesos de ese directorio (verificable: ningun `file` de `accesses` empieza con `el-templo-api/test/`)
    - `pnpm exec tsc --noEmit` sale 0 con los fixtures presentes
    - Prueba negativa registrada en el SUMMARY: relajar el matcher a `getLeadingCommentRanges` sin el chequeo de `MultiLineCommentTrivia` hace que `src/db/schema/tv.ts` aparezca como exento y deja el test ROJO; revertir sin commitear
  </acceptance_criteria>
  <done>El motor del lint esta congelado por tests contra fixtures controlados y contra los sitios reales de origin/master, incluidos los 2 rechazos de prosa.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                     | Description                                                                                           |
| -------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| codigo del PR ↔ gate de CI                   | El lint decide si un acceso nuevo sin scope entra al repo                                             |
| comentario del autor ↔ autorizacion del gate | Un `tenant-safe:` es una autorizacion escrita a mano: si el matcher es laxo, cualquier prosa autoriza |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                                  | Disposition | Mitigation Plan                                                                                                                                               |
| --------- | ---------------------- | ------------------------------------------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-170-01  | Information Disclosure | acceso nuevo a tabla gym-owned sin `tenant_id`                                             | mitigate    | Deteccion AST de `sql` crudo y query builder, con la union fail-closed de tablas referenciadas                                                                |
| T-170-09  | Spoofing               | tag `tenant-safe:` escrito en prosa o en un JSDoc pasando por exencion                     | mitigate    | Dos condiciones necesarias (MultiLineCommentTrivia + tag pegado al `/*`) validadas contra los 2 rechazos reales; test nominal que falla si alguien las relaja |
| T-170-06  | Elevation of Privilege | exencion file-level autoriza escrituras futuras que nadie reviso                           | mitigate    | `scope: "file"` marcado en el resultado y reportado aparte en el inventario (D-12); motivo del alcance mayor escrito en el codigo                             |
| T-170-12  | Tampering              | el lint ejecuta el codigo que analiza (import dinamico) y un fixture malicioso corre en CI | mitigate    | Analisis puramente sintactico: `ts.createSourceFile` sin `createProgram`, sin `eval`, sin `import()` dinamico. Criterio de aceptacion con grep                |
| T-170-04  | Repudiation            | el mapa de tablas resuelve solo una parte y los accesos no resueltos pasan invisibles      | mitigate    | AST en vez de regex (92 vs 21 declaraciones) + criterio de aceptacion de al menos 85 entradas                                                                 |
| T-170-SC  | Tampering              | instalacion de paquetes (parser/helper de AST)                                             | mitigate    | Cero deps nuevas: el compiler API de TypeScript 5.9.3 ya es devDependency. Instalar algo = PARAR y preguntarle a Franco                                       |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit` sale 0
- `pnpm exec vitest run test/tenancy/con-06-lint.test.ts` verde
- El pase completo sobre el repo tarda menos de 10 s (benchmark del RESEARCH: 606 ms para 382 archivos)
- Las 4 aceptaciones y los 2 rechazos de exencion afirmados por nombre de archivo
</verification>

<success_criteria>

- `lintTenantSources()` clasifica accesos, cumplimiento y exenciones sobre el repo entero
- El hallazgo 169-09 queda cerrado: 9 exenciones reales, 0 falsos positivos de prosa
- Alcance de archivos fijado y escrito antes de generar cualquier baseline (D-16)
- Cero deps nuevas, cero migraciones
  </success_criteria>

<output>
Create `.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-03-SUMMARY.md` when done
</output>
