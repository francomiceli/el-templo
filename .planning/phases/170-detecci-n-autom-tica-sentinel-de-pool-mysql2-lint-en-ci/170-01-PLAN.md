---
phase: 170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/tenant-tables.ts
  - el-templo-api/test/db/tenant-tables.test.ts
autonomous: true
requirements: [CON-05, CON-06]

must_haves:
  truths:
    - "El sentinel y el lint leen UNA sola lista de módulos ya migrados, que en la fase 170 está vacía"
    - "Agregar a la lista strict una tabla que no existe en GYM_OWNED_TABLES deja la suite roja"
    - "Agregar una entrada a la lista strict es una decisión de diseño explícita, no un detalle de implementación"
  artifacts:
    - path: "el-templo-api/src/db/tenant-tables.ts"
      provides: "TENANT_STRICT_MODULES + isStrictTable + strictTablesSet"
      contains: "TENANT_STRICT_MODULES"
    - path: "el-templo-api/test/db/tenant-tables.test.ts"
      provides: "Gates de forma de la lista strict (D-05/D-06)"
      contains: "TENANT_STRICT_MODULES"
  key_links:
    - from: "el-templo-api/test/db/tenant-tables.test.ts"
      to: "el-templo-api/src/db/tenant-tables.ts"
      via: "import de TENANT_STRICT_MODULES / isStrictTable"
      pattern: "isStrictTable"
---

<objective>
Crear la fuente canónica única de "qué módulos ya están migrados al patrón de tenant"
(`TENANT_STRICT_MODULES`, D-05/D-06) dentro de `src/db/tenant-tables.ts`, con sus gates
de forma, y dejar el worktree de la fase listo sobre `origin/master`.

Es la pieza compartida: el **sentinel** la usa para decidir throw vs silencio (CON-05) y
el **lint** la usa para el gate de coherencia strict/allowlist (CON-06, D-15).

Purpose: sin esta lista, ni el sentinel ni el lint pueden distinguir "deuda conocida" de
"regresión". Arranca **vacía a propósito**: en la 170 no hay ningún módulo migrado.
Output: `TENANT_STRICT_MODULES`, `isStrictTable()`, `strictTablesSet()` + 5 gates nuevos
en `test/db/tenant-tables.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-CONTEXT.md
@.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-PATTERNS.md
@.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-RESEARCH.md
@CLAUDE.md
@.claude/skills/el-templo-change-control/SKILL.md

**Precondición dura de toda la fase 170:** el checkout principal
(`/home/franco/projects/el-templo`) está ~262 commits atrás de `origin/master` y es
compartido con otras sesiones. **Todo el trabajo de la fase 170 ocurre en el worktree
`/home/franco/projects/et-170-deteccion`** (creado en el Task 1 de este plan). Los
planes 02 y 05 dependen de este plan justamente porque necesitan ese worktree.

<interfaces>
Contratos existentes en `origin/master` que este plan extiende (NO redefinir):

De `el-templo-api/src/db/tenant-tables.ts`:
export const GYM_OWNED_TABLES = [...] as const; // 87 tablas
export type GymOwnedTable = (typeof GYM_OWNED_TABLES)[number];
const GYM_OWNED_SET: ReadonlySet<string> = new Set(GYM_OWNED_TABLES);
export function isGymOwnedTable(name: string): boolean; // acepta string a propósito
export const TENANT_GLOBAL_UNIQUES: Record<string, string>; // molde del registro nuevo
export const TENANT_UNIQUE_ALLOWLIST: Record<string, string>;
export function isTenantGlobalUnique(table: string, indexName: string): boolean;
export function isPlatformPhysicalTable(name: string): boolean;

Contrato que este plan AGREGA y que consumen los planes 03 (sentinel) y 06 (lint):
export const TENANT_STRICT_MODULES: Record<string, readonly string[]>;
export function isStrictTable(name: string): boolean;
export function strictTablesSet(): ReadonlySet<string>;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear el worktree de la fase sobre origin/master</name>
  <files>(ninguno del repo — crea /home/franco/projects/et-170-deteccion)</files>
  <read_first>
    - .claude/skills/el-templo-change-control/SKILL.md (§1 staging-first, §3 worktree)
    - .planning/phases/170-.../170-CONTEXT.md (§domain: "arrancar desde origin/master")
  </read_first>
  <action>
    Traer `origin` (`git fetch origin`) y crear el worktree de la fase con
    `git worktree add /home/franco/projects/et-170-deteccion -b feat/170-deteccion-automatica origin/master`
    — mismo idioma que `et-166-tenancy`, `et-167-columnas`, `et-168-contratos`,
    `et-169-tenant-layer`. Si el worktree ya existe, NO recrearlo: verificar que su HEAD
    desciende de `origin/master` y seguir.

    Materializar dependencias en el worktree con `pnpm install --frozen-lockfile` dentro
    de `/home/franco/projects/et-170-deteccion/el-templo-api`. `--frozen-lockfile` es
    obligatorio: esta fase instala CERO paquetes nuevos y NO puede modificar
    `pnpm-lock.yaml` (regla del repo: nunca instalar ni actualizar dependencias sin
    preguntar; precedente de supply chain de axios). Si el comando quisiera escribir el
    lockfile, PARAR y reportar.

    NO tocar el checkout principal. NO hacer push. NO mergear a `master` ni a `staging`.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-170-deteccion && git rev-parse --abbrev-ref HEAD | grep -qx "feat/170-deteccion-automatica" && git merge-base --is-ancestor origin/master HEAD && git status --porcelain -- el-templo-api/pnpm-lock.yaml | grep -c . | grep -qx 0 && echo WORKTREE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `git worktree list` incluye `/home/franco/projects/et-170-deteccion`
    - La rama del worktree es `feat/170-deteccion-automatica` y `origin/master` es ancestro de su HEAD
    - `git status --porcelain el-templo-api/pnpm-lock.yaml` no imprime nada (lockfile intacto)
    - `/home/franco/projects/et-170-deteccion/el-templo-api/node_modules/typescript/package.json` existe (el compiler API que usa el lint ya está)
    - Ningún commit en `master` ni en `staging`, ningún `git push` ejecutado
  </acceptance_criteria>
  <done>El worktree `et-170-deteccion` existe sobre `origin/master`, con dependencias instaladas y lockfile sin cambios.</done>
</task>

<task type="auto">
  <name>Task 2: TENANT_STRICT_MODULES + isStrictTable + strictTablesSet en tenant-tables.ts</name>
  <files>el-templo-api/src/db/tenant-tables.ts</files>
  <read_first>
    - el-templo-api/src/db/tenant-tables.ts — COMPLETO. Molde a copiar: el docblock y la
      forma de `TENANT_GLOBAL_UNIQUES` (~:205-253), el `GYM_OWNED_SET` module-level (~:175)
      y los helpers `isGymOwnedTable` (~:185) / `isPlatformPhysicalTable`.
    - .planning/phases/170-.../170-PATTERNS.md §`src/db/tenant-tables.ts` (forma esperada de D-06)
  </read_first>
  <action>
    Agregar al final de la sección de registros de `src/db/tenant-tables.ts` (después de
    `TENANT_UNIQUE_ALLOWLIST`, antes de `PLATFORM_PHYSICAL_TABLES` o al cierre del archivo
    — respetar el orden "registros primero, helpers después" que el archivo ya tiene):

    1. `export const TENANT_STRICT_MODULES: Record<string, readonly string[]> = {};`
       inicializado **vacío**, con docblock que diga literalmente: (a) qué significa
       "módulo migrado" (todas sus escrituras y lecturas usan `tenantWhere`/`tenantValues`
       de `src/modules/shared/tenant.ts`); (b) que el sentinel hace THROW en test/dev sobre
       las tablas de este registro y deja el resto en silencio (D-08); (c) que arranca
       vacía a propósito porque en la 170 no hay ningún módulo migrado y la primera entrada
       la agrega la fase 172 (`finance`); (d) que agregar una entrada OBLIGA a vaciar las
       entradas de esas tablas en `el-templo-api/tenant-lint-allowlist.json`, porque el
       lint hace rojo si conviven (D-15); (e) que la clave es el nombre del módulo
       (`finance`, `members`, `subscriptions`, `scheduling`, `analytics`) y el valor son
       nombres de tabla FÍSICA tal cual figuran en `GYM_OWNED_TABLES`.
    2. `const STRICT_SET: ReadonlySet<string> = new Set(Object.values(TENANT_STRICT_MODULES).flat());`
       — module-level, mismo patrón que `GYM_OWNED_SET`.
    3. `export function isStrictTable(name: string): boolean` — devuelve `STRICT_SET.has(name)`.
       Docblock con el mismo racional que `isGymOwnedTable`: acepta `string` a propósito
       porque el consumidor (el sentinel) clasifica nombres extraídos del SQL en runtime.
    4. `export function strictTablesSet(): ReadonlySet<string>` — devuelve `STRICT_SET`.
       Es el default del parámetro `strictTables` del sentinel (D-07: la lista es
       inyectable para que el test del criterio 1 pueda pasar una tabla real sin
       declararla migrada en el código).

    Actualizar la cabecera del módulo (~:1-51): la línea que ya anuncia
    "Fase 170 (ISO): sentinel de pool mysql2 y lint en CI" pasa a nombrar el registro
    concreto `TENANT_STRICT_MODULES` y sus dos consumidores (`src/db/sentinel/install.ts`
    y `src/db/scripts/lint-tenant.ts`).

    Sin `any` (CLAUDE.md). No tocar ninguna de las listas existentes.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-170-deteccion/el-templo-api && pnpm exec tsc --noEmit && grep -c "export const TENANT_STRICT_MODULES" src/db/tenant-tables.ts | grep -qx 1 && grep -c "export function isStrictTable" src/db/tenant-tables.ts | grep -qx 1 && grep -c "export function strictTablesSet" src/db/tenant-tables.ts | grep -qx 1 && echo STRICT_API_OK</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec tsc --noEmit` sale 0
    - `src/db/tenant-tables.ts` exporta exactamente `TENANT_STRICT_MODULES`, `isStrictTable`, `strictTablesSet`
    - `TENANT_STRICT_MODULES` está inicializado como objeto vacío (`= {}`), sin ninguna clave
    - El docblock de `TENANT_STRICT_MODULES` nombra explícitamente `tenant-lint-allowlist.json` y la fase 172
    - `git diff --stat el-templo-api/src/db/tenant-tables.ts` muestra solo adiciones + la línea de cabecera actualizada (cero deleciones de entradas existentes)
    - Cero ocurrencias nuevas de `any` en el archivo
  </acceptance_criteria>
  <done>`isStrictTable("bookings")` devuelve `false` hoy y devolverá `true` en cuanto la fase 172 agregue `finance` al registro, sin tocar ningún consumidor.</done>
</task>

<task type="auto">
  <name>Task 3: Gates de forma de la lista strict en test/db/tenant-tables.test.ts</name>
  <files>el-templo-api/test/db/tenant-tables.test.ts</files>
  <read_first>
    - el-templo-api/test/db/tenant-tables.test.ts — COMPLETO. Moldes a copiar: el gate de
      motivo obligatorio (~:204-233), el gate de conteo exacto como decisión de diseño
      (~:235-247), el gate de referencia cruzada contra `GYM_OWNED_TABLES` (~:173-188) y la
      declaración "este describe NO toca la base de datos" (~:17-20).
    - el-templo-api/src/db/tenant-tables.ts (ya editado en el Task 2)
  </read_first>
  <action>
    Agregar un `describe("TENANT_STRICT_MODULES (fase 170, D-05/D-06)")` al final del
    archivo, importando `TENANT_STRICT_MODULES`, `isStrictTable`, `strictTablesSet`,
    `GYM_OWNED_TABLES`. Copiar el idioma del archivo: mensaje largo y accionable como
    SEGUNDO argumento de `expect`, nunca un `toEqual([])` pelado. Cinco gates:

    1. "arranca vacía en la fase 170": `Object.keys(TENANT_STRICT_MODULES)` tiene largo 0,
       con mensaje que diga que agregar la primera entrada es una DECISIÓN DE DISEÑO de
       una fase de adopción (172+), que activa el throw del sentinel sobre esas tablas, y
       que obliga a vaciar sus entradas de `tenant-lint-allowlist.json` (D-15).
    2. "toda tabla listada existe en GYM_OWNED_TABLES": las tablas huérfanas se reportan
       con nombre de módulo y de tabla. Fail-closed: una tabla mal escrita nunca queda como
       "silenciosamente no-strict".
    3. "ninguna tabla aparece en dos módulos": duplicados reportados con los dos módulos
       que la reclaman (dos dueños = ambigüedad sobre quién debe vaciar la allowlist).
    4. "toda clave de módulo es un identificador no vacío en minúsculas" (regex
       `^[a-z][a-z0-9-]*$`), con el motivo escrito: la clave es el nombre del módulo del
       ROADMAP, no una frase.
    5. "isStrictTable y strictTablesSet coinciden con el registro": para cada tabla de
       `Object.values(TENANT_STRICT_MODULES).flat()`, `isStrictTable(tabla)` es `true` y
       `strictTablesSet().has(tabla)` es `true`; y `isStrictTable("bookings")` es `false`
       hoy (con el comentario de que esa última aserción cambia cuando la 172 migre
       `finance`, y que en ese momento la línea correcta es actualizar el ejemplo, no
       borrar el gate).

    Encabezar el describe con el comentario "este describe NO toca la base de datos: es
    introspección de objetos importados" (el `setupFiles` provisiona MySQL igual para todo
    archivo de test, ~96 s — hallazgo 169-07).

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-170-deteccion/el-templo-api && pnpm exec vitest run test/db/tenant-tables.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run test/db/tenant-tables.test.ts` sale 0 y reporta 5 tests nuevos, con los preexistentes en verde
    - Los 5 `expect` nuevos pasan un mensaje explicativo como segundo argumento
    - Prueba negativa registrada en el SUMMARY: agregar temporalmente `{ finance: ["tabla_que_no_existe"] }` a `TENANT_STRICT_MODULES` pone el gate 2 en ROJO con la tabla nombrada en el mensaje; revertir sin commitear el estado roto (idioma 168-05 / 169-04)
    - `pnpm exec tsc --noEmit` sale 0
  </acceptance_criteria>
  <done>La lista strict no puede crecer con una tabla inventada, con una tabla en dos módulos, ni en silencio: cualquiera de esas tres deja la suite roja con un mensaje que nombra la acción concreta.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                        | Description                                                                                    |
| ----------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| gimnasio A ↔ gimnasio B (mismas tablas físicas) | Toda query sin `tenant_id` cruza este límite y devuelve/escribe datos del gimnasio equivocado  |
| código commiteado ↔ gate de CI                  | Los registros de `tenant-tables.ts` son el input de confianza de los dos vigilantes de la fase |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                                           | Disposition | Mitigation Plan                                                                                                                                                                                                        |
| --------- | ---------------------- | --------------------------------------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-170-01  | Information Disclosure | queries sobre tablas gym-owned sin `tenant_id`                                                      | mitigate    | Este plan aporta el registro que habilita el throw del sentinel (planes 03/04) y el gate D-15 del lint (plan 06)                                                                                                       |
| T-170-04  | Repudiation            | `TENANT_STRICT_MODULES` con una tabla inexistente o duplicada                                       | mitigate    | Gates 2, 3 y 5 del Task 3: fail-closed, una tabla mal escrita rompe la suite en vez de quedar "no-strict" en silencio                                                                                                  |
| T-170-11  | Tampering              | Marcar un módulo como migrado sin haberlo migrado (throw activado sobre código que sigue sin scope) | accept      | Es una decisión de diseño de la fase de adopción; el gate 1 (conteo exacto 0) obliga a que el cambio sea explícito y revisable en el diff                                                                              |
| T-170-SC  | Tampering              | Instalación de paquetes (npm/pnpm)                                                                  | mitigate    | La fase instala CERO paquetes. `pnpm install --frozen-lockfile` en el Task 1 y `pnpm-lock.yaml` sin cambios como criterio de aceptación. Cualquier tentación de agregar una dependencia = PARAR y preguntarle a Franco |

</threat_model>

<verification>
- `cd /home/franco/projects/et-170-deteccion/el-templo-api && pnpm exec tsc --noEmit` → 0
- `pnpm exec vitest run test/db/tenant-tables.test.ts` → verde, 5 tests nuevos
- `git -C /home/franco/projects/et-170-deteccion diff --stat` toca exactamente 2 archivos
- Prueba negativa del gate 2 registrada y revertida
</verification>

<success_criteria>

- `TENANT_STRICT_MODULES`, `isStrictTable` y `strictTablesSet` exportados desde la única
  fuente canónica (`src/db/tenant-tables.ts`), sin duplicar `GYM_OWNED_TABLES`
- La lista arranca vacía y sus gates de forma están en verde
- El worktree `et-170-deteccion` está listo sobre `origin/master`, con lockfile intacto
- Cero migraciones de DB, cero dependencias nuevas
  </success_criteria>

<output>
Create `.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-01-SUMMARY.md` when done
</output>
