---
phase: 170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci
plan: 09
type: execute
wave: 9
depends_on: [170-03, 170-05, 170-07, 170-08]
gap_closure: true
files_modified:
  - el-templo-api/src/db/scripts/lint-tenant.ts
  - el-templo-api/test/tenancy/__fixtures__/lint/tipos.ts
  - el-templo-api/test/tenancy/__fixtures__/lint/accesos.ts
  - el-templo-api/test/tenancy/con-06-lint.test.ts
autonomous: true
requirements: [CON-06]

must_haves:
  truths:
    - "El motor del lint resuelve la tabla accedida a traves de un alias de variable local (`const u = schema.users; ... .from(u)`) y la reporta como acceso"
    - "El motor resuelve el alias de Drizzle guardado en variable (`const o = alias(schema.users, \"o\"); ... .from(o)`)"
    - "Un join (`innerJoin`/`leftJoin`/`rightJoin`/`fullJoin`) sobre una tabla gym-owned genera su propio par (archivo, tabla)"
    - "Los cuatro accesos sin scope reales de campaigns/service.ts (users, subscriptions, branches, campaign_unsubscribes) aparecen en el pase sobre el repo real"
    - "El fixture falla si el motor vuelve a quedar ciego a cualquiera de las tres formas"
  artifacts:
    - path: "el-templo-api/src/db/scripts/lint-tenant.ts"
      provides: "Resolucion de alias locales + metodos de join en TABLE_METHODS"
      contains: "locals"
    - path: "el-templo-api/test/tenancy/__fixtures__/lint/accesos.ts"
      provides: "Casos de fixture: alias de variable local, alias() en variable y join a segunda tabla gym-owned"
      contains: "innerJoin"
    - path: "el-templo-api/test/tenancy/con-06-lint.test.ts"
      provides: "Aserciones de las formas nuevas sobre el fixture y sobre el repo real (campaigns/service.ts)"
      contains: "campaigns/service.ts"
  key_links:
    - from: "el-templo-api/test/tenancy/con-06-lint.test.ts"
      to: "el-templo-api/src/db/scripts/lint-tenant.ts"
      via: "REAL_RESULT: el pase sobre el repo entero tiene que ver los accesos por alias local de campaigns/service.ts"
      pattern: "campaigns/service\\.ts"
    - from: "el-templo-api/src/db/scripts/lint-tenant.ts (tableOfExpression)"
      to: "collectSchemaBindings"
      via: "bindings.locals consultado cuando la expresion es un identificador"
      pattern: "locals"
---

<objective>
Cerrar el punto ciego CR-01: el motor AST del lint no resuelve los alias de variable local
a tablas del schema (`const u = schema.users; ... .from(u)`) ni registra la tabla joineada,
asi que un acceso NUEVO sin `tenant_id` escrito con cualquiera de esas dos formas deja el
build en VERDE. El verificador lo reprodujo en vivo y hay evidencia ya mergeada a staging
(`campaigns/service.ts` toca `users`, `subscriptions`, `branches` y `campaign_unsubscribes`
sin scope, invisibles al gate y ausentes de las 423 entradas de la allowlist).

Purpose: es el unico criterio del ROADMAP que la verificacion dejo FALLIDO (criterio 4,
CON-06). Sin este fix el gate promete algo que no cumple, y cada dia que pasa agranda la
deuda invisible que despues hay que re-baselinear.

Output: el motor ve las tres formas, con fixture y aserciones sobre el repo real que se
ponen rojas si alguna vuelve a quedar afuera; y la MEDICION exacta de los pares nuevos que
el fix destapa, que es el insumo del re-baseline del plan 10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-VERIFICATION.md
@.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-REVIEW.md
@.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-CONTEXT.md
@.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-08-SUMMARY.md
@CLAUDE.md

**Trabajar en el worktree `/home/franco/projects/et-170-sentinel`** (rama
`feat/170-sentinel-lint`). NO tocar el checkout principal `/home/franco/projects/el-templo`.

**Reglas operativas vigentes de la fase:**

- Cero migraciones, cero dependencias nuevas (`pnpm-lock.yaml` intacto).
- Los tests de `test/tenancy/` son MySQL-backed: **un solo archivo de vitest por vez** y
  `--hookTimeout=250000` por linea de comandos. **Nunca** tocar `vitest.config.ts` (el
  limite de 120 s es del WSL2 local, no del repo — hallazgo del plan 08).
- `git add` siempre por ruta explicita (skill `el-templo-change-control`). Nada de `-A`.
- No pushear nada. El push a staging es un gate humano que NO es de este plan.

<motor_anchors>

```text
el-templo-api/src/db/scripts/lint-tenant.ts — estado actual (post plan 08)

:478  const TABLE_METHODS = new Set(["from", "insert", "update", "delete"]);
      -> WR-01: sin los joins, la tabla joineada nunca genera su par (archivo, tabla).

:598  interface SchemaBindings { namespaces: Set<string>; named: Map<string,string>; }
:605  function collectSchemaBindings(sourceFile, schemaMap): SchemaBindings
      -> HOY solo recorre `sourceFile.statements` buscando ImportDeclaration.
         `import * as schema` -> namespaces ; `import { users }` -> named.

:732  const tableOfExpression = (expression): string | undefined => { ... }
      (closure por archivo, dentro de lintTenantSources)
      caso 1: PropertyAccess sobre un namespace del schema  -> schemaMap.get(prop)
      caso 2: Identifier                                    -> bindings.named.get(text)
      caso 3: CallExpression con >=1 argumento              -> recursion sobre arguments[0]
      -> CR-01: un identificador ligado por `const u = schema.users` NO esta en `named`,
         asi que el caso 2 devuelve undefined y el acceso es INVISIBLE.

:753  collectTablesInExpression(node, out)  -> usa tableOfExpression sobre cada hijo:
      es el camino por el que las interpolaciones `sql`...${s}...`` resuelven (o no).
:805  visitForAccesses: TABLE_METHODS -> record(query-builder) ; sql`` -> record(sql-template)
:584  isSchemaModule()  -> el punto ciego GEMELO que el plan 08 cerro (imports profundos).
      Su docblock (:546-583) es el modelo de como se documenta un punto ciego cerrado.
```

</motor_anchors>

<evidencia_viva>

```text
el-templo-api/src/modules/campaigns/service.ts, metodo listEligible (~:64-108)

  const u = schema.users;              const s  = schema.subscriptions;
  const b = schema.bookings;           const unsub = schema.campaignUnsubscribes;
  const br = schema.branches;
  ...  sql`NOT EXISTS (SELECT 1 FROM ${s} ...)`      <- interpolacion de un alias local
  ...  .from(u)                                      <- users, sin tenant_id
  ...  .innerJoin(br, eq(br.id, u.branchId))         <- branches, sin tenant_id

Allowlist actual (423 entradas): campaigns/service.ts tiene bookings, campaigns,
campaign_events, campaign_sends y user_status_history. NO tiene users, subscriptions,
branches ni campaign_unsubscribes: el motor nunca los vio.
```

</evidencia_viva>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fixture y aserciones de las tres formas ciegas (RED)</name>
  <files>el-templo-api/test/tenancy/__fixtures__/lint/tipos.ts, el-templo-api/test/tenancy/__fixtures__/lint/accesos.ts, el-templo-api/test/tenancy/con-06-lint.test.ts</files>
  <read_first>
    - el-templo-api/test/tenancy/__fixtures__/lint/accesos.ts — COMPLETO (61 lineas): el
      docblock declara "Cinco accesos esperados en este archivo, dos de ellos violacion" y
      cada funcion lleva su veredicto escrito. La regla del archivo es explicita: si tocas
      una funcion, actualizas su asercion, no al reves.
    - el-templo-api/test/tenancy/__fixtures__/lint/tipos.ts — `FakeDb` no tiene metodos de
      join todavia; `FakeCtx` es el `TenantContext` en chiquito.
    - el-templo-api/test/tenancy/con-06-lint.test.ts — al menos: el helper `resumir()`
      (:113), el describe "motor sobre fixtures" (:132-254) con los `it` de conteo que van a
      cambiar, y el `it` "ve los archivos que importan el schema EN PROFUNDIDAD" (:328-362),
      que es el MODELO exacto de asercion de regresion sobre el repo real que hay que
      replicar para el alias local.
    - .planning/phases/170-.../170-REVIEW.md — CR-01 y WR-01 con el caso vivo.
    - el-templo-api/src/modules/campaigns/service.ts (~:60-112) — la evidencia viva.
  </read_first>
  <action>
    Escribir PRIMERO los casos que hoy fallan. Al terminar este task la bateria del fixture
    tiene que estar ROJA, y ese rojo es el entregable: es la prueba de que el fixture
    detecta el punto ciego y no lo describe nomas.

    1. `tipos.ts`: sumar a `FakeDb` los metodos `innerJoin(table: unknown, on?: unknown)` y
       `leftJoin(table: unknown, on?: unknown)`, ambos devolviendo `FakeDb`. Nada mas — el
       motor es un pase sintactico y el doble solo tiene que hacer que las formas compilen.

    2. `accesos.ts`: sumar cuatro funciones exportadas, cada una con su veredicto en el
       docblock, siguiendo el estilo de las cinco que ya estan:
       - alias de variable local a una tabla gym-owned que HOY no aparece en este archivo
         (usar `schema.attendance`): asignarla a un `const` y hacer `.from(<alias>)` sin
         filtro. Veredicto: VIOLACION, tabla `attendance`, kind `query-builder`.
       - alias de Drizzle guardado en variable: `alias(schema.subscriptions, "s")` asignado
         a un `const` y despues `.from(<alias>)` sin filtro. Importar `alias` desde
         `drizzle-orm/mysql-core`. Veredicto: VIOLACION, tabla `subscriptions`.
       - join a una SEGUNDA tabla gym-owned sin filtro: `.from(schema.bookings)` encadenado
         con `.innerJoin(schema.users, ...)`. Veredicto: DOS violaciones, `bookings` (por el
         from) y `users` (por el join) — el par nuevo del ratchet es el del join (WR-01).
       - el mismo join PERO con `tenantWhere(...)` en el `.where(...)` del mismo statement.
         Veredicto: los dos accesos CUMPLEN. Este caso es el que prueba que sumar los joins
         no inventa rojos donde el sitio si nombra al gimnasio.
       Actualizar el docblock de cabecera del fixture con el conteo nuevo de accesos y de
       violaciones esperadas.

    3. `con-06-lint.test.ts`:
       - Actualizar el `it` "clasifica las cinco formas de acceso de accesos.ts" (nombre
         incluido) y el `it` "el fixture tiene exactamente 4 violaciones y 4 accesos
         eximidos" con los conteos y las lineas nuevas de `resumir()`. **El orden exacto de
         la lista lo dicta el recorrido del AST (el nodo mas externo del encadenado se
         visita ANTES que el `.from()` interno): derivalo corriendo el test y leyendo el
         diff de la asercion, no lo adivines.** Cada linea nueva lleva su comentario, igual
         que las cinco actuales.
       - Sumar al describe "anclaje de exenciones contra los archivos reales" (o al que
         corresponda por cercania semantica) un `it` de regresion sobre `REAL_RESULT`,
         calcado del de imports profundos: afirmar que
         `el-templo-api/src/modules/campaigns/service.ts` tiene accesos para las cuatro
         tablas `users`, `subscriptions`, `branches` y `campaign_unsubscribes`. El mensaje
         de la asercion tiene que decir por que: son las dos tablas ancla de la fase 166 mas
         las otras dos del mismo metodo, todas invisibles al gate hasta este fix, y si el
         numero baja el punto ciego volvio. NO relajar esta asercion nunca: el arreglo es el
         motor.
       - No tocar el describe de los exit codes ni el `it` "ningun test de esta bateria
         escribio sobre la allowlist real".

    Correr la bateria y REGISTRAR el rojo literal (nombre de cada `it` que falla y el diff
    de la asercion) para el SUMMARY: es la evidencia de que los casos nuevos discriminan.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-170-sentinel/el-templo-api && pnpm exec vitest run test/tenancy/con-06-lint.test.ts --hookTimeout=250000 -t "fixture" > /tmp/claude-1000/-home-franco-projects-el-templo/235d4b73-8a62-4520-b464-cbe8222f93b9/scratchpad/red-09.txt 2>&1; test $? -ne 0 && echo "RED_OK: el fixture nuevo falla contra el motor ciego" || (echo "RED_FALTANTE: el fixture pasa sin el fix, no discrimina"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `accesos.ts` exporta las cuatro funciones nuevas y cada una tiene su veredicto escrito en el docblock
    - `tipos.ts` declara `innerJoin` y `leftJoin` en `FakeDb`
    - La corrida de vitest sobre `con-06-lint.test.ts` filtrada por "fixture" sale con exit distinto de 0 ANTES del Task 2, y el SUMMARY cita los nombres de los `it` que fallan
    - Existe un `it` que afirma sobre `REAL_RESULT` las cuatro tablas de `campaigns/service.ts` (`users`, `subscriptions`, `branches`, `campaign_unsubscribes`)
    - `git diff --stat` de este task toca exactamente 3 archivos, todos bajo `el-templo-api/test/`
  </acceptance_criteria>
  <done>El fixture y las aserciones describen las tres formas ciegas y estan rojas: el punto ciego quedo capturado por un test antes de existir el arreglo.</done>
</task>

<task type="auto">
  <name>Task 2: El motor resuelve alias locales y registra los joins (GREEN)</name>
  <files>el-templo-api/src/db/scripts/lint-tenant.ts</files>
  <read_first>
    - el-templo-api/src/db/scripts/lint-tenant.ts — COMPLETO el bloque :473-900 (deteccion,
      bindings, el pase) y el docblock de `isSchemaModule` (:546-583), que es el modelo de
      como este archivo documenta un punto ciego cerrado: que patron era, que consecuencia
      tenia y por que el arreglo va donde va.
    - .planning/phases/170-.../170-REVIEW.md — CR-01 propone llamar a `tableOfExpression`
      sobre el inicializador; leer tambien WR-01.
    - el-templo-api/test/tenancy/con-06-lint.test.ts — las aserciones del Task 1: son el
      contrato que este task tiene que poner en verde.
  </read_first>
  <action>
    Cerrar las dos formas ciegas en el motor, con la misma filosofia sintactica del resto
    del archivo (D-10: sin type checker, sin resolver simbolos).

    1. `TABLE_METHODS` (:478): sumar `innerJoin`, `leftJoin`, `rightJoin` y `fullJoin`.
       Actualizar el comentario de la constante para que diga que los joins tambien reciben
       una tabla y que su par (archivo, tabla) es propio — que es la razon de WR-01: la
       clave del ratchet es el par, no el statement.

    2. `SchemaBindings` (:598): sumar el campo `locals: Map<string, string>` (identificador
       local -> tabla fisica), documentado.

    3. `collectSchemaBindings` (:605): despues del pase de imports que ya existe, recorrer
       el archivo ENTERO con `ts.forEachChild` recursivo buscando
       `ts.isVariableDeclaration(node) && ts.isIdentifier(node.name) && node.initializer`, y
       resolver el inicializador con un resolver ACOTADO propio (no el `tableOfExpression`
       general) que acepte exactamente tres formas:
       (a) acceso a propiedad sobre un namespace del schema -> `schemaMap.get(<prop>)`;
       (b) identificador ya ligado -> `named` primero, `locals` despues (el recorrido va en
           orden de fuente, asi que una cadena `const a = schema.users; const b = a;`
           resuelve; una declarada al reves no, y esta bien: fail-closed es no resolver);
       (c) llamada cuyo `calleeName(...)` sea exactamente `alias` y tenga al menos un
           argumento -> recursion sobre `arguments[0]`.
       **NO** reusar el fallback generico "cualquier CallExpression -> primer argumento" en
       la posicion de inicializador. Ese fallback existe solo en la posicion de argumento de
       `TABLE_METHODS`, donde sobre-reportar es gratis. En un inicializador ligaria cosas
       como `const q = db.select().from(schema.users)` al nombre `q`, y cada par inventado
       asi entra al baseline one-shot que D-16 congela: acá sobre-reportar NO es gratis.
       Dejar ese razonamiento escrito en el docblock de la funcion.

    4. `tableOfExpression` (:742, caso identificador): consultar `bindings.named` y despues
       `bindings.locals`.

    5. Docblock: agregar una seccion breve al bloque de cabecera del archivo (o extender la
       de `isSchemaModule`) que registre esta TERCERA clase de punto ciego cerrado — alias
       de variable local — con su evidencia (`campaigns/service.ts`, las dos tablas ancla de
       la fase 166 invisibles al gate) y la limitacion asumida que queda: el mapa de locales
       es por archivo y no modela scopes, asi que una variable local que le pise el nombre a
       otra sobre-reporta. Sobre-reportar es recuperable; no ver un acceso, no.

    Al terminar: `pnpm exec tsc --noEmit` limpio y la bateria del fixture en verde. Los `it`
    del repo real que dependen del baseline viejo quedan ROJOS a proposito (ver Task 3).
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-170-sentinel/el-templo-api && pnpm exec tsc --noEmit && grep -v "^\s*\*" src/db/scripts/lint-tenant.ts | grep -q "innerJoin" && grep -v "^\s*\*" src/db/scripts/lint-tenant.ts | grep -q "locals" && pnpm exec vitest run test/tenancy/con-06-lint.test.ts --hookTimeout=250000 -t "fixture" && echo GREEN_FIXTURE</automated>
  </verify>
  <acceptance_criteria>
    - `TABLE_METHODS` contiene `innerJoin`, `leftJoin`, `rightJoin` y `fullJoin`
    - `SchemaBindings` tiene `locals` y `collectSchemaBindings` lo puebla recorriendo las declaraciones de variable de todo el archivo
    - El resolver de inicializadores acepta solo las tres formas (namespace.prop, identificador ya ligado, llamada a `alias`) y el docblock explica por que NO usa el fallback generico de CallExpression
    - `tableOfExpression` consulta `locals` cuando la expresion es un identificador
    - `pnpm exec tsc --noEmit` sale 0
    - Los `it` del describe "motor sobre fixtures" pasan todos
    - El docblock del archivo registra el punto ciego cerrado, su evidencia viva y la limitacion de scope asumida
  </acceptance_criteria>
  <done>Las tres formas que dejaban el build en verde ahora producen accesos: el motor ve por alias local, por `alias()` guardado en variable y por join.</done>
</task>

<task type="auto">
  <name>Task 3: Medicion del delta que destapa el fix</name>
  <files>(ninguno del repo — la salida va al SUMMARY)</files>
  <read_first>
    - el-templo-api/tenant-lint-allowlist.json — las 423 entradas actuales y el campo
      `generated` (el idioma con que el plan 08 documento el re-baseline anterior).
    - .planning/phases/170-.../170-08-SUMMARY.md — la tabla "Lente estatica: antes/despues"
      (entradas, accesos violadores, archivos con deuda, tablas con deuda, entradas
      perdidas). Este task produce la MISMA tabla para este fix.
  </read_first>
  <action>
    Medir exactamente cuanta deuda pre-existente destapa el fix. Es el insumo del
    re-baseline del plan 10 y la unica forma de que Franco apruebe con numeros y no con
    adjetivos.

    1. Correr `pnpm lint:tenant` (sin `--base`) y registrar: exit code (va a ser 1),
       cantidad de `unlistedViolations`, cantidad de PARES (archivo, tabla) distintos no
       listados, y cuantos archivos y cuantas tablas distintas involucran.
    2. Producir la tabla antes/despues con las cuatro filas del plan 08 (entradas de la
       allowlist, accesos violadores, archivos con deuda, tablas gym-owned con deuda) mas la
       fila "entradas perdidas", que tiene que dar 0: el fix solo puede SUMAR pares.
    3. Confirmar nominalmente los cuatro pares vivos de `campaigns/service.ts` (`users`,
       `subscriptions`, `branches`, `campaign_unsubscribes`) entre los pares nuevos.
    4. Spot-check honesto de 5 pares nuevos elegidos de archivos distintos: abrir el archivo,
       encontrar el acceso, y confirmar en el SUMMARY que es deuda REAL pre-existente (o
       sea: el acceso ya estaba, sin `tenant_id`, y el motor no lo veia) y no un falso
       positivo del resolver nuevo. Si alguno resulta falso positivo, PARAR y acotar el
       resolver del Task 2 antes de seguir: un falso positivo que entra al baseline queda
       congelado por D-16.
    5. Dejar escrito en el SUMMARY, para el plan 10: que el re-baseline es correccion del
       MISMO baseline one-shot defectuoso (el motor no veia estos pares cuando se genero),
       no un regenerador nuevo; y que el gate D-14 de entradas ganadas va a dar ROJO una
       sola vez, en el push que lleve el re-baseline a staging, porque `origin/staging` ya
       tiene la allowlist de 423 entradas como base del evento.

    Ningun archivo del repo se modifica en este task. Cualquier script auxiliar va al
    directorio de scratchpad de la sesion, NUNCA dentro del repo.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-170-sentinel/el-templo-api && pnpm lint:tenant > /tmp/claude-1000/-home-franco-projects-el-templo/235d4b73-8a62-4520-b464-cbe8222f93b9/scratchpad/delta-09.txt 2>&1; test $? -eq 1 && grep -q "unlistedViolations" /tmp/claude-1000/-home-franco-projects-el-templo/235d4b73-8a62-4520-b464-cbe8222f93b9/scratchpad/delta-09.txt && cd /home/franco/projects/et-170-sentinel && test -z "$(git status --porcelain -- el-templo-api)" && echo "DELTA_MEDIDO_Y_ARBOL_LIMPIO"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm lint:tenant` sale 1 y el reporte lista violaciones no cubiertas por la allowlist actual
    - El SUMMARY tiene la tabla antes/despues con las cinco filas y "entradas perdidas = 0"
    - El SUMMARY nombra los cuatro pares de `campaigns/service.ts` entre los nuevos
    - El SUMMARY documenta el spot-check de 5 pares nuevos de archivos distintos, cada uno con archivo, tabla y por que es deuda pre-existente
    - El SUMMARY deja escrito el racional del re-baseline (correccion del one-shot, no regenerador) y el rojo esperado del gate D-14 en el push a staging
    - `git status --porcelain -- el-templo-api` vacio al cierre del task (ningun script auxiliar quedo en el repo)
  </acceptance_criteria>
  <done>Se sabe con numeros cuanta deuda invisible existia, quien la genera y que el fix no invento violaciones: el plan 10 puede re-baselinear con evidencia.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR/push -> gate CON-06 | El lint es el unico control automatico que separa un acceso sin `tenant_id` de estar mergeado |
| Codigo fuente -> motor AST | Toda forma de nombrar una tabla que el motor no resuelve es un bypass silencioso del gate |
| Baseline one-shot (D-16) -> deuda futura | Un par que el motor no ve nunca entra a la allowlist y queda fuera del ratchet para siempre |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-170-01 | Information Disclosure | Acceso nuevo sin `tenant_id` por alias local o join que el gate deja pasar en verde -> fuga cross-tenant | mitigate | `locals` en `SchemaBindings` + joins en `TABLE_METHODS`; fixture con las tres formas y asercion sobre `campaigns/service.ts` en el repo real |
| T-170-04 | Repudiation | El gate queda decorativo: verde que nadie puede distinguir de "no hay deuda" | mitigate | Task 1 exige el ROJO antes del arreglo; el `it` de regresion sobre el repo real se pone rojo si el motor vuelve a quedar ciego |
| T-170-05 | Tampering | El resolver nuevo sobre-reporta y mete pares inventados al baseline que D-16 congela | mitigate | Resolver acotado a tres formas (sin el fallback generico de CallExpression) + spot-check de 5 pares nuevos en el Task 3, con orden explicita de parar si alguno es falso positivo |
| T-170-15 | Tampering | Aparece un comando regenerador de la allowlist (la puerta trasera de D-16) | mitigate | Este plan NO toca la allowlist ni agrega modo de generacion; el arbol tiene que quedar limpio al cierre del Task 3 |
| T-170-SC | Tampering | Instalacion de paquetes | mitigate | Cero dependencias nuevas; `typescript` y `vitest` ya son devDependencies. `pnpm-lock.yaml` sin cambios |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit` sale 0
- El describe "motor sobre fixtures" de `con-06-lint.test.ts` pasa entero
- El `it` de regresion sobre `campaigns/service.ts` pasa (las cuatro tablas visibles)
- `pnpm lint:tenant` sale 1 con los pares nuevos listados — es el estado ESPERADO al cierre de este plan
- ESPERADO Y DOCUMENTADO: el `it` "el repo real con el baseline del plan 07 sale 0" queda ROJO hasta que el plan 10 re-baselinee. Es la consecuencia directa del fix, no una regresion; el plan 10 lo pone en verde SIN tocar el test
- `git status --porcelain -- el-templo-api` limpio (todo commiteado, ningun script auxiliar dentro del repo)
</verification>

<success_criteria>
- Las tres formas ciegas (alias de variable local, `alias()` guardado en variable, join) producen accesos en el pase del motor
- El fixture falla si alguna vuelve a quedar afuera, y las aserciones sobre el repo real anclan la evidencia viva de `campaigns/service.ts`
- El delta de pares nuevos esta medido, spot-checkeado y escrito, con "entradas perdidas = 0"
- Cero migraciones, cero dependencias nuevas, nada pusheado
</success_criteria>

<output>
Crear `.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-09-SUMMARY.md` al terminar.
</output>
