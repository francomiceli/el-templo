---
phase: 170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci
plan: 07
type: execute
wave: 7
depends_on: [170-05, 170-06]
files_modified:
  - el-templo-api/tenant-lint-allowlist.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [CON-06]

must_haves:
  truths:
    - "La allowlist arranca COMPLETA: con ella, el lint sale 0 sobre el repo tal como esta hoy, generada una sola vez y sin comando regenerador permanente (D-16)"
    - "El lint corre en CI como step propio del job del API y rompe el build"
    - "El checkout del job del API tiene historia suficiente para resolver la rama base"
    - "Un acceso nuevo sin tenant_id ni anotacion deja el build rojo, demostrado en vivo"
    - "Agregar una entrada a la allowlist deja el build rojo, demostrado en vivo"
  artifacts:
    - path: "el-templo-api/tenant-lint-allowlist.json"
      provides: "Baseline one-shot de la deuda existente (archivo + tabla)"
      contains: "entries"
    - path: ".github/workflows/ci.yml"
      provides: "fetch-depth 0 en api-check + step bloqueante del lint"
      contains: "lint:tenant"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "el-templo-api/package.json"
      via: "step que corre pnpm lint:tenant --base=<sha>"
      pattern: "lint:tenant"
    - from: ".github/workflows/ci.yml"
      to: "git history"
      via: "actions/checkout con fetch-depth 0 (sin esto el merge-base falla)"
      pattern: "fetch-depth"
---

<objective>
Cerrar el gate: poblar la allowlist con el baseline one-shot de la deuda que existe hoy
(D-16), enchufar el lint como step bloqueante del job `api-check` con la historia de git
que el ratchet necesita (Pitfall 5), y DEMOSTRAR el rojo en vivo — las dos formas: un
acceso nuevo sin scope y una entrada nueva en la allowlist.

Purpose: es el criterio 4 del ROADMAP, que pide explicitamente "demostrado con un caso de
prueba" y "la allowlist arranca completa y solo puede achicarse".
Output: `tenant-lint-allowlist.json` poblado y revisado, `ci.yml` con el step, y la
evidencia de los dos rojos en el SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-CONTEXT.md
@.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-RESEARCH.md
@.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-PATTERNS.md
@CLAUDE.md

**Trabajar en el worktree `/home/franco/projects/et-170-deteccion`.**

<ci_anchors>

```text
.github/workflows/ci.yml, job api-check (~:20-54), con working-directory: el-templo-api

  - name: Checkout code
    uses: actions/checkout@v4          <-- SIN bloque with: => fetch-depth 1 (shallow)
  - name: Setup pnpm            (pnpm/action-setup@v4, version 10)
  - name: Setup Node.js         (actions/setup-node@v4, cache pnpm)
  - name: Install dependencies  (pnpm install --frozen-lockfile)
  - name: Type check            (pnpm exec tsc --noEmit)      <-- el step nuevo va DESPUES de este
  - name: Security audit        (advisory, tolera el fallo)   <-- contraste: ese SI es advisory
  - name: Build                 (pnpm run build)

Contraste deliberado del archivo: Security audit tolera el fallo y los Lint de los frontends
llevan continue-on-error: true (~:147, :191, :235). El del API NO: rompe el build (D-09/D-14).

Trampas confirmadas: actions/checkout@v4 trae fetch-depth 1 por default (sin historia no hay
merge-base); github.event.before es el NULL SHA de 40 ceros en el primer push de una rama;
en pull_request conviene github.event.pull_request.base.sha, que siempre existe.
```

</ci_anchors>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Baseline one-shot de la allowlist (D-16)</name>
  <files>el-templo-api/tenant-lint-allowlist.json</files>
  <read_first>
    - el-templo-api/src/db/scripts/lint-tenant.ts (planes 03 y 05) — el alcance escrito en el
      docblock y la forma de `lintTenantSources` / `lintTenant`
    - el-templo-api/tenant-lint-allowlist.json (estructura vacia del plan 05)
    - .planning/phases/170-.../170-CONTEXT.md (D-16) y 170-RESEARCH.md (Pitfall 9 y A2)
  </read_first>
  <action>
    Producir la allowlist inicial UNA SOLA VEZ. El script NO tiene ni debe tener modo de
    generacion (D-16: un comando regenerador es la puerta trasera del ratchet). El baseline
    se arma con un snippet DESCARTABLE, escrito en el directorio de scratchpad de la sesion
    (NO dentro del repo, NO commiteado), que importe `lintTenantSources` y `buildSchemaTableMap`
    del script real y emita el JSON con la forma que el plan 05 definio: `note`, `scope`,
    `generated` y `entries` como pares unicos `{ file, table }` ordenados por `file` y luego
    por `table`, sin numeros de linea.

    Reglas del baseline:
    - Solo entran accesos que VIOLAN (no cumplidores y sin exencion anclada). Los exentos NO
      van a la allowlist: son el otro canal (D-12/D-17).
    - Alcance exactamente el que quedo escrito en el docblock del lint (`src/**` +
      `scripts/**` del API, sin `test/**`). Si el conteo sale de cientos de entradas por
      haber incluido tests, PARAR: es la senal de alarma de Pitfall 9 y hay que corregir el
      alcance ANTES de commitear, porque despues corregirlo obliga a regenerar.
    - Revisar la salida antes de commitear: contar entradas, verificar que ningun `file`
      empieza con `el-templo-api/test/`, y hacer un spot-check de 5 entradas abriendo el
      archivo y confirmando que efectivamente hay un acceso sin filtro de tenant a esa tabla.
    - Actualizar `generated` con la fecha y la frase "fase 170, one-shot (D-16): esta lista
      no se regenera; achicar es borrar entradas a mano al migrar cada modulo".

    Registrar en el SUMMARY: cantidad de entradas, cantidad de archivos distintos, top 5 de
    tablas por entradas y cantidad de exenciones del inventario (deben ser 9, los sitios
    conocidos de la fase 169; si son mas o menos, explicar por que).

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-170-deteccion/el-templo-api && pnpm lint:tenant && node -e "const a=require('./tenant-lint-allowlist.json'); const bad=a.entries.filter(e=>e.file.includes('/test/')); const dup=new Set(a.entries.map(e=>e.file+'::'+e.table)); if(bad.length||dup.size!==a.entries.length){console.error('BASELINE_FAIL bad='+bad.length);process.exit(1)} console.log('BASELINE_OK entries='+a.entries.length)"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm lint:tenant` (sin `--base`) sale 0 sobre el repo con la allowlist commiteada
    - Ninguna entrada apunta a un archivo bajo `test/`
    - Cero entradas duplicadas; las entradas estan ordenadas por `file` y luego `table`
    - El inventario de exenciones que imprime el lint lista los sitios conocidos de la fase 169 y el SUMMARY explica cualquier diferencia
    - El snippet generador NO quedo en el repo (verificable: `git status --porcelain` no muestra archivos nuevos fuera de la allowlist)
    - `git diff` de este task toca UN solo archivo
  </acceptance_criteria>
  <done>El lint pasa en verde sobre el repo tal como esta hoy, con toda la deuda existente inventariada y ninguna entrada de mas.</done>
</task>

<task type="auto">
  <name>Task 2: Step bloqueante en CI + fetch-depth 0</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml — COMPLETO. Prestar atencion al job `api-check` (~:20-54), al
      contraste entre `Security audit` (advisory con `|| true`) y los `Lint` de los frontends
      (`continue-on-error: true`), y al estilo de comentario inline en la linea del `name:`
    - el-templo-api/src/db/scripts/lint-tenant.ts — como consume `--base` y cuando sale 2
    - .planning/memory/reference_deploy_paths_filter_trap.md (via MEMORY.md) — `paths-filter`
      usa `event.before`
  </read_first>
  <action>
    1. Agregar `with: { fetch-depth: 0 }` al step `Checkout code` del job `api-check`
       UNICAMENTE (los otros jobs no corren el lint y no necesitan la historia). Comentario
       inline explicando el motivo: el gate anti-crecimiento de la allowlist resuelve la
       rama base con `merge-base`, y con el clone shallow por default eso falla — o, peor,
       "no encuentra diferencias" y el ratchet queda decorativo.
    2. Agregar un step nuevo DESPUES de `Type check` y ANTES de `Security audit`, llamado
       `Tenant lint (CON-06)`, que corra `pnpm lint:tenant --base="$LINT_BASE"` con
       `LINT_BASE` seteado por `env:` a la base del evento: en `pull_request`, el
       `base.sha` del PR; en push, el `before` del evento (el script ya cae a
       `merge-base origin/master HEAD` cuando eso viene en el NULL SHA de 40 ceros o vacio).
       SIN `|| true` y SIN `continue-on-error` — este step ROMPE el build, y el comentario
       inline lo dice, contrastando con el `Security audit` de la linea siguiente que si es
       advisory.
    3. No tocar ningun otro job ni el workflow de deploy.
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo && python3 -c "import yaml,sys; w=yaml.safe_load(open('/home/franco/projects/et-170-deteccion/.github/workflows/ci.yml')); j=w['jobs']['api-check']['steps']; co=[s for s in j if 'actions/checkout' in str(s.get('uses',''))][0]; assert co.get('with',{}).get('fetch-depth')==0, 'fetch-depth'; lint=[s for s in j if 'lint:tenant' in str(s.get('run',''))]; assert len(lint)==1, 'step'; s=lint[0]; assert '|| true' not in s['run'] and not s.get('continue-on-error'), 'blocking'; names=[x.get('name','') for x in j]; assert names.index(s['name'])>names.index('Type check'), 'order'; print('CI_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `actions/checkout` del job `api-check` tiene `fetch-depth: 0` (y solo ese job cambia)
    - Existe exactamente un step que corre `pnpm lint:tenant`, ubicado despues de `Type check`
    - Ese step NO tiene `|| true` ni `continue-on-error: true`, y su comentario inline dice que rompe el build
    - `LINT_BASE` resuelve `github.event.pull_request.base.sha` en PR y `github.event.before` en push
    - `.github/workflows/deploy.yml` sin cambios
  </acceptance_criteria>
  <done>Un push o PR al API corre el lint con historia suficiente para comparar contra la base, y un rojo del lint deja el build rojo.</done>
</task>

<task type="auto">
  <name>Task 3: Demostracion en vivo del rojo (criterio 4)</name>
  <files>(sondas temporales, revertidas — ningun archivo del repo queda modificado)</files>
  <read_first>
    - el-templo-api/src/db/scripts/lint-tenant.ts (planes 03 y 05)
    - el-templo-api/tenant-lint-allowlist.json (Task 1)
    - .planning/phases/169-.../169-08-SUMMARY.md o 168-05 — el idioma del repo para demostrar
      un gate fail-closed: sonda temporal, registrar el rojo, revertir sin commitear
  </read_first>
  <action>
    Ejercitar el gate de las dos maneras que pide el criterio 4, registrando la salida
    literal en el SUMMARY y REVIRTIENDO cada sonda sin commitear el estado roto.

    Sonda A — acceso nuevo sin scope: crear un archivo temporal bajo `el-templo-api/src/`
    (por ejemplo `src/modules/__probe-tenant-lint.ts`) con un `.from(schema.<tabla
    gym-owned>)` sin filtro de tenant y sin anotacion. Correr `pnpm lint:tenant` y afirmar
    exit 1, con el archivo y la tabla nombrados en la salida. Repetir agregando al mismo
    archivo un `sql` crudo sobre otra tabla gym-owned para verificar que las dos formas de
    acceso se detectan. Borrar el archivo y confirmar que vuelve a salir 0.

    Sonda B — allowlist agrandada (el ratchet, D-14): commitear el estado limpio en la rama
    de la fase, despues agregar a mano una entrada nueva a `tenant-lint-allowlist.json`
    (archivo + tabla que hoy no estan) y correr `pnpm lint:tenant --base=HEAD`. Afirmar
    exit 1 con esa entrada listada como GANADA. Revertir la edicion de la allowlist
    (`git checkout -- el-templo-api/tenant-lint-allowlist.json`) y confirmar exit 0.

    Sonda C — base irresoluble (fail-closed, Pitfall 5): correr
    `pnpm lint:tenant --base=0000000000000000000000000000000000000000` en un contexto donde
    `origin/master` NO se pueda resolver (por ejemplo pasando un `--root` a un directorio sin
    repo git) y afirmar exit 2 con el mensaje que nombra `fetch-depth: 0`. Es la prueba de
    que el gate no puede pasar en verde por no haber podido mirar.

    Al terminar: `git status --porcelain` limpio salvo lo que este plan commitea. Pegar en el
    SUMMARY las tres salidas (exit code + las lineas relevantes del reporte).

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-170-deteccion && git status --porcelain | grep -v "^?? .planning" | grep -c . | grep -qx 0 && cd el-templo-api && pnpm lint:tenant && echo CLEAN_AND_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - El SUMMARY contiene las tres salidas literales: sonda A con exit 1 y el archivo/tabla nombrados, sonda B con exit 1 y la entrada listada como ganada, sonda C con exit 2 y el mensaje que menciona `fetch-depth: 0`
    - Despues de revertir, `pnpm lint:tenant` vuelve a salir 0
    - `git status --porcelain` del worktree queda limpio (ninguna sonda commiteada, ningun archivo temporal olvidado)
    - Ninguna sonda quedo dentro de `tenant-lint-allowlist.json`
  </acceptance_criteria>
  <done>El criterio 4 del ROADMAP queda demostrado con evidencia, no asumido: el build se pone rojo por un acceso nuevo y por una allowlist agrandada, y no puede pasar en verde por no poder resolver la base.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                                      |
| ------------------------------ | -------------------------------------------------------------------------------- |
| PR ↔ pipeline de CI            | El step nuevo es el que convierte el lint en un gate real                        |
| runner de CI ↔ historia de git | Sin `fetch-depth: 0` el ratchet no puede comparar y el gate se vuelve decorativo |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                                        | Disposition | Mitigation Plan                                                                                                                          |
| --------- | ---------------------- | ------------------------------------------------------------------------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| T-170-04  | Repudiation            | El step pasa siempre en verde (clone shallow, null SHA, o marcado como advisory)                 | mitigate    | `fetch-depth: 0` en `api-check`, step sin sufijo advisory ni `continue-on-error`, y sonda C que prueba el exit 2 con base irresoluble    |
| T-170-05  | Tampering              | Baseline inflado (incluye `test/**`) que vuelve irrevisable la allowlist y tolera cualquier cosa | mitigate    | Alcance fijado antes de generar + criterio de aceptacion que rechaza entradas bajo `test/` + revision manual con spot-check              |
| T-170-15  | Tampering              | Queda un comando regenerador de la allowlist                                                     | mitigate    | El baseline se genera con un snippet descartable fuera del repo; criterio de aceptacion que verifica que no quedo commiteado             |
| T-170-01  | Information Disclosure | Acceso nuevo sin `tenant_id` mergeado sin que nadie lo vea                                       | mitigate    | Sonda A demuestra el rojo para las dos formas de acceso (`sql` crudo y query builder)                                                    |
| T-170-20  | Denial of Service      | El step suma minutos a todo push del API                                                         | accept      | El pase AST completo mide 606 ms sobre 382 archivos; el costo real es despreciable frente al `tsc --noEmit` que ya corre en el mismo job |
| T-170-SC  | Tampering              | Instalacion de paquetes                                                                          | mitigate    | Cero deps nuevas; el step usa `tsx`, que ya esta en devDependencies                                                                      |

</threat_model>

<verification>
- `pnpm lint:tenant` sale 0 con la allowlist commiteada
- El parseo YAML de `ci.yml` confirma `fetch-depth: 0`, el step unico y su caracter bloqueante
- Las tres sondas registradas con su exit code en el SUMMARY
- `git status --porcelain` limpio al cierre del plan
</verification>

<success_criteria>

- Allowlist inicial completa, revisada y commiteada (D-16, one-shot)
- Lint enchufado en CI como gate bloqueante con historia suficiente
- Criterio 4 del ROADMAP demostrado con evidencia (acceso nuevo, allowlist agrandada y base irresoluble)
- Cero migraciones, cero deps nuevas
  </success_criteria>

<output>
Create `.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-07-SUMMARY.md` when done
</output>
