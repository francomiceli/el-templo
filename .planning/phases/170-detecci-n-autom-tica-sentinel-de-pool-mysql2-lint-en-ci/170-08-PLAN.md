---
phase: 170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci
plan: 08
type: execute
wave: 8
depends_on: [170-06, 170-07]
files_modified:
  - .planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-INVENTORY.md
autonomous: false
requirements: [CON-05]

must_haves:
  truths:
    - "Existe un inventario deterministico de las violaciones que hoy produce el codigo real, obtenido corriendo la suite con el sentinel en modo inventario"
    - "El sentinel corre en staging sin romper ningun camino"
    - "Tras la ventana de observacion de 2-3 dias en staging, la lista de excepciones queda cerrada: no hay falsos positivos ruidosos recurrentes (D-04)"
  artifacts:
    - path: ".planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-INVENTORY.md"
      provides: "Inventario de violaciones (suite + staging) con totales, top tablas y veredicto de cierre"
      min_lines: 40
  key_links:
    - from: ".planning/phases/170-.../170-INVENTORY.md"
      to: "el-templo-api/src/db/sentinel/install.ts"
      via: "reporte agregado del modo inventario (SENTINEL_INVENTORY=1)"
      pattern: "SENTINEL_INVENTORY"
---

<objective>
Cerrar el criterio 2 del ROADMAP: producir el inventario grueso de excepciones de forma
deterministica (corriendo la suite con `SENTINEL_INVENTORY=1`, que ejercita el SQL real de
~140 archivos de test), llevar la fase a staging con el OK de Franco, y confirmar tras 2-3
dias de uso del staff que el sentinel no genera falsos positivos ruidosos recurrentes.

Purpose: el sentinel se disena para estar SILENCIOSO en prod antes de que empiece la
adopcion modulo a modulo. Este plan es la evidencia de que lo esta.
Output: `170-INVENTORY.md` + fase en staging verificada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-CONTEXT.md
@.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-RESEARCH.md
@.claude/skills/el-templo-change-control/SKILL.md
@CLAUDE.md

**Trabajar en el worktree `/home/franco/projects/et-170-deteccion`, rama
`feat/170-deteccion-automatica`.**

Reglas operativas que gobiernan este plan (no negociables):

- **Siempre preguntar antes de pushear.** Ningun `git push` sin OK explicito de Franco en la
  conversacion. La memoria, el contexto previo o "ya lo dijo antes" NO cuentan.
- **Staging-first estricto.** El trabajo de fase va a `staging`, JAMAS directo a `master`.
- **Preguntar antes de SSHear al servidor.**
- Cero migraciones en esta fase: el deploy de staging no debe aplicar ninguna migracion
  nueva. Si el runner reporta una migracion nueva, algo salio mal — PARAR.
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Inventario deterministico corriendo la suite con SENTINEL_INVENTORY=1</name>
  <files>.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-INVENTORY.md</files>
  <read_first>
    - el-templo-api/src/db/sentinel/install.ts — que acumula y que imprime `report()` en modo inventario
    - el-templo-api/.env.example — la documentacion del flag (plan 06)
    - .planning/phases/170-.../170-CONTEXT.md (D-04 y D-08)
  </read_first>
  <action>
    Correr la suite COMPLETA del API una sola vez con el flag de inventario activo
    (`SENTINEL_INVENTORY=1 pnpm test` desde el worktree) y capturar el reporte agregado.
    Esta corrida local completa es la EXCEPCION deliberada a la regla del repo de no correr
    el suite local: el inventario ES el entregable del criterio 2 (D-04) y sale justamente
    de ejercitar el SQL real de ~140 archivos de test. Dejarlo escrito en el SUMMARY para que
    no parezca un descuido.

    Escribir `170-INVENTORY.md` con:
    - Como se genero (comando exacto, fecha, commit del worktree).
    - Totales: violaciones acumuladas, fingerprints distintos, tablas distintas.
    - Tabla de las violaciones por tabla gym-owned, ordenada por frecuencia, con el SQL
      representativo truncado de cada una.
    - Una seccion "Exenciones que aparecen como violacion (esperado, D-17)": las exenciones
      de la fase 169 que NO viajan en el SQL (6 file-level + 3 comentarios TS) van a
      figurar acá, y eso es CORRECTO — son deuda real y ninguna tabla es strict todavia.
      Nombrarlas para que nadie las lea como falsos positivos.
    - Una seccion "Candidatos a falso positivo": cualquier violacion que NO sea deuda real
      (por ejemplo, statements de infraestructura que el skiplist no atrapo). Si aparece
      alguno, describir el patron; si no aparece ninguno, decirlo explicitamente.
    - Advertencia escrita: los contadores son por proceso y se resetean en cada restart de
      pm2 — un contador en cero NO significa "no hay violaciones".

    Si la suite queda colgada al terminar, es Pitfall 4 (timer sin `unref` o sin `stop`):
    PARAR y arreglarlo antes de seguir, no bajar el flag.

  </action>
  <verify>
    <automated>test -s /home/franco/projects/el-templo/.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-INVENTORY.md && grep -c "SENTINEL_INVENTORY" /home/franco/projects/el-templo/.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-INVENTORY.md | grep -qv "^0$" && echo INVENTORY_OK</automated>
  </verify>
  <acceptance_criteria>
    - La suite completa termina SOLA con el flag activo (no queda colgada: Pitfall 4 descartado en vivo)
    - `170-INVENTORY.md` contiene el comando exacto, la fecha, los totales y la tabla por tabla gym-owned
    - Tiene la seccion que explica por que las exenciones de la 169 aparecen como violacion (D-17)
    - Tiene la seccion de candidatos a falso positivo, aunque sea para decir que no hay ninguno
    - El resultado de la suite (verde / archivos rojos) queda registrado en el SUMMARY
  </acceptance_criteria>
  <done>Existe la foto determinista de cuanto ruido produce el sentinel hoy, y esta explicada linea por linea.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Aprobacion de Franco para pushear la fase 170 a staging</name>
  <what-built>
    Fase 170 completa en la rama `feat/170-deteccion-automatica` del worktree
    `/home/franco/projects/et-170-deteccion`:
    - Sentinel de pool instalado por debajo de Drizzle (query + execute + getConnection), con
      throw solo para tablas de `TENANT_STRICT_MODULES` (hoy VACIA, o sea: cero throws) y
      `log.error` deduplicado + resumen periodico en staging/prod.
    - Lint `pnpm lint:tenant` con allowlist decreciente, enchufado como step bloqueante del
      job `api-check` de CI.
    - Cero migraciones de DB, cero dependencias nuevas, `pnpm-lock.yaml` intacto.
    - Inventario deterministico en `170-INVENTORY.md`.
  </what-built>
  <how-to-verify>
    1. Revisar el diff completo de la rama:
       `git -C /home/franco/projects/et-170-deteccion diff origin/master --stat`
       Esperado: solo archivos de `el-templo-api/` mas `.github/workflows/ci.yml`.
       NINGUN archivo bajo `el-templo-api/src/db/migrations/`.
    2. Confirmar que `pnpm exec tsc --noEmit` y `pnpm lint:tenant` salen 0 en el worktree.
    3. Leer `170-INVENTORY.md`, en particular la seccion de candidatos a falso positivo.
    4. Decidir: aprobar el push de `feat/170-deteccion-automatica` a `staging`
       (`git push origin feat/170-deteccion-automatica:staging`).
       Recordatorio de la regla del repo: NADA va a `master` en este plan.
    5. Tras el push, mirar el run de CI: el step nuevo `Tenant lint (CON-06)` tiene que estar
       en VERDE (si sale rojo con el baseline recien commiteado, es un bug del gate, no deuda).
  </how-to-verify>
  <files>(ningun archivo del repo — es un gate de aprobacion sobre la rama feat/170-deteccion-automatica)</files>
  <action>
    ANTES del gate (Claude, autonomo): dejar el arbol del worktree limpio y commiteado en
    `feat/170-deteccion-automatica`, correr `pnpm exec tsc --noEmit` y `pnpm lint:tenant`, y
    producir el resumen del diff contra `origin/master` (archivos tocados, ausencia de
    migraciones, `pnpm-lock.yaml` intacto). Presentarle a Franco ese resumen junto con los
    pasos de `<how-to-verify>`.
    DURANTE el gate: NO pushear. Esperar el OK explicito.
    DESPUES del OK: ejecutar `git push origin feat/170-deteccion-automatica:staging` (y NADA
    mas: cero pushes a `master`), esperar el run de CI y registrar en el SUMMARY el resultado
    del step `Tenant lint (CON-06)` y del job `api-test`. Si CI sale rojo, reportar y PARAR.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-170-deteccion && git status --porcelain | grep -c . | grep -qx 0 && git diff origin/master --name-only | grep -c "el-templo-api/src/db/migrations/" | grep -qx 0 && cd el-templo-api && pnpm exec tsc --noEmit && pnpm lint:tenant && echo READY_FOR_STAGING</automated>
  </verify>
  <resume-signal>Escribir "aprobado" para que Claude ejecute el push a staging, o describir que cambiar antes</resume-signal>
  <acceptance_criteria>
    - El push a `staging` ocurre SOLO despues del OK explicito de Franco en esta conversacion
    - Ningun push a `master` en este plan
    - El run de CI de staging queda registrado en el SUMMARY con el resultado del step `Tenant lint (CON-06)`
    - El deploy de staging no aplica ninguna migracion nueva (cero migraciones en la fase)
  </acceptance_criteria>
  <done>La fase 170 esta corriendo en staging con CI en verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Ventana de observacion en staging (2-3 dias) y cierre de la lista de excepciones</name>
  <what-built>
    El sentinel corriendo en staging en modo `log`: primera aparicion de cada SQL violador
    distinto emite `log.error`; las repeticiones solo suman al contador; cada hora sale un
    `log.info` con el resumen (totales, fingerprints distintos, top tablas).
  </what-built>
  <how-to-verify>
    1. Dejar pasar 2-3 dias de uso normal del staff sobre staging (D-04: la ventana es corta
       a proposito — el inventario grueso ya salio de la suite; esto CONFIRMA con trafico real).
    2. Con OK previo para SSHear, revisar los logs de pm2 del API de staging: buscar las
       lineas del sentinel (los `log.error` con el detalle y los `log.info` del resumen).
    3. Contestar las tres preguntas y anotarlas en `170-INVENTORY.md`:
       a. Aparecio alguna violacion que NO estuviera en el inventario de la suite? Cual?
       b. Hay algun patron que sea claramente FALSO POSITIVO (statement de infraestructura,
          introspeccion, forma de SQL que el parser lee mal)? Si lo hay, es trabajo de arreglo
          del parser, no de bajar el sentinel.
       c. El volumen es manejable (el dedup esta funcionando: los `log.error` distintos son
          del orden de los fingerprints del inventario, no del trafico)?
    4. Confirmar que ningun camino de la app se rompio y que Sentry NO se lleno con este ruido
       (D-02: el resumen va por `log.info` justamente para eso).
  </how-to-verify>
  <files>.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-INVENTORY.md</files>
  <action>
    ANTES del gate (Claude, autonomo): preparar los comandos exactos de lectura de logs para
    que Franco los apruebe (pm2 del API de staging, filtrando las lineas del sentinel) y el
    esqueleto de la seccion "Ventana de observacion en staging" en `170-INVENTORY.md` con las
    tres preguntas a responder.
    DURANTE el gate: esperar los 2-3 dias de uso real y el OK de Franco para SSHear al
    servidor. Sin ese OK, no se ejecuta ningun comando remoto.
    DESPUES: completar la seccion con fechas, totales observados, respuestas a las tres
    preguntas y el veredicto de cierre. Si aparecio un falso positivo, registrarlo como
    hallazgo con la correccion propuesta (parser o skiplist) — NUNCA "bajar el sentinel" ni
    "aceptar el ruido" como resolucion.
  </action>
  <verify>
    <automated>grep -c "Ventana de observacion en staging" /home/franco/projects/el-templo/.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-INVENTORY.md | grep -qv "^0$" && echo OBSERVATION_RECORDED</automated>
  </verify>
  <resume-signal>Escribir "cerrada" si no hay falsos positivos ruidosos recurrentes, o describir los patrones a corregir</resume-signal>
  <acceptance_criteria>
    - `170-INVENTORY.md` tiene una seccion "Ventana de observacion en staging" con fechas, las respuestas a las tres preguntas y el veredicto de cierre
    - Si aparecieron falsos positivos, quedan registrados como hallazgos con la correccion propuesta (parser o skiplist), no como "aceptar el ruido"
    - Queda registrado que ningun camino de staging se rompio y que el ruido no fue a Sentry
    - Ningun push a `master` en este plan
  </acceptance_criteria>
  <done>Criterio 2 del ROADMAP cerrado: el sentinel emite en staging sin romper nada y su lista de excepciones esta cerrada y escrita.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                                                                                    |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| staging ↔ prod             | Comparten host EC2 y servidor MySQL (bases distintas): un cambio que rompe el arranque en staging es la senal temprana de prod |
| logs de staging ↔ operador | El sentinel escribe SQL en un canal que persiste y que lee una persona                                                         |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                              | Disposition | Mitigation Plan                                                                                                                         |
| --------- | ---------------------- | ---------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| T-170-02  | Information Disclosure | SQL del sentinel en los logs de staging                                | mitigate    | Solo texto con placeholders `?`, truncado, sin `params`; verificado en el Task 3 leyendo lineas reales                                  |
| T-170-13  | Information Disclosure | Ruido del sentinel inundando Sentry                                    | mitigate    | Dedup por fingerprint + resumen por `log.info`; el Task 3 lo confirma mirando el dashboard                                              |
| T-170-18  | Denial of Service      | El sentinel rompe un camino del staff en staging                       | mitigate    | En staging el modo es `log`, nunca `throw`, y la lista strict esta vacia; la ventana de observacion es justamente la deteccion temprana |
| T-170-21  | Elevation of Privilege | Que la fase llegue a `master`/produccion sin la ventana de observacion | mitigate    | Staging-first estricto, dos checkpoints bloqueantes, push a master explicitamente fuera del alcance de este plan                        |
| T-170-22  | Repudiation            | Leer un contador en cero como "no hay violaciones"                     | mitigate    | Advertencia escrita en `170-INVENTORY.md`: los contadores son por proceso y se resetean en cada restart de pm2                          |
| T-170-SC  | Tampering              | Instalacion de paquetes durante el rollout                             | mitigate    | Cero deps nuevas; el deploy usa el lockfile existente                                                                                   |

</threat_model>

<verification>
- `170-INVENTORY.md` existe, con inventario de la suite y seccion de ventana de observacion
- La suite con `SENTINEL_INVENTORY=1` termina sola (Pitfall 4 descartado en vivo)
- CI de staging verde, incluido el step `Tenant lint (CON-06)`
- Cero migraciones aplicadas en el deploy de staging
- Cero pushes a `master`
</verification>

<success_criteria>

- Criterio 2 del ROADMAP cerrado con evidencia escrita
- Sentinel silencioso (sin throws) y sin falsos positivos ruidosos recurrentes en staging
- Lista de excepciones cerrada y documentada para que la fase 172 arranque desde ahi
  </success_criteria>

<output>
Create `.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-08-SUMMARY.md` when done
</output>
