---
phase: 170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci
plan: 05
type: execute
wave: 5
depends_on: [170-03, 170-04]
files_modified:
  - el-templo-api/src/db/scripts/lint-tenant.ts
  - el-templo-api/tenant-lint-allowlist.json
  - el-templo-api/package.json
  - el-templo-api/test/tenancy/con-06-lint.test.ts
autonomous: true
requirements: [CON-06]

must_haves:
  truths:
    - "Un acceso no cumplidor que no esta en la allowlist deja el lint en exit 1"
    - "Una entrada de la allowlist que ya no corresponde a una violacion viva deja el lint en exit 1"
    - "Una entrada nueva en la allowlist respecto de la rama base deja el lint en exit 1"
    - "Una tabla declarada strict con entradas vivas en la allowlist deja el lint en exit 1"
    - "Si la base no se puede resolver o la allowlist esta malformada, el lint sale 2 y nunca en verde"
    - "El inventario completo de exenciones sale en una sola corrida revisable"
  artifacts:
    - path: "el-templo-api/src/db/scripts/lint-tenant.ts"
      provides: "allowlist + gates del ratchet + reporte + CLI con exit codes 0/1/2"
      exports: ["lintTenant", "formatReport", "runLint", "LintArgError"]
    - path: "el-templo-api/tenant-lint-allowlist.json"
      provides: "Allowlist decreciente (archivo + tabla), sin numeros de linea"
      contains: "entries"
    - path: "el-templo-api/package.json"
      provides: "Comando local del lint"
      contains: "lint:tenant"
  key_links:
    - from: "el-templo-api/src/db/scripts/lint-tenant.ts"
      to: "el-templo-api/src/db/tenant-tables.ts"
      via: "TENANT_STRICT_MODULES / isStrictTable para el gate de coherencia D-15"
      pattern: "isStrictTable|TENANT_STRICT_MODULES"
    - from: "el-templo-api/src/db/scripts/lint-tenant.ts"
      to: "git (merge-base / git show de la rama base)"
      via: "resolucion fail-closed de la allowlist de la base"
      pattern: "merge-base"
---

<objective>
Convertir el motor del plan 03 en un gate: allowlist decreciente (D-13), los tres gates del
ratchet (D-14 entradas ganadas y entradas stale, D-15 coherencia strict/allowlist), el
reporte con inventario de exenciones (D-12) y la CLI con el contrato de exit codes 0/1/2
del repo (D-09).

Purpose: sin el ratchet, la allowlist es una alfombra debajo de la cual barrer accesos sin
scope. Con el ratchet, la deuda solo puede achicarse.
Output: `lint-tenant.ts` completo + `tenant-lint-allowlist.json` (estructura vacia, la
puebla el plan 07) + `pnpm lint:tenant` + bateria de gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-CONTEXT.md
@.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-RESEARCH.md
@.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-PATTERNS.md
@CLAUDE.md

**Trabajar en el worktree `/home/franco/projects/et-170-deteccion`.**

<interfaces>

```ts
Ya existente (plan 170-03), en el mismo archivo:
  export interface TenantAccess { file; line; table; kind; compliant; exemption? }
  export interface LintSourceResult { filesScanned; accesses; violations; exemptions }
  export function lintTenantSources(opts): LintSourceResult;
  export function buildSchemaTableMap(schemaDir): ReadonlyMap<string, string>;

Ya existente (plan 170-01), src/db/tenant-tables.ts:
  export const TENANT_STRICT_MODULES: Record<string, readonly string[]>;
  export function isStrictTable(name: string): boolean;

Contrato que este plan agrega:
  export interface AllowlistEntry { file: string; table: string }
  export interface Allowlist { scope: string; generated: string; entries: AllowlistEntry[] }
  export interface TenantLintReport {
    filesScanned: number;
    allowlistSize: number;
    unlistedViolations: TenantAccess[];       // violan y NO estan en la allowlist  (D-13)
    staleMissingFile: AllowlistEntry[];       // el archivo ya no existe             (D-14)
    staleNoLongerViolating: AllowlistEntry[]; // el archivo existe y ya no viola     (D-14)
    gainedEntries: AllowlistEntry[];          // nuevas respecto de la base          (D-14)
    strictWithAllowlist: AllowlistEntry[];    // tabla strict con entradas vivas     (D-15)
    exemptions: TenantAccess[];               // inventario, NO suman                (D-12)
    warnings: string[];                       // NO suman
    discrepancies: number;                    // suma de los cinco arrays de hallazgo
  }
  export class LintArgError extends Error { readonly exitCode = 2 }
  export function lintTenant(opts: {
    rootDir: string;
    allowlist: Allowlist;
    baseAllowlist?: Allowlist;   // ausente = sin gate de entradas ganadas
  }): TenantLintReport;
  export function formatReport(report: TenantLintReport): string;
  export function runLint(argv: string[]): Promise<{ code: 0 | 1 | 2; output: string }>;

Idioma de CLI a copiar literal de src/db/scripts/verify-tenant-uniques.ts (~:587-627) y de
src/db/scripts/require-tenant.ts (~:110-117, ~:222-226):
  0 = limpio, 1 = discrepancias, 2 = error de uso o interno
  guard: if (require.main === module)
  console.* permitido porque es tooling CLI
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Allowlist + gates de stale y de coherencia strict + reporte</name>
  <files>el-templo-api/src/db/scripts/lint-tenant.ts, el-templo-api/tenant-lint-allowlist.json</files>
  <read_first>
    - el-templo-api/src/db/scripts/verify-tenant-uniques.ts — COMPLETO. Moldes: interfaces por
      tipo de hallazgo con doc de una linea (~:187-233), deteccion de stale (~:402-426), y
      `formatReport` que devuelve string con un renglon "Que hacer" por hallazgo (~:514-581)
    - el-templo-api/src/db/tenant-tables.ts (~:255-359) — `TENANT_UNIQUE_ALLOWLIST` y su
      docblock: el racional "clave a motivo obligatorio, fail-closed" y el de partir claves
    - el-templo-api/src/db/scripts/lint-tenant.ts (planes 03)
  </read_first>
  <action>
    1. Crear `el-templo-api/tenant-lint-allowlist.json` con la estructura y `entries` VACIO
       (lo puebla el plan 07 con el baseline one-shot). Campos: `note`, `scope`, `generated`,
       `entries` (array de objetos `{ file, table }`, ordenado por `file` y despues por
       `table`). El campo `note` es la unica documentacion posible en JSON, asi que lleva
       escrito: (a) que la entrada es archivo + tabla SIN numero de linea, para ser estable
       ante ediciones (D-13); (b) que la lista solo puede ACHICARSE — agregar una entrada
       deja el build rojo (D-14) y la forma de sacar una es migrar el modulo o escribir la
       exencion con motivo; (c) que NO existe comando para regenerarla (D-16: un regenerador
       seria la puerta trasera del ratchet).
       Escribir tambien, en el docblock de `lint-tenant.ts`, POR QUE JSON Y NO UN REGISTRO
       .ts como los del milestone: el gate anti-crecimiento tiene que leer la version de la
       allowlist en la RAMA BASE con `git show <base>:<ruta>`, y parsear un `.ts` de una
       revision arbitraria exigiria o un pase AST extra o importar/ejecutar codigo de esa
       revision — riesgo que un `JSON.parse` no tiene.

    2. Carga y validacion fail-closed de la allowlist: archivo ausente, JSON invalido,
       `entries` que no es array, entrada sin `file` o sin `table`, `file` con separadores de
       Windows, o entrada duplicada => `LintArgError` (exit 2). Nunca "asumir vacia".

    3. `lintTenant(opts)` que compone el resultado de `lintTenantSources` con la allowlist:
       - `unlistedViolations`: accesos que violan y cuyo par (file, table) NO esta en la
         allowlist.
       - `staleMissingFile`: entradas cuyo `file` no existe en el arbol. Mensaje distinto del
         siguiente a proposito.
       - `staleNoLongerViolating`: entradas cuyo archivo existe pero que ya no tiene ningun
         acceso no cumplidor ni exento a esa tabla. Es el gate que FUERZA el achique.
         La distincion entre los dos tipos de stale es lo que decide si el gate se respeta o
         se esquiva (Open Question 4 del RESEARCH): "el archivo no existe, seguramente lo
         renombraste: actualiza la ruta de la entrada" versus "ese archivo ya no viola:
         borra la entrada, la deuda se pago".
       - `strictWithAllowlist`: entradas cuya tabla esta en `TENANT_STRICT_MODULES` (D-15).
         Mensaje: al activar el throw para esa tabla hay que vaciar sus entradas.
       - `exemptions`: inventario completo (D-12), va a WARNINGS y NO suma discrepancias; las
         de `scope: "file"` se reportan aparte, marcadas como de alcance mayor (cubren codigo
         que todavia no se escribio, T-170-06).
       - `discrepancies` = suma de los cinco arrays de hallazgo. Los warnings no suman.

    4. `formatReport(report)`: devuelve string (no imprime), con el separador de 72 signos
       igual del molde, una seccion por tipo de hallazgo, un renglon "Que hacer:" concreto
       debajo de cada hallazgo, el total de DISCREPANCIAS y una seccion final de
       advertencias con el inventario de exenciones (motivo incluido, y marca de file-level).

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-170-deteccion/el-templo-api && pnpm exec tsc --noEmit && node -e "const a=require('./tenant-lint-allowlist.json'); if(!Array.isArray(a.entries)||a.entries.length!==0||!a.note||!a.scope){process.exit(1)} console.log('ALLOWLIST_SHAPE_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `el-templo-api/tenant-lint-allowlist.json` existe, parsea, y tiene `entries` vacio, mas `note` y `scope` con contenido
    - El `note` menciona explicitamente que la lista solo puede achicarse y que no hay comando regenerador
    - `lint-tenant.ts` exporta `lintTenant`, `formatReport`, `LintArgError`, `Allowlist`, `AllowlistEntry`, `TenantLintReport`
    - Los dos tipos de stale son arrays separados con mensajes distintos en `formatReport`
    - Cada hallazgo impreso lleva su renglon "Que hacer:"
    - El inventario de exenciones sale como advertencia y NO suma a `discrepancies`
    - `pnpm exec tsc --noEmit` sale 0
  </acceptance_criteria>
  <done>Con una allowlist en memoria y un rootDir cualquiera, `lintTenant` dice cuantas discrepancias hay y `formatReport` explica que hacer con cada una.</done>
</task>

<task type="auto">
  <name>Task 2: Ratchet contra la rama base (fail-closed) + CLI con exit codes + pnpm script</name>
  <files>el-templo-api/src/db/scripts/lint-tenant.ts, el-templo-api/package.json</files>
  <read_first>
    - el-templo-api/src/db/scripts/lint-tenant.ts (Task 1)
    - el-templo-api/src/db/scripts/verify-tenant-uniques.ts (~:587-627) — bloque CLI, guard
      `require.main === module`, exit 0/1/2
    - el-templo-api/src/db/scripts/require-tenant.ts (~:110-117, ~:222-226) — `TenantArgError`
      con `exitCode = 2` y `failTenantArg`
    - el-templo-api/package.json (~:6-21) — bloque de scripts, idioma `tsx <ruta>`
    - .planning/phases/170-.../170-RESEARCH.md (Pitfalls 5 y 6: fetch-depth 1 por default y
      `github.event.before` en null SHA en el primer push de una rama)
  </read_first>
  <action>
    1. Resolucion de la allowlist de la BASE (gate de entradas ganadas, D-14):
       - Flag `--base=<ref>`. Si no se pasa, el gate de entradas ganadas NO corre (uso local)
         y el reporte lo dice explicitamente en una advertencia — el resto de los gates SI
         corren.
       - Si se pasa: normalizar el caso del NULL SHA (40 ceros, que es lo que manda
         `github.event.before` en el primer push de una rama) y el caso de string vacio: en
         los dos, caer a `git merge-base origin/master HEAD`.
       - Leer la allowlist de la base con `git show <ref>:el-templo-api/tenant-lint-allowlist.json`
         ejecutado con `execFileSync` de `node:child_process` (nunca con shell interpolando
         strings). Resolver la raiz del repo con `git rev-parse --show-toplevel`, porque el
         script corre con working-directory `el-templo-api` en CI.
       - FAIL-CLOSED: si la ref no se resuelve, si `git` falla, o si la salida no parsea =>
         `LintArgError` (exit 2) con mensaje que nombre la causa y la solucion
         (`fetch-depth: 0` en el checkout del job). Jamas "asumir sin cambios": un gate que
         nunca reporta nada es peor que no tenerlo.
       - EXCEPCION UNICA, escrita y acotada: si el archivo NO EXISTE en la base, es el commit
         que INTRODUCE la allowlist (esta fase). En ese caso el gate de entradas ganadas se
         salta con una advertencia RUIDOSA que diga cuantas entradas trae el baseline y que
         borrar el archivo para resetear el ratchet es visible en el diff. Cualquier otro
         error de lectura sigue siendo exit 2.
       - Entradas ganadas = pares (file, table) presentes en la allowlist actual y ausentes
         en la de la base => discrepancia.

    2. CLI: `export async function runLint(argv: string[]): Promise<{ code: 0|1|2; output: string }>`
       que arma opciones desde `--base=<ref>`, `--root=<ruta>` y `--allowlist=<ruta>`
       (los dos ultimos existen para los tests y para correrlo desde otro cwd), corre
       `lintTenant` y devuelve `{ code, output }` — sin `process.exit` adentro, para que el
       test pueda afirmar los codigos sin matar al worker. Flag desconocido => `LintArgError`
       (exit 2). Debajo, el guard `if (require.main === module)` imprime el output con
       `console.log` y sale con el codigo, con el `catch (err: unknown)` del molde que sale 2
       ante error interno.
       `console.*` esta permitido en este archivo por ser tooling CLI (precedente escrito en
       `require-tenant.ts:70-72`); dejarlo dicho en el docblock.

    3. `package.json`: agregar `"lint:tenant": "tsx src/db/scripts/lint-tenant.ts"` junto a
       los `db:verify-*`. NO tocar `dependencies` ni `devDependencies` ni `pnpm-lock.yaml`.

    4. Completar la seccion `COMO SE CORRE` del docblock con los tres modos: local
       (`pnpm lint:tenant`), CI (`pnpm lint:tenant --base=<sha>`) y el motivo por el que NO
       corre en pre-commit (D-11: no sumar segundos de analisis de proyecto entero a todos
       los commits).

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-170-deteccion/el-templo-api && pnpm exec tsc --noEmit && pnpm lint:tenant; test $? -le 1 && grep -c '"lint:tenant"' package.json | grep -qx 1 && git -C /home/franco/projects/et-170-deteccion status --porcelain -- el-templo-api/pnpm-lock.yaml | grep -c . | grep -qx 0 && echo CLI_OK</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm lint:tenant` corre de punta a punta y sale 0 o 1 (nunca 2) con la allowlist vacia del Task 1
    - `pnpm lint:tenant --base=deadbeefdeadbeefdeadbeefdeadbeefdeadbeef` sale 2 y el mensaje nombra `fetch-depth: 0`
    - `pnpm lint:tenant --flag-inventado` sale 2
    - `runLint` no llama `process.exit` (verificable: el `process.exit` solo aparece dentro del bloque `require.main === module`)
    - El comando usa `execFileSync`, no `exec` con string de shell (verificable por grep: cero `child_process.exec(`)
    - `package.json` tiene `lint:tenant` y `git diff` NO toca `dependencies`, `devDependencies` ni `pnpm-lock.yaml`
    - `pnpm exec tsc --noEmit` sale 0
  </acceptance_criteria>
  <done>`pnpm lint:tenant` es un gate ejecutable con el contrato 0/1/2 del repo, y no puede pasar en verde por no haber podido mirar la base.</done>
</task>

<task type="auto">
  <name>Task 3: Bateria de los cuatro gates y de los exit codes</name>
  <files>el-templo-api/test/tenancy/con-06-lint.test.ts</files>
  <read_first>
    - el-templo-api/test/tenancy/con-06-lint.test.ts (plan 03) — extender, no reescribir
    - el-templo-api/test/unit/require-tenant.test.ts (~:204-235) — spy de `process.exit` y
      aserciones de exit codes sin matar al worker
    - el-templo-api/src/db/scripts/lint-tenant.ts (Tasks 1 y 2)
  </read_first>
  <action>
    Agregar un `describe("lint-tenant — gates del ratchet")` que llame a `lintTenant` con el
    rootDir de fixtures del plan 03 y allowlists EN MEMORIA (sin tocar el archivo real ni
    `git`). Un `it` por gate:

    - Violacion no listada => aparece en `unlistedViolations` y `discrepancies` es mayor a 0.
    - La misma violacion CON su entrada en la allowlist => `unlistedViolations` vacio y
      `discrepancies` 0.
    - Entrada stale por archivo inexistente => cae en `staleMissingFile`, y el texto de
      `formatReport` contiene la accion de actualizar la ruta.
    - Entrada stale por archivo que ya no viola => cae en `staleNoLongerViolating`, y el texto
      contiene la accion de borrar la entrada.
    - D-15: con una allowlist cuya tabla figura tambien en una lista strict inyectada
      (pasando la tabla por parametro, sin declarar ningun modulo migrado en
      `tenant-tables.ts`) => cae en `strictWithAllowlist`.
    - D-14: `baseAllowlist` sin una entrada que la actual si tiene => esa entrada cae en
      `gainedEntries`. Y el caso inverso (la base tiene una que la actual borro) NO es
      discrepancia: achicar siempre es legal.
    - D-12: las exenciones salen en `exemptions` y NO suman a `discrepancies`; el texto del
      reporte marca las de alcance de archivo.
    - Exit codes via `runLint`: allowlist malformada => 2; `--base` irresoluble => 2; flag
      desconocido => 2; fixture limpio => 0; fixture con violacion no listada => 1.

    Mantener el idioma del archivo: mensaje largo y accionable como segundo argumento de
    `expect`. Ampliar el `QUE HACER CUANDO ESTE GUARD SE CAIGA` del docblock con la
    instruccion explicita: agrandar la allowlist NO es una opcion valida para poner el build
    en verde.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-170-deteccion/el-templo-api && pnpm exec vitest run test/tenancy/con-06-lint.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run test/tenancy/con-06-lint.test.ts` sale 0 con al menos 12 tests
    - Hay un `it` por cada uno de los cuatro gates (no listada, stale x2, ganada, strict) y por cada exit code (0, 1, 2)
    - El test de D-15 inyecta la lista strict por parametro y NO modifica `TENANT_STRICT_MODULES`
    - Ningun test escribe sobre `el-templo-api/tenant-lint-allowlist.json` (verificable: `git status --porcelain el-templo-api/tenant-lint-allowlist.json` vacio despues de correr la suite)
    - Prueba negativa registrada en el SUMMARY: hacer que la resolucion de base devuelva "sin cambios" en vez de exit 2 deja ROJO el `it` de `--base` irresoluble; revertir sin commitear
  </acceptance_criteria>
  <done>Los cuatro gates del ratchet y los tres exit codes estan congelados por tests que corren sin `git` ni MySQL.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                | Description                                                                                                 |
| ----------------------- | ----------------------------------------------------------------------------------------------------------- |
| PR ↔ pipeline de CI     | El lint decide si el build queda rojo; la allowlist es el unico canal para tolerar deuda                    |
| rama base ↔ rama del PR | El ratchet compara dos revisiones: si la lectura de la base falla en silencio, el gate se vuelve decorativo |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                       | Disposition | Mitigation Plan                                                                                                                                |
| --------- | ---------------------- | ------------------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| T-170-05  | Tampering              | Agrandar la allowlist para poner el build en verde                              | mitigate    | Gate de entradas ganadas contra la base (D-14) + `note` del JSON + docblock del test que dice que agrandarla no es una opcion                  |
| T-170-04  | Repudiation            | El gate pasa en verde porque no pudo resolver la base (clone shallow, null SHA) | mitigate    | Fail-closed: `LintArgError` exit 2 con el mensaje que nombra `fetch-depth: 0`; `it` dedicado; jamas "asumir sin cambios"                       |
| T-170-15  | Tampering              | Puerta trasera: un comando que regenera la allowlist entera                     | mitigate    | D-16: el script NO tiene modo de generacion; el baseline se produce una sola vez en el plan 07 con un snippet descartable                      |
| T-170-16  | Tampering              | Ejecutar codigo de una revision arbitraria al leer la allowlist de la base      | mitigate    | La allowlist es JSON y se lee con `git show` + `JSON.parse`; nunca se importa ni se evalua un `.ts` de otra revision. `execFileSync` sin shell |
| T-170-01  | Information Disclosure | Deuda que crece: mas accesos sin `tenant_id` en el codigo                       | mitigate    | La allowlist solo puede achicarse; los dos gates de stale fuerzan el achique al migrar                                                         |
| T-170-17  | Elevation of Privilege | Tabla declarada migrada (throw activo) con accesos sin scope todavia tolerados  | mitigate    | Gate D-15: strict + entradas vivas = rojo                                                                                                      |
| T-170-SC  | Tampering              | Instalacion de paquetes                                                         | mitigate    | Cero deps nuevas; criterio de aceptacion sobre `pnpm-lock.yaml` sin cambios                                                                    |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit` sale 0
- `pnpm lint:tenant` corre y devuelve 0 o 1 con la allowlist vacia
- `pnpm lint:tenant --base=<sha inexistente>` sale 2
- `pnpm exec vitest run test/tenancy/con-06-lint.test.ts` verde, 12 o mas tests
- `git status --porcelain el-templo-api/pnpm-lock.yaml` vacio
</verification>

<success_criteria>

- Allowlist con formato decidido y justificado, arrancando vacia
- Cuatro gates del ratchet implementados y probados
- Contrato de exit codes 0/1/2 respetado, fail-closed ante base irresoluble
- Inventario de exenciones (D-12) en una sola corrida revisable
- Cero deps nuevas, cero migraciones
  </success_criteria>

<output>
Create `.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-05-SUMMARY.md` when done
</output>
