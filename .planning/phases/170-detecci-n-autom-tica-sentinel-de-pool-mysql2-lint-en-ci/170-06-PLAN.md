---
phase: 170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci
plan: 06
type: execute
wave: 6
depends_on: [170-04, 170-05]
files_modified:
  - el-templo-api/src/plugins/database.ts
  - el-templo-api/src/db/index.ts
  - el-templo-api/.env.example
  - el-templo-api/test/tenancy/con-05-sentinel.test.ts
autonomous: true
requirements: [CON-05]

must_haves:
  truths:
    - "Todo el SQL de la app pasa por el sentinel porque se instala sobre el unico pool, por debajo de Drizzle"
    - "Una query sin tenant_id adentro de db.transaction() la ve el sentinel"
    - "Un select sin where sobre una tabla strict hace throw pese a que Drizzle expande tenant_id en la proyeccion"
    - "El arranque de la app, las transacciones y las migraciones no se rompen por el sentinel"
    - "No existe un segundo camino de SQL sin vigilar"
  artifacts:
    - path: "el-templo-api/src/plugins/database.ts"
      provides: "installSentinel entre createPool y drizzle(pool) + stop() en onClose + decorate dbSentinel"
      contains: "installSentinel"
    - path: "el-templo-api/test/tenancy/con-05-sentinel.test.ts"
      provides: "Bateria de integracion contra SQL real de Drizzle, incluida transaccion"
      min_lines: 150
    - path: "el-templo-api/.env.example"
      provides: "Flag SENTINEL_INVENTORY documentado"
      contains: "SENTINEL_INVENTORY"
  key_links:
    - from: "el-templo-api/src/plugins/database.ts"
      to: "el-templo-api/src/db/sentinel/install.ts"
      via: "installSentinel(pool, ...) llamado ANTES de drizzle(pool, ...)"
      pattern: "installSentinel"
    - from: "el-templo-api/test/tenancy/con-05-sentinel.test.ts"
      to: "el-templo-api/src/db/sentinel/install.ts"
      via: "instalacion sobre un pool propio con strictTables inyectado (D-07)"
      pattern: "strictTables"
---

<objective>
Cablear el sentinel en el unico pool de la aplicacion y demostrar con SQL real de Drizzle
—incluida una transaccion— que ve lo que tiene que ver: el trap de la proyeccion, el
camino transaccional y el silencio de las tablas no strict.

Purpose: hasta este plan el sentinel es codigo con tests de laboratorio. Acá pasa a estar
instalado, con la regresion que prueba que no es ciego adentro de las 65 llamadas a
`.transaction(` del repo.
Output: `plugins/database.ts` cableado, `.env.example` con el flag, `src/db/index.ts`
desmentido, y `test/tenancy/con-05-sentinel.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-CONTEXT.md
@.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-RESEARCH.md
@.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-PATTERNS.md
@CLAUDE.md

**Trabajar en el worktree `/home/franco/projects/et-170-deteccion`.**

<interfaces>
Ya existente (plan 170-04), `src/db/sentinel/install.ts`:
  export function installSentinel(pool: mysql.Pool, opts: SentinelOptions): SentinelHandle;
  export interface SentinelHandle { inspect(sql); snapshot(); report(); stop() }
  export class TenantSentinelError extends Error { readonly sql: string; readonly tables: string[] }

`src/plugins/database.ts` en origin/master (42 lineas) — puntos de anclaje exactos:
:17-28 const pool = mysql.createPool({ ... }) ← el sentinel se instala DESPUES de esto
:30 const db = drizzle(pool, { schema, mode: "default" }) ← y ANTES de esta linea; no se toca
:32-33 fastify.decorate("db", db); fastify.decorate("dbPool", pool)
:35-37 fastify.addHook("onClose", async () => { await pool.end() }) ← sumar sentinel.stop()
:39 fastify.log.info("Database connected") ← ese logger es el que recibe el sentinel

Config de la base per-worker en tests: `test/setup.ts` setea `process.env.DB_NAME` a
`eltemplo_test_<VITEST_POOL_ID>`; `DB_HOST`/`DB_PORT`/`DB_USER`/`DB_PASSWORD` vienen de
`vitest.config.ts`. Un test puede crear su propio pool con esos valores.
</interfaces>

<known_traps>

- `createDbConnection()` en `src/db/index.ts:6-17` dice "Used by the main application" y
  MIENTE: tiene CERO consumidores. Es un segundo pool que quedaria sin sentinel.
- `createSingleConnection()` (`src/db/index.ts:23-29`) SI se usa (~8 scripts CLI) y devuelve
  una `Connection`, no un Pool: queda fuera del alcance del sentinel a proposito — esos
  caminos los cubre la regla `--tenant` de la fase 169 y sus exenciones.
- `.iterator()` de Drizzle toma la conexion CORE de callbacks y esquiva cualquier wrap de la
  capa promise. Hoy hay CERO usos de `.iterator(` en `src` (verificado por grep).
- El sentinel ve el control de transaccion por el mismo canal (`conn.query("begin")`): si el
  skiplist de no-DML fallara, reventarian transacciones legitimas.
  </known_traps>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar el sentinel en plugins/database.ts + flag en .env.example + desmentir src/db/index.ts</name>
  <files>el-templo-api/src/plugins/database.ts, el-templo-api/.env.example, el-templo-api/src/db/index.ts</files>
  <read_first>
    - el-templo-api/src/plugins/database.ts — COMPLETO (42 lineas)
    - el-templo-api/src/db/index.ts — COMPLETO (createDbConnection + createSingleConnection)
    - el-templo-api/.env.example — el bloque Wellhub del final es el molde de estilo
    - el-templo-api/src/db/sentinel/install.ts (plan 170-04)
  </read_first>
  <action>
    1. `src/plugins/database.ts`: llamar `installSentinel(pool, { log: fastify.log })` entre
       el `mysql.createPool({...})` y el `drizzle(pool, { schema, mode: "default" })`,
       guardando el handle en una constante del closure. NO tocar la linea del `drizzle(...)`
       — el wrap va POR DEBAJO del ORM. NO reemplazar el pool por un `Proxy`:
       `fastify.decorate("dbPool", pool)` y 18 sitios de test usan esa misma instancia.
       Ampliar el `declare module "fastify"` con `dbSentinel: SentinelHandle` y decorarlo
       (lo consumen los tests de este plan y el inventario del plan 08).
       En el hook `onClose` que ya existe, llamar `sentinel.stop()` ANTES de `pool.end()`
       (Pitfall 4: sin eso, el timer del resumen sobrevive al cierre).
       `fastify.log` satisface estructuralmente `TenantLogger` — pasarlo tal cual, sin
       adaptadores nuevos.
    2. `.env.example`: agregar al final un bloque con el estilo del de Wellhub (encabezado
       `# <Nombre> — <que es>` + 2-5 lineas de prosa + la variable COMENTADA por ser
       opcional) para `SENTINEL_INVENTORY`. La prosa dice: que con `SENTINEL_INVENTORY=1` el
       sentinel acumula TODAS las violaciones (incluidas las que en la corrida normal quedan
       en silencio) y las reporta agregadas; que sirve para producir el inventario
       deterministico corriendo la suite; y que apagado no cambia ningun comportamiento.
    3. `src/db/index.ts`: corregir el comentario mentiroso de `createDbConnection` — decir
       que tiene CERO consumidores, que el unico pool de la aplicacion es el de
       `src/plugins/database.ts` y que es el unico vigilado por el sentinel, asi que usar
       esta funcion crearia un camino de SQL sin vigilancia. Agregar tambien, sobre
       `createSingleConnection`, la nota de alcance: devuelve una `Connection` (no un Pool),
       la usan ~8 scripts CLI y esos caminos los cubre la regla `--tenant` de la fase 169,
       no el sentinel. NO borrar ninguna de las dos funciones en esta fase (borrar codigo
       muerto es un cambio de otra naturaleza y no aporta a CON-05).
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-170-deteccion/el-templo-api && pnpm exec tsc --noEmit && node -e "const s=require('fs').readFileSync('src/plugins/database.ts','utf8'); const i=s.indexOf('installSentinel('), d=s.indexOf('drizzle(pool'); if(i<0||d<0||i>d||!s.includes('dbSentinel')||s.includes('new Proxy')){console.error('WIRING_FAIL');process.exit(1)} console.log('WIRING_OK')" && grep -c SENTINEL_INVENTORY .env.example | grep -qv "^0$" && echo ENV_OK</automated>
  </verify>
  <acceptance_criteria>
    - En `src/plugins/database.ts`, la linea de `installSentinel(` aparece DESPUES de `mysql.createPool(` y ANTES de `drizzle(pool`
    - `fastify.decorate("dbPool", pool)` sigue recibiendo la misma instancia (cero `new Proxy` en el archivo)
    - El hook `onClose` llama `stop()` del handle antes de `pool.end()`
    - `declare module "fastify"` declara `dbSentinel`
    - `.env.example` contiene `SENTINEL_INVENTORY` comentado, con prosa que explica que hace y que apagado no cambia nada
    - `src/db/index.ts` ya no afirma que `createDbConnection` la usa la aplicacion, y nombra `src/plugins/database.ts` como el unico pool vigilado
    - `pnpm exec tsc --noEmit` sale 0
  </acceptance_criteria>
  <done>Levantar la app instala el sentinel sobre el unico pool, y cerrarla apaga su timer.</done>
</task>

<task type="auto">
  <name>Task 2: Bateria de integracion contra SQL real de Drizzle (incluida transaccion)</name>
  <files>el-templo-api/test/tenancy/con-05-sentinel.test.ts</files>
  <read_first>
    - el-templo-api/test/tenancy/con-03-write-paths-tenant-id.test.ts — idioma de test/tenancy:
      imports, docblock con QUE HACER CUANDO ESTE GUARD SE CAIGA, estructura de dos describe
    - el-templo-api/test/setup.ts (~:20-40) — como se resuelve la base per-worker (DB_NAME)
    - el-templo-api/src/db/sentinel/install.ts y analyze.ts (planes 170-02 y 170-04)
    - .planning/phases/170-.../170-RESEARCH.md (Pitfalls 1, 2 y 3)
  </read_first>
  <action>
    Crear `test/tenancy/con-05-sentinel.test.ts` con un `describe("sentinel — SQL real de
    Drizzle")` que en `beforeAll` cree SU PROPIO pool con `mysql.createPool` usando
    `process.env.DB_HOST`, `DB_PORT`, `DB_USER`, `DB_PASSWORD` y `DB_NAME` (la base
    per-worker que dejo `test/setup.ts`), instale el sentinel con `strictTables` inyectado
    (D-07: una tabla gym-owned REAL, por ejemplo `users`, sin declarar ningun modulo migrado
    en `tenant-tables.ts`), `mode: "throw"`, `inventory: true` y un `TenantLogger` falso, y
    arme `drizzle(pool, { schema, mode: "default" })` encima. En `afterAll`: `stop()` del
    handle y `pool.end()`.

    Casos (uno por `it`):
    - **Trap de la proyeccion (Pitfall 2):** `db.select().from(schema.users)` SIN `where`
      lanza `TenantSentinelError` y el mensaje contiene el texto del SQL. Comentar en el
      test que Drizzle expande la proyeccion e incluye `tenant_id`, por lo que un chequeo de
      presencia sobre el string completo lo dejaria pasar: esta es la fuga mas grave posible.
    - La misma consulta con `.where(tenantWhere(schema.users, ctx))` NO lanza.
    - **Regresion de Pitfall 1:** una query sin filtro de tenant EJECUTADA DENTRO de
      `db.transaction(async (tx) => ...)` lanza. Comentar que si alguien saca el wrap de
      `getConnection`, este `it` es el unico que se pone rojo, y que representa 65 call sites
      de `.transaction(` en 25 archivos.
    - Una transaccion que solo hace queries cumplidoras commitea sin error: prueba que
      `begin`/`commit` no disparan (Pitfall 3).
    - Tabla gym-owned NO strict (por ejemplo `bookings`) sin filtro de tenant: NO lanza, el
      logger falso no recibio nada, y `snapshot().totalViolations` subio (D-08).
    - Modo inventario: `report()` incluye la tabla no strict con su contador.
    - INSERT con `tenantValues(...)` sobre una tabla strict: NO lanza (el camino correcto no
      se castiga).

    Docblock con `QUE HACER CUANDO ESTE GUARD SE CAIGA`: "no saques la tabla de la lista
    strict ni desactives el sentinel — la query que lo disparo no tiene filtro de tenant;
    arreglala con tenantWhere/tenantValues o escribi la exencion con motivo". Y el
    `POR QUE ESTE ARCHIVO SI TOCA LA DB` (a diferencia de los unitarios de los planes 02 y
    04): lo que se prueba acá es el SQL que emite Drizzle de verdad y el camino de
    `getConnection`, que un pool falso no puede reproducir.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-170-deteccion/el-templo-api && pnpm exec vitest run test/tenancy/con-05-sentinel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run test/tenancy/con-05-sentinel.test.ts` sale 0 con al menos 7 tests
    - Existe un `it` que ejecuta la query violadora DENTRO de `db.transaction(` y afirma el throw
    - Existe un `it` del trap de la proyeccion cuyo nombre menciona la proyeccion
    - El `afterAll` llama `stop()` y `pool.end()` (la suite no queda colgada: el comando de verify termina solo)
    - La lista strict se inyecta por parametro; `git diff el-templo-api/src/db/tenant-tables.ts` NO muestra cambios en este plan
    - Prueba negativa registrada en el SUMMARY: quitar el wrap de `getConnection` en `install.ts` deja ROJO solo el `it` de la transaccion; revertir sin commitear
  </acceptance_criteria>
  <done>El sentinel esta probado contra el SQL que Drizzle emite de verdad, adentro y afuera de una transaccion.</done>
</task>

<task type="auto">
  <name>Task 3: Guards de cableado y de caminos sin vigilar</name>
  <files>el-templo-api/test/tenancy/con-05-sentinel.test.ts</files>
  <read_first>
    - el-templo-api/test/tenancy/con-05-sentinel.test.ts (Task 2)
    - el-templo-api/test/tenancy/con-03-write-paths-tenant-id.test.ts (~:16-37) — el
      "POR QUE EL GUARD ES POR IMPORT Y NO POR GREP" y la forma de los guards estaticos
    - el-templo-api/src/plugins/database.ts (Task 1)
  </read_first>
  <action>
    Agregar al mismo archivo un primer `describe` (que corre ANTES del de integracion) con
    los guards, incluyendo en su nombre la palabra "cableado":

    - **Cableado por lectura del fuente:** leer `src/plugins/database.ts` y afirmar que el
      indice de `installSentinel(` es menor que el indice de `drizzle(pool` — el sentinel va
      POR DEBAJO del ORM. Mensaje de fallo: envolver `drizzle(pool)` o el objeto `db` deja
      fuera el SQL crudo, que es exactamente lo que la capa 3 vino a resolver.
    - **Cableado en runtime:** con `createTestApp()`, afirmar que `app.dbSentinel` existe y
      que despues de una lectura sin filtro de tenant sobre una tabla gym-owned (por ejemplo
      `app.db.select().from(schema.bookings).limit(1)`) el `snapshot().totalViolations` de la
      app subio. Con `TENANT_STRICT_MODULES` vacia no hay throw, asi que el test es seguro.
    - **Guard de `.iterator(`:** afirmar que no aparece ningun uso de `.iterator(` bajo
      `src/` (hoy hay cero). Motivo escrito: `MySql2PreparedQuery.iterator()` toma la
      conexion core de callbacks y esquiva cualquier wrap de la capa promise, asi que
      introducir uno abriria un agujero silencioso. Que hacer si se cae: no borres el guard,
      decidi conscientemente si esa query necesita streaming y como se vigila.
    - **Guard de segundo pool:** afirmar que `createDbConnection` no tiene consumidores en
      `src/` (solo su definicion en `src/db/index.ts`). Que hacer si se cae: usar el pool de
      `plugins/database.ts` o instalarle el sentinel al pool nuevo.

    Los guards de grep se hacen sobre el arbol de `src/` filtrando comentarios donde
    corresponda, con mensaje accionable como segundo argumento de `expect`.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-170-deteccion/el-templo-api && pnpm exec vitest run test/tenancy/con-05-sentinel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - El describe de guards corre antes del de integracion y todos sus `it` pasan
    - El guard de orden compara posiciones reales dentro de `src/plugins/database.ts` (no un simple "contiene installSentinel")
    - El guard de `.iterator(` y el de `createDbConnection` tienen mensaje con "que hacer" concreto
    - El guard de runtime usa `app.dbSentinel` y afirma un incremento de contador, no solo la existencia del decorator
    - Prueba negativa registrada en el SUMMARY: mover `installSentinel(` debajo de `drizzle(pool` deja ROJO el guard de orden; revertir sin commitear
  </acceptance_criteria>
  <done>Un PR futuro no puede desarmar el cableado ni abrir un segundo camino de SQL sin que la suite lo diga.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary            | Description                                                                                                   |
| ------------------- | ------------------------------------------------------------------------------------------------------------- |
| app Fastify ↔ MySQL | Todo el SQL de la app cruza el pool de `plugins/database.ts`; cualquier otro pool es un camino sin vigilancia |
| ORM ↔ driver        | El sentinel tiene que quedar por debajo de Drizzle para ver el SQL final                                      |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                                               | Disposition | Mitigation Plan                                                                                                             |
| --------- | ---------------------- | ------------------------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------- |
| T-170-01  | Information Disclosure | queries transaccionales sin `tenant_id`                                                                 | mitigate    | `it` de integracion que ejecuta la query dentro de `db.transaction(` y exige el throw                                       |
| T-170-08  | Tampering              | segundo pool sin sentinel (`createDbConnection`) o bypass por `.iterator()`                             | mitigate    | Comentario corregido en `src/db/index.ts` + dos guards de test con mensaje accionable                                       |
| T-170-18  | Denial of Service      | el sentinel rompe el arranque, las transacciones o las migraciones                                      | mitigate    | `it` de transaccion cumplidora que commitea; skiplist de no-DML probado en el plan 02; en prod el modo es log (nunca throw) |
| T-170-07  | Denial of Service      | el timer del resumen sobrevive al cierre de la app y cuelga la suite                                    | mitigate    | `stop()` en el hook `onClose` + `afterAll` del test que cierra pool y handle                                                |
| T-170-19  | Repudiation            | alguien mueve el `installSentinel` por encima de `drizzle(pool)` y el sentinel deja de ver el SQL crudo | mitigate    | Guard de orden por lectura del fuente, con prueba negativa demostrada                                                       |
| T-170-02  | Information Disclosure | el logger real recibe SQL en tests                                                                      | accept      | En tests el logger es falso y `LOG_LEVEL` es silent; el SQL ya viene con placeholders                                       |
| T-170-SC  | Tampering              | instalacion de paquetes                                                                                 | mitigate    | Cero deps nuevas                                                                                                            |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit` sale 0
- `pnpm exec vitest run test/tenancy/con-05-sentinel.test.ts` verde y TERMINA (no queda colgado)
- `grep -n "installSentinel\|drizzle(pool" src/plugins/database.ts` muestra el orden correcto
- `git diff --stat` toca 4 archivos y ninguna migracion
</verification>

<success_criteria>

- Sentinel instalado sobre el unico pool, por debajo de Drizzle, con `stop()` en `onClose`
- Regresion de transaccion en verde (Pitfall 1 cerrado con evidencia)
- Trap de la proyeccion probado contra SQL real de Drizzle (Pitfall 2 cerrado)
- Caminos sin vigilancia (`createDbConnection`, `.iterator(`) declarados y guardados
- Cero migraciones, cero deps nuevas
  </success_criteria>

<output>
Create `.planning/phases/170-detecci-n-autom-tica-sentinel-de-pool-mysql2-lint-en-ci/170-06-SUMMARY.md` when done
</output>
