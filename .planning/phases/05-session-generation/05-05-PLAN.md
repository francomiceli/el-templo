---
phase: 05-session-generation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/sessions/pipeline/index.ts
  - el-templo-api/src/modules/sessions/pipeline/initium-pipeline.ts
  - el-templo-api/src/modules/sessions/pipeline/stage-1-rotator.ts
  - el-templo-api/src/modules/sessions/pipeline/context.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "POST /sessions/generate returns complete session with 5 blocks including INITIUM"
    - "INITIUM block skips SPOM lookup (no route-based resolution)"
    - "INITIUM uses fixed warmup parameters (intensity ~30%, no repsBudget)"
    - "INITIUM selects exercises from FLOW pattern or Movilidad category"
  artifacts:
    - path: "el-templo-api/src/modules/sessions/pipeline/initium-pipeline.ts"
      provides: "Special INITIUM block generation bypassing standard pipeline"
      min_lines: 80
    - path: "el-templo-api/src/modules/sessions/pipeline/index.ts"
      provides: "INITIUM branch detection before runBlockPipeline"
      contains: "role === 'INITIUM'"
  key_links:
    - from: "el-templo-api/src/modules/sessions/pipeline/index.ts"
      to: "initium-pipeline.ts"
      via: "import and conditional call"
      pattern: "runInitiumPipeline"
    - from: "initium-pipeline.ts"
      to: "format-fallback.ts"
      via: "import selectFormatWithFallback"
      pattern: "selectFormatWithFallback"
---

<objective>
Fix INITIUM block generation to bypass SPOM lookup

Purpose: INITIUM is a warmup block that per system specs (line 266, 506) has NO route and does NOT use reps_budget. The current implementation hardcodes 'MOV' route which doesn't exist in SPOM rules, causing 500 error. This plan creates a dedicated INITIUM pipeline path with fixed warmup parameters.

Output: Working session generation where INITIUM uses hardcoded warmup config instead of SPOM lookup
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-session-generation/05-01-SUMMARY.md
@.planning/phases/05-session-generation/05-UAT.md

# Key spec references:
# - docs/system-specs/Especificacion-Parte-1-Objetivo-y-Modelo.txt:266 - "route.code obligatorio excepto en Initium"
# - docs/system-specs/Especificacion-Parte-2-SPOM-e-Intensidad.txt:506 - "INITIUM -> no usa reps_budget (warm-up / skill prep)"
# - docs/system-specs/Especificacion-Parte-3-Formatos-y-Ejercicios.txt:584 - "INITIUM -> preferir Technical > Structure-based"

# Existing pipeline files:
@el-templo-api/src/modules/sessions/pipeline/index.ts
@el-templo-api/src/modules/sessions/pipeline/context.ts
@el-templo-api/src/modules/sessions/pipeline/stage-1-rotator.ts
@el-templo-api/src/modules/sessions/pipeline/stage-5-format.ts
@el-templo-api/src/modules/sessions/pipeline/stage-6-exercises.ts
@el-templo-api/src/modules/sessions/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create INITIUM Pipeline Handler</name>
  <files>
    el-templo-api/src/modules/sessions/pipeline/initium-pipeline.ts
  </files>
  <action>
Create a dedicated INITIUM pipeline handler that bypasses SPOM-based resolution.

The INITIUM block is a warmup/mobility block that:
- Has NO route (per spec line 266: "route.code obligatorio excepto en Initium")
- Does NOT use reps_budget (per spec line 506)
- Uses fixed warmup intensity (~30%)
- Selects 3-4 mobility/flow exercises

Implementation:

1. Create `initium-pipeline.ts` with function `runInitiumPipeline(ctx: BlockContext, db: MySql2Database)`:

2. Hardcode INITIUM parameters (per spec and existing validator ranges INITIUM: 10-40%):
   - route: 'INITIUM' (marker, not a real route)
   - intensity: 30 (low warmup intensity)
   - pattern: 'FLOW' (mobility/warmup pattern)
   - category: 'Movilidad' (warmup category)
   - repsBudget: 0 (not used per spec)
   - exerciseCount: 3 (fixed warmup count)
   - difficultyBucket: '3' (easy warmup exercises)

3. Create mock contraction mix for INITIUM (even distribution or all CON):
   - contractionMix: { CON: 2, EXC: 1, ISO: 0 } (focus on concentric for warmup)

4. Call `selectFormatWithFallback` with:
   - block: 'initium'
   - level: derived from ctx.levelGroup
   - intensity: 30

5. For exercise selection, query exercises directly:
   - Filter by pattern LIKE '%FLOW%' OR category = 'Movilidad'
   - Limit to 3 exercises
   - Use allowedLevels from levelGroup
   - Sort by id ASC for determinism

6. Generate simple prescriptions:
   - reps: 10 per exercise (standard warmup)
   - rest: 30s (short warmup rest)

7. Add trace events:
   - INITIUM_PIPELINE_USED: INFO - marks special handling
   - INITIUM_FORMAT_SELECTED: INFO
   - INITIUM_EXERCISES_SELECTED: INFO
   - INITIUM_PRESCRIPTIONS_GENERATED: INFO

8. Return BlockPlan with all required fields populated

Import types and functions from:
- ./context (BlockContext, createTraceEvent, appendTrace)
- ../fallback/format-fallback (selectFormatWithFallback)
- ../types (BlockPlan, ContractionMix, etc.)
- drizzle-orm for exercise queries
  </action>
  <verify>
File exists: `ls el-templo-api/src/modules/sessions/pipeline/initium-pipeline.ts`
TypeScript compiles: `cd el-templo-api && npx tsc --noEmit`
  </verify>
  <done>
initium-pipeline.ts exports `runInitiumPipeline` function that returns complete BlockPlan for INITIUM role without SPOM lookup
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate INITIUM Branch in Pipeline Orchestrator</name>
  <files>
    el-templo-api/src/modules/sessions/pipeline/index.ts
    el-templo-api/src/modules/sessions/pipeline/stage-1-rotator.ts
  </files>
  <action>
Modify the pipeline orchestrator to branch to INITIUM-specific handler.

1. In `pipeline/index.ts`:
   - Import `runInitiumPipeline` from './initium-pipeline'
   - At the START of `runBlockPipeline`, before stage 1:
     ```typescript
     // INITIUM uses special pipeline (no SPOM lookup per spec)
     if (initialContext.role === 'INITIUM') {
       return runInitiumPipeline(initialContext, db);
     }
     ```
   - This ensures INITIUM never goes through stages 1-7

2. In `stage-1-rotator.ts`:
   - Remove the INITIUM_ROUTE = 'MOV' constant
   - Remove the INITIUM special case block (it will never be called now)
   - The stage will only handle NUCLEUS, DEUTEROS_1, DEUTEROS_2, ATHLOS_EPIKOS

The pipeline flow becomes:
- INITIUM -> runInitiumPipeline (fixed params, direct format/exercise selection)
- Other roles -> runBlockPipeline (stages 1-7 with SPOM lookup)
  </action>
  <verify>
TypeScript compiles: `cd el-templo-api && npx tsc --noEmit`
INITIUM handling removed from stage-1: `grep -c "INITIUM" el-templo-api/src/modules/sessions/pipeline/stage-1-rotator.ts` should return 0
INITIUM branch exists: `grep -c "runInitiumPipeline" el-templo-api/src/modules/sessions/pipeline/index.ts` should return 2 (import + call)
  </verify>
  <done>
Pipeline orchestrator branches to dedicated INITIUM handler before stage 1, and stage-1-rotator no longer has INITIUM special case
  </done>
</task>

<task type="auto">
  <name>Task 3: Test Session Generation End-to-End</name>
  <files>
    (no files created - verification only)
  </files>
  <action>
Verify the fix by running actual session generation.

1. Start the dev server:
   ```bash
   cd el-templo-api && npm run dev &
   sleep 3
   ```

2. Generate a session (adjust week/day if needed based on rotator data):
   ```bash
   curl -X POST http://localhost:3000/sessions/generate \
     -H "Content-Type: application/json" \
     -d '{"week": 1, "day": "lunes", "levelGroup": "sigma"}' \
     2>/dev/null | jq '.blocks[] | {role, route, intensity, exerciseCount: (.exercises | length)}'
   ```

3. Expected output should show:
   - INITIUM with route='INITIUM', intensity=30, ~3 exercises
   - Other blocks with real routes (FL, MU, etc.) and higher intensity

4. Check trace shows INITIUM_PIPELINE_USED event:
   ```bash
   curl -X POST http://localhost:3000/sessions/generate \
     -H "Content-Type: application/json" \
     -d '{"week": 1, "day": "lunes", "levelGroup": "sigma"}' \
     2>/dev/null | jq '.blocks[0].trace[] | select(.code | contains("INITIUM"))'
   ```

5. Stop the dev server:
   ```bash
   pkill -f "node.*el-templo-api" || true
   ```

If curl fails with connection error, the fix may still be valid - the key is TypeScript compilation success from Task 2.
  </action>
  <verify>
Either:
- curl returns 200 with 5 blocks (INITIUM + 4 others), OR
- TypeScript compiles and dev server starts without immediate crash
  </verify>
  <done>
Session generation no longer fails with "No SPOM rule found for week=1, route=MOV" error. INITIUM block generates with fixed warmup parameters.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. TypeScript compilation passes:
   ```bash
   cd el-templo-api && npx tsc --noEmit
   ```

2. INITIUM pipeline file exists with correct exports:
   ```bash
   grep "export.*runInitiumPipeline" el-templo-api/src/modules/sessions/pipeline/initium-pipeline.ts
   ```

3. No 'MOV' route references remain:
   ```bash
   grep -r "MOV" el-templo-api/src/modules/sessions/pipeline/ || echo "Clean - no MOV references"
   ```

4. Session generation API works (manual test):
   - POST /sessions/generate returns 200 with complete session
   - INITIUM block has route='INITIUM', intensity=30
   - Other blocks have real routes from rotator
</verification>

<success_criteria>
1. POST /sessions/generate no longer returns 500 error about missing SPOM rule for MOV
2. INITIUM block generates with fixed warmup parameters (intensity=30, route='INITIUM')
3. INITIUM has 3 exercises with warmup-appropriate prescriptions
4. Other 4 blocks (NUCLEUS, DEUTEROS_1, DEUTEROS_2, ATHLOS_EPIKOS) still use normal SPOM pipeline
5. All TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-session-generation/05-05-SUMMARY.md`
</output>
