---
phase: 58-production-deployment
plan: 02
type: execute
wave: 2
depends_on:
  - 58-01
files_modified: []
autonomous: false
requirements:
  - DEPLOY-01

must_haves:
  truths:
    - "Production API, admin app, and member app serve the same version as staging"
    - "All database migrations (0000-0037) have run successfully on production"
    - "Production database has real operational data (6 branches with correct capacities, 6 subscription plans, schedules)"
    - "Smoke tests pass on production URLs (API health, admin login, app login)"
  artifacts: []
  key_links:
    - from: "staging branch"
      to: "master branch"
      via: "git merge"
      pattern: "merge staging into master"
---

<objective>
Merge staging to master to trigger production deployment, then seed the production database with real operational data.

Purpose: Bring production to parity with staging so all environments run identical code, and populate production with the real branch/plan/schedule data needed for operations.
Output: Production running same code as staging, production database seeded with operational data.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-production-deployment/58-CONTEXT.md
@.planning/phases/58-production-deployment/58-01-SUMMARY.md
@.github/workflows/deploy.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge staging to master and push</name>
  <files></files>
  <action>
    Merge the staging branch into master to trigger the production deploy pipeline.

    1. `git checkout master`
    2. `git merge staging` (fast-forward or merge commit -- should fast-forward since master hasn't diverged)
    3. `git push origin master`

    This triggers the production CI pipeline (deploy.yml): detect changes, build all 3 apps (type check + tests + build), deploy via rsync, run migrations (0000-0037), restart PM2 process, smoke test /health.

    DO NOT force push. If the merge has conflicts, stop and report them.

    After pushing, switch back to the staging branch: `git checkout staging`

  </action>
  <verify>
    <automated>git log master --oneline -1</automated>
  </verify>
  <done>
    - Master branch contains all staging commits (v4.0 phases 45-57 + booking/attendance unification + production seed script)
    - Push to origin/master triggers the Deploy workflow
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify production CI passes and production deployment works</name>
  <files></files>
  <action>
    Human verifies that the production CI pipeline passed and production is serving the latest code.

    What was built: Staging merged to master and pushed. The "Deploy" GitHub Actions workflow should auto-trigger and deploy to production (build all 3 apps, rsync to server, run migrations 0000-0037, restart API, smoke test).

    How to verify:
    1. Check GitHub Actions: verify the "Deploy" workflow completed successfully (all jobs green)
    2. Alternatively run: `gh run list --workflow=deploy.yml --limit=1` to check status
    3. Visit the production API health endpoint and confirm it returns 200
    4. Visit the production admin app and confirm it loads (login page)
    5. Visit the production member app and confirm it loads (login page)
    6. Log into the admin app with real credentials and verify the dashboard loads
    7. Confirm migration 0037 (booking_attendance_unification) ran without errors in the deploy logs

    Resume signal: Type "production deployed" if CI passed and production is up, or describe any issues.

  </action>
  <verify>
    <automated>gh run list --workflow=deploy.yml --limit=1</automated>
  </verify>
  <done>Production CI pipeline passed (all jobs green), production API/admin/app are accessible, migrations ran successfully</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Run production seed on server</name>
  <files></files>
  <action>
    The production seed script needs to run on the production server against the production database. Claude cannot SSH into the production server.

    SSH into the production server and run the seed script:

    ```bash
    cd /path/to/api/deploy  # (the API_DEPLOY_PATH from your secrets)
    CONFIRM_PRODUCTION_SEED=yes NODE_ENV=production node dist/db/seed-production.js
    ```

    The script will:
    - Update/create 6 branches (alem, constitucion, jujuy, mogotes, moreno, Templo Online) with correct capacities
    - Create 6 subscription plans (Flex, Flex+, Foundation, Foundation+, Performance, Sesion de Prueba)
    - Create 1 activity (Sesion Grupal)
    - Create schedule slots for all 5 physical branches (Mon-Fri 8 slots + Saturday 3 slots)
    - All operations are idempotent (safe to re-run)

    Verify the seed output shows all data created/updated without errors.

    Resume signal: Type "seed complete" with the seed output summary, or describe any issues.

  </action>
  <verify>
    <automated>echo "Manual verification required -- production seed run on server"</automated>
  </verify>
  <done>Production database has 6 branches with correct capacities, 6 subscription plans matching real tiers, 1 activity, and schedule slots for all 5 physical branches</done>
</task>

</tasks>

<verification>
- Production API health endpoint returns 200
- Production admin app loads and login works
- Production member app loads and login works
- `gh run list --workflow=deploy.yml --limit=1` shows success
- Production database has 6 branches, 6 subscription plans, 1 activity, schedule slots for 5 branches
</verification>

<success_criteria>

- Production API, admin, and member app are running the same code version as staging
- All 37 migrations (0000-0037) have run on production database
- Production database is seeded with real operational data (branches, plans, activity, schedules)
- Smoke tests pass on all production URLs
- DEPLOY-01 requirement is satisfied
  </success_criteria>

<output>
After completion, create `.planning/phases/58-production-deployment/58-02-SUMMARY.md`
</output>
