---
phase: 58-production-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/seed-production.ts
  - el-templo-api/src/db/schema/bookings.ts
  - el-templo-api/src/modules/attendance/index.ts
  - el-templo-api/src/modules/attendance/schemas.ts
  - el-templo-api/src/modules/attendance/types.ts
  - el-templo-api/src/modules/scheduling/schemas.ts
  - el-templo-api/src/modules/scheduling/types.ts
  - el-templo-api/src/modules/shared/index.ts
  - el-templo-api/src/index.ts
  - el-templo-api/src/db/migrations/0037_booking_attendance_unification.sql
  - el-templo-api/src/jobs/mark-no-shows.ts
  - el-templo-api/src/modules/shared/qr-token.ts
  - el-templo-api/test/unit/format-params.test.ts
  - el-templo-admin/src/composables/useAttendanceApi.ts
  - el-templo-admin/src/router/routes.ts
  - el-templo-admin/src/types/attendance.ts
  - el-templo-admin/src/types/scheduling.ts
  - el-templo-admin/pnpm-lock.yaml
  - el-templo-app/src/composables/useSchedulingApi.ts
  - el-templo-app/src/pages/ReservasPage.vue
  - el-templo-app/src/types/scheduling.ts
autonomous: false
requirements:
  - DEPLOY-01

must_haves:
  truths:
    - "All v4.0 booking/attendance unification code is committed to staging branch"
    - "Production seed script creates the 6 real subscription plans, 6 branches, 1 activity, and schedule slots matching live operations"
    - "Staging CI pipeline passes (type check, tests, build for all 3 apps)"
    - "Staging deployment serves the unified booking/attendance code"
  artifacts:
    - path: "el-templo-api/src/db/seed-production.ts"
      provides: "Production seed script with real operational data"
      min_lines: 80
  key_links:
    - from: "el-templo-api/src/db/seed-production.ts"
      to: "el-templo-api/src/db/schema/subscription-plans.ts"
      via: "inserts real plan data"
      pattern: "subscriptionPlans"
    - from: "el-templo-api/src/db/seed-production.ts"
      to: "el-templo-api/src/db/schema/schedules.ts"
      via: "inserts schedule slots"
      pattern: "schedules"
---

<objective>
Commit all v4.0 uncommitted work (booking/attendance unification), create the production seed script with real operational data, and push to staging for CI verification.

Purpose: Prepare the staging branch with all v4.0 code committed and a production seed script ready, so we can verify everything builds/tests cleanly before merging to production.
Output: Clean staging branch with all WIP committed, production seed script ready, staging CI green.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-production-deployment/58-CONTEXT.md
@el-templo-api/src/db/seed-staging.ts
@el-templo-api/src/db/schema/subscription-plans.ts
@el-templo-api/src/db/schema/branches.ts
@el-templo-api/src/db/schema/activities.ts
@el-templo-api/src/db/schema/schedules.ts
@.docs/admin-docs/datos-membresias-actuales-templo.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit v4.0 WIP and create production seed script</name>
  <files>
    el-templo-api/src/db/seed-production.ts
    el-templo-api/src/db/schema/bookings.ts
    el-templo-api/src/modules/attendance/index.ts
    el-templo-api/src/modules/attendance/schemas.ts
    el-templo-api/src/modules/attendance/types.ts
    el-templo-api/src/modules/scheduling/schemas.ts
    el-templo-api/src/modules/scheduling/types.ts
    el-templo-api/src/modules/shared/index.ts
    el-templo-api/src/index.ts
    el-templo-api/src/db/migrations/0037_booking_attendance_unification.sql
    el-templo-api/src/jobs/mark-no-shows.ts
    el-templo-api/src/modules/shared/qr-token.ts
    el-templo-api/test/unit/format-params.test.ts
    el-templo-admin/src/composables/useAttendanceApi.ts
    el-templo-admin/src/router/routes.ts
    el-templo-admin/src/types/attendance.ts
    el-templo-admin/src/types/scheduling.ts
    el-templo-admin/pnpm-lock.yaml
    el-templo-app/src/composables/useSchedulingApi.ts
    el-templo-app/src/pages/ReservasPage.vue
    el-templo-app/src/types/scheduling.ts
  </files>
  <action>
    **Step A: Commit all v4.0 WIP (booking/attendance unification)**

    Stage all modified files from the working tree that are part of the unification work. Include:
    - All modified files (M): bookings.ts schema, attendance module files, scheduling module files, shared module, API index, admin composables/types/routes, app composables/pages/types, admin pnpm-lock.yaml
    - Deleted file (D): el-templo-admin/src/pages/AsistenciaHoyPage.vue (intentional removal)
    - New untracked files to commit: migration 0037, mark-no-shows.ts, qr-token.ts, format-params.test.ts

    DO NOT stage: seed-v4.ts, digital-initiatives/, .claude/agents/, run-phases.sh, el-templo-web/dist, el-templo-web/.data/, .planning/ files, REP_COUNT_DIAGNOSIS.md, SESSION_PIPELINE_ADMIN_EDIT_REFACTOR_PLAN.md, el-templo-api/.planning/

    Also stage `el-templo-admin/pnpm-lock.yaml` (modified) and `.planning/config.json` (modified, GSD config update).

    Commit message: "feat: unify booking and attendance modules with Spanish status enum"

    **Step B: Create production seed script**

    Create `el-templo-api/src/db/seed-production.ts` following the pattern of `seed-staging.ts` but with REAL operational data. Key differences from staging seed:

    1. **Safety check**: Require `CONFIRM_PRODUCTION_SEED=yes` env var. Refuse to run without it.
    2. **No SPOM seeding** (production already has SPOM data from initial setup).
    3. **No user deletion** (production has real users -- this seed is ADDITIVE for operational data only).
    4. **No faker dependency** (real data only).

    Seed the following operational data (use INSERT IGNORE or ON DUPLICATE KEY patterns to be idempotent):

    **Branches** (6 total, update existing + add missing):
    - alem: name="El Templo Alem", code="ALEM", maxCapacity=12
    - constitucion: name="El Templo Constitucion", code="CONST", maxCapacity=22
    - jujuy: name="El Templo Jujuy", code="JUJUY", maxCapacity=12
    - mogotes: name="El Templo Mogotes", code="MOGOTES", maxCapacity=22
    - moreno: name="El Templo Moreno", code="MORENO", maxCapacity=22 (note: code is MORENO, not CENTRO -- rename from old Centro)
    - Templo Online: name="Templo Online", code="ONLINE", isVirtual=true, maxCapacity=999

    For existing branches that need capacity updates, use `ON DUPLICATE KEY UPDATE maxCapacity = VALUES(maxCapacity)` on the `code` unique column.

    **Activity** (1):
    - name="Sesion Grupal", isActive=true

    **Subscription Plans** (6 total):
    1. Flex: tier=flex, bookingMode=fixed, priceRegular=80000, priceZero=65000, durationDays=30, classesPerWeek=2, multiBranch=false, isTrial=false
    2. Flex+: tier=flex, bookingMode=flexible, priceRegular=100000, priceZero=80000, durationDays=30, classesPerWeek=6, multiBranch=false, isTrial=false
    3. Foundation: tier=foundation, bookingMode=fixed, priceRegular=250000, priceZero=220000, durationDays=120, classesPerWeek=2, multiBranch=false, isTrial=false
    4. Foundation+: tier=foundation, bookingMode=flexible, priceRegular=350000, priceZero=315000, priceCreditCard=370000, durationDays=120, classesPerWeek=6, multiBranch=true, isTrial=false
    5. Performance: tier=performance, bookingMode=flexible, priceRegular=600000, priceZero=560000, priceCreditCard=670000, durationDays=240, classesPerWeek=6, multiBranch=true, isTrial=false
    6. Sesion de Prueba: tier=other, bookingMode=flexible, priceRegular=0, priceZero=0, durationDays=1, classesPerWeek=1, multiBranch=false, isTrial=true

    **Schedule Slots** (per branch, for activity "Sesion Grupal"):
    - Mon-Fri (dayOfWeek 1-5): 07:00, 08:00, 09:00, 10:00, 17:00, 18:00, 19:00, 20:00 (1 hour each)
    - Saturday (dayOfWeek 6): 09:00, 10:00, 11:00 (ROM classes, same structure)
    - Create for all 5 physical branches (not Templo Online)

    Use `createSingleConnection()` from `./index` (same pattern as seed-staging.ts). Import schema tables from `./schema`. Use Drizzle's query builder for inserts.

    The script should:
    - Log each step clearly
    - Print a summary at the end
    - Handle existing data gracefully (idempotent -- can be re-run safely)
    - Use `process.exit(0)` on success, `process.exit(1)` on error
    - Export the function for potential programmatic use
    - Follow project logging guidelines (this is a script, `console.log` is acceptable for seed scripts like seed-staging.ts does)

    Commit the seed script: "feat(58): add production seed script with real operational data"

    **Step C: Push to staging**

    Run `git push origin staging` to trigger the staging CI pipeline.

  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>
    - All v4.0 WIP committed to staging branch (no uncommitted booking/attendance changes remain)
    - seed-production.ts exists and type-checks
    - Staging branch pushed to origin
    - No files that should be excluded (.planning/, seed-v4.ts, digital-initiatives/, etc.) were committed
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify staging CI passes and staging deployment works</name>
  <files></files>
  <action>
    Human verifies that the staging CI pipeline passed and the staging deployment is serving the unified code.

    What was built: All v4.0 code committed and pushed to staging. CI pipeline should auto-trigger (type check, tests, build, deploy, smoke test).

    How to verify:
    1. Check GitHub Actions: go to the repository's Actions tab and verify the "Deploy Staging" workflow completed successfully (all jobs green)
    2. Alternatively run: `gh run list --workflow=deploy-staging.yml --limit=1` to check status
    3. Visit the staging API health endpoint and confirm it returns 200
    4. Visit the staging admin app and confirm it loads (login page)
    5. Visit the staging member app and confirm it loads (login page)
    6. Confirm the booking/attendance unification is live on staging (the old AsistenciaHoy page should be gone, attendance is now part of the scheduling flow)

    Resume signal: Type "staging verified" if CI passed and staging looks good, or describe any issues.

  </action>
  <verify>
    <automated>gh run list --workflow=deploy-staging.yml --limit=1</automated>
  </verify>
  <done>Staging CI pipeline passed (all jobs green) and staging apps are accessible and serving the unified booking/attendance code</done>
</task>

</tasks>

<verification>
- `git log --oneline -3` shows the WIP commit and seed script commit
- `git status` shows no uncommitted booking/attendance changes (only excluded files like seed-v4.ts, digital-initiatives/, etc.)
- `el-templo-api/src/db/seed-production.ts` exists and contains 6 plans, 6 branches, 1 activity, schedule data
- Staging CI pipeline passes (type check, tests, builds for API + admin + app)
</verification>

<success_criteria>

- All v4.0 booking/attendance unification code committed to staging
- Production seed script with real operational data created and type-checks
- Staging CI green (type check + tests + build + deploy + smoke test)
- Staging apps accessible and serving the unified code
  </success_criteria>

<output>
After completion, create `.planning/phases/58-production-deployment/58-01-SUMMARY.md`
</output>
