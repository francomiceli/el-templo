---
phase: 115-evento-desafio-de-la-barra
plan: 06
type: execute
wave: 3
depends_on:
  - 115-03
  - 115-05
files_modified:
  - el-templo-app/src/modules/bar-challenge/pages/Explicacion.vue
  - el-templo-app/src/modules/bar-challenge/pages/Timer.vue
  - el-templo-app/src/modules/bar-challenge/pages/Resultado.vue
autonomous: true
requirements:
  - R5
  - R6
  - R7
  - R8
  - R9
  - R10
  - R11

must_haves:
  truths:
    - "Explicacion.vue renderiza heading + body + 3 rules + CTA 'Comenzar'; tap navega a /desafio-barra/timer y dispara store.start() en mount del Timer"
    - "Timer.vue muestra cronómetro ascendente MM:SS via setInterval(100ms) que llama store.tick(); a los 90s exactos cambia gold-gradient color + texto '¡LO LOGRASTE! SEGUÍ AGUANTANDO'; KeepAwake activado en mount, allowSleep en cleanup; CTAs 'Tomar foto' (Capacitor Camera) + 'Finalizar' (navega a /resultado)"
    - "Resultado.vue muestra 'Aguantaste {N} segundos' + body según completed; si hay photoBase64 → preview + 'Compartir foto' (Capacitor Share + composer); si no → 'Sacar foto ahora' fallback; banner discreto si retry pendiente"
    - "Cámara denegada no rompe el flujo (timer + resultado siguen sin foto)"
    - "Estructura import compatible con plugins NO instalados todavía (Plan 08) — usar dynamic import() para @capacitor/camera y @capacitor/share, con guard que muestra mensaje 'función no disponible' si el import falla, así Plan 06 puede compilar antes de Plan 08"
    - "R11 single-attempt frontend guard: si `userStore.profile?.barChallengeAttemptedAt != null`, Timer.vue redirige a /desafio-barra/resultado en onMounted (ANTES de store.start()) y Explicacion.vue swap del CTA 'Comenzar' por 'Ver mi intento' (navega a /resultado, no a /timer). Deep-link directo a /timer no puede iniciar un segundo intento."
  artifacts:
    - path: "el-templo-app/src/modules/bar-challenge/pages/Explicacion.vue"
      provides: "Pantalla explicativa con CTA Comenzar / Ver mi intento (R11 guard)"
      contains: "Desafío de la Barra"
    - path: "el-templo-app/src/modules/bar-challenge/pages/Timer.vue"
      provides: "Pantalla fullscreen dark con cronómetro + camera + finalize + R11 guard"
      contains: "Tomar foto"
    - path: "el-templo-app/src/modules/bar-challenge/pages/Resultado.vue"
      provides: "Pantalla de resultado con share + banner retry + fallback foto"
      contains: "Compartir foto"
  key_links:
    - from: "el-templo-app/src/modules/bar-challenge/pages/Timer.vue"
      to: "el-templo-app/src/modules/bar-challenge/stores/useBarChallengeStore.ts"
      via: "store.start() / store.tick() via setInterval / store.setPhoto / store.finalize"
      pattern: "store\\.(start|tick|setPhoto|finalize)\\(\\)"
    - from: "el-templo-app/src/modules/bar-challenge/pages/Timer.vue"
      to: "src/stores/useUserStore"
      via: "userStore.profile.barChallengeAttemptedAt — R11 single-attempt guard"
      pattern: "barChallengeAttemptedAt"
    - from: "el-templo-app/src/modules/bar-challenge/pages/Explicacion.vue"
      to: "src/stores/useUserStore"
      via: "userStore.profile.barChallengeAttemptedAt — R11 CTA swap"
      pattern: "barChallengeAttemptedAt"
    - from: "el-templo-app/src/modules/bar-challenge/pages/Resultado.vue"
      to: "el-templo-app/src/modules/bar-challenge/composables/useImageComposer.ts"
      via: "composeWithFrame(photoBase64, '/desafio-barra/marco-placeholder.png')"
      pattern: "composeWithFrame\\("
---

<objective>
Implementar las 3 pages del flujo del desafío con todo el contenido UI lockeado en UI-SPEC + interacciones lockeadas en SPEC R5-R11. Timer usa setInterval(100ms) → store.tick() con Date.now() math (D-09 source of truth). Resultado compone foto+marco y comparte via Capacitor Share. Cámara denegada no rompe el flujo. R11 single-attempt guards a nivel UI: deep-link a /timer redirige a /resultado si ya hubo intento; Explicacion swap del CTA por "Ver mi intento".

Purpose: Cierra todo el flujo del desafío except (a) la integración del card en MiTemplo (Plan 07), (b) el install de plugins Capacitor (Plan 08). Para que Plan 06 sea compilable ANTES de Plan 08, los imports de @capacitor/camera y @capacitor/share usan dynamic `await import()` con try/catch — el código compila contra type-only declarations o se queda con un guard runtime que muestra "función no disponible".
Output: 3 pages production-ready con R11 enforcement client-side (defensa en profundidad sobre el 409 del backend).
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-evento-desafio-de-la-barra/115-SPEC.md
@.planning/phases/115-evento-desafio-de-la-barra/115-CONTEXT.md
@.planning/phases/115-evento-desafio-de-la-barra/115-UI-SPEC.md
@.planning/phases/115-03-SUMMARY.md
@.planning/phases/115-05-SUMMARY.md
@CLAUDE.md

@el-templo-app/src/modules/bar-challenge/stores/useBarChallengeStore.ts
@el-templo-app/src/modules/bar-challenge/composables/useImageComposer.ts

<interfaces>
<!-- API surface que las pages consumen. -->

KeepAwake (already installed `@capacitor-community/keep-awake`):

```ts
import { KeepAwake } from "@capacitor-community/keep-awake";
await KeepAwake.keepAwake();
await KeepAwake.allowSleep();
```

Capacitor Camera (NOT YET INSTALLED — handled in Plan 08; this Plan 06 uses dynamic import):

```ts
async function takePhoto(): Promise<string | null> {
  try {
    // Dynamic import so this file compiles before Plan 08 installs the dep.
    const { Camera, CameraResultType, CameraSource } =
      await import("@capacitor/camera");
    const photo = await Camera.getPhoto({
      quality: 80,
      source: CameraSource.Camera,
      resultType: CameraResultType.Base64,
      saveToGallery: false,
    });
    return photo.base64String ?? null;
  } catch (err: unknown) {
    logger.warn("camera unavailable or denied", {
      err: err instanceof Error ? err.message : String(err),
    });
    return null;
  }
}
```

Capacitor Share (NOT YET INSTALLED):

```ts
async function shareBlob(blob: Blob, filename: string): Promise<{ ok: boolean; fallback?: 'download' }> {
  try {
    const { Share } = await import('@capacitor/share')
    const { Filesystem, Directory } = await import('@capacitor/filesystem')
    // Convert blob to base64, write to cache, share path. If Filesystem isn't installed either,
    // fall back to <a download> link.
    ...
  } catch (err: unknown) {
    // fallback to download via blob URL
    return { ok: true, fallback: 'download' }
  }
}
```

Note: project may not have `@capacitor/filesystem`. If absent, use direct `Share.share({ files: [dataUrl] })` if supported, else fallback to creating an `<a download>` link with blob URL — show toast per UI-SPEC copy "Tu dispositivo no permite compartir directamente. Descargamos la foto."

Copy locks (UI-SPEC Copywriting Contract — verbatim):

- See full table in 115-UI-SPEC.md lines 137-171.

Timer math (D-09 — LOCKED):

- setInterval(100ms) trigger only re-renders / re-tick.
- secondsHeld = Math.floor((Date.now() - startTimestamp) / 1000)
- Visual change at exact 90s threshold (≥ 90).

Photo path on Timer (R7):

- Tap "Tomar foto" → takePhoto() → if result not null → store.setPhoto(result).
- Timer keeps running because the interval persists across the camera intent (the SO may throttle JS, but Date.now() math recovers the real elapsed on resume).

R11 single-attempt frontend guard (NEW — defensa en profundidad sobre el 409 del backend):

- `userStore.profile.barChallengeAttemptedAt: string | null` — set when backend returns success on POST /bar-challenge/attempt; persisted in profile.
- Explicacion.vue: si `attemptedAt != null` al mount, swap del CTA "Comenzar" (→ /timer) por "Ver mi intento" (→ /resultado). El texto explicativo sigue visible (sirve de contexto).
- Timer.vue: si `attemptedAt != null` en onMounted, ANTES de `store.start()`, hacer `router.replace('/desafio-barra/resultado')` y retornar early. Esto previene que un deep-link directo (o el botón "Comenzar" en una vista vieja con profile stale) inicie un segundo intento client-side. El backend sigue siendo authoritative (409 si llega un submit duplicado).
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Explicacion.vue + Timer.vue — explainer + cronómetro fullscreen dark + photo capture + R11 single-attempt guards</name>
  <files>el-templo-app/src/modules/bar-challenge/pages/Explicacion.vue, el-templo-app/src/modules/bar-challenge/pages/Timer.vue</files>
  <read_first>
    - 115-UI-SPEC.md secciones "Component Inventory" filas Explicacion + Timer, "Color", "Typography" (display tokens)
    - 115-UI-SPEC.md "Copywriting Contract" entero (filas con copy verbatim)
    - 115-CONTEXT.md D-09 (Date.now() source of truth), Claude's Discretion (cleanup pattern + camera denied UI)
    - el-templo-app/src/modules/bar-challenge/stores/useBarChallengeStore.ts (de Plan 05 — usar todas las actions)
    - SPEC R11 (un solo intento — frontend debe deshabilitar el flujo del timer cuando el usuario ya intentó)
    - src/stores/useUserStore.ts (forma del profile: campo `barChallengeAttemptedAt: string | null`)
  </read_first>
  <action>
    **Explicacion.vue** (reemplazar stub):
    - `<q-page>` con padding-top safe-area (3xl = 64px).
    - Heading 28/600 Montserrat "Desafío de la Barra".
    - Body 14/400 Geologica: "Aguantá colgado de la barra por al menos 1:30. Si lo lográs, compartí tu foto etiquetando a @eltemplo y mostrásela al staff para llevarte tu premio. Tenés un solo intento."
    - Rules list (3 items, check icon `check` cream + texto 14/400):
      - "Un solo intento."
      - "El staff opera el cronómetro."
      - "Para reclamar el premio, mostrá la foto con la etiqueta @eltemplo al staff del local."
    - **R11 single-attempt CTA swap (NEW)**:
      ```ts
      import { computed } from 'vue'
      import { useRouter } from 'vue-router'
      import { useUserStore } from 'src/stores/useUserStore'

      const router = useRouter()
      const userStore = useUserStore()
      const alreadyAttempted = computed(() => userStore.profile?.barChallengeAttemptedAt != null)

      function onPrimaryCta() {
        if (alreadyAttempted.value) {
          void router.push('/desafio-barra/resultado')
        } else {
          void router.push('/desafio-barra/timer')
        }
      }
      ```
    - Single CTA full-width gold gradient #c4956a → #a07850, label dinámico (14/600 Montserrat cream, 10px radius, padding 12px 20px, 44px target):
      - Si `alreadyAttempted` → label "Ver mi intento", onClick → router.push('/desafio-barra/resultado').
      - Si NO → label "Comenzar", onClick → router.push('/desafio-barra/timer').
    - El cuerpo explicativo (heading + body + rules) se sigue mostrando en ambos casos (contexto útil aunque ya haya intentado).
    - Background cream-on-dark, layout vertical stack con `gap: 16px` (md spacing).

    **Timer.vue** (reemplazar stub):
    - `<q-page class="bar-challenge-timer">` con background `#1a1612` (deep ebony), fullscreen-dark (sin header, sin bottom-nav — verificar que el MainLayout no lo imponga; si lo hace, usar `meta: { hideLayout: true }` o un workaround similar — opcionalmente ver si Quasar permite `q-no-route-leave`).
      - **Critical:** investigar cómo otras pages "fullscreen" del proyecto se renderizan sin nav (ej. CheckInPage.vue del miembro app si tiene modo fullscreen, sino usar CSS para ocultar siblings con z-index alto temporal). Si requiere cambio de layout, NO hacerlo en este plan — usar CSS absoluto + `position: fixed; inset: 0; z-index: 9999` como solución contenida.
    - Layout:
      - Top: status label 14/600 uppercase letter-spacing 1px. Cream `#f0e6d6` cuando `secondsHeld < 90`, gold gradient `linear-gradient(135deg, #c4956a, #dcbe82)` con `background-clip: text` cuando `>= 90`. Texto: `AGUANTÁ` o `¡LO LOGRASTE! SEGUÍ AGUANTANDO`.
      - Center: digit cluster 120px Montserrat 800 tabular-nums, mismo gold-gradient cuando `>= 90`. Formato `MM:SS` con padStart(2,'0') en ambos.
      - Bottom: 2 CTAs row con `gap: 16px`:
        - "Tomar foto" (outlined gold, 14/600, padding 12px 20px, border 1.5px `#c4956a`, transparente fondo, cream text).
        - "Finalizar" (filled gold gradient, 14/600).
    - **R11 single-attempt guard (NEW — CRITICAL ORDER):**
      - En `onMounted`, ANTES de cualquier llamada a `store.start()`, `setInterval`, o `KeepAwake.keepAwake()`:
        ```ts
        if (userStore.profile?.barChallengeAttemptedAt != null) {
          await router.replace('/desafio-barra/resultado')
          return
        }
        ```
      - Esto evita que se resetee el estado del store (`store.start()` re-inicializa `startTimestamp`) y que se monten timers / KeepAwake en una sesión que va a redirigir.
    - Setup script:
      ```ts
      import { ref, onMounted, onUnmounted, computed } from 'vue'
      import { useRouter } from 'vue-router'
      import { useBarChallengeStore } from 'src/modules/bar-challenge/stores/useBarChallengeStore'
      import { useUserStore } from 'src/stores/useUserStore'
      import { KeepAwake } from '@capacitor-community/keep-awake'
      import { createLogger } from 'src/utils/logger'

      const router = useRouter()
      const store = useBarChallengeStore()
      const userStore = useUserStore()
      const logger = createLogger('bar-challenge-timer')
      const cameraError = ref<string | null>(null)
      let intervalId: number | undefined

      const formatted = computed(() => {
        const s = store.secondsHeld
        const mm = Math.floor(s / 60).toString().padStart(2, '0')
        const ss = (s % 60).toString().padStart(2, '0')
        return `${mm}:${ss}`
      })
      const isSuccess = computed(() => store.secondsHeld >= 90)

      onMounted(async () => {
        // R11 single-attempt guard — MUST run BEFORE store.start() / setInterval / KeepAwake.
        if (userStore.profile?.barChallengeAttemptedAt != null) {
          logger.info('R11 guard: already attempted, redirecting to /resultado', {
            attemptedAt: userStore.profile.barChallengeAttemptedAt,
          })
          await router.replace('/desafio-barra/resultado')
          return
        }

        store.start()
        intervalId = window.setInterval(() => store.tick(), 100)
        try {
          await KeepAwake.keepAwake()
        } catch (err: unknown) {
          logger.warn('keepAwake failed', { err: err instanceof Error ? err.message : String(err) })
        }
      })
      onUnmounted(async () => {
        if (intervalId !== undefined) window.clearInterval(intervalId)
        try {
          await KeepAwake.allowSleep()
        } catch { /* noop */ }
      })

      async function onTakePhoto() {
        cameraError.value = null
        try {
          const { Camera, CameraResultType, CameraSource } = await import('@capacitor/camera')
          const photo = await Camera.getPhoto({
            quality: 80,
            source: CameraSource.Camera,
            resultType: CameraResultType.Base64,
            saveToGallery: false,
          })
          if (photo.base64String) {
            store.setPhoto(photo.base64String)
          }
        } catch (err: unknown) {
          const msg = err instanceof Error ? err.message : String(err)
          logger.warn('camera failed', { err: msg })
          cameraError.value = 'Sin permiso de cámara. Podés seguir el desafío sin foto.'
        }
      }

      function onFinalize() {
        store.finalize()
        void router.push('/desafio-barra/resultado')
      }
      ```
    - Show `cameraError` as inline cream-text error message below the "Tomar foto" button when not null.
    - SCSS scoped: background `#1a1612`, text cream, tabular-nums in digit cluster (`font-feature-settings: 'tnum'; font-variant-numeric: tabular-nums`), transition 200ms ease on color change for the success state.

    Verificar `cd el-templo-app && pnpm exec vue-tsc --noEmit`.
    Nota: vue-tsc puede quejarse de `@capacitor/camera` no encontrado porque todavía no está instalado. **Mitigación:** usar `// @ts-expect-error - installed in Plan 08` ABOVE the dynamic import line, o configurar el tsconfig para tolerar el module faltante. NO instalar el plugin ahora (D-Capacitor approval gate, Plan 08).

  </action>
  <verify>
    <automated>cd el-templo-app && pnpm exec vue-tsc --noEmit 2>&1 | tee /tmp/tsc-timer.log; grep -qE "error TS(?!2307)" /tmp/tsc-timer.log && exit 1 || exit 0</automated>
  </verify>
  <done>
    - Explicacion + Timer compilan (TS2307 sobre @capacitor/camera tolerado vía @ts-expect-error).
    - Explicacion swap del CTA: "Comenzar" si no intentó, "Ver mi intento" si ya intentó.
    - Timer redirige a /resultado en onMounted si `barChallengeAttemptedAt != null`, ANTES de store.start().
    - Timer arranca store.start() en mount (sólo si no hay intento previo), llama store.tick() cada 100ms, libera KeepAwake en unmount.
    - Visual de éxito al pasar 90s (color + texto).
    - Cámara denegada muestra mensaje pero no detiene el flujo.
  </done>
  <acceptance_criteria>
    - `grep -c "Desafío de la Barra" el-templo-app/src/modules/bar-challenge/pages/Explicacion.vue` >= 1
    - `grep -c "Comenzar" el-templo-app/src/modules/bar-challenge/pages/Explicacion.vue` >= 1
    - `grep -c "Ver mi intento" el-templo-app/src/modules/bar-challenge/pages/Explicacion.vue` >= 1
    - `grep -c "barChallengeAttemptedAt" el-templo-app/src/modules/bar-challenge/pages/Explicacion.vue` >= 1
    - `grep -c "/desafio-barra/resultado" el-templo-app/src/modules/bar-challenge/pages/Explicacion.vue` >= 1
    - `grep -c "AGUANTÁ" el-templo-app/src/modules/bar-challenge/pages/Timer.vue` >= 1
    - `grep -c "¡LO LOGRASTE! SEGUÍ AGUANTANDO" el-templo-app/src/modules/bar-challenge/pages/Timer.vue` >= 1
    - `grep -c "@capacitor-community/keep-awake" el-templo-app/src/modules/bar-challenge/pages/Timer.vue` >= 1
    - `grep -c "store.tick()" el-templo-app/src/modules/bar-challenge/pages/Timer.vue` >= 1
    - `grep -c "setInterval" el-templo-app/src/modules/bar-challenge/pages/Timer.vue` >= 1
    - `grep -c "store.start()" el-templo-app/src/modules/bar-challenge/pages/Timer.vue` >= 1
    - `grep -c "store.finalize" el-templo-app/src/modules/bar-challenge/pages/Timer.vue` >= 1
    - `grep -c "Sin permiso de cámara" el-templo-app/src/modules/bar-challenge/pages/Timer.vue` >= 1
    - `grep -c "barChallengeAttemptedAt" el-templo-app/src/modules/bar-challenge/pages/Timer.vue` >= 1
    - `grep -c "router.replace" el-templo-app/src/modules/bar-challenge/pages/Timer.vue` >= 1
    - **R11 ordering check (Timer):** in `Timer.vue`, the line containing `barChallengeAttemptedAt` (the guard) must appear BEFORE the line containing `store.start()`. Verify: `awk '/barChallengeAttemptedAt/ {g=NR} /store\.start\(\)/ {s=NR} END {exit (g && s && g<s) ? 0 : 1}' el-templo-app/src/modules/bar-challenge/pages/Timer.vue`
    - **R11 behavior (Timer manual smoke):** Navigating to `/desafio-barra/timer` while `userStore.profile.barChallengeAttemptedAt` is non-null results in immediate redirect to `/desafio-barra/resultado` without the timer starting (`store.isRunning === false` after route resolution, `store.startTimestamp` unchanged).
    - **R11 behavior (Explicacion manual smoke):** When `userStore.profile.barChallengeAttemptedAt` is non-null, the Explicacion page shows a "Ver mi intento" CTA that routes to `/desafio-barra/resultado` (NOT "Comenzar" → `/desafio-barra/timer`).
    - vue-tsc no reporta errores que no sean TS2307 (module not found para @capacitor/camera, tolerado).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Resultado.vue — preview foto + compose + share + fallback + banner retry</name>
  <files>el-templo-app/src/modules/bar-challenge/pages/Resultado.vue</files>
  <read_first>
    - 115-UI-SPEC.md fila Resultado del Component Inventory + sección Color (gold gradient on completed, cream on not-completed; banner gold-tinted)
    - 115-CONTEXT.md D-10 (banner copy, optimistic UI), D-16/D-19 (composer)
    - el-templo-app/src/modules/bar-challenge/composables/useImageComposer.ts (de Plan 03)
  </read_first>
  <action>
    Reemplazar el stub de `Resultado.vue` con la implementación completa:

    Template structure (scoped class `.bar-challenge-resultado`):
    - `<q-page>` fullscreen dark `#1a1612`, padding-top safe-area.
    - Si `userStore.profile?.barChallengeAttemptedAt == null` y store.attemptResult == null: empty state — heading "El desafío terminó", body "Vení al próximo evento.", CTA "Volver" → router.push('/mi-templo'). Use cream colors.
    - Else: contenido normal.
      - Si retryPending (banner D-10): banner gold-tinted en top: background `rgba(220, 190, 130, 0.15)`, text `#dcbe82`, padding 12px 16px, radius 10px, text 14/400: "No se pudo guardar el intento, se está reintentando."
      - Foto preview (si `displayPhoto` no es null): img tag, max-width 240px, aspect-ratio 9/16, border-radius 16px, border 1.5px shimmer gold.
      - Headline: "Aguantaste <span class='resultado-num'>{N}</span> segundos". `.resultado-num` 64px Montserrat 800 tabular-nums; gold gradient (background-clip:text) si completed=true, cream `#f0e6d6` si false. Words around it 28/600.
      - Body 14/400 cream:
        - if completed: "Mostrá la foto etiquetando a @eltemplo al staff del local y llevate tu premio."
        - if !completed: "La barra te está esperando. Vení a entrenar."
      - CTA cluster:
        - si hay foto cacheada (`displayPhoto != null`): "Compartir foto" (gold gradient).
        - si NO hay foto: "Sacar foto ahora" (outlined gold, mismo flow que Timer's onTakePhoto, después de capturar dispara handleShare directamente).
      - CTA secundario "Volver" (text-button cream, navigates back to /mi-templo).

    Setup script logic:
    ```ts
    import { ref, computed, onMounted, onUnmounted } from 'vue'
    import { useRouter } from 'vue-router'
    import { useBarChallengeStore } from 'src/modules/bar-challenge/stores/useBarChallengeStore'
    import { useUserStore } from 'src/stores/useUserStore'
    import { useImageComposer } from 'src/modules/bar-challenge/composables/useImageComposer'
    import { useQuasar } from 'quasar'
    import { createLogger } from 'src/utils/logger'

    const router = useRouter()
    const store = useBarChallengeStore()
    const userStore = useUserStore()
    const { composeWithFrame } = useImageComposer()
    const $q = useQuasar()
    const logger = createLogger('bar-challenge-resultado')

    const retryPending = ref(false)
    const sharing = ref(false)

    // Resolve display data: prefer store (this session), fallback to user profile (cached)
    const displayPhoto = computed(() => store.photoBase64)
    const displaySeconds = computed(() => store.attemptResult?.seconds ?? userStore.profile?.barChallengeSeconds ?? 0)
    const displayCompleted = computed(() => store.attemptResult?.completed ?? userStore.profile?.barChallengeCompleted ?? false)
    const isEmpty = computed(() =>
      store.attemptResult == null && userStore.profile?.barChallengeAttemptedAt == null
    )

    onMounted(() => {
      // Check if sessionStorage queue has pending submits → show banner
      try {
        const raw = sessionStorage.getItem('bar-challenge-pending-submit')
        retryPending.value = !!raw && JSON.parse(raw).length > 0
      } catch { /* noop */ }
      // Drain queue in background
      void store.drainPendingSubmits().then(() => {
        try {
          const raw = sessionStorage.getItem('bar-challenge-pending-submit')
          retryPending.value = !!raw && JSON.parse(raw).length > 0
        } catch { /* noop */ }
      })
    })

    async function handleShare() {
      if (!displayPhoto.value) return
      sharing.value = true
      try {
        const blob = await composeWithFrame(displayPhoto.value, '/desafio-barra/marco-placeholder.png')
        // Convert blob → base64 data URL
        const dataUrl = await blobToDataUrl(blob)
        try {
          // @ts-expect-error - installed in Plan 08
          const { Share } = await import('@capacitor/share')
          // Capacitor Share file param requires URL/path; write to cache first OR pass base64 dataUrl if SO accepts
          await Share.share({
            title: 'Desafío de la Barra — El Templo',
            dialogTitle: 'Compartir tu desafío',
            url: dataUrl,
          })
        } catch (shareErr: unknown) {
          logger.warn('share failed → fallback to download', { err: shareErr instanceof Error ? shareErr.message : String(shareErr) })
          downloadBlob(blob, 'desafio-barra.jpg')
          $q.notify({ message: 'Tu dispositivo no permite compartir directamente. Descargamos la foto.', color: 'warning', timeout: 4000 })
        }
      } catch (err: unknown) {
        logger.error('share flow failed', { err: err instanceof Error ? err.message : String(err) })
      } finally {
        sharing.value = false
      }
    }

    async function handleTakePhotoAndShare() {
      try {
        // @ts-expect-error - installed in Plan 08
        const { Camera, CameraResultType, CameraSource } = await import('@capacitor/camera')
        const photo = await Camera.getPhoto({
          quality: 80,
          source: CameraSource.Camera,
          resultType: CameraResultType.Base64,
          saveToGallery: false,
        })
        if (photo.base64String) {
          store.setPhoto(photo.base64String)
          await handleShare()
        }
      } catch (err: unknown) {
        logger.warn('fallback camera failed', { err: err instanceof Error ? err.message : String(err) })
        $q.notify({ message: 'Sin permiso de cámara. Podés seguir el desafío sin foto.', color: 'warning', timeout: 4000 })
      }
    }

    function blobToDataUrl(blob: Blob): Promise<string> {
      return new Promise((resolve, reject) => {
        const reader = new FileReader()
        reader.onloadend = () => resolve(typeof reader.result === 'string' ? reader.result : '')
        reader.onerror = reject
        reader.readAsDataURL(blob)
      })
    }

    function downloadBlob(blob: Blob, filename: string) {
      const url = URL.createObjectURL(blob)
      const a = document.createElement('a')
      a.href = url
      a.download = filename
      document.body.appendChild(a)
      a.click()
      a.remove()
      URL.revokeObjectURL(url)
    }
    ```

    Verificar `cd el-templo-app && pnpm exec vue-tsc --noEmit 2>&1` — TS2307 sobre @capacitor/share + @capacitor/camera tolerado.

  </action>
  <verify>
    <automated>cd el-templo-app && pnpm exec vue-tsc --noEmit 2>&1 | tee /tmp/tsc-resultado.log; grep -qE "error TS(?!2307)" /tmp/tsc-resultado.log && exit 1 || exit 0</automated>
  </verify>
  <done>
    - Resultado.vue compila (solo TS2307 toleradas).
    - 3 ramas + empty state cubiertas.
    - Composer + share + fallback download + banner D-10 implementados.
  </done>
  <acceptance_criteria>
    - `grep -c "Aguantaste" el-templo-app/src/modules/bar-challenge/pages/Resultado.vue` >= 1
    - `grep -c "Mostrá la foto etiquetando a @eltemplo" el-templo-app/src/modules/bar-challenge/pages/Resultado.vue` >= 1
    - `grep -c "La barra te está esperando" el-templo-app/src/modules/bar-challenge/pages/Resultado.vue` >= 1
    - `grep -c "Sacar foto ahora" el-templo-app/src/modules/bar-challenge/pages/Resultado.vue` >= 1
    - `grep -c "Compartir foto" el-templo-app/src/modules/bar-challenge/pages/Resultado.vue` >= 1
    - `grep -c "composeWithFrame" el-templo-app/src/modules/bar-challenge/pages/Resultado.vue` >= 1
    - `grep -c "marco-placeholder.png" el-templo-app/src/modules/bar-challenge/pages/Resultado.vue` >= 1
    - `grep -c "No se pudo guardar el intento" el-templo-app/src/modules/bar-challenge/pages/Resultado.vue` >= 1
    - `grep -c "Tu dispositivo no permite compartir directamente" el-templo-app/src/modules/bar-challenge/pages/Resultado.vue` >= 1
    - `grep -c "El desafío terminó" el-templo-app/src/modules/bar-challenge/pages/Resultado.vue` >= 1
    - vue-tsc no reporta errores que no sean TS2307.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-16 | Information Disclosure | photo storage in memory | accept | D-08 in-memory only; no sessionStorage; lost on refresh; foto nunca sale del device sin user action explícita (share) |
| T-115-17 | DoS | setInterval(100ms) leak | mitigate | onUnmounted clearInterval; KeepAwake released; explicit cleanup |
| T-115-18 | Spoofing | dynamic import('@capacitor/camera') before install | accept | Plan 08 instala con user approval; meanwhile @ts-expect-error + runtime try/catch surface error gracefully |
| T-115-19 | Tampering | client-side `secondsHeld` reported via store.finalize() | mitigate | Server is authoritative; secondsHeld bounded 0-600 (Plan 04 schema); player honestly reports — staff supervises |
| T-115-20 | Tampering / Business Logic Bypass | deep-link to /desafio-barra/timer after attempt → second attempt UI | mitigate | R11 frontend guard in Timer.vue (router.replace BEFORE store.start) + Explicacion CTA swap; backend 409 remains authoritative |
</threat_model>

<verification>
- vue-tsc no reporta errores fuera de TS2307 sobre `@capacitor/camera`/`@capacitor/share` (TS2307 = module not found; aceptable porque Plan 08 instala los plugins).
- 3 pages renderizan en dev (manual smoke).
- Cámara denegada → mensaje cream visible, flow continúa.
- Banner retry visible cuando hay entries en sessionStorage queue.
- R11 guard: deep-link manual a `/desafio-barra/timer` con `barChallengeAttemptedAt` non-null redirige a `/resultado` sin iniciar el timer.
- R11 CTA swap: Explicacion muestra "Ver mi intento" en vez de "Comenzar" cuando ya hubo intento.
</verification>

<success_criteria>

- Todas las copies del UI-SPEC + SPEC R8 están literal-verbatim en el código.
- Timer math via Date.now() (no contador incrementado por interval).
- KeepAwake activado/liberado correctamente.
- Composer invocado con framePath correcto.
- Share + fallback download + camera permission denied + banner retry — todos implementados.
- Empty state cubre la ruta accedida sin attempt previo y fuera de ventana (defensa contra navegación directa post-evento).
- R11 single-attempt frontend guard implementado en Timer.vue (redirect early) y Explicacion.vue (CTA swap). Verificado por ordering check + grep + manual smoke.
  </success_criteria>

<output>
Crear `.planning/phases/115-evento-desafio-de-la-barra/115-06-SUMMARY.md` con: lista de copy literals que aparecen en cada page (verificable contra UI-SPEC), nota explícita sobre los `@ts-expect-error` agregados para los dynamic imports de Capacitor, descripción de la implementación del R11 guard (Timer redirect order + Explicacion CTA swap), y screenshot describir (visual smoke) de cada page si se pudo correr dev mode.
</output>
