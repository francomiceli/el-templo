---
phase: 115-evento-desafio-de-la-barra
plan: 07
type: execute
wave: 4
depends_on:
  - 115-02
  - 115-05
  - 115-06
files_modified:
  - el-templo-app/src/modules/progression/pages/MiTemplo.vue
autonomous: true
requirements:
  - R3
  - R4
  - D-05

must_haves:
  truths:
    - "MiTemplo.vue importa useBarChallengeWindow y BarChallengeCard"
    - "Computed local showPremiumCarousel = computed(() => showUpsellBadge.value || barChallengeWindow.isActive.value)"
    - "Cuando barChallengeWindow.isActive es true, el carrusel se renderiza (incluso para members sin showUpsellBadge), con BarChallengeCard como PRIMER slide (índice 0)"
    - "Fuera de ventana, comportamiento del carrusel idéntico al actual (regression): showUpsellBadge controla visibilidad, slides son [UpsellBadge, ProgramCtaCard] sin BarChallengeCard"
    - "El número de dots del carrusel refleja el número real de slides (2 vs 3) según ventana activa"
    - "D-05: Override del carrusel en MiTemplo.vue vía computed showPremiumCarousel; BarChallengeCard insertado como primer slide cuando window.isActive"
  artifacts:
    - path: "el-templo-app/src/modules/progression/pages/MiTemplo.vue"
      provides: "Carrusel premium con override de visibilidad + BarChallengeCard como primer slide condicional"
      contains: "useBarChallengeWindow"
  key_links:
    - from: "el-templo-app/src/modules/progression/pages/MiTemplo.vue"
      to: "el-templo-app/src/modules/bar-challenge/components/BarChallengeCard.vue"
      via: "import + render as first slide cuando window.isActive"
      pattern: "<BarChallengeCard"
    - from: "el-templo-app/src/modules/progression/pages/MiTemplo.vue"
      to: "el-templo-app/src/modules/bar-challenge/composables/useBarChallengeWindow.ts"
      via: "useBarChallengeWindow().isActive"
      pattern: "useBarChallengeWindow\\(\\)"
---

<objective>
Modificar `MiTemplo.vue` para que, durante la ventana del evento (D-04 / D-05), el carrusel premium se muestre a TODOS los usuarios autenticados (override del gate `showUpsellBadge`) y `BarChallengeCard` se inserte como PRIMER slide (índice 0). Fuera de ventana, comportamiento idéntico al actual (regression check no roto).

Purpose: Único punto de integración con la app existente. Mantiene la lógica del desafío aislada en su módulo; este plan solo agrega 2 imports + 1 computed + 1 slide condicional. Cambio quirúrgico, fácil de revertir post-evento (incluso reemplazando este plan por su revert si se necesita).
Output: MiTemplo.vue editado con tres cambios concretos (import, computed, template slide).
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-evento-desafio-de-la-barra/115-CONTEXT.md
@.planning/phases/115-02-SUMMARY.md
@.planning/phases/115-05-SUMMARY.md
@CLAUDE.md

@el-templo-app/src/modules/progression/pages/MiTemplo.vue

<interfaces>
<!-- Estado actual del carrusel + cambio mínimo requerido. -->

Current MiTemplo.vue carousel block (líneas 36-57):

```vue
<template v-else-if="showUpsellBadge">
  <div class="premium-carousel">
    <div class="premium-carousel__dots">
      <span
        class="premium-carousel__dot"
        :class="{ 'premium-carousel__dot--active': premiumSlide === 0 }"
      />
      <span
        class="premium-carousel__dot"
        :class="{ 'premium-carousel__dot--active': premiumSlide === 1 }"
      />
    </div>
    <div
      ref="premiumScroller"
      class="premium-carousel__scroller"
      @scroll="onPremiumScroll"
    >
      <div class="premium-carousel__slide"><UpsellBadge /></div>
      <div class="premium-carousel__slide">
        <ProgramCtaCard :segment="userStore.segment" />
      </div>
    </div>
  </div>
</template>
```

Target after this plan:

```vue
<template v-else-if="showPremiumCarousel">
  <div class="premium-carousel">
    <div class="premium-carousel__dots">
      <span
        v-if="barChallengeWindow.isActive.value"
        class="premium-carousel__dot"
        :class="{ 'premium-carousel__dot--active': premiumSlide === 0 }"
      />
      <span
        class="premium-carousel__dot"
        :class="{
          'premium-carousel__dot--active':
            premiumSlide === (barChallengeWindow.isActive.value ? 1 : 0),
        }"
      />
      <span
        class="premium-carousel__dot"
        :class="{
          'premium-carousel__dot--active':
            premiumSlide === (barChallengeWindow.isActive.value ? 2 : 1),
        }"
      />
    </div>
    <div
      ref="premiumScroller"
      class="premium-carousel__scroller"
      @scroll="onPremiumScroll"
    >
      <div
        v-if="barChallengeWindow.isActive.value"
        class="premium-carousel__slide"
      >
        <BarChallengeCard />
      </div>
      <div class="premium-carousel__slide"><UpsellBadge /></div>
      <div class="premium-carousel__slide">
        <ProgramCtaCard :segment="userStore.segment" />
      </div>
    </div>
  </div>
</template>
```

Computed addition in script setup:

```ts
import { useBarChallengeWindow } from "src/modules/bar-challenge/composables/useBarChallengeWindow";
import BarChallengeCard from "src/modules/bar-challenge/components/BarChallengeCard.vue";

const barChallengeWindow = useBarChallengeWindow();
const showPremiumCarousel = computed(
  () => showUpsellBadge.value || barChallengeWindow.isActive.value,
);
```

NOTE: when accessing `barChallengeWindow.isActive` inside template, in Vue 3 setup syntax, refs are auto-unwrapped, so it's `barChallengeWindow.isActive` not `.value`. Inside script, `.value` is required.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrar BarChallengeCard + window composable en MiTemplo.vue</name>
  <files>el-templo-app/src/modules/progression/pages/MiTemplo.vue</files>
  <read_first>
    - el-templo-app/src/modules/progression/pages/MiTemplo.vue ENTERO (todo el archivo — necesitas ver tanto el bloque template del carrusel como el script setup donde defines computed)
    - 115-CONTEXT.md D-05 (override exacto)
    - 115-SPEC.md R3 + R4 (in-window: visible a todos + BarChallengeCard como primer slide)
  </read_first>
  <action>
    1. **Imports (script setup):** Agregar dos imports en la sección de imports al inicio del `<script setup>`:
       ```ts
       import { useBarChallengeWindow } from 'src/modules/bar-challenge/composables/useBarChallengeWindow'
       import BarChallengeCard from 'src/modules/bar-challenge/components/BarChallengeCard.vue'
       ```
    2. **Computed:** Agregar después de las otras computeds del archivo (cerca de donde se define `showUpsellBadge`):
       ```ts
       const barChallengeWindow = useBarChallengeWindow()
       const showPremiumCarousel = computed(() => showUpsellBadge.value || barChallengeWindow.isActive.value)
       ```
    3. **Template:** Modificar el bloque del carrusel (líneas ~36-57):
       - Cambiar `v-else-if="showUpsellBadge"` por `v-else-if="showPremiumCarousel"`.
       - En el bloque `.premium-carousel__dots`, agregar un dot condicional al inicio:
         ```vue
         <span v-if="barChallengeWindow.isActive" class="premium-carousel__dot" :class="{ 'premium-carousel__dot--active': premiumSlide === 0 }" />
         ```
         Y ajustar los dots existentes para que reflejen los índices correctos según si la ventana está activa:
         ```vue
         <span class="premium-carousel__dot" :class="{ 'premium-carousel__dot--active': premiumSlide === (barChallengeWindow.isActive ? 1 : 0) }" />
         <span class="premium-carousel__dot" :class="{ 'premium-carousel__dot--active': premiumSlide === (barChallengeWindow.isActive ? 2 : 1) }" />
         ```
       - En `.premium-carousel__scroller`, agregar el primer slide condicional ANTES del slide de UpsellBadge:
         ```vue
         <div v-if="barChallengeWindow.isActive" class="premium-carousel__slide">
           <BarChallengeCard />
         </div>
         ```
       - Dejar UpsellBadge y ProgramCtaCard sin cambios.
    4. NO modificar `onPremiumScroll` ni la lógica de `premiumSlide` — la math de detectar el slide activo via scroll se basa en `scrollLeft / slideWidth`, lo cual sigue funcionando con 3 slides (solo más dots a renderizar). Verificar manualmente que la indexación sigue siendo correcta — si surge un bug de off-by-one, ajustar `onPremiumScroll` con un comentario explicando el ajuste.
    5. Verificar `cd el-templo-app && pnpm exec vue-tsc --noEmit`.
    6. Manual smoke local (opcional): correr `pnpm dev`, navegar a `/mi-templo`, agregar `?bar-challenge-force=1` a la URL. Confirmar que el carrusel aparece para cualquier usuario y que BarChallengeCard es la primera slide.
  </action>
  <verify>
    <automated>cd el-templo-app && pnpm exec vue-tsc --noEmit && grep -c "BarChallengeCard" src/modules/progression/pages/MiTemplo.vue | grep -qE '^[2-9]$'</automated>
  </verify>
  <done>
    - 2 imports agregados.
    - 2 computeds agregadas (barChallengeWindow, showPremiumCarousel).
    - Template del carrusel modificado para incluir slide condicional + dots ajustados.
    - Cambio quirúrgico: el diff vs HEAD debe estar contenido en 1 archivo, ~15-25 líneas.
  </done>
  <acceptance_criteria>
    - `grep -c "useBarChallengeWindow" el-templo-app/src/modules/progression/pages/MiTemplo.vue` >= 1
    - `grep -c "BarChallengeCard" el-templo-app/src/modules/progression/pages/MiTemplo.vue` >= 2 (1 import + ≥1 uso en template)
    - `grep -c "showPremiumCarousel" el-templo-app/src/modules/progression/pages/MiTemplo.vue` >= 2 (1 computed + 1 v-else-if)
    - `grep -c "barChallengeWindow.isActive" el-templo-app/src/modules/progression/pages/MiTemplo.vue` >= 3 (≥1 dot v-if + 1 slide v-if + ≥1 dot index ternario)
    - `grep -c "showUpsellBadge" el-templo-app/src/modules/progression/pages/MiTemplo.vue | head -1` — verifica que `showUpsellBadge` SIGUE existiendo (no fue eliminado, solo wrapeado) — debe ser >= 1.
    - `cd el-templo-app && pnpm exec vue-tsc --noEmit` exit code 0
    - Test manual smoke: usuario con plan presencial vinculado y reloj forzado a `?bar-challenge-force=1` ve el carrusel con BarChallengeCard primero. Documentar en SUMMARY.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-20 | Repudiation | regression del carrusel fuera de ventana | mitigate | showPremiumCarousel = showUpsellBadge OR window.isActive; fuera de ventana solo showUpsellBadge controla → mismo comportamiento existente |
| T-115-21 | Information Disclosure | card visible a member presencial | accept | El card es marketing visible a todos durante 48h — diseño explícito del SPEC R3 |
</threat_model>

<verification>
- type-check verde.
- Regression check: con el reloj fuera de ventana (sin force query) y un member con plan vinculado → no se renderiza el carrusel (mismo comportamiento que hoy).
- In-window check: con `?bar-challenge-force=1`, member presencial ve el carrusel con BarChallengeCard primero.
- Dots count = slides count (sin off-by-one).
</verification>

<success_criteria>

- Cambio quirúrgico contenido en MiTemplo.vue.
- Sin imports nuevos fuera del módulo bar-challenge.
- Sin tocar lógica de scroll / premiumSlide existente (a menos que surja bug obvio).
- Reversion trivial post-evento.
  </success_criteria>

<output>
Crear `.planning/phases/115-evento-desafio-de-la-barra/115-07-SUMMARY.md` con: diff exacto del bloque del carrusel (antes/después), confirmación del smoke test in-window vs out-of-window, y nota explícita de si se ajustó `onPremiumScroll` o no.
</output>
