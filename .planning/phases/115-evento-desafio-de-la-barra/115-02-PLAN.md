---
phase: 115-evento-desafio-de-la-barra
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-app/src/modules/bar-challenge/composables/useBarChallengeWindow.ts
  - el-templo-app/src/modules/bar-challenge/stores/useBarChallengeStore.ts
  - el-templo-app/src/modules/bar-challenge/pages/Explicacion.vue
  - el-templo-app/src/modules/bar-challenge/pages/Timer.vue
  - el-templo-app/src/modules/bar-challenge/pages/Resultado.vue
  - el-templo-app/src/router/routes.ts
  - el-templo-app/src/stores/useUserStore.ts
autonomous: true
requirements:
  - R3
  - R5
  - R6
  - R11
  - D-01
  - D-02
  - D-03
  - D-04
  - D-06
  - D-07

must_haves:
  truths:
    - "Existen 3 rutas children de MainLayout: /desafio-barra, /desafio-barra/timer, /desafio-barra/resultado, lazy-loaded"
    - "Composable useBarChallengeWindow exporta { start, end, isActive, isBeforeWindow, isAfterWindow } y respeta ?bar-challenge-force=1 query param"
    - "Pinia store useBarChallengeStore (setup) tiene shape completo definido (acciones stub, estado reactivo declarado) — implementación de submit en Plan 05"
    - "UserProfile en useUserStore agrega 3 fields nuevos (barChallengeCompleted, barChallengeSeconds, barChallengeAttemptedAt) para reflejar response del GET /me"
    - "Pages stub (Explicacion/Timer/Resultado) renderizan q-page vacío sin romper navegación — implementación en Plan 06"
    - "D-01: Módulo dedicado el-templo-app/src/modules/bar-challenge/ con subcarpetas pages/, components/, composables/, stores/"
    - "D-02: Ubicación de archivos según convención del módulo (pages/Explicacion.vue, Timer.vue, Resultado.vue; composables/; stores/)"
    - "D-03: 3 rutas registradas en src/router/routes.ts como children de MainLayout, lazy-loaded (/desafio-barra, /desafio-barra/timer, /desafio-barra/resultado)"
    - "D-04: Composable useBarChallengeWindow() expone { start, end, isActive, isBeforeWindow, isAfterWindow } con BAR_CHALLENGE_WINDOW hardcoded"
    - "D-06: useBarChallengeWindow lee route.query['bar-challenge-force'] y devuelve isActive=true si está presente (testing en staging)"
  artifacts:
    - path: "el-templo-app/src/modules/bar-challenge/composables/useBarChallengeWindow.ts"
      provides: "Date window composable + force-flag override"
      contains: "BAR_CHALLENGE_WINDOW"
    - path: "el-templo-app/src/modules/bar-challenge/stores/useBarChallengeStore.ts"
      provides: "Pinia setup store skeleton"
      contains: "useBarChallengeStore"
    - path: "el-templo-app/src/router/routes.ts"
      provides: "3 rutas del desafío"
      contains: "desafio-barra/timer"
    - path: "el-templo-app/src/stores/useUserStore.ts"
      provides: "UserProfile extendido con campos del desafío"
      contains: "barChallengeAttemptedAt"
  key_links:
    - from: "el-templo-app/src/router/routes.ts"
      to: "el-templo-app/src/modules/bar-challenge/pages/"
      via: "lazy import dynamic"
      pattern: "modules/bar-challenge/pages/Explicacion.vue"
---

<objective>
Scaffolding completo del módulo frontend del desafío: 3 rutas registradas en el router (children de MainLayout), 3 pages stub que renderizan, composable de ventana de fecha funcional con force-flag, Pinia store esqueleto, y extensión del `UserProfile` para los 3 fields nuevos del GET /me.

Purpose: Establecer el shape de archivos que Planes 04 (endpoint), 05 (card+store full) y 06 (pages full) consumen. Sin esto, las olas siguientes no pueden importar tipos ni navegar. Cero acoplamiento con backend (Plan 01) — corre en paralelo.
Output: Módulo `el-templo-app/src/modules/bar-challenge/` creado con estructura D-01/D-02; profile type extendido; navegación entre rutas funciona aunque las pages estén vacías.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-evento-desafio-de-la-barra/115-SPEC.md
@.planning/phases/115-evento-desafio-de-la-barra/115-CONTEXT.md
@.planning/phases/115-evento-desafio-de-la-barra/115-UI-SPEC.md
@CLAUDE.md

@el-templo-app/src/router/routes.ts
@el-templo-app/src/stores/useUserStore.ts
@el-templo-app/src/modules/training/stores/sessionPlayerStore.ts

<interfaces>
<!-- Patrones del proyecto que el ejecutor debe seguir literalmente. -->

Pinia setup store pattern (sessionPlayerStore precedent):

```ts
import { defineStore } from "pinia";
import { ref } from "vue";
import { createLogger } from "src/utils/logger";

const logger = createLogger("bar-challenge-store");

export const useBarChallengeStore = defineStore("barChallenge", () => {
  // state via ref()
  // actions as plain functions
  // return { ...state, ...actions }
});
```

Composable convention (project rule):

- Expose `cleanup()` if subscribing to external sources
- NO `onUnmounted` inside composables — callers own lifecycle

Router pattern (routes.ts:21-47):

```ts
{ path: 'desafio-barra', name: 'desafio-barra-explicacion', component: () => import('src/modules/bar-challenge/pages/Explicacion.vue') }
```

All 3 routes go INSIDE the `children: [...]` array of the `/` MainLayout entry.

UserProfile contract (useUserStore.ts:30-43):
After this plan, add 3 fields:

- barChallengeCompleted: boolean | null
- barChallengeSeconds: number | null
- barChallengeAttemptedAt: string | null (ISO string from MySQL TIMESTAMP)

Window constant (D-04 locked):

```ts
export const BAR_CHALLENGE_WINDOW = {
  start: "2026-05-23T15:00:00Z",
  end: "2026-05-25T15:00:00Z",
} as const;
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear módulo bar-challenge — composable de ventana + store skeleton + UserProfile extension</name>
  <files>el-templo-app/src/modules/bar-challenge/composables/useBarChallengeWindow.ts, el-templo-app/src/modules/bar-challenge/stores/useBarChallengeStore.ts, el-templo-app/src/stores/useUserStore.ts</files>
  <read_first>
    - el-templo-app/src/modules/training/stores/sessionPlayerStore.ts (header completo + un par de actions — patrón Pinia composition + logger import + ref shape)
    - el-templo-app/src/stores/useUserStore.ts líneas 1-60 (UserProfile interface entera)
    - 115-CONTEXT.md D-04, D-06, D-07, D-08 (window composable behavior + store shape exacto)
  </read_first>
  <action>
    1. Crear `el-templo-app/src/modules/bar-challenge/composables/useBarChallengeWindow.ts`:
       - Export `BAR_CHALLENGE_WINDOW = { start: '2026-05-23T15:00:00Z', end: '2026-05-25T15:00:00Z' } as const`
       - Export function `useBarChallengeWindow()` que:
         - Importa `useRoute` desde `vue-router` y `computed` desde `vue`
         - Lee `const route = useRoute()`
         - Crea `const now = computed(() => Date.now())` NO — para D-06 testing usar `Date.now()` directo dentro de cada computed (re-evaluado en cada llamada).
         - Mejor: usar un `ref<number>(Date.now())` reactivo actualizado en montaje del consumidor — pero per simplicidad y D-06, hacer los computed dependan de `route.query` para que reaccionen a `?bar-challenge-force=1`:
           ```ts
           const forced = computed(() => route.query['bar-challenge-force'] === '1')
           const isActive = computed(() => {
             if (forced.value) return true
             const t = Date.now()
             return t >= new Date(BAR_CHALLENGE_WINDOW.start).getTime() && t < new Date(BAR_CHALLENGE_WINDOW.end).getTime()
           })
           const isBeforeWindow = computed(() => Date.now() < new Date(BAR_CHALLENGE_WINDOW.start).getTime())
           const isAfterWindow = computed(() => Date.now() >= new Date(BAR_CHALLENGE_WINDOW.end).getTime())
           return { start: BAR_CHALLENGE_WINDOW.start, end: BAR_CHALLENGE_WINDOW.end, isActive, isBeforeWindow, isAfterWindow }
           ```
         - Tipo de retorno explícito (no `any`).
    2. Crear `el-templo-app/src/modules/bar-challenge/stores/useBarChallengeStore.ts`:
       - Setup store con `defineStore('barChallenge', () => { ... })`
       - State (refs):
         - `startTimestamp: Ref<number | null>` init null
         - `secondsHeld: Ref<number>` init 0
         - `isRunning: Ref<boolean>` init false
         - `photoBase64: Ref<string | null>` init null
         - `attemptResult: Ref<{ completed: boolean; seconds: number } | null>` init null
       - Actions (stubs vacíos que solo loggean — implementación real en Plan 05):
         - `start(): void` — `logger.debug('start stub')`
         - `tick(): void` — `logger.debug('tick stub')`
         - `setPhoto(base64: string): void` — `logger.debug('setPhoto stub')`
         - `finalize(): void` — `logger.debug('finalize stub')`
         - `submit(): Promise<void>` — `async; logger.debug('submit stub'); return Promise.resolve()`
         - `reset(): void` — `logger.debug('reset stub')`
       - Logger via `createLogger('bar-challenge-store')` from `src/utils/logger`.
       - Return: todas las refs + todas las acciones.
    3. Modificar `el-templo-app/src/stores/useUserStore.ts`:
       - Solo la `interface UserProfile` (líneas ~30-43). Agregar 3 fields al final, antes de la `}`:
         - `barChallengeCompleted: boolean | null`
         - `barChallengeSeconds: number | null`
         - `barChallengeAttemptedAt: string | null`
       - NO tocar el resto del store. Es change quirúrgico a la interface únicamente.
    4. `cd el-templo-app && pnpm type-check` debe pasar (vue-tsc).
  </action>
  <verify>
    <automated>cd el-templo-app && pnpm exec vue-tsc --noEmit</automated>
  </verify>
  <done>
    - Composable exporta los 5 valores documentados
    - Store skeleton existe con todas las acciones y state declarado
    - UserProfile incluye los 3 fields nuevos
    - Type-check verde
  </done>
  <acceptance_criteria>
    - `grep -c "BAR_CHALLENGE_WINDOW" el-templo-app/src/modules/bar-challenge/composables/useBarChallengeWindow.ts` >= 1
    - `grep -c "bar-challenge-force" el-templo-app/src/modules/bar-challenge/composables/useBarChallengeWindow.ts` >= 1
    - `grep -c "defineStore('barChallenge'" el-templo-app/src/modules/bar-challenge/stores/useBarChallengeStore.ts` == 1
    - `grep -c "barChallengeAttemptedAt: string | null" el-templo-app/src/stores/useUserStore.ts` == 1
    - `cd el-templo-app && pnpm exec vue-tsc --noEmit` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 3 pages stub + registro de rutas en router</name>
  <files>el-templo-app/src/modules/bar-challenge/pages/Explicacion.vue, el-templo-app/src/modules/bar-challenge/pages/Timer.vue, el-templo-app/src/modules/bar-challenge/pages/Resultado.vue, el-templo-app/src/router/routes.ts</files>
  <read_first>
    - el-templo-app/src/router/routes.ts entero (47 líneas)
    - 115-CONTEXT.md D-03 (path names + nesting)
  </read_first>
  <action>
    1. Crear `el-templo-app/src/modules/bar-challenge/pages/Explicacion.vue` con un stub mínimo válido:
       ```vue
       <template>
         <q-page class="bar-challenge-explicacion">
           <p>Explicación stub — implementación en Plan 06.</p>
         </q-page>
       </template>
       <script setup lang="ts">
       // Plan 06 implementa el contenido real (heading, body, rules list, CTA).
       </script>
       ```
    2. Crear `Timer.vue` y `Resultado.vue` con stubs análogos (mismo shape, distintos class names: `bar-challenge-timer`, `bar-challenge-resultado`).
    3. En `el-templo-app/src/router/routes.ts`, agregar 3 entries DENTRO del array `children` de la entrada `path: '/'` (MainLayout), después de `change-password`:
       ```ts
       {
         path: 'desafio-barra',
         name: 'desafio-barra-explicacion',
         component: () => import('src/modules/bar-challenge/pages/Explicacion.vue'),
       },
       {
         path: 'desafio-barra/timer',
         name: 'desafio-barra-timer',
         component: () => import('src/modules/bar-challenge/pages/Timer.vue'),
       },
       {
         path: 'desafio-barra/resultado',
         name: 'desafio-barra-resultado',
         component: () => import('src/modules/bar-challenge/pages/Resultado.vue'),
       },
       ```
    4. NO agregar guards de auth — `MainLayout` ya impone autenticación a todos sus children (regression check: las rutas existentes como `/reservas` no tienen meta requireAuth, el layout lo enforce).
    5. `cd el-templo-app && pnpm exec vue-tsc --noEmit` debe pasar.
    6. `cd el-templo-app && pnpm dev` (o build, lo que sea más rápido en CI local) — verificar manualmente o vía smoke test que navegar a `/desafio-barra` no rompe (solo render del stub).
  </action>
  <verify>
    <automated>cd el-templo-app && pnpm exec vue-tsc --noEmit && grep -c "desafio-barra/timer" src/router/routes.ts | grep -q '^1$'</automated>
  </verify>
  <done>
    - 3 archivos .vue creados, válidos, stub funcional
    - 3 entries de router registradas como children de MainLayout
    - type-check + lint verde
  </done>
  <acceptance_criteria>
    - `ls el-templo-app/src/modules/bar-challenge/pages/` lista exactamente: Explicacion.vue, Timer.vue, Resultado.vue
    - `grep -c "desafio-barra-explicacion" el-templo-app/src/router/routes.ts` == 1
    - `grep -c "desafio-barra-timer" el-templo-app/src/router/routes.ts` == 1
    - `grep -c "desafio-barra-resultado" el-templo-app/src/router/routes.ts` == 1
    - Las 3 rutas están DENTRO del bloque `children: [` del path `'/'` (no como rutas top-level) — verificar con `awk` o lectura manual
    - `cd el-templo-app && pnpm exec vue-tsc --noEmit` exit code 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-04 | Information Disclosure | ?bar-challenge-force=1 query param | accept | Solo fuerza visibilidad UI, no bypass de auth ni datos sensibles; cost: cero, beneficio: debugging post-evento |
| T-115-05 | Tampering | Frontend window const | accept | Hard-coded; cambiar fechas requiere rebuild + redeploy (acordado en SPEC constraints) |
</threat_model>

<verification>
- type-check vue-tsc verde en el-templo-app.
- 3 archivos .vue stub renderizan sin error en dev mode.
- `grep -c "desafio-barra" el-templo-app/src/router/routes.ts` == 3.
</verification>

<success_criteria>

- Módulo `el-templo-app/src/modules/bar-challenge/` creado con estructura D-01/D-02 completa.
- Rutas registradas, navegación funciona (los stubs renderizan).
- UserProfile extendido — type-check verde aunque GET /me todavía no devuelva los campos (son `boolean | null`, OK con `null`).
- Composable de ventana usable: `const { isActive } = useBarChallengeWindow()` retorna ref reactivo.
- Store skeleton listo para que Plan 05 implemente las acciones.
  </success_criteria>

<output>
Crear `.planning/phases/115-evento-desafio-de-la-barra/115-02-SUMMARY.md` con: lista de archivos creados, shape final del UserProfile (paste), interface del composable retornado, y nota explícita de qué queda como stub (acciones del store, contenido de pages).
</output>
