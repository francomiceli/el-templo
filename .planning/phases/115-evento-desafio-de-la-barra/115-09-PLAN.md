---
phase: 115-evento-desafio-de-la-barra
plan: 09
type: execute
wave: 5
depends_on:
  - 115-04
  - 115-07
  - 115-08
files_modified: []
autonomous: false
requirements:
  - R12

must_haves:
  truths:
    - "AAB firmado de Android (versionCode/versionName 1.5.0) está en Play Console internal testing"
    - "AAB promovido (o en proceso de promoción) a producción de Play Store ANTES del 23/05 12:00 ART"
    - "Smoke test en device físico Android desde Play Store interno: app abre, /mi-templo carga, ?bar-challenge-force=1 muestra el card, /desafio-barra navega, cámara abre"
    - "Backend staging tiene la migration 0124 aplicada antes del Android build production (staging-first STRICT)"
  artifacts:
    - path: ".planning/phases/115-evento-desafio-de-la-barra/115-09-SUMMARY.md"
      provides: "Track de subida + URLs + timestamps + screenshots smoke"
      contains: "internal testing"
  key_links:
    - from: "el-templo-app build artifact"
      to: "Google Play Console"
      via: "manual upload by user OR CI workflow (verificar Phase 75 SUMMARY si hay automation)"
      pattern: "AAB upload"
---

<objective>
Build firmado del AAB para Android (con la versión 1.5.0 de Plan 08), upload a Play Console internal testing, validación en device físico, y promoción a producción. Plan **autonomous: false** porque tanto el upload a Play Console como la promoción a production requieren acceso humano al dashboard.

Purpose: Asegurar que la feature está disponible en Play Store de producción antes del **23/05 12:00 ART** (hard deadline del evento). Play Store internal → production puede tardar ~24-48h.

**Crítico:** staging del backend debe tener la migration 0124 corrida ANTES de promover el AAB de producción, sino los devices con el build nuevo van a recibir error 500 desde /me hasta que el migration aplique en prod.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-evento-desafio-de-la-barra/115-SPEC.md
@.planning/phases/115-evento-desafio-de-la-barra/115-CONTEXT.md
@.planning/phases/115-08-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Estado actual del pipeline Android. -->

Phase 74 + 75 (STATE.md decisions) establecieron:

- ProGuard solo en production release
- Variant-scoped signing — solo productionRelease firma con el upload keystore
- Workflow dispatch para release de master (Phase 75)
- Keystore local file: `eltemplo-upload-key.keystore` (en project root, NO en repo — leer `eltemplo-upload-key.base64.txt` para ver si está disponible localmente)
- Phase 76 (STATE.md): Play Store setup en progreso; descriptions y privacy listos; AAB upload pendiente

**Pre-requisitos:**

1. Staging del API debe tener migration 0124 (Plan 01) aplicada ANTES de buildear production.
2. Versión 1.5.0 confirmada en build.gradle (Plan 08).
3. Plugins instalados + cap sync corrido (Plan 08).
4. master / staging branch debe estar limpia con todos los commits previos mergeados (o local branch del usuario contiene todo el trabajo).

**Operador humano hace:**

- Decide cuándo está listo el deploy a staging.
- Tiene acceso al keystore (file local).
- Tiene acceso a Play Console.
- Decide en qué momento promover a producción.

**Claude hace:**

- Verifica pre-requisitos.
- Corre el build local si es necesario.
- Verifica el AAB resultante.
- Documenta links y timestamps.
- NO sube nada al Play Console (esa acción es exclusivamente del operador).
  </interfaces>
  </context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: BLOCKING — Confirmar que staging API tiene migration 0124 aplicada</name>
  <what-built>Nada hasta que se confirme staging.</what-built>
  <how-to-verify>
    1. Operador SSH al staging EC2 (con aprobación explícita previa — memoria: "Always ask before SSHing into the server").
    2. Correr en staging API path: `mysql staging_db -e "SELECT name FROM _migrations WHERE name='0124_users_bar_challenge_fields.sql'"`.
       - Output esperado: 1 row con `0124_users_bar_challenge_fields.sql`.
    3. Correr: `mysql staging_db -e "DESCRIBE users" | grep bar_challenge`.
       - Output esperado: 3 columnas (completed/seconds/attempted_at).
    4. Smoke test endpoint en staging: `curl -X POST -H "Authorization: Bearer <staging-member-token>" -H "Content-Type: application/json" -d '{"secondsHeld": 90}' https://api-staging.eltemplo.org/api/bar-challenge/result` debe retornar 200.
    5. Verificar que `GET /api/auth/me` con un token de staging retorna las 3 keys nuevas (incluso null).
  </how-to-verify>
  <resume-signal>Operador responde "staging-verified" con timestamp + outputs de los comandos anteriores. Si falla algo, el operador hace gap-closure (ej. correr migration en staging) y vuelve a confirmar.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Build AAB Android signed (production variant)</name>
  <files></files>
  <read_first>
    - .planning/phases/74-android-* (si existe) o Phase 74/75 SUMMARYs para refrescar el comando exacto de build
    - el-templo-app/src-capacitor/android/app/build.gradle
    - el-templo-app/quasar.config.js (donde se setea capacitor)
  </read_first>
  <action>
    1. Verificar pre-requisitos:
       - `cd el-templo-app && pnpm exec vue-tsc --noEmit` (verde)
       - `cat package.json | jq -r .version` → "1.5.0"
       - `git status` → working tree limpio (o cambios committeados en branch local)
    2. Build web bundle:
       ```bash
       cd el-templo-app && pnpm build
       ```
       Confirmar `dist/spa/` existe con index.html.
    3. Capacitor sync:
       ```bash
       cd el-templo-app && pnpm exec cap sync android
       ```
    4. Build AAB signed (Phase 75 SUMMARY — usar el workflow dispatch O comando gradle directo según preferencia del operador):
       - **Opción A (comando local con env vars):**
         ```bash
         cd el-templo-app/src-capacitor/android
         export KEYSTORE_PATH=/home/franco/projects/el-templo/eltemplo-upload-key.keystore
         export KEYSTORE_PASSWORD=<from operator>
         export KEY_PASSWORD=<from operator>
         export VERSION_NAME=1.5.0
         export VERSION_CODE=<incremented value, e.g. 10500>
         ./gradlew bundleProductionRelease
         ```
       - **Opción B (CI workflow dispatch desde master):** operador corre el workflow en GitHub Actions.
    5. Verificar el AAB resultante:
       - `ls el-templo-app/src-capacitor/android/app/build/outputs/bundle/productionRelease/` debe contener `app-production-release.aab` (o nombre similar).
       - Verificar tamaño razonable (>5MB, <80MB).
       - Verificar signing: `keytool -printcert -jarfile <aab path>` debe mostrar el certificate del upload key.
    6. NO upload todavía — Task 3 (human) lo hace.
  </action>
  <verify>
    <automated>test -f el-templo-app/src-capacitor/android/app/build/outputs/bundle/productionRelease/*.aab 2>/dev/null && stat -c%s el-templo-app/src-capacitor/android/app/build/outputs/bundle/productionRelease/*.aab | awk '$1 > 5000000 && $1 < 80000000 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>
    - AAB firmado generado, en disco, tamaño razonable.
    - Versión 1.5.0 confirmada en el manifest del AAB (usar `aapt dump badging <aab>` para confirmar).
  </done>
  <acceptance_criteria>
    - Archivo AAB existe en outputs/bundle/productionRelease/
    - `aapt dump badging <aab> 2>/dev/null | grep versionName` muestra "1.5.0"
    - Tamaño entre 5MB y 80MB
    - `keytool -printcert -jarfile <aab>` muestra el upload key (no debug)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: BLOCKING — Operador sube AAB a Play Console internal testing + smoke test en device + promueve a producción</name>
  <what-built>AAB en disco listo para upload.</what-built>
  <how-to-verify>
    Operador hace estos pasos:
    1. **Upload a Internal Testing:**
       - Abrir Play Console → app El Templo → Testing → Internal testing.
       - Crear nuevo release.
       - Upload del AAB de `el-templo-app/src-capacitor/android/app/build/outputs/bundle/productionRelease/*.aab`.
       - Release notes: "Desafío de la Barra — evento 24/05".
       - Save + Review + Roll out to Internal testing.
    2. **Smoke test en device físico:**
       - Aceptar invitación al programa interno desde un device Android.
       - Instalar la app via Play Store (no sideload).
       - Login con un member real.
       - Navegar a `/mi-templo` → carrusel NO muestra BarChallengeCard (fuera de ventana).
       - Navegar a `/mi-templo?bar-challenge-force=1` → carrusel muestra BarChallengeCard como primer slide.
       - Tap → `/desafio-barra` → ver heading + rules + CTA Comenzar.
       - Tap "Comenzar" → `/desafio-barra/timer` → timer arranca, llega a 90s, color/texto cambian.
       - Tap "Tomar foto" → cámara abre (primer uso pide permiso); aceptar; volver al timer.
       - Tap "Finalizar" → `/desafio-barra/resultado` → ver foto + headline + body + CTA share.
       - Tap "Compartir foto" → share sheet aparece con la foto compuesta.
       - Sin errores en consola Sentry / Logcat.
    3. **Promote to Production:**
       - Si smoke OK: Play Console → Production → Create new release → Promote from Internal testing.
       - Release notes en español para el público: "Nueva: Desafío de la Barra — evento 24/05. Aguantá 1:30 colgado y ganá tu premio en el local."
       - Submit for review.
       - Esperar review (~24-48h Play Store).
    4. Documentar todos los timestamps + URLs del Play Console release.
  </how-to-verify>
  <resume-signal>Operador responde "android-promoted-to-prod" con: (a) URL del internal testing release, (b) URL del production release pending review, (c) timestamp del submit, (d) confirmación de smoke test verde, (e) screenshot opcional del console.</resume-signal>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-24 | Tampering | AAB signing | mitigate | Upload keystore es file local del operador; firma se valida con keytool antes de upload |
| T-115-25 | Repudiation | producción sin migration | mitigate | Task 1 BLOCKING confirma staging migration aplicada; producción debe correrla en mismo deploy window |
| T-115-26 | DoS | Play Store review delays | accept | Riesgo aceptado del cronograma; mitigar subiendo lo antes posible (memoria: hard deadline 23/05 12:00 ART) |
</threat_model>

<verification>
- AAB existe, firmado, versionName 1.5.0.
- Internal testing release creado.
- Smoke test en device físico verde (5+ pasos del flujo + Sentry sin errores).
- Production release submitted for review.
</verification>

<success_criteria>

- AAB en Play Store production (in review O released) ANTES del 23/05 12:00 ART.
- Staging API tiene migration 0124 confirmada antes del build production.
- Sin auto-deploys de Claude — todos los uploads/promotes los hace el operador.
  </success_criteria>

<output>
Crear `.planning/phases/115-evento-desafio-de-la-barra/115-09-SUMMARY.md` con:
- Versión final del AAB (versionName + versionCode)
- Output de `aapt dump badging` del AAB
- URL del internal testing release en Play Console
- URL del production release (in review)
- Timestamp del submit a producción
- Lista de pasos del smoke test que pasaron
- Cualquier issue encontrado durante smoke + cómo se resolvió
</output>
