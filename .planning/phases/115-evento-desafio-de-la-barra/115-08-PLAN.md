---
phase: 115-evento-desafio-de-la-barra
plan: 08
type: execute
wave: 4
depends_on:
  - 115-06
files_modified:
  - el-templo-app/package.json
  - el-templo-app/pnpm-lock.yaml
  - el-templo-app/src-capacitor/ios/App/App/Info.plist
  - el-templo-app/src-capacitor/android/app/build.gradle
  - el-templo-app/src-capacitor/package.json
autonomous: false
requirements:
  - R7
  - R10
  - R12
user_setup:
  - service: capacitor-camera
    why: "Captura de foto del desafío via Camera plugin (R7)"
    env_vars: []
    dashboard_config: []
    requires_approval: true
    requires_approval_reason: "Memoria del usuario: 'Never install OR update dependencies without asking' — sospecha activa por axios supply chain attack precedent"
  - service: capacitor-share
    why: "Share sheet nativo para compartir foto compuesta (R10)"
    env_vars: []
    dashboard_config: []
    requires_approval: true
    requires_approval_reason: "Misma regla — no instalar sin aprobación explícita"

must_haves:
  truths:
    - "@capacitor/camera y @capacitor/share instalados en el-templo-app/package.json con versiones lockeadas en pnpm-lock.yaml"
    - "Info.plist tiene NSCameraUsageDescription actualizado o adicional reflejando el uso de cámara para QR + desafío (single string sufficient)"
    - "Android: no se requiere cambio de manifest (Capacitor maneja permisos via @capacitor/camera automáticamente)"
    - "npx cap sync ejecutado, plugins iOS/Android registrados en native projects"
    - "Versión del app bumpeada de 1.4.3 → 1.5.0 (minor bump = feature, per project memory) en package.json, build.gradle versionName, Info.plist MARKETING_VERSION (xcconfig si aplica)"
    - "Android versionCode incrementado al menos +1 sobre el último build de producción"
    - "pnpm install corrió sin errores; vue-tsc verde sin TS2307 sobre los plugins ya instalados"
  artifacts:
    - path: "el-templo-app/package.json"
      provides: "Dependencies con los 2 plugins de Capacitor + version 1.5.0"
      contains: "@capacitor/camera"
    - path: "el-templo-app/src-capacitor/ios/App/App/Info.plist"
      provides: "NSCameraUsageDescription que cubre desafío de la barra"
      contains: "NSCameraUsageDescription"
    - path: "el-templo-app/src-capacitor/android/app/build.gradle"
      provides: "versionName 1.5.0 + versionCode incrementado"
      contains: "1.5.0"
  key_links:
    - from: "el-templo-app/package.json"
      to: "el-templo-app/src/modules/bar-challenge/pages/Timer.vue"
      via: "dynamic import('@capacitor/camera') resuelve a paquete real"
      pattern: "@capacitor/camera"
---

<objective>
Plan **BLOCKING** que requiere **aprobación explícita del usuario** antes de cualquier `pnpm install`. Una vez aprobado, instalar `@capacitor/camera` + `@capacitor/share`, correr `npx cap sync`, actualizar permisos iOS (Info.plist), y bumpear versión 1.4.3 → 1.5.0 (minor = feature) en los 3 archivos requeridos (package.json, build.gradle, Info.plist / xcconfig).

Purpose: Prepara el codebase para los builds nativos firmados (Plans 09 y 10). Sin este plan, los dynamic imports de Plan 06 fallan en runtime y las stores rechazan el upload por versionCode duplicado.

**¿Por qué autonomous: false?** Memoria del usuario es explícita: "Never install OR update dependencies without asking. No adding, bumping, or updating any package without explicit approval (axios supply chain attack precedent)." El executor DEBE pausar al inicio, mostrar los 2 paquetes con versiones target, esperar texto "approved" del usuario, y SOLO ENTONCES correr `pnpm install`.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-evento-desafio-de-la-barra/115-SPEC.md
@.planning/phases/115-evento-desafio-de-la-barra/115-CONTEXT.md
@CLAUDE.md

@el-templo-app/package.json
@el-templo-app/src-capacitor/ios/App/App/Info.plist
@el-templo-app/src-capacitor/android/app/build.gradle

<interfaces>
<!-- Estado actual y target -->

Current version: 1.4.3 (package.json line 3)
Current Android versionName: "1.4.3" (env-driven default in build.gradle)
Current iOS MARKETING_VERSION: driven by xcconfig — verify with `grep MARKETING_VERSION el-templo-app/src-capacitor/ios/App/App/*.xcconfig` if present.

Target version: **1.5.0** (minor bump per project memory: feature = minor)

Capacitor version constraint: `@capacitor/core ^8.0.1` (already installed). The new plugins MUST be compatible with Capacitor 8:

- `@capacitor/camera` → version ^8.x (latest)
- `@capacitor/share` → version ^8.x (latest)

Verify compatibility via npm registry view (no install yet):

```bash
npm view @capacitor/camera versions --json | tail -10
npm view @capacitor/share versions --json | tail -10
```

iOS permission string current:

```xml
<key>NSCameraUsageDescription</key>
<string>El Templo necesita acceso a la cámara para escanear el código QR de asistencia</string>
```

Target (update to cover desafío use case too):

```xml
<key>NSCameraUsageDescription</key>
<string>El Templo necesita acceso a la cámara para escanear el código QR de asistencia y para sacar la foto del Desafío de la Barra</string>
```

Android: Capacitor's @capacitor/camera adds CAMERA permission to AndroidManifest automatically during `cap sync`. No manual edit needed for the new plugins.

NSPhotoLibraryAddUsageDescription: only needed if `saveToGallery: true` in Camera.getPhoto OR Share writes to gallery. Plan 06 uses `saveToGallery: false`. Share uses URL-based share, not gallery write. → NO NEED.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: BLOCKING — Aprobación explícita del usuario para instalar 2 plugins de Capacitor</name>
  <what-built>Nada todavía. Esperando aprobación.</what-built>
  <decision>Aprobar instalación de:
    1. `@capacitor/camera` (última versión compatible con Capacitor 8 — verificar con `npm view @capacitor/camera versions --json | tail -5` antes de proponer)
    2. `@capacitor/share` (última versión compatible con Capacitor 8 — idem)
    Ambos como `dependencies` (no devDependencies) en `el-templo-app/package.json`.
  </decision>
  <context>
    Memoria del usuario (axios supply chain attack precedent): nunca instalar OR actualizar dependencias sin aprobación.
    Spec R7 y R10 requieren estos plugins. Sin ellos, el evento no se puede correr.
    Versiones target deben verificarse con `npm view` antes de mostrar.
    Si el usuario quiere ver el changelog / repo de los paquetes:
    - https://github.com/ionic-team/capacitor-plugins/tree/main/camera
    - https://github.com/ionic-team/capacitor-plugins/tree/main/share
  </context>
  <options>
    <option id="approve-latest">
      <name>Approve — instalar últimas versiones compatibles con Capacitor 8</name>
      <pros>Bug fixes recientes, soporte oficial activo</pros>
      <cons>Pequeño riesgo de regression nueva</cons>
    </option>
    <option id="approve-specific">
      <name>Approve con versión específica (pinned)</name>
      <pros>Determinismo total</pros>
      <cons>Hay que decidir cuál; tiempo extra</cons>
    </option>
    <option id="defer">
      <name>NO instalar — buscar alternativa</name>
      <pros>Cero riesgo de dep nueva</pros>
      <cons>BLOQUEA el evento del 24/05; alternativas (getUserMedia web + Web Share API) tienen UX inferior y compat parcial en iOS</cons>
    </option>
  </options>
  <resume-signal>Usuario responde: "approved" (con o sin versión específica) o "blocked"</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Instalar plugins + Info.plist + version bumps</name>
  <files>el-templo-app/package.json, el-templo-app/pnpm-lock.yaml, el-templo-app/src-capacitor/ios/App/App/Info.plist, el-templo-app/src-capacitor/android/app/build.gradle, el-templo-app/src-capacitor/package.json</files>
  <read_first>
    - el-templo-app/package.json (versión actual + capacitor entries existentes)
    - el-templo-app/src-capacitor/ios/App/App/Info.plist (cómo está estructurado)
    - el-templo-app/src-capacitor/android/app/build.gradle (líneas de versionCode/versionName)
    - el-templo-app/src-capacitor/package.json (¿también declara plugins? confirmar pattern existente)
  </read_first>
  <action>
    **Solo correr DESPUÉS del approved en Task 1.**

    1. **Install (workspace-aware):**
       ```bash
       cd el-templo-app && pnpm add @capacitor/camera@^8 @capacitor/share@^8
       ```
       Verificar que `package.json` ahora tiene ambas entries en `dependencies`.
       Verificar `pnpm-lock.yaml` se actualizó (no committear yet — Task 3 lo hace junto con cap sync).
       Si `el-templo-app/src-capacitor/package.json` también declara plugins (mirror), agregar las mismas entries ahí — verificar pattern con `@capacitor-community/keep-awake` o `@capacitor/haptics` que ya existen.

    2. **Capacitor sync:**
       ```bash
       cd el-templo-app && pnpm exec cap sync
       ```
       Si requiere Node >=22 (Phase 74 nota: nvm switch), correr `nvm use 22 && pnpm exec cap sync`.
       Verificar output: ambos plugins listados como "found" para iOS y Android.

    3. **iOS — Info.plist:**
       En `el-templo-app/src-capacitor/ios/App/App/Info.plist`, actualizar el contenido de `NSCameraUsageDescription` para cubrir ambos casos:
       ```xml
       <key>NSCameraUsageDescription</key>
       <string>El Templo necesita acceso a la cámara para escanear el código QR de asistencia y para sacar la foto del Desafío de la Barra</string>
       ```
       NO agregar `NSPhotoLibraryAddUsageDescription` ni `NSPhotoLibraryUsageDescription` (Plan 06 usa `saveToGallery:false`).

    4. **Version bump (3 lugares):**
       - `el-templo-app/package.json`: cambiar `"version": "1.4.3"` → `"version": "1.5.0"`.
       - `el-templo-app/src-capacitor/android/app/build.gradle`:
         - Buscar las líneas `versionCode = (System.getenv("VERSION_CODE") ?: "1").toInteger()` y `versionName = System.getenv("VERSION_NAME") ?: "1.4.3"`.
         - Cambiar el fallback `"1.4.3"` → `"1.5.0"`.
         - `versionCode` fallback: incrementar el "1" a un valor mayor; si build se hace via CI con `VERSION_CODE` env, el fallback solo afecta builds locales. **MEJOR**: leer `Phase 74 + 75 SUMMARY` (referenciados en STATE.md) o verificar si hay un counter persistido en repo. Si no es claro, usar la convención semver-like: 1_05_00 = 10500. Cambiar fallback a `"10500"`.
       - **iOS MARKETING_VERSION:** investigar dónde se setea (xcconfig, project.pbxproj, env var). Comando: `grep -rn "MARKETING_VERSION\\|1.4.3" el-templo-app/src-capacitor/ios/ 2>&1 | head -10`. Actualizar al valor "1.5.0".
       - **iOS CURRENT_PROJECT_VERSION (build number):** incrementar +1 sobre el valor actual. Comando: `grep -rn "CURRENT_PROJECT_VERSION" el-templo-app/src-capacitor/ios/ 2>&1 | head`. Si actualmente es "X", cambiar a "X+1".

    5. **Verificación post-cambios:**
       - `cd el-templo-app && pnpm exec vue-tsc --noEmit` — los TS2307 toleradas en Plan 06 deben desaparecer (paquetes ya existen).
       - `cd el-templo-app && pnpm test` para correr unit tests (smoke). Si algún test del store roto por algo, debuggear.
       - `pnpm exec cap doctor` debe reportar sin errores críticos.

    6. NO buildear todavía (Plans 09 y 10 hacen eso). NO push, NO commit (operador puede committear al final).

  </action>
  <verify>
    <automated>cd el-templo-app && pnpm exec vue-tsc --noEmit && grep -c "@capacitor/camera" package.json | grep -q '^1$' && grep -c "@capacitor/share" package.json | grep -q '^1$' && grep -q '"version": "1.5.0"' package.json</automated>
  </verify>
  <done>
    - 2 plugins en package.json + pnpm-lock.yaml.
    - cap sync ejecutado, plugins registrados en native projects.
    - NSCameraUsageDescription actualizado.
    - Versión 1.5.0 en los 3 archivos.
    - vue-tsc verde sin TS2307 sobre los 2 plugins.
  </done>
  <acceptance_criteria>
    - `cd el-templo-app && cat package.json | jq -r '.dependencies."@capacitor/camera"'` retorna un version string no-null.
    - `cd el-templo-app && cat package.json | jq -r '.dependencies."@capacitor/share"'` retorna un version string no-null.
    - `cd el-templo-app && cat package.json | jq -r '.version'` == "1.5.0"
    - `grep "Desafío de la Barra" el-templo-app/src-capacitor/ios/App/App/Info.plist` retorna >= 1 match.
    - `grep -c "1.5.0" el-templo-app/src-capacitor/android/app/build.gradle` >= 1
    - `cd el-templo-app && pnpm exec vue-tsc --noEmit` exit 0 y NO reporta TS2307 sobre @capacitor/camera ni @capacitor/share.
    - iOS MARKETING_VERSION o equivalente seteado a 1.5.0 (documentar el archivo exacto en SUMMARY).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-SC | Tampering | npm supply chain @capacitor/{camera,share} | mitigate | Plugins oficiales del equipo Ionic (capacitor-plugins repo); install gated por aprobación humana explícita (axios precedent); pnpm-lock.yaml committeado para reproducibilidad |
| T-115-22 | Elevation of Privilege | Camera API access | mitigate | NSCameraUsageDescription presente y descriptivo; usuario otorga permiso al SO; flow degrada graceful si denegado (Plan 06 Resultado fallback) |
| T-115-23 | Information Disclosure | photo via Share sheet | accept | Usuario elige explícitamente qué app receptora (Instagram, WhatsApp, etc.); fuera del control de la app |
</threat_model>

<verification>
- pnpm install corrió, lock actualizado.
- cap sync sin errores.
- vue-tsc verde (TS2307 anteriores desaparecen).
- Versión 1.5.0 en 3 lugares (package.json + Android + iOS).
- Info.plist permission string cubre el uso del desafío.
</verification>

<success_criteria>

- Aprobación humana obtenida ANTES del install (no fait accompli).
- Cero supply chain risk: paquetes oficiales del equipo Ionic, versiones explícitas en lock.
- Versión bumpeada correctamente en los 3 archivos (1.5.0 minor = feature).
- Listo para que Plans 09 (Android) y 10 (iOS) ejecuten los builds firmados.
  </success_criteria>

<output>
Crear `.planning/phases/115-evento-desafio-de-la-barra/115-08-SUMMARY.md` con:
- Versión exacta instalada de cada plugin
- Output completo de `pnpm exec cap sync`
- Diff del Info.plist
- 3 archivos donde se bumpeó la versión + valor final
- Output de `vue-tsc --noEmit` (confirmando que TS2307 anteriores se fueron)
- Confirmación explícita de la aprobación del usuario (timestamp + texto literal de approval)
</output>
