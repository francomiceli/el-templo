---
phase: 115-evento-desafio-de-la-barra
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/schema/users.ts
  - el-templo-api/src/db/migrations/0124_users_bar_challenge_fields.sql
  - el-templo-api/src/modules/auth/routes.ts
autonomous: true
requirements:
  - R1
  - R15-partial
  - D-15
  - D-17

must_haves:
  truths:
    - "DB users tiene 3 columnas nuevas nullable: bar_challenge_completed (boolean), bar_challenge_seconds (int), bar_challenge_attempted_at (timestamp)"
    - "Migration 0124 corre en local via pnpm db:migrate sin error y queda registrada en _migrations"
    - "GET /api/auth/me responde con los 3 campos nuevos (siempre, null si no participó)"
    - "Drizzle schema users.ts tiene los 3 campos exportados con tipos correctos"
    - "D-15: GET /me expone los 3 campos del desafío (barChallengeCompleted, barChallengeSeconds, barChallengeAttemptedAt) siempre, no solo durante la ventana"
  artifacts:
    - path: "el-templo-api/src/db/schema/users.ts"
      provides: "Drizzle schema con bar_challenge_* columns"
      contains: "barChallengeCompleted"
    - path: "el-templo-api/src/db/migrations/0124_users_bar_challenge_fields.sql"
      provides: "Migration SQL para 3 columnas nuevas"
      contains: "ADD COLUMN bar_challenge_completed"
    - path: "el-templo-api/src/modules/auth/routes.ts"
      provides: "GET /me con 3 campos nuevos en response"
      contains: "barChallengeAttemptedAt"
  key_links:
    - from: "el-templo-api/src/modules/auth/routes.ts"
      to: "el-templo-api/src/db/schema/users.ts"
      via: "import users + select campos en GET /me"
      pattern: "barChallengeCompleted: users\\.barChallengeCompleted"
---

<objective>
Backend foundation: agregar 3 columnas nullable a `users` (`bar_challenge_completed` BOOL NULL, `bar_challenge_seconds` INT NULL, `bar_challenge_attempted_at` TIMESTAMP NULL), correr migration localmente, y extender `GET /api/auth/me` para devolver los 3 campos siempre (null si no participó).

Purpose: Establecer el contrato de datos del intento del desafío. Todo lo que viene después (endpoint POST, frontend reading from userStore.profile, single-attempt enforcement) depende de estas columnas.
Output: schema extendido, migration 0124 aplicada en local + commit del SQL, /me devuelve los 3 campos.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/115-evento-desafio-de-la-barra/115-SPEC.md
@.planning/phases/115-evento-desafio-de-la-barra/115-CONTEXT.md
@CLAUDE.md

@el-templo-api/src/db/schema/users.ts
@el-templo-api/src/modules/auth/routes.ts
@el-templo-api/src/db/migrations/0121_users_lead_fields.sql

<interfaces>
<!-- Patrón existente para migraciones hand-written y para GET /me. -->

GET /me current response shape (auth/routes.ts:502-517):

```
{
  id, email, firstName, lastName, role, level,
  branchId, branchName, branchIsVirtual, branchCountry,
  gender, dateOfBirth, segment, onboardingCompleted
}
```

After this plan, add: barChallengeCompleted, barChallengeSeconds, barChallengeAttemptedAt.

Drizzle column patterns from users.ts:

- boolean nullable: `boolean("col_name")` (sin .notNull())
- int nullable: `int("col_name")`
- timestamp nullable: `timestamp("col_name")`

Migration runner contract (CLAUDE.md + 0121 precedent):

- File numbered NNNN_descr.sql, hand-written, no `;` inside SQL comments
- Runner: el-templo-api/src/db/run-migrations.ts, tracked en `_migrations` table
- Comando: `cd el-templo-api && pnpm db:migrate`
- DO NOT use `pnpm db:generate` (drizzle-kit journal desincronizado en 0059, DB en 0123)
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Drizzle schema + migration SQL hand-written + run local</name>
  <files>el-templo-api/src/db/schema/users.ts, el-templo-api/src/db/migrations/0124_users_bar_challenge_fields.sql</files>
  <read_first>
    - el-templo-api/src/db/schema/users.ts (entero — agregar campos al objeto users + comentario JSDoc estilo Phase 114 D-15)
    - el-templo-api/src/db/migrations/0121_users_lead_fields.sql (template completo: header con razón, regla de no-`;`-en-comentarios, ALTER TABLE en lineas separadas)
    - CLAUDE.md sección "Database Changes" (NEVER use drizzle-kit migrate)
  </read_first>
  <action>
    1. En `el-templo-api/src/db/schema/users.ts`, agregar 3 columnas al objeto `users` (dentro del `mysqlTable("users", { ... })`) DESPUÉS de `currentProgramEnrollmentId` y ANTES de `createdAt`:
       - `barChallengeCompleted: boolean("bar_challenge_completed")` (nullable, sin .notNull(), sin default)
       - `barChallengeSeconds: int("bar_challenge_seconds")` (nullable)
       - `barChallengeAttemptedAt: timestamp("bar_challenge_attempted_at")` (nullable)
       Agregar un comentario JSDoc arriba estilo `// Phase 115 (R1, D-15): bar challenge attempt fields. NULL en las 3 = no participó. Single-attempt enforced via UPDATE condicional en POST /api/bar-challenge/result.`
    2. Crear `el-templo-api/src/db/migrations/0124_users_bar_challenge_fields.sql` siguiendo el formato exacto del 0121:
       - Header de comentarios explicando: Phase 115 R1, 3 columnas nullable, idempotencia via `_migrations` row + el invariante de "no `;` dentro de comentarios" (referencia Phase 103-01)
       - 3 sentencias `ALTER TABLE users ADD COLUMN ...` separadas, cada una terminada en `;` en su propia linea no-comentario:
         ```
         ALTER TABLE users
           ADD COLUMN bar_challenge_completed BOOLEAN NULL DEFAULT NULL AFTER current_program_enrollment_id;
         ALTER TABLE users
           ADD COLUMN bar_challenge_seconds INT NULL DEFAULT NULL AFTER bar_challenge_completed;
         ALTER TABLE users
           ADD COLUMN bar_challenge_attempted_at TIMESTAMP NULL DEFAULT NULL AFTER bar_challenge_seconds;
         ```
       - NO usar `IF NOT EXISTS` (precedent: Phase 114, 111-02, 108).
    3. Ejecutar `cd el-templo-api && pnpm db:migrate` y verificar que reporta migration 0124 aplicada.
    4. Verificar con MySQL: `mysql eltemplo -e "DESCRIBE users" | grep bar_challenge` debe mostrar las 3 columnas con tipos correctos (`tinyint(1)`, `int`, `timestamp`) y nullables.
    5. Verificar `_migrations`: `mysql eltemplo -e "SELECT name FROM _migrations WHERE name LIKE '%bar_challenge%'"` devuelve 1 row.
    6. Correr `pnpm type-check` en `el-templo-api/` para confirmar que Drizzle types compilan.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm type-check && mysql eltemplo -e "SELECT name FROM _migrations WHERE name='0124_users_bar_challenge_fields.sql'" | grep -c '0124' | grep -q '^1$'</automated>
  </verify>
  <done>
    - Schema users.ts compila con los 3 campos nuevos
    - Archivo 0124_users_bar_challenge_fields.sql existe y está en disco (será committeado en Task 2)
    - Migration aplicada en local DB, registrada en _migrations
    - DESCRIBE users muestra las 3 columnas nuevas como nullable
  </done>
  <acceptance_criteria>
    - `mysql eltemplo -e "DESCRIBE users"` incluye 3 filas: `bar_challenge_completed tinyint(1) YES NULL`, `bar_challenge_seconds int YES NULL`, `bar_challenge_attempted_at timestamp YES NULL`
    - `mysql eltemplo -e "SELECT COUNT(*) FROM _migrations WHERE name='0124_users_bar_challenge_fields.sql'"` retorna 1
    - `grep -c "barChallengeCompleted" el-templo-api/src/db/schema/users.ts` >= 1
    - `cd el-templo-api && pnpm type-check` exit code 0
    - El archivo SQL NO contiene `;` dentro de líneas que empiezan con `--`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extender GET /me con los 3 campos nuevos</name>
  <files>el-templo-api/src/modules/auth/routes.ts</files>
  <read_first>
    - el-templo-api/src/modules/auth/routes.ts líneas 411-519 (handler GET /me entero)
    - 115-CONTEXT.md D-15 (siempre devolver los 3 campos, independiente de la ventana)
  </read_first>
  <action>
    1. En el handler `fastify.get("/me", ...)` de `auth/routes.ts`:
       - En el `.select({ ... })` del primer query a `users` (líneas ~423-434), agregar 3 campos:
         - `barChallengeCompleted: users.barChallengeCompleted`
         - `barChallengeSeconds: users.barChallengeSeconds`
         - `barChallengeAttemptedAt: users.barChallengeAttemptedAt`
       - En el objeto `return { ... }` (líneas ~502-517), agregar después de `onboardingCompleted`:
         - `barChallengeCompleted: user.barChallengeCompleted,`
         - `barChallengeSeconds: user.barChallengeSeconds,`
         - `barChallengeAttemptedAt: user.barChallengeAttemptedAt,`
    2. NO modificar nada del handler `POST /me/change-password` ni otros (cambio quirúrgico a GET /me únicamente).
    3. Confirmar `pnpm type-check` en `el-templo-api/`.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm type-check && grep -c "barChallengeAttemptedAt: user.barChallengeAttemptedAt" src/modules/auth/routes.ts | grep -q '^1$'</automated>
  </verify>
  <done>
    - GET /me devuelve los 3 campos del desafío en la response
    - type-check del API pasa sin errores
  </done>
  <acceptance_criteria>
    - `grep -c "barChallengeCompleted: user.barChallengeCompleted" el-templo-api/src/modules/auth/routes.ts` == 1
    - `grep -c "barChallengeSeconds: user.barChallengeSeconds" el-templo-api/src/modules/auth/routes.ts` == 1
    - `grep -c "barChallengeAttemptedAt: user.barChallengeAttemptedAt" el-templo-api/src/modules/auth/routes.ts` == 1
    - Manual smoke (opcional): `curl` con token member válido a `/api/auth/me` devuelve JSON que incluye keys `barChallengeCompleted: null`, `barChallengeSeconds: null`, `barChallengeAttemptedAt: null`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                                         |
| --------------------- | ------------------------------------------------------------------- |
| client → /api/auth/me | Authenticated member token; response exposes user profile data      |
| migration runner → DB | Local dev runs migration; staging+prod runs gated by human operator |

## STRIDE Threat Register

| Threat ID | Category               | Component        | Disposition | Mitigation Plan                                                                                            |
| --------- | ---------------------- | ---------------- | ----------- | ---------------------------------------------------------------------------------------------------------- |
| T-115-01  | Information Disclosure | GET /me response | accept      | 3 new fields are user-self data (own attempt); leak risk = none (own profile)                              |
| T-115-02  | Tampering              | Migration SQL    | mitigate    | Migration tracked in `_migrations`; SQL committed alongside schema; staging→prod gated by human checkpoint |
| T-115-03  | Repudiation            | Schema change    | accept      | Phase 115 is feature-additive, no data loss; rollback = drop 3 columns                                     |

</threat_model>

<verification>
- Migration applied locally: `mysql eltemplo -e "DESCRIBE users" | grep bar_challenge` shows 3 rows.
- Schema compiles: `cd el-templo-api && pnpm type-check`.
- /me response shape extended: `grep -c "barChallengeAttemptedAt: user" el-templo-api/src/modules/auth/routes.ts` == 1.
</verification>

<success_criteria>

- Migration 0124 corre limpia, queda en `_migrations`, las 3 columnas son nullable y default NULL.
- GET /me devuelve los 3 campos nuevos (todos `null` para usuarios sin participación).
- Schema TypeScript en `users.ts` tiene los 3 campos exportados (no `any`).
- Archivo SQL committeable (no `;` en comentarios, formato consistente con 0121).
  </success_criteria>

<output>
Crear `.planning/phases/115-evento-desafio-de-la-barra/115-01-SUMMARY.md` al terminar, listando: número exacto de migration aplicada, output de DESCRIBE users de las 3 columnas, response shape final de GET /me.
</output>
