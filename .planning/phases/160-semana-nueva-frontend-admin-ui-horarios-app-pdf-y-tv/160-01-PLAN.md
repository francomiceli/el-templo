---
phase: 160-semana-nueva-frontend-admin-ui-horarios-app-pdf-y-tv
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/shared/role-labels.ts
  - el-templo-api/src/modules/tv/roster.ts
  - el-templo-api/src/modules/tv/types.ts
  - el-templo-api/src/modules/tv/class-day.ts
  - el-templo-api/src/modules/admin/service.ts
  - el-templo-api/test/tv/tv-roster.test.ts
autonomous: true
requirements: [SEM-11, SEM-15]
must_haves:
  truths:
    - "Un día combos/técnica aprobado renderiza en la TV los 4 bloques (INITIUM/PYROS → COMBOS_I → COMBOS_II → STRETCHING, o TECNICA_I/II) en vez de caer a solo INITIUM"
    - "D160-02: la TV rotula COMBOS I / COMBOS II / TÉCNICA I / TÉCNICA II / STRETCHING (numerales romanos, espejando DEUTEROS I/II)"
    - "El badge corto del listado admin (routesSummary) usa I/II para COMBOS/TECNICA y Stretch para STRETCHING (resuelve el fallback charAt(0)→C/T de la 159)"
    - "SEM-11: existe UN diccionario rol→label en el API (shared/role-labels.ts) consumido por roster.ts y por el badge de admin/service.ts; las copias divergentes se unifican"
    - "SEM-15 es SOLO datos: el layout visual del kiosco (render.ts) NO se toca"
  artifacts:
    - path: "el-templo-api/src/modules/shared/role-labels.ts"
      provides: "ROLE_LABELS (full, TV) + ROLE_BADGE_LABELS (short, listado) centralizados para el API"
      contains: "COMBOS_I"
    - path: "el-templo-api/src/modules/tv/roster.ts"
      provides: "COMBOS_ROLES/TECNICA_ROLES + rama combos/tecnica en buildRoster; ROLE_LABELS importado del módulo compartido"
      contains: "COMBOS_ROLES"
    - path: "el-templo-api/test/tv/tv-roster.test.ts"
      provides: "cobertura del roster combos/tecnica (4 bloques, labels D160-02)"
      contains: "COMBOS_I"
  key_links:
    - from: "tv/class-day.ts resolveClassDay"
      to: "TvClassMode ampliado a combos/tecnica desde sessions.sessionMode"
      via: "mode derivado de la sesión aprobada real (misma query, sin N+1)"
      pattern: "combos|tecnica"
    - from: "tv/roster.ts buildRoster"
      to: "COMBOS_ROLES/TECNICA_ROLES según classDay.mode"
      via: "selección de roster por modo"
      pattern: "COMBOS_ROLES|TECNICA_ROLES"
---

<objective>
Enseñarle a la TV de sucursal a renderizar los días combos/técnica (SEM-15) y centralizar el diccionario de labels del API (SEM-11). Hoy un día combos/técnica aprobado caería en `REGULAR_ROLES` de `roster.ts`, no encontraría los roles COMBOS_*/TECNICA_*/STRETCHING y mostraría SOLO INITIUM — la clase desaparece de la pantalla. Este plan crea el roster de esos modos, amplía `TvClassMode` para que `class-day.ts` lo derive de la sesión real, y unifica las etiquetas del API en un único módulo (`shared/role-labels.ts`) consumido por el roster y por el badge del listado admin.

Purpose: SEM-15 es la razón dura por la que 159 no puede shippearse sola (rompería la TV apenas el profe genere un día combos/técnica). SEM-11 (api) elimina la divergencia entre `roster.ts:92` (ROLE_LABELS) y el badge `admin/service.ts:166-186`.

Output: módulo compartido + roster combos/tecnica + `TvClassMode` ampliado + badge D160-02; `tsc` verde y el test de roster verde. SIN migraciones (lectura pura), SIN rutas HTTP nuevas (ISO-01 intacto), SIN tocar el layout de `render.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

REGLA OPERATIVA: trabajás en el worktree `et-159` (branch `feat/159-semana-nueva-backend`, YA tiene el backend de 159 + TV/PDF de master). `git add` SIEMPRE por ruta explícita, nunca `git add -A`/`.`. Nunca `console.log` (usar el logger Pino). Sin `any`; `catch (err: unknown)` con `instanceof Error`. NO instalar ni actualizar dependencias. Typecheck local del API sí (`pnpm exec tsc --noEmit`). Tests SOLO en foreground con timeout amplio: PROHIBIDO `run_in_background` y PROHIBIDO loops de espera con `pgrep`. El MySQL de test es COMPARTIDO entre worktrees y Vitest 4 pisa `VITEST_POOL_ID` → el gate REAL de los tests de DB es CI; igual el ejecutor DEBE intentar correr el archivo en foreground (con `VITEST_POOL_ID=et159`) y declarar el resultado. Los tests de `tv-roster` son unitarios (sin DB) → sí corren local sin colisión.
</execution_context>

<context>
@.planning/phases/159-semana-nueva-backend-modos-de-d-a-generadores-roles-de-bloqu/159-06-SUMMARY.md
@.planning/phases/160-semana-nueva-frontend-admin-ui-horarios-app-pdf-y-tv/160-CONTEXT.md

<interfaces>
<!-- Contratos vigentes en et-159. Usar directamente, no re-explorar. -->

tv/types.ts:96 — TvClassMode (HOY binario):
export type TvClassMode = "regular" | "rom";
(consumido por roster.ts, class-day.ts, tv/service.ts, TvControlPage.vue y el kiosco.
El kiosco render.ts NO ramifica por mode='rom' — grep confirmó cero `=== "rom"` en render.ts;
renderiza de columns/labels/format del payload. Ampliar la unión NO cambia el layout.)

tv/roster.ts:28-45 — REGULAR_ROLES y ROM_ROLES (as const):
REGULAR_ROLES = [INITIUM, NUCLEUS, DEUTEROS_1, DEUTEROS_2, EPIKOS]
ROM_ROLES = [INITIUM, ROM_LOWER, ROM_CORE, ROM_UPPER]

tv/roster.ts:92-102 — ROLE_LABELS (Record<string,string>) HOY: INITIUM→PYROS, NUCLEUS,
DEUTEROS_1→"DEUTEROS I", DEUTEROS_2→"DEUTEROS II", EPIKOS, ATHLOS, ROM_LOWER→"TREN INFERIOR",
ROM_CORE→"ZONA MEDIA", ROM_UPPER→"TREN SUPERIOR". (INITIUM se rotula PYROS — decisión v8 UI-SPEC.)

tv/roster.ts:134-136 — visualGroupOf: solo colapsa DEUTEROS_1/2 → "DEUTEROS"; default devuelve
el rol tal cual. COMBOS_I/II y TECNICA_I/II NO deben colapsar → el default ya es correcto, NO tocar.

tv/roster.ts:227-248 — buildRoster: `const roles = classDay.mode === "rom" ? ROM_ROLES : REGULAR_ROLES;`
Luego por cada rol busca findInitiumBlock (INITIUM) o findCanonicalBlock; arma TvBlockSummary con blockTitle.

tv/roster.ts:210-215 — blockTitle: `label = ROLE_LABELS[block.role] ?? ROLE_LABELS[role] ?? role`; usa
formatNameWithParams (espejo del PDF — NO se toca en este plan).

tv/class-day.ts:135-177 — resolveClassDay: `fallbackMode` desde day_modes (rom|regular); tras cargar
sessionRows con sessionMode, hoy: `const mode: TvClassMode = sessionRows.some(s=>s.sessionMode==="rom") ? "rom" : "regular";`
(el select ya trae sessionMode — CERO queries nuevas). class-day.ts:284 — `order = mode === "rom" ? ROM_LEVEL_ORDER : REGULAR_LEVEL_ORDER;`

admin/service.ts:166-187 — routesBySession (badge del listado): ROM→ROM_DISPLAY; INITIUM→"I";
`let label = b.role.charAt(0)`; DEUTEROS_1→"DA", DEUTEROS_2→"DB". COMBOS_I hoy caería a "C", TECNICA_I a "T"
(bug que D160-02 manda resolver). El badge es `${label}: ${b.route} ${b.intensity}%`.

Roles nuevos (ya en el tipo BlockRole del API, plan 159-01): COMBOS_I, COMBOS_II, TECNICA_I, TECNICA_II, STRETCHING.
Modos nuevos (ya en sessionMode del API): "combos", "tecnica".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Módulo compartido de labels del API + badge D160-02 (SEM-11 api)</name>
  <read_first>
    - el-templo-api/src/modules/tv/roster.ts (ROLE_LABELS :92-102, blockTitle :210-215)
    - el-templo-api/src/modules/admin/service.ts (routesBySession :160-187)
    - .planning/phases/160-.../160-CONTEXT.md (D160-02, D160-03: un dict por app, mirror a propósito)
  </read_first>
  <action>
    Crear `el-templo-api/src/modules/shared/role-labels.ts` con DOS mapas exportados y un docblock que declare que es la ÚNICA fuente de labels del API (patrón "espejo a propósito" de D160-03; NO es un paquete cross-app):
    - `ROLE_LABELS: Record<string, string>` (etiqueta LARGA, para la TV/pantalla): INITIUM→"PYROS", NUCLEUS→"NUCLEUS", DEUTEROS_1→"DEUTEROS I", DEUTEROS_2→"DEUTEROS II", EPIKOS→"EPIKOS", ATHLOS→"ATHLOS", ROM_LOWER→"TREN INFERIOR", ROM_CORE→"ZONA MEDIA", ROM_UPPER→"TREN SUPERIOR", COMBOS_I→"COMBOS I", COMBOS_II→"COMBOS II", TECNICA_I→"TÉCNICA I", TECNICA_II→"TÉCNICA II", STRETCHING→"STRETCHING". (Numerales romanos para combos/técnica espejando DEUTEROS I/II — D160-02.)
    - `ROLE_BADGE_LABELS: Record<string, string>` (etiqueta CORTA, para el routesSummary del listado admin): INITIUM→"I", NUCLEUS→"N", DEUTEROS_1→"DA", DEUTEROS_2→"DB", EPIKOS→"E", ATHLOS→"A", COMBOS_I→"I", COMBOS_II→"II", TECNICA_I→"I", TECNICA_II→"II", STRETCHING→"Stretch". (ROM se sigue rotulando con ROM_DISPLAY inline en admin/service.ts — son nombres de zona, no badge de rol: dejar ROM_DISPLAY donde está.)
    Refactorizar `tv/roster.ts`: eliminar el `ROLE_LABELS` local (:92-102) e importar `ROLE_LABELS` del módulo nuevo; `blockTitle` sigue igual (usa la constante importada). Actualizar el docblock del archivo para apuntar al módulo compartido como fuente.
    Refactorizar `admin/service.ts` (:166-187): reemplazar `let label = b.role.charAt(0); if (DEUTEROS_1)... else if (DEUTEROS_2)...` por `const label = ROLE_BADGE_LABELS[b.role] ?? b.role.charAt(0);` (el fallback charAt(0) queda solo como red para roles futuros; los conocidos salen del mapa). El INITIUM que hoy retorna "I" temprano puede seguir con el early-return o unificarse al mapa — mantené el comportamiento observable idéntico para ROM/INITIUM/DEUTEROS y agregá I/II/Stretch para los nuevos. NO tocar ROM_DISPLAY ni la forma `${label}: ${route} ${intensity}%`.
    Nunca `console.log`. Sin `any`.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `el-templo-api/src/modules/shared/role-labels.ts` existe y exporta `ROLE_LABELS` y `ROLE_BADGE_LABELS`.
    - `grep -c "COMBOS_I\|COMBOS_II\|TECNICA_I\|TECNICA_II\|STRETCHING" el-templo-api/src/modules/shared/role-labels.ts` >= 5 (roles nuevos en ambos mapas).
    - `roster.ts` importa `ROLE_LABELS` del módulo compartido y NO define un `ROLE_LABELS` local (`grep -c "^const ROLE_LABELS\|^  ROLE_LABELS: Record" el-templo-api/src/modules/tv/roster.ts` == 0).
    - `admin/service.ts` usa `ROLE_BADGE_LABELS` y ya NO tiene el `label = b.role.charAt(0)` como camino principal para DEUTEROS/COMBOS/TECNICA (`grep -n "ROLE_BADGE_LABELS" el-templo-api/src/modules/admin/service.ts` >= 1).
    - `pnpm exec tsc --noEmit` en 0 errores.
  </acceptance_criteria>
  <done>Existe un único diccionario rol→label del API; roster y el badge admin lo consumen; el badge rotula I/II/Stretch para combos/técnica/stretching; tsc verde.</done>
</task>

<task type="auto">
  <name>Task 2: Roster combos/técnica en la TV + TvClassMode ampliado (SEM-15)</name>
  <read_first>
    - el-templo-api/src/modules/tv/roster.ts (REGULAR_ROLES/ROM_ROLES :28-45, buildRoster :227-248, visualGroupOf :134-136)
    - el-templo-api/src/modules/tv/types.ts (TvClassMode :96)
    - el-templo-api/src/modules/tv/class-day.ts (resolveClassDay :135-177 mode; order :284)
    - el-templo-api/test/tv/tv-roster.test.ts (molde de test unitario del roster)
    - el-templo-admin/src/tv/render.ts (SOLO para confirmar por grep que NO ramifica por mode='rom' — NO tocar)
  </read_first>
  <behavior>
    - Test 1: un `RosterClassDay` con `mode: "combos"` y sesiones con roles INITIUM/COMBOS_I/COMBOS_II/STRETCHING produce un roster de 4 entradas en orden [INITIUM, COMBOS_I, COMBOS_II, STRETCHING].
    - Test 2: los títulos usan D160-02: `blockTitle("COMBOS_I", ...)` empieza con "COMBOS I", COMBOS_II con "COMBOS II", STRETCHING con "STRETCHING".
    - Test 3: un `mode: "tecnica"` produce [INITIUM, TECNICA_I, TECNICA_II, STRETCHING] con labels "TÉCNICA I"/"TÉCNICA II".
    - Test 4: `visualGroupOf("COMBOS_I") === "COMBOS_I"` y `visualGroupOf("COMBOS_II") === "COMBOS_II"` (NO colapsan, a diferencia de DEUTEROS).
    - Test 5 (regresión): un `mode: "regular"` sigue produciendo REGULAR_ROLES y `mode: "rom"` ROM_ROLES sin cambios.
  </behavior>
  <action>
    En `tv/types.ts`: ampliar `TvClassMode` a `"regular" | "rom" | "combos" | "tecnica"`. Actualizar el docblock: `rom` sigue siendo el caso especial de 2 tiers; combos/tecnica se comportan como `regular` (6 niveles, mismo orden) y solo cambian el roster de roles.
    En `tv/roster.ts`: agregar `export const COMBOS_ROLES = ["INITIUM","COMBOS_I","COMBOS_II","STRETCHING"] as const;` y `export const TECNICA_ROLES = ["INITIUM","TECNICA_I","TECNICA_II","STRETCHING"] as const;` (docblock: orden canónico del día combos/técnica; STRETCHING es la lista compartida del cierre). En `buildRoster` cambiar la selección de roles a un switch por modo: `rom`→ROM_ROLES, `combos`→COMBOS_ROLES, `tecnica`→TECNICA_ROLES, default→REGULAR_ROLES. El resto de buildRoster (findInitiumBlock/findCanonicalBlock/blockTitle/shared) NO cambia — STRETCHING no es shared (cada nivel puede diferir en teoría; aunque el generador lo hace idéntico, el roster no asume shared salvo INITIUM). NO tocar `visualGroupOf` (el default ya deja COMBOS/TECNICA sin colapsar).
    En `tv/class-day.ts` (:175-177): derivar el modo de la sesión aprobada real para los 4 valores. Reemplazar el `mode = some(rom) ? "rom" : "regular"` por: si alguna sesión tiene sessionMode==="rom" → "rom"; else si alguna tiene "combos" → "combos"; else si alguna tiene "tecnica" → "tecnica"; else "regular". El `order` (:284) debe usar `ROM_LEVEL_ORDER` SOLO para "rom" y `REGULAR_LEVEL_ORDER` para regular/combos/tecnica (combos/técnica tienen los 6 niveles). Ajustar el comentario Pitfall 6.
    NO tocar `el-templo-admin/src/tv/render.ts` ni ningún archivo de layout del kiosco (SEM-15 = solo datos). NO agregar queries nuevas en class-day (el select ya trae sessionMode). Nunca `console.log`. Sin `any`.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit && VITEST_POOL_ID=et159 pnpm vitest run test/tv/tv-roster.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "COMBOS_ROLES\|TECNICA_ROLES" el-templo-api/src/modules/tv/roster.ts` >= 2 y `buildRoster` ramifica por combos/tecnica.
    - `tv/types.ts` TvClassMode contiene `"combos"` y `"tecnica"`.
    - `class-day.ts` deriva mode combos/tecnica de sessionMode (`grep -c "combos\|tecnica" el-templo-api/src/modules/tv/class-day.ts` >= 2) y el order de niveles NO usa ROM_LEVEL_ORDER para combos/tecnica.
    - `git diff --stat -- el-templo-admin/src/tv/render.ts` vacío (render.ts intacto).
    - `test/tv/tv-roster.test.ts` cubre los 5 comportamientos y pasa en foreground.
    - tsc verde.
  </acceptance_criteria>
  <done>La TV arma el roster de 4 bloques para combos/técnica con las labels D160-02; class-day deriva el modo de la sesión real; regular/rom sin regresión; render.ts intacto; tsc + test verdes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| -------- | ----------- |
| API (server) → TV kiosco (pared pública) | El roster y las labels los fija el server; el kiosco solo pinta. Ningún dato de socio cruza (T-164-08 heredado) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --------- | -------- | --------- | ----------- | --------------- |
| T-160-01 | Tampering | Rol/label inyectado en el roster | accept | Roles y labels se resuelven server-side desde la sesión aprobada; el kiosco no los controla |
| T-160-02 | Information Disclosure | Datos de socio en el payload TV | accept | Sin cambios al shape del payload; solo se amplía la unión TvClassMode y el roster de roles |
| T-160-03 | Elevation of Privilege | Rutas HTTP nuevas | accept | Este plan NO agrega rutas (ISO-01 intacto); solo lógica de roster/labels |
| T-160-SC | Tampering | npm/pip installs | accept | Esta fase NO instala ni actualiza dependencias |

</threat_model>

<verification>
- `cd el-templo-api && pnpm exec tsc --noEmit` verde.
- `VITEST_POOL_ID=et159 pnpm vitest run test/tv/tv-roster.test.ts` verde en foreground (advertencia: MySQL de test compartido; este test es unitario sin DB, no colisiona).
- Regresión: `pnpm vitest run test/tv/tv-class-day.test.ts` sigue verde (mode rom/regular intacto).
- `render.ts` sin diff (SEM-15 = solo datos).
- Sin rutas HTTP nuevas; sin migraciones.
</verification>

<success_criteria>
La TV renderiza los días combos/técnica con sus 4 bloques y las labels D160-02; el diccionario de labels del API está centralizado y consumido por roster y badge; el badge del listado usa I/II/Stretch; regular/rom sin regresión; tsc y test de roster verdes.
</success_criteria>

<output>
Create `.planning/phases/160-semana-nueva-frontend-admin-ui-horarios-app-pdf-y-tv/160-01-SUMMARY.md` when done. Documentar la ruta del módulo compartido (`shared/role-labels.ts`) y sus exports, que el resto de la fase referencia como precedente del patrón "un dict por app".
</output>
</content>
</invoke>
