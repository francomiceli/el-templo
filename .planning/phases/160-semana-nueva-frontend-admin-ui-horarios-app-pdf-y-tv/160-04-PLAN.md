---
phase: 160-semana-nueva-frontend-admin-ui-horarios-app-pdf-y-tv
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-app/src/modules/training/types/session.ts
  - el-templo-app/src/modules/training/utils/blockColors.ts
  - el-templo-app/src/constants/roleLabels.ts
  - el-templo-app/src/modules/training/components/BlockCard.vue
autonomous: true
requirements: [SEM-10, SEM-11]
must_haves:
  truths:
    - "El tipo BlockRole del app incluye COMBOS_I, COMBOS_II, TECNICA_I, TECNICA_II, STRETCHING"
    - "Session.sessionMode del app incluye 'combos' y 'tecnica'"
    - "Los 4 Record<BlockRole,...> exhaustivos del app (BlockCard.formatRole + 3 mapas de blockColors) cubren los 5 roles nuevos con colores de marca — el typecheck lo fuerza"
    - "SEM-11: existe UN diccionario rol→label en el app (constants/roleLabels.ts) que BlockCard consume; sincroniza las divergencias existentes"
  artifacts:
    - path: "el-templo-app/src/modules/training/types/session.ts"
      provides: "BlockRole += 5 roles; Session.sessionMode += 2 modos"
      contains: "COMBOS_I"
    - path: "el-templo-app/src/modules/training/utils/blockColors.ts"
      provides: "3 mapas de color exhaustivos con COMBOS/TECNICA/STRETCHING (paleta de marca)"
      contains: "STRETCHING"
    - path: "el-templo-app/src/constants/roleLabels.ts"
      provides: "ROLE_LABELS del app (fuente única) — consumido por BlockCard y por 160-05"
      contains: "COMBOS_I"
  key_links:
    - from: "types/session.ts BlockRole"
      to: "blockColors.ts (3 Record<BlockRole>) + BlockCard.formatRole"
      via: "Record exhaustivo (el typecheck fuerza los 5 roles)"
      pattern: "Record<BlockRole"
---

<objective>
Extender los contratos de tipo del member app (BlockRole +5, sessionMode +2) y centralizar el diccionario de labels del app (SEM-11), completando además los mapas de color exhaustivos (SEM-10). Al ampliar `BlockRole`, TypeScript fuerza a completar los 4 `Record<BlockRole,...>` exhaustivos (BlockCard.formatRole + 3 mapas de `blockColors.ts`), garantizando que no queden huecos silenciosos cuando lleguen sesiones combos/técnica.

Purpose: es la base de tipos+labels+colores que consume el resto del render del app (plan 160-05). Sin ella, un bloque COMBOS/TECNICA/STRETCHING caería al `|| 'block-bg--default'` / `|| role` sin color ni label propios.

Output: tipos ampliados + 4 mapas exhaustivos completos + dict de labels; `vue-tsc` verde. SIN migraciones.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

REGLA OPERATIVA: worktree `et-159`. `git add` por ruta explícita. Nunca `console.log` (usar el logger del app). Sin `any`. NO instalar dependencias. **CI NO typechequea el app** → el gate de tipos es `pnpm exec vue-tsc --noEmit` corrido LOCAL en foreground (obligatorio). Tests del app en foreground con timeout amplio, sin `run_in_background`/`pgrep`.
</execution_context>

<context>
@.planning/phases/160-semana-nueva-frontend-admin-ui-horarios-app-pdf-y-tv/160-CONTEXT.md

<interfaces>
<!-- Contratos vigentes en et-159. Usar directamente. -->

training/types/session.ts:12-21 — BlockRole (9 valores hoy):
'INITIUM'|'NUCLEUS'|'DEUTEROS_1'|'DEUTEROS_2'|'ATHLOS'|'EPIKOS'|'ROM_LOWER'|'ROM_CORE'|'ROM_UPPER'
training/types/session.ts:140 — Session.sessionMode?: 'regular' | 'rom'

training/utils/blockColors.ts — 3 Record<BlockRole,string> EXHAUSTIVOS (fallan tsc si faltan claves):
 :25 getBlockColorClass → opacityMap (clases 'block-bg--*'); usa 'block-bg--default' de fallback
 :50 getBlockAccentColor → colorMap ('primary'|'secondary')
 :73 getBlockCSSColor → colorMap (BRAND_TERRACOTTA|BRAND_AGED_GOLD)
 (además getBlockHeaderGradient usa `role === 'NUCLEUS' || role === 'DEUTEROS_1'` — NO exhaustivo, no rompe)
 Paleta de marca (reference_brand_palette): BRAND_TERRACOTTA / BRAND_AGED_GOLD desde src/utils/brandColors. SIN azul/púrpura fuera de marca.

training/components/BlockCard.vue:77-90 — formatRole: `roleNames: Record<BlockRole,string>` EXHAUSTIVO:
 INITIUM:'Initium', NUCLEUS:'Nucleus', DEUTEROS_1:'Deuteros 1', DEUTEROS_2:'Deuteros 2', ATHLOS:'Athlos',
 EPIKOS:'Epikos', ROM_LOWER:'Tren Inferior', ROM_CORE:'Zona Media', ROM_UPPER:'Tren Superior'. `roleNames[role] || role`.

Divergencias de label a sincronizar (D160-03) — inventario del CONTEXT (los otros consumidores se refactorizan en 160-05):
 BlockCard 'Initium' · DayPlayer 'Initium' · BlockProgressionView 'Pyros' · goal-plan 'Initium'.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ampliar BlockRole/sessionMode + completar los mapas de color exhaustivos (SEM-10)</name>
  <read_first>
    - el-templo-app/src/modules/training/types/session.ts (BlockRole :12-21, sessionMode :140)
    - el-templo-app/src/modules/training/utils/blockColors.ts (3 Record<BlockRole> :25/:50/:73)
    - el-templo-app/src/utils/brandColors (BRAND_TERRACOTTA/BRAND_AGED_GOLD)
    - .planning/phases/160-.../160-CONTEXT.md (SEM-10; paleta de marca sin azul)
  </read_first>
  <action>
    En `training/types/session.ts`: agregar a `BlockRole` los cinco literales `'COMBOS_I'`, `'COMBOS_II'`, `'TECNICA_I'`, `'TECNICA_II'`, `'STRETCHING'` con un comentario de una línea cada uno (estilo de los existentes). Ampliar `Session.sessionMode` a `'regular' | 'rom' | 'combos' | 'tecnica'`.
    En `training/utils/blockColors.ts`: completar los 3 `Record<BlockRole,string>` con entradas para los 5 roles nuevos, usando SOLO la paleta de marca (terracotta / aged gold — NADA de azul/púrpura, reference_brand_palette). Sugerencia consistente: COMBOS_I/COMBOS_II → 'primary'/'secondary' (terracotta/oro, como NUCLEUS/DEUTEROS), TECNICA_I/TECNICA_II → 'primary'/'secondary', STRETCHING → 'secondary' (oro, como ROM/cierre). Para `getBlockColorClass` (clases CSS 'block-bg--*'): reusar clases existentes o agregar 'block-bg--default' donde no haya una clase específica (verificá que las clases usadas existan en el CSS del componente; si agregás una clase nueva 'block-bg--stretching' definí su estilo o reusá una existente — preferir reusar). Documentar la elección en comentarios de una línea.
    NO tocar `getBlockHeaderGradient` salvo que quieras incluir combos/tecnica en la rama terracotta (opcional; su default oro es aceptable). Sin `any`. Nunca `console.log`.
  </action>
  <verify>
    <automated>cd el-templo-app && pnpm exec vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "COMBOS_I\|COMBOS_II\|TECNICA_I\|TECNICA_II\|STRETCHING" el-templo-app/src/modules/training/types/session.ts` >= 5.
    - `session.ts` sessionMode contiene `'combos'` y `'tecnica'`.
    - Los 3 mapas de `blockColors.ts` incluyen los 5 roles nuevos (`grep -c "COMBOS_I\|STRETCHING" el-templo-app/src/modules/training/utils/blockColors.ts` >= 2 y `pnpm exec vue-tsc --noEmit` en 0 errores — antes del cambio los 3 Record exhaustivos fallan por claves faltantes, esa es la señal).
    - Solo colores de marca (sin 'blue'/'purple'/'cyan' nuevos en blockColors.ts: `grep -ci "blue\|purple\|cyan" el-templo-app/src/modules/training/utils/blockColors.ts` no aumenta respecto de master).
  </acceptance_criteria>
  <done>BlockRole/sessionMode ampliados; los 3 mapas de color exhaustivos cubren los 5 roles con paleta de marca; vue-tsc verde.</done>
</task>

<task type="auto">
  <name>Task 2: Dict de labels del app + refactor de BlockCard.formatRole (SEM-11)</name>
  <read_first>
    - el-templo-app/src/modules/training/components/BlockCard.vue (formatRole :77-90)
    - el-templo-app/src/modules/training/types/session.ts (BlockRole ampliado en Task 1)
    - .planning/phases/160-.../160-CONTEXT.md (D160-03: un dict por app + sincronizar divergencias)
  </read_first>
  <action>
    Crear `el-templo-app/src/constants/roleLabels.ts` con un `ROLE_LABELS: Record<BlockRole, string>` (exhaustivo, tipado con `BlockRole` para que el typecheck garantice cobertura) y un docblock que lo declare fuente única de labels del app (D160-03, espejo a propósito; NO paquete cross-app). Valores canónicos (sincronizando divergencias): INITIUM→'Initium', NUCLEUS→'Nucleus', DEUTEROS_1→'Deuteros 1', DEUTEROS_2→'Deuteros 2', ATHLOS→'Athlos', EPIKOS→'Epikos', ROM_LOWER→'Tren Inferior', ROM_CORE→'Zona Media', ROM_UPPER→'Tren Superior', COMBOS_I→'Combos I', COMBOS_II→'Combos II', TECNICA_I→'Técnica I', TECNICA_II→'Técnica II', STRETCHING→'Stretching'. (Nota: en 160-05, BlockProgressionView pasa de 'Pyros' a 'Initium' por esta sincronización — declararlo en el SUMMARY para UAT.)
    Refactorizar `BlockCard.vue` `formatRole`: eliminar el `roleNames` local e importar `ROLE_LABELS` del módulo nuevo; `return ROLE_LABELS[role] || role`.
    Sin `any`. Nunca `console.log`.
  </action>
  <verify>
    <automated>cd el-templo-app && pnpm exec vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `el-templo-app/src/constants/roleLabels.ts` existe, tipado `Record<BlockRole,string>`, con los 5 roles nuevos (`grep -c "COMBOS_I\|TECNICA_I\|STRETCHING" ...` >= 3).
    - `BlockCard.vue` importa `ROLE_LABELS` y ya NO define `roleNames` local (`grep -c "roleNames" el-templo-app/src/modules/training/components/BlockCard.vue` == 0).
    - `pnpm exec vue-tsc --noEmit` en 0 errores.
  </acceptance_criteria>
  <done>Existe el dict de labels del app tipado y exhaustivo; BlockCard lo consume; vue-tsc verde.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| -------- | ----------- |
| API → app (member device) | El app solo renderiza datos ya autorizados de la sesión; sin input no confiable nuevo. Cambios de tipo/label/color, sin lógica de acceso |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --------- | -------- | --------- | ----------- | --------------- |
| T-160-08 | Tampering | Rol no reconocido en el render | mitigate | El widening de BlockRole vuelve exhaustivos los mapas (compile-time); no quedan roles sin label/color |
| T-160-SC | Tampering | npm/pip installs | accept | Esta fase NO instala ni actualiza dependencias |

</threat_model>

<verification>
- `cd el-templo-app && pnpm exec vue-tsc --noEmit` verde (CI NO typechequea el app → gate local obligatorio).
- Sin migraciones; sin cambios de lógica de sesión (solo tipos/labels/colores).
</verification>

<success_criteria>
BlockRole/sessionMode del app ampliados; los 4 mapas exhaustivos (labels + 3 de color) cubren los 5 roles nuevos con paleta de marca; el dict de labels del app está centralizado y consumido por BlockCard; vue-tsc verde.
</success_criteria>

<output>
Create `.planning/phases/160-semana-nueva-frontend-admin-ui-horarios-app-pdf-y-tv/160-04-SUMMARY.md` when done. Documentar la ruta/exports de `constants/roleLabels.ts` (el plan 160-05 lo importa) y anotar el cambio 'Pyros'→'Initium' de BlockProgressionView que 160-05 aplicará (para UAT).
</output>
</content>
