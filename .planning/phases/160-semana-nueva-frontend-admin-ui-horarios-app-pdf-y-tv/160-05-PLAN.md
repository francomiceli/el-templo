---
phase: 160-semana-nueva-frontend-admin-ui-horarios-app-pdf-y-tv
plan: 05
type: execute
wave: 2
depends_on: [160-04]
files_modified:
  - el-templo-app/src/modules/training/pages/DayPlayer.vue
  - el-templo-app/src/modules/training/components/BlockProgressionView.vue
  - el-templo-app/src/modules/goal-plan/composables/useGoalPlanSession.ts
  - el-templo-app/src/modules/goal-plan/pages/GoalPlanSession.vue
  - el-templo-app/test/session-player-combos.test.ts
autonomous: true
requirements: [SEM-10]
must_haves:
  truths:
    - "Los consumidores de label del app (DayPlayer, BlockProgressionView, goal-plan) leen del dict centralizado en vez de sus BLOCK_NAMES locales"
    - "D160-05: en una sesión combos/técnica (sin bloques DEUTEROS) el player NO muestra la pantalla de elección de DEUTEROS — needsDeuterosChoice queda false y playableBlocks devuelve los 4 bloques en orden"
    - "Existe un test que prueba D160-05 de punta a punta sobre useSessionPlayer con una sesión combos"
  artifacts:
    - path: "el-templo-app/test/session-player-combos.test.ts"
      provides: "cobertura D160-05: sin prompt DEUTEROS en combos/técnica"
      contains: "needsDeuterosChoice"
    - path: "el-templo-app/src/modules/training/pages/DayPlayer.vue"
      provides: "BLOCK_NAMES → dict centralizado"
      contains: "roleLabels"
  key_links:
    - from: "useSessionPlayer.playableBlocks / needsDeuterosChoice"
      to: "sin bloques DEUTEROS → reproduce todos los bloques, sin choice"
      via: "hasDeuterosBlocks=false"
      pattern: "hasDeuterosBlocks|needsDeuterosChoice"
    - from: "DayPlayer/BlockProgressionView/goal-plan"
      to: "constants/roleLabels.ts (creado en 160-04)"
      via: "import del dict"
      pattern: "roleLabels|ROLE_LABELS"
---

<objective>
Terminar SEM-10 en el member app: refactorizar los consumidores de label restantes al dict centralizado (SEM-11) y VERIFICAR de punta a punta el criterio de aceptación D160-05 (en días sin bloques DEUTEROS — combos, técnica y ROM — el player NO pregunta qué DEUTEROS hacer), agregando el test que faltaba. El relevamiento confirma que `useSessionPlayer` ya cae al modo "reproducir todos los bloques en orden" cuando `hasDeuterosBlocks` es falso (`playableBlocks` :79-84, `needsDeuterosChoice` :150-153 devuelve false, `showDeuterosChoice` de DayPlayer :204-207 gatea sobre eso), así que esto es verificación + red de seguridad, no una superficie nueva.

Purpose: cerrar los últimos huecos de labels divergentes del app y blindar con un test que un día combos/técnica nunca dispare la elección de DEUTEROS (que no existe en esos modos).

Output: consumidores refactorizados + test D160-05; `vue-tsc` verde y el test verde. SIN migraciones.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

REGLA OPERATIVA: worktree `et-159`. `git add` por ruta explícita. Nunca `console.log`. Sin `any`. NO instalar dependencias. **CI NO typechequea el app** → gate de tipos = `pnpm exec vue-tsc --noEmit` LOCAL en foreground (obligatorio). Tests del app en foreground con timeout amplio: PROHIBIDO `run_in_background` y loops con `pgrep`.

Depende de 160-04: `el-templo-app/src/constants/roleLabels.ts` ya existe con los 14 roles. Importarlo, NO recrearlo.
</execution_context>

<context>
@.planning/phases/160-semana-nueva-frontend-admin-ui-horarios-app-pdf-y-tv/160-CONTEXT.md
@.planning/phases/160-semana-nueva-frontend-admin-ui-horarios-app-pdf-y-tv/160-04-SUMMARY.md

<interfaces>
<!-- Contratos vigentes en et-159. Usar directamente. -->

training/pages/DayPlayer.vue:267-276 — BLOCK_NAMES: Record<string,string> (INITIUM:'Initium', DEUTEROS_1/2:'Deuteros', ROM...) → migrar al dict.
training/pages/DayPlayer.vue:204-207 — showDeuterosChoice = splashDismissed && player.needsDeuterosChoice (ya gatea bien).
training/components/BlockProgressionView.vue:192-201 — BLOCK_NAMES local (INITIUM:'Pyros') → migrar al dict PERO mantener 'Pyros' vía LABEL_OVERRIDES local (excepción de Franco; ver action).
goal-plan/composables/useGoalPlanSession.ts:87-94 — BLOCK_NAMES inline en el computed blockLabels → migrar al dict.
goal-plan/pages/GoalPlanSession.vue:231-239 — BLOCK_NAMES local → migrar al dict.
(goal-plan nunca renderiza combos/tecnica/STRETCHING, pero usar el dict superset es inocuo y cumple D160-03.)

useSessionPlayer.ts:65-67 — hasDeuterosBlocks = blocks.some(role==='DEUTEROS_1'||'DEUTEROS_2').
useSessionPlayer.ts:79-84 — playableBlocks: si !hasDeuterosBlocks → `[...blocks].sort((a,b)=>a.sortOrder-b.sortOrder)` (todos, sin choice).
useSessionPlayer.ts:150-153 — needsDeuterosChoice: `if (!hasDeuterosBlocks) return false; return !deuterosChoice && currentBlockIndex>=2`.
useSessionPlayer factory: `useSessionPlayer(session: Session, ...)` → devuelve { playableBlocks, needsDeuterosChoice, hasDeuterosBlocks, ... } (ver export :440-470). Revisar la firma exacta del factory para el test.

Molde de test unitario del app: el-templo-app/test/level-display.test.ts (vitest puro, sin jsdom pesado).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor de los consumidores de label restantes al dict centralizado (SEM-11)</name>
  <read_first>
    - el-templo-app/src/constants/roleLabels.ts (dict de 160-04)
    - el-templo-app/src/modules/training/pages/DayPlayer.vue (BLOCK_NAMES :267)
    - el-templo-app/src/modules/training/components/BlockProgressionView.vue (BLOCK_NAMES :192)
    - el-templo-app/src/modules/goal-plan/composables/useGoalPlanSession.ts (BLOCK_NAMES :87)
    - el-templo-app/src/modules/goal-plan/pages/GoalPlanSession.vue (BLOCK_NAMES :231)
  </read_first>
  <action>
    En cada uno de los 4 archivos, eliminar el `BLOCK_NAMES` local e importar `ROLE_LABELS` de `src/constants/roleLabels.ts`, reemplazando las lecturas `BLOCK_NAMES[role] ?? role` / `BLOCK_NAMES[b.role] ?? b.role` por `ROLE_LABELS[role] ?? role` (mantener el fallback). En `useGoalPlanSession.ts` el `BLOCK_NAMES` está DENTRO del computed `blockLabels` → mover el import al tope del archivo y usar la constante importada.
    ⚠ **EXCEPCIÓN `BlockProgressionView.vue` (decisión de Franco 2026-08-14): esta pantalla MANTIENE 'Pyros' para INITIUM como override intencional** (el dict centralizado sigue en 'Initium' para el resto del app). Implementarlo con un override local explícito y documentado: `const LABEL_OVERRIDES: Partial<Record<BlockRole,string>> = { INITIUM: 'Pyros' };` y leer `LABEL_OVERRIDES[role] ?? ROLE_LABELS[role] ?? role`. Comentario: "override intencional — esta vista históricamente rotula INITIUM como 'Pyros' (confirmado con Franco); el resto del app usa el canónico 'Initium'". NO cambiar 'Pyros'→'Initium' acá.
    Verificar por grep que no queda ningún `BLOCK_NAMES` local en estos 4 archivos. Sin `any`. Nunca `console.log`.
  </action>
  <verify>
    <automated>cd el-templo-app && pnpm exec vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "const BLOCK_NAMES" el-templo-app/src/modules/training/pages/DayPlayer.vue el-templo-app/src/modules/training/components/BlockProgressionView.vue el-templo-app/src/modules/goal-plan/composables/useGoalPlanSession.ts el-templo-app/src/modules/goal-plan/pages/GoalPlanSession.vue` == 0 en los 4.
    - Los 4 importan `ROLE_LABELS` de `src/constants/roleLabels.ts`.
    - `pnpm exec vue-tsc --noEmit` en 0 errores.
  </acceptance_criteria>
  <done>Los 4 consumidores de label restantes leen del dict centralizado; queda una sola fuente de labels en el app; vue-tsc verde.</done>
</task>

<task type="auto">
  <name>Task 2: Verificar + testear que combos/técnica no dispara el prompt de DEUTEROS (D160-05)</name>
  <read_first>
    - el-templo-app/src/modules/training/composables/useSessionPlayer.ts (hasDeuterosBlocks :65, playableBlocks :79, needsDeuterosChoice :150, firma del factory + export :440-470)
    - el-templo-app/src/modules/training/pages/DayPlayer.vue (showDeuterosChoice :204-207)
    - el-templo-app/test/level-display.test.ts (molde de test unitario vitest)
    - el-templo-app/src/modules/training/types/session.ts (Session/Block/BlockRole ampliados en 160-04)
  </read_first>
  <behavior>
    - Test 1: `useSessionPlayer(combosSession)` con bloques [INITIUM, COMBOS_I, COMBOS_II, STRETCHING] (sin DEUTEROS): `hasDeuterosBlocks.value === false`.
    - Test 2: `needsDeuterosChoice.value === false` en ese estado (y sigue false tras avanzar de bloque, p.ej. `currentBlockIndex >= 2`).
    - Test 3: `playableBlocks.value` tiene los 4 bloques en orden de `sortOrder` (INITIUM, COMBOS_I, COMBOS_II, STRETCHING), sin exigir choice.
    - Test 4 (técnica): lo mismo con [INITIUM, TECNICA_I, TECNICA_II, STRETCHING].
    - Test 5 (regresión): una sesión regular con DEUTEROS_1/DEUTEROS_2 SÍ tiene `hasDeuterosBlocks===true` y `needsDeuterosChoice===true` al llegar a currentBlockIndex>=2 (el prompt sigue vivo donde corresponde).
  </behavior>
  <action>
    Primero VERIFICAR de punta a punta (lectura + razonamiento) que ninguna pantalla intermedia muestre el selector de DEUTEROS en combos/técnica: `showDeuterosChoice` (DayPlayer :204) depende de `needsDeuterosChoice`, que devuelve false sin bloques DEUTEROS; `playableBlocks` reproduce todo en orden. Confirmar que no hay OTRO punto que fuerce la elección (grep de `selectDeuteros`/`deuterosChoice`/`needsDeuterosChoice` en las pantallas del flujo). Documentar el resultado en el SUMMARY. Si aparece un camino que muestra el prompt en combos/técnica, corregirlo (guarda por `hasDeuterosBlocks`).
    Crear `el-templo-app/test/session-player-combos.test.ts` sobre el molde de un test unitario del app. Construir sesiones mock (`Session`) con la forma real (dayId, week, day, levelGroup, blockCount, blocks con role/sortOrder/exercises mínimos). Instanciar `useSessionPlayer` con la sesión (revisar la firma exacta del factory: qué args toma además de la sesión — pasar mocks/no-ops donde haga falta). Cubrir los 5 comportamientos. Para el Test 5, avanzar el estado (setear `currentBlockIndex` o usar la API de navegación disponible) hasta el punto de choice.
    Sin `any`. Nunca `console.log`.
  </action>
  <verify>
    <automated>cd el-templo-app && pnpm exec vue-tsc --noEmit && pnpm vitest run test/session-player-combos.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `el-templo-app/test/session-player-combos.test.ts` existe y cubre los 5 comportamientos.
    - El test asserta `needsDeuterosChoice===false` y `playableBlocks.length===4` para combos y técnica, y el caso de regresión regular con DEUTEROS mantiene el prompt.
    - El test corre en foreground y pasa.
    - `pnpm exec vue-tsc --noEmit` en 0 errores.
  </acceptance_criteria>
  <done>Verificado y testeado que combos/técnica no disparan el prompt de DEUTEROS; el prompt sigue vivo en días regulares; vue-tsc + test verdes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| -------- | ----------- |
| API → app (member device) | El app solo renderiza y navega la sesión ya autorizada; sin input no confiable nuevo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --------- | -------- | --------- | ----------- | --------------- |
| T-160-09 | Tampering | Flujo de player con datos combos/técnica | mitigate | El player gatea el choice por `hasDeuterosBlocks`; el test D160-05 blinda que combos/técnica reproduzcan todo sin prompt |
| T-160-SC | Tampering | npm/pip installs | accept | Esta fase NO instala ni actualiza dependencias |

</threat_model>

<verification>
- `cd el-templo-app && pnpm exec vue-tsc --noEmit` verde (CI NO typechequea el app → gate local obligatorio).
- `pnpm vitest run test/session-player-combos.test.ts` verde en foreground.
- Sin migraciones.
</verification>

<success_criteria>
Los consumidores de label del app leen del dict centralizado; verificado y testeado que en combos/técnica (y ROM) el player nunca pide elegir DEUTEROS y reproduce todos los bloques en orden, mientras el prompt sigue vivo en días regulares; vue-tsc y el test verdes.
</success_criteria>

<output>
Create `.planning/phases/160-semana-nueva-frontend-admin-ui-horarios-app-pdf-y-tv/160-05-SUMMARY.md` when done. Documentar el resultado de la verificación de punta a punta del no-prompt de DEUTEROS y la firma exacta usada para instanciar useSessionPlayer en el test.
</output>
</content>
