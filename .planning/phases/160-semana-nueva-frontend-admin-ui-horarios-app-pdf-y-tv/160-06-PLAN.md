---
phase: 160-semana-nueva-frontend-admin-ui-horarios-app-pdf-y-tv
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/scheduling/derived-label.ts
  - el-templo-api/src/modules/scheduling/service.ts
  - el-templo-api/src/modules/scheduling/booking-service.ts
  - el-templo-api/test/scheduling/my-bookings-derived-label.test.ts
  - el-templo-app/version.txt
autonomous: true
requirements: [SEM-14, SEM-10]
must_haves:
  truths:
    - "En las reservas del alumno (getMyBookings), un slot de la actividad genérica en un día combos/técnica muestra 'Combos'/'Técnica' en vez de 'General' — derivado de la sesión aprobada del día (misma regla que el grid admin, 159-06)"
    - "En la asistencia del alumno (getMyWeeklyAttendance, línea 'Asististe' de ReservasPage), un día combos/técnica ya cursado muestra 'Combos'/'Técnica' — la MISMA derivación que las reservas, para que ReservasPage no muestre 'General' en 'Asististe' y 'Técnica' en las próximas reservas del mismo día (inconsistencia)"
    - "La derivación respeta isSpecial: una actividad especial NUNCA se renombra, aunque el día tenga combos/técnica aprobado"
    - "La lógica de derivación está en UN helper compartido (scheduling/derived-label.ts) consumido por getWeeklyGrid, getMyBookings Y getMyWeeklyAttendance — no duplicada"
    - "El admin HorariosPage ya muestra el nombre derivado (getWeeklyGrid, 159-06): verificado; el app ReservasPage muestra el activityName que devuelve el API"
    - "La member app queda bumpeada a 1.7.6"
  artifacts:
    - path: "el-templo-api/src/modules/scheduling/derived-label.ts"
      provides: "deriveActivityLabel() + helper de modeByDay compartidos (DRY)"
      contains: "deriveActivityLabel"
    - path: "el-templo-api/src/modules/scheduling/booking-service.ts"
      provides: "getMyBookings Y getMyWeeklyAttendance derivan Combos/Técnica/General (ambas fuentes de ReservasPage)"
      contains: "deriveActivityLabel"
    - path: "el-templo-api/test/scheduling/my-bookings-derived-label.test.ts"
      provides: "integración: reservas del socio derivan combos/tecnica, respetan isSpecial y General por defecto"
      contains: "Combos"
    - path: "el-templo-app/version.txt"
      provides: "bump a 1.7.6"
      contains: "1.7.6"
  key_links:
    - from: "booking-service.getMyBookings"
      to: "deriveActivityLabel(activityName, isSpecial, dayMode) del helper compartido"
      via: "modeByDay de la sesión aprobada de la semana (query tenant-safe nueva)"
      pattern: "deriveActivityLabel|modeByDay"
    - from: "scheduling/service.ts getWeeklyGrid"
      to: "el mismo helper compartido (refactor DRY del DERIVED_CLASS_LABEL de 159-06)"
      via: "import de derived-label.ts"
      pattern: "deriveActivityLabel"
---

<objective>
Completar SEM-14 en la superficie del socio: hoy las reservas del alumno (`getMyBookings`) muestran el nombre crudo de la actividad (`activities.name` = "General"), no el derivado. El grid admin ya deriva "Combos"/"Técnica"/"General" de la sesión aprobada del día (159-06, `getWeeklyGrid` + `DERIVED_CLASS_LABEL`), pero la ruta de reservas del socio no. Este plan extrae la lógica a un helper compartido (DRY) y la aplica a `getMyBookings`, verifica que el admin HorariosPage y el app ReservasPage muestran el nombre correcto, y cierra la fase con el bump de la app a 1.7.6.

Purpose: es donde el socio ve el nombre de la clase que reservó (ítem resuelto por Franco: "se ve en la parte de reservas del alumno"). Sin esto, un alumno que reservó un miércoles de Técnica ve "General".

Output: helper compartido + derivación en getMyBookings + test + bump; `tsc` verde y el test verde (gate real en CI). SIN migraciones (la mig 0204 "General" fue 159-06; se reusa).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

REGLA OPERATIVA: worktree `et-159`. `git add` por ruta explícita. Nunca `console.log` (logger Pino en el API). Sin `any`; `catch (err: unknown)` con `instanceof Error`. NO instalar dependencias. Los usuarios se resuelven por email (`registerUser`/`getAuthToken`), NUNCA con ids hardcodeados. Typecheck del API local sí (`pnpm exec tsc --noEmit`). Tests SOLO en foreground con `VITEST_POOL_ID=et159` y timeout amplio: PROHIBIDO `run_in_background` y loops con `pgrep`. El MySQL de test es COMPARTIDO entre worktrees y Vitest 4 pisa `VITEST_POOL_ID` → la verificación local del test de DB puede COLISIONAR con otro worktree; el gate REAL es CI, pero el ejecutor DEBE intentar correrlo en foreground y declarar el resultado.

⚠ TENANCY: `scheduling` NO adoptó tenantWhere (doc 03 §3, grandfathered en el allowlist). La query nueva de modeByDay en getMyBookings debe llevar el marcador `/* tenant-safe: scheduling aun no adopto tenantWhere (doc 03 §3); mismo patron no-strict que el resto del archivo, single-tenant activo hoy */` (igual que 159-06 en service.ts) para pasar el gate de entradas ganadas de lint:tenant en CI. NO agregarla al allowlist. Correr `pnpm lint` (o el script de lint:tenant) local si está disponible.
</execution_context>

<context>
@.planning/phases/159-semana-nueva-backend-modos-de-d-a-generadores-roles-de-bloqu/159-06-SUMMARY.md
@.planning/phases/160-semana-nueva-frontend-admin-ui-horarios-app-pdf-y-tv/160-CONTEXT.md

<interfaces>
<!-- Contratos vigentes en et-159. Usar directamente. -->

scheduling/service.ts:63-66 — DERIVED_CLASS_LABEL (a extraer al helper):
const DERIVED_CLASS_LABEL: Record<string, string> = { combos: "Combos", tecnica: "Técnica" };
scheduling/service.ts:245 — const week = dateToWeekNumber(weekStartDate);
scheduling/service.ts:247-260 — modeRows (selectDistinct day+sessionMode de sessions aprobadas, goalPlanType null) + modeByDay Map; ya lleva el marcador /* tenant-safe ... */.
scheduling/service.ts:355-365 — derivación por slot: isGenericActivity = activityName==="General" && !isSpecial; dayMode = modeByDay.get(dayName); derivedLabel = isGenericActivity ? DERIVED_CLASS_LABEL[dayMode ?? ""] : undefined; activityName: derivedLabel ?? row.activityName.
scheduling/service.ts:27-30 — imports: addDays/todayInTz, dateToWeekNumber (../shared/week-dates), DAY_OF_WEEK_MAP (../shared/training-constants).

booking-service.ts:529-574 — getMyBookings(memberId, weekStartDate): select con activityName: schema.activities.name, dayOfWeek: schema.schedules.dayOfWeek, bookingDate, isSpecial: schema.activities.isSpecial; `.map(r => this.mapBookingRow(r))`.
booking-service.ts:2174-2213 — mapBookingRow(row): pasa activityName tal cual a BookingRecord.activityName. (row ya trae dayOfWeek e isSpecial.)
booking-service.ts:579-625 — getMyWeeklyAttendance(memberId, weekStartDate): SEGUNDA fuente de ReservasPage (línea "Asististe", ReservasPage.vue:1298 `activityName: att.activityName`). Trae `activityName: schema.activities.name` CRUDO + su dayOfWeek + isSpecial de la actividad → hay que derivar acá TAMBIÉN, con el mismo helper y el mismo modeByDay de la semana. VERIFICAR los nombres de columna exactos del select (dayOfWeek/isSpecial) al leer el código; si falta isSpecial o dayOfWeek en el select, agregarlos.
booking-service.ts:18 — imports addDays de ../shared/date-utils. dateToWeekNumber y DAY_OF_WEEK_MAP hay que IMPORTARLOS (mismo origen que service.ts).

Test mold: test/scheduling/derived-class-label.test.ts (159-06) — createTestApp, getAuthToken, cleanAllTestData, siembra sesiones aprobadas por (week, day, sessionMode) y activities. Reusar su patrón de seed.
DAY_OF_WEEK_MAP: number→nombre de día ("lunes".."sabado").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helper compartido de derivación + aplicarlo en getMyBookings (SEM-14 member)</name>
  <read_first>
    - el-templo-api/src/modules/scheduling/service.ts (DERIVED_CLASS_LABEL :63; modeRows/modeByDay :245-260; derivación por slot :355-372)
    - el-templo-api/src/modules/scheduling/booking-service.ts (getMyBookings :529-574; mapBookingRow :2174-2213; imports :18)
    - el-templo-api/test/scheduling/derived-class-label.test.ts (molde de seed/aserción)
    - el-templo-api/src/modules/shared/training-constants.ts (DAY_OF_WEEK_MAP)
  </read_first>
  <behavior>
    - Test 1: un socio con una reserva en un día con sesión aprobada `session_mode='combos'` para (week, ese día), en la actividad genérica "General" no-especial → `getMyBookings` devuelve `activityName: "Combos"`.
    - Test 2: `session_mode='tecnica'` → `activityName: "Técnica"`.
    - Test 3: un día sin sesión combos/técnica (regular/rom) → conserva "General".
    - Test 4: una reserva en una actividad `isSpecial` en el MISMO día de combos NO se renombra (conserva su nombre).
    - Test 5 (regresión): `getWeeklyGrid` (admin) sigue derivando igual (el test derived-class-label.test.ts de 159-06 sigue verde tras el refactor DRY).
    - Test 6 (asistencia): un socio que YA cursó (asistencia registrada) un día combos/técnica → `getMyWeeklyAttendance` devuelve `activityName: "Combos"/"Técnica"` (misma derivación que las reservas); un día regular conserva "General"; una asistencia a actividad especial ese día NO se renombra.
  </behavior>
  <action>
    Crear `el-templo-api/src/modules/scheduling/derived-label.ts` con: (1) `export const DERIVED_CLASS_LABEL: Record<string,string> = { combos: "Combos", tecnica: "Técnica" };` (movido desde service.ts); (2) `export function deriveActivityLabel(activityName: string, isSpecial: boolean, dayMode: string | undefined): string` que replica la regla actual: `if (activityName === "General" && !isSpecial) { return DERIVED_CLASS_LABEL[dayMode ?? ""] ?? activityName; } return activityName;`. Docblock: fuente única de la derivación de nombre de clase (D-15/D-17); consumida por getWeeklyGrid, getMyBookings y getMyWeeklyAttendance.
    Refactorizar `scheduling/service.ts`: importar `deriveActivityLabel`/`DERIVED_CLASS_LABEL` del helper y reemplazar la construcción inline de `derivedLabel`/`isGenericActivity` (:355-365) por `activityName: deriveActivityLabel(row.activityName, row.isSpecial, dayMode)`. NO cambiar el comportamiento observable (el test de 159-06 debe seguir verde). Eliminar el `DERIVED_CLASS_LABEL` local de service.ts (ahora vive en el helper).
    En `booking-service.ts`: importar `dateToWeekNumber` (../shared/week-dates), `DAY_OF_WEEK_MAP` (../shared/training-constants) y `deriveActivityLabel` (./derived-label). **Para NO duplicar la carga del modo, extraé un método privado** `private async loadModeByDay(weekStartDate): Promise<Map<string,string>>` con la query nueva (mismo patrón que service.ts:247-260) CON el marcador `/* tenant-safe: ... */` (ver execution_context): `const week = dateToWeekNumber(weekStartDate); const modeRows = await this.db.selectDistinct({ day: schema.sessions.day, sessionMode: schema.sessions.sessionMode }).from(schema.sessions).where(and(eq(week), eq(status,'approved'), isNull(goalPlanType)));` → devuelve `modeByDay`. Consumilo en AMBAS rutas:
    - `getMyBookings`: `const modeByDay = await this.loadModeByDay(weekStartDate);` y en el `.map`, por row: `const dayName = DAY_OF_WEEK_MAP[row.dayOfWeek]; const dayMode = dayName ? modeByDay.get(dayName) : undefined; const derived = deriveActivityLabel(row.activityName, row.isSpecial, dayMode);` → pasar `{...row, activityName: derived}` a `mapBookingRow` (o mutar `row.activityName` antes de mapear).
    - `getMyWeeklyAttendance` (booking-service.ts:579-625, la 2ª fuente de ReservasPage — línea "Asististe"): aplicar EXACTAMENTE la misma derivación sobre `activityName: schema.activities.name` (hoy crudo). Confirmá al leer el código que el select trae `dayOfWeek` e `isSpecial`; si falta alguno, agregalo al select para poder derivar. Sin esto, "Asististe" queda en "General" mientras las reservas del mismo día dicen "Técnica" (el blocker que marcó el checker).
    NO tocar cupos/gating/reservas/asistencia — solo la etiqueta visible (D-17).
    Crear `test/scheduling/my-bookings-derived-label.test.ts` sobre el molde de derived-class-label.test.ts: sembrar sesiones aprobadas por modo + schedules de la actividad "General" y una especial, reservar un socio (por email) Y registrarle asistencia, llamar el endpoint de "mis reservas" Y el de "mi asistencia semanal", y assertear los 6 casos (reservas: combos/tecnica/General/isSpecial; asistencia: combos/tecnica derivan + isSpecial no se renombra). Usuarios por email, nunca ids hardcodeados.
    Sin `any`. Nunca `console.log`.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit && VITEST_POOL_ID=et159 pnpm vitest run test/scheduling/my-bookings-derived-label.test.ts test/scheduling/derived-class-label.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `el-templo-api/src/modules/scheduling/derived-label.ts` existe y exporta `deriveActivityLabel` y `DERIVED_CLASS_LABEL`.
    - `scheduling/service.ts` importa el helper y ya NO define `DERIVED_CLASS_LABEL` local (`grep -c "const DERIVED_CLASS_LABEL" el-templo-api/src/modules/scheduling/service.ts` == 0).
    - `getMyBookings` Y `getMyWeeklyAttendance` usan `deriveActivityLabel` (vía el helper privado `loadModeByDay`), y la query lleva el marcador tenant-safe (`grep -c "deriveActivityLabel" el-templo-api/src/modules/scheduling/booking-service.ts` >= 2; `grep -c "tenant-safe" el-templo-api/src/modules/scheduling/booking-service.ts` >= 1).
    - El test nuevo cubre reservas Y asistencia (combos/tecnica/General/isSpecial en ambas) y no hardcodea ids de users.
    - `derived-class-label.test.ts` (159-06) sigue verde tras el refactor.
    - `pnpm exec tsc --noEmit` en 0 errores.
  </acceptance_criteria>
  <done>La derivación vive en un helper compartido; getMyBookings muestra Combos/Técnica/General respetando isSpecial; el grid admin sin regresión; tsc verde y tests verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Verificar superficies de lectura (admin/app) + bump de la app a 1.7.6</name>
  <read_first>
    - el-templo-admin/src/pages/HorariosPage.vue (slot.activityName :242/:348 — muestra el derivado de getWeeklyGrid)
    - el-templo-app/src/pages/ReservasPage.vue (activityName :165/:314/:386 — muestra el derivado de getMyBookings)
    - el-templo-app/version.txt (1.7.5) y el-templo-app/bump-version.sh
  </read_first>
  <action>
    VERIFICACIÓN (lectura + razonamiento, sin cambios de código salvo que se detecte un hueco):
    - Admin HorariosPage: confirmar que `slot.activityName` viene de `getWeeklyGrid` (endpoint del grid semanal) → ya derivado (159-06). Si el HorariosPage usara OTRO endpoint que devuelve el nombre crudo, documentarlo como hallazgo (posible seguimiento) — NO lo arregles en este plan salvo que sea trivial y dentro de scheduling.
    - App ReservasPage: confirmar que muestra `activityName` de la respuesta de "mis reservas" (getMyBookings) → ahora derivado por Task 1. `nextBooking.activityName` (:314) también. Si hay OTRA vista de reservas del socio (p.ej. ReservasPageLegacy o próxima clase) que use un endpoint distinto con nombre crudo, documentarla como hallazgo.
    Declarar el resultado de ambas verificaciones en el SUMMARY (qué endpoint alimenta cada superficie y si quedó derivado).
    BUMP: actualizar `el-templo-app/version.txt` a `1.7.6` (usar `bump-version.sh` si el flujo del repo lo requiere; si solo edita version.txt, editarlo directo). Este es el cierre de la fase 160 (instrucción de Franco).
    Sin `any`. Nunca `console.log`.
  </action>
  <verify>
    <automated>cd el-templo-app && test "$(cat version.txt)" = "1.7.6" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `el-templo-app/version.txt` == `1.7.6`.
    - El SUMMARY documenta qué endpoint alimenta HorariosPage (admin) y ReservasPage (app) y confirma que muestran el nombre derivado (o lista los huecos hallados).
    - Si se tocó algún .vue del app, `pnpm exec vue-tsc --noEmit` verde.
  </acceptance_criteria>
  <done>Verificado que las superficies de lectura del socio y del admin muestran el nombre derivado; la app queda en 1.7.6.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| -------- | ----------- |
| API → app/admin (lectura) | La etiqueta derivada se calcula server-side desde la sesión aprobada; el cliente solo la muestra. La derivación NO toca reservas/cupos/gating (D-17) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --------- | -------- | --------- | ----------- | --------------- |
| T-160-10 | Tampering | Renombrar una actividad especial por error | mitigate | `deriveActivityLabel` solo renombra la actividad genérica "General" no-especial; el test 4 cubre isSpecial |
| T-160-11 | Information Disclosure | Query de modeByDay cross-tenant | accept | Catálogo de sesiones single-tenant hoy; query con marcador tenant-safe (scheduling no strict-adoptado, doc 03 §3), mismo patrón que getWeeklyGrid |
| T-160-12 | Denial of Service | N+1 en getMyBookings | mitigate | UNA query de modeByDay por semana (no por reserva), igual que getWeeklyGrid |
| T-160-SC | Tampering | npm/pip installs | accept | Esta fase NO instala ni actualiza dependencias |

</threat_model>

<verification>
- `cd el-templo-api && pnpm exec tsc --noEmit` verde.
- `VITEST_POOL_ID=et159 pnpm vitest run test/scheduling/my-bookings-derived-label.test.ts test/scheduling/derived-class-label.test.ts` verde en foreground (advertencia: MySQL de test compartido puede colisionar; gate real en CI).
- `lint:tenant` sin discrepancias nuevas (query nueva con marcador tenant-safe, no en allowlist).
- `el-templo-app/version.txt` == 1.7.6.
- Sin migraciones nuevas (se reusa la 0204 de 159-06).
</verification>

<success_criteria>
Las reservas del alumno muestran Combos/Técnica/General derivado de la sesión aprobada, respetando isSpecial, con la lógica en un helper compartido (DRY) sin regresión del grid admin; superficies de lectura verificadas; app en 1.7.6; tsc y tests verdes.
</success_criteria>

<output>
Create `.planning/phases/160-semana-nueva-frontend-admin-ui-horarios-app-pdf-y-tv/160-06-SUMMARY.md` when done. Documentar el helper compartido, la query tenant-safe agregada, y el mapa de qué endpoint alimenta cada superficie de lectura (admin/app).
</output>
</content>
