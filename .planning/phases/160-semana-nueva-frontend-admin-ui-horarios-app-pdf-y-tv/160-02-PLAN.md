---
phase: 160-semana-nueva-frontend-admin-ui-horarios-app-pdf-y-tv
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-admin/src/constants/roleLabels.ts
  - el-templo-admin/src/utils/pdf/pdf-types.ts
  - el-templo-admin/src/utils/pdf/session-data-transformer.ts
  - el-templo-admin/src/utils/pdf/session-pdf-builder.ts
  - el-templo-admin/src/utils/pdf/__tests__/combos-tecnica-pdf.test.ts
autonomous: true
requirements: [SEM-09, SEM-11]
must_haves:
  truths:
    - "Un día combos aprobado produce un PDF con INITIUM/PYROS + COMBOS I (grid) + COMBOS II (grid) + STRETCHING (lista simple estilo INITIUM) + cierre — no 'solo INITIUM + cierre'"
    - "Un día técnica produce INITIUM + TÉCNICA I + TÉCNICA II + STRETCHING + cierre"
    - "D160-04: STRETCHING se imprime como lista simple compartida estilo INITIUM (una lista de ~4 ejercicios común a los 6 niveles), NO como grid por nivel"
    - "SEM-11: existe UN diccionario rol→label en el admin (constants/roleLabels.ts); el PDF lo consume en vez de literales dispersos"
    - "formatNameWithParams (3 copias espejo) NO se modifica en este plan — ya maneja combos/stretching (verificar antes de tocar)"
  artifacts:
    - path: "el-templo-admin/src/constants/roleLabels.ts"
      provides: "ROLE_LABELS del admin (full, para PDF y editor) centralizado — consumido también por 160-03"
      contains: "COMBOS_I"
    - path: "el-templo-admin/src/utils/pdf/session-data-transformer.ts"
      provides: "rama combos/tecnica en sessionsToPdfDay (COMBOS/TECNICA I/II grid + STRETCHING simpleExercises)"
      contains: "STRETCHING"
    - path: "el-templo-admin/src/utils/pdf/session-pdf-builder.ts"
      provides: "rama combos/tecnica en buildDayContent (bloques + STRETCHING estilo INITIUM)"
      contains: "STRETCHING"
    - path: "el-templo-admin/src/utils/pdf/__tests__/combos-tecnica-pdf.test.ts"
      provides: "cobertura de estructura del PDF combos/tecnica + STRETCHING como lista simple"
      contains: "STRETCHING"
  key_links:
    - from: "sessionsToPdfDay"
      to: "PdfBlockPage con isStretching + simpleExercises para STRETCHING"
      via: "detección de modo por roles COMBOS_/TECNICA_/STRETCHING"
      pattern: "isStretching|STRETCHING"
    - from: "buildDayContent"
      to: "buildInitiumPage-style para STRETCHING + buildFullBlockPage para COMBOS/TECNICA I/II"
      via: "tercera rama de layout"
      pattern: "isStretching|COMBOS|TECNICA"
---

<objective>
Enseñarle al generador de PDF de planis a imprimir los días combos y técnica (SEM-09), y centralizar el diccionario de labels del admin (SEM-11). Hoy `sessionsToPdfDay` tiene 2 ramas (ROM / regular): un día combos/técnica caería en la rama regular, no encontraría NUCLEUS/DEUTEROS/EPIKOS y saldría "solo INITIUM + página de cierre". Este plan agrega la tercera rama (combos/técnica) en el transformer y en el builder, con STRETCHING como lista simple compartida estilo INITIUM (D160-04), y crea el módulo `constants/roleLabels.ts` que también consume el editor (plan 160-03).

Purpose: SEM-09 es, junto con SEM-15, la razón dura por la que 159 no puede shippearse sola. El PDF impreso es la superficie que el profe usa en el gimnasio.

Output: rama combos/técnica en transformer + builder + `roleLabels.ts` + test de estructura; `vue-tsc` verde y el test verde. SIN migraciones. `formatNameWithParams` NO se toca (ya maneja combos/stretching).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

REGLA OPERATIVA: worktree `et-159`. `git add` por ruta explícita. Nunca `console.log` (usar `createLogger()`). Sin `any`. NO instalar dependencias. **CI NO typechequea el admin** → el gate de tipos de este plan es `pnpm exec vue-tsc --noEmit` corrido LOCAL en foreground (obligatorio). Los tests del admin (vitest jsdom) corren local en foreground con timeout amplio: PROHIBIDO `run_in_background` y loops con `pgrep`.

⚠ COPIAS ESPEJO: `formatNameWithParams` tiene 3 copias (`el-templo-admin/src/utils/pdf/session-data-transformer.ts:62`, `session-pdf-builder.ts`, y el espejo del API `el-templo-api/src/modules/admin/format-params.ts` + `tv/roster.ts`). CONTEXT confirma que ya manejan `combos` (rounds-only) y `stretching` (default). Este plan NO debe modificar `formatNameWithParams`; si el ejecutor cree necesitarlo, PARAR y revisar — cualquier cambio ahí obliga a sincronizar las 3 copias en el MISMO commit y afecta también la TV y el PDF impreso.
</execution_context>

<context>
@.planning/phases/160-semana-nueva-frontend-admin-ui-horarios-app-pdf-y-tv/160-CONTEXT.md

<interfaces>
<!-- Contratos vigentes en et-159. Usar directamente. -->

pdf-types.ts:32-45 — PdfBlockPage:
export interface PdfBlockPage {
  role: string;            // INITIUM/NUCLEUS/DEUTEROS I/II/EPIKOS/ATHLOS o zona ROM
  blockName?: string;      // "PYROS" para INITIUM
  formatName?: string;
  customTitle?: string | null;
  mobility?: string;
  levelBlocks?: PdfLevelBlock[];  // grid por nivel
  simpleExercises?: string[];     // lista simple (INITIUM/PYROS)
  isRom?: boolean;
}
pdf-types.ts:48-51 — PdfDaySession { dayName, week, blocks: PdfBlockPage[] }

session-data-transformer.ts:401-499 — sessionsToPdfDay(sessions): PdfDaySession:
 - detecta ROM: `const isRom = sessions.some(s => s.blocks.some(b => b.role.startsWith('ROM_')));`
 - INITIUM shared: findInitiumBlock(sessions) → push {role:'INITIUM', blockName, formatName, customTitle, simpleExercises: exercises.map(formatInitiumExercise)}
 - grid: `buildGridPage(role, displayRole, sessionsByLevel)` (NUCLEUS/DEUTEROS/EPIKOS) → PdfBlockPage con levelBlocks
 - DEUTEROS: buildGridPage('DEUTEROS_1','DEUTEROS I') / ('DEUTEROS_2','DEUTEROS II')
session-data-transformer.ts:351-385 — buildGridPage: junta los bloques del rol en TODOS los niveles (LEVEL_ORDER),
 formato/movilidad del nivel canónico. Sirve tal cual para COMBOS_I/II y TECNICA_I/II (6 niveles).
session-data-transformer.ts:387-393 — ROM_ZONE_LABELS (literal local a migrar al dict admin).
session-data-transformer.ts:428-431 — formatInitiumExercise(e, formatName) arma la línea de la lista simple.

session-pdf-builder.ts:305 — buildInitiumPage(block: PdfBlockPage): Content[] (usa block.simpleExercises).
session-pdf-builder.ts:648 — buildFullBlockPage(block): Content[] (grid de un bloque, levelBlocks).
session-pdf-builder.ts:842 — buildDeuterosSplitPages(deut1, deut2) (par lado a lado).
session-pdf-builder.ts:1054-1102 — buildDayContent(day): cover + `isRomDay = day.blocks.some(b=>b.isRom)`:
   ROM → buildInitiumPage(initium) + buildRomBlockPage por zona.
   regular → initium/nucleus/deut1/deut2/epikos con buildInitiumPage/buildFullBlockPage/buildDeuterosSplitPages.
   + buildClosingPage.

admin types/session.ts:99 — SessionDetail.blocks[].role: string (los roles COMBOS_/TECNICA_/STRETCHING ya llegan como strings crudos del API).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dict de labels del admin + rama combos/técnica en el transformer (SEM-11, SEM-09 datos)</name>
  <read_first>
    - el-templo-admin/src/utils/pdf/session-data-transformer.ts (sessionsToPdfDay :401-499, buildGridPage :351-385, ROM_ZONE_LABELS :387-393)
    - el-templo-admin/src/utils/pdf/pdf-types.ts (PdfBlockPage :32-45)
    - el-templo-admin/src/utils/pdf/session-pdf-builder.ts (SOLO leer buildInitiumPage :305 para entender el shape de simpleExercises)
    - .planning/phases/160-.../160-CONTEXT.md (D160-04: STRETCHING lista simple; D160-02 labels; D160-03 un dict por app)
  </read_first>
  <action>
    Crear `el-templo-admin/src/constants/roleLabels.ts` con un `ROLE_LABELS: Record<string,string>` (etiqueta LARGA para PDF y editor): INITIUM→"INITIUM", NUCLEUS→"NUCLEUS", DEUTEROS_1→"DEUTEROS I", DEUTEROS_2→"DEUTEROS II", EPIKOS→"EPIKOS", ATHLOS→"ATHLOS", ROM_LOWER→"TREN INFERIOR", ROM_CORE→"ZONA MEDIA", ROM_UPPER→"TREN SUPERIOR", COMBOS_I→"COMBOS I", COMBOS_II→"COMBOS II", TECNICA_I→"TÉCNICA I", TECNICA_II→"TÉCNICA II", STRETCHING→"STRETCHING". Docblock: fuente única de labels del admin (D160-03, espejo a propósito del API; NO paquete compartido). Reemplazar el `ROM_ZONE_LABELS` local del transformer por referencias a este dict (o dejar ROM_ZONE_LABELS como alias que deriva del dict, para no reescribir la rama ROM — preferir consumir el dict directo donde se usa `ROM_ZONE_LABELS[zone]`).
    En `pdf-types.ts`: agregar a `PdfBlockPage` el flag opcional `isStretching?: boolean` (paralelo a `isRom`), para que el builder distinga la página de STRETCHING (lista simple) de un grid.
    En `session-data-transformer.ts` `sessionsToPdfDay`: agregar la detección y la rama combos/técnica ANTES del return regular (después de la rama `if (isRom)`):
    - Detectar: `const isCombosTecnica = sessions.some(s => s.blocks.some(b => b.role.startsWith('COMBOS_') || b.role.startsWith('TECNICA_')));`
    - Si `isCombosTecnica`: push INITIUM shared (mismo patrón que la rama regular, findInitiumBlock + simpleExercises). Luego, para COMBOS: `buildGridPage('COMBOS_I', ROLE_LABELS['COMBOS_I'], sessionsByLevel)` y `('COMBOS_II', ROLE_LABELS['COMBOS_II'], ...)`; para TECNICA lo análogo con TECNICA_I/TECNICA_II. Solo pushear el que exista (buildGridPage devuelve null si no hay bloques) — un día es combos O técnica, no ambos, así que en la práctica se pushea uno de los dos pares. Para STRETCHING: NO usar buildGridPage; armar un `PdfBlockPage` estilo INITIUM: tomar el bloque STRETCHING de la fuente determinista (reusar `findInitiumBlock`-style pero por rol STRETCHING; o el nivel canónico via findCanonicalBlock('STRETCHING', sessionsByLevel)), con `role: ROLE_LABELS['STRETCHING']`, `isStretching: true`, `simpleExercises: block.exercises.map(e => formatInitiumExercise(e, block.formatName))`, `formatName: formatNameWithParams(block.formatName, block.formatParams)`. (D160-04: lista simple compartida, no grid por nivel — el generador la hace idéntica en los 6 niveles.)
    - `return { dayName, week, blocks };`
    NO modificar `formatNameWithParams`. NO tocar la rama ROM ni la regular más allá de migrar el literal de labels al dict. Sin `any`. Nunca `console.log`.
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm exec vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `el-templo-admin/src/constants/roleLabels.ts` existe con COMBOS_I/COMBOS_II/TECNICA_I/TECNICA_II/STRETCHING (`grep -c "COMBOS_I\|TECNICA_I\|STRETCHING" ...` >= 3).
    - `pdf-types.ts` PdfBlockPage tiene `isStretching`.
    - `sessionsToPdfDay` detecta combos/técnica por rol y arma INITIUM + grids COMBOS/TECNICA + STRETCHING con `simpleExercises` e `isStretching:true` (`grep -c "isStretching\|COMBOS_\|TECNICA_" el-templo-admin/src/utils/pdf/session-data-transformer.ts` >= 3).
    - `git diff -- el-templo-admin/src/utils/pdf/session-data-transformer.ts | grep -c "formatNameWithParams(" ` no introduce cambios a la DEFINICIÓN de formatNameWithParams (solo llamadas).
    - `pnpm exec vue-tsc --noEmit` en 0 errores.
  </acceptance_criteria>
  <done>El transformer produce el PdfDaySession de combos/técnica con INITIUM + grids I/II + STRETCHING como lista simple; el dict de labels admin existe; vue-tsc verde.</done>
</task>

<task type="auto">
  <name>Task 2: Rama combos/técnica en buildDayContent + test de estructura (SEM-09)</name>
  <read_first>
    - el-templo-admin/src/utils/pdf/session-pdf-builder.ts (buildDayContent :1054-1102, buildInitiumPage :305, buildFullBlockPage :648)
    - el-templo-admin/src/utils/pdf/__tests__ (buscar un test existente de PDF como molde; si no hay, usar el patrón de vitest del admin)
    - el-templo-admin/src/utils/pdf/session-data-transformer.ts (salida de Task 1)
  </read_first>
  <behavior>
    - Test 1: `sessionsToPdfDay` sobre sesiones combos (6 niveles con INITIUM/COMBOS_I/COMBOS_II/STRETCHING) devuelve `blocks` con INITIUM, COMBOS I (levelBlocks presente), COMBOS II (levelBlocks), y STRETCHING con `isStretching===true` y `simpleExercises.length > 0` y `levelBlocks` ausente/vacío.
    - Test 2: `buildDayContent` sobre ese day devuelve `Content[]` no vacío que incluye una página de STRETCHING renderizada estilo INITIUM (lista simple), no un grid.
    - Test 3 (técnica): análogo con TECNICA_I/II.
    - Test 4 (regresión): un día regular (NUCLEUS/DEUTEROS) sigue produciendo el layout regular sin STRETCHING; un día ROM sigue con buildRomBlockPage.
    - Test 5 (D160-04): STRETCHING sale UNA sola vez (no una página por nivel).
  </behavior>
  <action>
    En `session-pdf-builder.ts` `buildDayContent` (:1054-1102): agregar la tercera rama. Detectar `const isCombosTecnica = day.blocks.some(b => b.role === ROLE_LABELS['COMBOS_I'] || b.role === ROLE_LABELS['COMBOS_II'] || b.role === ROLE_LABELS['TECNICA_I'] || b.role === ROLE_LABELS['TECNICA_II']);` — OJO: en el PdfBlockPage el `role` ya viene con el label largo (Task 1 lo setea con ROLE_LABELS). Alternativa más robusta: detectar `day.blocks.some(b => b.isStretching)` combinado con la presencia de un bloque combos/técnica; elegí el criterio que quede inequívoco y documentalo. Ubicar la rama entre `isRomDay` y el `else` regular.
    En la rama combos/técnica: `if (initium) content.push(...buildInitiumPage(initium));` luego por cada bloque que NO sea INITIUM ni STRETCHING → `content.push(...buildFullBlockPage(block))` (COMBOS/TECNICA I/II usan el grid full-page, igual que NUCLEUS); para el bloque `isStretching` → renderizarlo estilo INITIUM: reusar `buildInitiumPage(stretchingBlock)` (que consume `simpleExercises`) — verificá que `buildInitiumPage` no hardcodee el título "PYROS"; SÍ lo hace (:315 `text: 'PYROS'`). Por eso: NO reusar buildInitiumPage tal cual para STRETCHING (imprimiría "PYROS"). Crear un `buildStretchingPage(block)` mínimo derivado de `buildInitiumPage` que use `block.role` (="STRETCHING") como título en vez del literal "PYROS", con el mismo layout de lista simple. Mantener el resto idéntico. Cierre: `buildClosingPage` igual que las otras ramas.
    Test: crear `el-templo-admin/src/utils/pdf/__tests__/combos-tecnica-pdf.test.ts` cubriendo los 5 comportamientos (assert sobre la estructura de `sessionsToPdfDay` y sobre que `buildDayContent` devuelve Content no vacío; para STRETCHING assertear que aparece una sola página con la lista simple). Construir los `SessionDetail` de entrada a mano (fixtures mínimos con roles COMBOS_/TECNICA_/STRETCHING y ejercicios).
    Sin `any`. Nunca `console.log`.
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm exec vue-tsc --noEmit && pnpm vitest run src/utils/pdf/__tests__/combos-tecnica-pdf.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `buildDayContent` tiene una rama combos/técnica que renderiza INITIUM + COMBOS/TECNICA I/II + STRETCHING (lista simple) + cierre.
    - Existe `buildStretchingPage` (o equivalente) que NO imprime "PYROS" como título (`grep -c "PYROS" el-templo-admin/src/utils/pdf/session-pdf-builder.ts` no aumenta respecto de master en la función de STRETCHING).
    - `combos-tecnica-pdf.test.ts` cubre los 5 comportamientos y pasa.
    - Regresión: un test de PDF regular/ROM existente (si hay) sigue verde.
    - `pnpm exec vue-tsc --noEmit` en 0 errores.
  </acceptance_criteria>
  <done>El PDF de combos/técnica imprime los 4 bloques con STRETCHING como lista simple estilo INITIUM; regular/ROM sin regresión; vue-tsc + test verdes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| -------- | ----------- |
| API → admin (browser) → PDF | El PDF se arma en el browser del admin desde datos ya autorizados (SessionDetail); sin input no confiable nuevo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --------- | -------- | --------- | ----------- | --------------- |
| T-160-04 | Tampering | Roles/labels en el PDF | accept | Los roles vienen de la sesión aprobada del API; el transformer solo mapea a layout |
| T-160-05 | Denial of Service | PDF con datos malformados (STRETCHING sin ejercicios) | mitigate | La rama STRETCHING tolera lista vacía (simpleExercises: []) sin romper el render; test cubre el caso |
| T-160-SC | Tampering | npm/pip installs | accept | Esta fase NO instala ni actualiza dependencias |

</threat_model>

<verification>
- `cd el-templo-admin && pnpm exec vue-tsc --noEmit` verde (CI NO typechequea el admin → gate local obligatorio).
- `pnpm vitest run src/utils/pdf/__tests__/combos-tecnica-pdf.test.ts` verde en foreground.
- `formatNameWithParams` sin cambios de definición (las 3 copias espejo intactas).
- Sin migraciones.
</verification>

<success_criteria>
El PDF imprime los días combos y técnica con INITIUM + los dos bloques I/II en grid + STRETCHING como lista simple compartida estilo INITIUM (D160-04); el diccionario de labels del admin está centralizado; regular/ROM sin regresión; vue-tsc y el test verdes.
</success_criteria>

<output>
Create `.planning/phases/160-semana-nueva-frontend-admin-ui-horarios-app-pdf-y-tv/160-02-SUMMARY.md` when done. Documentar la ruta y exports de `constants/roleLabels.ts` (el plan 160-03 lo importa) y el criterio de detección de la rama combos/técnica en el builder.
</output>
</content>
