---
phase: 160-semana-nueva-frontend-admin-ui-horarios-app-pdf-y-tv
plan: 03
type: execute
wave: 2
depends_on: [160-02]
files_modified:
  - el-templo-admin/src/composables/useGenerateApi.ts
  - el-templo-admin/src/pages/GeneratePage.vue
  - el-templo-admin/src/types/session.ts
  - el-templo-admin/src/pages/SessionsPage.vue
  - el-templo-admin/src/components/sessions/EditableBlockCard.vue
autonomous: true
requirements: [SEM-07, SEM-08, SEM-11]
must_haves:
  truths:
    - "D160-01: el generador tiene un control de modo por día en el área de generación, con default constante NO persistido miércoles→Técnica, jueves→Combos, editable por corrida"
    - "El override viaja por el body dayModes de POST /admin/generate (NO por la tabla day_modes persistida — su enum queda regular/rom)"
    - "SessionsPage muestra un badge Combos/Técnica en los días combos/técnica (paralelo al badge ROM), usando los 6 niveles normales"
    - "SEM-08: EditableBlockCard rotula y colorea COMBOS I/II, TÉCNICA I/II y STRETCHING; el dropdown de formatos usa el fetch de formatos compatibles con el rol real"
    - "SEM-11: el editor consume el dict admin (constants/roleLabels.ts) en vez de su ROLE_DISPLAY_NAMES local"
  artifacts:
    - path: "el-templo-admin/src/composables/useGenerateApi.ts"
      provides: "generateWeek acepta dayModes?: Record<string,string> y lo manda en el body"
      contains: "dayModes"
    - path: "el-templo-admin/src/pages/GeneratePage.vue"
      provides: "control de modo por día (default mié→Técnica/jue→Combos, no persiste) + envío de dayModes"
      contains: "dayModes"
    - path: "el-templo-admin/src/pages/SessionsPage.vue"
      provides: "badge Combos/Técnica en el listado (6 niveles)"
      contains: "combos"
    - path: "el-templo-admin/src/components/sessions/EditableBlockCard.vue"
      provides: "labels+colores de COMBOS/TECNICA/STRETCHING desde el dict admin"
      contains: "STRETCHING"
  key_links:
    - from: "GeneratePage.handleGenerate/doGenerate"
      to: "useGenerateApi.generateWeek({...,dayModes})"
      via: "override per-corrida, no persiste en day_modes"
      pattern: "dayModes"
    - from: "EditableBlockCard displayRoleName/blockColor"
      to: "constants/roleLabels.ts ROLE_LABELS (creado en 160-02)"
      via: "import del dict centralizado"
      pattern: "roleLabels|ROLE_LABELS"
---

<objective>
Cablear el generador para que el profe elija combos/técnica por día (SEM-07/D160-01), agregar los badges del listado y las labels/colores del editor (SEM-07/SEM-08/SEM-11). El backend YA acepta el override (`dayModes` en el body de `/generate`, enum de 4 modos, 159-01/159-05) y YA restringe formatos por rol (block-validator, 159-01); lo que falta es el frontend: `useGenerateApi.generateWeek` no manda `dayModes`, `GeneratePage` no arma el control, `SessionsPage` no muestra badge combos/técnica y `EditableBlockCard` no conoce los roles nuevos.

Purpose: es el punto donde el profe selecciona el tipo de sesión por día. El default constante (mié→Técnica, jue→Combos) refleja el régimen observado, pero NO se persiste en `day_modes` (D-02: eso reabriría el modelo de "día fijo").

Output: control de modo + envío de dayModes + badges + editor; `vue-tsc` verde. SIN migraciones. SIN tocar el enum de `PUT /admin/sessions/day-modes`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

REGLA OPERATIVA: worktree `et-159`. `git add` por ruta explícita. Nunca `console.log` (usar `createLogger()`). Sin `any`. NO instalar dependencias. **CI NO typechequea el admin** → gate de tipos = `pnpm exec vue-tsc --noEmit` LOCAL en foreground (obligatorio). Tests del admin en foreground, timeout amplio, sin `run_in_background`/`pgrep`.

Depende de 160-02: `el-templo-admin/src/constants/roleLabels.ts` ya existe con COMBOS/TECNICA/STRETCHING. Importarlo, NO recrearlo.
</execution_context>

<context>
@.planning/phases/160-semana-nueva-frontend-admin-ui-horarios-app-pdf-y-tv/160-CONTEXT.md
@.planning/phases/160-semana-nueva-frontend-admin-ui-horarios-app-pdf-y-tv/160-02-SUMMARY.md

<interfaces>
<!-- Contratos vigentes en et-159. Usar directamente. -->

composables/useGenerateApi.ts:36-56 — generateWeek(options):
  options: { week: number; days?: string[]; levelGroups?: string[]; regenerate?: boolean }
  → api.post('/admin/generate', options, { timeout: 120_000 }). FALTA dayModes en el tipo de options.
  El body enum ["regular","rom","combos","tecnica"] YA lo acepta el schema del API (admin/schemas.ts, 159-01).

pages/GeneratePage.vue:
  :384-387 MODE_OPTIONS = [{Regular,regular},{ROM,rom}]  (control de la tabla day_modes persistida — NO tocar su enum)
  :100-114 <q-select :options="MODE_OPTIONS" @update="updateDayMode"> en la columna 'modo' del summary → escribe day_modes (persistente). NO agregar combos/tecnica acá.
  :440-463 generationScope ('week'|'day'|'day_level'), selectedDay, dayOptions, levelOptions
  :42-80 área "Alcance de generacion" (donde va el control nuevo de modo por día del override)
  :583-624 handleGenerate() → doGenerate()
  doGenerate(): arma `options` {week, regenerate, [days], [levelGroups]} y llama generateApi.generateWeek(options).
  DAY_OF_WEEK_MAP / dayOptions ya listan los días.

types/session.ts:15 — SessionSummary.sessionMode?: 'regular' | 'rom'  (AMPLIAR a +'combos'+'tecnica')

pages/SessionsPage.vue:
  :407-409 isDayGroupRom(dayGroup) → sessions.some(s => s.sessionMode === 'rom')
  :429-430 dayGroups: `isRom = daySessions.some(s=>s.sessionMode==='rom'); displayLevels = isRom ? ROM_DISPLAY_LEVELS : DISPLAY_LEVELS`
  :84 <q-badge v-if="isDayGroupRom(dayGroup)" color="info" label="ROM" /> (molde del badge)
  combos/tecnica usan los 6 DISPLAY_LEVELS normales → ya caen bien en el else; falta SOLO el badge.

components/sessions/EditableBlockCard.vue:
  :400-404 ROLE_DISPLAY_NAMES (Record) solo ROM zones → migrar al dict admin + combos/tecnica/stretching
  :406-409 isRomBlock / displayRoleName = ROLE_DISPLAY_NAMES[role] || role
  :436-444 blockColor (computed): rom_→blue-grey-7, initium→brown-5, nucleus→deep-orange-8, deuteros→brown-8, athlos/epikos→brown-6, default grey. FALTA combos/tecnica/stretching.
  El dropdown de formatos (compatibleFormats) hace fetch por rol → como block-validator (159-01) ya tiene COMBOS_I:["Combos"], TECNICA_*:["For Quality",...], STRETCHING:["Stretching"], el fetch devuelve los formatos correctos: VERIFICAR, no cablear de nuevo.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Control de modo por día en el generador + envío de dayModes (SEM-07, D160-01)</name>
  <read_first>
    - el-templo-admin/src/composables/useGenerateApi.ts (generateWeek :36-56)
    - el-templo-admin/src/pages/GeneratePage.vue (área de generación :42-80; scope/day :440-463; doGenerate; MODE_OPTIONS :384; updateDayMode :100-114)
    - .planning/phases/160-.../160-CONTEXT.md (D160-01: control de override separado de day_modes; default mié→Técnica/jue→Combos NO persiste)
  </read_first>
  <action>
    En `useGenerateApi.ts` `generateWeek`: agregar `dayModes?: Record<string, string>` al tipo de `options` y NO hace falta nada más (ya se manda `options` entero en el body; el schema del API acepta `dayModes`). Confirmar que el objeto `options` se pasa tal cual.
    En `GeneratePage.vue`: agregar estado reactivo del override por día — `const generateDayModes = ref<Record<string,string>>({ miercoles: 'tecnica', jueves: 'combos' })` (default CONSTANTE, D160-01; NO se lee ni se escribe en `day_modes`). Definir `const OVERRIDE_MODE_OPTIONS = [{label:'Regular',value:'regular'},{label:'ROM',value:'rom'},{label:'Combos',value:'combos'},{label:'Técnica',value:'tecnica'}]` (SEPARADO de `MODE_OPTIONS`, que sigue siendo Regular/ROM para la tabla persistida). En el área "Alcance de generacion" (:42-80), agregar un bloque visible cuando `generationScope !== 'day_level'` (o siempre en scope 'week'/'day') con un selector de modo por cada día en scope: para scope 'week', mostrar una fila por día (lunes..sabado) con un `q-select`/`q-btn-toggle` de `OVERRIDE_MODE_OPTIONS` ligado a `generateDayModes[day]`; para scope 'day', un solo selector para `selectedDay`. Texto de ayuda: "Modo por día (solo para esta generación, no se guarda)". Mantener el estilo Quasar existente.
    En `doGenerate()`: construir el `dayModes` a enviar SOLO con los días en scope y SOLO los que difieren de 'regular' (para no inflar el body): `const dayModes = Object.fromEntries(Object.entries(generateDayModes.value).filter(([d,m]) => daysInScope.includes(d) && m !== 'regular'))`. Si `dayModes` no está vacío, agregarlo a `options`. `daysInScope` = `options.days ?? ['lunes','martes','miercoles','jueves','viernes','sabado']`. NO tocar `updateDayMode`/`MODE_OPTIONS`/la columna 'modo' del summary (esos siguen escribiendo la tabla day_modes persistida — regular/rom).
    Sin `any`. Nunca `console.log`.
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm exec vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `useGenerateApi.ts` `generateWeek` acepta `dayModes` (`grep -c "dayModes" el-templo-admin/src/composables/useGenerateApi.ts` >= 1).
    - `GeneratePage.vue` tiene `generateDayModes` con default `{ miercoles:'tecnica', jueves:'combos' }` y un `OVERRIDE_MODE_OPTIONS` con los 4 modos (`grep -c "generateDayModes\|OVERRIDE_MODE_OPTIONS" ...` >= 2).
    - `doGenerate` arma y envía `dayModes` en `options` (`grep -c "dayModes" el-templo-admin/src/pages/GeneratePage.vue` >= 2).
    - `MODE_OPTIONS` (la tabla persistida) sigue solo Regular/ROM (`git diff -- el-templo-admin/src/pages/GeneratePage.vue` no agrega combos/tecnica a `MODE_OPTIONS` ni a `updateDayMode`).
    - `pnpm exec vue-tsc --noEmit` en 0 errores.
  </acceptance_criteria>
  <done>El generador tiene el control de modo por día con default no persistido y envía dayModes en el body; la tabla day_modes queda intacta; vue-tsc verde.</done>
</task>

<task type="auto">
  <name>Task 2: Badges del listado + labels/colores del editor (SEM-07, SEM-08, SEM-11)</name>
  <read_first>
    - el-templo-admin/src/types/session.ts (SessionSummary.sessionMode :15)
    - el-templo-admin/src/pages/SessionsPage.vue (isDayGroupRom :407, dayGroups :429, badge ROM :84)
    - el-templo-admin/src/components/sessions/EditableBlockCard.vue (ROLE_DISPLAY_NAMES :400, blockColor :436, compatibleFormats fetch)
    - el-templo-admin/src/constants/roleLabels.ts (creado en 160-02 — importar)
  </read_first>
  <action>
    En `types/session.ts` (:15): ampliar `SessionSummary.sessionMode` a `'regular' | 'rom' | 'combos' | 'tecnica'`.
    En `SessionsPage.vue`: agregar un helper `dayGroupModeBadge(dayGroup): { label:string; color:string } | null` — si alguna sesión es 'combos' → {label:'Combos', color:'deep-orange'}, 'tecnica' → {label:'Técnica', color:'purple'}, 'rom' → {label:'ROM', color:'info'} (o mantené el badge ROM existente y agregá dos badges nuevos con `v-if`). Reemplazar/complementar el `<q-badge v-if="isDayGroupRom(dayGroup)">` (:84 y su duplicado en la otra vista ~:204/:257) para mostrar también Combos/Técnica. combos/tecnica NO cambian `displayLevels` (siguen 6 niveles — el `isRom ? ROM : DISPLAY` ya los deja en DISPLAY). NO tocar la lógica de niveles.
    En `EditableBlockCard.vue`: reemplazar el `ROLE_DISPLAY_NAMES` local (:400-404) por el import de `ROLE_LABELS` desde `src/constants/roleLabels.ts` (dict admin de 160-02) — `displayRoleName = ROLE_LABELS[role] || role`. En `blockColor` (:436-444) agregar ramas: `role.includes('combos')` → un color de marca (p.ej. 'deep-orange-8' o el que use NUCLEUS; elegí uno consistente y documentá), `role.includes('tecnica')` → otro (p.ej. 'purple-8'), `role.includes('stretching')` → 'teal-7'. Mantener rom/initium/nucleus/deuteros/athlos/epikos como están.
    VERIFICAR (SEM-08, sin cablear de nuevo): el fetch de `compatibleFormats` por rol devuelve los formatos correctos para COMBOS_* (["Combos"]) y TECNICA_* (calidad) y STRETCHING (["Stretching"]) — el endpoint lee `FORMAT_COMPATIBILITY` de block-validator (159-01). Si el fetch usa el rol crudo, ya funciona; declarar el resultado de la verificación en el SUMMARY. NO modificar FormatParamsEditor (ya agrupa combos rounds-only per CONTEXT).
    Sin `any`. Nunca `console.log`.
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm exec vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `types/session.ts` SessionSummary.sessionMode incluye 'combos' y 'tecnica'.
    - `SessionsPage.vue` muestra badge Combos/Técnica (`grep -c "Combos\|Técnica\|Tecnica" el-templo-admin/src/pages/SessionsPage.vue` >= 2) sin cambiar displayLevels para esos modos.
    - `EditableBlockCard.vue` importa `ROLE_LABELS` del dict admin y ya NO define `ROLE_DISPLAY_NAMES` local (`grep -c "ROLE_DISPLAY_NAMES" el-templo-admin/src/components/sessions/EditableBlockCard.vue` == 0).
    - `blockColor` tiene ramas combos/tecnica/stretching (`grep -c "combos\|tecnica\|stretching" el-templo-admin/src/components/sessions/EditableBlockCard.vue` >= 3).
    - `pnpm exec vue-tsc --noEmit` en 0 errores.
  </acceptance_criteria>
  <done>El listado muestra badges combos/técnica; el editor rotula y colorea los roles nuevos desde el dict admin; el dropdown de formatos por rol se verificó; vue-tsc verde.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| -------- | ----------- |
| admin (browser) → POST /admin/generate | `dayModes` decide el session_mode; la validación (enum + additionalProperties:false) vive en el schema del API (159-01), no en el cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --------- | -------- | --------- | ----------- | --------------- |
| T-160-06 | Tampering | Persistir combos/tecnica en day_modes desde el UI | mitigate | El control de override es SEPARADO de MODE_OPTIONS/updateDayMode; el UI nunca escribe combos/tecnica en day_modes (D-02) |
| T-160-07 | Tampering | session_mode arbitrario vía dayModes | mitigate | El enum del body y additionalProperties:false (schema del API, 159-01) rechazan valores no reconocidos; el cliente solo ofrece los 4 modos |
| T-160-SC | Tampering | npm/pip installs | accept | Esta fase NO instala ni actualiza dependencias |

</threat_model>

<verification>
- `cd el-templo-admin && pnpm exec vue-tsc --noEmit` verde (CI NO typechequea el admin → gate local obligatorio).
- El control de override NO escribe en day_modes (revisión del diff: `updateDayMode`/`MODE_OPTIONS` sin combos/tecnica).
- Sin migraciones; sin rutas nuevas.
</verification>

<success_criteria>
El generador ofrece el override de modo por día (default no persistido mié→Técnica/jue→Combos) y lo envía en dayModes; el listado muestra badges combos/técnica; el editor rotula/colorea COMBOS/TECNICA/STRETCHING desde el dict admin y su dropdown de formatos por rol funciona; la tabla day_modes queda intacta; vue-tsc verde.
</success_criteria>

<output>
Create `.planning/phases/160-semana-nueva-frontend-admin-ui-horarios-app-pdf-y-tv/160-03-SUMMARY.md` when done. Documentar el resultado de la verificación del fetch de formatos compatibles por rol (SEM-08) y los colores elegidos para combos/tecnica/stretching.
</output>
</content>
