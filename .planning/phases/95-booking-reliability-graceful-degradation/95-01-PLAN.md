---
phase: 95-booking-reliability-graceful-degradation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/95-booking-reliability-graceful-degradation/95-AUDIT.md
  - el-templo-api/test/whatsapp/v5-3-3-booking.integration.test.ts
  - el-templo-bot/test/v5-3-3-booking-reliability.test.ts
autonomous: false
requirements:
  - BOOK-01
tags:
  - audit-first
  - tdd-fail-in-main
  - adversarial-checker
  - whatsapp-bot
  - booking

must_haves:
  truths:
    # —— Audit deliverable ——
    - "A Phase 95 audit document (`.planning/phases/95-booking-reliability-graceful-degradation/95-AUDIT.md`) exists with per-candidate verdicts (FIRES / DOES NOT FIRE / INCONCLUSIVE) written for ALL FIVE BUG-03 candidates: (i) LIKE-search ambiguity at `el-templo-bot/src/ai/tools.ts:455`, (ii) cross-branch result mixing at `tools.ts:285-302`, (iii) Sunday=0 vs Sunday=7 day-of-week confusion (model/prompt-side, `system-prompt.ts:223` SILENCIO rule region AND `tools.ts:25-33`/`:90` DAY_NAMES convention), (iv) LIMIT-6 truncation at `tools.ts:301`, (v) `booking_count` correlated subquery with today-filter at `tools.ts:267, 281`."
    - "`95-AUDIT.md` enumerates THREE explicit final-verdict branches: **Branch 1** (single candidate fires — names which of i/ii/iii/iv/v), **Branch 2** (a different single candidate fires — names which one — for the case the audit names a different single root cause than the initially-most-suspected one), **Branch 3** (compound: 2+ candidates fire simultaneously — explicitly enumerates which subset). The shape mirrors `93-AUDIT.md` Final Branch Verdict. Branch enumeration is exhaustive across the candidate space."
    - "`95-AUDIT.md` defines the fix scope per branch **upfront** so plan 95-02 does NOT re-litigate scope. For each branch (1, 2, 3-each-compound-shape) the audit names: which file(s) plan 95-02 modifies, which RED test(s) from this plan turn GREEN under that fix, and any DEGR-02 (SC#3) implications. If Branch 3 (compound) requires fixes in two distinct code surfaces, the audit states whether plan 95-02 implements both in one PR or splits into 95-02a/95-02b — Claude's Discretion item from CONTEXT.md D-08-related decision space (CONTEXT.md final 'Claude's Discretion' bullet)."
    # —— RED integration tests (SQL-discriminating candidates i, ii, iv, v) ——
    - "An integration test file `el-templo-api/test/whatsapp/v5-3-3-booking.integration.test.ts` exists. Runs against real `eltemplo_test` MySQL per `el-templo-bot/CLAUDE.md` integration-test convention. Each test in the file discriminates ONE candidate (or a named compound) by either: (a) calling `executeTool('check_schedule', ...)` / `executeTool('get_location', ...)` against seeded fixtures and asserting on the returned string shape that the candidate predicts versus the correct shape, OR (b) issuing the exact SELECT/JOIN that `checkSchedule`/`getLocation` issues, asserting on the row-shape the candidate predicts versus the correct row-shape."
    - "The integration test file covers ALL FOUR SQL-discriminating candidates (i, ii, iv, v) — each candidate has at least one dedicated `it()` block. Candidate (iii) Sunday=0/7 is OUT OF SCOPE for this file because it is model/prompt-side (no SQL touches the day-of-week-conversion-from-text discriminator) — it lives in the bot-side unit test below."
    - "EVERY new `it()` block in the integration test file FAILS against current `master` / current code (RED state). After plan 95-02 lands the audit-named fix, all RED tests MUST PASS without test modification. This is the TDD fail-in-main discipline locked by CONTEXT.md D-20."
    - "Integration test fixtures follow the per-test `beforeEach` truncate + inline seed convention from `el-templo-api/test/whatsapp/ai-tools.test.ts:45-100` (no shared fixture state). Seed shape is determined by the candidate being discriminated — e.g., for candidate (ii) cross-branch mixing, seed TWO branches each with a schedule on the same day/time so the LIMIT-6 result naturally interleaves; for candidate (iv) LIMIT-6 truncation, seed 7+ schedules across branches so the result-set boundary is exercised; for candidate (v) booking_count today-filter, seed bookings dated tomorrow and assert the count returned for a tomorrow-query matches actual tomorrow bookings, not zero-because-today-filter; for candidate (i) LIKE-search ambiguity, seed branches named e.g. 'Moreno' and 'Mar del Plata Moreno' (or other substring-overlap pair representative of real venue names) and assert disambiguation."
    # —— RED unit test (candidate iii, Sunday=0/7) ——
    - "A unit test file `el-templo-bot/test/v5-3-3-booking-reliability.test.ts` exists. Discriminates candidate (iii) Sunday=0 vs Sunday=7 day-of-week confusion at the model/prompt boundary."
    - "Unit test asserts at MINIMUM: (a) the rendered system prompt instructs the model on a single, unambiguous day-of-week encoding (e.g., grep the rendered prompt for the literal '1=lunes' AND '0=domingo' OR equivalent — assert presence/absence per the audit's verdict on what the prompt currently says vs what it SHOULD say); AND (b) when the audit names candidate (iii) as firing, an assertion that fails against current `master` exists. If candidate (iii) DOES NOT FIRE per the audit, the unit test still contains regression-protector assertions for the existing day-of-week encoding shape (these may PASS on master — they are forward-looking guards, not fail-in-main)."
    - "Unit test uses the existing vitest pattern in `el-templo-bot/test/v5-3-3-openai-latency.test.ts` (`describe`/`beforeEach`/`afterEach` from vitest; `vi.resetModules()` for env-isolated imports). NO real OpenAI call. If the unit test needs to mock the AI provider for a model-behavior-shaped assertion, it follows the `vi.doMock('../src/ai/provider')` pattern from `el-templo-bot/test/v5-3-3-handler-concurrency.test.ts` (capture `chatCalls`, assert `messages[0].content` — the rendered system prompt — contains/lacks the day-of-week directive)."
    - "If the audit verdict for candidate (iii) is DOES NOT FIRE (e.g., the model is reading `tools.ts:90` correctly and the live-test failure was elsewhere), this file still exists with at least one regression-protector `it()` block locking the current day-of-week directive shape against future regression."
    # —— Atomic commit + TDD discipline ——
    - "All deliverables (95-AUDIT.md + the integration test file + the bot-side unit test file) are committed in ONE atomic git commit with subject `audit(95-01): branch verdict for BUG-03 + RED tests`. CONTEXT.md D-20: 'All RED tests committed atomically with 95-AUDIT.md in the same git commit.' No multi-commit split for this plan."
    - "Before commit, `pnpm test` is run against current `master` for BOTH test surfaces — `cd el-templo-api && pnpm test --run test/whatsapp/v5-3-3-booking.integration.test.ts` MUST exit non-zero (≥1 failing test) AND `cd el-templo-bot && pnpm test --run test/v5-3-3-booking-reliability.test.ts` MUST exit non-zero (≥1 failing test) — unless the audit verdict for candidate (iii) is DOES NOT FIRE, in which case the bot-side unit test contains only regression-protectors and the test command exits 0 (this case is recorded in 95-AUDIT.md Final Branch Verdict explicitly)."
    - "Failure transcripts from the RED run are captured in 95-AUDIT.md's `## Appendix: RED test failure transcripts` section (mirrors Phase 93's Appendix B reservation), so plan 95-02 can verify the same tests transition fail → pass without ambiguity."
    # —— Cross-Phase Invariant byte-verification (read-only, this plan mutates none of the canonical block locations) ——
    - "The canonical Cross-Phase Invariant block at all SIX location-pairs hashes to `67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344`. The six pairs (per the orchestrator-locked verification spec for this plan) are: `.planning/phases/93-handler-concurrency/93-CONTEXT.md:84`, `.planning/phases/94-openai-latency-graceful-failure/94-CONTEXT.md:34`, `.planning/phases/95-booking-reliability-graceful-degradation/95-CONTEXT.md:37`, `.planning/ROADMAP.md:62`, `.planning/ROADMAP.md:93`, `.planning/MACRO-ROADMAP.md:99`. Plan 95-01 is investigative and mutates NONE of these files — the hash check is a drift sentry, not an editor."
    - "If ANY location reports DRIFT during the pre-commit verification step, the executor HALTS before committing the audit + RED tests and reports the drift to the user. The executor does NOT auto-resync the docs. This matches user-MEMORY rule `feedback_multi_doc_invariant_hash_check.md` (same formula/rule across N docs → byte-verification, paraphrase is drift waiting to happen)."
    # —— Anti-scope-creep guards ——
    - "NO production source files in `el-templo-bot/src/**/*.ts` or `el-templo-api/src/**/*.ts` are modified by this plan. Specifically (negative assertions verified before commit): `el-templo-bot/src/ai/tools.ts` UNCHANGED, `el-templo-bot/src/webhook/handler.ts` UNCHANGED, `el-templo-bot/src/ai/system-prompt.ts` UNCHANGED, `el-templo-bot/src/ai/openai.ts` UNCHANGED, `el-templo-bot/.env.example` UNCHANGED, `el-templo-bot/scripts/check-debounce-invariant.sh` UNCHANGED. The fix lives in plan 95-02; this plan is the audit + RED test scaffolding only."
    - "NO new files outside the three listed in `files_modified` are created. No new helper module, no `with-timeout.ts` (that is plan 95-02's deliverable per CONTEXT.md D-04/D-15/D-16), no new env vars, no new package dependencies."
    - "Plan 95-01's commit does NOT touch any of the five canonical-invariant markdown documents (`93-CONTEXT.md`, `94-CONTEXT.md`, `95-CONTEXT.md`, `ROADMAP.md`, `MACRO-ROADMAP.md`) across six location-pairs (`ROADMAP.md` appears twice at `:62` and `:93`). The sha256 drift check above confirms post-commit hashes match pre-commit."
    - "No changes to `el-templo-bot/test/v5-3-2-regression.test.ts` (RLOK suite — Phase 91/97 territory). No changes to `pb1-e1a-lead-rendered.snap.txt` or `POST_RLOK_04_BYTES` (Phase 96 territory per CONTEXT.md `<domain>` 'NOT in scope' bullet)."
    # —— Project-discipline guards (CLAUDE.md + el-templo-bot/CLAUDE.md) ——
    - "Zero new `console.log/warn/error` calls in the new test files — Pino-only discipline is for production source but new test files MUST NOT introduce `console.*`. The integration test file may use `request.log` if a Fastify instance is constructed (per Phase 93's integration test pattern); the unit test relies on `vi`-captured emissions, not `console`."
    - "Zero new `: any`, `<any[,>]`, `as any`, `any[]`, `Record<..., any>`, `Promise<any>`, `Array<any>` patterns in the new test files. Use `unknown` + type narrowing per `el-templo-bot/CLAUDE.md` Standards."
    - "`cd el-templo-bot && pnpm tsc --noEmit` exits 0 after the new test file is added (no new type errors). `cd el-templo-api && pnpm tsc --noEmit` exits 0 after the new integration test is added."
    # —— Plan 95-02/95-03 unblocking criterion ——
    - "After this plan commits, plan 95-02 has a SINGLE, UNAMBIGUOUS fix scope to consume from 95-AUDIT.md's Final Branch Verdict. Specifically: 95-02 reads the verdict, modifies exactly the file/line ranges the audit names, runs the RED integration test suite, observes GREEN. Plan 95-03 then wires the BUG-05 retry counter + escalation on top of 95-02's `withTimeout` helper. The Phase 95 plan ordering (95-01 → 95-02 → 95-03) is locked by CONTEXT.md D-04."
  artifacts:
    - path: ".planning/phases/95-booking-reliability-graceful-degradation/95-AUDIT.md"
      provides: "Per-candidate verdicts for the 5 BUG-03 candidates; Final Branch Verdict (Branch 1/2/3 with explicit named candidate(s) firing); per-branch fix-scope definition for plan 95-02; RED test failure transcripts appendix."
      contains: "Branch 1, Branch 2, Branch 3, FIRES, DOES NOT FIRE, Final Branch Verdict"
    - path: "el-templo-api/test/whatsapp/v5-3-3-booking.integration.test.ts"
      provides: "RED integration tests for SQL-discriminating BUG-03 candidates (i LIKE-search, ii cross-branch mix, iv LIMIT-6 truncation, v booking_count today-filter) against real eltemplo_test MySQL. Tests fail on master; pass after plan 95-02 fix."
      min_lines: 120
    - path: "el-templo-bot/test/v5-3-3-booking-reliability.test.ts"
      provides: "RED (or regression-protector if audit names DOES NOT FIRE) unit test for BUG-03 candidate (iii) Sunday=0/7 model/prompt-side discriminator. Mirrors the v5-3-3-openai-latency / v5-3-3-handler-concurrency test patterns."
      min_lines: 40
  key_links:
    - from: "el-templo-api/test/whatsapp/v5-3-3-booking.integration.test.ts"
      to: "el-templo-bot/src/ai/tools.ts (checkSchedule + getLocation)"
      via: "executeTool('check_schedule', ...) + executeTool('get_location', ...) + raw SQL discriminators against eltemplo_test"
      pattern: "executeTool\\(.+check_schedule|executeTool\\(.+get_location|FROM schedules|FROM branches"
    - from: "el-templo-bot/test/v5-3-3-booking-reliability.test.ts"
      to: "el-templo-bot/src/ai/system-prompt.ts AND el-templo-bot/src/ai/tools.ts (DAY_NAMES)"
      via: "renderedSystemPrompt grep / DAY_NAMES import / vi.doMock('../src/ai/provider') captured messages[0].content assertions"
      pattern: "DAY_NAMES|domingo|day_of_week|0=domingo"
    - from: ".planning/phases/95-booking-reliability-graceful-degradation/95-AUDIT.md"
      to: "plan 95-02 fix scope"
      via: "Final Branch Verdict section names the file:line range plan 95-02 modifies, and lists the RED tests plan 95-02 must turn GREEN"
      pattern: "Final Branch Verdict|Fix scope for plan 95-02"
    - from: ".planning/phases/95-booking-reliability-graceful-degradation/95-AUDIT.md"
      to: ".planning/v5.3.3-codebase-audit.md:185-260"
      via: "candidate enumeration draws from the codebase audit's pre-existing BUG-03 inventory"
      pattern: "candidate \\(i\\)|candidate \\(ii\\)|candidate \\(iii\\)|candidate \\(iv\\)|candidate \\(v\\)"
---

<objective>
**Audit-first** close of BUG-03 (BOOK-01) — investigative phase only. This plan does NOT fix BUG-03; it identifies which of FIVE plausible root-cause candidates fires (or which COMPOUND of candidates fires) and commits the RED tests that plan 95-02 will turn GREEN.

The five candidates are pre-enumerated in `.planning/v5.3.3-codebase-audit.md:185-260` and locked in CONTEXT.md D-01:

(i) LIKE-search ambiguity at `el-templo-bot/src/ai/tools.ts:455` (`WHERE name LIKE %branchName%` in `getLocation`).
(ii) Cross-branch result mixing at `tools.ts:285-302` (`check_schedule` without `branchId` filter joins across ALL active branches with `LIMIT 6`).
(iii) Sunday=0 vs Sunday=7 day-of-week confusion (model/prompt-side — `DAY_NAMES` in `tools.ts:25-33`/`:90` uses MySQL DAYOFWEEK convention `0=domingo`, but the model may emit Sunday=7 if `system-prompt.ts:223` SILENCIO rule region (or surrounding day-of-week framing) is ambiguous about day numbering convention).
(iv) LIMIT-6 truncation at `tools.ts:301` (`ORDER BY s.day_of_week, s.start_time LIMIT 6` may hide the user's actual desired slot if it sorts after the 6th row).
(v) `booking_count` correlated subquery with today-filter at `tools.ts:267, 281` (`bk.booking_date = ${today}` returns 0 bookings for any tomorrow-query, so "cupos disponibles" appears artificially full / available).

Static analysis (the codebase audit, the post-v5.3.2 live transcripts) can name the candidates but cannot deterministically pick which one fires — per CONTEXT.md D-01: "Static analysis can name the candidates but cannot pick one — Phase 94's 'trust the audit' pattern is insufficient here because the audit explicitly says 'Reproduction would resolve which of these is firing. Without it, BOOK-01 risks fixing the wrong thing.'" (`v5.3.3-codebase-audit.md:257`).

Resolution path (CONTEXT.md D-02): write 95-AUDIT.md in the Phase 93 `93-AUDIT.md` shape — per-candidate verdicts, Final Branch Verdict (Branch 1/2/3 with explicit named firing candidate(s)), per-branch fix-scope definition for plan 95-02 UPFRONT (so 95-02 does not re-litigate scope). The RED tests are the discriminator: each test exercises ONE candidate's predicted SQL behavior or prompt-shape behavior against current code; the candidate that fires is the one whose RED test ACTUALLY fails on master.

**Plan-checker mode: ADVERSARIAL** (CONTEXT.md D-22). 95-01 has genuine investigative branching (Branch 1/2/3 outcome space — five candidates, compound subsets) — adversarial framing justified, mirroring Phase 93. Structural integrity check (XML/markup tag balance) MUST be in framing per the locked feedback rule.

**TDD fail-in-main discipline** (CONTEXT.md D-20): Every RED test in the integration + unit test files is authored against current `master` AND observed FAILING before the commit lands. Plan 95-02 turns them GREEN without test modification.

**Atomic commit cadence** (CONTEXT.md D-21, single-commit variant for the audit plan): ONE commit, subject `audit(95-01): branch verdict for BUG-03 + RED tests`, body referencing `v5.3.3-codebase-audit.md:185-260` and the 5 candidates. SUMMARY.md is owned by plans 95-02/95-03 collectively — 95-01's audit doc IS the artifact for this plan's read-out.

**Stop point** (downstream consumer contract): After plan-checker passes and this PLAN.md commits, STOP. Do not execute. The user reviews the plan and authorizes execution separately. Once executed, the user reviews 95-AUDIT.md's Final Branch Verdict before authorizing plan 95-02 spawning.

Purpose: enable plan 95-02 to fix BUG-03 at the correct code surface (not the wrong one) by deterministic empirical discrimination via RED tests rather than guesswork. Bind audit output to TDD discipline so the audit cannot be "soft" — its verdict is empirically falsifiable by `pnpm test`.

Output: `95-AUDIT.md` (the audit + branch verdict + RED test failure transcripts), `el-templo-api/test/whatsapp/v5-3-3-booking.integration.test.ts` (RED integration tests for candidates i, ii, iv, v), `el-templo-bot/test/v5-3-3-booking-reliability.test.ts` (RED unit test for candidate iii, or regression-protector if iii does not fire), all in ONE atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/95-booking-reliability-graceful-degradation/95-CONTEXT.md
@.planning/v5.3.3-codebase-audit.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MACRO-ROADMAP.md
@CLAUDE.md
@el-templo-bot/CLAUDE.md

# Phase 93 audit + plan — the deliverable-shape template this plan mirrors

@.planning/phases/93-handler-concurrency/93-AUDIT.md
@.planning/phases/93-handler-concurrency/93-01-PLAN.md

# Phase 94-02 — canonical hash-check verification recipe this plan reuses verbatim

@.planning/phases/94-openai-latency-graceful-failure/94-02-PLAN.md

# Code under audit (read-only — this plan modifies none of these)

@el-templo-bot/src/ai/tools.ts
@el-templo-bot/src/ai/system-prompt.ts
@el-templo-bot/src/webhook/handler.ts

# Test pattern anchors (read-only — new test files mirror these conventions)

@el-templo-api/test/whatsapp/ai-tools.test.ts
@el-templo-api/test/whatsapp/v5-3-3-handler-concurrency.integration.test.ts
@el-templo-api/test/helpers.ts
@el-templo-bot/test/v5-3-3-handler-concurrency.test.ts
@el-templo-bot/test/v5-3-3-openai-latency.test.ts

<candidate_index>

<!-- The 5 BUG-03 candidates locked by CONTEXT.md D-01 and indexed against actual code lines. -->
<!-- This block is the executor's authoritative pointer set — no codebase exploration needed to find candidates. -->

(i) `el-templo-bot/src/ai/tools.ts:446-484` (`getLocation`), specifically `:455`:
`WHERE is_active = true AND name LIKE ${`%${branchName}%`} ORDER BY name`
Discriminator: seed branches whose names are substrings of each other (e.g., 'Moreno' vs 'Mar del Plata Moreno' — or any pair representative of real venue-name substring overlap). Invoke `getLocation` with the SHORTER name. Candidate (i) FIRES iff the LIKE returns BOTH rows, leaving the model to disambiguate from a multi-row response — and the live-test BUG-03 transcript shape ("se confundió entre Constitución y Moreno") matches that confusion pattern.

(ii) `el-templo-bot/src/ai/tools.ts:258-331` (`checkSchedule`), specifically `:285-302` (the SELECT/JOIN region without a forced `branchId` filter) and `:301` (`LIMIT 6`):
When the model calls `check_schedule` without a `branchId`, the result interleaves schedules from up to 5 venues. The `displayRows = rows.slice(0, 5)` at `:310` may pick the "wrong" subset for the user's mentioned venue.
Discriminator: seed schedules across TWO branches at the same `day_of_week + start_time`. Invoke `executeTool('check_schedule', {dayOfWeek: 1})` (no branchId). Assert the result string contains rows from BOTH branches — if so, candidate (ii) FIRES (the model is fed cross-venue data without disambiguation hints).

(iii) `el-templo-bot/src/ai/tools.ts:25-33` (`DAY_NAMES`) AND `tools.ts:88-91` (the `check_schedule` tool definition's parameter description: "1=lunes, 2=martes, ..., 6=sábado, 0=domingo"). Convention is MySQL DAYOFWEEK-style (Sunday=0). The model may instead emit Sunday=7 (ISO-8601 / `Date.getDay()` JS convention is Sunday=0, but ISO is 1-7 Mon-Sun; the model may pick either).
Discriminator: render the system prompt (importing `system-prompt.ts`) and grep for explicit day-of-week directive presence/absence. If the prompt does NOT bind the model to a specific encoding for "domingo", candidate (iii) FIRES — the parameter description at `tools.ts:90` is the ONLY model-facing day-of-week binding; assert this binding is present or call out its absence.

(iv) `el-templo-bot/src/ai/tools.ts:301`:
`ORDER BY s.day_of_week, s.start_time LIMIT 6`
Discriminator: seed 7+ schedules whose `(day_of_week, start_time)` ordering pushes the user's desired slot to position 7+. Invoke `check_schedule` and assert the user's slot is ABSENT from the result. Candidate (iv) FIRES iff the truncation hides a slot that genuinely exists.

(v) `el-templo-bot/src/ai/tools.ts:267, 281`:
`const today = new Date().toISOString().slice(0, 10);`
`WHERE bk.schedule_id = s.id AND bk.booking_date = ${today} AND bk.status != 'cancelado'`
Discriminator: seed a schedule with bookings dated TOMORROW (not today). Invoke `check_schedule` for that schedule. The `booking_count` returned is 0 (because today-filter excludes tomorrow's bookings), so "cupos disponibles" reports max_capacity instead of (max_capacity - actual_count). Candidate (v) FIRES iff the count returned for a tomorrow-relevant query disagrees with the actual count of bookings for the relevant date.
</candidate_index>

<cross_phase_invariant>
**CANONICAL BLOCK — must remain byte-identical across SIX location-pairs (per the orchestrator-locked verification spec for this plan):**

```
.planning/phases/93-handler-concurrency/93-CONTEXT.md:84
.planning/phases/94-openai-latency-graceful-failure/94-CONTEXT.md:34
.planning/phases/95-booking-reliability-graceful-degradation/95-CONTEXT.md:37
.planning/ROADMAP.md:62
.planning/ROADMAP.md:93
.planning/MACRO-ROADMAP.md:99
```

Each pair was byte-verified at plan-time (2026-05-18) and all SIX hash to:

```
67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344
```

**Note on the 6-pair framing:** The 95-CONTEXT.md D-23 wording says "FIVE locations" but is computed inclusively (95-CONTEXT counted, ROADMAP counted once). Phase 94's verified set is also "5 locations" but excludes 95-CONTEXT and counts ROADMAP twice. The orchestrator locked the 6-pair UNION (the literal list above) as plan 95-01's verification spec because it is the only framing that catches drift everywhere the block lives today. Encode this verbatim in the verification step below — do NOT paraphrase "5 locations" (per user-MEMORY rule `feedback_multi_doc_invariant_hash_check.md`).

**95-01 mutates NONE of these locations.** This plan is investigative — it adds new files only (`95-AUDIT.md`, two test files). The hash check is a drift SENTRY (catches accidental collateral edits during the audit-and-test-authoring session), not an editor. If ANY location drifts during the executor's session, the executor halts BEFORE commit and reports the drift to the user — does NOT auto-resync.
</cross_phase_invariant>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 95-AUDIT.md (per-candidate verdicts, Final Branch Verdict, per-branch fix scope) AND author + commit the RED tests atomically</name>
  <files>
    .planning/phases/95-booking-reliability-graceful-degradation/95-AUDIT.md,
    el-templo-api/test/whatsapp/v5-3-3-booking.integration.test.ts,
    el-templo-bot/test/v5-3-3-booking-reliability.test.ts
  </files>
  <read_first>
    - `.planning/v5.3.3-codebase-audit.md` lines 185-260 (the 5 BUG-03 candidates — Phase 95 section). Authoritative candidate enumeration.
    - `.planning/phases/95-booking-reliability-graceful-degradation/95-CONTEXT.md` full file (23 locked decisions; this plan implements D-01..D-04 + D-20..D-23 directly).
    - `.planning/phases/93-handler-concurrency/93-AUDIT.md` full file. **This is the deliverable-shape template.** Per-Check Verdicts, Final Branch Verdict, Pipeline Map, Implementation Pointers, Appendix sections — mirror this shape for 95-AUDIT.md.
    - `.planning/phases/94-openai-latency-graceful-failure/94-02-PLAN.md` lines 305-374 and 491-515 (the sha256 extract_block + drift-check recipe — reuse VERBATIM).
    - `el-templo-bot/src/ai/tools.ts` lines 1-100 (DAY_NAMES + branch lookup tables), 200-331 (executeTool + checkSchedule), 440-510 (getLocation + formatBranchLocations). The 5 candidates' code surfaces.
    - `el-templo-bot/src/ai/system-prompt.ts` lines 215-225 (the model rule region; specifically `:223` SILENCIO rule context — for candidate iii's discriminator).
    - `el-templo-api/test/whatsapp/ai-tools.test.ts` lines 1-150. The closest analog for the new integration test: beforeAll DB pool, beforeEach truncate + inline seed, direct `executeTool('check_schedule', {}, db)` invocation, string-output assertions. Reuse this convention.
    - `el-templo-api/test/whatsapp/v5-3-3-handler-concurrency.integration.test.ts` lines 1-200 (alternative integration pattern — heavier mocking, full webhook pipeline; only needed if a candidate requires whole-handler exercise. Most candidates can be discriminated at the `executeTool` boundary alone).
    - `el-templo-api/test/helpers.ts` full file (~80 lines). `createTestApp()` and the eltemplo_test wiring — only needed if the integration test exercises the Fastify webhook path. For pure `executeTool` discrimination, the direct-pool pattern from `ai-tools.test.ts` is simpler.
    - `el-templo-bot/test/v5-3-3-openai-latency.test.ts` lines 1-120 (vitest describe/beforeEach/afterEach pattern + `vi.resetModules()` env isolation). Closest analog for the bot-side unit test on candidate (iii).
    - `el-templo-bot/test/v5-3-3-handler-concurrency.test.ts` lines 1-100 (vi.doMock pattern for `../src/ai/provider` if candidate iii needs to capture `chatCalls[0].messages[0].content` for prompt-grep assertions).
    - `./CLAUDE.md` AND `./el-templo-bot/CLAUDE.md` (full files — already loaded at conversation start, but re-read for the discipline rules: integration tests against real `eltemplo_test` MySQL, no `any` types, no `console.log`, Pino logger on the API side).
  </read_first>
  <behavior>
    This is an audit + RED-test authoring task with a SINGLE atomic commit. No production source code is modified. Three files are CREATED:

    **File 1 — `95-AUDIT.md`** (target shape mirrors `93-AUDIT.md`):

    Section A — Header + Baseline Captures. Date (2026-05-18 or session date), Auditor identification (GSD plan executor, Phase 95 Plan 95-01). Read-only audit notice. Baseline captures: `console.*` count in `el-templo-bot/src/**/*.ts` AND `el-templo-api/src/**/*.ts` AND `any`-type count (use the same type-syntax-precise grep from `93-AUDIT.md:24` — matches `: any|<any[,>]|<any\s|as\s+any\b|any\[\]|Record<[^>]*,\s*any\s*>|Array<any>|Promise<any>`). Record as named integers — `console_count_baseline_bot`, `console_count_baseline_api`, `any_count_baseline_bot`, `any_count_baseline_api`. These are the comparison baselines for plan 95-02 and 95-03's verifications.

    Section B — Per-Candidate Verdicts. ONE subsection per candidate (i, ii, iii, iv, v). Each subsection: code surface (file:line), discriminator description (the SQL or prompt assertion the RED test issues), VERDICT (FIRES / DOES NOT FIRE / INCONCLUSIVE), reasoning with concrete numeric or string evidence from the code + the RED test's actual fail/pass shape on master. Candidates that DOES NOT FIRE still get a documented reasoning paragraph (don't skip — Phase 93's audit explicitly recorded DOES NOT FIRE verdicts).

    Section C — Final Branch Verdict. THREE branches (mirrors Phase 93's Final Branch Verdict shape, expanded to 3 outcomes per CONTEXT.md D-02):
      - **Branch 1** (single candidate fires): names exactly which of i/ii/iii/iv/v fires. States: "All RED tests for candidate X fail on master; all RED tests for the other 4 candidates pass on master OR contain regression-protector-only assertions." Fix scope for plan 95-02 named UPFRONT — exact file(s) and line range(s) plan 95-02 modifies, plus which RED test(s) plan 95-02 must turn GREEN.
      - **Branch 2** (a DIFFERENT single candidate fires than initial suspicion): same shape as Branch 1 but accommodates the case where the audit's empirical evidence overrules the "most likely" candidate suggested by static analysis. Use this shape if the firing candidate is unexpected (e.g., audit math suggested iv, RED test discriminator names ii instead).
      - **Branch 3** (compound — 2+ candidates fire simultaneously): explicitly enumerates the firing subset (e.g., "Branch 3-{i,v}: candidate (i) LIKE-search ambiguity + candidate (v) booking_count today-filter both fire"). For each compound shape encountered, fix scope for plan 95-02 explicitly addresses BOTH firing candidates. State whether plan 95-02 ships ONE PR or splits into 95-02a/95-02b (CONTEXT.md final 'Claude's Discretion' bullet — Claude's Discretion answer for THIS audit). Justification recorded.

    Sections per Branch ALSO include: per-branch DEGR-02 (SC#3) implications. If a Branch's fix touches `system-prompt.ts` (candidate iii or any candidate whose fix is prompt-side), the audit notes that plan 95-02 then MUST coordinate with Phase 96 on snapshot regen — per CONTEXT.md `<domain>` "NOT in scope" rule that 95 does not regen the snapshot. If the fix is purely SQL-side (candidates i/ii/iv/v), the audit explicitly notes "system-prompt.ts unchanged — Phase 96 snapshot ownership preserved".

    Section D — Pipeline Map (mirrors `93-AUDIT.md` Pipeline Map). The inbound user message → AI reply path for a booking-shaped inbound. File:line markers reference actual code:
      1. handler.ts:323 outer try/catch entry
      2. handler.ts:589-642 tool-loop region
      3. tools.ts:220-243 executeTool dispatch
      4. tools.ts:228 → checkSchedule branch (candidates ii, iv, v surfaces)
      5. tools.ts:232 → getLocation branch (candidate i surface)
      6. tools.ts:236 → bookClass branch (downstream consumer; not a BUG-03 candidate but listed for completeness)
      7. tools.ts:269-302 checkSchedule SELECT/JOIN region (candidates ii, iv, v)
      8. tools.ts:301 LIMIT 6 (candidate iv specifically)
      9. tools.ts:267, 281 today-filter (candidate v specifically)
      10. tools.ts:455 LIKE-search (candidate i specifically)
      11. tools.ts:90 day-of-week parameter description (candidate iii surface — model-facing)
      12. system-prompt.ts:223 SILENCIO rule region (candidate iii context, NOT the binding itself)
    State which steps the audit reads and which it leaves to the RED tests as runtime discriminators.

    Section E — Implementation Pointers for Plan 95-02. Per branch (1, 2, 3-each-compound-shape), exact file:line ranges plan 95-02 modifies. For candidate (i) LIKE-search: state the canonical fix shape (e.g., exact-match-with-fallback, or branch_code lookup, or normalized-name index — Claude's Discretion in plan 95-02; the audit recommends ONE based on the codebase pattern but leaves final choice to 95-02's planner). Same depth for ii, iii, iv, v.

    Section F — Appendix: RED test failure transcripts. Reserved section (mirrors `93-AUDIT.md` Appendix B reservation). Executor APPENDS the actual `pnpm test` output for each RED test from the master-baseline run, capturing the failure message + assertion-actual vs assertion-expected lines. This is the empirical evidence backing the Final Branch Verdict.

    **File 2 — `el-templo-api/test/whatsapp/v5-3-3-booking.integration.test.ts`** (RED integration tests):

    Test file follows the `ai-tools.test.ts:1-100` pattern (direct mysql2 pool + drizzle + beforeEach truncate + inline seed + direct `executeTool` invocation). NOT the `v5-3-3-handler-concurrency.integration.test.ts` heavy-mock pattern — that pattern's Fastify-inject layer is unnecessary for `executeTool`-boundary discrimination.

    Top-of-file imports (mirror `ai-tools.test.ts:1-12` + `el-templo-bot/CLAUDE.md` standards):
      - `vitest`: `describe`, `it`, `expect`, `beforeAll`, `afterAll`, `beforeEach` (NOT vi — no mocking needed for SQL discrimination).
      - `drizzle-orm/mysql2`: `drizzle`.
      - `mysql2/promise`.
      - Local: `executeTool` from `'../../../el-templo-bot/src/ai/tools'`. Type-only: `* as schema` from `'../../src/db/schema/index'`.
      - NO `vi.mock` calls. NO `console.*`. Use the existing test-pool pattern from `ai-tools.test.ts`.

    beforeAll: create pool against `eltemplo_test` (use the same env-var fallback pattern as `ai-tools.test.ts:30-36`). beforeEach: truncate `bookings`, `whatsapp_messages`, `whatsapp_conversations`, `schedules`, `subscriptions`, `subscription_plans WHERE name LIKE 'Test%'`, `users WHERE email LIKE 'test-book-%'`, `activities WHERE name LIKE 'Test%'`, `branches WHERE code LIKE 'TST%'`. Inline-seed per-test fixtures (NOT a shared fixture).

    Describe blocks — one per SQL-discriminating candidate (4 describes for i, ii, iv, v; candidate iii lives in the unit-test file):

    `describe("BUG-03 candidate (i) — LIKE-search ambiguity at tools.ts:455", () => { ... })`
      - `beforeEach`: seed two branches with substring-overlapping names. Concrete example (executor MUST adapt to actual venue-naming patterns — read `el-templo-bot/src/ai/tools.ts:39-45` BRANCH_ADDRESSES + the `branches` table seed pattern in `ai-tools.test.ts`): seed branches `('Moreno', 'TSTB1', ..., is_active=true)` AND `('Mar del Plata Moreno', 'TSTB2', ..., is_active=true)` so that `WHERE name LIKE '%moreno%'` matches BOTH.
      - At least one `it()` block: `it("returns both branches when name is a substring of multiple — RED demonstrates ambiguity", async () => { const result = await executeTool('get_location', { branchName: 'moreno' }, db); expect(result).toMatch(/Moreno/i); expect(result).toMatch(/Mar del Plata Moreno/i); })`. **This passes today but the RED assertion is the FOLLOW-UP `it()`**: `it("RED: returns exactly one disambiguated branch for substring-match input (FAILS on master)", async () => { const result = await executeTool('get_location', { branchName: 'moreno' }, db); /* Audit-named expected fix: getLocation disambiguates via exact-match first, fallback to LIKE. The RED expectation is: result contains EXACTLY ONE branch name. */ const moreno_count = (result.match(/^- \*/gm) ?? []).length; expect(moreno_count).toBe(1); })`. The exact assertion shape is determined by the audit's verdict on the canonical fix; if the audit determines candidate (i) DOES NOT FIRE, this RED `it()` is omitted or downgraded to regression-protector (passes on master, locks current behavior).

    `describe("BUG-03 candidate (ii) — Cross-branch result mixing at tools.ts:285-302", () => { ... })`
      - `beforeEach`: seed TWO branches each with the same day_of_week=1 schedule at the same start_time '08:00' (different activities or same activity). E.g., branch 'Test Alem' + activity 'Test CrossFit' day_of_week=1 08:00-09:00; AND branch 'Test Constitucion' + activity 'Test CrossFit' day_of_week=1 08:00-09:00.
      - `it("RED: check_schedule without branchId returns rows from MULTIPLE branches without disambiguation hint (FAILS on master)", async () => { const result = await executeTool('check_schedule', { dayOfWeek: 1 }, db); /* Audit-named expected fix: either require branchId OR include a clear venue-disambiguation directive in the result string. RED expectation: result string MUST NOT contain both branch names simultaneously without an explicit "from multiple venues" notice. */ const containsAlem = result.includes('Test Alem'); const containsConstitucion = result.includes('Test Constitucion'); /* RED assertion shape options — choose ONE based on audit's canonical-fix choice: (a) audit named "force branchId" → expect(result).toContain('necesito que me digas la sede'); OR (b) audit named "explicit venue header" → expect(result).toMatch(/En .+ y en .+:/); OR (c) some other shape. */ /* Default RED shape if no canonical fix is chosen: */ expect([containsAlem, containsConstitucion]).not.toEqual([true, true]); })`. The exact assertion is whatever the audit's Section E names as the canonical fix shape for candidate (ii); the test asserts the POST-FIX shape so it FAILS on master.

    `describe("BUG-03 candidate (iv) — LIMIT-6 truncation at tools.ts:301", () => { ... })`
      - `beforeEach`: seed 7+ schedules whose `(day_of_week, start_time)` ordering pushes a known "target" schedule (e.g., 'Test Yoga' at day_of_week=6 21:00) to position 7+ in the ORDER BY. Concretely: 6 schedules at day_of_week=1 spanning 08:00..13:00 (positions 1-6) and 1 schedule at day_of_week=6 21:00 (position 7+).
      - `it("RED: check_schedule with no filters hides the 7th+ schedule (FAILS on master)", async () => { const result = await executeTool('check_schedule', {}, db); /* Audit-named expected fix: either bump LIMIT, OR add explicit "hay X clases más" with a real count derived from a COUNT(*) sub-query, OR paginate. RED expectation: result string mentions the target schedule OR mentions the real total count. */ expect(result).toMatch(/Test Yoga|Sábado 21:00|hay .+ clases más/i); })`. Adapt the shape to whatever Section E names as the canonical fix.

    `describe("BUG-03 candidate (v) — booking_count today-filter at tools.ts:267, 281", () => { ... })`
      - `beforeEach`: seed a schedule + a user + a booking dated TOMORROW (NOT today) for that schedule with `status='confirmed'`. Branch max_capacity 10. So actual count for tomorrow is 1; today's count is 0.
      - `it("RED: check_schedule returns max-capacity cupos despite tomorrow having existing bookings (FAILS on master)", async () => { const result = await executeTool('check_schedule', {}, db); /* Audit-named expected fix: booking_count is parameterized by the query date (the user's requested day), not hardcoded to today. RED expectation: 'spots remaining' reflects tomorrow's actual bookings (9 of 10), not today's zero. */ expect(result).toMatch(/9 cupos disponibles|9 lugares/); })`. Adapt assertion shape to actual `formatBranchLocations` / `checkSchedule` output format (look at `tools.ts:313-330` for the actual line format).

    **CRITICAL — executor adaptation contract:** The exact assertion text in each RED `it()` is determined AT EXECUTION TIME by reading the actual `formatBranchLocations` / `checkSchedule` / `executeTool` output strings on master FIRST (capture them in scratch space), then writing the post-fix assertion such that master-output ≠ assertion. The `expect(...).toBe`/`expect(...).toMatch` is the discriminator — the failure message must clearly indicate "candidate X fires" so plan 95-02's executor can run the same test post-fix and see GREEN.

    **Pre-commit RED verification gate**: After writing all 4 candidate-describe blocks, run from the el-templo-api directory:
      `cd el-templo-api && pnpm test --run test/whatsapp/v5-3-3-booking.integration.test.ts 2>&1 | tee /tmp/red-output-95-01-integration.txt`
    Expected: exit code non-zero. The audit's Final Branch Verdict identifies WHICH `it()` blocks fail (those name the firing candidate(s)); other `it()` blocks may PASS on master (those are the candidates that DO NOT FIRE — their `it()` blocks are downgraded to regression-protector shape OR explicitly marked `.skip` with audit-rationale comment). Capture the actual fail/pass shape into 95-AUDIT.md Section F (Appendix).

    **File 3 — `el-templo-bot/test/v5-3-3-booking-reliability.test.ts`** (RED unit test for candidate iii):

    Test file follows the `v5-3-3-openai-latency.test.ts:1-120` pattern (describe / beforeEach `vi.resetModules()` / afterEach env restore). Imports: `vitest` (`describe`, `it`, `expect`, `vi`, `beforeEach`, `afterEach`).

    `describe("BUG-03 candidate (iii) — Sunday=0 vs Sunday=7 day-of-week confusion", () => { ... })`

    Candidate (iii)'s discriminator is model/prompt-side. Two assertion strategies (executor picks ONE based on the audit's verdict shape):

    Strategy A — direct prompt grep (preferred if the audit's canonical fix is a prompt rule):
      - `it("RED: system prompt does NOT bind the model to a single day-of-week encoding (FAILS on master if audit verdict is FIRES)", async () => { const { renderSystemPrompt } = await import('../src/ai/system-prompt'); /* renderSystemPrompt signature: pass a minimal context object — look at existing test calls in v5-3-2-regression.test.ts or system-prompt-playbook.test.ts for the canonical call shape. */ const prompt = renderSystemPrompt(/* minimal context per existing test pattern */); /* RED: audit named "prompt must contain explicit Sunday=0 binding". If the binding is absent, test passes on the FIXED version, fails on master. */ expect(prompt).toMatch(/0\s*=\s*domingo|domingo\s*=\s*0/i); })`. The exact regex matches whatever Section E of 95-AUDIT.md names as the canonical fix's prompt token.

    Strategy B — DAY_NAMES + tool-description alignment (if audit's canonical fix is data-side):
      - `it("RED: tool definition's dayOfWeek parameter description is not in sync with DAY_NAMES (FAILS on master if drift exists)", async () => { const { BOT_TOOLS } = await import('../src/ai/tools'); const checkScheduleTool = BOT_TOOLS.find(t => t.name === 'check_schedule'); /* RED expectation matches whatever Section E names as canonical alignment. */ })`.

    If the audit verdict for candidate (iii) is DOES NOT FIRE, this file still exists and contains AT LEAST ONE regression-protector `it()` block that locks the CURRENT day-of-week encoding shape (e.g., asserts `DAY_NAMES[0] === 'Domingo'` and that `BOT_TOOLS.find(t => t.name === 'check_schedule').parameters.properties.dayOfWeek.description` contains the string `'0=domingo'`). These tests PASS on master and are forward-looking — they detect future regression where someone changes one without the other. CONTEXT.md `<domain>` D-01 wording explicitly contemplates this — the candidate may not fire and the test still ships as a regression-protector.

    **Pre-commit RED verification gate (unit test)**: After writing the unit test, run from the el-templo-bot directory:
      `cd el-templo-bot && pnpm test --run test/v5-3-3-booking-reliability.test.ts 2>&1 | tee /tmp/red-output-95-01-unit.txt`
    Expected behavior: if audit verdict for candidate (iii) is FIRES, exit code is non-zero (the RED `it()` for candidate iii fails on master); if DOES NOT FIRE, exit code is 0 (the file contains only regression-protectors) — and that fact is recorded in 95-AUDIT.md's Final Branch Verdict.

    **Pre-commit sha256 invariant drift sentry** (reuse 94-02-PLAN.md:305-326 recipe verbatim, expanded to 6 location-pairs):

      ```bash
      cd /Users/bores/el-templo
      extract_block() { awk -v s="$2" 'NR>=s {print; if (/Minimum TTL =/) exit}' "$1"; }
      expected="67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344"
      fail=0
      for spec in \
        ".planning/phases/93-handler-concurrency/93-CONTEXT.md:84" \
        ".planning/phases/94-openai-latency-graceful-failure/94-CONTEXT.md:34" \
        ".planning/phases/95-booking-reliability-graceful-degradation/95-CONTEXT.md:37" \
        ".planning/ROADMAP.md:62" \
        ".planning/ROADMAP.md:93" \
        ".planning/MACRO-ROADMAP.md:99"; do
        f="${spec%:*}"; l="${spec##*:}"
        got=$(extract_block "$f" "$l" | shasum -a 256 | cut -d' ' -f1)
        if [ "$got" = "$expected" ]; then
          echo "OK   $spec = $got"
        else
          echo "DRIFT $spec = $got (expected $expected)" >&2
          fail=1
        fi
      done
      [ "$fail" -eq 0 ]
      ```

    Expected: 6 `OK` lines (one per location-pair — note `ROADMAP.md` appears twice at `:62` and `:93`), script exits 0. If ANY location reports `DRIFT`, halt before commit and report drift to the user; do NOT auto-resync.

    **Type / lint gates** (CLAUDE.md + el-templo-bot/CLAUDE.md discipline):
      `cd el-templo-bot && pnpm tsc --noEmit` — exit 0 (no new type errors).
      `cd el-templo-api && pnpm tsc --noEmit` — exit 0 (no new type errors).
      `cd el-templo-bot && pnpm lint 2>&1 | tail -10` — exit 0.
      `cd el-templo-api && pnpm lint 2>&1 | tail -10` — exit 0.

    **Console / `any` baselines** (recorded in 95-AUDIT.md Section A; verified equal to post-commit):
      `grep -rEn "console\." el-templo-bot/src/ | wc -l` — should be 0 (Phase 93 baseline) and remain 0.
      `grep -rEn "console\." el-templo-api/src/ | wc -l` — record actual baseline (likely 0; if not, lock at observed).
      `grep -rEn ':\s*any\b|<any[,>]|<any\s|as\s+any\b|\bany\[\]|Record<[^>]*,\s*any\s*>|Array<any>|Promise<any>' el-templo-bot/src/ | wc -l` — should match 93-AUDIT.md baseline 0.
      `grep -rEn ':\s*any\b|<any[,>]|<any\s|as\s+any\b|\bany\[\]|Record<[^>]*,\s*any\s*>|Array<any>|Promise<any>' el-templo-api/src/ | wc -l` — record baseline.
      NEW test files MUST NOT introduce `console.*` or new `any` types — re-grep against the test files after authoring:
        `grep -E "console\." el-templo-api/test/whatsapp/v5-3-3-booking.integration.test.ts` — empty.
        `grep -E "console\." el-templo-bot/test/v5-3-3-booking-reliability.test.ts` — empty.
        `grep -E ':\s*any\b|<any[,>]|as\s+any\b|\bany\[\]' el-templo-api/test/whatsapp/v5-3-3-booking.integration.test.ts` — empty.
        `grep -E ':\s*any\b|<any[,>]|as\s+any\b|\bany\[\]' el-templo-bot/test/v5-3-3-booking-reliability.test.ts` — empty.

    **Atomic commit** (CONTEXT.md D-20 + D-21 single-commit variant):

      ```
      git add .planning/phases/95-booking-reliability-graceful-degradation/95-AUDIT.md \
              el-templo-api/test/whatsapp/v5-3-3-booking.integration.test.ts \
              el-templo-bot/test/v5-3-3-booking-reliability.test.ts
      git commit -m "$(cat <<'EOF'
      audit(95-01): branch verdict for BUG-03 + RED tests

      Phase 95 Plan 95-01 — audit-first close of BUG-03 (BOOK-01).

      Per .planning/v5.3.3-codebase-audit.md:185-260 and 95-CONTEXT.md D-01,
      BUG-03 has 5 plausible root-cause candidates. Static analysis cannot
      deterministically pick one — the audit DOES pick via RED tests
      (each test discriminates one candidate's predicted SQL or prompt-shape
      behavior against current master).

      95-AUDIT.md final branch verdict: <Branch 1|2|3-{subset}> — <named
      candidate(s)> fires. Fix scope for plan 95-02 named upfront in
      95-AUDIT.md Section E (Implementation Pointers).

      RED tests (TDD fail-in-main per 95-CONTEXT.md D-20):
        - el-templo-api/test/whatsapp/v5-3-3-booking.integration.test.ts
          (candidates i, ii, iv, v — real eltemplo_test MySQL discriminators)
        - el-templo-bot/test/v5-3-3-booking-reliability.test.ts
          (candidate iii — model/prompt-side discriminator)

      Failing test transcripts captured in 95-AUDIT.md Section F.

      Cross-Phase Invariant block at all 6 canonical locations
      (93/94/95-CONTEXT.md + ROADMAP.md ×2 + MACRO-ROADMAP.md) verified
      byte-identical to sha256 67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344 — no drift.

      No production source modified. Plan 95-02 turns the RED tests GREEN
      at the audit-named code surface.
      EOF
      )"
      ```

    **Plan-checker (downstream stop point):** After commit, STOP. Per the orchestrator's downstream-consumer contract, the user reviews 95-AUDIT.md's Final Branch Verdict before authorizing plan 95-02 spawning. Do NOT spawn 95-02 from this task.

  </behavior>
  <action>
    Execute in order:

    1. Read all files listed in `<read_first>`. **Single pass each — no re-reads. Extract everything needed in one read; if you need more detail later, use Grep with a specific pattern against the in-scope file.**

    2. Capture baselines into scratch space:
       - `grep -rEn "console\." el-templo-bot/src/ | wc -l`
       - `grep -rEn "console\." el-templo-api/src/ | wc -l`
       - `grep -rEn ':\s*any\b|<any[,>]|<any\s|as\s+any\b|\bany\[\]|Record<[^>]*,\s*any\s*>|Array<any>|Promise<any>' el-templo-bot/src/ | wc -l`
       - `grep -rEn ':\s*any\b|<any[,>]|<any\s|as\s+any\b|\bany\[\]|Record<[^>]*,\s*any\s*>|Array<any>|Promise<any>' el-templo-api/src/ | wc -l`
       Numbers go into 95-AUDIT.md Section A.

    3. Capture current code-surface state for the 5 candidates by reading the exact line ranges from `<candidate_index>`. Compute the per-candidate verdict (FIRES / DOES NOT FIRE / INCONCLUSIVE) using static reasoning + the discriminator description; write Section B of 95-AUDIT.md.

    4. Author the integration test file (`el-templo-api/test/whatsapp/v5-3-3-booking.integration.test.ts`) per the structure in `<behavior>`. Each describe block follows the `ai-tools.test.ts` pattern. Within each describe, write the RED `it()` block(s) per the executor-adaptation contract: read actual `executeTool(...)` output on master FIRST (call it once in scratch with `vi` or a quick node-eval), then craft the assertion such that current output FAILS the post-fix expected shape.

    5. Author the unit test file (`el-templo-bot/test/v5-3-3-booking-reliability.test.ts`) per the structure in `<behavior>`. Choose Strategy A or Strategy B based on the audit's verdict for candidate (iii).

    6. Run RED verification gates:
       - `cd el-templo-api && pnpm test --run test/whatsapp/v5-3-3-booking.integration.test.ts 2>&1 | tee /tmp/red-output-95-01-integration.txt`
       - `cd el-templo-bot && pnpm test --run test/v5-3-3-booking-reliability.test.ts 2>&1 | tee /tmp/red-output-95-01-unit.txt`
       Capture the failure transcript output into 95-AUDIT.md Section F (Appendix). The exact `Tests N failed` line shape from each command goes into the audit for empirical evidence.

    7. Synthesize Section C (Final Branch Verdict). Based on which RED `it()` blocks actually FAIL on master, name Branch 1 (single candidate, audit names which) OR Branch 2 (different single candidate) OR Branch 3-{subset} (compound, audit enumerates which). For each branch, write Section E (Implementation Pointers for Plan 95-02) — exact file:line ranges plan 95-02 modifies, which RED tests plan 95-02 must turn GREEN, and any DEGR-02 / SC#3 / Phase 96 snapshot-coordination notes.

    8. Run type / lint gates:
       - `cd el-templo-bot && pnpm tsc --noEmit`
       - `cd el-templo-api && pnpm tsc --noEmit`
       - `cd el-templo-bot && pnpm lint 2>&1 | tail -10`
       - `cd el-templo-api && pnpm lint 2>&1 | tail -10`
       Each MUST exit 0.

    9. Run console/`any` post-write check on the NEW test files:
       - `grep -E "console\." el-templo-api/test/whatsapp/v5-3-3-booking.integration.test.ts` — empty.
       - `grep -E "console\." el-templo-bot/test/v5-3-3-booking-reliability.test.ts` — empty.
       - `grep -E ':\s*any\b|<any[,>]|as\s+any\b|\bany\[\]' el-templo-api/test/whatsapp/v5-3-3-booking.integration.test.ts` — empty.
       - `grep -E ':\s*any\b|<any[,>]|as\s+any\b|\bany\[\]' el-templo-bot/test/v5-3-3-booking-reliability.test.ts` — empty.

    10. Run sha256 drift sentry across the 6 canonical-block location-pairs (recipe verbatim in `<behavior>` Pre-commit sha256 invariant drift sentry section). Expect exit 0. If DRIFT, HALT and report; do NOT commit.

    11. Run negative-assertion guardrails (no production source modified):
       - `git status --short el-templo-bot/src/ el-templo-api/src/` — empty.
       - `git status --short el-templo-bot/.env.example el-templo-bot/scripts/check-debounce-invariant.sh` — empty.
       - `git diff --stat HEAD -- el-templo-bot/test/v5-3-2-regression.test.ts` — empty.
       - `git diff --stat HEAD -- el-templo-bot/test/fixtures/` — empty.
       - `git diff --stat HEAD -- .planning/ROADMAP.md .planning/MACRO-ROADMAP.md .planning/phases/93-handler-concurrency/93-CONTEXT.md .planning/phases/94-openai-latency-graceful-failure/94-CONTEXT.md .planning/phases/95-booking-reliability-graceful-degradation/95-CONTEXT.md` — empty.

    12. Atomically commit the 3 new files with the HEREDOC commit message in `<behavior>`. After commit, run `git log --oneline -n 1 --stat` to confirm the commit landed and touched ONLY the 3 expected files.

    13. STOP. Do not spawn plan 95-02. Per the orchestrator's downstream-consumer contract, the user reviews 95-AUDIT.md before authorizing 95-02.

  </action>
  <verify>
    <automated>
test -f .planning/phases/95-booking-reliability-graceful-degradation/95-AUDIT.md \
&& grep -qE "^## .*Branch 1\b" .planning/phases/95-booking-reliability-graceful-degradation/95-AUDIT.md \
&& grep -qE "^## .*Branch 2\b" .planning/phases/95-booking-reliability-graceful-degradation/95-AUDIT.md \
&& grep -qE "^## .*Branch 3\b" .planning/phases/95-booking-reliability-graceful-degradation/95-AUDIT.md \
&& grep -qE "FIRES|DOES NOT FIRE" .planning/phases/95-booking-reliability-graceful-degradation/95-AUDIT.md \
&& grep -qE "candidate \(i\)" .planning/phases/95-booking-reliability-graceful-degradation/95-AUDIT.md \
&& grep -qE "candidate \(ii\)" .planning/phases/95-booking-reliability-graceful-degradation/95-AUDIT.md \
&& grep -qE "candidate \(iii\)" .planning/phases/95-booking-reliability-graceful-degradation/95-AUDIT.md \
&& grep -qE "candidate \(iv\)" .planning/phases/95-booking-reliability-graceful-degradation/95-AUDIT.md \
&& grep -qE "candidate \(v\)" .planning/phases/95-booking-reliability-graceful-degradation/95-AUDIT.md \
&& test -f el-templo-api/test/whatsapp/v5-3-3-booking.integration.test.ts \
&& test -f el-templo-bot/test/v5-3-3-booking-reliability.test.ts \
&& [ "$(wc -l < el-templo-api/test/whatsapp/v5-3-3-booking.integration.test.ts)" -ge 120 ] \
&& [ "$(wc -l < el-templo-bot/test/v5-3-3-booking-reliability.test.ts)" -ge 40 ] \
&& grep -cE "^\s*(it|describe)\(" el-templo-api/test/whatsapp/v5-3-3-booking.integration.test.ts | grep -qE "^[5-9]|^[0-9]{2,}" \
&& grep -cE "^\s*(it|describe)\(" el-templo-bot/test/v5-3-3-booking-reliability.test.ts | grep -qE "^[1-9]" \
&& git log -1 --format=%s | grep -qE "^audit\(95-01\): " \
&& [ "$(git show --stat HEAD | grep -cE '\.planning/phases/95-booking-reliability-graceful-degradation/95-AUDIT.md|el-templo-api/test/whatsapp/v5-3-3-booking.integration.test.ts|el-templo-bot/test/v5-3-3-booking-reliability.test.ts')" -eq 3 ] \
&& [ -z "$(git show --stat HEAD --name-only HEAD | grep -E '^el-templo-bot/src/|^el-templo-api/src/|^el-templo-bot/\.env\.example|^el-templo-bot/scripts/' || true)" ] \
&& cd /Users/bores/el-templo \
&& extract_block() { awk -v s="$2" 'NR>=s {print; if (/Minimum TTL =/) exit}' "$1"; } \
&& expected="67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344" \
&& for spec in \
   ".planning/phases/93-handler-concurrency/93-CONTEXT.md:84" \
   ".planning/phases/94-openai-latency-graceful-failure/94-CONTEXT.md:34" \
   ".planning/phases/95-booking-reliability-graceful-degradation/95-CONTEXT.md:37" \
   ".planning/ROADMAP.md:62" \
   ".planning/ROADMAP.md:93" \
   ".planning/MACRO-ROADMAP.md:99"; do \
     f="${spec%:*}"; l="${spec##*:}"; \
     got=$(extract_block "$f" "$l" | shasum -a 256 | cut -d' ' -f1); \
     [ "$got" = "$expected" ] || { echo "DRIFT $spec"; exit 1; }; \
   done \
&& grep -v '^\s*[/*]' el-templo-api/test/whatsapp/v5-3-3-booking.integration.test.ts | grep -cE "console\." | grep -qE "^0$" \
&& grep -v '^\s*[/*]' el-templo-bot/test/v5-3-3-booking-reliability.test.ts | grep -cE "console\." | grep -qE "^0$" \
&& grep -v '^\s*[/*]' el-templo-api/test/whatsapp/v5-3-3-booking.integration.test.ts | grep -cE ":\s*any\b|<any[,>]|as\s+any\b|\bany\[\]" | grep -qE "^0$" \
&& grep -v '^\s*[/*]' el-templo-bot/test/v5-3-3-booking-reliability.test.ts | grep -cE ":\s*any\b|<any[,>]|as\s+any\b|\bany\[\]" | grep -qE "^0$" \
&& cd el-templo-bot && pnpm tsc --noEmit \
&& cd ../el-templo-api && pnpm tsc --noEmit \
&& cd /Users/bores/el-templo \
&& if ! ( cd el-templo-api && pnpm test --run test/whatsapp/v5-3-3-booking.integration.test.ts 2>&1 | grep -qE "Tests +[1-9][0-9]* +failed" ); then echo "FAIL: integration RED tests not failing against main (95-02 has not shipped — tests MUST be RED post-95-01)"; exit 1; fi \
&& cd /Users/bores/el-templo \
&& if ! awk '/candidate \(iii\)/,/candidate \(iv\)/' .planning/phases/95-booking-reliability-graceful-degradation/95-AUDIT.md | grep -qE "DOES NOT FIRE"; then if ! ( cd el-templo-bot && pnpm test --run test/v5-3-3-booking-reliability.test.ts 2>&1 | grep -qE "Tests +[1-9][0-9]* +failed" ); then echo "FAIL: candidate (iii) is not marked DOES NOT FIRE in 95-AUDIT.md AND unit RED test is not failing against main"; exit 1; fi; fi \
&& cd /Users/bores/el-templo \
&& cd el-templo-bot && pnpm lint \
&& cd ../el-templo-api && pnpm lint
    </automated>
    <human-check>
Reviewer inspects `.planning/phases/95-booking-reliability-graceful-degradation/95-AUDIT.md` Section C (Final Branch Verdict) and confirms:

1. The branch verdict is one of Branch 1, Branch 2, or Branch 3-{subset} — clearly named, with the firing candidate(s) explicitly identified by their (i)/(ii)/(iii)/(iv)/(v) letter.
2. Section E (Implementation Pointers for Plan 95-02) names exact file:line ranges plan 95-02 modifies, and lists which RED tests plan 95-02 must turn GREEN.
3. Section F (Appendix: RED test failure transcripts) contains actual `pnpm test` output (failure transcript, not paraphrased).
4. For each candidate marked DOES NOT FIRE, Section B includes documented reasoning (not silently omitted).
5. If candidate (iii) is DOES NOT FIRE, the unit test file's regression-protector status is recorded in Section C.
6. If a Branch's fix touches `system-prompt.ts`, Section C notes the Phase 96 snapshot-coordination handoff explicitly.

Resume signal: type `approved` to authorize plan 95-02 spawning, or describe required revisions to 95-AUDIT.md.
</human-check>
</verify>
<done> 1. `.planning/phases/95-booking-reliability-graceful-degradation/95-AUDIT.md` exists with Sections A-F populated (Baselines, Per-Candidate Verdicts for all 5 candidates, Final Branch Verdict naming Branch 1/2/3-{subset} and the firing candidate(s), Pipeline Map, Implementation Pointers for plan 95-02, Appendix with RED test failure transcripts). 2. `el-templo-api/test/whatsapp/v5-3-3-booking.integration.test.ts` exists (≥120 lines, ≥5 `describe`/`it` blocks across the 4 SQL-discriminating candidates). At least one `it()` block FAILS against master (RED state). Real `eltemplo_test` MySQL pool pattern from `ai-tools.test.ts`. Zero `console.*`, zero `any` types. 3. `el-templo-bot/test/v5-3-3-booking-reliability.test.ts` exists (≥40 lines, ≥1 `it()` block). If audit verdict for candidate (iii) is FIRES, at least one `it()` block FAILS against master; if DOES NOT FIRE, file contains ≥1 regression-protector `it()` block and that status is recorded in 95-AUDIT.md Section C. Zero `console.*`, zero `any` types. 4. All three files committed in ONE atomic commit with subject matching `^audit\(95-01\): ` (HEREDOC body referencing the branch verdict, the 5 candidates, the v5.3.3-codebase-audit.md:185-260 source, and the sha256 67670b1e drift sentry). 5. Commit touches EXACTLY 3 files (the audit + the 2 test files). No production source touched (`git show --stat HEAD` confirms zero `src/` files in the diff). 6. Sha256 drift sentry across the 6 canonical-block location-pairs PASSES (6 `OK` lines, exit 0). 7. `pnpm tsc --noEmit` PASSES in both `el-templo-bot` and `el-templo-api`. 8. `pnpm lint` PASSES in both `el-templo-bot` and `el-templo-api`. 9. STOP point reached — plan 95-02 NOT spawned. User reviews 95-AUDIT.md and approves before 95-02 planning begins.
</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                        | Description                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| static audit reasoning → RED test discriminator | The audit's per-candidate verdict is empirically falsifiable by the RED tests. If the audit names "candidate (ii) fires" but the RED test for (ii) PASSES on master and a DIFFERENT candidate's RED test FAILS, the audit's verdict is wrong — must be corrected. The RED tests are the ground truth.                                                                                                                                             |
| auditor → 95-AUDIT.md                           | The audit is a written deliverable consumed by plan 95-02's planner + executor. Ambiguous or hedging language in Section E (Implementation Pointers) causes 95-02 to re-litigate scope. The audit MUST name exact file:line ranges, not "around tools.ts".                                                                                                                                                                                        |
| executor → multi-doc canonical invariant block  | Plan 95-01 mutates none of the 6 canonical-block location-pairs by design. Accidental collateral edit (e.g., during 95-AUDIT.md authoring, the executor pastes a paraphrased copy into the audit doc that lives at a future "shouldn't exist" 7th location) is detected by the sha256 drift sentry. The sentry is the safeguard against the user-MEMORY rule `feedback_multi_doc_invariant_hash_check.md` (paraphrase = drift waiting to happen). |
| executor → eltemplo_test database               | Integration tests run against the shared MySQL test DB. `beforeEach` truncates rows; concurrent test runs from different files MUST NOT collide on row IDs. The existing `ai-tools.test.ts` and `v5-3-3-handler-concurrency.integration.test.ts` patterns use `email LIKE 'test-...-%'` and `code LIKE 'TST%'` namespacing — the new integration test reuses the same namespace conventions to avoid cross-suite contamination.                   |
| executor → bot test surface                     | Unit test imports `src/ai/system-prompt` and/or `src/ai/tools` directly. If the imports themselves trigger DB or network reads (unlikely given current code), the test isolates via `vi.doMock`. Otherwise direct import is safe.                                                                                                                                                                                                                 |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                                                                                                                                                  | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                  |
| ---------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-95-01-01 | Tampering              | 95-AUDIT.md Final Branch Verdict can be written based on static reasoning alone, ignoring the RED test transcripts                                                                         | mitigate    | Section F (Appendix) requires the actual `pnpm test` failure transcripts pasted in. The audit's Final Branch Verdict in Section C MUST reference the specific `it()` block names that failed in Section F. Plan-checker (adversarial framing per CONTEXT.md D-22) verifies this cross-reference exists.                          |
| T-95-01-02 | Tampering              | Canonical invariant block at any of the 6 location-pairs drifts during the executor's 95-AUDIT.md authoring session as collateral of a markdown edit                                       | mitigate    | sha256 drift sentry runs BEFORE commit. If any of the 6 location-pairs reports DRIFT, executor halts and reports — does NOT auto-resync. Pattern inherited verbatim from `94-02-PLAN.md:305-326`.                                                                                                                                |
| T-95-01-03 | Tampering              | Executor "softens" a RED test (e.g., `expect(...).toMatch(/.*/)` instead of a discriminating assertion) so the test passes on master to avoid the RED state                                | mitigate    | The pre-commit RED verification gate (`pnpm test --run test/whatsapp/v5-3-3-booking.integration.test.ts` exits non-zero) catches soft tests. Section F (Appendix) requires the failure transcript — a "soft" test that passes on master produces no failure transcript, leaving Section F empty, which the plan-checker catches. |
| T-95-01-04 | Tampering              | Plan 95-02's planner, reading 95-AUDIT.md, hand-waves the fix scope and modifies the wrong file:line range                                                                                 | mitigate    | Section E (Implementation Pointers) names exact file:line ranges PER BRANCH. Plan 95-02's must_haves frontmatter inherits these ranges verbatim. Plan-checker for 95-02 verifies the consumed scope matches the audit's named scope.                                                                                             |
| T-95-01-05 | Tampering              | Production source files (`tools.ts`, `handler.ts`, `system-prompt.ts`, etc.) get modified accidentally during the audit session (e.g., by reading-with-intent-to-fix instead of read-only) | mitigate    | Negative-assertion guardrail at Action step 11: `git status --short el-templo-bot/src/ el-templo-api/src/` MUST be empty pre-commit. The verify block's `git show --stat HEAD` post-commit check confirms zero `src/` files in the diff.                                                                                         |
| T-95-01-06 | Information Disclosure | Test seed data (e.g., synthetic phone numbers, emails) leaks into the test transcripts pasted into Section F (Appendix)                                                                    | accept      | Seed data uses the existing `test-...-%`/`TST%` namespace which is intentionally non-real. No PII; safe to paste.                                                                                                                                                                                                                |
| T-95-01-07 | Denial-of-Service      | A RED test pollutes `eltemplo_test` and breaks subsequent test runs (e.g., leaves a row with conflicting unique key)                                                                       | mitigate    | `beforeEach` truncate convention from `ai-tools.test.ts:45-60` is mandatory. The new integration test's beforeEach MUST truncate ALL seeded tables before re-seeding. No "leftover state from prior `it()`" allowed.                                                                                                             |
| T-95-01-08 | Tampering              | Future planner re-introduces the "wrong" fix scope by re-reading the v5.3.3-codebase-audit.md candidates and picking a different one than 95-AUDIT.md named                                | mitigate    | 95-AUDIT.md Section E is the authoritative scope. Plan 95-02's must_haves frontmatter references "per 95-AUDIT.md Section E Branch <N>" by ID. Plan-checker verifies the reference exists in 95-02.                                                                                                                              |
| T-95-01-09 | Spoofing               | The model could be told to "produce 95-AUDIT.md" without actually running the RED tests, fabricating Section F transcripts                                                                 | mitigate    | The verify block's automated assertion `[ "$(git show --stat HEAD                                                                                                                                                                                                                                                                | grep -cE ...)" -eq 3 ]` confirms 3 files were committed; the human-check verifies Section F contains real transcript output. The TDD pre-commit gate (Step 6) is the empirical floor — if the gate isn't actually run, the audit doesn't ship. |
| T-95-01-10 | Information Disclosure | `pnpm test` output may include connection strings or env vars in error messages                                                                                                            | accept      | `eltemplo_test` env vars are local-dev only; no production secrets in test environment. Existing test files already produce such output without redaction; not new exposure.                                                                                                                                                     |
| T-95-SC    | Tampering              | npm/pip/cargo installs                                                                                                                                                                     | accept      | Plan 95-01 installs NO new packages. All required test infrastructure (vitest, drizzle-orm, mysql2) is already a dependency at v5.3.3 baseline. No `[ASSUMED]` / `[SUS]` / `[SLOP]` packages introduced. Package legitimacy gate not triggered.                                                                                  |

</threat_model>

<verification>

## Phase-Level Verification (run after the atomic commit)

1. **Commit shape — exactly 3 files modified, all new:**

   ```bash
   git show --stat HEAD --name-only HEAD | tail -10
   ```

   Expected: exactly the three files `95-AUDIT.md`, `v5-3-3-booking.integration.test.ts`, `v5-3-3-booking-reliability.test.ts` listed; no `el-templo-bot/src/...`, no `el-templo-api/src/...`, no `.env.example`, no canonical-block markdown documents. Commit subject matches `^audit\(95-01\): `.

2. **95-AUDIT.md structural integrity:**

   ```bash
   grep -cE "^## .*Branch [123]\b" .planning/phases/95-booking-reliability-graceful-degradation/95-AUDIT.md
   grep -cE "candidate \([iv]+\)" .planning/phases/95-booking-reliability-graceful-degradation/95-AUDIT.md
   grep -cE "FIRES|DOES NOT FIRE" .planning/phases/95-booking-reliability-graceful-degradation/95-AUDIT.md
   ```

   Expected: ≥3 (one for each Branch heading), ≥5 (one per candidate i..v), ≥5 (one verdict per candidate). Section F (Appendix) contains text matching `pnpm` and one of `failed` / `passed`.

3. **RED tests fail on master baseline (re-run sanity):**

   The integration test file MUST contain ≥1 `it()` block that fails when invoked against current code (where current code = HEAD pre-this-plan; post-commit it = HEAD pre-plan-95-02). Re-run:

   ```bash
   cd el-templo-api && pnpm test --run test/whatsapp/v5-3-3-booking.integration.test.ts 2>&1 | tail -20
   cd ../el-templo-bot && pnpm test --run test/v5-3-3-booking-reliability.test.ts 2>&1 | tail -10
   ```

   Expected: at least one of the two commands exits non-zero (RED). If the audit's Final Branch Verdict for candidate (iii) is DOES NOT FIRE, the bot-side unit-test command may exit 0 (regression-protectors only) — but the integration-test command MUST still exit non-zero (because at least one of i/ii/iv/v MUST fire for BUG-03 to be a real bug, per the codebase-audit's empirical anchor in `v5.3.3-codebase-audit.md:233-260`). The fail/pass shape is recorded in 95-AUDIT.md Section F.

4. **No regression in pre-existing test suites:**

   ```bash
   cd el-templo-bot && pnpm test 2>&1 | tail -5
   cd ../el-templo-api && pnpm test 2>&1 | tail -5
   ```

   Expected: all PRE-EXISTING tests still pass. The new RED tests' failures are expected and isolated — they do NOT break Phase 93's or Phase 94's tests, nor the v5-3-2-regression suite. The full-suite output shows `Tests N+M passed, K failed` where K is exactly the count of new RED tests asserting the firing candidate's predicted-fix shape; N+M = (Phase 93+94 tests passing) + (regression-protector new tests passing).

5. **Type / lint clean:**

   ```bash
   cd el-templo-bot && pnpm tsc --noEmit
   cd ../el-templo-api && pnpm tsc --noEmit
   cd ../el-templo-bot && pnpm lint 2>&1 | tail -5
   cd ../el-templo-api && pnpm lint 2>&1 | tail -5
   ```

   Expected: all four exit 0. No new `any` types in test files, no `console.*`.

6. **Cross-Phase Invariant — sha256 drift sentry across SIX location-pairs (verbatim from 94-02-PLAN.md:305-326 recipe, expanded by 1 pair to include 95-CONTEXT.md):**

   ```bash
   cd /Users/bores/el-templo
   extract_block() { awk -v s="$2" 'NR>=s {print; if (/Minimum TTL =/) exit}' "$1"; }
   expected="67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344"
   fail=0
   for spec in \
     ".planning/phases/93-handler-concurrency/93-CONTEXT.md:84" \
     ".planning/phases/94-openai-latency-graceful-failure/94-CONTEXT.md:34" \
     ".planning/phases/95-booking-reliability-graceful-degradation/95-CONTEXT.md:37" \
     ".planning/ROADMAP.md:62" \
     ".planning/ROADMAP.md:93" \
     ".planning/MACRO-ROADMAP.md:99"; do
     f="${spec%:*}"; l="${spec##*:}"
     got=$(extract_block "$f" "$l" | shasum -a 256 | cut -d' ' -f1)
     if [ "$got" = "$expected" ]; then
       echo "OK   $f:$l = $got"
     else
       echo "DRIFT $f:$l = $got (expected $expected)" >&2
       fail=1
     fi
   done
   [ "$fail" -eq 0 ]
   ```

   Expected: 6 `OK` lines, script exits 0. The 6-pair framing is the literal orchestrator-locked spec for this plan (see `<cross_phase_invariant>` block in `<context>` above). Do NOT collapse to "5 locations" — the framing is exhaustive over actual file:line pairs (`ROADMAP.md` contributes 2 occurrences).

7. **Negative-assertion guardrails (no scope leak):**

   ```bash
   PHASE_95_PARENT=$(git log --format=%H -n 1 HEAD^)
   git diff ${PHASE_95_PARENT}..HEAD -- el-templo-bot/src/                                           # expect empty
   git diff ${PHASE_95_PARENT}..HEAD -- el-templo-api/src/                                           # expect empty
   git diff ${PHASE_95_PARENT}..HEAD -- el-templo-bot/.env.example                                   # expect empty
   git diff ${PHASE_95_PARENT}..HEAD -- el-templo-bot/scripts/check-debounce-invariant.sh            # expect empty
   git diff ${PHASE_95_PARENT}..HEAD -- el-templo-bot/test/v5-3-2-regression.test.ts                 # expect empty
   git diff ${PHASE_95_PARENT}..HEAD -- el-templo-bot/test/fixtures/                                 # expect empty
   git diff ${PHASE_95_PARENT}..HEAD -- .planning/ROADMAP.md                                          # expect empty
   git diff ${PHASE_95_PARENT}..HEAD -- .planning/MACRO-ROADMAP.md                                    # expect empty
   git diff ${PHASE_95_PARENT}..HEAD -- .planning/phases/93-handler-concurrency/93-CONTEXT.md         # expect empty
   git diff ${PHASE_95_PARENT}..HEAD -- .planning/phases/94-openai-latency-graceful-failure/94-CONTEXT.md  # expect empty
   git diff ${PHASE_95_PARENT}..HEAD -- .planning/phases/95-booking-reliability-graceful-degradation/95-CONTEXT.md  # expect empty
   ```

   Expected: every diff empty. Confirms plan 95-01 stayed within its declared `files_modified` (only the 3 new files).

8. **STOP point honored — plan 95-02 NOT created:**

   ```bash
   ls .planning/phases/95-booking-reliability-graceful-degradation/95-02-PLAN.md 2>/dev/null
   ```

   Expected: file does NOT exist. Plan 95-02 is spawned in a separate session after user reviews 95-AUDIT.md per the orchestrator's downstream-consumer contract.

</verification>

<success_criteria>

The Phase 95 audit-first investigation plan is complete when ALL of the following are true:

- **Audit deliverable shipped:** `95-AUDIT.md` exists with Sections A (Baselines), B (Per-Candidate Verdicts for all 5), C (Final Branch Verdict naming Branch 1/2/3-{subset} with explicit firing candidate(s)), D (Pipeline Map), E (Implementation Pointers for plan 95-02 with exact file:line ranges), F (Appendix with `pnpm test` RED transcripts pasted verbatim).
- **Branch verdict named:** Section C resolves to Branch 1 (single named candidate), Branch 2 (different single candidate), or Branch 3-{subset} (compound subset enumerated). No INCONCLUSIVE final-verdict — INCONCLUSIVE may exist per-candidate in Section B but Section C must commit to a branch (or escalate to a Branch 4 out-of-band per CONTEXT.md `<deferred>` "Audit Branch 5 observability fallback" — Phase 93 had such a fallback; Phase 95's CONTEXT.md deferred it because the 5 candidates have deterministic SQL signatures; if literally none of them fire, escalate out of band before commit).
- **RED tests fail on master:** At least one `it()` block in `v5-3-3-booking.integration.test.ts` FAILS against pre-plan code. If candidate (iii) FIRES, at least one `it()` block in `v5-3-3-booking-reliability.test.ts` also FAILS. If candidate (iii) DOES NOT FIRE, the unit test exists with ≥1 regression-protector `it()` block and that status is recorded in Section C.
- **TDD discipline preserved:** Single atomic commit with subject matching `^audit\(95-01\): `. Commit touches exactly the 3 files in `files_modified` — no production source, no env, no scripts, no canonical-block markdown.
- **Cross-Phase Invariant holds:** All 6 canonical-block location-pairs hash to `67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344`. Sha256 drift sentry exits 0.
- **Type / lint clean:** `pnpm tsc --noEmit` and `pnpm lint` pass in both `el-templo-bot` and `el-templo-api`.
- **Zero scope leak:** No production source modified; no env or script file modified; no canonical-block markdown modified; no Phase 91/92/93/94 test files modified; no `pb1-e1a-lead-rendered.snap.txt` modified.
- **Discipline guards pass:** Zero new `console.*` in test files; zero new `any` types in test files; `console_count` and `any_count` baselines from Section A unchanged post-commit (test files contribute zero of either).
- **Unblocks plan 95-02:** 95-AUDIT.md Section E provides a single, unambiguous fix scope for plan 95-02 — exact file:line ranges, RED test names to turn GREEN, and per-branch DEGR-02 / Phase 96 snapshot coordination notes.
- **STOP point honored:** Plan 95-02 NOT spawned. User reviews 95-AUDIT.md and approves before 95-02 planning begins, per the orchestrator's downstream-consumer contract.

</success_criteria>

<output>
This plan is investigative. No SUMMARY.md is produced by 95-01 — the audit document `95-AUDIT.md` IS the readout artifact (mirrors Phase 93's pattern where the audit doc carries the per-check verdicts and Section F transcripts).

The cumulative Phase 95 SUMMARY.md is owned by plans 95-02 and 95-03 collectively (per CONTEXT.md D-21 atomic-commit cadence — one SUMMARY.md per plan that produces shippable artifacts; an audit is an artifact but does not need a SUMMARY).

After this plan's commit lands, the next agent action is determined by the user's review of `95-AUDIT.md`:

- If user approves: orchestrator spawns `/gsd:plan-phase 95` again (or invokes the plan-phase agent with branch-verdict context) to produce `95-02-PLAN.md` (BUG-03 fix at audit-named site + `withTimeout` helper + apply to `tools.ts:636/:806`).
- If user requests revisions: re-enter planning with `<revision_context>` and amend `95-AUDIT.md` per feedback.
- If user defers: 95-AUDIT.md remains as the readout; no further action until milestone resumes.
  </output>
