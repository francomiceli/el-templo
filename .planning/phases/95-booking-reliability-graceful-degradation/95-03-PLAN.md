---
phase: 95-booking-reliability-graceful-degradation
plan: 03
type: execute
wave: 1
depends_on: ["02"]
files_modified:
  - el-templo-bot/src/webhook/handler.ts
  - el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts
autonomous: false
requirements:
  - DEGR-01
  - DEGR-02
tags:
  - fix-mechanical
  - tdd-fail-in-main
  - whatsapp-bot
  - degr-01-escalation
  - request-human
  - sc-3-guardrail

must_haves:
  truths:
    # —— Retry-counter declaration (D-10) ——
    - "A new local `let failedToolCalls = 0;` declaration is added inside `processWithAiInner` at `el-templo-bot/src/webhook/handler.ts`, lexically adjacent to the existing `let humanTakeoverTriggered = false;` declaration at handler.ts:659. Per CONTEXT.md D-10: 'Counter is a local `let failedToolCalls = 0` inside `processWithAiInner`. Lives only for the duration of one inbound; resets to 0 on every new `processWithAi` entry. No Redis state, no cross-inbound persistence.' Post-fix: `grep -nE 'let failedToolCalls = 0' el-templo-bot/src/webhook/handler.ts | wc -l == 1`."

    # —— Locked handoff phrase constant (D-06) ——
    - 'A module-level `const HANDOFF_ESCALATION_PHRASE = "Te paso con alguien del equipo, te escriben enseguida 🙌";` is added to `el-templo-bot/src/webhook/handler.ts`. The byte sequence is verbatim: includes the 🙌 emoji, lowercase ''equipo'', a single ASCII space between ''enseguida'' and the emoji, and no trailing whitespace. Per CONTEXT.md D-06: ''Defined as `const HANDOFF_ESCALATION_PHRASE` in `handler.ts` near the retry-counter logic (NOT in `system-prompt.ts` — this is handler-facing, not model-facing). NOT moved to a shared phrases module yet because the only consumer is this single trigger.'' Post-fix: `grep -cE ''const HANDOFF_ESCALATION_PHRASE = "Te paso con alguien del equipo, te escriben enseguida 🙌"'' el-templo-bot/src/webhook/handler.ts == 1`.'

    # —— Failure-trigger allowlist (D-11, D-15, D-18) ——
    - 'A module-level `FAILURE_ALLOWLIST` (`ReadonlySet<string>`) is declared in `el-templo-bot/src/webhook/handler.ts` containing the THREE static byte-exact strings from CONTEXT.md D-11: (a) `''No pude completar la reserva. Intenta de nuevo en un momento.''` (tools.ts:806 bookClass fail-string), (b) `''No pude completar el registro. Intenta de nuevo en un momento.''` (tools.ts:968 registerTrial fail-string), (c) `''Herramienta agotada por tiempo de espera.''` (the 95-02-shipped executeTool catch return per CONTEXT.md D-15+D-18). The fourth allowlist entry — the executeTool default-case dynamic string `''Herramienta "${name}" no disponible.''` at tools.ts:264 — is discriminated by a separate `startsWith(''Herramienta "'')` check, NOT placed in the static Set. Per CONTEXT.md D-11.'
    - 'The classification helper `isFailureToolResult(result: string): boolean` (or equivalent inline expression that reads as a single clear predicate) returns true if EITHER (a) `FAILURE_ALLOWLIST.has(result)` is true OR (b) `result.startsWith(''Herramienta "'') && result.endsWith(''" no disponible.'')` is true. This is the SOLE classifier for the (b) return-string branch of the failure trigger. The (a) throw branch is handled separately inside the existing tool-call try-block (see below).'

    # —— Counter increment surface (D-11, D-12, D-15) ——
    - "Inside the tool loop at `el-templo-bot/src/webhook/handler.ts` (the `for (const toolCall of response.toolCalls)` body around handler.ts:676-694), the existing `executeTool(...)` invocation is wrapped in a try/catch so the counter can capture BOTH failure modes per CONTEXT.md D-11:\n  - (a) Throw branch: if `executeTool` propagates an uncaught error (i.e., any error type other than `ToolTimeoutError` — that one is already caught inside `executeTool` per 95-02 e213ee80 commit and converted to the allowlist return string), `failedToolCalls++` AND `lastToolName = toolCall.name` AND `lastToolError = err instanceof Error ? err.message : String(err)` are recorded BEFORE the existing error-propagation flow continues. The catch re-throws the error per existing handler.ts shape — counter increment is a side-effect that does NOT change error propagation semantics.\n  - (b) Return-string branch: AFTER the existing `lastToolResult = toolResult;` line at handler.ts:690, evaluate `isFailureToolResult(toolResult)`. If true, `failedToolCalls++` AND `lastToolName = toolCall.name` AND `lastToolError = toolResult` are recorded.\nPer CONTEXT.md D-12: 'Counter increments for EVERY failing tool call across the tool loop's iterations. Successful tool calls do NOT reset.' The counter is NEVER decremented or reset within the same inbound."

    # —— Threshold check + escalation flow (D-13, D-05, D-07, D-08, D-09) ——
    - "Directly AFTER the per-tool-call increment(s) — i.e., still inside the `for (const toolCall of response.toolCalls)` body, AFTER the `messages.push({ role: 'tool', ... })` line that records the tool result for the next AI iteration — a single comparison `if (failedToolCalls >= 2) { ... }` triggers the synthetic escalation flow per CONTEXT.md D-13. Per CONTEXT.md D-13: 'failedToolCalls >= 2 triggers the synthetic phrase + escalation. Per ROADMAP SC#2 \"after 2 failed attempts\". No fuzzy thresholds, no hysteresis.' The branch executes EXACTLY the following actions in order, then breaks out of the `for` and `while` loops via the existing `humanTakeoverTriggered` exit path:\n  1. `log.warn({ phone, conversationId, failureCount: failedToolCalls, lastToolName, lastToolError }, 'DEGR-01 escalation triggered')` per CONTEXT.md D-09.\n  2. `await sendTextMessage(phone, HANDOFF_ESCALATION_PHRASE)` per CONTEXT.md D-05.\n  3. `await executeTool('request_human', { reason: 'auto_escalation_after_2_failures' }, db, conversationId, { phone, clientState })` per CONTEXT.md D-07 — synthetic invocation NOT triggered by the model.\n  4. `humanTakeoverTriggered = true;` per CONTEXT.md D-07 — reuses the existing local boolean; does NOT introduce a parallel flag.\n  5. `break;` out of the inner `for` loop. The outer `while` loop exits on its next condition check because `humanTakeoverTriggered` short-circuits the loop continuation (the existing exit path at handler.ts:928-950 is preserved).\nPost-fix: `grep -cE 'failedToolCalls >= 2' el-templo-bot/src/webhook/handler.ts == 1` AND `grep -cE 'auto_escalation_after_2_failures' el-templo-bot/src/webhook/handler.ts == 1` AND `grep -cE \"DEGR-01 escalation triggered\" el-templo-bot/src/webhook/handler.ts == 1`."

    # —— Drop model response.content on counter trip (D-08) ——
    - "After the escalation `break`, the existing post-tool-loop branch at handler.ts:928-950 takes the synthetic-phrase path WITHOUT emitting `segments[0]` from the model's `response.content`. This is implemented by: between Task 2's edit point (the escalation block) and the existing `if (humanTakeoverTriggered)` handler.ts:928 block, an early `return;` is placed inside the escalation `if` block AFTER sending the synthetic phrase and setting `humanTakeoverTriggered = true` — the early return preempts the post-loop `splitMessage(replyText)` + `segments[0]` emission entirely. Per CONTEXT.md D-08: 'The user receives exactly ONE outbound message for that turn: HANDOFF_ESCALATION_PHRASE. The existing segments[0]-only branch at handler.ts:844-866 is replaced for this code path — even segments[0] from the model is suppressed when the handler-side synthetic phrase fires.' The existing handoffSegment branch at handler.ts:928-950 is reachable ONLY when the model itself emits `request_human` (the existing :692-694 path, not the synthetic-escalation path) — that pathway is preserved unchanged."

    # —— SC#3 / DEGR-02 guardrail (D-14) ——
    - "The retry counter and Phase 91 `whyAsked` flag share NO state. The counter is declared inside the tool-loop region (around handler.ts:659-694); `whyAsked` is read pre-AI from the playbook state (handler.ts:478-490 region per CONTEXT.md canonical_refs §Phase-91-soft-rejection-rule-injection-DEGR-02-guardrail-surface). Per CONTEXT.md D-14: 'The retry counter and Phase 91 whyAsked flag are different semantic objects on different code surfaces.' No new state shape introduced in playbook state. Negative test asserts a soft-rejection inbound ('lo pienso') does NOT increment the counter, does NOT emit `HANDOFF_ESCALATION_PHRASE`, and does NOT invoke `executeTool('request_human', ...)` synthetically — pre-empts Phase 97 RGUARD-02."

    # —— Test file naming + canonical location ——
    - "A new test file `el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts` exists. Naming rationale: mirrors the precedent of `el-templo-bot/test/v5-3-3-openai-latency.test.ts` (one new file per locked-decision-cluster within v5.3.3). The file is bot-side (not under `el-templo-api/test/whatsapp/`) because the behavior under test is purely the bot's handler-loop control flow — the DB / WhatsApp / OpenAI surfaces are fully mocked, mirroring the v5-3-3-openai-latency.test.ts and v5-3-3-handler-concurrency.test.ts mocking patterns established earlier in v5.3.3. No integration test surface is required (no SQL discrimination needed — the test asserts handler control-flow side effects)."

    # —— Test scenario coverage (D-11, D-12, D-13, D-14, D-18) ——
    - 'Test SC-A (POSITIVE — escalation triggers at count=2): mocks the AI provider''s `chat()` to return ONE tool call (`book_class`) per AI iteration. Mocks `executeTool` to return the byte-exact string `''No pude completar la reserva. Intenta de nuevo en un momento.''` for the first two invocations. Asserts: (1) `sendTextMessage` is invoked exactly ONCE with the byte-exact `HANDOFF_ESCALATION_PHRASE` (verified by spy capture + `toBe("Te paso con alguien del equipo, te escriben enseguida 🙌")`), (2) `executeTool` is invoked synthetically with `name === ''request_human''` AND `args.reason === ''auto_escalation_after_2_failures''` (verified by spy capture), (3) the tool loop exits — `provider.chat` is invoked AT MOST 2 times total (the initial call + at most one follow-up depending on where the break lands; the third iteration MUST NOT happen), (4) `response.content` from the most-recent model turn is NOT emitted via `sendTextMessage` (i.e., `sendTextMessage` calls total === 1 + the synthetic-phrase call; no model-content segment leaks). Per CONTEXT.md D-05, D-06, D-07, D-08, D-13.'
    - "Test SC-B (POSITIVE — granularity, no reset on intermediate success per D-12): mocks `executeTool` to return: iteration 1 → first call `'No pude completar la reserva. Intenta de nuevo en un momento.'` (count=1), second call within same iteration → `'Schedule found: ...'` (a non-allowlist success string; count remains 1, NOT reset), iteration 2 → `'No pude completar la reserva. Intenta de nuevo en un momento.'` (count=2 → ESCALATE). Asserts: (1) the synthetic phrase fires on the SECOND iteration's failure (not the first iteration's), (2) the intermediate success between the two failures did NOT reset the counter, (3) all assertions from SC-A about exit-path + segments-suppression hold. Per CONTEXT.md D-12: 'Counter increments for EVERY failing tool call across the tool loop's iterations. Successful tool calls do NOT reset.'"
    - 'Test SC-C (POSITIVE — allowlist coverage per D-11): four sub-cases (one `it()` per allowlist entry, OR a parameterized table), each asserting that a tool returning the given allowlist string increments the counter and combines with one other failure to trip escalation: (i) `''No pude completar la reserva. Intenta de nuevo en un momento.''` (tools.ts:806), (ii) `''No pude completar el registro. Intenta de nuevo en un momento.''` (tools.ts:968), (iii) `''Herramienta agotada por tiempo de espera.''` (tools.ts catch site per CONTEXT.md D-15+D-18 shipped in 95-02), (iv) the default-case dynamic string — assertion uses `''Herramienta "unknown_tool" no disponible.''` to exercise the `startsWith(''Herramienta "'') && endsWith(''" no disponible.'')` discriminator. Each sub-case combines its tool''s failure with one paired failure of another allowlist entry to trip the count=2 threshold. Per CONTEXT.md D-11.'
    - "Test SC-D (POSITIVE — throw path per D-15, D-18): mocks `executeTool` to throw a `ToolTimeoutError` (imported from `el-templo-bot/src/ai/with-timeout.ts`, shipped in 95-02 commit e213ee80) on its first invocation. Note: in practice, executeTool's own try/catch (shipped in 95-02 tools.ts:266-274) converts `ToolTimeoutError` to the allowlist return string before the handler sees a throw — this test instead constructs a NON-`ToolTimeoutError` throw (e.g., a generic `new Error('fetch failed')`) to exercise the handler-side try/catch path independently. Asserts: (1) the counter increments on the throw, (2) the error is re-thrown to preserve existing semantics, (3) NO escalation fires after only ONE failure (count=1; threshold is `>= 2`). This sub-test verifies the throw branch's increment-and-re-throw shape WITHOUT modifying the 95-02-shipped allowlist behavior. Per CONTEXT.md D-11(a)."

    # —— SC#3 negative test (D-14, RGUARD-02 pre-emption) ——
    - "Test SC-E (NEGATIVE — SC#3 / DEGR-02 guardrail per D-14, pre-empts Phase 97 RGUARD-02): mocks the AI provider to return a normal-text response with NO tool calls for an inbound containing a soft-rejection phrase (e.g., 'lo pienso', 'no estoy seguro', 'mañana te digo'). Asserts: (1) `failedToolCalls` is NEVER incremented (verifiable by asserting `sendTextMessage` is called at most once with the model's response.content, NEVER with HANDOFF_ESCALATION_PHRASE), (2) `executeTool` is NEVER invoked with `name === 'request_human'` (no synthetic escalation), (3) `humanTakeoverTriggered` remains false through the inbound's full lifecycle. Per CONTEXT.md D-14: 'The retry counter and Phase 91 whyAsked flag are different semantic objects on different code surfaces.' Per CONTEXT.md `<specifics>` 'Plan 95-03's tests should pre-empt RGUARD-02's assertions with at least one negative case'."

    # —— Allowlist boundary negative test (D-11 SC#3 guardrail) ——
    - "Test SC-F (NEGATIVE — degraded-UX strings explicitly NOT on allowlist per D-11): mocks `executeTool` to return the byte-exact string `'Esa clase esta llena y no hay alternativas disponibles esta semana.'` (the bookClass no-alternatives degraded-UX message, an example of the 'NOT on allowlist' set in CONTEXT.md D-11). Asserts: even with TWO such degraded-UX returns in the same inbound (which WOULD trip the count=2 threshold if they were on the allowlist), (1) `failedToolCalls` remains `0` after both returns, (2) `sendTextMessage` is NEVER invoked with `HANDOFF_ESCALATION_PHRASE`, (3) `executeTool('request_human', ...)` is NEVER invoked synthetically. This empirically pins the boundary between 'failure' (allowlist) and 'degraded-UX' (NOT allowlist) called out in CONTEXT.md D-11. Per CONTEXT.md D-11: 'Explicitly NOT on the allowlist (these are degraded but not failures — SC#3 guardrail).'"

    # —— TDD make-green discipline (D-20) ——
    - "All six test scenarios (SC-A..SC-F) are authored against current master (the post-95-02 codebase at branch HEAD) and observed to FAIL via `cd el-templo-bot && pnpm test --run test/v5-3-3-degr-01-escalation.test.ts` BEFORE the implementation lands. This observation is recorded in the `<human-check>` checklist. The RED → GREEN transition is verified by re-running the same command AFTER the GREEN commit; expected post-fix output: `Tests 6 passed (6)` (or however the parameterized SC-C is reported — could be 4 passing under one parent `it()` if a `.each` form is used). Per CONTEXT.md D-20: 'Plan 95-01 (audit) commits RED tests; plans 95-02 and 95-03 turn them GREEN without modification.' This plan ships BOTH the RED tests AND the GREEN implementation, mirroring the 95-02 TDD-make-green pattern but with both RED and GREEN landing in distinct atomic commits within this plan (per D-21)."

    # —— Atomic commit cadence (D-21) ——
    - "The plan ships as exactly TWO atomic commits (Phase 93/94 + 95-02 pattern, simplified because the surface is smaller). Commit subject regexes — verify each verbatim:\n  1. `git log -1 --format=%s HEAD~1 | grep -cE '^test\\(95-03\\): RED — DEGR-01 escalation counter \\+ synthetic phrase' == 1` (the RED test file commit; observed failing on master pre-implementation).\n  2. `git log -1 --format=%s HEAD | grep -cE '^feat\\(95-03\\): retry counter \\+ HANDOFF_ESCALATION_PHRASE \\+ request_human escalation' == 1` (the GREEN implementation commit in handler.ts).\nEach commit independently produces a green `pnpm tsc --noEmit` in both packages. Per CONTEXT.md D-21: 'Atomic commit cadence per plan. Mirror Phase 93/94 pattern.'"

    # —— TypeScript + discipline guards ——
    - "Both `cd el-templo-bot && pnpm tsc --noEmit` and `cd el-templo-api && pnpm tsc --noEmit` exit 0 after each commit lands. No `: any`, no `<any>`, no `as any` introduced anywhere — the baselines `any_count_baseline_bot = 0` and `any_count_baseline_api = 0` (per 95-AUDIT.md §A) are preserved. `console_count_baseline_bot = 0` is preserved (the implementation uses ONLY `log.warn(...)` via the Fastify-injected `log: FastifyBaseLogger` parameter, NEVER `console.*`)."

    # —— Anti-scope-creep guards (per CONTEXT.md "Out of scope") ——
    - "NO modifications to `el-templo-bot/src/ai/system-prompt.ts`. Per CONTEXT.md `<domain>` 'NOT in scope' and Plan 95-03 framing: touching `system-prompt.ts` would invalidate `pb1-e1a-lead-rendered.snap.txt` and `POST_RLOK_04_BYTES = 18370` at `el-templo-bot/test/v5-3-2-regression.test.ts:57`. The handler-side synthetic phrasing chosen in CONTEXT.md D-05 is precisely so Phase 96 owns the snapshot regen, NOT Phase 95. Plan 95-03's PR description MUST note: 'system-prompt.ts unchanged — snapshot regen deferred to Phase 96.' Verifier: `git diff HEAD~1..HEAD -- el-templo-bot/src/ai/system-prompt.ts | wc -l | tr -d ' '` returns `0`."
    - "NO modifications to `el-templo-bot/src/ai/tools.ts`. The 4 allowlist strings live in `tools.ts` already (the 3 in-place static strings at :806, :968, :241/:264 + the :271 catch return shipped in 95-02 e213ee80). Plan 95-03 ONLY consumes them via the handler-side `FAILURE_ALLOWLIST` Set; it does NOT mutate any tool-handler return path. Per CONTEXT.md `<scope_locked>`: 'Plan 95-03 only consumes the `Herramienta agotada por tiempo de espera.` return string via the allowlist; does NOT modify `tools.ts`.' Verifier: `git diff HEAD~1..HEAD -- el-templo-bot/src/ai/tools.ts | wc -l | tr -d ' '` returns `0`."
    - "NO modifications to `el-templo-bot/src/ai/with-timeout.ts`. The `ToolTimeoutError` class is consumed only inside the test file (SC-D imports it for the throw-path test). The handler-side counter never directly references `ToolTimeoutError` — that catch is already inside `executeTool` (95-02 shipped). Verifier: `git diff HEAD~1..HEAD -- el-templo-bot/src/ai/with-timeout.ts | wc -l | tr -d ' '` returns `0`."
    - "NO modifications to `el-templo-bot/test/v5-3-2-regression.test.ts` (specifically: `POST_RLOK_04_BYTES = 18370` at `:57` remains untouched). NO modifications to `el-templo-bot/test/fixtures/pb1-e1a-lead-rendered.snap.txt` (or any `.snap.txt`). Verifier: `git log --name-only HEAD~1..HEAD | grep -cE '\\.snap\\.txt$' | tr -d ' '` returns `0` AND `git diff HEAD~1..HEAD -- el-templo-bot/test/v5-3-2-regression.test.ts | wc -l | tr -d ' '` returns `0`."
    - "NO modifications to any of the SIX canonical-invariant-block location-pairs: `.planning/phases/93-handler-concurrency/93-CONTEXT.md:84`, `.planning/phases/94-openai-latency-graceful-failure/94-CONTEXT.md:34`, `.planning/phases/95-booking-reliability-graceful-degradation/95-CONTEXT.md:37`, `.planning/ROADMAP.md:62`, `.planning/ROADMAP.md:93`, `.planning/MACRO-ROADMAP.md:99`. The pre-commit sha256 drift sentry confirms all six hash to `67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344`. Per user-MEMORY rule `feedback_multi_doc_invariant_hash_check.md`."

  artifacts:
    - path: "el-templo-bot/src/webhook/handler.ts"
      provides: 'DEGR-01 retry counter + escalation flow — `let failedToolCalls = 0` local declaration inside `processWithAiInner`, `const HANDOFF_ESCALATION_PHRASE` module-level locked phrase, `FAILURE_ALLOWLIST` static set + dynamic `startsWith(''Herramienta "'')` discriminator, per-tool-call increment for throw + return-string failure modes, single `failedToolCalls >= 2` threshold check that invokes `sendTextMessage(phone, HANDOFF_ESCALATION_PHRASE)` + synthetic `executeTool(''request_human'', { reason: ''auto_escalation_after_2_failures'' }, ...)` + sets `humanTakeoverTriggered = true` + early-returns to suppress model `segments[0]` per CONTEXT.md D-08, + Pino `log.warn` per CONTEXT.md D-09.'
      contains: "failedToolCalls, HANDOFF_ESCALATION_PHRASE, FAILURE_ALLOWLIST, auto_escalation_after_2_failures, DEGR-01 escalation triggered, Te paso con alguien del equipo, te escriben enseguida 🙌"
    - path: "el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts"
      provides: "Six test scenarios (SC-A escalation triggers at count=2, SC-B granularity-no-reset, SC-C allowlist 4-string coverage, SC-D throw-path increment, SC-E SC#3 soft-rejection negative, SC-F degraded-UX-NOT-allowlist negative) authored as RED against current master, observed failing, then GREEN after the handler.ts commit. Uses vi.doMock for `../src/ai/provider`, `../src/ai/tools`, `../src/whatsapp/client`, `../src/redis`, `../src/memory/profile`, `../src/memory/playbook-state`, `../src/state/machine`, `../src/playbooks/*` — same mocking pattern as `el-templo-bot/test/v5-3-3-openai-latency.test.ts`."
      min_lines: 200
      contains: "v5-3-3-degr-01-escalation, HANDOFF_ESCALATION_PHRASE, FAILURE_ALLOWLIST, failedToolCalls, auto_escalation_after_2_failures, lo pienso, Esa clase esta llena, Te paso con alguien del equipo"

  key_links:
    - from: "el-templo-bot/src/webhook/handler.ts (processWithAiInner tool-loop region)"
      to: "el-templo-bot/src/ai/tools.ts (executeTool throw + return-string failure modes)"
      via: 'FAILURE_ALLOWLIST static Set lookup + `startsWith(''Herramienta "'')` discriminator on toolResult; try/catch wrapper on executeTool() invocation'
      pattern: "FAILURE_ALLOWLIST\\.has|startsWith\\(['\"]Herramienta"
    - from: "el-templo-bot/src/webhook/handler.ts (escalation block)"
      to: "el-templo-bot/src/whatsapp/client.ts (sendTextMessage)"
      via: "synthetic locked-phrase emission BEFORE synthetic executeTool invocation"
      pattern: "sendTextMessage\\(phone, HANDOFF_ESCALATION_PHRASE\\)"
    - from: "el-templo-bot/src/webhook/handler.ts (escalation block)"
      to: "el-templo-bot/src/ai/tools.ts (executeTool — request_human handler at tools.ts:512-531)"
      via: "synthetic executeTool('request_human', { reason: 'auto_escalation_after_2_failures' }, ...) invocation NOT triggered by the model"
      pattern: "executeTool\\([\"']request_human[\"'],\\s*\\{\\s*reason:\\s*[\"']auto_escalation_after_2_failures[\"']"
    - from: "el-templo-bot/src/webhook/handler.ts (escalation block)"
      to: "el-templo-bot/src/webhook/handler.ts (existing humanTakeoverTriggered exit path at :659 + :928-950)"
      via: "shared local boolean — synthetic-path sets the same flag the model-emitted path sets; early-return preempts segments[0] emission"
      pattern: "humanTakeoverTriggered = true"
    - from: "el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts"
      to: ".planning/phases/95-booking-reliability-graceful-degradation/95-CONTEXT.md D-05..D-18"
      via: "six test scenarios encode the locked decisions one-to-one; SC-A=D-05/06/07/08/13, SC-B=D-12, SC-C=D-11(b), SC-D=D-11(a)+D-15+D-18, SC-E=D-14, SC-F=D-11 boundary"
      pattern: "SC-A|SC-B|SC-C|SC-D|SC-E|SC-F|DEGR-01|D-05|D-11"
---

<objective>
**Mechanical encoding** of CONTEXT.md D-05..D-18 (plus D-09) for DEGR-01 + DEGR-02 closure. This plan ships:

1. **Retry counter** — `let failedToolCalls = 0` local inside `processWithAiInner` (handler.ts:659 region, mirrors `humanTakeoverTriggered` pattern). Per-handler-invocation scope per CONTEXT.md D-10.
2. **Failure-trigger allowlist** — `FAILURE_ALLOWLIST: ReadonlySet<string>` of the 3 static byte-exact strings from tools.ts:806/:968/:271 + a `startsWith('Herramienta "')` discriminator for the tools.ts:264 default-case dynamic string. Per CONTEXT.md D-11.
3. **Per-tool-call increment** for BOTH throw (caught by handler-side try/catch wrapper around `executeTool`) AND return-string-on-allowlist (classified via `isFailureToolResult(result)`). Per CONTEXT.md D-12 — no reset on intermediate success.
4. **Threshold check + escalation** — single `if (failedToolCalls >= 2)` triggers (a) Pino `log.warn` per CONTEXT.md D-09, (b) `sendTextMessage(phone, HANDOFF_ESCALATION_PHRASE)` per CONTEXT.md D-05, (c) synthetic `executeTool('request_human', { reason: 'auto_escalation_after_2_failures' }, ...)` per CONTEXT.md D-07, (d) `humanTakeoverTriggered = true` per CONTEXT.md D-07, (e) early-`return` to suppress model `segments[0]` per CONTEXT.md D-08.
5. **Locked handoff phrase** — `const HANDOFF_ESCALATION_PHRASE = "Te paso con alguien del equipo, te escriben enseguida 🙌"` module-level constant in `handler.ts`. Byte-exact, including 🙌 emoji and lowercase 'equipo'. Per CONTEXT.md D-06.
6. **Six test scenarios** in a new bot-side unit test file `el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts` covering: (A) escalation triggers at count=2, (B) no-reset on intermediate success per D-12 granularity, (C) all 4 allowlist entries increment (including the dynamic default-case string), (D) throw-path increment per D-11(a), (E) SC#3 soft-rejection negative per D-14 (pre-empts Phase 97 RGUARD-02), (F) degraded-UX strings NOT on allowlist negative per D-11 boundary.

**This plan is mechanical, not investigative.** The audit (95-AUDIT.md) is the contract for BUG-03 (Plan 95-02). CONTEXT.md D-05..D-18 are the contract for BUG-05 (this plan). The executor has NO decisions to make beyond Claude's Discretion items explicitly enumerated below (FAILURE_ALLOWLIST representation — `ReadonlySet<string>` vs inline `const` array, `isFailureToolResult` helper vs inline expression, test file naming — chose `v5-3-3-degr-01-escalation.test.ts`, atomic commit cadence — chose 2 commits RED+GREEN per CONTEXT.md D-21).

**In-scope deliverables:**

1. `el-templo-bot/src/webhook/handler.ts` — modifications described above. ALL of the counter declaration, allowlist constants, helper predicate, try/catch around `executeTool`, return-string classification, threshold check, escalation block (log.warn → sendTextMessage → synthetic executeTool → humanTakeoverTriggered → early-return) lands here. Mirrors the existing `humanTakeoverTriggered` flow at handler.ts:659/:692-694/:928-950 — does NOT introduce a parallel flag.
2. `el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts` — new file, 6 test scenarios (SC-A..SC-F), mocking pattern mirrors `v5-3-3-openai-latency.test.ts` (vi.doMock for provider/tools/whatsapp/redis/memory/playbook + vi.mock for pino).

**Explicitly OUT of scope** (per CONTEXT.md `<deferred>` and `<scope_locked>`):

- ANY change to `el-templo-bot/src/ai/system-prompt.ts` — Phase 96 territory; would invalidate `POST_RLOK_04_BYTES = 18370` and `pb1-e1a-lead-rendered.snap.txt`.
- ANY change to `el-templo-bot/src/ai/tools.ts` — the 4 allowlist strings live there already; this plan is a pure consumer.
- ANY change to `el-templo-bot/src/ai/with-timeout.ts` — `ToolTimeoutError` shipped in 95-02 e213ee80.
- Per-conversation Redis-backed retry counter (CONTEXT.md `<deferred>` — v5.4+ scope).
- Per-tool-name granularity (CONTEXT.md `<deferred>` — v5.4+ scope).
- Extracting `HANDOFF_ESCALATION_PHRASE` to a shared phrases module (CONTEXT.md D-06 — deferred until a second consumer emerges).
- Refactoring the existing `humanTakeoverTriggered` flow at handler.ts:928-950 to unify with the synthetic-phrase path (CONTEXT.md `<deferred>` — v5.4+ if duplication becomes visible).
- Plan 95-04 or beyond (Phase 95 has exactly 3 plans per ROADMAP.md and CONTEXT.md D-04).

**Plan-checker mode: NORMAL** (per CONTEXT.md D-22). This plan is mechanical, not investigative — locked decisions D-05..D-18 are the contract. Structural integrity (XML/markup tag balance) MUST be in framing per the locked feedback rule (`memory/feedback_plan_checker_structural_integrity.md`).

**TDD discipline** (per CONTEXT.md D-20): the RED test commit observes failing tests against current master BEFORE the GREEN handler.ts commit lands. After GREEN, the RED tests now PASS without modification. The RED → GREEN transition is recorded empirically in the `<human-check>` checklist.

**Atomic commit cadence** (per CONTEXT.md D-21): two atomic commits — `test(95-03): RED — DEGR-01 escalation counter + synthetic phrase` followed by `feat(95-03): retry counter + HANDOFF_ESCALATION_PHRASE + request_human escalation`. No SUMMARY.md commit — that is owned by the future Phase 95 milestone wrap-up plan.

**Stop point** (downstream consumer contract): after plan-checker passes and this PLAN.md commits, STOP. The user reviews the plan before authorizing `/gsd:execute-phase 95 --plan 03`.

Purpose: close DEGR-01 + DEGR-02 (BUG-05 safety net) so Phase 95 ships its three deliverables (BOOK-01 closed by 95-01+95-02, DEGR-01+DEGR-02 closed here). Plan 95-03 consumes the artifacts shipped in 95-02 (the `'Herramienta agotada por tiempo de espera.'` allowlist return + the `ToolTimeoutError` class used only by SC-D) — does NOT modify them.

Output: `el-templo-bot/src/webhook/handler.ts` (modified), `el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts` (new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/95-booking-reliability-graceful-degradation/95-CONTEXT.md
@.planning/phases/95-booking-reliability-graceful-degradation/95-AUDIT.md
@.planning/phases/95-booking-reliability-graceful-degradation/95-01-PLAN.md
@.planning/phases/95-booking-reliability-graceful-degradation/95-02-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MACRO-ROADMAP.md
@CLAUDE.md
@el-templo-bot/CLAUDE.md

# Modification target (the SOLE production source file Plan 95-03 mutates)

@el-templo-bot/src/webhook/handler.ts

# Read-only consumer surface (NOT mutated by this plan — only consumed via allowlist + ToolTimeoutError import in test)

@el-templo-bot/src/ai/tools.ts
@el-templo-bot/src/ai/with-timeout.ts
@el-templo-bot/src/whatsapp/client.ts

# Test scaffolding precedent — vi.doMock pattern mirrored in the new SC-A..SC-F test file

@el-templo-bot/test/v5-3-3-openai-latency.test.ts
@el-templo-bot/test/v5-3-3-handler-concurrency.test.ts

# Plan 95-02 — sha256 drift sentry recipe + 2-commit atomic cadence precedent reused

@.planning/phases/94-openai-latency-graceful-failure/94-02-PLAN.md

<interfaces>
<!-- Key identifiers and signatures the executor needs. Extracted from the codebase via Read. -->
<!-- Executor should use these directly — no codebase re-exploration needed. -->

From `el-templo-bot/src/webhook/handler.ts:659` (existing `humanTakeoverTriggered` declaration — pattern Plan 95-03's counter mirrors):

```
let iterations = 0;
let humanTakeoverTriggered = false;     // <— Plan 95-03 declares `let failedToolCalls = 0;` adjacent to this line
let lastToolResult = "";
```

From `el-templo-bot/src/webhook/handler.ts:676-694` (the tool-loop body — Plan 95-03's increment + threshold-check site):

```
for (const toolCall of response.toolCalls) {
  log.info(
    { tool: toolCall.name, args: toolCall.arguments, conversationId },
    "Executing tool call",
  );

  const toolResult = await executeTool(           // <— Plan 95-03 wraps THIS in a try/catch
    toolCall.name,
    toolCall.arguments,
    db,
    conversationId,
    { phone, clientState },
  );

  lastToolResult = toolResult;                    // <— Plan 95-03 inserts isFailureToolResult(toolResult) check AFTER this line

  if (toolCall.name === "request_human") {        // <— existing model-emitted path; preserved unchanged
    humanTakeoverTriggered = true;
  }

  messages.push({                                 // <— existing tool-result append; Plan 95-03's threshold check fires AFTER this push
    role: "tool",
    content: toolResult,
    toolCallId: toolCall.id,
    name: toolCall.name,
  });
                                                  // <— Plan 95-03 inserts: if (failedToolCalls >= 2) { ... escalation block ... return; }
}
```

From `el-templo-bot/src/webhook/handler.ts:928-950` (existing humanTakeoverTriggered exit path — preserved unchanged for the model-emitted-request_human path; Plan 95-03's escalation block uses an early-return BEFORE reaching this branch to suppress model `segments[0]`):

```
if (humanTakeoverTriggered) {
  const handoffSegment = segments[0];
  // ... existing handoff segment send + suppression logic — unchanged
  return;
}
```

From `el-templo-bot/src/ai/tools.ts:242-274` (post-95-02 `executeTool` shape — confirms the four allowlist strings exist on master at HEAD; Plan 95-03 does NOT mutate this file):

```
export async function executeTool(
  name: string,
  args: Record<string, unknown>,
  db: DB,
  conversationId?: number,
  context?: { phone: string; clientState: ClientState },
): Promise<string> {
  try {
    switch (name) {
      case "check_schedule":   return await checkSchedule(db, args);
      case "check_membership": return await checkMembership(db, args);
      case "get_location":     return await getLocation(db, args);
      case "request_human":    return await requestHuman(db, args, conversationId);  // <— Plan 95-03 invokes this synthetically on escalation
      case "book_class":       return await bookClass(db, args, context);
      case "register_trial":   return await registerTrial(db, args, context);
      default:                 return `Herramienta "${name}" no disponible.`;        // <— allowlist entry (4) — dynamic; matched via startsWith
    }
  } catch (err: unknown) {
    if (err instanceof ToolTimeoutError) {
      return "Herramienta agotada por tiempo de espera.";                            // <— allowlist entry (3) — shipped in 95-02 e213ee80
    }
    throw err;
  }
}
```

Other allowlist entries (read-only references for Plan 95-03's FAILURE_ALLOWLIST Set):

- `el-templo-bot/src/ai/tools.ts:806` (bookClass fail): `'No pude completar la reserva. Intenta de nuevo en un momento.'` ← allowlist entry (1)
- `el-templo-bot/src/ai/tools.ts:968` (registerTrial fail): `'No pude completar el registro. Intenta de nuevo en un momento.'` ← allowlist entry (2)

Explicitly NOT on the allowlist (degraded-UX strings — pinned in CONTEXT.md D-11 and exercised by SC-F):

- `el-templo-bot/src/ai/tools.ts` near :685 (class-full no-alternatives): `'Esa clase esta llena y no hay alternativas disponibles esta semana.'`
- `el-templo-bot/src/ai/tools.ts` near :667 (already-booked guard): `'Ya tenes una reserva para esa clase. Te esperamos!'`
- `el-templo-bot/src/ai/tools.ts` near :842 (trial-used guard): `'Ya tuviste una clase de prueba con nosotros...'`

From `el-templo-bot/src/whatsapp/client.ts:59-62` (sendTextMessage signature — invoked from the escalation block):

```
export async function sendTextMessage(
  phone: string,
  text: string,
): Promise<string>
```

From `el-templo-bot/src/ai/with-timeout.ts` (shipped in 95-02 — confirms `ToolTimeoutError` export name + signature):

```
export class ToolTimeoutError extends Error {
  constructor(message = "Tool operation timed out") {
    super(message);
    this.name = "ToolTimeoutError";
  }
}

export async function withTimeout<T>(
  operation: (signal: AbortSignal) => Promise<T>,
  ms: number,
): Promise<T>
```

(SC-D imports `ToolTimeoutError` from this path for the throw-path increment test — but the handler-side counter never directly references this class; the catch already lives inside `executeTool` per the 95-02 e213ee80 commit.)

From `el-templo-bot/src/webhook/handler.ts:13` (logger type — `log: FastifyBaseLogger` parameter on `processWithAiInner`):

```
import type { FastifyBaseLogger } from "fastify";
// ... processWithAiInner(db, log: FastifyBaseLogger, conversationId, phone, ...)
// `log.warn({ ... }, "DEGR-01 escalation triggered")` is the CONTEXT.md D-09 observability surface
```

Target shape for `el-templo-bot/src/webhook/handler.ts` post-Task-2 changes (executor produces; no fenced code in `<action>` per anti-shallow rules):

- Module-level const additions near top of file:
  - `const HANDOFF_ESCALATION_PHRASE = "Te paso con alguien del equipo, te escriben enseguida 🙌";`
  - `const FAILURE_ALLOWLIST: ReadonlySet<string> = new Set([` (3 entries: bookClass-fail, registerTrial-fail, executeTool-timeout-return).
  - `function isFailureToolResult(result: string): boolean { return FAILURE_ALLOWLIST.has(result) || (result.startsWith('Herramienta "') && result.endsWith('" no disponible.')); }`
- Inside `processWithAiInner`, adjacent to `let humanTakeoverTriggered = false;`:
  - `let failedToolCalls = 0;`
  - `let lastToolName = "";` (for the D-09 log shape)
  - `let lastToolError = "";` (for the D-09 log shape)
- Inside the tool-loop `for (const toolCall ...)` body:
  - The `executeTool(...)` call is wrapped in `try { ... } catch (err: unknown) { failedToolCalls++; lastToolName = toolCall.name; lastToolError = err instanceof Error ? err.message : String(err); throw err; }`.
  - AFTER the existing `lastToolResult = toolResult;` line: `if (isFailureToolResult(toolResult)) { failedToolCalls++; lastToolName = toolCall.name; lastToolError = toolResult; }`.
  - AFTER the existing `messages.push({ role: "tool", ... });` line: `if (failedToolCalls >= 2) { log.warn({ phone, conversationId, failureCount: failedToolCalls, lastToolName, lastToolError }, "DEGR-01 escalation triggered"); await sendTextMessage(phone, HANDOFF_ESCALATION_PHRASE); await executeTool("request_human", { reason: "auto_escalation_after_2_failures" }, db, conversationId, { phone, clientState }); humanTakeoverTriggered = true; return; }`. The `return` exits `processWithAiInner` entirely — preempts the post-loop `splitMessage(replyText)` + `segments[0]` emission per CONTEXT.md D-08.

</interfaces>

<cross_phase_invariant>
**Read-only sentry block** — Plan 95-03 mutates NONE of the SIX canonical-invariant location-pairs.

```
.planning/phases/93-handler-concurrency/93-CONTEXT.md:84
.planning/phases/94-openai-latency-graceful-failure/94-CONTEXT.md:34
.planning/phases/95-booking-reliability-graceful-degradation/95-CONTEXT.md:37
.planning/ROADMAP.md:62
.planning/ROADMAP.md:93
.planning/MACRO-ROADMAP.md:99
```

All six pairs hash to:

```
67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344
```

If ANY location reports DRIFT during the pre-commit verification step, the executor HALTS before committing and reports the drift to the user. Per user-MEMORY rule `feedback_multi_doc_invariant_hash_check.md` (same formula/rule across N docs → byte-verify with sha256, paraphrase = drift waiting to happen).
</cross_phase_invariant>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — author six failing test scenarios (SC-A..SC-F) in a new bot-side unit test file; observe failing on master; commit</name>
  <files>
    el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts
  </files>
  <read_first>
    - `el-templo-bot/src/webhook/handler.ts` lines 427-694 — full `processWithAiInner` function body including the tool-loop region (this is the implementation surface the tests assert against).
    - `el-templo-bot/src/webhook/handler.ts` lines 144-371 — `handleInboundMessage` (the public entry point the tests invoke via dynamic `await import('../src/webhook/handler')`).
    - `el-templo-bot/test/v5-3-3-openai-latency.test.ts` (FULL file ~570 lines) — canonical precedent for the vi.doMock pattern: provider mock, tools mock, whatsapp client mock with sendCalls spy array, redis mock with redisStore Map, pino mock, makeMockDb, makeMessage, makeMockLog helpers. Copy these helpers verbatim into the new test file (or factor a shared helper module if minimal — Claude's Discretion).
    - `el-templo-bot/test/v5-3-3-handler-concurrency.test.ts` (FULL file ~485 lines) — second precedent for the bot-side handler-loop test pattern (provider.chat capture, beforeEach reset, vi.resetModules).
    - `el-templo-bot/src/ai/tools.ts` lines 242-274 — confirms the four allowlist strings byte-for-byte (the test mocks `executeTool` so the strings need to appear in the TEST file, not be imported from tools.ts — this prevents test/source coupling drift but executor SHOULD verify the test's string literals match the tools.ts source byte-for-byte at authoring time).
    - `el-templo-bot/src/ai/with-timeout.ts` (FULL file, ~54 lines) — confirms `ToolTimeoutError` export shape. SC-D imports this class for the throw-path test (NOT for the handler — for direct construction in the mocked `executeTool` to verify the increment-on-non-ToolTimeoutError-throw behavior).
    - `.planning/phases/95-booking-reliability-graceful-degradation/95-CONTEXT.md` D-05 through D-18 (lines 60-95) — full spec for all six scenarios. Cross-reference each SC-X to its source D-XX decision in the test's `describe`/`it` labels.
    - `.planning/phases/95-booking-reliability-graceful-degradation/95-CONTEXT.md` `<specifics>` section (lines 200-211) — locks: 'The exact handoff phrase is locked verbatim. Any deviation (dropping emoji, capitalizing Equipo) triggers a regression.'
    - `el-templo-bot/CLAUDE.md` — bot test-file conventions (vitest, vi.doMock for per-test isolation, no `console.*`, no `: any`).
  </read_first>
  <behavior>
    Author a NEW unit test file `el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts` containing the six test scenarios (SC-A..SC-F) described in must_haves.truths. ALL six scenarios MUST FAIL when run against current master (the post-95-02 codebase at the branch HEAD before Task 2's commit). Failure shape: each `it()` block asserts an observable side-effect (sendTextMessage spy contains the locked phrase, executeTool spy contains the synthetic request_human invocation, etc.) that CANNOT be produced by the master codebase because the counter + escalation block do not yet exist. The RED state is the empirical proof that the GREEN commit's diff is necessary.

    **Test file structure** (executor adapts the v5-3-3-openai-latency.test.ts skeleton):

    1. **Imports + mock setup** at file top:
       - `import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';`
       - `import { ToolTimeoutError } from '../src/ai/with-timeout';` (used only in SC-D — see note below)
       - `import type { ParsedInboundMessage } from '../src/whatsapp/types';`
       - vi.mock('../src/redis', ...) — mirrors v5-3-3-openai-latency.test.ts lines 249-268.
       - vi.mock('pino', ...) — mirrors v5-3-3-openai-latency.test.ts lines 270-281.

    2. **Spy arrays at module scope** (reset in beforeEach):
       - `let sendCalls: string[] = [];` (captures every `sendTextMessage(phone, text)` call's text argument)
       - `let executeToolCalls: Array<{ name: string; args: Record<string, unknown> }> = [];` (captures every `executeTool(name, args, ...)` invocation — including the synthetic `request_human` one)
       - `let providerChatCalls: number = 0;` (captures iteration count for the loop-exit assertion)

    3. **vi.doMock helpers** (mirror v5-3-3-openai-latency.test.ts setupSurroundingMocks):
       - `../src/ai/provider` — `createAiProvider()` returns `{ chat: async () => ({ content, toolCalls }) }` where the response shape is CONTROLLED per-test via a module-level `nextChatResponses: Array<...> = [];` queue. Each call to `chat()` shifts the next response, increments `providerChatCalls`.
       - `../src/ai/tools` — `BOT_TOOLS: []`, `resolvePendingAction: async () => null`, `executeTool: async (name, args, ...) => { executeToolCalls.push({ name, args }); return nextToolResults.shift() ?? ""; }` where `nextToolResults: Array<string | Error> = [];` is the per-test queue. If the shifted value is an Error instance, the mock THROWS it instead of returning it (this is how SC-D exercises the throw-path increment).
       - `../src/whatsapp/client` — `sendTextMessage: async (_phone, text) => { sendCalls.push(text); return 'wamid_' + sendCalls.length; }`, `sendInteractiveMessage: async () => 'wamid_interactive';`.
       - `../src/ai/system-prompt` — `getSystemPrompt: () => 'TEST'`.
       - `../src/memory/profile`, `../src/memory/playbook-state`, `../src/state/machine`, `../src/playbooks/resolver`, `../src/playbooks/advance`, `../src/playbooks/profile-tag` — return-noop mocks per v5-3-3-openai-latency.test.ts lines 312-343.

    4. **makeMockDb + makeMessage + makeMockLog helpers** — copy from v5-3-3-openai-latency.test.ts lines 346-411 verbatim.

    5. **`describe('Plan 95-03 / DEGR-01 — escalation counter + synthetic phrase')` with six `it()` (or describes containing it()s) per the SC-A..SC-F shapes above.**

       Each `it()` block:
       a. Calls `vi.resetModules()` in beforeEach + re-applies all vi.doMock calls.
       b. Pre-populates `nextChatResponses` and `nextToolResults` per the scenario's mock plan.
       c. `const { handleInboundMessage } = await import('../src/webhook/handler');`
       d. `await handleInboundMessage(makeMessage('5491100000000', 'test inbound', 'wamid_in_1'), makeMockDb(), makeMockLog());` (or however the post-95-02 handler signature requires — read handler.ts to confirm).
       e. Assertions per the SC-X spec (see must_haves.truths for the full set).

    6. **CRITICAL — RED observation step before commit:**
       After authoring, the executor runs `cd el-templo-bot && pnpm test --run test/v5-3-3-degr-01-escalation.test.ts 2>&1 | tee /tmp/95-03-c01-red.log | tail -40` and confirms ALL six scenarios FAIL on current master (the post-95-02 HEAD). Expected failure mode varies per scenario — likely a mix of:
       - "Expected `sendCalls` to contain the locked phrase, but `sendCalls === []`" (SC-A, SC-B, SC-C variants 1-4 — the synthetic phrase isn't emitted by current master).
       - "Expected `executeToolCalls` to contain a `request_human` invocation, but it does not" (SC-A, SC-B, SC-C, SC-D).
       - "Expected providerChatCalls === 2 (loop exits at threshold), got 5 (full MAX_TOOL_ITERATIONS exhaustion)" (SC-A, SC-B — counter doesn't exist so the loop runs to MAX).
       - SC-E and SC-F may PASS coincidentally on master because their assertions are "the synthetic phrase is NOT emitted" — which is also true on master because no synthetic phrase logic exists. These two tests are still RED-shaped in the sense that they encode a GUARANTEE about post-95-03 behavior, but their observed status on master is PASS-by-vacuous-truth. Executor records this in /tmp/95-03-c01-red.log notes and proceeds — the GREEN commit's job is to make them STILL pass after the counter+escalation block is added (i.e., to verify the SC#3 + degraded-UX boundaries are correctly drawn).

       The expected RED-state observation is: at minimum 4 of the 6 scenarios are observed RED, and the executor records the specific failure modes for each. The RED commit is then made.

    7. **Test file constraints (project discipline):**
       - Zero `console.*` — use `log` mock or vitest stdout per existing precedent.
       - Zero `: any`, `as any`, `<any>` — `unknown` + narrowing only.
       - Zero modifications to any other file. Strictly additive: one new file, no edits elsewhere.

    **Why this is a single atomic RED commit (CONTEXT.md D-21):**

    The six scenarios share the same mock scaffolding (setupSurroundingMocks, makeMockDb, etc.) and the same import statement for `handleInboundMessage`. Splitting them across multiple RED commits would require duplicating the scaffolding or creating a shared test-helper module — over-engineered for a single-file new test surface. One RED commit + one GREEN commit mirrors the 94-02 cadence.

    After this commit lands:
    - `cd el-templo-bot && pnpm tsc --noEmit` exits 0 (the new test file is type-clean).
    - `cd el-templo-api && pnpm tsc --noEmit` exits 0 (unchanged).
    - `cd el-templo-bot && pnpm test --run test/v5-3-3-degr-01-escalation.test.ts; echo "EXIT=$?"` reports `EXIT≠0` (the RED state — at least one failing test causes vitest to exit non-zero). Manual verification step in `<human-check>`; this assertion is observable without parsing vitest stderr and therefore robust against ANSI colour codes. Per STATE.md carry-forward §F-1 deprecation.
    - No other test file changes; the deferred (iii) Sunday=0 RED in `test/v5-3-3-booking-reliability.test.ts` continues to fail as before.

  </behavior>
  <action>
    Execute in order:

    1. Re-read `el-templo-bot/src/webhook/handler.ts` lines 427-694 to confirm the post-95-02 shape of `processWithAiInner` and the tool-loop region. Record in working memory the exact lines where `humanTakeoverTriggered` is declared (`:659`), where it is set (`:692-694`), and where the tool-result is appended (`:696-701`).

    2. Re-read `el-templo-bot/test/v5-3-3-openai-latency.test.ts` (full file, ~570 lines) to lock in the mock pattern — note the exact shape of `vi.doMock('../src/ai/tools', ...)`, `vi.doMock('../src/whatsapp/client', ...)`, `makeMockDb()`, `makeMessage()`, `makeMockLog()`.

    3. Create `el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts` with:
       - File header comment naming the plan + scenario taxonomy (SC-A..SC-F mapped to D-05..D-18).
       - Imports (vitest, ToolTimeoutError for SC-D, ParsedInboundMessage type).
       - Module-level mock-state arrays (sendCalls, executeToolCalls, providerChatCalls, nextChatResponses, nextToolResults).
       - vi.mock('../src/redis', ...) and vi.mock('pino', ...) at module scope.
       - Helper functions: makeMockDb (copy from v5-3-3-openai-latency.test.ts), makeMessage, makeMockLog, setupSurroundingMocks (factored so each `it()` block calls it after `vi.resetModules()`).
       - Six test scenarios SC-A through SC-F as `it()` blocks (or a parent `describe` with nested `describe`s per the parameterized SC-C structure — Claude's Discretion). Each `it()` label MUST include the scenario tag (SC-A, SC-B, etc.) AND the source D-XX decision reference.
       - All allowlist string literals byte-equal to the tools.ts source — confirmed by reading tools.ts at authoring time.

    4. Run TypeScript compile: `cd el-templo-bot && pnpm tsc --noEmit`. MUST exit 0. If errors, fix the test file ONLY (do NOT modify production source).

    5. Run the new test file in isolation to observe the RED state:
       ```
       cd el-templo-bot && pnpm test --run test/v5-3-3-degr-01-escalation.test.ts 2>&1 | tee /tmp/95-03-c01-red.log | tail -60
       ```
       Confirm: at minimum 4 of the 6 scenarios fail. Record the exact failure mode of each in /tmp/95-03-c01-red.log (kept by `tee`).

    6. Run discipline grep on the new file:
       ```
       grep -cE 'console\.|: any\b|<any[,>]|as any\b' el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts | tr -d ' '
       ```
       MUST equal `0`.

    7. Negative diff guards:
       ```
       git status --short
       ```
       MUST show EXACTLY ONE new file (`?? el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts`). Any modification (`M` line) to any other file = halt.

       ```
       git diff HEAD -- el-templo-bot/src/webhook/handler.ts el-templo-bot/src/ai/tools.ts el-templo-bot/src/ai/with-timeout.ts el-templo-bot/src/ai/system-prompt.ts el-templo-bot/test/v5-3-2-regression.test.ts
       ```
       MUST be empty.

    8. Verify the SIX canonical-invariant pairs sha256-clean (lift the recipe verbatim from `.planning/phases/94-openai-latency-graceful-failure/94-02-PLAN.md:305-326` — see also Task 2 step 4):
       ```
       cd /Users/bores/el-templo
       extract_block() { awk -v s="$2" 'NR>=s {print; if (/Minimum TTL =/) exit}' "$1"; }
       expected="67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344"
       fail=0
       for spec in \
         ".planning/phases/93-handler-concurrency/93-CONTEXT.md:84" \
         ".planning/phases/94-openai-latency-graceful-failure/94-CONTEXT.md:34" \
         ".planning/phases/95-booking-reliability-graceful-degradation/95-CONTEXT.md:37" \
         ".planning/ROADMAP.md:62" \
         ".planning/ROADMAP.md:93" \
         ".planning/MACRO-ROADMAP.md:99"; do
         f="${spec%:*}"; l="${spec##*:}"
         got=$(extract_block "$f" "$l" | shasum -a 256 | cut -d' ' -f1)
         if [ "$got" = "$expected" ]; then
           echo "OK   $spec = $got"
         else
           echo "DRIFT $spec = $got (expected $expected)" >&2
           fail=1
         fi
       done
       [ "$fail" -eq 0 ]
       ```
       Expected: 6 `OK` lines, exit 0. If ANY DRIFT, halt before committing.

    9. Commit the new test file:
       ```
       git add el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts
       git commit -m "$(cat <<'EOF'

test(95-03): RED — DEGR-01 escalation counter + synthetic phrase

Phase 95 Plan 95-03 — RED test commit (per CONTEXT.md D-20 TDD
fail-in-main discipline + D-21 atomic commit cadence).

New file el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts with
six scenarios encoding CONTEXT.md D-05..D-18:

SC-A (POSITIVE): two failed tool calls in one inbound trigger the
synthetic phrase + synthetic request_human + humanTakeoverTriggered + segments[0] suppression. (D-05, D-06, D-07, D-08, D-13.)
SC-B (POSITIVE): no reset on intermediate success — failure (count=1) + success (count=1, NOT reset) + failure (count=2 -> ESCALATE).
(D-12.)
SC-C (POSITIVE x4): each of the 4 allowlist strings increments the
counter — bookClass-fail, registerTrial-fail, executeTool-timeout
(95-02 e213ee80 shipped), default-case dynamic
'Herramienta "{name}" no disponible.' (D-11.)
SC-D (POSITIVE): non-ToolTimeoutError throw from executeTool
increments via the handler-side try/catch wrapper. (D-11(a).)
SC-E (NEGATIVE / SC#3 guardrail): soft-rejection inbound ("lo pienso")
does NOT increment, does NOT emit synthetic phrase, does NOT
invoke synthetic request_human. Pre-empts Phase 97 RGUARD-02.
(D-14.)
SC-F (NEGATIVE / allowlist boundary): degraded-UX strings (e.g., "Esa
clase esta llena...") are NOT on the allowlist; two such returns
in one inbound do NOT trip the count=2 threshold. (D-11 boundary.)

Observed RED on current master (post-95-02 HEAD): at minimum 4 of 6
scenarios fail because the failedToolCalls counter, HANDOFF_ESCALATION_PHRASE
constant, FAILURE_ALLOWLIST, isFailureToolResult helper, and the
threshold-check escalation block do not yet exist in handler.ts. SC-E
and SC-F may pass by vacuous truth on master (no synthetic phrase logic
exists to violate their negative assertions) — they remain RED-shaped
guarantees about post-95-03 behavior.

Mocking pattern mirrors el-templo-bot/test/v5-3-3-openai-latency.test.ts
(vi.doMock for provider/tools/whatsapp/redis/memory/playbook + vi.mock
for pino). No production source modified. No other test file modified.
No system-prompt.ts touch (Phase 96 owns snapshot regen). No
tools.ts/with-timeout.ts touch (95-02 shipped those surfaces).

Next commit: feat(95-03) GREEN — the handler.ts changes that turn
SC-A..SC-D RED -> GREEN while preserving SC-E + SC-F as PASS.
EOF
)"

````

    10. Confirm post-commit:
        - `git log -1 --format=%s | grep -cE '^test\(95-03\): RED — DEGR-01 escalation counter \+ synthetic phrase' | tr -d ' '` → `1`.
        - `git show --stat HEAD --name-only HEAD | grep -cE '^el-templo-bot/test/v5-3-3-degr-01-escalation\.test\.ts$' | tr -d ' '` → `1`.
        - `git show --stat HEAD --name-only HEAD | grep -vE '^el-templo-bot/test/v5-3-3-degr-01-escalation\.test\.ts$|^$' | wc -l | tr -d ' '` → `0`.

  </action>
  <acceptance_criteria>
    - `test -f el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts` → exit 0
    - `grep -cE "Te paso con alguien del equipo, te escriben enseguida 🙌" el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts` → ≥ `1` (test asserts the byte-exact locked phrase)
    - `grep -cE 'auto_escalation_after_2_failures' el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts` → ≥ `1` (SC-A asserts the synthetic request_human reason)
    - `grep -cE "lo pienso|no estoy seguro|mañana te digo" el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts` → ≥ `1` (SC-E uses at least one soft-rejection phrase)
    - `grep -cE 'Esa clase esta llena' el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts` → ≥ `1` (SC-F uses the canonical degraded-UX example)
    - `grep -cE 'ToolTimeoutError' el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts` → ≥ `1` (SC-D imports the class — though it tests a non-ToolTimeoutError throw path per behavior block)
    - `grep -cE 'SC-A|SC-B|SC-C|SC-D|SC-E|SC-F' el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts` → ≥ `6` (each scenario tag appears at least once in describe/it labels)
    - `grep -cE 'console\.|: any\b|<any[,>]|as any\b' el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts | tr -d ' '` → `0`
    - `cd el-templo-bot && pnpm tsc --noEmit` exits 0
    - `cd el-templo-api && pnpm tsc --noEmit` exits 0
    - `cd el-templo-bot && pnpm test --run test/v5-3-3-degr-01-escalation.test.ts; echo "EXIT=$?"` reports `EXIT≠0` (the RED state — at least one failing test causes vitest to exit non-zero). Manual verification step in `<human-check>`; this assertion is observable without parsing vitest stderr and therefore robust against ANSI colour codes. Per STATE.md carry-forward §F-1 deprecation.
    - `git log -1 --format=%s | grep -cE '^test\(95-03\): RED — DEGR-01 escalation counter \+ synthetic phrase' | tr -d ' '` → `1`
    - `git show --stat HEAD --name-only HEAD | grep -cE '^el-templo-bot/test/v5-3-3-degr-01-escalation\.test\.ts$' | tr -d ' '` → `1`
    - `git show --stat HEAD --name-only HEAD | grep -vE '^el-templo-bot/test/v5-3-3-degr-01-escalation\.test\.ts$|^$' | wc -l | tr -d ' '` → `0` (no other files in this commit)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement retry counter + locked phrase + allowlist + threshold escalation in handler.ts; turn SC-A..SC-D from RED to GREEN; preserve SC-E + SC-F as PASS</name>
  <files>
    el-templo-bot/src/webhook/handler.ts
  </files>
  <read_first>
    - `el-templo-bot/src/webhook/handler.ts` lines 1-100 — module-level imports + constants (insertion site for `HANDOFF_ESCALATION_PHRASE`, `FAILURE_ALLOWLIST`, `isFailureToolResult`).
    - `el-templo-bot/src/webhook/handler.ts` lines 427-694 — `processWithAiInner` body including the tool-loop region. Re-confirm post-Task-1 state (Task 1 added only a test file; production source unchanged).
    - `el-templo-bot/src/webhook/handler.ts` lines 696-950 — post-tool-loop region including the existing `humanTakeoverTriggered` exit path at `:928-950` (preserved unchanged for the model-emitted-request_human path).
    - `el-templo-bot/src/ai/tools.ts` lines 242-274 — `executeTool` dispatch (read-only; do NOT modify).
    - `el-templo-bot/src/ai/tools.ts` lines 685-688 + 842-845 + 264 — the 4 allowlist source strings (byte-equality is REQUIRED for the Set lookup; copy verbatim into the FAILURE_ALLOWLIST literal).
    - `el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts` (the file just committed in Task 1) — read to confirm exactly what assertions the GREEN implementation must satisfy.
    - `.planning/phases/95-booking-reliability-graceful-degradation/95-CONTEXT.md` D-05..D-18 (lines 60-95) — the locked decisions.
  </read_first>
  <behavior>
    Implement the counter + allowlist + threshold-escalation logic in `el-templo-bot/src/webhook/handler.ts` such that ALL six test scenarios from Task 1 transition from RED to GREEN. The implementation is fully described in the `<interfaces>` "Target shape" block — executor follows that shape literally, with Claude's Discretion only on micro-decisions explicitly listed below.

    **Module-level additions (place near the existing module-level constants, e.g., adjacent to `MAX_TOOL_ITERATIONS = 5` at handler.ts:91 region):**

    1. `const HANDOFF_ESCALATION_PHRASE: string = "Te paso con alguien del equipo, te escriben enseguida 🙌";`
       - Byte-exact, including the 🙌 emoji (U+1F64C `RAISED HANDS`) and the lowercase 'equipo'. NO trailing whitespace.
       - The leading-`:` type annotation is OPTIONAL (Claude's Discretion — `: string` is explicit; omitting lets TS infer `'Te paso con...'` literal type). Either form passes the byte-equality grep test.

    2. `const FAILURE_ALLOWLIST: ReadonlySet<string> = new Set([` + 3 entries + `]);`
       - Entry 1: `'No pude completar la reserva. Intenta de nuevo en un momento.'` (byte-equal to tools.ts:806).
       - Entry 2: `'No pude completar el registro. Intenta de nuevo en un momento.'` (byte-equal to tools.ts:968).
       - Entry 3: `'Herramienta agotada por tiempo de espera.'` (byte-equal to tools.ts:271 — shipped in 95-02 e213ee80).
       - The dynamic default-case string (`'Herramienta "${name}" no disponible.'`) is NOT in this Set — it is matched via the `startsWith` discriminator inside `isFailureToolResult`.

    3. `function isFailureToolResult(result: string): boolean { return FAILURE_ALLOWLIST.has(result) || (result.startsWith('Herramienta "') && result.endsWith('" no disponible.')); }`
       - Single-line implementation; pure function; no side effects.
       - Claude's Discretion: this can also be written as an inline expression at the call site if the executor prefers. The grep tests assert behavior, not function declaration — both shapes pass.

    **Inside `processWithAiInner` (handler.ts:427-694 region):**

    4. Adjacent to the existing `let humanTakeoverTriggered = false;` declaration at handler.ts:659, add three new locals (placement: immediately AFTER `let humanTakeoverTriggered = false;` and BEFORE `let lastToolResult = "";`):
       - `let failedToolCalls = 0;`
       - `let lastToolName = "";`
       - `let lastToolError = "";`

    5. Wrap the existing `executeTool(...)` invocation at handler.ts:682-688 in a try/catch:
       - Before: `const toolResult = await executeTool(toolCall.name, toolCall.arguments, db, conversationId, { phone, clientState });`
       - After: a `let toolResult: string;` declared above, then `try { toolResult = await executeTool(toolCall.name, toolCall.arguments, db, conversationId, { phone, clientState }); } catch (err: unknown) { failedToolCalls++; lastToolName = toolCall.name; lastToolError = err instanceof Error ? err.message : String(err); throw err; }`.
       - The catch RE-THROWS the error — error propagation semantics are unchanged for non-counter purposes (the outer `processWithAi` -> `handleInboundMessage` try/catch at handler.ts:345-369 still handles it).

    6. After the existing `lastToolResult = toolResult;` line at handler.ts:690, add:
       ```
       if (isFailureToolResult(toolResult)) {
         failedToolCalls++;
         lastToolName = toolCall.name;
         lastToolError = toolResult;
       }
       ```

    7. AFTER the existing `messages.push({ role: "tool", content: toolResult, toolCallId: toolCall.id, name: toolCall.name });` block at handler.ts:696-701, add the threshold check:
       ```
       if (failedToolCalls >= 2) {
         log.warn(
           {
             phone,
             conversationId,
             failureCount: failedToolCalls,
             lastToolName,
             lastToolError,
           },
           "DEGR-01 escalation triggered",
         );
         await sendTextMessage(phone, HANDOFF_ESCALATION_PHRASE);
         await executeTool(
           "request_human",
           { reason: "auto_escalation_after_2_failures" },
           db,
           conversationId,
           { phone, clientState },
         );
         humanTakeoverTriggered = true;
         return;
       }
       ```
       The early `return;` exits `processWithAiInner` entirely — preempting the post-tool-loop `splitMessage(replyText) -> segments[0]` emission at handler.ts:923-950. Per CONTEXT.md D-08: 'the user receives exactly ONE outbound message for that turn: HANDOFF_ESCALATION_PHRASE.'

       Note on inbound persistence + session updates: per CONTEXT.md D-07/D-08 and the existing handler.ts:692-694 model-emitted-request_human path, the `humanTakeoverTriggered` flag is preserved for any pre-existing post-loop side effects that intentionally fire on takeover (none in current master beyond the segments[0] send + DB insert — both of which are correctly preempted by the early return). The synthetic `executeTool('request_human', ...)` invocation handles the `conversation_status = 'human_takeover'` DB write per `tools.ts:512-531`. Pino observability (`log.warn`) is the trigger record per CONTEXT.md D-09.

    **Existing model-emitted-request_human path (handler.ts:692-694 + :928-950) is PRESERVED UNCHANGED.** The synthetic-escalation path exits via the early `return` BEFORE reaching `:928-950`. The existing `if (toolCall.name === "request_human") { humanTakeoverTriggered = true; }` at `:692-694` is preserved (matches the model-emitting-request_human flow, which CAN coexist with the synthetic-escalation flow — e.g., the model emits request_human ON ITS OWN in a non-failure scenario; that path continues to use the segments[0] emission at :928-950).

    **TypeScript discipline:**
    - Zero `: any`, `as any`, `<any>`. All new locals are typed (`let failedToolCalls: number = 0` or just `let failedToolCalls = 0` with inferred type; `let lastToolName = ""` infers string).
    - Zero `console.*` — observability is via the `log: FastifyBaseLogger` parameter (already in scope via `processWithAiInner`'s signature).
    - All new `await` calls are inside the `async` function — no top-level awaits introduced.

    **Post-commit state:**
    - `cd el-templo-bot && pnpm tsc --noEmit` exits 0.
    - `cd el-templo-api && pnpm tsc --noEmit` exits 0.
    - `cd el-templo-bot && pnpm test --run test/v5-3-3-degr-01-escalation.test.ts` exits 0 (ALL 6 scenarios PASS — SC-A..SC-D transition from RED to GREEN; SC-E + SC-F remain PASS).
    - `cd el-templo-bot && pnpm test 2>&1 | tail -5` — the full bot test suite shows the pre-existing baseline (Phase 93 + Phase 94 + v5-3-2 regressions) still passing; the deferred (iii) Sunday=0 RED in `test/v5-3-3-booking-reliability.test.ts` continues to fail as before; no other regressions.
    - `cd el-templo-api && pnpm test --run test/whatsapp/v5-3-3-booking.integration.test.ts` exits 0 (9/9 GREEN — no regression from the handler change).

  </behavior>
  <action>
    Execute in order:

    1. Re-read `el-templo-bot/src/webhook/handler.ts` lines 1-100 + 427-700 to refresh the post-Task-1 state in working memory.

    2. Add the three module-level declarations (`HANDOFF_ESCALATION_PHRASE`, `FAILURE_ALLOWLIST`, `isFailureToolResult`) near the existing module-level constants (specifically: alongside or directly after the `MAX_TOOL_ITERATIONS` constant region around handler.ts:91). The relative ordering of the three is Claude's Discretion; recommended order is: `HANDOFF_ESCALATION_PHRASE` first (the public-facing constant), then `FAILURE_ALLOWLIST`, then `isFailureToolResult` (the helper depends on `FAILURE_ALLOWLIST`).

    3. Run `cd el-templo-bot && pnpm tsc --noEmit` after the module-level additions — MUST exit 0.

    4. Inside `processWithAiInner`, after `let humanTakeoverTriggered = false;` at :659, add `let failedToolCalls = 0;`, `let lastToolName = "";`, `let lastToolError = "";`.

    5. Wrap the existing `const toolResult = await executeTool(...)` at handler.ts:682-688 in a try/catch per behavior step 5. Convert `const toolResult` to `let toolResult: string;` declared above the try.

    6. AFTER the existing `lastToolResult = toolResult;` at :690, add the `if (isFailureToolResult(toolResult)) { ... }` block per behavior step 6.

    7. AFTER the existing `messages.push({ role: "tool", ... });` block ending around :701, add the threshold-check block per behavior step 7 (the `if (failedToolCalls >= 2) { ... return; }` block).

    8. Run `cd el-templo-bot && pnpm tsc --noEmit` after EACH substantive edit (module-level adds, locals add, try/catch wrap, classification check, threshold check). Each pass MUST exit 0.

    9. Run the SC-A..SC-F test file to confirm all six now PASS:
       ```
       cd el-templo-bot && pnpm test --run test/v5-3-3-degr-01-escalation.test.ts 2>&1 | tee /tmp/95-03-c02-green.log | tail -30
       ```
       Expected: `Tests 6 passed (6)` (or however the parameterized SC-C is reported — e.g., 9 passed if SC-C splits into 4 sub-its).

    10. Run the full bot test suite to confirm no regression:
        ```
        cd el-templo-bot && pnpm test 2>&1 | tee /tmp/95-03-c02-full-bot.log | tail -10
        ```
        Expected: the ONLY failing test in the entire bot package is the deferred (iii) Sunday=0 RED in `test/v5-3-3-booking-reliability.test.ts:55` (carried over from 95-02). All other suites pass at their previous green baselines.

    11. Run the full API test suite to confirm no regression from the handler change (no API source modified, but the integration tests exercise the bot via the webhook):
        ```
        cd el-templo-api && pnpm test --run test/whatsapp/v5-3-3-booking.integration.test.ts 2>&1 | tail -10
        ```
        Expected: `Tests 9 passed (9)`.

    12. Discipline grep on the modified file (only the NEW lines added by this commit):
        ```
        git diff HEAD -- el-templo-bot/src/webhook/handler.ts | grep -E '^\+' | grep -cE 'console\.|: any\b|<any[,>]|as any\b' | tr -d ' '
        ```
        MUST equal `0`.

    13. Negative diff guards:
        ```
        git diff --name-only HEAD
        ```
        MUST show ONLY `el-templo-bot/src/webhook/handler.ts`. Any other file = halt.

        ```
        git diff HEAD -- el-templo-bot/src/ai/system-prompt.ts el-templo-bot/src/ai/tools.ts el-templo-bot/src/ai/with-timeout.ts el-templo-bot/src/ai/openai.ts el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts el-templo-bot/test/v5-3-3-booking-reliability.test.ts el-templo-bot/test/v5-3-2-regression.test.ts el-templo-bot/.env.example el-templo-bot/scripts/check-debounce-invariant.sh el-templo-api/test/whatsapp/v5-3-3-booking.integration.test.ts
        ```
        MUST be empty.

    14. Verify the SIX canonical-invariant pairs are sha256-clean (re-run the recipe from Task 1 step 8). Expected: 6 `OK` lines, exit 0.

    15. Commit:
        ```
        git add el-templo-bot/src/webhook/handler.ts
        git commit -m "$(cat <<'EOF'

feat(95-03): retry counter + HANDOFF_ESCALATION_PHRASE + request_human escalation

Phase 95 Plan 95-03 — GREEN commit (per CONTEXT.md D-20 TDD
fail-in-main + D-21 atomic commit cadence). Turns SC-A..SC-D in
el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts from RED to
GREEN; preserves SC-E + SC-F as PASS. Closes DEGR-01 + DEGR-02.

Module-level additions to el-templo-bot/src/webhook/handler.ts:

- HANDOFF_ESCALATION_PHRASE: byte-exact locked string per
  CONTEXT.md D-06, including the 🙌 emoji and lowercase 'equipo'.
- FAILURE_ALLOWLIST: ReadonlySet<string> of the 3 static fail
  strings (tools.ts:806 bookClass-fail, tools.ts:968 registerTrial
  -fail, tools.ts:271 executeTool-timeout shipped in 95-02
  e213ee80). The 4th allowlist entry — the executeTool default-case
  dynamic 'Herramienta "{name}" no disponible.' — is discriminated
  via a separate startsWith/endsWith check.
- isFailureToolResult(result): single helper combining the static
  Set lookup + the dynamic-string discriminator. Per CONTEXT.md
  D-11.

Inside processWithAiInner, adjacent to the existing
humanTakeoverTriggered local (handler.ts:659 region):

- let failedToolCalls = 0; (D-10 per-handler-invocation scope —
  mirrors the humanTakeoverTriggered pattern; no Redis, no
  cross-inbound persistence)
- let lastToolName, lastToolError (captures for the D-09 log
  shape)

Tool-loop body changes:

- executeTool() invocation wrapped in try/catch. Throw branch:
  failedToolCalls++, capture lastToolName/lastToolError, re-throw
  the original error (existing error-propagation semantics
  preserved). Per CONTEXT.md D-11(a).
- AFTER lastToolResult = toolResult: isFailureToolResult(toolResult)
  check; on match: failedToolCalls++, capture lastToolName,
  lastToolError. Per CONTEXT.md D-11(b), D-12.
- AFTER messages.push({ role: 'tool', ... }): single threshold
  check `if (failedToolCalls >= 2) { ... }` triggers (D-13):
  1. log.warn({ phone, conversationId, failureCount: ...,
     lastToolName, lastToolError }, 'DEGR-01 escalation triggered')
     per CONTEXT.md D-09.
  2. sendTextMessage(phone, HANDOFF_ESCALATION_PHRASE) per D-05.
  3. await executeTool('request_human', { reason:
     'auto_escalation_after_2_failures' }, db, conversationId,
     { phone, clientState }) synthetically per D-07.
  4. humanTakeoverTriggered = true per D-07 (REUSES existing
     flag; does NOT introduce a parallel one).
  5. early return — preempts the post-loop segments[0] emission
     at :923-950 per D-08 (model response.content discarded).

Existing model-emitted-request_human path at :692-694 + :928-950
PRESERVED UNCHANGED. The synthetic-escalation path early-returns
BEFORE reaching :928-950; the segments-emission branch there
continues to serve the model-emits-request_human flow.

SC#3 / DEGR-02 guardrail (CONTEXT.md D-14): the retry counter and
Phase 91 whyAsked flag share NO state. Counter lives in the
tool-loop region (~:659-:701); whyAsked is read pre-AI from
playbook state (~:478-:490 region). SC-E asserts a soft-rejection
inbound ('lo pienso') does NOT increment, does NOT emit the
synthetic phrase, does NOT invoke synthetic request_human —
pre-empts Phase 97 RGUARD-02.

Out of scope (preserved unchanged):

- el-templo-bot/src/ai/system-prompt.ts (Phase 96 owns snapshot
  regen of pb1-e1a-lead-rendered.snap.txt + POST_RLOK_04_BYTES =
  18370 at v5-3-2-regression.test.ts:57).
- el-templo-bot/src/ai/tools.ts (the 4 allowlist source strings
  live here; this commit is a pure consumer).
- el-templo-bot/src/ai/with-timeout.ts (ToolTimeoutError shipped
  in 95-02 e213ee80; SC-D imports it only for the throw-path test).

PR description (when this branch merges): "system-prompt.ts
unchanged — snapshot regen deferred to Phase 96."
EOF
)"
````

    16. Confirm post-commit:
        - `git log -1 --format=%s | grep -cE '^feat\(95-03\): retry counter \+ HANDOFF_ESCALATION_PHRASE \+ request_human escalation' | tr -d ' '` → `1`.
        - `git show --stat HEAD --name-only HEAD | grep -cE '^el-templo-bot/src/webhook/handler\.ts$' | tr -d ' '` → `1`.

  </action>
  <acceptance_criteria>
    - `grep -cE 'let failedToolCalls = 0' el-templo-bot/src/webhook/handler.ts` → `1`
    - `grep -cE 'const HANDOFF_ESCALATION_PHRASE\b' el-templo-bot/src/webhook/handler.ts` → `1`
    - `grep -cE 'Te paso con alguien del equipo, te escriben enseguida 🙌' el-templo-bot/src/webhook/handler.ts` → `1` (byte-exact, including the 🙌 emoji and lowercase 'equipo')
    - `grep -cE 'FAILURE_ALLOWLIST' el-templo-bot/src/webhook/handler.ts` → ≥ `2` (declaration + at least one use site inside isFailureToolResult or inline)
    - `grep -cE 'No pude completar la reserva\. Intenta de nuevo en un momento\.' el-templo-bot/src/webhook/handler.ts` → `1`
    - `grep -cE 'No pude completar el registro\. Intenta de nuevo en un momento\.' el-templo-bot/src/webhook/handler.ts` → `1`
    - `grep -cE 'Herramienta agotada por tiempo de espera\.' el-templo-bot/src/webhook/handler.ts` → `1`
    - `grep -cE 'startsWith\(["\x27]Herramienta ' el-templo-bot/src/webhook/handler.ts` → ≥ `1` (the dynamic default-case discriminator; either single or double quotes accepted)
    - `grep -cE 'failedToolCalls >= 2' el-templo-bot/src/webhook/handler.ts` → `1`
    - `grep -cE 'auto_escalation_after_2_failures' el-templo-bot/src/webhook/handler.ts` → `1`
    - `grep -cE 'DEGR-01 escalation triggered' el-templo-bot/src/webhook/handler.ts` → `1`
    - `grep -cE 'sendTextMessage\(phone, HANDOFF_ESCALATION_PHRASE\)' el-templo-bot/src/webhook/handler.ts` → `1`
    - `grep -cE 'executeTool\(\s*["\x27]request_human["\x27]' el-templo-bot/src/webhook/handler.ts` → ≥ `1` (the synthetic invocation; the existing :692-694 path is a `toolCall.name === "request_human"` check, NOT an executeTool call, so this grep matches ONLY the new synthetic invocation)
    - `grep -cE 'humanTakeoverTriggered = true' el-templo-bot/src/webhook/handler.ts` → `2` (the existing model-emitted-request_human assignment at :693 + the new synthetic-escalation assignment)
    - Discipline grep on commit additions: `git diff HEAD~1 HEAD -- el-templo-bot/src/webhook/handler.ts | grep -E '^\+' | grep -cE 'console\.|: any\b|<any[,>]|as any\b' | tr -d ' '` → `0`
    - `cd el-templo-bot && pnpm tsc --noEmit` exits 0
    - `cd el-templo-api && pnpm tsc --noEmit` exits 0
    - `cd el-templo-bot && pnpm test --run test/v5-3-3-degr-01-escalation.test.ts` exits 0 (GREEN state — all tests in the new file pass; raw exit code, no stderr parsing).
    - `cd el-templo-api && pnpm test --run test/whatsapp/v5-3-3-booking.integration.test.ts` exits 0 (no regression from 95-02 GREEN; raw exit code).
    - `git log -1 --format=%s | grep -cE '^feat\(95-03\): retry counter \+ HANDOFF_ESCALATION_PHRASE \+ request_human escalation' | tr -d ' '` → `1`
    - `git show --stat HEAD --name-only HEAD | grep -cE '^el-templo-bot/src/webhook/handler\.ts$' | tr -d ' '` → `1`
    - `git show --stat HEAD --name-only HEAD | grep -vE '^el-templo-bot/src/webhook/handler\.ts$|^$' | wc -l | tr -d ' '` → `0`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                              | Description                                                                                                                                                                                                                                                                                                                                                          |
| ----------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CONTEXT.md D-05..D-18 → this plan's encoded behavior  | The locked decisions are the contract. Drift from byte-exact phrase or from the per-tool-call increment + threshold-`>= 2` semantics widens scope beyond the user-approved decision record. Plan-checker (NORMAL framing) verifies each acceptance-criteria grep matches the locked decision.                                                                        |
| handler-controlled tool RETURNS → counter classifier  | The FAILURE_ALLOWLIST set + the `startsWith('Herramienta "')` discriminator compare against `executeTool`'s RETURN value, which is handler-controlled (the 4 strings are emitted by handler-internal code paths in `tools.ts`). User input flows into tool ARGUMENTS, not tool RETURNS — there is no user-controlled path into the classifier surface.               |
| HANDOFF_ESCALATION_PHRASE → outbound WhatsApp message | The locked phrase is a `const` declaration; tampering would require modifying handler.ts. Tests (SC-A asserts byte-exact equality on `sendCalls[0]`) catch any future drift.                                                                                                                                                                                         |
| Pino structured log → log stream                      | The D-09 observability shape is `log.warn({ phone, conversationId, failureCount, lastToolName, lastToolError }, 'DEGR-01 escalation triggered')`. Pino uses structured object logging (fields are JSON-encoded), so log injection via newlines or escape sequences in `lastToolError` is structurally prevented — the values are JSON fields, not concatenated text. |
| Counter local → cross-request isolation               | `let failedToolCalls = 0` is declared inside `processWithAiInner`. Each new inbound creates a fresh closure; no module-level state, no Redis, no class instance — counter-state bleed across requests is impossible by construction. CONTEXT.md D-10's design choice is also a security choice.                                                                      |
| executor → multi-doc canonical invariant block        | Plan 95-03 mutates none of the 6 canonical-block location-pairs by design. The sha256 drift sentry catches accidental collateral edits. Per user-MEMORY rule `feedback_multi_doc_invariant_hash_check.md` (paraphrase = drift waiting to happen).                                                                                                                    |
| executor → existing handler.ts surface (handoff path) | The existing model-emitted-`request_human` path at handler.ts:692-694 + :928-950 is PRESERVED unchanged. The synthetic-escalation path adds a NEW branch (early return) that diverts BEFORE the existing branch — does not replace or unify the two paths. CONTEXT.md `<deferred>` deferred the unification to v5.4+.                                                |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                                                                                                                                  | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---------- | ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-95-03-01 | Tampering              | A future tool returns user-influenced content that LOOKS like an allowlist fail-string, spoofing the counter increment                                                     | mitigate    | The allowlist comparison is on `executeTool`'s RETURN value (handler-controlled). User input flows into tool ARGUMENTS, not tool RETURNS — there is no user path into the comparison surface in current code. If a future tool added user-echo behavior (none in v5.3.3), the comparison would still match only the static strings emitted by handler-internal `tools.ts` code paths; user-supplied free-text would not byte-equal those strings. SC-F (allowlist boundary negative test) empirically pins this for the degraded-UX strings (the closest "false-positive" risk surface).                                                     |
| T-95-03-02 | Tampering              | `HANDOFF_ESCALATION_PHRASE` is mutated by a future commit (e.g., dropping the 🙌 emoji or capitalizing 'Equipo'), causing inconsistent handoff UX                          | mitigate    | The phrase is a `const` declaration. SC-A asserts byte-exact equality on the captured `sendCalls[0]`. Any future mutation of the const triggers an SC-A failure in CI. Phase 97 RGUARD-01 will additionally include a byte-equality assertion in the v5-3-3-regression.test.ts suite (locked at plan time per CONTEXT.md `<specifics>`: 'Phase 97 RGUARD-01 will assert byte-equal match in the v5.3.3 regression suite').                                                                                                                                                                                                                   |
| T-95-03-03 | Information Disclosure | The Pino `log.warn` shape includes `lastToolError` — if a future tool error message contains PII or a secret token, it ends up in the log stream                           | accept      | Current tools.ts error strings are all generic Spanish UX copy ('No pude completar la reserva...', 'Herramienta agotada por tiempo de espera.', etc.) — no PII, no tokens, no URLs with credentials. Future tools that surface sensitive error content would need an explicit scrubbing layer (out of scope for v5.3.3 per REQUIREMENTS.md 'Replacing Pino with structured external sink in dev' Out of Scope). Pino structured logging encodes fields as JSON, so even if a sensitive string appears in `lastToolError`, it cannot break out of the log structure via control characters. v5.4.0 owns the file-sink + scrubbing-layer work. |
| T-95-03-04 | Tampering              | Counter-state bleed across requests via accidental module-level declaration (e.g., a typo placing `let failedToolCalls = 0` outside `processWithAiInner`)                  | mitigate    | The acceptance-criteria grep `grep -cE 'let failedToolCalls = 0' el-templo-bot/src/webhook/handler.ts` → `1` ensures exactly one declaration. The implementation guidance in `<interfaces>` explicitly places the declaration adjacent to `let humanTakeoverTriggered = false;` (which is inside `processWithAiInner`). SC-A + SC-B run sequential inbounds — if the counter were module-level, SC-B's first inbound would carry state into SC-A's second inbound, breaking both. Vitest's `beforeEach` + `vi.resetModules` further isolates test runs.                                                                                      |
| T-95-03-05 | Tampering              | The synthetic `request_human` invocation is missing the `reason` argument or uses a misspelled reason string                                                               | mitigate    | Acceptance-criteria grep `grep -cE 'auto_escalation_after_2_failures' el-templo-bot/src/webhook/handler.ts` → `1` and SC-A asserts the captured `executeToolCalls` entry has `args.reason === 'auto_escalation_after_2_failures'`. Any drift fails CI.                                                                                                                                                                                                                                                                                                                                                                                       |
| T-95-03-06 | Denial of Service      | The early `return` after the escalation block bypasses a future side-effect (e.g., session persistence) that becomes load-bearing, leaving conversation state inconsistent | mitigate    | The synthetic `executeTool('request_human', ...)` call inside the escalation block invokes `requestHuman` at `tools.ts:512-531`, which writes `conversation_status = 'human_takeover'` to the DB synchronously. The session-update + playbook-state writes at handler.ts:786-905 are POST-tool-loop optimization side effects — bypassing them on the escalation path is intentional per CONTEXT.md D-08 (the user gets exactly ONE outbound message; the next inbound from the same phone will see `conversation_status = 'human_takeover'` and the human-takeover branch at handler.ts:326-331 returns early without re-processing).       |
| T-95-03-07 | Tampering              | SC#3 invariant breaks — soft-rejection inbound trips the counter via accidental coupling between `whyAsked` and `failedToolCalls`                                          | mitigate    | The counter is declared inside the tool-loop region; soft rejections produce NO tool calls (the model emits text content directly per Phase 91). With zero tool calls, the `for (const toolCall ...)` body never executes, so `failedToolCalls` stays 0. SC-E empirically pins this: soft-rejection inbound → no tool calls → no increment → no synthetic phrase → no synthetic request_human. Plan 95-03 introduces NO shared state between the counter and the `whyAsked` flag.                                                                                                                                                            |
| T-95-03-08 | Tampering              | Executor commits both RED and GREEN as a single mega-commit instead of the 2-commit cadence specified in CONTEXT.md D-21                                                   | mitigate    | Task 1's acceptance criteria assert exactly 1 file in commit 1 (the new test file). Task 2's acceptance criteria assert exactly 1 file in commit 2 (handler.ts). A mega-commit would fail both per-commit file-count assertions. The RED commit subject regex (`^test\(95-03\): RED — DEGR-01...`) and GREEN commit subject regex (`^feat\(95-03\): retry counter...`) further enforce the cadence.                                                                                                                                                                                                                                          |
| T-95-03-09 | Tampering              | One of the 6 canonical-block location-pairs is accidentally edited as collateral of a markdown edit during the executor's session                                          | mitigate    | sha256 drift sentry runs in BOTH Task 1 step 8 AND in the phase-level `<verification>` (see below). If any of the 6 location-pairs reports DRIFT, executor halts and reports — does NOT auto-resync. Pattern inherited verbatim from `94-02-PLAN.md:305-326` and `95-02-PLAN.md`.                                                                                                                                                                                                                                                                                                                                                            |
| T-95-03-10 | Tampering              | npm/pip/cargo installs                                                                                                                                                     | accept      | Plan 95-03 installs NO new packages. All required infrastructure (vitest, drizzle-orm, mysql2, the openai SDK, fastify) is already a dependency. The new test file uses ONLY existing deps (vitest's `describe`/`it`/`expect`/`vi`); the modified handler.ts uses ONLY existing in-codebase imports (`sendTextMessage`, `executeTool`, `log`). No `[ASSUMED]` / `[SUS]` / `[SLOP]` packages introduced. Package legitimacy gate not triggered.                                                                                                                                                                                               |
| T-95-03-SC | Tampering              | npm/pip/cargo installs (formal STRIDE supply-chain row)                                                                                                                    | accept      | See T-95-03-10. Zero package installs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

</threat_model>

<verification>

## Phase-Level Verification (run after both commits land)

1. **Commit chain shape — exactly 2 commits matching the locked regex pattern:**

   ```
   git log --format=%s -n 2 | tac
   ```

   Expected (top to bottom = oldest to newest within this plan's range):

   ```
   test(95-03): RED — DEGR-01 escalation counter + synthetic phrase
   feat(95-03): retry counter + HANDOFF_ESCALATION_PHRASE + request_human escalation
   ```

   Commit subject regex per commit (verify each):
   - `git log -1 --format=%s HEAD~1 | grep -cE '^test\(95-03\): RED — DEGR-01 escalation counter \+ synthetic phrase' | tr -d ' '` → `1`
   - `git log -1 --format=%s HEAD | grep -cE '^feat\(95-03\): retry counter \+ HANDOFF_ESCALATION_PHRASE \+ request_human escalation' | tr -d ' '` → `1`

2. **Commit file-count assertions (per-commit; concrete integers):**
   - Commit 1 (HEAD~1): exactly 1 file (`el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts`).

     ```
     git show --stat HEAD~1 --name-only | grep -cE '^el-templo-bot/test/v5-3-3-degr-01-escalation\.test\.ts$' | tr -d ' '
     ```

     → `1`. Total files in commit 1 (excluding commit metadata lines):

     ```
     git show --name-only --format='' HEAD~1 | grep -vE '^$' | sort -u | wc -l | tr -d ' '
     ```

     → `1`.

   - Commit 2 (HEAD): exactly 1 file (`el-templo-bot/src/webhook/handler.ts`).
     ```
     git show --stat HEAD --name-only | grep -cE '^el-templo-bot/src/webhook/handler\.ts$' | tr -d ' '
     ```
     → `1`.
     ```
     git show --name-only --format='' HEAD | grep -vE '^$' | sort -u | wc -l | tr -d ' '
     ```
     → `1`.

   Aggregate commit-range file count (all 2 commits combined; should equal 2 unique files):

   ```
   git log --name-only --format='' HEAD~1..HEAD | grep -vE '^$' | sort -u | wc -l | tr -d ' '
   ```

   → `2`.

3. **Negative `git diff` guards — these files MUST be unchanged across HEAD~1..HEAD:**

   ```
   git diff HEAD~1..HEAD -- el-templo-bot/src/ai/system-prompt.ts | wc -l | tr -d ' '
   git diff HEAD~1..HEAD -- el-templo-bot/src/ai/tools.ts | wc -l | tr -d ' '
   git diff HEAD~1..HEAD -- el-templo-bot/src/ai/with-timeout.ts | wc -l | tr -d ' '
   git diff HEAD~1..HEAD -- el-templo-bot/src/ai/openai.ts | wc -l | tr -d ' '
   git diff HEAD~1..HEAD -- el-templo-bot/.env.example | wc -l | tr -d ' '
   git diff HEAD~1..HEAD -- el-templo-bot/scripts/check-debounce-invariant.sh | wc -l | tr -d ' '
   git diff HEAD~1..HEAD -- el-templo-bot/test/v5-3-2-regression.test.ts | wc -l | tr -d ' '
   git diff HEAD~1..HEAD -- el-templo-bot/test/v5-3-3-booking-reliability.test.ts | wc -l | tr -d ' '
   git diff HEAD~1..HEAD -- el-templo-bot/test/v5-3-3-openai-latency.test.ts | wc -l | tr -d ' '
   git diff HEAD~1..HEAD -- el-templo-bot/test/v5-3-3-handler-concurrency.test.ts | wc -l | tr -d ' '
   git diff HEAD~1..HEAD -- el-templo-api/test/whatsapp/v5-3-3-booking.integration.test.ts | wc -l | tr -d ' '
   git diff HEAD~1..HEAD -- el-templo-bot/test/fixtures/ | wc -l | tr -d ' '
   ```

   Each MUST output `0`.

   Also verify no `.snap.txt` files are touched:

   ```
   git log --name-only HEAD~1..HEAD | grep -cE '\.snap\.txt$' | tr -d ' '
   ```

   → `0`.

   Verify no canonical-invariant location is touched:

   ```
   for f in .planning/phases/93-handler-concurrency/93-CONTEXT.md .planning/phases/94-openai-latency-graceful-failure/94-CONTEXT.md .planning/phases/95-booking-reliability-graceful-degradation/95-CONTEXT.md .planning/ROADMAP.md .planning/MACRO-ROADMAP.md; do
     git diff HEAD~1..HEAD -- "$f" | wc -l | tr -d ' '
   done
   ```

   Each MUST output `0`.

4. **TypeScript clean across both packages:**

   ```
   cd el-templo-bot && pnpm tsc --noEmit
   cd ../el-templo-api && pnpm tsc --noEmit
   ```

   Both exit 0.

5. **TDD make-green bridge — the RED file's 6 scenarios transition from RED (post-HEAD~1) to GREEN (post-HEAD):**

   ```
   cd el-templo-bot && pnpm test --run test/v5-3-3-degr-01-escalation.test.ts 2>&1 | tee /tmp/95-03-final-degr.log | tail -15
   ```

   Expected: `Tests N passed (N)` where N is the actual scenario count (6 if each SC-X is one `it`; 9 if SC-C parameterizes into 4 sub-its plus the other 5). Exit 0. Zero failed-tests lines.

6. **No regression in pre-existing test suites:**

   ```
   cd el-templo-bot && pnpm test 2>&1 | tee /tmp/95-03-final-bot-all.log | tail -10
   cd ../el-templo-api && pnpm test 2>&1 | tee /tmp/95-03-final-api-all.log | tail -10
   ```

   Expected: the bot package shows EXACTLY ONE pre-existing failing test (`BUG-03 candidate (iii) > RED: system prompt explicitly binds Sunday=0 (FAILS on master)` in `test/v5-3-3-booking-reliability.test.ts:55` — the deferred (iii) carried over from Phase 95-02 and owned by a future plan in coordination with Phase 96). All other suites pass. The API package's full suite passes (including `v5-3-3-booking.integration.test.ts` 9/9 GREEN — no regression from the handler change).

7. **Cross-Phase Invariant — sha256 drift sentry across SIX location-pairs** (verbatim from 94-02-PLAN.md:305-326 / 95-02-PLAN.md recipe):

   ```
   cd /Users/bores/el-templo
   extract_block() { awk -v s="$2" 'NR>=s {print; if (/Minimum TTL =/) exit}' "$1"; }
   expected="67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344"
   fail=0
   for spec in \
     ".planning/phases/93-handler-concurrency/93-CONTEXT.md:84" \
     ".planning/phases/94-openai-latency-graceful-failure/94-CONTEXT.md:34" \
     ".planning/phases/95-booking-reliability-graceful-degradation/95-CONTEXT.md:37" \
     ".planning/ROADMAP.md:62" \
     ".planning/ROADMAP.md:93" \
     ".planning/MACRO-ROADMAP.md:99"; do
     f="${spec%:*}"; l="${spec##*:}"
     got=$(extract_block "$f" "$l" | shasum -a 256 | cut -d' ' -f1)
     if [ "$got" = "$expected" ]; then
       echo "OK   $spec = $got"
     else
       echo "DRIFT $spec = $got (expected $expected)" >&2
       fail=1
     fi
   done
   [ "$fail" -eq 0 ]
   ```

   Expected: 6 `OK` lines, exit 0. If ANY location reports `DRIFT`, halt before further verification and report drift to the user; do NOT auto-resync.

8. **Console / `any` baseline preservation** (per 95-AUDIT.md §A baselines, preserved through 95-02):

   ```
   grep -rEn "console\." el-templo-bot/src/ | wc -l | tr -d ' '
   ```

   → `0` (baseline `console_count_baseline_bot = 0` preserved).

   ```
   grep -rEn ':\s*any\b|<any[,>]|<any\s|as\s+any\b|\bany\[\]|Record<[^>]*,\s*any\s*>|Array<any>|Promise<any>' el-templo-bot/src/ | wc -l | tr -d ' '
   ```

   → `0` (baseline `any_count_baseline_bot = 0` preserved).

   ```
   grep -rEn ':\s*any\b|<any[,>]|<any\s|as\s+any\b|\bany\[\]|Record<[^>]*,\s*any\s*>|Array<any>|Promise<any>' el-templo-api/src/ | wc -l | tr -d ' '
   ```

   → `0` (baseline `any_count_baseline_api = 0` preserved).

   Test-file discipline grep (only added lines; the test file is new in commit 1 so the entire body is new):

   ```
   grep -cE 'console\.|: any\b|<any[,>]|as any\b' el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts | tr -d ' '
   ```

   → `0`.

   Handler.ts new-line discipline grep:

   ```
   git diff HEAD~1..HEAD -- el-templo-bot/src/webhook/handler.ts | grep -E '^\+' | grep -cE 'console\.|: any\b|<any[,>]|as any\b' | tr -d ' '
   ```

   → `0`.

9. **Locked-decision behavioral grep on the post-GREEN handler.ts** (re-asserts Task 2's acceptance criteria as a phase-level invariant):

   ```
   grep -cE 'let failedToolCalls = 0' el-templo-bot/src/webhook/handler.ts | tr -d ' '
   grep -cE 'Te paso con alguien del equipo, te escriben enseguida 🙌' el-templo-bot/src/webhook/handler.ts | tr -d ' '
   grep -cE 'failedToolCalls >= 2' el-templo-bot/src/webhook/handler.ts | tr -d ' '
   grep -cE 'auto_escalation_after_2_failures' el-templo-bot/src/webhook/handler.ts | tr -d ' '
   grep -cE 'DEGR-01 escalation triggered' el-templo-bot/src/webhook/handler.ts | tr -d ' '
   grep -cE 'sendTextMessage\(phone, HANDOFF_ESCALATION_PHRASE\)' el-templo-bot/src/webhook/handler.ts | tr -d ' '
   ```

   Each MUST output `1`.

10. **Existing pre-95-03 production paths preserved byte-for-byte:**

    The existing model-emitted-request_human path at handler.ts:692-694 + :928-950 MUST be unchanged in semantics. Verify by inspecting the diff:

    ```
    git diff HEAD~2..HEAD -- el-templo-bot/src/webhook/handler.ts | grep -E '^-' | grep -cE 'humanTakeoverTriggered = true|if \(toolCall\.name === "request_human"\)' | tr -d ' '
    ```

    → `0` (zero DELETIONS of the existing humanTakeoverTriggered/request_human shape — only ADDITIONS of the new synthetic path).

<human-check>
Reviewer empirically re-runs the test suites and verifies the post-95-03 state:

1. **DEGR-01 escalation test suite GREEN:**

   ```
   cd el-templo-bot && pnpm test --run test/v5-3-3-degr-01-escalation.test.ts
   ```

   Confirm: zero failed tests, all six scenarios (SC-A through SC-F) pass. The output line `Tests N passed (N)` shows the actual count (≥6 depending on SC-C parameterization).

2. **TDD make-green observation (RED state recoverable from log):**

   Inspect `/tmp/95-03-c01-red.log` (left by Task 1's `tee`). Confirm that at the time of the RED commit (`git log HEAD~1 --format=%h`), at minimum 4 of the 6 scenarios were observed FAILING. SC-E and SC-F may have passed by vacuous truth on master (no synthetic phrase logic existed to violate the negative assertions). This empirical RED→GREEN transition is the TDD discipline mandated by CONTEXT.md D-20.

3. **Bot-side unit suite — exactly 1 expected failure (deferred (iii) carried over from 95-02):**

   ```
   cd el-templo-bot && pnpm test 2>&1 | tail -20
   ```

   Confirm: the ONLY failing test in the entire bot package is `BUG-03 candidate (iii) > RED: system prompt explicitly binds Sunday=0 (FAILS on master)` in `test/v5-3-3-booking-reliability.test.ts:55` — the deferred (iii) per `95-AUDIT.md §C`. All other suites (handler-concurrency, openai-latency, booking-reliability protectors, the new v5-3-3-degr-01-escalation) continue to pass.

4. **API test suite — no regression from the handler change:**

   ```
   cd el-templo-api && pnpm test --run test/whatsapp/v5-3-3-booking.integration.test.ts 2>&1 | tail -10
   ```

   Confirm: `Tests 9 passed (9)` — Plan 95-02's GREEN state preserved. (The DEGR-01 counter logic does not touch SQL, so the booking-integration suite is structurally insulated; this re-run is a defense-in-depth check.)

5. **Visual inspection of handler.ts changes:**
   - Confirm `const HANDOFF_ESCALATION_PHRASE = "Te paso con alguien del equipo, te escriben enseguida 🙌";` is module-level (NOT inside any function); byte-exact phrase (🙌 emoji present; lowercase 'equipo'; no trailing whitespace).
   - Confirm `const FAILURE_ALLOWLIST: ReadonlySet<string> = new Set([...]);` contains EXACTLY 3 entries (the 3 static byte-exact strings). The dynamic default-case 4th entry is NOT in the Set — it's matched via `startsWith` inside `isFailureToolResult` (visually verify the helper's body).
   - Confirm `let failedToolCalls = 0;` is declared INSIDE `processWithAiInner` (the function body, NOT module-level). Visually it appears adjacent to `let humanTakeoverTriggered = false;` at handler.ts:659.
   - Confirm the `executeTool(...)` invocation in the tool loop is wrapped in `try { ... } catch (err: unknown) { failedToolCalls++; lastToolName = toolCall.name; lastToolError = err instanceof Error ? err.message : String(err); throw err; }`. The catch RE-THROWS — error propagation semantics are preserved.
   - Confirm the `isFailureToolResult(toolResult)` check immediately follows `lastToolResult = toolResult;` and increments the counter ONLY when the classifier returns true.
   - Confirm the threshold check `if (failedToolCalls >= 2) { ... }` block contains:
     - `log.warn` with the shape `{ phone, conversationId, failureCount, lastToolName, lastToolError }` and the message `'DEGR-01 escalation triggered'`.
     - `await sendTextMessage(phone, HANDOFF_ESCALATION_PHRASE);`
     - `await executeTool('request_human', { reason: 'auto_escalation_after_2_failures' }, db, conversationId, { phone, clientState });`
     - `humanTakeoverTriggered = true;`
     - `return;` (the early-return that preempts segments[0])

6. **Visual inspection of the new test file:**
   - Confirm `el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts` contains 6 describe/it blocks tagged SC-A through SC-F.
   - Confirm SC-A asserts byte-exact equality on `sendCalls[0]` against `"Te paso con alguien del equipo, te escriben enseguida 🙌"`.
   - Confirm SC-A asserts `executeToolCalls` contains an entry with `name === 'request_human'` AND `args.reason === 'auto_escalation_after_2_failures'`.
   - Confirm SC-B asserts a 3-iteration mock plan (failure / success / failure) trips escalation on the second failure (count=2), NOT after the first.
   - Confirm SC-C covers all 4 allowlist entries (the 3 static + the dynamic default-case `'Herramienta "unknown_tool" no disponible.'`).
   - Confirm SC-D constructs a non-ToolTimeoutError throw (e.g., `new Error('fetch failed')`) inside the mocked executeTool to exercise the handler-side try/catch increment path.
   - Confirm SC-E uses a soft-rejection inbound phrase (one of: 'lo pienso', 'no estoy seguro', 'mañana te digo') and asserts NO synthetic phrase + NO synthetic request_human.
   - Confirm SC-F uses the byte-exact 'Esa clase esta llena y no hay alternativas disponibles esta semana.' string and asserts even 2 such returns do NOT trip the count=2 threshold.

7. **Anti-scope-creep visual check (negative diff guards re-verified by eye):**
   - `system-prompt.ts` unchanged across `HEAD~1..HEAD`.
   - `tools.ts` unchanged across `HEAD~1..HEAD`.
   - `with-timeout.ts` unchanged across `HEAD~1..HEAD`.
   - No `.snap.txt` file modifications.
   - No `.env.example` modifications (95-03 ships no new env vars).
   - PR description (when the branch merges): "system-prompt.ts unchanged — snapshot regen deferred to Phase 96."

8. **Cross-Phase Invariant byte-verification** (re-run the sha256 sentry from `<verification>` step 7) — confirm 6 `OK` lines, exit 0.

9. **Pino observability sanity** (optional spot-check):
   - In `/tmp/95-03-c02-green.log` (left by Task 2's `tee`), search for the structured log line emitted during SC-A's run: `grep -E 'DEGR-01 escalation triggered' /tmp/95-03-c02-green.log` should show the structured-log capture (Vitest may suppress real Pino output under the pino-mock; the log shape is asserted indirectly by SC-A's spy assertions on `sendCalls` and `executeToolCalls`).

10. **CLAUDE.md compliance** (project standards from `el-templo-bot/CLAUDE.md`):
    - No `console.*` added in any new code (Pino-only logging via the `log: FastifyBaseLogger` parameter).
    - No `any` types added (`catch (err: unknown)` + `instanceof Error` narrowing throughout).
    - Pre-commit hooks (Husky + lint-staged) ran cleanly on both commits (no Prettier formatting violations introduced).

- [ ] RED → GREEN bridge observed: after RED commit, run `cd el-templo-bot && pnpm test --run test/v5-3-3-degr-01-escalation.test.ts` — confirm exit code non-zero AND at least one of SC-A/B/C/D fails with a clear assertion failure (not an import/syntax error). After GREEN commit, run the same command — confirm exit code 0 AND SC-A..SC-F all show as passing. The RED → GREEN transition is the TDD discipline observation; F-1's deprecated grep is replaced by this human check + raw exit code per STATE.md carry-forward.

Resume signal: type `approved` to authorize ship of Plan 95-03, or describe required revisions.
</human-check>

</verification>

<success_criteria>

1. Plan 95-03 ships as exactly 2 atomic commits with subjects matching `^test\(95-03\): RED — DEGR-01 escalation counter \+ synthetic phrase` and `^feat\(95-03\): retry counter \+ HANDOFF_ESCALATION_PHRASE \+ request_human escalation` (in that order). No SUMMARY.md commit (owned by the future Phase 95 milestone wrap-up).
2. `el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts` exists with six scenarios (SC-A through SC-F) covering CONTEXT.md D-05..D-18; was observed RED on master (Task 1 step 5's log retained at `/tmp/95-03-c01-red.log`); transitions to GREEN after Task 2's commit — `Tests N passed (N)` (N ≥ 6) with zero failed tests.
3. `el-templo-bot/src/webhook/handler.ts` contains: (a) module-level `const HANDOFF_ESCALATION_PHRASE = "Te paso con alguien del equipo, te escriben enseguida 🙌";` (byte-exact), (b) module-level `const FAILURE_ALLOWLIST: ReadonlySet<string>` with the 3 static entries, (c) module-level `isFailureToolResult(result)` helper (or inline equivalent) classifying return strings via `FAILURE_ALLOWLIST.has` + `startsWith('Herramienta "')` discriminator, (d) `let failedToolCalls = 0` local inside `processWithAiInner` mirroring the `humanTakeoverTriggered` pattern, (e) per-tool-call increment for throw + return-string failure modes via try/catch + `isFailureToolResult` check, (f) single `if (failedToolCalls >= 2) { ... }` threshold check triggering log.warn + sendTextMessage(HANDOFF_ESCALATION_PHRASE) + synthetic executeTool('request_human', { reason: 'auto_escalation_after_2_failures' }) + humanTakeoverTriggered = true + early return.
4. DEGR-01 + DEGR-02 are CLOSED by this plan (Phase 95's third and final deliverable per ROADMAP.md / CONTEXT.md / REQUIREMENTS.md). Combined with Plans 95-01 (audit, shipped) and 95-02 (BOOK-01 fix + withTimeout helper + executeTool catch site, shipped), Phase 95 has shipped all three requirements (BOOK-01, DEGR-01, DEGR-02).
5. SC#3 invariant preserved (DEGR-02 / RGUARD-02 pre-emption): the retry counter and Phase 91 `whyAsked` flag share NO state; SC-E empirically pins that a soft-rejection inbound does NOT increment the counter, does NOT emit `HANDOFF_ESCALATION_PHRASE`, and does NOT invoke synthetic `request_human`. Phase 97 RGUARD-02 has a known-PASS to extend.
6. `el-templo-bot/src/ai/system-prompt.ts`, `el-templo-bot/src/ai/tools.ts`, `el-templo-bot/src/ai/with-timeout.ts`, `el-templo-bot/.env.example`, and all `.snap.txt` files are UNCHANGED. `POST_RLOK_04_BYTES = 18370` at `el-templo-bot/test/v5-3-2-regression.test.ts:57` is UNCHANGED. PR description notes: "system-prompt.ts unchanged — snapshot regen deferred to Phase 96."
7. `cd el-templo-bot && pnpm tsc --noEmit` and `cd el-templo-api && pnpm tsc --noEmit` both exit 0. `console_count_baseline_bot = 0` and `any_count_baseline_bot = any_count_baseline_api = 0` baselines (per 95-AUDIT.md §A) are preserved. The Cross-Phase Invariant block remains byte-identical across all 6 canonical-block location-pairs (hash `67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344`).
8. Full test suites green (modulo the deferred (iii) Sunday=0 RED at `test/v5-3-3-booking-reliability.test.ts:55` carried over from 95-02 — a future plan in coordination with Phase 96 closes it). `el-templo-api/test/whatsapp/v5-3-3-booking.integration.test.ts` continues to show `Tests 9 passed (9)` — no regression from the handler change. Phase 93 + Phase 94 + v5-3-2 regression suites all green.

</success_criteria>

<rationale>

## Atomic commit strategy chosen: 2 commits (RED + GREEN)

Per CONTEXT.md D-21 ("Atomic commit cadence per plan. Mirror Phase 93/94 pattern: one RED commit per task, one GREEN commit per task, one SUMMARY.md commit per plan. No multi-task atomic commits.") and the 95-02 precedent (4-commit cadence for a multi-task plan), the 2-commit cadence is the minimal-but-sufficient form for this plan:

1. **Single new test file + single source-file modification → 2 deliverables → 2 commits.** Plan 95-03's surface is smaller than 95-02's (only 2 files in `files_modified` vs 95-02's 4 files). One commit per file is the natural decomposition.

2. **TDD make-green observability**: with 2 commits, an empirical reviewer (or future bisecter) can run `git checkout HEAD~1` → `pnpm test --run test/v5-3-3-degr-01-escalation.test.ts` → observe ≥4 of 6 scenarios FAILING → `git checkout HEAD` → observe all 6 PASSING. Each commit shifts test-suite state in a verifiable way. This is the same RED→GREEN signal CONTEXT.md D-20 mandates.

3. **No SUMMARY.md commit**: Phase 95's milestone-level summary covers Plans 95-01, 95-02, 95-03 collectively and is owned by a future wrap-up plan (consistent with 95-02's plan boundary — `<rationale>` in 95-02-PLAN.md states "No SUMMARY.md commit — that is owned by the future plan-95 milestone wrap-up.").

4. **Phase 93/94 + 95-02 precedent alignment**: Phase 94 Plan 94-02 shipped 3 atomic commits (RED → GREEN → SUMMARY). Plan 95-02 shipped 4 atomic commits (P0 (vi) → P0+P1 bundle (ii)+(iv)+(v) → withTimeout helper → drift-guard test). Plan 95-03's 2-commit cadence is the smallest valid cadence for the actual deliverable surface; smaller would conflate test + source (mega-commit, violates D-21), larger would split the handler.ts modifications artificially (the counter + allowlist + threshold all belong in one coherent diff).

## Test file location: `el-templo-bot/test/v5-3-3-degr-01-escalation.test.ts` (new file)

Naming + location chosen per the precedents established in v5.3.3:

1. **File-per-locked-decision-cluster pattern**: v5.3.3 has one bot-side unit test file per cluster — `test/v5-3-3-handler-concurrency.test.ts` for CONC-01, `test/v5-3-3-openai-latency.test.ts` for LAT-01..03, `test/v5-3-3-booking-reliability.test.ts` for BOOK-01/BUG-03 (deferred (iii) RED + 95-02 regression-protectors + 95-02 drift-guard). Plan 95-03's six scenarios all cover DEGR-01 + DEGR-02 (the same locked-decision cluster) — a single file is the natural unit. The filename `v5-3-3-degr-01-escalation.test.ts` mirrors the milestone-prefix-and-requirement-id convention.

2. **Bot-side (not under `el-templo-api/test/whatsapp/`) because the behavior under test is pure handler control-flow**: the test mocks DB / WhatsApp / OpenAI / Redis surfaces entirely (mirroring v5-3-3-openai-latency.test.ts's `setupSurroundingMocks`). No real `eltemplo_test` MySQL is needed; no SQL discrimination is required. Per `el-templo-bot/CLAUDE.md`: "Integration tests for bot logic should live in `el-templo-api/test/whatsapp/` (shared test DB) | Unit tests for bot-specific logic (AI provider, memory, state machine) can live in `el-templo-bot/test/`."

3. **Mocking pattern reused from v5-3-3-openai-latency.test.ts**: the vi.doMock surface (provider, tools, whatsapp, redis, memory, playbook), vi.mock for pino, makeMockDb / makeMessage / makeMockLog helpers all transfer cleanly. The executor copies these helpers rather than factoring a shared module (Claude's Discretion — the duplication is bounded and consistent with the existing v5.3.3 test-file pattern; factoring would be premature without a third call site emerging).

## FAILURE_ALLOWLIST representation: `ReadonlySet<string>` for the 3 static entries + separate `startsWith` discriminator for the dynamic 4th

Claude's Discretion exercised here per CONTEXT.md D-11's allowlist enumeration:

1. **Static set lookup is O(1) and matches the byte-equality semantics CONTEXT.md D-11 mandates** ("a `const FAILURE_ALLOWLIST: ReadonlySet<string>` of byte-exact strings"). The 3 static strings (`tools.ts:806`, `:968`, `:271`) are known at compile time and never mutate.

2. **The dynamic 4th entry (`'Herramienta "${name}" no disponible.'` at `tools.ts:264`) is NOT a static string** — it interpolates the tool name. CONTEXT.md D-11 explicitly notes: "the live executeTool returns this string with the tool name interpolated" — so a Set lookup would miss every actual invocation. The `startsWith('Herramienta "') && endsWith('" no disponible.')` discriminator captures the family of dynamic strings the default-case can produce, without requiring an exhaustive enumeration of tool names.

3. **CONTEXT.md `<scope_locked>` explicitly defers this micro-decision to the planner**: "Implementation hint: a `const FAILURE_ALLOWLIST: ReadonlySet<string>` of byte-exact strings (for the 3 static entries) plus a separate `startsWith("Herramienta \"")` discriminator for the dynamic default-case string. The planner picks the cleanest shape; both approaches are acceptable." The two-track approach (static Set + dynamic discriminator) is the cleanest reading of D-11 because it matches the semantic distinction between "fixed error string" (allowlist entry) and "templated error string" (dynamic family).

## Plan-checker mode: NORMAL (CONTEXT.md D-22)

This plan is mechanical, not investigative. CONTEXT.md D-05..D-18 are the locked contract; the executor has no decisions to make beyond Claude's Discretion items explicitly enumerated above (test file naming, FAILURE_ALLOWLIST representation, atomic commit cadence). Adversarial framing is reserved for plans with genuine investigative branching (Plan 95-01 had it; this plan does not).

Structural integrity (XML/markup tag balance) IS included in the self-check per the locked feedback rule from `memory/feedback_plan_checker_structural_integrity.md` — this rule applies to ALL plan-checker framings, NORMAL or ADVERSARIAL. The plan-checker MUST verify every `<task>`, `<read_first>`, `<acceptance_criteria>`, `<action>`, `<threat_model>`, `<verification>`, `<human-check>` tag opens and closes; that `must_haves.truths` / `must_haves.artifacts` / `must_haves.key_links` YAML blocks parse cleanly; that the frontmatter `---` delimiters match.

## SC#3 / DEGR-02 guardrail framing

Per CONTEXT.md D-14: "The retry counter and Phase 91 whyAsked flag are different semantic objects on different code surfaces. They share NO state."

Plan 95-03 enforces this STRUCTURALLY: the counter is declared inside `processWithAiInner`'s tool-loop region (around handler.ts:659-701); `whyAsked` is read pre-AI from playbook state (around handler.ts:478-490 region — outside the tool loop entirely). There is no code path where the counter writes to `whyAsked` or vice versa. The SC-E negative test empirically pins this: a soft-rejection inbound produces NO tool calls (the model emits text content directly per Phase 91's SOFT_REJECTION_BACKOFF_RULE in `system-prompt.ts:70-86`), so the `for (const toolCall ...)` body never executes, so `failedToolCalls` stays 0.

Phase 97 RGUARD-02 will assert this invariant in the v5.3.3 regression suite. Plan 95-03 pre-empts that assertion with SC-E to provide RGUARD-02 a known-PASS to extend.

## Anti-scope-creep discipline (per CONTEXT.md `<deferred>` and `<scope_locked>`)

The single largest risk in Plan 95-03 is touching `system-prompt.ts` — even a one-character edit would invalidate `POST_RLOK_04_BYTES = 18370` at `test/v5-3-2-regression.test.ts:57` and require Phase 96 snapshot coordination. CONTEXT.md D-06 explicitly chose handler-side synthetic phrasing PRECISELY to avoid this: "Defined as `const HANDOFF_ESCALATION_PHRASE` in `handler.ts` near the retry-counter logic (NOT in `system-prompt.ts` — this is handler-facing, not model-facing)."

The plan's negative diff guards (Task 2 step 13 + phase-level verification step 3) assert `git diff -- el-templo-bot/src/ai/system-prompt.ts` is empty across `HEAD~1..HEAD`. The PR description note ("system-prompt.ts unchanged — snapshot regen deferred to Phase 96.") is a downstream signal to reviewers that this scope boundary was respected.

</rationale>

<output>
After both atomic commits land and the verification block exits GREEN, the user reviews the plan output via the `<human-check>` empirical re-run. No SUMMARY.md is written by this plan — the milestone-level summary for Phase 95 (covering plans 95-01, 95-02, 95-03 collectively) is owned by a future wrap-up plan or by the Phase 95 close. Plan 95-03's deliverable is the 2-commit chain + the post-fix test-suite state.

Next step (after user authorizes execution): `/gsd:execute-phase 95 --plan 03` — runs the 2-commit RED → GREEN cadence sequentially. After GREEN ships, Phase 95 is structurally complete (all three requirements BOOK-01, DEGR-01, DEGR-02 closed across plans 95-01, 95-02, 95-03). Phase 96 begins next.

Plan 95-03 consumes (read-only) from prior plans:

- `'Herramienta agotada por tiempo de espera.'` — the allowlist return string emitted by `executeTool` on `ToolTimeoutError` catch, shipped in Plan 95-02 commit `e213ee80`.
- `ToolTimeoutError` class — exported from `el-templo-bot/src/ai/with-timeout.ts`, shipped in Plan 95-02. Imported ONLY by the test file's SC-D scenario for the throw-path test; the handler-side counter never references it directly.

Plan 95-03 produces (for downstream phases):

- The retry counter + escalation flow at `handler.ts` — Phase 97 RGUARD-02 will extend the SC-E negative-test pattern to the milestone-wide regression suite.
- The locked `HANDOFF_ESCALATION_PHRASE` — Phase 97 RGUARD-01 will assert byte-equality in `test/v5-3-3-regression.test.ts` (mirroring the v5-3-2 RLOK-04 byte-tripwire pattern).
- Pino structured log `'DEGR-01 escalation triggered'` — v5.4.0 file-sink work will capture this durably; in v5.3.3 it routes to stdout (acceptable per CONTEXT.md D-09).
  </output>
