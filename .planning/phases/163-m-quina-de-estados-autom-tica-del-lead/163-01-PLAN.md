---
phase: 163-m-quina-de-estados-autom-tica-del-lead
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/schema/users.ts
  - el-templo-api/src/db/migrations/0182_lead_status_source.sql
  - el-templo-api/src/modules/settings/keys.ts
  - el-templo-api/src/modules/settings/service.ts
  - el-templo-api/test/leads-perdido-window.test.ts
autonomous: true
requirements: [AUTO-02, AUTO-04]
must_haves:
  truths:
    - "The users table has a nullable lead_status_source ENUM('auto','manual') column"
    - "A system_settings row leads.perdido_window_days holds an integer window (p90 or default 14)"
    - "SettingsService exposes getPerdidoWindowDays() returning an int, defaulting to 14 when the key is absent or unparseable"
    - "Migration 0182 is applied locally and recorded in the _migrations table"
  artifacts:
    - path: "el-templo-api/src/db/schema/users.ts"
      provides: "leadStatusSource enum column declaration"
      contains: "leadStatusSourceEnum"
    - path: "el-templo-api/src/db/migrations/0182_lead_status_source.sql"
      provides: "ALTER users ADD lead_status_source + idempotent p90 seed of leads.perdido_window_days"
      contains: "lead_status_source"
    - path: "el-templo-api/src/modules/settings/keys.ts"
      provides: "canonical key literal leads.perdido_window_days"
      contains: "perdido_window_days"
    - path: "el-templo-api/src/modules/settings/service.ts"
      provides: "getPerdidoWindowDays reader with default-14 fallback"
      contains: "getPerdidoWindowDays"
  key_links:
    - from: "el-templo-api/src/modules/settings/service.ts"
      to: "system_settings.leads.perdido_window_days"
      via: "select on systemSettings where settingKey = LEADS_SETTINGS_KEYS.perdidoWindowDays"
      pattern: "perdidoWindowDays"
---

<objective>
Lay the foundation for the lead state machine: add the `lead_status_source` audit column to `users`, create migration 0182 that adds the column and idempotently seeds the configurable window `leads.perdido_window_days` from the historical p90 (default 14 when the sample is thin), and expose a reusable int reader `getPerdidoWindowDays()` on `SettingsService`.

Purpose: Every downstream plan (cron, state-transition writes, backfill) depends on this column existing, the window key being seeded, and the reader being available. This is Wave 1 — nothing else can run until the migration is applied locally.

Output: Modified `users.ts` schema, migration `0182_lead_status_source.sql`, new settings key + reader, an integration test for the reader, and migration 0182 applied to the local DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/163-m-quina-de-estados-autom-tica-del-lead/163-CONTEXT.md
@.planning/phases/163-m-quina-de-estados-autom-tica-del-lead/163-PATTERNS.md
@CLAUDE.md
@.claude/skills/el-templo-db-migrations/SKILL.md

<interfaces>
From el-templo-api/src/db/schema/users.ts (lines 75-79, existing pattern to copy):
  export const leadStatusEnum = mysqlEnum("lead_status", ["en_seguimiento","ganado","perdido"]);
  // column at line 141: leadStatus: leadStatusEnum,

From el-templo-api/src/modules/settings/keys.ts (existing const to extend):
export const PRICING_SETTINGS_KEYS = { cardSurcharge: "...", zeroPrice: "..." } as const;

From el-templo-api/src/modules/settings/service.ts (getFlag reader to adapt boolean->int, lines 37-45):
private async getFlag(key: string): Promise<boolean> { ... row?.settingValue === ON ... }
constructor(private readonly db: DbInstance, private readonly log: FastifyBaseLogger)
imports: eq from drizzle-orm; systemSettings from ../../db/schema

Idempotent seed precedent — el-templo-api/src/db/migrations/0166_seed_pricing_card_surcharge.sql:9-13:
INSERT ... SELECT <value> WHERE NOT EXISTS (SELECT 1 FROM system_settings WHERE setting_key = ...)

"última booking is_trial no cancelada" derivation — 0170_lead_purchased_plan_ganado.sql:98-118
(MAX(id) per member, is_trial=1 AND booking_status <> 'cancelado').
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lead_status_source column to schema and write migration 0182</name>
  <files>el-templo-api/src/db/schema/users.ts, el-templo-api/src/db/migrations/0182_lead_status_source.sql</files>
  <read_first>
    - el-templo-api/src/db/schema/users.ts (see leadStatusEnum lines 75-79 and leadStatus column line 141; copy the enum+column shape and the heavy doc-comment house style)
    - el-templo-api/src/db/migrations/0170_lead_purchased_plan_ganado.sql (header block lines 1-27, backup+ALTER pattern, and the last-non-cancelled-trial-booking derivation lines 95-118)
    - el-templo-api/src/db/migrations/0166_seed_pricing_card_surcharge.sql (idempotent NOT EXISTS seed lines 5-13)
    - el-templo-api/src/db/run-migrations.ts (confirm the runner splits raw statements on `;` BEFORE stripping `--` comments — so NO `;` may appear inside comment text)
    - .claude/skills/el-templo-db-migrations/SKILL.md (runner rules, numbering, no-semicolon-in-comments)
  </read_first>
  <action>
    In users.ts: declare `leadStatusSourceEnum = mysqlEnum("lead_status_source", ["auto","manual"])` (first arg MUST be the exact column name `lead_status_source`, per the mysqlEnum-column-name reference in MEMORY). Add column `leadStatusSource: leadStatusSourceEnum` immediately after the existing `leadStatus` declaration. The value list `["auto","manual"]` MUST stay byte-identical to the ALTER enum in the migration (enum-drift lesson). Add a doc comment in house style noting: Phase 163 (D-07), values auto|manual, NULL = histórico/desconocido tratado como auto. No new index (D-05: idx_users_lead_status already exists).

    Create el-templo-api/src/db/migrations/0182_lead_status_source.sql (verified next free number: last applied is 0180, an unmerged 0181_debt_management.sql exists in tree, so 0182 is the next free — do NOT reuse 0181). Header block in the 0170 style: hand-written (db:generate hits the sessions.goal_plan_type interactive drift), NEVER drizzle-kit push/migrate, _migrations is source of truth, no `;` inside `--` comments. Migration body:
    (1) `ALTER TABLE `users` ADD COLUMN `lead_status_source` enum('auto','manual') NULL AFTER `lead_status`;`
    (2) Idempotent seed of `leads.perdido_window_days` following the 0166 NOT EXISTS pattern, but the value is the dynamically computed p90 (D-06). Compute p90 in pure MySQL 8 over the days-to-convert distribution of Ganado leads: for each user with `converted_at IS NOT NULL` that has a non-cancelled `is_trial=1` booking (reuse the MAX(id) per member / `booking_status <> 'cancelado'` derivation from 0170:98-118), days = DATEDIFF between that booking's `booking_date` and `MIN(subscriptions.created_at)` for the user; take the p90 of that distribution rounded up (CEIL). Fall back to literal 14 when fewer than 20 usable cases exist (COUNT gate). Keep all date math in the SQL DATE domain (DATEDIFF/CURDATE, never JS). The INSERT ... SELECT must be guarded by `WHERE NOT EXISTS (SELECT 1 FROM system_settings WHERE setting_key = 'leads.perdido_window_days')` so a prior PUT is never clobbered.

  </action>
  <verify>
    <automated>cd el-templo-api && grep -c "lead_status_source" src/db/schema/users.ts src/db/migrations/0182_lead_status_source.sql && grep -v '^--' src/db/migrations/0182_lead_status_source.sql | grep -c "leads.perdido_window_days"</automated>
  </verify>
  <acceptance_criteria>
    - users.ts contains `leadStatusSourceEnum` declared via `mysqlEnum("lead_status_source", ["auto","manual"])` and a `leadStatusSource` column
    - 0182_lead_status_source.sql contains the ADD COLUMN `lead_status_source` enum('auto','manual') statement
    - 0182 contains an idempotent `NOT EXISTS` guarded INSERT seeding `leads.perdido_window_days`
    - No line beginning with `--` in 0182 contains a `;` character (runner-safe)
    - The enum value list in schema (`["auto","manual"]`) matches the ALTER enum list byte-for-byte
  </acceptance_criteria>
  <done>Schema column and migration 0182 exist with matching enum values and an idempotent p90 seed; no semicolons in SQL comments.</done>
</task>

<task type="auto">
  <name>Task 2: Apply migration 0182 locally and typecheck</name>
  <files>el-templo-api (no source edits — migration apply + gate)</files>
  <read_first>
    - el-templo-api/package.json (confirm db:migrate script maps to the custom runner src/db/run-migrations.ts)
    - .claude/skills/el-templo-db-migrations/SKILL.md
  </read_first>
  <action>
    Apply the migration to the local DB with `pnpm db:migrate` (the custom runner — NEVER drizzle-kit migrate/push). This is a BLOCKING gate: the `lead_status_source` column and the `leads.perdido_window_days` row must exist before Task 3's test and before any Wave 2 plan runs. Then run the typecheck gate `pnpm tsc --noEmit`. Do NOT run the full local vitest suite (repo rule: suite runs in CI).
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm db:migrate && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm db:migrate` exits 0 and reports 0182 applied (or already applied)
    - `_migrations` table contains a row named `0182_lead_status_source.sql`
    - `pnpm tsc --noEmit` exits 0 with no type errors
  </acceptance_criteria>
  <done>Migration 0182 is applied and recorded; typecheck passes. Column and settings key exist in the local DB.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add settings key + getPerdidoWindowDays reader with test</name>
  <files>el-templo-api/src/modules/settings/keys.ts, el-templo-api/src/modules/settings/service.ts, el-templo-api/test/leads-perdido-window.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/settings/keys.ts (extend, keep the single-source-of-truth comment discipline)
    - el-templo-api/src/modules/settings/service.ts (adapt getFlag lines 37-45 from boolean to int; reuse constructor db + imports)
    - el-templo-api/test/reports-trial-sessions.test.ts (test header/lifecycle style, direct Drizzle seeding)
    - el-templo-api/test/helpers.ts (createTestApp, cleanAllTestData — note systemSettings IS in TABLES_TO_CLEAN line 216, so seed the key per-test)
  </read_first>
  <behavior>
    - getPerdidoWindowDays() returns 14 when the key `leads.perdido_window_days` is absent
    - getPerdidoWindowDays() returns 14 when the stored value is non-numeric or <= 0
    - getPerdidoWindowDays() returns the stored integer (e.g. 21) when a valid row is seeded
  </behavior>
  <action>
    In keys.ts add a `LEADS_SETTINGS_KEYS = { perdidoWindowDays: "leads.perdido_window_days" } as const` block with a doc comment (días de ventana antes de vencer En seguimiento → Perdido). Do NOT re-declare the literal anywhere else.

    In service.ts add a public `async getPerdidoWindowDays(): Promise<number>` that selects `settingValue` from `systemSettings` where `settingKey = LEADS_SETTINGS_KEYS.perdidoWindowDays`, coerces with `Number(row?.settingValue)`, and returns `Number.isFinite(n) && n > 0 ? Math.trunc(n) : 14` (default-14 fallback per D-06). No persistent cache (D-05: cron reads each run). Import LEADS_SETTINGS_KEYS from ./keys.

    Write test/leads-perdido-window.test.ts (integration, real eltemplo_test DB) covering the three behavior cases: seed the key via a direct `db.insert(systemSettings)` where needed, and clean per-test. Do NOT run the full suite as a gate — run this file only via targeted vitest.

  </action>
  <verify>
    <automated>cd el-templo-api && pnpm vitest run test/leads-perdido-window.test.ts && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - keys.ts exports `LEADS_SETTINGS_KEYS.perdidoWindowDays === "leads.perdido_window_days"`
    - service.ts has a public `getPerdidoWindowDays(): Promise<number>` returning 14 when the key is absent
    - test/leads-perdido-window.test.ts passes with cases: absent→14, invalid→14, seeded 21→21
    - `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The int window reader exists, defaults to 14, reads the seeded value, and is covered by a passing integration test.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                          |
| ---------------------------------------- | ------------------------------------------------------------------------------------ |
| migration runner → users/system_settings | Migration 0182 mutates schema and seeds a config row in bulk on the DB where it runs |

## STRIDE Threat Register

| Threat ID | Category               | Component                                    | Disposition | Mitigation Plan                                                                                  |
| --------- | ---------------------- | -------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------ |
| T-163-01  | Tampering              | 0182 seed clobbering an operator-set window  | mitigate    | Idempotent `WHERE NOT EXISTS` guard — never overwrites a prior `leads.perdido_window_days` value |
| T-163-02  | Information Disclosure | p90 computed over subscription/booking dates | accept      | Aggregate-only (single int), no PII persisted; value is internal config                          |
| T-163-03  | Tampering              | enum drift between schema and ALTER          | mitigate    | Enum value list kept byte-identical schema↔migration; acceptance criterion asserts it            |
| T-163-SC  | Tampering              | npm/pip/cargo installs                       | mitigate    | No new dependencies added in this plan                                                           |

</threat_model>

<verification>
- `lead_status_source` column exists on `users` (nullable enum auto|manual)
- `system_settings` has a `leads.perdido_window_days` integer row after 0182
- `getPerdidoWindowDays()` returns int with a 14 default; covered by passing test
- Migration 0182 recorded in `_migrations`; typecheck clean
- HUMAN VERIFICATION ITEM (D-06): the effective seeded p90 value against prod/staging data is validated during the deploy dry-run, not in this plan
</verification>

<success_criteria>

- Column, migration 0182, settings key, and reader all exist and are wired
- Migration applied locally; test green; typecheck clean
- No `;` in SQL comments; enum values byte-identical schema↔migration
  </success_criteria>

<output>
Create `.planning/phases/163-m-quina-de-estados-autom-tica-del-lead/163-01-SUMMARY.md` when done
</output>
