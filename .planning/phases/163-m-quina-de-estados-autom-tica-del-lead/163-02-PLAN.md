---
phase: 163-m-quina-de-estados-autom-tica-del-lead
plan: 02
type: execute
wave: 2
depends_on: [163-01]
files_modified:
  - el-templo-api/src/jobs/expire-lost-leads.ts
  - el-templo-api/src/index.ts
  - el-templo-api/test/expire-lost-leads.test.ts
autonomous: true
requirements: [AUTO-01, AUTO-04]
must_haves:
  truths:
    - "A daily cron flips leads (status prueba/freemium, lead_status en_seguimiento or NULL, converted_at IS NULL, purchased_plan_id IS NULL) to 'perdido' when their last non-cancelled trial booking is older than X days"
    - "The cron reads X from system_settings each run via getPerdidoWindowDays()"
    - "The cron NEVER touches a lead whose lead_status_source='manual'"
    - "runExpireLostLeads(db) is invocable directly and returns { expired, skippedManual }"
    - "The job is wired into index.ts and starts at boot"
  artifacts:
    - path: "el-templo-api/src/jobs/expire-lost-leads.ts"
      provides: "runExpireLostLeads + startExpireLostLeadsJob"
      exports: ["runExpireLostLeads", "startExpireLostLeadsJob"]
    - path: "el-templo-api/test/expire-lost-leads.test.ts"
      provides: "integration coverage of the sweep"
  key_links:
    - from: "el-templo-api/src/index.ts"
      to: "startExpireLostLeadsJob"
      via: "sync call after startAutoResumePausesJob(app.db)"
      pattern: "startExpireLostLeadsJob\\(app.db\\)"
    - from: "el-templo-api/src/jobs/expire-lost-leads.ts"
      to: "system_settings.leads.perdido_window_days"
      via: "getPerdidoWindowDays reader before the SQL sweep"
      pattern: "getPerdidoWindowDays|perdido_window_days"
---

<objective>
Build the daily cron that vences leads En seguimiento → Perdido: a new `src/jobs/expire-lost-leads.ts` in the existing node-cron infra that reads the configurable window X and flips qualifying leads to `perdido` while never overwriting a manually-set state, plus an invocable `runExpireLostLeads(db)` for tests and manual runs, wired into `index.ts`.

Purpose: This is the core of the state machine (AUTO-01) — automatic loss detection so gestión never has to manually mark stale trial leads. It reuses the exact "última booking is_trial no cancelada" derivation so cron/report/backfill agree.

Output: New job file, one-line wiring in index.ts, and an integration test covering expire / no-expire-in-window / skip-manual / reads-X-from-settings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/163-m-quina-de-estados-autom-tica-del-lead/163-CONTEXT.md
@.planning/phases/163-m-quina-de-estados-autom-tica-del-lead/163-PATTERNS.md
@CLAUDE.md

<interfaces>
From el-templo-api/src/jobs/mark-no-shows.ts (structure to copy):
  export async function runMarkNoShows(db: MySql2Database<typeof schema>): Promise<{updated,decremented}>
  export async function startMarkNoShowsJob(db): Promise<void>  // cron.schedule(cronExpr, handler, { timezone })
  imports: cron from "node-cron"; pino; { sql, eq, and, inArray } from "drizzle-orm"; * as schema; const log = pino({ name: "..." })

From el-templo-api/src/jobs/notification-cron.ts (internal single-tz batch, lines 299-303,498-504):
cron.schedule(expr, handler, { timezone: "America/Argentina/Buenos_Aires" })
SQL date domain: CURDATE(), DATE_ADD(col, INTERVAL n DAY) (lines 223-234)

From el-templo-api/src/index.ts (lines 17-39, wire alongside):
startAutoApproveJob(app.db); startAutoResumePausesJob(app.db); // sync internal-batch calls
await startMarkNoShowsJob(app.db);

From el-templo-api/src/modules/settings (163-01 output):
new SettingsService(db, log).getPerdidoWindowDays(): Promise<number> // default 14

Last-non-cancelled-trial-booking derivation — 0170_lead_purchased_plan_ganado.sql:98-118:
MAX(id) per member where is_trial=1 AND booking_status <> 'cancelado', join back to that booking's booking_date.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the expire-lost-leads cron job and wire it into index.ts</name>
  <files>el-templo-api/src/jobs/expire-lost-leads.ts, el-templo-api/src/index.ts</files>
  <read_first>
    - el-templo-api/src/jobs/mark-no-shows.ts (copy the imports+logger block, the exported invocable runMarkNoShows shape lines 177-189, and the start*Job try/catch + conditional info-log lines 191-223)
    - el-templo-api/src/jobs/notification-cron.ts (single-tz internal-batch schedule lines 299-303,498-504; SQL date math lines 223-234)
    - el-templo-api/src/index.ts (lines 17-39: job imports and the sync/await start calls)
    - el-templo-api/src/db/migrations/0170_lead_purchased_plan_ganado.sql (lines 95-118: the MAX(id) last-non-cancelled-trial-booking join to reuse verbatim in the sweep query)
    - el-templo-api/src/modules/settings/service.ts (getPerdidoWindowDays from 163-01)
    - el-templo-api/src/db/schema/users.ts (lead_status, lead_status_source, converted_at, purchased_plan_id, status columns)
  </read_first>
  <action>
    Create `expire-lost-leads.ts` mirroring mark-no-shows.ts. Export `async function runExpireLostLeads(db: MySql2Database<typeof schema>): Promise<{ expired: number; skippedManual: number }>`:
    (1) Read X first via `new SettingsService(db, <logger>).getPerdidoWindowDays()` — do NOT roll a bespoke settings read.
    (2) Identify candidate leads via the last-non-cancelled-trial-booking derivation (MAX(id) per member, `is_trial=1 AND booking_status <> 'cancelado'`, from 0170:95-118), gated on: `users.status IN ('prueba','freemium')`, `lead_status = 'en_seguimiento' OR lead_status IS NULL`, `converted_at IS NULL`, `purchased_plan_id IS NULL`, `deleted_at IS NULL`, and `DATE_ADD(b.booking_date, INTERVAL ${x} DAY) < CURDATE()`. Keep all date math in the SQL DATE domain (CURDATE/DATE_ADD) — never JS Date math (booking_date is a DATE column).
    (3) The flip UPDATE sets `lead_status='perdido'`, `lead_status_source='auto'` and additionally guards `WHERE (lead_status_source <> 'manual' OR lead_status_source IS NULL)` (NULL treated as auto per D-07) so a manual state is NEVER overwritten (D-04). Capture affected rows as `expired`.
    (4) Run a second COUNT query for candidates that matched the window but had `lead_status_source = 'manual'` → `skippedManual`, logged separately (specifics line 78).
    Export `function startExpireLostLeadsJob(db): void` using `cron.schedule("0 4 * * *", handler, { timezone: "America/Argentina/Buenos_Aires" })` (madrugada AR, discretion in D). Wrap the sweep in try/catch, `log.error({ err }, "Expire-lost-leads job failed")`, and only `log.info({ expired, skippedManual }, ...)` when expired>0 or skippedManual>0. Use a module-scoped `pino({ name: "expire-lost-leads" })` logger — never console.log.

    In index.ts: add `import { startExpireLostLeadsJob } from "./jobs/expire-lost-leads"` beside the other job imports, and call `startExpireLostLeadsJob(app.db)` synchronously (no await — it's an internal batch like startAutoApproveJob) after `startAutoResumePausesJob(app.db)`.

  </action>
  <verify>
    <automated>cd el-templo-api && grep -c "startExpireLostLeadsJob(app.db)" src/index.ts && grep -c "runExpireLostLeads\|getPerdidoWindowDays\|America/Argentina/Buenos_Aires" src/jobs/expire-lost-leads.ts && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - expire-lost-leads.ts exports `runExpireLostLeads` and `startExpireLostLeadsJob`
    - The sweep reads X via `getPerdidoWindowDays()` and interpolates it into `DATE_ADD(b.booking_date, INTERVAL x DAY) < CURDATE()`
    - The flip UPDATE includes `lead_status_source='auto'` AND a `lead_status_source <> 'manual' OR lead_status_source IS NULL` guard
    - index.ts calls `startExpireLostLeadsJob(app.db)` synchronously
    - `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The cron job exists, reads X from settings, flips only qualifying non-manual leads to perdido, tallies expired + skippedManual, and is wired at boot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests for the expire sweep</name>
  <files>el-templo-api/test/expire-lost-leads.test.ts</files>
  <read_first>
    - el-templo-api/test/reports-trial-sessions.test.ts (header/coverage-list style, direct Drizzle seeding of users + bookings + schedules)
    - el-templo-api/test/helpers.ts (createTestApp, cleanAllTestData — note systemSettings IS in TABLES_TO_CLEAN line 216, seed the window key per-test; users.lead_status_source needs no registration; no backup table involved here)
    - el-templo-api/src/jobs/expire-lost-leads.ts (the runExpireLostLeads signature from Task 1)
  </read_first>
  <behavior>
    - Case 1 (vence básico): a lead status='prueba', lead_status='en_seguimiento', converted_at NULL, whose last non-cancelled trial booking is X+1 days ago → after runExpireLostLeads, lead_status='perdido', lead_status_source='auto', expired>=1
    - Case 2 (no vence dentro de ventana): same lead but booking X-1 days ago → stays 'en_seguimiento'
    - Case 3 (no pisa manual): a lead with lead_status_source='manual' and a stale booking → stays unchanged, counted in skippedManual, not expired
    - Case 4 (lee X de settings): seed a custom leads.perdido_window_days (e.g. 7) and assert the X+1/X-1 boundary shifts accordingly
    - Guard: a converted lead (converted_at set / purchased_plan_id set) with a stale booking is NEVER flipped
  </behavior>
  <action>
    Write test/expire-lost-leads.test.ts (integration, real eltemplo_test DB) invoking `runExpireLostLeads(app.db)` directly (no cron wait), seeding users/bookings/schedules via Drizzle per the reports-trial-sessions.test.ts pattern. Seed `system_settings.leads.perdido_window_days` per-test (it is wiped by cleanAllTestData). Cover the five behavior cases above. Use SQL/relative dates anchored to CURDATE for booking_date so the window boundaries are deterministic. Do NOT run the full local suite as a gate — run this file only.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm vitest run test/expire-lost-leads.test.ts && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - test/expire-lost-leads.test.ts passes all five cases
    - The skip-manual case asserts skippedManual>=1 and the lead remains unchanged
    - The reads-X case asserts the boundary flips when a custom window is seeded
    - The converted-lead guard asserts no flip
    - `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The sweep is proven to expire only qualifying non-manual, non-converted leads and to honor the configured window.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                        |
| -------------------------- | ------------------------------------------------------------------ |
| cron → users (bulk UPDATE) | Daily job mutates user lead_status in bulk with no request context |

## STRIDE Threat Register

| Threat ID | Category               | Component                                         | Disposition | Mitigation Plan                                                                                                                                                                 |
| --------- | ---------------------- | ------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-163-04  | Tampering              | cron bulk flip hitting wrong rows (staff/actives) | mitigate    | Strict WHERE guard: status IN ('prueba','freemium'), lead_status en_seguimiento/NULL, converted_at IS NULL, purchased_plan_id IS NULL, deleted_at IS NULL — never staff/actives |
| T-163-05  | Tampering              | cron overwriting a manual override                | mitigate    | UPDATE guard `lead_status_source <> 'manual' OR NULL`; skip-manual asserted by integration test (Case 3)                                                                        |
| T-163-06  | Denial of Service      | heavy sweep query at scale                        | accept      | Dataset is small (~452 leads); query filters on indexed lead_status first (idx_users_lead_status)                                                                               |
| T-163-07  | Elevation of Privilege | new HTTP surface                                  | accept      | No new endpoint added; runExpireLostLeads is internal + cron-only                                                                                                               |
| T-163-SC  | Tampering              | npm/pip/cargo installs                            | mitigate    | No new dependencies added                                                                                                                                                       |

</threat_model>

<verification>
- Cron file present with runExpireLostLeads + startExpireLostLeadsJob; wired in index.ts
- Sweep reads X from settings each run; flips only qualifying non-manual, non-converted leads
- Integration test green (expire / no-expire / skip-manual / reads-X / converted-guard)
- Typecheck clean; no console.log; Pino logger used
</verification>

<success_criteria>

- AUTO-01 satisfied: daily cron vences leads to perdido after X days
- AUTO-04 (cron half): cron skips lead_status_source='manual'
- Test coverage proves the window boundary and the manual guard
  </success_criteria>

<output>
Create `.planning/phases/163-m-quina-de-estados-autom-tica-del-lead/163-02-SUMMARY.md` when done
</output>
