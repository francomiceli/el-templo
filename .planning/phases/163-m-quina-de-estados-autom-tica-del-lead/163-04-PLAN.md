---
phase: 163-m-quina-de-estados-autom-tica-del-lead
plan: 04
type: execute
wave: 2
depends_on: [163-01]
files_modified:
  - el-templo-api/src/db/migrations/0183_backfill_lost_leads.sql
  - el-templo-api/src/db/scripts/0183_backfill_lost_leads_dryrun.sql
  - el-templo-api/test/backfill-lost-leads.test.ts
autonomous: true
requirements: [AUTO-05]
must_haves:
  truths:
    - "Migration 0183 creates a backup table users_lead_backup_0183 before mutating"
    - "0183 retroactively flips En seguimiento / NULL leads with an expired last trial booking to 'perdido' (source auto), reading X from system_settings"
    - "0183 never touches leads with lead_status_source='manual', nor converted/actives/staff"
    - "A committed dry-run COUNT-only script reports how many rows the backfill would touch"
    - "Migration 0183 is applied locally and recorded in _migrations"
  artifacts:
    - path: "el-templo-api/src/db/migrations/0183_backfill_lost_leads.sql"
      provides: "backup table + retroactive reclassify using the cron rule"
      contains: "users_lead_backup_0183"
    - path: "el-templo-api/src/db/scripts/0183_backfill_lost_leads_dryrun.sql"
      provides: "COUNT-only preview of the backfill impact"
      contains: "leads.perdido_window_days"
  key_links:
    - from: "el-templo-api/src/db/migrations/0183_backfill_lost_leads.sql"
      to: "system_settings.leads.perdido_window_days"
      via: "subquery reading the window inside the UPDATE"
      pattern: "leads.perdido_window_days"
---

<objective>
Backfill the state machine retroactively: migration `0183_backfill_lost_leads.sql` that first snapshots the lead columns into `users_lead_backup_0183`, then applies the cron rule once to the existing ~112 En seguimiento leads whose last non-cancelled trial booking is already older than X days — flipping them to `perdido` (source `auto`) while skipping any manual overrides. Ships alongside a committed COUNT-only dry-run script so the prod impact can be validated by a human before the pipeline applies it.

Purpose: AUTO-05 — bring historical data in line with the new automatic loss detection without a manual sweep, safely (backup + dry-run), using the exact same window value and booking derivation the cron uses so cron/report/backfill agree.

Output: Migration 0183, a dry-run script under src/db/scripts/, an integration test proving the backfill logic, and the migration applied locally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/163-m-quina-de-estados-autom-tica-del-lead/163-CONTEXT.md
@.planning/phases/163-m-quina-de-estados-autom-tica-del-lead/163-PATTERNS.md
@CLAUDE.md
@.claude/skills/el-templo-db-migrations/SKILL.md

<interfaces>
Precedent — el-templo-api/src/db/migrations/0170_lead_purchased_plan_ganado.sql:
  backup table pattern lines 29-34: CREATE TABLE users_lead_backup_0170 AS SELECT ... WHERE ...
  last-non-cancelled-trial-booking + retroactive reclassify lines 95-118 (MAX(id) per member, is_trial=1 AND booking_status <> 'cancelado', b.booking_date < CURDATE())
  header discipline lines 1-27 (hand-written, no drizzle-kit, no `;` in `--` comments)

Settings key seeded by 163-01 migration 0182: system_settings.leads.perdido_window_days (integer)

Scripts location: el-templo-api/src/db/scripts/ (e.g. roster-collapse-effective-dated.sql) — OUTSIDE the migrations dir so the runner (globs migrations/\*.sql) never applies the dry-run.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write backfill migration 0183 + dry-run script</name>
  <files>el-templo-api/src/db/migrations/0183_backfill_lost_leads.sql, el-templo-api/src/db/scripts/0183_backfill_lost_leads_dryrun.sql</files>
  <read_first>
    - el-templo-api/src/db/migrations/0170_lead_purchased_plan_ganado.sql (backup pattern 29-34, retroactive reclassify 95-118, header 1-27)
    - el-templo-api/src/db/run-migrations.ts (confirm migrations/*.sql are globbed+applied by name; confirm the `;`-split-before-comment-strip behavior)
    - .claude/skills/el-templo-db-migrations/SKILL.md
    - el-templo-api/src/db/scripts/roster-collapse-effective-dated.sql (scripts-dir convention — this dir is NOT applied by the runner)
  </read_first>
  <action>
    Create el-templo-api/src/db/migrations/0183_backfill_lost_leads.sql (next free number after 0182; last applied 0180, unmerged 0181 exists — 0183 follows 0182 which 163-01 introduces). Header block in the 0170 style (hand-written, NEVER drizzle-kit push/migrate, _migrations is source of truth, no `;` inside `--` comments). Body:
    (1) Backup table (verbatim 0170:29-34 pattern, renamed): `CREATE TABLE users_lead_backup_0183 AS SELECT id, status, lead_status, lead_status_source, converted_at, updated_at FROM users WHERE lead_status IS NOT NULL OR converted_at IS NOT NULL;`
    (2) Retroactive reclassify = the cron rule applied once (adapt 0170:95-118). Join each user to their last non-cancelled trial booking (MAX(id) per member, is_trial=1 AND booking_status <> 'cancelado'). Flip `lead_status` from 'en_seguimiento' or NULL to 'perdido' and set `lead_status_source='auto'` WHERE: booking's `DATE_ADD(booking_date, INTERVAL x DAY) < CURDATE()` (x read via a subquery on `system_settings` where setting_key='leads.perdido_window_days', so the backfill uses the SAME value the cron will), `converted_at IS NULL`, `purchased_plan_id IS NULL`, `deleted_at IS NULL`, `status IN ('prueba','freemium')`, AND `(lead_status_source <> 'manual' OR lead_status_source IS NULL)` (respect manual overrides). Keep all date math in the SQL DATE domain (CURDATE/DATE_ADD).

    Create el-templo-api/src/db/scripts/0183_backfill_lost_leads_dryrun.sql: a COUNT-only SELECT (no mutation) using the identical WHERE predicate as the reclassify UPDATE, returning how many rows the backfill would flip. This file lives in scripts/ so the runner never applies it. Reference expectation from the brief (15/07): ≈112 En seguimiento vencidos should move to Perdido — the real prod dry-run is a pending human item.

  </action>
  <verify>
    <automated>cd el-templo-api && grep -c "users_lead_backup_0183" src/db/migrations/0183_backfill_lost_leads.sql && grep -v '^--' src/db/migrations/0183_backfill_lost_leads.sql | grep -c "leads.perdido_window_days" && test -f src/db/scripts/0183_backfill_lost_leads_dryrun.sql</automated>
  </verify>
  <acceptance_criteria>
    - 0183 creates `users_lead_backup_0183` via CREATE TABLE AS before any UPDATE
    - The UPDATE reads X via a `system_settings.leads.perdido_window_days` subquery (not a hardcoded literal)
    - The UPDATE predicate includes converted_at IS NULL, purchased_plan_id IS NULL, deleted_at IS NULL, status IN ('prueba','freemium'), and the manual guard
    - No line beginning with `--` in 0183 contains a `;`
    - The dry-run script exists in src/db/scripts/ and is COUNT-only (no INSERT/UPDATE/DELETE)
  </acceptance_criteria>
  <done>Backup-before-mutate backfill migration and a COUNT-only dry-run preview exist, sharing the cron's window and booking derivation.</done>
</task>

<task type="auto">
  <name>Task 2: Apply 0183 locally, verify backup, and add an integration test</name>
  <files>el-templo-api/test/backfill-lost-leads.test.ts</files>
  <read_first>
    - el-templo-api/test/reports-trial-sessions.test.ts (Drizzle seeding pattern)
    - el-templo-api/test/helpers.ts (createTestApp, cleanAllTestData — note: users_lead_backup_0183 is a CREATE TABLE AS, NOT a Drizzle-schema table, so it is NOT in TABLES_TO_CLEAN and needs no registration; systemSettings IS cleaned, seed the window per-test)
    - el-templo-api/src/db/run-migrations.ts (the exported runSqlStatements helper if reused by data-fix tests, per the Phase 111 comment ~line 37)
  </read_first>
  <action>
    Apply the migration locally with `pnpm db:migrate` (custom runner — NEVER drizzle-kit). This is a BLOCKING gate. Confirm `users_lead_backup_0183` was created and `_migrations` has a `0183_backfill_lost_leads.sql` row.

    Write test/backfill-lost-leads.test.ts (integration, real eltemplo_test DB) that validates the reclassify predicate in isolation: seed the `leads.perdido_window_days` key, seed (a) an En seguimiento lead with an expired last trial booking → expect it would flip to perdido, (b) an En seguimiento lead within the window → stays, (c) a lead_status_source='manual' expired lead → stays, (d) a converted lead → stays. Prove the predicate by running the same reclassify UPDATE (or the exported SQL-runner against the reclassify statement) and asserting the resulting lead_status values. Since 0183 runs once at migrate-time, the test exercises the RULE against test-seeded rows (the migration having already run does not affect newly-seeded rows). Run this file only — do NOT run the full suite.

  </action>
  <verify>
    <automated>cd el-templo-api && pnpm db:migrate && pnpm vitest run test/backfill-lost-leads.test.ts && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm db:migrate` exits 0; `_migrations` contains `0183_backfill_lost_leads.sql`
    - `users_lead_backup_0183` table exists after migration
    - test/backfill-lost-leads.test.ts passes cases (a) expired→perdido, (b) in-window→stays, (c) manual→stays, (d) converted→stays
    - `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Migration 0183 applied and recorded; backup table present; the reclassify rule proven by a passing integration test.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                      | Description                                         |
| --------------------------------------------- | --------------------------------------------------- |
| migration 0183 → users (one-time bulk UPDATE) | Retroactive reclassify mutates ~112 historical rows |

## STRIDE Threat Register

| Threat ID | Category    | Component                                          | Disposition | Mitigation Plan                                                                                                                                                                             |
| --------- | ----------- | -------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-163-11  | Tampering   | irreversible bulk reclassify                       | mitigate    | users_lead_backup_0183 snapshot created BEFORE the UPDATE enables audit/rollback (0170 precedent)                                                                                           |
| T-163-12  | Tampering   | backfill hitting wrong rows (actives/staff/manual) | mitigate    | Same strict predicate as cron: status IN ('prueba','freemium'), converted_at/purchased_plan_id IS NULL, deleted_at IS NULL, manual guard; COUNT-only dry-run for pre-apply human validation |
| T-163-13  | Repudiation | unverified prod impact                             | mitigate    | Committed dry-run script produces a pre-apply row count (expected ≈112) — human verification item before the pipeline applies                                                               |
| T-163-SC  | Tampering   | npm/pip/cargo installs                             | mitigate    | No new dependencies added                                                                                                                                                                   |

</threat_model>

<verification>
- 0183 snapshots to users_lead_backup_0183 then reclassifies using the cron's window + booking derivation
- Manual/converted/active/staff rows untouched
- Dry-run script present (COUNT-only); expected ≈112 flips — human validates against prod before apply
- Migration applied locally; integration test green; typecheck clean
- HUMAN VERIFICATION ITEM (D-08): run the dry-run count against staging/prod and confirm ≈112 before the pipeline applies 0183
</verification>

<success_criteria>

- AUTO-05 satisfied: retroactive backfill with backup + dry-run, sharing the cron's rule and window
  </success_criteria>

<output>
Create `.planning/phases/163-m-quina-de-estados-autom-tica-del-lead/163-04-SUMMARY.md` when done
</output>
