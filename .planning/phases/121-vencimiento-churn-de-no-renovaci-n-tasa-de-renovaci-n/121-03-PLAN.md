---
phase: 121-vencimiento-churn-de-no-renovaci-n-tasa-de-renovaci-n
plan: 03
type: execute
wave: 3
depends_on: ["121-01", "121-02"]
files_modified:
  - el-templo-api/src/modules/analytics/renewal-service.ts
  - el-templo-api/src/modules/analytics/schemas.ts
  - el-templo-api/src/modules/analytics/routes.ts
  - el-templo-api/src/modules/analytics/service.ts
  - el-templo-api/test/analytics/renewal.test.ts
autonomous: true
requirements: [RENOV-01, RENOV-02, RENOV-03, RENOV-04]
user_setup: []

must_haves:
  truths:
    - "GET /api/admin/analytics/renewal returns renovados / vencidos over the SAME expiry cohort as churn (RENOV-01)."
    - "The renovacion/reactivacion cutoff is configurable and defaults to 15 days (RENOV-02)."
    - "renewal is a numero vivo: renov% + churn% is not forced to 100; enGracia is exposed so consistency holds only when enGracia is 0 (RENOV-03)."
    - "Renewal is sortable/comparable by branch/country/duration/plan (RENOV-04)."
    - "The old getRenewalRate 7/14/30 metric stays live but is annotated deprecated, pointing to /renewal as canonical (D-09)."
  artifacts:
    - path: "el-templo-api/src/modules/analytics/renewal-service.ts"
      provides: "RenewalService.getRenewal(filters): person-based renewal over the shared expiry cohort"
      min_lines: 90
      exports: ["RenewalService"]
    - path: "el-templo-api/src/modules/analytics/routes.ts"
      provides: "GET /renewal registered under requireAdminAnalytics"
      contains: "/renewal"
    - path: "el-templo-api/src/modules/analytics/schemas.ts"
      provides: "renewalSchema with window querystring + full response declaration"
      contains: "renewalSchema"
  key_links:
    - from: "el-templo-api/src/modules/analytics/renewal-service.ts"
      to: "el-templo-api/src/modules/analytics/expiry-cohort.ts"
      via: "shared cohort + retention + maturity predicates (same denominator as churn)"
      pattern: "expiry-cohort"
    - from: "el-templo-api/src/modules/analytics/renewal-service.ts"
      to: "el-templo-api/src/modules/analytics/scope.ts"
      via: "applyScope on subscriptions.branchId"
      pattern: "applyScope"
    - from: "el-templo-api/src/modules/analytics/routes.ts"
      to: "el-templo-api/src/modules/analytics/renewal-service.ts"
      via: "new RenewalService(...).getRenewal"
      pattern: "RenewalService"
---

<objective>
Ship the person-based renewal metric (Block 2): a RenewalService that computes renovados / vencidos over the SAME expiry cohort as churn (the shared denominator), a GET /renewal endpoint under the ADMIN guard, its schema, and an integration test. Annotate the legacy getRenewalRate 7/14/30 metric as deprecated (D-09).

Purpose: Renovacion and churn must sit on one cohort so the two reconcile (RENOV-01). Renewal is a numero vivo: enGracia is exposed and the sum with churn only reaches 100 when enGracia is 0 (RENOV-03). The cutoff is configurable, default 15 (RENOV-02).

Output: renewal-service.ts, renewalSchema, /renewal route, deprecation annotation on getRenewalRate, renewal.test.ts.

Sequenced AFTER Plan 02 because both touch routes.ts, schemas.ts, and service.ts — wave 3 prevents file-ownership conflicts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-vencimiento-churn-de-no-renovaci-n-tasa-de-renovaci-n/121-CONTEXT.md
@.planning/phases/121-vencimiento-churn-de-no-renovaci-n-tasa-de-renovaci-n/121-PATTERNS.md
@.planning/phases/121-vencimiento-churn-de-no-renovaci-n-tasa-de-renovaci-n/121-01-SUMMARY.md
@.planning/phases/121-vencimiento-churn-de-no-renovaci-n-tasa-de-renovaci-n/121-02-SUMMARY.md

<interfaces>
<!-- Contracts from Plans 01/02. Build against these directly. -->

From src/modules/analytics/expiry-cohort.ts (Plan 01):

- expiryCohortConditions(from, to): SQL[] -- SAME cohort as churn (D-01/D-03)
- lastExpiryPerPersonExpr(from, to): SQL -- one row per person (D-04)
- retainedExpr(windowDays): SQL -- "renovo": later sub starts by E + window (D-05/D-06 = RENOV-01)
- maturedExpr(windowDays): SQL -- only matured persons enter num+den (D-08)
- RENOVATION_WINDOW_DEFAULT_DAYS = 15 -- RENOV-02 default cutoff

From src/modules/analytics/types.ts (Plan 01):

- RenewalAnalytics { windowDays; renewal: MetricShape; enGracia; breakdowns }
- RenewalSegmentRow { axis; key; renewal: MetricShape }
- AnalyticsFilters carries optional window.

Foundation (reuse):

- applyScope({ branchId, country, branchColumn: schema.subscriptions.branchId })
- metricShape(nominal, total) -> { nominal, percentage, n }
- breakdownKeyExpr / breakdownSegmentKey / durationTierFromDays (breakdowns.ts)

Analogs:

- src/modules/analytics/churn-service.ts (Plan 02) -- the sibling service; reuse its cohort-composition approach so renewal sits on the IDENTICAL denominator.
- src/modules/analytics/ticket-service.ts -- imports/DI/finalize structure.
- routes.ts /ticket block (360-389) + /churn (Plan 02) for the route pattern.
- schemas.ts churnSchema (Plan 02) + ticketSchema for the schema pattern.
- service.ts:676-721 getRenewalRate -- the legacy metric to annotate deprecated.
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RenewalService.getRenewal — renovados / vencidos over the shared cohort</name>
  <files>el-templo-api/src/modules/analytics/renewal-service.ts</files>
  <read_first>
    el-templo-api/src/modules/analytics/churn-service.ts (Plan 02 sibling: how the shared cohort + maturity + retention predicates are composed and scoped — mirror it so the denominator is IDENTICAL)
    el-templo-api/src/modules/analytics/expiry-cohort.ts (Plan 01 predicates)
    el-templo-api/src/modules/analytics/ticket-service.ts (imports block, DI ctor, Map accumulators, finalize methods)
    el-templo-api/src/modules/analytics/breakdowns.ts (breakdown helpers for RENOV-04)
    el-templo-api/src/modules/analytics/service.ts:676-721 (getRenewalRate: the renovados/vencidos math, but person-based on the SAME cohort, not 7/14/30 CURDATE windows)
  </read_first>
  <behavior>
    - getRenewal returns renewal.nominal as the count of DISTINCT matured persons whose last in-range expiry satisfies retainedExpr(window); renewal.n is the matured cohort size (the SAME denominator churn uses).
    - windowDays in the response equals the effective window (filters.window otherwise RENOVATION_WINDOW_DEFAULT_DAYS).
    - enGracia equals the count of cohort persons NOT yet matured (numero vivo): renewal% + churn% only sum to 100 when enGracia is 0.
    - A duration-change renewal and an early renewal both count as renovado; a paused sub in range never enters the cohort.
    - breakdowns return one RenewalSegmentRow per (axis, segment) for branch/country/duration/plan with metricShape.
  </behavior>
  <action>
    Create src/modules/analytics/renewal-service.ts mirroring ChurnService (Plan 02) so it composes the IDENTICAL cohort: spread expiryCohortConditions + lastExpiryPerPersonExpr + applyScope({ branchColumn: schema.subscriptions.branchId }).conditions, gate on maturedExpr(window) for both numerator and denominator. Imports block + DI ctor copied from ticket-service. Public method getRenewal(filters: AnalyticsFilters) returning Promise of RenewalAnalytics. Resolve effective window as filters.window otherwise RENOVATION_WINDOW_DEFAULT_DAYS. Compute via Promise.all:

    1. denominator = COUNT(DISTINCT user_id) of the matured cohort.
    2. numerator (renovados) = COUNT(DISTINCT user_id) where maturedExpr AND retainedExpr(window). renewal = metricShape(numerator, denominator).
    3. enGracia = COUNT(DISTINCT user_id) of the cohort NOT matured at window (RENOV-03 numero vivo).
    4. breakdowns (RENOV-04): for each axis in branch, country, duration, plan, group by breakdownKeyExpr(axis, cols), innerJoin branches/subscriptionPlans as needed, accumulate per breakdownSegmentKey, emit RenewalSegmentRow with metricShape. Duration buckets via durationTierFromDays in JS. Breakdowns are additive groupBy keys only, never access filters.

    renewal = retainedExpr; churn = NOT retainedExpr over the same matured cohort, so the two are consistent BY CONSTRUCTION when enGracia is 0 (do NOT force the sum to 100 — expose enGracia, RENOV-03 / D-07). Guard every division via metricShape. No any; errors bubble to handleServiceError at the route.

  </action>
  <verify>
    <automated>cd el-templo-api; G=$(grep -c 'expiry-cohort\|applyScope\|metricShape\|enGracia' src/modules/analytics/renewal-service.ts); T=$(pnpm exec tsc --noEmit -p tsconfig.json 2>/dev/null | grep -c 'renewal-service\.ts'); test "$G" -ge 4 -a "$T" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - src/modules/analytics/renewal-service.ts exports RenewalService with getRenewal(filters) returning RenewalAnalytics.
    - It composes the SAME cohort predicates from expiry-cohort.ts that churn-service uses (grep proves the import), so renewal.n equals churn.n for identical filters.
    - renewal uses retainedExpr (the complement of churn's NOT retainedExpr); enGracia is computed and returned; nothing forces renov+churn to 100.
    - Every query spreads applyScope(...).conditions on subscriptions.branchId; breakdown axes are additive only.
    - tsc --noEmit reports zero errors in renewal-service.ts; no any; no console.*.
  </acceptance_criteria>
  <done>RenewalService computes renovados/vencidos on the shared matured cohort with enGracia and 4-axis breakdowns, scope-guarded and div-by-zero safe.</done>
</task>

<task type="auto">
  <name>Task 2: Register GET /renewal route + renewalSchema + annotate getRenewalRate deprecated (D-09)</name>
  <files>el-templo-api/src/modules/analytics/schemas.ts, el-templo-api/src/modules/analytics/routes.ts, el-templo-api/src/modules/analytics/service.ts</files>
  <read_first>
    el-templo-api/src/modules/analytics/routes.ts:1-100 and :360-389 (imports, instantiation, /ticket block) + the /churn block added in Plan 02
    el-templo-api/src/modules/analytics/schemas.ts:9-24 (errorSchema + analyticsQuerystring) + the churnSchema added in Plan 02 (reuse its metricShapeSchema const and window querystring extension)
    el-templo-api/src/modules/analytics/service.ts:676-721 (getRenewalRate -- legacy metric to annotate, D-09)
    el-templo-api/src/modules/analytics/types.ts (RenewalAnalytics shape to declare)
  </read_first>
  <action>
    1) schemas.ts: add renewalSchema mirroring churnSchema. querystring: the SAME local analyticsQuerystring extension that adds window: { type: "integer", minimum: 1, maximum: 365 } (reuse the const Plan 02 introduced if exported; otherwise define an equivalent local one). response: 200 declaring every RenewalAnalytics field (windowDays, renewal MetricShape, enGracia, breakdowns array of { axis, key, renewal }) plus 400/401/403/500 errorSchema. Reuse the metricShapeSchema const from Plan 02 (or define it if absent). Declare every nested field (fast-json-stringify strips undeclared fields).

    2) routes.ts: import RenewalService and renewalSchema; instantiate const renewalService = new RenewalService(fastify.db, fastify.log) next to churnService. Register GET /renewal mirroring the /churn block: Querystring adds window?: number; preHandler [requireAdminAnalytics, requireBranchAccess({ from: "query.branchId", optional: true })]; build AnalyticsFilters from query + request.scope.country + window; call renewalService.getRenewal(filters); catch (err: unknown) -> handleServiceError(err, reply, request.log, "get renewal"). Do NOT touch old endpoints.

    3) service.ts: add a deprecation doc-comment above getRenewalRate (line 676) and an inline "// @deprecated Phase 121 D-09: use RenewalService / GET /renewal" marker on its signature, stating GET /api/admin/analytics/renewal is the canonical person-based replacement (physical removal in the admin-UI phase). Do NOT change behavior or remove callers.

  </action>
  <verify>
    <automated>cd el-templo-api; S=$(grep -c 'renewalSchema' src/modules/analytics/schemas.ts); R=$(grep -c '/renewal' src/modules/analytics/routes.ts); D=$(grep -c '@deprecated Phase 121 D-09' src/modules/analytics/service.ts); T=$(pnpm exec tsc --noEmit -p tsconfig.json 2>/dev/null | grep -c 'routes\.ts\|schemas\.ts\|service\.ts'); test "$S" -ge 1 -a "$R" -ge 1 -a "$D" -ge 3 -a "$T" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - GET /renewal is registered with requireAdminAnalytics + requireBranchAccess (gestion gets 403).
    - renewalSchema declares the window querystring with integer bounds and a 200 response covering every RenewalAnalytics field plus 400/401/403/500.
    - getRenewalRate carries an "@deprecated Phase 121 D-09" marker pointing to /renewal (now 3 total markers in service.ts including Plan 02's two); its behavior and callers are unchanged.
    - tsc --noEmit reports zero errors in routes.ts, schemas.ts, service.ts.
  </acceptance_criteria>
  <done>The /renewal endpoint is live under the ADMIN guard with a validated window param and a fully-declared schema; the legacy 7/14/30 metric is annotated deprecated.</done>
</task>

<task type="auto">
  <name>Task 3: Integration test for GET /renewal and the renov/churn reconciliation</name>
  <files>el-templo-api/test/analytics/renewal.test.ts</files>
  <read_first>
    el-templo-api/test/analytics/churn.test.ts (Plan 02 sibling test: scaffold, seeding, ADMIN 403 pattern, CURDATE-anchored maturity)
    el-templo-api/test/analytics/ticket.test.ts (service-through-app integration scaffold)
    el-templo-api/src/modules/analytics/renewal-service.ts (the service under test)
    el-templo-api/src/modules/analytics/churn-service.ts (to assert renov.n equals churn.n for the same filters)
  </read_first>
  <action>
    Create test/analytics/renewal.test.ts (real-MySQL; CI-only). Scaffold from churn.test.ts. Seed members (registerUser/passwordHash) and subscriptions (priceTypeApplied regular); anchor maturity to a CURDATE-derived reference date (evening-TZ flake guard). Cover:

    - RENOV-01: renewal.n (vencidos denominator) equals the churn denominator for identical filters (same cohort) — instantiate both ChurnService and RenewalService and assert renewal.renewal.n === churn.window.churn.n.
    - RENOV-01: renovados counts DISTINCT persons whose last expiry has a later sub within window; duration-change and early-renewal both count as renovado.
    - RENOV-02: default cutoff is 15 (no window param) and ?window=10 changes windowDays in the response.
    - RENOV-03: renov% + churn% sum to 100 ONLY when enGracia is 0; seed a person inside the grace window and assert enGracia is non-zero and the two percentages do NOT sum to 100.
    - RENOV-04: breakdowns return branch/country/duration/plan segments with nominal+%+n.
    - Auth: a gestion-role token gets 403 from GET /renewal.

  </action>
  <verify>
    <automated>cd el-templo-api; test -f test/analytics/renewal.test.ts; T=$(pnpm exec tsc --noEmit -p tsconfig.json 2>/dev/null | grep -c 'renewal\.test\.ts'); test "$T" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - test/analytics/renewal.test.ts covers RENOV-01..04 plus the ADMIN 403 case, including an explicit assertion that renewal.n equals churn's denominator for the same filters.
    - The numero vivo case (RENOV-03) asserts enGracia non-zero and renov%+churn% not equal to 100.
    - All member inserts use registerUser/passwordHash; all subscription inserts set priceTypeApplied regular; maturity anchored to CURDATE-derived date.
    - The test file type-checks with zero tsc errors (executes in CI on staging push).
  </acceptance_criteria>
  <done>GET /renewal has integration coverage proving RENOV-01..04, the shared-denominator reconciliation with churn, the numero vivo behavior, and the ADMIN guard; CI-ready.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                   |
| ---------------------------------------- | ----------------------------------------------------------------------------- |
| client to API (GET /renewal querystring) | Untrusted branchId/dateFrom/dateTo/window cross into the analytics service.   |
| API to DB                                | RenewalService composes SQL over subscriptions; scope must not be bypassable. |

## STRIDE Threat Register

| Threat ID | Category                               | Component                                       | Disposition | Mitigation Plan                                                                                                                                                                                              |
| --------- | -------------------------------------- | ----------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-121-07  | Spoofing / Elevation (authz)           | GET /renewal                                    | mitigate    | Route reuses requireAdminAnalytics (ADMIN_ROLES only) + onRequest operational-role gate + requireBranchAccess; gestion gets 403 (same as /churn). Verified by the test 403 case.                             |
| T-121-08  | Tampering (input validation)           | renewalSchema querystring window/branchId/dates | mitigate    | window declared integer minimum 1 / maximum 365 (rejects malformed/oversized N); branchId integer; dates format date; response schema declares only intended fields.                                         |
| T-121-09  | Information disclosure (scope leakage) | RenewalService queries                          | mitigate    | Every query spreads applyScope(...).conditions on subscriptions.branchId; breakdown axes are additive groupBy keys, never access filters; request.scope.country drives the country filter, not client input. |
| T-121-10  | Tampering (SQL injection)              | window/N interval in SQL                        | mitigate    | window is schema-validated then passed as a typed integer to retainedExpr/maturedExpr where it becomes sql.raw(String(n)); no raw user string reaches SQL.                                                   |
| T-121-SC  | Tampering                              | npm/pip/cargo installs                          | mitigate    | No new packages; no install task in this plan.                                                                                                                                                               |

</threat_model>

<verification>
- cd el-templo-api && pnpm exec tsc --noEmit passes for renewal-service.ts, schemas.ts, routes.ts, service.ts, renewal.test.ts.
- grep confirms /renewal registered with requireAdminAnalytics + renewalSchema.
- grep confirms 3 total "@deprecated Phase 121 D-09" markers in service.ts (2 from Plan 02 + getRenewalRate).
- The renewal test asserts renewal.n equals churn's denominator (shared cohort), the numero vivo behavior, and the ADMIN 403.
</verification>

<success_criteria>

- GET /api/admin/analytics/renewal returns renovados/vencidos over the SAME cohort as churn (RENOV-01) with a configurable cutoff defaulting to 15 (RENOV-02), a numero vivo via enGracia (RENOV-03), and 4-axis breakdowns (RENOV-04).
- The legacy getRenewalRate 7/14/30 stays live but annotated deprecated, pointing to /renewal (D-09 conscious deviation — literal deletion deferred to the admin-UI phase).
- Integration test is green-ready in CI.
  </success_criteria>

<output>
Create `.planning/phases/121-vencimiento-churn-de-no-renovaci-n-tasa-de-renovaci-n/121-03-SUMMARY.md` when done.
</output>
