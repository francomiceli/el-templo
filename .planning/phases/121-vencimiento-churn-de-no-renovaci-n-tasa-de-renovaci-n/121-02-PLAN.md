---
phase: 121-vencimiento-churn-de-no-renovaci-n-tasa-de-renovaci-n
plan: 02
type: execute
wave: 2
depends_on: ["121-01"]
files_modified:
  - el-templo-api/src/modules/analytics/churn-service.ts
  - el-templo-api/src/modules/analytics/schemas.ts
  - el-templo-api/src/modules/analytics/routes.ts
  - el-templo-api/src/modules/analytics/service.ts
  - el-templo-api/test/analytics/churn.test.ts
autonomous: true
requirements: [CHURN-01, CHURN-02, CHURN-03, CHURN-04, CHURN-05, CHURN-06]
user_setup: []

must_haves:
  truths:
    - "GET /api/admin/analytics/churn returns churn as DISTINCT persons whose sub expired in [from,to) without a new sub inside the window (CHURN-01)."
    - "The window N is configurable via querystring and the response carries the 5/10/15 comparative columns side by side (CHURN-02)."
    - "Only persons whose expiry was at least window days ago enter numerator AND denominator; in-grace persons are surfaced as enGracia, not counted (CHURN-03 / D-08)."
    - "Early renewal and duration change count as retention; a paused sub does not count as expired (CHURN-04)."
    - "The response includes a monthly historical series with provisional flags for immature cohorts (CHURN-05)."
    - "Churn opens by branch/country/duration/plan with nominal+%+n (CHURN-06)."
    - "The old countChurnedMembers/computeRetentionRate metrics remain live but are annotated deprecated, pointing to /churn as canonical (D-09)."
  artifacts:
    - path: "el-templo-api/src/modules/analytics/churn-service.ts"
      provides: "ChurnService.getChurn(filters): person-based churn with multi-N, maturity, series, breakdowns"
      min_lines: 120
      exports: ["ChurnService"]
    - path: "el-templo-api/src/modules/analytics/routes.ts"
      provides: "GET /churn registered under requireAdminAnalytics"
      contains: "/churn"
    - path: "el-templo-api/src/modules/analytics/schemas.ts"
      provides: "churnSchema with window querystring + full response declaration"
      contains: "churnSchema"
  key_links:
    - from: "el-templo-api/src/modules/analytics/churn-service.ts"
      to: "el-templo-api/src/modules/analytics/expiry-cohort.ts"
      via: "import cohort/retention/maturity predicates"
      pattern: "expiry-cohort"
    - from: "el-templo-api/src/modules/analytics/churn-service.ts"
      to: "el-templo-api/src/modules/analytics/scope.ts"
      via: "applyScope on subscriptions.branchId"
      pattern: "applyScope"
    - from: "el-templo-api/src/modules/analytics/routes.ts"
      to: "el-templo-api/src/modules/analytics/churn-service.ts"
      via: "new ChurnService(...).getChurn"
      pattern: "ChurnService"
---

<objective>
Ship the person-based churn metric (Block 1): a new ChurnService consuming the Plan 01 expiry-cohort engine, a GET /churn endpoint under the ADMIN guard, its response schema, and an integration test. Also annotate the legacy churn/retention metrics as deprecated, pointing to /churn as the canonical replacement (D-09).

Purpose: Replace the fragile updatedAt-based churn with a correct count of DISTINCT persons whose subscription expired in the panel range and did not renew within the configured window, with churn maduro (D-08), multi-N comparison, a monthly provisional series, and standard breakdowns.

Output: churn-service.ts, churnSchema, /churn route, deprecation annotations in service.ts, churn.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-vencimiento-churn-de-no-renovaci-n-tasa-de-renovaci-n/121-CONTEXT.md
@.planning/phases/121-vencimiento-churn-de-no-renovaci-n-tasa-de-renovaci-n/121-PATTERNS.md
@.planning/phases/121-vencimiento-churn-de-no-renovaci-n-tasa-de-renovaci-n/121-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 + the ticket-service analog. Build against these directly. -->

From src/modules/analytics/expiry-cohort.ts (Plan 01):

- expiryCohortConditions(from, to): SQL[] -- cohort = endDate in [from,to) AND status not paused (D-01/D-03)
- lastExpiryPerPersonExpr(from, to): SQL -- keep only each person's last in-range expiry (D-04)
- retainedExpr(windowDays): SQL -- TRUE if a later sub starts by E + window (D-05/D-06)
- maturedExpr(windowDays): SQL -- TRUE if expiry happened at least window days ago (D-08)
- RENOVATION_WINDOW_DEFAULT_DAYS = 15
- CHURN_COMPARISON_WINDOWS = [5, 10, 15] as const

From src/modules/analytics/types.ts (Plan 01):

- ChurnAnalytics, ChurnWindowResult, ChurnSeriesPoint, ChurnSegmentRow
- AnalyticsFilters now carries optional window.

Foundation (reuse, do NOT reimplement):

- applyScope({ branchId, country, branchColumn: schema.subscriptions.branchId }) returns { conditions, needsBranchJoin }
- metricShape(nominal, total) returns { nominal, percentage, n } (guards div-by-zero to 0)
- bucketExpr(schema.subscriptions.endDate, "monthly") -- the CHURN-05 series bucket key
- breakdownKeyExpr(axis, cols) / breakdownSegmentKey(axis, row) / durationTierFromDays (breakdowns.ts)
- deriveDurationTier(durationDays)

Analog (copy its structure verbatim):

- src/modules/analytics/ticket-service.ts -- imports block (lines 60-77), DI ctor (172-176),
  getX fan-out via Promise.all, per-segment Map accumulator, finalize methods, mean/metricShape guards.

Route analog: routes.ts:360-389 (/ticket block) + routes.ts:73 (service instantiation) +
requireAdminAnalytics (48-58) + requireBranchAccess preHandler + handleServiceError catch.

Schema analog: ticketSchema (schemas.ts:552-574) + analyticsQuerystring (17-24) + errorSchema (9-15).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChurnService.getChurn — person-based churn with multi-N, maturity, series, breakdowns</name>
  <files>el-templo-api/src/modules/analytics/churn-service.ts</files>
  <read_first>
    el-templo-api/src/modules/analytics/ticket-service.ts (structural twin: imports, DI ctor, Promise.all fan-out, Map accumulators, finalize methods, guarded mean/metricShape)
    el-templo-api/src/modules/analytics/expiry-cohort.ts (Plan 01 predicates this service composes)
    el-templo-api/src/modules/analytics/breakdowns.ts (breakdownKeyExpr / breakdownSegmentKey / durationTierFromDays for CHURN-06)
    el-templo-api/src/modules/analytics/cohorts.ts (bucketExpr monthly for the CHURN-05 series)
    el-templo-api/src/modules/analytics/metric-shape.ts (metricShape envelope)
    el-templo-api/src/modules/analytics/service.ts:676-721 (getRenewalRate: the Promise.all-of-windows shape to mirror for multi-N)
  </read_first>
  <behavior>
    - getChurn returns window.churn.nominal as the count of DISTINCT matured persons whose last in-range expiry did NOT satisfy retainedExpr(window); window.churn.n is the matured cohort size (denominator).
    - comparison contains one ChurnWindowResult per CHURN_COMPARISON_WINDOWS value (churn@5/@10/@15), each over the same cohort but its own maturity+retention window.
    - enGracia is the count of cohort persons NOT yet matured at the official window (excluded from numerator AND denominator) so churn% + renov% only reconcile when enGracia is 0.
    - A person whose only later sub is a duration change or an early renewal is counted as retained (not churned).
    - A status paused sub whose endDate is in range never enters the cohort.
    - series has one ChurnSeriesPoint per month bucket of the cohort; provisional is true when that month's cohort has not fully matured (within window days of today).
    - breakdowns has one ChurnSegmentRow per (axis, segment) for branch/country/duration/plan with metricShape.
  </behavior>
  <action>
    Create src/modules/analytics/churn-service.ts. Copy the ticket-service shape: imports block (MySql2Database, the and/eq/sql/isNull/SQL drizzle imports, FastifyBaseLogger, schema as star import, applyScope, the Plan 01 expiry-cohort exports, metricShape, bucketExpr, the breakdowns helpers, deriveDurationTier, and the Plan 01 Churn wire types). DI constructor (db, log) identical to TicketService lines 172-176.

    Public method getChurn(filters: AnalyticsFilters) returning Promise of ChurnAnalytics. Resolve effective window as filters.window otherwise RENOVATION_WINDOW_DEFAULT_DAYS. Compose every query from: the spread of expiryCohortConditions(filters.dateFrom, filters.dateTo), lastExpiryPerPersonExpr(filters.dateFrom, filters.dateTo) as a WHERE fragment (D-04 one row per person), the applyScope({ branchColumn: schema.subscriptions.branchId }).conditions, plus maturity/retention predicates as needed. ALL queries route through applyScope (T-121-03 scope, T-120-10) and operate on subscriptions only. Use Promise.all to fan out:

    1. Official window: COUNT(DISTINCT user_id) of the matured cohort (maturedExpr(window)) as denominator; COUNT(DISTINCT user_id) where maturedExpr AND NOT retainedExpr(window) as numerator. window.churn is metricShape(numerator, denominator).
    2. Multi-N comparison: for each w in CHURN_COMPARISON_WINDOWS, the same numerator/denominator at window w (mirror getRenewalRate Promise.all-of-windows). Push ChurnWindowResult of windowDays w and churn.
    3. enGracia: COUNT(DISTINCT user_id) of the cohort NOT matured at the official window (cohort minus matured).
    4. Series (CHURN-05): group the cohort by bucketExpr(schema.subscriptions.endDate, "monthly"); per bucket compute matured churn metricShape; mark provisional true when the bucket month is within window days of CURDATE (cohort not fully matured). Use the last-expiry rows.
    5. Breakdowns (CHURN-06): for each axis in branch, country, duration, plan: group by breakdownKeyExpr(axis, cols) (cols pull branches.name/country, subscriptionPlans.name+country, subscriptionPlans.durationDays); innerJoin branches/subscriptionPlans as the axis requires; accumulate per breakdownSegmentKey into a Map; emit ChurnSegmentRow with metricShape. Duration axis buckets via durationTierFromDays in JS (the 1/31 threshold stays in duration-tier.ts only). Breakdowns are ADDITIVE grouping keys, NEVER access filters (access stays in applyScope, T-120-06).

    Guard every division through metricShape (never NaN). No any; do not catch here (errors bubble to handleServiceError at the route). No fenced code; follow ticket-service finalize-method decomposition so getChurn stays readable.

  </action>
  <verify>
    <automated>cd el-templo-api; G=$(grep -c 'expiry-cohort\|applyScope\|metricShape' src/modules/analytics/churn-service.ts); T=$(pnpm exec tsc --noEmit -p tsconfig.json 2>/dev/null | grep -c 'churn-service\.ts'); test "$G" -ge 3 -a "$T" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - src/modules/analytics/churn-service.ts exports ChurnService with getChurn(filters) returning ChurnAnalytics.
    - The service imports and uses expiryCohortConditions, lastExpiryPerPersonExpr, retainedExpr, maturedExpr from expiry-cohort.ts (grep proves import).
    - Every query spreads applyScope(...).conditions on subscriptions.branchId; no breakdown axis is used as an access filter.
    - window, comparison (one entry per CHURN_COMPARISON_WINDOWS), enGracia, series (monthly with provisional), and breakdowns (4 axes) are all populated.
    - tsc --noEmit reports zero errors in churn-service.ts; no any; no console.*.
  </acceptance_criteria>
  <done>ChurnService computes person-based churn with multi-N, maturity gating, monthly provisional series, and 4-axis breakdowns, all scope-guarded and div-by-zero safe.</done>
</task>

<task type="auto">
  <name>Task 2: Register GET /churn route + churnSchema + annotate legacy metrics deprecated (D-09)</name>
  <files>el-templo-api/src/modules/analytics/schemas.ts, el-templo-api/src/modules/analytics/routes.ts, el-templo-api/src/modules/analytics/service.ts</files>
  <read_first>
    el-templo-api/src/modules/analytics/routes.ts:1-100 (imports, requireAdminAnalytics, service instantiation block, onRequest guard)
    el-templo-api/src/modules/analytics/routes.ts:360-389 (the /ticket block to mirror)
    el-templo-api/src/modules/analytics/schemas.ts:9-24 (errorSchema + analyticsQuerystring) and :548-574 (ticketSchema shape)
    el-templo-api/src/modules/analytics/service.ts:329-358 (countChurnedMembers) and :360-426 (computeRetentionRate) -- the legacy methods to annotate (D-09)
    el-templo-api/src/modules/analytics/types.ts (ChurnAnalytics shape to declare in the schema)
  </read_first>
  <action>
    1) schemas.ts: add churnSchema mirroring ticketSchema. querystring: a LOCAL extension of analyticsQuerystring that ADDS window: { type: "integer", minimum: 1, maximum: 365 } (validate + bound the N to reject malformed/oversized values, T-121-04). Do NOT mutate the shared analyticsQuerystring const. response: 200 declaring EVERY wire field of ChurnAnalytics (window, comparison array, enGracia, series array with provisional, breakdowns array) plus 400/401/403/500 each errorSchema. Declare every nested field explicitly (fast-json-stringify STRIPS undeclared fields). Build a small reusable metricShapeSchema const ({ nominal, percentage, n }) and reference it for each churn MetricShape.

    2) routes.ts: import ChurnService and churnSchema; instantiate const churnService = new ChurnService(fastify.db, fastify.log) next to ticketService (line 73). Register GET /churn mirroring the /ticket block: Querystring generic adds window?: number; preHandler [requireAdminAnalytics, requireBranchAccess({ from: "query.branchId", optional: true })]; build AnalyticsFilters from query branchId/dateFrom/dateTo + request.scope.country + window; call churnService.getChurn(filters); catch (err: unknown) -> handleServiceError(err, reply, request.log, "get churn"). Do NOT touch the old endpoints.

    3) service.ts: add a deprecation doc-comment above countChurnedMembers (line 329) and computeRetentionRate (line 360) stating they are DEPRECATED per Phase 121 D-09 and that GET /api/admin/analytics/churn is the canonical person-based replacement; physical removal happens in the admin-UI phase. Add an inline "// @deprecated Phase 121 D-09: use ChurnService / GET /churn" marker on each method signature line. Do NOT change their behavior or remove callers (the admin dashboard still consumes them).

  </action>
  <verify>
    <automated>cd el-templo-api; S=$(grep -c 'churnSchema' src/modules/analytics/schemas.ts); R=$(grep -c '"/churn"\|/churn' src/modules/analytics/routes.ts); D=$(grep -c '@deprecated Phase 121 D-09' src/modules/analytics/service.ts); T=$(pnpm exec tsc --noEmit -p tsconfig.json 2>/dev/null | grep -c 'routes\.ts\|schemas\.ts\|service\.ts'); test "$S" -ge 1 -a "$R" -ge 1 -a "$D" -ge 2 -a "$T" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - GET /churn is registered with requireAdminAnalytics + requireBranchAccess; gestion role would receive 403 (same guard as /ticket).
    - churnSchema declares the window querystring with integer bounds and a 200 response covering every ChurnAnalytics field plus 400/401/403/500 errorSchema.
    - countChurnedMembers and computeRetentionRate each carry an "@deprecated Phase 121 D-09" marker pointing to /churn; their behavior and callers are unchanged.
    - tsc --noEmit reports zero errors in routes.ts, schemas.ts, service.ts.
  </acceptance_criteria>
  <done>The /churn endpoint is live under the ADMIN guard with a validated window param and a fully-declared schema; the legacy metrics are annotated deprecated without breaking the admin dashboard.</done>
</task>

<task type="auto">
  <name>Task 3: Integration test for GET /churn and ChurnService</name>
  <files>el-templo-api/test/analytics/churn.test.ts</files>
  <read_first>
    el-templo-api/test/analytics/ticket.test.ts (full service-through-app integration scaffold: createTestApp, getAuthToken, registerUser, createStaffUser, cleanAllTestData, subscription seed with priceTypeApplied regular, ADMIN 403 pattern)
    el-templo-api/test/analytics/breakdowns-cohorts.test.ts:40-130 (raw user insert with passwordHash; beforeEach clean)
    el-templo-api/src/modules/analytics/churn-service.ts (the service under test)
    el-templo-api/test/helpers.ts (helpers)
  </read_first>
  <action>
    Create test/analytics/churn.test.ts (real-MySQL; CI-only). Scaffold from ticket.test.ts. Seed members via registerUser (or raw passwordHash) and subscriptions with priceTypeApplied: "regular". Compute a reference today via SQL CURDATE or the project date helper so maturity assertions avoid the evening-TZ flake (MEMORY: analytics seed UTC-vs-local skew after ~21:00 AR). Cover:

    - CHURN-01: a person whose last in-range expiry has no later sub within window appears in churn.nominal; the denominator counts DISTINCT persons (a person with two in-range expiries counts once).
    - CHURN-02: passing ?window=10 changes the official window; comparison returns entries for 5/10/15.
    - CHURN-03/D-08: a person whose expiry is inside the grace window is excluded from numerator AND denominator and counted in enGracia.
    - CHURN-04/D-05/D-06: a duration-change renewal and an early renewal both count as retained (not churned); a paused sub in range does not enter the cohort.
    - CHURN-05: series is monthly and the most-recent (immature) month is flagged provisional true while an older fully-matured month is provisional false.
    - CHURN-06: breakdowns return branch/country/duration/plan segments with nominal+%+n.
    - Auth: a gestion-role token gets 403 from GET /churn (mirror ticket.test ADMIN 403 case).

  </action>
  <verify>
    <automated>cd el-templo-api; test -f test/analytics/churn.test.ts; T=$(pnpm exec tsc --noEmit -p tsconfig.json 2>/dev/null | grep -c 'churn\.test\.ts'); test "$T" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - test/analytics/churn.test.ts covers CHURN-01..06 plus the ADMIN 403 case, with at least one assertion per requirement.
    - All member inserts use registerUser/passwordHash; all subscription inserts set priceTypeApplied regular.
    - Maturity assertions are anchored to a CURDATE-derived reference date (no hardcoded today).
    - The test file type-checks with zero tsc errors (executes in CI on staging push, not locally).
  </acceptance_criteria>
  <done>GET /churn and ChurnService have integration coverage proving CHURN-01..06 and the ADMIN guard; the file compiles and is CI-ready.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                                                 |
| -------------------------------------- | --------------------------------------------------------------------------- |
| client to API (GET /churn querystring) | Untrusted branchId/dateFrom/dateTo/window cross into the analytics service. |
| API to DB                              | ChurnService composes SQL over subscriptions; scope must not be bypassable. |

## STRIDE Threat Register

| Threat ID | Category                               | Component                                     | Disposition | Mitigation Plan                                                                                                                                                                                                              |
| --------- | -------------------------------------- | --------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-121-03  | Spoofing / Elevation (authz)           | GET /churn                                    | mitigate    | Route reuses requireAdminAnalytics (ADMIN_ROLES only) + the plugin onRequest operational-role gate + requireBranchAccess; gestion role gets 403 exactly like /ticket. Verified by the test's 403 case.                       |
| T-121-04  | Tampering (input validation)           | churnSchema querystring window/branchId/dates | mitigate    | window declared as integer with minimum 1 / maximum 365 (rejects malformed/oversized N); branchId integer, dates format date. fast-json-stringify response schema declares only the intended fields.                         |
| T-121-05  | Information disclosure (scope leakage) | ChurnService queries                          | mitigate    | Every query spreads applyScope(...).conditions on subscriptions.branchId; breakdown axes are additive groupBy keys only, never access filters (T-120-06). request.scope.country drives the country filter, not client input. |
| T-121-06  | Tampering (SQL injection)              | window/N interval in SQL                      | mitigate    | window is integer-validated at the schema then passed as a typed number to retainedExpr/maturedExpr where it becomes sql.raw(String(n)); no raw user string reaches SQL.                                                     |
| T-121-SC  | Tampering                              | npm/pip/cargo installs                        | mitigate    | No new packages; no install task in this plan.                                                                                                                                                                               |

</threat_model>

<verification>
- cd el-templo-api && pnpm exec tsc --noEmit passes for churn-service.ts, schemas.ts, routes.ts, service.ts, churn.test.ts.
- grep confirms /churn is registered with requireAdminAnalytics and churnSchema.
- grep confirms two "@deprecated Phase 121 D-09" markers in service.ts; the legacy methods are unchanged otherwise.
- The churn test asserts the ADMIN 403, multi-N comparison, enGracia exclusion, retention edge cases, and provisional series flag.
</verification>

<success_criteria>

- GET /api/admin/analytics/churn returns person-based churn (CHURN-01) with a configurable + multi-N window (CHURN-02), churn maduro + enGracia (CHURN-03/D-08), retention/duration/paused rules (CHURN-04), a monthly provisional series (CHURN-05), and 4-axis breakdowns (CHURN-06).
- The legacy churn/retention metrics stay live but are annotated deprecated, pointing to /churn (D-09 conscious deviation from ROADMAP success criterion #1 — literal deletion deferred to the admin-UI phase).
- Integration test is green-ready in CI.
  </success_criteria>

<output>
Create `.planning/phases/121-vencimiento-churn-de-no-renovaci-n-tasa-de-renovaci-n/121-02-SUMMARY.md` when done.
</output>
