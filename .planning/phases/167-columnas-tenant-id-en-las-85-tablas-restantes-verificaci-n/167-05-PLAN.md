---
phase: 167-columnas-tenant-id-en-las-85-tablas-restantes-verificaci-n
plan: 05
type: execute
wave: 5
depends_on: [167-04]
files_modified:
  - el-templo-api/src/db/schema/aura-balances.ts
  - el-templo-api/src/db/schema/aura-transactions.ts
  - el-templo-api/src/db/schema/aura-config.ts
  - el-templo-api/src/db/schema/check-in-responses.ts
  - el-templo-api/src/db/schema/onboarding-analytics.ts
  - el-templo-api/src/db/schema/micro-programs.ts
  - el-templo-api/src/db/schema/program-enrollments.ts
  - el-templo-api/src/db/schema/plan-programs.ts
  - el-templo-api/src/db/schema/blog-posts.ts
  - el-templo-api/src/db/schema/blog-tags.ts
  - el-templo-api/src/db/schema/academy-inquiries.ts
  - el-templo-api/src/db/schema/app-waitlist.ts
  - el-templo-api/src/db/schema/franchise-applications.ts
  - el-templo-api/src/db/schema/gladius-products.ts
  - el-templo-api/src/db/schema/gladius-inquiries.ts
  - el-templo-api/src/db/schema/tv.ts
  - el-templo-api/src/db/migrations/0195_tenant_id_templo_rest.sql
autonomous: true
requirements: [COL-01]
must_haves:
  truths:
    - "Las 20 tablas restantes de modulos Templo (AURA, wellness, programs, marketing y TV) tienen tenant_id NOT NULL DEFAULT 1 con FK a tenants y el 100% de las filas en 1"
    - "Con este plan, las 85 tablas de la tanda C tienen la columna: 88 FKs hacia tenants en la DB local"
    - "tv_pairings recibe la columna aunque el flujo device-code sea pre-tenant por diseno (mina M7), con la exencion anotada para la fase 169"
  artifacts:
    - path: "el-templo-api/src/db/migrations/0195_tenant_id_templo_rest.sql"
      provides: "Tanda C4: tenant_id en las 20 tablas restantes de modulos Templo"
      contains: "ADD COLUMN tenant_id INT NULL DEFAULT 1"
    - path: "el-templo-api/src/db/schema/tv.ts"
      provides: "Las 3 tablas de TV de sucursal con tenantId y la mina M7 anotada"
      contains: "tenantIdColumn()"
  key_links:
    - from: "el-templo-api/src/db/schema/tv.ts"
      to: "el-templo-api/src/db/schema/tenant-column.ts"
      via: "import { tenantIdColumn } from './tenant-column'"
      pattern: "tenantIdColumn"
    - from: "el-templo-api/src/db/migrations/0195_tenant_id_templo_rest.sql"
      to: "el-templo-api/src/db/schema/tenants.ts"
      via: "FOREIGN KEY (tenant_id) REFERENCES tenants(id)"
      pattern: "REFERENCES tenants\\(id\\)"
---

<objective>
Tanda C4 del doc 06 §1: las 20 tablas restantes de modulos Templo (gamificacion AURA, wellness y
onboarding, programs, marketing/marca y TV de sucursal) reciben `tenant_id NOT NULL DEFAULT 1` con FK
a `tenants`. Con este plan se completan las **85 tablas** de la tanda C.

Purpose: cierra COL-01 a nivel de schema y de DB local. A partir de aca, el modelo de datos completo
es tenant-aware aunque el codigo todavia no lo use, que es exactamente el end state de la fase.
Output: 16 schemas Drizzle con 20 tablas anotadas + `0195_tenant_id_templo_rest.sql` aplicada local.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/166-fundaci-n-tenants-anclas-y-scope-server-side/166-PATTERNS.md
@CLAUDE.md

<working_directory>
Todo el codigo se escribe en `/home/franco/projects/et-167-columnas` (worktree del plan 167-01, rama
`feat/167-tenant-columns`). No tocar el checkout principal ni los worktrees `et-166-tenancy` y
`et-164-tv` — este ultimo tiene la fase de TV abierta y toca `tv.ts`: si al editar `tv.ts` aparece
una diferencia inesperada respecto de `origin/master`, detenerse y reportar en vez de resolverla.
Nunca `git add -A`: stagear por ruta.

Si el plan 167-01 reservo numeros distintos de 0192-0195, usar el CUARTO numero reservado en vez de
`0195`. Si el 167-01 activo el fallback del helper, la linea a insertar es `...tenantIdColumns`.
</working_directory>

<interfaces>
Mismo contrato y mismo patron SQL de 4 statements por tabla que el plan 167-02 (ver su bloque
`<interfaces>`): `ADD COLUMN tenant_id INT NULL DEFAULT 1` -> `UPDATE ... WHERE tenant_id IS NULL` ->
`MODIFY COLUMN tenant_id INT NOT NULL DEFAULT 1` -> `ADD CONSTRAINT fk_<t>_tenant FOREIGN KEY
(tenant_id) REFERENCES tenants(id)`. Sin clausula de posicionamiento, sin `ADD INDEX` explicito,
`DEFAULT 1` repetido en el MODIFY.

Particularidades de este grupo (doc 05 §2.4-2.8 y §6):

- **Dos archivos declaran dos tablas**: `micro-programs.ts` (programs, program_content_blocks) y
  `blog-tags.ts` (blog_tags, blog_post_tags). `tv.ts` declara **tres** (tv_devices, tv_pairings,
  tv_class_state).
- `tv_pairings` es **pre-tenant por diseno** (mina M7): `branch_id` es NULL hasta que el staff reclama
  el dispositivo, y `user_code` UNIQUE global es necesario para que el claim resuelva por codigo sin
  scope. Recibe la columna igual (el claim la estampa con el scope del staff en la fase 169).
- `blog_post_tags.post_id` / `.tag_id` son **FKs logicas sin constraint** (mina M9): las verifica el
  script del plan 167-06 con joins manuales.
- `plan_programs` es un acople core->Templo (une `subscription_plans` con `programs`): recibe la
  columna como cualquier otra.
- Uniques globales que **NO se tocan aca**: slugs de `blog_posts` / `blog_tags` / `gladius_products`,
  `aura_config.source_type`, `tv_devices.token_hash`, `tv_pairings.user_code` y `device_code_hash`
  (estas dos ultimas quedan globales a proposito para siempre — lista M8 aprobada).
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: tenantId en las 20 tablas restantes de modulos Templo</name>
  <files>los 16 archivos de `el-templo-api/src/db/schema/` listados en el action</files>
  <read_first>
    - /home/franco/projects/et-167-columnas/el-templo-api/src/db/schema/tenant-column.ts (el helper: forma exacta de la columna)
    - /home/franco/projects/et-167-columnas/el-templo-api/src/db/schema/tv.ts (3 tablas, incluida la pre-tenant `tv_pairings`)
    - /home/franco/projects/et-167-columnas/el-templo-api/src/db/schema/micro-programs.ts y blog-tags.ts (2 tablas cada uno)
    - Un archivo ya editado por el plan 167-02 (por ejemplo `bookings.ts`) para copiar comentario y ubicacion exactos
    - Cada uno de los 16 archivos ANTES de editarlo
  </read_first>
  <action>
    Agregar `tenant_id` a estas 20 tablas (tabla -> archivo en `el-templo-api/src/db/schema/`):
    AURA (3): aura_balances -> aura-balances.ts, aura_transactions -> aura-transactions.ts,
    aura_config -> aura-config.ts.
    wellness y onboarding (2): check_in_responses -> check-in-responses.ts,
    onboarding_analytics -> onboarding-analytics.ts.
    programs (4): programs -> micro-programs.ts, program_content_blocks -> micro-programs.ts,
    program_enrollments -> program-enrollments.ts, plan_programs -> plan-programs.ts.
    marketing y marca (8): blog_posts -> blog-posts.ts, blog_tags -> blog-tags.ts,
    blog_post_tags -> blog-tags.ts, academy_inquiries -> academy-inquiries.ts,
    app_waitlist -> app-waitlist.ts, franchise_applications -> franchise-applications.ts,
    gladius_products -> gladius-products.ts, gladius_inquiries -> gladius-inquiries.ts.
    TV de sucursal (3): tv_devices -> tv.ts, tv_pairings -> tv.ts, tv_class_state -> tv.ts.

    **Edicion por tabla, identica al plan 167-02:** import `tenantIdColumn` una vez por archivo, y
    `tenantId: tenantIdColumn(),` inmediatamente despues de la columna `id` de CADA tabla listada, con
    el comentario
    `// Fase 167 (COL-01): tenancy. Valor server-side, nunca de payload. Ver src/db/schema/tenant-column.ts`.

    Dos comentarios adicionales de una linea:
    - En `tv_pairings`: que el pairing es pre-tenant por diseno (mina M7), que `user_code` /
      `device_code_hash` quedan uniques GLOBALES a proposito (lista M8 aprobada) y que la exencion
      `/* tenant-safe: pairing pre-claim */` del sentinel la agrega la fase 169/170.
    - En `blog_post_tags`: que `post_id` / `tag_id` son FKs logicas sin constraint (mina M9),
      verificadas por `src/db/scripts/verify-tenant-backfill.ts`.

    NO tocar uniques ni slugs, NO agregar indices, NO reordenar columnas. Sin `any`.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-167-columnas/el-templo-api && npx tsc --noEmit && test $(grep -c 'tenantIdColumn()' src/db/schema/tv.ts) -eq 3 && test $(grep -c 'tenantIdColumn()' src/db/schema/micro-programs.ts) -eq 2 && test $(grep -c 'tenantIdColumn()' src/db/schema/blog-tags.ts) -eq 2 && test $(grep -rho 'tenantIdColumn()' src/db/schema/ | wc -l) -eq 85 && echo C4_SCHEMA_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` limpio
    - `grep -rho 'tenantIdColumn()' src/db/schema/ | wc -l` devuelve `85` — las 85 tablas de la tanda C quedan declaradas (27 + 16 + 22 + 20)
    - `grep -c 'tenantIdColumn()'` devuelve `3` en `tv.ts`, `2` en `micro-programs.ts`, `2` en `blog-tags.ts` y `1` en cada uno de los otros 13 archivos
    - `git -C /home/franco/projects/et-167-columnas diff --name-only` lista exactamente los 16 archivos de schema de este plan
    - `grep -c 'M7' src/db/schema/tv.ts` >= 1 y `grep -c 'M9' src/db/schema/blog-tags.ts` >= 1
    - `git diff` no muestra cambios en lineas con `uniqueIndex(`, `unique(` ni `slug` de estos 16 archivos
    - `grep -nE ':\s*any\b'` sobre los 16 archivos no devuelve lineas nuevas
  </acceptance_criteria>
  <done>Las 85 tablas de la tanda C declaran `tenant_id` en el schema Drizzle usando una sola definicion, con las minas M7 y M9 anotadas.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Migracion 0195 (tanda C4) + verificacion del total de 85 tablas en local</name>
  <files>el-templo-api/src/db/migrations/0195_tenant_id_templo_rest.sql</files>
  <read_first>
    - .claude/skills/el-templo-db-migrations/SKILL.md (Hard Rules 1-6)
    - /home/franco/projects/et-167-columnas/el-templo-api/src/db/migrations/0192_tenant_id_core_ops.sql (gemelo: mismo patron y estilo de header)
    - /home/franco/projects/et-167-columnas/el-templo-api/src/db/tenant-tables.ts (las 87 gym-owned: la verificacion final de este task se hace contra esta lista)
    - /home/franco/projects/et-167-columnas/el-templo-api/src/db/run-migrations.ts lineas 24-140 (`splitSqlStatements` y `alreadyApplied`)
  </read_first>
  <action>
    Escribir a mano `el-templo-api/src/db/migrations/0195_tenant_id_templo_rest.sql` con los 4
    statements del patron para cada una de las 20 tablas, en orden alfabetico (academy_inquiries,
    app_waitlist, aura_balances, aura_config, aura_transactions, blog_post_tags, blog_posts,
    blog_tags, check_in_responses, franchise_applications, gladius_inquiries, gladius_products,
    onboarding_analytics, plan_programs, program_content_blocks, program_enrollments, programs,
    tv_class_state, tv_devices, tv_pairings) = **80 statements**, cada bloque precedido por
    `-- <tabla>` sin `;`.

    Header `--` con: fase 167 / COL-01, tanda C4 del doc 06 §1 (AURA, wellness, programs, marketing y
    TV — 20 tablas), que con esta migracion se completan las 85 tablas de la tanda C, por que la
    columna nace con `DEFAULT 1` (fix WR-01), por que el UPDATE normalmente afecta 0 filas, que
    `tv_pairings` es pre-tenant por diseno (mina M7) y recibe la columna igual, y la nota de
    idempotencia. **Ninguna linea puede contener `--` y `;` a la vez.**

    Aplicar y verificar contra la DB local:
    - `pnpm db:migrate`. PROHIBIDO `drizzle-kit push` / `drizzle-kit migrate` / `pnpm db:push`.
    - Si falla a mitad de camino, NO re-correr a ciegas: arreglar el SQL,
      `DELETE FROM _migrations WHERE name='0195_tenant_id_templo_rest.sql'` si quedo la fila, y recien
      entonces re-correr.
    - Verificacion de este grupo: 20 columnas con `IS_NULLABLE='NO'`, `COLUMN_DEFAULT='1'`,
      `DATA_TYPE='int'`; 20 constraints `fk_<t>_tenant` hacia `tenants`; `COUNT(*) WHERE tenant_id <> 1
      OR tenant_id IS NULL` = 0 en cada una.
    - **Verificacion ACUMULADA de la fase (el hito real de este plan):** contra la DB local,
      (a) `SELECT COUNT(*) FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_SCHEMA=DATABASE() AND
      COLUMN_NAME='tenant_id' AND IS_NULLABLE='NO' AND COLUMN_DEFAULT='1'` debe dar **87** (las 85 de
      la tanda C mas las 2 anclas de la fase 166); (b) `SELECT COUNT(*) FROM
      INFORMATION_SCHEMA.REFERENTIAL_CONSTRAINTS WHERE CONSTRAINT_SCHEMA=DATABASE() AND
      REFERENCED_TABLE_NAME='tenants'` debe dar **88** (87 + `fk_tenant_settings_tenant`);
      (c) `system_settings` y `labs_inquiries` NO tienen columna `tenant_id`;
      (d) el set de tablas con columna `tenant_id` coincide EXACTAMENTE con `GYM_OWNED_TABLES` de
      `src/db/tenant-tables.ts` — comparar los dos conjuntos e imprimir las diferencias (deben ser
      vacias en ambas direcciones).
    - Prueba empirica de compatibilidad: insertar sin `tenant_id` en `app_waitlist` o
      `academy_inquiries` (leads del sitio, sin FK obligatoria), verificar `tenant_id = 1`, borrar.
    - Correr `pnpm db:migrate` una SEGUNDA vez: sin error y sin cambios.

    Commitear el `.sql` junto a los 16 `.ts` (Hard Rule 3), stageando por ruta explicita, mensaje
    `feat(167): tenant_id en AURA, programs, marketing y TV (tanda C4, COL-01)`.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-167-columnas/el-templo-api && ! grep -n -- '--' src/db/migrations/0195_tenant_id_templo_rest.sql | grep -q ';' && test $(grep -v -- '--' src/db/migrations/0195_tenant_id_templo_rest.sql | grep -c 'ADD COLUMN tenant_id INT NULL DEFAULT 1') -eq 20 && test $(grep -v -- '--' src/db/migrations/0195_tenant_id_templo_rest.sql | grep -c 'MODIFY COLUMN tenant_id INT NOT NULL DEFAULT 1') -eq 20 && test $(grep -v -- '--' src/db/migrations/0195_tenant_id_templo_rest.sql | grep -c 'REFERENCES tenants(id)') -eq 20 && test $(grep -v -- '--' src/db/migrations/0195_tenant_id_templo_rest.sql | grep -ci 'AFTER ') -eq 0 && pnpm db:migrate && set -a && . ./.env.development && set +a && MYSQL_PWD="$DB_PASSWORD" mysql -h "$DB_HOST" -P "$DB_PORT" -u "$DB_USER" "$DB_NAME" -N -e "SELECT DATABASE(); SELECT COUNT(*) AS cols_87 FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_SCHEMA=DATABASE() AND COLUMN_NAME='tenant_id' AND IS_NULLABLE='NO' AND COLUMN_DEFAULT='1'; SELECT COUNT(*) AS fks_88 FROM INFORMATION_SCHEMA.REFERENTIAL_CONSTRAINTS WHERE CONSTRAINT_SCHEMA=DATABASE() AND REFERENCED_TABLE_NAME='tenants'; SELECT COUNT(*) AS exentas_0 FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_SCHEMA=DATABASE() AND COLUMN_NAME='tenant_id' AND TABLE_NAME IN ('system_settings','labs_inquiries');"</automated>
  </verify>
  <acceptance_criteria>
    - Ninguna linea del `.sql` contiene `--` y `;` a la vez
    - Filtrando comentarios: 20 `ADD COLUMN tenant_id INT NULL DEFAULT 1`, 20 `UPDATE ... WHERE tenant_id IS NULL`, 20 `MODIFY COLUMN tenant_id INT NOT NULL DEFAULT 1`, 20 `REFERENCES tenants(id)`, 0 ocurrencias de `AFTER `
    - `cols_87` devuelve exactamente `87` y `fks_88` exactamente `88`
    - `exentas_0` devuelve `0`: ni `system_settings` ni `labs_inquiries` tienen la columna (criterio de exito 1 de la fase, verificado explicitamente)
    - La comparacion de conjuntos entre las tablas con columna `tenant_id` en la DB y `GYM_OWNED_TABLES` da diferencia vacia en las dos direcciones (el SUMMARY pega la salida)
    - La suma de `COUNT(*) WHERE tenant_id <> 1 OR tenant_id IS NULL` sobre las 87 tablas es `0`
    - El insert de prueba sin `tenant_id` queda con `tenant_id = 1` y se borra despues
    - Segunda corrida de `pnpm db:migrate` sin error y `SELECT COUNT(*) FROM _migrations WHERE name LIKE '019[2-5]%'` devuelve `4`
    - `git show --stat HEAD` lista los 17 archivos del plan (16 schemas + 1 migracion) en un unico commit
  </acceptance_criteria>
  <done>Las 85 tablas de la tanda C tienen `tenant_id NOT NULL DEFAULT 1` con FK en la DB local, el conjunto coincide exactamente con la lista canonica, y las 2 tablas exentas siguen sin la columna.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                                              |
| --------------------------------- | -------------------------------------------------------------------------------------------------------- |
| flujo device-code de TV -> tenant | El TV no pertenece a nadie hasta el claim (mina M7): hay escritura legitima antes de conocer el tenant   |
| sitio publico (leads/CMS) -> DB   | `academy_inquiries`, `app_waitlist`, `franchise_applications`, `gladius_inquiries` reciben input anonimo |
| worktree `et-164-tv` -> `tv.ts`   | Otra fase abierta toca el mismo archivo: riesgo de conflicto de merge                                    |

## STRIDE Threat Register

| Threat ID | Category               | Component                                        | Disposition | Mitigation Plan                                                                                                                                                  |
| --------- | ---------------------- | ------------------------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-167-22  | Spoofing               | `tv_pairings` pre-claim (mina M7)                | accept      | La columna entra con DEFAULT 1; la exencion anotada del sentinel y el estampado en el claim son CON-04 (fase 169). Con un solo tenant el riesgo residual es nulo |
| T-167-23  | Tampering              | Formularios publicos que insertan sin scope      | mitigate    | `NOT NULL DEFAULT 1`: la fila nace con tenant valido aunque el handler publico no lo mande. La derivacion real por host/tenant es trabajo de las fases 169/175   |
| T-167-24  | Denial of Service      | Conflicto con el worktree `et-164-tv` en `tv.ts` | mitigate    | Instruccion explicita de detenerse y reportar ante una diferencia inesperada en `tv.ts`, en vez de resolver un merge a ciegas                                    |
| T-167-25  | Elevation of Privilege | Cobertura incompleta de la tanda C               | mitigate    | Verificacion acumulada contra `GYM_OWNED_TABLES`: el conjunto de tablas con columna debe coincidir exactamente con la lista canonica, en las dos direcciones     |
| T-167-26  | Tampering              | Exclusiones de diseno                            | mitigate    | Assert explicito de que `system_settings` y `labs_inquiries` NO tienen la columna (una exclusion olvidada seria una decision de diseno revertida en silencio)    |
| T-167-SC  | Tampering              | pnpm installs                                    | mitigate    | Cero paquetes nuevos en este plan                                                                                                                                |

</threat_model>

<verification>
- `npx tsc --noEmit` verde
- 87 columnas `tenant_id` NOT NULL DEFAULT 1 y 88 FKs hacia `tenants` en la DB local
- El conjunto de tablas con la columna coincide exactamente con `GYM_OWNED_TABLES`
- `system_settings` y `labs_inquiries` sin la columna
- Cero filas con `tenant_id` distinto de 1 o nulo en las 87 tablas
- Migracion idempotente (dos corridas) y `.sql` junto a los 16 `.ts` en el mismo commit
</verification>

<success_criteria>
COL-01 completo en local: las 85 tablas de la tanda C mas las 2 anclas tienen `tenant_id NOT NULL` con
FK a `tenants` y backfill al 100%, con las exclusiones de diseno verificadas.
</success_criteria>

<output>
Create `.planning/phases/167-columnas-tenant-id-en-las-85-tablas-restantes-verificaci-n/167-05-SUMMARY.md` when done.
</output>
