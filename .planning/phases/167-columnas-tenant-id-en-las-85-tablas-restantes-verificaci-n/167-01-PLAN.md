---
phase: 167-columnas-tenant-id-en-las-85-tablas-restantes-verificaci-n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/schema/tenant-column.ts
  - el-templo-api/src/db/tenant-tables.ts
  - el-templo-api/test/db/tenant-tables.test.ts
autonomous: true
requirements: [COL-01]
must_haves:
  truths:
    - "El trabajo de la fase 167 vive en un worktree limpio sobre origin/master post-166 — el checkout compartido no se toca"
    - "Los 4 numeros de migracion de la tanda C estan reservados verificando el maximo REAL en _migrations y en todas las ramas vivas (tope conocido: 0191)"
    - "Existe UNA sola definicion de la columna tenant_id reutilizable por las 85 tablas (DRY): tenantIdColumn()"
    - "Existe la clasificacion canonica de TODAS las tablas del schema: 87 gym-owned + 4 exentas, sin tablas sin clasificar"
    - "Una tabla nueva que nazca sin clasificar deja el test en rojo (fail-closed), no pasa silenciosa"
  artifacts:
    - path: "el-templo-api/src/db/schema/tenant-column.ts"
      provides: "Helper Drizzle unico para la columna tenant_id (int notNull default 1 FK a tenants)"
      exports: ["tenantIdColumn"]
    - path: "el-templo-api/src/db/tenant-tables.ts"
      provides: "Clasificacion canonica gym-owned vs exenta, insumo de las fases 168/169/170"
      exports: ["GYM_OWNED_TABLES", "TENANT_EXEMPT_TABLES", "isGymOwnedTable"]
    - path: "el-templo-api/test/db/tenant-tables.test.ts"
      provides: "Gate fail-closed: toda tabla del schema Drizzle esta clasificada exactamente una vez"
      contains: "GYM_OWNED_TABLES"
  key_links:
    - from: "el-templo-api/src/db/schema/tenant-column.ts"
      to: "el-templo-api/src/db/schema/tenants.ts"
      via: "references(() => tenants.id)"
      pattern: "tenants\\.id"
    - from: "el-templo-api/test/db/tenant-tables.test.ts"
      to: "el-templo-api/src/db/schema/index.ts"
      via: "import * as schema + getTableName sobre cada MySqlTable exportada"
      pattern: "getTableName"
---

<objective>
Base de trabajo aislada de la fase 167 + el contrato compartido que consumen los 4 planes de
migracion: el helper unico de la columna `tenant_id` y la clasificacion canonica de las 91 tablas
del schema (87 gym-owned, 4 exentas), con un test fail-closed que impide que una tabla nueva quede
sin clasificar.

Purpose: la tanda C toca 85 tablas. Sin un helper unico, son 85 definiciones copiadas a mano (y 85
oportunidades de escribir mal el default o la FK). Sin la lista canonica, no hay forma de demostrar
que no falta ninguna — y las fases 168 (indices/uniques), 169 (helpers de escritura) y 170 (sentinel
de pool) necesitan exactamente esa lista.
Output: worktree `/home/franco/projects/et-167-columnas` en rama `feat/167-tenant-columns`,
`tenant-column.ts`, `tenant-tables.ts` y su test, con los numeros 0192-0195 reservados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/166-fundaci-n-tenants-anclas-y-scope-server-side/166-PATTERNS.md
@.docs/saas-multitenancy/06-estrategia-migracion.md
@CLAUDE.md

<working_directory>
**TODO el codigo de la fase 167 se escribe en el worktree `/home/franco/projects/et-167-columnas`**
(lo crea el Task 1 de este plan). Los paths de `files_modified` son relativos a la raiz del repo:
prefijalos con `/home/franco/projects/et-167-columnas/`.

El checkout principal `/home/franco/projects/el-templo` esta en `fix/referral-preview-y-refresh-ficha`
con cambios sin commitear de OTRA sesion: **no hacer checkout, stash, ni edits de codigo ahi**. Los
artefactos de planning de GSD (SUMMARY.md de cada plan) SI se commitean en el checkout principal.

El worktree `/home/franco/projects/et-166-tenancy` (fase anterior, rama ya mergeada a master) y el
worktree `/home/franco/projects/et-164-tv` (fase TV, abierta) siguen vivos: **no tocarlos ni
borrarlos**.

Nunca `git add -A` ni `git add .` — stagear siempre por ruta explicita.
</working_directory>

<interfaces>
Contrato que este plan publica y que consumen los planes 167-02 a 167-06 (cambiar un nombre despues
obliga a tocar 5 planes):

De `el-templo-api/src/db/schema/tenant-column.ts`:

- `tenantIdColumn()` — funcion sin argumentos que devuelve un builder de columna Drizzle NUEVO en
  cada llamada, equivalente exacto a lo que las anclas declaran hoy (fase 166):
  `int("tenant_id").notNull().default(1).references(() => tenants.id)`.
  Se usa como `tenantId: tenantIdColumn(),` dentro del objeto de columnas de cada `mysqlTable`.
  El `.default(1)` es lo que deja `tenantId` OPCIONAL en el tipo de insert: sin eso, decenas de
  inserts existentes y el provisioning de `test/setup.ts` dejarian de compilar o de insertar.

De `el-templo-api/src/db/tenant-tables.ts`:

- `GYM_OWNED_TABLES` — tupla `as const` de 87 nombres de tabla FISICOS (las 85 de la tanda C mas
  las 2 anclas `users` y `branches` que ya recibieron la columna en la fase 166).
- `TENANT_EXEMPT_TABLES` — tupla `as const` de 4 nombres: `tenants`, `tenant_settings`
  (raiz/plataforma), `system_settings` (mina M2: deprecacion gradual hacia `tenant_settings`),
  `labs_inquiries` (GLOBAL de plataforma, doc 05 §4).
- `isGymOwnedTable(name: string): boolean`.

Los nombres de las 4 migraciones de la tanda C, reservados por este plan y escritos por los planes
167-02 a 167-05: `0192_tenant_id_core_ops.sql`, `0193_tenant_id_core_comms.sql`,
`0194_tenant_id_templo_spom.sql`, `0195_tenant_id_templo_rest.sql`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Base de trabajo aislada + reserva de los numeros 0192-0195</name>
  <files>ninguno del repo (solo se crea el worktree)</files>
  <read_first>
    - .claude/skills/el-templo-db-migrations/SKILL.md (Hard Rules 1-6: numeracion a mano, `_migrations` como fuente de verdad, nunca `;` en comentarios, nunca drizzle-kit push/migrate)
    - .claude/skills/el-templo-change-control/SKILL.md (staging-first, stagear por ruta, no tocar el checkout compartido)
    - .planning/phases/166-fundaci-n-tenants-anclas-y-scope-server-side/166-01-PLAN.md (Task 1: el procedimiento exacto de worktree que esta fase replica)
    - .planning/phases/166-fundaci-n-tenants-anclas-y-scope-server-side/166-06-SUMMARY.md (seccion "Next Phase Readiness": el tope aplicado en prod paso de 0189 a 0191)
  </read_first>
  <action>
    Crear la base de trabajo de TODA la fase 167, sin tocar el checkout compartido.

    1. `git -C /home/franco/projects/el-templo fetch origin` (obligatorio ANTES de nada).
    2. Crear el worktree:
       `git -C /home/franco/projects/el-templo worktree add -b feat/167-tenant-columns /home/franco/projects/et-167-columnas origin/master`.
       Si el directorio o la rama ya existieran, NO borrar nada: reportar y detenerse.
    3. Materializar el entorno del API en el worktree (los `.env*` y `node_modules` estan
       gitignoreados): copiar `/home/franco/projects/el-templo/el-templo-api/.env` y
       `.env.development` a `/home/franco/projects/et-167-columnas/el-templo-api/`, y correr
       `pnpm install --frozen-lockfile` en `/home/franco/projects/et-167-columnas/el-templo-api`.
       PROHIBIDO agregar, actualizar o instalar dependencias nuevas (regla dura del repo):
       `--frozen-lockfile` solo materializa el lockfile ya commiteado. No correr `pnpm approve-builds`
       (la suite corre igual desde el worktree: argon2 trae prebuild linux-x64).
    4. Reservar el bloque de numeracion verificando el maximo REAL en CUATRO fuentes:
       (a) el arbol de migraciones del worktree recien creado,
       (b) `SELECT MAX(name) FROM _migrations` en la DB local de desarrollo,
       (c) `git ls-tree --name-only origin/staging el-templo-api/src/db/migrations/`,
       (d) el worktree `/home/franco/projects/et-164-tv` (fase TV abierta) y cualquier otra rama
           local con migraciones: `git -C /home/franco/projects/el-templo branch -a`.
       El maximo esperado es `0191_tenant_anchors.sql` (0190 y 0191 ya estan en master, en staging y
       en las dos bases de produccion segun 166-06-SUMMARY) -> se reservan **0192, 0193, 0194 y
       0195**. Si el maximo real resulto mayor que 0191, usar los cuatro numeros libres siguientes y
       DEJARLO ESCRITO en el SUMMARY: los planes 167-02 a 167-05 nombran los archivos por numero.
    5. Verificar la precondicion de la fase en la DB local: `SELECT id, slug, status FROM tenants`
       devuelve la fila `1 / el-templo / active`, y `users.tenant_id` / `branches.tenant_id` existen.
       Si `tenants` no existiera en la DB local, correr `pnpm db:migrate` (que aplicara 0190/0191) y
       recien despues continuar: sin esa fila, las 85 FK de la tanda C no se pueden crear.
    6. No commitear nada en este task (no hay cambios de codigo todavia).

  </action>
  <verify>
    <automated>git -C /home/franco/projects/et-167-columnas rev-parse --abbrev-ref HEAD && git -C /home/franco/projects/et-167-columnas rev-list --left-right --count origin/master...HEAD && ls /home/franco/projects/et-167-columnas/el-templo-api/src/db/migrations | grep -E '^0[0-9]{3}_' | sort | tail -2 && test -f /home/franco/projects/et-167-columnas/el-templo-api/.env.development && test -d /home/franco/projects/et-167-columnas/el-templo-api/node_modules && echo BASE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `git -C /home/franco/projects/et-167-columnas rev-parse --abbrev-ref HEAD` imprime `feat/167-tenant-columns`
    - `git -C /home/franco/projects/et-167-columnas rev-list --left-right --count origin/master...HEAD` imprime `0	0`
    - El ultimo archivo de `el-templo-api/src/db/migrations` en el worktree es `0191_tenant_anchors.sql` (o mayor si otra rama mergeo — en ese caso el SUMMARY registra los cuatro numeros efectivamente reservados)
    - Las cuatro fuentes de numeracion quedan registradas en el SUMMARY con su valor observado (arbol, `_migrations` local, `origin/staging`, worktree `et-164-tv`)
    - `SELECT id, slug, status FROM tenants` en la DB local devuelve exactamente `1  el-templo  active`
    - `SELECT COUNT(*) FROM information_schema.COLUMNS WHERE TABLE_SCHEMA=DATABASE() AND COLUMN_NAME='tenant_id' AND TABLE_NAME IN ('users','branches')` devuelve `2`
    - Existen `/home/franco/projects/et-167-columnas/el-templo-api/.env.development` y `node_modules/`
    - `git -C /home/franco/projects/el-templo status --porcelain` no muestra archivos nuevos ni modificados respecto del estado previo al task
    - `git -C /home/franco/projects/et-167-columnas status --porcelain` esta vacio
    - Los worktrees `/home/franco/projects/et-166-tenancy` y `/home/franco/projects/et-164-tv` siguen existiendo (`git -C /home/franco/projects/el-templo worktree list` los lista)
  </acceptance_criteria>
  <done>Existe un worktree limpio sobre origin/master post-166 donde corre toda la fase, los cuatro numeros de migracion estan reservados con evidencia de cuatro fuentes, y la precondicion (tenant 1 + anclas) esta verificada en la DB local.</done>
</task>

<task type="auto">
  <name>Task 2: Helper unico de la columna tenant_id (DRY para las 85 tablas)</name>
  <files>el-templo-api/src/db/schema/tenant-column.ts</files>
  <read_first>
    - /home/franco/projects/et-167-columnas/el-templo-api/src/db/schema/users.ts (la declaracion de `tenantId` de la fase 166 que este helper debe reproducir byte a byte, incluido el comentario de por que lleva DEFAULT 1)
    - /home/franco/projects/et-167-columnas/el-templo-api/src/db/schema/branches.ts (segunda ancla, misma declaracion)
    - /home/franco/projects/et-167-columnas/el-templo-api/src/db/schema/tenants.ts (la tabla referenciada)
    - /home/franco/projects/et-167-columnas/el-templo-api/tsconfig.json (`declaration: true` — relevante para el fallback de este task)
  </read_first>
  <action>
    Crear `el-templo-api/src/db/schema/tenant-column.ts` con una unica exportacion, `tenantIdColumn()`,
    que devuelva un builder NUEVO en cada llamada con exactamente la forma que la fase 166 uso en las
    anclas: `int("tenant_id").notNull().default(1).references(() => tenants.id)`.

    Reglas de construccion:
    - Bloque de comentario de cabecera `//` que explique, en este orden: (a) que es la definicion
      UNICA de la columna de tenancy para las 87 tablas gym-owned (v6.0, COL-01); (b) que el valor
      SALE SIEMPRE del servidor (`scope.tenantId` / `TenantContext`), jamas de un payload ni del JWT;
      (c) por que lleva `DEFAULT 1` — compatibilidad con codigo viejo durante el rolling deploy y con
      los inserts sin `tenant_id` de `test/setup.ts`, y el `.default(1)` de Drizzle es lo que deja
      `tenantId` opcional en el tipo de insert; (d) que la columna se agrega FISICAMENTE al final de
      la tabla por el ALTER (el orden en los archivos de schema es de lectura, no fisico);
      (e) que la fase 167 NO declara indice explicito: InnoDB crea el indice de la FK
      (`fk_<tabla>_tenant`) automaticamente, y la normalizacion de indices/uniques compuestas es
      trabajo de la fase 168 (CON-02).
    - El archivo NO se agrega al barrel `src/db/schema/index.ts`: no es un modulo de tabla, es un
      helper. Cada schema lo importa directo desde `./tenant-column`.
    - `tenant-column.ts` importa unicamente `int` de `drizzle-orm/mysql-core` y `tenants` de
      `./tenants`. No importa `users` ni `branches` (evita el triangulo de imports).
    - Sin `any` (CLAUDE.md).

    **Fallback obligatorio si la emision de tipos se queja:** `tsconfig.json` tiene
    `"declaration": true`. Si `npx tsc --noEmit` o `pnpm run build` fallan con TS2742 o TS4023 (el
    tipo inferido del builder no es nombrable en el `.d.ts`), NO poner `any` ni desactivar
    `declaration`: cambiar el helper al patron oficial de columnas reutilizables de Drizzle — un
    objeto exportado `tenantIdColumns` con la propiedad `tenantId` ya construida, que cada tabla
    esparce con `...tenantIdColumns` dentro de su objeto de columnas. Si se toma ese camino, dejarlo
    escrito en el SUMMARY, porque cambia la linea que insertan los planes 167-02 a 167-05.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-167-columnas/el-templo-api && npx tsc --noEmit && pnpm run build && grep -c 'int("tenant_id")' src/db/schema/tenant-column.ts && grep -c 'default(1)' src/db/schema/tenant-column.ts && ! grep -q 'tenant-column' src/db/schema/index.ts && echo HELPER_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` limpio y `pnpm run build` limpio en `/home/franco/projects/et-167-columnas/el-templo-api` (el build es el que ejerce la emision de `.d.ts`, donde aparecerian TS2742/TS4023)
    - `grep -c 'int("tenant_id")' src/db/schema/tenant-column.ts` devuelve `1`
    - `grep -c '\.notNull()' src/db/schema/tenant-column.ts` y `grep -c '\.default(1)' src/db/schema/tenant-column.ts` devuelven `1` cada uno, y `grep -c 'references(() => tenants.id)' ...` devuelve `1`
    - `grep -c 'tenant-column' src/db/schema/index.ts` devuelve `0` (el helper NO esta en el barrel)
    - `grep -nE 'from "\./(users|branches)"' src/db/schema/tenant-column.ts` no devuelve nada
    - `grep -nE ':\s*any\b' src/db/schema/tenant-column.ts` no devuelve nada
    - Si se activo el fallback del objeto reutilizable, el simbolo exportado es `tenantIdColumns` y el SUMMARY lo declara explicitamente
  </acceptance_criteria>
  <done>Existe una sola definicion de `tenant_id` en el repo, compila con emision de tipos, y es la que van a usar las 85 tablas de la tanda C.</done>
</task>

<task type="auto">
  <name>Task 3: Clasificacion canonica de tablas + test fail-closed de cobertura</name>
  <files>el-templo-api/src/db/tenant-tables.ts, el-templo-api/test/db/tenant-tables.test.ts</files>
  <read_first>
    - .docs/saas-multitenancy/05-inventario-tablas-2026-07-26.md (§1 las 46 CORE, §2 las 42 TEMPLO-MODULO, §3 GLOBAL, §4 `labs_inquiries` GLOBAL, §1.7 `system_settings` sin columna)
    - /home/franco/projects/et-167-columnas/el-templo-api/src/db/schema/index.ts (el barrel completo: la fuente de la que el test enumera las tablas)
    - /home/franco/projects/et-167-columnas/el-templo-api/test/migrations/0190-0191-tenants.test.ts (estilo de test de introspeccion de la fase 166: casts `as unknown as`, ciclo `createTestApp`/`cleanAllTestData`)
    - /home/franco/projects/et-167-columnas/el-templo-api/vitest.config.ts (include `test/**/*.test.ts` — el subdirectorio `test/db/` es nuevo y queda cubierto)
  </read_first>
  <action>
    1. Crear `el-templo-api/src/db/tenant-tables.ts` (fuera de `schema/`: es metadata del modelo, no una
       tabla) exportando dos tuplas `as const` y un predicado.

    `GYM_OWNED_TABLES` — exactamente estos 87 nombres fisicos, en orden alfabetico:
    academy_inquiries, activities, app_waitlist, attendance, audit_log, aura_balances, aura_config,
    aura_transactions, balances, blog_post_tags, blog_posts, blog_tags, bookings, branches,
    campaign_events, campaign_sends, campaign_unsubscribes, campaigns, cash_registers,
    check_in_responses, class_coach_assignments, coach_ratings, completed_sessions,
    contraction_rules, cost_centers, day_modes, debt_management, device_tokens, evaluation_requests,
    exercise_adjustments, exercise_dimension_proposals, exercise_milestone_proposals,
    exercise_progressions, exercises, financial_transactions, format_compatibility, formats,
    franchise_applications, gladius_inquiries, gladius_products, holidays, improvement_proposals,
    intensity_rules, member_logins, member_notes, member_profiles, notification_preferences,
    notification_templates, onboarding_analytics, pending_notifications, plan_programs,
    program_content_blocks, program_enrollments, programs, promo_plans, referral_credits,
    referral_cta_clicks, referrals, refresh_tokens, routes, saved_blocks, schedule_exceptions,
    schedules, session_blocks, session_edit_logs, session_prescriptions, session_traces, sessions,
    spom_config, spom_rules, subscription_plans, subscription_schedule_changes,
    subscription_schedules, subscriptions, transaction_links, tv_class_state, tv_devices,
    tv_pairings, user_branches, user_sepa_details, user_status_history, users, weekly_rotator,
    wellhub_bookings, wellhub_classes, wellhub_events, wellhub_slots.

    `TENANT_EXEMPT_TABLES` — exactamente 4 nombres: tenants, tenant_settings, system_settings,
    labs_inquiries.

    `isGymOwnedTable(name: string): boolean` — pertenencia a `GYM_OWNED_TABLES`.

    Comentarios obligatorios en el archivo: (a) que la lista sale del inventario cerrado
    `.docs/saas-multitenancy/05-inventario-tablas-2026-07-26.md` (46 CORE + 42 TEMPLO-MODULO, menos
    `system_settings`, mas las 2 anclas ya migradas en la fase 166); (b) el motivo POR TABLA de cada
    una de las 4 exentas — `tenants`/`tenant_settings` son la raiz del modelo, `system_settings` es
    la mina M2 (se deprecia gradualmente hacia `tenant_settings`, no recibe columna en todo v6.0),
    `labs_inquiries` es GLOBAL de plataforma (leads del propio SaaS, doc 05 §4); (c) que esta lista
    es el insumo de las fases 168 (indices y uniques compuestas), 169 (helpers de escritura) y 170
    (sentinel de pool y lint), asi que agregar una tabla nueva al schema OBLIGA a clasificarla aca.
    Sin `any`.

    2. Crear `el-templo-api/test/db/tenant-tables.test.ts`, que enumera las tablas reales del schema
       Drizzle y las cruza con la clasificacion. Enumeracion: `import * as schema from "../../src/db/schema"`,
       recorrer los valores exportados quedandose con los que sean tablas (`is(value, MySqlTable)` de
       `drizzle-orm` + `drizzle-orm/mysql-core`) y sacar el nombre fisico con `getTableName(value)`.
       Deduplicar (el barrel reexporta cada tabla una sola vez, pero el set evita sorpresas).

    Cuatro aserciones:
    - Toda tabla del schema esta en `GYM_OWNED_TABLES` o en `TENANT_EXEMPT_TABLES` — el mensaje de
      error debe imprimir los nombres sin clasificar (este es el gate fail-closed: una tabla nueva
      sin clasificar deja el test en rojo).
    - Ningun nombre esta en las dos listas a la vez.
    - Todo nombre de las dos listas existe en el schema (atrapa typos y renames).
    - Los conteos son exactamente 87 y 4, y la union cubre las 91 tablas del schema.

    Este test NO toca la DB (es introspeccion de objetos Drizzle), pero corre bajo el
    `setupFiles: test/setup.ts` del repo igual que cualquier otro: no agregar configuracion nueva de
    vitest ni `createTestApp()` si no hace falta.

    3. Correr SOLO este archivo de test: `npx vitest run test/db/tenant-tables.test.ts`. No correr la
       suite completa en local (corre en CI).

    4. Commitear los tres archivos de este plan stageando por ruta explicita
       (`el-templo-api/src/db/schema/tenant-column.ts`, `el-templo-api/src/db/tenant-tables.ts`,
       `el-templo-api/test/db/tenant-tables.test.ts`), mensaje
       `feat(167): helper tenantIdColumn + clasificacion canonica de tablas gym-owned (COL-01)`.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-167-columnas/el-templo-api && npx tsc --noEmit && npx vitest run test/db/tenant-tables.test.ts && node -e "const t=require('./src/db/tenant-tables.ts')" 2>/dev/null; node --experimental-strip-types -e "1" >/dev/null 2>&1; grep -c "'" src/db/tenant-tables.ts >/dev/null && echo TABLES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/db/tenant-tables.test.ts` pasa con las 4 aserciones descritas
    - El test enumera 91 tablas del schema Drizzle (87 + 4); si el numero difiere, el test falla nombrando las tablas sin clasificar
    - `GYM_OWNED_TABLES` tiene exactamente 87 entradas y `TENANT_EXEMPT_TABLES` exactamente 4 (verificable con `npx tsx -e` imprimiendo `.length` de cada una)
    - `grep -c 'system_settings' src/db/tenant-tables.ts` >= 1 y `system_settings` NO aparece dentro de `GYM_OWNED_TABLES` (verificable imprimiendo `isGymOwnedTable('system_settings')` -> `false`, idem `labs_inquiries`)
    - `isGymOwnedTable('users')` y `isGymOwnedTable('branches')` devuelven `true` (las anclas ya migradas cuentan como gym-owned)
    - Prueba negativa explicita del gate: agregar temporalmente un nombre inventado a `GYM_OWNED_TABLES` deja el test en ROJO (tercera asercion), y se revierte antes de commitear — el SUMMARY registra que se ejercito
    - `npx tsc --noEmit` limpio
    - `grep -nE ':\s*any\b' src/db/tenant-tables.ts test/db/tenant-tables.test.ts` no devuelve nada
    - `git -C /home/franco/projects/et-167-columnas show --stat HEAD` lista exactamente los 3 archivos del plan en un unico commit
  </acceptance_criteria>
  <done>La fase tiene su fuente de verdad de "que tabla lleva tenant_id", verificada contra el schema real y protegida por un test que se pone rojo si alguien agrega una tabla sin clasificarla.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                     | Description                                                                                      |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| worktree/rama -> produccion                  | Un numero de migracion mal reservado produce un archivo que prod nunca aplica o aplica dos veces |
| clasificacion de tablas -> fases 168/169/170 | Una tabla mal clasificada como exenta queda para siempre fuera del aislamiento multi-tenant      |
| helper compartido -> 85 tablas               | Un error en la unica definicion de `tenant_id` se propaga a las 85 tablas de una sola vez        |

## STRIDE Threat Register

| Threat ID | Category               | Component                            | Disposition | Mitigation Plan                                                                                                                                                     |
| --------- | ---------------------- | ------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-167-01  | Tampering              | Numeracion de migraciones            | mitigate    | Verificacion del maximo real en 4 fuentes (arbol, `_migrations`, `origin/staging`, worktree `et-164-tv`) antes de escribir un numero, con los valores en el SUMMARY |
| T-167-02  | Elevation of Privilege | Clasificacion gym-owned vs exenta    | mitigate    | Lista canonica derivada del inventario cerrado + test fail-closed que rompe si una tabla queda sin clasificar, y prueba negativa ejercitada en el Task 3            |
| T-167-03  | Tampering              | Definicion de la columna `tenant_id` | mitigate    | Una sola definicion (`tenantIdColumn`), identica a la que las anclas ya tienen en produccion, con gate de `tsc` + `build`                                           |
| T-167-04  | Denial of Service      | Inserts existentes sin `tenant_id`   | mitigate    | `.default(1)` en el helper: `tenantId` queda opcional en el tipo de insert y la DB completa el valor (mismo camino validado en la fase 166)                         |
| T-167-05  | Repudiation            | Trabajo sobre el checkout compartido | mitigate    | Todo el codigo vive en worktree propio; criterio de aceptacion explicito de que `git status` del checkout principal no cambio                                       |
| T-167-SC  | Tampering              | pnpm installs                        | mitigate    | Cero paquetes nuevos: solo `pnpm install --frozen-lockfile`. Cualquier `pnpm add` requiere checkpoint humano bloqueante                                             |

</threat_model>

<verification>
- El worktree existe, esta en `feat/167-tenant-columns` y arranca exactamente en `origin/master`
- El checkout principal y los worktrees `et-166-tenancy` / `et-164-tv` quedaron intactos
- Los 4 numeros de migracion estan reservados con evidencia de 4 fuentes
- `tenants` tiene la fila 1 activa en la DB local y las anclas ya tienen la columna
- `npx tsc --noEmit` y `pnpm run build` verdes
- `npx vitest run test/db/tenant-tables.test.ts` verde, con la prueba negativa del gate ejercitada
</verification>

<success_criteria>
La fase 167 tiene base de trabajo aislada, numeracion reservada y el contrato compartido (helper unico
de columna + clasificacion canonica de 87/4 tablas con gate fail-closed) que consumen los 5 planes
siguientes.
</success_criteria>

<output>
Create `.planning/phases/167-columnas-tenant-id-en-las-85-tablas-restantes-verificaci-n/167-01-SUMMARY.md` when done.
El SUMMARY DEBE registrar: (a) path del worktree y nombre de rama, (b) los cuatro numeros de migracion
efectivamente reservados y el valor observado en cada una de las 4 fuentes, (c) si se activo el
fallback del helper (`tenantIdColumns` en vez de `tenantIdColumn()`), porque cambia la linea que
insertan los planes 167-02 a 167-05, (d) el hash del commit.
</output>
