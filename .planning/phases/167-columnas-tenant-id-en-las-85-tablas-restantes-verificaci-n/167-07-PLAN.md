---
phase: 167-columnas-tenant-id-en-las-85-tablas-restantes-verificaci-n
plan: 07
type: execute
wave: 7
depends_on: [167-06]
files_modified: []
autonomous: false
requirements: [COL-01, COL-02]
must_haves:
  truths:
    - "Las 4 migraciones de la tanda C corren verdes primero en eltemplo_staging y despues en eltemplo, sin downtime y sin rollback"
    - "Las 87 tablas gym-owned tienen tenant_id NOT NULL DEFAULT 1 con FK a tenants en las DOS bases, verificado por information_schema"
    - "El script de verificacion corre en staging y en prod y reporta 0 discrepancias en ambas (COL-02)"
    - "system_settings y labs_inquiries siguen SIN la columna en las dos bases"
    - "Nada se pushea ni se accede por SSH sin aprobacion explicita y separada de Franco"
  artifacts:
    - path: ".planning/phases/167-columnas-tenant-id-en-las-85-tablas-restantes-verificaci-n/167-07-SUMMARY.md"
      provides: "Evidencia del rollout: conteos por base, salida del verificador en cada una, resultado de cada pipeline a nivel de step"
  key_links:
    - from: "rama feat/167-tenant-columns"
      to: "origin/staging y origin/master"
      via: "rollout staging-first con aprobacion humana entre etapas"
      pattern: "staging"
---

<objective>
Rollout staging-first de la tanda C: las 4 migraciones (0192-0195) se aplican primero en
`eltemplo_staging` y despues en `eltemplo`, con verificacion por `information_schema` y con el script
de COL-02 corriendo contra cada base, y con aprobacion humana explicita antes de cada push y antes de
cada acceso SSH.

Purpose: es el unico plan de la fase que toca produccion. La fase 166 dejo la secuencia probada
(166-06) y una trampa documentada: `origin/staging` puede estar adelantado respecto de `origin/master`
y mergear staging dentro de la rama de fase arrastraria trabajo ajeno a produccion.
Output: las dos bases con las 87 columnas, el verificador en 0 discrepancias en ambas, y el SUMMARY
con la evidencia.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/166-fundaci-n-tenants-anclas-y-scope-server-side/166-06-SUMMARY.md
@CLAUDE.md

<working_directory>
El codigo de la fase esta en `/home/franco/projects/et-167-columnas`, rama `feat/167-tenant-columns`,
con 6 commits (uno por plan). Este plan NO escribe codigo: verifica, pide aprobacion y despliega.
El SUMMARY se commitea en el checkout principal `/home/franco/projects/el-templo`.

**Reglas duras del proyecto que aplican a este plan, sin excepcion:**

- Nunca pushear sin aprobacion explicita de Franco, ni a `staging` ni a `master`.
- Nunca hacer SSH al servidor sin aprobacion explicita, y pedirla POR SEPARADO para staging y prod.
- Staging primero, siempre. Prod solo despues de staging verde y verificado.
- Nunca `git add -A`. Nunca instalar ni actualizar dependencias.
  </working_directory>

<interfaces>
Consumido de los planes anteriores:
- Migraciones `0192_tenant_id_core_ops.sql`, `0193_tenant_id_core_comms.sql`,
  `0194_tenant_id_templo_spom.sql`, `0195_tenant_id_templo_rest.sql` (o los cuatro numeros que el
  plan 167-01 haya reservado realmente — su SUMMARY los registra).
- `pnpm db:verify-tenant` en local y `node dist/db/scripts/verify-tenant-backfill.js` en el servidor
  (el build del pipeline emite el script en `dist/db/scripts/`), con exit code 0 = 0 discrepancias.
- `src/db/tenant-tables.ts` con las 87 gym-owned y las 4 exentas.

Numeros esperados en CADA base despues del rollout: **87** columnas `tenant_id` con
`IS_NULLABLE='NO'` y `COLUMN_DEFAULT='1'`; **88** FKs con `REFERENCED_TABLE_NAME='tenants'`;
**0** filas con `tenant_id` distinto de 1 en cualquier gym-owned; **0** columnas `tenant_id` en
`system_settings` y `labs_inquiries`; **4** filas nuevas en `_migrations`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate local consolidado y preparacion de la secuencia de rollout</name>
  <files>ninguno (verificacion y preparacion)</files>
  <read_first>
    - .claude/skills/el-templo-change-control/SKILL.md (staging-first, el accidente de la fase 78, `paths-filter` con `event.before`)
    - .claude/skills/el-templo-db-migrations/SKILL.md (la trampa del host compartido entre staging y prod)
    - .planning/phases/166-fundaci-n-tenants-anclas-y-scope-server-side/166-06-SUMMARY.md (secuencia completa que este plan replica, incluida la rama descartable y los deploy paths reales)
    - Los SUMMARY de 167-01 a 167-06 (numeros de migracion realmente reservados, desvios, y si se activo el fallback del helper)
  </read_first>
  <action>
    Gate local, todo desde `/home/franco/projects/et-167-columnas/el-templo-api` salvo donde se diga:

    1. `npx tsc --noEmit` y `pnpm run build`, ambos limpios. (El API **no** tiene script `lint` y CI no
       lintea el API: el build es lo que CI exige — hallazgo de la fase 166.)
    2. Higiene de las 4 migraciones, con el archivo TERMINADO: para cada una,
       `grep -n -- '--' <archivo> | grep ';'` no devuelve nada; filtrando comentarios, la cantidad de
       `ADD COLUMN tenant_id INT NULL DEFAULT 1`, de `MODIFY COLUMN tenant_id INT NOT NULL DEFAULT 1` y
       de `REFERENCES tenants(id)` coincide con el numero de tablas del grupo (27/16/22/20) y
       `AFTER ` aparece 0 veces.
    3. `pnpm db:verify-tenant` contra la DB local: exit code 0, 87 tablas, 0 discrepancias. Guardar la
       salida completa para el SUMMARY.
    4. `npx vitest run test/db/tenant-tables.test.ts test/migrations/0192-0195-tenant-columns.test.ts`
       en verde (los dos archivos nuevos de la fase; la suite completa la corre CI).
    5. **Medicion para estimar el impacto en prod:** cronometrar cuanto tarda el paso de migraciones
       en local (por ejemplo, corriendo las 4 sobre una base limpia o midiendo la corrida ya hecha) y
       listar las 10 tablas gym-owned con mas filas en local
       (`SELECT TABLE_NAME, TABLE_ROWS FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_SCHEMA=DATABASE()
       ORDER BY TABLE_ROWS DESC LIMIT 10`). Dejarlo en el SUMMARY: el `ADD COLUMN` es INSTANT, el
       `MODIFY ... NOT NULL` es INPLACE con rebuild y el `ADD FOREIGN KEY` con `foreign_key_checks`
       activo es COPY — sobre 85 tablas de decenas de miles de filas se espera del orden de minutos,
       no de decenas de minutos, y el dato sirve para saber si el paso de migraciones del pipeline
       tarda lo previsto o algo anda mal.
    6. `git -C /home/franco/projects/el-templo fetch origin` y verificar que **nadie tomo los numeros
       reservados**: `git ls-tree --name-only origin/master el-templo-api/src/db/migrations/` y lo
       mismo con `origin/staging` no deben contener `0192_`, `0193_`, `0194_` ni `0195_`. Si aparecen,
       DETENERSE y reportar (habria que renumerar antes de pushear).
    7. Inventario del diff contra `origin/master`: `git -C /home/franco/projects/et-167-columnas diff
       --name-only origin/master...HEAD` debe listar **82 archivos** (3 del plan 01 + 28 del 02 + 8 del
       03 + 23 del 04 + 17 del 05 + 3 del 06) y **6 commits**, sin `.env*`, sin `node_modules`, sin
       lockfile, y con `package.json` tocado unicamente en el bloque `scripts`.
    8. Checkout compartido intacto: `git -C /home/franco/projects/el-templo status --porcelain` no
       muestra archivos de codigo de esta fase.
    9. Estado de `origin/staging` respecto de `origin/master`:
       `git rev-list --left-right --count origin/master...origin/staging`. Si staging esta adelantado,
       la secuencia del paso B usa una **rama descartable basada en `origin/staging`** con merge
       `--no-ff` de la fase — NUNCA mergear `origin/staging` dentro de `feat/167-tenant-columns`,
       porque el push posterior a master arrastraria trabajo ajeno a produccion (fase 78, y
       exactamente el riesgo que la 166 esquivo).
    10. Preparar (sin ejecutar) los comandos exactos de cada etapa del Task 2 y dejarlos escritos en la
        salida del task, para que el checkpoint humano apruebe sobre comandos concretos.

    No pushear, no hacer SSH y no commitear nada en este task.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-167-columnas/el-templo-api && npx tsc --noEmit && pnpm run build && pnpm db:verify-tenant && npx vitest run test/db/tenant-tables.test.ts test/migrations/0192-0195-tenant-columns.test.ts && test $(git -C /home/franco/projects/et-167-columnas diff --name-only origin/master...HEAD | wc -l) -eq 82 && test $(git -C /home/franco/projects/et-167-columnas diff --name-only origin/master...HEAD | grep -cE '(^|/)\.env|node_modules|pnpm-lock') -eq 0 && echo GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - El comando `<automated>` imprime `GATE_OK`
    - `pnpm db:verify-tenant` exit code 0, 87 tablas, 0 discrepancias, y la salida completa queda guardada para el SUMMARY
    - Los dos archivos de test nuevos pasan en verde
    - Las 4 migraciones pasan la higiene: cero lineas con `--` y `;` a la vez, conteos 27/16/22/20 por tipo de statement, cero `AFTER `
    - `origin/master` y `origin/staging` no contienen `0192_`, `0193_`, `0194_` ni `0195_` (verificado tras `git fetch`)
    - El diff contra `origin/master` son 82 archivos en 6 commits, sin `.env*`, sin `node_modules`, sin lockfile, y `package.json` tocado solo en `scripts`
    - `git -C /home/franco/projects/el-templo status --porcelain` sin archivos de codigo de esta fase
    - Queda registrada la divergencia `origin/master` vs `origin/staging` y, si staging esta adelantado, el nombre de la rama descartable a usar
    - Queda registrada la medicion de tiempo de migracion local y el top 10 de tablas por filas
  </acceptance_criteria>
  <done>Todo lo verificable sin tocar servidores esta verde, los numeros de migracion siguen libres, el alcance del diff esta acotado y la secuencia de rollout esta escrita comando por comando.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [CHECKPOINT BLOQUEANTE] Rollout staging-first y verificacion en las dos bases</name>
  <files>ninguno (rollout: no se escribe codigo, se despliega lo ya commiteado)</files>
  <what-built>
    Los 6 commits de la fase 167 en `feat/167-tenant-columns`: 85 tablas con `tenant_id NOT NULL
    DEFAULT 1` y FK a `tenants` en 4 migraciones (0192-0195), el helper unico de columna, la
    clasificacion canonica de las 91 tablas con test fail-closed, y el verificador de backfill de
    COL-02 con su test de CI. Gate local completo en verde (Task 1), incluido el verificador en 0
    discrepancias contra la DB local.
  </what-built>
  <how-to-verify>
    La secuencia se ejecuta por etapas y **cada etapa arranca solo con una senal humana explicita**.
    Ninguna etapa puede adelantarse.

    **A. Push de la rama (requiere: "aprobado staging")**
    1. `git -C /home/franco/projects/et-167-columnas push -u origin feat/167-tenant-columns`
       (respaldo de la rama en origin).
    2. Llevarla a staging: si `origin/staging` esta al dia con master, push directo
       `feat/167-tenant-columns:staging`. Si esta adelantado, crear la **rama descartable**
       `tren/167-staging` basada en `origin/staging`, mergear `--no-ff` la rama de fase, verificar el
       arbol resultante archivo por archivo (el merge de la estrategia `ort` puede imprimir CONFLICT
       durante la base virtual y cerrar en exit 0 igual: no leer el exit code, mirar el arbol), correr
       `npx tsc --noEmit` sobre el arbol mergeado, y recien entonces pushear
       `tren/167-staging:staging`.

    **B. CI y deploy de staging (sin senal humana, es espera)**
    3. Seguir el run de CI y el de Deploy Staging con UN solo poller cada 90 segundos (la API publica
       de GitHub tiene 60 req/h por IP sin autenticar y en la 166 se agoto). Confirmar a nivel de
       **step**, no de badge ni de conclusion: `Run database migrations` success, `Restart staging
       API` success, `Post-deploy smoke test` success, `Rollback staging on failure` SKIPPED.
    4. Anotar cuanto tardo el step de migraciones y compararlo con la medicion local del Task 1.

    **C. Verificacion en `eltemplo_staging` (requiere autorizacion de SSH para staging)**
    5. Deploy path descubierto con `pm2 describe`, no asumido (en la 166 staging resulto
       `/opt/el-templo-staging/api`, no `/var/www/staging`). Password por `MYSQL_PWD`, nunca en el
       argv (host compartido con otros proyectos).
    6. Primero, como statement separado: `SELECT DATABASE()`. Si no dice `eltemplo_staging`, cortar.
    7. Despues, en la misma sesion: 87 columnas `tenant_id` con `IS_NULLABLE='NO'` y
       `COLUMN_DEFAULT='1'`; 88 FKs hacia `tenants`; 0 columnas `tenant_id` en `system_settings` y
       `labs_inquiries`; las 4 migraciones en `_migrations`, una vez cada una.
    8. Correr el verificador contra staging desde el deploy path:
       `NODE_ENV=production node dist/db/scripts/verify-tenant-backfill.js` (en el server, el
       `.env.production` del path de staging apunta a `eltemplo_staging` — confirmarlo con la primera
       linea de salida del propio script). Debe imprimir la base correcta y **0 discrepancias**, exit
       code 0. Guardar la salida completa.

    **D. Push a produccion (requiere: "aprobado prod")**
    9. `git fetch origin` y re-confirmar que `origin/master` no se movio y que sigue sin contener
       `019[2-5]_`.
    10. Push **fast-forward de la rama de fase** a master (`feat/167-tenant-columns:master`), NO del
        arbol de staging: los commits propios de staging no viajan a produccion.
    11. Seguir CI y Deploy con el mismo criterio de steps del paso 3, mas `Backup current deployment`
        success y `Rollback on failure` SKIPPED.

    **E. Verificacion en `eltemplo` (requiere autorizacion de SSH para prod, SEPARADA de la de staging)**
    12. Mismo procedimiento del bloque C contra la base de produccion: `SELECT DATABASE()` primero
        (debe decir `eltemplo`), las 4 consultas, y el verificador
        `NODE_ENV=production node dist/db/scripts/verify-tenant-backfill.js` desde `/var/www/api`
        (path a confirmar con `pm2 describe`), con **0 discrepancias**.
    13. `https://api-staging.eltemplo.org/health` y `https://api.eltemplo.org/health` responden 200
        desde fuera del servidor, y `pm2` muestra los dos procesos `online` sin unstable restarts.

    **F. Smoke funcional (Franco, por UI — no lo puede hacer Claude)**
    14. Admin: listado de socios, carga de un cobro, pantalla de reservas, reporte de deuda.
    15. Member app: un socio ve sus planes y sus turnos.
    16. Criterio: ninguna pantalla muestra un error nuevo y los numeros son los mismos que ayer. Si
        algo falla, se reporta ANTES de dar la fase por cerrada.

    **G. Limpieza**
    17. Si se uso rama descartable: borrarla local y confirmar que nunca se publico en origin.
    18. Conservar el worktree `/home/franco/projects/et-167-columnas` y la rama hasta el cierre de fase
        y el UAT.

  </how-to-verify>
  <resume-signal>
    Cuatro senales humanas separadas, en este orden: "aprobado staging" -> autorizacion de SSH para
    staging -> "aprobado prod" -> autorizacion de SSH para prod. Al final, "aprobado" o la descripcion
    de lo que fallo en el smoke funcional.
  </resume-signal>
  <read_first>
    - .planning/phases/166-fundaci-n-tenants-anclas-y-scope-server-side/166-06-SUMMARY.md (timeline, deploy paths reales, trampas de CI y de la API de GitHub)
    - .claude/skills/el-templo-change-control/SKILL.md
    - Los SUMMARY de 167-01 a 167-06
  </read_first>
  <action>
    Ejecutar la secuencia A-G de `<how-to-verify>` respetando las cuatro senales humanas. Sin senal, no
    hay push ni SSH. Registrar en el SUMMARY, por etapa: comando ejecutado, resultado a nivel de step
    del pipeline, salida de las consultas por base y salida completa del verificador en cada base.
    Si cualquier etapa falla, DETENERSE y reportar: no improvisar un fix sobre produccion.
  </action>
  <verify>
    <automated>MISSING — la verificacion de esta tarea es por SSH y por UI con aprobacion humana; el gate automatizable es el del Task 1 (`GATE_OK`) mas el exit code 0 de `verify-tenant-backfill.js` en cada base, que se registra en el SUMMARY</automated>
    <human-check>Franco confirma smoke funcional en admin y member app, en staging y en produccion</human-check>
  </verify>
  <acceptance_criteria>
    - Las 4 migraciones figuran una sola vez cada una en `_migrations` de `eltemplo_staging` y de `eltemplo`
    - En las DOS bases: 87 columnas `tenant_id` con `IS_NULLABLE='NO'` y `COLUMN_DEFAULT='1'`, y 88 FKs con `REFERENCED_TABLE_NAME='tenants'`
    - En las DOS bases: `system_settings` y `labs_inquiries` sin columna `tenant_id`
    - En las DOS bases: `verify-tenant-backfill.js` imprime el nombre de base correcto y sale con exit code 0 y 0 discrepancias (salida completa pegada en el SUMMARY)
    - Los pipelines de staging y de prod terminan con `Run database migrations` success, `Restart` success, `Post-deploy smoke test` success y `Rollback` SKIPPED, verificado a nivel de step
    - `/health` responde 200 en staging y en prod desde fuera del servidor, y pm2 muestra ambos procesos online sin unstable restarts
    - El SUMMARY registra el tiempo del step de migraciones en cada entorno y lo compara con la medicion local
    - Ningun push ni SSH ocurrio sin su senal humana correspondiente (las cuatro quedan registradas con su hora en el SUMMARY)
    - Si se uso rama descartable, quedo borrada y nunca se publico en origin
  </acceptance_criteria>
  <done>Las 87 tablas gym-owned son tenant-aware en staging y en produccion, el verificador de COL-02 da 0 discrepancias en las dos bases, y el staff no percibio ningun cambio.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                                                                      |
| ----------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| rama de fase -> `origin/staging`    | Si staging esta adelantado, un merge mal hecho arrastra trabajo ajeno a produccion en el push siguiente                          |
| pipeline -> base de produccion      | 4 migraciones con 340 statements corren sobre la base real durante un rolling deploy                                             |
| sesion SSH -> host MySQL compartido | Staging y produccion viven en el mismo host, con otros proyectos: la base equivocada y la password en el argv son riesgos reales |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                | Disposition | Mitigation Plan                                                                                                                                                                    |
| --------- | ---------------------- | -------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-167-32  | Tampering              | Push a master arrastrando commits de staging             | mitigate    | Rama descartable basada en `origin/staging` para el paso B y fast-forward de la rama de fase para master, con verificacion del arbol archivo por archivo                           |
| T-167-33  | Information Disclosure | Verificacion contra la base equivocada (host compartido) | mitigate    | `SELECT DATABASE()` como statement separado y previo en cada base, con corte previsto, y el propio verificador imprimiendo la base antes que nada                                  |
| T-167-34  | Elevation of Privilege | Push o SSH sin aprobacion                                | mitigate    | Cuatro senales humanas separadas y registradas; el checkpoint es bloqueante y no auto-aprobable                                                                                    |
| T-167-35  | Denial of Service      | Duracion de los ALTER sobre 85 tablas en prod            | mitigate    | Medicion local en el Task 1, medicion del step en staging antes de prod, y `ADD COLUMN` sin `AFTER` (INSTANT). Si staging tarda mucho mas de lo previsto, se detiene antes de prod |
| T-167-36  | Repudiation            | Evidencia del rollout                                    | mitigate    | El SUMMARY registra por entorno: commits desplegados, resultado por step, conteos por base y salida completa del verificador                                                       |
| T-167-37  | Denial of Service      | Password de MySQL visible en `ps` del host compartido    | mitigate    | `MYSQL_PWD` en vez de `-p` en la linea de comandos (leccion de la fase 166)                                                                                                        |
| T-167-SC  | Tampering              | pnpm installs en el servidor                             | mitigate    | El deploy corre `Install API dependencies` desde el lockfile commiteado; la fase no agrega ni actualiza un solo paquete                                                            |

</threat_model>

<verification>
- Gate local verde (typecheck, build, higiene de las 4 migraciones, verificador en 0, tests nuevos)
- Numeros de migracion todavia libres en `origin/master` y `origin/staging` al momento del push
- Staging: CI y deploy verdes a nivel de step, 4 consultas con los valores esperados, verificador en 0
- Prod: mismas consultas, mismos valores, verificador en 0
- `/health` 200 en los dos entornos y pm2 online sin unstable restarts
- Ningun push ni SSH sin aprobacion explicita
- Smoke funcional por UI confirmado por Franco (o reportado como pendiente si no ocurrio)
</verification>

<success_criteria>
COL-01 y COL-02 verificados en staging y en produccion: 87 columnas con FK, 0 discrepancias en el
verificador, las 2 exclusiones respetadas y cero cambio visible para el staff.
</success_criteria>

<output>
Create `.planning/phases/167-columnas-tenant-id-en-las-85-tablas-restantes-verificaci-n/167-07-SUMMARY.md` when done.
El SUMMARY DEBE incluir: commits desplegados por entorno con su SHA, resultado de cada pipeline a
nivel de step, las 4 consultas con su salida en cada base, la salida completa del verificador en cada
base, el tiempo del step de migraciones en cada entorno, y la hora de cada una de las cuatro senales
humanas.
</output>
