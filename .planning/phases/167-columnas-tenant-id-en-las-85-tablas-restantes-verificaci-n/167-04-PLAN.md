---
phase: 167-columnas-tenant-id-en-las-85-tablas-restantes-verificaci-n
plan: 04
type: execute
wave: 4
depends_on: [167-03]
files_modified:
  - el-templo-api/src/db/schema/routes.ts
  - el-templo-api/src/db/schema/spom-rules.ts
  - el-templo-api/src/db/schema/intensity-rules.ts
  - el-templo-api/src/db/schema/contraction-rules.ts
  - el-templo-api/src/db/schema/weekly-rotator.ts
  - el-templo-api/src/db/schema/formats.ts
  - el-templo-api/src/db/schema/format-compatibility.ts
  - el-templo-api/src/db/schema/spom-config.ts
  - el-templo-api/src/db/schema/day-modes.ts
  - el-templo-api/src/db/schema/exercises.ts
  - el-templo-api/src/db/schema/exercise-dimension-proposals.ts
  - el-templo-api/src/db/schema/exercise-milestone-proposals.ts
  - el-templo-api/src/db/schema/exercise-progressions.ts
  - el-templo-api/src/db/schema/exercise-adjustments.ts
  - el-templo-api/src/db/schema/sessions.ts
  - el-templo-api/src/db/schema/session-blocks.ts
  - el-templo-api/src/db/schema/session-prescriptions.ts
  - el-templo-api/src/db/schema/session-traces.ts
  - el-templo-api/src/db/schema/session-edit-logs.ts
  - el-templo-api/src/db/schema/completed-sessions.ts
  - el-templo-api/src/db/schema/saved-blocks.ts
  - el-templo-api/src/db/schema/evaluation-requests.ts
  - el-templo-api/src/db/migrations/0194_tenant_id_templo_spom.sql
autonomous: true
requirements: [COL-01]
must_haves:
  truths:
    - "Las 22 tablas del motor SPOM (reglas, catalogos, arbol de ejercicios y sesiones) tienen tenant_id NOT NULL DEFAULT 1 con FK a tenants y el 100% de las filas en 1"
    - "El singleton duro spom_config (CHECK id = 1, mina M1) recibe la columna sin romper su CHECK ni su fila unica"
    - "Los uniques globales del motor SPOM (sessions.day_id, formats.name, day_modes.day_of_week) quedan intactos: convertirlos no es esta fase"
  artifacts:
    - path: "el-templo-api/src/db/migrations/0194_tenant_id_templo_spom.sql"
      provides: "Tanda C3: tenant_id en las 22 tablas del motor SPOM"
      contains: "ADD COLUMN tenant_id INT NULL DEFAULT 1"
    - path: "el-templo-api/src/db/schema/spom-config.ts"
      provides: "Singleton SPOM con tenantId y la mina M1 anotada"
      contains: "tenantIdColumn()"
  key_links:
    - from: "el-templo-api/src/db/schema/sessions.ts"
      to: "el-templo-api/src/db/schema/tenant-column.ts"
      via: "import { tenantIdColumn } from './tenant-column'"
      pattern: "tenantIdColumn"
    - from: "el-templo-api/src/db/migrations/0194_tenant_id_templo_spom.sql"
      to: "el-templo-api/src/db/schema/tenants.ts"
      via: "FOREIGN KEY (tenant_id) REFERENCES tenants(id)"
      pattern: "REFERENCES tenants\\(id\\)"
---

<objective>
Tanda C3 del doc 06 §1: las 22 tablas del **motor SPOM** (reglas y catalogos, arbol de ejercicios y
sesiones) reciben `tenant_id NOT NULL DEFAULT 1` con FK a `tenants`.

Purpose: la denormalizacion es universal (doc 05: TODAS las TEMPLO-MODULO reciben la columna aunque
su scoping lo gobierne el mecanismo de modulos de la fase 176). Sin la columna, el sentinel de la
fase 170 no puede tratar estas tablas como gym-owned y el modulo Templo quedaria fuera del modelo.
Output: 22 schemas Drizzle con `tenantId` + `0194_tenant_id_templo_spom.sql` aplicada a la DB local.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/166-fundaci-n-tenants-anclas-y-scope-server-side/166-PATTERNS.md
@CLAUDE.md

<working_directory>
Todo el codigo se escribe en `/home/franco/projects/et-167-columnas` (worktree del plan 167-01, rama
`feat/167-tenant-columns`). No tocar el checkout principal ni los worktrees `et-166-tenancy` y
`et-164-tv`. Nunca `git add -A`: stagear por ruta.

Si el plan 167-01 reservo numeros distintos de 0192-0195, usar el TERCER numero reservado en vez de
`0194`. Si el 167-01 activo el fallback del helper, la linea a insertar es `...tenantIdColumns`.
</working_directory>

<interfaces>
Mismo contrato y mismo patron SQL de 4 statements por tabla que el plan 167-02 (ver su bloque
`<interfaces>`): `ADD COLUMN tenant_id INT NULL DEFAULT 1` -> `UPDATE ... WHERE tenant_id IS NULL` ->
`MODIFY COLUMN tenant_id INT NOT NULL DEFAULT 1` -> `ADD CONSTRAINT fk_<t>_tenant FOREIGN KEY
(tenant_id) REFERENCES tenants(id)`. Sin clausula de posicionamiento, sin `ADD INDEX` explicito,
`DEFAULT 1` repetido en el MODIFY.

Particularidades de este grupo (todas del doc 05 §2.1-2.3 y §6):

- La mayoria son **[SIN-ANCLA]**: catalogos de la metodologia sin ninguna FK a `users`/`branches`. El
  backfill es directo (`= 1`), no derivado — exactamente igual de valido, y el script de verificacion
  del plan 167-06 las lista aparte como caso legitimo.
- `spom_config` es un **singleton duro** (`PK id` con default 1 + `CHECK (id = 1)`, mina M1): agregar
  una columna no toca el CHECK. No modificar ni eliminar el CHECK en esta fase.
- Uniques globales que **NO se tocan aca**: `sessions.day_id` (mina M5, viaja denormalizado como
  varchar a `completed_sessions.day_id` y `exercise_adjustments.day_id`), `formats.name`,
  `day_modes.day_of_week`, `routes.code`, y los uniques de catalogo del arbol. Su conversion es la
  fase 168 y, para los del modulo Templo, deuda consciente documentada en doc 06 §1-D.
- `session_prescriptions.exercise_id` es una **FK logica sin constraint** (mina M9): el script del
  plan 167-06 la verifica con un join manual.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: tenantId en los 22 schemas Drizzle del motor SPOM</name>
  <files>los 22 archivos de `el-templo-api/src/db/schema/` listados en el action</files>
  <read_first>
    - /home/franco/projects/et-167-columnas/el-templo-api/src/db/schema/tenant-column.ts (el helper: forma exacta de la columna)
    - /home/franco/projects/et-167-columnas/el-templo-api/src/db/schema/spom-config.ts (singleton con CHECK, comillas simples)
    - /home/franco/projects/et-167-columnas/el-templo-api/src/db/schema/sessions.ts (tabla mas grande del grupo, con el unique `day_id` de la mina M5)
    - Un archivo ya editado por el plan 167-02 (por ejemplo `bookings.ts`) para copiar comentario y ubicacion exactos
    - Cada uno de los 22 archivos ANTES de editarlo (todos < 150 lineas)
  </read_first>
  <action>
    Agregar `tenant_id` a estas 22 tablas (tabla -> archivo en `el-templo-api/src/db/schema/`):
    reglas y catalogos (9): routes -> routes.ts, spom_rules -> spom-rules.ts,
    intensity_rules -> intensity-rules.ts, contraction_rules -> contraction-rules.ts,
    weekly_rotator -> weekly-rotator.ts, formats -> formats.ts,
    format_compatibility -> format-compatibility.ts, spom_config -> spom-config.ts,
    day_modes -> day-modes.ts.
    arbol de ejercicios (5): exercises -> exercises.ts,
    exercise_dimension_proposals -> exercise-dimension-proposals.ts,
    exercise_milestone_proposals -> exercise-milestone-proposals.ts,
    exercise_progressions -> exercise-progressions.ts,
    exercise_adjustments -> exercise-adjustments.ts.
    sesiones (8): sessions -> sessions.ts, session_blocks -> session-blocks.ts,
    session_prescriptions -> session-prescriptions.ts, session_traces -> session-traces.ts,
    session_edit_logs -> session-edit-logs.ts, completed_sessions -> completed-sessions.ts,
    saved_blocks -> saved-blocks.ts, evaluation_requests -> evaluation-requests.ts.

    **Edicion por archivo, identica al plan 167-02:** import `tenantIdColumn` + `tenantId:
    tenantIdColumn(),` inmediatamente despues de la columna `id`, con el comentario
    `// Fase 167 (COL-01): tenancy. Valor server-side, nunca de payload. Ver src/db/schema/tenant-column.ts`.
    **Nueve de estos archivos usan comillas simples** (spom-rules, intensity-rules, contraction-rules,
    weekly-rotator, format-compatibility, spom-config, session-edit-logs, saved-blocks,
    evaluation-requests): respetar el estilo de cada archivo, no mezclar.

    Dos comentarios adicionales de una linea:
    - En `spom_config`: que el `CHECK (id = 1)` es la mina M1 y queda intacto en v6.0 (si algun dia
      un tenant distinto de 1 corre SPOM, migra a un unique por `tenant_id`, como hace `tv_class_state`
      con `branch_id`).
    - En `session_prescriptions`: que `exercise_id` es FK logica sin constraint (mina M9) y que la
      consistencia de tenant sobre esa arista la verifica `src/db/scripts/verify-tenant-backfill.ts`.

    NO tocar uniques, NO tocar el CHECK, NO agregar indices, NO reordenar columnas. Sin `any`.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-167-columnas/el-templo-api && npx tsc --noEmit && test $(grep -rho 'tenantIdColumn()' src/db/schema/ | wc -l) -eq 65 && grep -q 'tenantIdColumn()' src/db/schema/spom-config.ts && grep -c 'spom_config_single_row' src/db/schema/spom-config.ts && echo C3_SCHEMA_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` limpio
    - `grep -rho 'tenantIdColumn()' src/db/schema/ | wc -l` devuelve `65` (27 + 16 + 22)
    - Cada uno de los 22 archivos tiene exactamente una ocurrencia de `tenantIdColumn()`
    - `grep -c 'spom_config_single_row' src/db/schema/spom-config.ts` sigue devolviendo `1` (el CHECK no se toco)
    - `git -C /home/franco/projects/et-167-columnas diff --name-only` lista exactamente los 22 archivos de schema de este plan
    - `grep -c 'M1' src/db/schema/spom-config.ts` >= 1 y `grep -c 'M9' src/db/schema/session-prescriptions.ts` >= 1
    - `git diff` no muestra cambios en ninguna linea con `uniqueIndex(` ni `unique(` de estos 22 archivos
    - `grep -nE ':\s*any\b'` sobre los 22 archivos no devuelve lineas nuevas
  </acceptance_criteria>
  <done>Las 22 tablas del motor SPOM declaran `tenant_id` en Drizzle, con el singleton M1 y la FK logica M9 anotadas, y sin tocar un solo unique.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Migracion 0194 (tanda C3) + aplicacion y verificacion local</name>
  <files>el-templo-api/src/db/migrations/0194_tenant_id_templo_spom.sql</files>
  <read_first>
    - .claude/skills/el-templo-db-migrations/SKILL.md (Hard Rules 1-6)
    - /home/franco/projects/et-167-columnas/el-templo-api/src/db/migrations/0192_tenant_id_core_ops.sql (gemelo: mismo patron y estilo de header)
    - /home/franco/projects/et-167-columnas/el-templo-api/src/db/run-migrations.ts lineas 24-140 (`splitSqlStatements` y `alreadyApplied`)
  </read_first>
  <action>
    Escribir a mano `el-templo-api/src/db/migrations/0194_tenant_id_templo_spom.sql` con los 4
    statements del patron para cada una de las 22 tablas, en orden alfabetico (completed_sessions,
    contraction_rules, day_modes, evaluation_requests, exercise_adjustments,
    exercise_dimension_proposals, exercise_milestone_proposals, exercise_progressions, exercises,
    format_compatibility, formats, intensity_rules, routes, saved_blocks, session_blocks,
    session_edit_logs, session_prescriptions, session_traces, sessions, spom_config, spom_rules,
    weekly_rotator) = **88 statements**, cada bloque precedido por `-- <tabla>` sin `;`.

    Header `--` con: fase 167 / COL-01, tanda C3 del doc 06 §1 (motor SPOM, 22 tablas
    TEMPLO-MODULO), que la denormalizacion es universal aunque el scoping lo gobierne el mecanismo de
    modulos (fase 176), por que la columna nace con `DEFAULT 1` (fix WR-01), por que el UPDATE
    normalmente afecta 0 filas, que `spom_config` conserva su `CHECK (id = 1)` (mina M1) y la nota de
    idempotencia. **Ninguna linea puede contener `--` y `;` a la vez.**

    Aplicar y verificar contra la DB local:
    - `pnpm db:migrate` desde `/home/franco/projects/et-167-columnas/el-templo-api`. PROHIBIDO
      `drizzle-kit push` / `drizzle-kit migrate` / `pnpm db:push`.
    - Si falla a mitad de camino, NO re-correr a ciegas (heuristica `alreadyApplied`): arreglar el
      SQL, `DELETE FROM _migrations WHERE name='0194_tenant_id_templo_spom.sql'` si quedo la fila, y
      recien entonces re-correr, cerrando siempre con las consultas de verificacion.
    - Verificar por SQL sobre las 22 tablas: 22 columnas con `IS_NULLABLE='NO'`, `COLUMN_DEFAULT='1'`,
      `DATA_TYPE='int'`; 22 constraints `fk_<t>_tenant` hacia `tenants`; `COUNT(*) WHERE tenant_id <> 1
      OR tenant_id IS NULL` = 0 en cada una.
    - Verificacion especifica de `spom_config`: `SHOW CREATE TABLE spom_config` sigue conteniendo el
      CHECK `spom_config_single_row` y la tabla sigue teniendo como maximo una fila.
    - Prueba empirica de compatibilidad: insertar sin `tenant_id` en una tabla del grupo sin FK
      obligatoria (candidatas: `routes`, `intensity_rules`, `formats`), verificar `tenant_id = 1`,
      borrar la fila.
    - Correr `pnpm db:migrate` una SEGUNDA vez: sin error y sin cambios.

    Commitear el `.sql` junto a los 22 `.ts` (Hard Rule 3), stageando por ruta explicita, mensaje
    `feat(167): tenant_id en el motor SPOM (tanda C3, COL-01)`.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-167-columnas/el-templo-api && ! grep -n -- '--' src/db/migrations/0194_tenant_id_templo_spom.sql | grep -q ';' && test $(grep -v -- '--' src/db/migrations/0194_tenant_id_templo_spom.sql | grep -c 'ADD COLUMN tenant_id INT NULL DEFAULT 1') -eq 22 && test $(grep -v -- '--' src/db/migrations/0194_tenant_id_templo_spom.sql | grep -c 'MODIFY COLUMN tenant_id INT NOT NULL DEFAULT 1') -eq 22 && test $(grep -v -- '--' src/db/migrations/0194_tenant_id_templo_spom.sql | grep -c 'REFERENCES tenants(id)') -eq 22 && test $(grep -v -- '--' src/db/migrations/0194_tenant_id_templo_spom.sql | grep -ci 'AFTER ') -eq 0 && pnpm db:migrate && set -a && . ./.env.development && set +a && MYSQL_PWD="$DB_PASSWORD" mysql -h "$DB_HOST" -P "$DB_PORT" -u "$DB_USER" "$DB_NAME" -N -e "SELECT DATABASE(); SELECT COUNT(*) FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_SCHEMA=DATABASE() AND COLUMN_NAME='tenant_id' AND IS_NULLABLE='NO' AND COLUMN_DEFAULT='1' AND TABLE_NAME IN ('completed_sessions','contraction_rules','day_modes','evaluation_requests','exercise_adjustments','exercise_dimension_proposals','exercise_milestone_proposals','exercise_progressions','exercises','format_compatibility','formats','intensity_rules','routes','saved_blocks','session_blocks','session_edit_logs','session_prescriptions','session_traces','sessions','spom_config','spom_rules','weekly_rotator'); SELECT COUNT(*) FROM INFORMATION_SCHEMA.REFERENTIAL_CONSTRAINTS WHERE CONSTRAINT_SCHEMA=DATABASE() AND REFERENCED_TABLE_NAME='tenants';"</automated>
  </verify>
  <acceptance_criteria>
    - Ninguna linea del `.sql` contiene `--` y `;` a la vez
    - Filtrando comentarios: 22 `ADD COLUMN tenant_id INT NULL DEFAULT 1`, 22 `UPDATE ... WHERE tenant_id IS NULL`, 22 `MODIFY COLUMN tenant_id INT NOT NULL DEFAULT 1`, 22 `REFERENCES tenants(id)`, 0 ocurrencias de `AFTER `
    - La consulta de `INFORMATION_SCHEMA.COLUMNS` sobre las 22 tablas devuelve `22`
    - `SELECT COUNT(*) FROM INFORMATION_SCHEMA.REFERENTIAL_CONSTRAINTS WHERE CONSTRAINT_SCHEMA=DATABASE() AND REFERENCED_TABLE_NAME='tenants'` devuelve `68` (46 acumuladas + 22)
    - La suma de `COUNT(*) WHERE tenant_id <> 1 OR tenant_id IS NULL` sobre las 22 tablas es `0`
    - `SHOW CREATE TABLE spom_config` sigue conteniendo `spom_config_single_row` y `SELECT COUNT(*) FROM spom_config` devuelve `1` o menos
    - El insert de prueba sin `tenant_id` queda con `tenant_id = 1` y se borra despues (el SUMMARY dice en que tabla)
    - Segunda corrida de `pnpm db:migrate` sin error y `SELECT COUNT(*) FROM _migrations WHERE name='0194_tenant_id_templo_spom.sql'` devuelve `1`
    - `git show --stat HEAD` lista los 23 archivos del plan (22 schemas + 1 migracion) en un unico commit
  </acceptance_criteria>
  <done>Las 22 tablas del motor SPOM son tenant-aware en schema y en la DB local, con el singleton M1 intacto y backfill al 100%.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                                                                                 |
| -------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| catalogos de metodologia -> tenants    | Son [SIN-ANCLA]: nada en el modelo relaciona una regla SPOM con un gimnasio salvo la columna nueva          |
| singleton `spom_config` (CHECK id = 1) | Guarda `current_week` de TODO el sistema: incompatible con multi-tenant real, sobrevive por ser Templo-only |
| generador de sesiones -> DB            | Crea sesiones y bloques fuera de un request con scope                                                       |

## STRIDE Threat Register

| Threat ID | Category               | Component                                    | Disposition | Mitigation Plan                                                                                                                                    |
| --------- | ---------------------- | -------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-167-17  | Tampering              | `CHECK (id = 1)` de `spom_config` (mina M1)  | accept      | La columna se agrega sin tocar el CHECK; con un solo tenant corriendo SPOM el invariante sigue siendo cierto. Verificado por `SHOW CREATE TABLE`   |
| T-167-18  | Elevation of Privilege | Catalogos [SIN-ANCLA] del motor SPOM         | mitigate    | `tenant_id` directo con FK a `tenants` (README §4.1: la denormalizacion es la regla, no la excepcion) + verificacion del plan 167-06 que los lista |
| T-167-19  | Tampering              | Uniques globales del modulo (M5, formats)    | accept      | No se tocan en esta fase: es deuda consciente documentada (doc 06 §1-D). Criterio de aceptacion explicito de que el diff no toca ninguna unique    |
| T-167-20  | Denial of Service      | `alreadyApplied` del runner en 88 statements | mitigate    | Prohibicion de re-correr a ciegas + verificacion por `information_schema` + test de CI del plan 167-06                                             |
| T-167-21  | Denial of Service      | Generador de sesiones durante rolling deploy | mitigate    | `ADD COLUMN ... NULL DEFAULT 1` desde el inicio + `NOT NULL DEFAULT 1`: el generador viejo sigue insertando sin conocer la columna                 |
| T-167-SC  | Tampering              | pnpm installs                                | mitigate    | Cero paquetes nuevos en este plan                                                                                                                  |

</threat_model>

<verification>
- `npx tsc --noEmit` verde
- 22 columnas `tenant_id` NOT NULL DEFAULT 1 con FK, acumulado 68 FKs hacia `tenants`
- Cero filas con `tenant_id` distinto de 1 o nulo en las 22 tablas
- `spom_config` conserva su CHECK y su fila unica
- Ningun unique modificado
- Migracion idempotente (dos corridas) y `.sql` junto a los 22 `.ts` en el mismo commit
</verification>

<success_criteria>
COL-01 cumplido para el motor SPOM (65 de las 85 tablas acumuladas), sin tocar uniques ni el singleton
y sin cambios de comportamiento.
</success_criteria>

<output>
Create `.planning/phases/167-columnas-tenant-id-en-las-85-tablas-restantes-verificaci-n/167-04-SUMMARY.md` when done.
</output>
