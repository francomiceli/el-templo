---
phase: 167-columnas-tenant-id-en-las-85-tablas-restantes-verificaci-n
plan: 03
type: execute
wave: 3
depends_on: [167-02]
files_modified:
  - el-templo-api/src/db/schema/campaigns.ts
  - el-templo-api/src/db/schema/notifications.ts
  - el-templo-api/src/db/schema/referrals.ts
  - el-templo-api/src/db/schema/referral-credits.ts
  - el-templo-api/src/db/schema/referral-cta-clicks.ts
  - el-templo-api/src/db/schema/improvement-proposals.ts
  - el-templo-api/src/db/schema/wellhub.ts
  - el-templo-api/src/db/migrations/0193_tenant_id_core_comms.sql
autonomous: true
requirements: [COL-01]
must_haves:
  truths:
    - "Las 16 tablas de comunicacion, crecimiento e integracion Wellhub tienen tenant_id NOT NULL DEFAULT 1 con FK a tenants y el 100% de las filas en 1"
    - "campaign_unsubscribes tiene la columna (mitad de schema de la mina M3): la unique compuesta (tenant_id, email) la agrega la fase 168"
    - "wellhub_events tiene la columna aunque no tenga ninguna FK (mina M6): el backfill es directo, no derivado"
  artifacts:
    - path: "el-templo-api/src/db/migrations/0193_tenant_id_core_comms.sql"
      provides: "Tanda C2: tenant_id en las 16 tablas de comunicacion, crecimiento e integraciones"
      contains: "ADD COLUMN tenant_id INT NULL DEFAULT 1"
    - path: "el-templo-api/src/db/schema/campaigns.ts"
      provides: "Las 4 tablas de campanias con tenantId (campaigns, campaign_sends, campaign_events, campaign_unsubscribes)"
      contains: "tenantIdColumn()"
  key_links:
    - from: "el-templo-api/src/db/schema/wellhub.ts"
      to: "el-templo-api/src/db/schema/tenant-column.ts"
      via: "import { tenantIdColumn } from './tenant-column'"
      pattern: "tenantIdColumn"
    - from: "el-templo-api/src/db/migrations/0193_tenant_id_core_comms.sql"
      to: "el-templo-api/src/db/schema/tenants.ts"
      via: "FOREIGN KEY (tenant_id) REFERENCES tenants(id)"
      pattern: "REFERENCES tenants\\(id\\)"
---

<objective>
Tanda C2 del doc 06 §1: las 16 tablas de **comunicacion, crecimiento e integraciones** (campanias,
notificaciones, referidos, sugerencias y Wellhub) reciben `tenant_id NOT NULL DEFAULT 1` con FK a
`tenants`.

Purpose: cierra la mitad de schema de la mina M3 (`campaign_unsubscribes` no puede tener supresion
por tenant sin la columna) y prepara la mina M6 (`wellhub_events` no tiene ninguna FK: su tenant se
descubre parseando el payload, y la fase 169 necesita la columna para estampar el `TenantContext`).
Output: 7 archivos de schema con 16 tablas anotadas + `0193_tenant_id_core_comms.sql` aplicada local.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/166-fundaci-n-tenants-anclas-y-scope-server-side/166-PATTERNS.md
@CLAUDE.md

<working_directory>
Todo el codigo se escribe en `/home/franco/projects/et-167-columnas` (worktree del plan 167-01, rama
`feat/167-tenant-columns`). No tocar el checkout principal ni los worktrees `et-166-tenancy` y
`et-164-tv`. Nunca `git add -A`: stagear por ruta.

Si el plan 167-01 reservo numeros distintos de 0192-0195, usar el SEGUNDO numero reservado en vez de
`0193`. Si el 167-01 activo el fallback del helper, la linea a insertar es `...tenantIdColumns`.
</working_directory>

<interfaces>
Mismo contrato y mismo patron SQL de 4 statements por tabla que el plan 167-02 (ver su bloque
`<interfaces>`): `ADD COLUMN tenant_id INT NULL DEFAULT 1` -> `UPDATE ... WHERE tenant_id IS NULL` ->
`MODIFY COLUMN tenant_id INT NOT NULL DEFAULT 1` -> `ADD CONSTRAINT fk_<t>_tenant FOREIGN KEY
(tenant_id) REFERENCES tenants(id)`. Sin clausula de posicionamiento, sin `ADD INDEX` explicito
(InnoDB crea el indice de la FK), `DEFAULT 1` repetido en el MODIFY.

Particularidad de este grupo: **tres archivos declaran varias tablas** — `campaigns.ts` (campaigns,
campaign_sends, campaign_events, campaign_unsubscribes), `notifications.ts` (device_tokens,
notification_templates, notification_preferences, pending_notifications) y `wellhub.ts`
(wellhub_classes, wellhub_slots, wellhub_bookings, wellhub_events). En esos archivos el import va una
sola vez y la columna va en las CUATRO tablas.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tenantId en las 16 tablas de comunicacion, crecimiento e integraciones</name>
  <files>el-templo-api/src/db/schema/campaigns.ts, el-templo-api/src/db/schema/notifications.ts, el-templo-api/src/db/schema/referrals.ts, el-templo-api/src/db/schema/referral-credits.ts, el-templo-api/src/db/schema/referral-cta-clicks.ts, el-templo-api/src/db/schema/improvement-proposals.ts, el-templo-api/src/db/schema/wellhub.ts</files>
  <read_first>
    - /home/franco/projects/et-167-columnas/el-templo-api/src/db/schema/tenant-column.ts (el helper: forma exacta de la columna)
    - /home/franco/projects/et-167-columnas/el-templo-api/src/db/schema/campaigns.ts (4 tablas en un archivo)
    - /home/franco/projects/et-167-columnas/el-templo-api/src/db/schema/notifications.ts (4 tablas en un archivo)
    - /home/franco/projects/et-167-columnas/el-templo-api/src/db/schema/wellhub.ts (4 tablas en un archivo)
    - /home/franco/projects/et-167-columnas/el-templo-api/src/db/schema/referrals.ts, referral-credits.ts, referral-cta-clicks.ts, improvement-proposals.ts (una tabla cada uno)
    - Un archivo ya editado por el plan 167-02 (por ejemplo `bookings.ts`) para copiar el comentario y la ubicacion exactos
  </read_first>
  <action>
    Agregar `tenant_id` a estas 16 tablas (tabla -> archivo en `el-templo-api/src/db/schema/`):
    comunicacion (8): campaigns, campaign_sends, campaign_events, campaign_unsubscribes -> campaigns.ts;
    device_tokens, notification_templates, notification_preferences, pending_notifications ->
    notifications.ts.
    crecimiento y feedback (4): referrals -> referrals.ts, referral_credits -> referral-credits.ts,
    referral_cta_clicks -> referral-cta-clicks.ts, improvement_proposals -> improvement-proposals.ts.
    integracion Wellhub (4): wellhub_classes, wellhub_slots, wellhub_bookings, wellhub_events ->
    wellhub.ts.

    **Edicion por tabla, identica al plan 167-02:** import `tenantIdColumn` una vez por archivo, y
    `tenantId: tenantIdColumn(),` inmediatamente despues de la columna `id` de CADA tabla listada,
    precedido por el comentario
    `// Fase 167 (COL-01): tenancy. Valor server-side, nunca de payload. Ver src/db/schema/tenant-column.ts`.

    Dos comentarios adicionales de una linea, solo en estas dos tablas (dejan constancia de las minas
    para las fases 168 y 169):
    - En `campaign_unsubscribes`: que la unique global `email` pasa a `(tenant_id, email)` en la fase
      168 (mina M3, supresion por tenant, doc 06 §8-Q5), y que la fila puede tener `user_id` NULL
      (solo-email), asi que su backfill es directo y no derivado.
    - En `wellhub_events`: que la tabla no tiene ninguna FK (mina M6) y que el tenant se descubre por
      `payload.gym.id` -> `branches.wellhub_gym_id` en la fase 169; hoy el backfill es directo.

    NO agregar indices, NO tocar uniques existentes (eso es la fase 168), NO reordenar columnas. Sin `any`.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-167-columnas/el-templo-api && npx tsc --noEmit && test $(grep -c 'tenantIdColumn()' src/db/schema/campaigns.ts) -eq 4 && test $(grep -c 'tenantIdColumn()' src/db/schema/notifications.ts) -eq 4 && test $(grep -c 'tenantIdColumn()' src/db/schema/wellhub.ts) -eq 4 && test $(grep -rho 'tenantIdColumn()' src/db/schema/ | wc -l) -eq 43 && echo C2_SCHEMA_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` limpio
    - `grep -c 'tenantIdColumn()'` devuelve `4` en `campaigns.ts`, `4` en `notifications.ts`, `4` en `wellhub.ts` y `1` en cada uno de `referrals.ts`, `referral-credits.ts`, `referral-cta-clicks.ts`, `improvement-proposals.ts`
    - `grep -rho 'tenantIdColumn()' src/db/schema/ | wc -l` devuelve `43` (27 del plan 167-02 + 16 de este)
    - `git -C /home/franco/projects/et-167-columnas diff --name-only` lista exactamente los 7 archivos de schema de este plan
    - `grep -c 'M3' src/db/schema/campaigns.ts` >= 1 y `grep -c 'M6' src/db/schema/wellhub.ts` >= 1 (los comentarios de mina quedaron escritos)
    - `grep -nE ':\s*any\b'` sobre los 7 archivos no devuelve lineas nuevas
  </acceptance_criteria>
  <done>Las 16 tablas de comunicacion, crecimiento e integraciones declaran `tenant_id` en Drizzle, con las minas M3 y M6 anotadas en el codigo para las fases que las cierran.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Migracion 0193 (tanda C2) + aplicacion y verificacion local</name>
  <files>el-templo-api/src/db/migrations/0193_tenant_id_core_comms.sql</files>
  <read_first>
    - .claude/skills/el-templo-db-migrations/SKILL.md (Hard Rules 1-6)
    - /home/franco/projects/et-167-columnas/el-templo-api/src/db/migrations/0192_tenant_id_core_ops.sql (el archivo del plan 167-02: este es su gemelo, mismo patron y mismo estilo de header)
    - /home/franco/projects/et-167-columnas/el-templo-api/src/db/run-migrations.ts lineas 24-140 (`splitSqlStatements` y la heuristica `alreadyApplied`)
  </read_first>
  <action>
    Escribir a mano `el-templo-api/src/db/migrations/0193_tenant_id_core_comms.sql` con los 4
    statements del patron para cada una de las 16 tablas, en orden alfabetico (campaign_events,
    campaign_sends, campaign_unsubscribes, campaigns, device_tokens, improvement_proposals,
    notification_preferences, notification_templates, pending_notifications, referral_credits,
    referral_cta_clicks, referrals, wellhub_bookings, wellhub_classes, wellhub_events, wellhub_slots)
    = **64 statements**, cada bloque precedido por una linea de comentario `-- <tabla>` sin `;`.

    Header `--` con: fase 167 / COL-01, tanda C2 del doc 06 §1 (comunicacion + crecimiento +
    integraciones, 16 tablas), por que esta hand-written, por que la columna nace con `DEFAULT 1`
    (fix WR-01 del review 166), por que el UPDATE normalmente afecta 0 filas, y la nota de
    idempotencia. **Ninguna linea puede contener `--` y `;` a la vez.**

    Aplicar y verificar contra la DB local:
    - `pnpm db:migrate` desde `/home/franco/projects/et-167-columnas/el-templo-api`. PROHIBIDO
      `drizzle-kit push` / `drizzle-kit migrate` / `pnpm db:push`.
    - Si la corrida falla a mitad de camino, NO re-correr a ciegas (la heuristica `alreadyApplied` del
      runner puede tragarse errores reales y marcar la migracion como aplicada): arreglar el SQL,
      `DELETE FROM _migrations WHERE name='0193_tenant_id_core_comms.sql'` si quedo la fila, y recien
      entonces re-correr, cerrando siempre con las consultas de verificacion.
    - Verificar por SQL sobre las 16 tablas: 16 columnas `tenant_id` con `IS_NULLABLE='NO'`,
      `COLUMN_DEFAULT='1'`, `DATA_TYPE='int'`; 16 constraints `fk_<t>_tenant` apuntando a `tenants`;
      y `COUNT(*) WHERE tenant_id <> 1 OR tenant_id IS NULL` = 0 en cada una.
    - Prueba empirica de compatibilidad con codigo viejo: insertar una fila sin `tenant_id` en la
      tabla del grupo que menos columnas obligatorias tenga y no dependa de otra fila (candidata:
      `campaign_unsubscribes` con un email de prueba unico, o `wellhub_events` si su set de columnas
      NOT NULL lo permite), verificar que queda con `tenant_id = 1`, y borrarla.
    - Correr `pnpm db:migrate` una SEGUNDA vez: sin error y sin cambios.

    Commitear el `.sql` junto a los 7 `.ts` (Hard Rule 3), stageando por ruta explicita, mensaje
    `feat(167): tenant_id en comunicacion, crecimiento e integraciones (tanda C2, COL-01)`.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-167-columnas/el-templo-api && ! grep -n -- '--' src/db/migrations/0193_tenant_id_core_comms.sql | grep -q ';' && test $(grep -v -- '--' src/db/migrations/0193_tenant_id_core_comms.sql | grep -c 'ADD COLUMN tenant_id INT NULL DEFAULT 1') -eq 16 && test $(grep -v -- '--' src/db/migrations/0193_tenant_id_core_comms.sql | grep -c 'MODIFY COLUMN tenant_id INT NOT NULL DEFAULT 1') -eq 16 && test $(grep -v -- '--' src/db/migrations/0193_tenant_id_core_comms.sql | grep -c 'REFERENCES tenants(id)') -eq 16 && test $(grep -v -- '--' src/db/migrations/0193_tenant_id_core_comms.sql | grep -ci 'AFTER ') -eq 0 && pnpm db:migrate && set -a && . ./.env.development && set +a && MYSQL_PWD="$DB_PASSWORD" mysql -h "$DB_HOST" -P "$DB_PORT" -u "$DB_USER" "$DB_NAME" -N -e "SELECT DATABASE(); SELECT COUNT(*) FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_SCHEMA=DATABASE() AND COLUMN_NAME='tenant_id' AND IS_NULLABLE='NO' AND COLUMN_DEFAULT='1' AND TABLE_NAME IN ('campaign_events','campaign_sends','campaign_unsubscribes','campaigns','device_tokens','improvement_proposals','notification_preferences','notification_templates','pending_notifications','referral_credits','referral_cta_clicks','referrals','wellhub_bookings','wellhub_classes','wellhub_events','wellhub_slots'); SELECT COUNT(*) FROM INFORMATION_SCHEMA.REFERENTIAL_CONSTRAINTS WHERE CONSTRAINT_SCHEMA=DATABASE() AND REFERENCED_TABLE_NAME='tenants';"</automated>
  </verify>
  <acceptance_criteria>
    - Ninguna linea del `.sql` contiene `--` y `;` a la vez
    - Filtrando comentarios: 16 `ADD COLUMN tenant_id INT NULL DEFAULT 1`, 16 `UPDATE ... WHERE tenant_id IS NULL`, 16 `MODIFY COLUMN tenant_id INT NOT NULL DEFAULT 1`, 16 `REFERENCES tenants(id)`, 0 ocurrencias de `AFTER `
    - La consulta de `INFORMATION_SCHEMA.COLUMNS` sobre las 16 tablas devuelve `16`
    - `SELECT COUNT(*) FROM INFORMATION_SCHEMA.REFERENTIAL_CONSTRAINTS WHERE CONSTRAINT_SCHEMA=DATABASE() AND REFERENCED_TABLE_NAME='tenants'` devuelve `46` (30 acumuladas hasta 167-02 + 16 de este plan)
    - La suma de `COUNT(*) WHERE tenant_id <> 1 OR tenant_id IS NULL` sobre las 16 tablas es `0`
    - El insert de prueba sin `tenant_id` queda con `tenant_id = 1` y la fila se borra despues (el SUMMARY dice en que tabla se hizo)
    - Segunda corrida de `pnpm db:migrate` sin error y `SELECT COUNT(*) FROM _migrations WHERE name='0193_tenant_id_core_comms.sql'` devuelve `1`
    - `git show --stat HEAD` lista los 8 archivos del plan (7 schemas + 1 migracion) en un unico commit
  </acceptance_criteria>
  <done>Las 16 tablas de comunicacion, crecimiento e integraciones son tenant-aware en schema y en la DB local, con backfill al 100% y FK nombrada.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                     | Description                                                                                                     |
| -------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| webhook Wellhub -> DB                        | Escrituras que nacen fuera de un request con scope (mina M6): el tenant no viaja en el payload de forma directa |
| motor de envio de campanias -> destinatarios | La supresion de unsubscribes hoy es cross-sistema (mina M3): sin `tenant_id` no puede ser por tenant            |
| codigo viejo (rolling deploy) -> DB migrada  | El webhook y los crons de notificaciones insertan durante el deploy                                             |

## STRIDE Threat Register

| Threat ID | Category               | Component                                            | Disposition | Mitigation Plan                                                                                                                                             |
| --------- | ---------------------- | ---------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-167-12  | Information Disclosure | `campaign_unsubscribes` (mina M3)                    | mitigate    | La columna entra ahora; la unique compuesta `(tenant_id, email)` y el filtro de envio scopeado son CON-01 (fase 168) y ADO (fase 175), anotado en el codigo |
| T-167-13  | Spoofing               | `wellhub_events` sin ninguna FK (mina M6)            | accept      | Con un solo tenant el backfill directo `=1` es correcto por construccion; la derivacion real por `branches.wellhub_gym_id` es CON-04 (fase 169), anotada    |
| T-167-14  | Denial of Service      | Insert del webhook Wellhub durante el rolling deploy | mitigate    | `ADD COLUMN ... NULL DEFAULT 1` desde el inicio (fix WR-01) + `NOT NULL DEFAULT 1` final: el webhook viejo sigue insertando sin conocer la columna          |
| T-167-15  | Denial of Service      | `alreadyApplied` del runner ante una falla parcial   | mitigate    | Prohibicion de re-correr a ciegas + verificacion por `information_schema` independiente de `_migrations` + test de CI del plan 167-06                       |
| T-167-16  | Tampering              | Split del archivo por `;` en comentarios             | mitigate    | Gate automatizado: ninguna linea con `--` y `;` a la vez, mas doble corrida del runner                                                                      |
| T-167-SC  | Tampering              | pnpm installs                                        | mitigate    | Cero paquetes nuevos en este plan                                                                                                                           |

</threat_model>

<verification>
- `npx tsc --noEmit` verde
- 16 columnas `tenant_id` NOT NULL DEFAULT 1 con FK a `tenants`, acumulado 46 FKs hacia `tenants`
- Cero filas con `tenant_id` distinto de 1 o nulo en las 16 tablas
- Insert sin `tenant_id` sigue funcionando
- Migracion idempotente (dos corridas)
- `.sql` y los 7 `.ts` en el mismo commit
</verification>

<success_criteria>
COL-01 cumplido para comunicacion, crecimiento e integraciones (43 de las 85 tablas acumuladas), sin
cambios de comportamiento y con las minas M3 y M6 documentadas en el codigo.
</success_criteria>

<output>
Create `.planning/phases/167-columnas-tenant-id-en-las-85-tablas-restantes-verificaci-n/167-03-SUMMARY.md` when done.
</output>
