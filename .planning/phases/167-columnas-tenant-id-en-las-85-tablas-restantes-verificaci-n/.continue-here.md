# Handoff — Fase 167 pausada (2026-07-27, noche)

## Estado al pausar

**Planes 167-01 a 167-05: COMPLETOS** (5 SUMMARYs commiteados, STATE/ROADMAP al día).
La tanda C está cerrada en local: 87 columnas `tenant_id NOT NULL DEFAULT 1`, 88 FKs a `tenants`,
0 filas mal backfilleadas sobre 26.519. Migraciones **0192-0195** aplicadas e idempotentes en la
DB local. Worktree `/home/franco/projects/et-167-columnas` @ `feat/167-tenant-columns`,
último commit `935e3a91`. **Nada de la fase 167 fue pusheado a origin todavía.**

## ⚠ Trabajo parcial del 167-06 (interrumpido al arrancar)

El ejecutor del plan 167-06 fue interrumpido apenas arrancó y dejó archivos SIN COMMITEAR
en el worktree:

- `el-templo-api/src/db/scripts/verify-tenant-backfill.ts` (untracked)
- `el-templo-api/test/migrations/0192-0195-tenant-columns.test.ts` (untracked)
- `el-templo-api/package.json` (modificado — revisar el diff; probablemente un script npm nuevo)

**Instrucción para el próximo ejecutor del 167-06:** revisar esos archivos contra el plan.
Si el contenido es correcto y completo, verificarlo (correr el script y el test) y seguir el
plan desde ahí. Si está a medias, completarlo o reescribirlo. NO asumir que está terminado
por el hecho de existir.

## Trampas descubiertas que aplican al 167-06 (de los SUMMARYs 04 y 05)

1. **GROUP_CONCAT trunca a 1024 y produce resultados inventados sin error** — el script debe
   iterar las 87 tablas en TypeScript, nunca armar UNION vía GROUP_CONCAT.
2. **MySQL `LIKE` no soporta clases de caracteres** (`LIKE '019[2-5]%'` = 0 siempre) — usar REGEXP.
3. **El predicado ingenuo "tablas con tenant_id" da 88, no 87** — `tenant_settings` tiene la
   columna sin DEFAULT como clave lógica y es exenta. Usar el predicado `NOT NULL DEFAULT 1`
   o la clasificación canónica del módulo tenant-tables (167-01).
4. `getTableName` se importa de `drizzle-orm`, no de `drizzle-orm/mysql-core`.
5. Los hooks de pre-commit no corren en el worktree — `npx prettier --check` a mano antes de
   cada commit; para archivos que ya fallaban prettier en baseline, verificar por equivalencia
   (ver 167-04-SUMMARY), no `--write` a ciegas.

## Cómo retomar

```
/gsd:execute-phase 167
```

El discovery va a encontrar los 5 SUMMARYs y retomar en el 167-06 (wave 6), después el
167-07 (checkpoint humano de rollout staging-first — requiere aprobaciones de Franco para
push y SSH, como en la 166).

## Pendientes fuera de la fase 167

- **UAT de la fase 166 sigue pendiente** (166-HUMAN-UAT.md, 3 ítems): smoke por UI en admin +
  member app, staging y prod. La 166 ya está COMPLETA y en producción.
- Worktrees vivos: `et-166-tenancy` (rama ya mergeada, se conserva hasta UAT), `et-164-tv`,
  y `et-167-columnas` (activo, NO tocar desde otra sesión).

## Critical Anti-Patterns

| Anti-pattern                                                                | Severity |
| --------------------------------------------------------------------------- | -------- |
| Asumir que los archivos parciales del 167-06 están completos porque existen | warning  |
| Tocar código en el checkout principal (rama vieja con WIP ajeno)            | warning  |
