---
phase: 167-columnas-tenant-id-en-las-85-tablas-restantes-verificaci-n
verified: 2026-07-27T18:00:00Z
status: human_needed
score: 6/7 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Smoke funcional por UI en admin (listado de socios, carga de un cobro, pantalla de reservas, reporte de deuda) y en member app (un socio ve sus planes y sus turnos), en staging y en producción"
    expected: "Ninguna pantalla muestra un error nuevo y los números son los mismos que antes del rollout (criterio transversal del milestone: 'los mismos números y los mismos flujos que ayer')"
    why_human: "Requiere observación visual y funcional real de las pantallas del admin y del member app; el smoke automatizado del pipeline (step 19 de cada deploy) y /health 200 no cubren la experiencia de uso real. Franco decidió explícitamente cerrar el plan 167-07 con esta etapa (Etapa F) pendiente, igual que en la fase 166. Ítem harvested del propio checkpoint del plan 167-07 (`<verify><human-check>`)."
deferred:
  - truth: "La arista lógica completed_sessions.day_id -> sessions.day_id cubre la derivación del tenant en producción (hoy el 98.8% de las filas de completed_sessions no tiene match en sessions)"
    addressed_in: "Phase 172 (ISO-03, ancla de la batería de aislamiento)"
    evidence: "REQUIREMENTS.md: 'ISO-03 (batería de aislamiento) se ancla en la fase 172 (piloto finance), que es donde la batería se construye y corre verde por primera vez'; 167-07-SUMMARY.md registra explícitamente el hallazgo como 'derivado a la fase de aislamiento (ISO-03 / fase 171)' y aclara que no es una discrepancia del script (badRows=0, discrepancies=0) sino una debilidad de la arista de derivación para cuando exista un tenant 2"
---

# Phase 167: Columnas — `tenant_id` en las 85 tablas restantes + verificación — Verification Report

**Phase Goal:** Toda tabla gym-owned del sistema tiene su columna `tenant_id NOT NULL` backfilleada a 1, aplicada en migraciones agrupadas por dominio y compatibles con el código viejo en cada paso, y existe un script versionado que demuestra —recorriendo las cadenas de FK del inventario— que el backfill es consistente. End state: el modelo de datos completo es tenant-aware aunque el código todavía no lo use.

**Verified:** 2026-07-27T18:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Nota metodológica

El código de la fase NO vive en el checkout principal (rama `fix/referral-preview-y-refresh-ficha`, sin los cambios de la 167). Toda verificación de código se hizo contra el worktree `/home/franco/projects/et-167-columnas`, cuyo `HEAD` (`68c447cf`) es idéntico a `origin/master` (confirmado con `git fetch` + `git rev-parse`). Se corrieron comandos read-only propios (no se confió en las cifras del SUMMARY sin recomputarlas): `npx tsc --noEmit`, `pnpm db:verify-tenant` contra la DB local, `npx vitest run` sobre los dos archivos de test nuevos de la fase (NO la suite completa, por regla del proyecto), y consultas directas a `information_schema` sobre la base local. No se hizo SSH a staging/prod (instrucción explícita); para esas dos bases se aceptó la evidencia ya verificada por Franco en el 167-07-SUMMARY.md, que es internamente consistente con lo que se reprodujo en local (mismos conteos 87/88/0, mismo formato de salida del verificador).

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                                                                                                                    | Status                   | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Las 85 tablas de la tanda C (27+16+22+20) declaran `tenant_id` vía el helper único `tenantIdColumn()`, sumadas a las 2 anclas de la fase 166 = 87 gym-owned                                                                                              | ✓ VERIFIED               | `grep -rho 'tenantIdColumn()' src/db/schema/ --exclude=tenant-column.ts \| wc -l` = 85 en el worktree; `GYM_OWNED_TABLES.length` = 87, `TENANT_EXEMPT_TABLES.length` = 4 (ejecutado con `tsx`, no leído del código)                                                                                                                                                                                                                                                                                            |
| 2   | Las 4 migraciones (0192-0195) tienen exactamente el patrón de 4 statements por tabla (ADD COLUMN NULL DEFAULT 1 → UPDATE → MODIFY NOT NULL DEFAULT 1 → FK), sin `AFTER`, sin `--`+`;` mezclados                                                          | ✓ VERIFIED               | Conteos recomputados por archivo: 0192=27/27/27, 0193=16/16/16, 0194=22/22/22, 0195=20/20/20 (ADD/MODIFY/REFERENCES), `AFTER `=0 en las 4, `grep -- '--' \| grep ';'` vacío en las 4                                                                                                                                                                                                                                                                                                                           |
| 3   | En la DB local, las 87 tablas gym-owned tienen `tenant_id NOT NULL DEFAULT 1` con FK a `tenants`, y `system_settings`/`labs_inquiries` NO tienen la columna                                                                                              | ✓ VERIFIED               | Query directa a `information_schema` contra la DB local: `cols_87=87`, `fks=88` (87 gym-owned + `fk_tenant_settings_tenant`), `exentas_con_columna=0`; `_migrations` tiene las 4 filas                                                                                                                                                                                                                                                                                                                         |
| 4   | Existe un script versionado (`verify-tenant-backfill.ts`, COL-02) que recorre las cadenas de FK reales (incl. las 9 aristas lógicas M9) y reporta 0 discrepancias, listando aparte SIN-ANCLA y las 4 filas parciales de M4                               | ✓ VERIFIED               | Corrida propia de `pnpm db:verify-tenant` contra la DB local: exit 0, `DISCREPANCIAS: 0`, 32 tablas SIN-ANCLA listadas, 4 filas M4 nombradas explícitamente, 2 warnings informativos (no discrepancias)                                                                                                                                                                                                                                                                                                        |
| 5   | El gate de CI (`test/db/tenant-tables.test.ts` fail-closed + `test/migrations/0192-0195-tenant-columns.test.ts`) pasa y cubre lo mismo que el script CLI                                                                                                 | ✓ VERIFIED               | Corridos individualmente (no la suite completa, por regla del proyecto): 5/5 tests verdes en `tenant-tables.test.ts` (105s), 6/6 tests verdes en `0192-0195-tenant-columns.test.ts` (110s)                                                                                                                                                                                                                                                                                                                     |
| 6   | Las 4 migraciones corrieron verdes primero en `eltemplo_staging` y después en `eltemplo`, con 87/88/0 confirmado por `information_schema` en las DOS bases y el verificador en 0 discrepancias en las dos, sin downtime, con 4 señales humanas separadas | ✓ VERIFIED               | 167-07-SUMMARY.md: salidas de `information_schema` y del verificador pegadas para ambas bases, con los mismos valores (87/88/0) que se reprodujeron en local; 4 runs de CI/Deploy con conclusión `success` y steps citados; `/health` 200 en los dos entornos; `merge-base --is-ancestor origin/staging HEAD` falla (confirma que los 28 commits de staging no viajaron a master) — evidencia internamente consistente, no re-verificada por SSH propio (fuera de alcance de este verificador por instrucción) |
| 7   | Smoke funcional manual por UI en admin y member app, en staging y en producción (criterio transversal: "el staff no percibió ningún cambio")                                                                                                             | ? UNCERTAIN → human item | 167-07-SUMMARY.md, sección "Etapa F": explícitamente marcada PENDIENTE por decisión de Franco. Solo está cubierto el smoke automatizado del pipeline (step 19) y `/health`, no la observación humana de pantallas reales                                                                                                                                                                                                                                                                                       |

**Score:** 6/7 truths verified programmatically. La 7ª es un ítem de verificación humana explícitamente pendiente (no un gap de ejecución).

### Required Artifacts

| Artifact                                                              | Expected                                      | Status                                                           | Details                                                                                                                                                             |
| --------------------------------------------------------------------- | --------------------------------------------- | ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `el-templo-api/src/db/schema/tenant-column.ts`                        | Helper único `tenantIdColumn()`               | ✓ VERIFIED                                                       | Existe, exporta `tenantIdColumn()`, produce `int("tenant_id").notNull().default(1).references(() => tenants.id)`; sin `any`; no está en el barrel `schema/index.ts` |
| `el-templo-api/src/db/tenant-tables.ts`                               | Clasificación canónica 87/4 con predicado     | ✓ VERIFIED                                                       | `GYM_OWNED_TABLES` (87), `TENANT_EXEMPT_TABLES` (4), `isGymOwnedTable()` — probados con `tsx` directamente, no solo leídos                                          |
| `el-templo-api/test/db/tenant-tables.test.ts`                         | Gate fail-closed sobre el schema real         | ✓ VERIFIED                                                       | 5/5 tests verdes, corridos independientemente                                                                                                                       |
| `el-templo-api/src/db/migrations/0192_tenant_id_core_ops.sql` (…0195) | 4 migraciones, patrón de 4 statements × tabla | ✓ VERIFIED                                                       | Conteos recomputados coinciden exactamente con lo declarado (27/16/22/20)                                                                                           |
| `el-templo-api/src/db/scripts/verify-tenant-backfill.ts`              | COL-02: script CLI + funciones exportadas     | ✓ VERIFIED                                                       | Corrido en vivo contra la DB local, exit 0, 0 discrepancias; exporta `verifyTenantBackfill`/`formatReport` (usados por el test de CI, que también pasó)             |
| `el-templo-api/test/migrations/0192-0195-tenant-columns.test.ts`      | Test de CI que invoca el mismo verificador    | ✓ VERIFIED                                                       | 6/6 tests verdes, corridos independientemente                                                                                                                       |
| `el-templo-api/package.json` (`db:verify-tenant`)                     | Script wired, sin tocar deps                  | ✓ VERIFIED                                                       | Diff del archivo entre `e6cab5f6..1c15b300`: una sola línea agregada en `scripts`, cero cambios en `dependencies`/`devDependencies`                                 |
| `.../167-07-SUMMARY.md`                                               | Evidencia del rollout en staging y prod       | ✓ VERIFIED (como evidencia documental, no re-verificada por SSH) | Consistente internamente y con los valores reproducidos en local                                                                                                    |

### Key Link Verification

| From                                               | To                          | Via                                                           | Status | Details                                                                                  |
| -------------------------------------------------- | --------------------------- | ------------------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------- |
| 85 schemas de la tanda C                           | `tenant-column.ts`          | `import { tenantIdColumn } from "./tenant-column"`            | WIRED  | 62 archivos con el import, 85 usos de `tenantIdColumn()` fuera del propio helper         |
| `tenant-column.ts`                                 | `tenants.ts`                | `references(() => tenants.id)`                                | WIRED  | Confirmado en el código fuente del helper                                                |
| 4 migraciones SQL                                  | `tenants` (DB)              | `REFERENCES tenants(id)` / `FOREIGN KEY`                      | WIRED  | 85 ocurrencias recomputadas (27+16+22+20), y 88 FKs reales en `information_schema` local |
| `test/db/tenant-tables.test.ts`                    | `src/db/schema/index.ts`    | `import * as schema` + `getTableName` sobre cada `MySqlTable` | WIRED  | Test corre y pasa; enumera las tablas reales del barrel, no una lista hardcodeada        |
| `test/migrations/0192-0195-tenant-columns.test.ts` | `verify-tenant-backfill.ts` | `import { verifyTenantBackfill }` + `QueryFn` sobre `app.db`  | WIRED  | Test corre y pasa contra la base de test per-worker                                      |
| `verify-tenant-backfill.ts`                        | `tenant-tables.ts`          | `import { GYM_OWNED_TABLES, TENANT_EXEMPT_TABLES }`           | WIRED  | Confirmado por la salida del script (87 tablas gym-owned, exclusiones respetadas)        |

### Data-Flow Trace (Level 4)

No aplica en el sentido estricto de "componente que renderiza datos": esta fase es schema + migraciones + script de verificación. El equivalente de Level 4 aquí es "¿el script lee datos reales de la DB o devuelve valores estáticos?" — verificado: `verify-tenant-backfill.ts` ejecuta queries reales contra `information_schema` y contra las tablas de negocio (no hay ningún `return []`/`return {}` estático), y los conteos cambiaron de forma coherente entre staging (5725 users) y prod (7131 users) según el propio SUMMARY, lo cual es evidencia de que el script lee la base real y no un valor hardcodeado.

### Behavioral Spot-Checks

| Behavior                                                                  | Command                                                                                 | Result                                          | Status |
| ------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- | ----------------------------------------------- | ------ |
| El script de verificación corre y da 0 discrepancias contra una DB real   | `pnpm db:verify-tenant` (worktree, DB local)                                            | exit 0, `DISCREPANCIAS: 0`, 87 tablas cubiertas | ✓ PASS |
| El typecheck del repo entero sigue limpio con las 85 tablas modificadas   | `npx tsc --noEmit` (worktree)                                                           | sin errores                                     | ✓ PASS |
| El test fail-closed de clasificación de tablas pasa contra el schema real | `npx vitest run test/db/tenant-tables.test.ts --no-file-parallelism`                    | 5/5 tests verdes                                | ✓ PASS |
| El test de CI que invoca el verificador pasa contra la base de test       | `npx vitest run test/migrations/0192-0195-tenant-columns.test.ts --no-file-parallelism` | 6/6 tests verdes                                | ✓ PASS |
| Ninguna migración mezcla comentario y `;` (trampa del runner)             | `grep -n -- '--' <sql> \| grep ';'` sobre las 4                                         | vacío en las 4                                  | ✓ PASS |

### Probe Execution

No aplica — la fase no declara probes (`scripts/*/tests/probe-*.sh`); su mecanismo de verificación es el script `verify-tenant-backfill.ts` (cubierto arriba) y los tests de vitest (cubiertos arriba).

### Requirements Coverage

| Requirement | Source Plan     | Description                                                                       | Status      | Evidence                                                                                                                                                                                  |
| ----------- | --------------- | --------------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| COL-01      | 167-01 a 167-07 | Las 85 tablas gym-owned restantes tienen `tenant_id NOT NULL` + FK, backfill `=1` | ✓ SATISFIED | 85 tablas confirmadas en el worktree + 87/88 columnas/FKs confirmadas por `information_schema` local; staging y prod confirmados por el SUMMARY con los mismos valores                    |
| COL-02      | 167-06, 167-07  | Script versionado de verificación, 0 discrepancias, incl. M9                      | ✓ SATISFIED | `verify-tenant-backfill.ts` corrido en vivo (local): exit 0, 0 discrepancias, 9 aristas M9 declaradas (14-15 verificadas tras expandir por `target_kind`), SIN-ANCLA y M4 listados aparte |

**Nota de sincronización documental (no bloqueante):** `.planning/REQUIREMENTS.md` línea 40 todavía marca `COL-01` como `[ ]` (no tildado) en la lista de checkboxes, aunque la tabla de mapeo (línea 103) y toda la evidencia de código/despliegue confirman que está completo. Es un desajuste de bookkeeping en el documento, no un gap de implementación — recomendado corregir el checkbox al cerrar la fase.

**Sin requerimientos huérfanos:** COL-01 y COL-02 son los dos únicos IDs que ROADMAP.md asigna a la fase 167, y ambos aparecen declarados en `requirements:` de al menos un plan (COL-01 en 167-01 a 167-06; COL-02 en 167-06 y 167-07).

### Anti-Patterns Found

| File                                               | Line              | Pattern                                                                                                                                                                                           | Severity                           | Impact                                                                                                                                                  |
| -------------------------------------------------- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `verify-tenant-backfill.ts`                        | ~256-262, 904-924 | Mapa `TARGET_KIND_TABLES` único aplicado a 3 hosts heterogéneos (`transaction_links`, `balances`, `audit_log`); para `balances.target_kind='debt_balance'` el join no tiene semántica establecida | ℹ️ Info (del 167-REVIEW.md, WR-01) | Indetectable con tenant único; relevante quirúrgicamente para cuando exista tenant 2. No bloquea el goal de esta fase (0 discrepancias hoy es correcto) |
| `test/migrations/0192-0195-tenant-columns.test.ts` | ~114-116          | Test 2 asegura `gymOwnedChecked === 87` contra una constante que se devuelve a sí misma, no contra cobertura real medida                                                                          | ℹ️ Info (del 167-REVIEW.md, WR-02) | El Test 1 (`ddlMissing` vacío) sí cubre la cobertura real; este test específico es más débil de lo que su nombre promete                                |
| `verify-tenant-backfill.ts`                        | 507, 885-887      | `COLUMN_TYPE` se guarda en lowercase, asimetría con el path `distinct` de `audit_log`; riesgo de falso verde si un enum futuro usa mayúsculas bajo collation `_bin`                               | ℹ️ Info (del 167-REVIEW.md, WR-03) | Benigno hoy (todas las collations son `_ci` y todos los valores son lowercase); riesgo latente documentado                                              |

Ningún `TBD`/`FIXME`/`XXX` sin resolver en los 83 archivos del diff de la fase (verificado con grep propio). Ningún `: any` nuevo (verificado con grep propio). Los 3 warnings de arriba son del propio `167-REVIEW.md` (0 critical), reclasificados aquí como Info porque son debilidades del verificador de cara al tenant 2 (fases 168-170), no gaps del objetivo de la fase 167 (que es tener las columnas y un verificador que hoy da 0 discrepancias con un solo tenant).

### Human Verification Required

### 1. Smoke funcional por UI (Etapa F del plan 167-07)

**Test:** En el admin: listado de socios, carga de un cobro, pantalla de reservas, reporte de deuda. En el member app: que un socio vea sus planes y sus turnos. En staging y en producción.
**Expected:** Ninguna pantalla muestra un error nuevo y los números son los mismos que antes del rollout de la 167.
**Why human:** Requiere observación visual y de flujo real; el smoke automatizado del pipeline (step "Post-deploy smoke test") y el `/health` 200 no verifican la experiencia de uso genuina. Franco decidió explícitamente cerrar el plan 167-07 con esto pendiente (ítem harvested de `<verify><human-check>` del checkpoint bloqueante del plan 167-07).

### Gaps Summary

No hay gaps de ejecución: las 85 tablas tienen la columna, el script de verificación existe y da 0 discrepancias (verificado en vivo, no solo leído del SUMMARY), los tests de CI pasan, y el rollout a staging y producción está documentado con evidencia consistente con lo reproducido localmente. El único ítem abierto es el smoke funcional manual (Etapa F), que Franco decidió dejar pendiente de forma explícita — por eso el status es `human_needed` y no `passed`. El hallazgo de `completed_sessions.day_id` (98.8% de filas huérfanas para la derivación M9) está correctamente derivado a la fase 172 (ISO-03) y no es un gap de esta fase: el verificador da 0 discrepancias porque el backfill directo a `tenant_id=1` es la verdad hoy (single-tenant); el problema es de cara a un futuro tenant 2, fuera del alcance de COL-01/COL-02.

---

_Verified: 2026-07-27T18:00:00Z_
_Verifier: Claude (gsd-verifier)_
