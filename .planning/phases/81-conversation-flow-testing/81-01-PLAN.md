---
phase: 81-conversation-flow-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-bot/test/conversation-flows.test.ts
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03

must_haves:
  truths:
    - "All 14 QA questions have test assertions verifying knowledge contains correct answer data"
    - "Key conversation flows (lead->trial, renewal, objections, escalation, reactivation) have test assertions verifying prompt+knowledge produce correct behavior"
    - "Mica tone rules (tuteo, emoji limit, one question, no headers, short messages) have test assertions verifying prompt contains these instructions"
  artifacts:
    - path: "el-templo-bot/test/conversation-flows.test.ts"
      provides: "Comprehensive conversation flow and QA tests"
      min_lines: 200
  key_links:
    - from: "el-templo-bot/test/conversation-flows.test.ts"
      to: "el-templo-bot/src/ai/knowledge.ts"
      via: "import getBusinessKnowledge"
      pattern: "getBusinessKnowledge"
    - from: "el-templo-bot/test/conversation-flows.test.ts"
      to: "el-templo-bot/src/ai/system-prompt.ts"
      via: "import getSystemPrompt"
      pattern: "getSystemPrompt"
---

<objective>
Create comprehensive unit tests verifying that the system prompt and knowledge contain everything needed for Mica to handle real-world conversation scenarios correctly.

Purpose: Validate that the 14 QA questions are answerable from knowledge data, that conversation flows for each client state are properly guided by the prompt, and that Mica's tone rules are enforced in the prompt text.
Output: A single test file `el-templo-bot/test/conversation-flows.test.ts` covering all 3 TEST requirements.
</objective>

<execution_context>
@/Users/bores/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bores/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@el-templo-bot/src/ai/system-prompt.ts
@el-templo-bot/src/ai/knowledge.ts
@el-templo-bot/test/knowledge.test.ts
@contexto/conversaciones-reales-referencia.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create conversation flow test file covering TEST-01, TEST-02, TEST-03</name>
  <files>el-templo-bot/test/conversation-flows.test.ts</files>
  <action>
Create `el-templo-bot/test/conversation-flows.test.ts` with three top-level describe blocks. All tests are pure unit tests — no AI API calls. They verify the system prompt and knowledge strings contain the right content.

**TEST-01: 14 QA Questions (describe "QA questions answered correctly")**

For each of the 14 questions, create an `it()` that asserts the knowledge or prompt contains the data needed to answer it. Use `getBusinessKnowledge()` and `getSystemPrompt()`:

1. "Horario de clases en Constitucion" — knowledge contains "Constitucion" and schedule times (7:00, 8:00, 9:00, 17:00, 18:00, 19:00, 20:00). Verify Constitucion weekday morning has 3 slots (7, 8, 9) not 4 (no 10:00).
2. "Como renuevo mi membresia" — knowledge contains "renovar" or "renovacion" and payment alias info (eltemplo.mdp or eltemplomdp).
3. "Dias y horarios de atencion" — knowledge contains "Lunes a viernes" and "7 a 21".
4. "Formas de pago" — knowledge contains "Efectivo" and "transferencia" and "tarjeta de credito".
5. "Descuento aplica solo si pago en efectivo" — knowledge contains Zero rules explaining Boarding Pass and first-time conversion, NOT tied to payment method.
6. "Puedo ir todas las veces que quiera con pase libre" — knowledge contains Flex+ with "Hasta 6 por semana" and explains modality differences (turnos fijos vs libres).
7. "Cuanto dura cada clase" — knowledge contains "60 minutos" or "60 min".
8. "Puedo cambiar de plan" — knowledge contains upgrade paths section with Flex -> Flex+, Flex -> Foundation, etc.
9. "Hay costo extra por clases especiales" — knowledge contains ROM info and mentions which plans include it (Flex+, Foundation+, Performance). Also mentions "2 sesiones de regalo" for basic plans to try ROM.
10. "El pago es automatico o manual" — knowledge contains payment info mentioning efectivo/transferencia (manual), no mention of automatic recurring billing.
11. "Puedo congelar mi membresia" — knowledge contains "congelamiento" and specifies only Performance plan allows it.
12. "Tienen ofertas para nuevos miembros" — knowledge contains "Boarding Pass", "bonificada", and Zero prices for first-timers.
13. "Puedo transferir mi membresia" — knowledge does NOT contain a transfer policy, so verify prompt has fallback behavior ("No estoy segura" or "escalar").
14. "Como se si mi membresia esta activa o cuando vence" — knowledge contains app help section with "Ver membresia" and "Mis servicios/membresias".

**TEST-02: Conversation Flows (describe "Conversation flow correctness")**

Create tests that verify the system prompt + knowledge + state sections produce correct guidance for each flow:

- "Lead asking for info is guided toward trial": Call `getSystemPrompt({ clientState: 'lead' })`, assert it contains "clase de prueba" and "guiarlo". Also verify knowledge contains trial flow data ("Boarding Pass", "bonificada", "24 horas").
- "Active member gets retention focus": Call `getSystemPrompt({ clientState: 'active_member' })`, assert it contains "comunidad" or "familiaridad", and "upgrades" or "beneficios".
- "Inactive member gets warm reactivation": Call `getSystemPrompt({ clientState: 'inactive_member' })`, assert it contains "reactivarla" or "volver", "calidez", "no presion". Also verify knowledge retention section has inactive member strategies.
- "Expired member gets renewal guidance": Call `getSystemPrompt({ clientState: 'expired_member' })`, assert it contains "renueve" or "renovar" and "planes actuales" or "opciones".
- "Objection handling covers all 7 objections": Verify knowledge contains all 7 objection keywords: "caro", "tiempo", "miedo", "pensarlo", "otro lado" (or "otro"), "lejos", "por clase". Assert all 7 are present (stricter than existing test which allows 5/7).
- "Escalation uses exact phrase": Verify prompt contains the exact string "Te paso con alguien del equipo, te escriben enseguida" followed by the emoji and "SILENCIO" or "silencio".
- "Trial registration asks minimal data": Verify prompt contains instruction to ask only "nombre" and "preferencia" and that "telefono ya lo ten" is mentioned.

**TEST-03: Tone Verification (describe "Mica tone rules")**

- "System prompt contains Argentine tuteo instructions": Assert prompt contains "tuteo argentino" and the specific examples "vos", "queres", "podes", "tenes".
- "System prompt limits emojis to 1-2 per message": Assert prompt matches /[Mm]aximo 1-2 emoji/.
- "System prompt requires one question at a time": Assert prompt contains "Una pregunta a la vez".
- "Knowledge output contains no ### headers": Assert `getBusinessKnowledge()` does NOT match /^#{1,3}\s/m.
- "System prompt prefers short messages": Assert prompt contains "cortos" and "escaneables".
- "Golden rules reinforce tone": Assert knowledge contains rules about tuteo, emoji limit, one question at a time, and closing with a question or call to action.

Use vitest imports: `import { describe, it, expect } from 'vitest'`. Import `getBusinessKnowledge` from `../src/ai/knowledge.js` and `getSystemPrompt` from `../src/ai/system-prompt.js`.

Add a file header comment explaining these are conversation flow validation tests for Phase 81 (TEST-01, TEST-02, TEST-03).
</action>
<verify>
<automated>cd /Users/bores/el-templo/el-templo-bot && npx vitest run test/conversation-flows.test.ts</automated>
</verify>
<done>All tests pass. The test file covers all 14 QA questions (TEST-01), all key conversation flows (TEST-02), and all tone rules (TEST-03).</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite to verify no regressions</name>
  <files>el-templo-bot/test/conversation-flows.test.ts</files>
  <action>
Run the full bot test suite (`cd el-templo-bot && pnpm test`) to verify:
1. The new conversation-flows.test.ts passes alongside existing tests
2. No regressions in knowledge.test.ts or other existing test files
3. All test files import cleanly

If any test fails, fix the failing assertion in conversation-flows.test.ts. Do NOT modify existing test files or source code — this is a testing-only phase.
</action>
<verify>
<automated>cd /Users/bores/el-templo/el-templo-bot && pnpm test</automated>
</verify>
<done>Full test suite passes with 0 failures. New conversation flow tests coexist with existing knowledge and system prompt tests.</done>
</task>

</tasks>

<verification>
- `cd el-templo-bot && pnpm test` passes with 0 failures
- `conversation-flows.test.ts` has 3 describe blocks matching TEST-01, TEST-02, TEST-03
- All 14 QA questions have individual test assertions
- All 5 client states are tested for correct flow behavior
- All tone rules have assertions
</verification>

<success_criteria>

- All 14 QA questions have passing tests verifying knowledge contains correct answer data
- Key conversation flows (lead->trial, active->retain, inactive->reactivate, expired->renew, objections, escalation) verified
- Mica tone rules (tuteo, emoji, one question, no headers, short messages) verified
- Full test suite green with no regressions
  </success_criteria>

<output>
After completion, create `.planning/phases/81-conversation-flow-testing/81-01-SUMMARY.md`
</output>
