---
phase: 36-seo-analytics
plan: 04
type: execute
wave: 2
depends_on: ["36-02"]
files_modified:
  - el-templo-web/components/SectionHero.vue
  - el-templo-web/components/SectionIdentity.vue
  - el-templo-web/components/SectionMethod.vue
  - el-templo-web/components/SectionLevels.vue
  - el-templo-web/components/SectionApproaches.vue
  - el-templo-web/components/SectionConversion.vue
  - el-templo-web/components/SectionLocations.vue
  - el-templo-web/components/SectionCommunity.vue
  - el-templo-web/components/SectionEcosystem.vue
  - el-templo-web/components/SectionFaq.vue
  - el-templo-web/components/FranHero.vue
  - el-templo-web/components/GladHero.vue
autonomous: true
requirements:
  - SEO-01
  - SEO-07
  - SEO-08

must_haves:
  truths:
    - "Every page uses semantic HTML (main, article, nav, section, aside) correctly"
    - "Heading hierarchy is correct on every page (one H1, sequential H2-H4)"
    - "Target keywords appear naturally in H1s, H2s, and first paragraphs of key sections"
    - "No accessibility anti-patterns (empty headings, skip-level headings)"
    - "All sections use <section> tags with proper heading inside"
  artifacts:
    - path: "el-templo-web/components/SectionHero.vue"
      provides: "Keyword-rich H1 for home page"
    - path: "el-templo-web/components/FranHero.vue"
      provides: "Keyword-rich H1 for franchise page"
    - path: "el-templo-web/components/GladHero.vue"
      provides: "Keyword-rich H1 for Gladius page"
  key_links:
    - from: "Section components"
      to: "HTML heading hierarchy"
      via: "H2 tags per section, H1 only in hero"
      pattern: "<h[1-6]"
---

<objective>
Audit and fix semantic HTML structure, heading hierarchy, and keyword placement across all section components to maximize SEO value and ensure proper document outline for search engines.

Purpose: Search engines weight semantic structure heavily. Proper heading hierarchy, semantic HTML5 tags, and natural keyword placement in headings and early paragraphs directly impact rankings for target terms.

Output: All section components use correct semantic HTML with optimized heading hierarchy and natural keyword presence.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@el-templo-web/components/SectionHero.vue
@el-templo-web/components/SectionIdentity.vue
@el-templo-web/components/SectionMethod.vue
@el-templo-web/components/SectionLevels.vue
@el-templo-web/components/SectionApproaches.vue
@el-templo-web/components/SectionConversion.vue
@el-templo-web/components/SectionLocations.vue
@el-templo-web/components/SectionCommunity.vue
@el-templo-web/components/SectionEcosystem.vue
@el-templo-web/components/SectionFaq.vue
@el-templo-web/components/FranHero.vue
@el-templo-web/components/GladHero.vue
@el-templo-web/pages/index.vue
@el-templo-web/pages/franquicias.vue
@el-templo-web/pages/gladius.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Semantic HTML audit and heading hierarchy fix</name>
  <files>
    el-templo-web/components/SectionHero.vue
    el-templo-web/components/SectionIdentity.vue
    el-templo-web/components/SectionMethod.vue
    el-templo-web/components/SectionLevels.vue
    el-templo-web/components/SectionApproaches.vue
    el-templo-web/components/SectionConversion.vue
    el-templo-web/components/SectionLocations.vue
    el-templo-web/components/SectionCommunity.vue
    el-templo-web/components/SectionEcosystem.vue
    el-templo-web/components/SectionFaq.vue
    el-templo-web/components/FranHero.vue
    el-templo-web/components/GladHero.vue
  </files>
  <action>
    **Audit checklist for each component — read the file, check, and fix if needed:**

    **A. Heading Hierarchy Rules:**
    - Each PAGE has exactly one `<h1>` (in its Hero component) — already verified, all good
    - Each `<section>` should have an `<h2>` as its primary heading
    - Sub-headings within sections should be `<h3>`, not `<h2>` or `<h4>` (no skipping)
    - Tags used decoratively (e.g., "FAQ", "NIVEL") that aren't actual headings should be `<span>` not `<h3>/<h4>`

    **B. Semantic HTML Rules:**
    - All section components should use `<section>` tags (already verified — they do)
    - Blog post content should be in `<article>` (already is)
    - Navigation is in `<nav>` (already is)
    - Main content wrapper should be `<main>` (already is in page templates)

    **C. For each section component, verify and fix heading levels:**

    Read each component file and check:
    1. Is the primary section heading an `<h2>`? If it's wrongly an `<h3>`, `<h4>`, or `<span>`, change to `<h2>`.
    2. Are there sub-headings? They should be `<h3>` under `<h2>`.
    3. Are decorative tags/labels using heading tags unnecessarily? Change to `<span>`.

    **D. Keyword placement optimization in headings:**

    Target keywords per CONTEXT.md: calistenia, entrenamiento peso corporal, gimnasio funcional [ciudad], wellness.

    Review and enhance heading text (H1s and H2s) to naturally include target keywords where they don't yet appear:

    **Home page sections — check that H2s include relevant terms:**
    - SectionIdentity H2: should reference "calistenia" or "escuela de movimiento" naturally
    - SectionMethod H2: should reference "método" and "entrenamiento" naturally
    - SectionLevels H2: should reference "niveles" and "entrenamiento con peso corporal" naturally
    - SectionLocations H2: should reference "sedes" and city names
    - SectionFaq H2: already "LO QUE TODOS PREGUNTAN" — consider if it can naturally include a keyword

    **Important constraints:**
    - Do NOT keyword-stuff — text must read naturally in brand voice
    - Do NOT change the H1 text (it's the brand hero statement)
    - Do NOT change visual design or add new elements
    - Only adjust heading TAG levels and wording where it reads naturally
    - The UI is in Spanish — keep all text in Spanish
    - Never change section IDs or class names (other plans depend on them)

    **E. Verify the `<main>` wrapper:**
    - Each page template (index.vue, franquicias.vue, gladius.vue, blog/index.vue, blog/[slug].vue) wraps content in `<main>` — already confirmed they do.
    - The `<nav>` in AppNav.vue and links in AppFooter.vue are semantic — verify they use `<nav>` tags.

    **Output:** List of specific changes made per file (heading level changes, keyword additions).

  </action>
  <verify>
    <automated>cd el-templo-web && pnpm typecheck 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Every section component has correct heading hierarchy (H1 in hero only, H2 per section, H3 for sub-headings)
    - No heading levels are skipped (no H1→H3 without H2)
    - Decorative tags/labels don't misuse heading elements
    - Target keywords appear naturally in key H2 headings where appropriate
    - All pages use semantic HTML tags correctly (main, section, article, nav)
    - No changes to visual design, section IDs, or class names
  </done>
</task>

<task type="auto">
  <name>Task 2: Internal cross-linking between pages</name>
  <files>
    el-templo-web/components/SectionIdentity.vue
    el-templo-web/components/SectionMethod.vue
    el-templo-web/components/SectionEcosystem.vue
  </files>
  <action>
    **Internal linking strategy per CONTEXT.md — add cross-links between pages where natural:**

    Review existing CTA links and verify they create a healthy internal link graph:

    1. **SectionEcosystem** — Already has 4 pathway cards linking to /app, /academy, /franquicias, /gladius. Verify these are `<NuxtLink>` tags (not `<a>`) for optimal internal linking. If any are `<a>` tags pointing to internal routes, convert to `<NuxtLink>`.

    2. **SectionIdentity** — The ghost CTA "CONOCÉ NUESTRA FILOSOFÍA" currently may point to `#metodo`. If it doesn't link anywhere, make it link to `#metodo` (same-page section link). This creates an internal link from the identity section to the method section.

    3. **SectionMethod** — Verify the CTA "PROBÁ EL MÉTODO" links to `#sesion-prueba` or the conversion section. This is already likely in place.

    4. **Blog sidebar** — The BlogSidebar component likely has links back to the blog index and potentially to the home page. Verify these are `<NuxtLink>` tags.

    5. **AppFooter** — Footer nav links should use `<NuxtLink>` for internal routes (/franquicias, /gladius, /blog) and `<a>` for external links. Verify this pattern.

    **Key principle:** Every page should have at least 2-3 internal links to other pages on the site. The home page already links to /franquicias, /gladius, /blog via nav and ecosystem cards. Franquicias/Gladius pages link back home via nav. Blog posts link to blog index via sidebar.

    **If cross-linking is already sufficient** (which it likely is given the nav + ecosystem + footer), document the existing link graph and make no unnecessary changes.

    **Add a link to /blog from SectionMethod or SectionCommunity** if one doesn't already exist — blog content strengthens internal linking and gives a reason for search engines to visit the blog.

  </action>
  <verify>
    <automated>cd el-templo-web && pnpm typecheck 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Internal links between pages use NuxtLink (not raw <a> tags) for optimal crawling
    - Every page has 2+ internal links to other site pages
    - Blog is linked from the home page (nav and/or section content)
    - Footer nav uses NuxtLink for internal routes
    - No broken internal links
  </done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes
2. View source: every page has exactly one `<h1>`, sections have `<h2>`, sub-items have `<h3>`
3. No heading levels are skipped in any page's document outline
4. Target keywords appear in at least 3 distinct H2 headings across the home page
5. Internal links use `<a>` (NuxtLink renders as `<a>`) with relative paths
</verification>

<success_criteria>

- Correct heading hierarchy on every page (verifiable via document outline tools)
- Semantic HTML tags used throughout (section, article, nav, main, aside)
- Target keywords naturally present in key headings and descriptions
- Healthy internal link graph between all pages
- No visual design changes — SEO is structural, not visual
  </success_criteria>

<output>
After completion, create `.planning/phases/36-seo-analytics/36-04-SUMMARY.md`
</output>
