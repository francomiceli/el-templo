---
phase: 06-weekly-view
plan: 05
type: execute
wave: 4
depends_on: ["06-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Weekly view displays 7 days correctly"
    - "Today is visually prominent and auto-centered"
    - "Blocks expand to show exercises"
    - "Start button works for today's session"
  artifacts: []
  key_links: []
---

<objective>
Verify the Weekly View feature works correctly through manual testing.

Purpose: Confirm all Phase 6 requirements are met before proceeding to Phase 7 (Day Player).

Output: Human verification that UI works as expected on mobile viewport.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Weekly View feature with:
- Horizontal day carousel (7 days, Mon-Sun)
- Day cards with state-based styling (today prominent, completed green, others muted)
- Auto-center on today when page loads
- Block list for selected day with expandable cards
- Start Session button (appears only for today)
- Navigation to Day Player placeholder
  </what-built>
  <how-to-verify>
1. Start the frontend dev server: `cd el-templo-app && npm run dev`
2. Start the backend: `cd el-templo-api && npm run dev`
3. Open http://localhost:9000/#/login in browser (use mobile viewport in DevTools)
4. Login with test credentials
5. Navigate to Training module

**Verify these behaviors:**

A. **Week Display (WEEK-01)**
   - [ ] 7 days visible via horizontal scroll (Lunes through Domingo)
   - [ ] Week header shows week number and date range

B. **Day Cards (WEEK-02, WEEK-03)**
   - [ ] Today's card is prominent (larger, border, elevated)
   - [ ] Today auto-centers on page load
   - [ ] Adjacent days peek at sides with reduced opacity
   - [ ] Past days appear muted/faded
   - [ ] Future days appear muted/faded
   - [ ] Sunday shows as rest day (distinct styling)
   - [ ] (If any completed) Completed days show green styling with checkmark

C. **Block List**
   - [ ] Selected day shows 5 blocks in vertical list
   - [ ] Each block shows: name, route, format, exercise count
   - [ ] Tapping block expands to show exercise list
   - [ ] Each exercise shows name, reps/seconds, contraction type
   - [ ] Block colors match role identity (Initium light blue, Nucleus primary, etc.)

D. **Start Button (WEEK-05)**
   - [ ] Start button visible only when today is selected
   - [ ] Start button hidden when scrolled to other days
   - [ ] Clicking Start navigates to Day Player placeholder
   - [ ] Placeholder shows date and back button

E. **Mobile Experience**
   - [ ] Swipe gestures work smoothly for day navigation
   - [ ] Scroll snap centers cards properly
   - [ ] No horizontal overflow on mobile viewport (375px width)
   - [ ] Start button respects safe area (no overlap with home indicator)

F. **Error States**
   - [ ] Loading spinner shows while fetching data
   - [ ] Empty state shown if no session for a day
   - [ ] API errors handled gracefully (not blank screen)
  </how-to-verify>
  <resume-signal>
Type "approved" if all checks pass.
If issues found, describe what failed and any error messages seen.
  </resume-signal>
</task>

</tasks>

<verification>
Human verification checkpoint - no automated verification needed.
</verification>

<success_criteria>
All checklist items in how-to-verify pass.
</success_criteria>

<output>
After completion, create `.planning/phases/06-weekly-view/06-05-SUMMARY.md` with verification results.
</output>
