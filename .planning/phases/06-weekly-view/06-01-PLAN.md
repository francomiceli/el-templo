---
phase: 06-weekly-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-app/src/modules/training/types/session.ts
  - el-templo-app/src/modules/training/stores/weekStore.ts
  - el-templo-app/src/modules/training/composables/useDateNavigation.ts
autonomous: true

must_haves:
  truths:
    - "TypeScript types exist for Session, Block, DayCard data"
    - "Pinia store tracks selected day and week sessions"
    - "Date utilities generate Spanish day names (Lunes-Domingo)"
  artifacts:
    - path: "el-templo-app/src/modules/training/types/session.ts"
      provides: "Session, Block, Prescription, DayState TypeScript interfaces"
      exports: ["Session", "Block", "Prescription", "DayState", "BlockRole"]
    - path: "el-templo-app/src/modules/training/stores/weekStore.ts"
      provides: "Pinia store for week state management"
      exports: ["useWeekStore"]
    - path: "el-templo-app/src/modules/training/composables/useDateNavigation.ts"
      provides: "Date utilities for week generation and Spanish locale"
      exports: ["useDateNavigation"]
  key_links:
    - from: "stores/weekStore.ts"
      to: "types/session.ts"
      via: "import types"
      pattern: "import.*from.*types/session"
    - from: "composables/useDateNavigation.ts"
      to: "Date utilities"
      via: "native Date with Spanish locale"
      pattern: "getDay|toISOString|new Date"
---

<objective>
Create foundation types, Pinia store, and date utilities for the Weekly View feature.

Purpose: Establish the data layer that all UI components will consume. Types ensure consistency, store centralizes state, date utils handle Spanish locale and week calculations.

Output: Three TypeScript files providing the complete data foundation for Phase 6.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-session-generation/05-02-SUMMARY.md
@el-templo-api/src/modules/sessions/routes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create session types for frontend</name>
  <files>el-templo-app/src/modules/training/types/session.ts</files>
  <action>
Create TypeScript interfaces matching the API response structure from Phase 5.

Define these types:
- `BlockRole`: Union type 'INITIUM' | 'NUCLEUS' | 'DEUTEROS_1' | 'DEUTEROS_2' | 'ATHLOS_EPIKOS'
- `Prescription`: exerciseId (number), exerciseName (string), contraction (string), reps (number | null), seconds (number | null), rest (number), notes (string | null), sortOrder (number)
- `Block`: blockId (string), role (BlockRole), route (string), pattern (string), intensity (number), repsBudget (number), format (string), sortOrder (number), exercises (Prescription[])
- `Session`: dayId (string), week (number), day (string), levelGroup (string), blockCount (number), blocks (Block[])
- `DayState`: 'today' | 'past' | 'future' | 'completed' | 'rest'
- `WeekDay`: date (string YYYY-MM-DD), dayName (string - Spanish), dayOfWeek (number 0-6), state (DayState), session (Session | null)

Export all types. Add JSDoc comments explaining each type's purpose.
  </action>
  <verify>
Run `npx tsc --noEmit` from el-templo-app directory - no type errors.
  </verify>
  <done>
Types file exists at el-templo-app/src/modules/training/types/session.ts with all 6 type definitions exported.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create week Pinia store</name>
  <files>el-templo-app/src/modules/training/stores/weekStore.ts</files>
  <action>
Create a Pinia store using Composition API pattern (matching existing useAuthStore).

Store state:
- `weekDays`: WeekDay[] - array of 7 days for current week
- `selectedDate`: string | null - currently centered date (YYYY-MM-DD)
- `loading`: boolean - fetch in progress
- `error`: string | null - last error message

Getters:
- `selectedDay`: computed WeekDay for selectedDate
- `todayIndex`: computed index of today in weekDays array
- `hasCompletedDays`: boolean if any day is completed

Actions:
- `fetchWeekSessions(dates: string[])`: Fetch sessions for multiple dates from API
- `selectDate(date: string)`: Set selectedDate
- `markDayCompleted(date: string)`: Update day state to 'completed'
- `reset()`: Clear all state

The store should NOT make API calls directly - it receives data from composables.
Use `defineStore('week', () => { ... })` pattern with ref/computed/function exports.
Import types from '../types/session'.
  </action>
  <verify>
Run `npx tsc --noEmit` from el-templo-app - no type errors. Import resolves correctly.
  </verify>
  <done>
Store file exists at el-templo-app/src/modules/training/stores/weekStore.ts with defineStore exporting useWeekStore.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create date navigation composable</name>
  <files>el-templo-app/src/modules/training/composables/useDateNavigation.ts</files>
  <action>
Create composable for date utilities with Spanish locale support.

Functions to export:
- `getWeekDates(referenceDate?: Date)`: Returns array of 7 date strings (Mon-Sun) for the week containing referenceDate (defaults to today)
- `formatDayName(date: string | Date)`: Returns Spanish day name ('Lunes', 'Martes', etc.)
- `formatShortDate(date: string | Date)`: Returns 'DD/MM' format
- `isToday(date: string)`: Boolean check
- `isSunday(date: string)`: Boolean check (rest day indicator)
- `getDateState(date: string, completedDates: string[])`: Returns DayState based on comparison with today

Spanish day names array:
```typescript
const SPANISH_DAYS = ['Domingo', 'Lunes', 'Martes', 'Miercoles', 'Jueves', 'Viernes', 'Sabado']
```

Use native Date methods. Return dates as YYYY-MM-DD strings for API compatibility.
Week starts on Monday (ISO week).
  </action>
  <verify>
Run `npx tsc --noEmit` from el-templo-app - no type errors.
  </verify>
  <done>
Composable file exists at el-templo-app/src/modules/training/composables/useDateNavigation.ts with all 6 functions exported.
  </done>
</task>

</tasks>

<verification>
1. All three files exist in correct locations
2. `npx tsc --noEmit` passes with no errors
3. Types can be imported: `import { Session, Block } from '../types/session'`
4. Store can be used: `const store = useWeekStore()`
5. Date functions return expected formats (verify with manual test)
</verification>

<success_criteria>
- TypeScript compiles without errors
- Types match API response structure from GET /sessions/daily
- Store provides reactive state management for week view
- Date utilities correctly handle Spanish locale and Monday-start weeks
</success_criteria>

<output>
After completion, create `.planning/phases/06-weekly-view/06-01-SUMMARY.md`
</output>
