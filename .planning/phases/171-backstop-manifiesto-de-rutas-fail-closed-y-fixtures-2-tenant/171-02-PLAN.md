---
phase: 171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant
plan: 02
type: execute
wave: 2
depends_on: [171-01]
files_modified:
  - el-templo-api/test/tenant-manifest.ts
  - .planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-CLASIFICACION.md
autonomous: true
requirements: [ISO-01]

must_haves:
  truths:
    - "El manifiesto tiene una entrada explicita por ruta exacta (method + path) para el 100% de las rutas registradas hoy — 370 segun el volcado del RESEARCH — agrupadas por modulo y sin una sola regla por prefijo ni comodin (D-01)"
    - "Toda entrada `global` lleva motivo escrito, oracion completa que nombra la causa concreta (D-02)"
    - "Toda entrada `templo-module` declara a cual de los 4 modulos Templo pertenece (D-07)"
    - "Las rutas dudosas quedan marcadas en el archivo y listadas aparte con recomendacion y porque, para el checkpoint humano (D-04)"
    - "El baseline se genero UNA vez con un script descartable fuera del repo: no queda regenerador commiteado (D-16 de la fase 170)"
    - "Franco tiene un dossier revisable donde `templo-module` se presenta por modulo x prefijo con conteos, no como 141 lineas sueltas"
  artifacts:
    - path: "el-templo-api/test/tenant-manifest.ts"
      provides: "TENANT_MANIFEST poblado con las 370 entradas clasificadas"
      contains: "categoria"
      min_lines: 420
    - path: ".planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-CLASIFICACION.md"
      provides: "Dossier de revision humana: global con motivos, templo-module agrupado, dudosas con recomendacion"
      contains: "Dudosas"
  key_links:
    - from: "el-templo-api/test/tenant-manifest.ts"
      to: "el-templo-api/src/app.ts"
      via: "las claves salen del volcado del hook onRoute sobre buildApp real"
      pattern: "OPTIONS \\*"
    - from: ".planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-CLASIFICACION.md"
      to: "el-templo-api/test/tenant-manifest.ts"
      via: "el checkpoint del plan 06 aplica sobre el manifiesto lo que Franco decida en el dossier"
      pattern: "D-04"
---

<objective>
Poblar el manifiesto: volcar el inventario real de rutas una sola vez y escribir las 370
entradas explicitas clasificadas en `tenant-scoped` / `global` (con motivo) / `templo-module`
(con modulo), y dejar preparado el dossier que el checkpoint de D-03/D-04 le pone delante a
Franco.

Purpose: es el trabajo humano que el goal pide y que no se automatiza sin vaciarlo. El
volcado da las CLAVES; la clasificacion es una decision por ruta. Las 12 zonas grises no se
deciden aca: se recomiendan y se marcan.

Output: `TENANT_MANIFEST` con 370 entradas + `171-CLASIFICACION.md` con las tres secciones
que el checkpoint necesita.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-CONTEXT.md
@.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-RESEARCH.md
@.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-PATTERNS.md
@.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-01-SUMMARY.md
@CLAUDE.md

**Worktree:** `/home/franco/projects/et-170-sentinel` (rama `feat/170-sentinel-lint`). Cero
migraciones, cero deps nuevas, `git add` por ruta, nada de pushear.

**Fuentes de la clasificacion (no re-litigar, estan cerradas):**

- `.docs/saas-multitenancy/04-mecanismo-modulos.md` §2.1 — mapeo carpeta -> modulo Templo,
  validado con Nacho el 2026-07-02. Es la fuente del reparto `templo-training` /
  `templo-marketing` / `templo-onboarding` / `templo-gamification`.
- `.docs/saas-multitenancy/06-estrategia-migracion.md` §8 — Q2 (`labs_inquiries` es GLOBAL,
  plataforma) y Q5 (supresion de unsubscribes POR TENANT). Las dos decisiones chocan con el
  mapeo del doc 04 en casos concretos: ese choque es exactamente lo que va al checkpoint (D-04).
- `171-RESEARCH.md` §"Inventario real de rutas" (370 claves, 199 HEAD descartados, 7 HEAD
  fantasma con barra final, `OPTIONS *`, `/health` sin `/api`, las 5 rutas de `/spom/*` fuera
  de `/api`), §"Candidatas global firmes" (tabla con motivo propuesto) y §"Dudosas" (12 filas).

<estado_del_baseline>

El volcado es ONE-SHOT (precedente D-16 de la fase 170). El plan 171-01 dejo el seam
commiteado, asi que el script del volcado se reduce a llamar `buildApp({ onRoute })` desde un
archivo en el scratchpad de la sesion. Ese archivo NO entra al repo, NO se commitea y NO se
convierte en un `pnpm run` — un regenerador permanente seria la puerta trasera que vacia la
decision consciente de D-01.

El conteo 370 es un snapshot de esta misma rama tomado el 2026-07-29. Si el volcado da otro
numero, NO ajustar el numero y seguir: parar, listar el delta contra el inventario del
RESEARCH y explicar de donde salieron o adonde se fueron esas rutas en el SUMMARY.

</estado_del_baseline>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Volcado one-shot del inventario de rutas (descartable, fuera del repo)</name>
  <files>(ninguno del repo — la salida vive en el scratchpad de la sesion)</files>
  <read_first>
    - 171-RESEARCH.md §"Code Examples" -> "Volcado one-shot del inventario" — el comando exacto
      con `set -a && . ./.env.development`, `NODE_ENV=test`, `DB_NAME=eltemplo_test_1`,
      `JWT_SECRET=dummy` y `./node_modules/.bin/tsx`.
    - 171-RESEARCH.md §"Pitfall 2" — `buildApp()` NECESITA MySQL vivo: `src/modules/sessions/routes.ts:376`
      hace un SELECT sobre `formats` durante el registro del plugin.
    - el-templo-api/src/app.ts — la firma `buildApp(opts)` que dejo el plan 01.
    - el-templo-api/test/tenant-manifest.ts — `clavesDeEvento` y `particionarObservadas`, que el
      script del volcado debe reusar en vez de reimplementar la normalizacion.
  </read_first>
  <action>
    Escribir en el directorio de scratchpad de la sesion (NUNCA dentro del repo) un script tsx
    descartable que: importe `buildApp` de `src/app.ts` y `clavesDeEvento`/`particionarObservadas`
    de `test/tenant-manifest.ts`; llame `buildApp({ onRoute })` acumulando las claves; haga
    `await app.ready()` y `await app.close()`; e imprima, ordenadas y unicas, (a) las rutas sin
    HEAD, (b) el conteo total de eventos, (c) el conteo de HEAD, (d) los HEAD huerfanos.

    Correrlo con MySQL local vivo y las variables de `.env.development` (el comando literal esta
    en el RESEARCH). Guardar la salida en el scratchpad como insumo del Task 2.

    Contrastar contra el inventario del RESEARCH: 569 eventos totales, 199 HEAD, **370 rutas**,
    0 duplicados, 0 HEAD huerfanos, 7 HEAD con barra final (que el guard tolera porque tienen
    GET hermano sin barra). Si algun numero difiere, documentar el delta ruta por ruta antes de
    escribir una sola entrada.

    Verificar antes de cerrar el task que no quedo NINGUN archivo nuevo bajo `el-templo-api/`:
    el script vive afuera y muere con la sesion.
  </action>
  <acceptance_criteria>
    - El archivo de salida del scratchpad tiene 370 lineas y `sort -u` da 370 (0 duplicados).
    - `grep -c '^HEAD ' <salida>` devuelve 0.
    - La salida contiene literalmente `OPTIONS *`, `GET /health` y `GET /spom/week`.
    - Los HEAD huerfanos reportados son 0.
    - `git status --porcelain el-templo-api/` no lista ningun archivo nuevo (el script no toco el repo).
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && test "$(sort -u "$DUMP" | wc -l)" = "$(wc -l < "$DUMP")" && test "$(wc -l < "$DUMP")" = 370</automated>
  </verify>
  <done>Las 370 claves reales estan volcadas, contrastadas contra el RESEARCH, y el generador no existe en el repo.</done>
</task>

<task type="auto">
  <name>Task 2: Escribir las 370 entradas clasificadas en TENANT_MANIFEST</name>
  <files>el-templo-api/test/tenant-manifest.ts</files>
  <read_first>
    - La salida del volcado del Task 1 (las 370 claves exactas).
    - 171-RESEARCH.md §"Candidatas global firmes" (8 filas con motivo propuesto) y
      §"Sizing de las listas del checkpoint" (218 tenant-scoped / 141 templo-module: training 102,
      marketing 35, onboarding 3, gamification 1) y §"Dudosas" (12 filas con recomendacion).
    - `.docs/saas-multitenancy/04-mecanismo-modulos.md` §2.1 — el mapeo carpeta -> modulo.
    - `.docs/saas-multitenancy/06-estrategia-migracion.md` §8 — Q2 y Q5.
    - el-templo-api/src/db/tenant-tables.ts lineas 233-260 — el formato literal a copiar:
      separadores de seccion con caja `// ── nombre ──…` y el motivo como oracion completa que
      nombra la causa concreta, no una etiqueta.
  </read_first>
  <action>
    Poblar `TENANT_MANIFEST` con UNA entrada por clave del volcado, agrupadas por modulo con
    separadores de caja en este orden: plataforma (`/health`, `OPTIONS *`), auth, y despues los
    prefijos del API ordenados alfabeticamente, cerrando con los 4 bloques `templo-*`.

    Reglas de clasificacion:
    - Default masivo: `{ categoria: "tenant-scoped" }`, sin anotacion (D-02: la anotacion es
      para la excepcion, no para la regla).
    - `global`: solo las candidatas firmes del RESEARCH, cada una con `motivo` en castellano,
      oracion completa que nombra la causa (liveness sin tablas / preflight CORS sin logica /
      identidad resuelta antes de conocer el gimnasio / tenant derivado server-side desde
      `branches.wellhub_gym_id` / fila pre-claim que nace antes de saber de quien es el televisor).
      Prohibido un motivo generico tipo "publica" o "no aplica".
    - `templo-module`: `modulo` obligatorio, tomado del mapeo del doc 04 §2.1.
    - Las 12 filas dudosas del RESEARCH se escriben con la categoria RECOMENDADA ahi y llevan,
      en la linea de arriba, un comentario que arranca literalmente con `D-04 dudosa:` seguido
      del porque en una linea (incluyendo, cuando corresponde, el conflicto de docs: `labs_inquiries`
      doc 06 §8 Q2 vs doc 04 §2.1; `/api/campaigns/track/*` y `/unsubscribe` vs Q5).

    Prohibiciones duras de este task: ni una regla por prefijo, ni un comodin, ni un helper que
    "complete" categorias por defecto a partir del path (D-01). Ni una entrada `global` sin
    motivo, ni una `templo-module` sin modulo — el gate del plan 03 las va a nombrar una por una.

    Al terminar, dejar en el docblock de cabecera la linea con la fecha del volcado, el conteo
    total y el reparto real por categoria (el reparto exacto lo define este task; el RESEARCH da
    218/141/11 como estimacion, no como contrato).
  </action>
  <acceptance_criteria>
    - `pnpm exec tsc --noEmit --strict --skipLibCheck test/tenant-manifest.ts` sale 0.
    - Un one-liner (`pnpm exec tsx -e` o script descartable del scratchpad) que importe el modulo
      imprime: total de claves = 370; suma de las 3 categorias = 370; `global` sin `motivo` = 0;
      `templo-module` sin `modulo` = 0; categorias fuera de las 3 = 0.
    - El conjunto de claves del manifiesto es IGUAL (no subconjunto) al del volcado del Task 1:
      diff de las dos listas ordenadas, vacio.
    - `grep -v '^\s*[*/]' el-templo-api/test/tenant-manifest.ts | grep -c "categoria:"` devuelve 370.
    - `grep -c "D-04 dudosa:" el-templo-api/test/tenant-manifest.ts` devuelve >= 12.
    - `grep -Ec "\*\"|prefijo\.\*|startsWith\(" el-templo-api/test/tenant-manifest.ts` no revela
      ninguna clasificacion por prefijo (revision manual del resultado; `OPTIONS *` es una clave
      literal de Fastify, no un comodin).
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit --strict --skipLibCheck test/tenant-manifest.ts</automated>
  </verify>
  <done>Las 370 rutas tienen entrada explicita; ninguna `global` sin motivo, ninguna `templo-module` sin modulo, las 12 dudosas marcadas.</done>
</task>

<task type="auto">
  <name>Task 3: Dossier de revision humana 171-CLASIFICACION.md (insumo del checkpoint D-03/D-04)</name>
  <files>.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-CLASIFICACION.md</files>
  <read_first>
    - el-templo-api/test/tenant-manifest.ts recien escrito — los conteos y motivos reales.
    - 171-CONTEXT.md D-03 y D-04 — que se revisa y que NO se revisa (la masa `tenant-scoped` no
      va a revision: equivocarse hacia tenant-scoped sobra proteccion, no falta).
    - 171-RESEARCH.md §"Sizing de las listas del checkpoint" — la advertencia de que
      `templo-module` son ~141 rutas y hay que presentarlas por modulo x prefijo con conteo,
      no una por una.
  </read_first>
  <action>
    Escribir el dossier con exactamente tres secciones y nada mas:

    A. `global` — tabla ruta / motivo escrito, una fila por entrada `global` del manifiesto. Es
       la lista corta y peligrosa: se revisa entera.
    B. `templo-module` — tabla modulo / prefijo / conteo / que hay ahi, ordenada por modulo. El
       total de la columna conteo tiene que dar exactamente el numero de entradas `templo-module`
       del manifiesto, y esa igualdad se afirma en el texto. Objetivo: ~20 lineas revisables en
       vez de 141.
    C. `Dudosas (D-04)` — una fila POR CASO, con: ruta(s), recomendacion escrita en el manifiesto,
       por que es dudosa, y el documento/decision que la respalda o la contradice. Los dos
       conflictos reales van explicitos: `labs_inquiries` (doc 06 §8 Q2 dice plataforma/global vs
       doc 04 §2.1 que mete `app-landing` en `templo-marketing`) y `/api/campaigns/track/open`,
       `/click`, `/unsubscribe` (recomendadas `tenant-scoped` porque la decision Q5 hizo la
       supresion por tenant; marcarlas `global` contradiria esa decision).

    Cerrar con una nota de una linea: la decision de Franco se aplica sobre el manifiesto en el
    plan 171-06 y esta seccion se actualiza ahi con el resultado. NO agregar recomendaciones de
    alcance ni "proximos pasos": el dossier es para decidir, no para planificar.
  </action>
  <acceptance_criteria>
    - El archivo existe y tiene las tres secciones A, B y C con esos titulos.
    - La seccion C tiene >= 12 filas y menciona literalmente `labs-inquiry`, `campaigns/track` y `unsubscribe`.
    - La suma de la columna de conteo de la seccion B es igual al numero de entradas
      `templo-module` del manifiesto (verificado con el one-liner del Task 2 y escrito en el texto).
    - La seccion A tiene una fila por cada entrada `global` del manifiesto y ningun motivo vacio.
    - El dossier NO lista rutas `tenant-scoped` una por una (D-03: esa masa no va a revision).
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-CLASIFICACION.md && grep -c "D-04" .planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-CLASIFICACION.md</automated>
  </verify>
  <done>Franco puede revisar global + fronteras de modulo + dudosas en una sola lectura corta.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| ruta ↔ categoria | Clasificar de mas como `global` desactiva el backstop de esa ruta para siempre |
| decision de docs ↔ manifiesto | El manifiesto es donde una decision de producto (Q2/Q5) se vuelve enforceable o se pierde |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-171-04 | Elevation of Privilege | Entrada `global` puesta para "que pase el test" | mitigate | D-02: motivo escrito obligatorio, oracion que nombra la causa; gate `sinMotivo` en el plan 03; revision humana de la lista corta (D-03) |
| T-171-05 | Information Disclosure | Ruta gris clasificada sola y mal (blog, academy, franchise, labs) | mitigate | D-04: las 12 dudosas se marcan con `D-04 dudosa:` y van al dossier con recomendacion; nadie las cierra sin Franco |
| T-171-06 | Tampering | Regenerador del baseline que permita "refrescar" la clasificacion | mitigate | D-16: script descartable en el scratchpad, criterio de aceptacion de que `git status` del API queda sin archivos nuevos |
| T-171-07 | Repudiation | Clasificacion sin trazabilidad de por que | mitigate | Motivos en el archivo + dossier `171-CLASIFICACION.md` versionado con la fuente de cada decision |
| T-171-SC | Tampering | Instalacion de paquetes | mitigate | Cero deps nuevas; `pnpm-lock.yaml` sin cambios |

</threat_model>

<verification>
- Volcado: 370 rutas, 0 duplicados, 0 HEAD huerfanos
- `pnpm exec tsc --noEmit --strict --skipLibCheck test/tenant-manifest.ts` sale 0
- Claves del manifiesto == claves del volcado (diff vacio)
- 0 entradas `global` sin motivo, 0 `templo-module` sin modulo
- `171-CLASIFICACION.md` con las 3 secciones y la seccion C con >= 12 casos
- `git status --porcelain el-templo-api/` sin archivos nuevos ademas del manifiesto ya trackeado
</verification>

<success_criteria>
- 100% de las rutas registradas hoy tienen entrada explicita por ruta exacta (criterio 1 del ROADMAP, mitad manifiesto)
- Cero comodines, cero reglas por prefijo (D-01)
- Motivos y modulos escritos (D-02, D-07)
- Dudosas marcadas y ruteadas al checkpoint (D-04)
- Baseline one-shot sin regenerador commiteado (D-16)
</success_criteria>

<output>
Create `.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-02-SUMMARY.md` when done
</output>
