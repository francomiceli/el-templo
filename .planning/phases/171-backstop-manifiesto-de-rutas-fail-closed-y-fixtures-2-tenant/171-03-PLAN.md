---
phase: 171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant
plan: 03
type: execute
wave: 3
depends_on: [171-01, 171-02]
files_modified:
  - el-templo-api/test/tenancy/iso-01-manifiesto.test.ts
autonomous: true
requirements: [ISO-01]

must_haves:
  truths:
    - "El test cruza el manifiesto contra lo que el hook `onRoute` observa en el app REAL: si los conteos difieren, rojo (criterio 1 del ROADMAP)"
    - "Una ruta observada sin entrada deja el test rojo con un mensaje que la NOMBRA y dice que hacer — demostrado con fixture sintetico en CI y con una sonda en vivo, no asumido (criterio 2)"
    - "Una entrada del manifiesto que ya no existe (typo, rename, ruta borrada) tambien deja rojo"
    - "Una entrada `global` sin motivo (D-02) y una `templo-module` sin modulo (D-07) dejan rojo, validadas en RUNTIME porque `tsc` de CI no mira `test/`"
    - "Todo HEAD observado tiene un GET hermano: un HEAD declarado a mano se pone rojo en vez de colarse por el filtro"
    - "El mensaje de rojo dice explicitamente que agrandar la categoria `global` no es una salida valida sin motivo escrito"
  artifacts:
    - path: "el-templo-api/test/tenancy/iso-01-manifiesto.test.ts"
      provides: "Gate fail-closed bidireccional del manifiesto + motor probado con fixtures sinteticos"
      contains: "compararManifiesto"
      min_lines: 180
  key_links:
    - from: "el-templo-api/test/tenancy/iso-01-manifiesto.test.ts"
      to: "el-templo-api/test/helpers.ts"
      via: "createTestApp({ onRoute }) recolecta las rutas del app real"
      pattern: "createTestApp\\(\\{"
    - from: "el-templo-api/test/tenancy/iso-01-manifiesto.test.ts"
      to: "el-templo-api/test/tenant-manifest.ts"
      via: "import de compararManifiesto / particionarObservadas / TENANT_MANIFEST"
      pattern: "tenant-manifest"
---

<objective>
Cerrar ISO-01 por comportamiento: el gate que deja CI en rojo cuando aparece una ruta sin
clasificar, con el mensaje que la nombra, probado de las dos maneras que el criterio 2 exige
— con fixtures sinteticos que quedan corriendo en CI para siempre, y con una sonda en vivo
que se agrega, se ve caer y se borra sin commitear.

Purpose: sin la mitad sintetica el criterio 2 seria una afirmacion; sin la sonda en vivo el
cableado completo (seam -> hook -> normalizacion -> comparador -> mensaje) nunca se ejercita
de punta a punta.

Output: `test/tenancy/iso-01-manifiesto.test.ts` verde + la evidencia literal del rojo de la
sonda registrada en el SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-CONTEXT.md
@.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-RESEARCH.md
@.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-PATTERNS.md
@.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-02-SUMMARY.md
@CLAUDE.md

**Worktree:** `/home/franco/projects/et-170-sentinel` (rama `feat/170-sentinel-lint`).

**Costo de la corrida:** este archivo es MySQL-backed a la fuerza (Pitfall 2 del RESEARCH:
`buildApp()` hace un SELECT sobre `formats` al registrar el plugin de sessions), y cada archivo
de vitest paga ~96 s de provisioning del worker. Correr **un archivo por vez**:
`pnpm exec vitest run test/tenancy/iso-01-manifiesto.test.ts --hookTimeout=250000`. No tocar
`vitest.config.ts`. No correr la suite completa local: eso es CI.

<interfaces>

De `el-templo-api/test/tenant-manifest.ts` (plan 171-01 / 171-02):
export function compararManifiesto(observadas, manifiesto?): Discrepancias;  // faltantes, fantasmas, sinMotivo, sinModulo, categoriaInvalida
export function particionarObservadas(claves): { rutas: string[]; headHuerfanos: string[] };
export function clavesDeEvento(metodo, url): string[];
export const TENANT_MANIFEST: Record<string, EntradaManifiesto>;

De `el-templo-api/test/helpers.ts` (plan 171-01):
export async function createTestApp(opts?: BuildAppOptions): Promise<FastifyInstance>;

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: El gate `test/tenancy/iso-01-manifiesto.test.ts`</name>
  <files>el-templo-api/test/tenancy/iso-01-manifiesto.test.ts</files>
  <read_first>
    - el-templo-api/test/db/tenant-tables.test.ts COMPLETO — es el modelo literal: docblock
      `POR QUE EXISTE ESTE ARCHIVO`, gate bidireccional (`unclassified` / `ghosts`), el gate de
      motivos vacios o con marcadores TODO/FIXME, y el test de conteo exacto cuyo mensaje explica
      que mover el numero es una DECISION DE DISENO.
    - el-templo-api/test/tenancy/con-06-lint.test.ts lineas 55-150 — la estructura de dos
      `describe` (motor sobre fixtures sinteticos / contra lo real) y el aviso de que agrandar la
      allowlist no es una salida valida.
    - el-templo-api/test/tenancy/con-04-crons-per-tenant.test.ts lineas 250-370 — el comentario
      de bloque con `POR QUE ES FAIL-CLOSED` / `QUE HACER CUANDO SE CAIGA` y el enunciado de que
      el filtro no es cosmetico (modelo para el guard de HEAD).
    - 171-RESEARCH.md §"Pattern 2" y §"Pattern 3" (tabla de los 4 casos sinteticos) y
      §"Pitfall 2/3/4/5".
    - el-templo-api/test/tenant-manifest.ts — las firmas exactas que este archivo importa.
  </read_first>
  <action>
    Crear el archivo en `test/tenancy/`, en castellano y con el docblock de cabecera del repo
    (`POR QUE EXISTE ESTE ARCHIVO`, `LO QUE SE AFIRMA`, `QUE NO ES ESTE ARCHIVO`,
    `QUE HACER CUANDO SE CAIGA`, mas la nota de como correrlo). El docblock debe decir
    EXPLICITAMENTE que este archivo SI necesita `createTestApp()` —`buildApp()` consulta
    `formats` durante el registro del plugin de sessions— para que nadie lo "optimice" moviendolo
    a `test/unit/`.

    Estructura en dos `describe`:

    A. `contra el app real` — `beforeAll` unico que hace
       `createTestApp({ onRoute: (r) => observadas.push(...clavesDeEvento(r.method, r.url)) })`,
       guarda el resultado de `particionarObservadas(observadas)` y cierra el app en `afterAll`.
       Tests:
       1. Toda ruta observada esta clasificada: `faltantes` vacio. Mensaje con los 4 elementos
          (que fallo nombrando `test/tenant-manifest.ts`; la lista `.sort().join(", ")`; que hacer
          — agregar la linea en la categoria que corresponda; por que importa — criterio 2 del
          ROADMAP y el aislamiento de las fases 172-175) MAS la frase de que **agrandar `global`
          no es una salida valida sin motivo escrito** (D-02).
       2. Toda entrada del manifiesto existe realmente: `fantasmas` vacio, mensaje que dice que
          atrapa typos, renames y rutas borradas.
       3. Guard de HEAD: `headHuerfanos` vacio, con el mensaje que explica que un HEAD declarado
          a mano hay que clasificarlo o quitarlo, y que filtrar HEAD a secas era el anti-patron.
       4. Conteo exacto: el manifiesto tiene el numero de entradas del baseline (el que dejo el
          plan 02) y el mensaje explica que mover ese numero es una decision, no un detalle: sube
          cuando se agrega una ruta CON su linea, baja cuando se borra una ruta Y su linea.
    B. `motor con fixtures sinteticos (criterio 2, demostrado)` — sin app, invocando
       `compararManifiesto` con listas inventadas y un manifiesto inyectado por el segundo
       parametro:
       5. observadas = [una clave real, `"POST /api/_probe-171-sin-clasificar"`] -> `faltantes`
          contiene esa clave exacta.
       6. manifiesto con `"GET /api/ruta-que-ya-no-existe"` -> `fantasmas` la nombra.
       7. entrada `{ categoria: "global", motivo: "" }` y otra con `motivo: "TODO"` -> las dos en
          `sinMotivo` (D-02 enforced en runtime: `tsc` de CI no typechequea `test/`).
       8. entrada `{ categoria: "templo-module" }` sin `modulo` -> `sinModulo` la nombra (D-07).
       9. entrada con `categoria` fuera de las 3 -> `categoriaInvalida` la nombra.

    Cero `any`. Cero `console.log`. Las aserciones usan siempre `expect(valor, mensaje)` con el
    mensaje accionable; una aserción sin mensaje no cumple el criterio 2.
  </action>
  <acceptance_criteria>
    - `cd el-templo-api && pnpm exec vitest run test/tenancy/iso-01-manifiesto.test.ts --hookTimeout=250000` sale 0 con >= 9 tests verdes.
    - `grep -c "expect(" el-templo-api/test/tenancy/iso-01-manifiesto.test.ts` es >= 9 y cada `expect` de los 9 tests lleva segundo argumento (revision del archivo).
    - `grep -c "createTestApp({" el-templo-api/test/tenancy/iso-01-manifiesto.test.ts` devuelve 1 (un solo `beforeAll`, no uno por test: cada `buildApp` cuesta segundos).
    - El mensaje del test 1 contiene literalmente la frase que dice que agrandar `global` no es una salida valida.
    - `grep -c ": any\|console\." el-templo-api/test/tenancy/iso-01-manifiesto.test.ts` devuelve 0.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm exec vitest run test/tenancy/iso-01-manifiesto.test.ts --hookTimeout=250000</automated>
  </verify>
  <done>El gate corre verde contra el app real y el motor esta probado con las 5 formas de discrepancia.</done>
</task>

<task type="auto">
  <name>Task 2: Sonda en vivo — ver el rojo con una ruta de verdad y borrarla sin commitear</name>
  <files>el-templo-api/src/app.ts (modificacion TEMPORAL, revertida dentro del mismo task)</files>
  <read_first>
    - .planning/phases/169-capa-de-escritura-helpers-tenantwhere-tenantvalues-y-tenantc/169-04-SUMMARY.md
      — el precedente literal de la sonda (`src/jobs/__gate-probe.ts`): se agrega, se ve caer el
      gate listandola, se borra sin commitear.
    - el-templo-api/src/app.ts — donde registrar la ruta boba sin romper nada.
    - 171-RESEARCH.md §"Pattern 3" ultimo parrafo — por que hacen falta LAS DOS cosas (la sonda
      prueba el cableado completo, los fixtures lo dejan probado en CI).
  </read_first>
  <action>
    Agregar temporalmente en `src/app.ts` una ruta boba `app.get("/api/_probe-171", async () => ({ ok: true }))`
    despues de los registros existentes. Correr el archivo de test del Task 1 y capturar la salida
    LITERAL del rojo: tiene que ser el test 1 (`faltantes`) y el mensaje tiene que nombrar
    `GET /api/_probe-171` y decir que hacer.

    Revertir la sonda con `git checkout -- el-templo-api/src/app.ts` (nunca commitear el estado
    roto) y volver a correr el test: verde.

    Pegar en el SUMMARY del plan las dos evidencias: el bloque literal del mensaje de rojo y la
    linea de resultado del verde posterior. Sin eso, el criterio 2 del ROADMAP sigue siendo una
    afirmacion.
  </action>
  <acceptance_criteria>
    - La corrida con la sonda falla y la salida capturada contiene literalmente `GET /api/_probe-171`.
    - El mensaje del rojo incluye la instruccion de que hacer (agregar la linea al manifiesto) y la frase sobre `global`.
    - Tras revertir: `git status --porcelain el-templo-api/src/app.ts` vacio y `git diff --numstat el-templo-api/src/app.ts` sin salida.
    - La re-corrida del test sale 0.
    - El SUMMARY contiene las dos evidencias pegadas (rojo literal + verde posterior).
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && git status --porcelain src/app.ts | wc -l | grep -qx 0 && pnpm exec vitest run test/tenancy/iso-01-manifiesto.test.ts --hookTimeout=250000</automated>
  </verify>
  <done>Criterio 2 del ROADMAP demostrado en vivo y el repo queda exactamente como estaba.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| PR ↔ pipeline de CI | El gate es lo unico que impide que una ruta nueva entre a master sin decision de clasificacion |
| test ↔ codigo de produccion | La sonda toca `src/app.ts`: si se commitea por accidente, queda una ruta publica sin auth en el API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-171-08 | Information Disclosure | Ruta nueva sin filtro de tenant que nadie clasifica | mitigate | Gate `faltantes` fail-closed contra el app real + mensaje que nombra la ruta |
| T-171-09 | Tampering | Sonda `/api/_probe-171` commiteada por accidente | mitigate | Revert con `git checkout --` dentro del mismo task + criterio de aceptacion sobre `git status --porcelain src/app.ts` vacio; nunca se commitea el estado roto |
| T-171-10 | Repudiation | Gate verde por vacuidad (el hook no vio ninguna ruta) | mitigate | Test de conteo exacto contra el baseline: 0 rutas observadas deja rojo igual que 371 |
| T-171-11 | Elevation of Privilege | Entrada `global` sin motivo colada por falta de typecheck en `test/` | mitigate | Gates `sinMotivo` / `sinModulo` / `categoriaInvalida` en RUNTIME (Pitfall 5) |
| T-171-SC | Tampering | Instalacion de paquetes | mitigate | Cero deps nuevas |

</threat_model>

<verification>
- `pnpm exec vitest run test/tenancy/iso-01-manifiesto.test.ts --hookTimeout=250000` sale 0, >= 9 tests
- Sonda en vivo: rojo capturado nombrando `GET /api/_probe-171`, revert limpio, verde posterior
- `git status --porcelain el-templo-api/src/app.ts` vacio al cerrar el plan
- Cero migraciones, cero deps nuevas
</verification>

<success_criteria>
- Criterio 1 del ROADMAP: el manifiesto se compara contra lo que `onRoute` observa y difieren = rojo
- Criterio 2 del ROADMAP: demostrado dos veces (fixture sintetico permanente en CI + sonda en vivo)
- D-02 y D-07 enforced en runtime
- El guard de HEAD se pone rojo en vez de tragarse un HEAD manual
</success_criteria>

<output>
Create `.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-03-SUMMARY.md` when done
</output>
