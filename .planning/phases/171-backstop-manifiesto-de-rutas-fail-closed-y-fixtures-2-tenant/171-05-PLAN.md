---
phase: 171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant
plan: 05
type: execute
wave: 5
depends_on: [171-04]
files_modified:
  - el-templo-api/test/tenancy/iso-02-fixtures.test.ts
autonomous: true
requirements: [ISO-02]

must_haves:
  truths:
    - "Las 7 piezas del espejo de D-06 (sede, admin, coach, 2 socios, plan, schedule) nacen con `tenant_id` del gimnasio 2, afirmado por `SELECT tenant_id` sobre la fila REAL y no por la respuesta HTTP"
    - "Las aserciones son de doble lado: las filas del gimnasio 2 aparecen Y las del 1 no — una aserción de sola inclusion pasaria en verde con un WHERE sin filtro"
    - "`createStaffUser` y `createTestMember` SIN `tenantId` siguen escribiendo en el tenant 1 (retrocompat probada, no asumida)"
    - "`limpiarSegundoGimnasio` deja 0 filas del gimnasio 2 en tenants, branches y users, y el gimnasio 1 intacto"
    - "La siembra es opt-in: `test/setup.ts` no referencia el fixture, asi que ningun archivo existente ve el tenant 2 (criterio 4 por construccion)"
  artifacts:
    - path: "el-templo-api/test/tenancy/iso-02-fixtures.test.ts"
      provides: "Verificacion del espejo del gimnasio 2, retrocompat de los helpers e higiene del archivo"
      contains: "seedSecondTenant"
      min_lines: 200
  key_links:
    - from: "el-templo-api/test/tenancy/iso-02-fixtures.test.ts"
      to: "el-templo-api/test/fixtures/second-tenant.ts"
      via: "import de seedSecondTenant / limpiarSegundoGimnasio / TENANT_DOS"
      pattern: "second-tenant"
    - from: "el-templo-api/test/tenancy/iso-02-fixtures.test.ts"
      to: "el-templo-api/test/setup.ts"
      via: "gate D-05: el setup global no importa el fixture (lectura por fs)"
      pattern: "setup.ts"
---

<objective>
Probar por comportamiento que el fixture del gimnasio 2 hace lo que dice —contra MySQL real y
mirando la BASE, no la respuesta HTTP— y dejar afirmado que la suite existente no lo ve.

Purpose: un fixture de tenancy que "funciona" pero siembra en el tenant 1 es el peor resultado
posible: verde y falso. Ya paso dos veces en la fase 169 (T-168-15). La unica evidencia valida
es `SELECT tenant_id` sobre la fila realmente creada.

Output: `test/tenancy/iso-02-fixtures.test.ts` verde + la regresion dirigida que respalda el
criterio 4 mientras CI corre la suite completa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-CONTEXT.md
@.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-RESEARCH.md
@.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-PATTERNS.md
@.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-04-SUMMARY.md
@CLAUDE.md

**Worktree:** `/home/franco/projects/et-170-sentinel`. Un archivo de vitest por vez
(`--hookTimeout=250000`), ~100 s cada uno. **La suite completa NO se corre local: corre en CI**
(memoria del proyecto + skill `el-templo-change-control`).
</context>

<tasks>

<task type="auto">
  <name>Task 1: `test/tenancy/iso-02-fixtures.test.ts` — el espejo es real y la limpieza no deja rastro</name>
  <files>el-templo-api/test/tenancy/iso-02-fixtures.test.ts</files>
  <read_first>
    - el-templo-api/test/tenancy/tenant-helpers.test.ts COMPLETO — el analog: constantes arriba,
      aserciones de doble lado (`toContain` + `not.toContain` + `every`), `contarTenant`, y el
      `describe("higiene del archivo")` de cierre.
    - el-templo-api/test/tv/tv-pairing-tenant.test.ts lineas 24-45 y 200-240 — por que las
      aserciones van contra la BASE y no contra el body, y el orden `cleanAllTestData` -> `limpiarRastro`.
    - el-templo-api/test/fixtures/second-tenant.ts — lo que el plan 04 escribio: nombres exportados,
      forma de `SegundoGimnasio`, que siembra y que no.
    - el-templo-api/test/helpers.ts — `createStaffUser` / `createTestMember` con el parametro nuevo.
    - 171-RESEARCH.md §"Phase Requirements -> Test Map" (las 3 filas de ISO-02) y §"Pitfall 7/8".
  </read_first>
  <action>
    Crear el archivo en castellano con docblock de cabecera (`POR QUE EXISTE ESTE ARCHIVO`,
    `LO QUE SE AFIRMA`, `QUE NO ES ESTE ARCHIVO` — no es la bateria de aislamiento, eso es ISO-03
    en la fase 172 —, y la nota de como correrlo solo).

    Ciclo de vida: `beforeAll` crea el app; `beforeEach` hace `cleanAllTestData(app)` y DESPUES
    `seedSecondTenant(app)` (orden obligado: al reves el fixture se borra antes del primer `it`);
    `afterAll` hace `cleanAllTestData` + `limpiarSegundoGimnasio` + `app.close()`.

    Tests, agrupados en tres `describe`:

    A. `el espejo minimo de D-06 nace en el gimnasio 2`
       1. Un test por familia de entidad (sede, staff, socios, plan, schedule, actividad) que hace
          `SELECT tenant_id` por `id` sobre la fila realmente creada y espera `TENANT_DOS`. Se
          permite agrupar staff (admin + coach) y socios (los 2) en un test cada uno, pero cada
          fila se afirma individualmente.
       2. Cardinalidad de D-06: filtrando por `tenant_id = TENANT_DOS` hay exactamente 1 sede,
          1 plan, 1 schedule, 1 actividad y 4 usuarios (1 admin + 1 coach + 2 socios).
       3. Doble lado: la consulta filtrada por el gimnasio 2 NO devuelve ninguna fila del gimnasio 1
          (`not.toContain` sobre ids conocidos del 1) y todas las devueltas tienen `tenantId === TENANT_DOS`.
       4. Los tokens del gimnasio 2 son utilizables: `getAuthToken` devolvio un JWT para el admin y
          para los 2 socios, y los emails del gimnasio 2 no coinciden con ningun email del 1.
    B. `retrocompatibilidad de los helpers (criterio 3)`
       5. `createStaffUser` SIN `tenantId` -> la fila queda con `tenant_id = 1`.
       6. `createTestMember` SIN `tenantId` -> la fila queda con `tenant_id = 1` y sigue pasando
          por `POST /api/auth/register` (el socio existe con la forma que devuelve register).
    C. `higiene del archivo (D-05)`
       7. `limpiarSegundoGimnasio` deja 0 filas en `tenants`, `branches` y `users` para
          `TENANT_DOS`, y el gimnasio 1 sigue con su fila (`contarTenant(app, 1) === 1`).
       8. Gate de opt-in: leer `test/setup.ts` con `fs` y afirmar que NO menciona
          `second-tenant` ni `seedSecondTenant`. Mensaje accionable: sembrar el gimnasio 2 en el
          setup global rompe el criterio 4 del ROADMAP (los ~232 archivos existentes cambiarian
          sus conteos); la siembra es opt-in por archivo.

    Cero `any`, cero `console.log`, todas las aserciones con mensaje accionable como segundo
    argumento de `expect`. Normalizar la salida de `app.db.execute` con el helper `contarTenant`
    copiado del analog (la forma que devuelve es incomoda y los dos precedentes la normalizan igual).
  </action>
  <acceptance_criteria>
    - `cd el-templo-api && pnpm exec vitest run test/tenancy/iso-02-fixtures.test.ts --hookTimeout=250000` sale 0 con >= 8 tests verdes.
    - `grep -c "tenantId: schema\|tenant_id" el-templo-api/test/tenancy/iso-02-fixtures.test.ts` es >= 6 (las aserciones van contra la columna, no contra el body HTTP).
    - `grep -c "not.toContain\|not.toBe(1)\|!==" el-templo-api/test/tenancy/iso-02-fixtures.test.ts` es >= 1 (al menos una aserción de exclusion explicita).
    - El test 8 falla si alguien agrega `seedSecondTenant` a `test/setup.ts` (probarlo mentalmente contra el contenido actual: hoy da 0 menciones).
    - `grep -c ": any\|console\." el-templo-api/test/tenancy/iso-02-fixtures.test.ts` devuelve 0.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm exec vitest run test/tenancy/iso-02-fixtures.test.ts --hookTimeout=250000</automated>
  </verify>
  <done>El espejo del gimnasio 2 esta probado contra la base y la limpieza no deja rastro.</done>
</task>

<task type="auto">
  <name>Task 2: Regresion dirigida del criterio 4 (la suite completa la corre CI)</name>
  <files>(ninguno — corridas de verificacion, sin edits)</files>
  <read_first>
    - el-templo-api/test/branch-access.test.ts lineas 380-470 — el archivo mas expuesto a una sede
      extra; el RESEARCH verifico que usa `toContain`/`not.toContain` y no conteos exactos de sedes.
    - .claude/skills/el-templo-change-control/SKILL.md — la regla de no correr la suite completa local.
    - 171-RESEARCH.md §"Pitfall 8" y §"Assumptions Log" A6 — el riesgo residual y quien lo cierra.
  </read_first>
  <action>
    Correr, uno por vez y en este orden, los archivos mas expuestos al cambio de `test/helpers.ts`
    y a la existencia de una sede/gimnasio extra en la base del worker:
    `test/branch-access.test.ts`, `test/tv/tv-pairing-tenant.test.ts` y un tercero de alto volumen
    que use `createStaffUser` intensivamente. El tercero se elige ANTES de correr, con
    `grep -c "createStaffUser" test/*.test.ts | sort -t: -k2 -rn | head -1`, y se exporta como
    `TERCERO=<ruta>` para que la cadena del `<verify>` lo incluya. Los tres tienen que salir 0 **sin tocar una sola linea de esos
    archivos**: si alguno pide ajustar expectativas, eso es una violacion del criterio 4 y hay que
    arreglar el fixture o el helper, no el test.

    Dejar registrado en el SUMMARY: los archivos corridos, el conteo de tests de cada uno y la
    frase explicita de que la suite COMPLETA se valida en CI (job `api-test` contra MySQL 8.0),
    no local.
  </action>
  <acceptance_criteria>
    - Los 3 archivos salen 0, encadenados con `&&` en una sola corrida (si uno falla, la cadena corta).
    - Los dos primeros son fijos (`test/branch-access.test.ts` y `test/tv/tv-pairing-tenant.test.ts`);
      el tercero se elige por mayor uso de `createStaffUser` (`grep -c "createStaffUser" test/*.test.ts | sort -t: -k2 -rn | head -1`)
      y se exporta como `TERCERO` antes de correr la cadena.
    - `git status --porcelain el-templo-api/test/` no lista ninguno de esos 3 archivos (cero ajustes de expectativas).
    - El SUMMARY nombra los 3 archivos (incluido cual salio elegido como tercero), sus conteos de tests y deja escrito que el criterio 4 completo lo cierra CI.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm exec vitest run test/branch-access.test.ts --hookTimeout=250000 && pnpm exec vitest run test/tv/tv-pairing-tenant.test.ts --hookTimeout=250000 && pnpm exec vitest run "$TERCERO" --hookTimeout=250000</automated>
  </verify>
  <done>Ningun test existente cambio de expectativa por el tenant 2; el cierre formal del criterio 4 queda en CI.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| fixture ↔ suite existente | 232 archivos comparten la base del worker: un residuo del gimnasio 2 los contamina |
| aserción ↔ evidencia | Afirmar sobre el body HTTP en vez de sobre la columna produce verdes falsos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-171-16 | Spoofing | Verde falso: el fixture escribio en el tenant 1 y el test no lo nota | mitigate | Toda aserción por `SELECT tenant_id` sobre la fila real + doble lado (incluye y excluye) |
| T-171-17 | Denial of Service | Residuo del gimnasio 2 que rompe archivos posteriores del mismo worker | mitigate | `limpiarSegundoGimnasio` en `afterAll` + test de higiene que lo afirma con conteos en 0 |
| T-171-18 | Tampering | Alguien enchufa la siembra al setup global y cambia los conteos de 232 archivos | mitigate | Gate por `fs` sobre `test/setup.ts` (D-05) con mensaje que cita el criterio 4 |
| T-171-19 | Repudiation | "La suite sigue verde" afirmado sin evidencia | mitigate | 3 archivos expuestos corridos y registrados + cierre formal en CI, escrito en el SUMMARY |
| T-171-SC | Tampering | Instalacion de paquetes | mitigate | Cero deps nuevas |

</threat_model>

<verification>
- `pnpm exec vitest run test/tenancy/iso-02-fixtures.test.ts --hookTimeout=250000` sale 0, >= 8 tests
- 3 archivos existentes expuestos corriendo en verde SIN modificarlos
- `test/setup.ts` sin referencias al fixture
- Cero migraciones, cero deps nuevas
</verification>

<success_criteria>
- Criterio 3 del ROADMAP completo: los fixtures siembran 2 gimnasios y los helpers aceptan el tenant
- Criterio 4 del ROADMAP respaldado por regresion dirigida, con el cierre formal delegado a CI
- Cero cambios de expectativas en tests existentes
</success_criteria>

<output>
Create `.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-05-SUMMARY.md` when done
</output>
