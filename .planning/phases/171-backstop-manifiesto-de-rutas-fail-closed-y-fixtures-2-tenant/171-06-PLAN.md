---
phase: 171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant
plan: 06
type: execute
wave: 6
depends_on: [171-02, 171-03, 171-05]
files_modified:
  - el-templo-api/test/tenant-manifest.ts
  - .planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-CLASIFICACION.md
autonomous: false
requirements: [ISO-01]

must_haves:
  truths:
    - "Franco reviso las dos listas cortas y peligrosas —`global` con sus motivos y `templo-module` agrupada por modulo x prefijo— y las 12 dudosas una por una (D-03, D-04)"
    - "Ninguna ruta dudosa quedo clasificada sin decision humana: los dos conflictos de docs (labs_inquiries, campaigns/track+unsubscribe) tienen veredicto escrito"
    - "Lo que Franco decidio esta APLICADO en el manifiesto y registrado en el dossier con su fuente"
    - "El gate ISO-01 sigue verde despues de aplicar las correcciones: ninguna `global` quedo sin motivo ni ninguna `templo-module` sin modulo"
    - "El etiquetado `templo-module` queda cerrado para que la fase 176 agregue `requireModule` sin re-clasificar 141 rutas (D-07)"
  artifacts:
    - path: "el-templo-api/test/tenant-manifest.ts"
      provides: "Manifiesto con la clasificacion aprobada por el dueño del producto"
      contains: "categoria"
    - path: ".planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-CLASIFICACION.md"
      provides: "Dossier con el veredicto de cada dudosa y la fecha de la revision"
      contains: "Veredicto"
  key_links:
    - from: ".planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-CLASIFICACION.md"
      to: "el-templo-api/test/tenant-manifest.ts"
      via: "cada veredicto del dossier se aplica como categoria/motivo/modulo en el manifiesto"
      pattern: "D-04"
---

<objective>
Cerrar la fase con la decision humana que el goal exige: Franco revisa las listas peligrosas y
las zonas grises, y lo que decide se aplica al manifiesto y queda escrito.

Purpose: el manifiesto es el backstop de autorizacion horizontal del milestone. Clasificar de
mas como `global` desactiva la proteccion de esa ruta para siempre, y las 12 grises incluyen dos
conflictos reales entre documentos cerrados. Ninguna de esas decisiones es del ejecutor.

Output: manifiesto con la clasificacion aprobada + dossier con el veredicto de cada caso + gate
ISO-01 verde despues de aplicar los cambios.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-CONTEXT.md
@.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-CLASIFICACION.md
@.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-02-SUMMARY.md
@.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-03-SUMMARY.md
@CLAUDE.md

**Worktree:** `/home/franco/projects/et-170-sentinel`. Este plan NO es autonomo: la Task 1 es un
checkpoint bloqueante y nada se aplica sin la respuesta de Franco.

**Por que el checkpoint va ultimo y no antes:** asi llega con el gate ya funcionando (plan 03,
sonda en vivo incluida) y con los fixtures listos (planes 04-05). Franco decide sobre listas
reales y sobre un mecanismo que ya demostro que anda, en vez de sobre una propuesta en el aire.
Recategorizar una ruta no rompe el gate: cambia la etiqueta, no el conteo.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Checkpoint — Franco aprueba la clasificacion de rutas (D-03 / D-04)</name>
  <files>(ninguno)</files>
  <read_first>
    - .planning/phases/171-.../171-CLASIFICACION.md — el dossier completo: es lo que se le presenta.
    - .planning/phases/171-.../171-02-SUMMARY.md — el reparto real por categoria y como se genero.
    - .planning/phases/171-.../171-03-SUMMARY.md — la evidencia de que el gate funciona (rojo de la
      sonda + verde posterior), para poder responder si Franco pregunta que pasa cuando se cae.
  </read_first>
  <what-built>
    El manifiesto `el-templo-api/test/tenant-manifest.ts` con una entrada explicita por cada ruta
    registrada del API (370 segun el volcado de esta rama), clasificadas en `tenant-scoped`,
    `global` (con motivo escrito) y `templo-module` (con su modulo), y el gate
    `test/tenancy/iso-01-manifiesto.test.ts` que deja CI en rojo si aparece una ruta sin
    clasificar, nombrandola. Ademas, los fixtures del gimnasio 2 (ISO-02) ya verdes.
  </what-built>
  <how-to-verify>
    1. Abrir `.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-CLASIFICACION.md`.
    2. **Seccion A — `global`** (lista corta, ~10 rutas): leer ruta por ruta CON su motivo. La
       pregunta es una sola: "¿esta ruta puede ejecutarse sin saber de que gimnasio es, y el motivo
       escrito lo explica?". Marcar la que no.
    3. **Seccion B — `templo-module`** (~141 rutas presentadas como ~20 lineas de modulo x prefijo
       con conteo): revisar FRONTERAS, no rutas. La pregunta: "¿este prefijo es un feature exclusivo
       de El Templo que un gimnasio nuevo podria no tener?". El mapeo carpeta -> modulo del doc 04
       §2.1 ya esta validado; lo que se confirma es que estos prefijos caen ahi.
    4. **Seccion C — dudosas (12 casos)**: una por una, con la recomendacion y su porque. Los dos
       conflictos reales que necesitan veredicto tuyo:
       a. `labs-inquiry` / `labs-inquiries` (3 rutas): el doc 06 §8 Q2 dice que son leads del propio
          SaaS -> `global`; el doc 04 §2.1 mete `app-landing` entero en `templo-marketing`.
       b. `/api/campaigns/track/open`, `/click` y `/unsubscribe` (3 rutas): recomendadas
          `tenant-scoped` porque la decision Q5 hizo la supresion de unsubscribes POR TENANT;
          marcarlas `global` contradiria esa decision.
    5. **Lo que NO se revisa** (D-03, a proposito): la masa `tenant-scoped`. Equivocarse hacia
       tenant-scoped sobra proteccion, no falta.
    6. Recordatorio de la regla que sostiene todo: agrandar la categoria `global` NO es una salida
       valida para que un test pase — cada `global` cuesta un motivo escrito y esta revision.
  </how-to-verify>
  <resume-signal>
    Escribi "aprobado" si va tal cual, o lista los cambios en la forma
    `<ruta o prefijo> -> <categoria>` (mas el motivo, si la mandas a `global`).
  </resume-signal>
  <action>
    Presentar el checkpoint EN EL CHAT, no mandando a leer el archivo entero: pegar la seccion A
    completa (es corta), la seccion B como la tabla de modulo x prefijo x conteo, y las 12 filas de
    la seccion C con su recomendacion. Agregar el reparto final por categoria y el recordatorio de
    que el gate ya esta verde y demostrado (evidencia del 171-03), asi Franco decide sobre la
    clasificacion y no sobre el mecanismo. PARAR ahi: no aplicar nada, no auto-aprobar, no seguir
    con el Task 2 hasta tener la respuesta escrita. Este checkpoint no es auto-aprobable ni con
    `workflow.auto_advance` activo.
  </action>
  <verify>
    <human-check>Franco respondio "aprobado" o listo los cambios ruta/prefijo -> categoria; la respuesta quedo transcripta con fecha en el SUMMARY</human-check>
  </verify>
  <acceptance_criteria>
    - Franco respondio explicitamente sobre la seccion A, la B y los 12 casos de la C.
    - Los dos conflictos de docs (labs_inquiries y campaigns/track+unsubscribe) tienen veredicto.
    - La respuesta quedo transcripta literal en el SUMMARY del plan, con fecha.
  </acceptance_criteria>
  <done>La clasificacion tiene dueño humano y esta registrada.</done>
</task>

<task type="auto">
  <name>Task 2: Aplicar el veredicto al manifiesto y re-correr el gate</name>
  <files>el-templo-api/test/tenant-manifest.ts, .planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-CLASIFICACION.md</files>
  <read_first>
    - La respuesta literal de Franco de la Task 1.
    - el-templo-api/test/tenant-manifest.ts — las entradas afectadas y sus comentarios `D-04 dudosa:`.
    - el-templo-api/test/tenancy/iso-01-manifiesto.test.ts — que gates pueden ponerse rojos al mover
      una categoria (`sinMotivo` si una ruta pasa a `global` sin motivo; `sinModulo` si pasa a
      `templo-module` sin modulo).
  </read_first>
  <action>
    Aplicar cada cambio pedido: cambiar `categoria`, agregar `motivo` (oracion completa que nombra
    la causa, nunca una etiqueta) cuando una ruta pase a `global`, y agregar `modulo` cuando pase a
    `templo-module`. Reemplazar cada comentario `D-04 dudosa:` de los casos resueltos por un
    comentario que registre el veredicto y su fuente (ej.: decision de Franco 2026-XX-XX + el doc
    que la respalda). Si Franco aprobo sin cambios, dejar escrito eso mismo en cada linea en vez de
    borrar el comentario: la duda existio y su resolucion es la informacion.

    Actualizar `171-CLASIFICACION.md`: agregar a la seccion C una columna o bloque `Veredicto` con
    la decision y la fecha, y una nota de cierre con el reparto final por categoria.

    Re-correr el gate ISO-01 completo. Si algo se puso rojo, arreglar el manifiesto — nunca el test.

    NO tocar `.planning/REQUIREMENTS.md` ni `.planning/ROADMAP.md`: el marcado de ISO-01/ISO-02 como
    completos es de `/gsd:verify-phase`.
  </action>
  <acceptance_criteria>
    - `cd el-templo-api && pnpm exec vitest run test/tenancy/iso-01-manifiesto.test.ts --hookTimeout=250000` sale 0.
    - `grep -c "D-04 dudosa:" el-templo-api/test/tenant-manifest.ts` es 0 O todas las ocurrencias
      restantes incluyen la palabra `veredicto` en la misma linea (ninguna duda queda abierta).
    - El total de claves del manifiesto sigue siendo el del baseline (una recategorizacion no agrega ni quita rutas).
    - `171-CLASIFICACION.md` tiene el bloque `Veredicto` con fecha y el reparto final por categoria.
    - `git status --porcelain el-templo-api/` lista unicamente `test/tenant-manifest.ts`.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm exec vitest run test/tenancy/iso-01-manifiesto.test.ts --hookTimeout=250000</automated>
  </verify>
  <done>El manifiesto refleja la decision del dueño del producto y el gate sigue verde.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| decision de producto ↔ codigo | El checkpoint es el unico punto donde una categoria se vuelve decision consciente y no default de un agente |
| documentos cerrados ↔ manifiesto | Dos decisiones cerradas (Q2, Q5) se contradicen con el mapeo del doc 04 en casos concretos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-171-20 | Elevation of Privilege | Ruta gris marcada `global` sin dueño humano | mitigate | Checkpoint bloqueante D-03/D-04: el ejecutor no puede auto-aprobarlo (`workflow.auto_advance` no aplica) |
| T-171-21 | Repudiation | Veredicto sin registro: dentro de un año nadie sabe por que | mitigate | Transcripcion literal en el SUMMARY + bloque `Veredicto` con fecha y fuente en el dossier |
| T-171-22 | Information Disclosure | Recategorizar y romper el gate sin darse cuenta | mitigate | Re-corrida del gate ISO-01 como criterio de aceptacion; `sinMotivo`/`sinModulo` atrapan la aplicacion incompleta |
| T-171-23 | Tampering | Arreglar el test en vez del manifiesto cuando algo se pone rojo | mitigate | Escrito como prohibicion explicita en el `<action>`; el criterio de aceptacion mide `git status` limitado al manifiesto |

</threat_model>

<verification>
- Respuesta de Franco transcripta con fecha en el SUMMARY
- `pnpm exec vitest run test/tenancy/iso-01-manifiesto.test.ts --hookTimeout=250000` sale 0 despues de aplicar
- Cero dudas abiertas (`D-04 dudosa:` resuelto o con veredicto en la misma linea)
- Total de claves del manifiesto sin cambios
- Cero migraciones, cero deps nuevas
</verification>

<success_criteria>
- D-03 cumplido: Franco reviso `global` y `templo-module` (agrupada), no la masa tenant-scoped
- D-04 cumplido: las 12 dudosas se decidieron una por una, con los 2 conflictos de docs resueltos
- D-07 cerrado: el etiquetado `templo-module` queda listo para que la 176 solo agregue enforcement
- El gate ISO-01 sigue verde con la clasificacion aprobada
</success_criteria>

<output>
Create `.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-06-SUMMARY.md` when done
</output>
