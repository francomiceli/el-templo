---
phase: 171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/app.ts
  - el-templo-api/test/helpers.ts
  - el-templo-api/test/tenant-manifest.ts
autonomous: true
requirements: [ISO-01]

must_haves:
  truths:
    - "`buildApp()` acepta un seam opcional test-only `onRoute` y lo cuelga ANTES del primer register, asi que el hook ve las 370 rutas (RESEARCH Pattern 1 / Pitfall 1)"
    - "En produccion (`src/index.ts`) el seam es exactamente `undefined`: no se agrega ningun hook y no cambia una sola linea de ese archivo"
    - "`createTestApp(opts)` reenvia las opciones a `buildApp(opts)` pero NO cuelga el hook (colgarlo ahi veria 0 rutas)"
    - "Existe el modulo `test/tenant-manifest.ts` con los 3 nombres de categoria, el motivo obligatorio para `global` (D-02) y el modulo obligatorio para `templo-module` (D-07) expresados en el tipo"
    - "`compararManifiesto` es una funcion PURA exportada con el manifiesto inyectable por parametro: es lo que habilita demostrar el criterio 2 con fixtures sinteticos"
    - "La clave de ruta esta normalizada en un solo lugar: metodo expandido (array) y HEAD separado con guard, nunca filtrado en silencio"
  artifacts:
    - path: "el-templo-api/src/app.ts"
      provides: "BuildAppOptions.onRoute — seam test-only del inventario de rutas"
      contains: "BuildAppOptions"
    - path: "el-templo-api/test/helpers.ts"
      provides: "createTestApp(opts) que reenvia el seam"
      contains: "BuildAppOptions"
    - path: "el-templo-api/test/tenant-manifest.ts"
      provides: "Tipos del manifiesto + compararManifiesto + particionarObservadas (manifiesto todavia vacio)"
      contains: "compararManifiesto"
  key_links:
    - from: "el-templo-api/test/helpers.ts"
      to: "el-templo-api/src/app.ts"
      via: "createTestApp reenvia BuildAppOptions a buildApp"
      pattern: "buildApp\\(opts"
    - from: "el-templo-api/test/tenant-manifest.ts"
      to: "el-templo-api/test/tenancy/iso-01-manifiesto.test.ts"
      via: "el gate del plan 03 importa compararManifiesto y particionarObservadas"
      pattern: "compararManifiesto"
---

<objective>
Poner las dos piezas estructurales de ISO-01 antes de escribir una sola de las 370 lineas
del manifiesto: (a) el **seam** de una linea en `buildApp()` que permite observar las rutas
registradas, y (b) el **modulo del manifiesto** con sus tipos y su comparador puro, con el
registro `TENANT_MANIFEST` todavia VACIO.

Purpose: el orden importa. El volcado del baseline (plan 02) sale mucho mas barato usando el
seam ya commiteado que parcheando la cache de require, y el comparador tiene que existir
antes que las entradas para que las 370 se escriban contra un contrato ya definido (categoria,
motivo, modulo) y no al reves.

Output: `BuildAppOptions` en `src/app.ts`, `createTestApp(opts)` en `test/helpers.ts` y
`test/tenant-manifest.ts` con tipos + `compararManifiesto` + `particionarObservadas`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-CONTEXT.md
@.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-RESEARCH.md
@.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-PATTERNS.md
@CLAUDE.md
@.claude/skills/el-templo-change-control/SKILL.md

**Worktree de toda la fase 171:** `/home/franco/projects/et-170-sentinel`, rama
`feat/170-sentinel-lint`. NO tocar el checkout principal `/home/franco/projects/el-templo`
(compartido, rama ajena). **Precondicion dura antes del primer edit:** `git status --porcelain`
del worktree tiene que estar limpio de codigo del API; si quedaron cambios sin commitear de la
gap-closure de la fase 170 (planes 170-09/170-10), PARAR y avisar en vez de mezclarlos.

**Reglas operativas de la fase (todas heredadas y verificadas en el RESEARCH):**

- Cero migraciones. Si alguna apareciera —no deberia— reservar desde **0197**.
- Cero dependencias nuevas: `pnpm-lock.yaml` no se toca.
- `git add` por ruta explicita, nunca `git add -A` ni `git add .`.
- Nada de correr la suite completa local: solo los archivos del plan. Un archivo de vitest por
  vez (`--hookTimeout=250000`), sin tocar `vitest.config.ts`.
- Nada de pushear.

<interfaces>

Contratos existentes que este plan extiende (NO redefinir):

De `el-templo-api/src/app.ts`:
export async function buildApp(): Promise<FastifyInstance>; // hoy SIN parametros, 2 call sites

De `el-templo-api/test/helpers.ts`:
export async function createTestApp(): Promise<FastifyInstance>; // hoy llama buildApp()

De `fastify` 5.7.4:
import type { RouteOptions } from "fastify";
// el hook `onRoute` recibe RouteOptions; `method` puede ser string o string[]

Contratos que este plan AGREGA y que consumen los planes 02, 03 y 06:
export interface BuildAppOptions { onRoute?: (route: RouteOptions) => void }
export type Categoria = "tenant-scoped" | "global" | "templo-module";
export type ModuloTemplo = "templo-training" | "templo-gamification" | "templo-marketing" | "templo-onboarding";
export interface EntradaManifiesto { categoria: Categoria; motivo?: string; modulo?: ModuloTemplo }
export const TENANT_MANIFEST: Record<string, EntradaManifiesto>;
export interface Discrepancias { faltantes: string[]; fantasmas: string[]; sinMotivo: string[]; sinModulo: string[]; categoriaInvalida: string[] }
export function compararManifiesto(observadas: readonly string[], manifiesto?: Readonly<Record<string, EntradaManifiesto>>): Discrepancias;
export function particionarObservadas(claves: readonly string[]): { rutas: string[]; headHuerfanos: string[] };
export function clavesDeEvento(metodo: string | readonly string[], url: string): string[];

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seam `onRoute` en buildApp + reenvio desde createTestApp</name>
  <files>el-templo-api/src/app.ts, el-templo-api/test/helpers.ts</files>
  <read_first>
    - el-templo-api/src/app.ts lineas 1-95 — la firma actual de `buildApp`, el `Fastify({...})`
      y el primer `addContentTypeParser`/`await app.register(cors, ...)`: el seam va ANTES de
      los dos.
    - el-templo-api/test/helpers.ts lineas 1-30 — `createTestApp()` y su docblock actual.
    - el-templo-api/src/index.ts alrededor de la linea 26 — el otro call site de `buildApp`,
      que este plan NO modifica.
    - 171-RESEARCH.md §"Pattern 1" y §"Pitfall 1" — la evidencia ejecutada de por que el hook
      no puede colgarse desde `createTestApp()` (`FST_ERR_INSTANCE_ALREADY_LISTENING`, y un
      hook tardio ve 0 rutas previas).
    - 171-PATTERNS.md §`src/app.ts` — el tono del docblock que defiende una decision de
      ubicacion contra un refactor futuro (precedente `src/modules/shared/tenant.ts:39-47`).
  </read_first>
  <action>
    En `src/app.ts`: importar `RouteOptions` como type desde `fastify`, exportar la interfaz
    `BuildAppOptions` con el unico campo `onRoute?: (route: RouteOptions) => void`, cambiar la
    firma a `buildApp(opts: BuildAppOptions = {})` y agregar como PRIMER statement despues del
    `const app = Fastify({...})` la unica linea funcional del task:
    `if (opts.onRoute) app.addHook("onRoute", opts.onRoute);`.

    El docblock de `BuildAppOptions` (estilo del repo: secciones en MAYUSCULAS) tiene que
    decir tres cosas, porque son las que impiden que alguien lo "limpie" en seis meses:
    (1) es TEST-ONLY y `src/index.ts` no lo pasa, o sea que en produccion es `undefined` y no
    se agrega ningun hook; (2) por que vive aca y no en `createTestApp()`: un hook `onRoute`
    solo ve rutas registradas DESPUES de colgarse y para cuando `buildApp()` retorna ya se
    registraron las 370 (cada `await app.register(...)` ejecuta el plugin en el acto), y
    colgarlo despues de `ready()` tira `FST_ERR_INSTANCE_ALREADY_LISTENING`; (3) que lo
    consume el gate ISO-01 de la fase 171 (`test/tenancy/iso-01-manifiesto.test.ts`).

    En `test/helpers.ts`: `createTestApp(opts: BuildAppOptions = {})` importando el tipo desde
    `../src/app` y reenviando `buildApp(opts)`. Sumar al docblock la linea que aclara que aca
    NO se cuelga ningun hook: solo se reenvia (colgarlo aca veria 0 rutas).

    NO tocar `src/index.ts`. NO agregar ningun otro campo a `BuildAppOptions` "por las dudas":
    la superficie minima es parte de la decision (se descarto `plugins: FastifyPluginCallback[]`
    por ceremoniosa — RESEARCH open question 1).
  </action>
  <acceptance_criteria>
    - `cd el-templo-api && pnpm exec tsc --noEmit` sale 0.
    - `git diff --numstat el-templo-api/src/index.ts` no imprime nada (0 lineas cambiadas).
    - `grep -c "opts.onRoute" el-templo-api/src/app.ts` devuelve 1 (una sola linea funcional).
    - `grep -n "addHook(\"onRoute\"" el-templo-api/src/app.ts` aparece ANTES de la primera
      linea que matchea `addContentTypeParser` (comparar numeros de linea).
    - `grep -c "buildApp(opts)" el-templo-api/test/helpers.ts` devuelve 1.
    - `git status --porcelain el-templo-api/pnpm-lock.yaml` vacio.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>`buildApp({ onRoute })` compila, produccion queda intacta y `createTestApp` puede pasar el seam.</done>
</task>

<task type="auto">
  <name>Task 2: Modulo `test/tenant-manifest.ts` — tipos, comparador puro y particion de HEAD</name>
  <files>el-templo-api/test/tenant-manifest.ts</files>
  <read_first>
    - el-templo-api/src/db/tenant-tables.ts lineas 1-60 y 181-250 — el analog EXACTO: docblock
      de registro canonico con secciones en MAYUSCULAS, `Record<clave, motivo>` con el motivo
      como oracion completa, y el `Set` derivado a nivel de modulo.
    - 171-PATTERNS.md §`test/tenant-manifest.ts` — que copiar y que no.
    - 171-RESEARCH.md §"Pattern 2" (clave de ruta, method array, los 7 HEAD fantasma con barra
      final, el anti-patron de normalizar la barra final de todas las urls) y §"Pattern 3"
      (por que el comparador es funcion pura con manifiesto inyectable).
    - 171-CONTEXT.md — D-01 (sin comodines), D-02 (motivo obligatorio en `global`), D-07
      (`templo-module` etiquetado ya, con su modulo).
  </read_first>
  <action>
    Crear `test/tenant-manifest.ts` **sin importar nada** (TS puro: eso permite typechequearlo
    con `tsc` standalone, que es la unica red que tiene — `tsconfig.json` incluye solo `src/**`,
    asi que CI no typechequea `test/`).

    Contenido, en este orden:

    1. Docblock de cabecera con secciones en MAYUSCULAS: `POR QUE EXISTE ESTE ARCHIVO`,
       `DE DONDE SALE LA LISTA` (volcado one-shot del hook `onRoute` sobre `buildApp()`, D-16
       de la fase 170: NO se commitea ningun regenerador), `POR QUE NO HAY COMODINES` (D-01:
       una regla por prefijo clasificaria rutas futuras sin que nadie lo piense y vaciaria el
       criterio 2 del ROADMAP), `POR QUE EL MOTIVO ES OBLIGATORIO EN global` (D-02),
       `POR QUE templo-module SE ETIQUETA HOY` (D-07: el enforcement `requireModule` es de la
       fase 176 y va a leer ESTA etiqueta), `QUE NO ES ESTE ARCHIVO` (no es runtime de
       produccion; vive en `test/` a proposito y por eso el lint de tenancy de la 170 no lo mira).
    2. `Categoria`, `ModuloTemplo`, `EntradaManifiesto` con los comentarios que atan
       `motivo` a D-02 y `modulo` a D-07. Sin `any` en ningun lado.
    3. `export const TENANT_MANIFEST: Record<string, EntradaManifiesto> = {};` con un comentario
       que diga explicitamente que arranca VACIO y que lo puebla el plan 171-02 (precedente:
       `JOBS_EXENTOS` de con-04 existe vacio para que la unica forma de eximir sea escribir).
    4. `clavesDeEvento(metodo, url)`: acepta `string | readonly string[]` y devuelve una clave
       `` `${METODO} ${url}` `` por metodo (Fastify dispara UN evento con `method: ["POST","PUT"]`).
    5. `particionarObservadas(claves)`: separa HEAD del resto y devuelve
       `{ rutas, headHuerfanos }`. `headHuerfanos` son los HEAD cuya url no tiene un `GET`
       hermano ni con barra final ni sin ella. Comentario obligatorio explicando que el filtro
       NO es cosmetico: filtrar HEAD a secas dejaria un HEAD declarado a mano fuera del backstop
       en silencio, y que NO se normaliza la barra final de todas las urls porque
       `ignoreTrailingSlash` esta en `false` y colapsarlas esconderia una ruta real.
    6. `compararManifiesto(observadas, manifiesto = TENANT_MANIFEST)` -> `Discrepancias` con
       las 5 listas, todas `.sort()`-eadas y sin duplicados: `faltantes` (observadas sin entrada),
       `fantasmas` (entradas sin ruta observada), `sinMotivo` (categoria `global` con `motivo`
       ausente, vacio tras `trim()` o con marcador de trabajo pendiente TODO/FIXME/TBD/XXX/pendiente),
       `sinModulo` (categoria `templo-module` sin `modulo` valido), `categoriaInvalida`
       (valor de `categoria` fuera de las 3). El segundo parametro con default es lo que
       habilita el criterio 2 con fixtures sinteticos: no borrarlo ni volverlo obligatorio.
  </action>
  <acceptance_criteria>
    - `cd el-templo-api && pnpm exec tsc --noEmit --strict --skipLibCheck test/tenant-manifest.ts` sale 0.
    - `grep -c "^import\|require(" el-templo-api/test/tenant-manifest.ts` devuelve 0 (modulo sin dependencias).
    - `grep -c ": any" el-templo-api/test/tenant-manifest.ts` devuelve 0.
    - `grep -c "export function compararManifiesto\|export function particionarObservadas\|export function clavesDeEvento" el-templo-api/test/tenant-manifest.ts` devuelve 3.
    - `grep -v '^\s*[*/]' el-templo-api/test/tenant-manifest.ts | grep -c "TENANT_MANIFEST"` es >= 2 (declaracion + default del comparador).
    - El docblock nombra literalmente `D-01`, `D-02`, `D-07` y `criterio 2`.
    - La prueba de COMPORTAMIENTO del comparador la agrega el plan 171-03 (`test/tenancy/iso-01-manifiesto.test.ts`): en este plan no existe todavia ningun test que lo ejecute.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit --strict --skipLibCheck test/tenant-manifest.ts</automated>
  </verify>
  <done>El contrato del manifiesto existe y compila; las 370 entradas del plan 02 tienen donde entrar.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| produccion ↔ infraestructura de test | Un seam agregado a `src/app.ts` corre en el mismo binario que sirve a los socios: tiene que ser inerte cuando nadie lo pasa |
| ruta nueva ↔ decision de clasificacion | El manifiesto es el unico lugar donde se declara si una ruta ve datos de un gimnasio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-171-01 | Tampering | Seam `onRoute` usado en produccion | mitigate | `opts.onRoute` opcional y `src/index.ts` intacto (0 lineas cambiadas, verificado por `git diff --numstat`); sin el parametro no se agrega ningun hook |
| T-171-02 | Denial of Service | Hook `onRoute` que agregue latencia al boot | accept | El hook solo corre durante el registro y solo cuando un test lo pasa; produccion nunca lo instancia |
| T-171-03 | Elevation of Privilege | Comodin/prefijo que clasifique rutas futuras sin decision humana | mitigate | D-01 escrito en el docblock y ningun mecanismo de matching por prefijo en el modulo: `Record` de clave exacta |
| T-171-SC | Tampering | Instalacion de paquetes | mitigate | Cero deps nuevas; criterio de aceptacion sobre `pnpm-lock.yaml` sin cambios |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit` sale 0
- `pnpm exec tsc --noEmit --strict --skipLibCheck test/tenant-manifest.ts` sale 0
- `git diff --numstat el-templo-api/src/index.ts` vacio
- `git status --porcelain el-templo-api/pnpm-lock.yaml` vacio
- Cero archivos nuevos bajo `el-templo-api/src/db/migrations/`
</verification>

<success_criteria>
- El seam existe, es de una linea, esta antes del primer register y es inerte en produccion
- `createTestApp(opts)` reenvia sin colgar nada
- El modulo del manifiesto existe con los 3 contratos exportados y el manifiesto vacio a proposito
- Cero deps nuevas, cero migraciones, `src/index.ts` intacto
</success_criteria>

<output>
Create `.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-01-SUMMARY.md` when done
</output>
