---
phase: 171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant
plan: 04
type: execute
wave: 4
depends_on: [171-01]
files_modified:
  - el-templo-api/test/helpers.ts
  - el-templo-api/test/fixtures/second-tenant.ts
autonomous: true
requirements: [ISO-02]

must_haves:
  truths:
    - "`createStaffUser` y `createTestMember` aceptan `tenantId` como parametro, con default 1: los ~215 archivos que ya los llaman no cambian ni una linea (criterio 3 del ROADMAP)"
    - "Existe `seedSecondTenant(app)` opt-in por archivo que siembra el espejo minimo de D-06 con `tenantValues` en TODOS los inserts"
    - "El socio del gimnasio 2 se crea por INSERT directo, no por `POST /api/auth/register`: esa ruta no conoce el tenant y el DEFAULT 1 se lo comeria"
    - "Los emails del gimnasio 2 son distintos de los del 1: el login resuelve por email SIN filtro de tenant"
    - "`limpiarSegundoGimnasio` borra el rastro en orden de FK (hijas -> users -> branches -> tenants) con `WHERE tenant_id = ...` en cada DELETE"
    - "`TABLES_TO_CLEAN` no se toca: meter `branches` ahi romperia los 165 archivos que dependen de la sede semilla"
  artifacts:
    - path: "el-templo-api/test/helpers.ts"
      provides: "createStaffUser/createTestMember con tenantId opcional (default 1) via tenantValues"
      contains: "tenantValues"
    - path: "el-templo-api/test/fixtures/second-tenant.ts"
      provides: "seedSecondTenant + limpiarSegundoGimnasio + TENANT_DOS"
      contains: "seedSecondTenant"
      min_lines: 150
  key_links:
    - from: "el-templo-api/test/fixtures/second-tenant.ts"
      to: "el-templo-api/src/modules/shared/tenant.ts"
      via: "tenantValues(CTX, {...}) en todos los INSERT"
      pattern: "tenantValues\\("
    - from: "el-templo-api/test/fixtures/second-tenant.ts"
      to: "el-templo-api/test/helpers.ts"
      via: "createStaffUser({ ..., tenantId }) y getAuthToken para los tokens"
      pattern: "createStaffUser"
---

<objective>
Dar de alta la infraestructura de ISO-02: los helpers existentes aceptan el gimnasio como
parametro y aparece un fixture opt-in que siembra el segundo gimnasio completo (espejo minimo
de D-06) y lo limpia sin dejar rastro.

Purpose: las fases 172-175 necesitan poder ejecutar una ruta como staff del gimnasio A y
comprobar que no ve al B. Hoy eso no se puede: `createStaffUser` no expone el tenant y el unico
precedente (169-06) lo resolvia con un `UPDATE users SET tenant_id` a mano despues del insert.

Output: `test/helpers.ts` con `tenantId?: number` retrocompatible y `test/fixtures/second-tenant.ts`
con `seedSecondTenant` / `limpiarSegundoGimnasio` / `TENANT_DOS`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-CONTEXT.md
@.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-RESEARCH.md
@.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-PATTERNS.md
@CLAUDE.md

**Worktree:** `/home/franco/projects/et-170-sentinel` (rama `feat/170-sentinel-lint`). Cero
migraciones, cero deps nuevas, `git add` por ruta, nada de pushear. Un archivo de vitest por vez.

<interfaces>

De `el-templo-api/src/modules/shared/tenant.ts` (fase 169, NO redefinir):
export interface TenantContext { tenantId: number }
export function tenantValues<V extends Record<string, unknown>>(scope: { tenantId: number }, values: V): V & { tenantId: number };
export function tenantWhere(table, scope): SQL;

De `el-templo-api/test/helpers.ts` (firmas actuales que este plan EXTIENDE de forma aditiva):
export async function createStaffUser(app, data: { email; password; firstName; lastName; role; branchId; country? }): Promise<number>;
export async function createTestMember(app, overrides?: Record<string, unknown>): Promise<{ id; token; email; ... }>;
export async function getAuthToken(app, email, password): Promise<string>;
export async function cleanAllTestData(app): Promise<void>;   // sin filtro de tenant, vacia ~90 tablas

Columnas notNull que el fixture tiene que llenar (leidas del schema real):
  schema.subscriptionPlans: name, planTier ("flex"), bookingMode ("flexible"), planCategory ("presencial"), priceRegular, priceZero, durationDays
  schema.schedules: branchId, activityId (FK a activities), dayOfWeek (1..6), startTime "HH:MM", endTime "HH:MM"
  schema.activities: name        // es gym-owned: el schedule del gimnasio 2 NO puede colgar de una actividad del gimnasio 1
  schema.branches: name, code (<=20, unique compuesta con tenant desde la 168), country, isVirtual, isActive
  schema.tenants: id, name, slug, status

</interfaces>

<trampas_verificadas>

Las cinco que ya mordieron en la fase 169 y que este plan tiene que absorber:

1. `POST /api/auth/register` NO conoce el tenant (cero menciones de tenant en
   `src/modules/auth/routes.ts`): un socio del gimnasio 2 creado por la API cae en el DEFAULT 1.
2. `POST /api/auth/login` resuelve con `eq(users.email, email)` SIN filtro de tenant, y
   `register` rechaza email duplicado globalmente: los emails del gimnasio 2 tienen que ser
   distintos aunque la unique ya sea compuesta.
3. `cleanAllTestData` vacia ~90 tablas sin filtro de tenant y 165 archivos lo llaman en
   `beforeEach`: `seedSecondTenant` va SIEMPRE despues y tiene que ser idempotente.
4. `branches` NO esta en `TABLES_TO_CLEAN`: la sede del gimnasio 2 sobrevive entre archivos del
   mismo worker (`isolate: false`) y la fila de `tenants` no se puede borrar mientras una sede la
   referencie (`fk_branches_tenant`).
5. `aura_config.source_type` es unique GLOBAL: el espejo NO siembra `aura_config`.

Y una del sentinel: los DELETE del fixture llevan `WHERE tenant_id = ...` desde ahora, porque
cuando `finance` entre a `TENANT_STRICT_MODULES` en la 172 un DELETE crudo sin la columna va a
hacer throw.

</trampas_verificadas>
</context>

<tasks>

<task type="auto">
  <name>Task 1: `tenantId` opcional (default 1) en createStaffUser y createTestMember</name>
  <files>el-templo-api/test/helpers.ts</files>
  <read_first>
    - el-templo-api/test/helpers.ts lineas 425-560 — `createStaffUser` COMPLETO (los DOS inserts:
      `users` y `userBranches` para coach/recepcion), `createEligibleFreemium` (el precedente de
      INSERT directo cuando la ruta publica no sirve) y `createTestMember` con su delegacion a
      `registerUser`.
    - el-templo-api/src/modules/shared/tenant.ts — la firma exacta de `tenantValues` y por que el
      tenant va DESPUES del spread.
    - 171-RESEARCH.md §"Pattern 5" — la tabla de opciones para `createTestMember` (A recomendada)
      y el porque del default 1.
    - 171-PATTERNS.md §`test/helpers.ts` — el molde del docblock que explica la retrocompatibilidad
      (precedente Phase 110 `country?`).
  </read_first>
  <action>
    `createStaffUser`: sumar `tenantId?: number` al objeto `data` y envolver los DOS `.values({...})`
    —el de `schema.users` y el de `schema.userBranches`— en `tenantValues({ tenantId: data.tenantId ?? 1 }, {...})`.
    Nada mas cambia: la derivacion de `country` de la Phase 110 queda intacta.

    `createTestMember`: aceptar `tenantId` dentro de `overrides`. Camino A del RESEARCH:
    - `tenantId` ausente o 1 -> camino ACTUAL por `registerUser` (`POST /api/auth/register`), byte
      por byte igual que hoy. Es lo que preserva los ~215 archivos existentes.
    - `tenantId` distinto de 1 -> INSERT directo en `schema.users` con `tenantValues`
      (password hasheada con `argon2`, `role: "member"`, `status` equivalente al que deja register)
      + token con `getAuthToken`. Devuelve la misma forma `{ id, token, email, ... }`.

    Agregar a los dos docblocks un parrafo `Fase 171 (ISO-02)` con el tono del precedente Phase 110:
    que el default 1 = El Templo, que los call sites previos no cambian, y —en `createTestMember`—
    POR QUE el tenant 2 no puede ir por la API (register no conoce el tenant hasta la fase 175, ADO-06)
    y que el socio del tenant 2 por eso no tiene los efectos colaterales de register (codigo de
    referido, etc.), que D-06 no pide.

    Importar `tenantValues` por path directo desde `../src/modules/shared/tenant` (ese archivo NO
    esta en el barrel a proposito). Cero `any`.
  </action>
  <acceptance_criteria>
    - `grep -c "tenantValues(" el-templo-api/test/helpers.ts` devuelve >= 3 (users, userBranches, socio del tenant 2), sin contar el import.
    - `grep -c "tenantId?: number" el-templo-api/test/helpers.ts` devuelve >= 1.
    - `cd el-templo-api && pnpm exec vitest run test/tenancy/tenant-helpers.test.ts --hookTimeout=250000` sale 0 (13 tests, retrocompat de los helpers contra MySQL real).
    - `git diff el-templo-api/test/helpers.ts` no borra ninguna linea de la derivacion de `country` de la Phase 110 ni del insert de `userBranches`.
    - Ningun archivo de test existente fue modificado en este task: `git status --porcelain el-templo-api/test/` solo lista `helpers.ts`.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm exec vitest run test/tenancy/tenant-helpers.test.ts --hookTimeout=250000</automated>
  </verify>
  <done>Los helpers aceptan el gimnasio como parametro sin que ningun call site previo cambie.</done>
</task>

<task type="auto">
  <name>Task 2: `test/fixtures/second-tenant.ts` — espejo minimo D-06 opt-in + limpieza</name>
  <files>el-templo-api/test/fixtures/second-tenant.ts</files>
  <read_first>
    - el-templo-api/test/tv/tv-pairing-tenant.test.ts lineas 24-45, 58-115 y 170-235 — el analog
      exacto: constantes de gimnasio con el porque del id, `seedBranch` con `tenantId` explicito,
      `limpiarRastro` con el orden de FKs y el uso incondicional en `beforeAll`/`afterAll`.
    - el-templo-api/test/tenancy/tenant-helpers.test.ts lineas 154-240 — `contarTenant` (la forma
      incomoda que devuelve `app.db.execute` ya normalizada) y el patron de limpieza defensiva.
    - el-templo-api/test/helpers.ts 290-330 — `ensureEfectivoCaja` como molde de helper idempotente.
    - 171-RESEARCH.md §"Pattern 4" y §"Pitfall 7/8/9/10/11".
    - `vitest.config.ts` — confirmar que `include` es `test/**/*.test.ts`, o sea que este archivo
      es un MODULO y no un archivo de test.
  </read_first>
  <action>
    Crear `test/fixtures/second-tenant.ts` con docblock de cabecera en castellano y secciones en
    MAYUSCULAS (`POR QUE EXISTE ESTE ARCHIVO`, `COMO SE USA` con el ejemplo de `beforeEach`
    despues de `cleanAllTestData`, `QUE SIEMBRA` con el espejo de D-06, `QUE NO SIEMBRA` —
    `aura_config` por la unique global y `cash_registers` porque D-06 no lo pide: si una bateria
    de finance lo necesita, que llame `ensureEfectivoCaja`).

    Contenido:
    - `export const TENANT_DOS = 90671;` con el parrafo que justifica el id y lista los ids ya
      tomados. **Re-grepear `test/` antes de fijarlo**: al 2026-07-29 estan tomados 90168, 90169,
      90269, 90369, 90418, 90469, 90569 y 90940. Si 90671 aparece, elegir otro y actualizar el parrafo.
    - `const CTX: TenantContext = { tenantId: TENANT_DOS };` y un sufijo unico para emails/codigos
      (mismo generador que ya usa `createEligibleFreemium`).
    - `export interface SegundoGimnasio` con `tenantId`, `branchId`, `activityId`, `adminId`/`adminToken`,
      `coachId`/`coachToken`, `socios` (2, cada uno con `id`, `email` y `token`), `planId`, `scheduleId`.
    - `export async function seedSecondTenant(app): Promise<SegundoGimnasio>`: arranca llamando a
      `limpiarSegundoGimnasio` (idempotente, defensivo contra una corrida abortada), inserta la fila
      de `tenants` (id `TENANT_DOS`, name "Gimnasio de prueba 171", slug `test-171-<TENANT_DOS>`,
      status `active`), y despues, TODO con `tenantValues(CTX, ...)`: 1 sede, 1 actividad, 1 plan,
      1 schedule (colgado de la sede Y de la actividad del gimnasio 2, nunca de las del 1).
      El admin y el coach salen de `createStaffUser({ ..., tenantId: TENANT_DOS })`; los 2 socios de
      `createTestMember({ tenantId: TENANT_DOS, branchId })`. Los tokens, con `getAuthToken`
      (el login es tenant-agnostico hoy). Todos los emails llevan sufijo propio del gimnasio 2.
      **Prohibido** el workaround del 169-06 (`UPDATE users SET tenant_id` despues del insert): eso
      es justamente lo que el Task 1 vino a eliminar.
    - `export async function limpiarSegundoGimnasio(app): Promise<void>`: DELETEs en orden de FK
      —schedules, subscription_plans, activities, user_branches, users, branches y por ultimo
      tenants— cada uno con `WHERE tenant_id = ${TENANT_DOS}` (el de `tenants`, con `id`). Comentario
      obligatorio explicando por que la limpieza es local y por que NO se agrega `branches` a
      `TABLES_TO_CLEAN` (romperia los 165 archivos que dependen de la sede semilla).

    Cero `console.log`, cero `any`. El fixture NO se enchufa a `test/setup.ts`: la siembra es opt-in
    por archivo (D-05) y eso es lo que hace que el criterio 4 se cumpla por construccion.
  </action>
  <acceptance_criteria>
    - `grep -rc "90671" el-templo-api/test/ --include=*.ts | grep -v ":0" ` lista solo `test/fixtures/second-tenant.ts` (id libre y usado en un solo lugar).
    - `grep -c "tenantValues(" el-templo-api/test/fixtures/second-tenant.ts` es >= 4 (sede, actividad, plan, schedule), sin contar el import.
    - `grep -c "UPDATE users SET tenant_id" el-templo-api/test/fixtures/second-tenant.ts` devuelve 0.
    - Todos los DELETE del archivo llevan `tenant_id`: `grep -c "DELETE FROM" ` es igual a `grep -c "tenant_id = \|id = "` dentro de `limpiarSegundoGimnasio`.
    - `grep -rc "second-tenant" el-templo-api/test/setup.ts` devuelve 0 (D-05: nada en el setup global).
    - `git diff el-templo-api/test/helpers.ts` de este task no toca `TABLES_TO_CLEAN`.
    - La verificacion por COMPORTAMIENTO (que las 7 filas nacen con `tenant_id = 90671` y que la limpieza no deja rastro) es del plan 171-05: en este plan todavia no existe el archivo de test que lo ejecuta.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && grep -c "tenantValues(" test/fixtures/second-tenant.ts && grep -c "second-tenant" test/setup.ts || true</automated>
  </verify>
  <done>El espejo del gimnasio 2 se puede sembrar y limpiar desde cualquier archivo que haga opt-in.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| fixture ↔ base compartida del worker | Un archivo siembra datos que otros archivos del mismo worker van a ver (`isolate: false`) |
| gimnasio 2 ↔ gimnasio 1 | La limpieza del 2 no puede tocar una sola fila del 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-171-12 | Spoofing | Fixture que dice sembrar el gimnasio 2 y escribe en el 1 (DEFAULT 1) | mitigate | `tenantValues(CTX, ...)` en TODO insert + INSERT directo para el socio (register no conoce el tenant); el plan 05 lo afirma por `SELECT tenant_id` sobre la fila real |
| T-171-13 | Denial of Service | Limpieza que borra datos del gimnasio 1 y rompe la suite | mitigate | Todos los DELETE con `WHERE tenant_id = TENANT_DOS`; nunca DELETE sin WHERE; `TABLES_TO_CLEAN` intacta |
| T-171-14 | Tampering | Sede del gimnasio 2 filtrada al archivo siguiente del worker | mitigate | `limpiarSegundoGimnasio` incondicional en `beforeAll` y `afterAll` del archivo que hace opt-in (documentado en `COMO SE USA`) |
| T-171-15 | Spoofing | Token del gimnasio 2 no deterministico por email repetido entre tenants | mitigate | Sufijo propio en los emails del gimnasio 2 (login resuelve por email sin filtro de tenant) |
| T-171-SC | Tampering | Instalacion de paquetes | mitigate | Cero deps nuevas (`argon2`, `drizzle-orm` ya estan) |

</threat_model>

<verification>
- `pnpm exec vitest run test/tenancy/tenant-helpers.test.ts --hookTimeout=250000` sale 0 (retrocompat)
- `test/fixtures/second-tenant.ts` existe, con `tenantValues` en todos los inserts y `tenant_id` en todos los DELETE
- `test/setup.ts` sin ninguna referencia al fixture (D-05)
- `TABLES_TO_CLEAN` sin cambios
- Cero migraciones, cero deps nuevas
</verification>

<success_criteria>
- Criterio 3 del ROADMAP (mitad helpers): `createStaffUser` y afines aceptan el tenant como parametro
- Espejo minimo de D-06 sembrable en una llamada, idempotente y barato
- Siembra opt-in por archivo (D-05): el setup global no cambia
- Cero cambios en archivos de test existentes
</success_criteria>

<output>
Create `.planning/phases/171-backstop-manifiesto-de-rutas-fail-closed-y-fixtures-2-tenant/171-04-SUMMARY.md` when done
</output>
