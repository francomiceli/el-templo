---
phase: 18-technical-debt-audit-domain-subdomain-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/app.ts
  - deploy/.env.production.template
  - .github/SECRETS.md
  - el-templo-admin/.env.example
  - deploy/nginx.conf
autonomous: true

must_haves:
  truths:
    - "All eltemplo.com references in runtime code and config templates are updated to eltemplo.org"
    - "ADMIN_URL env var is included in .env.production.template and deploy pipeline env creation"
    - "CORS fallback origins use correct .org domain"
    - "SECRETS.md documents all required secrets including new ADMIN_DEPLOY_PATH and ADMIN_URL"
  artifacts:
    - path: "el-templo-api/src/app.ts"
      provides: "CORS with correct .org fallback origins"
      contains: "eltemplo.org"
    - path: "deploy/.env.production.template"
      provides: "Production env template with ADMIN_URL"
      contains: "ADMIN_URL"
    - path: ".github/SECRETS.md"
      provides: "Complete secrets documentation"
      contains: "ADMIN_DEPLOY_PATH"
    - path: "el-templo-admin/.env.example"
      provides: "Admin env example with correct .org domain"
      contains: "eltemplo.org"
  key_links:
    - from: "el-templo-api/src/app.ts"
      to: ".env.production.template"
      via: "ADMIN_URL env var"
      pattern: "process\\.env\\.ADMIN_URL"
    - from: ".github/SECRETS.md"
      to: "deploy/.env.production.template"
      via: "documented secrets match template vars"
      pattern: "ADMIN_URL|ADMIN_DEPLOY_PATH"
---

<objective>
Fix the domain mismatch (.com -> .org) across all runtime code and configuration files, add the missing ADMIN_URL to production env config, and update secrets documentation for the new subdomain deployment.

Purpose: Ensures all environment configuration and CORS origins are correct BEFORE the deploy pipeline and Nginx configs are updated (Plan 02) and before manual server setup (Plan 03). Without these fixes, the API would reject CORS requests from the .org subdomains.

Output: Updated app.ts, .env.production.template, SECRETS.md, admin .env.example, and nginx.conf comment.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-technical-debt-audit-domain-subdomain-deployment/18-CONTEXT.md
@.planning/phases/18-technical-debt-audit-domain-subdomain-deployment/18-RESEARCH.md
@el-templo-api/src/app.ts
@deploy/.env.production.template
@.github/SECRETS.md
@el-templo-admin/.env.example
@deploy/nginx.conf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix domain mismatch and add ADMIN_URL to production config</name>
  <files>
    el-templo-api/src/app.ts
    deploy/.env.production.template
    el-templo-admin/.env.example
    deploy/nginx.conf
  </files>
  <action>
Fix all `eltemplo.com` references to `eltemplo.org` in runtime code and configuration templates. These are the files that affect production behavior (NOT seed data email addresses like admin@eltemplo.com which are fine as-is, and NOT planning docs which are historical):

1. **el-templo-api/src/app.ts** (lines 19-20):
   - Change `'https://app.eltemplo.com'` to `'https://app.eltemplo.org'`
   - Change `'https://admin.eltemplo.com'` to `'https://admin.eltemplo.org'`

2. **deploy/.env.production.template** (line 16):
   - Change the comment `FRONTEND_URL=http://YOUR_ELASTIC_IP` to include proper production subdomain example
   - Add ADMIN_URL variable: `ADMIN_URL=https://admin.eltemplo.org`
   - Update FRONTEND_URL example comment to show `https://app.eltemplo.org`
   - Full updated template should include these env vars:
     ```
     # Frontend URLs (for CORS)
     FRONTEND_URL=https://app.eltemplo.org
     ADMIN_URL=https://admin.eltemplo.org
     ```

3. **el-templo-admin/.env.example** (line 8):
   - Change `https://api.eltemplo.com/api` to `https://api.eltemplo.org/api`

4. **deploy/nginx.conf** (line 9):
   - Change the commented `server_name api.eltemplo.com;` to `server_name api.eltemplo.org;`
   - NOTE: This file will be superseded by the new per-subdomain configs in Plan 02, but fix it for consistency while it still exists in the repo.

Do NOT change:
- Seed data email addresses (admin@eltemplo.com, coach1@eltemplo.com, etc.) -- these are test user emails, not domain references
- Planning documents (.planning/) -- these are historical records
  </action>
  <verify>
Run `grep -r 'eltemplo\.com' --include='*.ts' --include='*.template' --include='*.example' --include='*.conf' --include='*.md' . | grep -v '.planning/' | grep -v 'seed.ts' | grep -v 'node_modules'` and confirm only SECRETS.md remains (will be fixed in Task 2).
  </verify>
  <done>
CORS fallback origins in app.ts use .org domain. Production env template includes both FRONTEND_URL and ADMIN_URL with correct .org values. Admin .env.example shows .org domain. Nginx.conf comment shows .org domain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update SECRETS.md with complete subdomain deployment secrets</name>
  <files>
    .github/SECRETS.md
  </files>
  <action>
Rewrite `.github/SECRETS.md` to document ALL secrets needed for the subdomain deployment. The file currently has:
- `.com` domain references in examples (lines 38-39)
- Missing `ADMIN_DEPLOY_PATH` secret
- Missing `ADMIN_URL` secret

Update the file with these changes:

1. **Application URLs section** -- update existing entries and add new ones:
   | Secret | Description | Example |
   |--------|-------------|---------|
   | `VITE_API_URL` | Full API URL for frontend builds | `https://api.eltemplo.org/api` |
   | `FRONTEND_URL` | Member app URL for CORS | `https://app.eltemplo.org` |
   | `ADMIN_URL` | Admin app URL for CORS | `https://admin.eltemplo.org` |

2. **Deployment Paths section** -- add admin deploy path:
   | Secret | Description | Example |
   |--------|-------------|---------|
   | `API_DEPLOY_PATH` | Path on server for API files | `/var/www/el-templo-api` |
   | `APP_DEPLOY_PATH` | Path on server for member app files | `/var/www/member-app` |
   | `ADMIN_DEPLOY_PATH` | Path on server for admin app files | `/var/www/admin-app` |

3. Keep all other sections (Server Access, Database, Authentication, Optional, Setting Up Secrets, Generating Secure Values, Security Notes) intact, just fix any .com references to .org.
  </action>
  <verify>
Run `grep 'eltemplo\.com' .github/SECRETS.md` and confirm zero results. Run `grep 'ADMIN_DEPLOY_PATH\|ADMIN_URL' .github/SECRETS.md` and confirm both appear.
  </verify>
  <done>
SECRETS.md documents all secrets needed for subdomain deployment: ADMIN_DEPLOY_PATH, ADMIN_URL, updated VITE_API_URL and FRONTEND_URL examples with .org domain. No .com references remain.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn 'eltemplo\.com' el-templo-api/src/ deploy/ .github/ el-templo-admin/.env.example | grep -v seed.ts` returns zero results
2. `grep 'ADMIN_URL' deploy/.env.production.template` shows ADMIN_URL line exists
3. `grep 'ADMIN_URL' .github/SECRETS.md` shows ADMIN_URL documented
4. `grep 'ADMIN_DEPLOY_PATH' .github/SECRETS.md` shows ADMIN_DEPLOY_PATH documented
5. `grep 'eltemplo\.org' el-templo-api/src/app.ts` shows .org in CORS fallback origins
</verification>

<success_criteria>
All production-affecting code and config files use eltemplo.org (not .com). ADMIN_URL is present in .env.production.template. SECRETS.md comprehensively documents all secrets including the new admin-related ones.
</success_criteria>

<output>
After completion, create `.planning/phases/18-technical-debt-audit-domain-subdomain-deployment/18-01-SUMMARY.md`
</output>
