---
phase: 09-level-specific-sessions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/sessions/types.ts
  - el-templo-api/src/modules/sessions/pipeline/context.ts
  - el-templo-api/src/modules/sessions/fallback/types.ts
autonomous: true

must_haves:
  truths:
    - "BlockContext carries both memberLevel and levelGroup as separate fields"
    - "createInitialContext accepts memberLevel parameter and includes it in blockId"
    - "ExerciseRequirements includes memberLevel for Tier 0 exact matching"
    - "TraceWhere includes memberLevel for audit trail completeness"
  artifacts:
    - path: "el-templo-api/src/modules/sessions/types.ts"
      provides: "ExerciseLevel type export, memberLevel in DaySession"
      contains: "ExerciseLevel"
    - path: "el-templo-api/src/modules/sessions/pipeline/context.ts"
      provides: "memberLevel field in BlockContext"
      contains: "memberLevel"
    - path: "el-templo-api/src/modules/sessions/fallback/types.ts"
      provides: "memberLevel field in ExerciseRequirements"
      contains: "memberLevel"
  key_links:
    - from: "el-templo-api/src/modules/sessions/pipeline/context.ts"
      to: "el-templo-api/src/modules/sessions/types.ts"
      via: "imports ExerciseLevel type"
      pattern: "import.*ExerciseLevel"
---

<objective>
Add `memberLevel` field to the session generation type system so the pipeline can distinguish between a member's individual level (alfa, delta, sigma, omega) and their level group (alfa_delta, sigma, omega).

Purpose: Foundation for level-specific exercise selection. Every downstream plan needs memberLevel in BlockContext and types.
Output: Updated type definitions with memberLevel threading, no behavioral changes yet.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-level-specific-sessions/09-CONTEXT.md
@.planning/phases/09-level-specific-sessions/09-RESEARCH.md
@el-templo-api/src/modules/sessions/types.ts
@el-templo-api/src/modules/sessions/pipeline/context.ts
@el-templo-api/src/modules/sessions/fallback/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ExerciseLevel to session types and memberLevel to DaySession</name>
  <files>el-templo-api/src/modules/sessions/types.ts</files>
  <action>
1. Export `ExerciseLevel` type (reuse definition from fallback/types.ts or define here and re-export there):
   ```typescript
   export type ExerciseLevel = 'alfa' | 'delta' | 'sigma' | 'omega' | 'spartan';
   ```

2. Add `memberLevel` to `TraceWhere` interface (after `levelGroup`):
   ```typescript
   readonly memberLevel?: ExerciseLevel;
   ```
   Make it optional so existing trace events without memberLevel remain valid.

3. Add `memberLevel` to `DaySession` interface (after `levelGroup`):
   ```typescript
   readonly memberLevel: ExerciseLevel;
   ```
   This is required (not optional) because new sessions always have a member level.

Do NOT remove `levelGroup` from any interface — it's still needed for route lookup.
  </action>
  <verify>TypeScript compiles: `cd el-templo-api && npx tsc --noEmit 2>&1 | head -20`. Expect errors in files that create DaySession without memberLevel (service.ts, routes.ts) — this is expected and will be fixed in Plan 09-03.</verify>
  <done>ExerciseLevel exported from types.ts, memberLevel present in TraceWhere and DaySession interfaces</done>
</task>

<task type="auto">
  <name>Task 2: Add memberLevel to BlockContext and createInitialContext</name>
  <files>el-templo-api/src/modules/sessions/pipeline/context.ts</files>
  <action>
1. Import `ExerciseLevel` from the parent types:
   ```typescript
   import type { LevelGroup, BlockRole, TraceEvent, ..., ExerciseLevel } from '../types';
   ```

2. Add `memberLevel` to `BlockContext` interface (after `levelGroup`):
   ```typescript
   readonly memberLevel: ExerciseLevel;
   ```

3. Update `createInitialContext` function:
   - Add `memberLevel: ExerciseLevel` parameter (after `levelGroup`, before `role`)
   - Change blockId to include memberLevel instead of levelGroup:
     ```typescript
     const blockId = `W${week}-${day}-${memberLevel}-${role}`;
     ```
   - Include `memberLevel` in the returned object

4. Update `createTraceEvent` to include memberLevel in the `where` object:
   ```typescript
   where: {
     week: ctx.week,
     day: ctx.day,
     levelGroup: ctx.levelGroup,
     memberLevel: ctx.memberLevel,
     blockId: ctx.blockId,
     role: ctx.role,
   },
   ```
  </action>
  <verify>TypeScript compiles with expected errors in callers that don't pass memberLevel yet (service.ts, initium-pipeline.ts). Check: `cd el-templo-api && npx tsc --noEmit 2>&1 | grep -c "memberLevel"` should show errors only in caller files.</verify>
  <done>BlockContext has memberLevel field, createInitialContext accepts and threads memberLevel, blockId uses memberLevel</done>
</task>

<task type="auto">
  <name>Task 3: Add memberLevel to ExerciseRequirements</name>
  <files>el-templo-api/src/modules/sessions/fallback/types.ts</files>
  <action>
1. Update `ExerciseLevel` import: Since ExerciseLevel is now exported from `../types.ts`, update the import to use it from there (or keep the local definition and add a re-export — whichever is cleaner). The simplest approach: keep the local `ExerciseLevel` type as-is (it's already correctly defined) and add `ExerciseLevel` as an export from `../types.ts` in Task 1. Both files define the same type — this is fine since they're identical string unions.

   Actually, the cleanest approach: import `ExerciseLevel` from `../types` in this file and remove the local definition to avoid duplication:
   ```typescript
   import type { Contraction, LevelGroup, ExerciseLevel } from '../types';
   ```
   Remove the local `export type ExerciseLevel = ...` line. Update any re-exports if other files import ExerciseLevel from this file.

2. Add `memberLevel` to `ExerciseRequirements` interface:
   ```typescript
   readonly memberLevel: ExerciseLevel;
   ```

3. Keep `allowedLevels` and `levelGroup` in ExerciseRequirements — they're used by the existing fallback tiers.

Check if other files import `ExerciseLevel` from `fallback/types.ts`:
- `exercise-fallback.ts` imports it
- `stage-6-exercises.ts` imports it

If they import from `fallback/types.ts`, re-export from there:
```typescript
export type { ExerciseLevel } from '../types';
```
Or keep importing from the local file. The key is no breaking imports.
  </action>
  <verify>`cd el-templo-api && npx tsc --noEmit 2>&1 | head -30` — expect errors only in callers that construct ExerciseRequirements without memberLevel (stage-6-exercises.ts, exercise-fallback.ts callers).</verify>
  <done>ExerciseRequirements has memberLevel field, ExerciseLevel type consolidated to single source</done>
</task>

</tasks>

<verification>
After all 3 tasks:
1. `cd el-templo-api && npx tsc --noEmit 2>&1 | head -50` — TypeScript errors should only be in:
   - `service.ts` (DaySession missing memberLevel, createInitialContext missing memberLevel arg)
   - `routes.ts` (if it constructs sessions)
   - `stage-6-exercises.ts` (ExerciseRequirements missing memberLevel)
   - `initium-pipeline.ts` (createInitialContext call)
   These are expected and will be fixed in Plans 09-02 and 09-03.
2. No runtime errors — this plan only changes types and signatures.
</verification>

<success_criteria>
- ExerciseLevel type exported from types.ts
- memberLevel field exists in BlockContext, DaySession, TraceWhere, ExerciseRequirements
- createInitialContext signature includes memberLevel parameter
- blockId format uses memberLevel: `W${week}-${day}-${memberLevel}-${role}`
- No duplicate ExerciseLevel type definitions (consolidated to one source)
</success_criteria>

<output>
After completion, create `.planning/phases/09-level-specific-sessions/09-01-SUMMARY.md`
</output>
