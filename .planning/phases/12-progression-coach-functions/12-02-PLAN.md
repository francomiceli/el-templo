---
phase: 12-progression-coach-functions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-app/package.json
  - el-templo-app/src/modules/progression/stores/progressionStore.ts
  - el-templo-app/src/modules/progression/composables/useProgressionApi.ts
  - el-templo-app/src/modules/progression/types.ts
autonomous: true

must_haves:
  truths:
    - "vue-chart-3 and chart.js are installed as dependencies"
    - "Progression store holds stats, RPE trend, and evaluation state"
    - "API composable fetches stats and submits evaluation requests"
  artifacts:
    - path: "el-templo-app/src/modules/progression/stores/progressionStore.ts"
      provides: "Pinia store for progression data"
      exports: ["useProgressionStore"]
    - path: "el-templo-app/src/modules/progression/composables/useProgressionApi.ts"
      provides: "API calls for progression endpoints"
      exports: ["useProgressionApi"]
    - path: "el-templo-app/src/modules/progression/types.ts"
      provides: "TypeScript interfaces for progression data"
      exports: ["ProgressionStats", "RpeTrend", "EvaluationStatus"]
  key_links:
    - from: "el-templo-app/src/modules/progression/composables/useProgressionApi.ts"
      to: "el-templo-app/src/modules/progression/stores/progressionStore.ts"
      via: "Store action calls"
      pattern: "progressionStore\\.set"
---

<objective>
Create frontend foundation for progression tracking: install chart library, create Pinia store, and API composable.

Purpose: Provides the data layer and API integration that components will use to display progression data.
Output: Installed vue-chart-3/chart.js, Pinia store with progression state, composable for API calls.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-progression-coach-functions/12-CONTEXT.md
@.planning/phases/12-progression-coach-functions/12-RESEARCH.md
@el-templo-app/src/modules/training/stores/weekStore.ts
@el-templo-app/src/stores/userStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install chart libraries</name>
  <files>
    el-templo-app/package.json
  </files>
  <action>
Install vue-chart-3 and chart.js as dependencies:

```bash
cd el-templo-app && pnpm add vue-chart-3 chart.js
```

vue-chart-3 is the Vue 3 Composition API wrapper for Chart.js.
chart.js 4.x is the underlying chart library.

Do NOT use chart.js/auto import (defeats tree-shaking).
  </action>
  <verify>
```bash
cd el-templo-app && pnpm list vue-chart-3 chart.js
```
Both packages appear in dependencies.

```bash
cat el-templo-app/package.json | grep -E "(vue-chart-3|chart.js)"
```
Shows both in dependencies section.
  </verify>
  <done>
vue-chart-3 and chart.js are in package.json dependencies.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create progression module with types, store, and API composable</name>
  <files>
    el-templo-app/src/modules/progression/types.ts
    el-templo-app/src/modules/progression/stores/progressionStore.ts
    el-templo-app/src/modules/progression/composables/useProgressionApi.ts
  </files>
  <action>
Create progression module directory structure:
```
el-templo-app/src/modules/progression/
├── types.ts
├── stores/
│   └── progressionStore.ts
└── composables/
    └── useProgressionApi.ts
```

**types.ts** - TypeScript interfaces matching API response:
```typescript
export interface ProgressionLevel {
  current: 'alfa' | 'delta' | 'sigma' | 'omega' | 'spartan';
  displayName: string;
  greekLetter: string;
}

export interface ProgressionStats {
  totalSessions: number;
  totalDaysTrained: number;
  sessionsThisWeek: number;
  currentStreak: number;
}

export interface RpeTrend {
  labels: string[];
  data: (number | null)[];
  averageRpe: number;
}

export interface EvaluationStatus {
  eligible: boolean;
  averageRpeLast2Weeks: number | null;
  pendingRequest: boolean;
  requestedAt: string | null;
}

export interface ProgressionResponse {
  level: ProgressionLevel;
  stats: ProgressionStats;
  rpeTrend: RpeTrend;
  evaluation: EvaluationStatus;
}
```

**progressionStore.ts** - Pinia composition API store:
- State: level, stats, rpeTrend, evaluation (all refs, nullable)
- State: loading, error for async status
- Computed: evaluationEligible (eligible && !pendingRequest)
- Actions: setProgressionData(response), setEvaluationPending(), reset()

Follow pattern from weekStore.ts - use defineStore with setup function.

**useProgressionApi.ts** - API composable:
- Uses useApi() from boot/api.ts
- Uses useProgressionStore()
- fetchStats(): GET /progression/stats, calls store.setProgressionData()
- requestEvaluation(): POST /progression/request-evaluation, calls store.setEvaluationPending()
- Returns { fetchStats, requestEvaluation, loading, error }

Loading state is local ref in composable, set during API calls.
Error handling: catch, set error state, optionally show Quasar Notify.
  </action>
  <verify>
```bash
cd el-templo-app && pnpm build
```
TypeScript compiles without errors.

Files exist in correct locations:
```bash
ls -la el-templo-app/src/modules/progression/
ls -la el-templo-app/src/modules/progression/stores/
ls -la el-templo-app/src/modules/progression/composables/
```
  </verify>
  <done>
- types.ts exports ProgressionStats, RpeTrend, EvaluationStatus, ProgressionResponse
- progressionStore.ts exports useProgressionStore with state, computed, actions
- useProgressionApi.ts exports useProgressionApi with fetchStats, requestEvaluation
- All TypeScript types match API response structure
  </done>
</task>

</tasks>

<verification>
- [ ] pnpm list shows vue-chart-3 and chart.js installed
- [ ] TypeScript compiles: `cd el-templo-app && pnpm build`
- [ ] progressionStore exports useProgressionStore
- [ ] useProgressionApi exports fetchStats and requestEvaluation
</verification>

<success_criteria>
Frontend progression foundation is complete:
- Chart libraries installed and ready for use
- Store can hold all progression data
- API composable can fetch stats and submit requests
- All types are defined matching API contract
</success_criteria>

<output>
After completion, create `.planning/phases/12-progression-coach-functions/12-02-SUMMARY.md`
</output>
