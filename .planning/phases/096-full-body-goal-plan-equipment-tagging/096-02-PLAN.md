---
phase: 096-full-body-goal-plan-equipment-tagging
plan: 02
type: execute
wave: 2
depends_on: ["096-01"]
files_modified:
  - el-templo-admin/src/types/goal-plan.ts
  - el-templo-admin/src/types/exercise.ts
  - el-templo-admin/src/composables/useExercisesApi.ts
  - el-templo-admin/src/pages/ExercisesPage.vue
  - el-templo-admin/src/pages/SessionEditPage.vue
autonomous: true
requirements: []
must_haves:
  truths:
    - "Admin exercises page shows equipment as an inline-editable dropdown for each exercise"
    - "Admin can select equipment value (barras, anillas, cajon, ninguno) inline on any exercise"
    - "Admin exercises page equipment filter dropdown filters exercises by equipment value"
    - "Admin goal plan session editing: approving a full_body day triggers auto-tagging splash"
    - "Auto-tagging splash lists exercises in the session and offers to tag as ninguno"
    - "Confirming the splash bulk-updates exercises equipment field via API"
    - "full_body appears in GeneratePage and ProgramWizardDialog goal plan type lists"
    - "Program and subscription plan can be created with goalPlanType full_body"
  artifacts:
    - path: "el-templo-admin/src/types/goal-plan.ts"
      provides: "full_body in GoalPlanType union and all related maps"
      contains: "full_body"
    - path: "el-templo-admin/src/types/exercise.ts"
      provides: "equipment field in Exercise interface"
      contains: "equipment"
    - path: "el-templo-admin/src/composables/useExercisesApi.ts"
      provides: "updateExercise accepts equipment, bulkUpdateEquipment method"
      contains: "bulkUpdateEquipment"
    - path: "el-templo-admin/src/pages/ExercisesPage.vue"
      provides: "Equipment column with inline dropdown and equipment filter"
      contains: "equipment"
    - path: "el-templo-admin/src/pages/SessionEditPage.vue"
      provides: "Auto-tagging splash on full_body approve"
      contains: "bulkUpdateEquipment"
  key_links:
    - from: "el-templo-admin/src/pages/ExercisesPage.vue"
      to: "el-templo-admin/src/composables/useExercisesApi.ts"
      via: "updateExercise call with equipment field"
      pattern: "equipment"
    - from: "el-templo-admin/src/pages/ExercisesPage.vue"
      to: "GET /admin/exercises?equipment=X"
      via: "loadExercises passes equipment filter param to API"
      pattern: "equipment"
    - from: "el-templo-admin/src/pages/SessionEditPage.vue"
      to: "el-templo-admin/src/composables/useExercisesApi.ts"
      via: "bulkUpdateEquipment call from splash confirmation"
      pattern: "bulkUpdateEquipment"
    - from: "el-templo-admin/src/types/goal-plan.ts"
      to: "el-templo-admin/src/pages/GeneratePage.vue"
      via: "GOAL_PLAN_TYPE_OPTIONS import consumed by GeneratePage"
      pattern: "GOAL_PLAN_TYPE_OPTIONS"
    - from: "el-templo-admin/src/types/goal-plan.ts"
      to: "el-templo-admin/src/components/ProgramWizardDialog.vue"
      via: "GOAL_PLAN_TYPE_OPTIONS import consumed by ProgramWizardDialog"
      pattern: "GOAL_PLAN_TYPE_OPTIONS"
---

<objective>
Add full_body to admin goal plan types, add equipment column to ExercisesPage with inline editing and filter, and build the auto-tagging splash in SessionEditPage that triggers on full_body session approval.

Purpose: Frontend admin experience for equipment tagging and full body goal plan support.
Output: Updated admin types, exercises page with equipment dropdown and filter, session edit page with auto-tagging splash.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/096-full-body-goal-plan-equipment-tagging/096-01-SUMMARY.md

@el-templo-admin/src/types/goal-plan.ts
@el-templo-admin/src/types/exercise.ts
@el-templo-admin/src/composables/useExercisesApi.ts
@el-templo-admin/src/pages/ExercisesPage.vue
@el-templo-admin/src/pages/SessionEditPage.vue
@el-templo-admin/src/pages/GeneratePage.vue
@el-templo-admin/src/components/ProgramWizardDialog.vue

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan consumes -->

From el-templo-api (Plan 01 output):

- GoalPlanType now includes "full_body"
- PATCH /admin/exercises/:exerciseId accepts `{ effort?: string; exercise?: string; equipment?: string | null }`
- POST /admin/exercises/bulk-update-equipment accepts `{ exerciseIds: number[]; equipment: string }` and returns `{ success: boolean; updatedCount: number }`
- GET /admin/exercises now supports `equipment` query param (enum: barras, anillas, cajon, ninguno, empty) and returns `equipment` field in each exercise
- exercises table has `equipment` column: enum('barras','anillas','cajon','ninguno') nullable

From el-templo-admin/src/types/exercise.ts:

```typescript
export interface Exercise {
  id: number;
  exercise: string;
  category: string;
  level: string | null;
  route: string;
  effort: string;
  difficulty: number;
  dificultadLineal: number;
  videoUrl: string | null;
  // ADD: equipment: string | null;
}
```

From el-templo-admin/src/composables/useExercisesApi.ts:

```typescript
async function updateExercise(
  exerciseId: number,
  fields: { effort?: string; exercise?: string }, // ADD: equipment?: string | null
): Promise<Exercise>;
```

From el-templo-admin/src/pages/SessionEditPage.vue:

- handleApproveDay() at line 339 — this is where the auto-tagging splash must intercept
- sessions ref contains SessionDetail[] with blocks[].exercises[].exerciseId
- goalPlanType computed from route.query.goalPlanType

From el-templo-admin/src/types/session.ts:

```typescript
export interface SessionExercise {
  id: number;
  exerciseId: number;
  exerciseName: string;
  // ... other fields
}
```

From el-templo-admin/src/types/goal-plan.ts:

```typescript
// GOAL_PLAN_TYPE_OPTIONS is imported by:
// - src/pages/GeneratePage.vue (line ~imports)
// - src/components/ProgramWizardDialog.vue (line 387)
// Adding full_body to GOAL_PLAN_TYPE_OPTIONS automatically propagates to both consumers.
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add full_body to admin types and equipment to exercise types/API</name>
  <read_first>
    - el-templo-admin/src/types/goal-plan.ts
    - el-templo-admin/src/types/exercise.ts
    - el-templo-admin/src/composables/useExercisesApi.ts
    - el-templo-admin/src/pages/GeneratePage.vue (verify GOAL_PLAN_TYPE_OPTIONS import and usage)
    - el-templo-admin/src/components/ProgramWizardDialog.vue (verify GOAL_PLAN_TYPE_OPTIONS import and usage)
  </read_first>
  <files>
    el-templo-admin/src/types/goal-plan.ts,
    el-templo-admin/src/types/exercise.ts,
    el-templo-admin/src/composables/useExercisesApi.ts
  </files>
  <action>
**1. el-templo-admin/src/types/goal-plan.ts** — Add `full_body` everywhere:

a) GoalPlanType union — add `| 'full_body'` at the end:

```typescript
export type GoalPlanType =
  | "tren_superior"
  | "tren_inferior"
  | "gluteos"
  | "cuadriceps"
  | "empuje"
  | "traccion"
  | "planche"
  | "front_lever"
  | "full_body";
```

b) ALL_GOAL_PLAN_TYPES — append `'full_body'`:

```typescript
export const ALL_GOAL_PLAN_TYPES: GoalPlanType[] = [
  "tren_superior",
  "tren_inferior",
  "gluteos",
  "cuadriceps",
  "empuje",
  "traccion",
  "planche",
  "front_lever",
  "full_body",
];
```

c) GOAL_PLAN_TIER_MAP — add entry:

```typescript
  full_body: 'principiante',
```

d) GOAL_PLAN_TYPE_LABELS — add entry:

```typescript
  full_body: 'Full Body',
```

e) GOAL_PLAN_TIER_LABELS — no change needed (principiante already exists).

f) GOAL_PLAN_TIER_COLORS — no change needed.

g) GOAL_PLAN_TYPE_OPTIONS — add entry at the end:

```typescript
  { label: 'Full Body', value: 'full_body' },
```

This automatically propagates to GeneratePage.vue and ProgramWizardDialog.vue which both import and consume GOAL_PLAN_TYPE_OPTIONS from this file.

h) GOAL_PLAN_TYPE_COLORS — add entry:

```typescript
  full_body: 'green-7',
```

**2. el-templo-admin/src/types/exercise.ts** — Add `equipment` to interfaces:

a) Exercise interface — add after `videoUrl`:

```typescript
equipment: string | null; // barras, anillas, cajon, ninguno, or null (untagged)
```

b) ExerciseFilters interface — add after `hasVideo`:

```typescript
equipment: string; // filter by equipment value
```

**3. el-templo-admin/src/composables/useExercisesApi.ts** — Two changes:

a) Extend `updateExercise` fields parameter type:

```typescript
  async function updateExercise(
    exerciseId: number,
    fields: { effort?: string; exercise?: string; equipment?: string | null }
  ): Promise<Exercise> {
```

b) Add `bulkUpdateEquipment` method before the `return` statement:

```typescript
async function bulkUpdateEquipment(
  exerciseIds: number[],
  equipment: string,
): Promise<{ updatedCount: number }> {
  try {
    const { data } = await api.post<{ success: boolean; updatedCount: number }>(
      "/admin/exercises/bulk-update-equipment",
      { exerciseIds, equipment },
    );
    allExercisesCache = null; // invalidate cache
    return { updatedCount: data.updatedCount };
  } catch (err: unknown) {
    const message = extractError(err, "Error actualizando equipamiento");
    log.error("Failed to bulk update equipment", {
      exerciseIds,
      error: message,
    });
    Notify.create({ type: "negative", message });
    throw err;
  }
}
```

c) Add `bulkUpdateEquipment` to the return object.
</action>
<verify>
<automated>cd /home/franco/projects/el-templo/el-templo-admin && grep -q "full_body" src/types/goal-plan.ts && grep -q "equipment" src/types/exercise.ts && grep -q "bulkUpdateEquipment" src/composables/useExercisesApi.ts && grep -q "GOAL_PLAN_TYPE_OPTIONS" src/pages/GeneratePage.vue && grep -q "GOAL_PLAN_TYPE_OPTIONS" src/components/ProgramWizardDialog.vue && echo "PASS" || echo "FAIL"</automated>
</verify>
<acceptance_criteria> - `grep "full_body" el-templo-admin/src/types/goal-plan.ts` returns matches in GoalPlanType, ALL_GOAL_PLAN_TYPES, GOAL_PLAN_TIER_MAP, GOAL_PLAN_TYPE_LABELS, GOAL_PLAN_TYPE_OPTIONS, GOAL_PLAN_TYPE_COLORS - `grep "equipment" el-templo-admin/src/types/exercise.ts` returns match in Exercise interface and ExerciseFilters interface - `grep "bulkUpdateEquipment" el-templo-admin/src/composables/useExercisesApi.ts` returns matches (function def and return) - `grep "equipment" el-templo-admin/src/composables/useExercisesApi.ts` returns matches in updateExercise parameter type and bulkUpdateEquipment function - `grep "GOAL_PLAN_TYPE_OPTIONS" el-templo-admin/src/pages/GeneratePage.vue` confirms import exists (full_body propagates automatically) - `grep "GOAL_PLAN_TYPE_OPTIONS" el-templo-admin/src/components/ProgramWizardDialog.vue` confirms import exists (full_body propagates automatically)
</acceptance_criteria>
<done>full_body is a recognized goal plan type in admin (propagating to GeneratePage and ProgramWizardDialog via GOAL_PLAN_TYPE_OPTIONS), Exercise type includes equipment, API composable supports single and bulk equipment updates</done>
</task>

<task type="auto">
  <name>Task 2: Add equipment column and filter to ExercisesPage and auto-tagging splash to SessionEditPage</name>
  <read_first>
    - el-templo-admin/src/pages/ExercisesPage.vue
    - el-templo-admin/src/pages/SessionEditPage.vue
    - el-templo-admin/src/types/session.ts (for SessionExercise.exerciseId)
  </read_first>
  <files>
    el-templo-admin/src/pages/ExercisesPage.vue,
    el-templo-admin/src/pages/SessionEditPage.vue
  </files>
  <action>
**1. ExercisesPage.vue** — Add equipment column with inline-editable dropdown and equipment filter.

a) Add equipment filter in the filter bar (after the effort filter `<div class="col-6 col-sm-1">`):

```html
<div class="col-6 col-sm-1">
  <q-select
    v-model="filters.equipment"
    :options="equipmentFilterOptions"
    label="Equipo"
    dense
    outlined
    emit-value
    map-options
    @update:model-value="onFilterChange"
  />
</div>
```

b) Add `equipment: ''` to the `filters` reactive object.

c) Add equipment filter options constant:

```typescript
const equipmentFilterOptions = [
  { label: "Todos", value: "" },
  { label: "Sin asignar", value: "empty" },
  { label: "Barras", value: "barras" },
  { label: "Anillas", value: "anillas" },
  { label: "Cajon", value: "cajon" },
  { label: "Ninguno", value: "ninguno" },
];
```

d) Add inline equipment options constant (for the cell dropdown):

```typescript
const inlineEquipmentOptions = [
  { label: "Barras", value: "barras" },
  { label: "Anillas", value: "anillas" },
  { label: "Cajon", value: "cajon" },
  { label: "Ninguno", value: "ninguno" },
];
```

e) Add equipment column to the `columns` array, after the `effort` column:

```typescript
  {
    name: 'equipment',
    label: 'Equipo',
    field: 'equipment',
    align: 'left',
    sortable: false,
    style: 'width: 130px',
  },
```

f) Add body-cell-equipment template slot to the QTable (after body-cell-effort template):

```html
<!-- Equipment column: inline select, always editable -->
<template #body-cell-equipment="props">
  <q-td :props="props">
    <q-select
      :model-value="props.row.equipment"
      :options="inlineEquipmentOptions"
      :label="props.row.equipment ? undefined : 'Sin asignar'"
      dense
      outlined
      emit-value
      map-options
      clearable
      style="min-width: 120px"
      :color="props.row.equipment ? 'primary' : 'warning'"
      @update:model-value="(val: string | null) => onInlineEquipmentChange(props.row.id, val)"
    />
  </q-td>
</template>
```

g) Add `onInlineEquipmentChange` handler function (next to `onInlineEffortChange`):

```typescript
async function onInlineEquipmentChange(
  exerciseId: number,
  equipment: string | null,
) {
  try {
    await exercisesApi.updateExercise(exerciseId, { equipment });
    $q.notify({
      type: "positive",
      message: equipment
        ? `Equipo actualizado a ${equipment}`
        : "Equipo removido",
    });
    loadExercises();
  } catch {
    // Error handled by composable
  }
}
```

h) Add `equipment` to the filter params in `loadExercises`. Find where filter params are assembled into a query object (alongside search, category, level, route, effort, hasVideo) and add:

```typescript
      equipment: filters.equipment || undefined,
```

This sends the `equipment` query param to `GET /admin/exercises` which Plan 01 extended to support equipment filtering (including 'empty' for untagged).

**2. SessionEditPage.vue** — Add auto-tagging splash on full_body session approval.

a) Import `useExercisesApi` at the top of the script (alongside existing imports):

```typescript
import { useExercisesApi } from "src/composables/useExercisesApi";
```

b) Instantiate at setup level (alongside existing composables):

```typescript
const exercisesApi = useExercisesApi();
```

c) Add state for the tagging dialog:

```typescript
// Equipment auto-tagging splash state
const showEquipmentSplash = ref(false);
const splashExercises = ref<
  Array<{ exerciseId: number; exerciseName: string }>
>([]);
const taggingInProgress = ref(false);
```

d) Modify `handleApproveDay` to intercept when goalPlanType is `full_body`. Replace the existing `handleApproveDay` function with:

```typescript
async function handleApproveDay() {
  const pendingIds = sessions.value
    .filter((s) => s.status === "pending_review")
    .map((s) => s.id);
  if (pendingIds.length === 0) return;

  $q.dialog({
    title: "Aprobar Dia",
    message: `Aprobar ${pendingIds.length} sesiones pendientes?`,
    cancel: true,
    persistent: true,
  }).onOk(async () => {
    try {
      const result = await sessionsApi.bulkApprove(pendingIds);
      $q.notify({
        type: "positive",
        message: `${result.approvedCount} sesiones aprobadas`,
      });
      refreshDay();
      adminStore.fetchPendingCount();
      adminStore.checkSessionCoverage();

      // After successful approve, show equipment tagging splash for full_body
      if (goalPlanType.value === "full_body") {
        showEquipmentTaggingSplash();
      }
    } catch {
      $q.notify({ type: "negative", message: "Error aprobando sesiones" });
    }
  });
}
```

e) Add the `showEquipmentTaggingSplash` function:

```typescript
function showEquipmentTaggingSplash() {
  // Collect unique exercises from all sessions in this day
  const exerciseMap = new Map<number, string>();
  for (const session of sessions.value) {
    for (const block of session.blocks) {
      for (const ex of block.exercises) {
        if (ex.exerciseId && !exerciseMap.has(ex.exerciseId)) {
          exerciseMap.set(ex.exerciseId, ex.exerciseName);
        }
      }
      // Also include mobility exercises
      if (
        block.mobilityExercise?.exerciseId &&
        !exerciseMap.has(block.mobilityExercise.exerciseId)
      ) {
        exerciseMap.set(
          block.mobilityExercise.exerciseId,
          block.mobilityExercise.exerciseName,
        );
      }
    }
  }

  if (exerciseMap.size === 0) return;

  splashExercises.value = Array.from(exerciseMap.entries()).map(
    ([exerciseId, exerciseName]) => ({
      exerciseId,
      exerciseName,
    }),
  );
  showEquipmentSplash.value = true;
}
```

f) Add the `confirmEquipmentTagging` function:

```typescript
async function confirmEquipmentTagging() {
  taggingInProgress.value = true;
  try {
    const ids = splashExercises.value.map((e) => e.exerciseId);
    const result = await exercisesApi.bulkUpdateEquipment(ids, "ninguno");
    $q.notify({
      type: "positive",
      message: `${result.updatedCount} ejercicios marcados como "ninguno" (sin equipamiento)`,
    });
    showEquipmentSplash.value = false;
  } catch {
    $q.notify({ type: "negative", message: "Error actualizando equipamiento" });
  } finally {
    taggingInProgress.value = false;
  }
}
```

g) Add the equipment tagging dialog in the template. Place it right before the closing `</q-page>` tag (after the block swap dialog):

```html
<!-- Equipment Auto-Tagging Splash (full_body goal plans) -->
<q-dialog v-model="showEquipmentSplash" persistent>
  <q-card style="min-width: 480px; max-width: 600px">
    <q-card-section>
      <div class="text-h6">Etiquetar Ejercicios — Equipamiento</div>
      <div class="text-body2 text-grey-7 q-mt-sm">
        Esta sesion de Full Body contiene {{ splashExercises.length }}
        ejercicios. Marcarlos como <strong>"ninguno"</strong> (sin
        equipamiento)?
      </div>
    </q-card-section>

    <q-separator />

    <q-card-section style="max-height: 300px; overflow-y: auto">
      <q-list dense>
        <q-item v-for="ex in splashExercises" :key="ex.exerciseId">
          <q-item-section avatar>
            <q-icon name="fitness_center" color="grey-6" />
          </q-item-section>
          <q-item-section>
            <q-item-label>{{ ex.exerciseName }}</q-item-label>
            <q-item-label caption>ID: {{ ex.exerciseId }}</q-item-label>
          </q-item-section>
        </q-item>
      </q-list>
    </q-card-section>

    <q-separator />

    <q-card-actions align="right" class="q-pa-md">
      <q-btn
        flat
        label="Omitir"
        color="grey"
        @click="showEquipmentSplash = false"
      />
      <q-btn
        label="Marcar como Ninguno"
        icon="check"
        color="positive"
        :loading="taggingInProgress"
        @click="confirmEquipmentTagging"
      />
    </q-card-actions>
  </q-card>
</q-dialog>
```

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && grep -q "equipment" src/pages/ExercisesPage.vue && grep -q "equipmentFilterOptions" src/pages/ExercisesPage.vue && grep -q "showEquipmentSplash" src/pages/SessionEditPage.vue && grep -q "bulkUpdateEquipment" src/pages/SessionEditPage.vue && grep -q "full_body" src/pages/SessionEditPage.vue && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "inlineEquipmentOptions" el-templo-admin/src/pages/ExercisesPage.vue` returns a match
    - `grep "body-cell-equipment" el-templo-admin/src/pages/ExercisesPage.vue` returns a match (template slot)
    - `grep "onInlineEquipmentChange" el-templo-admin/src/pages/ExercisesPage.vue` returns matches (handler + template)
    - `grep "equipmentFilterOptions" el-templo-admin/src/pages/ExercisesPage.vue` returns a match
    - `grep "filters.equipment" el-templo-admin/src/pages/ExercisesPage.vue` returns a match (filter param sent to API)
    - `grep "showEquipmentSplash" el-templo-admin/src/pages/SessionEditPage.vue` returns matches (ref, template, functions)
    - `grep "full_body" el-templo-admin/src/pages/SessionEditPage.vue` returns a match in handleApproveDay
    - `grep "bulkUpdateEquipment" el-templo-admin/src/pages/SessionEditPage.vue` returns a match in confirmEquipmentTagging
    - `grep "Marcar como Ninguno" el-templo-admin/src/pages/SessionEditPage.vue` returns a match in dialog template
    - `grep "Omitir" el-templo-admin/src/pages/SessionEditPage.vue` returns a match (skip button in splash)
  </acceptance_criteria>
  <done>ExercisesPage has equipment column with inline dropdown and equipment filter for all exercises (backed by GET /admin/exercises?equipment=X from Plan 01), SessionEditPage shows auto-tagging splash on full_body day approval listing exercises with option to bulk-tag as ninguno or skip</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary        | Description                                                  |
| --------------- | ------------------------------------------------------------ |
| admin UI -> API | Equipment values sent from dropdowns and splash confirmation |

## STRIDE Threat Register

| Threat ID | Category      | Component                     | Disposition | Mitigation Plan                                                                                                    |
| --------- | ------------- | ----------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------ |
| T-96-04   | T (Tampering) | ExercisesPage inline dropdown | accept      | Values constrained by QSelect options, API validates server-side (Plan 01 T-96-01)                                 |
| T-96-05   | T (Tampering) | SessionEditPage splash        | accept      | Splash only sends "ninguno" as hardcoded value, exerciseIds extracted from session data already loaded from server |

</threat_model>

<verification>
1. `cd el-templo-admin && pnpm exec vue-tsc --noEmit` passes (TypeScript compiles)
2. `grep "full_body" src/types/goal-plan.ts` matches in type, array, maps, options, colors
3. `grep "equipment" src/types/exercise.ts` matches in Exercise and ExerciseFilters interfaces
4. `grep "bulkUpdateEquipment" src/composables/useExercisesApi.ts` matches
5. `grep "body-cell-equipment" src/pages/ExercisesPage.vue` matches
6. `grep "equipmentFilterOptions" src/pages/ExercisesPage.vue` matches
7. `grep "filters.equipment" src/pages/ExercisesPage.vue` matches (filter param sent to API)
8. `grep "showEquipmentSplash" src/pages/SessionEditPage.vue` matches
9. `grep "full_body" src/pages/SessionEditPage.vue` matches in handleApproveDay
10. `grep "GOAL_PLAN_TYPE_OPTIONS" src/pages/GeneratePage.vue` confirms import exists (full_body propagates)
11. `grep "GOAL_PLAN_TYPE_OPTIONS" src/components/ProgramWizardDialog.vue` confirms import exists (full_body propagates)
</verification>

<success_criteria>

- full_body appears as a selectable goal plan type in admin (GeneratePage, ProgramWizardDialog via shared GOAL_PLAN_TYPE_OPTIONS)
- ExercisesPage shows equipment column with inline dropdown (always editable, like effort pattern but always visible)
- ExercisesPage has equipment filter in filter bar (backed by GET /admin/exercises?equipment=X)
- SessionEditPage: approving a full_body day shows splash listing exercises
- Splash offers "Marcar como Ninguno" or "Omitir"
- Confirming splash bulk-updates all listed exercises equipment to "ninguno"
- TypeScript compiles with zero errors in admin app
  </success_criteria>

<output>
After completion, create `.planning/phases/096-full-body-goal-plan-equipment-tagging/096-02-SUMMARY.md`
</output>
