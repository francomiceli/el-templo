---
phase: 99-member-selectable-training-level
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/migrations/0091_rename_level_at_completion_to_session_level.sql
  - el-templo-api/src/db/schema/completed-sessions.ts
  - el-templo-api/src/modules/sessions/routes.ts
  - el-templo-api/src/modules/goal-plans/routes.ts
  - el-templo-api/src/modules/members/routes.ts
  - el-templo-api/src/modules/members/schemas.ts
  - el-templo-api/src/modules/members/service.ts
  - el-templo-api/test/sessions/sessions.test.ts
  - el-templo-api/test/members/session-levels.test.ts
  - el-templo-api/test/goal-plans/goal-plans.test.ts
autonomous: true
requirements: [R3, R8, R9, R10, R11]

must_haves:
  truths:
    - "completed_sessions column is named session_level; level_at_completion no longer exists in schema, routes, or tests"
    - "Admin GET /admin/members/:userId/session-levels?days=30 returns per-level counts for the last N days"
    - "?level= override is honored on /sessions/weekly, /sessions/daily, and /goal-plans/session"
    - "Goal-plan completion at a non-assigned level advances program_enrollments.currentWeek exactly as same-level completion would"
    - "ROM Saturday + ?level=omega yields delta content server-side"
  artifacts:
    - path: "el-templo-api/src/db/migrations/0091_rename_level_at_completion_to_session_level.sql"
      provides: "Forward-only column rename DDL"
      contains: "ALTER TABLE completed_sessions"
    - path: "el-templo-api/src/db/schema/completed-sessions.ts"
      provides: "Drizzle field sessionLevel mapped to session_level column"
      contains: "sessionLevel"
    - path: "el-templo-api/src/modules/members/routes.ts"
      provides: "GET /:userId/session-levels endpoint"
      contains: "session-levels"
    - path: "el-templo-api/test/members/session-levels.test.ts"
      provides: "Integration tests for admin session-levels endpoint"
      min_lines: 60
  key_links:
    - from: "el-templo-api/src/modules/sessions/routes.ts"
      to: "completed_sessions.session_level"
      via: ".values({ ..., sessionLevel })"
      pattern: "sessionLevel"
    - from: "el-templo-api/src/modules/goal-plans/routes.ts"
      to: "completed_sessions.session_level"
      via: ".values({ ..., sessionLevel })"
      pattern: "sessionLevel"
    - from: "el-templo-api/src/modules/members/routes.ts"
      to: "completed_sessions.session_level"
      via: "aggregation query GROUP BY session_level"
      pattern: "session-levels"
---

<objective>
Land the API-side half of Phase 99: rename completed_sessions.level_at_completion -> session_level
(new migration 0091), rename the Drizzle field levelAtCompletion -> sessionLevel everywhere it is
referenced, add the admin endpoint GET /admin/members/:userId/session-levels?days=30 that returns
per-level 30-day counts, and extend the integration test suite with R9 (cross-level goal-plan
currentWeek advance), R10 (ROM Saturday collapse with ?level=omega), R3 (/sessions/daily override),
and R11 (admin endpoint) plus rename-parity assertions (R8).

Purpose: Finalizes the Phase-1 data plumbing (commit c8d0726b) with a semantically correct column
name, unlocks the admin chip row on AlumnoDetailPage with a dedicated read endpoint, and proves
via integration tests that existing ?level= override behaviour (R3) is preserved on all three read
endpoints (weekly, daily, goal-plans/session) and that ROM Saturday collapse (R10) still wins
against a client-selected level.

Output: New migration SQL, renamed schema field and call sites, new admin route + schema + tests,
plus R3 (daily)/R9/R10 regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-member-selectable-training-level/99-SPEC.md
@.planning/phases/99-member-selectable-training-level/99-CONTEXT.md
@.planning/phases/99-member-selectable-training-level/99-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Drizzle field currently in el-templo-api/src/db/schema/completed-sessions.ts:25 -->
```ts
levelAtCompletion: mysqlEnum("level_at_completion", [
  "alfa", "delta", "sigma", "omega", "spartan",
]).notNull(),
```

<!-- TRAINING_LEVELS enum + type guard already live in: -->
<!-- el-templo-api/src/modules/shared/training-constants.ts -->

```ts
export const TRAINING_LEVELS = ["alfa", "delta", "sigma", "omega", "spartan"] as const;
export type TrainingLevel = typeof TRAINING_LEVELS[number];
export function isTrainingLevel(v: unknown): v is TrainingLevel { ... }
export function parseDayId(dayId: string): { level: TrainingLevel | null; ... }
```

<!-- Admin endpoint precedent (reuse this authorization + schema shape pattern): -->
<!-- el-templo-api/src/modules/goal-plans/routes.ts:516 -->
<!-- fastify.get<{ Params: { userId: number } }>("/admin/members/:userId", ...) with MEMBER_ROLES auth -->

<!-- Existing ROM collapse logic (already honors ?level= via effectiveLevel): -->
<!-- el-templo-api/src/modules/sessions/routes.ts:204-220 (daily), :320-324 (weekly) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename level_at_completion -> session_level (migration + schema + routes + existing tests)</name>
  <files>
    el-templo-api/src/db/migrations/0091_rename_level_at_completion_to_session_level.sql,
    el-templo-api/src/db/schema/completed-sessions.ts,
    el-templo-api/src/modules/sessions/routes.ts,
    el-templo-api/src/modules/goal-plans/routes.ts,
    el-templo-api/test/sessions/sessions.test.ts
  </files>
  <read_first>
    el-templo-api/src/db/migrations/0090_completed_sessions_level.sql (Phase-1 migration — do NOT edit),
    el-templo-api/src/db/schema/completed-sessions.ts (line 25 — field to rename),
    el-templo-api/src/modules/sessions/routes.ts (lines 479, 521, 540 — variable + .values assignments),
    el-templo-api/src/modules/goal-plans/routes.ts (lines 255, 306, 324 — variable + .values assignments),
    el-templo-api/test/sessions/sessions.test.ts (lines 192, 225, 234, 237, 272, 281 — 6 rename spots),
    el-templo-api/src/modules/shared/training-constants.ts (source of truth for enum + parseDayId),
    CLAUDE.md (database changes + migration tracking rules — _migrations table is source of truth, never drizzle-kit migrate)
  </read_first>
  <behavior>
    - After this task: `SELECT session_level FROM completed_sessions` works; `level_at_completion` column no longer exists.
    - After this task: `grep -rn "level_at_completion\|levelAtCompletion" el-templo-api/src el-templo-api/test` returns zero matches.
    - Existing sessions tests that previously asserted `levelAtCompletion === 'omega'` now assert `sessionLevel === 'omega'`.
    - `pnpm test` (full API suite) stays green.
  </behavior>
  <action>
    1. Create `el-templo-api/src/db/migrations/0091_rename_level_at_completion_to_session_level.sql` with header comment referencing Phase-1 commit c8d0726b and Phase 99 SPEC R8 (per D-17). Body (per D-15):
       ```sql
       -- Rename level_at_completion -> session_level.
       -- Semantic: the level describes the SESSION (which level was trained),
       -- not a promotion/downgrade event.
       -- Preserves Phase-1 backfilled rows (single DDL on MySQL 8+).
       -- Originally introduced in migration 0090 / commit c8d0726b.
       -- Phase 99 SPEC: .planning/phases/99-member-selectable-training-level/99-SPEC.md R8.
       ALTER TABLE completed_sessions
         CHANGE COLUMN level_at_completion session_level
           ENUM('alfa','delta','sigma','omega','spartan') NOT NULL;
       ```
       No DROP/RECREATE. Single atomic DDL. Do NOT edit migration 0090.
    2. In `el-templo-api/src/db/schema/completed-sessions.ts` rename the exported field on the `completedSessions` table from `levelAtCompletion` -> `sessionLevel` and the first arg of `mysqlEnum(...)` from `"level_at_completion"` -> `"session_level"`. Keep the enum values identical.
    3. In `el-templo-api/src/modules/sessions/routes.ts`:
       - Line ~479: rename the local `const levelAtCompletion = parseDayId(dayId).level;` -> `const sessionLevel = parseDayId(dayId).level;`. Update the `isTrainingLevel(levelAtCompletion)` guard call to pass `sessionLevel`.
       - Lines ~521 and ~540: rename the `levelAtCompletion` shorthand property inside `.values({...})` / `.set({...})` to `sessionLevel` (Drizzle object key MUST match the new schema field).
    4. In `el-templo-api/src/modules/goal-plans/routes.ts` apply the same renames at lines ~255 (local const + guard) and ~306 / ~324 (.values / .set properties).
    5. In `el-templo-api/test/sessions/sessions.test.ts` rename every `levelAtCompletion` reference to `sessionLevel` and every quoted `"level_at_completion"` / `'level_at_completion'` to `session_level`. The 6 spots are at lines ~192, ~225, ~234, ~237, ~272, ~281 per research; grep to confirm none are missed.
    6. Hand-author the migration SQL file exactly as written in step 1. Do NOT run `pnpm db:generate` — this migration is hand-authored because `drizzle-kit generate` would typically offer an ambiguous rename-vs-drop+add prompt and a column rename is a one-shot semantic change we want to control. Apply the migration via `pnpm db:migrate`, which runs the custom runner (`src/db/run-migrations.ts`) against the `_migrations` tracking table (the single source of truth per CLAUDE.md). Do NOT use `drizzle-kit migrate` and do NOT edit `meta/_journal.json`.
    7. Run `cd el-templo-api && pnpm test -- test/sessions/sessions.test.ts` then the full `pnpm test` suite. All tests must pass.
  </action>
  <acceptance_criteria>
    - File `el-templo-api/src/db/migrations/0091_rename_level_at_completion_to_session_level.sql` exists and contains `CHANGE COLUMN level_at_completion session_level ENUM('alfa','delta','sigma','omega','spartan') NOT NULL`.
    - `grep -rn "level_at_completion" el-templo-api/src` returns 0 matches.
    - `grep -rn "levelAtCompletion" el-templo-api/src el-templo-api/test` returns 0 matches.
    - `grep -n "sessionLevel" el-templo-api/src/db/schema/completed-sessions.ts` returns at least 1 match.
    - `grep -n "sessionLevel" el-templo-api/src/modules/sessions/routes.ts` returns at least 3 matches (local const + 2 usages).
    - `grep -n "sessionLevel" el-templo-api/src/modules/goal-plans/routes.ts` returns at least 3 matches.
    - `cd el-templo-api && pnpm test` exits 0.
    - `cd el-templo-api && pnpm tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm db:migrate && pnpm tsc --noEmit && pnpm test</automated>
  </verify>
  <done>Column renamed in DB + schema + 4 code sites + 6 test sites; full API suite green; no `level_at_completion` or `levelAtCompletion` string remains in `el-templo-api/src` or `el-templo-api/test`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Admin endpoint GET /admin/members/:userId/session-levels?days=30 + R3/R9/R10/R11 integration tests</name>
  <files>
    el-templo-api/src/modules/members/routes.ts,
    el-templo-api/src/modules/members/schemas.ts,
    el-templo-api/src/modules/members/service.ts,
    el-templo-api/test/members/session-levels.test.ts,
    el-templo-api/test/sessions/sessions.test.ts,
    el-templo-api/test/goal-plans/goal-plans.test.ts
  </files>
  <read_first>
    el-templo-api/src/modules/members/routes.ts (auth preHandler pattern + getMemberById handler shape),
    el-templo-api/src/modules/members/schemas.ts (existing JSON schemas),
    el-templo-api/src/modules/goal-plans/routes.ts (lines 516-655 — /admin/goal-plans/members/:userId precedent with MEMBER_ROLES auth),
    el-templo-api/src/modules/shared/permissions.ts (MEMBER_ROLES / ADMIN_ROLES constants),
    el-templo-api/src/db/schema/completed-sessions.ts (post-rename sessionLevel field — Task 1 must be done first),
    el-templo-api/src/modules/sessions/routes.ts (lines 204-220 ROM collapse on /sessions/daily; 320-324 weekly ROM collapse — context for R3 daily + R10 tests),
    el-templo-api/test/sessions/sessions.test.ts (existing stamp tests — reference shape for new tests; also grep for existing /sessions/daily coverage with `?level=` to decide whether to extend an existing describe block or add a new one),
    el-templo-api/test/goal-plans/goal-plans.test.ts (if exists; otherwise test/goal-plans/ directory — reference shape),
    el-templo-api/test/helpers.ts (auth + request utilities — CLAUDE.md reference),
    CLAUDE.md (new API routes require integration tests)
  </read_first>
  <behavior>
    - `GET /admin/members/:userId/session-levels` authed as admin/coach returns `{ counts: [{ level, count }] }` with only non-zero levels, restricted to rows where `date >= today - days` (default 30).
    - Unauthenticated or member-role caller receives 401/403 (same as `/admin/members/:userId`).
    - Invalid `days` outside [1, 365] is clamped to the nearest boundary OR returns 400; pick clamp per research Example 2 for consistency.
    - Member with 12 sigma + 4 omega completions in last 30 days returns `[{level:"sigma",count:12},{level:"omega",count:4}]` (order not asserted).
    - Member with 0 completions in the window returns `{ counts: [] }`.
    - R3 daily test: `GET /api/sessions/daily?date=<non-Sunday, non-Saturday>&level=omega` authed as a Sigma member returns the Omega-level content for that weekday (server-side override honored; SPEC R3 requires override on weekly AND daily AND goal-plans/session).
    - R9 test: Sigma user on goal-plan week 3 completes a week-3 Omega session (dayId=`GP-<ctx>-W3-lunes-omega`) -> `program_enrollments.currentWeek` advances to 4.
    - R10 test: Saturday date + `?level=omega` on `/sessions/daily` (or weekly with Saturday in window) returns the delta ROM session (effectiveLevel collapsed to delta server-side). Badge/client is not involved here — pure API assertion.
    - ALL existing tests still pass.
  </behavior>
  <action>
    1. Add JSON schema `getMemberSessionLevelsSchema` in `el-templo-api/src/modules/members/schemas.ts`:
       - Params: `{ userId: { type: 'integer', minimum: 1 } }`.
       - Querystring: `{ days: { type: 'integer', minimum: 1, maximum: 365, default: 30 } }`.
       - Response 200: `{ counts: { type: 'array', items: { type: 'object', properties: { level: { enum: ['alfa','delta','sigma','omega','spartan'] }, count: { type: 'integer', minimum: 0 } }, required: ['level','count'], additionalProperties: false } } }`.
       - Response 401/403 via existing error schemas.
       Export the schema.
    2. Add service method `getMemberSessionLevelCounts(db, userId, days)` in `el-templo-api/src/modules/members/service.ts` (colocate with other per-member queries). Implementation (Drizzle):
       ```ts
       const since = new Date(Date.now() - days * 86400000).toISOString().slice(0, 10);
       const rows = await db
         .select({
           level: completedSessions.sessionLevel,
           count: sql<number>`COUNT(*)`.as("count"),
         })
         .from(completedSessions)
         .where(and(
           eq(completedSessions.userId, userId),
           gte(completedSessions.date, since),
         ))
         .groupBy(completedSessions.sessionLevel);
       return rows
         .map((r) => ({ level: r.level, count: Number(r.count) }))
         .filter((r) => r.count > 0);
       ```
       Use Pino logger via `request.log` inside the route handler — never `console.*`. No `any` types (use `TrainingLevel` from shared/training-constants).
    3. Register new route in `el-templo-api/src/modules/members/routes.ts` under the existing members plugin prefix (same prefix as `/:userId` — the `/admin/members` mount is at plugin registration). Use the SAME auth preHandler / role group as the existing `getMemberById` handler. Clamp `days` with `Math.max(1, Math.min(365, request.query.days ?? 30))` (matches research Example 2 for defence-in-depth even though schema validates).
       ```ts
       fastify.get<{ Params: { userId: number }; Querystring: { days?: number } }>(
         "/:userId/session-levels",
         { schema: getMemberSessionLevelsSchema },
         async (request) => {
           const { userId } = request.params;
           const days = Math.max(1, Math.min(365, request.query.days ?? 30));
           const counts = await getMemberSessionLevelCounts(fastify.db, userId, days);
           return { counts };
         },
       );
       ```
    4. Create `el-templo-api/test/members/session-levels.test.ts` with:
       - Seed: insert completed_sessions rows for a test user — 12 with sessionLevel='sigma' within last 30d, 4 with 'omega' within last 30d, 1 with 'alfa' OUTSIDE the 30d window (older).
       - Test A: admin auth returns `counts` containing exactly sigma=12 and omega=4, NO alfa entry (out-of-window), no zero entries. Verify array length === 2.
       - Test B: admin auth with `?days=365` includes the older 'alfa' row -> counts contains 3 entries.
       - Test C: member-role auth returns 401 or 403 (match `getMemberById` behaviour).
       - Test D: userId with zero completions returns `{ counts: [] }`.
       Use `test/helpers.ts` for auth + request setup. Name each test case descriptively (e.g. `'GET /admin/members/:userId/session-levels — returns non-zero level counts within default 30-day window'`) — the grep acceptance_criteria in Plan 99-01 below checks for matching patterns.
    5. Extend `el-templo-api/test/sessions/sessions.test.ts` with BOTH:
       - **R3 (daily override)** test: on a weekday (pick a Monday or Tuesday — NOT Saturday to avoid ROM collapse, NOT Sunday for rest day), a Sigma member hits `GET /api/sessions/daily?date=<weekday>&level=omega`. Assert the response's session payload corresponds to the Omega-level content (e.g. the session id/title or exercise list matches the Omega fixture for that weekday, NOT the Sigma one). The test MUST include `/sessions/daily` and `level=omega` in the request URL; name the test something that matches `/sessions.daily.*level.*omega|daily.*omega.*override/`.
       - **R10 (Saturday ROM collapse)** test: on a Saturday date (pick a fixed Saturday — verify via `new Date('<date>').getDay()===6`), a member with users.level='sigma' hits `/sessions/weekly?weekStart=<saturday-week>&level=omega`. Assert the Saturday cell's session is ROM/delta (matches the existing ROM collapse on sessions/routes.ts:320-324). The test must pass with no server change — it's a regression proof. Name the test to match `/ROM.*omega|Saturday.*level|collapse.*delta/`.
       Both tests must pass with no server change.
    6. Extend `el-templo-api/test/goal-plans/goal-plans.test.ts` (or create if absent; follow sessions.test.ts structure) with R9 test: seed a Sigma user with a program enrollment at `currentWeek=3`, complete `/goal-plans/complete` with `dayId=GP-<programSlug>-W3-lunes-omega`, then SELECT `current_week FROM program_enrollments WHERE user_id=?` and assert it === 4. Also assert the `completed_sessions.session_level` row for that completion equals 'omega'. Name the test to match `/currentWeek.*cross-level|omega.*week.*advance|W3.*W4/`.
    7. Run full API test suite.
  </action>
  <acceptance_criteria>
    - `grep -n "session-levels" el-templo-api/src/modules/members/routes.ts` returns 1 match for the route registration.
    - `grep -n "getMemberSessionLevelsSchema" el-templo-api/src/modules/members/schemas.ts` returns at least 1 export.
    - `grep -n "getMemberSessionLevelCounts" el-templo-api/src/modules/members/service.ts` returns at least 1 export.
    - File `el-templo-api/test/members/session-levels.test.ts` exists with >= 4 test cases: `grep -c "^  it(" el-templo-api/test/members/session-levels.test.ts` returns >= 4 (covering admin 30d, 365d, forbidden-for-member-role, empty case).
    - `cd el-templo-api && pnpm test -- test/members/session-levels.test.ts` passes.
    - `cd el-templo-api && pnpm test -- test/sessions/sessions.test.ts` exits 0, and the file contains:
      - At least one test whose name matches `/ROM.*omega|Saturday.*level|collapse.*delta/` (R10 proof).
      - At least one test asserting `GET /api/sessions/daily?date=<non-Sunday, non-Saturday>&level=omega` returns omega content (R3 daily override) — verify with `grep -En "sessions/daily.*level=omega|daily.*\\?.*level.*omega|level=omega.*daily" el-templo-api/test/sessions/sessions.test.ts` returning >= 1 match, AND a test name matching `/daily.*omega|omega.*daily.*override|R3.*daily/i`.
    - `cd el-templo-api && pnpm test -- test/goal-plans/goal-plans.test.ts` passes, and the file contains at least 1 test whose name matches `/currentWeek.*cross-level|omega.*week.*advance|W3.*W4/`.
    - Full API test suite exits 0: `cd el-templo-api && pnpm test`.
    - `cd el-templo-api && pnpm tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm tsc --noEmit && pnpm test</automated>
  </verify>
  <done>New admin endpoint live behind same auth as `/admin/members/:userId`; R3 (daily override), R9 (cross-level goal-plan advance), R10 (ROM Saturday collapse), R11 (admin endpoint — 4+ cases) integration tests green; full suite green; endpoint shape matches the locked SPEC R11 acceptance criteria (non-zero levels only, last-N-days window, `{ counts: [...] }` response).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                              | Description                                             |
| ----------------------------------------------------- | ------------------------------------------------------- |
| admin client -> /admin/members/:userId/session-levels | Admin-authed request; `userId` param is untrusted input |
| member client -> /sessions/weekly?level=              | Authed member request; `level` query is untrusted input |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                  | Disposition | Mitigation Plan                                                                                                                                                   |
| --------- | ---------------------- | ---------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-99-01   | Tampering              | `GET /admin/members/:userId/session-levels` `userId` param | mitigate    | JSON schema validates `userId` as positive integer; Drizzle param binding prevents SQLi; handler reads userId straight from params (no string concat).            |
| T-99-02   | Tampering              | `GET /admin/members/:userId/session-levels` `days` query   | mitigate    | Schema bounds [1,365]; runtime `Math.max(1, Math.min(365, ...))` clamp as defense-in-depth; used only in `Date.now() - days*86400000` arithmetic (no SQL concat). |
| T-99-03   | Information Disclosure | Admin endpoint exposes per-member training history         | mitigate    | Same auth preHandler as existing `/admin/members/:userId` (MEMBER_ROLES — admin/coach/owner/recepcionista); no PII beyond counts and levels.                      |
| T-99-04   | Elevation of Privilege | Regular member role calls admin endpoint                   | mitigate    | Route registered under admin prefix; preHandler rejects non-MEMBER_ROLES with 401/403. Integration test C proves it.                                              |
| T-99-05   | Tampering              | `?level=` query on content reads                           | mitigate    | Existing Phase-1 validation via `isTrainingLevel` guard rejects unknown values with 400 (already covered by existing tests; preserved through rename).            |
| T-99-06   | Repudiation            | Migration 0091 applied partially across envs               | accept      | Single-DDL `CHANGE COLUMN` is atomic on MySQL 8; `_migrations` table records the run; rollback = reverse `CHANGE COLUMN`. Low risk for internal project.          |

</threat_model>

<verification>
1. `cd el-templo-api && pnpm db:migrate` applies 0091 on local + test DB.
2. `grep -rn "level_at_completion\|levelAtCompletion" el-templo-api/src el-templo-api/test` returns 0 matches.
3. `cd el-templo-api && pnpm tsc --noEmit` passes.
4. `cd el-templo-api && pnpm test` passes (all existing + new tests for R3 daily, R9, R10, R11).
5. Per-file new-case assertions (see acceptance_criteria in Task 2): `grep -c "^  it("` on the new `test/members/session-levels.test.ts` returns >= 4; both the `/sessions/daily` omega test and the `/sessions/weekly` ROM Saturday test exist in `test/sessions/sessions.test.ts` with matching name patterns; the cross-level week-advance test exists in `test/goal-plans/goal-plans.test.ts`.
6. Manual sanity: `curl -H "Authorization: Bearer <admin>" http://localhost:3000/admin/members/1/session-levels?days=30` returns `{ "counts": [...] }`.
</verification>

<success_criteria>

- Migration 0091 exists, applied, and recorded in `_migrations` on local + test DBs.
- `completed_sessions.session_level` column exists; `level_at_completion` no longer exists in schema OR runtime.
- Drizzle field `sessionLevel` used consistently across `sessions/routes.ts`, `goal-plans/routes.ts`, tests.
- New admin endpoint `GET /admin/members/:userId/session-levels?days=30` returns correct per-level counts under admin auth; rejects member auth.
- New integration tests (per-file new-case assertions, not a brittle total-count threshold):
  - `test/members/session-levels.test.ts`: >= 4 new `it(...)` cases (R11).
  - `test/sessions/sessions.test.ts`: >= 1 new case asserting `/sessions/daily?level=omega` override (R3 daily); >= 1 new case asserting ROM Saturday collapse with `?level=omega` (R10).
  - `test/goal-plans/goal-plans.test.ts`: >= 1 new case asserting `currentWeek` advance on cross-level completion (R9).
- Full API test suite green.
- `pnpm tsc --noEmit` clean; no `any` types introduced.
- No `console.*` in new code — only `request.log` / `app.log`.
  </success_criteria>

<output>
Create `.planning/phases/99-member-selectable-training-level/99-01-SUMMARY.md` documenting:
- Rename call-site count (files touched)
- Migration file path + DDL summary (hand-authored, applied via custom runner per CLAUDE.md)
- New endpoint path + response shape
- New tests added (R3 daily, R9, R10, R11) with file paths and `it(...)` names
- Commit hash
</output>
</output>
