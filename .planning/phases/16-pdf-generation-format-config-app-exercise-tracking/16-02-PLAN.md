---
phase: 16-pdf-generation-format-config-app-exercise-tracking
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - el-templo-admin/src/components/sessions/FormatParamsEditor.vue
  - el-templo-admin/src/components/sessions/EditableBlockCard.vue
  - el-templo-api/src/modules/admin/edit-service.ts
  - el-templo-api/src/modules/admin/routes.ts
autonomous: true

must_haves:
  truths:
    - "Coach can view and edit format parameters for any block in the session edit page"
    - "Format parameter changes persist to the database and survive page reload"
    - "Changing block format resets formatParams to new format defaults"
  artifacts:
    - path: "el-templo-admin/src/components/sessions/FormatParamsEditor.vue"
      provides: "Format-specific parameter editing UI component"
      min_lines: 50
    - path: "el-templo-api/src/modules/admin/routes.ts"
      provides: "PATCH endpoint for updating formatParams"
      contains: "formatParams"
  key_links:
    - from: "el-templo-admin/src/components/sessions/FormatParamsEditor.vue"
      to: "/admin/sessions/:id/blocks/:blockId/format-params"
      via: "editApi PATCH call"
      pattern: "formatParams"
    - from: "el-templo-api/src/modules/admin/edit-service.ts"
      to: "session_blocks.formatParams"
      via: "Drizzle update"
      pattern: "formatParams"
---

<objective>
Add format parameter editing UI and API endpoint so coaches can view and adjust format-specific parameters (AMRAP minutes, EMOM intervals, Complex rounds, etc.) during session editing.

Purpose: SC #4 and #5 require format parameters to be settable during session generation (done in Plan 01) AND editing (this plan). Coaches need a UI to adjust parameters and an API to persist changes.
Output: FormatParamsEditor component, PATCH API endpoint, integration in EditableBlockCard.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-pdf-generation-format-config-app-exercise-tracking/16-01-SUMMARY.md
@el-templo-admin/src/components/sessions/EditableBlockCard.vue
@el-templo-api/src/modules/admin/edit-service.ts
@el-templo-api/src/modules/admin/routes.ts
@el-templo-api/src/modules/admin/format-params.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add formatParams PATCH API endpoint and edit service method</name>
  <files>
    el-templo-api/src/modules/admin/edit-service.ts
    el-templo-api/src/modules/admin/routes.ts
  </files>
  <action>
1. In `edit-service.ts`, add a method `updateFormatParams(params: { sessionId: number; blockId: number; formatParams: Record<string, unknown>; userId: number })`:
   - Verify session exists and block belongs to session (same pattern as other edit methods)
   - Update `session_blocks.formatParams` for the given blockId via Drizzle `update().set({ formatParams }).where(eq(id, blockId))`
   - Log the edit in the edit_logs table (same pattern as existing edit logging)
   - Return the updated block row

2. Also update the existing `changeBlockFormat` method: after changing the format, also set formatParams to the defaults for the new format. Import `getDefaultFormatParams` from `./format-params`. Call it with the new format name and block context (intensity, exerciseCount from the block row). This ensures format changes reset params to sensible defaults.

3. In `routes.ts`, add a new route:
   ```
   PATCH /admin/sessions/:sessionId/blocks/:blockId/format-params
   Body: { formatParams: object }
   ```
   Use the same authentication and admin check patterns as existing editing routes. Call `editService.updateFormatParams(...)`. Return the updated formatParams.

4. Add the route schema in the schemas section (same pattern as other edit schemas).
  </action>
  <verify>
Start the dev server and test with curl:
`curl -X PATCH http://localhost:3000/admin/sessions/{id}/blocks/{blockId}/format-params -H "Authorization: Bearer {token}" -H "Content-Type: application/json" -d '{"formatParams": {"type": "amrap", "minutes": 12}}'`
Should return 200 with updated formatParams.
  </verify>
  <done>PATCH /admin/sessions/:id/blocks/:blockId/format-params endpoint works. changeBlockFormat also resets formatParams to new format defaults. Edit logged.</done>
</task>

<task type="auto">
  <name>Task 2: Create FormatParamsEditor component and integrate into EditableBlockCard</name>
  <files>
    el-templo-admin/src/components/sessions/FormatParamsEditor.vue
    el-templo-admin/src/components/sessions/EditableBlockCard.vue
    el-templo-admin/src/composables/useEditApi.ts
  </files>
  <action>
1. Create `FormatParamsEditor.vue` -- a component that renders format-specific input fields based on the `type` discriminator:
   - Props: `formatParams: object | null`, `formatName: string`, `blockId: number`, `sessionId: number`
   - Emits: `update:formatParams` when params change
   - For each format type, render appropriate Quasar inputs:
     - AMRAP: QInput type=number for `minutes` (label "Minutos", min 1, max 30)
     - EMOM: Two QInputs for `intervalSeconds` (label "Intervalo (seg)") and `totalMinutes` (label "Minutos totales")
     - Complex: QInput for `rounds` (label "Rondas", min 1, max 10)
     - Tabata: QInputs for `workSeconds`, `restSeconds`, `rounds`
     - Interval: QInputs for `workSeconds`, `restSeconds`, `rounds`
     - For Time: Optional QInput for `timeCapMinutes` (label "Tiempo limite (min)", can be empty)
     - Ladder: QSelect for `direction` with options ascending/descending
     - Cluster: QInputs for `clusterSize` and `restBetweenClusters`
     - Time Cap: QInput for `minutes`
     - Buy-in/Cash-out: Optional QInput for `rounds`
     - Chipper: QInput for `rounds`
     - Standard/Unbroken/Couplet/Triplet/For Max: Show "Sin parametros configurables" text
   - Use blur-save pattern: on blur of any input, if value changed, emit `update:formatParams` with the full params object
   - Show compact inline layout (row with q-gutter-sm) to fit within block card
   - If `formatParams` is null (pre-Phase 16 sessions), show a small "Configurar parametros" button that initializes defaults

2. In `EditableBlockCard.vue`:
   - Import and add `<format-params-editor>` below the block stats section (below contraction-mix-badge)
   - Pass `block.formatParams`, `block.formatName`, `block.id`, `sessionId` as props
   - On `@update:formatParams`, call `editApi.updateFormatParams(sessionId, blockId, newParams)` (see step 3)
   - Show green success toast on save, no page reload (consistent with SC #11)

3. In `useEditApi.ts`, add method:
   `updateFormatParams(sessionId: number, blockId: number, formatParams: object): Promise<any>`
   Calls PATCH `/admin/sessions/${sessionId}/blocks/${blockId}/format-params` with `{ formatParams }`.
  </action>
  <verify>
Run `cd /home/franco/projects/el-templo/el-templo-admin && npx quasar build` -- should compile without errors. Then start dev server and navigate to a session edit page. Blocks with recognized formats should show parameter inputs. Changing a value and blurring should save without page reload.
  </verify>
  <done>FormatParamsEditor renders format-specific inputs. Coach can edit format params per block. Changes persist via API without page reload. Format change resets params to defaults. Pre-Phase 16 sessions with null formatParams show "Configurar parametros" button.</done>
</task>

</tasks>

<verification>
- Format params editable in admin UI for all HIGH/MEDIUM formats
- Changes persist and survive page reload
- Format change resets params to new format defaults
- Null formatParams handled gracefully
- No scroll reset on param save
</verification>

<success_criteria>
Coach can view, edit, and save format-specific parameters for any block. Changing the block format resets parameters to new defaults. Pre-existing sessions with null formatParams show initialization option.
</success_criteria>

<output>
After completion, create `.planning/phases/16-pdf-generation-format-config-app-exercise-tracking/16-02-SUMMARY.md`
</output>
