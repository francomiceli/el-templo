---
phase: 16-pdf-generation-format-config-app-exercise-tracking
plan: 06
type: execute
wave: 2
depends_on: ["16-05"]
files_modified:
  - el-templo-app/src/modules/training/components/player/ExerciseList.vue
  - el-templo-app/src/modules/training/components/player/ExerciseCard.vue
  - el-templo-app/src/modules/training/pages/DayPlayer.vue
  - el-templo-app/src/modules/training/composables/useSessionCompletion.ts
  - el-templo-api/src/db/schema/completed-sessions.ts
  - el-templo-api/src/modules/sessions/routes.ts
autonomous: true

must_haves:
  truths:
    - "Each exercise in DayPlayer shows a completion checkmark that members can tap"
    - "Completed exercises are visually distinct (checkmark, muted styling)"
    - "Exercise completion data is submitted to the server on session finish"
    - "Weekly View shows day as complete only when session completion is submitted"
  artifacts:
    - path: "el-templo-app/src/modules/training/components/player/ExerciseCard.vue"
      provides: "Per-exercise completion checkbox/checkmark UI"
      contains: "complete"
    - path: "el-templo-api/src/db/schema/completed-sessions.ts"
      provides: "exercisesCompleted JSON column"
      contains: "exercisesCompleted"
  key_links:
    - from: "el-templo-app/src/modules/training/pages/DayPlayer.vue"
      to: "el-templo-app/src/modules/training/composables/useSessionPlayer.ts"
      via: "toggleExerciseComplete from Plan 05"
      pattern: "toggleExerciseComplete"
    - from: "el-templo-app/src/modules/training/composables/useSessionCompletion.ts"
      to: "/api/sessions/complete"
      via: "POST with exercisesCompleted payload"
      pattern: "exercisesCompleted"
---

<objective>
Wire per-exercise completion into the DayPlayer UI, add checkmarks to exercise cards, and extend the session completion API to accept exercise-level completion data.

Purpose: SC #7, #8, and #9 require visual exercise completion in DayPlayer, auto-advance on all-complete, and consistent state across views. Plan 05 built the data layer; this plan wires UI and API.
Output: Exercise checkmarks in DayPlayer, completion data in API payload, exercisesCompleted column in DB.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-pdf-generation-format-config-app-exercise-tracking/16-05-SUMMARY.md
@el-templo-app/src/modules/training/components/player/ExerciseList.vue
@el-templo-app/src/modules/training/components/player/ExerciseCard.vue
@el-templo-app/src/modules/training/pages/DayPlayer.vue
@el-templo-app/src/modules/training/composables/useSessionCompletion.ts
@el-templo-api/src/db/schema/completed-sessions.ts
@el-templo-api/src/modules/sessions/routes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add exercise completion UI to DayPlayer components</name>
  <files>
    el-templo-app/src/modules/training/components/player/ExerciseCard.vue
    el-templo-app/src/modules/training/components/player/ExerciseList.vue
    el-templo-app/src/modules/training/pages/DayPlayer.vue
  </files>
  <action>
1. In `ExerciseCard.vue`:
   - Add a new prop: `completed: boolean` (default false)
   - Add a new emit: `toggle-complete`
   - Add a checkmark button/icon in the exercise card:
     - Position: right side of the card, vertically centered
     - When NOT completed: outline circle icon (`radio_button_unchecked`), subtle grey color
     - When completed: filled check circle icon (`check_circle`), green color
     - On tap: emit `toggle-complete`
   - When completed, add visual styling: reduce opacity of exercise details to 0.5, add a subtle strikethrough or muted text on the exercise name
   - Use a smooth transition for the completion state change (0.3s ease)

2. In `ExerciseList.vue`:
   - Add props: `completedExercises: number[]` (array of completed prescription IDs)
   - Add emit: `toggle-exercise-complete` with payload `{ prescriptionId: number }`
   - Pass `completed` prop to each ExerciseCard: `:completed="completedExercises.includes(exercise.id)"`
   - Wire `@toggle-complete` on each card to emit `toggle-exercise-complete` with the prescription ID

3. In `DayPlayer.vue`:
   - Destructure `toggleExerciseComplete`, `isExerciseComplete`, `completedExercises`, `completedExerciseCount`, `totalExerciseCount` from useSessionPlayer (these are the exports added in Plan 05)
   - Pass completedExercises for the current block to ExerciseList:
     ```
     :completed-exercises="completedExercises[currentBlock?.role ?? ''] ?? []"
     ```
   - Wire `@toggle-exercise-complete` to call `toggleExerciseComplete(prescriptionId)`
   - Add a progress indicator showing `completedExerciseCount / totalExerciseCount` near the block header or progress bar
   - The existing "Listo" / block complete button can remain, but now auto-advance will fire when all exercises are checked (from Plan 05 logic). The manual button serves as a "skip" for coaches/power users who don't want to check each exercise.
  </action>
  <verify>
Run `cd /home/franco/projects/el-templo/el-templo-app && npx quasar build` -- compiles without errors. Start dev server, navigate to a session's DayPlayer. Each exercise should show an unchecked circle. Tapping it should show a green checkmark and mute the exercise text. Checking all exercises in a block should auto-advance to the next block.
  </verify>
  <done>Exercise cards show completion checkmarks. Tapping toggles complete/incomplete. Completed exercises are visually muted. All-complete triggers auto-advance. Manual "Listo" button still works as skip.</done>
</task>

<task type="auto">
  <name>Task 2: Extend session completion API to accept and store exercisesCompleted</name>
  <files>
    el-templo-api/src/db/schema/completed-sessions.ts
    el-templo-api/src/modules/sessions/routes.ts
    el-templo-app/src/modules/training/composables/useSessionCompletion.ts
  </files>
  <action>
1. In `completed-sessions.ts`, add a new column:
   ```typescript
   exercisesCompleted: json('exercises_completed'),  // nullable for backward compat
   ```
   Format: `{ "NUCLEUS": [123, 456], "DEUTEROS_1": [789] }` -- maps block role to array of prescription IDs completed.

2. Create and run a migration to add the `exercises_completed` column to the `completed_sessions` table. Use the same migration pattern as previous phases:
   ```sql
   ALTER TABLE completed_sessions ADD COLUMN exercises_completed JSON;
   ```
   Run via `mysql` CLI or Drizzle push.

3. In the sessions completion route (find it -- likely `POST /sessions/complete` or similar in `el-templo-api/src/modules/sessions/routes.ts`):
   - Accept optional `exercisesCompleted` field in the request body (JSON object)
   - Add it to the INSERT into `completed_sessions` alongside existing fields
   - Keep `blocksCompleted` as-is for backward compatibility
   - Validate that exercisesCompleted is an object (if provided)

4. In `useSessionCompletion.ts` in the member app:
   - Read the composable to understand how it currently submits completion data
   - Add `exercisesCompleted` to the payload sent to the API
   - Source the data from `sessionPlayerStore.loadProgress(dayId)` -- the `completedExercises` record
   - The payload should include both `blocksCompleted` (array of role strings) AND `exercisesCompleted` (record of role -> prescription IDs)
   - This maintains backward compatibility: the server gets both, existing code reading blocksCompleted still works

5. Ensure the Weekly View consistency (SC #9): The Weekly View checks server-side completion status, which is set by the existing completion flow. Since we're extending (not replacing) the existing flow, the Weekly View will continue to work correctly -- a day is marked complete when the completion API is called, regardless of whether it includes exercisesCompleted or not.
  </action>
  <verify>
Run the migration. Then test the completion flow:
1. Build the app: `cd /home/franco/projects/el-templo/el-templo-app && npx quasar build`
2. Start the API server
3. Complete a session in the member app (or via curl)
4. Query: `SELECT exercises_completed FROM completed_sessions ORDER BY id DESC LIMIT 1;` -- should show the JSON with exercise IDs
  </verify>
  <done>completed_sessions table has exercisesCompleted column. Session completion API accepts and stores per-exercise data. Member app submits completedExercises alongside blocksCompleted. Weekly View remains consistent.</done>
</task>

</tasks>

<verification>
- Exercise checkmarks visible in DayPlayer for each exercise
- Tapping checkmark toggles visual state
- All exercises checked -> block auto-completes
- Session completion submits exercisesCompleted to API
- Database stores exercisesCompleted JSON
- Weekly View shows correct completion state
- Both apps build without errors
</verification>

<success_criteria>
Members can tap exercises to mark them complete. Auto-advance fires on all-complete. Completion data includes exercise-level detail in the API payload and database. Weekly View consistency maintained.
</success_criteria>

<output>
After completion, create `.planning/phases/16-pdf-generation-format-config-app-exercise-tracking/16-06-SUMMARY.md`
</output>
