---
phase: 16-pdf-generation-format-config-app-exercise-tracking
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-admin/src/components/sessions/ExerciseSwapDialog.vue
  - el-templo-api/src/modules/admin/edit-service.ts
  - el-templo-api/src/modules/admin/routes.ts
autonomous: true

must_haves:
  truths:
    - "Exercise swap dialog groups exercises by category field instead of first word of exercise name"
    - "Category pills show fewer, semantically meaningful groups"
  artifacts:
    - path: "el-templo-admin/src/components/sessions/ExerciseSwapDialog.vue"
      provides: "Category-based filtering pills"
      contains: "category"
  key_links:
    - from: "el-templo-admin/src/components/sessions/ExerciseSwapDialog.vue"
      to: "pool API response"
      via: "exercise.category field"
      pattern: "category"
---

<objective>
Switch the exercise swap dialog from grouping by first word of exercise name to grouping by the `category` field from the exercises table.

Purpose: SC #6 requires category-based grouping for "fewer, cleaner pill selections." Currently the dialog uses `exerciseGroup()` which extracts the first word of the exercise name (e.g., "HT", "P.U", "LUNGE"), producing many small groups. The `category` field (e.g., "Press", "Sentadilla", "Tiron") provides more meaningful, fewer groups.
Output: Updated ExerciseSwapDialog with category pills, pool API returns category field.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@el-templo-admin/src/components/sessions/ExerciseSwapDialog.vue
@el-templo-api/src/modules/admin/edit-service.ts
@el-templo-api/src/modules/admin/routes.ts
@el-templo-api/src/db/schema/exercises.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add category to pool API response and switch dialog to category grouping</name>
  <files>
    el-templo-api/src/modules/admin/edit-service.ts
    el-templo-admin/src/components/sessions/ExerciseSwapDialog.vue
  </files>
  <action>
1. In `edit-service.ts`, find the `getExercisePool` method. In the SELECT query that returns pool exercises, add `category: schema.exercises.category` to the selected fields. This ensures each pool exercise has a `category` string alongside the existing `exercise`, `effort`, `dificultadLineal`, `route`, `pattern` fields.

2. In `ExerciseSwapDialog.vue`:
   a. Update the `PoolExercise` type usage (or the import from session types) to include `category: string`.
   b. Remove the `exerciseGroup()` function that extracts the first word of exercise name.
   c. Change the `patternChips` computed to group by `ex.category` instead of `exerciseGroup(ex.exercise)`:
      ```typescript
      const categoryChips = computed<PatternChip[]>(() => {
        const counts = new Map<string, number>();
        for (const ex of contractionFiltered.value) {
          const cat = ex.category || 'Sin categoria';
          counts.set(cat, (counts.get(cat) || 0) + 1);
        }
        const chips: PatternChip[] = [
          { name: '', label: 'Todos', count: contractionFiltered.value.length }
        ];
        const sorted = [...counts.entries()].sort(([, ca], [, cb]) => cb - ca);
        for (const [name, count] of sorted) {
          chips.push({ name, label: name, count });
        }
        return chips;
      });
      ```
   d. Rename `selectedGroup` to `selectedCategory` for clarity.
   e. Update the section label from "Patron" to "Categoria".
   f. Update `displayedExercises` computed to filter by `ex.category` instead of `exerciseGroup(ex.exercise)`.
   g. Update the watcher that resets group selection on contraction change to check if selectedCategory still exists in the category chips.
   h. On dialog open, in add mode default selectedCategory to '' (all). In swap mode, default to the current exercise's category if available, or '' if not.

3. Also update the `PoolExercise` type definition in `el-templo-admin/src/types/session.ts` to include `category: string` if it's defined there. Check the actual type file location first.
  </action>
  <verify>
Run `cd /home/franco/projects/el-templo/el-templo-admin && npx quasar build` -- should compile without errors. Start the admin dev server, navigate to a session edit page, open the swap dialog for any exercise. The filter section should show "Categoria" with category-based chips (e.g., "Press (12)", "Sentadilla (8)") instead of first-word groups (e.g., "HT (3)", "P.U (2)").
  </verify>
  <done>Exercise swap dialog uses category field for grouping pills. Pool API returns category. Chips show semantically meaningful groups. Fewer pills than the previous first-word approach.</done>
</task>

</tasks>

<verification>
- Category pills show meaningful groups from exercises.category field
- Pool API response includes category string
- Contraction filter + category filter + search all work together
- Swap and add modes both work with new category grouping
- Build compiles without TypeScript errors
</verification>

<success_criteria>
Exercise swap dialog shows category-based pills instead of first-word-of-name pills. The number of filter pills is noticeably fewer and more semantically meaningful.
</success_criteria>

<output>
After completion, create `.planning/phases/16-pdf-generation-format-config-app-exercise-tracking/16-03-SUMMARY.md`
</output>
