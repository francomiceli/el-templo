---
phase: 118-analytics-estrat-gico-funnel-de-conversi-n-retenci-n-por-cic
plan: 06
type: execute
wave: 4
depends_on: ["118-02", "118-03", "118-04", "118-05"]
files_modified:
  - el-templo-admin/src/composables/useAnalyticsApi.ts
  - el-templo-admin/src/types/analytics.ts
  - el-templo-admin/src/pages/AnaliticasPage.vue
  - el-templo-admin/src/components/analytics/FunnelTab.vue
  - el-templo-admin/src/components/analytics/RetencionTab.vue
  - el-templo-admin/src/components/analytics/FinanzasAvanzadasTab.vue
autonomous: false
requirements: [D-01, D-08, D-10, D-11]
user_setup: []
must_haves:
  truths:
    - "AnaliticasPage.vue muestra 3 tabs nuevas (Funnel / Retención / Finanzas avanzadas) con su propio espacio, lazy-loaded vía watch(activeTab)"
    - "FunnelTab muestra el funnel freemium→prueba→activo + medianas por etapa + banner de caveat permanente de ramp-up"
    - "RetencionTab muestra la curva multi-cohorte por ciclo N (no meses) + distribución de ciclos + filtro plan_category + banner de caveat permanente"
    - "FinanzasAvanzadasTab muestra Caja vs Devengado en gráficos separados POR MONEDA (ARS/EUR nunca sumadas) + ARPU por moneda + caption de exclusión cuando N>0"
    - "useAnalyticsApi tiene getFunnel/getRetention/getAdvancedFinance; getEngagement se conserva (D-09)"
    - "Los gráficos usan chart.js/vue-chartjs (ya instalados), colores de chart-colors.ts (sin hex hardcodeados), formato de moneda con format-price.ts"
    - "No se agregan FinanzasTab/MiembrosTab — las 3 tabs son componentes separados (D-10)"
  artifacts:
    - path: "el-templo-admin/src/components/analytics/FunnelTab.vue"
      provides: "Tab de funnel"
    - path: "el-templo-admin/src/components/analytics/RetencionTab.vue"
      provides: "Tab de retención por ciclos"
    - path: "el-templo-admin/src/components/analytics/FinanzasAvanzadasTab.vue"
      provides: "Tab de caja vs devengado + ARPU por moneda"
  key_links:
    - from: "AnaliticasPage.vue fetchTabData"
      to: "useAnalyticsApi getFunnel/getRetention/getAdvancedFinance"
      via: "switch(activeTab) + lazy watch"
      pattern: "getFunnel|getRetention|getAdvancedFinance"
    - from: "tabs nuevas"
      to: "chart-colors.ts + format-price.ts"
      via: "COLORS/chartColors + formatPrice"
      pattern: "chart-colors"
---

<objective>
Construir las 3 tabs nuevas admin-only en AnaliticasPage.vue (D-10), cada una consumiendo su
endpoint del backend (Plans 02/03/04): Funnel, Retención, Finanzas avanzadas. El contrato visual
es `118-UI-SPEC.md` (autoritativo para copy/colores/layout). Analog de viz: FinanzasTab.vue.

Decisiones LOCKED:

- D-10: 3 tabs separadas, componentes propios (NO engordar FinanzasTab/MiembrosTab). chart.js +
  vue-chartjs ya instalados — sin dependencias nuevas.
- D-01: banner de caveat permanente de ramp-up en Funnel y Retención (textos verbatim en UI-SPEC).
- D-08: Caja vs Devengado en gráficos separados por moneda; ARPU por moneda; ARS/EUR nunca sumadas.
- D-11: endpoints admin-only (ya enforced en backend); las tabs viven en AnaliticasPage (admin).

Purpose: tableros estratégicos/financieros que el equipo usa para decidir.
Output: composable + tipos + página + 3 componentes de tab + verificación visual.

NOTA: este plan tiene un checkpoint de verificación visual (autonomous: false).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/118-analytics-estrat-gico-funnel-de-conversi-n-retenci-n-por-cic/118-CONTEXT.md
@.planning/phases/118-analytics-estrat-gico-funnel-de-conversi-n-retenci-n-por-cic/118-UI-SPEC.md
@.planning/phases/118-analytics-estrat-gico-funnel-de-conversi-n-retenci-n-por-cic/118-PATTERNS.md
@.planning/phases/118-analytics-estrat-gico-funnel-de-conversi-n-retenci-n-por-cic/118-02-SUMMARY.md
@.planning/phases/118-analytics-estrat-gico-funnel-de-conversi-n-retenci-n-por-cic/118-03-SUMMARY.md
@.planning/phases/118-analytics-estrat-gico-funnel-de-conversi-n-retenci-n-por-cic/118-04-SUMMARY.md

<interfaces>
<!-- Contratos del frontend a reutilizar. -->

De useAnalyticsApi.ts (líneas 104-152): patrón getUniqueMembers/getCheckInAdoption/getEngagement —
try/catch/finally + buildParams(filters) + extractError(err, '...'); agregar al return (159-170).
KEEP getEngagement (D-09).

De AnaliticasPage.vue: q-tabs (~144-147) + q-tab-panels (~150-159) + watch(activeTab) +
fetchTabData() switch (~503-525). currentFilters computed (~375-380). displayCurrency.
Refs de datos+loading (~388-396). Fetch fns (~479-489).

De FinanzasTab.vue: shell loading→no-data→content (1-9); chart card + wrapper fijo
`style="height: 300px; position: relative"` (13-23); ChartJS.register por archivo (99-118);
multi-moneda nunca sumada (163-216); per-currency zero card (220-230); options
`responsive:true, maintainAspectRatio:false` (144-145).

De src/utils/chart-colors.ts: COLORS, chartColors (NUNCA hardcodear hex).
De src/utils/format-price.ts: formatPrice(value, 'ARS'|'EUR').
De src/utils/logger: createLogger().error() (forward a Sentry).

UI-SPEC (118-UI-SPEC.md) es autoritativo: tab labels/icons (Funnel/filter_alt, Retención/replay,
Finanzas avanzadas/trending_up), títulos de sección (text-subtitle2), textos de banners de caveat
(verbatim), copy de empty states, caption de exclusión, error state sin toast.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useAnalyticsApi (3 métodos) + tipos frontend + wiring de 3 tabs en AnaliticasPage</name>
  <files>el-templo-admin/src/composables/useAnalyticsApi.ts, el-templo-admin/src/types/analytics.ts, el-templo-admin/src/pages/AnaliticasPage.vue</files>
  <read_first>
    - el-templo-admin/src/composables/useAnalyticsApi.ts (patrón getUniqueMembers/getEngagement + return)
    - el-templo-admin/src/types/analytics.ts (estilo de tipos de respuesta + AnalyticsFilters)
    - el-templo-admin/src/pages/AnaliticasPage.vue (q-tabs, q-tab-panels, fetchTabData, currentFilters, refs)
    - .planning/phases/118-analytics-estrat-gico-funnel-de-conversi-n-retenci-n-por-cic/118-02-SUMMARY.md (shape de RetentionAnalytics)
    - .planning/phases/118-analytics-estrat-gico-funnel-de-conversi-n-retenci-n-por-cic/118-03-SUMMARY.md (shape de AdvancedFinanceAnalytics)
    - .planning/phases/118-analytics-estrat-gico-funnel-de-conversi-n-retenci-n-por-cic/118-04-SUMMARY.md (shape de FunnelAnalytics)
    - .planning/phases/118-analytics-estrat-gico-funnel-de-conversi-n-retenci-n-por-cic/118-UI-SPEC.md (tab labels/icons)
  </read_first>
  <action>
    En useAnalyticsApi.ts agregar `getFunnel(filters)`, `getRetention(filters)` (acepta planCategory
    opcional) y `getAdvancedFinance(filters)`, copiando el patrón exacto de getUniqueMembers
    (try/catch/finally + buildParams + extractError con verbo en español) y agregarlos al objeto de
    retorno. CONSERVAR getEngagement (D-09).

    En src/types/analytics.ts agregar los tipos de respuesta del frontend que reflejan los shapes que
    devuelven los endpoints de Plans 02/03/04 (FunnelAnalytics, RetentionAnalytics,
    AdvancedFinanceAnalytics) — leer los SUMMARYs para el shape exacto.

    En AnaliticasPage.vue agregar 3 `<q-tab>` (Funnel/filter_alt, Retención/replay, Finanzas
    avanzadas/trending_up) + 3 `<q-tab-panel>` que renderizan los nuevos componentes (Task 2) pasando
    `:loading`, `:data` y `:currency="displayCurrency"`. Agregar los refs de datos+loading (mirror de
    los existentes), las fetch fns (copiar las existentes), y los casos nuevos en el switch de
    fetchTabData. Importar los 3 componentes nuevos. Inherit de filtros globales vía currentFilters.

  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm exec vue-tsc --noEmit 2>&1 | grep -iE "AnaliticasPage|useAnalyticsApi|types/analytics" | head; grep -c 'getFunnel\|getRetention\|getAdvancedFinance' src/composables/useAnalyticsApi.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'getFunnel\|getRetention\|getAdvancedFinance' src/composables/useAnalyticsApi.ts` muestra los 3 métodos
    - Source assertion: `grep -c 'getEngagement' src/composables/useAnalyticsApi.ts` >= 1 (conservado)
    - Source assertion: AnaliticasPage tiene 3 q-tab nuevas (grep de los name/label de las tabs)
    - vue-tsc limpio
  </acceptance_criteria>
  <done>3 métodos nuevos en el composable (getEngagement conservado), tipos frontend agregados, 3 tabs cableadas con lazy-load en AnaliticasPage; type-check limpio.</done>
</task>

<task type="auto">
  <name>Task 2: FunnelTab.vue + RetencionTab.vue + FinanzasAvanzadasTab.vue</name>
  <files>el-templo-admin/src/components/analytics/FunnelTab.vue, el-templo-admin/src/components/analytics/RetencionTab.vue, el-templo-admin/src/components/analytics/FinanzasAvanzadasTab.vue</files>
  <read_first>
    - el-templo-admin/src/components/analytics/FinanzasTab.vue (archivo completo — analog de viz: shell, chart card, ChartJS.register, multi-moneda, per-currency zero, options)
    - el-templo-admin/src/utils/chart-colors.ts (COLORS, chartColors)
    - el-templo-admin/src/utils/format-price.ts (formatPrice)
    - .planning/phases/118-analytics-estrat-gico-funnel-de-conversi-n-retenci-n-por-cic/118-UI-SPEC.md (contrato visual completo — textos de banners verbatim, títulos, empty states, caption de exclusión, error state)
    - el-templo-admin/src/types/analytics.ts (tipos del Task 1)
  </read_first>
  <action>
    Crear los 3 componentes en src/components/analytics/. Todos siguen el shell de FinanzasTab
    (loading→no-data→content), wrapper de chart fijo `style="height: 300px; position: relative"`,
    options `responsive:true, maintainAspectRatio:false`, ChartJS.register por archivo (solo los
    elementos que cada uno usa: Retención necesita LineElement+PointElement para `<Line>`;
    Funnel/Finanzas usan BarElement). Props `{ data, loading, currency }`. Colores de chart-colors.ts
    (NUNCA hex). Error state: NO toast — `catch (err:unknown)` → createLogger().error() → data null →
    empty state. Copy/títulos/banners verbatim del UI-SPEC.

    FunnelTab.vue: banner de caveat permanente (texto del UI-SPEC seccion Funnel banner body) +
    `q-banner dense rounded class="bg-orange-2 text-orange-10 q-mb-md"` con `#avatar` slot
    `<q-icon name="info" color="orange-9" />`. Gráfico Bar (funnel/embudo, 3 etapas freemium→prueba→
    activo con COLORS.primary/secondary/accent) + fila de metric cards con medianas por etapa
    (`text-h4 text-primary` valor + `text-caption text-grey-7` label). Empty state:
    "Aún no hay cohortes con datos de conversión en este alcance".

    RetencionTab.vue: banner de caveat permanente (texto seccion Retención banner body). Gráfico `<Line>`
    multi-cohorte, X = ciclo N, una línea por cohorte (`chartColors[i % len]`, `tension:0.3`). Sección
    secundaria "Distribución de ciclos completados (activos actuales)" (Bar ciclo 1/2/3+ o
    metric-card row). Filtro local `q-select` (dense, outlined) plan_category con opciones
    Presencial/Online regular/Online goal/Online coach/Todas (default Todas) que re-pide o re-filtra.
    Empty: "Aún no hay cohortes con suscripciones activas en este alcance".

    FinanzasAvanzadasTab.vue: un gráfico Caja-vs-Devengado dual-series POR MONEDA (gráfico ARS y
    gráfico EUR separados — NUNCA uno que sume monedas). Caja = COLORS.primary, Devengado =
    COLORS.accent, superpuestos en el mismo eje temporal. ARPU por moneda como metric card
    (`text-h4` valor + caption `ARPU mensual (ARS)` / `(EUR)`). Solo renderizar monedas con datos; si
    una moneda da 0, mostrar `0` explícito (per-currency zero card). Caption de exclusión SOLO cuando
    excludedInvalidWindow > 0: "Se excluyeron N suscripciones con ventana inválida (sin fecha de fin o
    duración 0).". Empty: "No hay ingresos para el periodo seleccionado".

  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm exec vue-tsc --noEmit 2>&1 | grep -iE "FunnelTab|RetencionTab|FinanzasAvanzadasTab" | head; grep -L 'chart-colors' src/components/analytics/FunnelTab.vue src/components/analytics/RetencionTab.vue src/components/analytics/FinanzasAvanzadasTab.vue</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: los 3 componentes importan de chart-colors (grep -L no lista ninguno)
    - Source assertion: `grep -c 'bg-orange-2' src/components/analytics/FunnelTab.vue` >= 1 y en RetencionTab >= 1 (banners de caveat permanentes)
    - Source assertion: FinanzasAvanzadasTab no tiene una sola serie que sume ARS+EUR (revisión: dos charts/secciones por moneda)
    - Source assertion: ningún hex hardcodeado en los 3 componentes (`grep -nE '#[0-9a-fA-F]{3,6}' src/components/analytics/{FunnelTab,RetencionTab,FinanzasAvanzadasTab}.vue` vacío)
    - vue-tsc limpio
  </acceptance_criteria>
  <done>3 componentes de tab creados siguiendo el contrato del UI-SPEC: banners de caveat, charts con chart-colors, multi-moneda separada, ARPU, captions de exclusión y error state sin toast; type-check limpio.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificación visual de las 3 tabs nuevas + baja de engagement</name>
  <action>Pausar para verificación humana. El usuario levanta el admin local y revisa visualmente las 3 tabs nuevas (Funnel/Retención/Finanzas avanzadas) y la baja de las cards de engagement en Asistencia, siguiendo los pasos de <how-to-verify>. No hay automatización adicional: las tareas 1 y 2 ya implementaron todo; este checkpoint solo confirma la presentación.</action>
  <what-built>
    3 tabs nuevas en Analíticas del admin: Funnel (embudo freemium→prueba→activo + medianas + caveat),
    Retención (curva por ciclos + distribución + filtro plan_category + caveat) y Finanzas avanzadas
    (Caja vs Devengado por moneda + ARPU). Más la baja de las 2 cards de engagement por segmento de la
    tab Asistencia (Plan 05).
  </what-built>
  <how-to-verify>
    1. Levantar el admin local (`cd el-templo-admin && pnpm dev`) y entrar como admin/owner.
    2. Ir a Analíticas. Verificar que aparecen las 3 tabs nuevas: Funnel, Retención, Finanzas avanzadas.
    3. Funnel: ver el embudo de 3 etapas, las medianas por etapa, y el banner naranja de caveat de
       ramp-up arriba (siempre visible).
    4. Retención: ver la curva multi-cohorte con eje X = ciclo N (no meses), la distribución de ciclos
       completados, el filtro plan_category (probar cambiar a Presencial/Online), y el banner de caveat.
    5. Finanzas avanzadas: ver Caja vs Devengado en gráficos SEPARADOS por moneda (ARS y EUR nunca en
       un mismo gráfico sumado), ARPU por moneda, y si hay subs con ventana inválida, el caption de
       exclusión bajo el gráfico de devengado.
    6. Cambiar filtros globales (sede/país/rango de fechas) y confirmar que las 3 tabs refrescan.
    7. Ir a Reportes → tab Asistencia: confirmar que YA NO está la card "Activos por segmento de
       engagement" ni la worklist en_riesgo/ghost con WhatsApp, pero SÍ siguen únicos 7/14/30, el
       warning <50% de check-in, no-show, heatmap y ocupación.
    8. Confirmar que gestion/coach/recepción NO ven las 3 tabs nuevas (admin-only).
  </how-to-verify>
  <resume-signal>Escribí "approved" o describí los problemas visuales/de comportamiento encontrados</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                   | Description                                                                   |
| ------------------------------------------ | ----------------------------------------------------------------------------- |
| cliente admin → AnaliticasPage tabs nuevas | render de datos sensibles; el gate de rol está en el backend (Plans 02/03/04) |

## STRIDE Threat Register

| Threat ID | Category               | Component                                        | Disposition | Mitigation Plan                                                                                |
| --------- | ---------------------- | ------------------------------------------------ | ----------- | ---------------------------------------------------------------------------------------------- |
| T-118-15  | Information Disclosure | tabs admin-only                                  | mitigate    | endpoints ya enforced ADMIN_ROLES (backend); las tabs solo consumen lo que el backend autoriza |
| T-118-16  | Integrity              | sumar ARS+EUR en un gráfico                      | mitigate    | charts separados por moneda; revisión en acceptance + checkpoint visual                        |
| T-118-17  | Integrity              | presentar data histórica aproximada como precisa | mitigate    | banners de caveat permanentes en Funnel y Retención (D-01), textos verbatim del UI-SPEC        |
| T-118-SC  | Tampering              | npm installs                                     | mitigate    | sin dependencias nuevas (chart.js/vue-chartjs ya instalados — D-10)                            |

</threat_model>

<verification>
- vue-tsc limpio en los 6 archivos
- 3 métodos nuevos en el composable + getEngagement conservado
- charts con chart-colors, sin hex hardcodeados, multi-moneda separada
- checkpoint visual aprobado
</verification>

<success_criteria>

- 3 tabs nuevas funcionales (Funnel/Retención/Finanzas avanzadas) consumiendo sus endpoints, con caveats, filtros y multi-moneda separada
- getEngagement del composable conservado (D-09)
- Sin dependencias nuevas
- Verificación visual aprobada
  </success_criteria>

<output>
Create `.planning/phases/118-analytics-estrat-gico-funnel-de-conversi-n-retenci-n-por-cic/118-06-SUMMARY.md` when done
</output>
