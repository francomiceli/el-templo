---
phase: 118-analytics-estrat-gico-funnel-de-conversi-n-retenci-n-por-cic
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-admin/src/components/analytics/AsistenciaTab.vue
  - el-templo-admin/src/pages/ReportesPage.vue
autonomous: true
requirements: [D-09]
must_haves:
  truths:
    - "AsistenciaTab.vue ya NO muestra la card 'Activos por segmento de engagement' ni la worklist nominal en_riesgo/ghost con botón WhatsApp"
    - "El código muerto resultante (segmentCountCards, engagementColumns, formatMemberName, contactMember, segmentLabel/segmentColor, prop engagement, imports SEGMENT_* / EngagementMember / EngagementAnalytics sin uso) fue eliminado"
    - "AsistenciaTab conserva: warning <50% check-in, únicos 7/14/30, no-show, asistencias/día, heatmap, ocupación, ratio check-in"
    - "ReportesPage.vue ya NO pide analyticsApi.getEngagement en el Promise.all de fetchAttendanceData ni pasa la prop :engagement"
    - "El backend (endpoint /engagement, EngagementService, engagement.test.ts), el método getEngagement del composable y el módulo segmentation quedan INTACTOS"
  artifacts:
    - path: "el-templo-admin/src/components/analytics/AsistenciaTab.vue"
      provides: "Tab de asistencia sin las 2 cards de engagement por segmento"
  key_links:
    - from: "ReportesPage.vue fetchAttendanceData"
      to: "ya no llama analyticsApi.getEngagement"
      via: "Promise.all reducido"
      pattern: "getEngagement"
---

<objective>
Borrar del FRONTEND las 2 cards de engagement por segmento de AsistenciaTab.vue (D-09): mezclan
poblaciones (online no hace check-in presencial → cae en "Digital" e infla el segmento). NO se
rediseña la segmentación; se BORRA del display. Es una deleción de template/código, no una acción
destructiva de runtime.

Alcance EXACTO (solo frontend) — NOTES-FROM-117 §🗑️ + PATTERNS.md §D-09:

- AsistenciaTab.vue: quitar card "Activos por segmento de engagement" + worklist nominal
  en_riesgo/ghost con botón WhatsApp + todo el código muerto que queda + la prop `engagement`.
- ReportesPage.vue: sacar `analyticsApi.getEngagement` del Promise.all de fetchAttendanceData,
  quitar `engagementData` y la prop `:engagement`.

CONSERVAR (NO tocar): backend /api/admin/analytics/engagement, EngagementService, engagement.test.ts,
tipos, método getEngagement del composable useAnalyticsApi, módulo segmentation (lo usan
AlumnosPage/NotificacionesPage), y el resto de AsistenciaTab (únicos 7/14/30, ratio + warning <50%,
no-show, heatmap, ocupación).

Purpose: dejar de mostrar un número engañoso; lo operativo ya está en Reportes (Inactivos/Vencimientos).
Output: AsistenciaTab y ReportesPage sin engagement por segmento.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/118-analytics-estrat-gico-funnel-de-conversi-n-retenci-n-por-cic/118-CONTEXT.md
@.planning/phases/118-analytics-estrat-gico-funnel-de-conversi-n-retenci-n-por-cic/118-NOTES-FROM-117.md
@.planning/phases/118-analytics-estrat-gico-funnel-de-conversi-n-retenci-n-por-cic/118-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Borrar las 2 cards de engagement + código muerto de AsistenciaTab.vue</name>
  <read_first>
    - el-templo-admin/src/components/analytics/AsistenciaTab.vue (archivo completo — identificar los 2 bloques v-if="props.engagement" + el script asociado)
    - .planning/phases/118-analytics-estrat-gico-funnel-de-conversi-n-retenci-n-por-cic/118-PATTERNS.md (§AsistenciaTab.vue — targets exactos por número de línea)
  </read_first>
  <action>
    En AsistenciaTab.vue quitar del template: (1) la card "Activos por segmento de engagement"
    (bloque `v-if="props.engagement"` con los conteos por segmento, ~líneas 65-93) y (2) la card de
    worklist nominal en_riesgo/ghost con botón WhatsApp (segundo bloque `v-if="props.engagement"`, con
    columns/segment chips/contactMember). Del script borrar el código que queda muerto:
    `segmentCountCards`, `engagementColumns`, `formatMemberName`, `contactMember`,
    `segmentLabel`/`segmentColor`, la prop `engagement`, y los imports que queden SIN uso:
    `SEGMENT_LABELS`/`SEGMENT_COLORS`/`SEGMENT_DESCRIPTIONS` y `EngagementAnalytics`/`EngagementMember`.
    Verificar con grep que cada símbolo borrado no se use en otra parte del archivo antes de quitar el
    import (no remover imports que sigan en uso). CONSERVAR el banner de warning <50% check-in, únicos
    7/14/30, no-show, asistencias/día, heatmap, ocupación y el ratio de check-in. No usar console.log.
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm exec vue-tsc --noEmit 2>&1 | grep -i "AsistenciaTab" | head; grep -c 'props.engagement\|contactMember\|segmentCountCards\|engagementColumns' src/components/analytics/AsistenciaTab.vue</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'props.engagement' src/components/analytics/AsistenciaTab.vue` === 0
    - Source assertion: `grep -c 'contactMember\|segmentCountCards\|engagementColumns\|formatMemberName' src/components/analytics/AsistenciaTab.vue` === 0
    - Source assertion: el banner <50% y únicos 7/14/30 siguen presentes (grep de sus textos/refs)
    - vue-tsc sin errores nuevos en AsistenciaTab.vue (sin imports sin uso)
  </acceptance_criteria>
  <done>AsistenciaTab.vue sin las 2 cards de engagement ni código muerto; conserva el resto; type-check limpio.</done>
</task>

<task type="auto">
  <name>Task 2: Quitar getEngagement del fetch de ReportesPage.vue</name>
  <read_first>
    - el-templo-admin/src/pages/ReportesPage.vue (fetchAttendanceData Promise.all ~línea 1094; ref engagementData ~1073; prop :engagement ~197)
    - .planning/phases/118-analytics-estrat-gico-funnel-de-conversi-n-retenci-n-por-cic/118-PATTERNS.md (§ReportesPage.vue targets)
  </read_first>
  <action>
    En ReportesPage.vue: quitar la prop `:engagement="engagementData"` que se pasa a AsistenciaTab
    (~línea 197), eliminar el ref `engagementData` (~línea 1073), y sacar `analyticsApi.getEngagement(...)`
    del `Promise.all` de `fetchAttendanceData` (~línea 1094) junto con el uso destructurado de
    `engagement` (~línea 1099). Ajustar el `Promise.all`/destructuring restante para que siga
    devolviendo el resto de datos de asistencia sin el engagement. NO borrar el método getEngagement del
    composable useAnalyticsApi (se conserva por D-09). Verificar que no quede ninguna referencia colgante
    a engagementData. No usar console.log.
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm exec vue-tsc --noEmit 2>&1 | grep -i "ReportesPage" | head; grep -c 'engagementData\|:engagement' src/pages/ReportesPage.vue</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'engagementData' src/pages/ReportesPage.vue` === 0
    - Source assertion: `grep -c ':engagement=' src/pages/ReportesPage.vue` === 0
    - Source assertion: el método getEngagement del composable sigue existiendo (`grep -c 'getEngagement' src/composables/useAnalyticsApi.ts` >= 1)
    - vue-tsc sin errores nuevos en ReportesPage.vue
  </acceptance_criteria>
  <done>ReportesPage.vue ya no pide ni pasa engagement; getEngagement del composable intacto; type-check limpio.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                          |
| -------- | ---------------------------------------------------- |
| n/a      | deleción de UI; no cruza límites de confianza nuevos |

## STRIDE Threat Register

| Threat ID | Category  | Component                       | Disposition | Mitigation Plan                                                   |
| --------- | --------- | ------------------------------- | ----------- | ----------------------------------------------------------------- |
| T-118-14  | Integrity | borrar imports en uso por error | mitigate    | grep de cada símbolo antes de quitar el import; vue-tsc como gate |
| T-118-SC  | Tampering | npm installs                    | mitigate    | sin dependencias nuevas; solo deleción                            |

</threat_model>

<verification>
- vue-tsc limpio en ambos archivos
- grep de props.engagement / contactMember / engagementData === 0
- backend y composable getEngagement intactos
</verification>

<success_criteria>

- Las 2 cards de engagement por segmento y su código muerto están eliminados de AsistenciaTab.vue
- ReportesPage.vue no pide ni pasa engagement
- El backend, engagement.test.ts, el método getEngagement del composable y segmentation quedan intactos
  </success_criteria>

<output>
Create `.planning/phases/118-analytics-estrat-gico-funnel-de-conversi-n-retenci-n-por-cic/118-05-SUMMARY.md` when done
</output>
