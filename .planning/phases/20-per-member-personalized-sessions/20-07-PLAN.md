---
phase: 20-per-member-personalized-sessions
plan: 07
type: execute
wave: 4
depends_on: ["20-02", "20-03"]
files_modified:
  - el-templo-admin/src/pages/GeneratePage.vue
  - el-templo-admin/src/pages/SessionsPage.vue
  - el-templo-admin/src/composables/useJourneyAdminApi.ts
  - el-templo-admin/src/types/journey.ts
  - el-templo-api/src/modules/admin/service.ts
  - el-templo-api/src/modules/admin/routes.ts
autonomous: true
requirements:
  - ADMIN-JOURNEY-GENERATION
  - ADMIN-JOURNEY-SESSIONS
must_haves:
  truths:
    - "Generation page has a tab for personalized journey session generation alongside existing general generation"
    - "Coaches can generate 6 journey variations per semana (one per journey type)"
    - "Coaches can generate all 6 at once or specific journey types"
    - "Sesiones page has two tabs: General (existing) and Personalizadas (journey sessions)"
    - "Personalizadas tab mirrors General layout: shows journey sessions by day with journey type labels"
    - "Coaches can manage and modify journey sessions like regular sessions"
  artifacts:
    - path: "el-templo-admin/src/pages/GeneratePage.vue"
      provides: "Updated generation page with Personalizadas tab for journey session generation"
    - path: "el-templo-admin/src/pages/SessionsPage.vue"
      provides: "Updated sessions page with General and Personalizadas tabs"
    - path: "el-templo-admin/src/composables/useJourneyAdminApi.ts"
      provides: "Composable for admin journey API calls"
      exports: ["useJourneyAdminApi"]
  key_links:
    - from: "el-templo-admin/src/pages/GeneratePage.vue"
      to: "el-templo-api/src/modules/journeys/routes.ts"
      via: "POST /api/admin/journeys/generate endpoint"
      pattern: "admin/journeys/generate"
    - from: "el-templo-admin/src/pages/SessionsPage.vue"
      to: "el-templo-api/src/modules/journeys/routes.ts"
      via: "GET sessions with journeyType filter"
      pattern: "journey_type"
---

<objective>
Add journey session generation and management to the admin app.

Purpose: Coaches need to generate journey-specific sessions and manage them alongside regular Entrenamiento sessions. This adds generation controls and a sessions view tab for personalized sessions.
Output: Updated GeneratePage with journey generation tab, updated SessionsPage with Personalizadas tab, admin API composable.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-per-member-personalized-sessions/20-CONTEXT.md
@.planning/phases/20-per-member-personalized-sessions/20-RESEARCH.md
@.planning/phases/20-per-member-personalized-sessions/20-02-SUMMARY.md
@.planning/phases/20-per-member-personalized-sessions/20-03-SUMMARY.md
@el-templo-admin/src/pages/GeneratePage.vue
@el-templo-admin/src/pages/SessionsPage.vue
@el-templo-admin/src/composables/useSessionsApi.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create admin journey API composable and types, add generation tab</name>
  <files>
    el-templo-admin/src/composables/useJourneyAdminApi.ts
    el-templo-admin/src/types/journey.ts
    el-templo-admin/src/pages/GeneratePage.vue
  </files>
  <action>
    **Create `types/journey.ts`:**
    - Mirror API types: JourneyType, JourneyDuration, JourneyMetadata, MemberJourneyInfo.
    - ALL_JOURNEY_TYPES array constant.
    - JOURNEY_TIER_MAP for display grouping.

    **Create `useJourneyAdminApi.ts` composable:**
    - `generateJourneySessions(week: number, journeyType: JourneyType, options?): Promise<{ generated, skipped }>` — POST /api/admin/journeys/generate.
    - `getMembers(params): Promise<{ members, total }>` — GET /api/admin/journeys/members.
    - `getMemberDetail(userId): Promise<MemberJourneyDetail>` — GET /api/admin/journeys/members/:userId.
    - Uses existing axios API instance. Returns `cleanup()`.

    **Update GeneratePage.vue:**
    - Add `q-tabs` at the top of the page: "General" (existing content) and "Personalizadas" (new journey generation).
    - "General" tab contains all existing generation functionality (unchanged).
    - "Personalizadas" tab contains:
      - Week selector (same pattern as General: current SPOM week + 1 minimum).
      - Journey type selector: 6 checkboxes or chips for each journey type, grouped by tier. Default: all selected.
      - "Generar Todo" button: generates sessions for all selected journey types for the specified week.
      - "Generar" button per journey type row for targeted generation.
      - Results display: shows generated/skipped count per journey type after generation.
      - Note: coaches generate 6 journey variations per semana (not 18 — duration is front-end filter per CONTEXT.md).
    - Use `q-tab-panels` for tab content switching.
    - Per Claude's discretion: tab organization uses q-tabs with aligned panels, keeping existing General UI intact.

    Use structured logging per CLAUDE.md. No console.log.
    Spanish labels: "Personalizadas", "Generar Todo", "Semana", etc.

  </action>
  <verify>
    `cd el-templo-admin && npx tsc --noEmit` passes.
    GeneratePage shows two tabs: General and Personalizadas.
    Journey generation tab shows week selector and journey type selectors.
    Generate button calls admin API endpoint.
  </verify>
  <done>
    Admin journey API composable provides generation and member management calls.
    GeneratePage has Personalizadas tab for journey session generation.
    Coaches can generate all 6 journey types at once or individually.
    Existing General generation functionality unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Personalizadas tab to SessionsPage</name>
  <files>
    el-templo-admin/src/pages/SessionsPage.vue
  </files>
  <action>
    **Update SessionsPage.vue:**
    - Add `q-tabs` at the top: "General" (existing) and "Personalizadas" (journey sessions).
    - "General" tab contains all existing session list functionality (unchanged).
    - "Personalizadas" tab:
      - Mirrors General layout structure.
      - Filters: week selector, day tabs (Lunes through Sabado), journey type filter dropdown (all 6 types + "Todos").
      - Session list: shows journey sessions grouped by day, similar to General tab.
      - Each session row shows: dayId, level group, journey type badge (e.g., "Empuje" in terracotta), status, block count.
      - Journey type badge uses color coding by tier: principiante=sage, intermedio=terracotta, avanzado=deep purple.
      - Day view shows sessions: "Lunes: Tren Superior (α/Δ), Tren Superior (Σ), Tren Superior (Ω), Tren Inferior (α/Δ)..." — per CONTEXT.md: "shows by day (Lunes: Upper, Lower, Push, Pull...)".
      - Clicking a session navigates to the existing SessionEditPage (journey sessions use same edit workflow).
      - Status badges (pending_review, approved) same as General tab.

    The session data for Personalizadas tab:
    - Reuse existing `useSessionsApi` but add `journeyType` filter parameter.
    - API already returns sessions — just filter by `journeyType IS NOT NULL` for personalized tab.
    - If the existing sessions API doesn't support journeyType filter, add it: modify the admin sessions list endpoint to accept an optional `journeyType` query param and filter accordingly. This is a small API change — add the filter in AdminSessionService.getSessions().

    Coaches can manage and modify journey sessions like regular sessions — the edit page works identically.

  </action>
  <verify>
    `cd el-templo-admin && npx tsc --noEmit` passes.
    SessionsPage shows two tabs: General and Personalizadas.
    Personalizadas tab loads journey sessions with journey type badges.
    Day tabs filter correctly.
    Session click navigates to edit page.
  </verify>
  <done>
    SessionsPage has General and Personalizadas tabs.
    Personalizadas tab shows journey sessions by day with journey type labels.
    Journey type filter and day tabs work correctly.
    Coaches can navigate to edit page for journey sessions (same workflow as General).
  </done>
</task>

</tasks>

<verification>
- `cd el-templo-admin && npx tsc --noEmit` passes
- GeneratePage has two tabs with journey generation working
- SessionsPage has two tabs with journey sessions displayed
- Journey type badges show correct tier colors
- Existing General functionality unchanged in both pages
- Journey sessions editable via existing edit workflow
</verification>

<success_criteria>
Coaches can generate journey sessions per semana via Personalizadas generation tab.
Coaches can view and manage journey sessions in Personalizadas sessions tab.
Both tabs coexist cleanly with existing General functionality.
Journey sessions flow into existing edit workflow seamlessly.
</success_criteria>

<output>
After completion, create `.planning/phases/20-per-member-personalized-sessions/20-07-SUMMARY.md`
</output>
