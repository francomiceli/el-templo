---
phase: 20-per-member-personalized-sessions
plan: 06
type: execute
wave: 3
depends_on: ["20-04", "20-05"]
files_modified:
  - el-templo-app/src/layouts/MainLayout.vue
  - el-templo-app/src/modules/progression/pages/MiCaminoPage.vue
  - el-templo-app/src/modules/progression/components/JourneySection.vue
  - el-templo-app/src/modules/progression/composables/useJourneyProgress.ts
  - el-templo-app/src/modules/progression/types.ts
autonomous: true
requirements:
  - JOURNEY-NAVIGATION
  - JOURNEY-MI-CAMINO
  - JOURNEY-SWITCH
  - JOURNEY-ARCHIVE
must_haves:
  truths:
    - "Journey option visible in left panel navigation alongside Entrenamiento"
    - "Both Entrenamiento and Journey are independent parallel routes"
    - "Mi Camino shows journey progress section with per-duration semana counters"
    - "Mi Camino shows archived journey history as summary cards"
    - "Change journey option accessible from Mi Camino"
    - "Switching journeys shows warning that progress will reset"
    - "Archived journeys display: journey name, dates active, semanas completed, total sessions"
  artifacts:
    - path: "el-templo-app/src/layouts/MainLayout.vue"
      provides: "Updated navigation with Journey item in left drawer"
    - path: "el-templo-app/src/modules/progression/components/JourneySection.vue"
      provides: "Journey progress and history section for Mi Camino"
    - path: "el-templo-app/src/modules/progression/composables/useJourneyProgress.ts"
      provides: "Composable for fetching journey progress data for Mi Camino"
  key_links:
    - from: "el-templo-app/src/layouts/MainLayout.vue"
      to: "el-templo-app/src/modules/journey/routes.ts"
      via: "Navigation link to /journey"
      pattern: 'to="/journey"'
    - from: "el-templo-app/src/modules/progression/components/JourneySection.vue"
      to: "el-templo-app/src/modules/journey/composables/useJourneyApi.ts"
      via: "Fetches active + archived journey data"
      pattern: "useJourneyApi"
---

<objective>
Wire journey into the member app navigation and integrate journey progress/history into Mi Camino.

Purpose: Members need to access journeys from the main navigation and track their journey progress alongside Entrenamiento in Mi Camino. This completes the member-facing journey experience.
Output: Updated navigation, Mi Camino journey section with progress and archived history, journey switching capability.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-per-member-personalized-sessions/20-CONTEXT.md
@.planning/phases/20-per-member-personalized-sessions/20-04-SUMMARY.md
@.planning/phases/20-per-member-personalized-sessions/20-05-SUMMARY.md
@el-templo-app/src/layouts/MainLayout.vue
@el-templo-app/src/modules/progression/pages/MiCaminoPage.vue
@el-templo-app/src/modules/progression/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add journey to left panel navigation</name>
  <files>
    el-templo-app/src/layouts/MainLayout.vue
  </files>
  <action>
    Update `MainLayout.vue` to add a "Journey" item in the left drawer navigation:

    - Add new `q-item` after the "Entrenamiento" item:
      ```html
      <q-item clickable to="/journey" @click="leftDrawerOpen = false">
        <q-item-section avatar>
          <q-icon name="explore" />
        </q-item-section>
        <q-item-section>Journey</q-item-section>
      </q-item>
      ```
    - Use "explore" icon (compass — represents exploration/journey).
    - Position: after "Entrenamiento", before "Conceptos".
    - Both Entrenamiento and Journey are independent parallel routes — no coupling between them.
    - The label stays as "Journey" (not translated — it's a brand term for the feature per CONTEXT.md naming convention).

    Navigation order becomes:
    1. Mi Camino (trending_up)
    2. Entrenamiento (fitness_center)
    3. Journey (explore)
    4. Conceptos (menu_book)
    5. Mi Perfil (person)

  </action>
  <verify>
    Run dev server (`cd el-templo-app && npx quasar dev`).
    Left drawer shows "Journey" item between Entrenamiento and Conceptos.
    Tapping Journey navigates to /journey.
  </verify>
  <done>
    Journey navigation item added to left drawer.
    Both Entrenamiento and Journey accessible independently.
    Navigation order logical: progress tracking → training → journey → knowledge → profile.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate journey progress and history into Mi Camino</name>
  <files>
    el-templo-app/src/modules/progression/pages/MiCaminoPage.vue
    el-templo-app/src/modules/progression/components/JourneySection.vue
    el-templo-app/src/modules/progression/composables/useJourneyProgress.ts
    el-templo-app/src/modules/progression/types.ts
  </files>
  <action>
    **Create `useJourneyProgress.ts` composable:**
    - Fetches active journey (from journey API: GET /api/journeys/active).
    - Fetches archived journeys (from journey API: GET /api/journeys/archived).
    - Computes journey stats: total sessions per duration, total semanas completed.
    - Exposes `cleanup()` method per composable convention.

    **Create `JourneySection.vue` component:**
    - **Active Journey Section** (if member has active journey):
      - Card showing: journey name (Cinzel heading), tier badge, "Activo desde {date}".
      - Per-duration semana display: "Semana {n}" for each duration (20/40/60 min) with small duration label.
      - Use 3 small progress counters or simple text rows.
      - "Cambiar Journey" button (terracotta outline) at bottom.
      - Tapping "Cambiar Journey" shows `q-dialog` warning: "Cambiar de Journey reiniciara tu progreso actual. Tu historial sera guardado." with "Cancelar" and "Cambiar" buttons. On confirm → navigates to /journey (selection screen).

    - **No Active Journey** (if member has no journey):
      - Light prompt card: "Comienza tu Journey personalizado" with brief explanation.
      - CTA button → navigates to /journey.

    - **Archived Journeys Section** (always shown if any exist):
      - Header: "Historial de Journeys".
      - Each archived journey as a summary card:
        - Journey name + tier badge.
        - "Activo: {startedAt} - {archivedAt}" date range.
        - Semanas completadas per duration: "20 min: S{n} · 40 min: S{n} · 60 min: S{n}".
        - Total sessions completed count.
      - If no archived journeys, this section is hidden.

    **Update MiCaminoPage.vue:**
    - Import and render `JourneySection` component.
    - Position after existing progression content (stats, chart, evaluation).
    - Add a `q-separator` or section divider before Journey section.
    - Load journey data on mount via `useJourneyProgress`.

    **Update `progression/types.ts`:**
    - Add any types needed for journey progress display (or import from journey module types).

    Use `createLogger()` per CLAUDE.md.
    All UI text in Spanish.

  </action>
  <verify>
    `cd el-templo-app && npx tsc --noEmit` passes.
    Mi Camino page shows Journey section.
    Active journey displays per-duration semana counters.
    "Cambiar Journey" shows warning dialog.
    Archived journeys shown as summary cards.
  </verify>
  <done>
    Mi Camino integrates journey progress (active journey with per-duration tracking).
    Journey history shows archived journeys with date ranges and completion stats.
    "Cambiar Journey" flow includes warning dialog about progress reset.
    Journey section gracefully handles no-journey state with CTA to start.
  </done>
</task>

</tasks>

<verification>
- `cd el-templo-app && npx tsc --noEmit` passes
- Left drawer shows Journey item in correct position
- Mi Camino shows active journey progress with per-duration semanas
- Mi Camino shows archived journey history
- "Cambiar Journey" shows warning dialog
- No-journey state shows prompt to start
- Both Entrenamiento and Journey accessible independently from nav
</verification>

<success_criteria>
Journey accessible from main navigation alongside Entrenamiento.
Mi Camino displays journey progress (per-duration semana tracking) and archived history.
Journey switching includes progress reset warning.
Member can access both Entrenamiento and Journey independently.
</success_criteria>

<output>
After completion, create `.planning/phases/20-per-member-personalized-sessions/20-06-SUMMARY.md`
</output>
