---
phase: 20-per-member-personalized-sessions
plan: 08
type: execute
wave: 5
depends_on: ["20-03", "20-07"]
files_modified:
  - el-templo-admin/src/pages/AlumnosPage.vue
  - el-templo-admin/src/pages/AlumnoDetailPage.vue
  - el-templo-admin/src/router/routes.ts
  - el-templo-admin/src/layouts/AdminLayout.vue
autonomous: false
requirements:
  - ADMIN-ALUMNOS
  - JOURNEY-VERIFICATION
must_haves:
  truths:
    - "New Alumnos option visible in admin sidebar"
    - "Alumnos page shows ALL members, not just those with journeys"
    - "Members with active journeys show their journey name; others show Sin journey"
    - "Search by name and filters by journey type and activity level"
    - "Detail view shows active journey, current semana per duration, session history"
    - "Detail view shows both Entrenamiento AND journey progress"
    - "End-to-end flow verified: member selects journey → coach generates → member plays session → progress updates"
  artifacts:
    - path: "el-templo-admin/src/pages/AlumnosPage.vue"
      provides: "Member overview page with journey status"
    - path: "el-templo-admin/src/pages/AlumnoDetailPage.vue"
      provides: "Individual member detail with journey and entrenamiento progress"
    - path: "el-templo-admin/src/router/routes.ts"
      provides: "Updated admin routes with /alumnos and /alumnos/:id"
    - path: "el-templo-admin/src/layouts/AdminLayout.vue"
      provides: "Updated sidebar with Alumnos item"
  key_links:
    - from: "el-templo-admin/src/pages/AlumnosPage.vue"
      to: "el-templo-api/src/modules/journeys/routes.ts"
      via: "GET /api/admin/journeys/members endpoint"
      pattern: "admin/journeys/members"
    - from: "el-templo-admin/src/layouts/AdminLayout.vue"
      to: "el-templo-admin/src/router/routes.ts"
      via: "Sidebar navigation link to /alumnos"
      pattern: 'to="/alumnos"'
---

<objective>
Create the admin Alumnos page for member journey overview and run end-to-end verification.

Purpose: Coaches need visibility into which members are on journeys and their progress. This is a new admin section. The verification checkpoint ensures the complete journey flow works end-to-end.
Output: AlumnosPage, AlumnoDetailPage, updated admin routing and sidebar, verification.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-per-member-personalized-sessions/20-CONTEXT.md
@.planning/phases/20-per-member-personalized-sessions/20-03-SUMMARY.md
@el-templo-admin/src/router/routes.ts
@el-templo-admin/src/layouts/AdminLayout.vue
@el-templo-admin/src/composables/useJourneyAdminApi.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AlumnosPage and AlumnoDetailPage with admin sidebar wiring</name>
  <files>
    el-templo-admin/src/pages/AlumnosPage.vue
    el-templo-admin/src/pages/AlumnoDetailPage.vue
    el-templo-admin/src/router/routes.ts
    el-templo-admin/src/layouts/AdminLayout.vue
  </files>
  <action>
    **AlumnosPage.vue** — Member overview page:
    - Uses `useJourneyAdminApi().getMembers()` for data.
    - `q-table` with server-side pagination (same pattern as ExercisesPage).
    - Columns (per Claude's discretion for layout):
      - Nombre (name)
      - Nivel (member level, Greek letter format: α, Δ, Σ, Ω)
      - Sucursal (branch name)
      - Journey (active journey name or "Sin journey" in muted text)
      - Semana (current semana display if journey active, showing highest semana across durations)
      - Acciones (view detail button)
    - Filters above table:
      - Search input: filters by name (q-input with search icon).
      - Journey type dropdown: "Todos", then 6 journey types grouped by tier.
      - These filters trigger API refetch.
    - Rows show journey type as colored badge (same tier coloring as SessionsPage: principiante=sage, intermedio=terracotta, avanzado=deep purple). "Sin journey" shown in grey muted text.
    - Action column: eye icon button → navigates to /alumnos/:userId.

    **AlumnoDetailPage.vue** — Individual member detail:
    - Route param: userId.
    - Uses `useJourneyAdminApi().getMemberDetail(userId)`.
    - Sections:
      1. **Header:** Member name, level (Greek letter), branch.
      2. **Active Journey** (if any): Journey name, tier, started date, per-duration semana counters (20/40/60 min), total sessions in current journey.
      3. **Entrenamiento Progress:** Total sessions completed (from completed_sessions where journeyType IS NULL), total days trained, current streak.
      4. **Journey Progress:** Total journey sessions completed (from completed_sessions where journeyType IS NOT NULL), breakdown by duration.
      5. **Session History:** Recent completions (last 20) in a timeline or table, showing date, type (Entrenamiento or journey name), duration (if journey), RPE.
      6. **Archived Journeys:** List of past journeys with date ranges and semana summary.
    - Back button → returns to /alumnos.

    **Update routes.ts:**
    - Add route: `{ path: 'alumnos', component: () => import('pages/AlumnosPage.vue') }`.
    - Add route: `{ path: 'alumnos/:userId', component: () => import('pages/AlumnoDetailPage.vue') }`.

    **Update AdminLayout.vue sidebar:**
    - Add new `q-item` for Alumnos:
      ```html
      <q-item clickable v-ripple to="/alumnos">
        <q-item-section avatar>
          <q-icon name="people" />
        </q-item-section>
        <q-item-section>Alumnos</q-item-section>
      </q-item>
      ```
    - Position: after Ejercicios, making sidebar order: Sesiones → Generar → Ejercicios → Alumnos.

    Use structured logging per CLAUDE.md. No console.log.
    Spanish labels throughout.

  </action>
  <verify>
    `cd el-templo-admin && npx tsc --noEmit` passes.
    AlumnosPage loads with member list.
    Search and filter work.
    AlumnoDetailPage shows member info with journey and entrenamiento progress.
    Sidebar shows Alumnos item.
  </verify>
  <done>
    AlumnosPage shows all members with journey status, search, and filters.
    AlumnoDetailPage shows comprehensive member info (journey + entrenamiento progress).
    Admin sidebar updated with Alumnos navigation item.
    Routes registered for /alumnos and /alumnos/:userId.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end journey flow verification</name>
  <what-built>
    Complete journey feature across all 3 apps:
    - DB: member_journeys table, sessions.journey_type, completed_sessions.journey_type + duration
    - API: Journey CRUD, generation, session retrieval, completion, admin member management
    - Member App: Journey selection (6 types, 3 tiers), overview, duration picker, session player, Mi Camino integration, navigation
    - Admin App: Journey generation tab, Personalizadas sessions tab, Alumnos page
  </what-built>
  <how-to-verify>
    **Admin Flow:**
    1. Open admin app at localhost:9100
    2. Navigate to "Generar" → verify "Personalizadas" tab exists
    3. In Personalizadas tab, generate sessions for current week + 1 for "Tren Superior"
    4. Navigate to "Sesiones" → verify "Personalizadas" tab shows generated journey sessions
    5. Click a journey session → verify it opens in edit page like regular sessions
    6. Navigate to "Alumnos" → verify member list loads with "Sin journey" for all members

    **Member Flow:**
    7. Open member app at localhost:9000
    8. Log in as a test member
    9. Open left drawer → verify "Journey" item exists between Entrenamiento and Conceptos
    10. Tap "Journey" → verify 6 journey cards grouped by tier (Principiante, Intermedio, Avanzado)
    11. Tap "Tren Superior" → verify overview screen shows zones, description, idealFor
    12. Tap "Elegir este Journey" → verify duration picker shows 20/40/60 min options
    13. Verify encouraging message on 20 min card
    14. Select "40 min" → verify session loads with Initium + Nucleus + Deuteros blocks (3 blocks)
    15. Play through session → verify completion records and shows progress indicator
    16. Navigate to "Mi Camino" → verify journey section shows active journey with semana counters
    17. Verify "Cambiar Journey" button shows warning dialog

    **Cross-verification:**
    18. Return to admin "Alumnos" → verify the test member now shows "Tren Superior" journey
    19. Click member → verify detail shows journey progress with 1 completed session at 40 min

  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- All 3 apps compile: API (`tsc --noEmit`), member app (`tsc --noEmit`), admin (`tsc --noEmit`)
- API tests pass: `cd el-templo-api && pnpm test`
- Complete journey flow works end-to-end across all 3 apps
- No regression in existing Entrenamiento functionality
</verification>

<success_criteria>
Admin can manage journey sessions (generate, view, edit) and see member journey status.
End-to-end flow verified: member selects journey, plays session, progress updates across all apps.
No regression in existing Entrenamiento, session review, or Mi Camino features.
</success_criteria>

<output>
After completion, create `.planning/phases/20-per-member-personalized-sessions/20-08-SUMMARY.md`
</output>
