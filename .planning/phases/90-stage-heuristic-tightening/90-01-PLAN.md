---
phase: 90-stage-heuristic-tightening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-bot/src/webhook/handler.ts
  - el-templo-bot/src/playbooks/definitions.ts
  - el-templo-bot/src/playbooks/types.ts
  - el-templo-bot/src/memory/playbook-state.ts
  - el-templo-bot/test/conversation-flows.test.ts
  - el-templo-bot/test/pb1-discovery-flow.test.ts
autonomous: true
requirements: [STAGE-01, STAGE-02]
must_haves:
  truths:
    - '"primera vez" alone (single-category answer) does NOT advance PB1.E1A → E2A'
    - "Two-category answer (e.g. 'nunca entrené, quiero arrancar') + turn_count >= 2 DOES advance PB1.E1A → E2A"
    - "Three-category answer (e.g. 'hice crossfit hace 2 años') at turn 1 does NOT advance yet (AND gate requires turn_count >= 2)"
    - "Escape hatch fires after exactly N=3 substantive user turns in E1A or E1B without passing content gate, force-advancing to PB1.E2A and emitting a Pino `log.warn` with stageId + conversationId + turn_count + last 1-3 user messages"
    - "E1B content gate behaves symmetrically to E1A (same category lookup, same AND composition, same escape hatch)"
    - "E2A / E2B / E3 heuristics are byte-identical to pre-90 behavior (no sibling-symmetry contamination)"
    - "PB1.E1A and PB1.E1B completionCriteria text references 'múltiples categorías o al menos 2 turnos' instead of 'dijo primera vez avanzar'"
    - "Rendered PB1.E1A lead prompt byte-delta from the Phase 89 snapshot is ≤ +50 chars (KGATE-05 headroom preserved at ≥ +575)"
    - "Full bot test suite (pnpm test) is 537/537 green (existing baseline) after any in-place test alignments"
  artifacts:
    - path: "el-templo-bot/src/webhook/handler.ts"
      provides: "Category-diversity content gate for E1A/E1B + AND composition in computeAdvanceSignals + escape-hatch log.warn"
      contains: "E1A_E1B_CATEGORIES"
    - path: "el-templo-bot/src/playbooks/definitions.ts"
      provides: "Updated completionCriteria text for PB1.E1A and PB1.E1B"
      contains: "múltiples categorías"
    - path: "el-templo-bot/src/playbooks/types.ts"
      provides: "PlaybookSessionState.discoveryTurnCount field (optional, backward-compatible)"
      contains: "discoveryTurnCount"
    - path: "el-templo-bot/src/memory/playbook-state.ts"
      provides: "Reads/writes discoveryTurnCount transparently via JSON"
  key_links:
    - from: "handler.ts computeAdvanceSignals"
      to: "hasStageSpecificContent (category-diversity gate)"
      via: "direct call, receives stageId + inbound"
      pattern: "hasStageSpecificContent\\("
    - from: "handler.ts (post-AI stage-advance block ~line 643)"
      to: "PlaybookSessionState.discoveryTurnCount in Redis"
      via: "setPlaybookState with incremented count when in E1A/E1B and turn is substantive"
      pattern: "discoveryTurnCount"
    - from: "computeAdvanceSignals (E1A/E1B branch)"
      to: "escape hatch Pino log.warn"
      via: "turn_count >= 3 AND !content_gate triggers force-advance + structured warn"
      pattern: "discovery escape fired"
---

<objective>
Tighten PB1.E1A and PB1.E1B stage advancement so single-keyword answers like "primera vez" no longer trigger advance to E2A. Implement category-diversity content gate (STAGE-01), AND composition with turn-count (STAGE-02), wire turn-count tracking through existing PlaybookSessionState, update completionCriteria wording to match new intent, and add an infinite-loop escape hatch with observability-grade Pino logging. Sibling symmetry applies to E1A + E1B only; E2A/E2B/E3 remain untouched.

Purpose: Close the behavioral gap observed in the v5.3.2 live test (Mar del Plata, 2026-04-14) where `"Hola mica soy mati, sería la primera vez"` passed `hasMinimumContent` AND single-keyword match, advancing discovery after a single user turn against the promptSection's explicit "idealmente 2-3 preguntas" intent.

Output: Updated heuristic + turn-count-aware discoveryAnswered + escape-hatch logging + aligned completionCriteria prose + backward-compatible schema + in-place test alignments. Phase 92 will author NEW behavioral locks (RLOK-01); Phase 90 only produces the source state.
</objective>

<execution_context>
@/Users/bores/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bores/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/90-stage-heuristic-tightening/90-CONTEXT.md
@.planning/phases/89-knowledge-fixes/89-01-SUMMARY.md

# Source files this plan modifies (for the executor)

@el-templo-bot/src/webhook/handler.ts
@el-templo-bot/src/playbooks/definitions.ts
@el-templo-bot/src/playbooks/types.ts
@el-templo-bot/src/memory/playbook-state.ts
@el-templo-bot/test/conversation-flows.test.ts
@el-templo-bot/test/pb1-discovery-flow.test.ts

# Project conventions

@el-templo-bot/CLAUDE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (STAGE-01): Category-diversity content gate for PB1.E1A / PB1.E1B</name>
  <files>
    el-templo-bot/src/webhook/handler.ts
  </files>
  <action>
Refactor the PB1.E1A / PB1.E1B branch of `hasStageSpecificContent` (handler.ts ~line 870-874) from the current single-keyword regex to a **category-diversity gate** that requires matches across **at least 2 distinct semantic categories**.

**Encoding:** at module scope, define a typed table

```ts
const E1A_E1B_CATEGORIES: Record<string, RegExp> = {
  level:
    /(^|[^a-záéíóúñ])(principiante|primera vez|nunca|arrancar|empezar|arranco)([^a-záéíóúñ]|$)/i,
  experience:
    /(^|[^a-záéíóúñ])(entren[oaé]|entrené|entrenaba|hice|hago|vengo de|experiencia|activ[oa]|sedentari[oa])([^a-záéíóúñ]|$)/i,
  duration: /(^|[^a-záéíóúñ])(a[ñn]os?|meses?|semanas?)([^a-záéíóúñ]|$)/i,
  context:
    /(^|[^a-záéíóúñ])(gym|gimnasio|crossfit|pesas|running|yoga|pilates|deporte)([^a-záéíóúñ]|$)/i,
};
```

The regex alternation style MUST match the existing non-word-boundary pattern in `hasStageSpecificContent` (JS `\b` is ASCII-only — Spanish accents break it). Preserve every keyword from the pre-90 single regex; redistribute across the four categories per the CONTEXT.md mapping. DO NOT drop any keyword; if a keyword does not fit a category cleanly, place it in the best-fit bucket and note the rationale in a JSDoc comment.

**Gate logic:** in the E1A / E1B branch of `hasStageSpecificContent`, count how many category regexes match the lowered inbound; return `true` iff the count is `>= 2`. Use a small loop / `Object.values(...).filter(rx => rx.test(lower)).length`.

**Sibling symmetry guard:** do NOT touch the E2A / E2B, E3, or passthrough branches. Their regexes and return semantics remain byte-identical. Add a one-line comment above the E1A/E1B branch: `// STAGE-01 (v5.3.2 Phase 90): category-diversity gate — E1A+E1B only`.

**JSDoc:** update the function-level JSDoc to describe the category-diversity rationale and cite STAGE-01 / Phase 90. Keep explanations terse; prefer an example: `"primera vez" → 1 category (level) → false; "nunca entrené, quiero arrancar" → 2 categories (level + experience) → true`.

**Discretion:** you may encode the table inline inside the function instead of module scope if you prefer tighter locality — the trade-off is per-call regex allocation vs. single-allocation at module load. Recommendation: module scope (allocation once).

**Explicitly DO NOT:**

- Add a length-based fallback (CONTEXT.md decision: length is weak proxy).
- Add a single-turn override for 3+ categories (CONTEXT.md deferred to post-Phase-92).
- Touch E2A / E2B / E3 category logic.
- Touch `hasMinimumContent` — its definition remains canonical "substantive" for downstream use.
  </action>
  <verify>
  <automated>cd el-templo-bot && pnpm tsc --noEmit && pnpm test -- --testPathPattern=playbook-flow-coverage</automated>
  <manual>grep "STAGE-01" el-templo-bot/src/webhook/handler.ts returns the guard comment. Manual-test the gate by running a throwaway node script: `hasStageSpecificContent("primera vez", "PB1.E1A") === false`; `hasStageSpecificContent("nunca entrené, quiero arrancar", "PB1.E1A") === true`; `hasStageSpecificContent("hice crossfit hace 2 años", "PB1.E1A") === true`.</manual>
  <sampling_rate>run after this task commits, before Task 2 begins</sampling_rate>
  </verify>
  <done>
  `hasStageSpecificContent` for stageId `"PB1.E1A"` or `"PB1.E1B"` returns `true` only when ≥ 2 of the four `E1A_E1B_CATEGORIES` regexes match the inbound; returns `false` for single-category inbounds including the live-test string `"Hola mica soy mati, sería la primera vez"`. E2A / E2B / E3 branches unchanged. `pnpm tsc --noEmit` exits 0.
  </done>
  </task>

<task type="auto">
  <name>Task 2 (STAGE-02 + escape hatch + wording + alignments): Turn-count tracking, AND composition, completionCriteria, escape hatch, and test alignments</name>
  <files>
    el-templo-bot/src/playbooks/types.ts
    el-templo-bot/src/memory/playbook-state.ts
    el-templo-bot/src/webhook/handler.ts
    el-templo-bot/src/playbooks/definitions.ts
    el-templo-bot/test/conversation-flows.test.ts
    el-templo-bot/test/pb1-discovery-flow.test.ts
  </files>
  <action>
Four tightly-coupled edits, then run the suite and align any tests that fail as a structural consequence (Phase 89 precedent, user-approved alignment policy — see 89-01-SUMMARY.md "Test Alignment" section).

**Edit A — Schema evolution (`types.ts`):**

Add an optional field to `PlaybookSessionState`:

```ts
/**
 * Count of substantive user turns observed while the conversation was in
 * PB1.E1A or PB1.E1B (uses `hasMinimumContent` as the "substantive" check).
 * Used by STAGE-02 AND gate and the infinite-loop escape hatch (v5.3.2 P90).
 * Optional + backward-compatible: absent on pre-90 entries, treated as 0.
 */
discoveryTurnCount?: number;
```

Place it near the `avatar` field. No other field shape changes.

**Edit B — Transparent persistence (`memory/playbook-state.ts`):**

No code change required — the file already round-trips the entire JSON shape. Add a one-line JSDoc note near `setPlaybookState` documenting the new optional field and its backward-compat semantics (mirrors the existing `avatar` comment). This is a documentation-only touch; if you prefer, you may skip this file entirely (listed here because the field transits through it).

**Edit C — AND composition + turn-count tracking + escape hatch (`webhook/handler.ts`):**

1. **Read prior count.** In the handler's post-AI advancement block (around line 643 where `computeAdvanceSignals` is called and `advanceStageIfComplete` runs), read the prior `discoveryTurnCount` from `priorPlaybookState.discoveryTurnCount ?? 0`. (Locate where the prior state is already loaded via `getPlaybookState(phone)` — the read exists; if the variable is not in scope at the advance block, hoist it or pass it through.)

2. **Compute "substantive this turn."** Use the existing `hasMinimumContent(inboundText) && !isQuestion(inboundText) && !isMetaIdentityQuery(inboundText) && !isBareGreeting(inboundText)` composition — DRY this by reusing the pieces already computed for `discoveryAnswered`. Introduce a local `const isSubstantiveTurn = ...` that captures exactly the "counted as a real user turn" semantics.

3. **Compute new count.** `const newTurnCount = (resolved.stageId === "PB1.E1A" || resolved.stageId === "PB1.E1B") && isSubstantiveTurn ? priorTurnCount + 1 : priorTurnCount;`. The counter increments only while the stage is E1A or E1B — on advance to E2A/E2B, stops mattering; on other stages, it passes through unchanged.

4. **AND composition for `discoveryAnswered` (refactor `computeAdvanceSignals`).** Extend the signature to accept the prior turn count (or accept an already-computed `isSubstantiveTurn`; prefer the cleanest refactor — turn count + stageId is sufficient). For stages E1A / E1B specifically, the `discoveryAnswered` signal becomes:

   ```
   discoveryAnswered_E1 = turn_count_including_this >= 2
                         AND hasStageSpecificContent(...)
                         AND [all existing gates: !isQuestion, !isMetaIdentityQuery, !isBareGreeting, hasMinimumContent]
   ```

   For other stages, `discoveryAnswered` retains pre-90 semantics (no turn-count gate — Phase 90 scope is E1A/E1B only per sibling-symmetry decision).

   **Cleanest refactor:** add a new optional parameter to `computeAdvanceSignals` (e.g. `priorDiscoveryTurnCount: number = 0`) and branch internally. Document the parameter in JSDoc. All existing call sites pass the new value from the handler; no other callers exist in `src/` (grep confirms only handler.ts and tests).

5. **Escape hatch.** After computing `discoveryAnswered` but before `advanceStageIfComplete`, add:

   ```
   const inDiscoveryE1 = resolved.stageId === "PB1.E1A" || resolved.stageId === "PB1.E1B";
   const escapeFired = inDiscoveryE1
                     && isSubstantiveTurn
                     && newTurnCount >= 3
                     && !hasStageSpecificContent(inboundText, resolved.stageId);
   if (escapeFired) {
     log.warn({
       event: "discovery_escape_fired",
       stageId: resolved.stageId,
       phone, // (conversationId proxy — bot scopes convo by phone)
       turnCount: newTurnCount,
       // Last 1-3 substantive user messages: pull from session.messages filter role === "user" slice(-3)
       recentUserMessages: session.messages.filter(m => m.role === "user").slice(-3).map(m => m.content),
     }, "discovery escape fired");
   }
   ```

   Message text `"discovery escape fired"` MUST be greppable (exact literal). When `escapeFired === true`, force-advance by overriding the `advanceStageIfComplete` result: if it returned `null`, advance to `"PB1.E2A"` (the canonical default `nextStageHints.onCompletion` for E1A/E1B). Document with an inline comment citing "escape hatch (STAGE-02, Phase 90)". The escape is observability-only — no admin notification, no test failure, no UI surfacing.

   **Important:** when escape fires, the persisted new stage is `PB1.E2A` and the `discoveryTurnCount` persisted with it should be `newTurnCount` (preserved) OR reset to 0. **Preserve it** — Phase 92 may want to assert on it in escape-fired paths; resetting loses signal.

6. **Persist `discoveryTurnCount`.** Every `setPlaybookState` call in the advance block must include `discoveryTurnCount: newTurnCount` in the payload. Currently there are two paths (avatar-only update and stage-advance update at lines ~624 and ~655); both must preserve/update the counter.

**Edit D — completionCriteria prose (`playbooks/definitions.ts`):**

Replace the `completionCriteria` text for both `PB1.E1A` (line 30-31) and `PB1.E1B` (line 39-40). Suggested exact wording (finalize per tone; locked constraint is the semantic content):

- **E1A:** `"El lead respondió con información relevante a través de múltiples categorías (nivel + experiencia, o experiencia + duración, etc.) o ha respondido en al menos 2 turnos. Si menciona claramente experiencia previa avanzar a PB1.E2B; si menciona principiante/primera vez/nunca con suficiente contexto avanzar a PB1.E2A."`
- **E1B:** same shape, final sentence adjusted to the E1B branching rule already present (`Avanzar a PB1.E2A o PB1.E2B según respuesta.`).

**No mention of "categories" as a technical term.** Describe by example. Preserve tuteo (respondió, avanzar). Language remains Spanish, instructional.

**Edit E — Test alignments (in-place, Phase 89 precedent).**

Run the full bot test suite. Expect that some existing assertions using "primera vez advances to E2A" patterns may now FAIL because the behavior they locked is the one Phase 90 intentionally breaks. These are **structural alignments**, not new coverage, and Phase 92 owns the authoritative RLOK-01 assertions.

For each failing assertion, update in place with an inline comment:

```
// [STAGE-01/STAGE-02 alignment, v5.3.2 Phase 90]
// Pre-90: single-keyword "primera vez" advanced E1A → E2A. Post-90: requires
// ≥2 categories AND turn_count ≥2. New assertion reflects the intended gate.
// Authoritative behavioral lock lives in Phase 92 (RLOK-01).
```

Known candidates (search before editing — may not all be present):

- `el-templo-bot/test/pb1-discovery-flow.test.ts` — assertions advancing after a single "primera vez" reply.
- `el-templo-bot/test/conversation-flows.test.ts` — flows that send one inbound and expect stage = E2A.
- `el-templo-bot/test/playbook-flow-coverage.test.ts` — coverage table entries for E1A → E2A single-turn advance.

**Alignment limits:** only update assertions that break DUE TO the Phase 90 change; do not author new assertions (Phase 92 scope). If a test breaks for an unrelated reason, investigate rather than silently adjust. Document every alignment update in SUMMARY.md with the structural cause (same format as 89-01-SUMMARY.md "Five alignment updates" section).

**Measurement check.** After all edits, run the existing rendered-snapshot check and compute: `(post-90 PB1.E1A snap bytes) - (post-89 baseline = 18,291)`. The delta MUST be ≤ +50 chars (CONTEXT.md budget). If it exceeds, the completionCriteria rewrite bloated the prompt — trim wording while preserving semantics. Record final delta in SUMMARY.md.

**Do not regenerate the snapshot fixture** unless the completionCriteria rewrite actually changes its bytes AND the change is intended (completionCriteria is NOT rendered directly into the lead prompt — it is metadata consumed by the resolver/advance logic, not injected into STATE_SECTIONS). Confirm by grepping the fixture for "dijo primera vez avanzar" — if absent pre-90, no snapshot regeneration needed. If present, regenerate and commit atomically (v5.3.1 update discipline).

**Commit strategy:** single atomic commit covering all five edits + any test alignments, per v5.3.2 atomic-commit convention. Commit message:

```
feat(bot): tighten PB1.E1A/E1B stage heuristic (v5.3.2 STAGE-01/02) (90-01)

- Category-diversity content gate (level/experience/duration/context, ≥2 match)
- AND composition with turn_count ≥ 2 in discoveryAnswered for E1A/E1B only
- discoveryTurnCount persisted through PlaybookSessionState (backward-compat)
- Infinite-loop escape hatch: N=3 substantive turns → force-advance + log.warn
- completionCriteria rewrite for E1A/E1B (multi-signal intent)
- Test alignments (Phase 89 precedent): <N> in-place updates, documented

Scope: E1A + E1B only. E2A/E2B/E3 untouched.
Requirements: STAGE-01, STAGE-02
```

  </action>
  <verify>
    <automated>cd el-templo-bot && pnpm tsc --noEmit && pnpm test</automated>
    <manual>
1. grep -n "discovery escape fired" el-templo-bot/src/webhook/handler.ts — must return exactly one hit (greppable log message literal).
2. grep -n "múltiples categorías" el-templo-bot/src/playbooks/definitions.ts — must return ≥ 2 hits (E1A and E1B completionCriteria).
3. grep -n "discoveryTurnCount" el-templo-bot/src/playbooks/types.ts — must return one hit inside PlaybookSessionState.
4. Byte-count check: `wc -c el-templo-bot/test/fixtures/pb1-e1a-lead-rendered.snap.txt` — delta vs Phase 89 baseline (18,291) must be ≤ +50.
5. Full bot test suite: pnpm test shows all tests green (baseline 537, post-alignment may be ±0 if alignments are in-place not add/remove).
6. Manual scenario run (throwaway script or REPL): feed `{ stageId: "PB1.E1A", inbound: "primera vez", priorTurnCount: 0 }` → discoveryAnswered false. Feed `{ stageId: "PB1.E1A", inbound: "nunca entrené, quiero arrancar", priorTurnCount: 1 }` → discoveryAnswered true. Feed `{ stageId: "PB1.E1A", inbound: "sí", priorTurnCount: 2 }` with 3rd substantive turn (hasMinimumContent true — "sí" is 2 chars so it's NOT substantive; pick "no sé la verdad" as monosyllabic-but-substantive) → escape fires, log.warn emitted, next stage = PB1.E2A.
    </manual>
    <sampling_rate>run after this task commits (phase complete)</sampling_rate>
  </verify>
  <done>
    1. `PlaybookSessionState.discoveryTurnCount` exists as an optional number. 2. `discoveryAnswered` for E1A/E1B evaluates to `content_gate AND turn_count >= 2 AND existing_gates`. 3. Escape hatch fires iff `inDiscoveryE1 && isSubstantiveTurn && newTurnCount >= 3 && !content_gate`, emits Pino warn with greppable `"discovery escape fired"` payload, and forces advance to `PB1.E2A`. 4. `completionCriteria` for PB1.E1A and PB1.E1B contains "múltiples categorías" and "al menos 2 turnos" (or equivalent wording), no longer references "dijo primera vez avanzar". 5. Rendered PB1.E1A snap byte-delta ≤ +50 chars. 6. `pnpm test` is fully green (alignments in place). 7. `pnpm tsc --noEmit` exits 0. 8. Single atomic commit on branch.
  </done>
</task>

</tasks>

<verification>
Phase-level goal-backward checks (must ALL pass before SUMMARY.md writes):

1. **STAGE-01 observable:** `hasStageSpecificContent("primera vez", "PB1.E1A") === false` AND `hasStageSpecificContent("nunca entrené, quiero arrancar", "PB1.E1A") === true` — verified by Task 1 done-check + Task 2 manual scenario.
2. **STAGE-02 observable:** `discoveryAnswered` for E1A requires both content gate AND turn_count ≥ 2 (AND, not OR) — verified by Task 2 manual scenario with priorTurnCount=0 and priorTurnCount=1.
3. **Sibling symmetry:** E2A / E2B / E3 branches of `hasStageSpecificContent` byte-identical to pre-90 (diff check).
4. **Escape hatch:** after N=3 substantive E1A/E1B turns without passing the gate, next stage advances and `log.warn` fires with payload (stageId, phone, turnCount, recentUserMessages).
5. **completionCriteria updated:** both E1A and E1B reference "múltiples categorías o al menos 2 turnos" wording.
6. **Char budget:** KGATE-05 headroom ≥ +575 chars (Phase 89 banked 625, Phase 90 may consume up to 50).
7. **Full test suite:** `pnpm test` in `el-templo-bot/` is green (baseline was 537/537; post-alignment may remain 537 if updates are in-place).
8. **TypeScript:** `pnpm tsc --noEmit` exits 0.
9. **Phase 92 handoff:** SUMMARY.md enumerates the new behaviors for RLOK-01 to lock (category-gate, AND gate, escape hatch, completionCriteria prose).
   </verification>

<success_criteria>

- All nine verification checks above pass.
- Single atomic commit on current branch (`feature/whatsapp-bot-scaffold` — or whatever's active at execute time; do not create a new branch).
- `90-01-SUMMARY.md` written to `.planning/phases/90-stage-heuristic-tightening/` following the shape of `89-01-SUMMARY.md` (frontmatter + sections: Final Implementation, Measurements, Observable Truths Status, Test Alignment, Deviations from Plan, Phase 92 Handoff Notes).
- STATE.md updated: position advances to "Phase 90 complete, next: `/gsd:plan-phase 91`".
- ROADMAP.md Phase 90 entry: `- [ ]` → `- [x]` (completed <date>).
  </success_criteria>

<output>
After completion, create `.planning/phases/90-stage-heuristic-tightening/90-01-SUMMARY.md`
</output>
