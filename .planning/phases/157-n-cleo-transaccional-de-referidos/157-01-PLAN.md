---
phase: 157-n-cleo-transaccional-de-referidos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/schema/referrals.ts
  - el-templo-api/src/db/schema/referral-credits.ts
  - el-templo-api/src/db/schema/users.ts
  - el-templo-api/src/db/schema/subscriptions.ts
  - el-templo-api/src/db/schema/index.ts
  - el-templo-api/src/db/migrations/0176_referrals_core.sql
autonomous: true
requirements: [REF-01, REF-04, DESC-04, AURA-02]
user_setup: []

must_haves:
  truths:
    - "La tabla referrals existe con referredId UNIQUE (un miembro tiene a lo sumo un referidor, D-14/REF-04)"
    - "Cada socio puede tener un referralCode único y un referredBy (self-FK) en users (REF-01)"
    - "subscriptions tiene columnas referralDiscountPercent/referralDiscountAmount separadas de auraDiscount* (D-23)"
    - "aura_config tiene la fila referral con default_amount=10 y system_settings tiene referral.max_percent_cap=40 (D-12/D-22/DESC-04/AURA-02)"
    - "referral_credits existe con idempotencia por subscriptionId (D-18)"
  artifacts:
    - path: "el-templo-api/src/db/schema/referrals.ts"
      provides: "Modelo del vínculo de referido (status/attributionChannel enums, FKs, UNIQUE referredId)"
      contains: "referrals"
    - path: "el-templo-api/src/db/schema/referral-credits.ts"
      provides: "Registro auditable append-only del descuento aplicado sin inflar saldo"
      contains: "referralCredits"
    - path: "el-templo-api/src/db/migrations/0176_referrals_core.sql"
      provides: "CREATE TABLE referrals + referral_credits, ALTER users/subscriptions, seed idempotente"
      contains: "CREATE TABLE"
  key_links:
    - from: "el-templo-api/src/db/schema/index.ts"
      to: "referrals.ts + referral-credits.ts"
      via: "export * re-export"
      pattern: "export \\* from \"./referrals\""
    - from: "0176_referrals_core.sql"
      to: "aura_config / system_settings"
      via: "INSERT ... SELECT ... WHERE NOT EXISTS (idempotente)"
      pattern: "WHERE NOT EXISTS"
---

<objective>
Fundaciones de datos del sistema de referidos: dos tablas nuevas (`referrals`, `referral_credits`), dos columnas en `users` (`referralCode`, `referredBy`), dos columnas nuevas en `subscriptions` (`referralDiscountPercent`, `referralDiscountAmount`, D-23 — NO reusar `auraDiscount*`), y una migración hand-written 0176 que crea todo y siembra la calibración (10% por vínculo, tope 40%).

Purpose: sin este esquema no hay dónde escribir la atribución (plan 03), computar el descuento (plan 04) ni parametrizar la magnitud (D-12/AURA-02). Es la base foundational de la que dependen todos los demás planes.
Output: schema Drizzle + migración SQL 0176 lista para aplicar por el runner custom.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/157-n-cleo-transaccional-de-referidos/157-CONTEXT.md
@.planning/phases/157-n-cleo-transaccional-de-referidos/157-PATTERNS.md
@CLAUDE.md
@.claude/skills/el-templo-db-migrations/SKILL.md

<interfaces>
<!-- Analog schema shapes the executor must replicate. Verified 2026-07-10. -->

aura_config columns (el-templo-api/src/db/schema/aura-config.ts): source_type (enum), default_amount int, description varchar(255), is_active boolean default true, created_at, updated_at. Enum value order for aura_config_source_type includes "referral" already.

users self-FK pattern (users.ts:163): createdBy: int("created_by").references((): AnyMySqlColumn => users.id, { onDelete: "set null" }); index at :191. AnyMySqlColumn is already imported at users.ts:11.

subscriptions existing discount columns (subscriptions.ts:60-61): auraDiscount: int("aura_discount"), auraDiscountPercent: int("aura_discount_percent"). Do NOT reuse these — add NEW columns.

aura_transactions idempotency (aura-transactions.ts:42-49): uniqueIndex("unique_user_source_ref").on(userId, sourceType, referenceType, referenceId).

Migration numbering: origin/master top is 0175 (verified 2026-07-10). New migration = 0176. Re-verify with `git ls-tree origin/master el-templo-api/src/db/migrations/ | tail` on the execution branch before writing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Crear schemas referrals.ts y referral-credits.ts + exports</name>
  <files>el-templo-api/src/db/schema/referrals.ts, el-templo-api/src/db/schema/referral-credits.ts, el-templo-api/src/db/schema/index.ts</files>
  <read_first>
    - el-templo-api/src/db/schema/aura-transactions.ts (enum a module scope + mysqlTable + uniqueIndex + relations — analog exacto)
    - el-templo-api/src/db/schema/user-sepa-details.ts (tabla aditiva reciente, estilo de columnas y timestamps)
    - el-templo-api/src/db/schema/users.ts (patrón self-FK AnyMySqlColumn en :11 y :163)
    - el-templo-api/src/db/schema/index.ts (línea :54 export * from "./user-sepa-details")
  </read_first>
  <action>
    Crear `referrals.ts`: `mysqlTable("referrals", ...)` con `id` autoincrement PK; `referrerId: int("referrer_id").references(() => users.id).notNull()`; `referredId: int("referred_id").references(() => users.id).notNull().unique()` (D-14/REF-04); `status` como `mysqlEnum("status", ["pending","qualified","revoked"])` declarado a module scope, `.notNull().default("pending")`; `attributionChannel` como `mysqlEnum("attribution_channel", ["self_service","assisted"]).notNull()`; `qualifiedAt: timestamp("qualified_at")` nullable; `createdBy: int("created_by").references((): AnyMySqlColumn => users.id, { onDelete: "set null" })` (importar `AnyMySqlColumn`); `createdAt`/`updatedAt` clonados de user-sepa-details.ts:30-31 (`.defaultNow().notNull()` / `.onUpdateNow().notNull()`). Exportar un `relations(...)` con `referrer`/`referred` (dos `one(users, ...)`). Las FKs a `users` desde otra tabla usan la forma `() => users.id`; solo `createdBy` (self-ref conceptual no aplica aquí porque apunta a users, forma simple OK).
    Crear `referral-credits.ts`: `mysqlTable("referral_credits", ...)` append-only con `id` autoincrement; `userId: int("user_id").references(() => users.id).notNull()`; `subscriptionId: int("subscription_id").references(() => subscriptions.id).notNull()`; `percent: int("percent").notNull()`; `amount: int("amount").notNull()`; `createdAt: timestamp("created_at").defaultNow().notNull()`; `uniqueIndex("unique_referral_credit_sub").on(table.subscriptionId)` — idempotencia POR CARGO (subscriptionId, NO referralId; una carga = una fila).
    Editar `index.ts`: agregar `export * from "./referrals";` y `export * from "./referral-credits";` junto a la línea :54.
    Comentar cada columna nueva con su REQ/decisión (disciplina de inline-comments del repo). No incluir `;` dentro de comentarios en el schema (irrelevante en TS pero mantener consistencia si copiás notas al SQL).
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>referrals.ts y referral-credits.ts existen, exportados en index.ts, `pnpm build` (tsc) verde.</done>
  <acceptance_criteria>
    - `grep -c 'mysqlEnum("status"' el-templo-api/src/db/schema/referrals.ts` devuelve 1 y los valores son exactamente `["pending","qualified","revoked"]`
    - `grep -c 'mysqlEnum("attribution_channel"' el-templo-api/src/db/schema/referrals.ts` devuelve 1 con valores `["self_service","assisted"]`
    - `grep -c '.unique()' el-templo-api/src/db/schema/referrals.ts` >= 1 (referredId)
    - `grep -c 'unique_referral_credit_sub' el-templo-api/src/db/schema/referral-credits.ts` == 1
    - `grep -c 'export \* from "./referrals"' el-templo-api/src/db/schema/index.ts` == 1 y lo mismo para referral-credits
    - `cd el-templo-api && pnpm build` finaliza con exit code 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Agregar referralCode/referredBy a users y referralDiscount* a subscriptions</name>
  <files>el-templo-api/src/db/schema/users.ts, el-templo-api/src/db/schema/subscriptions.ts</files>
  <read_first>
    - el-templo-api/src/db/schema/users.ts (createdBy en :163, su index en :191, import AnyMySqlColumn :11)
    - el-templo-api/src/db/schema/subscriptions.ts (columnas auraDiscount/auraDiscountPercent en :60-61 — referencia de forma, NO reusar)
  </read_first>
  <action>
    En `users.ts`: agregar `referralCode: varchar("referral_code", { length: 16 }).unique()` (nullable — fill lazy/eager per D-25, REF-01) y `referredBy: int("referred_by").references((): AnyMySqlColumn => users.id, { onDelete: "set null" })` clonando verbatim el patrón de `createdBy` (:163-165). Agregar `index("idx_users_referred_by").on(table.referredBy)` junto al bloque de índices (~:191).
    En `subscriptions.ts`: agregar DOS columnas NUEVAS `referralDiscountPercent: int("referral_discount_percent")` y `referralDiscountAmount: int("referral_discount_amount")` (D-23 — separadas de `auraDiscount*` en :60-61, NO reusar). Ambas nullable.
    Comentar cada columna con su decisión (D-23/D-25/REF-01).
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>users tiene referral_code (unique) + referred_by (self-FK, index); subscriptions tiene referral_discount_percent + referral_discount_amount; tsc verde.</done>
  <acceptance_criteria>
    - `grep -c 'referral_code' el-templo-api/src/db/schema/users.ts` >= 1 y aparece `.unique()` en la misma columna
    - `grep -c 'idx_users_referred_by' el-templo-api/src/db/schema/users.ts` == 1
    - `grep -c 'referral_discount_percent\|referral_discount_amount' el-templo-api/src/db/schema/subscriptions.ts` == 2
    - `grep -c 'aura_discount' el-templo-api/src/db/schema/subscriptions.ts` sigue siendo >= 2 (columnas viejas intactas, no renombradas)
    - `cd el-templo-api && pnpm build` exit code 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Escribir migración hand-written 0176_referrals_core.sql</name>
  <files>el-templo-api/src/db/migrations/0176_referrals_core.sql</files>
  <read_first>
    - el-templo-api/src/db/migrations/0171_user_sepa_details.sql (CREATE TABLE IF NOT EXISTS + CONSTRAINT PRIMARY KEY/UNIQUE/FOREIGN KEY, header comment)
    - el-templo-api/src/db/migrations/0157_seed_finance_overdue_threshold.sql (INSERT ... SELECT ... WHERE NOT EXISTS idempotente)
    - el-templo-api/src/db/schema/referrals.ts y referral-credits.ts (recién creados — los nombres físicos de columnas/enums DEBEN coincidir byte-for-byte)
    - .claude/skills/el-templo-db-migrations/SKILL.md (numeración, nunca `;` en comentarios, schema+SQL mismo commit)
  </read_first>
  <action>
    RE-VERIFICAR primero `git ls-tree origin/master el-templo-api/src/db/migrations/ | tail` en la rama de ejecución: si el tope es 0175, el archivo es `0176_referrals_core.sql`; si master avanzó, usar el siguiente número libre. La rama de ejecución debe partir de origin/master actualizado.
    Header comment (3-4 líneas `--`): "hand-written: drizzle-kit generate roto por drift". CERO `;` dentro de cualquier línea `--` (el runner splittea por `;`).
    Statements: (1) `CREATE TABLE IF NOT EXISTS referrals` con columnas físicas `id`, `referrer_id`, `referred_id` (UNIQUE), `status` enum('pending','qualified','revoked') DEFAULT 'pending', `attribution_channel` enum('self_service','assisted'), `qualified_at` timestamp NULL, `created_by` int NULL, `created_at`, `updated_at`, más CONSTRAINTs FK a `users(id)` (created_by con ON DELETE SET NULL) — orden de valores del enum byte-for-byte igual al schema. (2) `CREATE TABLE IF NOT EXISTS referral_credits` con `id`, `user_id`, `subscription_id`, `percent`, `amount`, `created_at`, UNIQUE KEY sobre `subscription_id`, FKs a users/subscriptions. (3) `ALTER TABLE users ADD COLUMN referral_code varchar(16) NULL`, `ADD COLUMN referred_by int NULL`, `ADD UNIQUE KEY uq_users_referral_code (referral_code)`, `ADD INDEX idx_users_referred_by (referred_by)`, y FK de referred_by a users(id) ON DELETE SET NULL. (4) `ALTER TABLE subscriptions ADD COLUMN referral_discount_percent int NULL`, `ADD COLUMN referral_discount_amount int NULL`. (5) seed idempotente `INSERT INTO aura_config (source_type, default_amount, description, is_active) SELECT 'referral', 10, 'Descuento por vínculo de referido activo', 1 WHERE NOT EXISTS (SELECT 1 FROM aura_config WHERE source_type='referral')` (D-12/D-22/AURA-02). (6) seed idempotente `INSERT INTO system_settings (setting_key, setting_value) SELECT 'referral.max_percent_cap', '40' WHERE NOT EXISTS (SELECT 1 FROM system_settings WHERE setting_key='referral.max_percent_cap')` (D-12/DESC-04). NO backfillear ~2000 códigos acá (D-25).
    NO correr `pnpm db:migrate` sobre staging/prod: eso lo maneja el pipeline/operador (staging-first STRICT). Localmente se puede aplicar contra la DB de test para validar.
  </action>
  <verify>
    <automated>grep -nE '^\s*--.*;' el-templo-api/src/db/migrations/0176_referrals_core.sql | grep -c ';' ; echo "expect 0 comment-semicolons above"</automated>
  </verify>
  <done>0176_referrals_core.sql existe, sin `;` en comentarios, enums byte-for-byte con el schema, seeds idempotentes, sin backfill de códigos.</done>
  <acceptance_criteria>
    - El archivo se llama exactamente `0176_referrals_core.sql` (o el siguiente libre si master avanzó) — verificado contra `git ls-tree origin/master`
    - `grep -E '^\s*--' el-templo-api/src/db/migrations/0176_referrals_core.sql | grep -c ';'` == 0 (ningún punto y coma en comentarios)
    - `grep -c "CREATE TABLE IF NOT EXISTS" el-templo-api/src/db/migrations/0176_referrals_core.sql` == 2
    - `grep -c "WHERE NOT EXISTS" el-templo-api/src/db/migrations/0176_referrals_core.sql` == 2 (ambos seeds idempotentes)
    - `grep -c "referral_discount_percent\|referral_discount_amount" 0176...sql` == 2 (path relativo desde el dir de migraciones)
    - El orden de valores de los enums `status` y `attribution_channel` en el SQL coincide con el schema Drizzle (revisión source-to-source)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                                                          |
| ---------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| runner de migraciones → MySQL      | El SQL hand-written se ejecuta con privilegios de schema; un `;` mal ubicado o un enum desalineado corrompe el apply |
| seed → aura_config/system_settings | valores de calibración (10%/40%) que gobiernan erosión de ingreso                                                    |

## STRIDE Threat Register

| Threat ID | Category  | Component               | Disposition | Mitigation Plan                                                                                                    |
| --------- | --------- | ----------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------ |
| T-157-01  | Tampering | 0176 SQL enums          | mitigate    | Orden/nombres de valores de enum byte-for-byte contra el schema (acceptance criteria + Pitfall 3)                  |
| T-157-02  | Denial    | runner splittea por `;` | mitigate    | Cero `;` en comentarios `--` (verify automatizado)                                                                 |
| T-157-03  | Elevation | doble-referidor         | mitigate    | `referred_id` UNIQUE a nivel DB (D-14) — el segundo reclamo falla en el constraint, no en app logic                |
| T-157-SC  | Tampering | npm/pip/cargo installs  | accept      | Esta fase NO instala paquetes nuevos (todo reuso de infra existente); no aplica el gate de legitimidad de paquetes |

</threat_model>

<verification>
- `cd el-templo-api && pnpm build` (tsc) verde con los schemas nuevos.
- Enum column names/valores del SQL == schema Drizzle.
- Sin `;` en comentarios del SQL.
- Aplicación en staging/prod: diferida al pipeline (operador corre `pnpm db:migrate` en staging, verifica fila en `_migrations` + `SHOW COLUMNS`, luego aprueba prod). NO ejecutar en este plan.
</verification>

<success_criteria>

- Tablas `referrals` y `referral_credits` definidas en schema + migración.
- `users.referral_code` (UNIQUE) y `users.referred_by` (self-FK, index) agregadas.
- `subscriptions.referral_discount_percent`/`referral_discount_amount` agregadas (separadas de auraDiscount\*).
- `aura_config['referral']=10` y `system_settings['referral.max_percent_cap']=40` sembrados idempotentemente.
- Migración numerada 0176+ tras re-verificar master, sin `;` en comentarios.
  </success_criteria>

<output>
Crear `.planning/phases/157-n-cleo-transaccional-de-referidos/157-01-SUMMARY.md` al terminar.
</output>
