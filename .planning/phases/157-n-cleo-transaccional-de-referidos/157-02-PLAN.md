---
phase: 157-n-cleo-transaccional-de-referidos
plan: 02
type: execute
wave: 2
depends_on: ["157-01"]
files_modified:
  - el-templo-api/src/modules/referrals/service.ts
  - el-templo-api/src/modules/referrals/types.ts
  - el-templo-api/src/scripts/backfill-referral-codes.ts
  - el-templo-api/test/referrals/code-generation.test.ts
  - el-templo-api/test/referrals/discount-computation.test.ts
  - el-templo-api/test/referrals/aura-annotation.test.ts
  - el-templo-api/test/referrals/backfill-codes.test.ts
autonomous: true
requirements: [REF-01, DESC-02, DESC-03, DESC-04, DESC-05, AURA-01, AURA-02]

must_haves:
  truths:
    - "Un socio obtiene un referralCode legible tipo FRAN-A3B2, único por colisión-DB+retry (D-16, REF-01)"
    - "computeReferralDiscountPercent suma 10% por vínculo qualified cuya contraparte está activa, topeado a 40% (DESC-02/03/04)"
    - "'Activo' se determina SOLO con deriveCoveredUntil de la contraparte, nunca con users.status (D-09/D-24)"
    - "El % por vínculo y el tope se leen de aura_config/system_settings con fallback 10/40 (D-12/AURA-02)"
    - "recordReferralCredit escribe referral_credits + anotación aura_transactions sourceType='referral' SIN inflar balance (AURA-01/D-06/D-18)"
    - "Existe un script idempotente de backfill de referralCode para los ~2000 socios existentes, disparable a demanda, que NO corre en ninguna migración ni pipeline (D-25/D-17)"
  artifacts:
    - path: "el-templo-api/src/modules/referrals/service.ts"
      provides: "generateReferralCode, resolveReferralCode, computeReferralDiscountPercent, qualifyFirstPayment, recordReferralCredit, getReferralConfig"
      min_lines: 80
    - path: "el-templo-api/test/referrals/discount-computation.test.ts"
      provides: "cobertura del cómputo simétrico condicional con tope"
    - path: "el-templo-api/src/scripts/backfill-referral-codes.ts"
      provides: "script one-shot idempotente que puebla users.referral_code para socios sin código (dry-run + --apply, D-25)"
  key_links:
    - from: "referrals/service.ts computeReferralDiscountPercent"
      to: "deriveCoveredUntil(db, counterpartyId)"
      via: "import desde subscriptions/service.ts"
      pattern: "deriveCoveredUntil"
    - from: "referrals/service.ts recordReferralCredit"
      to: "aura_transactions (INSERT directo amount=0)"
      via: "db.insert sin AuraService.award/spend"
      pattern: "referral_credits|aura_transactions"
    - from: "scripts/backfill-referral-codes.ts"
      to: "ReferralService.generateReferralCode"
      via: "reuso del generador (no reimplementa el formato)"
      pattern: "generateReferralCode"
---

<objective>
Servicio de referidos: la lógica pura del feature en un módulo dedicado (`modules/referrals/service.ts`) que los planes 03 (atribución) y 04 (cobro) consumen. Cinco piezas: generación de código legible único, resolución de código→referrerId, lectura de config con fallback, cómputo del descuento simétrico/condicional/topeado, y escritura del registro auditable (referral_credits + anotación AURA sin saldo). Más un script one-shot de backfill (D-25) para poblar el código de los ~2000 socios ya existentes, a demanda y fuera de todo pipeline.

Purpose: centralizar la mecánica "de la plata" (D-09/D-10/D-24 + DESC-02..05 + AURA-01) para que el hook de cobro y las rutas de atribución no reimplementen nada. Es el corazón del vínculo. El backfill cierra el borde de los socios previos a la feature sin generar 2000 códigos en el deploy (D-17/D-25).
Output: servicio con DI (db, log) + tests de integración wave-0 (code-generation, discount-computation, aura-annotation) + script de backfill idempotente con su test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/157-n-cleo-transaccional-de-referidos/157-CONTEXT.md
@.planning/phases/157-n-cleo-transaccional-de-referidos/157-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Verified 2026-07-10. -->

deriveCoveredUntil (subscriptions/service.ts:176): export async function deriveCoveredUntil(db: MySql2Database<typeof schema>, userId: number): Promise<string | null> — MAX(endDate) sobre subs status active|scheduled. Uso: const cov = await deriveCoveredUntil(db, counterpartyId); const active = cov !== null && cov >= todayStr.

Config read-with-fallback (settings/service.ts:37-45): single select().where(eq(key)).limit(1) + fallback hardcodeado si la fila falta. Constructor DI (db, log) en :26-30.

aura_config: source_type (enum, incluye 'referral'), default_amount int. Leer default_amount WHERE source_type='referral', fallback 10.
system_settings: setting_key/setting_value. Leer 'referral.max_percent_cap', fallback 40.

aura_transactions (aura-transactions.ts): sourceType enum incluye "referral"; uniqueIndex(userId, sourceType, referenceType, referenceId). Para la anotación SIN inflar balance: INSERT directo con amount=0, sourceType='referral', referenceType='subscription', referenceId=subscriptionId (per-charge, idempotente y no toca getBalance que suma amounts). NO usar AuraService.award (infla) ni spend (decrementa).

referrals table (157-01): referrerId, referredId (UNIQUE), status ('pending'|'qualified'|'revoked'), attributionChannel, qualifiedAt, createdBy.
referral_credits table (157-01): userId, subscriptionId, percent, amount + uniqueIndex(subscriptionId).

Script one-shot pattern (src/scripts/backfill-historical-payments.ts): header-doc con propósito+idempotencia+usage; `import "dotenv/config"`; `createSingleConnection` desde ../db/index; dry-run por defecto, `--apply` para escribir; corre vía `pnpm tsx src/scripts/<x>.ts` (local) o `node dist/scripts/<x>.js` (server). NO se referencia en package.json scripts de arranque ni en el runner de migraciones.

Test harness (test/subscriptions/renewal.test.ts:1-34): createTestApp, getAuthToken, cleanAllTestData. Fixtures en test/subscriptions/\_helpers.ts: createPlan, createMember, assignPlan, seedAuraBalance, todayStr/dateOffsetStr.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold del servicio + generación de código + resolución + config</name>
  <files>el-templo-api/src/modules/referrals/service.ts, el-templo-api/src/modules/referrals/types.ts, el-templo-api/test/referrals/code-generation.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/settings/service.ts (DI constructor :26-30 + getFlag :37-45 read-with-fallback)
    - el-templo-api/src/db/schema/referrals.ts + users.ts (columnas referralCode/referredBy del plan 01)
    - el-templo-api/src/db/schema/promo-plans.ts (referencia de "código único + retry", NO reusar directamente)
  </read_first>
  <behavior>
    - generateReferralCode(userId): produce `PREFIJO-XXXX` (prefijo = primeras 4 letras del nombre en mayúsculas, sufijo alfanumérico); si el UNIQUE de la DB colisiona, reintenta con nuevo sufijo (unicidad por constraint, NO por entropía — Security V6); persiste en users.referral_code y lo devuelve; si ya existe, devuelve el existente (lazy, D-17/D-25). Tras N reintentos agotados (colisión persistente), lanza un error que el llamador (plan 03) traga best-effort — el método en sí NO decide la política de fallo del alta.
    - resolveReferralCode(code): devuelve el userId del dueño del code o null si no existe.
    - getReferralConfig(): { percentPerLink, maxPercentCap } leídos de aura_config['referral'].default_amount (fallback 10) y system_settings['referral.max_percent_cap'] (fallback 40).
  </behavior>
  <action>
    Crear `modules/referrals/service.ts` con clase `ReferralService` (constructor DI `db`, `log` como settings/service.ts:26-30) y `modules/referrals/types.ts` con las interfaces de retorno. Implementar `generateReferralCode`, `resolveReferralCode`, `getReferralConfig`. La generación: prefijo derivado del `firstName` (fallback a algo estable si es null), sufijo aleatorio corto (no crypto RNG — Math.random alcanza porque la unicidad la garantiza el UNIQUE + retry, máx N reintentos). NO reusar `auraDiscountPercent`; el % vive en aura_config. Sin `any`; `catch (err: unknown)` con `instanceof Error`. Logger vía `this.log`, nunca console.
    Tests en `test/referrals/code-generation.test.ts` (harness verbatim de renewal.test.ts:1-34): unicidad ante colisión forzada, idempotencia (2da llamada devuelve el mismo), formato PREFIJO-XXXX, config con y sin fila sembrada (fallback).
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm test referrals/code-generation 2>&1 | tail -15</automated>
  </verify>
  <done>ReferralService genera códigos únicos e idempotentes, resuelve code→userId, lee config con fallback; test code-generation verde (corre en CI).</done>
  <acceptance_criteria>
    - `test/referrals/code-generation.test.ts` cubre: (a) formato `^[A-Z]+-[A-Z0-9]+$`, (b) 2da llamada devuelve el mismo código, (c) colisión forzada → reintenta y persiste distinto, (d) getReferralConfig sin fila → {10,40}
    - `grep -c "console\." el-templo-api/src/modules/referrals/service.ts` == 0
    - `grep -c ": any" el-templo-api/src/modules/referrals/service.ts` == 0
    - `cd el-templo-api && pnpm build` exit code 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: computeReferralDiscountPercent + qualifyFirstPayment + recordReferralCredit</name>
  <files>el-templo-api/src/modules/referrals/service.ts, el-templo-api/test/referrals/discount-computation.test.ts, el-templo-api/test/referrals/aura-annotation.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/subscriptions/service.ts (deriveCoveredUntil :176-194 — usar tal cual)
    - el-templo-api/src/db/schema/aura-transactions.ts (uniqueIndex + columnas para la anotación amount=0)
    - el-templo-api/src/db/schema/referral-credits.ts (plan 01)
    - el-templo-api/src/modules/referrals/service.ts (task 1, para extender la misma clase)
  </read_first>
  <behavior>
    - computeReferralDiscountPercent(userId): consulta referrals donde `or(eq(referrerId,userId), eq(referredId,userId))` AND `status='qualified'`; para cada vínculo determina la contraparte (el otro id) y cuenta como activo SOLO si `deriveCoveredUntil(db, counterpartyId) >= hoy` (D-09/D-24, nunca users.status); devuelve `Math.min(activos * percentPerLink, maxPercentCap)` (DESC-02/03/04). Cero vínculos activos → 0. Un vínculo cuya contraparte venció NO cuenta ese ciclo.
    - qualifyFirstPayment(payerUserId): si el payer tiene un vínculo `pending` como referredId, lo marca `qualified` + `qualifiedAt=now` con UPDATE guardado (`WHERE referred_id=? AND status='pending'`, idempotente); no-op si no hay pending o ya qualified (DESC-01). NOTA: el gate de "primer pago con pricePaid>0" lo aplica el llamador (plan 04, D-20); este método solo hace el flip idempotente.
    - recordReferralCredit(userId, subscriptionId, percent, amount): INSERT en referral_credits (idempotente por subscriptionId, ignora duplicado) + INSERT directo en aura_transactions con amount=0, sourceType='referral', referenceType='subscription', referenceId=subscriptionId (AURA-01: anotación sin inflar balance; NO AuraService.award/spend).
  </behavior>
  <action>
    Extender `ReferralService`. Importar `deriveCoveredUntil` desde subscriptions/service.ts (o su ubicación exacta). Calcular "hoy" como `new Date().toISOString().split("T")[0]` para comparar contra el string de deriveCoveredUntil. El cómputo es server-side puro (sin input del cliente — DESC-05). La anotación AURA debe tolerar el UNIQUE `unique_user_source_ref` (INSERT idempotente / onDuplicate ignore) para no romper en re-cobros del mismo subscriptionId. recordReferralCredit se llama una vez por parte por cargo.
    Tests: `test/referrals/discount-computation.test.ts` — 0 vínculos→0; 1 vínculo qualified contraparte activa→10; 4 vínculos→40 (tope, no 50 con 5); contraparte vencida→ese vínculo no suma; bidireccionalidad (cuenta como referrer Y como referred). `test/referrals/aura-annotation.test.ts` — recordReferralCredit inserta referral_credits + fila aura_transactions amount=0 sourceType='referral', y getBalance del usuario NO cambia; segundo llamado con mismo subscriptionId no duplica.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm test referrals/discount-computation referrals/aura-annotation 2>&1 | tail -20</automated>
  </verify>
  <done>computeReferralDiscountPercent aplica tope y usa deriveCoveredUntil; qualifyFirstPayment flip idempotente; recordReferralCredit escribe sin tocar balance; tests verdes.</done>
  <acceptance_criteria>
    - discount-computation.test.ts asserta: cap a maxPercentCap con 5 vínculos activos (resultado 40, no 50), y que una contraparte con `deriveCoveredUntil < hoy` no suma
    - aura-annotation.test.ts asserta: `SELECT balance` (o getBalance) idéntico antes/después de recordReferralCredit, y fila en aura_transactions con amount=0 sourceType='referral'
    - `grep -c "auraService.award\|auraService.spend\|\.award(\|\.spend(" el-templo-api/src/modules/referrals/service.ts` == 0 (anotación por INSERT directo)
    - `grep -c "users.status\|userStatus" el-templo-api/src/modules/referrals/service.ts` == 0 ("activo" solo por deriveCoveredUntil)
    - `grep -c "deriveCoveredUntil" el-templo-api/src/modules/referrals/service.ts` >= 1
    - `cd el-templo-api && pnpm build` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Script one-shot de backfill de referralCode (D-25)</name>
  <files>el-templo-api/src/scripts/backfill-referral-codes.ts, el-templo-api/test/referrals/backfill-codes.test.ts</files>
  <read_first>
    - el-templo-api/src/scripts/backfill-historical-payments.ts (patrón one-shot: header-doc, dotenv, createSingleConnection, dry-run + --apply)
    - el-templo-api/src/modules/referrals/service.ts (task 1: generateReferralCode — reusar, NO reimplementar el formato)
    - el-templo-api/src/db/schema/users.ts (columna referral_code del plan 01)
  </read_first>
  <behavior>
    - El script selecciona los users con `referral_code IS NULL` y les genera un código con `ReferralService.generateReferralCode(userId)` (o el mismo algoritmo reusado), poblando la columna.
    - Idempotente: una 2da corrida NO toca a los que YA tienen código (solo procesa `referral_code IS NULL`); re-correr es seguro y no cambia códigos asignados.
    - Dry-run por defecto (reporta cuántos socios recibirían código sin escribir); `--apply` para persistir. Best-effort por socio: si uno colisiona irrecuperablemente, loguea y sigue con el resto (no aborta el batch).
    - NO se ejecuta en ninguna migración ni en el pipeline de deploy: es un archivo en src/scripts/ disparable a demanda (pnpm tsx / node dist).
  </behavior>
  <action>
    Crear `el-templo-api/src/scripts/backfill-referral-codes.ts` siguiendo el patrón de `backfill-historical-payments.ts` (header-doc con propósito + idempotencia + usage local/server, `import "dotenv/config"`, `createSingleConnection`). Query `WHERE referral_code IS NULL`, iterar y llamar `generateReferralCode(userId)` (importar/instanciar ReferralService, o extraer el generador a una función pura reutilizable si es más limpio — a criterio, sin duplicar el formato). Flag `--apply` (default dry-run). Contador de asignados/saltados/errores en el log final. NO agregar el script a `package.json` scripts de arranque, NI al runner de migraciones, NI al deploy — solo se corre a mano. Sin `any`, sin console (usar el logger disponible o el patrón de logging del script existente).
    Test `test/referrals/backfill-codes.test.ts`: sembrar 2 users sin referral_code, correr la lógica de backfill (exportar una función `backfillReferralCodes(db, { apply: true })` invocable desde el test), assertar que ambos quedan con código válido; correrla de nuevo y assertar que los códigos NO cambian (idempotencia); un user que ya tenía código no es tocado.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm test referrals/backfill-codes 2>&1 | tail -15</automated>
  </verify>
  <done>Script de backfill idempotente creado (dry-run + --apply), reusa generateReferralCode, fuera de todo pipeline; test de idempotencia verde.</done>
  <acceptance_criteria>
    - backfill-codes.test.ts asserta que los users sin código quedan con formato `^[A-Z]+-[A-Z0-9]+$` tras el backfill
    - backfill-codes.test.ts asserta idempotencia: 2da corrida NO cambia los códigos ya asignados
    - `grep -rc "backfill-referral-codes" el-templo-api/package.json el-templo-api/src/db/run-migrations.ts` == 0 (no está cableado a ningún pipeline)
    - `grep -c "generateReferralCode\|backfillReferralCodes" el-templo-api/src/scripts/backfill-referral-codes.ts` >= 1 (reusa el generador, no duplica el formato)
    - `cd el-templo-api && pnpm build` exit code 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                                   |
| -------------------------------------- | ------------------------------------------------------------- |
| cómputo de descuento → ingreso del gym | un cap mal aplicado erosiona ingreso sin freno                |
| anotación AURA → saldo gastable        | un INSERT con amount≠0 daría crédito gastable no intencionado |

## STRIDE Threat Register

| Threat ID | Category    | Component                      | Disposition | Mitigation Plan                                                                                       |
| --------- | ----------- | ------------------------------ | ----------- | ----------------------------------------------------------------------------------------------------- |
| T-157-04  | Elevation   | recordReferralCredit           | mitigate    | INSERT directo con amount=0; test asserta balance sin cambio; prohibido award/spend (acceptance)      |
| T-157-05  | Tampering   | computeReferralDiscountPercent | mitigate    | `Math.min(activos*pct, cap)` server-side, sin input del cliente (DESC-05); test de tope               |
| T-157-06  | Repudiation | qualifyFirstPayment race       | mitigate    | UPDATE guardado `WHERE status='pending'` idempotente (race-safe)                                      |
| T-157-07  | Spoofing    | contraparte "activa" falsa     | mitigate    | fuente única deriveCoveredUntil (cobertura vigente), nunca users.status derivado por cron (D-09/D-24) |
| T-157-17  | Denial      | backfill corre en deploy       | mitigate    | script fuera de package.json/migraciones/deploy; solo a demanda (acceptance grep==0)                  |

</threat_model>

<verification>
- `pnpm build` verde.
- Tests referrals/code-generation, discount-computation, aura-annotation, backfill-codes verdes en CI.
- Sin console.*, sin `any`, sin award/spend en el servicio.
- Script de backfill presente y desconectado de todo pipeline.
</verification>

<success_criteria>

- Código único legible generado idempotentemente (REF-01).
- Descuento simétrico condicional topeado computado con deriveCoveredUntil (DESC-02/03/04, D-09/D-24).
- Config leída de aura_config/system_settings con fallback (AURA-02, D-12).
- Registro auditable escrito sin inflar saldo (AURA-01, D-06/D-18).
- Script de backfill idempotente de códigos disponible a demanda, fuera del deploy (D-25/D-17).
  </success_criteria>

<output>
Crear `.planning/phases/157-n-cleo-transaccional-de-referidos/157-02-SUMMARY.md` al terminar.
</output>
</content>
