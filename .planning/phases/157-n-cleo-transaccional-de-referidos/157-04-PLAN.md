---
phase: 157-n-cleo-transaccional-de-referidos
plan: 04
type: execute
wave: 3
depends_on: ["157-01", "157-02"]
files_modified:
  - el-templo-api/src/modules/subscriptions/service.ts
  - el-templo-api/test/referrals/qualification.test.ts
  - el-templo-api/test/referrals/discount-charge.test.ts
  - el-templo-api/test/subscriptions/member-plans.test.ts
autonomous: true
requirements: [DESC-01, DESC-02, DESC-03, DESC-05, AURA-01]

must_haves:
  truths:
    - "El primer pago del referido (pricePaid>0) flippea su vínculo pending→qualified ANTES de computar el descuento (DESC-01, D-20/D-21)"
    - "Las 4 charge-paths (assignPlan, changePlanNow, changePlanAfterCurrent, renewSubscription) aplican el descuento simétrico vía un helper compartido (DESC-02/03)"
    - "El descuento se materializa en referralDiscountPercent/referralDiscountAmount (NUEVAS columnas, D-23) y reduce pricePaid sin usar auraService.spend"
    - "getPricingPreview muestra el mismo precio que se cobrará (preview parity, Pitfall 4)"
    - "Cada cargo con descuento escribe referral_credits + anotación AURA sourceType='referral' sin inflar balance (AURA-01)"
  artifacts:
    - path: "el-templo-api/src/modules/subscriptions/service.ts"
      provides: "computePriceWithReferralDiscount compartido + flip de cualificación en las 4 paths + preview parity"
    - path: "el-templo-api/test/referrals/qualification.test.ts"
      provides: "cobertura del flip pending→qualified al primer pago"
  key_links:
    - from: "subscriptions/service.ts (4 charge-paths + getPricingPreview)"
      to: "ReferralService.computeReferralDiscountPercent"
      via: "helper compartido computePriceWithReferralDiscount"
      pattern: "computeReferralDiscountPercent|referralDiscount"
    - from: "charge-path"
      to: "ReferralService.qualifyFirstPayment + recordReferralCredit"
      via: "flip antes del descuento + registro tras cobro"
      pattern: "qualifyFirstPayment|recordReferralCredit"
---

<objective>
El hook "de la plata" en `subscriptions/service.ts`: (1) al primer pago del referido con `pricePaid>0`, flippear su vínculo `pending→qualified` ANTES de computar el descuento (D-20/D-21); (2) un helper compartido `computePriceWithReferralDiscount` que las 4 charge-paths y `getPricingPreview` invocan para reducir `pricePaid` según los vínculos activos (DESC-02/03), materializando en las columnas NUEVAS `referralDiscount*` (D-23); (3) escribir `referral_credits` + anotación AURA por cargo (AURA-01). El descuento se compone independientemente sobre cualquier `auraSpend` existente.

Purpose: es donde el vínculo se convierte en plata. Sin esto, la atribución (plan 03) no rinde beneficio. La paridad de preview evita que el PoS muestre un precio distinto al cobrado.
Output: subscriptions/service.ts modificado + tests de cualificación, descuento-en-cobro y preview parity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/157-n-cleo-transaccional-de-referidos/157-CONTEXT.md
@.planning/phases/157-n-cleo-transaccional-de-referidos/157-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Verified 2026-07-10 contra subscriptions/service.ts. -->

auraSpend discount block (:1252-1274): reduce pricePaid con Math.floor(basePrice \* pct/100); pricePaid = basePrice - discountAmount; escribe auraDiscount/auraDiscountPercent (:1206-1207, :1389-1390). COPIAR la price-math, NO copiar auraService.spend().

Las 4 charge-paths convergen en recordAssignmentCharge (:387) con chargeBase = pricePaid y enforce amountReceived <= chargeBase (:450):

- assignPlan (:1058, aura block :1252)
- changePlanNow (:2906)
- changePlanAfterCurrent (:3332, aura block :3484)
- renewSubscription (:3652)

getPricingPreview (:4195): hoy solo computa descuento auraSpend (:4236-4244); mirror discountAmount/finalPrice shape.

ReferralService (plan 02): computeReferralDiscountPercent(userId), qualifyFirstPayment(payerUserId), recordReferralCredit(userId, subscriptionId, percent, amount). Instanciar con this.db/this.log.

Columnas destino (plan 01): subscriptions.referralDiscountPercent / referralDiscountAmount (NUEVAS, D-23 — NO auraDiscount\*).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helper compartido computePriceWithReferralDiscount + flip de cualificación</name>
  <files>el-templo-api/src/modules/subscriptions/service.ts, el-templo-api/test/referrals/qualification.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/subscriptions/service.ts (aura block :1252-1274, recordAssignmentCharge :387-450, deriveCoveredUntil :176)
    - el-templo-api/src/modules/referrals/service.ts (plan 02: computeReferralDiscountPercent, qualifyFirstPayment)
    - el-templo-api/src/db/schema/subscriptions.ts (columnas referralDiscount* del plan 01)
  </read_first>
  <behavior>
    - computePriceWithReferralDiscount(userId, basePrice): llama ReferralService.computeReferralDiscountPercent(userId); si pct>0 devuelve { percent, amount: Math.floor(basePrice*pct/100), pricePaid: basePrice-amount }; si 0 devuelve el basePrice sin descuento. Se compone sobre un precio ya reducido por auraSpend (recibe el precio corriente, no el de lista).
    - flip: qualifyFirstPayment(payerUserId) se invoca en la charge-path SOLO cuando pricePaid resuelto > 0 (D-20, mata el fantasma del mes gratis) y ANTES de computar el descuento del propio payer (D-21) — el payer recién cualificado no se descuenta a sí mismo en ese primer cargo, pero su referidor sí puede beneficiarse en su próximo cobro.
  </behavior>
  <action>
    Agregar un método privado `computePriceWithReferralDiscount(userId, basePrice)` en SubscriptionService que delega en ReferralService (instanciada con this.db/this.log). Copiar la price-math de :1252-1274 (`Math.floor`, `pricePaid = basePrice - discountAmount`) pero NO el `auraService.spend()`. Devolver también percent/amount para materializar en `referralDiscountPercent`/`referralDiscountAmount` (D-23, NO auraDiscount*). Definir el orden canónico dentro de una charge-path: (1) resolver pricePaid con auraSpend (código existente); (2) si pricePaid>0, `qualifyFirstPayment(payerUserId)` (D-20/D-21); (3) `computePriceWithReferralDiscount(payerUserId, pricePaidActual)` → nuevo pricePaid + columnas; (4) tras `recordAssignmentCharge`, `recordReferralCredit(payerUserId, subscriptionId, percent, amount)` si amount>0. Esta task solo introduce el helper + el flip y los cablea en assignPlan (:1058); las otras 3 paths van en task 2.
    Tests en `test/referrals/qualification.test.ts`: (a) referido con vínculo pending que hace su 1er pago con precio>0 → vínculo qualified + qualifiedAt; (b) pago con pricePaid===0 (mes gratis) → NO cualifica (D-20); (c) 2do pago no re-flippea ni rompe (idempotente).
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm test referrals/qualification 2>&1 | tail -15</automated>
  </verify>
  <done>Helper compartido creado + flip cableado en assignPlan con gate pricePaid>0 antes del descuento; test qualification verde.</done>
  <acceptance_criteria>
    - qualification.test.ts asserta flip pending→qualified con qualifiedAt solo cuando pricePaid>0
    - qualification.test.ts asserta que pricePaid===0 NO cualifica (D-20)
    - `grep -c "auraService.spend" el-templo-api/src/modules/subscriptions/service.ts` NO aumenta respecto del baseline (no se agregó spend nuevo para referidos)
    - El helper escribe en referralDiscountPercent/referralDiscountAmount, no en auraDiscount* — verificación source
    - `cd el-templo-api && pnpm build` exit code 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cablear el descuento + registro en las 4 charge-paths</name>
  <files>el-templo-api/src/modules/subscriptions/service.ts, el-templo-api/test/referrals/discount-charge.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/subscriptions/service.ts (assignPlan :1058, changePlanNow :2906, changePlanAfterCurrent :3332/:3484, renewSubscription :3652, recordAssignmentCharge :387)
    - el-templo-api/src/modules/referrals/service.ts (recordReferralCredit del plan 02)
  </read_first>
  <behavior>
    - Cada una de las 4 charge-paths, tras resolver su pricePaid (incl. auraSpend si aplica): aplica el flip (si pricePaid>0) + computePriceWithReferralDiscount → nuevo pricePaid + columnas referralDiscount*, y tras el cargo llama recordReferralCredit si amount>0.
    - El descuento se compone sobre auraSpend (no lo pisa): si ambos aplican, el precio final refleja los dos.
    - Simetría: al cobrarle a cualquiera de las dos partes de un vínculo qualified con contraparte activa, se aplica el % (DESC-02); si la contraparte está inactiva ese ciclo, 0 (DESC-03).
  </behavior>
  <action>
    Replicar el bloque orquestado de la task 1 en `changePlanNow` (:2906), `changePlanAfterCurrent` (:3332, respetando su aura block :3484) y `renewSubscription` (:3652). Cada path escribe `referralDiscountPercent`/`referralDiscountAmount` en su INSERT/UPDATE de subscription y respeta `amountReceived <= chargeBase` (:450) con el chargeBase ya descontado. Llamar `recordReferralCredit(payerUserId, subscriptionId, percent, amount)` tras `recordAssignmentCharge` cuando amount>0 (AURA-01). Todo server-computed, sin input del cliente (DESC-05). Sin `any`, logger vía this.log.
    Tests en `test/referrals/discount-charge.test.ts`: (a) renewSubscription de un socio con 1 vínculo qualified contraparte activa → pricePaid reducido 10% + fila referral_credits + fila aura_transactions amount=0; (b) contraparte inactiva → sin descuento ese ciclo (DESC-03); (c) 4 vínculos → tope 40%; (d) simetría: descuento aplica tanto al referrer como al referido; (e) composición con auraSpend (ambos descuentos reflejados).
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm test referrals/discount-charge 2>&1 | tail -20</automated>
  </verify>
  <done>Las 4 charge-paths aplican descuento simétrico condicional topeado + registro auditable; test discount-charge verde.</done>
  <acceptance_criteria>
    - discount-charge.test.ts asserta pricePaid reducido y fila referral_credits tras renewSubscription
    - discount-charge.test.ts asserta DESC-03 (contraparte inactiva → 0 ese ciclo) y simetría (aplica a ambos roles)
    - `grep -c "computePriceWithReferralDiscount\|referralDiscountPercent" el-templo-api/src/modules/subscriptions/service.ts` >= 4 (presente en las 4 paths + helper)
    - `grep -c "recordReferralCredit" el-templo-api/src/modules/subscriptions/service.ts` >= 1
    - `cd el-templo-api && pnpm build` exit code 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preview parity en getPricingPreview</name>
  <files>el-templo-api/src/modules/subscriptions/service.ts, el-templo-api/test/subscriptions/member-plans.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/subscriptions/service.ts (getPricingPreview :4195, descuento auraSpend :4236-4244)
    - el-templo-api/test/subscriptions/member-plans.test.ts (test de preview existente a extender)
  </read_first>
  <behavior>
    - getPricingPreview(userId, ...) devuelve el precio con el descuento de referido ya aplicado (mismo valor que la charge-path cobraría), mirroreando el shape discountAmount/finalPrice de :4231-4244. NO flippea cualificación (es preview, no cobra) ni escribe referral_credits.
  </behavior>
  <action>
    En `getPricingPreview` (:4195), tras el descuento auraSpend existente (:4236-4244), llamar `computeReferralDiscountPercent(userId)` (solo lectura, NO qualifyFirstPayment ni recordReferralCredit) y reducir el finalPrice mirroreando el shape existente, exponiendo el desglose (percent/amount de referido) para que el PoS muestre el precio real. La preview usa el precio ya reducido por auraSpend como base (misma composición que la charge-path, Pitfall 4).
    Extender `test/subscriptions/member-plans.test.ts`: la preview de un socio con vínculo qualified contraparte activa devuelve el mismo finalPrice que el pricePaid del cobro real; sin vínculos, precio de lista.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm test subscriptions/member-plans 2>&1 | tail -15</automated>
  </verify>
  <done>getPricingPreview refleja el descuento de referido (paridad con el cobro); test verde.</done>
  <acceptance_criteria>
    - member-plans.test.ts asserta preview.finalPrice === pricePaid del cobro real para el mismo socio/vínculo
    - getPricingPreview NO llama qualifyFirstPayment ni recordReferralCredit — verificación source (es read-only)
    - `cd el-templo-api && pnpm build` exit code 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                               |
| ------------------------------------ | ------------------------------------------------------------------------- |
| charge-path → subscription.pricePaid | descuento server-computed; ningún input del cliente lo fija               |
| preview vs cobro                     | divergencia = el PoS muestra un precio que no se cobra (fraude/confusión) |

## STRIDE Threat Register

| Threat ID | Category    | Component                     | Disposition | Mitigation Plan                                               |
| --------- | ----------- | ----------------------------- | ----------- | ------------------------------------------------------------- |
| T-157-13  | Tampering   | descuento inflado por cliente | mitigate    | 100% server-computed, sin input.\* (DESC-05); tope por config |
| T-157-14  | Elevation   | cualificación con mes gratis  | mitigate    | flip solo si pricePaid>0 (D-20, mata el fantasma)             |
| T-157-15  | Information | preview ≠ cobro               | mitigate    | mismo helper en getPricingPreview y las 4 paths (Pitfall 4)   |
| T-157-16  | Elevation   | anotación AURA gastable       | mitigate    | recordReferralCredit escribe amount=0 (heredado del plan 02)  |

</threat_model>

<verification>
- `pnpm build` verde.
- Tests referrals/qualification, referrals/discount-charge, subscriptions/member-plans verdes en CI.
- Helper compartido presente en las 4 paths + preview; sin auraService.spend nuevo.
</verification>

<success_criteria>

- Primer pago (pricePaid>0) cualifica el vínculo antes del descuento (DESC-01, D-20/D-21).
- Descuento simétrico condicional topeado aplicado en las 4 paths (DESC-02/03).
- Materializado en columnas nuevas + registro auditable sin inflar saldo (D-23, AURA-01).
- Preview parity garantizada (Pitfall 4).
- No-discrecional: todo server-side (DESC-05).
  </success_criteria>

<output>
Crear `.planning/phases/157-n-cleo-transaccional-de-referidos/157-04-SUMMARY.md` al terminar.
</output>
