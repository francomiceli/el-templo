---
phase: 157-n-cleo-transaccional-de-referidos
plan: 05
type: execute
wave: 4
depends_on: ["157-03"]
files_modified:
  - el-templo-app/src/pages/RegisterPage.vue
  - el-templo-admin/src/components/MemberFormDialog.vue
autonomous: false
requirements: [REF-02, REF-03]

must_haves:
  truths:
    - "RegisterPage captura ?ref=CODE, lo pasa a authStore.register({ ref }) y muestra un badge de confirmación optimista (REF-02, UI-SPEC S1)"
    - "El badge reusa .promo-badge 1:1 (icono group_add, eyebrow 'CÓDIGO DE REFERIDO', subtext 'Te invitó un miembro del Templo'); nunca bloquea el registro"
    - "MemberFormDialog agrega un q-select debounced '¿Quién lo trajo? (opcional)' en el paso 2 del alta (create-only), con búsqueda de socios (REF-03, UI-SPEC S2)"
    - "El campo es opcional y NUNCA bloquea el alta; el id elegido se envía como referredBy al server"
  artifacts:
    - path: "el-templo-app/src/pages/RegisterPage.vue"
      provides: "captura de ?ref + badge de referido"
    - path: "el-templo-admin/src/components/MemberFormDialog.vue"
      provides: "picker '¿Quién lo trajo?' search-as-you-type"
  key_links:
    - from: "RegisterPage.vue refCode computed"
      to: "authStore.register({ ref })"
      via: "payload al POST /register"
      pattern: "route.query.ref|ref:"
    - from: "MemberFormDialog.vue referrer q-select"
      to: "membersApi.searchMembers + submit referredBy"
      via: "onReferrerSearch debounced"
      pattern: "searchMembers|referredBy"
---

<objective>
Las dos superficies UI del feature (UI-SPEC S1/S2), deliberadamente pequeñas y ADITIVAS: (1) RegisterPage lee `?ref=CODE`, lo manda en el body de registro y muestra un badge de confirmación optimista reusando `.promo-badge` 1:1; (2) MemberFormDialog agrega un `q-select` debounced "¿Quién lo trajo? (opcional)" en el paso 2 del alta (create-only) que busca socios y envía el id elegido como `referredBy`. Ambas son opcionales y NUNCA bloquean su flujo.

Purpose: cerrar el canal de datos de cara al usuario (self-service link + alta asistida). Sin UI, la atribución del plan 03 no tiene entrada real. Esta fase NO introduce ningún patrón visual nuevo (la pantalla "Mis referidos" y el panel admin son fase 158).
Output: dos componentes modificados + verificación visual humana.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/157-n-cleo-transaccional-de-referidos/157-UI-SPEC.md
@.planning/phases/157-n-cleo-transaccional-de-referidos/157-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Verified 2026-07-10. -->

RegisterPage.vue:213-216: const promoCode = computed(() => { const code = route.query.promo; return typeof code === 'string' ? code : null }). Body wiring en :249-259: authStore.register({ ..., promoCode: promoCode.value ?? undefined }). Badge .promo-badge markup :33-39, estilos :958-993 (padding 10px 16px, margin-bottom 20px, borde/gradiente amber #d4a843, icono material). Copy: eyebrow "CÓDIGO DE REFERIDO", subtext "Te invitó un miembro del Templo", success toast GENÉRICO "Cuenta creada exitosamente".

MemberFormDialog.vue: q-selects estáticos existentes en :17, :79 (NO usar como analog — fijos). onSubmit catch con $q.notify negative + extractError en :1104-1111.

SlotDetailDialog.vue:366-399 (q-select search) + :898-924/:1009 (onMemberSearch → membersApi.searchMembers(val, 10) → map { id, displayLabel: "Nombre Apellido (DNI)" }). useMembersApi.searchMembers(search, limit=10) en el-templo-admin/src/composables/useMembersApi.ts:112.

Backend contrato (plan 03): POST /register acepta body.ref; POST /admin/members acepta body.referredBy (el id del referrer). Ambos validan/resuelven server-side.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: RegisterPage captura ?ref + badge de confirmación</name>
  <files>el-templo-app/src/pages/RegisterPage.vue</files>
  <read_first>
    - el-templo-app/src/pages/RegisterPage.vue (promoCode computed :213-216, body wiring :249-259, .promo-badge markup :33-39 y estilos :958-993)
    - .planning/phases/157-n-cleo-transaccional-de-referidos/157-UI-SPEC.md (sección S1: estados, copy, hard rule "nunca bloquea")
  </read_first>
  <action>
    Clonar el computed `promoCode` como `refCode = computed(() => typeof route.query.ref === 'string' ? route.query.ref : null)`. Agregar `ref: refCode.value ?? undefined` al payload de `authStore.register({...})` (:249-259) — separado del promoCode existente (canales distintos, pueden coexistir). Renderizar un badge condicional (visible solo con `?ref` presente) en el mismo slot que `.promo-badge`, reusando su markup+estilos 1:1: icono `group_add`, eyebrow "CÓDIGO DE REFERIDO", línea prominente = `{{ refCode }}`, subtext "Te invitó un miembro del Templo". Es OPTIMISTA (el cliente no valida el code). Si `?promo` y `?ref` coexisten, ambos badges se apilan (promo arriba, referral abajo) — dos bloques condicionales, sin lógica de layout nueva. El success toast permanece GENÉRICO "Cuenta creada exitosamente" (NO prometer descuento — D-01). Sin campo requerido ni path de error del referido. Usar createLogger si hace falta loggear, nunca console.
  </action>
  <verify>
    <automated>cd el-templo-app && pnpm exec quasar build 2>&1 | tail -8</automated>
  </verify>
  <done>RegisterPage lee ?ref, lo envía en register(), muestra el badge reusando .promo-badge; build verde.</done>
  <acceptance_criteria>
    - `grep -c "route.query.ref" el-templo-app/src/pages/RegisterPage.vue` >= 1
    - `grep -c "ref:" el-templo-app/src/pages/RegisterPage.vue` >= 1 (en el payload de register)
    - `grep -c "group_add\|CÓDIGO DE REFERIDO\|Te invitó un miembro del Templo" el-templo-app/src/pages/RegisterPage.vue` >= 2
    - `grep -c "console\." el-templo-app/src/pages/RegisterPage.vue` no aumenta respecto del baseline
    - `cd el-templo-app && pnpm exec quasar build` exit code 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: MemberFormDialog "¿Quién lo trajo?" search q-select</name>
  <files>el-templo-admin/src/components/MemberFormDialog.vue</files>
  <read_first>
    - el-templo-admin/src/components/MemberFormDialog.vue (paso 2 "Datos Personales", bloque "Contacto de Emergencia", onSubmit catch :1104-1111, q-selects estáticos :17/:79 — NO usar como analog)
    - el-templo-admin/src/components/scheduling/SlotDetailDialog.vue (q-select search :366-399, onMemberSearch :898-924/:1009)
    - el-templo-admin/src/composables/useMembersApi.ts (searchMembers :112)
    - .planning/phases/157-n-cleo-transaccional-de-referidos/157-UI-SPEC.md (sección S2: placement, estados, copy, hard rule opcional)
  </read_first>
  <action>
    Agregar en el paso 2 "Datos Personales" del flujo CREATE (NO en EDIT — D-14, la atribución se fija una vez), como fila full-width DESPUÉS del bloque "Contacto de Emergencia", un `q-select` `dense outlined clearable use-input input-debounce="300"` con `v-model="referrer"`, `:options="referrerSearchResults"`, `option-value="id" option-label="displayLabel"`, label "¿Quién lo trajo? (opcional)", hint "Buscá por nombre o DNI al socio que lo refirió", `@filter="onReferrerSearch"`, `:loading="searchingReferrer"`, slot `no-option` "No se encontró ningún socio". `onReferrerSearch` mirrorea `onMemberSearch`: `<2` chars → limpia resultados; si no `membersApi.searchMembers(val, 10)` → map a `{ id, displayLabel: "Nombre Apellido (DNI)" }`. Sin prepend icon (match los campos planos alrededor). Al submit, enviar `referrer?.id` como `referredBy` en el body del create (el server valida — nunca confía el body). El error de rechazo server-side (auto-referido / ya tiene referidor / no encontrado) se surface por el toast negativo existente de onSubmit (extractError :1104-1111), sin UI de error inline nueva. NINGUNA regla de validación que pueda bloquear el alta. createLogger, nunca console.
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm exec quasar build 2>&1 | tail -8</automated>
  </verify>
  <done>MemberFormDialog tiene el picker debounced en create-only, opcional, que envía referredBy; build verde.</done>
  <acceptance_criteria>
    - `grep -c "onReferrerSearch\|¿Quién lo trajo" el-templo-admin/src/components/MemberFormDialog.vue` >= 2
    - `grep -c "searchMembers" el-templo-admin/src/components/MemberFormDialog.vue` >= 1
    - `grep -c "referredBy" el-templo-admin/src/components/MemberFormDialog.vue` >= 1 (enviado en el submit)
    - El picker está en el flujo CREATE, no en EDIT — verificación source
    - `grep -c "console\." el-templo-admin/src/components/MemberFormDialog.vue` no aumenta respecto del baseline
    - `cd el-templo-admin && pnpm exec quasar build` exit code 0
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificación visual de ambas superficies</name>
  <files>el-templo-app/src/pages/RegisterPage.vue, el-templo-admin/src/components/MemberFormDialog.vue</files>
  <action>Pausa para verificación visual humana — NO modifica código. El operador valida las dos superficies (RegisterPage badge de referido y MemberFormDialog picker '¿Quién lo trajo?') siguiendo how-to-verify; confirma que ninguna atribución bloquea su flujo.</action>
  <what-built>
    RegisterPage con badge de referido optimista (reusa .promo-badge, amber, icono group_add) y MemberFormDialog con el picker "¿Quién lo trajo? (opcional)" search-as-you-type en el paso 2 del alta.
  </what-built>
  <how-to-verify>
    1. App: abrir el registro con `?ref=FRAN-A3B2` en la URL (ej. `/registro?ref=FRAN-A3B2`). Verificar que aparece el badge amber con eyebrow "CÓDIGO DE REFERIDO", el código, y subtext "Te invitó un miembro del Templo". Sin `?ref` el badge NO aparece. Completar un registro con `?ref` → toast GENÉRICO "Cuenta creada exitosamente" (no promete descuento).
    2. App: probar con `?ref` y `?promo` juntos → ambos badges se apilan sin romper el layout.
    3. Admin: abrir el alta de un alumno (create), ir al paso 2 "Datos Personales", debajo de "Contacto de Emergencia" verificar el q-select "¿Quién lo trajo? (opcional)"; tipear 2+ letras de un socio → aparecen resultados "Nombre Apellido (DNI)"; dejar vacío → el alta procede igual (no bloquea).
    4. Admin: en EDIT de un miembro existente, el campo NO debe aparecer.
  </how-to-verify>
  <verify><human-check>Ambas superficies validadas visualmente por el operador; sin patrón visual nuevo; picker en create-only.</human-check></verify>
  <done>El usuario respondió "approved" (o se aplicaron los ajustes visuales solicitados).</done>
  <resume-signal>Escribí "approved" o describí los ajustes visuales necesarios.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                     |
| ------------------------------ | --------------------------------------------------------------- |
| URL `?ref` → cliente           | el badge es optimista; el cliente no valida; el server resuelve |
| admin picker → body.referredBy | el id elegido viaja en el body; el server valida (nunca confía) |

## STRIDE Threat Register

| Threat ID | Category  | Component                           | Disposition | Mitigation Plan                                                                            |
| --------- | --------- | ----------------------------------- | ----------- | ------------------------------------------------------------------------------------------ |
| T-157-17  | Spoofing  | badge muestra code arbitrario       | accept      | badge es puramente informativo/optimista; la atribución real la decide el server (plan 03) |
| T-157-18  | Denial    | atribución bloquea signup/alta      | mitigate    | campo opcional sin regla de validación; badge no agrega path de error (UI-SPEC hard rules) |
| T-157-19  | Tampering | referredBy manipulado desde cliente | mitigate    | el server valida el id (plan 03, Security V4); la UI solo lo propone                       |

</threat_model>

<verification>
- `pnpm exec quasar build` verde en app y admin.
- Badge reusa .promo-badge 1:1 (sin patrón visual nuevo); picker en create-only.
- Checkpoint humano de verificación visual aprobado.
</verification>

<success_criteria>

- ?ref capturado y enviado; badge optimista mostrado (REF-02 UI).
- Picker "¿Quién lo trajo?" opcional en create-only que envía referredBy (REF-03 UI).
- Ningún flujo bloqueado por la atribución.
  </success_criteria>

<output>
Crear `.planning/phases/157-n-cleo-transaccional-de-referidos/157-05-SUMMARY.md` al terminar.
</output>
