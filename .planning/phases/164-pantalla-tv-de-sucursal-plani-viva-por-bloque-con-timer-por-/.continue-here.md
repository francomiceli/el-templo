---
context: phase
phase: 164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-07-25T20:40:00.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: worktree-only — La fase 164 vive en `/home/franco/projects/et-164-tv` (rama `feat/164-tv-sucursal`, base `origin/master`). El checkout principal `/home/franco/projects/el-templo` está en `fix/referral-preview-y-refresh-ficha`, 135 commits atrás, con WIP sin commitear de CR-CAJA de OTRA sesión. El ejecutor de 164-01 abortó al intentar arrancar ahí (gate del plan). Mitigación estructural: `cd /home/franco/projects/et-164-tv` como primera acción de todo agente; jamás editar/commitear/stashear en el checkout principal.
- [ ] CONSTRAINT: no-push-sin-ok — El push a staging es gate humano (regla dura del proyecto). Ya se pushearon 4 tandas CON OK explícito de Franco; cada tanda nueva necesita el suyo.
- [ ] CONSTRAINT: merge-a-staging-no-es-ff — `staging` tiene 31+ commits propios (Wellhub, ABM de cajas, migs 0186-0188): `git push origin feat/164-tv-sucursal:staging` es RECHAZADO. Hay que mergear en un worktree descartable desde `origin/staging` y pushear esa rama. Los conflictos son siempre los mismos dos y son de registro adyacente (`el-templo-api/src/app.ts` y `src/db/schema/index.ts`: import de Wellhub vs import de TV en la misma línea) — se conservan LOS DOS lados.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern                         | Description                                                                                                       | Severity | Prevention Mechanism                                                                                  |
| ------------------------------- | ----------------------------------------------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------- |
| ejecutar-en-checkout-compartido | Task 1 de 164-01 falló: rama atrasada + WIP ajeno; `git checkout -b` habría pisado 6 archivos                     | blocking | cwd = worktree `/home/franco/projects/et-164-tv` en toda acción                                       |
| verify-paths-del-plan           | Los `<verify>` de TODOS los planes apuntan a `/home/franco/projects/el-templo` — corridos ahí dan falso resultado | advisory | reescribir el path al worktree antes de correr cualquier verify                                       |
| vue-tsc-ausente                 | `vue-tsc` no está en el-templo-admin; `npx` lo bajaría de la red (prohibido C-08)                                 | advisory | baseline-diff de `tsc` + extracción del script block + `quasar build` (documentado en 164-09-SUMMARY) |
| tests-api-en-paralelo           | Corridas paralelas de varios archivos de test rompen el provisioning (`Table ... doesn't exist`)                  | advisory | `MAX_TEST_WORKERS=1` al correr más de un archivo                                                      |
| roadmap-doble-164               | ROADMAP.md tiene DOS "Phase 164" (TV y legacy v5.8); el handler pisó la equivocada una vez                        | advisory | actualizar a mano la entrada del TV (lista de checkboxes)                                             |
| grep-gates-vs-prosa             | 3 veces un comentario/docblock que nombraba una feature prohibida disparó su propio gate de grep                  | advisory | reformular menciones en prosa antes de correr los gates                                               |

<current_state>
12/13 planes completos con SUMMARY (waves 1-6 enteras). Plan 164-13: Task 1 hecho (`1a9745d1`, `deploy/TV-KIOSK-RUNBOOK.md`); Task 2 (checkpoint human-verify) **se hizo parcialmente el 2026-07-25**: la fase está EN STAGING y Franco la probó en un televisor real — el kiosco conecta, lo maneja desde el celular y el circuito funciona.

La verificación en hardware encontró 3 cosas, las 3 arregladas y desplegadas:

1. **El kiosco no conectaba** ("Conectando…" eterno): no heredaba `VITE_API_URL` porque no pasa por Vite, y el vhost del admin no proxea `/api` → 405. Fix `e491253c`: `build-tv.mjs` hornea `window.__TV_API_BASE__` y lo suma al hash de version.
2. **Hueco vacío donde va el video**: `lastVideoUrl` arrancaba en `null`, igual que "sin video", y la guardia de `paintVideo` cortaba antes de mostrar el cartel. Fix `4c407f3d`.
3. **El timer saltaba de 00:00 a 00:03** al iniciar (el TV se entera en su poll de 2,5 s). Fix `2e341c5a`: arranque programado 3 s adelante + aviso en el control del profe. **Esto revierte D-16 — ver decisions_made.**

Rama `feat/164-tv-sucursal`: 70 commits sobre `origin/master`, working tree limpio, SIN push a origin. Kiosco desplegado en staging: `version.txt` = `811030c8`. `tsc --noEmit` del API limpio.
</current_state>

<completed_work>

Completed Tasks:

- 164-01: schema TV + mig 0189 — Done
- 164-02: contrato + timer puro (43/43) — Done
- 164-03: pairing RFC 8628 + device auth (25/25) — Done
- 164-04: build del kiosco autocontenido (ES2015 gate) — Done
- 164-05: roster + resolveClassDay + TvService (28/28 + regresión) — Done
- 164-06: CSS Chromium 53 + scaleTv + ?diag=1 — Done
- 164-07: timer.ts port 1:1 + ?selftest=1 (34/34) — Done
- 164-08: GET /api/tv/state + client-log (19/19; módulo 110/110) — Done
- 164-09: useTvApi + TvDevicesPage + ruta tv/devices — Done
- 164-10: control del profe: context/state/end-class (25/25; módulo 135/135) — Done
- 164-11: kiosco completo (64 checks sobre el bundle real) — Done
- 164-12: TvControlPage + smoke E2E local 23/23 — Done
- 164-13 Task 1: TV-KIOSK-RUNBOOK.md — Done
- 164-13 Task 2: checkpoint humano — In progress, esperando push OK + reporte del TV
  </completed_work>

<remaining_work>

- 164-13 Task 2 (lo que queda): re-probar en el TV con el build `811030c8` — (a) INICIAR arranca en **00:00** sin saltar a 00:03, y el celular muestra "Arranca en 3…2…1"; (b) los videos se reproducen (a staging se le copiaron 144 referencias de video de prod: 6% → 32% de las prescripciones); (c) donde no hay video aparece "VIDEO PRÓXIMAMENTE" y no un hueco. El TV se auto-recarga solo (chequea `version.txt` cada minuto).
- Con "aprobado": SUMMARY de 164-13 + STATE/ROADMAP.
- Cierre de fase: `/gsd:code-review 164` (gate), gsd-verifier → VERIFICATION.md, update_roadmap, offer_next.
- Post-fase (fuera del alcance GSD): decidir merge de `feat/164-tv-sucursal` (staging-first estricto; nunca a master directo).
  </remaining_work>

<decisions_made>

- **DEVIATION de D-16 (decidida por Franco el 2026-07-25):** el timer ya no arranca al instante. `TIMER_START_LEAD_MS = 3000` en `tv/service.ts` programa `timerStartedAt` 3 s adelante, así el televisor recibe el estado ANTES de que empiece y el conteo arranca en cero (y dos TVs de la misma sala arrancan sincronizados). La pantalla sigue sin cuenta previa — el espíritu de D-16 — y el aviso vive en el control del profe: texto fijo bajo el timer + cuenta 3-2-1 al apretar, medida desde el tap y no contra el reloj del celular. El test "D-16: iniciar arranca al instante" fue reescrito en `tv-control.test.ts`. **El verifier tiene que leer esto antes de marcar incumplimiento de D-16.**
- El kiosco recibe la URL del API horneada en el build, NO la deriva del host que lo sirve: el vhost del admin sirve estáticos y no proxea `/api` (nunca lo hizo — el SPA siempre llamó cross-origin con `VITE_API_URL`).
- Fase en worktree aislado (ver BLOCKING CONSTRAINTS) — los 8 commits docs(164) se cherry-pickearon desde `fix/referral-preview-y-refresh-ficha`; UI-SPEC v8 + mockup estaban untracked y se commitearon.
- `formatDigits` redondea con ceil (fiel al mockup v8); contracara: count-up arranca en 00:01 — validar en UAT, cambiarlo es cambio de firma en 164-11.
- Escritura de control con blockRole/level inexistente → descarte con warn (no rechaza ni clampea): no puede mover al profe de bloque ni matar el timer.
- `tv_class_state.class_date` pasó a `mode: "string"` (fix TZ sobre 164-01; solo driver, sin migración).
- Auth del kiosco: `Authorization: Device <token>` (NO Bearer); el pairing devuelve `deviceToken` (no `token`).
- `GET /api/tv/me` agregado en 164-03 para ejercitar el guard (threat flag en su SUMMARY); T-164-15 (rate limit del /pair/status) sigue aceptado — mitigarlo requiere dependencia nueva (gate humano).
  </decisions_made>

<blockers>
- Verificación D-20 en hardware real: solo una persona en sede puede hacerla. La primera pasada ya se hizo (2026-07-25) y salió bien; falta la re-prueba del arranque del timer y de los videos con el build nuevo.
</blockers>

<pendientes_no_bloqueantes>

- La rama `feat/164-tv-sucursal` vive SOLO en el disco de Franco (70 commits). A origin fue el merge a `staging`, no la rama. Decidir si se pushea como respaldo.
- Mirar CI #1303 (`ed02444d`): si fue verde, el test de Wellhub que falló en #1304 era flaky y ya está arreglado (`f8d3c3a4`, un `vi.waitFor` que faltaba); si estaba rojo, venía roto de antes y hay que buscar otra causa. `gh` no está instalado local.
  </pendientes_no_bloqueantes>

## Required Reading (in order)

1. `.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-13-PLAN.md` — el checkpoint pendiente y sus criterios
2. `deploy/TV-KIOSK-RUNBOOK.md` — guion de la verificación en el TV
3. SUMMARYs 164-11 y 164-12 — contratos del kiosco y del control + smoke E2E
4. `.planning/HANDOFF.json` — estado estructurado

## Infrastructure State

- Worktree `/home/franco/projects/et-164-tv`: deps instaladas (raíz, api, admin), `.env`/`.env.development` copiados al api (gitignoreados). Ningún server corriendo.
- DB local: migrada hasta 0189; el smoke de 164-12 restauró tv_devices/tv_pairings/tv_class_state a 0 filas.
- Checkout principal: intacto, con su WIP de CR-CAJA — no tocar.

<context>
La ejecución fue secuencial (use_worktrees=false de GSD; un solo escritor en el worktree de fase). El agente ejecutor del checkpoint era de la sesión anterior — en una sesión nueva NO se le puede mandar mensaje: re-correr `/gsd:execute-phase 164` detecta los 12 SUMMARYs y retoma 164-13 con agente de continuación, pasándole la respuesta del usuario ("aprobado" o el reporte de fallas con la salida de ?diag=1).
</context>

<next_action>
Start with: pedirle a Franco el reporte de la re-prueba en el TV (arranque del timer sin salto + videos). Con eso, `/gsd:execute-phase 164` DESDE EL WORKTREE para cerrar 164-13 y correr la verificación de fase — pasándole la deviation de D-16 al verifier.
</next_action>
