---
phase: 164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-
plan: 05
type: execute
wave: 3
depends_on: ["164-03"]
files_modified:
  - el-templo-api/src/modules/shared/week-dates.ts
  - el-templo-api/src/modules/sessions/routes.ts
  - el-templo-api/src/modules/tv/roster.ts
  - el-templo-api/src/modules/tv/class-day.ts
  - el-templo-api/src/modules/tv/service.ts
  - el-templo-api/test/tv/tv-roster.test.ts
  - el-templo-api/test/tv/tv-class-day.test.ts
autonomous: true
requirements: [D-06, D-07, D-08, D-09, D-14, D-15, D-23]
must_haves:
  truths:
    - "El TV resuelve SIEMPRE la plani regular del dia de su sede, con la fecha en la TZ de esa sede (D-14)"
    - "Los sabados el roster es ROM (INITIUM + ROM_LOWER/CORE/UPPER) y solo hay dos niveles, rotulados BASICO/AVANZADO (D-23, Pitfall 2)"
    - "El estado de clase de un dia anterior se ignora al leer: el TV amanece en reposo sin cron (D-07, Pattern 4)"
    - "Sin sesion aprobada el payload es screen=idle SIN ningun campo de error (D-09)"
    - "El bloque se identifica por ROL canonico, nunca por indice: cambiar de nivel no cambia de bloque (D-15, Pitfall 1)"
    - "El ejercicio se clampa contra la lista del (rol, nivel) vigente"
  artifacts:
    - path: "el-templo-api/src/modules/tv/roster.ts"
      provides: "Orden canonico de roles regular/ROM, INITIUM determinista, findBlock con alias ATHLOS"
      exports: ["buildRoster", "findBlock", "REGULAR_ROLES", "ROM_ROLES"]
    - path: "el-templo-api/src/modules/tv/class-day.ts"
      provides: "resolveClassDay(branch) -> sesiones aprobadas del dia + modo + niveles"
      exports: ["resolveClassDay"]
    - path: "el-templo-api/src/modules/tv/service.ts"
      provides: "TvService.buildPollPayload(device) y helpers de estado"
      exports: ["TvService"]
    - path: "el-templo-api/src/modules/shared/week-dates.ts"
      provides: "WEEK_ONE_MONDAY / dateToWeekNumber / dateToDayName compartidos"
      exports: ["dateToWeekNumber", "dateToDayName"]
  key_links:
    - from: "el-templo-api/src/modules/tv/class-day.ts"
      to: "schema.dayModes"
      via: "deteccion de dia ROM por dayOfWeek"
      pattern: "dayModes"
    - from: "el-templo-api/src/modules/tv/service.ts"
      to: "todayInTz(branch.timezone)"
      via: "expire-on-read del estado de clase"
      pattern: "todayInTz"
    - from: "el-templo-api/src/modules/tv/service.ts"
      to: "assembleVideoUrl"
      via: "URL publica del video del ejercicio"
      pattern: "assembleVideoUrl"
---

<objective>
El cerebro del TV del lado del servidor: que sesion es "la de hoy" para esta sede, en que orden van los bloques, que niveles existen ese dia, y como se arma el payload completo del poll.

Purpose: toda la logica derivable tiene que vivir en el API porque es el unico lugar del repo con tests automaticos. Si el TV calculara el dia, el orden de bloques o el modo ROM, quedaria fuera de cobertura y ademas dependeria del reloj y de la ICU del televisor.
Output: `roster.ts`, `class-day.ts` y `service.ts` con sus tests, mas la extraccion DRY de `dateToWeekNumber`/`dateToDayName` a `shared/week-dates.ts`.

Decision explicita (riesgo #2 de PATTERNS.md): el port de la logica canonica de block_role/INITIUM desde `el-templo-admin/src/utils/pdf/session-data-transformer.ts` a `roster.ts` ACEPTA la duplicacion cross-app — el codigo del PDF corre en el browser del admin y no puede importar modulos del server. Consecuencia documentada: cualquier cambio futuro al orden canonico en `session-data-transformer.ts` (INITIUM_SOURCE_ORDER, alias EPIKOS/ATHLOS) REQUIERE el cambio espejo en `el-templo-api/src/modules/tv/roster.ts`; ambos archivos deben llevar un comentario cruzado apuntando al otro.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-CONTEXT.md
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-RESEARCH.md
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-PATTERNS.md
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-UI-SPEC.md
@CLAUDE.md

<interfaces>
Firmas que este plan publica (el plan 164-08 monta las rutas encima y el 164-10 reusa el clamp):

```
resolveClassDay(db, branch: { id: number; timezone: string }, now?: Date): Promise<ClassDay>
  ClassDay = {
    date: string;              // "YYYY-MM-DD" en la TZ de la sede
    week: number;
    dayName: string;           // "lunes".."sabado"
    mode: "regular" | "rom";
    approved: boolean;         // false => D-09 reposo sin mensaje
    levels: string[];          // niveles con sesion aprobada ese dia, en orden canonico
    sessions: DaySessionDetail[];   // sesiones + bloques + prescripciones (batch, sin N+1)
  }

buildRoster(classDay): TvBlockSummary[]     // orden canonico por ROL, con title y shared
findBlock(blocks, role): SessionBlock | undefined   // EPIKOS matchea tambien ATHLOS

TvService.buildPollPayload(device: { id: number; branchId: number }): Promise<TvPollResponse>
TvService.readState(branchId, classDate): Promise<TvClassState | null>   // expire-on-read
TvService.clampState(state, classDay): TvControlState                    // rol y ejercicio validos
```

Constantes canonicas (portadas de `el-templo-admin/src/utils/pdf/session-data-transformer.ts`):
REGULAR_ROLES = ["INITIUM", "NUCLEUS", "DEUTEROS_1", "DEUTEROS_2", "EPIKOS"]
ROM_ROLES = ["INITIUM", "ROM_LOWER", "ROM_CORE", "ROM_UPPER"]
INITIUM_SOURCE_ORDER = ["alfa", "delta", "sigma", "kairos"]
EPIKOS matchea tambien el rol "ATHLOS"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: shared/week-dates.ts (extraccion DRY) + roster.ts + test unitario</name>
  <files>el-templo-api/src/modules/shared/week-dates.ts, el-templo-api/src/modules/sessions/routes.ts, el-templo-api/src/modules/tv/roster.ts, el-templo-api/test/tv/tv-roster.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/sessions/routes.ts lineas 60-125 (`dateToDayName`, `WEEK_ONE_MONDAY`, `dateToWeekNumber`, `buildDayIdCandidates`)
    - el-templo-admin/src/utils/pdf/session-data-transformer.ts lineas 19-40 y 284-291 (orden canonico, `findInitiumBlock` con su comentario, `findBlock` con el alias EPIKOS/ATHLOS)
    - el-templo-api/src/modules/admin/format-params.ts (`formatParamsLabel`, para el titulo `ROL · FORMATO`)
    - el-templo-api/test/rbac-sets.test.ts (forma de un test unitario puro)
  </read_first>
  <action>
    1. Crear `el-templo-api/src/modules/shared/week-dates.ts` moviendo ahi `WEEK_ONE_MONDAY`,
       `dateToWeekNumber(date)` y `dateToDayName(date)` desde `sessions/routes.ts`, y hacer que
       `sessions/routes.ts` los importe. Movimiento MECANICO, sin cambiar la logica (el ancla
       `2026-02-23T00:00:00` se conserva byte a byte). Motivo: hoy esta duplicado en el admin
       (`src/utils/weekDates.ts`) y una tercera copia garantizaria divergencia.
    2. Crear `el-templo-api/src/modules/tv/roster.ts` con:
       - `REGULAR_ROLES`, `ROM_ROLES`, `INITIUM_SOURCE_ORDER` (constantes del bloque `<interfaces>`).
       - `findBlock(blocks, role)`: para `"EPIKOS"` matchea `role === "EPIKOS" || role === "ATHLOS"`
         (si no se porta, el ultimo bloque desaparece del TV en las sesiones con ATHLOS).
       - `findInitiumBlock(sessions)`: primer nivel de `INITIUM_SOURCE_ORDER` que tenga INITIUM, con
         fallback a cualquier sesion. Copiar TAMBIEN el comentario que explica por que la fuente es
         determinista.
       - `buildRoster(classDay)`: recorre `REGULAR_ROLES` o `ROM_ROLES` segun `mode`, incluye solo
         los roles presentes en alguna sesion, y devuelve `{ role, title, shared }` donde
         `shared === true` solo para INITIUM (lista compartida, selector de nivel deshabilitado) y
         `title` = `"PYROS · " + formatParamsLabel(...)` para INITIUM (o `customTitle` si el bloque
         lo tiene, fase 100) y `"<ETIQUETA_ROL> · " + formatParamsLabel(...)` para el resto, con
         `DEUTEROS_1 -> "DEUTEROS I"`, `DEUTEROS_2 -> "DEUTEROS II"` y las etiquetas ROM tal como
         las rotula el PDF. NUNCA rotular INITIUM como "CALENTAMIENTO" (UI-SPEC).
    3. `test/tv/tv-roster.test.ts` (unitario puro, sin MySQL): orden canonico regular; orden ROM;
       rol ausente se omite; EPIKOS resuelto desde un bloque con rol ATHLOS; INITIUM tomado de alfa
       cuando existe en varios niveles y de delta cuando alfa no lo tiene; `shared` true solo en
       INITIUM.
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-api && npx tsc --noEmit && pnpm vitest run test/tv/tv-roster.test.ts && pnpm vitest run test/sessions/sessions-gating.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run test/tv/tv-roster.test.ts` verde con >= 6 tests
    - `pnpm vitest run test/sessions/sessions-gating.test.ts` sigue verde (la extraccion no rompio `/sessions/daily`)
    - `grep -c "WEEK_ONE_MONDAY" src/modules/sessions/routes.ts` devuelve `0` (quedo solo en shared)
    - `grep -c "ATHLOS" src/modules/tv/roster.ts` >= 1
    - `grep -c "CALENTAMIENTO" src/modules/tv/roster.ts` devuelve `0`
    - `npx tsc --noEmit` limpio
  </acceptance_criteria>
  <done>El orden canonico de bloques vive en el API, testeado, y las utilidades de semana dejaron de estar duplicadas dentro del backend.</done>
</task>

<task type="auto">
  <name>Task 2: class-day.ts — que sesion es "hoy" para esta sede (TZ, ROM, aprobada)</name>
  <files>el-templo-api/src/modules/tv/class-day.ts, el-templo-api/test/tv/tv-class-day.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/sessions/routes.ts lineas 456-507 (deteccion de dia ROM leyendo `dayModes`, mapeo de nivel en ROM, `getSessionsByDayIds(candidates, true)` con approvedOnly)
    - el-templo-api/src/modules/admin/service.ts lineas 316-393 (`getDaySessionDetails`: batch sesiones -> bloques -> prescripciones con leftJoin a exercises, agrupado en Maps)
    - el-templo-api/src/modules/shared/date-utils.ts lineas 109-150 (`todayInTz`, `dowInTz`, ambas con `now` inyectable)
    - el-templo-api/src/db/schema/day-modes.ts y sessions.ts (columnas `dayOfWeek`/`sessionMode`, `status`, `goalPlanType`, `memberLevel`, `dayId`)
    - el-templo-api/test/coach/outstanding-balances.test.ts (fixtures de sede AR/ES con `code` unico)
  </read_first>
  <action>
    Crear `resolveClassDay(db, branch, now?)` en `el-templo-api/src/modules/tv/class-day.ts`:
    1. `date = todayInTz(branch.timezone, now)` y `dayName = dateToDayName(date)`; `week =
       dateToWeekNumber(date)`. Domingo -> `approved: false`, `sessions: []` (no hay sesiones).
    2. Modo del dia: leer `schema.dayModes` por `dayOfWeek` (`DAY_NAME_TO_NUMBER`); `sessionMode ===
       "rom"` -> `mode = "rom"`. Sabado viene seedeado como ROM desde la migracion 0080 (D-23).
    3. Traer en BATCH todas las sesiones `status = "approved"` con `goalPlanType IS NULL` (la plani
       regular, D-14) para ese `week`+`dayName`, con sus bloques (ordenados por `sortOrder`) y sus
       prescripciones (leftJoin a `exercises` incluyendo `videoUrl`), replicando la estructura de
       `getDaySessionDetails` — sin N+1 y con TODAS las columnas del `.select()` calificadas
       (`schema.tabla.columna`), por la trampa de drizzle documentada en el repo.
    4. `levels` = niveles con sesion aprobada, ordenados `["kairos","alfa","delta","sigma"]` para
       regular y `["alfa","delta"]` para ROM (en ROM solo existen esos dos tiers).
    5. `approved` = hay al menos una sesion. Nunca lanzar excepcion por "no hay sesion": es un estado
       normal (D-09).
    `test/tv/tv-class-day.test.ts` (integracion, MySQL real):
    - dia habil con 3 niveles aprobados -> `mode: "regular"`, `levels` incluye alfa/delta/sigma,
      roster con los roles esperados
    - sesion en `draft` -> `approved: false` (no la toma)
    - sabado (dia ROM) -> `mode: "rom"`, `levels` == `["alfa","delta"]`, roles ROM_LOWER/CORE/UPPER
    - sede con `timezone: "Europe/Madrid"` y una `now` inyectada en el borde del dia -> resuelve una
      fecha distinta que una sede `America/Argentina/Buenos_Aires` con el mismo instante
    - domingo -> `approved: false`
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-api && npx tsc --noEmit && pnpm vitest run test/tv/tv-class-day.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run test/tv/tv-class-day.test.ts` verde con >= 5 tests, incluyendo el caso sabado/ROM y el caso `Europe/Madrid`
    - `grep -c "dayModes" src/modules/tv/class-day.ts` >= 1
    - `grep -c "todayInTz" src/modules/tv/class-day.ts` >= 1 y `grep -c "new Date()" src/modules/tv/class-day.ts` devuelve `0` fuera del parametro `now` con default
    - `grep -c "approved" src/modules/tv/class-day.ts` >= 1 y la funcion nunca hace `throw` por ausencia de sesion
    - `npx tsc --noEmit` limpio
  </acceptance_criteria>
  <done>Dado un branch y un instante, el API sabe que sesion mostrar, en que modo y con que niveles, con cobertura de sabado ROM y de TZ europea.</done>
</task>

<task type="auto">
  <name>Task 3: TvService — expire-on-read, clamp y armado del payload del poll</name>
  <files>el-templo-api/src/modules/tv/service.ts</files>
  <read_first>
    - el-templo-api/src/modules/subscriptions/service.ts lineas 4635-4656 (patron "expire on read — no cron job needed")
    - el-templo-api/src/modules/shared/video-url.ts (`assembleVideoUrl`)
    - el-templo-admin/src/utils/pdf/session-data-transformer.ts lineas 205-250 (formateo del rx: reps/repsMax, seconds/secondsMax, sufijo de contraccion CON./ISO./EXC., y la separacion de prescripciones `exerciseType === "mobility"`)
    - el-templo-api/src/modules/tv/types.ts (TvPollResponse y TvClassPayload — contrato del plan 164-02)
    - el-templo-api/src/modules/tv/timer-spec.ts (`toTimerSpec`)
    - el-templo-api/src/modules/admin/format-params.ts (`formatParamsLabel`)
  </read_first>
  <action>
    Crear `TvService` en `el-templo-api/src/modules/tv/service.ts` (DI por constructor con `db` y
    `log`, patron del repo) con:

    - `readState(branchId, classDate)`: SELECT de `tv_class_state` por `branch_id`. **Expire-on-read
      (D-07):** si la fila existe pero `class_date !== classDate` (la fecha de HOY en la TZ de la
      sede), devolver `null` — como si no existiera. Sin cron, sin borrar la fila (se sobrescribe
      sola en el proximo "iniciar clase"). OJO: el analog de subscriptions calcula "hoy" con
      `toISOString()` (UTC) — aca DEBE venir de `todayInTz(branch.timezone)` o Barcelona limpia a la
      hora equivocada.
    - `clampState(state, classDay)`: si `block_role` no esta en el roster vigente -> caer al primer
      rol del roster; si `level` no esta en `classDay.levels` -> caer al primero (alfa); clampear
      `exercise_index` a `[0, len(ejercicios del rol+nivel) - 1]`. El clamp es la red que hace que
      cambiar de nivel nunca rompa el bloque (Pitfall 1).
    - `buildPollPayload(device)`:
      1. lee la sede del device (`branch_id` de la FILA, jamas del request);
      2. `resolveClassDay`;
      3. `readState` + `clampState`;
      4. arma `TvPollResponse`: `serverNow = Date.now()` (Pattern 6),
         `branch = { name, utcOffsetMinutes, dateLabel }` donde `utcOffsetMinutes` se calcula para
         la TZ de la sede en ese instante y `dateLabel` es `"<DIA> · SEMANA <n>"` en mayusculas y ya
         formateado (el TV no puede depender de `Intl` con `timeZone`);
         `screen` = `"idle"` si no hay estado o `!classDay.approved` (D-09: SIN campo de error),
         `"closing"` si el estado dice closing (D-08), si no `"class"`;
         `class` = null salvo en `screen === "class"`.
      5. Para `class`: `levels`, `level`, `levelLabel` (`"NIVEL α/Δ/Σ/☉"` en regular; `"BASICO"` /
         `"AVANZADO"` en ROM; `"TODOS LOS NIVELES"` cuando el bloque es shared), `blocks` del
         roster, `blockRole`, `blockIndex` derivado del roster, `title` del roster,
         `listHeader` (`"<levelLabel> | <route> <intensity>%"`, o `"INITIUM | TODOS LOS NIVELES"`
         si shared), `mobilityLine` (`"MOVILIDAD · "` + prescripciones con
         `exerciseType === "mobility"`, o null), `exercises` (solo `exerciseType === "main"`, con
         `name`, `rx` formateado igual que el PDF y `videoUrl` via `assembleVideoUrl`),
         `exerciseIndex`, y `timer` con `spec = toTimerSpec(block.formatParams)`, `status`,
         `startedAt`/`pausedAt` como epoch ms (`.getTime()`, no strings) y `pausedAccumMs`.
    Prohibido `any`; errores con `AppError` + `handleServiceError` en la capa de rutas; logging con
    `this.log.warn` (nunca `error`) para condiciones esperables.
    El payload NO debe incluir ningun dato de socios: solo ejercicios, prescripciones y estado.

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-api && npx tsc --noEmit && pnpm vitest run test/tv/tv-class-day.test.ts test/tv/tv-roster.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` limpio y `TvService` exportado
    - `grep -c "assembleVideoUrl" src/modules/tv/service.ts` >= 1 (nunca concatenar `R2_PUBLIC_URL` a mano)
    - `grep -c "todayInTz" src/modules/tv/service.ts` >= 1 y `grep -c "toISOString" src/modules/tv/service.ts` devuelve `0`
    - `grep -c "serverNow" src/modules/tv/service.ts` >= 1
    - `grep -nE ':\s*any\b' src/modules/tv/service.ts` no devuelve lineas
    - El payload no referencia ninguna columna de `users`/`members` (revision manual del `.select()` + criterio: `grep -c "schema.users" src/modules/tv/service.ts` devuelve `0`)
  </acceptance_criteria>
  <done>`buildPollPayload` devuelve el contrato completo del plan 164-02 a partir de una fila de dispositivo, con expire-on-read y clamp.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                      | Description                                                            |
| --------------------------------------------- | ---------------------------------------------------------------------- |
| device token -> datos de la sede              | El `branch_id` sale SIEMPRE de la fila `tv_devices`, nunca del request |
| DB de sesiones -> pantalla publica en la sala | Todo lo que entra al payload lo ven socios y visitas                   |

## STRIDE Threat Register

| Threat ID | Category               | Component                                              | Disposition | Mitigation Plan                                                                                                                                 |
| --------- | ---------------------- | ------------------------------------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| T-164-20  | Elevation of Privilege | Lectura cross-sede desde un TV                         | mitigate    | `buildPollPayload(device)` toma `device.branchId` de la fila; no hay parametro de sede en ninguna ruta de device (verificado en el plan 164-08) |
| T-164-21  | Information Disclosure | Fuga de PII por el endpoint del TV                     | mitigate    | El payload solo lleva ejercicios, prescripciones, roster y estado; criterio de aceptacion prohibe referencias a `schema.users`                  |
| T-164-22  | Information Disclosure | Cocina interna expuesta a los socios (D-09)            | mitigate    | Sin sesion aprobada el payload es `screen: "idle"` sin campo de error ni mensaje: el reposo es indistinguible de "no hay clase"                 |
| T-164-23  | Tampering              | Estado viejo de otro dia mostrado como si fuera de hoy | mitigate    | Expire-on-read por `class_date` en la TZ de la sede (D-07), cubierto por tests con `Europe/Madrid`                                              |
| T-164-24  | Denial of Service      | Payload que crece sin control (listas largas + roster) | accept      | ~2.4 req/s con 6 sedes y payload acotado por bloque. Si crece, subir el intervalo de poll antes que agregar infra                               |
| T-164-SC  | Tampering              | npm/pnpm installs                                      | mitigate    | Cero paquetes nuevos. Cualquier `pnpm add` requiere checkpoint humano bloqueante (C-08)                                                         |

</threat_model>

<verification>
- `cd el-templo-api && npx tsc --noEmit` limpio
- `pnpm vitest run test/tv/tv-roster.test.ts test/tv/tv-class-day.test.ts` verde
- `pnpm vitest run test/sessions/sessions-gating.test.ts` verde (regresion de la extraccion DRY)
</verification>

<success_criteria>

- El roster canonico y el modo ROM estan implementados y testeados
- La resolucion del dia usa la TZ de la sede y solo sesiones aprobadas
- El estado de clase caduca al cambiar el dia sin cron
- `buildPollPayload` produce exactamente el contrato de `types.ts`
  </success_criteria>

<output>
Create `.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-05-SUMMARY.md` when done
</output>
