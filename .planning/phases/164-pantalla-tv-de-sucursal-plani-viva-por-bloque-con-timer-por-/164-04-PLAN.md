---
phase: 164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-
plan: 04
type: execute
wave: 2
depends_on: ["164-02"]
files_modified:
  - el-templo-admin/src/utils/pdf/quotes.ts
  - el-templo-admin/src/utils/pdf/session-pdf-builder.ts
  - el-templo-admin/src/tv/tsconfig.tv.json
  - el-templo-admin/src/tv/index.html
  - el-templo-admin/src/tv/styles.css
  - el-templo-admin/src/tv/main.ts
  - el-templo-admin/src/tv/logger.ts
  - el-templo-admin/src/tv/scale.ts
  - el-templo-admin/src/tv/diag.ts
  - el-templo-admin/src/tv/selftest.ts
  - el-templo-admin/src/tv/boot.ts
  - el-templo-admin/scripts/build-tv.mjs
  - el-templo-admin/package.json
  - el-templo-admin/.gitignore
autonomous: true
requirements: [D-06, D-08, D-20, D-22, D-24]
must_haves:
  truths:
    - "`/tv/` es una pagina estatica autocontenida fuera del SPA: el TV nunca parsea el bundle de Quasar/Vue (D-24, Pitfall 3)"
    - "El CSS y el JS del kiosco van INLINE en index.html — el nginx del admin cachea 1 año cualquier .js/.css sin hash (Pitfall 7)"
    - "tsc con target es2015 + lib es2015,dom actua como linter de compatibilidad y rompe el build ante built-ins modernos (Pitfall 5)"
    - "Las fuentes, el marmol y el logo se sirven self-hosted desde el repo, sin CDN (C-12)"
    - "El build publica una version consultable para el auto-reload (D-22)"
    - "Las QUOTES del PDF quedan en un modulo propio y las consumen tanto el PDF como el kiosco (D-06/D-08, DRY)"
  artifacts:
    - path: "el-templo-admin/scripts/build-tv.mjs"
      provides: "Pipeline: typecheck es2015 -> bundle esbuild IIFE -> assets -> index.html inline -> version.txt"
    - path: "el-templo-admin/src/tv/tsconfig.tv.json"
      provides: "target es2015 / lib es2015,dom — el linter de compatibilidad del kiosco"
      contains: '"target": "es2015"'
    - path: "el-templo-admin/src/utils/pdf/quotes.ts"
      provides: "QUOTES exportadas (10 frases del PDF)"
      exports: ["QUOTES"]
    - path: "el-templo-admin/public/tv/index.html"
      provides: "Artefacto generado (gitignored) — la pagina del kiosco"
  key_links:
    - from: "el-templo-admin/package.json"
      to: "scripts/build-tv.mjs"
      via: "script build encadenado antes de quasar build"
      pattern: "build-tv.mjs && quasar build"
    - from: "el-templo-admin/src/tv/main.ts"
      to: "src/utils/pdf/quotes.ts"
      via: "import de QUOTES al bundle del kiosco"
      pattern: "quotes"
---

<objective>
El andamiaje del kiosco: pipeline de build que produce `public/tv/index.html` autocontenido (CSS+JS inline, fuentes/imagenes self-hosted, version.txt), el tsconfig que actua de linter de compatibilidad, el esqueleto de modulos de `src/tv/` y la extraccion DRY de las QUOTES del PDF.

Purpose: D-24 exige que `/tv` viva fuera del SPA. Sin este pipeline, cada plan del kiosco improvisaria su propio empaquetado, y el nginx del admin (cache de 1 año para `.js`/`.css` sin hash) dejaria TVs congelados en una version vieja para siempre.
Output: `scripts/build-tv.mjs` funcionando end-to-end contra stubs, `src/tv/` con sus modulos vacios pero compilando, `quotes.ts` extraido, `public/tv/` gitignorado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-CONTEXT.md
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-RESEARCH.md
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-PATTERNS.md
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-UI-SPEC.md
@CLAUDE.md
@.claude/skills/el-templo-change-control/SKILL.md

<interfaces>
Contratos internos que este plan fija y que los planes 06/07/11 rellenan:

`src/tv/main.ts` (entry unico del bundle) despacha por query string:

- `?selftest=1` -> `runSelfTest()` de `./selftest` (plan 164-07)
- `?diag=1` -> `renderDiag()` de `./diag` (plan 164-06)
- default -> `boot()` de `./boot` (plan 164-11)
  y siempre llama `scaleTv()` de `./scale` + `window.addEventListener("resize", scaleTv)`.

`src/tv/logger.ts` exporta `createTvLogger(context: string)` con la MISMA forma que
`src/utils/logger.ts` del SPA (`debug/info/warn/error`) pero sin Sentry ni imports del SPA
(Pitfall 12) — el docblock debe explicar por que no reutiliza el util del SPA.

Artefactos que `build-tv.mjs` deja en `el-templo-admin/public/tv/` (gitignored):
index.html CSS + JS inline, autocontenido
version.txt hash corto del contenido (D-22)
timer-vectors.json copiado de el-templo-api/test/tv/**fixtures**/ (plan 164-02)
marble.jpg logo.png
fonts/cinzel-regular.ttf fonts/cinzel-bold.ttf fonts/nunito-regular.ttf fonts/nunito-bold.ttf

`window.__TV_VERSION__` queda inyectado en el HTML generado con el mismo valor de version.txt.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extraer QUOTES del PDF a un modulo propio</name>
  <files>el-templo-admin/src/utils/pdf/quotes.ts, el-templo-admin/src/utils/pdf/session-pdf-builder.ts</files>
  <read_first>
    - el-templo-admin/src/utils/pdf/session-pdf-builder.ts (lineas ~200-255: el array `QUOTES`, hoy NO exportado, y su uso en la pagina de cierre)
    - .planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-PATTERNS.md (seccion `quotes.ts` — advertencia de blast radius)
  </read_first>
  <action>
    Mover el array `QUOTES` (10 objetos con `text` / `goldText` / `author`) de
    `session-pdf-builder.ts` a `el-templo-admin/src/utils/pdf/quotes.ts` como
    `export const QUOTES`, e importarlo de vuelta en `session-pdf-builder.ts`.
    Es un movimiento MECANICO: no reformatear el contenido, no cambiar las comillas tipograficas
    escapadas, no reordenar. `session-pdf-builder.ts` no tiene tests — cualquier cambio de contenido
    seria invisible hasta que alguien genere un PDF.
    Exportar tambien un tipo `TvQuote` (`{ text: string; goldText: string; author: string }`) para
    que el kiosco lo consuma tipado.
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && npx vue-tsc --noEmit -p tsconfig.json 2>&1 | tail -5; grep -c "export const QUOTES" src/utils/pdf/quotes.ts && grep -c "from \"./quotes\"\|from './quotes'" src/utils/pdf/session-pdf-builder.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const QUOTES" src/utils/pdf/quotes.ts` devuelve `1`
    - `session-pdf-builder.ts` importa QUOTES desde `./quotes` y ya no declara el array
    - `node -e "..."` no aplica: verificar por diff que las 10 frases quedaron byte a byte iguales (`git diff` muestra solo movimiento de lineas, sin cambios de texto)
    - `npx vue-tsc --noEmit` no agrega errores nuevos respecto del baseline conocido del admin
  </acceptance_criteria>
  <done>QUOTES vive en su propio modulo, el PDF sigue compilando y el kiosco puede importarlo.</done>
</task>

<task type="auto">
  <name>Task 2: Esqueleto de src/tv (tsconfig es2015, entry, logger, stubs, html y css de partida)</name>
  <files>el-templo-admin/src/tv/tsconfig.tv.json, el-templo-admin/src/tv/index.html, el-templo-admin/src/tv/styles.css, el-templo-admin/src/tv/main.ts, el-templo-admin/src/tv/logger.ts, el-templo-admin/src/tv/scale.ts, el-templo-admin/src/tv/diag.ts, el-templo-admin/src/tv/selftest.ts, el-templo-admin/src/tv/boot.ts</files>
  <read_first>
    - .planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-tv-mockup-template.html (fuente literal del HTML y del CSS del kiosco)
    - el-templo-admin/tsconfig.json (para saber que NO heredar: el SPA usa lib esnext)
    - el-templo-admin/src/utils/logger.ts (forma de `createLogger` a replicar sin Sentry)
    - .planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-RESEARCH.md (Pitfall 5 y Pitfall 12)
  </read_first>
  <action>
    1. `src/tv/tsconfig.tv.json`: NO extiende el tsconfig del admin. `"target": "es2015"`,
       `"lib": ["es2015", "dom"]`, `"strict": true`, `"noEmit": true`, `"moduleResolution": "bundler"`
       (o `"node"`), `"include": ["./**/*.ts", "../utils/pdf/quotes.ts"]`. El `lib` acotado es el
       punto: rompe la compilacion ante `String.padStart`, `Object.entries`, `Array.flat`,
       `Object.fromEntries` — built-ins que ningun transpilador polifillea y que revientan en un
       webOS 4.x (Chromium 53).
    2. `src/tv/index.html`: copiar el `<body>` del mockup template (el `#tv` con topbar, cabecera,
       stage, columnas izquierda/derecha, panel de timer y columna de video) MENOS el footer `.demo`
       (esa botonera es la maqueta del control, que en el producto vive en el celular). Reemplazar
       el `<style>` inline por el marcador `<!--__STYLE__-->` y agregar `<!--__SCRIPT__-->` antes de
       `</body>`. `<img src="logo.png">` y el `<video>` sin `src` fijo. Agregar
       `<meta name="viewport" content="width=device-width, initial-scale=1">` y
       `<html lang="es">`. Conservar todos los `id=` del mockup (`fecha`, `reloj`, `titulo`,
       `movilidad`, `bloqueNum`, `dots`, `stage`, `cabNivel`, `listaBox`, `timerPanel`, `timerCab`,
       `fase`, `digitos`, `sub`, `progreso`, `hint`, `cabVideo`): el render del plan 164-11 los usa.
       Agregar tambien contenedores vacios `id="pantallaReposo"` y `id="pantallaCierre"` ocultos
       (D-06/D-08) y `id="pantallaPairing"`.
    3. `src/tv/styles.css`: mover el contenido del `<style>` del mockup TAL CUAL (la conversion de
       compatibilidad es el plan 164-06). Ajustar solo las `@font-face` para que apunten a
       `url(fonts/cinzel-regular.ttf) format("truetype")` etc. en lugar de los placeholders base64,
       y el fondo marmol a `url(marble.jpg)`.
    4. `src/tv/logger.ts`: `createTvLogger(context)` con `debug/info/warn/error` que escriben por
       `console` PREFIJADOS con `[tv:context]` y ademas empujan a un buffer circular de 50 entradas
       exportado como `getTvLogBuffer()` (lo consumen `?diag=1` y el envio a `/api/tv/client-log`).
       Docblock explicando por que no usa `src/utils/logger.ts` (Pitfall 12: el kiosco no puede
       importar medio SPA). Este es el UNICO lugar de `src/` donde `console` esta permitido y el
       docblock debe decirlo.
    5. `main.ts` con el dispatcher del bloque `<interfaces>` + `scale.ts`, `diag.ts`, `selftest.ts`,
       `boot.ts` como stubs exportando funciones vacias con un `TODO(plan 164-06/07/11)`.
    Regla dura para todo `src/tv/`: sin `async`/`await` innecesario, sin optional chaining, sin
    `??`, sin `padStart`, sin `Object.entries`, sin `ResizeObserver`, sin `fetch` con
    `AbortController`. Usar `XMLHttpRequest` o `fetch` simple (Chromium 42+).
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && npx tsc -p src/tv/tsconfig.tv.json --noEmit && grep -c '__STYLE__' src/tv/index.html && grep -c '__SCRIPT__' src/tv/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -p src/tv/tsconfig.tv.json --noEmit` sale sin errores
    - `grep -c '"target": "es2015"' src/tv/tsconfig.tv.json` devuelve `1` y `grep -c '"es2015", "dom"' src/tv/tsconfig.tv.json` devuelve `1`
    - `grep -c '__STYLE__' src/tv/index.html` y `grep -c '__SCRIPT__' src/tv/index.html` devuelven `1` cada uno
    - `grep -c 'demoBar\|class="demo"' src/tv/index.html` devuelve `0` (la botonera de la maqueta no va)
    - `grep -c 'data:font' src/tv/styles.css` devuelve `0` (fuentes como archivo, no base64 inline)
    - `grep -rc "from 'src/\|from \"src/" src/tv/*.ts` no encuentra imports del SPA salvo `../utils/pdf/quotes`
  </acceptance_criteria>
  <done>El kiosco tiene su arbol de modulos, compila bajo el target es2015 y el HTML/CSS de partida esta en el repo.</done>
</task>

<task type="auto">
  <name>Task 3: build-tv.mjs — typecheck, bundle, assets, inline y version</name>
  <files>el-templo-admin/scripts/build-tv.mjs, el-templo-admin/package.json, el-templo-admin/.gitignore</files>
  <read_first>
    - el-templo-admin/scripts/copy-ffmpeg.mjs (archivo COMPLETO: ESM, resolucion de rutas con fileURLToPath, prefijo de logs, process.exit(1), utimesSync)
    - el-templo-admin/src/utils/pdf/pdf-assets.ts (nombres exactos de los exports base64: CINZEL_REGULAR_BASE64, CINZEL_BOLD_BASE64, NUNITO_SANS_REGULAR_BASE64, NUNITO_SANS_BOLD_BASE64, MARBLE_BG_BASE64, LOGO_BASE64)
    - deploy/nginx/admin.eltemplo.org (linea 20: el regex de cache de 1 año cubre js/css/ttf pero NO html/txt/json)
    - el-templo-admin/package.json (scripts actuales)
    - el-templo-admin/.gitignore (seccion "Build output")
  </read_first>
  <action>
    Crear `el-templo-admin/scripts/build-tv.mjs` (ESM, prefijo de logs `[build-tv]`, `process.exit(1)`
    ante cualquier fallo para que CI lo detecte) con estos pasos en orden:

    1. Resolver `esbuild` con `await import("esbuild")` dentro de try/catch. esbuild YA esta en
       `node_modules` como dependencia transitiva de vite (0.25.x, verificado). Si no resuelve:
       `console.error` explicando que agregarlo como devDependency explicita requiere aprobacion
       humana (C-08) y `process.exit(1)`. NO instalar nada.
    2. Correr `node_modules/.bin/tsc -p src/tv/tsconfig.tv.json --noEmit` con `execFileSync`
       (stdio inherit). Este paso es el gate de compatibilidad: si falla, abortar.
    3. Bundlear: `esbuild.build({ entryPoints: ["src/tv/main.ts"], bundle: true, format: "iife",
       target: ["es2015"], minify: true, write: false, legalComments: "none" })` y quedarse con el
       texto del unico output.
    4. Extraer los assets base64: bundlear `src/utils/pdf/pdf-assets.ts` con
       `format: "cjs", bundle: true, write: false`, evaluar el resultado con
       `new Function("module", "exports", code)` y leer los exports. Decodificar a
       `public/tv/fonts/cinzel-regular.ttf`, `cinzel-bold.ttf`, `nunito-regular.ttf`,
       `nunito-bold.ttf`, `public/tv/marble.jpg`, `public/tv/logo.png` (quitar el prefijo
       `data:...;base64,` si estuviera presente).
    5. Copiar `../el-templo-api/test/tv/__fixtures__/timer-vectors.json` a
       `public/tv/timer-vectors.json`. Si el archivo fuente no existe -> `process.exit(1)` con
       mensaje claro (lo produce el plan 164-02).
    6. Calcular `version` = primeros 8 chars del sha256 de (bundle + styles.css + index.html).
       Escribir `public/tv/version.txt` con ese valor (sin newline final).
    7. Generar `public/tv/index.html`: tomar `src/tv/index.html` y reemplazar
       `<!--__STYLE__-->` por `<style>` + contenido de `src/tv/styles.css` + `</style>`, y
       `<!--__SCRIPT__-->` por `<script>window.__TV_VERSION__="<version>";` + bundle + `</script>`.
       CSS y JS van INLINE a proposito: el nginx del admin cachea `.js`/`.css` un año con
       `immutable` y nuestro bundle no lleva hash en el nombre (Pitfall 7).
    8. `utimesSync` sobre los assets que no cambiaron (patron de copy-ffmpeg) para no re-rsyncar
       ~500 KB en cada deploy.
    9. Log final con el tamaño del HTML generado y la version.

    `package.json`: `"build": "node scripts/build-tv.mjs && quasar build"` y
    `"build:tv": "node scripts/build-tv.mjs"`. NO tocar `postinstall`.
    `.gitignore` del admin: agregar `/public/tv` en la seccion "Build output" (decision explicita:
    se GENERA, no se commitea — a diferencia de `public/ffmpeg`, evita ~500 KB de binarios por diff).

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && node scripts/build-tv.mjs && node -e "const fs=require('fs');const p='public/tv/';['index.html','version.txt','timer-vectors.json','marble.jpg','logo.png','fonts/cinzel-regular.ttf','fonts/cinzel-bold.ttf','fonts/nunito-regular.ttf','fonts/nunito-bold.ttf'].forEach(function(f){ if(!fs.existsSync(p+f)) throw new Error('falta '+f) }); const h=fs.readFileSync(p+'index.html','utf8'); if(h.indexOf('__STYLE__')>=0||h.indexOf('__SCRIPT__')>=0) throw new Error('placeholders sin reemplazar'); if(h.indexOf('<script src=')>=0||h.indexOf('<link rel=\"stylesheet\"')>=0) throw new Error('assets externos: deben ir inline'); console.log('TV_BUILD_OK', h.length)"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/build-tv.mjs` termina en 0 y genera los 9 archivos listados en el comando de verify
    - `public/tv/index.html` NO contiene `<script src=` ni `<link rel="stylesheet"` (todo inline)
    - `public/tv/index.html` contiene `window.__TV_VERSION__` con el mismo valor que `public/tv/version.txt`
    - `grep -c "build-tv.mjs && quasar build" package.json` devuelve `1`
    - `grep -c "^/public/tv" .gitignore` devuelve `1`
    - Borrar `src/tv/styles.css` y correr el script falla con exit code distinto de 0 (comportamiento fail-fast; restaurar despues)
    - `git status --porcelain public/tv | wc -l` devuelve `0` (ignorado)
  </acceptance_criteria>
  <done>`pnpm build:tv` produce un `/tv/` autocontenido y versionado a partir de los stubs.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                            |
| --------------------------------- | -------------------------------------------------------------------------------------- |
| build del admin -> browser del TV | Todo lo que el kiosco ejecuta se decide en build time: no hay carga dinamica de codigo |
| repo -> assets servidos           | Fuentes e imagenes salen del repo (base64 versionado), nunca de un CDN                 |

## STRIDE Threat Register

| Threat ID | Category          | Component                                                           | Disposition | Mitigation Plan                                                                                                                                                      |
| --------- | ----------------- | ------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-164-16  | Tampering         | Carga de recursos de terceros en el kiosco                          | mitigate    | Cero CDN (C-12): fuentes decodificadas del base64 del repo a `public/tv/fonts/`, CSS y JS inline. Criterio de aceptacion verifica que no haya `<script src=` externo |
| T-164-17  | Denial of Service | TVs congelados en una version vieja por el cache de 1 año del nginx | mitigate    | CSS/JS inline en `index.html` (no matchea el regex de cache) + `version.txt` (`.txt` tampoco matchea) leido con cache-buster (plan 164-11)                           |
| T-164-18  | Tampering         | Dependencia nueva colada por el pipeline                            | mitigate    | `build-tv.mjs` no instala nada: usa `esbuild` ya presente y falla con mensaje explicito si no resuelve. Agregarlo como dep explicita es gate humano (C-08)           |
| T-164-19  | Spoofing          | Bundle del kiosco fuera de sincro con los vectores del API          | mitigate    | El build copia `timer-vectors.json` desde el API y aborta si falta: el selftest en el TV siempre corre contra los vectores de esa build                              |
| T-164-SC  | Tampering         | npm/pnpm installs                                                   | mitigate    | Cero paquetes nuevos (RESEARCH: auditoria de paquetes vacia por diseño). Cualquier `pnpm add` requiere checkpoint humano bloqueante (C-08)                           |

</threat_model>

<verification>
- `node scripts/build-tv.mjs` verde y `public/tv/index.html` autocontenido
- `npx tsc -p src/tv/tsconfig.tv.json --noEmit` limpio
- `npx vue-tsc --noEmit` sin errores nuevos en el admin
- `git status --porcelain` no muestra `public/tv`
</verification>

<success_criteria>

- El pipeline del kiosco existe y es fail-fast
- El target de compilacion del kiosco es es2015 con lib acotada
- Las QUOTES son un modulo compartido
- `/tv/` se genera en build y esta gitignorado
  </success_criteria>

<output>
Create `.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-04-SUMMARY.md` when done
</output>
