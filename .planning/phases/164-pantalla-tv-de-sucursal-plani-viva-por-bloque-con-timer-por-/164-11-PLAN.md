---
phase: 164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-
plan: 11
type: execute
wave: 5
depends_on: ["164-06", "164-07", "164-08"]
files_modified:
  - el-templo-admin/src/tv/boot.ts
  - el-templo-admin/src/tv/poll.ts
  - el-templo-admin/src/tv/render.ts
  - el-templo-admin/src/tv/audio.ts
autonomous: true
requirements: [D-04, D-06, D-08, D-09, D-16, D-17, D-19, D-20, D-22]
must_haves:
  truths:
    - "Un TV sin vincular muestra el codigo de 6 caracteres y se conecta solo cuando el staff lo reclama (estilo Netflix)"
    - "Con clase en curso, la pantalla muestra bloque, lista del nivel elegido, timer por formato y video del ejercicio, con el segundero siempre visible (pedido #1 de los socios)"
    - "Fuera de clase muestra reposo: reloj gigante + logo + quote del PDF (D-06). Tras el ultimo bloque, pantalla de cierre (D-08)"
    - "Con wifi caido sigue contando el timer con el ultimo estado y se realinea al volver"
    - "Detecta una version nueva del frontend y recarga SOLO en reposo, nunca en medio de un bloque (D-22)"
    - "El video del ejercicio se reproduce en loop y cae a placeholder si no hay URL o falla la carga"
  artifacts:
    - path: "el-templo-admin/src/tv/poll.ts"
      provides: "Ciclo de poll, token en localStorage, offset de reloj, pairing, chequeo de version"
      exports: ["startPolling"]
    - path: "el-templo-admin/src/tv/render.ts"
      provides: "Render idempotente de las 4 pantallas (pairing / clase / reposo / cierre)"
      exports: ["renderState", "tickClock", "tickTimer"]
    - path: "el-templo-admin/src/tv/boot.ts"
      provides: "boot(): arma los intervalos y arranca el kiosco"
      exports: ["boot"]
    - path: "el-templo-admin/src/tv/audio.ts"
      provides: "Beeps WebAudio opcionales en cambio de fase (D-19)"
      exports: ["beep"]
  key_links:
    - from: "el-templo-admin/src/tv/poll.ts"
      to: "GET /api/tv/state"
      via: "Authorization: Device <token> cada 2.5 s"
      pattern: "api/tv/state"
    - from: "el-templo-admin/src/tv/render.ts"
      to: "phaseAt / elapsedFrom / formatDigits"
      via: "tick local del timer sin red"
      pattern: "phaseAt"
    - from: "el-templo-admin/src/tv/poll.ts"
      to: "version.txt"
      via: "auto-reload por version (D-22)"
      pattern: "version.txt"
---

<objective>
El kiosco funcionando: vinculacion, ciclo de poll con correccion de reloj, render de las cuatro pantallas, tick local del timer, video del ejercicio, beeps opcionales y auto-reload por version.

Purpose: es la pantalla que ven los socios. Todo lo anterior de la fase existe para alimentar este archivo.
Output: `boot.ts`, `poll.ts`, `render.ts` y `audio.ts` en `src/tv/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-UI-SPEC.md
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-CONTEXT.md
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-RESEARCH.md
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-PATTERNS.md
@CLAUDE.md

<interfaces>
Reglas duras de `src/tv/` (el tsconfig con `lib: es2015` ya rechaza varias en compilacion):
  PROHIBIDO: `padStart`, `Object.entries/values/fromEntries`, `Array.flat`, `?.`, `??`,
             `ResizeObserver`, `AbortController`, `innerHTML` con datos del API, `navigator.wakeLock`
             (en webOS la promesa se cuelga: la pantalla encendida se resuelve por configuracion del
             TV, documentada en el runbook del plan 164-13).
  OBLIGATORIO: `textContent` / `createElement`; UN solo `<video>` cuyo `src` cambia + `load()`;
               UN solo `setInterval` de reloj y UNO de poll, creados una unica vez; render
               idempotente que ACTUALIZA nodos existentes (nada de reconstruir el DOM por tick).

Claves de `localStorage`: `tv.deviceToken`, `tv.deviceCode`.
Intervalos: poll 2500 ms, reloj/timer 250 ms (para que el segundero y los digitos no salten),
chequeo de version cada 60 s.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: poll.ts — vinculacion, ciclo de poll, offset de reloj y auto-reload</name>
  <files>el-templo-admin/src/tv/poll.ts</files>
  <read_first>
    - el-templo-api/src/modules/tv/types.ts (TvPollResponse: el contrato que consume)
    - el-templo-api/src/modules/tv/device-routes.ts (rutas /api/tv/pair/start, /pair/status, /state, /client-log)
    - el-templo-admin/src/tv/logger.ts y src/tv/timer.ts
    - .planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-RESEARCH.md (Pattern 6: offset de reloj suavizado; Pitfall 7: cache; Pitfall 13: memoria)
  </read_first>
  <action>
    Implementar en `src/tv/poll.ts`:
    - Resolucion de la base del API: derivarla del host actual (mismo esquema que usa el admin) con
      la posibilidad de override por query `?api=` para pruebas en la sede. Documentar la eleccion.
    - `ensureDevice()`: si no hay `tv.deviceToken` en `localStorage`, hacer
      `POST /api/tv/pair/start`, guardar `tv.deviceCode`, mostrar el `userCode` en
      `#pantallaPairing` (texto gigante, agrupado de a 3 caracteres para leerlo a 4 m) y poll de
      `GET /api/tv/pair/status?deviceCode=...` cada 3 s hasta recibir `status: "paired"`; entonces
      guardar el token y arrancar el poll normal. Si `status: "consumed"` o `"unknown"`, limpiar
      `localStorage` y volver a `start`.
    - `pollOnce()`: `GET /api/tv/state` con `Authorization: Device <token>`. En 401: borrar
      `tv.deviceToken` y volver al flujo de pairing (D-03: el TV entiende que fue revocado).
      En error de red: NO tocar el estado en memoria — el timer sigue corriendo con el ultimo
      estado conocido y la pantalla no parpadea; loguear `warn` y reintentar en el proximo tick.
    - Offset de reloj: `clockOffsetMs = serverNow - Date.now()` en cada poll, SUAVIZADO (media movil
      simple de las ultimas 5 muestras, o ignorar variaciones menores a 500 ms) para que los digitos
      no salten. Exportar `nowCorrected()`.
    - Auto-reload (D-22): cada 60 s, `GET version.txt?t=<epoch>` (ruta relativa, cache-buster). Si
      difiere de `window.__TV_VERSION__` marcar `pendingReload = true`, y ejecutar
      `location.reload()` SOLO cuando `screen === "idle"` — nunca en medio de un bloque. Aplicar el
      mismo criterio como reset preventivo de memoria: si el uptime supera las 12 h y la pantalla
      esta en reposo, recargar.
    - Reporte de errores: ante una excepcion no esperada del render o del poll, mandar
      `POST /api/tv/client-log` con `level: "error"` y un mensaje corto (throttle: como maximo uno
      por minuto, para no inundar).
    Sin `AbortController`, sin `async`/`await` innecesario; `fetch` simple con `.then()` es
    suficiente y mas seguro para el piso de compatibilidad.
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && npx tsc -p src/tv/tsconfig.tv.json --noEmit && node -e "const s=require('fs').readFileSync('src/tv/poll.ts','utf8');['AbortController','ResizeObserver','padStart','Object.entries','innerHTML'].forEach(function(t){if(s.indexOf(t)>=0)throw new Error('prohibido: '+t)});if(s.indexOf('version.txt')<0)throw new Error('falta el chequeo de version (D-22)');console.log('POLL_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -p src/tv/tsconfig.tv.json --noEmit` limpio
    - El chequeo de node no encuentra `AbortController`, `ResizeObserver`, `padStart`, `Object.entries` ni `innerHTML` en `poll.ts`
    - `grep -c "tv.deviceToken" src/tv/poll.ts` >= 2 (guardar y borrar)
    - Existe la logica de 401 -> volver a pairing
    - `grep -c "clockOffset" src/tv/poll.ts` >= 1 y el offset se suaviza (no se asigna crudo en cada poll)
    - `location.reload()` aparece SOLO dentro de una guarda que exige `screen === "idle"` (revision manual + `grep -c "reload" src/tv/poll.ts` = 1)
  </acceptance_criteria>
  <done>El kiosco se vincula solo, pollea con correccion de reloj, sobrevive a la caida de wifi y se actualiza en el momento seguro.</done>
</task>

<task type="auto">
  <name>Task 2: render.ts — pantalla de clase, tick del timer y video</name>
  <files>el-templo-admin/src/tv/render.ts</files>
  <read_first>
    - .planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-tv-mockup-template.html (el `<script>` del mockup ya implementa el render de la lista, el timer y los dots: adaptarlo a los datos del payload en vez de la sesion de ejemplo)
    - .planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-UI-SPEC.md (layout, estados TRABAJO pulsante / DESCANSO / BLOQUE COMPLETO, modo compacto para listas de mas de 5 items)
    - el-templo-app/src/modules/training/components/player/VideoPlaceholder.vue (atributos del `<video>`, `load()` + autoplay defensivo, placeholder "Video proximamente")
    - el-templo-admin/src/tv/timer.ts (phaseAt, elapsedFrom, formatDigits)
    - el-templo-admin/src/tv/index.html (todos los `id` disponibles)
  </read_first>
  <action>
    Implementar en `src/tv/render.ts`:
    - `renderState(payload)`: idempotente. Actualiza SOLO los nodos que cambiaron:
      topbar (`EL TEMPLO <SEDE>` + `dateLabel`), cabecera (`title` + `mobilityLine` + "BLOQUE n / M"
      con los dots de progreso), header de lista (`listHeader`), lista de ejercicios (el actual
      gigante con `▶` dorado y su rx en oro, el resto atenuado; modo compacto automatico si hay mas
      de 5 items), header del panel de video con el nombre del ejercicio actual y el timer
      (formato como header del panel).
      Detectar cambios comparando contra el ultimo payload renderizado (por `blockRole`, `level`,
      `exerciseIndex` y longitudes) para no reconstruir el DOM en cada poll.
    - Video: UN solo elemento `<video autoplay loop muted playsinline>`; cambiar `src` + `load()` +
      intento de `play()` con catch silencioso SOLO cuando cambia el ejercicio. Si `videoUrl` es
      null o el elemento dispara `error`, mostrar el placeholder "Video proximamente" (mismo
      criterio que la app), manteniendo el ultimo frame cuando sea posible en vez de dejar el panel
      en negro.
    - `tickClock()`: cada 250 ms, calcular la hora de la sede como
      `new Date(nowCorrected() + utcOffsetMinutes * 60000)` y leerla con `getUTCHours/Minutes/Seconds`
      (NUNCA `Intl` con `timeZone`: la ICU del TV puede venir reducida). Pintar `HH:MM:SS` con el
      segundero en oro (Cinzel) — es el pedido #1 de los socios.
    - `tickTimer()`: cada 250 ms, `elapsedFrom(timer, nowCorrected())` -> `phaseAt(elapsed, spec)` ->
      pintar fase (TRABAJO pulsante / DESCANSO con borde muted / PAUSA / BLOQUE COMPLETO con fondo
      arena), digitos con `formatDigits`, sub-linea ("RONDA 3/8", "INTERVALO 4/10") y barra de
      progreso. Al terminar: "BLOQUE COMPLETO" + hint "el profe avanza al siguiente bloque".
      Estado `paused` -> cartel PAUSA con los digitos congelados (D-17). Estado `idle` -> "LISTOS"
      con los digitos en el valor inicial del spec (D-16: no hay cuenta previa; al iniciar arranca
      TRABAJO al instante).
    Todo el texto con `textContent`. Nada de `innerHTML` con datos del API (los nombres de
    ejercicios salen de la DB).
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && npx tsc -p src/tv/tsconfig.tv.json --noEmit && node -e "const s=require('fs').readFileSync('src/tv/render.ts','utf8');['innerHTML','padStart','Object.entries','Intl.'].forEach(function(t){if(s.indexOf(t)>=0)throw new Error('prohibido: '+t)});console.log('RENDER_OK')" && node scripts/build-tv.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -p src/tv/tsconfig.tv.json --noEmit` limpio
    - `render.ts` no contiene `innerHTML`, `padStart`, `Object.entries` ni `Intl.`
    - `grep -c "getUTCHours" src/tv/render.ts` >= 1 (reloj sin Intl)
    - `grep -c "querySelector\|getElementById" src/tv/render.ts` >= 1 y `grep -c "createElement(\"video\")\|document.createElement('video')" src/tv/render.ts` devuelve `0` (un solo `<video>`, el del HTML)
    - `node scripts/build-tv.mjs` verde
  </acceptance_criteria>
  <done>Con un payload de clase, la pantalla se ve como el mockup v8 y el timer corre localmente.</done>
</task>

<task type="auto">
  <name>Task 3: boot.ts + reposo/cierre + beeps</name>
  <files>el-templo-admin/src/tv/boot.ts, el-templo-admin/src/tv/audio.ts, el-templo-admin/src/tv/render.ts</files>
  <read_first>
    - el-templo-admin/src/utils/pdf/quotes.ts (las 10 frases del PDF, extraidas en el plan 164-04)
    - .planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-CONTEXT.md (D-06, D-08, D-09, D-19)
    - el-templo-admin/src/tv/poll.ts y src/tv/render.ts (creados en los tasks 1 y 2)
    - .planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-RESEARCH.md (Pitfall 13: longevidad del kiosco)
  </read_first>
  <action>
    1. Pantallas de reposo y cierre en `render.ts`: `screen === "idle"` -> mostrar `#pantallaReposo`
       con reloj gigante `HH:MM:SS` (Cinzel), logo y una quote de `QUOTES` (rotar cada 60 s,
       eleccion estable — no cambiar en cada tick). `screen === "closing"` -> `#pantallaCierre` con
       logo + reloj + quote (D-08), hasta que el profe termine la clase. Sin sesion aprobada la API
       ya manda `idle`: el TV NO muestra ningun mensaje de error (D-09).
    2. `audio.ts`: `beep(kind)` con WebAudio (`AudioContext` con fallback `webkitAudioContext`),
       tonos cortos distintos para cambio de fase y para fin de bloque. Silencioso si
       `soundEnabled` es false (D-19: OFF por default, lo activa el profe desde el celular) o si el
       navegador bloquea el autoplay de audio: envolver todo en try/catch y degradar sin romper el
       render. Nunca crear un `AudioContext` por beep: uno solo, perezoso.
    3. `boot.ts`: crea UNA sola vez los intervalos (poll 2500 ms, tick 250 ms, version 60 s), llama
       `scaleTv()`, arranca `ensureDevice()` y conecta `renderState` al resultado del poll.
       Manejo global: `window.onerror` -> `client-log` con throttle. Nada de `wakeLock` (en webOS la
       promesa se cuelga; va al runbook).
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && npx tsc -p src/tv/tsconfig.tv.json --noEmit && node scripts/build-tv.mjs && node -e "const fs=require('fs');const h=fs.readFileSync('public/tv/index.html','utf8');['padStart(','Object.entries(','Object.fromEntries(','ResizeObserver','AbortController'].forEach(function(t){if(h.indexOf(t)>=0)throw new Error('el bundle contiene una API prohibida: '+t)});if(h.indexOf('CADENAS DE LA DISCIPLINA')<0&&h.indexOf('QUOTES')<0&&h.indexOf('Jim Rohn')<0)throw new Error('las quotes no quedaron inlineadas');console.log('BUNDLE_OK',h.length)"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -p src/tv/tsconfig.tv.json --noEmit` limpio
    - El BUNDLE GENERADO (`public/tv/index.html`) no contiene `padStart(`, `Object.entries(`, `Object.fromEntries(`, `ResizeObserver` ni `AbortController`
    - Las quotes del PDF estan inlineadas en el bundle (verificado por el comando node)
    - `grep -c "wakeLock" src/tv/*.ts` devuelve `0`
    - `grep -c "new AudioContext\|webkitAudioContext" src/tv/audio.ts` >= 1 y el AudioContext es unico y perezoso
    - `boot.ts` crea exactamente 3 intervalos (`grep -c "setInterval" src/tv/boot.ts` devuelve `3`)
    - Abriendo el `public/tv/index.html` servido localmente contra la API local: sin token muestra el codigo de pairing; tras vincular y escribir estado desde el control, muestra la pantalla de clase
  </acceptance_criteria>
  <done>El kiosco cubre las cuatro pantallas, los beeps opcionales y la longevidad de un dia entero encendido.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                              |
| --------------------------------- | -------------------------------------------------------- |
| API -> DOM del kiosco             | Nombres de ejercicios y prescripciones vienen de la DB   |
| localStorage del TV -> credencial | El device token vive en un aparato fisicamente accesible |
| R2 publico -> `<video>`           | Los mp4 se sirven sin auth desde el worker publico       |

## STRIDE Threat Register

| Threat ID | Category               | Component                                        | Disposition | Mitigation Plan                                                                                                     |
| --------- | ---------------------- | ------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------- |
| T-164-46  | Tampering              | XSS por datos de la DB renderizados en el kiosco | mitigate    | `textContent`/`createElement` en todo el render; gate automatico que rechaza `innerHTML` en `render.ts` y `poll.ts` |
| T-164-47  | Information Disclosure | Device token visible en pantalla                 | mitigate    | El token nunca se pinta: en pantalla solo va el `user_code` de pairing; el diag lo trunca a 4 chars                 |
| T-164-48  | Denial of Service      | Fuga de memoria tras 12 h encendido (Pitfall 13) | mitigate    | Un solo `<video>`, render idempotente, 3 intervalos creados una vez, y reload preventivo si uptime > 12 h EN REPOSO |
| T-164-49  | Denial of Service      | Wifi caido durante la clase                      | mitigate    | El poll fallido no altera el estado en memoria: el timer sigue corriendo local y se realinea al volver              |
| T-164-50  | Tampering              | Version vieja servida por cache tras un deploy   | mitigate    | Chequeo de `version.txt` con cache-buster + `location.reload()` solo en reposo (D-22)                               |
| T-164-SC  | Tampering              | npm/pnpm installs                                | mitigate    | Cero paquetes nuevos. Cualquier `pnpm add` requiere checkpoint humano bloqueante (C-08)                             |

</threat_model>

<verification>
- `npx tsc -p src/tv/tsconfig.tv.json --noEmit` limpio
- `node scripts/build-tv.mjs` verde y el bundle sin APIs prohibidas
- Prueba local end-to-end contra la API: pairing -> clase -> reposo -> cierre
- `/tv/?selftest=1` sigue en PASS tras el bundle final
</verification>

<success_criteria>

- Las cuatro pantallas funcionan contra la API local
- El timer corre localmente y sobrevive a la caida de red
- El auto-reload solo dispara en reposo
- El bundle no contiene ninguna API posterior al piso de compatibilidad
  </success_criteria>

<output>
Create `.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-11-SUMMARY.md` when done
</output>
