---
phase: 164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-
plan: 06
type: execute
wave: 3
depends_on: ["164-04"]
files_modified:
  - el-templo-admin/src/tv/styles.css
  - el-templo-admin/src/tv/index.html
  - el-templo-admin/src/tv/scale.ts
  - el-templo-admin/src/tv/diag.ts
autonomous: true
requirements: [D-20, D-24]
must_haves:
  truths:
    - "El layout del mockup v8 se ve igual sin usar container queries, aspect-ratio, gap en flex ni CSS grid (piso Chromium 53 — D-20)"
    - "El marco 16:9 y la tipografia escalan por JS calculando el font-size raiz, no por unidades cqw"
    - "Los simbolos α Δ Σ no dependen de Cinzel y el ☉ se dibuja con CSS (Pitfall 6)"
    - "`/tv/?diag=1` imprime en pantalla user agent, feature-detects, version y latencia: es el unico instrumento de diagnostico posible en un TV sin devtools (D-24)"
  artifacts:
    - path: "el-templo-admin/src/tv/styles.css"
      provides: "CSS del kiosco compatible con Chromium 53"
    - path: "el-templo-admin/src/tv/scale.ts"
      provides: "scaleTv(): marco 16:9 + font-size raiz calculado"
      exports: ["scaleTv"]
    - path: "el-templo-admin/src/tv/diag.ts"
      provides: "renderDiag(): pantalla de diagnostico ?diag=1"
      exports: ["renderDiag"]
  key_links:
    - from: "el-templo-admin/src/tv/main.ts"
      to: "scaleTv / renderDiag"
      via: "dispatcher del entry"
      pattern: "scaleTv|renderDiag"
    - from: "el-templo-admin/src/tv/styles.css"
      to: "fonts/*.ttf + marble.jpg + logo.png"
      via: "assets self-hosted generados por build-tv.mjs"
      pattern: "fonts/"
---

<objective>
Convertir el mockup v8 (que usa container queries, CSS grid, `gap` en flex y `aspect-ratio`) en un layout que corra en el browser nativo del peor televisor plausible de una sede, y darle al kiosco su unico instrumento de diagnostico.

Purpose: D-20 es una restriccion dura y no verificable desde esta maquina. El modo de falla es pantalla en blanco sin error visible en la sede. Escribir el layout compatible UNA sola vez (no detras de un `@supports`) hace que el camino probado sea el unico camino.
Output: `styles.css` sin ninguna feature posterior a Chromium 53, `index.html` ajustado, `scale.ts` y `diag.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-UI-SPEC.md
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-RESEARCH.md
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-CONTEXT.md
@CLAUDE.md

<interfaces>
Umbrales que este plan NO puede cruzar (matriz de motores de TV del RESEARCH):

PROHIBIDO desde Chromium primer TV que lo tiene
container-type / cqw / @container 105 Tizen 9 y webOS 25 (2025)
aspect-ratio 88 2023
gap en flexbox 84 2023
display: grid 57 2019
ResizeObserver 64 2020
String.padStart / Object.entries 57 / 54 2019
PERMITIDO: flexbox, custom properties, object-fit, fetch, Promise, clases ES2015, template literals

Conversion mecanica acordada (RESEARCH Pattern 7):
`Xcqw` del mockup -> `Xrem`, con `document.documentElement.style.fontSize = tvW / 100 + "px"`
(rem y NO em: los em se componen en cascada y el mockup anida 4 niveles)
`aspect-ratio: 16/9` -> alto calculado en JS
`display: grid` + `fr` -> `display: flex` + `flex: 0 0 45%` / `flex: 0 0 55%` + `min-width: 0`
`gap: Xcqw` -> `margin` explicito en los hijos
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Conversion de compatibilidad del CSS y del HTML</name>
  <files>el-templo-admin/src/tv/styles.css, el-templo-admin/src/tv/index.html</files>
  <read_first>
    - .planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-tv-mockup-template.html (fuente visual: el `<style>` completo con todas las medidas en cqw)
    - .planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-UI-SPEC.md (tokens de color, tipografias y layout 45/55)
    - .planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-RESEARCH.md (Pitfall 4, Pitfall 6, Pattern 7)
    - el-templo-admin/src/tv/styles.css (estado actual: copia literal del mockup, del plan 164-04)
  </read_first>
  <action>
    Reescribir `src/tv/styles.css` para que sea equivalente visual del mockup v8 sin cruzar los
    umbrales del bloque `<interfaces>`:
    - Reemplazar TODAS las medidas `Xcqw` por `Xrem` (el factor es identico: 1cqw = 1% del ancho del
      marco = 1rem con la raiz que fija `scale.ts`).
    - `#tv`: sacar `container-type: size` y `aspect-ratio: 16/9`; el ancho y alto los fija JS.
    - `.stage` (2 columnas 45/55): `display: flex` + `flex: 0 0 45%` / `flex: 0 0 55%` +
      `min-width: 0` en ambas columnas. Igual para `.tvWrap` y `.topbar` (que hoy son grid).
    - Todo `gap:` desaparece: separacion con `margin-right`/`margin-bottom` explicitos en los hijos
      (el `gap` en flexbox es Chromium 84 — mas nuevo que grid, es la trampa facil de pasar por alto).
    - Tokens de color como custom properties (`--cream: #F2EBE1`, `--navy: #24364A`,
      `--gold: #B08D6E`, `--sand: #DBCAB4`, `--border-muted: #c5b9a8`) — soportadas desde Chromium 49.
    - Clase `.glyph` para α Δ Σ con stack de sistema (`"Segoe UI", Arial, "Noto Sans", sans-serif`),
      NUNCA dentro de un elemento con `font-family: Cinzel`: el propio PDF cambia de fuente para los
      simbolos griegos. El ☉ de kairos se dibuja con CSS puro (circulo con `border` dorado +
      punto central via `::after`, `border-radius: 50%`), no como caracter U+2609.
    - `@font-face` apuntando a `fonts/cinzel-regular.ttf`, `fonts/cinzel-bold.ttf`,
      `fonts/nunito-regular.ttf`, `fonts/nunito-bold.ttf` con `format("truetype")`.
    - Estilos para las tres pantallas alternativas del kiosco: `#pantallaReposo` (reloj gigante
      Cinzel + logo + quote, D-06), `#pantallaCierre` (idem, D-08) y `#pantallaPairing` (codigo de 6
      chars gigante + instruccion), todas `display: none` por defecto.
    En `index.html`: quitar cualquier atributo/estilo inline que dependa de features prohibidas
    (el mockup tiene un `style="display:inline-flex;gap:0.4cqw"` en el contenedor de dots) y
    conservar todos los `id` existentes.
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && node -e "const fs=require('fs');let c=fs.readFileSync('src/tv/styles.css','utf8').replace(/\/\*[\s\S]*?\*\//g,'');let h=fs.readFileSync('src/tv/index.html','utf8').replace(/<!--[\s\S]*?-->/g,'');const bad=['cqw','cqh','container-type','@container','aspect-ratio','display:grid','display: grid','gap:','gap :'];bad.forEach(function(t){if(c.indexOf(t)>=0)throw new Error('CSS usa feature prohibida: '+t);if(h.indexOf(t)>=0)throw new Error('HTML usa feature prohibida: '+t)});if(c.indexOf('data:font')>=0)throw new Error('fuentes inline base64');console.log('CSS_COMPAT_OK')" && node scripts/build-tv.mjs</automated>
  </verify>
  <acceptance_criteria>
    - El comando de verify (que quita comentarios antes de buscar) no encuentra `cqw`, `cqh`, `container-type`, `@container`, `aspect-ratio`, `display:grid` ni `gap:` en `styles.css` ni en `index.html`
    - `grep -c "flex: 0 0 45%" src/tv/styles.css` y `grep -c "flex: 0 0 55%" src/tv/styles.css` devuelven `1` cada uno
    - `grep -c "min-width: 0" src/tv/styles.css` >= 2
    - `grep -c "\.glyph" src/tv/styles.css` >= 1 y `grep -c "☉" src/tv/styles.css` devuelve `0` (el glifo se dibuja, no se escribe)
    - `grep -c "pantallaReposo" src/tv/styles.css` >= 1, idem `pantallaCierre` y `pantallaPairing`
    - `node scripts/build-tv.mjs` sigue verde
  </acceptance_criteria>
  <done>El CSS del kiosco reproduce el mockup v8 sin ninguna feature posterior a Chromium 53.</done>
</task>

<task type="auto">
  <name>Task 2: scale.ts — marco 16:9 y escalado tipografico por JS</name>
  <files>el-templo-admin/src/tv/scale.ts</files>
  <read_first>
    - .planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-RESEARCH.md (Pattern 7 y el snippet "Escalado + reloj sin dependencias")
    - el-templo-admin/src/tv/main.ts (dispatcher creado en el plan 164-04)
    - el-templo-admin/src/tv/styles.css (medidas en rem tras el task 1)
  </read_first>
  <action>
    Implementar `scaleTv(): void` en `src/tv/scale.ts`:
    calcular `tvW = Math.min(window.innerWidth, window.innerHeight * 16 / 9)`, setear
    `el.style.width = tvW + "px"` y `el.style.height = (tvW * 9 / 16) + "px"` sobre `#tv`
    (reemplaza `aspect-ratio`), y
    `document.documentElement.style.fontSize = (tvW / 100) + "px"` (reemplaza `container-type: size`
    y hace que cada `rem` valga exactamente lo que valia un `cqw` en el mockup).
    Registrar `window.addEventListener("resize", scaleTv)` desde `main.ts` — NUNCA `ResizeObserver`
    (Chromium 64). Guard: si `#tv` no existe, salir sin tirar.
    Exportar tambien `pad2(n: number): string` implementado a mano (`n < 10 ? "0" + n : String(n)`)
    porque `String.prototype.padStart` es ES2017 y revienta en webOS 4.x — el `lib: es2015` del
    tsconfig ya lo prohibe en compilacion, pero el helper tiene que existir en algun lado.
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && npx tsc -p src/tv/tsconfig.tv.json --noEmit && grep -c "ResizeObserver" src/tv/scale.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -p src/tv/tsconfig.tv.json --noEmit` limpio
    - `grep -c "ResizeObserver" src/tv/scale.ts` devuelve `0`
    - `grep -c "padStart" src/tv/*.ts` devuelve `0`
    - `scale.ts` exporta `scaleTv` y `pad2`
    - `main.ts` llama `scaleTv()` una vez y registra el listener de `resize` (verificable con `grep -c "addEventListener(\"resize\"" src/tv/main.ts` = 1)
  </acceptance_criteria>
  <done>El marco 16:9 y toda la tipografia escalan con el ancho disponible sin depender de features modernas.</done>
</task>

<task type="auto">
  <name>Task 3: diag.ts — pantalla de diagnostico ?diag=1</name>
  <files>el-templo-admin/src/tv/diag.ts</files>
  <read_first>
    - .planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-RESEARCH.md (seccion "Environment Availability": que tiene que imprimir el diag)
    - el-templo-admin/src/tv/logger.ts (buffer circular de logs creado en el plan 164-04)
    - el-templo-admin/src/tv/main.ts (dispatcher)
  </read_first>
  <action>
    Implementar `renderDiag(): void` en `src/tv/diag.ts`: reemplaza el contenido de `#tv` por una
    lista legible A 4 METROS (texto grande, alto contraste) con:
    - `navigator.userAgent` completo (identifica Tizen/webOS y su Chromium)
    - `window.innerWidth` x `window.innerHeight` y el `tvW` resultante
    - feature-detects (cada uno con OK/FALTA): `typeof window.fetch`, `typeof Promise`,
      `"padStart" in String.prototype`, `typeof Object.entries`, `typeof ResizeObserver`,
      `CSS && CSS.supports && CSS.supports("container-type", "size")`,
      `CSS.supports("aspect-ratio", "16/9")`, `CSS.supports("display", "grid")`,
      `typeof (window as unknown as { AudioContext?: unknown }).AudioContext`
    - `window.__TV_VERSION__` y el contenido de `/tv/version.txt`
    - si hay device token en `localStorage`: mostrar solo los ULTIMOS 4 caracteres, nunca el token
      completo (alguien puede fotografiar la pantalla)
    - resultado de un GET de prueba a `/api/tv/state` (status HTTP + latencia en ms), sin volcar el
      payload
    - las ultimas 20 lineas del buffer de `logger.ts`
    Escribir SIEMPRE con `textContent` / `createElement`, nunca `innerHTML` con datos externos
    (XSS + Pitfall 13). El modo diag no arranca el poll ni el video.
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && npx tsc -p src/tv/tsconfig.tv.json --noEmit && node scripts/build-tv.mjs && node -e "const h=require('fs').readFileSync('public/tv/index.html','utf8'); if(h.indexOf('userAgent')<0) throw new Error('diag no quedo en el bundle'); console.log('DIAG_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -p src/tv/tsconfig.tv.json --noEmit` limpio
    - `grep -c "innerHTML" src/tv/diag.ts` devuelve `0`
    - `diag.ts` imprime al menos: userAgent, dimensiones, 8 feature-detects, version, latencia del poll
    - El token de localStorage aparece truncado (`grep -c "slice(-4)" src/tv/diag.ts` >= 1)
    - El bundle generado contiene el codigo del diag (verificado por el comando node del verify)
  </acceptance_criteria>
  <done>`/tv/?diag=1` da, en la pantalla del televisor, todos los datos necesarios para diagnosticar sin devtools.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                  |
| ---------------------------------------- | ---------------------------------------------------------------------------- |
| pantalla del TV -> cualquiera en la sala | Todo lo que renderiza el kiosco es publico, incluido el modo diag            |
| datos del API -> DOM del kiosco          | Nombres de ejercicios y prescripciones vienen de la DB (editables por staff) |

## STRIDE Threat Register

| Threat ID | Category               | Component                                          | Disposition | Mitigation Plan                                                                                                                                                                     |
| --------- | ---------------------- | -------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-164-25  | Tampering              | XSS en el kiosco por datos de la DB                | mitigate    | Regla dura para todo `src/tv/`: `textContent` / `createElement`, jamas `innerHTML` con datos del API. Criterio de aceptacion lo verifica en `diag.ts` y se repite en el plan 164-11 |
| T-164-26  | Information Disclosure | `?diag=1` expone el device token en pantalla       | mitigate    | El diag muestra solo los ultimos 4 chars del token y nunca el payload del poll                                                                                                      |
| T-164-27  | Denial of Service      | Pantalla en blanco por feature no soportada (D-20) | mitigate    | Layout compatible escrito una sola vez (sin `@supports`) + gate automatico que rechaza features prohibidas + `?diag=1` como instrumento de campo                                    |
| T-164-SC  | Tampering              | npm/pnpm installs                                  | mitigate    | Cero paquetes nuevos. Cualquier `pnpm add` requiere checkpoint humano bloqueante (C-08)                                                                                             |

</threat_model>

<verification>
- Gate automatico de CSS/HTML sin features prohibidas
- `npx tsc -p src/tv/tsconfig.tv.json --noEmit` limpio
- `node scripts/build-tv.mjs` verde
- Abrir `el-templo-admin/public/tv/index.html` en un browser local y comprobar que el marco 16:9 y la tipografia escalan al redimensionar
</verification>

<success_criteria>

- El CSS del kiosco no cruza ningun umbral de la matriz de motores
- El escalado reproduce el mockup v8 a cualquier tamaño de ventana
- `?diag=1` funciona sin arrancar el poll
  </success_criteria>

<output>
Create `.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-06-SUMMARY.md` when done
</output>
