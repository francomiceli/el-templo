---
phase: 164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/tv/types.ts
  - el-templo-api/src/modules/tv/timer-spec.ts
  - el-templo-api/src/modules/tv/timer-phase.ts
  - el-templo-api/test/tv/tv-timer-spec.test.ts
  - el-templo-api/test/tv/tv-timer-phase.test.ts
  - el-templo-api/test/tv/__fixtures__/timer-vectors.json
autonomous: true
requirements: [D-16, D-17, D-18, D-23, D-24]
must_haves:
  truths:
    - "Existe UN contrato tipado del poll y del control que TV, API y admin comparten (sin caceria de contratos en waves posteriores)"
    - "Las 50 variantes de FormatParams mapean a un TimerSpec de 4 formas con cierre exhaustivo (Pattern 5)"
    - "El calculo de fase/ronda/digitos vive en el API y esta cubierto por vitest — el TV solo porta la funcion"
    - "Existe un archivo de vectores dorados que el kiosco corre en el TV real (unica verificacion automatica sobre hardware)"
  artifacts:
    - path: "el-templo-api/src/modules/tv/types.ts"
      provides: "TimerSpec, TimerFrame, TvPollResponse, TvControlContext, TvStateWrite"
      exports:
        [
          "TimerSpec",
          "TimerFrame",
          "TvPollResponse",
          "TvControlContext",
          "TvStateWrite",
        ]
    - path: "el-templo-api/src/modules/tv/timer-spec.ts"
      provides: "toTimerSpec(params) -> TimerSpec"
      exports: ["toTimerSpec"]
    - path: "el-templo-api/src/modules/tv/timer-phase.ts"
      provides: "phaseAt(elapsedMs, spec) -> TimerFrame"
      exports: ["phaseAt"]
    - path: "el-templo-api/test/tv/__fixtures__/timer-vectors.json"
      provides: "Vectores dorados params -> spec -> frames para el selftest del kiosco"
  key_links:
    - from: "el-templo-api/src/modules/tv/timer-spec.ts"
      to: "el-templo-api/src/modules/admin/format-params.ts"
      via: "import type FormatParams + switch exhaustivo"
      pattern: "FormatParams"
    - from: "el-templo-api/test/tv/tv-timer-phase.test.ts"
      to: "el-templo-api/test/tv/__fixtures__/timer-vectors.json"
      via: "emision + assert del fixture"
      pattern: "timer-vectors"
---

<objective>
El contrato de datos de toda la fase y las dos funciones puras del timer, del lado del API (unico lugar del repo donde corren tests automaticos de esta clase de logica).

Purpose: el TV no puede conocer las 50 variantes de `FormatParams` ni cargar logica sin cobertura (el admin no tiene test runner). Este plan mueve la normalizacion y la aritmetica del timer al API y publica vectores dorados para que el kiosco se autoverifique en el TV real.
Output: `modules/tv/types.ts` (contrato), `timer-spec.ts`, `timer-phase.ts`, tests unitarios y `test/tv/__fixtures__/timer-vectors.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-CONTEXT.md
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-RESEARCH.md
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-UI-SPEC.md
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-PATTERNS.md
@CLAUDE.md

<interfaces>
Contrato que este plan DEBE publicar tal cual (los planes 04/05/08/10/11/12 compilan contra estos
nombres — cambiarlos despues obliga a tocar 6 planes):

TimerSpec = union de 4 formas:
{ kind: "work_rest"; workMs: number; restMs: number; rounds: number }
{ kind: "interval"; intervalMs: number; rounds: number }
{ kind: "countdown"; totalMs: number }
{ kind: "countup" }

TvTimerPhase = "work" | "rest" | "run" | "done"
TimerFrame = { phase: TvTimerPhase; displayMs: number; round: number; totalRounds: number; progress: number; finished: boolean }
TvTimerStatus = "idle" | "running" | "paused"
TvScreen = "idle" | "class" | "closing"
TvClassMode = "regular" | "rom"

TvBlockSummary = { role: string; title: string; shared: boolean }
TvExercise = { name: string; rx: string; videoUrl: string | null }
TvTimerState = { spec: TimerSpec; status: TvTimerStatus; startedAt: number | null; pausedAt: number | null; pausedAccumMs: number; soundEnabled: boolean }
TvClassPayload = { mode: TvClassMode; levels: string[]; level: string; levelLabel: string;
blocks: TvBlockSummary[]; blockRole: string; blockIndex: number; title: string;
listHeader: string; mobilityLine: string | null; exercises: TvExercise[];
exerciseIndex: number; timer: TvTimerState }
TvPollResponse = { serverNow: number; branch: { name: string; utcOffsetMinutes: number; dateLabel: string };
screen: TvScreen; class: TvClassPayload | null }

TvControlBlock = TvBlockSummary & { exerciseCountByLevel: Record<string, number> }
TvControlState = { screen: TvScreen; blockRole: string; level: string; exerciseIndex: number;
timerStatus: TvTimerStatus; timerStartedAt: number | null; pausedAt: number | null;
pausedAccumMs: number; soundEnabled: boolean }
TvControlContext = { branch: { id: number; name: string }; sessionApproved: boolean; mode: TvClassMode;
levels: string[]; blocks: TvControlBlock[]; state: TvControlState | null }
TvStateWrite = { branchId: number; screen?: TvScreen; blockRole?: string; level?: string;
exerciseIndex?: number; timer?: "start" | "pause" | "resume" | "reset"; soundEnabled?: boolean }

Decisiones de contrato de este plan (area "Claude's Discretion" del CONTEXT):

- La quote NO viaja en el poll. D-06/D-08 piden las frases del PDF, que viven en el admin
  (`session-pdf-builder.ts`); el kiosco las inlinea en build (plan 164-04). Duplicarlas en el API
  seria una tercera copia sin necesidad.
- El reloj del TV se calcula con `serverNow + utcOffsetMinutes` y metodos `getUTC*` — nunca con
  `Intl.DateTimeFormat({ timeZone })`, que puede venir con ICU reducida en un TV.
- `blockIndex` es DERIVADO del roster, nunca persistido (Pitfall 1).
- `timer` en la escritura es un comando idempotente de 4 valores (D-18: sin saltar rondas).
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Contrato tipado del modulo tv (types.ts)</name>
  <files>el-templo-api/src/modules/tv/types.ts</files>
  <read_first>
    - .planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-UI-SPEC.md (layout: que dato necesita cada zona de la pantalla)
    - .planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-RESEARCH.md (seccion "Contrato del poll" en Code Examples)
    - el-templo-api/src/modules/coach/types.ts (estilo de archivo de tipos del repo)
  </read_first>
  <action>
    Crear `el-templo-api/src/modules/tv/types.ts` con EXACTAMENTE las uniones e interfaces listadas
    en el bloque `<interfaces>` de este plan, sin agregar ni renombrar campos. Cada interfaz lleva
    un docblock corto citando la decision que la justifica: `screen` -> D-06/D-08/D-09,
    `level`/`blockRole` -> D-15 + Pitfall 1, `pausedAt`/`pausedAccumMs` -> D-17, `soundEnabled` ->
    D-19, `mode`/`levels` -> D-23, `serverNow` -> Pattern 6, `timer` de TvStateWrite -> D-18.
    Prohibido `any` (C-03). El archivo no importa drizzle ni fastify: es contrato puro.
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-api && npx tsc --noEmit && grep -c "export interface TvPollResponse" src/modules/tv/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` limpio en el-templo-api
    - `grep -nE ':\s*any\b' src/modules/tv/types.ts` no devuelve ninguna linea
    - Estan exportados: `TimerSpec`, `TimerFrame`, `TvTimerPhase`, `TvTimerStatus`, `TvScreen`, `TvClassMode`, `TvBlockSummary`, `TvExercise`, `TvTimerState`, `TvClassPayload`, `TvPollResponse`, `TvControlBlock`, `TvControlState`, `TvControlContext`, `TvStateWrite`
    - `grep -ci "quote" src/modules/tv/types.ts` devuelve `0` (la quote la resuelve el kiosco)
  </acceptance_criteria>
  <done>El contrato compila y expone los 15 simbolos que consumen los planes 04/05/08/10/11/12.</done>
</task>

<task type="auto">
  <name>Task 2: toTimerSpec — normalizar las 50 variantes de FormatParams</name>
  <files>el-templo-api/src/modules/tv/timer-spec.ts, el-templo-api/test/tv/tv-timer-spec.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/admin/format-params.ts (la union discriminada COMPLETA y el cierre exhaustivo de `formatParamsLabel`, ~linea 710)
    - el-templo-api/src/modules/tv/types.ts (TimerSpec — creado en el task 1)
    - el-templo-api/test/rbac-sets.test.ts (estructura de test unitario puro: sin createTestApp, sin MySQL)
    - .planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-RESEARCH.md (Pattern 5 — mapeos no obvios)
  </read_first>
  <action>
    Crear `toTimerSpec(params: FormatParams | null): TimerSpec` en
    `el-templo-api/src/modules/tv/timer-spec.ts`, con `switch (params.type)` y cierre
    `default: { const _exhaustive: never = params; ... }` para que agregar un formato al catalogo
    ROMPA la compilacion en vez de degradar en silencio.

    Mapeos obligatorios (agrupar con fall-through de `case` donde la forma es identica — DRY):
    - `tabata` / `interval` / `hiit` (workSeconds, restSeconds, rounds) -> `work_rest` en ms.
    - `emom` / `every_x_seconds` (intervalSeconds, totalMinutes) -> `interval` con
      `rounds = Math.max(1, Math.round(totalMinutes * 60 / intervalSeconds))`.
    - `on_the_x` (intervalSeconds, rounds) -> `interval` directo.
    - `amrap` / `amrap_series` / `time_cap` (minutes) -> `countdown` con `totalMs = minutes * 60000`.
    - `rom` (rounds, restSeconds) -> `work_rest` con `workMs = 0` (trabajo libre, descanso pautado).
      Es el unico caso ambiguo del RESEARCH: documentar la eleccion en un comentario.
    - `death_by`, `death_by_unbroken`, `for_time`, `for_max_reps`, `for_max_distancia`,
      `rounds_for_time` y cualquier otro con `timeCapMinutes` OPCIONAL -> `countdown` si viene el
      cap, `countup` si no.
    - `params === null` y todo lo demas (standard, chipper, ...) -> `countup`.
    Clampear: nunca ms negativos, nunca `rounds < 1` en los kinds con rondas.

    Test `test/tv/tv-timer-spec.test.ts` (unitario puro): un `it()` por familia + un test que
    recorre TODOS los `type` de la union (lista literal derivada de `format-params.ts`) y afirma
    que ninguno tira ni produce un `kind` fuera de las 4 formas.

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-api && npx tsc --noEmit && pnpm vitest run test/tv/tv-timer-spec.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run test/tv/tv-timer-spec.test.ts` verde
    - `grep -c "_exhaustive: never" src/modules/tv/timer-spec.ts` devuelve `1`
    - El test cubre explicitamente: tabata, interval, hiit, emom, every_x_seconds, on_the_x, amrap, amrap_series, time_cap, rom, death_by (con y sin cap), for_time (con y sin cap), standard/null
    - `npx tsc --noEmit` limpio
  </acceptance_criteria>
  <done>Cualquier `formatParams` real de `session_blocks` produce un `TimerSpec` de una de las 4 formas, con cobertura de vitest.</done>
</task>

<task type="auto">
  <name>Task 3: phaseAt + emision de vectores dorados</name>
  <files>el-templo-api/src/modules/tv/timer-phase.ts, el-templo-api/test/tv/tv-timer-phase.test.ts, el-templo-api/test/tv/__fixtures__/timer-vectors.json</files>
  <read_first>
    - el-templo-api/src/modules/tv/types.ts (TimerFrame, TimerSpec)
    - el-templo-api/src/modules/tv/timer-spec.ts (creado en el task 2)
    - .planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-tv-mockup-template.html (el `<script>` ya implementa el tick de fases/rondas para tabata, emom, amrap y libre: replicar esa semantica de display)
    - .planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-UI-SPEC.md (tabla "Timers por formato", estados TRABAJO / DESCANSO / BLOQUE COMPLETO)
  </read_first>
  <action>
    Crear `phaseAt(elapsedMs: number, spec: TimerSpec): TimerFrame` en
    `el-templo-api/src/modules/tv/timer-phase.ts`. Pura y determinista: sin `Date.now()` adentro.

    Semantica por kind:
    - `work_rest`: ciclo = `workMs + restMs`. `phase = "work"` mientras el offset dentro del ciclo
      es menor que `workMs`, si no `"rest"`. `displayMs` = tiempo RESTANTE de la fase actual.
      `round = floor(elapsed / ciclo) + 1`, `totalRounds = rounds`. Al superar `rounds` ciclos:
      `finished = true`, `phase = "done"`, `displayMs = 0`, `progress = 1`. Caso `workMs === 0`
      (rom): fase siempre `"work"` y `displayMs` cuenta hacia ADELANTE dentro de la ronda.
    - `interval`: igual pero un solo tramo por ronda (`displayMs` = restante del intervalo).
    - `countdown`: `phase = "run"`, `displayMs = max(0, totalMs - elapsed)`, `finished` al llegar a 0.
    - `countup`: `phase = "run"`, `displayMs = elapsed`, `finished = false` siempre, `progress = 0`.
    Guards (Pitfall 8): si `elapsedMs < 0` o `elapsedMs > 24h`, devolver el frame de `elapsedMs = 0`.

    Test `test/tv/tv-timer-phase.test.ts`: bordes exactos (t = 0, 1 ms antes y despues de cada
    transicion, ultima ronda, pasado el final) para los 4 kinds. Ademas el test GENERA
    `test/tv/__fixtures__/timer-vectors.json` con `fs.writeFileSync` y luego lo reafirma contra los
    valores esperados, de modo que el archivo commiteado y el codigo no puedan divergir.
    Formato de cada entrada del array: `name`, `params` (FormatParams de entrada), `spec`
    (salida de `toTimerSpec`), `samples` (array de `{ elapsedMs, phase, displayMs, round,
    totalRounds, finished }`). Minimo 6 vectores (tabata, emom, on_the_x, amrap, for_time sin cap,
    rom) con al menos 4 samples cada uno. Este archivo es el que el kiosco corre en el TV real con
    `?selftest=1` (plan 164-07).

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-api && pnpm vitest run test/tv/tv-timer-phase.test.ts && node -e "const v=require('./test/tv/__fixtures__/timer-vectors.json'); if(v.length<6) throw new Error('pocos vectores'); v.forEach(function(x){ if(!x.spec||!x.samples||x.samples.length<4) throw new Error('vector incompleto: '+x.name) }); console.log('VECTORS_OK', v.length)"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run test/tv/tv-timer-phase.test.ts` verde
    - `test/tv/__fixtures__/timer-vectors.json` existe, tiene >= 6 entradas y cada una >= 4 samples (verificado por el comando node del verify)
    - Cada entrada tiene las claves `name`, `params`, `spec`, `samples`
    - `grep -c "Date.now()" src/modules/tv/timer-phase.ts` devuelve `0` (funcion pura)
    - Hay un test que afirma el guard de `elapsedMs` negativo y otro para `> 24h`
  </acceptance_criteria>
  <done>La aritmetica del timer esta implementada, testeada y exportada como vectores dorados reutilizables por el kiosco.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                                                      |
| ------------------------------------ | ---------------------------------------------------------------------------------------------------------------- |
| catalogo de formatos -> timer del TV | Datos de `session_blocks.format_params` (editables por staff) alimentan la aritmetica que se muestra en pantalla |
| API -> kiosco                        | El contrato de tipos es la unica interfaz entre dos runtimes con capacidades muy distintas                       |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                | Disposition | Mitigation Plan                                                                                                                |
| --------- | ---------------------- | -------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------ |
| T-164-06  | Tampering              | `toTimerSpec` ante params corruptos/desconocidos         | mitigate    | Cierre exhaustivo `never` + fallback `countup` + clamps de ms/rondas: ningun input produce NaN ni ms negativos                 |
| T-164-07  | Denial of Service      | `phaseAt` con `elapsedMs` absurdo (reloj del TV corrido) | mitigate    | Guard explicito: `elapsedMs < 0` o `> 24h` colapsa al frame de t=0 en vez de renderizar basura (Pitfall 8)                     |
| T-164-08  | Information Disclosure | `TvPollResponse`                                         | mitigate    | El contrato no incluye NINGUN dato de socios: solo ejercicios, prescripciones y estado (revisado campo por campo en el task 1) |
| T-164-SC  | Tampering              | npm/pnpm installs                                        | mitigate    | Cero paquetes nuevos en este plan. Cualquier `pnpm add` requiere checkpoint humano bloqueante (C-08)                           |

</threat_model>

<verification>
- `cd el-templo-api && npx tsc --noEmit` limpio
- `pnpm vitest run test/tv/tv-timer-spec.test.ts test/tv/tv-timer-phase.test.ts` verde
- `test/tv/__fixtures__/timer-vectors.json` commiteado
</verification>

<success_criteria>

- El contrato `types.ts` existe con los 15 simbolos y sin `any`
- `toTimerSpec` es exhaustivo sobre `FormatParams`
- `phaseAt` cubre los 4 kinds con bordes testeados
- Los vectores dorados existen y son consumibles por el kiosco
  </success_criteria>

<output>
Create `.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-02-SUMMARY.md` when done
</output>
