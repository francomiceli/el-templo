---
phase: 164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-
plan: 13
type: execute
wave: 7
depends_on: ["164-11", "164-12"]
files_modified:
  - deploy/TV-KIOSK-RUNBOOK.md
autonomous: false
requirements: [D-20, D-21, D-22, D-24]
must_haves:
  truths:
    - "Existe un runbook corto de setup del kiosco en el repo: URL, fullscreen, evitar que la pantalla se apague, beeps, como re-vincular, como diagnosticar (D-21)"
    - "Alguien confirmo, en un televisor REAL de una sede, que /tv/ carga y renderiza (D-20 — no es verificable desde esta maquina)"
    - "Alguien confirmo que https://admin-staging.eltemplo.org/tv/ sirve la pagina del kiosco y no el index del SPA (asuncion A1 del research sobre el nginx real)"
    - "El selftest de vectores pasa EN el televisor de la sede"
  artifacts:
    - path: "deploy/TV-KIOSK-RUNBOOK.md"
      provides: "Runbook operativo del kiosco (D-21)"
      contains: "?diag=1"
  key_links:
    - from: "deploy/TV-KIOSK-RUNBOOK.md"
      to: "/tv/ y /tv/?diag=1"
      via: "instrucciones de operacion y diagnostico"
      pattern: "diag=1"
---

<objective>
Cerrar la fase con lo unico que no se puede automatizar: el runbook de operacion del kiosco y la verificacion humana en hardware real.

Purpose: D-20 es una restriccion dura ("tiene que funcionar si o si en el browser que traiga el TV") y no hay ningun smart TV disponible para probar durante el desarrollo. Sin este checkpoint, la fase se puede dar por terminada y descubrirse rota en la sede, sin devtools para diagnosticar.
Output: `deploy/TV-KIOSK-RUNBOOK.md` y el reporte del checkpoint humano.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-CONTEXT.md
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-RESEARCH.md
@CLAUDE.md
@.claude/skills/el-templo-change-control/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: deploy/TV-KIOSK-RUNBOOK.md</name>
  <files>deploy/TV-KIOSK-RUNBOOK.md</files>
  <read_first>
    - deploy/RUNBOOK.md lineas 1-40 (formato: titulo, parrafo de proposito, metadatos en negrita con "Last updated", separador, secciones numeradas)
    - .planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-RESEARCH.md (secciones "Kiosco / operacion" y "Environment Availability")
    - .planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-CONTEXT.md (D-19 a D-22)
    - el-templo-admin/src/tv/poll.ts (claves de localStorage y comportamiento ante 401)
  </read_first>
  <action>
    Escribir `deploy/TV-KIOSK-RUNBOOK.md` con el mismo formato que `deploy/RUNBOOK.md` y estas
    secciones numeradas:
    1. **Que es** — una pagina web por sede, 16:9, controlada desde el celular del profe.
    2. **Setup de un TV nuevo** — abrir el browser del televisor en `https://admin.eltemplo.org/tv/`
       (y el equivalente de staging), guardar como marcador / pagina de inicio, poner el browser en
       pantalla completa, anotar el codigo de 6 caracteres que aparece y cargarlo en el admin
       (Televisores -> Vincular, eligiendo la sede). Aclarar que el codigo NO expira (D-02) y que se
       usa una sola vez.
    3. **Evitar que la pantalla se apague** — configuracion del televisor, NO codigo: en Samsung
       *Settings > General > Eco Solution > Auto Power Off* al maximo y *System Manager > Time >
       Screen Saver Time* en off/maximo; en LG los ajustes equivalentes de screensaver y apagado
       automatico. Documentar por que no se usa `navigator.wakeLock` (en webOS la promesa se cuelga:
       ni resuelve ni rechaza).
    4. **Al encender el TV** — el browser nativo no puede abrir una URL automaticamente al arrancar:
       el flujo es encender, abrir el browser y entrar a la ultima pestaña o al marcador. Nota como
       salida operativa conocida (NO implementada, contradice D-20): un Android TV barato con Fully
       Kiosk Browser.
    5. **Beeps** — vienen apagados por default y los activa el profe desde el control (D-19). Si el
       televisor no suena, revisar el volumen del TV y que el navegador permita audio.
    6. **Re-vincular / revocar** — revocar desde el admin deja al TV en 401: el kiosco borra su
       token solo y vuelve a mostrar un codigo nuevo. Si hace falta forzarlo desde el televisor,
       abrir `/tv/` con la URL limpia y, si el browser lo permite, borrar los datos del sitio.
    7. **Diagnostico sin devtools** — `/tv/?diag=1` (user agent, feature-detects, version, latencia
       del poll, ultimas lineas de log) y `/tv/?selftest=1` (vectores del timer, tiene que decir
       PASS). Que mirar primero segun el sintoma: pantalla en blanco, codigo de pairing que no
       avanza, timer desfasado, video que no carga.
    8. **Actualizaciones** — el kiosco compara `version.txt` con la version embebida y recarga solo
       cuando esta en reposo (D-22). Si un TV quedo viejo, alcanza con dejarlo en reposo un minuto.
    9. **Deploy** — advertencia operativa: el workflow usa `paths-filter` contra `event.before`, asi
       que un push que solo toca el API no reconstruye el admin y `/tv/` no se actualiza. Al
       shippear cambios del kiosco, tocar los paths del admin o usar `workflow_dispatch`.
    10. **Prerequisito por sede** — sin wifi no hay kiosco (Moreno pendiente).
    Escribir en castellano, tono operativo, sin jerga innecesaria: lo va a leer alguien de pie
    frente a un televisor.
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo && test -f deploy/TV-KIOSK-RUNBOOK.md && grep -c "diag=1" deploy/TV-KIOSK-RUNBOOK.md && grep -c "selftest=1" deploy/TV-KIOSK-RUNBOOK.md && grep -c "paths-filter" deploy/TV-KIOSK-RUNBOOK.md && grep -ci "screen saver\|screensaver" deploy/TV-KIOSK-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - `deploy/TV-KIOSK-RUNBOOK.md` existe con las 10 secciones numeradas
    - Menciona `?diag=1`, `?selftest=1`, `paths-filter` y la configuracion de screensaver (verificado por los greps del verify)
    - Explica por que NO se usa `navigator.wakeLock`
    - Incluye el bloque de metadatos con **Last updated** al estilo de `deploy/RUNBOOK.md`
  </acceptance_criteria>
  <done>Alguien que nunca vio el sistema puede poner en marcha un televisor nuevo siguiendo el documento.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [CHECKPOINT] Verificacion en staging y en un televisor real</name>
  <files>(ninguno — verificacion en staging y en hardware real)</files>
  <read_first>
    - deploy/TV-KIOSK-RUNBOOK.md (escrito en el task 1: es el guion que se prueba en la sede)
    - .planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-RESEARCH.md (asuncion A1 sobre el nginx real y A2 sobre el año de los TVs)
    - .claude/skills/el-templo-change-control/SKILL.md (seccion 5: cada push se confirma con el usuario en el momento)
  </read_first>
  <action>
    Preparar y acompanar la verificacion humana. Concretamente: correr el build completo local
    (`cd el-templo-admin && pnpm build:tv` y `cd el-templo-api && npx tsc --noEmit`), resumir para
    el usuario que se va a verificar y por que no se puede automatizar, PEDIR OK para pushear a
    staging asegurando que el push toque los paths del admin (o disparar `workflow_dispatch`), y
    una vez verde el deploy, guiar los pasos de la seccion how-to-verify y registrar textualmente
    lo reportado (user agent del televisor, feature-detects de `?diag=1`, resultado de
    `?selftest=1`) en el SUMMARY. No dar la fase por terminada sin ese reporte.
  </action>
  <what-built>
    La fase completa: modulo `tv` en el API (vinculacion, poll, control), la pagina estatica del
    kiosco en `el-templo-admin/public/tv/` (generada en build, compilada a ES2015, CSS y JS inline)
    y las dos pantallas del admin (`/tv/devices` y `/tv/control`). Todo verificado con tests de
    integracion contra MySQL real y con el selftest de vectores en un browser de escritorio.
    Lo que NO se pudo verificar desde esta maquina: que la pagina cargue en el browser nativo de un
    smart TV (D-20) y que el nginx real del EC2 sirva `/tv/` como la pagina del kiosco y no como el
    index del SPA (asuncion A1 del research).
  </what-built>
  <how-to-verify>
    1. **Pedir OK para pushear a staging** (regla de change-control: cada push se confirma en el
       momento). Al pushear, asegurarse de que el push toque los paths del admin, o disparar
       `workflow_dispatch`, o el `/tv/` nuevo no se construye (trampa de `paths-filter`).
    2. Esperar CI verde y el deploy de staging.
    3. Desde una computadora, abrir `https://admin-staging.eltemplo.org/tv/`.
       Esperado: la pantalla del kiosco con marmol, logo, reloj y un codigo de 6 caracteres.
       Si aparece el login del admin, nginx no esta resolviendo el directorio -> reportar y frenar.
    4. Abrir `https://admin-staging.eltemplo.org/tv/?selftest=1`. Esperado: **PASS n/n** en verde.
    5. **En el televisor de una sede** (el paso que solo puede hacer una persona): abrir el browser
       del TV en `https://admin-staging.eltemplo.org/tv/` y reportar:
       (a) si carga o queda en blanco;
       (b) que muestra `https://admin-staging.eltemplo.org/tv/?diag=1` — copiar/fotografiar el user
           agent y la lista de feature-detects;
       (c) si `?selftest=1` dice PASS;
       (d) si el layout se ve completo (nada cortado ni desbordado) y el texto es legible a 4 m.
    6. Vincular ese televisor desde `/tv/devices` con un usuario coach, e iniciar una clase desde
       `/tv/control`. Verificar en la pared: cambio de bloque, cambio de nivel, cambio de ejercicio
       (la lista y el video se mueven juntos), iniciar/pausar/reanudar el timer, activar sonido,
       pantalla de cierre y terminar clase. Cada cambio deberia verse en menos de 3 segundos.
     7. Dejar el TV encendido en reposo un rato y confirmar que el reloj sigue con segundero y que
        la pantalla no se apaga con la configuracion del runbook.
  </how-to-verify>
  <verify>
    <automated>MISSING — no hay smart TV disponible en el entorno de desarrollo; la verificacion de D-20 es manual por definicion. El sustituto automatico mas cercano ya existe y corre EN el televisor: `/tv/?selftest=1` (vectores dorados del plan 164-02).</automated>
    <human-check>El usuario reporta: carga o no carga en el TV real, salida de `?diag=1`, resultado de `?selftest=1`, y si el circuito completo de control funciona desde el celular.</human-check>
  </verify>
  <acceptance_criteria>
    - `https://admin-staging.eltemplo.org/tv/` devuelve la pagina del kiosco (no el SPA)
    - `?selftest=1` reporta PASS en el televisor real
    - El usuario confirma que el layout se ve completo y legible a 4 m
    - El circuito control -> TV responde en menos de 3 s en cada accion
    - Si algo falla, queda registrado el user agent del TV y la lista de feature-detects para acotar el arreglo
  </acceptance_criteria>
  <done>El usuario confirmo que /tv/ carga y opera en un televisor real de una sede, o reporto el fallo con user agent y feature-detects.</done>
  <resume-signal>Escribi "aprobado" o describi que fallo (pegando la salida de ?diag=1 si hubo pantalla en blanco)</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                           |
| -------------------------------- | --------------------------------------------------------------------- |
| repo -> staging/produccion       | El push dispara un deploy real; staging y prod comparten host y MySQL |
| televisor de la sede -> internet | El kiosco queda encendido en un espacio publico, sin supervision      |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                        | Disposition | Mitigation Plan                                                                                                                         |
| --------- | ---------------------- | ---------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| T-164-55  | Denial of Service      | `/tv/` sirviendo el index del SPA en el nginx real (asuncion A1) | mitigate    | Verificacion explicita en staging ANTES de dar la fase por terminada; si falla, la correccion es de nginx (SSH = gate humano)           |
| T-164-56  | Denial of Service      | Pantalla en blanco en el TV real (D-20)                          | mitigate    | Checkpoint humano bloqueante + `?diag=1` para capturar user agent y feature-detects sin devtools                                        |
| T-164-57  | Elevation of Privilege | Deploy accidental a produccion                                   | mitigate    | Staging-first estricto: el push se pide y se confirma en el momento, y master solo con OK explicito del usuario                         |
| T-164-58  | Information Disclosure | Televisor encendido mostrando datos internos                     | accept      | El payload del kiosco no contiene datos de socios (verificado en el plan 164-05); lo que se ve es la misma plani que ve cualquier socio |
| T-164-SC  | Tampering              | npm/pnpm installs                                                | mitigate    | Cero paquetes nuevos en toda la fase. Cualquier `pnpm add` requiere checkpoint humano bloqueante (C-08)                                 |

</threat_model>

<verification>
- `deploy/TV-KIOSK-RUNBOOK.md` commiteado
- CI verde en staging
- Reporte humano del televisor real registrado en el SUMMARY (incluido el user agent)
</verification>

<success_criteria>

- El runbook cubre setup, screensaver, beeps, re-vinculacion, diagnostico y deploy
- D-20 esta verificado en hardware real o, si fallo, hay datos concretos (user agent + feature-detects) para acotar el arreglo
  </success_criteria>

<output>
Create `.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-13-SUMMARY.md` when done
</output>
