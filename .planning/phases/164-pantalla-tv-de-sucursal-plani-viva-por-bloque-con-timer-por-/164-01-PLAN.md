---
phase: 164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/schema/tv.ts
  - el-templo-api/src/db/schema/index.ts
  - el-templo-api/src/db/migrations/0189_tv_screen.sql
  - el-templo-api/test/helpers.ts
  - el-templo-api/test/tv/tv-schema.test.ts
autonomous: true
requirements: [D-02, D-03, D-04, D-05, D-07, D-08, D-17, D-19]
must_haves:
  truths:
    - "Existen las tablas tv_devices, tv_pairings y tv_class_state en la DB local y en la DB de tests (D-03/D-04)"
    - "tv_class_state admite una sola fila por sede (D-04: N TVs espejan un unico estado)"
    - "timer_started_at conserva milisegundos en el round-trip (Pitfall 9 — sin esto un tabata de 20s tiene 5% de error)"
    - "Las tablas nuevas se vacian entre archivos de test (Pitfall 11)"
  artifacts:
    - path: "el-templo-api/src/db/schema/tv.ts"
      provides: "Definicion Drizzle de tvDevices / tvPairings / tvClassState"
      contains: 'mysqlTable("tv_devices"'
    - path: "el-templo-api/src/db/migrations/0189_tv_screen.sql"
      provides: "DDL hand-written de las 3 tablas"
      contains: "CREATE TABLE"
    - path: "el-templo-api/test/tv/tv-schema.test.ts"
      provides: "Round-trip de fsp:3 + unicidad por sede"
  key_links:
    - from: "el-templo-api/src/db/schema/index.ts"
      to: "./tv"
      via: "barrel export"
      pattern: "export \\* from \"./tv\""
    - from: "el-templo-api/test/helpers.ts"
      to: "schema.tvClassState / schema.tvDevices / schema.tvPairings"
      via: "TABLES_TO_CLEAN"
      pattern: "schema\\.tvDevices"
---

<objective>
Fundacion de datos de la fase 164: las tres tablas del TV (dispositivos, pairings y estado de clase por sede), su migracion hand-written 0189 y la higiene de tests.

Purpose: todo el modulo `tv` del API se apoya en este schema. Si el numero de migracion colisiona o el `fsp:3` no llega a la DB, la fase entera se construye sobre arena.
Output: `src/db/schema/tv.ts`, `0189_tv_screen.sql` aplicada localmente, `TABLES_TO_CLEAN` actualizado y un test de integracion que prueba precision de milisegundos y unicidad por sede.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-CONTEXT.md
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-RESEARCH.md
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-PATTERNS.md
@CLAUDE.md
@.claude/skills/el-templo-db-migrations/SKILL.md
@.claude/skills/el-templo-change-control/SKILL.md

<interfaces>
Columnas fisicas que el resto de la fase asume (los planes 03/05/08/10 escriben queries contra estos nombres exactos):

tv_devices: id, branch_id, token_hash(64,unique), name, is_active, last_seen_at, revoked_at, paired_by, created_at
tv_pairings: id, user_code(6,unique), device_code_hash(64,unique), branch_id, device_name,
claimed_at, claimed_by, device_id, created_at
tv_class_state: id, branch_id(unique), class_date(date), block_role, level, exercise_index,
screen, timer_status, timer_started_at(3), paused_at(3), paused_accum_ms,
sound_enabled, updated_by, updated_at

NOTA C-07: si alguna de estas columnas se declara con `mysqlEnum`, el PRIMER argumento es el
nombre fisico de la columna y la lista de valores debe coincidir byte a byte con el SQL.
Decision de este plan: se usan `varchar` (no `mysqlEnum`) para `block_role`, `level`,
`timer_status` y `screen`, exactamente como propone el RESEARCH, para no pisar esa trampa.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Verificar base de rama y numero de migracion libre</name>
  <files>(ninguno — verificacion de estado de git)</files>
  <read_first>
    - .claude/skills/el-templo-db-migrations/SKILL.md (seccion "Hand-written migration pattern" + "Shipping out of order")
    - .claude/skills/el-templo-change-control/SKILL.md (secciones 1, 3 y 4)
    - .planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-PATTERNS.md (seccion "Notas de riesgo para el planner", punto 1)
  </read_first>
  <action>
    Correr `git fetch origin`. El checkout de trabajo esta ATRASADO: su maxima migracion local es
    0181 mientras `origin/master` y `origin/staging` estan en 0188. Escribir 0182 desde este
    checkout seria una colision garantizada en `_migrations`.

    Secuencia:
    1. `git status --porcelain` — si el working tree tiene cambios sin commitear que NO son de esta
       fase, ABORTAR y escalar al usuario: el checkout principal es compartido con otras sesiones
       (regla de change-control), no stashear ni descartar nada.
    2. Confirmar el maximo real en ambos remotos:
       `git ls-tree --name-only origin/master el-templo-api/src/db/migrations/ | sort | tail -2`
       `git ls-tree --name-only origin/staging el-templo-api/src/db/migrations/ | sort | tail -2`
       Ambos deben terminar en `0188_bookings_trial_date_index.sql`. Si aparece un numero mayor,
       el numero de esta fase pasa a ser (maximo + 1) y hay que renombrar todas las referencias a
       0189 en los planes 164-01.
    3. Con el working tree limpio, crear y posicionarse en la rama de la fase desde el remoto
       actualizado: `git checkout -b feat/164-tv-sucursal origin/master`.
    4. Verificar que la rama quedo alineada: `git rev-list --count HEAD..origin/master` debe dar 0.

    No pushear nada (staging-first estricto, C-10). No tocar `src/db/migrations/meta/`.

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo && test "$(git rev-list --count HEAD..origin/master)" = "0" && test "$(ls el-templo-api/src/db/migrations/*.sql | sort | tail -1 | xargs basename)" = "0188_bookings_trial_date_index.sql" && echo BASE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-list --count HEAD..origin/master` devuelve exactamente `0`
    - `ls el-templo-api/src/db/migrations/*.sql | sort | tail -1` devuelve `0188_bookings_trial_date_index.sql` (o sea: 0189 esta libre)
    - `git branch --show-current` empieza con `feat/164`
    - No se ejecuto `git stash`, `git reset --hard` ni `git checkout` sobre archivos ajenos a la fase
  </acceptance_criteria>
  <done>La rama de la fase esta basada en origin/master actualizado y 0189 esta confirmado como el proximo numero libre en master Y staging.</done>
</task>

<task type="auto">
  <name>Task 2: Schema Drizzle tv.ts (3 tablas) + barrel</name>
  <files>el-templo-api/src/db/schema/tv.ts, el-templo-api/src/db/schema/index.ts</files>
  <read_first>
    - el-templo-api/src/db/schema/refresh-tokens.ts (analog exacto: token opaco + sha256, docblock de seguridad, index en array-form, bloque relations())
    - el-templo-api/src/db/schema/index.ts (ultimas lineas del barrel)
    - el-templo-api/src/db/schema/branches.ts (columnas id/timezone/country para las FKs)
    - .planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-RESEARCH.md (seccion "Code Examples — Schema de las tablas")
  </read_first>
  <action>
    Crear `el-templo-api/src/db/schema/tv.ts` con tres tablas y sus `relations()`, replicando el
    estilo de `refresh-tokens.ts` (docblock de cabecera que explica POR QUE solo se persiste el
    sha256 del token, imports de `drizzle-orm/mysql-core`, indices en el array-form del segundo
    argumento).

    `tvDevices` -> tabla "tv_devices": id autoincrement PK; branchId int("branch_id") notNull
    references branches.id; tokenHash varchar("token_hash",{length:64}) notNull unique; name
    varchar("name",{length:100}) nullable; isActive boolean("is_active").default(true).notNull();
    lastSeenAt timestamp("last_seen_at") nullable (D-05); revokedAt timestamp("revoked_at")
    nullable (D-03); pairedBy int("paired_by") references users.id nullable; createdAt
    timestamp("created_at").defaultNow().notNull(). Indice `idx_tv_devices_branch` sobre branchId.
    SIN `expires_at`: D-03 dice explicitamente que el token no expira, se revoca por fila.

    `tvPairings` -> tabla "tv_pairings": id PK; userCode varchar("user_code",{length:6}) notNull
    unique (D-02: no expira, queda fijo hasta que se use); deviceCodeHash
    varchar("device_code_hash",{length:64}) notNull unique (secreto del TV, patron RFC 8628 —
    NUNCA se muestra en pantalla); branchId int("branch_id") references branches.id nullable
    (la sede la elige el staff al reclamar, D-01); deviceName varchar("device_name",{length:100})
    nullable; claimedAt timestamp("claimed_at") nullable; claimedBy int("claimed_by") references
    users.id nullable; deviceId int("device_id") references tvDevices.id nullable (se setea recien
    cuando el TV retira su token: el plaintext se emite una sola vez); createdAt defaultNow notNull.

    `tvClassState` -> tabla "tv_class_state": id PK; branchId int("branch_id") notNull references
    branches.id; classDate date("class_date") notNull (D-07 expire-on-read: es una FECHA en la TZ
    de la sede, no un timestamp); blockRole varchar("block_role",{length:20}) notNull (Pitfall 1:
    ROL, nunca indice numerico); level varchar("level",{length:20}) notNull; exerciseIndex
    int("exercise_index").default(0).notNull(); screen varchar("screen",{length:10})
    .default("class").notNull() (valores "class" | "closing", D-08); timerStatus
    varchar("timer_status",{length:10}).default("idle").notNull() (valores "idle" | "running" |
    "paused"); timerStartedAt timestamp("timer_started_at",{ fsp: 3 }) nullable; pausedAt
    timestamp("paused_at",{ fsp: 3 }) nullable (D-17); pausedAccumMs
    int("paused_accum_ms").default(0).notNull() (D-17); soundEnabled
    boolean("sound_enabled").default(false).notNull() (D-19: beeps OFF por default); updatedBy
    int("updated_by") references users.id nullable; updatedAt
    timestamp("updated_at").defaultNow().onUpdateNow().notNull(). `uniqueIndex("uq_tv_class_state_branch")`
    sobre branchId — D-04: un unico estado por sede que espejan N televisores.

    Documentar con comentario que `fsp: 3` es el PRIMER uso en el repo y por que (Pitfall 9: sin
    milisegundos el timer arranca hasta 1s adelantado).

    Agregar `export * from "./tv";` al final de `src/db/schema/index.ts` — sin esto
    `schema.tvDevices` es `undefined` en runtime aunque tsc pase.

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-api && npx tsc --noEmit && grep -c 'export \* from "./tv"' src/db/schema/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` en el-templo-api sale sin errores
    - `grep -c 'export \* from "./tv"' el-templo-api/src/db/schema/index.ts` devuelve `1`
    - `grep -c 'fsp: 3' el-templo-api/src/db/schema/tv.ts` devuelve `2` (timer_started_at y paused_at)
    - `grep -c 'uniqueIndex("uq_tv_class_state_branch")' el-templo-api/src/db/schema/tv.ts` devuelve `1`
    - `grep -c 'expires_at' el-templo-api/src/db/schema/tv.ts` devuelve `0` (D-03)
    - `grep -c 'mysqlEnum' el-templo-api/src/db/schema/tv.ts` devuelve `0` (decision explicita: varchar para evitar la trampa C-07)
  </acceptance_criteria>
  <done>Las tres tablas estan declaradas con los nombres fisicos del bloque `<interfaces>` y exportadas por el barrel.</done>
</task>

<task type="auto">
  <name>Task 3: Migracion 0189 hand-written, aplicacion local, TABLES_TO_CLEAN y test de round-trip</name>
  <files>el-templo-api/src/db/migrations/0189_tv_screen.sql, el-templo-api/test/helpers.ts, el-templo-api/test/tv/tv-schema.test.ts</files>
  <read_first>
    - el-templo-api/src/db/migrations/0188_bookings_trial_date_index.sql (header narrativo, cero `;` en comentarios)
    - el-templo-api/src/db/migrations/0185_users_branch_change_tracking.sql (DDL multi-columna)
    - el-templo-api/src/db/run-migrations.ts (comportamiento de splitSqlStatements — la trampa del `;`)
    - el-templo-api/test/helpers.ts (TABLES_TO_CLEAN, createTestApp, cleanAllTestData)
    - el-templo-api/test/coach/outstanding-balances.test.ts (estructura de un test de integracion: beforeAll/afterAll/beforeEach, sufijos unicos de `code` de sede)
  </read_first>
  <action>
    1. Escribir a mano `el-templo-api/src/db/migrations/0189_tv_screen.sql` con las tres
       `CREATE TABLE` en orden tv_devices -> tv_pairings -> tv_class_state (tv_pairings tiene FK a
       tv_devices). Header `--` explicando: fase 164, por que es hand-written (db:generate roto por
       drift preexistente), y que `timer_started_at`/`paused_at` usan `timestamp(3)` a proposito.
       PROHIBIDO cualquier `;` dentro de los comentarios `--` (rompe el runner Y la provision de la
       DB de tests — incidentes 0119 y 0188). Tipos exactos: `varchar(64) NOT NULL UNIQUE` para los
       hashes, `date NOT NULL` para class_date, `timestamp(3) NULL` para los dos sellos de timer,
       `tinyint(1) NOT NULL DEFAULT 1/0` para los boolean de drizzle, `UNIQUE KEY
       uq_tv_class_state_branch (branch_id)`, `KEY idx_tv_devices_branch (branch_id)`.
       Cross-check obligatorio: los nombres de columna del `.sql` deben coincidir byte a byte con el
       primer argumento de cada helper de `tv.ts`.
    2. Aplicar local: `cd el-templo-api && pnpm db:migrate`. NUNCA `drizzle-kit migrate` ni `db:push`.
    3. Agregar `schema.tvClassState`, `schema.tvPairings`, `schema.tvDevices` a `TABLES_TO_CLEAN` en
       `test/helpers.ts`, en el bloque previo a las "Core entity tables" (antes de branches/users por
       las FKs), con un comentario del estilo del resto: "Fase 164: estado del TV por sede — sin
       limpiar se filtra entre archivos de test".
    4. Crear `test/tv/tv-schema.test.ts` (integracion, MySQL real) con tres casos:
       (a) round-trip de milisegundos: insertar `tvClassState` con `timerStartedAt: new Date(ms)`
           donde `ms % 1000 === 123`, releer y esperar `getTime()` identico (Pitfall 9);
       (b) unicidad por sede: un segundo insert de tv_class_state con el mismo branch_id rechaza
           (D-04);
       (c) unicidad de token: dos tv_devices con el mismo token_hash rechaza.
       El archivo tambien prueba, por el solo hecho de correr, que 0189 parsea: la provision de
       `eltemplo_test_<POOL_ID>` aplica todas las migraciones con el mismo parser del runner.
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-api && grep -v '^--' src/db/migrations/0189_tv_screen.sql | grep -c 'CREATE TABLE' && ! grep -n '^\s*--.*;' src/db/migrations/0189_tv_screen.sql && pnpm db:migrate && pnpm vitest run test/tv/tv-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '^\s*--.*;' el-templo-api/src/db/migrations/0189_tv_screen.sql` no devuelve NINGUNA linea
    - `grep -v '^--' 0189_tv_screen.sql | grep -c 'CREATE TABLE'` devuelve `3`
    - `pnpm db:migrate` termina en exito y `SELECT name FROM _migrations ORDER BY name DESC LIMIT 1` devuelve `0189_tv_screen.sql`
    - `pnpm vitest run test/tv/tv-schema.test.ts` pasa con 3 tests verdes
    - `grep -c 'schema.tvDevices' el-templo-api/test/helpers.ts` es >= 1 y `grep -c 'schema.tvClassState' el-templo-api/test/helpers.ts` es >= 1
    - El `.sql` y `tv.ts` quedan staged en el MISMO commit (regla dura del skill de migraciones)
  </acceptance_criteria>
  <done>0189 aplicada limpia en local, las tablas se limpian entre tests y el test prueba milisegundos + unicidad por sede.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                                                           |
| ----------------------------- | ----------------------------------------------------------------------------------------------------- |
| migracion -> DB de produccion | Todo `.sql` commiteado termina corriendo contra `eltemplo` (prod) cuando staging mergea a master      |
| schema TS -> DB fisica        | Los tipos de TS no validan la DB real: un nombre de columna desalineado pasa tsc y explota en CI/prod |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                | Disposition | Mitigation Plan                                                                                                                                                         |
| --------- | ---------------------- | -------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-164-01  | Tampering              | `0189_tv_screen.sql`                                     | mitigate    | Cero `;` dentro de comentarios `--` (verificado por grep en el gate del task 3): un `;` en comentario parte la migracion y rompe la provision de TODA la suite de tests |
| T-164-02  | Tampering              | numeracion de migracion                                  | mitigate    | Task 1 confirma 0188 como maximo en master Y staging antes de escribir 0189; el checkout local esta en 0181 y escribir 0182 colisionaria en `_migrations`               |
| T-164-03  | Information Disclosure | `tv_devices.token_hash` / `tv_pairings.device_code_hash` | mitigate    | Solo se persiste sha256 hex (varchar 64), nunca el plaintext — patron auditado de la fase 116                                                                           |
| T-164-04  | Elevation of Privilege | `tv_devices` sin expiracion (D-03)                       | mitigate    | Columnas `is_active` + `revoked_at` habilitan revocacion por fila desde el admin (endpoint en el plan 164-03)                                                           |
| T-164-05  | Denial of Service      | `tv_class_state` multiples filas por sede                | mitigate    | `uniqueIndex uq_tv_class_state_branch` — la DB impone el invariante D-04, no el codigo                                                                                  |
| T-164-SC  | Tampering              | npm/pnpm installs                                        | mitigate    | Esta fase NO instala paquetes (RESEARCH: Package Legitimacy Audit vacia por diseño). Cualquier `pnpm add` requiere checkpoint humano bloqueante (C-08)                  |

</threat_model>

<verification>
- `cd el-templo-api && npx tsc --noEmit` limpio
- `pnpm db:migrate` idempotente: correrlo dos veces no falla
- `pnpm vitest run test/tv/tv-schema.test.ts` verde
- `git diff --stat` muestra `tv.ts` y `0189_tv_screen.sql` en el mismo cambio
</verification>

<success_criteria>

- Las 3 tablas existen en la DB local con los nombres fisicos del bloque `<interfaces>`
- `timer_started_at` conserva milisegundos (test verde)
- El estado de clase es unico por sede a nivel DB
- Las tablas nuevas estan en `TABLES_TO_CLEAN`
  </success_criteria>

<output>
Create `.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-01-SUMMARY.md` when done
</output>
