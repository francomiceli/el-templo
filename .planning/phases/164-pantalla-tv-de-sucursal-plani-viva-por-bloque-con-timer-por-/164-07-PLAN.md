---
phase: 164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-
plan: 07
type: execute
wave: 3
depends_on: ["164-04"]
files_modified:
  - el-templo-admin/src/tv/timer.ts
  - el-templo-admin/src/tv/selftest.ts
autonomous: true
requirements: [D-16, D-17, D-18, D-20, D-24]
must_haves:
  truths:
    - "El TV calcula fase, ronda y digitos localmente a partir de un sello de tiempo: el tiempo NO viaja por la red (regla de oro del UI-SPEC)"
    - "La pausa congela y reanuda exacto donde quedo (D-17)"
    - "Iniciar arranca TRABAJO al instante, sin cuenta previa (D-16)"
    - "`/tv/?selftest=1` corre los vectores dorados del API en el televisor real e imprime PASS/FAIL — unica verificacion automatica posible sobre ese hardware"
  artifacts:
    - path: "el-templo-admin/src/tv/timer.ts"
      provides: "phaseAt (port 1:1 del API), elapsedFrom y formatDigits"
      exports: ["phaseAt", "elapsedFrom", "formatDigits"]
    - path: "el-templo-admin/src/tv/selftest.ts"
      provides: "runSelfTest(): corre timer-vectors.json contra phaseAt e imprime el resultado en pantalla"
      exports: ["runSelfTest"]
  key_links:
    - from: "el-templo-admin/src/tv/selftest.ts"
      to: "/tv/timer-vectors.json"
      via: "fetch del fixture copiado por build-tv.mjs"
      pattern: "timer-vectors.json"
    - from: "el-templo-admin/src/tv/timer.ts"
      to: "el-templo-api/src/modules/tv/timer-phase.ts"
      via: "port 1:1 de la funcion pura cubierta por vitest"
      pattern: "phaseAt"
---

<objective>
El motor de tiempo del kiosco: la copia exacta de `phaseAt` del API, el calculo de elapsed con correccion de reloj y pausa, y el autotest de vectores dorados que se puede correr EN el televisor de la sede.

Purpose: el admin no tiene test runner, asi que la unica forma de saber que el timer del TV es correcto sobre hardware real es correr los vectores que el API ya valida con vitest. Ademas, un reloj de TV mal configurado haria mentir al timer si el elapsed no se corrige con el offset del servidor.
Output: `src/tv/timer.ts` y `src/tv/selftest.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-RESEARCH.md
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-UI-SPEC.md
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-CONTEXT.md
@CLAUDE.md

<interfaces>
```
// src/tv/timer.ts — mismos tipos que el contrato del API (copiados, no importados: el kiosco no
// puede importar del backend)
type TimerSpec = { kind: "work_rest"; workMs: number; restMs: number; rounds: number }
               | { kind: "interval"; intervalMs: number; rounds: number }
               | { kind: "countdown"; totalMs: number }
               | { kind: "countup" };
type TimerFrame = { phase: "work"|"rest"|"run"|"done"; displayMs: number; round: number;
                    totalRounds: number; progress: number; finished: boolean };

phaseAt(elapsedMs: number, spec: TimerSpec): TimerFrame // port 1:1 de timer-phase.ts

elapsedFrom(t: { status: "idle"|"running"|"paused"; startedAt: number|null;
pausedAt: number|null; pausedAccumMs: number },
nowCorrected: number): number
// running: nowCorrected - startedAt - pausedAccumMs
// paused : pausedAt - startedAt - pausedAccumMs (congelado, D-17)
// idle : 0

formatDigits(displayMs: number): string // "MM:SS" y "H:MM:SS" si supera la hora, con pad2 propio

```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: timer.ts — port de phaseAt, elapsedFrom y formatDigits</name>
  <files>el-templo-admin/src/tv/timer.ts</files>
  <read_first>
    - el-templo-api/src/modules/tv/timer-phase.ts (la implementacion de referencia, ya cubierta por vitest en el plan 164-02)
    - el-templo-api/src/modules/tv/types.ts (TimerSpec y TimerFrame)
    - el-templo-admin/src/tv/scale.ts (`pad2`, creado en el plan 164-06)
    - .planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-RESEARCH.md (Pattern 6: correccion de reloj)
  </read_first>
  <action>
    Portar `phaseAt` de `el-templo-api/src/modules/tv/timer-phase.ts` a `src/tv/timer.ts` linea por
    linea, con un docblock que diga explicitamente: "port 1:1 de modules/tv/timer-phase.ts —
    cualquier cambio va primero al API (donde hay tests) y despues aca; los vectores dorados
    detectan la divergencia". Los tipos `TimerSpec`/`TimerFrame` se DUPLICAN en este archivo (el
    kiosco no puede importar del backend); documentar esa duplicacion deliberada.

    Agregar `elapsedFrom(timer, nowCorrected)` con la semantica del bloque `<interfaces>`: en
    `paused` el elapsed queda congelado en `pausedAt - startedAt - pausedAccumMs` (D-17: reanuda
    exacto donde quedo); en `idle` es 0. `nowCorrected` lo provee el runtime (plan 164-11) como
    `Date.now() + clockOffsetMs`, nunca `Date.now()` crudo (Pitfall 8).

    Agregar `formatDigits(displayMs)`: `MM:SS` con `pad2` propio, y `H:MM:SS` cuando supera la hora.
    Prohibido `padStart` (ES2017), `Math.trunc` esta OK (ES2015).

    Guards heredados del API: `elapsedMs` negativo o mayor a 24 h colapsa al frame de t = 0.
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && npx tsc -p src/tv/tsconfig.tv.json --noEmit && grep -c "padStart" src/tv/timer.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -p src/tv/tsconfig.tv.json --noEmit` limpio
    - `grep -c "padStart" src/tv/timer.ts` devuelve `0`
    - `timer.ts` exporta `phaseAt`, `elapsedFrom` y `formatDigits`
    - `grep -c "port 1:1" src/tv/timer.ts` >= 1 (docblock de trazabilidad al API)
    - `grep -c "Date.now()" src/tv/timer.ts` devuelve `0` (las funciones son puras: el "ahora" entra por parametro)
  </acceptance_criteria>
  <done>El kiosco puede calcular fase/ronda/digitos de cualquier `TimerSpec` sin pedirle nada a la red.</done>
</task>

<task type="auto">
  <name>Task 2: selftest.ts — vectores dorados corriendo en el televisor</name>
  <files>el-templo-admin/src/tv/selftest.ts</files>
  <read_first>
    - el-templo-api/test/tv/__fixtures__/timer-vectors.json (formato: name / params / spec / samples)
    - el-templo-admin/scripts/build-tv.mjs (copia el fixture a public/tv/timer-vectors.json)
    - el-templo-admin/src/tv/timer.ts (creado en el task 1)
    - el-templo-admin/src/tv/logger.ts
  </read_first>
  <action>
    Implementar `runSelfTest(): void` en `src/tv/selftest.ts`:
    1. `fetch("timer-vectors.json?t=" + Date.now())` (ruta relativa: el archivo vive junto al
       `index.html` del kiosco). Sin `AbortController` (Chromium 66).
    2. Para cada vector, correr `phaseAt(sample.elapsedMs, vector.spec)` y comparar campo por campo
       con el sample esperado (`phase`, `displayMs`, `round`, `totalRounds`, `finished`).
    3. Ademas correr 3 casos de `elapsedFrom` con estados running/paused/idle y 3 de `formatDigits`
       (0, 65_000, 3_725_000) contra valores literales esperados.
    4. Pintar en pantalla, con texto GRANDE y alto contraste: `PASS n/n` en verde o `FAIL` en rojo
       con la lista de los primeros 10 casos fallados (`nombre · elapsedMs · esperado vs obtenido`),
       ademas de `window.__TV_VERSION__` y `navigator.userAgent`. Todo con `textContent`.
    5. Loguear el resumen por `createTvLogger("selftest")`.
    El selftest NO arranca el poll ni el video: es autocontenido y se puede correr en la sede
    escribiendo `?selftest=1` en la URL.
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && npx tsc -p src/tv/tsconfig.tv.json --noEmit && node scripts/build-tv.mjs && node -e "const fs=require('fs');const h=fs.readFileSync('public/tv/index.html','utf8');if(h.indexOf('timer-vectors.json')<0)throw new Error('selftest no quedo en el bundle');const v=JSON.parse(fs.readFileSync('public/tv/timer-vectors.json','utf8'));if(!v.length)throw new Error('vectores vacios');console.log('SELFTEST_BUNDLED_OK',v.length)"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -p src/tv/tsconfig.tv.json --noEmit` limpio
    - `public/tv/timer-vectors.json` existe tras el build y tiene >= 6 vectores
    - El bundle inline contiene la cadena `timer-vectors.json`
    - `grep -c "innerHTML" src/tv/selftest.ts` devuelve `0`
    - `grep -c "AbortController" src/tv/selftest.ts` devuelve `0`
    - Corriendo `public/tv/index.html?selftest=1` en un browser local (servido por `npx serve` o equivalente) la pantalla muestra `PASS` con el total de casos
  </acceptance_criteria>
  <done>Existe un boton de "probar el timer en este televisor" que no requiere devtools ni herramientas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vectores del API -> kiosco | El fixture viaja como archivo estatico del mismo origen que `/tv/` |
| reloj del televisor -> timer en pantalla | El reloj local puede estar arbitrariamente corrido |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-28 | Tampering | Divergencia silenciosa entre `timer-phase.ts` (API) y `timer.ts` (TV) | mitigate | Vectores dorados generados por el test del API y ejecutados por `?selftest=1`: cualquier divergencia sale FAIL en la pantalla del televisor |
| T-164-29 | Spoofing | Reloj del TV corrido hace mentir al timer | mitigate | `elapsedFrom` recibe `nowCorrected` (offset del servidor), nunca `Date.now()` crudo; guards de elapsed negativo o mayor a 24 h |
| T-164-30 | Tampering | XSS por el render del selftest | mitigate | Todo con `textContent`; el fixture es del mismo origen y generado en build |
| T-164-SC | Tampering | npm/pnpm installs | mitigate | Cero paquetes nuevos. Cualquier `pnpm add` requiere checkpoint humano bloqueante (C-08) |
</threat_model>

<verification>
- `npx tsc -p src/tv/tsconfig.tv.json --noEmit` limpio
- `node scripts/build-tv.mjs` verde con `timer-vectors.json` copiado
- `index.html?selftest=1` muestra PASS en un browser local
</verification>

<success_criteria>
- `phaseAt` del kiosco es un port fiel del API
- La pausa congela y reanuda exacto
- El selftest corre en el TV real sin herramientas
</success_criteria>

<output>
Create `.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-07-SUMMARY.md` when done
</output>
```
