---
phase: 164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-
plan: 09
type: execute
wave: 4
depends_on: ["164-03"]
files_modified:
  - el-templo-admin/src/composables/useTvApi.ts
  - el-templo-admin/src/pages/TvDevicesPage.vue
  - el-templo-admin/src/router/routes.ts
  - el-templo-admin/src/config/templo-config.ts
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05]
must_haves:
  truths:
    - "El Dueño y los coaches pueden vincular un TV cargando el codigo que muestra la pantalla y eligiendo la sede (D-01)"
    - "La pantalla lista los dispositivos con 'visto hace X' y un boton revocar (D-05, D-03)"
    - "Se pueden vincular varios TVs a la misma sede (D-04)"
    - "El item de menu solo aparece para los roles habilitados"
  artifacts:
    - path: "el-templo-admin/src/composables/useTvApi.ts"
      provides: "Wrapper tipado de /api/admin/tv con loading/error y cleanup()"
      exports: ["useTvApi"]
    - path: "el-templo-admin/src/pages/TvDevicesPage.vue"
      provides: "Vinculacion + monitoreo + revocacion de televisores"
  key_links:
    - from: "el-templo-admin/src/router/routes.ts"
      to: "pages/TvDevicesPage.vue"
      via: "ruta /tv/devices con allowedRoles"
      pattern: "TvDevicesPage"
    - from: "el-templo-admin/src/composables/useTvApi.ts"
      to: "/admin/tv/pair/claim"
      via: "api.post del boot de axios"
      pattern: "admin/tv"
---

<objective>
La superficie de staff para vincular y monitorear televisores: composable tipado del modulo TV y la pantalla de dispositivos con "visto hace X" y revocacion.

Purpose: sin esta pantalla, un TV no puede vincularse (el kiosco muestra un codigo que alguien tiene que cargar) ni revocarse. Es la contraparte operativa de D-03 (token que no expira pero se revoca).
Output: `useTvApi.ts`, `TvDevicesPage.vue`, ruta y entrada de menu.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-CONTEXT.md
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-PATTERNS.md
@.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-RESEARCH.md
@CLAUDE.md

<interfaces>
Endpoints que consume (publicados por el plan 164-03):
  POST /admin/tv/pair/claim         { userCode, branchId, name? } -> { ok, branchId }   409 / 404
  GET  /admin/tv/devices?branchId=  -> { devices: [{ id, name, branchId, branchName, isActive, lastSeenAt, createdAt }] }
  POST /admin/tv/devices/:id/revoke -> { ok }

Tipos que este plan publica en `useTvApi.ts` (el plan 164-12 los extiende con los del control):
TvDeviceRow, TvClaimInput
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useTvApi.ts — composable del modulo TV (parte dispositivos)</name>
  <files>el-templo-admin/src/composables/useTvApi.ts</files>
  <read_first>
    - el-templo-admin/src/composables/useFinanceLoadApi.ts (analog mas reciente: header con las reglas del proyecto declaradas, refs de loading/error, try/catch/finally, `cleanup()` sin `onUnmounted`, interfaces documentadas en el mismo archivo)
    - el-templo-admin/src/boot/axios.ts (instancia `api` y su base URL)
    - el-templo-admin/src/utils/extract-error.ts
  </read_first>
  <action>
    Crear `el-templo-admin/src/composables/useTvApi.ts` copiando la estructura de
    `useFinanceLoadApi.ts`: docblock inicial declarando las reglas (sin `onUnmounted` adentro, sin
    `console.*`, sin `any`), `loading`/`error` como `ref`, y metodos:
    - `claimPairing(input: { userCode: string; branchId: number; name?: string }): Promise<void>`
      -> `api.post("/admin/tv/pair/claim", input)`. Normalizar el `userCode` a MAYUSCULAS y sin
      espacios antes de mandarlo.
    - `listDevices(branchId?: number): Promise<TvDeviceRow[]>` -> `api.get("/admin/tv/devices", { params })`
    - `revokeDevice(id: number): Promise<void>` -> `api.post("/admin/tv/devices/" + id + "/revoke")`
    - `cleanup()` que resetea `loading` y `error`.
    Interfaces `TvDeviceRow` y `TvClaimInput` en el mismo archivo, con comentarios que citen la
    decision que las justifica (D-05 para `lastSeenAt`, D-03 para `isActive`).
    Errores: `catch (err: unknown)` + `extractError(err, "<mensaje en castellano>")`, re-lanzar.
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && npx vue-tsc --noEmit 2>&1 | grep -c "useTvApi" ; npx eslint -c ./eslint.config.js src/composables/useTvApi.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vue-tsc --noEmit` no reporta ningun error en `useTvApi.ts` (el grep del verify devuelve `0`)
    - `npx eslint src/composables/useTvApi.ts` sin errores
    - `grep -c "onUnmounted" src/composables/useTvApi.ts` devuelve `0` (C-14)
    - `grep -c "cleanup" src/composables/useTvApi.ts` >= 1
    - `grep -cE ':\s*any\b' src/composables/useTvApi.ts` devuelve `0`
    - `grep -c "console\." src/composables/useTvApi.ts` devuelve `0`
  </acceptance_criteria>
  <done>El admin tiene un unico wrapper tipado de las rutas de TV, listo para que el plan 164-12 le agregue los metodos del control.</done>
</task>

<task type="auto">
  <name>Task 2: TvDevicesPage.vue — vincular, monitorear y revocar</name>
  <files>el-templo-admin/src/pages/TvDevicesPage.vue</files>
  <read_first>
    - el-templo-admin/src/pages/AppWaitlistPage.vue (analog exacto: los 4 estados loading/error/empty/table y columnas con `format`)
    - el-templo-admin/src/pages/DeudasPage.vue lineas 68-145 (selector de sede, gating por rol, `createLogger`, carga del catalogo de sedes con `membersApi.getBranches()`)
    - el-templo-admin/src/composables/useTvApi.ts (creado en el task 1)
    - el-templo-admin/src/utils/logger.ts
  </read_first>
  <action>
    Crear `src/pages/TvDevicesPage.vue` (Quasar, `<script setup lang="ts">`,
    `const log = createLogger("TvDevicesPage")`):
    - Cabecera "Televisores" + un formulario compacto de vinculacion: input de `userCode` (6 chars,
      mayusculas automaticas, hint "el codigo que muestra la pantalla del TV"), `q-select` de sede
      (default = la sede del usuario logueado, D-11) y un input opcional de nombre ("TV sala",
      "TV recepcion"). Boton "Vincular". Al reclamar con exito: notificacion verde con la
      instruccion "el televisor se conecta solo en unos segundos" y refresco del listado.
      Errores esperables mapeados a mensajes claros: 404 -> "ese codigo no existe", 409 -> "ese
      codigo ya fue usado".
    - Tabla con los 4 estados del analog. Columnas: Nombre, Sede, Estado (chip verde "Activo" /
      gris "Revocado"), "Visto hace" (texto relativo calculado desde `lastSeenAt`: "hace 3 s",
      "hace 4 min", "hace 2 h", "nunca"), Vinculado el, y Acciones (boton "Revocar" con
      `q-dialog` de confirmacion, oculto si ya esta revocado).
    - Refresco automatico del listado cada 15 s mientras la pantalla esta montada (D-05: monitoreo
      pasivo), con `clearInterval` en `onUnmounted` DE LA PAGINA (no del composable).
    - Nunca mostrar ningun token: el listado no lo trae y la pantalla no debe pedirlo.
  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && npx vue-tsc --noEmit 2>&1 | grep -c "TvDevicesPage" ; npx eslint -c ./eslint.config.js src/pages/TvDevicesPage.vue</automated>
  </verify>
  <acceptance_criteria>
    - `npx vue-tsc --noEmit` no reporta errores en `TvDevicesPage.vue` (grep del verify = `0`)
    - `npx eslint src/pages/TvDevicesPage.vue` sin errores
    - `grep -c "console\." src/pages/TvDevicesPage.vue` devuelve `0` y `grep -c "createLogger" src/pages/TvDevicesPage.vue` >= 1
    - La pagina implementa los 4 estados (loading / error / empty / table)
    - `grep -ci "token" src/pages/TvDevicesPage.vue` devuelve `0`
    - Existe `clearInterval` en el `onUnmounted` de la pagina
  </acceptance_criteria>
  <done>Un coach puede vincular un TV nuevo, ver cuando reporto por ultima vez y revocarlo.</done>
</task>

<task type="auto">
  <name>Task 3: Ruta y entrada de menu</name>
  <files>el-templo-admin/src/router/routes.ts, el-templo-admin/src/config/templo-config.ts</files>
  <read_first>
    - el-templo-admin/src/router/routes.ts (patron de rutas hijas del layout con `meta.allowedRoles`)
    - el-templo-admin/src/config/templo-config.ts (NAV_MODEL: categorias, `roles`, `icon`, `path`)
    - el-templo-admin/src/router/index.ts (guard que consume `meta.allowedRoles`)
    - .planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-PATTERNS.md (aclaracion critica: `/tv` NO se agrega al SPA)
  </read_first>
  <action>
    Agregar la ruta hija `tv/devices` -> `pages/TvDevicesPage.vue` con
    `meta: { allowedRoles: ['coach', 'admin', 'owner'] as AdminRole[] }` (equivalente frontend de
    `TV_CONTROL_ROLES`, D-01), y el item correspondiente en `NAV_MODEL` de `templo-config.ts`
    (label "Televisores", icono `tv`, `roles` con los mismos tres).

    CRITICO: NO agregar ninguna ruta cuyo `path` sea exactamente `tv` ni `/tv`. La pantalla del
    kiosco es `public/tv/index.html`, servida por nginx via `index index.html` sobre el directorio;
    una ruta `/tv` en el SPA romperia esa resolucion y anularia toda la estrategia de compatibilidad
    (D-24). Las rutas del SPA son `/tv/devices` y (plan 164-12) `/tv/control`, que nginx resuelve al
    fallback `/index.html` porque no existen como archivo ni como directorio.
    Nota para desarrollo: con `quasar dev` el kiosco se abre en `/tv/index.html`.

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && grep -c "TvDevicesPage" src/router/routes.ts && node -e "const s=require('fs').readFileSync('src/router/routes.ts','utf8'); if(/path:\s*['\"]\/?tv['\"]/.test(s)) throw new Error('ruta /tv en el SPA: rompe el kiosco'); console.log('ROUTES_OK')" && npx vue-tsc --noEmit 2>&1 | grep -c "routes.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TvDevicesPage" src/router/routes.ts` devuelve `1`
    - El chequeo de node confirma que NO existe una ruta con `path: 'tv'` ni `'/tv'`
    - El item de menu esta en `NAV_MODEL` con `roles: ['coach','admin','owner']`
    - `npx vue-tsc --noEmit` sin errores nuevos
  </acceptance_criteria>
  <done>La pantalla de televisores es accesible desde el menu para Dueño, admin y coaches, sin pisar la ruta estatica del kiosco.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                        |
| ------------------------------------ | ------------------------------------------------------------------ |
| navegador del staff -> API           | Escrituras autenticadas por JWT, gateadas por rol y sede           |
| menu del admin -> superficie visible | La visibilidad de nav no es seguridad: el gate real esta en el API |

## STRIDE Threat Register

| Threat ID | Category               | Component                                          | Disposition | Mitigation Plan                                                                                                                |
| --------- | ---------------------- | -------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------ |
| T-164-36  | Elevation of Privilege | Rol no habilitado accediendo a la pantalla         | mitigate    | Doble gate: `meta.allowedRoles` en el router (UX) + `TV_CONTROL_ROLES` en el plugin del API (autoridad real, plan 164-03)      |
| T-164-37  | Information Disclosure | Device token expuesto en la UI de staff            | mitigate    | El endpoint de listado no devuelve el token y la pagina no lo pide; criterio de aceptacion prohibe la palabra en el componente |
| T-164-38  | Elevation of Privilege | Vincular un TV a una sede ajena                    | mitigate    | `requireBranchAccess({ from: "body.branchId" })` en el API; el selector de la UI arranca en la sede del usuario                |
| T-164-39  | Denial of Service      | Refresco cada 15 s dejado corriendo tras desmontar | mitigate    | `clearInterval` en el `onUnmounted` de la pagina (el composable no registra hooks, C-14)                                       |
| T-164-SC  | Tampering              | npm/pnpm installs                                  | mitigate    | Cero paquetes nuevos. Cualquier `pnpm add` requiere checkpoint humano bloqueante (C-08)                                        |

</threat_model>

<verification>
- `npx vue-tsc --noEmit` sin errores nuevos (CI no typechequea el admin — hay que correrlo local)
- `npx eslint` limpio en los archivos nuevos
- Flujo manual contra la API local: start desde el kiosco -> claim desde la pantalla -> el dispositivo aparece listado -> revocar lo marca inactivo
</verification>

<success_criteria>

- Vincular, listar y revocar funcionan end-to-end contra la API local
- "Visto hace X" refleja el `lastSeenAt` que actualiza el poll
- La ruta `/tv` estatica sigue intacta
  </success_criteria>

<output>
Create `.planning/phases/164-pantalla-tv-de-sucursal-plani-viva-por-bloque-con-timer-por-/164-09-SUMMARY.md` when done
</output>
