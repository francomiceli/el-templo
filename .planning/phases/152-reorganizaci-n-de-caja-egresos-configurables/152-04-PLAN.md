---
phase: 152-reorganizaci-n-de-caja-egresos-configurables
plan: 04
type: execute
wave: 3
depends_on: ["152-01", "152-03"]
files_modified:
  - el-templo-api/src/modules/finance/cash-register-service.ts
  - el-templo-api/src/modules/finance/routes.ts
  - el-templo-api/src/modules/finance/schemas.ts
  - el-templo-api/src/modules/finance/types.ts
  - el-templo-api/test/finance/cost-centers-abm.test.ts
autonomous: true
requirements: [CAJA-05]

must_haves:
  truths:
    - "Un admin/owner puede crear un centro de costo, renombrarlo, desactivarlo y reactivarlo desde la API"
    - "No existe borrado físico de centros de costo (solo baja lógica is_active=false)"
    - "El nombre de un centro de costo es único por país; crear/renombrar a un nombre duplicado devuelve 409"
    - "gestion/coach reciben 403 en las rutas de escritura; owner/admin reciben 2xx"
    - "Un egreso con un centro de costo desactivado es rechazado (registerExpense ya filtra is_active)"
  artifacts:
    - path: "el-templo-api/src/modules/finance/cash-register-service.ts"
      provides: "CRUD de cost_centers + assertUniqueName"
      contains: "createCostCenter"
    - path: "el-templo-api/src/modules/finance/routes.ts"
      provides: "rutas ABM cost-centers con guard ADMIN_ROLES"
      contains: "cost-centers"
    - path: "el-templo-api/test/finance/cost-centers-abm.test.ts"
      provides: "matriz RBAC + unicidad + selector excluye inactivos"
      contains: "cost-centers"
  key_links:
    - from: "el-templo-api/src/modules/finance/routes.ts cost-centers writes"
      to: "ADMIN_ROLES"
      via: "in-handler guard 403"
      pattern: "ADMIN_ROLES"
    - from: "el-templo-api/src/modules/finance/cash-register-service.ts"
      to: "cost_centers uq index"
      via: "assertUniqueName + DB unique"
      pattern: "assertUniqueName"
---

<objective>
ABM de centros de costo desde la API (CAJA-05, levanta EGR-F2 de v5.3). Nuevos métodos en `cash-register-service.ts` (crear/renombrar/desactivar/reactivar/listAll incl. inactivos) con guard de unicidad por país, rutas CRUD en `routes.ts` con el guard in-handler `ADMIN_ROLES` (patrón bank-accounts fase 150), schemas de validación y tipos. Sin borrado físico (D-08): baja = `is_active=false`. El selector de egresos existente ya lista solo activos, así que desactivar saca la categoría del selector sin cambios adicionales.

Purpose: Backend del ABM que consume la UI de 152-06. La seguridad real vive acá (149 D-04); la UI solo esconde.
Output: service + routes + schemas + types + tests de integración.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-CONTEXT.md
@.planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-PATTERNS.md
@.planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-01-SUMMARY.md

<interfaces>
<!-- Análogos exactos del ABM de cuentas bancarias (fase 150). Ver PATTERNS.md. -->
- `cash-register-service.ts`: métodos análogos `createBankAccount`/`updateBankAccount`/`closeBankAccount`/`reactivateBankAccount` ~478-636; `listActiveCostCenters` ~360-376 (ya existe, read path del selector); `listBankAccounts` ~625-636 (dropea el filtro isActive → template de listAll); guard de unicidad análogo `assertTransferIdentifier` ~414-423.
- `routes.ts`: rutas ABM bank-accounts ~1198-1303; guard in-handler `if (!(ADMIN_ROLES as readonly string[]).includes(request.user.role)) return reply.code(403)...` ~1203-1208; `GET /cost-centers` (active-only, selector) ~1155 con resolución de país owner-aware ~1169-1176; `handleServiceError(err, reply, request.log, "...")` en cada handler.
- `schemas.ts`: `createBankAccountSchema`/`updateBankAccountSchema`/`closeBankAccountSchema`/`reactivateBankAccountSchema` ~844-922; `BANK_ACCOUNT_ID_PARAMS` ~832-836; `errorSchema` compartido (400/401/403/404/500).
- `movement-service.registerExpense` ~294-304 ya filtra `isActive=true` al validar el centro de costo — desactivar lo saca del egreso sin cambios extra.
- `ADMIN_ROLES` importado en routes.ts (línea 58). NO usar FINANCE_VOID_ROLES (incluye gestion).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Métodos CRUD de cost_centers en cash-register-service (+ assertUniqueName)</name>
  <files>el-templo-api/src/modules/finance/cash-register-service.ts</files>
  <read_first>
    - el-templo-api/src/modules/finance/cash-register-service.ts (createBankAccount/updateBankAccount/closeBankAccount/reactivateBankAccount ~478-636, listActiveCostCenters ~360-376, listBankAccounts ~625-636, assertTransferIdentifier ~414-423)
    - el-templo-api/src/modules/shared/errors (BadRequestError/NotFoundError/ConflictError)
    - .planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-PATTERNS.md (sección cash-register-service.ts)
  </read_first>
  <action>
    Agregar métodos: `createCostCenter(name, country)`, `renameCostCenter(id, name)`, `deactivateCostCenter(id)`, `reactivateCostCenter(id)`, `listAllCostCenters(country)` (activos + inactivos, para el ABM — espejar `listBankAccounts` que dropea el filtro isActive). Agregar un guard `assertUniqueName(name, country, excludeId?)` (espejando `assertTransferIdentifier` ~414-423) que lanza `ConflictError` si ya existe un centro con ese (name, country) — belt-and-suspenders con el uniqueIndex de la DB; llamarlo en create y rename. Desactivar/reactivar = `set({ isActive: false/true })` (patrón `closeBankAccount` ~593-601), SIN borrado físico (D-08). Definir la case-sensitivity del check para alinear con la collation MySQL default (case-insensitive) — el uniqueIndex ya dedup case-insensitive, alinear el guard igual.
  </action>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Existen los métodos `createCostCenter`, `renameCostCenter`, `deactivateCostCenter`, `reactivateCostCenter`, `listAllCostCenters` (grep de cada nombre == 1 definición)
    - `grep -c 'assertUniqueName' el-templo-api/src/modules/finance/cash-register-service.ts` >= 3 (definición + create + rename)
    - Ningún método hace DELETE físico sobre cost_centers (grep de `.delete(` sobre cost_centers == 0 en los métodos nuevos)
    - deactivate/reactivate usan `isActive: false/true`
    - `npx tsc --noEmit` verde
  </acceptance_criteria>
  <done>El service crea/renombra/desactiva/reactiva/lista centros con unicidad por país y sin borrado físico.</done>
</task>

<task type="auto">
  <name>Task 2: Rutas ABM cost-centers + schemas + types (guard ADMIN_ROLES)</name>
  <files>el-templo-api/src/modules/finance/routes.ts, el-templo-api/src/modules/finance/schemas.ts, el-templo-api/src/modules/finance/types.ts</files>
  <read_first>
    - el-templo-api/src/modules/finance/routes.ts (rutas ABM bank-accounts ~1198-1303, guard ADMIN_ROLES ~1203-1208, GET /cost-centers active-only ~1155, resolución de país owner-aware ~1169-1176, handleServiceError)
    - el-templo-api/src/modules/finance/schemas.ts (createBankAccountSchema etc. ~844-922, BANK_ACCOUNT_ID_PARAMS ~832-836, errorSchema)
    - el-templo-api/src/modules/finance/types.ts (tipos de cost center si existen)
    - .planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-PATTERNS.md (secciones routes.ts + schemas.ts + types.ts)
  </read_first>
  <action>
    Rutas nuevas (todas con el guard in-handler `ADMIN_ROLES` verbatim del patrón bank-accounts ~1203-1208 y `handleServiceError(err, reply, request.log, "...")` en el catch): `POST /cost-centers` (crear), `PATCH /cost-centers/:id` (renombrar), `POST /cost-centers/:id/deactivate`, `POST /cost-centers/:id/reactivate`, `GET /cost-centers/all` (ABM, incluye inactivos). NO tocar el `GET /cost-centers` existente (~1155) que sigue active-only para el selector de egresos. La resolución de país copia el bloque owner-aware de `GET /cost-centers` (~1169-1176).
    En `schemas.ts`: agregar `createCostCenterSchema` (body `{ name: {type:string, minLength:1, maxLength:100}, country: {type:string, enum:["AR","ES"]} }`), `renameCostCenterSchema` (body `{ name }` solo), `COST_CENTER_ID_PARAMS` (espejo de BANK_ACCOUNT_ID_PARAMS), reusando el `errorSchema` compartido y `additionalProperties: false`.
    En `types.ts`: agregar el tipo `CostCenter` (id, name, country, isActive) y el tipo de fila del listAll si hace falta.
  </action>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Existen las rutas `POST /cost-centers`, `PATCH /cost-centers/:id`, `POST /cost-centers/:id/deactivate`, `POST /cost-centers/:id/reactivate`, `GET /cost-centers/all` (grep de cada path)
    - Cada handler de escritura contiene el guard `ADMIN_ROLES` con `reply.code(403)`
    - El `GET /cost-centers` original (active-only) sigue existiendo y sin cambios de comportamiento
    - `schemas.ts` tiene `createCostCenterSchema`, `renameCostCenterSchema`, `COST_CENTER_ID_PARAMS` con `additionalProperties: false`
    - `types.ts` exporta `CostCenter`
    - `npx tsc --noEmit` verde
  </acceptance_criteria>
  <done>El ABM está expuesto con guard admin/owner, validación estricta y país scopeado; el selector de egresos no cambia.</done>
</task>

<task type="auto">
  <name>Task 3: Tests de integración del ABM (RBAC + unicidad + egreso con centro desactivado)</name>
  <files>el-templo-api/test/finance/cost-centers-abm.test.ts</files>
  <read_first>
    - el-templo-api/test/finance/bank-accounts.test.ts (harness completo: createTestApp, createStaffUser, getAuthToken, helper createAccount, cleanup, matriz RBAC owner/gestion/coach)
    - el-templo-api/test/finance/cost-centers.test.ts (harness del read path del selector, para la aserción de exclusión de inactivos)
    - .planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-PATTERNS.md (sección Tests, análogo bank-accounts.test.ts)
  </read_first>
  <action>
    Copiar `bank-accounts.test.ts` a `cost-centers-abm.test.ts` y adaptar a los endpoints de cost-centers. Casos requeridos (corren en CI): crear/renombrar/desactivar/reactivar happy path (owner/admin → 2xx); nombre único por país → 409 (crear y renombrar a un nombre existente); gestion y coach en escritura → 403; egreso con un centro desactivado → 400 (assert contra `movement-service.registerExpense` que ya filtra isActive=true, ~294-304); `GET /cost-centers/all` incluye inactivos y `GET /cost-centers` (selector) los excluye. Usar el harness de bank-accounts (no crear app nuevo) + cleanup de ids creados.
  </action>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - El archivo `cost-centers-abm.test.ts` existe con casos de create/rename/deactivate/reactivate
    - Hay un caso de unicidad por país que espera 409
    - Hay casos RBAC: gestion/coach → 403, owner → 2xx
    - Hay un caso de egreso con centro desactivado → 400
    - Hay un caso de que el selector (GET /cost-centers) excluye inactivos y GET /cost-centers/all los incluye
    - `npx tsc --noEmit` verde
  </acceptance_criteria>
  <done>Suite cubre CRUD, unicidad, RBAC y la exclusión de inactivos del selector; typecheck verde.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                                                |
| -------------------------------------- | -------------------------------------------------------------------------- |
| cliente admin → rutas ABM cost-centers | escrituras (create/rename/deactivate/reactivate) cruzan input no confiable |
| país del request → scope del catálogo  | el país se resuelve server-side (owner-aware), no se confía en el cliente  |

## STRIDE Threat Register

| Threat ID | Category               | Component                                  | Disposition | Mitigation Plan                                                                                                    |
| --------- | ---------------------- | ------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------ |
| T-152-08  | Elevation of Privilege | rutas de escritura de cost-centers         | mitigate    | guard in-handler `ADMIN_ROLES` (no FINANCE_VOID_ROLES) → gestion/coach 403; la seguridad vive en la API (149 D-04) |
| T-152-09  | Tampering              | body de create/rename                      | mitigate    | schema con `additionalProperties:false`, name minLength/maxLength, country enum ["AR","ES"]                        |
| T-152-10  | Tampering              | unicidad de nombre por país                | mitigate    | assertUniqueName en el service + uniqueIndex DB (0165) → doble barrera, 409                                        |
| T-152-11  | Tampering              | borrado de categoría con egresos imputados | mitigate    | sin DELETE físico; baja lógica is_active=false (D-08) preserva históricos                                          |
| T-152-SC  | Tampering              | npm/pip/cargo installs                     | accept      | esta fase no instala paquetes nuevos                                                                               |

</threat_model>

<verification>
- `npx tsc --noEmit` verde en el-templo-api.
- Tests de cost-centers-abm cubren CRUD, unicidad, RBAC y exclusión de inactivos (corren en CI).
</verification>

<success_criteria>

- CRUD de cost_centers desde la API con guard admin/owner y unicidad por país, sin borrado físico.
- El selector de egresos sigue mostrando solo activos.
- Cobertura de integración de todos los caminos.
  </success_criteria>

<output>
Create `.planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-04-SUMMARY.md` when done
</output>
