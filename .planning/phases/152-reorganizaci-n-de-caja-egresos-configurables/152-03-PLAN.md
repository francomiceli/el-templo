---
phase: 152-reorganizaci-n-de-caja-egresos-configurables
plan: 03
type: execute
wave: 2
depends_on: ["152-01"]
files_modified:
  - el-templo-api/src/modules/finance/transaction-service.ts
  - el-templo-api/src/modules/finance/routes.ts
  - el-templo-api/src/modules/finance/schemas.ts
  - el-templo-api/src/modules/finance/types.ts
  - el-templo-api/test/finance/validate-caja.test.ts
autonomous: true
requirements: [CAJA-02, CAJA-04]

must_haves:
  truths:
    - "Al validar un cobro pendiente, la fila queda con validated_by = admin que validó y validated_at = timestamp de la validación"
    - "Los cobros corregidos (anular+recrear) y los cargados por admin/owner (nacen validados) quedan con validated_by/validated_at NULL"
    - "GET /transactions devuelve validationStatus, validatedAt y validatorName en cada fila"
    - "GET /transactions acepta un filtro opcional por validationStatus (validado/pendiente) que filtra server-side"
  artifacts:
    - path: "el-templo-api/src/modules/finance/transaction-service.ts"
      provides: "validate() setea columnas + listTransactions surface + leftJoin validator"
      contains: "validatedBy"
    - path: "el-templo-api/src/modules/finance/types.ts"
      provides: "TransactionListItem con validationStatus/validatedAt/validatorName"
      contains: "validatorName"
    - path: "el-templo-api/test/finance/validate-caja.test.ts"
      provides: "cobertura de columnas del validador + filtro por estado"
      contains: "validated_by"
  key_links:
    - from: "el-templo-api/src/modules/finance/transaction-service.ts listTransactions"
      to: "users (validator alias)"
      via: "leftJoin validatedBy"
      pattern: "leftJoin.*validator"
    - from: "el-templo-api/src/modules/finance/routes.ts GET /transactions"
      to: "listTransactions conditions"
      via: "validationStatus querystring"
      pattern: "validationStatus"
---

<objective>
Read/write path del validador para el Historial de cobros (CAJA-02 backend, CAJA-04). `validate()` completa las columnas `validated_by`/`validated_at` (D-05); `correct()` y la carga admin/owner NO las setean (D-06, se distinguen como "Validado al registrar"). `listTransactions()` expone `validationStatus`, `validatedAt` y `validatorName` (LEFT JOIN al alias `validator`, copiando el patrón `recorder`) y el endpoint `GET /transactions` gana un filtro opcional por estado server-side (discreción resuelta como query param porque la lista es server-paginada).

Purpose: Provee a la UI (152-05) el chip validada/pendiente, el filtro por estado y el validador+fecha del detalle. El audit_log sigue siendo la fuente forense; las columnas son el read path denormalizado.
Output: transaction-service + routes + schemas + types + tests de integración.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-CONTEXT.md
@.planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-PATTERNS.md
@.planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-01-SUMMARY.md

<interfaces>
<!-- Refs concretas en transaction-service.ts (ver PATTERNS.md sección transaction-service). -->
- `validate()` ~634-734; el `.set({ validationStatus: "validado", ... })` a extender está ~690-698; adminId ya es param; el write a audit_log (~704-717, action `transaction_validated`) SE MANTIENE.
- `correct()` ~819+ y la carga admin crean filas con `validationStatus: "validado"` (~línea 891) — NO setear validatedBy/At ahí (D-06).
- `listTransactions()`: select raw ~1117-1137; alias `recorder = alias(schema.users, "recorder")` ~1092; `.innerJoin` recorder ~1147-1150; map de fila (recorderName) ~1192-1212; `conditions[]` con eq(kind)/eq(paymentMethod) ya existentes.
- `TransactionListItem` en types.ts ~216-242 (hoy NO tiene validationStatus). El tipo `ValidationStatus` (enum fase 137) ya existe en el módulo.
- IMPORTANTE (PATTERNS NOTE): `listTransactions` (GET /transactions, MovimientosTab) NO selecciona hoy `validationStatus`. La afirmación de CONTEXT "la API ya lo devuelve (routes.ts:1374)" refiere a `/movements-history` (otro query). Hay que AGREGARLO acá.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: validate() setea validated_by/at; correct()/admin-load quedan NULL (D-05/D-06)</name>
  <files>el-templo-api/src/modules/finance/transaction-service.ts</files>
  <behavior>
    - validate(txId, adminId): la fila queda validation_status='validado', validated_by=adminId, validated_at≈now.
    - correct() (anular+recrear): la fila nueva nace validation_status='validado' PERO validated_by=NULL y validated_at=NULL.
    - carga admin/owner (nace validado): validated_by=NULL, validated_at=NULL.
  </behavior>
  <read_first>
    - el-templo-api/src/modules/finance/transaction-service.ts (validate ~634-734, .set ~690-698, audit_log write ~704-717, correct ~819+, admin-load create ~891)
    - .planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-PATTERNS.md (sección transaction-service.ts)
  </read_first>
  <action>
    Extender el `.set({...})` de `validate()` (~690-698) para escribir `validatedBy: adminId` y `validatedAt: new Date()` junto al `validationStatus: "validado"` existente (respetar el spread condicional de `cashRegisterId` que ya está). Mantener intacto el write a `audit_log` (~704-717) — sigue siendo la fuente forense; las columnas son el read path denormalizado. En `correct()` (~819+) y en el path de carga admin que crea filas con `validationStatus: "validado"` (~891): NO setear `validatedBy`/`validatedAt` (quedan NULL — D-06, la UI las distingue como "Validado al registrar" vía recordedBy/createdAt). Verificar que ningún otro call site que crea filas validadas setee estas columnas por accidente.
  </action>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'validatedBy: adminId' el-templo-api/src/modules/finance/transaction-service.ts` == 1 (solo en validate())
    - `grep -c 'validatedAt: new Date()' el-templo-api/src/modules/finance/transaction-service.ts` == 1
    - El write a audit_log con action `transaction_validated` sigue presente
    - correct() y el create admin-load NO referencian validatedBy/validatedAt
    - `npx tsc --noEmit` verde
  </acceptance_criteria>
  <done>validate() persiste el validador y su fecha; los cobros corregidos y los nacidos validados quedan NULL en esas columnas.</done>
</task>

<task type="auto">
  <name>Task 2: listTransactions expone el validador + filtro por estado (routes/schemas/types)</name>
  <files>el-templo-api/src/modules/finance/transaction-service.ts, el-templo-api/src/modules/finance/routes.ts, el-templo-api/src/modules/finance/schemas.ts, el-templo-api/src/modules/finance/types.ts</files>
  <read_first>
    - el-templo-api/src/modules/finance/transaction-service.ts (listTransactions select ~1117-1137, recorder alias ~1092, innerJoin recorder ~1147-1150, map fila ~1192-1212, conditions[])
    - el-templo-api/src/modules/finance/routes.ts (GET /transactions handler + su querystring schema; patrón de filtros kind/paymentMethod)
    - el-templo-api/src/modules/finance/schemas.ts (querystring del listado de transacciones)
    - el-templo-api/src/modules/finance/types.ts (TransactionListItem ~216-242, tipo ValidationStatus)
    - .planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-PATTERNS.md (sección listTransactions + State filter)
  </read_first>
  <action>
    En `listTransactions()`: declarar `const validator = alias(schema.users, "validator")` junto al alias `recorder`; agregar al select raw las columnas `validationStatus`, `validatedAt`, `validatorFirstName: validator.firstName`, `validatorLastName: validator.lastName`; agregar `.leftJoin(validator, eq(validator.id, schema.financialTransactions.validatedBy))` — LEFT (validatedBy nullable), NO inner como recorder. En el map de fila (~1192-1212) construir `validatorName` = concat firstName+lastName o `null` (espejando `recorderName`), y pasar `validationStatus`/`validatedAt`. Agregar un parámetro opcional `validationStatus?: 'validado' | 'pendiente'` a la firma/opts y, si viene, push de `eq(schema.financialTransactions.validationStatus, ...)` a `conditions[]` (misma forma que kind/paymentMethod).
    En `routes.ts`: agregar `validationStatus` (enum ['validado','pendiente']) al querystring del `GET /transactions` y pasarlo a `listTransactions`.
    En `schemas.ts`: agregar la propiedad `validationStatus` con enum al querystring schema del listado (mantener `additionalProperties: false`).
    En `types.ts`: agregar a `TransactionListItem` los campos `validationStatus: ValidationStatus`, `validatedAt: string | null`, `validatorName: string | null`.
  </action>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'alias(schema.users, "validator")' el-templo-api/src/modules/finance/transaction-service.ts` == 1
    - `grep -c 'leftJoin(validator' el-templo-api/src/modules/finance/transaction-service.ts` == 1
    - `grep -c 'validatorName' el-templo-api/src/modules/finance/transaction-service.ts` >= 1 y en `types.ts` == 1
    - `grep -c 'validationStatus' el-templo-api/src/modules/finance/schemas.ts` >= 1 (querystring)
    - `TransactionListItem` en types.ts incluye validationStatus, validatedAt, validatorName
    - `npx tsc --noEmit` verde
  </acceptance_criteria>
  <done>GET /transactions devuelve estado + validador por fila y acepta filtro por estado server-side.</done>
</task>

<task type="auto">
  <name>Task 3: Tests de integración del validador + filtro por estado</name>
  <files>el-templo-api/test/finance/validate-caja.test.ts</files>
  <read_first>
    - el-templo-api/test/finance/validate-caja.test.ts (harness existente: TransactionService + caja ids seedeadas)
    - el-templo-api/test/helpers.ts (createTestApp, createStaffUser, getAuthToken, utilidades de request)
    - .planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-PATTERNS.md (sección Tests, análogo validate-caja.test.ts)
  </read_first>
  <action>
    Agregar casos al harness existente (corren en CI, no local): (1) validar un cobro pendiente y assert que la fila queda con `validated_by` = id del admin y `validated_at` no-null; (2) que el endpoint de listado/detalle expone `validatorName` con el nombre del validador; (3) que un cobro corregido (correct/anular+recrear) y uno cargado por admin/owner quedan con `validated_by`/`validated_at` NULL y el listado los muestra sin validatorName (D-06); (4) que `GET /transactions?validationStatus=pendiente` devuelve solo pendientes y `=validado` solo validados. Reusar los helpers del harness (no crear un app nuevo).
  </action>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - El archivo contiene assertions sobre `validated_by` y `validated_at` tras validate()
    - Hay un caso que valida validatorName en el listado/detalle
    - Hay un caso D-06 (corregido/admin-load → columnas NULL)
    - Hay un caso del filtro `validationStatus` en el query
    - `npx tsc --noEmit` verde (los tests corren en CI al pushear a staging)
  </acceptance_criteria>
  <done>Suite cubre validate() setea columnas, listado expone validatorName, D-06 (NULL) y filtro por estado; typecheck verde.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                      |
| --------------------------------- | ---------------------------------------------------------------- |
| cliente admin → GET /transactions | query params (validationStatus) cruzan hacia el listado paginado |
| validate() → columnas             | escritura de identidad del validador (dato de auditoría)         |

## STRIDE Threat Register

| Threat ID | Category               | Component                            | Disposition | Mitigation Plan                                                                                                      |
| --------- | ---------------------- | ------------------------------------ | ----------- | -------------------------------------------------------------------------------------------------------------------- |
| T-152-05  | Tampering              | querystring validationStatus         | mitigate    | enum ['validado','pendiente'] en el schema del querystring (additionalProperties:false); valores fuera de enum → 400 |
| T-152-06  | Information Disclosure | validatorName expuesto en el listado | accept      | el endpoint ya está gated FINANCE_READ_ROLES (hook del módulo); validatorName es staff, no PII de socio              |
| T-152-07  | Repudiation            | quién validó                         | mitigate    | validated_by denormaliza el audit_log (que se mantiene como fuente forense); doble registro                          |
| T-152-SC  | Tampering              | npm/pip/cargo installs               | accept      | esta fase no instala paquetes nuevos                                                                                 |

</threat_model>

<verification>
- `npx tsc --noEmit` verde en el-templo-api.
- Tests de validate-caja cubren columnas, validatorName, D-06 y filtro (corren en CI).
</verification>

<success_criteria>

- validate() persiste validated_by/at; correct()/admin-load quedan NULL.
- GET /transactions expone validationStatus/validatedAt/validatorName y filtra por estado.
- Cobertura de integración de los cuatro comportamientos.
  </success_criteria>

<output>
Create `.planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-03-SUMMARY.md` when done
</output>
