---
phase: 152-reorganizaci-n-de-caja-egresos-configurables
plan: 05
type: execute
wave: 3
depends_on: ["152-03"]
files_modified:
  - el-templo-admin/src/components/caja/DateRangeFilter.vue
  - el-templo-admin/src/components/caja/MovEgresosTab.vue
  - el-templo-admin/src/components/caja/MovimientosTab.vue
  - el-templo-admin/src/types/transaction.ts
  - el-templo-admin/src/composables/useTransactionsApi.ts
autonomous: true
requirements: [CAJA-02, CAJA-03, CAJA-04]

must_haves:
  truths:
    - "Cada fila del Historial de cobros muestra un chip con el estado validada/pendiente"
    - "El Historial de cobros tiene un filtro todas/validadas/pendientes que filtra el listado"
    - "El filtro de fecha en Historial de cobros y Movimientos de caja viene por mes pero permite cambiar a rango por días"
    - "El detalle de un cobro validado por la bandeja muestra el validador + fecha de validación; el nacido validado muestra 'Validado al registrar' + quién lo cargó"
  artifacts:
    - path: "el-templo-admin/src/components/caja/DateRangeFilter.vue"
      provides: "control de fecha compartido mes↔días con contrato { dateFrom, dateTo }"
      contains: "dateFrom"
    - path: "el-templo-admin/src/components/caja/MovimientosTab.vue"
      provides: "chip de estado + filtro por estado + drill-down + validador en detalle"
      contains: "validationColor"
  key_links:
    - from: "el-templo-admin/src/components/caja/MovimientosTab.vue"
      to: "GET /transactions (validationStatus/validatedAt/validatorName)"
      via: "useTransactionsApi + filtro estado"
      pattern: "validationStatus"
    - from: "el-templo-admin/src/components/caja/DateRangeFilter.vue"
      to: "MovimientosTab + MovEgresosTab"
      via: "emit { dateFrom, dateTo }"
      pattern: "dateFrom"
---

<objective>
Historial de cobros con estado por fila + filtro por estado + drill-down por día, y el validador en el detalle (CAJA-02, CAJA-03, CAJA-04). Se extrae un control de fecha compartido `DateRangeFilter.vue` (mes default con toggle a rango por días, D-03) usado por ambos tabs (DRY, la lógica hoy está duplicada byte-idéntica). `MovimientosTab.vue` gana el chip validada/pendiente (reusando el mapa de estados de `MovEgresosTab`), el filtro todas/validadas/pendientes (query param server-side, la lista es paginada) y el validador+fecha en el dialog de detalle con la rama D-06 ("Validado al registrar").

Purpose: Consume el read path de 152-03. Cierra las quejas de Nacho sobre no ver el estado del cobro y no poder bajar a días.
Output: 1 componente nuevo compartido + 2 tabs + tipos + api client.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-CONTEXT.md
@.planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-PATTERNS.md
@.planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-03-SUMMARY.md

<interfaces>
<!-- Análogos concretos. Ver PATTERNS.md secciones MovimientosTab.vue / MovEgresosTab.vue. -->
- Mapa de estados a reusar: `MovEgresosTab.vue` ~319-335 (labels pendiente/observado/corregido/validado + colores validado→positive, pendiente→warning, observado→info, resto→grey-6). Candidato a extraer a `src/utils/validation-status.ts` (DRY, discreción).
- Columna de chip a copiar: `MovEgresosTab.vue:130-137` (`#body-cell-estado`).
- Filtro single-select existente en `MovimientosTab.vue:143-156` (filters.kind) + reactive `filters` ~511-517 → agregar `estado`.
- Bloque `selectedMonth`/`dateRange` byte-idéntico en `MovimientosTab.vue:500-509` y `MovEgresosTab.vue:364-403`; `dateRange` emite `{ dateFrom, dateTo }` consumido por `loadTransactions`/`loadHistory`. El control compartido DEBE exponer el MISMO contrato `{ dateFrom, dateTo }` en ambos modos (mes y días) para no tocar loaders.
- Dialog de detalle: `MovimientosTab.vue:294-386`, bloque "Registrado por" ~355-359 (patrón q-item/q-item-section side).
- Backend (152-03) ya devuelve en cada fila `validationStatus`, `validatedAt`, `validatorName` y acepta `?validationStatus=validado|pendiente`.
- Regla de proyecto: createLogger() nunca console.*, sin `any`, Pinia composition; `onUnmounted(() => transactionsApi.cleanup())` a nivel SFC (no dentro del composable).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DateRangeFilter.vue compartido (mes↔días) + wiring en MovEgresosTab (D-03)</name>
  <files>el-templo-admin/src/components/caja/DateRangeFilter.vue, el-templo-admin/src/components/caja/MovEgresosTab.vue, el-templo-admin/src/types/transaction.ts, el-templo-admin/src/composables/useTransactionsApi.ts</files>
  <read_first>
    - el-templo-admin/src/components/caja/MovEgresosTab.vue (bloque selectedMonth/dateRange ~364-403, loadHistory, mapa de estados ~319-335)
    - el-templo-admin/src/components/caja/MovimientosTab.vue (bloque dateRange ~500-509, para confirmar el contrato idéntico)
    - el-templo-admin/src/types/transaction.ts (TransactionListItem actual)
    - el-templo-admin/src/composables/useTransactionsApi.ts (métodos de listado, patrón de params)
    - .planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-PATTERNS.md (sección DateRangeFilter + Month→days drill-down)
  </read_first>
  <action>
    Crear `DateRangeFilter.vue`: un control con modo mes (default, `type="month"`, replica el `dateRange` computed) y un toggle/botón a modo "por días" (rango desde–hasta, `type="date"` x2). En AMBOS modos emite el mismo contrato `{ dateFrom, dateTo }` (strings 'YYYY-MM-DD') vía `v-model`/`update:modelValue` para que los loaders no cambien. Forma del toggle = discreción (botón o tabs de modo). Sin `onUnmounted` dentro (es componente, no composable).
    Wire en `MovEgresosTab.vue`: reemplazar el bloque `selectedMonth`/`dateRange` (~364-403) por `<DateRangeFilter>` que alimenta `loadHistory` con `{ dateFrom, dateTo }`. Comportamiento por mes idéntico al actual.
    En `types/transaction.ts`: agregar a `TransactionListItem` los campos `validationStatus`, `validatedAt: string | null`, `validatorName: string | null` (espejo del backend 152-03) — los consume la Task 2.
    En `useTransactionsApi.ts`: agregar el parámetro opcional `validationStatus` al método de listado de transacciones (query param), para el filtro de la Task 2.
  </action>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `DateRangeFilter.vue` existe y emite `{ dateFrom, dateTo }` en modo mes Y en modo días
    - `MovEgresosTab.vue` usa `<DateRangeFilter` y su `loadHistory` sigue recibiendo dateFrom/dateTo; el modo mes se comporta igual que antes
    - `TransactionListItem` en `types/transaction.ts` incluye validationStatus, validatedAt, validatorName
    - `useTransactionsApi.ts` acepta un param `validationStatus` en el listado de transacciones
    - Ningún `console.*` nuevo; sin `any`
    - `npx vue-tsc --noEmit` verde
  </acceptance_criteria>
  <done>Control de fecha compartido operativo en Movimientos con contrato { dateFrom, dateTo }; tipos y api client listos para el chip/filtro.</done>
</task>

<task type="auto">
  <name>Task 2: MovimientosTab — chip de estado + filtro por estado + drill-down (CAJA-02/03)</name>
  <files>el-templo-admin/src/components/caja/MovimientosTab.vue</files>
  <read_first>
    - el-templo-admin/src/components/caja/MovimientosTab.vue (columnas + filters.kind ~143-156, reactive filters ~511-517, dateRange ~500-509, loadTransactions, tablePagination)
    - el-templo-admin/src/components/caja/MovEgresosTab.vue (columna chip ~130-137, mapa de estados ~319-335)
    - .planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-PATTERNS.md (sección MovimientosTab, puntos 1-3)
  </read_first>
  <action>
    Chip (CAJA-02): agregar una columna `estado` con `#body-cell-estado` que renderiza un `q-chip`/`q-badge` con label+color del mapa de estados. Reusar el mapa de `MovEgresosTab.vue:319-335`; extraerlo a `src/utils/validation-status.ts` (DRY — discreción, recomendado) e importarlo en ambos tabs. Usa `row.validationStatus`.
    Filtro por estado (D-04): agregar `estado: null as 'validado' | 'pendiente' | null` al reactive `filters` y un single-select todas/validadas/pendientes espejando `filters.kind`; pasar el valor como query param `validationStatus` a `loadTransactions` (server-side, porque la tabla es paginada — el filtro client-side solo cubriría la página actual).
    Drill-down (CAJA-03): reemplazar el bloque `selectedMonth`/`dateRange` (~500-509) por `<DateRangeFilter>` (creado en Task 1), alimentando `loadTransactions` con `{ dateFrom, dateTo }`.
  </action>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `MovimientosTab.vue` tiene una columna/celda `estado` que renderiza el chip usando el mapa de labels/colores
    - Existe un filtro single-select de estado (todas/validadas/pendientes) que pasa `validationStatus` al listado
    - El listado usa `<DateRangeFilter>` para el rango de fecha (mes default + días)
    - Si se extrae el mapa: `src/utils/validation-status.ts` existe y lo importan ambos tabs
    - Sin `console.*` ni `any`; `npx vue-tsc --noEmit` verde
  </acceptance_criteria>
  <done>El Historial de cobros muestra el estado por fila, filtra por estado server-side y permite bajar a días.</done>
</task>

<task type="auto">
  <name>Task 3: MovimientosTab — validador + fecha en el dialog de detalle (CAJA-04, D-05/D-06)</name>
  <files>el-templo-admin/src/components/caja/MovimientosTab.vue</files>
  <read_first>
    - el-templo-admin/src/components/caja/MovimientosTab.vue (dialog de detalle ~294-386, bloque "Registrado por" ~355-359, helper formatDate)
    - .planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-PATTERNS.md (sección MovimientosTab punto 4)
  </read_first>
  <action>
    Extender el dialog de detalle (~294-386) agregando filas q-item en el bloque "Registrado por" (~355-359) con el mismo patrón q-item/q-item-section side. Ramificar según el dato de la fila: si `validatedBy`/`validatorName` presente → "Validado por {validatorName}" + `formatDate(validatedAt)`; si nació validado (validationStatus='validado' pero sin validatedAt/validatorName) → literal "Validado al registrar" + quién lo cargó (`recorderName`) + `formatDate(createdAt)` (D-06). No mostrar el bloque si el estado no es validado (queda pendiente/observado).
  </action>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - El dialog de detalle muestra "Validado por {nombre}" + fecha cuando hay validatorName
    - El dialog muestra "Validado al registrar" + recorderName + createdAt cuando el cobro nació validado (sin validatorName) — rama D-06
    - No se rompe el layout del bloque "Registrado por" existente
    - `grep -c "Validado al registrar" el-templo-admin/src/components/caja/MovimientosTab.vue` == 1
    - Sin `console.*` ni `any`; `npx vue-tsc --noEmit` verde
  </acceptance_criteria>
  <done>El detalle distingue el cobro validado por la bandeja (validador+fecha) del nacido validado ("Validado al registrar").</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                                                           |
| ----------------------------- | ----------------------------------------------------------------------------------------------------- |
| navegador → GET /transactions | el filtro de estado y el rango de fecha se envían como query params (validados server-side en 152-03) |

## STRIDE Threat Register

| Threat ID | Category               | Component                           | Disposition | Mitigation Plan                                                                                               |
| --------- | ---------------------- | ----------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------- |
| T-152-12  | Tampering              | filtro validationStatus desde la UI | mitigate    | el valor lo valida el enum del querystring server-side (152-03); la UI solo ofrece todas/validadas/pendientes |
| T-152-13  | Information Disclosure | validatorName en la tabla/detalle   | accept      | la Caja ya es admin/owner-only (149 D-04, 150 D-12); validatorName es staff                                   |
| T-152-SC  | Tampering              | npm/pip/cargo installs              | accept      | esta fase no instala paquetes nuevos                                                                          |

</threat_model>

<verification>
- `npx vue-tsc --noEmit` verde en el-templo-admin.
- Chip + filtro + drill-down en Historial de cobros; DateRangeFilter compartido en ambos tabs; validador en el detalle con rama D-06.
</verification>

<success_criteria>

- Historial de cobros con estado por fila, filtro por estado y filtro por día.
- DateRangeFilter compartido (DRY) usado por Historial y Movimientos.
- Detalle con validador+fecha o "Validado al registrar" según el caso.
  </success_criteria>

<output>
Create `.planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-05-SUMMARY.md` when done
</output>
