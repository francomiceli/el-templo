---
phase: 152-reorganizaci-n-de-caja-egresos-configurables
plan: 06
type: execute
wave: 4
depends_on: ["152-04", "152-05"]
files_modified:
  - el-templo-admin/src/composables/useTransactionsApi.ts
  - el-templo-admin/src/types/transaction.ts
  - el-templo-admin/src/components/caja/CategoriaEgresoFormDialog.vue
  - el-templo-admin/src/components/caja/CuentasTab.vue
autonomous: true
requirements: [CAJA-05]

must_haves:
  truths:
    - "En el tab Cuentas hay una sección 'Categorías de egreso' con el listado de centros de costo del país seleccionado"
    - "Se puede crear una categoría de egreso, renombrarla, desactivarla y reactivarla desde la UI"
    - "Las categorías desactivadas se muestran atenuadas con un badge (no se borran)"
    - "La sección respeta el selector de país existente de CajaPage (scope por país)"
  artifacts:
    - path: "el-templo-admin/src/components/caja/CategoriaEgresoFormDialog.vue"
      provides: "form dialog crear/renombrar categoría"
      contains: "name"
    - path: "el-templo-admin/src/components/caja/CuentasTab.vue"
      provides: "sección Categorías de egreso (tabla + acciones)"
      contains: "Categorías de egreso"
  key_links:
    - from: "el-templo-admin/src/components/caja/CuentasTab.vue"
      to: "cost-centers API (ABM)"
      via: "useTransactionsApi cost-center methods"
      pattern: "CostCenter"
    - from: "el-templo-admin/src/components/caja/CuentasTab.vue"
      to: "selector de país de CajaPage"
      via: "watch(props.selectedCountry)"
      pattern: "selectedCountry"
---

<objective>
ABM de categorías de egreso en la UI (CAJA-05, D-07/D-08). Dentro del tab Cuentas se agrega una segunda sección apilada "Categorías de egreso" que replica el patrón del ABM de cuentas bancarias: tabla con filas atenuadas + badge para las cerradas, acciones crear/renombrar/desactivar/reactivar, sin borrado físico. Nuevo `CategoriaEgresoFormDialog.vue` (copia simplificada de `CuentaBancariaFormDialog.vue`, solo `name`). Scopeado por el selector de país existente (D-08, nombre único por país) — a diferencia del ABM de cuentas que es país-agnóstico, este mantiene el `watch` de país.

Purpose: Consume las rutas de 152-04. Levanta EGR-F2 de v5.3 (el ABM estaba diferido). Nacho administra el catálogo white-label desde la UI sin tocar seeds.
Output: form dialog nuevo + sección en CuentasTab + api client + tipos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-CONTEXT.md
@.planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-PATTERNS.md
@.planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-04-SUMMARY.md

<interfaces>
<!-- Análogos exactos del ABM de cuentas bancarias en el mismo tab. Ver PATTERNS.md secciones CuentasTab / CategoriaEgresoFormDialog. -->
- Tabla + dimming: `CuentasTab.vue:19-87` — patrón `<q-tr :class="{ 'text-grey-5': !row.isActive }">` + `<q-badge ... label="Cerrada" />` (D-08 "cerradas atenuadas").
- Botones de acción: `CuentasTab.vue:39-77` — editar + toggle cerrar/reactivar (DROPEAR el botón "Registrar retiro"/payments, no aplica a categorías).
- Close-with-confirm: `CuentasTab.vue:188-218` — `$q.dialog` confirm → service → `load()`. Para categorías el warning es más simple (no hay saldo), misma forma try/catch + extractError + $q.notify + reload.
- Load/handlers/lifecycle: `CuentasTab.vue:147-259` — `load()` con extractError, openCreate/openEdit, onSaved, onMounted(load), `watch(() => props.selectedCountry, load)`, `onUnmounted(() => transactionsApi.cleanup())`. Las categorías SÍ son país-scoped → mantener el `watch` de país (única desviación vs cuentas bancarias, que es país-agnóstico).
- Form dialog análogo: `CuentaBancariaFormDialog.vue` (v-model computed `show`, `isEditMode` computed, `submit()` branch create/update, extractError+notify, `onUnmounted` cleanup). Colapsar a un solo `q-input` para `name` con regla required-trim (`CuentaBancariaFormDialog.vue:18-19`).
- Backend (152-04): `GET /cost-centers/all?country=..`, `POST /cost-centers`, `PATCH /cost-centers/:id`, `POST /cost-centers/:id/deactivate`, `POST /cost-centers/:id/reactivate`; tipo `CostCenter` en la API.
- Regla de proyecto: createLogger() nunca console.*, sin `any`, Pinia composition.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Métodos cost-center en useTransactionsApi + tipo CostCenter (api client)</name>
  <files>el-templo-admin/src/composables/useTransactionsApi.ts, el-templo-admin/src/types/transaction.ts</files>
  <read_first>
    - el-templo-admin/src/composables/useTransactionsApi.ts (métodos análogos de bank-account CRUD: createBankAccount/closeBankAccount/reactivateBankAccount + patrón de request/cleanup)
    - el-templo-admin/src/types/transaction.ts (tipos BankAccount, patrón de interfaces)
    - .planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-PATTERNS.md (sección types.ts / useTransactionsApi)
  </read_first>
  <action>
    En `useTransactionsApi.ts`: agregar métodos que consumen las rutas de 152-04, copiando el patrón de los métodos bank-account: `listAllCostCenters(country)` → `GET /cost-centers/all?country=`, `createCostCenter({ name, country })` → `POST /cost-centers`, `renameCostCenter(id, { name })` → `PATCH /cost-centers/:id`, `deactivateCostCenter(id)` → `POST /cost-centers/:id/deactivate`, `reactivateCostCenter(id)` → `POST /cost-centers/:id/reactivate`. Reusar el manejo de request/errores del composable; NO poner `onUnmounted` dentro del composable (regla de proyecto).
    En `types/transaction.ts`: agregar la interface `CostCenter` (`id: number; name: string; country: string; isActive: boolean`).
  </action>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `useTransactionsApi.ts` expone `listAllCostCenters`, `createCostCenter`, `renameCostCenter`, `deactivateCostCenter`, `reactivateCostCenter` (grep de cada nombre)
    - `types/transaction.ts` exporta la interface `CostCenter` con id/name/country/isActive
    - No hay `onUnmounted` dentro del composable
    - Sin `console.*` ni `any`; `npx vue-tsc --noEmit` verde
  </acceptance_criteria>
  <done>El api client tiene los cinco métodos del ABM de categorías y el tipo CostCenter.</done>
</task>

<task type="auto">
  <name>Task 2: CategoriaEgresoFormDialog.vue (crear/renombrar categoría)</name>
  <files>el-templo-admin/src/components/caja/CategoriaEgresoFormDialog.vue</files>
  <read_first>
    - el-templo-admin/src/components/caja/CuentaBancariaFormDialog.vue (estructura completa: v-model computed show, isEditMode, submit() branch create/update, extractError+notify, onUnmounted cleanup, regla required-trim ~18-19)
    - el-templo-admin/src/composables/useTransactionsApi.ts (los métodos creados en Task 1)
    - .planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-PATTERNS.md (sección CategoriaEgresoFormDialog)
  </read_first>
  <action>
    Crear `CategoriaEgresoFormDialog.vue` copiando la estructura de `CuentaBancariaFormDialog.vue` pero colapsando el form a un solo `q-input` para `name` con regla required-trim. `v-model` computed `show`; `isEditMode` computed según haya `props.categoria`; `submit()` ramifica `createCostCenter({ name, country })` (modo alta, country = país seleccionado) vs `renameCostCenter(id, { name })` (modo edición); manejo `try/catch (err: unknown)` con `extractError` + `$q.notify({ type: 'negative' })` + `log.error`; emite `saved` al éxito; `onUnmounted(() => transactionsApi.cleanup())` a nivel SFC. Recibe `selectedCountry` por prop para el alta.
  </action>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `CategoriaEgresoFormDialog.vue` existe con un `q-input` de `name` y regla required-trim
    - `submit()` ramifica alta (createCostCenter) vs edición (renameCostCenter)
    - Emite un evento `saved` al éxito y usa extractError + $q.notify en el catch
    - Usa `createLogger()`/`log`, no `console.*`; sin `any`
    - `npx vue-tsc --noEmit` verde
  </acceptance_criteria>
  <done>Form dialog reutilizable para crear y renombrar categorías de egreso.</done>
</task>

<task type="auto">
  <name>Task 3: Sección "Categorías de egreso" en CuentasTab (tabla + acciones + país scope)</name>
  <files>el-templo-admin/src/components/caja/CuentasTab.vue</files>
  <read_first>
    - el-templo-admin/src/components/caja/CuentasTab.vue (tabla+dimming ~19-87, botones ~39-77, close-confirm ~188-218, load/handlers/lifecycle ~147-259, watch selectedCountry, onUnmounted cleanup)
    - el-templo-admin/src/components/caja/CategoriaEgresoFormDialog.vue (Task 2)
    - .planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-PATTERNS.md (sección CuentasTab + Categorías de egreso ABM)
  </read_first>
  <action>
    Agregar una segunda sección apilada bajo el ABM de cuentas bancarias, con heading "Categorías de egreso" (D-07). Replicar el patrón: una tabla de centros de costo con filas atenuadas (`:class="{ 'text-grey-5': !row.isActive }"`) + `<q-badge ... label="Cerrada">` para inactivos (~19-87); botones editar (abre `CategoriaEgresoFormDialog` en modo edición) + toggle desactivar/reactivar (SIN el botón de retiro). Estado local propio: `costCenters` ref, `loadCostCenters()` que llama `transactionsApi.listAllCostCenters(props.selectedCountry)` con el mismo try/catch + extractError del análogo; `openCreateCategoria`/`openEditCategoria`; `onSavedCategoria` que recarga; confirm de desactivar con `$q.dialog` (warning simple, sin saldo) → `deactivateCostCenter` → reload; reactivar directo → reload. Llamar `loadCostCenters()` en `onMounted` junto al load de cuentas y agregarlo al `watch(() => props.selectedCountry, ...)` existente (las categorías SON país-scoped, D-08). Renombrar el tab si conviene ("Cuentas y categorías") es discreción; layout secciones apiladas.
  </action>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `CuentasTab.vue` tiene una sección con heading que contiene "Categorías de egreso"
    - La tabla de categorías atenúa las inactivas (text-grey-5) y les muestra un badge "Cerrada"
    - Hay acciones crear/renombrar (via CategoriaEgresoFormDialog) + desactivar/reactivar; ninguna borra físicamente
    - `loadCostCenters()` usa `props.selectedCountry` y está incluido en el `watch` de país + en `onMounted`
    - Sin `console.*` ni `any`; `npx vue-tsc --noEmit` verde
  </acceptance_criteria>
  <done>El tab Cuentas administra las categorías de egreso por país con baja lógica, atenuando las cerradas.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                              |
| ---------------------------------- | ---------------------------------------------------------------------------------------- |
| navegador → rutas ABM cost-centers | las acciones de la UI llegan a las rutas gated ADMIN_ROLES de 152-04; la UI solo esconde |

## STRIDE Threat Register

| Threat ID | Category               | Component                          | Disposition | Mitigation Plan                                                                                                                       |
| --------- | ---------------------- | ---------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| T-152-14  | Elevation of Privilege | acciones del ABM en la UI          | mitigate    | la autorización real vive en la API (152-04, guard ADMIN_ROLES); la UI oculta según rol pero no es la barrera de seguridad (149 D-04) |
| T-152-15  | Tampering              | alta/renombre con nombre duplicado | mitigate    | el backend (152-04) rechaza con 400 por unicidad; la UI muestra el error vía extractError+notify                                      |
| T-152-16  | Tampering              | scope de país                      | mitigate    | el país se toma del selector de CajaPage y se pasa como param; el backend re-resuelve owner-aware (152-04)                            |
| T-152-SC  | Tampering              | npm/pip/cargo installs             | accept      | esta fase no instala paquetes nuevos                                                                                                  |

</threat_model>

<verification>
- `npx vue-tsc --noEmit` verde en el-templo-admin.
- Sección Categorías de egreso operativa: crear/renombrar/desactivar/reactivar, atenuación de cerradas, scope por país.
</verification>

<success_criteria>

- El catálogo de categorías de egreso se administra desde la UI (levanta EGR-F2) con baja lógica y scope por país.
- Reusa el patrón del ABM de cuentas bancarias en el mismo tab (D-07).
- Las cerradas quedan atenuadas y visibles (D-08), sin borrado físico.
  </success_criteria>

<output>
Create `.planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-06-SUMMARY.md` when done
</output>
