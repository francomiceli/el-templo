---
phase: 152-reorganizaci-n-de-caja-egresos-configurables
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-admin/src/constants/caja.ts
  - el-templo-admin/src/pages/CajaPage.vue
  - el-templo-admin/src/components/caja/SaldosPorCajaTab.vue
autonomous: true
requirements: [CAJA-01, CAJA-06]

must_haves:
  truths:
    - "En Caja, 'Movimientos de caja' es el primer tab (portada), 'Pendientes' segundo, 'Historial de cobros' tercero, 'Saldos' cuarto, 'Cuentas' quinto"
    - "El tab que antes se llamaba 'Transacciones' ahora muestra el label 'Historial de cobros'"
    - "Al entrar a /caja sin ?tab=, aterriza en Movimientos de caja"
    - "La pantalla Saldos muestra una nota explicativa que aclara qué muestra (saldo firme por caja) y advierte que sin registrar egresos/retiros los saldos no reflejan la realidad"
  artifacts:
    - path: "el-templo-admin/src/constants/caja.ts"
      provides: "Orden de tabs + default = movimientosCaja"
      contains: "CAJA_DEFAULT_TAB"
    - path: "el-templo-admin/src/components/caja/SaldosPorCajaTab.vue"
      provides: "q-banner con la nota D-10"
      contains: "q-banner"
  key_links:
    - from: "el-templo-admin/src/pages/CajaPage.vue"
      to: "el-templo-admin/src/constants/caja.ts"
      via: "CAJA_TAB_NAMES / CAJA_DEFAULT_TAB"
      pattern: "CAJA_TAB_NAMES"
---

<objective>
Reorganización visual de la portada de Caja (CAJA-01) y nota explicativa de Saldos (CAJA-06). Reordenar los tabs a Movimientos de caja (portada) → Pendientes → Historial de cobros → Saldos → Cuentas (D-01), renombrar el label del tab "Transacciones" a "Historial de cobros" (D-02, sin tocar la key `transacciones` que sostiene el contrato `?tab=`), y agregar un `q-banner` fijo en Saldos con la nota que explica el saldo firme + el aviso de registrar egresos/retiros (D-10).

Purpose: Cierra dos quejas de Nacho (orden de tabs + Saldos no se entiende) sin backend ni dependencias — puede correr en paralelo con el cimiento de schema.
Output: 3 archivos de admin editados; sin API.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-CONTEXT.md
@.planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-PATTERNS.md

<interfaces>
<!-- Estado actual de constants/caja.ts (fase 141). El default y el orden cambian; las KEYS del objeto CAJA_TABS NO. -->
- `CAJA_TABS` = { pendientes, saldos, transacciones, movimientosCaja, cuentas } (keys inmutables — contrato ?tab=).
- Hoy `CAJA_DEFAULT_TAB = CAJA_TABS.pendientes` y `CAJA_TAB_NAMES = [pendientes, saldos, transacciones, movimientosCaja, cuentas]`.
- `CajaPage.vue` renderiza un bloque de `<q-tab>` (~líneas 31-37) + persistencia `?tab=` (~114-129) + selector de país arriba. El `vencidoCount` es un badge flotante ligado al tab Pendientes.
- "saldo firme" ya se verbaliza en `CuentasTab.vue:191-194` (reusar vocabulario para consistencia).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reordenar tabs + default (constants) y renombrar el label en CajaPage (D-01/D-02)</name>
  <files>el-templo-admin/src/constants/caja.ts, el-templo-admin/src/pages/CajaPage.vue</files>
  <read_first>
    - el-templo-admin/src/constants/caja.ts (CAJA_TABS, CAJA_DEFAULT_TAB, CAJA_TAB_NAMES)
    - el-templo-admin/src/pages/CajaPage.vue (bloque q-tab ~31-37, persistencia ?tab= ~114-129, badge vencidoCount)
    - .planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-PATTERNS.md (sección CajaPage.vue + constants/caja.ts)
  </read_first>
  <action>
    En `constants/caja.ts`: cambiar `CAJA_DEFAULT_TAB` a `CAJA_TABS.movimientosCaja` (portada D-01) y reordenar `CAJA_TAB_NAMES` a `[movimientosCaja, pendientes, transacciones, saldos, cuentas]`. NO tocar las keys del objeto `CAJA_TABS` (sostienen el contrato `?tab=`). Actualizar el comentario de encabezado que hoy dice "Pendientes (landing)" para reflejar el nuevo landing.
    En `CajaPage.vue`: reordenar el bloque de `<q-tab>` para que coincida con el nuevo orden (Movimientos de caja primero) y cambiar SOLO el label del tab `transacciones` de "Transacciones" a "Historial de cobros" (D-02). Mantener el badge `vencidoCount` ligado al tab Pendientes dondequiera que aterrice. No tocar la lógica de persistencia `?tab=`.
  </action>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'CAJA_TABS.movimientosCaja' el-templo-admin/src/constants/caja.ts` con `CAJA_DEFAULT_TAB` en la misma línea/asignación (default = movimientosCaja)
    - El primer elemento de `CAJA_TAB_NAMES` es `CAJA_TABS.movimientosCaja` y el orden es movimientosCaja, pendientes, transacciones, saldos, cuentas
    - `grep -c "Historial de cobros" el-templo-admin/src/pages/CajaPage.vue` == 1
    - Las keys de `CAJA_TABS` no cambiaron (siguen siendo pendientes/saldos/transacciones/movimientosCaja/cuentas)
    - `npx vue-tsc --noEmit` verde
  </acceptance_criteria>
  <done>Los tabs quedan en el orden D-01 con Movimientos de caja como default, el tab Transacciones muestra "Historial de cobros", y el contrato ?tab= sigue intacto.</done>
</task>

<task type="auto">
  <name>Task 2: Nota explicativa en Saldos (q-banner, D-10 / CAJA-06)</name>
  <files>el-templo-admin/src/components/caja/SaldosPorCajaTab.vue</files>
  <read_first>
    - el-templo-admin/src/components/caja/SaldosPorCajaTab.vue (top del template ~2-14, fila de export)
    - el-templo-admin/src/components/caja/CuentasTab.vue (líneas ~191-194, vocabulario "saldo firme" a reusar)
    - .planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-PATTERNS.md (sección SaldosPorCajaTab.vue)
  </read_first>
  <action>
    Agregar un `q-banner` fijo (no dismissible — la confusión es recurrente, D-10) al tope del template, antes de la fila de export. La nota combina en un solo bloque: (a) qué muestra la pantalla — "saldo firme por caja: movimientos validados desde el corte; los pendientes se muestran aparte" (reusar el vocabulario "saldo firme" de CuentasTab.vue:191-194); y (b) el aviso "si no se registran egresos y retiros, los saldos no reflejarán la realidad" (copy literal de CAJA-06). Copy final es discreción mientras cubra ambos puntos de D-10. Usar un ícono informativo del q-banner (p.ej. `info`).
  </action>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'q-banner' el-templo-admin/src/components/caja/SaldosPorCajaTab.vue` >= 1
    - El texto del banner contiene la frase de aviso sobre egresos/retiros (grep de "egresos" y "retiros" en el archivo dentro del bloque de la nota)
    - El texto del banner menciona el saldo firme / movimientos validados
    - El banner NO es dismissible (no hay acción que lo oculte permanentemente)
    - `npx vue-tsc --noEmit` verde
  </acceptance_criteria>
  <done>Saldos muestra un banner fijo que explica qué es el saldo firme y advierte sobre registrar egresos/retiros.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                                                     |
| --------------------- | ------------------------------------------------------------------------------- |
| navegador → admin SPA | esta superficie es puramente presentacional; no cruza datos nuevos ni endpoints |

## STRIDE Threat Register

| Threat ID | Category  | Component              | Disposition | Mitigation Plan                                                                                            |
| --------- | --------- | ---------------------- | ----------- | ---------------------------------------------------------------------------------------------------------- |
| T-152-04  | Tampering | contrato ?tab=         | mitigate    | no se tocan las keys de CAJA_TABS; solo cambia orden y label, la persistencia de query-param queda intacta |
| T-152-SC  | Tampering | npm/pip/cargo installs | accept      | esta fase no instala paquetes nuevos                                                                       |

</threat_model>

<verification>
- `npx vue-tsc --noEmit` verde en el-templo-admin.
- Orden de tabs y default correctos en constants; label "Historial de cobros" en CajaPage; banner presente en Saldos.
</verification>

<success_criteria>

- Portada de Caja = Movimientos de caja; orden D-01 completo; tab Transacciones renombrado a "Historial de cobros".
- Saldos con nota explicativa fija (qué muestra + aviso egresos/retiros).
- Contrato ?tab= sin romperse.
  </success_criteria>

<output>
Create `.planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-02-SUMMARY.md` when done
</output>
