---
phase: 152-reorganizaci-n-de-caja-egresos-configurables
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/schema/financial-transactions.ts
  - el-templo-api/src/db/schema/cost-centers.ts
  - el-templo-api/src/db/migrations/0165_validated_by_and_cost_center_abm.sql
autonomous: true
requirements: [CAJA-04, CAJA-05]

must_haves:
  truths:
    - "financial_transactions tiene columnas validated_by (int NULL FK users) y validated_at (timestamp NULL)"
    - "cost_centers tiene un índice único sobre (name, country)"
    - "Los seeds Templo-céntricos quedan renombrados a genéricos (Alquiler, Viáticos) y existe 'Pago a proveedores' (AR)"
    - "Las validaciones históricas quedan backfilleadas con validated_by/validated_at desde audit_log"
  artifacts:
    - path: "el-templo-api/src/db/migrations/0165_validated_by_and_cost_center_abm.sql"
      provides: "Columns + backfill + seed renames + unique index"
      contains: "validated_by"
    - path: "el-templo-api/src/db/schema/financial-transactions.ts"
      provides: "validatedBy/validatedAt columns + validator relation"
      contains: "validated_by"
    - path: "el-templo-api/src/db/schema/cost-centers.ts"
      provides: "uniqueIndex (name, country)"
      contains: "uq_cost_centers_name_country"
  key_links:
    - from: "el-templo-api/src/db/schema/financial-transactions.ts"
      to: "0165 migration"
      via: "column names byte-for-byte"
      pattern: "validated_by"
    - from: "el-templo-api/src/db/schema/cost-centers.ts"
      to: "0165 migration"
      via: "unique index name byte-for-byte"
      pattern: "uq_cost_centers_name_country"
---

<objective>
Cimiento de schema + migración de la fase 152. Agrega las columnas `validated_by`/`validated_at` a `financial_transactions` (D-05, read path denormalizado del validador) y el índice único `(name, country)` sobre `cost_centers` (D-08, unicidad del ABM), más los renames de seeds Templo-céntricos a genéricos y el seed "Pago a proveedores" (D-09). Todo en una única migración `0165`.

Purpose: Habilita el read path del validador (152-03/152-05) y el ABM de centros de costo (152-04/152-06) sin que ninguno tenga que tocar schema ni migración.
Output: 2 schemas Drizzle extendidos + migración `0165` escrita a mano y aplicada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-CONTEXT.md
@.planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-PATTERNS.md

<interfaces>
<!-- Convenciones existentes que el executor DEBE replicar. Ver PATTERNS.md para line refs. -->
- `financial-transactions.ts` ya tiene el par nullable-FK `voidedBy: int("voided_by").references(() => users.id)` + `voidedAt: timestamp("voided_at")` (líneas ~74-75) y la relación `voider` (~152-156). El par `validatedBy`/`validatedAt` + relación `validator` es una copia exacta de ese template.
- `financial-transactions.ts` ya importa `uniqueIndex` (usado en `uq_financial_tx_idempotency_key`).
- `cost-centers.ts` importa `index` pero NO `uniqueIndex` — hay que agregar el import.
- Migración análoga de columnas encadenadas + seed idempotente: `0163_bank_accounts_and_retiros.sql`. Migración análoga de seed `FROM DUAL WHERE NOT EXISTS`: `0161_cost_centers.sql`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender los schemas Drizzle (validated_by/at + validator relation + unique index)</name>
  <files>el-templo-api/src/db/schema/financial-transactions.ts, el-templo-api/src/db/schema/cost-centers.ts</files>
  <read_first>
    - el-templo-api/src/db/schema/financial-transactions.ts (par voidedBy/voidedAt ~74-75, relación voider ~152-156, enum validationStatus fase 137, uso de uniqueIndex)
    - el-templo-api/src/db/schema/cost-centers.ts (tabla completa; imports)
    - .planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-PATTERNS.md (secciones "financial-transactions.ts" y "cost-centers.ts")
  </read_first>
  <action>
    En `financial-transactions.ts`: agregar dos columnas nullable inmediatamente después de `validationStatus`: `validatedBy: int("validated_by").references(() => users.id)` y `validatedAt: timestamp("validated_at")`. Copiar el comentario que explica que son NULLABLE — solo la transición pendiente→validado (transaction-service.validate) las setea; los cobros nacidos validados (correct/admin-load) y todo histórico no-backfilleado quedan NULL (D-06). Agregar la relación `validator` en `financialTransactionsRelations` espejando `voider`: `one(users, { fields: [financialTransactions.validatedBy], references: [users.id], relationName: "financialTxValidator" })`. Los nombres de columna DEBEN coincidir byte-for-byte con la migración 0165 (`validated_by`, `validated_at`).
    En `cost-centers.ts`: agregar `uniqueIndex` al import desde `drizzle-orm/mysql-core` y agregar al array de índices `uniqueIndex("uq_cost_centers_name_country").on(table.name, table.country)`. Nombre del índice byte-for-byte con la migración.
  </action>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'validated_by' el-templo-api/src/db/schema/financial-transactions.ts` >= 1 y `grep -c 'validated_at'` >= 1
    - `grep -c 'financialTxValidator' el-templo-api/src/db/schema/financial-transactions.ts` == 1
    - `grep -c 'uq_cost_centers_name_country' el-templo-api/src/db/schema/cost-centers.ts` == 1
    - `grep -c 'uniqueIndex' el-templo-api/src/db/schema/cost-centers.ts` >= 2 (import + uso)
    - `npx tsc --noEmit` en el-templo-api termina con exit 0
  </acceptance_criteria>
  <done>Ambos schemas compilan con las columnas y el índice nuevos, con nombres de columna/índice idénticos a los que usará la migración.</done>
</task>

<task type="auto">
  <name>Task 2: Escribir la migración 0165 (columnas + backfill + renames de seeds + Pago a proveedores + unique index)</name>
  <files>el-templo-api/src/db/migrations/0165_validated_by_and_cost_center_abm.sql</files>
  <read_first>
    - el-templo-api/src/db/migrations/0163_bank_accounts_and_retiros.sql (header boilerplate + ALTER ADD COLUMN chain + seed idempotente)
    - el-templo-api/src/db/migrations/0161_cost_centers.sql (seed FROM DUAL WHERE NOT EXISTS + seeds AR existentes: 'Alquiler Constitución', 'Viáticos profes', 'Librería', 'Varios')
    - el-templo-api/src/db/schema/audit-log.ts (columnas target_kind, target_id, action, actor_id, created_at — fuente del backfill)
    - .planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-PATTERNS.md (sección "0165_*.sql", cuatro bloques ordenados)
  </read_first>
  <action>
    Crear `0165_validated_by_and_cost_center_abm.sql` (última en disco = 0164, verificar con `ls src/db/migrations/ | tail`). Escribir a mano (db:generate roto por drift pre-existente). NUNCA usar `;` dentro de comentarios `--` (el runner splittea por `;` antes de strippear `--`). Cuatro bloques ordenados:
    (1) `ALTER TABLE financial_transactions ADD COLUMN validated_by int NULL AFTER validation_status, ADD COLUMN validated_at timestamp NULL AFTER validated_by;` seguido de `ADD CONSTRAINT financial_transactions_validated_by_users_id_fk FOREIGN KEY (validated_by) REFERENCES users(id);`.
    (2) Backfill: `UPDATE financial_transactions ft JOIN audit_log al ON al.target_kind='transaction' AND al.target_id=ft.id AND al.action='transaction_validated' SET ft.validated_by=al.actor_id, ft.validated_at=al.created_at WHERE ft.validated_by IS NULL;`. Verificar en audit-log.ts los nombres reales de las columnas (`target_kind`/`target_id`/`action`/`actor_id`) antes de escribir; validate() escribe exactamente un evento por tx, así que el JOIN plano no multiplica.
    (3) Renames de seeds (D-09), UPDATE por (name, country), NO delete: `UPDATE cost_centers SET name='Alquiler' WHERE name='Alquiler Constitución' AND country='AR';` y `UPDATE cost_centers SET name='Viáticos' WHERE name='Viáticos profes' AND country='AR';`.
    (4) Seed idempotente 'Pago a proveedores' (AR) con el patrón `INSERT ... SELECT ... FROM DUAL WHERE NOT EXISTS (SELECT 1 FROM cost_centers WHERE name='Pago a proveedores' AND country='AR');` + `ALTER TABLE cost_centers ADD UNIQUE INDEX uq_cost_centers_name_country (name, country);`. El ADD UNIQUE INDEX debe correr DESPUÉS de los renames para no colisionar.
    Copiar el header comentado de 0163. Nombres de columna/índice byte-for-byte con los schemas de la Task 1.
  </action>
  <verify>
    <automated>test -f el-templo-api/src/db/migrations/0165_validated_by_and_cost_center_abm.sql && ! grep -nE '^\s*--.*;' el-templo-api/src/db/migrations/0165_validated_by_and_cost_center_abm.sql</automated>
  </verify>
  <acceptance_criteria>
    - El archivo `0165_validated_by_and_cost_center_abm.sql` existe
    - Ningún comentario `--` contiene `;` (grep `'^\s*--.*;'` no matchea ninguna línea)
    - `grep -c 'validated_by' 0165_*.sql` >= 2 (ADD COLUMN + FK + backfill)
    - `grep -c "Pago a proveedores" 0165_*.sql` >= 1 y `grep -c 'uq_cost_centers_name_country'` == 1
    - `grep -c "Alquiler Constitución" 0165_*.sql` == 1 y `grep -c "Viáticos profes"` == 1 (renames por nombre viejo)
    - Los nombres de columna en el SQL coinciden con los de la Task 1 (`validated_by`, `validated_at`)
  </acceptance_criteria>
  <done>Migración 0165 escrita, idempotente en los seeds, sin `;` en comentarios, con nombres de columna/índice idénticos al schema.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Aplicar la migración 0165 y verificar el estado del schema</name>
  <files>el-templo-api/src/db/migrations/0165_validated_by_and_cost_center_abm.sql</files>
  <read_first>
    - el-templo-api/src/db/run-migrations.ts (runner custom; tabla _migrations es la fuente de verdad)
    - CLAUDE.md (Database Changes: pnpm db:migrate, NUNCA drizzle-kit migrate ni db:push)
  </read_first>
  <action>
    Correr `pnpm db:migrate` (custom runner `src/db/run-migrations.ts`, trackea en la tabla `_migrations`). NUNCA `drizzle-kit migrate` ni `db:push`. Esta task es BLOCKING: debe completarse ANTES de que 152-03 y 152-04 corran, porque ambos leen/escriben las columnas y el índice nuevos. Tras aplicar, verificar contra la DB local que las columnas y el índice existen y que los seeds quedaron renombrados. Si `db:migrate` falla por sintaxis, corregir el SQL de la Task 2 (no amendar; el runner re-corre solo las no-aplicadas).
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm db:migrate 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm db:migrate` termina sin error y reporta 0165 como aplicada
    - `DESCRIBE financial_transactions` (o `SHOW COLUMNS`) incluye `validated_by` y `validated_at`
    - `SHOW INDEX FROM cost_centers` incluye `uq_cost_centers_name_country`
    - `SELECT name FROM cost_centers WHERE country='AR'` NO contiene 'Alquiler Constitución' ni 'Viáticos profes', SÍ contiene 'Alquiler', 'Viáticos' y 'Pago a proveedores'
    - La tabla `_migrations` contiene una fila para `0165_validated_by_and_cost_center_abm`
  </acceptance_criteria>
  <done>Migración 0165 aplicada a la DB local; columnas, índice único y renames de seeds verificados por query.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                                                       |
| ------------------------------ | ------------------------------------------------------------------------------------------------- |
| migración → DB prod compartida | 0165 viaja a prod con el tren staging→master y muta la caja real de Nacho (renames de seeds D-09) |

## STRIDE Threat Register

| Threat ID | Category          | Component                                   | Disposition | Mitigation Plan                                                                                                                               |
| --------- | ----------------- | ------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| T-152-01  | Tampering         | rename de seeds en 0165                     | mitigate    | UPDATE por (name, country) exacto, NO delete; ninguna categoría en uso se pierde (D-09 decidido con impacto prod explícito)                   |
| T-152-02  | Denial of Service | backfill JOIN audit_log                     | mitigate    | validate() escribe exactamente 1 evento `transaction_validated` por tx → JOIN plano no multiplica; verificado en read_first antes de escribir |
| T-152-03  | Tampering         | ADD UNIQUE INDEX sobre seeds pre-existentes | mitigate    | los seeds actuales ya son distintos por (name, country); el índice se agrega DESPUÉS de los renames para no colisionar                        |
| T-152-SC  | Tampering         | npm/pip/cargo installs                      | accept      | esta fase no instala paquetes nuevos                                                                                                          |

</threat_model>

<verification>
- `npx tsc --noEmit` en el-templo-api verde con los schemas nuevos.
- `pnpm db:migrate` aplica 0165 limpio; DESCRIBE / SHOW INDEX / SELECT confirman columnas, índice y renames.
</verification>

<success_criteria>

- financial_transactions tiene validated_by (FK users, NULL) y validated_at (timestamp, NULL).
- cost_centers tiene índice único (name, country).
- Seeds AR renombrados a genéricos + 'Pago a proveedores' presente; validaciones históricas backfilleadas.
- Schemas y migración con nombres de columna/índice byte-for-byte idénticos.
  </success_criteria>

<output>
Create `.planning/phases/152-reorganizaci-n-de-caja-egresos-configurables/152-01-SUMMARY.md` when done
</output>
