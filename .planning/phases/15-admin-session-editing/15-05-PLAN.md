---
phase: 15-admin-session-editing
plan: 05
type: execute
wave: 3
depends_on: ["15-03", "15-04"]
files_modified:
  - el-templo-admin/src/pages/SessionEditPage.vue
  - el-templo-admin/src/components/sessions/EditableExerciseRow.vue
  - el-templo-admin/src/components/sessions/EditableBlockCard.vue
  - el-templo-admin/src/router/routes.ts
autonomous: true

must_haves:
  truths:
    - "Coach can navigate to an edit page for any session"
    - "Each exercise shows inline swap button and editable prescription fields"
    - "Each block shows editable header with format and exercises"
  artifacts:
    - path: "el-templo-admin/src/pages/SessionEditPage.vue"
      provides: "Session edit page with block cards and exercise rows"
      min_lines: 100
    - path: "el-templo-admin/src/components/sessions/EditableExerciseRow.vue"
      provides: "Exercise row with inline edit controls and swap button"
      min_lines: 50
    - path: "el-templo-admin/src/components/sessions/EditableBlockCard.vue"
      provides: "Block card with editable exercises and format controls"
      min_lines: 80
  key_links:
    - from: "el-templo-admin/src/pages/SessionEditPage.vue"
      to: "el-templo-admin/src/composables/useEditApi.ts"
      via: "composable import for API calls"
      pattern: "useEditApi"
    - from: "el-templo-admin/src/router/routes.ts"
      to: "el-templo-admin/src/pages/SessionEditPage.vue"
      via: "route registration"
      pattern: "SessionEditPage"
---

<objective>
Create the session edit page with editable block cards and exercise rows. Coaches can view a session and modify exercise prescriptions inline, with swap buttons on each exercise.

Purpose: Core editing UI that coaches interact with. This is the main page for modifying sessions.
Output: SessionEditPage, EditableBlockCard, EditableExerciseRow components, and route registration.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-admin-session-editing/15-CONTEXT.md
@.planning/phases/15-admin-session-editing/15-RESEARCH.md
@.planning/phases/15-admin-session-editing/15-03-SUMMARY.md
@.planning/phases/15-admin-session-editing/15-04-SUMMARY.md
@el-templo-admin/src/pages/SessionDetailPage.vue
@el-templo-admin/src/components/sessions/BlockCard.vue
@el-templo-admin/src/components/sessions/ExerciseRow.vue
@el-templo-admin/src/types/session.ts
@el-templo-admin/src/composables/useEditApi.ts
@el-templo-admin/src/composables/useSessionsApi.ts
@el-templo-admin/src/router/routes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EditableExerciseRow component</name>
  <files>el-templo-admin/src/components/sessions/EditableExerciseRow.vue</files>
  <action>
    Create `el-templo-admin/src/components/sessions/EditableExerciseRow.vue`.

    This extends the read-only ExerciseRow with inline editing capabilities.
    Per user decision: inline swap button on each exercise card (no separate edit mode).

    Template structure:
    - QItem layout similar to ExerciseRow but with editable fields
    - Left section: exercise name with contraction badge (CON/EXC/ISO colored chip)
    - Middle section: inline editable fields in a row:
      - QInput (dense, type=number) for reps (show when exercise has reps)
      - QInput (dense, type=number) for seconds (show when exercise is time-based)
      - QInput (dense, type=number) for rest (always show, in seconds)
      - QInput (dense) for notes (optional, smaller)
    - Right section: action buttons
      - Swap button (swap_horiz icon) - emits 'swap' with exercise data
      - Remove button (delete icon, color negative) - emits 'remove' with prescriptionId
    - Show dificultadLineal as small caption badge if present

    Props:
    - exercise: SessionExercise
    - sessionId: number
    - blockId: number

    Emits:
    - 'swap': { exercise: SessionExercise } - triggers swap dialog opening
    - 'remove': { prescriptionId: number } - triggers exercise removal
    - 'update': { prescriptionId: number, fields: PrescriptionUpdate } - triggers field save

    Prescription field editing behavior:
    - Use @blur or debounced @update:model-value to emit 'update' when field changes
    - Use local refs for field values, initialized from props
    - Compare local value with prop value before emitting (avoid unnecessary API calls)
    - Fields: reps (number), seconds (number), rest (number), notes (string)
    - Per user decision: full prescription editing including reps, sets, rest, tempo, notes

    Visual styling:
    - Compact layout using dense Quasar inputs
    - Same contraction badge colors as ExerciseRow (CON=blue-grey, EXC=teal, ISO=orange)
    - Swap button: flat, dense, primary color
    - Remove button: flat, dense, negative color, with confirmation tooltip
  </action>
  <verify>Build compiles: `cd /home/franco/projects/el-templo/el-templo-admin && npx vue-tsc --noEmit 2>&1 | head -20`</verify>
  <done>EditableExerciseRow component renders exercise with inline-editable prescription fields, swap button, and remove button</done>
</task>

<task type="auto">
  <name>Task 2: Create EditableBlockCard and SessionEditPage</name>
  <files>
    el-templo-admin/src/components/sessions/EditableBlockCard.vue
    el-templo-admin/src/pages/SessionEditPage.vue
    el-templo-admin/src/router/routes.ts
  </files>
  <action>
    **EditableBlockCard.vue:**
    Extends BlockCard with editing capabilities. Same visual structure (colored header by block role, stats section, exercise list) but with editing controls.

    Template:
    - Colored header: block role name, route, format badge
    - Stats row: exercise count, reps budget, intensity%, avg difficulty
    - Exercise list using EditableExerciseRow (not plain ExerciseRow)
    - Footer actions:
      - "Agregar Ejercicio" button (add icon) - emits 'add-exercise' with blockId
      - Format currently displayed as text - will be made into dropdown in plan 15-07

    Props:
    - block: SessionBlock (from session detail)
    - sessionId: number

    Emits:
    - 'swap-exercise': { blockId, exercise } - bubble up from EditableExerciseRow
    - 'remove-exercise': { blockId, prescriptionId } - bubble up
    - 'update-prescription': { blockId, prescriptionId, fields } - bubble up
    - 'add-exercise': { blockId } - from add button
    - 'refresh': void - request parent to reload session data

    Handles 'update' emit from EditableExerciseRow by calling useEditApi().updatePrescription, then emitting 'refresh'.
    Handles 'remove' emit from EditableExerciseRow by showing QDialog confirmation, calling useEditApi().removeExercise, then emitting 'refresh'.

    **SessionEditPage.vue:**
    Main edit page. Similar to SessionDetailPage but uses EditableBlockCard instead of BlockCard.

    Template:
    - Header: "Editar Sesion - Semana X - Dia" with back button to /sessions/:id
    - Session meta card: level, block count, status badge
    - Action bar:
      - "Resetear al Algoritmo" button (restore icon) - calls resetToAlgorithm, shows only when session has algorithmSnapshot
      - Approve/Revert buttons (same as detail page)
    - Block list: EditableBlockCard for each block
    - Uses useSessionsApi().fetchSessionDetail to load session
    - Uses useEditApi() for all editing operations

    Script:
    - Load session on mount using route param id
    - Provide reload function that re-fetches session after any edit
    - Wire up all events from EditableBlockCard to API calls
    - Show Quasar Notify toasts for success/error on each operation
    - Per user decision: editing approved session auto-reverts to pending (handled by API, UI refreshes to show new status)

    **Router:**
    Add route: `{ path: 'sessions/:id/edit', component: () => import('pages/SessionEditPage.vue') }`
    Also add an "Editar" button to the existing SessionDetailPage that navigates to the edit page.

    NOTE: The exercise swap dialog and format change dropdown are NOT in this plan. They will be added in plans 15-06 and 15-07 respectively. For now, the swap button on EditableExerciseRow will emit the event, and the page will handle opening the dialog (placeholder for now that shows a "Proximamente" toast). Similarly, format is displayed as text only.
  </action>
  <verify>
    1. Build compiles: `cd /home/franco/projects/el-templo/el-templo-admin && npx vue-tsc --noEmit 2>&1 | head -20`
    2. Dev server starts: `cd /home/franco/projects/el-templo/el-templo-admin && timeout 10 npx quasar dev 2>&1 | tail -5` (check for compilation success)
  </verify>
  <done>SessionEditPage renders session with EditableBlockCard components. Prescription editing works inline. Route registered at /sessions/:id/edit. Edit button added to SessionDetailPage header.</done>
</task>

</tasks>

<verification>
- Navigate to /sessions/1/edit shows editable session
- Each exercise row has editable reps/seconds/rest/notes fields
- Each exercise has swap and remove buttons
- Each block has "add exercise" button
- Back navigation returns to session detail
- Edit button on detail page navigates to edit page
</verification>

<success_criteria>
- SessionEditPage loads session data and renders EditableBlockCard per block
- EditableExerciseRow shows inline-editable fields with blur-save behavior
- Prescription updates call API and show toast feedback
- Exercise removal shows confirmation dialog then calls API
- Route /sessions/:id/edit registered and accessible
- Edit button added to SessionDetailPage
</success_criteria>

<output>
After completion, create `.planning/phases/15-admin-session-editing/15-05-SUMMARY.md`
</output>
