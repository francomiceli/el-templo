---
phase: 15-admin-session-editing
plan: 04
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - el-templo-admin/src/types/session.ts
  - el-templo-admin/src/composables/useEditApi.ts
autonomous: true

must_haves:
  truths:
    - "Frontend types cover all editing data structures"
    - "API composable provides methods for all editing endpoints"
  artifacts:
    - path: "el-templo-admin/src/types/session.ts"
      provides: "Extended types for editing (PoolExercise, CompatibleFormat, etc.)"
      contains: "PoolExercise"
    - path: "el-templo-admin/src/composables/useEditApi.ts"
      provides: "Composable with all editing API methods"
      exports: ["useEditApi"]
  key_links:
    - from: "el-templo-admin/src/composables/useEditApi.ts"
      to: "/admin/exercises/pool"
      via: "axios GET"
      pattern: "api\\.get.*exercises/pool"
---

<objective>
Create frontend types and API composable for all session editing operations.

Purpose: Type-safe frontend layer for all editing API calls. This composable will be consumed by the edit page and its sub-components.
Output: Extended types and useEditApi composable ready for UI consumption.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-admin-session-editing/15-CONTEXT.md
@.planning/phases/15-admin-session-editing/15-RESEARCH.md
@el-templo-admin/src/types/session.ts
@el-templo-admin/src/composables/useSessionsApi.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend session types for editing operations</name>
  <files>el-templo-admin/src/types/session.ts</files>
  <action>
    Add to the existing `el-templo-admin/src/types/session.ts` (do NOT remove existing types):

    ```typescript
    // Exercise pool for swap dialog
    export interface PoolExercise {
      id: number;
      exercise: string;        // exercise name
      effort: string;          // CON, EXC, ISO
      dificultadLineal: number;
      pattern: string;
      route: string;
    }

    export interface ExercisePoolResponse {
      exercises: PoolExercise[];
    }

    // Compatible formats for format change dropdown
    export interface CompatibleFormat {
      formatId: number;
      formatName: string;
      compatibility: number;   // score, higher = more compatible
    }

    export interface CompatibleFormatsResponse {
      formats: CompatibleFormat[];
    }

    // Prescription update payload
    export interface PrescriptionUpdate {
      reps?: number;
      seconds?: number;
      rest?: number;
      notes?: string;
    }

    // Session preview for member preview modal
    export interface SessionPreview {
      id: number;
      dayId: string;
      week: number;
      day: string;
      memberLevel: string;
      levelGroup: string;
      blocks: SessionBlock[];
    }
    ```
  </action>
  <verify>TypeScript compiles: `cd /home/franco/projects/el-templo/el-templo-admin && npx vue-tsc --noEmit 2>&1 | head -20` (check for new type errors only)</verify>
  <done>All editing-related types defined: PoolExercise, CompatibleFormat, PrescriptionUpdate, SessionPreview</done>
</task>

<task type="auto">
  <name>Task 2: Create useEditApi composable with all editing methods</name>
  <files>el-templo-admin/src/composables/useEditApi.ts</files>
  <action>
    Create `el-templo-admin/src/composables/useEditApi.ts` following the same pattern as useSessionsApi.ts:

    ```typescript
    import { ref } from 'vue';
    import { api } from 'src/boot/axios';
    import type { ... } from 'src/types/session';

    export function useEditApi() {
      const loading = ref(false);
      const error = ref<string | null>(null);

      // Exercise pool for swap
      async function fetchExercisePool(params: {
        route: string;
        blockId: number;
        contraction?: string;
        pattern?: string;
      }): Promise<ExercisePoolResponse> { ... }

      // Swap exercise
      async function swapExercise(
        sessionId: number,
        blockId: number,
        prescriptionId: number,
        newExerciseId: number
      ): Promise<void> { ... }

      // Update prescription fields
      async function updatePrescription(
        sessionId: number,
        blockId: number,
        prescriptionId: number,
        fields: PrescriptionUpdate
      ): Promise<void> { ... }

      // Change block format
      async function changeBlockFormat(
        sessionId: number,
        blockId: number,
        formatId: number,
        formatName: string
      ): Promise<void> { ... }

      // Add exercise to block
      async function addExercise(
        sessionId: number,
        blockId: number,
        exerciseId: number
      ): Promise<void> { ... }

      // Remove exercise from block
      async function removeExercise(
        sessionId: number,
        blockId: number,
        prescriptionId: number
      ): Promise<void> { ... }

      // Reset to algorithm
      async function resetToAlgorithm(sessionId: number): Promise<void> { ... }

      // Get compatible formats
      async function fetchCompatibleFormats(params: {
        blockRole: string;
        level: string;
        intensity: number;
      }): Promise<CompatibleFormatsResponse> { ... }

      // Get member preview
      async function fetchPreview(
        sessionId: number,
        memberLevel?: string
      ): Promise<SessionPreview> { ... }

      return {
        loading, error,
        fetchExercisePool, swapExercise, updatePrescription,
        changeBlockFormat, addExercise, removeExercise,
        resetToAlgorithm, fetchCompatibleFormats, fetchPreview,
      };
    }
    ```

    Each method follows the same try/catch/finally pattern as useSessionsApi:
    - Set loading=true, error=null
    - Make API call via `api.get/post/patch/delete`
    - Catch errors with Spanish fallback messages
    - Set loading=false in finally
  </action>
  <verify>TypeScript compiles: `cd /home/franco/projects/el-templo/el-templo-admin && npx vue-tsc --noEmit 2>&1 | head -20`</verify>
  <done>useEditApi composable exports all 9 methods matching the API endpoints, with loading/error state management</done>
</task>

</tasks>

<verification>
- Types compile without errors
- Composable exports all methods
- API paths match the routes defined in plan 15-03
- Error handling follows existing useSessionsApi pattern
</verification>

<success_criteria>
- PoolExercise, CompatibleFormat, PrescriptionUpdate, SessionPreview types defined
- useEditApi composable has methods for all 9 editing operations
- Loading and error state managed reactively
- Spanish error fallback messages
</success_criteria>

<output>
After completion, create `.planning/phases/15-admin-session-editing/15-04-SUMMARY.md`
</output>
