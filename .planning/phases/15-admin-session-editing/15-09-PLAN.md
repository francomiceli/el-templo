---
phase: 15-admin-session-editing
plan: 09
type: execute
wave: 5
depends_on: ["15-07", "15-08"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Coach can swap an exercise and see updated prescription"
    - "Coach can edit reps/seconds/rest/notes inline"
    - "Coach can change block format and see re-prescribed exercises"
    - "Coach can add and remove exercises from a block"
    - "Budget bar reflects changes in real-time"
    - "Editing an approved session reverts it to pending"
    - "Reset to algorithm restores original session"
    - "Preview shows member-facing view"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete session editing workflow.

Purpose: Validate all editing features work end-to-end before marking the phase complete.
Output: Verified editing workflow or list of issues to fix.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-admin-session-editing/15-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete session editing system:
    - Database: edit logs table, algorithm snapshots, format params column
    - API: 9 new endpoints for exercise pool, swap, prescription edit, format change, add/remove, reset, compatible formats, preview
    - UI: Session edit page with editable block cards, inline prescription editing, exercise swap dialog, budget bar, contraction mix badge, format dropdown, add/remove exercise, member preview modal, reset to algorithm
  </what-built>
  <how-to-verify>
    Start both servers:
    1. API: `cd /home/franco/projects/el-templo/el-templo-api && npx tsx src/server.ts`
    2. Admin: `cd /home/franco/projects/el-templo/el-templo-admin && npx quasar dev`

    Then verify at http://localhost:9100:

    **1. Navigate to edit page:**
    - Go to Sessions list, click on any session
    - From detail page, click "Editar" button
    - Verify: edit page loads with all blocks and exercises editable

    **2. Inline prescription editing:**
    - Change reps value on an exercise, click away (blur)
    - Verify: value saved (reload page to confirm persistence)
    - Change rest and notes similarly

    **3. Exercise swap:**
    - Click swap button on any exercise
    - Verify: dialog opens with filtered exercise list
    - Verify: exercises show name, CON/EXC/ISO badge, difficulty, pattern badge
    - Filter by contraction type
    - Select an exercise to swap
    - Verify: exercise replaced, prescription updated by algorithm

    **4. Format change:**
    - In a block header, change the format dropdown
    - Verify: toast shows "Formato cambiado"
    - Verify: all exercises in block re-prescribed

    **5. Add/Remove exercise:**
    - Click "Agregar Ejercicio" on a block
    - Select an exercise from the pool
    - Verify: exercise added with blank prescription
    - Click remove on the new exercise
    - Verify: exercise removed after confirmation

    **6. Budget bar:**
    - Observe budget bar colors on each block
    - Edit reps to exceed budget by >10%
    - Verify: bar turns red

    **7. Reset to Algorithm:**
    - Make several edits to a session
    - Click "Resetear al Algoritmo"
    - Verify: session restored to original state

    **8. Auto-revert to pending:**
    - Find an approved session (approve one if needed)
    - Navigate to its edit page
    - Make any edit (change reps)
    - Verify: status changes from "approved" to "pending_review"

    **9. Member preview:**
    - Click "Vista Previa" on edit page
    - Verify: clean read-only modal opens
    - Switch level in dropdown
    - Verify: preview updates for different level

    **10. Preview from sessions list:**
    - Back in sessions list, click preview icon on a session row
    - Verify: preview dialog opens correctly
  </how-to-verify>
  <resume-signal>Type "approved" if all 10 checks pass, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
All 10 verification steps pass.
</verification>

<success_criteria>
- All 7 phase success criteria from ROADMAP met:
  1. Coach can swap exercises within a block
  2. Coach can modify prescription (reps, sets, rest times)
  3. Coach can change block format
  4. Coach can add/remove exercises from a block
  5. Edit history tracked
  6. Validation prevents invalid sessions (soft warnings)
  7. Preview shows how session will appear to members
</success_criteria>

<output>
After completion, create `.planning/phases/15-admin-session-editing/15-09-SUMMARY.md`
</output>
