---
phase: 15-admin-session-editing
plan: 08
type: execute
wave: 4
depends_on: ["15-05"]
files_modified:
  - el-templo-admin/src/components/sessions/MemberPreviewDialog.vue
  - el-templo-admin/src/pages/SessionEditPage.vue
  - el-templo-admin/src/pages/SessionsPage.vue
  - el-templo-admin/src/pages/SessionDetailPage.vue
autonomous: true

must_haves:
  truths:
    - "Preview button appears in sessions list AND edit page"
    - "Preview modal shows simplified read-only session representation"
    - "Level dropdown in preview allows switching between member levels"
  artifacts:
    - path: "el-templo-admin/src/components/sessions/MemberPreviewDialog.vue"
      provides: "Member preview modal with level selector"
      min_lines: 60
  key_links:
    - from: "el-templo-admin/src/components/sessions/MemberPreviewDialog.vue"
      to: "el-templo-admin/src/composables/useEditApi.ts"
      via: "fetchPreview call"
      pattern: "fetchPreview"
---

<objective>
Create the member preview dialog that shows how a session will appear to members, with level-specific preview switching.

Purpose: Coaches need to verify how their edits look from the member perspective before approving.
Output: MemberPreviewDialog component wired into sessions list and edit page.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-admin-session-editing/15-CONTEXT.md
@.planning/phases/15-admin-session-editing/15-RESEARCH.md
@.planning/phases/15-admin-session-editing/15-05-SUMMARY.md
@el-templo-admin/src/types/session.ts
@el-templo-admin/src/composables/useEditApi.ts
@el-templo-admin/src/pages/SessionEditPage.vue
@el-templo-admin/src/pages/SessionsPage.vue
@el-templo-admin/src/pages/SessionDetailPage.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MemberPreviewDialog component</name>
  <files>el-templo-admin/src/components/sessions/MemberPreviewDialog.vue</files>
  <action>
    Create `el-templo-admin/src/components/sessions/MemberPreviewDialog.vue`.
    Per user decision: modal with simplified read-only representation, not pixel-identical to member app.

    **Props:**
    - modelValue: boolean (v-model for dialog open state)
    - sessionId: number
    - currentMemberLevel: string (initial level to preview)

    **Emits:**
    - 'update:modelValue': boolean

    **Template structure:**
    QDialog (v-model, maximized on mobile, wide on desktop):
      QCard (min-width: 500px, max-width: 700px, max-height: 80vh):
        - Header row: "Vista Previa del Miembro" title + close button
        - Level selector: QSelect with options [alfa, delta, sigma, omega, spartan]
          - Per user decision: dropdown to select which member level to preview
          - On change: re-fetch preview for the selected level
        - Loading state: QSpinnerDots
        - Content (scrollable):
          For each block in preview:
          - Block section with colored left border (same block role colors)
          - Block header: role name, format badge
          - Exercise list (read-only, clean layout):
            For each exercise:
            - Exercise name
            - Prescription: "X reps" or "Xs" for time-based
            - Rest: "Descanso: Xs" (if > 0)
            - Notes shown as italic caption
          - Simple separator between blocks

    **Script logic:**
    - On dialog open (watch modelValue): call useEditApi().fetchPreview(sessionId, currentMemberLevel)
    - Level change: re-fetch with new memberLevel
    - Display data in clean, read-only format
    - Block colors: same mapping as BlockCard (INITIUM=light-blue, NUCLEUS=deep-purple, DEUTEROS=teal, ATHLOS/EPIKOS=amber)
    - No action buttons in preview - purely informational

    **Visual styling:**
    - Clean, minimal layout
    - Block sections with subtle colored borders (not full colored headers like admin view)
    - Exercise names in medium weight, prescriptions in regular
    - Format badge as small QBadge
    - Overall feel: how a member would understand their workout
  </action>
  <verify>Build compiles: `cd /home/franco/projects/el-templo/el-templo-admin && npx vue-tsc --noEmit 2>&1 | head -20`</verify>
  <done>MemberPreviewDialog shows simplified read-only session with level selector. Clean layout focuses on exercise names, reps, and format.</done>
</task>

<task type="auto">
  <name>Task 2: Add preview buttons to sessions list, detail page, and edit page</name>
  <files>
    el-templo-admin/src/pages/SessionsPage.vue
    el-templo-admin/src/pages/SessionDetailPage.vue
    el-templo-admin/src/pages/SessionEditPage.vue
  </files>
  <action>
    Per user decision: preview button in sessions list actions column AND in the edit page.

    **SessionsPage.vue:**
    - Import MemberPreviewDialog
    - Add dialog state: previewOpen, previewSessionId, previewMemberLevel
    - In the QTable row actions, add a preview button (visibility icon) next to existing action buttons
    - On click: set previewSessionId from row, previewMemberLevel from row's memberLevel, open dialog
    - Include MemberPreviewDialog component in template with v-model binding

    **SessionDetailPage.vue:**
    - Import MemberPreviewDialog
    - Add preview button in the action buttons row (next to Approve/Revert)
    - Button: "Vista Previa" with visibility icon
    - On click: open preview dialog with current session.id and session.memberLevel
    - Include MemberPreviewDialog in template

    **SessionEditPage.vue:**
    - Import MemberPreviewDialog
    - Add preview button in the action bar (next to Reset/Approve/Revert)
    - Button: "Vista Previa" with visibility icon
    - On click: open preview dialog with current session.id and session.memberLevel
    - Include MemberPreviewDialog in template
  </action>
  <verify>
    1. Build compiles: `cd /home/franco/projects/el-templo/el-templo-admin && npx vue-tsc --noEmit 2>&1 | head -20`
    2. Dev server starts without errors
  </verify>
  <done>Preview button appears in sessions list (per row), session detail page, and session edit page. Opens MemberPreviewDialog with level switcher.</done>
</task>

</tasks>

<verification>
- Sessions list has preview button per row
- Session detail page has "Vista Previa" button
- Session edit page has "Vista Previa" button
- Preview dialog shows clean read-only session
- Level dropdown switches preview to different member level
</verification>

<success_criteria>
- Per user decision: preview button in sessions list actions AND in edit page
- Per user decision: modal with simplified read-only representation
- Per user decision: level-specific preview dropdown
- Preview shows blocks, exercises, reps, format info in clean layout
- Not pixel-identical to member app per user decision
</success_criteria>

<output>
After completion, create `.planning/phases/15-admin-session-editing/15-08-SUMMARY.md`
</output>
