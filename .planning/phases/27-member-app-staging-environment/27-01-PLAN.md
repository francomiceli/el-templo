---
phase: 27-member-app-staging-environment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/seed-staging.ts
  - deploy/staging/reset-staging.sh
  - deploy/nginx/api-staging.eltemplo.org
  - deploy/nginx/app-staging.eltemplo.org
  - deploy/nginx/admin-staging.eltemplo.org
  - el-templo-api/package.json
autonomous: true

must_haves:
  truths:
    - "Staging database can be populated with fake users, SPOM reference data, and sample sessions"
    - "Two known test users exist with fixed credentials (test-member@eltemplo.org and test-admin@eltemplo.org)"
    - "Weekly cron can reset staging database safely without risk to production"
    - "Nginx configs route staging subdomains to correct backend/frontend paths"
  artifacts:
    - path: "el-templo-api/src/db/seed-staging.ts"
      provides: "Staging seed script with faker data"
      contains: "test-member@eltemplo.org"
    - path: "deploy/staging/reset-staging.sh"
      provides: "Weekly cron reset script with production safety check"
      contains: "eltemplo_staging"
    - path: "deploy/nginx/api-staging.eltemplo.org"
      provides: "Nginx reverse proxy config for staging API"
      contains: "api-staging.eltemplo.org"
    - path: "deploy/nginx/app-staging.eltemplo.org"
      provides: "Nginx SPA config for staging member app"
      contains: "app-staging.eltemplo.org"
    - path: "deploy/nginx/admin-staging.eltemplo.org"
      provides: "Nginx SPA config for staging admin app"
      contains: "admin-staging.eltemplo.org"
  key_links:
    - from: "el-templo-api/src/db/seed-staging.ts"
      to: "el-templo-api/src/db/seed-spom.ts"
      via: "imports seedSPOM() for reference data"
      pattern: "seedSPOM"
    - from: "deploy/staging/reset-staging.sh"
      to: "el-templo-api/src/db/seed-staging.ts"
      via: "runs compiled seed-staging.js after migrations"
      pattern: "seed-staging"
---

<objective>
Create the staging infrastructure configuration files: Nginx configs for 3 staging subdomains, a staging seed script that populates the staging database with fake data and known test users, and a weekly cron reset script for the staging database.

Purpose: These are the server-side configuration files needed before the staging deploy workflow can deploy to the staging environment.
Output: 3 Nginx config files, 1 staging seed script, 1 reset script, @faker-js/faker added as devDependency.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@deploy/nginx/api.eltemplo.org
@deploy/nginx/app.eltemplo.org
@deploy/nginx/admin.eltemplo.org
@el-templo-api/src/db/seed.ts
@el-templo-api/src/db/seed-spom.ts
@el-templo-api/src/db/schema/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create staging seed script and install faker</name>
  <files>
    el-templo-api/src/db/seed-staging.ts
    el-templo-api/package.json
  </files>
  <action>
    1. Install @faker-js/faker as a devDependency in el-templo-api: `pnpm add -D @faker-js/faker`

    2. Create `el-templo-api/src/db/seed-staging.ts` that:
       - Uses `@faker-js/faker` with Spanish locale (`@faker-js/faker/locale/es`) and fixed seed (`faker.seed(12345)`) for reproducible data
       - Safety check at the top: refuse to run if `DB_NAME === 'eltemplo'` (production database name). Throw an error with message "SAFETY: Refusing to seed production database. Set DB_NAME=eltemplo_staging"
       - Imports `seedSPOM` from `./seed-spom` for reference data (routes, exercises, SPOM rules, etc.)
       - Imports argon2 for password hashing
       - Imports schema tables from `./schema`
       - Uses `createSingleConnection` from `./index` for database access

       Seed order:
       a. Call `seedSPOM()` first to populate all reference data (exercises, routes, formats, etc.)
       b. Clear users and branches tables
       c. Create 5 branches (same as existing seed.ts: Centro, Alem, Constitucion, Jujuy, Mogotes)
       d. Create 2 known test users with a fixed password (use env var `STAGING_SEED_PASSWORD` with fallback to `"templo2026"`):
          - test-member@eltemplo.org: role=member, level=sigma, branchId=1, firstName="Test", lastName="Member"
          - test-admin@eltemplo.org: role=superadmin, level=spartan, branchId=1, firstName="Test", lastName="Admin"
       e. Create 3 coaches using faker for names: role=coach, level=omega, spread across branches 1-3, emails like coach-staging-1@eltemplo.org
       f. Create 15 fake members using faker for names: spread across branches and levels (alfa, delta, sigma, omega), emails like member-staging-{n}@eltemplo.org. All use the same password hash.

       At the end, print a summary of created records and close the connection.

       The script should be self-executing when run directly (check `require.main === module` pattern matching existing seed.ts).

    3. Do NOT generate sessions or completed-sessions in the seed script. Sessions are algorithm-generated at runtime and would be too complex to fake correctly. The staging environment will have its reference data and users; sessions can be generated via the admin app's generation UI.

  </action>
  <verify>
    - `pnpm exec tsc --noEmit` passes in el-templo-api (no type errors)
    - @faker-js/faker listed in devDependencies of el-templo-api/package.json
    - seed-staging.ts imports and uses seedSPOM, argon2, and schema correctly
  </verify>
  <done>
    Staging seed script exists that creates reference data + 5 branches + 2 known test users + 3 coaches + 15 fake members with production safety check. faker installed as devDependency.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Nginx staging configs and weekly reset script</name>
  <files>
    deploy/nginx/api-staging.eltemplo.org
    deploy/nginx/app-staging.eltemplo.org
    deploy/nginx/admin-staging.eltemplo.org
    deploy/staging/reset-staging.sh
  </files>
  <action>
    1. Create `deploy/nginx/api-staging.eltemplo.org`:
       - Copy the pattern from `deploy/nginx/api.eltemplo.org`
       - Change `server_name` to `api-staging.eltemplo.org`
       - Change `proxy_pass` to `http://127.0.0.1:4001` (staging port, per user decision)
       - Keep all proxy headers, timeouts, and security headers identical to production
       - Comment at top: "# Staging API Reverse Proxy (port 4001)"
       - HTTP-only (SSL will be added by certbot)

    2. Create `deploy/nginx/app-staging.eltemplo.org`:
       - Copy pattern from `deploy/nginx/app.eltemplo.org`
       - Change `server_name` to `app-staging.eltemplo.org`
       - Change `root` to `/opt/el-templo-staging/app` (per user decision)
       - Keep `try_files` for SPA routing, static asset caching, and security headers
       - Comment at top: "# Staging Member App (SPA)"

    3. Create `deploy/nginx/admin-staging.eltemplo.org`:
       - Copy pattern from `deploy/nginx/admin.eltemplo.org`
       - Change `server_name` to `admin-staging.eltemplo.org`
       - Change `root` to `/opt/el-templo-staging/admin` (per user decision)
       - Keep `try_files` for SPA routing, static asset caching, and security headers
       - Comment at top: "# Staging Admin App (SPA)"

    4. Create `deploy/staging/reset-staging.sh`:
       - Bash script with `set -euo pipefail`
       - Shebang line: `#!/bin/bash`
       - Comment with cron schedule: `# Cron: 0 4 * * 0  (every Sunday at 04:00 UTC / 01:00 Argentina)`
       - SAFETY CHECK: Hardcode `DB_NAME="eltemplo_staging"` (do NOT read from env for the drop/create step). Add explicit check: `if [ "$DB_NAME" = "eltemplo" ]; then echo "ERROR: Refusing to reset production database"; exit 1; fi`
       - Read `STAGING_DB_USER` and `STAGING_DB_PASSWORD` from environment
       - Steps:
         a. Log timestamp: "Resetting staging database..."
         b. Drop and recreate the staging database: `mysql -u "$STAGING_DB_USER" -p"$STAGING_DB_PASSWORD" -e "DROP DATABASE IF EXISTS $DB_NAME; CREATE DATABASE $DB_NAME;"`
         c. Log: "Running migrations..."
         d. `cd /opt/el-templo-staging/api && NODE_ENV=production node dist/db/run-migrations.js`
         e. Log: "Seeding fake data..."
         f. `NODE_ENV=production node dist/db/seed-staging.js`
         g. Log: "Staging reset complete"
       - Make the script executable (note in summary that `chmod +x` will be needed on server)

  </action>
  <verify>
    - All 3 Nginx config files exist in deploy/nginx/ with correct server_name and paths
    - api-staging config proxies to port 4001
    - app-staging root is /opt/el-templo-staging/app
    - admin-staging root is /opt/el-templo-staging/admin
    - reset-staging.sh has production safety check (hardcoded eltemplo_staging, refuses eltemplo)
    - reset-staging.sh runs migrations before seeding
  </verify>
  <done>
    3 Nginx staging subdomain configs ready for certbot SSL injection. Weekly reset script with production safety check ready for cron installation.
  </done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit` passes in el-templo-api
- All 5 files created with correct content
- No production database references in staging configs (all point to eltemplo_staging or staging paths)
- Staging seed script has production safety check
- Reset script has production safety check
</verification>

<success_criteria>
Staging infrastructure configuration files exist and are ready for server deployment. Seed script can populate a staging database with fake data. Reset script can safely drop and recreate the staging database.
</success_criteria>

<output>
After completion, create `.planning/phases/28-member-app-staging-environment/27-01-SUMMARY.md`
</output>
