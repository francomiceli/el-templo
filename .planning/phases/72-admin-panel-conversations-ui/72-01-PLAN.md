---
phase: 72-admin-panel-conversations-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/whatsapp/service.ts
  - el-templo-api/src/modules/whatsapp/types.ts
  - el-templo-api/test/whatsapp/conversations.test.ts
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02

must_haves:
  truths:
    - "GET /api/admin/whatsapp/conversations returns paginated list with search and filters"
    - "GET /api/admin/whatsapp/conversations/:id returns conversation with paginated messages"
    - "Unauthenticated or non-admin requests get 403"
  artifacts:
    - path: "el-templo-api/src/modules/whatsapp/service.ts"
      provides: "listConversations and getConversation implementations"
      contains: "listConversations"
    - path: "el-templo-api/test/whatsapp/conversations.test.ts"
      provides: "Integration tests for conversation endpoints"
      min_lines: 80
  key_links:
    - from: "el-templo-api/src/modules/whatsapp/service.ts"
      to: "el-templo-api/src/db/schema/whatsapp.ts"
      via: "Drizzle query on whatsappConversations and whatsappMessages"
      pattern: "whatsappConversations|whatsappMessages"
    - from: "el-templo-api/src/modules/whatsapp/routes.ts"
      to: "el-templo-api/src/modules/whatsapp/service.ts"
      via: "Service method calls in route handlers"
      pattern: "service\\.listConversations|service\\.getConversation"
---

<objective>
Implement the WhatsApp conversation list and detail API endpoints (currently TODO stubs in service.ts).

Purpose: The admin frontend needs working API endpoints to display conversations and messages. Routes, schemas, and types already exist -- only the service methods need implementation plus integration tests.
Output: Working GET /api/admin/whatsapp/conversations and GET /api/admin/whatsapp/conversations/:id endpoints with test coverage.
</objective>

<execution_context>
@/Users/bores/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bores/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@el-templo-api/src/modules/whatsapp/routes.ts
@el-templo-api/src/modules/whatsapp/service.ts
@el-templo-api/src/modules/whatsapp/types.ts
@el-templo-api/src/modules/whatsapp/schemas.ts
@el-templo-api/src/db/schema/whatsapp.ts
@el-templo-api/test/helpers.ts
@el-templo-api/src/modules/attendance/service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix types.ts ClientState enum and implement service methods</name>
  <files>
    el-templo-api/src/modules/whatsapp/types.ts
    el-templo-api/src/modules/whatsapp/service.ts
  </files>
  <action>
IMPORTANT: The types.ts ClientState enum uses "lapsed" | "returning" but the DB schema (whatsapp.ts) uses "inactive_member" | "expired_member". Update types.ts ClientState to match the DB schema: "lead" | "trial" | "active_member" | "inactive_member" | "expired_member". Also update schemas.ts clientState enum array to match.

Then implement the two TODO service methods in service.ts:

1. **listConversations(params)**:
   - Build WHERE conditions array: filter by status (eq), clientState (eq), search (OR of like on phone and contactName with `%search%`)
   - Use a subquery or separate query to get last message preview: select content from whatsappMessages where conversationId = conv.id order by createdAt DESC limit 1
   - Count total matching conversations for pagination
   - Select conversations with LEFT JOIN to users table to get linkedMemberName (concat firstName + lastName from users where users.id = linkedMemberId)
   - Order by lastMessageAt DESC (most recent first)
   - Paginate: offset = (page - 1) \* limit, limit = limit
   - Include messageCount per conversation (count of messages)
   - Return { conversations: ConversationRecord[], total, page, limit }
   - Use drizzle sql template literals for subqueries (per project convention from decision [67-02])

2. **getConversation(id, messagePage, messageLimit)**:
   - Fetch conversation by id, throw NotFoundError if not found
   - LEFT JOIN users for linkedMemberName
   - Fetch messages for this conversationId, ordered by createdAt ASC (oldest first for chat display)
   - Paginate messages: offset = (messagePage - 1) \* messageLimit
   - Count totalMessages
   - Return { conversation: ConversationRecord, messages: MessageRecord[], totalMessages }

Follow the Drizzle query patterns from modules/attendance/service.ts. Use sql template literals from drizzle-orm for subqueries. Import whatsappConversations, whatsappMessages from schema. Use eq, desc, like, or, and, sql, count from drizzle-orm.
</action>
<verify>
<automated>cd /Users/bores/el-templo/el-templo-api && npx tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
<manual>Verify types compile and service methods are no longer throwing "Not implemented"</manual>
</verify>
<done>listConversations returns paginated conversations with filters/search/preview. getConversation returns conversation with paginated messages. ClientState enum matches DB schema.</done>
</task>

<task type="auto">
  <name>Task 2: Integration tests for conversation list and detail endpoints</name>
  <files>el-templo-api/test/whatsapp/conversations.test.ts</files>
  <action>
Create integration test file following the project test pattern (see test/helpers.ts for createTestApp, getAuthToken).

**Setup (beforeAll):**

- Create test app, get adminToken via getAuthToken
- Insert 2-3 test conversations directly into whatsappConversations table using app.db
- Insert 5-10 test messages into whatsappMessages for at least one conversation
- Use unique phone numbers that won't conflict (e.g., "+5491199990001")

**Teardown (afterAll):**

- Delete test messages and conversations (clean up by phone number)
- Close app

**Test cases:**

1. `GET /conversations` returns 200 with conversations array and total count
2. `GET /conversations?status=active` filters by status
3. `GET /conversations?clientState=lead` filters by clientState
4. `GET /conversations?search=9999` filters by phone substring match
5. `GET /conversations?page=1&limit=1` returns correct pagination (1 result, total > 1)
6. `GET /conversations` without auth returns 401
7. `GET /conversations/:id` returns conversation with messages array
8. `GET /conversations/:id` with non-existent id returns 404
9. `GET /conversations/:id` messages are ordered by createdAt ASC (oldest first)

Use app.inject() pattern from existing tests. Assert response structure matches the schema (has conversations array, total number, conversation has phone/status/clientState fields, messages have direction/content/createdAt).

**Important:** Use `catch (err: unknown)` with `instanceof Error` checks per project TypeScript rules. Use test db to insert/query (available via createTestApp).
</action>
<verify>
<automated>cd /Users/bores/el-templo/el-templo-api && pnpm test -- --reporter=verbose test/whatsapp/conversations.test.ts 2>&1 | tail -30</automated>
</verify>
<done>All 9 test cases pass. Conversation list endpoint returns filtered, paginated results. Detail endpoint returns conversation with ordered messages. Auth guard works.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `cd el-templo-api && npx tsc --noEmit`
2. All whatsapp tests pass: `cd el-templo-api && pnpm test test/whatsapp/`
3. ClientState enum in types.ts matches DB schema values
</verification>

<success_criteria>

- GET /api/admin/whatsapp/conversations returns paginated list with search/filter support
- GET /api/admin/whatsapp/conversations/:id returns conversation with paginated messages
- Integration tests verify all query parameters, pagination, auth guard, and 404 handling
- No TypeScript compilation errors
  </success_criteria>

<output>
After completion, create `.planning/phases/72-admin-panel-conversations-ui/72-01-SUMMARY.md`
</output>
