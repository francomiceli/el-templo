---
phase: 13-session-generation-review-improvement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/session-logic/[Planificaciones] - Base de Datos - Ejercicios.csv
  - el-templo-api/src/db/schema/exercises.ts
  - el-templo-api/src/db/migrations/XXXX_add_linear_difficulty.ts
  - el-templo-api/src/modules/sessions/pipeline/stage-6-exercises.ts
  - el-templo-api/src/modules/sessions/fallback/exercise-fallback.ts
autonomous: true

must_haves:
  truths:
    - "Ejercicios.csv contains Dificultad Lineal column with values 1-12"
    - "Database exercises table has dificultad_lineal column populated"
    - "Pipeline uses linear difficulty (1-12) instead of bucket (1-3)"
    - "Nivel Superior at 85%+ maps to next level's first difficulty"
  artifacts:
    - path: "docs/session-logic/[Planificaciones] - Base de Datos - Ejercicios.csv"
      provides: "Exercise CSV with Dificultad Lineal column"
      contains: "Dificultad Lineal"
    - path: "el-templo-api/src/db/schema/exercises.ts"
      provides: "Exercise schema with dificultadLineal field"
      contains: "dificultadLineal"
    - path: "el-templo-api/src/modules/sessions/pipeline/stage-6-exercises.ts"
      provides: "Exercise selection using linear difficulty"
      contains: "dificultadLineal"
  key_links:
    - from: "stage-6-exercises.ts"
      to: "exercises table"
      via: "dificultadLineal column lookup"
      pattern: "dificultadLineal"
    - from: "CSV Dificultad Lineal"
      to: "DB dificultad_lineal"
      via: "seed script migration"
      pattern: "dificultad_lineal"
---

<objective>
Implement linear difficulty scale (1-12) throughout the session generation system.

Purpose: The current difficulty system uses buckets (1-3 + "Nivel Superior") which doesn't capture the full difficulty progression across levels. Requirements specify a 1-12 linear scale where Alfa=1-3, Delta=4-6, Sigma=7-8, Omega=9-10, Spartan=11-12.

Output: Updated CSV with Dificultad Lineal column, database migration, and pipeline changes to use linear difficulty for exercise selection.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-session-generation-review-improvement/13-RESEARCH.md

# Key source files
@el-templo-api/src/db/schema/exercises.ts
@el-templo-api/src/modules/sessions/pipeline/stage-6-exercises.ts
@el-templo-api/src/modules/sessions/fallback/exercise-fallback.ts
@docs/session-logic/[Planificaciones] - Base de Datos - Ejercicios.csv
@docs/session-logic/[Planificaciones] - Base de Datos - SPOM - Intensidad.csv
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Dificultad Lineal column to CSV and update exercises schema</name>
  <files>
    docs/session-logic/[Planificaciones] - Base de Datos - Ejercicios.csv
    el-templo-api/src/db/schema/exercises.ts
  </files>
  <action>
1. Read Ejercicios.csv and add "Dificultad Lineal" column after "Dificultad Relativa".

2. Calculate value using formula based on Nivel and Dificultad Relativa:
   - Alfa (Nivel=1): Dificultad Lineal = Dificultad Relativa (1,2,3 -> 1,2,3)
   - Delta (Nivel=2): Dificultad Lineal = 3 + Dificultad Relativa (1,2,3 -> 4,5,6)
   - Sigma (Nivel=3): Dificultad Lineal = 6 + Dificultad Relativa (1,2 -> 7,8)
   - Omega (Nivel=4): Dificultad Lineal = 8 + Dificultad Relativa (1,2 -> 9,10)
   - Spartan (Nivel=5): Dificultad Lineal = 10 + Dificultad Relativa (1,2 -> 11,12)

   Note: Use the numeric "Nivel" column (Código Numérico column header shows 1,2,3,4,5 not alfa/delta names).

3. Update el-templo-api/src/db/schema/exercises.ts:
   - Add `dificultadLineal: int('dificultad_lineal').notNull().default(1)` field
   - Keep existing `difficulty` field for backward compatibility
   - Update index to include new column

4. Create migration file: `el-templo-api/src/db/migrations/0010_add_linear_difficulty.ts`
   - ALTER TABLE exercises ADD COLUMN dificultad_lineal INT NOT NULL DEFAULT 1
   - UPDATE exercises SET dificultad_lineal based on level and difficulty values

Formula for migration UPDATE:
```sql
UPDATE exercises SET dificultad_lineal = CASE
  WHEN level = 'alfa' THEN difficulty
  WHEN level = 'delta' THEN difficulty + 3
  WHEN level = 'sigma' THEN difficulty + 6
  WHEN level = 'omega' THEN difficulty + 8
  WHEN level = 'spartan' THEN difficulty + 10
  ELSE difficulty
END;
```
  </action>
  <verify>
- CSV has "Dificultad Lineal" column with values ranging 1-12
- Schema compiles: `cd el-templo-api && npx tsc --noEmit`
- Migration file exists and is syntactically correct
  </verify>
  <done>
- Ejercicios.csv has Dificultad Lineal column populated correctly
- exercises.ts schema includes dificultadLineal field
- Migration file ready for application
  </done>
</task>

<task type="auto">
  <name>Task 2: Update pipeline to use linear difficulty and implement Nivel Superior mapping</name>
  <files>
    el-templo-api/src/modules/sessions/pipeline/stage-6-exercises.ts
    el-templo-api/src/modules/sessions/fallback/exercise-fallback.ts
    el-templo-api/src/modules/sessions/fallback/types.ts
  </files>
  <action>
1. Update stage-6-exercises.ts:
   - Remove `parseDifficultyBucket()` function that returns 1/2/3/999
   - Add `getLinearDifficultyTarget()` function that:
     - Takes (intensity, memberLevel) as input
     - Returns { maxDificultadLineal: number, targetLevel: ExerciseLevel }
     - Uses SPOM-Intensidad.csv mapping: intensity ranges -> difficulty bucket
     - Maps bucket to linear scale based on memberLevel:
       - Bucket "1" -> level's first linear difficulty (alfa:1, delta:4, sigma:7, etc.)
       - Bucket "2" -> level's second linear difficulty
       - Bucket "3" -> level's third linear difficulty
       - Bucket "Nivel Superior" at 85-95%: shift to next level, difficulty=1

2. Update Nivel Superior logic (intensity >= 85):
   - Current: shifts targetLevel up, sets maxDifficulty=1
   - New: shifts targetLevel up, sets maxDificultadLineal to next level's first value
   - Example: Alfa at 90% -> target Delta, maxDificultadLineal=4 (Delta's lowest)

3. Update exercise-fallback.ts:
   - Change `maxDifficulty: number` parameter to `maxDificultadLineal: number`
   - Update SQL queries to filter on `dificultad_lineal <= ?` instead of `difficulty <= ?`
   - Keep fallback tier logic intact (difficulty relaxation now uses linear scale)

4. Update types.ts if needed:
   - Add `dificultadLineal` to ExerciseRow type

Important: Do NOT break existing tests. The fallback ladder logic stays the same, only the numeric scale changes from 1-3 to 1-12.
  </action>
  <verify>
- TypeScript compiles: `cd el-templo-api && npx tsc --noEmit`
- Existing tests pass: `cd el-templo-api && npm test`
- Exercise selection queries use dificultad_lineal column
  </verify>
  <done>
- Pipeline uses linear difficulty scale (1-12)
- Nivel Superior at 85%+ correctly maps to next level
- Fallback ladder works with linear difficulty
- All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Apply migration and reseed exercises with linear difficulty</name>
  <files>
    el-templo-api/src/db/seed/seed-exercises.ts
  </files>
  <action>
1. Update seed-exercises.ts to read Dificultad Lineal from CSV:
   - Add column mapping for 'Dificultad Lineal' -> dificultadLineal
   - Ensure CSV parsing includes the new column

2. Apply database migration:
   - Connect to the database
   - Run: `ALTER TABLE exercises ADD COLUMN dificultad_lineal INT NOT NULL DEFAULT 1;`
   - Run UPDATE to populate existing rows (see Task 1 formula)

3. Reseed exercises table with updated CSV data:
   - Run seed script: `cd el-templo-api && npx ts-node src/db/seed/seed-exercises.ts`
   - Or use npm script if available

4. Verify data integrity:
   - Query: `SELECT level, MIN(dificultad_lineal), MAX(dificultad_lineal) FROM exercises GROUP BY level`
   - Expected: alfa(1-3), delta(4-6), sigma(7-8), omega(9-10), spartan(11-12)
  </action>
  <verify>
- Seed script runs without errors
- Database query confirms correct dificultad_lineal values per level
- Application starts and can generate sessions: `cd el-templo-api && npm run dev` (briefly test)
  </verify>
  <done>
- Database has dificultad_lineal column with correct values
- Seed script updated to handle new column
- DIFF-01, DIFF-02, DIFF-03, DIFF-04 requirements satisfied
  </done>
</task>

</tasks>

<verification>
Requirements coverage:
- DIFF-01: Dificultad Lineal column added to Ejercicios.csv with correct mappings
- DIFF-02: Database exercises table updated with linear difficulty values
- DIFF-03: Algorithm uses linear difficulty scale for exercise selection
- DIFF-04: "Nivel Superior 1" maps to next level's first difficulty

Run validation:
```bash
# TypeScript compilation
cd el-templo-api && npx tsc --noEmit

# Run tests
cd el-templo-api && npm test

# Verify CSV has new column
head -1 "docs/session-logic/[Planificaciones] - Base de Datos - Ejercicios.csv" | grep "Dificultad Lineal"

# Verify database values
mysql -u root -p el_templo -e "SELECT level, MIN(dificultad_lineal), MAX(dificultad_lineal) FROM exercises GROUP BY level ORDER BY MIN(dificultad_lineal)"
```
</verification>

<success_criteria>
1. Ejercicios.csv contains "Dificultad Lineal" column with values 1-12 mapped correctly per level
2. exercises table in database has dificultad_lineal column populated
3. stage-6-exercises.ts uses dificultadLineal for exercise filtering
4. Nivel Superior mapping shifts to next level at 85%+ intensity
5. All existing tests continue to pass
6. Application starts and generates sessions successfully
</success_criteria>

<output>
After completion, create `.planning/phases/13-session-generation-review-improvement/13-01-SUMMARY.md`
</output>
