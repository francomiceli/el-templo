---
phase: 13-session-generation-review-improvement
plan: 05
type: execute
wave: 3
depends_on: ["13-03", "13-04"]
files_modified:
  - el-templo-api/src/modules/sessions/validation/VALIDATION-RESULTS.md
  - el-templo-api/src/modules/sessions/pipeline/*.ts
autonomous: false

must_haves:
  truths:
    - "Validation suite runs against all 19 weeks without errors"
    - "Algorithm generates valid sessions that follow patterns observed in examples"
    - "Block difficulty average validated within +/-0.5 of target"
    - "Format assignment compatible with block, level, and intensity"
  artifacts:
    - path: "el-templo-api/src/modules/sessions/validation/VALIDATION-RESULTS.md"
      provides: "Final validation report showing algorithm accuracy"
      contains: "PASSED"
  key_links:
    - from: "validation/run-validation.ts"
      to: "pipeline/"
      via: "full session generation and comparison"
      pattern: "runValidation"
---

<objective>
Run full validation suite, fix remaining discrepancies, and verify algorithm produces accurate sessions.

Purpose: After implementing difficulty system (Plan 01), block specs (Plan 02), validation suite (Plan 03), and Initium enhancement (Plan 04), this plan runs the complete validation to identify and fix any remaining issues.

Output: Updated validation results showing high accuracy, any final pipeline fixes, human verification of algorithm output.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-session-generation-review-improvement/13-RESEARCH.md
@.planning/phases/13-session-generation-review-improvement/13-01-SUMMARY.md
@.planning/phases/13-session-generation-review-improvement/13-02-SUMMARY.md
@.planning/phases/13-session-generation-review-improvement/13-03-SUMMARY.md
@.planning/phases/13-session-generation-review-improvement/13-04-SUMMARY.md

# Key source files
@el-templo-api/src/modules/sessions/validation/run-validation.ts
@el-templo-api/src/modules/sessions/validation/VALIDATION-RESULTS.md
@el-templo-api/src/modules/sessions/pipeline/stage-5-format.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full validation suite and identify discrepancies</name>
  <files>
    el-templo-api/src/modules/sessions/validation/VALIDATION-RESULTS.md
  </files>
  <action>
1. Ensure database is seeded with updated exercises (from Plan 01)

2. Run complete validation suite:
```bash
cd el-templo-api && npx ts-node src/modules/sessions/validation/run-validation.ts
```

3. Capture output and update VALIDATION-RESULTS.md with:
   - Total blocks compared
   - Pass rate (target: >90%)
   - Failure patterns by type
   - Specific examples of failures

4. Categorize failures:
   - Route mismatches -> Check weekly rotator data
   - Intensity mismatches -> Check SPOM lookup
   - Exercise count -> Verify cap is applied
   - Difficulty average -> Check linear difficulty calculation
   - Contraction mix -> Check contraction rules lookup
   - Format issues -> Check format compatibility

5. For each failure category, identify if it's:
   a. Algorithm bug (needs code fix)
   b. Data issue (needs CSV/DB correction)
   c. Expected variation (document as acceptable)

6. Document findings in VALIDATION-RESULTS.md
  </action>
  <verify>
- Validation suite completes without script errors
- VALIDATION-RESULTS.md updated with current statistics
- Failure patterns identified and categorized
  </verify>
  <done>
- Full validation run completed
- Current accuracy measured
- Discrepancies documented with root causes
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix identified algorithm discrepancies</name>
  <files>
    el-templo-api/src/modules/sessions/pipeline/*.ts
  </files>
  <action>
Based on Task 1 findings, fix the identified issues:

**Common fixes needed:**

1. If route mismatches:
   - Check stage-1-rotator.ts query logic
   - Verify weekly_rotator table data matches CSV

2. If intensity mismatches:
   - Check stage-2-spom.ts lookup
   - Verify SPOM table data

3. If difficulty average out of tolerance (DIFF-05):
   - Verify stage-6-exercises.ts uses dificultadLineal correctly
   - Check that linear difficulty target calculation is correct
   - May need to adjust exercise selection to balance average

4. If contraction distribution wrong (EXER-04):
   - Check stage-4-contraction.ts lookup
   - Verify contraction_rules table matches Contraccion.txt

5. If format assignment wrong (FORM-01, FORM-02):
   - Check stage-5-format.ts compatibility matrix
   - Verify format_compatibility table data

**For each fix:**
- Make minimal targeted change
- Add trace event explaining the fix
- Re-run validation for affected weeks to verify

**If validation shows >90% pass rate with remaining issues being edge cases:**
- Document edge cases as "known variations"
- Proceed to human verification
  </action>
  <verify>
- TypeScript compiles after each fix: `cd el-templo-api && npx tsc --noEmit`
- Tests pass: `cd el-templo-api && npm test`
- Re-run validation shows improved accuracy
  </verify>
  <done>
- Algorithm issues fixed
- Validation pass rate improved
- EXER-01, EXER-02, EXER-03, EXER-04 requirements addressed
- FORM-01, FORM-02 requirements verified
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Session generation algorithm improvements:
- Linear difficulty scale (1-12) for accurate exercise selection
- Exercise count cap (max 3 for non-Initium blocks)
- Contextual Initium selection based on day's Nucleus route
- Validation suite comparing algorithm to 19 weeks of coach examples
  </what-built>
  <how-to-verify>
1. Start the API server:
   ```bash
   cd el-templo-api && npm run dev
   ```

2. Generate a sample session for a specific week/day/level:
   ```bash
   curl -X POST http://localhost:3000/api/sessions/generate \
     -H "Content-Type: application/json" \
     -d '{"weekNumber": 10, "day": "Lunes", "memberLevel": "delta"}'
   ```

3. Compare generated session to coach example:
   - Open: docs/session-logic/examples/[Planificaciones] - Semana 10 - 20251020 - Lunes.csv
   - Find rows where Nivel = delta
   - Compare:
     - Routes match?
     - Exercise count (3 or less per block)?
     - Exercises are appropriate difficulty for delta level?

4. Review validation results:
   - Open: el-templo-api/src/modules/sessions/validation/VALIDATION-RESULTS.md
   - Check pass rate (should be >90%)
   - Review any documented edge cases

5. Test generating a future week (22+):
   ```bash
   curl -X POST http://localhost:3000/api/sessions/generate \
     -H "Content-Type: application/json" \
     -d '{"weekNumber": 22, "day": "Lunes", "memberLevel": "sigma"}'
   ```
   - Verify session structure is valid (5 blocks, exercises in each)
  </how-to-verify>
  <resume-signal>Type "approved" if algorithm generates accurate sessions matching coach patterns, or describe specific issues found.</resume-signal>
</task>

</tasks>

<verification>
Requirements coverage:
- DIFF-05: Block difficulty average validated within +/-0.5 of target
- EXER-01: Filters apply correctly (route, pattern, category, contraction, level, difficulty)
- EXER-02: High intensity = strict filters, low intensity = loose filters
- EXER-03: No exercise repeated within same level's session
- EXER-04: Exercise selection respects contraction distribution
- ALGO-05: Algorithm generates valid sessions for future weeks
- FORM-01: Format selected from Formatos compatibility table
- FORM-02: Format compatible with block, level, and intensity

Final validation:
```bash
# Run full validation
cd el-templo-api && npx ts-node src/modules/sessions/validation/run-validation.ts

# Check pass rate in results
grep "Pass rate" el-templo-api/src/modules/sessions/validation/VALIDATION-RESULTS.md
```
</verification>

<success_criteria>
1. Validation suite passes >90% of blocks when compared to coach examples
2. Remaining <10% are documented edge cases or acceptable variations
3. Algorithm generates structurally valid sessions for future weeks (22+)
4. Human verification confirms algorithm matches coach patterns
5. All Phase 13 requirements (DIFF, BLOCK, EXER, ALGO, INIT, FORM) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/13-session-generation-review-improvement/13-05-SUMMARY.md`
</output>
