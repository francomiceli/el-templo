---
phase: 137-m-quina-de-estados-de-validaci-n-cimiento
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/schema/financial-transactions.ts
  - el-templo-api/src/db/migrations/0153_validation_status.sql
  - el-templo-api/src/modules/shared/audit-log.ts
  - el-templo-api/src/modules/finance/firm-money.ts
  - el-templo-api/test/finance/validation-state.test.ts
  - el-templo-api/test/finance/validation-regression.test.ts
autonomous: true
requirements: [VAL-01, VAL-05]
must_haves:
  truths:
    - "financial_transactions tiene una columna validation_status con los 4 valores del enum"
    - "Las filas existentes quedan validado tras la migracion (backfill por DEFAULT)"
    - "Existe un unico helper que expresa el predicado de dinero firme (validado AND voided_at IS NULL)"
    - "audit-log acepta los 3 nuevos action types de validacion"
  artifacts:
    - path: "el-templo-api/src/db/schema/financial-transactions.ts"
      provides: "Columna validationStatus mysqlEnum + indice compuesto"
      contains: "validation_status"
    - path: "el-templo-api/src/db/migrations/0153_validation_status.sql"
      provides: "ALTER TABLE ADD COLUMN validation_status DEFAULT 'validado' + indice"
      contains: "validation_status"
    - path: "el-templo-api/src/modules/finance/firm-money.ts"
      provides: "firmMoneyConditions() y FIRM_MONEY_SQL — fuente unica del filtro firme"
      exports: ["firmMoneyConditions", "FIRM_MONEY_SQL"]
    - path: "el-templo-api/src/modules/shared/audit-log.ts"
      provides: "AuditAction extendido con transaction_validated/observed/corrected"
      contains: "transaction_validated"
  key_links:
    - from: "el-templo-api/src/modules/finance/firm-money.ts"
      to: "schema.financialTransactions.validationStatus"
      via: "eq(...validationStatus, 'validado')"
      pattern: "validationStatus.*validado"
---

<objective>
Cimiento de datos de la maquina de estados de validacion: agregar la columna ortogonal `validation_status` a `financial_transactions` con su migracion 0153 (backfill `validado`), extender el enum de `audit-log` con los 3 action types de validacion, y crear el helper canonico unico de "dinero firme" que las fases siguientes (plan 02 y 03) consumiran. Tambien crea los archivos de test scaffold (Wave 0) que los planes 02 y 03 llenaran.

Purpose: Sin la columna y el helper no se puede construir ni la maquina de estados (plan 02) ni el refactor de call sites (plan 03). Este plan es backend-only puro (D-01) y NO toca ningun call site del filtro todavia — solo deja la herramienta lista.

Output: Schema extendido + migracion SQL committeada + audit action types + firm-money.ts + 2 archivos de test scaffold vacios (RED-listos).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/137-m-quina-de-estados-de-validaci-n-cimiento/137-CONTEXT.md
@.planning/phases/137-m-quina-de-estados-de-validaci-n-cimiento/137-RESEARCH.md
@./CLAUDE.md

<interfaces>
<!-- Identificadores exactos extraidos del codebase. El executor NO necesita explorar. -->

financial_transactions ya tiene (financial-transactions.ts):

- soft-void triplet: voidedAt: timestamp("voided_at"), voidedBy: int("voided_by"), voidReason: text("void_reason") (lineas 51-53)
- enums inline en columna; ej kind: mysqlEnum("kind", [...]).notNull() (linea 26)
- bloque de indices al final (lineas 58-66): idx_financial_tx_kind_voided ya existe como precedente del estilo index("nombre").on(table.col, ...)
- mysqlEnum, index ya importados desde "drizzle-orm/mysql-core" (lineas 1-11)

audit-log.ts:

- export type AuditAction = "subscription_cancelled" | "transaction_voided" | "plan_assigned" | "reconciliation" | "days_compensated" (linea 24)
- export type AuditTargetKind = "subscription" | "transaction" | "member" (linea 31) — 'transaction' YA existe, no tocar
- auditLog.write(tx, { actorId, action, targetKind, targetId, payload, reason }) — firma ya usada por void()

Estilo de filtro firme actual (transaction-service.ts:772-775, getSummary):
conds: SQL[] = [
eq(schema.financialTransactions.direction, "inflow"),
isNull(schema.financialTransactions.voidedAt),
]
imports drizzle disponibles: eq, and, isNull, sql, type SQL desde "drizzle-orm"

Migracion previa de referencia (estilo enum-alter, sin `;` en comentarios): 0151_attendance_label_enum.sql
Proxima migracion confirmada: 0153 (la ultima es 0152).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Columna validation_status en schema + migracion 0153</name>
  <read_first>
    - el-templo-api/src/db/schema/financial-transactions.ts (archivo a modificar)
    - el-templo-api/src/db/migrations/0151_attendance_label_enum.sql (estilo de ALTER de enum, sin `;` en comentarios)
    - .planning/phases/137-m-quina-de-estados-de-validaci-n-cimiento/137-RESEARCH.md (seccion "Schema: agregar el enum ortogonal" + "Migracion (estilo 0153)")
    - reference: MEMORY.md — nunca `;` dentro de comentarios SQL; mysqlEnum 1er-arg = nombre de columna debe matchear la migracion byte-a-byte
  </read_first>
  <action>
    En financial-transactions.ts agregar el campo `validationStatus: mysqlEnum("validation_status", ["pendiente","observado","corregido","validado"]).default("validado").notNull()` despues de `voidReason` (linea ~53). El primer arg del mysqlEnum DEBE ser exactamente `"validation_status"` y el orden de los 4 valores DEBE coincidir byte-a-byte con la migracion (drift = CI rojo "Unknown column" que tsc no detecta). Agregar en el bloque de indices un `index("idx_financial_tx_validation_voided").on(table.validationStatus, table.voidedAt)` siguiendo el patron de idx_financial_tx_kind_voided.

    Generar la migracion con `cd el-templo-api && pnpm db:generate` (drizzle-kit generate). Si el nombre generado no es `0153_*`, renombrar el archivo a `0153_validation_status.sql`. Verificar que el SQL haga `ALTER TABLE financial_transactions ADD COLUMN validation_status enum('pendiente','observado','corregido','validado') NOT NULL DEFAULT 'validado' AFTER void_reason` + el ADD INDEX. El DEFAULT 'validado' es lo que backfillea las filas existentes (VAL-05). NUNCA poner `;` dentro de comentarios `--` en el SQL (el runner splittea por `;` antes de strippear `--`); usar guiones/em-dash para prosa. NO usar drizzle-kit push ni drizzle-kit migrate. Committear el archivo SQL junto al cambio de schema.

    NO correr `pnpm db:migrate` contra una DB que no sea local; el typecheck local (`pnpm typecheck` o `tsc`) DEBE pasar. La suite corre en CI al pushear.

  </action>
  <acceptance_criteria>
    - financial-transactions.ts contiene `validationStatus: mysqlEnum("validation_status"` con los 4 valores en orden pendiente,observado,corregido,validado y `.default("validado").notNull()`
    - financial-transactions.ts contiene `index("idx_financial_tx_validation_voided").on(table.validationStatus, table.voidedAt)`
    - Existe `el-templo-api/src/db/migrations/0153_validation_status.sql` con `ADD COLUMN` + `DEFAULT 'validado'` + el ADD INDEX
    - El SQL de migracion NO contiene ningun `;` dentro de lineas que empiezan con `--`
    - typecheck local pasa (grep verificacion): `grep -q "validation_status" el-templo-api/src/db/schema/financial-transactions.ts`
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm tsc --noEmit 2>&1 | tail -5; grep -c "validation_status" src/db/schema/financial-transactions.ts src/db/migrations/0153_validation_status.sql; ! grep -nE "^\s*--.*;" src/db/migrations/0153_validation_status.sql</automated>
  </verify>
  <done>La columna existe en schema + migracion 0153 generada y committeada, sin `;` en comentarios, typecheck verde.</done>
</task>

<task type="auto">
  <name>Task 2: Helper canonico firm-money.ts + audit action types</name>
  <read_first>
    - el-templo-api/src/modules/finance/transaction-service.ts lineas 15-26 (imports drizzle) y 771-775 (patron conds[] del filtro firme actual)
    - el-templo-api/src/modules/shared/audit-log.ts lineas 24-36 (AuditAction union + write signature)
    - .planning/phases/137-m-quina-de-estados-de-validaci-n-cimiento/137-RESEARCH.md (Pattern 1: filtro canonico centralizado — la forma exacta del helper)
  </read_first>
  <action>
    Crear `el-templo-api/src/modules/finance/firm-money.ts` exportando DOS formas equivalentes (D-08 / discrecion): (1) `export function firmMoneyConditions(): SQL[]` que devuelve `[isNull(schema.financialTransactions.voidedAt), eq(schema.financialTransactions.validationStatus, "validado")]` — para los call sites Drizzle; (2) `export const FIRM_MONEY_SQL = "voided_at IS NULL AND validation_status = 'validado'"` — constante estatica (sin interpolacion de input de usuario) para los 3 sites de SQL crudo. Importar `and, eq, isNull, type SQL` desde "drizzle-orm" y `* as schema from "../../db/schema"`. Documentar en el JSDoc que `direction='inflow'` y los filtros `kind IN(...)` quedan caller-specific (cada site ya los tiene); el unico predicado nuevo que TODOS comparten es `validation_status='validado'`. Este es el unico lugar del codebase donde se expresa el predicado de dinero firme — prohibido inline en otro lado a futuro.

    En audit-log.ts extender el union `AuditAction` agregando `| "transaction_validated" | "transaction_observed" | "transaction_corrected"`. NO tocar `AuditTargetKind` (el valor 'transaction' ya existe). NO crear tabla validation_events (D-08: se reusa audit-log).

  </action>
  <acceptance_criteria>
    - firm-money.ts exporta `firmMoneyConditions` (retorna SQL[] con isNull(voidedAt) + eq(validationStatus,'validado')) y `FIRM_MONEY_SQL` (string con `validation_status = 'validado'`)
    - firm-money.ts NO contiene `direction` ni `kind` hardcodeado (esos quedan caller-specific)
    - audit-log.ts AuditAction incluye los 3 strings nuevos: transaction_validated, transaction_observed, transaction_corrected
    - typecheck local pasa
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && grep -c "firmMoneyConditions\|FIRM_MONEY_SQL" src/modules/finance/firm-money.ts && grep -c "transaction_validated\|transaction_observed\|transaction_corrected" src/modules/shared/audit-log.ts && pnpm tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <done>firm-money.ts existe con ambas formas exportadas; audit-log tiene los 3 action types nuevos; typecheck verde.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 — scaffolds de test de estado y regresion</name>
  <read_first>
    - el-templo-api/test/finance/summary-sanity.test.ts (patron de test de finance existente)
    - el-templo-api/test/helpers.ts (createTestApp, createStaffUser, getAuthToken, createTestMember, assignTestPlan)
    - .planning/phases/137-m-quina-de-estados-de-validaci-n-cimiento/137-RESEARCH.md (Validation Architecture — Wave 0 Gaps + Test de regresion gate)
  </read_first>
  <action>
    Crear DOS archivos de test scaffold (los planes 02 y 03 los llenaran con casos reales — aqui solo el esqueleto RED-listo para no dejar verify sin automated):
    1. `el-templo-api/test/finance/validation-state.test.ts` — describe block "validation state machine" con un `it.todo` por cada transicion: validate (pendiente->validado), observe (pendiente->observado), correct (void+recreate, link transaction), void extendido (keepMembershipActive), y RBAC (coach NO puede validate/void). Importar los helpers de test/helpers.ts. Plan 02 reemplaza los todos por casos reales.
    2. `el-templo-api/test/finance/validation-regression.test.ts` — describe block "firm-money regression" con `it.todo` para: R1 (PENDIENTE no mueve summary/saldo), R2 (validar SI suma), R3 (6 metricas v5.0 identicas tras backfill), R4 (PENDIENTE SI salda balances del socio). Plan 03 los implementa.

    Usar el setup estandar de la suite (createTestApp). Los archivos deben compilar y correr sin fallar (it.todo no falla). NO implementar la logica todavia — eso es de los planes 02/03.

  </action>
  <acceptance_criteria>
    - validation-state.test.ts existe, compila, contiene it.todo para validate/observe/correct/void/RBAC
    - validation-regression.test.ts existe, compila, contiene it.todo para R1/R2/R3/R4
    - Ambos importan desde test/helpers.ts
    - typecheck local pasa
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && grep -c "it.todo\|describe" test/finance/validation-state.test.ts test/finance/validation-regression.test.ts && pnpm tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <done>Ambos scaffolds existen, compilan, con it.todo por cada caso; planes 02/03 los llenaran.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                                                          |
| ---------------------- | ---------------------------------------------------------------------------------------------------- |
| migracion → datos prod | Un ALTER mal escrito (orden de enum, `;` en comentario) rompe el deploy o cambia numeros de reportes |

## STRIDE Threat Register

| Threat ID | Category          | Component                           | Disposition | Mitigation Plan                                                                                                                      |
| --------- | ----------------- | ----------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| T-137-01  | Tampering         | migracion 0153 (orden de enum)      | mitigate    | Orden de los 4 valores byte-a-byte identico schema↔SQL; verify por grep; CI atrapa "Unknown column"                                  |
| T-137-02  | Tampering         | migracion 0153 (`;` en comentarios) | mitigate    | Verify automatizado `! grep -nE '^\s*--.*;'`; usar em-dash/guiones en prosa                                                          |
| T-137-03  | Denial of Service | backfill de filas historicas        | accept      | ALTER aditivo con DEFAULT — no reescribe filas, riesgo de perdida nulo; el deploy no respalda DB pero la migracion es no-destructiva |
| T-137-SC  | Tampering         | npm/pip/cargo installs              | mitigate    | Cero dependencias nuevas (decision de stack del milestone, verificado contra package.json); no hay install task en este plan         |

</threat_model>

<verification>
- Schema + migracion 0153 generada con pnpm db:generate, committeada, sin `;` en comentarios.
- firm-money.ts es el unico lugar con el predicado de dinero firme.
- audit-log tiene los 3 action types nuevos.
- Scaffolds de test RED-listos.
- typecheck local verde (la suite corre en CI al pushear).
</verification>

<success_criteria>

- VAL-01: columna validation_status ortogonal al soft-void existe en schema + migracion.
- VAL-05 (parcial): el helper canonico existe y el backfill DEFAULT 'validado' esta en la migracion; el refactor de call sites + regresion es plan 03.
  </success_criteria>

<output>
Create `.planning/phases/137-m-quina-de-estados-de-validaci-n-cimiento/137-01-SUMMARY.md` when done
</output>
