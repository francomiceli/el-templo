---
phase: 137-m-quina-de-estados-de-validaci-n-cimiento
plan: 03
type: execute
wave: 3
depends_on: ["137-01", "137-02"]
files_modified:
  - el-templo-api/src/modules/finance/transaction-service.ts
  - el-templo-api/src/modules/analytics/ticket-service.ts
  - el-templo-api/src/modules/analytics/ltv-service.ts
  - el-templo-api/src/modules/analytics/service.ts
  - el-templo-api/src/modules/analytics/advanced-finance-service.ts
  - el-templo-api/src/modules/reports/service.ts
  - el-templo-api/src/modules/subscriptions/service.ts
  - el-templo-api/test/finance/validation-regression.test.ts
autonomous: true
requirements: [VAL-05, VAL-07]
must_haves:
  truths:
    - "13 de 14 call sites del filtro de ingresos ganan el predicado validation_status='validado'"
    - "subscriptions/service.ts:2127 queda con voided_at IS NULL solo (excepcion deliberada documentada)"
    - "Un PENDIENTE no mueve summary/saldo firme ni las 6 metricas v5.0"
    - "Tras el backfill validado, las 6 metricas dan los mismos numeros que antes (suites analytics/reports verdes sin cambios)"
    - "Un PENDIENTE SI salda balances del socio (deuda -> 0)"
  artifacts:
    - path: "el-templo-api/src/modules/finance/firm-money.ts"
      provides: "Consumido por los 13 call sites (helper o constante)"
      contains: "firmMoneyConditions"
    - path: "el-templo-api/test/finance/validation-regression.test.ts"
      provides: "R1-R4: pendiente no mueve firme, valida si, 6 metricas identicas, balances saldado"
      contains: "regression"
  key_links:
    - from: "el-templo-api/src/modules/analytics/service.ts"
      to: "FIRM_MONEY_SQL"
      via: "yaPagoExpr raw SQL"
      pattern: "validation_status"
    - from: "el-templo-api/src/modules/finance/transaction-service.ts"
      to: "firmMoneyConditions"
      via: "getSummary conds[]"
      pattern: "validationStatus|firmMoneyConditions"
---

<objective>
Cerrar el blast radius (VAL-05): refactorizar los 13 call sites del filtro canonico de "dinero firme" para que pasen por el helper/constante de firm-money.ts (plan 01), agregando el predicado validation_status='validado'. Preservar EXPLICITAMENTE la unica excepcion (subscriptions/service.ts:2127, guard de integridad). Implementar el test de regresion (R1-R4) que prueba que un PENDIENTE no mueve la caja firme ni las 6 metricas v5.0, que validar si lo suma, y que un PENDIENTE ya salda balances.

Purpose: Es el riesgo unico y central de la fase. Tras el backfill 'validado' (plan 01) los numeros NO cambian; el predicado solo cambia el comportamiento futuro (un pendiente no contara como firme). El gate de la fase es la suite de regresion + analytics/reports verdes.

Output: 13 call sites consumiendo el helper, 1 excepcion documentada, test de regresion completo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/137-m-quina-de-estados-de-validaci-n-cimiento/137-CONTEXT.md
@.planning/phases/137-m-quina-de-estados-de-validaci-n-cimiento/137-RESEARCH.md
@./CLAUDE.md
@.planning/phases/137-m-quina-de-estados-de-validaci-n-cimiento/137-01-SUMMARY.md
@.planning/phases/137-m-quina-de-estados-de-validaci-n-cimiento/137-02-SUMMARY.md

<interfaces>
<!-- firm-money.ts (plan 01): -->
firmMoneyConditions(): SQL[] -> [isNull(voidedAt), eq(validationStatus,'validado')] — para call sites Drizzle
FIRM_MONEY_SQL: string = "voided_at IS NULL AND validation_status = 'validado'" — para los 3 sites de SQL crudo

<!-- CALL-SITE AUDIT — los 13 sites que ganan el predicado (de 137-RESEARCH.md, seccion final).
     Cada site ya tiene su voided_at IS NULL + (kind/direction caller-specific); SOLO se agrega validation_status='validado'.
     Lineas APROXIMADAS — verificar por grep al editar (el plan 02 pudo correr numeros en transaction-service.ts). -->

#1 finance/transaction-service.ts ~772-775 getSummary (conds[]) Drizzle -> push eq(validationStatus,'validado') o spread firmMoneyConditions()
#2 analytics/ticket-service.ts ~437 linkedCharges Drizzle -> + eq(validationStatus,'validado')
#3 analytics/ticket-service.ts ~522 universeCountByCurrency Drizzle -> idem
#4 analytics/ltv-service.ts ~286 realPaymentsByMember Drizzle -> idem
#5 analytics/service.ts ~1048 getRevenueTrend Drizzle -> idem
#6 analytics/service.ts ~1118 getRevenueByMethod Drizzle -> idem
#7 analytics/service.ts ~1181 getRevenueByBranch Drizzle -> idem
#8 analytics/service.ts ~1307 sumRevenue Drizzle -> idem
#9 analytics/service.ts ~543 yaPagoExpr (EXISTS) RAW SQL -> inyectar AND ${FIRM_MONEY_SQL}
#10 analytics/advanced-finance-service.ts ~187 cashTrend Drizzle -> + eq(validationStatus,'validado')
#11 reports/service.ts ~388 charge-history listing RAW SQL -> inyectar FIRM_MONEY_SQL
#12 reports/service.ts ~932 trial->conversion subquery RAW SQL -> inyectar FIRM_MONEY_SQL
#13 reports/service.ts ~1793 buildChargeConditions Drizzle -> + eq(validationStatus,'validado')

EXCEPCION (NO tocar) — site #14: subscriptions/service.ts ~2127, guard "sub tiene cobros activos antes de cancelar".
Es integridad, NO ingresos. Un PENDIENTE ES un cobro vivo que DEBE seguir bloqueando la cancelacion.
Queda con voided_at IS NULL solo. Documentar con comentario inline para que el plan-checker no lo marque como olvido.

test/helpers.ts: createTestApp, createStaffUser({role:'admin'|'coach'}), createTestMember, assignTestPlan, getAuthToken. DB real eltemplo*test*<POOL_ID>.
Suites de regresion existentes (deben pasar SIN cambios): test/analytics/_, test/reports/_, test/finance/summary-\*.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor de los 9 call sites de finance + analytics (Drizzle + raw)</name>
  <read_first>
    - el-templo-api/src/modules/finance/firm-money.ts (helper del plan 01)
    - el-templo-api/src/modules/finance/transaction-service.ts (getSummary, site #1 — verificar linea por grep tras plan 02)
    - el-templo-api/src/modules/analytics/ticket-service.ts (sites #2,#3)
    - el-templo-api/src/modules/analytics/ltv-service.ts (site #4)
    - el-templo-api/src/modules/analytics/service.ts (sites #5,#6,#7,#8 Drizzle + #9 raw yaPagoExpr)
    - el-templo-api/src/modules/analytics/advanced-finance-service.ts (site #10)
    - .planning/phases/137-m-quina-de-estados-de-validaci-n-cimiento/137-RESEARCH.md (CALL-SITE AUDIT)
  </read_first>
  <action>
    Para cada site Drizzle (#1,#2,#3,#4,#5,#6,#7,#8,#10): localizar el conds/where que ya tiene isNull(...voidedAt) y agregar eq(schema.financialTransactions.validationStatus, "validado"). Donde el predicado de dinero firme esta completo (voided + el nuevo), preferir spread de firmMoneyConditions() del helper; donde el site mezcla con kind IN/direction muy especifico, agregar solo eq(...validationStatus,'validado') (elegir un enfoque consistente por archivo). Importar desde "../finance/firm-money" segun corresponda.

    Para el site RAW #9 (analytics/service.ts:543 yaPagoExpr, EXISTS booleano): inyectar AND ${FIRM_MONEY_SQL} en el string SQL (constante estatica, sin interpolacion de input de usuario — sin riesgo de injection). Un PENDIENTE NO debe marcar "ya pago" como firme.

    Verificar lineas por grep antes de editar (el plan 02 movio numeros en transaction-service.ts). En getSummary (#1), conservar el eq(direction,'inflow') existente y agregar el predicado nuevo.

  </action>
  <acceptance_criteria>
    - getSummary (transaction-service.ts) incluye validation_status='validado' (via helper o eq directo) ademas de direction='inflow' y voided_at IS NULL
    - ticket-service.ts (2 sites), ltv-service.ts (1), analytics/service.ts (4 Drizzle + 1 raw), advanced-finance-service.ts (1) cada uno gana el predicado validado
    - El site raw #9 usa FIRM_MONEY_SQL (constante, no input interpolado)
    - typecheck local pasa
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && grep -lr "validationStatus\|FIRM_MONEY_SQL\|firmMoneyConditions\|validation_status = 'validado'" src/modules/finance/transaction-service.ts src/modules/analytics/ticket-service.ts src/modules/analytics/ltv-service.ts src/modules/analytics/service.ts src/modules/analytics/advanced-finance-service.ts | wc -l; pnpm tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <done>9 sites (finance+analytics, incl. el raw #9) ganan el predicado; typecheck verde.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor reports + excepcion documentada del guard de subscriptions</name>
  <read_first>
    - el-templo-api/src/modules/reports/service.ts (sites #11 raw:388, #12 raw:932, #13 Drizzle:1793 buildChargeConditions)
    - el-templo-api/src/modules/finance/firm-money.ts (FIRM_MONEY_SQL + firmMoneyConditions)
    - el-templo-api/src/modules/subscriptions/service.ts ~linea 2127 (guard a NO tocar, solo documentar)
    - .planning/phases/137-m-quina-de-estados-de-validaci-n-cimiento/137-RESEARCH.md (CALL-SITE AUDIT — site #14 excepcion)
  </read_first>
  <action>
    reports/service.ts: en los 2 sites RAW (#11 charge-history listing ~388, #12 trial->conversion subquery ~932) inyectar AND ${FIRM_MONEY_SQL} (o el fragmento validation_status = 'validado') en el WHERE crudo. En el site Drizzle #13 (buildChargeConditions ~1793) agregar eq(schema.financialTransactions.validationStatus, "validado") al builder. Verificar lineas por grep antes de editar.

    subscriptions/service.ts:~2127: NO agregar el predicado. Agregar un comentario inline corto encima del isNull(...voidedAt) de ese guard explicando: este site es un guard de integridad (bloquea cancelar una sub con cobros vivos), NO un filtro de ingresos firmes; un PENDIENTE ES un cobro vivo y DEBE seguir bloqueando la cancelacion, por eso queda con voided_at IS NULL solo (excepcion deliberada de fase 137, VAL-05). Esto evita que el plan-checker lo marque como olvido. Nota: el plan 02 ya pudo tocar este archivo; preservar sus cambios.

  </action>
  <acceptance_criteria>
    - reports/service.ts: los 3 sites (#11,#12,#13) ganan el predicado validation_status='validado'
    - subscriptions/service.ts (guard de cancelacion) NO agrega validation_status; tiene un comentario que documenta la excepcion deliberada
    - typecheck local pasa
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && grep -c "validation_status = 'validado'\|validationStatus\|FIRM_MONEY_SQL" src/modules/reports/service.ts; grep -c "excepcion\|integridad\|cobro vivo" src/modules/subscriptions/service.ts; pnpm tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <done>3 sites de reports ganan el predicado; el guard de subscriptions queda documentado como excepcion deliberada; typecheck verde.</done>
</task>

<task type="auto">
  <name>Task 3: Test de regresion R1-R4 (gate de la fase)</name>
  <read_first>
    - el-templo-api/test/finance/validation-regression.test.ts (scaffold del plan 01 a llenar)
    - el-templo-api/test/finance/summary-sanity.test.ts (patron de assert de summary firme)
    - el-templo-api/test/helpers.ts (createTestApp, createStaffUser, createTestMember, assignTestPlan, getAuthToken)
    - .planning/phases/137-m-quina-de-estados-de-validaci-n-cimiento/137-RESEARCH.md (Test de regresion — gate de la fase, VAL-05/VAL-07)
  </read_first>
  <action>
    Llenar validation-regression.test.ts reemplazando los it.todo por casos reales contra DB:
    - R1: cargar un cobro como coach (nace PENDIENTE) → getSummary/monthlyRevenue NO cambia respecto al baseline (assert igual antes/despues); el saldo firme no se mueve.
    - R2: validar ese mismo pago (validate por admin) → ahora SI suma al summary firme (assert delta == amount).
    - R3: cargar N cobros VALIDADOS (admin) → las metricas de revenue (getRevenueTrend/byMethod/byBranch/sumRevenue/ticket/ltv segun lo accesible por API o servicio) dan exactamente lo mismo que el baseline pre-predicado (los validados no se ven afectados por el nuevo filtro — backfill garantiza identidad).
    - R4: cargar un cobro PENDIENTE atado a una sub con deuda → balances del socio baja a 0 (la deuda se salda) AUNQUE el summary firme no lo cuente. Prueba que applyDelta corre independiente de validation_status (VAL-07, Pitfall 5).

    Usar createStaffUser({role:'coach'}) y {role:'admin'} para forzar el status server-side. Correr contra eltemplo_test_<POOL_ID>. NO modificar las suites existentes test/analytics/* ni test/reports/* — esas son el guard de regresion real y deben pasar sin cambios (verificar que siguen verdes en CI).

  </action>
  <acceptance_criteria>
    - validation-regression.test.ts no contiene it.todo; tiene casos R1, R2, R3, R4 implementados
    - R1 prueba pendiente no mueve summary; R2 valida si suma; R3 metricas identicas; R4 balances saldado con pendiente
    - Usa createStaffUser con role coach y admin para derivar status
    - Las suites existentes test/analytics/* y test/reports/* NO se modifican
    - `pnpm test -- test/finance/validation-regression.test.ts` pasa (en CI; typecheck local sí)
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && ! grep -q "it.todo" test/finance/validation-regression.test.ts && grep -c "role.*coach\|role.*admin\|balances\|monthlyRevenue\|summary" test/finance/validation-regression.test.ts; git diff --name-only -- test/analytics test/reports | wc -l; pnpm tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <done>R1-R4 implementados y verdes; suites de metricas existentes intactas; typecheck verde. Este es el gate de VAL-05.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                                               |
| ---------------------------------- | --------------------------------------------------------------------------------------------------------- |
| read-side filtro firme → reportes  | Un site que NO gana el predicado deja entrar PENDIENTES como plata firme (inconsistencia caja vs reporte) |
| SQL crudo (analytics/reports) → DB | Inyeccion si el fragmento firme se construyera con input de usuario                                       |

## STRIDE Threat Register

| Threat ID | Category                   | Component                             | Disposition | Mitigation Plan                                                                                                                 |
| --------- | -------------------------- | ------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------- |
| T-137-09  | Tampering                  | site olvidado del filtro firme        | mitigate    | Auditoria de 14 sites; 13 ganan el predicado; test de regresion R1 prueba que un pendiente no mueve summary                     |
| T-137-10  | Tampering (SQL injection)  | sites raw #9,#11,#12                  | mitigate    | FIRM_MONEY_SQL es constante estatica sin interpolacion de input de usuario; predicados parametrizados de Drizzle en el resto    |
| T-137-11  | Integrity                  | guard de cancelacion de sub (#14)     | accept      | Se documenta como excepcion deliberada: un PENDIENTE debe seguir bloqueando la cancelacion (no es ingreso firme, es cobro vivo) |
| T-137-12  | Tampering (cross-currency) | filtro firme vs aislamiento de moneda | accept      | El predicado nuevo no toca el aislamiento ARS/EUR existente; R3 verifica numeros identicos por moneda                           |
| T-137-SC  | Tampering                  | npm installs                          | mitigate    | Cero dependencias nuevas; sin install task                                                                                      |

</threat_model>

<verification>
- 13 de 14 sites ganan validation_status='validado'; el #14 queda documentado como excepcion.
- Los 3 sites raw usan la constante estatica FIRM_MONEY_SQL.
- Test de regresion R1-R4 verde + suites analytics/reports intactas y verdes en CI.
- typecheck local verde.
</verification>

<success_criteria>

- VAL-05: filtro canonico cuenta solo validados, centralizado; 6 metricas v5.0 identicas tras backfill (probado por R3 + suites existentes); 13 sites verdes, 1 excepcion documentada.
- VAL-07: un PENDIENTE salda balances pero no suma a caja firme (probado por R1+R4).
  </success_criteria>

<output>
Create `.planning/phases/137-m-quina-de-estados-de-validaci-n-cimiento/137-03-SUMMARY.md` when done
</output>
