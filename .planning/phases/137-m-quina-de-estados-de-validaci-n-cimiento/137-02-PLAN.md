---
phase: 137-m-quina-de-estados-de-validaci-n-cimiento
plan: 02
type: execute
wave: 2
depends_on: ["137-01"]
files_modified:
  - el-templo-api/src/modules/finance/transaction-service.ts
  - el-templo-api/src/modules/finance/types.ts
  - el-templo-api/src/modules/finance/schemas.ts
  - el-templo-api/src/modules/finance/routes.ts
  - el-templo-api/src/modules/subscriptions/service.ts
  - el-templo-api/test/finance/validation-state.test.ts
autonomous: true
requirements: [VAL-02, VAL-03, VAL-04, VAL-06, VAL-07]
must_haves:
  truths:
    - "Un pago cargado por coach nace pendiente; por admin/gestion/owner/recepcion nace validado (rol server-side)"
    - "Un assignment iniciado por admin (4 callers internos de recordAssignmentCharge) sigue naciendo validado sin editarse"
    - "validate() pasa un pendiente a validado con rastro en audit-log"
    - "observe() marca un pendiente como observado con rastro"
    - "correct() anula la vieja (validation_status=corregido) y crea la nueva (validado) linkeada via transaction_links, atomico"
    - "void() acepta keepMembershipActive (default true); en false desactiva la subscription linkeada via _cancelSubscription(tx,...) en la misma tx"
    - "Solo FINANCE_VOID_ROLES puede validate/observe/correct/void; coach es rechazado"
    - "applyDelta corre en el create independiente de validation_status (un pendiente ya salda balances)"
  artifacts:
    - path: "el-templo-api/src/modules/finance/transaction-service.ts"
      provides: "validate/observe/correct + _void(tx,...) interno + void() extendido + derivacion rol->status en create()"
      contains: "async validate"
    - path: "el-templo-api/src/modules/finance/routes.ts"
      provides: "Endpoints validate/observe/correct con guard FINANCE_VOID_ROLES + derivacion server-side"
      contains: "FINANCE_VOID_ROLES"
    - path: "el-templo-api/src/modules/subscriptions/service.ts"
      provides: "_cancelSubscription(tx,...) interno reusado por void() + recorderRole opcional (default admin->validado)"
      contains: "_cancelSubscription"
    - path: "el-templo-api/test/finance/validation-state.test.ts"
      provides: "Tests de transiciones + RBAC + keepMembershipActive"
      contains: "validate"
  key_links:
    - from: "el-templo-api/src/modules/finance/transaction-service.ts"
      to: "transaction_links target_kind='transaction'"
      via: "correct() linkea nueva->vieja"
      pattern: "targetKind.*transaction"
    - from: "el-templo-api/src/modules/finance/transaction-service.ts"
      to: "auditLog.write"
      via: "rastro de cada transicion"
      pattern: "auditLog.write"
---

<objective>
La maquina de estados: agregar a TransactionService los metodos `validate()`, `observe()`, `correct()` (anular+recrear atomico) y extender `void()` con el contrato `keepMembershipActive`. Derivar `validation_status` del rol autenticado server-side (coach→pendiente, resto→validado) en el path de create, incluyendo el create que dispara `recordAssignmentCharge`. Exponer los endpoints REST nuevos con guard `FINANCE_VOID_ROLES`. Llenar el scaffold de test de estado con casos reales + RBAC.

Purpose: Es el corazon backend de VAL-02/03/04/06/07. NO abre el endpoint de create al rol coach (eso es fase 140) — solo deja la derivacion rol→status implementada y testeada (resolved open question 1).

Output: 4 metodos de servicio + 3 endpoints + derivacion de status + tests de transiciones y autorizacion verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/137-m-quina-de-estados-de-validaci-n-cimiento/137-CONTEXT.md
@.planning/phases/137-m-quina-de-estados-de-validaci-n-cimiento/137-RESEARCH.md
@./CLAUDE.md
@.planning/phases/137-m-quina-de-estados-de-validaci-n-cimiento/137-01-SUMMARY.md

<interfaces>
<!-- Identificadores exactos extraidos del codebase. -->

TransactionService (transaction-service.ts):

- constructor(db, log, balanceService)
- create(input: CreateTransactionInput, recordedBy: number, tx?: TxHandle): Promise<TransactionDetail> (linea 77) — ya acepta tx opcional; el INSERT de financial_transactions esta en linea 172-187 (sin validationStatus aun — usa el DEFAULT 'validado')
- void(id, voidedBy, input: VoidTransactionInput): Promise<TransactionDetail> (linea 239) — ABRE SU PROPIA db.transaction (linea 244); setea voidedAt/voidedBy/voidReason; llama balanceService.applyDelta(tx, existing, linkRows, -1) (linea 276); auditLog.write con action "transaction_voided" (linea 282)
- NO existe update() (inmutabilidad deliberada del ledger)
- imports drizzle disponibles: eq, desc, and, sql, gt, gte, lte, inArray, isNull, type SQL

void() interno a extraer: `private async _void(tx: TxHandle, id, voidedBy, input, statusOverride?: "corregido"): Promise<void>` que reciba el handle; el void() publico lo envuelve en this.db.transaction. Espejo del patron create(tx?). Cuando statusOverride='corregido' tambien setea validationStatus='corregido' (distingue void-por-correccion de void-por-devolucion, D-05).

correct(originalId, correctedFields, adminId): dentro de UNA this.db.transaction → \_void(tx, originalId, adminId, {reason}, "corregido") + create(nuevoInput, adminId, tx) + INSERT transaction_links {transactionId: nuevoId, targetKind: "transaction", targetId: originalId} + auditLog.write(tx, {action: "transaction_corrected", targetKind: "transaction", targetId: originalId, ...}). correctedFields = subset de {amount, memberId, paymentMethod} (resolved open question 2); el resto se copia del original.

create() derivacion rol→status: agregar a CreateTransactionInput un campo opcional `validationStatus?: "pendiente"|"validado"` y pasarlo al INSERT (default 'validado' si undefined — coincide con el DEFAULT de la columna). La derivacion del valor vive en el CALLER (route/subscriptions), nunca en el body crudo del cliente (VAL-02). applyDelta NO conoce validationStatus (anti-pattern: no atarlo).

routes.ts:

- onRequest hook ya hace authenticate + FINANCE_READ_ROLES (linea 60-71)
- POST /transactions (linea 76): guard requiredRoles segun kind; en este path agregar la derivacion: const initialStatus = COACH_ROLES-only? No — usar: `["coach"].includes(request.user.role) ? "pendiente" : "validado"`. En 137 el create sigue gated por FINANCE_WRITE_ROLES (coach NO esta) → en la practica siempre 'validado' por REST; pero la derivacion debe existir y testearse simulando rol. NO agregar coach a FINANCE_WRITE_ROLES (fase 140).
- POST /transactions/:id/void (linea 172): guard FINANCE_VOID_ROLES; extender body con keepMembershipActive?: boolean (default true)
- handleServiceError(err, reply, request.log, "...") patron de catch

permissions.ts (importables): FINANCE_WRITE_ROLES=["owner","admin","gestion","recepcion"], FINANCE_VOID_ROLES=["owner","admin","gestion"], COACH_ROLES incluye "coach". coach NO esta en FINANCE_WRITE_ROLES ni FINANCE_VOID_ROLES. Tipo `AdminRole` exportado en permissions.ts (= ALL_STAFF_ROLES[number]) — usarlo para tipar recorderRole.

subscriptions/service.ts:

- recordAssignmentCharge(tx, params) (linea 248) llama transactionService.create({...}, params.adminId, tx) (linea 280). params ya trae adminId. Para derivar status aca: agregar a params un `recorderRole?: AdminRole` OPCIONAL que DEFAULTEA al path admin (→ validado). **CRITICO (warning 1 del checker):** recordAssignmentCharge tiene 4 callers internos (lineas ~1192, ~2699, ~3042, ~3363) que NO pasan rol — TODOS deben seguir produciendo VALIDADO sin tocarse. Por eso `recorderRole` es OPCIONAL y la derivacion es `validationStatus = recorderRole === "coach" ? "pendiente" : "validado"` (undefined → validado). Asi un assignment iniciado por admin NUNCA filtra un PENDIENTE y los 4 callers existentes quedan correctos sin editarse (solo se edita el site de definicion para la firma). La membresia se activa ANTES/independiente del create (VAL-07) — no tocar esa parte.

- cancelSubscription(userId, actorId, notes?, subscriptionId?) (linea 2052) = el metodo de cancelacion de sub existente. **Setea status='cancelled' + cancelledAt + recomputeUserStatus**, atomico dentro de una db.transaction. **PROBLEMA (warning 2 del checker):** ABRE SU PROPIA db.transaction (linea 2104) — mismo pitfall que void(). Ademas CONTIENE el guard SUB_HAS_ACTIVE_TRANSACTIONS (lineas ~2104-2145, el MISMO site #14 que el plan 03 documenta) que bloquea cancelar si hay cobros vivos linkeados. Por eso NO se puede llamar al cancelSubscription() publico desde void(): (a) abriria una 2da tx → no atomico con el soft-void; (b) su guard re-detectaria el cobro que se esta anulando. SOLUCION: extraer `private async _cancelSubscription(tx: TxHandle, userId, actorId, notes?, subscriptionId?)` moviendo el cuerpo del `db.transaction` (linea 2104+) adentro y operando contra el tx recibido; el `cancelSubscription` publico lo envuelve en this.db.transaction (comportamiento identico para callers existentes). Cuando lo invoca void() debe SALTEAR el guard SUB_HAS_ACTIVE_TRANSACTIONS (el cobro ya quedo soft-voided en el mismo tx); parametrizar con un flag interno tipo `skipActiveChargesGuard` que solo void() setea.

void() keepMembershipActive=false: dentro del \_void (en la misma tx), si false y existe un link target_kind='subscription', invocar `_cancelSubscription(tx, sub.memberId, voidedBy, ...)` con el guard salteado, en la MISMA tx. Default true = no tocar la sub (comportamiento actual).

audit-log: AuditAction ya extendido en plan 01 con transaction_validated/observed/corrected. targetKind 'transaction' ya existe.

test/helpers.ts: createTestApp, createStaffUser({role}), getAuthToken, createTestMember, assignTestPlan. DB real eltemplo*test*<POOL_ID>.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _void interno + validate/observe en TransactionService</name>
  <read_first>
    - el-templo-api/src/modules/finance/transaction-service.ts lineas 60-314 (constructor, create, void completo)
    - el-templo-api/src/modules/finance/types.ts (VoidTransactionInput, TransactionDetail, CreateTransactionInput)
    - el-templo-api/src/modules/shared/audit-log.ts (write signature + nuevos action types)
    - .planning/phases/137-m-quina-de-estados-de-validaci-n-cimiento/137-RESEARCH.md (Pitfall 3: void abre su propia tx; Pattern 2)
  </read_first>
  <behavior>
    - validate(id, adminId): un pendiente → validado, escribe audit transaction_validated; falla si no existe, si ya esta validado, o si esta anulado
    - observe(id, adminId, reason): un pendiente → observado, escribe audit transaction_observed
    - _void(tx, id, voidedBy, input, statusOverride?): opera dentro del tx del caller; setea voidedAt/voidedBy/voidReason (+ validationStatus='corregido' si statusOverride), revierte applyDelta(-1), escribe audit
    - void() publico: envuelve _void en this.db.transaction (comportamiento identico al actual para callers existentes)
  </behavior>
  <action>
    Extraer `private async _void(tx, id, voidedBy, input, statusOverride?: "corregido")` moviendo el cuerpo actual de void() (lineas 245-300) adentro, operando contra el `tx` recibido en vez de abrir uno nuevo. Cuando statusOverride='corregido', incluir `validationStatus: "corregido"` en el .set() del update. El `void()` publico pasa a ser `return this.db.transaction(tx => this._void(tx, ...))` + el re-read final. Agregar `keepMembershipActive` al manejo (Task 3 completa la rama de desactivacion — aca solo aceptar el parametro en VoidTransactionInput).

    Agregar `async validate(id, adminId)`: this.db.transaction → select existing; throw NotFoundError si no existe; throw BadRequestError si voidedAt!=null o validationStatus!=='pendiente'; update validationStatus='validado'; auditLog.write(tx, {actorId: adminId, action:"transaction_validated", targetKind:"transaction", targetId:id, payload:{...}}); re-read y return TransactionDetail.

    Agregar `async observe(id, adminId, input)`: similar, transicion pendiente→observado, exige reason, audit action "transaction_observed". Usar `catch (err: unknown)` con instanceof Error donde aplique; logger Pino (this.log), nunca console.

  </action>
  <acceptance_criteria>
    - transaction-service.ts contiene `private async _void(` y el `void()` publico lo llama dentro de this.db.transaction
    - transaction-service.ts contiene `async validate(` y `async observe(`
    - validate transiciona pendiente→validado y escribe action "transaction_validated"; observe → "transaction_observed"
    - _void con statusOverride='corregido' setea validationStatus='corregido'
    - typecheck local pasa
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && grep -c "_void\|async validate\|async observe\|transaction_validated\|transaction_observed" src/modules/finance/transaction-service.ts && pnpm tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <done>_void extraido, void() lo envuelve, validate/observe implementados con audit; typecheck verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: correct() atomico + derivacion rol->status en create</name>
  <read_first>
    - el-templo-api/src/modules/finance/transaction-service.ts (create linea 77-231, _void de Task 1)
    - el-templo-api/src/db/schema/transaction-links.ts (target_kind='transaction' ya existe)
    - el-templo-api/src/modules/finance/types.ts (CreateTransactionInput)
    - .planning/phases/137-m-quina-de-estados-de-validaci-n-cimiento/137-RESEARCH.md (Pattern 2 correct(); Pattern 3 rol->status; resolved open questions 1 y 2)
  </read_first>
  <behavior>
    - correct(originalId, correctedFields, adminId): en UNA tx → _void(original, statusOverride='corregido') + create(nueva nacida validado) + link target_kind='transaction' → original + audit transaction_corrected; si el create falla, el void NO commitea (atomicidad)
    - correctedFields acepta subset de {amount, memberId, paymentMethod}; el resto se copia del original
    - create() con validationStatus='pendiente' inserta una fila pendiente PERO applyDelta corre igual (balances se salda)
  </behavior>
  <action>
    Agregar `validationStatus?: "pendiente"|"validado"` a CreateTransactionInput (types.ts) y pasarlo en el INSERT de create() (linea ~174) como `validationStatus: input.validationStatus ?? "validado"`. NO condicionar applyDelta a este valor (corre siempre, linea 215) — anti-pattern critico (Pitfall 5).

    Implementar `async correct(originalId: number, correctedFields: Partial<Pick<CreateTransactionInput, "amount"|"memberId"|"paymentMethod">>, adminId: number): Promise<TransactionDetail>` dentro de this.db.transaction(tx => ...): (1) select original (throw si no existe / ya anulado); (2) _void(tx, originalId, adminId, {reason:"Correccion"}, "corregido"); (3) armar nuevo input copiando campos del original + override de correctedFields + validationStatus:"validado" + links copiados del original; (4) create(nuevoInput, adminId, tx); (5) INSERT transaction_links {transactionId: nuevoId, targetKind:"transaction", targetId: originalId}; (6) auditLog.write(tx, {action:"transaction_corrected", targetKind:"transaction", targetId: originalId, payload:{newTxId, correctedFields}}). Devolver el TransactionDetail de la nueva.

  </action>
  <acceptance_criteria>
    - CreateTransactionInput tiene `validationStatus?` opcional; create() lo pasa al INSERT con default 'validado'
    - applyDelta sigue corriendo sin condicionar a validationStatus (grep: applyDelta no esta dentro de un if de status)
    - correct() existe, abre UNA tx, llama _void con 'corregido' + create + link target_kind='transaction' + audit transaction_corrected
    - typecheck local pasa
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && grep -c "async correct\|validationStatus\|transaction_corrected" src/modules/finance/transaction-service.ts && grep -c "validationStatus" src/modules/finance/types.ts && pnpm tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <done>correct() atomico con link + audit; create acepta validationStatus sin tocar applyDelta; typecheck verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Endpoints + derivacion server-side + keepMembershipActive + tests de estado/RBAC</name>
  <read_first>
    - el-templo-api/src/modules/finance/routes.ts lineas 55-233 (onRequest hook, POST /transactions, POST void)
    - el-templo-api/src/modules/finance/schemas.ts (createTransactionSchema, voidTransactionSchema — patron de JSON schema Fastify)
    - el-templo-api/src/modules/subscriptions/service.ts lineas 248-318 (recordAssignmentCharge) + sus 4 callers internos (~1192, ~2699, ~3042, ~3363) — confirmar que NINGUNO pasa rol hoy
    - el-templo-api/src/modules/subscriptions/service.ts lineas 2052-2145 (cancelSubscription: abre su propia db.transaction en ~2104 + contiene el guard SUB_HAS_ACTIVE_TRANSACTIONS — el metodo concreto a extraer como _cancelSubscription)
    - el-templo-api/test/finance/validation-state.test.ts (scaffold del plan 01 a llenar)
    - el-templo-api/src/modules/shared/permissions.ts (FINANCE_VOID_ROLES, COACH_ROLES, FINANCE_WRITE_ROLES, tipo AdminRole)
  </read_first>
  <behavior>
    - POST /transactions/:id/validate (FINANCE_VOID_ROLES) → 200, pendiente→validado; coach → 403
    - POST /transactions/:id/observe (FINANCE_VOID_ROLES, body reason) → 200
    - POST /transactions/:id/correct (FINANCE_VOID_ROLES, body correctedFields) → 201 nueva tx, vieja corregida+anulada
    - POST /transactions/:id/void con keepMembershipActive=false → cancela la sub linkeada (status='cancelled') via _cancelSubscription en la misma tx; default true la deja activa
    - create vía recordAssignmentCharge con recorderRole='coach' → fila pendiente; recorderRole admin O AUSENTE (undefined) → validado; balances se salda en ambos
    - los 4 callers internos de recordAssignmentCharge (que no pasan recorderRole) siguen produciendo validado sin editarse
  </behavior>
  <action>
    En routes.ts agregar 3 endpoints: `POST /transactions/:id/validate`, `/observe`, `/correct`, cada uno con guard `if (!(FINANCE_VOID_ROLES as readonly string[]).includes(request.user.role)) return 403`, llamando transactionService.validate/observe/correct con request.user.userId como adminId, patron try/catch + handleServiceError. Definir sus JSON schemas en schemas.ts (observe/correct requieren reason/correctedFields validados — V5 input validation).

    En POST /transactions: derivar `const initialStatus = (["coach"] as readonly string[]).includes(request.user.role) ? "pendiente" : "validado"` y pasarlo como `validationStatus: initialStatus` al transactionService.create input. NO agregar coach a FINANCE_WRITE_ROLES (se difiere a fase 140); el guard de create sigue igual. En POST void: leer keepMembershipActive del body (default true) y pasarlo a void().

    En subscriptions/service.ts (warning 1): agregar `recorderRole?: AdminRole` OPCIONAL a los params de recordAssignmentCharge, con derivacion `validationStatus = recorderRole === "coach" ? "pendiente" : "validado"` (undefined → validado) pasada al create input. NO editar los 4 callers internos (~1192, ~2699, ~3042, ~3363) — al ser opcional y defaultear a validado, quedan correctos sin tocarse y ningun assignment de admin filtra un PENDIENTE.

    En subscriptions/service.ts (warning 2): extraer `private async _cancelSubscription(tx, userId, actorId, notes?, subscriptionId?, opts?: { skipActiveChargesGuard?: boolean })` moviendo el cuerpo del db.transaction de cancelSubscription (~2104) adentro; el publico `cancelSubscription` lo envuelve en this.db.transaction (callers existentes sin cambios). Cuando `opts.skipActiveChargesGuard` es true (solo lo setea void()), saltear el guard SUB_HAS_ACTIVE_TRANSACTIONS. En void()/_void (transaction-service.ts): implementar la rama keepMembershipActive=false invocando `_cancelSubscription(tx, sub.memberId, voidedBy, ..., { skipActiveChargesGuard: true })` dentro del MISMO tx del void (NO llamar al cancelSubscription publico — abriria una 2da tx). Si esto requiere inyectar SubscriptionService en TransactionService, usar el patron de inyeccion existente del modulo finance.

    Llenar validation-state.test.ts reemplazando los it.todo por casos reales con DB: (a) coach create → validation_status='pendiente'; admin create → 'validado' (simular rol via createStaffUser); recordAssignmentCharge sin recorderRole → validado; (b) validate pendiente→validado; (c) observe pendiente→observado; (d) correct → vieja corregido+void, nueva validado, link transaction presente; (e) RBAC: coach recibe 403 en validate/observe/correct/void; (f) void keepMembershipActive=false cancela la sub (status='cancelled'), default true la deja activa.

  </action>
  <acceptance_criteria>
    - routes.ts tiene los 3 endpoints validate/observe/correct, cada uno con guard FINANCE_VOID_ROLES
    - POST /transactions deriva initialStatus server-side (coach→pendiente) y lo pasa a create; NO se agrega coach a FINANCE_WRITE_ROLES
    - recordAssignmentCharge tiene `recorderRole?` OPCIONAL que defaultea a validado; `git diff` muestra que SOLO el site de definicion de recordAssignmentCharge cambia su firma — los 4 callers (~1192, ~2699, ~3042, ~3363) quedan SIN modificar (`git diff src/modules/subscriptions/service.ts` no toca esas lineas de llamada)
    - subscriptions/service.ts contiene `_cancelSubscription(` y el `cancelSubscription` publico lo envuelve en this.db.transaction
    - void() implementa la rama keepMembershipActive=false invocando `_cancelSubscription(tx, ...)` con skipActiveChargesGuard en el MISMO tx (NO el cancelSubscription publico)
    - validation-state.test.ts tiene casos reales (no it.todo) para transiciones + RBAC + keepMembershipActive=false-cancela / true-deja-activa + recordAssignmentCharge sin rol → validado
    - `pnpm test -- test/finance/validation-state.test.ts` pasa (en CI; typecheck local sí)
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && grep -c "validate\|observe\|correct\|keepMembershipActive\|FINANCE_VOID_ROLES" src/modules/finance/routes.ts && grep -c "_cancelSubscription\|recorderRole\|skipActiveChargesGuard" src/modules/subscriptions/service.ts && ! grep -q "it.todo" test/finance/validation-state.test.ts && grep -c "coach.*FINANCE_WRITE_ROLES" src/modules/shared/permissions.ts; pnpm tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <done>3 endpoints + derivacion server-side + recorderRole opcional (4 callers intactos) + keepMembershipActive=false via _cancelSubscription(tx) atomico + tests de estado/RBAC sin todos; typecheck verde. NO se abrio el create al rol coach (diferido a 140).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                           |
| ------------------------------- | --------------------------------------------------------------------- |
| cliente → API finance           | El cliente NO debe poder elegir su validation_status ni su rol        |
| coach → endpoints de validacion | Un coach no debe validar/observar/corregir/anular (escalada)          |
| correct() multi-write           | Estado parcial (vieja anulada sin reemplazo) si no es atomico         |
| void()+cancel sub multi-write   | Estado parcial (cobro anulado pero sub no cancelada) si no es atomico |

## STRIDE Threat Register

| Threat ID | Category                    | Component                                           | Disposition | Mitigation Plan                                                                                                                                                                                         |
| --------- | --------------------------- | --------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-137-04  | Spoofing/Tampering          | POST /transactions (status del body)                | mitigate    | validation_status derivado server-side de request.user.role; nunca leido del body (VAL-02); test que un coach simulado nace pendiente                                                                   |
| T-137-05  | Elevation of Privilege      | validate/observe/correct/void                       | mitigate    | Guard FINANCE_VOID_ROLES en cada endpoint; test RBAC que coach recibe 403                                                                                                                               |
| T-137-06  | Tampering (money integrity) | correct() void+recreate                             | mitigate    | Una sola this.db.transaction via \_void(tx,...); si el create falla el void no commitea; test de atomicidad                                                                                             |
| T-137-07  | Repudiation                 | cada transicion                                     | mitigate    | auditLog.write con actorId del principal autenticado + reason en validate/observe/correct/void                                                                                                          |
| T-137-08  | Elevation of Privilege      | apertura prematura del create a coach               | accept      | NO se agrega coach a FINANCE_WRITE_ROLES en 137; la apertura del rol profe es fase 140 (resolved open question 1)                                                                                       |
| T-137-13  | Tampering (money integrity) | void(keepMembershipActive=false)+cancelSubscription | mitigate    | Extraer \_cancelSubscription(tx,...) e invocarlo en el MISMO tx del void (salteando el guard SUB_HAS_ACTIVE_TRANSACTIONS porque el cobro ya quedo soft-voided en ese tx); evita 2da tx y estado parcial |
| T-137-14  | Tampering                   | recordAssignmentCharge recorderRole por defecto     | mitigate    | recorderRole OPCIONAL defaultea a validado: un assignment de admin nunca nace PENDIENTE; los 4 callers internos quedan correctos sin editarse                                                           |
| T-137-SC  | Tampering                   | npm installs                                        | mitigate    | Cero dependencias nuevas; sin install task                                                                                                                                                              |

</threat_model>

<verification>
- 4 metodos de servicio (validate/observe/correct + void extendido con _void interno).
- Derivacion rol→status server-side, sin abrir create a coach.
- recorderRole opcional (default validado): 4 callers de recordAssignmentCharge intactos, sin fuga de PENDIENTE de admin.
- keepMembershipActive=false cancela la sub real via _cancelSubscription(tx) atomico (backend testeable, VAL-06 backend-complete).
- applyDelta NO atado a validation_status (un pendiente salda balances).
- Tests de transiciones + RBAC + keepMembershipActive verdes.
</verification>

<success_criteria>

- VAL-02: coach→pendiente, admin→validado, server-side (incl. recordAssignmentCharge con recorderRole opcional).
- VAL-03: validate() pendiente→validado.
- VAL-04: observe()+correct() (void+recreate atomico, link transaction).
- VAL-06: solo admin anula; keepMembershipActive=false cancela la sub atomicamente.
- VAL-07: membresia activa al instante; pendiente salda balances (applyDelta independiente del status).
  </success_criteria>

<output>
Create `.planning/phases/137-m-quina-de-estados-de-validaci-n-cimiento/137-02-SUMMARY.md` when done
</output>
