---
phase: 89-knowledge-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-bot/src/ai/knowledge.ts
  - el-templo-bot/src/ai/system-prompt.ts
  - el-templo-bot/test/fixtures/pb1-e1a-lead-rendered.snap.txt
autonomous: false
requirements: [KFIX-01, KFIX-02, KFIX-03, KFIX-04]

must_haves:
  truths:
    - "PB1.E1A lead rendered prompt contains zero membership plan price numbers (Flex, Foundation, Foundation+, Performance monthly prices)."
    - "Trial class nominal price ($20,000) still appears in the PB1.E1A lead rendered prompt (Boarding Pass anchor preserved)."
    - "'Planes y Precios' section heading is absent from the PB1.E1A lead rendered prompt."
    - "The Metodo (elevator) section appears in the lead prompt BEFORE 'Que es El Templo' section."
    - "ELEVATOR_TEXT contains all three team hooks: 'método internacional', a 'cuatro niveles' variant, and 'no salirse del grupo' (or faithful variant preserving meaning)."
    - "The canonical Boarding Pass definition in ZERO_RULES names BOTH benefits: (1) trial class 100% bonificada AND (2) precios Zero en primera membresía."
    - "system-prompt.ts *Reglas de conversacion* block contains a price-deferral rule instructing Mica to never invent prices and to re-anchor to the trial class."
    - "Rendered PB1.E1A lead prompt size stays under KGATE-05 threshold (18,916 chars) with positive headroom (target: >1,000 chars free, banked for Phases 90-92)."
    - "Snapshot fixture pb1-e1a-lead-rendered.snap.txt is regenerated to match the new rendered output and is committed in the same atomic task as the source change."
    - "All 537 existing bot tests still pass after the snapshot regeneration (no regression in QT11-18 or v5.3.1 state-gating behavior)."
  artifacts:
    - path: "el-templo-bot/src/ai/knowledge.ts"
      provides: "Repositioned elevator (SECTIONS[1]), restored ELEVATOR_TEXT ~131 chars, rewritten ZERO_RULES BP canonical with dual benefits, 'Planes y Precios' untagged"
      contains: "Both 'método internacional' AND 'no salirse del grupo' in ELEVATOR_TEXT; 'clase de prueba' AND 'precios Zero' in BP canonical"
    - path: "el-templo-bot/src/ai/system-prompt.ts"
      provides: "Universal price-deferral framing rule in *Reglas de conversacion* block"
      contains: "Nunca inventes precios"
    - path: "el-templo-bot/test/fixtures/pb1-e1a-lead-rendered.snap.txt"
      provides: "Regenerated snapshot matching new lead render"
  key_links:
    - from: "knowledge.ts SECTIONS[1] (Metodo elevator)"
      to: "rendered lead prompt (encountered FIRST on method questions)"
      via: "SECTIONS array ordering + discovery-tag filter"
      pattern: "Metodo \\(elevator\\).*Que es El Templo"
    - from: "knowledge.ts ZERO_RULES canonical BP"
      to: "rendered lead prompt (Reglas Zero is discovery-tagged)"
      via: "ZERO_RULES string embedded in Reglas Zero section body"
      pattern: "dos beneficios.*clase de prueba.*precios Zero|precios Zero.*clase de prueba"
    - from: "system-prompt.ts *Reglas de conversacion* block"
      to: "rendered prompt for ALL client states"
      via: "universal framing (not state-gated)"
      pattern: "Nunca inventes precios"
---

<objective>
Fix four behavioral knowledge gaps surfaced by the first post-v5.3.1 live test so that the PB1 lead prompt (a) no longer leaks membership plan prices during discovery, (b) surfaces the team method elevator ahead of the generic "Que es El Templo" description, (c) presents the Boarding Pass with BOTH of its benefits, and (d) carries an explicit defense-in-depth framing rule against price invention.

Purpose: Produce the source state that Phase 92 regression assertions will lock. Close the structural contradiction that caused plan prices to leak during E2A discovery despite the "no prices during discovery" rule.

Output:

- Modified el-templo-bot/src/ai/knowledge.ts (SECTIONS reorder, ELEVATOR_TEXT restored, ZERO_RULES BP canonical rewritten, Planes y Precios untagged).
- Modified el-templo-bot/src/ai/system-prompt.ts (new price-deferral framing rule).
- Regenerated el-templo-bot/test/fixtures/pb1-e1a-lead-rendered.snap.txt committed atomically with the source change.
- Measurement report in SUMMARY: rendered lead char count, KGATE-05 headroom delta, banked chars for downstream phases.
  </objective>

<execution_context>
@/Users/bores/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bores/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/89-knowledge-fixes/89-CONTEXT.md

# Prior-phase SUMMARYs — load because we are editing sections they created

@.planning/phases/87-boarding-pass-method-description/87-02-SUMMARY.md
@.planning/phases/88-quality-regression-lock/88-02-SUMMARY.md

# Source files this plan modifies

@el-templo-bot/src/ai/knowledge.ts
@el-templo-bot/src/ai/system-prompt.ts
@el-templo-bot/test/fixtures/pb1-e1a-lead-rendered.snap.txt
@el-templo-bot/test/ai/rendered-prompt-snapshot.test.ts
@el-templo-bot/test/ai/prompt-size.test.ts
@el-templo-bot/CLAUDE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft the three new/modified wordings into a review scratchpad</name>
  <files>.planning/phases/89-knowledge-fixes/89-01-DRAFT-WORDINGS.md</files>
  <action>
Produce draft wordings for the three user-review items. DO NOT TOUCH SOURCE FILES YET. Write a scratchpad file at `.planning/phases/89-knowledge-fixes/89-01-DRAFT-WORDINGS.md` containing three clearly-labeled sections, each with: proposed text, char count, hooks/elements preserved, and placement context.

**Draft A — restored ELEVATOR_TEXT (target ~131 chars, ≤150 hard cap):**

- Current (95 chars): "Método internacional de calistenia, cuatro niveles por clase — progresás sin salirte del grupo."
- Pre-v5.3.1 reference length: ~131 chars (from Phase 87-02 pre-compression).
- MUST preserve all three hooks: "método internacional", a "cuatro niveles" variant (e.g., "cuatro niveles simultáneos" OR "cuatro niveles por clase" OR "cuatro niveles activos simultáneamente"), and "no salirse/salirte del grupo" (tuteo OK).
- MUST restore conversational warmth lost in the 95-char compression (the body currently reads as a terse spec line; should read as a confident team pitch).
- Tuteo preserved (progresás, etc.).
- Provide ONE recommended draft plus ONE alternative for user to pick between.

**Draft B — ZERO_RULES canonical Boarding Pass rewrite:**

- Current location: knowledge.ts line ~156 (inside ZERO_RULES const), currently the first paragraph of ZERO_RULES contains the BP definition.
- User-suggested shape: "El Boarding Pass es un pase digital único que recibís al contactarte por primera vez con El Templo. Tiene dos beneficios: (1) la clase de prueba 100% bonificada, y (2) precios Zero en la primera membresía que contrates."
- MUST explicitly name BOTH benefits; ordering (1)-(2) should match the user draft.
- Natural Spanish, tuteo (recibís, contrates). Numbered enumeration "(1)...(2)..." kept because it makes the dual-benefit structure unmissable to the model.
- Show the EXACT diff context: the current first paragraph of ZERO_RULES that will be replaced, plus the full rewritten text so the user can compare.

**Draft C — price-deferral framing rule for system-prompt.ts:**

- Placement: inside the `*Reglas de conversacion*` block in system-prompt.ts, adjacent to (not replacing) the existing v5.3.1 method-internals deflection rule at line 194. Prepend or append — propose placement WITH rationale (my recommendation: append, so the newer rule sits beneath the established one).
- User-suggested shape: "Nunca inventes precios de membresías. Si el lead pregunta por precios durante discovery, respondé con el defer pattern de la stage actual y re-anclá la prueba gratuita. Si no estás seguro de un precio, NO lo menciones — solo ofrecé la clase de prueba como próximo paso."
- Target: ~200 chars. Tuteo. Bullet style matching adjacent rules ("- ...").
- MUST work for all client states (universal framing — see 87-02 decision pattern).

For each draft, include:

1. The exact text as it will be written to the file.
2. Char count (body only; for ELEVATOR_TEXT count the string between backticks; for BP count the replacement paragraph; for the rule count the full bullet incl. leading "- ").
3. Hooks/benefits preserved (checklist).
4. Delta estimate vs current rendered PB1.E1A lead prompt (approximate; KFIX-01 untagging alone will free ~1,700 chars, so the net is comfortably positive).

Also include a top-level "Budget projection" table estimating post-application rendered-lead chars:
| Change | Δ chars (lead render) |
| KFIX-01 remove 'discovery' tag from Planes y Precios | -~1,700 |
| KFIX-03 reposition elevator (no char delta, ordering only) | 0 |
| KFIX-03 restore ELEVATOR_TEXT 95 → ~131 | +~36 |
| KFIX-04 ZERO_RULES BP canonical rewrite | +(delta from current first para) |
| Framing rule in system-prompt.ts | +~200 (universal) |
| **Projected net** | **strongly negative (= banked headroom)** |

Current baseline rendered PB1.E1A lead = 18,858 chars (from 88-02 SUMMARY). Projected post = ~17,350-17,450. KGATE-05 threshold = 18,916. Expected banked headroom ~1,470 chars.

DO NOT run vitest in this task — this task is pure drafting + projection. No source edits. No snapshot regen.
</action>
<verify>
<automated>test -f .planning/phases/89-knowledge-fixes/89-01-DRAFT-WORDINGS.md && grep -c "método internacional" .planning/phases/89-knowledge-fixes/89-01-DRAFT-WORDINGS.md | xargs -I{} test {} -ge 1 && grep -c "precios Zero" .planning/phases/89-knowledge-fixes/89-01-DRAFT-WORDINGS.md | xargs -I{} test {} -ge 1 && grep -c "Nunca inventes precios" .planning/phases/89-knowledge-fixes/89-01-DRAFT-WORDINGS.md | xargs -I{} test {} -ge 1</automated>
<manual>Scratchpad contains three clearly-labeled draft sections with exact text, char counts, hook checklists, and the budget projection table.</manual>
<sampling_rate>run at end of Task 1 before checkpoint</sampling_rate>
</verify>
<done>
Scratchpad `.planning/phases/89-knowledge-fixes/89-01-DRAFT-WORDINGS.md` exists with three draft sections (ELEVATOR_TEXT, BP canonical, framing rule), char counts, hook checklists, and a budget projection table. Zero source files modified. No snapshot regenerated.
</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User review gate — approve all three wordings before source edit</name>
  <what-built>
    Scratchpad `.planning/phases/89-knowledge-fixes/89-01-DRAFT-WORDINGS.md` with three drafts:
    - Draft A: restored ELEVATOR_TEXT (~131 chars, 3 hooks preserved)
    - Draft B: ZERO_RULES canonical Boarding Pass rewrite (dual benefits)
    - Draft C: price-deferral framing rule for system-prompt.ts (~200 chars)
    Plus a budget projection table showing expected post-application rendered-lead char count.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/89-knowledge-fixes/89-01-DRAFT-WORDINGS.md`.
    2. Review Draft A (ELEVATOR_TEXT). Confirm tone, all three hooks present (método internacional / cuatro niveles variant / no salirse del grupo), tuteo, ~131 chars. If more than one alternative was provided, pick one (or request a revision).
    3. Review Draft B (BP canonical). Confirm BOTH benefits named clearly, natural Spanish, tuteo.
    4. Review Draft C (price-deferral rule). Confirm placement (near line 194 in *Reglas de conversacion*), tuteo, bullet style matches adjacent rules.
    5. Review the budget projection table. Confirm the net is comfortably negative (freed chars) and the projected lead render stays well under 18,916.
    6. If ANY draft needs revision, reply with the specific change you want (e.g., "Draft A: use 'cuatro niveles simultáneos' instead of 'cuatro niveles por clase'"). Claude will revise and loop back to this checkpoint.
    7. If all three drafts are approved, reply with "approved" (or equivalent).
  </how-to-verify>
  <resume-signal>Type "approved" if all three drafts are good, or describe the specific revisions needed for each draft.</resume-signal>
  <files>.planning/phases/89-knowledge-fixes/89-01-DRAFT-WORDINGS.md</files>
  <action>Pause execution and wait for user to review the three draft wordings produced by Task 1. User inspects the scratchpad, confirms each draft is acceptable (or requests specific revisions). If revisions requested, Claude revises the scratchpad and loops back to this checkpoint. If approved, resume with Task 3.</action>
  <verify>
    <automated>MISSING — human verification; no automated command applicable for a checkpoint gate</automated>
    <manual>User replies with "approved" (or equivalent) after reviewing all three drafts in the scratchpad.</manual>
    <sampling_rate>blocking — Task 3 cannot start until user confirms</sampling_rate>
  </verify>
  <done>User has explicitly approved all three draft wordings (ELEVATOR_TEXT, BP canonical, price-deferral framing rule). Any requested revisions have been applied to the scratchpad and re-approved.</done>
</task>

<task type="auto">
  <name>Task 3: Apply all four knowledge fixes + framing rule + regenerate snapshot atomically</name>
  <files>
el-templo-bot/src/ai/knowledge.ts
el-templo-bot/src/ai/system-prompt.ts
el-templo-bot/test/fixtures/pb1-e1a-lead-rendered.snap.txt
  </files>
  <action>
Apply ALL source edits in this single task, then regenerate the snapshot, then run the full suite. All changes land in one atomic commit so the regenerated snapshot is never separated from the source change that invalidates it (per v5.3.1 update discipline, Phase 88-02 SUMMARY).

**Edit 1 — KFIX-01: Remove 'discovery' tag from Planes y Precios (SECTIONS[4])**

- File: `el-templo-bot/src/ai/knowledge.ts`.
- Locate the SECTIONS array entry for "Planes y Precios" (around line 570-574 based on current grep). The entry has `tags: ['discovery']`.
- Change `tags: ['discovery']` to `tags: []`.
- DO NOT touch the section body. DO NOT rename. DO NOT change position.
- Add a single-line comment above the entry explaining: `// [KFIX-01] 'discovery' tag removed — plan prices no longer injected into PB1 lead prompt. Section still renders for non-lead states (trial/active/inactive/expired).`

**Edit 2 — KFIX-03 (part 1): Reposition Metodo (elevator) to SECTIONS[1], above Que es El Templo**

- File: `el-templo-bot/src/ai/knowledge.ts`.
- Current order in SECTIONS: [0] Que es El Templo, [1] Metodo (elevator), [2] Metodo (detalle), [3] ROM, [4] Planes y Precios, [5] Mejora de plan, [6] Reglas Zero, ...
- Target order: [0] Metodo (elevator), [1] Que es El Templo, [2] Metodo (detalle), [3] ROM, [4] Planes y Precios, [5] Mejora de plan, [6] Reglas Zero, ... (swap positions 0 and 1 only).
- Rationale comment above the repositioned elevator: `// [KFIX-03] Elevator repositioned ahead of 'Que es El Templo' so model encounters team-preferred method framing first on method questions. Was SECTIONS[1], now SECTIONS[0].`
- Update the module-level overview JSDoc block at lines ~20-26 to reflect the new ordering (the comment currently says "Metodo (elevator) [NEW 87-02, discovery]" appears after "Que es El Templo" — swap that wording too).
- Update the SECTIONS introductory JSDoc near line ~495-498 similarly.

**Edit 3 — KFIX-03 (part 2): Restore ELEVATOR_TEXT to the user-approved wording from Draft A**

- File: `el-templo-bot/src/ai/knowledge.ts`, line ~439.
- Replace the current 95-char body with the user-approved Draft A text (expected ~131 chars).
- Update the preceding comment at line ~433 to reflect the new char count (e.g., "Restored to ~131 chars after KFIX-03 — three team hooks preserved, conversational warmth restored").

**Edit 4 — KFIX-04: Rewrite canonical BP in ZERO_RULES**

- File: `el-templo-bot/src/ai/knowledge.ts`, ZERO_RULES const at line ~155.
- Replace the current first paragraph (BP definition) of ZERO_RULES with the user-approved Draft B text.
- MUST explicitly name BOTH benefits: (1) clase de prueba 100% bonificada, (2) precios Zero en primera membresía.
- The rest of ZERO_RULES stays byte-for-byte identical.

**Edit 5 — Price-deferral framing rule in system-prompt.ts**

- File: `el-templo-bot/src/ai/system-prompt.ts`, inside `*Reglas de conversacion*` block.
- Insert the user-approved Draft C text as a new bullet immediately AFTER the existing line-194 method-internals deflection rule (or at the placement the user approved in Task 2).
- Bullet prefix `- ` matches adjacent rules.

**Regenerate snapshot:**

- Run: `cd el-templo-bot && pnpm vitest run test/ai/rendered-prompt-snapshot.test.ts` — this will FAIL (expected) because the fixture is stale.
- Regenerate the fixture. The test at `el-templo-bot/test/ai/rendered-prompt-snapshot.test.ts` reads the fixture path; the regeneration command is documented in that test's file header (per 88-02 SUMMARY, update discipline is "explicit commit with justification required; do NOT auto-update from failing test runs"). Read the test file header, follow its documented regeneration procedure (likely a one-liner that writes the currently-rendered prompt to the fixture path).
- If no explicit regeneration command exists in the test header, generate the new fixture content by running a small inline Node script: `node -e "import('./src/ai/system-prompt.ts').then(m => import('fs').then(fs => fs.writeFileSync('test/fixtures/pb1-e1a-lead-rendered.snap.txt', m.buildSystemPrompt(/* PB1.E1A lead args matching the existing test */))))"` — but first READ the snapshot test file to identify the exact call signature it uses to produce the expected content, and mirror that exactly.
- Commit the regenerated fixture in the SAME git commit as the source edits.

**Verify + measure:**

- Run: `cd el-templo-bot && pnpm vitest run` (full suite). Target: all tests green (537/537 or equivalent post-regen baseline).
- Run: `cd el-templo-bot && pnpm tsc --noEmit`. Target: exit 0.
- Measure rendered PB1.E1A lead char count (the snapshot test will reveal the new length; also check `test/ai/prompt-size.test.ts` output).
- Record in task output:
  - Rendered PB1.E1A lead chars (old 18,858 → new ?)
  - KGATE-05 headroom (threshold 18,916 − new chars)
  - Chars banked for Phases 90-92
- If rendered lead exceeds the KGATE-05 threshold OR if any non-snapshot test fails, HALT and surface for user decision (do NOT silently consume headroom; do NOT modify other tests to make them pass).

**Guardrails:**

- DO NOT edit `test/knowledge-gating.test.ts` or any other test file besides the regenerated snapshot fixture. Phase 89 does not add or modify assertions (Phase 92 does).
- DO NOT remove `'discovery'` tag from any section other than Planes y Precios.
- DO NOT touch TRIAL_FLOW (trial $20,000 stays — KFIX-02 requirement).
- DO NOT add the elevator framing rule to system-prompt.ts (held in reserve for Phase 92, per CONTEXT.md locked decision).
- DO NOT commit separately — the snapshot fixture MUST be in the same commit as the source edits.

**Commit message suggestion:**

```
feat(bot): knowledge fixes for v5.3.2 live-test findings

- [KFIX-01] Remove 'discovery' tag from Planes y Precios — plan prices
  no longer injected into PB1 lead prompt (structural fix for E2A
  no-prices-during-discovery rule)
- [KFIX-03] Reposition Metodo (elevator) to SECTIONS[0], restore
  ELEVATOR_TEXT to ~131 chars with three team hooks preserved
- [KFIX-04] Rewrite canonical Boarding Pass in ZERO_RULES to name
  BOTH benefits (trial gratis + precios Zero en primera membresía)
- Add universal price-deferral framing rule to system-prompt.ts
  (*Reglas de conversacion*) — defense-in-depth against price
  invention under gpt-4o-mini behavior
- Regenerate pb1-e1a-lead-rendered.snap.txt to match new lead render
  (expected regeneration per v5.3.1 update discipline)

Rendered PB1.E1A lead: 18,858 → <new> chars (KGATE-05 headroom:
58 → <new>, banked for phases 90-92)
All 537 bot tests green, tsc clean.
```

  </action>
  <verify>
    <automated>cd el-templo-bot && pnpm vitest run && pnpm tsc --noEmit</automated>
    <manual>
1. `git diff --stat` shows exactly 3 files changed: knowledge.ts, system-prompt.ts, pb1-e1a-lead-rendered.snap.txt — all in one commit.
2. `grep -c "método internacional" el-templo-bot/src/ai/knowledge.ts` ≥ 1.
3. `grep -c "no salirse del grupo\|no salirte del grupo" el-templo-bot/src/ai/knowledge.ts` ≥ 1.
4. `grep -c "Nunca inventes precios" el-templo-bot/src/ai/system-prompt.ts` == 1.
5. `grep "Planes y Precios" el-templo-bot/test/fixtures/pb1-e1a-lead-rendered.snap.txt` returns nothing (section no longer in lead render).
6. `grep -E '\$80[.,]000|\$100[.,]000|\$250[.,]000' el-templo-bot/test/fixtures/pb1-e1a-lead-rendered.snap.txt` returns nothing (no plan prices).
7. `grep "\$20[.,]000" el-templo-bot/test/fixtures/pb1-e1a-lead-rendered.snap.txt` returns at least one hit (trial price preserved, KFIX-02).
8. In the regenerated fixture, the "Metodo (elevator)" heading appears BEFORE the "Que es El Templo" heading (sort order check: `grep -n "Metodo (elevator)\|Que es El Templo" el-templo-bot/test/fixtures/pb1-e1a-lead-rendered.snap.txt`).
9. Rendered fixture char count is strictly less than 18,916 (KGATE-05 threshold).
    </manual>
    <sampling_rate>run after all source edits, before commit</sampling_rate>
  </verify>
  <done>
knowledge.ts carries the four knowledge fixes (tag removal, reposition, elevator restore, BP canonical rewrite). system-prompt.ts carries the new price-deferral framing rule. The snapshot fixture is regenerated and committed atomically with the source changes. Full bot suite (537/537 or equivalent) passes. `tsc --noEmit` clean. Rendered PB1.E1A lead char count strictly below 18,916 with at least ~1,000 chars banked for downstream phases. Committed on `feature/whatsapp-bot-scaffold`.
  </done>
</task>

</tasks>

<verification>
Overall phase checks (pre-commit):

1. **KFIX-01 — tag removal**:
   - `grep -A2 "title: \"Planes y Precios\"" el-templo-bot/src/ai/knowledge.ts | grep "tags:"` shows `tags: []`.
   - Rendered lead snapshot does NOT contain "Planes y Precios" heading.

2. **KFIX-02 — no plan prices, trial price preserved**:
   - Snapshot fixture contains zero of: `$80,000`, `$100,000`, `$250,000` (Flex/Foundation/Foundation+/Performance plan prices — verify exact numbers against current knowledge.ts Planes y Precios body before snapshotting).
   - Snapshot fixture DOES contain `$20,000` (trial class, in TRIAL_FLOW / Clase de Prueba section).

3. **KFIX-03 — elevator reposition + restore**:
   - SECTIONS[0] is "Metodo (elevator)", SECTIONS[1] is "Que es El Templo" (verify via a small test-free grep of SECTIONS order).
   - ELEVATOR_TEXT length ≥ 120 chars (up from 95).
   - ELEVATOR_TEXT contains "método internacional" AND a "cuatro niveles" variant AND "no salirse/salirte del grupo".
   - Snapshot fixture shows "Metodo (elevator)" heading appearing earlier (smaller byte offset) than "Que es El Templo".

4. **KFIX-04 — BP canonical dual benefit**:
   - ZERO_RULES first paragraph mentions "clase de prueba" AND "precios Zero" (or unambiguous variants thereof).
   - Snapshot fixture contains both phrases.

5. **Price-deferral framing rule**:
   - system-prompt.ts contains "Nunca inventes precios" in the _Reglas de conversacion_ block.
   - Snapshot fixture contains "Nunca inventes precios".

6. **Budget discipline**:
   - Rendered PB1.E1A lead chars < 18,916 (KGATE-05 threshold).
   - Banked headroom ≥ 1,000 chars for downstream phases.

7. **No regressions**:
   - All existing 537 tests pass.
   - `tsc --noEmit` exits 0.
   - Zero unintended files modified (`git diff --stat` = exactly 3 files).

8. **Atomic commit discipline**:
   - Snapshot fixture regeneration is in the SAME commit as the source edits (per v5.3.1 update discipline, Phase 88-02 SUMMARY).
     </verification>

<success_criteria>

- [ ] `el-templo-bot/src/ai/knowledge.ts` has Planes y Precios untagged, Metodo (elevator) at SECTIONS[0], ELEVATOR_TEXT ~131 chars with three hooks, ZERO_RULES BP canonical naming both benefits.
- [ ] `el-templo-bot/src/ai/system-prompt.ts` carries the price-deferral rule in _Reglas de conversacion_.
- [ ] `el-templo-bot/test/fixtures/pb1-e1a-lead-rendered.snap.txt` regenerated and committed in the same commit as source changes.
- [ ] Rendered PB1.E1A lead char count < 18,916 with ≥ 1,000 chars banked.
- [ ] All 537 bot tests pass. `tsc --noEmit` clean.
- [ ] No plan prices in snapshot; trial $20,000 preserved.
- [ ] User approved all three draft wordings (ELEVATOR_TEXT, BP canonical, framing rule) via Task 2 checkpoint.
- [ ] Phase 92 assertions (to be authored later) will pass against this source state — observable truths in must_haves are true.
      </success_criteria>

<output>
After completion, create `.planning/phases/89-knowledge-fixes/89-01-SUMMARY.md` with:
- Final approved wordings for ELEVATOR_TEXT, BP canonical, and framing rule
- Measurements table (pre/post rendered lead char count, KGATE-05 headroom, banked chars)
- Commit hash(es) — should be a single atomic commit per v5.3.1 update discipline
- Observable truths status (each must_have truth marked verified / not-yet-verified)
- Notes for Phase 90: banked headroom available for stage-heuristic inline comments / completionCriteria changes
- Notes for Phase 92: the four KFIX assertions and snapshot byte-equal lock are the authoring targets
</output>
