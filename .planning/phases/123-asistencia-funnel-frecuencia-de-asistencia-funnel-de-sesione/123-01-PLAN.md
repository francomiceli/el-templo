---
phase: 123-asistencia-funnel-frecuencia-de-asistencia-funnel-de-sesione
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/analytics/frequency-service.ts
  - el-templo-api/src/modules/analytics/types.ts
  - el-templo-api/src/modules/analytics/schemas.ts
  - el-templo-api/src/modules/analytics/routes.ts
  - el-templo-api/test/analytics/frequency.test.ts
autonomous: true
requirements: [FREQ-01, FREQ-02, FREQ-03, FREQ-04]
must_haves:
  truths:
    - "GET /api/admin/analytics/frequency returns 200 for admin, 403 for gestión"
    - "The endpoint returns visits/week per member over the rolling last 4 weeks, normalized for members with <4 weeks of membership"
    - "The endpoint returns a band distribution (Inactivo/Bajo/Medio/Alto) counting members per band, INCLUDING active members with 0 visits as Inactivo"
    - "The endpoint returns a cooling-down list of members who dropped ≥1 band between the current and prior 4-week windows, with the % variation alongside"
    - "The endpoint returns the per-branch check-in adoption ratio alongside, reusing AttendanceMetricsService.checkInAdoptionByBranch (not reimplemented)"
    - "FrequencyService exposes a reusable per-member golden-case signal (active member with 0 visits in window, or cooling down) for the segmentation batch"
  artifacts:
    - path: "el-templo-api/src/modules/analytics/frequency-service.ts"
      provides: "FrequencyService.getFrequency + golden-case helper"
      contains: "class FrequencyService"
    - path: "el-templo-api/test/analytics/frequency.test.ts"
      provides: "Real-MySQL integration coverage of FREQ-01..04"
      contains: "FrequencyService"
  key_links:
    - from: "frequency-service.ts"
      to: "AttendanceMetricsService.checkInAdoptionByBranch"
      via: "instantiation + delegation"
      pattern: "checkInAdoptionByBranch"
    - from: "routes.ts"
      to: "frequencyService.getFrequency"
      via: "GET /frequency under requireAdminAnalytics"
      pattern: "/frequency"
---

<objective>
Build the attendance-frequency metric (Block 4, FREQ-01..04): a new `FrequencyService` in `el-templo-api/src/modules/analytics/` that computes per-member visits/week over the rolling last 4 weeks (normalized for <4-week members), classifies members into bands (Inactivo/Bajo/Medio/Alto), produces the band distribution INCLUDING active members with 0 visits, builds the "cooling-down" list (members who dropped ≥1 band vs the prior 4 weeks) with the % variation, and surfaces the per-branch check-in adoption ratio as a validity gate by REUSING `AttendanceMetricsService.checkInAdoptionByBranch`. It also exposes a reusable per-member golden-case signal consumed by Plan 03 (segmentation batch).

Purpose: The cooling-down list is the actionable proactive churn alarm — the headline output gestión uses to contact members before losing them. Backend-only; admin UI is a later frontend phase.

Output: `GET /api/admin/analytics/frequency` endpoint + `FrequencyService` + wire types + response schema + real-MySQL integration test (CI-only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-asistencia-funnel-frecuencia-de-asistencia-funnel-de-sesione/123-CONTEXT.md
@.planning/phases/123-asistencia-funnel-frecuencia-de-asistencia-funnel-de-sesione/123-PATTERNS.md

<interfaces>
<!-- Foundation helpers (Phase 120). Use directly — do NOT reinvent. -->

From el-templo-api/src/modules/analytics/scope.ts:
applyScope({ branchId, country, branchColumn: AnyColumn }): { conditions: SQL[]; needsBranchJoin: boolean }
// needsBranchJoin === true ONLY when a country filter is active. When true the
// caller MUST conditionally innerJoin(branches) via .$dynamic() — else the
// branches.country condition references a table not in FROM → 500 (121/122 lesson).

From el-templo-api/src/modules/analytics/metric-shape.ts:
metricShape(nominal: number, total: number): { nominal, percentage, n } // div-by-zero → percentage 0, never NaN
median(values: number[]): number | null // [] → null, never NaN

From el-templo-api/src/modules/analytics/breakdowns.ts:
type BreakdownAxis = "branch" | "country" | "duration" | "plan"
breakdownKeyExpr(axis, cols): SQL[] // groupBy keys; NEVER access filters
breakdownSegmentKey(axis, row): string // U+2016 composite for plan, "excluded" for one-off duration
durationTierFromDays(durationDays: number | null): "monthly" | "long_term" | null

From el-templo-api/src/modules/analytics/attendance-metrics-service.ts (REUSE — D-123-06):
class AttendanceMetricsService { constructor(db, log) }
checkInAdoptionByBranch(filters: AnalyticsFilters): Promise<CheckInAdoptionRow[]>
// CheckInAdoptionRow = { branchId, branchName, confirmados, conCheckin, ratio (0..1) }

From el-templo-api/src/modules/analytics/types.ts:
interface AnalyticsFilters { branchId?, country?: "AR"|"ES", dateFrom?, dateTo?, planId?, entryOrigin?, window? }
// window? ALREADY exists — do NOT re-add. Frequency does NOT use window.

DB schema (el-templo-api/src/db/schema):
attendance: { memberId: int, branchId: int, scheduleId: int, sessionDate: date, checkedInAt: timestamp, source }
users: { id, branchId, country: varchar(2)|null, status: enum(freemium|prueba|activo|inactivo)|null, createdAt: timestamp }
subscriptions: { id, userId, planId, branchId, status: enum(active|paused|cancelled|expired|completed|changed|scheduled), startDate: date, endDate: date|null, pricePaid: int, priceTypeApplied: enum(regular|zero|credit_card), createdAt }
branches: { id, name, code, country: varchar(2) "AR", timezone }
subscriptionPlans: { id, name, country, durationDays, classesPerWeek }

Half-open timestamp window pattern (from attendance-metrics-service.ts:44-47):
isoSecond(d: Date): "YYYY-MM-DD HH:MM:SS" // for attendance.checkedInAt comparisons; do NOT wrap column in DATE()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FrequencyService — visits/week, bands, distribution, cooling-down, check-in adoption, golden-case helper</name>
  <read_first>
    - el-templo-api/src/modules/analytics/attendance-metrics-service.ts (FULL — copy the DI ctor, the isoSecond half-open window, the applyScope flavor-B conditional innerJoin shape; REUSE checkInAdoptionByBranch by instantiating it)
    - el-templo-api/src/modules/analytics/churn-service.ts (the per-person JS-fold + per-axis Promise.all + Map accumulator + deterministic .sort() — structural analog for the band distribution + breakdowns)
    - el-templo-api/src/modules/analytics/scope.ts (applyScope contract + needsBranchJoin)
    - el-templo-api/src/modules/analytics/metric-shape.ts (metricShape / median)
    - el-templo-api/src/modules/analytics/breakdowns.ts (axes + breakdownSegmentKey + durationTierFromDays)
    - el-templo-api/src/modules/analytics/duration-tier.ts:30-50 (named-constant exported style to mirror for FREQ_BANDS cutoffs)
    - el-templo-api/src/modules/segmentation/service.ts:180-196 (the active/paused subscription lookup shape — for "active member" detection)
  </read_first>
  <behavior>
    - visitsPerWeek per member = COUNT(attendance) in [now-28d, now) ÷ min(weeksOfMembership, 4). A member with 2 visits in their only week of membership is Alto (3+? no — 2/1=2 → Medio), NOT falsely Bajo (D-123-03 normalization).
    - Band classification: 0 visits in window → Inactivo; >0 and visitsPerWeek < BAJO_MAX → Bajo; < MEDIO_MAX → Medio; else Alto (D-123-04).
    - Distribution counts members per band; an ACTIVE member (has an active/paused subscription) with 0 visits is counted as Inactivo (D-123-04). Each band count returned via metricShape over the active-population total.
    - Cooling-down: a member whose current-window band is strictly lower than their prior-window band ([now-56d, now-28d)) is "enfriándose", with pctVariacion = guarded (current vs prior visitsPerWeek). Trigger is the band drop, NOT the raw % (D-123-05). pctVariacion may be null when prior is 0 → declare null-safe.
    - Check-in adoption: the response includes the AttendanceMetricsService.checkInAdoptionByBranch(filters) rows verbatim (D-123-06).
    - Golden-case helper returns the set of user IDs that are active (paying — has active/paused subscription) AND (0 visits in window OR cooling down), for the segmentation batch (Plan 03 consumes it).
    - Breakdowns by branch/country/duration/plan group the band counts per segment (D-123-14).
  </behavior>
  <action>
    Create `el-templo-api/src/modules/analytics/frequency-service.ts`. Export `class FrequencyService` with the constructor DI pattern `constructor(private db: MySql2Database<typeof schema>, private log: FastifyBaseLogger)`. Imports mirror churn-service.ts (and/eq/sql/SQL from drizzle-orm, schema, applyScope, metricShape/median, breakdown helpers, the new types from ./types).

    Export named band-cutoff constants mirroring duration-tier.ts style (exported `const`, NOT env vars, per D-123-04): `BAJO_MAX_VISITS_PER_WEEK` and `MEDIO_MAX_VISITS_PER_WEEK` (reasonable defaults: Bajo = visitsPerWeek > 0 && < 1.5; Medio = >= 1.5 && < 2.5; Alto = >= 2.5 — document the ~1/~2/3+ spec mapping in a JSDoc). Export a `FREQ_BANDS` literal union type `"inactivo" | "bajo" | "medio" | "alto"` and a pure `classifyBand(visitsPerWeek: number): FrequencyBand` function applying the cutoffs (0 visits → "inactivo").

    Implement `async getFrequency(filters: AnalyticsFilters): Promise<FrequencyAnalytics>`:
    1. Compute the active member population: members with an active/paused subscription (mirror segmentation/service.ts:180-196 join `subscriptions` inarray status ['active','paused']). Scope these via `applyScope({ branchId, country, branchColumn: schema.subscriptions.branchId })`. This is the denominator population for the distribution (so Inactivo = active member, 0 visits).
    2. Per-member visit counts for the CURRENT window [now-28d, now) and the PRIOR window [now-56d, now-28d), grouped by `attendance.memberId`. Use the isoSecond half-open pattern on `attendance.checkedInAt` (copy attendance-metrics-service.ts:44-47, 77-83), scoped via `applyScope({ branchColumn: schema.attendance.branchId })` with the **conditional `.$dynamic()` innerJoin(branches) when needsBranchJoin** (121/122 lesson — else 500). Build Maps userId→count for each window.
    3. weeksOfMembership per member = clamp(`(now − users.createdAt) / 7days`, max 4, min ~1 to avoid div-by-zero); visitsPerWeek = currentCount ÷ min(weeksOfMembership, 4) (D-123-03). For prior-window band, use the same normalization against the prior window.
    4. classifyBand each member for current and prior windows; build the distribution (count per band via metricShape over the active population) folding active-with-0-visits into Inactivo.
    5. Cooling-down list = members whose current band rank < prior band rank (define a numeric rank inactivo<bajo<medio<alto), each row `{ userId, currentBand, priorBand, pctVariacion }` where pctVariacion = prior>0 ? round(((current−prior)/prior)*100) : null.
    6. checkInAdoption = await new AttendanceMetricsService(this.db, this.log).checkInAdoptionByBranch(filters) — REUSE, do NOT reimplement (D-123-06).
    7. breakdowns by branch/country/duration/plan: mirror churn-service.ts:348-429 (CHURN_AXES const + per-axis Promise.all + Map accumulator + deterministic .sort()), grouping the band counts per segment; one-off duration plans (tier null) dropped, never use the literal `"excluded"` as a surfaced segment.
    8. Return `FrequencyAnalytics` (typed in Task 2) with every count via metricShape.

    Also export `async coolingOrInactiveUserIds(windowDays: number): Promise<Set<number>>` (or accept it as part of getFrequency's intermediate computation refactored into a private method the public method calls) returning the golden-case set: active members (active/paused subscription) that are Inactivo (0 visits in [now-windowDays, now)) OR cooling down. This is the single batched query Plan 03's cron consumes — do NOT duplicate the window math in the cron (D-123-01). Keep it scope-unaware (the batch is global) but reuse the same band/window logic.

    No `console.log` (use `this.log`). No `any` (use `unknown` + narrowing or typed rows). `catch (err: unknown)` if you add try/catch. Do NOT touch `attendance-metrics-service.ts` (read-only reuse).

  </action>
  <verify>
    <automated>cd el-templo-api && pnpm tsc --noEmit 2>&1 | grep -v node_modules | (! grep -iE "error TS")</automated>
  </verify>
  <acceptance_criteria>
    - `el-templo-api/src/modules/analytics/frequency-service.ts` exists and exports `class FrequencyService`, `classifyBand`, `BAJO_MAX_VISITS_PER_WEEK`, `MEDIO_MAX_VISITS_PER_WEEK`.
    - `grep -n "checkInAdoptionByBranch" el-templo-api/src/modules/analytics/frequency-service.ts` matches (adoption is reused, not reimplemented — D-123-06).
    - `grep -n "needsBranchJoin" el-templo-api/src/modules/analytics/frequency-service.ts` matches AND a `.$dynamic()` + conditional `innerJoin` on `branches` is present for every attendance query that applies scope (121/122 lesson).
    - `grep -n "coolingOrInactiveUserIds\|isGoldenCase" el-templo-api/src/modules/analytics/frequency-service.ts` matches (the golden-case helper Plan 03 consumes).
    - `grep -cE "process\.env|console\.(log|warn|error)" el-templo-api/src/modules/analytics/frequency-service.ts` returns 0 (band/turno cutoffs are named constants, logging via this.log — D-123-04).
    - `cd el-templo-api && pnpm tsc --noEmit` reports no error in frequency-service.ts.
  </acceptance_criteria>
  <done>FrequencyService computes visits/week (normalized), bands, distribution (incl. active-0-visits), cooling-down with % variation, reuses checkInAdoptionByBranch, exposes the golden-case helper, and type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire types + response schema + GET /frequency route</name>
  <read_first>
    - el-templo-api/src/modules/analytics/types.ts:517-598 (ChurnAnalytics/ChurnRenewalAxis family to mirror; AnalyticsFilters already at :783-811 — do NOT re-add window)
    - el-templo-api/src/modules/analytics/schemas.ts:580-663 (churnQuerystring, churnMetricShapeSchema, churnSchema, errorSchema — mirror these; frequency has NO window so reuse analyticsQuerystring)
    - el-templo-api/src/modules/analytics/routes.ts:70-158 (instantiation site near ltsService; the KPI block; the preHandler guard pattern requireAdminAnalytics + requireBranchAccess)
  </read_first>
  <action>
    Edit `el-templo-api/src/modules/analytics/types.ts`: add `FrequencyBand` re-export (or import from frequency-service), `FrequencyDistributionRow`, `FrequencyCoolingRow { userId, currentBand, priorBand, pctVariacion: number | null }`, `FrequencyBreakdownAxis = "branch" | "country" | "duration" | "plan"`, `FrequencyBreakdownRow`, and the top-level `FrequencyAnalytics { distribution: FrequencyDistributionRow[]; coolingDown: FrequencyCoolingRow[]; checkInAdoption: CheckInAdoptionRow[]; breakdowns: FrequencyBreakdownRow[] }`. Reuse `MetricShape` for counts. Do NOT re-declare `AnalyticsFilters.window`.

    Edit `el-templo-api/src/modules/analytics/schemas.ts`: add a reusable `metricShapeSchema` const if not already shared (else copy churnMetricShapeSchema), and `frequencySchema` with `querystring: analyticsQuerystring` (no window param — frequency does not use it), and a full `response.200` declaring EVERY wire field (distribution array with band + MetricShape; coolingDown array with userId/currentBand/priorBand and pctVariacion as `["number","null"]`; checkInAdoption array with branchId/branchName/confirmados/conCheckin/ratio; breakdowns array with axis enum/key/band MetricShapes) plus 400/401/403/500 via `errorSchema`. Every field MUST be declared or fast-json-stringify strips it.

    Edit `el-templo-api/src/modules/analytics/routes.ts`: import `FrequencyService`; instantiate `const frequencyService = new FrequencyService(fastify.db, fastify.log);` next to `ltvService` (routes.ts:82). Register `fastify.get<{ Querystring: { branchId?: number; dateFrom?: string; dateTo?: string } }>("/frequency", { schema: frequencySchema, preHandler: [requireAdminAnalytics, requireBranchAccess({ from: "query.branchId", optional: true })] }, ...)`. Build `AnalyticsFilters` from query + `request.scope.country ?? undefined` (no window). Return `await frequencyService.getFrequency(filters)`. Use the single shared `handleServiceError(err, reply, request.log, "get frequency")` in `catch (err: unknown)`. Do NOT touch the existing `/funnel`, `/churn`, `/renewal`, `/ltv`, `/attendance/*` endpoints.

  </action>
  <verify>
    <automated>cd el-templo-api && pnpm tsc --noEmit 2>&1 | grep -v node_modules | (! grep -iE "error TS")</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "FrequencyAnalytics" el-templo-api/src/modules/analytics/types.ts` matches; `grep -c "window" el-templo-api/src/modules/analytics/types.ts` does NOT increase for frequency (no new window field).
    - `grep -n "frequencySchema" el-templo-api/src/modules/analytics/schemas.ts` matches AND the 200 response declares `distribution`, `coolingDown`, `checkInAdoption`, `breakdowns`, and `pctVariacion` typed `["number","null"]`.
    - `grep -n '"/frequency"' el-templo-api/src/modules/analytics/routes.ts` matches with `requireAdminAnalytics` in its preHandler array (gestión → 403, D-123-14).
    - `grep -c "trial-funnel\|funnel-service" el-templo-api/src/modules/analytics/routes.ts` shows the existing `/funnel` block was NOT modified (no diff to its handler).
    - `cd el-templo-api && pnpm tsc --noEmit` reports no errors.
  </acceptance_criteria>
  <done>Types, response schema (every field declared), and the guarded `GET /frequency` route exist and type-check; existing analytics endpoints untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Integration test — frequency.test.ts (real MySQL, CI-only)</name>
  <read_first>
    - el-templo-api/test/analytics/funnel.test.ts:1-130 (createTestApp/getAuthToken/registerUser/cleanAllTestData, ES-branch seeding, controlled created_at, plan seeding with priceRegular/priceZero/durationDays/classesPerWeek notNull)
    - el-templo-api/test/helpers.ts (auth + request utilities, passwordHash convention)
    - el-templo-api/test/analytics/churn.test.ts (CURDATE-derived offsets for TZ-flake safety; gestión 403 / admin 200 assertions)
    - el-templo-api/src/db/schema/attendance.ts (seed columns memberId/branchId/scheduleId/sessionDate/checkedInAt)
  </read_first>
  <action>
    Create `el-templo-api/test/analytics/frequency.test.ts` mirroring funnel.test.ts setup. Seed an AR `TEST` branch and an ES `TESTES` branch, a plan with `priceRegular`/`priceZero`/`durationDays: 30`/`classesPerWeek: 3` notNull, members via `registerUser` (or raw insert with `passwordHash: "x"` — NEVER `password`), subscriptions with `priceTypeApplied: "regular"`, and `attendance` rows with controlled `checkedInAt`/`sessionDate`/`branchId`/`memberId`/`scheduleId`. Derive EVERY date from `CURDATE()`/now offsets (TZ-flake-safe — analytics tests fail after ~21:00 AR otherwise).

    Cover: (a) band classification incl. an ACTIVE member with 0 visits → Inactivo (D-123-04); (b) <4-week normalization — a member registered 1 week ago with 2 visits is NOT Bajo (D-123-03); (c) cooling-down detection on a band drop between current and prior 4-week windows + the pctVariacion field (D-123-05); (d) check-in adoption ratio surfaced alongside via reuse (assert the array appears with a ratio — D-123-06); (e) breakdowns by branch/country; (f) gestión token → 403, admin token → 200 (D-123-14). Call `GET /api/admin/analytics/frequency`. Tests run in CI only — do NOT run locally (project policy).

  </action>
  <verify>
    <automated>cd el-templo-api && pnpm tsc --noEmit 2>&1 | grep -v node_modules | (! grep -iE "error TS")</automated>
  </verify>
  <acceptance_criteria>
    - `el-templo-api/test/analytics/frequency.test.ts` exists and imports `FrequencyService` + `createTestApp`/`getAuthToken`/`cleanAllTestData`.
    - `grep -nE "Inactivo|inactivo" el-templo-api/test/analytics/frequency.test.ts` matches (active-with-0-visits case covered).
    - `grep -n "403" el-templo-api/test/analytics/frequency.test.ts` AND `grep -n "200" el-templo-api/test/analytics/frequency.test.ts` both match (gestión/admin authz).
    - `grep -cE "\bpassword:\s" el-templo-api/test/analytics/frequency.test.ts` returns 0 (uses passwordHash / registerUser, never raw `password` — CI lesson).
    - `cd el-templo-api && pnpm tsc --noEmit` reports no errors in the test file.
  </acceptance_criteria>
  <done>frequency.test.ts covers FREQ-01..04 incl. normalization, cooling-down, adoption reuse, breakdowns, and gestión 403 / admin 200; type-checks clean (CI runs the suite).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                | Description                                                                   |
| ----------------------- | ----------------------------------------------------------------------------- |
| client → GET /frequency | authenticated admin/gestión request; branchId/dates are untrusted query input |
| service → MySQL         | scope-filtered analytics reads over attendance/subscriptions/branches         |

## STRIDE Threat Register

| Threat ID | Category               | Component                                  | Disposition | Mitigation Plan                                                                                                                                                                         |
| --------- | ---------------------- | ------------------------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-123-01  | Elevation of Privilege | GET /frequency authz                       | mitigate    | `requireAdminAnalytics` (ADMIN_ROLES) on the route; gestión gets 403 (test-covered). Plugin onRequest gate already restricts to operational roles.                                      |
| T-123-02  | Information Disclosure | branch/country scope leakage               | mitigate    | Every query spreads `applyScope(...).conditions`; `request.scope.country` (not client input) drives the country filter; breakdown axes are additive groupBy keys, never access filters. |
| T-123-03  | Information Disclosure | needsBranchJoin missing → 500 / wrong rows | mitigate    | Conditional `.$dynamic()` innerJoin(branches) whenever `needsBranchJoin` (active country filter) — verified by grep gate in Task 1.                                                     |
| T-123-04  | Tampering / Injection  | branchId/dateFrom/dateTo query params      | mitigate    | `frequencySchema.querystring = analyticsQuerystring` validates types (integer/date) before the service; dates are bound parameters in rangeConditions; no raw string interpolation.     |
| T-123-SC  | Tampering              | npm/pip/cargo installs                     | accept      | No new dependencies added in this plan (reuses existing analytics module + drizzle).                                                                                                    |

</threat_model>

<verification>
- `cd el-templo-api && pnpm tsc --noEmit` clean (local typecheck only — CI runs the test suite; do not run tests locally per project policy).
- Grep gates in each task's acceptance_criteria pass (needsBranchJoin conditional join, checkInAdoptionByBranch reuse, golden-case helper, no env vars, gestión 403, no raw `password`).
- The `/frequency` 200 response declares every wire field (fast-json-stringify discipline).
</verification>

<success_criteria>

1. `GET /api/admin/analytics/frequency` returns 200 for admin, 403 for gestión.
2. Response delivers visits/week per member over rolling 4 weeks (normalized for <4-week members) and the band distribution incl. active-with-0-visits as Inactivo.
3. Response delivers the cooling-down list (≥1 band drop) with % variation and the per-branch check-in adoption ratio alongside (reused, not reimplemented).
4. FrequencyService exposes the golden-case helper Plan 03 consumes.
5. Type-checks clean; integration test covers FREQ-01..04 + authz (CI-only).
   </success_criteria>

<output>
Create `.planning/phases/123-asistencia-funnel-frecuencia-de-asistencia-funnel-de-sesione/123-01-SUMMARY.md` when done.
</output>
