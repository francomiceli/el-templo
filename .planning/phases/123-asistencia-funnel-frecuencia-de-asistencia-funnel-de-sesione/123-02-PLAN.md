---
phase: 123-asistencia-funnel-frecuencia-de-asistencia-funnel-de-sesione
plan: 02
type: execute
wave: 2
depends_on: ["123-01"]
files_modified:
  - el-templo-api/src/modules/analytics/trial-funnel-service.ts
  - el-templo-api/src/modules/analytics/types.ts
  - el-templo-api/src/modules/analytics/schemas.ts
  - el-templo-api/src/modules/analytics/routes.ts
  - el-templo-api/test/analytics/trial-funnel.test.ts
autonomous: true
requirements: [FUNNEL-01, FUNNEL-02, FUNNEL-03, FUNNEL-04, FUNNEL-05]
must_haves:
  truths:
    - "GET /api/admin/analytics/trial-funnel returns 200 for admin, 403 for gestión"
    - "The endpoint returns the cascade reservó→asistió→compró with tasa_show, tasa_cierre (over asistentes), and punta_a_punta, each as a MetricShape with div-by-zero → 0"
    - "Asistió is counted from bookings.status IN ('qr_escaneado','confirmado'), NOT the attendance table"
    - "Only new leads with no PRIOR paid subscription count; previously-paid returners are excluded"
    - "The cohort is anchored by the scheduled-session date with weekly/monthly cuts; compró is the first paid subscription within a configurable ~21d attribution window that matures on its own"
    - "Breakdowns by branch/country/turno/plan; turno is bucketed from schedules.startTime (mañana 07-10, tarde 17-20, else otro); the plan axis groups by the plan they BOUGHT"
  artifacts:
    - path: "el-templo-api/src/modules/analytics/trial-funnel-service.ts"
      provides: "TrialFunnelService.getTrialFunnel: reserva→asistencia→compra cascade"
      contains: "class TrialFunnelService"
    - path: "el-templo-api/test/analytics/trial-funnel.test.ts"
      provides: "Real-MySQL integration coverage of FUNNEL-01..05"
      contains: "TrialFunnelService"
  key_links:
    - from: "trial-funnel-service.ts"
      to: "bookings.status"
      via: "asistió = qr_escaneado/confirmado"
      pattern: "qr_escaneado"
    - from: "routes.ts"
      to: "trialFunnelService.getTrialFunnel"
      via: "GET /trial-funnel under requireAdminAnalytics"
      pattern: "/trial-funnel"
---

<objective>
Build the trial-session funnel (Block 3, FUNNEL-01..05): a NEW `TrialFunnelService` in `el-templo-api/src/modules/analytics/` computing the cascade reservó→asistió→compró with the three counts and the rates `tasa_show = asistieron ÷ reservaron`, `tasa_cierre = compraron ÷ asistieron` (denominator = asistentes), `punta_a_punta = compraron ÷ reservaron`. The cohort is anchored by the scheduled-session date (weekly/monthly cuts), counts ONLY new leads (no prior paid subscription), and marks compró via the first paid subscription within a configurable ~21d attribution window that matures on its own. Breakdowns by branch/country/turno/plan (turno bucketed from `schedules.startTime`; plan = the plan they BOUGHT). This is a NEW funnel — do NOT modify the Phase-118 `funnel-service.ts`.

Purpose: A diagnostic conversion funnel so gestión sees where trial leads drop off (no-show vs no-close). Backend-only; admin UI is a later frontend phase.

Output: `GET /api/admin/analytics/trial-funnel` endpoint + `TrialFunnelService` + wire types + response schema + real-MySQL integration test (CI-only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-asistencia-funnel-frecuencia-de-asistencia-funnel-de-sesione/123-CONTEXT.md
@.planning/phases/123-asistencia-funnel-frecuencia-de-asistencia-funnel-de-sesione/123-PATTERNS.md
@.planning/phases/123-asistencia-funnel-frecuencia-de-asistencia-funnel-de-sesione/123-01-PLAN.md

<interfaces>
<!-- Foundation helpers (Phase 120) + the structural analog. Use directly. -->

From el-templo-api/src/modules/analytics/scope.ts:
applyScope({ branchId, country, branchColumn }): { conditions: SQL[]; needsBranchJoin: boolean }
// needsBranchJoin true ONLY when country filter active → caller MUST conditionally innerJoin(branches) via .$dynamic() (121/122 lesson).

From el-templo-api/src/modules/analytics/cohorts.ts:
rangeConditions(dateColumn, from, to): SQL[] // half-open [from, to) — EXCLUSIVE upper bound, NEVER <= to
bucketExpr(dateColumn, "weekly" | "monthly"): SQL<string> // weekly = ISO %x-W%v, monthly = %Y-%m

From el-templo-api/src/modules/analytics/metric-shape.ts:
metricShape(nominal, total): { nominal, percentage, n } // div-by-zero → 0, never NaN

From el-templo-api/src/modules/analytics/types.ts:
interface AnalyticsFilters { branchId?, country?, dateFrom?, dateTo?, planId?, entryOrigin?, window? }
// window? ALREADY exists — REUSE it as the funnel attribution window (default ~21d). Do NOT re-add it.

Structural analog — el-templo-api/src/modules/analytics/funnel-service.ts:
// The stage-cascade shape: read cohort, build per-person stage flags in JS, fold per cohort,
// guard div-by-zero → metricShape/0, deterministic .sort(). DO NOT MODIFY THIS FILE (Phase 118, different funnel).

Scope discipline analog — el-templo-api/src/modules/analytics/churn-service.ts:176-196, 348-429:
// The .$dynamic() conditional innerJoin + the per-axis breakdown loop (Map accumulator + sort).

DB schema (el-templo-api/src/db/schema):
bookings: { id, memberId, scheduleId, bookingDate: date, status: enum(reservado|qr_escaneado|confirmado|cancelado|lista_espera|no_show), isTrial: boolean }
// NO branch_id — sede comes from schedules.branchId (join bookings→schedules→branches, like checkInAdoptionByBranch).
// bookingDate IS the scheduled-session date (the cohort anchor, D-123-10).
schedules: { id, branchId, startTime: varchar(5) "HH:MM", dayOfWeek }
subscriptions: { id, userId, planId, branchId, status, startDate: date, endDate, pricePaid: int, priceTypeApplied: enum(regular|zero|credit_card), createdAt }
// "paid sub" = pricePaid > 0 (a zero-price freemium/promo grant is NOT a purchase). Confirm against priceTypeApplied != 'zero'.
users: { id, branchId, country, status, convertedAt: timestamp|null } // convertedAt set on first sub if the user had an isTrial booking
subscriptionPlans: { id, name, country, durationDays }
branches: { id, name, code, country, timezone }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TrialFunnelService — cascade, rates, new-lead exclusion, attribution window, maturity, turno + plan-bought breakdowns</name>
  <read_first>
    - el-templo-api/src/modules/analytics/funnel-service.ts (FULL — copy the DI ctor, the JS-fold + median + 0-on-empty discipline; this is the STRUCTURAL analog. Do NOT modify it.)
    - el-templo-api/src/modules/analytics/churn-service.ts:176-196 (the .$dynamic() conditional innerJoin shape) and :348-429 (the per-axis breakdown loop) and :389-396 (the EXPLICIT `sql\`${schema.branches.id} = ${schema.subscriptions.branchId}\`` qualification — the pattern for correlated-subquery column refs)
    - el-templo-api/src/modules/analytics/attendance-metrics-service.ts:132-201 (the bookings→schedules→branches join + the bookings.status filter — the canonical sede-from-schedule join)
    - el-templo-api/src/modules/analytics/cohorts.ts (rangeConditions half-open + bucketExpr)
    - el-templo-api/src/modules/analytics/scope.ts + metric-shape.ts
    - el-templo-api/src/db/schema/bookings.ts, schedules.ts, subscriptions.ts, users.ts (the enums + columns above)
  </read_first>
  <behavior>
    - Reservó (denominator, D-123-08): every bookings.isTrial=1 row of a NEW LEAD in the cohort, regardless of final status (cancelado/no_show still count).
    - Asistió (D-123-07 — CRITICAL): bookings.status IN ('qr_escaneado','confirmado') ONLY — NOT the attendance table. no_show/cancelado/lista_espera/reservado do NOT count.
    - Compró (D-123-09): the lead's FIRST paid subscription (pricePaid > 0) within the attribution window from the session date. The plan axis groups by the plan they BOUGHT (subscriptions.planId → subscriptionPlans.name/country), NOT the trial plan.
    - New lead (D-123-10): only leads with NO PRIOR paid subscription before the session date. A previously-paid returner = reactivation, EXCLUDED.
    - Rates (D-123-11): tasa_show = asistieron ÷ reservaron; tasa_cierre = compraron ÷ asistieron (denominator = asistentes); punta_a_punta = compraron ÷ reservaron — each metricShape, div-by-zero → 0 never NaN.
    - Cohort anchored by bookings.bookingDate (the scheduled-session date), half-open windowed, bucketed weekly/monthly (D-123-04/10).
    - Maturity (D-123-12): a cohort period whose attribution window has not fully elapsed (session date + window > today) is marked provisional (mirror churn series provisional flag).
    - Turno (D-123-13): bucket by schedules.startTime hour — mañana 07:00-09:59, tarde 17:00-19:59, else "otro" (named constants, NOT env vars).
  </behavior>
  <action>
    Create `el-templo-api/src/modules/analytics/trial-funnel-service.ts`. Export `class TrialFunnelService` with `constructor(private db: MySql2Database<typeof schema>, private log: FastifyBaseLogger)`. Imports mirror funnel-service.ts + cohorts helpers + the new types from ./types.

    Export named turno-cutoff constants (D-123-13, NOT env vars): `TURNO_MANANA_START_HOUR = 7`, `TURNO_MANANA_END_HOUR = 10`, `TURNO_TARDE_START_HOUR = 17`, `TURNO_TARDE_END_HOUR = 20`, and a pure `classifyTurno(startTime: string): "manana" | "tarde" | "otro"` parsing the `HH` from the `"HH:MM"` string (startTime is already wall-clock in the sede's TZ — schedules are sede-local — so no cross-TZ conversion is needed; parse the leading hour). Also export the default attribution window constant `TRIAL_ATTRIBUTION_WINDOW_DEFAULT_DAYS = 21` (D-123-12).

    Implement `async getTrialFunnel(filters: AnalyticsFilters): Promise<TrialFunnelAnalytics>`:
    1. attributionWindowDays = filters.window ?? TRIAL_ATTRIBUTION_WINDOW_DEFAULT_DAYS (REUSE AnalyticsFilters.window — D-123-12).
    2. Read the trial-booking cohort: `bookings.isTrial = 1` joined bookings→schedules→branches (sede from schedules.branchId — D-123-05 in PATTERNS). Scope via `applyScope({ branchId, country, branchColumn: schema.schedules.branchId })` with the **conditional `.$dynamic()` innerJoin(branches) when needsBranchJoin** (121/122 lesson — else 500). Window the cohort half-open on `bookings.bookingDate` via `rangeConditions(schema.bookings.bookingDate, filters.dateFrom, filters.dateTo)`. Select per-booking: memberId, bookingDate, status, schedules.startTime, schedules.branchId, branches.name, branches.country.
    3. New-lead exclusion (D-123-10): exclude any booking whose member has a PRIOR paid subscription (pricePaid > 0) with startDate/createdAt before the booking's session date. Implement via a correlated NOT EXISTS subquery over `subscriptions`; **qualify the outer-table column refs with the literal `schema.bookings.memberId` / `schema.bookings.bookingDate` prefix** (121/122 lesson — Drizzle renders unqualified correlated refs against the inner table, silently binding wrong). Confirm "paid" = `subscriptions.pricePaid > 0` (a zero/freemium grant is not a prior paid sub).
    4. Compró (D-123-09): for each surviving (new-lead) booking, determine the member's FIRST paid subscription with startDate (or createdAt — pick the purchase-date column; document the choice) in [sessionDate, sessionDate + attributionWindowDays). Implement via a correlated scalar/EXISTS subquery, again with explicit `schema.bookings.*` qualification. Capture the bought planId for the plan axis.
    5. Build per-booking stage flags in JS (reservó always true; asistió = status IN ('qr_escaneado','confirmado'); compró = first-paid-sub-in-window true), fold into the official cascade counts + per-axis Map accumulators (mirror funnel-service.ts fold + churn-service.ts:348-429 loop).
    6. Rates via metricShape: tasa_show = metricShape(asistieron, reservaron); tasa_cierre = metricShape(compraron, asistieron); punta_a_punta = metricShape(compraron, reservaron) — denominators exactly per D-123-11.
    7. Weekly/monthly series via `bucketExpr(schema.bookings.bookingDate, bucket)`; mark a bucket provisional when any of its bookings' (sessionDate + attributionWindowDays) is > today (D-123-12).
    8. Breakdowns by branch / country / turno / plan-bought: branch/country reuse the breakdown axes; turno is a funnel-LOCAL axis (classifyTurno over startTime — do NOT add turno to the shared breakdowns.ts); plan groups by the BOUGHT plan (planId → name/country composite). Each segment yields the three cascade counts → three metricShapes. Deterministic .sort().
    9. Return `TrialFunnelAnalytics` (typed in Task 2).

    No `console.log` (use `this.log`). No `any`. `catch (err: unknown)` if you add try/catch. Do NOT modify `funnel-service.ts` (Phase 118 — different funnel, hidden in UI).

  </action>
  <verify>
    <automated>cd el-templo-api && pnpm tsc --noEmit 2>&1 | grep -v node_modules | (! grep -iE "error TS")</automated>
  </verify>
  <acceptance_criteria>
    - `el-templo-api/src/modules/analytics/trial-funnel-service.ts` exists and exports `class TrialFunnelService`, `classifyTurno`, `TURNO_MANANA_START_HOUR`, `TRIAL_ATTRIBUTION_WINDOW_DEFAULT_DAYS`.
    - `grep -nE "qr_escaneado.*confirmado|confirmado.*qr_escaneado" el-templo-api/src/modules/analytics/trial-funnel-service.ts` matches AND `grep -c "schema.attendance" el-templo-api/src/modules/analytics/trial-funnel-service.ts` returns 0 (asistió from bookings.status, NOT attendance — D-123-07).
    - `grep -n "needsBranchJoin" el-templo-api/src/modules/analytics/trial-funnel-service.ts` matches AND a `.$dynamic()` conditional `innerJoin` on `branches` is present (121/122 lesson).
    - `grep -n "schema.bookings.memberId" el-templo-api/src/modules/analytics/trial-funnel-service.ts` matches inside the correlated subqueries (explicit outer-table qualification — 121/122 lesson).
    - `grep -n "rangeConditions\|bucketExpr" el-templo-api/src/modules/analytics/trial-funnel-service.ts` matches (cohort anchored on bookingDate, half-open).
    - `grep -cE "process\.env|console\.(log|warn|error)" el-templo-api/src/modules/analytics/trial-funnel-service.ts` returns 0 (turno/window cutoffs are named constants — D-123-13).
    - `grep -c "funnel-service" el-templo-api/src/modules/analytics/trial-funnel-service.ts` returns 0 (does NOT import/extend the Phase-118 funnel).
    - `cd el-templo-api && pnpm tsc --noEmit` reports no error in trial-funnel-service.ts.
  </acceptance_criteria>
  <done>TrialFunnelService computes the cascade with the three correct-denominator rates, asistió from bookings.status, new-lead exclusion + first-paid-sub-in-window via explicitly-qualified correlated subqueries, conditional branch join, weekly/monthly provisional series, and branch/country/turno/plan-bought breakdowns; type-checks clean; funnel-service.ts untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Wire types + response schema (window param) + GET /trial-funnel route</name>
  <read_first>
    - el-templo-api/src/modules/analytics/types.ts:330-337 (FunnelAnalytics/FunnelCohort) + :783-811 (AnalyticsFilters.window already present — REUSE, extend JSDoc to note funnel attribution usage)
    - el-templo-api/src/modules/analytics/schemas.ts:580-663 (churnQuerystring with window 1..365 bounded + churnSchema response declaration — mirror; the funnel DOES use window)
    - el-templo-api/src/modules/analytics/routes.ts:80-82 (instantiation) + the /churn block at :405-436 (window param in Querystring generic + schema)
    - .planning/phases/123-asistencia-funnel-frecuencia-de-asistencia-funnel-de-sesione/123-PATTERNS.md:138-173 (the route shape to copy verbatim)
  </read_first>
  <action>
    Edit `el-templo-api/src/modules/analytics/types.ts`: add `TrialFunnelAxis = "branch" | "country" | "turno" | "plan"` (NEW union — turno is funnel-only), `TrialFunnelStageCounts { reservaron, asistieron, compraron }`, `TrialFunnelRates { tasaShow: MetricShape; tasaCierre: MetricShape; puntaAPunta: MetricShape }`, `TrialFunnelSeriesRow { bucket: string; rates: TrialFunnelRates; counts: TrialFunnelStageCounts; provisional: boolean }`, `TrialFunnelBreakdownRow { axis: TrialFunnelAxis; key: string; rates: TrialFunnelRates; counts: TrialFunnelStageCounts }`, and `TrialFunnelAnalytics { counts: TrialFunnelStageCounts; rates: TrialFunnelRates; series: TrialFunnelSeriesRow[]; breakdowns: TrialFunnelBreakdownRow[]; attributionWindowDays: number }`. Extend the `AnalyticsFilters.window` JSDoc to document funnel attribution usage (do NOT re-declare the field).

    Edit `el-templo-api/src/modules/analytics/schemas.ts`: add a local `trialFunnelQuerystring` copying `churnQuerystring` (branchId/dateFrom/dateTo + `window: { type:"integer", minimum:1, maximum:365 }`) WITHOUT mutating the shared `analyticsQuerystring`. Add `trialFunnelSchema` with `querystring: trialFunnelQuerystring` and a full `response.200` declaring EVERY field (counts, rates with three MetricShape sub-objects, series array with bucket/counts/rates/provisional, breakdowns array with axis enum ["branch","country","turno","plan"]/key/counts/rates, attributionWindowDays) plus 400/401/403/500 via `errorSchema`. Reuse a shared `metricShapeSchema` const (copy churnMetricShapeSchema if no shared one).

    Edit `el-templo-api/src/modules/analytics/routes.ts`: import `TrialFunnelService`; instantiate `const trialFunnelService = new TrialFunnelService(fastify.db, fastify.log);` next to the other services (routes.ts:82). Register `fastify.get<{ Querystring: { branchId?: number; dateFrom?: string; dateTo?: string; window?: number } }>("/trial-funnel", { schema: trialFunnelSchema, preHandler: [requireAdminAnalytics, requireBranchAccess({ from: "query.branchId", optional: true })] }, ...)` building `AnalyticsFilters` from query + `request.scope.country ?? undefined` + window, returning `await trialFunnelService.getTrialFunnel(filters)`, with `handleServiceError(err, reply, request.log, "get trial funnel")` in `catch (err: unknown)`. Do NOT touch the existing `/funnel` endpoint or any other route.

  </action>
  <verify>
    <automated>cd el-templo-api && pnpm tsc --noEmit 2>&1 | grep -v node_modules | (! grep -iE "error TS")</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "TrialFunnelAnalytics" el-templo-api/src/modules/analytics/types.ts` matches AND `grep -n '"turno"' el-templo-api/src/modules/analytics/types.ts` matches (turno-bearing axis union).
    - `grep -n "trialFunnelSchema" el-templo-api/src/modules/analytics/schemas.ts` matches AND its querystring declares `window` bounded `minimum: 1, maximum: 365`; the 200 response declares `counts`, `rates`, `series`, `breakdowns`, `attributionWindowDays`.
    - `grep -n '"/trial-funnel"' el-templo-api/src/modules/analytics/routes.ts` matches with `requireAdminAnalytics` in its preHandler (gestión → 403).
    - `grep -c '"/funnel"' el-templo-api/src/modules/analytics/routes.ts` shows the existing `/funnel` route still registered (not removed/renamed).
    - `cd el-templo-api && pnpm tsc --noEmit` reports no errors.
  </acceptance_criteria>
  <done>Types (turno axis), response schema (window bounded + every field declared), and the guarded `GET /trial-funnel` route exist and type-check; existing `/funnel` and other endpoints untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Integration test — trial-funnel.test.ts (real MySQL, CI-only)</name>
  <read_first>
    - el-templo-api/test/analytics/funnel.test.ts:1-130 (setup, plan/branch/registerUser seeding, ES branch, controlled timestamps)
    - el-templo-api/test/analytics/churn.test.ts (CURDATE-derived dates, provisional/window assertions, gestión 403 / admin 200)
    - el-templo-api/test/helpers.ts (passwordHash convention; cleanAllTestData FK ordering for bookings/schedules/subscriptions)
    - el-templo-api/src/db/schema/bookings.ts, schedules.ts (seed columns: bookings isTrial/status/bookingDate/scheduleId/memberId; schedules startTime/branchId/dayOfWeek/activityId)
  </read_first>
  <action>
    Create `el-templo-api/test/analytics/trial-funnel.test.ts` mirroring funnel.test.ts/churn.test.ts setup. Seed AR `TEST` + ES `TESTES` branches; a schedule with a `startTime` in the mañana band (e.g. "08:00") and another in the tarde band (e.g. "18:00") and one in "otro" (e.g. "13:00"); members via `registerUser` / raw insert with `passwordHash: "x"` (NEVER `password`); plans with `priceRegular`/`priceZero`/`durationDays` notNull; subscriptions with `priceTypeApplied: "regular"` and `pricePaid > 0` for purchases. Seed `bookings` with `isTrial: true`, the `status` enum values, controlled `bookingDate`/`scheduleId`. Derive EVERY date from `CURDATE()`/now offsets (TZ-flake-safe).

    Cover: (a) reservó counts cancelado/no_show trials (D-123-08); (b) asistió = bookings.status IN ('qr_escaneado','confirmado') only — seed a trial WITHOUT any attendance row and confirm it still counts via status (D-123-07); (c) compró = first paid sub within the window — seed one purchase INSIDE and one OUTSIDE the ~21d window, assert only the in-window one counts (D-123-09/12); (d) new-lead exclusion — a member with a PRIOR paid subscription does NOT count (D-123-10); (e) the three rates with correct denominators (tasa_cierre over asistentes, not reservas) and div-by-zero → 0 (D-123-11); (f) provisional flag on an immature cohort period (session date too recent for the window to close — D-123-12); (g) turno bucketing mañana/tarde/otro from startTime (D-123-13); (h) plan axis groups by the plan BOUGHT (D-123-09); (i) gestión token → 403, admin token → 200. Call `GET /api/admin/analytics/trial-funnel` (with and without a `window` query param). CI-only — do NOT run locally.

  </action>
  <verify>
    <automated>cd el-templo-api && pnpm tsc --noEmit 2>&1 | grep -v node_modules | (! grep -iE "error TS")</automated>
  </verify>
  <acceptance_criteria>
    - `el-templo-api/test/analytics/trial-funnel.test.ts` exists and imports `TrialFunnelService` + `createTestApp`/`getAuthToken`/`cleanAllTestData`.
    - `grep -nE "qr_escaneado|confirmado" el-templo-api/test/analytics/trial-funnel.test.ts` matches (asistió-via-status case) AND a comment/case asserts a trial without an attendance row still counts (D-123-07).
    - `grep -n "provisional" el-templo-api/test/analytics/trial-funnel.test.ts` matches (maturity case — D-123-12).
    - `grep -nE "manana|tarde|otro|turno" el-templo-api/test/analytics/trial-funnel.test.ts` matches (turno bucketing — D-123-13).
    - `grep -n "403" el-templo-api/test/analytics/trial-funnel.test.ts` AND `grep -n "200" el-templo-api/test/analytics/trial-funnel.test.ts` both match.
    - `grep -cE "\bpassword:\s" el-templo-api/test/analytics/trial-funnel.test.ts` returns 0 (passwordHash/registerUser only).
    - `cd el-templo-api && pnpm tsc --noEmit` reports no errors in the test file.
  </acceptance_criteria>
  <done>trial-funnel.test.ts covers FUNNEL-01..05 incl. asistió-via-status, new-lead exclusion, in/out-of-window compró, denominators, provisional, turno, plan-bought, and gestión 403 / admin 200; type-checks clean (CI runs the suite).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                                                     |
| -------------------------- | ----------------------------------------------------------------------------------------------- |
| client → GET /trial-funnel | authenticated admin/gestión request; branchId/dates/window are untrusted query input            |
| service → MySQL            | scope-filtered reads over bookings/schedules/subscriptions/branches incl. correlated subqueries |

## STRIDE Threat Register

| Threat ID | Category               | Component                                       | Disposition | Mitigation Plan                                                                                                                                                                                                                       |
| --------- | ---------------------- | ----------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-123-05  | Elevation of Privilege | GET /trial-funnel authz                         | mitigate    | `requireAdminAnalytics` (ADMIN_ROLES) on the route; gestión 403 (test-covered); plugin onRequest operational-role gate.                                                                                                               |
| T-123-06  | Information Disclosure | branch/country scope leakage                    | mitigate    | Every query spreads `applyScope(...).conditions` on `schedules.branchId` / `subscriptions.branchId`; `request.scope.country` drives the country filter; breakdown axes (incl. turno) are additive groupBy keys, never access filters. |
| T-123-07  | Information Disclosure | needsBranchJoin missing → 500 / cross-sede rows | mitigate    | Conditional `.$dynamic()` innerJoin(branches) whenever `needsBranchJoin` — grep-verified in Task 1.                                                                                                                                   |
| T-123-08  | Tampering / Injection  | `window` query param into attribution math      | mitigate    | `trialFunnelQuerystring` bounds `window` integer 1..365 before the service; the value reaches the predicate as a SERVICE-controlled typed integer; dates are bound parameters.                                                        |
| T-123-09  | Tampering              | correlated subquery binding wrong table         | mitigate    | Outer-table refs qualified with explicit `schema.bookings.*` prefix (Drizzle renders unqualified correlated refs against the inner table) — grep-verified in Task 1.                                                                  |
| T-123-SC  | Tampering              | npm/pip/cargo installs                          | accept      | No new dependencies added in this plan.                                                                                                                                                                                               |

</threat_model>

<verification>
- `cd el-templo-api && pnpm tsc --noEmit` clean (local typecheck only — CI runs tests; do not run tests locally per project policy).
- Grep gates pass (asistió from bookings.status not attendance, conditional branch join, explicit correlated-subquery qualification, named-constant turno cutoffs, no funnel-service import, gestión 403, no raw `password`).
- The `/trial-funnel` 200 response declares every wire field; `window` bounded 1..365.
</verification>

<success_criteria>

1. `GET /api/admin/analytics/trial-funnel` returns 200 for admin, 403 for gestión.
2. Response delivers the cascade reservó→asistió→compró with tasa_show, tasa_cierre (over asistentes), punta_a_punta — div-by-zero → 0.
3. Asistió is from bookings.status; only new leads (no prior paid sub) count; compró is the first paid sub within a configurable ~21d window that matures on its own (provisional flag).
4. Breakdowns by branch/country/turno/plan-bought; turno from schedules.startTime (mañana/tarde/otro).
5. Type-checks clean; integration test covers FUNNEL-01..05 + authz (CI-only). funnel-service.ts untouched.
   </success_criteria>

<output>
Create `.planning/phases/123-asistencia-funnel-frecuencia-de-asistencia-funnel-de-sesione/123-02-SUMMARY.md` when done.
</output>
