---
phase: 134-rbol-del-miembro-estados-de-nodo-y-criterio-de-avance-objeti
plan: 02
type: execute
wave: 2
depends_on: ["134-01"]
files_modified:
  - el-templo-app/src/modules/progression/types.ts
  - el-templo-app/src/modules/progression/components/SubfamilyProgressRow.vue
  - el-templo-app/src/modules/progression/components/TreeCategorySection.vue
  - el-templo-app/src/modules/progression/pages/MiArbol.vue
autonomous: true
requirements: [R6]
must_haves:
  truths:
    - "Each node row in Mi Árbol shows a state icon+color (4 states), its dl number, and its band label/color"
    - "Dominado nodes render with a distinct 'done' treatment; en_progreso with a torch/next-up treatment; disponible neutral; bloqueado locked"
    - "The route % is re-labelled to communicate alcance (reach), not completion/domination (D-05)"
    - "Mi Árbol still shows only milestones (no contract change to the node set)"
  artifacts:
    - path: "el-templo-app/src/modules/progression/types.ts"
      provides: "TreeNode mirror with state + band unions"
      contains: "state"
    - path: "el-templo-app/src/modules/progression/components/SubfamilyProgressRow.vue"
      provides: "per-node state icon/color + dl + band rendering, % re-label"
      contains: "node.state"
  key_links:
    - from: "SubfamilyProgressRow.vue"
      to: "TreeNode.state / TreeNode.band"
      via: "render verbatim from server fields"
      pattern: "node\\.(state|band)"
---

<objective>
Refresh the member-app Mi Árbol view (R6, D-08) so each milestone node renders the 4 server-computed states (icon + color: dominado/en_progreso/disponible/bloqueado), its numeric `dl`, and its difficulty `band` (color per level), and re-label the route % as "a tu alcance" (reach, not completion — D-05). This is a refresh of the existing vertical-list view, NOT a redesign. The frontend renders the server fields verbatim and computes nothing about state (D-05 fase 127 pattern).

Purpose: bring fase 133's tree quality to the member experience. The contract (`state`, `band`) lands in Plan 01; this plan consumes it.
Output: updated TreeNode mirror + refreshed SubfamilyProgressRow with the agreed mockup, ring re-label, and a short disclaimer that ring (alcance) and green (dominado) measure different things.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/134-rbol-del-miembro-estados-de-nodo-y-criterio-de-avance-objeti/134-CONTEXT.md
@.planning/phases/134-rbol-del-miembro-estados-de-nodo-y-criterio-de-avance-objeti/134-PATTERNS.md
@.planning/phases/134-rbol-del-miembro-estados-de-nodo-y-criterio-de-avance-objeti/134-01-PLAN.md

<interfaces>
<!-- Server contract this plan consumes (defined in Plan 01). Render verbatim. -->

Backend TreeNode now emits (Plan 01):

- state: "dominado" | "en_progreso" | "disponible" | "bloqueado"
- band: "alfa" | "delta" | "sigma" | "omega" | "spartan" (server never emits "kairos" as a node band; keep it in the union for parity)

Current member-app mirror to extend (types.ts lines 66-72):

```typescript
export interface TreeNode {
  exerciseId: number;
  name: string;
  dificultadLineal: number;
  reached: boolean;
}
```

Current node render (SubfamilyProgressRow.vue lines 21-35) — binary reached icon to REPLACE with a 4-state mapping:

```vue
<q-icon
  :name="node.reached ? 'check_circle' : 'radio_button_unchecked'"
  size="16px"
/>
<span class="subfamily-row__node-name">{{ node.name }}</span>
```

Agreed mockup (CONTEXT D-08):

```
✅ Aussie Pull-up   dl3 · alfa    DOMINADO
🔥 Pull-up          dl5 · delta   EN PROGRESO
⚪ One-Arm Negative dl7 · sigma   DISPONIBLE
🔒 One-Arm Pull-up  dl10· omega   BLOQUEADO
```

Brand colors available (quasar.variables.scss, already @import'd at SubfamilyProgressRow line 65): $primary (terracotta), $secondary, $accent (charcoal), $positive. NO BLUE anywhere (MEMORY reference_brand_palette). Reuse the existing level palette for band colors.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mirror state + band on the member-app TreeNode</name>
  <files>el-templo-app/src/modules/progression/types.ts</files>
  <read_first>
    el-templo-app/src/modules/progression/types.ts (TreeNode lines 66-72, level union line 102 for the union style)
    el-templo-app/src/modules/progression/stores/treeProgressStore.ts (confirm it stores categories verbatim; no derivation to add)
  </read_first>
  <action>
    Extend the `TreeNode` interface (types.ts lines 66-72) with `state: 'dominado' | 'en_progreso' | 'disponible' | 'bloqueado'` and `band: 'kairos' | 'alfa' | 'delta' | 'sigma' | 'omega' | 'spartan'` — exact same union style already used for `level` (line 102). Keep the "mirror the authoritative DTO, render verbatim" comment intent. Do NOT add any derivation in the store or composable (would break D-05); the fields flow through transparently.
  </action>
  <verify>
    <automated>cd el-templo-app && pnpm exec vue-tsc --noEmit 2>&1 | grep -i "types.ts" | grep -c error || true</automated>
  </verify>
  <acceptance_criteria>
    - types.ts TreeNode contains `state:` and `band:` with the exact unions above
    - No new logic added to treeProgressStore.ts or useTreeProgressApi.ts
    - `cd el-templo-app && pnpm exec vue-tsc --noEmit` reports zero errors in types.ts
  </acceptance_criteria>
  <done>The member-app DTO mirror carries state + band; the contract is in sync with Plan 01.</done>
</task>

<task type="auto">
  <name>Task 2: Render the 4 states + dl + band per node and re-label the ring</name>
  <files>el-templo-app/src/modules/progression/components/SubfamilyProgressRow.vue, el-templo-app/src/modules/progression/components/TreeCategorySection.vue, el-templo-app/src/modules/progression/pages/MiArbol.vue</files>
  <read_first>
    el-templo-app/src/modules/progression/components/SubfamilyProgressRow.vue (whole file — node loop lines 21-35, header lines 3-6, styles lines 64-131)
    el-templo-app/src/modules/progression/components/TreeCategorySection.vue (ring header lines 5-15)
    el-templo-app/src/modules/progression/pages/MiArbol.vue (subtitle line 5, intro header lines 3-6)
  </read_first>
  <action>
    In SubfamilyProgressRow.vue, replace the binary reached icon (lines 28-32) with a per-`node.state` icon+color and add the `dl{n}` numeric and `band` label to each row, matching the agreed mockup. Map state→{icon,color} and band→color via local `computed`/lookup objects (presentation only, like `progressValue` line 61 — compute NOTHING about state itself). Concrete state mapping: dominado → icon `check_circle`, color `$positive`, label "DOMINADO"; en_progreso → icon `local_fire_department` (torch/next-up), color `$primary`, label "EN PROGRESO"; disponible → icon `radio_button_unchecked`, color neutral (`rgba($accent,0.5)`), label "DISPONIBLE"; bloqueado → icon `lock`, color `rgba($accent,0.4)`, label "BLOQUEADO". Band label is the raw band string (alfa/delta/sigma/omega/spartan); band color reuses the existing level palette (NO BLUE — terracotta/gold/charcoal family). Render `dl{{ node.dificultadLineal }}`. Re-label the route % in the header (line 5) from "{{ percent }}%" plain to convey alcance — e.g. keep the number but change surrounding copy to "X% a tu alcance" (D-05); do NOT change `progressValue`/`percent` math. In TreeCategorySection.vue, adjust the ring/subtitle copy to "a tu alcance" wording if a label exists; do not change the formula. In MiArbol.vue, update the `__subtitle` (line 5) or add a one-line disclaimer that the ring measures alcance and the green/dominado state measures mastery — they are different (D-05 "hay que comunicar que miden cosas distintas"). Icon/color exact choices are Claude's discretion within the brand palette; no blue.
  </action>
  <verify>
    <automated>cd el-templo-app && pnpm exec vue-tsc --noEmit 2>&1 | grep -iE "SubfamilyProgressRow|TreeCategorySection|MiArbol" | grep -c error || true</automated>
  </verify>
  <acceptance_criteria>
    - SubfamilyProgressRow renders a per-`node.state` icon+color (4 distinct states), not the binary reached icon
    - Each node row shows `dl{n}` and the `band` label
    - The route % header copy conveys "a tu alcance" (D-05) while `percent`/`progressValue` math is unchanged
    - MiArbol or TreeCategorySection communicates that ring (alcance) ≠ dominado
    - No blue color introduced (grep the touched files: no `#...` blue tokens / `blue` keyword)
    - `cd el-templo-app && pnpm exec vue-tsc --noEmit` reports zero errors in the touched files
  </acceptance_criteria>
  <done>Mi Árbol matches the agreed mockup: state icon/color + dl + band per node, ring re-labelled as alcance, milestones-only unchanged.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                     |
| ----------------------------------- | --------------------------------------------------------------- |
| member app render → server tree DTO | Client renders `state`/`band` it received; no new request input |

## STRIDE Threat Register

| Threat ID | Category               | Component         | Disposition | Mitigation Plan                                                                                                                                                         |
| --------- | ---------------------- | ----------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-134-04  | Information Disclosure | Mi Árbol render   | accept      | The view renders only the authenticated member's own tree DTO (server-scoped in Plan 01). No new id is sent; no cross-member data is fetched. Pure presentation change. |
| T-134-05  | Tampering              | client-side state | accept      | States are server-computed and rendered verbatim; the client computes nothing it could tamper with to gain access. Low risk, no PII.                                    |

</threat_model>

<verification>
- `cd el-templo-app && pnpm exec vue-tsc --noEmit` clean across the 4 touched files.
- Mi Árbol shows 4 state treatments + dl + band per node, ring says "a tu alcance".
- No blue introduced; milestones-only node set unchanged.
- This is a visual change → likely a manual UAT in the member app after merge (human-verify deferred per project end-of-phase UAT flow).
</verification>

<success_criteria>

- Each milestone node renders state icon/color + dl + band per the agreed mockup (D-08).
- Ring % re-labelled to alcance; disclaimer communicates ring ≠ dominado (D-05).
- Contract mirror in sync with Plan 01; no client-side state derivation.
  </success_criteria>

<output>
Create `.planning/phases/134-rbol-del-miembro-estados-de-nodo-y-criterio-de-avance-objeti/134-02-SUMMARY.md` when done.
</output>
