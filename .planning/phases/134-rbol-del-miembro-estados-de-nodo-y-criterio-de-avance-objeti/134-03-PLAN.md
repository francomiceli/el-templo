---
phase: 134-rbol-del-miembro-estados-de-nodo-y-criterio-de-avance-objeti
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-app/src/modules/training/components/BlockProgressionView.vue
autonomous: true
requirements: [R5]
must_haves:
  truths:
    - "The player shows the objective advance criterion next to the dominado/bajado adjust row"
    - "The criterion is derived in runtime from the exercise contraction: ISO → '3×30s isométrico'; CON/EXC → '3×8 dinámico (reinicia en 3×5)'"
    - "The fase-131 adjust mechanic (más fácil / más difícil tap) is unchanged — the criterion text complements it, never replaces it"
  artifacts:
    - path: "el-templo-app/src/modules/training/components/BlockProgressionView.vue"
      provides: "derived advance-criterion text near the adjust row"
      contains: "advanceCriterion"
  key_links:
    - from: "BlockProgressionView.vue"
      to: "currentSlideExercise.contraction"
      via: "computed advanceCriterion mapping contraction → criterion string"
      pattern: "contraction"
---

<objective>
Show the falsifiable "dominado" criterion (R5, D-07) in the player, right next to the fase-131 difficulty-adjustment row. The criterion is derived deterministically at runtime from the current exercise's `contraction` (already on the slide) — no migration, no new column, impossible to desync: ISO → "Objetivo: 3×30s"; CON/EXC → "Objetivo: 3×8 (reinicia en 3×5)". This COMPLEMENTS the fase-131 manual dominado tap, it does not replace it.

Purpose: give the member an objective definition of mastery so "dominado" means something (pairs with Plan 01's evidence-only dominado state).
Output: a short criterion line in BlockProgressionView next to the adjust row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/134-rbol-del-miembro-estados-de-nodo-y-criterio-de-avance-objeti/134-CONTEXT.md
@.planning/phases/134-rbol-del-miembro-estados-de-nodo-y-criterio-de-avance-objeti/134-PATTERNS.md

<interfaces>
<!-- Everything needed is already on the slide. No backend, no new prop required. -->

currentSlideExercise (BlockProgressionView.vue line 324) is a `Prescription | null`.
Prescription (el-templo-app/src/modules/training/types/session.ts line 27+) has `contraction: string` (line 35). Values are 'CON' | 'EXC' | 'ISO'.

The adjust row to anchor the text to (BlockProgressionView.vue lines 93-117):

```vue
<div v-if="canAdjustCurrentSlide" class="block-progression__detail-adjust-row">
  <q-btn ... @click="onAdjust('down')" />  <!-- más fácil / bajado -->
  <q-btn ... @click="onAdjust('up')" />    <!-- más difícil / dominado -->
</div>
```

canAdjustCurrentSlide (line 504) is true only for a real, active, non-mobility slide — the same gate that should show the criterion.

Project rules: createLogger() never console (CLAUDE.md). No `any`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive and render the advance criterion next to the adjust row</name>
  <files>el-templo-app/src/modules/training/components/BlockProgressionView.vue</files>
  <read_first>
    el-templo-app/src/modules/training/components/BlockProgressionView.vue (adjust row lines 93-117, currentSlideExercise lines 324-327, canAdjustCurrentSlide lines 504-506, the script computeds)
    el-templo-app/src/modules/training/types/session.ts (Prescription interface line 27+, contraction line 35)
  </read_first>
  <action>
    Add an `advanceCriterion` computed in BlockProgressionView's script: read `currentSlideExercise.value?.contraction`; if `'ISO'` return `'Objetivo: 3×30s'`, otherwise (CON / EXC, and null fallback) return `'Objetivo: 3×8 (reinicia en 3×5)'`. Render it as a short text line inside the same `v-if="canAdjustCurrentSlide"` region as the adjust row (lines 93-117) — directly above or below the two buttons — so it appears only on real, active, non-mobility slides, alongside the fase-131 dominado/bajado controls. Style it as a small muted caption using the brand palette (NO blue). Do NOT touch `onAdjust`, `canAdjustCurrentSlide`, or any fase-131 adjust mechanic — this is additive text only. Use proper typing; no `any`; use createLogger() if any logging is needed (none expected).
  </action>
  <verify>
    <automated>cd el-templo-app && pnpm exec vue-tsc --noEmit 2>&1 | grep -i "BlockProgressionView" | grep -c error || true</automated>
  </verify>
  <acceptance_criteria>
    - BlockProgressionView declares an `advanceCriterion` computed reading `contraction`
    - ISO maps to "Objetivo: 3×30s"; CON/EXC (and null) map to "Objetivo: 3×8 (reinicia en 3×5)"
    - The criterion line renders inside the `v-if="canAdjustCurrentSlide"` region next to the adjust buttons
    - `onAdjust` and the fase-131 adjust mechanic are unchanged (diff touches only additive markup + the new computed)
    - No blue color introduced
    - `cd el-templo-app && pnpm exec vue-tsc --noEmit` reports zero errors in BlockProgressionView
  </acceptance_criteria>
  <done>The player shows the objective advance criterion derived from contraction, next to the fase-131 adjust row, without changing the adjust mechanic.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                                  |
| -------------------------- | ---------------------------------------------------------------------------- |
| player render → slide data | Client derives a static label from data already on the slide; no new request |

## STRIDE Threat Register

| Threat ID | Category               | Component       | Disposition | Mitigation Plan                                                                                                                                                                                                  |
| --------- | ---------------------- | --------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-134-06  | Information Disclosure | criterion text  | accept      | The criterion is a deterministic label derived from the current exercise's `contraction` already loaded for the member's own session. No new data is fetched, no PII, no cross-member access. Pure presentation. |
| T-134-07  | Tampering              | adjust mechanic | mitigate    | The change is additive text only; `onAdjust`/`canAdjustCurrentSlide` are not modified, so the fase-131 dominado/bajado authorization path is untouched.                                                          |

</threat_model>

<verification>
- `cd el-templo-app && pnpm exec vue-tsc --noEmit` clean for BlockProgressionView.
- Criterion text appears only on adjustable slides, with the correct ISO vs CON/EXC wording.
- fase-131 adjust buttons behave exactly as before.
- Visual change → likely manual UAT in the player after merge (end-of-phase UAT).
</verification>

<success_criteria>

- The player shows "Objetivo: 3×30s" for ISO and "Objetivo: 3×8 (reinicia en 3×5)" for CON/EXC, next to the adjust row (D-07).
- No migration, no new column, no change to the fase-131 mechanic.
  </success_criteria>

<output>
Create `.planning/phases/134-rbol-del-miembro-estados-de-nodo-y-criterio-de-avance-objeti/134-03-SUMMARY.md` when done.
</output>
