---
phase: 145-pos-del-profe
plan: 02
type: execute
wave: 2
depends_on: ["145-01"]
files_modified:
  - el-templo-api/src/modules/finance/types.ts
  - el-templo-api/src/modules/finance/transaction-service.ts
  - el-templo-api/test/finance/pending-tray.test.ts
  - el-templo-admin/src/types/transaction.ts
  - el-templo-admin/src/components/caja/BandejaPendientesTab.vue
autonomous: true
requirements: [COBRO-02]
must_haves:
  truths:
    - "En la bandeja de Pendientes, una fila cuyo cobro tiene motivo 'sin_plan' muestra un chip 'Sin plan — asignar'"
    - "El chip navega a la ficha del alumno (/alumnos/:userId)"
    - "Las filas con motivo distinto de 'sin_plan' (o sin motivo) NO muestran el chip"
    - "listPendingTray expone el motivo (miscReason) en cada fila"
  artifacts:
    - path: "el-templo-api/src/modules/finance/transaction-service.ts"
      provides: "listPendingTray selecciona y mapea misc_reason a la fila"
      contains: "miscReason"
    - path: "el-templo-admin/src/components/caja/BandejaPendientesTab.vue"
      provides: "Chip 'Sin plan — asignar' navegable en filas con motivo sin_plan"
      contains: "Sin plan"
  key_links:
    - from: "el-templo-admin/src/components/caja/BandejaPendientesTab.vue"
      to: "/alumnos/:userId"
      via: "goToMember(row.memberId) reusado por el chip"
      pattern: "goToMember"
    - from: "el-templo-api/src/modules/finance/transaction-service.ts"
      to: "PendingTrayItem.miscReason"
      via: "select misc_reason + map a la fila"
      pattern: "miscReason"
---

<objective>
**COBRO-02** — En la bandeja de Pendientes (`BandejaPendientesTab.vue`), las filas
cuyo cobro suelto tiene motivo "Sin plan activo" (`misc_reason = 'sin_plan'`) muestran
un chip **"Sin plan — asignar"** que navega a la ficha del alumno para que gestión
le asigne el plan. Requiere exponer `miscReason` en `listPendingTray` (la query que
alimenta la bandeja) y consumirlo en el front.

Purpose: los cobros "sin plan activo" no quedan invisibles como plata suelta; gestión
los ve marcados y va directo a asignar el plan (cuya imputación implementa la fase 146).
Output: `miscReason` en el payload de la bandeja + chip navegable en el front + test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@BRIEF-FEEDBACK-V52-CAJA.md
@CLAUDE.md

<interfaces>
<!-- Contratos que el executor usa directamente. La columna misc_reason la crea 145-01. -->

`PendingTrayItem` (API: el-templo-api/src/modules/finance/types.ts ~línea 365;
mirror admin: el-templo-admin/src/types/transaction.ts ~línea 306) es la fila de la
bandeja. Hoy NO lleva el motivo. Agregar `miscReason: "sin_plan" | "otro" | null`
en AMBOS (el admin es un mirror manual del API — mantener sincronizados).

`listPendingTray` (transaction-service.ts ~línea 1030): hace un `.select({...})`
sobre financialTransactions con leftJoins, y luego `.map()` a `PendingTrayItem`
(~línea 1162). Agregar `miscReason: schema.financialTransactions.miscReason` al
select y `miscReason: r.miscReason` al map.

`BandejaPendientesTab.vue` ya tiene `goToMember(memberId)` →
`router.push('/alumnos/${memberId}')` (~línea 400) y una columna 'socio' clickable.
El chip reusa ese `goToMember`. La ruta del alumno es `alumnos/:userId` (confirmada
en router/routes.ts).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exponer miscReason en listPendingTray (+ tipos API/admin + test)</name>
  <files>el-templo-api/src/modules/finance/types.ts, el-templo-api/src/modules/finance/transaction-service.ts, el-templo-admin/src/types/transaction.ts, el-templo-api/test/finance/pending-tray.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/finance/types.ts (PendingTrayItem ~línea 365)
    - el-templo-api/src/modules/finance/transaction-service.ts (listPendingTray: select ~línea 1106, map a PendingTrayItem ~línea 1162)
    - el-templo-admin/src/types/transaction.ts (PendingTrayItem mirror ~línea 306)
    - el-templo-api/test/finance/pending-tray.test.ts (estructura de seed + assertions de la bandeja)
  </read_first>
  <action>
    1. Agregar `miscReason: "sin_plan" | "otro" | null` a la interfaz `PendingTrayItem`
       en `types.ts` (API) y a su mirror manual en `el-templo-admin/src/types/transaction.ts`
       (mantener ambos sincronizados — el comentario del mirror lo aclara).
    2. En `listPendingTray`, agregar `miscReason: schema.financialTransactions.miscReason`
       al objeto `.select({...})` de la query principal (~línea 1107) y
       `miscReason: r.miscReason` al objeto que arma cada fila en el `.map()` (~línea 1168).
       NO tocar el count query. La columna ya existe (creada en 145-01).
    3. Test en `pending-tray.test.ts`: seedear un cobro suelto (kind='advance_payment',
       pendiente) con `misc_reason='sin_plan'` y assert que la fila devuelta por
       `listPendingTray`/el endpoint trae `miscReason: 'sin_plan'`; y que una fila
       de otro kind (ej. plan_charge pendiente) trae `miscReason: null`. Reusar los
       helpers de seed del archivo.
  </action>
  <acceptance_criteria>
    - `PendingTrayItem` (API y admin) declara `miscReason: "sin_plan" | "otro" | null`.
    - El select de `listPendingTray` incluye `financialTransactions.miscReason` y el map lo asigna.
    - Un cobro suelto con `misc_reason='sin_plan'` aparece en la bandeja con `miscReason:'sin_plan'`; una fila no-misc trae `miscReason:null`.
    - `cd el-templo-api && pnpm exec tsc --noEmit` pasa.
    - `pnpm test test/finance/pending-tray.test.ts` pasa.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-api && grep -q "miscReason" src/modules/finance/types.ts && grep -q "miscReason" src/modules/finance/transaction-service.ts && pnpm exec tsc --noEmit && pnpm test test/finance/pending-tray.test.ts</automated>
  </verify>
  <done>La bandeja de Pendientes expone el motivo de cada cobro; cubierto por test de integración.</done>
</task>

<task type="auto">
  <name>Task 2: Chip "Sin plan — asignar" navegable en la bandeja</name>
  <files>el-templo-admin/src/components/caja/BandejaPendientesTab.vue</files>
  <read_first>
    - el-templo-admin/src/components/caja/BandejaPendientesTab.vue (columna 'socio' clickable ~línea 66, goToMember ~línea 400, definición de columns ~línea 370)
    - el-templo-admin/src/types/transaction.ts (PendingTrayItem con el nuevo miscReason)
  </read_first>
  <action>
    En la fila de la bandeja, mostrar un chip "Sin plan — asignar" cuando
    `row.miscReason === 'sin_plan'` y la fila tiene `memberId`. Implementación:
    renderizar un `q-chip` (clickable, color de acento ej. warning/amber, ícono
    como `person_add`) que al hacer click llame `goToMember(row.memberId)` (reusar
    la función existente — NO duplicar el push de ruta). Ubicarlo junto al nombre
    del socio (extender el slot `#body-cell-socio` existente) o como columna propia;
    preferir extender el slot del socio para no agregar una columna a la grilla.
    El chip NO aparece para filas con `miscReason` null/'otro'. No tocar el flujo de
    Validar/Observar/Corregir/Anular. Sin `any`, sin console.* (createLogger ya está).
  </action>
  <acceptance_criteria>
    - Una fila con `miscReason === 'sin_plan'` y `memberId` muestra el chip "Sin plan — asignar".
    - Click en el chip llama `goToMember(row.memberId)` → navega a `/alumnos/:userId`.
    - Filas con `miscReason` null o 'otro' NO muestran el chip.
    - El chip reusa `goToMember` (no hay un segundo `router.push` hardcodeado).
    - `cd el-templo-admin && npx vue-tsc --noEmit` pasa.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && grep -q "Sin plan" src/components/caja/BandejaPendientesTab.vue && grep -q "sin_plan" src/components/caja/BandejaPendientesTab.vue && npx vue-tsc --noEmit</automated>
  </verify>
  <done>Los cobros marcados "Sin plan activo" se ven en Pendientes con un chip que lleva a la ficha del alumno para asignar el plan.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                        | Description                                                                                                                  |
| ----------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| gestión (admin app) → GET bandeja de Pendientes | Lectura autenticada (FINANCE_READ_ROLES); el motivo es un dato derivado de la columna enum, no input de usuario en esta fase |

## STRIDE Threat Register

| Threat ID | Category               | Component                              | Disposition | Mitigation Plan                                                                                                        |
| --------- | ---------------------- | -------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------- |
| T-145-04  | Information Disclosure | miscReason en el payload de la bandeja | accept      | la bandeja ya está restringida a roles de lectura financiera (fase 141); el motivo es admin-only y no expone PII nueva |
| T-145-05  | Tampering              | chip → navegación a /alumnos/:userId   | accept      | el chip solo hace router.push del memberId ya presente en la fila; no muta estado ni dispara escritura                 |

Block-on: high. Sin instalación de paquetes (sin gate de legitimidad).
</threat_model>

<verification>
- Typecheck verde en api y admin.
- `pnpm test test/finance/pending-tray.test.ts` pasa (miscReason expuesto en la fila).
- El chip aparece solo en filas con motivo 'sin_plan' y navega a la ficha del alumno.
</verification>

<success_criteria>

- COBRO-02: en Pendientes, un cobro con motivo "Sin plan activo" muestra el chip "Sin plan — asignar" que navega a la ficha del alumno; `listPendingTray` expone el motivo de cada fila.
  </success_criteria>

<output>
Create `.planning/phases/145-pos-del-profe/145-02-SUMMARY.md` when done.
</output>
