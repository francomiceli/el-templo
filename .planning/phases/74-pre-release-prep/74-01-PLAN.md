---
phase: 74-pre-release-prep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-app/src-capacitor/package.json
  - el-templo-app/src-capacitor/android/variables.gradle
  - el-templo-app/src-capacitor/android/build.gradle
  - el-templo-app/src-capacitor/android/app/build.gradle
  - el-templo-app/src-capacitor/android/gradle/wrapper/gradle-wrapper.properties
  - el-templo-app/src-capacitor/android/settings.gradle
  - el-templo-app/package.json
autonomous: true
requirements:
  - PLAY-01

must_haves:
  truths:
    - "All @capacitor/* packages in src-capacitor/package.json are v8.x"
    - "variables.gradle has minSdkVersion=24, compileSdkVersion=36, targetSdkVersion=36"
    - "Gradle wrapper is 8.14.3 and AGP is 8.13.0"
    - "npx cap doctor reports no version mismatches"
    - "Main app package.json version is 1.0.0"
  artifacts:
    - path: "el-templo-app/src-capacitor/package.json"
      provides: "Capacitor v8 native dependencies"
      contains: "@capacitor/core.*8"
    - path: "el-templo-app/src-capacitor/android/variables.gradle"
      provides: "Android SDK and AndroidX version targets"
      contains: "minSdkVersion = 24"
    - path: "el-templo-app/src-capacitor/android/build.gradle"
      provides: "Root Gradle config with AGP 8.13.0"
      contains: "8.13.0"
    - path: "el-templo-app/src-capacitor/android/gradle/wrapper/gradle-wrapper.properties"
      provides: "Gradle wrapper version"
      contains: "gradle-8.14.3"
  key_links:
    - from: "el-templo-app/src-capacitor/package.json"
      to: "el-templo-app/package.json"
      via: "Same @capacitor/* major version (v8)"
      pattern: "@capacitor/core.*8"
    - from: "el-templo-app/src-capacitor/android/variables.gradle"
      to: "el-templo-app/src-capacitor/android/app/build.gradle"
      via: "rootProject.ext.* references"
      pattern: "rootProject.ext"
---

<objective>
Upgrade the Capacitor native project from v7 to v8, aligning CLI and all native plugins with the main app's already-v8 dependencies. This resolves the version mismatch between `el-templo-app/package.json` (v8) and `src-capacitor/package.json` (v7) that was left behind during the Phase 7 upgrade.

Purpose: PLAY-01 requires Capacitor CLI and native plugins on the same major version. The mismatch causes potential runtime incompatibilities and blocks the Play Store release pipeline.

Output: All Capacitor packages at v8.x, Gradle toolchain updated (AGP 8.13.0, Gradle 8.14.3), Android SDK targets at 36/36/24, Groovy DSL assignment syntax.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-pre-release-prep/74-CONTEXT.md
@.planning/phases/74-pre-release-prep/74-RESEARCH.md

<interfaces>
<!-- Current state of files being modified. Executor reads these via read_first. -->

From el-templo-app/src-capacitor/package.json (CURRENT - v7 mismatch):

```json
{
  "dependencies": {
    "@capacitor/android": "^7.4.5",
    "@capacitor/app": "^7.0.0",
    "@capacitor/cli": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.4.5",
    "@capacitor/preferences": "^7.0.3"
  }
}
```

From el-templo-app/package.json (already v8):

```json
{
  "dependencies": {
    "@capacitor/app": "^8.0.0",
    "@capacitor/cli": "^8.1.0",
    "@capacitor/core": "^8.0.1",
    "@capacitor/haptics": "^8.0.0",
    "@capacitor/preferences": "^8.0.0"
  }
}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Capacitor native project from v7 to v8</name>
  <files>
    el-templo-app/src-capacitor/package.json,
    el-templo-app/src-capacitor/android/variables.gradle,
    el-templo-app/src-capacitor/android/build.gradle,
    el-templo-app/src-capacitor/android/gradle/wrapper/gradle-wrapper.properties,
    el-templo-app/src-capacitor/android/settings.gradle,
    el-templo-app/package.json
  </files>
  <read_first>
    el-templo-app/src-capacitor/package.json,
    el-templo-app/src-capacitor/android/variables.gradle,
    el-templo-app/src-capacitor/android/build.gradle,
    el-templo-app/src-capacitor/android/gradle/wrapper/gradle-wrapper.properties,
    el-templo-app/src-capacitor/android/settings.gradle,
    el-templo-app/package.json,
    .planning/phases/74-pre-release-prep/74-RESEARCH.md
  </read_first>
  <action>
    **Step 1: Update src-capacitor/package.json dependencies to v8.**

    Replace all `@capacitor/*` dependency versions:
    ```json
    {
      "dependencies": {
        "@capacitor/android": "^8.2.0",
        "@capacitor/app": "^8.0.1",
        "@capacitor/cli": "^8.2.0",
        "@capacitor/core": "^8.2.0",
        "@capacitor/ios": "^8.2.0",
        "@capacitor/preferences": "^8.0.1"
      }
    }
    ```
    Keep `@capacitor/assets` devDependency as-is (v3.0.5 -- asset generation tool, version-independent).
    Keep `pnpm.onlyBuiltDependencies` as-is.

    **Step 2: Update el-templo-app/package.json version to 1.0.0.**

    Change `"version": "0.0.1"` to `"version": "1.0.0"` (user decision: first Play Store release is v1.0.0).

    **Step 3: Update variables.gradle to Capacitor 8 targets.**

    Replace the entire file with:
    ```groovy
    ext {
        minSdkVersion = 24
        compileSdkVersion = 36
        targetSdkVersion = 36
        androidxActivityVersion = '1.11.0'
        androidxAppCompatVersion = '1.7.1'
        androidxCoordinatorLayoutVersion = '1.3.0'
        androidxCoreVersion = '1.17.0'
        androidxFragmentVersion = '1.8.9'
        coreSplashScreenVersion = '1.2.0'
        androidxWebkitVersion = '1.14.0'
        junitVersion = '4.13.2'
        androidxJunitVersion = '1.3.0'
        androidxEspressoCoreVersion = '3.7.0'
        cordovaAndroidVersion = '14.0.1'
    }
    ```

    **Step 4: Update root build.gradle (AGP + google-services).**

    In `el-templo-app/src-capacitor/android/build.gradle`, update the `dependencies` block:
    - Change `classpath 'com.android.tools.build:gradle:8.7.2'` to `classpath 'com.android.tools.build:gradle:8.13.0'`
    - Change `classpath 'com.google.gms:google-services:4.4.2'` to `classpath 'com.google.gms:google-services:4.4.4'`

    **Step 5: Update Gradle wrapper to 8.14.3.**

    In `gradle-wrapper.properties`, change:
    ```
    distributionUrl=https\://services.gradle.org/distributions/gradle-8.14.3-all.zip
    ```

    **Step 6: Run pnpm install in src-capacitor/ to regenerate lockfile.**

    ```bash
    cd el-templo-app/src-capacitor && pnpm install
    ```

    **Step 7: Sync native projects.**

    ```bash
    cd el-templo-app/src-capacitor && npx cap sync android
    ```

    Note: Do NOT run `npx cap sync ios` in CI or on Linux -- iOS sync requires macOS. The package.json version bumps cover iOS plugin versions; iOS sync will happen naturally when the iOS workflow runs on macOS.

    **Step 8: Run cap doctor to verify alignment.**

    ```bash
    cd el-templo-app/src-capacitor && npx cap doctor
    ```

    Confirm no version mismatch warnings.

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-app/src-capacitor && npx cap doctor 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `el-templo-app/src-capacitor/package.json` contains `"@capacitor/core": "^8.2.0"`
    - `el-templo-app/src-capacitor/package.json` contains `"@capacitor/android": "^8.2.0"`
    - `el-templo-app/src-capacitor/package.json` contains `"@capacitor/ios": "^8.2.0"`
    - `el-templo-app/src-capacitor/package.json` contains `"@capacitor/cli": "^8.2.0"`
    - `el-templo-app/package.json` contains `"version": "1.0.0"`
    - `el-templo-app/src-capacitor/android/variables.gradle` contains `minSdkVersion = 24`
    - `el-templo-app/src-capacitor/android/variables.gradle` contains `compileSdkVersion = 36`
    - `el-templo-app/src-capacitor/android/variables.gradle` contains `targetSdkVersion = 36`
    - `el-templo-app/src-capacitor/android/variables.gradle` contains `cordovaAndroidVersion = '14.0.1'`
    - `el-templo-app/src-capacitor/android/build.gradle` contains `gradle:8.13.0`
    - `el-templo-app/src-capacitor/android/build.gradle` contains `google-services:4.4.4`
    - `el-templo-app/src-capacitor/android/gradle/wrapper/gradle-wrapper.properties` contains `gradle-8.14.3`
    - `npx cap doctor` output does not contain "mismatch" or "error"
  </acceptance_criteria>
  <done>All @capacitor/* packages in src-capacitor are v8.x, Gradle toolchain updated to AGP 8.13.0 + Gradle 8.14.3, Android SDK targets are 36/36/24, cap doctor reports clean alignment, main app version is 1.0.0.</done>
</task>

<task type="auto">
  <name>Task 2: Update app build.gradle to Groovy assignment syntax</name>
  <files>
    el-templo-app/src-capacitor/android/app/build.gradle
  </files>
  <read_first>
    el-templo-app/src-capacitor/android/app/build.gradle,
    .planning/phases/74-pre-release-prep/74-RESEARCH.md
  </read_first>
  <action>
    Update `el-templo-app/src-capacitor/android/app/build.gradle` to use Groovy assignment syntax (`=`) to avoid AGP 8.13.0 deprecation warnings. Also update versionName from "1.0" to "1.0.0".

    The full target state of the file (preserving repositories, dependencies, capacitor.build.gradle apply, and google-services block):

    ```groovy
    apply plugin: 'com.android.application'

    android {
        namespace = "com.eltemplo.app"
        compileSdk = rootProject.ext.compileSdkVersion
        defaultConfig {
            applicationId = "com.eltemplo.app"
            minSdkVersion rootProject.ext.minSdkVersion
            targetSdkVersion rootProject.ext.targetSdkVersion
            versionCode = 1
            versionName = "1.0.0"
            testInstrumentationRunner = "androidx.test.runner.AndroidJUnitRunner"
            aaptOptions {
                // Files and dirs to omit from the packaged assets dir, modified to accommodate modern web apps.
                // Default: https://android.googlesource.com/platform/frameworks/base/+/282e181b58cf72b6ca770dc7ca5f91f135444502/tools/aapt/AaptAssets.cpp#61
                ignoreAssetsPattern = '!.svn:!.git:!.ds_store:!*.scc:.*:!CVS:!thumbs.db:!picasa.ini:!*~'
            }
        }
        buildTypes {
            release {
                minifyEnabled = false
                proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            }
        }
        flavorDimensions = ["environment"]
        productFlavors {
            production {
                dimension = "environment"
                // Uses default applicationId from capacitor.config
            }
            staging {
                dimension = "environment"
                applicationIdSuffix = ".staging"
                // Actual applicationId becomes com.eltemplo.app.staging
            }
        }
    }

    repositories {
        flatDir{
            dirs '../capacitor-cordova-android-plugins/src/main/libs', 'libs'
        }
    }

    dependencies {
        implementation fileTree(include: ['*.jar'], dir: 'libs')
        implementation "androidx.appcompat:appcompat:$androidxAppCompatVersion"
        implementation "androidx.coordinatorlayout:coordinatorlayout:$androidxCoordinatorLayoutVersion"
        implementation "androidx.core:core-splashscreen:$coreSplashScreenVersion"
        implementation project(':capacitor-android')
        testImplementation "junit:junit:$junitVersion"
        androidTestImplementation "androidx.test.ext:junit:$androidxJunitVersion"
        androidTestImplementation "androidx.test.espresso:espresso-core:$androidxEspressoCoreVersion"
        implementation project(':capacitor-cordova-android-plugins')
    }

    apply from: 'capacitor.build.gradle'

    try {
        def servicesJSON = file('google-services.json')
        if (servicesJSON.text) {
            apply plugin: 'com.google.gms.google-services'
        }
    } catch(Exception e) {
        logger.info("google-services.json not found, google-services plugin not applied. Push Notifications won't work")
    }
    ```

    Key changes from current state:
    - `namespace "com.eltemplo.app"` -> `namespace = "com.eltemplo.app"`
    - `compileSdk rootProject.ext.compileSdkVersion` -> `compileSdk = rootProject.ext.compileSdkVersion`
    - `applicationId "com.eltemplo.app"` -> `applicationId = "com.eltemplo.app"`
    - `versionCode 1` -> `versionCode = 1` (stays 1 for now, CI env var wiring in Plan 02)
    - `versionName "1.0"` -> `versionName = "1.0.0"`
    - `testInstrumentationRunner "..."` -> `testInstrumentationRunner = "..."`
    - `ignoreAssetsPattern '...'` -> `ignoreAssetsPattern = '...'`
    - `minifyEnabled false` -> `minifyEnabled = false`
    - `flavorDimensions "environment"` -> `flavorDimensions = ["environment"]`
    - `dimension "environment"` -> `dimension = "environment"`
    - `applicationIdSuffix ".staging"` -> `applicationIdSuffix = ".staging"`

    Do NOT change `minifyEnabled` to `true` yet -- that happens in Plan 02 alongside ProGuard rules.
    Do NOT change `versionCode` to CI env var yet -- that also happens in Plan 02.

  </action>
  <verify>
    <automated>grep -c "= " /home/franco/projects/el-templo/el-templo-app/src-capacitor/android/app/build.gradle</automated>
  </verify>
  <acceptance_criteria>
    - `el-templo-app/src-capacitor/android/app/build.gradle` contains `namespace = "com.eltemplo.app"`
    - `el-templo-app/src-capacitor/android/app/build.gradle` contains `compileSdk = rootProject.ext.compileSdkVersion`
    - `el-templo-app/src-capacitor/android/app/build.gradle` contains `applicationId = "com.eltemplo.app"`
    - `el-templo-app/src-capacitor/android/app/build.gradle` contains `versionName = "1.0.0"`
    - `el-templo-app/src-capacitor/android/app/build.gradle` contains `versionCode = 1`
    - `el-templo-app/src-capacitor/android/app/build.gradle` contains `flavorDimensions = ["environment"]`
    - `el-templo-app/src-capacitor/android/app/build.gradle` contains `minifyEnabled = false`
    - `el-templo-app/src-capacitor/android/app/build.gradle` contains `dimension = "environment"`
    - File still contains `apply from: 'capacitor.build.gradle'` (not accidentally removed)
    - File still contains `implementation project(':capacitor-android')` (not accidentally removed)
  </acceptance_criteria>
  <done>app/build.gradle uses Groovy assignment syntax throughout, no AGP 8.13.0 deprecation warnings, versionName is "1.0.0".</done>
</task>

</tasks>

<verification>
1. `npx cap doctor` in `src-capacitor/` reports no version mismatches
2. All `@capacitor/*` packages in `src-capacitor/package.json` are `^8.x`
3. `variables.gradle` has correct SDK and AndroidX versions for Capacitor 8
4. `build.gradle` (root) has AGP 8.13.0 and google-services 4.4.4
5. `gradle-wrapper.properties` has Gradle 8.14.3
6. `app/build.gradle` uses `=` assignment syntax throughout
7. `el-templo-app/package.json` version is `1.0.0`
</verification>

<success_criteria>

- `npx cap doctor` reports clean alignment (all v8)
- No Groovy DSL deprecation warnings in `app/build.gradle`
- Main app package.json version is 1.0.0
- All version targets match 74-RESEARCH.md Standard Stack table
  </success_criteria>

<output>
After completion, create `.planning/phases/74-pre-release-prep/74-01-SUMMARY.md`
</output>
