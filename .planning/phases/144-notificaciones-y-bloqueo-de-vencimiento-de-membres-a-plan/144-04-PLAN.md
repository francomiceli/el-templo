---
phase: 144-notificaciones-y-bloqueo-de-vencimiento-de-membres-a-plan
plan: 04
type: execute
wave: 2
depends_on: ["144-01"]
files_modified:
  - el-templo-api/src/modules/shared/errors.ts
  - el-templo-api/src/modules/scheduling/booking-service.ts
  - el-templo-api/src/modules/scheduling/routes.ts
  - el-templo-api/test/scheduling-reserve-coverage.test.ts
  - el-templo-app/src/pages/ReservasPage.vue
autonomous: true
requirements: [BOOK-BLOCK]
user_setup: []

must_haves:
  truths:
    - "reserve() rejects a booking whose class date is after the member's covered-until (chain active+scheduled)"
    - "A class date covered by a scheduled successor IS allowed (D-13)"
    - "covered-until null (no end_date) never blocks (D-14 guard)"
    - "The rejection carries a distinguishable code COVERAGE_EXPIRED so the app shows the renewal dialog instead of the generic error"
    - "ReservasPage shows a skippable renewal dialog with a WhatsApp CTA on COVERAGE_EXPIRED; all other reserve errors keep the generic negative notify"
  artifacts:
    - path: "el-templo-api/src/modules/shared/errors.ts"
      provides: "CoverageExpiredError with code field"
      contains: "COVERAGE_EXPIRED"
    - path: "el-templo-api/src/modules/scheduling/booking-service.ts"
      provides: "coverage check in reserve()"
      contains: "CoveredUntil"
    - path: "el-templo-app/src/pages/ReservasPage.vue"
      provides: "booking-block dialog"
      contains: "COVERAGE_EXPIRED"
  key_links:
    - from: "booking-service.ts reserve()"
      to: "getCoveredUntil"
      via: "coverage check after membership gate"
      pattern: "CoveredUntil"
    - from: "scheduling/routes.ts /reserve catch"
      to: "{ code: COVERAGE_EXPIRED }"
      via: "structured route error branch"
      pattern: "COVERAGE_EXPIRED"
    - from: "ReservasPage.vue confirmReserve catch"
      to: "booking-block q-dialog"
      via: "err.response?.data?.code === COVERAGE_EXPIRED"
      pattern: "COVERAGE_EXPIRED"
---

<objective>
Close the latent bug where a member can reserve a presencial class dated after their plan expires: add a server-side coverage check in reserve(), surface a distinguishable COVERAGE_EXPIRED code, and show a renewal dialog (WhatsApp CTA) in ReservasPage.

Purpose: BOOK-BLOCK entregable 3 (D-11..D-15).
Output: CoverageExpiredError, reserve() coverage check + integration test, structured route error, ReservasPage booking-block dialog.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/144-notificaciones-y-bloqueo-de-vencimiento-de-membres-a-plan/144-CONTEXT.md
@.planning/phases/144-notificaciones-y-bloqueo-de-vencimiento-de-membres-a-plan/144-PATTERNS.md
@.planning/phases/144-notificaciones-y-bloqueo-de-vencimiento-de-membres-a-plan/144-UI-SPEC.md
@.planning/phases/144-notificaciones-y-bloqueo-de-vencimiento-de-membres-a-plan/144-01-SUMMARY.md

<interfaces>
<!-- Extracted from live code 2026-06-25. -->
el-templo-api/src/modules/shared/errors.ts: class AppError extends Error { readonly statusCode } (§8-15); BadRequestError extends AppError (§29-33, statusCode 400, NO code field today). error-handler.ts §36-39 default only serializes { error, message }.
el-templo-api/src/modules/scheduling/booking-service.ts §57-87: reserve(memberId, scheduleId, date: string). Membership gate at §82-87 (getMemberSubscription → BadRequestError "No tenes una suscripcion activa"). `this.subscriptionService` available; `date` is the booking date "YYYY-MM-DD".
el-templo-api/src/modules/subscriptions/service.ts (Plan 01): getCoveredUntil(userId): Promise<string | null>.
el-templo-api/src/modules/scheduling/routes.ts §721-730: POST /reserve handler → `bookingService.reserve(...)`, catch → `handleServiceError(err, reply, request.log, "member reserve")`. Precedent for richer route error: ConflictError + affectedSchedules branch §160-182.
el-templo-app/src/pages/ReservasPage.vue: confirmReserve catch §1200-1206 (extractError + $q.notify negative); buildWhatsAppUrl imported §628; userStore.profile?.branchCountry; WhatsApp open via `window.open(buildWhatsAppUrl(...), '_blank')` §688/§748; axios available for isAxiosError.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CoverageExpiredError + reserve() coverage check + route surfacing</name>
  <files>el-templo-api/src/modules/shared/errors.ts, el-templo-api/src/modules/scheduling/booking-service.ts, el-templo-api/src/modules/scheduling/routes.ts, el-templo-api/test/scheduling-reserve-coverage.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/shared/errors.ts §8-45 (AppError, BadRequestError)
    - el-templo-api/src/modules/scheduling/booking-service.ts §57-130 (reserve, membership gate insertion point)
    - el-templo-api/src/modules/scheduling/routes.ts §160-182 (ConflictError structured branch) + §721-730 (/reserve handler)
    - 144-PATTERNS.md "booking-service.ts — reserve() coverage check" + "routes.ts — surface the code" + "Structured route error" sections
    - el-templo-api/test/scheduling-reserve-trial.test.ts (reserve test fixture style) + test/helpers.ts
  </read_first>
  <behavior>
    - reserve() with class date 2026-07-20 and covered-until 2026-07-10 → throws CoverageExpiredError (code COVERAGE_EXPIRED), route responds 400 with body.code === "COVERAGE_EXPIRED"
    - reserve() with class date 2026-07-05 and covered-until 2026-07-10 → succeeds (within coverage)
    - reserve() with class date covered only by a scheduled successor (active ends 2026-07-10, scheduled ends 2026-08-09, class 2026-07-25) → succeeds (D-13)
    - reserve() with covered-until null (no end_date) → coverage check skipped, no COVERAGE_EXPIRED (D-14)
    - generic reserve failure (e.g. no active subscription) → still BadRequestError WITHOUT code COVERAGE_EXPIRED (regression guard)
  </behavior>
  <action>
    In errors.ts add an OPTIONAL `readonly code?: string` to `AppError` (constructor accepts an optional 3rd arg; existing callers unaffected). Add `class CoverageExpiredError extends BadRequestError` that sets `code = "COVERAGE_EXPIRED"` and a default message `Necesitás renovar tu membresía para reservar esta clase`. In booking-service.ts reserve(), immediately AFTER the membership gate (§87), compute `coveredUntil = await this.subscriptionService.getCoveredUntil(memberId)` and if `coveredUntil !== null && date > coveredUntil` throw `new CoverageExpiredError()` (string date comparison is safe — both are zero-padded YYYY-MM-DD). `coveredUntil === null` ⇒ never block (D-14). In scheduling/routes.ts /reserve catch, add a branch BEFORE `handleServiceError`: when `err instanceof CoverageExpiredError`, `reply.code(400).send({ error: "Solicitud invalida", message: err.message, code: "COVERAGE_EXPIRED" })` (mirrors the ConflictError/affectedSchedules precedent; the default handler does not emit `code`). Write the integration test covering the five behaviors above, asserting the HTTP body.code on the rejection and 201 on the allowed cases.
  </action>
  <verify>
    <automated>cd el-templo-api && grep -q "COVERAGE_EXPIRED" src/modules/shared/errors.ts && grep -q "CoverageExpiredError" src/modules/scheduling/booking-service.ts && grep -q "COVERAGE_EXPIRED" src/modules/scheduling/routes.ts && test -f test/scheduling-reserve-coverage.test.ts && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `AppError` gains optional `code`; `CoverageExpiredError extends BadRequestError` with `code = "COVERAGE_EXPIRED"`; existing error callers/tests unaffected.
    - reserve() blocks only when `coveredUntil !== null && date > coveredUntil`; scheduled-chain dates allowed; null never blocks.
    - /reserve catch emits `{ code: "COVERAGE_EXPIRED" }` for CoverageExpiredError, before handleServiceError; all other errors unchanged.
    - Integration test covers all five behaviors incl. the no-code regression guard.
    - `tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Server blocks too-late reservations with a distinguishable code; scheduled coverage and NULL pass; other errors untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Booking-block dialog in ReservasPage.vue</name>
  <files>el-templo-app/src/pages/ReservasPage.vue</files>
  <read_first>
    - el-templo-app/src/pages/ReservasPage.vue §618-690 (imports: extractError, buildWhatsAppUrl, userStore), §748 (window.open WhatsApp), §1179-1207 (confirmReserve + catch)
    - el-templo-app/src/components/RatingPromptDialog.vue (scoped charcoal-card styles to reuse)
    - 144-UI-SPEC.md §103-111 (booking-block copy) + §141-146 (interaction contract)
    - 144-PATTERNS.md "ReservasPage.vue — booking-block dialog"
  </read_first>
  <action>
    In confirmReserve's catch, discriminate the coverage error: when `axios.isAxiosError(err) && err.response?.data?.code === 'COVERAGE_EXPIRED'`, set a new `showCoverageDialog` ref to true and RETURN (do NOT also fire the generic notify); ALL other errors keep the existing `extractError` + `$q.notify({ type: 'negative' })` path unchanged. Add a new `q-dialog v-model="showCoverageDialog"` (NOT persistent) styled with the charcoal-card classes reused from RatingPromptDialog (duplicate the scoped block or extract a shared style — do NOT invent new styling per UI-SPEC §143; do NOT use $negative, use the terracotta accent). Title `Necesitás renovar tu membresía`, body from UI-SPEC §108, primary `q-btn` gradient label `Renovar por WhatsApp` → `window.open(buildWhatsAppUrl(userStore.profile?.branchCountry, 'Hola, quiero renovar mi membresía para reservar una clase 💪'), '_blank')`, flat secondary `Entendido` → close. No persistence (re-shows on every too-late retry). Logging via the existing createLogger instance, never console.
  </action>
  <verify>
    <automated>cd el-templo-app && grep -q "COVERAGE_EXPIRED" src/pages/ReservasPage.vue && grep -q "showCoverageDialog" src/pages/ReservasPage.vue && grep -q "para reservar una clase" src/pages/ReservasPage.vue && (npx vue-tsc --noEmit 2>/dev/null || npx tsc --noEmit -p tsconfig.json)</automated>
  </verify>
  <acceptance_criteria>
    - confirmReserve catch opens the new dialog ONLY on `err.response?.data?.code === 'COVERAGE_EXPIRED'`; every other error keeps the existing negative notify path (regression-safe).
    - New q-dialog is NOT persistent, reuses RatingPromptDialog charcoal-card styling (no new styling, no $negative).
    - Title/body/CTA copy match UI-SPEC §103-111; WhatsApp text `Hola, quiero renovar mi membresía para reservar una clase 💪`; secondary `Entendido` closes; no persistence.
    - Typecheck passes; no console.* (uses createLogger).
  </acceptance_criteria>
  <done>App shows renewal dialog with WhatsApp CTA on coverage rejection; other reserve errors unchanged.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                                              |
| -------------------------- | ---------------------------------------------------------------------------------------- |
| app client → POST /reserve | Untrusted client requests a booking; the class date is server-validated against coverage |

## STRIDE Threat Register

| Threat ID | Category                         | Component                  | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                       |
| --------- | -------------------------------- | -------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-144-11  | Elevation / authorization bypass | reserve() coverage check   | mitigate    | covered-until is SERVER-computed via getCoveredUntil(memberId) from the authenticated member's own subscriptions — never read from request input; the check lives in booking-service (server), not the client, so the app dialog is advisory only and cannot be the enforcement point |
| T-144-12  | Tampering                        | client forges covered date | mitigate    | the booking `date` is the only client input; coverage is derived independently server-side, so a forged client value cannot extend coverage                                                                                                                                           |
| T-144-13  | Information disclosure           | error message              | accept      | COVERAGE_EXPIRED message is generic renewal copy; no subscription internals leaked                                                                                                                                                                                                    |
| T-144-14  | Denial of service                | NULL end_date edge         | mitigate    | D-14 guard: coveredUntil null ⇒ never block, so legacy/manual NULL rows cannot lock a member out of all booking                                                                                                                                                                       |
| T-144-SC  | Tampering                        | npm installs               | accept      | no new packages; reuses existing errors + axios + whatsapp helper                                                                                                                                                                                                                     |

</threat_model>

<verification>
- el-templo-api tsc green; scheduling-reserve-coverage integration test passes (CI), covering block / allow / scheduled-chain / NULL / no-code-regression.
- el-templo-app typecheck green.
- Manual (UAT, separate): reserving a class after expiry shows the renewal dialog; WhatsApp opens; an in-coverage class reserves normally.
</verification>

<success_criteria>

- Server blocks too-late presencial reservations with COVERAGE_EXPIRED (scheduled coverage + NULL pass); ReservasPage shows the renewal dialog with WhatsApp CTA only for that code; all other reserve errors unchanged.
  </success_criteria>

<output>
Create `.planning/phases/144-notificaciones-y-bloqueo-de-vencimiento-de-membres-a-plan/144-04-SUMMARY.md` when done.
</output>
