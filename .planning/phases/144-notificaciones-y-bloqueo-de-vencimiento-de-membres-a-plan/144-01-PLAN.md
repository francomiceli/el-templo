---
phase: 144-notificaciones-y-bloqueo-de-vencimiento-de-membres-a-plan
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/schema/notifications.ts
  - el-templo-api/src/modules/notifications/types.ts
  - el-templo-api/src/modules/subscriptions/service.ts
  - el-templo-api/src/db/migrations/*_planes_notification_category.sql
  - el-templo-api/test/subscriptions/covered-until.test.ts
autonomous: false
requirements: [PLAN-NOTIF, PLAN-POPUP, BOOK-BLOCK]
user_setup: []

must_haves:
  truths:
    - "notificationCategoryEnum includes the value 'planes' and a member can silence it independently of entrenamiento/programas"
    - "Three plan-renewal templates (7d / 3d / expired) exist under category 'planes' with female-copy variants"
    - "deriveCoveredUntil(db, userId) returns the furthest end_date across the member's active+scheduled subscription chain, or null when none/all-NULL"
    - "A committed migration alters the enum on notification_templates AND notification_preferences and backfills a 'planes' preference row for every existing member"
  artifacts:
    - path: "el-templo-api/src/db/schema/notifications.ts"
      provides: "planes enum value"
      contains: "planes"
    - path: "el-templo-api/src/modules/notifications/types.ts"
      provides: "planes category + 3 plan_renewal templates"
      contains: "plan_renewal_warning"
    - path: "el-templo-api/src/modules/subscriptions/service.ts"
      provides: "covered-until derivation reused by cron, reserve and popup"
      contains: "deriveCoveredUntil"
    - path: "el-templo-api/src/db/migrations"
      provides: "enum ALTER on both tables + preferences backfill"
      contains: "notification_category"
  key_links:
    - from: "el-templo-api/src/modules/subscriptions/service.ts (getCoveredUntil method)"
      to: "deriveCoveredUntil standalone function"
      via: "thin delegation so cron + booking + routes share one derivation"
      pattern: "deriveCoveredUntil"
---

<objective>
Build the shared foundation every downstream surface depends on: the `planes` notification category + the three renewal templates, the single DRY covered-until derivation, and the migration that lands the enum + preference backfill in the DB.

Purpose: Three consumers (push cron, in-app expiry dialog, reserve coverage check) all need (a) the same "fecha cubierta" rule and (b) the `planes` category to exist in schema and DB. Centralizing them here prevents three divergent copies (CLAUDE.md: flag repetition aggressively).
Output: enum value `planes`, 3 templates, `deriveCoveredUntil`/`getCoveredUntil`, a committed migration, and an integration test for the helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/144-notificaciones-y-bloqueo-de-vencimiento-de-membres-a-plan/144-CONTEXT.md
@.planning/phases/144-notificaciones-y-bloqueo-de-vencimiento-de-membres-a-plan/144-PATTERNS.md

<interfaces>
<!-- Extracted from live code 2026-06-25. Use directly — no exploration needed. -->

el-templo-api/src/db/schema/notifications.ts §16 (shared by templates.category §63 and preferences.category §85):
notificationCategoryEnum = mysqlEnum("notification_category", ["entrenamiento","programas","motivacion","anuncios"])
notification_preferences columns: id, user_id (FK users.id, notNull), category (enum), enabled (boolean default true), unique(user_id, category)

el-templo-api/src/modules/notifications/types.ts:
type NotificationCategory union (§3-18) and NOTIFICATION_CATEGORIES const array
interface TemplateSeed { templateKey; category: NotificationCategory; title; body; titleFemale; bodyFemale; route } // titleFemale/bodyFemale REQUIRED
TEMPLATE_SEEDS: TemplateSeed[] // sibling: program_renewal_warning (category "programas", route "/mi-camino")

el-templo-api/src/modules/subscriptions/service.ts:
schema imported as `import * as schema from "../../db/schema"`
subscriptions.endDate = date("end_date", { mode: "string" }) // "YYYY-MM-DD" string, nullable
subscriptionStatusEnum values include: active, paused, cancelled, expired, completed, changed, scheduled
class SubscriptionService { constructor(private db: MySql2Database<typeof schema>, private log, private auraService, ...) } // heavy DI
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 'planes' category + three renewal templates</name>
  <files>el-templo-api/src/db/schema/notifications.ts, el-templo-api/src/modules/notifications/types.ts</files>
  <read_first>
    - el-templo-api/src/db/schema/notifications.ts §16 (notificationCategoryEnum)
    - el-templo-api/src/modules/notifications/types.ts §3-18 (category union + array), §60-73 (TemplateSeed), §168-177 (program_renewal_warning sibling seed)
    - 144-PATTERNS.md "types.ts — new category + templates" and "notifications.ts + migration" sections
    - 144-UI-SPEC.md §113-121 (push copy contract)
  </read_first>
  <action>
    In notifications.ts add the literal `"planes"` to the `notificationCategoryEnum` array (1st arg stays `"notification_category"` — must match the migration exactly per reference_drizzle_enum_column_name.md). In types.ts add `"planes"` to BOTH the `NotificationCategory` union and the `NOTIFICATION_CATEGORIES` const array. Append three `TEMPLATE_SEEDS` entries, all `category: "planes"`, `route: "/reservas"`: `plan_renewal_warning_7d`, `plan_renewal_warning_3d`, `plan_renewal_warning_expired`. Title constant `Renová tu membresía` for all three. Bodies verbatim from UI-SPEC §117-121 per trigger. Provide ASCII-safe `titleFemale`/`bodyFemale` (no accents/emoji in the female variants, mirroring existing seeds) — these fields are required by TemplateSeed. Do NOT add a tracking column; idempotency is handled by the cron's exact-date band (Plan 02).
  </action>
  <verify>
    <automated>cd el-templo-api && grep -q '"planes"' src/db/schema/notifications.ts && grep -c "plan_renewal_warning" src/modules/notifications/types.ts | grep -qv '^0$' && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `notificationCategoryEnum` array contains `"planes"`; 1st arg unchanged (`"notification_category"`).
    - `NotificationCategory` union AND `NOTIFICATION_CATEGORIES` array both contain `"planes"`.
    - Exactly three new TEMPLATE_SEEDS with keys `plan_renewal_warning_7d|_3d|_expired`, all `category: "planes"`, each with non-empty `titleFemale`/`bodyFemale`.
    - `tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Enum + category + 3 templates compile; bodies match UI-SPEC copy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add covered-until derivation (DRY, reused by all 3 consumers)</name>
  <files>el-templo-api/src/modules/subscriptions/service.ts, el-templo-api/test/subscriptions/covered-until.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/subscriptions/service.ts §150-160 (class + constructor), §568-640 (getMemberSubscription chain query)
    - 144-PATTERNS.md "subscriptions/service.ts — covered-until derivation" section
    - el-templo-api/test/helpers.ts (auth/request/db utilities) and el-templo-api/test/subscriptions/ existing tests for fixture style
  </read_first>
  <behavior>
    - active sub end_date 2026-07-10, no scheduled → deriveCoveredUntil returns "2026-07-10"
    - active end_date 2026-07-10 + scheduled successor end_date 2026-08-09 → returns "2026-08-09" (furthest)
    - member with only cancelled/expired subs → returns null
    - member with no subscriptions → returns null
    - active sub with end_date NULL only → returns null (D-14 guard: never derive a block date from NULL)
  </behavior>
  <action>
    Export a standalone function `deriveCoveredUntil(db: MySql2Database<typeof schema>, userId: number): Promise<string | null>` in service.ts: `SELECT MAX(end_date)` from `subscriptions` filtered to `userId` AND `status IN ('active','scheduled')` AND `end_date IS NOT NULL`; return the date string or null. Add a thin instance method `getCoveredUntil(userId): Promise<string | null>` on `SubscriptionService` that delegates to `deriveCoveredUntil(this.db, userId)` — so booking-service and subscriptions/routes (which hold the service) reuse it, while the cron imports the standalone function (it only has `db`, instantiating the heavy service is undesirable). ONE derivation, three call sites. Write the integration test in test/subscriptions/covered-until.test.ts covering all five behaviors above against the real `eltemplo_test` DB using test/helpers.ts.
  </action>
  <verify>
    <automated>cd el-templo-api && grep -q "export async function deriveCoveredUntil\|export function deriveCoveredUntil" src/modules/subscriptions/service.ts && grep -q "getCoveredUntil" src/modules/subscriptions/service.ts && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `deriveCoveredUntil(db, userId)` exported; `getCoveredUntil(userId)` method delegates to it (no duplicated query body).
    - Query filters `status IN ('active','scheduled')` and `end_date IS NOT NULL`; uses MAX(end_date).
    - test/subscriptions/covered-until.test.ts asserts all five behaviors; runs in CI (not locally — local tsc only per MEMORY).
    - `tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Helper returns furthest chain end_date or null; test covers single/chain/none/NULL cases.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Generate + apply enum migration with preference backfill</name>
  <files>el-templo-api/src/db/migrations/*_planes_notification_category.sql</files>
  <read_first>
    - CLAUDE.md "Database Changes" (db:generate / db:migrate, NEVER drizzle-kit push/migrate, _migrations is source of truth)
    - 144-PATTERNS.md "notifications.ts + migration" CRITICAL block
    - el-templo-api/src/db/schema/notifications.ts (notification_preferences columns: user_id, category, enabled, unique(user_id, category))
    - an existing migration .sql under el-templo-api/src/db/migrations/ for SQL style
  </read_first>
  <what-built>
    Claude runs `pnpm db:generate` to produce the migration for the `planes` enum change, edits it to ALTER the enum on BOTH `notification_templates` AND `notification_preferences`, and appends an idempotent backfill: insert a `planes` preference row (`enabled = true`) for every user who lacks one, e.g. `INSERT INTO notification_preferences (user_id, category, enabled) SELECT id, 'planes', true FROM users u WHERE NOT EXISTS (SELECT 1 FROM notification_preferences p WHERE p.user_id = u.id AND p.category = 'planes')`. No `;` inside `--` comments (the runner splits on `;` before stripping comments). Then applies it with `pnpm db:migrate`.
  </what-built>
  <how-to-verify>
    1. Confirm the migration FILE NUMBER assigned by db:generate does NOT collide with the in-flight v5.2 accounting migrations (0153-0157 live in staging, not yet merged to this branch). Inspect the highest existing number on THIS branch and the assigned number.
    2. Confirm the SQL `MODIFY COLUMN ... enum(...,'planes')` appears for BOTH notification_templates and notification_preferences.
    3. Confirm the backfill INSERT is idempotent (NOT EXISTS guard) and seeds enabled=true.
    4. Confirm the .sql file is staged for commit (executors routinely forget it).
    5. Run `pnpm db:migrate` against eltemplo_test and confirm it applies cleanly and `_migrations` records it.
  </how-to-verify>
  <acceptance_criteria>
    - Generated .sql exists under el-templo-api/src/db/migrations/, ALTERs the enum on both tables, backfills planes prefs idempotently, contains no `;` inside `--` comments.
    - Migration applied via pnpm db:migrate (NOT drizzle-kit migrate); appears in `_migrations`.
    - .sql file staged (git status shows it tracked).
    - Human has confirmed the assigned migration number does not collide with v5.2 (0153-0157).
  </acceptance_criteria>
  <resume-signal>Type "approved" once the migration number is confirmed non-colliding and applied, or describe the collision.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                                                                   |
| --------------------- | --------------------------------------------------------------------------------------------- |
| DB migration → schema | Enum/backfill DDL changes shared notification tables read by every member's preference checks |

## STRIDE Threat Register

| Threat ID | Category               | Component            | Disposition | Mitigation Plan                                                                                                                          |
| --------- | ---------------------- | -------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| T-144-01  | Tampering              | enum migration       | mitigate    | mysqlEnum 1st arg `"notification_category"` must equal column in migration (CI fails "Unknown column" otherwise); MODIFY on both tables  |
| T-144-02  | Denial of Service      | preferences backfill | mitigate    | backfill is idempotent (NOT EXISTS guard) so re-run cannot duplicate rows or violate unique(user_id, category)                           |
| T-144-03  | Information disclosure | deriveCoveredUntil   | accept      | reads only the passed userId's own subscriptions; callers must pass the authenticated member's id (enforced by consumers in Plans 02-04) |
| T-144-SC  | Tampering              | npm/pip installs     | accept      | no new packages installed in this phase (reuses existing notification + drizzle stack)                                                   |

</threat_model>

<verification>
- `npx tsc --noEmit` green for el-templo-api.
- Migration applied to eltemplo_test; `_migrations` row present.
- covered-until integration test passes in CI.
</verification>

<success_criteria>

- `planes` exists in enum (schema + DB on both tables), 3 templates seeded, every member has a planes preference row.
- `deriveCoveredUntil` + `getCoveredUntil` available and tested; no duplicated derivation.
  </success_criteria>

<output>
Create `.planning/phases/144-notificaciones-y-bloqueo-de-vencimiento-de-membres-a-plan/144-01-SUMMARY.md` when done. Record the assigned migration number for downstream reference.
</output>
