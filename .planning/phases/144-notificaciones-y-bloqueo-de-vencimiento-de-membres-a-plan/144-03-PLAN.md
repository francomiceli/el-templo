---
phase: 144-notificaciones-y-bloqueo-de-vencimiento-de-membres-a-plan
plan: 03
type: execute
wave: 2
depends_on: ["144-01"]
files_modified:
  - el-templo-api/src/modules/subscriptions/routes.ts
  - el-templo-api/test/subscriptions/coverage-endpoint.test.ts
  - el-templo-app/src/components/PlanExpiryDialog.vue
  - el-templo-app/src/layouts/MainLayout.vue
autonomous: true
requirements: [PLAN-POPUP]
user_setup: []

must_haves:
  truths:
    - "An authenticated member can fetch their own covered-until via a member-facing endpoint"
    - "On app open, if covered-until is ≤3 days away (incl. 0 = vence hoy), a skippable dialog appears at most once per calendar day"
    - "The dialog's primary CTA opens WhatsApp prefilled with the renewal text via buildWhatsAppUrl; 'Ahora no' closes it"
    - "If coverage extends beyond 3 days (renovación agendada) the dialog does NOT appear (D-10 suppression)"
  artifacts:
    - path: "el-templo-app/src/components/PlanExpiryDialog.vue"
      provides: "skippable expiry dialog cloned from RatingPromptDialog"
      min_lines: 80
    - path: "el-templo-api/src/modules/subscriptions/routes.ts"
      provides: "GET /subscriptions/coverage member endpoint"
      contains: "coveredUntil"
    - path: "el-templo-app/src/layouts/MainLayout.vue"
      provides: "mount point"
      contains: "PlanExpiryDialog"
  key_links:
    - from: "PlanExpiryDialog.vue"
      to: "GET /subscriptions/coverage"
      via: "fetch covered-until on auth"
      pattern: "coverage"
    - from: "PlanExpiryDialog.vue"
      to: "buildWhatsAppUrl"
      via: "primary CTA"
      pattern: "buildWhatsAppUrl"
---

<objective>
Surface the in-app expiry reminder: a member-facing covered-until endpoint plus a skippable, once-per-day `PlanExpiryDialog.vue` that fires at ≤3 days and pushes the member to renew via WhatsApp.

Purpose: PLAN-POPUP entregable 2 (D-06..D-10).
Output: GET /subscriptions/coverage (+ test), PlanExpiryDialog.vue (clone of RatingPromptDialog), mounted in MainLayout.vue.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/144-notificaciones-y-bloqueo-de-vencimiento-de-membres-a-plan/144-CONTEXT.md
@.planning/phases/144-notificaciones-y-bloqueo-de-vencimiento-de-membres-a-plan/144-PATTERNS.md
@.planning/phases/144-notificaciones-y-bloqueo-de-vencimiento-de-membres-a-plan/144-UI-SPEC.md
@.planning/phases/144-notificaciones-y-bloqueo-de-vencimiento-de-membres-a-plan/144-01-SUMMARY.md

<interfaces>
<!-- Extracted from live code 2026-06-25. -->
el-templo-api/src/modules/subscriptions/service.ts (Plan 01): getCoveredUntil(userId) method + deriveCoveredUntil(db, userId); covered-until is "YYYY-MM-DD" or null.
el-templo-api/src/modules/subscriptions/routes.ts: registers member routes via `fastify.get<...>(...)`; existing self-scoped routes read the authenticated user id from the request auth context — follow the SAME auth pattern used by the existing "my subscription" route (do not invent a new auth scheme).
el-templo-app/src/components/RatingPromptDialog.vue: q-dialog v-model="show" (NO persistent); .rating-dialog scoped styles (§163-237) — charcoal #2e2a26 card, terracotta gradient primary, flat cream-55 secondary, max-width 340px. Trigger: watch(authStore.isAuthenticated, { immediate: true }).
el-templo-app/src/components/PushPermissionDialog.vue §48-50: versioned Capacitor Preferences key convention (`push_permission_asked_v1`).
el-templo-app/src/utils/whatsapp.ts §28-32: buildWhatsAppUrl(country: 'AR'|'ES'|null|undefined, text?) → wa.me URL (defaults AR on null).
el-templo-app/src/stores/useAuthStore.ts: exposes `isAuthenticated` (NO `country` field).
el-templo-app/src/stores/useUserStore.ts §36: `profile?.branchCountry: 'AR'|'ES'` ← USE THIS for country (UI-SPEC said authStore.country, which does not exist — PATTERNS flagged the correction).
el-templo-app/src/layouts/MainLayout.vue §135-150: where PushPermissionDialog + RatingPromptDialog are mounted and imported.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GET /subscriptions/coverage member endpoint</name>
  <files>el-templo-api/src/modules/subscriptions/routes.ts, el-templo-api/test/subscriptions/coverage-endpoint.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/subscriptions/routes.ts (the existing self-scoped "my subscription" route — copy its auth pattern for reading the current member id)
    - 144-01-SUMMARY.md (getCoveredUntil method confirmed)
    - el-templo-api/test/helpers.ts + el-templo-api/test/subscriptions/ for auth-request fixture style
  </read_first>
  <action>
    Register an authenticated member route `GET /subscriptions/coverage` that resolves the current member id from the SAME auth context the existing self route uses (do not accept a userId from the request — server-derived only), calls `subscriptionService.getCoveredUntil(memberId)`, and returns `{ coveredUntil: string | null, daysRemaining: number | null }` where daysRemaining = whole days between today (AR date) and coveredUntil, or null when coveredUntil is null. Write an integration test asserting: member with active end_date today+2 → coveredUntil set, daysRemaining 2; member with scheduled extending to today+33 → daysRemaining 33; member with no coverage → both null; and that the route requires auth and never reads a userId from input.
  </action>
  <verify>
    <automated>cd el-templo-api && grep -q "coveredUntil" src/modules/subscriptions/routes.ts && test -f test/subscriptions/coverage-endpoint.test.ts && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `GET /subscriptions/coverage` registered, auth-gated, member id server-derived (no userId in request).
    - Returns `{ coveredUntil, daysRemaining }`; null-safe.
    - Integration test covers active / scheduled-extended / no-coverage and the auth requirement.
    - `tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Member can read their own covered-until + daysRemaining; cannot query another member.</done>
</task>

<task type="auto">
  <name>Task 2: Create PlanExpiryDialog.vue (skippable, once-per-day)</name>
  <files>el-templo-app/src/components/PlanExpiryDialog.vue</files>
  <read_first>
    - el-templo-app/src/components/RatingPromptDialog.vue (FULL — clone structure + scoped styles verbatim)
    - el-templo-app/src/components/PushPermissionDialog.vue §48-50 (versioned Preferences key)
    - el-templo-app/src/utils/whatsapp.ts (buildWhatsAppUrl)
    - el-templo-app/src/stores/useUserStore.ts §36 (profile.branchCountry) + useAuthStore.ts (isAuthenticated)
    - 144-UI-SPEC.md §92-101 (copy) + §129-139 (interaction contract)
    - 144-PATTERNS.md "PlanExpiryDialog.vue — NEW" deltas
  </read_first>
  <action>
    Clone RatingPromptDialog.vue: `<q-dialog v-model="show">` WITHOUT `persistent` (D-07 skippable). Copy the `.rating-dialog*` scoped style block verbatim (do NOT restyle). Body: a single title `Tu membresía está por vencer`, a dynamic body computed from `daysRemaining` per UI-SPEC §97-98 (use `1 día` singular when N=1; the `vence hoy` variant when N=0), primary `q-btn` gradient label `Renovar por WhatsApp`, flat secondary label `Ahora no`. `shouldShow()` gate (D-06/D-08/D-10): require `authStore.isAuthenticated`; GET `/subscriptions/coverage`; show only when `daysRemaining !== null && daysRemaining <= 3` AND not already shown today. Persist "shown today" as a `YYYY-MM-DD` string under a versioned Capacitor `Preferences` key `plan_expiry_shown_v1`; `shouldShow` returns false if the stored date equals today (local calendar day). Trigger via `watch(() => authStore.isAuthenticated, (v) => { if (v) void evaluate() }, { immediate: true })`, mirroring the siblings. Primary CTA: open `buildWhatsAppUrl(userStore.profile?.branchCountry, 'Hola, quiero renovar mi membresía 💪')` via the same external-open mechanism the app uses (`window.open(url, '_blank')`), then record shown-today + close. `Ahora no`: record shown-today + close. Use `createLogger()` for any logging, never console. No props (self-triggering).
  </action>
  <verify>
    <automated>cd el-templo-app && test -f src/components/PlanExpiryDialog.vue && grep -q "buildWhatsAppUrl" src/components/PlanExpiryDialog.vue && grep -q "plan_expiry_shown_v1" src/components/PlanExpiryDialog.vue && grep -Lq "persistent" src/components/PlanExpiryDialog.vue && npx vue-tsc --noEmit 2>/dev/null || npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - q-dialog has NO `persistent`; scoped styles copied from RatingPromptDialog (charcoal #2e2a26, terracotta gradient, max-width 340px).
    - Gate: authenticated AND daysRemaining ≤ 3 AND not shown today; once-per-day via `plan_expiry_shown_v1` storing YYYY-MM-DD.
    - Country from `userStore.profile?.branchCountry` (NOT authStore.country); CTA text `Hola, quiero renovar mi membresía 💪`.
    - Singular `1 día` and `vence hoy` (N=0) copy variants present.
    - Typecheck passes; no console.* (uses createLogger).
  </acceptance_criteria>
  <done>Dialog fires at ≤3d once/day, opens WhatsApp, suppresses when coverage extends.</done>
</task>

<task type="auto">
  <name>Task 3: Mount PlanExpiryDialog in MainLayout</name>
  <files>el-templo-app/src/layouts/MainLayout.vue</files>
  <read_first>
    - el-templo-app/src/layouts/MainLayout.vue §135-150 (PushPermissionDialog + RatingPromptDialog mount + imports)
    - 144-PATTERNS.md "MainLayout.vue — mount point"
  </read_first>
  <action>
    Add `<PlanExpiryDialog />` next to the existing `<PushPermissionDialog />` / `<RatingPromptDialog />` and add the matching `import PlanExpiryDialog from 'src/components/PlanExpiryDialog.vue'`. No props — it self-triggers via its own auth watcher, exactly like the siblings.
  </action>
  <verify>
    <automated>cd el-templo-app && grep -q "PlanExpiryDialog" src/layouts/MainLayout.vue && (npx vue-tsc --noEmit 2>/dev/null || npx tsc --noEmit -p tsconfig.json)</automated>
  </verify>
  <acceptance_criteria>
    - `<PlanExpiryDialog />` rendered and imported in MainLayout.vue beside the sibling dialogs.
    - Typecheck passes.
  </acceptance_criteria>
  <done>Dialog is mounted app-wide and self-triggers on auth.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                               |
| ---------------------------------------- | ----------------------------------------- |
| app client → GET /subscriptions/coverage | Member requests their own coverage status |

## STRIDE Threat Register

| Threat ID | Category               | Component               | Disposition | Mitigation Plan                                                                                                                              |
| --------- | ---------------------- | ----------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| T-144-08  | Elevation / IDOR       | /subscriptions/coverage | mitigate    | member id is server-derived from the auth context; the route MUST NOT accept a userId param — a member can only read their own covered-until |
| T-144-09  | Information disclosure | covered-until response  | accept      | response carries only the member's own date + day count; no PII beyond what the member already sees                                          |
| T-144-10  | Spoofing               | unauthenticated fetch   | mitigate    | route is auth-gated like the existing self routes; dialog gates on authStore.isAuthenticated before fetching                                 |
| T-144-SC  | Tampering              | npm installs (app)      | accept      | no new packages; reuses @capacitor/preferences + existing whatsapp helper                                                                    |

</threat_model>

<verification>
- el-templo-api tsc green; coverage-endpoint integration test passes (CI).
- el-templo-app typecheck green.
- Manual (UAT, separate): dialog appears at ≤3d once/day, WhatsApp opens, hidden when renewed.
</verification>

<success_criteria>

- Member endpoint returns own covered-until; PlanExpiryDialog fires ≤3d once/day, skippable, WhatsApp CTA, suppressed when coverage extends; mounted app-wide.
  </success_criteria>

<output>
Create `.planning/phases/144-notificaciones-y-bloqueo-de-vencimiento-de-membres-a-plan/144-03-SUMMARY.md` when done.
</output>
