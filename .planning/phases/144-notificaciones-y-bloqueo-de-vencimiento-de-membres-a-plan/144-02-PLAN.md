---
phase: 144-notificaciones-y-bloqueo-de-vencimiento-de-membres-a-plan
plan: 02
type: execute
wave: 2
depends_on: ["144-01"]
files_modified:
  - el-templo-api/src/jobs/notification-cron.ts
  - el-templo-api/test/notification-plan-renewal.test.ts
autonomous: true
requirements: [PLAN-NOTIF]
user_setup: []

must_haves:
  truths:
    - "A daily 03:00 AR cron enqueues a Planes push when the member's covered-until equals today+7, today+3, or today"
    - "Each threshold fires exactly once per renewal cycle (exact-date band = built-in idempotency, no tracking column)"
    - "A member with a scheduled successor extending coverage beyond the threshold date receives NO push (D-05 suppression)"
    - "A member who silenced category Planes receives NO push (queueNotification per-category preference gate)"
  artifacts:
    - path: "el-templo-api/src/jobs/notification-cron.ts"
      provides: "Plan Renewal Warning block (runPlanRenewalWarnings)"
      contains: "plan_renewal_warning"
    - path: "el-templo-api/test/notification-plan-renewal.test.ts"
      provides: "window + suppression coverage"
      exports: []
  key_links:
    - from: "el-templo-api/src/jobs/notification-cron.ts"
      to: "deriveCoveredUntil (subscriptions/service.ts)"
      via: "per-candidate suppression check"
      pattern: "deriveCoveredUntil"
    - from: "runPlanRenewalWarnings"
      to: "notificationService.queueNotification"
      via: "enqueue into pending_notifications"
      pattern: "queueNotification"
---

<objective>
Add the "Plan Renewal Warning" cron block: a daily job that enqueues a Planes push at 7d / 3d / expiry-day before the member's covered-until, suppressing anyone already renewed.

Purpose: PLAN-NOTIF entregable 1 — paridad con el aviso de renovación de programas, llevado a la cadena de subscriptions.
Output: testable `runPlanRenewalWarnings`, wired into the existing 03:00 AR batch cron, plus an integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/144-notificaciones-y-bloqueo-de-vencimiento-de-membres-a-plan/144-CONTEXT.md
@.planning/phases/144-notificaciones-y-bloqueo-de-vencimiento-de-membres-a-plan/144-PATTERNS.md
@.planning/phases/144-notificaciones-y-bloqueo-de-vencimiento-de-membres-a-plan/144-01-SUMMARY.md

<interfaces>
<!-- Extracted from live code 2026-06-25. -->
el-templo-api/src/jobs/notification-cron.ts:
  imports: cron(node-cron), pino, { eq, and, sql, isNotNull, gte, lte } from drizzle-orm, * as s from "../db/schema", NotificationService
  const log = pino({ name: "notification-cron" })
  startNotificationJobs(db): the 03:00 AR batch cron is registered via cron.schedule("0 3 * * *", ...) at §211 and instantiates `new NotificationService(db, log)`; the "Program Renewal Warning" molde block is §346-398 inside it.
el-templo-api/src/modules/notifications/service.ts §225:
  queueNotification({ userId, templateKey, titleOverride?, bodyOverride?, ... }): Promise<number>
  — already does: template lookup, isEnabled skip, PER-CATEGORY preference check (skips if member silenced the category), no-device-token skip, gender copy, insert into pending_notifications.
el-templo-api/src/modules/subscriptions/service.ts (from Plan 01):
  export deriveCoveredUntil(db, userId): Promise<string | null>  // MAX(end_date) over active+scheduled, null when none/NULL
subscriptions.endDate = date(... { mode: "string" }) → "YYYY-MM-DD"; status enum includes active, scheduled.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement runPlanRenewalWarnings cron block</name>
  <files>el-templo-api/src/jobs/notification-cron.ts</files>
  <read_first>
    - el-templo-api/src/jobs/notification-cron.ts §186-215 (startNotificationJobs, 03:00 batch cron) and §346-398 (Program Renewal Warning molde)
    - 144-PATTERNS.md "notification-cron.ts — new Plan Renewal Warning block" (Deltas + idempotency note) and "Notification enqueue" shared pattern
    - 144-01-SUMMARY.md (template keys + deriveCoveredUntil signature confirmed)
  </read_first>
  <action>
    Add an exported async function `runPlanRenewalWarnings(db: MySql2Database<typeof schema>, notificationService: NotificationService): Promise<number>` and call it inside the existing 03:00 AR batch cron callback (§211), right after the Program Renewal Warning block, wrapped in the same defensive outer try/catch that logs via `log.error`/`log.info` and never throws. For each threshold in `[{ days: 7, key: "plan_renewal_warning_7d" }, { days: 3, key: "plan_renewal_warning_3d" }, { days: 0, key: "plan_renewal_warning_expired" }]`: compute `targetDate = CURDATE() + days` (use a SQL `DATE` comparison, not JS Date math, to stay in the AR-local DATE domain that matches `end_date`). Select DISTINCT `userId` from `subscriptions` where `status IN ('active','scheduled')` AND `end_date = targetDate`. For each candidate userId call `deriveCoveredUntil(db, userId)`; ONLY enqueue if the returned covered-until `=== targetDate` (this is the D-05 suppression: a member whose chain MAX is beyond the threshold — i.e. has a scheduled successor — is skipped because deriveCoveredUntil returns the further date, not targetDate). Enqueue via `notificationService.queueNotification({ userId, templateKey: key })`; the per-category preference gate inside queueNotification handles the Planes opt-out (D-02). Per-item try/catch `log.warn` + continue, exactly like the molde. The exact-date band IS the idempotency mechanism — do NOT add a tracking column. Use `catch (err: unknown)` + `instanceof Error`; never `console.log`.
  </action>
  <verify>
    <automated>cd el-templo-api && grep -q "runPlanRenewalWarnings" src/jobs/notification-cron.ts && grep -q "deriveCoveredUntil" src/jobs/notification-cron.ts && grep -Eq "plan_renewal_warning_(7d|3d|expired)" src/jobs/notification-cron.ts && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `runPlanRenewalWarnings` exported and invoked inside the 03:00 cron after the program-renewal block, in a defensive try/catch.
    - Three thresholds mapped to the three template keys; candidate query filters `status IN ('active','scheduled')` and `end_date = targetDate`.
    - Suppression: enqueue gated on `deriveCoveredUntil(db, userId) === targetDate`.
    - No tracking column added; no `console.log`; `catch (err: unknown)` + `instanceof Error`.
    - `tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Cron block enqueues correct template per threshold, suppresses renewed members, never throws.</done>
</task>

<task type="auto">
  <name>Task 2: Integration test for windows + suppression + opt-out</name>
  <files>el-templo-api/test/notification-plan-renewal.test.ts</files>
  <read_first>
    - el-templo-api/test/notifications.test.ts (queue/preference assertions + fixture style)
    - el-templo-api/test/helpers.ts (db + user/subscription seeding utilities)
    - 144-PATTERNS.md "Notification enqueue" shared pattern (pending_notifications is the assertion target)
  </read_first>
  <action>
    Test `runPlanRenewalWarnings(db, notificationService)` against eltemplo_test. Cases: (a) member with active end_date = today+7 → one pending_notifications row with templateKey `plan_renewal_warning_7d`; (b) today+3 → `_3d`; (c) today (expiry day) → `_expired`; (d) member with active end_date = today+3 BUT a scheduled successor end_date = today+33 → NO row enqueued (suppression); (e) member at today+3 who has silenced category `planes` (preference enabled=false) → NO row enqueued; (f) member at today+5 (outside all bands) → NO row. Assert on `pending_notifications` rows for the seeded users. Seed dates with SQL DATE arithmetic so the test is timezone-stable.
  </action>
  <verify>
    <automated>cd el-templo-api && test -f test/notification-plan-renewal.test.ts && grep -q "runPlanRenewalWarnings" test/notification-plan-renewal.test.ts && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists and imports `runPlanRenewalWarnings`.
    - Covers all six cases (7d / 3d / expiry enqueue; scheduled-suppression; opt-out; out-of-band) asserting on pending_notifications.
    - `tsc --noEmit` passes (suite itself runs in CI).
  </acceptance_criteria>
  <done>Six-case integration test compiles and asserts enqueue/suppression/opt-out behavior.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                    |
| ------------------------------ | -------------------------------------------------------------- |
| cron (server) → member devices | Batch job enqueues push messages addressed to specific members |

## STRIDE Threat Register

| Threat ID | Category               | Component              | Disposition | Mitigation Plan                                                                                                                                                     |
| --------- | ---------------------- | ---------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-144-04  | Information disclosure | runPlanRenewalWarnings | mitigate    | each queueNotification is keyed by the candidate's own userId from their own subscription row — no cross-member fan-out; covered-until derived per that same userId |
| T-144-05  | Repudiation / spam     | per-threshold re-send  | mitigate    | exact-date band (end_date = today+N) fires each threshold once per cycle; no daily re-spam, no tracking table to drift                                              |
| T-144-06  | Tampering              | opt-out bypass         | mitigate    | enqueue routes through queueNotification, which honors notification_preferences for category planes (D-02) — cron does not write pending_notifications directly     |
| T-144-07  | Denial of Service      | cron throws            | mitigate    | outer + per-item try/catch log-and-continue (molde §346-398); a bad row cannot abort the batch                                                                      |

</threat_model>

<verification>
- `npx tsc --noEmit` green.
- Integration test asserts the three windows enqueue, scheduled-chain suppresses, opt-out suppresses (CI).
</verification>

<success_criteria>

- 7d/3d/expiry pushes enqueue under category Planes; renewed (scheduled) and opted-out members suppressed; cron never throws.
  </success_criteria>

<output>
Create `.planning/phases/144-notificaciones-y-bloqueo-de-vencimiento-de-membres-a-plan/144-02-SUMMARY.md` when done.
</output>
