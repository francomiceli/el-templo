---
phase: 98-multi-currency-and-country-scoped-plans
plan: 12
type: execute
wave: 7
depends_on: ["07", "08", "09", "10", "11"]
files_modified:
  - .planning/phases/98-multi-currency-and-country-scoped-plans/98-UAT.md
autonomous: false
requirements:
  - REQ-98-11
  - REQ-98-07
  - REQ-98-08
  - REQ-98-09
  - REQ-98-10

must_haves:
  truths:
    - "Currently-deployed iOS production build run against staging API displays no price crashes"
    - "Currently-deployed Android production build run against staging API displays no price crashes"
    - "Admin staging verification confirms owner Argentina/España selector works on PlanesPage, CajaPage, ReportesPage, AnaliticasPage, FinanzasTab"
    - "Admin staging verification confirms AR admin sees no selector and only AR data"
    - "Creating a member in the BCN branch in admin staging shows only ES plans in the plan dropdown"
    - "Cross-country plan assignment via admin UI triggers a visible Spanish error and keeps the dialog open"
    - "Exports download with Moneda column populated"
    - "UAT findings documented in 98-UAT.md for the final /gsd-verify-phase hand-off"
    - "If iOS OR Android deployed-build verification against staging is infeasible, the phase HALTS and the user is escalated — NO degrade-gracefully workaround per D-19"
  artifacts:
    - path: ".planning/phases/98-multi-currency-and-country-scoped-plans/98-UAT.md"
      provides: "User acceptance testing record — manual verification results"
      contains: "UAT"
  key_links:
    - from: "deployed production app (iOS/Android)"
      to: "staging API with 0091 migration applied"
      via: "developer's physical device + TestFlight/Play Internal Testing OR installed APK"
      pattern: "staging.*api\\|eltemplo.org"
---

<objective>
Manual verification gate per REQ-98-11 (app forward-compat) and the UX acceptance criteria in SPEC.md. Developer runs the currently-deployed iOS and Android apps against staging API, exercises every visible price screen, and logs outcomes. Admin app manual verification covers owner selector visibility, cross-country error messaging, and export file content. Results go in `98-UAT.md` as a pre-merge gate.

Purpose: Automated tests (Plan 11) prove server correctness AND include forward-compat superset assertions for /plans/catalog and /me/subscription. This plan proves end-user experience: deployed app versions still work when actually installed and run, admin UI flows match the spec's acceptance criteria, and Spanish error copy renders as intended.

**Per D-19 this verification is NON-NEGOTIABLE before master merge.** If the currently-deployed iOS or Android production build cannot be actually tested against the staging API, this plan HALTS the phase and escalates to the user. There is no "degrade gracefully" path — substituting automated tests, server-side-only checks, or a promoted staging candidate for real deployed-build verification is explicitly FORBIDDEN.

Output: Signed-off 98-UAT.md with each acceptance criterion checked or blocked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/98-multi-currency-and-country-scoped-plans/98-SPEC.md
@.planning/phases/98-multi-currency-and-country-scoped-plans/98-CONTEXT.md

<acceptance_criteria_from_spec>
From SPEC.md Acceptance Criteria list (final 13 bullets):

1. Owner logged into PlanesPage sees Argentina/España selector (default Argentina); no "Todos" mode
2. AR admin sees no selector, only AR plans; ES admin sees only ES plans
3. Same selector appears on CajaPage, ReportesPage, AnaliticasPage, FinanzasTab for owners only
4. Creating a member in BCN branch only shows ES plans in plan dropdown
5. API returns 400 assigning AR plan to ES member (covered by Plan 11 automated test — reconfirm manually via UI)
6. API returns 400 recording EUR payment on ARS sub (automated, reconfirm via UI if possible)
7. Non-owner cannot read other country's data (automated in Plan 11 cross-country member read test + this plan's admin UI reconfirmation)
8. Every price display shows correct currency symbol for EUR vs ARS plans
9. Financial reports / caja / exports show ARS and EUR separately; exports have Moneda column
10. Currently-deployed production app build does not crash against the new API — MUST be verified on real device, no substitution allowed
11. Existing AR integration tests pass (automated — Plan 11)
    </acceptance_criteria_from_spec>
    </context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1 [BLOCKING — human required]: App forward-compat verification on real devices</name>
  <read_first>
    - .planning/phases/98-multi-currency-and-country-scoped-plans/98-SPEC.md (Acceptance Criteria list)
    - .planning/phases/98-multi-currency-and-country-scoped-plans/98-CONTEXT.md D-19 (non-negotiable verification)
    - CLAUDE.md (staging-first workflow)
  </read_first>
  <what-built>
    Plans 01-11 produced: migrated DB schema, server-side country scoping + cross-country validation, admin country selector on PlanesPage + all reports pages, member app price migration, integration test suite (including automated forward-compat superset assertions for /plans/catalog and /me/subscription). Staging is now carrying Phase 98 changes. Production app builds (iOS + Android) are NOT yet updated — this task verifies the deployed production builds still work against staging.

    Per D-19: deployed iOS + Android must be verified on real devices before promoting master.

  </what-built>
  <how-to-verify>
    **Environment setup:**
    1. Confirm staging API is on latest branch with 0091 migration applied:
       ```bash
       ssh staging-api (or equivalent) 'mysql -uroot eltemplo_staging -e "SELECT name FROM _migrations WHERE name LIKE '\''0091%'\'';"'
       ```
       (ASK the user before SSHing — per memory `always_ask_before_ssh`.)

    2. Confirm currently-deployed production app versions (from App Store Connect / Play Console) — record version numbers in 98-UAT.md.

    **iOS verification:**
    3. Install the current PRODUCTION iOS app (from App Store, not TestFlight) on a physical device or simulator with production build signature.
    4. Configure the device to hit STAGING API. Options:
       - If the app has a dev menu for API URL swap, use it.
       - Otherwise, point DNS or use a Charles-proxy override to redirect `api.eltemplo.org` → `staging-api.eltemplo.org`.

    **HALT condition (D-19, non-negotiable) — no graceful degradation:**

    If neither of the above options is feasible (no dev menu AND no proxy available AND no DNS override possible), **STOP HERE**. Do NOT sign off with a workaround. Do NOT substitute:
       - running the app against production API and assuming forward-compat
       - running automated tests (Plan 11) as a stand-in for real-device verification
       - promoting a staging candidate to master "because staging worked"
       - running the LATEST-IN-DEV app (not the currently-deployed build) against staging

    Escalate to the user with:
       - exact reason the deployed build cannot be pointed at staging
       - proposed remediation (e.g., "add dev menu for URL swap in next release", "set up Charles proxy on test device", "add a staging-only flavor to the build pipeline")

    Per D-19, this verification is NON-NEGOTIABLE before master promotion. The phase does not ship without it.

    5. (Only reachable when HALT condition is NOT triggered.) Exercise these member-app screens, recording pass/fail for each:
       - [ ] Login → Home (no crash, no missing-field errors)
       - [ ] Plan catalog (PlanesPage) — AR member sees AR plans with `$` prices; no JS error, no blank badges
       - [ ] Mi Camino / current subscription screen (any price visible)
       - [ ] Upsell banner (if one shows)
       - [ ] QR check-in flow (regression — not price-related but high-traffic)
       - [ ] Booking flow (if accessible to this member)

    **Android verification:**
    6. Repeat step 5 with the current PRODUCTION Android APK (from Play Store). Apply the same HALT condition as iOS — no degrade-gracefully substitution.

    **Admin app verification:**
    7. Open staging admin app in browser as each role:

       **As owner:**
       - [ ] PlanesPage: Argentina/España QSelect visible at top, defaults to "Argentina"
       - [ ] Switching to España: table re-fetches, shows 12 ES plans with €-prefixed prices
       - [ ] Switching back to Argentina: table re-fetches, shows AR plans with $-prefixed prices
       - [ ] CajaPage: same selector visible; switching changes summary totals
       - [ ] ReportesPage: same selector; each tab respects it
       - [ ] AnaliticasPage: same selector; charts re-render with the right currency
       - [ ] Open PlanFormDialog in create mode, country=ES, submit → new ES plan visible in Spain list
       - [ ] Attempt to assign an AR plan to a BCN-branch member via AssignPlanDialog → either (a) plan doesn't appear in dropdown (UI pre-filter), OR (b) if forced via devtools, server returns 400 and admin sees Spanish "El plan no corresponde al pais de la sucursal" notification, dialog stays open
       - [ ] Create member in a BCN branch via MemberFormDialog → plan dropdown shows only 12 ES plans
       - [ ] Download a charge-history export from ReportesPage with Spain filter → .xlsx opens with "Moneda" column showing "EUR" on every row

       **As AR admin (non-owner):**
       - [ ] PlanesPage: NO selector visible, list shows only AR plans
       - [ ] CajaPage, ReportesPage, AnaliticasPage: no selectors, all data is AR-scoped
       - [ ] Attempt to manually visit `/admin/subscriptions/plans?country=ES` via browser URL (owner-only param) → server IGNORES the query; returns AR plans only

       **As ES admin (non-owner):**
       - [ ] PlanesPage: NO selector; shows only ES plans
       - [ ] CajaPage: totals are €-formatted (or "0" if no ES payments exist yet — expected state per production baseline)

    **Log UAT results in `98-UAT.md`:**

    Create the file with this template:
    ```markdown
    # Phase 98 UAT — Multi-currency + country-scoped plans

    **Date:** <YYYY-MM-DD>
    **Tester:** <name>
    **Staging API:** <git SHA / branch>
    **iOS app version tested:** <App Store version>
    **Android app version tested:** <Play Store version>

    ## Forward compat (REQ-98-11) — iOS
    - [ ] Home — pass/fail + notes
    - [ ] Plan catalog — pass/fail + notes
    - ... (all 5-6 screens)

    ## Forward compat — Android
    - [ ] (same list)

    ## Admin owner checks
    - [ ] PlanesPage selector + ES toggle
    - ...

    ## Admin AR admin checks
    - ...

    ## Admin ES admin checks
    - ...

    ## HALT record (if applicable)
    - If the phase halted per D-19 because deployed-build verification was infeasible,
      document the exact blocker here AND the remediation plan. DO NOT sign off below until
      the blocker is resolved and verification completes.

    ## Failures / issues found
    <describe each; link to a hotfix PR if created>

    ## Sign-off
    - [ ] All acceptance criteria verified pass OR documented workaround (workarounds NOT permitted for REQ-98-11 per D-19 — any unresolved REQ-98-11 item = no sign-off)
    - [ ] Safe to promote to master
    ```

    If ANY item fails, DO NOT sign off. Return to the relevant plan (07/08/09/10 for UI, 03-06 for server) and fix.

    If the deployed-build verification HALT was triggered, DO NOT sign off until it is resolved — this is non-negotiable per D-19.

  </how-to-verify>
  <acceptance_criteria>
    - `.planning/phases/98-multi-currency-and-country-scoped-plans/98-UAT.md` exists
    - File contains sections for iOS forward-compat, Android forward-compat, owner admin checks, AR admin checks, ES admin checks, HALT record, failures/issues, sign-off
    - Every SPEC acceptance criterion (1-11 from context above) has a corresponding checkbox entry in the UAT file
    - Sign-off section either has "Safe to promote to master" checked OR lists blocking issues with a plan to resolve
    - iOS + Android production-build versions tested are recorded by number (not "current" — explicit e.g. `v1.4.2 (build 142)`)
    - If HALT was triggered: the HALT record section documents the blocker and remediation plan, and sign-off is explicitly BLOCKED (no "Safe to promote to master" check)
    - No substitution of automated tests, staging candidate, or non-deployed builds is recorded in lieu of the deployed-build check (any such substitution = sign-off invalid)
  </acceptance_criteria>
  <resume-signal>
    Type "approved" when the UAT file is populated with results and sign-off is either complete OR failures are documented with an explicit remediation plan. If the HALT condition was triggered, do NOT type "approved" — escalate to the user for a remediation decision first, then resume only when verification can actually complete on real deployed builds.
  </resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                                  |
| ----------------------------------- | -------------------------------------------------------------------------------------------- |
| Staging deploy → master             | Manual verification required before promote                                                  |
| Deployed app → new API              | Forward-compat must hold; app cannot be hot-patched                                          |
| D-19 non-negotiable gate → sign-off | No degrade-gracefully substitution; HALT is the only alternative to real-device verification |

## STRIDE Threat Register

| Threat ID | Category               | Component                                           | Disposition | Mitigation Plan                                                                                                                                     |
| --------- | ---------------------- | --------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-98-22   | Denial of Service      | Deployed app crashes on new API response            | mitigate    | Plan 10 added `?? 'ARS'` fallback; this plan verifies against real installed production builds; Plan 11 automated superset tests catch it pre-merge |
| T-98-23   | Repudiation            | Silent UX regression shipped                        | mitigate    | UAT file documents every screen + owner/admin role tested; sign-off is explicit, not implicit                                                       |
| T-98-24   | Elevation of Privilege | Non-owner uses URL-manipulation to bypass selector  | mitigate    | Manual verification confirms server ignores non-owner `?country=` override (reconfirms automated test T-98-02 at the UI layer)                      |
| T-98-29   | Repudiation            | "Good enough" sign-off without deployed-build check | mitigate    | D-19 explicitly forbids degrade-gracefully; HALT condition in how-to-verify blocks sign-off if verification is infeasible                           |

</threat_model>

<verification>
- 98-UAT.md exists and is populated
- iOS + Android version numbers recorded
- Every SPEC acceptance criterion has a checked box with pass/fail outcome
- Sign-off section complete
- HALT record section is either empty (verification completed) or explicitly blocks sign-off with a remediation plan
</verification>

<success_criteria>

- Manual verification completed on iOS + Android production builds against staging API (or phase HALTED with documented blocker, no workaround)
- All admin-UI acceptance criteria visually verified at staging
- Failures (if any) documented with remediation
- 98-UAT.md committed alongside other phase artifacts
  </success_criteria>

<output>
The UAT file `.planning/phases/98-multi-currency-and-country-scoped-plans/98-UAT.md` IS the output. Also create `.planning/phases/98-multi-currency-and-country-scoped-plans/98-12-SUMMARY.md` pointing to UAT file and summarizing the pass/fail counts. If HALT was triggered, the SUMMARY must explicitly state that the phase cannot promote to master until the blocker is resolved.
</output>
</content>
</invoke>
