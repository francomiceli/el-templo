---
phase: 98-multi-currency-and-country-scoped-plans
plan: 10
type: execute
wave: 5
depends_on: ["05"]
files_modified:
  - el-templo-app/src/modules/plan/pages/PlanesPage.vue
autonomous: true
requirements:
  - REQ-98-09
  - REQ-98-11
  - REQ-98-05

must_haves:
  truths:
    - "Member app plan catalog displays only the member's branch country's plans (enforced server-side by Plan 05)"
    - "Every price display in the member app uses formatPrice(amount, currency ?? 'ARS')"
    - "Response reads use `plan.currency ?? 'ARS'` fallback per D-19 for forward-compatibility with older cached responses"
    - "Hard enumeration of price-toLocaleString sites in el-templo-app/src (grep performed during planning): both hits are in PlanesPage.vue lines 58 and 201 — no other .vue/.ts file contains a price-context toLocaleString"
  artifacts:
    - path: "el-templo-app/src/modules/plan/pages/PlanesPage.vue"
      provides: "Member plan catalog with formatPrice migration"
      contains: "formatPrice"
  key_links:
    - from: "plan row API response"
      to: "rendered price with correct currency symbol"
      via: "formatPrice(price, currency ?? 'ARS')"
      pattern: "formatPrice.*currency"
---

<objective>
Member app: migrate every price display in the plan catalog to `formatPrice(amount, currency ?? 'ARS')`. The `?? 'ARS'` fallback is the defense against older cached responses that predate the new `currency` field (D-19).

Purpose: REQ-98-09 (currency formatting member-side) + REQ-98-11 (forward-compat for the deployed app). Plan 05 already scoped the server response to the member's branch country, so the member sees only their own country's plans; this plan ensures they render correctly.

**Hard-enumerated scope (grep performed during Plan 98 revision):**

```
$ grep -rn "toLocaleString" el-templo-app/src --include="*.vue" --include="*.ts"
el-templo-app/src/modules/plan/pages/PlanesPage.vue:58: >${{ exp.price.toLocaleString() }}</q-badge
el-templo-app/src/modules/plan/pages/PlanesPage.vue:201: return Math.round(monthlyPrice / 4.33).toLocaleString()
```

Both hits are in `PlanesPage.vue`. There are NO other `.vue` or `.ts` files in `el-templo-app/src` with a price-context `toLocaleString` call. There are no upsell cards, Mi Camino price displays, or subscription summary components using `.toLocaleString` on a monetary value — confirmed by grep. The price display inventory for the member app is therefore exactly 2 sites, both in a single file.

Related pricing reference in member app (not display, just a type):

```
$ grep -rn "pricePaid\|priceRegular\|priceZero" el-templo-app/src --include="*.vue" --include="*.ts"
el-templo-app/src/stores/useUserStore.ts:49: pricePaid: number        ← TYPE DEFINITION only, no display, no migration needed
el-templo-app/src/modules/plan/pages/PlanesPage.vue:145: priceRegular: number   ← TYPE DEFINITION
el-templo-app/src/modules/plan/pages/PlanesPage.vue:197: * priceRegular is always the monthly price regardless...  ← COMMENT
el-templo-app/src/modules/plan/pages/PlanesPage.vue:213: const message = buildWhatsAppMessage(plan.name, plan.priceRegular)   ← handler; no display formatting here
```

Output: Member PlanesPage with formatPrice at lines 58 and 201 (plus any buildWhatsAppMessage body if it embeds a price string). Plan type extended with optional `currency`. TypeScript clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/98-multi-currency-and-country-scoped-plans/98-SPEC.md
@.planning/phases/98-multi-currency-and-country-scoped-plans/98-CONTEXT.md
@.planning/phases/98-multi-currency-and-country-scoped-plans/98-PATTERNS.md
@.planning/phases/98-multi-currency-and-country-scoped-plans/98-05-SUMMARY.md

<interfaces>
From el-templo-app/src/utils/format-price.ts (Plan 02):
```typescript
export type Currency = 'ARS' | 'EUR'
export function formatPrice(amount: number, currency: Currency): string
```

Every API row shape consumed by this page MUST be treated as:

```typescript
{ price?: number; currency?: 'ARS' | 'EUR'; ...otherFields }
```

with `currency` optional for forward-compat.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Member PlanesPage — formatPrice migration at the 2 grep-enumerated sites + currency fallback</name>
  <files>el-templo-app/src/modules/plan/pages/PlanesPage.vue</files>
  <read_first>
    - el-templo-app/src/modules/plan/pages/PlanesPage.vue (the ONLY file with price-context toLocaleString in the member app — lines 58 and 201 per grep)
    - el-templo-app/src/utils/format-price.ts (Plan 02)
    - el-templo-app/src/utils/extract-error.ts
    - el-templo-app/src/utils/logger.ts (createLogger)
    - .planning/phases/98-multi-currency-and-country-scoped-plans/98-PATTERNS.md §7 and §8
    - .planning/phases/98-multi-currency-and-country-scoped-plans/98-CONTEXT.md D-07, D-19
  </read_first>
  <behavior>
    - Line 58 badge: `${{ exp.price.toLocaleString() }}` → `{{ formatPrice(exp.price, exp.currency ?? 'ARS') }}`
    - Line 201 weekly-price helper: `Math.round(monthlyPrice / 4.33).toLocaleString()` → weekly formatted string using formatPrice with the plan's currency (fallback to 'ARS')
    - buildWhatsAppMessage at line 213 (takes plan.priceRegular) — if it embeds a formatted price in its message, migrate that interpolation to formatPrice
    - Type definition for the plan/upsell row (inline interface or imported type) includes `currency?: 'ARS' | 'EUR'` so TypeScript is happy with the fallback pattern
    - Zero other files in el-templo-app/src need migration (grep-confirmed during planning)
  </behavior>
  <action>
**Step 1 — Import formatPrice:**

```typescript
import { formatPrice } from "src/utils/format-price";
```

**Step 2 — Migrate template price display at line 58 (grep-enumerated — first of 2 sites):**

**Before:**

```html
<q-badge v-if="exp.price != null" outline color="primary"
  >${{ exp.price.toLocaleString() }}</q-badge
>
```

**After:**

```html
<q-badge v-if="exp.price != null" outline color="primary"
  >{{ formatPrice(exp.price, exp.currency ?? 'ARS') }}</q-badge
>
```

**Step 3 — Migrate the weekly-price helper at line 201 (grep-enumerated — second of 2 sites):**

**Before:**

```typescript
return Math.round(monthlyPrice / 4.33).toLocaleString();
```

**After:**

```typescript
const weeklyAmount = Math.round(monthlyPrice / 4.33);
return formatPrice(weeklyAmount, currency ?? "ARS");
```

The helper currently takes `monthlyPrice: number` — extend its signature to also accept `currency?: 'ARS' | 'EUR'`:

```typescript
function weeklyPriceLabel(
  monthlyPrice: number,
  currency?: "ARS" | "EUR",
): string {
  const weeklyAmount = Math.round(monthlyPrice / 4.33);
  return `${formatPrice(weeklyAmount, currency ?? "ARS")}/sem`;
}
```

Update all call sites of this helper to pass the plan's currency.

(Note: formatPrice uses Intl.NumberFormat with maximumFractionDigits=0, so fractional division will round. If the existing behavior is "weekly = price/weeks with comma decimal", confirm via the existing code; if precision is critical, switch to `minimumFractionDigits: 2`, but DO NOT change formatPrice's signature — pass a separate helper if needed. The safest path is to use formatPrice as-is; weekly pricing for members is typically displayed as a rough value.)

**Step 4 — buildWhatsAppMessage (line 213):** read the current body and check whether it embeds a price string (e.g., `Me interesa el plan ${name} ($${price.toLocaleString()})`). If yes, migrate that interpolation to `formatPrice(price, currency ?? 'ARS')` and extend the function signature to accept currency. If it does NOT format the price (just passes the name), no change needed.

**Step 5 — Type the plan/upsell row shape (line 145 area):**

Find the inline interface or imported type for `exp` / `plan` on this page. Add `currency?: 'ARS' | 'EUR'`. Keep the `?` optional so older cached responses still type-check (forward-compat per D-19).

**Step 6 — Final verification grep (run locally, should return 0 hits):**

```bash
grep -rn "\.toLocaleString()" el-templo-app/src --include="*.vue" --include="*.ts"
```

After this task, that grep MUST return zero hits. No "allow-listed non-price hits" required — the pre-planning grep confirmed both hits are price-related. Anything remaining is a FAIL. (If a date/time toLocaleString later appears via some other plan, it should be explicitly allow-listed in that plan's SUMMARY, not here.)

**Constraints:**

- No `console.log` / `console.warn` / `console.error`. Use `createLogger()` from `src/utils/logger.ts` per CLAUDE.md.
- No `any` types. `currency?: 'ARS' | 'EUR'` narrow union.
- DO NOT remove any existing WhatsApp-message fallback or null guard — migrate inline, keep existing null checks (`v-if="exp.price != null"`).
- DO NOT widen the file-modified scope beyond `PlanesPage.vue` — grep confirmed no other file in `el-templo-app/src` needs migration. If during execution the executor finds additional price-toLocaleString hits in another file (e.g., a file created between planning and execution), STOP and escalate — it means the planning-time grep missed something and the plan needs revision.
  </action>
  <verify>
  <automated>
  grep -q "formatPrice" el-templo-app/src/modules/plan/pages/PlanesPage.vue \
   && grep -q "currency" el-templo-app/src/modules/plan/pages/PlanesPage.vue \
   && test $(grep -c "formatPrice" el-templo-app/src/modules/plan/pages/PlanesPage.vue) -ge 2 \
   && ! grep -n "\\.toLocaleString()" el-templo-app/src/modules/plan/pages/PlanesPage.vue \
   && ! grep -rn "\\.toLocaleString()" el-templo-app/src --include="_.vue" --include="_.ts" \
   && ! grep -nE 'console\\.(log|warn|error)' el-templo-app/src/modules/plan/pages/PlanesPage.vue \
   && cd el-templo-app && pnpm tsc --noEmit
  </automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "formatPrice" el-templo-app/src/modules/plan/pages/PlanesPage.vue` returns **>= 2** (covers both the template badge site at line 58 and the weekly-helper site at line 201)
  - `grep -q "currency" el-templo-app/src/modules/plan/pages/PlanesPage.vue` succeeds
  - `grep -q "?? 'ARS'\\|?? \"ARS\"" el-templo-app/src/modules/plan/pages/PlanesPage.vue` succeeds (fallback per D-19)
  - `grep -c '\\.toLocaleString()' el-templo-app/src/modules/plan/pages/PlanesPage.vue` returns **0**
  - **Hard zero across the whole member app src tree:** `grep -rn "\\.toLocaleString()" el-templo-app/src --include="*.vue" --include="*.ts"` returns **0 matches**. Acceptable exceptions: explicitly allow-listed date/time uses introduced by a different plan, documented in that plan's SUMMARY — there are none today.
  - `grep -nE 'console\\.(log|warn|error)' el-templo-app/src/modules/plan/pages/PlanesPage.vue` returns no matches
  - `cd el-templo-app && pnpm tsc --noEmit` exits 0
    </acceptance_criteria>
    <done>
    Member app plan catalog uses formatPrice with the currency-fallback pattern at both grep-enumerated sites (lines 58 + 201). Full tree grep for `.toLocaleString()` returns zero. TypeScript clean.
    </done>
    </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                      |
| -------------------------------------- | ------------------------------------------------ |
| API plan response → member app display | currency field optional; fallback prevents crash |
| Deployed app version → new API         | forward-compat required (REQ-98-11)              |

## STRIDE Threat Register

| Threat ID | Category               | Component                                    | Disposition | Mitigation Plan                                                                                   |
| --------- | ---------------------- | -------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------- |
| T-98-18   | Denial of Service      | Deployed app crash on missing currency field | mitigate    | `?? 'ARS'` fallback per D-19; optional type annotation                                            |
| T-98-19   | Information Disclosure | Member seeing cross-country plans            | mitigate    | Plan 05 enforces server-side scoping by member branch; this UI merely renders what server returns |

</threat_model>

<verification>
- formatPrice present in member PlanesPage at >= 2 sites
- Full el-templo-app/src tree grep for .toLocaleString() returns 0 hits
- `?? 'ARS'` fallback applied for forward-compat
- TypeScript clean
</verification>

<success_criteria>

- Both grep-enumerated price display sites in the member app migrated to formatPrice with currency fallback
- Member app TypeScript compiles clean
- No console.log
- Full-tree grep confirms zero remaining price-context toLocaleString
  </success_criteria>

<output>
Create `.planning/phases/98-multi-currency-and-country-scoped-plans/98-10-SUMMARY.md` listing exactly the 2 sites migrated (lines 58 and 201 of PlanesPage.vue) and confirming the full-tree grep returns zero hits. If any additional hit was found during execution (meaning a new file landed between planning and execution), document it and flag as a deviation from the hard enumeration.
</output>
</content>
</invoke>
