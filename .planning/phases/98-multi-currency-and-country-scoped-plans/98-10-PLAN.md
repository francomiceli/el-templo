---
phase: 98-multi-currency-and-country-scoped-plans
plan: 10
type: execute
wave: 5
depends_on: ["05"]
files_modified:
  - el-templo-app/src/modules/plan/pages/PlanesPage.vue
autonomous: true
requirements:
  - REQ-98-09
  - REQ-98-11
  - REQ-98-05

must_haves:
  truths:
    - "Member app plan catalog displays only the member's branch country's plans (enforced server-side by Plan 05)"
    - "Every price display in the member app uses formatPrice(amount, currency ?? 'ARS')"
    - "Response reads use `plan.currency ?? 'ARS'` fallback per D-19 for forward-compatibility with older cached responses"
    - "Upsell surfaces (if any) use formatPrice"
  artifacts:
    - path: "el-templo-app/src/modules/plan/pages/PlanesPage.vue"
      provides: "Member plan catalog with formatPrice migration"
      contains: "formatPrice"
  key_links:
    - from: "plan row API response"
      to: "rendered price with correct currency symbol"
      via: "formatPrice(price, currency ?? 'ARS')"
      pattern: "formatPrice.*currency"
---

<objective>
Member app: migrate every price display in the plan catalog and any upsell surfaces to `formatPrice(amount, currency ?? 'ARS')`. The `?? 'ARS'` fallback is the defense against older cached responses that predate the new `currency` field (D-19).

Purpose: REQ-98-09 (currency formatting member-side) + REQ-98-11 (forward-compat for the deployed app). Plan 05 already scoped the server response to the member's branch country, so the member sees only their own country's plans; this plan ensures they render correctly.

Output: Member PlanesPage with formatPrice everywhere price is shown; any other price-bearing member-app surface (e.g., upsell banners, Mi Camino plan card) updated in the same pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/98-multi-currency-and-country-scoped-plans/98-SPEC.md
@.planning/phases/98-multi-currency-and-country-scoped-plans/98-CONTEXT.md
@.planning/phases/98-multi-currency-and-country-scoped-plans/98-PATTERNS.md
@.planning/phases/98-multi-currency-and-country-scoped-plans/98-05-SUMMARY.md

<interfaces>
From el-templo-app/src/utils/format-price.ts (Plan 02):
```typescript
export type Currency = 'ARS' | 'EUR'
export function formatPrice(amount: number, currency: Currency): string
```

Every API row shape consumed by this page MUST be treated as:

```typescript
{ price?: number; currency?: 'ARS' | 'EUR'; ...otherFields }
```

with `currency` optional for forward-compat.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Member PlanesPage — formatPrice migration with currency fallback + audit other price surfaces</name>
  <files>el-templo-app/src/modules/plan/pages/PlanesPage.vue</files>
  <read_first>
    - el-templo-app/src/modules/plan/pages/PlanesPage.vue (line 58 inline price, lines 196-221 weekly-price helpers + buildWhatsAppMessage per PATTERNS §8)
    - el-templo-app/src/utils/format-price.ts (Plan 02)
    - el-templo-app/src/utils/extract-error.ts
    - el-templo-app/src/utils/logger.ts (createLogger)
    - .planning/phases/98-multi-currency-and-country-scoped-plans/98-PATTERNS.md §7 and §8
    - .planning/phases/98-multi-currency-and-country-scoped-plans/98-CONTEXT.md D-07, D-19
  </read_first>
  <behavior>
    - Line 58 badge: `${{ exp.price.toLocaleString() }}` → `{{ formatPrice(exp.price, exp.currency ?? 'ARS') }}`
    - Weekly-price helper in lines 196-221: returns a formatted string using formatPrice with the plan's currency (fallback to 'ARS')
    - buildWhatsAppMessage (same lines) uses formatPrice for any price embedded in the message
    - Type definition for the plan/upsell row (inline interface or imported type) includes `currency?: 'ARS' | 'EUR'` so TypeScript is happy with the fallback pattern
  </behavior>
  <action>
**Step 1 — Import formatPrice:**

```typescript
import { formatPrice } from "src/utils/format-price";
```

**Step 2 — Migrate template price display at line 58 (per PATTERNS §8):**

**Before:**

```html
<q-badge v-if="exp.price != null" outline color="primary"
  >${{ exp.price.toLocaleString() }}</q-badge
>
```

**After:**

```html
<q-badge v-if="exp.price != null" outline color="primary"
  >{{ formatPrice(exp.price, exp.currency ?? 'ARS') }}</q-badge
>
```

**Step 3 — Migrate TS helpers at lines 196-221** (weekly-price computation and buildWhatsAppMessage). Example pattern:

**Before (rough shape):**

```typescript
const weeklyLabel = `$${(plan.priceRegular / weeks).toLocaleString()}/sem`;
```

**After:**

```typescript
const weeklyAmount = plan.priceRegular / weeks;
const weeklyLabel = `${formatPrice(weeklyAmount, plan.currency ?? "ARS")}/sem`;
```

(Note: formatPrice uses Intl.NumberFormat with maximumFractionDigits=0, so fractional division will round. If the existing behavior is "weekly = price/weeks with comma decimal", confirm via the existing code; if precision is critical, switch to `minimumFractionDigits: 2`, but DO NOT change formatPrice's signature — pass a separate helper if needed. The safest path is to use formatPrice as-is; weekly pricing for members is typically displayed as a rough value.)

For buildWhatsAppMessage: replace every inline `$${price.toLocaleString()}` in the message template with `formatPrice(price, currency ?? 'ARS')`.

**Step 4 — Type the plan/upsell row shape:**

Find the inline interface or imported type for `exp` / `plan` on this page. Add `currency?: 'ARS' | 'EUR'`. Keep the `?` optional so older cached responses still type-check (forward-compat per D-19).

**Step 5 — Audit other member-app price surfaces.**

Grep the entire `el-templo-app/src/` tree for `toLocaleString()` on price values:

```bash
grep -rn "toLocaleString" el-templo-app/src --include="*.vue" --include="*.ts"
```

For each hit in a price context (skip calendars/dates/large-number displays), migrate to formatPrice with the currency-fallback pattern. Common suspects:

- `src/modules/plan/components/UpsellCard.vue` (if exists)
- `src/pages/MyJourney.vue` or `MiCaminoPage.vue` if showing subscription price
- `src/components/subscription/SubscriptionSummary.vue` (if exists)

If a surface exists and has no accompanying `currency` on the data, add the currency to the composable / API response type consumption. If the API already returns it (Plan 05's additive fields), the fix is just consuming it in the component.

**Constraints:**

- No `console.log` / `console.warn` / `console.error`. Use `createLogger()` from `src/utils/logger.ts` per CLAUDE.md.
- No `any` types. `currency?: 'ARS' | 'EUR'` narrow union.
- DO NOT remove any existing WhatsApp-message fallback or null guard — migrate inline, keep existing null checks (`v-if="exp.price != null"`).
  </action>
  <verify>
  <automated>
  grep -q "formatPrice" el-templo-app/src/modules/plan/pages/PlanesPage.vue \
   && grep -q "currency" el-templo-app/src/modules/plan/pages/PlanesPage.vue \
   && ! grep -n "\\.toLocaleString()" el-templo-app/src/modules/plan/pages/PlanesPage.vue \
   && ! grep -nE 'console\\.(log|warn|error)' el-templo-app/src/modules/plan/pages/PlanesPage.vue \
   && cd el-templo-app && pnpm tsc --noEmit
  </automated>
  </verify>
  <acceptance_criteria> - `grep -c "formatPrice" el-templo-app/src/modules/plan/pages/PlanesPage.vue` returns >= 2 (template + TS helper) - `grep -q "currency" el-templo-app/src/modules/plan/pages/PlanesPage.vue` succeeds - `grep -q "?? 'ARS'\\|?? \"ARS\"" el-templo-app/src/modules/plan/pages/PlanesPage.vue` succeeds (fallback per D-19) - `grep -c '\\.toLocaleString()' el-templo-app/src/modules/plan/pages/PlanesPage.vue` returns 0 - `grep -nE 'console\\.(log|warn|error)' el-templo-app/src/modules/plan/pages/PlanesPage.vue` returns no matches - `cd el-templo-app && pnpm tsc --noEmit` exits 0 - Audit: `grep -rn "\\.toLocaleString" el-templo-app/src --include="*.vue" --include="*.ts"` yields only non-price hits (date/number displays unrelated to money) — if any remain, the SUMMARY must justify each
  </acceptance_criteria>
  <done>
  Member app plan catalog and any other price-bearing surfaces use formatPrice with the currency-fallback pattern. TypeScript clean.
  </done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                      |
| -------------------------------------- | ------------------------------------------------ |
| API plan response → member app display | currency field optional; fallback prevents crash |
| Deployed app version → new API         | forward-compat required (REQ-98-11)              |

## STRIDE Threat Register

| Threat ID | Category               | Component                                    | Disposition | Mitigation Plan                                                                                   |
| --------- | ---------------------- | -------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------- |
| T-98-18   | Denial of Service      | Deployed app crash on missing currency field | mitigate    | `?? 'ARS'` fallback per D-19; optional type annotation                                            |
| T-98-19   | Information Disclosure | Member seeing cross-country plans            | mitigate    | Plan 05 enforces server-side scoping by member branch; this UI merely renders what server returns |

</threat_model>

<verification>
- formatPrice present in member PlanesPage
- No toLocaleString on price contexts
- `?? 'ARS'` fallback applied for forward-compat
- TypeScript clean
</verification>

<success_criteria>

- Every price display in member app uses formatPrice with currency fallback
- Member app TypeScript compiles clean
- No console.log
- Audit of other .vue/.ts files confirms no lingering price toLocaleString
  </success_criteria>

<output>
Create `.planning/phases/98-multi-currency-and-country-scoped-plans/98-10-SUMMARY.md` listing all files touched in the audit sweep and confirming zero price-context toLocaleString remains in the member app.
</output>
