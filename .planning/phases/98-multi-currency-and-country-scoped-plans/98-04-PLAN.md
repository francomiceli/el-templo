---
phase: 98-multi-currency-and-country-scoped-plans
plan: 04
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - el-templo-api/src/modules/subscriptions/service.ts
  - el-templo-api/src/modules/payments/service.ts
autonomous: true
requirements:
  - REQ-98-06

must_haves:
  truths:
    - "assignPlan throws BadRequestError when plan.country !== member.branchCountry"
    - "changePlan throws BadRequestError when the new plan's country !== member's branch country"
    - "renewSubscription does NOT need a cross-country check (same plan, same sub)"
    - "recordPayment throws BadRequestError when payment.currency !== subscription.currency"
    - "Subscription inherits currency from plan at creation (assignPlan)"
    - "Payment inherits currency from subscription at creation (recordPayment default)"
    - "BadRequestError maps to HTTP 400 via handleServiceError"
  artifacts:
    - path: "el-templo-api/src/modules/subscriptions/service.ts"
      provides: "assignPlan + changePlan with cross-country guards + currency inheritance"
      contains: "El plan no corresponde al pais de la sucursal"
    - path: "el-templo-api/src/modules/payments/service.ts"
      provides: "recordPayment with currency guard + inheritance"
      contains: "No puedes registrar un pago"
  key_links:
    - from: "assignPlan"
      to: "BadRequestError -> 400 response"
      via: "handleServiceError in routes.ts"
      pattern: "handleServiceError.*BadRequestError"
    - from: "plan.currency"
      to: "subscription.currency"
      via: "inherited in assignPlan insert"
      pattern: "currency: plan.currency"
---

<objective>
Add cross-country plan validation to `assignPlan` and `changePlan` in `subscriptions/service.ts`. Add cross-currency payment validation to `recordPayment` in `payments/service.ts`. Make subscriptions inherit currency from their plan at creation; make payments inherit currency from their subscription at creation (when the client doesn't supply one).

Purpose: Server-layer enforcement of REQ-98-06. Even if the UI filters plans by branch country (Plan 08), an API-direct client or race condition must still be rejected at the service layer.

Output: Two service files with new guards + currency inheritance. Errors return 400 with Spanish messages via the existing BadRequestError / handleServiceError pipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/98-multi-currency-and-country-scoped-plans/98-SPEC.md
@.planning/phases/98-multi-currency-and-country-scoped-plans/98-CONTEXT.md
@.planning/phases/98-multi-currency-and-country-scoped-plans/98-PATTERNS.md
@.planning/phases/98-multi-currency-and-country-scoped-plans/98-03-SUMMARY.md

<interfaces>
From el-templo-api/src/modules/shared/errors.ts (existing):
```typescript
export class BadRequestError extends Error { ... }
export class NotFoundError extends Error { ... }
```

From el-templo-api/src/modules/subscriptions/service.ts existing select at ~lines 480-487 (to be extended):

```typescript
const [member] = await this.db
  .select({
    id: schema.users.id,
    branchId: schema.users.branchId,
    boardingPassUsed: schema.users.boardingPassUsed,
  })
  .from(schema.users)
  .where(eq(schema.users.id, userId));
```

After this plan, that select MUST also include `branchCountry: schema.branches.country` via innerJoin.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cross-country guards + currency inheritance in subscriptions/service.ts</name>
  <files>el-templo-api/src/modules/subscriptions/service.ts</files>
  <read_first>
    - el-templo-api/src/modules/subscriptions/service.ts (file being modified — read `assignPlan` and `changePlan` fully, paying attention to existing member select at ~line 480 and existing guards at ~line 489-502)
    - el-templo-api/src/modules/shared/errors.ts (confirm BadRequestError shape)
    - el-templo-api/src/db/schema/branches.ts (confirm country column)
    - el-templo-api/src/db/schema/subscriptions.ts (confirm currency column — added in Plan 01)
    - .planning/phases/98-multi-currency-and-country-scoped-plans/98-PATTERNS.md §3 (cross-country guard integration guidance, exact Spanish message)
  </read_first>
  <behavior>
    - Test 1: assignPlan({ userId: esUser, planId: arPlan }) throws BadRequestError with message "El plan no corresponde al pais de la sucursal"
    - Test 2: assignPlan({ userId: arUser, planId: arPlan }) succeeds and the created subscription has currency = arPlan.currency
    - Test 3: changePlan(existingSub, newPlan) throws BadRequestError when newPlan.country !== member.branchCountry
    - Test 4: renewSubscription(existingSub) does NOT re-check country (same plan)
    - Test 5: Created subscription's `currency` column equals the assigned plan's `currency` (inherited, not hardcoded)
  </behavior>
  <action>
**Step 1 — Extend member lookup in `assignPlan` to include branch country.**

Locate the existing select at ~lines 480-487 (exact line may have shifted — grep for `boardingPassUsed: schema.users.boardingPassUsed`). Modify it:

```typescript
const [member] = await this.db
  .select({
    id: schema.users.id,
    branchId: schema.users.branchId,
    branchCountry: schema.branches.country,
    boardingPassUsed: schema.users.boardingPassUsed,
  })
  .from(schema.users)
  .innerJoin(schema.branches, eq(schema.branches.id, schema.users.branchId))
  .where(eq(schema.users.id, userId));
```

(If the member has no branchId — edge case — the innerJoin excludes them; existing `if (!member)` branch handles it already.)

**Step 2 — Add country guard after the existing plan-active/archived checks (immediately after `plan.isArchived` check at ~line 502).**

```typescript
if (plan.country !== member.branchCountry) {
  throw new BadRequestError("El plan no corresponde al pais de la sucursal");
}
```

**Step 3 — Inherit currency on subscription insert.**

Locate the `.insert(schema.subscriptions).values({...})` block inside assignPlan. Add `currency: plan.currency,` to the values object. DO NOT hardcode `"ARS"` — use the plan's value so EUR plans produce EUR subscriptions.

**Step 4 — Repeat the country guard in `changePlan`.**

`changePlan` fetches the existing subscription and the new plan. Before applying the change, add the same guard:

```typescript
if (newPlan.country !== member.branchCountry) {
  throw new BadRequestError("El plan no corresponde al pais de la sucursal");
}
```

(Read changePlan carefully to see how it fetches the member / subscription. If the current select lacks branchCountry, extend it the same way as Step 1. If changePlan delegates to assignPlan internally for the insert, the currency inheritance in Step 3 is already transitive.)

Also update changePlan's subscription UPDATE (if it touches currency fields — typically it updates `planId`, `priceRegular`, `priceZero`) to also set `currency: newPlan.currency` if changing plans to a different currency is allowed. Per SPEC.md boundaries, cross-country transfers are impossible (member stays in one country), so within a country currency should always match, but set it explicitly for defense in depth.

**Step 5 — Do NOT add a guard to `renewSubscription`.**

Per SPEC: renewal extends the existing subscription with the same plan — no country change possible. Skip.

**Constraints:**

- Use `BadRequestError` from `../shared/errors` — matches existing pattern at lines 489-502.
- Spanish message VERBATIM: `"El plan no corresponde al pais de la sucursal"` (no accents per existing convention — grep other BadRequestError messages to confirm; if project uses accented Spanish, use "país" instead, but existing line `"El plan seleccionado no esta activo"` suggests no accents).
- No `any` types. No `console.log`.
- Write test cases in a new or existing subscriptions test file BEFORE implementing (TDD — test file lives at `el-templo-api/test/subscriptions/subscriptions.test.ts` or a new `test/multi-currency.test.ts`). Plan 11 covers the full test suite; this task may add one-off RED tests inline if that matches the local TDD flow, but the comprehensive test file is Plan 11.
  </action>
  <verify>
  <automated>
  grep -q "El plan no corresponde al pais de la sucursal" el-templo-api/src/modules/subscriptions/service.ts \
   && grep -q "branchCountry: schema.branches.country" el-templo-api/src/modules/subscriptions/service.ts \
   && grep -q "currency: plan.currency" el-templo-api/src/modules/subscriptions/service.ts \
   && cd el-templo-api && pnpm tsc --noEmit
  </automated>
  </verify>
  <acceptance_criteria> - `grep -c "El plan no corresponde al pais de la sucursal" el-templo-api/src/modules/subscriptions/service.ts` returns at least 2 (one for assignPlan, one for changePlan) - `grep -q "branchCountry: schema.branches.country" el-templo-api/src/modules/subscriptions/service.ts` succeeds - `grep -q "currency: plan.currency" el-templo-api/src/modules/subscriptions/service.ts` succeeds - `grep -q "innerJoin(schema.branches" el-templo-api/src/modules/subscriptions/service.ts` succeeds - No new `console.log` introduced: `grep -c "console\\." el-templo-api/src/modules/subscriptions/service.ts` unchanged from before this task - `cd el-templo-api && pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
  assignPlan and changePlan reject cross-country assignments with 400. Subscriptions inherit currency from plan. Code compiles.
  </done>
  </task>

<task type="auto" tdd="true">
  <name>Task 2: Cross-currency guard + inheritance in payments/service.ts</name>
  <files>el-templo-api/src/modules/payments/service.ts</files>
  <read_first>
    - el-templo-api/src/modules/payments/service.ts (file being modified — read recordPayment fully, the existing subscription fetch at ~line 52-59)
    - el-templo-api/src/modules/shared/errors.ts (BadRequestError)
    - el-templo-api/src/db/schema/subscriptions.ts (confirm currency column present)
    - el-templo-api/src/db/schema/payments.ts (confirm currency column present)
    - .planning/phases/98-multi-currency-and-country-scoped-plans/98-PATTERNS.md §3 (recordPayment guidance)
  </read_first>
  <behavior>
    - Test 1: recordPayment({ subscriptionId: arSub.id, currency: 'EUR' }) throws BadRequestError with message "No puedes registrar un pago en una moneda distinta a la suscripcion"
    - Test 2: recordPayment({ subscriptionId: arSub.id, currency: 'ARS' }) succeeds and created payment has currency='ARS'
    - Test 3: recordPayment({ subscriptionId: arSub.id }) (no currency supplied) creates a payment with currency inherited from subscription
    - Test 4: Auto-recorded payments triggered from assignPlan/changePlan/renewSubscription in subscriptions/service.ts pass the subscription's currency and pass validation
  </behavior>
  <action>
**Step 1 — Extend subscription fetch in `recordPayment` to include currency.**

Locate the existing subscription lookup at ~lines 52-59 (grep for the specific select shape in the file). Add `currency: schema.subscriptions.currency` to the selected columns if not already present after Plan 01 schema changes — it should be available via `schema.subscriptions.*` now.

Example:

```typescript
const [sub] = await this.db
  .select({
    id: schema.subscriptions.id,
    userId: schema.subscriptions.userId,
    currency: schema.subscriptions.currency,
    // ... existing selected fields
  })
  .from(schema.subscriptions)
  .where(eq(schema.subscriptions.id, input.subscriptionId));
```

**Step 2 — Currency inheritance: if `input.currency` is undefined, default to `sub.currency`.**

```typescript
const paymentCurrency = input.currency ?? sub.currency;
```

**Step 3 — Add guard: reject when a non-null input.currency does not match.**

```typescript
if (input.currency !== undefined && input.currency !== sub.currency) {
  throw new BadRequestError(
    "No puedes registrar un pago en una moneda distinta a la suscripcion",
  );
}
```

**Step 4 — Persist currency on payment insert.**

In the `.insert(schema.payments).values({...})` block, add `currency: paymentCurrency`.

**Step 5 — Type the input schema.**

If `RecordPaymentInput` (or equivalent) is defined in `payments/types.ts` or schemas, add an optional `currency?: 'ARS' | 'EUR'` field. Downstream admin UI will omit it (inherited from sub); integration tests (Plan 11) explicitly set it to test the guard.

**Step 6 — Auto-payment callers in subscriptions/service.ts.**

Per STATE.md Phase 63: "Auto-payment recording on assign/change/renew via PaymentService DI in SubscriptionService". Check where SubscriptionService calls `paymentService.recordPayment(...)` — those calls should now pass `currency: plan.currency` (for assignPlan/changePlan) or rely on inheritance (leave currency undefined, letting the subscription's own currency fill in). Either works; explicit-pass is clearer. Pick one and apply consistently.

**Constraints:**

- Spanish message VERBATIM: `"No puedes registrar un pago en una moneda distinta a la suscripcion"`.
- No `any`. No `console.log`.
- Use BadRequestError, mapped to 400 via existing handleServiceError pipeline in payments routes.
  </action>
  <verify>
  <automated>
  grep -q "No puedes registrar un pago en una moneda distinta a la suscripcion" el-templo-api/src/modules/payments/service.ts \
   && grep -q "currency: schema.subscriptions.currency" el-templo-api/src/modules/payments/service.ts \
   && grep -q "currency: paymentCurrency\\|currency: sub.currency\\|currency: input.currency" el-templo-api/src/modules/payments/service.ts \
   && cd el-templo-api && pnpm tsc --noEmit
  </automated>
  </verify>
  <acceptance_criteria> - `grep -q "No puedes registrar un pago en una moneda distinta a la suscripcion" el-templo-api/src/modules/payments/service.ts` succeeds - `grep -q "currency: schema.subscriptions.currency" el-templo-api/src/modules/payments/service.ts` succeeds (subscription currency selected) - `grep -E "currency: (paymentCurrency|sub\\.currency|input\\.currency)" el-templo-api/src/modules/payments/service.ts` matches (currency persisted on payment row) - `grep -q "BadRequestError" el-templo-api/src/modules/payments/service.ts` succeeds (uses correct error class) - `cd el-templo-api && pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
  recordPayment rejects cross-currency payments with 400, inherits subscription currency when client omits it, and persists the currency on the payment row. TypeScript clean.
  </done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                          |
| ----------------------------- | -------------------------------------------------------------------- |
| API client → service methods  | Client may supply mismatched plan/currency; service is the authority |
| UI filter → server validation | UI pre-filters but server is final authority                         |

## STRIDE Threat Register

| Threat ID | Category               | Component                         | Disposition | Mitigation Plan                                                                                            |
| --------- | ---------------------- | --------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------- |
| T-98-02   | Elevation of Privilege | assignPlan cross-country          | mitigate    | Service-layer guard returns 400; cannot be bypassed by UI or API-direct calls                              |
| T-98-03   | Tampering              | recordPayment currency forgery    | mitigate    | Service enforces payment.currency === subscription.currency; default inheritance prevents accidental drift |
| T-98-07   | Tampering              | changePlan cross-country transfer | mitigate    | Service-layer guard mirrors assignPlan guard; no path exists to bypass both                                |

</threat_model>

<verification>
- Two Spanish error messages grep-verified
- Currency inheritance present in both service files
- BadRequestError used (not NotFoundError or ConflictError)
- TypeScript clean
</verification>

<success_criteria>

- assignPlan + changePlan reject cross-country assignments with BadRequestError
- Subscription insert uses `currency: plan.currency`
- recordPayment rejects cross-currency payments with BadRequestError
- Payment insert uses the validated/inherited currency
- All writes produce 400 via handleServiceError when the guard trips
  </success_criteria>

<output>
Create `.planning/phases/98-multi-currency-and-country-scoped-plans/98-04-SUMMARY.md` with the exact line ranges modified in each service file and any notes about auto-payment caller updates.
</output>
