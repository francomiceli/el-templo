---
phase: 108-pago-de-saldo-historial-financiero
plan: 05
type: execute
wave: 2
depends_on: ["108-03"]
files_modified:
  - el-templo-admin/src/components/VoidTransactionDialog.vue
  - el-templo-admin/src/components/FinancialHistoryTab.vue
  - el-templo-admin/src/pages/AlumnoDetailPage.vue
autonomous: true
requirements:
  - PAYMENT-03
must_haves:
  truths:
    - "VoidTransactionDialog.vue existe con q-input textarea 'Razón de anulación' obligatoria min 5 chars (D-17)"
    - "Confirmar disabled hasta que reason tenga ≥5 chars; al confirmar dispara POST /transactions/:id/void"
    - "FinancialHistoryTab.vue existe usando q-list + q-expansion-item (D-12)"
    - "Cada row colapsada muestra: fecha, monto, payment method, kind label en español, badge 'ANULADO' si voided"
    - "Cada row expandida muestra: links del split con conceptLabel + allocatedAmount, void info si aplica"
    - "Rows anuladas tienen strikethrough + grey + badge rojo 'ANULADO' (D-15)"
    - "Botón 'Anular' por row visible solo para FINANCE_VOID_ROLES (owner|admin|gestion) (D-16)"
    - "Paginación append-mode con 'Cargar más' al pie, default size 50, button disabled cuando items.length >= total (D-14)"
    - "AlumnoDetailPage tiene 6to q-tab 'Finanzas' con FinancialHistoryTab montado (D-13)"
    - "Anular exitoso refresca historial + outstanding concepts (en parent)"
  artifacts:
    - path: "el-templo-admin/src/components/VoidTransactionDialog.vue"
      provides: "Dialog secundario con razón obligatoria min 5 chars"
      min_lines: 80
    - path: "el-templo-admin/src/components/FinancialHistoryTab.vue"
      provides: "Tab content con q-list + paginación append + Anular gated"
      min_lines: 200
    - path: "el-templo-admin/src/pages/AlumnoDetailPage.vue"
      provides: "6to q-tab 'Finanzas' montando FinancialHistoryTab"
      contains: "FinancialHistoryTab"
  key_links:
    - from: "FinancialHistoryTab Anular click"
      to: "VoidTransactionDialog open"
      via: "ref / show flag"
      pattern: "showVoidDialog"
    - from: "VoidTransactionDialog onConfirm"
      to: "transactionsApi.voidTransaction (Plan 03 / Phase 106)"
      via: "composable method call"
      pattern: "voidTransaction"
    - from: "FinancialHistoryTab onMounted / loadMore"
      to: "transactionsApi.getFinancialHistory"
      via: "composable method call"
      pattern: "getFinancialHistory"
---

<objective>
Construir el tab "Finanzas" en `AlumnoDetailPage.vue` con timeline cronológico paginado de transacciones, dialog de anulación con razón obligatoria, y RBAC del botón Anular gated por `FINANCE_VOID_ROLES`. Cubre PAYMENT-03.

Purpose: Phase 106 ya construyó GET `/admin/members/:userId/financial-history` y POST `/transactions/:id/void`. Plan 03 expuso ambos en el composable. Este plan trae el UX completo del tab + dialog de anulación.

Output:

- `VoidTransactionDialog.vue` (NEW) — dialog reutilizable con razón obligatoria min 5 chars.
- `FinancialHistoryTab.vue` (NEW) — q-list + expansión + paginación + Anular RBAC-gated.
- `AlumnoDetailPage.vue` — 6to tab "Finanzas".
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/108-pago-de-saldo-historial-financiero/108-CONTEXT.md
@.planning/phases/108-pago-de-saldo-historial-financiero/108-PATTERNS.md
@.planning/phases/108-pago-de-saldo-historial-financiero/108-03-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Composable y types — Plan 03 -->

From `el-templo-admin/src/composables/useTransactionsApi.ts`:

```typescript
async function getFinancialHistory(
  memberId,
  page,
  limit,
): Promise<PaginatedFinancialHistory>;
async function voidTransaction(
  id: number,
  reason: string,
): Promise<{ transaction: TransactionListItem }>;
```

From `el-templo-admin/src/types/transaction.ts` (post-Plan 03):

```typescript
export interface FinancialHistoryItem {
  transaction: { id, kind, direction, amount, currency, paymentMethod, transactionDate, voidedAt, voidReason, ... };
  links: Array<{ targetKind, targetId, allocatedAmount, conceptLabel? }>;
  voidInfo?: { voidedAt, voidedBy, voidReason };
}
export interface PaginatedFinancialHistory { items, total, page, limit, hasMore }
```

From `el-templo-admin/src/pages/CajaPage.vue` (analog para void flow + voided rendering):

- Líneas 184-192: strikethrough/grey pattern para rows voided.
- Línea 342: q-badge 'ANULADO' negative.
- Líneas 505-507: `isVoided(transaction)` helper.
- Líneas 601-625: `confirmVoid` inline pattern (CajaPage usa `$q.dialog.prompt` — Plan 108 lo extrae a componente con min-5-chars).

From `el-templo-admin/src/pages/AlumnoDetailPage.vue` (post-Plan 04):

- Líneas 130-143: q-tabs actuales (5 tabs: perfil, entrenamiento, notas, suscripcion, asistencia).
- Líneas 146-382: q-tab-panels actuales.
- Plan 04 ya agregó `outstandingConcepts` ref, `loadOutstanding`, `RegisterPaymentDialog`, `onPaymentRegistered` callback.

Kind labels en español (D-12):

```typescript
const KIND_LABELS_ES: Record<TransactionKind, string> = {
  plan_charge: "Cobro de plan",
  debt_settlement: "Pago de saldo",
  adjustment: "Ajuste",
  refund: "Reembolso",
  advance_payment: "Pago anticipado",
};
```

(Verificar el enum TransactionKind real — agregar las que falten.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VoidTransactionDialog.vue with min-5-chars reason validation</name>
  <files>el-templo-admin/src/components/VoidTransactionDialog.vue</files>

<read_first> - el-templo-admin/src/pages/CajaPage.vue (líneas 600-630 — confirmVoid pattern actual con $q.dialog.prompt). Es el analog inline a extraer. - el-templo-admin/src/pages/AlumnoDetailPage.vue (líneas 405-450 — patrón de delete confirm dialog para el modal scaffolding). - 108-PATTERNS.md §10 (lines 647-707) — pattern detallado del dialog. - 108-CONTEXT.md D-17 (min 5 chars), D-18 (anulación auditada). - el-templo-admin/src/composables/useTransactionsApi.ts — signature de `voidTransaction(id, reason)`.
</read_first>

  <action>
    Crear `el-templo-admin/src/components/VoidTransactionDialog.vue`:

    **Props:**
    - `modelValue: boolean`
    - `transactionId: number | null` — null cuando dialog está cerrado
    - `transactionLabel?: string` — opcional, muestra qué se está anulando ("Pago de $50.000 del 28/04/2026")

    **Emits:**
    - `update:modelValue`
    - `voided` — sin payload

    **State + logic:**
    ```typescript
    <script setup lang="ts">
    import { computed, ref, watch } from 'vue';
    import { useQuasar } from 'quasar';
    import { useTransactionsApi } from 'src/composables/useTransactionsApi';
    import { createLogger } from 'src/utils/logger';

    const props = defineProps<{
      modelValue: boolean;
      transactionId: number | null;
      transactionLabel?: string;
    }>();

    const emit = defineEmits<{
      (e: 'update:modelValue', value: boolean): void;
      (e: 'voided'): void;
    }>();

    const $q = useQuasar();
    const transactionsApi = useTransactionsApi();
    const log = createLogger('VoidTransactionDialog');

    const reason = ref('');
    const submitting = ref(false);

    const trimmedLength = computed(() => reason.value.trim().length);
    const isValid = computed(() => trimmedLength.value >= 5);

    // Reset al abrir.
    watch(() => props.modelValue, (open) => {
      if (open) {
        reason.value = '';
        submitting.value = false;
      }
    });

    async function onConfirm() {
      if (!isValid.value || props.transactionId === null) return;
      submitting.value = true;
      try {
        await transactionsApi.voidTransaction(props.transactionId, reason.value.trim());
        $q.notify({ type: 'positive', message: 'Transacción anulada' });
        emit('voided');
        emit('update:modelValue', false);
      } catch (err: unknown) {
        const message = err instanceof Error ? err.message : 'Error desconocido';
        log.error('Error anulando transacción', { error: message, transactionId: props.transactionId });
        $q.notify({ type: 'negative', message });
      } finally {
        submitting.value = false;
      }
    }
    </script>
    ```

    **Template:**
    ```vue
    <template>
      <q-dialog
        :model-value="modelValue"
        @update:model-value="(v) => emit('update:modelValue', v)"
        persistent
      >
        <q-card style="min-width: 400px;">
          <q-card-section>
            <div class="text-h6">Anular transacción</div>
            <div v-if="transactionLabel" class="text-body2 text-grey-8 q-mt-xs">
              {{ transactionLabel }}
            </div>
          </q-card-section>

          <q-card-section class="q-pt-none">
            <div class="text-body2 q-mb-sm">
              Esta acción es irreversible. La transacción se marca como anulada
              y los saldos asociados se revierten automáticamente.
            </div>

            <q-input
              v-model="reason"
              label="Razón de anulación *"
              type="textarea"
              dense outlined autogrow
              autofocus
              :rules="[(v) => (v?.trim().length ?? 0) >= 5 || 'Mínimo 5 caracteres']"
              hint="Mínimo 5 caracteres. Visible en el log de auditoría."
            />
          </q-card-section>

          <q-card-actions align="right">
            <q-btn
              flat label="Cancelar"
              :disable="submitting"
              @click="emit('update:modelValue', false)"
            />
            <q-btn
              color="negative"
              label="Anular"
              icon="cancel"
              :loading="submitting"
              :disable="!isValid"
              @click="onConfirm"
            />
          </q-card-actions>
        </q-card>
      </q-dialog>
    </template>
    ```

    **D-18 nota:** El backend (Phase 105/106) revierte balances automáticamente — frontend NO necesita lógica adicional, solo emitir `voided` para que el parent recargue.

  </action>

  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && pnpm typecheck 2>&1 | tail -10 && pnpm lint 2>&1 | tail -10</automated>
  </verify>

<acceptance_criteria> - File `el-templo-admin/src/components/VoidTransactionDialog.vue` existe con ≥80 líneas. - `grep -n "voidTransaction" el-templo-admin/src/components/VoidTransactionDialog.vue` retorna match. - `grep -nE ">= 5|length.*5" el-templo-admin/src/components/VoidTransactionDialog.vue` retorna ≥1 match (validation min 5 chars). - `grep -n "createLogger.*VoidTransactionDialog" el-templo-admin/src/components/VoidTransactionDialog.vue` retorna match. - `grep -n "console\\." el-templo-admin/src/components/VoidTransactionDialog.vue` retorna 0 matches. - `cd el-templo-admin && pnpm typecheck` exits 0 y `pnpm lint` exits 0.
</acceptance_criteria>

  <done>
    Dialog reutilizable, listo para mounting en FinancialHistoryTab.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create FinancialHistoryTab.vue with q-list + expansion + Anular RBAC-gated + Cargar más</name>
  <files>el-templo-admin/src/components/FinancialHistoryTab.vue</files>

<read_first> - el-templo-admin/src/pages/CajaPage.vue (completo) — para patrones de: strikethrough voided (líneas 184-192), badge ANULADO (342), isVoided helper (505-507), formatPrice/formatDate imports (368-369). Es el analog principal. - el-templo-admin/src/composables/useTransactionsApi.ts (post-Plan 03) — signature de getFinancialHistory. - el-templo-admin/src/types/transaction.ts (post-Plan 03) — FinancialHistoryItem y PaginatedFinancialHistory shape. - el-templo-api/src/modules/finance/transaction-service.ts `getFinancialHistory` — confirm response shape exacto (especialmente `hasMore` vs solo `total`). - 108-PATTERNS.md §9 (lines 567-643) — pattern detallado del tab. - 108-CONTEXT.md D-12 (q-list + q-expansion-item), D-13 (tab layout), D-14 (paginación 50 + Cargar más), D-15 (strikethrough), D-16 (Anular RBAC). - el-templo-admin/src/utils/format-price.ts y format-date.ts.
</read_first>

  <action>
    Crear `el-templo-admin/src/components/FinancialHistoryTab.vue`:

    **Props:**
    - `userId: number`

    **Emits:**
    - `voided` — para que el parent recargue outstanding también.

    **State:**
    ```typescript
    <script setup lang="ts">
    import { computed, onMounted, ref } from 'vue';
    import { useQuasar } from 'quasar';
    import { useAuthStore } from 'stores/auth';
    import { useTransactionsApi } from 'src/composables/useTransactionsApi';
    import type { FinancialHistoryItem, TransactionKind } from 'src/types/transaction';
    import { formatPrice } from 'src/utils/format-price';
    import { formatDate } from 'src/utils/format-date';
    import { createLogger } from 'src/utils/logger';
    import VoidTransactionDialog from './VoidTransactionDialog.vue';

    const props = defineProps<{ userId: number }>();
    const emit = defineEmits<{ (e: 'voided'): void }>();

    const $q = useQuasar();
    const authStore = useAuthStore();
    const transactionsApi = useTransactionsApi();
    const log = createLogger('FinancialHistoryTab');

    const PAGE_SIZE = 50; // D-14
    const items = ref<FinancialHistoryItem[]>([]);
    const total = ref(0);
    const currentPage = ref(0); // last loaded page
    const hasMore = ref(false);
    const loading = ref(false);

    const showVoidDialog = ref(false);
    const voidTargetId = ref<number | null>(null);
    const voidTargetLabel = ref<string>('');

    // D-16: FINANCE_VOID_ROLES = owner, admin, gestion (NO recepcion, NO coach).
    const canVoid = computed(() => {
      const role = authStore.user?.role;
      return role === 'owner' || role === 'admin' || role === 'gestion';
    });

    const KIND_LABELS_ES: Record<TransactionKind, string> = {
      plan_charge: 'Cobro de plan',
      debt_settlement: 'Pago de saldo',
      adjustment: 'Ajuste',
      refund: 'Reembolso',
      advance_payment: 'Pago anticipado',
    };

    const PAYMENT_METHOD_LABELS_ES: Record<string, string> = {
      cash: 'Efectivo',
      transfer: 'Transferencia',
      card: 'Tarjeta',
    };

    function isVoided(item: FinancialHistoryItem): boolean {
      return item.transaction.voidedAt !== null;
    }

    function kindLabel(kind: TransactionKind): string {
      return KIND_LABELS_ES[kind] ?? kind;
    }

    function methodLabel(m: string): string {
      return PAYMENT_METHOD_LABELS_ES[m] ?? m;
    }

    async function load(page: number, append: boolean): Promise<void> {
      loading.value = true;
      try {
        const res = await transactionsApi.getFinancialHistory(props.userId, page, PAGE_SIZE);
        if (append) {
          items.value = [...items.value, ...res.items];
        } else {
          items.value = res.items;
        }
        total.value = res.total;
        currentPage.value = res.page;
        hasMore.value = res.hasMore ?? items.value.length < res.total;
      } catch (err: unknown) {
        const message = err instanceof Error ? err.message : 'Error desconocido';
        log.error('Error cargando historial', { error: message });
        $q.notify({ type: 'negative', message });
      } finally {
        loading.value = false;
      }
    }

    function loadMore() {
      void load(currentPage.value + 1, true);
    }

    function refresh() {
      void load(1, false);
    }

    function onAnularClick(item: FinancialHistoryItem) {
      voidTargetId.value = item.transaction.id;
      voidTargetLabel.value = `${kindLabel(item.transaction.kind)} de ${formatPrice(item.transaction.amount, item.transaction.currency)} del ${formatDate(item.transaction.transactionDate)}`;
      showVoidDialog.value = true;
    }

    function onVoided() {
      // Refrescar historial (saldos cambiaron) + emitir al parent para que recargue outstanding.
      refresh();
      emit('voided');
    }

    defineExpose({ refresh });

    onMounted(() => {
      void load(1, false);
    });
    </script>
    ```

    **Template:**
    ```vue
    <template>
      <div>
        <div v-if="items.length === 0 && !loading" class="text-grey-6 q-pa-md">
          Sin movimientos registrados.
        </div>

        <q-list separator>
          <q-expansion-item
            v-for="item in items"
            :key="item.transaction.id"
            :header-class="isVoided(item) ? 'text-grey-5' : ''"
          >
            <template #header>
              <q-item-section>
                <q-item-label>
                  <span :class="{ 'text-strike text-grey-5': isVoided(item) }">
                    {{ formatDate(item.transaction.transactionDate) }} ·
                    {{ kindLabel(item.transaction.kind) }} ·
                    {{ formatPrice(item.transaction.amount, item.transaction.currency) }}
                  </span>
                  <q-badge
                    v-if="isVoided(item)"
                    color="negative"
                    label="ANULADO"
                    class="q-ml-sm"
                  />
                </q-item-label>
                <q-item-label caption>
                  {{ methodLabel(item.transaction.paymentMethod) }}
                  <span v-if="item.transaction.notes"> · {{ item.transaction.notes }}</span>
                </q-item-label>
              </q-item-section>

              <q-item-section side v-if="canVoid && !isVoided(item)">
                <q-btn
                  flat dense round
                  icon="cancel"
                  color="negative"
                  @click.stop="onAnularClick(item)"
                >
                  <q-tooltip>Anular transacción</q-tooltip>
                </q-btn>
              </q-item-section>
            </template>

            <q-card flat>
              <q-card-section>
                <div class="text-subtitle2 q-mb-sm">Distribución</div>
                <q-list dense>
                  <q-item v-for="(link, idx) in item.links" :key="idx">
                    <q-item-section>
                      <q-item-label>
                        {{ link.conceptLabel ?? `${link.targetKind} #${link.targetId}` }}
                      </q-item-label>
                    </q-item-section>
                    <q-item-section side>
                      {{ formatPrice(link.allocatedAmount, item.transaction.currency) }}
                    </q-item-section>
                  </q-item>
                </q-list>

                <div v-if="item.voidInfo" class="q-mt-md text-negative">
                  <div class="text-subtitle2">Información de anulación</div>
                  <div class="text-body2">
                    Anulada el {{ formatDate(item.voidInfo.voidedAt) }} · Razón: {{ item.voidInfo.voidReason }}
                  </div>
                </div>
              </q-card-section>
            </q-card>
          </q-expansion-item>
        </q-list>

        <div class="row justify-center q-mt-md">
          <q-btn
            v-if="items.length > 0 && hasMore"
            flat
            label="Cargar más"
            color="primary"
            :loading="loading"
            @click="loadMore"
          />
          <q-spinner v-if="loading && items.length === 0" color="primary" />
        </div>

        <VoidTransactionDialog
          v-model="showVoidDialog"
          :transaction-id="voidTargetId"
          :transaction-label="voidTargetLabel"
          @voided="onVoided"
        />
      </div>
    </template>
    ```

    **Notas:**
    - `defineExpose({ refresh })` permite que el parent llame `tabRef.value?.refresh()` después de un payment registrado en el dialog del Plan 04.
    - Si el campo en el backend es `voidedAt: string | null` y el shape de `voidInfo` es opcional, el template ya lo maneja con `v-if`.
    - El enum `TransactionKind` real puede tener más valores — verificar y completar `KIND_LABELS_ES` con los reales (no romper si aparece uno nuevo: el `?? kind` fallback cubre).

  </action>

  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && pnpm typecheck 2>&1 | tail -10 && pnpm lint 2>&1 | tail -10</automated>
  </verify>

<acceptance_criteria> - File `el-templo-admin/src/components/FinancialHistoryTab.vue` existe con ≥200 líneas. - `grep -n "q-expansion-item" el-templo-admin/src/components/FinancialHistoryTab.vue` retorna match (D-12). - `grep -n "Cargar más" el-templo-admin/src/components/FinancialHistoryTab.vue` retorna match (D-14). - `grep -nE "PAGE_SIZE\\s*=\\s*50" el-templo-admin/src/components/FinancialHistoryTab.vue` retorna match (D-14). - `grep -n "ANULADO" el-templo-admin/src/components/FinancialHistoryTab.vue` retorna match (D-15). - `grep -n "text-strike" el-templo-admin/src/components/FinancialHistoryTab.vue` retorna match (D-15). - `grep -nE "owner.*admin.*gestion" el-templo-admin/src/components/FinancialHistoryTab.vue` retorna match (D-16 — sin recepcion). - `grep -n "VoidTransactionDialog" el-templo-admin/src/components/FinancialHistoryTab.vue` retorna ≥2 matches (import + tag). - `grep -n "defineExpose" el-templo-admin/src/components/FinancialHistoryTab.vue` retorna match (refresh expuesto). - `cd el-templo-admin && pnpm typecheck` exits 0 y `pnpm lint` exits 0. - NO `console.` y NO `: any`.
</acceptance_criteria>

  <done>
    Tab listo para mounting en AlumnoDetailPage.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add 'Finanzas' tab to AlumnoDetailPage and wire FinancialHistoryTab</name>
  <files>el-templo-admin/src/pages/AlumnoDetailPage.vue</files>

<read_first> - el-templo-admin/src/pages/AlumnoDetailPage.vue (completo, post-Plan 04) — encontrar exactamente q-tabs (líneas 130-143) y q-tab-panels (146-382). Plan 04 ya agregó `outstandingConcepts`, `loadOutstanding`, `RegisterPaymentDialog`, `onPaymentRegistered`. - 108-PATTERNS.md §11 (lines 711-800) — pattern. - 108-CONTEXT.md D-13 (tab layout).
</read_first>

  <action>
    **Step 1 — Import:**
    ```typescript
    import FinancialHistoryTab from 'src/components/FinancialHistoryTab.vue';
    ```

    **Step 2 — Ref para llamar refresh del tab:**
    ```typescript
    const financialHistoryTabRef = ref<InstanceType<typeof FinancialHistoryTab> | null>(null);
    ```

    **Step 3 — Modificar `onPaymentRegistered`** (Plan 04 lo dejó como un void return) para refrescar también el historial:
    ```typescript
    function onPaymentRegistered() {
      void loadOutstanding();
      financialHistoryTabRef.value?.refresh();
    }

    function onTransactionVoided() {
      // Cuando se anula desde el tab, también recargar outstanding (saldos revirtieron).
      void loadOutstanding();
    }
    ```

    **Step 4 — Agregar 6to tab en `q-tabs` (líneas 130-143):**
    ```vue
    <q-tab name="finanzas" label="Finanzas" />
    ```

    **Step 5 — Agregar panel correspondiente en `q-tab-panels` (después del último panel ~línea 380):**
    ```vue
    <q-tab-panel name="finanzas">
      <FinancialHistoryTab
        ref="financialHistoryTabRef"
        :user-id="userId"
        @voided="onTransactionVoided"
      />
    </q-tab-panel>
    ```

    **NO modificar** los 5 tabs/panels existentes. NO mover el botón "Registrar pago" del Plan 04.

    **Verificar imports**: si Plan 04 ya importó `useTransactionsApi`, `OutstandingConcept`, etc., NO duplicar.

  </action>

  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && pnpm typecheck 2>&1 | tail -10 && pnpm lint 2>&1 | tail -10</automated>
  </verify>

<acceptance_criteria> - `grep -n "FinancialHistoryTab" el-templo-admin/src/pages/AlumnoDetailPage.vue` retorna ≥2 matches (import + tag). - `grep -n 'name="finanzas"' el-templo-admin/src/pages/AlumnoDetailPage.vue` retorna ≥2 matches (q-tab + q-tab-panel). - `grep -n "financialHistoryTabRef" el-templo-admin/src/pages/AlumnoDetailPage.vue` retorna ≥3 matches (declaración + ref attr + uso en onPaymentRegistered). - `grep -n "onTransactionVoided" el-templo-admin/src/pages/AlumnoDetailPage.vue` retorna ≥2 matches (declaración + handler). - `cd el-templo-admin && pnpm typecheck` exits 0 y `pnpm lint` exits 0. - Smoke manual: navegar a perfil de un alumno con saldos pendientes, ver tabs (incluyendo Finanzas), abrir tab, ver historial.
</acceptance_criteria>

  <done>
    Tab "Finanzas" funcional con timeline, expansion, Anular RBAC-gated, paginación. PAYMENT-03 cubierto.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter el-templo-admin typecheck` verde.
- `pnpm --filter el-templo-admin lint` verde.
- Smoke: tab muestra rows, expand muestra splits, Anular abre dialog (solo para owner/admin/gestion), confirm con razón ≥5 chars dispara void y refresca, voided rows quedan strikethrough + badge.
</verification>

<success_criteria>

- Tab "Finanzas" en AlumnoDetailPage.
- Historial paginado append-mode (Cargar más).
- Anular gated por FINANCE_VOID_ROLES.
- Razón obligatoria min 5 chars.
- Voided rows visualmente diferenciados.
- PAYMENT-03 cubierto.
  </success_criteria>

<output>
After completion, create `.planning/phases/108-pago-de-saldo-historial-financiero/108-05-SUMMARY.md`.
</output>
