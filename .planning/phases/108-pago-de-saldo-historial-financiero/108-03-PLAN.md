---
phase: 108-pago-de-saldo-historial-financiero
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-admin/src/types/transaction.ts
  - el-templo-admin/src/composables/useTransactionsApi.ts
autonomous: true
requirements:
  - PAYMENT-01
  - PAYMENT-02
  - PAYMENT-03
must_haves:
  truths:
    - "Type OutstandingConcept exportado desde transaction.ts (mirror del backend)"
    - "Type FinancialHistoryItem exportado desde transaction.ts (mirror del backend si no existe ya)"
    - "Type RegisterPaymentInput exportado para el payload del dialog"
    - "Composable useTransactionsApi expone: getOutstandingConcepts, getFinancialHistory, createTransaction (los 3 nuevos)"
    - "Composable existing methods (listTransactions, voidTransaction, getSummary, cleanup) NO modificados"
    - "Cada método nuevo maneja loading, error, extractError per patrón existente líneas 21-58"
  artifacts:
    - path: "el-templo-admin/src/types/transaction.ts"
      provides: "OutstandingConcept, FinancialHistoryItem, RegisterPaymentInput types"
      contains: "OutstandingConcept"
    - path: "el-templo-admin/src/composables/useTransactionsApi.ts"
      provides: "getOutstandingConcepts, getFinancialHistory, createTransaction methods"
      contains: "getOutstandingConcepts"
  key_links:
    - from: "useTransactionsApi.getOutstandingConcepts"
      to: "GET /admin/members/:memberId/outstanding-concepts"
      via: "axios api.get"
      pattern: "outstanding-concepts"
    - from: "useTransactionsApi.createTransaction"
      to: "POST /admin/finance/transactions"
      via: "axios api.post"
      pattern: "/admin/finance/transactions"
---

<objective>
Extender el composable frontend `useTransactionsApi` con 3 métodos nuevos consumidos por los dialogs/components de Phase 108, y agregar los types necesarios. Independiente del backend (Plan 01) — sólo agrega wrappers HTTP, no cambia lógica.

Purpose: Plans 04 (RegisterPaymentDialog) y 05 (FinancialHistoryTab) necesitan estos métodos como foundation. Construirlos primero como wave paralela del backend permite que ambos arranquen al mismo tiempo. Pattern map detectó que `getFinancialHistory` y `createTransaction` NO están en el composable hoy (a pesar de que el doc inicial dijera que sí — Phase 106-05 los dejó pendientes).

Output:

- Types `OutstandingConcept`, `FinancialHistoryItem`, `RegisterPaymentInput`, `VoidTransactionInput` (este último si no existe) en `transaction.ts`.
- Métodos `getOutstandingConcepts`, `getFinancialHistory`, `createTransaction` en `useTransactionsApi.ts`.
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/108-pago-de-saldo-historial-financiero/108-CONTEXT.md
@.planning/phases/108-pago-de-saldo-historial-financiero/108-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Estructura existente del composable — analog directo en el mismo archivo -->

From `el-templo-admin/src/composables/useTransactionsApi.ts` (líneas 17-90 actuales — verificadas por grep):

```typescript
export function useTransactionsApi() {
  const loading = ref(false);
  const error = ref<string | null>(null);

  async function listTransactions(params: TransactionListParams): Promise<...> { /* lines 21-39 */ }

  async function voidTransaction(transactionId: number, reason: string): Promise<{ transaction: TransactionListItem }> {
    loading.value = true;
    error.value = null;
    try {
      const { data } = await api.post<{ transaction: TransactionListItem }>(
        `/admin/finance/transactions/${transactionId}/void`,
        { reason }
      );
      return data;
    } catch (err: unknown) {
      error.value = extractError(err, 'Error anulando transaccion');
      throw err;
    } finally {
      loading.value = false;
    }
  }

  async function getSummary(params: FinanceSummaryParams = {}): Promise<FinanceSummary> { /* lines 60-75 */ }

  function cleanup() { /* line 76 */ }

  return { loading, error, listTransactions, voidTransaction, getSummary, cleanup };
}
```

From `el-templo-admin/src/types/transaction.ts` (líneas 1-119 actuales):

- Existe: `TransactionListItem`, `TransactionListParams`, `TransactionKind`, `TransactionDirection`, `PaymentMethod`, `PAYMENT_METHOD_OPTIONS`, `TargetKind`.
- NO existe: `OutstandingConcept`, `FinancialHistoryItem`, `RegisterPaymentInput`.

From `el-templo-api/src/modules/finance/types.ts:126-139` (backend mirror — usar como referencia para shape de FinancialHistoryItem):

```typescript
export interface FinancialHistoryItem {
  transaction: FinancialTransactionRow;
  links: Array<{
    targetKind: TargetKind;
    targetId: number;
    allocatedAmount: number;
    conceptLabel?: string;
  }>;
  voidInfo?: { voidedAt: string; voidedBy: number; voidReason: string };
}
```

Backend response del endpoint outstanding (Plan 01): `{ concepts: OutstandingConcept[] }`.
Backend response del endpoint financial-history (Phase 106-04): `PaginatedResult<FinancialHistoryItem>` con shape `{ items: [...], total, page, limit, hasMore }` (verificar exactamente con `cat el-templo-api/src/modules/finance/transaction-service.ts | grep -A 5 "getFinancialHistory"`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OutstandingConcept, FinancialHistoryItem, RegisterPaymentInput types</name>
  <files>el-templo-admin/src/types/transaction.ts</files>

<read_first> - el-templo-admin/src/types/transaction.ts (completo, ~119 líneas) — verificar exactamente qué types existen para no redefinir. - el-templo-api/src/modules/finance/types.ts (líneas 100-160) — `FinancialHistoryItem` backend shape (truth source). - el-templo-api/src/modules/finance/transaction-service.ts (método `getFinancialHistory`, especialmente el return type) — confirmar shape de PaginatedResult. - 108-PATTERNS.md §7 (lines 378-446) — pattern listo para copiar adaptado al codebase. - 108-CONTEXT.md D-01 (OutstandingConcept shape) y D-22 (RegisterPaymentInput payload).
</read_first>

  <action>
    **Step 1 — Verificar qué existe** con un grep antes de tocar:

    ```bash
    grep -nE "^export (interface|type|const)" el-templo-admin/src/types/transaction.ts
    ```

    Si `FinancialHistoryItem`, `RegisterPaymentInput`, `OutstandingConcept`, `PaginatedResult` ya existen, NO redefinirlos — extender solo lo que falta.

    **Step 2 — Append al final de `transaction.ts`** (per 108-PATTERNS §7 + D-01 + D-22):

    ```typescript
    /**
     * Phase 108 — Concepto pendiente con saldo abierto.
     * Mirror del backend (api/finance/types.ts OutstandingConcept).
     * Source: GET /admin/members/:memberId/outstanding-concepts.
     */
    export interface OutstandingConcept {
      targetKind: 'subscription' | 'debt_balance';
      targetId: number;
      description: string;
      currency: string;
      balance: number;
      ageInDays: number;
      effectiveDate: string; // YYYY-MM-DD
    }

    /**
     * Phase 108 — Item del historial financiero.
     * Mirror del backend (api/finance/types.ts FinancialHistoryItem:126-139).
     * Source: GET /admin/members/:memberId/financial-history (paginated).
     */
    export interface FinancialHistoryItem {
      transaction: {
        id: number;
        memberId: number;
        kind: TransactionKind;
        direction: TransactionDirection;
        amount: number;
        currency: string;
        paymentMethod: PaymentMethod;
        transactionDate: string;
        effectiveDate: string;
        branchId: number;
        notes: string | null;
        voidedAt: string | null;
        voidedBy: number | null;
        voidReason: string | null;
        createdAt: string;
      };
      links: Array<{
        targetKind: TargetKind;
        targetId: number;
        allocatedAmount: number;
        conceptLabel?: string;
      }>;
      voidInfo?: {
        voidedAt: string;
        voidedBy: number;
        voidReason: string;
      };
    }

    /**
     * Phase 108 — Resultado paginado del historial financiero (mirror backend PaginatedResult).
     */
    export interface PaginatedFinancialHistory {
      items: FinancialHistoryItem[];
      total: number;
      page: number;
      limit: number;
      hasMore: boolean;
    }

    /**
     * Phase 108 — Payload para POST /admin/finance/transactions cuando se registra
     * un pago de saldo (kind='debt_settlement'). Per CONTEXT D-22.
     */
    export interface RegisterPaymentInput {
      memberId: number;
      kind: 'debt_settlement';
      direction: 'inflow';
      amount: number;
      currency: string;
      paymentMethod: PaymentMethod;
      transactionDate: string; // YYYY-MM-DD
      effectiveDate: string;
      branchId: number;
      notes?: string | null;
      links: Array<{
        targetKind: TargetKind;
        targetId: number;
        allocatedAmount: number;
      }>;
    }
    ```

    **NOTA importante:**
    - Si `PaginatedFinancialHistory` ya tiene un nombre canonical en el codebase (ej. genérico `Paginated<T>`), reusar ese — NO duplicar.
    - Confirmar que `TargetKind`, `TransactionKind`, `TransactionDirection`, `PaymentMethod` ya están exportados desde el mismo archivo (líneas 1-119). Si no, importarlos donde corresponda.
    - Si Plan 01 backend retorna shape diferente (ej. el endpoint financial-history no devuelve `hasMore` sino solo `total`), ajustar `PaginatedFinancialHistory` para que matchee EXACTAMENTE el backend. Verificar con grep al backend antes de finalizar.

  </action>

  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && pnpm typecheck 2>&1 | tail -10</automated>
  </verify>

<acceptance_criteria> - `grep -nE "^export interface (OutstandingConcept|FinancialHistoryItem|RegisterPaymentInput|PaginatedFinancialHistory)" el-templo-admin/src/types/transaction.ts` retorna ≥4 matches (o 3 si `PaginatedFinancialHistory` se reusa de un genérico existente). - `cd el-templo-admin && pnpm typecheck` exits 0. - NO se redefinen `TargetKind`, `TransactionKind`, `TransactionDirection`, `PaymentMethod` — solo se referencian. - `RegisterPaymentInput.kind` es literal `'debt_settlement'` (NO `string`). - `RegisterPaymentInput.direction` es literal `'inflow'` (NO `string`).
</acceptance_criteria>

  <done>
    Types disponibles para Plans 04 y 05 sin redefinición. Typecheck verde.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add getOutstandingConcepts, getFinancialHistory, createTransaction methods to useTransactionsApi</name>
  <files>el-templo-admin/src/composables/useTransactionsApi.ts</files>

<read_first> - el-templo-admin/src/composables/useTransactionsApi.ts (completo) — confirmar imports actuales, helper `extractError`, signature de `loading`/`error`, y el return object al final. - 108-PATTERNS.md §6 (lines 302-374) — pattern detallado de los 3 métodos nuevos. Copiar la estructura try/catch/finally idéntica a `voidTransaction` líneas 40-58. - el-templo-admin/src/types/transaction.ts (post-Task 1) — para importar los nuevos types.
</read_first>

  <action>
    **Step 1 — Imports al top del archivo** (sumar a los existentes, NO reescribir):
    ```typescript
    import type {
      // ... existentes (TransactionListItem, TransactionListParams, etc.)
      OutstandingConcept,
      FinancialHistoryItem,
      PaginatedFinancialHistory,
      RegisterPaymentInput,
    } from 'src/types/transaction';
    ```

    **Step 2 — Insertar los 3 métodos nuevos DENTRO de `useTransactionsApi()` después de `voidTransaction` (línea 58) y antes de `getSummary`** (per patrón existente):

    ```typescript
    // Phase 108 — Lista saldos abiertos del miembro (FIFO).
    async function getOutstandingConcepts(memberId: number): Promise<OutstandingConcept[]> {
      loading.value = true;
      error.value = null;
      try {
        const { data } = await api.get<{ concepts: OutstandingConcept[] }>(
          `/admin/members/${memberId}/outstanding-concepts`,
        );
        return data.concepts;
      } catch (err: unknown) {
        error.value = extractError(err, 'Error cargando conceptos pendientes');
        throw err;
      } finally {
        loading.value = false;
      }
    }

    // Phase 108 — Historial financiero paginado del miembro.
    async function getFinancialHistory(
      memberId: number,
      page: number,
      limit: number,
    ): Promise<PaginatedFinancialHistory> {
      loading.value = true;
      error.value = null;
      try {
        const { data } = await api.get<PaginatedFinancialHistory>(
          `/admin/members/${memberId}/financial-history`,
          { params: { page, limit } },
        );
        return data;
      } catch (err: unknown) {
        error.value = extractError(err, 'Error cargando historial financiero');
        throw err;
      } finally {
        loading.value = false;
      }
    }

    // Phase 108 — Registrar pago de saldo (kind='debt_settlement') o cualquier otra transacción.
    async function createTransaction(
      input: RegisterPaymentInput,
    ): Promise<{ transaction: TransactionListItem }> {
      loading.value = true;
      error.value = null;
      try {
        const { data } = await api.post<{ transaction: TransactionListItem }>(
          '/admin/finance/transactions',
          input,
        );
        return data;
      } catch (err: unknown) {
        error.value = extractError(err, 'Error registrando pago');
        throw err;
      } finally {
        loading.value = false;
      }
    }
    ```

    **Step 3 — Append los nuevos métodos al return object** del composable (línea ~81):
    ```typescript
    return {
      loading,
      error,
      listTransactions,
      voidTransaction,
      getSummary,
      // Phase 108 additions:
      getOutstandingConcepts,
      getFinancialHistory,
      createTransaction,
      cleanup,
    };
    ```

    **Verificar:**
    - `extractError` y `api` ya están importados en el archivo (Phase 106-05 los introdujo).
    - El response shape de `createTransaction` (`{ transaction: TransactionListItem }`) puede no coincidir 100% con el backend de Phase 106 — si el backend retorna shape diferente (ej. `{ transactionId, links, affectedBalances }`), ajustar el return type. Verificar con grep al backend:
      ```bash
      grep -A 10 "POST.*transactions[\"']" el-templo-api/src/modules/finance/routes.ts | head -30
      ```
    - Si la signature real difiere, alinear el TS al shape real (NO inventar).

    **NO modificar** `listTransactions`, `voidTransaction`, `getSummary`, `cleanup` existentes.

  </action>

  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && pnpm typecheck 2>&1 | tail -10 && pnpm lint 2>&1 | tail -10</automated>
  </verify>

<acceptance_criteria> - `grep -nE "(getOutstandingConcepts|getFinancialHistory|createTransaction)" el-templo-admin/src/composables/useTransactionsApi.ts` retorna ≥6 matches (3 definitions + 3 returns). - `grep -n "outstanding-concepts" el-templo-admin/src/composables/useTransactionsApi.ts` retorna 1 match (URL en getOutstandingConcepts). - `grep -n "/admin/finance/transactions" el-templo-admin/src/composables/useTransactionsApi.ts` retorna ≥2 matches (createTransaction + voidTransaction existente). - Los 3 métodos nuevos tienen estructura `loading.value = true; error.value = null; try { ... } catch (err: unknown) { error.value = extractError(...); throw err; } finally { loading.value = false; }`. - `cd el-templo-admin && pnpm typecheck` exits 0. - `cd el-templo-admin && pnpm lint` exits 0. - NO `any` types (CLAUDE.md). - Existing methods (listTransactions, voidTransaction, getSummary) sin cambios — verificar con `git diff` (solo additions, no modificaciones).
</acceptance_criteria>

  <done>
    Composable extendido con los 3 métodos. Plans 04 y 05 pueden importar y usar.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter el-templo-admin typecheck` verde.
- `pnpm --filter el-templo-admin lint` verde.
- Composable expone 8 métodos en total (5 originales + 3 nuevos).
</verification>

<success_criteria>

- Types canonicales en transaction.ts.
- Composable con métodos consumibles por los dialogs.
- Plans 04 y 05 unblock.
  </success_criteria>

<output>
After completion, create `.planning/phases/108-pago-de-saldo-historial-financiero/108-03-SUMMARY.md`.
</output>
