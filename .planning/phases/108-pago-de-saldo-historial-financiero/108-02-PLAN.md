---
phase: 108-pago-de-saldo-historial-financiero
plan: 02
type: execute
wave: 2
depends_on: ["108-01"]
files_modified:
  - el-templo-api/test/members/outstanding-concepts.test.ts
autonomous: true
requirements:
  - PAYMENT-02
must_haves:
  truths:
    - "Test cubre happy path: subscription balance retorna description 'Mensualidad <Mes> <Año> — <PlanName>' y ageInDays correcto"
    - "Test cubre debt_balance fallback: description = 'Saldo libre #<id>'"
    - "Test cubre no-saldos: { concepts: [] } (D-03)"
    - "Test cubre orden FIFO: mixed subscription + debt_balance ordenado por effectiveDate ASC"
    - "Test cubre RBAC: coach role recibe 403, recepcion recibe 200"
    - "Test cubre cross-country: non-owner accediendo miembro de otro país recibe 404 (info-leak)"
  artifacts:
    - path: "el-templo-api/test/members/outstanding-concepts.test.ts"
      provides: "Integration tests del endpoint outstanding-concepts"
      min_lines: 150
  key_links:
    - from: "test/members/outstanding-concepts.test.ts"
      to: "test/helpers.ts (createApp + auth helpers)"
      via: "import"
      pattern: "from .*helpers"
---

<objective>
Cubrir el endpoint `GET /api/admin/members/:userId/outstanding-concepts` (Plan 01) con integration tests que verifiquen happy path, fallbacks, RBAC, cross-country y orden FIFO. Tests corren contra MySQL real (`eltemplo_test`) per CLAUDE.md.

Purpose: Sin tests, los invariantes de D-01/D-04/D-06 + cross-country 404 (info-leak prevention) son frágiles. Phase 106 estableció el patrón `test/finance/transactions-api.test.ts` — replicarlo.

Output:

- Archivo nuevo `el-templo-api/test/members/outstanding-concepts.test.ts` con casos enumerados.
- `pnpm --filter el-templo-api test outstanding-concepts` exits 0.
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/108-pago-de-saldo-historial-financiero/108-CONTEXT.md
@.planning/phases/108-pago-de-saldo-historial-financiero/108-PATTERNS.md
@.planning/phases/108-pago-de-saldo-historial-financiero/108-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Test helpers establecidos por Phase 106 -->

From `el-templo-api/test/helpers.ts` (verificar contenido real):

- `createApp()` — bootstrap Fastify app contra eltemplo_test.
- Funciones para crear users/admins con roles específicos, branches, subscriptions, balances.
- `authedRequest(role, ...)` o equivalent para inyectar JWT con role.

From `el-templo-api/test/finance/transactions-api.test.ts` (Phase 106 — patrón canonical):

- Estructura `describe`/`beforeEach`/`afterEach` con cleanup de tablas.
- Setup helpers para crear: user (member), admin con role, branch (BR/AR), subscription, transaction.

From schema (per Plan 01 read_first):

- `balances` table: `memberId, targetKind, targetId, currency, amount, createdAt`.
- Para crear un balance pendiente: o se crea via transaction creation (preferido — usa el flow real), o se inserta directo en la tabla balances (más rápido pero bypassea invariantes).
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Create outstanding-concepts integration test suite</name>
  <files>el-templo-api/test/members/outstanding-concepts.test.ts</files>

<read_first> - el-templo-api/test/helpers.ts (completo) — confirmar exports disponibles: createApp, factories de usuarios/branches/subscriptions, helper de requests autenticadas. - el-templo-api/test/finance/transactions-api.test.ts (completo, Phase 106) — patrón EXACTO de setup, teardown, role-based requests. COPIAR la estructura. - el-templo-api/test/members/financial-history.test.ts si existe (Phase 106-04) — patrón más cercano (mismo pathprefix, mismo RBAC, mismo cross-country guard). Si existe, es el analog #1. - el-templo-api/src/modules/finance/transaction-service.ts (método `getOutstandingConcepts` que se acaba de crear en Plan 01) — entender shape de respuesta para los asserts. - .planning/phases/108-pago-de-saldo-historial-financiero/108-PATTERNS.md §5 (lines 283-296) — lista de casos a cubrir. - .planning/phases/108-pago-de-saldo-historial-financiero/108-CONTEXT.md D-01..D-06 — invariantes a verificar.
</read_first>

  <action>
    **Step 1 — Identificar test helpers exactos.**

    Antes de escribir, confirmar con grep:
    ```bash
    grep -nE "export (async )?function|export const" el-templo-api/test/helpers.ts | head -40
    ```

    Si hay un test análogo `financial-history.test.ts`, listarlo:
    ```bash
    ls el-templo-api/test/members/ 2>/dev/null
    ```

    Reusar EXACTAMENTE los mismos helpers/factories que ese test (NO inventar nuevos).

    **Step 2 — Crear `el-templo-api/test/members/outstanding-concepts.test.ts`** con la estructura siguiente (adaptar imports/factories al estilo real del proyecto detectado en Step 1):

    ```typescript
    import { afterAll, afterEach, beforeAll, beforeEach, describe, expect, it } from "vitest";
    // Imports reales de helpers — adaptar al proyecto:
    import { createApp /*, ...factories*/ } from "../helpers.js";
    import type { FastifyInstance } from "fastify";

    describe("GET /api/admin/members/:userId/outstanding-concepts (Phase 108)", () => {
      let app: FastifyInstance;

      beforeAll(async () => {
        app = await createApp();
        await app.ready();
      });

      afterAll(async () => {
        await app.close();
      });

      beforeEach(async () => {
        // Cleanup data per patrón Phase 106 — match exactly the cleanup of financial-history.test.ts.
      });

      describe("happy paths", () => {
        it("returns empty array when member has no outstanding balances (D-03)", async () => {
          // 1. Create member with no balances (or all balances at 0).
          // 2. Authenticate as admin role (ARG country).
          // 3. GET /api/admin/members/:userId/outstanding-concepts
          // 4. expect(response.statusCode).toBe(200)
          // 5. expect(response.json()).toEqual({ concepts: [] })
        });

        it("returns subscription concept with description 'Mensualidad <Mes> <Año> — <PlanName>' (D-06)", async () => {
          // 1. Create member + subscription (startDate '2026-03-01', plan name 'Performance Mensual').
          // 2. Create a transaction (plan_charge) que deja balance pendiente — usar TransactionService o factory.
          //    Alternativa pragmática: insert directo en balances con targetKind='subscription', targetId=sub.id, amount=20000, currency='ARS'.
          // 3. GET endpoint as admin (ARG).
          // 4. expect(concepts.length).toBe(1).
          // 5. expect(concepts[0].description).toBe('Mensualidad Marzo 2026 — Performance Mensual').
          // 6. expect(concepts[0].targetKind).toBe('subscription').
          // 7. expect(concepts[0].balance).toBe(20000).
          // 8. expect(concepts[0].currency).toBe('ARS').
          // 9. expect(concepts[0].effectiveDate).toBe('2026-03-01').
          // 10. expect(concepts[0].ageInDays).toBeGreaterThanOrEqual(0).
        });

        it("returns debt_balance concept with fallback description 'Saldo libre #<id>' (D-06)", async () => {
          // 1. Create member.
          // 2. Insert directo en balances: targetKind='debt_balance', targetId=42, amount=5000, currency='ARS'.
          //    (No requiere subscription FK porque LEFT JOIN.)
          // 3. GET endpoint as admin.
          // 4. expect(concepts[0].description).toBe('Saldo libre #42').
          // 5. expect(concepts[0].targetKind).toBe('debt_balance').
        });

        it("orders concepts by effectiveDate ASC — FIFO mixed subscription + debt_balance (D-01)", async () => {
          // 1. Create 3 balances:
          //    a. subscription startDate='2026-01-15', amount=10000.
          //    b. debt_balance createdAt='2026-02-10', amount=5000.
          //    c. subscription startDate='2026-03-01', amount=20000.
          // 2. GET endpoint.
          // 3. expect(concepts.map(c => c.effectiveDate)).toEqual(['2026-01-15', '2026-02-10', '2026-03-01']).
        });

        it("clamps ageInDays to 0 when effectiveDate is in the future (D-04)", async () => {
          // 1. Create subscription with startDate = 30 days in the future.
          // 2. Insert balance amount=10000.
          // 3. GET endpoint.
          // 4. expect(concepts[0].ageInDays).toBe(0).
        });
      });

      describe("RBAC (D-04 / FINANCE_READ_ROLES)", () => {
        it("returns 403 for coach role", async () => {
          // 1. Create member with balance.
          // 2. Authenticate as coach.
          // 3. expect(response.statusCode).toBe(403).
        });

        it("returns 200 for recepcion role", async () => {
          // 1. Same setup, recepcion role.
          // 2. expect(response.statusCode).toBe(200).
        });

        it("returns 200 for admin role", async () => {
          // 1. admin in same country.
          // 2. expect(response.statusCode).toBe(200).
        });

        it("returns 401 when unauthenticated", async () => {
          // No auth header.
          // expect(response.statusCode).toBe(401).
        });
      });

      describe("Cross-country guard (info-leak prevention)", () => {
        it("returns 404 when non-owner admin tries to access member from foreign country", async () => {
          // 1. Create member in branch with country='AR'.
          // 2. Authenticate as admin scoped to country='ES'.
          // 3. expect(response.statusCode).toBe(404). (NOT 403 — info-leak prevention.)
          // 4. expect(response.json().error).toBe('No encontrado').
        });

        it("returns 200 when owner crosses countries (owner has global scope)", async () => {
          // 1. Member AR, owner role.
          // 2. expect(response.statusCode).toBe(200).
        });

        it("returns 200 when virtual branch is involved (branchIsVirtual short-circuits country check)", async () => {
          // 1. Member in virtual branch, non-owner admin from different country.
          // 2. expect(response.statusCode).toBe(200).
        });
      });

      describe("Edge cases", () => {
        it("returns 404 when member does not exist", async () => {
          // GET .../999999/outstanding-concepts → 404.
        });

        it("returns 404 when member is soft-deleted", async () => {
          // Member with deletedAt != null → 404.
        });

        it("filters out balances with amount = 0 or negative (only amount > 0 returned)", async () => {
          // Insert balances: amount=0 and amount=-1000 and amount=5000.
          // expect(concepts.length).toBe(1) y expect(concepts[0].balance).toBe(5000).
        });
      });
    });
    ```

    **Adaptaciones obligatorias:**
    - Usar EXACTAMENTE los nombres de factories y helpers que existan en el proyecto. Si `financial-history.test.ts` existe, copiar su patrón de setup/teardown.
    - Si los factories del proyecto crean balances vía `TransactionService.create()` (no insert directo), usar ese flow incluso si es más verboso — match al estilo del codebase.
    - Si no existe un helper para "auth as role X", consultar el patrón de Phase 106 tests.

    **NO crear** mocks. NO usar SQLite. Tests corren contra `eltemplo_test` MySQL real (CLAUDE.md).

  </action>

  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-api && pnpm test outstanding-concepts 2>&1 | tail -40</automated>
  </verify>

<acceptance_criteria> - File `el-templo-api/test/members/outstanding-concepts.test.ts` existe. - `grep -nE "describe\\(|it\\(" el-templo-api/test/members/outstanding-concepts.test.ts` retorna ≥10 matches (suite + ~13 cases). - `cd el-templo-api && pnpm test outstanding-concepts` exits 0 (todos los cases pasan). - Cubre: happy empty, subscription description, debt_balance description, FIFO order, ageInDays clamp future, coach 403, recepcion 200, admin 200, unauth 401, cross-country 404, owner cross-country 200, virtual branch 200, member not exist 404, soft-deleted 404, amount > 0 filter. - NO usa `as any` (CLAUDE.md no `any` types).
</acceptance_criteria>

  <done>
    Test suite verde, cubre los 6 must_haves truths arriba, ejecutable con `pnpm test outstanding-concepts`.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter el-templo-api test outstanding-concepts` exits 0.
- Cobertura mínima: 13 casos enumerados arriba.
</verification>

<success_criteria>

- Endpoint del Plan 01 verificado contra MySQL real.
- Invariantes D-01/D-03/D-04/D-06 + RBAC + cross-country cubiertos por tests.
- Ningún test usa `any`.
  </success_criteria>

<output>
After completion, create `.planning/phases/108-pago-de-saldo-historial-financiero/108-02-SUMMARY.md`.
</output>
