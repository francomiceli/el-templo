---
phase: 108-pago-de-saldo-historial-financiero
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/finance/types.ts
  - el-templo-api/src/modules/finance/schemas.ts
  - el-templo-api/src/modules/finance/transaction-service.ts
  - el-templo-api/src/modules/members/routes.ts
autonomous: true
requirements:
  - PAYMENT-02
must_haves:
  truths:
    - "GET /api/admin/members/:userId/outstanding-concepts retorna { concepts: OutstandingConcept[] } ordenado por effectiveDate ASC (FIFO)"
    - "Cuando el miembro no tiene saldos pendientes, retorna { concepts: [] } (no 404)"
    - "Concepto con targetKind='subscription' tiene description 'Mensualidad <Mes> <Año> — <PlanName>' (mes/año derivados de subscriptions.startDate)"
    - "Concepto con targetKind='debt_balance' tiene description 'Saldo libre #<id>' (fallback)"
    - "ageInDays = max(0, DATEDIFF(CURDATE(), effectiveDate))"
    - "Cross-country reads (no-owner accediendo miembro de otro país) retornan 404 (info-leak prevention)"
    - "Coach role recibe 403 (FINANCE_READ_ROLES excluye coach)"
  artifacts:
    - path: "el-templo-api/src/modules/finance/types.ts"
      provides: "OutstandingConcept type definition"
      contains: "export interface OutstandingConcept"
    - path: "el-templo-api/src/modules/finance/schemas.ts"
      provides: "outstandingConceptsSchema (Fastify JSON Schema as const)"
      contains: "outstandingConceptsSchema"
    - path: "el-templo-api/src/modules/finance/transaction-service.ts"
      provides: "getOutstandingConcepts(memberId) method on TransactionService"
      contains: "getOutstandingConcepts"
    - path: "el-templo-api/src/modules/members/routes.ts"
      provides: "GET /:userId/outstanding-concepts handler montado"
      contains: "outstanding-concepts"
  key_links:
    - from: "members/routes.ts handler"
      to: "transactionService.getOutstandingConcepts"
      via: "service call after auth + cross-country guard"
      pattern: "transactionService\\.getOutstandingConcepts"
    - from: "transaction-service.ts getOutstandingConcepts"
      to: "schema.balances LEFT JOIN subscriptions LEFT JOIN subscriptionPlans"
      via: "Drizzle select with WHERE amount > 0"
      pattern: "schema\\.balances"
---

<objective>
Construir el endpoint backend `GET /api/admin/members/:userId/outstanding-concepts` que retorna la lista autoritativa de saldos pendientes del miembro con descripción humana, antigüedad en días, y orden FIFO por effective date. Cubre PAYMENT-02 backend prep — el frontend del split allocation (Plan 04) consume este endpoint.

Purpose: Sin este endpoint, el dialog "Registrar pago" no tiene de dónde leer los saldos abiertos. Phase 106 trajo `financial-history` (lista plana de transacciones), pero NO una vista agrupada por concepto pendiente con balance y antigüedad. Este plan agrega exactamente eso.

Output:

- Type `OutstandingConcept` en `finance/types.ts`.
- JSON Schema `outstandingConceptsSchema` (Fastify as const, NOT Zod) en `finance/schemas.ts`.
- Método `TransactionService.getOutstandingConcepts(memberId)` en `transaction-service.ts`.
- Route handler montado en `members/routes.ts` con auth + RBAC + cross-country guard.
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-pago-de-saldo-historial-financiero/108-CONTEXT.md
@.planning/phases/108-pago-de-saldo-historial-financiero/108-PATTERNS.md
@.planning/phases/105-modelo-de-datos-drop-del-viejo/105-SPEC.md
@.planning/phases/106-endpoints-transaccionales/106-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Key types and infrastructure el executor debe usar. NO explorar codebase. -->

From `el-templo-api/src/modules/finance/types.ts` (per 108-PATTERNS §4):

```typescript
// Existing types — extend, no breaking changes
export type BalanceTargetKind = BalanceRow["targetKind"]; // 'subscription' | 'debt_balance'
export interface FinancialHistoryItem {
  /* existing — analog at lines 126-139 */
}
```

From `el-templo-api/src/modules/shared/permissions.ts:91-100`:

```typescript
export const FINANCE_VOID_ROLES = ["owner", "admin", "gestion"] as const;
export const FINANCE_READ_ROLES = [
  /* owner, admin, gestion, recepcion */
];
export const FINANCE_WRITE_ROLES = [
  /* same as READ */
];
```

From `el-templo-api/src/db/schema/balances.ts` (verified):

- `memberId`, `targetKind`, `targetId`, `currency`, `amount` (signed int), `createdAt: timestamp`.

From `el-templo-api/src/db/schema/subscriptions.ts:55`:

- `startDate: date("start_date", { mode: "string" }).notNull()` — THIS is the effective date (NO `effectiveDate` field).

From `el-templo-api/src/db/schema/subscription-plans.ts:31`:

- `name: varchar(...)` — plan name for description.

From `el-templo-api/src/modules/finance/schemas.ts:256-332` (analog `financialHistorySchema`):

```typescript
export const financialHistorySchema = {
  params: {
    type: "object",
    required: ["userId"],
    properties: { userId: { type: "integer", minimum: 1 } },
  },
  querystring: {
    /* page, limit */
  },
  response: {
    200: {
      type: "object",
      properties: {
        /* ... */
      },
      additionalProperties: true,
    },
    401: errorSchema,
    403: errorSchema,
    404: errorSchema,
    500: errorSchema,
  },
} as const;
```

From `el-templo-api/src/modules/members/routes.ts:714-784` (analog handler — copy verbatim, swap schema + service):

```typescript
fastify.get<{ Params: { userId: number } }>(
  "/:userId/financial-history",
  { schema: financialHistorySchema },
  async (request, reply) => {
    try {
      if (
        !(FINANCE_READ_ROLES as readonly string[]).includes(request.user.role)
      ) {
        return reply
          .code(403)
          .send({ error: "Acceso denegado", message: "..." });
      }
      const [target] = await fastify.db
        .select({
          /* id, deletedAt, branchCountry, branchIsVirtual */
        })
        .from(schema.users)
        .innerJoin(
          schema.branches,
          eq(schema.branches.id, schema.users.branchId),
        )
        .where(eq(schema.users.id, request.params.userId))
        .limit(1);
      if (!target || target.deletedAt)
        return reply.code(404).send({ error: "No encontrado", message: "..." });
      if (
        !request.scope.isOwner &&
        !target.branchIsVirtual &&
        target.branchCountry !== request.scope.country
      ) {
        return reply.code(404).send({ error: "No encontrado", message: "..." });
      }
      return await transactionService.getFinancialHistory(
        request.params.userId,
        {
          /* ... */
        },
      );
    } catch (err: unknown) {
      handleServiceError(err, reply, request.log, "get financial history");
    }
  },
);
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OutstandingConcept type + outstandingConceptsSchema</name>
  <files>el-templo-api/src/modules/finance/types.ts, el-templo-api/src/modules/finance/schemas.ts</files>

<read_first> - el-templo-api/src/modules/finance/types.ts (lines 1-150) — see existing `FinancialHistoryItem` (lines 126-139) and `BalanceTargetKind` (line 27) as analogs. - el-templo-api/src/modules/finance/schemas.ts (lines 200-340) — see `financialHistorySchema` (lines 256-332) as the exact pattern. Note the `additionalProperties: true` convention at lines 287-294. - .planning/phases/108-pago-de-saldo-historial-financiero/108-PATTERNS.md §3 + §4 (lines 192-280) — copy the JSON Schema and TS interface verbatim. - .planning/phases/108-pago-de-saldo-historial-financiero/108-CONTEXT.md D-01 (response shape) and D-02 (no pagination).
</read_first>

  <action>
    **Step 1 — Add type to `finance/types.ts`** (per D-01 + 108-PATTERNS §4):

    Append to the end of the existing types file:
    ```typescript
    /**
     * Concepto pendiente con saldo abierto. Resultado de
     * GET /api/admin/members/:userId/outstanding-concepts (Phase 108).
     * Source: balances WHERE amount > 0, ordenado por effectiveDate ASC (FIFO).
     */
    export interface OutstandingConcept {
      targetKind: BalanceTargetKind; // 'subscription' | 'debt_balance'
      targetId: number;
      description: string; // ej: "Mensualidad Marzo 2026 — Performance Mensual"
      currency: string; // ARS / EUR
      balance: number; // saldo pendiente positivo (entero)
      ageInDays: number; // días desde effectiveDate (clamp 0 si futuro)
      effectiveDate: string; // YYYY-MM-DD para auditoría / orden FIFO
    }
    ```

    Reusa `BalanceTargetKind` ya exportado (línea 27). NO redefinir.

    **Step 2 — Add JSON Schema to `finance/schemas.ts`** (Fastify as const, NOT Zod, per CLAUDE.md):

    Append after `financialHistorySchema`:
    ```typescript
    /**
     * Phase 108 — GET /api/admin/members/:userId/outstanding-concepts
     * D-02: No pagination — array completo (alumnos casi nunca tienen >20 saldos).
     * D-03: Cuando no hay saldos, retornar { concepts: [] } (no 404).
     */
    export const outstandingConceptsSchema = {
      params: {
        type: "object",
        required: ["userId"],
        properties: { userId: { type: "integer", minimum: 1 } },
      },
      response: {
        200: {
          type: "object",
          properties: {
            concepts: {
              type: "array",
              items: {
                type: "object",
                additionalProperties: true,
                properties: {
                  targetKind: { type: "string", enum: ["subscription", "debt_balance"] },
                  targetId: { type: "integer", minimum: 1 },
                  description: { type: "string" },
                  currency: { type: "string" },
                  balance: { type: "integer", minimum: 1 },
                  ageInDays: { type: "integer", minimum: 0 },
                  effectiveDate: { type: "string", format: "date" },
                },
              },
            },
          },
        },
        401: errorSchema,
        403: errorSchema,
        404: errorSchema,
        500: errorSchema,
      },
    } as const;
    ```

    Reusar `errorSchema` ya importado/definido en el archivo (verificar dónde está definido y reusar — no redefinir).

    **Decisión sobre `additionalProperties: true`:** matches loose-response convention de Fastify fast-json-stringify para evitar strip de campos. Per D-01 spec del schema.

    **NO cambiar nada más** en `schemas.ts` (no tocar el `financialHistorySchema` existente).

  </action>

  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-api && pnpm typecheck 2>&1 | tail -20</automated>
  </verify>

<acceptance_criteria> - `grep -n "export interface OutstandingConcept" el-templo-api/src/modules/finance/types.ts` retorna match. - `grep -n "outstandingConceptsSchema" el-templo-api/src/modules/finance/schemas.ts` retorna al menos 1 match (la definición). - `grep -n 'additionalProperties: true' el-templo-api/src/modules/finance/schemas.ts` retorna al menos 2 matches (financialHistory + outstanding). - `cd el-templo-api && pnpm typecheck` exits 0. - Type `OutstandingConcept` reusa `BalanceTargetKind` (no string literal duplicado).
</acceptance_criteria>

  <done>
    OutstandingConcept exportado, outstandingConceptsSchema exportado as const, typecheck verde.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement getOutstandingConcepts service method + mount handler in members/routes.ts</name>
  <files>el-templo-api/src/modules/finance/transaction-service.ts, el-templo-api/src/modules/members/routes.ts</files>

<read_first> - el-templo-api/src/modules/finance/transaction-service.ts (lines 1-50 imports + lines 513-604 `getFinancialHistory` analog, especialmente el JOIN de líneas 545-586). - el-templo-api/src/modules/members/routes.ts (lines 80-110 module hook + lines 714-784 financial-history handler analog — copia verbatim, swap schema + service call). - el-templo-api/src/db/schema/balances.ts (lines 1-60) — confirmar columnas: `memberId`, `targetKind`, `targetId`, `currency`, `amount`, `createdAt`. - el-templo-api/src/db/schema/subscriptions.ts (línea 55) — `startDate: date("start_date", { mode: "string" })`. - .planning/phases/108-pago-de-saldo-historial-financiero/108-PATTERNS.md §2 (lines 100-188) — query pattern exacto + hidden invariants críticos. - .planning/phases/108-pago-de-saldo-historial-financiero/108-CONTEXT.md D-04 (ageInDays = DATEDIFF clamp 0), D-05 (effectiveDate source), D-06 (description format). - el-templo-api/src/modules/shared/permissions.ts (líneas 80-100) — confirmar `FINANCE_READ_ROLES`.
</read_first>

  <action>
    **Step 1 — Add `getOutstandingConcepts` method to `TransactionService`** (per 108-PATTERNS §2 + D-01/D-04/D-05/D-06):

    Insertar el método después de `getFinancialHistory` en `transaction-service.ts` (~línea 605, después del cierre del método anterior):

    ```typescript
    /**
     * Phase 108 (D-01..D-06): Lista saldos pendientes del miembro con descripción humana
     * y antigüedad. Source: balances cache (105-SPEC §8) WHERE amount > 0.
     * Ordenado por effectiveDate ASC (FIFO).
     */
    async getOutstandingConcepts(memberId: number): Promise<OutstandingConcept[]> {
      this.log.info({ memberId }, "Loading outstanding concepts");

      const rows = await this.db
        .select({
          targetKind: schema.balances.targetKind,
          targetId: schema.balances.targetId,
          currency: schema.balances.currency,
          amount: schema.balances.amount,
          planName: schema.subscriptionPlans.name,
          subscriptionStartDate: schema.subscriptions.startDate,
          balanceCreatedAt: schema.balances.createdAt,
        })
        .from(schema.balances)
        .leftJoin(
          schema.subscriptions,
          and(
            eq(schema.balances.targetKind, "subscription"),
            eq(schema.subscriptions.id, schema.balances.targetId),
          ),
        )
        .leftJoin(
          schema.subscriptionPlans,
          eq(schema.subscriptionPlans.id, schema.subscriptions.planId),
        )
        .where(
          and(
            eq(schema.balances.memberId, memberId),
            gt(schema.balances.amount, 0),
          ),
        );

      // D-01/D-05: effectiveDate = subscriptions.startDate (subscription) | balances.createdAt date (debt_balance fallback).
      // D-04: ageInDays = max(0, DATEDIFF(today, effectiveDate)).
      const MONTHS_ES = [
        "Enero", "Febrero", "Marzo", "Abril", "Mayo", "Junio",
        "Julio", "Agosto", "Septiembre", "Octubre", "Noviembre", "Diciembre",
      ];

      const today = new Date();
      today.setHours(0, 0, 0, 0);

      const concepts: OutstandingConcept[] = rows.map((r) => {
        let effectiveDate: string;
        let description: string;

        if (r.targetKind === "subscription" && r.subscriptionStartDate) {
          effectiveDate = r.subscriptionStartDate; // 'YYYY-MM-DD'
          const d = new Date(effectiveDate + "T00:00:00");
          const month = MONTHS_ES[d.getMonth()];
          const year = d.getFullYear();
          const planName = r.planName ?? "Plan";
          // D-06: "Mensualidad <Mes> <Año> — <PlanName>"
          description = `Mensualidad ${month} ${year} — ${planName}`;
        } else {
          // D-06 fallback: 'Saldo libre #<id>'. effectiveDate = balances.createdAt (date portion).
          const created = r.balanceCreatedAt instanceof Date ? r.balanceCreatedAt : new Date(r.balanceCreatedAt);
          effectiveDate = created.toISOString().slice(0, 10);
          description = `Saldo libre #${r.targetId}`;
        }

        const effDate = new Date(effectiveDate + "T00:00:00");
        const diffMs = today.getTime() - effDate.getTime();
        const ageInDays = Math.max(0, Math.floor(diffMs / (1000 * 60 * 60 * 24)));

        return {
          targetKind: r.targetKind,
          targetId: r.targetId,
          description,
          currency: r.currency,
          balance: r.amount,
          ageInDays,
          effectiveDate,
        };
      });

      // D-01: Sort by effectiveDate ASC (FIFO). Estable across mixed targetKinds.
      concepts.sort((a, b) => a.effectiveDate.localeCompare(b.effectiveDate));

      return concepts;
    }
    ```

    Imports a agregar/verificar al top del archivo:
    - `import { and, eq, gt } from "drizzle-orm";` — `gt` puede faltar; agregar.
    - `import type { OutstandingConcept } from "./types.js";` — agregar al export (verificar .js extension match con resto del proyecto).

    **NO usar SQL DATEDIFF** — clamp se hace en TS para portabilidad y para evitar drift de timezone con `CURDATE()`. La query devuelve `subscriptionStartDate` y `balanceCreatedAt`, y TS computa `ageInDays`.

    **Step 2 — Mount handler in `members/routes.ts`** (per 108-PATTERNS §1, copy `financial-history` pattern verbatim):

    Localizar el handler `GET /:userId/financial-history` (~línea 714) y agregar INMEDIATAMENTE DESPUÉS un nuevo handler:

    ```typescript
    // Phase 108: GET /:userId/outstanding-concepts
    // D-01/D-02/D-03 + RBAC FINANCE_READ_ROLES + cross-country 404 (info-leak prevention).
    fastify.get<{ Params: { userId: number } }>(
      "/:userId/outstanding-concepts",
      { schema: outstandingConceptsSchema },
      async (request, reply) => {
        try {
          // D-04 privacy override (igual que financial-history)
          if (!(FINANCE_READ_ROLES as readonly string[]).includes(request.user.role)) {
            return reply.code(403).send({
              error: "Acceso denegado",
              message: "No tienes permiso para ver los saldos pendientes",
            });
          }

          // T-106-02 — verify target member + country scope
          const [target] = await fastify.db
            .select({
              id: schema.users.id,
              deletedAt: schema.users.deletedAt,
              branchCountry: schema.branches.country,
              branchIsVirtual: schema.branches.isVirtual,
            })
            .from(schema.users)
            .innerJoin(schema.branches, eq(schema.branches.id, schema.users.branchId))
            .where(eq(schema.users.id, request.params.userId))
            .limit(1);

          if (!target || target.deletedAt) {
            return reply.code(404).send({ error: "No encontrado", message: "Miembro no encontrado" });
          }
          if (
            !request.scope.isOwner &&
            !target.branchIsVirtual &&
            target.branchCountry !== request.scope.country
          ) {
            return reply.code(404).send({ error: "No encontrado", message: "Miembro no encontrado" });
          }

          const concepts = await transactionService.getOutstandingConcepts(request.params.userId);
          return { concepts };
        } catch (err: unknown) {
          handleServiceError(err, reply, request.log, "get outstanding concepts");
        }
      },
    );
    ```

    Imports a agregar al top del archivo (verificar):
    - `import { outstandingConceptsSchema } from "../finance/schemas.js";` — agregar a la línea de import de schemas.
    - `FINANCE_READ_ROLES` — ya debería estar importado (financial-history lo usa).

    **NO modificar** el handler `financial-history` existente. NO cambiar el module-level `addHook` (líneas 100-109).

  </action>

  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-api && pnpm typecheck 2>&1 | tail -10 && pnpm lint 2>&1 | tail -10</automated>
  </verify>

<acceptance_criteria> - `grep -n "getOutstandingConcepts" el-templo-api/src/modules/finance/transaction-service.ts` retorna ≥1 match (definición). - `grep -n "outstanding-concepts" el-templo-api/src/modules/members/routes.ts` retorna ≥1 match (route). - `grep -n "outstandingConceptsSchema" el-templo-api/src/modules/members/routes.ts` retorna ≥1 match (import + use). - `grep -nE "import .* gt .* from \"drizzle-orm\"|import .*\\{.*gt.*\\}.*drizzle-orm" el-templo-api/src/modules/finance/transaction-service.ts` retorna match (gt importado). - `cd el-templo-api && pnpm typecheck` exits 0. - `cd el-templo-api && pnpm lint` exits 0. - El handler usa `FINANCE_READ_ROLES` (NO `MEMBER_ROLES` solo, NO `FINANCE_WRITE_ROLES`). - Cross-country guard retorna `404` (NO `403`) — `grep -nA2 "branchCountry !== request.scope.country" el-templo-api/src/modules/members/routes.ts` muestra `code(404)`. - Service NO usa SQL DATEDIFF (cálculo en TS por D-04 clamp + timezone safety) — `grep -n "DATEDIFF" el-templo-api/src/modules/finance/transaction-service.ts` retorna 0 matches.
</acceptance_criteria>

  <done>
    Endpoint `GET /api/admin/members/:userId/outstanding-concepts` montado, service method implementado, typecheck + lint verdes. Listo para tests (Plan 02).
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter el-templo-api typecheck` verde.
- `pnpm --filter el-templo-api lint` verde.
- Endpoint responde (manual smoke con curl o tests del Plan 02).
</verification>

<success_criteria>

- OutstandingConcept type + outstandingConceptsSchema exportados.
- TransactionService.getOutstandingConcepts implementado con LEFT JOIN balances + subscriptions + subscriptionPlans, filtro `amount > 0`, clamp ageInDays ≥ 0, sort por effectiveDate ASC.
- GET /api/admin/members/:userId/outstanding-concepts montado con RBAC FINANCE_READ_ROLES + cross-country 404.
- Plan 02 puede consumir el endpoint en sus integration tests.
  </success_criteria>

<output>
After completion, create `.planning/phases/108-pago-de-saldo-historial-financiero/108-01-SUMMARY.md`.
</output>
