---
phase: 100-bot-takeover-ack-debounce-and-price-trigger
plan: 04
type: execute
wave: 4
depends_on:
  - 100-01
  - 100-02
  - 100-03
files_modified:
  - el-templo-bot/test/v5-3-3-phase-100-baseline-lock.test.ts
autonomous: false
requirements:
  - TEST-01

must_haves:
  truths:
    - "Full bot suite ends at expected pass/fail counts after all 3 prior plans: 644 baseline + new unit tests (Plan 01 × 4 + Plan 02 × 6 + Plan 03 × 18+) — actual final count documented in the lock test's assertion."
    - "The 4 deferred RED tests from Phases 94 LAT + 95 DEGR-01 remain the ONLY allowed failures — no NEW RED tests introduced by Phase 100."
    - "Full api suite gains 8 passing tests from Phases 100-01 (3) + 100-02 (2) + 100-03 (3); no regressions."
    - "All Phase 99 preservation strings + PB1.E4 REGLA FUERTE + PB1_PRICE_DISCLOSURE_UNLOCKED_ADDENDUM + DEBOUNCE_TTL_SECONDS line 167 are byte-equal to master."
    - "The 6-pair sha256 cross-phase invariant block continues to hash 67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344."
    - "Human-verify checkpoint confirms the live-bot behavioral changes match CONTEXT.md's intent for all 3 items."
  artifacts:
    - path: "el-templo-bot/test/v5-3-3-phase-100-baseline-lock.test.ts"
      provides: "Phase 100 baseline lock — asserts the post-phase test counts + invariant tokens are present + no new RED tests slipped through"
      contains: "Phase 100 baseline lock"
  key_links:
    - from: "v5-3-3-phase-100-baseline-lock.test.ts"
      to: "Phases 93 + 99 + 100 invariant tokens"
      via: "Byte-pattern grep assertions against source files"
      pattern: "DEBOUNCE_TTL_SECONDS|HANDOFF_ESCALATION_PHRASE|movimiento grupal|PB1_PRICE_DISCLOSURE_UNLOCKED_ADDENDUM"
---

<objective>
Lock the post-Phase-100 baseline. Add a small bot-side "invariant lock" test that asserts the carry-forward tokens (Phase 93 DEBOUNCE_TTL_SECONDS, Phase 99 preservation strings + REGLA FUERTE + PB1 addendum, Phase 100 new constants) are all present byte-exact in source. Run the FULL bot + api test suites and pin the expected pass/fail counts (656+/4/0 bot; api baseline + 8). Finish with a human-verify checkpoint that confirms the per-item live-bot behavior matches CONTEXT.md.

Purpose: Phase 99 lesson — the FULL bot suite must be the gate at end-of-phase, not just snap-consuming files. Phase 100's three items each ship their own per-plan tests; this rollup plan ensures NO new RED test slipped through AND captures the per-item live observation under operator review.

Output: 1 new bot-side test file (invariant grep-lock); a human-verify checkpoint documenting per-item live-test observations; updated phase-level SUMMARY.
</objective>

<execution_context>
@/Users/bores/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bores/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bores/el-templo/CLAUDE.md
@/Users/bores/el-templo/.planning/STATE.md
@/Users/bores/el-templo/.planning/phases/100-bot-takeover-ack-debounce-and-price-trigger/100-CONTEXT.md
@/Users/bores/el-templo/.planning/phases/100-bot-takeover-ack-debounce-and-price-trigger/100-01-SUMMARY.md
@/Users/bores/el-templo/.planning/phases/100-bot-takeover-ack-debounce-and-price-trigger/100-02-SUMMARY.md
@/Users/bores/el-templo/.planning/phases/100-bot-takeover-ack-debounce-and-price-trigger/100-03-SUMMARY.md
@/Users/bores/el-templo/.planning/phases/99-bot-copy-and-price-disclosure-fixes/99-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Authoring the invariant baseline-lock test</name>
  <files>el-templo-bot/test/v5-3-3-phase-100-baseline-lock.test.ts</files>
  <read_first>
    - el-templo-bot/test/v5-3-2-regression.test.ts (existing phase-level lock test pattern — POST_RLOK_04_BYTES style)
    - el-templo-bot/src/webhook/handler.ts (the constants that must be present byte-equal)
    - el-templo-bot/src/ai/system-prompt.ts (PB1_PRICE_DISCLOSURE_UNLOCKED_ADDENDUM byte-equal target)
    - el-templo-bot/src/ai/knowledge.ts (Phase 99 preservation strings: movimiento grupal, sin salirte del grupo, sin salirse del grupo)
    - el-templo-bot/src/playbooks/definitions.ts (Phase 99: framings de arranque grupal, lenguaje de arranque grupal, REGLA FUERTE)
  </read_first>
  <behavior>
    Test file uses Node's `fs.readFileSync` to read source files and assert string membership. No mocks, no async — pure synchronous file-read + assertions. Test groups:

    - **Phase 93 invariants:**
      - DEBOUNCE_TTL_SECONDS line at handler.ts:167 reads exactly `const DEBOUNCE_TTL_SECONDS = Number(process.env.DEBOUNCE_TTL_SECONDS ?? 600);` (byte-exact).
      - The DEBOUNCE_TTL_SECONDS JSDoc block above it contains the formula text `(OPENAI_TIMEOUT_MS/1000) * MAX_TOOL_ITERATIONS` (signature of the cross-phase invariant).

    - **Phase 99 invariants (preservation strings):**
      - knowledge.ts contains `"movimiento grupal"`.
      - knowledge.ts contains `"sin salirte del grupo"`.
      - knowledge.ts contains `"sin salirse del grupo"`.
      - definitions.ts contains `"framings de arranque grupal"`.
      - definitions.ts contains `"lenguaje de arranque grupal"`.
      - definitions.ts contains `"REGLA FUERTE"`.

    - **Phase 99 invariants (PB1 disclosure addendum byte-equal):**
      - system-prompt.ts contains `PB1_PRICE_DISCLOSURE_UNLOCKED_ADDENDUM = ` with the addendum body starting `"\\n\\n*Desbloqueo de disclosure de precios (PB1):*"` (substring check on the first 80 characters of the addendum string literal — this is the cheapest reliable byte-equal proxy).

    - **Phase 99 invariants (PB2 / detectPriceObjection consumer):**
      - handler.ts contains `priceInsistenceCount` (the counter name from Phase 99 still exists).
      - handler.ts contains `PB1_PRICE_INSISTENCE_THRESHOLD` (or its env-default = 2).

    - **Phase 100 DBNC-01 invariants:**
      - handler.ts contains `DEBOUNCE_QUIET_WINDOW_MS = Number(process.env.DEBOUNCE_QUIET_WINDOW_MS ?? 7000)`.
      - handler.ts contains `DEBOUNCE_HARD_CAP_MS = Number(process.env.DEBOUNCE_HARD_CAP_MS ?? 30000)`.
      - handler.ts does NOT contain `DEBOUNCE_DELAY_MS` (negative assertion — confirms the old constant is fully removed).
      - .env.example contains `DEBOUNCE_QUIET_WINDOW_MS=7000` and `DEBOUNCE_HARD_CAP_MS=30000`.

    - **Phase 100 TAKE-01/TAKE-02 invariants:**
      - handler.ts contains `HANDOFF_ESCALATION_PHRASE` (byte-equal preserved).
      - handler.ts contains `TAKEOVER_REASSURANCE_PHRASE = "Alguien del equipo te va a responder a la brevedad 🙏"` (byte-exact).
      - handler.ts contains `TAKEOVER_ACK_TTL_SECONDS = Number(process.env.TAKEOVER_ACK_TTL_SECONDS ?? 3600)`.
      - handler.ts contains `wa:takeover_ack:` (Redis key prefix).
      - system-prompt.ts contains `handoffReason` (the new option field).
      - system-prompt.ts contains `buildHandoffContextAwareAddendum` OR `HANDOFF_CONTEXT_AWARE_ADDENDUM` (whichever shape Task 1 of Plan 100-02 chose).
      - .env.example contains `TAKEOVER_ACK_TTL_SECONDS=3600`.

    - **Phase 100 TRIG-01 invariants:**
      - handler.ts detectPriceObjection regex contains `precios?` (the plural-form widening).
      - handler.ts detectPriceObjection regex contains `cu[aá]nto (sale|cuesta|val[eé])`.
      - handler.ts detectPriceObjection regex contains `valor(es)?`.
      - handler.ts detectPriceObjection regex contains `tarifa|cuota|mensualidad`.
      - handler.ts contains the regex pattern EXACTLY ONCE (single source of truth — count occurrences of `caro|carisimo` in src/**/*.ts and assert === 1).

    - **HARD GUARDS:**
      - .planning/phases/93-handler-concurrency/93-CONTEXT.md exists (sanity check) and its 6-pair sha256 invariant block sha256 still matches `67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344`. NOTE: this is hard to assert from inside a vitest run cleanly (different working dirs); RECOMMENDED simpler approach: just assert that 93-CONTEXT.md contains the literal string `67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344` somewhere in the file (the sha256 documentation itself; byte-equality of the documented hash is enough as a tripwire — if someone modifies the invariant block the documented hash diverges and this test catches it).

  </behavior>
  <action>
    Create `el-templo-bot/test/v5-3-3-phase-100-baseline-lock.test.ts`:

    1. Import `fs` and `path` from node. Define a helper `readSrc(relativePath: string): string` that reads from a stable basePath (use `path.join(__dirname, "..", "src", ...)`).
    2. Top-level `describe("v5.3.3 Phase 100 — Baseline Invariant Lock", () => { ... })`.
    3. Nested `describe`s for each group in `<behavior>`: "Phase 93 invariants", "Phase 99 invariants", "Phase 100 DBNC-01", "Phase 100 TAKE-01/02", "Phase 100 TRIG-01", "HARD GUARDS".
    4. Each `it` body: read the file, assert substring presence (or absence — `.not.toContain`). For the single-source-of-truth check on the regex, use `(text.match(/caro\|carisimo/g) ?? []).length` and assert `=== 1`.
    5. For the .env.example reads, navigate from `__dirname` to `path.join(__dirname, "..", ".env.example")`.
    6. For the 93-CONTEXT.md sha256-tripwire assertion, navigate from `__dirname` (which is `el-templo-bot/test/`) to `path.join(__dirname, "..", "..", ".planning", "phases", "93-handler-concurrency", "93-CONTEXT.md")`. Wrap in try/catch and skip with a `it.skip` if the file isn't found (some CI environments may not check out .planning/ — but our local + GitHub Actions setup does; treat skip as a SOFT signal, never as PASS).

    This test file is fast (no mocks, no async, no DB) — should add < 100ms to the suite.

    Do NOT modify any source file or any other test file in this task.

  </action>
  <verify>
    <automated>cd /Users/bores/el-templo/el-templo-bot && pnpm test test/v5-3-3-phase-100-baseline-lock.test.ts -- --run 2>&1 | tail -30</automated>
    <automated>cd /Users/bores/el-templo/el-templo-bot && pnpm tsc --noEmit 2>&1 | tail -10</automated>
    <automated>grep -q "Phase 93 invariants\|DEBOUNCE_TTL_SECONDS" /Users/bores/el-templo/el-templo-bot/test/v5-3-3-phase-100-baseline-lock.test.ts</automated>
    <automated>grep -q "Phase 100 TRIG-01\|precios?" /Users/bores/el-templo/el-templo-bot/test/v5-3-3-phase-100-baseline-lock.test.ts</automated>
    <automated>grep -q "67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344" /Users/bores/el-templo/el-templo-bot/test/v5-3-3-phase-100-baseline-lock.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `el-templo-bot/test/v5-3-3-phase-100-baseline-lock.test.ts` exists.
    - All baseline-lock assertions pass: `cd el-templo-bot && pnpm test test/v5-3-3-phase-100-baseline-lock.test.ts -- --run` exits 0.
    - `cd el-templo-bot && pnpm tsc --noEmit` exits 0.
    - The lock test asserts at least 20 distinct invariants (6 Phase 93 + 6 Phase 99 + 4 Phase 100 DBNC + 7 Phase 100 TAKE + 5 Phase 100 TRIG + 1 sha256 tripwire = ~29 individual `it` cases).
    - Full bot suite: `cd el-templo-bot && pnpm test -- --run` ends at the final Phase 100 baseline (post-Plans 01/02/03 baseline + ~29 from this lock test); 4 failures (deferred RED) remain the ONLY allowed failures.
  </acceptance_criteria>
  <done>
    Baseline-lock test file authored with ≥20 invariant assertions covering Phases 93 + 99 + 100 carry-forward tokens; full bot suite green at the final phase baseline; tsc clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator human-verify checkpoint — per-item live observation</name>
  <what-built>
    Three independent bot-behavior follow-ups, all bot-side:
    - **DBNC-01:** trailing-debounce poll-and-extend loop in handler.ts (replaces fixed 3s setTimeout); env-overridable via DEBOUNCE_QUIET_WINDOW_MS (default 7000) + DEBOUNCE_HARD_CAP_MS (default 30000); preserves Phase 93 SETNX serialization.
    - **TAKE-01:** context-aware handoff addendum injected into the system prompt when handoffReason is set (multi-step model-driven handoff continuation).
    - **TAKE-02:** rate-limited static reassurance ("Alguien del equipo te va a responder a la brevedad 🙏") at the human_takeover early-return; Redis key `wa:takeover_ack:<phone>` with TTL=TAKEOVER_ACK_TTL_SECONDS (default 3600s).
    - **TRIG-01:** widened detectPriceObjection regex covers price QUESTIONS (precios?, cuánto sale|cuesta|vale, valor(es), tarifa, cuota, mensualidad) in addition to existing objection-shaped terms.
  </what-built>
  <how-to-verify>
    **Pre-flight (operator runs locally):**

    1. Confirm full suites green:
       - `cd el-templo-bot && pnpm test -- --run 2>&1 | tail -10` — expect Phase 100 final baseline; 4 deferred RED tests are the ONLY allowed failures.
       - `cd el-templo-api && pnpm test -- --run 2>&1 | tail -10` — expect Phase 99 baseline (537/1) + Phase 100 additions (3 from 100-01 + 2 from 100-02 + 3 from 100-03 = +8 passing) = 545/1.
    2. Confirm tsc clean: `cd el-templo-bot && pnpm tsc --noEmit && cd ../el-templo-api && pnpm tsc --noEmit`.
    3. Confirm no API runtime changes: `git diff master -- 'el-templo-api/src/**' | wc -l` returns 0.
    4. Confirm HARD GUARDS: `grep -q "DEBOUNCE_TTL_SECONDS = Number(process.env.DEBOUNCE_TTL_SECONDS ?? 600)" el-templo-bot/src/webhook/handler.ts && echo OK`.

    **Live observation (operator runs the bot locally with the dev WhatsApp test number — same setup used for Phases 93/99 live-test):**

    **DBNC-01 — manual timing observation:**
    - Send a single message: "Hola". Observe: bot replies in ~7s (the new quiet-window), NOT 3s (legacy) and NOT 30s (cap). PASS if reply lands within 6-10s.
    - Send 3 rapid messages, ~1s apart: "Hola" → "buenas" → "una consulta". Observe: bot aggregates and replies ONCE, ~7s AFTER THE LAST message (~10s total from first inbound). PASS if exactly 1 reply that reflects context of all 3.
    - Send continuous typing simulation — message every ~500ms for 35 seconds (use a script if needed). Observe: bot fires once at ~30s (cap), NOT later. Check pino logs for `firedReason: "cap"`. PASS if reply lands within 28-33s AND log contains the cap reason.

    **TAKE-02 — manual takeover observation:**
    - Admin/coach side: trigger human_takeover for an active conversation (via the existing admin UI or DB toggle).
    - Send an inbound from the lead side: "¿hola? ¿alguien?". Observe: bot replies EXACTLY ONCE with "Alguien del equipo te va a responder a la brevedad 🙏" (byte-exact, with the 🙏 emoji). PASS.
    - Send another inbound within ~30s: "¿hay alguien?". Observe: bot is SILENT (no second reassurance). PASS.
    - Optional (slow path): wait > 3600s and send another inbound — second reassurance fires (expected per CONTEXT.md). Document as a separate observation; not required for PASS gate but useful to record if the operator can wait.

    **TAKE-01 — context-aware handoff observation (best-effort; multi-turn handoff path):**
    - In a conversation where the model emits `request_human` mid-tool-loop (e.g., the lead says "estoy lesionado, necesito hablar con alguien"), observe whether the model's user-facing handoff text references the SPECIFIC reason (e.g., "vi que estás lesionado, te paso...").
    - Acceptance: the model emits a context-aware handoff that references the specific reason. If the model emits the generic phrase, that is an acceptable single-step fallback (TAKE-01 covers multi-step continuation; see Plan 100-02 ACCEPTED LIMITATION in the SUMMARY).
    - Document observed model output verbatim in the SUMMARY.

    **TRIG-01 — price-question observation:**
    - In a PB1 lead conversation: send "¿cuánto cuesta?" THREE times (with neutral filler between). Observe: on the 3rd inbound, the bot SHOULD respond with plan prices (Phase 99 disclosure unlocked). PASS if disclosure fires; FAIL if the bot keeps deferring on price.
    - Send "preciosa idea" in any conversation. Observe: the bot does NOT treat this as a price-curious inbound (no counter increment, no PB2.E2 routing diversion). PASS.
    - In a PB2.E2 conversation: send "¿cuál es el plan más barato?". Observe: bot replies cleanly (any on-topic non-broken response). PASS if no crash + on-topic.

    **Aggregate signal:** for each of the 4 items, mark PASS / PARTIAL / FAIL with a one-line verbatim quote of the bot's reply.

  </how-to-verify>
  <resume-signal>
    Type "approved" with the per-item observation block (DBNC-01: PASS|PARTIAL|FAIL + quote; TAKE-02: PASS|PARTIAL|FAIL + quote; TAKE-01: PASS|PARTIAL|FAIL + quote; TRIG-01: PASS|PARTIAL|FAIL + quote). PARTIAL or FAIL on any item triggers gap-closure planning (`/gsd:plan-phase 100 --gaps`); ALL PASS triggers phase ship + ROADMAP update.
  </resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                                              |
| ---------- | -------------------------------------------------------------------------------------------------------- |
| (None new) | This plan adds a static-grep lock test + human-verify checkpoint. No new code paths or runtime surfaces. |

## STRIDE Threat Register

| Threat ID | Category                           | Component                            | Disposition | Mitigation Plan                                                                                                                                                                                                 |
| --------- | ---------------------------------- | ------------------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-100-12  | Tampering (silent invariant drift) | carry-forward tokens (Phase 93 + 99) | mitigate    | Baseline-lock test reads source files and asserts byte-equal presence of ~20 invariant tokens. Any silent edit to DEBOUNCE_TTL_SECONDS, preservation strings, REGLA FUERTE, or the PB1 addendum trips the test. |

**block_on:** high (the human-verify checkpoint is intentionally blocking).
</threat_model>

<verification>
- `cd el-templo-bot && pnpm test test/v5-3-3-phase-100-baseline-lock.test.ts -- --run` exits 0 with ≥20 invariant assertions GREEN.
- `cd el-templo-bot && pnpm test -- --run` final count documented in SUMMARY (4 deferred RED tests are the ONLY allowed failures).
- `cd el-templo-api && pnpm test -- --run` final count documented in SUMMARY (Phase 99 baseline 537/1 + Phase 100 additions = expected 545/1).
- Operator's per-item PASS|PARTIAL|FAIL observation block captured verbatim in the phase-level SUMMARY (per Phase 97-style guidance — inline transcript, not just a checkbox).
</verification>

<success_criteria>

- Invariant baseline-lock test ships with ≥20 distinct assertions covering Phase 93 + 99 + 100 carry-forward tokens.
- Full bot suite green at the final Phase 100 baseline; full api suite at Phase 99 baseline + 8 new passing tests.
- Operator human-verify checkpoint completes with all 4 items recorded as PASS or PARTIAL (FAIL on any item triggers gap-closure replan).
- Phase-level SUMMARY captures per-item observations + final test counts + confirmation of all HARD GUARDS.
  </success_criteria>

<output>
Create `.planning/phases/100-bot-takeover-ack-debounce-and-price-trigger/100-04-SUMMARY.md` AND `.planning/phases/100-bot-takeover-ack-debounce-and-price-trigger/100-VERIFICATION.md` when done. The VERIFICATION.md is the phase-level rollup: list each of the 5 internal requirement labels (DBNC-01, TAKE-01, TAKE-02, TRIG-01, TEST-01) with their PASS/PARTIAL/FAIL disposition + the verbatim operator observation quote + the test-count evidence. If any item is PARTIAL or FAIL, list the gap and recommend `/gsd:plan-phase 100 --gaps` as the next step. If all PASS, recommend the ROADMAP update + final commit message format `docs(100): close phase 100 — DBNC-01 + TAKE-01/02 + TRIG-01 + TEST-01 PASSED`.
</output>
</content>
</invoke>
