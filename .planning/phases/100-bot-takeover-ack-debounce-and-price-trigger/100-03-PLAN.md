---
phase: 100-bot-takeover-ack-debounce-and-price-trigger
plan: 03
type: execute
wave: 3
depends_on:
  - 100-02
files_modified:
  - el-templo-bot/src/webhook/handler.ts
  - el-templo-bot/test/v5-3-3-phase-100-price-trigger.test.ts
  - el-templo-api/test/whatsapp/v5-3-3-phase-100-price-trigger.integration.test.ts
autonomous: true
requirements:
  - TRIG-01
  - TEST-01
user_setup: []

must_haves:
  truths:
    - "detectPriceObjection (handler.ts:~1658) matches: caro, carisimo, car[ií]simo, precios? (singular + plural), no me alcanza, no puedo pagar, muy caro, barato, descuento, cuánto sale/cuesta/vale (with or without accent), valor(es), tarifa, cuota, mensualidad."
    - "Word boundaries (\\b) exclude false positives: \"preciosa idea\", \"preciosos paisajes\", and \"valoro la propuesta\" do NOT match (verified by negative test cases). Note: \"sin precio fijo de antemano\" DOES match — `precio` is a whole word there; it is a VALID match (lead mentioning price as a topic), asserted shouldMatch:true in the fixture (~line 138). Keep fixture and this truth consistent."
    - "Single source of truth at handler.ts:~1658 preserved — no parallel regex anywhere in el-templo-bot/src/**. `grep -rE \"caro\\|carisimo\" el-templo-bot/src/ --include=\"*.ts\"` returns EXACTLY 1 match (the helper body)."
    - 'Both consumers continue to function correctly: (a) Phase 99 PB1 priceInsistenceCount increments on question-shaped triggers like "¿cuánto cuesta?"; (b) PB2.E2 path handles neutral price questions without crashing.'
    - "Phase 99's PB1_PRICE_INSISTENCE_THRESHOLD (default 2) + PB1_PRICE_DISCLOSURE_UNLOCKED_ADDENDUM mechanism still triggers correctly — 3rd insistence (with question-shaped triggers) unlocks the disclosure addendum."
    - "Full bot test suite ends at ≥ (post-Wave-2 baseline) + (new tests in this plan) passed / **0 failed** / 0 todo. NO deferred RED carry-forward on the bot side — Wave 1's driver update at commit `af1db504` resolved all 4 previously-timing-blocked Phase 94+95 RED tests (post-Wave-1 baseline: 662/0/0). ANY bot-suite failure here is a regression — STOP and surface."
    - "Full api suite ends at ≥ 537 (Phase 99 deterministic baseline) + (new passing tests from this plan) passed / failures = 1 DETERMINISTIC (Phase 95 BUG-03 (i) LIKE-search RED at `tools.ts:455`, always fails) PLUS 0–3 INTERMITTENT pre-existing drift-flake failures in `el-templo-api/test/lint/raw-sql-column-drift.test.ts` + `el-templo-api/test/whatsapp/ai-tools-membership-drift.test.ts` (Phase 97.5/98-owned; the membership-drift flake is documented in `98-FINDINGS-phase-97-bound.md` / 98-FINDING-01, MySQL shared-state race; Wave 2 confirmed they are NOT Phase-100-caused) / 1 todo. Do NOT pin to an exact failed-count — the drift files are non-deterministic; on a clean-flake run the suite shows only 1 failed, on a bad run up to 4. A failure in ANY OTHER api test file = a Phase-100 regression — re-run once to confirm it's not a drift-flake; if it persists, STOP and surface."
  artifacts:
    - path: "el-templo-bot/src/webhook/handler.ts"
      provides: "Widened detectPriceObjection regex covering questions in addition to objections"
      contains: "cu[aá]nto (sale|cuesta|val[eé])"
    - path: "el-templo-bot/test/v5-3-3-phase-100-price-trigger.test.ts"
      provides: "Data-driven unit fixture covering each new phrase + negative word-boundary cases"
      contains: "PRICE_TRIGGER_FIXTURES"
    - path: "el-templo-api/test/whatsapp/v5-3-3-phase-100-price-trigger.integration.test.ts"
      provides: "End-to-end PB1 counter test (question-shaped 3rd insistence → disclosure addendum) + PB2.E2 neutral-question test"
      contains: "Phase 100 TRIG-01 integration"
  key_links:
    - from: "el-templo-bot/src/webhook/handler.ts (detectPriceObjection at :1658)"
      to: "Phase 99 PB1 priceInsistenceCount mechanism (counter-increment site + disclosureUnlocked flag)"
      via: "Widened regex now fires for question-shaped inputs; PB1 counter accumulates per insistence; threshold-cross unlocks disclosure addendum"
      pattern: "priceInsistenceCount|disclosureUnlocked"
    - from: "el-templo-bot/src/webhook/handler.ts (detectPriceObjection at :1658)"
      to: "PB2.E2 prompt selector / handler decisions"
      via: "Same helper; neutral price questions now route through PB2.E2 (acceptable per CONTEXT.md — mildly-defensive but on-topic response, not a regression)"
      pattern: "computeAdvanceSignals|signals.priceObjection"
---

<objective>
Implement TRIG-01 — widen the `detectPriceObjection` regex at `handler.ts:~1658` so it matches price QUESTIONS (not just objections). Add unit tests using a data-driven fixture covering each new matched phrase + negative word-boundary cases. Add integration tests verifying both consumers of the helper continue to function: (a) Phase 99 PB1 counter increments on question-shaped triggers and unlocks the disclosure addendum on the 3rd insistence; (b) PB2.E2 path handles neutral price questions cleanly.

Purpose: Live-test finding — Phase 99's price-disclosure mechanism keys off `detectPriceObjection`, which only matches objection-shaped phrases (e.g., "es caro", "no puedo pagar"). Genuine price-curious leads asking "¿cuánto cuesta?" or "¿cuál es el precio?" never increment the counter, so the 3rd-insistence disclosure never fires for them. Widening the regex closes the gap without introducing a parallel regex (single source of truth preserved).

Output: Modified `handler.ts` (one-line regex widening + JSDoc update) + 1 unit-test file with data-driven fixture + 1 integration-test file covering both downstream consumers.
</objective>

<execution_context>
@/Users/bores/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bores/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bores/el-templo/CLAUDE.md
@/Users/bores/el-templo/.planning/STATE.md
@/Users/bores/el-templo/.planning/phases/100-bot-takeover-ack-debounce-and-price-trigger/100-CONTEXT.md
@/Users/bores/el-templo/.planning/phases/100-bot-takeover-ack-debounce-and-price-trigger/100-01-SUMMARY.md
@/Users/bores/el-templo/.planning/phases/100-bot-takeover-ack-debounce-and-price-trigger/100-02-SUMMARY.md
@/Users/bores/el-templo/.planning/phases/99-bot-copy-and-price-disclosure-fixes/99-02-SUMMARY.md
@/Users/bores/el-templo/.planning/phases/99-bot-copy-and-price-disclosure-fixes/99-03-SUMMARY.md
@/Users/bores/el-templo/el-templo-bot/src/webhook/handler.ts
@/Users/bores/el-templo/el-templo-api/test/whatsapp/v5-3-3-phase-99-copy-and-price.integration.test.ts

<interfaces>
<!-- Source-of-truth identifier -->

From `el-templo-bot/src/webhook/handler.ts:1658`:

- `export function detectPriceObjection(inboundLower: string): boolean` — single source of truth. Current body (master):
  `return /\b(caro|carisimo|car[ií]simo|precio|no me alcanza|no puedo pagar|muy caro|barato|descuento)\b/i.test(inboundLower);`

The two consumers (per the JSDoc above the helper):

- `computeAdvanceSignals` — post-AI; sets `signals.priceObjection` for PB2.E2 advance logic.
- Pre-AI counter-increment site (Phase 99, somewhere in `processWithAiInner`) — drives `newPriceInsistenceCount` → `setPlaybookState` → `disclosureUnlocked` flag passed to `getSystemPrompt`.

The widening (LOCKED at plan-time, regex literal byte-exact):
`return /\b(caro|carisimo|car[ií]simo|precios?|no me alcanza|no puedo pagar|muy caro|barato|descuento|cu[aá]nto (sale|cuesta|val[eé])|valor(es)?|tarifa|cuota|mensualidad)\b/i.test(inboundLower);`

Changes:

- `precio` → `precios?` (plural form)
- Added: `cu[aá]nto (sale|cuesta|val[eé])` (with/without accent on cuánto + sale/cuesta/vale)
- Added: `valor(es)?` (singular + plural)
- Added: `tarifa`, `cuota`, `mensualidad`
- Kept verbatim: `caro|carisimo|car[ií]simo|no me alcanza|no puedo pagar|muy caro|barato|descuento`
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author unit-test fixture with data-driven cases (RED first)</name>
  <files>el-templo-bot/test/v5-3-3-phase-100-price-trigger.test.ts</files>
  <read_first>
    - el-templo-bot/src/webhook/handler.ts (current detectPriceObjection helper at :1658 + JSDoc above it)
    - el-templo-bot/test/v5-3-3-handler-concurrency.test.ts (existing unit-test scaffolding pattern — import structure, describe/it shape)
    - .planning/phases/100-bot-takeover-ack-debounce-and-price-trigger/100-CONTEXT.md (the PRICE_TRIGGER_FIXTURES shape suggested in `<specifics>`)
  </read_first>
  <behavior>
    Data-driven fixture in shape `Array<{ phrase: string; shouldMatch: boolean; rationale: string }>`. Each entry runs through `detectPriceObjection` (after lowercasing as the helper expects — the helper takes `inboundLower`, so the test lowercases first). Tests:

    **Positive cases (shouldMatch: true) — pre-Phase-100 baseline (must still match after widening):**
    - "es caro" → true (existing)
    - "carisimo!" → true (existing)
    - "carísimo" → true (existing accented variant)
    - "no me alcanza" → true (existing)
    - "no puedo pagar" → true (existing)
    - "muy caro" → true (existing)
    - "es barato" → true (existing)
    - "hay descuento?" → true (existing)
    - "el precio" → true (existing singular)

    **Positive cases (shouldMatch: true) — Phase 100 widening:**
    - "los precios" → true (plural)
    - "¿cuánto cuesta?" → true (PRIMARY live-test gap — question form)
    - "cuanto cuesta" → true (no accent + no question mark)
    - "¿cuánto sale?" → true
    - "¿cuánto vale?" → true
    - "cuanto vale" → true (no accent)
    - "el valor" → true (singular)
    - "los valores" → true (plural)
    - "qué tarifa tienen" → true
    - "la cuota mensual" → true
    - "cuánto sale la mensualidad" → true

    **Negative cases (shouldMatch: false) — word-boundary adversarial:**
    - "preciosa idea" → false (`\b` should exclude — `precios` is not a whole word here because `preciosa` ≠ `precios` + word boundary; verify the regex's `\b` behavior carefully)
    - "preciosos paisajes" → false (`preciosos` ≠ `precios` + boundary)
    - "sin precio fijo de antemano" → true (this IS a valid match — `precio` is a whole word; document in rationale as "valid match — the lead is mentioning price as a topic")
    - "hola buenas" → false (no price terms)
    - "tengo una lesión" → false (unrelated)
    - "cuántos años tenés" → false (`cuántos` is not followed by `sale|cuesta|vale` — regex requires the bigram)
    - "valoro la propuesta" → false (`valoro` ≠ `valor` + boundary because `\b` checks word boundaries on both sides; `valoro` is one word so `valor` does not match with `\b...\b` anchors)

    **Note on `valor` boundary:** the regex `\bvalor(es)?\b` requires a word boundary AFTER `valor` or `valores`. In "valoro", `valor` is followed by `o` (a word character), so `\b` after `valor` fails. Confirm this in test; if it surfaces as a false-positive at run-time, the test catches it.

  </behavior>
  <action>
    Create `el-templo-bot/test/v5-3-3-phase-100-price-trigger.test.ts`:

    1. Import `detectPriceObjection` from `../src/webhook/handler` (verify the helper is exported — it is per the existing JSDoc).
    2. Declare a typed fixture: `const PRICE_TRIGGER_FIXTURES: Array<{ phrase: string; shouldMatch: boolean; rationale: string }> = [ ... ]` with all the cases enumerated in `<behavior>`.
    3. Top-level `describe("v5.3.3 Phase 100 TRIG-01 — widened detectPriceObjection", () => { ... })`.
    4. Inside, two `describe` blocks: "Positive cases" and "Negative cases (word-boundary)".
    5. Use `it.each(PRICE_TRIGGER_FIXTURES.filter(f => f.shouldMatch))` and `it.each(PRICE_TRIGGER_FIXTURES.filter(f => !f.shouldMatch))` to iterate. Each `it` body: `expect(detectPriceObjection(phrase.toLowerCase())).toBe(shouldMatch);` — the helper expects pre-lowercased input.
    6. Add one extra check at the bottom: a "smoke" `it` that confirms the helper exists and is a function (defensive against import-shape changes).

    **IMPORTANT — this task is RED-first (TDD).** The test is authored against the WIDENED regex (which doesn't exist yet on master). When run BEFORE Task 2 lands, the new Phase 100 widening cases FAIL. After Task 2 lands they all PASS.

    Verify gate authorized RED count: ~10 of the ~20 positive cases will fail on master (the new widening cases). Document the expected RED count in the test file's top comment so the executor can validate before-and-after.

  </action>
  <verify>
    <automated>cd /Users/bores/el-templo/el-templo-bot && pnpm test test/v5-3-3-phase-100-price-trigger.test.ts -- --run 2>&1 | tail -25</automated>
    <automated>grep -q "PRICE_TRIGGER_FIXTURES" /Users/bores/el-templo/el-templo-bot/test/v5-3-3-phase-100-price-trigger.test.ts</automated>
    <automated>grep -q "cuánto cuesta" /Users/bores/el-templo/el-templo-bot/test/v5-3-3-phase-100-price-trigger.test.ts</automated>
    <automated>grep -q "preciosa idea" /Users/bores/el-templo/el-templo-bot/test/v5-3-3-phase-100-price-trigger.test.ts</automated>
    <automated>cd /Users/bores/el-templo/el-templo-bot && pnpm tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `el-templo-bot/test/v5-3-3-phase-100-price-trigger.test.ts` exists.
    - `grep -q "PRICE_TRIGGER_FIXTURES" el-templo-bot/test/v5-3-3-phase-100-price-trigger.test.ts` exits 0.
    - Fixture contains at least 18 entries (≥9 pre-Phase-100 positives + ≥9 Phase 100 widening + ≥4 negative cases).
    - `cd el-templo-bot && pnpm tsc --noEmit` exits 0.
    - **RED gate (TDD authorization):** `cd el-templo-bot && pnpm test test/v5-3-3-phase-100-price-trigger.test.ts -- --run 2>&1 | grep -E "Tests +[1-9][0-9]* +failed"` reports failures (the widening cases). This is EXPECTED — the test goes GREEN only after Task 2 ships the regex change.
    - Full bot baseline suite: `cd el-templo-bot && pnpm test test/v5-3-3-degr-01-escalation.test.ts test/v5-3-3-handler-concurrency.test.ts test/v5-3-2-regression.test.ts -- --run` shows no regressions in pre-existing tests.
  </acceptance_criteria>
  <done>
    Data-driven fixture authored with ≥18 cases (positive baseline + Phase 100 widening + negative word-boundary). The widening cases fail RED on master; pre-existing tests unchanged. Ready for Task 2 to flip RED → GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen detectPriceObjection regex (RED → GREEN)</name>
  <files>el-templo-bot/src/webhook/handler.ts</files>
  <read_first>
    - el-templo-bot/src/webhook/handler.ts (detectPriceObjection at :1658 + the JSDoc immediately above mentioning the dual consumers)
    - el-templo-bot/test/v5-3-3-phase-100-price-trigger.test.ts (the test authored in Task 1 — this regex change must make all positive cases GREEN and all negative cases stay GREEN)
  </read_first>
  <behavior>
    - After the change, all 18+ unit-test cases from Task 1 pass GREEN.
    - The helper signature is UNCHANGED (`(inboundLower: string): boolean`).
    - The JSDoc above the helper is updated to mention Phase 100 widening + the new captured phrase categories (precios/cuánto X/valor/tarifa/cuota/mensualidad).
    - No new helper, no new file, no parallel regex.
  </behavior>
  <action>
    Surgical edit to `el-templo-bot/src/webhook/handler.ts`:

    1. **Replace the regex literal** on the line after the `detectPriceObjection` JSDoc end (currently `~:1658`) with the widened form:
       `return /\b(caro|carisimo|car[ií]simo|precios?|no me alcanza|no puedo pagar|muy caro|barato|descuento|cu[aá]nto (sale|cuesta|val[eé])|valor(es)?|tarifa|cuota|mensualidad)\b/i.test(inboundLower);`

       Compare to the master form:
       `return /\b(caro|carisimo|car[ií]simo|precio|no me alcanza|no puedo pagar|muy caro|barato|descuento)\b/i.test(inboundLower);`

       Diff is one line. The new alternation members are: `precios?` (replacing `precio`), `cu[aá]nto (sale|cuesta|val[eé])`, `valor(es)?`, `tarifa`, `cuota`, `mensualidad`.

    2. **Update the JSDoc above the helper** to note Phase 100 widening. Add a line near the existing single-source-of-truth note:
       "Phase 100 TRIG-01 (2026-06-24): widened from objection-only to also match price QUESTIONS (precios? / cuánto sale|cuesta|vale / valor(es) / tarifa / cuota / mensualidad). Single source of truth preserved — do NOT introduce a parallel regex. Both consumers (Phase 99 PB1 counter + PB2.E2 path) continue to function; PB2.E2 routing for neutral price questions is acceptable per Phase 100 CONTEXT decisions."

    3. **No other edits.** Do NOT modify `computeAdvanceSignals`, the PB1 counter-increment site, the PB2.E2 advance logic, `setPlaybookState`, `getSystemPrompt`, or any test file. Plan 100-02 already wired TAKE-01/TAKE-02; Plan 100-03 changes ONLY this one regex literal + its JSDoc.

  </action>
  <verify>
    <automated>grep -q "precios?|no me alcanza" /Users/bores/el-templo/el-templo-bot/src/webhook/handler.ts</automated>
    <automated>grep -q "cu\[aá\]nto (sale|cuesta|val\[eé\])" /Users/bores/el-templo/el-templo-bot/src/webhook/handler.ts</automated>
    <automated>grep -q "valor(es)?" /Users/bores/el-templo/el-templo-bot/src/webhook/handler.ts</automated>
    <automated>grep -q "tarifa|cuota|mensualidad" /Users/bores/el-templo/el-templo-bot/src/webhook/handler.ts</automated>
    <automated>SINGLE_SOURCE=$(grep -rE "caro\|carisimo" /Users/bores/el-templo/el-templo-bot/src/ --include="*.ts" | wc -l); test "$SINGLE_SOURCE" -eq 1</automated>
    <automated>cd /Users/bores/el-templo/el-templo-bot && pnpm test test/v5-3-3-phase-100-price-trigger.test.ts -- --run 2>&1 | tail -10</automated>
    <automated>cd /Users/bores/el-templo/el-templo-bot && pnpm tsc --noEmit 2>&1 | tail -10</automated>
    <automated>cd /Users/bores/el-templo/el-templo-bot && pnpm test -- --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Single source of truth gate: `grep -rE "caro\|carisimo" el-templo-bot/src/ --include="*.ts" | wc -l` returns EXACTLY 1 (the helper body — no parallel regex).
    - Widened tokens present: `grep -q "precios?|no me alcanza" el-templo-bot/src/webhook/handler.ts`; `grep -q "cu\[aá\]nto (sale|cuesta|val\[eé\])" el-templo-bot/src/webhook/handler.ts`; `grep -q "valor(es)?" el-templo-bot/src/webhook/handler.ts`; `grep -q "tarifa|cuota|mensualidad" el-templo-bot/src/webhook/handler.ts` — all exit 0.
    - Old `precio|no me alcanza` (singular-only) form is gone: `grep -E "\bprecio\|no me alcanza\b" el-templo-bot/src/webhook/handler.ts` returns 0 lines.
    - All 18+ Task 1 unit tests GREEN: `cd el-templo-bot && pnpm test test/v5-3-3-phase-100-price-trigger.test.ts -- --run` exits 0.
    - Full bot suite: `cd el-templo-bot && pnpm test -- --run` ends at **≥ (post-Wave-2 baseline) + (new tests in this plan)** passed / **0 failed** / 0 todo. Exact pass count depends on fixture size (the 6 widening cases minimum; up to 18 if all fixture entries become individual `it` cases). NO deferred RED carry-forward; ANY failure is a regression.
    - HARD GUARD: `grep -q "DEBOUNCE_TTL_SECONDS = Number(process.env.DEBOUNCE_TTL_SECONDS ?? 600)" el-templo-bot/src/webhook/handler.ts` exits 0.
    - Phase 99 preservation strings byte-equal: `grep -q "movimiento grupal" el-templo-bot/src/ai/knowledge.ts`; `grep -q "sin salirte del grupo" el-templo-bot/src/ai/knowledge.ts`; `grep -q "sin salirse del grupo" el-templo-bot/src/ai/knowledge.ts`; `grep -q "framings de arranque grupal" el-templo-bot/src/playbooks/definitions.ts`; `grep -q "lenguaje de arranque grupal" el-templo-bot/src/playbooks/definitions.ts` — all exit 0.
    - `git diff master -- el-templo-bot/src/ai/system-prompt.ts | grep -E '^-.*PB1_PRICE_DISCLOSURE_UNLOCKED_ADDENDUM = ' | wc -l` returns 0 (PB1 addendum byte-equal).
    - `cd el-templo-bot && pnpm tsc --noEmit` exits 0.
    - Phase 99 PB1 counter regression test still passes: `cd el-templo-api && pnpm test test/whatsapp/v5-3-3-phase-99-copy-and-price.integration.test.ts -- --run` exits 0.
  </acceptance_criteria>
  <done>
    Regex widened in-place; single source of truth preserved (exactly 1 src hit); all Task 1 fixture cases GREEN; Phase 99 PB1 integration test still passes; full bot suite passes with the expected baseline + new cases.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integration tests — PB1 counter + PB2.E2 routing under widened regex</name>
  <files>el-templo-api/test/whatsapp/v5-3-3-phase-100-price-trigger.integration.test.ts</files>
  <read_first>
    - el-templo-api/test/whatsapp/v5-3-3-phase-99-copy-and-price.integration.test.ts (lines 540-620 — the existing PB1 counter integration test pattern; reuse the fixtures + helpers)
    - el-templo-bot/src/webhook/handler.ts (the priceInsistenceCount increment site + setPlaybookState wiring)
    - el-templo-bot/src/ai/system-prompt.ts (PB1_PRICE_DISCLOSURE_UNLOCKED_ADDENDUM is gated on `disclosureUnlocked && activePlaybook === "PB1"`)
  </read_first>
  <behavior>
    Three end-to-end scenarios:

    - **Scenario 1 — PB1 counter accumulates on question-shaped triggers:** seed conversation in PB1 (use the Phase 99 helper's setup); send 3 inbounds: "¿cuánto cuesta?" → "¿y cuánto vale?" → "¿qué tarifa tienen?". Assert after each inbound that `priceInsistenceCount` increments (1, 2, 3). On the 3rd, assert the system prompt rendered to the AI provider includes the PB1_PRICE_DISCLOSURE_UNLOCKED_ADDENDUM (use vi.spyOn on the provider's chat call args).
    - **Scenario 2 — PB1 counter does NOT increment on non-price questions:** seed PB1; send "¿qué clases tienen?" → "¿en qué horarios?". Assert `priceInsistenceCount === 0` after both.
    - **Scenario 3 — PB2.E2 neutral-question routing:** seed conversation in PB2.E2; send "¿cuál es el plan más barato?". Assert the handler completes without crash, an outbound is sent (any non-empty reply — we are checking no regression, not specific copy), and the AI provider is invoked exactly once (no infinite loop, no early-return).

  </behavior>
  <action>
    Create `el-templo-api/test/whatsapp/v5-3-3-phase-100-price-trigger.integration.test.ts`:

    1. Reuse the scaffold from `v5-3-3-phase-99-copy-and-price.integration.test.ts` — same `vi.mock` setup for AI provider + whatsapp client, same `waitForHandler()` helper, same per-test unique synthetic phone numbers.
    2. The 3 scenarios in `<behavior>`. For Scenario 1, capture the system-prompt argument the provider receives (via `vi.spyOn(provider, "chat")` + inspect call args) and assert the addendum substring is present on the 3rd-inbound call.
    3. For Scenario 3, seed the playbook state in Redis with `activePlaybook: "PB2", currentStage: "PB2.E2"` (use `setPlaybookState` helper if available, or directly write the state).
    4. Top-level describe: `describe("v5.3.3 Phase 100 TRIG-01 — widened regex integration (PB1 counter + PB2.E2 routing)", () => { ... })`.
    5. No production source changes.

  </action>
  <verify>
    <automated>cd /Users/bores/el-templo/el-templo-api && pnpm test test/whatsapp/v5-3-3-phase-100-price-trigger.integration.test.ts -- --run 2>&1 | tail -25</automated>
    <automated>cd /Users/bores/el-templo/el-templo-bot && pnpm test -- --run 2>&1 | tail -5</automated>
    <automated>cd /Users/bores/el-templo/el-templo-api && pnpm test -- --run 2>&1 | tail -5</automated>
    <automated>grep -q "cuánto cuesta" /Users/bores/el-templo/el-templo-api/test/whatsapp/v5-3-3-phase-100-price-trigger.integration.test.ts</automated>
    <automated>grep -q "priceInsistenceCount" /Users/bores/el-templo/el-templo-api/test/whatsapp/v5-3-3-phase-100-price-trigger.integration.test.ts</automated>
    <automated>grep -q "PB2.E2\|PB2\\.E2" /Users/bores/el-templo/el-templo-api/test/whatsapp/v5-3-3-phase-100-price-trigger.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `el-templo-api/test/whatsapp/v5-3-3-phase-100-price-trigger.integration.test.ts` exists with 3 scenarios, all passing.
    - `grep -q "cuánto cuesta" el-templo-api/test/whatsapp/v5-3-3-phase-100-price-trigger.integration.test.ts` exits 0.
    - `grep -q "priceInsistenceCount" el-templo-api/test/whatsapp/v5-3-3-phase-100-price-trigger.integration.test.ts` exits 0.
    - Phase 99 PB1 counter integration test still passes: `cd el-templo-api && pnpm test test/whatsapp/v5-3-3-phase-99-copy-and-price.integration.test.ts -- --run` exits 0.
    - Full bot suite: `cd el-templo-bot && pnpm test -- --run` ends at the post-Task-2 baseline + new tests / **0 failed** / 0 todo. NO deferred RED carry-forward — all 4 were resolved in Wave 1 commit `af1db504`. ANY failure is a regression.
    - Full api suite: `cd el-templo-api && pnpm test -- --run` baseline ≥ 537 (Phase 99 deterministic baseline) + 3 new passing tests from this plan / 1 deterministic failure (BUG-03 (i) LIKE-search RED at `tools.ts:455`, Phase-95-owned) PLUS 0–3 intermittent pre-existing drift-flake failures (`raw-sql-column-drift.test.ts` + `ai-tools-membership-drift.test.ts`, Phase 97.5/98-owned, MySQL shared-state race per `98-FINDING-01`) / 1 todo. Do NOT pin to exact failed-count. A failure in ANY OTHER api test file = a Phase-100 regression — re-run once to confirm it's not drift-flake; if it persists STOP and surface.
    - `git diff master -- 'el-templo-api/src/**'` returns 0 lines.
  </acceptance_criteria>
  <done>
    3 integration scenarios cover PB1 counter increment on question-shaped triggers (with disclosure addendum injection on 3rd), PB1 counter NON-increment on non-price questions, and PB2.E2 neutral-question routing. Full suite green; Phase 99 regression preserved.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                                                                                                               |
| ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| user inbound → detectPriceObjection | Untrusted text passes through the regex. False positives have minor UX consequences (route through PB2.E2 / increment PB1 counter when not intended); no security impact. |

## STRIDE Threat Register

| Threat ID | Category                                          | Component                  | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                               |
| --------- | ------------------------------------------------- | -------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-100-09  | Tampering (false-positive widening)               | detectPriceObjection regex | mitigate    | Word boundaries (`\b`) anchor each alternation member; negative test cases ("preciosa idea", "preciosos paisajes", "valoro la propuesta", "cuántos años tenés") explicitly assert non-match. Data-driven fixture makes future widening guard via expectation-as-code, not ad-hoc.                                                             |
| T-100-10  | DoS (regex catastrophic backtracking)             | detectPriceObjection regex | accept      | The widened regex is a simple alternation of literal + small character-class members; no nested quantifiers, no overlapping greedy patterns. Catastrophic backtracking risk is negligible. Inputs are bounded by WhatsApp message length (~4096 bytes).                                                                                       |
| T-100-11  | Repudiation (PB2.E2 routing of neutral questions) | PB2.E2 prompt branch       | accept      | Per CONTEXT.md: "a neutral price question in PB2 being treated as price-topic is acceptable per the user dictation. The PB2.E2 prompt branch already handles price-objection-shaped inputs; routing a neutral question through it produces a mildly-defensive but on-topic response, not a regression." Integration Scenario 3 locks this in. |

**block_on:** high
</threat_model>

<verification>
**Process guards (mandatory per CONTEXT.md `<specifics>`):**
- Full bot suite `cd el-templo-bot && pnpm test -- --run` — expect the post-Wave-2 baseline + Task 1's new tests (exact count depends on fixture entry count); **0 failed** / 0 todo. NO deferred RED carry-forward — Wave 1's driver update at commit `af1db504` resolved all 4 previously-timing-blocked Phase 94+95 RED tests (post-Wave-1 baseline: 662/0/0). ANY failure here is a regression — STOP and surface.
- Dual-grep on changed tokens:
  - Single-source-of-truth gate: `grep -rE "caro\|carisimo" el-templo-bot/src/ --include="*.ts" | wc -l` returns EXACTLY 1.
  - Phase 100 widening tokens present in src: `grep -rE "precios\?|cu\[aá\]nto \(sale\|cuesta\|val\[eé\]\)|valor\(es\)\?|tarifa\|cuota\|mensualidad" el-templo-bot/src/ --include="*.ts" | wc -l` returns ≥ 1 (helper body).
  - Test references to new phrases: `grep -rE "cuánto cuesta|cuánto sale|cuánto vale|valor|tarifa|cuota|mensualidad" el-templo-bot/test/ el-templo-api/test/ --include="*.ts" | wc -l` returns ≥ 6.
- Phase 99 PB1 counter regression: `cd el-templo-api && pnpm test test/whatsapp/v5-3-3-phase-99-copy-and-price.integration.test.ts -- --run` exits 0.
- Phase 99 preservation strings byte-equal: `grep -q "movimiento grupal" el-templo-bot/src/ai/knowledge.ts`; `grep -q "sin salirte del grupo" el-templo-bot/src/ai/knowledge.ts`; `grep -q "sin salirse del grupo" el-templo-bot/src/ai/knowledge.ts`; `grep -q "framings de arranque grupal" el-templo-bot/src/playbooks/definitions.ts`; `grep -q "lenguaje de arranque grupal" el-templo-bot/src/playbooks/definitions.ts` — all exit 0.
- PB1.E4 REGLA FUERTE byte-equal: `grep -c "REGLA FUERTE" el-templo-bot/src/playbooks/definitions.ts` returns 1.
- PB1_PRICE_DISCLOSURE_UNLOCKED_ADDENDUM byte-equal: `git diff master -- el-templo-bot/src/ai/system-prompt.ts | grep -E '^-.*PB1_PRICE_DISCLOSURE_UNLOCKED_ADDENDUM = ' | wc -l` returns 0.
- KGATE-05 NO-OP: this plan does NOT touch system-prompt.ts; POST_RLOK_04_BYTES unchanged.
- HARD GUARDS: DEBOUNCE_TTL_SECONDS line 167 byte-equal; `git diff master -- 'el-templo-api/src/**'` returns 0 lines.
- 6-pair sha256 cross-phase invariant block byte-equal: `git diff master -- .planning/phases/93-handler-concurrency/93-CONTEXT.md .planning/MACRO-ROADMAP.md` returns 0 lines.
</verification>

<success_criteria>

- TRIG-01: `detectPriceObjection` regex widened in place (one literal line + JSDoc update); covers `precios?`, `cu[aá]nto (sale|cuesta|val[eé])`, `valor(es)?`, `tarifa`, `cuota`, `mensualidad`; all pre-existing terms preserved byte-equal.
- Single source of truth preserved: exactly 1 src hit for the regex pattern.
- Data-driven unit fixture with ≥18 cases (positive baseline + Phase 100 widening + negative word-boundary).
- 3 integration scenarios cover PB1 counter behavior (positive + negative) + PB2.E2 routing.
- Phase 99 PB1 counter regression intact (existing integration test still passes).
- All Phase 99 preservation strings + PB1.E4 REGLA FUERTE + PB1_PRICE_DISCLOSURE_UNLOCKED_ADDENDUM byte-equal.
- Full bot suite at expected baseline + new tests; full api suite gains 3 passing tests without regression.
- HARD GUARDS: DEBOUNCE_TTL_SECONDS untouched; el-templo-api/src/\*\* unchanged; 6-pair sha256 invariant byte-equal.
  </success_criteria>

<output>
Create `.planning/phases/100-bot-takeover-ack-debounce-and-price-trigger/100-03-SUMMARY.md` when done. Document: (a) the actual fixture count (positive baseline + Phase 100 widening + negative), (b) any false-positive surprises discovered during fixture authoring (e.g., did "valoro" match unexpectedly?), (c) confirmation of single-source-of-truth grep (exactly 1 hit), (d) PB1 counter pre/post test counts (Phase 99 integration test still GREEN), (e) final test counts (bot ≥662 + new tests / 0 failed / 0 todo; api +3 passing).
</output>
</content>
</invoke>
