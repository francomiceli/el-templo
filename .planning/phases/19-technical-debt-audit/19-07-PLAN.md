---
phase: 19-technical-debt-audit
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/admin/edit-service.ts
  - el-templo-api/src/modules/admin/session-mutation-service.ts
  - el-templo-api/src/modules/admin/exercise-swap-service.ts
  - el-templo-api/src/modules/admin/routes.ts
  - el-templo-api/src/modules/sessions/routes.ts
  - el-templo-api/src/modules/sessions/service.ts
  - el-templo-api/src/modules/admin/types.ts
  - el-templo-api/src/db/seed-spom.ts
  - el-templo-admin/src/utils/pdf/session-pdf-builder.ts
  - el-templo-admin/src/components/sessions/EditableBlockCard.vue
  - el-templo-app/src/modules/training/composables/useWakeLock.ts
  - el-templo-app/src/modules/progression/pages/MiCamino.vue
autonomous: true

must_haves:
  truths:
    - "edit-service.ts is under 350 LOC and acts as a facade delegating to domain services"
    - "Session mutation logic lives in session-mutation-service.ts"
    - "Exercise swap logic lives in exercise-swap-service.ts"
    - "Zero any types remain in the API codebase (grep returns 0)"
    - "All route handlers still work correctly after refactoring"
  artifacts:
    - path: "el-templo-api/src/modules/admin/edit-service.ts"
      provides: "Facade/orchestrator for session editing"
      max_lines: 350
    - path: "el-templo-api/src/modules/admin/session-mutation-service.ts"
      provides: "Block and exercise add/remove/reorder logic"
      min_lines: 50
    - path: "el-templo-api/src/modules/admin/exercise-swap-service.ts"
      provides: "Exercise replacement and pool logic"
      min_lines: 50
  key_links:
    - from: "el-templo-api/src/modules/admin/edit-service.ts"
      to: "el-templo-api/src/modules/admin/session-mutation-service.ts"
      via: "imports and delegates mutation operations"
      pattern: "import.*session-mutation"
    - from: "el-templo-api/src/modules/admin/edit-service.ts"
      to: "el-templo-api/src/modules/admin/exercise-swap-service.ts"
      via: "imports and delegates swap operations"
      pattern: "import.*exercise-swap"
---

<objective>
Split the edit-service.ts god object (1232 LOC) into focused domain services, then eliminate all `any` types across the entire codebase (~30 occurrences in API, admin, and app).

Purpose: edit-service.ts is the largest file in the codebase and handles multiple responsibilities (session mutations, exercise swaps, format changes, validation). Splitting it makes each operation independently understandable and testable. Eliminating `any` types restores TypeScript's safety guarantees and catches bugs at compile time.
Output: edit-service.ts < 350 LOC as facade, domain services extracted, zero `any` types.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-technical-debt-audit/19-RESEARCH.md
@el-templo-api/src/modules/admin/edit-service.ts
@el-templo-api/src/modules/admin/routes.ts
@el-templo-api/src/modules/admin/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split edit-service.ts into domain services</name>
  <files>
    el-templo-api/src/modules/admin/edit-service.ts
    el-templo-api/src/modules/admin/session-mutation-service.ts
    el-templo-api/src/modules/admin/exercise-swap-service.ts
  </files>
  <action>
    Read edit-service.ts thoroughly and identify the distinct domains:

    **1. Extract `session-mutation-service.ts`** -- Block and exercise structural operations:
    - Functions that add/remove/reorder blocks within a session
    - Functions that add/remove exercises within a block
    - Functions that update exercise count on the block
    - Any validation logic specific to structural changes (e.g., minimum exercises per block)
    - Export a class or namespace (e.g., `SessionMutationService`) with these methods

    **2. Extract `exercise-swap-service.ts`** -- Exercise replacement and pool:
    - Functions that find compatible replacement exercises (by route, difficulty, contraction type)
    - Functions that execute the swap (replace one exercise with another in the database)
    - The exercise pool logic (filtering available exercises for a block)
    - Cross-route exercise handling
    - Export a class or namespace (e.g., `ExerciseSwapService`)

    **3. Keep `edit-service.ts` as facade:**
    - Import SessionMutationService and ExerciseSwapService
    - Keep the top-level orchestration methods that route handlers call
    - Keep any transaction management (operations that span multiple domain services)
    - Keep the database connection/injection pattern
    - Target: < 350 LOC

    **Extraction process:**
    1. Read the file, map every function to a domain (mutation, swap, format, or orchestration)
    2. Note: `format-params.ts` and `prescribe-service.ts` already exist as separate files -- this pattern is established
    3. Move functions to their domain service file, keeping the same signatures
    4. Update imports in edit-service.ts to delegate to the new services
    5. Update `routes.ts` if it imports anything directly from edit-service that moved
    6. Run `pnpm run build` to verify no broken imports

    **Important:** The existing `format-params.ts` and `prescribe-service.ts` in the same directory show the established pattern. Follow the same export style.
  </action>
  <verify>
    - `wc -l el-templo-api/src/modules/admin/edit-service.ts` shows < 350 lines
    - `el-templo-api/src/modules/admin/session-mutation-service.ts` exists
    - `el-templo-api/src/modules/admin/exercise-swap-service.ts` exists
    - `pnpm run build` succeeds in el-templo-api (no broken imports)
    - `pnpm exec tsc --noEmit` passes (no type errors from refactoring)
  </verify>
  <done>edit-service.ts reduced from 1232 LOC to < 350 LOC. Session mutation logic extracted to session-mutation-service.ts. Exercise swap logic extracted to exercise-swap-service.ts. All route handlers continue to work through the facade.</done>
</task>

<task type="auto">
  <name>Task 2: Eliminate all any types across the codebase</name>
  <files>
    el-templo-api/src/modules/admin/routes.ts
    el-templo-api/src/modules/admin/edit-service.ts
    el-templo-api/src/modules/sessions/routes.ts
    el-templo-api/src/modules/sessions/service.ts
    el-templo-api/src/modules/admin/types.ts
    el-templo-api/src/db/seed-spom.ts
    el-templo-admin/src/utils/pdf/session-pdf-builder.ts
    el-templo-admin/src/components/sessions/EditableBlockCard.vue
    el-templo-app/src/modules/training/composables/useWakeLock.ts
    el-templo-app/src/modules/progression/pages/MiCamino.vue
  </files>
  <action>
    Eliminate all `any` type usages. Current count: ~30 across all 3 projects.

    **API (18 occurrences across 5 files):**
    - `el-templo-api/src/modules/admin/routes.ts` (13 occurrences): These are likely Fastify request body/params types. Replace with proper typed request schemas using Fastify's generic type parameters:
      ```typescript
      // Before: (request: FastifyRequest, reply: FastifyReply) => { const body = request.body as any; }
      // After: (request: FastifyRequest<{ Body: UpdatePrescriptionBody }>, reply: FastifyReply) => { const body = request.body; }
      ```
      Define interface types for each route's Body, Params, and Querystring in types.ts (or inline if simple).
    - `el-templo-api/src/modules/admin/edit-service.ts` (2 occurrences): Should be resolved during Task 1's refactoring. If still present, replace with proper types.
    - `el-templo-api/src/modules/sessions/routes.ts` (1): Same pattern as admin routes.
    - `el-templo-api/src/modules/sessions/service.ts` (1): Likely a function parameter or return type.
    - `el-templo-api/src/db/seed-spom.ts` (1): CSV parsing result type. Use `Record<string, string>` or a specific row type.

    **Admin (10 occurrences across 2 files):**
    - `el-templo-admin/src/utils/pdf/session-pdf-builder.ts` (9): pdfmake types. Use pdfmake's built-in types: `TDocumentDefinitions`, `Content`, `ContentTable`, etc. Import from `pdfmake/interfaces`. If pdfmake types are incomplete, use `Record<string, unknown>` instead of `any`.
    - `el-templo-admin/src/components/sessions/EditableBlockCard.vue` (1): Likely a prop or event type. Use the actual type.

    **App (2 occurrences across 2 files):**
    - `el-templo-app/src/modules/training/composables/useWakeLock.ts` (1): Likely the WakeLock API type. Use `WakeLockSentinel` from lib.dom.d.ts or `unknown` with type narrowing.
    - `el-templo-app/src/modules/progression/pages/MiCamino.vue` (1): Likely a chart data type. Use the chart.js types.

    **Strategy:**
    - For Fastify route handlers: define request type interfaces, use FastifyRequest generic
    - For pdfmake: use `Content`, `TDocumentDefinitions` from pdfmake types, or `unknown` with assertions
    - For external APIs (WakeLock, chart.js): use the library's type definitions or `unknown`
    - NEVER use `as any` as a workaround -- use `unknown` with type guards if needed
    - If a type is genuinely unknowable at compile time, use `unknown` instead of `any`

    After fixing all occurrences, run `grep -r ": any\b" src/ && grep -r "as any\b" src/` in each project to verify zero results.
  </action>
  <verify>
    Run in each project:
    - `cd el-templo-api && grep -rE ": any\b|as any\b" src/ --include="*.ts" | grep -v node_modules | grep -v validation/` returns 0 results (excluding validation scripts which use console.log but may have any types -- check and fix those too if present)
    - `cd el-templo-admin && grep -rE ": any\b|as any\b" src/ --include="*.ts" --include="*.vue"` returns 0 results
    - `cd el-templo-app && grep -rE ": any\b|as any\b" src/ --include="*.ts" --include="*.vue"` returns 0 results
    - `pnpm run build` succeeds in all 3 projects
    - `pnpm exec tsc --noEmit` passes in el-templo-api
  </verify>
  <done>Zero `any` types remain in the production codebase. All replaced with proper types, `unknown` with type guards, or library-specific type definitions. TypeScript's type safety fully restored.</done>
</task>

</tasks>

<verification>
- edit-service.ts < 350 LOC, two domain services extracted
- `grep -rE ": any\b|as any\b" */src/ --include="*.ts" --include="*.vue"` returns 0 across all projects
- All 3 projects build successfully
- API type checks pass (tsc --noEmit)
</verification>

<success_criteria>
edit-service.ts reduced from 1232 LOC to < 350 LOC with session-mutation-service.ts and exercise-swap-service.ts extracted. Zero `any` types remain in any project. All builds pass. All route handlers function correctly through the refactored service facade.
</success_criteria>

<output>
After completion, create `.planning/phases/19-technical-debt-audit/19-07-SUMMARY.md`
</output>
