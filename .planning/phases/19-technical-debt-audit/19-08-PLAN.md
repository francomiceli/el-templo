---
phase: 19-technical-debt-audit
plan: 08
type: execute
wave: 2
depends_on: ["19-02"]
files_modified:
  - el-templo-api/src/index.ts
  - el-templo-api/src/jobs/auto-approve.ts
  - el-templo-app/src/modules/training/stores/weekStore.ts
  - el-templo-app/src/modules/training/stores/sessionPlayerStore.ts
  - el-templo-app/src/modules/training/pages/WeeklyView.vue
  - el-templo-app/src/modules/training/pages/DayPlayer.vue
  - el-templo-app/src/modules/training/composables/useSessionCompletion.ts
  - el-templo-app/src/modules/training/composables/useWeekData.ts
  - el-templo-app/src/modules/training/composables/useWakeLock.ts
  - el-templo-admin/src/pages/SessionsPage.vue
autonomous: true

must_haves:
  truths:
    - "grep for console.log/warn/error returns 0 results in production code (excluding validation scripts)"
    - "All logging uses createLogger() in frontends or Fastify's built-in Pino in API"
    - "Error-level logs include structured context (component name, relevant IDs)"
  artifacts:
    - path: "el-templo-app/src/utils/logger.ts"
      provides: "Frontend logger (created in Plan 02)"
      exports: ["createLogger"]
    - path: "el-templo-admin/src/utils/logger.ts"
      provides: "Frontend logger (created in Plan 02)"
      exports: ["createLogger"]
  key_links:
    - from: "el-templo-app/src/modules/training/pages/DayPlayer.vue"
      to: "el-templo-app/src/utils/logger.ts"
      via: "import createLogger"
      pattern: "createLogger"
    - from: "el-templo-api/src/jobs/auto-approve.ts"
      to: "fastify.log"
      via: "Pino logger via Fastify"
      pattern: "log\\.(info|error|warn|debug)"
---

<objective>
Replace all console.log/warn/error statements in production code with structured logger calls. API uses Fastify's built-in Pino logger. Frontend apps use the createLogger() utility created in Plan 02.

Purpose: Console statements provide no structure, no levels, no context, and cannot be suppressed in production. Replacing them with structured logging enables: production log suppression (debug/info hidden), error context for debugging, and future Sentry integration for the error path. The actual scope is ~18 statements (not 279 -- validation scripts stay as-is per user decision).
Output: Zero console.log/warn/error in production code paths.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-technical-debt-audit/19-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace console statements in API production code</name>
  <files>
    el-templo-api/src/index.ts
    el-templo-api/src/jobs/auto-approve.ts
  </files>
  <action>
    Replace console.log/warn/error in API production code with Fastify's Pino logger. The API already has `Fastify({ logger: true })` so the logger is available on the app instance.

    **el-templo-api/src/index.ts (1 statement):**
    - Find the console.log statement (likely the "Server listening on..." message).
    - Replace with `app.log.info(...)` or remove if Fastify already logs the listen event.

    **el-templo-api/src/jobs/auto-approve.ts (5 statements):**
    - This is a scheduled job. It needs a logger instance.
    - If the job has access to the Fastify app instance, use `app.log.info(...)`.
    - If it's a standalone script, create a Pino logger instance:
      ```typescript
      import pino from 'pino';
      const log = pino({ name: 'auto-approve' });
      ```
    - Replace each console.log with appropriate level:
      - Status messages -> `log.info()`
      - Success messages -> `log.info()`
      - Error messages -> `log.error()`
      - Warning messages -> `log.warn()`

    **Files to SKIP (validation scripts -- kept as-is per user decision):**
    - el-templo-api/src/modules/sessions/validation/*.ts (all 7 files with ~240 console statements)
    - el-templo-api/src/db/seed.ts (17 statements -- CLI seed script)
    - el-templo-api/src/db/seed-spom.ts (33 statements -- CLI seed script)
    - el-templo-api/src/db/run-migrations.ts (7 statements -- CLI migration script)

    These are standalone CLI tools that intentionally print to console. They are NOT production server code.
  </action>
  <verify>
    - `grep -rE "console\.(log|warn|error)" el-templo-api/src/index.ts el-templo-api/src/jobs/` returns 0 results
    - `pnpm run build` succeeds in el-templo-api
    - Validation scripts still have their console.log statements (not touched)
  </verify>
  <done>API production code (index.ts, auto-approve.ts) uses Pino structured logging. Validation scripts and seed/migration CLI tools retain console.log as intended.</done>
</task>

<task type="auto">
  <name>Task 2: Replace console statements in frontend apps</name>
  <files>
    el-templo-app/src/modules/training/stores/weekStore.ts
    el-templo-app/src/modules/training/stores/sessionPlayerStore.ts
    el-templo-app/src/modules/training/pages/WeeklyView.vue
    el-templo-app/src/modules/training/pages/DayPlayer.vue
    el-templo-app/src/modules/training/composables/useSessionCompletion.ts
    el-templo-app/src/modules/training/composables/useWeekData.ts
    el-templo-app/src/modules/training/composables/useWakeLock.ts
    el-templo-admin/src/pages/SessionsPage.vue
  </files>
  <action>
    Replace all console.log/warn/error statements in frontend production code with createLogger() calls. The createLogger utility was created in Plan 02.

    **el-templo-app (10 statements across 7 files):**

    For each file:
    1. Add `import { createLogger } from 'src/utils/logger';` at the top
    2. Create a logger instance: `const log = createLogger('ComponentName');`
    3. Replace each console statement:
       - `console.log('...')` -> `log.debug('...', { relevant: data })` or `log.info('...')`
       - `console.warn('...')` -> `log.warn('...')`
       - `console.error('...')` -> `log.error('...', { error: err.message })`
    4. Choose the appropriate log level:
       - debug: Development-only information (state changes, data loading)
       - info: Significant events (session started, block completed)
       - warn: Recoverable issues (fallback behavior triggered)
       - error: Failed operations (API call failed, unexpected state)

    Specific files:
    - `weekStore.ts` (1): Likely a data loading log -> `log.debug()`
    - `sessionPlayerStore.ts` (1): State transition log -> `log.debug()`
    - `WeeklyView.vue` (1): Data loading -> `log.debug()`
    - `DayPlayer.vue` (1): Session event -> `log.info()` or `log.debug()`
    - `useSessionCompletion.ts` (1): Completion event -> `log.info()`
    - `useWeekData.ts` (1): Data fetching -> `log.debug()`
    - `useWakeLock.ts` (4): WakeLock state changes -> `log.debug()` for acquire/release, `log.warn()` for failures

    **el-templo-admin (2 statements in 1 file):**
    - `SessionsPage.vue` (2): Replace with `log.debug()` or `log.info()` using `createLogger('SessionsPage')`

    **After all replacements, verify no console statements remain in production source:**
    ```bash
    grep -rE "console\.(log|warn|error)" el-templo-app/src/ --include="*.ts" --include="*.vue"
    grep -rE "console\.(log|warn|error)" el-templo-admin/src/ --include="*.ts" --include="*.vue"
    ```
    Both should return 0 results.
  </action>
  <verify>
    - `grep -rE "console\.(log|warn|error)" el-templo-app/src/ --include="*.ts" --include="*.vue"` returns 0 results
    - `grep -rE "console\.(log|warn|error)" el-templo-admin/src/ --include="*.ts" --include="*.vue"` returns 0 results
    - `pnpm run build` succeeds in both el-templo-app and el-templo-admin
    - All logger imports resolve correctly
  </verify>
  <done>Zero console.log/warn/error statements in frontend production code. All replaced with structured createLogger() calls with appropriate log levels and context. Production builds suppress debug/info output.</done>
</task>

</tasks>

<verification>
- `grep -rE "console\.(log|warn|error)" el-templo-api/src/index.ts el-templo-api/src/jobs/` returns 0
- `grep -rE "console\.(log|warn|error)" el-templo-app/src/ --include="*.ts" --include="*.vue"` returns 0
- `grep -rE "console\.(log|warn|error)" el-templo-admin/src/ --include="*.ts" --include="*.vue"` returns 0
- All 3 projects build successfully
- Validation scripts and CLI tools retain their console.log statements
</verification>

<success_criteria>
All production console.log/warn/error statements replaced with structured logger calls. API uses Pino. Frontends use createLogger(). Validation scripts and CLI tools are untouched. Zero console statements in production code paths.
</success_criteria>

<output>
After completion, create `.planning/phases/19-technical-debt-audit/19-08-SUMMARY.md`
</output>
