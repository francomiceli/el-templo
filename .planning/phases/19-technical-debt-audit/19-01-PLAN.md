---
phase: 19-technical-debt-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-app/package.json
  - el-templo-admin/package.json
  - el-templo-api/package.json
  - el-templo-admin/.gitignore
  - el-templo-admin/.env.example
  - el-templo-app/.env.example
  - el-templo-api/.env.example
autonomous: true

must_haves:
  truths:
    - "pnpm audit returns 0 high/critical vulnerabilities in all 3 projects"
    - "No .env files with real values are tracked in git"
    - ".env.example templates exist for all 3 projects documenting required variables"
  artifacts:
    - path: "el-templo-admin/.env.example"
      provides: "Admin app environment variable template"
      contains: "VITE_API_URL"
    - path: "el-templo-app/.env.example"
      provides: "Member app environment variable template"
      contains: "VITE_API_URL"
    - path: "el-templo-api/.env.example"
      provides: "API environment variable template"
      contains: "DB_HOST"
  key_links:
    - from: "el-templo-admin/.gitignore"
      to: ".env*"
      via: "gitignore pattern"
      pattern: "\\.env\\*"
---

<objective>
Fix all known security vulnerabilities (CVEs) across the 3-app monorepo and clean up environment variable handling. Remove tracked .env files from git, update .gitignore patterns, and create .env.example templates for all projects.

Purpose: Eliminate critical and high-severity CVEs that expose the production system to DoS attacks and validation bypasses. Establish proper .env handling to prevent secrets from entering version control.
Output: Zero high/critical CVEs, .env.example templates for all 3 projects, admin .env.development removed from git.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-technical-debt-audit/19-RESEARCH.md
@.docs/technical-debt/INVENTORY-REPORT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade all CVE-affected packages</name>
  <files>
    el-templo-app/package.json
    el-templo-admin/package.json
    el-templo-api/package.json
  </files>
  <action>
    Upgrade security-critical packages in all 3 projects:

    1. **el-templo-app**: `cd el-templo-app && pnpm update axios` to get axios >= 1.13.5 (fixes CVE-2026-25639 DoS via __proto__). Also `pnpm update @quasar/app-vite` to resolve qs CVE-2026-2391.

    2. **el-templo-admin**: `cd el-templo-admin && pnpm update axios` to get axios >= 1.13.5. Also `pnpm update @quasar/app-vite` for qs fix.

    3. **el-templo-api**: `cd el-templo-api && pnpm update fastify` to get fastify >= 5.7.4 (fixes CVE-2026-25223 validation bypass + CVE-2026-25224 DoS). Also `pnpm update drizzle-kit` to resolve esbuild GHSA-67mh-4wv8-2f99.

    After each update, run `pnpm audit --audit-level=high` to confirm zero high/critical vulnerabilities. Run `pnpm run build` in each project to verify builds still succeed.

    Do NOT force major version bumps. Only patch/minor updates within current ranges. If a package requires a major bump to fix CVE, note it but do not upgrade (pdfmake 0.2->0.3 and vue-router 4->5 are explicitly deferred).
  </action>
  <verify>
    Run in each project directory:
    - `pnpm audit --audit-level=high` returns 0 vulnerabilities
    - `pnpm run build` completes without errors
    Specifically verify: `pnpm ls axios` shows >= 1.13.5 in app/admin, `pnpm ls fastify` shows >= 5.7.4 in api.
  </verify>
  <done>All 3 projects report 0 high/critical CVEs from pnpm audit. All 3 projects build successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up .env handling and create .env.example templates</name>
  <files>
    el-templo-admin/.gitignore
    el-templo-admin/.env.example
    el-templo-app/.env.example
    el-templo-api/.env.example
  </files>
  <action>
    1. **Remove tracked .env.development from admin app:**
       - Run `git rm --cached el-templo-admin/.env.development` to untrack without deleting local file.
       - Update `el-templo-admin/.gitignore` to add comprehensive .env exclusion:
         ```
         .env*
         !.env.example
         ```
         This covers .env, .env.development, .env.production, .env.local, etc.

    2. **Verify other projects' .gitignore:** Check el-templo-app/.gitignore and el-templo-api/.gitignore already have .env exclusions. If not, add the same `.env*` + `!.env.example` pattern.

    3. **Create .env.example templates** for each project with placeholder values and comments:

       **el-templo-api/.env.example:**
       ```
       # Database
       DB_HOST=localhost
       DB_PORT=3306
       DB_USER=root
       DB_PASSWORD=your_password_here
       DB_NAME=eltemplo

       # Authentication
       JWT_SECRET=change_this_to_a_random_string
       JWT_EXPIRES_IN=7d

       # CORS Origins (production)
       FRONTEND_URL=https://app.eltemplo.org
       ADMIN_URL=https://admin.eltemplo.org

       # Optional: Sentry (added in Phase 19)
       # SENTRY_DSN=https://your-dsn@sentry.io/project-id

       # Server
       PORT=3000
       NODE_ENV=development
       ```

       **el-templo-app/.env.example:**
       ```
       # API Connection
       VITE_API_URL=http://localhost:3000/api

       # App
       VITE_APP_NAME=El Templo

       # Optional: Sentry (added in Phase 19)
       # VITE_SENTRY_DSN=https://your-dsn@sentry.io/project-id
       ```

       **el-templo-admin/.env.example:**
       ```
       # API Connection
       VITE_API_URL=http://localhost:3000/api

       # Optional: Sentry (added in Phase 19)
       # VITE_SENTRY_DSN=https://your-dsn@sentry.io/project-id
       ```

    Per user decision: NO git history rewrite. Old secrets in history will be invalidated via rotation (separate operational step, not in this plan).
  </action>
  <verify>
    - `git status` shows el-templo-admin/.env.development as deleted (from index)
    - `git ls-files el-templo-admin/.env.development` returns empty (not tracked)
    - All 3 .env.example files exist with documented variables
    - `grep -r ".env\*" el-templo-admin/.gitignore` confirms pattern exists
  </verify>
  <done>.env.development removed from git tracking. All 3 projects have .env.example templates. .gitignore patterns prevent future .env files from being committed.</done>
</task>

</tasks>

<verification>
- `pnpm audit --audit-level=high` returns 0 in all 3 project directories
- All 3 projects build successfully (`pnpm run build`)
- `git ls-files | grep "\.env\." | grep -v example` returns empty
- .env.example exists in all 3 project roots
</verification>

<success_criteria>
Zero high/critical CVEs across all 3 projects. No real .env files tracked in git. .env.example templates document all required environment variables for each project.
</success_criteria>

<output>
After completion, create `.planning/phases/19-technical-debt-audit/19-01-SUMMARY.md`
</output>
