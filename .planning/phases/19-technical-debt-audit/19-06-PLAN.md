---
phase: 19-technical-debt-audit
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-app/src/modules/training/pages/DayPlayer.vue
  - el-templo-app/src/modules/training/components/DeuterosSelector.vue
  - el-templo-app/src/modules/training/components/BlockProgressionView.vue
autonomous: true

must_haves:
  truths:
    - "DayPlayer.vue is under 350 LOC and acts as a container/orchestrator only"
    - "DeuterosSelector.vue handles the Deuteros variant choice screen independently"
    - "BlockProgressionView.vue handles block navigation and current block display independently"
    - "All existing DayPlayer functionality is preserved (splash, Deuteros choice, block flow, exercise display, completion)"
  artifacts:
    - path: "el-templo-app/src/modules/training/pages/DayPlayer.vue"
      provides: "Container/orchestrator for day player flow"
      max_lines: 350
    - path: "el-templo-app/src/modules/training/components/DeuterosSelector.vue"
      provides: "Deuteros variant selection UI"
      min_lines: 30
    - path: "el-templo-app/src/modules/training/components/BlockProgressionView.vue"
      provides: "Block navigation and current block display"
      min_lines: 50
  key_links:
    - from: "el-templo-app/src/modules/training/pages/DayPlayer.vue"
      to: "el-templo-app/src/modules/training/components/DeuterosSelector.vue"
      via: "component import and v-if rendering"
      pattern: "DeuterosSelector"
    - from: "el-templo-app/src/modules/training/pages/DayPlayer.vue"
      to: "el-templo-app/src/modules/training/components/BlockProgressionView.vue"
      via: "component import and v-if rendering"
      pattern: "BlockProgressionView"
---

<objective>
Split DayPlayer.vue (900 LOC god object) into focused sub-components. Extract DeuterosSelector and BlockProgressionView as independent components. DayPlayer.vue becomes a thin orchestrator managing state transitions between sub-views.

Purpose: DayPlayer.vue is the most-changed file in the codebase (20 commits in 90 days) and handles 6 distinct responsibilities: splash screen, Deuteros choice, block progression, exercise display, completion tracking, and timer management. Splitting it reduces cognitive load, makes each piece testable in isolation, and prevents merge conflicts on future changes.
Output: DayPlayer.vue < 350 LOC as orchestrator, DeuterosSelector.vue and BlockProgressionView.vue as independent components.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@el-templo-app/src/modules/training/pages/DayPlayer.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract DeuterosSelector and BlockProgressionView components</name>
  <files>
    el-templo-app/src/modules/training/components/DeuterosSelector.vue
    el-templo-app/src/modules/training/components/BlockProgressionView.vue
  </files>
  <action>
    Read DayPlayer.vue thoroughly to understand the full component structure, state management, and rendering flow. Then extract two sub-components:

    **1. DeuterosSelector.vue** -- The Deuteros variant choice screen:
    - Identify the template section that renders when the user must choose between Deuteros 1 and Deuteros 2.
    - This includes the two card/option views, any scroll-snap carousel, and the selection logic.
    - Props: the two Deuteros block objects (or the full session data needed to display the choice)
    - Emits: `@select(selectedDeuterosIndex: number)` when user picks a variant
    - Extract all associated CSS/styles that belong exclusively to this UI.
    - The component should be self-contained: receives data via props, emits selection via event.

    **2. BlockProgressionView.vue** -- Block navigation and current block display:
    - Identify the template section that renders the current block's exercises, the block header (name, format, route), exercise list/accordion, progress bar, and the "Listo" / "Completar Bloque" button.
    - This is likely the largest extraction -- it includes exercise rendering, per-exercise completion checkmarks, and the mobility "Descanso Activo" section.
    - Props: current block data, exercise completion state, block index, total blocks
    - Emits: `@complete-block`, `@toggle-exercise(exerciseId)`, etc.
    - Include the exercise accordion, exercise cards, contraction badges, and any per-exercise completion UI.

    **Extraction strategy:**
    - Start by reading the entire DayPlayer.vue to map which template sections, computed properties, methods, and watchers belong to which concern.
    - Cut template + related script logic + related styles into new component.
    - Replace in DayPlayer with `<DeuterosSelector :blocks="..." @select="..." />` and `<BlockProgressionView :block="..." @complete="..." />`.
    - Any shared state (like the session player store, exercise completion composable) stays in DayPlayer and is passed via props/emits, OR the sub-component imports the same composable/store directly if that's cleaner.

    **Important:** Do not change any behavior. The refactoring must be purely structural. The user should see zero visual or functional differences.
  </action>
  <verify>
    - `el-templo-app/src/modules/training/components/DeuterosSelector.vue` exists and has template + script
    - `el-templo-app/src/modules/training/components/BlockProgressionView.vue` exists and has template + script
    - `pnpm run build` succeeds in el-templo-app (no type errors, no missing imports)
  </verify>
  <done>DeuterosSelector.vue handles Deuteros variant choice independently. BlockProgressionView.vue handles block display and exercise rendering independently. Both components are imported and used by DayPlayer.vue.</done>
</task>

<task type="auto">
  <name>Task 2: Slim down DayPlayer.vue to orchestrator role</name>
  <files>
    el-templo-app/src/modules/training/pages/DayPlayer.vue
  </files>
  <action>
    After Task 1 created the sub-components, DayPlayer.vue should be trimmed to an orchestrator role:

    1. **Remove extracted template sections:** Replace Deuteros choice UI with `<DeuterosSelector>` component tag. Replace block progression UI with `<BlockProgressionView>` component tag.

    2. **Remove extracted script logic:** Methods, computed properties, and watchers that exclusively serve the extracted UI should move to those components. Keep only:
       - Session loading and state machine (splash -> deuteros choice -> block progression -> celebration -> summary)
       - Route/navigation guard logic (confirm-before-leave dialog)
       - Wake lock management
       - Session completion submission
       - State transitions between sub-views

    3. **Remove extracted styles:** Any scoped CSS that exclusively styles the extracted template sections should move to those components.

    4. **Keep the state machine:** DayPlayer.vue should manage WHICH sub-view is shown (v-if/v-show based on player state) and orchestrate transitions between them. The sub-components receive data and emit events back to the orchestrator.

    5. **Verify line count:** DayPlayer.vue should be under 350 LOC. If still too large, identify additional extraction candidates (e.g., the celebration screen, the summary/RPE screen could become separate components). But do NOT over-extract -- 350 LOC for an orchestrator with multiple sub-views is acceptable.

    6. **Smoke test:** Run the app locally (`cd el-templo-app && pnpm run dev`), navigate to the training module, and verify the DayPlayer flow still works: splash -> deuteros choice -> block progression -> complete.

    **Important:** If the component uses composables like useSessionPlayer or useExerciseCompletion, these can stay as direct imports in either DayPlayer (if orchestrating) or the sub-components (if they need the data). Choose the approach that minimizes prop drilling while keeping components focused.
  </action>
  <verify>
    - `wc -l el-templo-app/src/modules/training/pages/DayPlayer.vue` shows < 350 lines
    - `pnpm run build` succeeds in el-templo-app
    - `grep -c "DeuterosSelector\|BlockProgressionView" el-templo-app/src/modules/training/pages/DayPlayer.vue` shows >= 2 (both components imported/used)
  </verify>
  <done>DayPlayer.vue is under 350 LOC and acts as an orchestrator. It manages the state machine (splash/choice/play/celebration/summary) and delegates rendering to DeuterosSelector and BlockProgressionView. All existing functionality preserved.</done>
</task>

</tasks>

<verification>
- DayPlayer.vue < 350 LOC (down from 900)
- DeuterosSelector.vue and BlockProgressionView.vue exist as independent components
- el-templo-app builds successfully
- All DayPlayer functionality preserved (splash, Deuteros choice, block flow, exercises, completion)
</verification>

<success_criteria>
DayPlayer.vue reduced from 900 LOC to < 350 LOC. Two new focused components extracted. Zero functional regressions. The most-changed file in the codebase is now maintainable and each piece is independently understandable.
</success_criteria>

<output>
After completion, create `.planning/phases/19-technical-debt-audit/19-06-SUMMARY.md`
</output>
