---
phase: 138-entidad-caja-saldos
plan: 02
type: execute
wave: 2
depends_on: ["138-01"]
files_modified:
  - el-templo-api/src/modules/finance/cash-register-service.ts
  - el-templo-api/src/modules/finance/types.ts
  - el-templo-api/src/modules/finance/transaction-service.ts
  - el-templo-api/src/modules/subscriptions/routes.ts
  - el-templo-api/src/modules/auth/routes.ts
  - el-templo-api/src/modules/programs/routes.ts
  - el-templo-api/src/modules/finance/routes.ts
  - el-templo-api/src/modules/members/routes.ts
  - el-templo-api/src/jobs/auto-resume-pauses.ts
autonomous: true
requirements: [CAJA-02, CAJA-04]
must_haves:
  truths:
    - "resolveCashRegister(paymentMethod, branchId, currency) returns the efectivo caja of the branch for cash, the banco caja of the currency for transfer/card, and NULL for aura_credit/internal"
    - "Resolving an efectivo caja whose currency differs from the tx currency throws BadRequestError 'Moneda inconsistente'"
    - "TransactionService.create() stamps cash_register_id on every insert via the resolver — covering all 9 create paths through the single insert site"
    - "cash_register_id is server-derived; it is never read from the request body"
  artifacts:
    - path: "el-templo-api/src/modules/finance/cash-register-service.ts"
      provides: "CashRegisterService.resolveCashRegister + currency guard"
      contains: "class CashRegisterService"
    - path: "el-templo-api/src/modules/finance/transaction-service.ts"
      provides: "resolver wired into create() insert"
      contains: "resolveCashRegister"
  key_links:
    - from: "el-templo-api/src/modules/finance/transaction-service.ts"
      to: "CashRegisterService.resolveCashRegister"
      via: "create() before .insert(financialTransactions).values"
      pattern: "resolveCashRegister"
    - from: "el-templo-api/src/modules/finance/cash-register-service.ts"
      to: "cash_registers"
      via: "currency guard mirror of balance-service"
      pattern: "Moneda inconsistente"
---

<objective>
Create `CashRegisterService` housing the reusable `resolveCashRegister(paymentMethod, branchId, currency)` (D-01/D-02) with the currency guard (D-09/CAJA-04) embedded in the efectivo branch, then wire it into the single `TransactionService.create()` insert site so every create path auto-stamps `cash_register_id` (CAJA-02). Inject `CashRegisterService` into all 6 `new TransactionService(...)` sites.

Purpose: This is the runtime write-side. After this, every cash/transfer/card payment lands in the correct caja automatically, with cross-currency contamination rejected; aura_credit/internal resolve to NULL.
Output: New service, modified create() + input type, 6 instantiation sites updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/138-entidad-caja-saldos/138-CONTEXT.md
@.planning/phases/138-entidad-caja-saldos/138-RESEARCH.md

@el-templo-api/src/modules/finance/transaction-service.ts
@el-templo-api/src/modules/finance/balance-service.ts
@el-templo-api/src/modules/finance/types.ts

<interfaces>
<!-- Extracted from codebase. Use these directly. -->

TransactionService (transaction-service.ts):

- class TransactionService — constructor(db: DbInstance, log: FastifyBaseLogger, balanceService: BalanceService)
- The SINGLE runtime INSERT is at the `.insert(schema.financialTransactions).values({...})` block inside create()
  (currently lines ~214-236). The .values({...}) already includes `currency: input.currency ?? "ARS"` and
  `validationStatus: input.validationStatus ?? "validado"` — add `cashRegisterId` to this same block.
- create(input: CreateTransactionInput, recordedBy: number, tx?: TxHandle) — all 9 callers funnel here.

CreateTransactionInput (types.ts:57) — already carries: memberId, kind, direction, amount, currency?, paymentMethod,
transactionDate, effectiveDate, branchId, notes?, validationStatus?, links[]. PaymentMethod (types.ts:21) =
"cash" | "transfer" | "card" | "aura_credit" | "internal".

Currency guard precedent (balance-service.ts:154-158):
if (sub.currency !== transaction.currency) {
throw new BadRequestError(`Moneda inconsistente: la suscripcion es ${sub.currency}, la transaccion es ${transaction.currency}`);
}

BadRequestError: import from "../shared/errors".

CashRegisterService follows the BalanceService facade pattern: constructor(db, log).

The 6 instantiation sites (all currently `new TransactionService(fastify.db, fastify.log, balanceService)` /
`new TransactionService(db, log, balanceService)`):

- src/modules/subscriptions/routes.ts:63
- src/modules/auth/routes.ts:213
- src/modules/programs/routes.ts:167
- src/modules/finance/routes.ts:56
- src/modules/members/routes.ts:80
- src/jobs/auto-resume-pauses.ts:28
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CashRegisterService with resolveCashRegister + currency guard</name>
  <files>el-templo-api/src/modules/finance/cash-register-service.ts</files>
  <behavior>
    - resolver: paymentMethod 'cash' + branchId X + currency C → returns id of efectivo caja where branch_id=X AND is_active
    - resolver: paymentMethod 'transfer' or 'card' + currency C → returns id of banco caja where currency=C AND is_active
    - resolver: paymentMethod 'aura_credit' or 'internal' → returns null (no caja)
    - guard: efectivo caja found but its currency != C → throws BadRequestError "Moneda inconsistente: ..."
    - resolver: no matching efectivo/banco caja exists → throws BadRequestError "No existe caja ..."
  </behavior>
  <read_first>
    - el-templo-api/src/modules/finance/balance-service.ts (constructor(db, log) facade pattern + currency guard at lines 154-158)
    - el-templo-api/src/modules/finance/types.ts (PaymentMethod type)
    - el-templo-api/src/db/schema/cash-registers.ts (from plan 01 — column names id/type/branchId/currency/isActive)
    - .planning/phases/138-entidad-caja-saldos/138-RESEARCH.md § "Code Examples → Resolver", § "Currency Guard (CAJA-04 / D-09)"
    - .planning/phases/138-entidad-caja-saldos/138-CONTEXT.md D-01, D-02, D-09
  </read_first>
  <action>
    Create `cash-register-service.ts` exporting `class CashRegisterService` with `constructor(private readonly db: MySql2Database<typeof schema>, private readonly log: FastifyBaseLogger) {}` (mirror BalanceService). Add `async resolveCashRegister(paymentMethod: PaymentMethod, branchId: number, currency: string): Promise<number | null>` implementing D-01: return `null` for "aura_credit"/"internal"; for "transfer"/"card" select the banco caja where type='banco' AND currency=currency AND isActive, throw BadRequestError("No existe caja banco para {currency}") if absent, return its id; for "cash" select the efectivo caja where type='efectivo' AND branchId=branchId AND isActive, throw BadRequestError("No existe caja efectivo para la sucursal {branchId}") if absent, then enforce the currency guard (D-09): if the efectivo caja's currency !== currency throw BadRequestError("Moneda inconsistente: la caja es {caja.currency}, el cobro es {currency}") mirroring the balance-service message style. Use `firmMoneyConditions` NOT here (that is read-side). No `any`; type the rows via the select projection. The banco branch matches by currency by construction so no extra guard is needed there. Document with a comment that this is the single reusable choke-point reused by phase 140.
  </action>
  <verify>
    <automated>cd el-templo-api && grep -q "class CashRegisterService" src/modules/finance/cash-register-service.ts && grep -q "resolveCashRegister" src/modules/finance/cash-register-service.ts && grep -q "Moneda inconsistente" src/modules/finance/cash-register-service.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - cash-register-service.ts exports `class CashRegisterService` with constructor(db, log)
    - resolveCashRegister returns null for aura_credit/internal; banco-by-currency for transfer/card; efectivo-by-branch for cash
    - efectivo currency mismatch throws BadRequestError containing "Moneda inconsistente"
    - missing caja throws BadRequestError "No existe caja ..."
    - No `any` types; `pnpm typecheck` passes
  </acceptance_criteria>
  <done>Resolver + currency guard compile and encapsulate the D-01 rule as a reusable method.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire resolver into create() + thread cashRegisterId through the input type</name>
  <files>el-templo-api/src/modules/finance/types.ts, el-templo-api/src/modules/finance/transaction-service.ts</files>
  <behavior>
    - create() with paymentMethod 'cash' at branch X stamps cash_register_id = efectivo caja of X
    - create() with paymentMethod 'transfer' stamps cash_register_id = banco caja of tx.currency
    - create() with paymentMethod 'aura_credit' stamps cash_register_id = NULL
    - create() runs the resolver itself — callers (recordAssignmentCharge x4, enrollment add-on, correct() re-create, REST) need no edits
    - an optional input.cashRegisterId override slot is honored when provided, else the resolver runs
  </behavior>
  <read_first>
    - el-templo-api/src/modules/finance/transaction-service.ts (constructor at 94-98; the create() insert .values block ~214-236 with currency/validationStatus defaults to mirror)
    - el-templo-api/src/modules/finance/types.ts (CreateTransactionInput at 57)
    - el-templo-api/src/modules/finance/cash-register-service.ts (from task 1)
    - .planning/phases/138-entidad-caja-saldos/138-RESEARCH.md § "Create-Path Audit", § "Pattern 2", § "Wiring into create() (option A)"
    - .planning/phases/138-entidad-caja-saldos/138-CONTEXT.md D-01, D-03
  </read_first>
  <action>
    In `types.ts` add an optional `cashRegisterId?: number | null` to `CreateTransactionInput`, documented as server-derived (NEVER from the raw request body — like validationStatus). In `transaction-service.ts`: add `private readonly cashRegisterService: CashRegisterService` as a new constructor parameter (after balanceService). Inside `create()`, before the `.insert(schema.financialTransactions).values({...})` block, compute `const cashRegisterId = input.cashRegisterId !== undefined ? input.cashRegisterId : await this.cashRegisterService.resolveCashRegister(input.paymentMethod, input.branchId, input.currency ?? "ARS")`. Add `cashRegisterId,` to the `.values({...})` object (next to the existing currency/validationStatus defaults). This is option A from RESEARCH: one edit at the single insert site covers paths #1-#9. Per D-03 there is no manual override exposed through the REST body — the optional slot exists only for internal pre-resolution (e.g. future 139); the route schema must NOT accept it from clients. Do not edit any of the 9 callers.
  </action>
  <verify>
    <automated>cd el-templo-api && grep -q "cashRegisterId" src/modules/finance/types.ts && grep -q "resolveCashRegister" src/modules/finance/transaction-service.ts && grep -q "private readonly cashRegisterService" src/modules/finance/transaction-service.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - CreateTransactionInput has optional `cashRegisterId?: number | null` documented as server-derived
    - TransactionService constructor takes a CashRegisterService parameter
    - create() resolves cashRegisterId via the resolver (honoring the optional override) and stamps it in the single .values({...}) insert block
    - none of the 9 callers (recordAssignmentCharge, enrollment, correct, REST) are edited
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <done>Every create path auto-stamps cash_register_id server-side; the body cannot spoof it.</done>
</task>

<task type="auto">
  <name>Task 3: Inject CashRegisterService into all 6 TransactionService instantiation sites</name>
  <files>el-templo-api/src/modules/subscriptions/routes.ts, el-templo-api/src/modules/auth/routes.ts, el-templo-api/src/modules/programs/routes.ts, el-templo-api/src/modules/finance/routes.ts, el-templo-api/src/modules/members/routes.ts, el-templo-api/src/jobs/auto-resume-pauses.ts</files>
  <read_first>
    - el-templo-api/src/modules/finance/routes.ts:56 (the canonical `new TransactionService(fastify.db, fastify.log, balanceService)` shape + how balanceService is constructed just above it)
    - el-templo-api/src/jobs/auto-resume-pauses.ts:28 (the `db, log, balanceService` variant)
    - el-templo-api/src/modules/finance/cash-register-service.ts (from task 1)
    - .planning/phases/138-entidad-caja-saldos/138-RESEARCH.md § "Wiring into create()" → constructor note (5 routes + 1 job)
  </read_first>
  <action>
    At each of the 6 sites, construct a `CashRegisterService` (mirroring how `balanceService` is built right before each `new TransactionService(...)` — `new CashRegisterService(fastify.db, fastify.log)` in routes, `new CashRegisterService(db, log)` in the job) and pass it as the new 4th argument to `new TransactionService(...)`. Add the import of `CashRegisterService` from the finance module to each file. Keep the existing balanceService argument. No behavior change beyond DI wiring.
  </action>
  <verify>
    <automated>cd el-templo-api && test "$(grep -rl 'new CashRegisterService' src/modules/subscriptions/routes.ts src/modules/auth/routes.ts src/modules/programs/routes.ts src/modules/finance/routes.ts src/modules/members/routes.ts src/jobs/auto-resume-pauses.ts | wc -l)" -eq 6 && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All 6 files construct a CashRegisterService and pass it as the 4th arg to new TransactionService(...)
    - grep confirms `new CashRegisterService` appears in all 6 files
    - `pnpm typecheck` passes (constructor arity matches everywhere)
  </acceptance_criteria>
  <done>Every TransactionService instance has a CashRegisterService; the app compiles and boots.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                                     |
| ---------------------------- | ------------------------------------------------------------------------------- |
| REST request body → create() | Untrusted; paymentMethod/branchId/currency validated upstream by route schema   |
| create() → cash_registers    | cash_register_id is server-derived by the resolver, never trusted from the body |

## STRIDE Threat Register

| Threat ID | Category            | Component                               | Disposition | Mitigation Plan                                                                                                                                                                |
| --------- | ------------------- | --------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-138-04  | Spoofing            | cash_register_id from request body      | mitigate    | Resolver runs server-side in create(); the route schema does not accept cashRegisterId from clients (D-03). The optional input slot is internal-only.                          |
| T-138-05  | Tampering/Integrity | cross-currency association              | mitigate    | Currency guard inside resolveCashRegister throws "Moneda inconsistente" when efectivo caja currency != tx currency (D-09/CAJA-04). Banco resolves by currency by construction. |
| T-138-06  | Integrity           | missed create path → NULL caja for cash | mitigate    | Single insert site (transaction-service.ts) covers all 9 paths via create(); no per-caller resolution to miss (Pitfall 1).                                                     |
| T-138-SC  | Tampering           | npm/pip/cargo installs                  | accept      | Zero new packages (RESEARCH § Package Legitimacy Audit: not applicable). No install task.                                                                                      |

</threat_model>

<verification>
- `pnpm typecheck` green across all 9 modified files.
- Resolver returns correct caja per paymentMethod; currency mismatch throws.
- create() stamps cash_register_id server-side at the single insert site.
- All 6 instantiation sites updated (grep count = 6).
</verification>

<success_criteria>

- resolveCashRegister implements D-01 + the D-09 currency guard, reusable for phase 140 (CAJA-02, CAJA-04).
- create() auto-stamps cash_register_id on every path without editing callers.
- cash_register_id is never client-controlled.
  </success_criteria>

<output>
Create `.planning/phases/138-entidad-caja-saldos/138-02-SUMMARY.md` when done.
</output>
