---
phase: 138-entidad-caja-saldos
plan: 03
type: tdd
wave: 3
depends_on: ["138-01", "138-02"]
files_modified:
  - el-templo-api/src/modules/finance/cash-register-service.ts
  - el-templo-api/src/modules/finance/types.ts
  - el-templo-api/test/finance/cash-register-service.test.ts
autonomous: true
requirements: [CAJA-01, CAJA-02, CAJA-03, CAJA-04]
must_haves:
  truths:
    - "getBalance(cashRegisterId) returns firmeBalance = opening_balance + Σ(validated inflows since cutoff_date) for that caja"
    - "pendienteAmount (validation_status='pendiente', not voided, since cutoff) is returned SEPARATELY and never added to firmeBalance"
    - "A pre-cutoff validado transaction is labeled with cash_register_id but excluded from firmeBalance"
    - "Firm balance reuses firmMoneyConditions() so it inherits the phase-137 canonical filter"
  artifacts:
    - path: "el-templo-api/src/modules/finance/cash-register-service.ts"
      provides: "CashRegisterService.getBalance returning CashRegisterBalance"
      contains: "async getBalance"
    - path: "el-templo-api/test/finance/cash-register-service.test.ts"
      provides: "Integration tests for CAJA-01..04"
      contains: "getBalance"
  key_links:
    - from: "el-templo-api/src/modules/finance/cash-register-service.ts"
      to: "firmMoneyConditions"
      via: "getBalance firm SUM"
      pattern: "firmMoneyConditions"
    - from: "el-templo-api/src/modules/finance/cash-register-service.ts"
      to: "cash_registers.cutoff_date"
      via: "transaction_date >= cutoff gate"
      pattern: "cutoffDate|cutoff_date"
---

<objective>
Add `CashRegisterService.getBalance(cashRegisterId)` — the derived firm balance (opening_balance + Σ validated inflows since cutoff, reusing `firmMoneyConditions()`) with pendientes returned separately (CAJA-03) — and write the full integration test suite proving CAJA-01..04, filling the Wave 0 todos from plan 01. TDD: tests drive the getBalance shape.

Purpose: Closes the read-side and locks every CAJA invariant behind tests against real MySQL. Backend-only (D-10) — no REST endpoint, no UI (those are phases 141/142).
Output: getBalance method + CashRegisterBalance type + complete integration test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/138-entidad-caja-saldos/138-CONTEXT.md
@.planning/phases/138-entidad-caja-saldos/138-RESEARCH.md

@el-templo-api/src/modules/finance/firm-money.ts
@el-templo-api/src/modules/finance/cash-register-service.ts
@el-templo-api/test/finance/transaction-service.test.ts

<interfaces>
<!-- Extracted from codebase. Use these directly. -->

firmMoneyConditions() (firm-money.ts:32) returns:
[ isNull(financialTransactions.voidedAt), eq(financialTransactions.validationStatus, "validado") ]
Single source of truth for "firm money" (not voided AND validado). Reuse verbatim.

getSummary firm SUM shape to clone (transaction-service.ts:1176-1187):
const [totalRow] = await this.db
.select({ total: sql<number>`COALESCE(SUM(${schema.financialTransactions.amount}), 0)` })
.from(schema.financialTransactions)
.where(and(...conds));
// conds includes: eq(direction,"inflow"), ...firmMoneyConditions()

cash_registers columns (from plan 01): id, type, branchId (nullable), currency, openingBalance, cutoffDate, isActive.

Recommended return shape (RESEARCH § getBalance Pattern):
interface CashRegisterBalance {
cashRegisterId: number;
currency: string;
firmeBalance: number; // opening_balance + Σ validados since cutoff
pendienteAmount: number; // Σ pendientes since cutoff, shown SEPARATELY (CAJA-03)
}

Test scaffolding (from plan 01 + transaction-service.test.ts): createTestApp, registerUser, seedSubscription,
direct Drizzle inserts in beforeAll. Per-worker test MySQL via test/setup.ts.
Project rule (MEMORY): do NOT run the full local suite — run only this file locally; CI runs the suite on push to staging.
</interfaces>
</context>

<feature>
  <name>CashRegisterService.getBalance — derived firm balance + separate pendientes</name>
  <files>el-templo-api/src/modules/finance/cash-register-service.ts, el-templo-api/src/modules/finance/types.ts, el-templo-api/test/finance/cash-register-service.test.ts</files>
  <behavior>
    Cases (input → expected):
    - caja with opening_balance=1000, two validado inflows of 500 + 300 after cutoff → firmeBalance 1800
    - same caja with a pendiente inflow of 200 after cutoff → pendienteAmount 200, firmeBalance UNCHANGED at 1800
    - a validado inflow of 9999 dated BEFORE cutoff_date → labeled with cash_register_id but firmeBalance UNCHANGED (cutoff excludes it)
    - a voided validado inflow after cutoff → excluded from firmeBalance (firmMoneyConditions)
    - getBalance returns the caja's fixed currency in the result
    - aura_credit charge → cash_register_id NULL → never appears in any caja's firmeBalance
  </behavior>
  <implementation>
    Add `async getBalance(cashRegisterId: number): Promise<CashRegisterBalance>` to CashRegisterService.
    First SELECT the caja's openingBalance, currency, cutoffDate by id (throw NotFoundError if absent).
    firmeBalance: clone the getSummary SUM — `COALESCE(SUM(amount),0)` WHERE
      eq(cashRegisterId, X) AND eq(direction,"inflow") AND ...firmMoneyConditions() AND gte(transactionDate, cutoffDate);
      then firmeBalance = openingBalance + Number(total).
    pendienteAmount: a SEPARATE SUM WHERE eq(cashRegisterId, X) AND eq(validationStatus,"pendiente")
      AND isNull(voidedAt) AND gte(transactionDate, cutoffDate). NOT added to firmeBalance (CAJA-03).
    INFLOW-ONLY for 138 (no outflows yet); add a `// TODO 139: subtract outflows (cash_transfer/expense)` marker
    so phase 139 extends the body without changing the signature (D-08 hides derivation from callers). Add the
    `CashRegisterBalance` interface to finance/types.ts. No REST endpoint, no UI (D-10). No `any`;
    `catch (err: unknown)` + instanceof Error if any try/catch.
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1: RED — fill getBalance + resolver + seed tests against the real DB</name>
  <files>el-templo-api/test/finance/cash-register-service.test.ts</files>
  <read_first>
    - el-templo-api/test/finance/cash-register-service.test.ts (the Wave 0 scaffold from plan 01 — fill its todos)
    - el-templo-api/test/finance/transaction-service.test.ts (createTestApp/seedSubscription/insert patterns)
    - el-templo-api/src/modules/finance/firm-money.ts (the filter the firm assertion mirrors)
    - .planning/phases/138-entidad-caja-saldos/138-RESEARCH.md § "Phase Requirements → Test Map"
  </read_first>
  <action>
    Convert the 6 it.todo groups into real failing tests against the per-worker test MySQL. Cover: (seed) the migration produced 8 cajas with the right type/currency/branch_id and no efectivo for the virtual ONLINE branch; (resolver) cash→efectivo(branch), transfer/card→banco(currency), aura_credit/internal→NULL; (create stamps caja) a `cash` charge created via TransactionService.create() — and specifically NOT via REST, e.g. through a recordAssignmentCharge-style path — has a non-null cash_register_id; (getBalance firme) opening + Σ validados since cutoff equals expected; (cutoff excludes history) a pre-cutoff validado tx is labeled but absent from firmeBalance; (currency guard) resolving an efectivo caja whose currency != tx currency throws "Moneda inconsistente". Add the pendientes-don't-sum assertion (CAJA-03) inside the getBalance group. Use direct Drizzle inserts to place rows on both sides of cutoff_date. These MUST fail first (getBalance/CashRegisterBalance not implemented yet). No `any`.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm test test/finance/cash-register-service.test.ts; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - All 6 Test Map groups have real assertions (no remaining it.todo)
    - getBalance/currency-guard/pendientes tests reference the not-yet-implemented getBalance and fail RED
    - `pnpm test test/finance/cash-register-service.test.ts` exits non-zero (RED)
    - No `any` types
  </acceptance_criteria>
  <done>Failing tests encode CAJA-01..04 invariants.</done>
</task>

<task type="tdd">
  <name>Task 2: GREEN — implement getBalance + CashRegisterBalance</name>
  <files>el-templo-api/src/modules/finance/cash-register-service.ts, el-templo-api/src/modules/finance/types.ts</files>
  <read_first>
    - el-templo-api/src/modules/finance/cash-register-service.ts (resolver from plan 02 — add getBalance here)
    - el-templo-api/src/modules/finance/transaction-service.ts:1176-1187 (the getSummary SUM to clone)
    - el-templo-api/src/modules/finance/firm-money.ts (firmMoneyConditions to reuse verbatim)
    - el-templo-api/src/db/schema/cash-registers.ts (openingBalance/currency/cutoffDate columns)
    - .planning/phases/138-entidad-caja-saldos/138-RESEARCH.md § "getBalance Pattern & Cutoff/Opening-Balance Model"
    - .planning/phases/138-entidad-caja-saldos/138-CONTEXT.md D-06, D-08
  </read_first>
  <action>
    Add `interface CashRegisterBalance { cashRegisterId: number; currency: string; firmeBalance: number; pendienteAmount: number }` to finance/types.ts. Implement `getBalance` per the <implementation> block: SELECT caja (openingBalance/currency/cutoffDate; throw NotFoundError if missing), firm SUM = clone of getSummary filtered by cash_register_id + direction='inflow' + spread firmMoneyConditions() + gte(transactionDate, cutoffDate); firmeBalance = openingBalance + total. pendienteAmount = a separate SUM (validationStatus='pendiente' AND isNull(voidedAt) AND gte cutoff) returned separately, NOT added. Add the `// TODO 139: subtract outflows` marker. No `any`. Run only this test file (project rule: no full local suite) until green, then `pnpm typecheck`.
  </action>
  <verify>
    <automated>cd el-templo-api && grep -q "async getBalance" src/modules/finance/cash-register-service.ts && grep -q "firmMoneyConditions" src/modules/finance/cash-register-service.ts && grep -q "CashRegisterBalance" src/modules/finance/types.ts && pnpm test test/finance/cash-register-service.test.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - getBalance returns CashRegisterBalance with firmeBalance = opening + Σ validados since cutoff
    - getBalance reuses firmMoneyConditions() (grep confirms) and gates by cutoff_date
    - pendienteAmount computed in a separate SUM and never added to firmeBalance
    - pre-cutoff and voided rows excluded from firmeBalance (tests green)
    - `// TODO 139` outflow marker present; no REST endpoint added (backend-only D-10)
    - `pnpm test test/finance/cash-register-service.test.ts` green and `pnpm typecheck` passes
  </acceptance_criteria>
  <done>All CAJA-01..04 tests pass; firm balance derived correctly with pendientes apart.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                 | Description                                                                             |
| ------------------------ | --------------------------------------------------------------------------------------- |
| getBalance read → ledger | Read-only aggregation; no mutation; no external input beyond an internal cashRegisterId |

## STRIDE Threat Register

| Threat ID | Category  | Component                          | Disposition | Mitigation Plan                                                                                                      |
| --------- | --------- | ---------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------- |
| T-138-07  | Integrity | pendientes leaking into firme      | mitigate    | firmMoneyConditions() requires validado; pendienteAmount is a separate SUM never added (CAJA-03) — asserted by test. |
| T-138-08  | Integrity | backfilled history inflating saldo | mitigate    | gte(transactionDate, cutoffDate) on every SUM; test proves a pre-cutoff validado row is excluded (D-05/Pitfall 2).   |
| T-138-09  | Integrity | firm filter drift vs phase 137     | mitigate    | getBalance reuses firmMoneyConditions() verbatim — inherits the canonical filter, never inlined (D-08).              |
| T-138-SC  | Tampering | npm/pip/cargo installs             | accept      | Zero new packages (RESEARCH § Package Legitimacy Audit: not applicable). No install task.                            |

</threat_model>

<verification>
- RED: tests fail before getBalance exists.
- GREEN: `pnpm test test/finance/cash-register-service.test.ts` passes all CAJA-01..04 cases.
- `pnpm typecheck` green.
- No REST endpoint / UI added (backend-only, D-10).
- Full suite runs in CI on push to staging (not locally).
</verification>

<success_criteria>

- getBalance returns derived firm balance + separate pendientes, gated by cutoff, reusing the 137 filter (CAJA-03).
- All CAJA-01..04 invariants locked by integration tests.
- Phase remains backend-only.
  </success_criteria>

<output>
Create `.planning/phases/138-entidad-caja-saldos/138-03-SUMMARY.md` when done.
</output>
