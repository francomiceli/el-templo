---
phase: 112-enrollment-service-admin-add-ons
plan: 06
type: execute
wave: 5
depends_on: ["112-04"]
files_modified:
  - el-templo-api/test/programs/member-app-enrollment-dropdown.test.ts
  - el-templo-app/src/composables/useCurrentProgram.ts
  - el-templo-app/src/modules/programs/components/ProgramProgressCard.vue
autonomous: false
requirements:
  - ADDON-MEMBER-UI-01
  - ADDON-MEMBER-UI-02
must_haves:
  truths:
    - "Member-app program dropdown lists ALL active enrollments (plan_linked + plan_bundle + admin_addon) with no visual distinction"
    - "Member can switch programs via the dropdown and the weekly view re-renders against the selected enrollment"
    - "If a code-level gap exists (e.g. dropdown filters by source), patch the gap; otherwise document zero-code-change outcome"
    - "Integration test asserts GET /api/members/me/enrollments returns admin_addon enrollments alongside plan_linked/plan_bundle for an authenticated member"
  artifacts:
    - path: "el-templo-api/test/programs/member-app-enrollment-dropdown.test.ts"
      provides: "Integration test asserting member-facing enrollment list includes admin_addons"
      min_lines: 80
  key_links:
    - from: "el-templo-app/src/modules/programs/components/ProgramProgressCard.vue"
      to: "useCurrentProgram composable"
      via: "imports enrollments + currentEnrollmentId; renders dropdown when enrollments.length > 1"
      pattern: "useCurrentProgram"
---

<objective>
Verify (and patch only if needed) that the member-app's existing program dropdown — built in phase 104 around the bundle pattern — already surfaces admin_addon enrollments alongside plan_linked/plan_bundle. Per D-21 the expected outcome is ZERO frontend code change; the deliverable is an integration test plus a manual staging walkthrough confirming the dropdown works end-to-end.

Purpose: Closes ADDON-MEMBER-UI-01..02. The bundle pattern from phase 104 already iterates over `useCurrentProgram().enrollments`, which delegates to `GET /api/members/me/enrollments` — that endpoint reads `program_enrollments` regardless of source. Plan 04 wrote the new admin_addon rows; this plan confirms the read path picks them up automatically.

Output:

- 1 backend integration test asserting the member-facing GET endpoint returns admin_addon rows.
- 0 expected frontend code changes (composable + component already source-agnostic). If a gap is found (e.g. an unexpected `source` filter), patch the minimal lines needed.
- Manual member-app walkthrough verification on staging.
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-enrollment-service-admin-add-ons/112-CONTEXT.md
@.planning/phases/112-enrollment-service-admin-add-ons/112-04-PLAN.md
@CLAUDE.md
@el-templo-app/src/composables/useCurrentProgram.ts
@el-templo-app/src/modules/programs/components/ProgramProgressCard.vue
@el-templo-api/src/modules/programs/routes.ts

<interfaces>
Existing member-facing endpoints (programs/routes.ts, post-phase-104):
- `GET /api/members/me/current-program` (line ~445) — returns the active program pointer for the member.
- `PUT /api/members/me/current-program` (line ~464) — updates the pointer.
- `GET /api/members/me/enrollments` (line ~518) — lists ACTIVE enrollments, ordered by id ASC. Used by the program selector composable.

Expected behavior of GET /api/members/me/enrollments after Plan 04: returns rows for plan_linked, plan_bundle, AND admin_addon sources. The existing query (verify in programs/service.ts) should NOT filter by source — if it does, that's the gap this plan patches.

Frontend consumer:

- `el-templo-app/src/modules/training/composables/useCurrentProgram` — exposes `enrollments`, `currentEnrollmentId`, `select` per ProgramProgressCard.vue:175.
- `el-templo-app/src/modules/programs/components/ProgramProgressCard.vue:175,179` — `canSwitch = enrollments.value.length > 1`. Renders the dropdown unconditionally for multi-enrollment members.

Risk areas to inspect (each is a candidate for the zero-or-minimal patch):

1. The backend SELECT in `getEnrollmentsByMemberSelf` (or whatever the member-facing service method is named) — does it filter by source? If yes (unlikely), remove the filter.
2. The frontend `useCurrentProgram` composable — does it filter the response by source? Probably not, but verify.
3. The dropdown component — does it render some visual distinction by source? Per ADDON-MEMBER-UI-01 it MUST NOT distinguish; if Plan 04 backend response includes a `source` field that the frontend uses for badge styling, strip the badge for member-facing UX.
   </interfaces>
   </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integration test — GET /api/members/me/enrollments returns admin_addon rows</name>
  <files>el-templo-api/test/programs/member-app-enrollment-dropdown.test.ts</files>
  <read_first>
    - el-templo-api/test/helpers.ts (createTestApp, getAuthToken, cleanAllTestData)
    - el-templo-api/test/subscriptions/_helpers.ts (createMember, createPlan, assignPlan)
    - el-templo-api/test/programs/current-program.test.ts (closest pattern — same endpoint family)
    - el-templo-api/src/modules/programs/routes.ts:518+ (GET /api/members/me/enrollments handler)
    - el-templo-api/src/modules/programs/service.ts (the method backing this endpoint)
    - .planning/phases/112-enrollment-service-admin-add-ons/112-CONTEXT.md (D-21)
  </read_first>
  <behavior>
    - Test 1: Seed member with assigned linked-program plan → GET /api/members/me/enrollments returns 1 row with source='plan_linked'.
    - Test 2: Same member + admin assigns an add-on (POST /api/admin/users/:userId/program-addons with pricePaid=0) → GET /api/members/me/enrollments returns 2 rows, one source='plan_linked', one source='admin_addon', both status='active'.
    - Test 3: Same member + bundle plan switch → GET returns N+1 rows including the admin_addon (admin_addon survives because tearDownForSubscription protects programs covered by the new bundle, OR the addon was created post-switch — verify the actual semantics with a deliberate sequence).
    - Test 4: Member-token enforcement — admin token CANNOT call GET /api/members/me/enrollments (membership endpoint is member-scoped).
  </behavior>
  <action>
    Create `el-templo-api/test/programs/member-app-enrollment-dropdown.test.ts` patterned on `current-program.test.ts`:

    Helpers:
    - `seedMemberWithLinkedPlan(programId)` → returns `{userId, memberToken, subId}`.
    - `adminAssignAddon(adminToken, userId, programId, pricePaid)` → POST /api/admin/users/{userId}/program-addons.
    - `getMemberEnrollments(memberToken)` → GET /api/members/me/enrollments.

    Implement the 4 tests. Test 3 sequence:
    1. Seed member with linked-program plan A → assign addon for program B (pricePaid=0).
    2. Switch to bundle plan via admin assign. Bundle teardown for old sub runs; new bundle plan auto-enrolls all active programs.
    3. Assert GET response includes program A (re-enrolled by bundle), program B (addon, transferred to new sub via transferAddons), and any other goal programs the bundle includes.

    Constraints:
    - Real eltemplo_test MySQL.
    - No mocks. No `any`.
    - Member-token enforcement test (Test 4): expect 401 or 403 (whichever the existing route returns).

  </action>
  <verify>
    <automated>cd el-templo-api && pnpm test test/programs/member-app-enrollment-dropdown.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists at the specified path.
    - `grep -c "describe\|it(" file` returns at least 4.
    - `grep "admin_addon" file` matches.
    - `grep "/api/members/me/enrollments" file` matches.
    - `cd el-templo-api && pnpm test test/programs/member-app-enrollment-dropdown.test.ts` exits 0 with all 4 passing.
    - If Test 2 or 3 fails because the backend service filters by source, patch `programs/service.ts` to remove the filter and document the patch in the SUMMARY. Re-run until green.
  </acceptance_criteria>
  <done>
    Backend integration test confirms admin_addon enrollments are visible in the member-facing list endpoint.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit member-app composable + component for source-based filtering or visual distinction; patch only if needed</name>
  <files>
    el-templo-app/src/composables/useCurrentProgram.ts
    el-templo-app/src/modules/programs/components/ProgramProgressCard.vue
  </files>
  <read_first>
    - el-templo-app/src/modules/training/composables/useCurrentProgram.ts (the full composable — verify file path; the import in ProgramProgressCard.vue:153 says `src/modules/training/composables/useCurrentProgram`)
    - el-templo-app/src/modules/programs/components/ProgramProgressCard.vue (full file — dropdown rendering)
    - el-templo-app/src/modules/progression/pages/MiTemplo.vue:322 (consumer that hydrates enrollments)
    - .planning/phases/112-enrollment-service-admin-add-ons/112-CONTEXT.md (D-21, ADDON-MEMBER-UI-01..02)
  </read_first>
  <action>
    Read the composable + component carefully. For each file, check:

    1. **Composable** (`useCurrentProgram`): Does it filter the response by source? Search for `source`, `filter`, `admin_addon`, `plan_linked` in the file. If a filter exists, REMOVE it (the locked decision D-21 says no source-based filtering).

    2. **Component** (`ProgramProgressCard.vue`): Does the dropdown template render a `source`-based badge or styling difference? Look for `v-if`, `class={...}`, `chip`, `badge` near the enrollment iterator. If yes, strip the source-conditional classes/badges.

    Expected outcome: BOTH files are already source-agnostic and require zero changes. If that's confirmed:
    - Add a comment at the top of `ProgramProgressCard.vue` near the dropdown render block:
      ```
      <!-- Phase 112 D-21: dropdown lists ALL enrollments regardless of source.
           Verified zero-code outcome — admin_addon rows surface naturally via the bundle pattern from phase 104. -->
      ```
    - Otherwise, patch the minimal diff and document it.

    Do NOT add a "type: admin_addon" indicator to the member-facing UI — the locked decision D-21 explicitly says no visual distinction.

  </action>
  <verify>
    <automated>cd el-templo-app && pnpm tsc --noEmit &amp;&amp; grep -c "source.*admin_addon\|filter.*source" src/composables/useCurrentProgram.ts src/modules/training/composables/useCurrentProgram.ts 2>/dev/null || echo "no source filter found"</automated>
  </verify>
  <acceptance_criteria>
    - `cd el-templo-app && pnpm tsc --noEmit` exits 0 (zero introduced regressions).
    - Either: (a) zero diff in the two files except an optional explanatory comment, OR (b) a minimal patch with a clear before/after explanation in the task summary.
    - `grep -c "Add-on\|Incluido en plan" el-templo-app/src/modules/programs/components/ProgramProgressCard.vue` returns 0 (no member-facing source distinction).
  </acceptance_criteria>
  <done>
    Composable + component verified source-agnostic; no member-facing visual distinction by source; minimal patch applied if a gap was found.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual member-app walkthrough on staging — admin assigns add-on, member sees it in the dropdown</name>
  <what-built>
    End-to-end verification of the full member-facing surface: admin assigns an add-on via the new endpoint (Plan 04 + Plan 05 UI), the member opens the app, the program dropdown lists the new add-on alongside the plan_linked/plan_bundle programs, and switching to it loads the weekly view content.
  </what-built>
  <how-to-verify>
    Pre-conditions:
    - Migrations 0111 + 0112 applied to staging (operator confirms this happened during Plan 01 and Plan 04 sign-off).
    - Plan 05 admin frontend deployed to staging.
    - Member-app build deployed to staging (or use the local dev build pointed at staging API).
    - Test member account on staging with an active subscription.

    Steps:
    1. Open the staging admin app. Log in as admin.
    2. Navigate to /alumnos/{test-member-id} → "Programas" tab.
    3. Note the current enrollments. Click "Asignar programa adicional".
    4. Select a program the member is NOT currently enrolled in. Set pricePaid=0. Submit.
    5. Confirm the row appears in the admin "Programas" tab list with the "Add-on" badge.
    6. Open the staging member app. Log in as the test member (or use a logged-in session).
    7. Navigate to the member home / Mi Templo page.
    8. Confirm the program dropdown (in ProgramProgressCard) now shows TWO programs (or the previously-seen count + 1).
    9. Confirm the new program in the dropdown has NO visual distinction marking it as an add-on (per D-21).
    10. Select the new program from the dropdown. Confirm the weekly view re-renders showing that program's week 1 content.
    11. Repeat steps 2-10 with `pricePaid=5000`. Confirm:
        - Admin sees `$5.000` in the row.
        - Member-side experience is identical (no visual difference).
        - Backend `financial_transactions` table on staging has the new row (operator queries via existing tooling).

    If any step fails, capture the gap, hand it back to engineering, and re-iterate Task 2 to patch.

  </how-to-verify>
  <files>el-templo-app/src/modules/programs/components/ProgramProgressCard.vue</files>
  <read_first>
    - .planning/phases/112-enrollment-service-admin-add-ons/112-CONTEXT.md (D-21)
    - el-templo-app/src/modules/programs/components/ProgramProgressCard.vue
  </read_first>
  <action>
    Operator runs the 11-step walkthrough on staging. No code changes expected unless step 8/10 reveals a gap; in that case, return to Task 2 to patch.
  </action>
  <resume-signal>Type "approved" if all 11 steps pass. Otherwise list failed steps for re-iteration.</resume-signal>
  <verify>
    <automated>echo "Manual verification step — no automated check"</automated>
  </verify>
  <acceptance_criteria>
    - All 11 manual walkthrough steps pass.
    - Member dropdown shows the new admin_addon enrollment with no visual source distinction.
    - Selecting the addon program loads its weekly view.
    - Operator approves via "approved" signal.
  </acceptance_criteria>
  <done>
    End-to-end staging verification passes; member-facing surface confirmed working without code changes (or with documented minimal patch).
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                                                                |
| ------------------------------ | ---------------------------------------------------------------------------------------------------------- |
| member token → /me/enrollments | endpoint is member-scoped (request.user.userId is the only filter); admin tokens cannot impersonate member |

## STRIDE Threat Register

| Threat ID   | Category               | Component                            | Disposition | Mitigation Plan                                                                                             |
| ----------- | ---------------------- | ------------------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------- |
| T-112-06-01 | Information Disclosure | enrollment list visible cross-member | mitigate    | Endpoint already enforces request.user.userId scoping (phase 104); Test 4 verifies admin cannot call /me/\* |
| T-112-06-02 | Spoofing               | source-based UX manipulation         | accept      | D-21 forbids visual distinction by source; client-side enforcement only — no security implication           |

</threat_model>

<verification>
- `cd el-templo-api && pnpm test test/programs/member-app-enrollment-dropdown.test.ts` exits 0 with all 4 passing.
- `cd el-templo-app && pnpm tsc --noEmit` exits 0.
- Manual staging walkthrough (11 steps) approved by operator.
</verification>

<success_criteria>

1. Backend integration test confirms /me/enrollments returns admin_addon rows alongside plan_linked/plan_bundle.
2. Member-app composable + component verified source-agnostic (zero or minimal patch).
3. Manual staging walkthrough approved.
4. No regression in member-app tsc.
   </success_criteria>

<output>
After completion, create `.planning/phases/112-enrollment-service-admin-add-ons/112-06-SUMMARY.md`. Include:
- Whether any frontend patch was needed (expected: no).
- Output of the new test file (4 passing).
- Operator confirmation of staging walkthrough.
- Screenshot or description of member-app dropdown after admin assigns an add-on.
- End-of-phase status: all 24 requirements (ENROLL-01..05, ADDON-SCHEMA-01..05, ADDON-API-01..06, ADDON-LIFE-01..04, ADDON-ADMIN-UI-01..05, ADDON-MEMBER-UI-01..02) closed.
</output>
