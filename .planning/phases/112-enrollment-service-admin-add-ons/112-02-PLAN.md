---
phase: 112-enrollment-service-admin-add-ons
plan: 02
type: execute
wave: 2
depends_on: ["112-01"]
files_modified:
  - el-templo-api/src/modules/programs/enrollment-service.ts
  - el-templo-api/src/modules/programs/index.ts
  - el-templo-api/src/modules/subscriptions/service.ts
  - el-templo-api/src/modules/auth/routes.ts
  - el-templo-api/src/modules/subscriptions/routes.ts
  - el-templo-api/src/modules/subscriptions/member-routes.ts
  - el-templo-api/src/modules/members/routes.ts
  - el-templo-api/src/modules/scheduling/routes.ts
  - el-templo-api/src/modules/attendance/routes.ts
  - el-templo-api/src/jobs/auto-resume-pauses.ts
autonomous: true
requirements:
  - ENROLL-01
  - ENROLL-02
  - ENROLL-03
  - ENROLL-04
  - ENROLL-05
must_haves:
  truths:
    - "All 6 inline programEnrollments inserts in subscriptions/service.ts are replaced by EnrollmentService method calls"
    - "tearDownBundleEnrollments + tearDownLinkedProgramEnrollment helpers are replaced by EnrollmentService.tearDownForSubscription (generalized to all sources)"
    - "EnrollmentService is constructor-injected into SubscriptionService at every DI site (11 sites)"
    - "Mutator methods accept tx?: TxHandle optional, never opening their own transactions"
    - "Phase 111 regression tests (expire-cancel-linked-program.test.ts, bundle-todos-los-programas.test.ts) pass unmodified"
    - 'grep -nE "(insert|update).*programEnrollments" subscriptions/service.ts returns 0 hits'
  artifacts:
    - path: "el-templo-api/src/modules/programs/enrollment-service.ts"
      provides: "EnrollmentService class with enrollFromPlan, enrollAddon (stub), tearDownForSubscription, transferAddons (stub), pauseForSubscription (stub), resumeForSubscription (stub), getActiveEnrollmentsForSubscription"
      exports: ["EnrollmentService"]
      min_lines: 250
    - path: "el-templo-api/src/modules/programs/index.ts"
      provides: "Re-exports EnrollmentService for consumers"
      exports: ["EnrollmentService"]
  key_links:
    - from: "el-templo-api/src/modules/subscriptions/service.ts"
      to: "el-templo-api/src/modules/programs/enrollment-service.ts"
      via: "constructor parameter (5th positional arg)"
      pattern: "private enrollmentService\\??: EnrollmentService"
    - from: "all 11 SubscriptionService instantiation sites"
      to: "EnrollmentService"
      via: "passed as 5th constructor arg"
      pattern: "new SubscriptionService\\([^)]+enrollmentService"
---

<objective>
Extract a single EnrollmentService that owns every write to `program_enrollments`, replacing the 6 dispersed `tx.insert(programEnrollments)` calls and the 2 teardown helpers (`tearDownBundleEnrollments`, `tearDownLinkedProgramEnrollment`) currently embedded in `subscriptions/service.ts`. The refactor is BEHAVIOR-PRESERVING — no functional change, only relocation behind an interface.

Purpose: Plans 03 (lifecycle hooks for pause/resume + transferAddons) and 04 (admin add-on assignment endpoint) need a single chokepoint to add new behavior. Phase 111 surfaced the cost of the spaghetti: adding `tearDownBundleEnrollments` required hunting 6 duplicated insert sites.

Output:

- New `EnrollmentService` class in `el-templo-api/src/modules/programs/enrollment-service.ts` (full bodies for `enrollFromPlan`, `tearDownForSubscription`, `getActiveEnrollmentsForSubscription`; explicit-throw stubs for the other 4 methods that Plans 03/04 fill).
- All 6 inserts in `subscriptions/service.ts` replaced by `await this.enrollmentService.enrollFromPlan(...)`.
- The 2 teardown helpers replaced by a single generalized `tearDownForSubscription(subId, tx)` (covers all sources, not just bundle).
- 11 DI sites updated to pass `enrollmentService` into `SubscriptionService`.
- Phase 111 tests pass without modification (no-regression gate).
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-enrollment-service-admin-add-ons/112-CONTEXT.md
@.planning/phases/112-enrollment-service-admin-add-ons/112-01-PLAN.md
@CLAUDE.md
@el-templo-api/src/modules/finance/balance-service.ts
@el-templo-api/src/modules/finance/transaction-service.ts
@el-templo-api/src/modules/programs/service.ts
@el-templo-api/src/modules/subscriptions/service.ts

<interfaces>
TxHandle (canonical export):
```typescript
// el-templo-api/src/modules/finance/balance-service.ts
export type TxHandle = Parameters<Parameters<DbInstance["transaction"]>[0]>[0];
```
Import as `import type { TxHandle } from "../finance/balance-service";` — DO NOT redefine (phase 111-02 decision).

Existing 6 enrollment insert sites in `subscriptions/service.ts`:

- Site 1 (~1204): assignPlan, plan.linkedProgramId branch — single insert with cancel-then-create.
- Site 2 (~1257): assignPlan, plan.grantsAllPrograms branch — bulk insert across active programs (Foundation excluded).
- Site 3 (~2485): changePlanNow, target plan.linkedProgramId branch.
- Site 4 (~2536): changePlanNow, target plan.grantsAllPrograms branch.
- Site 5 (~3191): renewSubscription, plan.linkedProgramId branch.
- Site 6 (~3872): activateScheduledSub, runs on this.db (not in a tx).

Each becomes a single `await this.enrollmentService.enrollFromPlan(userId, {id, linkedProgramId, grantsAllPrograms}, subId, tx?)` call. The service handles BOTH branches internally driven by plan flags.

Existing teardown helpers (to be deleted):

- `tearDownBundleEnrollments` (line 3442) — bundle-only protection logic
- `tearDownLinkedProgramEnrollment` (line 3567) — single-program-plan protection logic

Generalized replacement: `tearDownForSubscription(subId, tx?)` — driven by `subscription_id` (the new column from Plan 01). Cancels all active|paused rows where `subscription_id = subId`, applying the same "another active|paused sub of the same user covers the same program → keep alive" protection rule.

11 SubscriptionService DI sites to update:

1. el-templo-api/src/modules/auth/routes.ts:210
2. el-templo-api/src/modules/subscriptions/routes.ts:66
3. el-templo-api/src/modules/subscriptions/member-routes.ts:20
4. el-templo-api/src/modules/members/routes.ts:495
5. el-templo-api/src/modules/members/routes.ts:698
6. el-templo-api/src/modules/members/routes.ts:842
7. el-templo-api/src/modules/scheduling/routes.ts:74
8. el-templo-api/src/modules/scheduling/routes.ts:466
9. el-templo-api/src/modules/attendance/routes.ts:35
10. el-templo-api/src/modules/attendance/routes.ts:171
11. el-templo-api/src/jobs/auto-resume-pauses.ts:25

Each gets `const enrollmentService = new EnrollmentService(fastify.db, fastify.log);` (or equivalent for non-fastify sites) and the 5th arg of `new SubscriptionService(...)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create EnrollmentService class skeleton + index.ts re-export</name>
  <files>
    el-templo-api/src/modules/programs/enrollment-service.ts
    el-templo-api/src/modules/programs/index.ts
  </files>
  <read_first>
    - el-templo-api/src/modules/finance/transaction-service.ts (DI + optional-tx pattern)
    - el-templo-api/src/modules/finance/balance-service.ts (TxHandle export site)
    - el-templo-api/src/modules/shared/audit-log.ts (required-tx contract — different from optional)
    - el-templo-api/src/modules/programs/service.ts (existing module conventions)
    - el-templo-api/src/modules/subscriptions/service.ts:1180-1280 (the source bodies)
    - el-templo-api/src/modules/subscriptions/service.ts:3442-3700 (both teardown helpers)
    - el-templo-api/src/db/schema/program-enrollments.ts (post-Plan-01 shape)
    - .planning/phases/112-enrollment-service-admin-add-ons/112-CONTEXT.md (D-05..D-09)
    - CLAUDE.md
  </read_first>
  <behavior>
    - Test 1: `EnrollmentService` is exported from `enrollment-service.ts` and re-exported from `programs/index.ts`.
    - Test 2: Constructor signature is `(db: MySql2Database<typeof schema>, log: FastifyBaseLogger)` matching ProgramsService.
    - Test 3: 7 public method names exist with `tx?: TxHandle` as trailing optional parameter on every mutator: `enrollFromPlan`, `enrollAddon`, `tearDownForSubscription`, `transferAddons`, `pauseForSubscription`, `resumeForSubscription`, `getActiveEnrollmentsForSubscription`.
    - Test 4: `pnpm tsc --noEmit` exits 0.
    - Test 5: Stubs (`enrollAddon`, `transferAddons`, `pauseForSubscription`, `resumeForSubscription`) throw `Error("Plan 0N — ...")` so accidental invocation surfaces loudly in tests.
  </behavior>
  <action>
    Create `el-templo-api/src/modules/programs/enrollment-service.ts` as a skeleton class. Bodies for `enrollFromPlan` and `tearDownForSubscription` are filled in Task 2; this task ships the surface only.

    Required content (verbatim guidance — paste directly, then adjust imports if needed):

    Header comment block: explain the responsibility (single chokepoint for program_enrollments writes), atomicity contract (D-07 — caller decides tx, service does NOT open its own), and source semantics (D-01 — plan_linked / plan_bundle / admin_addon).

    Imports:
    - `and, eq, inArray, isNotNull, ne, or, sql` from "drizzle-orm"
    - type `MySql2Database` from "drizzle-orm/mysql2"
    - type `FastifyBaseLogger` from "fastify"
    - `* as schema` from "../../db/schema"
    - `BadRequestError, ConflictError, NotFoundError` from "../shared/errors"
    - type `TxHandle` from "../finance/balance-service"

    Type aliases:
    ```typescript
    type DbInstance = MySql2Database<typeof schema>;
    type DbOrTx = DbInstance | TxHandle;
    ```

    Exported interfaces:
    ```typescript
    export interface EnrollFromPlanInput {
      id: number;
      linkedProgramId: number | null;
      grantsAllPrograms: boolean;
    }
    export interface EnrollAddonInput {
      programId: number;
      pricePaid?: number | null;
      assignedBy: number;
      notes?: string | null;
    }
    ```

    Class `EnrollmentService` with constructor `(db, log)`, private `runner(tx?: TxHandle): DbOrTx { return tx ?? this.db; }`, and these 7 method signatures (bodies as described — stubs throw, fillers wait for Task 2):

    1. `getActiveEnrollmentsForSubscription(subscriptionId: number, tx?: TxHandle): Promise<Array<{id, programId, userId, source, status}>>` — implement now (small read, used by Plans 03/04). Body: select id, programId, userId, source, status from programEnrollments where subscriptionId matches AND status IN ('active','paused').

    2. `enrollFromPlan(userId: number, plan: EnrollFromPlanInput, subscriptionId: number, tx?: TxHandle): Promise<void>` — Task 2 fills body. For Task 1, body is `throw new Error("Task 2 fills body");`.

    3. `enrollAddon(userId: number, subscriptionId: number, input: EnrollAddonInput, tx?: TxHandle): Promise<{enrollmentId: number}>` — `throw new Error("Plan 04 — admin add-on creation not yet implemented");`.

    4. `tearDownForSubscription(subscriptionId: number, tx?: TxHandle): Promise<void>` — Task 2 fills body. Stub: `throw new Error("Task 2 fills body");`.

    5. `transferAddons(fromSubId: number, toSubId: number, tx?: TxHandle): Promise<{transferred: number}>` — `throw new Error("Plan 03 — addon transfer not yet implemented");`.

    6. `pauseForSubscription(subscriptionId: number, tx?: TxHandle): Promise<void>` — `throw new Error("Plan 03 — pause hook not yet implemented");`.

    7. `resumeForSubscription(subscriptionId: number, tx?: TxHandle): Promise<void>` — `throw new Error("Plan 03 — resume hook not yet implemented");`.

    Then update (or create) `el-templo-api/src/modules/programs/index.ts` to add:
    ```typescript
    export { EnrollmentService } from "./enrollment-service";
    export type { EnrollFromPlanInput, EnrollAddonInput } from "./enrollment-service";
    ```
    Preserve any existing exports in that file.

    Constraints:
    - No `any`. No `console.log`. TxHandle imported from balance-service only.
    - File must compile cleanly under tsc strict mode.

  </action>
  <verify>
    <automated>cd el-templo-api &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `el-templo-api/src/modules/programs/enrollment-service.ts` exists.
    - `grep -c "export class EnrollmentService" enrollment-service.ts` returns 1.
    - `grep "import type { TxHandle } from \"../finance/balance-service\"" enrollment-service.ts` matches.
    - `grep -cE "async (enrollFromPlan|enrollAddon|tearDownForSubscription|transferAddons|pauseForSubscription|resumeForSubscription|getActiveEnrollmentsForSubscription)" enrollment-service.ts` returns 7.
    - `grep -c "tx?: TxHandle" enrollment-service.ts` returns at least 7 (one per method).
    - `grep -c "throw new Error" enrollment-service.ts` returns at least 4 (stubs for the 4 not-yet-implemented methods).
    - `grep "EnrollmentService" el-templo-api/src/modules/programs/index.ts` matches.
    - `cd el-templo-api && pnpm tsc --noEmit` exits 0.
    - `grep -cE ": any|as any" enrollment-service.ts` returns 0.
  </acceptance_criteria>
  <done>
    EnrollmentService skeleton file with 7 method signatures (1 implemented read + 2 task-2-stubs + 4 future-plan stubs), TxHandle imported canonically, index.ts re-exports the class, tsc clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill enrollFromPlan + tearDownForSubscription bodies; replace 6 insert sites and 4 teardown call sites in subscriptions/service.ts; delete the 2 legacy private helpers</name>
  <files>
    el-templo-api/src/modules/programs/enrollment-service.ts
    el-templo-api/src/modules/subscriptions/service.ts
  </files>
  <read_first>
    - el-templo-api/src/modules/subscriptions/service.ts (FULL FILE — 4413 LOC; specific ranges below)
    - el-templo-api/src/modules/subscriptions/service.ts:1189-1278 (Sites 1+2: assignPlan)
    - el-templo-api/src/modules/subscriptions/service.ts:2461-2557 (Sites 3+4: changePlanNow)
    - el-templo-api/src/modules/subscriptions/service.ts:3176-3205 (Site 5: renewSubscription)
    - el-templo-api/src/modules/subscriptions/service.ts:3853-3889 (Site 6: activateScheduledSub)
    - el-templo-api/src/modules/subscriptions/service.ts:3442-3546 (tearDownBundleEnrollments — to delete)
    - el-templo-api/src/modules/subscriptions/service.ts:3567-3700 (tearDownLinkedProgramEnrollment — to delete)
    - el-templo-api/src/modules/subscriptions/service.ts:2050-2060, 2310-2320, 3742-3765 (call sites of the 2 helpers)
    - el-templo-api/test/subscriptions/expire-cancel-linked-program.test.ts (regression gate)
    - el-templo-api/test/subscriptions/bundle-todos-los-programas.test.ts (regression gate)
  </read_first>
  <behavior>
    - Test 1: `grep -nE "(insert|update).*programEnrollments" el-templo-api/src/modules/subscriptions/service.ts` returns ZERO matches.
    - Test 2: `grep -c "tearDownBundleEnrollments\|tearDownLinkedProgramEnrollment" el-templo-api/src/modules/subscriptions/service.ts` returns 0 (both helpers deleted, no callers).
    - Test 3: `expire-cancel-linked-program.test.ts` passes unmodified.
    - Test 4: `bundle-todos-los-programas.test.ts` passes unmodified.
    - Test 5: All other phase 111 + assign + changePlan + renewal + lifecycle tests pass unmodified.
    - Test 6: `pnpm tsc --noEmit` exits 0.
  </behavior>
  <action>
    **Step A — Fill `enrollFromPlan` body** in `el-templo-api/src/modules/programs/enrollment-service.ts`. Port the exact logic from subscriptions/service.ts:1189-1278:

    - If `plan.linkedProgramId !== null`: cancel any existing active enrollment for `(userId, plan.linkedProgramId)` (set status='cancelled', cancelledAt=NOW), then insert a new row with `{userId, programId: plan.linkedProgramId, status: 'active', currentWeek: 1, sessionsCompletedThisWeek: 0, weekUnlockedAt: NOW, source: 'plan_linked', subscriptionId}`. Log info `"Auto-created program enrollment from plan (linkedProgramId)"` with `{userId, programId, subscriptionId}`.

    - If `plan.grantsAllPrograms === true`: select active programs where `isActive=true AND goalPlanType IS NOT NULL` (Foundation excluded per phase 104 R3+R7). Subtract programs already in active enrollments for this user. Bulk insert remaining as `source: 'plan_bundle'` with `subscriptionId`. Log info `"Bundle (Todos los Programas) auto-enroll completed (Foundation excluded)"`.

    Both branches use `runner(tx)` to honor the caller's transaction. Both branches MAY run inside the same call (a plan could conceivably set both flags, though current invariants forbid it — handle defensively: each `if` block is independent).

    **Step B — Fill `tearDownForSubscription` body** in `enrollment-service.ts`. Algorithm:

    1. Resolve the sub's userId + planId (`SELECT userId, planId FROM subscriptions WHERE id = subId LIMIT 1`). If no row, return silently (sub deleted).

    2. Compute "protected programs" — list of programIds covered by ANOTHER active|paused sub of the same user (excluding subscriptionId). Join subscriptions → subscription_plans, collect `linkedProgramId` (non-null) and a boolean `anyProtectorIsBundle` if any protector has `grantsAllPrograms=true`.

    3. Select all enrollments where `subscription_id = subId AND status IN ('active','paused')`.

    4. Filter out rows whose programId is in the protected set OR whose program would be covered by a bundle protector.

    5. UPDATE the survivors to `status='cancelled', cancelledAt=NOW` via `inArray(id, cancelIds)`.

    6. NULL `users.current_program_enrollment_id` if it pointed to any cancelled enrollment.

    7. Log info `"Enrollment teardown completed (tearDownForSubscription)"` with `{subscriptionId, userId, cancelledCount, protectedProgramCount, anyProtectorIsBundle}`.

    Use `runner(tx)` exclusively for all queries. Do NOT open `db.transaction` inside the service.

    **Step C — Wire EnrollmentService into SubscriptionService constructor.** In `el-templo-api/src/modules/subscriptions/service.ts`:

    1. Add import near the existing `import type { TransactionService } from "../finance";`:
       ```typescript
       import { EnrollmentService } from "../programs/enrollment-service";
       ```

    2. Extend constructor (line ~151) to add a 5th optional parameter:
       ```typescript
       constructor(
         private db: MySql2Database<typeof schema>,
         private log: FastifyBaseLogger,
         private auraService: AuraService,
         private transactionService?: TransactionService,
         private enrollmentService?: EnrollmentService,
       ) {}
       ```

    3. Add private guard helper:
       ```typescript
       private requireEnrollmentService(): EnrollmentService {
         if (!this.enrollmentService) {
           throw new Error("EnrollmentService not injected — DI site missing");
         }
         return this.enrollmentService;
       }
       ```

    **Step D — Replace 6 insert sites + delete the redundant cancel-block at changePlanNow:2461-2481.**

    For each site, the call shape is:
    ```typescript
    await this.requireEnrollmentService().enrollFromPlan(
      userId,
      { id: plan.id, linkedProgramId: plan.linkedProgramId, grantsAllPrograms: plan.grantsAllPrograms },
      sub.id,        // or subId / scheduled.id depending on context
      tx,            // omit (undefined) for activateScheduledSub site 6 which uses this.db
    );
    ```

    Site replacements:
    - Site 1 (assignPlan, ~1189-1217): replace the entire `if (plan.linkedProgramId)` block with a single `enrollFromPlan` call. Site 2 (~1219-1278) `if (plan.grantsAllPrograms)` block: DELETE — the consolidated call covers both branches via plan flags. Net result: one call replaces ~90 lines.
    - Sites 3+4 (changePlanNow, ~2461-2557): DELETE the explicit cancel-old-enrollment block (~2461-2481), DELETE both create-new blocks (~2483-2498 and ~2507-2557). Replace with a SINGLE `enrollFromPlan(userId, targetPlan, subId, tx)` call positioned where the old block 2483 was. Rationale: the `tearDownForSubscription(existingSub.id)` call at line ~2313 already cancels the OLD plan's enrollment (driven by subscription_id from Plan 01 backfill), so the inline cancel at 2461-2481 is redundant.
    - Site 5 (renewSubscription, ~3176-3205): replace the `if (plan.linkedProgramId)` block with `enrollFromPlan(userId, plan, subId, tx)`. Note: the existing renewal logic only created when no active enrollment exists; `enrollFromPlan` handles this idempotency internally via the cancel-then-insert pattern (cancel of an already-cancelled row is a no-op).
    - Site 6 (activateScheduledSub, ~3853-3889): replace the `if (newPlan.linkedProgramId !== prevPlan.linkedProgramId)` block with a guard `if (newPlan.linkedProgramId !== prevPlan.linkedProgramId && (newPlan.linkedProgramId || newPlan.grantsAllPrograms))` then `enrollFromPlan(scheduled.userId, newPlan, scheduled.id /* no tx */)`.

    **Step E — Replace 4 teardown call sites:**
    - Line ~2054-2055 (cancelSubscription): replace BOTH `tearDownBundleEnrollments(tx, ...)` AND `tearDownLinkedProgramEnrollment(tx, ...)` calls with ONE call: `await this.requireEnrollmentService().tearDownForSubscription(sub.id, tx);`.
    - Line ~2313 (changePlanNow, on `this.db`): replace the `if (currentPlan.grantsAllPrograms)` guard + call with: `await this.requireEnrollmentService().tearDownForSubscription(existingSub.id);` (no tx — preserves the "committed before new-sub tx" semantics already present).
    - Lines ~3752 + 3758 (autoExpireSubscriptions loop): replace BOTH calls with ONE: `await this.requireEnrollmentService().tearDownForSubscription(row.id);`.

    **Step F — DELETE the two private helpers entirely**: `tearDownBundleEnrollments` (lines 3442-3546) and `tearDownLinkedProgramEnrollment` (lines 3567-3700). Confirm via grep that no other site references them.

    **Step G — Run regression tests.** Execute the phase 111 tests UNMODIFIED:
    ```
    cd el-templo-api && pnpm test test/subscriptions/expire-cancel-linked-program.test.ts test/subscriptions/bundle-todos-los-programas.test.ts
    ```
    All tests must pass. If any fail, the refactor preserved insufficient behavior — debug the divergence rather than modifying the test file.

    Constraints:
    - No `any`. No `console.log`.
    - Preserve every `log.info` message wording (exact strings) inside `enrollFromPlan` and `tearDownForSubscription` — log monitoring downstream expects them.
    - The test files MUST remain unchanged (D-08 no-regression gate). If a test fails, fix the service, not the test.
    - During this task, the constructor parameter `enrollmentService` stays optional. Task 3 wires every DI site so the parameter is always provided; a follow-up plan may flip to required.

  </action>
  <verify>
    <automated>cd el-templo-api &amp;&amp; pnpm tsc --noEmit &amp;&amp; pnpm test test/subscriptions/expire-cancel-linked-program.test.ts test/subscriptions/bundle-todos-los-programas.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd el-templo-api && grep -nE "(insert|update).*programEnrollments" src/modules/subscriptions/service.ts | wc -l` returns 0.
    - `cd el-templo-api && grep -c "tearDownBundleEnrollments" src/modules/subscriptions/service.ts` returns 0 (helper + all callers deleted).
    - `cd el-templo-api && grep -c "tearDownLinkedProgramEnrollment" src/modules/subscriptions/service.ts` returns 0.
    - `cd el-templo-api && grep -c "this.requireEnrollmentService" src/modules/subscriptions/service.ts` returns at least 6 (4 teardown sites collapsed to 3 unique call points + 6 enrollFromPlan sites collapsed to 5 unique call points = at least 8 unique calls but minimum bar is 6).
    - `cd el-templo-api && grep -c "enrollFromPlan\|tearDownForSubscription" src/modules/subscriptions/service.ts` returns at least 8.
    - `cd el-templo-api && grep "import { EnrollmentService }" src/modules/subscriptions/service.ts` matches.
    - `cd el-templo-api && grep "private enrollmentService" src/modules/subscriptions/service.ts` matches.
    - `cd el-templo-api && pnpm tsc --noEmit` exits 0.
    - `cd el-templo-api && pnpm test test/subscriptions/expire-cancel-linked-program.test.ts` exits 0.
    - `cd el-templo-api && pnpm test test/subscriptions/bundle-todos-los-programas.test.ts` exits 0.
    - `git diff test/subscriptions/expire-cancel-linked-program.test.ts test/subscriptions/bundle-todos-los-programas.test.ts` produces zero output (test files unmodified).
    - In `enrollment-service.ts`, both `enrollFromPlan` and `tearDownForSubscription` no longer throw `"Task 2 fills body"`: `grep "Task 2 fills body" src/modules/programs/enrollment-service.ts` returns 0 lines.
  </acceptance_criteria>
  <done>
    Subscriptions service has zero direct programEnrollments mutations; both legacy teardown helpers deleted; all enrollment lifecycle goes through EnrollmentService; phase 111 regression tests pass without modification; tsc clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire EnrollmentService into all 11 SubscriptionService DI sites + run full subscription test suite</name>
  <files>
    el-templo-api/src/modules/auth/routes.ts
    el-templo-api/src/modules/subscriptions/routes.ts
    el-templo-api/src/modules/subscriptions/member-routes.ts
    el-templo-api/src/modules/members/routes.ts
    el-templo-api/src/modules/scheduling/routes.ts
    el-templo-api/src/modules/attendance/routes.ts
    el-templo-api/src/jobs/auto-resume-pauses.ts
  </files>
  <read_first>
    - el-templo-api/src/modules/auth/routes.ts:195-220 (existing service-instantiation block)
    - el-templo-api/src/modules/subscriptions/routes.ts:55-75 (canonical DI ordering)
    - el-templo-api/src/modules/subscriptions/member-routes.ts:15-30
    - el-templo-api/src/modules/members/routes.ts:485-510, 690-710, 835-855
    - el-templo-api/src/modules/scheduling/routes.ts:65-85, 460-475
    - el-templo-api/src/modules/attendance/routes.ts:25-45, 165-180
    - el-templo-api/src/jobs/auto-resume-pauses.ts (full file — small)
    - el-templo-api/src/modules/programs/index.ts (re-export verified in Task 1)
  </read_first>
  <action>
    For EACH of the 11 sites where `new SubscriptionService(...)` is called:

    1. Add (above the `new SubscriptionService(...)` call):
       ```typescript
       const enrollmentService = new EnrollmentService(fastify.db, fastify.log);
       ```
       For non-fastify sites (`el-templo-api/src/jobs/auto-resume-pauses.ts:25`):
       ```typescript
       const enrollmentService = new EnrollmentService(db, log);
       ```

    2. Add the import at top of file (near other module imports):
       ```typescript
       import { EnrollmentService } from "../programs/enrollment-service";
       ```
       (Adjust relative path per file location: subscriptions/* uses `../programs`, members/* uses `../programs`, scheduling/*, attendance/*, auth/*, jobs/* use respective paths.)

    3. Pass `enrollmentService` as the 5th positional argument:
       ```typescript
       const subscriptionService = new SubscriptionService(
         fastify.db,
         fastify.log,
         auraService,
         transactionService,
         enrollmentService,   // NEW 5th arg
       );
       ```

    Sites to update (verify line numbers via grep — they may have shifted slightly):
    1. `el-templo-api/src/modules/auth/routes.ts:210`
    2. `el-templo-api/src/modules/subscriptions/routes.ts:66`
    3. `el-templo-api/src/modules/subscriptions/member-routes.ts:20`
    4. `el-templo-api/src/modules/members/routes.ts:495`
    5. `el-templo-api/src/modules/members/routes.ts:698`
    6. `el-templo-api/src/modules/members/routes.ts:842`
    7. `el-templo-api/src/modules/scheduling/routes.ts:74`
    8. `el-templo-api/src/modules/scheduling/routes.ts:466`
    9. `el-templo-api/src/modules/attendance/routes.ts:35`
    10. `el-templo-api/src/modules/attendance/routes.ts:171`
    11. `el-templo-api/src/jobs/auto-resume-pauses.ts:25`

    DRY note: if a single file has multiple SubscriptionService instantiations (members/routes.ts has 3, scheduling/routes.ts has 2, attendance/routes.ts has 2), declare ONE `enrollmentService` per route handler if the handlers are independent, or ONE per file if they share scope at the module top-level (mirror the existing `auraService`/`transactionService` declaration pattern in that file).

    After all 11 sites are wired, run the FULL subscription test suite:
    ```
    cd el-templo-api && pnpm test test/subscriptions/ test/programs/
    ```

    All tests must pass.

    Constraints:
    - No `any`. No `console.log`.
    - Mirror the existing service-instantiation idiom in each file (some files use `fastify.db`/`fastify.log`, others use a local `db`/`log` variable).
    - Do NOT modify any test file in this task.

  </action>
  <verify>
    <automated>cd el-templo-api &amp;&amp; pnpm tsc --noEmit &amp;&amp; grep -rn "new SubscriptionService" src/ | wc -l &amp;&amp; grep -rln "new EnrollmentService" src/ &amp;&amp; pnpm test test/subscriptions/ test/programs/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "new SubscriptionService" el-templo-api/src/` returns exactly 11 lines (sites unchanged in count).
    - For each of the 11 sites, the 5th constructor argument is `enrollmentService`. Verify with: `grep -A 5 "new SubscriptionService" el-templo-api/src/{relevant_files} | grep -c "enrollmentService"` returns at least 11.
    - `grep -rn "new EnrollmentService" el-templo-api/src/` returns at least 11 lines (one per DI site, possibly more if multi-instance).
    - `cd el-templo-api && grep -rln "import { EnrollmentService }" src/` lists at least 8 files (the 7 listed + subscriptions/service.ts from Task 2).
    - `cd el-templo-api && pnpm tsc --noEmit` exits 0.
    - `cd el-templo-api && pnpm test test/subscriptions/` exits 0 (full subscription suite passes).
    - `cd el-templo-api && pnpm test test/programs/` exits 0.
    - `git diff el-templo-api/test/` returns zero output (no test file modified).
  </acceptance_criteria>
  <done>
    All 11 SubscriptionService instantiation sites pass an EnrollmentService; full subscription + programs test suites pass; tsc clean; no test files modified.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                     | Description                                                                                                |
| -------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| caller's tx → EnrollmentService              | atomicity is delegated; misuse (forgetting to pass tx when inside a transaction) silently breaks atomicity |
| subscriptions/service.ts → EnrollmentService | private `requireEnrollmentService` guard surfaces missing DI as a runtime error rather than a silent no-op |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                    | Disposition | Mitigation Plan                                                                                                                                                  |
| ----------- | ---------------------- | -------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| T-112-02-01 | Tampering              | refactor preserving teardown semantics       | mitigate    | Phase 111 regression tests (`expire-cancel-linked-program.test.ts`, `bundle-todos-los-programas.test.ts`) run unmodified — if any fails the refactor is rejected |
| T-112-02-02 | Denial of Service      | optional `enrollmentService` constructor arg | mitigate    | `requireEnrollmentService` guard throws explicit error if a DI site forgets to pass it; Task 3 wires all 11 sites                                                |
| T-112-02-03 | Tampering              | tearDownForSubscription protection logic     | mitigate    | Unified method preserves the legacy "another active                                                                                                              | paused sub covers the program → keep alive" rule (joins subscriptions→subscription_plans, computes protected program set) |
| T-112-02-04 | Information Disclosure | log message wording change                   | accept      | log.info messages preserved verbatim from legacy sites; downstream log monitoring is unaffected                                                                  |

</threat_model>

<verification>
- `cd el-templo-api && pnpm tsc --noEmit` exits 0.
- `cd el-templo-api && pnpm test test/subscriptions/ test/programs/` exits 0 with all tests green.
- `grep -nE "(insert|update).*programEnrollments" el-templo-api/src/modules/subscriptions/service.ts` returns 0 lines.
- `grep -c "tearDownBundleEnrollments\|tearDownLinkedProgramEnrollment" el-templo-api/src/modules/subscriptions/service.ts` returns 0.
- `git diff el-templo-api/test/` returns zero output.
- `grep -rn "new SubscriptionService" el-templo-api/src/` returns 11 sites, every one passing `enrollmentService` as the 5th arg.
</verification>

<success_criteria>

1. `EnrollmentService` exists with 7 methods; bodies for `enrollFromPlan`, `tearDownForSubscription`, `getActiveEnrollmentsForSubscription` filled; 4 stubs raise explicit errors.
2. `subscriptions/service.ts` has zero direct programEnrollments mutations; both legacy teardown helpers deleted; all enrollment lifecycle routes through EnrollmentService.
3. All 11 DI sites pass `enrollmentService` as 5th constructor arg.
4. Phase 111 regression tests pass without modification (D-08 gate).
5. Full `test/subscriptions/` + `test/programs/` test suites pass.
6. tsc clean, zero `any` introduced, zero `console.log` introduced.
   </success_criteria>

<output>
After completion, create `.planning/phases/112-enrollment-service-admin-add-ons/112-02-SUMMARY.md`. Include:
- Final LOC of `enrollment-service.ts` (post-Task 2 with bodies filled).
- Net LOC reduction in `subscriptions/service.ts` (target ≥ 200 lines removed).
- Confirmation that phase 111 tests passed unmodified.
- List of 11 DI sites updated (file:line for each).
- Any pre-existing test that needed adjustment (expected: zero — flag if any).
</output>
