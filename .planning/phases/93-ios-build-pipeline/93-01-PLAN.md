---
phase: 93-ios-build-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-app/src-capacitor/ios/App/App.xcodeproj/project.pbxproj
  - .github/SECRETS.md
autonomous: true
requirements: [IOS-01, IOS-05]

must_haves:
  truths:
    - "MARKETING_VERSION is 1.0.0 (three-part semver matching Android versionName)"
    - "CURRENT_PROJECT_VERSION can be overridden via xcodebuild build setting for CI"
    - "SECRETS.md documents all iOS signing secrets with step-by-step setup instructions"
    - "CODE_SIGN_STYLE is Manual for Release configuration in pbxproj"
  artifacts:
    - path: "el-templo-app/src-capacitor/ios/App/App.xcodeproj/project.pbxproj"
      provides: "Xcode project with production signing configuration and version management"
      contains: "MARKETING_VERSION = 1.0.0"
    - path: ".github/SECRETS.md"
      provides: "iOS signing secrets documentation with Apple Developer portal setup guide"
      contains: "IOS_BUILD_CERTIFICATE_BASE64"
  key_links:
    - from: "project.pbxproj"
      to: "build-ios-production.yml (Plan 02)"
      via: "xcodebuild reads bundle ID, signing style, version from pbxproj"
      pattern: "PRODUCT_BUNDLE_IDENTIFIER = com.eltemplo.app"
---

<objective>
Prepare the Xcode project for CI signing and document all Apple signing secrets.

Purpose: The iOS project's pbxproj needs version management aligned with the Android pattern (MARKETING_VERSION = 1.0.0, CURRENT_PROJECT_VERSION overridable from CI via github.run_number) and CODE_SIGN_STYLE set to Manual for Release so the GitHub Actions workflow can inject certificates. SECRETS.md needs iOS-specific secrets documented so the user can create certificates and add them to GitHub before running the production workflow.

Output: Updated project.pbxproj with production-ready signing settings, and SECRETS.md with iOS signing section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.github/SECRETS.md
@.github/workflows/build-ios-staging.yml

<interfaces>
<!-- Existing pbxproj build settings that the executor needs to modify. -->

From project.pbxproj Release target config (504EC3181FED79650016851F):

```
CODE_SIGN_STYLE = Automatic;
CURRENT_PROJECT_VERSION = 1;
MARKETING_VERSION = 1.0;
PRODUCT_BUNDLE_IDENTIFIER = com.eltemplo.app;
```

From project.pbxproj Debug target config (504EC3171FED79650016851F):

```
CODE_SIGN_STYLE = Automatic;
CURRENT_PROJECT_VERSION = 1;
MARKETING_VERSION = 1.0;
PRODUCT_BUNDLE_IDENTIFIER = com.eltemplo.app;
```

From build-ios-staging.yml secrets pattern:

```
IOS_BUILD_CERTIFICATE_BASE64
IOS_P12_PASSWORD
IOS_PROVISION_PROFILE_BASE64
APPLE_API_KEY_ID
APPLE_API_ISSUER_ID
APPLE_API_KEY_BASE64
```

From Android version management in build.gradle:

```groovy
versionCode = (System.getenv("VERSION_CODE") ?: "1").toInteger()
versionName = "1.0.0"
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Xcode project for CI signing and version management</name>
  <files>el-templo-app/src-capacitor/ios/App/App.xcodeproj/project.pbxproj</files>
  <read_first>
    - el-templo-app/src-capacitor/ios/App/App.xcodeproj/project.pbxproj (full current state)
    - .github/workflows/build-ios-staging.yml (see how CODE_SIGN_STYLE=Manual is passed to xcodebuild)
    - .github/workflows/build-android-production.yml (version management pattern with VERSION_CODE env var)
  </read_first>
  <action>
  Modify the project.pbxproj to prepare for CI-based manual signing and version management:

1. **MARKETING_VERSION**: Change from `1.0` to `1.0.0` in BOTH Debug and Release target configurations (504EC3171FED79650016851F and 504EC3181FED79650016851F). This three-part semver matches the Android versionName pattern.

2. **CODE_SIGN_STYLE**: Change from `Automatic` to `Manual` in the Release target configuration ONLY (504EC3181FED79650016851F). Keep Debug as `Automatic` so local Xcode development still works with automatic signing. The staging workflow already passes `CODE_SIGN_STYLE=Manual` as an xcodebuild arg, but having it in the Release config as the default is cleaner and consistent.

3. **CODE_SIGN_IDENTITY**: Add `CODE_SIGN_IDENTITY = "Apple Distribution";` to the Release target configuration (504EC3181FED79650016851F). This tells xcodebuild which certificate identity to use when manual signing.

4. **PROVISIONING_PROFILE_SPECIFIER**: Add `PROVISIONING_PROFILE_SPECIFIER = "";` to the Release target configuration. This will be overridden by xcodebuild args in the workflow, but needs to exist so Xcode doesn't fall back to automatic.

Note: CURRENT_PROJECT_VERSION stays at `1` in the pbxproj. The workflow will override it via the xcodebuild `CURRENT_PROJECT_VERSION=${{ github.run_number }}` build setting argument (same pattern as Android's VERSION_CODE env var). No pbxproj change needed for build number -- xcodebuild args take precedence.

The exact lines to change in the Release target config block (504EC3181FED79650016851F):

BEFORE:

```
CODE_SIGN_STYLE = Automatic;
CURRENT_PROJECT_VERSION = 1;
...
MARKETING_VERSION = 1.0;
```

AFTER:

```
CODE_SIGN_IDENTITY = "Apple Distribution";
CODE_SIGN_STYLE = Manual;
CURRENT_PROJECT_VERSION = 1;
...
MARKETING_VERSION = 1.0.0;
PROVISIONING_PROFILE_SPECIFIER = "";
```

For the Debug target config block (504EC3171FED79650016851F), only change MARKETING_VERSION:

BEFORE:

```
MARKETING_VERSION = 1.0;
```

AFTER:

```
MARKETING_VERSION = 1.0.0;
```

Do NOT change CODE_SIGN_STYLE in Debug -- keep it `Automatic` for local development.
</action>
<verify>
<automated>grep -c "MARKETING_VERSION = 1.0.0" el-templo-app/src-capacitor/ios/App/App.xcodeproj/project.pbxproj | grep -q "2" && grep -c 'CODE_SIGN_STYLE = Manual' el-templo-app/src-capacitor/ios/App/App.xcodeproj/project.pbxproj | grep -q "1" && grep -c 'CODE_SIGN_IDENTITY = "Apple Distribution"' el-templo-app/src-capacitor/ios/App/App.xcodeproj/project.pbxproj | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
</verify>
<acceptance_criteria> - grep 'MARKETING_VERSION = 1.0.0' project.pbxproj returns exactly 2 matches (Debug + Release) - grep 'CODE_SIGN_STYLE = Manual' project.pbxproj returns exactly 1 match (Release only) - grep 'CODE_SIGN_STYLE = Automatic' project.pbxproj returns exactly 1 match (Debug only) - grep 'CODE_SIGN_IDENTITY = "Apple Distribution"' project.pbxproj returns exactly 1 match (Release) - grep 'PROVISIONING_PROFILE_SPECIFIER' project.pbxproj returns at least 1 match (Release) - grep 'PRODUCT_BUNDLE_IDENTIFIER = com.eltemplo.app' project.pbxproj still returns 2 matches (unchanged) - grep 'CURRENT_PROJECT_VERSION = 1' project.pbxproj still returns 2 matches (unchanged, CI overrides at build time)
</acceptance_criteria>
<done>
Xcode project configured for manual signing on Release with Apple Distribution identity, MARKETING_VERSION is 1.0.0, Debug remains Automatic for local development. CURRENT_PROJECT_VERSION remains 1 (overridden by xcodebuild args in CI).
</done>
</task>

<task type="auto">
  <name>Task 2: Document iOS signing secrets in SECRETS.md</name>
  <files>.github/SECRETS.md</files>
  <read_first>
    - .github/SECRETS.md (current state -- has Android signing section as pattern to follow)
    - .github/workflows/build-ios-staging.yml (lines 8-27 list the prerequisite secrets)
  </read_first>
  <action>
  Add an "iOS Signing (App Store)" section to SECRETS.md, placed after the existing "Android Signing (Play Store)" section. Follow the same table format and documentation depth as the Android section.

**New secrets table to add (under heading `### iOS Signing (App Store)`):**

| Secret                         | Description                                                         | Example                                |
| ------------------------------ | ------------------------------------------------------------------- | -------------------------------------- |
| `IOS_BUILD_CERTIFICATE_BASE64` | Apple Distribution certificate (.p12) encoded as base64             | `MIIKfAIBAzCC...` (long base64 string) |
| `IOS_P12_PASSWORD`             | Password for the .p12 certificate export                            | `your-secure-p12-password`             |
| `IOS_PROVISION_PROFILE_BASE64` | App Store provisioning profile (.mobileprovision) encoded as base64 | `MIIKfAIBAzCC...` (long base64 string) |
| `APPLE_API_KEY_ID`             | App Store Connect API key ID                                        | `ABC123DEFG`                           |
| `APPLE_API_ISSUER_ID`          | App Store Connect API issuer ID                                     | `12345678-abcd-efgh-ijkl-123456789012` |
| `APPLE_API_KEY_BASE64`         | App Store Connect API private key (.p8) encoded as base64           | `LS0tLS1CRUdJ...`                      |

**Step-by-step generation guide (add below the table, matching the Android keystore guide format):**

#### Step 1: Create Apple Distribution Certificate

1. Go to https://developer.apple.com/account/resources/certificates/list
2. Click "+" to create a new certificate
3. Select "Apple Distribution" (NOT "iOS Distribution" which is legacy)
4. Upload a Certificate Signing Request (CSR) generated from Keychain Access on a Mac, OR generate one via the Apple Developer portal
5. Download the .cer file
6. If using a Mac: Double-click to install in Keychain Access, then export as .p12 with a password
7. If no Mac available: Use the portal's built-in private key download or a third-party tool

#### Step 2: Create App Store Provisioning Profile

1. Go to https://developer.apple.com/account/resources/profiles/list
2. Click "+" to create a new profile
3. Select "App Store Connect" under Distribution
4. Select the app ID: `com.eltemplo.app`
5. Select the Distribution certificate from Step 1
6. Name it: `El Templo App Store`
7. Download the .mobileprovision file

#### Step 3: Create App Store Connect API Key

1. Go to https://appstoreconnect.apple.com/access/integrations/api
2. Click "+" to generate a new API key
3. Name: `GitHub Actions Deploy`
4. Role: `App Manager` (minimum needed for TestFlight upload)
5. Download the .p8 file (can only be downloaded ONCE)
6. Note the Key ID and Issuer ID displayed on the page

#### Step 4: Encode for GitHub Secrets

```bash
# Certificate
base64 -i distribution_certificate.p12 | pbcopy
# Paste as IOS_BUILD_CERTIFICATE_BASE64

# Provisioning profile
base64 -i El_Templo_App_Store.mobileprovision | pbcopy
# Paste as IOS_PROVISION_PROFILE_BASE64

# API key
base64 -i AuthKey_ABC123DEFG.p8 | pbcopy
# Paste as APPLE_API_KEY_BASE64
```

On Linux (no pbcopy): `base64 -w 0 file.p12` and copy the output.

#### Step 5: Add secrets to GitHub

1. Go to repo **Settings** > **Secrets and variables** > **Actions**
2. Add all 6 secrets listed in the table above

#### Step 6: Backup

Store the original .p12 certificate, .mobileprovision profile, and .p8 API key securely. The certificate expires after 1 year and will need to be renewed.

**Also update the "Environment-Specific Workflows" section at the bottom to add:**

- `iOS staging` (`build-ios-staging.yml`): Manual trigger - requires `IOS_BUILD_CERTIFICATE_BASE64`, `IOS_P12_PASSWORD`, `IOS_PROVISION_PROFILE_BASE64`, `APPLE_API_KEY_ID`, `APPLE_API_ISSUER_ID`, `APPLE_API_KEY_BASE64`
- `iOS production` (`build-ios-production.yml`): Manual trigger - requires same iOS secrets as staging (both use same signing identity for App Store distribution)
  </action>
  <verify>
  <automated>grep -c "IOS_BUILD_CERTIFICATE_BASE64" .github/SECRETS.md | grep -q "[1-9]" && grep -c "APPLE_API_KEY_ID" .github/SECRETS.md | grep -q "[1-9]" && grep -c "IOS_PROVISION_PROFILE_BASE64" .github/SECRETS.md | grep -q "[1-9]" && grep -c "build-ios-production.yml" .github/SECRETS.md | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria> - grep "iOS Signing (App Store)" .github/SECRETS.md returns 1 match (section heading) - grep "IOS_BUILD_CERTIFICATE_BASE64" .github/SECRETS.md returns at least 2 matches (table + encode step) - grep "IOS_P12_PASSWORD" .github/SECRETS.md returns at least 1 match - grep "IOS_PROVISION_PROFILE_BASE64" .github/SECRETS.md returns at least 2 matches - grep "APPLE_API_KEY_ID" .github/SECRETS.md returns at least 2 matches - grep "APPLE_API_ISSUER_ID" .github/SECRETS.md returns at least 1 match - grep "APPLE_API_KEY_BASE64" .github/SECRETS.md returns at least 2 matches - grep "build-ios-production.yml" .github/SECRETS.md returns at least 1 match (environment workflows section) - grep "build-ios-staging.yml" .github/SECRETS.md returns at least 1 match - The section appears AFTER "Android Signing (Play Store)" heading
  </acceptance_criteria>
  <done>
  SECRETS.md contains complete iOS signing documentation with 6 secrets, step-by-step certificate/profile/API key creation guide, encoding instructions for both macOS and Linux, and updated environment-specific workflows section listing both iOS workflows.
  </done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                      |
| --------------------------- | ------------------------------------------------ |
| GitHub Secrets -> Runner    | Certificate material decoded on ephemeral runner |
| Runner -> App Store Connect | API key authenticates upload to Apple servers    |

## STRIDE Threat Register

| Threat ID | Category               | Component                         | Disposition | Mitigation Plan                                                                                            |
| --------- | ---------------------- | --------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------- |
| T-93-01   | Information Disclosure | .p12 certificate in runner temp   | mitigate    | Keychain cleanup step deletes decoded cert after build (already in staging workflow pattern)               |
| T-93-02   | Tampering              | pbxproj signing settings          | accept      | Only master branch can trigger production builds (branch guard in Plan 02); pbxproj changes reviewed in PR |
| T-93-03   | Information Disclosure | SECRETS.md documents secret names | accept      | Only secret names documented, not values; SECRETS.md is committed intentionally as setup guide             |

</threat_model>

<verification>
- project.pbxproj has MARKETING_VERSION = 1.0.0 in both Debug and Release
- project.pbxproj has CODE_SIGN_STYLE = Manual only in Release
- project.pbxproj has CODE_SIGN_IDENTITY = "Apple Distribution" in Release
- SECRETS.md has complete iOS signing section with 6 secrets documented
- SECRETS.md has step-by-step guide for certificate, provisioning profile, and API key creation
</verification>

<success_criteria>
Xcode project is configured for manual CI signing with correct version (1.0.0), and SECRETS.md provides a complete guide for the user to create and upload all required Apple signing credentials to GitHub Secrets.
</success_criteria>

<output>
After completion, create `.planning/phases/93-ios-build-pipeline/93-01-SUMMARY.md`
</output>
