---
phase: 31-hero-identity-method-sections
plan: 04
type: execute
wave: 3
depends_on: [31-01, 31-02, 31-03]
files_modified:
  - el-templo-web/pages/index.vue
  - el-templo-web/layouts/default.vue
autonomous: false
requirements: [HERO-01, HERO-04, HERO-06, IDEN-01, MET-01]

must_haves:
  truths:
    - "pages/index.vue uses SectionHero, SectionIdentity, and SectionMethod components instead of placeholder stubs"
    - "Hero section fills the full viewport including the area under the fixed nav"
    - "Identity section appears between hero and method sections"
    - "All section anchors (#hero, #que-es, #metodo) work with nav smooth scroll and active section tracking"
    - "Remaining placeholder stubs for Phase 32+ sections are preserved"
    - "The full page renders and scrolls correctly at desktop, tablet, and mobile breakpoints"
  artifacts:
    - path: "el-templo-web/pages/index.vue"
      provides: "Home page with real section components"
      min_lines: 30
  key_links:
    - from: "el-templo-web/pages/index.vue"
      to: "el-templo-web/components/SectionHero.vue"
      via: "auto-imported component"
      pattern: "<SectionHero"
    - from: "el-templo-web/pages/index.vue"
      to: "el-templo-web/components/SectionIdentity.vue"
      via: "auto-imported component"
      pattern: "<SectionIdentity"
    - from: "el-templo-web/pages/index.vue"
      to: "el-templo-web/components/SectionMethod.vue"
      via: "auto-imported component"
      pattern: "<SectionMethod"
---

<objective>
Wire the three new section components into the home page and verify the complete visual experience.

Purpose: Replace the Phase 30/31 placeholder stubs with real section components, ensure the hero handles the fixed nav offset correctly, verify all anchors work for nav active tracking, and do a full visual walkthrough at all breakpoints.

Output: Updated pages/index.vue with SectionHero, SectionIdentity, and SectionMethod. Visual verification checkpoint.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-hero-identity-method-sections/31-CONTEXT.md
@.planning/phases/31-hero-identity-method-sections/31-01-SUMMARY.md
@.planning/phases/31-hero-identity-method-sections/31-02-SUMMARY.md
@.planning/phases/31-hero-identity-method-sections/31-03-SUMMARY.md

@el-templo-web/pages/index.vue
@el-templo-web/layouts/default.vue
@el-templo-web/components/AppNav.vue

<interfaces>
<!-- New section components (auto-imported by Nuxt) -->
<SectionHero />        <!-- id="hero", full viewport, self-contained -->
<SectionIdentity />    <!-- id="que-es", Marble Cream bg -->
<SectionMethod />      <!-- id="metodo", 4 zones with alternating bg -->

<!-- Layout structure from default.vue -->
<div class="page">
  <AppNav />
  <div class="page__content">
    <slot />             <!-- pages/index.vue content goes here -->
  </div>
  <AppPrefooter />
  <AppFooter />
</div>

<!-- .page__content has padding-top: 64px (desktop) / 56px (mobile) -->
<!-- Hero handles this offset internally with negative margin -->

<!-- AppNav tracks active sections via useActiveSection composable -->
<!-- Section IDs that nav watches: hero, metodo, niveles, enfoques, sedes -->
<!-- Identity section id="que-es" is NOT in the nav (per CONTEXT.md) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire section components into pages/index.vue</name>
  <files>el-templo-web/pages/index.vue, el-templo-web/layouts/default.vue</files>
  <action>
Replace the placeholder stubs in pages/index.vue with the real section components.

**Updated pages/index.vue structure:**

```html
<template>
  <main>
    <SectionHero />
    <SectionIdentity />
    <SectionMethod />

    <!-- Phase 32+ placeholder stubs (keep these) -->
    <section id="niveles" class="section section--warm-stone">...</section>
    <section id="enfoques" class="section">...</section>
    <section id="descubri-nivel" class="section section--warm-stone">
      ...
    </section>
    <section id="sedes" class="section">...</section>
  </main>
</template>
```

**Key changes:**

1. **Remove** the hero placeholder stub (the `<section id="hero" ...>` with inline styles)
2. **Remove** the method placeholder stub (the `<section id="metodo" ...>`)
3. **Add** `<SectionHero />` at the top (Nuxt auto-imports it)
4. **Add** `<SectionIdentity />` after the hero (new section between hero and method — no placeholder stub existed for this)
5. **Add** `<SectionMethod />` where the method stub was
6. **Keep** all Phase 32+ placeholder stubs unchanged (niveles, enfoques, descubri-nivel, sedes)
7. **Keep** the `useHead` call with the page title

**Layout consideration:**
The hero component handles the nav offset internally (negative margin to go full-bleed under the fixed nav). Verify that `.page__content` in `layouts/default.vue` does NOT need changes. The `padding-top: 64px` is correct for ALL sections except the hero, and the hero handles its own offset.

If needed, add a CSS rule to `layouts/default.vue` to allow the hero to break out of the padding:

```css
.page__content > :first-child {
  margin-top: -64px;
}
```

But ONLY if SectionHero doesn't already handle this internally. Check Plan 01's implementation first.

**Verify anchor IDs are correct:**

- `#hero` — SectionHero has `id="hero"`
- `#que-es` — SectionIdentity has `id="que-es"` (not in nav, but available for internal linking)
- `#metodo` — SectionMethod has `id="metodo"` (nav watches this anchor)
- `#niveles`, `#enfoques`, `#descubri-nivel`, `#sedes` — keep as-is in stubs

**Cleanup:**

- Remove any unused imports or inline styles from the old placeholder stubs
- Ensure the component is clean and minimal
  </action>
  <verify>
  <automated>cd el-templo-web && npx nuxi typecheck 2>&1 | tail -20</automated>
  </verify>
  <done>pages/index.vue uses the three new section components, no placeholder stubs for hero/identity/method remain, Phase 32+ stubs preserved, all section anchor IDs correct for nav tracking</done>
  </task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete hero + identity + method sections replacing placeholders on the home page. The hero fills the viewport with Deep Charcoal background (no real video yet), staggered entrance animation, and two CTAs. The identity section shows a split layout with placeholder image and brand copy. The method section has 4 session block cards, 2 special session cards, an author section, and a conversion CTA.</what-built>
  <how-to-verify>
1. Start the dev server: `cd el-templo-web && pnpm dev`
2. Open http://localhost:9200 in browser

**Hero section (first viewport):**

- [ ] Full viewport height with Deep Charcoal background
- [ ] "Tu cuerpo es tu templo." in large Montserrat ExtraBold uppercase
- [ ] Subtitle in Cormorant Garamond italic below
- [ ] "Comenza tu camino" Terracotta button and "Abri tu Templo" gold bordered button
- [ ] Staggered entrance: text elements fade in one after another on page load
- [ ] Scroll down: parallax effect moves background slower than content (desktop only)
- [ ] Scroll indicator arrow at bottom (if visible)

**Identity section (scroll down):**

- [ ] Split layout: text on left (55%), placeholder image on right (45%)
- [ ] "Filosofia" tag in Terracotta, "Mas que un lugar para entrenar." title
- [ ] 3 paragraphs of body text in Geologica
- [ ] Ghost CTA "Conoce nuestra filosofia" is dimmed (not clickable)
- [ ] Scroll-reveal: text slides from left, image from right when section enters viewport

**Method section (scroll further):**

- [ ] Zone 1 (Warm Stone bg): "El Metodo" tag, title, subtitle, author block with circular placeholder
- [ ] Zone 2 (Marble Cream bg): 4 session cards with watermark numbers (01-04), Terracotta separator
- [ ] Zone 3 (Warm Stone bg): ROM and SKILLS cards with icons and hover elevation
- [ ] Zone 4 (Marble Cream bg): "Proba el metodo" CTA button
- [ ] Card hover: cards lift with increased shadow on hover

**Responsive (resize browser or use DevTools):**

- [ ] 480px: hero text scales down, identity stacks (image first), method cards stack to 1 column
- [ ] 768px: hero text at tablet size, identity maintains split, method cards in 2x2 grid
- [ ] 1200px+: full desktop layout as designed

**Navigation integration:**

- [ ] Scroll through page: nav active state highlights correct section
- [ ] Click "El Metodo" in nav: smooth scrolls to method section
- [ ] Hero "Comenza tu camino" scrolls to #descubri-nivel stub
- [ ] Hero "Abri tu Templo" navigates to /franquicias

**Accessibility:**

- [ ] Toggle "prefers-reduced-motion" in DevTools: all animations should be disabled
      </how-to-verify>
      <resume-signal>Type "approved" or describe issues to fix</resume-signal>
      </task>

</tasks>

<verification>
1. `cd el-templo-web && npx nuxi typecheck` — no type errors
2. `cd el-templo-web && pnpm lint` — no lint errors
3. `cd el-templo-web && pnpm build` — production build succeeds
4. Human verification of visual quality at all breakpoints
5. Navigation anchor tracking works correctly
</verification>

<success_criteria>

- Home page shows hero, identity, and method sections with real content (no placeholder stubs for these three)
- All visual elements match the brand specs
- Responsive layout works correctly at desktop, tablet, and mobile
- Navigation integration works (smooth scroll, active section tracking)
- Animations play correctly and respect prefers-reduced-motion
- Production build succeeds
  </success_criteria>

<output>
After completion, create `.planning/phases/31-hero-identity-method-sections/31-04-SUMMARY.md`
</output>
