---
phase: 31-hero-identity-method-sections
plan: 03
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - el-templo-web/components/SectionMethod.vue
autonomous: true
requirements: [MET-01, MET-02, MET-03, MET-04, MET-05]

must_haves:
  truths:
    - "Method section has 4 subsections with alternating Warm Stone / Marble Cream backgrounds (clean hard cuts, no gradient blending)"
    - "4 session structure cards (Initium, Nucleus, Deuteros, Athlos) render in a 4-col grid on desktop, 2x2 on tablet, 1-col on mobile"
    - "Each card shows watermark number (01-04) at opacity 0.15, Greek name, Spanish translation, Terracotta separator, and description"
    - "2 special session cards (ROM, SKILLS) with Warm Linen background, hover elevation, and simple SVG icons"
    - "Author section shows circular PlaceholderBox with Aged Gold border (40/60 split layout) and Ignacio Bordon credentials"
    - "CTA 'Proba el metodo' (Terracotta primary) scrolls to #descubri-nivel"
    - "Cards have subtle hover elevation: translateY(-4px) + increased shadow, watermark number opacity shifts to 0.25 on hover"
    - "Session cards have staggered entrance animation (~100ms delay between cards) triggered by scroll"
    - "prefers-reduced-motion disables all entrance/hover animations"
  artifacts:
    - path: "el-templo-web/components/SectionMethod.vue"
      provides: "Full method section with 4 zones"
      min_lines: 250
  key_links:
    - from: "el-templo-web/components/SectionMethod.vue"
      to: "el-templo-web/composables/useScrollReveal.ts"
      via: "composable import"
      pattern: "useScrollReveal"
    - from: "el-templo-web/components/SectionMethod.vue"
      to: "el-templo-web/components/PlaceholderBox.vue"
      via: "component for author photo"
      pattern: "PlaceholderBox"
    - from: "el-templo-web/components/SectionMethod.vue"
      to: "el-templo-web/assets/css/buttons.css"
      via: "btn btn--primary class"
      pattern: "btn btn--primary"
---

<objective>
Build the method section ("Nuestro Metodo") that demonstrates the structured training methodology.

Purpose: This section generates "confianza tecnica" — visitors understand HOW El Templo trains, see the 4-block session structure with Latin names, and recognize the authority behind the method. The structured cards with watermark numbers should feel methodical and serious.

Output: SectionMethod.vue component with 4 subsections (intro+authority, 4 session blocks, special sessions, CTA), alternating backgrounds, card interactions, and scroll-triggered staggered entrances.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-hero-identity-method-sections/31-CONTEXT.md
@.planning/phases/31-hero-identity-method-sections/31-01-SUMMARY.md

@el-templo-web/assets/css/tokens.css
@el-templo-web/assets/css/buttons.css
@el-templo-web/assets/css/layout.css
@el-templo-web/components/PlaceholderBox.vue
@el-templo-web/composables/useScrollReveal.ts

<!-- Spec reference for exact CSS values and HTML structure -->

@.docs/brand-landing/spec3-pt1.md
@.docs/brand-landing/spec3-pt2.md
@.docs/brand-landing/spec3-pt3.md
@.docs/brand-landing/spec3-pt4.md

<interfaces>
<!-- From Plan 01: useScrollReveal composable -->
interface UseScrollRevealOptions {
  threshold?: number;       // default 0.15
  rootMargin?: string;      // default '0px 0px -60px 0px'
  once?: boolean;           // default true
}

interface UseScrollRevealReturn {
revealed: Ref<boolean>;
elementRef: Ref<HTMLElement | null>;
cleanup: () => void;
}

export function useScrollReveal(options?: UseScrollRevealOptions): UseScrollRevealReturn

<!-- PlaceholderBox props -->

interface PlaceholderBoxProps {
label?: string; // default "Imagen pendiente"
aspectRatio?: string;
height?: string;
}

<!-- Design tokens -->

--color-marble-cream: #f2ede5; <!-- zone 2, zone 4 backgrounds -->
--color-warm-stone: #d9cfc1; <!-- zone 1, zone 3 backgrounds -->
--color-warm-linen: #ede4d6; <!-- special cards background -->
--color-terracotta: #c07a56; <!-- tags, separators, watermark numbers -->
--color-deep-charcoal: #3d3732; <!-- titles, text -->
--color-olive-stone: #8a8472; <!-- translations, CTA note -->
--color-charcoal-mist: #5c554e; <!-- subtitle, author bio -->
--color-aged-gold: #b89b5e; <!-- author photo border, author separator -->
--font-authority: "Montserrat";
--font-elegance: "Cormorant Garamond";
--font-clarity: "Geologica";
--space-base: 16px;
--space-comfortable: 24px;
--space-spacious: 32px;
--space-section: 48px;
--space-large: 64px;
--space-hero: 96px;
--radius-card: 8px;
--shadow-subtle: 0 2px 8px rgba(61, 55, 50, 0.1);
--shadow-medium: 0 4px 16px rgba(61, 55, 50, 0.15);
--transition-base: 300ms ease;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SectionMethod component — structure, typography, and author block</name>
  <files>el-templo-web/components/SectionMethod.vue</files>
  <action>
Create the method section component following spec3 exactly. This is a large component with 4 distinct zones.

**Overall HTML structure** (BEM classes from spec):

```html
<section class="method" id="metodo">
  <!-- ZONE 1: Intro + Authority (Warm Stone bg) -->
  <div class="method__intro">
    <div class="method__intro-container">
      <span class="method__tag">El Metodo</span>
      <h2 class="method__title">Nada es al azar. Todo es metodologia.</h2>
      <p class="method__subtitle">Un sistema de entrenamiento progresivo...</p>
      <!-- Author block -->
      <div class="method__author">
        <!-- PlaceholderBox for circular photo -->
        <div class="method__author-photo-wrap">
          <PlaceholderBox label="Foto Ignacio" aspect-ratio="1/1" />
        </div>
        <div class="method__author-info">
          <p class="method__author-name">Disenado por Ignacio Bordon</p>
          <div class="method__author-separator"></div>
          <p class="method__author-bio">
            Ex gimnasta de alto rendimiento. Mas de 10 anos formando atletas y
            entrenadores. Mas de 10.000 alumnos. CEO y fundador de El Templo.
          </p>
        </div>
      </div>
    </div>
  </div>

  <!-- ZONE 2: 4 Session Blocks (Marble Cream bg) -->
  <div class="method__blocks" ref="blocksRef">
    <div class="method__blocks-container">
      <span class="method__sub-tag">Estructura de cada sesion</span>
      <p class="method__blocks-intro">
        Cada sesion dura 1 hora y esta dividida en 4 bloques especificos...
      </p>
      <div
        class="method__blocks-grid"
        :class="{ 'is-visible': blocksRevealed }"
      >
        <!-- 4 block cards rendered via v-for -->
      </div>
    </div>
  </div>

  <!-- ZONE 3: Special Sessions (Warm Stone bg) -->
  <div class="method__specials" ref="specialsRef">
    <div class="method__specials-container">
      <span class="method__sub-tag">Sesiones especiales</span>
      <p class="method__specials-intro">
        Ademas de las sesiones diarias del metodo...
      </p>
      <div
        class="method__specials-grid"
        :class="{ 'is-visible': specialsRevealed }"
      >
        <!-- ROM card -->
        <!-- SKILLS card -->
      </div>
    </div>
  </div>

  <!-- ZONE 4: CTA (Marble Cream bg) -->
  <div class="method__cta-section">
    <a href="#descubri-nivel" class="btn btn--primary method__cta"
      >Proba el metodo</a
    >
    <p class="method__cta-note">
      Tu primera sesion es sin cargo. Veni a sentir la diferencia.
    </p>
  </div>
</section>
```

**Data-driven cards:**

Define the session blocks as a typed array in `<script setup>`:

```typescript
interface SessionBlock {
  number: string;
  nameGreek: string;
  nameSpanish: string;
  description: string;
}

const sessionBlocks: SessionBlock[] = [
  {
    number: "01",
    nameGreek: "Initium",
    nameSpanish: "Inicio",
    description:
      "Movilidad, activacion y entrada en calor. Preparamos el cuerpo y la mente para lo que viene. Cada sesion empieza con intencion, no con apuro.",
  },
  {
    number: "02",
    nameGreek: "Nucleus",
    nameSpanish: "Nucleo",
    description:
      "El patron principal de fuerza. El corazon de la sesion. Aca se trabaja el movimiento central del dia con progresion real y tecnica precisa.",
  },
  {
    number: "03",
    nameGreek: "Deuteros",
    nameSpanish: "Complementario",
    description:
      "Tecnica, control y trabajo complementario. Refuerza lo que el Nucleus construye. Equilibra el cuerpo y profundiza el dominio del movimiento.",
  },
  {
    number: "04",
    nameGreek: "Athlos",
    nameSpanish: "Desafio",
    description:
      "Retos, juegos de fuerza, trabajo en equipo y cierre. La parte que pone a prueba lo entrenado y construye comunidad. Cada sesion termina con energia, no con agotamiento.",
  },
];
```

Similarly for special sessions:

```typescript
interface SpecialSession {
  name: string;
  subtitle: string;
  description: string;
  iconSvg: string; // inline SVG string
}
```

**Zone-by-zone implementation details:**

**Zone 1 — Intro + Authority (Warm Stone background):**

- Background: `var(--color-warm-stone)`
- Padding: `var(--space-hero) 0` (96px desktop, 64px mobile)
- Container: max-width 900px, centered, text-align center
- Tag: uses `.section__tag` pattern (Montserrat Bold 11px uppercase Terracotta)
- Title: Montserrat ExtraBold 36px uppercase (uses `.method__title` from spec)
- Subtitle: Cormorant Garamond italic 20px, Charcoal Mist, max-width 700px
- Author block: 40/60 split layout per CONTEXT.md
  - Left (40%): circular PlaceholderBox with Aged Gold border (2px). Apply circular styling by wrapping PlaceholderBox in a div with `border-radius: 50%; overflow: hidden; border: 2px solid var(--color-aged-gold); width: 120px; height: 120px;`
  - Right (60%): "Disenado por Ignacio Bordon" in Cormorant Garamond Semibold 20px + Aged Gold separator (40px, 2px) + credentials in Geologica 15px Charcoal Mist
  - Mobile: stacks vertically, photo centered above, text below centered
  - max-width 600px, centered

**Zone 2 — 4 Session Blocks (Marble Cream background):**

- Background: `var(--color-marble-cream)`
- Padding: 80px top/bottom (48px mobile)
- Container: max-width 1100px
- Sub-tag: same style as tag
- Intro text: Geologica 16px, max-width 700px
- Grid: `grid-template-columns: repeat(4, 1fr)`, gap 32px
- Tablet: `repeat(2, 1fr)`, gap 24px
- Mobile: `1fr`
- Each block card:
  - `position: relative`, padding 24px
  - Watermark number: absolute positioned top-right, Montserrat Bold 48px, Terracotta opacity 0.15
  - Greek name: Montserrat Bold 16px uppercase, tracking 0.08em
  - Spanish name: Geologica italic 13px, Olive Stone
  - Separator: 40px wide, 2px tall, Terracotta, margin 12px vertical
  - Description: Geologica 15px, line-height 1.5
  - **Hover** (per CONTEXT.md): `translateY(-4px)` + shadow intensifies + watermark number opacity shifts from 0.15 to 0.25
  - All 4 cards use Terracotta separator (no unique colors per card — per CONTEXT.md)
  - No icons on session structure cards (per CONTEXT.md)
  - `transition: var(--transition-base)` on the card for hover

**Zone 3 — Special Sessions (Warm Stone background):**

- Background: `var(--color-warm-stone)`
- Padding: 80px top/bottom (48px mobile)
- Container: max-width 900px, centered
- Grid: 2 columns, gap 32px. Mobile: 1fr
- Each card:
  - Background: `var(--color-warm-linen)` (#EDE4D6)
  - Border: `1px solid var(--color-warm-stone)`
  - border-radius: `var(--radius-card)` (8px)
  - padding: 32px
  - box-shadow: `var(--shadow-subtle)`
  - Hover: `translateY(-4px)`, shadow intensifies to `var(--shadow-medium)` (per CONTEXT.md: same elevation as session cards)
  - Name: Montserrat Bold 18px uppercase, tracking 0.06em
  - Subtitle: Cormorant Garamond italic 15px, Olive Stone
  - Description: Geologica 15px
  - **SVG icons** (per CONTEXT.md: simple placeholder SVGs):
    - ROM: stretch/mobility symbol — a simple outline of a person in a stretching pose, or a circular arrow suggesting range of motion. Use inline SVG, stroke-based, color `var(--color-olive-stone)`, 32x32px. Keep it SIMPLE.
    - SKILLS: target/skill symbol — a crosshair/target icon or a simple skill star. Use inline SVG, stroke-based, color `var(--color-olive-stone)`, 32x32px. Keep it SIMPLE.
    - Position the icon at the top of the card, before the name

**Zone 4 — CTA (Marble Cream background):**

- Padding: 64px top/bottom (48px mobile)
- text-align center, max-width 600px, margin auto
- Button: `.btn.btn--primary` (reuse existing class), href="#descubri-nivel" (per CONTEXT.md)
- Note below: Geologica 14px italic, Olive Stone, margin-top 16px

**Scroll-triggered animations:**

- Use TWO instances of `useScrollReveal()` — one for the blocks grid, one for the specials grid
- Blocks grid: staggered entrance with ~100ms delay between cards using inline `transition-delay` via `:style` binding (same Phase 30 stagger pattern):
  ```css
  .method__block {
    opacity: 0;
    transform: translateY(20px);
    transition:
      opacity 500ms ease,
      transform 500ms ease;
  }
  .method__blocks-grid.is-visible .method__block {
    opacity: 1;
    transform: translateY(0);
  }
  ```
  Then in template: `:style="{ transitionDelay: blocksRevealed ? (index * 100) + 'ms' : '0ms' }"`
- Specials grid: similar stagger but only 2 cards (0ms, 150ms)
- `prefers-reduced-motion`: media query removes all transitions + always visible

**Copy accuracy**: Use exact Spanish copy from spec3-pt1.md with proper accents. "Sesion" not "clase". Keep it factual, not promotional.

**CSS organization**: `<style scoped>`. All BEM classes prefixed with `method__`. Every color/font/spacing value uses `var(--*)` tokens.
</action>
<verify>
<automated>cd el-templo-web && npx nuxi typecheck 2>&1 | tail -20</automated>
</verify>
<done>SectionMethod.vue renders 4 zones with alternating backgrounds, session block cards in responsive grid with watermark numbers and hover elevation, special session cards (ROM/SKILLS) with SVG icons and hover elevation, author block with circular PlaceholderBox and credentials, primary CTA scrolling to #descubri-nivel, staggered entrance animations on scroll, and all typography/colors from design tokens</done>
</task>

</tasks>

<verification>
1. `cd el-templo-web && npx nuxi typecheck` — no type errors
2. `cd el-templo-web && pnpm lint` — no lint errors
3. Visual check (when wired in Plan 04):
   - Zone backgrounds alternate correctly (Warm Stone, Marble Cream, Warm Stone, Marble Cream)
   - 4 block cards in 4-col grid on desktop, 2x2 tablet, stack on mobile
   - Watermark numbers visible at low opacity
   - Card hover shows elevation + opacity shift
   - Special cards have SVG icons and hover elevation
   - Author block shows circular placeholder with gold border
   - CTA button works with smooth scroll
   - Staggered entrance fires when scrolling into view
</verification>

<success_criteria>

- SectionMethod.vue is a self-contained component with 4 distinct zones
- All 4 session blocks render with data-driven v-for, correct typography, and watermark numbers
- Special session cards have SVG icons, Warm Linen background, and hover elevation
- Author block has 40/60 split (circular photo placeholder, credentials)
- CTA uses existing .btn--primary class
- Alternating backgrounds with clean hard cuts (no gradient blending)
- Staggered entrance animations with scroll-triggered reveal
- Type-checks and lints cleanly
  </success_criteria>

<output>
After completion, create `.planning/phases/31-hero-identity-method-sections/31-03-SUMMARY.md`
</output>
