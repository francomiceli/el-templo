---
phase: 141-reportes-para-la-admin
plan: 03
type: execute
wave: 3
depends_on: [141-01, 141-02]
files_modified:
  - el-templo-admin/src/composables/useTransactionsApi.ts
  - el-templo-admin/src/types/transaction.ts
  - el-templo-admin/src/constants/caja.ts
  - el-templo-admin/src/pages/CajaPage.vue
  - el-templo-admin/src/components/caja/MovimientosTab.vue
  - el-templo-admin/src/components/caja/SaldosPorCajaTab.vue
autonomous: true
requirements: [REP-02, REP-04]
must_haves:
  truths:
    - "/caja renders a q-tabs hub: Pendientes (landing) / Saldos / Movimientos / Mov. y egresos, gated isCajaRole (coach excluded)"
    - "The Movimientos tab is the existing CajaPage body migrated verbatim (summary + table + Excel export), minus the egresos placeholder"
    - "The Saldos tab shows cards grouped by tipo with per-currency subtotals and never a cross-currency total"
    - "The composable exposes getPendingTray, getCashRegisterBalances, getMovEgresosHistory, validate/observe/correct, voidTransaction(id,reason,keepMembershipActive), and 3 export…ToExcel — with cleanup(), no onUnmounted inside"
  artifacts:
    - path: "el-templo-admin/src/composables/useTransactionsApi.ts"
      provides: "extended API calls for the 3 reads + 4 actions + 3 exports"
      contains: "getPendingTray"
    - path: "el-templo-admin/src/constants/caja.ts"
      provides: "frontend caja constants (tab names; threshold is read from server payload, not hardcoded here)"
      contains: "caja"
    - path: "el-templo-admin/src/pages/CajaPage.vue"
      provides: "thin hub shell: q-tabs + q-tab-panels + owner country selector"
      contains: "q-tabs"
    - path: "el-templo-admin/src/components/caja/SaldosPorCajaTab.vue"
      provides: "cards grouped by tipo, per-currency subtotals, currency badge beside amount"
      contains: "Subtotal"
    - path: "el-templo-admin/src/components/caja/MovimientosTab.vue"
      provides: "verbatim migration of the existing CajaPage summary+table+export"
      contains: "q-table"
  key_links:
    - from: "CajaPage.vue hub"
      to: "MovimientosTab / SaldosPorCajaTab"
      via: "q-tab-panel component render with shared selectedCountry prop"
      pattern: "SaldosPorCajaTab"
    - from: "SaldosPorCajaTab"
      to: "useTransactionsApi.getCashRegisterBalances"
      via: "composable call → GET /cash-registers/balances"
      pattern: "getCashRegisterBalances"
---

<objective>
Extend useTransactionsApi with all the Plan-01/02 endpoint calls + the four 137 action calls, add the matching frontend types, and reorganize CajaPage.vue into the q-tabs hub shell with the Movimientos tab (verbatim migration of today's body) and the Saldos tab (cards by tipo, per-currency subtotals). This plan delivers the hub skeleton + 2 of the 4 tabs; Plan 04 adds the Bandeja + Mov-Egresos tabs.

Purpose: Stand up the cash-control hub and the composable surface the whole phase depends on, honoring 141-UI-SPEC. Saldos enforces the currency-isolation contract (never cross-currency totals).
Output: extended composable + FE types, hub shell CajaPage, MovimientosTab.vue, SaldosPorCajaTab.vue.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-reportes-para-la-admin/141-CONTEXT.md
@.planning/phases/141-reportes-para-la-admin/141-RESEARCH.md
@.planning/phases/141-reportes-para-la-admin/141-UI-SPEC.md
@.planning/phases/141-reportes-para-la-admin/141-01-SUMMARY.md
@.planning/phases/141-reportes-para-la-admin/141-02-SUMMARY.md
@el-templo-admin/src/pages/CajaPage.vue
@el-templo-admin/src/composables/useTransactionsApi.ts

<interfaces>
<!-- Extracted from codebase + Plan 01/02 output. -->

From useTransactionsApi.ts (existing — extend, keep cleanup(), NO onUnmounted):
listTransactions(params) ; voidTransaction(id, reason) // ← extend to (id, reason, keepMembershipActive?)
getSummary(params) ; exportToExcel(params) // existing /transactions/export blob download
pattern: each fn sets loading/error refs, calls api.get/post, extractError on catch, returns data; exports use responseType:'blob'

From CajaPage.vue (existing — body migrates verbatim into MovimientosTab):

- isOwner = computed(role==='owner'); selectedCountry ref<'AR'|'ES'>; displayCurrency
- methodColor / methodLabel helpers ; isVoided ; confirmVoid ; onExportCaja (blob → createObjectURL + anchor)
- summary cards + "Por tipo de transacción" kind cards + filter bar + q-table (acciones q-menu Detalles/Anular) + detail dialog
- the egresos "Próximamente" placeholder card → REMOVE (egresos live in Plan 04's Mov-Egresos tab)
- log = createLogger('CajaPage') ; onMounted load

New backend endpoints from Plan 01/02 (consume these):
GET /admin/finance/pending-tray → PaginatedResult<PendingTrayItem> & { thresholdDays }
GET /admin/finance/cash-registers/balances → CajaSaldoRow[] (cashRegisterId,name,type,branchId,currency,firmeBalance,pendienteAmount)
GET /admin/finance/movements-history → PaginatedResult<MovEgresoItem>
POST /transactions/:id/validate (no body) ; /observe { reason } ; /correct { correctedFields } ; /void { reason, keepMembershipActive? }

- sibling …/export endpoints (responseType:'blob') for pending-tray, cash-registers/balances, movements-history

From UI-SPEC (honor):

- Tab hub: q-tabs align=left active-color=primary indicator-color=primary; panels keep-alive; landing = pendientes; optional ?tab= query-param persistence
- owner-only País q-select moves to hub header; tabs read shared selectedCountry (prop or shared composable)
- Saldos: groups Efectivo sucursales (type=efectivo,branchId≠null) / Efectivo central (efectivo,branchId=null) / Banco (type=banco), q-mt-xl between groups; card = name + moneda badge beside + saldo firme (text-h5 bold) + "Pendiente: {monto}" (text-caption gold); per-currency subtotal chips ("Subtotal ARS: …" / "Subtotal EUR: …"), NEVER a cross-currency total; empty group → "Sin cajas de este tipo."
- warm palette, NO blue; createLogger not console; Pinia composition

From router (do NOT change gating): /caja stays meta allowedRoles ['gestion','admin','owner'] (coach excluded); isCajaRole in AdminLayout
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend useTransactionsApi + FE types</name>
  <files>el-templo-admin/src/composables/useTransactionsApi.ts, el-templo-admin/src/types/transaction.ts</files>
  <read_first>
    - el-templo-admin/src/composables/useTransactionsApi.ts (existing fns + cleanup() + return block; voidTransaction at ~46 sends only { reason })
    - el-templo-admin/src/types/transaction.ts (existing TransactionListItem etc. — add the new types)
    - .planning/phases/141-reportes-para-la-admin/141-01-SUMMARY.md + 141-02-SUMMARY.md (the exact PendingTrayItem/CajaSaldoRow/MovEgresoItem shapes the backend returns)
    - .planning/phases/141-reportes-para-la-admin/141-RESEARCH.md (Composable gap section — validate/observe/correct missing, void lacks keepMembershipActive)
  </read_first>
  <action>In types/transaction.ts add FE interfaces mirroring the backend Plan-01/02 shapes: PendingTrayItem (incl. ageInDays, isOverdue, validationStatus, recorderName, cashRegisterName), PendingTrayResult (PaginatedResult<PendingTrayItem> & { thresholdDays: number }), PendingTrayParams (status, country, branchId, dateFrom, dateTo, page, limit), CajaSaldoRow (cashRegisterId, name, type, branchId, currency, firmeBalance, pendienteAmount), MovEgresoItem + MovEgresoParams. Read the SUMMARY files for the authoritative field names.

In useTransactionsApi.ts add (mirroring the existing fn pattern — loading/error refs, extractError, return in the export block):

- getPendingTray(params): GET /admin/finance/pending-tray → PendingTrayResult
- getCashRegisterBalances(params): GET /admin/finance/cash-registers/balances → CajaSaldoRow[]
- getMovEgresosHistory(params): GET /admin/finance/movements-history → PaginatedResult<MovEgresoItem>
- validateTransaction(id): POST /transactions/:id/validate
- observeTransaction(id, reason): POST /transactions/:id/observe { reason }
- correctTransaction(id, correctedFields): POST /transactions/:id/correct { correctedFields }
- extend voidTransaction(id, reason, keepMembershipActive?): include keepMembershipActive in the POST body when provided
- exportPendingTrayToExcel(params), exportCashBalancesToExcel(params), exportMovEgresosToExcel(params): api.get(url, { params, responseType:'blob' }) → Blob
  Add all new fns to the returned object. Keep cleanup(); do NOT add onUnmounted inside the composable. No `any`; createLogger if logging needed.</action>
  <verify>
  <automated>cd el-templo-admin && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria> - useTransactionsApi exposes getPendingTray, getCashRegisterBalances, getMovEgresosHistory, validateTransaction, observeTransaction, correctTransaction, voidTransaction(id,reason,keepMembershipActive?), exportPendingTrayToExcel, exportCashBalancesToExcel, exportMovEgresosToExcel — all in the return block. - voidTransaction threads keepMembershipActive into the POST body. - cleanup() retained, NO onUnmounted inside; FE types added; no `any`; `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Composable + FE types extended with all Plan-01/02 reads, the four 137 actions (keepMembershipActive threaded), and the three exports; cleanup() kept, no onUnmounted.</done>
  </task>

<task type="auto">
  <name>Task 2: Hub shell CajaPage + MovimientosTab (verbatim migration)</name>
  <files>el-templo-admin/src/pages/CajaPage.vue, el-templo-admin/src/components/caja/MovimientosTab.vue, el-templo-admin/src/constants/caja.ts</files>
  <read_first>
    - el-templo-admin/src/pages/CajaPage.vue (the FULL current body — template + script — that migrates into MovimientosTab; isOwner/selectedCountry/displayCurrency, methodColor/methodLabel, confirmVoid, onExportCaja, detail dialog, onMounted)
    - .planning/phases/141-reportes-para-la-admin/141-UI-SPEC.md (Tab Hub Structure D-01; Tab 3 Movimientos migration mapping — remove egresos placeholder; query-param ?tab= persistence assumption)
    - el-templo-admin/src/layouts/AdminLayout.vue + router/routes.ts (isCajaRole / /caja gating — do NOT change)
  </read_first>
  <action>Create src/constants/caja.ts exporting the tab name constants (e.g. CAJA_TABS = { pendientes, saldos, movimientos, movEgresos }) — a thin module so 142/future can reference tab names. (The overdue threshold is NOT hardcoded here; it comes from the bandeja response thresholdDays per UI-SPEC.)

Create src/components/caja/MovimientosTab.vue: move the CURRENT CajaPage body into it verbatim (summary cards, "Por tipo de transacción" kind cards, filter bar, the transactions q-table with methodColor/methodLabel/isVoided/acciones q-menu Detalles+Anular, detail dialog, the Exportar Excel button → onExportCaja). REMOVE the egresos "Próximamente" placeholder card (egresos move to Plan 04's Mov-Egresos tab). Accept `selectedCountry` (and isOwner) as props from the hub instead of owning the country selector. Keep createLogger('MovimientosTab'), useTransactionsApi, cleanup() wiring; call cleanup() from the component's onUnmounted (component-level onUnmounted is fine — the rule forbids onUnmounted INSIDE the composable, not in the SFC).

Rewrite CajaPage.vue as the thin hub shell: q-page with header (text-h5 "Caja" + the owner-only País q-select moved up here, bound to selectedCountry) + q-tabs (align=left, active-color="primary", indicator-color="primary", dense; tabs pendientes/saldos/movimientos/movEgresos with Material icons inbox/account_balance_wallet/receipt_long/swap_horiz) + q-separator + q-tab-panels (keep-alive, swipeable=false) rendering MovimientosTab and SaldosPorCajaTab now, with placeholder panels for pendientes/movEgresos that Plan 04 fills. Landing tab = pendientes (default model value). Pass selectedCountry/isOwner down as props. Optional: sync activeTab to ?tab= query param for refresh persistence (UI-SPEC assumption). Use warm palette, NO blue. Do NOT change /caja router gating.</action>
<verify>
<automated>cd el-templo-admin && npx tsc --noEmit</automated>
</verify>
<acceptance_criteria> - CajaPage is a thin hub shell (q-tabs + q-tab-panels + owner country selector in header), landing tab = pendientes, gating unchanged (coach excluded). - MovimientosTab.vue holds the verbatim-migrated summary + kind cards + filter bar + transactions q-table + detail dialog + Excel export; the egresos "Próximamente" placeholder is REMOVED. - selectedCountry/isOwner are shared from the hub to the tabs (props); createLogger used; `npx tsc --noEmit` passes.
</acceptance_criteria>
<done>The /caja hub renders the 4-tab skeleton with Movimientos migrated verbatim (no egresos placeholder); pendientes/movEgresos panels are placeholders for Plan 04.</done>
</task>

<task type="auto">
  <name>Task 3: SaldosPorCajaTab — cards by tipo, per-currency subtotals</name>
  <files>el-templo-admin/src/components/caja/SaldosPorCajaTab.vue</files>
  <read_first>
    - .planning/phases/141-reportes-para-la-admin/141-UI-SPEC.md (Tab 2 Saldos por Caja: layout, caja card anatomy, critical contracts, empty state, copy)
    - el-templo-admin/src/pages/CajaPage.vue (q-card flat bordered summary-card pattern + q-skeleton loading pattern to reuse)
    - .planning/phases/141-reportes-para-la-admin/141-01-SUMMARY.md (CajaSaldoRow field names)
    - el-templo-admin/src/composables/useTransactionsApi.ts (getCashRegisterBalances, exportCashBalancesToExcel from Task 1)
  </read_first>
  <action>Create src/components/caja/SaldosPorCajaTab.vue. On mount (or when selectedCountry prop changes) call getCashRegisterBalances({ country: isOwner ? selectedCountry : undefined }). Group the CajaSaldoRow[] into three sections in fixed order: "Efectivo sucursales" (type=efectivo && branchId≠null), "Efectivo central" (type=efectivo && branchId=null), "Banco" (type=banco), separated by q-mt-xl. Each section: a row of q-card flat bordered (responsive col-6 col-sm-4 col-md-3) where each card shows the caja name (text-subtitle2) + moneda badge BESIDE it, the saldo firme as text-h5 text-weight-bold (large), and "Pendiente: {monto}" as text-caption in gold ($warning) below — pendiente NEVER added to firme. Below each section render per-currency subtotal chips: group that section's cajas by currency and render one chip per currency ("Subtotal ARS: {Σ firme ARS}", "Subtotal EUR: {Σ firme EUR}") — NEVER a single chip summing across currencies. Empty section → muted "Sin cajas de este tipo." (text-caption text-grey-5). Add an Exportar Excel outline btn (q-btn outline dense color=primary icon=download + :loading) wired to exportCashBalancesToExcel + the blob-download helper (reuse CajaPage's createObjectURL+anchor approach). Loading → q-skeleton per card value. Error → $q.notify negative "Error cargando saldos". createLogger('SaldosPorCajaTab'); warm palette, NO blue; format currency with the existing admin currency formatter. Provide cleanup()/onUnmounted at the component level.</action>
  <verify>
    <human-check>Open /caja → Saldos tab as an owner: cards appear grouped under Efectivo sucursales / Efectivo central / Banco; each card shows firme (large) + Pendiente (small) + moneda badge; subtotals are per-currency only (a group with ARS+EUR shows two chips, never one combined); empty group shows "Sin cajas de este tipo."; Exportar Excel downloads a .xlsx.</human-check>
    <automated>cd el-templo-admin && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Saldos tab groups cajas by tipo in fixed order with q-mt-xl separation; each card = name + moneda badge beside + firme (large) + "Pendiente: {monto}" (small, gold, never added to firme).
    - Per-currency subtotal chips only — NO cross-currency total anywhere; empty group → "Sin cajas de este tipo."
    - Export wired to exportCashBalancesToExcel; warm palette NO blue; `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>SaldosPorCajaTab renders firme+pendiente cards grouped by tipo with strict per-currency subtotals and an Excel export, honoring the currency-isolation contract.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                 |
| ---------------------- | ------------------------------------------- |
| coach → /caja          | coach must not reach the cash-control hub   |
| cross-currency display | UI must never render a total mixing ARS+EUR |

## STRIDE Threat Register

| Threat ID | Category               | Component                   | Disposition | Mitigation Plan                                                                                                                                                                                     |
| --------- | ---------------------- | --------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-141-09  | Elevation/Disclosure   | /caja route + tabs          | mitigate    | /caja router meta allowedRoles ['gestion','admin','owner'] unchanged (coach excluded); backend FINANCE_READ_ROLES is the authoritative gate (Plan 01/02). Frontend gating is defense-in-depth only. |
| T-141-10  | Information Disclosure | Saldos cross-currency total | mitigate    | Subtotals computed per-currency group only; no code path sums across currencies (currency-isolation contract, 137/138/D-06).                                                                        |
| T-141-SC  | Tampering              | npm installs                | accept      | Zero new packages (RESEARCH: "Cero dependencias nuevas").                                                                                                                                           |

</threat_model>

<verification>
- `cd el-templo-admin && npx tsc --noEmit` clean after each task (no full suite locally).
- Hub renders 4 tabs, landing = Pendientes; Movimientos is the migrated body minus egresos placeholder; Saldos enforces per-currency subtotals.
- No onUnmounted inside the composable (only in SFCs); cleanup() retained.
- Warm palette, no blue tokens introduced.
</verification>

<success_criteria>

- /caja is a q-tabs hub (Pendientes landing / Saldos / Movimientos / Mov. y egresos), coach excluded, gating unchanged.
- Movimientos = verbatim migration of today's CajaPage body, egresos placeholder removed.
- Saldos = cards by tipo, currency badge beside, per-currency subtotals only, never cross-currency; Excel export.
- Composable extended with all reads + 4 actions (keepMembershipActive) + 3 exports; cleanup() kept, no onUnmounted inside.
  </success_criteria>

<output>
Create `.planning/phases/141-reportes-para-la-admin/141-03-SUMMARY.md` when done.
</output>
