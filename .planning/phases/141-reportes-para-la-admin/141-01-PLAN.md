---
phase: 141-reportes-para-la-admin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/finance/transaction-service.ts
  - el-templo-api/src/modules/finance/cash-register-service.ts
  - el-templo-api/src/modules/finance/constants.ts
  - el-templo-api/src/modules/finance/routes.ts
  - el-templo-api/src/modules/finance/schemas.ts
  - el-templo-api/src/modules/finance/types.ts
  - el-templo-api/test/finance/pending-tray.test.ts
  - el-templo-api/test/finance/cash-balances.test.ts
autonomous: true
requirements: [REP-01, REP-02, REP-04]
must_haves:
  truths:
    - "Admin GETs a bandeja of pendiente+observado charges ordered oldest-first with aging days and recorder name"
    - "Each bandeja row carries isOverdue (true when ageInDays > 3, the shared OVERDUE_DAYS constant)"
    - "Admin GETs saldos for every active caja: firme + pendiente + tipo + moneda, straight from getBalance"
    - "A coach hitting /pending-tray or /cash-registers/balances gets 403"
    - "Each report exports a .xlsx via a sibling /export endpoint reusing the exceljs Workbook pattern"
  artifacts:
    - path: "el-templo-api/src/modules/finance/transaction-service.ts"
      provides: "listPendingTray() own query (LEFT JOIN users/branches/cash_registers + recorder alias), TS aging, asc order"
      contains: "listPendingTray"
    - path: "el-templo-api/src/modules/finance/cash-register-service.ts"
      provides: "listActiveCajasWithBalance() iterating active cajas over getBalance"
      contains: "listActiveCajasWithBalance"
    - path: "el-templo-api/src/modules/finance/constants.ts"
      provides: "OVERDUE_DAYS = 3 shared constant (142 swaps to config)"
      contains: "OVERDUE_DAYS"
    - path: "el-templo-api/src/modules/finance/routes.ts"
      provides: "GET /pending-tray, GET /cash-registers/balances + 2 sibling /export endpoints"
      contains: "pending-tray"
    - path: "el-templo-api/test/finance/pending-tray.test.ts"
      provides: "ordering + aging + recorder + overdue + coach 403 coverage"
      contains: "oldest-first"
    - path: "el-templo-api/test/finance/cash-balances.test.ts"
      provides: "per-caja firme/pendiente + coach 403 + central/banco scope coverage"
      contains: "firmeBalance"
  key_links:
    - from: "routes.ts GET /pending-tray"
      to: "transactionService.listPendingTray"
      via: "service call with country-scoped filters"
      pattern: "listPendingTray"
    - from: "routes.ts GET /cash-registers/balances"
      to: "cashRegisterService.listActiveCajasWithBalance"
      via: "service call"
      pattern: "listActiveCajasWithBalance"
    - from: "listPendingTray isOverdue"
      to: "constants.ts OVERDUE_DAYS"
      via: "import of shared constant"
      pattern: "OVERDUE_DAYS"
---

<objective>
Add the two read-only finance endpoints for the bandeja de pendientes (REP-01) and saldos por caja (REP-02), each with its own service method, plus their sibling Excel export endpoints (REP-04). No migration — these read existing columns (validation_status, cash_register_id, recorded_by) and reuse the 138 getBalance primitive.

Purpose: Give the admin the daily pending-validation control list (oldest-first, aging, overdue flag) and the firm/pending balance view per caja, both currency-isolated and coach-excluded. The frontend (Plan 03) consumes these.
Output: 2 GET reads + 2 GET …/export endpoints, 2 service methods, 1 shared OVERDUE_DAYS constant, 2 integration test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-reportes-para-la-admin/141-CONTEXT.md
@.planning/phases/141-reportes-para-la-admin/141-RESEARCH.md

<interfaces>
<!-- Extracted from codebase — use directly, no exploration needed. -->

From el-templo-api/src/modules/finance/types.ts (existing):
interface TransactionListFilters { branchId?; country?: CountryCode; kind?; dateFrom?; dateTo?; memberId?; paymentMethod?; recordedBy?; search?; page?; limit?; }
interface CashRegisterBalance { cashRegisterId: number; currency: string; firmeBalance: number; pendienteAmount: number; }

From cash-register-service.ts (existing — REUSE, do not reimplement):
async getBalance(cashRegisterId: number): Promise<CashRegisterBalance> // firme + pendiente, cutoff-gated, firmMoneyConditions()

From transaction-service.ts list() (the INNER-JOIN member-keyed path — DO NOT mutate):

- uses `const recorder = alias(schema.users, "recorder");` for "cargado por"
- orders desc(transactionDate), desc(createdAt) — the bandeja must be the OPPOSITE (asc)
- aging-in-TS precedent at getOutstandingConcepts (~line 1197): today.setHours(0,0,0,0); ageInDays = max(0, floor((today - effDate)/MS_PER_DAY))

From routes.ts (owner-aware country resolution, reuse verbatim):
if (request.scope.isOwner) { country = request.query.country ? request.query.country.toUpperCase() : undefined; }
else { country = request.scope.country ?? undefined; }

- module onRequest hook already enforces FINANCE_READ_ROLES (coach excluded → 403) + attachCountryScope
- export pattern: build `new Workbook()`, addWorksheet, set sheet.columns, style header row (bold + fgColor FFE0E0E0), addRow per row, `reply.header("Content-Type", "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet").header("Content-Disposition", ...).send(Buffer.from(buffer))`

From routes.ts caja-scope helper (for saldos non-owner scope):
resolveCajaCountry(cajaId) → { country: branchId===null ? null : branchCountry } // branch-less caja is country-agnostic → owner-only

From shared/permissions.ts:
FINANCE_READ_ROLES = [owner, admin, gestion, recepcion...] (coach ABSENT)

From test/helpers.ts:
createTestApp(), getAuthToken(app, email, pass), createStaffUser(app, {email,password,firstName,lastName,role,branchId,country?}), ensureEfectivoCaja(app, branchId)
coach 403 pattern (validation-state.test.ts): app.inject({ method, url, headers:{authorization:`Bearer ${coachToken}`} }) → expect(res.statusCode).toBe(403)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Wave-0 test scaffolds + shared OVERDUE_DAYS constant</name>
  <files>el-templo-api/src/modules/finance/constants.ts, el-templo-api/test/finance/pending-tray.test.ts, el-templo-api/test/finance/cash-balances.test.ts</files>
  <read_first>
    - el-templo-api/test/finance/validation-state.test.ts (RBAC-over-HTTP coach-403 pattern, beforeAll seed shape, FINANCE_URL constant)
    - el-templo-api/test/helpers.ts (createTestApp, getAuthToken, createStaffUser, ensureEfectivoCaja signatures)
    - el-templo-api/test/finance/cash-register-service.test.ts (how getBalance is seeded/asserted — mirror caja+transaction seeding)
    - .planning/phases/141-reportes-para-la-admin/141-RESEARCH.md (Phase Requirements → Test Map, Wave 0 Gaps)
  </read_first>
  <behavior>
    pending-tray.test.ts (RED until Task 1):
    - Test: GET /admin/finance/pending-tray returns only validation_status IN (pendiente, observado) rows, ordered oldest-first (a 12-day-old pendiente appears before a 1-day-old one).
    - Test: each row has ageInDays (integer ≥0) and recorderName (the staff who loaded it) and cashRegisterName.
    - Test: a row older than OVERDUE_DAYS (seed a transactionDate 5 days ago) has isOverdue=true; a 1-day row isOverdue=false.
    - Test: filter ?status=observados returns only observado rows; ?status=pendientes only pendiente; default/todos returns both.
    - Test: coach token → 403.
    cash-balances.test.ts (RED until Task 1):
    - Test: GET /admin/finance/cash-registers/balances returns one entry per active caja with firmeBalance, pendienteAmount, type, currency (assert firme excludes a pendiente row, pendiente reported separately).
    - Test: coach token → 403.
    - Test (scope): a non-owner gestion in AR does NOT see a branch-less central/banco caja (owner-only), but an owner does.
  </behavior>
  <action>Create el-templo-api/src/modules/finance/constants.ts exporting `export const OVERDUE_DAYS = 3;` with a comment that Phase 142 will swap this literal for a finance_settings read. Create the two test files importing from test/helpers.ts and mirroring validation-state.test.ts's beforeAll/afterAll app lifecycle + coach-token seeding. Write the behaviors above as failing tests (the endpoints do not exist yet → expect them to currently 404, which is fine for RED). Do NOT run the full suite (CI does that on push); only `npx tsc --noEmit` must pass for the test files to be type-correct. Use createStaffUser for a coach (role:"coach") to get coachToken. Reference REP-01/REP-02 in test describe blocks.</action>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - constants.ts exports OVERDUE_DAYS = 3 with a 142-swap comment.
    - Both test files exist, type-check clean, and encode oldest-first ordering, aging, isOverdue>3, status filter, coach 403, per-caja firme/pendiente, and central/banco owner-only scope as assertions.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>OVERDUE_DAYS constant + both test files exist, type-check clean, encoding all REP-01/REP-02 behaviors as (currently failing) assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: listPendingTray + listActiveCajasWithBalance service methods + types</name>
  <files>el-templo-api/src/modules/finance/transaction-service.ts, el-templo-api/src/modules/finance/cash-register-service.ts, el-templo-api/src/modules/finance/types.ts</files>
  <read_first>
    - el-templo-api/src/modules/finance/transaction-service.ts (list() at ~860 for the recorder alias + join chain to mirror; getOutstandingConcepts ~1197 for the TS aging clamp; buildListConditions ~991 for country/date filter reuse)
    - el-templo-api/src/modules/finance/cash-register-service.ts (getBalance ~139 — call it, do not reimplement)
    - el-templo-api/src/modules/finance/constants.ts (OVERDUE_DAYS from Task 0)
    - el-templo-api/src/modules/finance/types.ts (TransactionListFilters, CashRegisterBalance, PaginatedResult shape)
    - .planning/phases/141-reportes-para-la-admin/141-RESEARCH.md (endpoints (a) and (b) sections + Code Examples)
  </read_first>
  <behavior>
    - listPendingTray returns oldest-first pendiente+observado rows, each with ageInDays (TS-computed, clamped ≥0) and isOverdue (ageInDays > OVERDUE_DAYS).
    - listActiveCajasWithBalance returns one CajaSaldoRow per active caja (firme/pendiente from getBalance) with type, branchId, currency.
  </behavior>
  <action>Add to types.ts: `PendingTrayItem` (id, transactionDate, memberId, memberName, amount, currency, paymentMethod, cashRegisterId, cashRegisterName, recordedBy, recorderName, validationStatus, ageInDays, isOverdue), `PendingTrayFilters` (status?: 'pendientes'|'observados'|'todos', country?, branchId?, dateFrom?, dateTo?, page?, limit?), and `CajaSaldoRow` (cashRegisterId, name, type, branchId, currency, firmeBalance, pendienteAmount).

Add `listPendingTray(filters): Promise<PaginatedResult<PendingTrayItem> & { thresholdDays: number }>` to TransactionService as its OWN query — LEFT JOIN users (defensive/consistency), LEFT JOIN branches, LEFT JOIN cash_registers, and the `alias(schema.users,'recorder')` self-join for recorderName (mirror list()'s recorder pattern). WHERE: validation_status IN ('pendiente','observado') mapped from filters.status (pendientes→=pendiente, observados→=observado, todos/undefined→IN both), AND isNull(voidedAt), AND country/branch/date conditions reused from the buildListConditions approach. ORDER BY asc(transactionDate), asc(createdAt) — the OPPOSITE of list(). Compute ageInDays in TS (today.setHours(0,0,0,0), max(0, floor((today - txDate)/MS_PER_DAY)) — mirror getOutstandingConcepts) and isOverdue = ageInDays > OVERDUE_DAYS (import from constants.ts). Return thresholdDays: OVERDUE_DAYS in the payload so 142 swaps it without touching the UI. Paginate like list() (page/limit, defense-in-depth max 200).

Add `listActiveCajasWithBalance(scope?: { isOwner: boolean; country: string | null }): Promise<CajaSaldoRow[]>` to CashRegisterService: select active cajas (is_active=true), optionally filter by country for non-owner (branch_id → branches.country = scope.country; branch-less cajas owner-only — skip them when !isOwner, mirror enforceCajaScope semantics), then for each caja call existing getBalance(id) and assemble CajaSaldoRow with name/type/branchId/currency. Per RESEARCH: with a handful of cajas the per-caja getBalance loop is fine (no N+1 concern at this scale). No `any`; use unknown + narrowing where needed; Pino logger only.</action>
<verify>
<automated>cd el-templo-api && npx tsc --noEmit</automated>
</verify>
<acceptance_criteria> - listPendingTray uses its OWN query (does NOT call or mutate list()/exportRowsForExcel), LEFT JOINs users+branches+cash_registers+recorder, orders asc(transactionDate),asc(createdAt), returns ageInDays + isOverdue (from OVERDUE_DAYS) + thresholdDays. - listActiveCajasWithBalance composes getBalance per active caja; non-owner is scoped by caja country and branch-less cajas are owner-only. - Types PendingTrayItem/PendingTrayFilters/CajaSaldoRow exported; no `any`; `npx tsc --noEmit` passes.
</acceptance_criteria>
<done>Both service methods + types compile; bandeja is oldest-first with TS aging+overdue, saldos composes getBalance per active caja with country scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GET /pending-tray + GET /cash-registers/balances + 2 sibling /export endpoints</name>
  <files>el-templo-api/src/modules/finance/routes.ts, el-templo-api/src/modules/finance/schemas.ts</files>
  <read_first>
    - el-templo-api/src/modules/finance/routes.ts (GET /transactions ~686 for owner-aware country resolution + requireBranchAccess; /transactions/export ~823 for the exceljs Workbook build to mirror; resolveCajaCountry ~104 for saldos non-owner scope)
    - el-templo-api/src/modules/finance/schemas.ts (listTransactionsSchema ~243, exportTransactionsSchema ~502 — mirror querystring shape, additionalProperties:false, enum/format)
    - el-templo-api/src/modules/finance/transaction-service.ts (listPendingTray from Task 1), cash-register-service.ts (listActiveCajasWithBalance from Task 1)
    - .planning/phases/141-reportes-para-la-admin/141-RESEARCH.md (Pattern 1/2, export decision: sibling endpoints NOT ?type=)
  </read_first>
  <behavior>
    - GET /pending-tray returns the bandeja JSON (paginated + thresholdDays); coach → 403 (module guard); status filter honored.
    - GET /cash-registers/balances returns the flat CajaSaldoRow array; coach → 403; non-owner does not see central/banco.
    - Each /export sibling returns a styled .xlsx with that report's columns.
  </behavior>
  <action>In schemas.ts add `pendingTraySchema` (querystring: status enum ['pendientes','observados','todos'], country, branchId, dateFrom/dateTo format date, page, limit; loose response like listTransactionsSchema), `pendingTrayExportSchema` (same minus page/limit), `cashBalancesSchema` (querystring: country only for owner override; loose response), and `cashBalancesExportSchema`. All `additionalProperties:false`.

In routes.ts (inside financeRoutes, after the existing GET /transactions block) add:

1. `GET /pending-tray` — owner-aware country resolution (reuse the verbatim isOwner block), map query.status → filters.status, call transactionService.listPendingTray(filters), return the result. The module onRequest hook already enforces FINANCE_READ_ROLES (coach 403) + attachCountryScope.
2. `GET /pending-tray/export` — same filters minus page/limit, call listPendingTray with a high limit (all rows), build a Workbook sheet "Bandeja" with columns Fecha · Socio · Monto · Moneda · Medio · Caja · Cargado por · Antigüedad (días) · Estado · Vencido; style header (bold + fgColor FFE0E0E0); filename `bandeja-${today}.xlsx`.
3. `GET /cash-registers/balances` — resolve owner-aware country, pass scope { isOwner: request.scope.isOwner, country } to cashRegisterService.listActiveCajasWithBalance, return the flat array.
4. `GET /cash-registers/balances/export` — same scope, build a Workbook sheet "Saldos" with columns Caja · Tipo · Moneda · Saldo firme · Pendiente; same header styling; filename `saldos-${today}.xlsx`.

Use the existing exceljs import, KIND/method label helpers where applicable, Pino logger via handleServiceError on catch (err: unknown). No `any`. Do NOT touch the existing /transactions, /transactions/export, /transactions/summary routes.</action>
<verify>
<automated>cd el-templo-api && npx tsc --noEmit</automated>
</verify>
<acceptance_criteria> - GET /pending-tray and GET /cash-registers/balances registered under financeRoutes (coach inherits 403 from FINANCE_READ_ROLES module guard); each has a sibling /export building an exceljs Workbook with that report's columns (no ?type= param, no parallel mechanism). - Schemas added with additionalProperties:false + status enum; owner-aware country resolution reused; saldos non-owner scope excludes central/banco. - `npx tsc --noEmit` passes; existing /transactions routes untouched.
</acceptance_criteria>
<done>Four endpoints (2 reads + 2 exports) wired to the Task-1 service methods, coach-excluded via the module guard, exports reuse the exceljs pattern; the Wave-0 tests now have endpoints to hit (CI on push will run them green).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                            |
| ----------------------------------------- | ---------------------------------------------------------------------- |
| admin browser → finance API               | authenticated staff calls; role + country must be enforced server-side |
| non-owner staff → cross-country caja data | gestion/admin scoped to their country; central/banco are owner-only    |

## STRIDE Threat Register

| Threat ID | Category               | Component                                       | Disposition | Mitigation Plan                                                                                                                                                                                                    |
| --------- | ---------------------- | ----------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-141-01  | Elevation/Disclosure   | GET /pending-tray, GET /cash-registers/balances | mitigate    | Module onRequest hook enforces FINANCE_READ_ROLES (coach absent → 403). Coach-403 test in both Wave-0 files asserts it over HTTP.                                                                                  |
| T-141-02  | Information Disclosure | saldos non-owner cross-country                  | mitigate    | listActiveCajasWithBalance scopes non-owner by caja country (branch_id → branches.country); branch-less central/banco owner-only (mirror enforceCajaScope). Scope test asserts gestion does NOT see central/banco. |
| T-141-03  | Information Disclosure | mass export ignoring scope                      | mitigate    | Export endpoints reuse the SAME owner-aware country resolution + filters as their list endpoint; no unscoped path.                                                                                                 |
| T-141-04  | Tampering              | SQL injection via filters                       | mitigate    | Drizzle parameterized queries only; querystring schemas additionalProperties:false + enum/format.                                                                                                                  |
| T-141-SC  | Tampering              | npm installs                                    | accept      | Zero new packages (RESEARCH: "Cero dependencias nuevas"); slopcheck N/A.                                                                                                                                           |

</threat_model>

<verification>
- `cd el-templo-api && npx tsc --noEmit` clean after each task (local typecheck only — full suite runs in CI on push to staging).
- Wave-0 tests (pending-tray, cash-balances) encode: oldest-first ordering, aging, isOverdue>3, status filter, per-caja firme/pendiente, coach 403, central/banco owner-only scope. CI runs them green on push.
- No migration generated (verify no new files under el-templo-api/src/db/migrations/).
</verification>

<success_criteria>

- GET /pending-tray returns pendiente+observado oldest-first with ageInDays + isOverdue + thresholdDays; coach → 403.
- GET /cash-registers/balances returns firme+pendiente per active caja with type/currency; coach → 403; non-owner excludes central/banco.
- Both reports export .xlsx via sibling endpoints reusing the exceljs Workbook pattern.
- OVERDUE_DAYS is a single shared constant (142 swaps it).
- listPendingTray is its OWN query — list()/exportRowsForExcel() untouched.
  </success_criteria>

<output>
Create `.planning/phases/141-reportes-para-la-admin/141-01-SUMMARY.md` when done.
</output>
