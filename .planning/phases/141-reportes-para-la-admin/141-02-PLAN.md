---
phase: 141-reportes-para-la-admin
plan: 02
type: execute
wave: 2
depends_on: [141-01]
files_modified:
  - el-templo-api/src/modules/finance/transaction-service.ts
  - el-templo-api/src/modules/finance/routes.ts
  - el-templo-api/src/modules/finance/schemas.ts
  - el-templo-api/src/modules/finance/types.ts
  - el-templo-api/test/finance/mov-egresos-history.test.ts
autonomous: true
requirements: [REP-03, REP-04]
must_haves:
  truths:
    - "Admin GETs a historial of cash_transfer + expense + adjustment rows filterable by caja and período"
    - "The historial INCLUDES rows with member_id NULL (the LEFT JOIN proves it — INNER JOIN would drop them)"
    - "A coach hitting /movements-history gets 403"
    - "Non-owner country filtering scopes by the caja's country (does not hide central/banco via the LEFT-JOIN NULL-branch trap incorrectly)"
    - "The historial exports a .xlsx via a sibling /export endpoint reusing the exceljs pattern"
  artifacts:
    - path: "el-templo-api/src/modules/finance/transaction-service.ts"
      provides: "listMovEgresos() own LEFT-JOIN query (users+branches+cash_registers), kind IN (cash_transfer,expense,adjustment)"
      contains: "listMovEgresos"
    - path: "el-templo-api/src/modules/finance/routes.ts"
      provides: "GET /movements-history + sibling /export"
      contains: "movements-history"
    - path: "el-templo-api/test/finance/mov-egresos-history.test.ts"
      provides: "LEFT-JOIN-includes-NULL-member + caja/período filter + coach 403 coverage"
      contains: "member_id"
  key_links:
    - from: "routes.ts GET /movements-history"
      to: "transactionService.listMovEgresos"
      via: "service call with caja/período/country-scope filters"
      pattern: "listMovEgresos"
    - from: "listMovEgresos"
      to: "leftJoin(users)"
      via: "LEFT JOIN so NULL-member rows survive"
      pattern: "leftJoin"
---

<objective>
Add the historial de movimientos inter-caja y egresos endpoint (REP-03) plus its sibling Excel export (REP-04). This is the load-bearing 139 LEFT JOIN fix: cash_transfer/expense/adjustment rows have member_id = NULL (and often branch_id = NULL), which the shared list()/exportRowsForExcel() INNER JOINs silently drop. listMovEgresos() is a NEW sibling query that LEFT JOINs — the shared list() is NOT mutated (mutating it would leak orphan rows into the Movimientos tab and break Phase 109's tested export shape).

Purpose: Give the admin the complete inter-caja movement + egreso trail, filterable by caja/período, with the NULL-member rows visible. No migration.
Output: 1 GET read + 1 GET /export endpoint, listMovEgresos() service method + types, 1 integration test asserting NULL-member rows appear.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-reportes-para-la-admin/141-CONTEXT.md
@.planning/phases/141-reportes-para-la-admin/141-RESEARCH.md
@.planning/phases/141-reportes-para-la-admin/141-01-SUMMARY.md

<interfaces>
<!-- Extracted from codebase + Plan 01 output. -->

From transaction-service.ts list() (the INNER-JOIN member-keyed path — DO NOT mutate):

- INNER JOINs users (eq users.id, financialTransactions.memberId) AND branches (eq branches.id, financialTransactions.branchId) → drops NULL-member / NULL-branch rows
- `const recorder = alias(schema.users, "recorder");` reused for recorderName
- buildListConditions (~991) reused for date/caja conditions; filters.country does eq(branches.country, country) — UNSAFE under LEFT JOIN (NULL-branch row excluded)

From RESEARCH Code Examples (the new sibling query shape):
const recorder = alias(schema.users, "recorder");
db.select({ id, transactionDate, kind, direction, amount, currency, cashRegisterId,
cashRegisterName: cashRegisters.name, branchId, branchName: branches.name,
recordedBy, recorderFirstName, recorderLastName, voidedAt, voidReason, notes })
.from(financialTransactions)
.leftJoin(users, eq(users.id, financialTransactions.memberId))
.leftJoin(branches, eq(branches.id, financialTransactions.branchId))
.leftJoin(cashRegisters, eq(cashRegisters.id, financialTransactions.cashRegisterId))
.leftJoin(recorder, eq(recorder.id, financialTransactions.recordedBy))
.where(and(inArray(kind, ["cash_transfer","expense","adjustment"]), /_ caja/período/scope _/))
.orderBy(desc(transactionDate), desc(createdAt));

From routes.ts (owner-aware country resolution, reuse verbatim — same as Plan 01):
if (request.scope.isOwner) { country = request.query.country ? ...toUpperCase() : undefined } else { country = request.scope.country ?? undefined }

- country-scope trap: do NOT eq(branches.country, country) under the LEFT JOIN; for non-owner scope by the caja's country (cash_registers.branch_id → branches.country), branch-less central/banco owner-only (mirror enforceCajaScope ~128)
- export pattern: new Workbook(), addWorksheet, sheet.columns, header bold + fgColor FFE0E0E0, reply Content-Type spreadsheetml + Content-Disposition attachment

From test/helpers.ts:
createTestApp(), getAuthToken, createStaffUser(role:"coach"), ensureEfectivoCaja(app, branchId)
movement/expense seeding pattern → see test/finance/movement-service.test.ts (how a cash_transfer + expense with member_id NULL is created)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Wave-0 test scaffold — historial LEFT JOIN proof</name>
  <files>el-templo-api/test/finance/mov-egresos-history.test.ts</files>
  <read_first>
    - el-templo-api/test/finance/movement-service.test.ts (how a cash_transfer movement + an expense are seeded; member_id NULL shape)
    - el-templo-api/test/finance/validation-state.test.ts (coach-token seeding + 403-over-HTTP pattern, FINANCE_URL)
    - el-templo-api/test/helpers.ts (createTestApp, getAuthToken, createStaffUser, ensureEfectivoCaja)
    - .planning/phases/141-reportes-para-la-admin/141-RESEARCH.md (Pitfall 1 + endpoint (c) + Wave 0 Gaps)
  </read_first>
  <behavior>
    - Test (THE fix): seed an expense (member_id NULL) and a cash_transfer (member_id NULL); GET /admin/finance/movements-history returns BOTH rows (proves LEFT JOIN — an INNER JOIN would drop them). Assert a returned row has memberId null and still renders cashRegisterName + recorderName.
    - Test: filter by ?cashRegisterId returns only that caja's movimientos/egresos.
    - Test: filter by ?dateFrom/?dateTo (período) bounds the rows.
    - Test: kind IN (cash_transfer, expense, adjustment) — an adjustment row appears; a member cobro (kind=membership/charge) does NOT appear in this view.
    - Test: coach token → 403.
  </behavior>
  <action>Create el-templo-api/test/finance/mov-egresos-history.test.ts mirroring movement-service.test.ts seeding (use the movement + expense creation paths to produce member_id NULL rows) and validation-state.test.ts's coach-token + 403-over-HTTP pattern. Encode the behaviors above as failing tests (endpoint absent → currently 404, fine for RED). The NULL-member assertion is the load-bearing one — make it explicit and commented as the 139 LEFT JOIN proof. Do NOT run the full suite; `npx tsc --noEmit` must pass. Reference REP-03 in the describe block.</action>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists, type-checks clean, and asserts: a NULL-member expense AND a NULL-member cash_transfer both appear in the historial response (the LEFT JOIN proof); caja filter; período filter; adjustment included but member cobros excluded; coach 403.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>mov-egresos-history.test.ts encodes the LEFT-JOIN-includes-NULL-member proof + caja/período filter + kind scope + coach 403 as failing assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: listMovEgresos() own LEFT-JOIN query + types, then wire GET /movements-history + /export</name>
  <files>el-templo-api/src/modules/finance/transaction-service.ts, el-templo-api/src/modules/finance/types.ts, el-templo-api/src/modules/finance/routes.ts, el-templo-api/src/modules/finance/schemas.ts</files>
  <read_first>
    - el-templo-api/src/modules/finance/transaction-service.ts (list() ~860 for recorder alias + join chain to MIRROR-BUT-LEFT-JOIN; buildListConditions ~991 for date/caja reuse — but country handling must change, see action)
    - el-templo-api/src/modules/finance/routes.ts (GET /transactions ~686 owner-aware country resolution; /transactions/export ~823 exceljs build; resolveCajaCountry/enforceCajaScope ~104-143 for non-owner caja scope)
    - el-templo-api/src/modules/finance/schemas.ts (listTransactionsSchema ~243, exportTransactionsSchema ~502 to mirror)
    - .planning/phases/141-reportes-para-la-admin/141-RESEARCH.md (endpoint (c), the fix scoped precisely, Pitfall 2 country trap, Code Examples)
    - .planning/phases/141-reportes-para-la-admin/141-CONTEXT.md (D-: include adjustment in historial)
  </read_first>
  <behavior>
    - listMovEgresos returns cash_transfer + expense + adjustment rows including member_id NULL ones (LEFT JOIN), ordered desc(transactionDate), filterable by caja/período/country.
    - GET /movements-history returns the paginated JSON; coach → 403; non-owner is scoped by caja country (central/banco owner-only); /export returns the styled .xlsx.
  </behavior>
  <action>Add to types.ts: `MovEgresoItem` (id, transactionDate, kind, direction, amount, currency, cashRegisterId, cashRegisterName, branchId, branchName: string|null, recordedBy, recorderName, voidedAt: string|null, voidReason: string|null, notes: string|null) and `MovEgresoFilters` (cashRegisterId?, country?, dateFrom?, dateTo?, page?, limit?).

Add `listMovEgresos(filters): Promise<PaginatedResult<MovEgresoItem>>` to TransactionService as a NEW sibling method — do NOT call or mutate list()/exportRowsForExcel(). Build the LEFT-JOIN query per the RESEARCH Code Example: leftJoin users (memberId), leftJoin branches (branchId), leftJoin cash_registers (cashRegisterId), leftJoin recorder alias (recordedBy). WHERE: inArray(kind, ['cash_transfer','expense','adjustment']) AND optional eq(cashRegisterId), gte/lte(transactionDate) for período. ORDER BY desc(transactionDate), desc(createdAt). Paginate like list() with a matching COUNT query using the SAME LEFT-JOIN chain. Map recorderName from the recorder alias; memberName omitted (these rows have member_id NULL). Country scope: do NOT add eq(branches.country, country) (the LEFT-JOIN NULL-branch trap would wrongly exclude central/banco rows). Instead accept a resolved scope and, for non-owner, restrict cashRegisterId set to the caller's-country cajas (sub-select active cajas where branches.country = scope.country) and exclude branch-less central/banco (owner-only) — mirror enforceCajaScope semantics. Owner sees all (optional ?country narrows by caja country).

In schemas.ts add `movementsHistorySchema` (querystring: cashRegisterId, country, dateFrom/dateTo format date, page, limit; additionalProperties:false; loose response) and `movementsHistoryExportSchema` (same minus page/limit).

In routes.ts add (after the Plan-01 reads): `GET /movements-history` — owner-aware country resolution, resolve non-owner scope (isOwner + scope.country), call transactionService.listMovEgresos(filters), return result; and `GET /movements-history/export` — same filters minus page/limit, build a Workbook sheet "Mov-Egresos" with columns Fecha · Tipo · Dirección · Concepto/Notas · Monto · Moneda · Caja · Registrado por · Anulado · Razón; header bold + fgColor FFE0E0E0; filename `mov-egresos-${today}.xlsx`. handleServiceError on catch (err: unknown). No `any`; Pino logger. Do NOT touch /transactions, /transactions/export, /transactions/summary, or the existing list()/exportRowsForExcel().</action>
<verify>
<automated>cd el-templo-api && npx tsc --noEmit</automated>
</verify>
<acceptance_criteria> - listMovEgresos is a NEW method LEFT-JOINing users+branches+cash_registers+recorder, kind IN (cash_transfer,expense,adjustment); list()/exportRowsForExcel() are untouched. - Country scope avoids the eq(branches.country) NULL-branch trap: non-owner restricted to own-country cajas, branch-less central/banco owner-only. - GET /movements-history + /movements-history/export registered (coach 403 via module guard); export reuses the exceljs Workbook pattern (no parallel mechanism). - Types MovEgresoItem/MovEgresoFilters exported; no `any`; `npx tsc --noEmit` passes.
</acceptance_criteria>
<done>listMovEgresos LEFT-JOIN sibling query + GET /movements-history + /export wired; NULL-member rows survive, caja/período filter works, coach excluded, country trap avoided; Wave-0 test now has an endpoint (CI runs it green).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                      | Description                                                                  |
| --------------------------------------------- | ---------------------------------------------------------------------------- |
| admin browser → finance API                   | authenticated staff; role + country enforced server-side                     |
| non-owner → cross-country movimientos/egresos | the LEFT-JOIN NULL-branch trap could leak or wrongly hide central/banco rows |

## STRIDE Threat Register

| Threat ID | Category               | Component                                              | Disposition | Mitigation Plan                                                                                                                                                                             |
| --------- | ---------------------- | ------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-141-05  | Elevation/Disclosure   | GET /movements-history                                 | mitigate    | Module onRequest hook enforces FINANCE_READ_ROLES (coach absent → 403). Coach-403 test asserts over HTTP.                                                                                   |
| T-141-06  | Information Disclosure | non-owner cross-country via LEFT-JOIN NULL-branch trap | mitigate    | Do NOT eq(branches.country) under LEFT JOIN; scope non-owner by caja country (cash_registers.branch_id → branches.country), branch-less central/banco owner-only (mirror enforceCajaScope). |
| T-141-07  | Information Disclosure | mass export ignoring scope                             | mitigate    | /movements-history/export reuses the SAME owner-aware resolution + caja-country scope + filters as the list; no unscoped path.                                                              |
| T-141-08  | Tampering              | SQL injection via filters                              | mitigate    | Drizzle parameterized queries; movementsHistorySchema additionalProperties:false + format/enum.                                                                                             |
| T-141-SC  | Tampering              | npm installs                                           | accept      | Zero new packages (RESEARCH: "Cero dependencias nuevas").                                                                                                                                   |

</threat_model>

<verification>
- `cd el-templo-api && npx tsc --noEmit` clean after each task.
- mov-egresos-history.test.ts encodes: NULL-member rows appear (LEFT JOIN proof), caja filter, período filter, adjustment included, coach 403. CI runs it green on push.
- The shared list()/exportRowsForExcel() remain byte-for-byte unchanged (grep diff): the Movimientos tab and Phase 109's export-transactions.test.ts must not regress.
- No migration generated.
</verification>

<success_criteria>

- GET /movements-history returns cash_transfer + expense + adjustment INCLUDING member_id NULL rows; filterable by caja/período; coach → 403.
- Non-owner country scope goes through the caja's country (no NULL-branch trap); central/banco owner-only.
- Historial exports .xlsx via a sibling endpoint reusing the exceljs pattern.
- list()/exportRowsForExcel() untouched — Movimientos tab + 109 export unaffected.
  </success_criteria>

<output>
Create `.planning/phases/141-reportes-para-la-admin/141-02-SUMMARY.md` when done.
</output>
