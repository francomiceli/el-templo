---
phase: 141-reportes-para-la-admin
plan: 04
type: execute
wave: 4
depends_on: [141-03]
files_modified:
  - el-templo-admin/src/components/caja/BandejaPendientesTab.vue
  - el-templo-admin/src/components/caja/MovEgresosTab.vue
  - el-templo-admin/src/pages/CajaPage.vue
autonomous: false
requirements: [REP-01, REP-03, REP-04]
must_haves:
  truths:
    - "The Pendientes tab (landing) lists pendiente+observado oldest-first with socio·monto·medio·caja·cargado-por·antigüedad·estado, a prominent Validar button + ⋮ menu (Observar/Corregir/Anular)"
    - "Overdue rows (>thresholdDays from the payload) carry a 'vencido' badge + warm tint, and a counter banner '⚠ N pendientes superan los N días' shows above"
    - "A Pendientes/Observados/Todos filter switches the list; observados live in the same list with an amber badge"
    - "Anular opens the membership 1-a-1 popup with keepMembershipActive (default ON) wired to the void body"
    - "The Mov. y egresos tab lists cash_transfer+expense+adjustment (incl. member_id NULL rows) filterable by caja/período, with Excel export"
  artifacts:
    - path: "el-templo-admin/src/components/caja/BandejaPendientesTab.vue"
      provides: "daily-control bandeja: oldest-first list, Validar+menu actions, overdue alert, membership popup, Excel export"
      contains: "Validar"
    - path: "el-templo-admin/src/components/caja/MovEgresosTab.vue"
      provides: "historial table filterable by caja/período, renders NULL-member rows, Excel export"
      contains: "q-table"
  key_links:
    - from: "BandejaPendientesTab Anular"
      to: "useTransactionsApi.voidTransaction"
      via: "void with { reason, keepMembershipActive }"
      pattern: "keepMembershipActive"
    - from: "BandejaPendientesTab Validar"
      to: "useTransactionsApi.validateTransaction"
      via: "POST /transactions/:id/validate then refresh"
      pattern: "validateTransaction"
    - from: "MovEgresosTab"
      to: "useTransactionsApi.getMovEgresosHistory"
      via: "GET /movements-history with caja/período filters"
      pattern: "getMovEgresosHistory"
---

<objective>
Build the two remaining hub tabs: the Bandeja de Pendientes (the landing daily-control surface — REP-01) with one-tap Validar, the ⋮ menu for Observar/Corregir/Anular, the overdue alert (vencido badge + tint + counter banner driven by the server thresholdDays), the Pendientes/Observados/Todos filter, and the membership 1-a-1 popup on Anular; and the Mov. y egresos historial tab (REP-03) filterable by caja/período that renders the NULL-member rows. Both export via the Plan-01/02 sibling endpoints (REP-04). Closes with a human-verify checkpoint.

Purpose: Deliver the validation control that replaces el cierre de caja diario, honoring 141-UI-SPEC's bandeja and historial contracts. Reuses the 137 action endpoints (already built) wired through the Plan-03 composable.
Output: BandejaPendientesTab.vue, MovEgresosTab.vue, CajaPage placeholders replaced, human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-reportes-para-la-admin/141-CONTEXT.md
@.planning/phases/141-reportes-para-la-admin/141-UI-SPEC.md
@.planning/phases/141-reportes-para-la-admin/141-01-SUMMARY.md
@.planning/phases/141-reportes-para-la-admin/141-02-SUMMARY.md
@.planning/phases/141-reportes-para-la-admin/141-03-SUMMARY.md
@el-templo-admin/src/pages/CajaPage.vue
@el-templo-admin/src/composables/useTransactionsApi.ts

<interfaces>
<!-- Plan-03 composable + UI-SPEC contracts. -->

From useTransactionsApi.ts (Plan 03 extended — consume):
getPendingTray(params): PendingTrayResult (PaginatedResult<PendingTrayItem> & { thresholdDays })
getMovEgresosHistory(params): PaginatedResult<MovEgresoItem>
validateTransaction(id) ; observeTransaction(id, reason) ; correctTransaction(id, correctedFields)
voidTransaction(id, reason, keepMembershipActive?) // membership popup wires the boolean
exportPendingTrayToExcel(params) ; exportMovEgresosToExcel(params) // → Blob
PendingTrayItem fields: id, transactionDate, memberId, memberName, amount, currency, paymentMethod, cashRegisterName, recorderName, validationStatus ('pendiente'|'observado'), ageInDays, isOverdue
MovEgresoItem fields: id, transactionDate, kind, direction, amount, currency, cashRegisterId, cashRegisterName, branchName(nullable), recorderName, voidedAt, voidReason, notes

From CajaPage.vue (reuse patterns):
methodColor/methodLabel ; q-table @request server-side pattern ; body-cell slots ; row :class tint (isVoided) ; $q.dialog confirm ; detail dialog (q-list label/value) ; onExportCaja blob-download (createObjectURL+anchor) ; clickable member name → router.push('/alumnos/:id')

From UI-SPEC (honor — Tab 1 + Tab 4 + Copywriting + States):

- Bandeja: vencido counter banner (q-banner warm-red tint) only when vencidoCount>0: "⚠ {N} pendientes superan los {threshold} días"; filter q-btn-toggle Pendientes(default)/Observados/Todos; q-table oldest-first (server-enforced, column sort disabled); row fields socio(Terracotta link)·monto+moneda badge·medio(methodColor badge)·caja·cargado por(text-caption)·antigüedad("hace N días", gold 1-3 / warm-red >threshold)·estado(Pendiente gold / Observado amber badge)·acciones
- Actions: Validar prominent primary q-btn → lightweight $q.dialog confirm "¿Validar el pago de {monto} de {socio}?" → validate → notify "Pago validado" → refresh bandeja (+ the tab vencido badge). ⋮ q-menu: Observar (dialog, required "Motivo de observación \*" → observe → "Pago observado"), Corregir (correction form dialog amount/paymentMethod/cashRegister/notes prefilled → correct → "Pago corregido"), Anular (membership popup below).
- Anular membership popup: q-dialog ~480px, title "Anular pago de {socio}", body "...deja rastro y no se puede deshacer.", toggle "Mantener la membresía activa" default ON (keepMembershipActive:true) + helper, required "Motivo de anulación \*", actions [Cancelar][Anular negative] → void(id, reason, keepMembershipActive) → "Pago anulado" → refresh. Hide toggle if no active membership link (plain void).
- threshold comes from response.thresholdDays (NOT hardcoded) so 142 swaps it server-side; the vencido count drives a floating q-badge on the Pendientes tab (update the Plan-03 hub tab badge).
- Mov-Egresos: filter bar [Caja q-select][Período month/date-range][Tipo Movimientos/Egresos/Todos] + Exportar Excel; q-table server-side @request columns Fecha·Tipo(badge Movimiento/Egreso)·Origen→Destino(mov)/Concepto(egreso)·Monto+moneda badge·Caja·Registrado por·⋮Detalles; egreso amount shows leading "−" warm-red, NO cross-currency total; transfer rendered as one logical row; detail dialog reused.
- Empty states: bandeja Pendientes "Sin pendientes / No hay pagos esperando validación. Buen trabajo."; Observados "No hay pagos observados."; mov/egresos "Sin movimientos ni egresos para los filtros seleccionados."
- Accessibility: color never sole signal (vencido text badge + estado text label always present); warm palette NO blue.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: BandejaPendientesTab — daily-control list + actions + overdue alert + membership popup</name>
  <files>el-templo-admin/src/components/caja/BandejaPendientesTab.vue, el-templo-admin/src/pages/CajaPage.vue</files>
  <read_first>
    - .planning/phases/141-reportes-para-la-admin/141-UI-SPEC.md (Tab 1 Bandeja: layout, row anatomy, vencido alert, filter, actions table, membership popup, copywriting, states matrix, threshold)
    - el-templo-admin/src/pages/CajaPage.vue (q-table @request + body-cell slots + row :class tint + $q.dialog + detail dialog + confirmVoid + clickable member + methodColor/methodLabel patterns to reuse)
    - .planning/phases/141-reportes-para-la-admin/141-03-SUMMARY.md (composable fn names + hub tab-badge mechanism + how selectedCountry/isOwner are passed)
    - .planning/phases/141-reportes-para-la-admin/141-01-SUMMARY.md (PendingTrayItem + thresholdDays payload shape)
  </read_first>
  <action>Create src/components/caja/BandejaPendientesTab.vue. Load via getPendingTray({ status, country: isOwner?selectedCountry:undefined, page, limit }) on mount + on filter/page change; store thresholdDays from the response. Render: (1) a vencido counter q-banner (warm-red tint) shown only when vencidoCount>0 with copy "⚠ {N} pendientes superan los {thresholdDays} días" (count = rows where isOverdue); (2) a q-btn-toggle filter Pendientes(default)/Observados/Todos mapping to the status param, plus an Exportar Excel outline btn (exportPendingTrayToExcel + blob-download); (3) a q-table with server-side @request, column sorting disabled (oldest-first is server-enforced), body-cell slots rendering: socio as a Terracotta clickable link → router.push(`/alumnos/${memberId}`), monto with moneda badge beside, medio via methodColor/methodLabel badge, caja (cashRegisterName), cargado por (recorderName, text-caption), antigüedad ("hace {ageInDays} días" — gold if 1..thresholdDays, warm-red if >thresholdDays), estado badge (Pendiente gold / Observado amber). Apply a warm row tint (:class) when isOverdue and append a q-badge color=negative label="vencido" to the estado cell.

Actions cell: a prominent primary "Validar" q-btn (dense) → lightweight $q.dialog confirm "¿Validar el pago de {monto} de {socio}?" → validateTransaction(id) → $q.notify positive "Pago validado" → reload bandeja + emit an updated vencido count to the hub. A "⋮" q-menu with: Observar (q-dialog, required textarea "Motivo de observación \*", isValid non-empty → observeTransaction(id, reason) → "Pago observado"); Corregir (correction q-dialog form prefilled with amount/paymentMethod/cashRegister/notes → correctTransaction(id, correctedFields) → "Pago corregido"); Anular → open the membership popup.

Membership popup: a q-dialog (~480px) titled "Anular pago de {socio}", body copy per UI-SPEC, a "Mantener la membresía activa" q-toggle defaulting ON (keepMembershipActive:true) with the helper caption, a required "Motivo de anulación \*" textarea, actions [Cancelar flat][Anular color=negative] → voidTransaction(id, reason, keepMembershipActive) → "Pago anulado" → reload. If the row has no active membership link, hide the toggle (plain void with reason). (Use the row data / a link flag to decide; if the link state isn't on PendingTrayItem, show the toggle by default — safer.)

Empty states + error notifies + per-action btn :loading per the UI-SPEC States Matrix. createLogger('BandejaPendientesTab'); warm palette NO blue; format currency via the existing admin formatter; cleanup()/onUnmounted at component level.

In CajaPage.vue: replace the pendientes placeholder panel with <BandejaPendientesTab> (pass selectedCountry/isOwner), and wire the emitted vencido count to the floating q-badge on the Pendientes tab (per UI-SPEC, visible from other tabs).</action>
<verify>
<human-check>Open /caja (lands on Pendientes): list is oldest-first; a >threshold row shows "vencido" badge + warm tint and the counter banner appears; the Pendientes/Observados/Todos filter switches rows (observados show amber badge in the same list); Validar one-tap confirms + validates + refreshes; ⋮ → Observar/Corregir work; ⋮ → Anular opens the membership popup with "Mantener la membresía activa" default ON; Exportar Excel downloads a .xlsx.</human-check>
<automated>cd el-templo-admin && npx tsc --noEmit</automated>
</verify>
<acceptance_criteria> - Bandeja lists pendiente+observado oldest-first with all D-02 row fields; overdue rows have vencido badge + warm tint; counter banner uses thresholdDays from the payload (not hardcoded). - Pendientes/Observados/Todos filter works; observados in the same list with amber badge. - Validar = one-tap confirm→validate→refresh; ⋮ menu has Observar/Corregir/Anular; Anular popup wires keepMembershipActive (default ON) into void. - Excel export wired; CajaPage pendientes panel replaced + tab vencido badge wired; warm palette NO blue; `npx tsc --noEmit` passes.
</acceptance_criteria>
<done>BandejaPendientesTab delivers the daily-control surface (oldest-first, overdue alert, filter, Validar one-tap, ⋮ actions, membership popup) wired to the 137 endpoints via the composable, with Excel export and the hub tab badge.</done>
</task>

<task type="auto">
  <name>Task 2: MovEgresosTab — historial filterable by caja/período (renders NULL-member rows)</name>
  <files>el-templo-admin/src/components/caja/MovEgresosTab.vue, el-templo-admin/src/pages/CajaPage.vue</files>
  <read_first>
    - .planning/phases/141-reportes-para-la-admin/141-UI-SPEC.md (Tab 4 Movimientos y Egresos: layout, LEFT JOIN flag, tipo rendering, detail dialog, copy, states)
    - el-templo-admin/src/pages/CajaPage.vue (q-table @request + filter bar + detail dialog patterns to reuse)
    - .planning/phases/141-reportes-para-la-admin/141-02-SUMMARY.md (MovEgresoItem field names + the GET /movements-history filter params)
    - .planning/phases/141-reportes-para-la-admin/141-03-SUMMARY.md (getMovEgresosHistory + exportMovEgresosToExcel signatures; how selectedCountry/isOwner pass)
  </read_first>
  <action>Create src/components/caja/MovEgresosTab.vue. Filter bar (q-col-gutter-sm, items-end): a Caja q-select (options from getCashRegisterBalances or a caja list — reuse the saldos data if convenient, else fetch active cajas), a Período control (q-input type=month or a date range mapped to dateFrom/dateTo), and a Tipo toggle Movimientos/Egresos/Todos (maps client-side or via kind filter — Movimientos→cash_transfer, Egresos→expense, Todos→all three incl. adjustment), plus an Exportar Excel outline btn (exportMovEgresosToExcel + blob-download). Load via getMovEgresosHistory({ cashRegisterId, dateFrom, dateTo, country: isOwner?selectedCountry:undefined, page, limit }) on mount + filter/page change (server-side @request q-table). Columns: Fecha · Tipo (badge: neutral "Movimiento" / negative-toned "Egreso") · Concepto/Origen→Destino (concepto/notes for egreso; for a transfer render origen→destino as one logical row) · Monto (+ moneda badge; egreso shows leading "−" warm-red) · Caja (cashRegisterName) · Registrado por (recorderName, render "—" when absent) · ⋮ Detalles. CRITICAL: these rows have member_id NULL — they come from the LEFT-JOIN endpoint (Plan 02); render them with recorderName/"—", never assume a memberName. NO cross-currency total anywhere. Detail dialog: reuse the CajaPage q-list label/value dialog adapted for origen/destino/concepto/motivo/registrado por/fecha. Empty → "Sin movimientos ni egresos para los filtros seleccionados." Error → $q.notify negative. createLogger('MovEgresosTab'); warm palette NO blue; cleanup()/onUnmounted at component level.

In CajaPage.vue: replace the movEgresos placeholder panel with <MovEgresosTab> (pass selectedCountry/isOwner).</action>
<verify>
<human-check>Open /caja → Mov. y egresos tab: the table shows inter-caja movimientos AND egresos (rows with no socio still appear — the LEFT JOIN); filtering by Caja and by Período narrows the rows; Tipo toggle switches Movimientos/Egresos/Todos; egreso amounts show "−" in warm-red; no cross-currency total; ⋮ Detalles opens the detail dialog; Exportar Excel downloads a .xlsx.</human-check>
<automated>cd el-templo-admin && npx tsc --noEmit</automated>
</verify>
<acceptance_criteria> - Mov-Egresos tab lists cash_transfer+expense+adjustment including NULL-member rows (from the LEFT-JOIN endpoint), filterable by Caja and Período, with a Tipo toggle. - Egreso amounts show leading "−" warm-red; NO cross-currency total; transfer rendered as one logical row; detail dialog reused; empty/error states per UI-SPEC. - Excel export wired; CajaPage movEgresos panel replaced; warm palette NO blue; `npx tsc --noEmit` passes.
</acceptance_criteria>
<done>MovEgresosTab renders the complete inter-caja movement + egreso trail (NULL-member rows visible) filterable by caja/período with Excel export.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human-verify the complete /caja cash-control hub</name>
  <what-built>The complete /caja cash-control hub: q-tabs (Pendientes landing / Saldos / Movimientos / Mov. y egresos), the bandeja daily-control (oldest-first, overdue alert, Validar one-tap, ⋮ Observar/Corregir/Anular, membership popup), saldos cards (per-currency subtotals), and the mov/egresos historial (NULL-member rows visible) — all coach-excluded, all exportable.</what-built>
  <how-to-verify>
    1. Log in to el-templo-admin as owner/admin → go to /caja. It lands on **Pendientes**.
    2. **Bandeja:** confirm oldest-first ordering; a pending older than 3 days shows a "vencido" badge + warm tint and the "⚠ N pendientes superan los 3 días" banner appears. Toggle Pendientes/Observados/Todos. Tap **Validar** on one row → confirm → it validates and disappears from Pendientes. Open ⋮ → Observar (enter motivo) → row flips to amber "Observado". ⋮ → Anular → the membership popup shows "Mantener la membresía activa" default ON → anular with a motivo.
    3. **Saldos:** cards grouped Efectivo sucursales / Efectivo central / Banco; firme large, Pendiente small; moneda badge beside; subtotals are per-currency only (no ARS+EUR combined).
    4. **Movimientos:** the old CajaPage view is intact (summary, table, Excel export); no "Próximamente" egresos card.
    5. **Mov. y egresos:** movimientos + egresos appear INCLUDING rows with no socio; filter by Caja and Período; egreso amounts show "−"; Exportar Excel downloads a .xlsx.
    6. Confirm **no blue** anywhere (warm palette) and that a **coach** account does NOT see /caja.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues (wrong ordering, missing NULL-member rows, cross-currency total, coach access, blue colors, broken action).</resume-signal>
  <action>PAUSE for human verification. Present the how-to-verify steps to the user and wait for "approved" before marking the phase complete. This is a blocking checkpoint — do not auto-advance.</action>
  <verify><human-check>User confirms the full hub works end-to-end (bandeja actions, overdue alert, saldos currency isolation, historial NULL-member rows, coach exclusion, warm palette) and replies "approved".</human-check></verify>
  <done>User replied "approved" (or listed issues that were then resolved and re-verified).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary          | Description                                                              |
| ----------------- | ------------------------------------------------------------------------ |
| coach → /caja hub | coach must not reach the bandeja/historial                               |
| anular action     | membership cancel decision must reach the server as keepMembershipActive |

## STRIDE Threat Register

| Threat ID | Category               | Component                                   | Disposition | Mitigation Plan                                                                                                                                       |
| --------- | ---------------------- | ------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-141-11  | Tampering              | Validar/Observar/Corregir/Anular from UI    | mitigate    | Actions hit the 137 endpoints which enforce FINANCE_VOID_ROLES server-side (coach 403, asserted in Plan 01/02 tests); the bandeja only surfaces them. |
| T-141-12  | Tampering              | Anular membership decision dropped          | mitigate    | voidTransaction threads keepMembershipActive (Plan 03); the popup wires the toggle to the body; default ON (sub untouched).                           |
| T-141-13  | Information Disclosure | NULL-member historial rows / cross-currency | mitigate    | Rows render recorderName/"—" (never assume member); no cross-currency total path; coach excluded via router meta + backend FINANCE_READ_ROLES.        |
| T-141-SC  | Tampering              | npm installs                                | accept      | Zero new packages (RESEARCH: "Cero dependencias nuevas").                                                                                             |

</threat_model>

<verification>
- `cd el-templo-admin && npx tsc --noEmit` clean after each task.
- Human-verify checkpoint covers the full hub end-to-end (bandeja actions, overdue, saldos currency isolation, historial NULL-member rows, coach exclusion, warm palette).
- No onUnmounted inside the composable; component-level cleanup only.
</verification>

<success_criteria>

- Pendientes tab: oldest-first, overdue alert (thresholdDays from payload), filter, Validar one-tap, ⋮ Observar/Corregir/Anular, membership popup (keepMembershipActive default ON).
- Mov. y egresos tab: cash_transfer+expense+adjustment incl. NULL-member rows, caja/período filter, Excel export, no cross-currency total.
- Both panels wired into the hub; coach excluded; warm palette no blue.
- Human-verify checkpoint approved.
  </success_criteria>

<output>
Create `.planning/phases/141-reportes-para-la-admin/141-04-SUMMARY.md` when done.
</output>
