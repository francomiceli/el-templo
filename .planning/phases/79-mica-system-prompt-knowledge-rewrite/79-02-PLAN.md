---
phase: 79-mica-system-prompt-knowledge-rewrite
plan: 02
type: execute
wave: 2
depends_on: ["79-01"]
files_modified:
  - el-templo-bot/test/knowledge.test.ts
autonomous: true
requirements:
  [
    MICA-01,
    MICA-02,
    MICA-03,
    KNOW-01,
    KNOW-02,
    KNOW-03,
    KNOW-04,
    KNOW-05,
    KNOW-06,
    KNOW-07,
  ]

must_haves:
  truths:
    - "Tests verify all 12 knowledge sections are present in output"
    - "Tests verify Mica identity appears in system prompt"
    - "Tests verify state-adaptive sections exist for all 5 client states"
    - "Tests verify critical pricing data accuracy"
    - "All tests pass"
  artifacts:
    - path: "el-templo-bot/test/knowledge.test.ts"
      provides: "Updated test suite verifying new knowledge structure and Mica prompt"
      contains: "Mica"
  key_links:
    - from: "el-templo-bot/test/knowledge.test.ts"
      to: "el-templo-bot/src/ai/knowledge.ts"
      via: "import getBusinessKnowledge"
      pattern: "getBusinessKnowledge"
    - from: "el-templo-bot/test/knowledge.test.ts"
      to: "el-templo-bot/src/ai/system-prompt.ts"
      via: "import getSystemPrompt"
      pattern: "getSystemPrompt"
---

<objective>
Update the knowledge and system prompt test suite to verify the new Mica persona and 12-section knowledge structure.

Purpose: Ensure all business data is accurate, all new sections (sales techniques, objection handling, retention, golden rules) are present, and the Mica persona is correctly integrated into the system prompt.

Output: Updated `knowledge.test.ts` with comprehensive coverage of new content.
</objective>

<execution_context>
@/Users/bores/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bores/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (v5.2 section — Phase 79 success criteria)
@.planning/REQUIREMENTS.md (MICA-01..03, KNOW-01..07)
@.planning/phases/79-mica-system-prompt-knowledge-rewrite/79-01-SUMMARY.md
@el-templo-bot/test/knowledge.test.ts (CURRENT tests to UPDATE)
@el-templo-bot/src/ai/knowledge.ts (rewritten in Plan 01)
@el-templo-bot/src/ai/system-prompt.ts (rewritten in Plan 01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update knowledge and system prompt tests for Mica persona and 12 knowledge sections</name>
  <files>el-templo-bot/test/knowledge.test.ts</files>
  <action>
Rewrite `el-templo-bot/test/knowledge.test.ts` to verify the new content structure. Keep the existing imports (`getBusinessKnowledge`, `BRANCH_ADDRESSES`, `BRANCH_MAPS_LINKS`, `getSystemPrompt`) and the branch address accuracy tests (section 2 — those are still valid and unchanged).

**Replace section 1 (Business knowledge data accuracy) with these test groups:**

1. "Pricing" — keep existing price checks (they should still pass with same values):
   - Flex $80,000 / Zero $65,000
   - Foundation $250,000 / Zero $220,000
   - Performance $600,000 / Zero $560,000
   - Credit card prices ($670,000, $370,000, $280,000)
   - NEW: Single class price $20,000

2. "Branches" — keep existing: 5 branch names, schedule data, time slots

3. "ROM" — keep existing: mentions ROM and Range of Motion

4. "Trial class" — keep existing: Boarding Pass, $20,000 value, bonificada

5. "App help" — keep existing: download links, password recovery

6. "NEW: Sales techniques" — verify knowledge contains:
   - "urgencia" or "cupos limitados"
   - "anclaje" or anchoring pattern
   - "upselling" or upgrade mention
   - "soft close" or "call to action"

7. "NEW: Objection handling" — verify knowledge contains:
   - At least 5 of the 7 objection keywords: "caro", "tiempo", "miedo", "pensarlo", "otro" (gym), "lejos", "por clase"
   - Response patterns for each

8. "NEW: Retention strategies" — verify knowledge contains:
   - "inactivo" or "no viene"
   - "vencer" or "renovar"
   - "cancelacion" or "pausa"
   - "vuelve" or "regresa"

9. "NEW: Golden rules" — verify knowledge contains:
   - "Reglas de Oro" or "reglas" section header
   - At least 10 distinct rule items (count by numbered items or bullet points)
   - Key rules: "tuteo", "emoji", "cupos disponibles", "BUTTONS_SENT", "escalar"

10. "NEW: Policies" — verify knowledge contains:
    - "turnos fijos"
    - "autogestionados" or "autogestionar"
    - "cancelar" with time reference (20 minutos)
    - "congelamiento" or "vacaciones" (Performance only)

**Replace section 3 (System prompt integration) with:**

1. "contains Mica identity" — prompt includes "Mica"
2. "uses Argentine tuteo instructions" — prompt includes "vos" and tuteo-related instructions
3. "contains max emoji rule" — prompt includes "1-2 emoji" or "maximo" + "emoji"
4. "contains tool usage rules" — prompt includes "BUTTONS_SENT", "check_schedule", "request_human"
5. "contains escalation phrase" — prompt includes "Te paso con alguien del equipo"
6. "contains business knowledge" — prompt length > 3000 chars (knowledge injected)
7. "state-adaptive: lead state focuses on trial" — `getSystemPrompt({ clientState: 'lead' })` contains "clase de prueba" or "prueba"
8. "state-adaptive: active member focuses on retention" — `getSystemPrompt({ clientState: 'active_member' })` contains "comunidad" or "activo"
9. "state-adaptive: inactive member focuses on reactivation" — `getSystemPrompt({ clientState: 'inactive_member' })` contains "volver" or "reactivar" or "motivar"
10. "state-adaptive: expired member focuses on renewal" — `getSystemPrompt({ clientState: 'expired_member' })` contains "renovar" or "renueve" or "vencio"
11. "state-adaptive: trial focuses on conversion" — `getSystemPrompt({ clientState: 'trial' })` contains "miembro" or "membresia" or "convertir"

Keep the test structure clean with describe/it blocks. Use vitest (already imported). Follow project convention of descriptive test names.
</action>
<verify>
<automated>cd /Users/bores/el-templo/el-templo-bot && npx vitest run test/knowledge.test.ts</automated>
</verify>
<done>All tests pass. Test suite covers: pricing accuracy (5 plan types + single class + credit card), branch data (5 branches, schedules), ROM, trial class, app help, sales techniques (4 strategies), objection handling (7 objections), retention strategies (4 scenarios), 12 golden rules, business policies, Mica persona identity, Argentine tuteo, emoji rule, tool usage rules, escalation phrase, and state-adaptive behavior for all 5 client states.</done>
</task>

<task type="auto">
  <name>Task 2: Verify TypeScript compilation and run full bot test suite</name>
  <files></files>
  <action>
Run the full TypeScript compilation check and test suite for the bot to ensure nothing is broken:

1. `cd el-templo-bot && npx tsc --noEmit` — full type check
2. `cd el-templo-bot && npx vitest run` — all tests (not just knowledge tests)

If any other test files import from knowledge.ts or system-prompt.ts and fail due to the rewrite, fix the imports or assertions to match the new structure. Do NOT change the rewritten source files — only fix test expectations.

Also verify the knowledge output doesn't use markdown ### headers (WhatsApp incompatible) — the output should use _bold_ section headers instead.
</action>
<verify>
<automated>cd /Users/bores/el-templo/el-templo-bot && npx tsc --noEmit && npx vitest run</automated>
</verify>
<done>Full TypeScript compilation passes. All bot tests pass. No ### headers in knowledge output (WhatsApp compatible formatting only).</done>
</task>

</tasks>

<verification>
1. `cd el-templo-bot && npx vitest run test/knowledge.test.ts` — all knowledge and prompt tests pass
2. `cd el-templo-bot && npx tsc --noEmit` — TypeScript compilation clean
3. `cd el-templo-bot && npx vitest run` — full test suite passes
</verification>

<success_criteria>

- All existing pricing/branch/ROM/trial/app tests still pass (data unchanged)
- New tests verify sales techniques, objection handling, retention, golden rules, and policies sections exist in knowledge
- New tests verify Mica identity, tuteo, emoji rule, tool rules, and escalation phrase in system prompt
- New tests verify state-adaptive behavior for all 5 client states
- Full bot test suite passes with zero failures
  </success_criteria>

<output>
After completion, create `.planning/phases/79-mica-system-prompt-knowledge-rewrite/79-02-SUMMARY.md`
</output>
