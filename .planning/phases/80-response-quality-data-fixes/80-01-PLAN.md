---
phase: 80-response-quality-data-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-bot/src/ai/system-prompt.ts
  - el-templo-bot/src/ai/tools.ts
  - el-templo-bot/src/webhook/handler.ts
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-03, QUAL-04, QUAL-05, QUAL-06, QUAL-07]

must_haves:
  truths:
    - "AI responses never contain ### markdown headers — stripped in post-processing"
    - "Schedule tool output says 'cupos disponibles' not 'lugares'"
    - "Escalation system prompt includes exact phrase with emoji"
    - "After [BUTTONS_SENT] from any tool, bot sends zero text"
    - "Trial registration asks only name and class preference"
    - "Pricing instruction emphasizes Flex first"
  artifacts:
    - path: "el-templo-bot/src/webhook/handler.ts"
      provides: "Post-processing that strips ### headers from AI output"
      contains: "stripMarkdownHeaders"
    - path: "el-templo-bot/src/ai/tools.ts"
      provides: "Schedule output with 'cupos disponibles' terminology"
      contains: "cupos disponibles"
    - path: "el-templo-bot/src/ai/system-prompt.ts"
      provides: "Escalation phrase with emoji"
      contains: "te escriben enseguida \\ud83d\\ude4c"
  key_links:
    - from: "el-templo-bot/src/webhook/handler.ts"
      to: "splitMessage"
      via: "post-processing before message splitting"
      pattern: "stripMarkdownHeaders"
    - from: "el-templo-bot/src/ai/tools.ts"
      to: "checkSchedule output"
      via: "spots text formatting"
      pattern: "cupos disponibles"
---

<objective>
Apply all 7 response quality fixes to the bot's system prompt, tool implementations, and message handler.

Purpose: Make bot responses match WhatsApp conventions and real El Templo team communication patterns. These are small, targeted code changes across 3 files.

Output: Updated system-prompt.ts, tools.ts, and handler.ts with all QUAL-01 through QUAL-07 fixes applied.
</objective>

<execution_context>
@/Users/bores/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bores/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@el-templo-bot/src/ai/system-prompt.ts
@el-templo-bot/src/ai/knowledge.ts
@el-templo-bot/src/ai/tools.ts
@el-templo-bot/src/webhook/handler.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix tools.ts and system-prompt.ts quality issues</name>
  <files>el-templo-bot/src/ai/tools.ts, el-templo-bot/src/ai/system-prompt.ts</files>
  <action>
**In `el-templo-bot/src/ai/tools.ts` — checkSchedule function:**

1. Line 316: Change `${spotsRemaining} lugares` to `${spotsRemaining} cupos disponibles` (QUAL-04).
2. Line 316: Change `lleno` to `sin cupos` to stay consistent with "cupos" terminology.

That is the only change needed in tools.ts. The schedule LIMIT 6 / display 5 / hasMore logic (QUAL-03) is already correctly implemented. The Mogotes address (Mario Bravo 618) and Maps link are already correct in BRANCH_ADDRESSES and BRANCH_MAPS_LINKS.

**In `el-templo-bot/src/ai/system-prompt.ts` — system prompt text:**

1. QUAL-07: On the line about request_human escalation (around line 68), change `"Te paso con alguien del equipo, te escriben enseguida"` to `"Te paso con alguien del equipo, te escriben enseguida 🙌"` — add the emoji that the planning context specifies.

No other changes needed in system-prompt.ts. The prompt already correctly instructs:

- QUAL-01: "NUNCA usar ### ni headers markdown" (line 46)
- QUAL-02: "Mostrar Flex primero" (line 74)
- QUAL-03: "Maximo 5 clases" (line 64)
- QUAL-04: "cupos disponibles" en vez de "lugares" (line 75) — but tools.ts output was inconsistent
- QUAL-05: "[BUTTONS_SENT], NO enviar ningun texto" (line 66)
- QUAL-06: "Solo nombre y preferencia de clase" (line 67)
  </action>
  <verify>
  <automated>cd /Users/bores/el-templo && grep -n "cupos disponibles" el-templo-bot/src/ai/tools.ts && grep -n "enseguida 🙌" el-templo-bot/src/ai/system-prompt.ts && grep -c "lugares" el-templo-bot/src/ai/tools.ts | grep "^0$"</automated>
  <manual>Verify "lugares" no longer appears in checkSchedule output, "cupos disponibles" is used instead, and escalation phrase has emoji</manual>
  </verify>
  <done>tools.ts checkSchedule uses "cupos disponibles" and "sin cupos" (not "lugares"/"lleno"). system-prompt.ts escalation phrase includes the emoji. Zero instances of "lugares" remain in tools.ts checkSchedule spotsText.</done>
  </task>

<task type="auto">
  <name>Task 2: Add markdown header stripping post-processor in handler.ts</name>
  <files>el-templo-bot/src/webhook/handler.ts</files>
  <action>
**QUAL-01 defense-in-depth**: The system prompt already tells the AI not to use ### headers, but AI models sometimes ignore instructions. Add a post-processing step in handler.ts to strip any `###` (or `##` or `#`) markdown headers from the AI response before sending.

1. Add a `stripMarkdownHeaders` function near the `splitMessage` function:

```typescript
/**
 * Strip markdown headers (###, ##, #) from AI output.
 * WhatsApp doesn't render them — they look broken to users.
 * The system prompt instructs the AI not to use them, but this
 * is defense-in-depth for when the AI ignores the instruction.
 */
function stripMarkdownHeaders(text: string): string {
  // Replace lines starting with # (1-3 hashes) followed by space and text
  // with just the text in *bold* (WhatsApp formatting)
  return text.replace(/^#{1,3}\s+(.+)$/gm, "*$1*");
}
```

2. Apply `stripMarkdownHeaders` to `replyText` BEFORE the `splitMessage` call (around line 411 in the current code). Insert right after the `replyText` assignment and before the `updateSession` call:

```typescript
// Post-process: strip markdown headers (defense-in-depth for QUAL-01)
replyText = stripMarkdownHeaders(replyText);
```

Place this after the `replyText` assignment block (around line 387-388) and before `await updateSession(phone, "assistant", replyText)` (line 391). This way the cleaned text is both stored in Redis and sent to the user.

Do NOT modify any other handler logic. The [BUTTONS_SENT] suppression (QUAL-05) and human_takeover silence (QUAL-07 partial) are already correctly implemented.
</action>
<verify>
<automated>cd /Users/bores/el-templo && grep -n "stripMarkdownHeaders" el-templo-bot/src/webhook/handler.ts && cd el-templo-bot && npx tsc --noEmit</automated>
<manual>Function exists and TypeScript compiles cleanly</manual>
</verify>
<done>handler.ts has stripMarkdownHeaders function that converts # headers to _bold_ WhatsApp formatting. Applied to replyText before splitMessage and updateSession. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
1. `cd el-templo-bot && npx tsc --noEmit` — TypeScript compiles cleanly
2. `grep "cupos disponibles" src/ai/tools.ts` — found in checkSchedule
3. `grep "lugares" src/ai/tools.ts` — NOT found in checkSchedule spotsText
4. `grep "enseguida 🙌" src/ai/system-prompt.ts` — found
5. `grep "stripMarkdownHeaders" src/webhook/handler.ts` — found (function + usage)
</verification>

<success_criteria>

- checkSchedule output uses "cupos disponibles" and "sin cupos" (QUAL-03, QUAL-04)
- System prompt escalation phrase includes emoji (QUAL-07)
- Handler post-processes AI output to strip ### headers (QUAL-01)
- TypeScript compiles without errors
- No functional regressions in [BUTTONS_SENT] handling or human_takeover suppression
  </success_criteria>

<output>
After completion, create `.planning/phases/80-response-quality-data-fixes/80-01-SUMMARY.md`
</output>
