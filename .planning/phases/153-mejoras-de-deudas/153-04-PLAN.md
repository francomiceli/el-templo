---
phase: 153-mejoras-de-deudas
plan: 04
type: execute
wave: 3
depends_on: [153-02, 153-03]
files_modified:
  - el-templo-admin/src/composables/useTransactionsApi.ts
  - el-templo-admin/src/components/deudas/VencidosTab.vue
  - el-templo-admin/src/pages/DeudasPage.vue
autonomous: true
requirements: [DEUDA-04]
must_haves:
  truths:
    - "DeudasPage muestra un tercer tab 'Vencidos' con los socios de plan vencido sin renovar (últimos 60 días)"
    - "Cada fila del tab Vencidos muestra nombre, teléfono, plan vencido, fecha de vencimiento y días transcurridos — sin monto"
    - "El tab Vencidos ordena por vencimiento más reciente primero"
    - "El profe (coach) NO ve el tab 'Vencidos' (esconde en UI; el API ya devuelve 403)"
  artifacts:
    - path: "el-templo-admin/src/components/deudas/VencidosTab.vue"
      provides: "tabla read-only de leads de renovación, sin monto"
      contains: "expiryDate"
    - path: "el-templo-admin/src/composables/useTransactionsApi.ts"
      provides: "getExpiredMembers fetch a /admin/reports/expired-members"
      contains: "getExpiredMembers"
  key_links:
    - from: "VencidosTab.vue"
      to: "/admin/reports/expired-members"
      via: "useTransactionsApi.getExpiredMembers"
      pattern: "getExpiredMembers"
    - from: "DeudasPage.vue"
      to: "VencidosTab.vue"
      via: "tercer q-tab-panel gated por DEUDAS_DETAIL_ROLES"
      pattern: "VencidosTab"
---

<objective>
Agregar el tercer tab "Vencidos" al hub de Deudas: los socios con plan vencido sin renovar en los últimos 60 días (DEUDA-04), como leads de renovación (sin monto, D-06). Consume el endpoint `/admin/reports/expired-members` del plan 153-02, con el mismo gating de rol que "Por deuda" (D-12).

Purpose: Cerrar DEUDA-04 en la UI — "ocuparse del negocio desde una sola pantalla" (Nacho). Es la cohorte de no-renovaciones, separada de las deudas reales para no contaminar la vista de cobro (D-04).

Output: getExpiredMembers en useTransactionsApi, VencidosTab.vue (tabla read-only), y el tercer tab wireado en DeudasPage con gating.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-mejoras-de-deudas/153-CONTEXT.md
@.planning/phases/153-mejoras-de-deudas/153-PATTERNS.md

<interfaces>
<!-- Contratos disponibles tras planes 153-02 y 153-03. -->

Endpoint (plan 153-02): `GET /api/admin/reports/expired-members` → `ExpiredMembersResult { rows: ExpiredMemberRow[]; total; page; limit }`. `ExpiredMemberRow { userId, memberName, memberPhone, planName, expiryDate, daysOverdue }` (sin amount/currency).

Analog del composable: `getOutstandingBalances` en `el-templo-admin/src/composables/useTransactionsApi.ts:617-634` (loading/error, `api.get<...>('/admin/reports/outstanding-balances', { params })`, catch con extractError, registrado en el return del composable ~809). Copiar para `getExpiredMembers`.

Analog de la tabla (role-match): `DeudasReport.vue` q-table read-only + no-data slot + patrón error/log (createLogger + $q.notify). VencidosTab = misma estructura SIN monto, SIN buckets, SIN export.

Hub (plan 153-03): `DeudasPage.vue` ya tiene `q-tabs`/`q-tab-panels` con porSocio y porDeuda, `DEUDAS_TABS` (incluye `vencidos`), `DEUDAS_TAB_NAMES`, gating vía `DEUDAS_DETAIL_ROLES`, `tabFromQuery` que cae al default si el tab no es visible por rol.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: getExpiredMembers en useTransactionsApi</name>
  <read_first>
    - el-templo-admin/src/composables/useTransactionsApi.ts (getOutstandingBalances 617-634 como analog; imports de tipos ~18-19; objeto de return ~795-810)
    - el-templo-admin/src/types (donde viven OutstandingBalancesResult/Filters consumidos por el composable — definir ahí ExpiredMembersResult/Row/Filters o importarlos si el proyecto comparte tipos con la API)
  </read_first>
  <action>
    Declarar en el frontend los tipos `ExpiredMemberRow`, `ExpiredMembersFilters`, `ExpiredMembersResult` espejando la forma del backend (plan 153-02): row = `{ userId: number; memberName: string; memberPhone: string | null; planName: string; expiryDate: string; daysOverdue: number }`; filters = `{ branchId?; country?: 'AR'|'ES'; search?; page?; limit? }`; result paginado como OutstandingBalancesResult. Ubicarlos donde el composable ya toma los tipos OB (mismo archivo de types del admin).
    Agregar `getExpiredMembers(filters: ExpiredMembersFilters = {}): Promise<ExpiredMembersResult>` a `useTransactionsApi`, copiando la estructura de `getOutstandingBalances`: setear loading/error, `api.get<ExpiredMembersResult>('/admin/reports/expired-members', { params: filters })`, catch con `extractError(err, 'Error cargando vencidos')`, finally loading=false. Registrarlo en el objeto de retorno del composable (junto a getOutstandingBalances). Sin `any`; `err: unknown`.
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - grep `getExpiredMembers` en useTransactionsApi.ts aparece en la definición Y en el objeto de retorno del composable.
    - grep `/admin/reports/expired-members` en useTransactionsApi.ts lo encuentra.
    - Los tipos `ExpiredMemberRow`/`ExpiredMembersResult` existen en el frontend y NO declaran `amount`/`currency`.
    - `pnpm lint` pasa.
  </acceptance_criteria>
  <done>El composable puede traer el cohorte de vencidos desde el endpoint del plan 153-02.</done>
</task>

<task type="auto">
  <name>Task 2: VencidosTab.vue (tabla read-only sin monto)</name>
  <read_first>
    - el-templo-admin/src/components/DeudasReport.vue está borrado en plan 153-03 — usar en su lugar el nuevo `el-templo-admin/src/components/deudas/PorDeudaTab.vue` como analog de estructura (q-table read-only, no-data slot, error/log con createLogger + $q.notify, formatDate)
    - el-templo-admin/src/composables/useTransactionsApi.ts (getExpiredMembers de Task 1)
  </read_first>
  <action>
    Crear `el-templo-admin/src/components/deudas/VencidosTab.vue`: q-table read-only (sin acciones por fila — D-13, pantalla solo lectura) con columnas: Nombre (memberName), Teléfono (memberPhone), Plan vencido (planName), Fecha de vencimiento (expiryDate, formateada con formatDate), Días transcurridos (daysOverdue). SIN columna de monto (D-06 — es un lead, no una deuda), SIN buckets de antigüedad, SIN export. Orden default: vencimiento más reciente primero (menor daysOverdue). rowKey por `userId`. Slot `#no-data` con mensaje tipo "No hay socios con plan vencido sin renovar". Cargar datos vía `useTransactionsApi.getExpiredMembers`. Recibir del hub los props de scope necesarios (country/branch/search) consistentes con cómo PorDeudaTab recibe los suyos. Manejo de error: createLogger + $q.notify (patrón de PorDeudaTab), nunca console.*. Sin `any`; `err: unknown` con narrowing.
    Nota: el botón de WhatsApp por fila está DIFERIDO (CONTEXT §Deferred) — NO agregarlo.
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `el-templo-admin/src/components/deudas/VencidosTab.vue` existe; grep `expiryDate` y `daysOverdue` lo encuentran en sus columnas.
    - grep de `amount`/`currency` en VencidosTab.vue devuelve 0 (sin monto, D-06).
    - VencidosTab consume `getExpiredMembers` (grep lo confirma) y no tiene acciones/botones por fila.
    - No hay botón/link de WhatsApp (grep `whatsapp`/`wa.me` devuelve 0).
    - `pnpm lint` pasa.
  </acceptance_criteria>
  <done>VencidosTab lista los leads de renovación en una tabla read-only sin monto, ordenada por vencimiento más reciente.</done>
</task>

<task type="auto">
  <name>Task 3: Wire del tab "Vencidos" en el hub DeudasPage</name>
  <read_first>
    - el-templo-admin/src/pages/DeudasPage.vue (hub de 2 tabs del plan 153-03: q-tabs, q-tab-panels, DEUDAS_TABS, tabFromQuery, gating por DEUDAS_DETAIL_ROLES)
    - el-templo-admin/src/constants/deudas.ts (DEUDAS_TABS.vencidos ya existe en el enum)
    - el-templo-admin/src/components/deudas/VencidosTab.vue (Task 2)
  </read_first>
  <action>
    En `DeudasPage.vue`: importar `VencidosTab` y agregar un tercer `q-tab` para `DEUDAS_TABS.vencidos` (label "Vencidos") y su `q-tab-panel` con `<VencidosTab ...props />`. El tab "Vencidos" queda gated igual que "Por deuda": visible solo para roles en `DEUDAS_DETAIL_ROLES` (coach NO lo ve); reutilizar el mismo cómputo de visibilidad ya presente para porDeuda (no duplicar lógica de gating). Pasar a VencidosTab los mismos props de scope que recibe PorDeudaTab (country/branch/isOwner/search según corresponda). `tabFromQuery` ya cae al default si el coach fuerza `?tab=vencidos` (validación por visibilidad del plan 153-03) — verificar que cubre el caso de `vencidos` también.
    Nunca console.*. Sin `any`.
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - grep `VencidosTab` en DeudasPage.vue aparece en el import y en un q-tab-panel.
    - Existe un `q-tab` para `DEUDAS_TABS.vencidos` con label "Vencidos".
    - El tab "Vencidos" usa el mismo gating que "Por deuda" (grep muestra que su visibilidad depende de `DEUDAS_DETAIL_ROLES`, no de un set distinto ni siempre-visible).
    - `pnpm lint` pasa.
  </acceptance_criteria>
  <done>DeudasPage tiene los 3 tabs; "Vencidos" es visible solo para gestion/admin/owner y muestra la cohorte de no-renovaciones.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                                  |
| ---------------------- | ---------------------------------------------------------------------------- |
| UI admin → API reports | La UI esconde el tab; el acceso real lo decide el guard CAJA_ROLES de la API |

## STRIDE Threat Register

| Threat ID | Category               | Component                             | Disposition | Mitigation Plan                                                                                                                                                                                                                                                              |
| --------- | ---------------------- | ------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-153-09  | Elevation of Privilege | tab "Vencidos" visible para coach     | mitigate    | El hub esconde "Vencidos" vía `DEUDAS_DETAIL_ROLES` y `tabFromQuery` cae al default si el coach fuerza `?tab=vencidos`. La defensa real: `/admin/reports/expired-members` devuelve 403 a coach (plan 153-02, guard plugin). La UI escondiendo NO es la seguridad (D-04/149). |
| T-153-10  | Information Disclosure | teléfono del socio en la fila de lead | accept      | Dato de gestión legítimo para el rol autorizado; llega ya scoped por país desde el backend (plan 153-02).                                                                                                                                                                    |
| T-153-SC  | Tampering              | Dependencias npm                      | accept      | Este plan NO instala ni actualiza paquetes; sin superficie de supply chain.                                                                                                                                                                                                  |

</threat_model>

<verification>
- `pnpm lint` verde en el-templo-admin.
- DeudasPage tiene 3 tabs; "Vencidos" gated a gestion/admin/owner.
- VencidosTab sin monto, sin acciones, sin WhatsApp (diferido).
- El fetch pega a /admin/reports/expired-members.
</verification>

<success_criteria>

- DEUDA-04 cerrado en UI: la cohorte de no-renovaciones vive en el tab "Vencidos" de Deudas, separada de las deudas reales.
- Gating consistente con "Por deuda"; coach fuera (UI + API).
  </success_criteria>

<output>
Create `.planning/phases/153-mejoras-de-deudas/153-04-SUMMARY.md` when done
</output>
