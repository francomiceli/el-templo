---
phase: 153-mejoras-de-deudas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/reports/types.ts
  - el-templo-api/src/modules/reports/schemas.ts
  - el-templo-api/src/modules/reports/service.ts
  - el-templo-api/test/reports/outstanding-balances.test.ts
autonomous: true
requirements: [DEUDA-01, DEUDA-02, DEUDA-03]
must_haves:
  truths:
    - "Cada fila de 'Por deuda' (endpoint reports/outstanding-balances) trae la fecha de registro de la deuda (balances.createdAt)"
    - "Cada fila de deuda de cuota trae el motivo 'Cuota {plan}' y el período start–end del ciclo"
    - "Cada fila de deuda de cobro suelto trae el motivo derivado del miscReason ('Sin plan'/'Otro') y la nota libre de la transacción origen"
    - "El export Excel de outstanding-balances incluye las mismas columnas nuevas (motivo/período/fecha de registro)"
    - "El endpoint coach (/admin/coach/outstanding-balances) NO gana campos nuevos"
  artifacts:
    - path: "el-templo-api/src/modules/reports/service.ts"
      provides: "getOutstandingBalances + exportOutstandingBalances enriquecidos vía derivación compartida"
      contains: "reasonLabel"
    - path: "el-templo-api/src/modules/reports/types.ts"
      provides: "OutstandingBalanceRow con reasonLabel/periodStart/periodEnd/registeredAt/notes"
      contains: "OutstandingBalanceRow"
    - path: "el-templo-api/test/reports/outstanding-balances.test.ts"
      provides: "cobertura de motivo por origen + período + fecha de registro"
      contains: "reasonLabel"
  key_links:
    - from: "el-templo-api/src/modules/reports/service.ts"
      to: "transaction_links → financial_transactions"
      via: "join para derivar miscReason/notes de deudas debt_balance"
      pattern: "transactionLinks"
    - from: "getOutstandingBalances"
      to: "exportOutstandingBalances"
      via: "helper de derivación compartido (DRY)"
      pattern: "deriveEffectiveDateAndLabelOB"
---

<objective>
Enriquecer el endpoint detallado de deudas (`GET /api/admin/reports/outstanding-balances`) para que cada fila incluya los datos que pide la fase 153 para el tab "Por deuda": fecha desde que se registró la deuda (DEUDA-01), motivo (DEUDA-02) y plan+período asociado (DEUDA-03). Sin migración: todo se deriva de `balances`, `subscriptions` y de la transacción origen vía `transaction_links` → `financial_transactions`.

Purpose: El tab "Por deuda" del admin (plan 153-03) necesita estos campos en la respuesta para poder mostrarlos como columnas. Este plan define el contrato de datos primero.

Output: `OutstandingBalanceRow` con campos nuevos, `getOutstandingBalances` y `exportOutstandingBalances` enriquecidos vía un helper de derivación compartido, y tests que cubren cada origen de deuda.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-mejoras-de-deudas/153-CONTEXT.md
@.planning/phases/153-mejoras-de-deudas/153-PATTERNS.md

<interfaces>
<!-- Contratos existentes que el executor debe respetar. Extraídos del codebase. -->

Helper de derivación actual (`el-templo-api/src/modules/reports/service.ts:187-213`):
`deriveEffectiveDateAndLabelOB(input: { targetKind: "subscription" | "debt_balance"; targetId: number; subscriptionStartDate: string | null; planName: string | null; balanceCreatedAt: Date | string }): { effectiveDate: string; conceptLabel: string }`

Proyección actual del query de `getOutstandingBalances` (`service.ts:721-736`) trae: memberId, memberFirstName, memberLastName, memberPhone, branchId, branchName, targetKind, targetId, amount, currency, subscriptionStartDate (schema.subscriptions.startDate), planName, balanceCreatedAt (schema.balances.createdAt). NO trae `subscriptions.endDate`.

Cadena de derivación del motivo (verificada en PATTERNS §Hallazgos): `balances`(targetKind='debt_balance', targetId) → `transaction_links`(targetKind='debt_balance', targetId, índice `idx_tx_links_target`) → `financial_transactions.miscReason` (enum `misc_reason` `sin_plan`/`otro`, solo en `kind='advance_payment'`) + `financial_transactions.notes`.

El export duplica la derivación: `exportOutstandingBalances` (`service.ts:1642-1729`) también llama a `deriveEffectiveDateAndLabelOB`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender el contrato de OutstandingBalanceRow (types + schema)</name>
  <read_first>
    - el-templo-api/src/modules/reports/types.ts (definición actual de OutstandingBalanceRow / OutstandingBalancesFilters / OutstandingBalancesResult)
    - el-templo-api/src/modules/reports/schemas.ts (outstandingBalancesSchema — la forma de la respuesta que valida Fastify)
  </read_first>
  <action>
    Agregar a `OutstandingBalanceRow` (types.ts) los campos nuevos, todos derivados sin migración: `reasonLabel: string` (motivo corto estructurado, DEUDA-02), `periodStart: string | null` y `periodEnd: string | null` (rango del ciclo del plan en formato ISO `YYYY-MM-DD`, DEUDA-03; null en deudas debt_balance sin subscription), `registeredAt: string` (fecha de registro = `balances.createdAt` en formato ISO `YYYY-MM-DD`, DEUDA-01) y `notes: string | null` (nota libre de la transacción origen para el tooltip D-11). Conservar todos los campos existentes (conceptLabel, effectiveDate, ageInDays, bucket, etc.) — no romper consumidores actuales.
    Reflejar los cinco campos en `outstandingBalancesSchema` (schemas.ts) dentro de la definición de items del array de rows, respetando los tipos nullable con `nullable: true`. Mantener el schema del endpoint coach y cualquier otro schema intactos.
    No tocar `modules/coach/*` — el endpoint coach NO gana campos (D-12).
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `OutstandingBalanceRow` en types.ts declara `reasonLabel`, `periodStart`, `periodEnd`, `registeredAt`, `notes` con los tipos indicados (grep de los 5 identificadores en types.ts los encuentra).
    - `outstandingBalancesSchema` en schemas.ts declara las 5 propiedades nuevas en el item de rows (grep `reasonLabel` en schemas.ts lo encuentra).
    - `pnpm exec tsc --noEmit` en el-templo-api compila sin errores (los campos aún no se poblan en el service → tsc marcaría objetos incompletos; por eso este task se completa junto con Task 2, ver nota de ejecución).
    - Ningún archivo bajo `src/modules/coach/` fue modificado.
  </acceptance_criteria>
  <done>types.ts y schemas.ts declaran los 5 campos nuevos; el contrato de la fila de deuda está definido.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enriquecer getOutstandingBalances y exportOutstandingBalances (derivación compartida)</name>
  <read_first>
    - el-templo-api/src/modules/reports/service.ts (deriveEffectiveDateAndLabelOB líneas 187-213; getOutstandingBalances 653-793; exportOutstandingBalances 1642-1729)
    - el-templo-api/src/db/schema/financial-transactions.ts (columnas miscReason enum `sin_plan`/`otro` y notes; qué kind lleva miscReason)
    - el-templo-api/src/db/schema/transaction-links.ts (targetKind/targetId, índice idx_tx_links_target)
    - el-templo-api/src/db/schema/subscriptions.ts (startDate/endDate)
    - el-templo-api/src/db/schema/balances.ts (createdAt, targetKind, targetId)
  </read_first>
  <behavior>
    - Fila de deuda de cuota (targetKind='subscription' con subscription resuelta): reasonLabel = `Cuota {planName}` (fallback "Cuota" si planName null); periodStart = subscriptions.startDate; periodEnd = subscriptions.endDate; notes = null.
    - Fila de deuda de cobro suelto (targetKind='debt_balance' con transacción origen `advance_payment` linkeada): reasonLabel derivado del miscReason — 'sin_plan' → "Sin plan", 'otro' → "Otro"; notes = financial_transactions.notes de esa transacción; periodStart/periodEnd = null.
    - Fila debt_balance sin transacción origen resoluble (dato huérfano): reasonLabel = "Saldo a regularizar" (se conserva el wording de fallback actual); notes = null; period null.
    - Todas las filas: registeredAt = date-portion de balances.createdAt (ISO YYYY-MM-DD), independientemente del origen.
    - getOutstandingBalances y exportOutstandingBalances producen exactamente los mismos valores de reasonLabel/period/registeredAt/notes para la misma fila (una sola fuente de derivación).
  </behavior>
  <action>
    Extender `deriveEffectiveDateAndLabelOB` (o reemplazarlo por un helper de derivación más amplio, manteniendo su firma de retorno actual como superset) para que además devuelva `reasonLabel`, `periodStart`, `periodEnd`, `registeredAt` y `notes`, según el bloque `<behavior>`. Recibir como input adicional `subscriptionEndDate: string | null`, `miscReason: string | null` y `transactionNotes: string | null`. NO usar fenced code — describir la lógica en el helper.
    En el query de `getOutstandingBalances` (`service.ts:721-759`): agregar a la proyección `subscriptionEndDate: schema.subscriptions.endDate`, y un LEFT JOIN a `transaction_links` condicionado a `targetKind='debt_balance'` + `transaction_links.targetId = balances.targetId` seguido de LEFT JOIN a `financial_transactions` para traer `miscReason` y `notes` de la transacción origen (la de `kind='advance_payment'` — filtrar por ese kind en la condición del join para no traer pagos posteriores). Si un debt_balance tuviera múltiples links, quedarse con la transacción origen determinística (la más antigua por id/created); documentar la elección en un comentario. Mapear los campos nuevos en el objeto de la fila (`service.ts:761-788`) usando el helper.
    Replicar la MISMA proyección y mapeo en `exportOutstandingBalances` (`service.ts:1642-1729`) para que el Excel gane columnas "Motivo", "Período" (formateado `dd/mm–dd/mm` o "—" si null) y "Fecha de registro" — DRY vía el helper compartido, sin duplicar la lógica de derivación.
    Respetar el scoping y filtros existentes (branchId/country/currency/search) — no alterarlos. Nunca usar `console.*` (usar el logger inyectado). Sin `any`.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep `subscriptionEndDate` en service.ts aparece en la proyección de getOutstandingBalances Y de exportOutstandingBalances.
    - grep `transactionLinks` (o `transaction_links`) aparece en el query de getOutstandingBalances.
    - El helper de derivación devuelve `reasonLabel`, `periodStart`, `periodEnd`, `registeredAt`, `notes` y es invocado por ambos métodos (grep del nombre del helper devuelve ≥2 call sites fuera de su definición).
    - Las filas mapeadas en getOutstandingBalances y exportOutstandingBalances setean los 5 campos nuevos (no quedan objetos con campos faltantes → tsc lo garantiza junto al contrato del Task 1).
    - `pnpm exec tsc --noEmit` compila sin errores.
    - Ningún archivo bajo `src/modules/coach/` fue modificado.
  </acceptance_criteria>
  <done>getOutstandingBalances y exportOutstandingBalances devuelven motivo, período y fecha de registro por fila, derivados de datos existentes, sin migración y sin tocar el endpoint coach.</done>
</task>

<task type="auto">
  <name>Task 3: Tests de integración del motivo/período/fecha en outstanding-balances</name>
  <read_first>
    - el-templo-api/test/reports/outstanding-balances.test.ts (catálogo existente: fixtures, seedFixtures, seedRolesAndPlans, matriz RBAC, casos BUCKETS/SORT/FILTER/CROSS-COUNTRY, helper dateOffset)
    - el-templo-api/test/helpers.ts (createTestApp, cleanAllTestData, createStaffUser, getAuthToken, registerUser)
    - el-templo-api/src/db/schema/financial-transactions.ts y transaction-links.ts (para seedear la transacción origen con miscReason + link a debt_balance)
  </read_first>
  <action>
    Extender `outstanding-balances.test.ts` (NO crear archivo nuevo) con casos que cubran los tres orígenes de deuda, reutilizando el patrón de setup/re-seed y la matriz RBAC ya presentes:
    1. Deuda de cuota (subscription con balance amount>0): la fila devuelve `reasonLabel` que empieza con "Cuota ", `periodStart` = startDate de la sub y `periodEnd` = endDate de la sub, y `registeredAt` presente en formato YYYY-MM-DD.
    2. Deuda de cobro suelto con transacción origen `advance_payment` + `miscReason='sin_plan'` + `notes` no vacío, linkeada al debt_balance vía transaction_links: la fila devuelve `reasonLabel === "Sin plan"`, `notes` igual al texto seedeado, `periodStart`/`periodEnd` null.
    3. Ídem con `miscReason='otro'`: `reasonLabel === "Otro"`.
    4. registeredAt siempre presente y con la fecha de creación del balance (no la effectiveDate del ciclo).
    Confirmar que el caso coach-403 existente sigue verde (no se modifica el guard).
    Sin `any`; usar `err: unknown` con narrowing donde aplique.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit</automated>
    <human-check>Los tests corren en CI al pushear (vitest run) — NO ejecutar el suite localmente (preferencia del usuario). Verificar en CI que el bloque nuevo de outstanding-balances pasa.</human-check>
  </verify>
  <acceptance_criteria>
    - grep `reasonLabel` en outstanding-balances.test.ts aparece con aserciones `toBe("Sin plan")` y `toBe("Otro")`.
    - Existe al menos un caso que seedea `financial_transactions.miscReason` + `transaction_links` a un `debt_balance` y asierta el `notes` derivado.
    - Existe al menos un caso que asierta `periodStart`/`periodEnd` = start/end de la subscription para una deuda de cuota.
    - `pnpm exec tsc --noEmit` compila el archivo de test sin errores.
  </acceptance_criteria>
  <done>outstanding-balances.test.ts cubre motivo por cada origen (cuota/sin_plan/otro), período de cuota y fecha de registro; typecheck verde.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                          | Description                                                            |
| ----------------------------------------------------------------- | ---------------------------------------------------------------------- |
| cliente admin → API reports                                       | El rol viaja en el JWT; el guard plugin-level de reports decide acceso |
| balances/subscriptions → transaction_links/financial_transactions | Se cruza data financiera (notes libres, miscReason) hacia la respuesta |

## STRIDE Threat Register

| Threat ID | Category               | Component                                   | Disposition | Mitigation Plan                                                                                                                                                                                                |
| --------- | ---------------------- | ------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-153-01  | Elevation of Privilege | GET /api/admin/reports/outstanding-balances | mitigate    | El hook `onRequest` del plugin reports exige `CAJA_ROLES` (gestion/admin/owner) — coach recibe 403. No se agrega preHandler que baje el guard. Cubierto por el caso coach-403 existente en el test.            |
| T-153-02  | Information Disclosure | notes/miscReason en la fila enriquecida     | mitigate    | La derivación respeta el scoping por country/branch existente (whereClause sin cambios); la nota solo se expone dentro del scope del rol. No se agrega el campo al endpoint coach (proyección mínima intacta). |
| T-153-03  | Tampering              | JOIN a transaction_links por targetId       | accept      | Los ids son server-side (no vienen del cliente); el filtro `kind='advance_payment'` acota la transacción origen. Riesgo bajo.                                                                                  |
| T-153-SC  | Tampering              | Dependencias npm                            | accept      | Este plan NO instala ni actualiza paquetes; sin superficie de supply chain.                                                                                                                                    |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit` verde en el-templo-api.
- El endpoint coach de outstanding-balances no cambió su forma (diff de coach/* vacío).
- El export Excel y el JSON del endpoint reports comparten la misma derivación (un único helper).
- Tests de outstanding-balances extendidos; corren en CI.
</verification>

<success_criteria>

- DEUDA-01/02/03 cubiertos a nivel dato: cada fila de reports/outstanding-balances trae reasonLabel, periodStart, periodEnd, registeredAt y notes.
- Sin migración, sin cambios al endpoint coach, DRY entre JSON y export.
  </success_criteria>

<output>
Create `.planning/phases/153-mejoras-de-deudas/153-01-SUMMARY.md` when done
</output>
