---
phase: 153-mejoras-de-deudas
plan: 02
type: execute
wave: 2
depends_on: [153-01]
files_modified:
  - el-templo-api/src/modules/reports/types.ts
  - el-templo-api/src/modules/reports/schemas.ts
  - el-templo-api/src/modules/reports/service.ts
  - el-templo-api/src/modules/reports/routes.ts
  - el-templo-api/test/reports/expired-members.test.ts
autonomous: true
requirements: [DEUDA-04]
must_haves:
  truths:
    - "Existe GET /api/admin/reports/expired-members que devuelve los socios con plan vencido en los últimos 60 días sin plan activo hoy"
    - "El cohorte excluye a los socios que renovaron (tienen sub activa) y a las subs con ventana invertida (end_date < start_date)"
    - "Cada fila trae nombre, teléfono, plan vencido, fecha de vencimiento y días transcurridos — sin monto"
    - "coach y recepcion reciben 403 en el endpoint nuevo (guard plugin-level de reports)"
    - "El cohorte respeta el scoping por rol (gestion/admin → country, owner → todos los países)"
  artifacts:
    - path: "el-templo-api/src/modules/reports/service.ts"
      provides: "getExpiredMembers con ventana 60d + exclusión de datos sucios + dedup por miembro"
      contains: "getExpiredMembers"
    - path: "el-templo-api/src/modules/reports/routes.ts"
      provides: "ruta GET /expired-members dentro del plugin reports (hereda guard CAJA_ROLES)"
      contains: "expired-members"
    - path: "el-templo-api/test/reports/expired-members.test.ts"
      provides: "cobertura ventana/exclusión/RBAC/scope"
      contains: "expired-members"
  key_links:
    - from: "getExpiredMembers"
      to: "activeMemberExists"
      via: "negación del predicado activo canónico (vencido sin renovar)"
      pattern: "activeMemberExists"
    - from: "reports/routes.ts onRequest hook"
      to: "CAJA_ROLES"
      via: "guard plugin-level que devuelve 403 a coach/recepcion"
      pattern: "CAJA_ROLES"
---

<objective>
Agregar el tab "Vencidos" a nivel dato: un endpoint nuevo `GET /api/admin/reports/expired-members` que lista a los socios cuyo plan venció en los últimos 60 días y no renovaron (DEUDA-04). No son deudas (no hay monto) — son leads de renovación. Reusa el predicado "vencido sin renovar" de analytics (fase 121) adaptado a ventana de 60 días, con dedup por miembro y exclusión del dato sucio histórico.

Purpose: El tab "Vencidos" del admin (plan 153-04) necesita esta fuente de datos. Vive en `modules/reports/` para heredar gratis el guard `CAJA_ROLES` (coach → 403, D-12) sin tocar el endpoint coach.

Output: `getExpiredMembers` en reports service, ruta `/expired-members`, tipos/schema paginados, y tests de ventana/exclusión/RBAC/scope.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-mejoras-de-deudas/153-CONTEXT.md
@.planning/phases/153-mejoras-de-deudas/153-PATTERNS.md

<interfaces>
<!-- Contratos y patrones existentes que el executor debe reusar. -->

Predicado overdue de analytics (`el-templo-api/src/modules/analytics/service.ts:609-669`): condiciones `endDate < CURDATE()`, `endDate >= DATE_SUB(CURDATE(), INTERVAL 30 DAY)`, `NOT activeMemberExists(subscriptions.userId)`, más `scope.conditions`. Selecciona userId, firstName, lastName, planName, phone, `daysOverdue = DATEDIFF(CURDATE(), endDate)`. Dedup por usuario con Map conservando el vencimiento más reciente (menor daysOverdue).

Helper canónico (`el-templo-api/src/modules/shared/active-member.ts:27-35`): `activeMemberExists(userIdColumn): SQL` — EXISTS de sub con status active/paused y ventana vigente. NUNCA leer users.status directo.

Guard plugin-level (`el-templo-api/src/modules/reports/routes.ts:53-62`): hook `onRequest` que autentica, rechaza con 403 si el rol no está en `CAJA_ROLES` (gestion/admin/owner), y luego `attachCountryScope(request, fastify.db)`. Toda ruta nueva dentro de `reportsRoutes` hereda esto.

Resolución owner-aware de country (`reports/routes.ts:264-271`, patrón de OB): si `request.scope.isOwner` → usar `request.query.country` (owner sin country ve todos los países); si no → `request.scope.country ?? undefined`.

Prefijo registrado (`app.ts:199-201`): `/api/admin/reports`.
Manejo de errores: `handleServiceError(err, reply, request.log, "<contexto>")` de `../shared/error-handler`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contrato del cohorte de vencidos (types + schema)</name>
  <read_first>
    - el-templo-api/src/modules/reports/types.ts (patrón OutstandingBalanceRow / OutstandingBalancesFilters / OutstandingBalancesResult)
    - el-templo-api/src/modules/reports/schemas.ts (outstandingBalancesSchema como molde paginado)
  </read_first>
  <action>
    Definir en types.ts el trío `ExpiredMemberRow`, `ExpiredMembersFilters`, `ExpiredMembersResult` siguiendo la forma de los tipos OB. `ExpiredMemberRow`: `userId: number`, `memberName: string`, `memberPhone: string | null`, `planName: string`, `expiryDate: string` (ISO YYYY-MM-DD, la fecha de vencimiento = subscriptions.endDate), `daysOverdue: number`. Sin monto (D-06). `ExpiredMembersFilters`: `branchId?`, `country?: "AR" | "ES"`, `search?`, `page?`, `limit?` (mismo shape que OB filters). `ExpiredMembersResult`: `{ rows: ExpiredMemberRow[]; total: number; page: number; limit: number }` (o el shape paginado que use OB result — replicar exacto).
    Definir en schemas.ts `expiredMembersSchema` (querystring + response) espejando `outstandingBalancesSchema` pero con la fila sin `amount`/`currency`/`bucket`.
    NO tocar el endpoint coach ni sus tipos.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep `ExpiredMemberRow`, `ExpiredMembersFilters`, `ExpiredMembersResult` en types.ts los encuentra.
    - `ExpiredMemberRow` NO declara `amount` ni `currency` (grep confirma ausencia dentro de su bloque).
    - grep `expiredMembersSchema` en schemas.ts lo encuentra.
    - `pnpm exec tsc --noEmit` compila (los tipos aún no tienen implementación → válido; Task 2 los consume).
  </acceptance_criteria>
  <done>El contrato del cohorte de vencidos está definido, sin monto, con shape paginado idéntico al de OB.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: getExpiredMembers (service) + ruta GET /expired-members</name>
  <read_first>
    - el-templo-api/src/modules/analytics/service.ts (líneas 609-669: overdueConditions, query, dedup overdueByUser)
    - el-templo-api/src/modules/shared/active-member.ts (activeMemberExists)
    - el-templo-api/src/modules/reports/service.ts (estructura de getOutstandingBalances: conds SQL[], joins, scope owner-aware)
    - el-templo-api/src/modules/reports/routes.ts (ruta /outstanding-balances 247-293 como molde: schema, preHandler requireBranchAccess optional, resolución owner-aware de country, handleServiceError; hook onRequest 53-62)
    - el-templo-api/src/modules/shared/member-search.ts (buildMemberNameSearchCondition)
    - el-templo-api/src/db/schema/subscriptions.ts (startDate/endDate/status)
  </read_first>
  <behavior>
    - Un socio cuya sub venció hace 59 días y no tiene sub activa hoy → aparece en el cohorte con daysOverdue=59.
    - Un socio cuya sub venció hace 61 días → NO aparece (fuera de ventana 60d, D-05).
    - Un socio que venció hace 30 días pero renovó (tiene sub activa) → NO aparece (negación de activeMemberExists).
    - Una sub con end_date < start_date (dato sucio histórico, ~4260 filas cancelled) → NO aparece aunque el end_date caiga en la ventana.
    - Un socio con varias subs vencidas en la ventana → aparece UNA vez, con el vencimiento más reciente (menor daysOverdue).
    - gestion/admin ven solo su country; owner sin ?country ve todos los países; owner con ?country filtra.
    - La búsqueda por nombre (search) filtra por nombre del miembro.
  </behavior>
  <action>
    Implementar `getExpiredMembers(filters: ExpiredMembersFilters, scope: { isOwner: boolean }): Promise<ExpiredMembersResult>` en `ReportsService`. Construir `conds: SQL[]` reusando el predicado de analytics adaptado: `subscriptions.endDate < CURDATE()`, `subscriptions.endDate >= DATE_SUB(CURDATE(), INTERVAL 60 DAY)` (60, NO 30 — D-05), `NOT activeMemberExists(schema.subscriptions.userId)` (importar el helper canónico, nunca leer users.status), y la exclusión del dato sucio: `subscriptions.endDate >= subscriptions.startDate` (documentar en comentario que descarta ventanas invertidas cancelled). Aplicar scoping por country: si no owner y country definido → `eq(branches.country, country)` (o el equivalente vía users.country que use el patrón de reports); replicar la resolución owner-aware de la ruta. Aplicar `buildMemberNameSearchCondition` para `search`. INNER JOIN a users, branches y subscription_plans como en analytics. Seleccionar userId, firstName/lastName, phone, planName, `expiryDate = subscriptions.endDate`, `daysOverdue = DATEDIFF(CURDATE(), subscriptions.endDate)`. Dedup por usuario con el patrón Map (`expiredByUser`) conservando el menor daysOverdue. Componer memberName = `${firstName} ${lastName}`.trim(). Ordenar por vencimiento más reciente primero (daysOverdue ASC). Devolver el shape paginado con total. Sin `any`; logger inyectado, nunca console.
    Agregar la ruta `GET /expired-members` dentro de `reportsRoutes` (routes.ts), copiando la estructura de `/outstanding-balances`: `schema: expiredMembersSchema`, `preHandler: [requireBranchAccess({ from: "query.branchId", optional: true })]`, resolución owner-aware de country, `return await reportsService.getExpiredMembers(filters, { isOwner: request.scope.isOwner })`, y `handleServiceError(err, reply, request.log, "get expired members report")` en el catch. NO declarar guard de rol por ruta — lo hereda del hook onRequest del plugin.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep `getExpiredMembers` en service.ts encuentra la definición del método.
    - grep `INTERVAL 60 DAY` en service.ts aparece dentro de getExpiredMembers (ventana 60d, no 30).
    - grep `activeMemberExists` en service.ts aparece dentro de getExpiredMembers (predicado canónico, no lectura de users.status).
    - Existe una condición que compara `endDate >= startDate` (exclusión de ventana invertida) dentro de getExpiredMembers.
    - grep `expired-members` en routes.ts encuentra la ruta; el handler NO declara un preHandler/guard de rol propio (solo requireBranchAccess).
    - `pnpm exec tsc --noEmit` compila sin errores.
    - Ningún archivo bajo `src/modules/coach/` fue modificado.
  </acceptance_criteria>
  <done>El endpoint /expired-members devuelve el cohorte 60d sin renovar, deduplicado, scoped y sin monto; el 403 de coach lo da el guard plugin-level heredado.</done>
</task>

<task type="auto">
  <name>Task 3: Tests de integración expired-members</name>
  <read_first>
    - el-templo-api/test/reports/outstanding-balances.test.ts (analog canónico: seedFixtures, seedRolesAndPlans, re-seed en beforeEach, matriz RBAC coach/recepcion 403, helper dateOffset, casos CROSS-COUNTRY)
    - el-templo-api/test/helpers.ts (createTestApp, cleanAllTestData, createStaffUser, getAuthToken, registerUser)
    - el-templo-api/src/db/schema/subscriptions.ts (para seedear subs con endDate/startDate/status controlados)
  </read_first>
  <action>
    Crear `el-templo-api/test/reports/expired-members.test.ts` copiando el andamiaje del test de OB (imports, REPORTS_URL, dateOffset, beforeAll/beforeEach con re-seed). Casos mínimos que exige el CONTEXT §Established Patterns:
    1. Ventana: socio con endDate `dateOffset(-59)` aparece; socio con `dateOffset(-61)` NO aparece.
    2. Renovación: socio con sub vencida en ventana PERO con sub activa hoy → NO aparece.
    3. Dato sucio: sub con `endDate < startDate` (endDate en ventana, status cancelled) → NO aparece.
    4. Dedup: socio con dos subs vencidas en ventana → una sola fila, con el menor daysOverdue.
    5. Fila sin monto: la fila devuelta NO tiene `amount` ni `currency`; sí tiene planName, expiryDate y daysOverdue.
    6. RBAC: coach recibe 403 y recepcion recibe 403 en `/expired-members` (patrón de la matriz RBAC del test OB).
    7. Cross-country/scope: gestion de AR no ve al vencido de ES; owner ve ambos.
    8. Convivencia (D-07): un socio con deuda (balance amount>0) Y plan vencido aparece en /expired-members (y seguiría en /outstanding-balances) — sin lógica de exclusión.
    Sin `any`; `err: unknown` con narrowing.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit</automated>
    <human-check>Los tests corren en CI al pushear (vitest run) — NO ejecutar el suite localmente. Verificar que expired-members pasa en CI.</human-check>
  </verify>
  <acceptance_criteria>
    - El archivo `test/reports/expired-members.test.ts` existe y usa `dateOffset(-59)` y `dateOffset(-61)` en casos de ventana.
    - Existe un caso que asierta 403 para coach y 403 para recepcion en `/expired-members`.
    - Existe un caso que seedea `endDate < startDate` (status cancelled) y asierta que NO aparece.
    - Existe un caso que asierta que la fila no expone `amount`/`currency`.
    - Existe un caso cross-country que asierta scope por rol.
    - `pnpm exec tsc --noEmit` compila el test sin errores.
  </acceptance_criteria>
  <done>expired-members.test.ts cubre ventana 60d, exclusión de dato sucio, renovación, dedup, sin monto, RBAC 403 y scope; typecheck verde.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                        |
| ------------------------------------ | ------------------------------------------------------------------ |
| cliente admin → API reports          | El rol viaja en el JWT; el hook onRequest del plugin decide acceso |
| scope por país → cohorte de vencidos | El country/branch del rol acota qué socios se listan               |

## STRIDE Threat Register

| Threat ID | Category               | Component                              | Disposition | Mitigation Plan                                                                                                                                                                        |
| --------- | ---------------------- | -------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-153-04  | Elevation of Privilege | GET /api/admin/reports/expired-members | mitigate    | El endpoint vive dentro de `reportsRoutes` → hereda el hook onRequest que exige `CAJA_ROLES`; coach/recepcion → 403. No se declara preHandler que baje el guard. Test RBAC lo asegura. |
| T-153-05  | Information Disclosure | cohorte de vencidos cross-country      | mitigate    | `attachCountryScope` + resolución owner-aware de country replicada del OB; gestion/admin acotados a su country, owner explícito. Test cross-country lo asegura.                        |
| T-153-06  | Information Disclosure | teléfono del socio en la fila (lead)   | accept      | El teléfono es dato de gestión legítimo para el rol autorizado (mismo dato ya expuesto en OB/analytics); dentro del scope del rol.                                                     |
| T-153-SC  | Tampering              | Dependencias npm                       | accept      | Este plan NO instala ni actualiza paquetes; sin superficie de supply chain.                                                                                                            |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit` verde en el-templo-api.
- La ruta /expired-members hereda el guard CAJA_ROLES (sin guard propio).
- El endpoint coach quedó intacto (diff de coach/* vacío).
- Tests expired-members creados; corren en CI.
</verification>

<success_criteria>

- DEUDA-04 cubierto a nivel dato: /expired-members lista el cohorte 60d sin renovar, deduplicado, scoped, sin monto, con 403 para coach/recepcion.
- Sin migración; reusa activeMemberExists y el patrón overdue de analytics.
  </success_criteria>

<output>
Create `.planning/phases/153-mejoras-de-deudas/153-02-SUMMARY.md` when done
</output>
