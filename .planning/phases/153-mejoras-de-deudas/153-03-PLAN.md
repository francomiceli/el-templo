---
phase: 153-mejoras-de-deudas
plan: 03
type: execute
wave: 2
depends_on: [153-01]
files_modified:
  - el-templo-admin/src/constants/deudas.ts
  - el-templo-admin/src/config/templo-config.ts
  - el-templo-admin/src/pages/DeudasPage.vue
  - el-templo-admin/src/components/deudas/PorSocioTab.vue
  - el-templo-admin/src/components/deudas/PorDeudaTab.vue
  - el-templo-admin/src/components/DeudasReport.vue
  - el-templo-admin/src/pages/ReportesPage.vue
autonomous: true
requirements: [DEUDA-01, DEUDA-02, DEUDA-03]
must_haves:
  truths:
    - "DeudasPage muestra tabs 'Por socio' y 'Por deuda' con default en 'Por socio' (D-03)"
    - "El tab 'Por socio' es la tabla agregada actual sin cambios de columnas, pegándole al endpoint coach"
    - "El tab 'Por deuda' muestra una fila por deuda con columnas Motivo, Fecha de registro y la nota en tooltip, además de las columnas actuales"
    - "El reporte de Deudas ya no aparece en ReportesPage (se mudó); ?tab=deudas en Reportes no rompe"
    - "El profe (coach) NO ve el tab 'Por deuda' (esconde en UI; el guard real vive en la API)"
  artifacts:
    - path: "el-templo-admin/src/constants/deudas.ts"
      provides: "DEUDAS_TABS / DEUDAS_DEFAULT_TAB / DEUDAS_TAB_NAMES"
      contains: "DEUDAS_DEFAULT_TAB"
    - path: "el-templo-admin/src/pages/DeudasPage.vue"
      provides: "hub de tabs con ?tab sync y gating por rol"
      contains: "q-tabs"
    - path: "el-templo-admin/src/components/deudas/PorDeudaTab.vue"
      provides: "reporte detallado por deuda con columnas Motivo/Fecha de registro/nota"
      contains: "reasonLabel"
  key_links:
    - from: "DeudasPage.vue"
      to: "PorSocioTab.vue / PorDeudaTab.vue"
      via: "q-tab-panels + componentes hijos"
      pattern: "PorDeudaTab"
    - from: "PorDeudaTab.vue"
      to: "/admin/reports/outstanding-balances"
      via: "useTransactionsApi.getOutstandingBalances"
      pattern: "getOutstandingBalances"
    - from: "DeudasPage.vue"
      to: "templo-config DEUDAS_DETAIL_ROLES"
      via: "cómputo de visibilidad del tab por rol"
      pattern: "DEUDAS_DETAIL_ROLES"
---

<objective>
Convertir `DeudasPage.vue` en un hub de tabs (patrón CajaPage de fase 152) con "Por socio" (tabla agregada actual, sin cambios) y "Por deuda" (reporte detallado que se muda desde Reportes, ahora con Motivo/Fecha de registro/nota — DEUDA-01/02/03). Sacar el reporte de Deudas de ReportesPage sin dejar links rotos. Gating por rol: el profe solo ve "Por socio" (D-12; el guard real ya vive en la API, plan 153-01).

Purpose: Una sola casa para Deudas con las dos listas que pidió el usuario (agregada + por deuda), y el detalle enriquecido consumiendo los campos que agregó el plan 153-01.

Output: constants/deudas.ts, DeudasPage hub con 2 tabs funcionales, PorSocioTab (extracción verbatim), PorDeudaTab (mudanza de DeudasReport + columnas nuevas), ReportesPage sin el reporte.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-mejoras-de-deudas/153-CONTEXT.md
@.planning/phases/153-mejoras-de-deudas/153-PATTERNS.md

<interfaces>
<!-- Analogs y contratos. Ver PATTERNS.md para excerpts completos. -->

Analog del hub: `el-templo-admin/src/pages/CajaPage.vue` (q-tabs + q-tab-panels keep-alive, tabFromQuery/activeTab/watch→router.replace, gating con useAuthStore) y `el-templo-admin/src/constants/caja.ts` (CAJA_TABS/CAJA_DEFAULT_TAB/CAJA_TAB_NAMES).

Cuerpo actual a extraer a PorSocioTab: `el-templo-admin/src/pages/DeudasPage.vue` (113 líneas) — usa `useCoachApi.getOutstandingBalances` (endpoint coach, proyección mínima), rowKey `${memberId}-${currency}`. Se mueve verbatim, SIN cambios de columnas (D-01).

Reporte a mudar a PorDeudaTab: `el-templo-admin/src/components/DeudasReport.vue` (364 líneas). Props actuales: `{ branchOptions: BranchOption[]; displayCurrency: 'ARS'|'EUR'; countryScope: 'AR'|'ES'|undefined; isOwner: boolean }`. Consume `useTransactionsApi.getOutstandingBalances` → `/admin/reports/outstanding-balances` (línea 282). Columnas en `const columns` (188-252), incluye field `conceptLabel`, `effectiveDate`, `ageInDays`. Export por Blob (339-363), "Cargar más", buckets — se conservan.

Campos NUEVOS en la respuesta (plan 153-01, ya disponibles): `reasonLabel`, `periodStart`, `periodEnd`, `registeredAt`, `notes` en cada fila de OutstandingBalanceRow (types en el-templo-admin/src/types o el que consuma useTransactionsApi).

templo-config roles (`el-templo-admin/src/config/templo-config.ts`): `DEUDAS_ROLES = ['coach','gestion','admin','owner']` (línea 51), `REPORTES_ROLES = ['gestion','admin','owner']` (línea 56), `CAJA_ROLES`/`AdminRole` de `src/types/admin`. Regla del header: la nav solo ESCONDE, la seguridad vive en la API.

ReportesPage 5 puntos de remoción (PATTERNS §ReportesPage): línea 63 (q-tab deudas), 520-530 (q-tab-panel + DeudasReport), 755 (import), 858-861 (deudasBranchOptions), 926 ('deudas' en VALID_TABS). `?tab=deudas` degrada solo a 'accesos' vía VALID_TABS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: constants/deudas.ts + role set DEUDAS_DETAIL_ROLES</name>
  <read_first>
    - el-templo-admin/src/constants/caja.ts (analog exacto de la config de tabs)
    - el-templo-admin/src/config/templo-config.ts (DEUDAS_ROLES línea 51, REPORTES_ROLES línea 56, AdminRole, header sobre imports y "solo esconde")
  </read_first>
  <action>
    Crear `el-templo-admin/src/constants/deudas.ts` copiando el patrón de caja.ts: `DEUDAS_TABS = { porSocio: 'porSocio', porDeuda: 'porDeuda', vencidos: 'vencidos' } as const`, `type DeudasTab`, `DEUDAS_DEFAULT_TAB = DEUDAS_TABS.porSocio` (D-03), y `DEUDAS_TAB_NAMES: readonly DeudasTab[]` en el orden porSocio, porDeuda, vencidos. Incluir `vencidos` en el enum ahora (el tab lo wirea el plan 153-04) para que el contrato de `?tab=` sea estable. Los strings son el contrato de la query `?tab=`.
    Agregar en templo-config.ts un role set nuevo `DEUDAS_DETAIL_ROLES: AdminRole[] = ['gestion', 'admin', 'owner']` con docstring que diga que espeja `CAJA_ROLES` de la API (guard del plugin reports) y que aplica a los tabs "Por deuda" y "Vencidos" (D-12); el profe (coach) queda fuera. Conservar `DEUDAS_ROLES` sin cambios (sigue gobernando el acceso a la página Deudas completa, coach incluido).
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - grep `DEUDAS_DEFAULT_TAB` en constants/deudas.ts y su valor es `DEUDAS_TABS.porSocio`.
    - `DEUDAS_TAB_NAMES` incluye `porSocio`, `porDeuda`, `vencidos`.
    - grep `DEUDAS_DETAIL_ROLES` en templo-config.ts y su valor es `['gestion', 'admin', 'owner']` (sin coach).
    - `DEUDAS_ROLES` sigue siendo `['coach', 'gestion', 'admin', 'owner']` (sin cambios).
    - `pnpm lint` pasa sin errores en los archivos tocados.
  </acceptance_criteria>
  <done>Existe el contrato de tabs de Deudas y el role set que excluye al coach de los tabs de detalle.</done>
</task>

<task type="auto">
  <name>Task 2: DeudasPage hub (2 tabs) + PorSocioTab (extracción verbatim)</name>
  <read_first>
    - el-templo-admin/src/pages/DeudasPage.vue (cuerpo actual completo — se extrae a PorSocioTab)
    - el-templo-admin/src/pages/CajaPage.vue (patrón hub: q-tabs, q-tab-panels keep-alive, tabFromQuery/activeTab/watch, useAuthStore para gating)
    - el-templo-admin/src/constants/deudas.ts (creado en Task 1)
    - el-templo-admin/src/config/templo-config.ts (DEUDAS_DETAIL_ROLES)
  </read_first>
  <action>
    Crear `el-templo-admin/src/components/deudas/PorSocioTab.vue` moviendo VERBATIM el cuerpo actual de DeudasPage.vue (tabla agregada por socio+moneda vía `useCoachApi.getOutstandingBalances`, rowKey `${memberId}-${currency}`, search, loading/error). SIN cambios de columnas (D-01) — es la vista de cobro rápido en la puerta. Precedente: fase 152 "Transacciones — verbatim migration".
    Reescribir `DeudasPage.vue` como hub siguiendo CajaPage: `q-tabs` v-model activeTab con `q-tab` para `DEUDAS_TABS.porSocio` (label "Por socio") y `DEUDAS_TABS.porDeuda` (label "Por deuda"); `q-tab-panels` keep-alive con `<PorSocioTab />` y `<PorDeudaTab .../>`. Implementar `tabFromQuery()` que valide contra `DEUDAS_TAB_NAMES` Y contra la visibilidad por rol: si el tab pedido no es visible para el rol (coach pidiendo porDeuda), caer a `DEUDAS_DEFAULT_TAB`. Sincronizar activeTab ↔ `?tab=` con watch→`router.replace` (sin polución de history). Computar la visibilidad de los tabs de detalle con el rol de `useAuthStore` contra `DEUDAS_DETAIL_ROLES` (esconder "Por deuda" para coach; la seguridad real ya está en la API). Proveer a PorDeudaTab los props que hoy le da ReportesPage: `branchOptions`, `displayCurrency`, `countryScope`, `isOwner` — replicar cómo los arma ReportesPage o el patrón country-selector de CajaPage.
    Este task deja el hub compilando con DOS tabs; el tab "Vencidos" lo agrega el plan 153-04. NO referenciar VencidosTab todavía (aún no existe).
    Nunca console.*; usar createLogger. Sin `any`.
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `el-templo-admin/src/components/deudas/PorSocioTab.vue` existe y usa `useCoachApi` (grep lo confirma) — NO useTransactionsApi.
    - DeudasPage.vue contiene `q-tabs` y renderiza `PorSocioTab` y `PorDeudaTab` en `q-tab-panels`.
    - DeudasPage.vue referencia `DEUDAS_DETAIL_ROLES` para computar visibilidad y `DEUDAS_DEFAULT_TAB` como fallback.
    - DeudasPage.vue NO referencia `VencidosTab` (aún no existe; grep no lo encuentra).
    - `tabFromQuery` cae al default cuando el tab pedido no es visible para el rol (no solo cuando no existe).
    - `pnpm lint` pasa.
  </acceptance_criteria>
  <done>DeudasPage es un hub de 2 tabs con default "Por socio", ?tab sync y gating; PorSocioTab es el cuerpo actual movido sin cambios.</done>
</task>

<task type="auto">
  <name>Task 3: PorDeudaTab (mudanza de DeudasReport + columnas nuevas) + remoción en ReportesPage</name>
  <read_first>
    - el-templo-admin/src/components/DeudasReport.vue (364 líneas: props, const columns 188-252, fetch 278-313, export Blob 339-363, slot #body-cell-fechaDevengo)
    - el-templo-admin/src/pages/ReportesPage.vue (líneas 63, 520-530, 755, 858-861, 926 — 5 puntos de remoción; VALID_TABS y degradación a 'accesos')
    - el-templo-admin/src/composables/useTransactionsApi.ts (getOutstandingBalances → /admin/reports/outstanding-balances; tipos OutstandingBalancesResult/Row consumidos)
  </read_first>
  <action>
    Crear `el-templo-admin/src/components/deudas/PorDeudaTab.vue` mudando el contenido de `DeudasReport.vue` (props, columnas, aging buckets, export Excel, "Cargar más", fetch a `useTransactionsApi.getOutstandingBalances` → `/admin/reports/outstanding-balances`). Agregar a `const columns` (patrón del array existente) DOS columnas nuevas: "Motivo" (field `reasonLabel`, DEUDA-02) y "Fecha de registro" (field `registeredAt`, formateada con `formatDate`, DEUDA-01). Para DEUDA-03, el motivo/período ya viene resuelto por el backend: mostrar el período cuando `periodStart`/`periodEnd` existen (columna o subtítulo bajo el motivo, a discreción, formato `dd/mm–dd/mm`). Poner la nota libre (`notes`) en un tooltip/`q-tooltip` sobre la celda de Motivo (D-11), no como columna. Conservar la columna de antigüedad/devengo existente tal cual (D-10). Usar el patrón de slot `#body-cell-...` ya presente para el formato de celdas.
    Borrar `el-templo-admin/src/components/DeudasReport.vue` (se mudó, no se comparte).
    En `ReportesPage.vue` aplicar los 5 puntos de remoción: quitar el `q-tab name="deudas"` (línea 63), el `q-tab-panel name="deudas"` con `<DeudasReport .../>` (520-530), el import (755), el computed `deudasBranchOptions` (858-861 — solo si no lo usa nada más; verificar con grep antes de borrar) y `'deudas'` de `VALID_TABS` (926). La degradación de `?tab=deudas` a `'accesos'` ya está resuelta por la validación de VALID_TABS; opcionalmente (discreción) interceptar `tab === 'deudas'` para `router.replace('/deudas?tab=porDeuda')`. No dejar imports ni computeds huérfanos (lint lo detecta).
    Nunca console.*; createLogger + $q.notify en errores (patrón DeudasReport 306-312). Sin `any`.
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `el-templo-admin/src/components/deudas/PorDeudaTab.vue` existe; grep `reasonLabel` y `registeredAt` lo encuentran en su `columns`.
    - grep `notes` en PorDeudaTab.vue aparece asociado a un `q-tooltip` (nota en tooltip, no columna).
    - PorDeudaTab consume `useTransactionsApi` / `/admin/reports/outstanding-balances` (grep lo confirma).
    - `el-templo-admin/src/components/DeudasReport.vue` ya NO existe (ls/grep confirma su borrado).
    - ReportesPage.vue ya NO contiene `DeudasReport`, ni `name="deudas"`, ni `'deudas'` en VALID_TABS (grep de `DeudasReport` en ReportesPage.vue devuelve 0).
    - `pnpm lint` pasa sin imports/variables sin usar.
  </acceptance_criteria>
  <done>El tab "Por deuda" vive en Deudas con Motivo/Fecha de registro/período y nota en tooltip; el reporte salió de Reportes sin links rotos.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary       | Description                                                          |
| -------------- | -------------------------------------------------------------------- |
| UI admin → API | La UI solo esconde tabs; el acceso real lo decide el guard de la API |

## STRIDE Threat Register

| Threat ID | Category               | Component                          | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                     |
| --------- | ---------------------- | ---------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-153-07  | Elevation of Privilege | tab "Por deuda" visible para coach | mitigate    | El hub esconde "Por deuda" para coach vía `DEUDAS_DETAIL_ROLES`, y `tabFromQuery` cae al default si el coach fuerza `?tab=porDeuda`. La defensa real: el endpoint reports/outstanding-balances ya devuelve 403 a coach (plan 153-01/guard plugin). La UI escondiendo NO es la seguridad (D-04/149). |
| T-153-08  | Information Disclosure | nota libre (notes) en tooltip      | accept      | La nota solo se muestra en el tab de detalle, gateado a gestion/admin/owner en UI y API; el dato ya llega scoped desde el backend.                                                                                                                                                                  |
| T-153-SC  | Tampering              | Dependencias npm                   | accept      | Este plan NO instala ni actualiza paquetes; sin superficie de supply chain.                                                                                                                                                                                                                         |

</threat_model>

<verification>
- `pnpm lint` verde en el-templo-admin.
- DeudasPage compila con 2 tabs; PorSocioTab es el cuerpo actual sin cambios de columnas.
- DeudasReport.vue borrado; ReportesPage sin referencias a Deudas.
- El coach no ve "Por deuda" en la UI (y el API ya lo bloquea).
</verification>

<success_criteria>

- DEUDA-01/02/03 visibles en el tab "Por deuda" (fecha de registro, motivo, plan+período, nota en tooltip).
- Las dos listas que pidió el usuario conviven en una sola pantalla; default "Por socio".
- Reportes ya no aloja el reporte de deudas; sin links rotos.
  </success_criteria>

<output>
Create `.planning/phases/153-mejoras-de-deudas/153-03-SUMMARY.md` when done
</output>
