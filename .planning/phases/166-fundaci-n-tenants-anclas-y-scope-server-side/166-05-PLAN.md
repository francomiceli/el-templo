---
phase: 166-fundaci-n-tenants-anclas-y-scope-server-side
plan: 05
type: execute
wave: 4
depends_on: [166-04]
files_modified:
  - el-templo-api/test/shared/tenant-suspension-routes.test.ts
autonomous: true
requirements: [FUND-03, FUND-04]
must_haves:
  truths:
    - "Con el tenant suspendido, una ruta real del admin y una ruta real de la member app responden 403 con code TENANT_SUSPENDED (CD-01: la suspension alcanza TODO lo autenticado)"
    - "Con el tenant activo esas mismas rutas responden como siempre — el staff no ve ningun cambio"
    - "El login sigue respondiendo con el tenant suspendido (es pre-scope): la suspension corta lo scoped, no la autenticacion"
    - "Mandar tenant_id por query o body en una ruta real no cambia la respuesta"
    - "La suite existente sigue verde sin ajustar una sola expectativa"
  artifacts:
    - path: "el-templo-api/test/shared/tenant-suspension-routes.test.ts"
      provides: "Integracion extremo a extremo del enforcement por status sobre rutas admin y member reales"
      min_lines: 120
  key_links:
    - from: "el-templo-api/test/shared/tenant-suspension-routes.test.ts"
      to: "el-templo-api/src/modules/shared/country-scope.ts"
      via: "fastify.inject sobre rutas que registran attachCountryScope en su hook onRequest"
      pattern: "TENANT_SUSPENDED"
---

<objective>
La prueba que le importa al negocio: con el tenant suspendido, ninguna superficie autenticada
responde datos — ni el admin ni la member app — y el body trae el codigo estable que un frontend
podra mostrar el dia que exista un tenant 2. Y con el tenant activo, todo responde exactamente
igual que antes de la fase.

Purpose: el plan 166-04 probo el hook aislado. Este prueba que las 19 registraciones de hook
heredan el enforcement de verdad, atravesando Fastify (incluida la serializacion del `code`).
Output: `test/shared/tenant-suspension-routes.test.ts` verde + regresion dirigida sin ajustes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/166-fundaci-n-tenants-anclas-y-scope-server-side/166-CONTEXT.md
@.planning/phases/166-fundaci-n-tenants-anclas-y-scope-server-side/166-PATTERNS.md
@CLAUDE.md

<working_directory>
Todo el codigo se escribe en `/home/franco/projects/et-166-tenancy` (rama `feat/166-tenancy-fundacion`).
No tocar el checkout principal. Nunca `git add -A`.
</working_directory>

<interfaces>
Simbolos y rutas que este plan consume (ya existentes o creados en 166-04):

- `TENANT_SUSPENDED` (`src/modules/shared/country-scope.ts`) — importarlo, no hardcodear el string.
- Superficie admin: `/api/admin/members/` (hook en `src/modules/members/routes.ts:110`) y
  `/api/admin/subscriptions/plans` (hook en `src/modules/subscriptions/routes.ts:104`).
- Superficie member app (CD-01): `/api/members/subscription/plans` (hook en
  `src/modules/subscriptions/member-routes.ts:58`).
- Superficie pre-scope: la ruta de login (path y payload exactos en `test/auth/auth.test.ts`),
  que NO registra `attachCountryScope` y por lo tanto debe seguir respondiendo.
- Helpers: `createTestApp`, `createStaffUser`, `registerUser`, `getAuthToken`, `cleanAllTestData`
  de `test/helpers.ts`.

Body esperado del 403: `{ statusCode: 403, code: "TENANT_SUSPENDED", error: "Forbidden", message: "Acceso suspendido para este gimnasio" }`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integracion del enforcement por status sobre rutas reales</name>
  <files>el-templo-api/test/shared/tenant-suspension-routes.test.ts</files>
  <read_first>
    - /home/franco/projects/et-166-tenancy/el-templo-api/test/country-scope.test.ts (analogo exacto: fixtures de branches/staff/miembros, tokens, `app.inject` con `Authorization`, asserts de status code)
    - /home/franco/projects/et-166-tenancy/el-templo-api/test/auth/auth.test.ts (path y payload exactos del login)
    - /home/franco/projects/et-166-tenancy/el-templo-api/src/modules/subscriptions/member-routes.ts lineas 50-70 (la superficie member app: hook `authenticate` + `attachCountryScope`)
    - /home/franco/projects/et-166-tenancy/el-templo-api/src/modules/shared/country-scope.ts (tras el plan 166-04)
    - /home/franco/projects/et-166-tenancy/el-templo-api/test/helpers.ts
  </read_first>
  <action>
    Crear `test/shared/tenant-suspension-routes.test.ts` con `createTestApp()`, fixtures minimos
    (un staff owner o admin con token, y un member con token via `registerUser`/`getAuthToken`) y
    estos bloques, usando `app.inject`:

    1. **Control con tenant `active`:** `GET /api/admin/members/` con token de staff responde 200 (o
       el status que devuelve hoy — verificarlo ANTES de suspender y guardar el valor para
       compararlo despues de restaurar), y `GET /api/members/subscription/plans` con token de member
       responde su status habitual. Este bloque es la evidencia de "el staff no ve ningun cambio".
    2. **Tenant `suspended` (FUND-04 + CD-01):** con `UPDATE tenants SET status='suspended' WHERE id=1`,
       las tres rutas (`/api/admin/members/`, `/api/admin/subscriptions/plans`,
       `/api/members/subscription/plans`) responden **exactamente 403** (assert estricto
       `toBe(403)`, no `toContain([403,404])`) y el body parseado cumple
       `code === TENANT_SUSPENDED` (constante importada) y `error === "Forbidden"`.
    3. **Tenant `archived`:** idem, al menos sobre una ruta admin y una member.
    4. **Login sigue vivo (CD-01, pre-scope):** con el tenant suspendido, el login responde su
       status normal (200 con credenciales validas). Documenta la frontera: la suspension corta lo
       scoped, no la autenticacion.
    5. **Restauracion:** vuelto `status='active'`, las rutas del bloque 1 devuelven exactamente los
       mismos status que en el bloque 1.
    6. **Entradas hostiles en ruta real (FUND-03):** con tenant activo,
       `GET /api/admin/members/?tenant_id=999` y un POST/GET con header `x-tenant-id: 999`
       devuelven lo mismo que sin esos parametros (mismo status y misma cantidad de items si la
       respuesta es una lista). Mandar tenant no cambia nada.

    **Higiene obligatoria:** `afterEach` y `afterAll` con
    `UPDATE tenants SET status='active' WHERE id=1` incondicional, y cada bloque que suspende
    envuelto en try/finally. La DB es por worker y compartida entre archivos del mismo fork: dejar
    el tenant suspendido rompe todas las suites siguientes.
    Sin `any`. Nunca `console.*`.

    Commitear por ruta explicita: `test(166): 403 TENANT_SUSPENDED extremo a extremo en rutas admin y member`.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-166-tenancy/el-templo-api && npx tsc --noEmit && MAX_TEST_WORKERS=1 npx vitest run test/shared/tenant-suspension-routes.test.ts && set -a && . ./.env.development && set +a && mysql -h "$DB_HOST" -P "$DB_PORT" -u "$DB_USER" -p"$DB_PASSWORD" eltemplo_test_1 -N -e "SELECT status FROM tenants WHERE id=1;"</automated>
  </verify>
  <acceptance_criteria>
    - `MAX_TEST_WORKERS=1 npx vitest run test/shared/tenant-suspension-routes.test.ts` verde, con al menos 6 `it()`
    - El archivo cubre las 3 rutas nombradas en `<interfaces>` bajo `suspended` y al menos 2 bajo `archived`
    - Los asserts de status usan `toBe(403)` (estricto), verificable con `grep -c "toBe(403)" test/shared/tenant-suspension-routes.test.ts` >= 3
    - El archivo importa `TENANT_SUSPENDED` desde `../../src/modules/shared/country-scope` en vez de hardcodear el string (`grep -c 'from "../../src/modules/shared/country-scope"' ...` devuelve `1`)
    - Existe el caso de login respondiendo con el tenant suspendido
    - Existe el caso de entradas hostiles (`tenant_id` en query y `x-tenant-id` en header) sin efecto
    - Tras la corrida, `SELECT status FROM tenants WHERE id=1` en `eltemplo_test_1` devuelve `active`
    - `grep -nE ':\s*any\b|console\.' test/shared/tenant-suspension-routes.test.ts` no devuelve nada
  </acceptance_criteria>
  <done>El enforcement de tenant esta probado atravesando Fastify, en la superficie de staff y en la de socios, con el codigo estable visible en el body.</done>
</task>

<task type="auto">
  <name>Task 2: Gate de regresion dirigido de la fase</name>
  <files>ninguno (solo ejecucion y evidencia)</files>
  <read_first>
    - /home/franco/projects/et-166-tenancy/el-templo-api/test/country-scope.test.ts (el hermano directo del hook modificado: si algo del scope se rompio, rompe aca)
    - /home/franco/projects/et-166-tenancy/el-templo-api/test/branch-access.test.ts (consumidor de `request.scope`)
    - .planning/phases/166-fundaci-n-tenants-anclas-y-scope-server-side/166-CONTEXT.md (criterio de exito 4: la suite pasa SIN ajustar expectativas)
  </read_first>
  <action>
    Correr la regresion dirigida de la fase (la suite completa corre en CI, no local) sobre los
    consumidores directos de `request.scope` y las superficies que el hook atraviesa:
    `test/country-scope.test.ts`, `test/branch-access.test.ts`, `test/auth`, `test/subscriptions`,
    `test/members`, `test/finance` y `test/shared`, todos con `MAX_TEST_WORKERS=1`.

    Regla dura: **cero modificaciones a archivos de test existentes**. Un fallo se corrige en
    `country-scope.ts` o en las migraciones, jamas ajustando la expectativa de un test viejo
    (criterio de exito 4 de la fase). Documentar en el SUMMARY cualquier fallo, su causa raiz y el
    fix aplicado.

    Ademas, dejar registrado en el SUMMARY el inventario de lo que la fase toco: salida de
    `git -C /home/franco/projects/et-166-tenancy diff --stat origin/master` (debe ser exactamente:
    2 migraciones nuevas, 4 archivos de schema, `country-scope.ts` y 3 archivos de test nuevos).

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-166-tenancy/el-templo-api && MAX_TEST_WORKERS=1 npx vitest run test/country-scope.test.ts test/branch-access.test.ts test/auth test/subscriptions test/members test/finance test/shared && git -C /home/franco/projects/et-166-tenancy diff --stat origin/master</automated>
  </verify>
  <acceptance_criteria>
    - Las suites dirigidas corren verdes con `MAX_TEST_WORKERS=1`
    - `git -C /home/franco/projects/et-166-tenancy diff --name-only origin/master` lista exactamente 10 archivos: `src/db/schema/tenants.ts`, `src/db/schema/index.ts`, `src/db/schema/users.ts`, `src/db/schema/branches.ts`, las 2 migraciones nuevas, `src/modules/shared/country-scope.ts`, `test/migrations/0190-0191-tenants.test.ts`, `test/shared/tenant-scope.test.ts`, `test/shared/tenant-suspension-routes.test.ts`
    - Ningun archivo preexistente de `test/` aparece modificado (ni `setup.ts` ni `helpers.ts`)
    - `npx tsc --noEmit` limpio
  </acceptance_criteria>
  <done>La fase completa esta verde en local sobre los caminos que toca, sin haber movido una sola expectativa existente.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                           | Description                                                                                                              |
| -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| cliente HTTP -> rutas scoped                       | Superficie real donde se comprueba que la suspension y el origen server-side del tenant se sostienen atravesando Fastify |
| login (pre-scope) -> resto del API                 | Frontera deliberada: autenticar se puede, operar no                                                                      |
| DB de test compartida por worker -> suites vecinas | Un `tenants.status` mal restaurado convierte esta suite en un rompe-todo                                                 |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                      | Disposition | Mitigation Plan                                                                                                                |
| --------- | ---------------------- | ------------------------------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------ |
| T-166-20  | Elevation of Privilege | Superficie member app operando con el tenant suspendido                        | mitigate    | Caso explicito sobre `/api/members/subscription/plans` con token de socio (CD-01)                                              |
| T-166-21  | Spoofing               | `tenant_id` inyectado en una ruta real                                         | mitigate    | Caso con `?tenant_id=999` y header `x-tenant-id` que compara la respuesta contra la version sin parametros                     |
| T-166-22  | Information Disclosure | Serializador de respuesta que descarta `code` en alguna ruta con schema de 403 | mitigate    | El assert del `code` corre sobre 3 rutas reales de 3 modulos distintos; si alguna lo descarta, el test lo revela antes de prod |
| T-166-23  | Denial of Service      | Suspension que tambien rompe el login y deja al staff sin poder autenticarse   | mitigate    | Caso explicito de login respondiendo con el tenant suspendido                                                                  |
| T-166-24  | Denial of Service      | Estado `suspended` filtrado a otras suites del worker                          | mitigate    | `afterEach`/`afterAll` incondicionales + verificacion SQL del status tras la corrida                                           |
| T-166-SC  | Tampering              | pnpm installs                                                                  | mitigate    | Cero paquetes nuevos; cualquier `pnpm add` requiere checkpoint humano bloqueante                                               |

</threat_model>

<verification>
- El test de integracion pasa y cubre admin + member + login
- La regresion dirigida pasa sin tocar tests existentes
- El diff de la fase contra `origin/master` es exactamente los 10 archivos esperados
- El tenant queda `active` en la DB de test
</verification>

<success_criteria>
FUND-03 y FUND-04 verificados extremo a extremo, y criterio de exito 4 de la fase (sin cambio
visible para el staff, suite sin ajustes) demostrado con evidencia reproducible.
</success_criteria>

<output>
Create `.planning/phases/166-fundaci-n-tenants-anclas-y-scope-server-side/166-05-SUMMARY.md` when done.
</output>
