---
phase: 166-fundaci-n-tenants-anclas-y-scope-server-side
plan: 06
type: execute
wave: 5
depends_on: [166-03, 166-05]
files_modified: []
autonomous: false
requirements: [FUND-01, FUND-02, FUND-03, FUND-04]
must_haves:
  truths:
    - "Las migraciones de las tandas A y B corren verdes en staging y despues en prod, sin downtime"
    - "En cada base, tenants tiene exactamente una fila (id=1, el-templo, active) y el 100% de users y branches tiene tenant_id=1"
    - "El staff opera normal despues del deploy (smoke verde en staging y en prod)"
    - "Nada se pusheo ni se deployo sin aprobacion explicita del desarrollador"
  artifacts:
    - path: ".planning/phases/166-fundaci-n-tenants-anclas-y-scope-server-side/166-06-SUMMARY.md"
      provides: "Evidencia del rollout: conteos por base, commits desplegados y resultado del smoke"
  key_links:
    - from: "rama feat/166-tenancy-fundacion"
      to: "eltemplo_staging y eltemplo"
      via: "pipeline de deploy (staging primero, master despues)"
      pattern: "0190|0191"
---

<objective>
Cerrar la fase con el rollout staging-first y la evidencia de que las dos bases quedaron en el
estado que el criterio de exito 1 exige.

Purpose: la fundacion de tenancy no vale nada mientras viva solo en una rama local — pero tampoco
puede irse a prod sin que Franco lo apruebe, ni saltando staging (regla dura del repo).
Output: gate local consolidado + checkpoint humano con la secuencia exacta de push, deploy y
verificacion en cada base.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/166-fundaci-n-tenants-anclas-y-scope-server-side/166-CONTEXT.md
@.planning/phases/166-fundaci-n-tenants-anclas-y-scope-server-side/166-PATTERNS.md
@CLAUDE.md

<working_directory>
El codigo de la fase vive en `/home/franco/projects/et-166-tenancy` (rama `feat/166-tenancy-fundacion`).
Los SUMMARY de GSD se commitean en el checkout principal, como en la fase 164.

**Reglas duras que este plan NO puede saltear:** staging-first estricto (ningun merge de feature
directo a master), preguntar SIEMPRE antes de pushear, preguntar SIEMPRE antes de entrar por SSH al
servidor, nunca deploys manuales de frontend (esta fase no toca frontend: es API-only, sin bump de
version de app ni builds de tienda).
</working_directory>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate local consolidado y preparacion del rollout</name>
  <files>ninguno (verificacion y preparacion)</files>
  <read_first>
    - .claude/skills/el-templo-db-migrations/SKILL.md (el "shared-host trap": staging y prod son el MISMO servidor MySQL con bases `eltemplo_staging` y `eltemplo`; los pipelines solo difieren en DB_NAME)
    - .claude/skills/el-templo-change-control/SKILL.md (staging-first, orden de push)
    - /home/franco/projects/et-166-tenancy/el-templo-api/src/db/migrations/0190_tenants_core.sql
    - /home/franco/projects/et-166-tenancy/el-templo-api/src/db/migrations/0191_tenant_anchors.sql
    - /home/franco/projects/et-166-tenancy/el-templo-api/src/db/schema/tenants.ts
  </read_first>
  <action>
    Consolidar la evidencia local ANTES de proponer cualquier push:

    1. `npx tsc --noEmit` en `/home/franco/projects/et-166-tenancy/el-templo-api` y el lint del repo
       tal como lo corre CI (`pnpm lint` si existe en el package.json del API).
    2. Re-verificar las dos migraciones contra las hard rules del skill: cero `;` en lineas de
       comentario `--` de ambos archivos; nombres de columna del SQL identicos a los del schema
       Drizzle (en particular la columna del enum: `status`, NO `tenant_status`); y numeracion
       todavia libre — o sea, que `origin/master` y `origin/staging` sigan sin traer los numeros
       reservados (volver a correr `git fetch origin` y listar migraciones de ambas ramas; si otra
       rama aterrizo el mismo numero mientras la fase avanzaba, renumerar ANTES de pushear).
    3. Confirmar el inventario del diff: `git -C /home/franco/projects/et-166-tenancy diff --stat origin/master`
       debe listar exactamente los 10 archivos de la fase, sin `.env*`, sin `node_modules`, sin
       archivos de otras ramas.
    4. Confirmar que el checkout principal `/home/franco/projects/el-templo` no fue modificado por
       la fase (su `git status --porcelain` no debe tener nada nuevo de `el-templo-api/`).
    5. Preparar (SIN ejecutar) la secuencia exacta de comandos del rollout para que el checkpoint
       la muestre textual: push de la rama, merge/push a `staging`, y despues a `master`, con los
       nombres de rama y de remoto reales del repo.
    6. Preparar las 4 consultas de verificacion post-deploy que hay que correr contra cada base
       (`eltemplo_staging` primero, `eltemplo` despues):
       - `SELECT id, name, slug, status FROM tenants` (debe devolver 1 fila: 1, El Templo, el-templo, active)
       - `SELECT COUNT(*) FROM tenants` (debe ser 1)
       - `SELECT COUNT(*) FROM users WHERE tenant_id <> 1 OR tenant_id IS NULL` (debe ser 0), idem `branches`
       - `SELECT name FROM _migrations WHERE name LIKE '019%' ORDER BY name` (deben aparecer las dos migraciones de la fase, una vez cada una)

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-166-tenancy/el-templo-api && npx tsc --noEmit && grep -n '^--' src/db/migrations/0190_tenants_core.sql src/db/migrations/0191_tenant_anchors.sql | grep ';' ; test $? -eq 1 && git -C /home/franco/projects/et-166-tenancy fetch origin --quiet && git ls-tree --name-only origin/master el-templo-api/src/db/migrations/ | grep -E '019[01]_' ; test $? -eq 1 && git -C /home/franco/projects/et-166-tenancy diff --stat origin/master && echo GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` limpio y lint verde
    - Cero `;` en lineas de comentario de las dos migraciones
    - Ni `origin/master` ni `origin/staging` contienen archivos con los numeros de migracion reservados por la fase (si los contienen, el task renumera antes de continuar y lo documenta)
    - `git diff --name-only origin/master` lista exactamente los 10 archivos de la fase, sin `.env*` ni `node_modules`
    - `git -C /home/franco/projects/el-templo status --porcelain -- el-templo-api` no muestra archivos de la fase (el checkout compartido quedo intacto)
    - La secuencia de comandos de rollout y las 4 consultas de verificacion quedan escritas textualmente en la salida del task, listas para el checkpoint
  </acceptance_criteria>
  <done>Todo lo verificable sin salir de la maquina esta verde y el rollout esta preparado con comandos exactos.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Rollout staging-first y verificacion en las dos bases</name>
  <read_first>
    - .claude/skills/el-templo-change-control/SKILL.md (orden de push, staging-first estricto)
    - .claude/skills/el-templo-db-migrations/SKILL.md (shared-host trap: `eltemplo_staging` y `eltemplo` viven en el MISMO servidor MySQL)
    - La salida del Task 1 de este plan (secuencia de comandos y las 4 consultas ya preparadas)
  </read_first>
  <action>
    Presentar al desarrollador el estado de la fase y la secuencia exacta preparada en el Task 1, y
    ESPERAR aprobacion explicita. Claude no ejecuta nada de esto por iniciativa propia.

    Tras la senal "aprobado staging": pushear `feat/166-tenancy-fundacion` a origin y llevarla a
    `staging` con la secuencia preparada, seguir el CI hasta verde y —pidiendo autorizacion
    SEPARADA para el SSH— correr las 4 consultas de verificacion contra `eltemplo_staging`,
    confirmando antes `SELECT DATABASE()`.

    Tras la senal "aprobado prod" (solo con staging verde y smoke OK): llevar la rama a `master`,
    seguir el pipeline y repetir las 4 consultas contra `eltemplo`.

    Si cualquier paso falla: detenerse, NO avanzar a prod y reportar el error exacto (output de la
    consulta o log de CI). El rollback de las tablas nuevas es un DROP; revertir las columnas de las
    anclas requiere decision explicita del desarrollador.

  </action>
  <what-built>
    La fundacion de tenancy completa en la rama `feat/166-tenancy-fundacion` (worktree
    `/home/franco/projects/et-166-tenancy`), sin pushear:
    - `tenants` + `tenant_settings` con El Templo sembrado como tenant 1 (migracion de la tanda A).
    - `tenant_id NOT NULL DEFAULT 1` con FK e indice en `users` y `branches`, backfilleado al 100%
      (migracion de la tanda B).
    - `attachScope` (ex `attachCountryScope`, que queda como alias) resolviendo `scope.tenantId`
      server-side en la misma query que resuelve el pais, y cortando con 403 `TENANT_SUSPENDED`
      cuando el tenant no esta `active`.
    - 3 archivos de test nuevos verdes en local; ningun test existente modificado; typecheck limpio.
    Nada fue pusheado ni deployado todavia.
  </what-built>
  <how-to-verify>
    1. **Aprobar el push** (nada se pushea sin tu OK explicito). Al aprobar, se ejecuta la secuencia
       preparada en el Task 1: push de `feat/166-tenancy-fundacion` a origin y merge/push a
       `staging` (staging-first estricto: NUNCA a master primero).
    2. **CI de staging:** esperar el pipeline en verde (type check, lint, audit, tests de
       integracion, build). Los tests nuevos de la fase corren ahi por primera vez en paralelo con
       la suite completa.
    3. **Deploy de staging + verificacion de la base:** una vez deployado, correr las 4 consultas
       de verificacion contra `eltemplo_staging`. **Requiere SSH al servidor: se pide autorizacion
       por separado antes de conectarse.** Resultado esperado, exacto:
       - `tenants` -> 1 fila: `1 | El Templo | el-templo | active`
       - `SELECT COUNT(*) FROM tenants` -> `1`
       - `users` y `branches` con `tenant_id <> 1 OR IS NULL` -> `0` filas en ambas
       - `_migrations` -> las dos migraciones de la fase, una vez cada una
    4. **Smoke funcional en staging (el criterio "el staff no ve nada"):** entrar al admin de
       staging y confirmar que siguen normales el listado de socios, la carga de un cobro y la
       pantalla de reservas. Y en la member app de staging, que un socio ve sus planes/turnos.
       Ninguna pantalla debe mostrar un 403 nuevo.
    5. **Prod:** con staging verde y el smoke OK, aprobar el push a `master`. Repetir el paso 3
       contra la base `eltemplo` (prod) y el paso 4 contra el admin de produccion.
       Ojo con la trampa del host compartido: staging y prod son el MISMO servidor MySQL con bases
       distintas — verificar `SELECT DATABASE()` antes de leer los conteos.
    6. Si algo sale mal en cualquier paso: NO seguir a prod. Reportar el error exacto; el rollback
       de esta fase es trivial en tablas nuevas (DROP) pero las columnas de las anclas requieren
       decision explicita.
  </how-to-verify>
  <acceptance_criteria>
    - Ningun `git push` ni conexion SSH ocurrio antes de la senal de aprobacion correspondiente
    - CI de staging en verde antes de tocar master
    - En `eltemplo_staging`: `tenants` con 1 fila (`1 | El Templo | el-templo | active`), `COUNT(*)` = 1, `users` y `branches` con 0 filas fuera de `tenant_id = 1`, y las 2 migraciones de la fase en `_migrations` una vez cada una
    - Los mismos 4 resultados en `eltemplo` (prod), con `SELECT DATABASE()` confirmado antes de leer
    - Smoke funcional OK en staging y en prod: listado de socios, carga de un cobro y reservas en el admin, y planes/turnos en la member app, sin 403 nuevos
  </acceptance_criteria>
  <resume-signal>
    Escribi "aprobado staging" para autorizar el push a staging; despues "aprobado prod" para el
    push a master; o describi el problema encontrado (con el output de la consulta o el error de CI).
  </resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                    | Description                                                                                           |
| ------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| rama local -> staging -> prod               | Cada salto agrega irreversibilidad; el orden es la unica proteccion real                              |
| pipeline de deploy -> host MySQL compartido | Staging y prod son el mismo servidor con bases distintas: una migracion mal ruteada toca datos reales |
| Claude -> servidor de produccion            | Toda conexion SSH y todo push requieren autorizacion humana explicita                                 |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                       | Disposition | Mitigation Plan                                                                                                                                 |
| --------- | ---------------------- | ------------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| T-166-25  | Tampering              | Migracion aplicada a la base equivocada (host compartido)                       | mitigate    | Verificar `SELECT DATABASE()` antes de leer conteos, y correr las 4 consultas primero en `eltemplo_staging` y solo despues en `eltemplo`        |
| T-166-26  | Elevation of Privilege | Push a master salteando staging                                                 | mitigate    | Checkpoint bloqueante con dos senales separadas ("aprobado staging" y "aprobado prod"); el orden esta en el plan, no en la memoria del ejecutor |
| T-166-27  | Denial of Service      | Numero de migracion pisado por otra rama que aterrizo mientras la fase avanzaba | mitigate    | Re-fetch y re-verificacion de numeracion en el Task 1, inmediatamente antes del push                                                            |
| T-166-28  | Repudiation            | Deploy sin evidencia de estado final                                            | mitigate    | El SUMMARY registra los conteos por base, los commits desplegados y el resultado del smoke                                                      |
| T-166-29  | Denial of Service      | 403 inesperado para el staff en produccion                                      | mitigate    | Smoke funcional obligatorio en staging antes de prod, y despues en prod, sobre admin y member app                                               |
| T-166-SC  | Tampering              | pnpm installs                                                                   | mitigate    | Cero paquetes nuevos en toda la fase                                                                                                            |

</threat_model>

<verification>
- Gate local verde (typecheck, lint, migraciones sanas, diff acotado, checkout compartido intacto)
- Staging: CI verde, 4 consultas con los valores esperados, smoke funcional OK
- Prod: mismas 4 consultas con los mismos valores, smoke funcional OK
- Ningun push ni SSH ejecutado sin aprobacion explicita
</verification>

<success_criteria>
Criterio de exito 1 de la fase cumplido con evidencia (migraciones verdes en staging y prod, sin
downtime, con `tenants` en una sola fila y las anclas al 100% en tenant 1) y criterio 4 confirmado
por smoke (el staff no percibio ningun cambio).
</success_criteria>

<output>
Create `.planning/phases/166-fundaci-n-tenants-anclas-y-scope-server-side/166-06-SUMMARY.md` when done.
El SUMMARY DEBE incluir: conteos obtenidos en `eltemplo_staging` y en `eltemplo`, los commits
desplegados en cada entorno y el resultado del smoke funcional.
</output>
