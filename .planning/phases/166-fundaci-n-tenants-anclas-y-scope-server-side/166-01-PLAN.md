---
phase: 166-fundaci-n-tenants-anclas-y-scope-server-side
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/schema/tenants.ts
  - el-templo-api/src/db/schema/index.ts
  - el-templo-api/src/db/migrations/0190_tenants_core.sql
autonomous: true
requirements: [FUND-01]
must_haves:
  truths:
    - "El trabajo de la fase 166 vive en un worktree limpio sobre origin/master actualizado — el checkout compartido (rama atrasada con WIP ajeno) no se toca"
    - "El bloque de numeracion de migraciones del milestone quedo reservado verificando el maximo REAL en _migrations y en todas las ramas vivas (D-06)"
    - "La DB local tiene la tabla tenants con exactamente una fila: id=1, slug el-templo, status active"
    - "La DB local tiene la tabla tenant_settings, vacia, con unique (tenant_id, setting_key) y FK a tenants"
    - "El schema Drizzle y el SQL hand-written declaran los mismos nombres de columna byte a byte (trampa mysqlEnum resuelta)"
  artifacts:
    - path: "el-templo-api/src/db/schema/tenants.ts"
      provides: "Tablas Drizzle tenants + tenant_settings y la constante de slugs reservados (D-05)"
      exports:
        [
          "tenantStatusEnum",
          "tenants",
          "tenantSettings",
          "RESERVED_TENANT_SLUGS",
        ]
    - path: "el-templo-api/src/db/migrations/0190_tenants_core.sql"
      provides: "Tanda A: CREATE TABLE tenants + tenant_settings + seed idempotente del tenant 1"
      contains: "CREATE TABLE IF NOT EXISTS tenants"
    - path: "el-templo-api/src/db/schema/index.ts"
      provides: "Export del schema nuevo en el barrel"
      contains: 'export * from "./tenants"'
  key_links:
    - from: "el-templo-api/src/db/schema/index.ts"
      to: "el-templo-api/src/db/schema/tenants.ts"
      via: "barrel export (primera linea del archivo)"
      pattern: 'export \* from "\./tenants"'
    - from: "el-templo-api/src/db/migrations/0190_tenants_core.sql"
      to: "el-templo-api/src/db/schema/tenants.ts"
      via: "DDL que espeja el schema Drizzle byte a byte"
      pattern: "ENUM\\('active','suspended','archived'\\)"
---

<objective>
Tanda A del doc 06 §1: crear la base de trabajo aislada de la fase y las dos tablas fundacionales
(`tenants`, `tenant_settings`) con El Templo sembrado como tenant `id=1`.

Purpose: sin `tenants` no hay FK a la que apuntar en la tanda B ni status que chequear en el hook.
Y sin worktree limpio sobre `origin/master` la fase se construye sobre un arbol que ni siquiera
tiene las migraciones 0182-0189 (el checkout compartido llega a 0181), lo que produciria numeros
de migracion duplicados en produccion.
Output: worktree `/home/franco/projects/et-166-tenancy` en rama `feat/166-tenancy-fundacion`,
`src/db/schema/tenants.ts`, export en el barrel, `0190_tenants_core.sql` aplicada a la DB local.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/166-fundaci-n-tenants-anclas-y-scope-server-side/166-CONTEXT.md
@.planning/phases/166-fundaci-n-tenants-anclas-y-scope-server-side/166-PATTERNS.md
@CLAUDE.md

<working_directory>
**TODO el codigo de la fase 166 se escribe en el worktree `/home/franco/projects/et-166-tenancy`**
(lo crea el Task 1 de este plan). Los paths de `files_modified` son relativos a la raiz del repo:
prefijalos con `/home/franco/projects/et-166-tenancy/`.

El checkout principal `/home/franco/projects/el-templo` esta en `fix/referral-preview-y-refresh-ficha`
con cambios sin commitear de OTRA sesion y 219 commits detras de `origin/master`: **no hacer
checkout, stash, ni edits de codigo ahi**. Los artefactos de planning de GSD (SUMMARY.md de cada
plan) SI se commitean en el checkout principal, como se hizo en la fase 164.

Nunca `git add -A` ni `git add .` — stagear siempre por ruta explicita.
</working_directory>

<interfaces>
Contrato que este plan publica y que los planes 166-02 / 166-04 / 166-03 consumen tal cual
(cambiar un nombre despues obliga a tocar 3 planes):

De `el-templo-api/src/db/schema/tenants.ts`:

- `tenantStatusEnum` — mysqlEnum cuyo PRIMER ARGUMENTO (nombre de columna fisica) es `status`,
  valores en este orden: `active`, `suspended`, `archived`, default `active`, notNull.
  Ojo: el snippet del README §5 escribe `mysqlEnum("tenant_status", ...)`, lo que crearia la
  columna fisica `tenant_status`. La intencion del diseno es `tenants.status` (asi la nombran
  README §5 notas, doc 03 §3, el ROADMAP y el CONTEXT). Se adopta `status` y el SQL lo espeja.
- `tenants` — mysqlTable `"tenants"`, columnas: `id` (int PK autoincrement), `name`
  (varchar 255 notNull), `slug` (varchar 50 notNull unique), `status` (tenantStatusEnum),
  `defaultCountry` -> `default_country` (varchar 2, default `AR`, notNull),
  `defaultCurrency` -> `default_currency` (varchar 3, default `ARS`, notNull),
  `defaultTimezone` -> `default_timezone` (varchar 50, default
  `America/Argentina/Buenos_Aires`, notNull), `createdAt` -> `created_at` (timestamp defaultNow
  notNull), `updatedAt` -> `updated_at` (timestamp defaultNow onUpdateNow notNull).
- `tenantSettings` — mysqlTable `"tenant_settings"`, columnas: `id` (int PK autoincrement),
  `tenantId` -> `tenant_id` (int notNull, references tenants.id), `settingKey` -> `setting_key`
  (varchar 100 notNull), `settingValue` -> `setting_value` (text notNull), `updatedAt` ->
  `updated_at` (timestamp defaultNow onUpdateNow notNull). Tercer argumento de mysqlTable:
  `uniqueIndex("uq_tenant_setting").on(t.tenantId, t.settingKey)`.
- `RESERVED_TENANT_SLUGS` — tupla `as const` de slugs que no puede tomar un tenant (D-05, a nivel
  aplicacion, NO en DB): `admin`, `api`, `www`, `app`, `auth`, `staging`, `static`, `assets`,
  `cdn`, `mail`, `docs`, `support`, `dashboard`.

Nombres de constraints en SQL (el plan 166-03 los asserta por INFORMATION_SCHEMA):
`tenants_slug_unique`, `uq_tenant_setting`, `fk_tenant_settings_tenant`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Base de trabajo aislada + reserva del bloque de numeracion (D-06)</name>
  <files>ninguno del repo (solo se crea el worktree)</files>
  <read_first>
    - .claude/skills/el-templo-db-migrations/SKILL.md (numeracion a mano, _migrations como fuente de verdad, trampa del `;` en comentarios)
    - .claude/skills/el-templo-change-control/SKILL.md (staging-first, stagear por ruta)
    - .planning/phases/166-fundaci-n-tenants-anclas-y-scope-server-side/166-PATTERNS.md (seccion 0.1 — el checkout principal llega a 0181, origin/master a 0189)
  </read_first>
  <action>
    Crear la base de trabajo de TODA la fase 166, sin tocar el checkout compartido.

    1. `git -C /home/franco/projects/el-templo fetch origin` (obligatorio ANTES de nada: la rama
       base tiene que ser el `origin/master` de hoy, no el de la ultima vez).
    2. Crear el worktree: `git -C /home/franco/projects/el-templo worktree add -b feat/166-tenancy-fundacion /home/franco/projects/et-166-tenancy origin/master`.
       Si el directorio o la rama ya existieran, NO borrar nada: reportar y detenerse.
    3. Materializar el entorno del API en el worktree (los `.env*` y `node_modules` estan
       gitignoreados, igual que se hizo en el worktree `et-164-tv`):
       copiar `/home/franco/projects/el-templo/el-templo-api/.env` y `.env.development` a
       `/home/franco/projects/et-166-tenancy/el-templo-api/`, y correr
       `pnpm install --frozen-lockfile` en `/home/franco/projects/et-166-tenancy/el-templo-api`.
       PROHIBIDO agregar, actualizar o instalar dependencias nuevas (regla dura del repo):
       `--frozen-lockfile` solo materializa el lockfile ya commiteado.
    4. Reservar el bloque de numeracion verificando el maximo REAL en TRES fuentes (D-06):
       (a) el arbol de migraciones del worktree recien creado,
       (b) la tabla `_migrations` de la DB local (`SELECT MAX(name) FROM _migrations`),
       (c) las ramas vivas que todavia no mergearon: `git ls-tree --name-only origin/staging el-templo-api/src/db/migrations/`
           y el worktree `/home/franco/projects/et-164-tv` (fase TV, mig 0189 aun sin merge a origin).
       El maximo esperado al 2026-07-26 es `0189_tv_screen.sql` -> se reservan **0190** (tanda A,
       este plan) y **0191** (tanda B, plan 166-02). Si el maximo real resulto ser mayor que 0189,
       usar los dos numeros libres siguientes y DEJARLO ESCRITO en el SUMMARY del plan, porque el
       plan 166-02 y los tests del 166-03 nombran los archivos por numero.
    5. No commitear nada en este task (no hay cambios de codigo todavia).

  </action>
  <verify>
    <automated>git -C /home/franco/projects/et-166-tenancy rev-parse --abbrev-ref HEAD && git -C /home/franco/projects/et-166-tenancy rev-list --left-right --count origin/master...HEAD && ls /home/franco/projects/et-166-tenancy/el-templo-api/src/db/migrations | grep -E '^0[0-9]{3}_' | sort | tail -2 && test -f /home/franco/projects/et-166-tenancy/el-templo-api/.env.development && test -d /home/franco/projects/et-166-tenancy/el-templo-api/node_modules && echo BASE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `git -C /home/franco/projects/et-166-tenancy rev-parse --abbrev-ref HEAD` imprime `feat/166-tenancy-fundacion`
    - `git -C /home/franco/projects/et-166-tenancy rev-list --left-right --count origin/master...HEAD` imprime `0	0` (la rama arranca exactamente en origin/master)
    - El ultimo archivo de `el-templo-api/src/db/migrations` en el worktree es `0189_tv_screen.sql` (o mayor si otra rama mergeo — en ese caso el SUMMARY registra los dos numeros efectivamente reservados)
    - `SELECT MAX(name) FROM _migrations` en la DB local devuelve un nombre menor a los dos numeros reservados
    - Existen `/home/franco/projects/et-166-tenancy/el-templo-api/.env.development` y `node_modules/`
    - `git -C /home/franco/projects/el-templo status --porcelain` no muestra archivos nuevos ni modificados respecto del estado previo al task (el checkout compartido quedo intacto)
    - `git -C /home/franco/projects/et-166-tenancy status --porcelain` esta vacio
  </acceptance_criteria>
  <done>Existe un worktree limpio en origin/master donde corre toda la fase, y los dos numeros de migracion del milestone estan reservados con evidencia de las tres fuentes.</done>
</task>

<task type="auto">
  <name>Task 2: Schema Drizzle tenants + tenant_settings + slugs reservados</name>
  <files>el-templo-api/src/db/schema/tenants.ts, el-templo-api/src/db/schema/index.ts</files>
  <read_first>
    - /home/franco/projects/et-166-tenancy/el-templo-api/src/db/schema/debt-management.ts (analogo primario: mysqlEnum + uniqueIndex + FK + comentario de cabecera)
    - /home/franco/projects/et-166-tenancy/el-templo-api/src/db/schema/system-settings.ts (el KV que tenant_settings espeja)
    - /home/franco/projects/et-166-tenancy/el-templo-api/src/db/schema/branches.ts (defaults escalares de country/timezone)
    - /home/franco/projects/et-166-tenancy/el-templo-api/src/db/schema/index.ts (barrel: una linea por archivo)
    - .docs/saas-multitenancy/README.md seccion 5 (schema validado linea por linea)
  </read_first>
  <action>
    Crear `el-templo-api/src/db/schema/tenants.ts` con EXACTAMENTE los simbolos, nombres de columna
    fisica, tipos, longitudes y defaults listados en el bloque `<interfaces>` de este plan — sin
    agregar campos de billing/plan comercial (README §5: se agregan cuando exista modelo comercial).

    Reglas de construccion:
    - Comentario de cabecera `// Module: tenants` + bloque `//` explicando: que es la raiz de la
      jerarquia multi-tenant (v6.0, FUND-01), que El Templo es el tenant `id=1`, que `status` es la
      palanca comercial (suspended = falta de pago, datos intactos; archived = baja logica, NUNCA
      DELETE fisico) y que el enforcement vive en la capa 1 (`attachScope`).
    - Comentario explicito al lado de `tenantStatusEnum` avisando que el primer argumento de
      `mysqlEnum` es el NOMBRE DE COLUMNA FISICA (`status`) y que el snippet del README §5 dice
      `tenant_status` — se adopta `status`, y `0190_tenants_core.sql` lo espeja byte a byte.
    - `tenants.ts` NO importa `users` ni `branches` (esas dos importaran `tenants` en el plan
      166-02; el triangulo de imports se evita manteniendo `tenants` en la hoja).
    - `RESERVED_TENANT_SLUGS` exportada como tupla `as const` con los 13 valores del bloque
      `<interfaces>`, con comentario `// Fase 166 (D-05): reservados a nivel aplicacion, no en DB`.
      Todavia no tiene consumidor de runtime (el alta de tenants es post-v6.0); el plan 166-03
      la usa como assert.
    - Sin `any` (CLAUDE.md).

    Registrar el modulo en el barrel: agregar `export * from "./tenants";` como **PRIMERA linea**
    de `el-templo-api/src/db/schema/index.ts` (fundacional, y ademas evita el conflicto de merge
    con las ramas vivas que appendean al final — riesgo anotado en el CONTEXT, Integration Points).

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-166-tenancy/el-templo-api && npx tsc --noEmit && head -1 src/db/schema/index.ts && grep -c 'mysqlEnum("status"' src/db/schema/tenants.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` limpio en `/home/franco/projects/et-166-tenancy/el-templo-api`
    - `head -1 src/db/schema/index.ts` es exactamente `export * from "./tenants";`
    - `grep -c 'mysqlEnum("status"' src/db/schema/tenants.ts` devuelve `1` (y `grep -c 'mysqlEnum("tenant_status"' ...` devuelve `0`)
    - `grep -nE 'export const (tenants|tenantSettings|tenantStatusEnum|RESERVED_TENANT_SLUGS)' src/db/schema/tenants.ts` devuelve 4 lineas
    - `grep -c 'uniqueIndex("uq_tenant_setting")' src/db/schema/tenants.ts` devuelve `1`
    - `grep -nE 'from "\./(users|branches)"' src/db/schema/tenants.ts` no devuelve nada
    - `grep -nE ':\s*any\b' src/db/schema/tenants.ts` no devuelve nada
    - `node -e "const s=require('fs').readFileSync('src/db/schema/tenants.ts','utf8'); ['default_country','default_currency','default_timezone','setting_key','setting_value','tenant_id'].forEach(c=>{ if(!s.includes(c)) throw new Error('falta columna '+c) }); console.log('COLS_OK')"` imprime `COLS_OK`
  </acceptance_criteria>
  <done>El schema Drizzle de la raiz de tenancy compila, esta exportado en el barrel y declara las columnas fisicas que la migracion 0190 va a crear.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Migracion 0190 (tanda A) hand-written + aplicacion local</name>
  <files>el-templo-api/src/db/migrations/0190_tenants_core.sql</files>
  <read_first>
    - .claude/skills/el-templo-db-migrations/SKILL.md (hard rules: numeracion a mano, SQL junto al schema, nunca `;` en comentarios, nunca drizzle-kit push/migrate)
    - /home/franco/projects/et-166-tenancy/el-templo-api/src/db/migrations/0181_debt_management.sql (analogo de CREATE TABLE hand-written completa)
    - /home/franco/projects/et-166-tenancy/el-templo-api/src/db/migrations/0176_referrals_core.sql lineas 43-56 (seed idempotente INSERT ... SELECT ... WHERE NOT EXISTS, ya probado en prod)
    - /home/franco/projects/et-166-tenancy/el-templo-api/src/db/run-migrations.ts lineas 24-57 (splitSqlStatements: por que un `;` en un comentario rompe todo)
    - /home/franco/projects/et-166-tenancy/el-templo-api/src/db/schema/tenants.ts (el schema que este SQL debe espejar, creado en el Task 2)
  </read_first>
  <action>
    Escribir a mano `el-templo-api/src/db/migrations/0190_tenants_core.sql` (o el numero reservado
    en el Task 1 si 0190 estaba tomado) con estos tres statements, en este orden:

    1. `CREATE TABLE IF NOT EXISTS tenants` con: `id INT AUTO_INCREMENT PRIMARY KEY`;
       `name VARCHAR(255) NOT NULL`; `slug VARCHAR(50) NOT NULL`;
       `status ENUM('active','suspended','archived') NOT NULL DEFAULT 'active'`;
       `default_country VARCHAR(2) NOT NULL DEFAULT 'AR'`;
       `default_currency VARCHAR(3) NOT NULL DEFAULT 'ARS'`;
       `default_timezone VARCHAR(50) NOT NULL DEFAULT 'America/Argentina/Buenos_Aires'`;
       `created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP`;
       `updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`;
       y `UNIQUE KEY tenants_slug_unique (slug)` inline (nombre = el que genera Drizzle para
       `.unique()` sobre una columna: `<tabla>_<columna>_unique`).
    2. `CREATE TABLE IF NOT EXISTS tenant_settings` con: `id INT AUTO_INCREMENT PRIMARY KEY`;
       `tenant_id INT NOT NULL`; `setting_key VARCHAR(100) NOT NULL`;
       `setting_value TEXT NOT NULL`;
       `updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`;
       `UNIQUE KEY uq_tenant_setting (tenant_id, setting_key)`;
       `CONSTRAINT fk_tenant_settings_tenant FOREIGN KEY (tenant_id) REFERENCES tenants(id)`.
       La tabla nace VACIA (D-05: coexistencia gradual con `system_settings`, que NO recibe
       `tenant_id` en todo el milestone).
    3. Seed idempotente del tenant 1 con `id` EXPLICITO (la tanda B backfillea literalmente `= 1`):
       `INSERT INTO tenants (id, name, slug, status, default_country, default_currency, default_timezone)`
       + `SELECT 1, 'El Templo', 'el-templo', 'active', 'AR', 'ARS', 'America/Argentina/Buenos_Aires'`
       + `WHERE NOT EXISTS (SELECT 1 FROM tenants WHERE id = 1)`.

    Header de comentarios `--` obligatorio, con: fase 166 / FUND-01, tanda A del doc 06 §1, por que
    esta hand-written (`db:generate` roto por el drift de `sessions.goal_plan_type`, precedente
    0176/0181), y la nota de idempotencia. **NINGUN caracter `;` dentro de una linea de comentario
    `--`** (el runner y `test/setup.ts` splitean por `;` ANTES de stripear comentarios: un `;` en
    un comentario rompe la suite entera). Usar guiones dobles como separador de prosa, igual que
    0181. No usar backticks o usarlos en todo el archivo, pero ser consistente.

    Aplicar y verificar contra la DB local:
    - `cd /home/franco/projects/et-166-tenancy/el-templo-api && pnpm db:migrate` (runner propio).
      PROHIBIDO `pnpm db:push`, `drizzle-kit push` y `drizzle-kit migrate`.
    - Verificar con SQL directo: la fila `id=1` existe con slug `el-templo` y status `active`,
      `SELECT COUNT(*) FROM tenants` devuelve 1, `tenant_settings` existe y esta vacia, y
      `_migrations` tiene exactamente una fila con el nombre del archivo.
    - Correr `pnpm db:migrate` una SEGUNDA vez y confirmar que no falla y no duplica filas
      (idempotencia real, no teorica).

    Commitear en el worktree el `.sql` JUNTO al `.ts` del schema y el barrel, stageando por ruta
    explicita (`git -C /home/franco/projects/et-166-tenancy add el-templo-api/src/db/schema/tenants.ts el-templo-api/src/db/schema/index.ts el-templo-api/src/db/migrations/0190_tenants_core.sql`),
    mensaje `feat(166): tenants + tenant_settings (tanda A, FUND-01)`.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-166-tenancy/el-templo-api && grep -n '^--' src/db/migrations/0190_tenants_core.sql | grep ';' ; test $? -eq 1 && pnpm db:migrate && set -a && . ./.env.development && set +a && mysql -h "$DB_HOST" -P "$DB_PORT" -u "$DB_USER" -p"$DB_PASSWORD" "$DB_NAME" -N -e "SELECT id, slug, status FROM tenants; SELECT COUNT(*) FROM tenants; SELECT COUNT(*) FROM tenant_settings; SELECT COUNT(*) FROM _migrations WHERE name='0190_tenants_core.sql';"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '^--' src/db/migrations/0190_tenants_core.sql | grep ';'` no devuelve ninguna linea (cero `;` en comentarios)
    - `pnpm db:migrate` termina sin error, y una segunda corrida tambien
    - `SELECT id, slug, status FROM tenants` devuelve exactamente una fila: `1  el-templo  active`
    - `SELECT COUNT(*) FROM tenant_settings` devuelve `0`
    - `SELECT COUNT(*) FROM _migrations WHERE name='0190_tenants_core.sql'` devuelve `1`
    - `SHOW CREATE TABLE tenants` contiene `ENUM('active','suspended','archived')` y `UNIQUE KEY \`tenants_slug_unique\``
    - `SHOW CREATE TABLE tenant_settings` contiene `uq_tenant_setting` y `fk_tenant_settings_tenant`
    - `git -C /home/franco/projects/et-166-tenancy log --oneline -1` muestra el commit `feat(166): tenants + tenant_settings (tanda A, FUND-01)` con los 3 archivos (`git show --stat` lista schema/tenants.ts, schema/index.ts y la migracion)
    - `grep -rn "drizzle-kit push\|drizzle-kit migrate\|db:push" el-templo-api/src/db/migrations/` no devuelve nada
  </acceptance_criteria>
  <done>FUND-01 cumplido en local: existen `tenants` y `tenant_settings`, El Templo es el tenant 1, la migracion es idempotente y el SQL viaja en el mismo commit que el schema.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                           | Description                                                                                                                  |
| -------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| worktree/rama -> produccion                        | Un numero de migracion mal reservado se convierte en un archivo que prod nunca aplica (o aplica dos veces)                   |
| SQL hand-written -> runner + provisioning de tests | El mismo parser corre en prod y en `test/setup.ts`; un `;` en un comentario rompe ambos                                      |
| DDL -> datos futuros del SaaS                      | `tenants` es la raiz de aislamiento: un unique o una FK mal declarada compromete el aislamiento de todos los tenants futuros |

## STRIDE Threat Register

| Threat ID | Category               | Component                                             | Disposition | Mitigation Plan                                                                                                                                                           |
| --------- | ---------------------- | ----------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-166-01  | Tampering              | Numeracion de migraciones (D-06)                      | mitigate    | El Task 1 verifica el maximo real en 3 fuentes (arbol del worktree, `_migrations` local, ramas vivas incluido el worktree et-164-tv con 0189) antes de escribir un numero |
| T-166-02  | Denial of Service      | `run-migrations.ts` / `test/setup.ts` (split por `;`) | mitigate    | Gate automatizado: `grep -n '^--' <mig> \| grep ';'` debe devolver vacio; ademas se corre `pnpm db:migrate` dos veces                                                     |
| T-166-03  | Tampering              | Seed del tenant 1                                     | mitigate    | `INSERT ... SELECT ... WHERE NOT EXISTS (SELECT 1 FROM tenants WHERE id = 1)` con `id` explicito: un replay manual es un no-op de 0 filas                                 |
| T-166-04  | Elevation of Privilege | Unique de `tenant_settings`                           | mitigate    | `UNIQUE KEY uq_tenant_setting (tenant_id, setting_key)` compuesta desde el dia 1: un tenant no puede pisar la config de otro por colision de key                          |
| T-166-05  | Repudiation            | Trabajo sobre el checkout compartido                  | mitigate    | Todo el codigo vive en un worktree propio; criterio de aceptacion explicito de que `git status` del checkout principal no cambio                                          |
| T-166-SC  | Tampering              | pnpm installs                                         | mitigate    | Cero paquetes nuevos: solo `pnpm install --frozen-lockfile` sobre el lockfile ya commiteado. Cualquier `pnpm add` requiere checkpoint humano bloqueante                   |

</threat_model>

<verification>
- El worktree existe, esta en `feat/166-tenancy-fundacion` y arranca exactamente en `origin/master`
- El checkout principal quedo intacto (mismo `git status` que antes del plan)
- `tenants` y `tenant_settings` existen en la DB local con los constraints nombrados
- La fila `id=1 / el-templo / active` existe y es unica
- `npx tsc --noEmit` verde
- La migracion es idempotente (dos corridas seguidas, sin error ni duplicados)
</verification>

<success_criteria>
FUND-01 implementado y verificado en local: schema Drizzle + SQL hand-written + seed del tenant 1,
commiteados juntos en una rama limpia sobre origin/master, con el bloque de numeracion del
milestone reservado y documentado en el SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/166-fundaci-n-tenants-anclas-y-scope-server-side/166-01-SUMMARY.md` when done.
El SUMMARY DEBE registrar: (a) el path del worktree y el nombre de la rama, (b) los dos numeros de
migracion efectivamente reservados (0190/0191 o los que hayan resultado), (c) el hash del commit.
</output>
