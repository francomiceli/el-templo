---
phase: 166-fundaci-n-tenants-anclas-y-scope-server-side
plan: 03
type: execute
wave: 3
depends_on: [166-01, 166-02]
files_modified:
  - el-templo-api/test/migrations/0190-0191-tenants.test.ts
autonomous: true
requirements: [FUND-01, FUND-02]
must_haves:
  truths:
    - "Un test automatizado prueba que las migraciones 0190 y 0191 dejaron el DDL exacto (columnas, defaults, FK, indices, seed) — no solo que corrieron"
    - "El test prueba que un insert Drizzle SIN tenantId resuelve a 1, que es la garantia de compatibilidad con codigo viejo y con test/setup.ts"
    - "La suite existente sigue verde en los archivos que mas insertan anclas, sin ajustar una sola expectativa"
  artifacts:
    - path: "el-templo-api/test/migrations/0190-0191-tenants.test.ts"
      provides: "Introspeccion INFORMATION_SCHEMA de tenants/tenant_settings/anclas + seed + idempotencia + round-trip Drizzle"
      min_lines: 120
  key_links:
    - from: "el-templo-api/test/migrations/0190-0191-tenants.test.ts"
      to: "el-templo-api/src/db/schema/tenants.ts"
      via: "import de schema para el round-trip Drizzle y RESERVED_TENANT_SLUGS"
      pattern: "RESERVED_TENANT_SLUGS"
    - from: "el-templo-api/test/migrations/0190-0191-tenants.test.ts"
      to: "INFORMATION_SCHEMA"
      via: "app.db.execute(sql`SELECT ... FROM INFORMATION_SCHEMA...`)"
      pattern: "INFORMATION_SCHEMA"
---

<objective>
La red que cierra el agujero de `test/setup.ts`: el provisioning de tests aplica las migraciones con
`SET FOREIGN_KEY_CHECKS=0` y **tolera errores** (lineas 202-213), asi que una migracion rota puede
pasar en silencio. Este plan escribe el test de introspeccion que verifica que el DDL de las tandas
A y B realmente quedo, y corre la regresion dirigida sobre los archivos que mas insertan anclas.

Purpose: sin este test, "las migraciones corrieron" no prueba nada. Es la unica verificacion
automatica de FUND-01 y FUND-02.
Output: `test/migrations/0190-0191-tenants.test.ts` verde + evidencia de que la suite existente no
se movio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/166-fundaci-n-tenants-anclas-y-scope-server-side/166-CONTEXT.md
@.planning/phases/166-fundaci-n-tenants-anclas-y-scope-server-side/166-PATTERNS.md
@CLAUDE.md

<working_directory>
Todo el codigo se escribe en `/home/franco/projects/et-166-tenancy` (rama `feat/166-tenancy-fundacion`).
No tocar el checkout principal. Nunca `git add -A`.
Si el plan 166-01 reservo numeros distintos de 0190/0191, renombrar el archivo de test en
consecuencia (`test/migrations/<A>-<B>-tenants.test.ts`) y ajustar los asserts de `_migrations`.
</working_directory>

<interfaces>
Simbolos que este plan consume (creados por 166-01 y 166-02):

- `schema.tenants`, `schema.tenantSettings`, `RESERVED_TENANT_SLUGS` (de `src/db/schema/tenants.ts`)
- `schema.users.tenantId`, `schema.branches.tenantId`
- Nombres de constraints/indices a assertar: `tenants_slug_unique`, `uq_tenant_setting`,
  `fk_tenant_settings_tenant`, `fk_users_tenant`, `fk_branches_tenant`, `idx_users_tenant_id`,
  `idx_branches_tenant_id`
- Helpers de test: `createTestApp`, `cleanAllTestData` de `test/helpers.ts`
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Test de introspeccion del DDL de las tandas A y B</name>
  <files>el-templo-api/test/migrations/0190-0191-tenants.test.ts</files>
  <read_first>
    - /home/franco/projects/et-166-tenancy/el-templo-api/test/migrations/0121-users-lead-fields.test.ts (analogo exacto: helper `getColumn`, asserts de FK con DELETE_RULE, asserts de indices por STATISTICS, assert de idempotencia sobre `_migrations`, ciclo beforeAll/beforeEach/afterAll)
    - /home/franco/projects/et-166-tenancy/el-templo-api/test/setup.ts lineas 165-230 (por que el provisioning tolera errores: el motivo de existir de este test)
    - /home/franco/projects/et-166-tenancy/el-templo-api/src/db/schema/tenants.ts
    - /home/franco/projects/et-166-tenancy/el-templo-api/src/db/migrations/0190_tenants_core.sql y 0191_tenant_anchors.sql
  </read_first>
  <action>
    Crear `test/migrations/0190-0191-tenants.test.ts` copiando la estructura de
    `0121-users-lead-fields.test.ts` (interfaces `ColumnRow`/`FKRow`/`CountRow`, helper `getColumn`,
    casts `as unknown as [Row[]]`, `createTestApp()` en `beforeAll`, `cleanAllTestData(app)` en
    `beforeEach`, `app.close()` en `afterAll`). Sin `any`.

    Bloques de test obligatorios:

    1. **Tabla `tenants` (FUND-01):** `status` tiene `COLUMN_TYPE` exactamente
       `enum('active','suspended','archived')`, `IS_NULLABLE='NO'`, `COLUMN_DEFAULT='active'`;
       `slug` es `varchar(50)` NOT NULL; `default_country` default `AR`, `default_currency`
       default `ARS`, `default_timezone` default `America/Argentina/Buenos_Aires`. Ademas: NO
       existe una columna llamada `tenant_status` (assert explicito de la trampa `mysqlEnum`).
    2. **Unique e indices de `tenants` / `tenant_settings`:** existen `tenants_slug_unique`,
       `uq_tenant_setting` (sobre `tenant_id` + `setting_key`, dos filas en STATISTICS con
       SEQ_IN_INDEX 1 y 2) y la FK `fk_tenant_settings_tenant` con
       `REFERENCED_TABLE_NAME='tenants'`.
    3. **Seed del tenant 1:** exactamente una fila en `tenants` con `id=1`, `slug='el-templo'`,
       `name='El Templo'`, `status='active'`; `tenant_settings` con 0 filas.
    4. **Anclas (FUND-02):** para `users` y para `branches`, `tenant_id` es `int`,
       `IS_NULLABLE='NO'` y `COLUMN_DEFAULT='1'`; existen `idx_users_tenant_id` /
       `idx_branches_tenant_id` y las FK `fk_users_tenant` / `fk_branches_tenant` apuntando a
       `tenants`; y `SELECT COUNT(*) FROM <t> WHERE tenant_id <> 1` devuelve 0.
    5. **Round-trip Drizzle / compatibilidad con codigo viejo:** insertar una branch con
       `app.db.insert(schema.branches).values({ name, code })` SIN `tenantId` y afirmar que la fila
       queda con `tenantId === 1`; idem un user via `app.db.insert(schema.users)` con los campos
       minimos y sin `tenantId`. Este test es la prueba viva de por que la columna lleva DEFAULT.
    6. **Idempotencia:** `SELECT COUNT(*) FROM _migrations WHERE name IN ('0190_tenants_core.sql','0191_tenant_anchors.sql')`
       devuelve 2, y cada nombre aparece una sola vez.
    7. **Slugs reservados (D-05):** `RESERVED_TENANT_SLUGS` incluye `admin`, `api` y `www`, y NO
       incluye `el-templo` (el slug sembrado no puede colisionar con la lista).

    Nada de este test debe dejar filas colgadas: borrar las filas que inserte (o apoyarse en
    `cleanAllTestData` del `beforeEach`) y NUNCA modificar `tenants.status` en este archivo (eso
    es del plan 166-05, que restaura el estado).

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-166-tenancy/el-templo-api && npx tsc --noEmit && MAX_TEST_WORKERS=1 npx vitest run test/migrations/0190-0191-tenants.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `MAX_TEST_WORKERS=1 npx vitest run test/migrations/0190-0191-tenants.test.ts` verde, con al menos 7 `it()`
    - El archivo contiene un assert explicito de que NO existe la columna `tenant_status` en `tenants`
    - El archivo contiene el assert de `COLUMN_DEFAULT='1'` para `users.tenant_id` y `branches.tenant_id`
    - El archivo contiene el insert Drizzle sin `tenantId` que afirma `tenantId === 1`
    - `grep -nE ':\s*any\b' test/migrations/0190-0191-tenants.test.ts` no devuelve lineas
    - `grep -c "UPDATE tenants" test/migrations/0190-0191-tenants.test.ts` devuelve `0` (este archivo no toca el status del tenant)
    - `npx tsc --noEmit` limpio
  </acceptance_criteria>
  <done>El DDL de las tandas A y B esta cubierto por un test que falla si una migracion se aplica a medias.</done>
</task>

<task type="auto">
  <name>Task 2: Regresion dirigida sobre las suites que mas insertan anclas</name>
  <files>ninguno (solo ejecucion y commit del test del Task 1)</files>
  <read_first>
    - /home/franco/projects/et-166-tenancy/el-templo-api/test/country-scope.test.ts (inserta branches y staff a mano: el archivo con mas riesgo ante un cambio en las anclas)
    - /home/franco/projects/et-166-tenancy/el-templo-api/test/helpers.ts lineas 426-479 (`createStaffUser`: inserta users por Drizzle sin `tenantId`)
    - .planning/phases/166-fundaci-n-tenants-anclas-y-scope-server-side/166-PATTERNS.md (seccion 0.3: por que estos archivos son el canario)
  </read_first>
  <action>
    Correr de forma DIRIGIDA (la suite completa corre en CI, no local) los archivos que insertan
    anclas a mano y que romperian primero si el `DEFAULT 1` no funcionara:
    `test/country-scope.test.ts`, `test/branch-access.test.ts`, `test/members/` y
    `test/finance/`, todos con `MAX_TEST_WORKERS=1`.

    Criterio: **cero cambios en los archivos de test existentes**. Si alguno falla, la respuesta
    correcta NO es ajustar la expectativa del test (eso viola el criterio de exito 4 de la fase):
    es corregir la migracion o el schema. Documentar en el SUMMARY cualquier fallo y su causa raiz.

    Commitear el test nuevo en el worktree, por ruta explicita, mensaje
    `test(166): DDL de tandas A y B (tenants, tenant_settings, anclas)`.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-166-tenancy/el-templo-api && MAX_TEST_WORKERS=1 npx vitest run test/country-scope.test.ts test/branch-access.test.ts test/members test/finance && git -C /home/franco/projects/et-166-tenancy status --porcelain el-templo-api/test | grep -v '^A ' | grep -v '^?? el-templo-api/test/migrations/' ; test $? -eq 1 && echo NO_TEST_EDITS</automated>
  </verify>
  <acceptance_criteria>
    - `MAX_TEST_WORKERS=1 npx vitest run test/country-scope.test.ts test/branch-access.test.ts test/members test/finance` verde
    - `git -C /home/franco/projects/et-166-tenancy diff --name-only origin/master -- el-templo-api/test` lista UNICAMENTE `el-templo-api/test/migrations/0190-0191-tenants.test.ts` (ni `setup.ts` ni `helpers.ts` ni ningun test existente fue modificado)
    - `git -C /home/franco/projects/et-166-tenancy log --oneline -1` muestra el commit `test(166): ...` con un solo archivo
  </acceptance_criteria>
  <done>La suite existente pasa sin ajustar expectativas: el camino A (DEFAULT 1) cumplio su promesa.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                        | Description                                                                                                 |
| ----------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| provisioning de tests -> DDL real               | `test/setup.ts` tolera errores de migracion, asi que "los tests pasan" no implica "el DDL quedo bien"       |
| DB de test por worker -> otros archivos de test | La DB es por worker y compartida entre archivos del mismo fork: dejar estado sucio contamina suites vecinas |

## STRIDE Threat Register

| Threat ID | Category          | Component                                           | Disposition | Mitigation Plan                                                                                                                       |
| --------- | ----------------- | --------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| T-166-11  | Tampering         | Migracion aplicada a medias sin que nadie se entere | mitigate    | Test de introspeccion sobre INFORMATION_SCHEMA (columnas, defaults, FK, indices) que no depende del resultado del runner              |
| T-166-12  | Denial of Service | Estado residual en la DB de test                    | mitigate    | `cleanAllTestData` en `beforeEach`, borrado de las filas insertadas y prohibicion explicita de tocar `tenants.status` en este archivo |
| T-166-13  | Repudiation       | "La suite pasa" ajustando expectativas              | mitigate    | Criterio de aceptacion que exige que el diff contra `origin/master` en `test/` sea exactamente un archivo nuevo                       |
| T-166-SC  | Tampering         | pnpm installs                                       | mitigate    | Cero paquetes nuevos; cualquier `pnpm add` requiere checkpoint humano bloqueante                                                      |

</threat_model>

<verification>
- El test nuevo pasa y cubre los 7 bloques
- Las suites dirigidas pasan sin modificar ningun archivo existente
- `npx tsc --noEmit` verde
</verification>

<success_criteria>
FUND-01 y FUND-02 quedan probados por automatizacion, no por inspeccion manual, y queda demostrado
que el staff no ve cambios (ningun test existente necesito ajuste).
</success_criteria>

<output>
Create `.planning/phases/166-fundaci-n-tenants-anclas-y-scope-server-side/166-03-SUMMARY.md` when done.
</output>
