---
phase: 181-dise-o-del-m-dulo-gimnasio-bloqueante
plan: 06
type: execute
wave: 6
depends_on: ["181-05"]
files_modified:
  - .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md
  - .docs/saas-multitenancy/README.md
autonomous: false
requirements: [DIS-01, DIS-02]

must_haves:
  truths:
    - "El doc afirma la frontera A1/A2 de forma verificable: tablas propias, cero imports SPOM, exercises intacta, acople limitado"
    - "Cada decisión del doc está trazada al requirement que habilita, de modo que las fases 182-192 no re-litiguen el addendum"
    - "El README de la serie deja de contradecir al doc 08: la decisión diferida de login queda cerrada y el trigger de split re-enunciado"
    - "El verificador estructural pasa en modo --final: cero stubs, 7 definiciones, trazas y constancias presentes"
    - "Franco firma el documento (D-09)"
  artifacts:
    - path: ".docs/saas-multitenancy/08-diseno-modulo-gimnasio.md"
      provides: "Documento de diseño completo y firmado"
      contains: "## Trazabilidad REQ → sección"
    - path: ".docs/saas-multitenancy/README.md"
      provides: "Índice de la serie actualizado, diferida cerrada, trigger re-enunciado"
      contains: "08-diseno-modulo-gimnasio.md"
  key_links:
    - from: ".docs/saas-multitenancy/README.md"
      to: ".docs/saas-multitenancy/08-diseno-modulo-gimnasio.md"
      via: "el índice de la serie y las decisiones diferidas apuntan al doc 08"
      pattern: "08-diseno-modulo-gimnasio"
    - from: "Tabla de trazabilidad"
      to: ".planning/REQUIREMENTS.md"
      via: "cada REQ ID de v1 que el diseño condiciona tiene su sección"
      pattern: "(CAT|RUT|REG|VAL|EVO|PROF|PLAT|ONB|DIS)-[0-9]{2}"
---

<objective>
Cerrar el documento de diseño: escribir la frontera A1/A2 y el "qué NO se construye", la tabla de
trazabilidad REQ → sección, las decisiones que heredan las fases 182-192; reconciliar el README de la serie
para que no contradiga al doc 08; correr el verificador en modo final; y obtener la firma de Franco (D-09),
que es el gate real de cierre de la fase.

Purpose: sin la trazabilidad, el criterio de éxito 4 de la fase no se cumple y las fases siguientes
re-litigan el addendum. Sin la reconciliación del README, el repo queda diciendo dos cosas distintas sobre
el trigger del split (que es exactamente lo que H-4 advierte).
Output: el doc 08 completo, sin stubs, formateado y firmado; el README actualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/181-CONTEXT.md

<restricciones_duras>

- **Cero código de producto.** Los únicos archivos que este plan toca son los dos `.md` de
  `.docs/saas-multitenancy/`.
- **Commit local, sin push.** El push a `staging` y a `master` es un gate humano (skill
  `el-templo-change-control`). Al terminar, se propone el push; no se ejecuta.
- **D-09:** Franco firma. No hay gate de aprobación de Nacho — a Nacho el doc le llega como información.
- `PENDIENTE` es palabra reservada del verificador: al final del plan no puede quedar ninguna ocurrencia en
  el doc.
  </restricciones_duras>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir frontera A1/A2, trazabilidad REQ → sección y decisiones heredadas</name>

<files>.docs/saas-multitenancy/08-diseno-modulo-gimnasio.md</files>

<read_first> - `.docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` completo (todo lo escrito por las olas 1-5: la trazabilidad se arma leyendo qué REQ cita cada sección) - `.planning/REQUIREMENTS.md` (los 37 REQ IDs de v1 y su mapeo a fases) - `.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/181-RESEARCH.md` líneas 553-568 (Don't Hand-Roll), 542-550 (anti-patrones), 662-687 (Open Questions 2, 5 y 6), 645-659 (Assumptions Log) - `.docs/saas-multitenancy/brief-fran-modulo-gimnasio.md` líneas 300-380 (addendum A1-A7: la frontera que el doc tiene que afirmar) - `el-templo-api/src/modules/shared/active-member.ts` (las variantes de lectura de `subscriptions`: el único acople permitido hacia el core además de las FKs) - `el-templo-api/src/db/schema/users.ts` líneas 1-60 (el `roleEnum` con sus 6 valores, ninguno de plataforma — insumo para la decisión heredada de PLAT-01)
</read_first>

  <action>
    Completar las tres secciones que quedan como stub.

    **`## Frontera A1/A2 y qué NO se construye`**

    (a) **La frontera, afirmada de forma verificable** (criterio de éxito 3 de la fase): el módulo Gimnasio
    tiene tablas propias (todas con prefijo `gym_`, las de la Definición 6) y rutas propias registradas con
    `moduleScope(app, "gimnasio", gimnasioRoutes, { prefix: "/api/gimnasio" })`; **cero imports desde o hacia
    el SPOM, en ninguna dirección**; `exercises` del SPOM no se toca (A2); y el acople hacia el core se
    limita a FKs sobre `users`, `branches` y `tenants` más la **lectura** de `subscriptions` a través de los
    helpers de `shared/active-member.ts` — nunca con un SELECT propio. Escribir cómo se verifica esto cuando
    haya código: el lint de tenancy, el manifiesto de rutas y las baterías ISO ya existen y son fail-closed.

    (b) **Qué NO se construye (tabla `Problema | No construir | Usar en su lugar | Por qué`)** con las
    entradas del research: filtrar por gimnasio (usar `tenantWhere` / `tenantOrGlobalWhere` / `tenantValues`
    / `assertTenant`, no un WHERE a mano); gate del módulo (usar `moduleScope` + `requireModule`, no un `if`
    por handler); leer un flag (usar `enabledModulesFor` / `isModuleEnabled` y el helper análogo para
    `gimnasio.*`, no una query propia); saber si un socio está activo (usar `activeMemberExists` y familia);
    barrer gimnasios en un cron (usar `forEachActiveTenant`); colores por tenant (usar `setCssVar`, no
    regenerar CSS por gimnasio); storage local con rama nativo/web (usar el patrón de `useTokenStorage.ts`);
    deduplicar el sync (usar la unique `(tenant_id, client_set_uid)` + upsert, no comparar por timestamp o
    contenido); numerar una migración (hand-written desde 0216, nunca `drizzle-kit generate`/`migrate`).

    (c) **Anti-patrones nombrados**, en lista corta: recalcular récords en un hook `event`; definir los
    tokens de marca de la app nueva como variables SCSS; resolver un tenant no resoluble con un default o un
    non-null assertion; referenciar el ejercicio planificado en el registro (se guarda el realizado); un flag
    por carpeta en vez de un módulo comercial grueso; `;` dentro de comentarios `--` en las migraciones.

    **`## Trazabilidad REQ → sección`**

    Una tabla markdown con las columnas `REQ | Qué decide el doc | Sección`, cubriendo **todos** los REQ IDs
    de v1 que este diseño condiciona: DIS-01, DIS-02, CAT-01 a CAT-08, RUT-01 a RUT-08, REG-01 a REG-05,
    VAL-01, VAL-02, EVO-01 a EVO-03, PROF-01 a PROF-03, PLAT-03 y ONB-01. Para los REQ de plataforma que el
    doc no decide (PLAT-01, PLAT-02, PLAT-04, PLAT-05) agregar una fila explícita indicando que son de las
    fases 182/183 y qué de este doc los condiciona (o "nada", si es el caso). Esta tabla es el cumplimiento
    literal del criterio de éxito 4: las fases 182-192 tienen que poder planificarse sin re-litigar el
    addendum A1-A7.

    **`## Decisiones heredadas por las fases 182-192`**

    Lo que queda abierto y a quién le toca, sin dejarlo implícito:
    - **Modelo del rol de plataforma (PLAT-01, fase 182):** el doc lo **fija** aunque la implementación sea de
      la 182, porque el criterio de éxito 4 pide que no se re-litigue. Los hechos verificados que acotan la
      decisión: `roleEnum` tiene 6 valores (`member`, `coach`, `admin`, `owner`, `gestion`, `recepcion`),
      ninguno de plataforma; y `users.tenant_id` es `NOT NULL DEFAULT 1` con `assertTenant` que **deniega**
      un tenant nulo — o sea que hoy un usuario **no puede** existir por encima de los tenants. Elegir y
      escribir una de las tres formas (usuario del tenant 1 con rol especial, tabla `platform_users` aparte,
      o tenant reservado de plataforma) con su costo.
    - **Dominio de plataforma sin elegir ni registrar (fase 182):** bloquea el cert wildcard, el DNS-01 y la
      función de CORS. Es precondición del wizard.
    - **Contrato de tipos API↔frontends:** decisión abierta del README que empeora con un quinto frontend
      (hoy los tipos se espejan a mano). El doc no la resuelve; la nombra como consecuencia asumida.
    - **Reglas de producto que Franco firma junto con el doc:** la sesión que sincroniza después del timeout
      de abandono no vuelve a `completada` (Definición 3), y el default `3` de `gimnasio.signals.easy_streak`
      es elección del diseño, no del brief (Definición 7).
    - **Asunciones a re-verificar antes de comprometerlas:** el `manifest.json` por subdominio (DIS-02) y el
      texto vigente de las App Store Guidelines si el doc se ejecuta semanas después de escrito.

  </action>

  <verify>
    <automated>grep -oE '(CAT|RUT|REG|VAL|EVO|PROF|PLAT|ONB|DIS)-[0-9][0-9]' .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md | sort -u | wc -l</automated>
  </verify>

<acceptance_criteria> - Las tres secciones existen sin la palabra `PENDIENTE` - `grep -oE '(CAT|RUT|REG|VAL|EVO|PROF|PLAT|ONB|DIS)-[0-9][0-9]' <doc> | sort -u | wc -l` devuelve al menos `35` - La tabla de trazabilidad contiene una fila por cada uno de: DIS-01, DIS-02, CAT-01..CAT-08, RUT-01..RUT-08, REG-01..REG-05, VAL-01, VAL-02, EVO-01..EVO-03, PROF-01..PROF-03, PLAT-03, ONB-01 - La sección de frontera contiene las cadenas `moduleScope`, `/api/gimnasio`, `active-member.ts`, `exercises`, y la frase de cero imports en ninguna dirección - La tabla "qué NO se construye" tiene al menos 8 filas y cada una nombra el helper o mecanismo concreto a usar - La sección de decisiones heredadas fija el modelo del rol de plataforma con una opción elegida (no una lista de opciones sin decidir) y cita `roleEnum` y `assertTenant` - `pnpm exec prettier --check .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` sale 0
</acceptance_criteria>

<done>La frontera está afirmada, cada REQ tiene su sección y lo que queda abierto tiene dueño y fase.</done>
</task>

<task type="auto">
  <name>Task 2: Reconciliar el README de la serie y cerrar el documento</name>

  <files>
    .docs/saas-multitenancy/README.md
    .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md
  </files>

<read_first> - `.docs/saas-multitenancy/README.md` completo (el índice de la serie 01-07, la sección de decisiones diferidas y la definición vigente del trigger de split) - `.docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` completo (H-3 y H-4 ya escritos: la redacción normativa del trigger sale de ahí, no se inventa una nueva) - `.docs/saas-multitenancy/07-receta-adopcion.md` línea 346 y siguientes (formato del registro de cambios de la serie)
</read_first>

  <action>
    (a) **Actualizar `.docs/saas-multitenancy/README.md`** con tres cambios puntuales, sin reescribir el
    archivo:
    1. Agregar `08-diseno-modulo-gimnasio.md` al índice de la serie, con una línea de descripción
       ("Diseño del módulo Gimnasio: las 7 definiciones del brief + la superficie member-facing; fase 181,
       milestone v6.1").
    2. Marcar la decisión diferida **"login / resolución de tenant / unicidad de email"** (abierta desde
       2026-07-01) como **CERRADA por el doc 08 §H-3**, con la fecha 2026-08-27 y una línea con el sentido de
       la resolución (resolución de tenant por hostname anterior a `attachScope`, login scoped por el tenant
       del host). No borrar la entrada: marcarla cerrada y apuntar al doc, para que la historia siga
       legible.
    3. Reemplazar la definición del **trigger del split de repos** por la redacción normativa que quedó
       escrita en el doc 08 §H-4, con una nota de que la 181 la re-enunció y por qué. El README y el doc 08
       tienen que decir exactamente lo mismo — que digan cosas distintas es el problema que H-4 identificó.

    (b) **Cerrar el doc 08:**
    1. Verificar que no queda ninguna ocurrencia de la palabra reservada `PENDIENTE`; si alguna sección
       quedó incompleta, completarla (no borrar el heading).
    2. Actualizar el bloque de estado del encabezado: el doc pasa de "en redacción" a completo, con la fecha
       y la constancia de que la firma es de Franco (D-09).
    3. Completar el `## Registro de cambios` con una entrada `2026-08-27 — Creación` de 3-5 líneas que
       resuma qué se decidió: las 4 precondiciones de plataforma (H-1..H-4), las 7 definiciones respondidas,
       y la superficie member-facing (dos superficies, app nueva del monorepo, subdominio por gimnasio, app
       container única en tiendas con branding en runtime).
    4. Correr `pnpm exec prettier --write` sobre los dos archivos.
    5. Correr el verificador en modo final:
       `bash .planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/verificar-doc-08.sh --final` y dejarlo
       en verde.

    (c) **Commit local** con los dos archivos stageados por ruta explícita (nunca `git add -A` ni `git add .`).
    Mensaje sugerido: `docs(181): diseño del módulo Gimnasio — 7 definiciones + superficie member-facing`.
    **No pushear**: proponer al usuario el push a `staging` y `master` como docs-only, y esperar.

  </action>

  <verify>
    <automated>bash .planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/verificar-doc-08.sh --final</automated>
    <automated>pnpm exec prettier --check .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md .docs/saas-multitenancy/README.md</automated>
  </verify>

<acceptance_criteria> - `bash .planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/verificar-doc-08.sh --final` sale con código 0 y todos sus checks en `OK:` - `grep -c 'PENDIENTE' .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` devuelve `0` - `.docs/saas-multitenancy/README.md` contiene la cadena `08-diseno-modulo-gimnasio.md` - `.docs/saas-multitenancy/README.md` marca cerrada la diferida de login/resolución de tenant y apunta al doc 08 (contiene `H-3` o `doc 08` en esa entrada) - La redacción del trigger de split en el README y en el doc 08 §H-4 coinciden en sentido: ambas contienen la misma condición disparadora, verificable leyendo las dos - `pnpm exec prettier --check` sobre los dos archivos sale 0 - `git log -1 --name-only` muestra exactamente los dos archivos de `.docs/saas-multitenancy/` (más los artefactos de planning que GSD commitee), y ningún archivo bajo `el-templo-api/`, `el-templo-app/`, `el-templo-admin/` o `el-templo-web/` - No se ejecutó ningún `git push`
</acceptance_criteria>

<done>El doc está completo, formateado, sin stubs y commiteado localmente; el README ya no contradice al doc 08.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Firma de Franco (D-09) — gate de cierre de la fase</name>

  <what-built>
    El documento de diseño `.docs/saas-multitenancy/08-diseno-modulo-gimnasio.md`, completo: sección de
    precondiciones de plataforma con los 4 hallazgos bloqueantes decididos (H-1 scope mixto del catálogo,
    H-2 categoría de manifiesto, H-3 resolución de tenant por hostname y login scoped, H-4 re-enunciado del
    trigger de split), las 7 definiciones del brief respondidas, la superficie member-facing decidida
    (DIS-02), el registro STRIDE del diseño, la frontera A1/A2, la trazabilidad REQ → sección y las
    decisiones que heredan las fases 182-192. Más el README de la serie reconciliado.
  </what-built>

  <how-to-verify>
    1. Abrir `.docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` y leerlo de punta a punta (es el
       entregable único de la fase; bloquea a las 11 fases siguientes).
    2. Confirmar las decisiones de plataforma de la sección de precondiciones: ¿la Opción A del catálogo
       (`tenant_id` NULLable, NULL = global) es la que querés? ¿El rename de `templo-module` a
       `feature-module` vale el trabajo, o preferís el fallback B2 (dejar el nombre histórico documentado)?
    3. Confirmar las tres reglas de producto que el doc te pide firmar explícitamente: (a) una sesión que
       sincroniza después del timeout de abandono acepta las series pero NO vuelve a `completada`; (b) el
       default `3` para "Fácil repetido" es elección del diseño, no del brief; (c) el récord se acumula
       contra el ejercicio realizado, no el planificado.
    4. Confirmar DIS-02: el nombre elegido para la app de alumnos, el timing de la primera publicación en
       tiendas, y que la constancia sobre el split de repos dice lo que querés que diga.
    5. Correr `bash .planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/verificar-doc-08.sh --final` y
       ver todos los checks en `OK:`.
    6. Si estás de acuerdo, decidir el push: los cambios son docs-only y van a `master` **y** a `staging`
       (nada de esto toca código ni migraciones). El push es tuyo, no del agente.
  </how-to-verify>

  <action>
    Pausar la ejecución y presentarle a Franco el documento terminado para su firma (D-09: Franco firma, sin
    gate de aprobación de Nacho — a Nacho el doc le llega como información). Mostrar la ruta del entregable,
    la salida del verificador en modo `--final`, y los cuatro puntos de `<how-to-verify>` como preguntas
    concretas. No avanzar, no pushear y no cerrar la fase hasta recibir la señal de reanudación. Si Franco
    lista correcciones, aplicarlas sobre el doc y volver a correr el verificador antes de repreguntar.
  </action>

  <verify>
    <automated>bash .planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/verificar-doc-08.sh --final</automated>
    <human-check>Franco leyó el doc completo y respondió "aprobado", o listó las correcciones a aplicar</human-check>
  </verify>

<acceptance_criteria> - El verificador en modo `--final` sale con código 0 antes de presentar el checkpoint - Franco respondió con la señal de reanudación ("aprobado") o con una lista de correcciones aplicadas y re-verificadas - No se ejecutó ningún `git push` durante el checkpoint
</acceptance_criteria>

<done>Franco firmó el documento; la fase 181 queda cerrada y las fases 182-192 pueden planificarse contra el doc 08.</done>

<resume-signal>Escribí "aprobado" para cerrar la fase 181, o listá los puntos a corregir</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                                    |
| --------------------------------------- | ---------------------------------------------------------------------------------------------- |
| doc 08 ↔ README de la serie             | Dos documentos que pueden contradecirse sobre el trigger de split y sobre decisiones diferidas |
| repo local → remotos `master`/`staging` | El push es el momento en que un cambio sale del control del autor: es gate humano en este repo |

## STRIDE Threat Register

| Threat ID | Category    | Component                                       | Disposition | Mitigation Plan                                                                                                                            |
| --------- | ----------- | ----------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| T-181-23  | Repudiation | Documentación contradictoria (doc 08 vs README) | mitigate    | La Task 2 reconcilia el README: índice actualizado, diferida marcada cerrada apuntando a §H-3, trigger re-enunciado con la misma redacción |
| T-181-24  | Tampering   | Push no autorizado a `master`/`staging`         | mitigate    | El plan prohíbe el push: se propone y espera OK explícito del usuario (staging-first estricto, skill `el-templo-change-control`)           |
| T-181-25  | Tampering   | `git add` amplio arrastrando trabajo ajeno      | mitigate    | Stage por ruta explícita de los dos archivos; `git add -A` y `git add .` prohibidos (el checkout principal es compartido)                  |
| T-181-26  | Repudiation | Doc entregado sin firma del responsable         | mitigate    | Checkpoint bloqueante de Franco (D-09) como gate real de cierre de la fase                                                                 |
| T-181-SC  | Tampering   | Instalación de paquetes                         | accept      | La fase no instala nada                                                                                                                    |

</threat_model>

<verification>
- `bash .planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/verificar-doc-08.sh --final` sale con código 0
- `grep -c 'PENDIENTE' .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` devuelve 0
- `grep -cE '^## Definición [1-7] —' .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` devuelve 7
- `grep -oE '(CAT|RUT|REG|VAL|EVO|PROF|PLAT|ONB|DIS)-[0-9][0-9]' <doc> | sort -u | wc -l` devuelve ≥ 35
- `git status --porcelain el-templo-api el-templo-app el-templo-admin el-templo-web` está vacío
- Checkpoint de Franco resuelto con "aprobado"
</verification>

<success_criteria>

- El doc 08 está completo, sin stubs, formateado, trazado a REQ y firmado por Franco
- El README de la serie no contradice al doc 08
- Cero código de producto tocado; commit local sin push
  </success_criteria>

<output>
Create `.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/181-06-SUMMARY.md` when done
</output>
