---
phase: 181-dise-o-del-m-dulo-gimnasio-bloqueante
plan: 04
type: execute
wave: 4
depends_on: ["181-03"]
files_modified:
  - .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md
autonomous: true
requirements: [DIS-01]

must_haves:
  truths:
    - "El doc nombra todas las tablas del módulo Gimnasio con sus columnas clave, FKs e índices — nivel entidades, no DDL completo (D-08)"
    - "La consulta crítica 'historial de este alumno en este ejercicio' está cubierta por un índice compuesto explícito"
    - "El doc le responde a Nacho el volumen de datos con un número, no con 'crece rápido'"
    - "Todo parámetro que el brief declara configurable tiene su key en tenant_settings con default; ninguno queda hardcodeado"
  artifacts:
    - path: ".docs/saas-multitenancy/08-diseno-modulo-gimnasio.md"
      provides: "Definición 6 (esquema e índices) y Definición 7 (mapa de tenant_settings)"
      contains: "idx_gym_set_logs_hist"
  key_links:
    - from: "Definición 6"
      to: "el-templo-api/src/db/tenant-tables.ts"
      via: "cada tabla nueva se clasifica o el CI queda rojo"
      pattern: "tenant-tables\\.ts"
    - from: "Definición 7"
      to: "el-templo-api/src/db/schema/tenants.ts (tenant_settings)"
      via: "las keys viven en el KV existente, no en una tabla nueva"
      pattern: "tenant_settings"
---

<objective>
Responder en el doc 08 las **Definiciones 6 y 7**: el esquema completo del módulo (entidades, columnas
clave, FKs e índices para las consultas críticas, con la estimación de volumen) y el mapa de parámetros
configurables en `tenant_settings`.

Purpose: la Definición 6 es el plano que las fases 184-190 traducen a Drizzle y a migraciones desde la
0216; la Definición 7 es lo que evita que un umbral quede hardcodeado y viole la regla dura 5 del milestone.
Output: las secciones `## Definición 6` y `## Definición 7` del doc, completas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/181-CONTEXT.md
@.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/181-PATTERNS.md

<restricciones_duras>

- **Cero código de producto y CERO migraciones.** No se crea ningún archivo bajo `el-templo-api/`. El doc
  describe el esquema; el Drizzle y el SQL los escriben las fases 184+.
- **D-08 fija el nivel de detalle:** entidades, columnas clave, FKs e índices para las consultas críticas.
  Ni DDL completo ni solo-conceptual. Snippets cortos con comentario del porqué, al estilo de
  `03-diseno-tenant-db-layer.md` §3.
- **Frontera A1:** las tablas del módulo solo pueden tener FK a `users`, `branches` y `tenants`. Ninguna FK
  a tablas del SPOM, y `exercises` no se toca (A2).
- **Migraciones del milestone reservan desde 0216** (producción está en 0215). El doc lo consigna; no crea
  ninguna.
- Commit local, sin push. `PENDIENTE` es palabra reservada del verificador.
  </restricciones_duras>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir la Definición 6 — entidades, columnas clave, FKs, índices y volumen</name>

<files>.docs/saas-multitenancy/08-diseno-modulo-gimnasio.md</files>

<read_first> - `.docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` (Definiciones 2 a 5 ya escritas: las columnas de agrupación, el `client_set_uid`, el `status` del catálogo y `gym_personal_records` se citan desde acá sin re-decidirlos) - `.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/181-RESEARCH.md` líneas 257-273 (Definición 6 con el índice y la estimación de volumen) y 77-93 (Architectural Responsibility Map) - `.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/181-PATTERNS.md` líneas 157-250 (convención de schema a espejar: `tenant-column.ts`, `tenants.ts`, `completed-sessions.ts`, y por qué este caso se aparta de los índices simples) - `el-templo-api/src/db/schema/completed-sessions.ts` (el analog de forma más cercano: tabla histórica gym-owned con `tenantIdColumn()`, FKs a `users`/`branches`, índices) - `el-templo-api/src/db/schema/tenant-column.ts` (el docblock canónico de "por qué esta columna es así": el tono a imitar al justificar el `tenant_id` NULLable del catálogo) - `.docs/saas-multitenancy/brief-fran-modulo-gimnasio.md` líneas 36-60 (§2.2 ficha del ejercicio: los campos que la tabla de catálogo tiene que poder guardar) y 98-186 (§3, §4 y §5: plantilla, asignación, registro por serie)
</read_first>

  <action>
    Reemplazar el stub de `## Definición 6 — Volumen de datos, esquema e índices` por el plano de datos del
    módulo. Contenido obligatorio:

    (a) **Inventario de tablas nuevas, todas con prefijo `gym_`**, presentadas en una tabla markdown con las
    columnas `Tabla | Qué guarda | Scope de tenancy | REQ que habilita`. El conjunto mínimo:
    `gym_exercises` (catálogo, scope MIXTO por H-1), `gym_routine_templates` +
    `gym_routine_template_days` + `gym_routine_template_exercises` (plantillas; las globales también son
    scope mixto por H-1), `gym_assigned_routines` + `gym_assigned_routine_days` +
    `gym_assigned_routine_exercises` (la copia inmutable del alumno, RUT-04 — y cómo una modificación del profe sobre una rutina en curso queda representada como modificación con fecha visible para el alumno, RUT-06), `gym_sessions` (estados
    REG-02), `gym_session_exercises` (planificado vs realizado + valoración VAL-01/VAL-02),
    `gym_set_logs` (el registro por serie, REG-03), `gym_personal_records` (EVO-01, ya definida en la
    Definición 4). Para el log de ediciones (REG-05), decidir explícitamente entre una tabla propia
    `gym_log_edits` o reusar el patrón de la tabla `audit_log` existente, y escribir el porqué de la
    elección.

    (b) **Para cada tabla: columnas clave, FKs e índices.** No DDL completo — el nivel de
    `03-diseno-tenant-db-layer.md`: snippet corto o lista con comentario del porqué. Reglas transversales a
    escribir una sola vez y aplicar a todas: todas llevan `id` autoincrement y `tenantIdColumn()`
    (`int NOT NULL DEFAULT 1 REFERENCES tenants.id`) **salvo** las de scope mixto de H-1, cuya excepción se
    nombra y se justifica con el mismo tono del docblock de `tenant-column.ts` (una decisión, su motivo, el
    trade-off descartado); las únicas FKs hacia afuera del módulo son a `users`, `branches` y `tenants`
    (frontera A1), y la lectura de `subscriptions` va por los helpers de `shared/active-member.ts`, nunca
    por FK.

    (c) **LA decisión de esta definición — denormalizar `user_id`, `exercise_id` y `performed_at` en la
    fila de `gym_set_logs`**, aunque los tres sean derivables de la sesión. Escribir el fundamento: sin eso
    la consulta crítica necesita 2-3 JOINs (`set_log → session_exercise → session → user`) y ningún índice
    puede cubrirla; con eso, un índice la resuelve entera. Y escribir por qué es seguro: los tres son
    inmutables una vez creada la fila; el único que "cambia" es `exercise_id` cuando el alumno sustituye el
    ejercicio (REG-04), y ahí lo correcto es que la fila se escriba con el ejercicio **realizado**, no que se
    actualice después.

    (d) **El índice de cobertura, escrito con su nombre:**
    `idx_gym_set_logs_hist (tenant_id, user_id, exercise_id, performed_at DESC)`. Explicar en prosa por qué
    ese orden de columnas: `tenant_id` primero por la convención de la fase 168 y porque el filtro de tenant
    es el primer término de todo WHERE; y qué sirve el mismo índice — el historial alumno×ejercicio
    (la consulta más frecuente según Nacho), el `MAX()` del recálculo de récords de la Definición 4, y
    EVO-02 ("qué hizo la última vez") con `ORDER BY performed_at DESC LIMIT 1`. Escribir explícitamente por
    qué esta tabla se aparta del patrón de índices simples de una columna de `completed_sessions`: ahí las
    consultas son independientes, acá filtran por tres columnas a la vez.

    (e) **Las otras uniques/índices que ya decidieron las definiciones anteriores** y que hay que consignar
    acá: unique `(tenant_id, client_set_uid)` en `gym_set_logs` (idempotencia del sync, Definición 3);
    unique `(tenant_id, user_id, exercise_id, metric)` en `gym_personal_records` (Definición 4); la unique
    de nombre canónico del catálogo con la clave de scope estable (Definición 2); e índice por
    `(tenant_id, user_id, status)` en `gym_sessions` para "una rutina activa por vez" (RUT-05) y para el
    barrido del timeout de abandono (REG-02).

    (f) **Estimación de volumen, con números y marcada como aritmética sobre supuestos de uso (no
    medición):** un alumno activo con 4 sesiones/semana × 6 ejercicios × 4 series ≈ 96 filas/semana ≈ 5.000
    filas/año; un gimnasio de 500 alumnos activos ≈ 2,5 M filas/año; uno de 1.000 ≈ 5 M/año. Con el índice
    de arriba, MySQL 8 resuelve el historial de un alumno×ejercicio en un rango de índice de decenas de
    filas, **independientemente del total de la tabla**. Escribir la decisión de **no particionar en v1** y
    dejar un umbral de revisión concreto (revisar particionado por rango de `performed_at` cuando la tabla
    pase el orden de las decenas de millones de filas o cuando el índice deje de entrar en el buffer pool).
    Y decir explícitamente que, por el guardrail "nada se borra", la tabla crece monótonamente: no habrá
    purga.

    (g) **Argumento cuantitativo que refuerza H-1 y que hay que repetir acá:** con la Opción A (una tabla de
    catálogo con `tenant_id` NULLable) la referencia desde `gym_set_logs` es una FK simple a
    `gym_exercises(id)` y el índice funciona; con dos tablas la referencia sería polimórfica
    (`exercise_scope` + `exercise_id`), sin integridad referencial y con peor selectividad.

    (h) **Encaje con los gates de CI:** cada tabla nueva se clasifica en `el-templo-api/src/db/tenant-tables.ts`
    (`GYM_OWNED_TABLES` o la nueva `TENANT_MIXED_SCOPE_TABLES` de H-1) o el test fail-closed deja el CI
    rojo. Y consignar que las migraciones del módulo **reservan desde 0216**, hand-written junto al schema
    Drizzle en el mismo commit, sin `;` dentro de comentarios `--`, y nunca con `drizzle-kit migrate`.

    Cerrar con `**✅ CERRADA (doc 08, 2026-08-27 — nivel de detalle según D-08)**` y la traza de REQ:
    CAT-01, RUT-02, RUT-04, RUT-05, RUT-06, REG-02, REG-03, REG-04, REG-05, EVO-01, EVO-02, EVO-03, PROF-03.

  </action>

  <verify>
    <automated>awk '/^## Definición 6 —/,/^## Definición 7 —/' .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md | grep -oE '(CAT|RUT|REG|VAL|EVO|PROF|PLAT|ONB|DIS)-[0-9][0-9]' | sort -u | wc -l</automated>
    <automated>grep -c 'idx_gym_set_logs_hist' .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md</automated>
  </verify>

<acceptance*criteria> - La sección `## Definición 6 —` existe, no contiene `PENDIENTE`, y tiene al menos 60 líneas - Contiene una tabla markdown con al menos 10 tablas nuevas, todas con prefijo `gym*`    - Contiene las cadenas literales`gym_exercises`, `gym_routine_templates`, `gym_assigned_routines`, `gym_sessions`, `gym_session_exercises`, `gym_set_logs`, `gym_personal_records`    - Contiene`idx_gym_set_logs_hist (tenant_id, user_id, exercise_id, performed_at DESC)`escrito de forma literal
    - Contiene la palabra`denormaliza`(o`denormalización`) aplicada a `user_id`, `exercise_id`y`performed_at`    - Contiene la estimación de volumen con los números`96`, `5.000`y`2,5 M`(o equivalentes explícitos), marcada como aritmética sobre supuestos
    - Contiene la decisión de no particionar en v1 con un umbral de revisión concreto
    - Contiene`tenant-tables.ts`, `TENANT_MIXED_SCOPE_TABLES`y el número de migración`0216`    - Contiene la constancia de la frontera A1: las únicas FKs hacia afuera son a`users`, `branches`y`tenants`    - El doc NO propone ninguna FK hacia`exercises`ni hacia otra tabla del SPOM:`grep -n 'references(() => exercises' <doc>`no devuelve nada
    -`awk`sobre la sección encuentra al menos 10 REQ IDs únicos
    -`pnpm exec prettier --check .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` sale 0
</acceptance_criteria>

<done>El plano de datos del módulo está escrito a nivel de entidades, columnas clave, FKs e índices, con la consulta crítica cubierta y el volumen respondido con números.</done>
</task>

<task type="auto">
  <name>Task 2: Escribir la Definición 7 — mapa de parámetros y branding en tenant_settings</name>

<files>.docs/saas-multitenancy/08-diseno-modulo-gimnasio.md</files>

<read_first> - `.docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` (estado actual) - `.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/181-RESEARCH.md` líneas 274-307 (Definición 7: mecánica verificada, mapa de keys y claves de branding) y 654-656 (asunciones A6 y A7) - `el-templo-api/src/db/schema/tenants.ts` líneas 60-110 (la tabla `tenant_settings`: KV con unique `uq_tenant_setting`) - `el-templo-api/src/modules/shared/modules.ts` líneas 30-90 (vocabulario `"true"`/`"false"`, divergencia deliberada respecto de `system_settings`, `parseModuleFlagKey`) - `el-templo-api/src/modules/shared/module-flags.ts` líneas 40-90 (cache in-memory con TTL 60s en runtime y 0 en test, `invalidateModuleFlags()`) - `.docs/saas-multitenancy/brief-fran-modulo-gimnasio.md` líneas 135-186 (§4.2 autogestión, §5.2 estados y timeout, §5.3 unidades, §5.4 plazo de edición) y 220-246 (§8 umbrales del panel del profe)
</read_first>

  <action>
    Reemplazar el stub de `## Definición 7 — Mapa de parámetros en tenant_settings` por la respuesta
    completa. Contenido obligatorio:

    (a) **La mecánica verificada, en 4-5 líneas:** `tenant_settings` es un KV
    `(tenant_id, setting_key, setting_value TEXT)` con unique `uq_tenant_setting`; ya existe y no se propone
    de nuevo. El vocabulario de los flags de módulo es `"true"`/`"false"`, **divergencia deliberada**
    respecto de `system_settings` que usa `'on'`/`'off'` — no mezclar los dos vocabularios es una orden del
    docblock del repo. El cache de flags es in-memory con TTL 60 s en runtime y 0 en test, con
    `invalidateModuleFlags()` exportada.

    (b) **Mapa de parámetros del módulo**, en una tabla markdown con las columnas
    `Key | Default | REQ | Fuente en el brief`, con estas ocho filas exactas:
    `module.gimnasio.enabled` (default `"false"`, fail-closed; el wizard de la 182 lo siembra en `"true"`,
    PLAT-03); `gimnasio.self_service.enabled` (`"false"`, RUT-07, §4.2 "apagado por default");
    `gimnasio.session.abandon_timeout_hours` (`"12"`, REG-02, §5.2);
    `gimnasio.log.edit_window_hours` (`"24"`, REG-05, §5.4);
    `gimnasio.signals.hard_streak` (`"3"`, PROF-02, §8 "umbral parametrizable, default 3");
    `gimnasio.signals.easy_streak` (`"3"`, PROF-02 — **marcar que el brief NO da default para "Fácil"** y
    que el 3 es una elección de este diseño, espejando el de "Difícil");
    `gimnasio.signals.inactivity_days` (`"14"`, PROF-02, §8 "default 14");
    `gimnasio.units.weight` (`"kg"`, REG-03, §5.3 "kg en v1, libras previsto").

    (c) **Claves de branding (D-12: config, nunca código)**, en una segunda tabla con las columnas
    `Key | Uso`: `brand.display_name` (nombre del gimnasio dentro de la app y en el `<title>` de la web);
    `brand.subdomain` (hostname del tenant, D-06 — **decidir explícitamente en el doc** si se deriva de
    `tenants.slug` en vez de duplicarse, y escribir la decisión); `brand.color.primary`,
    `brand.color.secondary`, `brand.color.accent` (aplicados en runtime con `setCssVar`);
    `brand.logo_url` (asset por tenant, con hogar natural en R2, que ya está montado como plugin);
    `brand.icon_key` (cuál del set pre-empaquetado de íconos alternativos usa la build nativa, D-11).

    (d) **Reglas de forma que el doc fija** y que las fases ejecutoras no pueden improvisar:
    - **Tres namespaces que coexisten en la misma tabla:** `module.` (del mecanismo de módulos),
      `gimnasio.` (parámetros del módulo) y `brand.` (transversal a cualquier módulo). No se mezclan.
    - **Todo valor es TEXT** ⇒ el parseo con default vive en **un solo helper por módulo** (el patrón
      `parseOrDefault` que ya usan streaks y finance contra `system_settings`). Un default duplicado en dos
      call sites es el bug clásico y hay que nombrarlo.
    - **Los defaults viven en código, no en la migración de seed:** un tenant nuevo sin ninguna fila tiene
      que funcionar. La migración del wizard (PLAT-03) escribe solo las que el owner elige distinto.
    - **Cache:** decidir y escribir explícitamente una de dos — o los `gimnasio.*` se cachean como los
      flags de módulo y entonces el diseño pide el helper de invalidación análogo a `invalidateModuleFlags()`,
      o no se cachean y una query por request es aceptable (y evita una clase entera de bugs). No dejar la
      decisión abierta.
    - **Regla dura 5 del milestone:** ningún parámetro del brief queda hardcodeado. Escribir que la lista se
      cerró releyendo §4.2, §5.2, §5.3, §5.4 y §8 del brief, y que cualquier parámetro que aparezca después
      entra a esta tabla, no al código.

    Cerrar con `**✅ CERRADA (doc 08, 2026-08-27)**` y la traza de REQ: PLAT-03, RUT-07, REG-02, REG-03,
    REG-05, PROF-02, DIS-02.

  </action>

  <verify>
    <automated>awk '/^## Definición 7 —/,/^## DIS-02/' .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md | grep -oE '(CAT|RUT|REG|VAL|EVO|PROF|PLAT|ONB|DIS)-[0-9][0-9]' | sort -u | wc -l</automated>
    <automated>bash .planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/verificar-doc-08.sh</automated>
  </verify>

<acceptance_criteria> - La sección `## Definición 7 —` existe, no contiene `PENDIENTE`, y tiene al menos 30 líneas - Contiene las 8 keys de parámetros escritas literalmente: `module.gimnasio.enabled`, `gimnasio.self_service.enabled`, `gimnasio.session.abandon_timeout_hours`, `gimnasio.log.edit_window_hours`, `gimnasio.signals.hard_streak`, `gimnasio.signals.easy_streak`, `gimnasio.signals.inactivity_days`, `gimnasio.units.weight` - Contiene las 7 keys de branding escritas literalmente: `brand.display_name`, `brand.subdomain`, `brand.color.primary`, `brand.color.secondary`, `brand.color.accent`, `brand.logo_url`, `brand.icon_key` - Contiene los defaults `"false"`, `"12"`, `"24"`, `"3"`, `"14"`, `"kg"` - Marca explícitamente que el default de `gimnasio.signals.easy_streak` es una elección de este diseño y no del brief - Contiene la advertencia de no mezclar el vocabulario `"true"`/`"false"` con el `'on'`/`'off'` de `system_settings` - Contiene una decisión escrita (no una pregunta abierta) sobre el cacheo de los `gimnasio.*` - Contiene una decisión escrita sobre si `brand.subdomain` se deriva de `tenants.slug` o se duplica - `awk` sobre la sección encuentra al menos 6 REQ IDs únicos - `bash .planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/verificar-doc-08.sh` reporta C2 verde (las 7 definiciones presentes) y C4 verde para las 7 - `pnpm exec prettier --check .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` sale 0
</acceptance_criteria>

<done>Todos los parámetros configurables del brief tienen key, default, REQ y fuente; ninguno queda hardcodeado y las reglas de forma están fijadas.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                          | Description                                                                                              |
| ------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| body de request → fila de cualquier tabla `gym_*` | `tenant_id` no puede llegar desde el borde; se inyecta server-side después del spread                    |
| escritura de `tenant_settings` → runtime          | Un flag mal escrito o un cache no invalidado deja un módulo prendido o apagado para el tenant equivocado |

## STRIDE Threat Register

| Threat ID | Category               | Component                                        | Disposition | Mitigation Plan                                                                                                                                         |
| --------- | ---------------------- | ------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-181-14  | Elevation of Privilege | Mass assignment de `tenant_id` en tablas `gym_*` | mitigate    | El doc fija `tenantValues` (pone `tenantId` después del spread) como única vía de escritura y prohíbe exponer la columna en cualquier schema de request |
| T-181-15  | Information Disclosure | Tabla nueva sin clasificar en `tenant-tables.ts` | mitigate    | El doc exige clasificar cada tabla `gym_*` en `GYM_OWNED_TABLES` o `TENANT_MIXED_SCOPE_TABLES`; el test fail-closed deja el CI rojo si falta            |
| T-181-16  | Information Disclosure | Flag de módulo mal parseado                      | mitigate    | `module.gimnasio.enabled` default `"false"` (fail-closed) y `"gimnasio"` registrado en `MODULE_NAMES` por H-2, para que la key no se ignore             |
| T-181-17  | Tampering              | Parámetro de seguridad hardcodeado               | accept      | Ninguno de los 8 parámetros es un control de seguridad (son de producto); el riesgo real es de negocio, y la regla dura 5 lo cubre                      |
| T-181-SC  | Tampering              | Instalación de paquetes                          | accept      | La fase no instala nada                                                                                                                                 |

</threat_model>

<verification>
- `bash .planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/verificar-doc-08.sh` reporta C2 (7 definiciones) y C4 (traza por definición) en verde
- `pnpm exec prettier --check .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` sale 0
- `git status --porcelain el-templo-api` está vacío (cero migraciones, cero schema)
</verification>

<success_criteria>

- Definición 6 entrega el plano de datos con entidades, columnas clave, FKs, índices, volumen y encaje con los gates de CI
- Definición 7 entrega el mapa completo de `tenant_settings` con defaults y las reglas de forma decididas
- Las 7 definiciones del brief quedan presentes y trazadas en el doc
  </success_criteria>

<output>
Create `.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/181-04-SUMMARY.md` when done
</output>
