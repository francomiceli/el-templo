---
phase: 181-dise-o-del-m-dulo-gimnasio-bloqueante
plan: 02
type: execute
wave: 2
depends_on: ["181-01"]
files_modified:
  - .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md
autonomous: true
requirements: [DIS-01]

must_haves:
  truths:
    - "El doc responde con decisión explícita y fundamento si Calistenia y Gimnasio comparten modelo de datos: NO, sin matriz comparativa, y explica por qué el caso peso-corporal-con-lastre no crea un puente (D-01)"
    - "El doc responde la pregunta de Nacho '¿el alumno ve un historial o dos?' con una sola palabra: uno, siempre — exclusión mutua como invariante (D-02)"
    - "El doc especifica qué pasa exactamente al promover un ejercicio local a global sin romper los registros históricos"
    - "El doc fija las taxonomías cerradas y el mapeo fijo de categoría validado, sin dejar ningún 'validar con Nacho' abierto (D-10)"
  artifacts:
    - path: ".docs/saas-multitenancy/08-diseno-modulo-gimnasio.md"
      provides: "Definición 1 y Definición 2 respondidas"
      contains: "## Definición 2 — Alcance global/local"
  key_links:
    - from: "Definición 2 del doc 08"
      to: "H-1 de la sección de precondiciones"
      via: "la opción de scope mixto ya está firmada arriba y no se re-litiga"
      pattern: "H-1"
    - from: "Definición 2 del doc 08"
      to: "brief §2.3 (taxonomías cerradas) y addendum A4"
      via: "listas cerradas + mapeo fijo de 7 categorías"
      pattern: "CAT-06"
---

<objective>
Responder en el doc 08 las **Definiciones 1 y 2** del brief: si Calistenia y Gimnasio comparten modelo de
datos (cerrada por D-01/D-02: no comparten, y el alumno ve un solo historial), y el modelo de alcance
global/local del catálogo con su ciclo de vida completo — copia local automática, promoción sin romper
historial, desactivación sin borrar, taxonomías cerradas y categoría derivada.

Purpose: la Definición 1 es la bloqueante del brief ("define todo lo demás") y la 2 es la que condiciona
las fases 184, 185 y 186 enteras. Sin las dos escritas, el catálogo se diseña dos veces.
Output: las secciones `## Definición 1` y `## Definición 2` del doc, completas y trazadas a REQ.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/181-CONTEXT.md
@.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/181-PATTERNS.md

<restricciones_duras>

- **Cero código de producto.** El único archivo que este plan toca es el doc 08.
- **D-01 es una orden explícita:** NO escribir una matriz comparativa unificar-vs-separar. La alternativa
  unificada no se evalúa; se escribe el fundamento de la decisión ya tomada.
- **D-02 es un invariante, no una pregunta abierta:** el caso híbrido (un tenant con módulo Templo Y módulo
  Gimnasio prendidos) no existe ni existirá. No se menciona como "posible v2".
- **D-10:** el mapeo de categorías del addendum A4 está VALIDADO (2026-08-27). El doc no arrastra ningún
  "validar con Nacho".
- Commit local, sin push. `PENDIENTE` es palabra reservada del verificador: no usarla en prosa.
  </restricciones_duras>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir la Definición 1 — modelo de datos Calistenia vs Gimnasio</name>

<files>.docs/saas-multitenancy/08-diseno-modulo-gimnasio.md</files>

<read_first> - `.docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` (estructura y precondiciones ya escritas) - `.docs/saas-multitenancy/brief-fran-modulo-gimnasio.md` líneas 284-300 (el enunciado literal de las 7 definiciones) y 300-380 (addendum A1-A7) - `.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/181-RESEARCH.md` líneas 184-193 (evidencia de código para el fundamento de la Definición 1) - `el-templo-api/src/db/schema/exercises.ts` líneas 20-90 (las columnas reales del árbol del SPOM) - `el-templo-api/src/modules/shared/module-registry.ts` (el docblock que explica por qué el guard responde 404 y no 403)
</read_first>

  <action>
    Reemplazar el stub de `## Definición 1 — ¿Calistenia y Gimnasio comparten modelo de datos?` por la
    respuesta completa. Estructura:

    (a) **Respuesta en una línea, arriba de todo:** no comparten modelo de datos. Son dos módulos separados
    con tablas propias, y ni siquiera se unen en la capa de presentación, porque por D-02 un tenant nunca
    tiene los dos prendidos.

    (b) **Fundamento con evidencia del código** (no opinión): `exercises` del SPOM no es un catálogo, es un
    árbol de progresión — sus columnas son `pattern`, `route`, `progression_step`, `dificultad_lineal`,
    `habilidad`, `canonical_exercise_id`, `milestone_exercise_id`, `effort` y `level` (enum de niveles
    griegos), y ninguna de ellas tiene sentido en un catálogo de musculación. A la inversa, ninguno de los
    campos de la ficha del §2.2 del brief (alias, grupo muscular secundario, equipamiento múltiple, tipo de
    carga, ejecución paso a paso, errores frecuentes, variantes fácil/difícil, alternativas, alcance,
    estado) existe en `exercises`. Sumar el hecho estructural: `exercises` lleva `tenant_id` y está en
    `GYM_OWNED_TABLES`, o sea que es "el árbol del SPOM del gimnasio X", no un catálogo de plataforma — el
    catálogo global del brief es estructuralmente otra cosa (cross-ref a H-1).

    (c) **La diferencia de forma que Nacho pide entender:** en Calistenia la sesión la genera un algoritmo a
    partir del árbol de progresión; en Gimnasio la sesión sale de una plantilla que se clona y se completa
    con un registro de series. Son dos ciclos de vida distintos, no dos vistas del mismo dato.

    (d) **El caso peso-corporal-con-lastre** (dominadas, fondos), que es donde Nacho dice que se va a notar:
    fichas independientes en cada catálogo, sin puente de datos. El fundamento verificable: `exercises` ya
    tiene una columna `equipment` tipada con el vocabulario del SPOM, y el catálogo nuevo tendrá su propia
    lista cerrada de 25 equipamientos del §2.3 — dos vocabularios que nunca se cruzan. Es exactamente el
    "fichas independientes sin puente de datos" del addendum A3.

    (e) **La respuesta a "¿el alumno ve un historial o dos?": uno, siempre.** Fundamento: exclusión mutua de
    módulos como invariante del diseño (D-02) — un tenant tiene `templo-training` ON o `gimnasio` ON, nunca
    los dos. Y eso ya está implementado de hecho: `enabledModulesFor` es fail-closed y el guard
    `requireModule` responde 404 (no 403) para no revelar que el feature existe, así que un alumno de un
    gimnasio con el módulo Gimnasio prendido ve un solo sistema sin una línea de código condicional extra.

    (f) Cerrar con el marcador `**✅ CERRADA (CONTEXT 2026-08-27, D-01 / D-02)**` y la traza de REQ que
    habilita: CAT-01, CAT-02, REG-03, DIS-01.

    No escribir una tabla comparativa unificar-vs-separar (D-01 lo prohíbe explícitamente).

  </action>

  <verify>
    <automated>grep -c '^## Definición 1 —' .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md</automated>
    <automated>awk '/^## Definición 1 —/,/^## Definición 2 —/' .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md | grep -cE '(CAT|RUT|REG|VAL|EVO|PROF|PLAT|ONB|DIS)-[0-9][0-9]'</automated>
  </verify>

<acceptance_criteria> - La sección `## Definición 1 —` existe, no contiene la palabra `PENDIENTE`, y tiene al menos 20 líneas de contenido - Contiene las cadenas literales `progression_step`, `canonical_exercise_id`, `GYM_OWNED_TABLES`, `equipment` - Contiene una respuesta explícita a la pregunta del historial con la palabra `uno` o `un solo historial` - Contiene la cadena `404` explicando por qué el guard no responde 403 - Contiene el marcador `✅ CERRADA` con las referencias `D-01` y `D-02` - `awk` sobre la sección encuentra al menos una coincidencia de REQ ID - La sección NO contiene una tabla con columnas del tipo "unificar" vs "separar" (prohibido por D-01): `grep -i 'unificar' <sección>` no devuelve una fila de tabla markdown - `pnpm exec prettier --check .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` sale 0
</acceptance_criteria>

<done>La definición bloqueante del brief está respondida con fundamento verificable en el código, y la pregunta del historial tiene una respuesta de una palabra.</done>
</task>

<task type="auto">
  <name>Task 2: Escribir la Definición 2 — alcance global/local, ciclo de vida del catálogo y taxonomías</name>

<files>.docs/saas-multitenancy/08-diseno-modulo-gimnasio.md</files>

<read_first> - `.docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` (H-1 ya firmado en precondiciones: no re-litigarlo) - `.docs/saas-multitenancy/brief-fran-modulo-gimnasio.md` líneas 24-100 (§2 completo: propiedad y alcance, ficha del ejercicio, taxonomías cerradas §2.3, reglas de generación, variantes y alternativas) y líneas 247-272 (guardrails y carga inicial) y 334-354 (addendum A4: la categoría derivada) - `.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/181-RESEARCH.md` líneas 194-203 (Definición 2) y 121-138 (H-1, para citarlo sin reabrirlo) - `.docs/saas-multitenancy/02-inventario-modulos.md` §2 (la contradicción heredada sobre "disponibilidad por tenant según equipamiento") - `el-templo-api/src/db/schema/tenants.ts` líneas 1-30 y 80-110 (enum de estado con comentario de semántica de negocio; `slug` único global)
</read_first>

  <action>
    Reemplazar el stub de `## Definición 2 — Alcance global/local, ciclo de vida del catálogo y taxonomías`
    por la respuesta completa. Contenido obligatorio:

    (a) **La forma elegida**, citando la decisión ya firmada en H-1 sin volver a abrir la comparación: una
    sola tabla `gym_exercises` con `tenant_id` NULLable, donde `NULL` significa "fila global de la
    plataforma" y un valor significa "ejercicio propio de ese gimnasio". Todas las lecturas del catálogo
    usan `tenantOrGlobalWhere`; todas las escrituras desde rutas de tenant usan `tenantValues` (nadie
    escribe una fila global desde una ruta de gimnasio). CAT-01 y CAT-02 quedan resueltos por la misma
    tabla.

    (b) **CAT-03 — copia local automática:** editar un ejercicio global desde un gimnasio NO muta el global;
    inserta una fila nueva con `tenant_id = <gimnasio>` y una columna `copied_from_exercise_id` apuntando al
    global. Escribir explícitamente que ese puntero es **informativo** (sirve para "esta es tu versión de
    X") y **no** una FK que la lectura tenga que seguir: si la lectura tuviera que resolverlo, el índice de
    la Definición 6 dejaría de cubrir la consulta crítica.

    (c) **CAT-04 — promoción local→global:** es un `UPDATE gym_exercises SET tenant_id = NULL WHERE id = ?`.
    El `id` no cambia, así que todos los `gym_set_logs` históricos que apuntan por FK a ese `id` no se
    enteran: cero registros rotos, cero migración de datos. Decir explícitamente que **no** es un
    DELETE + INSERT (el guardrail del brief es "nada se borra") y que la promoción es una acción de
    plataforma, no de tenant.

    (d) **CAT-05 — desactivación:** una columna `status` con los valores `borrador`, `publicado`,
    `desactivado` (del §2.2 del brief), NO un `deleted_at`. Escribir la semántica de cada valor en el estilo
    del enum de `tenants.ts` (una línea por valor, qué significa y qué NO hace). Regla de lectura: el
    buscador y la creación de rutinas nuevas excluyen `desactivado`; las lecturas de historial **no filtran
    por `status`**, así que un ejercicio desactivado sigue resolviendo todo el pasado.

    (e) **Unicidad de nombres y el caso borde de MySQL:** la unicidad natural es
    `(tenant_id, nombre_canonico)`, pero en MySQL una unique con una columna `NULL` **no impide duplicados**
    — dos filas globales con el mismo nombre canónico pasarían. Prescribir la salida concreta: una columna
    generada/estable de scope (por ejemplo `tenant_scope_key` con el valor `COALESCE(tenant_id, 0)`)
    incluida en la unique, de modo que la unicidad se aplique tanto dentro de un gimnasio como dentro del
    catálogo global. Este caso borde tiene que quedar escrito, porque una fase ejecutora distraída declara
    la unique y cree que la protege.

    (f) **CAT-06 — taxonomías cerradas del §2.3:** 14 grupos musculares, 25 equipamientos y 9 patrones, como
    listas cerradas validadas **en la carga** (ningún valor fuera de lista entra a la base), no como
    validación posterior. Y la **categoría** (7 valores: Pecho, Espalda, Hombros, Bíceps, Tríceps, Piernas,
    Core) es una **capa derivada por mapeo fijo** desde el grupo muscular primario, no un campo editable —
    con `antebrazo → Bíceps` y `cuello → Core`. Escribir que este mapeo quedó **validado el 2026-08-27**
    (D-10) y que no queda ningún pendiente de confirmación con Nacho.

    (g) **Aislamiento del buscador (CAT-07):** un gimnasio ve las filas globales y las propias, nunca las
    locales de otro gimnasio — incluido el buscador, que es superficie explícita del guardrail. Es el mismo
    `tenantOrGlobalWhere`, sin excepción.

    (h) **Corrección heredada que el doc salda:** `02-inventario-modulos.md` §2 describe el catálogo global
    con "disponibilidad por tenant según equipamiento/máquinas". El brief §2.3 lo prohíbe explícitamente
    ("ningún gimnasio configura qué máquinas tiene… quedó explícitamente fuera de alcance"). Manda el brief;
    dejar la corrección escrita para que nadie la reabra en la 184. La salida para el caso real es la de
    variantes/alternativas manuales del §2.6.

    (i) **Alcance de plantillas:** la misma decisión de scope mixto aplica a las plantillas de rutina
    globales (RUT-01), tal como fijó H-1 — una sola regla para las dos entidades.

    Cerrar con `**✅ CERRADA (doc 08, 2026-08-27 — apoyada en H-1 y D-10)**` y la traza de REQ: CAT-01,
    CAT-02, CAT-03, CAT-04, CAT-05, CAT-06, CAT-07, CAT-08, RUT-01.

  </action>

  <verify>
    <automated>awk '/^## Definición 2 —/,/^## Definición 3 —/' .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md | grep -oE '(CAT|RUT|REG|VAL|EVO|PROF|PLAT|ONB|DIS)-[0-9][0-9]' | sort -u | wc -l</automated>
    <automated>bash .planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/verificar-doc-08.sh</automated>
  </verify>

<acceptance_criteria> - La sección `## Definición 2 —` existe, no contiene `PENDIENTE`, y tiene al menos 40 líneas - Contiene las cadenas literales `tenantOrGlobalWhere`, `copied_from_exercise_id`, `SET tenant_id = NULL`, `borrador`, `publicado`, `desactivado`, `COALESCE(tenant_id, 0)` - Contiene los tres números de las taxonomías cerradas: `14` grupos musculares, `25` equipamientos, `9` patrones - Contiene las 7 categorías derivadas por nombre (Pecho, Espalda, Hombros, Bíceps, Tríceps, Piernas, Core) y los dos mapeos especiales `antebrazo` y `cuello` - Contiene la constancia de que el mapeo está validado (`D-10`) y NO contiene ninguna cadena del tipo `validar con Nacho` - Contiene el caso borde de la unique con `NULL` en MySQL con su mitigación concreta - Contiene la corrección explícita sobre la contradicción de `02-inventario-modulos.md` (equipamiento por tenant fuera de alcance) - `awk` sobre la sección encuentra al menos 8 REQ IDs únicos - `pnpm exec prettier --check .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` sale 0
</acceptance_criteria>

<done>El ciclo de vida completo del catálogo (crear, copiar, promover, desactivar, buscar) está escrito con la mecánica exacta y sin ningún pendiente de validación.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                        | Description                                                                            |
| ----------------------------------------------- | -------------------------------------------------------------------------------------- |
| buscador de catálogo → filas de otros gimnasios | Superficie de lectura donde un filtro mal escrito filtra ejercicios locales ajenos     |
| carga de ejercicios → taxonomías                | Entrada de usuario/agente que debe validarse contra listas cerradas antes de persistir |

## STRIDE Threat Register

| Threat ID | Category               | Component                            | Disposition | Mitigation Plan                                                                                                                                 |
| --------- | ---------------------- | ------------------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| T-181-06  | Information Disclosure | Buscador del catálogo (CAT-07)       | mitigate    | El doc fija `tenantOrGlobalWhere` como único filtro de lectura del catálogo, sin excepción, y lo nombra como superficie explícita del guardrail |
| T-181-07  | Tampering              | Promoción local→global (CAT-04)      | mitigate    | El doc la define como acción de plataforma (no de tenant) y como `UPDATE` que preserva el `id`; nunca DELETE + INSERT                           |
| T-181-08  | Tampering              | Validación de taxonomías (CAT-06)    | mitigate    | Listas cerradas validadas en la carga (schema de Fastify por ruta), no después; la categoría es derivada por mapeo fijo, no un campo editable   |
| T-181-09  | Tampering              | Unique con `tenant_id` NULL en MySQL | mitigate    | El doc prescribe incluir una clave de scope estable (`COALESCE(tenant_id, 0)`) en la unique para que la unicidad global se aplique de verdad    |
| T-181-SC  | Tampering              | Instalación de paquetes              | accept      | La fase no instala nada                                                                                                                         |

</threat_model>

<verification>
- `bash .planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/verificar-doc-08.sh` reporta C5/C6 verdes y C4 verde para las Definiciones 1 y 2
- `pnpm exec prettier --check .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` sale 0
- `git diff --name-only` incluye únicamente `.docs/saas-multitenancy/08-diseno-modulo-gimnasio.md`
</verification>

<success_criteria>

- Definición 1 respondida con fundamento en el código real y con la respuesta de "un solo historial"
- Definición 2 respondida con el ciclo de vida completo del catálogo, las taxonomías cerradas y el mapeo de categoría validado
- Ambas trazadas a REQ; cero pendientes de validación con Nacho
  </success_criteria>

<output>
Create `.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/181-02-SUMMARY.md` when done
</output>
