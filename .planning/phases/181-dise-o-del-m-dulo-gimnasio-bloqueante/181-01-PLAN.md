---
phase: 181-dise-o-del-m-dulo-gimnasio-bloqueante
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md
  - .planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/verificar-doc-08.sh
autonomous: true
requirements: [DIS-01, DIS-02]

must_haves:
  truths:
    - "Existe .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md con la estructura completa de secciones que la fase entrega — un solo documento siguiendo la serie 01-07 (D-07)"
    - "El verificador estructural corre y reporta verde/rojo de forma determinista, sin intervención humana"
    - "Los 4 hallazgos bloqueantes H-1..H-4 tienen decisión firmada y nombran los artefactos concretos del repo que hay que tocar"
    - "Un planner de la fase 184 puede leer la sección de precondiciones y saber qué categoría de tenant-tables.ts y qué helper usar sin re-litigar nada"
  artifacts:
    - path: ".docs/saas-multitenancy/08-diseno-modulo-gimnasio.md"
      provides: "Esqueleto del doc de diseño + sección Precondiciones de plataforma completa"
      contains: "## Precondiciones de plataforma"
      min_lines: 150
    - path: ".planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/verificar-doc-08.sh"
      provides: "Verificación estructural determinista del entregable"
      contains: "grep -cE '^## Definición"
  key_links:
    - from: ".docs/saas-multitenancy/08-diseno-modulo-gimnasio.md"
      to: "el-templo-api/src/db/tenant-tables.ts"
      via: "H-1 nombra la categoría nueva y su motivo"
      pattern: "tenant-tables\\.ts"
    - from: ".docs/saas-multitenancy/08-diseno-modulo-gimnasio.md"
      to: "el-templo-api/test/tenant-manifest.ts"
      via: "H-2 nombra la categoría de manifiesto y el rename del tipo"
      pattern: "tenant-manifest\\.ts"
    - from: ".docs/saas-multitenancy/08-diseno-modulo-gimnasio.md"
      to: ".docs/saas-multitenancy/README.md"
      via: "H-3 cierra la decisión diferida de login/dominios; H-4 re-enuncia el trigger de split"
      pattern: "README"
---

<objective>
Crear el documento de diseño `.docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` con su esqueleto
completo y resolver la sección que abre todo: **Precondiciones de plataforma (H-1..H-4)**, los cuatro
hallazgos bloqueantes que el RESEARCH detectó y que, sin decisión escrita acá, cada una de las fases
182-192 vuelve a chocar por separado.

Purpose: el doc 08 es el ÚNICO entregable de la fase 181 y bloquea a las 11 fases siguientes. Esta ola
funda el archivo, deja el verificador determinista listo para que todas las olas siguientes lo corran, y
firma las cuatro decisiones de plataforma que condicionan definiciones enteras del brief.
Output: el doc con encabezado, estado, resumen ejecutivo, todas las secciones declaradas (stubs para las
que completan las olas 2-6), la sección de precondiciones completa, y el script `verificar-doc-08.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/181-CONTEXT.md
@.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/181-PATTERNS.md
@.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/181-VALIDATION.md

<restricciones_duras>

- **Cero código de producto.** Esta fase NO crea migraciones, NO toca `el-templo-api`, `el-templo-app`,
  `el-templo-admin` ni `el-templo-web`, NO instala paquetes. El único código que se escribe es el script
  de verificación del propio documento, y vive en `.planning/`.
- **Commit local, sin push.** El push es un gate humano (staging-first estricto, skill
  `el-templo-change-control`). Commitear en la rama actual (`master` local, docs-only) y NO pushear.
- **El pre-commit (Husky + lint-staged) corre Prettier sobre todo archivo Markdown del repo.** Si el
  pre-commit reformatea el doc, se hace un commit nuevo — nunca `--amend`.
- **La palabra `PENDIENTE` es un marcador reservado del verificador.** No usarla en prosa del documento;
  para lo que queda abierto usar "abierto", "a definir en la fase 182" o similar.
- **No re-litigar decisiones cerradas del CONTEXT:** D-01 (A3 cerrada, sin matriz comparativa
  unificar-vs-separar), D-02 (exclusión mutua como invariante), D-04, D-05, D-06, D-07, D-08, D-09, D-10,
  D-11, D-12.
  </restricciones_duras>

<estructura_canonica_del_doc>
El doc lleva EXACTAMENTE estas secciones de nivel 2, en este orden. Las olas 2-6 completan las que esta
ola deja como stub. Ninguna ola agrega ni renombra secciones de nivel 2.

1. `# Fase 181 — Diseño del módulo Gimnasio (bloqueante)` (título H1) + bloque de estado `>` debajo
2. `## Resumen ejecutivo`
3. `## Precondiciones de plataforma (H-1..H-4)` — esta ola
4. `## Definición 1 — ¿Calistenia y Gimnasio comparten modelo de datos?` — ola 2
5. `## Definición 2 — Alcance global/local, ciclo de vida del catálogo y taxonomías` — ola 2
6. `## Definición 3 — Comportamiento offline` — ola 3
7. `## Definición 4 — Recálculo de récords personales` — ola 3
8. `## Definición 5 — Superseries y circuitos` — ola 3
9. `## Definición 6 — Volumen de datos, esquema e índices` — ola 4
10. `## Definición 7 — Mapa de parámetros en tenant_settings` — ola 4
11. `## DIS-02 — Superficie member-facing multi-tenant` — ola 5
12. `## Seguridad del diseño (STRIDE)` — ola 5
13. `## Frontera A1/A2 y qué NO se construye` — ola 6
14. `## Trazabilidad REQ → sección` — ola 6
15. `## Decisiones heredadas por las fases 182-192` — ola 6
16. `## Registro de cambios` — provisional en esta ola, final en la ola 6

Los títulos de las 7 definiciones deben empezar literalmente con `## Definición N — ` (N de 1 a 7, guion
largo) porque el verificador cuenta con `grep -cE '^## Definición [1-7] —'`.
</estructura_canonica_del_doc>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear el esqueleto del doc 08 y el verificador estructural</name>

  <files>
    .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md
    .planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/verificar-doc-08.sh
  </files>

<read_first> - `.docs/saas-multitenancy/04-mecanismo-modulos.md` líneas 1-25 (encabezado + bloque de estado + resumen ejecutivo: es el analog exacto de forma) y 238-282 (sección de decisiones + registro de cambios) - `.docs/saas-multitenancy/07-receta-adopcion.md` líneas 28-45 (formato de la tabla de precondiciones) y 270-320 (checklist copiable con `[ ]`) - `.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/181-PATTERNS.md` completo (convenciones documentales de la serie 01-07) - `.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/181-VALIDATION.md` completo (mapa de verificación por tarea) - `.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/181-RESEARCH.md` líneas 62-75 (Summary y recomendación primaria) y 707-744 (Validation Architecture: los greps exactos)
</read_first>

  <action>
    Crear el archivo `.docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` con:

    (a) Título H1 `# Fase 181 — Diseño del módulo Gimnasio (bloqueante)` seguido de un bloque de cita `>`
    con el estado, siguiendo la forma de `04-mecanismo-modulos.md` líneas 1-8 pero con el contenido real de
    esta fase: firma de **Franco** (D-09, sin gate de aprobación de Nacho — a Nacho le llega como
    información), qué responde el doc (las 7 definiciones del brief de Nacho 2026-07-24 + addendum A1-A7,
    y DIS-02), y la evidencia (RESEARCH.md de la fase 181: inventario de `tenant-tables.ts`,
    `tenant-manifest.ts`, `modules.ts`, `auth/routes.ts`, `deploy.yml`, más documentación oficial de
    Quasar, Apple, WebKit y Let's Encrypt). No copiar "VALIDADO con Nacho" del analog.

    (b) `## Resumen ejecutivo`: 4-6 líneas + una lista numerada con la forma de la solución ANTES del
    detalle. El contenido: el módulo Gimnasio es un módulo duro (A1) que nace dentro de la maquinaria de
    tenancy de v6.0; el doc abre con las precondiciones de plataforma (H-1..H-4) porque sin eso cada fase
    182-192 choca con lo mismo por separado; después responde las 7 definiciones y decide la superficie
    member-facing (dos superficies: staff en `el-templo-admin`, alumnos en una app nueva del monorepo).

    (c) Las 16 secciones de `<estructura_canonica_del_doc>` en ese orden exacto. Para las secciones que
    completan las olas 2-6, el cuerpo es una sola línea en cursiva con la palabra reservada:
    `_PENDIENTE — se completa en el plan 181-0X._` (con el número de plan correspondiente: 02 para
    Definiciones 1-2, 03 para Definiciones 3-5, 04 para Definiciones 6-7, 05 para DIS-02 y Seguridad,
    06 para Frontera/Trazabilidad/Heredadas).

    (d) `## Registro de cambios` con una entrada provisional `- **2026-08-27** — Creación.` (la ola 6 la
    completa).

    Crear también `.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/verificar-doc-08.sh`,
    ejecutable (`chmod +x`), bash con `set -uo pipefail` (NO `set -e`: el script acumula fallas y las
    reporta todas). Se corre desde la raíz del repo. Acepta el flag opcional `--final`. Variable `DOC`
    con default `.docs/saas-multitenancy/08-diseno-modulo-gimnasio.md`. Una función `ok`/`falla` que
    imprime `OK: <check>` o `FALLA: <check>` y suma a un contador de fallas; al final imprime el resumen y
    sale con 0 si el contador es 0, con 1 si no. Los checks:

    - C1: el archivo `$DOC` existe (si no existe, sale 1 inmediatamente).
    - C2: `grep -cE '^## Definición [1-7] —' "$DOC"` devuelve exactamente `7`.
    - C3: `grep -cE '^### H-[1-4] ' "$DOC"` devuelve exactamente `4`.
    - C4: trazabilidad por definición. Recorrer el doc con `awk` partiendo por líneas `^## `; para cada
      sección cuyo título empiece con `Definición`, verificar que su cuerpo contenga al menos una
      coincidencia de la expresión `(CAT|RUT|REG|VAL|EVO|PROF|PLAT|ONB|DIS)-[0-9][0-9]`. Reportar por
      nombre cada definición que no trace.
    - C5: `grep -q 'el-templo-app' "$DOC"` **y** `grep -qi 'no se transforma' "$DOC"` (constancia literal
      exigida por el criterio de éxito 2 / D-04).
    - C6: `grep -qi 'split de repos' "$DOC"` (constancia explícita del trigger, H-4).
    - C7: solo con `--final` — `grep -c 'PENDIENTE' "$DOC"` devuelve `0`.
    - C8: formato — correr `pnpm exec prettier --check "$DOC"`. Si el binario no está disponible
      (`command -v pnpm` vacío o el exec falla por binario faltante), imprimir `WARN: prettier no
      disponible` y NO contarlo como falla; si corre y el archivo no está formateado, sí es falla.

    El script no escribe ni modifica el doc: solo verifica.

  </action>

  <verify>
    <automated>bash .planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/verificar-doc-08.sh; test $? -le 1</automated>
    <automated>grep -cE '^## ' .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md</automated>
  </verify>

<acceptance_criteria> - `.docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` existe y su primera línea es exactamente `# Fase 181 — Diseño del módulo Gimnasio (bloqueante)` - La línea 3 (o la inmediata siguiente al título) empieza con `> **Estado:**` y contiene la cadena `D-09` - `grep -c '^## ' .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` devuelve `15` (las 15 secciones de nivel 2 de la estructura canónica; el H1 no cuenta) - `grep -cE '^## Definición [1-7] —' .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` devuelve `7` - El doc contiene las cadenas literales `## Resumen ejecutivo`, `## Precondiciones de plataforma`, `## DIS-02 — Superficie member-facing multi-tenant`, `## Seguridad del diseño (STRIDE)`, `## Registro de cambios` - El doc NO contiene la cadena `VALIDADO con Nacho` - `.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/verificar-doc-08.sh` existe, tiene bit de ejecución (`test -x`), y su primera línea es un shebang de bash - Correr `bash .planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/verificar-doc-08.sh` imprime una línea `OK:` o `FALLA:` por cada uno de los checks C2 a C6 y termina con un resumen; sale con código 1 mientras el doc esté incompleto (C3 falla en este punto, que es lo esperado antes de la Task 3) - Correr el script con `--final` incluye además el check C7 sobre la palabra `PENDIENTE` - El script no modifica el doc: `git diff --stat .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` no cambia entre dos corridas consecutivas
</acceptance_criteria>

<done>El archivo del doc existe con las 15 secciones de nivel 2 en el orden canónico y el script verificador corre y reporta checks deterministas.</done>
</task>

<task type="auto">
  <name>Task 2: Escribir las precondiciones H-1 (catálogo de scope mixto) y H-2 (categoría de manifiesto)</name>

<files>.docs/saas-multitenancy/08-diseno-modulo-gimnasio.md</files>

<read_first> - `.docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` (estado actual tras la Task 1) - `.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/181-RESEARCH.md` líneas 117-151 (H-1 y H-2 completos, con la tabla de opciones y sus costos) y 571-585 (Pitfalls 1 y 2) y 618-623 (Pitfall 8: los docblocks stale) - `el-templo-api/src/db/tenant-tables.ts` (las dos categorías actuales `GYM_OWNED_TABLES` y `TENANT_EXEMPT_TABLES`, y el docblock que explica que una exención sin motivo escrito no se acepta) - `el-templo-api/src/modules/shared/tenant.ts` líneas 1-60 y 140-170 (`TenantContext`, `assertTenant`, `tenantWhere`, `tenantValues`) - `el-templo-api/src/modules/shared/modules.ts` líneas 30-90 (`MODULE_NAMES`, el vocabulario `"true"`/`"false"`, `parseModuleFlagKey`, `DEFAULT_PUBLIC_TENANT_ID`) - `el-templo-api/test/tenant-manifest.ts` líneas 130-160 (las tres categorías y el comentario que declara que la cuarta es una decisión de diseño) - `.docs/saas-multitenancy/05-inventario-tablas-2026-07-26.md` §3 (el "club GLOBAL" y su primer miembro confirmado)
</read_first>

  <action>
    Reemplazar el stub de `## Precondiciones de plataforma (H-1..H-4)` por la sección real, y escribir dentro
    de ella las dos primeras subsecciones. La sección abre con 3-4 líneas explicando qué es una precondición
    de plataforma (algo que el diseño tiene que resolver ANTES de la fase 184 porque la maquinaria de v6.0 no
    puede expresarlo tal cual) y sigue con:

    `### H-1 — El catálogo global+local no tiene forma de expresarse con los helpers de tenancy actuales`

    Escribir el hecho verificado: `tenantWhere(table, ctx)` es un `eq` estricto sobre `tenantId`;
    `tenantIdColumn()` produce `int NOT NULL DEFAULT 1 REFERENCES tenants.id`; `src/db/tenant-tables.ts`
    clasifica cada tabla en exactamente dos baldes (`GYM_OWNED_TABLES`, 91 entradas contadas sobre el
    código — el docblock dice 88 y está stale; `TENANT_EXEMPT_TABLES`, 4: `tenants`, `tenant_settings`,
    `system_settings`, `labs_inquiries`) y el test `test/db/tenant-tables.test.ts` es fail-closed. Y el
    conflicto: el §2.1 del brief pide `WHERE tenant_id IS NULL OR tenant_id = ?`, que no es `tenantWhere`
    ni es "tabla exenta"; el sentinel de pool evalúa por QUERY mientras el lint evalúa por TABLA, así que
    una tabla de scope mixto dispara los dos si no se la clasifica a propósito.

    Incluir la tabla de opciones con las columnas `Opción | Forma | Promoción local→global (CAT-04) | Costo
    en la maquinaria v6.0` y las tres filas A (una tabla, `tenant_id` NULLable, NULL = global),
    B (dos tablas: global exenta + local gym-owned) y C (tenant plataforma con id reservado), con los costos
    tal como los estableció el research.

    **Decisión que el doc firma: Opción A.** Fundamento a escribir: es la única donde CAT-04 ("promover sin
    romper historial") es un `UPDATE ... SET tenant_id = NULL` que preserva el `id`, o sea un no-evento en
    vez de una migración de datos; el "club GLOBAL" del doc 05 §3 ya declara al catálogo genérico como su
    primer miembro confirmado; y con B la FK aguas abajo se vuelve polimórfica (`exercise_scope` +
    `exercise_id`), sin integridad referencial y con peor selectividad para el índice crítico de la
    Definición 6.

    La decisión debe nombrar los artefactos concretos que las fases ejecutoras tienen que crear, porque no
    pueden inventarlos por su cuenta:
    - Una **tercera categoría** en `el-templo-api/src/db/tenant-tables.ts` llamada
      `TENANT_MIXED_SCOPE_TABLES`, con el motivo escrito al lado de cada entrada (el precedente formal es
      `TENANT_GLOBAL_UNIQUES`, cuyo docblock exige exactamente eso).
    - Un **helper hermano** `tenantOrGlobalWhere(table, ctx)` en
      `el-templo-api/src/modules/shared/tenant.ts`, que produce `tenant_id IS NULL OR tenant_id = ?` y que
      es el único filtro admitido para leer esas tablas. Escribir explícitamente que las ESCRITURAS siguen
      usando `tenantValues` (nadie escribe una fila global desde una ruta de tenant).
    - Una **exención nominal** para esas tablas en el sentinel de pool y en el lint de tenancy, con su
      motivo.
    - Las dos entidades afectadas por esta decisión son `gym_exercises` (CAT-01/CAT-02) y las plantillas de
      rutina globales (RUT-01): la decisión es UNA sola para las dos.

    `### H-2 — El manifiesto de rutas tiene tres categorías y declara que la cuarta es una decisión de diseño`

    El hecho: `test/tenant-manifest.ts` define `CATEGORIAS` con `"tenant-scoped"`, `"global"` y
    `"templo-module"` y el comentario dice literalmente que no hay una cuarta y que agregarla es una
    decisión de diseño; el campo `modulo` está tipado como `ModuloTemplo`, alias de `MODULE_NAMES`, hoy con
    4 valores (`templo-training`, `templo-gamification`, `templo-marketing`, `templo-onboarding`); el gate
    `test/tenancy/iso-01-manifiesto.test.ts` corre con `ENTRADAS_BASELINE = 389` (el header del archivo dice
    374 y está stale — contar sobre el código, nunca sobre el docblock).

    El conflicto: para que `parseModuleFlagKey` reconozca `module.gimnasio.enabled`, `"gimnasio"` tiene que
    entrar a `MODULE_NAMES`, porque una key con nombre desconocido se **ignora en silencio** y el módulo
    quedaría OFF para siempre. Pero al entrar, una ruta del módulo Gimnasio quedaría clasificada
    `categoria: "templo-module"`, semánticamente falso: el módulo Gimnasio es lo contrario de un feature
    exclusivo de El Templo.

    **Decisión que el doc firma: opción B1** — renombrar la categoría `templo-module` a `feature-module` y
    el tipo `ModuloTemplo` a `ModuloFeature` en `el-templo-api/test/tenant-manifest.ts`, y agregar
    `"gimnasio"` a `MODULE_NAMES` en `el-templo-api/src/modules/shared/modules.ts` como **un solo módulo
    comercial grueso** (doc 04 §2.1: nunca un flag por carpeta). Escribir el fundamento: es un rename
    mecánico sobre las entradas actuales del manifiesto + el tipo + el gate, barato hoy y carísimo con ~300
    rutas del módulo Gimnasio mal clasificadas; y dejar nombrada la opción B2 (dejar el nombre histórico
    documentado) como fallback si el rename toca más superficie de la esperada (asunción A4 del research).
    Escribir también que la fase que ejecute el rename mueve `ENTRADAS_BASELINE` y que ese número se cuenta
    sobre el `Record`, no sobre rutas conceptuales.

    Cada subsección cierra con el marcador `**✅ CERRADA (doc 08, 2026-08-27)**` y cita los REQ que habilita
    (H-1: CAT-01, CAT-02, CAT-04, RUT-01; H-2: DIS-01, PLAT-03).

  </action>

  <verify>
    <automated>grep -cE '^### H-[12] ' .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md</automated>
    <automated>grep -c 'tenantOrGlobalWhere\|TENANT_MIXED_SCOPE_TABLES\|feature-module\|ENTRADAS_BASELINE' .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md</automated>
  </verify>

<acceptance_criteria> - El doc contiene las cadenas literales `### H-1 —` y `### H-2 —` (una sola vez cada una) - La sección `## Precondiciones de plataforma (H-1..H-4)` ya no contiene la palabra `PENDIENTE` - El doc contiene las cadenas `TENANT_MIXED_SCOPE_TABLES`, `tenantOrGlobalWhere`, `tenant-tables.ts`, `tenant-manifest.ts`, `MODULE_NAMES`, `feature-module`, `ModuloFeature`, `ENTRADAS_BASELINE` - H-1 contiene una tabla markdown de 3 filas de opciones (A, B, C) con al menos 4 columnas, y una frase que empieza declarando la Opción A como elegida - H-1 menciona explícitamente que la misma decisión aplica a las plantillas de rutina globales (RUT-01), no solo al catálogo - H-2 menciona que una key de módulo con nombre desconocido se ignora en silencio (el módulo quedaría OFF) - El doc NO afirma los números stale: no contiene la cadena `374 rutas` ni `88 tablas` - `grep -oE '(CAT|RUT|REG|VAL|EVO|PROF|PLAT|ONB|DIS)-[0-9][0-9]' <doc> | sort -u` incluye al menos `CAT-01`, `CAT-04`, `RUT-01` - `pnpm exec prettier --check .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` sale con código 0
</acceptance_criteria>

<done>H-1 y H-2 tienen decisión firmada, con la categoría, el helper y el rename nombrados por su identificador real en el repo, y ninguna fase ejecutora necesita inventarlos.</done>
</task>

<task type="auto">
  <name>Task 3: Escribir las precondiciones H-3 (resolución de tenant y login) y H-4 (trigger de split) + checklist copiable</name>

<files>.docs/saas-multitenancy/08-diseno-modulo-gimnasio.md</files>

<read_first> - `.docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` (estado actual tras la Task 2) - `.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/181-RESEARCH.md` líneas 152-179 (H-3 y H-4) y 586-592 (Pitfall 3) y 763-775 (patrones de amenaza: host header injection, tenant confusion, CORS) - `el-templo-api/src/modules/auth/routes.ts` líneas 600-680 (el login cross-tenant con `.limit(1)` y la exención `tenant-safe` embebida, deuda T-173-15) - `el-templo-api/src/db/schema/tenants.ts` líneas 80-110 (`slug` único global y `RESERVED_TENANT_SLUGS`, reservados por si la decisión diferida terminaba en subdominios) - `el-templo-api/src/db/schema/users.ts` líneas 285-300 (la unique `uq_users_tenant_email`) - `.docs/saas-multitenancy/README.md` líneas 340-385 (las decisiones diferidas: login/resolución de tenant/unicidad de email, el trigger del split de repos, y el contrato de tipos API↔frontends) - `.docs/saas-multitenancy/07-receta-adopcion.md` líneas 270-320 (formato del checklist copiable)
</read_first>

  <action>
    Escribir las dos subsecciones restantes de las precondiciones y cerrar la sección con el checklist.

    `### H-3 — El login es cross-tenant por email y toda superficie pública resuelve al tenant 1`

    Los dos hechos verificados: `POST /api/auth/login` busca el usuario sin selector de gimnasio,
    cross-tenant, con `.limit(1)`, y recién después deriva el `TenantContext` de la fila encontrada (deuda
    pre-existente T-173-15, con exención `tenant-safe` embebida en el SQL; el chequeo de duplicado de
    `/register` tiene la misma exención); y `users.email` dejó de ser único global en la fase 168, la unique
    es `uq_users_tenant_email (tenant_id, email)`. Consecuencia a escribir sin suavizar: en cuanto exista
    tenant 2, dos personas de gimnasios distintos con el mismo email son legales en la base y el login le
    entrega la sesión a la que MySQL devuelva primero. No es hipotético (un socio de El Templo que también
    entrena en el gimnasio nuevo; un profe que trabaja en dos) y rompe ONB-01. Es un problema de
    autenticación, no de UX. Sumar el segundo hecho: `DEFAULT_PUBLIC_TENANT_ID = 1` resuelve como tenant 1
    toda ruta sin `request.user`, y el propio guard documenta que ese default deja de ser válido cuando
    exista más de un tenant con superficie pública propia — que es exactamente D-06.

    **Decisión que el doc firma:** D-06 (subdominio por gimnasio) no es solo una URL bonita, es la solución
    de las dos deudas a la vez.
    - Una capa nueva de **resolución de tenant por hostname**, ANTERIOR a `attachScope`, que mapea el `Host`
      al `slug` de `tenants` (`RESERVED_TENANT_SLUGS` ya existe en el schema para esto). Escribir que se
      implementa sin cambiar la forma de `CountryScope` (asunción A9 del research: si hubiera que cambiarla,
      el costo sube y toca los call sites de `country-scope.ts`).
    - El login de la app de alumnos pasa a ser **scoped por el tenant resuelto del host**: la query deja de
      ser cross-tenant y `.limit(1)` deja de ser ambiguo.
    - Decidir y escribir qué pasa con el login de `el-templo-admin` y de `el-templo-app` en v1 (recomendación:
      quedan como están sobre el tenant 1 mientras el host no resuelva otro tenant, y migran al mismo
      mecanismo cuando su hostname entre al esquema) — la decisión tiene que quedar escrita, no implícita.
    - **Seguridad obligatoria (no opcional):** si el tenant se resuelve por `Host`, ese header es entrada del
      atacante. Mitigación a prescribir: nginx fija `server_name` y `proxy_set_header Host`, y la API valida
      el host contra la lista de `tenants.slug` (lookup exacto), nunca con un parseo confiado ni con
      `endsWith`. Un host no resoluble se rechaza — jamás se cae a `?? 1` ni a un non-null assertion
      (prohibido en todo el repo).
    - **Registro formal:** la decisión DIFERIDA del README de saas-multitenancy ("login / resolución de
      tenant / unicidad de email", abierta desde 2026-07-01) queda **CERRADA por este doc**. Escribirlo acá
      y dejarlo anotado como trabajo de la ola de cierre sobre el README.
    - Escribir en qué fase se ejecuta: la resolución por host y el login scoped tienen que estar en producción
      **antes** de la 192 (ONB-01), y son precondición del wizard de la 182.

    `### H-4 — El trigger del split de repos es, textualmente, la app que esta fase decide construir`

    El hecho: el README de saas-multitenancy define el trigger del split como "el nacimiento de la app de
    miembros multi-tenant, que ESTRENA el repo SaaS", quedando `el-templo-app` + `el-templo-web` como repo
    del tenant 1. Y D-05 dice que el split NO se adelanta. Por la definición vigente, construir la app de
    alumnos ES disparar el trigger, así que la contradicción no se puede dejar implícita: el criterio de
    éxito 2 de la fase exige constancia explícita.

    **Decisión que el doc firma:** re-enunciar el trigger, no solo declararlo intacto. Elegir UNA de las dos
    redacciones y escribirla como texto normativo que reemplaza al del README: (a) el trigger se cumplió y
    se decide deliberadamente NO ejecutarlo en v6.1, re-armándolo sobre los triggers secundarios (identidad
    comercial / equipo propio) y sobre el primer tenant que pague; o (b) el trigger se redefine: lo dispara
    la app **en tiendas con tenants pagos**, no su existencia en el repo. Recomendación: (b), porque es la
    que deja el criterio verificable. Escribir además que la app nueva nace en el monorepo (reusa CI, deploy
    y convenciones; mismo EC2, un vhost más) y que la decisión abierta **"contrato de tipos API↔frontends"**
    (hoy los tipos se espejan a mano con comentarios `// Mirrors el-templo-api/...`) **empeora con un quinto
    frontend** — el doc no la resuelve, la nombra como consecuencia asumida.

    Cerrar la sección de precondiciones con un **checklist copiable** en bloque de código (formato de
    `07-receta-adopcion.md` §6: líneas con `[ ]`, agrupadas por encabezado en MAYÚSCULAS), con lo que debe
    estar hecho antes de que arranque la 184: H-1 (categoría `TENANT_MIXED_SCOPE_TABLES` + helper
    `tenantOrGlobalWhere` creados y clasificados), H-2 (rename a `feature-module` + `"gimnasio"` en
    `MODULE_NAMES` + baseline del manifiesto actualizado), H-3 (resolución por host implementada, login
    scoped, validación de `Host` contra `tenants.slug`, README actualizado), H-4 (trigger re-enunciado en el
    README y en este doc). Cada ítem indica entre paréntesis la fase que lo ejecuta.

    Cada subsección cierra con `**✅ CERRADA (doc 08, 2026-08-27)**` y cita sus REQ (H-3: ONB-01, PLAT-03,
    DIS-02; H-4: DIS-02).

  </action>

  <verify>
    <automated>bash .planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/verificar-doc-08.sh</automated>
  </verify>

<acceptance_criteria> - `grep -cE '^### H-[1-4] ' .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` devuelve `4` - El doc contiene las cadenas `T-173-15`, `uq_users_tenant_email`, `DEFAULT_PUBLIC_TENANT_ID`, `RESERVED_TENANT_SLUGS`, `attachScope`, `proxy_set_header Host` - El doc contiene la cadena `split de repos` (verificable con `grep -qi 'split de repos'`) - H-3 declara explícitamente cerrada la decisión diferida del README sobre login/resolución de tenant/unicidad de email (contiene la cadena `README` y una forma de "cerrada"/"CERRADA") - H-3 prescribe que un host no resoluble se rechaza y prohíbe el fallback a tenant 1 (el doc NO contiene la cadena `?? 1` como recomendación; si aparece, es dentro de una prohibición explícita) - H-4 contiene una redacción normativa del trigger que reemplaza a la del README, no solo la frase "el trigger sigue intacto" - La sección de precondiciones incluye un bloque de código con al menos 8 líneas que empiezan con `[ ]` - `bash .planning/phases/.../verificar-doc-08.sh` reporta OK en los checks C3, C5 y C6 (los checks C2 y C7 siguen en rojo hasta las olas siguientes, lo cual es esperado) - `pnpm exec prettier --check .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` sale con código 0
</acceptance_criteria>

<done>Las cuatro precondiciones de plataforma tienen decisión escrita y firmada, con checklist copiable de lo que debe existir antes de la fase 184.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                     | Description                                                                                                               |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| navegador del alumno → API (por subdominio)  | El header `Host` pasa a ser insumo de resolución de tenant: entrada no confiable que decide de qué gimnasio son los datos |
| body de request → capa de datos              | `tenant_id` nunca puede viajar por el borde; se resuelve server-side                                                      |
| catálogo global (filas sin tenant) → tenants | Una tabla de scope mixto es la primera del módulo que se lee con un filtro que NO es `tenantWhere`                        |

## STRIDE Threat Register

| Threat ID | Category               | Component                                 | Disposition | Mitigation Plan                                                                                                                                                                            |
| --------- | ---------------------- | ----------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-181-01  | Information Disclosure | Tabla de catálogo de scope mixto (H-1)    | mitigate    | El doc prescribe categoría `TENANT_MIXED_SCOPE_TABLES` + helper único `tenantOrGlobalWhere` + exención nominal del sentinel/lint con motivo escrito; escrituras siempre por `tenantValues` |
| T-181-02  | Spoofing               | Resolución de tenant por `Host` (H-3)     | mitigate    | nginx fija `server_name` y `proxy_set_header Host`; la API valida el host contra `tenants.slug` con lookup exacto; host no resoluble se rechaza, jamás fallback a tenant 1                 |
| T-181-03  | Spoofing               | `POST /api/auth/login` cross-tenant (H-3) | mitigate    | El doc firma el login scoped por el tenant resuelto del host, y fija que debe estar en producción antes de ONB-01 (fase 192)                                                               |
| T-181-04  | Information Disclosure | Rutas del módulo mal clasificadas (H-2)   | mitigate    | `"gimnasio"` entra a `MODULE_NAMES` y sus rutas se clasifican en la categoría renombrada `feature-module`; toda ruta nueva entra al manifiesto fail-closed o CI rojo                       |
| T-181-05  | Tampering              | El propio entregable (doc de diseño)      | accept      | Es un `.md` en el repo, sin superficie ejecutable; el control es el commit firmado en git y el gate humano de Franco (D-09)                                                                |
| T-181-SC  | Tampering              | Instalación de paquetes                   | accept      | La fase NO instala nada (Package Legitimacy Audit del RESEARCH: cero paquetes nuevos; los mencionados aguas abajo ya están en el repo y salieron `[OK]` en npm)                            |

</threat_model>

<verification>
- `bash .planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/verificar-doc-08.sh` corre y reporta C3/C5/C6 en verde
- `pnpm exec prettier --check .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` sale 0
- `git status --porcelain` muestra solo los dos archivos de este plan como modificados/nuevos (cero código de producto tocado)
- `git diff --name-only` no incluye ningún archivo bajo `el-templo-api/`, `el-templo-app/`, `el-templo-admin/` ni `el-templo-web/`
</verification>

<success_criteria>

- El doc 08 existe con las 15 secciones de nivel 2 del orden canónico y stubs marcados para las olas 2-6
- Los 4 hallazgos H-1..H-4 tienen decisión firmada que nombra artefactos reales del repo (categoría, helper, rename, capa de host)
- El verificador estructural existe, es ejecutable y es determinista
- Cero archivos de producto modificados; commit local sin push
  </success_criteria>

<output>
Create `.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/181-01-SUMMARY.md` when done
</output>
