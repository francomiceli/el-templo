---
phase: 181-dise-o-del-m-dulo-gimnasio-bloqueante
plan: 05
type: execute
wave: 5
depends_on: ["181-04"]
files_modified:
  - .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md
autonomous: true
requirements: [DIS-02]

must_haves:
  truths:
    - "El doc dice dónde viven la ejecución y el registro del alumno, y dónde vive la superficie del staff, con constancia de que el split de repos no se adelanta y el trigger queda re-enunciado (D-05)"
    - "El doc deja constancia literal de que el-templo-app no se transforma"
    - "El doc especifica la arquitectura del branding en runtime y el modelo de publicación en tiendas, con la cita de la guideline que lo respalda"
    - "El doc nombra los cuatro bloqueos técnicos de la superficie (login, tenant público, CORS, cert wildcard) y qué decide sobre cada uno, incluida la resolución de tenant por hostname del subdominio por gimnasio (D-06)"
    - "El doc trata las amenazas que introduce la superficie nueva (host header, CORS laxo, enumeración de módulos) con mitigación concreta"
  artifacts:
    - path: ".docs/saas-multitenancy/08-diseno-modulo-gimnasio.md"
      provides: "Sección DIS-02 (superficie member-facing) y sección de seguridad del diseño"
      contains: "## DIS-02 — Superficie member-facing multi-tenant"
  key_links:
    - from: "DIS-02"
      to: "H-3 (resolución de tenant por hostname)"
      via: "el subdominio por gimnasio es lo que cierra la deuda de login"
      pattern: "H-3"
    - from: "DIS-02"
      to: "Definición 7 (claves brand.*)"
      via: "el branding se lee de tenant_settings y se aplica con setCssVar"
      pattern: "setCssVar"
---

<objective>
Escribir la sección **DIS-02 — Superficie member-facing multi-tenant** (el segundo requirement de la fase)
y la sección **Seguridad del diseño (STRIDE)**: dónde viven ejecución y registro, la app nueva del monorepo,
el subdominio por gimnasio, el branding en runtime, el modelo de publicación en tiendas, y el registro de
amenazas que la superficie nueva introduce.

Purpose: DIS-02 es el requirement que habilita las fases 189-192 y condiciona el trabajo de plataforma de la 182. Sin esta sección escrita, la app de alumnos se empieza sin saber su stack, su nombre, su dominio ni su
modelo de tenant.
Output: las secciones `## DIS-02 — Superficie member-facing multi-tenant` y `## Seguridad del diseño (STRIDE)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/181-CONTEXT.md

<restricciones_duras>

- **Cero código de producto.** NO se crea la app nueva, NO se scaffoldea nada, NO se instala nada, NO se
  toca `deploy.yml` ni ningún vhost. El doc **describe** la app que las fases siguientes construyen.
- **Decisiones cerradas del CONTEXT que esta sección escribe, no re-litiga:** D-03 (dos superficies: staff
  en `el-templo-admin`, alumnos en app nueva del monorepo con Quasar+Vue+Capacitor, destino dual web +
  nativo), D-04 (`el-templo-app` NO se transforma), D-05 (el split de repos no se adelanta), D-06
  (subdominio por gimnasio), D-11 (una sola app container multi-tenant, Opción A, branding por tenant en
  runtime), D-12 (branding 100% config por tenant, nunca código; PWA por subdominio en v1; white-label 4.2.6
  como add-on futuro fuera de v6.1).
- **Prohibido:** proponer clonar `el-templo-app` como punto de partida (hereda paleta SCSS de build-time,
  `pwa: false`, el `appId` de El Templo, y arrastra imports del SPOM violando A1).
- Commit local, sin push. `PENDIENTE` es palabra reservada del verificador.
  </restricciones_duras>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir la sección DIS-02 — superficie member-facing multi-tenant</name>

<files>.docs/saas-multitenancy/08-diseno-modulo-gimnasio.md</files>

<read_first> - `.docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` (H-3 y H-4 ya firmados en precondiciones; Definición 7 ya trae las claves `brand.*`) - `.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/181-RESEARCH.md` líneas 310-360 (DIS-02: las 4 apps reales, el costo exacto de sumar la quinta, los bloqueos S-1..S-4, el branding en runtime, la consecuencia colateral de push), 363-397 (Standard Stack con versiones reales del repo), 467-486 (estructura de proyecto recomendada), 542-550 (anti-patrones), 606-617 (Pitfalls 6 y 7), 650-658 (asunciones A3 y A8), 662-687 (Open Questions 2, 3 y 4) - `.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/181-CONTEXT.md` (D-03, D-04, D-05, D-06, D-11, D-12 textuales) - `el-templo-app/quasar.config.js` líneas 130-180 (el modo PWA apagado y los bloques de service worker comentados) - `el-templo-app/src/css/quasar.variables.scss` (la paleta como variables SCSS de build-time: el patrón que la app nueva NO debe copiar) - `el-templo-api/src/app.ts` líneas 120-160 (la allowlist estática de CORS)
</read_first>

  <action>
    Reemplazar el stub de `## DIS-02 — Superficie member-facing multi-tenant` por la sección completa.
    Contenido obligatorio, en subsecciones de nivel 3:

    (a) **Las dos superficies.** (i) STAFF de los tenants (profes/admins) en `el-templo-admin`, que ya es
    multi-tenant desde v6.0: las secciones del módulo Gimnasio se gatean por `module.gimnasio.enabled`, así
    que el staff de El Templo no las ve. Decidir y escribir la convención concreta: las páginas nuevas van
    en una subcarpeta `src/pages/gimnasio/` (hoy `el-templo-admin/src/pages` es plano salvo `tv/`), y dejar
    escrito que **gatear el nav por `enabledModules` de `/auth/me` es trabajo NUEVO** — el endpoint ya
    devuelve `enabledModules` pero ningún frontend lo consume todavía. (ii) ALUMNOS en una app nueva del
    monorepo.

    (b) **Corrección de conteo:** el monorepo tiene HOY **cuatro** apps, no tres — `el-templo-api` (Fastify),
    `el-templo-app` (Quasar+Vue+Capacitor, miembros del tenant 1), `el-templo-admin` (Quasar+Vue, web-only,
    staff) y `el-templo-web` (Nuxt 4 + @nuxt/content, landing `eltemplo.org`). La app de alumnos es la
    **quinta**. Consignar que `CLAUDE.md` dice "3 apps" y está desactualizado (la corrección de ese archivo
    no es trabajo de esta fase, pero queda anotada).

    (c) **Constancia literal exigida por el criterio de éxito 2 (D-04):** `el-templo-app` **no se
    transforma** — sigue siendo la app de miembros del tenant 1 y no se convierte en la app multi-tenant.
    Usar esa frase literal ("no se transforma") porque el verificador la busca. Y el anti-patrón: la app
    nueva **no** se crea clonando `el-templo-app`; nace del scaffold de Quasar y toma de `el-templo-app`
    **patrones puntuales** (el logger con `createLogger()`, el token storage con la rama
    `Capacitor.isNativePlatform()`, el boot de Sentry, el session player) — nunca el proyecto entero.

    (d) **Stack y estructura de la app nueva**, con las versiones que ya están en el repo: `quasar` ^2.16.0,
    `@quasar/app-vite` ^2.4.1, `vue` ^3.5.22, `pinia` ^3.0.4, `@capacitor/core` ^8.0.1 / `@capacitor/cli`
    ^8.1.0, `@capacitor/preferences` ^8.0.0, `axios` ^1.13.5, `@sentry/vue` ^10.38.0. Modos de
    `quasar.config.js`: `spa` + **`pwa: true`** (a diferencia de `el-templo-app`, que lo tiene apagado) +
    `capacitor`. Estructura por feature-module espejando `el-templo-app`: `src/boot/` con `sentry.ts` como
    PRIMER boot file, `src/css/` con los tokens de marca como **CSS custom properties, NUNCA variables
    SCSS** (las SCSS son build-time y matarían D-12), `src/stores/` con Pinia composition API,
    `src/composables/` que exponen `cleanup()` y no usan `onUnmounted` adentro, `src/modules/rutina|sesion|evolucion/`,
    `src/utils/logger.ts` con `createLogger()` — nunca `console.*`. `src-capacitor/` con `appId` y `appName`
    **de plataforma**, no de un tenant.

    (e) **Nombre de la app** (discreción explícita del CONTEXT, "proponer en el doc"): proponer al menos dos
    candidatos y **elegir uno**, con (i) el nombre del directorio en el monorepo, (ii) el nombre visible en
    las tiendas y (iii) el `appId` en reverse-DNS del dominio de plataforma. Restricción dura de D-11: el
    nombre en la tienda es de **plataforma**, neutro — no puede ser "El Templo" ni el de un gimnasio — y
    tiene que sostener el modelo "picker" de la guideline 4.2.6.

    (f) **Acceso por subdominio (D-06)** y su aprovisionamiento: `gimnasioX.<dominio-plataforma>`; la
    resolución de tenant por hostname ya quedó firmada en H-3 (cross-ref, no re-decidir). Escribir lo que
    falta: wildcard DNS + **certificado wildcard, que exige el challenge DNS-01 de Let's Encrypt**
    (HTTP-01 no emite wildcards), o sea un certbot con plugin DNS del proveedor y credenciales de API de DNS
    en el servidor — **no es opcional**: sin eso, cada gimnasio nuevo es un cert emitido a mano. Y un vhost
    **wildcard** de nginx, no uno por gimnasio. Consignar que **el dominio de plataforma no está elegido ni
    registrado** (`eltemplo.org` es del tenant 1 y no sirve: sería branding de El Templo para todos los
    gimnasios) y que de él dependen el cert, el DNS-01 y el CORS ⇒ es **precondición de la fase 182**
    (PLAT-02/PLAT-03), y hay que nombrarla porque la bloquea.

    (g) **CORS (bloqueo S-3):** hoy es una allowlist estática de 5 orígenes en `src/app.ts`. Con N
    subdominios hace falta `origin` como **función**, con un anclaje estricto del tipo
    `^https://[a-z0-9-]+\.<dominio-plataforma>$` — **nunca** un `endsWith`, porque un dominio del tipo
    `evil-<plataforma>.com` pasaría. Y consignar que **ningún vhost de front proxea `/api`**, así que el
    llamado siempre es cross-origin y la URL del API se hornea en build (`VITE_API_URL`, con su entrada en
    el `.env.example` correspondiente).

    (h) **Publicación en tiendas (D-11):** UNA sola app container multi-tenant, publicada desde la cuenta de
    la plataforma. Citar el texto vigente de la guideline **4.2.6** de Apple, que nombra explícitamente como
    aceptable "a single binary to host all client content in an aggregated or 'picker' model", y **4.3(a)**
    ("Don't create multiple Bundle IDs of the same app"), que es por qué N apps clonadas está prohibido.
    Consignar los límites de plataforma: `setAlternateIconName` solo permite íconos **pre-declarados en el
    bundle** (`CFBundleAlternateIcons`) y el `CFBundleDisplayName` **no se puede cambiar en runtime**, así
    que el ícono y el nombre en la home del teléfono y en la ficha de la tienda quedan genéricos de
    plataforma. **Detección de tenant en la app nativa:** decidir (i) pantalla "picker" de gimnasio — que
    además es el modelo que Apple nombra, o sea el más defendible en review — más (iii) recordar el último
    tenant en `Preferences`; y escribir que (ii) deep link / universal link desde el subdominio **no está
    disponible** porque los Associated Domains de iOS siguen sin configurar. **Timing de la primera
    publicación** (D-03 pide que el doc lo especifique): decidirlo explícitamente — recomendación, la
    superficie web/PWA primero (habilita ONB-01 sin depender de un review) y la build de tiendas después,
    con el criterio concreto que la dispara.

    (i) **Branding en runtime (D-12):** los colores se aplican con `setCssVar` de Quasar sobre las custom
    properties `--q-<name>` (nombres válidos: `primary`, `secondary`, `accent`, `dark`, `positive`,
    `negative`, `info`, `warning`), leyendo las claves `brand.*` de `tenant_settings` (Definición 7) y el
    logo desde R2. **Regla de diseño concreta y fácil de violar:** todos los tokens de marca propios de la
    app (fondos, sombras, acentos) tienen que ser CSS custom properties, porque las variables SCSS de
    `el-templo-app` son build-time y no se pueden cambiar en runtime — si se copian, el branding por tenant
    queda a medias. **Estrategia de marca en dos niveles:** v1, la PWA por subdominio es la vía de ícono y
    nombre propios en el teléfono vía "Agregar a inicio", con tenants ilimitados y sin releases; futuro
    fuera de v6.1, "tu app con tu marca en la tienda" como add-on premium por el modelo 4.2.6 (el gimnasio
    publica desde SU cuenta Apple/Google y paga sus fees, la plataforma automatiza la build) — patrón
    estándar de SaaS white-label. Y la consecuencia que esto impone hoy: **todo el branding es config por
    tenant, nunca código**, para que esa build futura sea configuración + assets, sin fork.
    **Riesgo abierto a escribir (asunción A3 del research):** la PWA por subdominio necesita un
    `manifest.json` distinto por tenant (nombre + íconos), servido dinámicamente por `Host` o generado por
    tenant. Escribir que esa viabilidad se **verifica antes de comprometer** la promesa de marca propia en
    v1, y cuál es el fallback si no se puede.

    (j) **Costo exacto de sumar la quinta app al monorepo (D-05, el split no se adelanta):** un filtro más
    en `dorny/paths-filter`, un job de build más en CI y deploy, un secret `*_DEPLOY_PATH` más, un paso de
    `rsync` más, un smoke test más, y **un vhost nginx más en el EC2 — trabajo manual por SSH, que es un
    gate humano**. Más la trampa conocida: el `paths-filter` usa `event.before`, así que un commit vacío o
    mal armado deploya un no-op de 11 s. Cross-ref a H-4 para el trigger del split.

    (k) **Consecuencia colateral a registrar:** las notificaciones push están dormidas para tenants ≠ 1
    hasta que exista la app de miembros multi-tenant; esta app **desbloquea** eso, aunque el push no esté en
    el alcance de v6.1 (el canal member-facing del SaaS sigue siendo email).

    Cerrar con `**✅ CERRADA (CONTEXT 2026-08-27 — D-03 / D-04 / D-05 / D-06 / D-11 / D-12)**` y la traza de
    REQ: DIS-02, PLAT-03, ONB-01, REG-01.

  </action>

  <verify>
    <automated>bash .planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/verificar-doc-08.sh</automated>
    <automated>grep -c 'no se transforma' .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md</automated>
  </verify>

<acceptance_criteria> - La sección `## DIS-02 — Superficie member-facing multi-tenant` existe, no contiene `PENDIENTE`, y tiene al menos 70 líneas - Contiene la frase literal `no se transforma` referida a `el-templo-app` (el verificador C5 pasa) - Contiene las cuatro apps actuales por nombre (`el-templo-api`, `el-templo-app`, `el-templo-admin`, `el-templo-web`) y declara que la de alumnos es la quinta - Contiene las cadenas `setCssVar`, `--q-primary` (o la lista de nombres válidos de Quasar), `pwa: true`, `CSS custom properties`, `@capacitor/preferences` - Contiene la cita de la guideline `4.2.6` con la expresión `picker` y la referencia a `4.3` - Contiene `setAlternateIconName` y la constancia de que `CFBundleDisplayName` no cambia en runtime - Contiene `DNS-01` y la constancia de que HTTP-01 no emite wildcards - Contiene una expresión de CORS anclada con `^https://` y la prohibición explícita de `endsWith` - Contiene `VITE_API_URL` y la constancia de que ningún vhost de front proxea `/api` - Contiene un nombre elegido para la app (directorio + nombre de tienda + `appId` en reverse-DNS), no solo una lista de candidatos - Contiene una decisión escrita sobre el timing de la primera publicación en tiendas - Contiene el riesgo abierto del `manifest.json` por subdominio con su fallback - Contiene la prohibición explícita de clonar `el-templo-app` como punto de partida - Contiene `paths-filter` y `event.before` - `awk '/^## DIS-02/,/^## Seguridad del diseño/'` sobre el doc encuentra al menos 3 REQ IDs únicos - `pnpm exec prettier --check .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` sale 0
</acceptance_criteria>

<done>DIS-02 está respondido: dónde vive cada superficie, con qué stack, bajo qué dominio, con qué branding, con qué modelo de tiendas y a qué costo en el monorepo.</done>
</task>

<task type="auto">
  <name>Task 2: Escribir la sección Seguridad del diseño (STRIDE)</name>

<files>.docs/saas-multitenancy/08-diseno-modulo-gimnasio.md</files>

<read_first> - `.docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` (DIS-02 y las precondiciones ya escritas: las mitigaciones se citan desde ahí, no se re-inventan) - `.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/181-RESEARCH.md` líneas 747-775 (Security Domain: categorías ASVS aplicables y los 8 patrones de amenaza con su mitigación estándar) - `el-templo-api/src/modules/shared/tenant.ts` líneas 1-60 (las dos capas de enforcement y la prohibición del fallback de tenant) - `el-templo-api/src/modules/shared/module-registry.ts` (por qué el guard responde 404 y no 403)
</read_first>

  <action>
    Reemplazar el stub de `## Seguridad del diseño (STRIDE)` por la sección completa. Contenido obligatorio:

    (a) **Fronteras de confianza del módulo**, en una tabla `Frontera | Qué cruza | Por qué importa`: el
    navegador del alumno hacia la API por subdominio (el header `Host` decide de qué gimnasio son los
    datos); el body de request hacia la capa de datos (`tenant_id` jamás viaja por el borde); el outbox
    offline hacia el endpoint de sync (`client_set_uid` y `recorded_at` son entrada del cliente); el
    buscador del catálogo hacia filas de otros gimnasios; y el profe hacia los datos de alumnos.

    (b) **Categorías ASVS aplicables**, en una tabla `ASVS | ¿Aplica? | Control en este repo`: V2
    Authentication (sí — el login cross-tenant de H-3 es una debilidad real en cuanto exista tenant 2, y
    este diseño la cierra); V3 Session Management (sí — refresh tokens; consignar que hay un problema
    conocido de replay de refresh y que la app nueva suma un cliente más al mismo mecanismo); V4 Access
    Control (**el eje central** — `attachScope` + `assertTenant`/`tenantWhere` + uniques compuestas +
    sentinel + lint + manifiesto + baterías ISO; el diseño **cabe ahí, no lo rodea**); V5 Input Validation
    (sí — schemas de Fastify por ruta; las taxonomías cerradas de CAT-06 son validación de entrada en la
    carga); V6 Cryptography (no directamente — el módulo no maneja secretos propios, nunca hand-roll); V7
    Error Handling & Logging (sí — Pino hacia Sentry; el log de ediciones de REG-05 puede seguir el patrón
    de la tabla `audit_log` existente); V13 API (sí — toda ruta nueva entra al manifiesto fail-closed o CI
    rojo).

    (c) **Registro STRIDE del diseño**, en una tabla `Amenaza | STRIDE | Componente | Disposición |
    Mitigación que el diseño prescribe`, con estas ocho entradas como mínimo, cada una apuntando a la
    sección del doc donde ya quedó decidida (no repetir el razonamiento, referenciarlo):
    - Cross-tenant data leakage (Information Disclosure) — el riesgo #1 del milestone: `tenantWhere` /
      `tenantOrGlobalWhere` como primer término de todo WHERE, sentinel por query, lint por tabla, batería
      ISO-03; el buscador del catálogo es superficie explícita del guardrail.
    - Mass assignment de `tenant_id` desde el body (Tampering / Elevation): `tenantValues` pone `tenantId`
      después del spread; la columna no se expone en ningún schema de request.
    - Tenant confusion en el login (Spoofing): login scoped por el tenant resuelto del host (H-3).
    - **Host header injection** (Spoofing) — amenaza NUEVA que introduce D-06: nginx fija `server_name` y
      `proxy_set_header Host`; la API valida el host contra `tenants.slug` con lookup exacto, nunca con un
      parseo confiado; host no resoluble se rechaza.
    - Module enumeration (Information Disclosure): `requireModule` responde **404**, no 403 — un módulo
      apagado es indistinguible de una ruta inexistente.
    - Privacidad profe→alumno (Information Disclosure): sin toggle de consentimiento por decisión de
      producto (brief §9); el control es que el profe solo ve alumnos **de su gimnasio**, que es access
      control de tenant y ya está cubierto por V4.
    - Replay del sync offline (Tampering): unique `(tenant_id, client_set_uid)` + upsert idempotente; el
      `client_set_uid` se valida por formato y **jamás** se usa para derivar el tenant.
    - **CORS demasiado laxo** (Elevation) — amenaza NUEVA que introduce D-06: la función de `origin` matchea
      con anclaje estricto, nunca con `endsWith`.

    (d) **Cierre con la regla que ordena todo:** el módulo Gimnasio no construye enforcement propio. Cabe
    dentro de las cinco capas de v6.0 o el CI lo frena; y el único fallback prohibido para siempre es
    resolver un tenant no resoluble con un default o un non-null assertion.

    Trazar la sección a DIS-01 y DIS-02.

  </action>

  <verify>
    <automated>awk '/^## Seguridad del diseño/,/^## Frontera A1/' .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md | grep -cE '^\|'</automated>
    <automated>bash .planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/verificar-doc-08.sh</automated>
  </verify>

<acceptance_criteria> - La sección `## Seguridad del diseño (STRIDE)` existe, no contiene `PENDIENTE`, y tiene al menos 30 líneas - Contiene tres tablas markdown (fronteras de confianza, categorías ASVS, registro STRIDE) - El registro STRIDE tiene al menos 8 filas y cada fila tiene una disposición explícita (`mitigate`, `accept` o `transfer`) - Contiene las cadenas `Host header`, `proxy_set_header Host`, `tenantValues`, `client_set_uid`, `404`, `ISO-03` - Contiene las categorías ASVS `V2`, `V3`, `V4`, `V5`, `V7`, `V13` - Contiene la prohibición explícita del fallback de tenant (default o non-null assertion) - Ninguna mitigación es genérica: cada fila nombra un helper, un header, un índice, un código de estado o una sección del doc - `pnpm exec prettier --check .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` sale 0
</acceptance_criteria>

<done>El diseño tiene su registro de amenazas escrito, con las dos amenazas nuevas que introduce el subdominio tratadas explícitamente.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                      | Description                                                                                |
| --------------------------------------------- | ------------------------------------------------------------------------------------------ |
| navegador/app del alumno → API (cross-origin) | Nuevo origen por cada subdominio de gimnasio; la allowlist estática de CORS deja de servir |
| header `Host` → resolución de tenant          | Entrada del atacante que decide de qué gimnasio son los datos                              |
| tienda de apps → binario único multi-tenant   | Un solo binario sirve a N gimnasios: el tenant se elige en runtime, no en build            |

## STRIDE Threat Register

| Threat ID | Category               | Component                                     | Disposition | Mitigation Plan                                                                                                                                                    |
| --------- | ---------------------- | --------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-181-18  | Spoofing               | Resolución de tenant por `Host` (D-06)        | mitigate    | El doc prescribe `server_name` fijo + `proxy_set_header Host` en nginx y validación del host contra `tenants.slug` con lookup exacto; host no resoluble se rechaza |
| T-181-19  | Elevation of Privilege | Función `origin` de CORS con N subdominios    | mitigate    | Anclaje estricto `^https://[a-z0-9-]+\.<dominio-plataforma>$`; `endsWith` prohibido explícitamente en el doc                                                       |
| T-181-20  | Information Disclosure | Módulo apagado consultado desde la app nueva  | mitigate    | `requireModule` responde 404, no 403: un módulo apagado es indistinguible de una ruta inexistente                                                                  |
| T-181-21  | Spoofing               | Selección de tenant en la app nativa (picker) | mitigate    | El picker solo selecciona a qué host apuntar; la autorización real la resuelve el servidor con el login scoped de H-3, nunca el cliente                            |
| T-181-22  | Information Disclosure | Assets de branding por tenant en R2           | accept      | Logo y colores son información pública del gimnasio; no hay datos sensibles en los assets de marca                                                                 |
| T-181-SC  | Tampering              | Instalación de paquetes                       | accept      | La fase no instala nada; el stack de la app nueva se prescribe con versiones ya presentes en el repo y auditadas `[OK]` en npm                                     |

</threat_model>

<verification>
- `bash .planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/verificar-doc-08.sh` reporta C5 (constancia de `el-templo-app` / `no se transforma`) y C6 (split de repos) en verde
- `pnpm exec prettier --check .docs/saas-multitenancy/08-diseno-modulo-gimnasio.md` sale 0
- `git status --porcelain` no muestra ningún directorio de app nuevo ni cambios en `.github/workflows/`
</verification>

<success_criteria>

- DIS-02 respondido con las dos superficies, el stack de la app nueva, su nombre, el subdominio, el branding en runtime, el modelo de tiendas y el costo en el monorepo
- Constancia literal de que `el-templo-app` no se transforma y de qué pasa con el split de repos
- Registro STRIDE del diseño con las dos amenazas nuevas del subdominio tratadas con mitigación concreta
  </success_criteria>

<output>
Create `.planning/phases/181-dise-o-del-m-dulo-gimnasio-bloqueante/181-05-SUMMARY.md` when done
</output>
