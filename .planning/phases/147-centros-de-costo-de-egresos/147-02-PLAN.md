---
phase: 147-centros-de-costo-de-egresos
plan: 02
type: execute
wave: 2
depends_on: ["147-01"]
files_modified:
  - el-templo-admin/src/types/transaction.ts
  - el-templo-admin/src/composables/useTransactionsApi.ts
  - el-templo-admin/src/components/caja/RegistrarMovEgresoDialog.vue
  - el-templo-admin/src/components/caja/MovEgresosTab.vue
autonomous: true
requirements: [EGR-02, EGR-03]

must_haves:
  truths:
    - "El dialog de egreso muestra un selector OBLIGATORIO de centro de costo, cargado por país"
    - "No se puede registrar un egreso sin elegir un centro de costo (botón deshabilitado)"
    - "El egreso registrado envía costCenterId al backend"
    - "La lista 'Movimientos de caja' muestra una columna 'Centro de costo' con el nombre del centro en las filas expense"
    - "Las filas no-expense muestran '—' en la columna Centro de costo"
  artifacts:
    - path: "el-templo-admin/src/composables/useTransactionsApi.ts"
      provides: "getCostCenters(country) → CostCenter[]"
      contains: "getCostCenters"
    - path: "el-templo-admin/src/components/caja/RegistrarMovEgresoDialog.vue"
      provides: "q-select obligatorio de centro de costo en la tab egreso"
      contains: "costCenter"
    - path: "el-templo-admin/src/components/caja/MovEgresosTab.vue"
      provides: "columna Centro de costo en la tabla del arqueo"
      contains: "centro"
  key_links:
    - from: "RegistrarMovEgresoDialog.vue"
      to: "GET /admin/finance/cost-centers"
      via: "transactionsApi.getCostCenters(country) en onShow"
      pattern: "getCostCenters"
    - from: "MovEgresosTab.vue"
      to: "MovEgresoItem.costCenterName"
      via: "columna body-cell que lee row.costCenterName"
      pattern: "costCenterName"
---

<objective>
Frontend de los centros de costo de egresos (EGR-02 selector + EGR-03 columna).
Agrega el selector obligatorio de centro de costo en la tab "Egreso" del dialog de
registro, y la columna "Centro de costo" en la lista del arqueo por caja. Mirrors de
los tipos del backend (Plan 01) + un método nuevo en el composable para listar centros.

Purpose: que el usuario clasifique cada egreso al registrarlo y vea el rubro en la lista.
Output: tipos + composable + dialog + tab.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/147-centros-de-costo-de-egresos/147-CONTEXT.md
@.planning/phases/147-centros-de-costo-de-egresos/147-01-PLAN.md

<interfaces>
<!-- Contratos del backend (Plan 01) que el front consume. NO explorar el codebase. -->

Backend (Plan 01):
GET /api/admin/finance/cost-centers?country=AR → CostCenterItem[] { id: number; name: string; country: string }
(owner pasa country = selectedCountry; non-owner lo omite y el server usa su scope).
POST /api/admin/finance/expenses body ahora EXIGE costCenterId: { cajaId, amount, costCenterId, notes? }.
MovEgresoItem ahora trae costCenterName: string | null (null para filas no-expense).

Frontend existente (el-templo-admin/src/types/transaction.ts):
RegisterExpenseInput { cajaId: number; amount: number; notes?: string | null } // agregar costCenterId: number
MovEgresoItem { ...; notes: string | null } // agregar costCenterName: string | null
CashBalancesParams { country?: 'AR' | 'ES' } // patrón de params por país

Composable (el-templo-admin/src/composables/useTransactionsApi.ts):
getCashRegisterBalances(params) → api.get('/admin/finance/cash-registers/balances', { params }) // patrón a copiar para getCostCenters
registerExpense(input) → api.post('/admin/finance/expenses', input)
El composable retorna un objeto con todos los métodos al final (~línea 558) — agregar getCostCenters ahí.

Dialog (el-templo-admin/src/components/caja/RegistrarMovEgresoDialog.vue):
props { modelValue, selectedCountry: 'AR'|'ES', isOwner }
onShow() → void loadCajas(); reactive egreso { cajaId, amount, notes }
canSubmitEgreso computed; submitEgreso() arma el input de registerExpense.
Cargá las cajas con getCashRegisterBalances({ country: isOwner ? selectedCountry : undefined }) — MISMO patrón para getCostCenters.

Tab lista (el-templo-admin/src/components/caja/MovEgresosTab.vue):
columns: QTableProps['columns'] (array). isEgreso(row) = kind === 'expense' || 'refund'.
Tiene template #body-cell-<name> por columna y un detail dialog con q-list de label/value.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tipos + composable getCostCenters</name>
  <read_first>
    - el-templo-admin/src/types/transaction.ts (RegisterExpenseInput ~454, MovEgresoItem ~392, CashBalancesParams ~380)
    - el-templo-admin/src/composables/useTransactionsApi.ts (getCashRegisterBalances ~199, return object ~558)
  </read_first>
  <files>el-templo-admin/src/types/transaction.ts, el-templo-admin/src/composables/useTransactionsApi.ts</files>
  <action>
    En `transaction.ts`:
    - Agregar `costCenterId: number` (required) a `RegisterExpenseInput`.
    - Agregar `costCenterName: string | null` a `MovEgresoItem` (mirror del backend Plan 01).
    - Definir `export interface CostCenter { id: number; name: string; country: string }` (mirror de CostCenterItem).
    - Definir `export interface CostCenterParams { country?: 'AR' | 'ES' }` (mismo patrón que CashBalancesParams).

    En `useTransactionsApi.ts`: agregar el método `getCostCenters(params: CostCenterParams = {}): Promise<CostCenter[]>`
    copiando el patrón de `getCashRegisterBalances` (api.get('/admin/finance/cost-centers', { params }),
    set loading/error, extractError 'Error cargando centros de costo'). Importar los tipos nuevos. Agregar
    `getCostCenters` al objeto de retorno del composable (junto a getCashRegisterBalances).

  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm exec vue-tsc --noEmit 2>&1 | grep -E "transaction.ts|useTransactionsApi.ts" | head -5; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - vue-tsc no reporta errores en transaction.ts ni useTransactionsApi.ts.
    - getCostCenters está exportado por el composable y tipado a CostCenter[].
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Selector obligatorio de centro de costo en el dialog de egreso</name>
  <read_first>
    - el-templo-admin/src/components/caja/RegistrarMovEgresoDialog.vue (completo: onShow/loadCajas patrón, reactive egreso, canSubmitEgreso, submitEgreso, resetAll)
  </read_first>
  <files>el-templo-admin/src/components/caja/RegistrarMovEgresoDialog.vue</files>
  <action>
    Agregar estado: `costCenters = ref<CostCenter[]>([])`, `loadingCostCenters = ref(false)`. Agregar
    `costCenterId: null as number | null` al reactive `egreso`. Importar CostCenter del types.

    Función `loadCostCenters()` (espejo de loadCajas): `transactionsApi.getCostCenters({ country: props.isOwner ? props.selectedCountry : undefined })`,
    setear costCenters.value. Llamarla en `onShow()` junto a `void loadCajas()`. `costCenterOptions` computed
    = costCenters.value.map(c => ({ label: c.name, value: c.id })). Default razonable: tras cargar, si
    egreso.costCenterId es null, preseleccionar el centro cuyo name === 'Varios' (o el primero si no está) —
    el usuario puede cambiarlo. Registrar en AUTONOMOUS-DECISIONS-v5.3.md: "Varios" queda preseleccionado
    como escape, no forzado silenciosamente (el usuario ve y puede cambiar el valor), per decisión 4 del CONTEXT.

    En el template, dentro de la tab "egreso" (después del q-select de Caja, antes del Monto), agregar un
    q-select `v-model="egreso.costCenterId"` con `:options="costCenterOptions"` emit-value map-options,
    label "Centro de costo *", dense outlined, y un loading state cuando loadingCostCenters.

    `canSubmitEgreso`: agregar la condición `egreso.costCenterId !== null`. En `submitEgreso()` pasar
    `costCenterId: egreso.costCenterId as number` al `transactionsApi.registerExpense({ ... })`. En `resetAll()`
    resetear `egreso.costCenterId = null`.

  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm exec vue-tsc --noEmit 2>&1 | grep -E "RegistrarMovEgresoDialog" | head -5; echo "checked"</automated>
  </verify>
  <acceptance_criteria>
    - vue-tsc sin errores en RegistrarMovEgresoDialog.vue.
    - El botón "Registrar egreso" queda deshabilitado mientras costCenterId sea null.
    - registerExpense se llama con costCenterId.
    - "Varios" aparece preseleccionado al abrir la tab egreso (cambiable).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Columna "Centro de costo" en la lista del arqueo</name>
  <read_first>
    - el-templo-admin/src/components/caja/MovEgresosTab.vue (columns ~417, templates #body-cell, isEgreso ~331, detail dialog q-list ~168)
  </read_first>
  <files>el-templo-admin/src/components/caja/MovEgresosTab.vue</files>
  <action>
    Agregar una columna `{ name: 'centro', label: 'Centro de costo', field: 'costCenterName', align: 'left', sortable: false }`
    al array `columns` (ubicarla después de 'caja'). Agregar un template `#body-cell-centro` que renderice
    `row.costCenterName || '—'` (las filas no-expense traen costCenterName null → muestran '—', sin signo ni badge).
    En el detail dialog (q-list), agregar un q-item "Centro de costo" que muestre `detailRow.costCenterName || '—'`
    (mostrarlo siempre o solo cuando isEgreso(detailRow) — preferí mostrarlo cuando costCenterName no es null,
    con v-if, para no ensuciar las filas de cobros/movimientos). NO tocar el filtro Tipo, el export Excel
    (la columna en el .xlsx queda DIFERIDA — EGR-03 pide solo "la lista muestra"; registrar esta decisión en
    AUTONOMOUS-DECISIONS-v5.3.md), ni la lógica de cobros/ARQUEO existente.
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm exec vue-tsc --noEmit 2>&1 | grep -E "MovEgresosTab" | head -5; echo "checked"</automated>
  </verify>
  <acceptance_criteria>
    - vue-tsc sin errores en MovEgresosTab.vue.
    - La tabla muestra la columna "Centro de costo"; filas expense con el nombre del centro, otras con '—'.
    - El detail dialog muestra el centro de costo cuando la fila lo tiene.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                                           |
| ---------------------- | ------------------------------------------------------------------------------------- |
| admin UI → finance API | El selector arma el costCenterId del body; el server (Plan 01) revalida exists+active |

## STRIDE Threat Register

| Threat ID | Category  | Component               | Disposition | Mitigation Plan                                                                                     |
| --------- | --------- | ----------------------- | ----------- | --------------------------------------------------------------------------------------------------- |
| T-147-04  | Tampering | costCenterId del dialog | mitigate    | el front solo ofrece centros activos del país; la validación dura es server-side (Plan 01 T-147-01) |
| T-147-SC  | Tampering | npm installs            | accept      | sin paquetes nuevos en esta fase                                                                    |

</threat_model>

<verification>
- `pnpm exec vue-tsc --noEmit` verde para los 4 archivos tocados.
- Verificación visual (UAT manual, fuera de este plan autónomo): el selector obligatorio aparece y bloquea el submit; la columna "Centro de costo" se ve en la lista.
</verification>

<success_criteria>

- EGR-02 (UI): selector obligatorio de centro de costo en el dialog de egreso, default "Varios", submit bloqueado sin elección. ✓
- EGR-03 (UI): columna "Centro de costo" en la lista de movimientos de caja, nombre del centro en filas expense, '—' en el resto. ✓
  </success_criteria>

<output>
Create `.planning/phases/147-centros-de-costo-de-egresos/147-02-SUMMARY.md` when done.
Stagear por ruta (NO git add -A). Branch staging, NO push.
</output>
