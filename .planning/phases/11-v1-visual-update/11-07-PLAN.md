---
phase: 11-v1-visual-update
plan: 07
type: execute
wave: 3
depends_on: ["11-01", "11-02", "11-03", "11-04", "11-05", "11-06"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All brand colors are visually correct"
    - "Typography renders correctly on all screens"
    - "Greek letters display properly"
    - "App icons appear correctly"
  artifacts: []
  key_links: []
---

<objective>
Human verification of El Templo brand identity implementation

Purpose: Confirm visual changes match brand guidelines and look correct across all screens
Output: Verification approval or list of issues to address
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-v1-visual-update/11-RESEARCH.md
@docs/brand-visual/block level.png
@docs/brand-visual/block level alt.jpeg
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete El Templo brand identity implementation across the training module:

1. **Color Palette**
   - Primary: Navy (#2c3e5c) - used for headings, buttons, text
   - Secondary: Bronze (#b8956c) - used for accents, labels, borders
   - Cream: (#f5f0e8) - used for backgrounds, textures

2. **Typography**
   - Cinzel serif font for: block names, page titles, motivational text
   - Roboto (default) for: body text, metrics, labels
   - Roboto Mono for: timer displays

3. **Greek Letters**
   - Level display shows: α (Alfa), Δ (Delta), Σ (Sigma), Ω (Omega)

4. **Components Updated**
   - SplashScreen: navy gradient, Greek level, bronze accents
   - BlockHeader: Cinzel block names, bronze route/intensity
   - ExerciseCard: navy metrics, bronze labels
   - SessionSummary: navy header, cream stats section
   - CelebrationScreen: navy background, bronze trophy
   - WeeklyView: marble header, Cinzel title
   - DayCard: bronze selection, navy today indicator

5. **App Icons**
   - Temple icon generated for iOS and Android
   - Splash screens with cream background
  </what-built>

  <how-to-verify>
**Preparation:**
1. Restart dev server to ensure SCSS changes are loaded:
   ```bash
   cd el-templo-app && pnpm dev
   ```
2. Open browser to http://localhost:9000

**Weekly View (/):**
1. Navigate to /training
2. Verify header has cream/marble texture background
3. Verify "Semana X" title is in serif font (Cinzel), navy color
4. Verify date range subtitle is bronze color
5. Click through day cards - selected should have bronze border
6. Today's card should have navy indicator dot

**Day Player (/training/day/:date):**
1. Click "Comenzar" on today's card
2. Verify SplashScreen:
   - Navy gradient background
   - Bronze accent on icon container
   - Greek letter shows (e.g., "α Alfa" not just "ALFA")
   - "Vamos a entrenar!" in serif font
3. After splash, verify BlockHeader:
   - Block name (INITIUM, NUCLEUS) in serif font
   - Route/intensity text in bronze
4. Verify ExerciseCard:
   - Metric numbers (reps, seconds) in navy
   - Labels ("reps", "descanso") in bronze, uppercase
5. Complete blocks to verify transition splashes work

**Session Completion:**
1. Complete all 4 blocks (select one Deuteros)
2. Verify CelebrationScreen:
   - Navy gradient background
   - Bronze trophy styling
   - Serif font on completion message
3. Verify SessionSummary:
   - Navy gradient header
   - Serif font on "Resumen de Sesion"
   - Cream background on stats row
   - Navy "Terminar Sesion" button

**Compare to Brand Reference:**
- Open `docs/brand-visual/block level.png` as aesthetic reference
- Note: Implementation won't match mockup exactly (different data, layout)
- Focus on: colors feel right, fonts are classical/serif, Greek elements present

**App Icon (if device available):**
- Run `cd el-templo-app && pnpm build && npx cap sync`
- Open in iOS Simulator or Android Emulator
- Verify app icon shows temple logo (not default Capacitor icon)
  </how-to-verify>

  <resume-signal>
Type "approved" if brand identity looks correct.

If issues found, describe:
- Which screen/component has the issue
- What's wrong (wrong color, missing font, layout issue)
- Expected vs actual behavior
  </resume-signal>
</task>

</tasks>

<verification>
Human verification checkpoint - no automated verification needed.
</verification>

<success_criteria>
- User approves visual implementation
- All brand colors render correctly
- Cinzel font loads and displays on headings
- Greek letters appear in level displays
- App icons show temple logo
</success_criteria>

<output>
After completion, create `.planning/phases/11-v1-visual-update/11-07-SUMMARY.md`
</output>
