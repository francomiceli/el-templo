---
phase: 11-v1-visual-update
plan: 05
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - el-templo-app/src/modules/training/pages/WeeklyView.vue
  - el-templo-app/src/modules/training/components/DayCard.vue
autonomous: true

must_haves:
  truths:
    - "WeeklyView header uses marble texture background"
    - "WeeklyView header uses brand typography"
    - "DayCard uses brand colors for states"
    - "Selected day card has bronze accent"
  artifacts:
    - path: "el-templo-app/src/modules/training/pages/WeeklyView.vue"
      provides: "Brand-styled weekly view"
      contains: "marble-bg"
    - path: "el-templo-app/src/modules/training/components/DayCard.vue"
      provides: "Brand-styled day cards"
      contains: "$secondary"
  key_links:
    - from: "WeeklyView.vue"
      to: "app.scss"
      via: "marble-bg class"
      pattern: "marble-bg"
---

<objective>
Apply El Templo brand identity to Weekly View and DayCard components

Purpose: Transform the main weekly interface with brand colors, marble textures, and typography
Output: Updated WeeklyView page and DayCard component with consistent brand styling
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-v1-visual-update/11-RESEARCH.md
@el-templo-app/src/modules/training/pages/WeeklyView.vue
@el-templo-app/src/modules/training/components/DayCard.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update WeeklyView with brand styling and marble texture</name>
  <files>
    el-templo-app/src/modules/training/pages/WeeklyView.vue
  </files>
  <action>
Enhance WeeklyView.vue with El Templo brand identity:

1. Apply marble texture to the header:
   Add `marble-bg` class (from app.scss) or apply inline:
   ```html
   <div class="weekly-view__header marble-bg q-pa-md">
   ```

   Or define local styles if marble-bg class not available yet:
   ```scss
   &__header {
     flex-shrink: 0;
     background-color: #f5f0e8; // $cream
     border-bottom: 1px solid rgba(184, 149, 108, 0.3); // Bronze border
     position: relative;

     &::before {
       content: '';
       position: absolute;
       inset: 0;
       background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 400 400' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.8' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
       opacity: 0.04;
       pointer-events: none;
     }
   }
   ```

2. Update header title typography:
   ```html
   <div class="text-h5 text-weight-bold text-center weekly-view__title">
     Semana {{ weekNumber }}
   </div>
   ```

   ```scss
   &__title {
     font-family: 'Cinzel', Georgia, serif;
     color: #2c3e5c; // Navy
     letter-spacing: 0.05em;
     position: relative; // Above texture
     z-index: 1;
   }
   ```

3. Update subtitle styling:
   ```scss
   .text-subtitle2 {
     color: #b8956c; // Bronze instead of grey-7
     font-weight: 500;
     letter-spacing: 0.03em;
     position: relative;
     z-index: 1;
   }
   ```

4. Update loading spinner to use primary:
   ```html
   <q-spinner-dots color="primary" size="50px" />
   ```
   (This will now be navy since $primary is updated)

5. Keep existing:
   - Carousel component reference
   - Error banner styling
   - Overall layout structure
   - loadWeekData function and routing logic
  </action>
  <verify>
Navigate to /training route in dev mode.
Verify header has cream/marble background.
Verify "Semana X" title is in Cinzel font, navy color.
Verify week range subtitle is bronze color.
  </verify>
  <done>
WeeklyView.vue:
- Header uses cream background with marble texture
- Header has bronze bottom border
- Week title uses Cinzel font, navy color
- Subtitle uses bronze color
- Loading spinner uses primary (navy)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update DayCard with brand colors</name>
  <files>
    el-templo-app/src/modules/training/components/DayCard.vue
  </files>
  <action>
First, read the current DayCard.vue to understand its structure, then enhance with brand colors.

Expected updates based on typical day card structure:

1. Update selected/active state to use bronze accent:
   ```scss
   .day-card--selected {
     border-color: #b8956c; // Bronze
     box-shadow: 0 0 0 2px rgba(184, 149, 108, 0.3);
   }
   ```

2. Update today indicator to use navy:
   ```scss
   .day-card--today {
     &::after {
       // Today dot indicator
       background-color: #2c3e5c; // Navy
     }
   }
   ```

3. Update completed state checkmark to use brand positive (keep green for clarity) or bronze:
   ```scss
   .day-card--completed {
     // Consider bronze checkmark for brand consistency
     // OR keep green for clear "done" semantic
     .completion-icon {
       color: #21ba45; // Keep semantic green, or use bronze
     }
   }
   ```

4. Update day name typography:
   ```scss
   .day-card__name {
     font-family: 'Cinzel', Georgia, serif;
     font-weight: 700;
     color: #2c3e5c; // Navy
   }
   ```

5. Update day number styling:
   ```scss
   .day-card__number {
     color: #2c3e5c; // Navy
     font-weight: 600;
   }
   ```

6. Rest day (Sunday) styling:
   ```scss
   .day-card--rest {
     background-color: #f5f0e8; // $cream
     opacity: 0.7;
   }
   ```

Adapt these based on actual DayCard structure when reading the file.
  </action>
  <verify>
Navigate to /training and observe day cards.
Verify selected card has bronze border/accent.
Verify day names show navy color.
Verify today indicator is navy.
  </verify>
  <done>
DayCard.vue:
- Selected state uses bronze border and shadow
- Today indicator uses navy color
- Day name typography enhanced (optionally Cinzel)
- Day numbers use navy color
- Rest days use cream background
  </done>
</task>

</tasks>

<verification>
1. Start dev server and navigate to /training
2. Verify WeeklyView header:
   - Cream/marble texture background
   - Cinzel font on "Semana X"
   - Bronze subtitle text
3. Verify DayCards:
   - Selected card has bronze accent
   - Today card has navy indicator
   - Day names in navy color
4. Click through days to test selection states
5. Check Sunday shows rest day styling
</verification>

<success_criteria>
- WeeklyView header has marble texture background
- Week title uses Cinzel font in navy
- Week range subtitle is bronze
- DayCard selected state has bronze accent
- DayCard today indicator is navy
- All interaction states work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/11-v1-visual-update/11-05-SUMMARY.md`
</output>
