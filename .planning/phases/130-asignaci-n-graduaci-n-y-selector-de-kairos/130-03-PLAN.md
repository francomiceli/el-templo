---
phase: 130-asignaci-n-graduaci-n-y-selector-de-kairos
plan: 03
type: execute
wave: 2
depends_on: ["130-01"]
files_modified:
  - el-templo-admin/src/components/MemberFormDialog.vue
  - el-templo-admin/src/pages/AlumnosPage.vue
  - el-templo-admin/src/pages/AlumnoDetailPage.vue
autonomous: false
requirements: [KAIROS-07]
must_haves:
  truths:
    - "The admin member edit dialog level selector shows Kairos as the first option (D-04)"
    - "The admin alumnos list level filter shows Kairos as an option (D-04)"
    - "A kairos member renders correctly (Greek glyph + name + color) in the alumnos table and detail page — no raw 'kairos' string (D-04)"
    - "Admin create-member form defaults the level field to kairos (D-01 consistency)"
    - "Adding the 6th level does not break the selector/filter layout (D-04)"
  artifacts:
    - path: "el-templo-admin/src/components/MemberFormDialog.vue"
      provides: "levelOptions with kairos first + default level kairos"
      contains: "kairos"
    - path: "el-templo-admin/src/pages/AlumnosPage.vue"
      provides: "levelFilterOptions + LEVEL_GREEK_MAP + levelColor include kairos"
      contains: "kairos"
    - path: "el-templo-admin/src/pages/AlumnoDetailPage.vue"
      provides: "LEVEL_GREEK_MAP + LEVEL_NAMES + levelColor include kairos"
      contains: "kairos"
  key_links:
    - from: "MemberFormDialog.vue levelOptions"
      to: "form.level select"
      via: ":options binding"
      pattern: "levelOptions"
---

<objective>
Make the Kairos level visible and correctly rendered everywhere the admin shows or sets a member's level: the edit dialog selector (6th box, kairos first), the alumnos list filter, and the Greek/name/color display maps on the list and detail pages. Today these admin maps and option arrays predate Phase 129 and silently omit kairos, so a kairos member would render a raw "kairos" string and could not be filtered or assigned.

Purpose: KAIROS-07 (6th recuadrito in the admin selector without breaking layout) + D-01 form-default consistency. Honors D-04.
Output: kairos added (first, warm tokens, no blue) to all admin level option arrays and display maps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-asignaci-n-graduaci-n-y-selector-de-kairos/130-CONTEXT.md
@CLAUDE.md

<read_first>

- el-templo-admin/src/constants/levels.ts — `LEVEL_ORDER` ALREADY includes kairos first (Phase 129). Use this as the canonical order; prefer importing it where an ordered list is needed instead of re-listing.
- el-templo-admin/src/components/MemberFormDialog.vue:617 — `levelOptions` array (5 entries, kairos MISSING). :601 and :798 — two `level: 'alfa'` form defaults. :155-157 and :400-402 — the two `<q-select v-model="form.level" :options="levelOptions">` instances (create + edit modes).
- el-templo-admin/src/pages/AlumnosPage.vue:426 — `levelFilterOptions` (kairos MISSING). :577 — `LEVEL_GREEK_MAP` (kairos MISSING). :589 — `levelColor` switch (kairos MISSING). :230-233 — the `body-cell-nivel` table cell that renders `greekLevel(row.level)` with `levelColor`.
- el-templo-admin/src/pages/AlumnoDetailPage.vue:1000 — `LEVEL_GREEK_MAP` (kairos MISSING). :1008 — `LEVEL_NAMES` (kairos MISSING). :1026 — `levelColor` switch (kairos MISSING).
- el-templo-app/src/modules/training/utils/levelDisplay.ts — reference for the canonical Kairos glyph/name decision already shipped in the member app: kairos → Greek 'α' (reuses Alfa's glyph, Phase 129), display name 'Kairos'. Mirror this in admin for parity.
  </read_first>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Add Kairos to MemberFormDialog selector + default</name>
  <files>el-templo-admin/src/components/MemberFormDialog.vue</files>
  <action>
Add Kairos as the FIRST entry in `levelOptions` (line 617): `{ label: 'Kairos', value: 'kairos' }` before Alfa, so the order is kairos→alfa→delta→sigma→omega→spartan (matches LEVEL_ORDER / D-04). Change both form defaults `level: 'alfa'` (:601 and :798) to `level: 'kairos'` so a freshly-created member defaults to kairos consistently with the API default (D-01). The two `<q-select>` instances already bind `:options="levelOptions"`, so no markup change is needed; verify the select still renders cleanly with 6 options (q-select is a dropdown — no horizontal layout break).
  </action>
  <verify>
    <automated>cd el-templo-admin && grep -c "value: 'kairos'" src/components/MemberFormDialog.vue | grep -qx 1 && grep -c "level: 'alfa'" src/components/MemberFormDialog.vue | grep -qx 0 && pnpm build 2>/dev/null || pnpm vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - source: D-04 — Kairos is the first level option; CLI: in `levelOptions`, the `kairos` entry precedes the `alfa` entry.
    - source: D-01 — both form defaults are 'kairos'; CLI: `grep "level: 'alfa'" MemberFormDialog.vue` returns nothing.
    - source: CLAUDE.md §no-any — typecheck (`vue-tsc --noEmit`) passes.
  </acceptance_criteria>
  <done>levelOptions has Kairos first, both defaults are kairos, typecheck/build green.</done>
</task>

<task type="auto">
  <name>Task 2: Add Kairos to AlumnosPage + AlumnoDetailPage display maps and filter</name>
  <files>el-templo-admin/src/pages/AlumnosPage.vue, el-templo-admin/src/pages/AlumnoDetailPage.vue</files>
  <action>
AlumnosPage.vue: add `{ label: 'Kairos', value: 'kairos' }` to `levelFilterOptions` (after 'Todos', before 'Alfa'); add `kairos: 'α'` (lowercase alpha, same glyph as alfa per the member-app decision) to `LEVEL_GREEK_MAP` (:577); add a `case 'kairos':` to the `levelColor` switch (:589) returning a WARM token consistent with the brand (no blue) — reuse a warm hue distinct from alfa's `amber-8`, e.g. `orange-4` or `amber-6` (lighter, since kairos is the entry tier); confirm the chosen token is warm. This makes the `body-cell-nivel` cell render the glyph + color for kairos members instead of a raw string.
AlumnoDetailPage.vue: add `kairos: 'α'` to `LEVEL_GREEK_MAP` (:1000); add `kairos: 'Kairos'` to `LEVEL_NAMES` (:1008); add the same `case 'kairos':` warm color to the `levelColor` switch (:1026).
Keep the glyph/name choice byte-consistent with the member app (kairos → 'α' / 'Kairos').
  </action>
  <verify>
    <automated>cd el-templo-admin && grep -c "kairos" src/pages/AlumnosPage.vue | awk '$1>=3{ok=1} END{exit ok?0:1}' && grep -c "kairos" src/pages/AlumnoDetailPage.vue | awk '$1>=3{ok=1} END{exit ok?0:1}' && pnpm vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - source: D-04 — kairos present in levelFilterOptions, LEVEL_GREEK_MAP, levelColor on both pages; CLI: `grep -c kairos` ≥3 in each file.
    - source: D-04/brand — kairos color token is warm (no blue); CLI: the `case 'kairos'` returns a warm Quasar color (amber/orange/brown family), verified by inspection.
    - source: parity — kairos glyph is 'α' and name 'Kairos', matching the member app.
    - source: CLAUDE.md §no-any — `vue-tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Both pages render kairos members with glyph 'α', name 'Kairos', and a warm color; the list filter offers Kairos; typecheck green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Kairos (6th level) added to the admin: edit-dialog level selector (Kairos first), alumnos list level filter, and the Greek-glyph/name/color rendering on the alumnos table + alumno detail page.</what-built>
  <how-to-verify>
    1. Run the admin app (`cd el-templo-admin && pnpm dev`) and open the Alumnos page.
    2. Open the level filter — confirm "Kairos" appears as an option and selecting it filters the list (a newly-registered member should now appear as Kairos).
    3. Confirm the "Nivel" column shows a clean glyph (α) + warm color for a kairos member, not the raw text "kairos".
    4. Open a member's edit dialog — confirm the level selector lists Kairos FIRST, the dropdown is not visually broken with 6 entries, and creating a new member defaults to Kairos.
    5. Open the alumno detail page for a kairos member — confirm the level renders as "Kairos" with the warm glyph/color.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any layout/visual issues</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                                                                          |
| ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| admin client → member level select/filter | display-only + privileged level assignment; assignment goes through the already-gated PUT /members/:userId (Plan 01) |

## STRIDE Threat Register

| Threat ID | Category               | Component              | Disposition | Mitigation Plan                                                                                                                                       |
| --------- | ---------------------- | ---------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-130-07  | Information Disclosure | level filter/display   | accept      | level is non-sensitive display data already shown to staff; adding kairos exposes nothing new                                                         |
| T-130-08  | Elevation of Privilege | level select in dialog | mitigate    | the selector only populates a form; the actual level write stays behind the gated PUT /members/:userId from Plan 01 — no client-side authority change |
| T-130-SC  | Tampering              | npm/pip/cargo installs | accept      | no new packages installed in this plan                                                                                                                |

</threat_model>

<verification>
- kairos appears first in MemberFormDialog levelOptions; defaults are kairos.
- kairos present in AlumnosPage + AlumnoDetailPage glyph/name/color maps and the list filter.
- warm color token (no blue); glyph/name match the member app.
- `vue-tsc --noEmit` passes; human checkpoint confirms no layout break.
</verification>

<success_criteria>

- KAIROS-07 (admin half): the 6th level (Kairos) is selectable, filterable, and correctly rendered across the admin member surfaces without breaking layout (D-04).
  </success_criteria>

<output>
Create `.planning/phases/130-asignaci-n-graduaci-n-y-selector-de-kairos/130-03-SUMMARY.md` when done.
</output>
