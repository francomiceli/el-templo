---
phase: 130-asignaci-n-graduaci-n-y-selector-de-kairos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/migrations/0141_kairos_default_and_level_override.sql
  - el-templo-api/src/db/schema/users.ts
  - el-templo-api/src/modules/auth/routes.ts
  - el-templo-api/src/modules/members/service.ts
  - el-templo-api/src/modules/members/routes.ts
  - el-templo-api/test/kairos/kairos-default-and-override.test.ts
autonomous: true
requirements: [KAIROS-04, KAIROS-06]
must_haves:
  truths:
    - "A newly self-registered member (POST /auth/register) is created with level='kairos' (D-01)"
    - "A newly admin-created member (createMember) with no explicit level is created with level='kairos' (D-01)"
    - "A newly admin-created trial/lead member is created with level='kairos' (D-01)"
    - "An existing member's level is NOT changed by the migration (D-05)"
    - "When a coach changes a member's level via the admin edit endpoint, users.level_override is set to true (D-03)"
    - "users.level_override exists as a boolean column defaulting to false (D-03)"
  artifacts:
    - path: "el-templo-api/src/db/migrations/0141_kairos_default_and_level_override.sql"
      provides: "ALTER level DEFAULT kairos + ADD COLUMN level_override (D-01, D-03)"
      contains: "DEFAULT 'kairos'"
    - path: "el-templo-api/src/db/schema/users.ts"
      provides: "levelEnum.default('kairos') + levelOverride boolean column"
      contains: "level_override"
    - path: "el-templo-api/test/kairos/kairos-default-and-override.test.ts"
      provides: "Regression tests for default + override + existing-member invariant"
  key_links:
    - from: "el-templo-api/src/modules/members/service.ts updateMember"
      to: "users.level_override = true"
      via: "set on level change"
      pattern: "levelOverride"
---

<objective>
Make every new member be born `kairos` instead of `alfa`, additively and without touching existing rows, and make a coach's manual level change set the sticky `level_override` flag. This is the backend foundation (default + override column + migration) that the graduation service (Plan 02) and selectors (Plans 03/04) build on.

Purpose: KAIROS-04 (new members default to kairos) and KAIROS-06 (manual coach override flag), the brownfield-safe half. Honors D-01, D-03, D-05.
Output: Migration 0141, schema change, all new-member level write-sites flipped to kairos, override flag wired into the admin edit path, and regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-asignaci-n-graduaci-n-y-selector-de-kairos/130-CONTEXT.md
@CLAUDE.md

<read_first>

- el-templo-api/src/db/migrations/0140_add_kairos_level_enum.sql — EXACT format template for hand-written enum migration (comment-safety rules, no `;` in comments, enum order kairos→alfa→delta→sigma→omega→spartan). 0140 is the last migration; this plan creates 0141.
- el-templo-api/src/db/schema/users.ts:99 — `level: levelEnum.default("alfa").notNull()` → change to `.default("kairos")`; add `levelOverride` column right after `level`. The `levelEnum` (lines 29-36) ALREADY has kairos first — do NOT touch the enum definition, only the `.default()`.
- el-templo-api/src/modules/auth/routes.ts:177 — self-register DB insert `level: "alfa"`. Line 297 echoes `level: "alfa"` in the response object (cosmetic, must match the row written).
- el-templo-api/src/modules/members/service.ts:623 — local `type Level = "alfa" | ...` (kairos missing); :643 createMember insert `level: (input.level as Level) || "alfa"`; :752 trial/lead create insert `level: "alfa"`; :1045 updateMember level cast.
- el-templo-api/src/modules/members/routes.ts:770-790 — `PUT /:userId` route (calls memberService.updateMember). Plugin-level auth: `fastify.addHook("onRequest", authenticate)` (line 89) + per-route `requireBranchAccess`. This is the single coach/admin level-change entry point (the admin MemberFormDialog edit).
- el-templo-api/src/db/import-members.ts:798 — bulk CSV legacy import `level: "alfa"`. OUT OF SCOPE: this imports legacy members who carry a real historical level; leave as `"alfa"`. Add a one-line comment noting it is intentionally not flipped (per D-01 "do NOT change existing members").
  </read_first>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0141 + schema (default kairos + level_override)</name>
  <files>el-templo-api/src/db/migrations/0141_kairos_default_and_level_override.sql, el-templo-api/src/db/schema/users.ts</files>
  <action>
Create `0141_kairos_default_and_level_override.sql` modeled byte-for-byte on the 0140 comment/format conventions. Two statements in one file:
(1) `ALTER TABLE users MODIFY level ENUM('kairos','alfa','delta','sigma','omega','spartan') NOT NULL DEFAULT 'kairos';` — same enum values/order as 0140 and as `levelEnum` in users.ts (drift lesson). This only changes the DEFAULT from alfa to kairos; it does NOT touch existing rows (D-01, D-05).
(2) `ALTER TABLE users ADD COLUMN level_override BOOLEAN NOT NULL DEFAULT 0;` — new boolean column, default false (D-03). Existing rows get 0 (false) — they keep their level and are eligible-but-untouched.
Comment-safety invariant: NO semicolon character anywhere inside a comment line (the runner splits on `;` before stripping `--`). Header comment must cite Phase 130 / D-01 / D-03 / D-05 and note it is hand-written (do NOT run pnpm db:generate). Commit this .sql alongside the schema change in the same commit ([BLOCKING] CI applies it via `node dist/db/run-migrations.js`).

In `users.ts`: change line 99 `levelEnum.default("alfa")` → `levelEnum.default("kairos")`. Add immediately after the `level` column a `levelOverride: boolean("level_override").notNull().default(false)` column (import `boolean` if not already imported — it is already used at line 116 for staffDisabled, so the import exists). Add a short comment citing D-03 (sticky coach override; auto-graduation skips override=true). Enum values/order in the migration MUST match the schema enum exactly.
</action>
<verify>
<automated>cd el-templo-api && grep -v '^--' src/db/migrations/0141_kairos_default_and_level_override.sql | grep -c "DEFAULT 'kairos'" | grep -qx 1 && grep -q "level_override" src/db/schema/users.ts && pnpm typecheck</automated>
</verify>
<acceptance_criteria> - source: D-01 — migration sets users.level DEFAULT to 'kairos'; behavior: enum order kairos→alfa→delta→sigma→omega→spartan identical to schema; test-command: `grep "ENUM(" 0141*.sql` shows the 6 values in order with `DEFAULT 'kairos'`. - source: D-03 — `level_override` BOOLEAN NOT NULL DEFAULT 0 added; CLI: `grep "level_override" 0141*.sql` and `users.ts` both present. - source: CLAUDE.md §Database — no `;` inside any comment line; CLI: `grep -nE '^\s*--.*;' 0141*.sql` returns nothing. - source: schema parity — `pnpm typecheck` passes.
</acceptance_criteria>
<done>Migration 0141 exists with both statements, no `;` in comments, enum order matches schema; users.ts default is kairos and has level_override boolean; typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: New-member level=kairos everywhere + coach override flag</name>
  <files>el-templo-api/src/modules/auth/routes.ts, el-templo-api/src/modules/members/service.ts, el-templo-api/src/modules/members/routes.ts, el-templo-api/test/kairos/kairos-default-and-override.test.ts</files>
  <behavior>
    - Test 1: POST /auth/register creates a user whose persisted level is 'kairos' and whose response.user.level is 'kairos'.
    - Test 2: createMember (admin legajo create) with no explicit `level` persists level='kairos'; createMember WITH explicit level (e.g. 'delta') persists 'delta' (explicit input still honored).
    - Test 3: admin trial/lead create persists level='kairos'.
    - Test 4 (override): PUT /members/:userId with a `level` change persists level_override=true on that user.
    - Test 5 (invariant): seeding an existing member at level='alfa', level_override=false and then NOT changing their level (e.g. editing only their phone via PUT) leaves level='alfa' and level_override=false unchanged.
  </behavior>
  <action>
Flip every NEW-member level write-site from "alfa" to "kairos" (D-01):
- auth/routes.ts:177 (DB insert) and :297 (response echo) → "kairos".
- members/service.ts: add "kairos" to the local `type Level` union (line 623) so casts compile; createMember insert (:643) `|| "alfa"` → `|| "kairos"`; trial/lead create insert (:752) `level: "alfa"` → `level: "kairos"`; update the level cast unions (:1045 and any other `as "alfa" | ...` for the level field) to include "kairos".
- Leave db/import-members.ts:798 as "alfa" (legacy import) with a one-line comment per D-01.

Coach override (D-03): in `updateMember` (members/service.ts), when `input.level !== undefined` (the existing branch around line 1045 that sets `updateData.level`), ALSO set `updateData.levelOverride = true`. This is the single manual coach level-change path (admin MemberFormDialog → PUT /members/:userId). Do not set the flag when level is not part of the update. The route stays coach/owner-gated by the existing plugin `onRequest` authenticate hook + `requireBranchAccess`; do not loosen auth.

Create `test/kairos/kairos-default-and-override.test.ts` covering the 5 behaviors above using `test/helpers.ts` auth/request utilities and a direct DB read for the persisted `level`/`level_override`. These tests run in CI (do not run the suite locally per project policy).
</action>
<verify>
<automated>cd el-templo-api && grep -c 'level: "alfa"' src/modules/auth/routes.ts | grep -qx 0 && grep -q 'levelOverride = true' src/modules/members/service.ts && pnpm typecheck && grep -q "kairos" test/kairos/kairos-default-and-override.test.ts</automated>
</verify>
<acceptance_criteria> - source: D-01 — no NEW-member path writes "alfa"; CLI: `grep -rn 'level: "alfa"' src/modules/auth/ src/modules/members/service.ts` returns only legacy-import-adjacent NONE (auth + service new-member sites are kairos). import-members.ts is exempt and commented. - source: D-03 — updateMember sets levelOverride=true on level change; behavior: Test 4 asserts level_override=1 after a level-change PUT. - source: D-05 — existing member untouched; behavior: Test 5 asserts no level/override change on a non-level edit. - source: KAIROS-04 explicit-level honored; behavior: Test 2 asserts explicit 'delta' is persisted (default only applies when omitted). - source: CLAUDE.md §no-any — no `any` introduced; `pnpm typecheck` passes.
</acceptance_criteria>
<done>All new-member sites write kairos, explicit level still honored, coach level-change sets level_override=true, existing-member invariant covered by tests, typecheck green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                                         |
| ----------------------------------------- | ----------------------------------------------------------------------------------- |
| member client → POST /auth/register       | untrusted self-registration; level is server-assigned (kairos), NOT client-supplied |
| admin/coach client → PUT /members/:userId | authenticated staff input; level change is privileged and sets the sticky override  |

## STRIDE Threat Register

| Threat ID | Category               | Component                         | Disposition | Mitigation Plan                                                                                                                          |
| --------- | ---------------------- | --------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| T-130-01  | Elevation of Privilege | POST /auth/register level field   | mitigate    | level is hardcoded server-side to "kairos"; never read from request body — a member cannot self-promote                                  |
| T-130-02  | Elevation of Privilege | PUT /members/:userId level change | mitigate    | route stays behind existing plugin `onRequest` authenticate + `requireBranchAccess`; no auth loosened; only staff can set level/override |
| T-130-03  | Tampering              | migration 0141 enum drift         | mitigate    | enum values/order asserted byte-identical to schema; additive ALTER only, no DROP, existing rows untouched                               |
| T-130-SC  | Tampering              | npm/pip/cargo installs            | accept      | no new packages installed in this plan; `boolean` import already present in schema                                                       |

</threat_model>

<verification>
- Migration 0141 present, hand-written, no `;` in comments, enum order matches schema, commits with the schema change.
- `grep -rn 'level: "alfa"'` across auth + members service shows only the commented legacy import-members.ts site.
- level_override column exists; updateMember sets it true on level change.
- `pnpm typecheck` passes; regression tests authored (run in CI).
</verification>

<success_criteria>

- KAIROS-04 (default): new members born kairos via every registration/onboarding/trial path; existing members unchanged (D-01, D-05).
- KAIROS-06 (override flag): manual coach level change sets level_override=true (D-03).
- Migration 0141 + schema committed together; regression tests cover default, explicit-honored, override-set, existing-unchanged.
  </success_criteria>

<output>
Create `.planning/phases/130-asignaci-n-graduaci-n-y-selector-de-kairos/130-01-SUMMARY.md` when done.
</output>
