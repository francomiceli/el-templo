---
phase: 130-asignaci-n-graduaci-n-y-selector-de-kairos
plan: 04
type: execute
wave: 2
depends_on: ["130-01"]
files_modified:
  - el-templo-app/src/modules/onboarding/types.ts
  - el-templo-app/src/modules/onboarding/components/OnboardingQuestion.vue
autonomous: false
requirements: [KAIROS-07]
must_haves:
  truths:
    - "The app level selector shows the 6th box (Kairos), first in order, without breaking layout (D-04)"
    - "Adding the 6th box triggers the component's existing scrollable variant (>5 options) so no layout break (D-04)"
    - "The header level dropdown already lists Kairos (verified — Phase 129 widened TRAINING_LEVELS); no regression"
  artifacts:
    - path: "el-templo-app/src/modules/onboarding/types.ts"
      provides: "LEVEL_SELECTOR_QUESTION.options including Kairos first (pending decision)"
      contains: "kairos"
  key_links:
    - from: "el-templo-app/src/modules/onboarding/components/OnboardingQuestion.vue"
      to: "options-stack--scrollable"
      via: "scrollable computed (options.length > 5)"
      pattern: "scrollable"
---

<objective>
Make the member-app level selector show the 6th box (Kairos), first in order, without breaking layout. The header level dropdown (`HeaderLevelDropdown.vue`) already iterates `TRAINING_LEVELS` (kairos included since Phase 129), so the real work is the onboarding self-pick selector (`LEVEL_SELECTOR_QUESTION`), which today lists only alfa→omega. Adding a 6th option is layout-safe because `OnboardingQuestion.vue` already switches to a scrollable variant when `options.length > 5`.

Purpose: KAIROS-07 (app half — 6th recuadrito, kairos first, no layout break). Honors D-04.
Output: Kairos added (first) to the onboarding level options, plus a verification that the header dropdown already renders kairos correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-asignaci-n-graduaci-n-y-selector-de-kairos/130-CONTEXT.md
@CLAUDE.md

<read_first>

- el-templo-app/src/modules/onboarding/types.ts:347 — `LEVEL_SELECTOR_QUESTION` (`¿En qué nivel entrenás?`) with 4 options (alfa/delta/sigma/omega). Spartan is INTENTIONALLY excluded from this self-pick (earned, not claimed — see ONBOARDING_NARRATIVE_REDESIGN.md:142). The Kairos decision (include in self-pick?) is the checkpoint below.
- el-templo-app/src/modules/onboarding/components/OnboardingQuestion.vue:35 — `scrollable = computed(() => props.question.options.length > 5)` and the `options-stack--scrollable` class. This is exactly D-04's "scroll where needed" — adding a 6th box is layout-safe by design.
- el-templo-app/src/modules/training/components/HeaderLevelDropdown.vue — ALREADY iterates `TRAINING_LEVELS` (kairos first) via a q-menu list; kairos already appears. This plan only VERIFIES it (no change expected).
- el-templo-app/src/modules/training/level-display.ts + utils/levelDisplay.ts — already define kairos → glyph 'α', name 'Kairos' (Phase 129). Reuse; do not redefine.
  </read_first>
  </context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Should "Kairos" appear as a selectable box in the onboarding self-pick level selector (`¿En qué nivel entrenás?`), or stay out of the self-pick (assigned/auto-only, like spartan is excluded)?</decision>
  <context>
D-04 says "the selector shows the 6th recuadrito (Kairos), kairos first." The header dropdown already shows kairos. The onboarding self-pick is a member CHOOSING their level. Spartan is already excluded there (earned, not claimed). Kairos is the auto-assigned entry tier — new members are born kairos and graduate automatically. So either reading is defensible.
  </context>
  <options>
    <option id="include-kairos">
      <name>Add Kairos as the first box in the self-pick (kairos→alfa→delta→sigma→omega)</name>
      <pros>Literal reading of D-04 (6th box, kairos first); the component already supports 6 via scroll; lets an absolute beginner self-identify as Kairos.</pros>
      <cons>A self-picker could choose Kairos and then also be auto-graduated; mild redundancy with the auto-assigned default.</cons>
    </option>
    <option id="exclude-kairos">
      <name>Leave the self-pick at alfa→omega; rely on the auto-default + header dropdown for Kairos visibility</name>
      <pros>Consistent with spartan exclusion (Kairos is assigned, not claimed); the header dropdown already satisfies "selector shows the 6th box."</pros>
      <cons>The self-pick boxes won't show Kairos; arguably a narrower reading of D-04.</cons>
    </option>
  </options>
  <resume-signal>Select: include-kairos or exclude-kairos</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Apply the Kairos self-pick decision + verify header dropdown</name>
  <files>el-templo-app/src/modules/onboarding/types.ts, el-templo-app/src/modules/onboarding/components/OnboardingQuestion.vue</files>
  <action>
If the checkpoint chose `include-kairos`: prepend `{ value: 'kairos', label: 'α Kairos' }` as the FIRST option in `LEVEL_SELECTOR_QUESTION.options` (types.ts:347), keeping the α-glyph/name parity with level-display.ts. The 6th-option case is already handled by `OnboardingQuestion.vue`'s `scrollable` computed — verify the `options-stack--scrollable` styles render the 6-box list without overflow/clipping; tweak only if the 6th box visibly breaks (e.g. ensure max-height + scroll, no horizontal break). No glyph/name redefinition (reuse the shipped maps).

If the checkpoint chose `exclude-kairos`: leave `LEVEL_SELECTOR_QUESTION.options` unchanged and add a one-line comment citing the decision (Kairos is auto-assigned, excluded from self-pick like spartan). In this branch `types.ts` is the only touched file (comment only), and the header dropdown already satisfies D-04.

In BOTH branches: verify (by inspection) that `HeaderLevelDropdown.vue` already lists Kairos (it iterates `TRAINING_LEVELS`); no change expected there.
</action>
<verify>
<automated>cd el-templo-app && pnpm vue-tsc --noEmit && grep -q "TRAINING_LEVELS" src/modules/training/components/HeaderLevelDropdown.vue</automated>
</verify>
<acceptance_criteria> - source: D-04 — selector shows the 6th box per the chosen decision; behavior: if include-kairos, `LEVEL_SELECTOR_QUESTION.options[0].value === 'kairos'`; if exclude-kairos, a comment documents the intentional omission. - source: D-04 — no layout break; behavior: OnboardingQuestion `scrollable` (>5 options) handles the 6th box; visual checkpoint confirms. - source: parity — kairos glyph 'α' / name 'Kairos' reused, not redefined. - source: CLAUDE.md §no-any — `vue-tsc --noEmit` passes.
</acceptance_criteria>
<done>The self-pick reflects the decision, the header dropdown shows Kairos (verified), typecheck green, no layout break.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The member-app level selectors now surface Kairos: the header level dropdown (already iterating all 6 levels) and the onboarding self-pick (per the decision above), using the existing scrollable variant so the 6th box doesn't break layout.</what-built>
  <how-to-verify>
    1. Run the app (`cd el-templo-app && pnpm dev`).
    2. Open the header level dropdown (tap the level badge) — confirm Kairos appears in the list with the α glyph and reads cleanly (no overflow), and "Tu Nivel" marks a kairos member's own level.
    3. If include-kairos was chosen: walk the onboarding flow to the "¿En qué nivel entrenás?" step — confirm Kairos shows as the FIRST box, the 6-box list scrolls if needed, and selecting it advances without visual breakage.
    4. Confirm a freshly-registered member (Kairos) shows the Kairos badge correctly on the training/header surfaces.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any layout/visual issues</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                               | Description                                                                                                                                                                        |
| ------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| member client → onboarding self-pick / header dropdown | member selects a training level to VIEW sessions; the persisted users.level is server-controlled (default kairos + graduation), the dropdown drives session content selection only |

## STRIDE Threat Register

| Threat ID | Category               | Component                | Disposition | Mitigation Plan                                                                                                                                                             |
| --------- | ---------------------- | ------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-130-09  | Elevation of Privilege | self-pick / header level | mitigate    | the header dropdown sets the VIEWED level (session content), not users.level; the member's real level/graduation is server-side, so picking Kairos here grants no privilege |
| T-130-10  | Tampering              | onboarding level value   | accept      | onboarding-selected level is profile metadata; it does not bypass the server-side default/graduation, and the value is constrained to the option set                        |
| T-130-SC  | Tampering              | npm/pip/cargo installs   | accept      | no new packages installed in this plan                                                                                                                                      |

</threat_model>

<verification>
- LEVEL_SELECTOR_QUESTION reflects the checkpoint decision (kairos-first box or documented exclusion).
- HeaderLevelDropdown verified to already list Kairos (no regression).
- OnboardingQuestion scrollable variant handles 6 boxes without layout break.
- `vue-tsc --noEmit` passes; human checkpoint confirms visuals.
</verification>

<success_criteria>

- KAIROS-07 (app half): the level selector surfaces the 6th box (Kairos), first in order, without breaking layout (D-04), per the resolved self-pick decision.
  </success_criteria>

<output>
Create `.planning/phases/130-asignaci-n-graduaci-n-y-selector-de-kairos/130-04-SUMMARY.md` when done.
</output>
