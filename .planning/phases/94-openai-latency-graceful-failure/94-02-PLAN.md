---
phase: 94-openai-latency-graceful-failure
plan: 02
type: tdd
wave: 1
depends_on:
  - "94-01"
files_modified:
  - el-templo-bot/src/ai/openai.ts
  - el-templo-bot/test/v5-3-3-openai-latency.test.ts
autonomous: true
requirements:
  - LAT-01
tags:
  - openai
  - max-retries
  - invariant
  - gap-closure
  - whatsapp-bot

must_haves:
  truths:
    - "OpenAI SDK client is constructed with `maxRetries: 0` so the SDK does not double-retry slow upstreams. Real worst-case per `provider.chat` call is `(OPENAI_TIMEOUT_MS/1000)` seconds, not `3 × (OPENAI_TIMEOUT_MS/1000)`."
    - "Real worst-case end-to-end (5 tool iterations) is `45 × 1 × 5 + 30 × 5 + 20 = 395s ≤ 600s DEBOUNCE_TTL_SECONDS` — the original locked Cross-Phase Invariant formula holds without modification."
    - "The Cross-Phase Invariant block is byte-identical across all 4 canonical doc locations (sha256 `67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344`) — 94-02 does NOT mutate the formula text."
    - "Regression test in `el-templo-bot/test/v5-3-3-openai-latency.test.ts` asserts `client.maxRetries === 0` on a freshly-constructed OpenAiProvider, with and without `OPENAI_TIMEOUT_MS` override. Test fails against current main (where maxRetries is undefined / SDK default 2) and passes after the GREEN commit."
  artifacts:
    - path: "el-templo-bot/src/ai/openai.ts"
      provides: "OpenAI SDK client with explicit timeout AND maxRetries: 0 to honor Cross-Phase Invariant"
      contains: "maxRetries: 0"
    - path: "el-templo-bot/test/v5-3-3-openai-latency.test.ts"
      provides: "Phase-94 unit suite extended with maxRetries=0 regression coverage (CR-02 closure)"
      contains: "readClientMaxRetries"
  key_links:
    - from: "el-templo-bot/src/ai/openai.ts:50"
      to: "OpenAI SDK constructor option `maxRetries`"
      via: "constructor option object literal"
      pattern: "maxRetries:\\s*0"
    - from: "el-templo-bot/test/v5-3-3-openai-latency.test.ts (new it() blocks in LAT-01 SC#1 describe)"
      to: "provider.client.maxRetries"
      via: "readClientMaxRetries(provider) test-only accessor (mirrors readClientTimeout pattern)"
      pattern: "readClientMaxRetries\\(provider\\)\\)\\.toBe\\(0\\)"
---

<objective>
Close gap **CR-02** from `94-VERIFICATION.md` (NOT accepted as override — invariant discipline installed in Phase 93 must hold).

The OpenAI SDK defaults to `maxRetries: 2`. With Phase 94's `timeout: 45000` in place but no `maxRetries` override, a single `provider.chat(...)` call can take up to `3 × 45s = 135s` wall-clock. Plugged back into the canonical Cross-Phase Invariant formula, real worst-case end-to-end is `135 × 5 + 30 × 5 + 20 = 845s` — exceeding the `DEBOUNCE_TTL_SECONDS=600` lock TTL chosen in Phase 93 by 245s. That is exactly the failure mode the invariant exists to prevent (lock expires mid-handler → BUG-01 reintroduced).

Resolution: set `maxRetries: 0` on the `OpenAI` client constructor at `el-templo-bot/src/ai/openai.ts:50`. The handler already provides retry/recovery semantics via the Phase 94 LAT-02 interim UX + LAT-03 graceful fallback path; SDK-level double-retry is redundant with the handler-level recovery logic and is what breaks the invariant. With `maxRetries: 0`, real worst-case per-call is exactly `(OPENAI_TIMEOUT_MS / 1000)` seconds and the canonical formula `45 × 1 × 5 + 30 × 5 + 20 = 395s ≤ 600s` holds true byte-for-byte — no cross-doc resync required.

`maxRetries: 1` was rejected at plan time: it would yield real worst-case `90 × 5 + 30 × 5 + 20 = 620s`, exceeding the 600s TTL by 20s. Only `maxRetries: 0` preserves the original locked formula.

Two coupled deliverables, two atomic commits (TDD RED then GREEN — mirrors 94-01):

- **Task 1 (RED)** — Append a maxRetries-aware regression test to the existing Phase-94 test file `el-templo-bot/test/v5-3-3-openai-latency.test.ts`. Mirrors the existing `readClientTimeout` pattern at `:56`; adds a sibling `readClientMaxRetries` helper and two `it()` blocks inside the existing `describe("LAT-01 (SC#1) — OpenAI SDK client constructed with explicit timeout option", ...)` at `:61`. Tests fail against current main where SDK default `maxRetries=2` makes `expect(...).toBe(0)` false.
- **Task 2 (GREEN)** — Single-line addition at `el-templo-bot/src/ai/openai.ts:50` (`maxRetries: 0` inside the constructor options), companion logger-payload extension at `:52`, and a 2-3 sentence doc-comment append at `:24-36` explaining the rationale. Total LOC delta ≤ 8 lines.

**Out-of-scope for 94-02 (do NOT touch):**

- The Cross-Phase Invariant canonical block in any of the 4 markdown documents (`93-CONTEXT.md`, `94-CONTEXT.md`, `ROADMAP.md` × 2 locations, `MACRO-ROADMAP.md`). With `maxRetries=0` the formula text remains correct — the per-call wall-clock IS `OPENAI_TIMEOUT_MS / 1000`.
- `el-templo-bot/.env.example`. No new env var is introduced. `OPENAI_MAX_RETRIES` env-override is DELIBERATELY NOT added — the invariant locks this value at 0; allowing operator override would re-introduce the bug surface (see threat T-94-02-02 in the threat register below).
- `el-templo-bot/src/webhook/handler.ts`. No handler-side changes. The existing LAT-02 + LAT-03 catch paths already provide the recovery layer that the now-suppressed SDK retries previously double-performed.
- `el-templo-bot/scripts/check-debounce-invariant.sh`. Script formula is correct as-is with maxRetries=0; no edit needed. Verification re-runs it as a regression check.
- Anthropic provider path (`el-templo-bot/src/ai/anthropic.ts`). CR-01 (Anthropic-discriminator gap) was ACCEPTED as override in the disposition pass — separate concern.

**Plan-checker mode:** Normal (not adversarial). The change is mechanical and the design space is collapsed to a single locked answer (`maxRetries=0`) by the cross-phase invariant math. Adversarial framing would not surface new decisions.

**TDD discipline:** Two atomic commits — RED first (test only, fails against current main), GREEN second (source fix). Same fail-in-main pattern as 94-01.

**Stop point:** After plan-checker passes, commit this plan file ONLY. Do NOT spawn an executor. User reviews the plan tomorrow and authorizes execution separately.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/94-openai-latency-graceful-failure/94-CONTEXT.md
@.planning/phases/94-openai-latency-graceful-failure/94-VERIFICATION.md
@.planning/phases/94-openai-latency-graceful-failure/94-REVIEW.md
@.planning/phases/94-openai-latency-graceful-failure/94-01-PLAN.md
@.planning/phases/94-openai-latency-graceful-failure/94-01-SUMMARY.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@./el-templo-bot/CLAUDE.md

# Code targets (read only the explicit ranges referenced in tasks below — do NOT re-read whole files)

@el-templo-bot/src/ai/openai.ts
@el-templo-bot/test/v5-3-3-openai-latency.test.ts
@el-templo-bot/scripts/check-debounce-invariant.sh

<interfaces>
<!-- Existing contracts the executor uses directly — no codebase exploration needed. -->

From `el-templo-bot/src/ai/openai.ts` (current post-94-01 state, the file 94-02 mutates):

- `export class OpenAiProvider implements AiProvider`
- `private client: OpenAI` at `:45` — the field whose `.maxRetries` property the new test reads.
- Constructor at `:48-53`. Current line 50: `this.client = new OpenAI({ timeout: timeout });` — Task 2 changes this to `this.client = new OpenAI({ timeout: timeout, maxRetries: 0 });`.
- `resolveOpenAiTimeoutMs()` helper at `:37-42` — unchanged by 94-02 (pattern reference only).
- Doc-comment block at `:24-36` — Task 2 appends a 2-3 sentence paragraph explaining the maxRetries=0 rationale.
- `logger.info` call at `:52` — Task 2 extends the payload to include the resolved `maxRetries` value (currently logs `{ model, timeout }`; becomes `{ model, timeout, maxRetries: 0 }` so boot logs confirm the lock).

From `el-templo-bot/test/v5-3-3-openai-latency.test.ts` (current post-94-01 state, the file 94-02 appends to):

- `readClientTimeout(provider)` helper at `:56-59` — pattern anchor. The new `readClientMaxRetries` helper mirrors this exact shape.
- `describe("LAT-01 (SC#1) — OpenAI SDK client constructed with explicit timeout option", ...)` block at `:61-108` — Task 1 appends new `it()` blocks INSIDE this same `describe`, after the existing `:100-107` "falls back to 45000" test. Re-uses the existing `beforeEach`/`afterEach` env-management at `:64-80`.
- `process.env.OPENAI_API_KEY = "sk-test-fake-key-for-unit-test"` at `:68` — required because the SDK constructor throws if `OPENAI_API_KEY` is unset. Existing beforeEach handles this for the new tests too.

From the openai npm package:

- `OpenAI` default export, constructor accepts `{ timeout?: number; maxRetries?: number; ... }`.
- `client.maxRetries: number` is a public field on the SDK client instance (same surface shape as `client.timeout`). Read via the test-only `unknown` cast pattern — same brittleness caveat as `client.timeout` (see review IN WR-08; the entire SC#1 surface uses this pattern and the team accepted it for 94-01).
  </interfaces>

<cross_phase_invariant>
**CANONICAL BLOCK — must remain byte-identical across `.planning/phases/93-handler-concurrency/93-CONTEXT.md:84`, `.planning/phases/94-openai-latency-graceful-failure/94-CONTEXT.md:34`, `.planning/ROADMAP.md:62`, `.planning/ROADMAP.md:93`, `.planning/MACRO-ROADMAP.md:99`.**

```
DEBOUNCE_TTL_SECONDS >= (OPENAI_TIMEOUT_MS / 1000) × MAX_TOOL_ITERATIONS
                     + (executeTool_timeout_seconds × MAX_TOOL_ITERATIONS)
                     + safety_buffer

Concrete values (post-Phase-94+97 target):
  OPENAI_TIMEOUT_MS = 45000             (Phase 94 LAT-01)
  MAX_TOOL_ITERATIONS = 5               (existing handler config)
  executeTool_timeout_seconds = 30      (Phase 95 BOOK-01 + Phase 97 RGUARD-03)
  safety_buffer = 20
  Minimum TTL = 45 × 5 + 30 × 5 + 20 = 395s → round up to 600s (10 min)
```

**94-02 does NOT mutate this block in any of the 5 canonical locations.** With `maxRetries=0`, the per-call wall-clock equals `OPENAI_TIMEOUT_MS / 1000` exactly, so the formula's left-hand factor is correct as written. The SHA-256 drift check in `<verification>` step 9 confirms the block is still byte-identical after the GREEN commit (sha256 `67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344` across all 4 markdown locations — `ROADMAP.md` appears twice at lines `:62` and `:93`).

**Why `maxRetries=1` was rejected at plan time:** `maxRetries=1` yields per-call worst-case `2 × 45s = 90s`, end-to-end worst-case `90 × 5 + 30 × 5 + 20 = 620s` > 600s TTL. Only `maxRetries=0` preserves the original locked formula. `maxRetries=1` would force a canonical-block rewrite across 5 documents with sha256 resync — out of scope for a gap-closure plan and avoidable.

**Why no env override:** `OPENAI_MAX_RETRIES` env var was DELIBERATELY NOT introduced. The invariant locks this value at 0. Allowing operator override re-introduces exactly the bug surface this plan closes. If a future phase needs configurability, that phase must also re-prove the invariant — see threat T-94-02-02 below.
</cross_phase_invariant>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Append maxRetries=0 regression test to v5-3-3-openai-latency.test.ts</name>
  <files>el-templo-bot/test/v5-3-3-openai-latency.test.ts</files>
  <behavior>
    Append a sibling helper `readClientMaxRetries(provider)` alongside the existing `readClientTimeout(provider)` at `:56-59`. Append two `it()` blocks at the END of the existing `describe("LAT-01 (SC#1) — OpenAI SDK client constructed with explicit timeout option", ...)` block (current `:108` closing brace — insert before it):

    1. `it("constructs OpenAI client with maxRetries: 0 to keep Cross-Phase Invariant within bound", async () => { ... })` — instantiates `OpenAiProvider` with no env override, asserts `expect(readClientMaxRetries(provider)).toBe(0)`. RED state: SDK default `maxRetries` is `2`, so the assertion fails. GREEN state (after Task 2): assertion passes.

    2. `it("maxRetries remains 0 even when OPENAI_TIMEOUT_MS is overridden", async () => { ... })` — sets `process.env.OPENAI_TIMEOUT_MS = "12345"` (re-using the existing override pattern at `:92`), instantiates the provider, asserts `expect(readClientMaxRetries(provider)).toBe(0)`. This locks in that `maxRetries` is not coupled to (and not affected by) the timeout env var — a future regression where someone adds an `OPENAI_MAX_RETRIES` env read would still pass this test unless the new env is also set, but the test catches accidental coupling.

    Helper shape (mirror `readClientTimeout` byte-for-byte except the field name):

    - `function readClientMaxRetries(provider: object): unknown { const inner = (provider as { client: { maxRetries: unknown } }).client; return inner.maxRetries; }`
    - Place this directly under `readClientTimeout` at `:60` (i.e., insert at `:60` as a new function definition — the existing closing brace of `readClientTimeout` is at `:59`).

    Re-use the existing `beforeEach`/`afterEach` at `:64-80`. No new env management needed — `process.env.OPENAI_API_KEY` is already set to the fake key by `beforeEach`, and `OPENAI_TIMEOUT_MS` is already deleted in `beforeEach` and `afterEach`, so the new tests start from a clean slate.

    Do NOT modify any production source file in this task. Do NOT modify the existing `readClientTimeout` helper. Do NOT modify the existing 3 `it()` blocks inside the LAT-01 describe. Do NOT modify any other describe block (SC#2, SC#3, SC#4 remain untouched).

    The describe block's name string remains "LAT-01 (SC#1) — OpenAI SDK client constructed with explicit timeout option" — the new tests are conceptually under SC#1 (timeout boundary) because maxRetries=0 is what makes the timeout boundary actually be 45s wall-clock per call (without it, the boundary is silently 135s per call). Updating the describe name to also mention maxRetries is OPTIONAL and at executor discretion (a single-line edit if done); the test names themselves carry the intent.

    Commit message (use HEREDOC, mirror 94-01 style):

    `test(94-02): fail-in-main regression for SDK maxRetries=0 (CR-02 closure)`

    Body: reference 94-VERIFICATION.md gaps[0] (CR-02) and 94-REVIEW.md CR-02. State the invariant math: SDK default maxRetries=2 makes real per-call wall-clock 3×45s=135s, violating the canonical formula's assumption that per-call wall equals OPENAI_TIMEOUT_MS/1000. Note: test asserts the GREEN target (maxRetries=0); fails against current main.

  </behavior>
  <read_first>
    - `.planning/phases/94-openai-latency-graceful-failure/94-CONTEXT.md` lines 26-117 (decisions block including the Cross-Phase Invariant section at `:29-46`).
    - `.planning/phases/94-openai-latency-graceful-failure/94-VERIFICATION.md` lines 30-42 (gaps[0] frontmatter — the CR-02 spec being closed).
    - `.planning/phases/94-openai-latency-graceful-failure/94-REVIEW.md` lines 69-90 (CR-02 finding in detail with the recommended fix code snippet).
    - `el-templo-bot/test/v5-3-3-openai-latency.test.ts` lines 1-120 (full SC#1 describe + `readClientTimeout` helper — the exact pattern to mirror).
    - `el-templo-bot/src/ai/openai.ts` full file (≤ 170 lines — current GREEN state with timeout already wired; helps confirm the test's import surface and what the GREEN target will look like).
  </read_first>
  <action>
    Insert the `readClientMaxRetries` helper function definition into `el-templo-bot/test/v5-3-3-openai-latency.test.ts` immediately after the existing `readClientTimeout` helper (after line 59, before the existing `describe(...)` opening at line 61). Mirror the exact signature, cast pattern, and comment style of `readClientTimeout`.

    Inside the existing `describe("LAT-01 (SC#1) — OpenAI SDK client constructed with explicit timeout option", ...)` block, append two new `it(...)` blocks AFTER the existing "falls back to 45000 ms when OPENAI_TIMEOUT_MS is invalid (non-numeric)" test (currently `:100-107`). Insertion site is between the closing brace of the third `it` block and the closing brace of the `describe` (currently `:108`). Use the exact test titles specified in `<behavior>` above.

    Each new `it()` block follows the SAME body shape as the existing three tests:
    1. `const mod = await import("../src/ai/openai");`
    2. `const provider = new mod.OpenAiProvider();`
    3. `expect(readClientMaxRetries(provider)).toBe(0);`
    plus the env-override line where the second test needs it.

    No source file edits. No describe-block additions. No new top-level imports needed — `vi`, `describe`, `it`, `expect`, `beforeEach`, `afterEach` are already imported at `:32`.

    After editing, run from the bot directory:
    - `cd el-templo-bot && pnpm test v5-3-3-openai-latency 2>&1 | tee /tmp/red-output-94-02.txt`
    - Confirm the new maxRetries tests FAIL with the expected reason ("expected 0 to be 0" → actually "expected 2 to be 0", since SDK default `maxRetries=2`). The existing 6 tests must continue to PASS — only the 2 newly added tests fail in RED state.
    - Confirm `grep -qE "Tests +[1-9][0-9]* +failed" /tmp/red-output-94-02.txt` exits 0.

    Commit only the test file change. Use the HEREDOC commit pattern:

    ```
    git add el-templo-bot/test/v5-3-3-openai-latency.test.ts
    git commit -m "$(cat <<'EOF'
    test(94-02): fail-in-main regression for SDK maxRetries=0 (CR-02 closure)

    Closes gap CR-02 from 94-VERIFICATION.md gaps[0] (NOT accepted as
    override — invariant discipline installed in Phase 93 must hold).

    OpenAI SDK default maxRetries=2 makes real per-`provider.chat` wall-
    clock up to 3 × OPENAI_TIMEOUT_MS = 135s, not 45s. Plugged into the
    Cross-Phase Invariant formula, real worst-case end-to-end becomes
    135 × 5 + 30 × 5 + 20 = 845s — exceeding DEBOUNCE_TTL_SECONDS=600
    by 245s. That is exactly the failure mode the invariant exists to
    prevent (lock expires mid-handler → BUG-01 reintroduced).

    This RED commit appends two regression tests to the existing
    LAT-01 (SC#1) describe in v5-3-3-openai-latency.test.ts:

    - asserts client.maxRetries === 0 with default env
    - asserts client.maxRetries === 0 with OPENAI_TIMEOUT_MS override

    Both fail against current main (openai.ts:50 has no maxRetries
    option → SDK default 2). They pass after the companion GREEN
    commit lands maxRetries: 0 on the constructor.

    Mirrors the readClientTimeout pattern at test/v5-3-3-openai-
    latency.test.ts:56 (same `unknown`-cast accessor; same brittleness
    caveat already accepted by 94-01 for the SC#1 surface).

    No production source modified in this commit. GREEN follows in
    fix(bot): set OpenAI maxRetries=0 ...
    EOF
    )"
    ```

  </action>
  <verify>
    <automated>cd el-templo-bot && pnpm test v5-3-3-openai-latency 2>&1 | tee /tmp/red-output-94-02.txt; grep -qE "Tests +[1-9][0-9]* +failed" /tmp/red-output-94-02.txt && grep -c "readClientMaxRetries" test/v5-3-3-openai-latency.test.ts | grep -qE "^[3-9]|^[0-9]{2,}" && grep -c "expect(readClientMaxRetries(provider)).toBe(0)" test/v5-3-3-openai-latency.test.ts | grep -qE "^[2-9]|^[0-9]{2,}" && git log -1 --format=%s | grep -qE "test\(94-02\): .*CR-02 closure"</automated>
  </verify>
  <done>
    `el-templo-bot/test/v5-3-3-openai-latency.test.ts` contains:
    - The `readClientMaxRetries` helper function definition (≥ 1 declaration, ≥ 2 call sites in `expect(...)` lines).
    - Two new `it()` blocks asserting `client.maxRetries === 0` (default env + with `OPENAI_TIMEOUT_MS` override).

    Test run shows the new tests FAILING in RED state (their `expect(...).toBe(0)` fails because SDK default is 2). The existing 6 Phase-94 tests continue to pass. No production source files modified in this commit. Atomic commit `test(94-02): fail-in-main regression for SDK maxRetries=0 (CR-02 closure)` landed on the working branch.

  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Add maxRetries: 0 to OpenAI client constructor at openai.ts:50</name>
  <files>el-templo-bot/src/ai/openai.ts</files>
  <behavior>
    Single-file change. All edits to `el-templo-bot/src/ai/openai.ts` only. Three coordinated micro-edits totaling ≤ 8 LOC delta:

    **Edit 1 — Constructor option (line 50):**
    Change `this.client = new OpenAI({ timeout: timeout });` to `this.client = new OpenAI({ timeout: timeout, maxRetries: 0 });`. The verbose `timeout: timeout` property syntax (review nit IN-01 — left unchanged in 94-01) is preserved here for diff minimality; do NOT introduce object shorthand in this commit. The single-line addition is `, maxRetries: 0` between the closing `}` of the existing options object.

    **Edit 2 — Logger payload (line 52):**
    Change `logger.info({ model, timeout }, "OpenAI provider initialized");` to `logger.info({ model, timeout, maxRetries: 0 }, "OpenAI provider initialized");`. This makes the production boot log confirm the lock value — operators / verifiers can grep boot logs for `"maxRetries":0` to confirm the invariant guard is wired. The literal `0` (NOT a constant) is used because there is no module-level constant for maxRetries; introducing one for a single use is over-engineering. The doc comment (Edit 3) carries the rationale.

    **Edit 3 — Doc comment append (lines 24-36):**
    Append a 2-3 sentence paragraph to the existing function-level doc comment block on `resolveOpenAiTimeoutMs` at `:24-36`. The append documents the maxRetries=0 rationale inline with the timeout rationale, since the two values are jointly required for the Cross-Phase Invariant to hold. Append BEFORE the closing `*/` at `:36` (i.e., as a new paragraph at the end of the existing comment).

    Suggested append text (executor may rephrase for tone but must preserve the substantive claims):

    > Companion constraint: `maxRetries` on the OpenAI client is locked to `0`. The SDK's default `maxRetries: 2` would multiply real per-`chat()` wall-clock by 3× the configured timeout, violating the Cross-Phase Invariant (worst-case `3 × 45 × 5 + 30 × 5 + 20 = 845s` exceeds `DEBOUNCE_TTL_SECONDS=600`). Setting `maxRetries: 0` keeps the formula `45 × 1 × 5 + 30 × 5 + 20 = 395s ≤ 600s` true byte-for-byte. The handler already provides retry/recovery via the Phase 94 LAT-02 interim UX + LAT-03 graceful fallback path — SDK-level retries are redundant. No env-override is provided (`OPENAI_MAX_RETRIES` is deliberately absent); the invariant locks this value, and operator-tunability here would re-introduce the bug surface CR-02 closed.

    DO NOT introduce a configurable `OPENAI_MAX_RETRIES` env var. DO NOT add a helper function (`resolveOpenAiMaxRetries()` etc.) — the value is locked literal `0`, no resolution needed.

    DO NOT modify `el-templo-bot/.env.example`. DO NOT modify `el-templo-bot/src/webhook/handler.ts`. DO NOT modify `el-templo-bot/scripts/check-debounce-invariant.sh`. DO NOT modify any of the 4 markdown documents holding the canonical Cross-Phase Invariant block (`93-CONTEXT.md`, `94-CONTEXT.md`, `ROADMAP.md`, `MACRO-ROADMAP.md`). DO NOT modify any test file (Task 1 already shipped the test changes).

    The Anthropic provider path (`el-templo-bot/src/ai/anthropic.ts`) is NOT touched. CR-01 (Anthropic-discriminator gap) was accepted as override in the disposition pass — out of scope for 94-02.

    Commit message:

    `fix(bot): set OpenAI maxRetries=0 to honor Cross-Phase Invariant (CR-02)`

    Body: cite the formula with maxRetries=0 (395s ≤ 600s holds), reference 94-VERIFICATION.md gaps[0] explicitly, note "closes 94-VERIFICATION CR-02", state that the cross-phase invariant TTL holds at the minimum-required 395s with this lock in place, and confirm Task 1's RED test now passes.

  </behavior>
  <read_first>
    - `el-templo-bot/src/ai/openai.ts` full file (≤ 170 lines — current GREEN state from 94-01).
    - `el-templo-bot/test/v5-3-3-openai-latency.test.ts` lines 56-108 (the SC#1 describe block, including the new tests appended by Task 1).
    - `.planning/phases/94-openai-latency-graceful-failure/94-VERIFICATION.md` lines 30-42 (gaps[0] frontmatter — CR-02 spec).
    - `.planning/phases/94-openai-latency-graceful-failure/94-CONTEXT.md` lines 29-46 (Cross-Phase Invariant canonical block — read-only; this task does NOT mutate it).
    - `el-templo-bot/scripts/check-debounce-invariant.sh` full file (35 lines — script remains correct as-is with maxRetries=0; included in verification).
  </read_first>
  <action>
    Apply Edit 1 (line 50), Edit 2 (line 52), Edit 3 (doc-comment append before `:36`) to `el-templo-bot/src/ai/openai.ts`. Total LOC delta ≤ 8.

    After editing, run from the bot directory in order:
    1. `cd el-templo-bot && pnpm test v5-3-3-openai-latency 2>&1 | tail -20` — expect all tests in the suite passing (the existing 6 + the 2 new from Task 1 = 8 passing; the exact count is informative, not asserted, since the planner does not lock test counts).
    2. `cd el-templo-bot && pnpm test 2>&1 | tail -5` — expect zero regressions across the full bot test suite.
    3. `cd el-templo-bot && pnpm tsc --noEmit` — expect exit 0.
    4. `cd el-templo-bot && pnpm lint 2>&1 | tail -10` — expect exit 0, no new errors.
    5. `bash el-templo-bot/scripts/check-debounce-invariant.sh` — expect exit 0 with message `Cross-phase invariant OK: TTL=600 >= minimum 395`.
    6. Behavioral spot-check that the SDK accepts and stores the option:
       `node -e "import('openai').then(m => console.log(new m.default({timeout:45000, maxRetries:0, apiKey:'sk-test'}).maxRetries))"`
       Expected stdout: `0`.

    Then run the SHA-256 multi-doc invariant drift-check (mirrors 94-01 verification step 8 — extracts the canonical block from each location and compares hashes). Use the exact awk extraction pattern proven in 94-VERIFICATION.md (matches block-end on the literal `Minimum TTL =` line):

    ```bash
    extract_block() { awk -v s="$2" 'NR>=s {print; if (/Minimum TTL =/) exit}' "$1"; }
    expected="67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344"
    fail=0
    for spec in \
      ".planning/phases/93-handler-concurrency/93-CONTEXT.md:84" \
      ".planning/phases/94-openai-latency-graceful-failure/94-CONTEXT.md:34" \
      ".planning/ROADMAP.md:62" \
      ".planning/ROADMAP.md:93" \
      ".planning/MACRO-ROADMAP.md:99"; do
      f="${spec%:*}"; l="${spec##*:}"
      got=$(extract_block "$f" "$l" | shasum -a 256 | cut -d' ' -f1)
      if [ "$got" = "$expected" ]; then
        echo "OK   $f:$l = $got"
      else
        echo "DRIFT $f:$l = $got (expected $expected)" >&2
        fail=1
      fi
    done
    [ "$fail" -eq 0 ]
    ```

    Expected: 5 `OK` lines (one per canonical location — `ROADMAP.md` appears twice at `:62` and `:93`), script exits 0. If ANY location reports `DRIFT`, halt before commit and report the drift to the user; do NOT auto-resync the docs (multi-doc invariant resync is its own gated operation per the user-MEMORY rule on multi-doc invariants requiring sha256 verification).

    Once all checks pass, commit only the production source file change with the HEREDOC commit pattern:

    ```
    git add el-templo-bot/src/ai/openai.ts
    git commit -m "$(cat <<'EOF'
    fix(bot): set OpenAI maxRetries=0 to honor Cross-Phase Invariant (CR-02)

    Closes gap CR-02 from 94-VERIFICATION.md gaps[0].

    The OpenAI SDK defaults to maxRetries: 2. With Phase 94's timeout=45000
    in place but no maxRetries override, a single provider.chat() call can
    take up to 3 × 45s = 135s wall-clock. Plugged into the Cross-Phase
    Invariant formula, real worst-case end-to-end was
        135 × 5 + 30 × 5 + 20 = 845s
    exceeding DEBOUNCE_TTL_SECONDS=600 by 245s — the exact failure mode the
    Phase 93 invariant exists to prevent (lock expires mid-handler →
    BUG-01 reintroduced).

    With maxRetries: 0 explicitly set on the OpenAI client at openai.ts:50,
    real worst-case per-call is exactly OPENAI_TIMEOUT_MS / 1000 seconds
    and the canonical formula
        45 × 1 × 5 + 30 × 5 + 20 = 395s ≤ 600s
    holds true byte-for-byte. No cross-doc invariant block mutation
    required — sha256 67670b1e... remains identical across all 4 canonical
    locations after this commit.

    The handler already provides retry/recovery via the Phase 94 LAT-02
    interim UX + LAT-03 graceful fallback path; SDK-level double-retry is
    redundant with the handler-level recovery logic and is what broke the
    invariant. No env-override (OPENAI_MAX_RETRIES) is introduced — the
    invariant locks this value at 0; operator tunability here would
    re-introduce the bug surface CR-02 closed.

    maxRetries=1 was rejected at plan time: would yield real worst-case
    90 × 5 + 30 × 5 + 20 = 620s > 600s TTL. Only maxRetries=0 preserves
    the original locked formula without a multi-doc resync.

    Closes 94-VERIFICATION CR-02 — invariant TTL 600s ≥ minimum 395s
    holds. Regression test (test/v5-3-3-openai-latency.test.ts SC#1 maxRetries
    assertions, RED commit test(94-02): ...) now passes.
    EOF
    )"
    ```

  </action>
  <verify>
    <automated>cd el-templo-bot && pnpm test v5-3-3-openai-latency 2>&1 | tail -10; pnpm test 2>&1 | tail -5; pnpm tsc --noEmit; pnpm lint 2>&1 | tail -5; bash scripts/check-debounce-invariant.sh; cd .. && extract_block() { awk -v s="$2" 'NR>=s {print; if (/Minimum TTL =/) exit}' "$1"; }; expected="67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344"; for spec in ".planning/phases/93-handler-concurrency/93-CONTEXT.md:84" ".planning/phases/94-openai-latency-graceful-failure/94-CONTEXT.md:34" ".planning/ROADMAP.md:62" ".planning/ROADMAP.md:93" ".planning/MACRO-ROADMAP.md:99"; do f="${spec%:*}"; l="${spec##*:}"; got=$(extract_block "$f" "$l" | shasum -a 256 | cut -d' ' -f1); [ "$got" = "$expected" ] || { echo "DRIFT $f:$l"; exit 1; }; done; node -e "import('openai').then(m=>console.log(new m.default({timeout:45000,maxRetries:0,apiKey:'sk-test'}).maxRetries))"; grep -v '^\s*[/*]' el-templo-bot/src/ai/openai.ts | grep -c "maxRetries: 0"</automated>
  </verify>
  <done>
    `el-templo-bot/src/ai/openai.ts` modified in a single atomic commit:
    - Line 50 contains `maxRetries: 0` inside the `new OpenAI({...})` options object.
    - Line 52 logger payload includes `maxRetries: 0`.
    - Doc comment block at `:24-36` (now extended) explains the rationale.

    Comment-filtered grep `grep -v '^\s*[/*]' el-templo-bot/src/ai/openai.ts | grep -c "maxRetries: 0"` returns ≥ 2 (constructor option + logger payload; doc comment is excluded by the filter).

    All Phase-94 unit tests pass (existing 6 + Task 1's 2 = 8 in suite v5-3-3-openai-latency). Full bot test suite shows zero regressions. `pnpm tsc --noEmit` and `pnpm lint` both exit 0. `bash el-templo-bot/scripts/check-debounce-invariant.sh` exits 0 with message `Cross-phase invariant OK: TTL=600 >= minimum 395`.

    Behavioral spot-check via the Node one-liner prints `0` — the SDK accepts the option and stores it on the client instance.

    SHA-256 drift check: all 5 canonical block locations (`93-CONTEXT.md:84`, `94-CONTEXT.md:34`, `ROADMAP.md:62`, `ROADMAP.md:93`, `MACRO-ROADMAP.md:99`) hash to `67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344` — no drift introduced.

    Atomic commit `fix(bot): set OpenAI maxRetries=0 to honor Cross-Phase Invariant (CR-02)` landed on the working branch IMMEDIATELY after the RED commit from Task 1 (no other commits between them).

  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                   | Description                                                                                                                                                                                                       |
| ------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| bot → OpenAI API (HTTPS, SDK)              | Trusted-but-unreliable. 94-02 explicitly suppresses the SDK's silent retry behavior at this boundary so latency is bounded predictably by the handler-level recovery layer, not by the SDK's hidden 2-retry loop. |
| operator env → OpenAI client config        | Operator controls `OPENAI_TIMEOUT_MS`. 94-02 deliberately does NOT introduce `OPENAI_MAX_RETRIES` — the invariant locks this value. See T-94-02-02.                                                               |
| planner (future phases) → invariant docs   | Future planners can silently re-enable SDK retries by adding back a `maxRetries: N>0` argument. The regression test in 94-02 catches this. See T-94-02-01.                                                        |
| upstream openai package → installed semver | The `openai` npm package could change its constructor option name in a major version bump. The explicit `maxRetries: 0` argument insulates the bot from any change in the default. See T-94-02-03.                |
| executor → npm install                     | NOT relevant — no new packages installed in 94-02. `openai` is already a dependency at v5.3.3 baseline.                                                                                                           |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                                                                 | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ---------- | ---------------------- | --------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-94-02-01 | Denial-of-Service      | Future planner re-introduces `maxRetries > 0` silently (forgetting why it's locked)                       | mitigate    | Task 1's regression test asserts `expect(readClientMaxRetries(provider)).toBe(0)` in two `it()` blocks. Any commit that bumps maxRetries fires the test in CI before merging. The doc comment append (Edit 3) carries the rationale inline at the call site so a future reader sees WHY it's 0 without spelunking through history.                                                                                                                                                                                                   |
| T-94-02-02 | Tampering              | Future planner introduces `OPENAI_MAX_RETRIES` env var as an operator override (well-intentioned config)  | mitigate    | The doc comment at `openai.ts:24-36` (extended) explicitly states no env-override is provided; operator-tunability here re-introduces the bug surface. If a future phase genuinely needs configurability, that phase must also re-prove the invariant via the sha256 multi-doc check and adjust the canonical block accordingly. Threat is documented, not enforced via test (no test for absence of an env read).                                                                                                                   |
| T-94-02-03 | Tampering              | Upstream `openai` package changes default `maxRetries` (e.g., bumps from 2 → 3 in a minor version)        | mitigate    | The explicit `maxRetries: 0` argument insulates the bot from upstream-default drift. The regression test pins the wire value to `0` regardless of upstream defaults; any change in upstream defaults is irrelevant because we don't rely on the default.                                                                                                                                                                                                                                                                             |
| T-94-02-04 | Tampering              | Canonical invariant block drifts across the 4 markdown docs as collateral of an unrelated edit            | mitigate    | The sha256 drift-check in the verification block extracts the canonical block from each of 5 location-line pairs and compares against `67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344`. If any location drifts, the executor halts before commit. Pre-existing discipline from 94-01 (verification step 8); 94-02 inherits the same gate.                                                                                                                                                                          |
| T-94-02-05 | Information Disclosure | Logging `maxRetries: 0` at boot                                                                           | accept      | The value is non-sensitive (not a secret, not user PII). Confirming the value at boot helps incident debugging (operators / verifiers can grep boot logs for `"maxRetries":0` to confirm the invariant guard is wired).                                                                                                                                                                                                                                                                                                              |
| T-94-02-06 | Denial-of-Service      | With `maxRetries: 0`, transient OpenAI 5xx (e.g., 502 from a brief upstream blip) no longer auto-recovers | accept      | The handler-level recovery layer (LAT-02 interim UX + LAT-03 graceful fallback) explicitly covers this: user gets "Dame un segundo 🙌" then "Tuve un problemita técnico, ¿me lo escribís de nuevo?" and the bot returns cleanly. Trading SDK-internal silent retries (which inflate latency under the lock) for user-visible graceful failure is the design decision Phase 94 made. The empirical rate of transient 5xx that succeed on retry but fail without is unmeasured in dev — accepted; v5.4.0 telemetry phase will measure. |
| T-94-SC    | Tampering              | npm/pip/cargo installs                                                                                    | accept      | Phase 94-02 installs NO new packages. `openai` is already a dependency. No `[ASSUMED]` / `[SUS]` / `[SLOP]` packages introduced. Package legitimacy gate not triggered.                                                                                                                                                                                                                                                                                                                                                              |

</threat_model>

<verification>

## Phase-Level Verification (run after BOTH Task 1 and Task 2 commits)

1. **TDD RED→GREEN cycle (atomic ordering):**

   ```bash
   git log --oneline -n 3
   ```

   Expected: the GREEN commit (`fix(bot): set OpenAI maxRetries=0 to honor Cross-Phase Invariant (CR-02)`) is the most recent, IMMEDIATELY preceded by the RED commit (`test(94-02): fail-in-main regression for SDK maxRetries=0 (CR-02 closure)`). Same author, same day, no intervening commits.

2. **RED commit touched only the test file:**

   ```bash
   git show $(git log --format=%H --grep="test(94-02):" -n 1) --stat
   ```

   Expected: exactly one file in the stat — `el-templo-bot/test/v5-3-3-openai-latency.test.ts`. No production source modified.

3. **GREEN commit touched only the production source file:**

   ```bash
   git show $(git log --format=%H --grep="fix(bot): set OpenAI maxRetries=0" -n 1) --stat
   ```

   Expected: exactly one file in the stat — `el-templo-bot/src/ai/openai.ts`. No test, env, script, or doc modified.

4. **Phase 94 test suite all green:**

   ```bash
   cd el-templo-bot && pnpm test v5-3-3-openai-latency
   ```

   Expected: all tests in the suite passing (Phase 94's existing 6 + 94-02's added 2 = 8; the exact count is informative — the plan does not lock the count). Wall-time < 10 seconds.

5. **Phase 93 regression suite still green:**

   ```bash
   cd el-templo-bot && pnpm test v5-3-3-handler-concurrency
   ```

   Expected: same green count as the post-`d3de86b1` baseline (`Tests 3 passed (3)` per 94-VERIFICATION.md `:96`).

6. **Full bot test suite — zero regressions:**

   ```bash
   cd el-templo-bot && pnpm test
   ```

   Expected: all tests passing. 94-VERIFICATION.md baseline was `Test Files 28 passed (28) / Tests 615 passed (615)`; the new count is `615 + 2 = 617` (informative; not asserted as a hard count).

7. **Type / lint clean:**

   ```bash
   cd el-templo-bot && pnpm tsc --noEmit && pnpm lint
   ```

   Expected: both exit 0. No new `any` types, no `console.*` calls introduced.

8. **Cross-Phase Invariant script — unchanged, still passes:**

   ```bash
   bash el-templo-bot/scripts/check-debounce-invariant.sh
   ```

   Expected: exit 0 with message `Cross-phase invariant OK: TTL=600 >= minimum 395`. The script formula itself is correct as-is (per-call wall = `OPENAI_TIMEOUT_MS / 1000`) under the new maxRetries=0 lock; no script edit required.

9. **SHA-256 multi-doc invariant hash check (drift detection — MUST run even though Phase 94-02 does not mutate the block):**

   ```bash
   extract_block() { awk -v s="$2" 'NR>=s {print; if (/Minimum TTL =/) exit}' "$1"; }
   expected="67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344"
   fail=0
   for spec in \
     ".planning/phases/93-handler-concurrency/93-CONTEXT.md:84" \
     ".planning/phases/94-openai-latency-graceful-failure/94-CONTEXT.md:34" \
     ".planning/ROADMAP.md:62" \
     ".planning/ROADMAP.md:93" \
     ".planning/MACRO-ROADMAP.md:99"; do
     f="${spec%:*}"; l="${spec##*:}"
     got=$(extract_block "$f" "$l" | shasum -a 256 | cut -d' ' -f1)
     if [ "$got" = "$expected" ]; then
       echo "OK   $f:$l = $got"
     else
       echo "DRIFT $f:$l = $got (expected $expected)" >&2
       fail=1
     fi
   done
   [ "$fail" -eq 0 ]
   ```

   Expected: 5 `OK` lines and exit 0. The expected hash `67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344` is the byte-identical hash of the canonical block confirmed across all 4 markdown locations (`ROADMAP.md` contributes 2 occurrences at `:62` and `:93`) at plan-time (2026-05-18). If ANY location drifts, halt before commit and report the drift to the user. Do NOT auto-resync — multi-doc invariant resync is its own gated operation per the user-MEMORY rule (`feedback_multi_doc_invariant_hash_check.md`: same formula/rule across N docs requires byte-verification, not paraphrase).

10. **Behavioral spot-check — SDK accepts and stores the maxRetries option:**

    ```bash
    node -e "import('openai').then(m => console.log(new m.default({timeout:45000, maxRetries:0, apiKey:'sk-test'}).maxRetries))"
    ```

    Expected: stdout prints `0`. Confirms the SDK's public surface honors the option as we expect.

11. **Source-grep gates (comment-filtered per critical_rules — exclude lines starting with `*` or `//` or whitespace+them):**

    ```bash
    grep -v '^\s*[/*]' el-templo-bot/src/ai/openai.ts | grep -c "maxRetries: 0"   # expect >= 2 (constructor option + logger payload)
    grep -c "maxRetries: 0" el-templo-bot/src/ai/openai.ts                         # expect >= 3 (above + at least one doc-comment mention)
    grep -c "readClientMaxRetries" el-templo-bot/test/v5-3-3-openai-latency.test.ts # expect >= 3 (1 declaration + 2 call sites)
    grep -c "expect(readClientMaxRetries(provider)).toBe(0)" el-templo-bot/test/v5-3-3-openai-latency.test.ts # expect >= 2
    ```

12. **Out-of-scope guardrails (NEGATIVE assertions — must remain empty):**

    ```bash
    RED_SHA=$(git log --format=%H --grep="test(94-02):" -n 1)
    git diff ${RED_SHA}^..HEAD -- el-templo-bot/.env.example                                   # expect empty
    git diff ${RED_SHA}^..HEAD -- el-templo-bot/src/webhook/handler.ts                         # expect empty
    git diff ${RED_SHA}^..HEAD -- el-templo-bot/scripts/check-debounce-invariant.sh            # expect empty
    git diff ${RED_SHA}^..HEAD -- el-templo-bot/src/tools.ts                                   # expect empty (Phase 95/97 territory)
    git diff ${RED_SHA}^..HEAD -- el-templo-bot/src/ai/anthropic.ts                            # expect empty (CR-01 ACCEPTED, out of scope)
    git diff ${RED_SHA}^..HEAD -- .planning/phases/93-handler-concurrency/93-CONTEXT.md        # expect empty
    git diff ${RED_SHA}^..HEAD -- .planning/phases/94-openai-latency-graceful-failure/94-CONTEXT.md  # expect empty
    git diff ${RED_SHA}^..HEAD -- .planning/ROADMAP.md                                          # expect empty
    git diff ${RED_SHA}^..HEAD -- .planning/MACRO-ROADMAP.md                                    # expect empty
    ```

    Expected: every diff is empty. Confirms 94-02 stayed within its declared `files_modified` (only `el-templo-bot/src/ai/openai.ts` and `el-templo-bot/test/v5-3-3-openai-latency.test.ts`).

</verification>

<success_criteria>

The phase-94 gap-closure is complete when ALL of the following are true:

- **CR-02 closed:** `el-templo-bot/src/ai/openai.ts:50` constructs the OpenAI client with `maxRetries: 0` in the options object. Confirmed by Task 1's regression test (`readClientMaxRetries(provider) === 0`, with and without `OPENAI_TIMEOUT_MS` override).
- **Invariant holds:** With maxRetries=0, real worst-case end-to-end is `45 × 1 × 5 + 30 × 5 + 20 = 395s ≤ 600s DEBOUNCE_TTL_SECONDS`. `bash el-templo-bot/scripts/check-debounce-invariant.sh` exits 0.
- **No canonical-block drift:** All 4 markdown documents (`93-CONTEXT.md`, `94-CONTEXT.md`, `ROADMAP.md` × 2 locations, `MACRO-ROADMAP.md`) still hash the canonical block to `67670b1e1099bf7c8a5285414736f16e8a010a010348bf6566790d0db3163344`. Confirmed by verification step 9.
- **TDD discipline preserved:** Two atomic commits in order — `test(94-02): ...` (RED, touches only the test file, new tests fail) followed by `fix(bot): ...` (GREEN, touches only `openai.ts`, all tests pass).
- **Type / lint clean:** `pnpm tsc --noEmit` and `pnpm lint` both pass in `el-templo-bot`.
- **No regression:** Phase 93 and Phase 94 test suites remain green. Full bot test suite shows zero regressions vs. baseline.
- **No scope leak:** Out-of-scope guardrails (verification step 12) all empty. No edits to `.env.example`, `handler.ts`, the invariant script, `tools.ts`, the Anthropic provider, or any of the 4 markdown invariant docs.
- **Phase 94 unblocking:** With 94-02 shipped, Phase 94 status is unblocked for `passed` after the v5.4.0 live BUG-02 smoke test ships (the only remaining acceptance gate per `94-VERIFICATION.md` `human_verification[0]`).

</success_criteria>

<output>
After both tasks complete, create `.planning/phases/94-openai-latency-graceful-failure/94-02-SUMMARY.md` following the same structure as `94-01-SUMMARY.md`. Sections required: Goal (CR-02 closure), Outcome, Files Changed (the 2 files in `files_modified`), Tests Added (the 2 new `it()` blocks + the `readClientMaxRetries` helper), Commits (RED sha + GREEN sha + their full subject lines), Cross-Phase Invariant Status (formula now holds with maxRetries=0, sha256 `67670b1e…` unchanged across all 4 canonical doc locations), Out-of-Scope Verified (the verification step 12 negative-assertion list).
</output>
