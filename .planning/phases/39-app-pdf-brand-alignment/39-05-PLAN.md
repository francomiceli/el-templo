---
phase: 39-app-pdf-brand-alignment
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - el-templo-admin/src/css/app.scss
  - el-templo-admin/package.json
autonomous: false
requirements: []
must_haves:
  truths:
    - "Admin app body font is Geologica (not Roboto)"
    - "Admin app uses terracotta primary, aged gold secondary, deep charcoal accent via Quasar defaults"
    - "Admin app background is marble-cream (#f2ede5)"
    - "el-templo-app builds without errors after all brand changes"
    - "el-templo-admin builds without errors after all brand changes"
    - "Visual inspection confirms brand cohesion across app and admin"
  artifacts:
    - el-templo-admin/src/css/app.scss
  key_links:
    - "admin app.scss body font-family consumed by all admin UI text"
    - "Quasar config in quasar.config.ts may need brand color overrides if admin has no quasar.variables.scss"
---

<objective>
Apply the light brand touch to el-templo-admin (body font, palette, background per CONTEXT: "Light brand touch only"), then run full build verification across both apps and perform visual verification.

Purpose: Admin gets just enough brand alignment (Geologica body, marble-cream background, terracotta primary) without deep restyling, then both apps are verified to build cleanly.
Output: Updated admin app.scss, verified builds of both apps.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/39-app-pdf-brand-alignment/39-CONTEXT.md
@el-templo-admin/src/css/app.scss

<interfaces>
<!-- Current admin app.scss -->
```scss
// Admin app styles - clean and functional design
body {
  font-family: 'Roboto', sans-serif;
}
```

<!-- Brand tokens (source of truth) -->

```css
--color-marble-cream: #f2ede5;
--color-terracotta: #c07a56;
--color-aged-gold: #b89b5e;
--color-deep-charcoal: #3d3732;
--font-clarity: "Geologica", sans-serif;
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply light brand touch to admin app</name>
  <files>el-templo-admin/src/css/app.scss, el-templo-admin/package.json</files>
  <action>
Per CONTEXT: "Light brand touch only: update $primary/$secondary/$accent to new palette, swap Roboto for Geologica body text, marble-cream background. No deep restyling of admin UI components."

1. Install Geologica font package in el-templo-admin:

   ```bash
   cd el-templo-admin && pnpm add @fontsource/geologica
   ```

2. Update `app.scss`:

   ```scss
   // Admin app styles — light brand alignment (Phase 39)
   // Only body font, palette, and background. No deep restyling.

   @import "@fontsource/geologica/400.css";
   @import "@fontsource/geologica/500.css";
   @import "@fontsource/geologica/600.css";

   body {
     font-family: "Geologica", sans-serif;
     background-color: #f2ede5; // Marble Cream
   }
   ```

3. Check if el-templo-admin has a `quasar.variables.scss` or a `quasar.config.ts` with brand color overrides. The admin app may use Quasar's default colors.

   If admin has NO `quasar.variables.scss`, create one at `el-templo-admin/src/css/quasar.variables.scss` with:

   ```scss
   // Admin brand colors — light touch only
   $primary: #c07a56; // Terracotta
   $secondary: #b89b5e; // Aged Gold
   $accent: #3d3732; // Deep Charcoal
   ```

   And ensure the quasar.config.ts references this file in `css: ['src/css/app.scss']` or `framework.config.brand` or equivalent.

   If admin DOES have color configuration elsewhere (e.g., in quasar.config.ts framework.config.brand), update there instead.

4. Ensure no dark mode overrides for admin (per CONTEXT: "No dark mode update for admin").
   </action>
   <verify>
   <automated>cd el-templo-admin && grep "Geologica" src/css/app.scss && grep "#f2ede5" src/css/app.scss && grep -c "Roboto" src/css/app.scss | grep "^0$" && echo "PASS"</automated>
   </verify>
   <done>Admin app uses Geologica body font, marble-cream background, and terracotta primary via Quasar variables or config. No Roboto reference in app.scss.</done>
   </task>

<task type="checkpoint:human-verify">
  <name>Task 2: Build verification and visual inspection</name>
  <files>el-templo-app/, el-templo-admin/</files>
  <action>
1. Build both apps to verify no compilation errors:
   ```bash
   cd el-templo-app && npx quasar build 2>&1 | tail -5
   cd el-templo-admin && npx quasar build 2>&1 | tail -5
   ```

2. Run the comprehensive Cinzel/navy/bronze audit across both apps:

   ```bash
   echo "=== Cinzel references ==="
   grep -rn "Cinzel\|cinzel" el-templo-app/src/ el-templo-admin/src/ | grep -v node_modules || echo "None found"

   echo "=== Navy hex values ==="
   grep -rn "#2c3e5c\|#1a2a3e\|#0f1a28\|#24364A" el-templo-app/src/ el-templo-admin/src/ | grep -v node_modules || echo "None found"

   echo "=== Old bronze hex ==="
   grep -rn "#b8956c\|#B08D6E" el-templo-app/src/ el-templo-admin/src/ | grep -v node_modules || echo "None found"

   echo "=== Old cream hex ==="
   grep -rn "#f5f0e8\|#F2EBE1" el-templo-app/src/ el-templo-admin/src/ | grep -v node_modules || echo "None found"

   echo "=== Blue info hex ==="
   grep -rn "#31ccec" el-templo-app/src/ el-templo-admin/src/ | grep -v node_modules || echo "None found"

   echo "=== Roboto in admin ==="
   grep -rn "Roboto\|roboto" el-templo-admin/src/css/ || echo "None found"
   ```

3. Start dev servers for visual inspection:
   - el-templo-app: `cd el-templo-app && npx quasar dev`
   - el-templo-admin: `cd el-templo-admin && npx quasar dev`

4. Present to user for visual verification:
   - Header/toolbar: charcoal background with Montserrat title
   - Body text: Geologica renders correctly
   - Block backgrounds: warm terracotta/gold tints
   - Splash screen: charcoal gradient, not navy
   - Admin: Geologica body font, marble-cream background, terracotta accents
   - PDF: download a test PDF and verify fonts/colors
     </action>
     <verify>Both apps build without errors. Zero matches for old brand values (Cinzel, navy hex, old bronze, old cream, blue info). Visual inspection confirms brand cohesion.</verify>
     <done>Both el-templo-app and el-templo-admin build successfully. Full audit confirms zero legacy brand values remain. Visual inspection approved by user.</done>
     </task>

</tasks>

<verification>
- `cd el-templo-app && npx quasar build` — exits with code 0
- `cd el-templo-admin && npx quasar build` — exits with code 0
- Full audit grep returns zero matches for all legacy values
- PDF generation produces correctly branded output
</verification>

<success_criteria>

1. Admin app uses Geologica body font and marble-cream background
2. Admin app shows terracotta primary color for buttons and accents
3. Both apps build without errors
4. Zero Cinzel, navy, old bronze, old cream, or blue info hex values anywhere in el-templo-app/src/ or el-templo-admin/src/
5. User visually confirms the brand cohesion
   </success_criteria>

<output>
After completion, create `.planning/phases/39-app-pdf-brand-alignment/39-05-SUMMARY.md`
</output>
