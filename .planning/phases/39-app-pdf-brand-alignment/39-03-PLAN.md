---
phase: 39-app-pdf-brand-alignment
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - el-templo-app/src/layouts/MainLayout.vue
  - el-templo-app/src/modules/training/components/player/SplashScreen.vue
  - el-templo-app/src/modules/training/components/player/BlockHeader.vue
  - el-templo-app/src/modules/training/components/player/ProgressBar.vue
  - el-templo-app/src/modules/training/components/player/RpeSlider.vue
  - el-templo-app/src/modules/training/components/player/CelebrationScreen.vue
  - el-templo-app/src/modules/training/components/player/SessionSummary.vue
  - el-templo-app/src/modules/training/components/player/ExerciseCard.vue
  - el-templo-app/src/modules/training/components/player/VideoPlaceholder.vue
  - el-templo-app/src/modules/training/components/DayCard.vue
  - el-templo-app/src/modules/training/components/BlockCard.vue
  - el-templo-app/src/modules/training/components/BlockChoiceCard.vue
  - el-templo-app/src/modules/training/pages/WeeklyView.vue
  - el-templo-app/src/modules/training/pages/ConceptosPage.vue
  - el-templo-app/src/modules/progression/components/RpeTrendChart.vue
autonomous: true
requirements: []
must_haves:
  truths:
    - "MainLayout header uses deep charcoal gradient with terracotta accents, not navy gradient"
    - "All Cinzel font-family references in training and layout components are replaced with Montserrat"
    - "All hardcoded navy/bronze hex values in component styles are replaced with brand palette"
    - "Splash and celebration screens use charcoal gradients, not navy gradients"
    - "App header title uses Montserrat with brand letter-spacing"
    - "RpeTrendChart uses terracotta and aged gold instead of navy and bronze"
  artifacts:
    - el-templo-app/src/layouts/MainLayout.vue
    - el-templo-app/src/modules/training/components/player/SplashScreen.vue
    - el-templo-app/src/modules/training/components/player/CelebrationScreen.vue
    - el-templo-app/src/modules/training/components/player/SessionSummary.vue
  key_links:
    - "MainLayout.vue imports quasar.variables.scss — $primary now resolves to terracotta"
    - "BlockHeader.vue uses getBlockCSSColor() from blockColors.ts — now returns terracotta/aged-gold"
    - "RpeTrendChart.vue hardcoded colors must match new palette"
---

<objective>
Restyle el-templo-app's layout, training components, and player components: replace all Cinzel references with Montserrat, replace all hardcoded navy/bronze hex values with the new brand palette, update gradients to charcoal-based.

Purpose: The core user-facing training experience (DayPlayer, blocks, exercises, weekly view) visually matches the new brand system.
Output: 15 components updated — zero Cinzel references, zero navy hex values.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/39-app-pdf-brand-alignment/39-CONTEXT.md
@el-templo-app/src/css/quasar.variables.scss

<interfaces>
<!-- Plan 01 outputs these updated values -->
From el-templo-app/src/css/quasar.variables.scss (after Plan 01):
```scss
$primary: #c07a56;    // Terracotta
$secondary: #b89b5e;  // Aged Gold
$accent: #3d3732;     // Deep Charcoal
$dark: #3d3732;       // Deep Charcoal
$dark-page: #2e2a26;  // Darker charcoal
$cream: #f2ede5;      // Marble Cream
```

From el-templo-app/src/modules/training/utils/blockColors.ts (after Plan 01):

```typescript
// BRAND_TERRACOTTA = '#c07a56'
// BRAND_AGED_GOLD = '#b89b5e'
export function getBlockCSSColor(role: BlockRole): string;
export function getBlockHeaderGradient(role: BlockRole): string;
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle MainLayout, SplashScreen, CelebrationScreen, SessionSummary, and VideoPlaceholder</name>
  <files>
    el-templo-app/src/layouts/MainLayout.vue,
    el-templo-app/src/modules/training/components/player/SplashScreen.vue,
    el-templo-app/src/modules/training/components/player/CelebrationScreen.vue,
    el-templo-app/src/modules/training/components/player/SessionSummary.vue,
    el-templo-app/src/modules/training/components/player/VideoPlaceholder.vue
  </files>
  <action>
**MainLayout.vue:**
1. `.main-header` gradient: Change from `linear-gradient(135deg, $primary 0%, #3d5275 100%)` to `linear-gradient(135deg, #3d3732 0%, #4a433c 100%)` — per CONTEXT: "deep charcoal background with terracotta accents"
2. `.header-title` font-family: Change `'Cinzel', serif` → `'Montserrat', sans-serif`
3. `.drawer-header` font-family: Change `'Cinzel', serif` → `'Montserrat', sans-serif`; color stays `$primary` (now terracotta via updated variables)
4. `.main-drawer` background stays `$cream` (now marble-cream)

**SplashScreen.vue:**

1. `.splash-screen` gradient: Change `linear-gradient(135deg, #1a2a3e 0%, #2c3e5c 50%, #1a2a3e 100%)` → `linear-gradient(135deg, #2e2a26 0%, #3d3732 50%, #2e2a26 100%)` (charcoal)
2. `.logo-container` — Change `rgba(184, 149, 108, 0.2)` (#b8956c) → `rgba(184, 155, 94, 0.2)` (#b89b5e aged gold) for background; border similarly to aged gold with 0.4 opacity
3. `.motivation` font-family: Change `'Cinzel', Georgia, serif` → `'Montserrat', sans-serif`

**CelebrationScreen.vue:**

1. Background gradient: Change navy `linear-gradient(135deg, #1a2a3e 0%, #2c3e5c 50%, #1a2a3e 100%)` → charcoal `linear-gradient(135deg, #2e2a26 0%, #3d3732 50%, #2e2a26 100%)`
2. Font-family: Change `'Cinzel', Georgia, serif` → `'Montserrat', sans-serif`
3. Accent color: Change `#b8956c` → `#b89b5e` (aged gold)

**SessionSummary.vue:**

1. Summary header gradient: Change `linear-gradient(135deg, #1a2a3e 0%, #2c3e5c 100%)` → `linear-gradient(135deg, #2e2a26 0%, #3d3732 100%)` (charcoal)
2. Font-family on .session-label or similar heading: `'Cinzel', Georgia, serif` → `'Montserrat', sans-serif`
3. `.summary-card` or card background: Change `#f5f0e8` → `#f2ede5` (marble-cream)

**VideoPlaceholder.vue:**

1. Background color: Change `#1a2a3e` → `#2e2a26` (dark charcoal)
2. Background gradient: Change `linear-gradient(135deg, #0f1c2e 0%, #1a2a3e 50%, #243548 100%)` → `linear-gradient(135deg, #1e1b18 0%, #2e2a26 50%, #3d3732 100%)` (charcoal family)
   </action>
   <verify>
   <automated>cd el-templo-app && grep -rn "Cinzel\|#1a2a3e\|#2c3e5c\|#0f1a28\|#3d5275\|#0f1c2e\|#243548" src/layouts/MainLayout.vue src/modules/training/components/player/SplashScreen.vue src/modules/training/components/player/CelebrationScreen.vue src/modules/training/components/player/SessionSummary.vue src/modules/training/components/player/VideoPlaceholder.vue | wc -l | grep "^0$" && echo "PASS"</automated>
   </verify>
   <done>MainLayout uses charcoal header + Montserrat. Splash/Celebration/Summary screens use charcoal gradients + Montserrat. VideoPlaceholder uses charcoal background. No Cinzel or navy hex values in any of these files.</done>
   </task>

<task type="auto">
  <name>Task 2: Restyle BlockHeader, ExerciseCard, ProgressBar, RpeSlider, and RpeTrendChart</name>
  <files>
    el-templo-app/src/modules/training/components/player/BlockHeader.vue,
    el-templo-app/src/modules/training/components/player/ExerciseCard.vue,
    el-templo-app/src/modules/training/components/player/ProgressBar.vue,
    el-templo-app/src/modules/training/components/player/RpeSlider.vue,
    el-templo-app/src/modules/progression/components/RpeTrendChart.vue
  </files>
  <action>
**BlockHeader.vue:**
1. `.block-name` font-family: Change `'Cinzel', Georgia, serif` → `'Montserrat', sans-serif`
2. `.block-route` color: Change `#b8956c` → `#b89b5e` (aged gold)
3. Note: BlockHeader already consumes `getBlockCSSColor()` which Plan 01 updated — so accent colors update automatically

**ExerciseCard.vue:**

1. Search for hardcoded colors `#2c3e5c` and `#b8956c` — replace with `#3d3732` (charcoal) and `#b89b5e` (aged gold) respectively
2. If there are font-family references to Cinzel, replace with Montserrat

**ProgressBar.vue:**

1. Font-family: Change `'Cinzel', serif` → `'Montserrat', sans-serif`

**RpeSlider.vue:**

1. Font-family: Change `'Cinzel', serif` → `'Montserrat', sans-serif`

**RpeTrendChart.vue:**

1. Color constants: Change `BRONZE = '#b8956c'` → `AGED_GOLD = '#b89b5e'` and `NAVY = '#2c3e5c'` → `TERRACOTTA = '#c07a56'`
2. Update variable references throughout the file (chart line color, point color)
3. Update JSDoc comment
   </action>
   <verify>
   <automated>cd el-templo-app && grep -rn "Cinzel\|#2c3e5c\|#b8956c" src/modules/training/components/player/BlockHeader.vue src/modules/training/components/player/ExerciseCard.vue src/modules/training/components/player/ProgressBar.vue src/modules/training/components/player/RpeSlider.vue src/modules/progression/components/RpeTrendChart.vue | wc -l | grep "^0$" && echo "PASS"</automated>
   </verify>
   <done>BlockHeader, ExerciseCard, ProgressBar, RpeSlider use Montserrat and new palette. RpeTrendChart uses terracotta/aged-gold. No Cinzel or old hex values remain.</done>
   </task>

<task type="auto">
  <name>Task 3: Restyle DayCard, BlockCard, BlockChoiceCard, WeeklyView, and ConceptosPage</name>
  <files>
    el-templo-app/src/modules/training/components/DayCard.vue,
    el-templo-app/src/modules/training/components/BlockCard.vue,
    el-templo-app/src/modules/training/components/BlockChoiceCard.vue,
    el-templo-app/src/modules/training/pages/WeeklyView.vue,
    el-templo-app/src/modules/training/pages/ConceptosPage.vue
  </files>
  <action>
**DayCard.vue:**
1. Search for all `font-family: 'Cinzel', serif` references (at least 2 locations) → replace with `'Montserrat', sans-serif`
2. Any hardcoded navy/bronze hex → replace with charcoal/aged-gold

**BlockCard.vue:**

1. `.block-name` or similar font-family: `'Cinzel', serif` → `'Montserrat', sans-serif`
2. Any hardcoded colors → new palette

**BlockChoiceCard.vue:**

1. Font-family: `'Cinzel', serif` → `'Montserrat', sans-serif`

**WeeklyView.vue:**

1. Font-family: `'Cinzel', serif` → `'Montserrat', sans-serif`
2. Any hardcoded navy/bronze → charcoal/aged-gold

**ConceptosPage.vue:**

1. Font-family references (at least 2 locations): `'Cinzel', Georgia, serif` → `'Montserrat', sans-serif`
2. Any hardcoded colors → new palette
   </action>
   <verify>
   <automated>cd el-templo-app && grep -rn "Cinzel\|cinzel" src/modules/training/components/DayCard.vue src/modules/training/components/BlockCard.vue src/modules/training/components/BlockChoiceCard.vue src/modules/training/pages/WeeklyView.vue src/modules/training/pages/ConceptosPage.vue | wc -l | grep "^0$" && echo "PASS"</automated>
   </verify>
   <done>DayCard, BlockCard, BlockChoiceCard, WeeklyView, ConceptosPage all use Montserrat. No Cinzel references remain in any training module files.</done>
   </task>

</tasks>

<verification>
- Run `grep -rn "Cinzel\|cinzel" el-templo-app/src/` — must find zero matches (all Cinzel replaced)
- Run `grep -rn "#2c3e5c\|#1a2a3e\|#0f1a28\|#3d5275\|#0f1c2e\|#243548" el-templo-app/src/` — must find zero matches (all navy-family colors eliminated)
- Run `grep -rn "#b8956c" el-templo-app/src/` — must find zero matches (old bronze replaced with #b89b5e aged gold)
- Run `cd el-templo-app && npx quasar build` — must compile without errors
</verification>

<success_criteria>

1. Zero Cinzel font-family references in el-templo-app/src/
2. Zero navy-family hex values (#2c3e5c, #1a2a3e, #0f1a28, #3d5275) in el-templo-app/src/
3. Zero old bronze (#b8956c) hex values in el-templo-app/src/
4. MainLayout header is charcoal with Montserrat title
5. All splash/celebration/summary screens use charcoal gradients
6. All player components use Montserrat for branded text
7. RpeTrendChart uses terracotta and aged gold
   </success_criteria>

<output>
After completion, create `.planning/phases/39-app-pdf-brand-alignment/39-03-SUMMARY.md`
</output>
