---
phase: 146-caja-validaci-n-e-imputaci-n-fundacional
plan: 06
type: execute
wave: 3
depends_on: [146-04, 146-05]
files_modified:
  - el-templo-admin/src/components/caja/MovEgresosTab.vue
  - el-templo-admin/src/composables/useTransactionsApi.ts
autonomous: true
requirements: [ARQUEO-02, ARQUEO-03]
must_haves:
  truths:
    - "El filtro Tipo de 'Movimientos de caja' incluye Cobros (además de Movimientos / Egresos / Ajustes)"
    - "Cada fila muestra su estado de validación (pendiente / validado / observado) marcado"
    - "Elegida una caja, la lista muestra todo lo imputado a esa caja (cobros + egresos + traspasos + ajustes), reflejando el backend del plan 05"
  artifacts:
    - path: "el-templo-admin/src/components/caja/MovEgresosTab.vue"
      provides: "filtro Cobros + columna/badge de estado + mapeo de kinds de cobro"
  key_links:
    - from: "MovEgresosTab filteredRows / filtro Tipo"
      to: "rows con kind ∈ {plan_charge, debt_settlement, advance_payment, refund}"
      via: "opción 'Cobros' en tipoOptions"
      pattern: "Cobros|cobros"
---

<objective>
ARQUEO-03 + ARQUEO-02 (frontend): en "Movimientos de caja", agregar **Cobros** al
filtro Tipo (hoy Movimientos / Egresos / Ajustes) y **marcar el estado de validación**
de cada fila (pendiente / validado / observado). La lista ya recibe todos los kinds y
el `validationStatus` del backend del plan 05.

Purpose: el arqueo por caja se vuelve operable desde la UI (ver cobros imputados +
distinguir pendientes de validados).
Output: filtro Cobros + badge de estado en MovEgresosTab.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@BRIEF-FEEDBACK-V52-CAJA.md
@.planning/phases/146-caja-validaci-n-e-imputaci-n-fundacional/146-05-PLAN.md

<interfaces>
From el-templo-admin/src/components/caja/MovEgresosTab.vue:
  filters.tipo  (~302)  tipoOptions: Movimientos|Egresos|Ajustes  (~310-313)
  filteredRows  (~340-348)  // mapea tipo → kind (cash_transfer/expense/adjustment)
  MovEgresoItem rows  // ahora incluye validationStatus (plan 05) y kinds de cobro
  kindLabel/kindColor  (~266-271)  KIND_LABELS  // ampliar para plan_charge/debt_settlement/advance_payment/refund
  tabla columns  (~359-362)  // agregar columna/badge de estado
From el-templo-admin/src/composables/useTransactionsApi.ts:
  getMovEgresosHistory(...)  (~207)  // ya trae las filas; confirmar que pasa validationStatus
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Filtro Cobros + marcado de estado en MovEgresosTab</name>
  <read_first>
    - el-templo-admin/src/components/caja/MovEgresosTab.vue (filters/tipoOptions ~302-313; filteredRows ~340-348; KIND_LABELS/kindColor ~256-271; columns ~359-362; detail dialog ~150-185)
    - el-templo-admin/src/composables/useTransactionsApi.ts (getMovEgresosHistory ~207; tipos de MovEgresoItem en el FE)
    - el-templo-api/src/modules/finance/types.ts (MovEgresoItem con validationStatus — plan 05)
  </read_first>
  <action>
    Agregar la opción `{ label: 'Cobros', value: 'cobros' }` a `tipoOptions`. En
    `filteredRows`, mapear `tipo==='cobros'` a las filas con kind ∈
    {plan_charge, debt_settlement, advance_payment, refund} (los kinds de cobro de
    socio). Ampliar `KIND_LABELS` y `kindColor` para esos kinds (etiquetas en español:
    "Cobro de plan", "Pago de saldo", "Cobro suelto", "Reintegro"). Agregar al tipo
    local de fila (y/o al composable) el campo `validationStatus` y renderizar un
    badge de estado por fila (pendiente=warning, validado=positive, observado=info) en
    una columna nueva "Estado" y en el detail dialog. Asegurar que
    `getMovEgresosHistory` propaga `validationStatus`. No cambiar el filtro de caja ni
    el de período. La pestaña "Transacciones" (MovimientosTab) no se toca.
  </action>
  <acceptance_criteria>
    - El filtro Tipo muestra Cobros y al seleccionarlo lista solo los kinds de cobro de socio.
    - Cada fila muestra un badge de estado (pendiente/validado/observado).
    - Elegida una caja, se ven cobros + egresos + traspasos + ajustes imputados a esa caja.
    - `cd el-templo-admin && pnpm build` verde.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-admin && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>El arqueo por caja muestra Cobros en el filtro y marca el estado de validación de cada fila.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                         |
| --------------------------------- | --------------------------------------------------- |
| UI MovEgresosTab → GET movEgresos | el scope/visibilidad lo impone el backend (plan 05) |

## STRIDE Threat Register

| Threat ID | Category               | Component                              | Disposition | Mitigation Plan                                                                                                              |
| --------- | ---------------------- | -------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------- |
| T-146-16  | Information Disclosure | mostrar cobros de cajas fuera de scope | accept      | El backend (plan 05) ya scopea por país/owner; la UI solo renderiza lo que recibe. Sin lógica de autorización en el cliente. |

</threat_model>

<verification>
- Filtro Cobros operativo; estado marcado por fila.
- Build admin verde.
</verification>

<success_criteria>

- ARQUEO-03: el filtro Tipo incluye Cobros.
- ARQUEO-02 (FE): pendientes y validados marcados con su estado.
  </success_criteria>

<output>
Create `.planning/phases/146-caja-validaci-n-e-imputaci-n-fundacional/146-06-SUMMARY.md` when done
</output>
