---
phase: 146-caja-validaci-n-e-imputaci-n-fundacional
plan: 05
type: execute
wave: 2
depends_on: [146-02]
files_modified:
  - el-templo-api/src/modules/finance/transaction-service.ts
  - el-templo-api/src/modules/finance/types.ts
  - el-templo-api/src/modules/finance/schemas.ts
  - el-templo-api/src/modules/finance/routes.ts
  - el-templo-api/test/finance/mov-egresos-history.test.ts
autonomous: true
requirements: [ARQUEO-01, ARQUEO-02, ARQUEO-04]
must_haves:
  truths:
    - "listMovEgresos, dado un cash_register_id, devuelve TODO lo imputado a esa caja: cobros de socio (plan_charge, debt_settlement, advance_payment, refund) + egresos (expense) + traspasos (cash_transfer) + ajustes (adjustment)"
    - "Cada fila trae su validationStatus (pendiente/validado/observado) para marcarse en la UI"
    - "Las filas pendientes y validadas aparecen ambas (no se filtran por estado)"
    - "La query comercial por socio list() (pestaña Transacciones) NO cambia de criterio"
  artifacts:
    - path: "el-templo-api/src/modules/finance/transaction-service.ts"
      provides: "listMovEgresos por cash_register_id, todos los kinds, con validationStatus"
    - path: "el-templo-api/test/finance/mov-egresos-history.test.ts"
      provides: "cobertura de arqueo por caja: todos los kinds + estado + list() intacto"
  key_links:
    - from: "listMovEgresos"
      to: "financial_transactions WHERE cash_register_id = ?"
      via: "filtro por caja en lugar de kind IN(...)"
      pattern: "cashRegisterId"
---

<objective>
ARQUEO-01 + ARQUEO-02 + ARQUEO-04: convertir "Movimientos de caja" (`listMovEgresos`)
en el **arqueo por caja**. Hoy filtra `kind IN ('cash_transfer','expense','adjustment')`
(solo lo sin-socio). El cambio: filtrar por `cash_register_id` y traer **TODOS** los
kinds imputados a esa caja (incluyendo plan_charge, debt_settlement, advance_payment,
refund), cada fila con su `validationStatus` para marcarse; mostrar pendientes y
validados. La pestaña "Transacciones" (`list()`, vista comercial por socio) NO se
toca — ARQUEO-04.

Purpose: el arqueo por caja debe cuadrar con "Saldos por caja" (suma de lo imputado).
Output: listMovEgresos reescrito por caja con todos los kinds + estado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@BRIEF-FEEDBACK-V52-CAJA.md

<interfaces>
From el-templo-api/src/modules/finance/transaction-service.ts:
  listMovEgresos(filters: MovEgresoFilters): Promise<PaginatedResult<MovEgresoItem>>   // ~1213; hoy conditions = kind IN ('cash_transfer','expense','adjustment')
  list(filters): Promise<...>   // ~888 — NO TOCAR (vista comercial por socio, INNER JOIN users/branches)
From el-templo-api/src/modules/finance/types.ts:
  MovEgresoItem { id, transactionDate, kind, direction, amount, currency, memberId, cashRegisterId, cashRegisterName, branchId, branchName, recordedBy, recorderName, voidedAt, voidReason, notes }   // ~430
  MovEgresoFilters { cashRegisterId?, country?, isOwner?, dateFrom?, dateTo?, page?, limit? }   // ~456
From el-templo-api/src/modules/finance/schemas.ts:
  movEgresos query schema  ~706-740  (cashRegisterId, country, dateFrom/dateTo, page/limit)
From el-templo-api/src/modules/finance/routes.ts:
  GET de movEgresos history (~1236-1306) — owner-aware country scope
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: listMovEgresos por cash_register_id con todos los kinds + validationStatus</name>
  <read_first>
    - el-templo-api/src/modules/finance/transaction-service.ts (listMovEgresos ~1213-1365; OJO la nota de Pitfall 2 country-scope; list() ~888 para NO replicar su criterio)
    - el-templo-api/src/modules/finance/types.ts (MovEgresoItem/MovEgresoFilters ~430-465)
    - el-templo-api/test/finance/mov-egresos-history.test.ts (patrón de test existente a extender)
  </read_first>
  <behavior>
    - listMovEgresos({ cashRegisterId: X }) → devuelve filas de TODOS los kinds cuyo cash_register_id = X (plan_charge, debt_settlement, advance_payment, refund, expense, cash_transfer, adjustment)
    - cada fila incluye validationStatus
    - filas pendientes y validadas aparecen ambas
    - sin cashRegisterId (owner, vista global) → sigue funcionando devolviendo el trail filtrable; el country-scope para non-owner se mantiene (sub-select de cajas por país, sin la trampa del LEFT JOIN)
    - las filas anuladas (voidedAt) siguen apareciendo etiquetadas como hoy
  </behavior>
  <action>
    Quitar la condición `inArray(kind, ['cash_transfer','expense','adjustment'])` de
    `listMovEgresos` — el arqueo es por caja, no por kind. El filtro primario pasa a
    ser `cash_register_id` (ya soportado por `filters.cashRegisterId`). Agregar
    `validationStatus` al `.select()` y a `MovEgresoItem` (types.ts). Conservar el
    country-scope owner-aware vía el sub-select de cajas por país (la nota de Pitfall 2)
    sin cambios. Mantener el orden desc(transactionDate). NO replicar el INNER JOIN de
    `list()` (los LEFT JOIN actuales son los correctos para que sobrevivan filas sin
    socio/branch). NO modificar `list()` ni su criterio (ARQUEO-04). Si el schema/route
    de movEgresos necesita exponer el nuevo campo en la respuesta tipada, ajustarlos en
    la task 2.
  </action>
  <acceptance_criteria>
    - Test: una caja con un plan_charge + un expense + un cash_transfer → listMovEgresos({cashRegisterId}) devuelve las 3 filas (antes el plan_charge se perdía).
    - Test: cada fila trae validationStatus; una pendiente y una validada aparecen ambas.
    - Test ARQUEO-04: list() (vista por socio) sigue devolviendo el mismo set/criterio que antes (sin nuevos kinds colados ni filas sin socio).
    - tsc verde.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm test test/finance/mov-egresos-history.test.ts test/finance/transactions-api.test.ts</automated>
  </verify>
  <done>listMovEgresos es el arqueo por caja: todos los kinds por cash_register_id, con estado, pendientes y validados.</done>
</task>

<task type="auto">
  <name>Task 2: Exponer validationStatus en schema + route de movEgresos</name>
  <read_first>
    - el-templo-api/src/modules/finance/schemas.ts (movEgresos query schema ~706-740)
    - el-templo-api/src/modules/finance/routes.ts (route de movEgresos history ~1236-1306)
    - el-templo-api/src/modules/finance/types.ts (MovEgresoItem actualizado en task 1)
  </read_first>
  <action>
    Asegurar que la route de movEgresos history devuelve `validationStatus` por fila
    (el service shape ya lo incluye tras la task 1; confirmar que el mapeo de respuesta
    no lo descarta). Si la query schema o la respuesta están tipadas explícitamente,
    incluir el campo. No cambiar los filtros de entrada (cashRegisterId, country,
    dateFrom/dateTo, page/limit) salvo documentación. Mantener el scope owner-aware.
  </action>
  <acceptance_criteria>
    - La respuesta del endpoint de movEgresos incluye validationStatus por fila.
    - tsc verde; tests de mov-egresos siguen pasando.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm test test/finance/mov-egresos-history.test.ts</automated>
  </verify>
  <done>El endpoint expone validationStatus para que la UI (plan 06) marque el estado.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                 |
| ------------------------------ | ------------------------------------------- |
| admin/gestion → GET movEgresos | filtro por caja + country scope owner-aware |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                     | Disposition | Mitigation Plan                                                                                                         |
| --------- | ---------------------- | ------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------- |
| T-146-14  | Information Disclosure | non-owner viendo cajas de otro país / branch-less             | mitigate    | Se conserva el country-scope por sub-select de cajas por país (Pitfall 2); central/banco branch-less siguen owner-only. |
| T-146-15  | Integrity              | que el cambio de criterio contamine la vista comercial list() | mitigate    | list() NO se toca (ARQUEO-04); test que verifica su criterio inalterado.                                                |

</threat_model>

<verification>
- listMovEgresos por caja devuelve todos los kinds + validationStatus.
- list() intacto (ARQUEO-04) verificado por test.
- Suite finance verde; tsc verde.
</verification>

<success_criteria>

- ARQUEO-01: "Movimientos de caja" trae todo lo imputado a una caja por cash_register_id.
- ARQUEO-02: pendientes y validados, cada fila con su estado.
- ARQUEO-04: la pestaña Transacciones (list()) se mantiene sin cambios de criterio.
  </success_criteria>

<output>
Create `.planning/phases/146-caja-validaci-n-e-imputaci-n-fundacional/146-05-SUMMARY.md` when done
</output>
