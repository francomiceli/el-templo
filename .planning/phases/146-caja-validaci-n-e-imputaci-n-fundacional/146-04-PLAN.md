---
phase: 146-caja-validaci-n-e-imputaci-n-fundacional
plan: 04
type: execute
wave: 2
depends_on: [146-02]
files_modified:
  - el-templo-admin/src/components/caja/BandejaPendientesTab.vue
  - el-templo-admin/src/composables/useTransactionsApi.ts
autonomous: true
requirements: [CAJA-02, CAJA-03, COBRO-05]
must_haves:
  truths:
    - "Al validar un pendiente desde la bandeja, gestión ve un selector de caja (con la sugerida pre-seleccionada) y puede confirmarla o cambiarla, incluyendo elegir la cuenta banco (Galicia / Mercado Pago) para transferencias"
    - "La caja elegida se envía al endpoint de validación como cashRegisterId"
    - "El botón Validar queda bloqueado/deshabilitado para los cobros con miscReason='sin_plan'; en su lugar se muestra el chip 'Sin plan — asignar' que lleva a la ficha"
  artifacts:
    - path: "el-templo-admin/src/components/caja/BandejaPendientesTab.vue"
      provides: "selector de caja al validar + bloqueo del Validar para sin_plan"
    - path: "el-templo-admin/src/composables/useTransactionsApi.ts"
      provides: "validateTransaction(id, cashRegisterId?)"
  key_links:
    - from: "BandejaPendientesTab onValidar"
      to: "transactionsApi.validateTransaction(id, cashRegisterId)"
      via: "selector de caja del dialog de validación"
      pattern: "validateTransaction\\("
---

<objective>
CAJA-02 + CAJA-03 (frontend) + COBRO-05 (frontend): en la bandeja de pendientes,
gestión confirma o cambia la **caja imputada** al validar (incluida la elección de
cuenta banco entre múltiples), y el botón **Validar** queda **bloqueado** para los
cobros marcados "Sin plan activo" (que se redirigen al chip "Sin plan — asignar").

Consume el endpoint de validación abierto en el plan 02 (acepta `cashRegisterId`) y
la lista de cajas activas ya disponible en el composable.

Purpose: cerrar el flujo de imputación en la validación desde la UI; impedir que un
"sin plan" se valide como plata suelta.
Output: dialog de validación con selector de caja + guard visual de sin_plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@BRIEF-FEEDBACK-V52-CAJA.md
@.planning/phases/146-caja-validaci-n-e-imputaci-n-fundacional/146-02-PLAN.md

<interfaces>
From el-templo-admin/src/composables/useTransactionsApi.ts:
  validateTransaction(id)  // hoy sin cashRegisterId → extender a (id, cashRegisterId?)
  getCashRegisterBalances(params) → CajaSaldoRow[]   // id, name, type ('efectivo'|'banco'), currency — fuente de las opciones de caja
From el-templo-admin/src/components/caja/BandejaPendientesTab.vue:
  PendingTrayItem { id, memberName, amount, currency, paymentMethod, cashRegisterId, cashRegisterName, validationStatus, miscReason }
  onValidar(row)  ~462  // hoy: dialog confirm → validateTransaction(row.id)
  chip "Sin plan — asignar" para miscReason==='sin_plan' ~77-89  (COBRO-02, ya existe)
Backend (plan 02): POST /transactions/:id/validate body { cashRegisterId? }  — rechaza la validación si miscReason='sin_plan'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: validateTransaction(id, cashRegisterId?) en el composable</name>
  <read_first>
    - el-templo-admin/src/composables/useTransactionsApi.ts (validateTransaction ~93-111; getCashRegisterBalances ~186)
    - el-templo-api/src/modules/finance/routes.ts (POST /transactions/:id/validate — body cashRegisterId, plan 02)
  </read_first>
  <action>
    Extender `validateTransaction(id: number, cashRegisterId?: number)` para enviar
    `{ cashRegisterId }` en el body del POST cuando está definido (omitir la clave si
    es undefined, para mantener retrocompat). Tipar el retorno como hoy. No cambiar
    otras firmas del composable.
  </action>
  <acceptance_criteria>
    - validateTransaction acepta un segundo arg opcional y lo manda en el body.
    - `cd el-templo-admin && pnpm build` verde.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-admin && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>El composable puede enviar la caja elegida al validar.</done>
</task>

<task type="auto">
  <name>Task 2: Selector de caja al validar + bloqueo del Validar para sin_plan</name>
  <read_first>
    - el-templo-admin/src/components/caja/BandejaPendientesTab.vue (onValidar ~462; botón Validar ~157-170; chip sin_plan ~76-89; columna estado ~392)
    - el-templo-admin/src/composables/useTransactionsApi.ts (getCashRegisterBalances para poblar opciones)
  </read_first>
  <action>
    Reemplazar el confirm simple de `onValidar` por un dialog que muestre un
    **selector de caja** (q-select) poblado con las cajas activas de la moneda de la
    transacción (filtrar por `currency` de la fila; efectivo + banco), pre-seleccionando
    la caja sugerida (`row.cashRegisterId` / `row.cashRegisterName`). Para transferencias,
    el selector lista las cuentas banco (Galicia / Mercado Pago) de esa moneda, de modo
    que gestión elija la cuenta real. Al confirmar, llamar `validateTransaction(row.id,
    cajaElegida)` y refrescar. Si el endpoint devuelve 400 (ej. moneda incoherente o
    sin_plan), mostrar el mensaje del backend con $q.notify negative.
    Bloqueo COBRO-05: para filas con `miscReason==='sin_plan'`, deshabilitar el botón
    "Validar" (q-btn :disable + tooltip "Asigná un plan para imputar este cobro") y
    mantener visible el chip "Sin plan — asignar" que ya navega a la ficha. El botón
    no debe abrir el dialog de validación para esos cobros.
  </action>
  <acceptance_criteria>
    - Al validar un pendiente normal, aparece el selector de caja con la sugerida pre-seleccionada; confirmar envía cashRegisterId.
    - Para una transferencia, el selector ofrece las cuentas banco de la moneda (Galicia/Mercado Pago).
    - Filas miscReason='sin_plan': botón Validar deshabilitado (no abre dialog) + chip "Sin plan — asignar" visible.
    - `cd el-templo-admin && pnpm build` verde.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-admin && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>Gestión confirma/cambia la caja (incl. cuenta banco) al validar; el Validar de los "sin plan" queda bloqueado.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                                        |
| --------------------------- | ------------------------------------------------------------------ |
| UI bandeja → POST /validate | la UI envía cashRegisterId; el backend valida coherencia (plan 02) |

## STRIDE Threat Register

| Threat ID | Category  | Component                                | Disposition | Mitigation Plan                                                                                                  |
| --------- | --------- | ---------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------- |
| T-146-12  | Tampering | caja de moneda incorrecta elegida en UI  | mitigate    | El selector filtra por la moneda de la tx; el backend (plan 02) rechaza igual con 400 (defensa en profundidad).  |
| T-146-13  | Integrity | intentar validar un sin_plan desde la UI | mitigate    | Botón Validar deshabilitado para miscReason='sin_plan'; si se forzara, el backend (COBRO-05 guard) devuelve 400. |

</threat_model>

<verification>
- Selector de caja al validar con sugerida pre-seleccionada; cuentas banco elegibles.
- Validar bloqueado para sin_plan; chip de asignar visible.
- Build admin verde.
</verification>

<success_criteria>

- CAJA-02/CAJA-03 (FE): gestión confirma/cambia la caja y elige cuenta banco al validar.
- COBRO-05 (FE): no se puede disparar la validación manual de un cobro sin plan.
  </success_criteria>

<output>
Create `.planning/phases/146-caja-validaci-n-e-imputaci-n-fundacional/146-04-SUMMARY.md` when done
</output>
