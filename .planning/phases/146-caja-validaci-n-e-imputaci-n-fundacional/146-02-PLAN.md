---
phase: 146-caja-validaci-n-e-imputaci-n-fundacional
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/migrations/0160_seed_banco_cuentas.sql
  - el-templo-api/src/modules/finance/transaction-service.ts
  - el-templo-api/src/modules/finance/schemas.ts
  - el-templo-api/src/modules/finance/routes.ts
  - el-templo-api/src/modules/finance/types.ts
  - el-templo-api/test/finance/validate-caja.test.ts
autonomous: true
requirements: [CAJA-02, CAJA-03, COBRO-05]
must_haves:
  truths:
    - "POST /transactions/:id/validate acepta un cashRegisterId opcional en el body y lo persiste en la fila al validar"
    - "Si el body trae cashRegisterId, la caja debe existir, estar activa y tener la misma moneda que la transacción; si no, devuelve 400"
    - "Sin cashRegisterId en el body, validate conserva la caja sugerida actual (retrocompatible)"
    - "Validar una transacción con miscReason='sin_plan' devuelve 400 (no se valida a mano; se redirige a asignar plan)"
    - "Existen múltiples cajas banco ARS (Galicia y Mercado Pago) seedeadas; gestión puede imputar la transferencia a cualquiera de ellas al validar"
    - "Existe TransactionService.voidInTx(tx, ...) tx-aware y un listado de cobros sueltos pendientes por socio, consumibles por el plan 03"
  artifacts:
    - path: "el-templo-api/src/db/migrations/0160_seed_banco_cuentas.sql"
      provides: "seed de cajas banco Galicia + Mercado Pago (ARS)"
      contains: "INSERT INTO cash_registers"
    - path: "el-templo-api/src/modules/finance/transaction-service.ts"
      provides: "validate(cashRegisterId) + guard sin_plan + voidInTx + listPendingMiscForMember"
    - path: "el-templo-api/test/finance/validate-caja.test.ts"
      provides: "cobertura de validate con caja, guards de coherencia, bloqueo sin_plan"
  key_links:
    - from: "routes.ts POST /transactions/:id/validate"
      to: "transactionService.validate(id, adminId, cashRegisterId)"
      via: "validateTransactionSchema.body.cashRegisterId"
      pattern: "validate\\("
---

<objective>
CAJA-02 + CAJA-03 + COBRO-05 (backend): abrir el endpoint de validación (hoy
inmutable, body vacío) para que gestión **confirme o cambie** la caja imputada al
validar, eligiendo entre **múltiples cuentas banco** (Galicia / Mercado Pago,
seedeadas por migración). Bloquear la validación manual de los cobros "sin plan".

Además, este plan agrega los **primitivos backend** que el plan 03 (imputación del
anticipo) necesita: un `voidInTx` tx-aware (anular dentro de la transacción del
caller) y un `listPendingMiscForMember` (cobros sueltos pendientes del socio para
el AssignPlanDialog), expuesto vía endpoint.

Purpose: la caja se imputa/confirma en la validación, no en el cobro; los "sin
plan" no quedan como plata suelta validada.
Output: validate abierto con guards de coherencia + bloqueo sin_plan + seed banco

- primitivos para el plan 03.
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@BRIEF-FEEDBACK-V52-CAJA.md

<interfaces>
From el-templo-api/src/modules/finance/transaction-service.ts:
  validate(id: number, adminId: number): Promise<TransactionDetail>   // hoy: solo pendiente→validado, body vacío
  private _void(tx, id, voidedBy, input, statusOverride?)             // primitivo tx-aware existente
  // financialTransactions columns relevantes: cashRegisterId (nullable), currency, validationStatus, miscReason, voidedAt
From el-templo-api/src/modules/finance/schemas.ts:
  validateTransactionSchema  // hoy params {id}, sin body
From el-templo-api/src/db/schema/cash-registers.ts:
  cashRegisters: { id, name, type: 'efectivo'|'banco', branchId, currency, openingBalance, cutoffDate, isActive }
From el-templo-api/src/modules/finance/routes.ts:
  POST /transactions/:id/validate  // RBAC: FINANCE_VOID_ROLES (owner/admin/gestion)
</interfaces>

<migration_notes>

- Última migración aplicada: 0159 (misc_reason, fase 145). Próxima: 0160.
- El runner splittea por `;` ANTES de strippear `--` → NUNCA usar `;` dentro de comentarios SQL.
- cutoff_date es NOT NULL: copiar el valor global existente de una caja banco ya seedeada (INSERT ... SELECT) o de cash_registers.
- Las migraciones viajan a prod en el merge → Galicia y Mercado Pago aparecerán también en prod (son cuentas reales; aceptable).
  </migration_notes>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Migración 0160 — seed cajas banco Galicia + Mercado Pago (ARS)</name>
  <read_first>
    - el-templo-api/src/db/migrations/0154_cash_registers.sql (patrón del seed banco existente; nombres/columnas)
    - el-templo-api/src/db/schema/cash-registers.ts (columnas exactas + defaults)
    - el-templo-api/test/setup.ts (~63-95: cómo el test seedea banco ARS/EUR)
  </read_first>
  <files>el-templo-api/src/db/migrations/0160_seed_banco_cuentas.sql</files>
  <action>
    [BLOCKING — debe aplicarse y verificarse antes de las tasks 2/3 y de cualquier
    verificación downstream del milestone.] Crear la migración
    `0160_seed_banco_cuentas.sql` que inserta dos cajas tipo `banco`, currency ARS,
    branch_id NULL, is_active=1, opening_balance=0, con nombres "Galicia" y
    "Mercado Pago". cutoff_date copiado del valor global existente vía subquery
    `(SELECT MIN(cutoff_date) FROM cash_registers)` (la columna es NOT NULL).
    Cada INSERT idempotente con guard `WHERE NOT EXISTS (SELECT 1 FROM cash_registers
    WHERE name = 'Galicia' ...)` por nombre. NUNCA usar `;` dentro de comentarios SQL
    (el runner splittea por `;` antes de strippear `--`). Aplicar con `pnpm db:migrate`
    (NUNCA `drizzle-kit migrate`). Commitear el archivo SQL junto al cambio. Nota: las
    migraciones viajan a prod en el merge → Galicia y Mercado Pago aparecerán también
    en prod (son cuentas reales; aceptable). La verificación de existencia de las 2
    cajas banco vive en el test de la task 2 (validate-caja.test.ts), que las usa para
    imputar una transferencia.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm db:migrate && pnpm db:migrate</automated>
  </verify>
  <done>Existen 2 cajas banco ARS (Galicia, Mercado Pago) activas; la migración 0160 aplica sin error, es idempotente (segunda corrida no duplica) y está commiteada.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: validate() acepta y persiste cashRegisterId con guards de coherencia + bloqueo sin_plan</name>
  <read_first>
    - el-templo-api/src/modules/finance/transaction-service.ts (validate ~546-598; create resolver de caja ~255-270)
    - el-templo-api/src/modules/finance/schemas.ts (validateTransactionSchema ~133-146)
    - el-templo-api/src/modules/finance/routes.ts (route validate ~407-437; FINANCE_VOID_ROLES)
    - el-templo-api/test/finance/validation-state.test.ts (patrón de test de validate a copiar)
  </read_first>
  <behavior>
    - validate(id, adminId, cashRegisterId=banco Galicia) sobre una transferencia pendiente → fila queda validado con cash_register_id = Galicia
    - validate(id, adminId) sin cashRegisterId → conserva el cash_register_id sugerido actual y pasa a validado
    - validate con cashRegisterId de una caja inexistente / inactiva → 400
    - validate con cashRegisterId de una caja de OTRA moneda que la tx → 400 (mismo guard que el resolver)
    - validate de una fila con miscReason='sin_plan' → 400 ("asigná un plan para imputar este cobro")
    - validate de una fila ya validada o anulada → 400 (sin cambios al comportamiento existente)
  </behavior>
  <action>
    Extender `validate(id, adminId, cashRegisterId?)`: dentro de la misma
    db.transaction, antes de setear validado, (a) si `existing.miscReason === 'sin_plan'`
    lanzar BadRequestError("Los cobros sin plan se imputan al asignar un plan, no se
    validan a mano") — COBRO-05 guard de integridad; (b) si llega `cashRegisterId`,
    leer la caja (id, currency, isActive); si no existe o `!isActive` → BadRequestError;
    si `caja.currency !== existing.currency` → BadRequestError (espejo del guard del
    resolver, D-09); luego incluir `cashRegisterId` en el `.set({ validationStatus:
    'validado', cashRegisterId })`. Sin `cashRegisterId` no tocar la columna. Incluir
    el cashRegisterId final (o el conservado) en el payload del auditLog
    `transaction_validated`. Actualizar la firma del método y el único call site en
    routes.ts. Abrir `validateTransactionSchema.body` para aceptar
    `{ cashRegisterId?: integer ≥ 1 }` (additionalProperties false). En la route,
    leer `request.body?.cashRegisterId` y pasarlo a validate; mantener el guard RBAC
    FINANCE_VOID_ROLES sin cambios.
  </action>
  <acceptance_criteria>
    - Test: validar transferencia pendiente con cashRegisterId=Galicia → fila validado con esa caja.
    - Test: validar sin cashRegisterId → conserva la caja sugerida y pasa a validado.
    - Test: cashRegisterId inactiva/inexistente → 400; cashRegisterId de moneda distinta → 400.
    - Test: validar fila miscReason='sin_plan' → 400.
    - tsc verde; los tests de validate existentes (validation-state.test.ts) siguen pasando.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm test test/finance/validate-caja.test.ts test/finance/validation-state.test.ts</automated>
  </verify>
  <done>El endpoint de validación confirma/cambia la caja (incl. cuenta banco) con guards de coherencia y bloquea los "sin plan".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Primitivos para el plan 03 — voidInTx tx-aware + listPendingMiscForMember + endpoint</name>
  <read_first>
    - el-templo-api/src/modules/finance/transaction-service.ts (_void privado ~426; void/voidPair públicos; getFinancialHistory para patrón de read)
    - el-templo-api/src/modules/finance/types.ts (MovEgresoItem como patrón de shape de fila)
    - el-templo-api/src/modules/finance/routes.ts (patrón de route GET member-scoped)
  </read_first>
  <behavior>
    - voidInTx(tx, id, voidedBy, {reason}) anula la fila dentro de la transacción del caller (no abre db.transaction propia) reutilizando _void
    - listPendingMiscForMember(memberId) devuelve los advance_payment pendientes, no anulados, del socio (id, amount, currency, paymentMethod, cashRegisterId, miscReason, transactionDate, notes)
    - GET /transactions/pending-misc/:memberId devuelve esas filas (RBAC: roles que asignan plan)
  </behavior>
  <action>
    Agregar método público `voidInTx(tx: TxHandle, id, voidedBy, input)` que delega
    en `_void(tx, id, voidedBy, input)` — espejo de cómo `correct()` lo usa, pero
    expuesto para el caller externo (subscriptions assignPlan). Agregar
    `listPendingMiscForMember(memberId)` que selecciona financial_transactions con
    `kind='advance_payment'`, `validationStatus='pendiente'`, `voidedAt IS NULL`,
    `memberId = ?`, ordenado por transactionDate asc; devolver el shape de fila listado
    arriba (definir interfaz `PendingMiscItem` en types.ts). Agregar route
    `GET /transactions/pending-misc/:memberId` que devuelve `{ items }`, gateada por
    los roles que pueden asignar plan (reusar el set RBAC de las rutas de
    subscriptions/finance ya existente; si no hay uno claro, FINANCE_VOID_ROLES).
    NO modificar assignPlan acá (lo hace el plan 03).
  </action>
  <acceptance_criteria>
    - Test: listPendingMiscForMember devuelve solo advance_payment pendientes no anulados del socio.
    - Test: voidInTx dentro de una db.transaction del caller anula la fila y revierte el balance (sin abrir transacción propia).
    - Test del endpoint GET /transactions/pending-misc/:memberId → 200 con las filas; rol sin permiso → 403.
    - tsc verde.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm test test/finance/validate-caja.test.ts</automated>
  </verify>
  <done>Existen voidInTx y listPendingMiscForMember + endpoint, listos para que el plan 03 impute el anticipo.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                               |
| ---------------------------------------- | --------------------------------------------------------- |
| admin/gestion (cliente) → POST /validate | input no confiable: cashRegisterId elegido                |
| validate → cash_registers                | la caja elegida debe ser coherente (existe/activa/moneda) |

## STRIDE Threat Register

| Threat ID | Category               | Component                            | Disposition | Mitigation Plan                                                                                                                       |
| --------- | ---------------------- | ------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| T-146-04  | Elevation of Privilege | POST /validate                       | mitigate    | RBAC FINANCE_VOID_ROLES sin cambios; coach/recepción excluidos.                                                                       |
| T-146-05  | Tampering              | cashRegisterId arbitrario al validar | mitigate    | Guard: caja debe existir, estar activa y compartir moneda con la tx; si no, 400. Evita imputar a una caja de otra moneda/inexistente. |
| T-146-06  | Integrity              | validar a mano un cobro sin plan     | mitigate    | Guard miscReason='sin_plan' → 400; esos cobros solo se imputan al asignar plan (COBRO-05).                                            |
| T-146-07  | Tampering              | pending-misc filtra por socio        | mitigate    | listPendingMiscForMember scopea por memberId del path; RBAC en la route; solo advance_payment pendientes no anulados.                 |

</threat_model>

<verification>
- Migración 0160 aplicada e idempotente; 2 cajas banco ARS (Galicia, Mercado Pago).
- validate acepta/persiste cashRegisterId con guards; bloquea sin_plan.
- voidInTx + listPendingMiscForMember + endpoint operativos.
- Suite finance verde; tsc verde.
</verification>

<success_criteria>

- CAJA-02/CAJA-03: gestión confirma/cambia la caja (incl. múltiples cuentas banco) al validar.
- COBRO-05 (backend): no se puede validar a mano un cobro sin plan.
- Primitivos listos para la imputación del anticipo (plan 03).
  </success_criteria>

<output>
Create `.planning/phases/146-caja-validaci-n-e-imputaci-n-fundacional/146-02-SUMMARY.md` when done
</output>
