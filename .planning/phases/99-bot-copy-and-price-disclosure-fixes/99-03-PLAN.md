---
phase: 99-bot-copy-and-price-disclosure-fixes
plan: 03
type: execute
wave: 3
depends_on: ["99-01", "99-02"]
files_modified:
  - el-templo-api/test/whatsapp/v5-3-3-phase-99-copy-and-price.integration.test.ts
  - el-templo-api/test/whatsapp/v5-3-3-phase-99-no-hardcoded-prices.test.ts
autonomous: true
requirements: [PRICE-04, COPY-01, COPY-02]
tags: [bot, tests, integration, vi-mock, eltemplo_test, regression]

must_haves:
  truths:
    - "Mica name fidelity: a vitest integration test asserts that across at least one full inbound→outbound cycle on a PB1 lead, the rendered system prompt contains the literal anti-misspell directive `Tu nombre es Mica` (source assertion — strongest signal since LLM output is non-deterministic)."
    - "Class-name rename: a source-text assertion test verifies that `el-templo-bot/src/ai/knowledge.ts` and `el-templo-bot/src/ai/system-prompt.ts` contain `clases de calistenia` AND do NOT contain `Sesion Grupal` / `Sesión Grupal` (case-insensitive)."
    - "Preservation: a source-text assertion test verifies that `movimiento grupal`, `sin salirte del grupo`, `sin salirse del grupo`, `framings de arranque grupal`, and `lenguaje de arranque grupal` still match byte-for-byte in their files."
    - "Price-insistence counter — 1st insistence: counter increments to 1, outbound (mocked AI provider) is invoked WITHOUT disclosureUnlocked=true in the rendered prompt, no plan prices in the rendered prompt addendum."
    - "Price-insistence counter — 2nd insistence: counter increments to 2, outbound still NO disclosure addendum."
    - "Price-insistence counter — 3rd insistence (critical path): counter increments to 3, rendered prompt CONTAINS the disclosure-unlocked addendum text, AND a separate code-path assertion verifies the handler computes disclosureUnlocked=true."
    - "Price-insistence counter — 3rd insistence outbound: with a deterministic mocked chat reply, sendTextMessage is invoked with content containing the seeded test plan price (e.g., 99999) verbatim AND the free-trial re-anchor phrase token (e.g., /gratis/i). Satisfies CONTEXT.md `<specifics>` 'assert outbound contains those values verbatim AND the free-trial re-anchor phrase' — the prompt-content test alone is necessary but not sufficient for this requirement."
    - "PB2 transition resets counter: a test simulates a stage advance from PB1 to PB2; the post-AI playbook-state Redis write shows priceInsistenceCount=0."
    - "Negative test: bot source has zero hardcoded plan-price patterns; pre-existing `$20,000` (single-class drop-in reference at system-prompt.ts:321) is ALLOWLISTED with an inline justification, all other 4+ digit dollar-amount patterns FAIL the test."
    - "All new tests respect the Phase 98 baseline: post-plan 99-03 the el-templo-api test suite reports `(N+M) passed / 1 failed / total` where the 1 failed remains the Phase-95-deferred BUG-03 (i) LIKE-search RED, N is the prior pass count, and M is the new tests added by this plan."
  artifacts:
    - path: "el-templo-api/test/whatsapp/v5-3-3-phase-99-copy-and-price.integration.test.ts"
      provides: "Mica name + class-name + preservation + 1st/2nd/3rd price-insistence + PB2 reset tests"
      contains: 'describe("Phase 99'
    - path: "el-templo-api/test/whatsapp/v5-3-3-phase-99-no-hardcoded-prices.test.ts"
      provides: "Source-text negative grep test against bot/src/{ai,playbooks,webhook}/**"
      contains: "no hardcoded prices"
  key_links:
    - from: "test/whatsapp/v5-3-3-phase-99-copy-and-price.integration.test.ts"
      to: "el-templo-bot/src/ai/provider"
      via: "vi.mock per Phase 98 SUMMARY pattern — canned reply `{content, toolCalls: []}` for non-tool turns; canned reply with check_membership toolCall for the disclosure turn"
      pattern: "vi.mock.*el-templo-bot/src/ai/provider"
    - from: "test/whatsapp/v5-3-3-phase-99-copy-and-price.integration.test.ts"
      to: "el-templo-bot/src/memory/playbook-state"
      via: "direct getPlaybookState read between turns to assert counter value"
      pattern: "getPlaybookState"
    - from: "test/whatsapp/v5-3-3-phase-99-no-hardcoded-prices.test.ts"
      to: "el-templo-bot/src filesystem walk"
      via: "fs.readFileSync + regex against `\\$\\s*[0-9]{4,}` with allowlist for system-prompt.ts:321 `$20,000`"
      pattern: "\\$.*[0-9]{4"
---

<objective>
Ship integration-test coverage in `el-templo-api/test/whatsapp/` for the Phase 99 copy + price fixes. Depends on plans 99-01 (copy edits landed) and 99-02 (counter + disclosure addendum wired). Tests are the only deliverable in this plan — no runtime code changes.

Purpose: per CONTEXT.md `<specifics>` Integration tests block and PRICE-04, lock the behavior in CI so future phases cannot regress these fixes without an explicit test deletion. Phase 98 baseline must be preserved: 1 expected RED (BUG-03 (i) LIKE-search) remains, all new tests pass.

Output: 2 new test files in `el-templo-api/test/whatsapp/` (the testing-discipline location per `el-templo-bot/CLAUDE.md`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/99-bot-copy-and-price-disclosure-fixes/99-CONTEXT.md
@.planning/phases/99-bot-copy-and-price-disclosure-fixes/99-01-PLAN.md
@.planning/phases/99-bot-copy-and-price-disclosure-fixes/99-02-PLAN.md
@.planning/phases/98-test-hygiene-98-a-b-c/98-01-SUMMARY.md
@el-templo-bot/CLAUDE.md
@CLAUDE.md

<interfaces>
<!-- Patterns the executor will reuse verbatim. No exploration needed. -->

From el-templo-api/test/whatsapp/webhook.test.ts :1-60 (the vi.mock + waitForHandler pattern established in Phase 98-C):

```typescript
// 1. Mock sendTextMessage to avoid real WhatsApp API
vi.mock(
  "../../../el-templo-bot/src/whatsapp/client",
  async (importOriginal) => {
    const original =
      await importOriginal<
        typeof import("../../../el-templo-bot/src/whatsapp/client")
      >();
    return {
      ...original,
      sendTextMessage: vi.fn().mockResolvedValue("wamid.sent.mock123"),
    };
  },
);

// 2. Mock createAiProvider to avoid OpenAI calls
vi.mock("../../../el-templo-bot/src/ai/provider", async (importOriginal) => {
  const original =
    await importOriginal<
      typeof import("../../../el-templo-bot/src/ai/provider")
    >();
  return {
    ...original,
    createAiProvider: vi.fn(() => ({
      chat: vi
        .fn()
        .mockResolvedValue({ content: "Hola, soy Mica.", toolCalls: [] }),
    })),
  };
});

// 3. waitForHandler() pattern from webhook.test.ts:181 — flushes microtasks
function waitForHandler(): Promise<void> {
  return new Promise((resolve) => setImmediate(resolve));
}
```

From el-templo-api/test/helpers.ts (helpers exported for whatsapp tests; futureDateISO etc.):

- `futureDateISO(daysFromToday: number): string` — exported by helpers.ts post-Phase-98.

From el-templo-bot/src/memory/playbook-state.ts (read playbook state between turns to assert counter):

```typescript
export async function getPlaybookState(
  phone: string,
): Promise<PlaybookSessionState | null>;
export async function deletePlaybookState(phone: string): Promise<void>;
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Integration test file — Mica name + class-name + preservation + 1st/2nd/3rd price-insistence + PB2 reset (PRICE-04 + COPY-01 + COPY-02)</name>
  <files>el-templo-api/test/whatsapp/v5-3-3-phase-99-copy-and-price.integration.test.ts</files>
  <read_first>
    - el-templo-api/test/whatsapp/webhook.test.ts (the canonical Phase-98-C vi.mock + waitForHandler scaffolding; reuse verbatim)
    - el-templo-api/test/whatsapp/v5-3-3-handler-concurrency.integration.test.ts (existing v5-3-3 integration-test naming convention + scaffolding shape)
    - el-templo-api/test/helpers.ts (auth/request utilities + futureDateISO helper)
    - el-templo-bot/src/playbooks/constants.ts (the PB1_PRICE_INSISTENCE_THRESHOLD + shouldDisclosePrices contract from plan 99-02)
    - el-templo-bot/src/memory/playbook-state.ts (getPlaybookState + deletePlaybookState API)
    - el-templo-bot/src/ai/system-prompt.ts (the new PB1_PRICE_DISCLOSURE_UNLOCKED_ADDENDUM string from plan 99-02 — assertions need its substring)
    - el-templo-bot/src/playbooks/definitions.ts (verify PB1.E4 REGLA FUERTE byte-equal + COPY-02 preservation strings still present)
  </read_first>
  <action>
    Create a single new test file `el-templo-api/test/whatsapp/v5-3-3-phase-99-copy-and-price.integration.test.ts` with the following describe-blocks (each describe groups related tests so a future Phase 99 follow-up can extend the file in place):

    File-header conventions:
    - Top-of-file JSDoc: "Phase 99 integration tests (v5.3.3): bot copy and price disclosure fixes. Covers COPY-01 Mica name reinforcement, COPY-02 class-name rename + preservation, PRICE-01/02/03 counter + disclosure-unlocked addendum + PB2 transition reset."
    - Mirror the exact vi.mock pattern from `webhook.test.ts:27-60` (Phase 98-C established). Mock BOTH `el-templo-bot/src/whatsapp/client` (sendTextMessage) AND `el-templo-bot/src/ai/provider` (createAiProvider). The mocked `chat` returns a canned reply per test as needed.
    - Define `waitForHandler()` locally in the test file (mirror webhook.test.ts:181) — it is NOT exported from helpers.ts (confirmed during planning).
    - DB connection + Fastify app boot in `beforeAll` per existing whatsapp/*.test.ts pattern.
    - Per-test phone numbers: use a unique synthetic phone per test (e.g., `+5491100099001`, `+5491100099002`, ...) so tests do not interfere via Redis playbook-state keys. In `beforeEach`, call `deletePlaybookState(phone)` to wipe state for the chosen phone.

    Describe blocks (each describe MUST end with explicit `await deletePlaybookState(phone)` in `afterEach` if Redis is available):

    **describe("Phase 99 COPY-01 — Mica name reinforcement"):**
    - `it("renders system prompt with the Tu nombre es Mica directive when PB1 is active")` — boot the app, set playbook state to PB1.E1A for a synthetic lead phone, send any inbound that triggers the handler, intercept the system prompt passed to the mocked `chat` (the vi.fn captures the messages argument), assert messages[0].content (the system prompt) contains the literal string `Tu nombre es Mica`. This is the strongest assertion because outbound text is non-deterministic; the source assertion catches the regression at the prompt-anchoring layer.
    - `it("system-prompt.ts contains the Nunca te llames Micla anti-misspell anchor")` — pure source-text assertion via `fs.readFileSync` against `el-templo-bot/src/ai/system-prompt.ts`. No HTTP round-trip needed; this is a regression lock.

    **describe("Phase 99 COPY-02 — Class-name rename + preservation"):**
    - `it("knowledge.ts and system-prompt.ts use clases de calistenia instead of Sesion Grupal")` — source-text assertions:
      - `expect(knowledgeContents).toMatch(/clases de calistenia/);`
      - `expect(knowledgeContents).not.toMatch(/sesi[oó]n grupal/i);`
      - `expect(systemPromptContents).toMatch(/clases de calistenia/);` (at least 2 occurrences expected; assert count >= 2)
      - `expect(systemPromptContents).not.toMatch(/sesi[oó]n grupal/i);`
    - `it("preserves communal-value strings in knowledge.ts")` — source-text byte-checks:
      - `expect(knowledgeContents).toContain("movimiento grupal");`
      - `expect(knowledgeContents).toContain("sin salirte del grupo");`
      - `expect(knowledgeContents).toContain("sin salirse del grupo");`
    - `it("preserves TEAM-CORR-06 deprecated-framing guards in definitions.ts")` — source-text byte-checks:
      - `expect(definitionsContents).toContain("framings de arranque grupal");`
      - `expect(definitionsContents).toContain("lenguaje de arranque grupal");`

    **describe("Phase 99 PRICE-01 — Counter increment in PB1"):**
    - `it("1st price-insistence increments counter to 1 and does NOT inject disclosure addendum")` — pre-seed playbook state to PB1.E4 (use `setPlaybookState`), send an inbound containing "es muy caro" (matches the priceObjection regex), `await waitForHandler()`, then:
      - Read `getPlaybookState(phone)`, assert `priceInsistenceCount === 1`.
      - Inspect the captured system prompt messages: `expect(messages[0].content).not.toContain("Desbloqueo de disclosure de precios");` (the addendum's signature substring from plan 99-02's `PB1_PRICE_DISCLOSURE_UNLOCKED_ADDENDUM`).
    - `it("2nd price-insistence increments counter to 2 and still does NOT inject disclosure addendum")` — pre-seed playbook state with `priceInsistenceCount: 1, activePlaybook: "PB1", currentStage: "PB1.E4"`, send another priceObjection-matching inbound, assert `priceInsistenceCount === 2` and the addendum is absent from the prompt.
    - `it("counter does NOT increment when priceObjection regex does NOT fire")` — pre-seed state with `priceInsistenceCount: 1`, send an inbound like "hola gracias" (no priceObjection tokens), assert `priceInsistenceCount === 1` (unchanged).
    - `it("counter does NOT increment when activePlaybook is not PB1")` — pre-seed state with `activePlaybook: "PB3"` (or PB2.E2 — any non-PB1) and `priceInsistenceCount: 1`, send a priceObjection-matching inbound, assert the counter is unchanged. This is the per-PB1-session scoping check.

    **describe("Phase 99 PRICE-02 — Disclosure addendum unlocks on 3rd price insistence"):**
    - `it("3rd price-insistence injects the disclosure addendum into the rendered prompt")` — pre-seed state with `priceInsistenceCount: 2, activePlaybook: "PB1", currentStage: "PB1.E4"`. Seed `subscription_plans` with clearly-marked test rows (e.g., `name='TEST_PLAN_FLEX', price_regular=99999`, marked with a comment in the test for traceability; do NOT use any real production price values). Send a third priceObjection-matching inbound. Assert:
      - `priceInsistenceCount === 3` in playbook state.
      - `messages[0].content` (system prompt) contains the disclosure addendum substring "Desbloqueo de disclosure de precios" AND contains the re-anchor instruction substring "prueba gratis".
    - `it("3rd price-insistence outbound contains the real DB plan price verbatim + free-trial re-anchor (deterministic mock)")` — same pre-seed as the previous test (`priceInsistenceCount: 2, PB1, PB1.E4` + seeded `subscription_plans` with `name='TEST_PLAN_FLEX', price_regular=99999`). Configure the mocked `createAiProvider().chat` to return a canned reply that simulates the LLM correctly following the disclosure addendum:
      ```typescript
      vi.mocked(chat).mockResolvedValueOnce({
        content: "Entiendo. Si te sirve, tenemos el plan TEST_PLAN_FLEX a $99999. Muchos arrancan por ahí... pero lo mejor es que lo pruebes gratis primero. ¿Te anoto?",
        toolCalls: [],
      });
      ```
      Send the third priceObjection-matching inbound, `await waitForHandler()`, then assert:
      - `vi.mocked(sendTextMessage).mock.calls[0]` (or the most recent call) was invoked with content matching `/\b99999\b/` (the seeded test price verbatim).
      - The same content matches `/gratis/i` (the re-anchor phrase token from the addendum's required closing).
      - `priceInsistenceCount === 3` in playbook state.
      This test EXISTS to satisfy CONTEXT.md `<specifics>`: "assert outbound contains those values verbatim AND the free-trial re-anchor phrase." The deterministic mock is required because real LLM output is non-deterministic — but the mock proves the addendum + handler wiring delivers the disclosed price + re-anchor to `sendTextMessage` end-to-end. Pair with the prompt-content test above (the prompt assertion proves the addendum was injected; this test proves the outbound path doesn't drop or mutate the price/re-anchor on the way from `chat` reply to `sendTextMessage`).
    - `it("PB1.E4 REGLA FUERTE in definitions.ts is byte-equal regardless of disclosure unlock")` — source-text assertion: `expect(definitionsContents).toMatch(/REGLA FUERTE:\*\* en esta etapa NO recomendás/);` and `expect(definitionsContents).toContain("El ÚNICO CTA válido es la clase de prueba GRATIS");`. This locks Sub-option A — the static text never changes.

    **describe("Phase 99 PRICE-01 — Counter resets on PB1→PB2 transition"):**
    - `it("counter is reset to 0 when handler advances stage from PB1 to PB2")` — pre-seed state with `activePlaybook: "PB1", currentStage: "PB1.E7", priceInsistenceCount: 3` (a PB1 stage whose `nextStageHints.onCompletion` advances out of PB1, or simulate via post-AI advancement triggered by a mock signal). Send an inbound that triggers stage-advancement, await handler, read playbook state, assert `priceInsistenceCount === 0` AND `activePlaybook !== "PB1"` (or the field is absent — both interpreted as "reset"). NOTE: the test must use a real stage-transition path; if no PB1.E7→PB2 native trigger exists in the playbook engine, document this in the test as a known-limitation and skip with `it.todo(...)` rather than constructing a synthetic transition that misrepresents the production behavior.

    Common constraints:
    - All test prices used in seeded `subscription_plans` rows must be CLEARLY-MARKED test values (e.g., `99999`, `88888`, `77777`) with an inline comment like `// TEST VALUE — not a real price; bot reads dynamically via check_membership`.
    - No `any` types per CLAUDE.md.
    - No console.log; use the existing test logger or simply rely on vitest's built-in test output.
    - Mirror the cleanup discipline of Phase 98-C: `afterEach` clears the playbook-state for the test phone; `afterAll` closes the DB pool.
    - Where the test needs to inspect the rendered system prompt, capture the mocked `chat` call's first argument via `vi.mocked(chat).mock.calls[0][0]` (or whatever shape `createAiProvider().chat` accepts) and assert against `messages[0].content`. If the assertion target shape requires more than a one-liner, factor it into a local `getRenderedSystemPrompt()` helper at the top of the file.

  </action>
  <verify>
    <automated>cd /Users/bores/el-templo &amp;&amp; test -f el-templo-api/test/whatsapp/v5-3-3-phase-99-copy-and-price.integration.test.ts &amp;&amp; cd el-templo-api &amp;&amp; pnpm test --run test/whatsapp/v5-3-3-phase-99-copy-and-price.integration.test.ts</automated>
    <automated>cd /Users/bores/el-templo &amp;&amp; grep -c '"Phase 99' el-templo-api/test/whatsapp/v5-3-3-phase-99-copy-and-price.integration.test.ts | grep -qE '^[5-9]|^[1-9][0-9]'</automated>
    <human-check>Read through the test file; each describe block has 2-5 focused `it()` blocks; assertion targets are SOURCE-TEXT (knowledge.ts / system-prompt.ts / definitions.ts contents read via fs.readFileSync) or playbook-state Redis reads or rendered-system-prompt captures from the mocked chat call — NOT brittle outbound text assertions.</human-check>
  </verify>
  <done>
    - New test file exists and runs cleanly under `pnpm test --run` (exit 0).
    - All describe blocks present with at least one `it()` per CONTEXT.md `<specifics>` mandatory test bullet.
    - No hardcoded REAL prices in any test fixture — only clearly-marked test values like 99999/88888/77777.
    - PB2 transition reset test handles the "real stage path missing" case via `it.todo` rather than a fake transition.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Negative test — no hardcoded plan prices in bot source (PRICE-04 hardcoded-prices guard)</name>
  <files>el-templo-api/test/whatsapp/v5-3-3-phase-99-no-hardcoded-prices.test.ts</files>
  <read_first>
    - el-templo-bot/src/ai/system-prompt.ts :321 (the pre-existing `$20,000` single-class drop-in reference — this is allowlisted because it is the single-class drop-in price reference, NOT a plan price; CONTEXT.md does not mandate its removal in this phase)
    - el-templo-api/test/whatsapp/v5-3-3-phase-99-copy-and-price.integration.test.ts (created in Task 1 — to confirm test-file naming convention)
    - .planning/phases/99-bot-copy-and-price-disclosure-fixes/99-CONTEXT.md `<specifics>` Negative test bullet
  </read_first>
  <action>
    Create `el-templo-api/test/whatsapp/v5-3-3-phase-99-no-hardcoded-prices.test.ts`. Contents:

    Top-of-file JSDoc: "Phase 99 PRICE-04 guard: bot source must contain ZERO hardcoded plan-price patterns. Prices come exclusively from `subscription_plans.price_regular` via the `check_membership` tool. The other developer owns the DB price values; the bot never hardcodes them."

    Single describe block: `describe("Phase 99 PRICE-04 — no hardcoded prices in bot source")`.

    Test 1: `it("contains no $NNNN+ dollar-amount patterns in bot copy/prompts/playbooks")`:
    - Walk these directories synchronously with Node's `fs`:
      - `el-templo-bot/src/ai/`
      - `el-templo-bot/src/playbooks/`
      - `el-templo-bot/src/webhook/`
    - For each `*.ts` file, read contents, scan for the regex `/\$\s*\d{4,}/g`.
    - Maintain an explicit ALLOWLIST array:
      ```typescript
      const ALLOWLIST: Array<{ file: string; substring: string; reason: string }> = [
        {
          file: "el-templo-bot/src/ai/system-prompt.ts",
          substring: "$20,000",
          reason: "Single-class drop-in price reference (NOT a plan price). Phase 99 deliberately does not remove this; future phase may revisit per business-pricing review.",
        },
      ];
      ```
    - For each match, check whether `(file, substring)` is in the allowlist. If yes, ignore. If no, fail the test with a message naming the file and the offending substring.
    - The test PASSES if and only if every `$NNNN+` match across the scanned directories is allowlisted.

    Test 2: `it("contains no hardcoded plan-name + price tuples in bot copy")`:
    - Same directory walk, but scan for patterns like `/plan[^.]*\$\s*\d+/i` (a plan-word followed by a dollar amount within the same sentence). Same allowlist mechanism.
    - This catches more subtle copy like "el plan Flex sale $30,000" that the broader $NNNN+ regex might miss if the digit count is exactly 4 or the dollar sign is detached.

    Test 3: `it("test fixtures in el-templo-api/test/whatsapp/ use clearly-marked test values")` — positive control:
    - Walk `el-templo-api/test/whatsapp/*.ts` only (test files, not src).
    - Scan for `/\bprice_regular\s*[:=]\s*\d+/g` (Drizzle / SQL seed patterns).
    - Assert that every found price value is in the set `[99999, 88888, 77777, 0, 1, 100]` (the clearly-marked test sentinels). Any other value FAILS with a message asking the test author to use a sentinel value.
    - This is a forward-discipline test — keeps future test authors from using real-looking prices in seed rows.

    Constraints:
    - No `any` types. No `console.log` (use vitest's built-in `expect(...).toFail(message)` patterns or throw with descriptive messages).
    - Use `path.join` / `fs.readdirSync` recursively (or write a tiny recursive walker; do NOT shell out to grep — Node-native only so the test runs cross-platform).
    - The ALLOWLIST is the ONLY place to add an exception. Adding to the allowlist requires a documented `reason` field (enforced by TypeScript on the array type).
    - Test execution time should be < 5 seconds (the directory tree is small; reading every `.ts` file once is cheap).

  </action>
  <verify>
    <automated>cd /Users/bores/el-templo &amp;&amp; test -f el-templo-api/test/whatsapp/v5-3-3-phase-99-no-hardcoded-prices.test.ts &amp;&amp; cd el-templo-api &amp;&amp; pnpm test --run test/whatsapp/v5-3-3-phase-99-no-hardcoded-prices.test.ts</automated>
    <automated>cd /Users/bores/el-templo &amp;&amp; grep -q "ALLOWLIST" el-templo-api/test/whatsapp/v5-3-3-phase-99-no-hardcoded-prices.test.ts &amp;&amp; grep -q "Single-class drop-in price reference" el-templo-api/test/whatsapp/v5-3-3-phase-99-no-hardcoded-prices.test.ts</automated>
  </verify>
  <done>
    - New test file exists and PASSES under `pnpm test --run`.
    - The pre-existing `$20,000` reference at system-prompt.ts:321 is ALLOWLISTED with a documented `reason`.
    - Any future hardcoded price added to bot/src/{ai,playbooks,webhook}/** will fail this test.
    - Test fixture positive-control catches any future test author seeding price values outside the sentinel set.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Phase 98 baseline preservation gate</name>
  <files></files>
  <read_first>
    - .planning/phases/98-test-hygiene-98-a-b-c/98-01-SUMMARY.md (the Phase 98 SC#1 wording: post-97.5 baseline is "519 passed / 1 failed / 520 total", single failure is BUG-03 (i) LIKE-search RED at `el-templo-bot/src/ai/tools.ts:455`)
  </read_first>
  <action>
    Run the full `el-templo-api` test suite and confirm that adding Phase 99 tests preserves the Phase 98 baseline structural invariant. This is a verification-only task — no file edits.

    Steps:
    1. `cd el-templo-api && pnpm test --run` (full suite, single run for snapshot).
    2. Parse the final pass/fail/total numbers.
    3. The expected post-Phase-99 result: `(520 + N) passed / 1 failed / (521 + N) total`, where N is the number of new `it()` blocks added across both new test files in this plan. The 1 failing test MUST be the Phase-95-deferred BUG-03 (i) LIKE-search RED at `test/whatsapp/v5-3-3-booking.integration.test.ts:130`.
    4. Capture the output in the SUMMARY artifact with these specific assertions:
       - The single failing test description contains `BUG-03 candidate (i)` AND `LIKE-search ambiguity`.
       - The pass count is strictly greater than 520 (proves Phase 99 tests are running, not skipped).
       - No previously-passing test transitions to fail.
    5. If any of the above checks fail (e.g., new test fails, baseline shifts in an unexpected direction, a Phase-98-green test goes red), STOP and surface the failure in the SUMMARY with a halt recommendation — do not paper over it.

    Known flake context (per Phase 98 SUMMARY "Post-Merge & Sign-off" section): `ai-tools-membership-drift.test.ts` (Phase-97.5-owned, NOT Phase 99 file) can flake; if it shows red, log it in the SUMMARY with `98-FINDING-01` reference and re-run once. Persistent failure is a Phase 97 blocker, not a Phase 99 blocker.

  </action>
  <verify>
    <automated>cd /Users/bores/el-templo/el-templo-api &amp;&amp; pnpm test --run 2>&amp;1 | tail -50</automated>
    <human-check>The final test output shows the new Phase 99 tests passing and the single failing test is the documented BUG-03 (i) LIKE-search RED. No previously-green test went red (modulo the documented `ai-tools-membership-drift.test.ts` flake from Phase 98).</human-check>
  </verify>
  <done>
    - Full `el-templo-api` test suite runs to completion.
    - Final pass count > 520 (proves new tests are running).
    - Single failure is BUG-03 (i) LIKE-search RED (Phase 95 owned, by design).
    - SUMMARY captures the pass/fail/total numbers verbatim from the test output.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                                                                                                           |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| test→eltemplo_test DB | New tests seed `subscription_plans` rows. The DB is shared with the API project's test runs; cleanup discipline matters.              |
| test→Redis            | New tests read/write `wa:playbook:<phone>` keys. Per-test unique phone numbers + `afterEach` cleanup prevent cross-test interference. |
| test fixtures→source  | The no-hardcoded-prices test scans real source files; an allowlist drift would silently weaken the gate.                              |

## STRIDE Threat Register (block_on: high)

| Threat ID | Category                                          | Component                                                                 | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                  |
| --------- | ------------------------------------------------- | ------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-99-11   | Tampering (test allowlist drift)                  | `ALLOWLIST` array in `v5-3-3-phase-99-no-hardcoded-prices.test.ts`        | mitigate    | Allowlist is typed (`Array<{ file; substring; reason }>`); every entry requires a documented `reason` string. Future additions require a code-review surface. The current allowlist contains exactly one entry (`$20,000` single-class drop-in reference) documented inline.                     |
| T-99-12   | DoS (test runtime)                                | recursive directory walk in `v5-3-3-phase-99-no-hardcoded-prices.test.ts` | accept      | Bot src tree is small (~30 files in scope); reading every `.ts` file once is < 100ms. No risk of test timeout.                                                                                                                                                                                   |
| T-99-13   | Information disclosure (real prices in seed data) | new test fixtures                                                         | mitigate    | Test 3 in the no-hardcoded-prices file is a positive control: seeded `price_regular` values must be in the sentinel set `[99999, 88888, 77777, 0, 1, 100]`. Catches a future test author using a real-looking price by mistake.                                                                  |
| T-99-14   | Repudiation (flaky test in CI)                    | counter-increment timing across vi.useFakeTimers / waitForHandler         | mitigate    | Use `waitForHandler()` (setImmediate flush) for handler-async completion, mirroring Phase 98-C's proven pattern. Do NOT use `vi.useFakeTimers` — Phase 95/94 flake history (STATE.md "v5.3.3 test-suite flake") attributes flakiness to fake-timer interaction with promise-resolution ordering. |
| T-99-SC   | Tampering                                         | npm/pnpm installs                                                         | accept      | No new packages added in this plan. No package-legitimacy audit required.                                                                                                                                                                                                                        |

</threat_model>

<verification>
- `cd el-templo-api && pnpm test --run test/whatsapp/v5-3-3-phase-99-copy-and-price.integration.test.ts` exits 0.
- `cd el-templo-api && pnpm test --run test/whatsapp/v5-3-3-phase-99-no-hardcoded-prices.test.ts` exits 0.
- Full `cd el-templo-api && pnpm test --run` reports pass count > 520 and exactly 1 failure (the documented BUG-03 (i) LIKE-search RED).
- `cd el-templo-bot && pnpm exec tsc --noEmit` exit 0 (no bot-side TS changes in this plan, but verify untouched).
- `cd el-templo-api && pnpm exec tsc --noEmit` exit 0.
- `git diff HEAD -- 'el-templo-bot/src/**' 'el-templo-api/src/**'` produces zero changes in this plan's commit range (test-only plan).
- The Phase 98 SC#5 HARD GUARD invariant holds: zero `src/**` modifications across this plan's commits.
</verification>

<success_criteria>

- PRICE-04 closed: integration-test coverage in `el-templo-api/test/whatsapp/` for the counter (1st/2nd/3rd insistence), PB2 transition reset, disclosure-unlocked addendum injection, and no-hardcoded-prices guard.
- COPY-01 + COPY-02 source-text regression locks in the same test file.
- Full el-templo-api test suite passes with the documented Phase-95 BUG-03 (i) RED preserved as the only failure.
- Zero src/\*\* modifications by this plan.
- Commits use `test(99-03):` prefix per project conventions; pre-commit hook (Prettier) auto-fixes formatting; on hook failure, create a new commit (do NOT amend).
  </success_criteria>

<output>
Create `.planning/phases/99-bot-copy-and-price-disclosure-fixes/99-03-SUMMARY.md` when done. In the SUMMARY, include the verbatim final-line output of `pnpm test --run` showing pass/fail/total counts and explicitly cite the BUG-03 (i) RED's test description as the single allowed failure.
</output>
