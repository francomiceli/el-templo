---
phase: 99-bot-copy-and-price-disclosure-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-bot/src/ai/system-prompt.ts
  - el-templo-bot/src/ai/knowledge.ts
  - el-templo-bot/test/fixtures/pb1-e1a-lead-rendered.snap.txt
  - el-templo-bot/test/v5-3-2-regression.test.ts
autonomous: true
requirements: [COPY-01, COPY-02]
tags: [bot, copy, prompt-anchoring, mica, calistenia]

must_haves:
  truths:
    - "system-prompt.ts contains an explicit Mica-name reinforcement rule that names the wrong variants (Micla, Mika, Mics, ...) so the model has an anti-misspell anchor."
    - "knowledge.ts and system-prompt.ts no longer contain the class-name string 'Sesion Grupal' / 'Sesión Grupal' anywhere — the user-facing class-name is consistently 'clases de calistenia'."
    - "Preservation strings ('movimiento grupal', 'sin salirte del grupo', 'framings de arranque grupal', 'lenguaje de arranque grupal') survive byte-for-byte after the edit; the rename does not gut communal/method copy."
    - "tsc clean on el-templo-bot post-edit; no broken imports, no orphaned references."
    - "KGATE-05 snapshot byte-budget is satisfied: either Tasks 1+2 fit within the existing POST_RLOK_04_BYTES margin (Task 3 NO-OP), OR Task 3 regenerates the post-RLOK-04 fixture with frozen date kwargs (todayISO='2026-06-10', todayDayName='miércoles') and bumps POST_RLOK_04_BYTES in v5-3-2-regression.test.ts:57 in the same commit. The ≥20% rendered-cap (`rendered length ≤ floor(BASELINE_CHARS * 0.8)`) and the ≥35% knowledge-block cap remain GREEN either way."
  artifacts:
    - path: "el-templo-bot/src/ai/system-prompt.ts"
      provides: "Mica name-anchoring rule + class-name rename at :275 and :327"
      contains: "Tu nombre es Mica"
    - path: "el-templo-bot/src/ai/knowledge.ts"
      provides: "Class-name rename at :548 + intact preservation strings at :446/:448/:450"
      contains: "clases de calistenia"
  key_links:
    - from: "system-prompt.ts identity section (:259/:334-337)"
      to: "the new name-anchoring rule"
      via: "rule co-located with existing identity directives so the model reads them together"
    - from: "knowledge.ts SECTIONS[1] 'Que es El Templo' (:534-558)"
      to: "level-vs-class rule in system-prompt.ts :327"
      via: "both use the renamed noun consistently"
---

<objective>
Ship the two copy-only fixes (COPY-01 Mica name reinforcement; COPY-02 class-name rename "Sesión Grupal" → "clases de calistenia") in a single atomic plan. Pure prompt/knowledge surface, no runtime logic, no Redis, no DB. Independent of the PRICE work in plan 99-02 — these touch different files and ship in Wave 1.

Purpose: close two of the three live-test findings (Mica self-introduces as "Micla"; class is named "Sesión Grupal" instead of the team-preferred "clases de calistenia") at the prompt-anchoring layer per CONTEXT.md decisions. Negative space (per CONTEXT.md): NO outbound sanitization regex for the name; NO blanket replace of "grupo/grupal" — only the class-NAME instances change.

Output: edits to `system-prompt.ts` (3 sites: insert name-anchor rule + rename at :275 + rename at :327) and `knowledge.ts` (1 site: rename at :548). One commit. Tests for these copy fixes are written in plan 99-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/99-bot-copy-and-price-disclosure-fixes/99-CONTEXT.md
@el-templo-bot/CLAUDE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Mica name-anchoring rule to system-prompt.ts identity section (COPY-01)</name>
  <files>el-templo-bot/src/ai/system-prompt.ts</files>
  <read_first>
    - el-templo-bot/src/ai/system-prompt.ts (full file — already in context; pay specific attention to the identity opening at :259 and the "Preguntas sobre mi identidad" block at :331-337)
    - el-templo-bot/src/ai/knowledge.ts :446-450 (preservation strings — confirm you can see them and will not edit them)
    - .planning/phases/99-bot-copy-and-price-disclosure-fixes/99-CONTEXT.md (Issue 1 + Issue 2 sections + scope_fence)
  </read_first>
  <action>
    Add a new name-anchoring directive to `el-templo-bot/src/ai/system-prompt.ts` inside the existing "Preguntas sobre mi identidad" block (starts at :331 with the heading `*Preguntas sobre mi identidad*`). The directive enforces COPY-01 by naming the wrong variants explicitly so the LLM has a negative-example anchor.

    Required content (place as a NEW bullet inside the identity block, after the existing bullet at :337 which ends with `"Soy Mica, del equipo de administración de El Templo 🙋‍♀️ ¿En qué te ayudo?"`):

      `- *Tu nombre es Mica* — escribilo siempre exactamente así, nunca lo deformes ni lo abrevies. Nunca te llames Micla, Mika, Mics ni ninguna otra variante. Si por error te referís a vos misma con otro nombre, corregilo en el siguiente turno.`

    Constraints:
    - Place the bullet inside the identity block so it co-locates with existing identity rules (do NOT scatter it across the file).
    - Preserve all existing identity bullets verbatim — this is an addition, not a rewrite.
    - Keep wording argentino (tuteo) consistent with surrounding bullets ("escribilo", "deformes", "abrevies", "corregilo").
    - Do NOT add any post-processing sanitization code. Per CONTEXT.md "Negative space" this fix is prompt-anchoring only.
    - Do NOT touch the Mica references at :259, :334, :335, :337 — those remain as-is. The new bullet is the ONLY change for COPY-01.
    - KGATE-05 budget: this addition is ~280 chars including newline + leading bullet. The post-Phase-96.5 margin is ~93 chars remaining (per STATE.md Phase 96.5 SC#4 entry; 96.5 took ~25 chars of the 118 char margin). This change MAY exceed the snapshot byte budget. Do NOT pre-emptively edit `POST_RLOK_04_BYTES` in this task — Task 3 in this plan owns the regen path. Run Task 2 first, then Task 3 measures + regens + bumps the constant in one atomic step.

  </action>
  <verify>
    <automated>cd /Users/bores/el-templo/el-templo-bot && grep -c "Tu nombre es Mica" src/ai/system-prompt.ts | grep -qE '^[1-9]' &amp;&amp; grep -qE "Nunca te llames Micla" src/ai/system-prompt.ts &amp;&amp; pnpm exec tsc --noEmit</automated>
    <human-check>The bullet renders inside the identity block; reading the full block top-to-bottom, the new directive is naturally integrated and does not break the flow of the existing bullets.</human-check>
  </verify>
  <done>
    - `el-templo-bot/src/ai/system-prompt.ts` contains the literal string `"Tu nombre es Mica"` exactly once (the new bullet).
    - `el-templo-bot/src/ai/system-prompt.ts` contains the literal string `"Nunca te llames Micla"`.
    - `cd el-templo-bot && pnpm exec tsc --noEmit` exits 0.
    - No outbound-sanitization code was introduced (per CONTEXT.md Negative space).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rename "Sesión Grupal" / "Sesion Grupal" → "clases de calistenia" at the 3 verified sites + preservation byte-check (COPY-02)</name>
  <files>el-templo-bot/src/ai/knowledge.ts, el-templo-bot/src/ai/system-prompt.ts</files>
  <read_first>
    - el-templo-bot/src/ai/knowledge.ts :430-558 (the SECTIONS[1] "Que es El Templo" body containing the :548 class-name line + the METHOD_DETAIL/ELEVATOR_TEXT preservation strings at :446-454)
    - el-templo-bot/src/ai/system-prompt.ts :275 (example phrasing "Las clases se llaman Sesión Grupal y duran 60 minutos...") and :327 (level-vs-class rule "Todas las clases son Sesion Grupal y los niveles indican progresion")
    - .planning/phases/99-bot-copy-and-price-disclosure-fixes/99-CONTEXT.md Issue 2 (the 3 hardcoded sites AND the 5 preservation sites; both lists are byte-verified there)
  </read_first>
  <preserve>
    The following strings MUST still appear byte-for-byte in their current files after this task:
    - `el-templo-bot/src/ai/knowledge.ts` line :446 region — `"sin salirte del grupo"` (inside ELEVATOR_TEXT)
    - `el-templo-bot/src/ai/knowledge.ts` line :448 region — `"sin salirse del grupo"` (inside METHOD_DETAIL — note this is the "salirse" verbatim variant, the third-person reflexive; preserve regardless of -te/-se inflection)
    - `el-templo-bot/src/ai/knowledge.ts` line :450 region — `"movimiento grupal"` (inside METHOD_DETAIL)
    - `el-templo-bot/src/playbooks/definitions.ts` :138 region — `"framings de arranque grupal"` (TEAM-CORR-06 deprecated-anti-pattern guard; NOT TOUCHED IN THIS PLAN; just verify after this plan that the file is unchanged)
    - `el-templo-bot/src/playbooks/definitions.ts` :147 region — `"lenguaje de arranque grupal"` (same guard; NOT TOUCHED IN THIS PLAN)
    These are method/communal-value strings or deprecated-pattern guards — independent of the class-NAME rename. Verify-grep after edit.
  </preserve>
  <action>
    Rename the class-NAME at the 3 verified hardcoded sites. Use the canonical replacement noun phrase **"clases de calistenia"** per CONTEXT.md Issue 2.

    Site 1 — `el-templo-bot/src/ai/knowledge.ts` SECTIONS[1] body at :548 (the canonical class-name definition):
      Current text (literal, no markdown bold preserved — the asterisks in `*Sesion Grupal*` are markdown for WhatsApp bold and must be removed since the new noun phrase reads naturally without bold-emphasis):
        `*Importante:* Todas las clases se llaman *Sesion Grupal*. Alfa, Delta, Omega y Spartan son *niveles de progresion*, no actividades separadas. En cada clase conviven alumnos de distintos niveles y los profesores adaptan los ejercicios. Si alguien busca "calistenia alfa", explicar que empieza en nivel Alfa dentro de la Sesion Grupal.`
      Rewrite to (CONTEXT.md Issue 2 suggested phrasing, naturalized):
        `*Importante:* Todas las clases son *clases de calistenia*. Alfa, Delta, Omega y Spartan son *niveles de progresion* dentro de las clases de calistenia, no actividades separadas. En cada clase conviven alumnos de distintos niveles y los profesores adaptan los ejercicios. Si alguien busca "calistenia alfa", explicar que empieza en nivel Alfa dentro de las clases de calistenia.`

    Site 2 — `el-templo-bot/src/ai/system-prompt.ts` :275 (the example-phrasing demonstration in the "Longitud de respuesta" bullet):
      Current text:
        `*Ejemplo CORRECTO — responder "qué son las clases":* "Las clases se llaman Sesión Grupal y duran 60 minutos. Son multinivel, así que vas a estar con gente de distintos niveles y los profes te adaptan todo. Se entrena descalzo, todo con tu propio cuerpo."`
      Rewrite to:
        `*Ejemplo CORRECTO — responder "qué son las clases":* "Las clases se llaman clases de calistenia y duran 60 minutos. Son multinivel, así que vas a estar con gente de distintos niveles y los profes te adaptan todo. Se entrena descalzo, todo con tu propio cuerpo."`

    Site 3 — `el-templo-bot/src/ai/system-prompt.ts` :327 (level-vs-class rule in "Reglas de conversacion"):
      Current text:
        `Si alguien menciona "Alfa", "Delta", "Omega" o "Spartan" como si fuera una clase o actividad: aclarar que son *niveles*, no clases separadas. Todas las clases son *Sesion Grupal* y los niveles indican progresion.`
      Rewrite to:
        `Si alguien menciona "Alfa", "Delta", "Omega" o "Spartan" como si fuera una clase o actividad: aclarar que son *niveles*, no clases separadas. Todas las clases son *clases de calistenia* y los niveles indican progresion.`

    Constraints:
    - Do NOT touch `el-templo-bot/src/playbooks/definitions.ts` — the preservation strings at :138 and :147 are out of scope for this task (and that file is owned by plan 99-02). Verify post-edit that definitions.ts is unchanged by this task.
    - Do NOT rename any internal code identifier referencing "sesion" / "grupal" / "sp" — CONTEXT.md Terminology section is explicit that internal references (`sp`, `sesion_de_prueba`) stay as-is.
    - Do NOT introduce a hyphen, accent, or capitalization variant in the new noun phrase. The exact literal is the lowercase ASCII phrase `clases de calistenia`.
    - Do NOT split the rename into multiple commits. All 3 sites change in this single task's commit.

  </action>
  <verify>
    <automated>cd /Users/bores/el-templo &amp;&amp; grep -rniE "sesi[oó]n grupal" el-templo-bot/src | grep -v '^[^:]*:[[:space:]]*//' &amp;&amp; echo "FAIL: sesión grupal still present in non-comment src" &amp;&amp; exit 1 || true; grep -c "clases de calistenia" el-templo-bot/src/ai/knowledge.ts | grep -qE '^[1-9]' &amp;&amp; grep -c "clases de calistenia" el-templo-bot/src/ai/system-prompt.ts | grep -qE '^[2-9]' &amp;&amp; grep -q "movimiento grupal" el-templo-bot/src/ai/knowledge.ts &amp;&amp; grep -q "sin salirte del grupo" el-templo-bot/src/ai/knowledge.ts &amp;&amp; grep -q "sin salirse del grupo" el-templo-bot/src/ai/knowledge.ts &amp;&amp; grep -q "framings de arranque grupal" el-templo-bot/src/playbooks/definitions.ts &amp;&amp; grep -q "lenguaje de arranque grupal" el-templo-bot/src/playbooks/definitions.ts &amp;&amp; cd el-templo-bot &amp;&amp; pnpm exec tsc --noEmit</automated>
    <automated>cd /Users/bores/el-templo &amp;&amp; git diff --stat HEAD el-templo-bot/src/playbooks/definitions.ts | grep -qE "definitions\.ts" &amp;&amp; echo "FAIL: this task must not modify definitions.ts" &amp;&amp; exit 1 || true</automated>
    <human-check>Re-read the renamed sentence at knowledge.ts :548 region — it should read naturally in Spanish, not as a mechanical find/replace. Same check at system-prompt.ts :275 and :327.</human-check>
  </verify>
  <done>
    - `grep -rniE "sesi[oó]n grupal" el-templo-bot/src` returns 0 hits in non-comment source lines.
    - `el-templo-bot/src/ai/knowledge.ts` contains `"clases de calistenia"` at least once and still contains `"movimiento grupal"`, `"sin salirte del grupo"`, and `"sin salirse del grupo"` byte-for-byte.
    - `el-templo-bot/src/ai/system-prompt.ts` contains `"clases de calistenia"` at least twice.
    - `el-templo-bot/src/playbooks/definitions.ts` is UNCHANGED by this task and still contains `"framings de arranque grupal"` and `"lenguaje de arranque grupal"`.
    - `cd el-templo-bot && pnpm exec tsc --noEmit` exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Regenerate post-RLOK-04 snapshot + bump POST_RLOK_04_BYTES if KGATE-05 fires (atomic regen)</name>
  <files>el-templo-bot/test/fixtures/pb1-e1a-lead-rendered.snap.txt, el-templo-bot/test/v5-3-2-regression.test.ts</files>
  <read_first>
    - el-templo-bot/test/v5-3-2-regression.test.ts :30-72 (the `POST_RLOK_04_BYTES = 18884` constant at :57, the SNAP_PATH, the `renderE1ALead()` helper that uses FROZEN date kwargs `todayISO: '2026-06-10', todayDayName: 'miércoles'` per Phase 96.5 D-03 — regen MUST use these same frozen kwargs)
    - el-templo-bot/test/ai/rendered-prompt-snapshot.test.ts (the second consumer of the same snap fixture — regen must satisfy both consumers)
    - el-templo-bot/src/webhook/handler.ts :109 (the documented "regen on its own schedule" note — confirms regen is an expected/sanctioned operation, not a hack)
    - el-templo-bot/test/system-prompt-playbook.test.ts :313-319 (third consumer reading the same fixture)
    - .planning/phases/96.5-date-grounding-fix/* (the Phase 96.5 SUMMARY documents the established regen method — frozen date kwargs + snapshot rewrite + byte-count bump in one commit)
  </read_first>
  <action>
    Step-driven, fail-fast. The order matters: regen is a NO-OP if KGATE-05 still passes after Tasks 1 + 2; only run the regen if the byte test fails.

    Step 1 — Measure current state:
    - `cd el-templo-bot && pnpm test --run test/v5-3-2-regression.test.ts 2>&1 | tail -40`
    - Identify whether the failing assertions include either:
      (a) `RLOK-02: snapshot byte-equal lock` — the rendered prompt diverged from the on-disk snapshot.
      (b) `snapshot byte-count equals POST_RLOK_04_BYTES` — the JS-string length no longer matches the hardcoded constant.
    - If BOTH passed (no KGATE-05 violation): the Mica anchor + class-name rename fit within the existing margin. Task 3 is a NO-OP — record this in the SUMMARY and exit cleanly.
    - If either failed: proceed to Step 2.

    Step 2 — Compute the new JS-string length (the value that `POST_RLOK_04_BYTES` asserts against):
    - Use the established Phase 96.5 regen method. Write the new fixture by invoking `getSystemPrompt({ clientState: 'lead', activePlaybook: 'PB1', currentStage: 'E1A', todayISO: '2026-06-10', todayDayName: 'miércoles' })` from a tiny one-off node script run via `pnpm exec tsx`. The frozen date kwargs are MANDATORY per the inline comment at v5-3-2-regression.test.ts:59-64 — Production callers omit these kwargs; the fixture is regen-stress-baked with them so RLOK-02 stays deterministic.
    - Concrete command shape (single line, no script file required — use `tsx -e`):
      `cd el-templo-bot && pnpm exec tsx -e "import { getSystemPrompt } from './src/ai/system-prompt.ts'; const out = getSystemPrompt({ clientState: 'lead', activePlaybook: 'PB1', currentStage: 'E1A', todayISO: '2026-06-10', todayDayName: 'miércoles' }); process.stdout.write(out);" > test/fixtures/pb1-e1a-lead-rendered.snap.txt`
    - Then read the new JS-string length: `pnpm exec tsx -e "import { readFileSync } from 'node:fs'; console.log(readFileSync('test/fixtures/pb1-e1a-lead-rendered.snap.txt', 'utf8').length);"` — call the value `NEW_BYTES`.

    Step 3 — Bump POST_RLOK_04_BYTES in v5-3-2-regression.test.ts:
    - At `el-templo-bot/test/v5-3-2-regression.test.ts:57`, change `const POST_RLOK_04_BYTES = 18884;` to `const POST_RLOK_04_BYTES = NEW_BYTES;` (literal value, not a variable).
    - Append a new paragraph to the JSDoc block at :35-56 documenting this regen. Required content (one paragraph; mirror the existing 92-02 and Phase 96.5 entries in tone and structure):

      `Strengthened to NEW_BYTES during Phase 99 (COPY-01 + COPY-02): added Mica name-anchoring directive to system-prompt.ts identity block (~280 JS-chars) and renamed "Sesion Grupal" → "clases de calistenia" at knowledge.ts:548, system-prompt.ts:275, system-prompt.ts:327 (net +/-N JS-chars from the rename, depending on Spanish letter density). Frozen date kwargs unchanged. Regen via pnpm exec tsx + getSystemPrompt with todayISO='2026-06-10', todayDayName='miércoles' per Phase 96.5 D-03.`

    Step 4 — Verify upper-bound cap:
    - Read the rendered-cap assertion at v5-3-2-regression.test.ts:321 (`rendered PB1.E1A lead prompt length ≤ floor(BASELINE_CHARS * 0.8)` — the ≥20% cap). Compute `floor(BASELINE_CHARS * 0.8)` and confirm NEW_BYTES does not exceed it. If NEW_BYTES > that cap, STOP — this is a CONTEXT.md scope violation (the Mica anchor + rename collectively blow past the ≥20% rendered-cap guarantee). Roll back the snap regen and surface as a HALT in the SUMMARY — the wording in the new bullet needs trimming before this phase can ship.
    - Also re-check `system-prompt-playbook.test.ts:313-319` snapshot consumer and `rendered-prompt-snapshot.test.ts` still parse the new fixture correctly (run `pnpm test --run test/system-prompt-playbook.test.ts test/ai/rendered-prompt-snapshot.test.ts` and confirm both pass).

    Step 5 — Confirm all 4 snap-touching tests now pass:
    - `cd el-templo-bot && pnpm test --run test/v5-3-2-regression.test.ts test/v5-3-3-date-grounding.test.ts test/system-prompt-playbook.test.ts test/ai/rendered-prompt-snapshot.test.ts 2>&1 | tail -30` — expect green across all four files.

    Constraints (HARD GUARDS):
    - Do NOT regen unless Step 1 confirms KGATE-05 fired. A no-op regen pollutes history.
    - Do NOT introduce a new fixture file — overwrite the existing `pb1-e1a-lead-rendered.snap.txt` in place.
    - Do NOT change `BASELINE_CHARS` (at `el-templo-bot/test/fixtures/pb1-e1a-baseline.js`) — that's the v5.3.2 frozen baseline used as the ≥20% / ≥35% cap reference. It is intentionally immutable across Phase 99.
    - Do NOT modify the rendered-cap percentages at :321 or :326 — only the post-RLOK-04 hardcoded byte-count moves.
    - Do NOT delete or comment out the JSDoc history paragraphs at :35-56; this task appends a new paragraph documenting the Phase 99 regen.
    - This task touches ONLY `test/fixtures/pb1-e1a-lead-rendered.snap.txt` and `test/v5-3-2-regression.test.ts`. Verify the diff for this task's commit includes ONLY those two files.

  </action>
  <verify>
    <automated>cd /Users/bores/el-templo/el-templo-bot &amp;&amp; pnpm test --run test/v5-3-2-regression.test.ts test/v5-3-3-date-grounding.test.ts test/system-prompt-playbook.test.ts test/ai/rendered-prompt-snapshot.test.ts 2>&amp;1 | tail -30</automated>
    <automated>cd /Users/bores/el-templo &amp;&amp; git diff HEAD --stat el-templo-bot/test/ | awk '$NF == "fixtures/pb1-e1a-lead-rendered.snap.txt" || $NF == "v5-3-2-regression.test.ts" || /^$/ { next } { print "FAIL: unexpected test file modified: " $0; exit 1 }'</automated>
    <human-check>Reading the new POST_RLOK_04_BYTES value alongside the appended JSDoc paragraph, the regen is documented as a Phase 99 entry (not just a number bump). The new fixture rendered prompt contains the Mica anchor bullet AND uses "clases de calistenia" (sanity-check the fixture diff against Tasks 1+2).</human-check>
  </verify>
  <done>
    - If KGATE-05 did NOT fire after Tasks 1+2: Task 3 is a documented NO-OP in the SUMMARY ("byte budget held with X chars to spare; no regen required").
    - If KGATE-05 fired: `POST_RLOK_04_BYTES` is bumped to NEW_BYTES with a new JSDoc paragraph documenting the Phase 99 regen reason and date-kwarg discipline; `pb1-e1a-lead-rendered.snap.txt` regenerated via `pnpm exec tsx` with frozen date kwargs; all 4 snap-consuming test files pass.
    - The ≥20% rendered-cap (`rendered length ≤ floor(BASELINE_CHARS * 0.8)`) and the ≥35% knowledge-block cap remain GREEN.
    - Diff for this task's commit touches ONLY `test/fixtures/pb1-e1a-lead-rendered.snap.txt` and `test/v5-3-2-regression.test.ts`.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                                                                    |
| -------- | ------------------------------------------------------------------------------------------------------------------------------ |
| user→LLM | The model receives the rendered system prompt + business knowledge on every turn. A copy bug here ships to every conversation. |
| LLM→user | Outbound text. Mica name fidelity and class-name consistency are observable here.                                              |

## STRIDE Threat Register (block_on: high)

| Threat ID | Category               | Component                                                       | Disposition | Mitigation Plan                                                                                                                                                                                                       |
| --------- | ---------------------- | --------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-99-01   | Tampering              | system-prompt.ts identity bullet                                | mitigate    | The bullet names variants explicitly so the model has a negative-example anchor; outbound sanitization is intentionally deferred (CONTEXT.md Negative space) until the prompt-anchor proves insufficient in live use. |
| T-99-02   | Information disclosure | knowledge.ts SECTIONS[1] body                                   | accept      | Class-name rename is pure team-canonical-terminology; no PII, no business-sensitive data exposed. The rename leaks no information not already in the file.                                                            |
| T-99-03   | Tampering (regression) | knowledge.ts ELEVATOR_TEXT / METHOD_DETAIL preservation strings | mitigate    | Preservation byte-check in `<verify>` asserts `"movimiento grupal"`, `"sin salirte del grupo"`, and `"sin salirse del grupo"` still present after edit. Catches accidental blanket find/replace.                      |
| T-99-04   | Tampering (regression) | definitions.ts deprecated-framing guards (:138/:147)            | mitigate    | The Task 2 verify gate asserts definitions.ts is UNCHANGED by this task AND that both TEAM-CORR-06 anti-pattern strings still match byte-for-byte. Catches accidental scope creep into the price-plan's file.         |
| T-99-SC   | Tampering              | npm/pnpm installs                                               | accept      | No new packages added in this plan. No package-legitimacy audit required.                                                                                                                                             |

</threat_model>

<verification>
- `cd el-templo-bot && pnpm exec tsc --noEmit` exit 0.
- `grep -rniE "sesi[oó]n grupal" el-templo-bot/src` returns 0 hits in non-comment source lines.
- `grep -q "Tu nombre es Mica" el-templo-bot/src/ai/system-prompt.ts` exits 0.
- `grep -q "Nunca te llames Micla" el-templo-bot/src/ai/system-prompt.ts` exits 0.
- All 5 preservation strings (3 in knowledge.ts, 2 in definitions.ts) match byte-for-byte post-edit.
- Plan touches exactly 2 src files (`system-prompt.ts`, `knowledge.ts`) plus optionally 2 test files (`pb1-e1a-lead-rendered.snap.txt`, `v5-3-2-regression.test.ts`) IF Task 3's KGATE-05 regen fires. Diff confirms `definitions.ts`, `handler.ts`, `tools.ts`, `playbook-state.ts` UNCHANGED in this plan's commit range. The ≥20% rendered-cap and ≥35% knowledge-block cap stay GREEN.
- `cd el-templo-bot && pnpm test --run test/v5-3-2-regression.test.ts test/v5-3-3-date-grounding.test.ts test/system-prompt-playbook.test.ts test/ai/rendered-prompt-snapshot.test.ts` exits 0 — all 4 snapshot-consuming tests pass.
- No hardcoded prices introduced (`grep -rE '\$\s*[0-9]{4,}' el-templo-bot/src/ai/system-prompt.ts el-templo-bot/src/ai/knowledge.ts` returns 0 hits — pre-existing `$20,000` reference at system-prompt.ts:321 is in scope of plan 99-02's verification, not this one).
</verification>

<success_criteria>

- COPY-01 closed: Mica name-anchoring rule lives inside the identity block in `system-prompt.ts` with explicit negative examples (Micla, Mika, Mics).
- COPY-02 closed: 3 verified class-name hardcodes replaced with "clases de calistenia"; 5 preservation strings intact.
- Single commit with subject prefix `feat(99-01):` or `chore(99-01):` per `el-templo-bot/CLAUDE.md` and project commit conventions; pre-commit hook (Prettier) auto-fixes formatting; if pre-commit fails, create a new commit (do NOT amend).
- tsc clean on el-templo-bot.
- Test coverage for these changes is owned by plan 99-03 (PRICE-04 + COPY tests); this plan ships the copy fixes only.
  </success_criteria>

<output>
Create `.planning/phases/99-bot-copy-and-price-disclosure-fixes/99-01-SUMMARY.md` when done, following `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
