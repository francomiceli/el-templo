---
phase: 99-bot-copy-and-price-disclosure-fixes
plan: 02
type: execute
wave: 2
depends_on: ["99-01"]
files_modified:
  - el-templo-bot/src/playbooks/constants.ts
  - el-templo-bot/src/playbooks/types.ts
  - el-templo-bot/src/webhook/handler.ts
  - el-templo-bot/src/ai/system-prompt.ts
  - el-templo-bot/src/playbooks/definitions.ts
  - el-templo-bot/src/ai/tools.ts
  - el-templo-bot/.env.example
autonomous: true
requirements: [PRICE-01, PRICE-02, PRICE-03]
tags: [bot, price-disclosure, playbook-state, redis, counter, prompt-injection]

must_haves:
  truths:
    - "A price-insistence counter persists in the existing playbook-state Redis hash (key `wa:playbook:<phone>`) and increments at most once per inbound message when the existing handler.ts:1440 priceObjection regex fires AND the active playbook is PB1."
    - "The counter resets to 0 (or absent) on any transition out of PB1 — observed at the post-AI stage-advance write site at handler.ts:989-1003 where nextStage moves the conversation to a non-PB1 stage."
    - "A single tunable constant PB1_PRICE_INSISTENCE_THRESHOLD = 2 lives in a dedicated file (el-templo-bot/src/playbooks/constants.ts) with a doc-comment explaining what it gates. Env override PB1_PRICE_INSISTENCE_THRESHOLD is read once at module load."
    - "When (counter >= threshold) AND (activePlaybook === PB1) AND (currentStage is one of the PB1 stages where price questions appear: PB1.E1A/E1B/E2A/E2B/E3/E4), getSystemPrompt appends a disclosure-unlocked addendum to the rendered prompt that permits listing real DB plan prices via check_membership AND requires closing with a free-trial re-anchor phrase."
    - "The PB1.E4 REGLA FUERTE at definitions.ts:74 is UNCHANGED in static text — the unlock is purely additive in system-prompt.ts (Sub-option A locked, per CONTEXT.md PRICE-02; Sub-option B explicitly rejected because the codebase has no template syntax for playbook prompts)."
    - "The check_membership tool returns DB plan prices for a lead with no users-row. VERIFIED-BROKEN in planning: tools.ts:485-487 short-circuits with 'No encontré una cuenta...' before the subs/plans query runs, so the available-plans branch at :509-525 is unreachable for leads. Locked fix (Piece D): extract the formatting logic into a `formatAvailablePlans()` helper inside tools.ts and APPEND its output to the preserved 'no account' message for the lead branch (and refactor the existing no-active-subscription branch to use the same helper). HARD GUARD: bot-side only — NEVER touch el-templo-api/src/**."
    - "The PB2.E2 [plan_básico]/[precio] placeholder situation at definitions.ts:138 is verified (read the prompt-assembly path); if the placeholders are literal text passed to the LLM (no substitution layer exists), the script is rewritten to instruct the LLM to call check_membership first and read values from the tool result — NOT a template engine introduction."
    - "The bot source contains ZERO hardcoded price amounts (negative grep test). Pre-existing $20,000 reference at system-prompt.ts:321 stays because it refers to the single-class drop-in price (not a plan price) — but it is acknowledged in verification and not added to."
  artifacts:
    - path: "el-templo-bot/src/playbooks/constants.ts"
      provides: "PB1_PRICE_INSISTENCE_THRESHOLD constant + threshold accessor"
      contains: "PB1_PRICE_INSISTENCE_THRESHOLD"
    - path: "el-templo-bot/src/playbooks/types.ts"
      provides: "priceInsistenceCount field on PlaybookSessionState (optional, backward-compat per Phase 90/91 precedent)"
      contains: "priceInsistenceCount"
    - path: "el-templo-bot/src/webhook/handler.ts"
      provides: "counter-increment site near priceObjection detection + reset on PB1→non-PB1 transition + disclosure-unlocked flag passed to getSystemPrompt"
      contains: "priceInsistenceCount"
    - path: "el-templo-bot/src/ai/system-prompt.ts"
      provides: "disclosureUnlocked option + conditional addendum injection"
      contains: "disclosureUnlocked"
    - path: "el-templo-bot/.env.example"
      provides: "PB1_PRICE_INSISTENCE_THRESHOLD env var documented with default 2"
      contains: "PB1_PRICE_INSISTENCE_THRESHOLD"
    - path: "el-templo-bot/src/ai/tools.ts"
      provides: "formatAvailablePlans() helper + restructured users.length === 0 branch that appends the plan listing to the preserved 'No encontré una cuenta' prefix; refactored subs.length === 0 branch reuses the same helper"
      contains: "formatAvailablePlans"
  key_links:
    - from: "handler.ts priceObjection regex at :1440"
      to: "counter increment site (also in handler.ts, same function or sibling helper)"
      via: "reuse the existing regex variable — no parallel regex"
      pattern: "priceObjection"
    - from: "handler.ts post-AI setPlaybookState at :989-1003 and :1018-1028"
      to: "playbook-state.ts Redis write"
      via: "spread the new priceInsistenceCount field into the existing PlaybookSessionState writes; reset to 0 when nextStage leaves PB1"
      pattern: "priceInsistenceCount"
    - from: "handler.ts getSystemPrompt call at :617-630"
      to: "system-prompt.ts disclosure-unlocked addendum injection"
      via: "new option disclosureUnlocked: boolean computed pre-AI from (count >= threshold && activePlaybook === PB1)"
      pattern: "disclosureUnlocked"
---

<objective>
Implement the price-insistence counter (PRICE-01), threshold-based disclosure unlock (PRICE-02), and verify-then-fix the check_membership available-plans branch + PB2.E2 placeholder resolution (PRICE-03). Bot-only — no el-templo-api/src/** changes, no hardcoded prices, no new playbook stages.

Purpose: per CONTEXT.md Issue 3 (Option B is locked), hold prices during the first 2 price-insistences in PB1, then on the 3rd request disclose real DB plan prices via the existing check_membership available-plans path while still re-anchoring the free trial. The mechanism is wired bot-side; price values continue to come from `subscription_plans.price_regular` exclusively.

Output: 5 source file edits + .env.example + a new tiny constants file (file count 6 + 1 = 7 files, but the counter wiring is mechanical and the file count reflects the spread of responsibilities; each file has a focused single-purpose edit). Tests for this behavior are owned by plan 99-03.

Note on PRICE-03: the placeholder-resolution work is **verify-first, fix-if-broken**. The action below describes investigation gates and the conditional fix path; do NOT pre-decide which branch applies — read the prompt-assembly code, write a one-shot test or grep, then apply the right fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/99-bot-copy-and-price-disclosure-fixes/99-CONTEXT.md
@.planning/phases/98-test-hygiene-98-a-b-c/98-01-SUMMARY.md
@el-templo-bot/CLAUDE.md
@CLAUDE.md

<interfaces>
<!-- Pre-extracted contracts the executor will work against. No codebase exploration needed for these. -->

From el-templo-bot/src/playbooks/types.ts (current PlaybookSessionState — must extend, NOT replace):

```typescript
export interface PlaybookSessionState {
  activePlaybook: PlaybookId | null;
  currentStage: StageId | null;
  avatar?: AvatarProfile | null;
  discoveryTurnCount?: number;
  whyAsked?: boolean;
  updatedAt: number;
  // NEW (this plan): priceInsistenceCount?: number;
  //                  Phase 99 PRICE-01. Optional + backward-compatible per
  //                  Phase 90/91 precedent — absent on pre-99 entries, treated as 0.
}
```

From el-templo-bot/src/memory/playbook-state.ts (Redis layer — DOES NOT NEED MODIFICATION since the whole shape is JSON-serialized):

```typescript
export const PLAYBOOK_STATE_TTL = 21_600; // 6h, matches SESSION_TTL
export async function getPlaybookState(
  phone: string,
): Promise<PlaybookSessionState | null>;
export async function setPlaybookState(
  phone: string,
  state: PlaybookSessionState,
): Promise<void>;
export async function deletePlaybookState(phone: string): Promise<void>;
```

From el-templo-bot/src/webhook/handler.ts (existing priceObjection consumer — DO NOT DUPLICATE THE REGEX):

```typescript
// Line 1439-1442 inside computePreAiSignals (or similar — the function that returns priceObjection + softRejection + ...):
const priceObjection =
  /\b(caro|carisimo|car[ií]simo|precio|no me alcanza|no puedo pagar|muy caro|barato|descuento)\b/i.test(
    inbound,
  );
// Return object at :1473 includes priceObjection.
```

From el-templo-bot/src/ai/system-prompt.ts (existing SystemPromptOptions — extend):

```typescript
export interface SystemPromptOptions {
  clientState?: ClientState;
  profileContext?: string;
  activePlaybook?: PlaybookId | null;
  currentStage?: StageId | null;
  currentAvatar?: AvatarProfile | null;
  softRejectionRule?: "why" | "backoff";
  todayISO?: string;
  todayDayName?: string;
  // NEW (this plan): disclosureUnlocked?: boolean;
}
```

From el-templo-bot/src/ai/tools.ts (the check_membership available-plans branch — verify, do not modify unless broken):

```typescript
// Line 506-530 inside check_membership:
if (subs.length === 0) {
  const planResult = await db.execute<PlanRow[]>(
    sql`SELECT name, price_regular, duration_days, classes_per_week
        FROM subscription_plans
        WHERE is_active = true AND is_archived = false AND is_trial = false
        ORDER BY price_regular ASC`,
  );
  const plans = planResult[0] as unknown as PlanRow[];
  let response = `${userName} no tiene una suscripción activa.`;
  if (plans.length > 0) {
    response += "\n\nPlanes disponibles:\n";
    for (const plan of plans) {
      // ... renders DB price_regular ...
    }
  }
  return response;
}
```

The branch runs when subs.length === 0. This is the path the disclosure-unlocked prompt addendum will instruct the model to call.

From el-templo-bot/src/playbooks/definitions.ts (PB2.E2 placeholder line — note these are inside a string literal that is concatenated into a system-prompt section by `system-prompt.ts:404-405`):

```typescript
// PB2 stages[2] (PB2.E2) promptSection contains:
// "Objeción precio ('me gustó pero es caro', 'está caro', 'no me alcanza'):
//   Script: 'Entiendo. ¿Sabés qué? Si te sirve, tenemos el plan [plan_básico] a [precio]. ...'"
// → The [plan_básico] and [precio] tokens are LITERAL TEXT in the string.
// → There is NO template-substitution layer between definitions.ts and the LLM call.
// → Therefore: either the LLM substitutes from check_membership output (acceptable)
//   or it fabricates literal "[plan_básico]" text (broken). Verify before fixing.
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create constants file + extend PlaybookSessionState type with priceInsistenceCount + extend SystemPromptOptions with disclosureUnlocked (PRICE-01 + PRICE-02 contracts)</name>
  <files>el-templo-bot/src/playbooks/constants.ts, el-templo-bot/src/playbooks/types.ts, el-templo-bot/src/ai/system-prompt.ts, el-templo-bot/.env.example</files>
  <read_first>
    - el-templo-bot/src/playbooks/types.ts (current PlaybookSessionState — see the `<interfaces>` block above)
    - el-templo-bot/src/ai/system-prompt.ts :1-50 (SystemPromptOptions interface decl — locate it via `grep -n "SystemPromptOptions" el-templo-bot/src/ai/system-prompt.ts`)
    - el-templo-bot/.env.example (verify the env-var declaration style; mirror existing pattern)
    - .planning/phases/99-bot-copy-and-price-disclosure-fixes/99-CONTEXT.md `<specifics>` block on "single tunable constant"
  </read_first>
  <action>
    Create the new constants file and extend two type declarations. No runtime behavior changes in this task — only contracts. This is the "interface-first" task that downstream tasks consume.

    File 1 — NEW: `el-templo-bot/src/playbooks/constants.ts`. Contents (no fenced code shown; description of identifiers):
    - Module-level comment block explaining: this file holds tunable thresholds for PB1 behavior; price-insistence disclosure unlock per CONTEXT.md Phase 99 D-decisions (PRICE-02); env override `PB1_PRICE_INSISTENCE_THRESHOLD` parsed once at module load; default 2 means "disclose on the 3rd price request" (count strictly greater than threshold; or count >= 3 with threshold=2 — pick one comparison and document it).
    - Exported constant `PB1_PRICE_INSISTENCE_THRESHOLD: number` resolved from `Number(process.env.PB1_PRICE_INSISTENCE_THRESHOLD)` if finite-positive-integer, else `2`.
    - Use Pino-style guards for invalid env values (if NaN or negative, fall through to default; do NOT throw at module load — log a warn via the `pino` logger pattern used in playbook-state.ts).
    - Export a helper `shouldDisclosePrices(count: number | undefined): boolean` that returns `(count ?? 0) >= PB1_PRICE_INSISTENCE_THRESHOLD + 1` (3rd insistence = unlock when count was just incremented to 3). Document the off-by-one rationale inline. The executor MUST pick a consistent comparison and document it; the rest of this plan and 99-03 will use this helper as the single source of truth.

    File 2 — EDIT: `el-templo-bot/src/playbooks/types.ts`. Add the optional field to `PlaybookSessionState`:
    - Add `priceInsistenceCount?: number;` after `whyAsked?: boolean;` (line ~108).
    - Add a JSDoc block matching the style of `whyAsked` and `discoveryTurnCount`: "v5.3.3 Phase 99 (PRICE-01): per-conversation per-PB1-session price-insistence counter. Increments at most once per inbound when the priceObjection regex at handler.ts:1440 matches AND activePlaybook === PB1. Resets to 0 on transition out of PB1 (handler.ts post-AI stage-advance write site). Optional + backward-compatible: absent on pre-99 entries, treated as 0."

    File 3 — EDIT: `el-templo-bot/src/ai/system-prompt.ts`. Add the optional option to `SystemPromptOptions` interface:
    - Add `disclosureUnlocked?: boolean;` to the interface declaration (locate via `grep -n "interface SystemPromptOptions" el-templo-bot/src/ai/system-prompt.ts`).
    - JSDoc: "v5.3.3 Phase 99 (PRICE-02): when true, the rendered prompt includes a disclosure-unlocked addendum that permits listing real DB plan prices via check_membership while still re-anchoring the free trial. Computed by the handler from `shouldDisclosePrices(priceInsistenceCount)` from `playbooks/constants.ts`. Sub-option A locked: definitions.ts PB1.E4 promptSection stays static; the unlock is purely additive in system-prompt.ts (no template syntax introduced)."
    - Do NOT add the consumer logic in this task; that lives in Task 3.

    File 4 — EDIT: `el-templo-bot/.env.example`. Add a new section or append a line documenting the env var:
    - `# v5.3.3 Phase 99 (PRICE-02): threshold for PB1 price disclosure. Default 2 means disclose on the 3rd price-objection-style inbound. Override via env var. Bot-side only; DB plan prices are owned by subscription_plans.price_regular.`
    - `PB1_PRICE_INSISTENCE_THRESHOLD=2`
    - Mirror the surrounding env-var documentation style (one-line comment + assignment).

    Constraints:
    - Do NOT introduce any hardcoded price amounts. The constants file is for the COUNT threshold only.
    - No `any` types per CLAUDE.md.
    - Use `createLogger()` / pino — never console.log.
    - Self-check: `grep -c "PB1_PRICE_INSISTENCE_THRESHOLD" el-templo-bot/src/playbooks/constants.ts el-templo-bot/.env.example` returns 2 hits across files.

  </action>
  <verify>
    <automated>cd /Users/bores/el-templo &amp;&amp; test -f el-templo-bot/src/playbooks/constants.ts &amp;&amp; grep -q "PB1_PRICE_INSISTENCE_THRESHOLD" el-templo-bot/src/playbooks/constants.ts &amp;&amp; grep -q "shouldDisclosePrices" el-templo-bot/src/playbooks/constants.ts &amp;&amp; grep -q "priceInsistenceCount" el-templo-bot/src/playbooks/types.ts &amp;&amp; grep -q "disclosureUnlocked" el-templo-bot/src/ai/system-prompt.ts &amp;&amp; grep -q "PB1_PRICE_INSISTENCE_THRESHOLD=2" el-templo-bot/.env.example &amp;&amp; cd el-templo-bot &amp;&amp; pnpm exec tsc --noEmit</automated>
    <automated>cd /Users/bores/el-templo &amp;&amp; grep -rE '\$\s*[0-9]{4,}' el-templo-bot/src/playbooks/constants.ts &amp;&amp; echo "FAIL: hardcoded price in constants" &amp;&amp; exit 1 || true</automated>
  </verify>
  <done>
    - `el-templo-bot/src/playbooks/constants.ts` exists, exports `PB1_PRICE_INSISTENCE_THRESHOLD` and `shouldDisclosePrices`.
    - `PlaybookSessionState` includes optional `priceInsistenceCount?: number` with JSDoc.
    - `SystemPromptOptions` includes optional `disclosureUnlocked?: boolean` with JSDoc.
    - `.env.example` documents `PB1_PRICE_INSISTENCE_THRESHOLD=2` with a comment.
    - `pnpm exec tsc --noEmit` clean on el-templo-bot.
    - No `any`, no `console.log`, no hardcoded prices in any new file.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire the counter — increment-on-priceObjection-in-PB1, persist via setPlaybookState, reset on PB1→non-PB1 transition (PRICE-01)</name>
  <files>el-templo-bot/src/webhook/handler.ts</files>
  <read_first>
    - el-templo-bot/src/webhook/handler.ts :1439-1483 (priceObjection regex + signal-return shape)
    - el-templo-bot/src/webhook/handler.ts :540-610 (pre-AI setPlaybookState write site; understand priorWhyAskedPre / newWhyAsked pattern)
    - el-templo-bot/src/webhook/handler.ts :880-1028 (post-AI setPlaybookState writes, including the stage-advance write at :989-1003 and the "no advance but flags changed" write at :1014-1028 — both must include the new field)
    - el-templo-bot/src/playbooks/constants.ts (created in Task 1) — import PB1_PRICE_INSISTENCE_THRESHOLD if needed, but THIS task only needs the increment+reset logic, not the unlock evaluation
  </read_first>
  <action>
    Add the counter wiring to `el-templo-bot/src/webhook/handler.ts`. Five surgical edits:

    Edit 1 — Read the prior counter from `priorPbState`:
    - Locate the existing `priorPbState` destructuring/read site (search for `priorWhyAskedPre` to find the same neighborhood near :567).
    - Add `const priorPriceInsistenceCount = priorPbState?.priceInsistenceCount ?? 0;` immediately after the `priorWhyAskedPre` declaration.

    Edit 2 — Compute the new counter value:
    - Locate the `priceObjection` consumer (the `computePreAiSignals` return is destructured somewhere near the pre-AI writes; if it isn't, destructure it from the helper's return value). The signal is already returned at :1473-1482.
    - Add after the signal is available pre-AI:
      `const newPriceInsistenceCount = priceObjection && resolved.playbookId === "PB1" ? priorPriceInsistenceCount + 1 : priorPriceInsistenceCount;`
    - The condition uses the existing `priceObjection` boolean — do NOT introduce a parallel regex.
    - The condition gates on `resolved.playbookId === "PB1"` so the counter is per-conversation per-PB1-session.

    Edit 3 — Include `priceInsistenceCount` in the pre-AI `setPlaybookState` write at :597-610:
    - In the existing pre-AI write object, add `priceInsistenceCount: newPriceInsistenceCount,` alongside the other fields. Preserve the structural pattern used for `discoveryTurnCount` and `whyAsked`.

    Edit 4 — Include `priceInsistenceCount` in the post-AI stage-advance write at :989-1003 with reset-on-PB1-leave semantics:
    - When `nextStage !== null`, compute the resetting value:
      `priceInsistenceCount: nextStage.startsWith("PB1.") ? newPriceInsistenceCount : 0,`
    - This resets the counter to 0 when the conversation advances out of PB1 (e.g., into PB2 stages). Per CONTEXT.md PRICE-01: "Counter is scoped per-conversation per-PB1-session. It should reset cleanly when the conversation transitions to PB2 (post-trial) so the PB2 disclosure flow is not gated by PB1 state."

    Edit 5 — Include `priceInsistenceCount` in the post-AI "flags-changed" write at :1014-1028:
    - In the `else if` branch that fires when no stage advance happened but a flag changed, also persist the (unchanged within PB1, since we did not advance) counter:
      `priceInsistenceCount: newPriceInsistenceCount,`
    - Also extend the change-detection condition to include the counter:
      Replace `else if (newTurnCount !== priorTurnCount || newWhyAsked !== priorWhyAskedPre)` with `else if (newTurnCount !== priorTurnCount || newWhyAsked !== priorWhyAskedPre || newPriceInsistenceCount !== priorPriceInsistenceCount)` — so a price-insistence-without-stage-advance is durably persisted.

    Edit 6 — Same treatment in the avatar-only write at :886-908:
    - That branch persists the playbook state when a NEW avatar was detected. Include `priceInsistenceCount: newPriceInsistenceCount,` to avoid clobbering the counter to undefined on avatar-only writes (precedent: discoveryTurnCount and whyAsked are also preserved across this branch).

    Constraints:
    - The regex MUST be the existing one at handler.ts:1440 — no parallel regex, no helper extracted in this task (a future refactor can extract `priceObjection` to a shared helper; not in this phase).
    - Counter increments AT MOST ONCE per inbound message — the priceObjection boolean is a per-inbound bool, so this property holds by construction. Document that property in an inline comment near Edit 2.
    - Per CONTEXT.md threat-model: "Race condition: rapid-fire inbounds (the bug class Phase 93 fixed) interacting with the counter. Mitigation: the Phase 93 SETNX/dead-man-switch protections still apply; the counter inherits handler concurrency safety." — Add an inline comment near Edit 2 acknowledging the Phase 93 handler-entry guard handles concurrency for the counter (no new locking needed).
    - No `any` types. No console.log. Use the existing `log` Pino instance for any new info-level logging (recommended: log when the counter transitions to >= threshold so live debugging is observable).

  </action>
  <verify>
    <automated>cd /Users/bores/el-templo &amp;&amp; grep -c "priceInsistenceCount" el-templo-bot/src/webhook/handler.ts | grep -qE '^[5-9]|^[1-9][0-9]' &amp;&amp; grep -q "priorPriceInsistenceCount" el-templo-bot/src/webhook/handler.ts &amp;&amp; grep -q "newPriceInsistenceCount" el-templo-bot/src/webhook/handler.ts &amp;&amp; ! grep -q 'new RegExp.*caro.*precio' el-templo-bot/src/webhook/handler.ts &amp;&amp; cd el-templo-bot &amp;&amp; pnpm exec tsc --noEmit</automated>
    <automated>cd /Users/bores/el-templo &amp;&amp; grep -cE 'caro\|carisimo\|car\[' el-templo-bot/src/webhook/handler.ts | grep -qE '^1$' &amp;&amp; echo "OK: priceObjection regex appears exactly once (no parallel regex)"</automated>
  </verify>
  <done>
    - `priceInsistenceCount` referenced at least 5 times in handler.ts (Edit 1 read + Edit 2 compute + Edits 3,4,5,6 = 4 writes).
    - The priceObjection regex literal still appears exactly once in handler.ts — no parallel regex was introduced.
    - Counter resets to 0 in the stage-advance branch when `!nextStage.startsWith("PB1.")`.
    - `pnpm exec tsc --noEmit` clean.
    - Inline comments document (a) at-most-once-per-inbound increment property, (b) Phase 93 concurrency inheritance.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Inject disclosure-unlocked prompt addendum + verify-then-fix PB2.E2 placeholder resolution (PRICE-02 + PRICE-03)</name>
  <files>el-templo-bot/src/ai/system-prompt.ts, el-templo-bot/src/webhook/handler.ts, el-templo-bot/src/playbooks/definitions.ts</files>
  <read_first>
    - el-templo-bot/src/ai/system-prompt.ts :390-410 (the active-playbook section injection; the disclosure addendum should land AFTER this section, conditional on the new option)
    - el-templo-bot/src/webhook/handler.ts :617-630 (the getSystemPrompt call site; this is where the new option is passed in)
    - el-templo-bot/src/playbooks/constants.ts (created in Task 1) — import `shouldDisclosePrices`
    - el-templo-bot/src/playbooks/definitions.ts :135-142 (PB2.E2 promptSection with the `[plan_básico]` / `[precio]` placeholders — for PRICE-03 verification)
    - el-templo-bot/src/ai/tools.ts :475-545 (the FULL `check_membership` body: the `users.length === 0` early-return guard at :485-487 is REQUIRED context — it returns `"No encontré una cuenta con ese número..."` BEFORE the subs query can run for leads, so the available-plans branch at :509-525 is unreachable for leads with no `users` row. This is the verified-broken state the Piece C fix addresses.)
  </read_first>
  <preserve>
    The PB1.E4 REGLA FUERTE at `el-templo-bot/src/playbooks/definitions.ts:74` MUST remain byte-for-byte UNCHANGED — Sub-option A is locked per CONTEXT.md PRICE-02. The unlock is purely additive in system-prompt.ts. Verify post-edit:
    - `grep -q "REGLA FUERTE:\\*\\* en esta etapa NO recomendás" el-templo-bot/src/playbooks/definitions.ts`
    - `grep -q "El ÚNICO CTA válido es la clase de prueba GRATIS" el-templo-bot/src/playbooks/definitions.ts`
    Both must still pass after Task 3.

    The COPY-02 preservation strings from plan 99-01 (`framings de arranque grupal` at :138, `lenguaje de arranque grupal` at :147) MUST still match after this task's PB2.E2 edit (if any). If the PRICE-03 fix branch rewrites PB2.E2 text, the rewrite MUST keep both deprecated-anti-pattern guards.

    The existing `users.length === 0` short-circuit message at `el-templo-bot/src/ai/tools.ts:485-487` — `"No encontré una cuenta con ese número. Si sos miembro, puede que estés registrado con otro número."` — MUST remain present in the tool's response for leads. The Piece C fix APPENDS available-plans output to it, never replaces it. Verify post-edit: `grep -q "No encontré una cuenta con ese número" el-templo-bot/src/ai/tools.ts`.

  </preserve>
  <action>
    Three pieces.

    Piece A — Inject the disclosure-unlocked addendum in `el-templo-bot/src/ai/system-prompt.ts`:
    - At the bottom of `getSystemPrompt`, AFTER the active-playbook section injection at :404-406 but BEFORE the soft-rejection-rule injection at :435-439, add a new conditional block:
      ```
      if (options?.disclosureUnlocked && options?.activePlaybook === "PB1") {
        sections.push(PB1_PRICE_DISCLOSURE_UNLOCKED_ADDENDUM);
      }
      ```
    - Define `PB1_PRICE_DISCLOSURE_UNLOCKED_ADDENDUM` as a module-level const string. The addendum text:

      `\n\n*Desbloqueo de disclosure de precios (PB1):* el usuario insistió varias veces en saber precios. En este turno, llamá a check_membership y respondé listando los planes que devuelve con sus precios reales. NUNCA inventes precios ni los deduzcas de otros datos — los valores vienen exclusivamente del resultado del tool. Después de listar, cerrá SIEMPRE re-anclando la prueba gratis con una frase natural tipo "...pero lo mejor es que lo pruebes gratis primero". El re-anclaje es obligatorio: la regla de disclosure no reemplaza la regla de cierre con clase de prueba.`

    - The const MUST live at module top (alongside `SOFT_REJECTION_WHY_RULE` and similar) with JSDoc explaining the trigger and wording constraints.
    - Sub-option A discipline: do NOT edit `definitions.ts:74` (the PB1.E4 REGLA FUERTE). The addendum overrides at the prompt level when injected; when NOT injected, behavior is byte-identical to pre-Phase-99.
    - The gate `activePlaybook === "PB1"` is defensive — handler.ts only sets disclosureUnlocked=true when the counter crosses threshold inside PB1, but the prompt-side check is belt-and-suspenders.

    Piece B — Pass `disclosureUnlocked` from handler.ts:
    - In `el-templo-bot/src/webhook/handler.ts`, locate the `getSystemPrompt` call at :617-630.
    - Compute (use the helper from constants.ts):
      `const disclosureUnlocked = shouldDisclosePrices(newPriceInsistenceCount) && resolved.playbookId === "PB1";`
    - Add `disclosureUnlocked,` to the options object passed to `getSystemPrompt`.
    - Add `disclosureUnlocked` to the existing pino log statement at :633-644 so the diagnostic shows when the addendum was injected.
    - Import `shouldDisclosePrices` from `../playbooks/constants.js` (note the `.js` extension required by ESM-style imports in this codebase — confirm against neighboring imports).

    Piece C — PRICE-03 verify-then-fix for PB2.E2 placeholders:

    First, verify (do not fix yet). Run:
    - `grep -n "\\[plan_básico\\]\\|\\[precio\\]" el-templo-bot/src/playbooks/definitions.ts` — confirm the 2 placeholder tokens at line ~138 inside PB2.E2's promptSection.
    - Inspect the full prompt-assembly path. The promptSection string is concatenated at `system-prompt.ts:404-405` via `\`\n\n${directive}\n\n*Playbook activo: ${definition.id} (${stage.id})*\n\n${stage.promptSection}\`` — there is NO template-substitution step. The `[plan_básico]` and `[precio]` are PASSED AS LITERAL TEXT to the LLM.
    - This means the LLM either (a) reads the brackets as instructions to fill in from context/tool output, or (b) emits the literal text `[plan_básico]` to the user. Outcome (b) is broken UX.

    Then, fix (apply the **script rewrite** branch — NOT a template engine; CONTEXT.md Deferred Ideas explicitly defers template-engine introduction):
    - Edit `el-templo-bot/src/playbooks/definitions.ts` at the PB2.E2 `Objeción precio` script (line ~138). Rewrite the script to instruct the LLM to call `check_membership` first and read values from the tool result, instead of using the bracket placeholders.
    - Current text (verbatim):
      `*Objeción precio* ('me gustó pero es caro', 'está caro', 'no me alcanza'):\nScript: 'Entiendo. ¿Sabés qué? Si te sirve, tenemos el plan [plan_básico] a [precio]. Muchos arrancan por ahí y después van subiendo. ¿Te copa?'.`
    - Rewrite to:
      `*Objeción precio* ('me gustó pero es caro', 'está caro', 'no me alcanza'):\nScript: llamá a check_membership y leé los precios reales del resultado del tool. Respondé en este formato: 'Entiendo. ¿Sabés qué? Si te sirve, tenemos el plan [nombre del plan más accesible] a [precio del tool]. Muchos arrancan por ahí y después van subiendo. ¿Te copa?'. NUNCA inventes precios ni uses los bracket placeholders como texto literal — siempre vienen del tool.`
    - This is a script-rewrite, not a template engine. The brackets are now described as model-fill-from-tool slots, with explicit anti-literal-output instruction.

    Piece D — Extend check_membership to surface available plans for leads (VERIFIED-BROKEN — fix is locked, not optional):

    Verification result from the planning pass (do not re-verify; this is the locked finding): `el-templo-bot/src/ai/tools.ts:485-487` short-circuits with `"No encontré una cuenta con ese número. Si sos miembro, puede que estés registrado con otro número."` when `users.length === 0`. Every LEAD (no `users` row) hits this branch BEFORE the subs/plans query at :498-525 runs. The available-plans listing at :508-528 is unreachable for leads under current code. This means Piece A's disclosure-unlocked addendum, which instructs the LLM to "call check_membership and list the plans it returns," will receive the "no account" string instead of plan data — the addendum cannot satisfy its contract for leads without this Piece.

    Locked fix (bot-side only, `el-templo-bot/src/ai/tools.ts`):

    - Restructure the `users.length === 0` branch at :485-487 to APPEND the available-plans listing instead of returning the bare "no account" message. The existing message MUST remain present (CONTEXT.md preserve constraint above + LLM context for non-disclosure callers); the plan listing is appended.

    - Concrete shape — extract the available-plans query + formatting logic currently inline at :508-528 into a small helper inside `tools.ts` (e.g., `formatAvailablePlans(): Promise<string>`) so BOTH the no-user-row branch AND the existing no-active-subscription branch can call it. The helper queries `subscription_plans WHERE is_active = true AND is_archived = false AND is_trial = false ORDER BY price_regular ASC` (identical to the existing :510-515 SELECT) and returns the `"Planes disponibles:\n- *${plan.name}*: $${plan.price_regular} — ${plan.duration_days} días — ${classesText}"` block (identical formatting to :521-526).

    - Rewrite the users.length === 0 branch (:485-487) to:
      ```
      const planListing = await formatAvailablePlans();
      const baseMsg = "No encontré una cuenta con ese número. Si sos miembro, puede que estés registrado con otro número.";
      return planListing ? `${baseMsg}\n\n${planListing}` : baseMsg;
      ```
      If `formatAvailablePlans()` returns an empty string (no active non-trial plans in DB — unlikely but defend), the response degrades to the original bare "no account" message; no broken-looking trailing whitespace.

    - HARD GUARDS:
      - Do NOT touch `el-templo-api/src/**` (subscription_plans table values/migrations are the other dev's surface; this Piece only restructures the bot-side READ path).
      - The existing "no active subscription" branch at :507 (`if (subs.length === 0)`) MUST continue to work for registered-user-no-sub callers — refactor it to use the same `formatAvailablePlans()` helper. The user-facing prefix `"${userName} no tiene una suscripción activa."` remains; the plan listing comes from the helper.
      - The "no account found" message text MUST remain present byte-for-byte (CONTEXT.md preserve constraint).
      - Do NOT remove or alter the existing top-of-tool phone-validation message at :478-480 (`"Se necesita un número de teléfono para consultar la membresía."`).

    Constraints (HARD GUARDS — apply to Pieces A, B, C, and D collectively):
    - Do NOT touch `el-templo-api/src/**`.
    - Do NOT add a hardcoded price anywhere.
    - Do NOT introduce a template engine (handlebars / mustache / etc.) — script-rewrite only per CONTEXT.md Deferred Ideas.
    - The PB1.E4 REGLA FUERTE at definitions.ts:74 stays byte-equal.
    - The COPY-02 preservation strings at definitions.ts :138 and :147 (`framings de arranque grupal`, `lenguaje de arranque grupal`) MUST still match byte-for-byte after this task — verify with grep.
    - The "No encontré una cuenta con ese número" message at tools.ts:486 MUST still be present in the tool response (post-Piece-D it is the PREFIX of the lead response, no longer the entire response).

  </action>
  <verify>
    <automated>cd /Users/bores/el-templo &amp;&amp; grep -q "PB1_PRICE_DISCLOSURE_UNLOCKED_ADDENDUM" el-templo-bot/src/ai/system-prompt.ts &amp;&amp; grep -q "disclosureUnlocked" el-templo-bot/src/webhook/handler.ts &amp;&amp; grep -q "shouldDisclosePrices" el-templo-bot/src/webhook/handler.ts &amp;&amp; grep -q "REGLA FUERTE" el-templo-bot/src/playbooks/definitions.ts &amp;&amp; grep -q "El ÚNICO CTA válido es la clase de prueba GRATIS" el-templo-bot/src/playbooks/definitions.ts &amp;&amp; grep -q "framings de arranque grupal" el-templo-bot/src/playbooks/definitions.ts &amp;&amp; grep -q "lenguaje de arranque grupal" el-templo-bot/src/playbooks/definitions.ts &amp;&amp; cd el-templo-bot &amp;&amp; pnpm exec tsc --noEmit</automated>
    <automated>cd /Users/bores/el-templo &amp;&amp; grep -q "formatAvailablePlans" el-templo-bot/src/ai/tools.ts &amp;&amp; grep -q "No encontré una cuenta con ese número" el-templo-bot/src/ai/tools.ts</automated>
    <automated>cd /Users/bores/el-templo &amp;&amp; grep -rE '\$\s*[0-9]{4,}' el-templo-bot/src/ai/system-prompt.ts el-templo-bot/src/playbooks/ el-templo-bot/src/ai/tools.ts &amp;&amp; echo "FAIL: hardcoded prices in price-disclosure surfaces" &amp;&amp; exit 1 || true</automated>
    <automated>cd /Users/bores/el-templo &amp;&amp; git diff HEAD -- 'el-templo-api/src/**' | wc -l | grep -qE '^[[:space:]]*0[[:space:]]*$' &amp;&amp; echo "OK: zero el-templo-api/src/** changes"</automated>
  </verify>
  <done>
    - `PB1_PRICE_DISCLOSURE_UNLOCKED_ADDENDUM` module-level const exists in `system-prompt.ts` with the disclosure-unlocked text including the "prueba gratis" re-anchor clause.
    - The new conditional injection in `getSystemPrompt` is in place AFTER the active-playbook section, BEFORE the soft-rejection injection.
    - `handler.ts` computes `disclosureUnlocked` via `shouldDisclosePrices(newPriceInsistenceCount) && resolved.playbookId === "PB1"` and passes it to `getSystemPrompt`. The diagnostic log includes the field.
    - PB2.E2 `Objeción precio` script in `definitions.ts` is rewritten to instruct the LLM to call check_membership and read prices from the tool result — no template engine introduced.
    - PB1.E4 REGLA FUERTE byte-equal preservation verified.
    - COPY-02 preservation strings (`framings de arranque grupal`, `lenguaje de arranque grupal`) byte-equal preservation verified.
    - PRICE-03 + Piece D: `tools.ts` `users.length === 0` branch now appends `formatAvailablePlans()` output to the preserved "No encontré una cuenta" prefix; the helper is shared with the existing `subs.length === 0` branch (DRY); the unreachable-for-leads bug is closed and the disclosure-unlocked addendum's "call check_membership and list plans" instruction is now satisfiable for leads.
    - `git diff HEAD -- 'el-templo-api/src/**' | wc -l` returns 0.
    - `pnpm exec tsc --noEmit` clean.
    - Tests for end-to-end behavior live in plan 99-03 (especially the 3rd-insistence test that now exercises the lead → check_membership → plan listing path end-to-end with the deterministic mock from Task 1 of 99-03).
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary    | Description                                                                                                                                                                            |
| ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| user→regex  | The priceObjection regex at handler.ts:1440 classifies user text. A crafted inbound could spam matching tokens to game the counter.                                                    |
| user→Redis  | The counter persists per-conversation in the `wa:playbook:<phone>` hash. An attacker controlling a phone could "prime" the counter across sessions if the reset semantics were broken. |
| handler→LLM | The disclosure-unlocked addendum, when injected, instructs the LLM to call check_membership. The LLM could ignore the re-anchor instruction.                                           |
| tool→DB     | check_membership reads `subscription_plans.price_regular`. The other developer owns the DB price values; the bot reads them dynamically — no hardcoded prices.                         |

## STRIDE Threat Register (block_on: high)

| Threat ID | Category                                      | Component                                                                        | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                     |
| --------- | --------------------------------------------- | -------------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-99-05   | Tampering (prompt-injection price unlock)     | handler.ts:1440 priceObjection regex + counter increment                         | mitigate    | The counter increments AT MOST ONCE per inbound message (priceObjection is a per-inbound boolean). A user spamming "caro caro caro" in one message still only adds 1 to the counter. Documented in inline comment near the increment site. The regex itself is the existing one — no new attack surface introduced.                 |
| T-99-06   | Tampering (state persistence abuse)           | playbook-state.ts Redis key `wa:playbook:<phone>`                                | mitigate    | Per CONTEXT.md PRICE-01: counter scoped per-conversation per-PB1-session, resets to 0 on transition out of PB1 (handler.ts Edit 4). The 6h TTL on the Redis key bounds priming attacks; a fresh PB1 starts with count=0 because the field is absent in pre-99 entries and treated as 0 (backward-compat per Phase 90/91 precedent). |
| T-99-07   | Race condition                                | counter read-modify-write across rapid-fire inbounds                             | mitigate    | Phase 93 SETNX/dead-man-switch protects handler entry. Only one handler runs per phone at a time. The counter inherits this concurrency safety. Documented in inline comment near Edit 2.                                                                                                                                           |
| T-99-08   | Information disclosure                        | LLM emits literal `[plan_básico]` text to user                                   | mitigate    | PRICE-03 verify-then-fix: rewrite PB2.E2 script to explicitly forbid literal bracket-token output and require check*membership tool call first. Test in plan 99-03 asserts outbound contains no literal `[plan*` substring.                                                                                                         |
| T-99-09   | Information disclosure (price values leakage) | check_membership tool output                                                     | accept      | DB prices are business data owned by the other dev. The bot relays them dynamically; this plan does not change what is in the DB or how prices flow from DB to LLM. No new attack surface.                                                                                                                                          |
| T-99-10   | Repudiation                                   | when prompt addendum is injected, the LLM may NOT honor the free-trial re-anchor | mitigate    | Plan 99-03 integration test PRICE-04 asserts both (a) outbound contains the DB plan prices verbatim AND (b) outbound contains the free-trial re-anchor phrase substring (e.g., "gratis", "prueba"). If the LLM regularly drops the re-anchor in live use, a future phase adds a handler-side post-process check; out of scope here. |
| T-99-SC   | Tampering                                     | npm/pnpm installs                                                                | accept      | No new packages added in this plan. No package-legitimacy audit required.                                                                                                                                                                                                                                                           |

</threat_model>

<verification>
- `cd el-templo-bot && pnpm exec tsc --noEmit` exit 0.
- `grep -rE '\$\s*[0-9]{4,}' el-templo-bot/src` returns 0 hits in this plan's modified files (system-prompt.ts, constants.ts, types.ts, handler.ts, definitions.ts). The pre-existing `$20,000` reference in system-prompt.ts:321 (single-class drop-in price reference, not a plan price) is acknowledged and NOT modified by this plan — it remains as a discovered-prior-art reference; if removal is desired, a future phase owns that decision.
- `git diff HEAD -- 'el-templo-api/src/**'` produces zero changes.
- The existing priceObjection regex at handler.ts:1440 is unchanged (verified by grep that the literal regex pattern appears exactly once).
- PB1.E4 REGLA FUERTE at definitions.ts:74 byte-equal to pre-Phase-99 state.
- COPY-02 preservation strings (`framings de arranque grupal` at :138, `lenguaje de arranque grupal` at :147) byte-equal to pre-Phase-99 state.
- Sub-option A locked: no template syntax (`{{`, `{%`, `${...}` for plan/price interpolation) introduced in `definitions.ts` or its loaders.
- All-or-nothing semantics: a Redis-down scenario (the existing silent-degrade pattern in playbook-state.ts) means the counter is treated as 0 on every turn, which is the safe-by-default behavior (no false disclosure).
</verification>

<success_criteria>

- PRICE-01 closed: counter persists in `wa:playbook:<phone>`, increments at most once per inbound when `priceObjection && activePlaybook === PB1`, resets to 0 on PB1→non-PB1 advance.
- PRICE-02 closed: single tunable constant `PB1_PRICE_INSISTENCE_THRESHOLD = 2` in `playbooks/constants.ts`; env override documented in `.env.example`; Sub-option A wiring (additive prompt addendum, no `definitions.ts:74` change); free-trial re-anchor clause baked into the addendum.
- PRICE-03 closed: check_membership available-plans branch handles the lead-no-users-row case (verified; fixed bot-side only if broken); PB2.E2 `[plan_básico]`/`[precio]` placeholder situation resolved via script rewrite (no template engine).
- Bot source contains zero NEW hardcoded prices (the pre-existing $20,000 reference in system-prompt.ts:321 is acknowledged; not modified).
- Zero modifications to `el-templo-api/src/**`.
- tsc clean on `el-templo-bot`.
- Tests live in plan 99-03.
- Commits use the `feat(99-02):` / `refactor(99-02):` prefix per project commit conventions; pre-commit hook (Prettier) auto-fixes formatting; if pre-commit fails, create a new commit (do NOT amend).
  </success_criteria>

<output>
Create `.planning/phases/99-bot-copy-and-price-disclosure-fixes/99-02-SUMMARY.md` when done, following `@$HOME/.claude/get-shit-done/templates/summary.md`. In the SUMMARY, explicitly document the PRICE-03 verification outcome (was the check_membership available-plans branch broken for a lead with no users-row? was a fix applied? if not, why not?) and the PB2.E2 placeholder-resolution path taken (script rewrite vs other; cite which lines of definitions.ts changed).
</output>
