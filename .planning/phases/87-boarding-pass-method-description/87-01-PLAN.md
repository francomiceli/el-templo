---
phase: 87-boarding-pass-method-description
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-bot/src/ai/knowledge.ts
autonomous: true
requirements: [BPASS-01, BPASS-02, BPASS-03]
must_haves:
  truths:
    - "Exactly ONE canonical Boarding Pass definition exists in knowledge.ts (inside Reglas Zero section)"
    - "All 7 previously-identified non-canonical Boarding Pass mentions now read as the pointer form 'Boarding Pass (ver *Reglas Zero*)' with no inline recap"
    - "No section re-explains what the Boarding Pass is or contradicts the canonical definition"
    - "Per-state test suite (knowledge-gating.test.ts) still passes — lead knowledge still contains BP reference, non-lead still string-equals full"
    - "Prompt-size regression lock (prompt-size.test.ts) still passes (≥20% rendered / ≥35% knowledge block)"
  artifacts:
    - path: "el-templo-bot/src/ai/knowledge.ts"
      provides: "BP consolidation — canonical in Reglas Zero, pointer form at 7 sites"
      contains: "Boarding Pass (ver *Reglas Zero*)"
  key_links:
    - from: "el-templo-bot/src/ai/knowledge.ts"
      to: "Reglas Zero section body"
      via: "pointer-form references from 7 other sites"
      pattern: "Boarding Pass \\(ver \\*Reglas Zero\\*\\)"
---

<objective>
Consolidate Boarding Pass references in knowledge.ts into a single canonical definition inside "Reglas Zero" (existing, unchanged), and convert the 7 non-canonical mention sites to the pointer form `Boarding Pass (ver *Reglas Zero*)`.

Purpose: Eliminate Boarding Pass framing drift — 9 mentions across 6 sections today create contradictory explanations and waste prompt tokens. One canonical definition + short pointers gives Mica consistent grounding without re-explaining BP every time it appears.

Output: knowledge.ts with BP explained once (Reglas Zero) and referenced 7 times by name+pointer.
</objective>

<execution_context>
@/Users/bores/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bores/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/87-boarding-pass-method-description/87-CONTEXT.md
@.planning/phases/86-knowledge-gating/86-03-SUMMARY.md

# Implementation target

@el-templo-bot/src/ai/knowledge.ts

# Existing regression locks that must keep passing

@el-templo-bot/test/ai/prompt-size.test.ts
@el-templo-bot/test/knowledge-gating.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert 7 Boarding Pass non-canonical mentions to pointer form</name>
  <files>el-templo-bot/src/ai/knowledge.ts</files>
  <action>
Leave the canonical definition untouched. Canonical site is `ZERO_RULES` (line 156) inside the "Reglas Zero" section — this is the ONLY place that explains what the Boarding Pass IS.

Convert the 7 non-canonical mention sites to the pointer form `Boarding Pass (ver *Reglas Zero*)` — name plus short pointer, NO inline recap. Sites (per CONTEXT.md audit):

1. knowledge.ts:242 — `TRIAL_FLOW` bullet: "La primera clase tiene un valor de $20,000 pero es 100% bonificada con el Boarding Pass." → "…bonificada con el Boarding Pass (ver _Reglas Zero_)."
2. knowledge.ts:243 — `TRIAL_FLOW` bullet: "El Boarding Pass ademas habilita descuentos exclusivos en la primera membresia (precios Zero)." → REMOVE this bullet entirely. Reason: it is pure BP re-explanation (what BP does = already canonical in Reglas Zero). Keeping a pointer here would be redundant with the pointer added on line 242 in the same section. Removing shaves ~90 chars and removes contradictory framing. If the reviewer objects during verification, restore as: "El Boarding Pass (ver _Reglas Zero_) ademas habilita descuentos exclusivos en la primera membresia." — but default is removal.
3. knowledge.ts:250 — `TRIAL_FLOW` step 4: "Administracion envia el Boarding Pass con los detalles de la sesion." → "Administracion envia el Boarding Pass (ver _Reglas Zero_) con los detalles de la sesion."
4. knowledge.ts:341 — `SALES_TECHNIQUES` bullet: "…\"Beneficio unico del Boarding Pass\" (solo una vez)." → "…\"Beneficio unico del Boarding Pass (ver _Reglas Zero_)\" (solo una vez)."
5. knowledge.ts:342 — `SALES_TECHNIQUES` bullet: "…precio Zero/Boarding Pass (ej: ~$80,000~ -> _$65,000_)." → "…precio Zero/Boarding Pass (ver _Reglas Zero_) (ej: ~$80,000~ -> _$65,000_)."
6. knowledge.ts:357 — `OBJECTIONS_SALES` item 1: "Menciona el descuento del Boarding Pass (primera vez)." → "Menciona el descuento del Boarding Pass (ver _Reglas Zero_)." (the "(primera vez)" parenthetical is a partial re-explanation — drop it; Reglas Zero already specifies "primer mes / beneficio unico")
7. knowledge.ts:372 — `OBJECTIONS_SALES` item 4: "Mencionar el Boarding Pass como opcion suave: \"Tene en cuenta que el Boarding Pass es de uso unico, asi que cuando te decidas lo aprovechas.\"" → keep the full quote (it is Mica's conversational script, NOT a BP explanation in prompt voice) but prepend the pointer to the instruction verb: "Mencionar el Boarding Pass (ver _Reglas Zero_) como opcion suave: \"Tene en cuenta que el Boarding Pass es de uso unico, asi que cuando te decidas lo aprovechas.\"" The quoted Mica dialogue stays verbatim — it's an example of what Mica says to a user, not a definition in the prompt.

Audit after editing: grep for `Boarding Pass` in knowledge.ts should return exactly 8 hits — 1 canonical (line ~156) + 7 pointer-form (or 6 if line 243 is removed per above = 7 total). No other mentions should remain.

Do NOT touch: section tags, section ordering, Reglas Zero body (ZERO_RULES constant), any other content.

Avoid: paraphrasing the canonical definition, adding new BP explanations anywhere, introducing a new BP section, changing Spanish tone or wording beyond the exact substitutions above.
</action>
<verify>
<automated>cd el-templo-bot && pnpm tsc --noEmit && pnpm vitest run test/ai/prompt-size.test.ts test/knowledge-gating.test.ts</automated>
<manual> 1. `grep -c "Boarding Pass" el-templo-bot/src/ai/knowledge.ts` returns 7 or 8 (1 canonical + 6 or 7 pointers; 7 total if line 243 removed, 8 if preserved) 2. `grep -n "Boarding Pass (ver \*Reglas Zero\*)" el-templo-bot/src/ai/knowledge.ts` returns 6 or 7 pointer-form hits 3. `grep -n "Boarding Pass" el-templo-bot/src/ai/knowledge.ts | grep -v "Reglas Zero\|primer mes en El Templo"` returns ZERO hits (no re-explanations outside canonical)
</manual>
<sampling_rate>after this task commits, before plan 87-02 begins</sampling_rate>
</verify>
<done> - 7 non-canonical BP sites converted (or 6 converted + 1 removed per line 243 decision) - Canonical definition in Reglas Zero is byte-identical to pre-task state - `pnpm tsc --noEmit` clean - `pnpm vitest run test/ai/prompt-size.test.ts test/knowledge-gating.test.ts` passes (BP still reaches leads; KGATE-05 still green) - Commit: `refactor(87-01): consolidate Boarding Pass to canonical definition + 7 pointers`
</done>
</task>

</tasks>

<verification>
Run from `el-templo-bot/`:

```bash
pnpm tsc --noEmit
pnpm vitest run
```

Expected: 514/514 tests pass, zero type errors.

Prompt-size delta check (informational, no assertion here — 87-02 does the full check):

```bash
node -e "import('./dist/ai/system-prompt.js').then(m => console.log(m.getSystemPrompt({clientState:'lead',activePlaybook:'PB1',currentStage:'E1A'}).length))"
```

Expected: smaller than 18,617 (this plan only removes chars, never adds).
</verification>

<success_criteria>

- Exactly one canonical BP definition survives (Reglas Zero, unchanged)
- 6 or 7 pointer-form references exist at the identified sites
- No contradictory BP framings anywhere
- All 514 existing tests still pass
- `tsc --noEmit` clean
  </success_criteria>

<output>
After completion, create `.planning/phases/87-boarding-pass-method-description/87-01-SUMMARY.md` capturing:
- Exact site-by-site diff summary (before/after per line)
- Decision on line 243 (removed vs preserved with pointer)
- Rendered PB1.E1A size post-task (for 87-02 budget math)
- Any deviations from action list
</output>
