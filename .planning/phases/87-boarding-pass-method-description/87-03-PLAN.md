---
phase: 87-boarding-pass-method-description
plan: 03
type: execute
wave: 3
depends_on: ["87-01", "87-02"]
files_modified:
  - el-templo-bot/test/knowledge-gating.test.ts
autonomous: true
requirements:
  [BPASS-01, BPASS-02, BPASS-03, METHOD-01, METHOD-02, METHOD-03, METHOD-04]
must_haves:
  truths:
    - "knowledge-gating.test.ts has new assertions that lock: (a) exactly one canonical BP definition, (b) all 7 non-canonical sites read as pointer form, (c) elevator reaches leads and detalle does not, (d) both method sections reach non-leads, (e) deflection rule is present in rendered prompt for all client states"
    - "All existing 514 tests still pass with zero modifications to prior assertions"
    - "New assertions fail deterministically if any BP site regresses to re-explanation, if method content gets paraphrased, or if the deflection rule is removed"
    - "prompt-size.test.ts continues to pass (KGATE-05 dual-threshold lock)"
  artifacts:
    - path: "el-templo-bot/test/knowledge-gating.test.ts"
      provides: "Phase-87 regression locks for BP consolidation + method sections + deflection"
      contains: "Metodo (elevator)"
  key_links:
    - from: "el-templo-bot/test/knowledge-gating.test.ts"
      to: "el-templo-bot/src/ai/knowledge.ts::getBusinessKnowledge"
      via: "per-state call assertions for new method sections"
      pattern: "getBusinessKnowledge\\(['\"](lead|trial|active_member|inactive_member|expired_member)['\"]\\)"
    - from: "el-templo-bot/test/knowledge-gating.test.ts"
      to: "el-templo-bot/src/ai/system-prompt.ts::getSystemPrompt"
      via: "deflection-rule-in-rendered-prompt assertions"
      pattern: "lo sent[ií]s cuando lleg[aá]s"
---

<objective>
Lock Phase 87's behavior with new test assertions: Boarding Pass has exactly one canonical definition, method sections reach their intended audiences, and the deflection rule appears in rendered prompts for all states.

Purpose: Prevent silent regressions. Without these locks, a future refactor could (a) reintroduce BP re-explanations, (b) paraphrase METHOD_DETAIL, (c) lose the deflection rule, (d) accidentally re-tag Metodo (detalle) as discovery — and existing tests would not catch any of it.

Output: Extended knowledge-gating.test.ts (same file — new `describe` blocks, no new file). Existing prompt-size.test.ts unchanged; it already covers the size regression.
</objective>

<execution_context>
@/Users/bores/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bores/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/87-boarding-pass-method-description/87-CONTEXT.md
@.planning/phases/87-boarding-pass-method-description/87-01-SUMMARY.md
@.planning/phases/87-boarding-pass-method-description/87-02-SUMMARY.md
@.planning/phases/86-knowledge-gating/86-03-SUMMARY.md

# Extension target (not rewrite — add new describe blocks)

@el-templo-bot/test/knowledge-gating.test.ts

# Keep green, do not modify

@el-templo-bot/test/ai/prompt-size.test.ts

# Sources under test

@el-templo-bot/src/ai/knowledge.ts
@el-templo-bot/src/ai/system-prompt.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BP consolidation + method section + deflection-rule test assertions</name>
  <files>el-templo-bot/test/knowledge-gating.test.ts</files>
  <action>
Open `el-templo-bot/test/knowledge-gating.test.ts`. Do NOT touch existing `describe` blocks or assertions — they are the KGATE-02/03/04 locks. Append THREE new `describe` blocks at the bottom of the file:

**Block A — `describe("BP consolidation (BPASS-01/02/03)")`** (5 assertions):

1. `getBusinessKnowledge()` contains exactly ONE occurrence of the canonical phrase fragment `"primer mes en El Templo"` (the unique opener of the canonical BP paragraph in ZERO_RULES). `expect(full.split("primer mes en El Templo").length - 1).toBe(1)`.
2. `getBusinessKnowledge()` contains >= 6 occurrences of the pointer form `"Boarding Pass (ver *Reglas Zero*)"`. Use `>=` not `===` to tolerate the line-243 decision (6 if removed, 7 if preserved).
3. `getBusinessKnowledge('lead')` contains at least 1 occurrence of the pointer form (BP reaches leads via Reglas Zero canonical + any pointer sites in discovery-tagged sections).
4. `getBusinessKnowledge()` does NOT contain any "Boarding Pass" mention that is followed (within 200 chars) by a re-explanation keyword NOT in Reglas Zero. Concretely: regex-match `/Boarding Pass(?! \(ver \*Reglas Zero\*\))(?! \(primer mes en El Templo\))/g` across the string, and assert every match is within the substring bounds of the Reglas Zero body. Implementation hint: compute `regZeroStart = full.indexOf("*Precios Zero (Descuentos)*")`, `regZeroEnd = full.indexOf("\n\n", full.indexOf("Numero Cardio: 432 2555"))`; every `/Boarding Pass/g` match index NOT in `[regZeroStart, regZeroEnd]` MUST be immediately followed by `" (ver *Reglas Zero*)"`.
5. `getBusinessKnowledge('lead')` canonical-count check: the phrase `"primer mes en El Templo"` appears exactly once in the lead knowledge too (Reglas Zero is discovery-tagged).

**Block B — `describe("Method sections (METHOD-01/02/04)")`** (6 assertions):

1. `getBusinessKnowledge()` contains `"*Metodo (elevator)*"` — header present.
2. `getBusinessKnowledge()` contains `"*Metodo (detalle)*"` — header present.
3. `getBusinessKnowledge('lead')` contains `"*Metodo (elevator)*"` (METHOD-04 — elevator reaches leads).
4. `getBusinessKnowledge('lead')` does NOT contain `"*Metodo (detalle)*"` (detalle is untagged, full-only).
5. For each non-lead state via `Exclude<ClientState, "lead">[]` (same pattern as existing KGATE-03 tests), the returned string contains BOTH `"*Metodo (elevator)*"` AND `"*Metodo (detalle)*"`.
6. METHOD-01 verbatim lock: `getBusinessKnowledge()` contains three signature fragments proving the verbatim long-form survives: `"método internacional"`, `"cuatro niveles activos simultáneamente"`, `"No todos los días son iguales"`. If the CONTEXT.md pending_content is updated before execution, adjust these fragments to match the new verbatim — but pick THREE stable fragments that would not survive paraphrasing.

**Block C — `describe("Method-internals deflection rule (METHOD-03)")`** (3 assertions):

1. For `clientState='lead'`, `getSystemPrompt({clientState:'lead', activePlaybook:'PB1', currentStage:'E1A'})` contains the literal `"lo sentís cuando llegás"`.
2. For every non-lead state (iterated via `Exclude<ClientState, "lead">[]`), the rendered prompt also contains `"lo sentís cuando llegás"` (universal framing — reaches all states).
3. For `clientState='lead'`, the rendered prompt contains exactly ONE occurrence of `"lo sentís cuando llegás"` (no accidental duplication).

**Implementation notes:**

- Import style: follow existing file conventions. Import `getSystemPrompt` from `../src/ai/system-prompt.js` and `getBusinessKnowledge`, `ClientState` as already set up at top of file.
- No `any`. Use `Exclude<ClientState, "lead">[]` for iteration (same type pattern proven in 86-03).
- Use `it.each` for parametrised non-lead assertions in Blocks B.5 and C.2.
- No magic strings — declare const fragments at the top of each describe block if reused.
- Expected test count delta: +14 assertions. Total bot suite should go from 514 → 528.

Avoid: modifying existing describe blocks, changing `test/ai/prompt-size.test.ts`, adding new test files (append in-place keeps CI diff minimal), loosening any existing assertion to make room for new ones.
</action>
<verify>
<automated>cd el-templo-bot && pnpm tsc --noEmit && pnpm vitest run</automated>
<manual> 1. Full suite reports 528 passed (or 527 if the line-243 decision leaves BP pointer count at the `>= 6` boundary — count must be strictly greater than 514 by ≥ 13) 2. Deliberately break one assertion locally (e.g., rename ELEVATOR_TEXT) and confirm the relevant test fails with a clear message; restore before commit 3. `grep -c "describe(" el-templo-bot/test/knowledge-gating.test.ts` = previous count + 3
</manual>
<sampling_rate>after this task commits, phase verification can begin</sampling_rate>
</verify>
<done> - 14 new assertions added across 3 describe blocks - Full suite passes: expect 528 tests (514 baseline + 14 new) - `tsc --noEmit` clean - No existing test modified - Commit: `test(87-03): lock BP consolidation, method sections, and deflection rule`
</done>
</task>

</tasks>

<verification>
From `el-templo-bot/`:

```bash
pnpm tsc --noEmit
pnpm vitest run
```

Expected: 528 tests pass (or 527 boundary-case). Zero type errors.

Phase-87 requirements coverage check:

```bash
# BPASS-01, 02, 03 → Block A assertions
# METHOD-01, 02, 04 → Block B assertions
# METHOD-03 → Block C assertions
# All 7 requirements covered by new tests + existing prompt-size.test.ts (KGATE-05 headroom lock)
```

</verification>

<success_criteria>

- 3 new describe blocks, 14 new assertions total
- BP-consolidation block catches: re-explanations outside Reglas Zero, canonical drift, pointer-form regression
- Method-sections block catches: missing elevator, leaked detalle to leads, paraphrased long-form
- Deflection-rule block catches: missing rule, rule gated away from non-leads, accidental duplication
- Full test suite green (528 or 527)
- `tsc --noEmit` clean
  </success_criteria>

<output>
After completion, create `.planning/phases/87-boarding-pass-method-description/87-03-SUMMARY.md` capturing:
- Final assertion count (14 expected; note any trimmed for false-positive reasons)
- Final test-suite total (514 + 14 = 528)
- Any test that was rewritten vs appended
- Rendered PB1.E1A size at end of Phase 87 (for Phase 88 reference)
</output>
