---
phase: 87-boarding-pass-method-description
plan: 02
type: execute
wave: 2
depends_on: ["87-01"]
files_modified:
  - el-templo-bot/src/ai/knowledge.ts
  - el-templo-bot/src/ai/system-prompt.ts
autonomous: false
requirements: [METHOD-01, METHOD-02, METHOD-03, METHOD-04]
must_haves:
  truths:
    - "knowledge.ts contains a new 'Metodo (elevator)' section at position 2, tagged ['discovery'], body ≤200 chars (content only, excluding title)"
    - "knowledge.ts contains a new 'Metodo (detalle)' section at position 3, tagged [] (full-only), body is the verbatim team-authored long-form from CONTEXT.md <pending_content> with ZERO paraphrasing"
    - "ROM and all subsequent sections shift down by 2 positions; tag assignments for shifted sections are unchanged"
    - "system-prompt.ts framing contains a new deflection rule for method-internals questions that instructs Mica to answer 'lo sentís cuando llegás' and re-anchor the trial CTA"
    - "The deflection rule does NOT trigger on 'what is the method' (elevator pitch handles that)"
    - "getBusinessKnowledge('lead') output INCLUDES 'Metodo (elevator)' and EXCLUDES 'Metodo (detalle)'"
    - "getBusinessKnowledge() (no arg) and all non-lead states include BOTH method sections"
    - "Rendered PB1.E1A prompt stays ≤ 18,916 chars (KGATE-05 threshold); if it breaches, execution HALTS at the CHECKPOINT task and does NOT auto-trim team content"
  artifacts:
    - path: "el-templo-bot/src/ai/knowledge.ts"
      provides: "Two new method sections (elevator + detalle) inserted at positions 2 and 3"
      contains: "Metodo (elevator)"
    - path: "el-templo-bot/src/ai/system-prompt.ts"
      provides: "Method-internals deflection rule in framing"
      contains: "lo sentís cuando llegás"
  key_links:
    - from: "el-templo-bot/src/ai/knowledge.ts SECTIONS[1]"
      to: "lead knowledge output"
      via: "tags: ['discovery']"
      pattern: "tags: \\[\"discovery\"\\]"
    - from: "el-templo-bot/src/ai/knowledge.ts SECTIONS[2]"
      to: "full-knowledge output (non-lead only)"
      via: "tags: [] (untagged)"
      pattern: "title: \"Metodo \\(detalle\\)\""
    - from: "el-templo-bot/src/ai/system-prompt.ts framing"
      to: "Mica response behavior on method-internals questions"
      via: "deflection rule text"
      pattern: "lo sent[ií]s cuando lleg[aá]s"
---

<objective>
Add the team-authored method description to knowledge.ts as two sections (a ≤200-char elevator tagged 'discovery', and the verbatim long-form tagged empty), and add a method-internals deflection rule to the system-prompt.ts framing.

Purpose: Give Mica a consistent, grounded answer to "what is your method?" (elevator — leads get it) while protecting internal methodology details (detalle — non-leads only) and instructing Mica to deflect mechanical how-it-works questions with the team phrase "lo sentís cuando llegás".

Output: Two new knowledge sections + one framing rule + one CHECKPOINT verifying KGATE-05 headroom is preserved.
</objective>

<execution_context>
@/Users/bores/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bores/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/87-boarding-pass-method-description/87-CONTEXT.md
@.planning/phases/87-boarding-pass-method-description/87-01-SUMMARY.md
@.planning/phases/86-knowledge-gating/86-03-SUMMARY.md

# Implementation targets

@el-templo-bot/src/ai/knowledge.ts
@el-templo-bot/src/ai/system-prompt.ts

# Regression locks that must keep passing

@el-templo-bot/test/ai/prompt-size.test.ts
@el-templo-bot/test/knowledge-gating.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert 'Metodo (detalle)' and 'Metodo (elevator)' sections at positions 2 and 3</name>
  <files>el-templo-bot/src/ai/knowledge.ts</files>
  <action>
Add two new SECTIONS entries immediately after "Que es El Templo" (currently position 1, array index 0). Post-edit ordering:
  [0] Que es El Templo (unchanged)
  [1] Metodo (elevator)      ← NEW, tags: ['discovery']
  [2] Metodo (detalle)       ← NEW, tags: []
  [3] ROM                    ← shifted +2 from current [1]
  [4] Planes y Precios       ← shifted +2 from current [2]
  … (all subsequent sections shift by +2; tag assignments unchanged)

Final SECTIONS length: 16 (was 14).

**'Metodo (elevator)' section:**

- Title: `"Metodo (elevator)"`
- Tags: `["discovery"]`
- Body format: `\`_Metodo (elevator)_\n\n${ELEVATOR_TEXT}\``
- `ELEVATOR_TEXT` is a new module-level `const` declared near the other copy constants. **Budget: ≤200 chars content-only** (the string value of `ELEVATOR_TEXT`, excluding the `*Metodo (elevator)*\n\n` header).
- Draft the elevator from the verbatim long-form in CONTEXT.md `<pending_content>`. Hooks that must survive compression (per CONTEXT.md specifics): "método internacional", "cuatro niveles simultáneos", "no salirse del grupo". Two sentences, tuteo, Mica's voice, no emojis, no bold/italic inside the elevator body.
- Draft suggestion (134 chars — as a starting point; executor refines): `"Entrenamos calistenia con un método internacional: cuatro niveles en simultáneo en cada clase, para que progreses sin salirte del grupo."`
- If the executor's draft exceeds 200 chars: iterate. Do NOT dip below ~120 chars unless the three hooks naturally compress further — too-short dilutes signal.

**'Metodo (detalle)' section:**

- Title: `"Metodo (detalle)"`
- Tags: `[]` (untagged — leads never see this; only non-leads)
- Body format: `\`_Metodo (detalle)_\n\n${METHOD_DETAIL}\``
- `METHOD_DETAIL` is a new module-level `const` containing the verbatim team text from CONTEXT.md `<pending_content>` — copy it EXACTLY, preserving paragraph breaks, punctuation, tuteo ("conectás", "querés", "entrenaste"), em-dashes, and the "No todos los días son iguales" line. Do NOT paraphrase. Do NOT compress. Do NOT add headings. Do NOT reformat into bullets.
- The only permissible adjustment is collapsing soft-wrap line breaks in the source (CONTEXT.md renders with column wraps) back into flowing paragraphs. Paragraph boundaries (blank lines between the four paragraphs) must be preserved.

Placement rationale: positions 2 and 3 put method content immediately after "Que es El Templo" — semantic flow is "what is it → method elevator → method detail → ROM → plans…". CONTEXT.md locks this ordering.

Update the doc comment at the top of SECTIONS (lines ~474-482) to say "16 sections" and note the two new method entries. Update the header comment (lines ~20-25) from "14 sections" to "16 sections" and list the new entries in the enumeration.

Avoid: tagging Metodo (detalle) as 'discovery' (would blow KGATE-05 headroom — CONTEXT.md is explicit), paraphrasing team content, adding a third method section, moving Reglas Zero or other BP-related content.
</action>
<verify>
<automated>cd el-templo-bot && pnpm tsc --noEmit && pnpm vitest run test/knowledge-gating.test.ts</automated>
<manual> 1. `grep -c "title: \"Metodo" el-templo-bot/src/ai/knowledge.ts` returns 2 2. `node -e "import('./dist/ai/knowledge.js').then(m => { const lead = m.getBusinessKnowledge('lead'); console.log('lead has elevator:', lead.includes('Metodo (elevator)')); console.log('lead has detalle:', lead.includes('Metodo (detalle)')); })"` → lead has elevator: true / lead has detalle: false 3. ELEVATOR_TEXT .length ≤ 200 (print from a scratch node REPL or add a transient log)
</manual>
<sampling_rate>after this task commits, before Task 2 begins</sampling_rate>
</verify>
<done> - SECTIONS has 16 entries in the correct order - ELEVATOR_TEXT content-length ≤ 200 chars and contains the three hook concepts - METHOD_DETAIL is verbatim from CONTEXT.md `<pending_content>` (byte-for-byte match modulo soft-wrap reflow) - `tsc --noEmit` clean - Existing knowledge-gating.test.ts passes (per-state assertions still hold for non-method sections; new method sections reach their intended audiences) - Commit: `feat(87-02): add method elevator and detalle knowledge sections`
</done>
</task>

<task type="auto">
  <name>Task 2: Add method-internals deflection rule to system-prompt.ts framing</name>
  <files>el-templo-bot/src/ai/system-prompt.ts</files>
  <action>
Add a new framing rule to `getSystemPrompt` in system-prompt.ts that instructs Mica to deflect method-internals questions.

Location: inside the framing section of `getSystemPrompt` (NOT inside knowledge.ts — CONTEXT.md is explicit; the deflection is behavioral, not factual). Choose a natural seam near the tuteo / response-shape rules block (around line 130-225). Read the file's framing structure before inserting to match the existing numbered-rule or heading style.

Rule content (draft — refine to match surrounding rule style in the file; keep tuteo):

```
*Preguntas sobre cómo funciona el método (progresiones, periodización, bloques, ejercicios, sets, reps, loading):*
No explicar mecánicamente. El método es experiencial. Responder con el espíritu de "lo sentís cuando llegás" (adaptado al turno conversacional — no es una frase literal a copiar) y re-anclar la clase de prueba como el próximo paso para conocerlo.

Excepción: "¿Qué es el método / qué hacen?" NO dispara esta regla — respondé con la descripción de Metodo (elevator) del conocimiento.
```

Critical constraints:

- Rule lives in the **universal framing** (reaches ALL states, not just leads) because method-internals questions can come from trial/active/inactive/expired contacts too.
- Use tuteo: "sentís", "llegás", "respondé", "re-anclar".
- Do NOT mandate the literal phrase "lo sentís cuando llegás" — it's a tonal anchor, not a script. Mica should paraphrase in context. But the exact tuteo form of the phrase MUST appear in the rule text so it's present in the rendered prompt (needed by tests in 87-03).
- Do NOT put this rule inside `knowledge.ts`.
- Do NOT refactor surrounding framing rules (scope discipline — this is a single-rule addition).

**CHECKPOINT: Review deflection wording with user before commit.** The rule carries the team's voice. After drafting, present the exact text to the user in the chat before writing the file. User confirms wording → write + commit.

Avoid: hedging language that dilutes the deflection ("a veces podés explicar un poco…"), instructions that contradict the elevator pitch ("nunca hables del método" — wrong, elevator IS allowed), English keywords, adding a second CTA style (stick to trial class re-anchor).
</action>
<verify>
<automated>cd el-templo-bot && pnpm tsc --noEmit && pnpm vitest run</automated>
<manual> 1. `grep -c "lo sentís cuando llegás" el-templo-bot/src/ai/system-prompt.ts` returns 1 2. Rendered prompt for PB1.E1A contains the deflection rule text: `node -e "import('./dist/ai/system-prompt.js').then(m => { const p = m.getSystemPrompt({clientState:'lead',activePlaybook:'PB1',currentStage:'E1A'}); console.log('has deflection:', p.includes('lo sentís cuando llegás')); })"` → true 3. Rendered prompt for active_member contains the deflection rule (universal framing): same check with clientState='active_member' → true
</manual>
<sampling_rate>after user approves wording, before Task 3 checkpoint</sampling_rate>
</verify>
<done> - Exactly one occurrence of "lo sentís cuando llegás" in system-prompt.ts - Rule present in rendered prompt for ALL client states (verified lead + one non-lead) - User approved the wording before commit - Full test suite (514+) still passes - Commit: `feat(87-02): add method-internals deflection rule to system prompt framing`
</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: CHECKPOINT — measure KGATE-05 headroom and halt on breach</name>
  <files>el-templo-bot/test/ai/prompt-size.test.ts (read-only verification)</files>
  <action>Run the KGATE-05 prompt-size regression (rendered PB1.E1A ≤ 18,916 chars) and HALT on failure — do NOT auto-trim team content. See <what-built>, <how-to-verify>, <resume-signal> below for the full protocol.</action>
  <verify>pnpm vitest run test/ai/prompt-size.test.ts passes AND user confirms rendered length is within threshold</verify>
  <done>Rendered PB1.E1A ≤ 18,916 chars with prompt-size.test.ts green AND user approved (or user selected remediation path (a)/(b)/(c)/(d) and executor applied it + re-measured to pass)</done>
  <what-built>
    BP consolidation (87-01) + method sections (Task 1) + deflection rule (Task 2) are all in place. About to measure the combined prompt-size delta against the KGATE-05 threshold (≤ 18,916 chars rendered PB1.E1A).
  </what-built>
  <how-to-verify>
Run the measurement:

```bash
cd el-templo-bot
pnpm vitest run test/ai/prompt-size.test.ts
```

Also capture the exact numbers:

```bash
pnpm tsc --noEmit
node --experimental-vm-modules -e "
import('./dist/ai/system-prompt.js').then(m => {
  const rendered = m.getSystemPrompt({clientState:'lead', activePlaybook:'PB1', currentStage:'E1A'});
  const full = m.getSystemPrompt({clientState:'active_member', activePlaybook:'PB1', currentStage:'E1A'});
  console.log('Rendered PB1.E1A (lead):', rendered.length, 'chars');
  console.log('Headroom vs 18,916:', 18916 - rendered.length, 'chars');
  console.log('Full (active_member):', full.length, 'chars');
});
"
```

**Interpretation:**

- **PASS case:** `pnpm vitest run test/ai/prompt-size.test.ts` exits 0, rendered ≤ 18,916. Proceed to plan 87-03 tests. No user input required beyond acknowledgement.

- **FAIL case:** rendered > 18,916 OR prompt-size.test.ts fails. **HALT.** Report to user:
  - Measured rendered length + overshoot in chars
  - Measured elevator length (confirm still ≤ 200)
  - Count of BP pointer-form sites that landed in discovery-tagged sections (these cost tokens for leads)
  - Options for the user to pick:
    - **(a)** Compress elevator below 200 chars — user reviews new text
    - **(b)** Drop `(ver *Reglas Zero*)` from the 2-3 heaviest BP pointer sites in discovery-tagged sections (lead still sees the BP name, just no pointer back — slight loss of discoverability for Mica, not a rule violation)
    - **(c)** Move an existing discovery-tagged section to full-only (last-resort structural change; affects lead prompt content)
    - **(d)** Rebaseline KGATE-05 threshold (requires updating prompt-size.test.ts and documenting rationale; last resort)
  - **Do NOT** auto-trim METHOD_DETAIL, move it to discovery, or paraphrase team content. CONTEXT.md forbids.
  - **Do NOT** silently relax the test threshold.
    </how-to-verify>
    <resume-signal>
    Type "approved" if rendered ≤ 18,916 and all tests green — proceed to plan 87-03.
    If FAIL: user picks option (a), (b), (c), or (d), executor applies, measures again, loops back through this checkpoint.
    </resume-signal>
    </task>

</tasks>

<verification>
Run from `el-templo-bot/`:

```bash
pnpm tsc --noEmit
pnpm vitest run
```

Expected: All 514+ tests pass, zero type errors.

Programmatic content checks:

```bash
grep -c "Metodo (elevator)" el-templo-bot/src/ai/knowledge.ts       # 2 (title + body header)
grep -c "Metodo (detalle)"  el-templo-bot/src/ai/knowledge.ts       # 2 (title + body header)
grep -c "lo sentís cuando llegás" el-templo-bot/src/ai/system-prompt.ts  # 1
```

</verification>

<success_criteria>

- SECTIONS has 16 entries with method sections at positions 2 and 3
- ELEVATOR_TEXT ≤ 200 chars
- METHOD_DETAIL is verbatim team text
- Deflection rule in universal framing, containing "lo sentís cuando llegás"
- Lead prompt includes elevator, excludes detalle
- Non-lead prompts include both
- KGATE-05 regression (rendered ≤ 18,916) passes — OR user-approved remediation applied
- `tsc --noEmit` clean
  </success_criteria>

<output>
After completion, create `.planning/phases/87-boarding-pass-method-description/87-02-SUMMARY.md` capturing:
- Final ELEVATOR_TEXT verbatim + char count
- Final deflection rule verbatim + line numbers in system-prompt.ts
- Measured rendered PB1.E1A size (post-task) and headroom vs 18,916
- Measured knowledge-block-for-lead size and headroom vs `full * 0.65`
- CHECKPOINT outcome (approved-first-pass / remediation applied with option + rationale)
</output>
