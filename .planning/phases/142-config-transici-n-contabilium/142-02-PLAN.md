---
phase: 142-config-transicion-contabilium
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .docs/modulo-contable/TRANSICION-CONTABILIUM.md
  - .docs/modulo-contable/opening-balance-migration-template.sql
autonomous: true
requirements: [MIG-02]
must_haves:
  truths:
    - "An operations doc records 'qué dato manda' during Contabilium coexistence (Admin = ingresos/caja from cutoff; Contabilium = AFIP only, out of scope)"
    - "The doc records the clean-cutoff rule (cajas start at opening_balance, no historical backfill) and that Franco fixes the cutoff date at go-live"
    - "An opening-balance migration TEMPLATE exists OUTSIDE src/db/migrations/ so the runner cannot execute placeholder/zero values on deploy"
  artifacts:
    - path: ".docs/modulo-contable/TRANSICION-CONTABILIUM.md"
      provides: "MIG-02 written transition rule (cutoff + qué dato manda + opening-balance mechanism)"
      contains: "qué dato manda"
    - path: ".docs/modulo-contable/opening-balance-migration-template.sql"
      provides: "Commented opening-balance UPDATE template, filled with real counts at go-live"
      contains: "opening_balance"
  key_links:
    - from: ".docs/modulo-contable/opening-balance-migration-template.sql"
      to: "cash_registers.opening_balance / cutoff_date"
      via: "UPDATE ... SET opening_balance=<CONTEO>, cutoff_date='<YYYY-MM-DD>' WHERE id=<ID> (per caja)"
      pattern: "UPDATE `cash_registers`"
---

<objective>
Write the MIG-02 transition deliverable: a durable operations doc in `.docs/modulo-contable/` that
records the clean-cutoff rule and "qué dato manda" during Contabilium coexistence, plus an
opening-balance migration TEMPLATE kept OUTSIDE `src/db/migrations/` (so the runner can never
execute placeholder/zero values on deploy).

Purpose: Closes MIG-02. Franco fixes the actual cutoff date/strategy at go-live; 142 leaves the
written rule and the fill-in-the-blanks migration template ready. No code, no schema change.

Output: `TRANSICION-CONTABILIUM.md` + `opening-balance-migration-template.sql`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/142-config-transici-n-contabilium/142-CONTEXT.md
@.planning/phases/142-config-transici-n-contabilium/142-RESEARCH.md

<interfaces>
From el-templo-api/src/db/schema/cash-registers.ts:41-45:
  - cash_registers.opening_balance, cash_registers.cutoff_date (per-caja; the MIG-02 template targets these)
  - 138 D-05/D-06: clean cutoff — cutoff_date excludes pre-cutoff transactions from the firm balance; no historical backfill
From BRIEF-MODULO-CONTABLE-FRANCO.md §6 / §6-bis:
  - Contabilium progressive replacement: ingresos/caja first (Admin); AFIP/facturación electrónica last (out of scope)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the transition doc + opening-balance migration template</name>
  <files>.docs/modulo-contable/TRANSICION-CONTABILIUM.md, .docs/modulo-contable/opening-balance-migration-template.sql</files>
  <read_first>
    - .planning/phases/142-config-transici-n-contabilium/142-CONTEXT.md (D-07, D-08, D-09)
    - .planning/phases/142-config-transici-n-contabilium/142-RESEARCH.md (MIG-02 section, Pitfall 4, doc-contents sourcing)
    - el-templo-api/src/db/schema/cash-registers.ts (opening_balance / cutoff_date columns)
    - .planning/phases/138-*/138-SUMMARY.md (138 D-05/D-06 clean cutoff, no backfill)
    - BRIEF-MODULO-CONTABLE-FRANCO.md (§6 Contabilium progressive replacement)
  </read_first>
  <action>
    Create `.docs/modulo-contable/TRANSICION-CONTABILIUM.md` documenting, in Spanish: (a) **corte limpio** (D-07c) — desde el go-live el registro de ingresos y caja vive SOLO en el Administrador; antes del corte = Contabilium/Excel; las cajas arrancan en su saldo de apertura (conteo físico), SIN backfill histórico (ya implementado en 138 D-05/D-06: `cutoff_date` excluye transacciones pre-corte del saldo firme; `opening_balance` es la semilla). (b) **qué dato manda** (D-07b) — Admin = ingresos + caja (fuente de verdad desde el corte); Contabilium = solo facturación AFIP/electrónica (fuera de scope, último escalón del reemplazo). (c) **estrategia de corte** (D-08) — la fecha la define Franco al go-live; 142 NO la fija; recomendación documentada = corte limpio único (todas las sucursales el mismo día) por nitidez, pero escalonado es aceptable (`cutoff_date` es por-caja). (d) **mecanismo de apertura** (D-07d/D-09) — los saldos de apertura se cargan UNA sola vez por migración al go-live con los conteos físicos reales (regla del proyecto: datos de prod por migración, no por seed re-run ni UI); en 142 las cajas siguen en 0/placeholder; referencia al template. Use createLogger conventions are N/A (doc only). Source every claim from the read_first files (no invention).

    Create `.docs/modulo-contable/opening-balance-migration-template.sql` — a COMMENTED placeholder, explicitly NOT a tracked migration (it must NOT live under `src/db/migrations/` so the runner cannot execute it on deploy). Header comment instructs: at go-live, copy to `src/db/migrations/NNNN_load_opening_balances.sql`, replace `<CONTEO>` and `<YYYY-MM-DD>` with Franco's real physical counts + cutoff date, then `db:migrate`. Body = one `UPDATE cash_registers SET opening_balance = <CONTEO>, cutoff_date = '<YYYY-MM-DD>' WHERE id = <ID>;` per caja (efectivo×sucursal, central efectivo, banco ARS, banco EUR), noting `cutoff_date` is per-caja (may differ if escalonado). Migration-rules reminder in comments: NO `;` inside any `--` comment line; commit the filled SQL. Values are integers per app convention.

  </action>
  <verify>
    <automated>test -f .docs/modulo-contable/TRANSICION-CONTABILIUM.md && test -f .docs/modulo-contable/opening-balance-migration-template.sql && ! test -f el-templo-api/src/db/migrations/opening-balance-migration-template.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - TRANSICION-CONTABILIUM.md documents all four D-07 contents (corte limpio, qué dato manda, estrategia de corte, mecanismo de apertura) and that Franco fixes the cutoff date at go-live (D-08)
    - opening-balance-migration-template.sql exists under .docs/modulo-contable/ and is NOT under src/db/migrations/ (runner cannot execute it)
    - The template targets cash_registers.opening_balance / cutoff_date with one UPDATE per caja, per-caja cutoff_date noted
    - No `;` inside any `--` comment line in the template
    - In 142 the cajas remain at 0/placeholder (no real values filled)
  </acceptance_criteria>
  <done>MIG-02 doc + opening-balance template written; template is outside the runner's path; cutoff date deferred to Franco at go-live.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                   |
| ----------------------------------- | ----------------------------------------------------------------------------- |
| go-live operator → migration runner | A filled opening-balance migration will write real balances to cash_registers |

## STRIDE Threat Register

| Threat ID | Category  | Component                          | Disposition         | Mitigation Plan                                                                                                                                                                                                                                                              |
| --------- | --------- | ---------------------------------- | ------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-142-05  | Tampering | opening-balance migration template | mitigate            | Template lives in `.docs/modulo-contable/`, NOT `src/db/migrations/` — the runner cannot execute placeholder/zero values on deploy and stomp balances or set a wrong cutoff. It is copied + filled only at go-live. Verify command asserts absence under src/db/migrations/. |
| T-142-06  | Tampering | filled migration `;` in comments   | accept (documented) | Template comments warn: no `;` inside `--` comment lines (the runner splits on `;` before stripping comments). The actual filled migration is authored by Franco/ops at go-live, outside this phase.                                                                         |
| T-142-SC  | Tampering | npm/pip/cargo installs             | mitigate            | No packages installed (doc-only plan). N/A.                                                                                                                                                                                                                                  |

</threat_model>

<verification>
- Both files exist under `.docs/modulo-contable/`.
- The template is NOT under `src/db/migrations/` (runner cannot execute it).
- The doc covers cutoff + qué dato manda + opening-balance mechanism, with the cutoff date deferred to Franco.
</verification>

<success_criteria>

- MIG-02 deliverable complete: written transition rule + ready-to-fill opening-balance migration template, with cajas left at 0 in 142.
  </success_criteria>

<output>
Create `.planning/phases/142-config-transici-n-contabilium/142-02-SUMMARY.md` when done.
</output>
