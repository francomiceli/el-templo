---
phase: 142-config-transicion-contabilium
plan: 03
type: execute
wave: 2
depends_on: ["142-01"]
files_modified:
  - el-templo-admin/src/composables/useFinanceConfigApi.ts
  - el-templo-admin/src/pages/ConfiguracionCajaPage.vue
  - el-templo-admin/src/router/routes.ts
  - el-templo-admin/src/layouts/AdminLayout.vue
autonomous: true
requirements: [MIG-01]
must_haves:
  truths:
    - "Owner/admin can open a 'Configuración de Caja' page, see the current threshold, edit it, and save"
    - "The page loads the current threshold via GET on mount and persists via PUT on save"
    - "The route and nav item are gated to owner/admin (isAdminRole); coach/gestion/recepcion cannot reach it"
    - "Validation: integer minimum 1; invalid input blocks submit; save shows success/error feedback"
  artifacts:
    - path: "el-templo-admin/src/pages/ConfiguracionCajaPage.vue"
      provides: "Single-field config form (umbral de pendientes) per 142-UI-SPEC"
      contains: "Umbral de pendientes"
    - path: "el-templo-admin/src/composables/useFinanceConfigApi.ts"
      provides: "getThreshold/setThreshold composable with loading/saving/error + cleanup()"
      contains: "cleanup"
    - path: "el-templo-admin/src/router/routes.ts"
      provides: "/configuracion-caja route, allowedRoles ['admin','owner']"
      contains: "configuracion-caja"
  key_links:
    - from: "el-templo-admin/src/pages/ConfiguracionCajaPage.vue"
      to: "useFinanceConfigApi"
      via: "getThreshold() on mount, setThreshold() on submit"
      pattern: "useFinanceConfigApi"
    - from: "el-templo-admin/src/composables/useFinanceConfigApi.ts"
      to: "GET/PUT /admin/finance/config/overdue-threshold"
      via: "api (boot/axios)"
      pattern: "config/overdue-threshold"
---

<objective>
Build the minimal "Configuración de Caja" admin page (MIG-01, D-06) per `142-UI-SPEC`: one numeric
field (umbral de pendientes, default 3, min 1), Save, loading/saving/success/error states. Add the
composable, the owner/admin-gated route, and the nav item.

Purpose: Gives MIG-01's config house a UI surface — a single defined, extensible place for module
knobs. Backend (plan 01) already exposes GET/PUT; this plan wires the form to it.

Output: `ConfiguracionCajaPage.vue`, `useFinanceConfigApi.ts`, route + nav entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/142-config-transici-n-contabilium/142-CONTEXT.md
@.planning/phases/142-config-transici-n-contabilium/142-RESEARCH.md
@.planning/phases/142-config-transici-n-contabilium/142-UI-SPEC.md

<interfaces>
From plan 142-01 (backend, the API this page consumes):
  - The finance plugin is registered with prefix `/api/admin/finance` (app.ts:218-219); the admin
    axios baseURL already includes `/api` (boot/axios.ts) → the composable path must start at
    `/admin/finance/...` (mirror useTransactionsApi.ts which uses `/admin/finance/transactions`).
  - GET  /admin/finance/config/overdue-threshold → 200 { thresholdDays: number }  (owner/admin only)
  - PUT  /admin/finance/config/overdue-threshold body { thresholdDays: number } → 200 { thresholdDays }  (owner/admin; 400 if not 1..365)

From el-templo-admin/src/composables/useFinanceLoadApi.ts (composable template to mirror):

- loading/error refs, `api` from boot/axios, extractError, cleanup() method, NO onUnmounted inside composable

From el-templo-admin/src/pages/PuntuacionesPage.vue (gated-page template, newest):

- q-page q-pa-md, text-h5 header, owner-gated page shell

From el-templo-admin/src/router/routes.ts:

- PuntuacionesPage route pattern; meta: { allowedRoles: [...] as AdminRole[] }; e.g. caja route at :70-72 (gestion/admin/owner)

From el-templo-admin/src/layouts/AdminLayout.vue:

- isAdminRole computed = ['admin','owner']; section headers "Gestion"/"Administracion"; <q-item v-if="..." to="...">
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: useFinanceConfigApi composable + ConfiguracionCajaPage</name>
  <files>el-templo-admin/src/composables/useFinanceConfigApi.ts, el-templo-admin/src/pages/ConfiguracionCajaPage.vue</files>
  <read_first>
    - el-templo-admin/src/composables/useFinanceLoadApi.ts (composable conventions: loading/error refs, api, extractError, cleanup(), no onUnmounted, no any, no console)
    - el-templo-admin/src/pages/PuntuacionesPage.vue (gated-page shell template)
    - el-templo-admin/src/pages/CajaPage.vue (q-page q-pa-md + text-h5 header pattern)
    - el-templo-admin/src/components/caja/SaldosPorCajaTab.vue (q-card flat bordered + q-card-section + save btn pattern)
    - el-templo-admin/src/utils/logger.ts (createLogger)
    - .planning/phases/142-config-transici-n-contabilium/142-UI-SPEC.md (the full one-field contract: layout, states, copy, validation, accessibility)
    - .planning/phases/142-config-transici-n-contabilium/142-RESEARCH.md (Pattern 4)
  </read_first>
  <action>
    Create `useFinanceConfigApi.ts` mirroring `useFinanceLoadApi.ts` exactly: `loading` + `saving` + `error` refs, `api` from `boot/axios`, an `extractError` helper, a typed `CajaConfig`/threshold response interface (NO `any`), `getThreshold(): Promise<number>` (GET `/admin/finance/config/overdue-threshold`), `setThreshold(days: number): Promise<void>` (PUT same path with `{ thresholdDays }`) — use the FULL path `/admin/finance/config/overdue-threshold` (the finance plugin is mounted at `/api/admin/finance` and the admin axios baseURL already includes `/api`; `/finance/config/...` would 404). Mirror `useTransactionsApi.ts`, `createLogger('useFinanceConfigApi')` for error logging (never console.*), and a `cleanup()` method. NO `onUnmounted` inside the composable (CLAUDE.md rule) — the page calls cleanup() from its own onUnmounted.

    Create `ConfiguracionCajaPage.vue` per 142-UI-SPEC: `q-page q-pa-md` (cream bg) with a `text-h5` "Configuración de Caja" header row, then a `q-card flat bordered` (max-width ~480px) holding a `q-form @submit.prevent`. One control: `q-input v-model.number type="number" min="1"` label "Umbral de pendientes (días)", outlined dense, `:loading` while reading, `:rules` for required + integer + min 1 (error copy "Debe ser un número entero mayor o igual a 1." / "Ingresá un valor."), persistent `hint` "Días sin validar tras los que un pendiente dispara la alerta de la bandeja de Caja." Below: `q-btn type="submit" label="Guardar" color="primary" :loading="saving" :disable="loading"`. On mount call `getThreshold()` to populate the field; on submit call `setThreshold()` then `$q.notify({ type: 'positive', message: 'Configuración guardada' })`; on error `$q.notify({ type: 'negative', message: 'No se pudo guardar. Reintentá.' })`. Call `cleanup()` from the page's `onUnmounted`. Warm palette only (color="primary" terracotta, $positive/$negative notifies) — NO blue. Do NOT invent extra fields, sections, reset button, or upper-bound UI rule (142-UI-SPEC: single field only).

  </action>
  <verify>
    <automated>cd el-templo-admin && npx tsc --noEmit 2>/dev/null || npx vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - useFinanceConfigApi exposes getThreshold/setThreshold + loading/saving/error + cleanup(), no onUnmounted inside, no any, no console.* (createLogger)
    - The composable calls the FULL path `/admin/finance/config/overdue-threshold` for both GET and PUT (NOT `/finance/config/...`, which would 404 — the plugin is mounted at `/api/admin/finance` and the admin baseURL already includes `/api`)
    - ConfiguracionCajaPage is a single numeric field per 142-UI-SPEC — no invented fields/sections/reset/upper-bound
    - Page loads current value on mount and saves via PUT with positive/negative notify feedback
    - Validation: integer min 1 blocks submit (no request); page calls cleanup() on unmount
    - Warm palette only (no blue); typecheck/vue-tsc passes
  </acceptance_criteria>
  <done>Composable + single-field config page wired to the backend endpoints, honoring 142-UI-SPEC and CLAUDE.md composable/logger/no-any rules.</done>
</task>

<task type="auto">
  <name>Task 2: Route + nav entry (owner/admin gated)</name>
  <files>el-templo-admin/src/router/routes.ts, el-templo-admin/src/layouts/AdminLayout.vue</files>
  <read_first>
    - el-templo-admin/src/router/routes.ts (PuntuacionesPage route :99/:109 with allowedRoles ['admin','owner']; caja route :70-72)
    - el-templo-admin/src/layouts/AdminLayout.vue (isAdminRole computed, section headers "Gestion"/"Administracion", q-item nav pattern)
    - .planning/phases/142-config-transici-n-contabilium/142-UI-SPEC.md (Route & Nav placement, Assumption A1/A2)
  </read_first>
  <action>
    Add a route in `routes.ts`: `path: 'configuracion-caja'`, `component: () => import('pages/ConfiguracionCajaPage.vue')`, `meta: { allowedRoles: ['admin', 'owner'] as AdminRole[] }` (owner/admin only — D-06; excludes gestion/recepcion/coach). Mirror the PuntuacionesPage route entry.

    Add a nav `q-item` in `AdminLayout.vue` gated by `v-if="isAdminRole"` (= ['admin','owner']) under the existing **Administracion** section header (groups owner/admin management screens; matches the gate — 142-UI-SPEC A1). Label "Configuración de Caja", `to="/configuracion-caja"`, icon `settings`. If the Administracion section header is absent, place it adjacent to the Caja item gated by isAdminRole (A1 allows either; the gate is the locked part).

  </action>
  <verify>
    <automated>cd el-templo-admin && npx tsc --noEmit 2>/dev/null || npx vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - /configuracion-caja route exists with allowedRoles ['admin','owner'] (gestion/recepcion/coach excluded)
    - Nav q-item "Configuración de Caja" gated by isAdminRole, to="/configuracion-caja", icon settings
    - typecheck/vue-tsc passes
  </acceptance_criteria>
  <done>Owner/admin-only route + nav entry reach the config page.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                            |
| --------------------------------- | -------------------------------------------------------------------------------------- |
| admin browser → config page/route | Client-side route guard + nav gating (defense in depth; the API is the real authority) |

## STRIDE Threat Register

| Threat ID | Category               | Component                       | Disposition | Mitigation Plan                                                                                                                                                                                                                                    |
| --------- | ---------------------- | ------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-142-07  | Elevation of Privilege | /configuracion-caja route + nav | mitigate    | Route `meta.allowedRoles ['admin','owner']` + nav `v-if="isAdminRole"`. Client gating is convenience; the backend per-handler ADMIN_ROLES check (plan 01) is the authoritative gate — gestion/recepcion/coach get 403 even if they reach the page. |
| T-142-08  | Tampering              | client threshold input          | mitigate    | UI `:rules` (integer, min 1) block bad submits; the server re-validates 1..365 (plan 01). Client validation is UX, not security.                                                                                                                   |
| T-142-SC  | Tampering              | npm/pip/cargo installs          | mitigate    | No packages installed (reuses existing Quasar/axios). N/A.                                                                                                                                                                                         |

</threat_model>

<verification>
- `npx tsc --noEmit` (or `vue-tsc --noEmit`) passes in el-templo-admin.
- Page renders a single field per 142-UI-SPEC; loads current value, saves with notify feedback.
- Route + nav gated owner/admin; warm palette (no blue); composable uses cleanup() not onUnmounted.
- Manual UAT by Franco (no admin unit-test harness in scope).
</verification>

<success_criteria>

- MIG-01 UI complete: a single-field, owner/admin-gated "Configuración de Caja" page wired to the backend, honoring 142-UI-SPEC and CLAUDE.md frontend conventions.
  </success_criteria>

<output>
Create `.planning/phases/142-config-transici-n-contabilium/142-03-SUMMARY.md` when done.
</output>
