---
phase: 142-config-transicion-contabilium
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/migrations/0157_seed_finance_overdue_threshold.sql
  - el-templo-api/src/modules/finance/config-service.ts
  - el-templo-api/src/modules/finance/constants.ts
  - el-templo-api/src/modules/finance/transaction-service.ts
  - el-templo-api/src/modules/finance/routes.ts
  - el-templo-api/src/modules/finance/schemas.ts
  - el-templo-api/src/modules/finance/types.ts
  - el-templo-api/src/modules/finance/index.ts
  - el-templo-api/test/finance-config.test.ts
autonomous: true
requirements: [MIG-01]
must_haves:
  truths:
    - "Owner/admin can read the current pending-overdue threshold via GET config endpoint"
    - "Owner/admin can set the threshold via PUT config endpoint; gestion/recepcion/coach get 403"
    - "Setting the threshold to N flows to /pending-tray: thresholdDays=N and isOverdue reflects N"
    - "When the setting row is absent, reads fall back to the canonical default (3)"
    - "PUT with an out-of-range value (negative, 0, >365, non-integer) returns 400"
  artifacts:
    - path: "el-templo-api/src/modules/finance/config-service.ts"
      provides: "FinanceConfigService with getOverdueThreshold/setOverdueThreshold (read-with-fallback + upsert)"
      contains: "getOverdueThreshold"
    - path: "el-templo-api/src/db/migrations/0157_seed_finance_overdue_threshold.sql"
      provides: "Idempotent data-only seed of finance.pending_overdue_days=3"
      contains: "finance.pending_overdue_days"
    - path: "el-templo-api/test/finance-config.test.ts"
      provides: "Integration coverage: get/set, RBAC, fallback, dynamic-threshold, validation"
      contains: "pending-tray"
  key_links:
    - from: "el-templo-api/src/modules/finance/transaction-service.ts::listPendingTray"
      to: "FinanceConfigService.getOverdueThreshold()"
      via: "awaited read once before .map(), used in isOverdue and thresholdDays"
      pattern: "getOverdueThreshold"
    - from: "el-templo-api/src/modules/finance/routes.ts config handlers"
      to: "ADMIN_ROLES"
      via: "per-handler owner/admin check (excludes gestion in FINANCE_READ_ROLES)"
      pattern: "ADMIN_ROLES"
---

<objective>
Build the backend config house for MIG-01: reuse `system_settings` (no new table) to store the
pending-overdue threshold (`finance.pending_overdue_days`, default 3), expose owner/admin-only
GET/PUT config endpoints in the finance plugin, wire the threshold into both seam sites of
`listPendingTray`, and seed the default via migration 0157.

Purpose: The 141 bandeja-de-pendientes overdue threshold becomes admin-configurable instead of a
hard-coded literal — without touching any 141 UI (the bandeja already reads `thresholdDays` from
the response). This is the only behavioral change of the phase; the rest is wiring + a seed.

Output: `FinanceConfigService`, two config endpoints, the two seam-site edits, migration 0157,
and an integration test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-config-transici-n-contabilium/142-CONTEXT.md
@.planning/phases/142-config-transici-n-contabilium/142-RESEARCH.md

<interfaces>
<!-- Read-with-fallback exemplar to mirror EXACTLY (parseInt + NaN/undefined guard). -->
From el-templo-api/src/modules/streaks/service.ts:273-317 (getStreakMilestoneConfig):
  - select { settingKey, settingValue } from systemSettings where settingKey in keys
  - build Map(settingKey -> settingValue)
  - parseOrDefault(key, defaultVal): if val===undefined return default; parsed=parseInt(val,10); return isNaN(parsed)?default:parsed

From el-templo-api/src/db/schema/system-settings.ts:

- systemSettings table: settingKey (unique), settingValue (text)

From el-templo-api/src/modules/finance/constants.ts:16:

- export const OVERDUE_DAYS = 3; // keep as the canonical fallback default

From el-templo-api/src/modules/finance/transaction-service.ts:

- constructor(db, log, balanceService, cashRegisterService) at :33
- listPendingTray() seam sites: :1162 `isOverdue: ageInDays > OVERDUE_DAYS`, :1166 `return { rows, total, page, limit, thresholdDays: OVERDUE_DAYS }`

From el-templo-api/src/modules/finance/routes.ts:

- financeRoutes plugin; transactionService built at :75 with (db, log, balanceService, cashRegisterService)
- module-level onRequest guard at :189 uses FINANCE_READ_ROLES (owner/admin/gestion/recepcion)

From el-templo-api/src/modules/shared/permissions.ts:

- ADMIN_ROLES = ["admin","owner"] (:23)
- FINANCE_READ_ROLES = ["owner","admin","gestion","recepcion"] (:126) — INCLUDES gestion
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FinanceConfigService + constants + seam wiring + migration 0157</name>
  <files>el-templo-api/src/modules/finance/config-service.ts, el-templo-api/src/modules/finance/constants.ts, el-templo-api/src/modules/finance/transaction-service.ts, el-templo-api/src/modules/finance/index.ts, el-templo-api/src/db/migrations/0157_seed_finance_overdue_threshold.sql</files>
  <read_first>
    - el-templo-api/src/modules/streaks/service.ts (getStreakMilestoneConfig, the exact pattern to mirror)
    - el-templo-api/src/modules/finance/constants.ts (OVERDUE_DAYS = 3)
    - el-templo-api/src/modules/finance/transaction-service.ts (constructor :33, listPendingTray :1150-1167 seam sites)
    - el-templo-api/src/modules/finance/routes.ts (transactionService construction :75)
    - el-templo-api/src/db/schema/system-settings.ts (table shape)
    - .planning/phases/142-config-transici-n-contabilium/142-CONTEXT.md (D-04, D-05)
    - .planning/phases/142-config-transici-n-contabilium/142-RESEARCH.md (Patterns 1 and 2, Pitfalls 1 and 3)
  </read_first>
  <behavior>
    - getOverdueThreshold(): returns parsed int from system_settings key finance.pending_overdue_days
    - getOverdueThreshold() with absent row → returns OVERDUE_DAYS (3)
    - getOverdueThreshold() with non-numeric stored value → returns OVERDUE_DAYS (3)
    - setOverdueThreshold(n): upserts the key (insert ... onDuplicateKeyUpdate), value = String(n)
    - listPendingTray: with threshold 5 and a 4-day-old pendiente → isOverdue=false, thresholdDays=5
    - listPendingTray: with threshold 5 and a 6-day-old pendiente → isOverdue=true, thresholdDays=5
  </behavior>
  <action>
    Create `config-service.ts` exporting `FINANCE_SETTINGS_KEYS = { PENDING_OVERDUE_DAYS: "finance.pending_overdue_days" }` and a `FinanceConfigService` class constructed with (db, log) mirroring the streaks DI shape. Implement `getOverdueThreshold(): Promise<number>` that selects `settingValue` for key `finance.pending_overdue_days`, and returns `OVERDUE_DAYS` (imported from constants.ts) when the row is absent OR `parseInt` yields NaN — replicate BOTH guards from `getStreakMilestoneConfig`'s `parseOrDefault`. Implement `setOverdueThreshold(days: number): Promise<void>` as a Drizzle `.insert(systemSettings).values({ settingKey, settingValue: String(days) }).onDuplicateKeyUpdate({ set: { settingValue: String(days) } })` — last-write-wins on the unique settingKey, no SELECT-then-write. Keep `OVERDUE_DAYS` in constants.ts as the canonical default (do not delete it).

    Wire the seam (D-05): add `FinanceConfigService` as a 5th constructor param to `TransactionService` (after cashRegisterService — same DI style as balanceService/cashRegisterService), store on `this.financeConfig`. In `listPendingTray`, read `const threshold = await this.financeConfig.getOverdueThreshold();` ONCE before the `.map()`, then swap BOTH seam sites: `:1162` `isOverdue: ageInDays > threshold` and `:1166` `thresholdDays: threshold`. Update the construction in routes.ts so the new param is passed (instantiate `new FinanceConfigService(fastify.db, fastify.log)` and pass it). Export `FinanceConfigService` and `FINANCE_SETTINGS_KEYS` from `index.ts`.

    Hand-write migration `0157_seed_finance_overdue_threshold.sql` (data-only seed, default 3) using a guarded `INSERT ... SELECT ... WHERE NOT EXISTS` so it is idempotent. NO `;` inside any `--` comment line (the runner splits on `;` before stripping comments). Do NOT run `db:generate` (data-only; the sessions.goal_plan_type drift would block it anyway). Use no `any` types; type narrowing with `unknown` where needed.

  </action>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - FinanceConfigService.getOverdueThreshold reads finance.pending_overdue_days and falls back to OVERDUE_DAYS on absent/NaN
    - setOverdueThreshold uses onDuplicateKeyUpdate (no SELECT-then-write)
    - listPendingTray reads the threshold ONCE and uses it in BOTH :1162 (isOverdue) and :1166 (thresholdDays); OVERDUE_DAYS literal no longer appears at those two sites
    - OVERDUE_DAYS still exported from constants.ts as the fallback default
    - Migration 0157 exists, is data-only, idempotent, and has NO `;` inside any `--` comment line
    - `npx tsc --noEmit` passes; no `any` types introduced
  </acceptance_criteria>
  <done>Config service reads/writes the threshold with fallback; both seam sites swapped; migration 0157 hand-written and committed; typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GET/PUT config endpoints (owner/admin) + schemas/types + integration tests</name>
  <files>el-templo-api/src/modules/finance/routes.ts, el-templo-api/src/modules/finance/schemas.ts, el-templo-api/src/modules/finance/types.ts, el-templo-api/test/finance-config.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/finance/routes.ts (module guard :189, existing per-handler ADMIN_ROLES/write guards for the belt-and-suspenders pattern)
    - el-templo-api/src/modules/finance/schemas.ts (existing schema conventions)
    - el-templo-api/src/modules/finance/types.ts (existing request/response type conventions)
    - el-templo-api/src/modules/shared/permissions.ts (ADMIN_ROLES :23, FINANCE_READ_ROLES :126 — the gestion trap)
    - el-templo-api/test/helpers.ts (auth/request utilities, role tokens)
    - .planning/phases/142-config-transici-n-contabilium/142-RESEARCH.md (Pattern 3, Pitfalls 2 and 5, Validation Architecture, Security Domain)
  </read_first>
  <behavior>
    - GET /config/overdue-threshold as owner/admin → 200 { thresholdDays: <current> }
    - GET /config/overdue-threshold as gestion → 403 (FINANCE_READ_ROLES trap: passes module guard, fails per-handler ADMIN_ROLES)
    - PUT /config/overdue-threshold as owner with { thresholdDays: 5 } → 200; subsequent GET returns 5
    - PUT as gestion → 403; as coach/recepcion → 403
    - PUT with thresholdDays = 0, negative, > 365, or non-integer → 400 (no write)
    - Dynamic flow: PUT 5 → GET /pending-tray returns thresholdDays=5 and isOverdue reflects 5 (seed a pendiente of known age)
    - Absent setting: delete the row → GET /pending-tray (or GET config) reflects fallback 3
  </behavior>
  <action>
    Add two endpoints to the `financeRoutes` plugin: `GET /config/overdue-threshold` and `PUT /config/overdue-threshold`. Instantiate `FinanceConfigService` once at the top of the plugin (reuse the instance built for the TransactionService param in Task 1 if convenient). Each handler does a per-handler `ADMIN_ROLES` check FIRST: if `!(ADMIN_ROLES as readonly string[]).includes(request.user.role)` → `reply.code(403).send({ error: "Acceso denegado", message: "Solo owner/admin" })`. This is what excludes `gestion` (which passes the module-level FINANCE_READ_ROLES guard) — mirror the existing per-handler write/void guards in the plugin. GET returns `{ thresholdDays: await financeConfig.getOverdueThreshold() }`. PUT takes a typed body `{ thresholdDays: number }`, validates via a Fastify JSON schema (integer, minimum 1, maximum 365) so out-of-range yields 400, then calls `setOverdueThreshold` and returns `{ thresholdDays }`.

    Add the GET/PUT schemas to `schemas.ts` (PUT body: type integer, minimum 1, maximum 365) and the request/response types to `types.ts` (no `any`). Do NOT add country/branch scoping — config is global (D-06).

    Create `el-templo-api/test/finance-config.test.ts` using `test/helpers.ts` auth utilities. Cover: GET as owner/admin → 200 with current value; GET/PUT as gestion → 403; PUT as coach/recepcion → 403; PUT valid as owner → 200 and GET reflects it; PUT 0/negative/>365/non-integer → 400; the dynamic-threshold flow (seed a pendiente tx of known age, PUT a threshold, assert /pending-tray thresholdDays and isOverdue reflect it); absent-setting fallback to 3 (delete the row, assert read returns 3). Use Pino logger conventions — no console.*.

  </action>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - GET and PUT /config/overdue-threshold both 403 for gestion (the FINANCE_READ_ROLES trap closed) and for coach/recepcion
    - owner/admin get 200; PUT then GET round-trips the value
    - PUT validates 1..365 integer → 400 on violation, no write performed
    - finance-config.test.ts asserts the dynamic-threshold flow into /pending-tray (thresholdDays AND isOverdue) and the absent-setting fallback to 3
    - `npx tsc --noEmit` passes; no `any`; no console.* (Pino logger only)
  </acceptance_criteria>
  <done>Owner/admin-only config endpoints exist with bounds validation; gestion excluded; integration tests cover RBAC, validation, fallback, and dynamic-threshold flow. (Tests run in CI on push, not locally.)</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                |
| --------------------------------------- | -------------------------------------------------------------------------- |
| admin client → finance config endpoints | Authenticated staff request crosses into a global config write             |
| config endpoint → system_settings       | Untrusted threshold value persisted and later drives server-side isOverdue |

## STRIDE Threat Register

| Threat ID | Category               | Component                         | Disposition | Mitigation Plan                                                                                                                                                                                            |
| --------- | ---------------------- | --------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-142-01  | Elevation of Privilege | GET/PUT /config/overdue-threshold | mitigate    | Per-handler `ADMIN_ROLES` check FIRST in both handlers; module guard FINANCE_READ_ROLES includes gestion/recepcion, so the per-handler check is the real gate. Tests assert gestion/coach/recepcion → 403. |
| T-142-02  | Tampering / DoS-lite   | PUT body thresholdDays            | mitigate    | Fastify JSON schema: integer, minimum 1, maximum 365 → 400 on violation. A negative/absurd value would poison the global overdue counter for all sucursales.                                               |
| T-142-03  | Tampering              | setOverdueThreshold write         | mitigate    | Drizzle parameterized insert + `.onDuplicateKeyUpdate` on the unique setting_key — never string-concatenate; race-safe last-write-wins, no duplicate rows.                                                 |
| T-142-04  | Information Disclosure | config GET                        | accept      | Returns only a non-sensitive integer threshold to owner/admin; no PII.                                                                                                                                     |
| T-142-SC  | Tampering              | npm/pip/cargo installs            | mitigate    | No packages installed in this phase (all in-repo reuse). N/A — no install tasks.                                                                                                                           |

</threat_model>

<verification>
- `npx tsc --noEmit` passes in el-templo-api.
- Migration 0157 is data-only, idempotent, no `;` in `--` comments, committed alongside the change.
- Both seam sites in listPendingTray use the dynamic threshold; the OVERDUE_DAYS literal no longer appears at :1162/:1166.
- finance-config.test.ts covers get/set, RBAC (gestion/coach/recepcion 403), 1..365 validation, fallback to 3, and the dynamic-threshold flow to /pending-tray. (Suite runs in CI on push to staging — not run locally.)
</verification>

<success_criteria>

- MIG-01 backend complete: threshold stored in system_settings, owner/admin-gated GET/PUT, both seam sites dynamic, default seeded by 0157.
- Zero 141 UI changes (the bandeja consumes thresholdDays from the response).
- No new table, no resurrected settings module.
  </success_criteria>

<output>
Create `.planning/phases/142-config-transici-n-contabilium/142-01-SUMMARY.md` when done.
</output>
