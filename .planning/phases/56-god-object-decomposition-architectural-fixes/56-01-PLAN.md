---
phase: 56-god-object-decomposition-architectural-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-admin/src/pages/HorariosPage.vue
  - el-templo-admin/src/components/scheduling/SlotDetailDialog.vue
  - el-templo-admin/src/components/scheduling/ActivitiesDialog.vue
  - el-templo-admin/src/components/scheduling/HolidaysDialog.vue
autonomous: true
requirements: []

must_haves:
  truths:
    - "HorariosPage.vue is under 500 LOC after extraction"
    - "All 3 dialogs (slot detail, activities, holidays) still open and function identically"
    - "Weekly grid still renders and navigates weeks correctly"
  artifacts:
    - path: "el-templo-admin/src/components/scheduling/SlotDetailDialog.vue"
      provides: "Slot detail dialog with booking management"
    - path: "el-templo-admin/src/components/scheduling/ActivitiesDialog.vue"
      provides: "Activities CRUD dialog"
    - path: "el-templo-admin/src/components/scheduling/HolidaysDialog.vue"
      provides: "Holidays management dialog"
    - path: "el-templo-admin/src/pages/HorariosPage.vue"
      provides: "Slim page with grid + 3 dialog component imports"
  key_links:
    - from: "el-templo-admin/src/pages/HorariosPage.vue"
      to: "el-templo-admin/src/components/scheduling/SlotDetailDialog.vue"
      via: "v-model:show + props (scheduleId, date, branchId)"
      pattern: "v-model.*show"
    - from: "el-templo-admin/src/pages/HorariosPage.vue"
      to: "el-templo-admin/src/components/scheduling/ActivitiesDialog.vue"
      via: "v-model:show"
      pattern: "v-model.*show"
    - from: "el-templo-admin/src/pages/HorariosPage.vue"
      to: "el-templo-admin/src/components/scheduling/HolidaysDialog.vue"
      via: "v-model:show + emit bookings-changed for grid refresh"
      pattern: "v-model.*show"
---

<objective>
Extract 3 dialog components from HorariosPage.vue (1082 LOC) to reduce it to ~400 LOC.

Purpose: HorariosPage currently owns the weekly grid, slot detail dialog, activities dialog, and holidays dialog all in one file. Each dialog is self-contained with its own state, API calls, and template — ideal extraction targets.

Output: 3 new dialog components + slimmed HorariosPage that imports them.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@el-templo-admin/src/pages/HorariosPage.vue
@el-templo-admin/src/composables/useSchedulingApi.ts
@el-templo-admin/src/composables/useMembersApi.ts
@el-templo-admin/src/types/scheduling.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract 3 dialog components from HorariosPage</name>
  <files>
    el-templo-admin/src/components/scheduling/SlotDetailDialog.vue
    el-templo-admin/src/components/scheduling/ActivitiesDialog.vue
    el-templo-admin/src/components/scheduling/HolidaysDialog.vue
    el-templo-admin/src/pages/HorariosPage.vue
  </files>
  <action>
Extract each dialog into its own component. Each dialog component should:
- Accept `v-model:show` (boolean) for dialog visibility
- Own its internal state and API calls (move the related refs, functions, watchers)
- Emit events back to the parent when the parent needs to react (e.g., `@bookings-changed` or `@holidays-changed` to trigger grid refresh)

**SlotDetailDialog.vue** — Extract lines 122-218 (template) + lines 431-437 (slot detail state) + lines 438-442 (member search state) + lines 514-527 (activeBookings/slotDetailBookingCount/isSlotPast/slotDetailDayLabel/slotDetailDate computed) + lines 621-627 (booking status helpers) + lines 665-677 (loadSlotDetail) + lines 741-830 (cell click, booking management). Props: `scheduleId: number | null`, `date: string`, `show: boolean`. Emits: `update:show`, `bookings-changed`.

**ActivitiesDialog.vue** — Extract lines 224-290 (template) + lines 445-449 (activities state) + lines 679-689 (loadActivities) + lines 836-890 (activities management) + watcher line 951-956. Props: `show: boolean`. Emits: `update:show`.

**HolidaysDialog.vue** — Extract lines 295-383 (template) + lines 451-465 (holidays state + countryOptions) + lines 691-705 (loadHolidays) + lines 896-938 (holidays management). Props: `show: boolean`. Emits: `update:show`, `holidays-changed`.

**HorariosPage.vue** — Remove all extracted code. Import the 3 components. Replace dialog templates with:

```vue
<SlotDetailDialog
  v-model:show="showSlotDialog"
  :schedule-id="selectedSlotScheduleId"
  :date="selectedSlotDate"
  @bookings-changed="loadWeeklyGrid"
/>
<ActivitiesDialog v-model:show="showActivitiesDialog" />
<HolidaysDialog
  v-model:show="showHolidaysDialog"
  @holidays-changed="loadWeeklyGrid"
/>
```

Keep in HorariosPage: branch state, week navigation, grid data/rendering, cell click handler (which sets selectedSlotScheduleId/selectedSlotDate and opens dialog), the grid CSS. The page should be ~350-400 LOC after extraction.

Each extracted dialog creates its own composable instances (useSchedulingApi, useMembersApi) — these are lightweight wrappers around fetch, not singletons.

Use `createLogger` from `src/utils/logger` in each new component. Never `console.log`.
</action>
<verify>
<automated>cd /home/franco/projects/el-templo/el-templo-admin && npx vue-tsc --noEmit 2>&1 | head -30</automated>
</verify>
<done> - HorariosPage.vue is under 500 LOC - 3 new dialog components exist in components/scheduling/ - TypeScript compilation passes with no errors - All dialog functionality preserved (same props/events contract)
</done>
</task>

</tasks>

<verification>
- `cd el-templo-admin && npx vue-tsc --noEmit` — no type errors
- `wc -l el-templo-admin/src/pages/HorariosPage.vue` — under 500 lines
- All 3 dialog component files exist and are non-empty
</verification>

<success_criteria>
HorariosPage reduced from 1082 LOC to under 500 LOC. Three focused dialog components created. TypeScript compiles cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/56-god-object-decomposition-architectural-fixes/56-01-SUMMARY.md`
</output>
