---
phase: 56-god-object-decomposition-architectural-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-admin/src/pages/AnaliticasPage.vue
  - el-templo-admin/src/components/analytics/MiembrosTab.vue
  - el-templo-admin/src/components/analytics/AsistenciaTab.vue
  - el-templo-admin/src/components/analytics/FinanzasTab.vue
  - el-templo-admin/src/utils/chart-colors.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "AnaliticasPage.vue is under 500 LOC after extraction"
    - "All 3 tabs (miembros, asistencia, finanzas) render charts and data identically"
    - "Lazy loading per tab still works (data fetches only on tab activation)"
    - "KPI cards remain in the parent page (shared across all tabs)"
  artifacts:
    - path: "el-templo-admin/src/components/analytics/MiembrosTab.vue"
      provides: "Member analytics: stat cards, new vs churned chart, plan distribution donut, attention list"
    - path: "el-templo-admin/src/components/analytics/AsistenciaTab.vue"
      provides: "Attendance analytics: daily checkins chart, no-show rate, heatmap, slot occupancy table"
    - path: "el-templo-admin/src/components/analytics/FinanzasTab.vue"
      provides: "Financial analytics: revenue trend chart, revenue by method/branch, outstanding/collection"
    - path: "el-templo-admin/src/pages/AnaliticasPage.vue"
      provides: "Slim page: header, filters, KPI cards, tab container with 3 tab components"
    - path: "el-templo-admin/src/utils/chart-colors.ts"
      provides: "Shared COLORS and chartColors constants used by all 3 tab components"
  key_links:
    - from: "el-templo-admin/src/pages/AnaliticasPage.vue"
      to: "el-templo-admin/src/components/analytics/MiembrosTab.vue"
      via: "props (filters, loading, data)"
      pattern: ":filters.*:data"
    - from: "el-templo-admin/src/pages/AnaliticasPage.vue"
      to: "el-templo-admin/src/components/analytics/AsistenciaTab.vue"
      via: "props (filters, loading, data)"
      pattern: ":filters.*:data"
    - from: "el-templo-admin/src/pages/AnaliticasPage.vue"
      to: "el-templo-admin/src/components/analytics/FinanzasTab.vue"
      via: "props (filters, loading, data)"
      pattern: ":filters.*:data"
---

<objective>
Extract 3 tab panel components from AnaliticasPage.vue (1260 LOC) to reduce it to ~350 LOC.

Purpose: AnaliticasPage has 3 self-contained tab panels, each with its own chart configs, computed data, helper functions, and template. The parent page owns shared concerns (filters, KPIs, data fetching, tab switching). Tab panels are pure display components — ideal extraction targets.

Output: 3 new tab components + slimmed AnaliticasPage that passes data down as props.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@el-templo-admin/src/pages/AnaliticasPage.vue
@el-templo-admin/src/composables/useAnalyticsApi.ts
@el-templo-admin/src/types/analytics.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract 3 tab components from AnaliticasPage</name>
  <files>
    el-templo-admin/src/components/analytics/MiembrosTab.vue
    el-templo-admin/src/components/analytics/AsistenciaTab.vue
    el-templo-admin/src/components/analytics/FinanzasTab.vue
    el-templo-admin/src/utils/chart-colors.ts
    el-templo-admin/src/pages/AnaliticasPage.vue
  </files>
  <action>
Extract each tab panel into its own component. The parent AnaliticasPage keeps: header, filters (branch + date range), KPI cards, data fetching, tab switching logic. Each tab component receives data as props and owns its chart configs + display logic.

**Data flow pattern:** Parent fetches data -> passes to tab as prop. Tab owns chart configs, computed chart data, helper functions, table columns.

**chart-colors.ts** — Create `el-templo-admin/src/utils/chart-colors.ts` with the shared COLORS constant and chartColors array currently defined inline in AnaliticasPage. All 3 tab components will import from this shared file instead of duplicating the constants.

**MiembrosTab.vue** — Props: `data: MemberAnalytics | null`, `loading: boolean`. Receives data, owns:

- Template: lines 140-277 (miembros tab panel content — stat cards, Bar/Doughnut charts, attention table)
- Script: `newVsChurnedData` computed (lines 820-836), `planDistributionData` computed (lines 838-850), `barChartOptions` (lines 852-864), `doughnutChartOptions` (lines 866-872), `attentionColumns` (lines 1075-1103), `formatMemberName` (lines 1105-1108), `goToMember` (line 1110-1112), `contactMember` (lines 1114-1118), `showExtendDialog`/`extendDays`/`extendMemberName`/`openExtendDialog` (lines 1120-1130), extend dialog template (lines 500-528), chartColors from `../utils/chart-colors`
- Chart.js imports: Bar, Doughnut + their chart.js registrations

**AsistenciaTab.vue** — Props: `data: AttendanceAnalytics | null`, `loading: boolean`. Receives data, owns:

- Template: lines 282-396 (asistencia tab panel content — Line chart, no-show rate, heatmap, slot occupancy table)
- Script: `dailyCheckinsData` computed (lines 876-895), `lineChartOptions` (lines 897-906), heatmap helpers (lines 910-973 — heatmapDays, heatmapHours, heatmapLookup, heatmapCellColor, heatmapCellValue, dayName, occupancyColor), `slotOccupancyColumns` (lines 977-1006), COLORS from `../utils/chart-colors`
- Chart.js imports: Line + its registrations
- Heatmap CSS (extract from scoped styles)

**FinanzasTab.vue** — Props: `data: FinancialAnalytics | null`, `loading: boolean`. Receives data, owns:

- Template: lines 401-496 (finanzas tab panel content — revenue trend Bar, revenue by method, revenue by branch Bar, outstanding/collection)
- Script: `revenueChartOptions` (lines 1010-1024), `revenueTrendData` computed (lines 1026-1039), `branchChartOptions` (lines 1041-1056), `revenueByBranchData` computed (lines 1058-1071), `formatCurrency` (lines 750-752), arsFormatter (line 744-748), COLORS and chartColors from `../utils/chart-colors`
- Chart.js imports: Bar + its registrations

**AnaliticasPage.vue** — Keep: header, branch filter, date range filter, KPI cards section, tab switching, all data fetching functions, lifecycle. Replace tab panel contents with:

```vue
<q-tab-panel name="miembros">
  <MiembrosTab :data="memberData" :loading="loadingMembers" />
</q-tab-panel>
<q-tab-panel name="asistencia">
  <AsistenciaTab :data="attendanceData" :loading="loadingAttendance" />
</q-tab-panel>
<q-tab-panel name="finanzas">
  <FinanzasTab :data="financialData" :loading="loadingFinancial" />
</q-tab-panel>
```

Move the Chart.js registration block to a shared `el-templo-admin/src/utils/chart-setup.ts` file if desired, or let each tab register only what it needs. The parent page no longer needs any chart.js imports.

Use `createLogger` from `src/utils/logger` in each new component. Never `console.log`.
</action>
<verify>
<automated>cd /home/franco/projects/el-templo/el-templo-admin && npx vue-tsc --noEmit 2>&1 | head -30</automated>
</verify>
<done> - AnaliticasPage.vue is under 500 LOC - 3 new tab components exist in components/analytics/ - chart-colors.ts exists in utils/ with shared COLORS and chartColors - TypeScript compilation passes with no errors - Chart.js registrations work correctly in each tab - Lazy loading pattern preserved (parent fetches per active tab, tab just renders)
</done>
</task>

</tasks>

<verification>
- `cd el-templo-admin && npx vue-tsc --noEmit` — no type errors
- `wc -l el-templo-admin/src/pages/AnaliticasPage.vue` — under 500 lines
- All 3 tab component files exist and are non-empty
- `el-templo-admin/src/utils/chart-colors.ts` exists with COLORS and chartColors exports
</verification>

<success_criteria>
AnaliticasPage reduced from 1260 LOC to under 500 LOC. Three focused tab components created. Shared chart colors extracted to utils. Chart.js works in each tab. TypeScript compiles cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/56-god-object-decomposition-architectural-fixes/56-02-SUMMARY.md`
</output>
