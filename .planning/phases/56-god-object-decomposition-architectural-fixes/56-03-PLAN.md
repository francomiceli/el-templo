---
phase: 56-god-object-decomposition-architectural-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/scheduling/service.ts
  - el-templo-api/src/modules/scheduling/activity-service.ts
  - el-templo-api/src/modules/scheduling/booking-service.ts
  - el-templo-api/src/modules/scheduling/holiday-service.ts
  - el-templo-api/src/modules/scheduling/index.ts
  - el-templo-api/src/modules/scheduling/routes.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "SchedulingService (now ScheduleService) is under 600 LOC, focused on schedule CRUD + weekly grid"
    - "ActivityService, BookingService, HolidayService each own a single domain"
    - "All existing API routes continue to work identically"
    - "All scheduling integration tests pass"
  artifacts:
    - path: "el-templo-api/src/modules/scheduling/activity-service.ts"
      provides: "Activity CRUD operations"
      exports: ["ActivityService"]
    - path: "el-templo-api/src/modules/scheduling/booking-service.ts"
      provides: "Booking lifecycle (reserve, cancel, waitlist, admin add/remove)"
      exports: ["BookingService"]
    - path: "el-templo-api/src/modules/scheduling/holiday-service.ts"
      provides: "Holiday CRUD + holiday-date queries"
      exports: ["HolidayService"]
    - path: "el-templo-api/src/modules/scheduling/service.ts"
      provides: "Schedule CRUD + weekly grid (slimmed from 1634 to ~600 LOC)"
    - path: "el-templo-api/src/modules/scheduling/index.ts"
      provides: "Updated barrel exporting all 4 services"
  key_links:
    - from: "el-templo-api/src/modules/scheduling/routes.ts"
      to: "el-templo-api/src/modules/scheduling/activity-service.ts"
      via: "import + instantiation in route handler"
      pattern: "new ActivityService"
    - from: "el-templo-api/src/modules/scheduling/routes.ts"
      to: "el-templo-api/src/modules/scheduling/booking-service.ts"
      via: "import + instantiation in route handler"
      pattern: "new BookingService"
    - from: "el-templo-api/src/modules/scheduling/routes.ts"
      to: "el-templo-api/src/modules/scheduling/holiday-service.ts"
      via: "import + instantiation in route handler"
      pattern: "new HolidayService"
---

<objective>
Decompose SchedulingService (1634 LOC, 4 domains) into focused domain services.

Purpose: SchedulingService currently handles activities CRUD, schedule management, booking lifecycle, and holiday management — 4 distinct responsibilities. Each domain should be its own service class for maintainability, testability, and reduced cognitive load.

Output: 4 focused services (activity, schedule, booking, holiday) + updated routes and barrel.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@el-templo-api/src/modules/scheduling/service.ts
@el-templo-api/src/modules/scheduling/routes.ts
@el-templo-api/src/modules/scheduling/types.ts
@el-templo-api/src/modules/scheduling/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract ActivityService and HolidayService from SchedulingService</name>
  <files>
    el-templo-api/src/modules/scheduling/activity-service.ts
    el-templo-api/src/modules/scheduling/holiday-service.ts
    el-templo-api/src/modules/scheduling/service.ts
  </files>
  <action>
Extract the two simpler, self-contained domains first:

**ActivityService (activity-service.ts)** — Extract from SchedulingService:

- `createActivity` (lines ~56-69)
- `listActivities` (lines ~74-82)
- `updateActivity` (lines ~87-113)
- `getActivity` (lines ~115-124) — currently private, make public since it's needed standalone
- `mapActivityRow` private helper
- Constructor: `(db: MySql2Database<typeof schema>, log: FastifyBaseLogger)`
- Import types: `ActivityRecord` from `./types`
- Import schema, eq from drizzle-orm

**HolidayService (holiday-service.ts)** — Extract from SchedulingService:

- `addHoliday` (lines ~1169-1230) — NOTE: this calls booking cancellation. After extraction, it should accept a `cancelBookingsForDate` callback or depend on BookingService. For now, keep the direct DB delete of bookings inline (it's a simple DELETE WHERE, not complex booking logic). Add a TODO comment for future clean dependency.
- `removeHoliday` (lines ~1232-1246)
- `listHolidays` (lines ~1248-1278)
- `getHolidaysForWeek` (lines ~1280-1305) — currently private, make public since ScheduleService needs it for weekly grid
- Constructor: `(db: MySql2Database<typeof schema>, log: FastifyBaseLogger)`
- Import types: `HolidayRecord` from `./types`

**SchedulingService** — Remove all extracted methods. Keep: schedule CRUD (createSchedule, getWeeklyGrid, getSlotDetail, toggleSchedule, deleteSchedule, seedDefaultSchedules) + private helpers that schedule methods use. Add HolidayService as a dependency for `getWeeklyGrid` (it calls `getHolidaysForWeek`).

Each new service follows the existing pattern: class with `(db, log)` constructor, same error types from `../shared/errors`.
</action>
<verify>
<automated>cd /home/franco/projects/el-templo/el-templo-api && npx tsc --noEmit 2>&1 | head -30</automated>
</verify>
<done> - activity-service.ts exists with ActivityService class - holiday-service.ts exists with HolidayService class - SchedulingService no longer has activity or holiday methods - TypeScript compiles
</done>
</task>

<task type="auto">
  <name>Task 2: Extract BookingService, update routes and barrel</name>
  <files>
    el-templo-api/src/modules/scheduling/booking-service.ts
    el-templo-api/src/modules/scheduling/service.ts
    el-templo-api/src/modules/scheduling/routes.ts
    el-templo-api/src/modules/scheduling/index.ts
  </files>
  <action>
**BookingService (booking-service.ts)** — Extract from SchedulingService:
- `reserve` (lines ~674-860) — the most complex method with capacity checks, overdue block, weekly limit, waitlist
- `cancel` (lines ~861-932)
- `getMyBookings` (lines ~933-980)
- `getMyWeeklyAttendance` (lines ~981-1031)
- `adminAddBooking` (lines ~1032-1119)
- `adminRemoveBooking` (lines ~1120-1161)
- Private helpers used by booking methods: `promoteWaitlist`, `countActiveBookings`, `countWeeklyBookings`, `getMemberClassesPerWeek`, `getBranchCapacity`, `getScheduleSlotRaw`, `getScheduleSlot`, `getBookingRecord`
- Constructor: `(db: MySql2Database<typeof schema>, log: FastifyBaseLogger, paymentService: PaymentService, subscriptionService: SubscriptionService)` — accept dependencies via constructor injection (fixing the DI anti-pattern for this service at the same time). Do NOT `new` PaymentService/SubscriptionService inside constructor.
- Import types from `./types`, errors from `../shared/errors`, date-utils from `../shared/date-utils`

**SchedulingService (service.ts)** — After extraction, should contain only:

- Schedule CRUD: `createSchedule`, `toggleSchedule`, `deleteSchedule`, `seedDefaultSchedules`
- Weekly grid: `getWeeklyGrid`, `getSlotDetail`
- Constructor: `(db, log, holidayService: HolidayService)` — accept HolidayService via constructor (getWeeklyGrid needs getHolidaysForWeek). Remove PaymentService/SubscriptionService fields since they moved to BookingService.
- Target: ~500-600 LOC

**routes.ts** — Update both admin and member route plugins:

- Import all 4 services: ActivityService, SchedulingService, BookingService, HolidayService
- In the admin plugin: instantiate all 4 services. HolidayService first (no deps), then SchedulingService(db, log, holidayService), BookingService(db, log, paymentService, subscriptionService). Route each endpoint to the correct service:
  - Activity routes -> activityService
  - Schedule routes -> schedulingService
  - Booking admin routes -> bookingService
  - Holiday routes -> holidayService
- In the member plugin: instantiate BookingService for reserve/cancel/getMyBookings/getMyWeeklyAttendance, SchedulingService for getWeeklyGrid/getSlotDetail
- PaymentService and SubscriptionService are instantiated in routes.ts and passed to BookingService constructor (same pattern as before, just moved out of service constructor)

**index.ts** — Update barrel to export all 4 services:

```typescript
export { ActivityService } from "./activity-service";
export { BookingService } from "./booking-service";
export { HolidayService } from "./holiday-service";
export { SchedulingService } from "./service";
```

Preserve all existing type exports.
</action>
<verify>
<automated>cd /home/franco/projects/el-templo/el-templo-api && pnpm test -- --run test/scheduling/ 2>&1 | tail -20</automated>
</verify>
<done> - BookingService exists with all booking methods - SchedulingService is under 600 LOC - Routes updated to use all 4 services - Barrel exports all 4 services - All scheduling tests pass
</done>
</task>

</tasks>

<verification>
- `cd el-templo-api && npx tsc --noEmit` — no type errors
- `cd el-templo-api && pnpm test -- --run test/scheduling/` — all scheduling tests pass
- `wc -l el-templo-api/src/modules/scheduling/service.ts` — under 600 lines
- All 4 service files exist
</verification>

<success_criteria>
SchedulingService decomposed from 1634 LOC into 4 focused services. All scheduling tests pass. BookingService uses constructor DI (no `new` inside constructor). Routes wire the correct service for each endpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/56-god-object-decomposition-architectural-fixes/56-03-SUMMARY.md`
</output>
