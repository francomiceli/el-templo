---
phase: 72-unified-training-experience
plan: 03
type: execute
wave: 2
depends_on: [72-01]
files_modified:
  - el-templo-app/src/modules/progression/pages/MiCamino.vue
autonomous: true
requirements: [UTE-04, UTE-06, UTE-07]

must_haves:
  truths:
    - "Personalizada members see a single unified view on Mi Camino (no tabs)"
    - "Unified view shows: personalizada progress as primary, general stats as secondary/collapsible"
    - "Regular members see the same single-view GeneralContent as before (no change)"
    - "Members with expired personalizada see archived data + renewal prompt"
    - "New members with no sessions see the existing empty state"
  artifacts:
    - path: "el-templo-app/src/modules/progression/pages/MiCamino.vue"
      provides: "Unified Mi Camino with conditional layout"
      contains: "hasActivePersonalizada"
  key_links:
    - from: "el-templo-app/src/modules/progression/pages/MiCamino.vue"
      to: "el-templo-app/src/stores/useUserStore.ts"
      via: "useUserStore().hasActivePersonalizada computed"
      pattern: "hasActivePersonalizada"
    - from: "el-templo-app/src/modules/progression/pages/MiCamino.vue"
      to: "el-templo-app/src/modules/progression/components/PersonalizadaSection.vue"
      via: "component import, direct render (no tab panel)"
      pattern: "PersonalizadaSection"
    - from: "el-templo-app/src/modules/progression/pages/MiCamino.vue"
      to: "el-templo-app/src/modules/progression/components/GeneralContent.vue"
      via: "component render in collapsible expansion"
      pattern: "GeneralContent"
---

<objective>
Make Mi Camino show a single unified view for personalizada members (no Entrenamiento/Personalizadas tabs). Personalizada progress is the primary content, general training stats become a secondary collapsible section. Regular members see zero changes.

Purpose: Close the feedback loop — when a member lands on Mi Camino after a session, they see their personalizada progress front and center, with training stats available but secondary.
Output: Updated MiCamino.vue with conditional unified/tabbed layout.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-unified-training-experience/72-CONTEXT.md
@.planning/phases/72-unified-training-experience/72-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From el-templo-app/src/stores/useUserStore.ts (extended by Plan 01):

```typescript
const hasActivePersonalizada = computed(() => {
  return (
    subscription.value?.status === "active" &&
    subscription.value?.isPersonalizada === true
  );
});
const hasActiveSubscription = computed(() => {
  return (
    subscription.value?.status === "active" ||
    subscription.value?.status === "paused"
  );
});
```

From el-templo-app/src/modules/progression/pages/MiCamino.vue (current):

```typescript
// showTabs = true when activePersonalizada OR archivedPersonalizadas exist
// When showTabs=true: q-tabs with "general" and "personalizadas" panels
// When showTabs=false: just GeneralContent rendered directly
// activeTab defaults to 'personalizadas' when activePersonalizada exists (Phase 70)
```

From el-templo-app/src/modules/progression/components/PersonalizadaSection.vue:

```typescript
// Props: activePersonalizada, archivedPersonalizadas, allMetadata, cycleStats, loading, error
// Renders: CTA card, info card with cycle progress, duration rows, wrap-up, archived history
// The CTA card has "Entrenar" button linking to /personalizada/duration
// This button should link to /training instead (context-aware page now handles routing)
```

From el-templo-app/src/modules/progression/components/GeneralContent.vue:

```typescript
// Props: todayCompleted, todaySession, stats, rpeTrend, evaluation
// Renders: Today card, TrainingStats, RPE chart, EvaluationRequest
// The "Entrenar" button links to /training (correct, context-aware now)
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify Mi Camino layout for personalizada members</name>
  <files>el-templo-app/src/modules/progression/pages/MiCamino.vue</files>
  <read_first>
    - el-templo-app/src/modules/progression/pages/MiCamino.vue (full file)
    - el-templo-app/src/modules/progression/components/PersonalizadaSection.vue (full file — understand props and CTA button)
    - el-templo-app/src/modules/progression/components/GeneralContent.vue (first 80 lines — understand props)
    - el-templo-app/src/stores/useUserStore.ts (hasActivePersonalizada, hasActiveSubscription)
    - el-templo-app/src/modules/progression/composables/usePersonalizadaProgress.ts (fetchPersonalizadaData)
  </read_first>
  <action>
    Rework MiCamino.vue's content section to have three layout modes instead of the current two:

    **Current logic:**
    - `showTabs` = hasPersonalizadaData → show tabs (general + personalizadas)
    - `!showTabs` → show GeneralContent only

    **New logic (3 modes):**
    - `isUnifiedPersonalizada` = `userStore.hasActivePersonalizada` → unified single view (no tabs)
    - `showTabs` = has personalizada data but NOT active personalizada (e.g., only archived) → keep tabs
    - Neither → show GeneralContent only (regular member, unchanged)

    **Template changes for the content section (v-else after empty state):**

    ```html
    <div v-else class="mi-camino__content">
      <!-- Welcome Header (shared, always visible — unchanged) -->
      <div class="mi-camino__welcome">
        ...existing welcome header code...
      </div>

      <!-- MODE 1: Unified personalizada view (no tabs) -->
      <template v-if="isUnifiedPersonalizada">
        <!-- Personalizada Section as primary content (no CTA card — they came FROM training) -->
        <PersonalizadaSection
          :active-personalizada="personalizadaProgress.activePersonalizada.value"
          :archived-personalizadas="personalizadaProgress.archivedPersonalizadas.value"
          :all-metadata="personalizadaProgress.allMetadata.value"
          :cycle-stats="personalizadaProgress.cycleStats.value"
          :loading="false"
          :error="personalizadaProgress.error.value"
        />

        <!-- Entrenar CTA button -->
        <q-btn
          color="primary"
          unelevated
          no-caps
          label="Entrenar"
          icon-right="arrow_forward"
          to="/training"
          class="full-width mi-camino__train-btn"
        />

        <!-- General Training Stats (collapsible) -->
        <q-expansion-item
          dense
          header-class="mi-camino__stats-header"
          class="mi-camino__stats-expansion"
        >
          <template #header>
            <q-item-section>
              <q-item-label class="mi-camino__stats-title">
                Estadisticas de Entrenamiento
              </q-item-label>
            </q-item-section>
          </template>
          <GeneralContent
            :today-completed="todayCompleted"
            :today-session="progressionStore.todaySession"
            :stats="progressionStore.stats"
            :rpe-trend="progressionStore.rpeTrend"
            :evaluation="progressionStore.evaluation"
            @request-evaluation="handleRequestEvaluation"
          />
        </q-expansion-item>
      </template>

      <!-- MODE 2: Tabs (archived personalizadas but no active — keep existing tabs) -->
      <template v-else-if="showTabs">
        ...existing q-tabs + q-tab-panels code (unchanged)...
      </template>

      <!-- MODE 3: Regular member — GeneralContent only (unchanged) -->
      <GeneralContent
        v-else
        :today-completed="todayCompleted"
        :today-session="progressionStore.todaySession"
        :stats="progressionStore.stats"
        :rpe-trend="progressionStore.rpeTrend"
        :evaluation="progressionStore.evaluation"
        @request-evaluation="handleRequestEvaluation"
      />
    </div>
    ```

    **Script changes:**

    1. Import useUserStore (already imported):
       ```typescript
       const userStore = useUserStore()
       ```

    2. Add the unified mode computed:
       ```typescript
       const isUnifiedPersonalizada = computed(() => {
         return userStore.hasActivePersonalizada
       })
       ```

    3. Update `showTabs` to exclude the unified case:
       ```typescript
       const showTabs = computed(() => {
         // Don't show tabs if unified personalizada view is active
         if (isUnifiedPersonalizada.value) return false
         return (
           personalizadaProgress.activePersonalizada.value !== null ||
           personalizadaProgress.archivedPersonalizadas.value.length > 0
         )
       })
       ```

    4. Remove the default tab logic from onMounted (no longer needed since unified view doesn't use tabs):
       ```typescript
       onMounted(async () => {
         fetchStats()
         await personalizadaProgress.fetchPersonalizadaData()
         // Default to Personalizadas tab only when showing tabs (not unified mode)
         if (!isUnifiedPersonalizada.value && personalizadaProgress.activePersonalizada.value) {
           activeTab.value = 'personalizadas'
         }
       })
       ```

    5. Update the `isEmptyState` computed to also check for subscription — if member has no subscription AND no stats, show the empty state as before. No change needed if the existing logic already handles this correctly.

    **Expired personalizada handling:**
    When personalizada subscription expires, `userStore.hasActivePersonalizada` will be false but `personalizadaProgress.archivedPersonalizadas` may have data. This falls into MODE 2 (tabs) or MODE 3 (no personalizada data at all). The existing archived history in PersonalizadaSection already handles this with the collapsible history section.

    However, add a renewal prompt for the expired case. In the `isUnifiedPersonalizada` false + has-archived-but-no-active case, the existing tab UI already shows the archived data with "Consulta en recepcion" messaging. No additional handling needed.

    **Styles to add:**

    ```scss
    &__train-btn {
      margin-top: 8px;
    }

    &__stats-expansion {
      margin-top: 16px;
    }

    &__stats-header {
      padding: 8px 0;
    }

    &__stats-title {
      font-family: 'Montserrat', sans-serif;
      font-size: 14px;
      font-weight: 600;
      color: rgba($primary, 0.6);
    }
    ```

    **Key constraint per CONTEXT.md:**
    - Regular members keep the EXACT current single-view GeneralContent — zero changes for them
    - The "today session" card in GeneralContent links to /training which is now context-aware — this is correct behavior for both member types

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-app && npx vue-tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "isUnifiedPersonalizada" el-templo-app/src/modules/progression/pages/MiCamino.vue
    - grep -q "hasActivePersonalizada" el-templo-app/src/modules/progression/pages/MiCamino.vue
    - grep -q "Estadisticas de Entrenamiento" el-templo-app/src/modules/progression/pages/MiCamino.vue
    - grep -q "q-expansion-item" el-templo-app/src/modules/progression/pages/MiCamino.vue
    - The template has three layout modes: unified (isUnifiedPersonalizada), tabs (showTabs), and GeneralContent only
    - No q-tabs rendered when isUnifiedPersonalizada is true
    - GeneralContent appears inside a q-expansion-item in unified mode
    - PersonalizadaSection rendered directly (not in q-tab-panel) in unified mode
  </acceptance_criteria>
  <done>
    - Personalizada members see unified view: personalizada progress primary, "Entrenar" CTA, general stats in collapsible
    - Regular members see the exact same GeneralContent view (zero changes)
    - Members with only archived personalizadas still see tabs
    - No tabs shown for active personalizada members
  </done>
</task>

<task type="auto">
  <name>Task 2: Update PersonalizadaSection CTA to point to /training</name>
  <files>el-templo-app/src/modules/progression/components/PersonalizadaSection.vue</files>
  <read_first>
    - el-templo-app/src/modules/progression/components/PersonalizadaSection.vue (line 35 — the "Entrenar" button's `to` prop)
  </read_first>
  <action>
    In PersonalizadaSection.vue, the "Entrenar" button in the CTA card (around line 35) currently has:
    ```html
    to="/personalizada/duration"
    ```

    Change it to:
    ```html
    to="/training"
    ```

    This is because /training is now the context-aware entry point. For personalizada members, /training shows the info card + duration picker inline. No need to deep-link to the personalizada-specific duration route.

    This is a single-line change. The rest of PersonalizadaSection.vue stays exactly the same.

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-app && grep 'to="/training"' src/modules/progression/components/PersonalizadaSection.vue</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'to="/training"' el-templo-app/src/modules/progression/components/PersonalizadaSection.vue
    - The CTA card "Entrenar" button navigates to /training, not /personalizada/duration
  </acceptance_criteria>
  <done>PersonalizadaSection "Entrenar" CTA points to /training (context-aware page)</done>
</task>

</tasks>

<verification>
1. App type-checks: `cd el-templo-app && npx vue-tsc --noEmit` passes
2. MiCamino has 3 layout modes: unified, tabs, general-only
3. PersonalizadaSection CTA links to /training
4. No tabs visible when isUnifiedPersonalizada is true
</verification>

<success_criteria>

- Personalizada members see unified view on Mi Camino (personalizada progress primary, stats collapsible, no tabs)
- Regular members see exactly the same view as before (GeneralContent, no change)
- PersonalizadaSection "Entrenar" button navigates to /training
- Expired personalizada members see archived data in tabs or general view
  </success_criteria>

<output>
After completion, create `.planning/phases/72-unified-training-experience/72-03-SUMMARY.md`
</output>
