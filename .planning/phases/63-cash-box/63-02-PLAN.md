---
phase: 63-cash-box
plan: 02
type: execute
wave: 2
depends_on: [63-01]
files_modified:
  - el-templo-admin/src/pages/CajaPage.vue
  - el-templo-admin/src/router/routes.ts
  - el-templo-admin/src/layouts/AdminLayout.vue
  - el-templo-admin/src/pages/AlumnoDetailPage.vue
  - el-templo-admin/src/pages/AlumnosPage.vue
  - el-templo-admin/src/composables/usePaymentsApi.ts
  - el-templo-admin/src/types/payment.ts
autonomous: true
requirements: [CASH-02, CASH-03]

must_haves:
  truths:
    - "CajaPage displays 4 summary cards: Efectivo, Transferencia, Tarjeta, Total"
    - "CajaPage has a month picker for period selection defaulting to current month"
    - "CajaPage has an Egresos placeholder section with Proximamente badge"
    - "Sidebar shows 'Caja' label at /caja route accessible to recepcionista, admin, superadmin"
    - "MemberPaymentTab and RegisterPaymentDialog are deleted"
    - "AlumnoDetailPage has no Pagos tab"
    - "Morosos badge removed from sidebar and Alumnos filters"
  artifacts:
    - path: "el-templo-admin/src/pages/CajaPage.vue"
      provides: "Cash box page with per-method summary cards, month picker, transaction table, egresos placeholder"
      min_lines: 200
    - path: "el-templo-admin/src/router/routes.ts"
      provides: "Route /caja pointing to CajaPage with recepcionista access"
  key_links:
    - from: "el-templo-admin/src/pages/CajaPage.vue"
      to: "/api/admin/payments/payments/summary"
      via: "usePaymentsApi().getFinancialSummary"
      pattern: "getFinancialSummary"
    - from: "el-templo-admin/src/layouts/AdminLayout.vue"
      to: "/caja"
      via: "sidebar navigation item"
      pattern: 'to="/caja"'
---

<objective>
Rename PagosPage to CajaPage, replace old summary cards with per-method breakdown (Efectivo/Transferencia/Tarjeta/Total), add month picker, add egresos placeholder, update routes and sidebar, remove dead components (RegisterPaymentDialog, MemberPaymentTab), and remove all morosos UI references.

Purpose: Recepcionistas and admins get a clean cash box view that shows revenue by payment method for monthly reconciliation, without any of the old debt/morosos concepts.
Output: Working CajaPage at /caja with per-method summary cards, month picker, and cleaned-up navigation.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-cash-box/63-CONTEXT.md
@.planning/phases/63-cash-box/63-01-SUMMARY.md

<interfaces>
<!-- Key types from Plan 01 changes -->

From el-templo-admin/src/types/payment.ts (after Plan 01):

```typescript
export type PaymentMethod = "cash" | "transfer" | "card";
export const PAYMENT_METHOD_LABELS: Record<PaymentMethod, string> = {
  cash: "Efectivo",
  transfer: "Transferencia",
  card: "Tarjeta",
};
export const PAYMENT_METHOD_COLORS: Record<PaymentMethod, string> = {
  cash: "green",
  transfer: "blue",
  card: "purple",
};
// FinancialSummary will be updated by Plan 01 to remove totalOutstanding/collectionRate
```

From el-templo-admin/src/composables/usePaymentsApi.ts:

```typescript
export function usePaymentsApi() {
  // getFinancialSummary(branchId?, dateFrom?, dateTo?)
  // listPayments(params: PaymentListParams)
  // voidPayment(paymentId, reason)
  // getMorososCount(branchId?) — TO BE REMOVED
  // recordPayment(userId, input) — TO BE REMOVED from composable (no standalone recording)
  // getMemberPayments(userId) — TO BE REMOVED (no member payment tab)
  // getMemberBalance(userId) — TO BE REMOVED
}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename PagosPage to CajaPage with per-method summary, month picker, and egresos placeholder</name>
  <read_first>
    el-templo-admin/src/pages/PagosPage.vue
    el-templo-admin/src/composables/usePaymentsApi.ts
    el-templo-admin/src/types/payment.ts
  </read_first>
  <files>
    el-templo-admin/src/pages/CajaPage.vue
    el-templo-admin/src/composables/usePaymentsApi.ts
    el-templo-admin/src/types/payment.ts
  </files>
  <action>
    **1. Update frontend types (`payment.ts`):**
    Update `FinancialSummary` to match the Plan 01 API changes — remove `totalOutstanding` and `collectionRate`:
    ```typescript
    export interface FinancialSummary {
      monthlyRevenue: number;
      revenueByMethod: {
        cash: number;
        transfer: number;
        card: number;
      };
      revenueByBranch: Array<{
        branchId: number;
        branchName: string;
        revenue: number;
      }>;
    }
    ```
    Also remove `MemberBalance`, `RecordPaymentInput`, and `VoidPaymentInput` interfaces (payments are only recorded via subscription operations now, and void stays but uses inline type). Actually keep `VoidPaymentInput` since void still exists.

    **2. Update usePaymentsApi composable:**
    - Remove `recordPayment` method (no standalone payment recording)
    - Remove `getMemberPayments` method (no member payment tab)
    - Remove `getMemberBalance` method (balance concept removed)
    - Remove `getMorososCount` method (morosos concept removed)
    - Keep: `listPayments`, `getFinancialSummary`, `voidPayment`
    - Update return object accordingly
    - Remove unused type imports

    **3. Create CajaPage.vue (rename and transform PagosPage.vue):**
    Delete the old `PagosPage.vue` file. Create new `CajaPage.vue` based on PagosPage but with these changes:

    **Header:** Change "Pagos" to "Caja". Remove the "Registrar Pago" button entirely.

    **Month picker:** Replace the dateFrom/dateTo inputs in the filter bar with a single month picker:
    ```html
    <q-input
      v-model="selectedMonth"
      label="Mes"
      type="month"
      dense
      outlined
      @update:model-value="onFilterChange"
    />
    ```
    The `selectedMonth` ref defaults to `new Date().toISOString().slice(0, 7)` (YYYY-MM format). Compute `dateFrom` and `dateTo` from selectedMonth:
    ```typescript
    const dateRange = computed(() => {
      if (!selectedMonth.value) return { dateFrom: undefined, dateTo: undefined };
      const [year, month] = selectedMonth.value.split('-').map(Number);
      const dateFrom = `${year}-${String(month).padStart(2, '0')}-01`;
      const lastDay = new Date(year, month, 0).getDate();
      const dateTo = `${year}-${String(month).padStart(2, '0')}-${String(lastDay).padStart(2, '0')}`;
      return { dateFrom, dateTo };
    });
    ```
    Pass `dateRange.value.dateFrom` and `dateRange.value.dateTo` to both `loadSummary()` and `loadPayments()`.

    **Summary cards:** Replace the 3 old cards (Ingresos, Deudas, Tasa) with 4 per-method cards:
    ```html
    <div class="row q-col-gutter-md q-mb-md">
      <!-- Efectivo -->
      <div class="col-6 col-sm-3">
        <q-card flat bordered>
          <q-card-section>
            <div class="text-caption text-grey-7">
              <q-icon name="payments" color="green" class="q-mr-xs" /> Efectivo
            </div>
            <div v-if="loadingSummary" class="q-mt-xs"><q-skeleton type="text" width="100px" /></div>
            <div v-else class="text-h5 text-weight-bold q-mt-xs">
              ${{ summary.revenueByMethod.cash.toLocaleString() }}
            </div>
          </q-card-section>
        </q-card>
      </div>
      <!-- Transferencia -->
      <div class="col-6 col-sm-3">
        <q-card flat bordered>
          <q-card-section>
            <div class="text-caption text-grey-7">
              <q-icon name="account_balance" color="blue" class="q-mr-xs" /> Transferencia
            </div>
            <div v-if="loadingSummary" class="q-mt-xs"><q-skeleton type="text" width="100px" /></div>
            <div v-else class="text-h5 text-weight-bold q-mt-xs">
              ${{ summary.revenueByMethod.transfer.toLocaleString() }}
            </div>
          </q-card-section>
        </q-card>
      </div>
      <!-- Tarjeta -->
      <div class="col-6 col-sm-3">
        <q-card flat bordered>
          <q-card-section>
            <div class="text-caption text-grey-7">
              <q-icon name="credit_card" color="purple" class="q-mr-xs" /> Tarjeta
            </div>
            <div v-if="loadingSummary" class="q-mt-xs"><q-skeleton type="text" width="100px" /></div>
            <div v-else class="text-h5 text-weight-bold q-mt-xs">
              ${{ summary.revenueByMethod.card.toLocaleString() }}
            </div>
          </q-card-section>
        </q-card>
      </div>
      <!-- Total -->
      <div class="col-6 col-sm-3">
        <q-card flat bordered class="bg-grey-1">
          <q-card-section>
            <div class="text-caption text-grey-7">
              <q-icon name="account_balance_wallet" class="q-mr-xs" /> Total
            </div>
            <div v-if="loadingSummary" class="q-mt-xs"><q-skeleton type="text" width="100px" /></div>
            <div v-else class="text-h5 text-weight-bold text-positive q-mt-xs">
              ${{ summary.monthlyRevenue.toLocaleString() }}
            </div>
          </q-card-section>
        </q-card>
      </div>
    </div>
    ```

    Update the `summary` reactive to match new `FinancialSummary` shape (remove `totalOutstanding` and `collectionRate`).

    **Filter bar:** Keep search, branchId, paymentMethod. Replace dateFrom/dateTo with the month picker (single input). Keep the same layout.

    **Transaction table:** Same QTable as before. Remove the "Estado" column (no voided payments shown in Caja). Keep: Fecha, Alumno, Monto, Metodo, Plan, Registrado por, Acciones. The Acciones menu keeps "Detalles" and "Anular" (void action stays).

    **Egresos placeholder:** After the QTable, add:
    ```html
    <q-card flat bordered class="q-mt-lg">
      <q-card-section>
        <div class="row items-center q-gutter-sm">
          <div class="text-subtitle1 text-weight-bold text-grey-7">Egresos</div>
          <q-badge color="grey" label="Proximamente" />
        </div>
        <div class="text-caption text-grey-5 q-mt-sm">
          Seguimiento de gastos y fondo de caja. Disponible en una futura actualizacion.
        </div>
      </q-card-section>
    </q-card>
    ```

    Remove the `RegisterPaymentDialog` import and `<RegisterPaymentDialog>` component from the template. Remove `showRegisterDialog` ref.

    Update the logger name from `PagosPage` to `CajaPage`.

    **4. Delete old PagosPage.vue:**
    Delete `el-templo-admin/src/pages/PagosPage.vue` (it's been replaced by CajaPage.vue).

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && npx vue-tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - el-templo-admin/src/pages/CajaPage.vue exists and contains "Caja" as header text
    - el-templo-admin/src/pages/CajaPage.vue contains "Efectivo" and "Transferencia" and "Tarjeta" and "Total" (4 summary cards)
    - el-templo-admin/src/pages/CajaPage.vue contains 'type="month"' (month picker input)
    - el-templo-admin/src/pages/CajaPage.vue contains "Egresos" and "Proximamente" (placeholder section)
    - el-templo-admin/src/pages/CajaPage.vue does NOT contain "RegisterPaymentDialog"
    - el-templo-admin/src/pages/CajaPage.vue does NOT contain "Registrar Pago"
    - el-templo-admin/src/pages/PagosPage.vue does NOT exist
    - el-templo-admin/src/composables/usePaymentsApi.ts does NOT contain "getMorososCount"
    - el-templo-admin/src/composables/usePaymentsApi.ts does NOT contain "getMemberBalance"
    - el-templo-admin/src/composables/usePaymentsApi.ts does NOT contain "recordPayment"
    - el-templo-admin/src/types/payment.ts does NOT contain "totalOutstanding"
    - el-templo-admin/src/types/payment.ts does NOT contain "collectionRate"
    - vue-tsc --noEmit exits 0
  </acceptance_criteria>
  <done>CajaPage shows 4 per-method revenue cards, month picker, transaction table without voided records, and egresos placeholder. Old PagosPage deleted. API composable and types cleaned up.</done>
</task>

<task type="auto">
  <name>Task 2: Route/sidebar/navigation updates and dead component cleanup</name>
  <read_first>
    el-templo-admin/src/router/routes.ts
    el-templo-admin/src/layouts/AdminLayout.vue
    el-templo-admin/src/pages/AlumnoDetailPage.vue
    el-templo-admin/src/pages/AlumnosPage.vue
    el-templo-admin/src/components/RegisterPaymentDialog.vue
    el-templo-admin/src/components/MemberPaymentTab.vue
  </read_first>
  <files>
    el-templo-admin/src/router/routes.ts
    el-templo-admin/src/layouts/AdminLayout.vue
    el-templo-admin/src/pages/AlumnoDetailPage.vue
    el-templo-admin/src/pages/AlumnosPage.vue
  </files>
  <action>
    **1. Update routes.ts:**
    Change the pagos route:
    ```typescript
    // OLD:
    { path: 'pagos', component: () => import('pages/PagosPage.vue'), meta: { allowedRoles: ['admin', 'superadmin'] } },
    // NEW:
    { path: 'caja', component: () => import('pages/CajaPage.vue'), meta: { allowedRoles: ['recepcionista', 'admin', 'superadmin'] } },
    ```

    **2. Update AdminLayout.vue sidebar:**
    Change the Pagos sidebar item:
    ```html
    <!-- OLD: -->
    <q-item v-if="isAdminRole" clickable v-ripple to="/pagos">
      <q-item-section avatar><q-icon name="payments" /></q-item-section>
      <q-item-section>Pagos</q-item-section>
    </q-item>
    <!-- NEW: -->
    <q-item v-if="isCajaRole" clickable v-ripple to="/caja">
      <q-item-section avatar><q-icon name="point_of_sale" /></q-item-section>
      <q-item-section>Caja</q-item-section>
    </q-item>
    ```
    Add a new computed `isCajaRole`:
    ```typescript
    const isCajaRole = computed(() => ['recepcionista', 'admin', 'superadmin'].includes(authStore.user?.role ?? ''));
    ```

    Remove the morosos badge from the Alumnos sidebar item:
    ```html
    <!-- OLD: -->
    <q-item clickable v-ripple to="/alumnos">
      <q-item-section avatar><q-icon name="people" /></q-item-section>
      <q-item-section>Alumnos</q-item-section>
      <q-item-section side v-if="morososCount > 0">
        <q-badge color="negative" :label="morososCount" />
      </q-item-section>
    </q-item>
    <!-- NEW: -->
    <q-item clickable v-ripple to="/alumnos">
      <q-item-section avatar><q-icon name="people" /></q-item-section>
      <q-item-section>Alumnos</q-item-section>
    </q-item>
    ```

    Remove all morosos-related code from AdminLayout.vue:
    - Remove `morososCount` ref
    - Remove `morososInterval` variable
    - Remove `fetchMorososCount` function
    - Remove the `setInterval(fetchMorososCount, 60_000)` in onMounted
    - Remove the `clearInterval(morososInterval)` in onUnmounted
    - Remove `fetchMorososCount()` calls in onMounted and watch
    - Remove `usePaymentsApi` import (no longer needed if only morosos used it)
    - Remove `paymentsApi` variable

    **3. Update AlumnoDetailPage.vue:**
    Remove the Pagos tab entirely:
    - Remove `<q-tab name="pagos" label="Pagos" />` from the tabs
    - Remove the `<q-tab-panel name="pagos">` section with `<MemberPaymentTab>`
    - Remove `import MemberPaymentTab from 'src/components/MemberPaymentTab.vue'`
    - Remove `onPaymentChanged` function (which refreshed member profile on payment changes)

    **4. Update AlumnosPage.vue:**
    Remove the Morosos toggle filter:
    - Remove the `<q-toggle v-model="filters.overdue" label="Morosos" ...>` from the template
    - Remove `overdue: false` from the `filters` reactive object
    - Remove `overdue: filters.overdue || undefined` from the params passed to the API call

    **5. Delete dead components:**
    Delete these files (they are no longer imported anywhere):
    - `el-templo-admin/src/components/RegisterPaymentDialog.vue`
    - `el-templo-admin/src/components/MemberPaymentTab.vue`

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && npx vue-tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - el-templo-admin/src/router/routes.ts contains "path: 'caja'" and "CajaPage.vue"
    - el-templo-admin/src/router/routes.ts contains "recepcionista" in allowedRoles for caja route
    - el-templo-admin/src/router/routes.ts does NOT contain "pagos" or "PagosPage"
    - el-templo-admin/src/layouts/AdminLayout.vue contains 'to="/caja"' and "Caja"
    - el-templo-admin/src/layouts/AdminLayout.vue does NOT contain "morososCount"
    - el-templo-admin/src/layouts/AdminLayout.vue does NOT contain "fetchMorososCount"
    - el-templo-admin/src/layouts/AdminLayout.vue does NOT contain "usePaymentsApi"
    - el-templo-admin/src/pages/AlumnoDetailPage.vue does NOT contain "MemberPaymentTab"
    - el-templo-admin/src/pages/AlumnoDetailPage.vue does NOT contain 'name="pagos"'
    - el-templo-admin/src/pages/AlumnosPage.vue does NOT contain "Morosos"
    - el-templo-admin/src/pages/AlumnosPage.vue does NOT contain "overdue"
    - el-templo-admin/src/components/RegisterPaymentDialog.vue does NOT exist
    - el-templo-admin/src/components/MemberPaymentTab.vue does NOT exist
    - vue-tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Route updated to /caja with recepcionista access. Sidebar shows "Caja" with point_of_sale icon. Morosos badge and filter removed. Pagos tab removed from member detail. Dead components deleted. No type errors.</done>
</task>

</tasks>

<verification>
`cd el-templo-admin && npx vue-tsc --noEmit` — no type errors.
Navigate to /caja — shows 4 per-method cards, month picker, transaction table, egresos placeholder.
Navigate to /alumnos — no morosos toggle or badge.
Navigate to /alumnos/:id — no Pagos tab.
</verification>

<success_criteria>

- CajaPage at /caja with 4 revenue cards (Efectivo, Transferencia, Tarjeta, Total)
- Month picker for period selection
- Egresos placeholder section
- Route accessible to recepcionista, admin, superadmin
- All morosos/debt UI removed from sidebar, AlumnosPage, and member detail
- RegisterPaymentDialog and MemberPaymentTab deleted
- No TypeScript errors
  </success_criteria>

<output>
After completion, create `.planning/phases/63-cash-box/63-02-SUMMARY.md`
</output>
