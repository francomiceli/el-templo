---
phase: 53-codebase-health
plan: 03
type: execute
wave: 2
depends_on: ["53-01"]
files_modified:
  - el-templo-api/test/unit/progression.test.ts
  - el-templo-api/test/scheduling/scheduling.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Progression utility functions (calculateStreak, checkEligibility, formatDateLabel, getLevelDisplayName, getGreekLetter) have unit test coverage"
    - "Scheduling booking/cancel window tests verify timezone-correct behavior using the shared date-utils"
    - "Streak calculation handles edge cases: empty sessions, broken streak, today vs yesterday start, single session"
  artifacts:
    - path: "el-templo-api/test/unit/progression.test.ts"
      provides: "Unit tests for all 5 progression service functions"
      min_lines: 80
    - path: "el-templo-api/test/scheduling/scheduling.test.ts"
      provides: "Additional integration tests for booking/cancel window edge cases"
      contains: "booking window"
  key_links:
    - from: "el-templo-api/test/unit/progression.test.ts"
      to: "el-templo-api/src/modules/progression/service.ts"
      via: "import { calculateStreak, checkEligibility, ... }"
      pattern: "import.*progression/service"
---

<objective>
Add unit tests for the untested progression module and expand scheduling integration tests to cover timezone-dependent booking/cancel window behavior.

Purpose: Close the two highest-impact test coverage gaps: the progression module (zero tests for 5 pure functions used in member-facing progression stats) and the scheduling booking/cancel window logic (previously untestable due to timezone bugs fixed in Plan 01).

Output: New progression unit test file, expanded scheduling integration tests.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@el-templo-api/src/modules/progression/service.ts
@el-templo-api/test/scheduling/scheduling.test.ts
@el-templo-api/test/helpers.ts

<interfaces>
From el-templo-api/src/modules/progression/service.ts:
```typescript
export const GREEK_LETTER_MAP: Record<string, string>;
export function calculateStreak(sessions: { date: string }[]): number;
export function checkEligibility(avgRpe: number | null): boolean;
export function formatDateLabel(dateStr: string): string;
export function getLevelDisplayName(level: string): string;
export function getGreekLetter(level: string): string;
```

From el-templo-api/src/modules/shared/date-utils.ts (created by Plan 01):

```typescript
export function buildClassDateTime(bookingDate: string, timeStr: string): Date;
export function addDays(dateStr: string, days: number): string;
export function getWeekRange(date: Date): { monday: string; saturday: string };
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for progression module</name>
  <files>el-templo-api/test/unit/progression.test.ts</files>
  <action>
    Create `el-templo-api/test/unit/progression.test.ts` with unit tests for all 5 exported functions from `../../src/modules/progression/service`.

    **calculateStreak tests:**
    - Empty array returns 0
    - Single session today returns 1
    - Single session yesterday returns 1
    - Single session 2+ days ago returns 0 (streak broken)
    - 3 consecutive days ending today returns 3
    - 3 consecutive days ending yesterday returns 3
    - Gap in middle: today, yesterday, 3-days-ago returns 2 (streak counts from most recent consecutive)
    - Duplicate dates (same day, multiple sessions) count as 1 day
    - Sessions not sorted still work (function sorts internally)

    For date-dependent tests, use a pattern that constructs date strings relative to today:
    ```typescript
    const today = new Date().toISOString().split("T")[0];
    const daysAgo = (n: number) => {
      const d = new Date();
      d.setDate(d.getDate() - n);
      return d.toISOString().split("T")[0];
    };
    ```

    **checkEligibility tests:**
    - null returns false
    - 5.0 returns true (under threshold)
    - 6.0 returns true (at threshold)
    - 6.1 returns false (over threshold)
    - 0 returns true

    **formatDateLabel tests:**
    - "2026-03-10" returns a string containing "10" and a month abbreviation (locale-dependent, just check it's non-empty and contains day number)
    - Doesn't throw on edge dates like "2026-01-01"

    **getLevelDisplayName tests:**
    - "alfa" returns "Alfa"
    - "spartan" returns "Spartan"
    - "delta" returns "Delta"

    **getGreekLetter tests:**
    - "alfa" returns "α" (U+03B1)
    - "delta" returns "Δ" (U+0394)
    - "sigma" returns "Σ" (U+03A3)
    - "omega" returns "Ω" (U+03A9)
    - "spartan" returns "Ω" (same as omega per GREEK_LETTER_MAP)
    - Unknown level returns the level string itself (fallback)

  </action>
  <verify>
    <automated>cd el-templo-api && pnpm test -- --filter=progression</automated>
  </verify>
  <done>All progression utility functions have unit tests covering normal cases, edge cases, and error conditions. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Scheduling integration tests for booking/cancel windows</name>
  <files>el-templo-api/test/scheduling/scheduling.test.ts</files>
  <action>
    Add a new `describe("Booking/Cancel window timezone behavior")` block to the existing `el-templo-api/test/scheduling/scheduling.test.ts` file. Place it after the existing test blocks.

    These tests verify the INTEGRATION behavior of booking/cancel windows after the timezone fix in Plan 01. The key scenarios to test:

    **Test setup for this block:**
    - Create an activity + schedule with a known startTime (e.g., "09:00")
    - Create a member with an active subscription
    - Use the `getFutureSlot` helper pattern from the existing test file (it dynamically finds a bookable slot relative to current time)

    **Test: "rejects booking for a past class time":**
    - Find a schedule for today where the startTime is earlier than now
    - If current time is past all schedules for today, create a schedule with startTime in the past (e.g., "00:01")
    - Attempt to reserve — expect 400 "Este horario ya paso"

    **Test: "rejects cancellation within 20-min window":**
    - This is harder to test without time mocking. Add a comment explaining the window logic is now timezone-correct via `buildClassDateTime` and reference the unit tests in `date-utils.test.ts` for the pure function coverage.
    - If feasible, test by creating a booking and then checking that cancel succeeds (for a future slot well within the cancel window). This at least validates the happy path of the cancel window check.

    **Test: "booking for valid future slot succeeds" (happy path):**
    - Use `getFutureSlot` to find a valid slot
    - Reserve it — expect 201 with confirmed status
    - This validates the booking window check passes for a legitimate future class

    Keep these tests lightweight — they complement the unit tests in `date-utils.test.ts`, not replace them. The integration tests prove the wiring works (service calls buildClassDateTime correctly), while unit tests prove the date math works.

    Do NOT add more than 3-4 tests here to keep the test file manageable.

  </action>
  <verify>
    <automated>cd el-templo-api && pnpm test -- --filter=scheduling</automated>
  </verify>
  <done>Scheduling test file has booking/cancel window integration tests that validate timezone-correct behavior. Tests pass alongside existing scheduling tests.</done>
</task>

</tasks>

<verification>
- `cd el-templo-api && pnpm test` passes (all tests including new ones)
- `grep -c "describe\|it(" el-templo-api/test/unit/progression.test.ts` shows 15+ test cases
- Progression tests cover: calculateStreak (6+ cases), checkEligibility (4+ cases), formatDateLabel, getLevelDisplayName, getGreekLetter
</verification>

<success_criteria>

- Progression module has unit test file with coverage for all 5 exported functions
- calculateStreak edge cases covered (empty, today, yesterday, gap, duplicates)
- Scheduling tests include booking window integration tests
- Full test suite passes
  </success_criteria>

<output>
After completion, create `.planning/phases/53-codebase-health-timezone-fixes-god-object-decomposition-performance-optimization-test-coverage/53-03-SUMMARY.md`
</output>
