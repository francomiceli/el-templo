---
phase: 53-codebase-health
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/shared/date-utils.ts
  - el-templo-api/src/modules/shared/index.ts
  - el-templo-api/src/modules/scheduling/service.ts
  - el-templo-api/src/modules/analytics/service.ts
  - el-templo-api/test/unit/date-utils.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Booking window checks use Argentina time (UTC-3), not server-local time"
    - "Cancel window checks use Argentina time (UTC-3), not server-local time"
    - "Week range calculation returns correct Monday-Saturday regardless of server timezone"
    - "Analytics date defaults produce correct current-month boundaries"
    - "Analytics prior-period calculation returns correct day boundaries"
    - "Date utilities are pure functions with explicit timezone parameter, testable without server context"
  artifacts:
    - path: "el-templo-api/src/modules/shared/date-utils.ts"
      provides: "Pure date utility functions with explicit timezone handling"
      exports:
        [
          "addDays",
          "getWeekRange",
          "buildClassDateTime",
          "toDateString",
          "resolveMonthRange",
          "computePriorPeriod",
        ]
    - path: "el-templo-api/test/unit/date-utils.test.ts"
      provides: "Unit tests for all date utilities covering timezone edge cases"
      min_lines: 80
  key_links:
    - from: "el-templo-api/src/modules/scheduling/service.ts"
      to: "el-templo-api/src/modules/shared/date-utils.ts"
      via: "import { addDays, getWeekRange, buildClassDateTime }"
      pattern: "import.*date-utils"
    - from: "el-templo-api/src/modules/analytics/service.ts"
      to: "el-templo-api/src/modules/shared/date-utils.ts"
      via: "import { resolveMonthRange, computePriorPeriod, toDateString }"
      pattern: "import.*date-utils"
---

<objective>
Extract date/timezone utilities from SchedulingService and AnalyticsService into a shared, tested module, then fix timezone bugs in booking/cancel window checks.

Purpose: Fix critical timezone bugs where booking windows open/close at wrong times (server uses UTC, business operates in UTC-3 Argentina). Simultaneously DRY up date logic scattered across 2 services into reusable, tested pure functions.

Output: Shared date-utils module with full unit test coverage, timezone-correct booking/cancel windows, timezone-correct analytics date ranges.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@el-templo-api/src/modules/shared/index.ts
@el-templo-api/src/modules/scheduling/service.ts
@el-templo-api/src/modules/analytics/service.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared date-utils with TDD</name>
  <files>el-templo-api/src/modules/shared/date-utils.ts, el-templo-api/test/unit/date-utils.test.ts, el-templo-api/src/modules/shared/index.ts</files>
  <behavior>
    - addDays("2026-03-10", 1) returns "2026-03-11"
    - addDays("2026-03-31", 1) returns "2026-04-01" (month boundary)
    - addDays("2026-12-31", 1) returns "2027-01-01" (year boundary)
    - getWeekRange for a Wednesday returns that week's Monday and Saturday
    - getWeekRange for a Sunday returns prior week's Monday and Saturday (Mon-Sat week, Sun starts new week... actually the existing code treats Sun as part of prior week: diffToMonday = day === 0 ? -6 : 1 - day — so Sunday maps back 6 days to Monday. Preserve this behavior.)
    - buildClassDateTime("2026-03-10", "09:30") returns a Date representing 2026-03-10 09:30:00 in Argentina time (UTC-3), i.e. 2026-03-10T12:30:00Z
    - buildClassDateTime("2026-03-10", "18:00") returns 2026-03-10T21:00:00Z
    - toDateString(date) returns "YYYY-MM-DD" string consistently regardless of timezone
    - resolveMonthRange() with no args returns current month start/end (first day to last day)
    - computePriorPeriod("2026-03-01", "2026-03-31") returns prior period of same length ending the day before
  </behavior>
  <action>
    RED phase: Write test file `el-templo-api/test/unit/date-utils.test.ts` with tests for all behaviors above. Import from `../../src/modules/shared/date-utils`. Run tests — they MUST fail (module doesn't exist yet).

    GREEN phase: Create `el-templo-api/src/modules/shared/date-utils.ts` with these pure functions:

    1. `addDays(dateStr: string, days: number): string` — Use noon UTC pattern (`"T12:00:00Z"`) consistently. Same as existing `addDays` in scheduling service but exported.

    2. `getWeekRange(date: Date): { monday: string; saturday: string }` — Convert to noon UTC internally before calculating to avoid DST edge cases. Preserve existing behavior: Sunday maps to prior week's Monday.

    3. `buildClassDateTime(bookingDate: string, timeStr: string): Date` — This is the KEY timezone fix. Given a date like "2026-03-10" and a time like "09:30" (in Argentina local time), return a Date object representing that moment. Use fixed UTC-3 offset: parse as `${bookingDate}T${timeStr}:00-03:00`. This is safe because Argentina does not observe DST (fixed UTC-3 year-round since 2009).

    4. `toDateString(date: Date): string` — Formats date as "YYYY-MM-DD" using UTC methods (getUTCFullYear, etc.) to avoid local timezone shifting.

    5. `resolveMonthRange(now?: Date): { dateFrom: string; dateTo: string }` — Returns first and last day of the month. Accept optional `now` param for testability. Use UTC methods internally.

    6. `computePriorPeriod(dateFrom: string, dateTo: string): { priorFrom: string; priorTo: string }` — Compute prior period of equal length. Use noon-UTC pattern for date arithmetic to avoid off-by-one.

    Run tests — they MUST pass.

    Update `el-templo-api/src/modules/shared/index.ts` to re-export all functions from `./date-utils`.

  </action>
  <verify>
    <automated>cd el-templo-api && pnpm test -- --filter=date-utils</automated>
  </verify>
  <done>All date utility tests pass. Functions are pure, timezone-explicit, and exported from shared module barrel.</done>
</task>

<task type="auto">
  <name>Task 2: Replace inline date logic in SchedulingService and AnalyticsService</name>
  <files>el-templo-api/src/modules/scheduling/service.ts, el-templo-api/src/modules/analytics/service.ts</files>
  <action>
    **SchedulingService** (`el-templo-api/src/modules/scheduling/service.ts`):

    1. Add import: `import { addDays, getWeekRange, buildClassDateTime, toDateString } from "../shared/date-utils";`

    2. Replace `private addDays()` method (line ~1572) — delete it entirely. All call sites already use `this.addDays(...)` — change to just `addDays(...)` (the imported function). There are 4 call sites: lines ~263, ~297, ~913, ~1197.

    3. Replace `private getWeekRange()` method (line ~1224) — delete it entirely. The one call site at line ~694 changes from `this.getWeekRange(new Date())` to `getWeekRange(new Date())`.

    4. FIX `private isWithinBookingWindow()` (line ~1242): Replace the body with:
       ```typescript
       const now = new Date();
       const classTime = buildClassDateTime(bookingDate, scheduleStartTime);
       const cutoff = new Date(classTime.getTime() - 5 * 60 * 1000);
       return now <= cutoff;
       ```
       This fixes the timezone bug: `buildClassDateTime` creates the correct Argentina-time Date, so `now` (also a Date) compares correctly regardless of server timezone.

    5. FIX `private isWithinCancelWindow()` (line ~1258): Same fix pattern:
       ```typescript
       const now = new Date();
       const classTime = buildClassDateTime(bookingDate, scheduleStartTime);
       const cutoff = new Date(classTime.getTime() - 20 * 60 * 1000);
       return now <= cutoff;
       ```

    6. Line ~706: `const dateObj = new Date(date + "T12:00:00")` — this is already safe (noon avoids day shift), but change to noon UTC for consistency: `new Date(date + "T12:00:00Z")`. The `.getUTCDay()` call on next line already uses UTC, so this is correct.

    **AnalyticsService** (`el-templo-api/src/modules/analytics/service.ts`):

    1. Add import: `import { resolveMonthRange, computePriorPeriod, toDateString } from "../shared/date-utils";`

    2. Replace `private resolveDefaults()` method (line ~954) body with:
       ```typescript
       if (filters.dateFrom && filters.dateTo) {
         return { dateFrom: filters.dateFrom, dateTo: filters.dateTo };
       }
       const defaults = resolveMonthRange();
       return {
         dateFrom: filters.dateFrom ?? defaults.dateFrom,
         dateTo: filters.dateTo ?? defaults.dateTo,
       };
       ```

    3. Replace `private computePriorPeriod()` method (line ~978) — delegate to imported function:
       ```typescript
       private computePriorPeriod(dateFrom: string, dateTo: string) {
         return computePriorPeriod(dateFrom, dateTo);
       }
       ```
       Or inline at call sites if there's only one or two. Check usage first.

    After changes, run the full test suite to verify no regressions.

  </action>
  <verify>
    <automated>cd el-templo-api && pnpm test</automated>
  </verify>
  <done>SchedulingService and AnalyticsService use shared date-utils. No private date methods remain in SchedulingService (addDays, getWeekRange deleted). Booking/cancel window checks use buildClassDateTime with proper Argentina timezone. Full test suite passes.</done>
</task>

</tasks>

<verification>
- `cd el-templo-api && pnpm test` passes (all existing + new unit tests)
- `grep -rn "T00:00:00\b" el-templo-api/src/modules/scheduling/service.ts` returns zero results (all midnight-without-timezone patterns removed from scheduling)
- `grep -n "private addDays\|private getWeekRange" el-templo-api/src/modules/scheduling/service.ts` returns zero results (methods extracted to shared)
</verification>

<success_criteria>

- Shared date-utils module exists with 6+ exported pure functions
- Unit tests cover timezone edge cases (month boundaries, year boundaries, Argentina UTC-3)
- isWithinBookingWindow and isWithinCancelWindow produce correct results for Argentina timezone
- No duplicate date utility code between scheduling and analytics services
- Full test suite green
  </success_criteria>

<output>
After completion, create `.planning/phases/53-codebase-health-timezone-fixes-god-object-decomposition-performance-optimization-test-coverage/53-01-SUMMARY.md`
</output>
