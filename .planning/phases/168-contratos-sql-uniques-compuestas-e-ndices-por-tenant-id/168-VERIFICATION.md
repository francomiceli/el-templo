---
phase: 168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id
verified: 2026-07-27T23:15:00Z
status: human_needed
score: 7/7 must-haves verified (automatizados)
overrides_applied: 0
human_verification:
  - test: "Smoke funcional por UI en producción: dar de alta un socio con un DNI que ya exista y ver que lo sigue rechazando; crear una sede con un código repetido y ver lo mismo; revisar que listado de socios, carga de un cobro y reporte de deuda no muestren ningún error nuevo"
    expected: "El staff ve exactamente el mismo comportamiento de rechazo de duplicados que tenía ayer (criterio 5 del ROADMAP: cero cambio de comportamiento), sin errores nuevos en las pantallas mencionadas"
    why_human: "Requiere observación visual y funcional real de las pantallas del admin; el smoke automatizado del pipeline (step 19 de cada deploy) y /health 200 no cubren la experiencia de uso real. Franco decidió explícitamente cerrar el plan 168-06 con esta etapa pendiente, igual que en las fases 166 y 167 (mismo patrón, mismo criterio transversal del milestone). Ítem harvested del propio checkpoint del plan 168-06 (`<how-to-verify>`, 'Chequeo funcional final')."
---

# Phase 168: Contratos SQL — uniques compuestas e índices por `tenant_id` — Verification Report

**Phase Goal:** Los contratos de unicidad del schema dejan de ser globales donde un segundo gimnasio colisionaría, y toda tabla gym-owned tiene un índice que arranca por `tenant_id` para que el filtro obligatorio sea barato. End state: dos tenants pueden convivir en la misma base sin que uno le bloquee al otro un email, un DNI, un código de sede, un código promo o un unsubscribe — y el tenant 1 sigue rechazando duplicados exactamente como hoy.

**Verified:** 2026-07-27T23:15:00Z
**Status:** human_needed
**Re-verification:** No — verificación inicial

## Nota metodológica

El código de la fase NO vive en el checkout principal (`/home/franco/projects/el-templo`, rama `fix/referral-preview-y-refresh-ficha`, atrasada, migraciones hasta 0181). Toda verificación de código se hizo contra el worktree `/home/franco/projects/et-168-contratos`, cuyo `HEAD` (`1200b8af`) es idéntico a `origin/master` (confirmado con `git log` y contra el `1200b8af` que el 168-06-SUMMARY cita como SHA final de prod).

Se corrieron comandos read-only propios, sin confiar en las cifras del SUMMARY sin recomputarlas:

- `npx tsc --noEmit` sobre el árbol completo del worktree (via symlink temporal de `node_modules` al worktree 167, tras `cmp` byte-idéntico del lockfile — mismo patrón documentado en el propio 168-06-SUMMARY — symlink borrado al terminar).
- `pnpm db:verify-uniques` contra la base **local** `eltemplo` — corrida en vivo, no leída de un log: **`DISCREPANCIAS: 0`, M8 11/11**.
- Grep estructural sobre la migración 0196 (conteo de `DROP INDEX`/`ADD UNIQUE INDEX`/`ADD INDEX`, ausencia de `;` en comentarios).
- Lectura completa de los 12 schema files, `tenant-tables.ts`, `verify-tenant-uniques.ts` y los tres archivos de test nuevos, verificando nombres de índice byte a byte contra el `.sql`.
- **NO se corrió la suite de tests localmente** (regla del proyecto — `no correr el suite de tests local`); se verificó la sustancia de los tres archivos de test por lectura (describe/it, helpers de duplicado, imports) en vez de ejecutarlos.
- **NO se hizo SSH a staging/prod** (regla del proyecto — pedir señal antes de SSH; ninguna señal fue dada en este pedido de verificación). Para `eltemplo_staging` y `eltemplo` se aceptó la evidencia ya documentada en 168-06-SUMMARY.md (salidas del verificador pegadas, SHAs de CI/Deploy con IDs de run verificables, `merge-base --is-ancestor` de la propia fase), que es internamente consistente con lo reproducido en local (mismo formato de reporte, mismos 12/0/4, mismas 11 M8).

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Status                                                                                            | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Las 12 uniques (11 de la lista D-01 + `subscription_plans`, hallada por el propio gate) pasan a `UNIQUE(tenant_id, …)`: un segundo tenant puede usar el mismo `users.email`/`dni`/`referral_code`, `branches.code`, `cost_centers(name,country)`, `promo_plans.promo_code`, `notification_templates.template_key`, `day_modes.day_of_week`, `holidays(country,date)`, `formats.name` y `subscription_plans(name,country)` sin violar unique, y sigue violándola dentro del mismo tenant (CON-01) | ✓ VERIFIED                                                                                        | `0196_tenant_unique_contracts.sql`: 12 `DROP INDEX` + 12 `ADD UNIQUE INDEX` (recontados con grep, no asumidos), todos con `tenant_id` primero. Schema Drizzle (13 archivos) declara los 12 índices byte a byte con el `.sql` (`grep` cruzado). `test/tenancy/con-01-uniques-cross-tenant.test.ts` (944 líneas) tiene un `describe` por contrato (12) con aserción cross-tenant + aserción de rechazo intra-tenant vía helper `buscarDuplicado` (ER_DUP_ENTRY/1062 específico, no error genérico), y estampa `tenantId` explícito en cada insert (17 usos del helper de rechazo)                        |
| 2   | `campaign_unsubscribes` (mina M3) acepta el mismo email en dos tenants, manteniendo el rechazo intra-tenant, incluido el caso `user_id` NULL                                                                                                                                                                                                                                                                                                                                                     | ✓ VERIFIED                                                                                        | Migración: `ALTER TABLE campaign_unsubscribes DROP INDEX uniq_campaign_unsubscribe_email, ADD UNIQUE INDEX uq_campaign_unsubscribes_tenant_email (tenant_id, email)`. Test: `describe("campaign_unsubscribes.email — uq_campaign_unsubscribes_tenant_email (mina M3)")` con dos `it`: baja normal + baja solo-email (`user_id` NULL)                                                                                                                                                                                                                                                                   |
| 3   | Las 11 uniques de la lista M8 (ids de plataforma Wellhub + secretos random con lookup pre-scope) siguen globales, con motivo anotado en el código y verificado por assert                                                                                                                                                                                                                                                                                                                        | ✓ VERIFIED                                                                                        | `TENANT_GLOBAL_UNIQUES` en `tenant-tables.ts` tiene exactamente 11 entradas (recontado con `tsx -e`), cada una con motivo no vacío en castellano. 6 schema files (`users.ts`, `branches.ts`, `wellhub.ts`, `refresh-tokens.ts`, `notifications.ts`, `tv.ts`) tienen comentario `M8` inmediatamente arriba de cada unique. `test/db/tenant-tables.test.ts` tiene el gate `"TENANT_GLOBAL_UNIQUES tiene exactamente 11 entradas (la lista M8 cerrada)"`. Corrida en vivo de `pnpm db:verify-uniques` contra la base local: `Uniques M8 encontradas y correctamente clasificadas como globales: 11 de 11` |
| 4   | Toda tabla gym-owned (87) tiene un índice cuyo primer campo es `tenant_id`, verificado por query a `information_schema` dentro de la suite (CON-02)                                                                                                                                                                                                                                                                                                                                              | ✓ VERIFIED                                                                                        | Corrida en vivo de `pnpm db:verify-uniques` contra `eltemplo` local: `Tablas gym-owned verificadas: 87`, `tablesWithoutTenantIndex: 0`. Migración 0196 no agrega ningún `INDEX(tenant_id)` a propósito (D-07: las FK de las migraciones 0192-0195 ya dejaron el índice auto-creado); verificado que `grep -c 'INDEX(tenant_id)'` sobre el `.sql` sin comentarios da 0. `test/migrations/0196-tenant-unique-contracts.test.ts` Test 6 prueba lo mismo desde la suite                                                                                                                                    |
| 5   | Cero cambio de comportamiento para el staff: alta de alumno, sedes, códigos promo, centros de costo y campañas siguen rechazando los duplicados que rechazaban ayer                                                                                                                                                                                                                                                                                                                              | ✓ VERIFIED (automatizado) / pendiente confirmación humana por UI — ver sección Human Verification | La mitad automatizada está probada por comportamiento real (inserts + `ER_DUP_ENTRY`) en `con-01-uniques-cross-tenant.test.ts`, no solo por introspección de metadata. La mitad "cero error nuevo en las pantallas de producción" requiere observación visual, diferida por decisión explícita de Franco (mismo patrón que 166 y 167)                                                                                                                                                                                                                                                                  |
| 6   | La migración 0196 quedó aplicada exactamente una vez en `eltemplo_staging` y en `eltemplo`, con `verify-tenant-uniques.js` en 0 discrepancias y exit 0 en las DOS bases reales                                                                                                                                                                                                                                                                                                                   | ✓ VERIFIED (evidencia documentada, no re-verificada por SSH propio)                               | 168-06-SUMMARY.md pega la salida completa del verificador contra cada base (mismo formato que la corrida local reproducida en esta verificación: `DISCREPANCIAS: 0`, M8 11/11, 87 tablas gym-owned, 12 contratos con `tenant_id` primero, 4 secundarios), con los IDs de run de CI/Deploy (`30305699087`, `30305699098`, `30307350316`, `30307350305`) citados como verificables por separado                                                                                                                                                                                                          |
| 7   | Ningún commit ajeno a la fase 168 (los 29 commits de CAJA/finance parados en `origin/staging`) viajó a producción                                                                                                                                                                                                                                                                                                                                                                                | ✓ VERIFIED                                                                                        | Documentado en 168-06-SUMMARY con `git merge-base --is-ancestor origin/staging HEAD` fallando antes del push a master, y el push a master resultó fast-forward (`68c447cf..1200b8af`, los mismos 10 SHAs verificados en este reporte con `git log` sobre el worktree local)                                                                                                                                                                                                                                                                                                                            |

**Score:** 7/7 truths con evidencia automatizada verificada; 1 ítem (mitad de la Truth 5) requiere confirmación humana antes de cerrar la fase.

### Required Artifacts

| Artifact                                                                                                                                                     | Expected                                                                               | Status                                     | Details                                                                                                                                                                                                                                                                    |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------- | ------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `el-templo-api/src/db/migrations/0196_tenant_unique_contracts.sql`                                                                                           | DDL de la tanda D: 11(+1) uniques compuestas + 4 índices secundarios                   | ✓ VERIFIED                                 | 156 líneas, 12 `DROP INDEX`/12 `ADD UNIQUE INDEX`/4 `ADD INDEX` (recontados), cero `;` en líneas de comentario                                                                                                                                                             |
| `src/db/schema/{users,branches,cost-centers,promo-plans,campaigns,notifications,day-modes,holidays,formats,wellhub,refresh-tokens,tv,subscription-plans}.ts` | 13 schema files alineados byte a byte con la 0196                                      | ✓ VERIFIED                                 | Los 12 nombres nuevos aparecen en `uniqueIndex(...)` con `table.tenantId` primero; ningún nombre viejo sobrevive como declaración (una sola mención en comentario histórico de `subscription-plans.ts`, explicado en el propio código); 11 comentarios M8 presentes        |
| `src/db/tenant-tables.ts`                                                                                                                                    | `TENANT_GLOBAL_UNIQUES` + `TENANT_UNIQUE_ALLOWLIST` + helpers, extendiendo la fase 167 | ✓ VERIFIED                                 | 453 líneas; `TENANT_GLOBAL_UNIQUES` 11 entradas, `TENANT_UNIQUE_ALLOWLIST` 37 entradas, `PLATFORM_PHYSICAL_TABLES`, `isTenantGlobalUnique`/`isAllowedGlobalUnique`/`tenantUniqueMotive` exportados; `GYM_OWNED_TABLES`=87 y `TENANT_EXEMPT_TABLES`=4 intactos (recontados) |
| `src/db/scripts/verify-tenant-uniques.ts`                                                                                                                    | `verifyTenantUniques(QueryFn)` + `formatReport` + CLI, solo lectura                    | ✓ VERIFIED                                 | Exporta ambas funciones; cero `any`; cero SQL de escritura (grep de `insert/update/delete/alter/drop/create` fuera de la conexión mysql2 no encuentra nada); corrida en vivo contra la base local: exit implícito 0, `DISCREPANCIAS: 0`                                    |
| `package.json` (script `db:verify-uniques`)                                                                                                                  | Script registrado                                                                      | ✓ VERIFIED                                 | `"db:verify-uniques": "tsx src/db/scripts/verify-tenant-uniques.ts"` presente                                                                                                                                                                                              |
| `test/tenancy/con-01-uniques-cross-tenant.test.ts`                                                                                                           | Los 11(+1) pares de aserciones cross-tenant/intra-tenant de CON-01                     | ✓ VERIFIED (sustancia, no ejecutado local) | 944 líneas (mín. 250), 12 `describe` de contrato + 1 test de cierre de limpieza, helper `buscarDuplicado` centraliza la detección de `ER_DUP_ENTRY`/1062 (17 usos), `tenantId` estampado explícito en cada insert (15 apariciones directas + spreads con constantes)       |
| `test/migrations/0196-tenant-unique-contracts.test.ts`                                                                                                       | Introspección de los 12 contratos + 4 secundarios + gate fail-closed del verificador   | ✓ VERIFIED (sustancia, no ejecutado local) | 638 líneas (mín. 200), 10 `it` cubriendo los 12 contratos con columnas completas, los 4 secundarios, ausencia de los 12 nombres viejos, idempotencia de `_migrations`, y 5 categorías del verificador + `discrepancies` con `formatReport` en el mensaje                   |
| `test/db/tenant-tables.test.ts`                                                                                                                              | Gates fail-closed de los registros de uniques, sumado a los 5 tests de la 167          | ✓ VERIFIED (sustancia, no ejecutado local) | 315 líneas, 5 tests originales de la 167 intactos (87/4/91) + 9 tests nuevos sobre `TENANT_GLOBAL_UNIQUES`/`TENANT_UNIQUE_ALLOWLIST`/`PLATFORM_PHYSICAL_TABLES`                                                                                                            |

### Key Link Verification

| From                                                   | To                                        | Via                                                               | Status                          | Details                                                                                                                                                                  |
| ------------------------------------------------------ | ----------------------------------------- | ----------------------------------------------------------------- | ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `src/db/schema/*.ts`                                   | `0196_tenant_unique_contracts.sql`        | nombre de índice idéntico byte a byte                             | ✓ WIRED                         | Los 12 nombres nuevos declarados en el schema aparecen literalmente en el `.sql`; verificado con grep cruzado en ambas direcciones                                       |
| `src/db/scripts/verify-tenant-uniques.ts`              | `src/db/tenant-tables.ts`                 | `import ... from "../tenant-tables"`                              | ✓ WIRED                         | Import confirmado en línea 74, consume `GYM_OWNED_TABLES`/`TENANT_EXEMPT_TABLES`/`TENANT_GLOBAL_UNIQUES`/`TENANT_UNIQUE_ALLOWLIST`                                       |
| `test/migrations/0196-tenant-unique-contracts.test.ts` | `src/db/scripts/verify-tenant-uniques.ts` | `import { verifyTenantUniques, formatReport }`                    | ✓ WIRED                         | Import confirmado; el test corre el verificador vía `makeQueryFn(app)` en un `beforeAll` único, no reimplementa queries                                                  |
| `test/tenancy/con-01-uniques-cross-tenant.test.ts`     | `src/db/schema`                           | `insert(schema.*)`                                                | ✓ WIRED                         | `import * as schema from "../../src/db/schema"` confirmado, usado en los 12 `describe`                                                                                   |
| `0196_tenant_unique_contracts.sql`                     | `eltemplo` (producción)                   | `deploy.yml → NODE_ENV=production node dist/db/run-migrations.js` | ✓ WIRED (evidencia documentada) | 168-06-SUMMARY: `_migrations` con una fila de la 0196 en `eltemplo` tras el deploy de producción (run `30307350305`), step "Run database migrations" en 4s, exit success |

### Requirements Coverage

| Requirement | Source Plan                | Description                                                                                         | Status      | Evidence                                                         |
| ----------- | -------------------------- | --------------------------------------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------- |
| CON-01      | 168-01, 02, 03, 04, 05, 06 | Uniques globales convertidas a compuestas `(tenant_id, …)` según doc 06 §1-D; lista M8 queda global | ✓ SATISFIED | Truths 1-3 arriba; marcado `[x]` y "Complete" en REQUIREMENTS.md |
| CON-02      | 168-01, 03, 05, 06         | Toda tabla gym-owned tiene índice con prefijo `tenant_id` en la misma migración                     | ✓ SATISFIED | Truth 4 arriba; marcado `[x]` y "Complete" en REQUIREMENTS.md    |

Sin requirements huérfanos: REQUIREMENTS.md solo mapea CON-01/CON-02 a la fase 168, y ambos aparecen declarados en al menos un plan.

### Anti-Patterns Found

| File                                                                                   | Line             | Pattern                                                                                                                                                    | Severity                                          | Impact                                                                                                                                            |
| -------------------------------------------------------------------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/db/scripts/verify-tenant-uniques.ts`                                              | 333-343, 395-419 | `staleClassifications` no filtra tablas físicamente ausentes, a diferencia de `uniquesMissingTenantPrefix` (asimetría)                                     | ℹ️ Info (documentado como WR-01 en 168-REVIEW.md) | Solo dispara si una tabla de `GYM_OWNED_TABLES` desaparece de la base verificada; no ocurre hoy contra staging/prod completos                     |
| `src/db/scripts/verify-tenant-uniques.ts`                                              | 19, 57, 421      | Docblock dice "11 contratos" en 3 lugares; el array real tiene 12                                                                                          | ℹ️ Info (WR-02)                                   | Cosmético — no afecta el comportamiento del gate, `CONVERTED_CONTRACTS.length` = 12 en runtime                                                    |
| `src/modules/subscriptions/service.ts`, `cash-register-service.ts`, 2 archivos de test | varios           | Comentarios de producción referencian nombres de índice viejos (`ux_subscription_plans_name_country`, `uq_cost_centers_name_country`) que la 0196 renombró | ℹ️ Info (WR-03)                                   | Cosmético — el matching de duplicado en runtime es por `ER_DUP_ENTRY`/mensaje genérico, no por nombre                                             |
| `test/helpers.ts`                                                                      | 257-262          | `cleanAllTestData` protege `admin@test.com` sin filtrar por `tenant_id` — hueco latente para las fixtures 2-tenant de la fase 171                          | ℹ️ Info (WR-04)                                   | No dispara hoy (con-01 usa emails `*@tenancy.test`); riesgo diferido explícitamente a la fase 171 (ISO-03)                                        |
| —                                                                                      | —                | Ningún `TBD`/`FIXME`/`XXX` sin referencia en los 20 archivos de la fase                                                                                    | —                                                 | Único match es dentro de una regex de test que verifica la AUSENCIA de esos marcadores (`test/db/tenant-tables.test.ts:210`), no un marcador real |

0 hallazgos críticos (168-REVIEW.md: 0 critical, 4 warning, 4 info — los 4 warnings de arriba son de robustez/documentación, ninguno afecta el contrato ya desplegado).

### Behavioral Spot-Checks

| Behavior                                                                                                          | Command                                                                                                                                     | Result                                                                                                                        | Status |
| ----------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | ------ |
| El verificador de uniques corre en vivo contra la base local y reporta 0 discrepancias                            | `pnpm db:verify-uniques` (worktree `et-168-contratos`)                                                                                      | `DISCREPANCIAS: 0`, 87 tablas gym-owned, 60 uniques evaluadas, M8 11/11                                                       | ✓ PASS |
| El schema Drizzle typechea limpio con las 12 uniques compuestas declaradas                                        | `npx tsc --noEmit` (worktree completo, symlink temporal a `node_modules` de `et-167-columnas`, lockfile byte-idéntico confirmado con `cmp`) | exit 0, sin errores                                                                                                           | ✓ PASS |
| Los 12 nombres nuevos de índice existen en el `.sql` y en el schema; los 12 viejos no sobreviven como declaración | grep cruzado sobre `0196_tenant_unique_contracts.sql` y `src/db/schema/`                                                                    | 12/12 nuevos presentes en ambos lados; único match de nombre viejo es un comentario histórico explicativo, no una declaración | ✓ PASS |

### Human Verification Required

### 1. Smoke funcional por UI en producción (criterio 5 del ROADMAP)

**Test:** Dar de alta un socio con un DNI que ya exista y verificar que se sigue rechazando; crear una sede con un código de sede repetido y verificar lo mismo; revisar que el listado de socios, la carga de un cobro y el reporte de deuda no muestren ningún error nuevo.
**Expected:** El staff ve exactamente el mismo comportamiento de rechazo de duplicados que tenía ayer, sin ningún error nuevo en las pantallas mencionadas.
**Why human:** Requiere observación visual y funcional real de las pantallas del admin en producción; el smoke automatizado del pipeline (step 19 de cada deploy, verde en los dos entornos) y `/health` 200 no cubren la experiencia de uso real. La mitad de comportamiento a nivel de base de datos (inserts + `ER_DUP_ENTRY`) ya está probada automáticamente en `con-01-uniques-cross-tenant.test.ts`, pero eso no reemplaza la vista del staff. Franco decidió explícitamente cerrar el plan 168-06 con esta etapa pendiente, igual que en las fases 166 y 167 (mismo patrón documentado en ambos VERIFICATION.md previos, estado `human_needed`).

### Gaps Summary

No hay gaps: los 7 truths automatizables están VERIFIED con evidencia recomputada en esta verificación (no solo leída del SUMMARY), y los 4 warnings del code review son de robustez/documentación, ninguno bloquea el contrato ya desplegado. El único pendiente es el smoke funcional por UI en producción, explícitamente diferido por decisión de Franco con el mismo patrón usado en las fases 166 y 167 — no es una falla de la fase, es un checkpoint humano abierto.

---

_Verified: 2026-07-27T23:15:00Z_
_Verifier: Claude (gsd-verifier)_
