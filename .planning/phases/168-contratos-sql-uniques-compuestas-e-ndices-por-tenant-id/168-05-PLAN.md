---
phase: 168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id
plan: 05
type: execute
wave: 4
depends_on: ["168-01", "168-03"]
files_modified:
  - el-templo-api/test/migrations/0196-tenant-unique-contracts.test.ts
  - el-templo-api/test/db/tenant-tables.test.ts
autonomous: true
requirements: [CON-01, CON-02]

must_haves:
  truths:
    - "La suite falla si la 0196 se aplicó a medias, aunque el runner haya registrado la migración"
    - "La suite falla si alguna tabla gym-owned queda sin índice cuyo primer campo sea tenant_id"
    - "La suite falla si aparece una unique global nueva sin clasificar en tenant-tables.ts"
    - "La suite falla si una entrada de los registros apunta a un índice que no existe"
  artifacts:
    - path: "el-templo-api/test/migrations/0196-tenant-unique-contracts.test.ts"
      provides: "Asserts de introspección de los 11 contratos compuestos + los 4 índices secundarios + el gate fail-closed del verificador"
      min_lines: 200
    - path: "el-templo-api/test/db/tenant-tables.test.ts"
      provides: "Gates fail-closed de los registros de uniques, además de los 5 tests de tablas de la fase 167"
      contains: "TENANT_GLOBAL_UNIQUES"
  key_links:
    - from: "el-templo-api/test/migrations/0196-tenant-unique-contracts.test.ts"
      to: "el-templo-api/src/db/scripts/verify-tenant-uniques.ts"
      via: "adaptador QueryFn sobre app.db.execute(sql.raw(...))"
      pattern: "verifyTenantUniques"
---

<objective>
Convertir el verificador del plan 168-03 en un gate permanente de CI y probar por
introspección que la migración 0196 quedó aplicada ENTERA: las 11 uniques compuestas con
`tenant_id` primero, los 4 índices secundarios, los 11 nombres viejos desaparecidos, y las
tres condiciones fail-closed de D-11 y D-14.

Purpose: el provisioning de la base de test tolera "Can't DROP" y "Duplicate key name" e
inserta la fila en `_migrations` igual, así que una migración de DDL rota puede pasar en
silencio con la suite entera en verde. Sin este archivo, la fase 168 no tiene evidencia.
Output: un test de introspección nuevo y el test de clasificación de la 167 ampliado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-CONTEXT.md
@.planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-PATTERNS.md
@.planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-01-SUMMARY.md
@.planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-03-SUMMARY.md

<interfaces>
Los 11 contratos compuestos que el test tiene que encontrar (nombre final y orden exacto de
columnas — si el plan 01 registró alguna desviación, manda el `.sql`):

| tabla                  | índice                                | columnas en orden        |
| ---------------------- | ------------------------------------- | ------------------------ |
| users                  | uq_users_tenant_email                 | tenant_id, email         |
| users                  | uq_users_tenant_dni                   | tenant_id, dni           |
| users                  | uq_users_tenant_referral_code         | tenant_id, referral_code |
| branches               | uq_branches_tenant_code               | tenant_id, code          |
| cost_centers           | uq_cost_centers_tenant_name_country   | tenant_id, name, country |
| promo_plans            | uq_promo_plans_tenant_promo_code      | tenant_id, promo_code    |
| campaign_unsubscribes  | uq_campaign_unsubscribes_tenant_email | tenant_id, email         |
| notification_templates | uq_notification_templates_tenant_key  | tenant_id, template_key  |
| day_modes              | uq_day_modes_tenant_day_of_week       | tenant_id, day_of_week   |
| holidays               | uq_holidays_tenant_country_date       | tenant_id, country, date |
| formats                | uq_formats_tenant_name                | tenant_id, name          |

Índices secundarios que tienen que existir con `NON_UNIQUE = 1`: `idx_users_email`,
`idx_users_dni`, `idx_users_referral_code`, `idx_campaign_unsubscribes_email`.

Índices viejos que NO pueden sobrevivir: `users_email_unique`, `users_dni_unique`,
`users_referral_code_unique`, `branches_code_unique`, `uq_cost_centers_name_country`,
`promo_plans_promo_code_unique`, `uniq_campaign_unsubscribe_email`,
`notification_templates_template_key_unique`, `day_modes_day_of_week_unique`,
`idx_holidays_country_date`, `formats_name_unique`.

Analogs a copiar:

- `test/migrations/0190-0191-tenants.test.ts` — el único test del repo que interroga
  `INFORMATION_SCHEMA.STATISTICS` para verificar una unique COMPUESTA y el orden de sus
  columnas. De ahí salen el bloque "POR QUÉ EXISTE ESTE ARCHIVO", la interfaz `IndexRow`
  (INDEX_NAME, COLUMN_NAME, SEQ_IN_INDEX, NON_UNIQUE), el helper `queryRows` que normaliza
  el `[rows, fields]` de mysql2, el helper `getIndexRows` y el ciclo de vida
  `createTestApp` + `cleanAllTestData`.
- `test/migrations/0192-0195-tenant-columns.test.ts` — el estilo fail-closed obligatorio
  (`expect(valor, "mensaje que dice QUÉ falla y qué hacer").toEqual([])`, nunca un `toBe(0)`
  pelado), el adaptador `makeQueryFn(app)` que envuelve `app.db.execute(sql.raw(...))` en un
  `QueryFn`, y el assert de idempotencia sobre `_migrations` con la trampa del `LIKE`
  documentada (MySQL `LIKE '019[2-5]%'` matchea los caracteres literales y devuelve 0
  siempre: lista explícita de nombres).
- `test/db/tenant-tables.test.ts` — los 5 tests fail-closed de la fase 167 y sus conteos
  duros 87/4/91, que la 168 NO cambia.

`Number(...)` obligatorio antes de comparar `NON_UNIQUE` y `SEQ_IN_INDEX`: vienen numéricos
pero el repo los castea siempre.

Regla del repo: la suite completa corre en CI. Está permitido correr estos archivos solos
para sanity-check.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test de introspección de la migración 0196</name>
  <files>el-templo-api/test/migrations/0196-tenant-unique-contracts.test.ts</files>
  <read_first>
    - el-templo-api/test/migrations/0190-0191-tenants.test.ts (helpers IndexRow / queryRows / getIndexRows, assert de unique compuesta, ciclo de vida)
    - el-templo-api/test/migrations/0192-0195-tenant-columns.test.ts (estilo fail-closed con mensaje accionable y assert de idempotencia sobre _migrations)
    - el-templo-api/src/db/migrations/0196_tenant_unique_contracts.sql (los nombres reales, por si hubo desviación)
    - el-templo-api/test/setup.ts (la lista de errores tolerados por el provisioning, entre ellos "Can't DROP" — el motivo de que este archivo exista)
  </read_first>
  <action>
    Crear `test/migrations/0196-tenant-unique-contracts.test.ts` con el bloque de cabecera
    "POR QUÉ EXISTE ESTE ARCHIVO" adaptado a esta migración: `test/setup.ts` tolera
    literalmente "Can't DROP", así que un `DROP INDEX` con nombre equivocado deja la suite en
    verde y la base sin convertir. La verificación va contra `INFORMATION_SCHEMA`, nunca
    contra `_migrations`.

    Copiar los helpers de introspección del analog (`IndexRow`, `queryRows`, `getIndexRows`)
    en vez de inventar otros, y usar el mismo ciclo de vida (`createTestApp` en `beforeAll`,
    `cleanAllTestData` en `beforeEach` y `afterAll`, `app.close()`).

    Tres bloques de aserciones:
    (a) Los 11 contratos compuestos: por cada uno, las filas de `STATISTICS` en el orden
    exacto de la tabla de interfaces, con `tenant_id` en `SEQ_IN_INDEX = 1`, la cantidad de
    columnas correcta (2 o 3 según el contrato) y `NON_UNIQUE = 0`. Los de tres columnas
    (`cost_centers`, `holidays`) se verifican completos, no solo su primer campo.
    (b) Los 4 índices secundarios de D-05 existen con `NON_UNIQUE = 1` sobre la columna
    esperada, y los 11 nombres viejos NO existen en ninguna tabla de la base.
    (c) Idempotencia: `_migrations` tiene exactamente una fila para
    `0196_tenant_unique_contracts.sql`, consultada con un `IN` de nombre explícito y nunca
    con `LIKE` de clase de caracteres (la trampa documentada en el analog).

    Todos los `expect` en el estilo fail-closed del repo: comparar un array de hallazgos
    contra `[]` con un mensaje que diga qué índice falla, qué se esperaba y qué hacer. Un
    fallo que solo diga "esperaba 0" manda a bisecar la migración a mano.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-168-contratos/el-templo-api &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run test/migrations/0196-tenant-unique-contracts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/migrations/0196-tenant-unique-contracts.test.ts` pasa en verde
    - Los 11 nombres de índice nuevos y los 11 viejos aparecen literalmente en el archivo de test
    - Los contratos de `cost_centers` y `holidays` se verifican con sus 3 columnas y su orden, no solo con la primera
    - El assert de `_migrations` usa una lista explícita de nombres, sin `LIKE`
    - Ningún `expect` del archivo es un `toBe(0)` pelado: todos comparan contra `[]` o valores concretos con un segundo argumento de mensaje accionable
    - El archivo no ejecuta ningún DDL ni escribe en tablas de datos: solo `SELECT` sobre `INFORMATION_SCHEMA` y sobre `_migrations`
    - `npx tsc --noEmit` sale 0 y no hay `any` en el archivo
  </acceptance_criteria>
  <done>La suite ya no puede quedar en verde con la 0196 aplicada a medias.</done>
</task>

<task type="auto">
  <name>Task 2: Conectar el verificador a la suite como gate fail-closed de CON-02 y D-14</name>
  <files>el-templo-api/test/migrations/0196-tenant-unique-contracts.test.ts</files>
  <read_first>
    - el-templo-api/src/db/scripts/verify-tenant-uniques.ts (firma de verifyTenantUniques, campos del reporte y formatReport, del plan 168-03)
    - el-templo-api/test/migrations/0192-0195-tenant-columns.test.ts (el adaptador makeQueryFn(app) y cómo el analog invoca al verificador desde la suite)
    - el-templo-api/test/migrations/0196-tenant-unique-contracts.test.ts (lo escrito en el Task 1)
  </read_first>
  <action>
    Agregar al mismo archivo un `describe` que corra el verificador una sola vez en un
    `beforeAll` (es caro: recorre `information_schema` entero) y derive un test por categoría
    de hallazgo, con el mismo adaptador `makeQueryFn(app)` del analog de la fase 167 para que
    el CLI y la suite ejecuten LITERALMENTE el mismo código.

    Un test por categoría, cada uno comparando el array del reporte contra `[]`:
    - `uniquesMissingTenantPrefix` — D-14: toda unique de tabla gym-owned arranca con
      `tenant_id` o está clasificada. El mensaje de fallo tiene que decir tabla, índice,
      primera columna encontrada y las dos salidas posibles (componer el índice con
      `tenant_id` o clasificarlo en `tenant-tables.ts` con el motivo escrito).
    - `tablesWithoutTenantIndex` — CON-02 / D-11(a): toda tabla gym-owned tiene un índice
      cuyo primer campo es `tenant_id`, contando los auto-creados por las FK de la fase 167.
    - `unclassifiedTables` — D-11(b): toda tabla física está clasificada. El mensaje
      referencia las tres listas de `tenant-tables.ts`.
    - `staleClassifications` — anti-podredumbre: ninguna entrada de los registros apunta a un
      índice inexistente.
    - `missingConvertedContracts` — CON-01 estructural desde el verificador, redundante a
      propósito con el Task 1: si los dos caminos discrepan, algo está mal en el verificador.

    Agregar además un test que afirme que `discrepancies` es 0 y que imprima
    `formatReport(report)` en el mensaje del `expect`, para que un fallo en CI traiga el
    reporte completo en el log en vez de obligar a reproducir local.

    No duplicar la lógica de clasificación dentro del test: si una aserción necesita saber si
    una unique es aceptable, la respuesta la da el verificador. Este archivo consume, no
    reimplementa.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-168-contratos/el-templo-api &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run test/migrations/0196-tenant-unique-contracts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/migrations/0196-tenant-unique-contracts.test.ts` pasa en verde con los tests de las 5 categorías más el de `discrepancies`
    - El test importa `verifyTenantUniques` y `formatReport` desde `src/db/scripts/verify-tenant-uniques` — no reimplementa ninguna query de clasificación
    - El verificador se ejecuta UNA sola vez por archivo (en un `beforeAll`), no una vez por test
    - Cada `expect` de categoría compara contra `[]` con un mensaje que nombra la categoría y dice qué hacer
    - El test de `discrepancies` incluye `formatReport(report)` en el mensaje del `expect`
    - Insertar temporalmente una unique global de prueba sobre una tabla gym-owned deja el test en ROJO (prueba manual del fail-closed, revertida antes de cerrar el task y documentada en el SUMMARY)
    - `npx tsc --noEmit` sale 0
  </acceptance_criteria>
  <done>CI falla si aparece una unique sin scope o una tabla gym-owned sin índice de prefijo tenant_id, con el reporte completo en el log.</done>
</task>

<task type="auto">
  <name>Task 3: Ampliar el test de clasificación de la fase 167 con los registros de uniques</name>
  <files>el-templo-api/test/db/tenant-tables.test.ts</files>
  <read_first>
    - el-templo-api/test/db/tenant-tables.test.ts (los 5 tests existentes, sus mensajes fail-closed y los conteos duros 87/4/91)
    - el-templo-api/src/db/tenant-tables.ts (TENANT_GLOBAL_UNIQUES, TENANT_UNIQUE_ALLOWLIST, PLATFORM_PHYSICAL_TABLES y los helpers del plan 168-03)
  </read_first>
  <action>
    Extender el archivo existente sin tocar los 5 tests de la fase 167 ni sus conteos: la 168
    no agrega tablas, así que 87 / 4 / 91 tienen que seguir intactos y en verde. Un rojo ahí
    es señal de que se rompió otra cosa.

    Agregar un `describe` nuevo, sin base de datos (pura introspección de los objetos
    exportados, igual que el resto del archivo), con estos gates:
    - Toda clave de los dos registros tiene el formato `tabla.indice` con exactamente un
      punto separador, y la parte de tabla existe en `GYM_OWNED_TABLES`. Una clave sobre una
      tabla exenta o inexistente no tiene sentido y es un typo.
    - Ninguna clave está en los dos registros a la vez (mismo espíritu que el test de "en las
      dos listas" de la 167).
    - Todo motivo es una cadena no vacía y con contenido real: rechazar cadenas vacías, solo
      espacios, y los marcadores de trabajo pendiente (`TODO`, `FIXME`, `pendiente`, `TBD`).
    - `TENANT_GLOBAL_UNIQUES` tiene exactamente 11 entradas — es la lista M8 cerrada y
      aprobada el 2026-07-26, y agregar una duodécima es una decisión de diseño, no un
      detalle de implementación. El mensaje del fallo tiene que decir eso.
    - Los helpers `isTenantGlobalUnique`, `isAllowedGlobalUnique` y `tenantUniqueMotive`
      responden bien sobre una entrada M8 conocida, sobre una de la allowlist y sobre un par
      inexistente.
    - `PLATFORM_PHYSICAL_TABLES` no se solapa con `GYM_OWNED_TABLES` ni con
      `TENANT_EXEMPT_TABLES`.

    Todos los mensajes en el estilo fail-closed del archivo: decir qué entrada falla y qué
    hacer, nunca un `toBe` pelado.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-168-contratos/el-templo-api &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run test/db/tenant-tables.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/db/tenant-tables.test.ts` pasa en verde, incluidos los 5 tests originales de la fase 167
    - Los conteos duros del test original siguen siendo 87 gym-owned, 4 exentas y 91 tablas del schema, sin modificar
    - El `describe` nuevo cubre: formato de clave, tabla existente en `GYM_OWNED_TABLES`, sin claves duplicadas entre registros, motivos no vacíos y sin marcadores de pendiente, 11 entradas exactas en `TENANT_GLOBAL_UNIQUES`, comportamiento de los 3 helpers y no solapamiento de `PLATFORM_PHYSICAL_TABLES`
    - El test no toca la base de datos: no importa `createTestApp` ni ejecuta SQL
    - Ningún `expect` nuevo es un `toBe` sin mensaje: todos llevan segundo argumento accionable
    - `npx tsc --noEmit` sale 0
  </acceptance_criteria>
  <done>Los registros de uniques tienen el mismo gate anti-podredumbre que ya tenían las listas de tablas.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                | Description                                                                    |
| ------------------------------------------------------- | ------------------------------------------------------------------------------ |
| provisioning de la base de test → veracidad de la suite | `test/setup.ts` tolera errores de DDL e inserta la fila de `_migrations` igual |
| registros escritos a mano → realidad de la base         | una entrada obsoleta convierte el gate en decorativo                           |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                        | Disposition | Mitigation Plan                                                                                                                                                                |
| --------- | ---------------------- | ---------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-168-19  | Tampering              | migración aplicada a medias con la suite en verde                | mitigate    | Todo se verifica contra `INFORMATION_SCHEMA`, nunca contra `_migrations`, y los 11 nombres viejos se exigen ausentes además de exigir presentes los nuevos                     |
| T-168-20  | Elevation of Privilege | unique global nueva colada en una fase futura                    | mitigate    | El gate `uniquesMissingTenantPrefix` es fail-closed y se prueba a mano insertando una unique de prueba que debe dejar el test en rojo                                          |
| T-168-21  | Repudiation            | fallo en CI sin información suficiente para diagnosticar         | mitigate    | `formatReport(report)` va dentro del mensaje del `expect` de `discrepancies`: el log de CI trae el reporte completo                                                            |
| T-168-22  | Tampering              | test que reimplemente la clasificación y diverja del verificador | mitigate    | El test consume `verifyTenantUniques`, no duplica queries. `missingConvertedContracts` es redundante a propósito con el Task 1 para detectar divergencia entre los dos caminos |
| T-168-23  | Tampering              | allowlist podrida apuntando a índices inexistentes               | mitigate    | Categoría `staleClassifications` en el reporte más el gate de formato de clave en `tenant-tables.test.ts`                                                                      |
| T-168-SC  | Tampering              | instalación de paquetes npm                                      | mitigate    | Solo `vitest` y `drizzle-orm`, ya presentes. Cero dependencias nuevas, cero `pnpm install`                                                                                     |

</threat_model>

<verification>
- `npx tsc --noEmit` limpio.
- `npx vitest run test/migrations/0196-tenant-unique-contracts.test.ts` en verde.
- `npx vitest run test/db/tenant-tables.test.ts` en verde con los conteos 87/4/91 intactos.
- Prueba manual del fail-closed hecha y revertida, documentada en el SUMMARY.
- Sin commit: el commit único de la fase lo arma el plan 168-06.
</verification>

<success_criteria>

- Los 11 contratos compuestos y los 4 índices secundarios verificados por introspección.
- CON-02 (D-11) y las uniques (D-14) verificados fail-closed desde la suite, con el mismo código que corre el CLI contra staging y prod.
- Los registros de uniques con gate anti-podredumbre.
  </success_criteria>

<output>
Create `.planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-05-SUMMARY.md` when done
</output>
