---
phase: 168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id
plan: 04
type: execute
wave: 3
depends_on: ["168-01", "168-02"]
files_modified:
  - el-templo-api/test/tenancy/con-01-uniques-cross-tenant.test.ts
autonomous: true
requirements: [CON-01]

must_haves:
  truths:
    - "Un segundo tenant puede usar el mismo email, DNI, código de referido, código de sede, centro de costo, código promo, unsubscribe, template key, day mode, feriado y formato que el tenant 1"
    - "Repetir cualquiera de esos valores DENTRO del mismo tenant sigue violando la unique, igual que ayer"
    - "campaign_unsubscribes acepta el mismo email en dos tenants (mina M3 cerrada en su mitad de schema)"
    - "El test limpia el tenant 2 y sus filas: ningún archivo vecino de la suite queda contaminado"
  artifacts:
    - path: "el-templo-api/test/tenancy/con-01-uniques-cross-tenant.test.ts"
      provides: "Los 11 pares de aserciones cross-tenant / intra-tenant de CON-01"
      min_lines: 250
  key_links:
    - from: "el-templo-api/test/tenancy/con-01-uniques-cross-tenant.test.ts"
      to: "el-templo-api/src/db/schema"
      via: "inserts de Drizzle contra las tablas convertidas"
      pattern: "insert\\(schema\\."
---

<objective>
Probar por COMPORTAMIENTO —no por introspección— los criterios de éxito 1, 2 y 5 del
ROADMAP: sembrar un segundo tenant, insertar los mismos valores que ya tiene el tenant 1 en
las 11 tablas convertidas y ver que MySQL los acepta, y repetir cada valor dentro del mismo
tenant y ver que los sigue rechazando.

Purpose: la introspección de índices prueba que el DDL quedó bien, pero no que el contrato
que le importa al staff siga vivo. Esto es lo que garantiza que "el alta de alumno sigue
rechazando el DNI duplicado" no se rompió.
Output: `test/tenancy/con-01-uniques-cross-tenant.test.ts` con un par de aserciones por
contrato convertido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-CONTEXT.md
@.planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-PATTERNS.md
@.planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-01-SUMMARY.md
@.planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-02-SUMMARY.md

<interfaces>
Los 11 contratos a probar, con la columna que se duplica:

| tabla                  | columnas del contrato (después de tenant_id) |
| ---------------------- | -------------------------------------------- |
| users                  | email                                        |
| users                  | dni                                          |
| users                  | referral_code                                |
| branches               | code                                         |
| cost_centers           | name, country                                |
| promo_plans            | promo_code                                   |
| campaign_unsubscribes  | email                                        |
| notification_templates | template_key                                 |
| day_modes              | day_of_week                                  |
| holidays               | country, date                                |
| formats                | name                                         |

Infraestructura de test existente que hay que usar y NO reinventar:

- `test/helpers.ts` expone `createTestApp()` y `cleanAllTestData(app)`. `cleanAllTestData`
  borra las tablas de `TABLES_TO_CLEAN` y hace un `DELETE FROM users WHERE NOT (email <=> 'admin@test.com')`
  con comparación NULL-safe. `tenants` NO está en `TABLES_TO_CLEAN`.
- La fila `tenants id=1` la siembra la migración 0190 y existe siempre. El tenant 2 es un
  `INSERT` directo en `beforeAll` y un `DELETE` en `afterAll`, con un `id` fijo y alto para
  no chocar con nada.
- Tablas que NO están en `TABLES_TO_CLEAN` (hay que borrar sus filas en un `finally`, como
  hace `test/migrations/0190-0191-tenants.test.ts` con `branches`): verificarlo leyendo la
  constante, no asumirlo.
- Ciclo de vida del repo: `createTestApp()` en `beforeAll`, `cleanAllTestData` en
  `beforeEach` y en `afterAll`, `app.close()` al final.
- Las fixtures 2-tenant completas son trabajo de la fase 171: acá los helpers son mínimos y
  locales al archivo, no se adelanta esa API.

Regla del repo sobre correr tests: la suite completa corre en CI, no local. Está permitido
correr ESTE archivo solo para sanity-check.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold, seeding del tenant 2 y los contratos de users y branches</name>
  <files>el-templo-api/test/tenancy/con-01-uniques-cross-tenant.test.ts</files>
  <behavior>
    - users.email: el mismo email existe en tenant 1 y en tenant 2 sin error; repetirlo dentro del tenant 1 rechaza por unique
    - users.dni: idéntico patrón sobre `dni`
    - users.referral_code: idéntico patrón sobre `referral_code`
    - branches.code: el mismo código de sede en dos tenants sin error; repetido dentro del tenant rechaza
    - El tenant 2 se siembra una vez y se borra al final; ninguna fila del tenant 2 sobrevive al archivo
  </behavior>
  <read_first>
    - el-templo-api/test/helpers.ts (createTestApp, cleanAllTestData, TABLES_TO_CLEAN, el DELETE NULL-safe de users)
    - el-templo-api/test/migrations/0190-0191-tenants.test.ts (ciclo de vida beforeAll/beforeEach/afterAll y el patrón de limpieza en `finally` para tablas fuera de TABLES_TO_CLEAN)
    - el-templo-api/src/db/schema/users.ts y branches.ts (columnas obligatorias que hay que completar en cada insert, después de los cambios del plan 168-02)
    - el-templo-api/src/db/schema/tenants.ts (columnas de `tenants`: slug, status y demás obligatorias para sembrar el tenant 2)
  </read_first>
  <action>
    Crear `test/tenancy/con-01-uniques-cross-tenant.test.ts` con un bloque de cabecera que
    explique POR QUÉ existe, en el estilo del repo: la introspección de índices verifica el
    DDL, pero solo un insert real prueba que el contrato que le importa al staff sigue vivo,
    y que la mitad "sigue rechazando dentro del tenant" es tan importante como la mitad
    "acepta cross-tenant" (criterio 5 del ROADMAP: cero cambio de comportamiento).

    Sembrar el tenant 2 en `beforeAll` con un `id` fijo y alto (para no colisionar con
    autoincrementos ni con la fila 1 de la migración 0190), slug propio y status `active`, y
    borrarlo en `afterAll` después de `cleanAllTestData`. Definir dos constantes locales con
    los ids de tenant para que ninguna aserción use números mágicos sueltos.

    Escribir un helper local mínimo que ejecute un insert y devuelva si falló por violación
    de unique, distinguiendo el error de duplicado de MySQL (`ER_DUP_ENTRY` / código 1062) de
    cualquier otro error: un test que acepte "falló por lo que sea" pasaría en verde con una
    FK rota o una columna faltante y no probaría nada. Las aserciones de rechazo tienen que
    exigir específicamente el duplicado.

    Cubrir los cuatro contratos de este task (users email, users dni, users referral_code,
    branches code) con un par de aserciones cada uno: primero insertar la fila del tenant 1,
    después la del tenant 2 con el MISMO valor (debe pasar), después una segunda del tenant 1
    con el mismo valor (debe rechazar por duplicado). Completar todas las columnas
    obligatorias de cada tabla leyendo el schema, y estampar `tenantId` explícitamente en
    cada insert (la columna tiene DEFAULT 1, así que omitirla haría que el "tenant 2" fuera
    en realidad tenant 1 y el test pasaría por el motivo equivocado — esa es la trampa
    central de este archivo).

    Limpiar en `finally` las filas de las tablas que no estén en `TABLES_TO_CLEAN` — verificar
    cuáles leyendo la constante de `test/helpers.ts`, no asumirlo.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-168-contratos/el-templo-api &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run test/tenancy/con-01-uniques-cross-tenant.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sale 0 y el archivo no usa el tipo `any`
    - `npx vitest run test/tenancy/con-01-uniques-cross-tenant.test.ts` pasa en verde
    - Hay 8 aserciones (4 contratos por 2: acepta cross-tenant / rechaza intra-tenant)
    - Cada aserción de rechazo verifica específicamente el duplicado de MySQL (`ER_DUP_ENTRY` o código 1062), no un error genérico
    - Todo insert pasa `tenantId` de forma explícita: `grep -c "tenantId" ` sobre el archivo es mayor o igual a la cantidad de inserts
    - Tras correr el archivo, `SELECT COUNT(*) FROM tenants` en la base de test vuelve a 1 y no queda ninguna fila con `tenant_id = 2` en `users` ni en `branches`
    - El archivo no importa ninguna fixture de la fase 171 (no existe todavía) ni agrega helpers a `test/helpers.ts`
  </acceptance_criteria>
  <done>El archivo existe, siembra y limpia el tenant 2, y prueba los 4 contratos de users y branches en sus dos direcciones.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Los 7 contratos restantes, incluida la mina M3 de campaign_unsubscribes</name>
  <files>el-templo-api/test/tenancy/con-01-uniques-cross-tenant.test.ts</files>
  <behavior>
    - cost_centers (name, country): el mismo par en dos tenants sin error; repetido dentro del tenant rechaza
    - promo_plans.promo_code: idéntico patrón
    - campaign_unsubscribes.email: el mismo email de baja en dos tenants sin error — una baja en un gimnasio ya no suprime los envíos del otro
    - notification_templates.template_key: idéntico patrón
    - day_modes.day_of_week: idéntico patrón
    - holidays (country, date): el mismo feriado en dos tenants sin error
    - formats.name: idéntico patrón
  </behavior>
  <read_first>
    - el-templo-api/test/tenancy/con-01-uniques-cross-tenant.test.ts (lo escrito en el Task 1: helper de duplicado, constantes de tenant, ciclo de vida)
    - el-templo-api/src/db/schema/cost-centers.ts, promo-plans.ts, campaigns.ts, notifications.ts, day-modes.ts, holidays.ts, formats.ts (columnas obligatorias y FKs de cada tabla)
    - el-templo-api/test/helpers.ts (TABLES_TO_CLEAN: cuáles de estas 7 tablas se limpian solas y cuáles hay que borrar a mano)
  </read_first>
  <action>
    Extender el mismo archivo con los 7 contratos restantes, reutilizando el helper de
    duplicado y las constantes de tenant del Task 1, sin duplicar el ciclo de vida.

    Para cada contrato: insertar la fila del tenant 1, la del tenant 2 con el mismo valor
    (debe pasar) y una segunda del tenant 1 con el mismo valor (debe rechazar por duplicado).
    En los contratos de dos columnas (`cost_centers` name+country, `holidays` country+date)
    repetir el PAR completo: cambiar solo una de las dos no probaría el contrato compuesto.

    `campaign_unsubscribes` merece su propio `describe` y un comentario que nombre la mina M3
    (doc 06 §8-Q5): es el caso donde el bug era peor que una colisión molesta — con la unique
    global, una baja en un gimnasio suprimía los envíos de TODOS. Probar además el caso con
    `user_id` NULL, que es la baja solo-email y el motivo por el que el backfill de esa tabla
    fue directo y no derivado del socio.

    `day_modes.day_of_week` tiene un dominio chico (los días de la semana): elegir un valor y
    asegurarse de limpiar la fila del tenant 1 que se cree, para no chocar con las filas
    sembradas por migraciones si las hubiera. Si la tabla ya viene poblada en la base de
    test, usar como "fila del tenant 1" la que ya existe en vez de insertar una nueva, y
    documentarlo en el comentario del test.

    Limpiar en `finally` las filas de las tablas que no estén en `TABLES_TO_CLEAN`. Cerrar el
    archivo con un test que verifique que después de todo esto el tenant 1 sigue teniendo
    exactamente los datos que tenía: ninguna fila del tenant 2 quedó colgada.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-168-contratos/el-templo-api &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run test/tenancy/con-01-uniques-cross-tenant.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/tenancy/con-01-uniques-cross-tenant.test.ts` pasa en verde con los 11 contratos cubiertos
    - Hay 22 aserciones o más (11 contratos por 2 direcciones), y las de rechazo siguen exigiendo el duplicado de MySQL específicamente
    - `cost_centers` y `holidays` repiten el PAR completo de columnas, no una sola
    - Existe un `describe` propio para `campaign_unsubscribes` que menciona la mina M3, con un caso de `user_id` NULL
    - Tras correr el archivo, no queda ninguna fila con `tenant_id = 2` en ninguna de las 11 tablas, y `tenants` vuelve a tener una sola fila
    - El archivo no modifica `test/helpers.ts` ni ningún otro archivo de la suite
    - `npx tsc --noEmit` sale 0
  </acceptance_criteria>
  <done>Los 11 contratos de CON-01 están probados por comportamiento en sus dos direcciones, con la mina M3 cubierta explícitamente.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                    | Description                                                         |
| ------------------------------------------- | ------------------------------------------------------------------- |
| test → base de test compartida por worker   | filas colgadas contaminan archivos vecinos de la misma suite        |
| DEFAULT 1 de tenant_id → intención del test | omitir `tenantId` produce un test que pasa por el motivo equivocado |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                    | Disposition | Mitigation Plan                                                                                                                                      |
| --------- | ---------------------- | ------------------------------------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-168-15  | Spoofing               | test que cree estar insertando en el tenant 2 y esté en el 1 | mitigate    | Criterio de aceptación: todo insert estampa `tenantId` explícito. Con el DEFAULT 1 de la fase 167, omitirlo haría verde un test que no prueba nada   |
| T-168-16  | Tampering              | aserción de rechazo que acepte cualquier error               | mitigate    | El helper distingue `ER_DUP_ENTRY` / 1062 de cualquier otro error. Una FK rota o una columna faltante no puede hacer pasar una aserción de duplicado |
| T-168-17  | Denial of Service      | filas del tenant 2 colgadas rompiendo archivos vecinos       | mitigate    | `DELETE` del tenant 2 en `afterAll`, limpieza en `finally` de las tablas fuera de `TABLES_TO_CLEAN`, y un test final que verifica que no quedó nada  |
| T-168-18  | Information Disclosure | datos reales de socios en las fixtures                       | accept      | Los valores son sintéticos y locales al archivo. No se importa ningún dump ni dato de producción                                                     |
| T-168-SC  | Tampering              | instalación de paquetes npm                                  | mitigate    | Solo se usan `vitest` y los helpers existentes. Cero dependencias nuevas, cero `pnpm install`                                                        |

</threat_model>

<verification>
- `npx tsc --noEmit` limpio.
- `npx vitest run test/tenancy/con-01-uniques-cross-tenant.test.ts` en verde (corrida angosta de un solo archivo: la suite completa es trabajo de CI).
- Base de test sin residuos: `tenants` con una fila, cero filas con `tenant_id = 2`.
- Sin commit: el commit único de la fase lo arma el plan 168-06.
</verification>

<success_criteria>

- Los 11 contratos de D-01 probados en las dos direcciones (acepta cross-tenant, rechaza intra-tenant).
- Mina M3 cubierta con su caso de `user_id` NULL.
- Cero contaminación de la base de test.
  </success_criteria>

<output>
Create `.planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-04-SUMMARY.md` when done
</output>
