---
phase: 168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id
plan: 02
type: execute
wave: 2
depends_on: ["168-01"]
files_modified:
  - el-templo-api/src/db/schema/users.ts
  - el-templo-api/src/db/schema/branches.ts
  - el-templo-api/src/db/schema/cost-centers.ts
  - el-templo-api/src/db/schema/promo-plans.ts
  - el-templo-api/src/db/schema/campaigns.ts
  - el-templo-api/src/db/schema/notifications.ts
  - el-templo-api/src/db/schema/day-modes.ts
  - el-templo-api/src/db/schema/holidays.ts
  - el-templo-api/src/db/schema/formats.ts
  - el-templo-api/src/db/schema/wellhub.ts
  - el-templo-api/src/db/schema/refresh-tokens.ts
  - el-templo-api/src/db/schema/tv.ts
autonomous: true
requirements: [CON-01]

must_haves:
  truths:
    - "El schema Drizzle declara las 11 uniques como compuestas con tenant_id primero, byte a byte con la migración 0196"
    - "Ninguna de las 11 columnas convertidas conserva su .unique() inline"
    - "Las 11 uniques de la lista M8 tienen un comentario de una línea que dice por qué siguen globales"
    - "El typecheck del api pasa limpio con el schema modificado"
  artifacts:
    - path: "el-templo-api/src/db/schema/users.ts"
      provides: "uq_users_tenant_email/_dni/_referral_code + idx_users_email/_dni/_referral_code en el callback de tabla"
      contains: "uq_users_tenant_email"
    - path: "el-templo-api/src/db/schema/promo-plans.ts"
      provides: "callback de tabla nuevo con uq_promo_plans_tenant_promo_code"
      contains: "uq_promo_plans_tenant_promo_code"
    - path: "el-templo-api/src/db/schema/notifications.ts"
      provides: "callback nuevo en notification_templates + comentario M8 en device_tokens.token"
      contains: "uq_notification_templates_tenant_key"
  key_links:
    - from: "el-templo-api/src/db/schema/users.ts"
      to: "el-templo-api/src/db/migrations/0196_tenant_unique_contracts.sql"
      via: "nombre de índice idéntico byte a byte"
      pattern: "uq_users_tenant_email"
---

<objective>
Alinear el schema Drizzle con la migración 0196: mover a los callbacks de tabla las 11
uniques que pasaron a compuestas, declarar los 4 índices secundarios de D-05, y anotar con
un comentario de una línea las 11 uniques de la lista M8 que quedan GLOBALES a propósito
(D-13) para que nadie las "arregle" en una fase futura.

Purpose: el schema TypeScript es lo que lee el equipo y lo que introspeccionan los tests.
Si dice `.unique()` sobre `email` mientras la base tiene `(tenant_id, email)`, la próxima
persona que corra `db:generate` intenta deshacer la fase entera.
Output: 12 schema files alineados byte a byte con la 0196 y el typecheck limpio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-CONTEXT.md
@.planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-PATTERNS.md
@.planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-01-SUMMARY.md

<interfaces>
Nombres finales de índice. Deben coincidir BYTE A BYTE con la migración 0196: si el plan 01
registró alguna desviación, manda el `.sql` real del worktree, no esta tabla.

Uniques compuestas (D-01) — el `.unique()` inline o el `uniqueIndex(...)` viejo se REEMPLAZA:

| archivo          | tabla                  | índice final                          | columnas Drizzle en orden                 |
| ---------------- | ---------------------- | ------------------------------------- | ----------------------------------------- |
| users.ts         | users                  | uq_users_tenant_email                 | table.tenantId, table.email               |
| users.ts         | users                  | uq_users_tenant_dni                   | table.tenantId, table.dni                 |
| users.ts         | users                  | uq_users_tenant_referral_code         | table.tenantId, table.referralCode        |
| branches.ts      | branches               | uq_branches_tenant_code               | table.tenantId, table.code                |
| cost-centers.ts  | cost_centers           | uq_cost_centers_tenant_name_country   | table.tenantId, table.name, table.country |
| promo-plans.ts   | promo_plans            | uq_promo_plans_tenant_promo_code      | table.tenantId, table.promoCode           |
| campaigns.ts     | campaign_unsubscribes  | uq_campaign_unsubscribes_tenant_email | table.tenantId, table.email               |
| notifications.ts | notification_templates | uq_notification_templates_tenant_key  | table.tenantId, table.templateKey         |
| day-modes.ts     | day_modes              | uq_day_modes_tenant_day_of_week       | table.tenantId, table.dayOfWeek           |
| holidays.ts      | holidays               | uq_holidays_tenant_country_date       | table.tenantId, table.country, table.date |
| formats.ts       | formats                | uq_formats_tenant_name                | table.tenantId, table.name                |

Índices secundarios NO-unique (D-05), declarados con `index(...)`:

| archivo      | índice                          | columna            | lookup que lo motiva                                  |
| ------------ | ------------------------------- | ------------------ | ----------------------------------------------------- |
| users.ts     | idx_users_email                 | table.email        | login, src/modules/auth/routes.ts                     |
| users.ts     | idx_users_dni                   | table.dni          | registro, src/modules/auth/routes.ts                  |
| users.ts     | idx_users_referral_code         | table.referralCode | resolveReferralCode, src/modules/referrals/service.ts |
| campaigns.ts | idx_campaign_unsubscribes_email | table.email        | filtro NOT EXISTS de envíos                           |

Estado actual verificado sobre el worktree de la 167:

- Inline `.unique()` a SACAR de la columna: users.ts (email, dni, referralCode), branches.ts
  (code), formats.ts (name), promo-plans.ts (promoCode), notifications.ts (templateKey).
- Ya en callback, solo renombrar y anteponer `table.tenantId`: day-modes.ts, holidays.ts,
  campaigns.ts (campaign_unsubscribes), cost-centers.ts.
- `promo_plans` y `notification_templates` son `mysqlTable(nombre, {…})` de DOS argumentos:
  hay que CREAR el callback de tabla.
- `users`, `branches`, `formats`, `cost_centers`, `campaign_unsubscribes` ya tienen callback.

Sitios de los comentarios M8 (D-13), una línea encima de cada unique:

| archivo (línea aprox)  | unique                               | categoría del motivo                        |
| ---------------------- | ------------------------------------ | ------------------------------------------- |
| users.ts (231)         | gympassId .unique()                  | id de plataforma externa                    |
| branches.ts (51)       | wellhubGymId .unique()               | id de plataforma externa                    |
| wellhub.ts (58)        | idx_wellhub_classes_class_id         | id externo                                  |
| wellhub.ts (87)        | idx_wellhub_slots_slot_id            | id externo                                  |
| wellhub.ts (116)       | idx_wellhub_bookings_number          | id externo                                  |
| wellhub.ts (142)       | idx_wellhub_events_event_id          | id externo, idempotencia de webhook         |
| refresh-tokens.ts (42) | tokenHash .unique()                  | secreto random, lookup pre-scope            |
| notifications.ts (48)  | device_tokens.token .unique()        | token de push, lookup pre-scope             |
| tv.ts (52)             | tv_devices.tokenHash .unique()       | secreto random, lookup pre-scope            |
| tv.ts (83)             | tv_pairings.userCode .unique()       | pre-claim, ya tiene el comentario M7 arriba |
| tv.ts (85-87)          | tv_pairings.deviceCodeHash .unique() | pre-claim                                   |

NO son M8 y NO llevan comentario acá: `idx_wellhub_classes_branch_activity` y
`idx_wellhub_slots_schedule_date` — son compuestas sobre FKs ya scopeadas y entran en la
allowlist "derivada de FK" del plan 168-03.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mover las 11 uniques a los callbacks de tabla como compuestas con tenant_id</name>
  <files>el-templo-api/src/db/schema/users.ts, el-templo-api/src/db/schema/branches.ts, el-templo-api/src/db/schema/cost-centers.ts, el-templo-api/src/db/schema/promo-plans.ts, el-templo-api/src/db/schema/campaigns.ts, el-templo-api/src/db/schema/notifications.ts, el-templo-api/src/db/schema/day-modes.ts, el-templo-api/src/db/schema/holidays.ts, el-templo-api/src/db/schema/formats.ts</files>
  <read_first>
    - el-templo-api/src/db/migrations/0196_tenant_unique_contracts.sql (fuente de verdad de los nombres finales)
    - el-templo-api/src/db/schema/tenants.ts (analog canónico de unique compuesta con tenantId primero: uq_tenant_setting)
    - el-templo-api/src/db/schema/cost-centers.ts (analog del bloque de índices en callback con comentario que ata el índice a su migración)
    - el-templo-api/src/db/schema/tenant-column.ts (helper de la fase 167 y el nombre de propiedad tenantId)
    - los 9 schema files listados en files (estado actual antes de editar)
  </read_first>
  <action>
    En cada uno de los 9 archivos reemplazar el contrato viejo por el compuesto, usando
    exactamente los nombres e identificadores de la tabla de interfaces.

    Tres formas de edición según el estado del archivo:
    (a) Unique inline sobre la columna: QUITAR el `.unique()` de la definición de la columna
    dejando intactos `notNull()`, largos y defaults, y declarar el índice en el callback con
    `uniqueIndex(<nombre nuevo>).on(table.tenantId, …)`.
    (b) Unique ya en el callback: renombrar al nombre nuevo y anteponer `table.tenantId` a la
    lista de columnas, conservando el orden relativo de las que ya estaban.
    (c) Tablas sin callback (promo_plans, notification_templates): agregar el tercer
    argumento `(table) => [ … ]` al `mysqlTable`.

    Agregar además los 4 índices secundarios de D-05 con `index(...)`, no con `uniqueIndex`:
    los tres de `users` y el de `campaign_unsubscribes`. Importar `index` y `uniqueIndex`
    desde `drizzle-orm/mysql-core` en los archivos donde falten.

    Cada índice nuevo lleva encima un comentario en castellano con el formato del repo:
    fase, requisito, qué contrato queda, y que es byte-a-byte con la migración 0196. Para los
    secundarios, citar el lookup por el valor solo que los motiva (login y registro en
    `src/modules/auth/routes.ts`, `resolveReferralCode` en
    `src/modules/referrals/service.ts`, filtro NOT EXISTS de envíos).

    En campaigns.ts actualizar el comentario existente de la mina M3, hoy escrito en futuro
    ("pasa a ser compuesta … en la fase 168"), a pasado: la conversión ya ocurrió en la 0196.
    Conservar la nota de que `user_id` puede ser NULL.

    NO tocar `idx_users_tenant_id`, `idx_branches_tenant_id` ni ningún `fk_*_tenant` (D-07).
    NO agregar ni quitar columnas. NO correr `db:generate` ni `db:push`.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-168-contratos/el-templo-api &amp;&amp; npx tsc --noEmit &amp;&amp; grep -rlF uq_users_tenant_email src/db/schema/ &amp;&amp; grep -rlF idx_campaign_unsubscribes_email src/db/schema/</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` en `el-templo-api` sale 0
    - Los 11 nombres nuevos de la tabla de interfaces aparecen literalmente en `src/db/schema/` dentro de una llamada `uniqueIndex(...)`, cada uno con `table.tenantId` como PRIMER argumento de `.on(...)`
    - Los 4 nombres de índice secundario aparecen dentro de una llamada `index(...)` (no `uniqueIndex`)
    - Ninguno de los 11 nombres viejos (`users_email_unique`, `users_dni_unique`, `users_referral_code_unique`, `branches_code_unique`, `uq_cost_centers_name_country`, `promo_plans_promo_code_unique`, `uniq_campaign_unsubscribe_email`, `notification_templates_template_key_unique`, `day_modes_day_of_week_unique`, `idx_holidays_country_date`, `formats_name_unique`) sobrevive en `src/db/schema/`
    - Las 7 columnas que tenían `.unique()` inline (users email/dni/referralCode, branches code, formats name, promo-plans promoCode, notifications templateKey) ya no lo tienen, y conservan sus `notNull()` y largos originales
    - `promo-plans.ts` y `notifications.ts` declaran su `mysqlTable` con TRES argumentos para las tablas `promo_plans` y `notification_templates`
    - Cada nombre de índice presente en `src/db/schema/` para estas 9 tablas existe también literalmente en `src/db/migrations/0196_tenant_unique_contracts.sql`
    - El comentario de la mina M3 en `campaigns.ts` está redactado en pasado y menciona la migración 0196
  </acceptance_criteria>
  <done>Los 9 schema files declaran las uniques compuestas y los índices secundarios con los mismos nombres que la 0196, y el typecheck pasa.</done>
</task>

<task type="auto">
  <name>Task 2: Anotar con un comentario de una línea las 11 uniques que quedan globales (M8)</name>
  <files>el-templo-api/src/db/schema/users.ts, el-templo-api/src/db/schema/branches.ts, el-templo-api/src/db/schema/wellhub.ts, el-templo-api/src/db/schema/refresh-tokens.ts, el-templo-api/src/db/schema/notifications.ts, el-templo-api/src/db/schema/tv.ts</files>
  <read_first>
    - el-templo-api/src/db/schema/tv.ts (analog exacto del comentario de mina: la línea M7 sobre tv_pairings, molde del texto que pide D-13)
    - el-templo-api/src/db/schema/wellhub.ts, refresh-tokens.ts, notifications.ts, users.ts, branches.ts (estado actual de cada unique a anotar)
    - .planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-CONTEXT.md (D-02: la lista M8 completa y su racional)
  </read_first>
  <action>
    Agregar una línea de comentario en castellano justo encima de cada una de las 11 uniques
    de la lista M8 de la tabla de interfaces, siguiendo el molde del comentario M7 que ya
    existe en tv.ts. Cada comentario tiene que decir tres cosas y nada más: que la unique es
    tenant-global a propósito, la categoría del motivo (id de plataforma externa: la unique
    global impide que dos tenants reclamen el mismo recurso; o secreto random con lookup
    pre-scope: componer por tenant sería circular porque el tenant se resuelve DESPUÉS de
    encontrar la fila), y que está registrada en `TENANT_GLOBAL_UNIQUES` de
    `src/db/tenant-tables.ts` (lista M8, aprobada 2026-07-26, doc 06 §8-Q4).

    El objetivo del comentario es operativo: que quien vea una unique sin `tenant_id` en una
    fase futura sepa que NO es un olvido de la 168 y no la "arregle".

    Estas 11 uniques NO se modifican: siguen siendo `.unique()` inline o `uniqueIndex(...)`
    sobre una sola columna, sin `tenant_id`. Solo se les agrega el comentario. Las dos
    uniques de tv_pairings ya tienen el comentario M7 arriba del bloque: complementar
    señalando la pertenencia a M8 sin duplicar el texto existente.

    No agregar comentario a `idx_wellhub_classes_branch_activity` ni a
    `idx_wellhub_slots_schedule_date`: no son M8, y su clasificación la hace la allowlist del
    plan 168-03.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-168-contratos/el-templo-api &amp;&amp; npx tsc --noEmit &amp;&amp; test "$(grep -rc 'M8' src/db/schema/ | grep -v ':0' | wc -l)" -ge 6</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sale 0
    - Los 6 archivos de la lista (`users.ts`, `branches.ts`, `wellhub.ts`, `refresh-tokens.ts`, `notifications.ts`, `tv.ts`) contienen al menos un comentario que menciona `M8`
    - Las 11 uniques M8 tienen una línea de comentario inmediatamente arriba, y cada comentario nombra su categoría de motivo (id de plataforma externa o secreto random con lookup pre-scope)
    - Cada comentario M8 referencia `tenant-tables.ts` como registro central del motivo (D-13)
    - Ninguna de las 11 uniques M8 cambió: siguen sin `tenant_id` y con el mismo nombre de índice / la misma forma inline que antes (verificable con `git diff` mostrando solo líneas agregadas de comentario en esos bloques)
    - `idx_wellhub_classes_branch_activity` e `idx_wellhub_slots_schedule_date` NO recibieron comentario M8
  </acceptance_criteria>
  <done>Las 11 uniques globales están anotadas con su motivo y apuntando al registro central, sin haber cambiado ninguna declaración.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                       | Description                                                                           |
| ---------------------------------------------- | ------------------------------------------------------------------------------------- |
| schema Drizzle → base de datos real            | divergencia silenciosa: el TS compila verde aunque no coincida con la DDL viva        |
| schema Drizzle → futuras migraciones generadas | un nombre mal escrito acá se convierte en un DROP/ADD espurio en la próxima migración |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                    | Disposition | Mitigation Plan                                                                                                                                                                                                                                                 |
| --------- | ---------------------- | ---------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-168-06  | Tampering              | nombre de índice divergente entre schema y 0196                              | mitigate    | Criterio de aceptación explícito: cada nombre presente en `src/db/schema/` para estas 9 tablas existe literalmente en el `.sql` de la 0196. El nombre es el único puente entre los dos artefactos                                                               |
| T-168-07  | Elevation of Privilege | una unique M8 convertida por error a compuesta                               | mitigate    | Task 2 prohíbe modificar las 11 M8 y exige que el `git diff` de esos bloques sea solo líneas de comentario. Convertir `refresh_tokens.token_hash` a `(tenant_id, token_hash)` rompería el lookup pre-scope del refresh y dejaría el token sin unicidad efectiva |
| T-168-08  | Tampering              | pérdida de `notNull()` o del largo de columna al sacar el `.unique()` inline | mitigate    | Criterio de aceptación explícito sobre las 7 columnas afectadas + `tsc --noEmit`                                                                                                                                                                                |
| T-168-09  | Information Disclosure | comentario M8 que documente de más sobre secretos                            | accept      | Los comentarios describen la categoría del motivo, nunca valores ni algoritmos de generación de tokens                                                                                                                                                          |
| T-168-SC  | Tampering              | instalación de paquetes npm                                                  | mitigate    | Este plan no agrega dependencias y reutiliza el `node_modules` symlinkeado del plan 01. Cero `pnpm install`                                                                                                                                                     |

</threat_model>

<verification>
- `npx tsc --noEmit` limpio en `el-templo-api`.
- Los 15 nombres de índice nuevos (11 uniques + 4 secundarios) presentes en el schema y en la 0196.
- Los 11 nombres viejos ausentes del schema.
- 11 comentarios M8 presentes, con las 11 declaraciones M8 intactas.
- Sin commit todavía: el commit de schema + migración lo hace el plan 168-06 (Hard Rule 3, un solo commit con el `.sql`).
</verification>

<success_criteria>

- El schema Drizzle y la migración 0196 declaran exactamente el mismo conjunto de índices con los mismos nombres.
- Las 11 uniques M8 quedan globales, anotadas y sin tocar.
- El typecheck del api pasa.
  </success_criteria>

<output>
Create `.planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-02-SUMMARY.md` when done
</output>
