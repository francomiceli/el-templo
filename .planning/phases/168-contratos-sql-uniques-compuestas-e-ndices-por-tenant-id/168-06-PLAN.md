---
phase: 168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id
plan: 06
type: execute
wave: 5
depends_on: ["168-01", "168-02", "168-03", "168-04", "168-05"]
files_modified: []
autonomous: false
requirements: [CON-01, CON-02]

must_haves:
  truths:
    - "La migración 0196 está aplicada exactamente una vez en eltemplo_staging y una vez en eltemplo"
    - "El verificador de uniques da 0 discrepancias y exit 0 contra las DOS bases reales (D-12)"
    - "Ningún commit ajeno a la fase 168 viajó a producción en este push"
    - "Cada push y cada acceso SSH ocurrió después de una señal explícita de Franco en la conversación (D-09)"
  artifacts:
    - path: ".planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-06-SUMMARY.md"
      provides: "Evidencia del rollout: SHAs por entorno, salidas del verificador en ambas bases y las señales humanas con su alcance"
  key_links:
    - from: "el-templo-api/src/db/migrations/0196_tenant_unique_contracts.sql"
      to: "eltemplo (producción)"
      via: "deploy.yml → NODE_ENV=production node dist/db/run-migrations.js"
      pattern: "0196_tenant_unique_contracts"
---

<objective>
Llevar la tanda D a las dos bases reales con el patrón probado en las fases 166 y 167:
un commit único con schema + migración + tests, staging primero con verificación contra
`eltemplo_staging`, y después master con verificación contra `eltemplo` — cada push y cada
SSH detrás de una señal explícita de Franco.

Purpose: la fase cierra con evidencia contra las bases reales, no solo con la suite (D-12).
Un contrato de unicidad que solo existe en la base de test no protege a nadie.
Output: 0196 aplicada y verificada en staging y en prod, con el SUMMARY como evidencia.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-CONTEXT.md
@.planning/phases/167-columnas-tenant-id-en-las-85-tablas-restantes-verificaci-n/167-07-SUMMARY.md
@.claude/skills/el-templo-change-control/SKILL.md
@.claude/skills/el-templo-db-migrations/SKILL.md

<interfaces>
Secuencia probada en la fase 167 (167-07-SUMMARY), a repetir:

- Etapa A: push de la rama de fase a `origin`. Si `origin/staging` está adelantado y el push
  directo es imposible, crear una rama descartable basada en `origin/staging` y hacer un
  merge `--no-ff` de la rama de fase adentro, verificando ANTES que los archivos que difieren
  entre master y staging no intersecten con los de la fase.
- Etapa B: seguir CI y el deploy de staging con UN SOLO poller cada 90 s (la API pública de
  GitHub tiene rate limit y `gh` no está instalado local).
- Etapa C: verificación por SSH contra `eltemplo_staging`, solo lectura.
- Etapa D: merge `--no-ff` de `origin/master` DENTRO de la rama de fase (no rebase: preserva
  los SHAs citados en los SUMMARY) y push de la RAMA DE FASE a master — nunca de la rama
  descartable, para que los commits propios de staging no viajen a producción.
- Etapa E: verificación por SSH contra `eltemplo`, solo lectura.

Verificaciones no negociables del patrón 167:

- Verificar el ÁRBOL post-merge (`git status --short` vacío, `comm -3` del diff contra el
  diff de fase, migraciones agregadas, cero deleciones), no el exit code del merge.
- `npx tsc --noEmit` sobre el árbol mergeado antes de cada push: esa combinación de código
  no existió nunca ni en CI ni en local.
- `git merge-base --is-ancestor origin/staging HEAD` debe FALLAR antes del push a master: si
  pasa, staging es ancestro y sus commits viajarían a prod (el accidente de la fase 78).
- Deploy paths reales del servidor: `/opt/el-templo-staging/api` y `/var/www/api`,
  descubiertos con `pm2 describe`, no asumidos.
- En el servidor, `MYSQL_PWD` como variable de entorno en vez de `-p` en la línea de
  comandos: el host EC2 es compartido con otras APIs.
- `SELECT DATABASE()` como statement separado y previo en cada base: `eltemplo_staging` y
  `eltemplo` viven en el mismo servidor MySQL.

Comando de verificación en el servidor, sobre la build compilada:
`NODE_ENV=production node dist/db/scripts/verify-tenant-uniques.js` (exit 0 sin
discrepancias, 1 con discrepancias, 2 error de conexión o de uso).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate local — commit único de la fase y verificación del árbol antes de cualquier push</name>
  <files>(sin archivos nuevos — arma el commit de los artefactos de los planes 01 a 05)</files>
  <read_first>
    - .claude/skills/el-templo-change-control/SKILL.md (staging-first estricto, `git add` explícito por ruta, human gates, HARD CONSTRAINT de numeración)
    - .claude/skills/el-templo-db-migrations/SKILL.md (Hard Rule 3: el `.sql` va en el mismo commit que el schema)
    - .planning/phases/167-columnas-tenant-id-en-las-85-tablas-restantes-verificaci-n/167-07-SUMMARY.md (etapas y verificaciones del patrón)
    - .planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-01-SUMMARY.md a 168-05-SUMMARY.md (qué archivos tocó cada plan y qué desviaciones se registraron)
  </read_first>
  <action>
    En el worktree `/home/franco/projects/et-168-contratos`, sobre `feat/168-contratos-sql`.

    `git fetch origin` y re-verificar el estado del mundo antes de commitear: que
    `origin/master` no haya incorporado ninguna migración `0196_` de otra rama mientras la
    fase se ejecutaba (si apareció, hay que renumerar el archivo y re-aplicar en local), y
    anotar los SHAs actuales de `origin/master` y `origin/staging` con cuántos commits
    difieren.

    Correr el gate local completo: `npx tsc --noEmit` en `el-templo-api`,
    `pnpm db:verify-uniques` contra la base local con exit 0, y una corrida angosta de los
    tres archivos de test de la fase (`test/migrations/0196-tenant-unique-contracts.test.ts`,
    `test/db/tenant-tables.test.ts`, `test/tenancy/con-01-uniques-cross-tenant.test.ts`). La
    suite completa NO se corre local: eso es trabajo de CI.

    Armar UN solo commit con `git add` explícito por ruta —nunca `git add -A` ni `git add .`,
    hay untracked peligrosos en el repo— incluyendo: la migración `0196_*.sql`, los 12 schema
    files, `src/db/tenant-tables.ts`, `src/db/scripts/verify-tenant-uniques.ts`,
    `package.json` y los tres archivos de test. Que el `.sql` viaje en el mismo commit que el
    schema es Hard Rule 3 y es el miss histórico más frecuente del repo.

    Mensaje de commit en el estilo de la fase 167 (`feat(168): …`), en castellano, nombrando
    CON-01 y CON-02 y qué queda global a propósito.

    Verificar el resultado antes de declarar el task hecho: `git status --short` vacío, el
    diff contra `origin/master` contiene exactamente los archivos esperados y ninguno más,
    exactamente una migración agregada, y cero deleciones. Si el diff trae algo inesperado,
    parar y reportar en vez de "arreglar" al vuelo.

    NO pushear nada en este task. El push requiere señal de Franco y es el Task 2.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-168-contratos/el-templo-api &amp;&amp; npx tsc --noEmit &amp;&amp; pnpm db:verify-uniques &amp;&amp; test -z "$(git status --short)" &amp;&amp; test "$(git diff --name-only origin/master...HEAD -- 'el-templo-api/src/db/migrations/' | wc -l)" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sale 0
    - `pnpm db:verify-uniques` sale 0 con `discrepancias: 0` contra la base local
    - Los tres archivos de test de la fase pasan en corridas angostas
    - Existe exactamente UN commit nuevo sobre `origin/master` en `feat/168-contratos-sql`, y contiene la migración `0196_*.sql` junto al schema
    - `git diff --name-only origin/master...HEAD` lista solo los archivos de los planes 01-05 y ninguno más
    - Exactamente una migración agregada y cero archivos borrados
    - `git status --short` vacío
    - No se ejecutó ningún `git push`, ningún `git add -A` ni `git add .`
    - Los SHAs de `origin/master` y `origin/staging` al momento del fetch quedan registrados para el Task 2
  </acceptance_criteria>
  <done>La fase está en un commit único, verificada local, lista para viajar — y nada se pusheó todavía.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Rollout staging-first a las dos bases reales, con cuatro señales humanas</name>
  <read_first>
    - .planning/phases/167-columnas-tenant-id-en-las-85-tablas-restantes-verificaci-n/167-07-SUMMARY.md (etapas A-E, deploy paths reales, poller de 90 s y las cuatro señales del precedente)
    - .claude/skills/el-templo-change-control/SKILL.md (staging-first estricto, human gates, "todo push a master es un deploy a prod")
    - .planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-06-PLAN.md (la sección `<interfaces>` de este plan: verificaciones no negociables del patrón 167)
  </read_first>
  <action>
    Ejecutar la secuencia de cinco etapas descrita en `<how-to-verify>`, avanzando SOLO hasta
    donde llegue la señal humana recibida y volviendo a pedir la siguiente antes de seguir.
    Ninguna señal previa, ni de esta fase ni de otra, autoriza una etapa que no nombra.

    Antes de cada push: `git fetch origin`, re-verificar que no apareció una migración `0196_`
    ajena, verificar el árbol resultante (status vacío, `comm -3` del diff contra el diff de
    fase, solo la 0196 agregada, cero deleciones) y correr `npx tsc --noEmit` sobre el árbol
    mergeado. Antes del push a master, además, comprobar que `git merge-base --is-ancestor
    origin/staging HEAD` FALLA.

    Para el typecheck del árbol mergeado, symlinkear `node_modules` desde
    `/home/franco/projects/et-167-columnas/el-templo-api` tras verificar con `cmp` que el
    lockfile es byte-idéntico, y borrar el symlink antes del push. Cero `pnpm install`.

    En el servidor, siempre solo lectura: `SELECT DATABASE()` como statement separado y
    previo, `MYSQL_PWD` como variable de entorno, y descubrir los deploy paths con
    `pm2 describe` en vez de asumirlos. Correr
    `NODE_ENV=production node dist/db/scripts/verify-tenant-uniques.js` en cada entorno.

    Al cerrar, actualizar `.planning/STATE.md` con el tope de migraciones aplicado en
    producción (0196) para que las fases 169 en adelante reserven desde 0197, y dejar el
    worktree `et-168-contratos` vivo hasta el UAT (patrón de las fases 166 y 167).

  </action>
  <what-built>
    La tanda D completa, en un commit único sobre `origin/master` en el worktree
    `/home/franco/projects/et-168-contratos`, rama `feat/168-contratos-sql`:

    - Migración `0196_tenant_unique_contracts.sql`: las 11 uniques del doc 06 §1-D pasan a
      `UNIQUE(tenant_id, …)` y `users` / `campaign_unsubscribes` reciben los 4 índices
      secundarios de D-05. Cero DDL para CON-02: las FK de la fase 167 ya dejaron el índice.
    - Schema Drizzle alineado byte a byte, con las 11 uniques de la lista M8 anotadas como
      globales a propósito.
    - `src/db/tenant-tables.ts` extendido con el motivo por unique global y la allowlist.
    - `src/db/scripts/verify-tenant-uniques.ts` (solo lectura) más el gate fail-closed en la
      suite y los tests de comportamiento cross-tenant.
    - Gate local verde: typecheck, verificador con 0 discrepancias y los tres archivos de
      test de la fase en corridas angostas.

    Falta lo único que no puedo hacer sin tu OK: llevarlo a staging, verificarlo contra
    `eltemplo_staging`, llevarlo a master y verificarlo contra `eltemplo`.

  </what-built>
  <how-to-verify>
    Este checkpoint tiene CUATRO señales separadas, en este orden. Cada una habilita solo su
    etapa: ninguna autoriza la siguiente, y una señal previa de otra fase no cuenta.

    **Señal 1 — "aprobado staging"** habilita las etapas A y B (sin SSH, sin master):
    1. Push de `feat/168-contratos-sql` a `origin`, y a `staging` — directo si es
       fast-forward, o vía rama descartable basada en `origin/staging` con merge `--no-ff`
       si staging está adelantado. Antes del merge se verifica que los archivos que difieren
       entre master y staging no intersecten con los de la fase.
    2. Verificación del árbol resultante (status vacío, `comm -3` del diff contra el diff de
       fase, solo la 0196 agregada, cero deleciones) y `npx tsc --noEmit` sobre el árbol
       mergeado.
    3. Seguimiento de CI y del deploy de staging con un solo poller cada 90 s. Se te reporta
       el número de run y el resultado por job.
    Qué mirar vos: que CI quede verde, y en particular el job de integration tests, que es
    donde corren por primera vez en CI los tres archivos de test nuevos.

    **Señal 2 — "autorizado SSH para staging"** habilita la etapa C (solo lectura, prod
    excluido):
    4. Por SSH, sobre `/opt/el-templo-staging/api`: `SELECT DATABASE()` primero, después
       confirmar que `_migrations` tiene una sola fila `0196_tenant_unique_contracts.sql` y
       correr `NODE_ENV=production node dist/db/scripts/verify-tenant-uniques.js`.
    Qué mirar vos: que el reporte diga `eltemplo_staging` en su primera línea y termine con
    0 discrepancias y exit 0.

    **Señal 3 — "aprobado prod"** habilita la etapa D:
    5. Merge `--no-ff` de `origin/master` dentro de `feat/168-contratos-sql`, verificación
       del árbol y typecheck, y comprobación de que `origin/staging` NO es ancestro de HEAD
       (si lo fuera, los commits parados en staging viajarían a producción — el accidente de
       la fase 78).
    6. Push de la RAMA DE FASE a master, que dispara el deploy de producción, y seguimiento
       con el mismo poller.
    Qué mirar vos: el listado de commits que van a viajar, que se te muestra ANTES del push.
    Si aparece algo que no sea de la fase 168, la respuesta correcta es frenar.

    **Señal 4 — "autorizado SSH para producción"** habilita la etapa E:
    7. Por SSH, sobre `/var/www/api`: `SELECT DATABASE()` primero, `_migrations` con una sola
       fila de la 0196, y `NODE_ENV=production node dist/db/scripts/verify-tenant-uniques.js`.
    Qué mirar vos: que el reporte diga `eltemplo` y termine con 0 discrepancias y exit 0. Es
    posible que aparezca un warning por la tabla de backup `users_lead_backup_0183`, que
    existe solo en producción y no cuenta como discrepancia.

    Chequeo funcional final, en la UI de producción, para el criterio 5 del ROADMAP (cero
    cambio de comportamiento para el staff): dar de alta un socio con un DNI que ya exista y
    ver que lo sigue rechazando, y crear una sede con un código repetido y ver lo mismo. Si
    preferís cerrarlo como pendiente igual que en las fases 166 y 167, se registra así en el
    SUMMARY.

  </how-to-verify>
  <resume-signal>
    Respondé con la señal de la etapa que quieras habilitar: "aprobado staging",
    "autorizado SSH para staging", "aprobado prod", "autorizado SSH para producción" — o
    describí el problema que ves. Sin la señal correspondiente no se ejecuta ningún push ni
    ningún acceso SSH.
  </resume-signal>
  <acceptance_criteria>
    - `_migrations` tiene exactamente una fila `0196_tenant_unique_contracts.sql` en `eltemplo_staging` y una en `eltemplo`
    - `verify-tenant-uniques.js` sale con exit 0 y 0 discrepancias en las DOS bases, con el nombre de la base impreso en la primera línea de cada reporte
    - Las 11 uniques compuestas existen con `tenant_id` en `SEQ_IN_INDEX = 1` y los 11 nombres viejos no existen, verificado en las dos bases
    - Los 4 índices secundarios existen con `NON_UNIQUE = 1` en las dos bases
    - `git merge-base --is-ancestor origin/staging HEAD` FALLA antes del push a master (ningún commit propio de staging viaja a producción)
    - CI queda verde en el push a staging, con el job de integration tests incluido
    - El deploy de producción termina sin rollback automático y el smoke post-deploy queda verde
    - Cada push y cada acceso SSH está precedido por su señal humana, registrada en el SUMMARY con hora y alcance habilitado
    - El SUMMARY registra los SHAs por entorno, las salidas de los dos verificadores y cualquier warning observado
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                |
| --------------------------------- | -------------------------------------------------------------------------- |
| rama local → `origin/master`      | todo push a master es un deploy a producción                               |
| `origin/staging` → producción     | staging acumula trabajo ajeno que no puede viajar                          |
| host EC2 compartido → bases MySQL | `eltemplo_staging` y `eltemplo` conviven con otras APIs en el mismo server |
| agente → acciones irreversibles   | pushes y SSH requieren consentimiento humano explícito                     |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                                    | Disposition | Mitigation Plan                                                                                                                                                                                                          |
| --------- | ---------------------- | -------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-168-24  | Elevation of Privilege | commits de staging arrastrados a producción                                                  | mitigate    | Push de la RAMA DE FASE a master, nunca de la rama descartable, más el assert de que `origin/staging` NO es ancestro de HEAD antes del push. Es exactamente el accidente de la fase 78                                   |
| T-168-25  | Tampering              | DDL aplicado a la base equivocada del host compartido                                        | mitigate    | `SELECT DATABASE()` como statement separado y previo en cada base, y el nombre de la base como primera línea de cada reporte                                                                                             |
| T-168-26  | Repudiation            | push o SSH sin consentimiento explícito                                                      | mitigate    | Cuatro señales separadas, cada una habilitando solo su etapa. Ninguna señal previa ni contexto de memoria cuenta como autorización. El checkpoint es bloqueante y no auto-aprobable                                      |
| T-168-27  | Information Disclosure | contraseña de MySQL en la línea de comandos de un host compartido                            | mitigate    | `MYSQL_PWD` como variable de entorno, nunca `-p<pass>`, patrón heredado de la fase 167                                                                                                                                   |
| T-168-28  | Denial of Service      | `ALTER TABLE` sobre `users` bloqueando escrituras en producción                              | accept      | Un solo `ALTER` por tabla probado en local y en staging antes de prod. `users` en producción es chica y la ventana es de segundos. El deploy tiene rollback automático ante smoke rojo                                   |
| T-168-29  | Tampering              | renumeración: otra rama mete un `0196_` mientras la fase se ejecuta                          | mitigate    | `git fetch` y re-verificación del tope de migraciones en el Task 1 y otra vez antes de cada push                                                                                                                         |
| T-168-30  | Elevation of Privilege | pérdida silenciosa del contrato de unicidad en prod (unique vieja dropeada, nueva no creada) | mitigate    | El verificador corre contra la base real DESPUÉS del deploy en cada entorno y exige presentes los 11 nuevos y ausentes los 11 viejos. La suite no alcanza: `test/setup.ts` tolera "Can't DROP"                           |
| T-168-SC  | Tampering              | instalación de paquetes npm durante el rollout                                               | mitigate    | Cero dependencias nuevas en toda la fase. Para el typecheck del árbol mergeado se symlinkea `node_modules` tras verificar que el lockfile es byte-idéntico, y el symlink se borra antes del push (patrón de la fase 167) |

</threat_model>

<verification>
- `_migrations` con una sola fila de la 0196 en las dos bases.
- `verify-tenant-uniques.js` con exit 0 y 0 discrepancias en `eltemplo_staging` y en `eltemplo`.
- CI verde en staging, deploy de producción sin rollback, smoke verde.
- Ningún commit ajeno a la fase 168 en el push a master.
- Cuatro señales humanas registradas con hora y alcance.
</verification>

<success_criteria>

- CON-01 y CON-02 verificados contra las dos bases reales, no solo contra la suite.
- El tenant 1 sigue rechazando exactamente los duplicados que rechazaba ayer.
- STATE.md queda con el tope de migraciones actualizado a 0196 para las fases 169 en adelante.
  </success_criteria>

<output>
Create `.planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-06-SUMMARY.md` when done
</output>
