---
phase: 168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/migrations/0196_tenant_unique_contracts.sql
autonomous: true
requirements: [CON-01, CON-02]

must_haves:
  truths:
    - "Existe un worktree propio basado en origin/master donde el tope de migraciones es 0195 y tenant-tables.ts ya existe (D-03, D-09)"
    - "Las 11 uniques globales de D-01 son uniques compuestas (tenant_id, ...) en la base local"
    - "users tiene índices no-unique sobre email, dni y referral_code; campaign_unsubscribes sobre email — y ningún otro catálogo chico recibe índice secundario (D-05, D-06)"
    - "Ningún índice viejo de D-01 sobrevive en la base local"
    - "La 0196 es UNA sola migración con DROP+ADD atómico por tabla donde sea posible, y cero DDL nuevo de INDEX(tenant_id): las FK de la 167 ya lo cubren (D-07, D-08)"
  artifacts:
    - path: "el-templo-api/src/db/migrations/0196_tenant_unique_contracts.sql"
      provides: "DDL de la tanda D: 11 uniques compuestas + 4 índices secundarios"
      contains: "ALTER TABLE users"
  key_links:
    - from: "el-templo-api/src/db/migrations/0196_tenant_unique_contracts.sql"
      to: "INFORMATION_SCHEMA.STATISTICS de la base local"
      via: "pnpm db:migrate"
      pattern: "uq_users_tenant_email"
---

<objective>
Reservar el número **0196**, crear el worktree de la fase desde `origin/master` y escribir
la migración única de la tanda D: convertir las 11 uniques globales del doc 06 §1-D a
`UNIQUE(tenant_id, …)` y agregar los 4 índices secundarios de D-05, aplicándola contra la
base local para probar la mecánica DDL antes de tocar cualquier entorno compartido.

Purpose: es el contrato de unicidad del que dependen todas las tareas siguientes de la
fase (schema Drizzle, allowlist, tests, rollout). Si el DDL no está bien acá, todo lo
demás verifica sobre una base equivocada.
Output: worktree `et-168-contratos` en la rama `feat/168-contratos-sql`, el archivo
`0196_tenant_unique_contracts.sql` y la base local con las 11 uniques compuestas vivas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-CONTEXT.md
@.planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-PATTERNS.md
@.claude/skills/el-templo-db-migrations/SKILL.md
@.claude/skills/el-templo-change-control/SKILL.md

<interfaces>
<!-- Contratos que el executor NO tiene que salir a buscar. -->

Tabla de conversión D-01 — índice VIEJO (DROP, nombre físico verificado) → índice NUEVO
(ADD UNIQUE), con sus columnas en orden:

| tabla                  | DROP INDEX                                 | ADD UNIQUE INDEX                      | columnas                   |
| ---------------------- | ------------------------------------------ | ------------------------------------- | -------------------------- |
| users                  | users_email_unique                         | uq_users_tenant_email                 | (tenant_id, email)         |
| users                  | users_dni_unique                           | uq_users_tenant_dni                   | (tenant_id, dni)           |
| users                  | users_referral_code_unique                 | uq_users_tenant_referral_code         | (tenant_id, referral_code) |
| branches               | branches_code_unique                       | uq_branches_tenant_code               | (tenant_id, code)          |
| cost_centers           | uq_cost_centers_name_country               | uq_cost_centers_tenant_name_country   | (tenant_id, name, country) |
| promo_plans            | promo_plans_promo_code_unique              | uq_promo_plans_tenant_promo_code      | (tenant_id, promo_code)    |
| campaign_unsubscribes  | uniq_campaign_unsubscribe_email            | uq_campaign_unsubscribes_tenant_email | (tenant_id, email)         |
| notification_templates | notification_templates_template_key_unique | uq_notification_templates_tenant_key  | (tenant_id, template_key)  |
| day_modes              | day_modes_day_of_week_unique               | uq_day_modes_tenant_day_of_week       | (tenant_id, day_of_week)   |
| holidays               | idx_holidays_country_date                  | uq_holidays_tenant_country_date       | (tenant_id, country, date) |
| formats                | formats_name_unique                        | uq_formats_tenant_name                | (tenant_id, name)          |

Índices secundarios NO-unique de D-05 (solo estos cuatro — D-06 excluye los catálogos):

| tabla                 | ADD INDEX                       | columnas        | lookup que lo motiva                                        |
| --------------------- | ------------------------------- | --------------- | ----------------------------------------------------------- |
| users                 | idx_users_email                 | (email)         | login, src/modules/auth/routes.ts                           |
| users                 | idx_users_dni                   | (dni)           | registro, src/modules/auth/routes.ts                        |
| users                 | idx_users_referral_code         | (referral_code) | canje, src/modules/referrals/service.ts resolveReferralCode |
| campaign_unsubscribes | idx_campaign_unsubscribes_email | (email)         | filtro NOT EXISTS de envíos                                 |

Credenciales locales: `el-templo-api/.env.development` (DB_HOST=localhost, DB_NAME=eltemplo).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Worktree de la fase desde origin/master y reserva del número 0196</name>
  <files>(sin archivos de código — crea el worktree /home/franco/projects/et-168-contratos)</files>
  <read_first>
    - .claude/skills/el-templo-change-control/SKILL.md (secciones 1, 2 y 3: staging-first, worktree, HARD CONSTRAINT de numeración)
    - .claude/skills/el-templo-db-migrations/SKILL.md (Hard Rules 1-6 y "Hand-written migration pattern")
    - .planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-PATTERNS.md (AVISO DE CHECKOUT y "Hallazgo que cambia el alcance")
  </read_first>
  <action>
    Trabajar SIEMPRE fuera del checkout principal: `/home/franco/projects/el-templo` está en
    `fix/referral-preview-y-refresh-ficha`, atrasado (migraciones hasta 0181) y potencialmente
    compartido con otra sesión — no hacer checkout ni stash ahí.

    Desde el checkout principal, `git fetch origin` y luego crear el worktree
    `/home/franco/projects/et-168-contratos` con una rama nueva `feat/168-contratos-sql`
    basada en `origin/master` (que ya contiene la fase 167, commit `68c447cf`).

    Verificar tres cosas ANTES de escribir nada, y abortar reportando si alguna falla:
    (a) el archivo de migración de número más alto en el worktree es
    `0195_tenant_id_templo_rest.sql` — si apareciera un 0196 de otra rama, renumerar la
    migración de esta fase al siguiente libre y anotarlo en el SUMMARY;
    (b) `el-templo-api/src/db/tenant-tables.ts` ya existe (lo creó la fase 167 con
    GYM_OWNED_TABLES y TENANT_EXEMPT_TABLES) — la 168 lo EXTIENDE, no lo crea;
    (c) si hay acceso a la base local, que `_migrations` no contenga ninguna fila `0196%`.

    Preparar `node_modules` SIN instalar nada (mitigación T-168-SC): comparar
    `el-templo-api/pnpm-lock.yaml` del worktree nuevo contra el de
    `/home/franco/projects/et-167-columnas` con `cmp`; si son byte-idénticos, symlinkear
    `el-templo-api/node_modules` al del worktree 167. Si difieren, NO instalar: reportarlo
    y pedir instrucción. Cero `pnpm install` en esta fase.

  </action>
  <verify>
    <automated>test "$(git -C /home/franco/projects/et-168-contratos rev-parse --abbrev-ref HEAD)" = "feat/168-contratos-sql" && test "$(ls /home/franco/projects/et-168-contratos/el-templo-api/src/db/migrations/*.sql | sort | tail -1 | xargs basename)" = "0195_tenant_id_templo_rest.sql" && test -f /home/franco/projects/et-168-contratos/el-templo-api/src/db/tenant-tables.ts && test -e /home/franco/projects/et-168-contratos/el-templo-api/node_modules</automated>
  </verify>
  <acceptance_criteria>
    - `git -C /home/franco/projects/et-168-contratos rev-parse --abbrev-ref HEAD` imprime `feat/168-contratos-sql`
    - `git -C /home/franco/projects/et-168-contratos merge-base --is-ancestor origin/master HEAD` sale 0 (la rama parte de master, no del checkout viejo)
    - El último `.sql` por orden lexicográfico en `src/db/migrations/` es `0195_tenant_id_templo_rest.sql`
    - `src/db/tenant-tables.ts` existe en el worktree y exporta `GYM_OWNED_TABLES`
    - `el-templo-api/node_modules` existe como symlink al worktree 167 y no se ejecutó ningún `pnpm install` / `npm install`
    - El checkout principal sigue en `fix/referral-preview-y-refresh-ficha` con su working tree intacto (`git -C /home/franco/projects/el-templo rev-parse --abbrev-ref HEAD`)
  </acceptance_criteria>
  <done>El worktree existe sobre origin/master, el número 0196 está confirmado como libre y hay node_modules sin haber instalado nada.</done>
</task>

<task type="auto">
  <name>Task 2: Escribir la migración 0196 con las 11 uniques compuestas y los 4 índices secundarios</name>
  <files>el-templo-api/src/db/migrations/0196_tenant_unique_contracts.sql</files>
  <read_first>
    - el-templo-api/src/db/migrations/0192_tenant_id_core_ops.sql (worktree: el estilo de cabecera-narrativa a imitar y el bloque-por-tabla)
    - el-templo-api/src/db/migrations/0191_tenant_anchors.sql (sintaxis `ALTER TABLE ... ADD INDEX` ya usada en el repo, y los índices explícitos idx_users_tenant_id / idx_branches_tenant_id que NO se tocan)
    - el-templo-api/src/db/run-migrations.ts (splitSqlStatements y la heurística alreadyApplied)
    - .claude/skills/el-templo-db-migrations/SKILL.md (Hard Rule 2: el `;` en comentarios)
  </read_first>
  <action>
    Crear `src/db/migrations/0196_tenant_unique_contracts.sql` a mano (nunca `db:generate`)
    con el estilo de comentario-narrativa de la 0192, en castellano y SIN un solo punto y
    coma dentro de ninguna línea `--` (el runner splitea por `;` antes de stripear
    comentarios — precedente que rompió todo CI en la 0119 y se repitió en la 0188).

    La cabecera debe declarar: que es la tanda D de la fase 168 (CON-01, CON-02) del doc 06
    §1-D; que las 11 uniques dejan de ser globales porque un segundo gimnasio colisionaría
    email, DNI, código de referido, código de sede, centro de costo, código promo,
    unsubscribe, template key, day mode, feriado y formato; que la lista M8 (ids de
    plataforma Wellhub y secretos random con lookup pre-scope) queda GLOBAL a propósito y
    esta migración no la toca; que NO se crea ningún `INDEX(tenant_id)` porque las FK de las
    migraciones 0192-0195 ya dejaron índice auto-creado `fk_<tabla>_tenant` y `users` /
    `branches` tienen además el explícito de la 0191 (D-07); que es hand-written porque
    `db:generate` está roto por el drift de `sessions.goal_plan_type`; y la advertencia de
    idempotencia: en cuanto un statement tira "Can't DROP", la heurística del runner saltea
    TODOS los errores posteriores del archivo y lo registra igual en `_migrations`, así que
    la verificación va contra `INFORMATION_SCHEMA`, nunca contra `_migrations`.

    Cuerpo: un statement `ALTER TABLE` por tabla, cada uno precedido de un comentario de una
    línea que diga qué contrato queda. Agrupar en el MISMO `ALTER TABLE` todos los cambios de
    una tabla (un solo rebuild) y hacer el `DROP INDEX` y el `ADD UNIQUE INDEX` en el mismo
    statement, para no dejar ninguna ventana sin contrato de unicidad (D-08). Usar
    EXACTAMENTE los 11 nombres viejos y los 11 nuevos de la tabla de `<interfaces>` — un
    `DROP INDEX` con nombre equivocado pasa en verde porque `test/setup.ts` tolera
    "Can't DROP". Los 4 índices secundarios de D-05 van en el mismo `ALTER TABLE` de su tabla
    (`users` y `campaign_unsubscribes`), citando en el comentario el sitio de código que los
    motiva. Los catálogos chicos no reciben índice secundario (D-06).

    No tocar `tenant_settings`, `system_settings`, `labs_inquiries` ni `tenants`. No dropear
    ningún índice que no esté en la columna DROP de la tabla. No agregar `INDEX(tenant_id)`.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-168-contratos/el-templo-api && test "$(grep -c ';' src/db/migrations/0196_tenant_unique_contracts.sql)" = "$(grep -v '^\s*--' src/db/migrations/0196_tenant_unique_contracts.sql | grep -c ';')" && test "$(grep -v '^\s*--' src/db/migrations/0196_tenant_unique_contracts.sql | grep -c 'DROP INDEX')" -eq 11 && test "$(grep -v '^\s*--' src/db/migrations/0196_tenant_unique_contracts.sql | grep -c 'ADD UNIQUE INDEX')" -eq 11 && test "$(grep -v '^\s*--' src/db/migrations/0196_tenant_unique_contracts.sql | grep -c 'ADD INDEX')" -eq 4</automated>
  </verify>
  <acceptance_criteria>
    - El archivo `src/db/migrations/0196_tenant_unique_contracts.sql` existe en el worktree
    - Ninguna línea que empiece con `--` contiene el carácter `;` (el conteo total de `;` iguala el conteo sobre el archivo sin líneas de comentario)
    - Fuera de comentarios hay exactamente 11 `DROP INDEX`, 11 `ADD UNIQUE INDEX` y 4 `ADD INDEX`
    - Los 11 nombres viejos de la tabla de `<interfaces>` aparecen literalmente detrás de un `DROP INDEX`, y los 11 nuevos detrás de un `ADD UNIQUE INDEX`, cada uno con `tenant_id` como PRIMERA columna de la lista
    - `grep -c 'INDEX(tenant_id)\|ADD INDEX idx_.*_tenant_id' ` sobre el archivo sin comentarios da 0 (D-07: cero DDL extra para CON-02)
    - La cabecera menciona explícitamente la fase 168, CON-01, CON-02, la lista M8 que queda global y la trampa de la heurística `alreadyApplied` del runner
  </acceptance_criteria>
  <done>La migración 0196 existe, es hand-written, no tiene `;` en comentarios y cubre exactamente las 11 conversiones y los 4 índices secundarios.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Aplicar la 0196 a la base local y resolver la mecánica DDL fina</name>
  <files>el-templo-api/src/db/migrations/0196_tenant_unique_contracts.sql</files>
  <read_first>
    - el-templo-api/src/db/migrations/0196_tenant_unique_contracts.sql (lo escrito en Task 2)
    - el-templo-api/src/db/run-migrations.ts (carga de env, orden lexicográfico, registro en _migrations)
    - el-templo-api/.env.development (DB_HOST/DB_USER/DB_PASSWORD/DB_NAME de la base local)
    - .planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-PATTERNS.md (sección "No Analog Found": el riesgo errno 150)
  </read_first>
  <action>
    Aplicar la migración contra la base LOCAL (`eltemplo` en localhost, credenciales de
    `.env.development`) con `cd el-templo-api && pnpm db:migrate`. Nunca `drizzle-kit migrate`
    ni `db:push`.

    Riesgo conocido a resolver acá y no en staging: MySQL puede rechazar el `DROP INDEX` con
    errno 150 si el índice que se dropea es el que sostiene una FK. Aplica sobre todo a
    `users`, `branches` y `cost_centers`. Si aparece, la salida es partir ese `ALTER TABLE`
    en dos statements (primero `ADD UNIQUE INDEX` del nuevo, después `DROP INDEX` del viejo,
    que deja al viejo redundante y dropeable), documentando en el comentario del bloque por
    qué esa tabla no pudo ser atómica y que la ventana intermedia tiene DOS contratos
    vigentes (nunca ninguno). Si en cambio MySQL acepta el statement combinado, dejarlo tal
    cual y anotar en el SUMMARY que la atomicidad de D-08 se cumplió.

    Si el runner falla a mitad del archivo, NO re-correr a ciegas: la heurística
    `alreadyApplied` puede silenciar los errores posteriores y registrar la migración igual.
    Corregir el SQL, borrar a mano la fila `0196_tenant_unique_contracts.sql` de `_migrations`
    si quedó insertada, revertir a mano los índices ya tocados y volver a aplicar.

    Verificar el resultado contra `INFORMATION_SCHEMA.STATISTICS` (filtrando por
    `TABLE_SCHEMA = DATABASE()`), no contra `_migrations`: las 11 uniques nuevas existen con
    `NON_UNIQUE = 0` y `tenant_id` en `SEQ_IN_INDEX = 1`, los 11 nombres viejos ya no
    existen, y los 4 índices secundarios existen con `NON_UNIQUE = 1`. Usar `MYSQL_PWD` como
    variable de entorno, nunca la contraseña en la línea de comandos.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-168-contratos/el-templo-api && pnpm db:migrate && set -a && . ./.env.development && set +a && MYSQL_PWD="$DB_PASSWORD" mysql -h "$DB_HOST" -u "$DB_USER" "$DB_NAME" -N -B -e "SELECT (SELECT COUNT(*) FROM (SELECT INDEX_NAME FROM INFORMATION_SCHEMA.STATISTICS WHERE TABLE_SCHEMA=DATABASE() AND NON_UNIQUE=0 AND SEQ_IN_INDEX=1 AND COLUMN_NAME='tenant_id' AND INDEX_NAME IN ('uq_users_tenant_email','uq_users_tenant_dni','uq_users_tenant_referral_code','uq_branches_tenant_code','uq_cost_centers_tenant_name_country','uq_promo_plans_tenant_promo_code','uq_campaign_unsubscribes_tenant_email','uq_notification_templates_tenant_key','uq_day_modes_tenant_day_of_week','uq_holidays_tenant_country_date','uq_formats_tenant_name') GROUP BY INDEX_NAME) a) AS nuevas, (SELECT COUNT(DISTINCT INDEX_NAME) FROM INFORMATION_SCHEMA.STATISTICS WHERE TABLE_SCHEMA=DATABASE() AND INDEX_NAME IN ('users_email_unique','users_dni_unique','users_referral_code_unique','branches_code_unique','uq_cost_centers_name_country','promo_plans_promo_code_unique','uniq_campaign_unsubscribe_email','notification_templates_template_key_unique','day_modes_day_of_week_unique','idx_holidays_country_date','formats_name_unique')) AS viejas, (SELECT COUNT(DISTINCT INDEX_NAME) FROM INFORMATION_SCHEMA.STATISTICS WHERE TABLE_SCHEMA=DATABASE() AND NON_UNIQUE=1 AND INDEX_NAME IN ('idx_users_email','idx_users_dni','idx_users_referral_code','idx_campaign_unsubscribes_email')) AS secundarios" | grep -qx "11	0	4"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm db:migrate` termina sin error y `_migrations` contiene exactamente una fila `0196_tenant_unique_contracts.sql`
    - La query de `<verify>` devuelve `11 0 4`: 11 uniques nuevas con `tenant_id` en `SEQ_IN_INDEX=1` y `NON_UNIQUE=0`, cero supervivientes de los 11 nombres viejos, 4 índices secundarios no-unique
    - En `cost_centers` el índice `uq_cost_centers_tenant_name_country` tiene 3 columnas en orden `tenant_id, name, country`, y en `holidays` `uq_holidays_tenant_country_date` tiene 3 en orden `tenant_id, country, date` (verificable con un SELECT ordenado por `SEQ_IN_INDEX`)
    - Los índices `idx_users_tenant_id`, `idx_branches_tenant_id` y los `fk_*_tenant` de las migraciones 0192-0195 siguen existiendo (no se dropeó nada de la fase 167)
    - Si alguna tabla necesitó statements separados por errno 150, el comentario del bloque en el `.sql` lo explica y el SUMMARY lo registra como desviación de D-08
  </acceptance_criteria>
  <done>La base local tiene los 11 contratos compuestos y los 4 índices secundarios vivos, verificado contra information_schema y no contra `_migrations`.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                       | Description                                                    |
| ---------------------------------------------- | -------------------------------------------------------------- |
| migración `.sql` → MySQL local                 | DDL arbitrario ejecutado por el runner sin revisión intermedia |
| worktree nuevo → checkout principal compartido | operaciones de git que podrían pisar el trabajo de otra sesión |
| lockfile → node_modules                        | dependencias que entrarían al árbol si se corriera un install  |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                 | Disposition | Mitigation Plan                                                                                                                                                                                                        |
| --------- | ---------------------- | ------------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-168-01  | Tampering              | `DROP INDEX` con nombre inexistente en 0196                               | mitigate    | Los 11 nombres físicos salen verificados de PATTERNS.md y el assert de Task 3 exige `viejas = 0` contra information_schema, no contra `_migrations` (que `test/setup.ts` deja pasar tolerando "Can't DROP")            |
| T-168-02  | Elevation of Privilege | ventana sin contrato de unicidad durante el `ALTER TABLE`                 | mitigate    | `DROP` + `ADD UNIQUE` en el mismo statement (D-08). Si errno 150 obliga a separarlos, el orden es `ADD` primero: la ventana tiene dos contratos, nunca cero                                                            |
| T-168-03  | Denial of Service      | rebuild de `users` bloqueando escrituras                                  | accept      | Se agrupa un único `ALTER TABLE` por tabla (un solo rebuild) y se prueba local antes de cualquier entorno compartido. `users` en prod es chica y la ventana es de segundos                                             |
| T-168-04  | Tampering              | DDL aplicado a la base equivocada del host compartido                     | mitigate    | En este plan el destino es SOLO `localhost/eltemplo` vía `.env.development`. Staging y prod son responsabilidad exclusiva del plan 168-06                                                                              |
| T-168-05  | Tampering              | worktree/branch creados sobre el checkout principal atrasado y compartido | mitigate    | `git worktree add` desde `origin/master` tras `git fetch`, cero checkout/stash en `/home/franco/projects/el-templo`, y assert de que su rama sigue intacta                                                             |
| T-168-SC  | Tampering              | instalación de paquetes npm                                               | mitigate    | Esta fase no agrega ninguna dependencia. `node_modules` se resuelve por symlink al worktree 167 tras verificar que `pnpm-lock.yaml` es byte-idéntico (`cmp`). Si difiere, se corta y se pregunta — cero `pnpm install` |

</threat_model>

<verification>
- `_migrations` local contiene `0196_tenant_unique_contracts.sql` exactamente una vez.
- `INFORMATION_SCHEMA.STATISTICS` de la base local: 11 uniques nuevas con `tenant_id` primero, 0 índices viejos, 4 secundarios.
- El checkout principal no fue modificado.
- Nada fue commiteado todavía: el commit va junto al schema Drizzle (Hard Rule 3, plan 168-02).
</verification>

<success_criteria>

- Worktree `et-168-contratos` en `feat/168-contratos-sql` basado en `origin/master`.
- `0196_tenant_unique_contracts.sql` escrito a mano, sin `;` en comentarios, con 11+11+4 statements de índice.
- Migración aplicada en la base local y verificada por information_schema.
- Mecánica DDL fina (atómico vs separado por errno 150) resuelta y documentada.
  </success_criteria>

<output>
Create `.planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-01-SUMMARY.md` when done
</output>
