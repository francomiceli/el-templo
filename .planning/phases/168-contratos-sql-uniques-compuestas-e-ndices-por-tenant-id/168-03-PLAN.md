---
phase: 168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id
plan: 03
type: execute
wave: 3
depends_on: ["168-01", "168-02"]
files_modified:
  - el-templo-api/src/db/tenant-tables.ts
  - el-templo-api/src/db/scripts/verify-tenant-uniques.ts
  - el-templo-api/package.json
autonomous: true
requirements: [CON-01, CON-02]

must_haves:
  truths:
    - "Existe un registro central en tenant-tables.ts — extendido, no recreado — que dice, por cada unique global de una tabla gym-owned, POR QUÉ sigue global (D-02, D-10, D-13)"
    - "Un verificador de solo lectura reporta uniques sin prefijo tenant_id, tablas gym-owned sin índice de prefijo tenant_id y tablas sin clasificar (D-11, D-14)"
    - "El verificador corre contra la base local con 0 discrepancias y exit 0"
    - "El mismo verificador se puede invocar desde la suite y desde el CLI contra staging y prod"
  artifacts:
    - path: "el-templo-api/src/db/tenant-tables.ts"
      provides: "TENANT_GLOBAL_UNIQUES (M8) + TENANT_UNIQUE_ALLOWLIST + helpers, además de las listas de la fase 167"
      contains: "TENANT_GLOBAL_UNIQUES"
    - path: "el-templo-api/src/db/scripts/verify-tenant-uniques.ts"
      provides: "verifyTenantUniques(QueryFn) + formatReport + CLI con exit codes 0/1/2"
      exports: ["verifyTenantUniques", "formatReport"]
    - path: "el-templo-api/package.json"
      provides: "script db:verify-uniques"
      contains: "db:verify-uniques"
  key_links:
    - from: "el-templo-api/src/db/scripts/verify-tenant-uniques.ts"
      to: "el-templo-api/src/db/tenant-tables.ts"
      via: "import de GYM_OWNED_TABLES, TENANT_EXEMPT_TABLES, TENANT_GLOBAL_UNIQUES, TENANT_UNIQUE_ALLOWLIST"
      pattern: 'from "../tenant-tables"'
---

<objective>
Construir el gate fail-closed de la fase: extender el registro canónico
`src/db/tenant-tables.ts` con el motivo de cada unique que queda global (D-13), y escribir
un verificador de solo lectura que recorre `information_schema` y exige que TODA unique de
una tabla gym-owned arranque con `tenant_id` o esté clasificada (D-14), y que TODA tabla
gym-owned tenga un índice cuyo primer campo sea `tenant_id` (D-11 / CON-02).

Purpose: sin este gate, una unique global nueva entra en el schema en la fase 172 y nadie
se entera hasta que un tenant le bloquee un valor a otro. El mismo módulo lo consumen las
fases 170 (sentinel) y 171 (manifiesto), y el mismo verificador corre contra
`eltemplo_staging` y `eltemplo` en el plan 168-06.
Output: `tenant-tables.ts` extendido, `verify-tenant-uniques.ts` nuevo, script
`db:verify-uniques` en `package.json`, y 0 discrepancias contra la base local.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-CONTEXT.md
@.planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-PATTERNS.md
@.planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-01-SUMMARY.md
@.planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-02-SUMMARY.md

<interfaces>
`src/db/tenant-tables.ts` YA EXISTE (fase 167, COL-01). Exporta hoy:

- `GYM_OWNED_TABLES` (87 nombres físicos, `as const`)
- `TENANT_EXEMPT_TABLES` (4: tenants, tenant_settings, system_settings, labs_inquiries)
- `GymOwnedTable`, `TenantExemptTable`
- `isGymOwnedTable(name: string): boolean`

Su test `test/db/tenant-tables.test.ts` afirma conteos DUROS: 87 gym-owned, 4 exentas, 91
tablas en el schema Drizzle. La fase 168 NO agrega tablas: esos números no cambian.

Las 11 uniques M8 que quedan globales, con su clave `tabla.indice` y su categoría de motivo
(D-02, aprobada 2026-07-26, doc 06 §8-Q4):

| clave                                           | motivo                                     |
| ----------------------------------------------- | ------------------------------------------ |
| users.users_gympass_id_unique                   | id de plataforma externa (Gympass/Wellhub) |
| branches.branches_wellhub_gym_id_unique         | id de plataforma externa                   |
| wellhub_classes.idx_wellhub_classes_class_id    | id externo                                 |
| wellhub_slots.idx_wellhub_slots_slot_id         | id externo                                 |
| wellhub_bookings.idx_wellhub_bookings_number    | id externo                                 |
| wellhub_events.idx_wellhub_events_event_id      | id externo, idempotencia de webhook        |
| refresh_tokens.refresh_tokens_token_hash_unique | secreto random, lookup pre-scope           |
| device_tokens.device_tokens_token_unique        | token de push, lookup pre-scope            |
| tv_devices.tv_devices_token_hash_unique         | secreto random, lookup pre-scope           |
| tv_pairings.tv_pairings_user_code_unique        | pre-claim (mina M7), lookup sin scope      |
| tv_pairings.tv_pairings_device_code_hash_unique | pre-claim (mina M7), lookup sin scope      |

Los nombres físicos de las M8 declaradas inline con `.unique()` los genera Drizzle y hay
que LEERLOS de `information_schema` de la base local, no asumirlos: la columna de la
izquierda de esta tabla es la forma esperada, no una verificación.

Verificador análogo a copiar en forma y estilo: `src/db/scripts/verify-tenant-backfill.ts`
(fase 167). Aporta el contrato `QueryFn = (statement: string) => Promise<Record<string, unknown>[]>`
que permite que la MISMA verificación corra desde el CLI (conexión `mysql2` propia) y desde
la suite (`app.db.execute(sql.raw(...))`), la cabecera "SOLO LECTURA / CÓMO SE CORRE", el
primer statement `SELECT DATABASE()` para el host compartido, `formatReport` imprimiendo la
base primero, y exit codes 0 sin discrepancias / 1 con discrepancias / 2 error de conexión
o de uso. Registro en `package.json`: junto a `"db:verify-tenant": "tsx src/db/scripts/verify-tenant-backfill.ts"`.

Query canónica de índices del repo:
`SELECT TABLE_NAME, INDEX_NAME, COLUMN_NAME, SEQ_IN_INDEX, NON_UNIQUE FROM INFORMATION_SCHEMA.STATISTICS WHERE TABLE_SCHEMA = DATABASE()`.
`NON_UNIQUE` viene numérico y el repo lo castea siempre con `Number(...)`. `PRIMARY` aparece
con `NON_UNIQUE = 0` y hay que excluirlo por nombre.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender tenant-tables.ts con el registro M8 y la allowlist de uniques</name>
  <files>el-templo-api/src/db/tenant-tables.ts</files>
  <read_first>
    - el-templo-api/src/db/tenant-tables.ts (estado actual: cabecera, GYM_OWNED_TABLES, TENANT_EXEMPT_TABLES, isGymOwnedTable — es el analog de estilo de sí mismo)
    - el-templo-api/test/db/tenant-tables.test.ts (los 5 tests fail-closed existentes y los conteos duros 87/4/91 que NO pueden cambiar)
    - el-templo-api/src/db/schema/wellhub.ts, refresh-tokens.ts, tv.ts, notifications.ts, users.ts, branches.ts (las 11 uniques M8 y sus comentarios recién agregados por el plan 168-02)
    - .planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-PATTERNS.md (sección 4: patrón de array con motivo, tipos y helper Set-based)
  </read_first>
  <action>
    Extender el archivo existente sin reescribir lo que ya hay: `GYM_OWNED_TABLES`,
    `TENANT_EXEMPT_TABLES`, sus tipos e `isGymOwnedTable` quedan intactos, y la cabecera del
    módulo se amplía con un párrafo que explique la segunda responsabilidad que suma la 168
    (clasificar uniques, no solo tablas).

    Agregar `TENANT_GLOBAL_UNIQUES`: un `Record<string, string>` cuya clave es
    `"<tabla_física>.<nombre_de_índice>"` y cuyo valor es el motivo escrito, con las 11
    entradas de la lista M8 de interfaces. Un `Record` y no un array porque el motivo es
    obligatorio (D-13): no se puede agregar una entrada sin justificarla. Los nombres de
    índice se leen de `information_schema` de la base local para los que Drizzle genera
    solo — el motivo se escribe a mano y en castellano.

    Agregar `TENANT_UNIQUE_ALLOWLIST`: mismo formato clave→motivo, para las uniques de tablas
    gym-owned que NO arrancan con `tenant_id` y NO son M8. Dos categorías, distinguidas en el
    texto del motivo:
    (a) "derivada de FK scopeada": uniques compuestas cuya PRIMERA columna es una FK a una
    tabla ya scopeada, seguras por transitividad (`uq_check_in_daily`, `user_branch_unique`,
    `idx_bookings_member_schedule_date`, `uniq_tx_target`, `plan_program_unique`,
    `idx_wellhub_classes_branch_activity`, `idx_wellhub_slots_schedule_date`, y las que
    aparezcan en el inventario del Task 3);
    (b) "deuda consciente Templo-module": uniques de módulos que solo usa el tenant 1
    mientras SPOM/marketing/blog sean Templo-only (`sessions.day_id` —mina M5—,
    `aura_config.source_type`, slugs de blog y gladius, catálogos SPOM). El motivo de esta
    categoría debe decir explícitamente que se resuelve solo si un tenant distinto de 1
    activa esos módulos, y que NO es un olvido de la 168.

    En este task la allowlist se llena con lo que se pueda enumerar leyendo los schema files.
    El cierre empírico (que no falte ninguna) lo hace el Task 3 corriendo el verificador — la
    lista es fail-closed, así que una entrada faltante rompe, no pasa en silencio.

    Agregar también `PLATFORM_PHYSICAL_TABLES`: tablas físicas que existen en MySQL pero no
    en el schema Drizzle y por lo tanto no están en ninguna de las dos listas de la 167.
    Incluir `_migrations` con su motivo (tabla de tracking del runner propio). Documentar en
    el JSDoc que las tablas de backup creadas por migraciones (por ejemplo
    `users_lead_backup_0183`, viva solo en producción) NO van acá: el verificador las reporta
    como advertencia, no como discrepancia, para no volver la lista un basurero.

    Agregar los tipos y helpers en el estilo `Set`-based del archivo:
    `isTenantGlobalUnique(table: string, indexName: string): boolean` (true si la clave está
    en `TENANT_GLOBAL_UNIQUES`), `isAllowedGlobalUnique(table, indexName)` (true si está en
    cualquiera de los dos registros), y `tenantUniqueMotive(table, indexName): string | undefined`.
    Todos aceptan `string` a propósito, igual que `isGymOwnedTable`, porque los consumidores
    clasifican nombres que salen de `information_schema`.

    No cambiar los conteos de tablas: la 168 no agrega ni quita tablas.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-168-contratos/el-templo-api &amp;&amp; npx tsc --noEmit &amp;&amp; npx tsx -e "const m=require('./src/db/tenant-tables.ts');console.log(m.GYM_OWNED_TABLES.length, Object.keys(m.TENANT_GLOBAL_UNIQUES).length, m.isTenantGlobalUnique('refresh_tokens', Object.keys(m.TENANT_GLOBAL_UNIQUES).find(k=>k.startsWith('refresh_tokens.')).split('.')[1]))"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sale 0
    - `tenant-tables.ts` exporta `TENANT_GLOBAL_UNIQUES`, `TENANT_UNIQUE_ALLOWLIST`, `PLATFORM_PHYSICAL_TABLES`, `isTenantGlobalUnique`, `isAllowedGlobalUnique` y `tenantUniqueMotive`
    - `Object.keys(TENANT_GLOBAL_UNIQUES).length === 11` y cada clave tiene el formato `tabla.indice` con la tabla presente en `GYM_OWNED_TABLES`
    - Cada valor de los dos registros es una cadena no vacía en castellano que dice el motivo (ningún `""`, ningún `"TODO"`, ningún `"pendiente"`)
    - Los 11 nombres de índice de `TENANT_GLOBAL_UNIQUES` existen realmente en `INFORMATION_SCHEMA.STATISTICS` de la base local con `NON_UNIQUE = 0`
    - `GYM_OWNED_TABLES.length` sigue siendo 87 y `TENANT_EXEMPT_TABLES.length` sigue siendo 4
    - `isGymOwnedTable` y las dos listas de la fase 167 no cambiaron de forma (`git diff` sobre ellas solo muestra la cabecera ampliada)
    - El JSDoc de `TENANT_UNIQUE_ALLOWLIST` distingue las dos categorías (derivada de FK scopeada / deuda consciente Templo-module) y el de `PLATFORM_PHYSICAL_TABLES` explica por qué las tablas de backup NO van ahí
  </acceptance_criteria>
  <done>El registro canónico clasifica uniques además de tablas, con motivo obligatorio por entrada y helpers que aceptan nombres de information_schema.</done>
</task>

<task type="auto">
  <name>Task 2: Escribir el verificador verify-tenant-uniques.ts y registrarlo en package.json</name>
  <files>el-templo-api/src/db/scripts/verify-tenant-uniques.ts, el-templo-api/package.json</files>
  <read_first>
    - el-templo-api/src/db/scripts/verify-tenant-backfill.ts (analog directo: cabecera SOLO LECTURA / CÓMO SE CORRE, QueryFn, forma del report, formatReport, main con exit codes y carga de env)
    - el-templo-api/src/db/tenant-tables.ts (los registros y helpers del Task 1)
    - el-templo-api/package.json (dónde se registra el script, junto a db:verify-tenant)
    - el-templo-api/test/migrations/0192-0195-tenant-columns.test.ts (adaptador makeQueryFn desde la suite, para que la firma sea compatible)
  </read_first>
  <action>
    Crear `src/db/scripts/verify-tenant-uniques.ts` reutilizando el contrato `QueryFn` de
    `verify-tenant-backfill.ts` — importarlo de ahí si está exportado, o redeclararlo con la
    misma firma exacta si no lo está, para que el adaptador de la suite sirva para los dos.
    Solo `SELECT`: el script tiene que ser seguro de correr contra producción, y la cabecera
    lo declara igual que su analog, con el bloque "CÓMO SE CORRE" (local `pnpm db:verify-uniques`,
    desde la suite vía el test de la 168, y en el servidor sobre la build compilada con
    `NODE_ENV=production node dist/db/scripts/verify-tenant-uniques.js`) y los exit codes
    0/1/2.

    Primer statement `SELECT DATABASE()` y `database` como primer campo del reporte y primera
    línea de `formatReport`: staging y prod son dos bases del MISMO host MySQL y nadie puede
    leer el resultado equivocado.

    Exportar `verifyTenantUniques(query: QueryFn)` que devuelve un reporte con estos campos,
    poblados desde una única consulta a `INFORMATION_SCHEMA.STATISTICS` filtrada por
    `TABLE_SCHEMA = DATABASE()` más una a `INFORMATION_SCHEMA.TABLES` para el inventario de
    tablas físicas:
    - `database`, `gymOwnedChecked`, `uniquesChecked` (contadores)
    - `uniquesMissingTenantPrefix`: uniques de tablas gym-owned, excluyendo `PRIMARY`, cuya
      primera columna (`SEQ_IN_INDEX = 1`) no es `tenant_id` y que no están en
      `TENANT_GLOBAL_UNIQUES` ni en `TENANT_UNIQUE_ALLOWLIST` — es el corazón de D-14
    - `tablesWithoutTenantIndex`: tablas gym-owned sin ningún índice con `tenant_id` en
      `SEQ_IN_INDEX = 1`, unique o no (los auto-creados por FK cuentan) — es CON-02 / D-11(a)
    - `unclassifiedTables`: tablas físicas que no están en `GYM_OWNED_TABLES`, ni en
      `TENANT_EXEMPT_TABLES`, ni en `PLATFORM_PHYSICAL_TABLES` — es D-11(b)
    - `staleClassifications`: entradas de los dos registros de uniques que apuntan a un índice
      inexistente en esta base (typo, rename, índice borrado)
    - `missingConvertedContracts`: las 11 uniques compuestas de D-01 que deberían existir con
      `tenant_id` en `SEQ_IN_INDEX = 1`, reportando las que falten o tengan el orden de
      columnas equivocado — es la verificación estructural de CON-01
    - `warnings`: observaciones que NO son discrepancias, entre ellas las tablas físicas cuyo
      nombre coincide con el patrón de backup de una migración (existen en prod y no en
      staging ni en test) y las uniques M8 encontradas y correctamente clasificadas
    - `discrepancies`: la suma de los cinco arrays de hallazgo (los `warnings` NO suman)

    `formatReport(report)` imprime la base primero y después cada categoría con detalle
    accionable: qué tabla, qué índice, qué columna quedó primera y qué hacer (agregar
    `tenant_id` al índice o clasificarlo en `tenant-tables.ts` con el motivo).

    `main()` copia la carga de env de `verify-tenant-backfill.ts` (`.env.production` si
    `NODE_ENV=production`, si no `.env.development`, después `.env`), abre la conexión
    `mysql2`, corre, imprime y devuelve 0 / 1, con 2 para error de conexión o de uso. Guardar
    la invocación detrás de `require.main === module` como el analog. Errores tipados con
    `catch (err: unknown)` e `instanceof Error` — sin `any` (CLAUDE.md).

    Registrar en `el-templo-api/package.json` el script `"db:verify-uniques": "tsx src/db/scripts/verify-tenant-uniques.ts"`,
    junto a `db:verify-tenant`. No tocar dependencias.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-168-contratos/el-templo-api &amp;&amp; npx tsc --noEmit &amp;&amp; grep -q '"db:verify-uniques"' package.json &amp;&amp; git diff --stat package.json | grep -q 'package.json' &amp;&amp; git diff package.json | grep -c '^+' | grep -qx 2</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sale 0 y el archivo no contiene el tipo `any`
    - `src/db/scripts/verify-tenant-uniques.ts` exporta `verifyTenantUniques` y `formatReport`
    - El reporte tiene los campos `database`, `uniquesMissingTenantPrefix`, `tablesWithoutTenantIndex`, `unclassifiedTables`, `staleClassifications`, `missingConvertedContracts`, `warnings` y `discrepancies`
    - Todos los statements SQL del archivo son `SELECT` (`grep -iE "insert|update|delete|alter|drop|create" ` sobre el archivo sin comentarios no encuentra SQL de escritura)
    - Todas las consultas de introspección filtran por `TABLE_SCHEMA = DATABASE()`, sin ningún nombre de base hardcodeado
    - El primer statement ejecutado es `SELECT DATABASE()` y `formatReport` imprime la base en su primera línea
    - `PRIMARY` está excluido explícitamente del conjunto de uniques evaluadas
    - `package.json` gana exactamente una línea: `"db:verify-uniques": "tsx src/db/scripts/verify-tenant-uniques.ts"`, sin cambios en `dependencies` ni `devDependencies`
    - El CLI devuelve 0 sin discrepancias, 1 con discrepancias y 2 ante error de conexión o de uso
  </acceptance_criteria>
  <done>El verificador existe, es de solo lectura, comparte el contrato QueryFn con el de la fase 167 y está registrado como script.</done>
</task>

<task type="auto">
  <name>Task 3: Cerrar la allowlist contra la base real hasta llegar a 0 discrepancias</name>
  <files>el-templo-api/src/db/tenant-tables.ts</files>
  <read_first>
    - el-templo-api/src/db/tenant-tables.ts (los registros escritos en el Task 1)
    - el-templo-api/src/db/scripts/verify-tenant-uniques.ts (el verificador del Task 2)
    - el-templo-api/.env.development (credenciales de la base local)
    - .planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-PATTERNS.md ("La allowlist de D-14 es MUCHO más grande que 11": ~40 entradas esperadas)
  </read_first>
  <action>
    Correr `pnpm db:verify-uniques` contra la base local (que ya tiene la 0196 aplicada por el
    plan 168-01) y usar su salida como inventario: cada entrada de
    `uniquesMissingTenantPrefix` es una unique real de una tabla gym-owned que hoy no está
    clasificada. Esperar del orden de 40 entradas, no 11 — la mayoría son compuestas cuyo
    primer campo es una FK a una tabla ya scopeada.

    Clasificar UNA POR UNA, sin atajos y sin clasificar por lote: para cada índice reportado,
    mirar su primera columna y decidir la categoría, y escribir el motivo concreto de ESA
    unique en `TENANT_UNIQUE_ALLOWLIST`. Un motivo genérico copiado y pegado ("derivada de
    FK") no sirve: tiene que nombrar la FK y la tabla padre que la hace segura, por ejemplo
    que el primer campo apunta a una tabla que ya lleva `tenant_id`, de modo que dos tenants
    no pueden compartir el valor.

    Si alguna unique reportada NO es segura por transitividad y NO es deuda Templo-module
    consciente — es decir, si un segundo gimnasio colisionaría de verdad — NO agregarla a la
    allowlist: es un hallazgo. Registrarlo en el SUMMARY y reportarlo al orquestador, porque
    significa que la lista de conversión de D-01 estaba incompleta y esa decisión es de
    Franco, no del executor.

    Repetir hasta que el verificador salga con exit 0 y `discrepancies = 0` contra la base
    local. Las categorías `tablesWithoutTenantIndex` y `unclassifiedTables` deberían dar
    vacías desde la primera corrida gracias a las FK de la fase 167 (D-07) — si no lo hacen,
    es un hallazgo y va al SUMMARY.

    Registrar en el SUMMARY el conteo final de la allowlist por categoría y la lista de
    warnings que el verificador emitió, que son el insumo del plan 168-06 para saber qué
    esperar en staging y prod.

  </action>
  <verify>
    <automated>cd /home/franco/projects/et-168-contratos/el-templo-api &amp;&amp; pnpm db:verify-uniques</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm db:verify-uniques` contra la base local sale con exit code 0 y su reporte imprime `discrepancies: 0`
    - `uniquesMissingTenantPrefix`, `tablesWithoutTenantIndex`, `unclassifiedTables`, `staleClassifications` y `missingConvertedContracts` salen todos vacíos
    - `TENANT_UNIQUE_ALLOWLIST` tiene una entrada por cada unique de tabla gym-owned que no arranca con `tenant_id` y no es M8, y ninguna entrada de más (`staleClassifications` vacío lo prueba)
    - Ningún motivo de la allowlist es idéntico byte a byte a otro: cada uno nombra su propia FK o su propio módulo Templo
    - El reporte lista los 11 contratos convertidos de D-01 como presentes y con `tenant_id` en la primera posición
    - `npx tsc --noEmit` sigue en 0
    - El SUMMARY registra el conteo de la allowlist por categoría, los warnings emitidos, y cualquier unique que NO se haya podido clasificar como segura (hallazgo escalado, no silenciado)
  </acceptance_criteria>
  <done>El gate fail-closed está cerrado contra la base real: cero discrepancias y toda unique global con motivo escrito.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                       | Description                                                  |
| ---------------------------------------------- | ------------------------------------------------------------ |
| `information_schema` → clasificación en código | la base es la verdad, la lista escrita a mano se pudre       |
| verificador → base de producción               | el script se va a correr contra `eltemplo` en el plan 168-06 |
| host MySQL compartido → reporte                | `eltemplo_staging` y `eltemplo` conviven en el mismo server  |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                                                      | Disposition | Mitigation Plan                                                                                                                                                                          |
| --------- | ---------------------- | -------------------------------------------------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-168-10  | Elevation of Privilege | unique global nueva que nadie clasifica en una fase futura                                                     | mitigate    | `uniquesMissingTenantPrefix` es fail-closed: cualquier unique sin `tenant_id` y sin entrada en los registros cuenta como discrepancia y devuelve exit 1                                  |
| T-168-11  | Tampering              | allowlist usada como alfombra: clasificar como "segura" una unique que sí colisiona                            | mitigate    | El motivo es obligatorio y tiene que nombrar la FK o el módulo concreto. Task 3 prohíbe explícitamente allowlistear una unique realmente colisionable y obliga a escalarla como hallazgo |
| T-168-12  | Information Disclosure | verificador ejecutando escritura contra producción                                                             | mitigate    | Solo `SELECT`, declarado en la cabecera y verificado con un grep de SQL de escritura en los criterios de aceptación                                                                      |
| T-168-13  | Spoofing               | leer el reporte de la base equivocada del host compartido                                                      | mitigate    | `SELECT DATABASE()` como primer statement y la base como primera línea de `formatReport`, patrón heredado de la fase 167                                                                 |
| T-168-14  | Denial of Service      | tabla de backup de prod (`users_lead_backup_0183`) marcada como discrepancia y bloqueando el cierre de la fase | mitigate    | Las tablas físicas con patrón de backup van a `warnings`, no a `unclassifiedTables`, y no suman a `discrepancies`                                                                        |
| T-168-SC  | Tampering              | instalación de paquetes npm                                                                                    | mitigate    | El verificador usa `mysql2`, `dotenv` y `tsx`, ya presentes por el analog de la fase 167. Cero dependencias nuevas, cero `pnpm install`                                                  |

</threat_model>

<verification>
- `npx tsc --noEmit` limpio.
- `pnpm db:verify-uniques` sale 0 contra la base local con `discrepancies: 0`.
- `TENANT_GLOBAL_UNIQUES` tiene 11 entradas con motivo; `TENANT_UNIQUE_ALLOWLIST` cubre el resto.
- `package.json` solo suma la línea del script.
- Sin commit: el commit único de la fase lo arma el plan 168-06.
</verification>

<success_criteria>

- Registro canónico extendido con motivo obligatorio por unique global.
- Verificador reutilizable de solo lectura, compatible con la suite y con el CLI contra staging y prod.
- Cero discrepancias contra la base local.
  </success_criteria>

<output>
Create `.planning/phases/168-contratos-sql-uniques-compuestas-e-ndices-por-tenant-id/168-03-SUMMARY.md` when done
</output>
