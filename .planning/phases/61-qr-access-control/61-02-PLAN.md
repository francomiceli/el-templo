---
phase: 61-qr-access-control
plan: 02
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - el-templo-api/src/modules/subscriptions/service.ts
  - el-templo-api/src/modules/subscriptions/types.ts
  - el-templo-api/src/modules/subscriptions/schemas.ts
  - el-templo-api/src/modules/subscriptions/routes.ts
  - el-templo-api/src/modules/scheduling/booking-service.ts
  - el-templo-api/src/modules/scheduling/types.ts
  - el-templo-api/src/modules/scheduling/routes.ts
  - el-templo-api/src/modules/scheduling/schemas.ts
  - el-templo-api/src/modules/attendance/service.ts
  - el-templo-api/src/modules/attendance/routes.ts
  - el-templo-api/src/modules/attendance/schemas.ts
  - el-templo-api/test/subscriptions/subscriptions.test.ts
  - el-templo-api/test/scheduling/scheduling.test.ts
autonomous: true
requirements: [ACCESS-01, ACCESS-02, ACCESS-03, ACCESS-04, ACCESS-05]

must_haves:
  truths:
    - "Admin selects specific schedule slots for fixed-plan subscriptions (stored in subscription_schedules junction table)"
    - "System auto-generates bookings for the entire subscription period when a fixed-plan subscription is created"
    - "Holiday dates are skipped during booking generation and a replacement credit is stored"
    - "Cancelling a subscription cancels all future bookings (tomorrow onwards)"
    - "Admin can view attendance status (checked in / no show) per schedule slot for a given date"
    - "Coach can manually check in a member from a schedule slot endpoint"
    - "Coach can remove a check-in (undo) from a schedule slot endpoint"
    - "Number of selected schedule slots must exactly match classesPerWeek"
  artifacts:
    - path: "el-templo-api/src/modules/subscriptions/service.ts"
      provides: "assignPlan with schedule slot storage and bulk booking generation"
      min_lines: 400
    - path: "el-templo-api/src/modules/scheduling/booking-service.ts"
      provides: "Bulk booking generation, cancel future bookings, slot attendance query"
      min_lines: 300
  key_links:
    - from: "el-templo-api/src/modules/subscriptions/service.ts"
      to: "el-templo-api/src/db/schema/subscription-schedules.ts"
      via: "INSERT into subscription_schedules on assignPlan"
      pattern: "subscriptionSchedules.*insert"
    - from: "el-templo-api/src/modules/subscriptions/service.ts"
      to: "el-templo-api/src/modules/scheduling/booking-service.ts"
      via: "calls generateFixedBookings after subscription creation"
      pattern: "generateFixedBookings|bulkGenerateBookings"
    - from: "el-templo-api/src/modules/scheduling/booking-service.ts"
      to: "el-templo-api/src/modules/scheduling/holiday-service.ts"
      via: "checks holidays during bulk booking generation"
      pattern: "holiday"
---

<objective>
Implement fixed schedule slot reservations with bulk booking generation, holiday-aware skip, subscription cancellation cleanup, and schedule slot attendance visibility API.

Purpose: Replace the old day-checkbox fixed schedule system with specific schedule slot references. When a fixed-plan subscription is created, the system auto-generates all bookings for the subscription period, skipping holidays. Provide APIs for the admin UI to show attendance per schedule slot and for coaches to manually check in / remove check-ins.

Output: Updated SubscriptionService with schedule slot storage, BookingService with bulk generation, new admin API endpoints for slot attendance and coach check-in/removal.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-qr-access-control/61-CONTEXT.md
@.planning/phases/61-qr-access-control/61-01-SUMMARY.md

@el-templo-api/src/db/schema/subscription-schedules.ts
@el-templo-api/src/db/schema/schedules.ts
@el-templo-api/src/db/schema/bookings.ts
@el-templo-api/src/db/schema/subscriptions.ts
@el-templo-api/src/modules/subscriptions/service.ts
@el-templo-api/src/modules/subscriptions/types.ts
@el-templo-api/src/modules/subscriptions/schemas.ts
@el-templo-api/src/modules/subscriptions/routes.ts
@el-templo-api/src/modules/scheduling/booking-service.ts
@el-templo-api/src/modules/scheduling/holiday-service.ts
@el-templo-api/src/modules/scheduling/types.ts
@el-templo-api/src/modules/scheduling/routes.ts
@el-templo-api/src/modules/scheduling/schemas.ts
@el-templo-api/src/modules/attendance/service.ts
@el-templo-api/src/modules/attendance/routes.ts
@el-templo-api/src/modules/attendance/schemas.ts
@el-templo-api/test/subscriptions/subscriptions.test.ts
@el-templo-api/test/scheduling/scheduling.test.ts

<interfaces>
<!-- From Plan 01 output: subscription_schedules junction table -->
<!-- subscription_schedules schema (created in Plan 01): -->
```typescript
// el-templo-api/src/db/schema/subscription-schedules.ts
export const subscriptionSchedules = mysqlTable("subscription_schedules", {
  id: int("id").primaryKey().autoincrement(),
  subscriptionId: int("subscription_id").references(() => subscriptions.id).notNull(),
  scheduleId: int("schedule_id").references(() => schedules.id).notNull(),
  createdAt: timestamp("created_at").defaultNow().notNull(),
}, (table) => [
  uniqueIndex("idx_sub_schedule").on(table.subscriptionId, table.scheduleId),
  index("idx_subscription_schedules_sub_id").on(table.subscriptionId),
]);
```

<!-- Schedules table (existing): -->

```typescript
export const schedules = mysqlTable("schedules", {
  id: int("id").primaryKey().autoincrement(),
  branchId: int("branch_id").notNull(),
  activityId: int("activity_id").notNull(),
  dayOfWeek: tinyint("day_of_week").notNull(), // 1=Monday..6=Saturday (ISO)
  startTime: varchar("start_time", { length: 5 }).notNull(), // HH:MM
  endTime: varchar("end_time", { length: 5 }).notNull(), // HH:MM
  isActive: boolean("is_active").default(true).notNull(),
});
```

<!-- HolidayService (existing): -->

```typescript
class HolidayService {
  async getHolidaysForWeek(
    country: string,
    weekStartDate: string,
  ): Promise<HolidayRecord[]>;
  async listHolidays(country?: string, year?: number): Promise<HolidayRecord[]>;
}
```

<!-- From Plan 01: AssignPlanInput no longer has fixedDays. Need to add scheduleIds. -->
<!-- From Plan 01: SubscriptionDetail no longer has fixedDays/graceCheckInsAfterExpiry. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fixed schedule slot storage, bulk booking generation, and subscription cancel cleanup</name>
  <files>
    el-templo-api/src/modules/subscriptions/service.ts,
    el-templo-api/src/modules/subscriptions/types.ts,
    el-templo-api/src/modules/subscriptions/schemas.ts,
    el-templo-api/src/modules/subscriptions/routes.ts,
    el-templo-api/src/modules/scheduling/booking-service.ts,
    el-templo-api/src/modules/scheduling/types.ts,
    el-templo-api/src/modules/scheduling/routes.ts,
    el-templo-api/src/modules/scheduling/schemas.ts,
    el-templo-api/test/subscriptions/subscriptions.test.ts,
    el-templo-api/test/scheduling/scheduling.test.ts
  </files>
  <behavior>
    - Test 1: assignPlan with fixed-mode plan + scheduleIds inserts rows into subscription_schedules
    - Test 2: assignPlan with fixed-mode plan generates bookings for the entire subscription period
    - Test 3: assignPlan rejects if scheduleIds count does not match classesPerWeek
    - Test 4: assignPlan skips holiday dates during bulk booking generation
    - Test 5: cancelSubscription cancels all future bookings (bookingDate > today)
    - Test 6: changePlan cancels future bookings of old subscription before creating new
    - Test 7: assignPlan rejects if any scheduleId has no available capacity for at least one booking date
  </behavior>
  <action>
    **Types updates (subscriptions/types.ts):**
    - Add `scheduleIds?: number[]` to AssignPlanInput (replaces old fixedDays)
    - Add `scheduleIds: number[]` to SubscriptionDetail (populated from subscription_schedules JOIN)
    - Add `replacementCredits: number` to SubscriptionDetail (count of holidays skipped during booking generation)
    - Update ClassUsageInfo: replace `fixedDays` with `scheduleIds: number[]`

    **SubscriptionService (subscriptions/service.ts):**

    1. In `assignPlan()`:
       - Accept `input.scheduleIds` for fixed-mode plans
       - Validate: if plan.bookingMode === 'fixed', scheduleIds is REQUIRED and scheduleIds.length MUST equal plan.classesPerWeek
       - Validate: each scheduleId must exist, be active, and belong to input.branchId
       - After inserting subscription, insert rows into `subscription_schedules` junction table
       - After inserting junction rows, call `bookingService.generateFixedBookings(subscriptionId, scheduleIds, startDate, endDateStr, branchId)` to auto-generate bookings
       - Store the replacement credit count on the subscription notes or a new approach (see below)

    2. For replacement credits (holiday skips):
       - The simplest approach: `generateFixedBookings` returns `{ totalGenerated, holidaysSkipped }`. The holidaysSkipped count is stored as `replacementCredits` on the subscription record (add nullable int column, or store in notes).
       - DISCRETION: Use a simple integer field approach. Add `replacement_credits INT DEFAULT 0` to the subscription record in a new migration OR simply track this as metadata. Since Plan 01 already has migration 0041, create a small addendum migration 0042 OR add the column to the subscription INSERT with a default value. The simplest approach: store it as part of the subscription notes field (e.g., "Creditos de reemplazo: 2") -- but this is fragile. Better: add a `replacementCredits` nullable int column to subscriptions. Since this is a backward-compatible nullable addition, just add it directly in the Drizzle schema and generate a migration.
       - ACTUALLY: Keep it simple. Create migration `0042_replacement_credits.sql` that adds `replacement_credits INT DEFAULT 0` to subscriptions. Update Drizzle schema.

    3. In `cancelSubscription()`:
       - After updating subscription status to cancelled, call `bookingService.cancelFutureBookings(subscriptionId)` to cancel all bookings with bookingDate > today. Past bookings stay as historical records.

    4. In `changePlan()`:
       - The existing code already cancels the old subscription and calls assignPlan for the new one. Ensure the cancel path triggers cancelFutureBookings on the old subscription.

    5. In `getMemberSubscription()`, `getMemberSubscriptionHistory()`, `getSubscriptionById()`:
       - After fetching subscription, query subscription_schedules to get the scheduleIds array for the subscription. Include in the response.
       - Add scheduleIds to mapSubscriptionRow or as a post-query enrichment.

    6. In `getClassUsageThisWeek()`:
       - Replace fixedDays with scheduleIds from subscription_schedules

    **BookingService additions (scheduling/booking-service.ts):**

    1. New method `generateFixedBookings(subscriptionId, scheduleIds, startDate, endDate, branchId)`:
       - For each scheduleId, get the schedule's dayOfWeek
       - Generate all dates from startDate to endDate that match each schedule's dayOfWeek
       - For each date, check if it's a holiday (query holidays table for the branch's country). If holiday: skip and increment holidaysSkipped counter.
       - For each non-holiday date, insert a booking (memberId from subscription, scheduleId, bookingDate, status 'reservado')
       - Handle capacity: if a slot is full for a date, put on waitlist (same logic as existing reserve)
       - Return `{ totalGenerated: number, holidaysSkipped: number }`
       - NOTE: This method needs the memberId. Get it from the subscription record, or accept it as a param.

    2. New method `cancelFutureBookings(subscriptionId)`:
       - Get all schedule IDs from subscription_schedules for this subscriptionId
       - Get the memberId from the subscription
       - Cancel all bookings where: memberId matches, scheduleId in scheduleIds, bookingDate > CURDATE(), status IN ('reservado', 'lista_espera')
       - Do NOT cancel qr_escaneado or confirmado bookings (those are past/current-day attendance records)
       - Trigger waitlist promotion for each affected scheduleId+date

    3. Add `generateFixedBookings` and `cancelFutureBookings` to scheduling routes if they need admin endpoints, OR just use them as service methods called from SubscriptionService. The latter is simpler: SubscriptionService takes BookingService as a constructor dependency.

    **SubscriptionService DI update:**
    - Add BookingService as an optional constructor param (after auraService): `constructor(db, log, auraService, bookingService?)`
    - This maintains backward compatibility with existing callsites that don't pass BookingService
    - Update route files to pass BookingService when creating SubscriptionService

    **Schemas updates:**
    - Add `scheduleIds` to assign plan request schema (array of integers)
    - Add `scheduleIds` to subscription detail response schema
    - Add `replacementCredits` to subscription detail response schema

    **Integration tests:**
    - Test fixed-plan assignment creates subscription_schedules rows
    - Test fixed-plan assignment generates bookings for the subscription period
    - Test classesPerWeek validation (mismatch = error)
    - Test holiday skipping during booking generation
    - Test subscription cancel triggers future booking cancellation
    - Test plan change cancels future bookings of old sub

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-api && pnpm test</automated>
  </verify>
  <done>
    Fixed-plan subscriptions store schedule slot references in subscription_schedules. Bulk bookings are generated for the full subscription period, skipping holidays. Subscription cancel/change cancels future bookings. Tests cover all new behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Schedule slot attendance API — slot detail with attendance status, coach check-in, coach undo</name>
  <files>
    el-templo-api/src/modules/attendance/service.ts,
    el-templo-api/src/modules/attendance/routes.ts,
    el-templo-api/src/modules/attendance/schemas.ts,
    el-templo-api/src/modules/attendance/types.ts,
    el-templo-api/test/attendance/attendance.test.ts
  </files>
  <behavior>
    - Test 1: GET /admin/attendance/slot/:scheduleId/:date returns bookings with attendance status for that slot
    - Test 2: POST /admin/attendance/slot/:scheduleId/:date/check-in creates confirmado attendance for a member
    - Test 3: DELETE /admin/attendance/:attendanceId removes the attendance record and restores classesRemaining
    - Test 4: Slot attendance shows green checkmark data for checked-in members, empty for booked-not-shown
    - Test 5: Coach check-in from slot searches ALL members with subscription status warnings
  </behavior>
  <action>
    **New endpoint: GET /admin/attendance/slot/:scheduleId/:date**
    Returns the list of members who have a booking for this schedule+date, enriched with attendance status:
    - For each booking: memberId, memberName, bookingStatus, attendanceId (if checked in), checkedInAt (if checked in)
    - Also include "unbooked check-ins" -- members who checked in via QR to this schedule+date but didn't have a booking
    - Response shape:
      ```typescript
      interface SlotAttendanceItem {
        memberId: number;
        memberName: string;
        bookingId: number | null;     // null if no booking (walk-in QR)
        bookingStatus: string | null;
        attendanceId: number | null;   // null if not checked in
        checkedInAt: string | null;
        source: 'qr' | 'manual' | null;
      }
      ```

    **New endpoint: POST /admin/attendance/slot/:scheduleId/:date/check-in**
    Coach manual check-in from the Horarios attendance view. This IS the force check-in (unified mechanism per CONTEXT.md).
    - Body: `{ memberId: number, reason?: string }`
    - Searches ALL members (not just booked ones) -- member must have a subscription but coach sees warnings
    - Creates attendance record with status "confirmado", source "manual", scheduleId, branchId (from schedule)
    - Awards 10 AURA immediately
    - Decrements classesRemaining if tracked
    - If member has a booking for this slot+date, update booking status to "confirmado"
    - Returns the attendance record + subscription status warnings (expired, no classes remaining, etc.)
    - This replaces the old POST /admin/attendance/force endpoint. Remove the old force endpoint OR keep it as an alias.

    **New endpoint: DELETE /admin/attendance/:attendanceId**
    Coach undo: remove a check-in if someone scanned but didn't actually attend.
    - Deletes the attendance record
    - If the attendance was linked to a booking (via scheduleId+date match), revert booking status from "qr_escaneado"/"confirmado" back to "reservado"
    - Restore classesRemaining +1 on the member's subscription (if tracked)
    - Reverse the 10 AURA award (call auraService with negative amount or a "reversal" transaction)

    **AttendanceService additions:**
    - `getSlotAttendance(scheduleId, date)`: query bookings + attendance for this slot+date, return enriched list
    - `coachCheckIn(scheduleId, date, memberId, reason?)`: unified manual check-in with AURA + budget + booking link
    - `removeCheckIn(attendanceId, adminId)`: delete attendance, restore budget, reverse AURA, revert booking

    **Update AttendanceService constructor:**
    - May need to accept BookingService or at minimum access the db directly for booking queries
    - Actually, the service already has direct db access -- just query bookings table directly (same pattern as existing code in checkIn that finds todayBooking)

    **Route registration:**
    - Add the 3 new endpoints to attendanceAdminRoutes
    - Consider whether to remove the old POST /force endpoint or keep it. RECOMMENDATION: Remove it (the new slot-based check-in replaces it). But if removing breaks the member app or other consumers, keep both temporarily.

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-api && pnpm test</automated>
  </verify>
  <done>
    Three new admin endpoints exist: slot attendance view, coach check-in from slot, coach undo check-in. Each is tested. Slot attendance returns booking+attendance merged data. Coach check-in awards AURA and decrements budget. Undo reverses all effects.
  </done>
</task>

</tasks>

<verification>
- `cd el-templo-api && npx tsc --noEmit` compiles clean
- `cd el-templo-api && pnpm test` all tests pass
- `grep -r "subscription_schedules" el-templo-api/src/modules/subscriptions/` confirms junction table usage
- `grep -r "generateFixedBookings\|bulkGenerate" el-templo-api/src/modules/` confirms bulk generation
- `grep -r "cancelFutureBookings" el-templo-api/src/modules/` confirms cancel cleanup
- Admin endpoints `/admin/attendance/slot/:scheduleId/:date` and `/admin/attendance/slot/:scheduleId/:date/check-in` and `DELETE /admin/attendance/:id` are registered
</verification>

<success_criteria>

1. Fixed-plan subscriptions store specific schedule slot IDs in subscription_schedules junction table
2. classesPerWeek validation: must select exactly N slots matching the plan's classesPerWeek
3. Bulk bookings are auto-generated for the entire subscription period on creation
4. Holiday dates are skipped with replacement credit count tracked
5. Subscription cancel/change cancels all future bookings (past bookings preserved)
6. Slot attendance endpoint returns merged booking+attendance data for a schedule+date
7. Coach can check in any member from a slot (unified force check-in)
8. Coach can undo a check-in (removes attendance, restores budget, reverses AURA)
9. All tests pass
   </success_criteria>

<output>
After completion, create `.planning/phases/61-qr-access-control/61-02-SUMMARY.md`
</output>
