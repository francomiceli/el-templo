---
phase: 61-qr-access-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/migrations/0041_attendance_rework.sql
  - el-templo-api/src/db/schema/attendance.ts
  - el-templo-api/src/db/schema/subscriptions.ts
  - el-templo-api/src/db/schema/subscription-schedules.ts
  - el-templo-api/src/db/schema/index.ts
  - el-templo-api/src/modules/attendance/service.ts
  - el-templo-api/src/modules/attendance/types.ts
  - el-templo-api/src/modules/attendance/routes.ts
  - el-templo-api/src/modules/attendance/schemas.ts
  - el-templo-api/src/modules/subscriptions/service.ts
  - el-templo-api/src/modules/subscriptions/types.ts
  - el-templo-api/src/modules/scheduling/booking-service.ts
  - el-templo-api/src/modules/settings/service.ts
  - el-templo-api/src/modules/settings/routes.ts
  - el-templo-api/src/modules/settings/schemas.ts
  - el-templo-api/test/attendance/attendance.test.ts
  - el-templo-api/test/scheduling/scheduling.test.ts
  - el-templo-api/test/settings/settings.test.ts
autonomous: true
requirements: [ACCESS-01, ACCESS-02, ACCESS-05]

must_haves:
  truths:
    - "QR scan immediately creates attendance with status 'confirmado' (no registrado step)"
    - "AURA is awarded immediately on QR scan (10 AURA per check-in)"
    - "Expired subscription = immediate hard block (no grace period)"
    - "subscription_schedules junction table exists for fixed plan schedule slot references"
    - "graceCheckInsAfterExpiry column is removed from subscriptions"
    - "fixedDays column is removed from subscriptions"
    - "confirmedAt column is removed from attendance"
    - "registrado status no longer exists in attendance enum"
  artifacts:
    - path: "el-templo-api/src/db/migrations/0041_attendance_rework.sql"
      provides: "Schema migration for attendance rework"
      contains: "subscription_schedules"
    - path: "el-templo-api/src/db/schema/subscription-schedules.ts"
      provides: "Drizzle schema for subscription_schedules junction table"
      exports: ["subscriptionSchedules", "subscriptionSchedulesRelations"]
    - path: "el-templo-api/src/modules/attendance/service.ts"
      provides: "Simplified AttendanceService without grace period"
      min_lines: 100
  key_links:
    - from: "el-templo-api/src/modules/attendance/service.ts"
      to: "el-templo-api/src/db/schema/attendance.ts"
      via: "status always 'confirmado'"
      pattern: "status.*confirmado"
    - from: "el-templo-api/src/modules/attendance/service.ts"
      to: "el-templo-api/src/modules/aura/service.ts"
      via: "AURA award on QR check-in"
      pattern: "auraService.*award"
---

<objective>
Schema migration and service cleanup: remove grace period, auto-confirm QR check-ins, simplify attendance status enum, and create subscription_schedules junction table for fixed-plan schedule slot references.

Purpose: Eliminate the two-step attendance model (registrado -> confirmado) and grace period complexity. QR scan now immediately confirms and awards AURA. Prepare the schema for fixed-plan schedule slot references (used in Plan 02).

Output: Migration 0041, updated Drizzle schemas, simplified AttendanceService/SubscriptionService/BookingService/SettingsService, updated tests.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-qr-access-control/61-CONTEXT.md

@el-templo-api/src/db/schema/attendance.ts
@el-templo-api/src/db/schema/subscriptions.ts
@el-templo-api/src/db/schema/schedules.ts
@el-templo-api/src/db/schema/bookings.ts
@el-templo-api/src/db/schema/index.ts
@el-templo-api/src/modules/attendance/service.ts
@el-templo-api/src/modules/attendance/types.ts
@el-templo-api/src/modules/attendance/routes.ts
@el-templo-api/src/modules/attendance/schemas.ts
@el-templo-api/src/modules/subscriptions/service.ts
@el-templo-api/src/modules/subscriptions/types.ts
@el-templo-api/src/modules/scheduling/booking-service.ts
@el-templo-api/src/modules/settings/service.ts
@el-templo-api/src/modules/settings/routes.ts
@el-templo-api/src/modules/settings/schemas.ts
@el-templo-api/test/attendance/attendance.test.ts
@el-templo-api/test/scheduling/scheduling.test.ts
@el-templo-api/test/settings/settings.test.ts

<interfaces>
<!-- Key types the executor needs to understand before modifying -->

From el-templo-api/src/modules/attendance/types.ts:

```typescript
export type AttendanceStatus = "registrado" | "confirmado"; // REMOVE "registrado"
export type AttendanceSource = "qr" | "manual";

export interface AttendanceRecord {
  id: number;
  memberId: number;
  memberName: string;
  branchId: number;
  branchName: string;
  checkedInAt: string;
  confirmedAt: string | null; // REMOVE this field
  status: AttendanceStatus;
  source: AttendanceSource;
}
```

From el-templo-api/src/modules/subscriptions/types.ts:

```typescript
export interface SubscriptionDetail {
  // ... many fields ...
  classesRemaining: number | null;
  fixedDays: number[] | null; // REMOVE (replaced by subscription_schedules)
  graceCheckInsAfterExpiry: number; // REMOVE
  // ...
}

export interface AssignPlanInput {
  // ...
  fixedDays?: number[]; // REMOVE (replaced by scheduleIds in Plan 02)
}
```

Current attendance schema enum: ["registrado", "confirmado"] -> change to only ["confirmado"]
Current subscriptions columns to remove: fixedDays (json), graceCheckInsAfterExpiry (int)
Current attendance columns to remove: confirmedAt (timestamp)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema migration and Drizzle model updates</name>
  <files>
    el-templo-api/src/db/migrations/0041_attendance_rework.sql,
    el-templo-api/src/db/schema/attendance.ts,
    el-templo-api/src/db/schema/subscriptions.ts,
    el-templo-api/src/db/schema/subscription-schedules.ts,
    el-templo-api/src/db/schema/index.ts
  </files>
  <behavior>
    - Test 1: subscription_schedules table exists with subscriptionId, scheduleId columns and FK constraints
    - Test 2: attendance table no longer has confirmed_at column
    - Test 3: subscriptions table no longer has fixed_days or grace_check_ins_after_expiry columns
    - Test 4: attendance status enum only has 'confirmado' (no 'registrado')
  </behavior>
  <action>
    1. Create migration `0041_attendance_rework.sql`:
       - `UPDATE attendance SET status = 'confirmado' WHERE status = 'registrado';` (migrate existing registrado records)
       - `ALTER TABLE attendance MODIFY COLUMN status ENUM('confirmado') NOT NULL DEFAULT 'confirmado';` (remove registrado from enum)
       - `ALTER TABLE attendance DROP COLUMN confirmed_at;`
       - `ALTER TABLE subscriptions DROP COLUMN fixed_days;`
       - `ALTER TABLE subscriptions DROP COLUMN grace_check_ins_after_expiry;`
       - Create `subscription_schedules` table:
         ```sql
         CREATE TABLE subscription_schedules (
           id INT AUTO_INCREMENT PRIMARY KEY,
           subscription_id INT NOT NULL,
           schedule_id INT NOT NULL,
           created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
           FOREIGN KEY (subscription_id) REFERENCES subscriptions(id),
           FOREIGN KEY (schedule_id) REFERENCES schedules(id),
           UNIQUE INDEX idx_sub_schedule (subscription_id, schedule_id),
           INDEX idx_subscription_schedules_sub_id (subscription_id)
         );
         ```

    2. Update Drizzle schema `attendance.ts`:
       - Change attendanceStatusEnum to only `["confirmado"]`
       - Remove `confirmedAt` column from attendance table
       - Keep attendanceSourceEnum as-is (qr, manual)

    3. Update Drizzle schema `subscriptions.ts`:
       - Remove `fixedDays` column (json)
       - Remove `graceCheckInsAfterExpiry` column (int)
       - Remove the `json` import if no longer needed

    4. Create new Drizzle schema `subscription-schedules.ts`:
       - Table `subscription_schedules` with id, subscriptionId (FK to subscriptions), scheduleId (FK to schedules), createdAt
       - Unique index on (subscriptionId, scheduleId)
       - Index on subscriptionId for efficient lookup
       - Relations: one-to-one with subscriptions and schedules

    5. Update `schema/index.ts`:
       - Export subscription-schedules module

    6. Run migration: `cd el-templo-api && pnpm drizzle-kit push` or apply manually

    IMPORTANT: The migration must handle the enum change safely. MySQL requires all existing rows to have a valid value for the new enum before the ALTER. The UPDATE statement MUST come first.

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-api && npx tsc --noEmit</automated>
  </verify>
  <done>
    Migration 0041 exists. Drizzle schemas updated: attendance has only "confirmado" status, no confirmedAt; subscriptions has no fixedDays/graceCheckInsAfterExpiry; subscription_schedules table and schema exist. TypeScript compiles clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Service cleanup — remove grace period, auto-confirm, update tests</name>
  <files>
    el-templo-api/src/modules/attendance/service.ts,
    el-templo-api/src/modules/attendance/types.ts,
    el-templo-api/src/modules/attendance/routes.ts,
    el-templo-api/src/modules/attendance/schemas.ts,
    el-templo-api/src/modules/subscriptions/service.ts,
    el-templo-api/src/modules/subscriptions/types.ts,
    el-templo-api/src/modules/scheduling/booking-service.ts,
    el-templo-api/src/modules/settings/service.ts,
    el-templo-api/src/modules/settings/routes.ts,
    el-templo-api/src/modules/settings/schemas.ts,
    el-templo-api/test/attendance/attendance.test.ts,
    el-templo-api/test/scheduling/scheduling.test.ts,
    el-templo-api/test/settings/settings.test.ts
  </files>
  <action>
    **AttendanceService (service.ts):**
    - Remove the entire `getSubscriptionWithGracePeriod` private method
    - In `checkIn()`: replace `getSubscriptionWithGracePeriod(memberId)` with `this.subscriptionService.getMemberSubscription(memberId)`. Expired subs auto-expire on read and return null = hard block.
    - In `checkIn()`: change `status: "registrado"` to `status: "confirmado"` in the INSERT
    - In `checkIn()`: immediately after inserting attendance, call `this.auraService.award({ userId: memberId, amount: 10, description: 'Asistencia confirmada', sourceType: 'attendance', sourceId: recordId })`. Check how AuraService.award() is called -- use the pattern from existing code.
    - Remove `parseFixedDays` private method (no longer needed here -- fixedDays no longer on subscription)
    - Remove the fixed-day check block (lines ~91-99) from checkIn. Fixed-day enforcement will be handled by the booking system (Plan 02). QR check-in just validates subscription+overdue+branch+weekly limit+monthly budget+once-per-day.
    - In `forceCheckIn()`: change `status: "registrado"` to `status: "confirmado"` in the INSERT. Also award 10 AURA immediately on force check-in (same as QR).
    - Remove `confirmedAt` from all SELECT queries (getRecordById, listAttendance, mapAttendanceRow)
    - Remove `settingsService` from constructor (6th param). Update all callsites.
    - Remove SettingsService import

    **AttendanceService constructor DI change:**
    - Remove `settingsService: SettingsService` from constructor params (was 6th param)
    - Old: `constructor(db, log, paymentService, subscriptionService, auraService, settingsService)`
    - New: `constructor(db, log, paymentService, subscriptionService, auraService)`

    **attendance/types.ts:**
    - Remove `"registrado"` from AttendanceStatus: `export type AttendanceStatus = "confirmado";`
    - Remove `confirmedAt` from AttendanceRecord interface

    **attendance/routes.ts:**
    - Remove SettingsService instantiation and param from AttendanceService constructor calls in both admin and member route plugins
    - Remove SettingsService import

    **attendance/schemas.ts:**
    - Remove `confirmedAt` from any response schemas
    - Update status enum to only `["confirmado"]`

    **SubscriptionService (subscriptions/service.ts):**
    - Remove `graceCheckInsAfterExpiry` from ALL select queries (getMemberSubscription, getMemberSubscriptionHistory, getSubscriptionById)
    - Remove `graceCheckInsAfterExpiry` from mapSubscriptionRow
    - Remove grace-period-aware `autoExpireSubscriptions` — revert to simple: `endDate < today` (remove the DATE_ADD grace days logic, remove settingsService dependency)
    - Remove `settingsService` property and constructor param. Old: `constructor(db, log, auraService, settingsService?)`. New: `constructor(db, log, auraService)`.
    - Remove `fixedDays` from all select queries, mapSubscriptionRow, and assignPlan
    - In `assignPlan()`: remove the fixedDays validation and storage block (lines ~476-487). Remove `fixedDaysJson` variable. Remove `fixedDays: fixedDaysJson` from INSERT.
    - In `getClassUsageThisWeek()`: remove fixedDays parsing and return (set fixedDays to null in return, or remove field)
    - Remove `parseFixedDays` private method

    **subscriptions/types.ts:**
    - Remove `fixedDays` from SubscriptionDetail interface
    - Remove `graceCheckInsAfterExpiry` from SubscriptionDetail
    - Remove `fixedDays` from AssignPlanInput (will be replaced by `scheduleIds` in Plan 02)
    - Remove `fixedDays` from ClassUsageInfo (or rename to something appropriate in Plan 02)

    **BookingService (scheduling/booking-service.ts):**
    - Remove the entire fixed-day check block (step 6b, lines ~117-126) that uses `parseFixedDays`. Fixed schedule enforcement will move to the subscription_schedules junction table in Plan 02.
    - Remove the grace period check block (step 6d, lines ~139-165). Expired subs are caught by the subscription check in step 5 (getMemberSubscription auto-expires).
    - Remove `parseFixedDays` private method
    - Remove `settingsService` from constructor. Old: `constructor(db, log, paymentService, subscriptionService, settingsService?)`. New: `constructor(db, log, paymentService, subscriptionService)`.
    - Remove SettingsService import

    **BookingService routes (scheduling/routes.ts):**
    - Remove SettingsService instantiation and param from BookingService constructor call
    - Remove SettingsService import

    **SubscriptionService routes (subscriptions/routes.ts):**
    - Remove SettingsService instantiation and param from SubscriptionService constructor call
    - Remove SettingsService import

    **SettingsService (settings/service.ts):**
    - Remove `getGracePeriodDays` and `setGracePeriodDays` methods entirely
    - The service can remain as an empty shell (it uses the system_settings table pattern for future settings), or keep the class with just the constructor. The grace period methods and routes are the only things to remove.

    **Settings routes/schemas:**
    - Remove GET/PUT grace-period endpoints from settings routes
    - Remove grace period schemas

    **Test updates (attendance.test.ts):**
    - Update all test assertions: status should be "confirmado" (not "registrado") after check-in
    - Remove any tests that check for `confirmedAt`
    - Remove grace period test scenarios (first-warning, hard-block after grace)
    - Remove fixed-day enforcement tests from attendance tests (fixed-day enforcement moves to booking in Plan 02)
    - Update the expired subscription test to expect simple "no active subscription" error (auto-expire catches it)
    - Add test: verify AURA is awarded immediately on QR check-in (check aura_ledger after check-in)
    - Update SettingsService removal from test setup (no longer passed to AttendanceService constructor)

    **Test updates (scheduling.test.ts):**
    - Remove fixed-day booking enforcement tests (will be reimplemented in Plan 02 with schedule slot references)
    - Remove grace period booking enforcement tests
    - Remove SettingsService from BookingService constructor in test setup

    **Test updates (settings.test.ts):**
    - Remove grace period test cases (GET/PUT endpoints no longer exist)
    - If the test file becomes empty, delete it

    CRITICAL: Run full test suite after all changes. Fix any TypeScript errors. Ensure all tests pass.

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-api && pnpm test</automated>
  </verify>
  <done>
    All grace period code removed from all services. Attendance always creates with status "confirmado" and awards 10 AURA immediately. No "registrado" status anywhere. No fixedDays/graceCheckInsAfterExpiry in subscription types or schema. SettingsService no longer has grace period methods. All tests pass with updated assertions.
  </done>
</task>

</tasks>

<verification>
- `cd el-templo-api && npx tsc --noEmit` compiles clean
- `cd el-templo-api && pnpm test` all tests pass
- `grep -r "registrado" el-templo-api/src/` returns NO results in attendance-related code
- `grep -r "graceCheckInsAfterExpiry\|grace_check_ins" el-templo-api/src/` returns NO results
- `grep -r "fixedDays\|fixed_days" el-templo-api/src/db/schema/subscriptions.ts` returns NO results
- `grep -r "confirmedAt\|confirmed_at" el-templo-api/src/modules/attendance/` returns NO results
- `grep -r "getGracePeriodDays\|setGracePeriodDays" el-templo-api/src/` returns NO results
</verification>

<success_criteria>

1. Migration 0041 drops grace period column, fixedDays column, confirmedAt column, changes attendance enum, creates subscription_schedules table
2. QR check-in creates attendance with status "confirmado" and awards AURA immediately
3. Force check-in creates attendance with status "confirmado" and awards AURA immediately
4. Expired subscription = hard block (no grace period logic anywhere)
5. No references to registrado, fixedDays, graceCheckInsAfterExpiry in service code
6. All existing tests updated and passing
   </success_criteria>

<output>
After completion, create `.planning/phases/61-qr-access-control/61-01-SUMMARY.md`
</output>
