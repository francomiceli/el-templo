---
phase: 34-franquicias-page
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-web/components/FranExpansion.vue
  - el-templo-web/components/FranFounder.vue
  - el-templo-web/components/FranVideo.vue
autonomous: true
requirements:
  - FRAN-05
  - FRAN-06

must_haves:
  truths:
    - "Expansion section shows animated counters, styled SVG map with Argentina+Spain pins, and sede list on Deep Charcoal background"
    - "Founder section displays Ignacio Bordon bio with horizontal timeline on desktop and vertical on mobile"
    - "Timeline milestones reveal progressively on scroll with stagger animation"
    - "Video/PDF section is conditionally rendered — does not appear when videoUrl and pdfUrl are both null"
  artifacts:
    - path: "el-templo-web/components/FranExpansion.vue"
      provides: "Expansion map + counters + sede list section"
    - path: "el-templo-web/components/FranFounder.vue"
      provides: "Founder bio + responsive timeline section"
    - path: "el-templo-web/components/FranVideo.vue"
      provides: "Conditional video/PDF section"
  key_links:
    - from: "el-templo-web/components/FranExpansion.vue"
      to: "el-templo-web/data/franquicias.ts"
      via: "import { expansionStats, expansionCities }"
      pattern: "expansionStats"
    - from: "el-templo-web/components/FranExpansion.vue"
      to: "el-templo-web/composables/useCountUp.ts"
      via: "import { useCountUp }"
      pattern: "useCountUp"
    - from: "el-templo-web/components/FranFounder.vue"
      to: "el-templo-web/data/franquicias.ts"
      via: "import { founderTimeline }"
      pattern: "founderTimeline"
    - from: "el-templo-web/components/FranVideo.vue"
      to: "el-templo-web/data/franquicias.ts"
      via: "import { franquiciasConfig }"
      pattern: "franquiciasConfig"
---

<objective>
Build the bottom content sections of /franquicias: expansion map with counters, founder bio with timeline, and conditional video/PDF section.

Purpose: These sections provide social proof (expansion), credibility (founder), and optional media content. Together with Plan 02's top sections and Plan 04's form, they complete the full franchise funnel.

Output: 3 Vue components following established BEM/token patterns.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.docs/brand-landing/spec-franquicias-pt1.md
@.docs/brand-landing/spec-franquicias-pt2.md
@el-templo-web/components/SectionCommunity.vue
@el-templo-web/composables/useScrollReveal.ts
@el-templo-web/data/franquicias.ts
@el-templo-web/composables/useCountUp.ts
@el-templo-web/assets/css/tokens.css

<interfaces>
<!-- From Plan 02 outputs (same wave, but data file is a shared dependency) -->

From data/franquicias.ts:

```typescript
export interface ExpansionStat {
  value: number;
  prefix: string;
  label: string;
}
export const expansionStats: ExpansionStat[];
export interface ExpansionCity {
  city: string;
  sedes: string;
}
export const expansionCities: ExpansionCity[];
export interface TimelineMilestone {
  year: string;
  description: string;
  isFuture?: boolean;
}
export const founderTimeline: TimelineMilestone[];
export const franquiciasConfig: {
  videoUrl: string | null;
  pdfUrl: string | null;
  whatsappUrl: string;
  investmentFigure: string;
};
```

From composables/useCountUp.ts:

```typescript
export function useCountUp(
  items: CountUpItem[],
  options?: UseCountUpOptions,
): {
  displayValues: Ref<string[]>;
  trigger: () => void;
  cleanup: () => void;
};
```

From composables/useScrollReveal.ts:

```typescript
export function useScrollReveal(options?): {
  revealed: Ref<boolean>;
  elementRef: Ref<HTMLElement | null>;
  cleanup: () => void;
};
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FranExpansion — expansion map, animated counters, sede list</name>
  <files>el-templo-web/components/FranExpansion.vue</files>
  <action>
    Build "DE MAR DEL PLATA AL MUNDO." section on Deep Charcoal (#3D3732) background with light text.

    **Layout structure:**
    1. Section title "DE MAR DEL PLATA AL MUNDO." (H2, Montserrat Bold 700, Marble Cream)
    2. Stats counters (4 items) — uses `useCountUp` composable triggered by `useScrollReveal`
    3. Custom SVG map — Argentina + Spain outline with brand-colored pins
    4. Sede list — table-style listing of cities and sedes
    5. Closing phrase: "Nac&iacute; en un garage. Hoy cruzo el oc&eacute;ano. Ma&ntilde;ana, tu ciudad." (Cormorant Garamond italic, Marble Cream)

    **Animated counters:**
    - Import `useCountUp` from composable and `expansionStats` from data file
    - Trigger count-up when stats scroll into view (watch `useScrollReveal.revealed`)
    - 4 stats in flex row on desktop, 2x2 grid on mobile per spec
    - Number: Montserrat ExtraBold 800, 40px -> 32px mobile, Marble Cream
    - Separator bar: Terracotta, 24px wide, 2px height
    - Label: Geologica 400, 14px, uppercase, Olive Stone color (use rgba(242,237,229,0.6) since on dark bg)

    **SVG Map (per CONTEXT.md):**
    - Custom inline SVG showing simplified outlines of Argentina and Spain
    - Argentina positioned left-center, Spain positioned right-top (stylized, not geographically accurate)
    - Pins: small circles (r=6) positioned at approximate Mar del Plata and Barcelona locations
    - Active pins: Terracotta (#C07A56) fill
    - "Pr&oacute;ximamente" callout: Aged Gold (#B89B5E) text element with dashed circle, text "¿Tu ciudad?"
    - SVG fills use warm-toned lines (rgba(242,237,229,0.15) for country outlines) on Deep Charcoal bg
    - SVG scales proportionally on all breakpoints (no replacement by list on mobile per CONTEXT.md decision)
    - Map width: 100% max-width 800px, centered

    **Sede list:**
    - Below map, simple 2-column layout: city name (left) | sedes (right)
    - City: Geologica 500, Marble Cream
    - Sedes: Geologica 400, rgba(242,237,229,0.7)
    - Rows separated by subtle border (rgba(242,237,229,0.1))

    **Responsive:**
    - Desktop: counters in row, map full width, sede list as table
    - Tablet: counters 2x2, map scaled down
    - Mobile: counters 2x2, map scaled, sede list stacked (city above sedes)

    **Entrance animations:**
    - useScrollReveal on counters container (triggers count-up)
    - useScrollReveal on map (fade-in + scale)
    - Closing phrase: useScrollReveal with fade-in

    Follow all standard patterns: BEM, tokens, HTML entities, prefers-reduced-motion, cleanup on unmount.

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo && pnpm --filter el-templo-web build</automated>
  </verify>
  <done>
    - FranExpansion renders with Deep Charcoal bg, light text
    - 4 animated counters trigger on scroll
    - SVG map shows Argentina + Spain with Terracotta/Aged Gold pins
    - Sede list displays all 3 city groups
    - Closing phrase in Cormorant Garamond italic
    - Responsive across 3 breakpoints
  </done>
</task>

<task type="auto">
  <name>Task 2: FranFounder — bio section + responsive timeline + FranVideo conditional</name>
  <files>
    el-templo-web/components/FranFounder.vue,
    el-templo-web/components/FranVideo.vue
  </files>
  <action>
    **Component 1: `FranFounder.vue`** (FRAN-06)

    Section "FUNDADO POR UN ATLETA. CONSTRUIDO CON M&Eacute;TODO." on Warm Linen (#EDE4D6) background.

    **Bio layout:**
    - Split layout: 55% text / 45% photo on desktop (matching SectionIdentity pattern)
    - Stacked on mobile (photo first via order: -1)
    - Photo: PlaceholderBox with label "Ignacio Bordon" (aspect-ratio 4/5 on desktop, 16/9 on mobile)
    - Bio text (Geologica 400, 16px, line-height 1.6):
      - "Ignacio Bordon. Ex gimnasta de alto rendimiento. M&aacute;s de 20 a&ntilde;os de experiencia en calistenia y entrenamiento con el peso del cuerpo."
      - "El Templo naci&oacute; en septiembre de 2020, en el garage de su casa en Mar del Plata. Lo que empez&oacute; como un m&eacute;todo personal se convirti&oacute; en un sistema internacional con sedes en dos continentes."
      - "La visi&oacute;n: democratizar el movimiento con el peso del cuerpo. Que cualquier persona pueda dominar su cuerpo a trav&eacute;s de un m&eacute;todo progresivo, sin importar desde d&oacute;nde empiece."

    **Timeline:**
    - Import `founderTimeline` from data file
    - **Desktop (horizontal):** Flex row with connecting line (2px horizontal, Terracotta)
      - Each milestone: year badge (Montserrat Bold, Terracotta bg, Marble Cream text, small pill shape) above the line
      - Description below the line (Geologica 400, 14px)
      - Last milestone ("Pr&oacute;ximo") uses Aged Gold instead of Terracotta
    - **Mobile (vertical):** Left-aligned vertical timeline
      - Vertical connecting line (2px, Terracotta) on the left
      - Year badges as circles/pills on the line
      - Description to the right of each node
    - **Animation per CONTEXT.md:** Sequential reveal per milestone with 0.15s stagger, connecting line draws progressively
      - Use useScrollReveal to trigger, then CSS transitions with inline transition-delay per milestone
      - The connecting line uses CSS transition on width (horizontal) or height (vertical) synced with reveals

    **Component 2: `FranVideo.vue`** (spec section 2.8)

    Conditionally rendered section on Marble Cream background:
    - Import `franquiciasConfig` from data file
    - `v-if="franquiciasConfig.videoUrl || franquiciasConfig.pdfUrl"` — section does not render at all if both are null
    - If videoUrl exists: self-hosted `<video>` element with controls, max-width 800px, centered
    - If pdfUrl exists: "DESCARGAR BROCHURE" button (btn btn--primary, download link)
    - Since both are null by default, this component will not render. The layout must not feel incomplete without it.
    - Keep it simple — this is a future placeholder that needs to work once URLs are configured.

    **Both components must follow all established patterns:** BEM, tokens, HTML entities, reduced-motion, scoped CSS, cleanup.

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo && pnpm --filter el-templo-web build</automated>
  </verify>
  <done>
    - FranFounder renders bio split layout + responsive timeline
    - Timeline is horizontal on desktop, vertical on mobile
    - Milestones reveal sequentially on scroll with 0.15s stagger
    - FranVideo is conditionally rendered (hidden by default since config values are null)
    - Both components compile and build without errors
  </done>
</task>

</tasks>

<verification>
1. `cd el-templo-web && pnpm build` — SSG generates without errors
2. FranExpansion shows counters, SVG map, sede list on Deep Charcoal bg
3. FranFounder shows bio + timeline (horizontal desktop, vertical mobile)
4. FranVideo does NOT render when config values are null
5. All components use BEM, tokens, HTML entities, reduced-motion guards
</verification>

<success_criteria>

- FranExpansion: animated counters via useCountUp, custom SVG map with pins, sede list, closing phrase
- FranFounder: split bio layout, responsive timeline (horizontal/vertical), sequential milestone reveal
- FranVideo: conditionally rendered, hidden by default, ready for future content
- All responsive at 3 breakpoints
- All compile and follow established patterns
  </success_criteria>

<output>
After completion, create `.planning/phases/34-franquicias-page/34-03-SUMMARY.md`
</output>
