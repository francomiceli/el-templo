---
phase: 120-fundaci-n-transversal-ticket-promedio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/analytics/duration-tier.ts
  - el-templo-api/src/modules/analytics/metric-shape.ts
  - el-templo-api/test/analytics/foundation-helpers.test.ts
autonomous: true
requirements: [FUND-01, FUND-02]
must_haves:
  truths:
    - "D-01: Any metric block can derive monthly|long_term|excluded from durationDays (≤1 excluded, 2–31 monthly, >31 long_term) via one shared helper — no column, no migration"
    - "D-02: The tier derivation is a single centralized helper with the thresholds as named constants (not magic numbers); a plan rename never changes its duration tier (tier is derived from durationDays, not name)"
    - "Any metric can be expressed as a uniform { nominal, percentage, n } shape with n always reported"
    - "Percentage and median helpers never produce NaN on empty input (return 0 / null respectively)"
  artifacts:
    - path: "el-templo-api/src/modules/analytics/duration-tier.ts"
      provides: "deriveDurationTier + named threshold constants"
      exports:
        [
          "deriveDurationTier",
          "ONE_OFF_MAX_DURATION_DAYS",
          "MONTHLY_MAX_DURATION_DAYS",
          "DurationTier",
        ]
    - path: "el-templo-api/src/modules/analytics/metric-shape.ts"
      provides: "nominal+%+n shape builder + median helper"
      exports: ["metricShape", "median", "MetricShape"]
    - path: "el-templo-api/test/analytics/foundation-helpers.test.ts"
      provides: "unit coverage for both helpers"
  key_links:
    - from: "metric-shape.ts"
      to: "duration-tier.ts"
      via: "no import — siblings; both consumed by ticket-service and breakdowns"
      pattern: "export function (deriveDurationTier|metricShape|median)"
---

<objective>
Build the two pure foundation utilities consumed by all 6 metric blocks: `duration-tier.ts` (FUND-01 — derive monthly/long_term/excluded from `subscription_plans.durationDays` with named-constant thresholds) and `metric-shape.ts` (FUND-02 — the uniform `{ nominal, percentage, n }` shape + a reusable `median` helper). These are stateless functions with no DB access; Block 6 (ticket) is their first consumer.

Purpose: Lock the cross-block contracts (duration tier rule + metric shape) in one place so phases 121-123 consume them identically. Per D-01/D-02 there is NO migration and NO column for duration_tier — it is pure derivation.
Output: Two new utility modules + a unit test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-fundaci-n-transversal-ticket-promedio/120-CONTEXT.md
@.planning/phases/120-fundaci-n-transversal-ticket-promedio/120-PATTERNS.md

<interfaces>
<!-- House-style references the executor must mirror. No codebase exploration needed beyond these. -->

Named-threshold constant convention (retention-service.ts:48-54):
export const CONSECUTIVE_CYCLE_GAP_DAYS = 30; // exported product rule, NOT env/column

median() to copy verbatim (funnel-service.ts:81-87):
function median(values: number[]): number | null {
if (values.length === 0) return null;
const sorted = [...values].sort((a, b) => a - b);
const mid = Math.floor(sorted.length / 2);
if (sorted.length % 2 === 1) return sorted[mid];
return (sorted[mid - 1] + sorted[mid]) / 2;
}

Div-by-zero guard convention (advanced-finance-service.ts:148-150, retention-service.ts:189-192):
n > 0 ? Math.round((reached / n) \* 100) : 0 // never NaN

Source column: subscription_plans.durationDays is `int("duration_days").notNull()` (subscription-plans.ts:40).
Real plan durations validated in DB: 1 / 30 / 120 / 180 / 240 days.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: duration-tier.ts — derive tier from durationDays (FUND-01/D-01/D-02)</name>
  <read_first>
    - el-templo-api/src/modules/analytics/retention-service.ts (lines 48-54, named-constant convention to mirror)
    - el-templo-api/src/db/schema/subscription-plans.ts (durationDays column, line 40)
    - .planning/phases/120-fundaci-n-transversal-ticket-promedio/120-PATTERNS.md (duration-tier.ts section)
  </read_first>
  <behavior>
    - deriveDurationTier(null) → null (excluded)
    - deriveDurationTier(0) → null; deriveDurationTier(1) → null (≤ ONE_OFF_MAX_DURATION_DAYS → excluded one-off: Clase única / Sesión de Prueba)
    - deriveDurationTier(2) → "monthly"; deriveDurationTier(30) → "monthly"; deriveDurationTier(31) → "monthly"
    - deriveDurationTier(32) → "long_term"; deriveDurationTier(120) → "long_term"; deriveDurationTier(180) → "long_term"; deriveDurationTier(240) → "long_term"
  </behavior>
  <action>
    Create `el-templo-api/src/modules/analytics/duration-tier.ts`. Export two named constants with docblocks mirroring retention-service.ts:48-54 style (exported product rule, NOT env var, NOT DB column): `ONE_OFF_MAX_DURATION_DAYS = 1` and `MONTHLY_MAX_DURATION_DAYS = 31`. Export `type DurationTier = "monthly" | "long_term"`. Export pure function `deriveDurationTier(durationDays: number | null): DurationTier | null` implementing D-01: return `null` when `durationDays === null` OR `durationDays <= ONE_OFF_MAX_DURATION_DAYS` (excluded one-off); otherwise return `durationDays <= MONTHLY_MAX_DURATION_DAYS ? "monthly" : "long_term"`. Document in the file header that null = excluded from duration breakdowns (one-off, not a recurring membership) and that the enum `planTier` is intentionally NOT used (per D-01 the day threshold is robust to renames). No `any`. No DB access.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit 2>&1 | grep -i "duration-tier" ; test $? -ne 0 && echo "no type errors in duration-tier"</automated>
  </verify>
  <acceptance_criteria>
    - duration-tier.ts exports `ONE_OFF_MAX_DURATION_DAYS = 1` and `MONTHLY_MAX_DURATION_DAYS = 31` as named constants (no inline `1`/`31` inside deriveDurationTier).
    - `deriveDurationTier` returns null for ≤1 and for null, "monthly" for 2..31, "long_term" for >31.
    - File has no DB import and no `any` type.
  </acceptance_criteria>
  <done>deriveDurationTier and both threshold constants exported; tier boundaries (1/31) honored exactly; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: metric-shape.ts — uniform nominal+%+n shape + median (FUND-02)</name>
  <read_first>
    - el-templo-api/src/modules/analytics/funnel-service.ts (lines 81-87, median to copy verbatim)
    - el-templo-api/src/modules/analytics/advanced-finance-service.ts (lines 148-150, div-by-zero guard)
    - .planning/phases/120-fundaci-n-transversal-ticket-promedio/120-PATTERNS.md (metric-shape.ts section)
  </read_first>
  <behavior>
    - metricShape(nominal: 50, total: 200) → { nominal: 50, percentage: 25, n: 200 }
    - metricShape(nominal: 3, total: 0) → { nominal: 3, percentage: 0, n: 0 } (n===0 guard, never NaN)
    - metricShape rounds percentage to an integer (Math.round)
    - median([]) → null
    - median([5]) → 5; median([1,2,3]) → 2; median([1,2,3,4]) → 2.5
  </behavior>
  <action>
    Create `el-templo-api/src/modules/analytics/metric-shape.ts`. Export `interface MetricShape { nominal: number; percentage: number; n: number; }`. Export `function metricShape(nominal: number, total: number): MetricShape` returning `{ nominal, percentage: total > 0 ? Math.round((nominal / total) * 100) : 0, n: total }` (guard div-by-zero → 0, documented, NEVER NaN; mirror advanced-finance-service.ts:148-150). Export `function median(values: number[]): number | null` copied verbatim from funnel-service.ts:81-87 (empty → null, never NaN). Document the "always declare every field, default 0/null" discipline referenced in schemas.ts:119-121 (fast-json-stringify strips undeclared fields downstream). No `any`.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit 2>&1 | grep -i "metric-shape" ; test $? -ne 0 && echo "no type errors in metric-shape"</automated>
  </verify>
  <acceptance_criteria>
    - metric-shape.ts exports `metricShape`, `median`, and `MetricShape`.
    - metricShape(3, 0) returns `{ nominal: 3, percentage: 0, n: 0 }` (no NaN).
    - median([]) returns null; median([1,2,3,4]) returns 2.5.
  </acceptance_criteria>
  <done>metricShape + median exported with documented div-by-zero/empty guards; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: foundation-helpers.test.ts — unit coverage for both helpers</name>
  <read_first>
    - el-templo-api/test/analytics/advanced-finance.test.ts (lines 1-70, test harness imports/style)
    - el-templo-api/src/modules/analytics/duration-tier.ts (the file under test, after Task 1)
    - el-templo-api/src/modules/analytics/metric-shape.ts (the file under test, after Task 2)
  </read_first>
  <action>
    Create `el-templo-api/test/analytics/foundation-helpers.test.ts` using vitest (`import { describe, it, expect } from "vitest"`). These are pure functions — NO MySQL, NO createTestApp needed. Cover deriveDurationTier for the boundary table (null, 0, 1 → null; 2, 30, 31 → "monthly"; 32, 120, 180, 240 → "long_term") asserting against the real validated durations from CONTEXT (1/30/120/180/240). Cover metricShape: normal ratio (50/200 → 25%), zero-total guard (3/0 → percentage 0, n 0, no NaN via `Number.isNaN` assertion). Cover median: empty → null, single, odd-length, even-length (2.5). Do NOT run the suite locally (MEMORY: tests run in CI).
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit 2>&1 | grep -i "foundation-helpers" ; test $? -ne 0 && echo "test file typechecks"</automated>
  </verify>
  <acceptance_criteria>
    - foundation-helpers.test.ts asserts deriveDurationTier(31)==="monthly" and deriveDurationTier(32)==="long_term" and deriveDurationTier(1)===null.
    - Asserts `Number.isNaN(metricShape(3,0).percentage)` is false.
    - Asserts median([]) is null and median([1,2,3,4]) is 2.5.
  </acceptance_criteria>
  <done>Test file compiles, covers both helpers including null/empty/boundary cases. Not run locally.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                                  |
| -------- | -------------------------------------------------------------------------------------------- |
| (none)   | Pure in-process utilities with no I/O, no DB, no network, no user input crossing a boundary. |

## STRIDE Threat Register

| Threat ID | Category          | Component                                 | Disposition | Mitigation Plan                                                                                                                                     |
| --------- | ----------------- | ----------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-120-01  | Tampering         | deriveDurationTier threshold drift        | accept      | Thresholds are named exported constants with docblocks; any change is a deliberate, reviewable code edit. No runtime/external override path exists. |
| T-120-02  | Denial of Service | NaN/Infinity propagation from div-by-zero | mitigate    | metricShape guards `total > 0` returning 0; median returns null on empty. Unit tests assert no NaN.                                                 |
| T-120-SC  | Tampering         | npm/pip/cargo installs                    | mitigate    | No package installs in this plan. No new dependencies.                                                                                              |

</threat_model>

<verification>
- Typecheck passes: `cd el-templo-api && pnpm exec tsc --noEmit` reports no errors in the two new files or the test.
- Both helpers exported and importable by downstream plans (02 breakdowns, 04 ticket).
- No `console.log`, no `any`, no DB import in either utility.
</verification>

<success_criteria>

- deriveDurationTier resolves the real plan durations (1/30/120/180/240) to excluded/monthly/long_term correctly.
- metricShape and median never return NaN.
- duration_tier introduces NO migration and NO column (D-01/D-02 honored).
  </success_criteria>

<output>
Create `.planning/phases/120-fundaci-n-transversal-ticket-promedio/120-01-SUMMARY.md` when done.
</output>
