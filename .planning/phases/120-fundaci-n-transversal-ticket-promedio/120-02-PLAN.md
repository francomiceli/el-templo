---
phase: 120-fundaci-n-transversal-ticket-promedio
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/schema/subscriptions.ts
  - el-templo-api/src/db/migrations/0136_add_subscriptions_price_regular_snapshot.sql
  - el-templo-api/src/modules/subscriptions/service.ts
autonomous: true
requirements: [TICKET-03]
must_haves:
  truths:
    - "Every NEW membership charge (assign/change-now/change-after/renew) records the plan's current priceRegular at charge time"
    - "Existing subscription rows keep a NULL snapshot (no backfill — the historical list price was never stored)"
    - "The bulkMigratePlan legacy-migration insert (pricePaid:0, no charge) is INTENTIONALLY left with a NULL snapshot — it is not a real charge and stays out of the ticket"
    - "The discount base for new charges is faithful (price_paid vs the captured snapshot, not a drifting current priceRegular)"
  artifacts:
    - path: "el-templo-api/src/db/schema/subscriptions.ts"
      provides: "priceRegularSnapshot nullable int column"
      contains: "priceRegularSnapshot"
    - path: "el-templo-api/src/db/migrations/0136_add_subscriptions_price_regular_snapshot.sql"
      provides: "single ADD COLUMN migration, no backfill"
      contains: "ADD COLUMN price_regular_snapshot"
    - path: "el-templo-api/src/modules/subscriptions/service.ts"
      provides: "snapshot capture at exactly the 4 real-charge insert sites (5th legacy-migration site deliberately excluded)"
  key_links:
    - from: "subscriptions/service.ts"
      to: "subscription_plans.priceRegular"
      via: "priceRegularSnapshot: <resolvedPlan>.priceRegular at each of the 4 real-charge tx.insert(subscriptions)"
      pattern: "priceRegularSnapshot: (plan|newPlan|targetPlan)\\.priceRegular"
---

<objective>
Add the forward price snapshot (D-05/D-06): one nullable column `price_regular_snapshot` on `subscriptions`, and capture the plan's current `priceRegular` into it at the 4 membership-charge insert sites in `SubscriptionService` (assign, change-now, change-after-current, renew). This is the ONLY migration in the phase and the data foundation for the faithful ticket discount (TICKET-03, consumed in Plan 04).

Purpose: There is no historical list-price snapshot (verified). Capturing it forward makes the discount metric truthful for all new charges; historical charges fall back to current `priceRegular` with a disclaimer (handled in Plan 04). Column is nullable because pre-migration rows have no snapshot and no backfill is possible.
Output: schema column + 0136 migration SQL + 4 capture edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-fundaci-n-transversal-ticket-promedio/120-CONTEXT.md
@.planning/phases/120-fundaci-n-transversal-ticket-promedio/120-PATTERNS.md

<interfaces>
<!-- Exact insert-site variable names verified in code. The resolved plan variable DIFFERS per site. -->

subscriptions schema price columns (subscriptions.ts:57-64):
pricePaid: int("price_paid").notNull(),
currency: varchar("currency", { length: 3 }).default("ARS").notNull(),
priceTypeApplied, auraDiscount, auraDiscountPercent, boardingPassUsed,
priceOverrideAmount: int("price_override_amount"),
priceOverrideReason: text("price_override_reason"), // ← migration ADD COLUMN ... AFTER price_override_reason

Source value: subscription_plans.priceRegular is `int("price_regular").notNull()` (subscription-plans.ts:37).

The 4 REAL-CHARGE tx.insert(schema.subscriptions).values({...}) sites and the IN-SCOPE plan variable each uses for `currency`
(grep-verified — names differ; reuse the SAME variable for the snapshot):
~1075 assignPlan → currency: plan.currency → ADD priceRegularSnapshot: plan.priceRegular
~2414 changePlanNow → currency: newPlan.currency → ADD priceRegularSnapshot: newPlan.priceRegular
~2788 changePlanAfterCurrent → currency: targetPlan.currency → ADD priceRegularSnapshot: targetPlan.priceRegular
~3059 renewSubscription → currency: plan.currency → ADD priceRegularSnapshot: plan.priceRegular

5th insert site — DELIBERATELY EXCLUDED (do NOT add a snapshot here):
~3271 bulkMigratePlan → legacy migration insert with `pricePaid: 0`, NO `currency:` field, NO `plan_charge` financial_transaction.
This is NOT a real membership charge (it is a one-shot legacy plan migration that records $0 and emits no charge), so it
correctly stays OUT of the ticket universe and MUST leave `priceRegularSnapshot` NULL. The grep-count gate below is `== 4`
ON PURPOSE — capturing a snapshot here would be a bug. Leave this site untouched.

Migration analog: 0133_add_bookings_source.sql (single ADD COLUMN, no backfill). Last migration is 0135 → next is 0136.

Migration comment safety (Phase 103-01 invariant): the custom runner splits on `;` BEFORE stripping `--` line
comments, so NO semicolon character may appear inside any comment line. ADD COLUMN without IF NOT EXISTS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add priceRegularSnapshot column to subscriptions schema</name>
  <read_first>
    - el-templo-api/src/db/schema/subscriptions.ts (full file — price columns block at 57-64)
    - .planning/phases/120-fundaci-n-transversal-ticket-promedio/120-PATTERNS.md (subscriptions schema section)
  </read_first>
  <action>
    In `el-templo-api/src/db/schema/subscriptions.ts`, add a nullable int column `priceRegularSnapshot: int("price_regular_snapshot")` immediately after the `priceOverrideReason` column (line 64), mirroring the existing nullable-int style (e.g. `priceOverrideAmount: int("price_override_amount")` at line 63). Add an inline comment: nullable — NULL for all rows before migration 0136; captures the plan's current price_regular at each new membership charge for the faithful ticket discount (D-05). Do NOT add any index. Do NOT run drizzle-kit generate.
  </action>
  <verify>
    <automated>cd el-templo-api && grep -c "priceRegularSnapshot" src/db/schema/subscriptions.ts</automated>
  </verify>
  <acceptance_criteria>
    - subscriptions.ts contains `priceRegularSnapshot: int("price_regular_snapshot")` (nullable — no `.notNull()`).
    - Column placed after priceOverrideReason.
    - No drizzle-kit generate was run (migration is hand-written in Task 2).
  </acceptance_criteria>
  <done>priceRegularSnapshot nullable int column present in subscriptions schema.</done>
</task>

<task type="auto">
  <name>Task 2: Hand-write 0136 ADD COLUMN migration (no semicolons in comments)</name>
  <read_first>
    - el-templo-api/src/db/migrations/0133_add_bookings_source.sql (exact single-ADD-COLUMN analog)
    - el-templo-api/CLAUDE.md project rules (manual SQL, no drizzle-kit, no `;` in comments)
  </read_first>
  <action>
    Create `el-templo-api/src/db/migrations/0136_add_subscriptions_price_regular_snapshot.sql`. Hand-written SQL only. Header comment (Phase 120 D-05/D-06) explaining: captures plan price_regular at each new membership charge so the ticket discount is faithful forward; existing rows stay NULL (no backfill — list price was never stored); include the comment-safety note that the runner splits on `;` before stripping `--`. CRITICAL: NO `;` character may appear inside ANY comment line. Exactly ONE statement: `ALTER TABLE subscriptions ADD COLUMN price_regular_snapshot INT NULL AFTER price_override_reason;`. No `IF NOT EXISTS`. Commit this SQL file alongside the schema change (MEMORY: always commit migration SQL).
  </action>
  <verify>
    <automated>cd el-templo-api && f=src/db/migrations/0136_add_subscriptions_price_regular_snapshot.sql; grep -c "ADD COLUMN price_regular_snapshot" "$f" && ! grep -n '^--.*;' "$f" && echo "no semicolons in comment lines"</automated>
  </verify>
  <acceptance_criteria>
    - File 0136_add_subscriptions_price_regular_snapshot.sql contains exactly one ADD COLUMN statement for `price_regular_snapshot INT NULL AFTER price_override_reason`.
    - No line beginning with `--` contains a `;` character.
    - No `IF NOT EXISTS`, no `drizzle-kit` artifacts.
  </acceptance_criteria>
  <done>0136 migration exists, single ADD COLUMN, semicolon-safe comments, committed with the schema change.</done>
</task>

<task type="auto">
  <name>Task 3: Capture priceRegularSnapshot at the 4 real-charge SubscriptionService insert sites (NOT the 5th legacy site)</name>
  <read_first>
    - el-templo-api/src/modules/subscriptions/service.ts (the 4 real-charge insert blocks near lines 1075, 2414, 2788, 3059 — confirm the in-scope plan variable name at each; ALSO read the 5th insert at ~3271 bulkMigratePlan to confirm it is the legacy $0 migration to LEAVE UNTOUCHED)
    - .planning/phases/120-fundaci-n-transversal-ticket-promedio/120-PATTERNS.md (subscriptions/service.ts capture section)
  </read_first>
  <action>
    In `el-templo-api/src/modules/subscriptions/service.ts`, add `priceRegularSnapshot: <plan>.priceRegular` to each of the 4 REAL-CHARGE `tx.insert(schema.subscriptions).values({...})` blocks, reusing the SAME resolved-plan variable already used for the adjacent `currency:` field at that site (verified names): assign ~1075 → `plan.priceRegular`; change-now ~2414 → `newPlan.priceRegular`; change-after-current ~2788 → `targetPlan.priceRegular`; renew ~3059 → `plan.priceRegular`. Place the new field next to the existing `currency:` line in each values object. Before editing each site, confirm the plan variable is in scope and carries `priceRegular` (subscription_plans.priceRegular is notNull int). DO NOT touch the 5th insert site at ~3271 (`bulkMigratePlan`): it is a legacy plan migration that writes `pricePaid: 0`, sets no `currency`, and emits no `plan_charge` — it must keep `priceRegularSnapshot` NULL so it stays out of the ticket. The grep-count gate is `== 4` precisely because this 5th site is intentionally excluded; do not "fix" it to 5. No `any`. Do not alter pricePaid or any other field.
  </action>
  <verify>
    <automated>cd el-templo-api && grep -c "priceRegularSnapshot:" src/modules/subscriptions/service.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "priceRegularSnapshot:" src/modules/subscriptions/service.ts` returns exactly 4 (the 5th `bulkMigratePlan` insert is DELIBERATELY excluded — count of 5 means a regression).
    - Each occurrence uses the same plan variable as the sibling `currency:` line (plan / newPlan / targetPlan / plan).
    - The `bulkMigratePlan` insert (~3271, `pricePaid: 0`, no `currency`) still has NO `priceRegularSnapshot` field.
    - `cd el-templo-api && pnpm exec tsc --noEmit` reports no errors in subscriptions/service.ts.
  </acceptance_criteria>
  <done>The 4 real membership-charge insert sites capture the plan's current priceRegular into priceRegularSnapshot; the 5th legacy-migration insert is left NULL on purpose; typecheck clean.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary           | Description                                                                                                                                                                                       |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| service → DB write | New column value written inside the existing `this.db.transaction(...)` at sub-insert time. Same trust level as adjacent fields; value sourced from an already-resolved plan row, not user input. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                 | Disposition | Mitigation Plan                                                                                                                                                                                                         |
| --------- | ---------------------- | ------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-120-03  | Tampering              | SQL migration corruption via `;` in comments                              | mitigate    | Verify step asserts no `--` comment line contains `;`; single ADD COLUMN statement only.                                                                                                                                |
| T-120-04  | Information disclosure | snapshot exposes nothing new (already-readable list price)                | accept      | priceRegular is the plan's public list price; storing it per-subscription leaks no new info beyond existing finance data.                                                                                               |
| T-120-05  | Tampering              | snapshot captured from wrong plan variable (NaN/null discount downstream) | mitigate    | Each site reuses the SAME verified plan variable as the adjacent `currency:` field; grep-count == 4 asserted (5th legacy site deliberately excluded); downstream discount in Plan 04 guards null snapshot via fallback. |
| T-120-SC  | Tampering              | npm/pip/cargo installs                                                    | mitigate    | No package installs in this plan. No new dependencies.                                                                                                                                                                  |

</threat_model>

<verification>
- Migration is hand-written, single ADD COLUMN, no `;` in comment lines, file numbered 0136.
- Schema column nullable; no backfill attempted.
- The 4 real-charge insert sites capture the snapshot (grep count == 4) with the correct in-scope plan variable; the 5th `bulkMigratePlan` site is left NULL intentionally.
- Typecheck clean across schema + service.
- Migration applied in CI/staging via `pnpm db:migrate` (NOT locally; NOT via drizzle-kit migrate).
</verification>

<success_criteria>

- New subscriptions written by assign/change/renew carry a non-null priceRegularSnapshot equal to the plan's priceRegular at charge time.
- Pre-migration rows AND bulkMigratePlan legacy-migration rows remain NULL.
- This is the only migration in Phase 120 (D-06).
  </success_criteria>

<output>
Create `.planning/phases/120-fundaci-n-transversal-ticket-promedio/120-02-SUMMARY.md` when done.
</output>
