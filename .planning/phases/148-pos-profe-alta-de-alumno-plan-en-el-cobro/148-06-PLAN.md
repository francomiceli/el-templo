---
phase: 148-pos-profe-alta-de-alumno-plan-en-el-cobro
plan: 06
type: execute
wave: 3
depends_on: ["148-03"]
files_modified:
  - el-templo-admin/src/components/caja/BandejaPendientesTab.vue
autonomous: true
requirements: [ALTA-06]
must_haves:
  truths:
    - "Al anular en la bandeja una carga que creó un alumno nuevo, gestión ve una advertencia: la membresía se desactivará y el alumno quedará inactivo (no se elimina)"
    - "Al anular una carga de alumno preexistente, la copy es la actual (sin advertencia de cascade)"
  artifacts:
    - path: "el-templo-admin/src/components/caja/BandejaPendientesTab.vue"
      provides: "Copy condicional en el dialog Anular según createdMemberName"
      contains: "createdMember"
  key_links:
    - from: "dialog Anular"
      to: "row.createdMemberName"
      via: "copy condicional creó-alumno-nuevo vs preexistente"
      pattern: "createdMember"
---

<objective>
La mitad frontend de ALTA-06: en la bandeja de Pendientes (`BandejaPendientesTab.vue`, Fase 141), cuando gestión va a **anular** una carga que **creó un alumno nuevo** (la transacción trae `createdMemberName`, surfacado por 148-03), el dialog de Anular muestra la copy de advertencia del UI-SPEC (sección Destructive):

> "Esta carga creó al alumno **{nombre}**. Al anular, también se desactivará su membresía y el alumno quedará **inactivo** (no se elimina). ¿Anular de todos modos?" — botón confirmar rojo $negative.

Para cargas de alumno **preexistente** (`createdMemberName` null) la copy queda como hoy (solo se anula ese pago/membresía).

Purpose: que gestión entienda el efecto-cascada antes de confirmar, evitando anulaciones sorpresivas que dejan inactivo a un alumno.
Output: copy condicional en el dialog Anular; lint verde; UAT visual al cierre.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/148-pos-profe-alta-de-alumno-plan-en-el-cobro/148-UI-SPEC.md
@.planning/phases/148-pos-profe-alta-de-alumno-plan-en-el-cobro/148-03-SUMMARY.md

<interfaces>
<!-- BandejaPendientesTab.vue: la acción "Anular" está en el menú de fila (L189-191);
     el dialog Anular membership 1-a-1 está alrededor de L313. La fila (row) de la
     transacción ahora trae createdMemberId + createdMemberName (surfacados por 148-03).
     La copy destructiva exacta está en 148-UI-SPEC.md sección "Destructive confirmation".
     Palette: confirmar rojo $negative; la advertencia previa puede usar $warning/$negative
     pero el botón de confirmar es $negative. Copy en español. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy condicional en el dialog Anular según createdMemberName</name>
  <files>el-templo-admin/src/components/caja/BandejaPendientesTab.vue</files>
  <read_first>
    - el-templo-admin/src/components/caja/BandejaPendientesTab.vue (acción Anular L189-191, dialog Anular ~L313, cómo se setea la row seleccionada al abrir openAnular)
    - .planning/phases/148-pos-profe-alta-de-alumno-plan-en-el-cobro/148-UI-SPEC.md (sección Destructive confirmation, copy exacta)
  </read_first>
  <action>
    En el dialog de Anular, agregar un bloque condicional: cuando la transacción seleccionada tiene `createdMemberName` (no null), mostrar arriba del confirm la advertencia con la copy EXACTA del UI-SPEC, interpolando el nombre: "Esta carga creó al alumno {createdMemberName}. Al anular, también se desactivará su membresía y el alumno quedará inactivo (no se elimina). ¿Anular de todos modos?" (usar `q-banner`/texto con icono, tono de alerta; botón confirmar rojo $negative — ya existente). Cuando `createdMemberName` es null, mantener la copy actual del dialog sin cambios.

    Asegurar que `createdMemberName` esté disponible en la row/objeto que alimenta el dialog (proviene del payload de 148-03 a través del composable que lista pendientes — `useTransactionsApi`/el listado de la bandeja). Si el tipo de la fila no lo incluye, agregar el campo opcional al tipo consumido (sin `any`).

  </action>
  <acceptance_criteria>
    - El dialog Anular muestra la advertencia de cascade SOLO cuando createdMemberName no es null, con el nombre interpolado
    - Para createdMemberName null la copy es la actual (sin advertencia)
    - El botón de confirmar de anular sigue siendo rojo $negative
    - `grep -c "createdMember" el-templo-admin/src/components/caja/BandejaPendientesTab.vue` ≥ 1
    - Sin `any` (CLAUDE.md)
    - `cd el-templo-admin && pnpm lint` sin errores sobre el archivo
    - HUMAN-CHECK (UAT cierre): anular una carga de alumno-nuevo muestra la advertencia; una de preexistente no
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-admin && pnpm exec eslint -c ./eslint.config.js src/components/caja/BandejaPendientesTab.vue 2>&1 | tail -5; grep -c "createdMember" src/components/caja/BandejaPendientesTab.vue</automated>
    <human-check>En la bandeja de Pendientes, anular una carga creada por un profe que dio de alta a un alumno nuevo muestra la advertencia "Esta carga creó al alumno {nombre}…"; anular una carga de un alumno ya existente no la muestra.</human-check>
  </verify>
  <done>Dialog Anular con copy condicional de cascade según createdMemberName; preexistente intacto. lint verde, sin any.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary             | Description                                                                          |
| -------------------- | ------------------------------------------------------------------------------------ |
| gestión → API (void) | El cascade real es server-side (148-03); esta copy es informativa antes de confirmar |

## STRIDE Threat Register

| Threat ID | Category         | Component                             | Disposition | Mitigation Plan                                                                                                         |
| --------- | ---------------- | ------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------- |
| T-148-20  | Repudiation / UX | gestión anula sin saber del cascade   | mitigate    | copy de advertencia interpolando el alumno creado antes del confirm rojo                                                |
| T-148-21  | Info disclosure  | createdMemberName a roles sin permiso | accept      | el listado de pendientes ya está gateado a FINANCE_VOID_ROLES/gestión; el campo viaja en el mismo payload ya autorizado |
| T-148-SC  | Tampering        | npm/pip/cargo installs                | accept      | sin dependencias nuevas en esta fase                                                                                    |

</threat_model>

<verification>
- `cd el-templo-admin && pnpm lint` verde sobre BandejaPendientesTab.vue.
- UAT visual: copy condicional según created-member.
</verification>

<success_criteria>

- Dialog Anular advierte del cascade cuando la carga creó alumno; copy actual para preexistente; confirmar $negative.
  </success_criteria>

<output>
Create `.planning/phases/148-pos-profe-alta-de-alumno-plan-en-el-cobro/148-06-SUMMARY.md` when done
</output>
