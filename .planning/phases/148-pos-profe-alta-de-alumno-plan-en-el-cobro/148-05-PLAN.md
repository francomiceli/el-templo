---
phase: 148-pos-profe-alta-de-alumno-plan-en-el-cobro
plan: 05
type: execute
wave: 3
depends_on: ["148-02"]
files_modified:
  - el-templo-admin/src/composables/useFinanceLoadApi.ts
  - el-templo-admin/src/pages/CargarPagoPage.vue
autonomous: true
requirements: [ALTA-01, ALTA-02, ALTA-03, ALTA-04, ALTA-07]
must_haves:
  truths:
    - "El profe ve un 3er modo 'Alta + plan' en CargarPagoPage y puede crear un alumno nuevo o elegir uno existente"
    - "El DNI dispara dedup on-blur y, si matchea, ofrece cargar sobre el alumno existente (no duplica)"
    - "El profe elige sucursal (default su sede), plan (grid por tier) con toggle Zero, precio auto según medio de pago (parcial → banner deuda), y turnos solo para planes fixed"
    - "Confirmar manda un alta atómica idempotente y el ticket aparece con badge Pendiente + chip Nuevo si creó alumno"
  artifacts:
    - path: "el-templo-admin/src/composables/useFinanceLoadApi.ts"
      provides: "altaConPlan + tipos CoachAltaInput/CoachAltaResponse"
      contains: "altaConPlan"
    - path: "el-templo-admin/src/pages/CargarPagoPage.vue"
      provides: "Modo 'alta' con sucursal, mini-form alumno+dedup, plan grid, FixedSchedulePicker, precio, Confirmar"
      contains: "alta"
  key_links:
    - from: "CargarPagoPage.vue modo alta"
      to: "useFinanceLoadApi.altaConPlan"
      via: "Confirmar"
      pattern: "altaConPlan"
    - from: "DNI on-blur"
      to: "useMembersApi check-duplicates"
      via: "dedup banner"
      pattern: "check-duplicates"
    - from: "plan fixed seleccionado"
      to: "FixedSchedulePicker"
      via: "render condicional bookingMode==='fixed'"
      pattern: "FixedSchedulePicker"
---

<objective>
Frontend del PoS (coach): el 3er modo **"Alta + plan"** dentro de `CargarPagoPage.vue` (panel inline, NO dialog — A1 de UI-SPEC), más el método `altaConPlan` del composable. Materializa el UI-SPEC aprobado (6/6).

Reusa verbatim de la pantalla existente: socio typeahead, botones de medio de pago (≥56px), Confirmar sticky, ciclo de idempotencia (`currentIdempotencyKey`), `resetChargeFields`/`onModeChange`, lista "Mis cargas de hoy". Superficie nueva: chip sucursal, "+ Nuevo alumno" + mini-form (Nombre/Apellido/DNI), banner dedup DNI on-blur, grilla de planes por tier (copiada de AssignPlanDialog), toggle Precio Zero, precio auto por medio de pago + banner deuda, `FixedSchedulePicker` condicional para planes fixed.

Purpose: que el profe cargue alumno + plan en mostrador en mínimos pasos, reemplazando el Google Form.
Output: composable + página, lint verde, comportamiento según UI-SPEC; UAT visual al cierre de fase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/148-pos-profe-alta-de-alumno-plan-en-el-cobro/148-UI-SPEC.md
@.planning/phases/148-pos-profe-alta-de-alumno-plan-en-el-cobro/148-PATTERNS.md
@.planning/phases/148-pos-profe-alta-de-alumno-plan-en-el-cobro/148-02-SUMMARY.md

<interfaces>
<!-- Análogos exactos. Reusar, no inventar. -->

useFinanceLoadApi.ts: payPlan (L114-129) / miscCharge (L135-150) — misma forma
(loading/error refs, api.post, extractError, re-throw). CoachPayPlanInput (L28-35).
Exportar altaConPlan en el return (L179-187).

CargarPagoPage.vue:

- q-btn-toggle (L7-19), @update:model-value="onModeChange"; type Mode = 'renew' | 'misc' (L240) → agregar 'alta'.
- socio typeahead (L25-56), onMemberSearch; #no-option (L39-45) para "+ Nuevo alumno".
- deuda banner pattern (L60-71) → reusar para el banner dedup DNI (bg-warning text-dark).
- payment-method buttons (L141-161), Monto input (L97-103).
- canConfirm (L302-310), resetChargeFields (L370), onModeChange (L387).
- currentIdempotencyKey (L283), regen-on-success (L427-430, L457).
- Mis cargas list (L180-202).
- Confirmar sticky (L208-221, :disable="!canConfirm || submitting").

Plan grid: AssignPlanDialog.vue plan grid (L54-83) + plansByTier (L1192-1204) +
tierColor/tierLabel (L1414-1426) + getBasePrice mapping (L1428-1438) + isPartialCharge
(L1276-1279) + banner parcial (L783-794). Planes vía useSubscriptionsApi().getPlans(true, { branchId }) (L1447).

FixedSchedulePicker.vue (REUSE sin editar): v-model number[], :branch-id (sucursalId),
:required-count="plan.classesPerWeek", :allow-partial="false", :multi-branch sólo si plan.multiBranch.
Renderiza SÓLO si plan.bookingMode==='fixed'.

useMembersApi: searchMembers (typeahead) + check-duplicates?dni= (ya importado en la page, L237).

Copy EXACTA y palette: ver Copywriting Contract + Color de 148-UI-SPEC.md (terracotta $primary
para selección activa; $warning gold para deuda/dedup; nunca azul).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: altaConPlan + tipos en useFinanceLoadApi</name>
  <files>el-templo-admin/src/composables/useFinanceLoadApi.ts</files>
  <read_first>
    - el-templo-admin/src/composables/useFinanceLoadApi.ts (payPlan L114-129, miscCharge L135-150, CoachPayPlanInput L28-35, return L179-187)
    - el-templo-api/src/modules/finance/coach-load-routes.ts (contrato real del body/response de /alta de 148-02)
  </read_first>
  <action>
    Definir `CoachAltaInput` (espejo de CoachPayPlanInput + campos nuevos): `userId?` XOR `{ firstName, lastName, dni }`, `branchId`, `planId`, `zero: boolean`, `paymentMethod`, `amountReceived?`, `scheduleIds?: number[]`, `notes?`, `idempotencyKey`. Definir `CoachAltaResponse` (transacción + flag/campo "creó alumno"). Implementar `async function altaConPlan(body: CoachAltaInput): Promise<CoachAltaResponse>` idéntico en forma a payPlan: setear loading/error, `api.post('/admin/finance/coach-load/alta', body)`, `extractError(err, 'No se pudo cargar. Reintentá.')`, re-throw, finally loading=false. `catch (err: unknown)`. Exportarla en el return.

    Mantener el contrato EXACTO al del endpoint de 148-02 (leer su SUMMARY/source para nombres de campos).

  </action>
  <acceptance_criteria>
    - `grep -c "altaConPlan" el-templo-admin/src/composables/useFinanceLoadApi.ts` ≥ 2 (def + export)
    - El path posteado es `/admin/finance/coach-load/alta`
    - Tipos sin `any` (CLAUDE.md); catch (err: unknown)
    - `cd el-templo-admin && pnpm lint` sin errores sobre el archivo
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-admin && pnpm exec eslint -c ./eslint.config.js src/composables/useFinanceLoadApi.ts 2>&1 | tail -5; grep -c "altaConPlan" src/composables/useFinanceLoadApi.ts</automated>
  </verify>
  <done>altaConPlan + CoachAltaInput/CoachAltaResponse, sin any, posteando a /coach-load/alta, exportado. lint verde.</done>
</task>

<task type="auto">
  <name>Task 2: Modo 'alta' — tab, sucursal, socio typeahead, mini-form nuevo alumno + dedup DNI</name>
  <files>el-templo-admin/src/pages/CargarPagoPage.vue</files>
  <read_first>
    - el-templo-admin/src/pages/CargarPagoPage.vue (q-btn-toggle L7-19, type Mode L240, typeahead L25-56 + #no-option L39-45, deuda banner L60-71, resetChargeFields L370, onModeChange L387)
    - .planning/phases/148-pos-profe-alta-de-alumno-plan-en-el-cobro/148-UI-SPEC.md (Layout, States, Copywriting, Color)
  </read_first>
  <action>
    Agregar `'alta'` a `type Mode` y la 3ª opción del q-btn-toggle `{ label:'Alta + plan', value:'alta' }`. Bloque `<template v-if="mode === 'alta'">` con:

    - Chip/`q-select` Sucursal (dense, outlined) al tope, label "Sede", default = sede del profe (branchId del user), editable a las sedes accesibles; sólo en modo alta. Dirige el `:branch-id` del picker y el filtro de planes.
    - Socio typeahead reusado; en `#no-option` agregar acción "+ Nuevo alumno" que revela el mini-form inline (Nombre, Apellido, DNI con inputmode="numeric"; sin email/teléfono — locked). Al revelar, limpiar el valor del typeahead.
    - DNI on-blur (≥7 dígitos): llamar `useMembersApi` check-duplicates?dni=. Match → banner gold (bg-warning text-dark, patrón L60-71): "Ya existe un alumno con ese DNI: {nombre}. Se cargará sobre ese alumno." + acción "Usar ese alumno" que colapsa el mini-form y selecciona el member existente. Sin match → conservar los datos para crear en Confirmar.

    Extender `resetChargeFields`/`onModeChange` para limpiar sucursal/mini-form/dedup al salir; al entrar a alta, conservar el socio si ya estaba elegido (UI-SPEC mode-switch). Copy EXACTA del Copywriting Contract.

  </action>
  <acceptance_criteria>
    - El q-btn-toggle tiene 3 opciones incluyendo value 'alta'; `type Mode` incluye 'alta'
    - "+ Nuevo alumno" revela un mini-form de exactamente Nombre/Apellido/DNI (sin email/phone)
    - DNI on-blur llama check-duplicates y, ante match, muestra banner gold + "Usar ese alumno"
    - `grep -c "check-duplicates\|checkDuplicates\|Nuevo alumno" el-templo-admin/src/pages/CargarPagoPage.vue` ≥ 1
    - `cd el-templo-admin && pnpm lint` sin errores sobre el archivo
    - HUMAN-CHECK (UAT cierre de fase): el modo Alta muestra sede/typeahead/mini-form y el dedup ofrece reusar el alumno
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-admin && pnpm exec eslint -c ./eslint.config.js src/pages/CargarPagoPage.vue 2>&1 | tail -5; grep -c "'alta'" src/pages/CargarPagoPage.vue</automated>
    <human-check>En el admin, modo "Alta + plan": el chip Sede aparece con la sede del profe; "+ Nuevo alumno" abre el mini-form; al tipear un DNI existente sale el banner gold con "Usar ese alumno".</human-check>
  </verify>
  <done>Modo alta con tab, sucursal default editable, typeahead + mini-form nuevo alumno y dedup DNI on-blur. lint verde.</done>
</task>

<task type="auto">
  <name>Task 3: Plan grid + Zero + precio por medio de pago + turnos fixed + Confirmar</name>
  <files>el-templo-admin/src/pages/CargarPagoPage.vue</files>
  <read_first>
    - el-templo-admin/src/components/AssignPlanDialog.vue (plan grid L54-83, plansByTier L1192-1204, tierColor/tierLabel L1414-1426, getBasePrice L1428-1438, isPartialCharge L1276-1279, banner parcial L783-794, getPlans L1447, FixedSchedulePicker usage L376-387)
    - el-templo-admin/src/components/scheduling/FixedSchedulePicker.vue (props L172-199)
    - el-templo-admin/src/pages/CargarPagoPage.vue (payment buttons L141-161, Monto L97-103, canConfirm L302-310, currentIdempotencyKey + regen L283/L427-430/L457, Mis cargas L180-202, Confirmar L208-221)
  </read_first>
  <action>
    En el bloque alta, debajo del alumno:

    - Plan grid: cargar `useSubscriptionsApi().getPlans(true, { branchId: sucursalId })`; render por tier reusando plansByTier + tierColor/tierLabel; cada fila ≥56px clickable v-ripple; seleccionada highlight $primary. Estado vacío: "No hay planes activos para esta sede." Skeleton mientras carga.
    - `q-toggle` "Precio Zero" debajo de la grilla (regular↔zero).
    - Turnos: si `plan.bookingMode==='fixed'`, sección "Turnos fijos" + `<FixedSchedulePicker v-model="scheduleIds" :branch-id="sucursalId" :required-count="plan.classesPerWeek" :allow-partial="false" :multi-branch="plan.multiBranch">`. Si flexible: caption "Este plan reserva semana a semana — no se eligen turnos ahora." (sin picker).
    - Medio de pago (botones reusados) + Monto autocalc: card → priceCreditCard; cash/transfer → priceZero si Zero else priceRegular (mapping de getBasePrice). Monto editable; si < precio → banner gold "El alumno quedará deudor por {monto}." (isPartialCharge). Caption "Queda pendiente de validación."
    - Confirmar (sticky): extender `canConfirm` — (member elegido OR Nombre+DNI presentes) AND sucursal AND plan AND método AND monto>0 AND (fixed ⇒ scheduleIds.length===classesPerWeek). On tap: generar/reusar currentIdempotencyKey (mismo ciclo existente), llamar `financeApi.altaConPlan({...})`, notify positive "Alumno y plan cargados — pendiente de validación", reset, prepend del ticket a "Mis cargas" con badge Pendiente + chip "Nuevo" si creó alumno. Error → notify negative "No se pudo cargar. Reintentá." reusando la MISMA key (idempotente). Dedupe del prepend por id ante replay 200.

  </action>
  <acceptance_criteria>
    - Plan grid agrupado por tier con selección $primary; estado vacío y skeleton presentes
    - Toggle "Precio Zero" presente; el monto se autocalcula: card→priceCreditCard, else zero?priceZero:priceRegular; monto editable; banner deuda cuando monto<precio
    - FixedSchedulePicker se renderiza SÓLO para bookingMode==='fixed' con required-count=classesPerWeek; flexible muestra caption sin picker
    - canConfirm exige (member OR nombre+DNI) AND sede AND plan AND método AND monto>0 AND (fixed⇒count exacto)
    - Confirmar llama altaConPlan con el idempotencyKey del ciclo existente; ticket con badge Pendiente + chip Nuevo
    - `grep -c "FixedSchedulePicker\|altaConPlan\|Precio Zero" el-templo-admin/src/pages/CargarPagoPage.vue` ≥ 2
    - `cd el-templo-admin && pnpm lint` sin errores
    - HUMAN-CHECK (UAT): alta de plan fixed pide turnos; tarjeta sube el monto; parcial muestra deuda; el ticket sale con chip Nuevo
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-admin && pnpm exec eslint -c ./eslint.config.js src/pages/CargarPagoPage.vue 2>&1 | tail -5; grep -c "FixedSchedulePicker\|altaConPlan" src/pages/CargarPagoPage.vue</automated>
    <human-check>Modo Alta: elegir plan fixed pide turnos (badge n/m); tarjeta recalcula el monto a priceCreditCard; monto parcial muestra banner deuda; Confirmar crea la carga, notify pendiente, ticket con chip "Nuevo".</human-check>
  </verify>
  <done>Plan grid + Zero + precio por medio + turnos fixed condicionales + Confirmar idempotente con ticket Pendiente/Nuevo. lint verde.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                 | Description                                                                               |
| ------------------------ | ----------------------------------------------------------------------------------------- |
| cliente(admin SPA) → API | Todo el body se re-valida server-side (148-02); el frontend es conveniencia, no autoridad |

## STRIDE Threat Register

| Threat ID | Category        | Component                                | Disposition | Mitigation Plan                                                                                                                  |
| --------- | --------------- | ---------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------- |
| T-148-17  | Tampering       | dedup DNI sólo client-side               | mitigate    | el server es la autoridad en Confirmar (checkDuplicates en 148-02); el banner es UX, no la garantía                              |
| T-148-18  | DoS / dup       | doble-tap de Confirmar                   | mitigate    | :disable durante submit + currentIdempotencyKey reusada en retry (endpoint idempotente)                                          |
| T-148-19  | Info disclosure | precio/validationStatus desde el cliente | accept      | el cliente sugiere monto/priceType; el server deriva el status y persiste el cobro 'pendiente' — el cliente no decide validación |
| T-148-SC  | Tampering       | npm/pip/cargo installs                   | accept      | sin dependencias nuevas (UI-SPEC: sólo Quasar + componentes in-repo)                                                             |

</threat_model>

<verification>
- `cd el-templo-admin && pnpm lint` verde sobre composable + página.
- UAT visual al cierre de fase (HUMAN-UAT): modo Alta completo según UI-SPEC.
</verification>

<success_criteria>

- Modo "Alta + plan" funcional según UI-SPEC: sucursal default editable, alumno nuevo/existente con dedup, plan+Zero+precio por medio (parcial→deuda), turnos sólo fixed, Confirmar idempotente con ticket Pendiente/Nuevo.
- lint verde, sin any.
  </success_criteria>

<output>
Create `.planning/phases/148-pos-profe-alta-de-alumno-plan-en-el-cobro/148-05-SUMMARY.md` when done
</output>
