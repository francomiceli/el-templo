---
phase: 148-pos-profe-alta-de-alumno-plan-en-el-cobro
plan: 04
type: execute
wave: 3
depends_on: ["148-02", "148-03"]
files_modified:
  - el-templo-api/test/coach-load-alta.test.ts
autonomous: true
requirements: [ALTA-08]
must_haves:
  truths:
    - "Existen tests de integración que prueban el alta crear-nuevo, dedup, parcial→deuda, fixed con scheduleIds, void→cascade, retry-tras-fallo-parcial e idempotencia"
    - "Los tests corren contra MySQL real (eltemplo_test) en CI"
  artifacts:
    - path: "el-templo-api/test/coach-load-alta.test.ts"
      provides: "Suite de integración del endpoint /alta + cascade"
      contains: "coach-load/alta"
  key_links:
    - from: "test"
      to: "POST /admin/finance/coach-load/alta + /transactions/:id/void"
      via: "request helpers de test/helpers.ts"
      pattern: "coach-load/alta"
---

<objective>
ALTA-08: la suite de integración del endpoint `/alta` y el cascade en void. Ruta nueva ⇒ tests de integración obligatorios (CLAUDE.md), contra MySQL real `eltemplo_test`, usando los helpers de `test/helpers.ts`.

Casos requeridos (CONTEXT L182 / PATTERNS L242):

1. crear-nuevo: alumno {firstName,lastName,dni} sin match ⇒ 1 member (email null, prueba) + 1 sub + 1 charge 'pendiente'; transacción con createdMemberId.
2. dedup-contra-existente: {dni} que matchea un member existente ⇒ NO crea member; carga contra el existente; createdMemberId null.
3. parcial→deuda: amountReceived < precio ⇒ deja deuda (balance positivo).
4. fixed-con-scheduleIds: plan bookingMode='fixed' con scheduleIds.length===classesPerWeek ⇒ asigna subscription_schedules + genera bookings; count erróneo ⇒ 400.
5. void→cascade: anular una carga de alumno-nuevo ⇒ member 'inactivo' + sub cancelada; anular una de preexistente ⇒ status del alumno intacto.
6. idempotencia: dos POST con el mismo idempotencyKey ⇒ 1 member + 1 charge (el 2º devuelve 200 con el existente).
7. (I-1) retry-tras-fallo-parcial: alumno creado, reintento con la MISMA idempotency_key ⇒ el `createdMemberId` queda SETEADO (no NULL) en el alumno realmente creado por el flujo; el void posterior lo desactiva.

Purpose: blindar el corazón de la fase; estos tests son la red de seguridad del tren a prod.
Output: archivo de test que pasa en CI.

> NOTA (MEMORY): el suite NO se corre local — corre en CI al pushear a staging. La verificación local es typecheck. El comando de test queda documentado para CI.
> </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/148-pos-profe-alta-de-alumno-plan-en-el-cobro/148-CONTEXT.md
@.planning/phases/148-pos-profe-alta-de-alumno-plan-en-el-cobro/148-02-SUMMARY.md
@.planning/phases/148-pos-profe-alta-de-alumno-plan-en-el-cobro/148-03-SUMMARY.md

<interfaces>
<!-- Leer test/helpers.ts para auth/request utilities (login coach/admin, post/get autenticados,
     seed de branches/plans/members, reset de eltemplo_test entre tests). Buscar un test
     existente de coach-load en test/ como plantilla de setup (login coach, headers, cleanup). -->
Endpoint bajo prueba: POST /admin/finance/coach-load/alta  (rol coach).
Void bajo prueba: POST /admin/finance/transactions/:id/void  (rol admin/gestion — coach excluido).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tests crear-nuevo, dedup, parcial→deuda, fixed</name>
  <files>el-templo-api/test/coach-load-alta.test.ts</files>
  <read_first>
    - el-templo-api/test/helpers.ts (auth/request utilities, seed, cleanup)
    - Un test existente de coach-load en el-templo-api/test/ (plantilla: login coach, headers, seed de plan/branch, asserts de financial_transactions)
    - el-templo-api/src/modules/finance/coach-load-routes.ts (contrato del body /alta de 148-02)
  </read_first>
  <action>
    Crear `coach-load-alta.test.ts` con el setup de los tests de coach-load existentes (login como coach, seed de branch + plan fixed y plan flexible + un member preexistente con DNI conocido). Tests:

    - crear-nuevo: POST /alta con {firstName,lastName,dni nuevo, branchId, planId(flexible), paymentMethod:'cash', idempotencyKey}. Assert: status 200/201, se creó 1 user (email null, status 'prueba'), 1 subscription activa, 1 financial_transaction validation_status='pendiente' con createdMemberId = nuevo user.
    - dedup: POST /alta con {dni del member preexistente,...}. Assert: NO se creó user nuevo (count users sin cambios), la carga referencia al member existente, createdMemberId null.
    - parcial→deuda: POST /alta con amountReceived < precio del plan. Assert: queda deuda/balance positivo por la diferencia.
    - fixed-con-scheduleIds: POST /alta con plan fixed + scheduleIds.length===classesPerWeek. Assert: subscription_schedules creados + bookings generados. Caso negativo: scheduleIds con count erróneo ⇒ 400.

  </action>
  <acceptance_criteria>
    - El archivo existe y cubre los 4 casos con asserts sobre filas reales (users, subscriptions, financial_transactions, subscription_schedules/bookings)
    - Los asserts verifican validation_status='pendiente' y createdMemberId según corresponda
    - `cd el-templo-api && pnpm exec tsc --noEmit` sin errores en el test
    - En CI: `pnpm test coach-load-alta` pasa
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit 2>&1 | tail -5; test -f test/coach-load-alta.test.ts && echo OK</automated>
  </verify>
  <done>Tests crear-nuevo, dedup, parcial→deuda y fixed (happy + 400) escritos con asserts sobre DB real. tsc verde.</done>
</task>

<task type="auto">
  <name>Task 2: Tests void→cascade, retry-tras-fallo-parcial (I-1) e idempotencia doble-submit</name>
  <files>el-templo-api/test/coach-load-alta.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/finance/transaction-service.ts (cascade de 148-03)
    - el-templo-api/src/modules/finance/routes.ts (void handler, rol admin/gestion)
  </read_first>
  <action>
    Agregar al mismo archivo:

    - void→cascade: crear vía /alta un alumno nuevo (como coach), luego POST /transactions/:id/void como admin/gestion. Assert: la transacción queda anulada, la subscription cancelada, y el user creado queda status='inactivo' (NO borrado) + fila en userStatusHistory. Caso preexistente: alta vía dedup contra un member existente, void ⇒ el status del member preexistente NO cambia.
    - retry-tras-fallo-parcial (I-1): simular el reintento de un alta que ya creó el alumno — un segundo POST /alta con el MISMO idempotencyKey y el mismo body de alumno-nuevo. Assert: (a) el `createdMemberId` de la transacción del charge queda SETEADO (no NULL) y apunta al alumno realmente creado por el flujo (el id que devolvió el primer alta / el resuelto por dedup-de-dni en el replay), garantizando que el id NO se perdió por una persistencia desacoplada (W-1); (b) un void posterior de esa carga desactiva a ese alumno (status='inactivo'). Esto prueba que createdMemberId viajó atómicamente con el charge y que el cascade lo encuentra incluso tras un replay.
    - idempotencia: dos POST /alta consecutivos con el MISMO idempotencyKey (mismo body). Assert: solo 1 user creado, solo 1 financial_transaction; el segundo response es 200 y referencia el mismo charge.

  </action>
  <acceptance_criteria>
    - El test de void verifica: tx anulada + sub cancelada + user 'inactivo' (no borrado) + history; y que un preexistente NO cambia de status
    - El test retry-tras-fallo-parcial (I-1) verifica createdMemberId no-NULL en la transacción tras el replay con misma key, y que el void posterior desactiva a ese alumno
    - El test de idempotencia verifica exactamente 1 user + 1 transaction tras dos POST con misma key
    - `cd el-templo-api && pnpm exec tsc --noEmit` sin errores
    - En CI: `pnpm test coach-load-alta` pasa (los 7 casos)
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit 2>&1 | tail -5; grep -c "void\|idempotenc\|createdMemberId" test/coach-load-alta.test.ts</automated>
  </verify>
  <done>Tests void→cascade (nuevo inactivo + preexistente intacto), retry-tras-fallo-parcial (createdMemberId no-NULL + void desactiva) e idempotencia (1 member + 1 charge) escritos. tsc verde; suite pasa en CI.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary      | Description                                                                                                            |
| ------------- | ---------------------------------------------------------------------------------------------------------------------- |
| test → API/DB | Los tests ejercen los boundaries reales (auth coach/admin, branch access, idempotencia, atomicidad de createdMemberId) |

## STRIDE Threat Register

| Threat ID | Category    | Component                               | Disposition | Mitigation Plan                                                                                                  |
| --------- | ----------- | --------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------- |
| T-148-15  | Repudiation | regresión silenciosa en el alta/cascade | mitigate    | suite de integración contra MySQL real cubre los 7 escenarios críticos antes del tren a prod                     |
| T-148-16  | Tampering   | idempotencia rota deja duplicados       | mitigate    | test explícito doble-submit (1 member + 1 charge)                                                                |
| T-148-24  | Integrity   | createdMemberId perdido en replay (W-1) | mitigate    | test retry-tras-fallo-parcial verifica createdMemberId no-NULL tras replay y que el cascade de void lo desactiva |
| T-148-SC  | Tampering   | npm/pip/cargo installs                  | accept      | sin dependencias nuevas en esta fase                                                                             |

</threat_model>

<verification>
- `cd el-templo-api && pnpm exec tsc --noEmit` verde sobre el test.
- En CI: `pnpm test coach-load-alta` pasa los 7 casos contra eltemplo_test.
</verification>

<success_criteria>

- 7 casos cubiertos con asserts sobre filas reales (incluye retry-tras-fallo-parcial I-1); suite verde en CI.
  </success_criteria>

<output>
Create `.planning/phases/148-pos-profe-alta-de-alumno-plan-en-el-cobro/148-04-SUMMARY.md` when done
</output>
