---
phase: 148-pos-profe-alta-de-alumno-plan-en-el-cobro
plan: 02
type: execute
wave: 2
depends_on: ["148-01"]
files_modified:
  - el-templo-api/src/modules/finance/coach-load-routes.ts
autonomous: true
requirements: [ALTA-01, ALTA-02, ALTA-03, ALTA-04, ALTA-05, ALTA-07]
must_haves:
  truths:
    - "Un profe puede crear un alumno nuevo (o reusar uno por DNI) y asignarle un plan + turnos + cobro en una sola llamada atómica"
    - "El cobro nace 'pendiente' (derivado del rol coach, nunca del body) y registra createdMemberId si creó alumno"
    - "El profe solo puede cargar para sedes a las que tiene acceso (requireBranchAccess sobre body.branchId)"
    - "Un doble-submit con el mismo idempotencyKey no crea segundo alumno ni segundo cobro"
  artifacts:
    - path: "el-templo-api/src/modules/finance/coach-load-routes.ts"
      provides: "Endpoint POST /alta con schema, preHandler requireBranchAccess y orquestación atómica idempotente"
      contains: "/alta"
  key_links:
    - from: "POST /alta"
      to: "memberService.checkDuplicates + memberService.createMinimalMember"
      via: "dedup por DNI antes de crear"
      pattern: "checkDuplicates"
    - from: "POST /alta"
      to: "subscriptionService.assignPlan"
      via: "recorderRole + idempotencyKey + recorderBranchId + scheduleIds + priceTypeApplied"
      pattern: "assignPlan"
    - from: "POST /alta"
      to: "financial_transactions.createdMemberId"
      via: "registrar el alumno creado en la transacción"
      pattern: "createdMemberId"
---

<objective>
El corazón de la Fase 148: el endpoint `POST /admin/finance/coach-load/alta` en `coach-load-routes.ts`. Orquesta, atómico e idempotente:

1. Resuelve alumno: si viene `userId` lo usa; si vienen `{firstName, lastName, dni}` → dedup por DNI (`memberService.checkDuplicates({dni})`) → match no-borrado ⇒ usa el existente; si no ⇒ `createMinimalMember` (y marca que esta carga creó al alumno).
2. `assignPlan(memberId, input, adminId)` con `priceTypeApplied` (regular/zero/credit_card según toggle Zero + medio de pago), `scheduleIds?` (planes fixed), `amountReceived?` (parcial → deuda), `recorderRole='coach'` (⇒ charge nace 'pendiente'), `idempotencyKey`, `recorderBranchId` (caja sugerida = sede del profe, CAJA-01).
3. Persiste `createdMemberId` en la transacción resultante cuando creó alumno nuevo, para el cascade en void (148-03) y la copy de la bandeja (148-06).

Es el PRIMER endpoint del plugin coach-load que acepta `branchId` en el body → suma un preHandler `requireBranchAccess({ from: 'body.branchId' })`.

Purpose: reemplaza el Google Form→Excel→admin con una carga linkeada al catálogo real, validada después por gestión.
Output: endpoint nuevo, tsc verde, listo para tests (148-04) y frontend (148-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/148-pos-profe-alta-de-alumno-plan-en-el-cobro/148-CONTEXT.md
@.planning/phases/148-pos-profe-alta-de-alumno-plan-en-el-cobro/148-PATTERNS.md
@.planning/phases/148-pos-profe-alta-de-alumno-plan-en-el-cobro/148-01-SUMMARY.md
@BRIEF-POS-PROFE-ALTA-ALUMNO.md

<interfaces>
<!-- coach-load-routes.ts — análogos en el MISMO archivo. Copiar agresivamente. -->

Module guard (L144-159): el plugin YA autentica + gatea FINANCE_LOAD_ROLES (coach ∈) +
attachCountryScope vía onRequest hook. El endpoint nuevo lo HEREDA — NO agregar otro guard.

Servicios instanciados en el closure del plugin (L124-142): transactionService (L127),
subscriptionService (L135). memberService NO está wireado acá → instanciar `new MemberService(...)`
en el closure espejando members/routes.ts L82-91.

Helpers existentes (L194-233):

- resolveRecorderBranchId(recorderUserId) → Promise<number> (sede del profe desde su userId)
- resolveSuggestedCaja(...) (caja sugerida CAJA-01)

Schema inline pattern (coachPayPlanSchema L76, coachMiscLoadSchema L90): type:"object",
additionalProperties:false, required mínimos, paymentMethod enum PAYMENT_METHOD_ENUM,
idempotencyKey {minLength:1,maxLength:64}. NUNCA cashRegisterId/validationStatus en el body.

Status server-derived (L301-306, repetido L414-419): copiar exacto — pero acá se pasa
recorderRole='coach' a assignPlan y assignPlan lo deriva; el endpoint NO setea validationStatus.

Idempotency no-op (L373-389 pay-plan / L453-462 misc): catch isDuplicateKeyError →
transactionService.findByIdempotencyKey → 200 con el charge existente. La orquestación
COMPLETA debe rollback en bloque ante el dup (un replay nunca deja un member a medias).

handleServiceError(err, reply, request.log, "...") para errores de servicio.

assignPlan(userId, input: AssignPlanInput, adminId) — AssignPlanInput ya extendido en 148-01
con recorderRole/idempotencyKey/recorderBranchId; campos relevantes: planId, branchId,
startDate, priceTypeApplied (PriceType: 'regular'|'zero'|'credit_card'), paymentMethod,
scheduleIds?, amountReceived?. Valida internamente scheduleIds.length === plan.classesPerWeek
para bookingMode==='fixed' (L1107-1142) y corre generateFixedBookings en la misma tx.

checkDuplicates({dni}) → { matches: [{ id, firstName, lastName, deletedAt, matchedField }] }
(filtra isNull(deletedAt)). Un match con matchedField==='dni' ⇒ usar match.id.

requireBranchAccess({ from: "body.branchId" }) — preHandler de shared/branch-access.ts,
usado en members/routes.ts L489/L600/L642.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema /alta + wire MemberService + preHandler requireBranchAccess</name>
  <files>el-templo-api/src/modules/finance/coach-load-routes.ts</files>
  <read_first>
    - el-templo-api/src/modules/finance/coach-load-routes.ts (coachPayPlanSchema L76, coachMiscLoadSchema L90, closure de servicios L124-142, module guard L144-159)
    - el-templo-api/src/modules/members/routes.ts (instanciación de MemberService L82-91; requireBranchAccess L489)
    - el-templo-api/src/modules/shared/branch-access.ts (firma de requireBranchAccess)
  </read_first>
  <action>
    Definir `coachAltaSchema` (inline, como coachPayPlanSchema) con `additionalProperties:false`. Body: rama alumno-o-nuevo — `userId?` (integer ≥1) XOR `{ firstName, lastName, dni }` (strings; dni saneado, inputmode numérico del lado UI); `branchId` (integer ≥1, la sede elegida — NUEVO en este plugin); `planId` (integer ≥1); `priceType`/`zero` (boolean toggle) — el endpoint mapea a `priceTypeApplied`; `paymentMethod` (enum PAYMENT_METHOD_ENUM); `amountReceived?` (integer ≥0, en cents); `scheduleIds?` (array de integers); `notes?` (string opcional, ≤ límite); `idempotencyKey` (string minLength:1 maxLength:64, required). NUNCA `cashRegisterId` ni `validationStatus`.

    Instanciar `new MemberService(...)` en el closure del plugin (espejar members/routes.ts L82-91) junto a transactionService/subscriptionService.

    Registrar `fastify.post<{ Body: CoachAltaBody }>("/alta", { schema: coachAltaSchema, preHandler: requireBranchAccess({ from: "body.branchId" }) }, handler)`. El handler se implementa en Task 2 (stub que tipa por ahora). Definir el tipo `CoachAltaBody` acorde al schema.

  </action>
  <acceptance_criteria>
    - `grep -c '"/alta"' el-templo-api/src/modules/finance/coach-load-routes.ts` ≥ 1
    - El registro de `/alta` incluye `preHandler` con `requireBranchAccess({ from: "body.branchId" })`
    - `new MemberService` aparece en el closure del plugin: `grep -c "new MemberService" el-templo-api/src/modules/finance/coach-load-routes.ts` ≥ 1
    - El schema tiene `additionalProperties: false` y NO contiene `cashRegisterId` ni `validationStatus`
    - `cd el-templo-api && pnpm exec tsc --noEmit` sin errores
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit 2>&1 | tail -5; grep -c '"/alta"' src/modules/finance/coach-load-routes.ts</automated>
  </verify>
  <done>Schema coachAltaSchema + tipo CoachAltaBody + MemberService wireado + ruta /alta registrada con preHandler requireBranchAccess. tsc verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Handler /alta — orquestación atómica e idempotente</name>
  <files>el-templo-api/src/modules/finance/coach-load-routes.ts</files>
  <read_first>
    - el-templo-api/src/modules/finance/coach-load-routes.ts (handler pay-plan L247-391 completo: status server-derived L301-306, idempotency no-op L373-389, resolveSuggestedCaja L296/L409, resolveRecorderBranchId L351)
    - el-templo-api/src/modules/subscriptions/service.ts (assignPlan L892; validación scheduleIds fixed L1107-1142)
    - el-templo-api/src/modules/members/service.ts (createMinimalMember de 148-01; checkDuplicates L1360)
  </read_first>
  <behavior>
    - Crear-nuevo: body con {firstName,lastName,dni} sin match ⇒ crea 1 member (email null, status prueba) + 1 subscription + 1 charge 'pendiente'; la transacción tiene createdMemberId = id del nuevo member.
    - Dedup: body con {dni} que matchea un member existente no-borrado ⇒ NO crea member; carga contra el existente; createdMemberId queda NULL.
    - userId directo: usa ese member; createdMemberId NULL.
    - Precio: paymentMethod 'card'/'credit_card' ⇒ priceTypeApplied='credit_card'; si zero=true ⇒ 'zero'; else 'regular'. amountReceived < precio ⇒ deja deuda (assignPlan ya lo soporta).
    - Fixed: scheduleIds requeridos = plan.classesPerWeek (assignPlan valida; un count erróneo ⇒ 400). Flexible: scheduleIds ausente/ignorado.
    - Idempotencia: dos requests con el mismo idempotencyKey ⇒ 1 member + 1 charge; el segundo devuelve 200 con el charge existente.
    - Pendiente: el charge nace validation_status='pendiente' (derivado de recorderRole='coach', NUNCA del body).
  </behavior>
  <action>
    Implementar el handler. Orden de orquestación (CONTEXT L78-82 + PATTERNS L109-112):

    1. Resolver alumno: si `body.userId` → usarlo (createdMemberId = null). Si no → `memberService.checkDuplicates({ dni: body.dni })`; si hay match `matchedField==='dni'` no-borrado → usar `match.id` (createdMemberId = null); si no → `memberService.createMinimalMember({ firstName, lastName, dni, branchId: body.branchId }, request.user.userId)` y recordar `createdMemberId = nuevoId`. `createdBy`/createdMinimal proviene SIEMPRE de `request.user.userId` (no del body — anti-spoof).

    2. Mapear precio: `priceTypeApplied = body.paymentMethod === 'card' ? 'credit_card' : (body.zero ? 'zero' : 'regular')`.

    3. `recorderBranchId = await resolveRecorderBranchId(request.user.userId)` (sede del profe → caja sugerida CAJA-01; NO la sede elegida del socio).

    4. `subscriptionService.assignPlan(memberId, { planId: body.planId, branchId: body.branchId, startDate: <hoy>, priceTypeApplied, paymentMethod: body.paymentMethod, scheduleIds: body.scheduleIds, amountReceived: body.amountReceived, notes: body.notes, recorderRole: request.user.role, idempotencyKey: body.idempotencyKey, recorderBranchId }, request.user.userId)`. assignPlan crea sub + charge 'pendiente' (coach) idempotente + bookings fixed dentro de su tx.

    5. Persistir `createdMemberId` en la transacción del charge resultante cuando se creó alumno nuevo (update por el id del charge devuelto, o threadear createdMemberId al sink del charge — elegir lo que NO requiera segunda tx desacoplada; preferible setear createdMemberId en el mismo flujo del charge).

    6. Idempotency no-op: envolver en catch `isDuplicateKeyError` → `transactionService.findByIdempotencyKey(body.idempotencyKey)` → si existe, `reply.code(200).send({ transaction: existing })`. La orquestación completa debe ser un replay seguro: un dup NO deja member huérfano (createMinimalMember también es idempotente por dni UNIQUE, 148-01). Errores restantes → `handleServiceError(err, reply, request.log, "coach alta-con-plan")`.

    Devolver el charge/transacción creada (+ flag/campo indicando si creó alumno, para el ticket "Nuevo" del frontend).

  </action>
  <acceptance_criteria>
    - El handler llama `checkDuplicates` antes de `createMinimalMember` (dedup por DNI): `grep -c "checkDuplicates" el-templo-api/src/modules/finance/coach-load-routes.ts` ≥ 1
    - `recorderRole: request.user.role` se pasa a assignPlan (charge server-derived 'pendiente'); el body nunca setea validationStatus
    - createdMemberId se persiste solo cuando se creó alumno nuevo (NULL en dedup/userId)
    - Catch isDuplicateKeyError → findByIdempotencyKey → 200 con el existente
    - `cd el-templo-api && pnpm exec tsc --noEmit` sin errores
    - Los escenarios de <behavior> quedan cubiertos por los tests de 148-04 (corren en CI)
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit 2>&1 | tail -5; grep -c "checkDuplicates\|createMinimalMember\|assignPlan" src/modules/finance/coach-load-routes.ts</automated>
  </verify>
  <done>POST /alta resuelve/crea alumno (dedup DNI), asigna plan + turnos + cobro 'pendiente', persiste createdMemberId, idempotente y atómico. tsc verde.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary             | Description                                                                     |
| -------------------- | ------------------------------------------------------------------------------- |
| cliente(profe) → API | Body no confiable: branchId, datos del alumno nuevo, planId, monto, scheduleIds |
| API → servicios/DB   | Orquestación member+sub+charge debe ser atómica e idempotente                   |

## STRIDE Threat Register

| Threat ID | Category         | Component                                                                       | Disposition | Mitigation Plan                                                                                                                                               |
| --------- | ---------------- | ------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-148-05  | Elevation / IDOR | `body.branchId` (sede elegida)                                                  | mitigate    | preHandler `requireBranchAccess({ from: "body.branchId" })` — primer endpoint del plugin que acepta branchId; un profe no puede cargar en una sede sin acceso |
| T-148-06  | Tampering        | rol/permiso de carga                                                            | mitigate    | módulo guard heredado (FINANCE_LOAD_ROLES, coach ∈) — el coach NO puede validar/anular (esos son FINANCE_VOID_ROLES)                                          |
| T-148-07  | Tampering        | validation_status del cobro                                                     | mitigate    | derivado de `request.user.role` (coach⇒'pendiente') en assignPlan/recordAssignmentCharge; jamás del body                                                      |
| T-148-08  | DoS / dup        | doble-submit (doble-tap o retry de red)                                         | mitigate    | idempotencyKey UNIQUE + catch no-op → findByIdempotencyKey → 200; createMinimalMember idempotente por dni UNIQUE; replay no crea 2º member/charge             |
| T-148-09  | Tampering        | spoof de createdBy del alumno                                                   | mitigate    | createMinimalMember recibe createdBy = `request.user.userId` (JWT), no del body (D-31)                                                                        |
| T-148-10  | Integrity        | input inválido (monto negativo, scheduleIds de otra sede, additionalProperties) | mitigate    | schema fastify additionalProperties:false + amountReceived integer ≥0; assignPlan valida scheduleIds vs plan.classesPerWeek y país/sede del plan              |
| T-148-SC  | Tampering        | npm/pip/cargo installs                                                          | accept      | sin dependencias nuevas en esta fase                                                                                                                          |

</threat_model>

<verification>
- `cd el-templo-api && pnpm exec tsc --noEmit` verde.
- preHandler requireBranchAccess presente en /alta.
- Sin cashRegisterId/validationStatus en el body schema.
- Comportamientos de <behavior> verificados por 148-04 en CI.
</verification>

<success_criteria>

- POST /alta operativo: crear-nuevo / dedup / userId, plan+turnos+cobro 'pendiente', createdMemberId, idempotente, branch-gated.
- tsc verde; sin regresión en pay-plan/misc del mismo plugin.
  </success_criteria>

<output>
Create `.planning/phases/148-pos-profe-alta-de-alumno-plan-en-el-cobro/148-02-SUMMARY.md` when done
</output>
