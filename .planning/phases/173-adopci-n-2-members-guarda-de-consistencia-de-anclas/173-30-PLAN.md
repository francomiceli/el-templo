---
phase: 173-adopci-n-2-members-guarda-de-consistencia-de-anclas
plan: 30
type: execute
wave: 15
depends_on: ["173-29"]
files_modified:
  - el-templo-api/tenant-lint-allowlist.json
  - el-templo-api/src/db/tenant-tables.ts
  - el-templo-api/test/db/tenant-tables.test.ts
autonomous: true
requirements: [ADO-02, ADO-07]
must_haves:
  truths:
    - "D-01: `members` entra a `TENANT_STRICT_MODULES` con sus 8 tablas (nombres FÍSICOS de MySQL), y el sentinel hace throw sobre ellas en test y dev"
    - "D-01: las 90+ entradas de allowlist del módulo (79 por tabla + 11 por archivo) están borradas — la allowlist baja de 450 a ≤ 360"
    - "El switch va en DOS commits y en este orden: allowlist primero, entrada strict después. Al revés, el gate D-15 tumba el commit intermedio"
    - "El fail-closed está DEMOSTRADO en vivo: sonda sobre una query de UNA SOLA TABLA de un método que algún test ejercita, con la salida real del TenantSentinelError transcrita y la sonda revertida"
  artifacts:
    - path: "el-templo-api/src/db/tenant-tables.ts"
      provides: "entrada `members` en TENANT_STRICT_MODULES con las 8 tablas en orden alfabético"
      contains: "members:"
    - path: "el-templo-api/tenant-lint-allowlist.json"
      provides: "allowlist sin una sola excusa del módulo de socios"
    - path: "el-templo-api/test/db/tenant-tables.test.ts"
      provides: "gate de forma actualizado contra una SEGUNDA COPIA escrita a mano"
  key_links:
    - from: "el-templo-api/src/db/tenant-tables.ts"
      to: "el-templo-api/src/db/sentinel/"
      via: "TENANT_STRICT_MODULES.members enciende el throw para las 8 tablas"
      pattern: "members:"
---

<objective>
Paso 8 de la receta: **el interruptor**. Vaciar la allowlist del módulo y encender el throw del
sentinel para las 8 tablas, en dos commits y en el orden correcto.

Purpose: el switch va **último** porque el throw del sentinel es global: en cuanto una tabla entra a
`TENANT_STRICT_MODULES`, **toda** query sin `tenant_id` sobre ella revienta, venga de `src/` o de un
`beforeEach`. Si fuera primero, la suite quedaría roja durante los 28 planes del medio y ningún
commit intermedio sería verificable.

Y el orden **interno** es el inverso al que parece natural: con la entrada strict puesta y la
allowlist todavía llena, el gate D-15 ("ninguna tabla strict tiene entradas vivas en la allowlist")
se cae y el commit intermedio queda rojo. Al revés, los dos commits son verdes por separado.

Output: `members` en strict, la allowlist en ≤ 360, el gate de forma actualizado y el fail-closed
demostrado con la salida real transcrita.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-CONTEXT.md
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-PATTERNS.md
@.docs/saas-multitenancy/07-receta-adopcion.md

**Ruta de código:** worktree `/home/franco/projects/et-173`.

**Este plan es el ÚNICO dueño de `tenant-lint-allowlist.json`.** Los planes 173-04..173-20 corrieron
contra allowlists de trabajo en `/tmp/allowlist-173-<NN>.json`; el archivo real quedó "rojo" contra
la rama hasta ahora, y **eso era esperado**: es lo que bloqueaba el merge antes de tiempo.

**Las 8 tablas, nombres FÍSICOS de MySQL, orden alfabético:** `audit_log`, `member_logins`,
`member_notes`, `member_profiles`, `user_branches`, `user_sepa_details`, `user_status_history`,
`users`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit 1 — la allowlist se queda sin una sola excusa de members</name>
  <files>el-templo-api/tenant-lint-allowlist.json</files>
  <read_first>
    - el-templo-api/tenant-lint-allowlist.json (las 450 entradas y el header que documenta el ratchet, incluido que NO existe comando que la regenere)
    - el-templo-api/src/db/scripts/lint-tenant.ts (cómo el motor decide qué entradas están vivas y cuáles son stale)
    - .planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-20-SUMMARY.md (el balance contra las 90 entradas objetivo)
    - .planning/phases/172-adopci-n-1-piloto-finance/172-21-SUMMARY.md (la reconciliación 47 → 51 del piloto: por qué se borran MÁS de las planeadas)
  </read_first>
  <action>
    Correr `pnpm lint:tenant` contra el archivo **real** y **listar las stale reales** — las entradas
    que ya no corresponden a ninguna violación viva. Borrarlas **todas**.
    El objetivo del plan es **≥ 90** (79 por tabla + 11 por archivo bajo `src/modules/members/`), y
    **el criterio del plan es un PISO**: borrar de más siempre es legal en el ratchet. Esperar más de
    90 — el piloto planeó 47 y borró **51**, por tablas joineadas que el motor cuenta como accesos
    propios de un archivo que otro plan scopeó. Dejarlas habría dejado el lint rojo igual que
    agregarlas.
    **Nada se agrega.** El header lo dice: esta lista **solo puede achicarse**, y agregar una entrada
    deja el build rojo contra la rama base. Si el lint pide una entrada nueva, las salidas válidas son
    dos: migrar el acceso, o escribir la exención `/* tenant-safe: <motivo> */` — sumarla acá **no** es
    una salida válida.
    Registrar en el SUMMARY: el conteo antes (450), el conteo después, la diferencia, y la lista de las
    entradas borradas que **no** estaban en las 90 previstas, con el motivo de cada una.
    Commit 1: `chore(173-30): la allowlist se queda sin una sola excusa de members`.
    ⚠ **Este commit va SOLO.** La entrada strict es el commit 2. Juntarlos tumba el gate D-15.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm lint:tenant && node -e "const a=require('./tenant-lint-allowlist.json');const T=['users','member_profiles','user_status_history','user_branches','member_notes','user_sepa_details','member_logins','audit_log'];const s=a.entries.filter(e=>T.includes(e.table)||/src\/modules\/members\//.test(e.file));if(s.length!==0){console.error('quedan',s.length);process.exit(1)}console.log('total',a.entries.length)"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm lint:tenant` sale con código 0 y reporta `DISCREPANCIAS: 0` contra la allowlist **real**
    - `node -e "..."` (el del verify) imprime un total ≤ **360** y **0** entradas del módulo
    - `node -e "const a=require('./tenant-lint-allowlist.json');console.log(a.entries.filter(e=>/src\/modules\/members\//.test(e.file)).length)"` imprime `0`
    - El SUMMARY tiene el conteo antes/después, la diferencia, y las entradas borradas fuera de las 90 previstas con su motivo
    - Ninguna entrada fue **agregada** (verificado comparando contra `git show origin/staging:el-templo-api/tenant-lint-allowlist.json`: el conjunto nuevo es un subconjunto estricto del viejo)
    - El commit contiene **solo** `tenant-lint-allowlist.json`
  </acceptance_criteria>
  <done>La allowlist no tiene una sola entrada del módulo, por ninguno de los dos criterios, y el commit es verde por sí solo.</done>
</task>

<task type="auto">
  <name>Task 2: Commit 2 — members entra a TENANT_STRICT_MODULES y el gate de forma</name>
  <files>el-templo-api/src/db/tenant-tables.ts, el-templo-api/test/db/tenant-tables.test.ts</files>
  <read_first>
    - el-templo-api/src/db/tenant-tables.ts (:524-533 la entrada `finance` con sus 6 tablas: el formato exacto a copiar)
    - el-templo-api/test/db/tenant-tables.test.ts (:379 el `it` "declara exactamente los módulos ya adoptados, con sus tablas exactas (172-21: finance es el primero)": se compara contra una SEGUNDA COPIA escrita a mano, y el nombre del `it` queda stale)
    - el-templo-api/src/db/tenant-tables.ts (`isStrictTable`, `GYM_OWNED_TABLES`, `TENANT_GLOBAL_UNIQUES`)
  </read_first>
  <action>
    Agregar a `TENANT_STRICT_MODULES` la entrada `members` con las **8 tablas**, nombres **FÍSICOS** de
    MySQL, en orden alfabético: `audit_log`, `member_logins`, `member_notes`, `member_profiles`,
    `user_branches`, `user_sepa_details`, `user_status_history`, `users`.
    **Gate de forma** (`test/db/tenant-tables.test.ts:379`): el registro se compara contra una
    **SEGUNDA COPIA escrita a mano en el test** — no contra sí mismo, y no "el módulo está presente".
    Agregar `members` con sus 8 tablas a esa copia. Y **renombrar el `it`**: hoy dice "172-21: finance
    es el primero", que ya no describe el estado.
    Verificar el `it` de coherencia **D-15** ("ninguna tabla strict tiene entradas vivas en la
    allowlist"): con el commit 1 ya hecho, nace verde. Si estuviera rojo, el commit 1 no borró todo.
    Commit 2: `feat(173-30): members entra a TENANT_STRICT_MODULES`.
    Después del commit, correr la **suite completa** sin sonda —ahora el strict es real— con
    `--no-file-parallelism`. Es la primera corrida en la que el throw viene del código commiteado y no
    de una sonda.
    ⚠ Correr `prettier --write` antes de la corrida larga.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec prettier --write "src/**/*.ts" "test/**/*.ts" && pnpm exec tsc -p tsconfig.test-check.json --noEmit && pnpm exec vitest run --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const t=require('./dist/db/tenant-tables.js')||{}" ` no aplica; en su lugar: `grep -A10 "members:" el-templo-api/src/db/tenant-tables.ts` muestra las 8 tablas en orden alfabético con sus nombres físicos
    - `test/db/tenant-tables.test.ts` tiene `members` con sus 8 tablas en la **segunda copia escrita a mano**, y el nombre del `it` ya no dice "finance es el primero"
    - El `it` de coherencia D-15 sale verde sin cambios (la allowlist ya está limpia)
    - `pnpm exec vitest run --no-file-parallelism test/db/tenant-tables.test.ts` sale con código 0
    - `pnpm exec vitest run --no-file-parallelism` (suite COMPLETA, sin sonda) sale con código 0
    - `pnpm exec tsc -p tsconfig.test-check.json --noEmit` sale con código 0
    - El commit 2 contiene **solo** `src/db/tenant-tables.ts` y `test/db/tenant-tables.test.ts`
  </acceptance_criteria>
  <done>El sentinel hace throw sobre las 8 tablas del módulo y la suite entera pasa con el enforcement real encendido.</done>
</task>

<task type="auto">
  <name>Task 3: Demo del fail-closed en vivo, con la salida real transcrita</name>
  <files>ninguno versionado (sonda revertida)</files>
  <read_first>
    - el-templo-api/src/db/sentinel/analyze.ts (por qué el analizador es una regex por statement y marca `ok` toda la query con un solo `tenant_id` en la zona de predicado — trampa (h))
    - .docs/saas-multitenancy/07-receta-adopcion.md (§4(h): la demo fallida del piloto y por qué la sonda tiene que ir sobre una query de UNA SOLA TABLA de un método que algún test ejercite)
    - .planning/phases/172-adopci-n-1-piloto-finance/172-21-SUMMARY.md (la transcripción del `TenantSentinelError` del piloto)
    - el-templo-api/src/modules/members/service.ts (para elegir el método: una query de una sola tabla, con call sites reales en `src/` y ejercitada por algún test)
  </read_first>
  <action>
    Un switch sin demo del fail-closed es una afirmación de lectura de código. Demostrarlo **en vivo**:
    1. Elegir una query de **UNA SOLA TABLA** (crítico: si la query nombra dos tablas y la otra lleva su
       filtro, el sentinel marca `ok` toda la query y la demo da falso verde — el piloto perdió un
       intento así) de un método que **algún test ejercite** (el piloto perdió otro intento sobre
       `getById`, que no tenía un solo call site en `src/`). Justificar la elección en el SUMMARY con
       el archivo, el método y el test que lo ejercita.
    2. Sacarle el `tenantWhere` **sin commitear**.
    3. Correr ese test dirigido. Tiene que dar **rojo** con un `TenantSentinelError`.
    4. **Transcribir la salida real** en el SUMMARY: el SQL de la query, la cadena de `cause`
       (`DrizzleQueryError.cause` → `TenantSentinelError`) y los campos propios `sql` y `tables` del
       error. Un `expect(...).rejects.toBeInstanceOf(TenantSentinelError)` **no entra nunca**: el error
       llega envuelto.
    5. **Revertir la sonda** y cerrar con `git status --porcelain` **vacío** y el test verde otra vez.
    Cerrar el plan con el **checklist de cierre del doc 07 §6**, punto por punto, marcado en el
    SUMMARY: código, switch, fail-closed, tests. (Los puntos de NÚMEROS Y GENTE son del plan 173-31.)
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173 && git status --porcelain && cd el-templo-api && pnpm lint:tenant</automated>
  </verify>
  <acceptance_criteria>
    - El SUMMARY transcribe la salida real del `TenantSentinelError`: SQL, cadena de `cause`, y los campos `sql` y `tables`
    - El SUMMARY justifica la elección de la sonda: query de una sola tabla, método con call sites en `src/`, y el test que lo ejercita
    - `git -C /home/franco/projects/et-173 status --porcelain` está **vacío** después de revertir
    - El test elegido vuelve a salir verde después de revertir
    - El checklist del doc 07 §6 (bloques CÓDIGO, EL SWITCH, FAIL-CLOSED, TESTS) está copiado y marcado punto por punto en el SUMMARY
    - `grep -rnE "tenantId!|tenantId\s*\?\?" el-templo-api/src/modules/members/ el-templo-api/src/modules/users/ el-templo-api/src/modules/shared/branch-consistency.ts` no devuelve líneas
    - `pnpm lint:tenant` sale con código 0 con `DISCREPANCIAS: 0`
  </acceptance_criteria>
  <done>El fail-closed está demostrado con evidencia ejecutada, no afirmado, y la sonda no quedó en el repo.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                            |
| ---------------------------------------- | -------------------------------------------------------------------------------------- |
| `TENANT_STRICT_MODULES` → runtime        | encenderlo hace throw sobre toda query sin `tenant_id` en las 8 tablas, en test y dev  |
| allowlist → ratchet                      | agregar una entrada revierte la garantía de toda la plataforma, no solo de este módulo |
| demo con query multi-tabla → falso verde | el sentinel marca `ok` la query entera si cualquier tabla lleva su filtro (trampa (h)) |

## STRIDE Threat Register

| Threat ID   | Category    | Component                               | Disposition | Mitigation Plan                                                                                                  |
| ----------- | ----------- | --------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------- |
| T-173-30-01 | Repudiation | orden de los dos commits                | mitigate    | allowlist primero, strict después: al revés el gate D-15 tumba el commit intermedio y la suite queda roja        |
| T-173-30-02 | Repudiation | allowlist con entradas agregadas        | mitigate    | criterio de aceptación que verifica que el conjunto nuevo es un **subconjunto estricto** del de `origin/staging` |
| T-173-30-03 | Repudiation | gate de forma comparado contra sí mismo | mitigate    | la comparación es contra una SEGUNDA COPIA escrita a mano en el test, no contra el registro                      |
| T-173-30-04 | Repudiation | demo del fail-closed con falso verde    | mitigate    | sonda sobre query de UNA SOLA TABLA de un método ejercitado por un test, con la elección justificada             |
| T-173-30-05 | Tampering   | sonda de la demo commiteada             | mitigate    | `git status --porcelain` vacío como criterio de aceptación, y el test verde otra vez después de revertir         |
| T-173-30-06 | Repudiation | número de gate bajado sin explicar      | mitigate    | toda baja queda contabilizada por una aserción que explica cada unidad perdida (doc 07 §6)                       |

</threat_model>

<verification>
- `pnpm lint:tenant` con `DISCREPANCIAS: 0` contra la allowlist real, y 0 entradas del módulo.
- Allowlist ≤ 360 y subconjunto estricto de la de `origin/staging`.
- Suite **completa** verde con el strict real encendido.
- Salida del `TenantSentinelError` transcrita y sonda revertida (`git status --porcelain` vacío).
- Checklist del doc 07 §6 marcado punto por punto.
</verification>

<success_criteria>
`members` está adoptado por las **cinco** condiciones de la definición operativa: services con
`TenantContext` y helpers en todo acceso, tablas en `TENANT_STRICT_MODULES`, allowlist sin una sola
entrada por tabla ni por archivo, batería con caso + control por ruta y gate que impide que
envejezca. La quinta —los mismos números para el staff— la cierra el plan 173-31.
</success_criteria>

<output>
Create `.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-30-SUMMARY.md` when done
</output>
