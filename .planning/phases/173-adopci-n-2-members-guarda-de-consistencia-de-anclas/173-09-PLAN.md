---
phase: 173-adopci-n-2-members-guarda-de-consistencia-de-anclas
plan: 09
type: execute
wave: 3
depends_on: ["173-04"]
files_modified:
  - el-templo-api/src/modules/progression/routes.ts
  - el-templo-api/src/modules/programs/service.ts
  - el-templo-api/src/modules/programs/enrollment-service.ts
  - el-templo-api/src/modules/goal-plans/routes.ts
  - el-templo-api/src/modules/goal-plans/service.ts
  - el-templo-api/src/modules/sessions/routes.ts
  - el-templo-api/src/modules/streaks/service.ts
  - el-templo-api/src/modules/tree-progress/service.ts
  - el-templo-api/src/modules/bar-challenge/service.ts
autonomous: true
requirements: [ADO-02]
must_haves:
  truths:
    - "D-02: los 9 archivos de entrenamiento y gamificación dejan de violar sobre `users` y `member_profiles`, tocando ÚNICAMENTE esas queries"
    - "Las 10 entradas de allowlist de este grupo desaparecen contra la allowlist de trabajo"
    - "D-09: estas rutas son member-facing y NO reciben caso de aislamiento en esta fase — su query queda filtrada y el caso viaja con su módulo"
  artifacts:
    - path: "el-templo-api/src/modules/progression/routes.ts"
      provides: "progresión del socio resuelta dentro del propio gimnasio"
      contains: "tenantWhere"
  key_links:
    - from: "el-templo-api/src/modules/{progression,goal-plans,sessions,streaks,tree-progress,bar-challenge}"
      to: "schema.users / schema.memberProfiles"
      via: "tenantWhere inline"
      pattern: "tenantWhere\\(schema\\.(users|memberProfiles)"
---

<objective>
Cirugía mínima (D-02) sobre los 9 archivos de entrenamiento, progresión y gamificación: solo sus
queries sobre `users` y `member_profiles`.

Purpose: son las rutas **member-facing** de la app (sesiones, árbol, rachas, goal-plans,
bar-challenge, progresión, programas). D-09 las deja **fuera** de la batería ISO-03 de esta fase
—su caso de aislamiento viaja con su módulo— pero sus queries **sí** tienen que quedar filtradas
ahora: con `users` en strict (D-01), cada una de ellas revienta en el switch. Es el caso más puro
de "migro el acceso, no el módulo".

Output: las 10 entradas de este grupo fuera de la allowlist de trabajo, sin tocar una sola tabla
de entrenamiento.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-CONTEXT.md
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-PATTERNS.md
@.docs/saas-multitenancy/07-receta-adopcion.md

**Ruta de código:** worktree `/home/franco/projects/et-173`.
**Allowlist de trabajo:** `/tmp/allowlist-173-09.json`, derivada de la del plan 173-04.

**Entradas objetivo (10):** `progression/routes.ts` → `member_profiles`, `users`;
`programs/service.ts` → `users`; `programs/enrollment-service.ts` → `users`;
`goal-plans/routes.ts` → `users`; `goal-plans/service.ts` → `users`; `sessions/routes.ts` →
`users`; `tree-progress/service.ts` → `users`; `bar-challenge/service.ts` → `users`;
`streaks/service.ts` → `member_profiles`.

⚠ `programs/enrollment-service.ts` y `programs/routes.ts` ya fueron tocados por el plan 173-04
(call site de `audit-log`). Partir del estado post-173-04.
</context>

<tasks>

<task type="auto">
  <name>Task 1: progresión, sesiones, árbol y rachas</name>
  <files>el-templo-api/src/modules/progression/routes.ts, el-templo-api/src/modules/sessions/routes.ts, el-templo-api/src/modules/tree-progress/service.ts, el-templo-api/src/modules/streaks/service.ts, el-templo-api/src/modules/bar-challenge/service.ts</files>
  <read_first>
    - el-templo-api/src/modules/progression/routes.ts (sus 2 accesos y si están en el handler o en un service)
    - el-templo-api/src/modules/sessions/routes.ts
    - el-templo-api/src/modules/tree-progress/service.ts
    - el-templo-api/src/modules/streaks/service.ts (su acceso a `member_profiles`)
    - el-templo-api/src/modules/bar-challenge/service.ts
    - el-templo-api/src/modules/finance/coach-load-routes.ts (:288-321 y :478 — cómo una closure de rutas recibe `ctx` y cómo se saca del borde con `assertTenant`)
    - .planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-02-SUMMARY.md
  </read_first>
  <action>
    Para cada acceso: `ctx: TenantContext` PRIMERO en la firma del método, `tenantWhere` **inline** en
    el statement que nombra la tabla, filtro **en el `ON`** para todo join.
    ⚠ Son rutas **member-facing**: el `ctx` sale igual de `assertTenant(request.scope,
    "<módulo>.<operación>")` — el socio autenticado tiene scope, y su `tenantId` viene del server, no
    del JWT ni del payload. Si alguna ruta acá es **pública** (sin `request.scope`), NO inventar un
    `assertTenant`: el `ctx` se deriva de una **fila leída del server** (el patrón del doc 07 §4(f)),
    con guard fail-closed y nunca un default numérico al gimnasio 1. Documentarlo si aparece.
    ⚠ Si la query vive en el cuerpo del plugin (closure sin `request` a mano), recibe `ctx` como
    **primer parámetro** y cada call site le pasa el que ya resolvió.
    D-02: cero cambios sobre `completed_sessions`, `tree_nodes`, `streaks`, `bar_challenge_*` ni
    ninguna otra tabla de entrenamiento — esas van con la adopción de su módulo.
    Dejar escrito en el SUMMARY, para cada ruta tocada, que **NO** recibe caso de aislamiento en esta
    fase (D-09) y que su cobertura es deuda con dueño en la fase de su módulo.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec tsc -p tsconfig.test-check.json --noEmit && pnpm exec vitest run --no-file-parallelism test/progression test/sessions test/tree-progress test/streaks test/bar-challenge</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm lint:tenant --allowlist /tmp/allowlist-173-09.json` no reporta las 6 entradas de estos 5 archivos
    - Cada `assertTenant` nuevo tiene etiqueta `"<módulo>.<operación>"` y hay **uno solo** por handler
    - Ninguna ruta pública resuelve el gimnasio con `!`, `?? 1` ni un literal numérico (verificado por grep sobre los 5 archivos)
    - El diff no toca queries sobre tablas de entrenamiento (D-02), verificado leyendo el diff completo
    - `pnpm exec vitest run --no-file-parallelism test/progression test/sessions test/tree-progress test/streaks test/bar-challenge` sale con código 0
    - El SUMMARY lista las rutas tocadas marcadas como "sin caso ISO-03 en 173, dueño: fase de su módulo (D-09)"
  </acceptance_criteria>
  <done>La app del socio resuelve su propio perfil dentro de su gimnasio, sin que se migre ninguna tabla de entrenamiento.</done>
</task>

<task type="auto">
  <name>Task 2: programas y goal-plans</name>
  <files>el-templo-api/src/modules/programs/service.ts, el-templo-api/src/modules/programs/enrollment-service.ts, el-templo-api/src/modules/goal-plans/routes.ts, el-templo-api/src/modules/goal-plans/service.ts</files>
  <read_first>
    - el-templo-api/src/modules/programs/service.ts y enrollment-service.ts (su acceso a `users`; enrollment ya tiene `ctx` desde 173-04 por el call site de audit-log)
    - el-templo-api/src/modules/goal-plans/routes.ts y service.ts
    - el-templo-api/src/modules/programs/routes.ts (cómo resuelve el scope: es el único archivo de rutas que lo hace per-ruta con `await attachCountryScope(...)` en vez de un hook de plugin)
    - .planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-02-SUMMARY.md
  </read_first>
  <action>
    Mismo tratamiento, una entrada de `users` por archivo.
    ⚠ `programs/routes.ts` es el único archivo de rutas que **no** monta `attachCountryScope` en un
    hook `onRequest` de plugin: lo resuelve per-ruta. Si acá hace falta un `assertTenant`, verificar
    que el scope ya esté resuelto **antes** de llamarlo, o `request.scope` llega `undefined` (esa fue
    una regresión real del piloto).
    `enrollment-service.ts` ya recibe `ctx` desde 173-04: reusarlo, no agregar un segundo parámetro.
    Recordar que **tener `ctx` ≠ estar migrado**.
    D-02: cero cambios sobre `programs`, `program_enrollments`, `goal_plans` ni sus tablas.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec tsc -p tsconfig.test-check.json --noEmit && pnpm exec vitest run --no-file-parallelism test/programs test/goal-plans</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm lint:tenant --allowlist /tmp/allowlist-173-09.json` reporta CERO entradas para los 9 archivos de este plan
    - `enrollment-service.ts` no gana un segundo parámetro de contexto: reusa el `ctx` que 173-04 le dio
    - Si se agregó un `assertTenant` en `programs/routes.ts` o `goal-plans/routes.ts`, el scope ya está resuelto en el mismo handler antes de la llamada (verificado leyendo el diff)
    - `grep -rnE "tenantId!|tenantId\s*\?\?" el-templo-api/src/modules/{programs,goal-plans}/` no devuelve líneas
    - `pnpm exec tsc -p tsconfig.test-check.json --noEmit` sale con código 0
    - `pnpm exec vitest run --no-file-parallelism test/programs test/goal-plans` sale con código 0
  </acceptance_criteria>
  <done>Las 10 entradas de este grupo están fuera de la allowlist de trabajo.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                                               |
| -------------------------- | ----------------------------------------------------------------------------------------- |
| app del socio → `users`    | el `userId` puede llegar por params en rutas member-facing                                |
| ruta pública → `tenant_id` | sin `request.scope`, el gimnasio tiene que salir de una fila leída del server             |
| `programs/routes.ts`       | resuelve el scope per-ruta, no por hook: un `assertTenant` antes de tiempo ve `undefined` |

## STRIDE Threat Register

| Threat ID   | Category               | Component                               | Disposition | Mitigation Plan                                                                                                               |
| ----------- | ---------------------- | --------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------- |
| T-173-09-01 | Information Disclosure | perfiles y progresos de socios          | mitigate    | `tenantWhere` inline sobre `users` y `member_profiles`; el `userId` de params deja de matchear si es ajeno                    |
| T-173-09-02 | Elevation of Privilege | ruta pública sin `request.scope`        | mitigate    | el `ctx` sale de una fila del server con guard fail-closed (doc 07 §4(f)); prohibido el default numérico                      |
| T-173-09-03 | Denial of Service      | `assertTenant` con scope `undefined`    | mitigate    | en `programs/routes.ts` se verifica que el scope esté resuelto antes de la llamada (regresión real del piloto)                |
| T-173-09-SC | Repudiation            | cobertura ISO-03 de rutas member-facing | accept      | D-09: sus casos de aislamiento viajan con la fase de su módulo; se registra explícitamente como deuda con dueño en el SUMMARY |

</threat_model>

<verification>
- `pnpm lint:tenant --allowlist /tmp/allowlist-173-09.json` sin las 10 entradas.
- `pnpm exec tsc -p tsconfig.test-check.json --noEmit` verde.
- Suites de progression, sessions, tree-progress, streaks, bar-challenge, programs y goal-plans verdes.
- El SUMMARY lista las rutas member-facing tocadas y su deuda de cobertura con dueño.
</verification>

<success_criteria>
Todas las rutas de entrenamiento y gamificación resuelven al socio dentro de su gimnasio, sin
que se haya migrado una sola tabla de esos módulos, y la deuda de cobertura queda escrita con
dueño en vez de olvidada.
</success_criteria>

<output>
Create `.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-09-SUMMARY.md` when done
</output>
