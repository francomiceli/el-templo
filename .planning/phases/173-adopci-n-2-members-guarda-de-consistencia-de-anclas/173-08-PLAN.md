---
phase: 173-adopci-n-2-members-guarda-de-consistencia-de-anclas
plan: 08
type: execute
wave: 3
depends_on: ["173-01", "173-02"]
files_modified:
  - el-templo-api/src/modules/campaigns/service.ts
  - el-templo-api/src/modules/segmentation/service.ts
  - el-templo-api/src/modules/notifications/routes.ts
  - el-templo-api/src/modules/notifications/service.ts
  - el-templo-api/src/modules/onboarding/service.ts
  - el-templo-api/src/modules/onboarding/routes.ts
  - el-templo-api/src/modules/improvement-proposals/service.ts
  - el-templo-api/src/modules/admin/service.ts
  - el-templo-api/src/modules/referrals/service.ts
autonomous: true
requirements: [ADO-02]
must_haves:
  truths:
    - "D-02: campañas, segmentación, notificaciones, onboarding, sugerencias, admin y referidos dejan de violar sobre `users`, `member_profiles`, `member_logins` y `user_status_history`"
    - "Las 14 entradas de allowlist de estos 9 archivos desaparecen contra la allowlist de trabajo"
    - "Ninguna audiencia de campaña ni segmento puede incluir socios de otro gimnasio"
  artifacts:
    - path: "el-templo-api/src/modules/segmentation/service.ts"
      provides: "segmentos calculados solo sobre socios del propio gimnasio (`member_logins`, `member_profiles`, `users`)"
      contains: "tenantWhere"
  key_links:
    - from: "el-templo-api/src/modules/campaigns/service.ts"
      to: "schema.users / schema.userStatusHistory"
      via: "tenantWhere inline en la construcción de la audiencia"
      pattern: "tenantWhere\\(schema\\.(users|userStatusHistory)"
---

<objective>
Cirugía mínima (D-02) sobre los 9 archivos de comunicación, segmentación, onboarding y
referidos: solo sus queries sobre tablas del módulo.

Purpose: acá vive la peor consecuencia práctica de una fuga: una **campaña de email o push que
sale a socios de otro gimnasio**. La audiencia se construye leyendo `users`,
`user_status_history`, `member_profiles` y `member_logins`; sin filtro, el destinatario ajeno
entra a la lista y el mensaje sale. No hay 404 que lo mitigue después.

Output: las 14 entradas de estos 9 archivos fuera de la allowlist de trabajo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-CONTEXT.md
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-PATTERNS.md
@.docs/saas-multitenancy/07-receta-adopcion.md

**Ruta de código:** worktree `/home/franco/projects/et-173`.
**Allowlist de trabajo:** `/tmp/allowlist-173-08.json`.

**Entradas objetivo (14):** `segmentation/service.ts` → `member_logins`, `member_profiles`,
`users`; `campaigns/service.ts` → `user_status_history`, `users`; `notifications/routes.ts` →
`member_profiles`, `users`; `onboarding/service.ts` → `member_profiles`, `users`;
`notifications/service.ts` → `users`; `onboarding/routes.ts` → `users`;
`improvement-proposals/service.ts` → `users`; `admin/service.ts` → `users`;
`referrals/service.ts` → `users`.

⚠ `referrals/service.ts`: el backmerge de 173-01 trajo `a36b759d` ("acotar al gimnasio el chequeo
de primer pago"), que **ya toca tenancy de referidos**. Leer ese commit antes de migrar para no
duplicar ni deshacer su filtro.
</context>

<tasks>

<task type="auto">
  <name>Task 1: segmentación, campañas y notificaciones — la construcción de audiencias</name>
  <files>el-templo-api/src/modules/segmentation/service.ts, el-templo-api/src/modules/campaigns/service.ts, el-templo-api/src/modules/notifications/routes.ts, el-templo-api/src/modules/notifications/service.ts</files>
  <read_first>
    - el-templo-api/src/modules/segmentation/service.ts (sus 3 accesos y cómo compone el segmento)
    - el-templo-api/src/modules/campaigns/service.ts (cómo se arma la audiencia de una campaña)
    - el-templo-api/src/modules/notifications/routes.ts y service.ts (a quién se le manda el push)
    - el-templo-api/src/modules/finance/transaction-service.ts (`buildListConditions`: helper que devuelve `SQL[]` con el `tenantWhere` como primer elemento)
    - .planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-02-SUMMARY.md
  </read_first>
  <action>
    `ctx: TenantContext` PRIMERO en la firma del método que hace cada query, `tenantWhere` **inline**
    en el statement que nombra la tabla, filtro **en el `ON`** de todo join, y un `tenantWhere` por
    **cada** tabla del módulo del statement.
    ⚠ Si la audiencia se construye componiendo condiciones en un array (`const conditions = [...]`
    armado arriba y reusado), el gimnasio **no** puede vivir ahí sin más: o se nombra inline en cada
    statement, o el array se produce por un helper que reciba `ctx` y ponga el `tenantWhere` como
    **primer elemento**, y entonces no se duplica en el llamador. Es la única excepción reconocida
    (doc 07 §4(b)) y la que hace que la próxima condición que alguien agregue nazca scopeada.
    ⚠ Si algún camino de segmentación arma SQL crudo, el filtro va como
    `WHERE tenant_id = ${ctx.tenantId}` dentro del propio SQL.
    D-02: no se migran `campaigns`, `campaign_sends`, `segments` ni `notifications` como tablas.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec tsc -p tsconfig.test-check.json --noEmit && pnpm exec vitest run --no-file-parallelism test/campaigns test/segmentation test/notifications</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm lint:tenant --allowlist /tmp/allowlist-173-08.json` no reporta las 8 entradas de estos 4 archivos
    - Toda condición de audiencia que nombra `users` lleva el `tenantWhere` en el mismo statement, o proviene de un helper con `ctx` que lo pone en el elemento `[0]` del array (documentado en el SUMMARY, caso por caso)
    - `grep -rnE "tenantId!|tenantId\s*\?\?" el-templo-api/src/modules/{campaigns,segmentation,notifications}/` no devuelve líneas
    - Si hay SQL crudo, contiene el literal `tenant_id` en su zona de predicado
    - `pnpm exec vitest run --no-file-parallelism test/campaigns test/segmentation test/notifications` sale con código 0
  </acceptance_criteria>
  <done>Una campaña o un push no puede alcanzar a un socio de otro gimnasio.</done>
</task>

<task type="auto">
  <name>Task 2: onboarding, sugerencias, admin y referidos</name>
  <files>el-templo-api/src/modules/onboarding/service.ts, el-templo-api/src/modules/onboarding/routes.ts, el-templo-api/src/modules/improvement-proposals/service.ts, el-templo-api/src/modules/admin/service.ts, el-templo-api/src/modules/referrals/service.ts</files>
  <read_first>
    - el-templo-api/src/modules/onboarding/service.ts y routes.ts (sus 3 accesos)
    - el-templo-api/src/modules/improvement-proposals/service.ts (el canal ANÓNIMO de sugerencias: verificar qué se lee de `users` y por qué)
    - el-templo-api/src/modules/admin/service.ts
    - el-templo-api/src/modules/referrals/service.ts (y el commit `a36b759d` del backmerge, que ya toca tenancy acá)
    - .planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-02-SUMMARY.md
  </read_first>
  <action>
    Mismo tratamiento, una entrada por archivo salvo onboarding (3 entre sus 2 archivos).
    Puntos específicos:
    - `improvement-proposals/service.ts`: el canal es **anónimo** por diseño. Si la lectura de
      `users` existe solo para verificar pertenencia, filtrarla por gimnasio **no** rompe el
      anonimato (no expone al autor) y sí evita que una propuesta se atribuya al gimnasio
      equivocado. Dejarlo escrito en un comentario de bloque.
    - `referrals/service.ts`: leer `a36b759d` primero. Si ese commit ya puso el filtro en la query
      objetivo, la tarea es verificar y **no duplicar**; si puso otro filtro (por gimnasio del
      referidor, por ejemplo), el `tenantWhere` de `users` va igual y se documenta la diferencia.
    - `onboarding/routes.ts`: revisar si la query está en el handler y no en el service; en ese
      caso el `ctx` sale de `assertTenant(request.scope, "onboarding.<operación>")`.
    D-02: en `admin/service.ts` y `onboarding/*` no se migra ninguna otra tabla.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec tsc -p tsconfig.test-check.json --noEmit && pnpm exec vitest run --no-file-parallelism test/onboarding test/referrals test/improvement-proposals test/admin</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm lint:tenant --allowlist /tmp/allowlist-173-08.json` reporta CERO entradas para los 9 archivos de este plan
    - El SUMMARY documenta explícitamente cómo quedó `referrals/service.ts` respecto de `a36b759d` (filtro nuevo, filtro ya presente, o los dos y por qué)
    - `improvement-proposals/service.ts` tiene un comentario de bloque explicando que el filtro de gimnasio no rompe el anonimato del canal
    - `grep -rnE "tenantId!|tenantId\s*\?\?" el-templo-api/src/modules/{onboarding,improvement-proposals,admin,referrals}/` no devuelve líneas
    - `pnpm exec tsc -p tsconfig.test-check.json --noEmit` sale con código 0
    - `pnpm exec vitest run --no-file-parallelism test/onboarding test/referrals test/improvement-proposals test/admin` sale con código 0
  </acceptance_criteria>
  <done>Las 14 entradas de este grupo están fuera de la allowlist de trabajo.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                             |
| --------------------------------- | --------------------------------------------------------------------------------------- |
| audiencia de campaña → email/push | un destinatario ajeno que entra a la lista recibe el mensaje: no hay 404 que lo mitigue |
| segmento → decisión comercial     | un segmento contaminado mueve precios y promos sobre socios de otro gimnasio            |
| canal anónimo → identidad         | filtrar por gimnasio no puede convertirse en una vía para desanonimizar al autor        |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                      | Disposition | Mitigation Plan                                                                                                                                 |
| ----------- | ---------------------- | ---------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| T-173-08-01 | Information Disclosure | audiencia de campañas (`users`)                | mitigate    | `tenantWhere` inline en cada statement; si la audiencia sale de un array de condiciones, el filtro va en el elemento `[0]` del helper con `ctx` |
| T-173-08-02 | Information Disclosure | segmentos (`member_logins`, `member_profiles`) | mitigate    | un `tenantWhere` por tabla del módulo presente en el statement (el sentinel no lo caza, trampa (h))                                             |
| T-173-08-03 | Spoofing               | atribución de una sugerencia anónima           | mitigate    | el filtro por gimnasio se aplica sin exponer al autor; comentario de bloque que documenta la decisión                                           |
| T-173-08-04 | Tampering              | duplicación/deshecho del filtro de `a36b759d`  | mitigate    | leer el commit antes de migrar `referrals/service.ts` y documentar en el SUMMARY cómo quedó                                                     |

</threat_model>

<verification>
- `pnpm lint:tenant --allowlist /tmp/allowlist-173-08.json` sin las 14 entradas.
- `pnpm exec tsc -p tsconfig.test-check.json --noEmit` verde.
- Suites de campaigns, segmentation, notifications, onboarding, referrals, improvement-proposals y admin verdes.
- El SUMMARY documenta la interacción con `a36b759d`.
</verification>

<success_criteria>
Ninguna audiencia, segmento, notificación ni referido puede alcanzar o atribuir a un socio de
otro gimnasio, y el filtro se escribe donde el próximo que agregue una condición lo herede.
</success_criteria>

<output>
Create `.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-08-SUMMARY.md` when done
</output>
