---
phase: 173-adopci-n-2-members-guarda-de-consistencia-de-anclas
plan: 23
type: execute
wave: 9
depends_on: ["173-22"]
files_modified:
  - el-templo-api/test/subscriptions/
  - el-templo-api/test/finance/
  - el-templo-api/test/reports/
  - el-templo-api/test/coach/
  - el-templo-api/test/programs/
  - el-templo-api/test/referrals/
  - el-templo-api/test/subscriptions-conversion-hook.test.ts
  - el-templo-api/test/notification-plan-renewal.test.ts
autonomous: true
requirements: [ADO-02]
must_haves:
  truths:
    - "Los ~75 archivos de suscripciones, finanzas, reportes, coach, programas y referidos sobreviven al throw sobre las 8 tablas"
    - "Ninguna exención nueva aparece dentro de `test/finance/`: ese grupo ya fue endurecido por la 172 y una exención nueva ahí es señal de atajo"
    - "Los DELETE de conveniencia se acotan con filtro parametrizado; solo la limpieza global y las lecturas de evidencia se eximen"
  artifacts:
    - path: "el-templo-api/test/subscriptions/"
      provides: "el grupo más grande de la suite (26 archivos) verde con el sentinel en throw"
  key_links:
    - from: "los beforeEach de test/subscriptions y test/finance"
      to: "el pool envuelto por el sentinel"
      via: "filtro WHERE tenant_id = ? parametrizado sobre las 8 tablas del módulo"
      pattern: "tenant_id"
---

<objective>
Paso 5 de la receta, plan 3 de 5: los tests de los módulos que **consumen** socios — suscripciones,
finanzas, reportes, coach, programas y referidos.

Purpose: es el grupo más grande de la suite (~75 archivos) y el que más socios crea por test:
cobrar, renovar, asignar plan y cerrar caja empiezan todos con "creo un socio". `test/finance/` ya
fue endurecido por la 172 para **sus** 6 tablas, pero sus `beforeEach` crean usuarios y perfiles
que hasta ahora eran inocuos y a partir del switch no lo son.

Output: el grupo verde con la sonda encendida, sin exenciones nuevas en el territorio que la 172 ya
había dejado limpio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-CONTEXT.md
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-PATTERNS.md
@.docs/saas-multitenancy/07-receta-adopcion.md

**Ruta de código:** worktree `/home/franco/projects/et-173`.
**Sonda:** mismo procedimiento del plan 173-21, con pre-check `git status --porcelain`.
**Notas de corrida:** `--no-file-parallelism`; `prettier --write` antes de la corrida larga; el
throw llega envuelto en `DrizzleQueryError.cause`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: test/subscriptions y test/finance (~47 archivos)</name>
  <files>el-templo-api/test/subscriptions/, el-templo-api/test/finance/, el-templo-api/test/subscriptions-conversion-hook.test.ts, el-templo-api/test/notification-plan-renewal.test.ts</files>
  <read_first>
    - el-templo-api/test/helpers.ts (post-173-21: la regla de decisión de 4 categorías)
    - .planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-22-SUMMARY.md (la clasificación ya hecha para el grupo del módulo — copiar el criterio, no reinventarlo)
    - .planning/phases/172-adopci-n-1-piloto-finance/172-14-SUMMARY.md (cómo quedó `test/finance/` después del endurecimiento de la 172)
    - el-templo-api/test/finance/coach-load.test.ts (el archivo que ya tenía el patrón de limpieza FK-safe con conexión única y `FOREIGN_KEY_CHECKS = 0`)
    - .planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-02-SUMMARY.md
  </read_first>
  <action>
    Encender la sonda y correr los dos directorios en un solo lote (el tiempo de reloj lo domina
    vitest: planificar corridas grandes, no de a un archivo).
    Clasificar cada throw con la regla del doc 07 §4(d), copiando el criterio ya aplicado en 173-22:
    conveniencia → **filtro** parametrizado; evidencia → **exención** con motivo; limpieza global →
    **exención** adentro del SQL; tabla no strict → **no se anota**.
    ⚠ **`test/finance/` es territorio ya endurecido.** Una exención **nueva** dentro de ese directorio
    es señal de atajo: la 172 lo dejó limpio para sus tablas y el criterio no cambió. Si aparece una,
    justificarla explícitamente en el SUMMARY o convertirla en filtro.
    ⚠ Buscar por los tres canales: Drizzle, **SQL crudo con backticks**, y tabla dinámica (`sql.raw`)
    clasificada a mano contra el `.sql` que ejecuta.
    ⚠ `test/subscriptions/` tiene tests con **fecha fija y ventana relativa** que rompen CI solo por el
    calendario. Si un test de este grupo se pone rojo y el diff no lo toca, sospechar del almanaque
    antes que del cambio.
    ⚠ Si aparece un `ER_ROW_IS_REFERENCED_2` en un archivo que la fase no tocó, **no es el sentinel** —
    mirar el patrón de limpieza (conexión única + `FOREIGN_KEY_CHECKS = 0`).
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec vitest run --no-file-parallelism test/subscriptions test/finance test/subscriptions-conversion-hook.test.ts test/notification-plan-renewal.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Con la sonda encendida, los dos directorios y los 2 archivos sueltos salen con código 0
    - `git diff --stat el-templo-api/test/finance/` — cada exención nueva en ese directorio está justificada nominalmente en el SUMMARY (idealmente son cero)
    - El SUMMARY lista cada sitio tocado con archivo, línea, canal y clasificación
    - Los sitios de SQL crudo con backticks están listados aparte con su conteo
    - Después de revertir la sonda, `git status --porcelain` no muestra `src/db/tenant-tables.ts`
    - `pnpm exec tsc -p tsconfig.test-check.json --noEmit` sale con código 0
  </acceptance_criteria>
  <done>El grupo más grande de la suite sobrevive al throw sin ensuciar el territorio que la 172 ya había limpiado.</done>
</task>

<task type="auto">
  <name>Task 2: test/reports, test/coach, test/programs y test/referrals (~28 archivos)</name>
  <files>el-templo-api/test/reports/, el-templo-api/test/coach/, el-templo-api/test/programs/, el-templo-api/test/referrals/</files>
  <read_first>
    - el-templo-api/test/referrals/ (14 archivos: el grupo más grande de esta tarea; el sistema de referidos vive de `users`)
    - el-templo-api/test/reports/, test/coach/, test/programs/
    - .planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-22-SUMMARY.md (el criterio)
    - .planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-08-SUMMARY.md (cómo quedó `referrals/service.ts` respecto de `a36b759d`)
  </read_first>
  <action>
    Mismo tratamiento con la sonda.
    ⚠ `test/referrals/` es el grupo con más densidad de `users` **por diseño** (un referido es una
    relación entre dos usuarios) y el que más va a tirar throw por statement. Además el backmerge trajo
    `a36b759d` y `0661f987`, que tocan tenancy de referidos: si algún test de este directorio cambia de
    significado por esos commits o por el plan 173-08, documentarlo — no bajar la aserción.
    ⚠ `test/coach/` y `test/reports/` usan `member-search` y `covered-until`, migrados en 173-05: sus
    call sites de test pueden tener mocks posicionales. El `tsconfig.test-check.json` los caza en el
    typecheck, pero un mock reasignado a mano (`servicio.metodo = async ...`) **no** lo caza el
    compilador: revisar el barrido del plan 173-02.
    Cerrar la tarea con una corrida de **regresión del grupo completo** de este plan más el del plan
    173-22, en un solo lote.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec vitest run --no-file-parallelism test/reports test/coach test/programs test/referrals</automated>
  </verify>
  <acceptance_criteria>
    - Con la sonda encendida, los 4 directorios salen con código 0
    - Con la sonda encendida, la corrida de regresión `test/members test/users test/auth test/shared test/subscriptions test/finance test/reports test/coach test/programs test/referrals` sale con código 0
    - El SUMMARY documenta los tests de `test/referrals/` que cambiaron de significado y por qué
    - Toda exención nueva tiene motivo escrito
    - Los mocks posicionales encontrados en `test/coach` y `test/reports` están listados y arreglados
    - Después de revertir la sonda, `git status --porcelain` no muestra `src/db/tenant-tables.ts`
  </acceptance_criteria>
  <done>Los módulos que consumen socios corren verdes con el sentinel en throw.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                                                       |
| -------------------------------------- | --------------------------------------------------------------------------------- |
| `beforeEach` → pool envuelto           | el sentinel ve toda query de test, incluidas las de módulos ya endurecidos        |
| territorio ya limpio (`test/finance/`) | una exención nueva ahí revierte trabajo de la fase anterior sin que nadie lo note |
| mock reasignado a mano → firma real    | el compilador no lo caza; el test queda verde probando nada                       |

## STRIDE Threat Register

| Threat ID   | Category          | Component                            | Disposition | Mitigation Plan                                                                                             |
| ----------- | ----------------- | ------------------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------- |
| T-173-23-01 | Repudiation       | exenciones nuevas en `test/finance/` | mitigate    | criterio de aceptación que las exige justificadas nominalmente; idealmente cero                             |
| T-173-23-02 | Repudiation       | mocks posicionales en coach/reports  | mitigate    | barrido del plan 173-02 aplicado a este grupo; el typecheck no los caza                                     |
| T-173-23-03 | Repudiation       | aserciones bajadas en referidos      | mitigate    | los tests que cambian de significado por `a36b759d` o por 173-08 se documentan, no se bajan                 |
| T-173-23-04 | Denial of Service | bombas FK entre workers              | mitigate    | patrón FK-safe; un `ER_ROW_IS_REFERENCED_2` en archivo ajeno se diagnostica como limpieza, no como sentinel |

</threat_model>

<verification>
- Con la sonda encendida, la corrida de regresión de los grupos 173-22 + 173-23 verde.
- `git status --porcelain` sin `src/db/tenant-tables.ts` al cerrar.
- `pnpm exec tsc -p tsconfig.test-check.json --noEmit` verde.
- El SUMMARY tiene la tabla de clasificación y las exenciones nuevas justificadas.
</verification>

<success_criteria>
Los ~75 archivos que crean socios para probar otra cosa aguantan el throw, y la limpieza que la fase
anterior dejó en finanzas sigue intacta.
</success_criteria>

<output>
Create `.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-23-SUMMARY.md` when done
</output>
