---
phase: 173-adopci-n-2-members-guarda-de-consistencia-de-anclas
plan: 16
type: execute
wave: 4
depends_on: ["173-11", "173-12"]
files_modified:
  - el-templo-api/src/jobs/reassign-multibranch.ts
  - el-templo-api/src/jobs/notification-cron.ts
  - el-templo-api/src/jobs/expire-lost-leads.ts
  - el-templo-api/test/tenancy/ancla-cron-reassign.test.ts
autonomous: true
requirements: [ADO-02, ADO-07]
must_haves:
  truths:
    - "D-04: los 3 jobs que tocan tablas del módulo migran sus queries — tener `forEachActiveTenant` no era estar migrado (trampa (a))"
    - "D-07: las 4 queries de `runReassignMultibranchForTenant` usan el `ctx` que ya reciben, y `reassignMemberBranch` recibe `ctx` y valida la sede con el helper antes del UPDATE"
    - "D-07 / SC3: si la sede candidata no es del gimnasio, el cron SALTEA al socio, lo loguea con `tenantId` como campo estructurado y SIGUE — nunca aborta el barrido"
    - "SC3: hay un test que le ofrece al cron sedes de los DOS gimnasios y verifica que solo considera las propias"
  artifacts:
    - path: "el-templo-api/src/jobs/reassign-multibranch.ts"
      provides: "recategorización multisucursal que nunca mueve un socio a una sede de otro gimnasio"
      contains: "tenantWhere"
    - path: "el-templo-api/test/tenancy/ancla-cron-reassign.test.ts"
      provides: "el test del criterio 3 de la fase, con sedes de ambos gimnasios sobre la mesa"
  key_links:
    - from: "el-templo-api/src/jobs/reassign-multibranch.ts"
      to: "el-templo-api/src/modules/shared/branch-consistency.ts"
      via: "validación de la sede dominante antes del UPDATE de la sede del socio"
      pattern: "BranchDelGimnasio"
---

<objective>
Migrar los 3 crons que tocan tablas del módulo y cerrar el criterio 3 de la fase: el cron de
recategorización multisucursal nunca mueve a un socio a una sede de otro gimnasio.

Purpose: `reassign-multibranch.ts` es la trampa (a) en su forma más pura. **Ya barre por gimnasio**
(`forEachActiveTenant`, fases 169/171) y `runReassignMultibranchForTenant` **recibe `ctx` y no lo
usa en ninguna de sus 4 queries**: los candidatos salen de `subscriptions` sin filtro, el estado
actual de `users` sin filtro, y el mapa de sedes lee **todas las sedes de todos los gimnasios**. El
UPDATE (`:287-296`) vive en un helper que **ni siquiera recibe `ctx`**. Es un proceso automático,
sin nadie mirando, que reescribe el ancla del socio: exactamente el escenario de la mina M10.

Output: los 3 jobs migrados, el UPDATE del cron validado por el helper con la política de
saltear-y-seguir, y el test SC3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-CONTEXT.md
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-PATTERNS.md
@.docs/saas-multitenancy/07-receta-adopcion.md

**Ruta de código:** worktree `/home/franco/projects/et-173`.
**Allowlist de trabajo:** `/tmp/allowlist-173-16.json`.

**Entradas objetivo (4):** `notification-cron.ts` → `member_profiles`, `users`;
`expire-lost-leads.ts` → `users`; `reassign-multibranch.ts` → `users`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: reassign-multibranch.ts — las 4 queries y el UPDATE con guarda (D-04, D-07)</name>
  <files>el-templo-api/src/jobs/reassign-multibranch.ts</files>
  <read_first>
    - el-templo-api/src/jobs/reassign-multibranch.ts (archivo completo, 350 líneas: :110-125 el `forEachActiveTenant`, :130-136 el log con `tenantId` estructurado —el único ejemplar bueno del archivo—, las 4 queries de `runReassignMultibranchForTenant`, y :287-296 `reassignMemberBranch`)
    - el-templo-api/src/modules/shared/branch-consistency.ts (el helper del plan 173-11)
    - el-templo-api/src/modules/shared/tenant.ts (`forEachActiveTenant`, `tenantWhere`, `TenantContext`)
    - el-templo-api/src/modules/finance/cash-register-service.ts (:446-465 — filtro de join en el ON)
  </read_first>
  <action>
    Las **4 queries** de `runReassignMultibranchForTenant(db, ctx, opts)` pasan a usar el `ctx` que ya
    reciben:
    1. **Candidatos** — `selectDistinct` desde `subscriptions` con `innerJoin` a `subscriptionPlans`:
       el filtro va **en el `ON`** del join y en el `where` de la tabla base. ⚠ `subscriptions` no es
       tabla strict de esta fase (es de la 174): filtrarla igual es correcto y necesario para que el
       barrido sea por gimnasio, pero **no** se anota nada en la allowlist por ella.
    2. **Estado actual** — `select ... from users where inArray(users.id, candidateIds)`:
       `tenantWhere(schema.users, ctx)` **inline** como primer término del `and(...)`.
    3. **Mapa de sedes → país** — `select ... from branches` **sin ningún filtro**, o sea todas las
       sedes de todos los gimnasios. `tenantWhere(schema.branches, ctx)` inline. Esta es la query que
       hace posible que el cron elija una sede ajena como "dominante".
    4. La cuarta query del método, según el inventario del plan 173-02.
    **`reassignMemberBranch` (D-07):** cambiar la firma a `(ctx: TenantContext, db, memberId,
    branchId)` — `ctx` PRIMERO. Antes del UPDATE, resolver la sede con
    `resolveBranchDelGimnasio(ctx, branchId, tx)`:
    - si resuelve → UPDATE con `tenantWhere(schema.users, ctx)` en el `where`, escribiendo el `id`
      de la fila resuelta, y manteniendo el trío `branchId` + `branchUpdatedAt` + `branchSource`;
    - si **no** resuelve → **saltear al socio, loguear y seguir**. Nunca abortar el barrido: un
      gimnasio con un dato raro no puede dejar sin recategorizar a los otros. El log lleva
      `tenantId`, `memberId` y `branchId` como **campos estructurados** de Pino, jamás interpolados en
      el mensaje (el ejemplar es `:130-136` del propio archivo). Contar los salteados y reportarlos en
      el resumen del job.
    ⚠ Con la FK compuesta del plan 173-12 aplicada, un UPDATE cross-tenant además **explota en la
    base**. La guarda de app existe para que el cron no muera con un error de FK: el objetivo es
    saltear limpio, no reventar.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec tsc -p tsconfig.test-check.json --noEmit && pnpm exec vitest run --no-file-parallelism test/jobs test/tenancy/con-04-crons-per-tenant.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Las 4 queries de `runReassignMultibranchForTenant` nombran el gimnasio inline (verificadas una por una y listadas en el SUMMARY con su línea)
    - `grep -c "from(schema.branches)" el-templo-api/src/jobs/reassign-multibranch.ts` — cada ocurrencia tiene su `tenantWhere` en el mismo statement
    - La firma de `reassignMemberBranch` tiene `ctx: TenantContext` como primer parámetro
    - `grep -c "resolveBranchDelGimnasio\|assertBranchDelGimnasio" el-templo-api/src/jobs/reassign-multibranch.ts` devuelve al menos `1`
    - El camino de "sede ajena" NO lanza: hay un `continue`/`return` que saltea al socio y un `log.warn` con `tenantId` como campo estructurado (primer argumento objeto de Pino, no interpolado)
    - El resumen del job reporta el conteo de socios salteados
    - `pnpm exec vitest run --no-file-parallelism test/tenancy/con-04-crons-per-tenant.test.ts` sale con código 0
  </acceptance_criteria>
  <done>El cron que reescribe la sede del socio solo puede elegir entre sedes del propio gimnasio, y un caso raro lo saltea en vez de tumbar el barrido.</done>
</task>

<task type="auto">
  <name>Task 2: notification-cron.ts y expire-lost-leads.ts</name>
  <files>el-templo-api/src/jobs/notification-cron.ts, el-templo-api/src/jobs/expire-lost-leads.ts</files>
  <read_first>
    - el-templo-api/src/jobs/notification-cron.ts (sus 2 accesos: `member_profiles`, `users`; y cómo usa `forEachActiveTenant`)
    - el-templo-api/src/jobs/expire-lost-leads.ts (su acceso a `users`)
    - el-templo-api/test/tenancy/con-04-crons-per-tenant.test.ts (los 8 tests que ya cubren el barrido por gimnasio de los 7 jobs — el idioma a respetar)
  </read_first>
  <action>
    Mismo tratamiento: los jobs ya reciben `ctx` por `forEachActiveTenant`; el trabajo es **usarlo**.
    `tenantWhere` inline en cada statement que nombre `users` o `member_profiles`, filtro en el `ON`
    de los joins, y un `tenantWhere` por **cada** tabla del módulo del statement.
    ⚠ `notification-cron.ts` decide **a quién se le manda un push**: una fuga acá manda notificaciones
    a socios de otro gimnasio, y no hay 404 que lo mitigue después.
    ⚠ `expire-lost-leads.ts` **escribe estado** sobre `users`: el update lleva `tenantWhere` o expira
    leads de otros gimnasios.
    Verificar que los 8 tests de `con-04-crons-per-tenant.test.ts` siguen verdes: cubren que el cuerpo
    del cron corre una vez por gimnasio ACTIVO, y son el contrato que estos cambios no pueden romper.
    D-02: no se migra ninguna otra tabla de estos jobs.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec tsc -p tsconfig.test-check.json --noEmit && pnpm exec vitest run --no-file-parallelism test/jobs test/tenancy/con-04-crons-per-tenant.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm lint:tenant --allowlist /tmp/allowlist-173-16.json` reporta CERO entradas para los 3 jobs del plan
    - Todo `update`/`delete` sobre `users` en `expire-lost-leads.ts` lleva `tenantWhere`
    - Todo log de estos jobs que mencione el gimnasio lo pasa como **campo estructurado**, nunca interpolado en el mensaje (verificado por grep de plantillas de string con `tenantId`)
    - `grep -rnE "tenantId!|tenantId\s*\?\?" el-templo-api/src/jobs/` no devuelve líneas
    - `pnpm exec vitest run --no-file-parallelism test/tenancy/con-04-crons-per-tenant.test.ts` sale con código 0 (los 8 tests)
  </acceptance_criteria>
  <done>Ningún cron puede notificar, expirar ni mover a un socio de otro gimnasio.</done>
</task>

<task type="auto">
  <name>Task 3: Test SC3 — el cron con sedes de los dos gimnasios sobre la mesa</name>
  <files>el-templo-api/test/tenancy/ancla-cron-reassign.test.ts</files>
  <read_first>
    - el-templo-api/test/tenancy/con-04-crons-per-tenant.test.ts (Tests 1-5 y el `describe("criterio 3 — el cuerpo del cron corre una vez por gimnasio ACTIVO")`: el idioma exacto)
    - el-templo-api/test/fixtures/second-tenant.ts (`TENANT_DOS = 90671`, sedes y socios que provee)
    - el-templo-api/src/jobs/reassign-multibranch.ts (post-tarea 1: la lógica de sede dominante por asistencia y el modo `dryRun`)
    - el-templo-api/test/tenancy/iso-03-finance-cajas.test.ts (:225-236 `tenantDeLaFila`)
  </read_first>
  <action>
    Crear `test/tenancy/ancla-cron-reassign.test.ts` con el ciclo de vida canónico.
    **Precondición obligatoria:** las sedes candidatas de los dos gimnasios comparten **país** — si no,
    el filtro de país del propio cron daría el aislamiento y el test pasaría sin ejercer la tenancy.
    Escenario del criterio 3 de la fase: un socio de El Templo con asistencia que haría "dominante" a
    una sede de `TENANT_DOS` (sembrar la asistencia de modo que la sede ajena gane si no hubiera
    filtro), y correr el cron.
    Aserciones:
    - **aislamiento:** después de la corrida, `campoDeLaFila(app, "users", socioId, "branch_id")` NO
      es la sede de `TENANT_DOS`; y `tenantDeLaFila(app, "users", socioId)` sigue siendo
      `TENANT_TEMPLO`. Leer la evidencia de la **base**, no del resumen del job.
    - **control positivo:** un socio de El Templo cuya sede dominante **propia** cambia → el cron
      **sí** lo mueve, y `branch_source` queda en `"auto"`. Sin este control, "no lo movió" sería
      trivialmente cierto si el cron estuviera roto para todos.
    - **el barrido no aborta:** con un socio problemático en el medio, el cron **termina** y recategoriza
      al resto; el resumen reporta al menos 1 salteado.
    - **simetría:** un socio de `TENANT_DOS` tampoco puede ser movido a una sede de El Templo.
    ⚠ El throw del sentinel (cuando `users` esté en strict) llega envuelto en `DrizzleQueryError.cause`.
    ⚠ **Cero `toBe(403)`**; describir la ausencia en castellano, sin escribir el número.
    ⚠ Este archivo nuevo re-baraja qué archivos comparten base por worker en CI: si aparece un
    `ER_ROW_IS_REFERENCED_2` en un archivo que la fase no tocó, **no es el sentinel** — mirar el
    patrón de limpieza.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec vitest run --no-file-parallelism test/tenancy/ancla-cron-reassign.test.ts test/tenancy/con-04-crons-per-tenant.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run --no-file-parallelism test/tenancy/ancla-cron-reassign.test.ts` sale con código 0
    - El archivo tiene los 4 casos: aislamiento, control positivo, barrido que no aborta, y simetría
    - Hay un `it` de precondición que afirma que las sedes candidatas comparten país
    - La evidencia se lee de la base (`tenantDeLaFila` / `campoDeLaFila`), no del resumen del job
    - `grep -c "toBe(403)\|toEqual(403)" el-templo-api/test/tenancy/ancla-cron-reassign.test.ts` devuelve `0`
    - Mutación de cierre: sacarle el `tenantWhere` a la query 3 (mapa de sedes) pone en rojo el caso de aislamiento; la salida se transcribe en el SUMMARY y la mutación se revierte (`git status --porcelain` vacío)
  </acceptance_criteria>
  <done>El criterio 3 de la fase (SC3) está probado con sedes de los dos gimnasios y la mutación demuestra que el test lo sostiene.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                                                             |
| -------------------------------------- | --------------------------------------------------------------------------------------- |
| proceso automático → `users.branch_id` | el cron reescribe el ancla sin nadie mirando, en horario nocturno                       |
| mapa global de sedes → decisión        | la query 3 lee todas las sedes de todos los gimnasios y de ahí sale la "sede dominante" |
| falla de un gimnasio → barrido         | abortar por un dato raro deja sin recategorizar a los demás gimnasios                   |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                | Disposition | Mitigation Plan                                                                                                  |
| ----------- | ---------------------- | ---------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------- |
| T-173-16-01 | Tampering              | `reassignMemberBranch` UPDATE de la sede | mitigate    | `ctx` PRIMERO en la firma + `resolveBranchDelGimnasio` antes del UPDATE + `tenantWhere` en el `where` (mina M10) |
| T-173-16-02 | Tampering              | query 3 (mapa de sedes sin filtro)       | mitigate    | `tenantWhere(schema.branches, ctx)` inline: la sede ajena deja de poder ser elegida como dominante               |
| T-173-16-03 | Information Disclosure | `notification-cron.ts`                   | mitigate    | `tenantWhere` inline: un push no puede salir a un socio de otro gimnasio, y no hay 404 que lo mitigue después    |
| T-173-16-04 | Tampering              | `expire-lost-leads.ts` (write)           | mitigate    | `tenantWhere` en el update: no puede expirar leads de otro gimnasio                                              |
| T-173-16-05 | Denial of Service      | barrido que aborta por un dato raro      | mitigate    | política saltear-y-seguir con log estructurado y conteo de salteados; test que exige que el cron termine         |
| T-173-16-06 | Repudiation            | log con `tenantId` interpolado           | mitigate    | `tenantId` como campo estructurado de Pino (ejemplar `:130-136`), verificado por grep de plantillas de string    |

</threat_model>

<verification>
- `pnpm exec tsc -p tsconfig.test-check.json --noEmit` verde.
- `pnpm lint:tenant --allowlist /tmp/allowlist-173-16.json` sin las 4 entradas de jobs.
- `pnpm exec vitest run --no-file-parallelism test/jobs test/tenancy` verde, incluidos los 8 de `con-04`.
- La mutación de la query 3 puso en rojo el caso de aislamiento y quedó revertida.
</verification>

<success_criteria>
El criterio 3 de la fase está cumplido y probado: el cron de recategorización nunca mueve a un
socio a una sede de otro gimnasio, aunque los datos hagan que esa sede parezca la dominante, y un
caso raro lo saltea con un log auditable en vez de tumbar el barrido de todos los gimnasios.
</success_criteria>

<output>
Create `.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-16-SUMMARY.md` when done
</output>
