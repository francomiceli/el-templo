---
phase: 173-adopci-n-2-members-guarda-de-consistencia-de-anclas
plan: 29
type: execute
wave: 14
depends_on: ["173-26", "173-27", "173-28"]
files_modified:
  - el-templo-api/test/tenancy/iso-03-cobertura-members.test.ts
autonomous: true
requirements: [ADO-02]
must_haves:
  truths:
    - "D-09: existe un gate fail-closed que exige caso de aislamiento para las 30 rutas `tenant-scoped` de los TRES prefijos del módulo, derivadas del manifiesto y no escritas a mano"
    - "El criterio de prefijo es `url === P || url.startsWith(P + '/')` — con `startsWith(P + '/')` solo, `GET` y `POST /api/admin/members` se perderían EN SILENCIO y el gate quedaría verde mintiendo"
    - "El gate es bidireccional (faltantes + fantasmas), borra los comentarios antes de buscar, prueba su propio motor con fixtures sintéticos y le pone gate a la lista de excepciones"
    - "`describe.skip` y `.todo` NO cuentan como cobertura, y el gate no toca la base"
  artifacts:
    - path: "el-templo-api/test/tenancy/iso-03-cobertura-members.test.ts"
      provides: "gate de cobertura de la batería de socios, con CASOS_BASELINE = 30 y los 3 prefijos"
      contains: "CASOS_BASELINE"
  key_links:
    - from: "el-templo-api/test/tenancy/iso-03-cobertura-members.test.ts"
      to: "el-templo-api/test/tenant-manifest.ts"
      via: "derivación de las rutas por categoría tenant-scoped y los 3 prefijos"
      pattern: "TENANT_MANIFEST"
---

<objective>
Paso 7 de la receta: el gate de cobertura fail-closed que impide que la batería envejezca en
silencio.

Purpose: sin gate, una ruta nueva de socios entra al manifiesto y **nadie** escribe su caso de
aislamiento. Con gate, la ruta nueva rompe CI hasta que alguien lo escriba o la exima con motivo y
dueño.

⚠ **La plantilla NO se copia cambiando una constante**, aunque el CONTEXT lo diga: medido son **tres**
cambios y el primero es una trampa. `PREFIJO_FINANCE = "/api/admin/finance/"` lleva **barra final**,
y `GET /api/admin/members` y `POST /api/admin/members` **no la tienen**: un `startsWith` con barra
**perdería esas dos rutas en silencio** y el gate quedaría verde midiendo 28 en vez de 30. Y son
**tres** prefijos, no uno.

Output: `iso-03-cobertura-members.test.ts`, con el rojo encendido a propósito en los dos sentidos y
revertido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-CONTEXT.md
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-PATTERNS.md
@.docs/saas-multitenancy/07-receta-adopcion.md

**Ruta de código:** worktree `/home/franco/projects/et-173`.

**Los 3 prefijos:** `/api/admin/members` (24) + `/api/admin/users` (5) + `/api/admin/leads` (1) =
**30** rutas `tenant-scoped`. Verificado que no hay colisiones (`/api/admin/member-*` no existe) y
que `/api/members/*` (app-facing) **no** matchea porque el ancla es `/api/admin/`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir el gate con los 3 prefijos y el criterio de barra corregido</name>
  <files>el-templo-api/test/tenancy/iso-03-cobertura-members.test.ts</files>
  <read_first>
    - el-templo-api/test/tenancy/iso-03-cobertura.test.ts (470 líneas, la plantilla completa: `PREFIJO_FINANCE` :118, `EXCEPCIONES_NOMBRADAS` :136, `CASOS_BASELINE` :172, `ARCHIVOS_BATERIA` :175, `sinComentarios` :198, `clavesDeLosDescribe` :218, y el filtro del manifiesto :239)
    - el-templo-api/test/tenant-manifest.ts (las 30 claves de los 3 prefijos)
    - los 3 archivos de la batería creados en 173-26, 173-27 y 173-28
    - .planning/phases/172-adopci-n-1-piloto-finance/172-20-SUMMARY.md (cómo el piloto encendió el rojo en los dos sentidos)
  </read_first>
  <action>
    Copiar la plantilla y hacer los **tres** cambios medidos:
    1. **El criterio de prefijo.** No `url.startsWith(P)` a secas (matchearía `/api/admin/members-algo`
       si existiera) ni `url.startsWith(P + "/")` (perdería `GET` y `POST /api/admin/members`). El
       criterio es **`url === P || url.startsWith(P + "/")`**. Escribir en un comentario de bloque por
       qué, con el ejemplo concreto de las dos rutas que se perderían: es exactamente el modo de falla
       que este gate existe para no tener.
    2. **Tres prefijos**, no uno: `/api/admin/members`, `/api/admin/users`, `/api/admin/leads`.
    3. **`CASOS_BASELINE = 30`** y `ARCHIVOS_BATERIA` con los 3 archivos nuevos.
    Las **4 piezas no obvias que sí se copian tal cual**:
    - **Chequeo bidireccional**: `faltantes` (ruta del manifiesto sin caso) **y** `fantasmas` (caso que
      apunta a una ruta inexistente). Un typo enciende los dos a la vez.
    - **Borrar los comentarios antes de buscar** (`sinComentarios`): los archivos de la batería listan
      sus rutas en sus propios docblocks; sin esto el gate está **verde midiendo prosa** aunque no
      exista un solo `it`.
    - **Probar el motor con fixtures sintéticos** (4 `it`, ~2 ms): si el borrador de comentarios se
      rompe, los tests reales siguen verdes con la batería vacía.
    - **Gate sobre la lista de excepciones**: toda entrada de `EXCEPCIONES_NOMBRADAS` lleva su motivo
      escrito y su dueño, y un `it` le exige seguir existiendo en el manifiesto, seguir **fuera** del
      prefijo del módulo y seguir con un motivo utilizable (≥ 20 chars, sin marcador de trabajo
      pendiente). Sin eso, la lista es la salida de una línea para apagar el rojo de `faltantes`.
    `describe.skip` / `.todo` **no cuentan** como cobertura. El gate **no toca la base**: lee texto con
    `readFileSync` y corre en ~10 ms (importar los archivos de la batería los **ejecutaría**, ~250 s
    contra MySQL, y un gate que cuesta eso deja de correr).
    ⚠ Si `CASOS_BASELINE` alguna vez baja, la baja tiene que quedar **contabilizada** por una aserción
    nueva que explique cada unidad perdida: bajar el número pelado está prohibido.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec vitest run --no-file-parallelism test/tenancy/iso-03-cobertura-members.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run --no-file-parallelism test/tenancy/iso-03-cobertura-members.test.ts` sale con código 0 en menos de 5 s (no toca la base)
    - El filtro del manifiesto usa `url === P || url.startsWith(P + "/")` y hay un comentario de bloque que explica el modo de falla de la barra final
    - `grep -c "CASOS_BASELINE = 30" el-templo-api/test/tenancy/iso-03-cobertura-members.test.ts` devuelve `1`
    - El archivo declara los 3 prefijos y `ARCHIVOS_BATERIA` con los 3 archivos de la batería
    - Existen los 4 `it` de motor sintético y el `it` que le pone gate a `EXCEPCIONES_NOMBRADAS`
    - El gate cuenta `describe.skip` y `.todo` como NO cubiertos (probado con un fixture sintético)
    - El archivo no importa ninguno de los 3 archivos de la batería (los lee con `readFileSync`)
  </acceptance_criteria>
  <done>El gate existe, mide las 30 rutas y no puede quedar verde midiendo docblocks.</done>
</task>

<task type="auto">
  <name>Task 2: Encender el rojo en los dos sentidos y revertirlo</name>
  <files>el-templo-api/test/tenancy/iso-03-cobertura-members.test.ts</files>
  <read_first>
    - el-templo-api/test/tenancy/iso-03-cobertura-members.test.ts (la tarea 1)
    - .planning/phases/172-adopci-n-1-piloto-finance/172-20-SUMMARY.md (cómo se transcribió la evidencia del rojo en el piloto)
    - .docs/saas-multitenancy/07-receta-adopcion.md (§4(d): una auditoría que no se probó contra un caso conocido-malo es un placebo)
  </read_first>
  <action>
    Un gate que nunca se vio en rojo no es un gate. Encenderlo **en los dos sentidos**, sin commitear:
    1. **`faltantes`:** comentar o renombrar un `describe` de uno de los 3 archivos de la batería
       (rompiendo la clave del manifiesto en su nombre) → el gate tiene que fallar nombrando esa ruta.
       Transcribir la salida real.
    2. **`fantasmas`:** agregar un `describe` con una clave que **no existe** en el manifiesto (por
       ejemplo `GET /api/admin/members/inventado`) → el gate tiene que fallar nombrándola.
       Transcribir la salida real.
    3. **Barra final:** cambiar temporalmente el criterio a `url.startsWith(P + "/")` y verificar que
       `CASOS_BASELINE` deja de coincidir (las 2 rutas sin barra se pierden). Transcribir el número que
       da (debería ser 28). Es la prueba de que la trampa está cubierta.
    4. **Excepción tramposa:** agregar a `EXCEPCIONES_NOMBRADAS` una entrada con motivo vacío o con un
       marcador de trabajo pendiente → el `it` del gate de excepciones tiene que fallar.
    Revertir **las cuatro** y verificar `git status --porcelain` vacío salvo el archivo del gate.
    Cerrar corriendo `test/tenancy` entero.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec vitest run --no-file-parallelism test/tenancy && git -C /home/franco/projects/et-173 status --porcelain</automated>
  </verify>
  <acceptance_criteria>
    - El SUMMARY transcribe las **cuatro** salidas de rojo provocado, cada una nombrando lo que corresponde
    - La prueba de la barra final transcribe el conteo que da el criterio incorrecto (28) frente al correcto (30)
    - Después de revertir, `pnpm exec vitest run --no-file-parallelism test/tenancy` sale con código 0
    - `git -C /home/franco/projects/et-173 status --porcelain` no muestra ningún archivo de la batería modificado
    - El SUMMARY declara explícitamente cuántas rutas del manifiesto quedan sin caso: tiene que ser **0** para los 3 prefijos
  </acceptance_criteria>
  <done>El gate está probado contra los cuatro modos de falla que existe para prevenir, y quedó revertido y verde.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                                              |
| ----------------------------- | ---------------------------------------------------------------------------------------- |
| ruta nueva → cobertura        | una ruta que entra al manifiesto sin caso de aislamiento envejece la batería en silencio |
| docblock de la batería → gate | un gate que lee el fuente crudo da verde midiendo prosa                                  |
| lista de excepciones → escape | sin gate propio, la lista es la salida de una línea para apagar el rojo                  |
| criterio de prefijo → conteo  | una barra final de más pierde 2 rutas sin que nada falle                                 |

## STRIDE Threat Register

| Threat ID   | Category          | Component                       | Disposition | Mitigation Plan                                                                                             |
| ----------- | ----------------- | ------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------- |
| T-173-29-01 | Repudiation       | criterio de prefijo con barra   | mitigate    | `url === P \|\| url.startsWith(P + "/")`, probado encendiendo el rojo con el criterio incorrecto (28 vs 30) |
| T-173-29-02 | Repudiation       | gate verde midiendo docblocks   | mitigate    | `sinComentarios` antes de buscar, más 4 `it` de motor sintético que prueban el borrador                     |
| T-173-29-03 | Repudiation       | cobertura desincronizada        | mitigate    | chequeo bidireccional (`faltantes` + `fantasmas`), con el rojo encendido a propósito en los dos sentidos    |
| T-173-29-04 | Repudiation       | excepciones sin motivo ni dueño | mitigate    | `it` que exige existir en el manifiesto, estar fuera del prefijo y tener motivo utilizable ≥ 20 chars       |
| T-173-29-05 | Repudiation       | `describe.skip` como cobertura  | mitigate    | `clavesDeLosDescribe` ignora `.skip`/`.todo`, probado con fixture sintético                                 |
| T-173-29-06 | Denial of Service | gate que cuesta 250 s           | mitigate    | lee texto con `readFileSync`, no importa los archivos de la batería: un gate caro deja de correr            |

</threat_model>

<verification>
- El gate verde en < 5 s y sin tocar la base.
- Las 4 salidas de rojo provocado transcritas en el SUMMARY.
- 0 rutas sin caso para los 3 prefijos.
- `git status --porcelain` sin archivos de la batería modificados.
</verification>

<success_criteria>
Una ruta nueva de socios, staff o leads no puede entrar al sistema sin caso de aislamiento sin
romper CI, y el gate está probado contra los cuatro modos de falla que lo volverían un placebo —
incluida la trampa de la barra final que habría dejado 2 rutas afuera en silencio.
</success_criteria>

<output>
Create `.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-29-SUMMARY.md` when done
</output>
