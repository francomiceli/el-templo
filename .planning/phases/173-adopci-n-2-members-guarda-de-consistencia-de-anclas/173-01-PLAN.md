---
phase: 173-adopci-n-2-members-guarda-de-consistencia-de-anclas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/tsconfig.test-check.json
autonomous: false
requirements: [ADO-02, ADO-07]
must_haves:
  truths:
    - "D-16: el backmerge origin/master → origin/staging está hecho y pusheado ANTES de crear el worktree, con los 4 commits de master (6724f46e, 0661f987, a36b759d, f77e05b4) resueltos en staging"
    - "D-17: existe el worktree /home/franco/projects/et-173 en feat/173-adopcion-members, creado desde origin/staging recién fetcheado, con node_modules propio y SIN upstream"
    - "La base arranca verde: tsc --noEmit exit 0 y pnpm lint:tenant exit 0 con DISCREPANCIAS 0"
    - "Los baselines quedan registrados en el SUMMARY: 450 entradas de allowlist, manifiesto 372/224, TENANT_STRICT_MODULES con 1 entrada (finance), y la lista exacta de exenciones tenant-safe heredadas"
    - "tsconfig.test-check.json existe, está commiteado y typechequea test/ de verdad (no devuelve un TS2554: 0 falso)"
  artifacts:
    - path: "el-templo-api/tsconfig.test-check.json"
      provides: "typecheck real de test/ con rootDir '.' — sin esto los call sites de test desactualizados no dan rojo hasta que el test corre"
      contains: '"rootDir": "."'
    - path: "/home/franco/projects/et-173/el-templo-api/.env"
      provides: "credenciales locales de MySQL para typecheck y tests dirigidos (no viaja por git)"
  key_links:
    - from: "/home/franco/projects/et-173"
      to: "origin/staging"
      via: "git worktree add -b feat/173-adopcion-members"
      pattern: "feat/173-adopcion-members"
---

<objective>
Montar la base de ejecución de la fase 173, cerrar el gate de secuencia D-16 y crear la
herramienta que el piloto usó sin commitear.

Purpose: master tiene 4 commits que staging no, y uno de ellos (`f77e05b4`, gemelo de
`e1952606`) toca `members/routes.ts` y `members/schemas.ts` — exactamente los archivos que
esta fase reescribe. Partir de antes del backmerge garantiza conflicto sobre las líneas en
juego (mismo razonamiento del gate CR-CAJA del piloto). Además `tsconfig.test-check.json`
**no existe en el repo**: el piloto lo creó en su worktree y nunca lo commiteó, así que hoy
`tsc --noEmit` devuelve un `TS2554: 0` falso sobre `test/`. Tiene que existir ANTES de la
primera ola que cambie firmas, o los ~200 call sites de test rotos no se ven hasta que el
test corre.

Output: worktree `/home/franco/projects/et-173` sobre `feat/173-adopcion-members`, baselines
registrados y `tsconfig.test-check.json` commiteado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-CONTEXT.md
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-PATTERNS.md
@.claude/skills/el-templo-change-control/SKILL.md

**Regla de rutas para TODA la fase 173:** las rutas de código (`el-templo-api/...`) de todos
los planes son relativas al worktree `/home/franco/projects/et-173`. Los documentos de
planning (`.planning/...`, incluidos los SUMMARY) se leen y escriben en el checkout principal
`/home/franco/projects/el-templo`. **Nunca** se edita código en el checkout principal, que es
compartido con otras sesiones.

**Destino del merge de la fase: `origin/staging`, NO master.** Franco fijó que 173/174/175 se
resuelven en staging y viajan juntas a master (D-17).
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Gate D-16 — backmerge origin/master → origin/staging antes de crear el worktree</name>
  <what-built>Nada de código todavía. Este es el gate de secuencia que fijó D-16.</what-built>
  <how-to-verify>
    1. En `/home/franco/projects/el-templo`: `git fetch origin` y
       `git rev-list --left-right --count origin/staging...origin/master`.
       Al mapear daba `60 4`: staging tiene 60 commits que master no, master tiene 4 que
       staging no (`6724f46e`, `0661f987`, `a36b759d`, `f77e05b4`).
    2. Revisar los 4 commits antes de mergear:
       - `f77e05b4` (asignar referidor desde la ficha) tiene **gemelo cherry-pickeado** en
         staging (`e1952606`): el merge va a ver el mismo cambio por dos caminos sobre
         `members/routes.ts` y `members/schemas.ts`. Resolver quedándose con UNA sola copia
         del cambio, sin duplicar rutas ni schemas.
       - `a36b759d` ("acotar al gimnasio el chequeo de primer pago") toca tenancy de
         referidos: leerlo contra `173-PATTERNS.md` §5 y confirmar que no colisiona con las
         deudas D-13/D-15 antes de asumir que entra limpio.
       - `6724f46e` (replay de refresh) y `0661f987` (asignación retroactiva de referidor)
         son código nuevo.
    3. Hacer el merge en un worktree/rama de trabajo desde `origin/staging`, correr
       `pnpm exec tsc --noEmit` en `el-templo-api` (precedente: el cherry-pick staging→master
       dejó `assertTenant` sin importar) y pedir OK explícito **antes de pushear a staging**.
    4. Confirmar `git rev-list --left-right --count origin/staging...origin/master` = `N 0`.
  </how-to-verify>
  <action>
    Presentar a Franco el `git log --oneline` de los 4 commits, el diff de `a36b759d`, el plan
    de resolución del conflicto de `f77e05b4`/`e1952606` y la salida del typecheck post-merge.
    Pedir OK explícito para pushear a `origin/staging` (regla del repo: siempre preguntar antes
    de pushear). NO crear el worktree de la fase hasta que el conteo dé `N 0`.
    Registrar en el SUMMARY el SHA del merge y cómo se resolvió el gemelo.
  </action>
  <done>`git rev-list --left-right --count origin/staging...origin/master` devuelve `N 0` y Franco confirmó el push.</done>
  <resume-signal>Escribí "backmerge hecho" para seguir, o "frenar" para parar la fase</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Crear el worktree et-173 y registrar los baselines</name>
  <files>ninguno versionado (crea /home/franco/projects/et-173)</files>
  <read_first>
    - .claude/skills/el-templo-change-control/SKILL.md (worktree por fase, staging-first, git add explícito por ruta)
    - .planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-PATTERNS.md (§0.3 "Baselines a registrar ANTES de tocar una línea")
    - .planning/phases/172-adopci-n-1-piloto-finance/172-01-PLAN.md (el gemelo del piloto, para copiar la forma)
  </read_first>
  <action>
    Desde `/home/franco/projects/el-templo`: `git fetch origin`, después
    `git worktree add /home/franco/projects/et-173 -b feat/173-adopcion-members origin/staging`.
    Sacarle el upstream a la rama (`git branch --unset-upstream` dentro del worktree): en este
    repo un `git push` sin argumentos a master es un deploy a producción.
    Copiar los archivos de entorno gitignoreados desde un worktree hermano vivo
    (`/home/franco/projects/et-172`): `el-templo-api/.env` y, si existe, `.env.development`.
    Correr `pnpm install --frozen-lockfile` (PROHIBIDO agregar o actualizar dependencias en
    toda la fase 173).
    Registrar en el SUMMARY los 5 baselines de §0.3 de PATTERNS, medidos, no supuestos:
    (a) entradas de `tenant-lint-allowlist.json` = **450**;
    (b) manifiesto = **372 rutas / 224 tenant-scoped / 141 templo-module / 7 global**
        (⚠ el header del archivo dice "223 tenant-scoped" y quedó **stale** al entrar
        `e1952606` — anotarlo, lo corrige el plan 173-02);
    (c) `TENANT_STRICT_MODULES` = 1 entrada (`finance`, 6 tablas);
    (d) la **lista exacta** de archivos de `src/` + `scripts/` con exenciones
        `/* tenant-safe:` — si al cerrar la fase hay una NUEVA dentro de
        `src/modules/members/`, alguien tomó un atajo;
    (e) las **90** entradas de allowlist a borrar (79 por tabla + 11 por archivo bajo
        `src/modules/members/`), con la lista completa.
    NO tocar el checkout principal ni ninguna otra rama.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec tsc --noEmit && pnpm lint:tenant && node -e "const a=require('./tenant-lint-allowlist.json');console.log('entradas',a.entries.length)"</automated>
  </verify>
  <acceptance_criteria>
    - `git -C /home/franco/projects/et-173 rev-parse --abbrev-ref HEAD` imprime `feat/173-adopcion-members`
    - `git -C /home/franco/projects/et-173 rev-parse --abbrev-ref --symbolic-full-name @{u}` falla (no hay upstream)
    - `git -C /home/franco/projects/et-173 merge-base --is-ancestor origin/staging HEAD` sale con código 0
    - Existe `/home/franco/projects/et-173/el-templo-api/.env` con al menos una línea que empieza con `DB_` o `DATABASE_`
    - Existen `el-templo-api/src/modules/shared/tenant.ts`, `src/db/sentinel/install.ts`, `src/db/tenant-tables.ts`, `test/tenant-manifest.ts` y `test/fixtures/second-tenant.ts` en el worktree
    - `node -e "const a=require('./tenant-lint-allowlist.json');console.log(a.entries.length)"` imprime `450`
    - `node -e "const a=require('./tenant-lint-allowlist.json');const T=['users','member_profiles','user_status_history','user_branches','member_notes','user_sepa_details','member_logins','audit_log'];const s=new Set();a.entries.forEach((e,i)=>{if(T.includes(e.table)||/src\/modules\/members\//.test(e.file))s.add(i)});console.log(s.size)"` imprime `90`
    - `pnpm exec tsc --noEmit` sale con código 0 y `pnpm lint:tenant` sale con código 0
    - `git -C /home/franco/projects/el-templo status --porcelain` no muestra archivos nuevos bajo `el-templo-api/`
  </acceptance_criteria>
  <done>Worktree operativo sin upstream, baselines de §0.3 registrados en el SUMMARY con sus valores medidos, checkout principal intacto.</done>
</task>

<task type="auto">
  <name>Task 3: Crear y commitear tsconfig.test-check.json (drift 3 — no existe en el repo)</name>
  <files>el-templo-api/tsconfig.test-check.json</files>
  <read_first>
    - el-templo-api/tsconfig.json (el base: `rootDir: "./src"`, `include: ["src/**/*"]`, module NodeNext, strict)
    - .planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-PATTERNS.md (§7.3, por qué el rootDir heredado devuelve un TS2554: 0 falso)
  </read_first>
  <action>
    Crear `el-templo-api/tsconfig.test-check.json` que extienda `./tsconfig.json` y sobrescriba
    `rootDir` a `"."`, `noEmit` a `true`, e `include` a `["src/**/*", "test/**/*", "scripts/**/*"]`.
    Con el `rootDir` heredado (`./src`) el compilador tira `TS6059` por cada archivo de `test/`,
    deja de chequearlos y devuelve un `TS2554: 0` falso — ese es exactamente el modo de falla que
    este archivo existe para matar.
    Probar que **muerde** antes de confiar en él (regla del doc 07 §4(d): una auditoría que no se
    probó contra un caso conocido-malo es un placebo): romper a mano un call site de test
    (agregar un argumento de más a una llamada tipada en cualquier archivo de `test/`), correr
    `pnpm exec tsc -p tsconfig.test-check.json --noEmit`, verificar que sale con código ≠ 0 y con
    un `TS2554` real, revertir la rotura y verificar `git status --porcelain` limpio salvo el
    tsconfig nuevo. Transcribir la salida del rojo provocado en el SUMMARY.
    Commit: `chore(173-01): tsconfig.test-check.json para typechequear test/ de verdad`.
    Agregar al archivo un comentario JSON en un campo `"//"` explicando por qué `rootDir` es `"."`.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec tsc -p tsconfig.test-check.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f /home/franco/projects/et-173/el-templo-api/tsconfig.test-check.json` sale 0
    - `node -e "const c=require('./tsconfig.test-check.json');if(c.compilerOptions.rootDir!=='.')process.exit(1)"` sale 0
    - `pnpm exec tsc -p tsconfig.test-check.json --noEmit --listFiles | grep -c '/test/'` devuelve un número > 100
    - `pnpm exec tsc -p tsconfig.test-check.json --noEmit` sale con código 0 sobre la rama limpia
    - El SUMMARY transcribe la salida del `TS2554` real provocado a mano y su reversión
    - `git -C /home/franco/projects/et-173 status --porcelain` está vacío después del commit
  </acceptance_criteria>
  <done>`tsconfig.test-check.json` commiteado, probado contra un rojo provocado y revertido. Todos los planes siguientes que cambien firmas lo usan como verificación.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                                                      |
| -------------------------------------- | -------------------------------------------------------------------------------- |
| checkout compartido → worktree de fase | otra sesión puede estar editando el checkout principal con cambios sin commitear |
| rama local → `origin/staging`          | staging se despliega solo: un push equivocado publica código a medio migrar      |
| rama local → `origin/master`           | master es **producción**: un `git push` sin argumentos deploya                   |
| lockfile → node_modules                | `pnpm install` materializa código de terceros en el worktree                     |

## STRIDE Threat Register

| Threat ID   | Category    | Component                                       | Disposition | Mitigation Plan                                                                                                                                                                                                    |
| ----------- | ----------- | ----------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-173-01-01 | Tampering   | checkout principal `/home/franco/.../el-templo` | mitigate    | todo el código se edita SOLO en `/home/franco/projects/et-173`; criterio de aceptación verifica `git status --porcelain` limpio en el principal                                                                    |
| T-173-01-02 | Tampering   | base de la rama                                 | mitigate    | la rama se crea desde `origin/staging` recién fetcheado y se verifica con `merge-base --is-ancestor`; una base pre-172 borraría las 5 capas                                                                        |
| T-173-01-03 | Elevation   | `git push` sin argumentos                       | mitigate    | `git branch --unset-upstream` en el worktree; criterio de aceptación verifica que `@{u}` falla                                                                                                                     |
| T-173-01-04 | Repudiation | typecheck de `test/`                            | mitigate    | `tsconfig.test-check.json` probado contra un `TS2554` provocado a mano antes de confiar en su verde (doc 07 §4(d))                                                                                                 |
| T-173-01-SC | Tampering   | pnpm install                                    | mitigate    | solo `pnpm install --frozen-lockfile` del lockfile existente; CERO dependencias nuevas o actualizadas en toda la fase 173. Si apareciera una: gate de legitimidad + checkpoint humano bloqueante antes de instalar |

</threat_model>

<verification>
- `git rev-list --left-right --count origin/staging...origin/master` devuelve `N 0` (D-16 cerrado).
- Worktree en `feat/173-adopcion-members` desde `origin/staging`, sin upstream, con `.env`.
- `pnpm exec tsc --noEmit` y `pnpm lint:tenant` verdes ANTES de tocar una línea de la fase.
- `tsconfig.test-check.json` commiteado y probado contra un rojo provocado.
- Los 5 baselines de PATTERNS §0.3 escritos en el SUMMARY con valores medidos.
</verification>

<success_criteria>
La fase puede ejecutarse: la base de código tiene la 172 adentro y los 4 commits de master
resueltos, el worktree arranca verde y sin upstream, y existe la herramienta que hace que un
call site de test roto dé rojo en el typecheck en vez de esconderse hasta la corrida.
</success_criteria>

<output>
Create `.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-01-SUMMARY.md` when done
</output>
