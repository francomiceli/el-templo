---
phase: 173-adopci-n-2-members-guarda-de-consistencia-de-anclas
plan: 10
type: execute
wave: 2
depends_on: ["173-01"]
files_modified:
  - el-templo-api/src/db/import-members.ts
  - el-templo-api/src/db/import-fecha-ingreso.ts
  - el-templo-api/src/db/import-turnos.ts
  - el-templo-api/src/db/import-vigentes.ts
  - el-templo-api/src/db/seed-staging.ts
  - el-templo-api/src/scripts/backfill-referral-codes.ts
autonomous: true
requirements: [ADO-02]
must_haves:
  truths:
    - "D-03: los 6 scripts CLI que tocan `users` reciben retrofit `requireTenant` más helpers, todos por igual"
    - "D-03: CERO exenciones `tenant-safe` nuevas por esta vía — el baseline de exenciones del plan 173-01 es lo que lo verifica"
    - "Un script corrido sin `--tenant` corta con exit 2 ANTES de escribir una fila, y un gimnasio inexistente también"
  artifacts:
    - path: "el-templo-api/src/db/import-members.ts"
      provides: "importador de socios que exige el gimnasio explícito y estampa `tenantValues`"
      contains: "requireTenant"
    - path: "el-templo-api/src/db/seed-staging.ts"
      provides: "seed de staging que siembra en un gimnasio elegido, no en el DEFAULT 1"
      contains: "requireTenant"
  key_links:
    - from: "los 6 scripts CLI"
      to: "el-templo-api/src/db/scripts/require-tenant.ts"
      via: "requireTenant(queryFnFromConnection(connection))"
      pattern: "requireTenant"
---

<objective>
Retrofit del contrato CLI `requireTenant` en los 6 scripts que escriben o leen `users`
(D-03), con la misma receta que el piloto aplicó a `backfill-historical-payments.ts`.

Purpose: un script de importación es la vía más rápida de crear filas cross-tenant, y no tiene
`request.scope` de donde sacar el gimnasio. Sin el retrofit, con `users` en strict (D-01) estos
scripts revientan en runtime; con una exención `tenant-safe` "para que anden", el operador queda
escribiendo en el `DEFAULT 1` para siempre. D-03 es explícito: **cero exenciones nuevas por esta
vía**, todos por igual.

Output: los 6 scripts exigiendo `--tenant`, con `tenantValues` en las escrituras y `tenantWhere`
en las lecturas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-CONTEXT.md
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-PATTERNS.md
@.docs/saas-multitenancy/07-receta-adopcion.md

**Ruta de código:** worktree `/home/franco/projects/et-173`.
**Allowlist de trabajo:** `/tmp/allowlist-173-10.json`.

**Entradas objetivo (7):** `import-members.ts` → `member_notes`, `users`;
`import-fecha-ingreso.ts` → `users`; `import-turnos.ts` → `users`; `import-vigentes.ts` →
`users`; `seed-staging.ts` → `users`; `backfill-referral-codes.ts` → `users`.

**`console.*` está permitido en scripts CLI** — excepción explícita a la regla de logging del
CLAUDE.md, con precedente en `require-tenant.ts`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retrofit de los 4 importadores y del seed de staging</name>
  <files>el-templo-api/src/db/import-members.ts, el-templo-api/src/db/import-fecha-ingreso.ts, el-templo-api/src/db/import-turnos.ts, el-templo-api/src/db/import-vigentes.ts, el-templo-api/src/db/seed-staging.ts</files>
  <read_first>
    - el-templo-api/src/db/scripts/require-tenant.ts (el docblock de cabecera con el contrato copiable: `createSingleConnection`, `requireTenant(queryFnFromConnection(connection))`, `failTenantArg`, los exit codes)
    - el-templo-api/scripts/seed-onboarding-aura.ts (el ejemplar completo)
    - el-templo-api/src/scripts/backfill-historical-payments.ts (el retrofit que hizo el piloto, sobre un script del mismo estilo)
    - el-templo-api/src/db/import-members.ts (el script más grande del grupo: 2 tablas)
    - .claude/skills/el-templo-db-migrations/SKILL.md
  </read_first>
  <action>
    Aplicar el contrato de `require-tenant.ts` a los 5 scripts, sin reinventar nada de lo que el
    helper ya trae:
    - exit **2** = error de USO (flag ausente, id inválido, gimnasio inexistente); exit **1** =
      error de datos; exit **0** = OK;
    - el helper **verifica contra la DB que el gimnasio exista**, así que un typo corta **antes** de
      escribir una sola fila;
    - un gimnasio no `active` **avisa por stderr y sigue** (D-07 de la 169: el CLI es tooling de
      operador). Que nadie lo "unifique" después con el criterio de los crons: **la diferencia es la
      decisión, no un olvido** — dejarlo escrito en un comentario;
    - `main().catch((err: unknown) => failTenantArg(err, "<nombre-del-script>"))`.
    Después del `requireTenant`, **recién ahí** las queries: `tenantValues(ctx, { ... })` en todo
    insert (el `tenantId` estampado DESPUÉS del spread) y `tenantWhere` inline en toda lectura,
    update o delete. `import-members.ts` toca **dos** tablas (`users` y `member_notes`): las dos
    llevan lo suyo.
    `seed-staging.ts` es el más sensible: hoy siembra al `DEFAULT 1`. Con el retrofit, el operador
    elige el gimnasio y el seed nace donde se pidió.
    ⚠ **CERO exenciones `tenant-safe` nuevas** (D-03). Si algún statement parece necesitar una,
    es señal de que falta el `ctx`, no de que el statement sea global.
    Actualizar el `.env.example` si algún script gana una variable nueva (no debería).
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec tsc -p tsconfig.test-check.json --noEmit && pnpm exec tsx src/db/import-fecha-ingreso.ts; test $? -eq 2</automated>
  </verify>
  <acceptance_criteria>
    - Los 5 scripts importan y llaman `requireTenant` antes de la primera query (verificado leyendo cada archivo)
    - Correr cada script **sin** `--tenant` sale con código **2** y no escribe ninguna fila (verificado corriendo los 5 y contando filas antes/después)
    - Correr cada script con un id de gimnasio inexistente sale con código **2**
    - `grep -c "tenant-safe" el-templo-api/src/db/import-*.ts el-templo-api/src/db/seed-staging.ts` devuelve `0`
    - `grep -c "tenantValues" el-templo-api/src/db/import-members.ts` devuelve al menos `2` (las dos tablas)
    - `pnpm lint:tenant --allowlist /tmp/allowlist-173-10.json` no reporta las 6 entradas de estos 5 archivos
    - `pnpm exec tsc -p tsconfig.test-check.json --noEmit` sale con código 0
  </acceptance_criteria>
  <done>Ningún importador ni seed puede escribir un socio sin que el operador diga en qué gimnasio.</done>
</task>

<task type="auto">
  <name>Task 2: Retrofit de backfill-referral-codes.ts y verificación del baseline de exenciones</name>
  <files>el-templo-api/src/scripts/backfill-referral-codes.ts</files>
  <read_first>
    - el-templo-api/src/scripts/backfill-referral-codes.ts (el script completo)
    - el-templo-api/src/scripts/backfill-historical-payments.ts (el gemelo ya retrofiteado por el piloto — copiar su forma tal cual)
    - .planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-01-SUMMARY.md (el baseline de exenciones `tenant-safe` heredadas)
  </read_first>
  <action>
    Mismo retrofit. Es un backfill: recorre socios sin código de referido y les escribe uno.
    El barrido lee `users` con `tenantWhere(schema.users, ctx)` inline y el update también.
    ⚠ Un backfill que corre **por gimnasio** es semánticamente distinto de uno que corre sobre todo:
    dejar escrito en el docblock que el operador tiene que correrlo una vez por gimnasio, y que ese
    es el comportamiento buscado (no un `forEachActiveTenant`, que es el criterio de los **crons**).
    Cerrar el plan verificando el **baseline de exenciones** del plan 173-01: comparar la lista de
    archivos de `src/` + `scripts/` con `/* tenant-safe:` contra la registrada al arrancar. Tiene
    que ser **idéntica**. Si apareció una nueva, alguien tomó un atajo y hay que revertirla.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec tsc -p tsconfig.test-check.json --noEmit && grep -rl "tenant-safe" src/ scripts/ | sort</automated>
  </verify>
  <acceptance_criteria>
    - `backfill-referral-codes.ts` llama `requireTenant` antes de la primera query y sale con código 2 sin `--tenant`
    - `grep -c "tenantWhere" el-templo-api/src/scripts/backfill-referral-codes.ts` devuelve al menos `1`
    - `grep -rl "tenant-safe" src/ scripts/ | sort` devuelve **exactamente** la misma lista que el baseline del plan 173-01 (diff vacío, transcrito en el SUMMARY)
    - `pnpm lint:tenant --allowlist /tmp/allowlist-173-10.json` reporta CERO entradas para los 6 scripts del plan
    - `pnpm exec tsc -p tsconfig.test-check.json --noEmit` sale con código 0
  </acceptance_criteria>
  <done>Las 7 entradas de scripts están fuera de la allowlist de trabajo y no se ganó ni una exención.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                                                |
| ------------------------------ | ------------------------------------------------------------------------------------------ |
| CSV / archivo de entrada → DB  | los importadores escriben filas de socios desde archivos que arma una persona              |
| operador → gimnasio de destino | sin flag explícito, todo cae al `DEFAULT 1` y se descubre meses después                    |
| script CLI → sentinel          | los scripts no pasan por `request.scope`; una exención acá apaga el vigilante para siempre |

## STRIDE Threat Register

| Threat ID   | Category    | Component                          | Disposition | Mitigation Plan                                                                                                     |
| ----------- | ----------- | ---------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------- |
| T-173-10-01 | Tampering   | importadores de socios             | mitigate    | `requireTenant` con exit 2 antes de la primera escritura; `tenantValues` con el tenant estampado después del spread |
| T-173-10-02 | Tampering   | `seed-staging.ts`                  | mitigate    | el seed deja de caer al `DEFAULT 1`: el gimnasio se elige por flag y se verifica contra la DB                       |
| T-173-10-03 | Repudiation | exenciones `tenant-safe` nuevas    | mitigate    | D-03: cero exenciones por esta vía; criterio de aceptación que compara la lista contra el baseline del plan 173-01  |
| T-173-10-04 | Tampering   | backfill sobre todos los gimnasios | mitigate    | el backfill corre por gimnasio con `tenantWhere` inline; el docblock explica por qué NO usa `forEachActiveTenant`   |

</threat_model>

<verification>
- Los 6 scripts salen con código 2 sin `--tenant` y con un gimnasio inexistente.
- `pnpm lint:tenant --allowlist /tmp/allowlist-173-10.json` sin las 7 entradas.
- La lista de exenciones `tenant-safe` es idéntica al baseline del plan 173-01.
- `pnpm exec tsc -p tsconfig.test-check.json --noEmit` verde.
</verification>

<success_criteria>
Ninguna herramienta de operador puede crear ni modificar un socio sin decir explícitamente en qué
gimnasio, y el retrofit no compró velocidad apagando el vigilante.
</success_criteria>

<output>
Create `.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-10-SUMMARY.md` when done
</output>
