---
phase: 173-adopci-n-2-members-guarda-de-consistencia-de-anclas
plan: 28
type: execute
wave: 13
depends_on: ["173-26"]
files_modified:
  - el-templo-api/test/tenancy/iso-03-members-altas-y-staff.test.ts
autonomous: true
requirements: [ADO-02, ADO-07]
must_haves:
  truths:
    - "D-09: las 11 rutas de alta, edición y baja de socios, el PATCH de leads y las 5 de `/api/admin/users` tienen caso de aislamiento + control positivo"
    - "ADO-07: crear o editar un socio o un staff con una sede de OTRO gimnasio se rechaza, y la fila no queda escrita — probado leyendo la base"
    - "D-06: cero `403` esperados; el rechazo por sede ajena es indistinguible de 'sede inexistente'"
    - "El alta con recursos PROPIOS del gimnasio 2 funciona de punta a punta (control positivo): es lo que el ancla de la 172 no podía escribir"
  artifacts:
    - path: "el-templo-api/test/tenancy/iso-03-members-altas-y-staff.test.ts"
      provides: "11 describes de escritura, incluidos los casos de sede cruzada de ADO-07"
      min_lines: 800
  key_links:
    - from: "el-templo-api/test/tenancy/iso-03-members-altas-y-staff.test.ts"
      to: "el-templo-api/src/modules/shared/branch-consistency.ts"
      via: "ejercicio del rechazo de sede ajena por las rutas de alta y edición"
      pattern: "branchId"
---

<objective>
Paso 6 de la receta, plan 3 de 3: la batería de las **11 rutas de escritura** — altas, edición y
baja de socios, el PATCH de leads y las 5 rutas de staff.

Purpose: acá se prueban **las dos cosas a la vez**. El aislamiento (ADO-02): el staff del gimnasio 2
no puede editar ni dar de baja a un socio de El Templo. Y el invariante de anclas (ADO-07): ninguna
de estas rutas acepta un `branchId` de **otro** gimnasio, que es el criterio 2 de la fase —
_"los ~10 sitios de escritura de `branch_id` rechazan con error explícito, cubierto por test con 2
tenants"_.

Y el control positivo del alta completa en el gimnasio 2 es exactamente lo que el ancla
autodestructiva de la 172 decía que **no se podía escribir** hasta que esta fase arreglara D-13.

Output: el tercer archivo de la batería, con los casos de sede cruzada de ADO-07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-CONTEXT.md
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-PATTERNS.md
@.docs/saas-multitenancy/07-receta-adopcion.md

**Ruta de código:** worktree `/home/franco/projects/et-173`.

**Las 11 rutas de este archivo:** `POST /api/admin/members`, `POST /api/admin/members/trial`,
`POST /api/admin/members/:userId/convert-to-trial`, `PUT /api/admin/members/:userId`,
`DELETE /api/admin/members/:userId`, `PATCH /api/admin/leads/:userId`,
`GET /api/admin/users`, `POST /api/admin/users`, `PUT /api/admin/users/:userId`,
`PATCH /api/admin/users/:userId/status`, `POST /api/admin/users/:userId/program-addons`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Altas, edición y baja de socios, más el PATCH de leads (6 rutas)</name>
  <files>el-templo-api/test/tenancy/iso-03-members-altas-y-staff.test.ts</files>
  <read_first>
    - el-templo-api/test/tenancy/iso-03-members-listados.test.ts (ciclo de vida, precondiciones y helpers de mensaje del plan 173-26 — reusarlos)
    - el-templo-api/test/fixtures/members-gimnasio-dos.ts (`tenantDeLaFila`, `campoDeLaFila`, los valores irrepetibles)
    - el-templo-api/src/modules/members/service.ts (post-173-18: qué escribe cada alta además de `users`)
    - el-templo-api/test/tenancy/iso-03-finance-coach-load.test.ts (post-173-14: el control positivo del alta completa en el gimnasio 2, que este archivo complementa)
    - el-templo-api/test/tenant-manifest.ts (las claves literales)
  </read_first>
  <action>
    Mismo ciclo de vida y las mismas 4 precondiciones que el plan 173-26.
    Un `describe` por ruta, con la clave literal del manifiesto, y **dos `it`** por ruta. En las rutas
    de escritura hacen falta **tres** dimensiones, y conviene un tercer `it` donde apliquen las dos:
    1. **Aislamiento clásico** (ADO-02): el staff del gimnasio 2 le pega a la ruta con un `userId` de
       El Templo → rechazo y **cero cambios en la base**, afirmados con **varias columnas juntas** en
       un solo `toEqual` (gimnasio, sede, status, `deleted_at`, `updated_at`). Un rechazo que ya
       escribió la mitad se ve igual que uno limpio si se mira una sola columna.
    2. **Sede cruzada** (ADO-07, criterio 2 de la fase): el staff del gimnasio 2 crea/edita un socio
       **propio** pero mandando un `branchId` de **El Templo** → rechazo, y la fila **no** queda
       escrita (o queda con su sede anterior). El error tiene que ser el de "sede no encontrada"
       (D-06), **cero 403** — un 403 confirmaría que la sede existe en otro gimnasio. Aplica a
       `POST /members`, `POST /members/trial`, `PUT /members/:userId` y `PATCH /leads/:userId`.
    3. **Control positivo**: la misma operación con recursos **propios** del gimnasio 2 funciona, y la
       fila nace/queda con `tenant_id = TENANT_DOS` y con la sede propia. Para `POST /members` esto es
       el alta completa que el ancla de la 172 no podía escribir.
    Además:
    - `DELETE /members/:userId`: la baja lógica de un socio ajeno no puede escribir `deleted_at` —
      comparar el valor de la base antes y después.
    - `POST /members/:userId/convert-to-trial`: verificar que no convierte un socio ajeno y que no
      escribe en `user_status_history` del otro gimnasio.
    - `POST /members` y `/trial` escriben en **varias** tablas (`users`, `member_profiles`,
      `user_status_history`, y a veces `user_sepa_details` y `user_branches`): el control positivo
      afirma el `tenant_id` de **todas** las filas creadas, no solo la de `users`.
    **Rol mínimo real** explícito en cada call site.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec vitest run --no-file-parallelism test/tenancy/iso-03-members-altas-y-staff.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Los 6 `describe` de socios y leads existen con su clave literal del manifiesto
    - Las 4 rutas que aceptan `branchId` tienen su `it` de **sede cruzada** (ADO-07), con el rechazo afirmado por motivo y la fila verificada sin cambios en la base
    - Cada caso de rechazo afirma la evidencia con varias columnas juntas en un solo `toEqual`
    - El control positivo de `POST /api/admin/members` afirma el `tenant_id` de **todas** las tablas escritas por el alta (listadas en el SUMMARY)
    - `DELETE /members/:userId` compara `deleted_at` de la base antes y después
    - `grep -c "403" el-templo-api/test/tenancy/iso-03-members-altas-y-staff.test.ts` devuelve `0`
    - `pnpm exec vitest run --no-file-parallelism test/tenancy/iso-03-members-altas-y-staff.test.ts` sale con código 0
  </acceptance_criteria>
  <done>El criterio 2 de la fase está probado sobre las rutas de socios: una sede de otro gimnasio se rechaza con error explícito y nada queda escrito.</done>
</task>

<task type="auto">
  <name>Task 2: Las 5 rutas de staff (/api/admin/users) y el caso de branchIds[]</name>
  <files>el-templo-api/test/tenancy/iso-03-members-altas-y-staff.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/users/service.ts (post-173-13: las 4 escrituras de `user_branches` y cómo se resuelve `branchIds[]`)
    - el-templo-api/src/modules/users/routes.ts (post-173-13: las 5 rutas y sus roles)
    - el-templo-api/test/fixtures/second-tenant.ts (qué roles puede crear el fixture en el gimnasio 2)
    - el-templo-api/test/tenant-manifest.ts (las 5 claves de `/api/admin/users`)
  </read_first>
  <action>
    Los 5 `describe` de staff, con caso de aislamiento + control positivo, y los casos de ancla:
    - `GET /api/admin/users`: el listado del gimnasio 2 no puede contener staff de El Templo. Barrido
      sobre **cada fila devuelta**, leyendo su `tenant_id` de la base. Y si acepta `?branchId=`,
      probarlo con una sede de El Templo → lista vacía / 404, nunca 403.
    - `POST /api/admin/users` (alta de staff): (a) con `branchId` de El Templo → rechazo y **cero
      filas nuevas** en `users`; (b) con `branchIds[]` **mixto** (una sede propia + una de El Templo) →
      **la operación entera se rechaza**, no se guarda la mitad. Contar filas de `user_branches` antes
      y después: filtrar en silencio dejaría al operador creyendo que guardó 2 sedes cuando guardó 1.
      Este es el caso que el plan 173-13 diseñó y el único lugar donde se prueba.
    - `PUT /api/admin/users/:userId`: editar un staff de El Templo desde el gimnasio 2 → rechazo con
      la foto de columnas sin cambios. Y el caso de sede cruzada sobre un staff propio.
    - `PATCH /api/admin/users/:userId/status`: deshabilitar un staff ajeno → rechazo, y el campo
      `disabled` de la base sin cambiar.
    - `POST /api/admin/users/:userId/program-addons`: sobre un usuario ajeno → rechazo, sin filas
      nuevas.
    **Rol mínimo real** explícito: estas rutas suelen exigir `owner`/`admin`; usar el más barato que
    el fixture pueda crear en el gimnasio 2 y dejarlo escrito.
    ⚠ **Cero `403`** en todo el archivo.
    ⚠ Este archivo nuevo re-baraja qué archivos comparten base por worker en CI. Si aparece un
    `ER_ROW_IS_REFERENCED_2` en un archivo ajeno, **no es el sentinel** — mirar el patrón de limpieza.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec vitest run --no-file-parallelism test/tenancy</automated>
  </verify>
  <acceptance_criteria>
    - El archivo tiene **11** `describe` en total, cada uno con su clave literal del manifiesto
    - El caso de `branchIds[]` mixto existe y afirma que la operación se rechaza **entera**, contando filas de `user_branches` antes y después
    - `GET /api/admin/users` hace el barrido leyendo el `tenant_id` de cada fila devuelta
    - Cada rechazo afirma la evidencia con varias columnas juntas en un solo `toEqual`
    - `grep -c "403" el-templo-api/test/tenancy/iso-03-members-altas-y-staff.test.ts` devuelve `0`
    - El rol usado en cada `describe` está explícito en el call site y listado en el SUMMARY
    - Mutación de cierre: quitar el `assertBranchDelGimnasio` de `users/service.ts` pone en rojo los casos de sede cruzada de staff; la salida se transcribe, los describes que NO se pusieron rojos se anotan (puede haber dos defensas independientes: la guarda de app **y** la FK compuesta del plan 173-12), y la mutación se revierte (`git status --porcelain` vacío)
    - `pnpm exec vitest run --no-file-parallelism test/tenancy` sale con código 0
  </acceptance_criteria>
  <done>Las 5 rutas de staff están cubiertas, incluido el caso de lista de sedes parcialmente ajena que ninguna otra prueba toca.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                       |
| ------------------------------------- | --------------------------------------------------------------------------------- |
| `branchId` del body → ancla           | la ruta acepta un número de sede y decide con él dónde queda apuntando el usuario |
| `branchIds[]` mixto → `user_branches` | una lista con sedes propias y ajenas: filtrar en silencio miente al operador      |
| `userId` de la URL → baja lógica      | `DELETE` sobre un socio ajeno escribiría `deleted_at` en otro gimnasio            |
| alta multi-tabla → consistencia       | un alta escribe en 4-5 tablas: afirmar solo `users` deja las otras sin verificar  |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                    | Disposition | Mitigation Plan                                                                                                       |
| ----------- | ---------------------- | -------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------- |
| T-173-28-01 | Tampering              | `branchId` ajeno en alta/edición             | mitigate    | `it` de sede cruzada por cada ruta que lo acepta: rechazo con motivo, fila sin cambios verificada en la base (ADO-07) |
| T-173-28-02 | Tampering              | `branchIds[]` parcialmente ajeno             | mitigate    | caso dedicado: la operación se rechaza entera, con conteo de `user_branches` antes y después                          |
| T-173-28-03 | Tampering              | `DELETE /members/:userId` ajeno              | mitigate    | comparación de `deleted_at` de la base antes y después                                                                |
| T-173-28-04 | Information Disclosure | rechazo con 403                              | mitigate    | D-06: cero 403 en el archivo, verificado por grep; el 403 confirmaría que la sede o el socio existen                  |
| T-173-28-05 | Repudiation            | alta que escribe en 4-5 tablas               | mitigate    | el control positivo afirma el `tenant_id` de TODAS las filas creadas, no solo la de `users`                           |
| T-173-28-06 | Elevation of Privilege | `PATCH /users/:userId/status`                | mitigate    | deshabilitar un staff ajeno se rechaza y el campo `disabled` de la base no cambia                                     |
| T-173-28-07 | Repudiation            | defensas redundantes que ocultan cuál muerde | mitigate    | la mutación anota también los describes que NO se pusieron rojos: la FK compuesta puede estar sosteniendo el rechazo  |

</threat_model>

<verification>
- El archivo verde con las 11 claves del manifiesto presentes literal.
- `grep -c "403"` en cero.
- El caso de `branchIds[]` mixto con conteo antes/después.
- La mutación documentada en los dos sentidos y revertida.
- `pnpm exec vitest run --no-file-parallelism test/tenancy` verde.
</verification>

<success_criteria>
El criterio 2 de la fase está probado con dos gimnasios sobre las 11 rutas de escritura: asignar una
sede de otro gimnasio se rechaza con error explícito, no queda nada escrito, y una lista de sedes
parcialmente ajena no se guarda a medias.
</success_criteria>

<output>
Create `.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-28-SUMMARY.md` when done
</output>
