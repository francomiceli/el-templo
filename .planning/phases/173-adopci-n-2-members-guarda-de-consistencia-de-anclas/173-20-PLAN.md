---
phase: 173-adopci-n-2-members-guarda-de-consistencia-de-anclas
plan: 20
type: execute
wave: 5
depends_on: ["173-17"]
files_modified:
  - el-templo-api/src/modules/members/routes.ts
  - el-templo-api/src/modules/members/leads-routes.ts
autonomous: true
requirements: [ADO-02]
must_haves:
  truths:
    - "Las ~9 queries que `members/routes.ts` hace DIRECTAMENTE en los handlers filtran por gimnasio (líneas 133, 545, 576, 638, 881, 914, 1194, 1310, 1377 según el mapeo)"
    - "Las 2 entradas por ARCHIVO de `members/routes.ts` (`bookings`, `branches`) quedan cubiertas — criterio §0.3 tabla O archivo"
    - "Toda closure declarada en el cuerpo del plugin que haga queries recibe `ctx` como primer parámetro (trampa (c))"
    - "`GET /api/admin/members/branches` no puede devolver sedes de otro gimnasio"
  artifacts:
    - path: "el-templo-api/src/modules/members/routes.ts"
      provides: "handlers de socios sin una sola query directa sin gimnasio"
      contains: "tenantWhere"
  key_links:
    - from: "el-templo-api/src/modules/members/routes.ts"
      to: "schema.branches"
      via: "tenantWhere inline en GET /api/admin/members/branches"
      pattern: "tenantWhere\\(schema\\.branches"
---

<objective>
Migrar las queries que viven **en los handlers** de `members/routes.ts` y `leads-routes.ts`, y no
en el service.

Purpose: el punto ciego del módulo son los archivos de rutas. `grep -n "fastify.db"` sobre
`members/routes.ts` (1.867 líneas) da queries en handlers en al menos **9** líneas, y el archivo
hoy importa `assertTenant` pero **no** `tenantWhere`. Las closures declaradas en el cuerpo del
plugin son el caso peor: no tienen `request` a mano, así que nadie les puede pasar el scope sin
cambiarles la firma (trampa (c)).

Además `GET /api/admin/members/branches` devuelve el selector de sedes de toda la UI de socios: sin
filtro, el staff de un gimnasio ve —y puede elegir— sedes del otro, que es la puerta de entrada
directa a la divergencia de anclas que ADO-07 protege.

Output: los dos archivos de rutas sin una entrada de allowlist, por ninguno de los dos criterios.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-CONTEXT.md
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-PATTERNS.md
@.docs/saas-multitenancy/07-receta-adopcion.md

**Ruta de código:** worktree `/home/franco/projects/et-173`.
**Allowlist de trabajo:** `/tmp/allowlist-173-20.json`.

**Entradas objetivo de `members/routes.ts` (5 en total):** 3 por tabla del módulo + 2 por archivo
(`bookings`, `branches`). `leads-routes.ts` tiene **cero** entradas propias pero delega en
`memberService.updateLead`.

⚠ Los `assertTenant` ya están puestos por el plan 173-17: este plan **usa** ese `ctx`, no lo agrega.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Las queries directas de los handlers y el selector de sedes</name>
  <files>el-templo-api/src/modules/members/routes.ts</files>
  <read_first>
    - el-templo-api/src/modules/members/routes.ts (el archivo completo; en particular las queries directas alrededor de :133, :545, :576, :638, :881, :914, :1194, :1310, :1377, y :35 donde hoy importa `assertTenant` pero no `tenantWhere`)
    - el-templo-api/src/modules/finance/coach-load-routes.ts (:288-321 `resolveUserBranchId` — el ejemplar de closure con `ctx` primero, y el fallback por NOMBRE que fue la query más peligrosa del piloto)
    - el-templo-api/src/modules/finance/cash-register-service.ts (:446-465 — filtro de join en el ON)
    - .planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-02-SUMMARY.md (el listado medido de queries directas y de búsquedas por nombre)
  </read_first>
  <action>
    Re-derivar la lista con `grep -n "fastify.db" src/modules/members/routes.ts` **antes** de medir
    nada (los conteos de un doc de patterns son referencias, no queries — mordió 6 veces en el
    piloto). Para cada query encontrada:
    - `tenantWhere` **inline** en el statement que nombra la tabla, primer término del `and(...)`,
      usando el `ctx` que el `assertTenant` del handler ya resolvió;
    - filtro **en el `ON`** de todo join, INNER incluido;
    - un `tenantWhere` por **cada** tabla del módulo del statement.
    Agregar el import de `tenantWhere` (hoy `:35` importa solo `assertTenant`).
    **`GET /api/admin/members/branches` (~:132):** devuelve el selector de sedes. `tenantWhere(schema.
    branches, ctx)` inline. Es el camino por el que el staff elige la sede que después se escribe en
    el ancla: sin filtro, la UI le ofrece sedes ajenas y el helper del plan 173-11 tiene que
    rechazarlas después. Filtrar acá es lo que hace que el rechazo nunca haga falta.
    **Closures del cuerpo del plugin (trampa (c)):** si hay funciones declaradas dentro de
    `export default async function (fastify)` que hagan queries, reciben `ctx` como **primer
    parámetro** y cada call site le pasa el que ya resolvió. No tienen `request` a mano: es el punto
    ciego que el piloto encontró en `finance/routes.ts`.
    ⚠ Barrer específicamente búsquedas por `name` / `email` / `dni` en este archivo.
    Cubrir las 2 entradas por archivo (`bookings`, `branches`): son queries de un archivo del módulo
    y entran al alcance por el criterio §0.3 (tabla O archivo).
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec tsc -p tsconfig.test-check.json --noEmit && pnpm exec vitest run --no-file-parallelism test/members</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "fastify.db" el-templo-api/src/modules/members/routes.ts` — cada línea listada en el SUMMARY con su tabla y su filtro
    - `grep -c "tenantWhere" el-templo-api/src/modules/members/routes.ts` devuelve al menos `9`
    - `GET /api/admin/members/branches` filtra `branches` por gimnasio (statement verificado y citado en el SUMMARY)
    - Toda closure del cuerpo del plugin que haga queries tiene `ctx` como primer parámetro
    - Ningún join queda con su filtro en el `.where(...)` en vez del `ON`
    - `pnpm exec tsc -p tsconfig.test-check.json --noEmit` sale con código 0
    - `pnpm exec vitest run --no-file-parallelism test/members` sale con código 0
  </acceptance_criteria>
  <done>El selector de sedes solo ofrece sedes propias, y ninguna query de handler queda sin gimnasio.</done>
</task>

<task type="auto">
  <name>Task 2: leads-routes.ts y cierre del criterio tabla-O-archivo de los dos archivos</name>
  <files>el-templo-api/src/modules/members/leads-routes.ts, el-templo-api/src/modules/members/routes.ts</files>
  <read_first>
    - el-templo-api/src/modules/members/leads-routes.ts (la única ruta, `PATCH /api/admin/leads/:userId`, y cómo delega en `memberService.updateLead`)
    - el-templo-api/src/modules/members/routes.ts (post-tarea 1)
    - .planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-01-SUMMARY.md (las 90 entradas objetivo, para chequear cuáles quedan vivas)
  </read_first>
  <action>
    `leads-routes.ts` no tiene entradas propias, pero: verificar que su handler use el `ctx` del
    `assertTenant` puesto por 173-17 y que no haga ninguna query directa. Si hace alguna (por ejemplo
    para validar la existencia del lead antes de delegar), migrarla.
    ⚠ Si la ruta acepta un `branchId` en el body, resolverlo con el helper del plan 173-11 antes de
    pasarlo al service: es un camino de escritura de ancla que no aparece en la lista de 12 porque el
    UPDATE está en el service, pero el dato entra por acá.
    **Cierre del criterio §0.3 para los dos archivos:** correr el lint contra
    `/tmp/allowlist-173-20.json` y verificar que **ninguna** entrada tenga `file` bajo
    `src/modules/members/`, por ninguna tabla. Con el service cerrado por el plan 173-19 y las rutas
    por este, el módulo queda sin entradas por archivo.
    Registrar en el SUMMARY el conteo acumulado de entradas del objetivo del switch que ya están
    cubiertas contra la allowlist de trabajo, y cuáles **faltan** — es el insumo del plan 173-30.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm lint:tenant --allowlist /tmp/allowlist-173-20.json && pnpm exec vitest run --no-file-parallelism test/members test/leads</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm lint:tenant --allowlist /tmp/allowlist-173-20.json` reporta **CERO** entradas cuyo `file` esté bajo `el-templo-api/src/modules/members/`
    - Si `PATCH /api/admin/leads/:userId` acepta `branchId`, lo resuelve con el helper antes de delegar
    - `grep -cE "403|Forbidden" el-templo-api/src/modules/members/routes.ts el-templo-api/src/modules/members/leads-routes.ts` no aumenta respecto de la rama base
    - El SUMMARY lista, contra el objetivo de 90 entradas, cuáles están cubiertas y cuáles faltan (insumo del plan 173-30)
    - `pnpm exec vitest run --no-file-parallelism test/members test/leads` sale con código 0
  </acceptance_criteria>
  <done>El módulo `members` no tiene una sola entrada de allowlist por archivo, y el plan del switch sabe exactamente qué le queda.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                      |
| ---------------------------------- | -------------------------------------------------------------------------------- |
| selector de sedes de la UI → ancla | `GET /members/branches` alimenta el `branchId` que después se escribe en `users` |
| closure del plugin → query         | no tiene `request` a mano: nadie le pasa el scope sin cambiarle la firma         |
| params/query de la ruta → `users`  | ids y textos de búsqueda llegan del cliente directo al handler                   |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                       | Disposition | Mitigation Plan                                                                                                         |
| ----------- | ---------------------- | ----------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------- |
| T-173-20-01 | Information Disclosure | `GET /api/admin/members/branches`               | mitigate    | `tenantWhere(schema.branches, ctx)` inline: la UI deja de ofrecer sedes ajenas y el rechazo del helper nunca hace falta |
| T-173-20-02 | Information Disclosure | queries directas de los handlers                | mitigate    | `tenantWhere` inline en las ~9 queries, re-derivadas con grep antes de medir                                            |
| T-173-20-03 | Information Disclosure | closures del cuerpo del plugin                  | mitigate    | `ctx` como primer parámetro de cada closure (trampa (c)); cada call site le pasa el que ya resolvió                     |
| T-173-20-04 | Tampering              | `branchId` en el body de `PATCH /leads/:userId` | mitigate    | se resuelve con el helper de anclas antes de delegar al service                                                         |
| T-173-20-05 | Information Disclosure | búsquedas por nombre en el handler              | mitigate    | barrido específico y `tenantWhere` inline; un nombre común devolvería la fila del primer gimnasio que lo tenga          |

</threat_model>

<verification>
- `pnpm lint:tenant --allowlist /tmp/allowlist-173-20.json` con cero entradas bajo `src/modules/members/`.
- `pnpm exec tsc -p tsconfig.test-check.json --noEmit` verde.
- `pnpm exec vitest run --no-file-parallelism test/members test/leads` verde.
- El SUMMARY tiene el balance contra las 90 entradas objetivo.
</verification>

<success_criteria>
Los archivos de rutas del módulo —el punto ciego que el piloto pagó caro— quedan sin una sola query
sin gimnasio, y el selector de sedes deja de ofrecer la puerta de entrada a la divergencia de
anclas.
</success_criteria>

<output>
Create `.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-20-SUMMARY.md` when done
</output>
