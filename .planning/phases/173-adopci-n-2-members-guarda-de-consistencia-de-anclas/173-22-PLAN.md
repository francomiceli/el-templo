---
phase: 173-adopci-n-2-members-guarda-de-consistencia-de-anclas
plan: 22
type: execute
wave: 8
depends_on: ["173-21"]
files_modified:
  - el-templo-api/test/members/
  - el-templo-api/test/users/
  - el-templo-api/test/auth/
  - el-templo-api/test/shared/
  - el-templo-api/test/country-scope.test.ts
  - el-templo-api/test/branch-access.test.ts
  - el-templo-api/test/admin-leads-patch.test.ts
  - el-templo-api/test/lead-status-transitions.test.ts
  - el-templo-api/test/leads-perdido-window.test.ts
  - el-templo-api/test/backfill-lost-leads.test.ts
  - el-templo-api/test/expire-lost-leads.test.ts
  - el-templo-api/test/convert-freemium-to-trial.test.ts
autonomous: true
requirements: [ADO-02]
must_haves:
  truths:
    - "Los tests del propio módulo (socios, staff, auth, leads y shared) sobreviven al throw del sentinel sobre las 8 tablas"
    - "Los DELETE de conveniencia de los `beforeEach` se ACOTAN con filtro parametrizado, no se eximen — el piloto acotó 34 sitios en un plan con CERO exenciones"
    - "Las lecturas de evidencia (leer el `tenant_id` de una fila) se eximen: filtrarlas volvería la aserción tautológica"
    - "Ninguna exención nueva queda sin motivo escrito"
  artifacts:
    - path: "el-templo-api/test/members/"
      provides: "suite del módulo de socios verde con el sentinel en throw"
  key_links:
    - from: "los beforeEach de test/members y test/users"
      to: "el pool envuelto por el sentinel"
      via: "filtro WHERE tenant_id = ? parametrizado, o exención con motivo"
      pattern: "tenant_id"
---

<objective>
Paso 5 de la receta, plan 2 de 5: dejar listos para el throw los tests del **propio módulo** —
socios, staff, auth, leads y los helpers compartidos.

Purpose: son los archivos con más densidad de escrituras sobre las 8 tablas: cada test de alta,
ficha, nota o export crea usuarios, perfiles e historial de estado. Y son los primeros que van a
tocar el throw. Van primero porque son también los que mejor enseñan cuál es la clasificación
correcta para los 3 planes que siguen.

Output: la suite del módulo verde con la sonda encendida, con la clasificación de cada sitio
documentada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-CONTEXT.md
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-PATTERNS.md
@.docs/saas-multitenancy/07-receta-adopcion.md

**Ruta de código:** worktree `/home/franco/projects/et-173`.

**Procedimiento de la sonda** (documentado en 173-21-SUMMARY): encender la entrada `members` en
`TENANT_STRICT_MODULES` de `src/db/tenant-tables.ts` a mano, correr, **revertir sin commitear**.
Pre-check obligatorio: `git status --porcelain` muestra solo ese archivo durante la corrida y queda
**vacío** al terminar. La sonda **nunca** se commitea.

**Notas de corrida:** `--no-file-parallelism` es más rápido; `prettier --write` **antes** de la
corrida larga; el throw llega envuelto en `DrizzleQueryError.cause` y `TenantSentinelError` lleva
`sql` y `tables` como campos propios.
</context>

<tasks>

<task type="auto">
  <name>Task 1: test/members, test/users y test/auth</name>
  <files>el-templo-api/test/members/, el-templo-api/test/users/, el-templo-api/test/auth/</files>
  <read_first>
    - el-templo-api/test/helpers.ts (post-173-21: la regla de decisión de 4 categorías en la cabecera)
    - el-templo-api/test/members/ (los 10 archivos: qué crean en el `beforeEach` y qué borran)
    - el-templo-api/test/users/ (4 archivos) y el-templo-api/test/auth/ (4 archivos)
    - .planning/phases/172-adopci-n-1-piloto-finance/172-14-PLAN.md (cómo el piloto clasificó el grupo más pesado)
    - .planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-02-SUMMARY.md (el inventario de `test/` por los 3 canales, con la clasificación propuesta de cada sitio)
  </read_first>
  <action>
    Encender la sonda y correr los tres directorios. Para **cada** sitio que tire throw, clasificar con
    la regla del doc 07 §4(d):
    - **DELETE / UPDATE de conveniencia en un `beforeEach`** → **filtro** `WHERE tenant_id = ?`
      **parametrizado**. Es la categoría mayoritaria: el piloto acotó 34 sitios así en un solo plan,
      con **cero** exenciones. Acotar no cambia lo que el test prueba porque el test siembra en un
      solo gimnasio.
    - **Lectura de evidencia** (`tenantDeLaFila`, `campoDeLaFila`, cualquier assert que lea el
      `tenant_id` de una fila) → **exención** con motivo: leer el `tenant_id` **es** la aserción, y
      filtrarla la volvería tautológica.
    - **Limpieza global deliberada** → **exención** con motivo, **adentro del SQL** entre el verbo y el
      `FROM`.
    - **Tabla que todavía no es strict** → **no se anota**.
    ⚠ Buscar por los **tres** canales, no solo por `.from(schema.X)`: (1) Drizzle; (2) **SQL crudo con
    backticks** (`conn.query("DELETE FROM \`users\`")`) — 15 sitios en el piloto que ninguna regex de
    Drizzle vio y que el sentinel cazó al primer intento; (3) nombre de tabla que **no está en el
    fuente** (`sql.raw(tabla)`), que se clasifica a mano contra el `.sql` que ejecuta.
    ⚠ Toda exención lleva **motivo obligatorio**. Una exención sin motivo es la salida de una línea
    para apagar un rojo.
    Correr `prettier --write` antes de la corrida larga.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec vitest run --no-file-parallelism test/members test/users test/auth</automated>
  </verify>
  <acceptance_criteria>
    - Con la sonda encendida, `pnpm exec vitest run --no-file-parallelism test/members test/users test/auth` sale con código 0
    - El SUMMARY lista cada sitio tocado con archivo, línea, canal (Drizzle / SQL crudo / dinámico) y clasificación (filtro / exención / no se anota)
    - La proporción filtro:exención está documentada, y toda exención tiene motivo escrito (verificado por grep: ningún `tenant-safe:` sin texto después)
    - Los sitios de SQL crudo con backticks están listados aparte con su conteo
    - Después de revertir la sonda, `git status --porcelain` no muestra `src/db/tenant-tables.ts`
    - `pnpm exec tsc -p tsconfig.test-check.json --noEmit` sale con código 0
  </acceptance_criteria>
  <done>Los tests del módulo pasan con el sentinel en throw, y la clasificación queda documentada como referencia para los 3 planes siguientes.</done>
</task>

<task type="auto">
  <name>Task 2: leads, shared, country-scope y branch-access</name>
  <files>el-templo-api/test/shared/, el-templo-api/test/country-scope.test.ts, el-templo-api/test/branch-access.test.ts, el-templo-api/test/admin-leads-patch.test.ts, el-templo-api/test/lead-status-transitions.test.ts, el-templo-api/test/leads-perdido-window.test.ts, el-templo-api/test/backfill-lost-leads.test.ts, el-templo-api/test/expire-lost-leads.test.ts, el-templo-api/test/convert-freemium-to-trial.test.ts</files>
  <read_first>
    - el-templo-api/test/country-scope.test.ts y branch-access.test.ts (los dos archivos que prueban lo que los planes 173-04 y 173-11 cambiaron)
    - el-templo-api/test/shared/ (4 archivos)
    - los 6 archivos sueltos de leads y conversión de freemium
    - .planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-11-SUMMARY.md (qué cambió `canAccessBranch`)
  </read_first>
  <action>
    Mismo tratamiento con la sonda.
    Dos archivos merecen atención especial porque **prueban lo que esta fase cambió**:
    - `branch-access.test.ts`: los tests que hoy afirman que `canAccessBranch` devuelve `true` para una
      sede virtual, o que decide por país, **cambian de significado** con D-14. No "arreglarlos" bajando
      la aserción: revisarlos uno por uno y decidir si (a) el caso sigue siendo válido dentro del mismo
      gimnasio y hay que agregarle el gimnasio a la siembra, o (b) el caso probaba el bypass y ahora
      tiene que afirmar `false`. Documentar cada decisión en el SUMMARY.
    - `country-scope.test.ts`: idem con la resolución del scope del plan 173-04.
    Los archivos de leads (`admin-leads-patch`, `lead-status-transitions`, `leads-perdido-window`,
    `backfill-lost-leads`, `expire-lost-leads`, `convert-freemium-to-trial`) escriben mucho sobre
    `user_status_history`: es la tabla que más va a tirar throw en este grupo.
    ⚠ Si un test se pone rojo por una razón que **no** es el sentinel (por ejemplo
    `ER_ROW_IS_REFERENCED_2` en un archivo que la fase no tocó), **no es el sentinel**: mirar el patrón
    de limpieza antes de sospechar de la tenancy.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec vitest run --no-file-parallelism test/shared test/country-scope.test.ts test/branch-access.test.ts test/admin-leads-patch.test.ts test/lead-status-transitions.test.ts test/leads-perdido-window.test.ts test/backfill-lost-leads.test.ts test/expire-lost-leads.test.ts test/convert-freemium-to-trial.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Con la sonda encendida, los 9 destinos de esta tarea salen con código 0
    - El SUMMARY documenta, caso por caso, cada test de `branch-access.test.ts` que cambió de significado con D-14 y por qué quedó como quedó
    - Ninguna aserción se "arregló" bajando la expectativa sin explicación (regla del doc 07 §6: bajar un número o una expectativa pelada está prohibido)
    - Toda exención nueva tiene motivo escrito
    - Después de revertir la sonda, `git status --porcelain` no muestra `src/db/tenant-tables.ts`
    - `pnpm exec vitest run --no-file-parallelism test/members test/users test/auth test/shared` sale con código 0 (regresión del grupo completo)
  </acceptance_criteria>
  <done>El grupo del propio módulo está listo para el throw, y los tests que probaban el bypass de `canAccessBranch` ahora prueban que está cerrado.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                  |
| ----------------------------------- | ---------------------------------------------------------------------------- |
| `beforeEach` → pool envuelto        | el sentinel ve toda query de test que pase por `app.dbPool`                  |
| aserción bajada → cobertura perdida | "arreglar" un test cambiando lo que espera apaga la garantía sin que se note |

## STRIDE Threat Register

| Threat ID   | Category    | Component                                  | Disposition | Mitigation Plan                                                                                                                  |
| ----------- | ----------- | ------------------------------------------ | ----------- | -------------------------------------------------------------------------------------------------------------------------------- |
| T-173-22-01 | Repudiation | exenciones de conveniencia en `beforeEach` | mitigate    | los DELETE de conveniencia se **filtran** con `WHERE tenant_id = ?` parametrizado, no se eximen; la proporción queda documentada |
| T-173-22-02 | Repudiation | aserciones bajadas para apagar un rojo     | mitigate    | cada test de `branch-access.test.ts` que cambia de significado se documenta caso por caso con su justificación                   |
| T-173-22-03 | Repudiation | exención sin motivo                        | mitigate    | motivo obligatorio, verificado por grep de `tenant-safe:` sin texto                                                              |
| T-173-22-04 | Tampering   | sonda commiteada por accidente             | mitigate    | pre-check `git status --porcelain` antes y después de cada corrida                                                               |

</threat_model>

<verification>
- Con la sonda encendida, `test/members test/users test/auth test/shared` y los 6 archivos de leads verdes.
- `git status --porcelain` sin `src/db/tenant-tables.ts` al cerrar.
- `pnpm exec tsc -p tsconfig.test-check.json --noEmit` verde.
- El SUMMARY tiene la tabla de clasificación por sitio y las decisiones de `branch-access.test.ts`.
</verification>

<success_criteria>
Los tests que más socios crean sobreviven al throw sin haber comprado el verde con exenciones, y
los que probaban el bypass de sedes virtuales ahora prueban que está tapado.
</success_criteria>

<output>
Create `.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-22-SUMMARY.md` when done
</output>
