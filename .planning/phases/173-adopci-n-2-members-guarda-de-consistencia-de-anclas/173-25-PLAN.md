---
phase: 173-adopci-n-2-members-guarda-de-consistencia-de-anclas
plan: 25
type: execute
wave: 11
depends_on: ["173-24"]
files_modified:
  - el-templo-api/test/migrations/
  - el-templo-api/test/tenancy/
  - el-templo-api/test/tv/
  - el-templo-api/test/admin/
  - el-templo-api/test/exercises/
  - el-templo-api/test/sessions/
  - el-templo-api/test/unit/
  - el-templo-api/test/db/
  - el-templo-api/test/jobs/
  - el-templo-api/test/wellhub/
  - el-templo-api/test/streaks/
  - el-templo-api/test/tree-progress/
  - el-templo-api/test/tree-editor/
  - el-templo-api/test/progression/
  - el-templo-api/test/goal-plans/
  - el-templo-api/test/gladius/
  - el-templo-api/test/kairos/
  - el-templo-api/test/academy/
  - el-templo-api/test/blog/
  - el-templo-api/test/franchise/
  - el-templo-api/test/settings/
  - el-templo-api/test/app-landing/
  - el-templo-api/test/bar-challenge.test.ts
  - el-templo-api/test/rbac-sets.test.ts
  - el-templo-api/test/exercise-adjustments.test.ts
  - el-templo-api/test/exercise-adjustments-coach.test.ts
  - el-templo-api/test/programs.test.ts
autonomous: true
requirements: [ADO-02]
must_haves:
  truths:
    - "La cola larga de la suite (migraciones, tenancy previos, TV, admin, entrenamiento, gamificación, wellhub, jobs y unit) sobrevive al throw sobre las 8 tablas"
    - "El barrido global sobre `test/` de accesos LITERALES a las 8 tablas queda en CERO, y las tablas dinámicas (`sql.raw`) están clasificadas a mano contra el `.sql` que ejecutan"
    - "El barrido de cobertura se probó contra un caso conocido-malo antes de confiar en su verde: una auditoría no probada es un placebo"
    - "La suite COMPLETA corre verde con la sonda encendida"
  artifacts:
    - path: ".planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-25-SUMMARY.md"
      provides: "el barrido global en cero y la lista de tablas dinámicas clasificadas a mano"
  key_links:
    - from: "el barrido global sobre test/"
      to: "las 8 tablas del módulo"
      via: "búsqueda por los 3 canales, probada contra un árbol pre-migración"
      pattern: "0 sitios"
---

<objective>
Paso 5 de la receta, plan 5 de 5: la cola larga de `test/` y el **barrido global** que declara
terminado el endurecimiento.

Purpose: los tres planes anteriores cubrieron los grupos densos. Lo que queda es donde se esconden
los sitios que ningún grep de Drizzle ve: los tests de **migraciones** (que ejecutan SQL crudo por
definición), los de **tenancy de fases anteriores** (que siembran a propósito en dos gimnasios), los
de **TV** y **unit** (que a veces usan `sql.raw` con el nombre de tabla armado), y las suites de
entrenamiento y gamificación que crean socios en el `beforeEach` sin que el módulo bajo prueba tenga
nada que ver.

Y el cierre: **una auditoría de cobertura que no se probó contra un caso conocido-malo no es un
gate, es un placebo.** El piloto corrió su barrido contra el árbol pre-migración (55 sitios) y contra
un archivo pre-arreglo (27 sitios) **antes** de confiar en su verde final.

Output: la suite completa verde con la sonda encendida, y el barrido global en cero, probado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-CONTEXT.md
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-PATTERNS.md
@.docs/saas-multitenancy/07-receta-adopcion.md

**Ruta de código:** worktree `/home/franco/projects/et-173`.
**Sonda:** procedimiento del plan 173-21, con pre-check `git status --porcelain`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: migraciones, tenancy previos, TV, admin, jobs, db y unit</name>
  <files>el-templo-api/test/migrations/, el-templo-api/test/tenancy/, el-templo-api/test/tv/, el-templo-api/test/admin/, el-templo-api/test/jobs/, el-templo-api/test/db/, el-templo-api/test/unit/, el-templo-api/test/wellhub/, el-templo-api/test/rbac-sets.test.ts</files>
  <read_first>
    - el-templo-api/test/migrations/ (8 archivos que ejecutan SQL crudo por definición: el canal 2 y 3 del inventario viven acá)
    - el-templo-api/test/tenancy/ (16 archivos, incluidos los de las fases 166-172 y los nuevos de esta fase: siembran en DOS gimnasios a propósito)
    - el-templo-api/test/tv/ (11 archivos), test/admin/ (8), test/unit/ (13)
    - .planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-02-SUMMARY.md (los sitios de SQL crudo y de tabla dinámica listados)
    - .planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-22-SUMMARY.md (el criterio de clasificación)
  </read_first>
  <action>
    Encender la sonda y correr los directorios en un lote.
    Puntos específicos de este grupo:
    - **`test/migrations/`**: ejecutan SQL crudo. Sus statements sobre las 8 tablas casi siempre son
      **globales a propósito** (verifican que una migración tocó todas las filas) → **exención con
      motivo, adentro del SQL**, entre el verbo y el `FROM`.
    - **`test/tenancy/`**: siembran en **dos gimnasios** a propósito. Las lecturas de evidencia
      (`tenantDeLaFila`, `campoDeLaFila`) son **exención**: filtrarlas volvería la aserción
      tautológica. Los `beforeEach` de limpieza de esos archivos sí se pueden acotar por gimnasio.
    - **`test/db/`** y **`test/unit/`**: revisar específicamente los `sql.raw(tabla)` — el nombre de la
      tabla **no está en el fuente** y ninguna auditoría estática puede decidir. Se clasifican **a
      mano** contra el `.sql` que ejecutan, y la decisión se escribe.
    - **`test/tv/`** y **`test/admin/`**: crean usuarios en el `beforeEach` aunque prueben otra cosa.
    - **`test/jobs/`**: post-173-16, sus fixtures tienen que sembrar por gimnasio.
    ⚠ Los statements sobre tablas que **todavía no** son strict **no se anotan**.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec vitest run --no-file-parallelism test/migrations test/tenancy test/tv test/admin test/jobs test/db test/unit test/wellhub test/rbac-sets.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Con la sonda encendida, los 9 destinos salen con código 0
    - Cada `sql.raw(...)` de `test/db` y `test/unit` que toque una de las 8 tablas está listado en el SUMMARY con la tabla que ejecuta y su clasificación manual
    - Las exenciones de `test/migrations/` van **adentro del SQL** entre el verbo y el `FROM`, con motivo
    - Ninguna lectura de evidencia de `test/tenancy/` quedó filtrada (eso la volvería tautológica)
    - Después de revertir la sonda, `git status --porcelain` no muestra `src/db/tenant-tables.ts`
  </acceptance_criteria>
  <done>Los tests que ejecutan SQL crudo y los que siembran en dos gimnasios a propósito quedan clasificados con criterio, no por inercia.</done>
</task>

<task type="auto">
  <name>Task 2: entrenamiento, gamificación y el resto de la cola</name>
  <files>el-templo-api/test/exercises/, el-templo-api/test/sessions/, el-templo-api/test/streaks/, el-templo-api/test/tree-progress/, el-templo-api/test/tree-editor/, el-templo-api/test/progression/, el-templo-api/test/goal-plans/, el-templo-api/test/gladius/, el-templo-api/test/kairos/, el-templo-api/test/academy/, el-templo-api/test/blog/, el-templo-api/test/franchise/, el-templo-api/test/settings/, el-templo-api/test/app-landing/, el-templo-api/test/bar-challenge.test.ts, el-templo-api/test/exercise-adjustments.test.ts, el-templo-api/test/exercise-adjustments-coach.test.ts, el-templo-api/test/programs.test.ts</files>
  <read_first>
    - los 18 destinos listados (la mayoría crea un socio en el `beforeEach` y prueba otra cosa)
    - .planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-09-SUMMARY.md (qué cambió en los services de entrenamiento y gamificación)
    - .planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-22-SUMMARY.md (el criterio)
  </read_first>
  <action>
    Mismo tratamiento con la sonda, en un lote grande.
    Son suites donde `users` y `member_profiles` aparecen solo en el `beforeEach`: la clasificación
    dominante es **filtro** parametrizado, no exención.
    ⚠ Varias de estas rutas son member-facing y el plan 173-09 les puso el filtro sin darles caso de
    aislamiento (D-09). Si algún test acá siembra socios de más de un gimnasio, es candidato natural a
    convertirse en el caso ISO-03 de su módulo cuando llegue su fase: anotarlo en el SUMMARY como
    insumo, sin escribirlo ahora.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec vitest run --no-file-parallelism test/exercises test/sessions test/streaks test/tree-progress test/tree-editor test/progression test/goal-plans test/gladius test/kairos test/academy test/blog test/franchise test/settings test/app-landing test/bar-challenge.test.ts test/exercise-adjustments.test.ts test/exercise-adjustments-coach.test.ts test/programs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Con la sonda encendida, los 18 destinos salen con código 0
    - La clasificación dominante en este grupo es **filtro** (la proporción filtro:exención está en el SUMMARY)
    - Toda exención nueva tiene motivo escrito
    - Después de revertir la sonda, `git status --porcelain` no muestra `src/db/tenant-tables.ts`
  </acceptance_criteria>
  <done>La cola larga corre verde con el throw.</done>
</task>

<task type="auto">
  <name>Task 3: Barrido global sobre test/ y suite completa con la sonda</name>
  <files>ninguno (verificación + SUMMARY)</files>
  <read_first>
    - .planning/phases/172-adopci-n-1-piloto-finance/172-16-SUMMARY.md (cómo el piloto cerró su barrido: 0 sitios sobre 245 archivos, probado antes contra 55 y 27 sitios conocidos-malos)
    - .docs/saas-multitenancy/07-receta-adopcion.md (§4(d), la regla de "una auditoría no probada es un placebo")
    - los SUMMARY de 173-22, 173-23, 173-24 y las dos tareas anteriores de este plan
  </read_first>
  <action>
    Escribir el barrido global sobre `test/` que busca accesos **literales** a las 8 tablas por los
    3 canales (Drizzle `.from(schema.X)`, SQL crudo con backticks, y `sql.raw` con nombre literal), y
    que ignora los que llevan exención anclada.
    **Probarlo contra un caso conocido-malo ANTES de confiar en su verde** (esto no es opcional):
    correrlo contra el árbol de la rama base (`git stash` o un checkout temporal de `origin/staging`)
    y verificar que **encuentra** los sitios que los planes 173-22..173-25 arreglaron. Un barrido que
    da 0 sobre el árbol viejo está roto. Transcribir los dos conteos en el SUMMARY: N sobre el árbol
    viejo, 0 sobre el actual.
    Las tablas **dinámicas** (`sql.raw(tabla)`) no las puede decidir ninguna auditoría estática: se
    listan aparte, clasificadas a mano contra el `.sql` que ejecutan, y esa lista va al SUMMARY.
    Cerrar con la **suite completa** con la sonda encendida:
    `pnpm exec vitest run --no-file-parallelism`. Es la última vez que se corre entera antes del
    switch, y su verde es la precondición del plan 173-30.
    ⚠ Correr `prettier --write` **antes** de la corrida larga, no después.
    ⚠ Al terminar, revertir la sonda y verificar `git status --porcelain` vacío.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec prettier --write "test/**/*.ts" && pnpm exec vitest run --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - El barrido global devuelve **0** sitios literales sin exención sobre las 8 tablas en `test/`
    - El mismo barrido, corrido contra el árbol de `origin/staging`, devuelve un número **> 0** (transcrito en el SUMMARY): la auditoría está probada contra un caso conocido-malo
    - La lista de tablas dinámicas clasificadas a mano está en el SUMMARY, con la tabla real de cada una
    - Con la sonda encendida, `pnpm exec vitest run --no-file-parallelism` (suite COMPLETA) sale con código 0
    - `pnpm exec tsc -p tsconfig.test-check.json --noEmit` sale con código 0
    - Después de revertir la sonda, `git status --porcelain` está vacío
  </acceptance_criteria>
  <done>El endurecimiento de `test/` está terminado y medido, y el plan del switch tiene su precondición cumplida.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                            |
| ----------------------------------- | -------------------------------------------------------------------------------------- |
| `sql.raw(tabla)` → auditoría        | el nombre de la tabla no está en el fuente: ninguna herramienta estática puede decidir |
| barrido no probado → confianza      | una auditoría que da verde sin haberse probado contra un caso malo es un placebo       |
| SQL crudo de migraciones → sentinel | ejecutan DDL/DML global por definición                                                 |

## STRIDE Threat Register

| Threat ID   | Category    | Component                             | Disposition | Mitigation Plan                                                                                                         |
| ----------- | ----------- | ------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------- |
| T-173-25-01 | Repudiation | barrido global de cobertura           | mitigate    | se prueba contra el árbol de `origin/staging` y tiene que encontrar > 0 sitios ahí antes de que su 0 valga algo         |
| T-173-25-02 | Repudiation | tablas dinámicas (`sql.raw`)          | mitigate    | se listan aparte y se clasifican a mano contra el `.sql` que ejecutan; la decisión queda escrita                        |
| T-173-25-03 | Repudiation | exenciones de `test/migrations/`      | mitigate    | motivo obligatorio, adentro del SQL entre el verbo y el `FROM`; solo para statements globales a propósito               |
| T-173-25-04 | Repudiation | evidencia filtrada en `test/tenancy/` | mitigate    | criterio de aceptación que verifica que las lecturas de evidencia siguen SIN filtro: filtrarlas las vuelve tautológicas |
| T-173-25-05 | Tampering   | sonda commiteada                      | mitigate    | pre-check y post-check `git status --porcelain`, vacío al cerrar el plan                                                |

</threat_model>

<verification>
- Suite **completa** verde con la sonda encendida (`--no-file-parallelism`).
- Barrido global en 0 sobre el árbol actual y > 0 sobre `origin/staging`.
- Tablas dinámicas clasificadas a mano y listadas.
- `git status --porcelain` vacío al cerrar.
</verification>

<success_criteria>
El endurecimiento de `test/` está terminado con evidencia: la suite entera pasa como si las 8 tablas
ya fueran strict, el barrido que lo declara está probado contra un caso conocido-malo, y no quedó
ninguna tabla dinámica sin clasificar.
</success_criteria>

<output>
Create `.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-25-SUMMARY.md` when done
</output>
