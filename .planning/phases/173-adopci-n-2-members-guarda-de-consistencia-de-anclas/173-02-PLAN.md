---
phase: 173-adopci-n-2-members-guarda-de-consistencia-de-anclas
plan: 02
type: execute
wave: 2
depends_on: ["173-01"]
files_modified:
  - el-templo-api/test/tenant-manifest.ts
autonomous: true
requirements: [ADO-02]
must_haves:
  truths:
    - "D-01: existe el inventario COMPLETO de accesos a las 8 tablas del módulo, medido con las DOS lentes (lint por archivo+tabla y SENTINEL_INVENTORY=1 por query en runtime)"
    - "Existe el inventario de accesos de test/ a las 8 tablas, incluidos los 3 puntos ciegos del grep (Drizzle .from, SQL crudo con backticks, sql.raw dinámico)"
    - "Existe el barrido de mocks posicionales (grep -rn '\\.<metodo> = async' test/) para los 23 métodos de members/service.ts"
    - "El header stale del manifiesto (dice 223 tenant-scoped, son 224) queda corregido y contabilizado"
  artifacts:
    - path: ".planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-02-SUMMARY.md"
      provides: "los 4 inventarios que consumen los planes 173-04..173-30 — la lista real de destinos, no la estimada"
    - path: "el-templo-api/test/tenant-manifest.ts"
      provides: "manifiesto con el header sincronizado en 224 tenant-scoped"
  key_links:
    - from: "173-02-SUMMARY.md"
      to: "los planes de migración 173-04..173-20"
      via: "lista de archivo+tabla+método por grupo"
      pattern: "inventario"
---

<objective>
Paso 1 de la receta (doc 07 §2): producir la lista **real** de accesos que esta fase tiene que
migrar, medida con las dos lentes, antes de tocar una línea de `src/`.

Purpose: el criterio de terminado es el inventario del lint, jamás la firma (trampa (a)). Y el
sentinel evalúa por **query** mientras el lint evalúa por **tabla** (trampa (h)): una tabla
strict puede quedar sin filtrar y pasar el sentinel si cualquier otra tabla de la misma query
lleva su filtro. Hacen falta las dos lentes o el switch del plan 173-30 descansa en una sola.
Además `test/` es un cuarto a un tercio del trabajo y su inventario tiene 3 puntos ciegos que
ningún grep de Drizzle ve.

Output: los 4 inventarios en el SUMMARY (src, test, mocks posicionales, exenciones) y el header
del manifiesto sincronizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-CONTEXT.md
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-PATTERNS.md
@.docs/saas-multitenancy/07-receta-adopcion.md

**Ruta de código:** worktree `/home/franco/projects/et-173`. Los SUMMARY se escriben en
`/home/franco/projects/el-templo/.planning/`.

Las 8 tablas del módulo (nombres FÍSICOS de MySQL): `audit_log`, `member_logins`,
`member_notes`, `member_profiles`, `user_branches`, `user_sepa_details`, `user_status_history`,
`users`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventario de src/ con las dos lentes</name>
  <files>ninguno (solo lectura + SUMMARY)</files>
  <read_first>
    - el-templo-api/tenant-lint-allowlist.json (las 450 entradas y el header que documenta el ratchet)
    - el-templo-api/src/db/scripts/lint-tenant.ts (cómo cuenta el motor: por archivo+tabla, incluidas las joineadas)
    - el-templo-api/src/db/sentinel/analyze.ts (por qué el sentinel es una regex por statement y no un parser)
    - el-templo-api/src/db/tenant-tables.ts (GYM_OWNED_TABLES, TENANT_STRICT_MODULES, isStrictTable)
    - .planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-PATTERNS.md (§0.1 las 90, §3 el inventario por archivo)
  </read_first>
  <action>
    Lente A (estática, por archivo+tabla): correr `pnpm lint:tenant` y derivar del JSON de la
    allowlist la lista de las **90** entradas del módulo: las 79 cuyo `table` es una de las 8
    tablas, más las 11 cuyo `file` cae bajo `src/modules/members/` sobre tablas de otros módulos
    (`bookings`, `branches`, `attendance`, `completed_sessions`, `referrals`, `schedules`,
    `subscription_plans`, `subscription_schedules`, `subscriptions`). Las 11 **entran al alcance**
    porque son queries de un archivo del módulo — no contradice D-02, que protege los archivos
    AJENOS. Presupuestar **más de 90**: el piloto planeó 47 y borró 51 por tablas joineadas que el
    motor cuenta como accesos propios.
    Lente B (runtime, por query): correr la suite en modo inventario con
    `SENTINEL_INVENTORY=1 pnpm test -- --no-file-parallelism` y volcar la lista determinística de
    queries violadoras sobre las 8 tablas. Cruzar contra la lente A y **anotar las diferencias en
    los dos sentidos**: una query que el lint ve y el sentinel no (multi-tabla con otra tabla
    filtrada) y un acceso que el sentinel ve y el lint no (fuera de `src/`).
    Para cada uno de los 4 grupos de cirugía mínima ajena (analytics / scheduling-attendance /
    comms-campañas / entrenamiento) y para cada plan de módulo, dejar en el SUMMARY una tabla
    `archivo | tabla | método o línea | qué falta (tenantWhere en WHERE, tenantWhere en ON,
    tenantValues)`.
    Barrer específicamente las búsquedas **por NOMBRE / email / DNI** sin filtro en
    `src/modules/members/` y en `src/modules/shared/member-search.ts`: la query más peligrosa del
    piloto fue la que buscaba "Templo Online" por nombre, y con un solo tenant es invisible.
    Verificar además que no haya `tenantId!` ni `tenantId ?? 1` en el árbol
    (`grep -rnE "tenantId!|tenantId\s*\?\?" el-templo-api/src`).
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm lint:tenant && node -e "const a=require('./tenant-lint-allowlist.json');const T=['users','member_profiles','user_status_history','user_branches','member_notes','user_sepa_details','member_logins','audit_log'];const s=a.entries.filter(e=>T.includes(e.table)||/src\/modules\/members\//.test(e.file));console.log('objetivo del switch:',s.length)"</automated>
  </verify>
  <acceptance_criteria>
    - El SUMMARY lista las **90** entradas objetivo, agrupadas por archivo, con la tabla de cada una
    - El SUMMARY tiene una sección por grupo de plan (173-04..173-20) con `archivo | tabla | método/línea | qué falta`
    - El SUMMARY transcribe el conteo de queries violadoras que devolvió `SENTINEL_INVENTORY=1` y las diferencias contra la lente estática, en los dos sentidos
    - El SUMMARY lista las búsquedas por nombre/email/DNI sin `tenantWhere` encontradas en `src/modules/members/` y `src/modules/shared/member-search.ts`
    - `grep -rnE "tenantId!|tenantId\s*\?\?" el-templo-api/src` no devuelve líneas (o las que devuelve quedan listadas como deuda con dueño)
    - `pnpm lint:tenant` sale con código 0 (la rama todavía no cambió nada)
  </acceptance_criteria>
  <done>Los planes 173-04..173-20 tienen la lista medida de sus destinos, no la estimada.</done>
</task>

<task type="auto">
  <name>Task 2: Inventario de test/ y barrido de mocks posicionales</name>
  <files>ninguno (solo lectura + SUMMARY)</files>
  <read_first>
    - el-templo-api/test/helpers.ts (cleanAllTestData: el DELETE sin filtro sobre ~70 tablas que corre en decenas de beforeEach)
    - el-templo-api/test/setup.ts (provisioning de base por worker, isolate: false)
    - .planning/phases/172-adopci-n-1-piloto-finance/172-13-PLAN.md (cómo el piloto inventarió y clasificó)
    - .planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-PATTERNS.md (§7.1 la regla de decisión, §7.2 los 3 puntos ciegos, §7.4 mocks posicionales)
  </read_first>
  <action>
    Inventariar los accesos de `test/` a las 8 tablas por los **tres** canales, porque
    `tenant-lint-allowlist.json` solo cubre `src/` y el sentinel envuelve el **pool**:
    (1) `.from(schema.X)` de Drizzle;
    (2) **SQL crudo con backticks** — `conn.query("DELETE FROM \`users\`")` y sus variantes; el
        piloto encontró 15 sitios así que ninguna regex de Drizzle vio y que el sentinel cazó al
        primer intento (50 tests rojos de 61);
    (3) nombre de tabla que **no está en el fuente** (`sql.raw(tabla)`, uniones cerradas): se
        marcan y se clasifican **a mano** contra el `.sql` que ejecutan.
    Clasificar cada sitio con la regla del doc 07 §4(d): lee o borra **a propósito** de todos los
    gimnasios → **exención** `/* tenant-safe: <motivo> */`; se puede acotar sin cambiar lo que el
    test prueba → **filtro** `WHERE tenant_id = ?` parametrizado. Los statements sobre tablas que
    **todavía no** son strict **no se anotan** (anotarlas apaga el tripwire del que migre ese
    módulo). Agrupar el resultado en los 4 bloques de endurecimiento (planes 173-21..173-25).
    Barrido de **mocks posicionales**, obligatorio antes de que 173-17 cambie 23 firmas:
    `grep -rn "\.\(listMembers\|searchMembers\|getMemberById\|createMember\|createTrialMember\|createMinimalMember\|convertFreemiumToTrial\|updateLead\|getLeadBranchId\|updateMember\|softDeleteMember\|resetMemberPassword\|checkDniUniqueness\|checkDuplicates\|updatePhoto\|exportMembers\|exportSepaMembers\|getNotes\|createNote\|updateNote\|deleteNote\|getSessionLevelCounts\) = async" test/`
    y el barrido genérico `grep -rn "\. *[a-zA-Z]* = async" test/`. Un mock con el `ctx` agregado
    adelante **rompe en silencio**: el test sigue verde probando nada. Listar cada mock encontrado
    con archivo, línea y método.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && grep -rn "= async" test/ | grep -cE "\.[a-zA-Z]+ = async" || true</automated>
  </verify>
  <acceptance_criteria>
    - El SUMMARY lista los accesos de `test/` a las 8 tablas por los 3 canales, con archivo y línea, y con la clasificación `filtro` / `exención` / `no se anota` de cada uno
    - El SUMMARY tiene los sitios de SQL crudo con backticks listados aparte, con su conteo
    - El SUMMARY lista los sitios de tabla dinámica (`sql.raw`) con la resolución manual de qué tabla ejecutan
    - El SUMMARY lista los mocks posicionales encontrados con archivo, línea y método afectado
    - Los 4 bloques de endurecimiento (173-21..173-25) tienen su lista de archivos asignada, sin solapes
  </acceptance_criteria>
  <done>El bloque de tests tiene su alcance medido antes de empezar, con los 3 puntos ciegos cubiertos y los mocks posicionales identificados.</done>
</task>

<task type="auto">
  <name>Task 3: Sincronizar el header stale del manifiesto</name>
  <files>el-templo-api/test/tenant-manifest.ts</files>
  <read_first>
    - el-templo-api/test/tenant-manifest.ts (el header con los conteos y el objeto TENANT_MANIFEST)
    - el-templo-api/test/tenancy/iso-01-manifiesto.test.ts (verificar si tiene un baseline numérico que también quedó stale)
  </read_first>
  <action>
    El header de `test/tenant-manifest.ts` declara **223** rutas `tenant-scoped`; medido hoy son
    **224** — `POST /api/admin/members/:userId/referrals` entró con `e1952606` y ya está en staging.
    Corregir el header a 372 rutas / 224 `tenant-scoped` / 141 `templo-module` / 7 `global`.
    Revisar `test/tenancy/iso-01-manifiesto.test.ts`: si tiene un baseline numérico que también
    quedó stale, actualizarlo **contabilizando la unidad ganada** por una aserción que explique de
    dónde sale (regla del doc 07 §6: bajar o subir un número pelado está prohibido).
    Confirmar por conteo que los tres prefijos del módulo suman **30** rutas `tenant-scoped`
    (`/api/admin/members` 24 + `/api/admin/users` 5 + `/api/admin/leads` 1) y dejarlo escrito: es
    el `CASOS_BASELINE` del gate de cobertura del plan 173-29.
    Commit: `chore(173-02): sincronizar el header del manifiesto con las 224 rutas tenant-scoped`.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec vitest run --no-file-parallelism test/tenancy/iso-01-manifiesto.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '223' el-templo-api/test/tenant-manifest.ts` devuelve `0` en el bloque del header (o el 223 remanente no describe el conteo de tenant-scoped)
    - `pnpm exec vitest run --no-file-parallelism test/tenancy/iso-01-manifiesto.test.ts` sale con código 0
    - Un `node -e` que parsee `TENANT_MANIFEST` y cuente `categoria === "tenant-scoped"` devuelve `224`
    - El mismo conteo restringido a `url === P || url.startsWith(P + "/")` para los tres prefijos devuelve `30`
    - El SUMMARY deja escrito el listado de las 30 rutas, que es el insumo del plan 173-29
  </acceptance_criteria>
  <done>El manifiesto declara los números que tiene, y las 30 rutas del módulo están enumeradas para la batería.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                         |
| --------------------------------------- | ----------------------------------------------------------------------------------- |
| inventario estático → decisión          | un inventario incompleto deja accesos sin migrar y el switch los convierte en throw |
| sentinel (por query) → lint (por tabla) | dos lentes con puntos ciegos distintos; confiar en una sola es mentirse             |

## STRIDE Threat Register

| Threat ID   | Category               | Component                         | Disposition | Mitigation Plan                                                                                                                                                 |
| ----------- | ---------------------- | --------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-173-02-01 | Information Disclosure | queries multi-tabla sobre `users` | mitigate    | inventario cruzado lint×sentinel con las diferencias anotadas en los dos sentidos: el sentinel marca `ok` una query entera si cualquier tabla lleva `tenant_id` |
| T-173-02-02 | Information Disclosure | búsquedas por nombre/email/DNI    | mitigate    | barrido específico de esas queries: un id ajeno no matchea, pero un NOMBRE que todo gimnasio tiene devuelve la fila del primero                                 |
| T-173-02-03 | Repudiation            | mocks posicionales de test        | mitigate    | barrido `\.<metodo> = async` antes de cambiar 23 firmas: un mock con `ctx` corrido queda verde probando nada                                                    |
| T-173-02-04 | Tampering              | `test/tenant-manifest.ts`         | mitigate    | el header se sincroniza con el conteo real y la unidad ganada queda contabilizada por una aserción, no bajada a mano                                            |

</threat_model>

<verification>
- Los 4 inventarios (src, test, mocks, exenciones) están en el SUMMARY con archivo y línea.
- El objetivo del switch está medido: 90 entradas, esperando más.
- El manifiesto declara 224 `tenant-scoped` y las 30 rutas del módulo están enumeradas.
- `pnpm lint:tenant` sigue verde (esta fase todavía no cambió `src/`).
</verification>

<success_criteria>
Cada plan de migración de la fase arranca con su lista medida de destinos, y el bloque de
tests tiene su alcance real —incluidos los 3 puntos ciegos del grep— antes de empezar.
</success_criteria>

<output>
Create `.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-02-SUMMARY.md` when done
</output>
