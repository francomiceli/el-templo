---
phase: 173-adopci-n-2-members-guarda-de-consistencia-de-anclas
plan: 19
type: execute
wave: 6
depends_on: ["173-18"]
files_modified:
  - el-templo-api/src/modules/members/service.ts
autonomous: true
requirements: [ADO-02]
must_haves:
  truths:
    - "Todas las queries de LECTURA y agregación de `members/service.ts` filtran por gimnasio: listado, búsqueda, ficha, duplicados, exports, notas y conteos"
    - "Las 11 entradas de allowlist por ARCHIVO (§0.1: `bookings`, `branches`, `attendance`, `completed_sessions`, `referrals`, `schedules`, `subscription_plans`, `subscription_schedules`, `subscriptions`) también quedan cubiertas — el criterio del doc 07 §0.3 es tabla O archivo"
    - "El docblock de `listMembers` deja de decir que las tablas del listado 'se migran en su propia fase': ya no es cierto"
    - "El `total` del listado paginado y `totalDebtByCurrency` filtran igual que las filas: son queries distintas"
  artifacts:
    - path: "el-templo-api/src/modules/members/service.ts"
      provides: "lecturas y agregados del módulo de socios scopeados, con el constructor de filtros llevando el tenantWhere como primer elemento"
      contains: "tenantWhere"
  key_links:
    - from: "el-templo-api/src/modules/members/service.ts (listMembers / exportMembers)"
      to: "schema.users y las tablas joineadas del listado"
      via: "constructor de condiciones con tenantWhere como primer elemento del array"
      pattern: "tenantWhere"
---

<objective>
Paso 4 de la receta, mitad lecturas: migrar el listado, la búsqueda, la ficha, los duplicados, los
exports, las notas y los conteos — incluidas las **11 entradas por archivo** que el criterio §0.3
del doc 07 obliga a borrar.

Purpose: acá está el drift más importante del CONTEXT. La definición de "adoptado" es
_"`tenant-lint-allowlist.json` no tiene una sola entrada con `file` bajo el módulo **ni** con
`table` entre sus tablas strict"_: son **dos** criterios unidos por O. Las 79 son solo el criterio
de tabla; `members/routes.ts` y `members/service.ts` tienen **11 entradas más** sobre tablas de
otros módulos (`subscriptions`, `subscription_plans`, `schedules`, `bookings`, `attendance`,
`completed_sessions`, `referrals`, `branches`). Esas 11 **están dentro del alcance** —son queries de
un archivo del módulo— y **no** contradicen D-02, que protege los archivos AJENOS.

Y acá está la trampa (a) escrita por el piloto: `listMembers` **tiene `ctx`** y solo filtra
`balances`. Migrarlo incluye **reescribir su docblock**.

Output: `members/service.ts` sin una sola entrada de allowlist, por ninguno de los dos criterios.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-CONTEXT.md
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-PATTERNS.md
@.docs/saas-multitenancy/07-receta-adopcion.md

**Ruta de código:** worktree `/home/franco/projects/et-173`.
**Allowlist de trabajo:** `/tmp/allowlist-173-19.json`.

**Métodos de lectura:** `listMembers`, `searchMembers`, `getMemberById`, `checkDniUniqueness`,
`checkDuplicates`, `exportMembers`, `exportSepaMembers`, `getNotes`, `getSessionLevelCounts`,
`getLeadBranchId`, `wasEverLead`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: listMembers y exportMembers — el constructor de filtros y las tablas joineadas</name>
  <files>el-templo-api/src/modules/members/service.ts</files>
  <read_first>
    - el-templo-api/src/modules/members/service.ts (:80-98 el docblock de `listMembers` y todo el método; `exportMembers` y `exportSepaMembers`; cómo arman sus condiciones y qué tablas joinean)
    - el-templo-api/src/modules/finance/transaction-service.ts (`buildListConditions`: el helper que devuelve `SQL[]` con el `tenantWhere` como PRIMER elemento, y por qué no se duplica en el llamador — doc 07 §4(b))
    - el-templo-api/src/modules/finance/cash-register-service.ts (:446-465 — el filtro de la tabla joineada va en el ON, con el porqué escrito adentro)
    - .planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-02-SUMMARY.md (las 11 entradas por archivo, con su tabla)
  </read_first>
  <action>
    `listMembers`, `exportMembers` y `exportSepaMembers` comparten la construcción de filtros. Son los
    dos candidatos naturales al patrón §4(b): un helper que reciba `ctx` y devuelva `SQL[]` con el
    `tenantWhere` como **primer elemento del array**, para que la próxima condición que alguien agregue
    **nazca scopeada**. Si se hace así, **no se duplica en el llamador**: daría el mismo SQL pero
    mataría la garantía.
    Las tablas joineadas del listado (`subscriptions`, `subscription_plans`, `subscription_schedules`,
    `branches`, `schedules`, `bookings`, `attendance`, `completed_sessions`, `referrals`, `balances`)
    llevan su filtro **en el `ON`**, también en INNER. ⚠ En un LEFT JOIN, ponerlo en el WHERE lo
    convierte en INNER y **borra filas en silencio con el lint en verde**: en este listado eso
    significa socios sin suscripción, sin reservas o sin deuda **desapareciendo de la ficha del
    staff**. `balances` ya está filtrado desde la 172 — no duplicar.
    ⚠ **El `total` del listado se afirma y se filtra aparte de las filas**: son **dos queries**. Y
    `totalDebtByCurrency` es una **tercera**. Un filtro que viva en una sola deja al staff viendo 3
    filas y un total de 6. Migrar las tres.
    **Reescribir el docblock de `listMembers`**: ya no es cierto que "el resto de las tablas del
    listado se migra en su propia fase". Documentar qué scopea ahora y cuáles de sus joins llevan el
    filtro en el `ON` y por qué.
    Los exports son las rutas que más datos entregan de una vez: revisar que **todas** sus columnas
    salgan de tablas filtradas, incluidas las de `user_sepa_details` en `exportSepaMembers`.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec tsc -p tsconfig.test-check.json --noEmit && pnpm exec vitest run --no-file-parallelism test/members</automated>
  </verify>
  <acceptance_criteria>
    - Si se usó el patrón §4(b), el `tenantWhere` es el elemento `[0]` del array devuelto y NO aparece duplicado en `listMembers` / `exportMembers` / `exportSepaMembers`
    - Todo `leftJoin` de estos tres métodos tiene su filtro dentro del `and(...)` del `ON`, y ninguno lo tiene en el `.where(...)` (verificado join por join y listado en el SUMMARY)
    - Las tres queries del listado (filas, `total`, `totalDebtByCurrency`) llevan el filtro de gimnasio
    - El docblock de `listMembers` ya no dice que las tablas del listado se migran en otra fase, y describe qué scopea y dónde
    - `pnpm exec tsc -p tsconfig.test-check.json --noEmit` sale con código 0
    - `pnpm exec vitest run --no-file-parallelism test/members` sale con código 0
  </acceptance_criteria>
  <done>El listado, sus dos agregados y los dos exports solo pueden devolver socios del gimnasio del `ctx`, sin perder filas por un join mal filtrado.</done>
</task>

<task type="auto">
  <name>Task 2: Búsqueda, ficha, duplicados, notas y conteos — y el cierre de las 11 entradas por archivo</name>
  <files>el-templo-api/src/modules/members/service.ts</files>
  <read_first>
    - el-templo-api/src/modules/members/service.ts (`searchMembers`, `getMemberById`, `checkDniUniqueness`, `checkDuplicates`, `getNotes`, `getSessionLevelCounts`, `getLeadBranchId`, `wasEverLead`)
    - el-templo-api/src/modules/finance/coach-load-routes.ts (:288-321 — el comentario sobre por qué la búsqueda por NOMBRE es la peligrosa)
    - .planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-02-SUMMARY.md (las búsquedas por nombre/email/DNI listadas)
  </read_first>
  <action>
    - `searchMembers` y `checkDuplicates` buscan **por nombre, email y DNI**. Es la clase de query que
      más caro salió en el piloto: un id ajeno no matchea, pero un **nombre** que todo gimnasio va a
      tener devuelve la fila del **primer gimnasio que la tenga**, silenciosamente. `tenantWhere`
      inline, y un comentario de bloque que lo explique.
    - `getMemberById` y `getNotes`: `tenantWhere` inline, y filtro en el `ON` de cada tabla joineada
      de la ficha.
    - `checkDniUniqueness`: mismo criterio que los SELECT de unicidad del plan 173-18 — un DNI repetido
      en otro gimnasio no puede dar conflicto ni delatar que existe.
    - `getSessionLevelCounts`: es un **agregado**; el filtro va en el statement que nombra la tabla, y
      si el statement toca dos tablas del módulo, van dos `tenantWhere`.
    - `getLeadBranchId` y `wasEverLead`: `tenantWhere` inline.
    **Cierre del criterio §0.3 (tabla O archivo):** correr el lint contra
    `/tmp/allowlist-173-19.json` y verificar que `src/modules/members/service.ts` no tiene **ninguna**
    entrada, ni por tabla del módulo ni por tabla ajena. Las 9 entradas por archivo que le
    corresponden al service (`attendance`, `bookings`, `branches`, `completed_sessions`, `referrals`,
    `schedules`, `subscription_plans`, `subscription_schedules`, `subscriptions`) tienen que estar
    todas cubiertas por las tareas 1 y 2. Si sobra alguna, migrarla acá: **es de este archivo**, no de
    otro módulo. Si aparecen entradas **nuevas** por tablas joineadas que el motor cuenta como accesos
    propios, también se migran — el piloto planeó 47 y borró 51 exactamente por esto.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec tsc -p tsconfig.test-check.json --noEmit && pnpm lint:tenant --allowlist /tmp/allowlist-173-19.json && pnpm exec vitest run --no-file-parallelism test/members test/leads</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm lint:tenant --allowlist /tmp/allowlist-173-19.json` reporta **CERO** entradas cuyo `file` sea `el-templo-api/src/modules/members/service.ts`, por cualquier tabla
    - Toda búsqueda por nombre/email/DNI del archivo lleva `tenantWhere` en el mismo statement, con un comentario de bloque que explica por qué el nombre es el caso peligroso
    - Todo statement que nombre dos tablas del módulo lleva dos `tenantWhere`
    - `grep -rnE "tenantId!|tenantId\s*\?\?" el-templo-api/src/modules/members/service.ts` no devuelve líneas
    - El SUMMARY lista las 9 entradas por archivo del service y cómo quedó cada una, más cualquier entrada colateral nueva que apareció
    - `pnpm exec vitest run --no-file-parallelism test/members test/leads` sale con código 0
  </acceptance_criteria>
  <done>`members/service.ts` no tiene una sola excusa en la allowlist de trabajo, por ninguno de los dos criterios.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                                                 |
| ------------------------------ | ------------------------------------------------------------------------------------------- |
| texto de búsqueda → `users`    | el nombre/email/DNI viene del staff y hoy matchea sobre todos los gimnasios                 |
| exports → archivo descargado   | `/members/export` y `/export-sepa` entregan el padrón completo de una vez, con DNIs e IBANs |
| `total` y agregados → decisión | un total sin filtrar deja al staff con 3 filas y un total de 6                              |
| LEFT JOIN → filas de la ficha  | un filtro en el WHERE borra socios sin sub, sin reserva o sin deuda, en silencio            |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                      | Disposition | Mitigation Plan                                                                                                  |
| ----------- | ---------------------- | ---------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------- |
| T-173-19-01 | Information Disclosure | `searchMembers` / `checkDuplicates` por nombre | mitigate    | `tenantWhere` inline: un nombre común deja de devolver la fila del primer gimnasio que lo tenga                  |
| T-173-19-02 | Information Disclosure | `exportMembers` / `exportSepaMembers`          | mitigate    | filtro en todas las tablas del export, incluida `user_sepa_details` (IBANs)                                      |
| T-173-19-03 | Information Disclosure | `total` y `totalDebtByCurrency`                | mitigate    | las tres queries del listado llevan el filtro: son queries distintas y una sola filtrada delata volumen ajeno    |
| T-173-19-04 | Tampering              | LEFT JOIN de la ficha                          | mitigate    | filtro en el `ON`, verificado join por join; en el WHERE borraría socios reales del listado del staff            |
| T-173-19-05 | Information Disclosure | `checkDniUniqueness` (409)                     | mitigate    | `tenantWhere` en el SELECT de unicidad: el conflicto solo puede venir del propio gimnasio                        |
| T-173-19-06 | Repudiation            | docblock stale de `listMembers`                | mitigate    | se reescribe: un docblock que dice "todavía no filtra" sobre un archivo ya migrado es peor que no tener docblock |

</threat_model>

<verification>
- `pnpm lint:tenant --allowlist /tmp/allowlist-173-19.json` con **cero** entradas de `members/service.ts`.
- `pnpm exec tsc -p tsconfig.test-check.json --noEmit` verde.
- `pnpm exec vitest run --no-file-parallelism test/members test/leads` verde.
- El SUMMARY lista los joins con la ubicación de su filtro y las entradas colaterales aparecidas.
</verification>

<success_criteria>
El service de socios está adoptado por los dos criterios de la definición operativa: ni una entrada
por tabla del módulo ni una entrada por archivo, con el listado, la búsqueda, la ficha y los
exports devolviendo exactamente lo mismo que antes para El Templo.
</success_criteria>

<output>
Create `.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-19-SUMMARY.md` when done
</output>
