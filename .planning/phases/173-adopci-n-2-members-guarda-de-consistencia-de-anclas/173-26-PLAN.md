---
phase: 173-adopci-n-2-members-guarda-de-consistencia-de-anclas
plan: 26
type: execute
wave: 12
depends_on: ["173-25"]
files_modified:
  - el-templo-api/test/fixtures/members-gimnasio-dos.ts
  - el-templo-api/test/tenancy/iso-03-members-listados.test.ts
autonomous: true
requirements: [ADO-02]
must_haves:
  truths:
    - "D-09: las 9 rutas de listado, búsqueda y export de `/api/admin/members` tienen caso de aislamiento + control positivo, con el nombre del describe conteniendo LITERAL la clave del manifiesto"
    - "D-06: cero `403` esperados en el archivo — un recurso ajeno es indistinguible del inexistente"
    - "Las 4 precondiciones están escritas y cada una mata un falso verde distinto; la primera (mismo país en las dos sedes) es la que D-14 vuelve crítica"
    - "El `total` del listado y `totalDebtByCurrency` se afirman APARTE de las filas: son tres queries"
  artifacts:
    - path: "el-templo-api/test/fixtures/members-gimnasio-dos.ts"
      provides: "siembra y limpieza de socios, perfiles, notas, historial y SEPA del gimnasio 2, reusable por 173-27 y 173-28"
      exports:
        [
          "sembrarSociosGimnasioDos",
          "limpiarSociosDeLaBateria",
          "tenantDeLaFila",
          "campoDeLaFila",
        ]
    - path: "el-templo-api/test/tenancy/iso-03-members-listados.test.ts"
      provides: "9 describes con caso de aislamiento + control positivo"
      min_lines: 600
  key_links:
    - from: "el-templo-api/test/tenancy/iso-03-members-listados.test.ts"
      to: "el-templo-api/test/tenant-manifest.ts"
      via: "el nombre de cada describe contiene LITERAL la clave `<MÉTODO> <url>`"
      pattern: "GET /api/admin/members"
---

<objective>
Paso 6 de la receta, plan 1 de 3: el fixture del gimnasio 2 y la batería de las **9 rutas de
listado, búsqueda y export**.

Purpose: es donde más datos se entregan de una vez. `GET /api/admin/members` devuelve el padrón
paginado con su total y su deuda agregada; `/export` y `/export-sepa` entregan el padrón completo en
un xlsx, con DNIs e IBANs. Si algo se filtra, se filtra acá y en volumen.

El nombre del test **es** el registro de cobertura: un array `RUTAS_CUBIERTAS` paralelo se
desincroniza y a partir de ahí el gate defiende la lista en vez de la batería.

Output: `test/fixtures/members-gimnasio-dos.ts` (reusable por 173-27 y 173-28) y el primer archivo
de la batería.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-CONTEXT.md
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-PATTERNS.md
@.docs/saas-multitenancy/07-receta-adopcion.md

**Ruta de código:** worktree `/home/franco/projects/et-173`.

**Las 9 rutas de este archivo:** `GET /api/admin/members`, `GET /api/admin/members/search`,
`GET /api/admin/members/branches`, `GET /api/admin/members/check-dni`,
`GET /api/admin/members/check-duplicates`, `GET /api/admin/members/export`,
`GET /api/admin/members/export-sepa`, `GET /api/admin/members/:userId`,
`GET /api/admin/members/:userId/session-levels`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: El fixture members-gimnasio-dos.ts</name>
  <files>el-templo-api/test/fixtures/members-gimnasio-dos.ts</files>
  <read_first>
    - el-templo-api/test/fixtures/finance-gimnasio-dos.ts (el gemelo exacto: estructura, `tenantDeLaFila`, `campoDeLaFila`, `sembrar…`, `limpiar…`, constantes irrepetibles)
    - el-templo-api/test/fixtures/second-tenant.ts (`TENANT_DOS = 90671`, `TENANT_TEMPLO = 1`, `seedSecondTenant` con `adminToken`, `coachToken`, `branchId`, `planId` y 2 socios con token; `limpiarSegundoGimnasio` FK-ordenado)
    - el-templo-api/test/tenancy/iso-03-finance-cajas.test.ts (:132-162 el ciclo de vida; :225-236 la lectura de evidencia)
  </read_first>
  <action>
    Crear `test/fixtures/members-gimnasio-dos.ts`, gemelo de `finance-gimnasio-dos.ts`.
    Los 2 socios que `seedSecondTenant` ya crea alcanzan para "el otro socio del MISMO gimnasio
    tampoco", pero la ficha necesita además, en el gimnasio 2: **notas** (`member_notes`), **perfil**
    (`member_profiles`), **historial de estado** (`user_status_history`), **logins**
    (`member_logins`) y —para `export-sepa`— **`user_sepa_details`**. Sembrarlos.
    **Valores irrepetibles y de otro orden de magnitud** en todo lo que después se afirme como
    agregado o como contenido de export (nombres, DNIs, importes de deuda): con valores repetidos, un
    total contaminado puede dar el mismo número que el correcto.
    Exportar `tenantDeLaFila(app, tabla, id)` y `campoDeLaFila(app, tabla, id, columna)` que lean
    **sin filtro de gimnasio**, con exención `/* tenant-safe: ... */` embebida y motivo: leer el
    `tenant_id` de la fila **es** la aserción, y filtrarla la volvería tautológica.
    `limpiarSociosDeLaBateria` en orden FK-seguro, con conexión única del pool y
    `FOREIGN_KEY_CHECKS = 0`.
    ⚠ El fixture `second-tenant.ts` se **consume**, no se rediseña (regla lockeada de la 171).
    ⚠ **Onboarding del gimnasio nuevo:** todo gimnasio necesita su propia sede virtual "Templo
    Online" o `resolveUserBranchId` cae en un fallback por nombre. Si el fixture del gimnasio 2 no la
    tiene, agregarla acá y anotarlo en el SUMMARY como insumo del checklist de onboarding (doc 07
    §1.4), no como fix de la batería.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec tsc -p tsconfig.test-check.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f el-templo-api/test/fixtures/members-gimnasio-dos.ts` sale 0
    - El fixture siembra en `TENANT_DOS`: socios, `member_profiles`, `member_notes`, `user_status_history`, `member_logins` y `user_sepa_details`
    - `tenantDeLaFila` y `campoDeLaFila` leen sin filtro de gimnasio y llevan exención `tenant-safe` con motivo
    - Los valores sembrados que después se afirman son irrepetibles (listados en el SUMMARY)
    - `limpiarSociosDeLaBateria` usa conexión única con `FOREIGN_KEY_CHECKS = 0` y orden FK-seguro
    - El gimnasio 2 tiene su propia sede virtual "Templo Online"
    - `pnpm exec tsc -p tsconfig.test-check.json --noEmit` sale con código 0
  </acceptance_criteria>
  <done>Existe la siembra del gimnasio 2 que los tres archivos de la batería van a consumir.</done>
</task>

<task type="auto">
  <name>Task 2: iso-03-members-listados.test.ts — las 9 rutas</name>
  <files>el-templo-api/test/tenancy/iso-03-members-listados.test.ts</files>
  <read_first>
    - el-templo-api/test/tenancy/iso-03-finance-cajas.test.ts (1.153 líneas, el idioma completo: ciclo de vida :132-162, precondiciones :242-310, el par canónico aislamiento+control :365-405, el barrido de listados :225-236, y el parseo de xlsx con exceljs por índice fijo :459-470)
    - el-templo-api/test/fixtures/members-gimnasio-dos.ts (la tarea 1)
    - el-templo-api/test/tenant-manifest.ts (las claves LITERALES `<MÉTODO> <url>` de las 9 rutas)
    - el-templo-api/src/modules/members/routes.ts (qué rol mínimo acepta cada una de las 9 rutas)
  </read_first>
  <action>
    Un `describe` por ruta, y el nombre **contiene LITERAL la clave del manifiesto** (`<MÉTODO> <url>`,
    con la prosa adelante separada por ` — `). El nombre del test **es** el registro de cobertura.
    **Ciclo de vida obligado** (el orden no es cosmético): `cleanAllTestData` →
    `limpiarSociosDeLaBateria` (ANTES de `seedSecondTenant`, que arranca borrando la fila de `tenants`
    del gimnasio 2) → `seedSecondTenant` → siembra de El Templo → siembra del gimnasio 2. `afterAll`
    con las tres limpiezas + `app.close()`: la base la comparten los archivos del worker
    (`isolate: false`).
    **Las 4 precondiciones**, cada una mata un falso verde distinto:
    | las dos sedes comparten **país** | si no, el aislamiento lo da el country scope y la tenancy no se ejerce — D-14 lo vuelve crítico |
    | El Templo tiene socios vivos | sin recurso ajeno, "no ve nada ajeno" es trivialmente cierto |
    | las filas del gimnasio 2 nacieron **ahí** | si la siembra cayó en el `DEFAULT 1`, los controles miran datos ajenos |
    | los valores sembrados son **irrepetibles** | un total contaminado puede dar el mismo número que el correcto |
    **Dos `it` por `describe`:** el caso de **aislamiento** y su **control positivo** (el control mata
    "el caso pasó porque la ruta está rota para todos").
    **Reglas de aserción, todas salidas de casos reales:**
    - El barrido de un listado **no** se afirma con "no aparece el id que sembré", sino leyendo el
      `tenant_id` de **cada fila devuelta**: así caza también filas ajenas que el test no sembró
      (seeds de migraciones y de `test/setup.ts`).
    - `GET /api/admin/members` devuelve `{ members, total, totalDebtByCurrency }` → **tres**
      aserciones, no una: son tres queries y un filtro que viva en una sola deja al staff viendo 3
      filas y un total de 6.
    - Los **exports** se parsean con `exceljs` y se afirman **por contenido**, leyendo columnas por
      índice fijo. `/export-sepa` entrega IBANs: afirmarlos explícitamente.
    - `check-dni` y `check-duplicates`: el caso de aislamiento es que un DNI **existente en el otro
      gimnasio** dé "no existe / sin duplicados". El **motivo del rechazo se afirma junto con el
      status**: una ruta con dos formas de decir "no encontrado" puede pasar por el motivo equivocado.
    - `GET /members/branches`: la lista **no** puede contener sedes de El Templo.
    **Rol mínimo real:** el actor de cada `describe` es el rol **más barato que la ruta acepta y que el
    fixture puede crear**, explícito en cada call site (no escondido en un helper). `/search`,
    `/check-dni` y `/check-duplicates` casi seguro aceptan `recepcion`/`coach`: en el piloto, las 7
    rutas probadas con token de **coach** destaparon los 2 hallazgos de seguridad.
    **Mensajes de rojo que nombran el archivo y el método a mirar** (como
    `porQueImportaElListado` de finance).
    ⚠ **Cero `403` esperados** — y **no lo "aclares" escribiendo el número en un comentario**: un gate
    por substring no distingue código de comentario. Describilo en castellano.
    ⚠ El throw del sentinel llega envuelto en `DrizzleQueryError.cause`.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec vitest run --no-file-parallelism test/tenancy/iso-03-members-listados.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run --no-file-parallelism test/tenancy/iso-03-members-listados.test.ts` sale con código 0
    - El archivo tiene **9** `describe` cuyos nombres contienen literal las 9 claves del manifiesto (verificado por grep de cada clave)
    - Cada `describe` tiene exactamente 2 `it`: aislamiento y control positivo
    - Existen los 4 `it` de precondición
    - `grep -c "toBe(403)\|toEqual(403)\|403" el-templo-api/test/tenancy/iso-03-members-listados.test.ts` devuelve `0`
    - El `describe` de `GET /api/admin/members` afirma por separado `members`, `total` y `totalDebtByCurrency`
    - Los dos exports se parsean con `exceljs` y se afirman por contenido con columnas por índice fijo
    - El rol usado en cada `describe` está explícito en el call site (no en un helper) y listado en el SUMMARY
    - Mutación de cierre: romper el `tenantWhere` del constructor de filtros de `listMembers` pone en rojo los describes esperados; **también se anotan los que NO se pusieron rojos** y por qué (puede haber dos filtros independientes sosteniendo el aislamiento). Mutación revertida, `git status --porcelain` vacío
  </acceptance_criteria>
  <done>Las 9 rutas que más datos entregan tienen caso y control, con la evidencia leída de la base y la mutación documentada en los dos sentidos.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                                      |
| -------------------------------- | -------------------------------------------------------------------------------- |
| staff del gimnasio 2 → API       | el actor de la batería: token real de otro gimnasio pegándole a rutas de socios  |
| export xlsx → archivo descargado | `/export-sepa` entrega IBANs y DNIs del padrón completo de una vez               |
| país de la sede → aislamiento    | si las sedes tienen países distintos, la batería da verde sin ejercer la tenancy |
| status del rechazo → existencia  | un 403 confirmaría que el recurso existe en otro gimnasio                        |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                 | Disposition | Mitigation Plan                                                                                                        |
| ----------- | ---------------------- | ----------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------- |
| T-173-26-01 | Information Disclosure | `GET /api/admin/members` (filas)          | mitigate    | barrido sobre **cada fila devuelta** leyendo su `tenant_id` de la base, no "no aparece el id que sembré"               |
| T-173-26-02 | Information Disclosure | `total` y `totalDebtByCurrency`           | mitigate    | tres aserciones separadas: son tres queries y una sola filtrada delata volumen y deuda ajenos                          |
| T-173-26-03 | Information Disclosure | `/export` y `/export-sepa`                | mitigate    | parseo con exceljs y aserción por contenido, incluidos los IBANs                                                       |
| T-173-26-04 | Information Disclosure | `check-dni` / `check-duplicates`          | mitigate    | un DNI del otro gimnasio da "no existe", con el **motivo** afirmado junto al status                                    |
| T-173-26-05 | Information Disclosure | `GET /members/branches`                   | mitigate    | la lista no puede contener sedes ajenas: es la puerta de entrada a la divergencia de anclas                            |
| T-173-26-06 | Elevation of Privilege | rol mínimo de cada ruta                   | mitigate    | actor explícito por `describe` con el rol más barato que la ruta acepta: en el piloto fue lo que destapó los hallazgos |
| T-173-26-07 | Repudiation            | falso verde por país / siembra al DEFAULT | mitigate    | 4 precondiciones ejecutables, cada una matando un falso verde distinto                                                 |

</threat_model>

<verification>
- El archivo sale verde con las 9 claves del manifiesto presentes literal.
- `grep -c "403"` en cero.
- Las 4 precondiciones existen como `it`.
- La mutación de cierre está documentada en los dos sentidos y revertida.
</verification>

<success_criteria>
Un staff del gimnasio 2 no puede ver, buscar ni exportar un solo socio de El Templo por ninguna de
las 9 rutas de listado, y cada caso tiene su control positivo que prueba que la ruta funciona.
</success_criteria>

<output>
Create `.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-26-SUMMARY.md` when done
</output>
