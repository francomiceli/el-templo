---
phase: 173-adopci-n-2-members-guarda-de-consistencia-de-anclas
plan: 14
type: execute
wave: 5
depends_on: ["173-05", "173-11", "173-13"]
files_modified:
  - el-templo-api/src/modules/subscriptions/service.ts
  - el-templo-api/test/tenancy/iso-03-finance-coach-load.test.ts
autonomous: true
requirements: [ADO-02, ADO-07]
must_haves:
  truths:
    - "D-13: `getMemberSubscription` recibe `ctx` y filtra, y `assignPlan` estampa `tenantValues` en su insert de `subscriptions` — las dos deudas que bloquean a un gimnasio nuevo quedan cerradas"
    - "D-13: el `it` autodestructivo de `iso-03-finance-coach-load.test.ts:1326` se convierte en el control positivo (201 + sub y charge en `TENANT_DOS`) y la nota de limitación se borra"
    - "D-15 / WR-02: el guard `SUB_HAS_ACTIVE_TRANSACTIONS` lleva `tenantWhere(financialTransactions)` **en el ON** del join, así que los campos del 409 ya no pueden venir de otro gimnasio"
    - "D-05: los 3 sitios de `update(users).set({branchId, branchUpdatedAt})` de este archivo (:1669, :3595, :4981) pasan por el helper de anclas"
    - "D-02: el resto de `subscriptions` NO se migra en esta fase — es de la 174"
  artifacts:
    - path: "el-templo-api/src/modules/subscriptions/service.ts"
      provides: "las 4 deudas heredadas de la 172 cerradas, con alcance acotado a D-13/D-15/D-05"
      contains: "tenantValues"
    - path: "el-templo-api/test/tenancy/iso-03-finance-coach-load.test.ts"
      provides: "el ancla autodestructiva convertida en control positivo"
  key_links:
    - from: "el-templo-api/src/modules/subscriptions/service.ts"
      to: "schema.financialTransactions"
      via: "tenantWhere dentro del ON del innerJoin del guard de cobros vivos"
      pattern: "innerJoin\\([\\s\\S]*tenantWhere\\(schema\\.financialTransactions"
---

<objective>
Cerrar las deudas heredadas de la 172 que viven en `subscriptions/service.ts` (D-13, D-15/WR-02) y
los 3 sitios de escritura de ancla del archivo (D-05), **sin** migrar el resto del módulo.

Purpose: las dos deudas de D-13 **bloquean a un gimnasio nuevo**: `getMemberSubscription` filtra a
un socio ajeno en `/api/admin/finance/coach-load/autocompletar/:userId`, y `assignPlan` inserta la
suscripción sin `tenantValues`, así que un alta en el gimnasio 2 nace en el gimnasio 1 y después el
cobro no encuentra al socio. El test-ancla ya les puso dueño: esta fase. Y WR-02 hace que los
campos de una tabla strict **no filtrada** se serialicen en el body de un 409.

Al arreglar D-13, la aserción "esperaba fallar" de `iso-03-finance-coach-load.test.ts:1326` **se
pone en rojo**: es autodestructiva por diseño y el propio mensaje de error da la instrucción de
convertirla en el control positivo.

Output: las 4 deudas cerradas, el ancla desmarcada, y `subscriptions` intacto para la fase 174.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-CONTEXT.md
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-PATTERNS.md
@.planning/phases/172-adopci-n-1-piloto-finance/172-REVIEW.md

**Ruta de código:** worktree `/home/franco/projects/et-173`.
**Allowlist de trabajo:** `/tmp/allowlist-173-14.json`.

**Entradas objetivo (2):** `subscriptions/service.ts` → `user_status_history`, `users`.

⚠ El archivo ya fue tocado por los planes 173-04 (call site de audit-log) y 173-05 (call site de
covered-until). Partir del estado actual de la rama.
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-13 — getMemberSubscription con ctx y assignPlan con tenantValues</name>
  <files>el-templo-api/src/modules/subscriptions/service.ts</files>
  <read_first>
    - el-templo-api/src/modules/subscriptions/service.ts (:919-921 la firma de `getMemberSubscription` sin `ctx`; :1253 `assignPlan`; :1592 su insert a `subscriptions` sin `tenantValues`; :624 el ÚNICO insert del archivo que sí usa `tenantValues` — el idioma a copiar)
    - el-templo-api/src/modules/finance/coach-load-routes.ts (el handler de `/autocompletar/:userId`: dónde sale el `ctx` y cómo se le pasa)
    - .planning/phases/172-adopci-n-1-piloto-finance/172-REVIEW.md (IN-03: la deuda nombrada con dueño 173)
    - el-templo-api/src/modules/shared/tenant.ts
  </read_first>
  <action>
    `getMemberSubscription(ctx: TenantContext, userId: number, ...)`: `ctx` PRIMERO, y `tenantWhere`
    **inline** en cada statement del método que nombre una tabla strict. Actualizar sus call sites —
    el compilador los nombra; el principal es el handler de
    `/api/admin/finance/coach-load/autocompletar/:userId`, que ya tiene `ctx` desde la 172.
    `assignPlan`: el insert de `:1592` pasa a `tx.insert(schema.subscriptions).values(tenantValues(ctx,
    {...}))`. ⚠ **Alcance acotado a D-13**: el archivo tiene 5 inserts a `subscriptions` (`:1592`,
    `:3434`, `:3876`, `:4333`, `:4587`) y solo `:624` usa `tenantValues` hoy. **Este plan toca el de
    `:1592`** porque es el que bloquea el alta en un gimnasio nuevo. Los otros son de la fase 174:
    dejarlos con un comentario de bloque que los nombre como deuda **con dueño explícito (fase 174)**,
    en vez de arreglarlos de más y ensanchar el alcance.
    Si el método ya recibía un `ctx` que no usaba, recordar: **tener `ctx` ≠ estar migrado** (trampa
    (a)). El criterio de terminado es el inventario del lint.
    ⚠ Antes de cambiar la firma, revisar los mocks posicionales de `getMemberSubscription` en `test/`
    (inventario del plan 173-02).
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec tsc -p tsconfig.test-check.json --noEmit && pnpm exec vitest run --no-file-parallelism test/subscriptions</automated>
  </verify>
  <acceptance_criteria>
    - La firma de `getMemberSubscription` tiene `ctx: TenantContext` como primer parámetro
    - `grep -c "tenantWhere" el-templo-api/src/modules/subscriptions/service.ts` aumenta respecto de la rama base, y el método `getMemberSubscription` contiene al menos un `tenantWhere`
    - El insert de `assignPlan` (~`:1592`) usa `tenantValues(ctx, ...)`
    - Los otros 4 inserts a `subscriptions` siguen igual y tienen un comentario de bloque que los nombra como deuda con dueño **fase 174**
    - `pnpm exec tsc -p tsconfig.test-check.json --noEmit` sale con código 0
    - `pnpm exec vitest run --no-file-parallelism test/subscriptions` sale con código 0
  </acceptance_criteria>
  <done>Un gimnasio nuevo puede dar de alta un plan y autocompletar la carga del profe sin caer en el gimnasio 1.</done>
</task>

<task type="auto">
  <name>Task 2: D-15/WR-02 en el guard de cobros vivos, y los 3 sitios de ancla del archivo</name>
  <files>el-templo-api/src/modules/subscriptions/service.ts</files>
  <read_first>
    - el-templo-api/src/modules/subscriptions/service.ts (:2859-2891 el guard `SUB_HAS_ACTIVE_TRANSACTIONS` completo, y :2898-2909 donde sus campos se serializan en el body del 409; :1669, :3595, :4981 los tres `update(users).set({branchId, branchUpdatedAt})`)
    - .planning/phases/172-adopci-n-1-piloto-finance/172-REVIEW.md (WR-02 con el fix exacto)
    - el-templo-api/src/modules/analytics/ticket-service.ts (`linkedCharges`: el statement con DOS tablas strict y DOS `tenantWhere`, uno por tabla — la convención del módulo)
    - el-templo-api/src/modules/shared/branch-consistency.ts (el helper del plan 173-11)
  </read_first>
  <action>
    **WR-02:** en el guard `SUB_HAS_ACTIVE_TRANSACTIONS`, el `innerJoin` a
    `schema.financialTransactions` une por PK sin filtro y solo `transaction_links` lleva
    `tenantWhere`. Mover/agregar el filtro **al `ON`**: el segundo argumento del `innerJoin` pasa a
    ser `and(tenantWhere(schema.financialTransactions, ctx), eq(...))`.
    Dejar escrito en un comentario de bloque **por qué en el `ON` y no en el WHERE**, y por qué el
    sentinel jamás lo iba a cazar: evalúa por **query** y el `tenant_id` de `transaction_links` ya la
    marcaba `ok` (trampa (h)); la lente que lo ve es el lint por tabla. Y por qué importa: los campos
    de la tabla no filtrada (`txId`, `amount`, `currency`) se **serializan en el body del 409**, así
    que un link corrupto cross-tenant sacaría ids e importes de otro gimnasio por el borde.
    ⚠ Aprovechar para corregir **IN-01** si el comentario de `analytics/ticket-service.ts:511-514`
    sigue afirmando que "el sentinel exige el literal `tenant_id` por CADA tabla migrada": es falso y
    hace creer que el vigilante cazaría un join a medias como este. Es convención del módulo (defensa
    en profundidad), no un requisito verificado por el sentinel. (Si ese archivo está fuera del diff
    de este plan, anotarlo en el SUMMARY como micro-deuda con dueño.)
    **D-05 — los 3 sitios de ancla:** los `update(schema.users).set({ branchId, branchUpdatedAt, ...})`
    de `:1669`, `:3595` y `:4981` toman la sede **de la suscripción**. Resolverla con
    `assertBranchDelGimnasio(ctx, branchId, tx)` dentro de la misma transacción y escribir el `id` de
    la fila resuelta. Si la sede no es del gimnasio, el error es el de "sede no encontrada" (404),
    **cero 403**. Los tres updates llevan además `tenantWhere` sobre `users`.
    D-02: nada más de `subscriptions` se migra acá.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec tsc -p tsconfig.test-check.json --noEmit && pnpm exec vitest run --no-file-parallelism test/subscriptions test/finance</automated>
  </verify>
  <acceptance_criteria>
    - El `innerJoin` del guard tiene `tenantWhere(schema.financialTransactions, ctx)` dentro del `and(...)` de su segundo argumento (el `ON`), y NO en el `.where(...)`
    - Un comentario de bloque explica el porqué del `ON` y la trampa (h) del sentinel por-query
    - `grep -c "assertBranchDelGimnasio\|resolveBranchDelGimnasio" el-templo-api/src/modules/subscriptions/service.ts` devuelve al menos `3`
    - Los 3 `update(schema.users)` de ancla llevan `tenantWhere` y escriben el `id` de la fila resuelta
    - `grep -cE "403|Forbidden" el-templo-api/src/modules/subscriptions/service.ts` no aumenta respecto de la rama base
    - `pnpm lint:tenant --allowlist /tmp/allowlist-173-14.json` reporta CERO entradas para `subscriptions/service.ts` sobre `users` y `user_status_history`
    - `pnpm exec vitest run --no-file-parallelism test/subscriptions test/finance` sale con código 0
  </acceptance_criteria>
  <done>El 409 de cobros vivos no puede filtrar datos de otro gimnasio, y la sede que la suscripción escribe en el socio está validada.</done>
</task>

<task type="auto">
  <name>Task 3: Desmarcar el ancla autodestructiva y convertirla en control positivo</name>
  <files>el-templo-api/test/tenancy/iso-03-finance-coach-load.test.ts</files>
  <read_first>
    - el-templo-api/test/tenancy/iso-03-finance-coach-load.test.ts (:1302-1343 completo — el bloque de comentario "LIMITACION CONOCIDA — DUEÑO: FASE 173", el `it` que afirma 404 + "Concepto enlazado no existe", y la instrucción que el propio mensaje de error contiene)
    - el-templo-api/test/fixtures/second-tenant.ts (`TENANT_DOS = 90671`, qué recursos propios provee el gimnasio 2)
    - el-templo-api/test/tenancy/iso-03-finance-cajas.test.ts (:365-405 el par canónico aislamiento + control positivo; :225-236 `tenantDeLaFila`, la lectura de evidencia desde la BASE)
  </read_first>
  <action>
    Al cerrar D-13, el `it` de `:1326` **se pone en rojo**: afirmaba 404 + "Concepto enlazado no
    existe" porque `assignPlan` insertaba sin gimnasio y el charge no encontraba al socio. Ahora el
    alta funciona.
    Convertirlo en el **control positivo que hoy no se puede escribir**: con recursos propios del
    gimnasio 2 (socio, plan y caja de `TENANT_DOS`), el alta devuelve **201** y la evidencia se lee de
    la **BASE**: `tenantDeLaFila(app, "subscriptions", subId)` y `tenantDeLaFila(app,
    "financial_transactions", chargeId)` devuelven `TENANT_DOS`. La evidencia de una escritura se
    afirma con **varias columnas juntas** en un solo `toEqual` (gimnasio, estado, caja imputada,
    importe): un rechazo que ya escribió la mitad se ve igual que uno limpio si se mira una sola
    columna.
    **Borrar el bloque de comentario "LIMITACION CONOCIDA — DUEÑO: FASE 173"** y renombrar el `it`:
    el nombre viejo describe un comportamiento que ya no existe.
    ⚠ El `describe` que contiene este `it` tiene la clave del manifiesto en su nombre: **no tocarla**,
    o el gate de cobertura de finance (`test/tenancy/iso-03-cobertura.test.ts`) la reporta como
    `faltante` y como `fantasma` a la vez.
    ⚠ Si `CASOS_BASELINE` de ese gate cuenta este caso, el número **no cambia** (el `it` se transforma,
    no desaparece). Verificarlo corriendo el gate.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec vitest run --no-file-parallelism test/tenancy/iso-03-finance-coach-load.test.ts test/tenancy/iso-03-cobertura.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LIMITACION CONOCIDA" el-templo-api/test/tenancy/iso-03-finance-coach-load.test.ts` devuelve `0`
    - `grep -c "DUEÑO: FASE 173\|dueño: fase 173" el-templo-api/test/tenancy/iso-03-finance-coach-load.test.ts` devuelve `0`
    - El `it` reescrito afirma `201` y lee `tenantDeLaFila` de `subscriptions` y de `financial_transactions`, ambos `TENANT_DOS`
    - El nombre del `describe` que lo contiene es idéntico al de la rama base (la clave del manifiesto intacta)
    - `pnpm exec vitest run --no-file-parallelism test/tenancy/iso-03-finance-coach-load.test.ts` sale con código 0
    - `pnpm exec vitest run --no-file-parallelism test/tenancy/iso-03-cobertura.test.ts` sale con código 0
  </acceptance_criteria>
  <done>La deuda que el piloto dejó anclada con un test ejecutable queda cerrada y su ancla se convirtió en la prueba de que está cerrada.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                   | Description                                                                          |
| ------------------------------------------ | ------------------------------------------------------------------------------------ |
| `userId` de params → autocompletar         | `/coach-load/autocompletar/:userId` resuelve la sub de un socio que llega por la URL |
| body del 409 → cliente                     | los campos de la tabla no filtrada se serializan en el mensaje de error              |
| sede de la suscripción → `users.branch_id` | 3 updates escriben el ancla desde un dato de la sub                                  |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                     | Disposition | Mitigation Plan                                                                                                 |
| ----------- | ---------------------- | --------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------- |
| T-173-14-01 | Information Disclosure | `getMemberSubscription` en `/autocompletar`   | mitigate    | `ctx` primero + `tenantWhere` inline: el socio ajeno deja de resolver y la ruta contesta vacío/404              |
| T-173-14-02 | Information Disclosure | body del 409 de `SUB_HAS_ACTIVE_TRANSACTIONS` | mitigate    | `tenantWhere(financialTransactions)` en el `ON`: los ids e importes del 409 solo pueden ser del propio gimnasio |
| T-173-14-03 | Tampering              | `assignPlan` insert sin `tenantValues`        | mitigate    | `tenantValues` en el insert de `:1592`: la sub deja de nacer en el gimnasio 1 por el `DEFAULT` de la columna    |
| T-173-14-04 | Tampering              | 3 `update(users)` de ancla                    | mitigate    | `assertBranchDelGimnasio` dentro de la transacción + `tenantWhere` en el update                                 |
| T-173-14-05 | Repudiation            | comentario IN-01 que sobreafirma el sentinel  | mitigate    | se corrige o se anota como micro-deuda con dueño: hace creer que el vigilante caza joins a medias               |
| T-173-14-SC | Repudiation            | resto de `subscriptions` (4 inserts)          | accept      | D-02/D-13 acotan el alcance a la fase 174; quedan marcados con comentario de bloque y dueño explícito           |

</threat_model>

<verification>
- `pnpm exec tsc -p tsconfig.test-check.json --noEmit` verde.
- `pnpm lint:tenant --allowlist /tmp/allowlist-173-14.json` sin las 2 entradas de `subscriptions/service.ts`.
- `pnpm exec vitest run --no-file-parallelism test/subscriptions test/finance test/tenancy` verde.
- El bloque "LIMITACION CONOCIDA — DUEÑO: FASE 173" ya no existe en el repo.
</verification>

<success_criteria>
Las cuatro deudas que la 172 le dejó con nombre y apellido a esta fase están cerradas, un gimnasio
nuevo puede dar de alta un plan y cobrarlo, y el test que documentaba la fuga ahora documenta que
está tapada.
</success_criteria>

<output>
Create `.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-14-SUMMARY.md` when done
</output>
