---
phase: 173-adopci-n-2-members-guarda-de-consistencia-de-anclas
plan: 03
type: execute
wave: 2
depends_on: ["173-01"]
files_modified:
  - el-templo-api/src/scripts/snapshot-finance-endpoints.ts
  - el-templo-api/src/scripts/snapshot-members-endpoints.ts
autonomous: false
requirements: [ADO-02]
must_haves:
  truths:
    - "D-15 / IN-02: los dos break del loop de paginación del script de snapshot marcan `truncado = true` cuando `filas.length < total`, así que un antes.json parcial ya no se declara completo"
    - "D-10: existe la foto ANTES de tocar una sola línea de código, tomada contra staging, en $HOME/.el-templo-snapshots/173/antes.json con permisos 0600 y FUERA del repo"
    - "El snapshot cubre /members con filtros y paginado agotado, /members/export, /members/export-sepa, /members/check-duplicates, /members/search y /leads"
  artifacts:
    - path: "el-templo-api/src/scripts/snapshot-members-endpoints.ts"
      provides: "captura y diff de los números que ve el staff en el módulo de socios"
      contains: "ENDPOINTS"
    - path: "$HOME/.el-templo-snapshots/173/antes.json"
      provides: "la línea de base del criterio 4 de la fase (mismos números antes y después)"
  key_links:
    - from: "el-templo-api/src/scripts/snapshot-members-endpoints.ts"
      to: "$HOME/.el-templo-snapshots/173/antes.json"
      via: "salida del script contra staging"
      pattern: "el-templo-snapshots/173"
---

<objective>
Escribir la herramienta de D-10 y capturar la línea de base **antes** de que la fase toque una
sola línea de `src/`.

Purpose: el criterio 4 de la fase ("sin cambio para el staff") se verifica con un diff contra
una foto tomada antes. Si la foto se toma después del primer commit de migración, deja de ser
señal. Y antes de tomarla hay que arreglar IN-02: los dos `break` del loop de paginación cortan
sin marcar `truncado`, así que el `antes.json` podría quedar **parcial declarándose completo** y
el diff compararía contra una base incompleta en silencio — sobre datos de socios, no de plata.

Output: `snapshot-members-endpoints.ts` funcionando, el fix IN-02 en el script original, y
`$HOME/.el-templo-snapshots/173/antes.json` con permisos `0600`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-CONTEXT.md
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-PATTERNS.md
@.planning/phases/172-adopci-n-1-piloto-finance/172-REVIEW.md

**Ruta de código:** worktree `/home/franco/projects/et-173`.
El JSON del snapshot va **FUERA del repo**: tiene DNIs, nombres, teléfonos y emails de socios.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix IN-02 — los dos break marcan el truncado (D-15)</name>
  <files>el-templo-api/src/scripts/snapshot-finance-endpoints.ts</files>
  <read_first>
    - el-templo-api/src/scripts/snapshot-finance-endpoints.ts (el loop de paginación completo, ~:398-427, y cómo el tope de MAX_PAGINAS sí marca truncado)
    - .planning/phases/172-adopci-n-1-piloto-finance/172-REVIEW.md (IN-02 con el fix propuesto)
  </read_first>
  <action>
    En el loop de paginación del script, los dos `break` de las líneas ~419 y ~420 —el de
    `!esObjeto(cuerpo) || !Array.isArray(cuerpo.rows)` y el de `cuerpo.rows.length === 0`—
    cortan sin setear `truncado`. Darles el mismo trato que el tope de `MAX_PAGINAS`: si
    `filas.length < total`, setear `truncado = true` y loguear el warn con el endpoint, la página
    y el par `filas.length / total`. La captura incompleta tiene que **delatarse**, que es
    exactamente para lo que existe el flag.
    Commit: `fix(173-03): el snapshot marca truncado cuando la paginación corta antes de agotar total`.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Los dos `break` del final del loop están precedidos por la asignación condicional de `truncado`
    - `grep -c "truncado = true" el-templo-api/src/scripts/snapshot-finance-endpoints.ts` devuelve al menos `3`
    - `pnpm exec tsc --noEmit` sale con código 0
    - El SUMMARY registra una prueba en caliente del flag: forzar una respuesta con `rows: []` y `total > 0` (mock o endpoint de prueba) y ver `truncado: true` en la salida
  </acceptance_criteria>
  <done>Un snapshot cortado deja de declararse completo.</done>
</task>

<task type="auto">
  <name>Task 2: Escribir snapshot-members-endpoints.ts</name>
  <files>el-templo-api/src/scripts/snapshot-members-endpoints.ts</files>
  <read_first>
    - el-templo-api/src/scripts/snapshot-finance-endpoints.ts (el script completo: forma de ENDPOINTS, autenticación, paginación, modo --diff, escritura del JSON)
    - el-templo-api/src/modules/members/schemas.ts (los nombres REALES de cada querystring: ajv strippea en silencio lo que el schema no declara)
    - el-templo-api/src/modules/members/routes.ts (los filtros que acepta GET /api/admin/members y la forma de la respuesta { members, total, totalDebtByCurrency })
  </read_first>
  <action>
    Copiar `snapshot-finance-endpoints.ts` a `snapshot-members-endpoints.ts` cambiando la constante
    `ENDPOINTS` por los del módulo (selección fina, Claude's Discretion):
    `GET /api/admin/members` con paginado agotado y con los filtros que el staff usa a diario
    (por estado, por sede, por deudor, por búsqueda de texto, y la combinación sin filtro),
    `GET /api/admin/members/export`, `GET /api/admin/members/export-sepa`,
    `GET /api/admin/members/check-duplicates`, `GET /api/admin/members/search`,
    `GET /api/admin/leads`.
    Conservar las **tres** lecciones que el script aprendió a la mala:
    (a) mandarle `dateFrom` a un endpoint cuyo schema no lo declara **no da 400** — Fastify compila
        ajv con `removeAdditional: true` y lo strippea en silencio, así que el snapshot diría
        "rango X" sobre el histórico completo: mapear el rango al **nombre real** de cada schema,
        leído de `members/schemas.ts`, y fijar un rango de fechas **fijo** (no relativo a hoy) para
        que el "después" sea comparable;
    (b) **paginar hasta agotar `total`** — la adopción cambia índices y un cambio de índice cambia
        qué filas caen en la página 1 sin que ningún número se mueva;
    (c) **el orden de las listas NO es señal** a propósito: MySQL devuelve los empates al revés al
        cambiar de índice; un cambio de orden visible lo caza el UAT, no el diff.
    Para los dos exports (xlsx), guardar el **contenido** normalizado (filas y columnas por índice
    fijo), no el binario ni su hash: un xlsx trae metadata con timestamp y un hash daría diff
    siempre.
    Además del `total` de cada listado, capturar `totalDebtByCurrency`: es el agregado de members
    y es una query **aparte** de las filas.
    Commit: `feat(173-03): script de snapshot de los endpoints de socios (D-10)`.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec tsc --noEmit && pnpm exec tsx src/scripts/snapshot-members-endpoints.ts --help</automated>
  </verify>
  <acceptance_criteria>
    - `test -f el-templo-api/src/scripts/snapshot-members-endpoints.ts` sale 0
    - `grep -c "removeAdditional\|strippea\|schema" el-templo-api/src/scripts/snapshot-members-endpoints.ts` devuelve > 0 (la lección (a) queda escrita en el archivo)
    - El archivo declara un rango de fechas **literal** (no `new Date()` ni `Date.now()`) para los endpoints que aceptan rango
    - `grep -c "truncado" el-templo-api/src/scripts/snapshot-members-endpoints.ts` devuelve al menos `3` (el fix IN-02 viaja en la copia)
    - `pnpm exec tsc --noEmit` sale con código 0
    - Cada nombre de querystring usado en `ENDPOINTS` aparece literal en `src/modules/members/schemas.ts` (verificado por grep, uno por uno, y listado en el SUMMARY)
  </acceptance_criteria>
  <done>La herramienta existe y sus parámetros están validados contra los schemas reales.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Capturar el antes.json contra staging (D-10)</name>
  <what-built>El script `snapshot-members-endpoints.ts` con el fix IN-02 adentro.</what-built>
  <how-to-verify>
    1. Confirmar que el backmerge del plan 173-01 **ya está desplegado en staging** (no solo
       mergeado): `git log` prueba el merge, no el deploy. Si `a36b759d` ("acotar al gimnasio el
       chequeo de primer pago", que toca tenancy de referidos) se despliega DESPUÉS de la captura,
       el diff del cierre mezcla ese cambio con la migración y deja de ser señal.
    2. Correr el script contra **staging** con un token de admin, escribiendo a
       `$HOME/.el-templo-snapshots/173/antes.json`.
    3. Verificar que `truncado` es `false` en **todos** los endpoints capturados. Si alguno vino
       `true`, la captura no sirve: subir `MAX_PAGINAS` o acotar el filtro y repetir.
    4. `chmod 0600` sobre el archivo y confirmar que el directorio está fuera del repo
       (`$HOME/.el-templo-snapshots/`, nunca `.planning/` ni el worktree).
    5. Confirmar con Franco que la foto es la buena y que a partir de acá empieza la migración.
  </how-to-verify>
  <action>
    Presentar: el commit desplegado en staging, la salida del script (conteos por endpoint,
    `total`, `totalDebtByCurrency`, flag `truncado` de cada uno), el `ls -l` del archivo mostrando
    `-rw-------`, y el `git status --porcelain` del worktree vacío (el JSON NO está en el repo).
    Registrar en el SUMMARY los conteos por endpoint — son la línea de base del criterio 4.
    NO arrancar ningún plan de migración de `src/` hasta que Franco confirme la captura.
  </action>
  <done>`$HOME/.el-templo-snapshots/173/antes.json` existe, con `truncado: false` en todos los endpoints, permisos 0600, y Franco confirmó.</done>
  <resume-signal>Escribí "snapshot antes capturado" para habilitar los planes de migración, o "repetir captura" si algo quedó truncado</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                         |
| ------------------------------------ | ----------------------------------------------------------------------------------- |
| API de staging → disco local         | la respuesta trae DNIs, nombres, teléfonos y emails de socios reales                |
| disco local → repositorio git        | un JSON con PII commiteado es una fuga permanente e irreversible en el historial    |
| captura parcial → criterio de cierre | un `antes.json` truncado que se declara completo invalida el criterio 4 en silencio |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                   | Disposition | Mitigation Plan                                                                                                               |
| ----------- | ---------------------- | ------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------- |
| T-173-03-01 | Information Disclosure | `$HOME/.el-templo-snapshots/173/antes.json` | mitigate    | archivo FUERA del repo, `chmod 0600`, y criterio de aceptación que verifica `git status --porcelain` vacío                    |
| T-173-03-02 | Repudiation            | flag `truncado` del loop de paginación      | mitigate    | fix IN-02: los dos `break` marcan `truncado` cuando `filas.length < total`, probado en caliente                               |
| T-173-03-03 | Tampering              | querystrings no declaradas en el schema     | mitigate    | cada nombre de parámetro se verifica literal contra `members/schemas.ts`: ajv con `removeAdditional` los strippea sin dar 400 |
| T-173-03-04 | Repudiation            | rango de fechas relativo a hoy              | mitigate    | rango literal fijo en el script: un rango relativo hace que el "después" mida otra ventana y el diff sea ruido                |

</threat_model>

<verification>
- `truncado` sale `false` en todos los endpoints capturados.
- `ls -l $HOME/.el-templo-snapshots/173/antes.json` muestra `-rw-------`.
- `git -C /home/franco/projects/et-173 status --porcelain` vacío: el JSON no entró al repo.
- Cada querystring del script existe literal en `members/schemas.ts`.
- `pnpm exec tsc --noEmit` verde.
</verification>

<success_criteria>
Existe una foto completa y verificable de lo que el staff ve hoy en el módulo de socios, tomada
antes del primer commit de migración, con la que el plan 173-31 va a poder afirmar "mismos
números" sin depender de la memoria de nadie.
</success_criteria>

<output>
Create `.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-03-SUMMARY.md` when done
</output>
