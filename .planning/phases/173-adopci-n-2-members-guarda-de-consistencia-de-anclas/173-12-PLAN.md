---
phase: 173-adopci-n-2-members-guarda-de-consistencia-de-anclas
plan: 12
type: execute
wave: 3
depends_on: ["173-11"]
files_modified:
  - el-templo-api/src/db/schema/branches.ts
  - el-templo-api/src/db/schema/users.ts
  - el-templo-api/src/db/migrations/0198_anclas_tenant_branch.sql
  - el-templo-api/test/db/anclas-fk-compuesta.test.ts
autonomous: true
requirements: [ADO-07]
must_haves:
  truths:
    - "D-05(b): `branches` tiene unique `(tenant_id, id)` y `users` tiene FK compuesta `(tenant_id, branch_id) → branches(tenant_id, id)`"
    - "D-18: la migración es la 0198 (staging tiene 0197, prod 0196), hand-written, aplicada con `pnpm db:migrate`, con el SQL commiteado junto al schema"
    - "D-18: se verifica 0 divergencias `user.tenant_id != branch.tenant_id` ANTES del ALTER, y la verificación queda escrita como script reusable"
    - "Un INSERT o UPDATE que apunte un usuario a una sede de otro gimnasio es rechazado POR LA BASE, no solo por la app — ningún SQL crudo, script ni backfill lo puede saltear"
  artifacts:
    - path: "el-templo-api/src/db/migrations/0198_anclas_tenant_branch.sql"
      provides: "unique (tenant_id, id) en branches + índice y FK compuesta en users"
      contains: "ALTER TABLE"
    - path: "el-templo-api/src/db/schema/branches.ts"
      provides: "uniqueIndex uq_branches_tenant_id_id, referenciable por la FK compuesta"
      contains: "uq_branches_tenant_id_id"
    - path: "el-templo-api/test/db/anclas-fk-compuesta.test.ts"
      provides: "prueba de que la base rechaza la divergencia de anclas"
  key_links:
    - from: "el-templo-api/src/db/schema/users.ts"
      to: "el-templo-api/src/db/schema/branches.ts"
      via: "FK compuesta (tenant_id, branch_id) → (tenant_id, id)"
      pattern: "fk_users_tenant_branch"
---

<objective>
La capa 2 del invariante ADO-07: el cinturón en la base de datos (D-05b, D-18).

Purpose: el helper del plan 173-11 protege los caminos que pasan por la aplicación. La FK
compuesta protege **todos los demás**: SQL crudo, scripts de operador, backfills, una consola de
MySQL a las 3 de la mañana. Es la única capa que no depende de que alguien se acuerde de llamar al
helper. La mina M10 dice que el par de anclas puede divergir; con esta migración, no puede.

Output: migración `0198` aplicada, schema Drizzle actualizado y un test que prueba que la base
rechaza la divergencia.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-CONTEXT.md
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-PATTERNS.md
@.claude/skills/el-templo-db-migrations/SKILL.md
@CLAUDE.md

**Ruta de código:** worktree `/home/franco/projects/et-173`.

**Reglas duras del repo (skill `el-templo-db-migrations`):** numeración **hand-written desde
0198**; aplicar con **`pnpm db:migrate`** (runner propio `src/db/run-migrations.ts`, trackea en la
tabla `_migrations`); **PROHIBIDO** `drizzle-kit migrate` (su `meta/_journal.json` está
desincronizado y no es la fuente de verdad) y **PROHIBIDO** `drizzle-kit push` en trabajo
commiteado; **nunca un `;` dentro de un comentario SQL**; el `.sql` se commitea **junto** al cambio
de schema, en el mismo commit.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar 0 divergencias y escribir el schema + la migración 0198</name>
  <files>el-templo-api/src/db/schema/branches.ts, el-templo-api/src/db/schema/users.ts, el-templo-api/src/db/migrations/0198_anclas_tenant_branch.sql</files>
  <read_first>
    - el-templo-api/src/db/schema/branches.ts (:62-69 — `index("idx_branches_tenant_id")` y `uniqueIndex("uq_branches_tenant_code")`: el idioma exacto a copiar)
    - el-templo-api/src/db/schema/users.ts (la definición de `tenantId` y `branchId`, y :144 el comentario sobre `branchUpdatedAt`/`branchSource`)
    - el-templo-api/src/db/migrations/0197_payment_method_direct_debit.sql (el formato hand-written: prosa larga arriba con QUÉ ARREGLA / POR QUÉ / ALCANCE, sin un solo `;` en los comentarios, y el DDL al final)
    - el-templo-api/src/db/migrations/0191_tenant_anchors.sql (lo más parecido: crea `fk_users_tenant`, pero es FK **simple** — la compuesta hay que escribirla desde cero)
    - el-templo-api/src/db/run-migrations.ts (cómo el runner trackea en `_migrations`)
    - .claude/skills/el-templo-db-migrations/SKILL.md
  </read_first>
  <action>
    **Primero la verificación de precondición (D-18), antes de escribir el ALTER:** contar filas con
    `users.branch_id IS NOT NULL` y `users.tenant_id <> (SELECT tenant_id FROM branches WHERE
    branches.id = users.branch_id)`. Hoy debería ser **0** (todo es gimnasio 1), pero si la FK se
    aplica sobre datos divergentes el ALTER **falla en el deploy**. Dejar la consulta escrita como
    script reusable (junto a `src/db/scripts/verify-tenant-backfill.ts`, o dentro del propio `.sql`
    como comentario copiable) — es lo que va a fallar el día del segundo gimnasio.
    Schema:
    - `branches.ts`: agregar `uniqueIndex("uq_branches_tenant_id_id").on(table.tenantId, table.id)`
      junto a los índices existentes. Sin ese unique, `users(tenant_id, branch_id)` no tiene a qué
      referenciar.
    - `users.ts`: agregar el índice `(tenant_id, branch_id)` y la FK compuesta
      `fk_users_tenant_branch` → `branches(tenant_id, id)`. Semántica MATCH SIMPLE de MySQL: una fila
      con `branch_id NULL` **pasa** la constraint, que es el comportamiento que queremos (una sede
      nula no viola el invariante).
    Migración `src/db/migrations/0198_anclas_tenant_branch.sql`, hand-written, copiando el formato de
    `0197`: cabecera de prosa con QUÉ ARREGLA (la mina M10: el par de anclas podía divergir), POR QUÉ
    (el helper de app no cubre SQL crudo, scripts ni backfills) y ALCANCE (D-08: solo `users`; las
    demás tablas con `branch_id` quedan para 174/175). **Ni un `;` dentro de los comentarios.**
    Decidir si va en uno o dos bloques (Claude's Discretion): el unique de `branches` **tiene** que
    estar antes de la FK de `users`, así que si van en el mismo archivo el orden es obligatorio.
    Documentar en la cabecera la elección y por qué.
    ⚠ `ON DELETE` / `ON UPDATE`: elegir explícitamente y justificarlo en la prosa. Un `CASCADE` sobre
    `tenant_id` sería un borrado masivo silencioso; `RESTRICT`/`NO ACTION` es el default seguro.
    ⚠ El `.sql` y los dos archivos de schema van en **un solo commit**.
    Commit: `feat(173-12): unique (tenant_id, id) en branches y FK compuesta en users (ADO-07)`.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec tsc --noEmit && node -e "const s=require('fs').readFileSync('src/db/migrations/0198_anclas_tenant_branch.sql','utf8');const c=s.split('\n').filter(l=>l.trim().startsWith('--'));if(c.some(l=>l.includes(';')))process.exit(1);console.log('sin ; en comentarios')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "uq_branches_tenant_id_id" el-templo-api/src/db/schema/branches.ts` devuelve al menos `1`
    - `grep -c "fk_users_tenant_branch" el-templo-api/src/db/schema/users.ts` devuelve al menos `1`
    - `test -f el-templo-api/src/db/migrations/0198_anclas_tenant_branch.sql` sale 0 y no existe ningún `0198_*` adicional
    - Ninguna línea de comentario (`--`) del `.sql` contiene un `;` (verificado por el `node -e` del verify)
    - El `.sql` crea el unique de `branches` **antes** de la FK de `users`
    - El `.sql` declara explícitamente `ON DELETE` / `ON UPDATE` y la cabecera justifica la elección
    - El SUMMARY transcribe el conteo de divergencias medido (tiene que ser `0`) y la consulta usada
    - `pnpm exec tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>El schema y el SQL existen, con 0 divergencias verificadas y el orden de los ALTER correcto.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Aplicar la migración con pnpm db:migrate y probar que la base rechaza</name>
  <files>el-templo-api/test/db/anclas-fk-compuesta.test.ts</files>
  <read_first>
    - el-templo-api/src/db/run-migrations.ts (el runner y la tabla `_migrations`)
    - el-templo-api/test/db/tenant-tables.test.ts (idioma de los tests de `test/db/`)
    - el-templo-api/test/fixtures/second-tenant.ts (`TENANT_DOS = 90671`, `seedSecondTenant`, `limpiarSegundoGimnasio`)
    - el-templo-api/test/setup.ts (cómo se provisiona la base por worker: la migración tiene que correr también ahí)
  </read_first>
  <action>
    **[BLOCKING] Aplicar la migración:** `pnpm db:migrate` en el worktree, contra la base local y
    contra `eltemplo_test`. **PROHIBIDO** `drizzle-kit migrate` y `drizzle-kit push`.
    Sin este paso el build y el typecheck pasan igual (los tipos salen del schema TS, no de la base
    viva) y se produce un **falso positivo de verificación**: el test de la FK pasaría por no
    ejercerla.
    Verificar contra la base viva que las dos constraints existen:
    `SHOW INDEX FROM branches` contiene `uq_branches_tenant_id_id`, y
    `SELECT * FROM information_schema.REFERENTIAL_CONSTRAINTS WHERE CONSTRAINT_NAME =
    'fk_users_tenant_branch'` devuelve una fila.
    Escribir `test/db/anclas-fk-compuesta.test.ts` con dos gimnasios:
    - **control positivo:** insertar/actualizar un usuario de `TENANT_DOS` apuntando a una sede de
      `TENANT_DOS` → funciona;
    - **rechazo:** intentar apuntar ese usuario a una sede de El Templo (`TENANT_TEMPLO = 1`) del
      **mismo país** → la base lo rechaza. Afirmar el **motivo** junto con el fallo: el error tiene
      que ser de FK (`ER_NO_REFERENCED_ROW_2` / `foreign key constraint fails`), no un error
      cualquiera. Un rechazo por la razón equivocada se ve igual que el correcto.
    - **`branch_id NULL`:** insertar un usuario sin sede → **pasa** (MATCH SIMPLE), y el test lo
      afirma explícitamente para que nadie "endurezca" la FK después rompiendo el alta sin sede.
    ⚠ El throw puede llegar envuelto: si se usa Drizzle, mirar la cadena de `cause`.
    ⚠ Agregar un archivo de test **re-baraja** qué archivos comparten base por worker en CI: si
    aparece un `ER_ROW_IS_REFERENCED_2` en un archivo que esta fase no tocó, **no es el sentinel** —
    mirar el patrón de limpieza (conexión única del pool con `FOREIGN_KEY_CHECKS = 0`).
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm db:migrate && pnpm exec vitest run --no-file-parallelism test/db/anclas-fk-compuesta.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm db:migrate` sale con código 0 y la fila de `0198_anclas_tenant_branch` aparece en la tabla `_migrations`
    - `SHOW INDEX FROM branches` incluye `uq_branches_tenant_id_id` (salida transcrita en el SUMMARY)
    - `information_schema.REFERENTIAL_CONSTRAINTS` devuelve una fila para `fk_users_tenant_branch` (salida transcrita en el SUMMARY)
    - `pnpm exec vitest run --no-file-parallelism test/db/anclas-fk-compuesta.test.ts` sale con código 0
    - El test tiene los tres casos: control positivo, rechazo con motivo de FK afirmado, y `branch_id NULL` permitido
    - No se ejecutó `drizzle-kit migrate` ni `drizzle-kit push` en ningún momento (declarado en el SUMMARY)
    - `pnpm exec vitest run --no-file-parallelism test/db` sale con código 0
  </acceptance_criteria>
  <done>La base rechaza la divergencia de anclas, y está probado contra la base viva, no contra los tipos.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                   |
| ----------------------------------- | ----------------------------------------------------------------------------- |
| SQL crudo / consola MySQL → `users` | ningún helper de aplicación intercepta este camino                            |
| scripts y backfills → `users`       | corren fuera del request y pueden escribir sin `ctx`                          |
| migración → deploy de producción    | un ALTER que falla sobre datos divergentes corta el deploy en el peor momento |

## STRIDE Threat Register

| Threat ID   | Category          | Component                                    | Disposition | Mitigation Plan                                                                                                                     |
| ----------- | ----------------- | -------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| T-173-12-01 | Tampering         | `users.branch_id` por fuera de la aplicación | mitigate    | FK compuesta `(tenant_id, branch_id) → branches(tenant_id, id)`: la base rechaza, no la app                                         |
| T-173-12-02 | Tampering         | integridad del par de anclas (mina M10)      | mitigate    | unique `(tenant_id, id)` en `branches` + índice y FK en `users`, probados contra la base viva con 2 gimnasios                       |
| T-173-12-03 | Denial of Service | ALTER sobre datos divergentes                | mitigate    | verificación de 0 divergencias ANTES del ALTER, dejada como script reusable para el día del segundo gimnasio                        |
| T-173-12-04 | Denial of Service | `ON DELETE CASCADE` accidental               | mitigate    | la acción referencial se elige explícitamente y se justifica en la cabecera del `.sql`; `RESTRICT`/`NO ACTION` es el default seguro |
| T-173-12-05 | Repudiation       | falso positivo de verificación               | mitigate    | tarea `[BLOCKING]` que aplica `pnpm db:migrate` antes del test: los tipos salen del schema TS y pasarían igual sin la constraint    |

</threat_model>

<verification>
- `pnpm db:migrate` verde y `0198` registrada en `_migrations`.
- Las dos constraints existen en la base viva (salidas transcritas).
- `pnpm exec vitest run --no-file-parallelism test/db` verde, con el rechazo afirmado por motivo de FK.
- Ninguna línea de comentario del `.sql` tiene `;`.
- `pnpm exec tsc --noEmit` verde.
</verification>

<success_criteria>
Un usuario apuntando a una sede de otro gimnasio deja de ser una fila posible en esta base de
datos, y eso ya no depende de que ningún código se acuerde de verificarlo.
</success_criteria>

<output>
Create `.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-12-SUMMARY.md` when done
</output>
