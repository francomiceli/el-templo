---
phase: 173-adopci-n-2-members-guarda-de-consistencia-de-anclas
plan: 06
type: execute
wave: 2
depends_on: ["173-01"]
files_modified:
  - el-templo-api/src/modules/analytics/service.ts
  - el-templo-api/src/modules/analytics/engagement-service.ts
  - el-templo-api/src/modules/analytics/funnel-service.ts
  - el-templo-api/src/modules/analytics/advanced-finance-service.ts
  - el-templo-api/src/modules/analytics/frequency-service.ts
  - el-templo-api/src/modules/analytics/member-flows-service.ts
autonomous: true
requirements: [ADO-02]
must_haves:
  truths:
    - "D-02: los 6 services de analytics dejan de violar sobre `users`, `member_profiles` y `user_status_history`, tocando ÚNICAMENTE esas queries"
    - "Las 10 entradas de allowlist de analytics sobre tablas del módulo desaparecen contra la allowlist de trabajo"
    - "Ningún LEFT JOIN a `users` o `member_profiles` de analytics pierde filas: el filtro va en el ON"
  artifacts:
    - path: "el-templo-api/src/modules/analytics/service.ts"
      provides: "métricas de socios scopeadas por gimnasio"
      contains: "tenantWhere"
  key_links:
    - from: "el-templo-api/src/modules/analytics/*"
      to: "schema.users / schema.memberProfiles / schema.userStatusHistory"
      via: "tenantWhere inline (en el ON si es join)"
      pattern: "tenantWhere\\(schema\\.(users|memberProfiles|userStatusHistory)"
---

<objective>
Cirugía mínima (D-02) sobre los 6 services de analytics: solo las queries que tocan las 8 tablas
del módulo reciben helpers. El resto de analytics se migra en la fase 175.

Purpose: analytics es el consumidor más pesado de `users` y `member_profiles`, y casi todas sus
queries son **agregados con LEFT JOIN**. Es exactamente donde la trampa del filtro en el WHERE
—que convierte el LEFT en INNER y borra filas en silencio con el lint en verde— hace más daño:
un número de tablero que baja sin que nadie lo note. Migrar estos accesos ahora es precondición
del switch de `users` (D-01): con la tabla en strict, cada una de estas queries revienta.

Output: las 10 entradas de analytics fuera de la allowlist de trabajo, con los mismos números en
los tableros.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-CONTEXT.md
@.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-PATTERNS.md
@.docs/saas-multitenancy/07-receta-adopcion.md

**Ruta de código:** worktree `/home/franco/projects/et-173`.
**Allowlist de trabajo:** `/tmp/allowlist-173-06.json`. El archivo real tiene un solo dueño (173-30).

**Entradas objetivo (10):** `service.ts` → `member_profiles`, `users`; `engagement-service.ts` →
`member_profiles`, `users`; `funnel-service.ts` → `user_status_history`, `users`;
`advanced-finance-service.ts` → `users`; `frequency-service.ts` → `users`;
`member-flows-service.ts` → `users`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: analytics/service.ts, engagement-service.ts y funnel-service.ts</name>
  <files>el-templo-api/src/modules/analytics/service.ts, el-templo-api/src/modules/analytics/engagement-service.ts, el-templo-api/src/modules/analytics/funnel-service.ts</files>
  <read_first>
    - el-templo-api/src/modules/analytics/service.ts (los métodos que tocan `users` y `member_profiles`, listados en 173-02-SUMMARY)
    - el-templo-api/src/modules/analytics/engagement-service.ts
    - el-templo-api/src/modules/analytics/funnel-service.ts
    - el-templo-api/src/modules/finance/cash-register-service.ts (:446-465 — el ejemplar de manual del filtro en el ON, con el porqué escrito adentro)
    - el-templo-api/src/modules/shared/tenant.ts (`tenantWhere`, `TenantContext`, `assertTenant`)
    - .planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-02-SUMMARY.md (la lista medida: archivo, tabla, método, qué falta)
  </read_first>
  <action>
    Para cada acceso listado en el inventario: `ctx: TenantContext` PRIMERO en la firma del método
    que hace la query (antes del `tx` y de los ids), `tenantWhere` **inline** en el statement que
    nombra la tabla, y el `ctx` del borde saliendo de `assertTenant(request.scope,
    "analytics.<operación>")` en `analytics/routes.ts` si el método se llama desde una ruta.
    ⚠ **El gimnasio se nombra INLINE**: un `const conditions = [...]` armado arriba **no cuenta**,
    ni siquiera si es un ternario de una línea (mordió 5 veces en el piloto). La excepción es un
    helper que devuelve `SQL[]` con el `tenantWhere` como primer elemento del array, y entonces
    **no se duplica en el llamador**.
    ⚠ **Todo join a `users` / `member_profiles` / `user_status_history` lleva el filtro en el `ON`,
    también los INNER.** En un LEFT JOIN, ponerlo en el WHERE lo convierte en INNER y borra filas en
    silencio con el lint verde. Se usa la misma forma en los dos para que el próximo que agregue un
    join no tenga que elegir.
    ⚠ Si un statement toca **dos** tablas del módulo, **las dos** llevan su `tenantWhere`: el
    sentinel marca `ok` la query entera con un solo `tenant_id` en la zona de predicado (trampa
    (h)), así que la única lente que ve la mitad faltante es el lint por tabla.
    D-02: no se migra ninguna otra tabla de estos archivos (transacciones, reservas, planes) —
    eso es de la fase 175.
    ⚠ Ojo con el calendario: hay tests de analytics con fecha fija y ventana relativa que rompen CI
    solo por el almanaque. Si un test de analytics se pone rojo y el diff no lo toca, sospechar del
    almanaque antes que del cambio.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec tsc -p tsconfig.test-check.json --noEmit && pnpm exec vitest run --no-file-parallelism test/analytics</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm lint:tenant --allowlist /tmp/allowlist-173-06.json` no reporta las 6 entradas de estos 3 archivos
    - Todo `leftJoin(schema.users, ...)` / `leftJoin(schema.memberProfiles, ...)` de estos archivos tiene su `tenantWhere` dentro del `and(...)` del segundo argumento, y ninguno lo tiene en el `.where(...)` (verificado sitio por sitio y listado en el SUMMARY)
    - Todo statement que nombre dos tablas del módulo lleva DOS `tenantWhere` (uno por tabla)
    - `grep -rnE "tenantId!|tenantId\s*\?\?" el-templo-api/src/modules/analytics/{service,engagement-service,funnel-service}.ts` no devuelve líneas
    - `pnpm exec vitest run --no-file-parallelism test/analytics` sale con código 0
    - El diff no cambia una sola query sobre tablas que NO son del módulo (D-02)
  </acceptance_criteria>
  <done>Los tres services grandes de analytics leen socios del propio gimnasio, con los mismos números.</done>
</task>

<task type="auto">
  <name>Task 2: advanced-finance-service.ts, frequency-service.ts y member-flows-service.ts</name>
  <files>el-templo-api/src/modules/analytics/advanced-finance-service.ts, el-templo-api/src/modules/analytics/frequency-service.ts, el-templo-api/src/modules/analytics/member-flows-service.ts</files>
  <read_first>
    - el-templo-api/src/modules/analytics/advanced-finance-service.ts (su acceso a `users`; el archivo ya fue migrado para las tablas de finance en la 172 — copiar ese idioma)
    - el-templo-api/src/modules/analytics/frequency-service.ts
    - el-templo-api/src/modules/analytics/member-flows-service.ts
    - .planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-02-SUMMARY.md
  </read_first>
  <action>
    Mismo tratamiento que la tarea 1, con una entrada de `users` por archivo.
    `advanced-finance-service.ts` **ya tiene `ctx`** desde la 172 para sus tablas de finance: acá se
    aprovecha el `ctx` existente y solo se agrega el `tenantWhere` de `users`. Recordar que
    **tener `ctx` ≠ estar migrado** (trampa (a)): el criterio de terminado es el inventario del
    lint, jamás la firma.
    En `frequency-service.ts` y `member-flows-service.ts`, si el método todavía no recibe `ctx`,
    agregarlo PRIMERO y actualizar sus call sites en `analytics/routes.ts`.
    Reescribir cualquier docblock que diga que las queries de `users` "todavía no filtran": un
    docblock que afirma eso sobre un archivo ya migrado es peor que no tener docblock.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-173/el-templo-api && pnpm exec tsc -p tsconfig.test-check.json --noEmit && pnpm exec vitest run --no-file-parallelism test/analytics</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm lint:tenant --allowlist /tmp/allowlist-173-06.json` reporta CERO entradas para los 6 archivos de analytics de este plan
    - `grep -c "tenantWhere(schema.users" el-templo-api/src/modules/analytics/advanced-finance-service.ts` devuelve al menos `1`
    - No queda ningún docblock en estos archivos que afirme que las queries de `users` no filtran
    - `pnpm exec tsc -p tsconfig.test-check.json --noEmit` sale con código 0
    - `pnpm exec vitest run --no-file-parallelism test/analytics` sale con código 0
  </acceptance_criteria>
  <done>Las 10 entradas de analytics están fuera de la allowlist de trabajo.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                                       |
| ----------------------------- | --------------------------------------------------------------------------------- |
| tablero de analytics → staff  | los agregados exponen conteos y montos derivados de socios de todos los gimnasios |
| LEFT JOIN → filas del reporte | un filtro mal ubicado borra filas en silencio y baja un número sin dar error      |

## STRIDE Threat Register

| Threat ID   | Category               | Component                               | Disposition | Mitigation Plan                                                                                                |
| ----------- | ---------------------- | --------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------- |
| T-173-06-01 | Information Disclosure | agregados de `users` en analytics       | mitigate    | `tenantWhere` inline en cada statement que nombra la tabla; el `ctx` sale de `assertTenant`, nunca del payload |
| T-173-06-02 | Information Disclosure | statements con 2 tablas del módulo      | mitigate    | un `tenantWhere` por tabla: el sentinel marca `ok` la query entera con un solo `tenant_id` (trampa (h))        |
| T-173-06-03 | Tampering              | LEFT JOIN a `users` / `member_profiles` | mitigate    | filtro en el `ON`, nunca en el WHERE; criterio de aceptación que lo verifica sitio por sitio                   |
| T-173-06-04 | Information Disclosure | `user_status_history` en el funnel      | mitigate    | `tenantWhere` propio en el statement, aunque la otra tabla de la query ya lo lleve                             |

</threat_model>

<verification>
- `pnpm lint:tenant --allowlist /tmp/allowlist-173-06.json` sin las 10 entradas.
- `pnpm exec tsc -p tsconfig.test-check.json --noEmit` verde.
- `pnpm exec vitest run --no-file-parallelism test/analytics` verde.
- El SUMMARY lista cada join migrado con su ubicación del filtro (`ON` vs `WHERE`).
</verification>

<success_criteria>
Analytics puede seguir leyendo socios y perfiles, pero solo los del gimnasio del `ctx`, sin que
ningún número del tablero cambie para El Templo.
</success_criteria>

<output>
Create `.planning/phases/173-adopci-n-2-members-guarda-de-consistencia-de-anclas/173-06-SUMMARY.md` when done
</output>
