---
phase: 162-superficie-member-app-y-reporte-de-reparto
plan: 05
type: execute
wave: 3
depends_on: [162-04]
files_modified:
  - el-templo-app/src/pages/ReservasPage.vue
  - el-templo-app/src/modules/progression/pages/MiTemplo.vue
autonomous: false
requirements: [APP-01, APP-02, APP-03]

must_haves:
  truths:
    - "La grilla muestra un badge dorado 'Especial' en los slots de actividades especiales (slot.isSpecial)"
    - "Un socio con pase y saldo ve el botón Reservar en la especial (E1); con 0/2 ve pill 'Usaste tus 2 clases' sin botón (E2)"
    - "Un socio presencial sin pase ve la especial con 'Requiere plan especial' y al tocarla abre el dialog informativo (E3)"
    - "El externo-solo-pase entra a la grilla (canAccessGrid) y ve SOLO las especiales; las regulares se ocultan (E5)"
    - "Un intento de reservar especial sin pase (code PASS_REQUIRED) abre el dialog informativo con precios, sin CTA de pago (APP-03/D-02)"
    - "Un chip único 'Especiales · x/2' en el header de la grilla y una card en Mi Templo muestran el saldo (APP-02)"
    - "El contador se recarga (loadEspecialPass) al montar y tras reserva/cancelación"
  artifacts:
    - path: "el-templo-app/src/pages/ReservasPage.vue"
      provides: "badge + estados E1-E5 + dialog PASS_REQUIRED + gate canAccessGrid + chip x/2 + refresh"
      contains: "isSpecial"
    - path: "el-templo-app/src/modules/progression/pages/MiTemplo.vue"
      provides: "card del pase con saldo x/2"
      contains: "hasEspecialPass"
  key_links:
    - from: "confirmReserve catch"
      to: "showAuraInfoDialog"
      via: "code PASS_REQUIRED (espejo COVERAGE_EXPIRED)"
      pattern: "PASS_REQUIRED"
    - from: "gate de página"
      to: "canAccessGrid = hasPresencialReservationAccess || hasEspecialPass"
      via: "empty-state v-else-if"
      pattern: "canAccessGrid"
    - from: "chip/card"
      to: "userStore.especialClassesRemaining"
      via: "render gateado por hasEspecialPass"
      pattern: "especialClassesRemaining"
---

<objective>
Pintar la superficie del pase en el member app: badge dorado "Especial" por slot, los 5 estados de la matriz APP-01 (E1-E5), el dialog informativo APP-03 (disparado por `code=PASS_REQUIRED`, sin pago in-app), el gate de página refinado para que el externo-solo-pase entre (D-06, corrige Pitfall 1), el chip contador x/2 en el header y la card en Mi Templo (APP-02/D-03), con recarga del pase para que el contador no mienta. Honra el UI-SPEC (naming, acento Aged Gold reservado a los 4 usos, estados en olive/grey no rojo).

Purpose: cierre visible de la fase. Cierra con checkpoint humano (member app sin e2e). CI NO typechequea el app → gate `vue-tsc` local.
Output: grilla + Mi Templo con la superficie completa del pase, verificada por humano.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-UI-SPEC.md
@.planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-PATTERNS.md
@.planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-04-SUMMARY.md

<interfaces>
<!-- Del store (162-04): userStore.hasEspecialPass, .especialClassesRemaining, .especialClassesBudget, .hasOnlyEspecialPass, .loadEspecialPass() -->
<!-- Del grid (162-01): slot.isSpecial:boolean por slot. -->
<!-- Del backend (161-06): reserva de especial sin pase → err.response.data.code === 'PASS_REQUIRED'. -->
<!-- ReservasPage.vue: gate canReservePresencial :707; empty-state :55; slot-cards :359-469; slotCardClass :1053; -->
<!--   confirmReserve catch :1265-1281 (patrón COVERAGE_EXPIRED :1270); showCoverageDialog :610-638; buildWhatsAppUrl/openWhatsApp :722-724,805-808. -->
<!-- MiTemplo.vue: ReferralCtaCard :22 (primera card premium gateada). -->
<!-- UI-SPEC: matriz E1-E5 (E5=regulares OCULTAS client-side para externo-solo-pase); Aged Gold #7d6520 sobre #f5ecd9 -->
<!--   RESERVADO a: badge, ícono auto_awesome, chip contador, ícono del dialog. Bloqueo en olive/grey, NO rojo. -->
<!-- Copy exacto: Copywriting Contract del UI-SPEC. Naming lock D-01: NUNCA "pase(s)" visible. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ReservasPage — badge, estados E1-E5, gate, dialog PASS_REQUIRED, chip x/2, refresh</name>
  <files>el-templo-app/src/pages/ReservasPage.vue</files>
  <read_first>
    - el-templo-app/src/pages/ReservasPage.vue (gate :707; empty-state :55; slot-cards :359-469; slotCardClass :1053; confirmReserve catch :1265-1281; showCoverageDialog :610-638; openWhatsApp :722-724)
    - el-templo-app/src/css/quasar.variables.scss (paleta — fuente de verdad del color; $warning/dorado)
    - .planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-UI-SPEC.md (Component Contract — Member app; Color; Copywriting)
    - .planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-PATTERNS.md (§ReservasPage.vue — 5 puntos)
  </read_first>
  <behavior>
    - Badge dorado "Especial" con ícono auto_awesome en slots isSpecial (todos los estados).
    - E1 (pase + saldo>0): botón Reservar normal. E2 (0/2): pill neutra "Usaste tus 2 clases", sin botón, tap → toast corto. E3 (socio sin pase): afordancia "Requiere plan especial" + ícono lock, tap → dialog APP-03. E4 (externo especial): igual E1/E2 según saldo. E5 (externo-solo-pase viendo regular): slot OCULTO.
    - canAccessGrid = hasPresencialReservationAccess || hasEspecialPass; el externo-solo-pase pasa el gate de página.
    - Reserva de especial sin pase → code PASS_REQUIRED → showAuraInfoDialog (sin CTA de pago).
    - Chip único "Especiales · x/2" en el header, visible sólo si hasEspecialPass; en 0/2 cambia a "Especiales · 0/2 · se renuevan el próximo mes" en tono apagado.
    - loadEspecialPass() se llama al montar y tras cada reserva/cancelación.
  </behavior>
  <action>
    (1) Badge: en la slot-card junto a activityName, `<q-badge v-if="slot.isSpecial" class="slot-card__badge--special">` con ícono `auto_awesome` + "Especial"; CSS nuevo dorado (Aged Gold `#7d6520` sobre `#f5ecd9`, espeja `.slot-card__avail`). (2) Gate: agregar computed `canAccessGrid = hasPresencialReservationAccess || hasEspecialPass` y usarlo en el `v-else-if` del empty-state (:55) en lugar de `canReservePresencial`. (3) Estados por slot (E1-E5) según UI-SPEC: usar `slot.isSpecial` + capabilities del store para decidir la zona derecha del slot; E2 pill "Usaste tus 2 clases" sin botón; E3 afordancia "Requiere plan especial" (olive/grey + `lock`) cuyo tap abre el dialog; E5 ocultar los slots regulares cuando `hasOnlyEspecialPass` (filtrar client-side por `isSpecial`). El estado especial se evalúa DESPUÉS de holiday/attended/booked/full/past (conservan prioridad). (4) Dialog APP-03: agregar `showAuraInfoDialog` ref + un `q-dialog` clon de `showCoverageDialog` con ícono dorado `auto_awesome`, título "Actividades con Aura", body con precios $10.000 socios / $20.000 externos, UN solo botón "Entendido" (`v-close-popup`), SIN pago ni CTA de compra (D-02). En el catch de `confirmReserve` agregar la rama `axios.isAxiosError(err) && err.response?.data?.code === 'PASS_REQUIRED'` → cerrar reserveDialog + `showAuraInfoDialog = true`. (5) Chip contador: un único `q-chip` dorado en el header de la semana, visible sólo si `hasEspecialPass`, label "Especiales · {especialClassesRemaining}/{budget}"; en 0/2 el label y tono cambian. (6) Refresh: llamar `userStore.loadEspecialPass()` donde hoy se llama `loadGrid()`/`loadSubscription()` (montaje + post reserva/cancelación). Naming lock: NUNCA "pase(s)" en texto visible. Acento dorado SÓLO en los 4 usos listados; bloqueo en olive/grey, nunca rojo. Copy exacto del UI-SPEC.
  </action>
  <acceptance_criteria>
    - `grep -n 'slot.isSpecial\|isSpecial' el-templo-app/src/pages/ReservasPage.vue` matchea (badge + estados + filtro E5).
    - `grep -n 'canAccessGrid' el-templo-app/src/pages/ReservasPage.vue` matchea y se usa en el empty-state.
    - `grep -n 'PASS_REQUIRED' el-templo-app/src/pages/ReservasPage.vue` matchea (rama del catch).
    - `grep -n 'showAuraInfoDialog' el-templo-app/src/pages/ReservasPage.vue` matchea (ref + dialog).
    - `grep -n 'especialClassesRemaining\|loadEspecialPass' el-templo-app/src/pages/ReservasPage.vue` matchea (chip + refresh).
    - `grep -ci '>pase\|"pase\|pases\| pase ' el-templo-app/src/pages/ReservasPage.vue` == 0 en texto visible (naming lock D-01; revisar manualmente strings de template).
    - `cd el-templo-app && npx vue-tsc --noEmit` sin errores nuevos.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-app && npx vue-tsc --noEmit</automated>
  </verify>
  <done>Badge + E1-E5 + gate + dialog PASS_REQUIRED + chip x/2 + refresh implementados; naming/color respetados; vue-tsc verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Card del pase en Mi Templo</name>
  <files>el-templo-app/src/modules/progression/pages/MiTemplo.vue</files>
  <read_first>
    - el-templo-app/src/modules/progression/pages/MiTemplo.vue (ReferralCtaCard :22 — primera card premium gateada; showReservaCta :104,250)
    - .planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-UI-SPEC.md (§APP-02 ítem 2 — card Mi Templo)
  </read_first>
  <behavior>
    - Si hasEspecialPass, se muestra una card "Actividades con Aura" con ícono dorado auto_awesome y value "{especialClassesRemaining} de 2 clases este mes".
    - Si no hay pase, la card no se renderiza.
  </behavior>
  <action>
    Agregar una card (análoga a `ReferralCtaCard` / al `info-card` premium existente) visible sólo si `userStore.hasEspecialPass`, con ícono `auto_awesome` dorado, label "Actividades con Aura" y value "{especialClassesRemaining} de 2 clases este mes" + vencimiento del período si está disponible. Reusar el markup/estética de card existente (sin CSS nuevo salvo el color dorado del ícono). Asegurar `userStore.loadEspecialPass()` en el montaje de Mi Templo si la card se muestra ahí (o confiar en la carga global del store). Naming lock: "Actividades con Aura", nunca "pase".
  </action>
  <acceptance_criteria>
    - `grep -n 'hasEspecialPass' el-templo-app/src/modules/progression/pages/MiTemplo.vue` matchea (gate de la card).
    - `grep -n 'Actividades con Aura\|especialClassesRemaining' el-templo-app/src/modules/progression/pages/MiTemplo.vue` matchea.
    - `grep -ci '>pase\|"pase\|pases' el-templo-app/src/modules/progression/pages/MiTemplo.vue` == 0 en texto visible.
    - `cd el-templo-app && npx vue-tsc --noEmit` sin errores nuevos.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-app && npx vue-tsc --noEmit</automated>
  </verify>
  <done>Card del pase gateada por hasEspecialPass con saldo x/2; vue-tsc verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — UAT visual de la superficie del pase (member app + admin)</name>
  <what-built>
    Superficie completa de "Actividades con Aura": badge + estados E1-E5 en la grilla, dialog informativo APP-03, gate refinado (externo-solo-pase entra, socio no se bloquea), chip x/2 + card Mi Templo, y el tab "Especiales" del admin (KPIs + tabla + export) construido en 162-06.
  </what-built>
  <how-to-verify>
    Precondición: `cd el-templo-app && npx vue-tsc --noEmit` y `cd el-templo-admin && npx vue-tsc --noEmit` verdes (gate manual — CI no cubre frontends).

    Member app (correr el app localmente y abrir Reservas con distintos usuarios de prueba):
    1. Socio presencial CON pase y saldo 2/2 → ve badge dorado "Especial" en la especial de sábado, botón Reservar (E1), y el chip "Especiales · 2/2" en el header + card en Mi Templo. Reservar descuenta; reabrir → 1/2 (contador refresca).
    2. Socio presencial CON pase 0/2 → pill "Usaste tus 2 clases" sin botón (E2).
    3. Socio presencial SIN pase → especial con "Requiere plan especial" (olive/grey, sin rojo); al tocar abre el dialog "Actividades con Aura" con precios $10.000/$20.000 y un solo botón "Entendido" (SIN CTA de pago). El socio sigue viendo/reservando sus regulares normalmente (NO se bloquea — Pitfall 1).
    4. Externo-solo-pase → entra a la grilla (no ve el empty-state), ve SOLO las especiales (regulares ocultas — E5), con badge y estado según saldo.
    5. Naming: en ninguna pantalla aparece la palabra "pase"; el acento dorado sólo en badge/ícono/chip/dialog.

    Admin (abrir Analíticas → tab "Especiales"):
    6. El tab muestra KPIs Socios/Externos activos, la tabla de asistencias por actividad socio/externo/total (sin montos) del mes, y el export descarga especiales-YYYY-MM.xlsx.

  </how-to-verify>
  <action>Pausa de verificación humana (member app y admin no tienen e2e). El ejecutor NO escribe código en esta task: corre los dos vue-tsc como precondición, arranca las apps localmente y guía al humano por los pasos de how-to-verify. No continuar hasta recibir "approved".</action>
  <resume-signal>Escribí "approved" si todo se ve/comporta según el UI-SPEC, o describí los problemas encontrados.</resume-signal>
  <verify>Humano confirma los 6 puntos de how-to-verify; ambos vue-tsc verdes como precondición.</verify>
  <done>UAT visual aprobado por el humano ("approved"); superficie del pase (member + admin) verificada contra el UI-SPEC.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                  |
| ---------------------------------------- | ------------------------------------------------------------ |
| render del cliente → decisión de reserva | El estado del slot es display; el gating real es del backend |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                         | Disposition | Mitigation Plan                                                                                                                                                    |
| ----------- | ---------------------- | ------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-162-05-01 | Elevation of Privilege | estado de slot / gate de página como autorización | accept      | El render y el gate son sólo UX. El backend re-valida cada reserva (161-06); un externo que fuerce ver una regular igual recibe PASS_REQUIRED/GATE-04 al reservar. |
| T-162-05-02 | Information Disclosure | precios/copy en el cliente                        | accept      | El copy APP-03 (precios $10.000/$20.000) es marketing interno público, no PII.                                                                                     |
| T-162-05-03 | Tampering              | usuario fuerza isSpecial/capabilities en memoria  | accept      | No otorga acceso: la autoridad es server-side. Sin efecto de seguridad.                                                                                            |
| T-162-SC    | Tampering              | npm installs                                      | mitigate    | Cero paquetes nuevos (Quasar/axios ya presentes).                                                                                                                  |

</threat_model>

<verification>
- `cd el-templo-app && npx vue-tsc --noEmit` verde (gate manual — CI no cubre el app).
- Checkpoint humano aprobado (member app + admin).
- Naming lock (cero "pase" visible) y acento dorado reservado a los 4 usos.
</verification>

<success_criteria>

- Badge + estados E1-E5 en la grilla.
- Dialog informativo APP-03 sin pago in-app.
- Gate refinado: externo-solo-pase entra, socio no se bloquea.
- Chip x/2 + card Mi Templo con saldo, con refresh.
- UAT visual aprobado.
  </success_criteria>

<output>
Create `.planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-05-SUMMARY.md` when done
</output>
