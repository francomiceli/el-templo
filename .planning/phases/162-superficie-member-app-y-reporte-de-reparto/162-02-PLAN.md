---
phase: 162-superficie-member-app-y-reporte-de-reparto
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/subscriptions/member-routes.ts
  - el-templo-api/src/modules/subscriptions/schemas.ts
  - el-templo-api/test/subscriptions/especial-pass-member.test.ts
autonomous: true
requirements: [APP-02]

must_haves:
  truths:
    - "GET /members/subscription/me/especial-pass devuelve { hasPass:false } cuando el member no tiene pase especial activo"
    - "Para un socio con pase, devuelve hasPass:true, classesRemaining, classesBudget, endDate e isSocio:true"
    - "Para un externo con pase, devuelve isSocio:false"
    - "El userId sale SIEMPRE de request.user — el endpoint no acepta ningún userId param (mitigación IDOR)"
  artifacts:
    - path: "el-templo-api/src/modules/subscriptions/member-routes.ts"
      provides: "endpoint GET /me/especial-pass derivado de getMemberSubscriptions plural"
      contains: "/me/especial-pass"
    - path: "el-templo-api/test/subscriptions/especial-pass-member.test.ts"
      provides: "cobertura socio/externo/sin-pase del endpoint"
      contains: "especial-pass"
  key_links:
    - from: "GET /me/especial-pass"
      to: "subscriptionService.getMemberSubscriptions(request.user.userId)"
      via: "filtro categoryGroup(planCategory)==='especial' + status active/paused"
      pattern: "getMemberSubscriptions\\(request\\.user\\.userId\\)"
    - from: "clasificación isSocio"
      to: "getPlanById(pass.planId).requiresPresencial"
      via: "discriminador Socio↔Externo (161-01/02)"
      pattern: "requiresPresencial"
---

<objective>
Crear el endpoint aditivo `GET /me/especial-pass` que expone el pase especial del member y su saldo (`classesRemaining`/`classesBudget`/`endDate`) más el discriminador `isSocio`. El app hoy sólo conoce UNA sub (singular, sin `classesRemaining`); este endpoint alimenta el contador x/2 (APP-02) sin tocar la semántica del `subscription` singular que consume media app.

Purpose: aislar la superficie del pase en un endpoint chico y testeable (cero regresión sobre `/me/subscription`). Es el dato que consume `loadEspecialPass` del store (162-04).
Output: endpoint member protegido, con response schema y test de integración socio/externo/sin-pase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-RESEARCH.md
@.planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-PATTERNS.md

<interfaces>
<!-- Contratos del núcleo 161 verificados en esta rama: -->
<!-- getMemberSubscriptions(userId) plural (service.ts:963) → trae planCategory, classesRemaining, classesBudget, status, endDate, planId. -->
<!-- categoryGroup(c) (subscriptions/types.ts:39) → 'presencial' | 'especial' | 'online'. USAR ESTE, no isOnlinePlan (lumpea especial con online). -->
<!-- getPlanById(planId).requiresPresencial (mapPlanRow, 161-02) → boolean, discrimina Socio (true) ↔ Externo (false). -->
<!-- Patrón IDOR: GET /coverage (member-routes.ts:126-135) deriva userId de request.user, sin param. -->
<!-- Guard heredado del plugin (member-routes.ts:50-53): onRequest → fastify.authenticate + attachCountryScope. NO re-agregar. -->
<!-- Prefijo del plugin: /members/subscription (el app hoy llama /members/subscription/me/subscription). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Endpoint GET /me/especial-pass + response schema</name>
  <files>el-templo-api/src/modules/subscriptions/member-routes.ts, el-templo-api/src/modules/subscriptions/schemas.ts</files>
  <read_first>
    - el-templo-api/src/modules/subscriptions/member-routes.ts (GET /me/subscription :56-110 singular; GET /coverage :126-135 patrón IDOR; guard del plugin :50-53)
    - el-templo-api/src/modules/subscriptions/service.ts (getMemberSubscriptions plural :963; getPlanById)
    - el-templo-api/src/modules/subscriptions/types.ts (categoryGroup :39-51)
    - .planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-PATTERNS.md (§member-routes.ts — implementación de referencia)
  </read_first>
  <behavior>
    - Member sin sub especial active/paused → responde { hasPass: false }.
    - Member con pase Socio → { hasPass:true, classesRemaining, classesBudget, endDate, isSocio:true }.
    - Member con pase Externo → isSocio:false.
    - El endpoint no lee ningún userId de params/query/body.
  </behavior>
  <action>
    Agregar `fastify.get("/me/especial-pass", ...)` en member-routes.ts (mismo plugin, guard heredado — no re-declarar authenticate). Llamar `subscriptionService.getMemberSubscriptions(request.user.userId)`, buscar el primer sub con `categoryGroup(s.planCategory) === "especial"` y `status` en `active`/`paused`. Si no hay, retornar `{ hasPass: false }`. Si hay, resolver `plan = await subscriptionService.getPlanById(pass.planId)` y retornar `{ hasPass:true, classesRemaining: pass.classesRemaining ?? 0, classesBudget: pass.classesBudget ?? 0, endDate: pass.endDate, isSocio: plan?.requiresPresencial ?? false }`. Declarar el response schema en schemas.ts (patrón de las demás rutas member) con las 5 props tipadas — sin declararlas fast-json-stringify las strippea. NO extender `/me/subscription` a array (regresión de superficie amplia). NO usar `isOnlinePlan` (lumpea especial con online).
  </action>
  <acceptance_criteria>
    - `grep -n '/me/especial-pass' el-templo-api/src/modules/subscriptions/member-routes.ts` matchea.
    - `grep -n 'getMemberSubscriptions(request.user.userId)' el-templo-api/src/modules/subscriptions/member-routes.ts` matchea (userId server-derived).
    - `grep -c 'userId' el-templo-api/src/modules/subscriptions/member-routes.ts` — el nuevo handler NO introduce lectura de userId desde params/query/body (revisión: sólo request.user.userId).
    - `grep -n 'requiresPresencial' el-templo-api/src/modules/subscriptions/member-routes.ts` matchea (clasificación isSocio).
    - `cd el-templo-api && npx tsc --noEmit` sin errores nuevos.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit</automated>
  </verify>
  <done>Endpoint responde la forma contractada; userId siempre de request.user; response schema declarado; tsc verde.</done>
</task>

<task type="auto">
  <name>Task 2: Test de integración especial-pass-member.test.ts (socio/externo/sin-pase)</name>
  <files>el-templo-api/test/subscriptions/especial-pass-member.test.ts</files>
  <read_first>
    - el-templo-api/test/subscriptions/especial-pass.test.ts (o especial-consumption.test.ts de 161 — seed directo de subs especiales por Drizzle + createTestPlan con planCategory/monthlyClassBudget/requiresPresencial)
    - el-templo-api/test/helpers.ts (auth como member, requests, assignTestPlan)
  </read_first>
  <action>
    Crear el test que, contra MySQL real, cubre tres casos pegando a `/members/subscription/me/especial-pass` autenticado como el member: (a) sin pase → `hasPass:false`; (b) pase Socio (plan `requiresPresencial:true`, `monthlyClassBudget:2`) → `hasPass:true`, `isSocio:true`, `classesRemaining`/`classesBudget` correctos; (c) pase Externo (`requiresPresencial:false`) → `isSocio:false`. Seedear las subs especiales por Drizzle directo (patrón de los tests de 161, ya que el ABM admin no persiste monthly_class_budget aún). Reusar los helpers de plan/sub existentes.
  </action>
  <acceptance_criteria>
    - El test asierta los 3 casos (hasPass:false, socio isSocio:true, externo isSocio:false) con classesRemaining/classesBudget verificados.
    - `cd el-templo-api && npx vitest run test/subscriptions/especial-pass-member.test.ts` pasa.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && npx vitest run test/subscriptions/especial-pass-member.test.ts</automated>
  </verify>
  <done>Test verde cubriendo socio, externo y sin-pase con las props contractadas.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                                                  |
| -------------------------------------- | ---------------------------------------------------------------------------- |
| member autenticado → API subscriptions | El token identifica al member; el endpoint no debe leer datos de otro member |

## STRIDE Threat Register

| Threat ID   | Category               | Component                         | Disposition | Mitigation Plan                                                                                                                                                  |
| ----------- | ---------------------- | --------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-162-02-01 | Information Disclosure | GET /me/especial-pass (IDOR)      | mitigate    | userId derivado de `request.user.userId`, NUNCA de un param/query/body (espejo de /coverage). El test valida que la respuesta corresponde al member autenticado. |
| T-162-02-02 | Spoofing               | acceso sin auth                   | mitigate    | Guard heredado del plugin (onRequest → fastify.authenticate, member-routes.ts:50-53) cubre la ruta nueva sin código extra.                                       |
| T-162-02-03 | Information Disclosure | fuga de saldo/plan de otro member | mitigate    | La query filtra por el userId del token; classesRemaining/isSocio son del propio member.                                                                         |
| T-162-SC    | Tampering              | npm installs                      | mitigate    | Cero paquetes nuevos (RESEARCH: sin deps). Reusa service/helpers existentes.                                                                                     |

</threat_model>

<verification>
- `cd el-templo-api && npx tsc --noEmit` verde.
- `cd el-templo-api && npx vitest run test/subscriptions/especial-pass-member.test.ts` verde.
- Regresión: `/me/subscription` singular intacto (no se tocó).
</verification>

<success_criteria>

- Endpoint aislado que expone el pase especial + saldo + isSocio.
- IDOR mitigado (userId server-derived).
- Cobertura socio/externo/sin-pase verde.
  </success_criteria>

<output>
Create `.planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-02-SUMMARY.md` when done
</output>
