---
phase: 162-superficie-member-app-y-reporte-de-reparto
plan: 04
type: execute
wave: 2
depends_on: [162-01, 162-02]
files_modified:
  - el-templo-app/src/types/scheduling.ts
  - el-templo-app/src/stores/useUserStore.ts
autonomous: true
requirements: [APP-01, APP-02]

must_haves:
  truths:
    - "El type mirror del frontend WeeklySlotView incluye isSpecial:boolean (sin él, tsc del app falla o el badge se pierde)"
    - "useUserStore expone hasEspecialPass, especialClassesRemaining y hasOnlyEspecialPass derivados del pase cargado"
    - "loadEspecialPass() pega a GET /members/subscription/me/especial-pass y llena el ref del pase"
    - "hasPresencialReservationAccess y el subscription singular NO se modifican (cero regresión)"
    - "clearProfile limpia el ref del pase"
  artifacts:
    - path: "el-templo-app/src/types/scheduling.ts"
      provides: "isSpecial en el mirror de WeeklySlotView"
      contains: "isSpecial"
    - path: "el-templo-app/src/stores/useUserStore.ts"
      provides: "capabilities del pase + loadEspecialPass sin tocar el singular"
      contains: "hasEspecialPass"
  key_links:
    - from: "loadEspecialPass"
      to: "GET /members/subscription/me/especial-pass"
      via: "api.get, mismo try/catch que loadSubscription"
      pattern: "especial-pass"
    - from: "hasOnlyEspecialPass"
      to: "hasEspecialPass && !hasPresencialReservationAccess"
      via: "computed derivado"
      pattern: "hasOnlyEspecialPass"
---

<objective>
Refinar `useUserStore` para que conozca el pase especial sin romper el modelo del `subscription` singular que consume media app. Agregar el mirror `isSpecial` al type del frontend (para el badge de 162-05) y las capabilities del pase (`hasEspecialPass`, `especialClassesRemaining`, `hasOnlyEspecialPass`) + la action `loadEspecialPass`. Esto corrige la latencia de modelo del cliente (Pitfall 1): un socio con presencial+pase NO debe quedar bloqueado de la grilla.

Purpose: base de estado del cliente para la grilla, el gate y el contador (162-05). CI NO typechequea el app → el gate es `vue-tsc` local.
Output: type mirror + capabilities del pase, sin tocar el singular ni `hasPresencialReservationAccess`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-RESEARCH.md
@.planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-PATTERNS.md
@.planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-02-SUMMARY.md

<interfaces>
<!-- Contrato del endpoint (162-02): GET /members/subscription/me/especial-pass -->
<!--   → { hasPass:false } | { hasPass:true, classesRemaining:number, classesBudget:number, endDate:string|null, isSocio:boolean } -->
<!-- Contrato del grid (162-01): WeeklySlotView.isSpecial:boolean ya viaja en la respuesta. -->
<!-- useUserStore.ts: loadSubscription :248-270 (patrón de action con try/catch); -->
<!--   hasPresencialReservationAccess :193-200 (patrón de computed); clearProfile :234-242. -->
<!-- NO modificar subscription singular ni hasPresencialReservationAccess (sostienen entrenamiento/multiBranch/program selector). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mirror isSpecial en el type del frontend</name>
  <files>el-templo-app/src/types/scheduling.ts</files>
  <read_first>
    - el-templo-app/src/types/scheduling.ts (WeeklySlotView :15-29 — header aclara "Mirrors API types")
    - .planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-01-SUMMARY.md (contrato isSpecial del backend)
  </read_first>
  <behavior>
    - WeeklySlotView del frontend incluye isSpecial:boolean, alineado al backend.
  </behavior>
  <action>
    Agregar `isSpecial: boolean` a la interface `WeeklySlotView` (:15-29), con comentario "mirror de scheduling/types.ts (162 APP-01)". Es el único cambio del archivo.
  </action>
  <acceptance_criteria>
    - `grep -n 'isSpecial: boolean' el-templo-app/src/types/scheduling.ts` matchea.
    - `cd el-templo-app && npx vue-tsc --noEmit` sin errores NUEVOS (baseline: ~0 en app; verificar que no aparecen nuevos por isSpecial).
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-app && npx vue-tsc --noEmit</automated>
  </verify>
  <done>Mirror agregado; vue-tsc del app sin errores nuevos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Capabilities del pase + loadEspecialPass en useUserStore</name>
  <files>el-templo-app/src/stores/useUserStore.ts</files>
  <read_first>
    - el-templo-app/src/stores/useUserStore.ts (loadSubscription :248-270; hasPresencialReservationAccess :193-200; clearProfile :234-242; MemberSubscription type :49)
    - .planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-PATTERNS.md (§useUserStore — refs + action + computed)
  </read_first>
  <behavior>
    - especialPass ref se llena desde GET /me/especial-pass.
    - hasEspecialPass = pase presente; especialClassesRemaining = classesRemaining o 0.
    - hasOnlyEspecialPass = hasEspecialPass && !hasPresencialReservationAccess.
    - clearProfile resetea especialPass a null.
    - Ante error o 204, especialPass = null (mismo try/catch que loadSubscription).
  </behavior>
  <action>
    Agregar `especialPass` ref tipado `{ hasPass:boolean; classesRemaining:number; classesBudget:number; endDate:string|null; isSocio:boolean } | null`. Agregar la action `loadEspecialPass()` que pega a `/members/subscription/me/especial-pass` (patrón exacto de `loadSubscription`: try/catch, en catch/204 → null). Agregar computed `hasEspecialPass` (pase presente), `especialClassesRemaining` (`?? 0`), `especialClassesBudget` (`?? 2` o el budget del pase), `hasOnlyEspecialPass` (`hasEspecialPass && !hasPresencialReservationAccess`). Exportar todos en el return del `defineStore`. Resetear `especialPass.value = null` en `clearProfile`. NO tocar `subscription` singular ni `hasPresencialReservationAccess`. Sin `any`; `catch` sin variable o `catch (err: unknown)`.
  </action>
  <acceptance_criteria>
    - `grep -n 'loadEspecialPass' el-templo-app/src/stores/useUserStore.ts` matchea (action + export).
    - `grep -n 'hasEspecialPass\|especialClassesRemaining\|hasOnlyEspecialPass' el-templo-app/src/stores/useUserStore.ts` matchea las 3 computeds.
    - `grep -n 'especial-pass' el-templo-app/src/stores/useUserStore.ts` matchea la URL.
    - `grep -n 'especialPass.value = null' el-templo-app/src/stores/useUserStore.ts` matchea (limpieza en clearProfile).
    - Diff no modifica la definición de `hasPresencialReservationAccess`.
    - `cd el-templo-app && npx vue-tsc --noEmit` sin errores nuevos.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-app && npx vue-tsc --noEmit</automated>
  </verify>
  <done>Capabilities del pase + loadEspecialPass agregados y exportados; singular intacto; vue-tsc verde.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                         |
| ------------------------------ | ------------------------------------------------------------------- |
| store del cliente → gate de UI | Las capabilities derivan el estado de pantalla; NO son autorización |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                  | Disposition | Mitigation Plan                                                                                                                                                                           |
| ----------- | ---------------------- | ------------------------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-162-04-01 | Elevation of Privilege | capabilities client-side como autorización | accept      | El gate del store es sólo UX de pantalla. El backend (161-06) es la autoridad: reservar una regular como externo-solo-pase se rechaza server-side (GATE-04) aunque el cliente lo intente. |
| T-162-04-02 | Tampering              | usuario manipula especialPass en memoria   | accept      | Manipular el ref no otorga acceso: la reserva la valida el backend. Sin datos sensibles nuevos expuestos.                                                                                 |
| T-162-SC    | Tampering              | npm installs                               | mitigate    | Cero paquetes nuevos (Pinia/axios ya presentes).                                                                                                                                          |

</threat_model>

<verification>
- `cd el-templo-app && npx vue-tsc --noEmit` verde (CI NO lo cubre — gate manual obligatorio).
- El singular `subscription` y `hasPresencialReservationAccess` no cambiaron.
</verification>

<success_criteria>

- Type mirror con isSpecial.
- Capabilities del pase derivadas sin romper el singular.
- loadEspecialPass consume el endpoint aislado.
  </success_criteria>

<output>
Create `.planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-04-SUMMARY.md` when done
</output>
