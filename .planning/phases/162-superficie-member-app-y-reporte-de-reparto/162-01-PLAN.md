---
phase: 162-superficie-member-app-y-reporte-de-reparto
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/scheduling/service.ts
  - el-templo-api/src/modules/scheduling/types.ts
  - el-templo-api/src/modules/scheduling/schemas.ts
  - el-templo-api/test/scheduling/especial-gating.test.ts
autonomous: true
requirements: [APP-01]

must_haves:
  truths:
    - "El grid semanal del member (GET /weekly) devuelve isSpecial:true en los slots de actividades especiales y false en las regulares"
    - "El grid admin (GET /schedules/weekly) también expone isSpecial por slot (cobertura doble por schema compartido)"
    - "isSpecial se resuelve server-side desde activities.is_special vía el JOIN existente, nunca desde el request"
  artifacts:
    - path: "el-templo-api/src/modules/scheduling/service.ts"
      provides: "isSpecial seleccionado en getWeeklyGrid y propagado al slots.push"
      contains: "isSpecial"
    - path: "el-templo-api/src/modules/scheduling/schemas.ts"
      provides: "isSpecial declarado en weeklySlotViewSchema (sino fast-json-stringify lo strippea)"
      contains: "isSpecial"
    - path: "el-templo-api/src/modules/scheduling/types.ts"
      provides: "WeeklySlotView.isSpecial en el contrato de la API"
      contains: "isSpecial"
  key_links:
    - from: "getWeeklyGrid .select()"
      to: "schema.activities.isSpecial"
      via: "JOIN a activities ya existente (:189-192)"
      pattern: "isSpecial: schema\\.activities\\.isSpecial"
    - from: "weeklySlotViewSchema"
      to: "response del grid member (:769) y admin (:256)"
      via: "schema compartido como items"
      pattern: "isSpecial:\\s*\\{\\s*type:\\s*\"boolean\""
---

<objective>
Exponer el flag `isSpecial` por slot en la grilla semanal (`getWeeklyGrid`), que hoy NO viaja al frontend. Es el dato de dominio que permite al member app pintar el distintivo "Especial" y decidir el estado por slot (APP-01). El JOIN a `activities` ya existe en `getWeeklyGrid` — sólo falta seleccionar la columna, propagarla al slot, declararla en el response schema (crítico: fast-json-stringify strippea props no declaradas) y espejarla en el type.

Purpose: sin este flag el badge del member nunca aparece (Pitfall 2 del research). Es la base de datos que consumen los planes 162-04/162-05.
Output: `WeeklySlotView.isSpecial` disponible end-to-end en el grid member y admin, con test de integración que lo asierta.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-RESEARCH.md
@.planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-PATTERNS.md

<interfaces>
<!-- Contrato del núcleo 161 ya en esta rama. El JOIN a activities ya existe en getWeeklyGrid. -->
<!-- activities.isSpecial (boolean NOT NULL default false) — columna del schema 0179 (161-01). -->
<!-- Drizzle la mapea a schema.activities.isSpecial. Ya usada por getScheduleSlotRaw (161-05). -->
<!-- Recorrido de referencia idéntico: activityMaxCapacity (155-01) atraviesa los mismos 5 puntos. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Propagar isSpecial por los 4 puntos del backend en getWeeklyGrid</name>
  <files>el-templo-api/src/modules/scheduling/service.ts, el-templo-api/src/modules/scheduling/types.ts, el-templo-api/src/modules/scheduling/schemas.ts</files>
  <read_first>
    - el-templo-api/src/modules/scheduling/service.ts (getWeeklyGrid :134-327 — el .select() de scheduleRows :168-194 y el slots.push :299-319; el JOIN a activities ya está en :189-192)
    - el-templo-api/src/modules/scheduling/types.ts (WeeklySlotView :54-69)
    - el-templo-api/src/modules/scheduling/schemas.ts (weeklySlotViewSchema :57-71 — items de grid admin :256 y member :769)
    - .planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-PATTERNS.md (§scheduling/service.ts, §schemas.ts CRÍTICO)
    - .planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-05-SUMMARY.md (recorrido de isSpecial en getScheduleSlotRaw como espejo)
  </read_first>
  <behavior>
    - El grid member y admin devuelven isSpecial:true para un slot cuya activity tiene is_special=true.
    - El grid devuelve isSpecial:false para un slot de actividad regular.
    - isSpecial nunca se infiere del request — sale del JOIN a activities.
  </behavior>
  <action>
    Espejar exactamente el recorrido de `activityMaxCapacity`. (1) En el `.select()` de `scheduleRows` (service.ts :168-194), agregar `isSpecial: schema.activities.isSpecial` junto a `activityMaxCapacity`. (2) En el objeto de `slots.push({...})` (service.ts :299-319), agregar `isSpecial: row.isSpecial` junto a los demás campos derivados. (3) En `WeeklySlotView` (types.ts :54-69), agregar `isSpecial: boolean` con comentario de fase. (4) CRÍTICO — en `weeklySlotViewSchema.properties` (schemas.ts :57-71), agregar `isSpecial: { type: "boolean" }`; sin este paso fast-json-stringify strippea la prop y el badge nunca llega (Pitfall 2). El JOIN a `activities` ya existe (:189-192) — no agregarlo de nuevo. No tocar el gating (es del backend 161-06); esto es aditivo y de display.
  </action>
  <acceptance_criteria>
    - `grep -c "isSpecial" el-templo-api/src/modules/scheduling/service.ts` ≥ 2 (select + push).
    - `grep -n 'isSpecial: schema.activities.isSpecial' el-templo-api/src/modules/scheduling/service.ts` matchea.
    - `grep -n 'isSpecial: row.isSpecial' el-templo-api/src/modules/scheduling/service.ts` matchea.
    - `grep -n 'isSpecial' el-templo-api/src/modules/scheduling/schemas.ts` matchea `type: "boolean"`.
    - `grep -n 'isSpecial: boolean' el-templo-api/src/modules/scheduling/types.ts` matchea.
    - `cd el-templo-api && npx tsc --noEmit` sin errores nuevos.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit</automated>
  </verify>
  <done>Los 4 puntos tocados; tsc verde; isSpecial declarado en el response schema compartido (cubre member + admin).</done>
</task>

<task type="auto">
  <name>Task 2: Test de integración — el grid member asierta isSpecial por slot</name>
  <files>el-templo-api/test/scheduling/especial-gating.test.ts</files>
  <read_first>
    - el-templo-api/test/scheduling/especial-gating.test.ts (ya seedea actividades especiales y schedules — fixtures reutilizables)
    - el-templo-api/test/helpers.ts (auth + request utilities contra MySQL real)
    - el-templo-api/src/modules/scheduling/routes.ts (member grid GET /weekly :764-809)
  </read_first>
  <action>
    Extender el suite existente de gating especial con un caso que pega al grid semanal del member (GET del endpoint `/weekly`) con una actividad especial y una regular seedeadas, y asierta que el slot de la especial trae `isSpecial === true` y el de la regular `isSpecial === false`. Reusar los fixtures/helpers de seed de especiales ya presentes en el archivo (o `especial-consumption.test.ts` como referencia de seed de subs/actividades). No introducir watch-mode. Si el archivo no tiene un fixture regular, seedear uno mínimo con el helper existente de actividades.
  </action>
  <acceptance_criteria>
    - El nuevo caso asierta `slot.isSpecial === true` para la especial y `=== false` para la regular.
    - `cd el-templo-api && npx vitest run test/scheduling/especial-gating.test.ts` pasa (todos verdes).
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && npx vitest run test/scheduling/especial-gating.test.ts</automated>
  </verify>
  <done>Test verde asertando isSpecial en slots[] del grid member (especial=true, regular=false).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                |
| --------------------------------- | ---------------------------------------------------------- |
| cliente (member/admin) → API grid | El cliente recibe isSpecial pero NO es autoridad de acceso |

## STRIDE Threat Register

| Threat ID   | Category               | Component              | Disposition | Mitigation Plan                                                                                                                                                                                       |
| ----------- | ---------------------- | ---------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-162-01-01 | Tampering              | isSpecial en la grilla | accept      | isSpecial es sólo display; el gating real es server-side en reserve() (161-06). Un cliente que falsee isSpecial NO gana acceso: el backend re-valida por scheduleId→activity.isSpecial en la reserva. |
| T-162-01-02 | Information Disclosure | flag isSpecial         | accept      | isSpecial no es dato sensible (qué actividad es especial es público para cualquiera que vea la grilla). Sin PII.                                                                                      |
| T-162-SC    | Tampering              | npm/pip/cargo installs | mitigate    | Cero paquetes nuevos (RESEARCH: sin deps). No hay superficie de supply-chain en este plan.                                                                                                            |

</threat_model>

<verification>
- `cd el-templo-api && npx tsc --noEmit` verde.
- `cd el-templo-api && npx vitest run test/scheduling/especial-gating.test.ts` verde.
- Regresión: los tests existentes de scheduling siguen verdes en CI (isSpecial es aditivo).
</verification>

<success_criteria>

- El grid member y admin exponen isSpecial:boolean por slot.
- El flag se resuelve server-side desde activities.is_special.
- Test de integración asierta el flag en el grid member.
  </success_criteria>

<output>
Create `.planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-01-SUMMARY.md` when done
</output>
