---
phase: 162-superficie-member-app-y-reporte-de-reparto
plan: 06
type: execute
wave: 2
depends_on: [162-03]
files_modified:
  - el-templo-admin/src/types/analytics.ts
  - el-templo-admin/src/composables/useAnalyticsApi.ts
  - el-templo-admin/src/components/analytics/EspecialesTab.vue
  - el-templo-admin/src/pages/AnaliticasPage.vue
autonomous: true
requirements: [REP-01]

must_haves:
  truths:
    - "Analíticas muestra un tab 'Especiales' (icon auto_awesome) que carga lazy al activarse"
    - "El tab muestra KPIs socio/externo (D-05), una tabla de asistencias por actividad socio/externo/total (sin montos) y un selector de mes"
    - "Un botón exporta el reporte a especiales-YYYY-MM.xlsx vía blob"
    - "El tab vive dentro de la página Analíticas ya gateada por rol (mismo alcance que 'Referidos A/B')"
  artifacts:
    - path: "el-templo-admin/src/components/analytics/EspecialesTab.vue"
      provides: "tab del reporte (KPIs + tabla + export)"
      min_lines: 40
    - path: "el-templo-admin/src/composables/useAnalyticsApi.ts"
      provides: "getEspecialesReport(month) + exportEspeciales(month)"
      contains: "getEspecialesReport"
    - path: "el-templo-admin/src/pages/AnaliticasPage.vue"
      provides: "q-tab + q-tab-panel + fetch en el switch"
      contains: "especiales"
  key_links:
    - from: "AnaliticasPage fetchTabData switch"
      to: "fetchEspeciales → analyticsApi.getEspecialesReport"
      via: "watch(activeTab) lazy load"
      pattern: "case 'especiales'"
    - from: "exportEspeciales"
      to: "GET /admin/analytics/especiales/export (responseType blob)"
      via: "descarga a.download especiales-YYYY-MM.xlsx"
      pattern: "especiales.*export|especiales-\\$"
---

<objective>
Agregar el tab "Especiales" en Analíticas del admin, espejo exacto del patrón "Referidos A/B" (commit `de95b69a`): 4 archivos. Muestra los KPIs D-05 (subs especiales activas socio/externo), la tabla de asistencias por actividad socio/externo/total (SIN montos) con selector de mes, y un botón de export XLSX. Consume los endpoints de 162-03.

Purpose: superficie admin del reporte REP-01 + contador D-05. CI NO typechequea el admin → gate `vue-tsc` local.
Output: tab funcional en Analíticas, lazy-loaded, con export.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-UI-SPEC.md
@.planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-PATTERNS.md
@.planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-03-SUMMARY.md

<interfaces>
<!-- Endpoints (162-03): -->
<!--   GET /admin/analytics/especiales?month=YYYY-MM → { kpis:{sociosActivos,externosActivos}, rows:[{activityName,socioCount,externoCount,total}], month } -->
<!--   GET /admin/analytics/especiales/export?month=YYYY-MM → blob XLSX -->
<!-- Patrón "Referidos A/B" (commit de95b69a) — 4 archivos: -->
<!--   AnaliticasPage.vue: q-tab (:180), q-tab-panel (:299), refs (:573-575), fetch en switch fetchTabData (:800-851), watch(activeTab) (:870). -->
<!--   useAnalyticsApi.ts: getReferralAbResults (:387) [JSON], exportChurnedMembers (:293-308) [blob], return (:428-431). -->
<!--   types/analytics.ts: ReferralAbResults (:801-821). -->
<!--   components/analytics/ReferidosAbTab.vue (estructura props/loading/skeleton) + MiembrosTab.vue (q-table + onExportChurned :477-500). -->
<!-- El tab vive dentro de AnaliticasPage, ya gateada por rol al nivel de página (post-WR-05, mismo alcance que Referidos A/B); el backend re-valida con requireAdminAnalytics. -->
<!-- Ícono auto_awesome (coherente con Aura), NO star (ya lo usa "Clases"). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Type + composable (getEspecialesReport, exportEspeciales)</name>
  <files>el-templo-admin/src/types/analytics.ts, el-templo-admin/src/composables/useAnalyticsApi.ts</files>
  <read_first>
    - el-templo-admin/src/types/analytics.ts (ReferralAbResults :801-821 como patrón de interface del response)
    - el-templo-admin/src/composables/useAnalyticsApi.ts (getReferralAbResults :387 JSON; exportChurnedMembers :293-308 blob; return :428-431)
    - .planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-03-SUMMARY.md (forma exacta del response)
  </read_first>
  <behavior>
    - getEspecialesReport(month) devuelve el EspecialesReport tipado.
    - exportEspeciales(month) devuelve un Blob (responseType:'blob').
  </behavior>
  <action>
    En types/analytics.ts definir `EspecialesReport` (kpis socio/externo + rows actividad×origen), espejo del type del API (comentario de referencia). En useAnalyticsApi.ts agregar `getEspecialesReport(month: string)` (patrón `getReferralAbResults`, `api.get('/admin/analytics/especiales', { params:{ month } })`) y `exportEspeciales(month: string)` (patrón `exportChurnedMembers`, `responseType:'blob'`, endpoint `/admin/analytics/especiales/export`). Exponerlos en el `return` del composable e importar el type nuevo. `extractError` para el manejo de error; sin `any`.
  </action>
  <acceptance_criteria>
    - `grep -n 'EspecialesReport' el-templo-admin/src/types/analytics.ts` matchea.
    - `grep -n 'getEspecialesReport\|exportEspeciales' el-templo-admin/src/composables/useAnalyticsApi.ts` matchea ambas (def + return).
    - `grep -n "/admin/analytics/especiales" el-templo-admin/src/composables/useAnalyticsApi.ts` matchea (JSON + export).
    - `cd el-templo-admin && npx vue-tsc --noEmit` sin errores NUEVOS (baseline ~23 preexistentes; no aumentar).
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit</automated>
  </verify>
  <done>Type + composable listos y exportados; vue-tsc sin errores nuevos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EspecialesTab.vue + wiring en AnaliticasPage</name>
  <files>el-templo-admin/src/components/analytics/EspecialesTab.vue, el-templo-admin/src/pages/AnaliticasPage.vue</files>
  <read_first>
    - el-templo-admin/src/components/analytics/ReferidosAbTab.vue (estructura props/loading/skeleton/empty)
    - el-templo-admin/src/components/analytics/MiembrosTab.vue (q-table churnedColumns :502; onExportChurned :477-500)
    - el-templo-admin/src/pages/AnaliticasPage.vue (tabs :180; panels :299; refs :573-575; fetchTabData switch :800-851)
    - .planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-UI-SPEC.md (§Component Contract — Admin (REP-01), Copywriting Contract)
  </read_first>
  <behavior>
    - El tab "Especiales" aparece en Analíticas y carga al activarse (lazy).
    - Muestra KPIs "Socios con plan especial activo" / "Externos con plan especial activo".
    - Tabla: Actividad | Asistencias socio | Asistencias externo | Total (sin montos), con fila de totales.
    - Selector de mes (default mes en curso); empty state "No hubo asistencias a actividades especiales en {mes}.".
    - Botón export descarga especiales-{YYYY-MM}.xlsx.
  </behavior>
  <action>
    Crear `EspecialesTab.vue` (props `data: EspecialesReport | null`, `loading: boolean`): header `text-subtitle2` "Asistencias a Especiales — {mes}", fila de 2 `q-card` KPI (patrón MiembrosTab), `q-table` con columnas Actividad/socio/externo/Total + fila de totales, selector de mes, empty state, y botón export (icon `download`, `:loading`) que llama `exportEspeciales(month)` y descarga el blob con `a.download = \`especiales-${month}.xlsx\`` (patrón `onExportChurned`). SIN montos. Copy exacto del UI-SPEC Copywriting Contract. En AnaliticasPage.vue espejar los 5 puntos del patrón Referidos A/B: `<q-tab name="especiales" label="Especiales" icon="auto_awesome" />`, `<q-tab-panel name="especiales">` con `<EspecialesTab :data :loading />`, import del componente + type, refs `especialesData`/`loadingEspeciales`, y `fetchEspeciales()` + `case 'especiales': await fetchEspeciales(); break;` en `fetchTabData`. Naming lock D-01: "Especiales"/"Actividades con Aura", NUNCA "pase(s)".
  </action>
  <acceptance_criteria>
    - `grep -n 'name="especiales"' el-templo-admin/src/pages/AnaliticasPage.vue` matchea (tab + panel).
    - `grep -n "case 'especiales'" el-templo-admin/src/pages/AnaliticasPage.vue` matchea (fetch lazy).
    - `grep -n 'auto_awesome' el-templo-admin/src/pages/AnaliticasPage.vue` matchea (ícono correcto, no star).
    - `grep -ci 'pase' el-templo-admin/src/components/analytics/EspecialesTab.vue` == 0 (naming lock).
    - `grep -ci 'monto\|amount\|precio\|\\$' el-templo-admin/src/components/analytics/EspecialesTab.vue` == 0 (sin montos).
    - `grep -n 'especiales-\${\|especiales-' el-templo-admin/src/components/analytics/EspecialesTab.vue` matchea el filename de export.
    - `cd el-templo-admin && npx vue-tsc --noEmit` sin errores nuevos.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit</automated>
  </verify>
  <done>Tab renderiza KPIs + tabla sin montos + export; lazy load wireado; naming lock respetado; vue-tsc verde.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                    |
| ------------------------------- | ---------------------------------------------- |
| admin (browser) → API analytics | El tab consume endpoints ya protegidos por rol |

## STRIDE Threat Register

| Threat ID   | Category               | Component                    | Disposition | Mitigation Plan                                                                                                                                                                                         |
| ----------- | ---------------------- | ---------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-162-06-01 | Elevation of Privilege | tab visible a rol sin acceso | mitigate    | El tab vive dentro de AnaliticasPage, gateada por rol al nivel de página (mismo alcance que Referidos A/B post-WR-05); el backend re-valida con requireAdminAnalytics (162-03). El frontend sólo pinta. |
| T-162-06-02 | Information Disclosure | fuga de plata en la UI       | mitigate    | El tab no renderiza montos (D-04); acceptance grep == 0.                                                                                                                                                |
| T-162-SC    | Tampering              | npm installs                 | mitigate    | Cero paquetes nuevos (Quasar/axios ya presentes).                                                                                                                                                       |

</threat_model>

<verification>
- `cd el-templo-admin && npx vue-tsc --noEmit` verde (CI NO lo cubre — gate manual obligatorio).
- Naming lock: cero "pase" visible; sin montos.
</verification>

<success_criteria>

- Tab "Especiales" en Analíticas con KPIs D-05 + tabla socio/externo + export XLSX.
- Lazy load consistente con el patrón Referidos A/B.
- Naming y "sin montos" respetados.
  </success_criteria>

<output>
Create `.planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-06-SUMMARY.md` when done
</output>
