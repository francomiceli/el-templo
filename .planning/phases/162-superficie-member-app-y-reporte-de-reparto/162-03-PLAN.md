---
phase: 162-superficie-member-app-y-reporte-de-reparto
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/analytics/especial-report-service.ts
  - el-templo-api/src/modules/analytics/routes.ts
  - el-templo-api/test/analytics/especial-report.test.ts
autonomous: true
requirements: [REP-01]

must_haves:
  truths:
    - "GET /admin/analytics/especiales?month=YYYY-MM devuelve asistencias por actividad especial y mes, separadas en socio/externo, SIN montos"
    - "GET /admin/analytics/especiales/export?month=YYYY-MM devuelve un XLSX con las filas actividad×origen"
    - "El reporte incluye los KPIs D-05: conteo de subs especiales activas separadas socio/externo"
    - "Un socio y un externo que asisten a la misma actividad el mismo mes se cuentan como 1 y 1 en las columnas correctas"
    - "Un member con dos subs especial de periodos solapados/contiguos (renovacion Externo->Socio) que asistio una vez se cuenta una sola vez, sin JOIN-fanout"
    - "Sólo roles con acceso a Analíticas ven el reporte (requireAdminAnalytics + requireBranchAccess)"
  artifacts:
    - path: "el-templo-api/src/modules/analytics/especial-report-service.ts"
      provides: "derivación socio/externo por sub que cubre session_date + fallback + conteo D-05"
      min_lines: 40
    - path: "el-templo-api/src/modules/analytics/routes.ts"
      provides: "GET /especiales (JSON) + /especiales/export (XLSX) protegidos"
      contains: "/especiales"
    - path: "el-templo-api/test/analytics/especial-report.test.ts"
      provides: "cobertura clasificación socio/externo + agregación + export + D-05"
      contains: "especial-report"
  key_links:
    - from: "especial-report-service"
      to: "attendance → schedules → activities(is_special) → sub especial que cubre session_date → subscription_plans.requires_presencial"
      via: "JOIN acotado por startDate<=session_date<=endDate"
      pattern: "requires_presencial|requiresPresencial"
    - from: "GET /especiales/export"
      to: "sendExcelReply"
      via: "shared/excel.ts (styleHeaderRow + sendExcelReply)"
      pattern: "sendExcelReply"
---

<objective>
Construir el reporte REP-01: asistencias a actividades especiales por mes, clasificadas por origen socio/externo (insumo del reparto manual a profes de Nacho — SIN montos, D-04), más los KPIs D-05 (subs especiales activas por origen). Endpoint JSON + export XLSX, ambos protegidos por el guard de analytics. `attendance` NO guarda subscription_id, así que la clasificación se deriva del `requires_presencial` de la sub especial que cubre la `session_date`, con una regla de fallback explícita para asistencias huérfanas.

Purpose: dar visibilidad de la línea "Especiales" y el insumo del reparto. Es el dato que consume el tab admin (162-06).
Output: `especial-report-service.ts` (nuevo) + 2 rutas en `analytics/routes.ts` + test de integración.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-RESEARCH.md
@.planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-PATTERNS.md

<interfaces>
<!-- attendance (db/schema/attendance.ts): memberId, scheduleId, sessionDate, branchId, checkedInAt. NO subscription_id. -->
<!-- Índice idx_attendance_schedule_session_date sostiene el JOIN por scheduleId+sessionDate. -->
<!-- activities.isSpecial (boolean) — flag de gating (161-01). -->
<!-- subscription_plans.requiresPresencial (boolean) — discrimina Socio(true)↔Externo(false) (161-01). -->
<!-- categoryGroup(planCategory)==='especial' (subscriptions/types.ts:39) — identifica la sub del pase. -->
<!-- Patrón export: analytics/routes.ts /churned-members/export :539-606 (Workbook + styleHeaderRow + sendExcelReply). -->
<!-- Guard: requireAdminAnalytics (analytics/routes.ts:66) + requireBranchAccess({from:"query.branchId",optional:true}). -->
<!-- Service análogo: analytics/attendance-metrics-service.ts (clase que agrega sobre attendance). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: especial-report-service.ts — clasificación socio/externo + fallback + D-05</name>
  <files>el-templo-api/src/modules/analytics/especial-report-service.ts</files>
  <read_first>
    - el-templo-api/src/modules/analytics/attendance-metrics-service.ts (estructura de clase + queries sobre attendance :26-49)
    - el-templo-api/src/db/schema/attendance.ts (columnas — confirmar sin subscription_id)
    - el-templo-api/src/modules/analytics/scope.ts (applyScope para branch scope)
    - el-templo-api/src/modules/subscriptions/types.ts (categoryGroup :39)
    - .planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-PATTERNS.md (§especial-report-service — recorrido de JOINs)
  </read_first>
  <behavior>
    - Para un mes dado, agrega asistencias a actividades especiales por (actividad × origen).
    - Origen socio si la sub especial que cubre la session_date tiene requires_presencial=true; externo si =false.
    - Fallback (asistencia especial SIN sub especial que cubra la fecha, ej. bypass staff): si el member tenía una sub PRESENCIAL activa que cubre la session_date → socio; si no → externo.
    - D-05: conteo separado de subs especiales activas (active/paused) por origen (requires_presencial).
    - Sin montos en ningún punto.
  </behavior>
  <action>
    Crear la clase `EspecialReportService` (constructor `db` + `log`, espejo de `AttendanceMetricsService`). Método del reporte: `attendance` del mes → INNER JOIN `schedules` → INNER JOIN `activities` filtrando `isSpecial = true` → LEFT JOIN `subscriptions` del member con `categoryGroup==='especial'` y `startDate <= session_date <= endDate` → JOIN `subscription_plans` por `requiresPresencial`. Agrupar por `activityId` y origen (`requiresPresencial` → 'socio' | 'externo'). Regla de fallback explícita y COMENTADA en el código: cuando no hay sub especial que cubra la fecha, clasificar consultando si el member tenía una sub `presencial` active/paused cubriendo la `session_date` → 'socio', si no → 'externo' (mejor evidencia disponible; caso raro, sólo por bypass staff D-07). Método D-05: contar subs `especial` en estado active/paused agrupadas por `requiresPresencial` (socio/externo). Aplicar branch scope con `applyScope`. Sin `any`; `catch (err: unknown)`. Logging con `this.log`.
  </action>
  <acceptance_criteria>
    - `grep -n 'isSpecial' el-templo-api/src/modules/analytics/especial-report-service.ts` matchea (filtro de actividades especiales).
    - `grep -n 'requiresPresencial' el-templo-api/src/modules/analytics/especial-report-service.ts` matchea (clasificación).
    - Existe un comentario que documenta la regla de fallback (grep de `fallback` o `bypass` o `sin sub especial`).
    - `grep -v '^\s*//' el-templo-api/src/modules/analytics/especial-report-service.ts | grep -ci 'monto\|amount\|price\|precio'` == 0 (sin montos).
    - `cd el-templo-api && npx tsc --noEmit` sin errores nuevos.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit</automated>
  </verify>
  <done>Service con clasificación socio/externo por sub que cubre la fecha + fallback documentado + conteo D-05; tsc verde.</done>
</task>

<task type="auto">
  <name>Task 2: Rutas GET /especiales (JSON) + /especiales/export (XLSX) en analytics/routes.ts</name>
  <files>el-templo-api/src/modules/analytics/routes.ts</files>
  <read_first>
    - el-templo-api/src/modules/analytics/routes.ts (/churned-members + /churned-members/export :539-606; requireAdminAnalytics :66; imports :24-57)
    - el-templo-api/src/modules/shared/excel.ts (styleHeaderRow, sendExcelReply)
    - .planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-PATTERNS.md (§analytics/routes.ts — patrón ruta protegida + export)
  </read_first>
  <action>
    Agregar dos rutas espejando `/churned-members` y `/churned-members/export`. (1) `GET /especiales` con Querystring `{ month: string (YYYY-MM), branchId?: number }`, preHandler `[requireAdminAnalytics, requireBranchAccess({ from: "query.branchId", optional: true })]`, schema JSON que valida `month` con pattern `^\d{4}-\d{2}$`; llama al service y devuelve `{ kpis: { sociosActivos, externosActivos }, rows: [{ activityName, socioCount, externoCount, total }], month }`. (2) `GET /especiales/export` con la misma Querystring y guards; arma `Workbook` (`creator="El Templo"`), worksheet "Especiales" con columnas Actividad/Asistencias socio/Asistencias externo/Total/Mes, `styleHeaderRow`, filas del service, y retorna `sendExcelReply(workbook, reply, "especiales")`. Envolver en `try/catch (err: unknown)` con `handleServiceError`. NO armar el XLSX a mano (usar el helper). Instanciar el nuevo service donde se instancian los demás services del módulo.
  </action>
  <acceptance_criteria>
    - `grep -c '/especiales' el-templo-api/src/modules/analytics/routes.ts` ≥ 2 (JSON + export).
    - `grep -n 'requireAdminAnalytics' el-templo-api/src/modules/analytics/routes.ts` presente en ambas rutas nuevas (revisión de los preHandler).
    - `grep -n 'sendExcelReply(.*especiales' el-templo-api/src/modules/analytics/routes.ts` matchea.
    - `grep -n 'YYYY-MM\|\\d{4}-\\d{2}\|[0-9]{4}-[0-9]{2}' el-templo-api/src/modules/analytics/routes.ts` — month validado por schema.
    - `cd el-templo-api && npx tsc --noEmit` sin errores nuevos.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit</automated>
  </verify>
  <done>Ambas rutas protegidas y tipadas; export vía sendExcelReply; month validado; tsc verde.</done>
</task>

<task type="auto">
  <name>Task 3: Test de integración especial-report.test.ts</name>
  <files>el-templo-api/test/analytics/especial-report.test.ts</files>
  <read_first>
    - el-templo-api/test/scheduling/especial-gating.test.ts / especial-consumption.test.ts (seed de actividades especiales + subs + attendance helpers de 161)
    - el-templo-api/test/helpers.ts (auth admin, requests, createTestPlan con requiresPresencial/monthlyClassBudget)
    - el-templo-api/test/analytics/ (algún test existente de analytics como patrón de auth admin + branch scope)
  </read_first>
  <action>
    Crear el test que, contra MySQL real: (a) seedea una actividad especial + schedule, un member socio (pase `requiresPresencial:true`) y un member externo (pase `requiresPresencial:false`), registra 1 asistencia de cada uno a la misma actividad el mismo mes, y asierta que `GET /admin/analytics/especiales?month=YYYY-MM` devuelve socioCount=1 y externoCount=1 para esa actividad; (b) asierta los KPIs D-05 (sociosActivos/externosActivos ≥ 1 cada uno); (c) asierta que `/especiales/export` responde `content-type` de XLSX y un body no vacío; (d) caso de fallback: una asistencia a especial de un member SIN sub especial que cubra la fecha pero CON presencial activo esa fecha → clasifica 'socio' (fija la regla del orchestrator); (e) caso de renovacion/solape (anti JOIN-fanout, Pitfall 4): un member con DOS subs `especial` de periodos contiguos/solapados (renovo Externo->Socio: la sub externa termina y la socio arranca el mismo dia o con un dia de solape) que asistio UNA sola vez a la especial -> el reporte cuenta esa asistencia UNA sola vez (sin fanout por el LEFT JOIN de la sub) y la clasifica por la sub que cubre la `session_date` (Socio), sin duplicar filas ni sumar doble. Autenticar como admin con acceso a analytics.
  </action>
  <acceptance_criteria>
    - El test asierta socioCount=1 / externoCount=1 en la misma actividad/mes.
    - El test asierta los KPIs D-05.
    - El test asierta content-type XLSX en el export.
    - El test asierta el fallback (sin sub especial + presencial activo → socio).
    - El test asierta que una asistencia de un member con dos subs especial solapadas/contiguas (renovacion Externo->Socio) se cuenta UNA sola vez (sin duplicado por JOIN-fanout) y se clasifica por la sub que cubre la fecha.
    - `cd el-templo-api && npx vitest run test/analytics/especial-report.test.ts` pasa.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && npx vitest run test/analytics/especial-report.test.ts</automated>
  </verify>
  <done>Test verde cubriendo clasificación socio/externo, D-05, export XLSX, la regla de fallback y el caso de renovacion/solape sin duplicado (Pitfall 4).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                             |
| --------------------------------- | ----------------------------------------------------------------------- |
| admin autenticado → API analytics | Sólo roles con acceso a Analíticas pueden consultar/exportar el reporte |
| reporte → datos de members        | El reporte agrega asistencias; no debe filtrar datos financieros        |

## STRIDE Threat Register

| Threat ID   | Category               | Component                         | Disposition | Mitigation Plan                                                                                                                                   |
| ----------- | ---------------------- | --------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-162-03-01 | Elevation of Privilege | GET /especiales(/export)          | mitigate    | preHandler `requireAdminAnalytics` + `requireBranchAccess` en ambas rutas (patrón churned). Un coach/recepción sin acceso a analytics recibe 403. |
| T-162-03-02 | Information Disclosure | fuga de plata en el reporte       | mitigate    | REP-01 es de asistencias, SIN montos (D-04). El service no joinea transactions/caja; grep de acceptance == 0 en montos.                           |
| T-162-03-03 | Information Disclosure | fuga cross-branch                 | mitigate    | `applyScope`/`requireBranchAccess` acotan por sede del admin.                                                                                     |
| T-162-03-04 | Tampering              | month param malicioso (injection) | mitigate    | `month` validado por JSON-schema pattern YYYY-MM; querying vía Drizzle parametrizado (sin string concat).                                         |
| T-162-SC    | Tampering              | npm installs                      | mitigate    | Cero paquetes nuevos (exceljs ya presente).                                                                                                       |

</threat_model>

<verification>
- `cd el-templo-api && npx tsc --noEmit` verde.
- `cd el-templo-api && npx vitest run test/analytics/especial-report.test.ts` verde.
- Guards presentes en ambas rutas; sin montos en el service.
</verification>

<success_criteria>

- Reporte de asistencias socio/externo por actividad/mes, sin montos.
- KPIs D-05 (subs especiales activas por origen).
- Export XLSX vía sendExcelReply.
- Regla de fallback documentada y testeada.
  </success_criteria>

<output>
Create `.planning/phases/162-superficie-member-app-y-reporte-de-reparto/162-03-SUMMARY.md` when done
</output>
