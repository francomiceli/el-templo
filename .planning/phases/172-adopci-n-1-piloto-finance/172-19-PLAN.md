---
phase: 172-adopci-n-1-piloto-finance
plan: 19
type: execute
wave: 13
depends_on: ["172-17"]
files_modified:
  - el-templo-api/test/tenancy/iso-03-finance-coach-load.test.ts
autonomous: true
requirements: [ISO-03]
must_haves:
  truths:
    - "Las 7 rutas de coach-load y las 4 de movimientos/egresos tienen caso de aislamiento con control positivo (D-08)"
    - "El coach del gimnasio 2 no puede autocompletar, cobrar ni ver cargas del gimnasio 1"
    - "Un movimiento entre cajas o un egreso no puede apuntar a una caja o centro de costo del otro gimnasio"
  artifacts:
    - path: "el-templo-api/test/tenancy/iso-03-finance-coach-load.test.ts"
      provides: "batería ISO-03 para coach-load, movements y expenses"
      min_lines: 280
  key_links:
    - from: "el-templo-api/test/tenancy/iso-03-finance-coach-load.test.ts"
      to: "gym2.coachToken"
      via: "cada ruta corre con el rol mínimo real que puede usarla (D-10)"
      pattern: "coachToken"
---

<objective>
Tercer archivo de la batería ISO-03: las 7 rutas de `coach-load` (probadas con el rol
COACH, el mínimo real) y las 4 de movimientos y egresos.

Purpose: D-10 lo dice explícito — la evidencia más fuerte viene del borde menos
privilegiado. `coach-load-routes.ts` es el único archivo del módulo que deja entrar al
coach, y las cajas son el activo más sensible del arqueo.
Output: archivo verde que completa las 38 rutas finance del manifiesto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/172-adopci-n-1-piloto-finance/172-CONTEXT.md
@.planning/phases/172-adopci-n-1-piloto-finance/172-PATTERNS.md

Working dir: `/home/franco/projects/et-172`. Contrato D-09: 404 / vacío, nunca 403.
</context>

<tasks>

<task type="auto">
  <name>Task 1: coach-load con rol coach (7 rutas)</name>
  <files>el-templo-api/test/tenancy/iso-03-finance-coach-load.test.ts</files>
  <read_first>
    - el-templo-api/test/tenancy/iso-03-finance-cajas.test.ts (plantilla del plan 172-17)
    - el-templo-api/test/fixtures/second-tenant.ts L155-167 (el handle trae `coachToken` y `coachId` del gimnasio 2 — D-10 sale gratis) y el fixture de finanzas del plan 172-17
    - el-templo-api/test/tenant-manifest.ts L258-269 y L296-298 (GET /coach-load/autocompletar/:userId, /coach-load/bank-accounts, /coach-load/caja-efectivo, /coach-load/mis-cargas; POST /coach-load/alta, /coach-load/misc, /coach-load/pay-plan)
    - el-templo-api/src/modules/finance/coach-load-routes.ts L249 (hook con FINANCE_LOAD_ROLES: el coach entra acá y NO en finance/routes.ts)
  </read_first>
  <action>
    Crear `test/tenancy/iso-03-finance-coach-load.test.ts` con el ciclo de vida de la
    plantilla y **`gym2.coachToken` como actor por defecto** de las 7 rutas de coach-load
    (D-10: rol mínimo real).
    Por ruta, `it` de aislamiento + `it` de control:
    - `GET /coach-load/autocompletar/:userId` con el id de un SOCIO DEL GIMNASIO 1 → 404 o
      payload vacío; con un socio propio → datos completos.
    - `GET /coach-load/bank-accounts` y `/coach-load/caja-efectivo` → sin cuentas ni cajas
      del gimnasio 1; con las propias, sí.
    - `GET /coach-load/mis-cargas` → solo cargas propias del coach del gimnasio 2.
    - `POST /coach-load/alta`, `/misc` y `/pay-plan` apuntando a un socio, una sede, un plan
      o una caja DEL GIMNASIO 1 → falla (404), y ninguna fila nueva queda en las tablas del
      gimnasio 1 (verificar con `tenantDeLaFila` sobre lo que sí se haya creado, y contando
      filas del gimnasio 1 antes y después).
    - Control positivo de cada POST con recursos propios: la fila creada tiene
      `tenant_id = TENANT_DOS` en `financial_transactions` y en `balances`.
    Si alguna ruta exige un rol mayor que coach, crear el staff con
    `createStaffUser(app, { ..., tenantId: TENANT_DOS })` y dejar escrito en el nombre del
    `describe` con qué rol corre y por qué.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit && pnpm exec vitest run test/tenancy/iso-03-finance-coach-load.test.ts --hookTimeout=250000</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "coachToken" test/tenancy/iso-03-finance-coach-load.test.ts` devuelve 7 o más
    - Cada una de las 7 rutas de coach-load tiene `it` de aislamiento y `it` de control
    - Al menos un caso cuenta filas del gimnasio 1 antes y después del intento de escritura ajena
    - `grep -c "toBe(403)" test/tenancy/iso-03-finance-coach-load.test.ts` devuelve 0
    - El archivo pasa entero
  </acceptance_criteria>
  <done>El borde menos privilegiado de finance queda probado: el coach ajeno no ve ni escribe nada del gimnasio 1.</done>
</task>

<task type="auto">
  <name>Task 2: movimientos entre cajas y egresos (4 rutas)</name>
  <files>el-templo-api/test/tenancy/iso-03-finance-coach-load.test.ts</files>
  <read_first>
    - el-templo-api/test/tenant-manifest.ts L306-309 (POST /expenses, /expenses/:id/void, /movements, /movements/:id/void)
    - el-templo-api/src/modules/finance/movement-service.ts (loadCaja valida caja origen y destino; registerExpense valida el centro de costo)
    - el-templo-api/test/fixtures/finance-gimnasio-dos.ts (cajaId y costCenterId del gimnasio 2)
  </read_first>
  <action>
    Agregar los `describe` de movimientos y egresos, con el rol mínimo que las permite:
    - `POST /movements` con `origenCajaId` propio y `destinoCajaId` DEL GIMNASIO 1 (y la
      combinación inversa) → falla con 404 y NINGÚN saldo cambia en ninguno de los dos
      gimnasios (verificar saldos antes/después con consulta directa).
    - `POST /expenses` con un `costCenterId` DEL GIMNASIO 1 → 404; con el propio → la fila
      queda en `TENANT_DOS`.
    - `POST /movements/:id/void` y `/expenses/:id/void` sobre un movimiento/egreso DEL
      GIMNASIO 1 → 404 y la fila ajena sigue sin anular (`voidedAt` intacto).
    - Control positivo de cada una con recursos propios.
    Cerrar con la lista completa de las 38 rutas finance del manifiesto y en qué archivo de
    la batería quedó cada una — esa lista la consume el gate del plan 172-20.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit && pnpm exec vitest run test/tenancy/iso-03-finance-coach-load.test.ts --hookTimeout=250000</automated>
  </verify>
  <acceptance_criteria>
    - Las 4 rutas de movimientos/egresos tienen `it` de aislamiento y `it` de control
    - Al menos un caso compara saldos de caja antes y después de un movimiento cross-tenant rechazado
    - El SUMMARY lista las 38 rutas finance del manifiesto con el archivo de batería que las cubre (14 + 13 + 11)
    - `grep -c "toBe(403)" test/tenancy/iso-03-finance-coach-load.test.ts` devuelve 0
    - El archivo pasa entero
  </acceptance_criteria>
  <done>Movimientos y egresos cross-tenant rechazados sin mover un peso, y la batería cubre las 38 rutas.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                       | Description                                                        |
| -------------------------------------------------------------- | ------------------------------------------------------------------ |
| coach del gimnasio 2 → coach-load                              | rol mínimo con acceso a cobro: el borde más barato de atacar       |
| ids de caja y centro de costo del body → movimientos y egresos | mover plata a una caja ajena es la corrupción contable más directa |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                    | Disposition | Mitigation Plan                                                         |
| ----------- | ---------------------- | -------------------------------------------- | ----------- | ----------------------------------------------------------------------- |
| T-172-19-01 | Information Disclosure | autocompletar de socio con id ajeno          | mitigate    | caso con `coachToken` del gimnasio 2 y socio del gimnasio 1 → 404/vacío |
| T-172-19-02 | Tampering              | movimiento con caja destino de otro gimnasio | mitigate    | caso que además compara saldos antes/después en ambos gimnasios         |
| T-172-19-03 | Tampering              | anulación de movimiento/egreso ajeno         | mitigate    | 404 + `voidedAt` de la fila ajena verificado intacto                    |
| T-172-19-04 | Elevation of Privilege | cobro de coach-load con recursos ajenos      | mitigate    | conteo de filas del gimnasio 1 antes/después del intento                |
| T-172-SC    | Tampering              | instalación de paquetes                      | accept      | este plan no instala nada                                               |

</threat_model>

<verification>
- Archivo verde con las 11 rutas cubiertas.
- Saldos verificados antes/después en los rechazos.
- Lista de 38 rutas con su archivo de cobertura en el SUMMARY.
</verification>

<success_criteria>
La batería ISO-03 cubre las 38 rutas `tenant-scoped` de finance del manifiesto, con el rol
mínimo real por ruta.
</success_criteria>

<output>
Create `.planning/phases/172-adopci-n-1-piloto-finance/172-19-SUMMARY.md` when done
</output>
