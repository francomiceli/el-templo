---
phase: 172-adopci-n-1-piloto-finance
plan: 21
type: execute
wave: 12
depends_on: ["172-20"]
files_modified:
  - el-templo-api/src/db/tenant-tables.ts
  - el-templo-api/tenant-lint-allowlist.json
  - el-templo-api/test/db/tenant-tables.test.ts
autonomous: true
requirements: [ADO-01, ISO-03]
must_haves:
  truths:
    - "TENANT_STRICT_MODULES tiene la entrada finance con las 6 tablas físicas y el sentinel hace throw en test/dev sobre ellas"
    - "La allowlist del lint queda sin ninguna entrada sobre las 6 tablas strict ni sobre archivos de src/modules/finance/"
    - "El fail-closed está demostrado en vivo: una query sin tenant sobre una tabla strict revienta con TenantSentinelError"
  artifacts:
    - path: "el-templo-api/src/db/tenant-tables.ts"
      provides: "primera entrada de TENANT_STRICT_MODULES del milestone"
      contains: "financial_transactions"
    - path: "el-templo-api/tenant-lint-allowlist.json"
      provides: "allowlist sin las 47 entradas de finance"
    - path: "el-templo-api/test/db/tenant-tables.test.ts"
      provides: "gate de forma reescrito para la fase de adopción"
  key_links:
    - from: "el-templo-api/src/db/tenant-tables.ts"
      to: "el-templo-api/src/db/sentinel/install.ts"
      via: "strictTablesSet alimenta la matriz de severidad del sentinel"
      pattern: "TENANT_STRICT_MODULES"
---

<objective>
El interruptor (D-03): escribir la entrada `finance` en `TENANT_STRICT_MODULES`, vaciar
las 47 entradas de allowlist correspondientes, reescribir el gate de forma que hoy exige
lista vacía, y DEMOSTRAR el fail-closed en vivo.

Purpose: es la primera entrada strict del milestone. A partir de acá, cualquier query sin
`tenant_id` sobre las 6 tablas de finance es una REGRESIÓN que revienta en test y dev, no
deuda tolerada. Va último a propósito: la suite nunca queda roja entre planes.
Output: sentinel en throw para finance, allowlist limpia, gates verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/172-adopci-n-1-piloto-finance/172-CONTEXT.md
@.planning/phases/172-adopci-n-1-piloto-finance/172-PATTERNS.md

Working dir: `/home/franco/projects/et-172`. Este plan es el ÚNICO dueño de
`tenant-lint-allowlist.json` en toda la fase.
</context>

<tasks>

<task type="auto">
  <name>Task 1: entrada finance en TENANT_STRICT_MODULES + gate de forma reescrito</name>
  <files>el-templo-api/src/db/tenant-tables.ts, el-templo-api/test/db/tenant-tables.test.ts</files>
  <read_first>
    - el-templo-api/src/db/tenant-tables.ts L465-515 (docblock de TENANT_STRICT_MODULES: el VALOR son nombres de tabla FÍSICA, los de `getTableName()`, no las constantes TypeScript) y L64-170 (GYM_OWNED_TABLES, donde figuran balances L73, cash_registers L83, cost_centers L89, debt_management L91, financial_transactions L99, transaction_links L139)
    - el-templo-api/test/db/tenant-tables.test.ts L340-470 (el `it` de L351 que hoy afirma `modulos.length === 0` y cuyo mensaje ya explica qué implica agregar la primera entrada; y los otros 4 gates del describe: tabla existe en GYM_OWNED_TABLES, ninguna en dos módulos, claves en minúscula, coherencia isStrictTable/strictTablesSet)
    - el-templo-api/src/db/sentinel/install.ts L40-60 (matriz de severidad: en test/development una tabla strict pasa a THROW)
  </read_first>
  <action>
    Escribir `finance: ["balances", "cash_registers", "cost_centers", "debt_management",
    "financial_transactions", "transaction_links"]` en `TENANT_STRICT_MODULES` — EXACTAMENTE
    esas 6, nombres FÍSICOS, verificados contra `GYM_OWNED_TABLES`.
    `aura_balances` y `aura_transactions` quedan FUERA (D-05: son de gamification, su throw
    llega con su módulo).
    Reescribir el `it` de L351: en vez de "arranca vacía en la fase 170", afirmar que
    `finance` está presente con exactamente esas 6 tablas y que ninguna otra clave existe
    todavía; el mensaje de rojo tiene que decir que agregar un módulo nuevo es una decisión
    de la fase de adopción de ESE módulo y que obliga a vaciar sus entradas de allowlist
    (D-15). Los otros 4 gates del describe pasan a verde solos: NO reescribirlos, y dejar
    intacto el anti-tautología de L459 (`isStrictTable("bookings") === false`).
    Agregar un `it` nuevo que afirme la coherencia con la allowlist: ninguna entrada de
    `tenant-lint-allowlist.json` puede tener `table` entre las 6 strict (es el gate D-15
    escrito como test, además del que ya hace el lint).
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit && pnpm exec vitest run test/db/tenant-tables.test.ts --hookTimeout=250000</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "financial_transactions" src/db/tenant-tables.ts` devuelve 2 o más (GYM_OWNED_TABLES + la entrada strict)
    - `node -e "const {TENANT_STRICT_MODULES}=require('./dist/db/tenant-tables.js')"` no es necesario, pero `pnpm exec vitest run test/db/tenant-tables.test.ts` pasa entero
    - El `it` de L351 ya no afirma `length === 0` y su mensaje nombra la fase de adopción
    - `pnpm exec tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>El sentinel pasa a modo throw para las 6 tablas de finance en test y dev.</done>
</task>

<task type="auto">
  <name>Task 2: vaciar las 47 entradas de allowlist y dejar el lint verde</name>
  <files>el-templo-api/tenant-lint-allowlist.json</files>
  <read_first>
    - el-templo-api/tenant-lint-allowlist.json (header con el contrato del ratchet: la lista solo se achica; entradas ganadas o stale = CI rojo)
    - .planning/phases/172-adopci-n-1-piloto-finance/172-PATTERNS.md sección "Allowlist: las entradas exactas a borrar" (33 sobre tablas strict + 14 no-finance en archivos del módulo = 47)
    - los SUMMARY de los planes 172-02, 03, 04, 06, 09, 11 y 12 (cada uno ya probó con una copia filtrada que sus entradas quedaron muertas)
  </read_first>
  <action>
    Borrar exactamente 47 entradas: las 33 cuyo `table` esté entre las 6 strict (en
    analytics ×6, coach ×1, members ×1, reports ×4, subscriptions ×3, backfill ×3 y los 5
    archivos de `src/modules/finance/` ×15) y las 14 restantes cuyo `file` esté bajo
    `el-templo-api/src/modules/finance/` (branches, program_enrollments, subscription_plans,
    subscriptions, user_status_history, users). Resultado esperado: 501 → 454, y CERO
    entradas con `file` en `src/modules/finance/`.
    Correr `pnpm lint:tenant` (sin flags, contra la allowlist real): tiene que salir verde.
    Si aparece una violación, se arregla la QUERY — está prohibido devolver la entrada a la
    allowlist (D-14/D-15) y prohibido sacar la tabla de la lista strict.
    Registrar en el SUMMARY el conteo antes/después y el listado de las 47 borradas.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm lint:tenant && node -e "const a=require('./tenant-lint-allowlist.json');const l=Array.isArray(a)?a:a.entries;const S=['balances','cash_registers','cost_centers','debt_management','financial_transactions','transaction_links'];const mal=l.filter(e=>S.includes(e.table)||e.file.includes('src/modules/finance/'));if(mal.length){console.error(mal);process.exit(1)};console.log('entradas',l.length)"</automated>
  </verify>
  <acceptance_criteria>
    - La allowlist tiene 454 entradas (47 menos que las 501 iniciales)
    - Cero entradas con `table` entre las 6 strict y cero con `file` bajo `src/modules/finance/`
    - `pnpm lint:tenant` sale con código 0
    - El diff de `tenant-lint-allowlist.json` solo BORRA líneas (ninguna agregada)
  </acceptance_criteria>
  <done>La allowlist ya no tolera ni una query de finance sin gimnasio.</done>
</task>

<task type="auto">
  <name>Task 3: demo del fail-closed en vivo + suites dirigidas verdes</name>
  <files>ninguno (sonda revertida sin commitear)</files>
  <read_first>
    - el-templo-api/test/tenancy/con-05-sentinel.test.ts L30-40 y L285-386 (el throw llega envuelto: a través de Drizzle lo que se atrapa es un `DrizzleQueryError` con el `TenantSentinelError` en `cause`; hay que recorrer la cadena de `cause`, un `instanceof` directo NO entra nunca)
    - el-templo-api/src/db/sentinel/install.ts L175-195 (texto exacto del mensaje del error: "[sentinel de tenancy] query sin tenant_id sobre <tablas> …")
  </read_first>
  <action>
    Demo en vivo, patrón de sonda revertida (168-05 / 169-04 / 171): sacar TEMPORALMENTE el
    `tenantWhere` de UNA query de lectura de `transaction-service.ts` (por ejemplo la de
    `getById`), correr un test que la ejercite, y capturar la salida mostrando el
    `TenantSentinelError` en la cadena de `cause` con el SQL adentro del mensaje. Pegar esa
    salida en el SUMMARY como evidencia. Después REVERTIR con
    `git checkout -- src/modules/finance/transaction-service.ts` y confirmar que la suite
    vuelve a verde. La sonda NO se commitea.
    Cerrar corriendo dirigidas las suites que más superficie de finance tocan, ya con el
    strict encendido de verdad: `test/tenancy`, `test/finance`, `test/subscriptions`,
    `test/reports`, `test/analytics`, `test/members`, `test/coach`, `test/programs`.
    Registrar en el SUMMARY qué directorios corrieron y su resultado; el suite COMPLETO lo
    corre CI en el plan 172-22 (regla del repo: el suite completo no se corre local).
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec vitest run test/tenancy test/finance test/subscriptions test/reports --hookTimeout=900000</automated>
  </verify>
  <acceptance_criteria>
    - El SUMMARY incluye la salida real del `TenantSentinelError` (mensaje con "[sentinel de tenancy] query sin tenant_id sobre" y el SQL), obtenida con la sonda
    - `git -C /home/franco/projects/et-172 status --porcelain el-templo-api/src/modules/finance/transaction-service.ts` no devuelve nada al terminar
    - `test/tenancy`, `test/finance`, `test/subscriptions` y `test/reports` pasan enteros con el strict activo
    - `pnpm exec vitest run test/analytics test/members test/coach test/programs --hookTimeout=900000` pasa entero (resultado en el SUMMARY)
  </acceptance_criteria>
  <done>El fail-closed está probado con evidencia, no afirmado, y las suites pesadas pasan con el throw encendido.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                    |
| ------------------------------------- | -------------------------------------------------------------- |
| lista strict → severidad del sentinel | sacar una tabla de la lista apaga el gate de todo el módulo    |
| allowlist → lint de CI                | devolver una entrada convierte una regresión en deuda tolerada |

## STRIDE Threat Register

| Threat ID   | Category          | Component                                                  | Disposition | Mitigation Plan                                                                                                               |
| ----------- | ----------------- | ---------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------- |
| T-172-21-01 | Tampering         | apagar el throw sacando una tabla de TENANT_STRICT_MODULES | mitigate    | prohibido por el mensaje del propio error y por el gate de forma reescrito; el `it` nuevo exige las 6 tablas exactas          |
| T-172-21-02 | Tampering         | devolver entradas a la allowlist para tapar una violación  | mitigate    | criterio de aceptación exige que el diff del JSON solo BORRE líneas + gate D-15 como test                                     |
| T-172-21-03 | Repudiation       | afirmar el fail-closed sin probarlo                        | mitigate    | demo en vivo con salida real del `TenantSentinelError` leído por la cadena de `cause`, pegada en el SUMMARY                   |
| T-172-21-04 | Denial of Service | throw en producción                                        | accept      | por diseño el sentinel NUNCA hace throw en `mode: "log"` (prod/staging): la matriz de severidad ya lo garantiza (README §4.2) |
| T-172-SC    | Tampering         | instalación de paquetes                                    | accept      | este plan no instala nada                                                                                                     |

</threat_model>

<verification>
- Gates de `test/db/tenant-tables.test.ts` verdes con la entrada nueva.
- `pnpm lint:tenant` verde con 454 entradas y cero de finance.
- `TenantSentinelError` demostrado en vivo y sonda revertida.
- 8 directorios de test verdes con el strict activo.
</verification>

<success_criteria>
`finance` es el primer módulo del milestone en modo strict: toda query sin gimnasio sobre
sus 6 tablas es una regresión que revienta en test y dev, y no queda ni una excepción viva.
</success_criteria>

<output>
Create `.planning/phases/172-adopci-n-1-piloto-finance/172-21-SUMMARY.md` when done
</output>
