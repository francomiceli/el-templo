---
phase: 172-adopci-n-1-piloto-finance
plan: 22
type: execute
wave: 16
depends_on: ["172-21"]
files_modified: []
autonomous: false
requirements: [ADO-01, ISO-03]
must_haves:
  truths:
    - "El suite COMPLETO pasa en CI con el sentinel en throw para finance"
    - "Los agregadores de finance dan exactamente los mismos números en staging antes y después de la fase"
    - "El staff cobra, valida, arquea y consulta deuda sin notar ningún cambio"
  artifacts:
    - path: "$HOME/.el-templo-snapshots/172/despues.json"
      provides: "foto de los agregadores después del deploy, para el diff con antes.json"
  key_links:
    - from: "$HOME/.el-templo-snapshots/172/antes.json"
      to: "$HOME/.el-templo-snapshots/172/despues.json"
      via: "snapshot-finance-endpoints.ts --diff"
      pattern: "--diff"
---

<objective>
Cerrar el criterio 4 de la fase: CI verde con el suite completo, diff de números vacío en
staging, y confirmación del staff.

Purpose: todo lo anterior probó aislamiento; esto prueba que NO se rompió nada. Es la
mitad del contrato de la fase ("sin cambio para el staff") y la que no se puede automatizar
del todo.
Output: evidencia de CI, diff vacío y OK del staff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/172-adopci-n-1-piloto-finance/172-CONTEXT.md
@.claude/skills/el-templo-change-control/SKILL.md

Working dir: `/home/franco/projects/et-172`, rama `feat/172-adopcion-finance`.
Staging-first ESTRICTO: esta rama va a `staging`, nunca directo a master.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: aprobar el push a staging y confirmar CI verde</name>
  <what-built>
    `finance` migrado entero al patrón de tenancy (services con TenantContext, helpers en
    todo acceso, closures de rutas scopeadas), 47 entradas de allowlist borradas, entrada
    `finance` en TENANT_STRICT_MODULES (sentinel en throw en test/dev), batería ISO-03 con
    las 38 rutas y su gate de cobertura fail-closed.
  </what-built>
  <how-to-verify>
    1. Revisar el diff completo de la rama: `git -C /home/franco/projects/et-172 diff origin/master...HEAD --stat`.
    2. Confirmar que NO hay migraciones nuevas (esta fase no toca schema): el diff no debe
       incluir nada bajo `el-templo-api/src/db/migrations/`.
    3. Autorizar el push de `feat/172-adopcion-finance` a `staging` (regla del repo: nunca
       pushear sin pedir OK, y nunca mergear feature work directo a master).
    4. Esperar CI: type check, lint, audit, **suite de integración COMPLETO** y build. El
       suite completo con el sentinel en throw es la prueba que no se corre local.
    5. Si CI queda rojo por un `TenantSentinelError`, el mensaje trae el SQL: se arregla la
       query en un plan de gap, nunca sacando la tabla de la lista strict.
  </how-to-verify>
  <action>
    Mostrar el diff de la rama contra `origin/master` (stat + confirmación de que no hay
    migraciones nuevas), pedir autorización explícita para pushear
    `feat/172-adopcion-finance` a `staging`, y esperar el resultado de CI. Sin OK no se
    pushea (regla del repo).
  </action>
  <done>Rama en staging y CI verde con el suite completo, con el sentinel en throw.</done>
  <resume-signal>Escribí "ci verde en staging" (o pegá el fallo de CI)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: snapshot "después" y diff contra la línea de base</name>
  <files>ninguno versionado (escribe $HOME/.el-templo-snapshots/172/despues.json)</files>
  <read_first>
    - el-templo-api/src/scripts/snapshot-finance-endpoints.ts (modos --out y --diff, rango fijo 2026-01-01..2026-06-30, normalización)
    - .planning/phases/172-adopci-n-1-piloto-finance/172-05-SUMMARY.md (cómo se capturó `antes.json` y con qué baseUrl)
  </read_first>
  <action>
    Con el mismo `SNAPSHOT_BASE_URL` y un token de admin/owner de staging, capturar
    `$HOME/.el-templo-snapshots/172/despues.json` y diffear contra `antes.json` con el modo
    `--diff` del script.
    El diff tiene que ser VACÍO. Si algún endpoint difiere, NO se ajusta el script ni el
    rango: se investiga la query que cambió (el sospechoso más probable es un `tenantWhere`
    agregado a un join que antes traía filas de más, o un filtro puesto en el lugar
    equivocado del `and`). Documentar en el SUMMARY el endpoint, el path JSON divergente y
    la causa.
    Pegar en el SUMMARY el resumen del diff (endpoints comparados y resultado).
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsx src/scripts/snapshot-finance-endpoints.ts --diff=$HOME/.el-templo-snapshots/172/antes.json $HOME/.el-templo-snapshots/172/despues.json</automated>
  </verify>
  <acceptance_criteria>
    - `despues.json` existe con los mismos 7 endpoints en status 200 que `antes.json`
    - El modo `--diff` sale con código 0 (sin diferencias)
    - El SUMMARY registra los 7 endpoints comparados
  </acceptance_criteria>
  <done>Los agregadores de finance dan exactamente los mismos números que antes de la migración.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT del staff en staging — cobrar, validar, arquear, deudas</name>
  <what-built>
    El módulo de finanzas migrado a tenancy, ya desplegado en staging y con el diff de
    números vacío.
  </what-built>
  <how-to-verify>
    En el admin de STAGING, con un usuario de staff real:
    1. **Cobrar:** dar de alta un cobro por coach-load (alta / misc / pay-plan) y verificar
       que aparece en la bandeja de pendientes con el monto y la caja correctos.
    2. **Validar:** validar ese cobro eligiendo caja y confirmar que el saldo de la caja se
       mueve como siempre.
    3. **Arquear:** abrir Cajas → saldos y comparar los totales con los de antes del deploy
       (los mismos que capturó el snapshot).
    4. **Movimientos y egresos:** registrar un movimiento entre dos cajas y un egreso con
       centro de costo; anular uno de los dos.
    5. **Deudas:** abrir el reporte de deuda, filtrar por sede y estado, exportar, y editar
       una gestión de deuda.
    6. **Exports:** bajar el export de transacciones y el de bandeja; abrirlos y confirmar
       que traen las mismas columnas y filas de siempre.
    7. Cualquier 403 con mensaje "No se pudo resolver el gimnasio" es un bug de esta fase
       (un `assertTenant` en una ruta cuyo scope no resuelve): reportarlo con la URL.
  </how-to-verify>
  <action>
    Entregarle a Franco la lista de 7 verificaciones de staging, esperar el resultado y
    registrar en el SUMMARY qué se probó y con qué usuario. Cualquier desvío se anota como
    gap de la fase con URL y pasos.
  </action>
  <done>El staff confirmó que cobro, validación, arqueo, movimientos, deudas y exports se comportan igual que antes.</done>
  <resume-signal>Escribí "uat ok" o listá qué se comportó distinto (con URL y pasos)</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                     |
| ------------------------------- | --------------------------------------------------------------- |
| rama de fase → staging → master | staging-first estricto; el tren a master es una decisión aparte |
| token de staging → snapshot     | el JSON de salida tiene plata real                              |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                                | Disposition | Mitigation Plan                                                                                                          |
| ----------- | ---------------------- | -------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------ |
| T-172-22-01 | Denial of Service      | 403 TENANT_UNRESOLVED en una ruta cuyo scope no resuelve | mitigate    | UAT explícito busca ese error por nombre; fail-closed es correcto pero un scope no resoluble en una ruta legítima es bug |
| T-172-22-02 | Repudiation            | "los números están bien" sin evidencia                   | mitigate    | diff determinístico contra la baseline capturada antes del deploy                                                        |
| T-172-22-03 | Information Disclosure | snapshots con datos de socios                            | mitigate    | viven en `$HOME/.el-templo-snapshots/`, fuera del repo; no se commitean                                                  |
| T-172-SC    | Tampering              | instalación de paquetes                                  | accept      | este plan no instala nada                                                                                                |

</threat_model>

<verification>
- CI verde con el suite completo y el sentinel en throw.
- `--diff` con código 0.
- OK explícito del staff sobre cobro, validación, arqueo, movimientos, deudas y exports.
</verification>

<success_criteria>
La fase cumple su otra mitad: nadie del staff nota nada, y hay evidencia numérica de eso.
</success_criteria>

<output>
Create `.planning/phases/172-adopci-n-1-piloto-finance/172-22-SUMMARY.md` when done
</output>
