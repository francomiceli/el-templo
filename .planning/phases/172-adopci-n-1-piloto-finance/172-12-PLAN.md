---
phase: 172-adopci-n-1-piloto-finance
plan: 12
type: execute
wave: 7
depends_on: ["172-10"]
files_modified:
  - el-templo-api/src/modules/finance/transaction-service.ts
autonomous: true
requirements: [ADO-01]
must_haves:
  truths:
    - "Listados, bandeja de pendientes, historial de movimientos, resumen y exports solo devuelven filas del gimnasio del staff"
    - "El helper buildListConditions estampa el filtro de gimnasio dentro del fragmento que devuelve"
    - "transaction-service.ts queda sin ninguna entrada viva de allowlist"
  artifacts:
    - path: "el-templo-api/src/modules/finance/transaction-service.ts"
      provides: "caminos de lectura y agregación scopeados"
      contains: "tenantWhere(schema.financialTransactions"
  key_links:
    - from: "el-templo-api/src/modules/finance/transaction-service.ts"
      to: "buildListConditions"
      via: "el ctx entra al helper y el filtro sale dentro del SQL[]"
      pattern: "buildListConditions\\(\\s*ctx"
---

<objective>
Migrar las queries de los caminos de LECTURA y agregación de `transaction-service.ts`:
`list`, `buildListConditions`, `listForMember`, `listPendingMiscForMember`,
`listPendingTray`, `listMovEgresos`, `getSummary`, `getFinancialHistory`,
`getOutstandingConcepts` y `exportRowsForExcel`.

Purpose: son los números que el staff mira todo el día (bandeja, historial, resumen,
exports) y más de 150 referencias a tablas strict. Al terminar, el archivo más grande del
módulo queda sin una sola query sin gimnasio.
Las firmas ya tienen `ctx` desde el plan 172-08: este plan no cambia ninguna.
Output: `transaction-service.ts` completo y fuera de la allowlist (verificado con copia
filtrada).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/172-adopci-n-1-piloto-finance/172-CONTEXT.md
@.planning/phases/172-adopci-n-1-piloto-finance/172-PATTERNS.md

Working dir: `/home/franco/projects/et-172`. La allowlist real no se edita (plan 172-21).
</context>

<tasks>

<task type="auto">
  <name>Task 1: list, buildListConditions y los listados por socio</name>
  <files>el-templo-api/src/modules/finance/transaction-service.ts</files>
  <read_first>
    - el-templo-api/src/modules/finance/transaction-service.ts L1005-1091 (listForMember: 3 accesos a financial_transactions y 4 a transaction_links; listPendingMiscForMember L1049: 14 a financial_transactions)
    - el-templo-api/src/modules/finance/transaction-service.ts L1091-1261 (list: 27 a financial_transactions, 6 a transaction_links, 8 a users, 5 a branches)
    - el-templo-api/src/modules/finance/transaction-service.ts L1623-1681 (buildListConditions: helper privado que devuelve SQL[])
    - el-templo-api/src/modules/reports/service.ts (el patrón de "ctx dentro del fragmento SQL" que dejó escrito el plan 172-03)
  </read_first>
  <action>
    `buildListConditions` devuelve `SQL[]`: el filtro entra como PRIMER elemento del array
    devuelto (`tenantWhere(schema.financialTransactions, ctx)`), no en el llamador — así
    todas las queries que componen ese fragmento quedan scopeadas de una.
    En `list`, `listForMember` y `listPendingMiscForMember`: `tenantWhere` primer término
    sobre `schema.financialTransactions` y `schema.transactionLinks`, y también sobre
    `schema.users` y `schema.branches` (D-06: el archivo tiene que quedar sin ninguna
    entrada de allowlist).
    No cambiar paginación, orden ni filtros de negocio.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '1623,1681p' src/modules/finance/transaction-service.ts | grep -c "tenantWhere("` devuelve 1 o más
    - `sed -n '1005,1261p' src/modules/finance/transaction-service.ts | grep -c "tenantWhere("` devuelve 6 o más
    - `git diff -- el-templo-api/src/modules/finance/transaction-service.ts | grep -cE "^[-+].*(limit|offset|orderBy)\("` devuelve 0
    - `pnpm exec tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>Los listados de transacciones no pueden mostrar una fila de otro gimnasio.</done>
</task>

<task type="auto">
  <name>Task 2: bandeja de pendientes, movimientos/egresos y resumen</name>
  <files>el-templo-api/src/modules/finance/transaction-service.ts</files>
  <read_first>
    - el-templo-api/src/modules/finance/transaction-service.ts L1261-1462 (listPendingTray: 31 accesos a financial_transactions, 5 a cash_registers, 8 a users, 5 a branches)
    - el-templo-api/src/modules/finance/transaction-service.ts L1462-1623 (listMovEgresos: 31 a financial_transactions, 8 a cash_registers, 3 a cost_centers, 5 a users, 8 a branches)
    - el-templo-api/src/modules/finance/transaction-service.ts L1912-2068 (getSummary: 24 a financial_transactions, 11 a branches)
  </read_first>
  <action>
    `tenantWhere` primer término en cada query de los tres métodos, por CADA tabla que
    aparece en el statement: `financial_transactions`, `cash_registers`, `cost_centers`,
    `transaction_links`, `users` y `branches`. Estos tres métodos son los que alimentan
    `/pending-tray`, `/movements-history` y `/transactions/summary` — los agregadores que
    compara el script de snapshot D-12, así que cualquier cambio de resultado acá es un bug.
    Si algún subquery o `sql` crudo arma su propio predicado, el filtro se escribe adentro
    como `tenant_id = ${ctx.tenantId}`.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit && pnpm exec vitest run test/finance/pending-tray.test.ts --hookTimeout=250000</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '1261,1623p' src/modules/finance/transaction-service.ts | grep -c "tenantWhere("` devuelve 12 o más
    - `sed -n '1912,2068p' src/modules/finance/transaction-service.ts | grep -c "tenantWhere("` devuelve 4 o más
    - `test/finance/pending-tray.test.ts` pasa entero sin cambios en sus `expect(...)`
    - `pnpm exec tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>Bandeja, historial de movimientos y resumen quedan aislados sin cambiar un número.</done>
</task>

<task type="auto">
  <name>Task 3: historial financiero, conceptos adeudados, export y cierre del archivo</name>
  <files>el-templo-api/src/modules/finance/transaction-service.ts</files>
  <read_first>
    - el-templo-api/src/modules/finance/transaction-service.ts L1681-1912 (getFinancialHistory: 6 a financial_transactions, 8 a transaction_links, más subscription_plans y subscriptions; getOutstandingConcepts L1789: 10 a balances más subscription_plans/subscriptions)
    - el-templo-api/src/modules/finance/transaction-service.ts L2068-2172 (exportRowsForExcel: 20 a financial_transactions, 6 a transaction_links, 5 a users, 3 a branches)
    - el-templo-api/tenant-lint-allowlist.json (las 11 entradas de transaction-service.ts: balances, cash_registers, cost_centers, financial_transactions, transaction_links + branches, program_enrollments, subscription_plans, subscriptions, user_status_history, users)
  </read_first>
  <action>
    `tenantWhere` primer término en cada query de los tres métodos, incluidas
    `schema.balances`, `schema.subscriptionPlans`, `schema.subscriptions`, `schema.users` y
    `schema.branches`.
    Después, barrido final del archivo: `grep -n "\.from(schema\." src/modules/finance/transaction-service.ts`
    y confirmar que cada `.from(...)` tiene un `tenantWhere` asociado en su `where`. Si
    aparece un acceso legítimamente global, escribir la exención `tenant-safe: <motivo>` en
    comentario de BLOQUE aparte anclado al sitio (nunca sumar una entrada a la allowlist).
    Cerrar generando `/tmp/allowlist-172-12.json` = allowlist sin las 11 entradas de
    `src/modules/finance/transaction-service.ts` y corriendo el lint contra esa copia:
    verde ahí significa que el archivo más grande del módulo quedó limpio.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit && pnpm exec tsx src/db/scripts/lint-tenant.ts --allowlist=/tmp/allowlist-172-12.json && pnpm exec vitest run test/finance/summary-by-kind.test.ts --hookTimeout=250000</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/allowlist-172-12.json` tiene exactamente 11 entradas menos que `tenant-lint-allowlist.json` y el lint contra esa copia sale con código 0
    - `grep -c "tenantWhere(" src/modules/finance/transaction-service.ts` devuelve 45 o más
    - `test/finance/summary-by-kind.test.ts` pasa entero sin cambios en sus `expect(...)`
    - `git -C /home/franco/projects/et-172 status --porcelain el-templo-api/tenant-lint-allowlist.json` no devuelve nada
    - `pnpm exec tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>`transaction-service.ts` queda íntegramente migrado y probado como listo para salir de la allowlist.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                               | Description                                                       |
| ------------------------------------------------------ | ----------------------------------------------------------------- |
| filtros de listado del cliente → queries de agregación | fechas, sedes, tipos y estados llegan del cliente; el gimnasio no |
| helper que devuelve SQL[] → queries compuestas         | un fragmento sin filtro contamina todas las queries que lo usan   |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                                                 | Disposition | Mitigation Plan                                                                                                         |
| ----------- | ---------------------- | ------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------- |
| T-172-12-01 | Information Disclosure | list / listPendingTray / listMovEgresos / getSummary / exportRowsForExcel | mitigate    | `tenantWhere` primer término por cada tabla del statement; listas cross-tenant devuelven vacío (contrato D-09)          |
| T-172-12-02 | Information Disclosure | buildListConditions (SQL[] compartido)                                    | mitigate    | el filtro sale DENTRO del array devuelto, no en cada llamador: imposible olvidarlo en una query nueva que use el helper |
| T-172-12-03 | Information Disclosure | exports a Excel                                                           | mitigate    | mismo filtro que el listado en pantalla; el export es el vector más silencioso de fuga masiva                           |
| T-172-SC    | Tampering              | instalación de paquetes                                                   | accept      | este plan no instala nada                                                                                               |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit` verde.
- `pending-tray` y `summary-by-kind` verdes sin tocar expectativas.
- Lint verde contra la allowlist sin las 11 entradas del archivo.
</verification>

<success_criteria>
El archivo más grande y más caliente del módulo queda sin una sola query sin gimnasio, y
los agregadores que mira el staff siguen dando los mismos números.
</success_criteria>

<output>
Create `.planning/phases/172-adopci-n-1-piloto-finance/172-12-SUMMARY.md` when done
</output>
