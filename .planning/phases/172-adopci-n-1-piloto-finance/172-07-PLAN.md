---
phase: 172-adopci-n-1-piloto-finance
plan: 07
type: execute
wave: 3
depends_on: ["172-01", "172-04"]
files_modified:
  - el-templo-api/src/modules/subscriptions/service.ts
  - el-templo-api/src/modules/subscriptions/routes.ts
  - el-templo-api/src/modules/auth/routes.ts
  - el-templo-api/src/modules/members/routes.ts
  - el-templo-api/src/modules/finance/coach-load-routes.ts
  - el-templo-api/src/jobs/auto-resume-pauses.ts
  - el-templo-api/src/jobs/mark-no-shows.ts
  - el-templo-api/test/subscriptions/charge-on-assign.test.ts
  - el-templo-api/test/subscriptions/impute-advance-on-assign.test.ts
  - el-templo-api/test/subscriptions/lifecycle.test.ts
  - el-templo-api/test/subscriptions/change-plan.test.ts
  - el-templo-api/test/subscriptions/renewal.test.ts
  - el-templo-api/test/subscriptions/user-status-history.test.ts
  - el-templo-api/test/subscriptions-conversion-hook.test.ts
  - el-templo-api/test/users/user-status-transitions.test.ts
autonomous: true
requirements: [ADO-01]
must_haves:
  truths:
    - "Los métodos de suscripciones que escriben plata reciben TenantContext desde el borde (request, job o fila ya scopeada) (D-04)"
    - "El cargo de asignación y el saldo que crea se estampan con el tenant del servidor, nunca del payload"
    - "El autorregistro público con promo resuelve el gimnasio desde la sede elegida, no de un default"
  artifacts:
    - path: "el-templo-api/src/modules/subscriptions/service.ts"
      provides: "recordAssignmentCharge/assignPlan/changePlan*/renewSubscription/cancelSubscription con ctx primero y sus 2 queries finance scopeadas"
      contains: "tenantValues(ctx"
  key_links:
    - from: "el-templo-api/src/modules/auth/routes.ts"
      to: "subscriptionService.assignPlan"
      via: "ctx derivado del tenant_id de la sede elegida (ruta pública, sin request.scope)"
      pattern: "tenantId: .*branch"
---

<objective>
Dar `TenantContext` a los métodos de `subscriptions/service.ts` que escriben o leen las
tablas strict de finance, y migrar sus 2 accesos directos (INSERT en `balances` L599,
SELECT sobre `financial_transactions` L1754). Es el plan que rompe el ciclo
transaction-service ↔ subscriptions: acá el ctx entra por el borde, sin tocar todavía
ninguna firma de `transaction-service`.

Purpose: sin esto, cuando el plan 172-08 le agregue `ctx` a `transactionService.create`
no habría de dónde sacarlo en `subscriptions/service.ts`.

**Fuera de este plan a propósito:** `_cancelSubscription` (L2785) y sus queries de
L2844/L2936 NO se tocan — su firma la cambia el plan 172-08 junto con el tipo
`SubscriptionCanceller` de `transaction-service.ts:93`, que es quien la invoca.
Output: suscripciones con ctx en todos los caminos de plata, tsc verde.

**Nota de secuencia:** este plan también toca `members/routes.ts`, que el plan 172-04 ya
reescribió (por eso 172-04 está en `depends_on`): se parte de la versión de 172-04, no de
la de master. Y el trabajo de call sites va partido en dos tasks (producción / tests) para
que cada commit sea atómico y ningún task supere ~8 archivos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/172-adopci-n-1-piloto-finance/172-CONTEXT.md
@.planning/phases/172-adopci-n-1-piloto-finance/172-PATTERNS.md

Working dir: `/home/franco/projects/et-172`. La allowlist no se toca.
Este es un plan de PLOMERÍA + 2 queries: no se cambia lógica de negocio ni se ajusta
ninguna expectativa de test.
</context>

<tasks>

<task type="auto">
  <name>Task 1: ctx en recordAssignmentCharge y assignPlan + sus 2 queries finance</name>
  <files>el-templo-api/src/modules/subscriptions/service.ts</files>
  <read_first>
    - el-templo-api/src/modules/shared/tenant.ts (firmas lockeadas; `TenantContext` es estructuralmente compatible con un `CountryScope` narrowed, L99-106)
    - el-templo-api/src/modules/subscriptions/service.ts L487-640 (recordAssignmentCharge y el INSERT en schema.balances de L599)
    - el-templo-api/src/modules/subscriptions/service.ts L1224-1830 (assignPlan, el SELECT sobre schema.financialTransactions de L1754 y los call sites de recordAssignmentCharge en L1793 y L1817)
    - el-templo-api/src/modules/tv/pairing.ts L173-211 (docblock de por qué el ctx va PRIMERO en la firma + tenantValues en UPDATE)
  </read_first>
  <action>
    `ctx: TenantContext` como PRIMER parámetro de `recordAssignmentCharge` (antes del
    `tx`) y de `assignPlan`, y de los métodos internos que las llaman y que todavía no lo
    tengan (`changePlanNow` L3159, `changePlanAfterCurrent` L3621, `changePlan` L3141,
    `renewSubscription` L3969 — los call sites de recordAssignmentCharge están en L1793,
    L1817, L3516, L3913 y L4454). El ctx va antes del `tx` a propósito: un call site viejo
    con argumentos corridos no compila (regla 169-06).
    Queries: el INSERT de L599 pasa por `tenantValues(ctx, { ... })` (tenant DESPUÉS del
    spread, sin `as const`); el SELECT de L1754 lleva
    `tenantWhere(schema.financialTransactions, ctx)` como PRIMER término del `and(...)`.
    NO tocar las llamadas a `this.transactionService.*` ni a `this.balanceService.*`: esas
    firmas cambian en los planes 172-08 y 172-09.
    NO tocar `_cancelSubscription`.
    Import por path directo desde `../shared/tenant`.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tenantValues(ctx" src/modules/subscriptions/service.ts` devuelve 1 o más
    - `grep -c "tenantWhere(schema.financialTransactions" src/modules/subscriptions/service.ts` devuelve 1 o más
    - `sed -n '2785,2800p' src/modules/subscriptions/service.ts` muestra la firma de `_cancelSubscription` SIN `ctx` (queda para el plan 172-08)
    - `grep -nE "tenantId!|\?\? 1" src/modules/subscriptions/service.ts` no devuelve líneas
    - `pnpm exec tsc --noEmit` sale con código 0 (después de actualizar los call sites de los Tasks 2 y 3)
    - El SUMMARY registra cuántos métodos recibieron `ctx` y cuántas queries finance se migraron en `subscriptions/service.ts`
  </acceptance_criteria>
  <done>Los caminos de cargo y asignación de plan tienen gimnasio explícito y estampan el tenant en el saldo que crean.</done>
</task>

<task type="auto">
  <name>Task 2: call sites de producción — rutas de admin y jobs</name>
  <files>el-templo-api/src/modules/subscriptions/routes.ts, el-templo-api/src/modules/members/routes.ts, el-templo-api/src/modules/finance/coach-load-routes.ts, el-templo-api/src/jobs/auto-resume-pauses.ts, el-templo-api/src/jobs/mark-no-shows.ts</files>
  <read_first>
    - el-templo-api/src/modules/subscriptions/routes.ts L311 (assignPlan) y el resto de call sites que marque tsc
    - el-templo-api/src/modules/members/routes.ts L677 (assignPlan) — OJO: este archivo también lo toca el plan 172-04 (getOutstandingBalances); 172-04 ya está mergeado cuando arranca este plan, así que se parte de su versión, no de la de master
    - el-templo-api/src/modules/finance/coach-load-routes.ts L790 (assignPlan) y L984-1003 (variante `const ctx = assertTenant(...)` cuando el ctx se usa más de una vez)
    - el-templo-api/src/jobs/auto-resume-pauses.ts L60-150 (el cuerpo del job YA recibe `ctx: TenantContext` de forEachActiveTenant y su docblock dice que las firmas lo van a recibir en 172-175)
    - el-templo-api/src/jobs/mark-no-shows.ts L110-210 (mismo patrón, más `buildSubscriptionService(db)` L202)
  </read_first>
  <action>
    Actualizar los call sites de producción que marque `tsc`, con el ctx de su fuente
    legítima (`shared/tenant.ts` L23-38 las lista y no hay una quinta):
    - Rutas de admin (`subscriptions/routes.ts`, `members/routes.ts`,
      `finance/coach-load-routes.ts`): `assertTenant(request.scope, "<módulo>.<acción>")`.
    - Jobs (`auto-resume-pauses.ts`, `mark-no-shows.ts`): el `ctx` que ya provee
      `forEachActiveTenant` — pasarlo tal cual, sin reconstruirlo.
    Prohibidos `request.scope.tenantId!` y `?? 1`. Sin try/catch nuevos: `assertTenant`
    lanza `AppError(403, TENANT_UNRESOLVED)` y `handleServiceError` ya lo mapea.
    `tsc` va a seguir marcando los call sites de test hasta que termine el Task 3: eso es
    esperado, este task cierra solo la superficie de producción.
    Registrar en el SUMMARY el conteo de call sites actualizados POR ARCHIVO.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit 2>&1 | grep -v "^test/" | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec tsc --noEmit 2>&1 | grep -v "^test/" | grep -c "error TS"` devuelve 0 (cero errores fuera de `test/`)
    - `grep -nE "tenantId!|\?\? 1" src/modules/subscriptions/routes.ts src/modules/members/routes.ts src/modules/finance/coach-load-routes.ts src/jobs/auto-resume-pauses.ts src/jobs/mark-no-shows.ts` no devuelve líneas
    - `grep -c "assertTenant(request.scope" src/modules/subscriptions/routes.ts src/modules/members/routes.ts src/modules/finance/coach-load-routes.ts` devuelve 1 o más en cada archivo
    - Los jobs NO construyen un ctx propio: `grep -nE "assertTenant|tenantId:" src/jobs/auto-resume-pauses.ts src/jobs/mark-no-shows.ts` no muestra construcción de ctx nueva dentro del cuerpo del job
    - El SUMMARY lista, archivo por archivo, cuántos call sites se actualizaron (5 filas, una por archivo)
  </acceptance_criteria>
  <done>Rutas de admin y jobs entregan gimnasio explícito; el código de producción de suscripciones compila.</done>
</task>

<task type="auto">
  <name>Task 3: call sites de test de suscripciones y estados de usuario</name>
  <files>el-templo-api/test/subscriptions/charge-on-assign.test.ts, el-templo-api/test/subscriptions/impute-advance-on-assign.test.ts, el-templo-api/test/subscriptions/lifecycle.test.ts, el-templo-api/test/subscriptions/change-plan.test.ts, el-templo-api/test/subscriptions/renewal.test.ts, el-templo-api/test/subscriptions/user-status-history.test.ts, el-templo-api/test/subscriptions-conversion-hook.test.ts, el-templo-api/test/users/user-status-transitions.test.ts</files>
  <read_first>
    - el-templo-api/test/fixtures/second-tenant.ts L119 (TENANT_TEMPLO = 1)
    - el-templo-api/test/subscriptions/charge-on-assign.test.ts (el archivo con más llamadas directas al service del grupo)
    - la salida de `pnpm exec tsc --noEmit` al terminar el Task 2: es el inventario exacto de call sites de test pendientes
  </read_first>
  <action>
    En cada llamada directa a los métodos migrados en el Task 1
    (`assignPlan`, `changePlan*`, `renewSubscription`, `recordAssignmentCharge`), pasar
    `{ tenantId: TENANT_TEMPLO }` como PRIMER argumento, importando `TENANT_TEMPLO` desde
    `../fixtures/second-tenant` (o la ruta relativa que corresponda según la profundidad
    del archivo). Prohibido hardcodear `1` a mano y prohibido hardcodear ids de `users`:
    seguir resolviendo `admin@test.com` como ya hacen esos archivos.
    No cambiar ninguna expectativa de test: si una se pone roja, es un bug del cambio del
    Task 1, no de la expectativa.
    Este task NO agrega `tenantWhere`/`tenantValues` a las queries directas de esos tests
    — eso es del plan 172-15. Acá solo viajan argumentos de ctx.
    Registrar en el SUMMARY el conteo de call sites actualizados POR ARCHIVO.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit && pnpm exec vitest run test/subscriptions/charge-on-assign.test.ts --hookTimeout=250000</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec tsc --noEmit` sale con código 0
    - `test/subscriptions/charge-on-assign.test.ts` pasa entero sin cambios en sus `expect(...)`
    - `grep -c "TENANT_TEMPLO" test/subscriptions/charge-on-assign.test.ts` devuelve 1 o más
    - `git -C /home/franco/projects/et-172 diff --stat -- el-templo-api/test` muestra solo líneas de argumentos de ctx e imports (ninguna expectativa modificada)
    - `git diff -- el-templo-api/test | grep -c "^[-+].*expect("` devuelve 0
    - El SUMMARY lista, archivo por archivo, cuántos call sites se actualizaron (8 filas, una por archivo)
  </acceptance_criteria>
  <done>Los tests de suscripciones entregan gimnasio explícito; tsc y el test de cargo al asignar en verde.</done>
</task>

<task type="auto">
  <name>Task 4: autorregistro público con promo — ctx derivado de la sede</name>
  <files>el-templo-api/src/modules/auth/routes.ts</files>
  <read_first>
    - el-templo-api/src/modules/auth/routes.ts L140-265 (registro público: resolución de `branchId` L140-165, alta del user L177, bloque de promo L203-260 con el `subscriptionService.assignPlan` de L250)
    - el-templo-api/src/modules/shared/tenant.ts L23-38 (las 4 fuentes legítimas de un TenantContext) y L149-160 (tenantWhere)
    - el-templo-api/src/modules/tv/pairing.ts L285-308 (analog de ctx construido desde una fila ya leída del server, no de un request)
  </read_first>
  <action>
    Esta ruta es PÚBLICA: no hay `request.user` ni `request.scope`, así que `assertTenant`
    no aplica. El gimnasio se deriva del servidor leyendo `tenantId` de la fila de
    `branches` que la propia ruta ya resuelve: agregar `tenantId: branches.tenantId` a la
    proyección del SELECT de la sede por defecto (`code = "ONLINE"`, L152-156) y, para el
    camino donde la sede llega por `requestedBranchId`, hacer el SELECT de esa sede
    trayendo `id` y `tenantId` (validando que exista) en vez de confiar en el número del
    body. Con eso construir `const ctx: TenantContext = { tenantId: branch.tenantId };` y
    pasarlo como primer argumento de `subscriptionService.assignPlan`.
    Si la sede no resuelve o su `tenantId` no es un número, responder el mismo 500/404 que
    ya responde hoy la rama de sede no configurada: fail-closed, NUNCA `?? 1`.
    Dejar un comentario corto explicando por qué acá el ctx sale de la fila de sede y no
    de `request.scope` (ruta pública) — es el precedente que van a copiar las fases
    siguientes para los otros caminos sin JWT.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "tenantId!|\?\? 1" src/modules/auth/routes.ts` no devuelve líneas
    - `grep -c "tenantId: branch" src/modules/auth/routes.ts` devuelve 1 o más
    - El SELECT de la sede en el registro público proyecta `tenantId` (visible en `sed -n '145,170p' src/modules/auth/routes.ts`)
    - `pnpm exec tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>El autorregistro con promo asigna el plan en el gimnasio de la sede elegida, resuelto server-side.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                               | Description                                                       |
| ------------------------------------------------------ | ----------------------------------------------------------------- |
| público anónimo → POST /api/auth/register              | sin JWT ni scope; `branchId` y `promoCode` vienen del cliente     |
| staff del gimnasio A → assignPlan / changePlan / renew | escriben cargos y saldos: el peor caso de tampering del milestone |
| cron sin request → subscriptions                       | el tenant solo puede venir de `forEachActiveTenant`               |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                                           | Disposition | Mitigation Plan                                                                                                                                |
| ----------- | ---------------------- | ------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| T-172-07-01 | Elevation of Privilege | registro público con promo (auth/routes L250)                       | mitigate    | ctx derivado del `tenant_id` de la fila de `branches` leída por el server; la sede pedida se valida contra la base antes de usarse; sin `?? 1` |
| T-172-07-02 | Tampering              | INSERT en balances (recordAssignmentCharge L599)                    | mitigate    | `tenantValues(ctx, {...})`: el tenant va después del spread y pisa cualquier valor del payload                                                 |
| T-172-07-03 | Information Disclosure | SELECT de anticipos sobre financial_transactions (assignPlan L1754) | mitigate    | `tenantWhere` primer término: no se puede imputar un anticipo de otro gimnasio                                                                 |
| T-172-07-04 | Spoofing               | jobs que asignan/expiran suscripciones                              | mitigate    | el ctx es el de `forEachActiveTenant`, jamás reconstruido a mano dentro del job                                                                |
| T-172-SC    | Tampering              | instalación de paquetes                                             | accept      | este plan no instala nada                                                                                                                      |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit` verde con todos los call sites actualizados.
- `test/subscriptions/charge-on-assign.test.ts` verde sin tocar expectativas.
- `_cancelSubscription` intacto (lo migra el plan 172-08).
- El SUMMARY documenta el conteo de call sites migrados por archivo (13 filas: 5 de producción + 8 de test).
</verification>

<success_criteria>
Toda escritura de plata que arranca en suscripciones tiene un gimnasio explícito de
origen server-side, y el ciclo transaction-service ↔ subscriptions queda roto para que
el plan siguiente pueda avanzar sin cascada.
</success_criteria>

<output>
Create `.planning/phases/172-adopci-n-1-piloto-finance/172-07-SUMMARY.md` when done
</output>
