---
phase: 172-adopci-n-1-piloto-finance
plan: 02
type: execute
wave: 2
depends_on: ["172-01"]
files_modified:
  - el-templo-api/src/modules/analytics/service.ts
  - el-templo-api/src/modules/analytics/advanced-finance-service.ts
  - el-templo-api/src/modules/analytics/ltv-service.ts
  - el-templo-api/src/modules/analytics/ticket-service.ts
  - el-templo-api/src/modules/analytics/routes.ts
autonomous: true
requirements: [ADO-01]
must_haves:
  truths:
    - "Toda query de analytics sobre financial_transactions, transaction_links y balances filtra por tenant_id (D-01)"
    - "El tenant de analytics sale de assertTenant(request.scope, ...) en el handler, nunca del body ni de un filtro"
  artifacts:
    - path: "el-templo-api/src/modules/analytics/service.ts"
      provides: "getKpis/getFinancialAnalytics con TenantContext y tenantWhere en revenue y outstanding"
      contains: "tenantWhere(schema.financialTransactions"
    - path: "el-templo-api/src/modules/analytics/advanced-finance-service.ts"
      provides: "cashTrend scopeado"
      contains: "tenantWhere(schema.financialTransactions"
    - path: "el-templo-api/src/modules/analytics/ltv-service.ts"
      provides: "realPaymentsByMember scopeado"
      contains: "tenantWhere(schema.financialTransactions"
    - path: "el-templo-api/src/modules/analytics/ticket-service.ts"
      provides: "linkedCharges y universeCountByCurrency scopeados"
      contains: "tenantWhere(schema.transactionLinks"
  key_links:
    - from: "el-templo-api/src/modules/analytics/routes.ts"
      to: "assertTenant"
      via: "call site de cada método de analytics que toca tablas finance"
      pattern: "assertTenant\\(request\\.scope"
---

<objective>
Cirugía mínima (D-01 + D-07) sobre los 4 services de analytics: SOLO las queries que
tocan las 6 tablas strict de finance reciben `TenantContext` y helpers. El resto de
analytics se migra en su propia fase (173-175).

Purpose: el sentinel hace throw sobre `financial_transactions`, `transaction_links` y
`balances` venga la query de donde venga. Analytics es el módulo ajeno con más accesos
(6 de las 33 entradas de allowlist sobre tablas strict).
Output: 4 services con las queries finance scopeadas y 5 call sites de ruta con
`assertTenant`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/172-adopci-n-1-piloto-finance/172-CONTEXT.md
@.planning/phases/172-adopci-n-1-piloto-finance/172-PATTERNS.md

Working dir: `/home/franco/projects/et-172`; las rutas de código son relativas a esa raíz.
`tenant-lint-allowlist.json` NO se edita en este plan — su dueño único es el plan 172-21.
La verificación corre el lint contra una copia filtrada en `/tmp`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: analytics/service.ts y advanced-finance-service.ts scopeados</name>
  <files>el-templo-api/src/modules/analytics/service.ts, el-templo-api/src/modules/analytics/advanced-finance-service.ts, el-templo-api/src/modules/analytics/routes.ts</files>
  <read_first>
    - el-templo-api/src/modules/shared/tenant.ts (firmas lockeadas de tenantWhere/tenantValues/assertTenant y las 4 fuentes legítimas de un TenantContext)
    - el-templo-api/src/modules/analytics/service.ts (privados getRevenueTrend L1072, getRevenueByMethod L1143, getRevenueByBranch L1214, getOutstandingByCurrency L1306, sumRevenue L1352; públicos getKpis L58 y getFinancialAnalytics L152 que los llaman en L165-168 y L258-259)
    - el-templo-api/src/modules/analytics/advanced-finance-service.ts (getAdvancedFinance L120, cashTrend L179)
    - el-templo-api/src/modules/analytics/routes.ts (handlers /kpis L147, /financial L230, /advanced-finance L384 — ahí vive request.scope)
    - el-templo-api/src/modules/finance/cash-register-service.ts L782-830 (analog canónico: ctx PRIMER parámetro, tenantWhere primer término del and)
  </read_first>
  <action>
    En `analytics/service.ts` agregar `ctx: TenantContext` como PRIMER parámetro de
    `getKpis`, `getFinancialAnalytics`, `getRevenueTrend`, `getRevenueByMethod`,
    `getRevenueByBranch`, `getOutstandingByCurrency` y `sumRevenue`, propagándolo en las
    llamadas internas. En las queries de esos métodos agregar
    `tenantWhere(schema.financialTransactions, ctx)` — y `tenantWhere(schema.balances, ctx)`
    en `getOutstandingByCurrency` — como PRIMER término del `and(...)` o primer elemento
    del array de condiciones. Los joins contra `users` y `branches` NO se tocan (D-07).
    Import con `import { tenantWhere, type TenantContext } from "../shared/tenant";` —
    path directo, nunca por el barrel `shared/index.ts`.
    Igual en `advanced-finance-service.ts`: ctx primero en `getAdvancedFinance` y
    `cashTrend`, `tenantWhere(schema.financialTransactions, ctx)` en la query de L217.
    En `analytics/routes.ts` los call sites `analyticsService.getKpis`,
    `analyticsService.getFinancialAnalytics` y `advancedFinanceService.getAdvancedFinance`
    pasan `assertTenant(request.scope, "analytics.kpis")`,
    `assertTenant(request.scope, "analytics.financial")` y
    `assertTenant(request.scope, "analytics.advanced-finance")` como primer argumento.
    Prohibidos `request.scope.tenantId!` y `?? 1` (regla 169-01): `assertTenant` es el
    único puente entre `number | null` y `number`.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tenantWhere(schema.financialTransactions" src/modules/analytics/service.ts` devuelve 4 o más
    - `grep -c "tenantWhere(schema.balances" src/modules/analytics/service.ts` devuelve 1 o más
    - `grep -c "tenantWhere(schema.financialTransactions" src/modules/analytics/advanced-finance-service.ts` devuelve 1 o más
    - `grep -nE "tenantId!|\?\? 1" src/modules/analytics/service.ts src/modules/analytics/advanced-finance-service.ts src/modules/analytics/routes.ts` no devuelve líneas
    - `grep -c "assertTenant(request.scope" src/modules/analytics/routes.ts` devuelve 3 o más
    - `pnpm exec tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>Revenue, outstanding y cash trend de analytics no pueden leer filas de otro gimnasio.</done>
</task>

<task type="auto">
  <name>Task 2: ltv-service.ts y ticket-service.ts scopeados, y lint verde sin sus entradas de allowlist</name>
  <files>el-templo-api/src/modules/analytics/ltv-service.ts, el-templo-api/src/modules/analytics/ticket-service.ts, el-templo-api/src/modules/analytics/routes.ts</files>
  <read_first>
    - el-templo-api/src/modules/analytics/ltv-service.ts (getLtv L165, realPaymentsByMember L279 con call sites L171 y L428)
    - el-templo-api/src/modules/analytics/ticket-service.ts (getTicket L183, linkedCharges L429 con el join financial_transactions×transaction_links en L476-482, universeCountByCurrency L522)
    - el-templo-api/src/modules/analytics/routes.ts (handlers /ltv L779 y /ticket L421)
    - el-templo-api/tenant-lint-allowlist.json (header con el contrato del ratchet y el formato de entrada file+table)
  </read_first>
  <action>
    `ltv-service.ts`: ctx primero en `getLtv` y `realPaymentsByMember`;
    `tenantWhere(schema.financialTransactions, ctx)` en la query de L320.
    `ticket-service.ts`: ctx primero en `getTicket`, `linkedCharges` y
    `universeCountByCurrency`; `tenantWhere(schema.financialTransactions, ctx)` en ambas
    queries y ADEMÁS `tenantWhere(schema.transactionLinks, ctx)` en el join de
    `linkedCharges` — el sentinel exige el literal `tenant_id` por cada tabla strict que
    aparece en el statement, y `transaction_links` es una de las 6.
    Call sites en `analytics/routes.ts`: `assertTenant(request.scope, "analytics.ltv")` y
    `assertTenant(request.scope, "analytics.ticket")`.
    Después escribir con node un `/tmp/allowlist-172-02.json` = copia de
    `tenant-lint-allowlist.json` SIN las entradas cuyo `file` esté bajo
    `src/modules/analytics/` y cuya `table` sea `financial_transactions`,
    `transaction_links` o `balances` (deben ser 6), y correr el lint contra esa copia.
    Verde ahí significa que las 6 entradas quedaron muertas y el plan 172-21 las puede
    borrar. NO editar `tenant-lint-allowlist.json`.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit && pnpm exec tsx src/db/scripts/lint-tenant.ts --allowlist=/tmp/allowlist-172-02.json</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/allowlist-172-02.json` existe y tiene exactamente 6 entradas menos que `tenant-lint-allowlist.json`
    - El lint contra `/tmp/allowlist-172-02.json` sale con código 0 (cero violaciones sin allowlist)
    - `git -C /home/franco/projects/et-172 status --porcelain el-templo-api/tenant-lint-allowlist.json` no devuelve nada
    - `grep -c "tenantWhere(schema.financialTransactions" src/modules/analytics/ltv-service.ts` devuelve 1 o más
    - `grep -c "tenantWhere(schema.transactionLinks" src/modules/analytics/ticket-service.ts` devuelve 1 o más
    - `grep -c "assertTenant(request.scope" src/modules/analytics/routes.ts` devuelve 5 o más
    - `pnpm exec tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>Las 6 entradas de allowlist de analytics sobre tablas strict quedan muertas y el lint lo prueba.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                       | Description                                                                        |
| ---------------------------------------------- | ---------------------------------------------------------------------------------- |
| admin del gimnasio A → /api/admin/analytics/\* | el filtro de branch/country es del cliente; el gimnasio tiene que salir del server |
| service de analytics → tablas finance          | agregados de plata: una query sin scope suma ingresos de otro gimnasio             |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                                          | Disposition | Mitigation Plan                                                                                                                                  |
| ----------- | ---------------------- | ------------------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-172-02-01 | Information Disclosure | getRevenueTrend / cashTrend / realPaymentsByMember / linkedCharges | mitigate    | `tenantWhere(...)` como primer término del `and(...)` en cada query sobre las 3 tablas strict; verificado por grep + lint con allowlist filtrada |
| T-172-02-02 | Elevation of Privilege | resolución del tenant en el handler                                | mitigate    | `assertTenant(request.scope, "<dónde>")` es el único puente; prohibidos `tenantId!` y `?? 1`, verificado por grep                                |
| T-172-02-03 | Information Disclosure | join financial_transactions × transaction_links en linkedCharges   | mitigate    | `tenantWhere` en AMBAS tablas: el sentinel exige el literal por tabla strict presente en el statement                                            |
| T-172-SC    | Tampering              | instalación de paquetes                                            | accept      | este plan no instala nada; no hay superficie de supply chain                                                                                     |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit` verde.
- Lint de tenancy verde contra la allowlist filtrada (6 entradas menos).
- Ningún `tenantId!` ni `?? 1` en los archivos tocados.
</verification>

<success_criteria>
Ningún agregado financiero de analytics puede incluir filas de otro gimnasio, y sus 6
entradas de allowlist quedan listas para borrarse en el plan del switch.
</success_criteria>

<output>
Create `.planning/phases/172-adopci-n-1-piloto-finance/172-02-SUMMARY.md` when done
</output>
