---
phase: 172-adopci-n-1-piloto-finance
plan: 13
type: execute
wave: 8
depends_on: ["172-12", "172-11"]
files_modified:
  - el-templo-api/test/helpers.ts
  - el-templo-api/test/setup.ts
  - el-templo-api/test/finance/cash-register-service.test.ts
  - el-templo-api/test/finance/cost-centers.test.ts
  - el-templo-api/test/finance/cost-centers-abm.test.ts
  - el-templo-api/test/finance/bank-accounts.test.ts
  - el-templo-api/test/finance/cash-balances.test.ts
  - el-templo-api/test/finance/validate-caja.test.ts
autonomous: true
requirements: [ADO-01]
must_haves:
  truths:
    - "La limpieza global de la base de test lleva la exención tenant-safe escrita en el propio SQL"
    - "Los helpers de test que siembran cajas, centros de costo y transacciones estampan tenant_id explícito"
    - "Con finance en modo strict, los tests de cajas y centros de costo pasan sin tocar sus expectativas"
  artifacts:
    - path: "el-templo-api/test/helpers.ts"
      provides: "cleanAllTestData con exención tenant-safe y seeds con tenant explícito"
      contains: "tenant-safe:"
  key_links:
    - from: "el-templo-api/test/helpers.ts"
      to: "sentinel de pool"
      via: "comentario de bloque tenant-safe embebido en el SQL del DELETE"
      pattern: "tenant-safe:"
---

<objective>
Preparar la infraestructura de test y los archivos de cajas/centros de costo para el
throw del sentinel: toda query de test sobre las 6 tablas strict tiene que llevar
`tenant_id` en la zona de predicado (o la exención escrita en el SQL).

Purpose: el sentinel intercepta el pool mysql2 por debajo de Drizzle, así que también ve
las queries que hacen los tests para sembrar y para afirmar. `cleanAllTestData`
(`test/helpers.ts` L262) borra ~90 tablas sin filtro: cuando `finance` entre a strict,
ese DELETE hace throw y se cae TODA la suite. Este es el trabajo que evita que el plan
del switch (172-21) descubra 40 archivos rojos de golpe.
Output: helpers + 6 archivos de test de cajas listos para strict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/172-adopci-n-1-piloto-finance/172-CONTEXT.md
@.planning/phases/172-adopci-n-1-piloto-finance/172-PATTERNS.md

Working dir: `/home/franco/projects/et-172`.
Las 6 tablas strict: `financial_transactions`, `transaction_links`, `balances`,
`cash_registers`, `cost_centers`, `debt_management`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: helpers.ts y setup.ts listos para el throw</name>
  <files>el-templo-api/test/helpers.ts, el-templo-api/test/setup.ts</files>
  <read_first>
    - el-templo-api/src/db/sentinel/analyze.ts L20-40 (el canal de exención del SENTINEL es un comentario de bloque `tenant-safe: <motivo>` embebido EN EL SQL, con motivo no vacío; el canal del lint es otro y no aplica a `test/`)
    - el-templo-api/test/helpers.ts L157-280 (TABLES_TO_CLEAN, el `DELETE FROM \`<tabla>\`` de L262 y el delete de users de L271) y L321-350 (ensureEfectivoCaja, que ya usa tenantValues y cuyo 4º argumento tiene default 1)
    - el-templo-api/test/setup.ts (provisioning y cualquier query sobre tablas finance)
    - el-templo-api/test/fixtures/second-tenant.ts L360-401 (cómo la fase 171 ya escribió sus DELETEs crudos anticipando este throw)
  </read_first>
  <action>
    En `cleanAllTestData` el DELETE es global A PROPÓSITO (limpia todos los gimnasios),
    así que la salida correcta NO es filtrar sino declarar la exención en el SQL: emitir
    el statement con un comentario de bloque `tenant-safe: limpieza global de la base de
    test (todos los gimnasios)` embebido en el propio texto SQL, entre el verbo y el
    `FROM`. El motivo NO puede quedar vacío y el comentario NO puede quedar anidado dentro
    de otro comentario.
    Revisar el resto de `test/helpers.ts` y `test/setup.ts` con
    `grep -nE "financialTransactions|transactionLinks|balances|cashRegisters|costCenters|debtManagement" test/helpers.ts test/setup.ts`:
    todo SELECT/INSERT/UPDATE sobre esas 6 tablas tiene que llevar tenant explícito
    (`tenantWhere(schema.X, { tenantId: TENANT_TEMPLO })` en lecturas,
    `tenantValues({ tenantId: TENANT_TEMPLO }, {...})` en escrituras, o `tenant_id` en el
    predicado si es `sql` crudo). `ensureEfectivoCaja` ya lo hace: no cambiar su firma ni
    su default, solo documentar en su docblock que el 4º argumento pasa a ser obligatorio
    en la práctica para el gimnasio 2.
    Agregar arriba de `cleanAllTestData` un comentario corto explicando por qué la exención
    es legítima acá y por qué NO se puede resolver sumando entradas a la allowlist.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit && grep -c "tenant-safe:" test/helpers.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tenant-safe:" test/helpers.ts` devuelve 1 o más y el texto incluye un motivo no vacío
    - El comentario `tenant-safe` está dentro del string SQL que se manda al driver (visible en la línea del `DELETE`), no en un comentario TypeScript aparte
    - `grep -nE "\.(from|insert|update)\(\s*(schema\.)?(financialTransactions|transactionLinks|balances|cashRegisters|costCenters|debtManagement)" test/helpers.ts test/setup.ts` no devuelve ninguna línea sin `tenantWhere`/`tenantValues` en las 6 líneas siguientes
    - `pnpm exec tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>La limpieza global y el provisioning sobreviven al throw del sentinel.</done>
</task>

<task type="auto">
  <name>Task 2: 6 archivos de test de cajas y centros de costo, verificados con strict encendido en caliente</name>
  <files>el-templo-api/test/finance/cash-register-service.test.ts, el-templo-api/test/finance/cost-centers.test.ts, el-templo-api/test/finance/cost-centers-abm.test.ts, el-templo-api/test/finance/bank-accounts.test.ts, el-templo-api/test/finance/cash-balances.test.ts, el-templo-api/test/finance/validate-caja.test.ts</files>
  <read_first>
    - el-templo-api/test/finance/cash-register-service.test.ts (el archivo con más accesos directos: ~35 queries sobre cash_registers y financial_transactions)
    - el-templo-api/test/fixtures/second-tenant.ts L119 (TENANT_TEMPLO = 1)
    - el-templo-api/src/db/tenant-tables.ts L465-515 (docblock y forma de TENANT_STRICT_MODULES — acá se enciende el strict EN CALIENTE para verificar, y se revierte)
    - el-templo-api/test/tenancy/con-05-sentinel.test.ts L30-40 (el throw llega envuelto: `DrizzleQueryError` con el `TenantSentinelError` en `cause`)
  </read_first>
  <action>
    En los 6 archivos, agregar el filtro/estampa de gimnasio a cada query directa sobre las
    6 tablas strict: `tenantWhere(schema.X, { tenantId: TENANT_TEMPLO })` sumado al `and(...)`
    de las lecturas y `tenantValues({ tenantId: TENANT_TEMPLO }, { ... })` en las
    escrituras; en `sql` crudo, `AND tenant_id = 1` en el predicado. Importar
    `TENANT_TEMPLO` de `../fixtures/second-tenant`. Prohibido hardcodear ids de `users`:
    seguir resolviendo `admin@test.com` como ya hacen esos archivos.
    NO cambiar ninguna expectativa: si un `expect` se pone rojo, el bug está en la
    migración de código, no en el test.
    Verificación en caliente (patrón de sonda revertida de 168-05/169-04/171): agregar
    TEMPORALMENTE la entrada `finance` con las 6 tablas en `TENANT_STRICT_MODULES`
    (`src/db/tenant-tables.ts`), correr los 6 archivos, y REVERTIR ese cambio antes de
    commitear (`git checkout -- src/db/tenant-tables.ts`). El commit de este plan NO puede
    incluir `src/db/tenant-tables.ts` — el switch es del plan 172-21 (D-03).
    Registrar en el SUMMARY cuántas queries se tocaron por archivo.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit && pnpm exec vitest run test/finance/cash-register-service.test.ts test/finance/cost-centers.test.ts test/finance/cost-centers-abm.test.ts test/finance/bank-accounts.test.ts test/finance/cash-balances.test.ts test/finance/validate-caja.test.ts --hookTimeout=900000</automated>
  </verify>
  <acceptance_criteria>
    - Con la entrada `finance` puesta a mano en `TENANT_STRICT_MODULES`, los 6 archivos pasan enteros (evidencia en el SUMMARY: salida de vitest)
    - `git -C /home/franco/projects/et-172 status --porcelain el-templo-api/src/db/tenant-tables.ts` no devuelve nada al terminar (la sonda fue revertida)
    - `git diff -- el-templo-api/test/finance | grep -c "^[-+].*expect("` devuelve 0
    - `grep -c "TENANT_TEMPLO" test/finance/cash-register-service.test.ts` devuelve 1 o más
    - `pnpm exec tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>Los tests de cajas, cuentas y centros de costo ya sobreviven al modo throw.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                     |
| ---------------------------------- | ------------------------------------------------------------------------------- |
| código de test → pool con sentinel | los tests hablan con el mismo pool que la app: sus queries también son juzgadas |
| exención tenant-safe               | es un escape real: mal usada apaga el tripwire                                  |

## STRIDE Threat Register

| Threat ID   | Category    | Component                                  | Disposition | Mitigation Plan                                                                                                                                                             |
| ----------- | ----------- | ------------------------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-172-13-01 | Tampering   | exención tenant-safe en cleanAllTestData   | mitigate    | única exención del plan, con motivo explícito, sobre un DELETE de limpieza global que solo corre en test; comentario arriba explicando por qué no se resuelve con allowlist |
| T-172-13-02 | Spoofing    | tests que siembran en el tenant equivocado | mitigate    | `TENANT_TEMPLO` importado del fixture en vez de un `1` literal disperso; el 4º argumento de `ensureEfectivoCaja` documentado como obligatorio para el gimnasio 2            |
| T-172-13-03 | Repudiation | verificación en caliente sin dejar rastro  | mitigate    | la sonda de strict se revierte y el criterio de aceptación lo verifica con `git status`                                                                                     |
| T-172-SC    | Tampering   | instalación de paquetes                    | accept      | este plan no instala nada                                                                                                                                                   |

</threat_model>

<verification>
- 6 archivos verdes CON strict encendido a mano.
- Sonda revertida (git status limpio en `src/db/tenant-tables.ts`).
- Cero expectativas de test modificadas.
</verification>

<success_criteria>
La infraestructura de test y el grupo de cajas/centros de costo dejan de ser un
bloqueante para activar el throw.
</success_criteria>

<output>
Create `.planning/phases/172-adopci-n-1-piloto-finance/172-13-SUMMARY.md` when done
</output>
