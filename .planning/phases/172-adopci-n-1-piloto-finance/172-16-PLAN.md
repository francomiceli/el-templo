---
phase: 172-adopci-n-1-piloto-finance
plan: 16
type: execute
wave: 11
depends_on: ["172-15"]
files_modified:
  - el-templo-api/test/analytics/analytics.test.ts
  - el-templo-api/test/analytics/advanced-finance.test.ts
  - el-templo-api/test/analytics/breakdowns-cohorts.test.ts
  - el-templo-api/test/analytics/especial-exclusion.test.ts
  - el-templo-api/test/analytics/ltv.test.ts
  - el-templo-api/test/analytics/ticket.test.ts
  - el-templo-api/test/scheduling/scheduling.test.ts
  - el-templo-api/test/scheduling/155-horarios.test.ts
  - el-templo-api/test/scheduling/schedule-exceptions.test.ts
  - el-templo-api/test/attendance/attendance.test.ts
  - el-templo-api/test/migrations/0109_reconcile_soledad.test.ts
  - el-templo-api/test/migrations/0196-tenant-unique-contracts.test.ts
  - el-templo-api/test/wellhub/webhook-booking.test.ts
  - el-templo-api/test/tenancy/con-01-uniques-cross-tenant.test.ts
  - el-templo-api/test/tenancy/con-03-write-paths-tenant-id.test.ts
autonomous: true
requirements: [ADO-01]
must_haves:
  truths:
    - "Con finance en modo strict, los tests de analytics, scheduling, asistencia, migraciones, Wellhub y tenancy pasan sin tocar expectativas"
    - "Ningún archivo de test fuera de finance queda con una query sin tenant_id sobre las 6 tablas strict"
  artifacts:
    - path: "el-templo-api/test/analytics/analytics.test.ts"
      provides: "seeds de ingresos con tenant explícito"
      contains: "TENANT_TEMPLO"
  key_links:
    - from: "el-templo-api/test"
      to: "sentinel en modo throw"
      via: "tenant_id en la zona de predicado de toda query de test sobre tablas strict"
      pattern: "TENANT_TEMPLO"
---

<objective>
Cerrar la cola larga del trabajo de tests: analytics, scheduling, asistencia, tests de
migraciones, Wellhub y los tests de tenancy de fases anteriores que siembran plata.

Purpose: es el último grupo que puede poner la suite roja cuando se encienda el throw.
Al terminar este plan, la búsqueda global de queries de test sin tenant sobre las 6
tablas strict tiene que dar CERO.
Output: 15 archivos verdes y el inventario global en cero.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/172-adopci-n-1-piloto-finance/172-CONTEXT.md
@.planning/phases/172-adopci-n-1-piloto-finance/172-PATTERNS.md

Working dir: `/home/franco/projects/et-172`. Tablas strict: `financial_transactions`,
`transaction_links`, `balances`, `cash_registers`, `cost_centers`, `debt_management`.

**Serialización obligatoria de los planes 172-13 → 172-14 → 172-15 → 172-16 (waves 8-11):**
los cuatro hacen la misma verificación en caliente sobre el MISMO archivo compartido
`el-templo-api/src/db/tenant-tables.ts` (agregar `finance` a `TENANT_STRICT_MODULES`,
correr, y revertir con `git checkout -- src/db/tenant-tables.ts`). Si dos corrieran en
paralelo en este worktree, el revert de uno pisaría la sonda del otro y las corridas
darían falsos verdes o falsos rojos. Por eso cada plan depende del anterior y **nunca**
se lanzan en la misma wave. Antes de encender la sonda, verificar que
`git -C /home/franco/projects/et-172 status --porcelain el-templo-api/src/db/tenant-tables.ts`
esté vacío: si no lo está, hay otra sonda viva y hay que frenar.
</context>

<tasks>

<task type="auto">
  <name>Task 1: analytics, scheduling, asistencia y Wellhub</name>
  <files>el-templo-api/test/analytics/analytics.test.ts, el-templo-api/test/analytics/advanced-finance.test.ts, el-templo-api/test/analytics/breakdowns-cohorts.test.ts, el-templo-api/test/analytics/especial-exclusion.test.ts, el-templo-api/test/analytics/ltv.test.ts, el-templo-api/test/analytics/ticket.test.ts, el-templo-api/test/scheduling/scheduling.test.ts, el-templo-api/test/scheduling/155-horarios.test.ts, el-templo-api/test/scheduling/schedule-exceptions.test.ts, el-templo-api/test/attendance/attendance.test.ts, el-templo-api/test/wellhub/webhook-booking.test.ts</files>
  <read_first>
    - el-templo-api/test/fixtures/second-tenant.ts L119 (TENANT_TEMPLO = 1)
    - el-templo-api/test/analytics/analytics.test.ts (seeds de ingresos: financial_transactions + transaction_links)
    - el-templo-api/test/analytics/breakdowns-cohorts.test.ts (el que más siembra del grupo)
    - el-templo-api/src/db/tenant-tables.ts L465-515 (encendido en caliente del strict y su reversión)
  </read_first>
  <action>
    Inventariar con el grep del plan 172-14 sobre estos archivos y anotar el conteo en el
    SUMMARY. Agregar tenant explícito en cada sitio:
    `tenantWhere(schema.X, { tenantId: TENANT_TEMPLO })` en lecturas,
    `tenantValues({ tenantId: TENANT_TEMPLO }, { ... })` en escrituras, `AND tenant_id = 1`
    en `sql` crudo.
    Sin cambiar expectativas ni hardcodear ids de `users`.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Ninguna query directa sobre las 6 tablas strict queda sin tenant en esos 11 archivos
    - `git diff -- el-templo-api/test | grep -c "^[-+].*expect("` devuelve 0
    - `pnpm exec tsc --noEmit` sale con código 0
    - El SUMMARY lista, archivo por archivo, cuántas queries se migraron (11 filas, una por archivo de test) — el plan es mecánico pero de fan-out alto: sin ese conteo no hay auditoría post-hoc posible
  </acceptance_criteria>
  <done>Analytics, scheduling, asistencia y Wellhub listos para el throw.</done>
</task>

<task type="auto">
  <name>Task 2: tests de migraciones y de tenancy + inventario global en cero</name>
  <files>el-templo-api/test/migrations/0109_reconcile_soledad.test.ts, el-templo-api/test/migrations/0196-tenant-unique-contracts.test.ts, el-templo-api/test/tenancy/con-01-uniques-cross-tenant.test.ts, el-templo-api/test/tenancy/con-03-write-paths-tenant-id.test.ts</files>
  <read_first>
    - el-templo-api/test/migrations/0109_reconcile_soledad.test.ts (~36 referencias; es un test de migración histórica que siembra transacciones y saldos a mano)
    - el-templo-api/test/tenancy/con-01-uniques-cross-tenant.test.ts y con-03-write-paths-tenant-id.test.ts (tests de fases anteriores que YA escriben con tenant explícito en la mayoría de los sitios: verificar, no reescribir)
    - el-templo-api/test/fixtures/second-tenant.ts L360-401 (los DELETEs crudos que la fase 171 ya escribió con `WHERE tenant_id`, anticipando este throw)
  </read_first>
  <action>
    Mismo tratamiento, inventariando primero con el mismo grep y anotando el conteo por
    archivo en el SUMMARY. En los tests de tenancy, respetar lo que ya está escrito: solo
    completar lo que falte; NO rediseñar sus fixtures ni sus ids de tenant (90168, 90169,
    90269, 90369, 90418, 90469, 90569, 90671, 90940 ya están tomados).
    Cerrar con el barrido global: correr sobre TODO `test/` el grep de accesos directos a
    las 6 tablas strict y verificar que cada sitio tiene tenant o la exención
    `tenant-safe:` del plan 172-13. Anotar en el SUMMARY el comando exacto y su salida
    (debe ser vacía).
    Verificación en caliente: agregar TEMPORALMENTE la entrada `finance` en
    `TENANT_STRICT_MODULES`, correr los 15 archivos del plan, y REVERTIR antes de
    commitear. El commit NO incluye `src/db/tenant-tables.ts`.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit && pnpm exec vitest run test/analytics test/scheduling test/attendance test/migrations test/wellhub test/tenancy --hookTimeout=900000</automated>
  </verify>
  <acceptance_criteria>
    - Con la entrada `finance` puesta a mano en `TENANT_STRICT_MODULES`, los 15 archivos pasan (salida en el SUMMARY)
    - El barrido global sobre `test/` no encuentra ninguna query sobre las 6 tablas strict sin tenant ni exención (comando y salida vacía en el SUMMARY)
    - `git -C /home/franco/projects/et-172 status --porcelain el-templo-api/src/db/tenant-tables.ts` no devuelve nada al terminar
    - `pnpm exec tsc --noEmit` sale con código 0
    - El SUMMARY lista, archivo por archivo, cuántas queries se migraron (4 filas más, total 15 en el plan)
  </acceptance_criteria>
  <done>La suite entera queda preparada para el throw; el switch del plan 172-21 no debería encontrar sorpresas de fixtures.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                                      |
| ----------------------------------------- | -------------------------------------------------------------------------------- |
| tests de fases anteriores → tablas strict | ya fueron escritos con reglas de tenancy: tocarlos de más rompe evidencia ganada |

## STRIDE Threat Register

| Threat ID   | Category    | Component                                | Disposition | Mitigation Plan                                                                               |
| ----------- | ----------- | ---------------------------------------- | ----------- | --------------------------------------------------------------------------------------------- |
| T-172-16-01 | Tampering   | tests de tenancy de las fases 169-171    | mitigate    | solo se completa lo que falte; prohibido rediseñar fixtures o reusar ids de tenant ya tomados |
| T-172-16-02 | Repudiation | inventario incompleto de queries de test | mitigate    | barrido global sobre `test/` con comando y salida registrados en el SUMMARY                   |
| T-172-SC    | Tampering   | instalación de paquetes                  | accept      | este plan no instala nada                                                                     |

</threat_model>

<verification>
- 15 archivos verdes con strict encendido en caliente.
- Barrido global sobre `test/` con salida vacía.
- Sonda revertida.
- Conteo de sitios migrados por archivo registrado en el SUMMARY (15 filas).
</verification>

<success_criteria>
No queda ninguna query de test sobre las 6 tablas strict sin gimnasio explícito o exención
declarada.
</success_criteria>

<output>
Create `.planning/phases/172-adopci-n-1-piloto-finance/172-16-SUMMARY.md` when done
</output>
