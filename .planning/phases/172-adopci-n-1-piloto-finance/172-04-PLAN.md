---
phase: 172-adopci-n-1-piloto-finance
plan: 04
type: execute
wave: 2
depends_on: ["172-01"]
files_modified:
  - el-templo-api/src/modules/coach/service.ts
  - el-templo-api/src/modules/coach/routes.ts
  - el-templo-api/src/modules/members/service.ts
  - el-templo-api/src/modules/members/routes.ts
  - el-templo-api/src/scripts/backfill-historical-payments.ts
autonomous: true
requirements: [ADO-01]
must_haves:
  truths:
    - "El listado de deuda del profe y el listado de socios solo suman balances del gimnasio del staff logueado (D-01, D-07)"
    - "El script de backfill exige --tenant y no escribe una sola fila si el gimnasio no resuelve (D-02)"
  artifacts:
    - path: "el-templo-api/src/modules/coach/service.ts"
      provides: "getOutstandingBalances scopeado por tenant"
      contains: "tenantWhere(schema.balances"
    - path: "el-templo-api/src/modules/members/service.ts"
      provides: "agregado de deuda de listMembers scopeado"
      contains: "tenantWhere(schema.balances"
    - path: "el-templo-api/src/scripts/backfill-historical-payments.ts"
      provides: "retrofit CLI con requireTenant + tenantWhere/tenantValues"
      contains: "requireTenant"
  key_links:
    - from: "el-templo-api/src/scripts/backfill-historical-payments.ts"
      to: "src/db/scripts/require-tenant.ts"
      via: "requireTenant(queryFnFromConnection(connection)) antes de cualquier query"
      pattern: "requireTenant\\(queryFnFromConnection"
---

<objective>
Cerrar los tres accesos ajenos chicos a tablas strict: `coach/service.ts` (balances),
`members/service.ts` (balances) y el script CLI `backfill-historical-payments.ts`
(balances + financial_transactions + transaction_links), este último con el retrofit
completo de la receta 169 D-06 (D-02 de esta fase).

Purpose: son 5 de las 33 entradas de allowlist sobre tablas strict, en archivos que no
tienen otra relación con finance. Cirugía mínima (D-07).
Output: los 3 archivos scopeados y el script exigiendo `--tenant` con exit codes de uso.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/172-adopci-n-1-piloto-finance/172-CONTEXT.md
@.planning/phases/172-adopci-n-1-piloto-finance/172-PATTERNS.md

Working dir: `/home/franco/projects/et-172`. `tenant-lint-allowlist.json` no se edita acá.
</context>

<tasks>

<task type="auto">
  <name>Task 1: coach/service.ts y members/service.ts scopeados</name>
  <files>el-templo-api/src/modules/coach/service.ts, el-templo-api/src/modules/coach/routes.ts, el-templo-api/src/modules/members/service.ts, el-templo-api/src/modules/members/routes.ts</files>
  <read_first>
    - el-templo-api/src/modules/shared/tenant.ts (firmas lockeadas; assertTenant como único puente)
    - el-templo-api/src/modules/coach/service.ts (getOutstandingBalances L39, el array `conds` L47 y la query sobre schema.balances L67-82)
    - el-templo-api/src/modules/coach/routes.ts L38-52 (call site que hoy pasa un objeto de scope armado a mano con role/isOwner/country/branchIds)
    - el-templo-api/src/modules/members/service.ts L340-370 (subquery de deuda sobre schema.balances dentro de listMembers L83)
    - el-templo-api/src/modules/members/routes.ts L486 (call site de listMembers)
  </read_first>
  <action>
    `coach/service.ts`: `ctx: TenantContext` como PRIMER parámetro de
    `getOutstandingBalances` (antes de `filters` y del objeto de scope existente, que NO se
    toca) y `tenantWhere(schema.balances, ctx)` como PRIMER elemento del array `conds`.
    El `innerJoin` con `users` no se toca (D-07).
    `members/service.ts`: `ctx: TenantContext` como PRIMER parámetro de `listMembers` y
    `tenantWhere(schema.balances, ctx)` sumado al `and(...)` de la subquery de deuda
    (L367), como primer término.
    Call sites: `coach/routes.ts` pasa `assertTenant(request.scope, "coach.outstanding-balances")`
    y `members/routes.ts` L486 pasa `assertTenant(request.scope, "members.list")`.
    Import por path directo (`../shared/tenant`). Prohibidos `tenantId!` y `?? 1`.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tenantWhere(schema.balances" src/modules/coach/service.ts` devuelve 1 o más
    - `grep -c "tenantWhere(schema.balances" src/modules/members/service.ts` devuelve 1 o más
    - `grep -c "assertTenant(request.scope" src/modules/coach/routes.ts` devuelve 1 o más
    - `grep -nE "tenantId!|\?\? 1" src/modules/coach/service.ts src/modules/members/service.ts` no devuelve líneas
    - `pnpm exec tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>Ni el profe ni el listado de socios pueden ver deuda de otro gimnasio.</done>
</task>

<task type="auto">
  <name>Task 2: retrofit del script backfill-historical-payments.ts (D-02)</name>
  <files>el-templo-api/src/scripts/backfill-historical-payments.ts</files>
  <read_first>
    - el-templo-api/scripts/seed-onboarding-aura.ts (ejemplar COMPLETO de la receta CLI: requireTenant + queryFnFromConnection + failTenantArg + tenantWhere/tenantValues)
    - el-templo-api/src/db/scripts/require-tenant.ts (contrato de `--tenant`, exit code 2 en error de USO, 1 en el resto)
    - el-templo-api/src/scripts/backfill-historical-payments.ts (imports desnudos de tablas L31-41, SELECTs de idempotencia L392 y L410, los 3 INSERT dentro de la tx L473/L489/L498, y el catch de L520)
  </read_first>
  <action>
    Importar `failTenantArg`, `queryFnFromConnection` y `requireTenant` desde
    `../db/scripts/require-tenant`, y `tenantValues`/`tenantWhere` desde
    `../modules/shared/tenant`. En `main()`, ANTES de cualquier query, resolver
    `const ctx = await requireTenant(queryFnFromConnection(connection));` justo después de
    `createSingleConnection()`: si falta `--tenant` o el gimnasio no existe, el proceso
    corta con exit code 2 sin escribir nada.
    Los SELECT de idempotencia de L392 (`financialTransactions`) y L410 (`balances`) llevan
    `tenantWhere(financialTransactions, ctx)` / `tenantWhere(balances, ctx)` como primer
    término del `and(...)` — las tablas se importan DESNUDAS en este archivo (no por
    `schema.`), y `tenantWhere` acepta la tabla igual porque su firma pide la tabla, no el
    namespace. Los 3 INSERT (L473 financialTransactions, L489 transactionLinks, L498
    balances) envuelven su objeto en `tenantValues(ctx, { ... })`, sin agregar `as const`
    (hallazgo 169-07). Los SELECT sobre `subscriptions`/`users`/`branches` (L354/L364/L378)
    NO se tocan: no son tablas strict y este archivo no es del módulo finance (D-07).
    Reemplazar el `.catch` de L520 por `main().catch((err: unknown) => failTenantArg(err, "backfill-historical-payments"));`.
    Actualizar el docblock de arranque con el usage real (`pnpm tsx src/scripts/backfill-historical-payments.ts --tenant=<id>`).
    Después generar `/tmp/allowlist-172-04.json` = allowlist sin las 5 entradas de
    `src/modules/coach/service.ts`, `src/modules/members/service.ts` y
    `src/scripts/backfill-historical-payments.ts` sobre tablas strict, y correr el lint
    contra esa copia.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit && pnpm exec tsx src/db/scripts/lint-tenant.ts --allowlist=/tmp/allowlist-172-04.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "requireTenant(queryFnFromConnection" src/scripts/backfill-historical-payments.ts` devuelve exactamente 1
    - `grep -c "tenantValues(ctx" src/scripts/backfill-historical-payments.ts` devuelve 3
    - `grep -c "failTenantArg" src/scripts/backfill-historical-payments.ts` devuelve 2 o más (import + catch)
    - Correr el script sin `--tenant` (`pnpm exec tsx src/scripts/backfill-historical-payments.ts`) sale con código 2 y no escribe filas
    - `/tmp/allowlist-172-04.json` tiene 5 entradas menos y el lint contra esa copia sale con código 0
    - `pnpm exec tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>El backfill histórico no puede correr sin gimnasio explícito ni estampar filas en el gimnasio equivocado.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                               | Description                                                                                 |
| ---------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| coach (rol menos privilegiado) → /api/admin/coach/outstanding-balances | el borde más barato de atacar: si filtra, filtra para el rol más común                      |
| operador de consola → script de backfill                               | escribe plata histórica en batch, sin request ni JWT: el gimnasio solo puede venir del flag |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                                                     | Disposition | Mitigation Plan                                                                                                                 |
| ----------- | ---------------------- | ----------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------- |
| T-172-04-01 | Information Disclosure | coachService.getOutstandingBalances / memberService.listMembers               | mitigate    | `tenantWhere(schema.balances, ctx)` primer término; ctx desde `assertTenant(request.scope, ...)`                                |
| T-172-04-02 | Tampering              | INSERTs del backfill en financial_transactions / transaction_links / balances | mitigate    | `tenantValues(ctx, ...)` con el tenant después del spread; sin `--tenant` el script muere con exit 2 antes de abrir transacción |
| T-172-04-03 | Repudiation            | corrida del backfill contra el gimnasio equivocado                            | mitigate    | `requireTenant` valida que el gimnasio exista y deja el id en el log de arranque; fail-closed (exit 2), nunca default 1         |
| T-172-SC    | Tampering              | instalación de paquetes                                                       | accept      | este plan no instala nada                                                                                                       |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit` verde.
- Script sin `--tenant` → exit 2 y cero escrituras.
- Lint verde contra la allowlist filtrada (5 entradas menos).
</verification>

<success_criteria>
Los tres accesos ajenos chicos a tablas strict quedan cerrados y el script de backfill
adopta la receta CLI completa.
</success_criteria>

<output>
Create `.planning/phases/172-adopci-n-1-piloto-finance/172-04-SUMMARY.md` when done
</output>
