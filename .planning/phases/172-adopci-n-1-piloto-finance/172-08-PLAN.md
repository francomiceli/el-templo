---
phase: 172-adopci-n-1-piloto-finance
plan: 08
type: execute
wave: 4
depends_on: ["172-07"]
files_modified:
  - el-templo-api/src/modules/finance/transaction-service.ts
  - el-templo-api/src/modules/finance/routes.ts
  - el-templo-api/src/modules/finance/coach-load-routes.ts
  - el-templo-api/src/modules/subscriptions/service.ts
  - el-templo-api/src/modules/members/routes.ts
  - el-templo-api/src/modules/programs/enrollment-service.ts
  - el-templo-api/src/modules/programs/routes.ts
  - el-templo-api/test/finance/transaction-service.test.ts
  - el-templo-api/test/finance/validate-caja.test.ts
  - el-templo-api/test/finance/validation-regression.test.ts
  - el-templo-api/test/finance/validation-state.test.ts
  - el-templo-api/test/finance/summary-by-kind.test.ts
  - el-templo-api/test/finance/movement-service.test.ts
  - el-templo-api/test/branch-access.test.ts
  - el-templo-api/test/programs/admin-addons.test.ts
autonomous: true
requirements: [ADO-01]
must_haves:
  truths:
    - "Los 21 métodos de TransactionService reciben TenantContext como primer parámetro"
    - "Ningún call site de TransactionService compila sin entregar un gimnasio de una fuente legítima"
    - "_cancelSubscription recibe el ctx del void que la invoca y sus 2 queries finance quedan scopeadas"
  artifacts:
    - path: "el-templo-api/src/modules/finance/transaction-service.ts"
      provides: "firmas con ctx primero y tipo SubscriptionCanceller actualizado"
      contains: "ctx: TenantContext"
  key_links:
    - from: "el-templo-api/src/modules/finance/transaction-service.ts"
      to: "subscriptionCanceller._cancelSubscription"
      via: "el ctx del _void viaja al canceller"
      pattern: "_cancelSubscription\\("
---

<objective>
Plomería del corazón del módulo: `TenantContext` como primer parámetro en los 21 métodos
de `TransactionService`, el tipo `SubscriptionCanceller` actualizado, y TODOS los call
sites (rutas de finance, coach-load, members, programs, subscriptions, jobs y tests)
entregando un ctx de fuente legítima.

Purpose: `tsc` es el que va a probar que no quedó ningún camino de cobro sin gimnasio. Se
hace en un plan aparte de las queries porque la superficie es de firmas y llamadas, no de
SQL: separarlo evita releer 2172 líneas dos veces con criterios distintos.

**Este plan NO agrega ningún `tenantWhere`/`tenantValues` dentro de
`transaction-service.ts`** — eso es de los planes 172-10 (escrituras) y 172-12 (lecturas).
La excepción son las 2 queries de `_cancelSubscription` en `subscriptions/service.ts`,
que se cierran acá porque acá cambia su firma.
Output: el módulo entero compilando con ctx explícito de punta a punta.

**Nota de secuencia:** el trabajo de call sites ajenos va partido en dos tasks
(producción / tests) para que cada commit sea atómico y ningún task supere ~8 archivos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/172-adopci-n-1-piloto-finance/172-CONTEXT.md
@.planning/phases/172-adopci-n-1-piloto-finance/172-PATTERNS.md

Working dir: `/home/franco/projects/et-172`. La allowlist no se toca.
Plan mecánico: cero cambios de lógica, cero expectativas de test tocadas.
</context>

<tasks>

<task type="auto">
  <name>Task 1: firmas de TransactionService + tipo SubscriptionCanceller + _cancelSubscription</name>
  <files>el-templo-api/src/modules/finance/transaction-service.ts, el-templo-api/src/modules/subscriptions/service.ts</files>
  <read_first>
    - el-templo-api/src/modules/shared/tenant.ts (firmas lockeadas y las 4 fuentes legítimas de ctx)
    - el-templo-api/src/modules/finance/transaction-service.ts L85-160 (tipo SubscriptionCanceller L93, constructor L113, setSubscriptionCanceller L125, resolveCashRegister L136) y las firmas de create L158, void L375, voidPair L408, voidInTx L435, _void L459 (con la llamada al canceller en L522), validate L634, observe L750, correct L826, findByIdempotencyKey L955, getById L972, listForMember L1005, listPendingMiscForMember L1049, list L1091, listPendingTray L1261, listMovEgresos L1462, buildListConditions L1623, getFinancialHistory L1681, getOutstandingConcepts L1789, getSummary L1912, exportRowsForExcel L2068
    - el-templo-api/src/modules/subscriptions/service.ts L2699-2990 (cancelSubscription L2699 — ya tiene ctx del plan 172-07 — y _cancelSubscription L2785 con el SELECT sobre transaction_links L2844 y el UPDATE sobre balances L2936)
    - el-templo-api/src/modules/tv/pairing.ts L173-180 (docblock de por qué el ctx va primero)
  </read_first>
  <action>
    Agregar `ctx: TenantContext` como PRIMER parámetro de los 21 métodos listados arriba
    (incluidos los privados `_void` y `buildListConditions`, y antes del `tx` en
    `voidInTx`). Propagarlo en todas las llamadas internas del archivo.
    Cambiar el tipo `SubscriptionCanceller` (L93) para que `_cancelSubscription` reciba
    `ctx: TenantContext` primero, y pasar en L522 el ctx que `_void` ya tiene.
    En `subscriptions/service.ts`: `_cancelSubscription(ctx, tx, ...)`, actualizar su call
    site interno de L2736 (`cancelSubscription` ya tiene ctx), y migrar sus 2 accesos:
    `tenantWhere(schema.transactionLinks, ctx)` como primer término en L2844 y
    `tenantWhere(schema.balances, ctx)` en el `where` del UPDATE de L2936.
    Las llamadas a `this.balanceService.*` y `this.cashRegisterService.*` desde
    `transaction-service` NO se tocan (sus firmas cambian en el plan 172-09).
    Import por path directo desde `../shared/tenant`.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ctx: TenantContext" src/modules/finance/transaction-service.ts` devuelve 21 o más
    - `grep -c "tenantWhere(schema.transactionLinks" src/modules/subscriptions/service.ts` devuelve 1 o más
    - `grep -c "tenantWhere(schema.balances" src/modules/subscriptions/service.ts` devuelve 1 o más
    - `grep -nE "tenantId!|\?\? 1" src/modules/finance/transaction-service.ts src/modules/subscriptions/service.ts` no devuelve líneas
    - Al terminar el Task 4, `pnpm exec tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>Toda la superficie de TransactionService exige gimnasio, y la cancelación disparada por un void ya no puede tocar filas ajenas.</done>
</task>

<task type="auto">
  <name>Task 2: call sites de finance/routes.ts y coach-load-routes.ts</name>
  <files>el-templo-api/src/modules/finance/routes.ts, el-templo-api/src/modules/finance/coach-load-routes.ts</files>
  <read_first>
    - el-templo-api/src/modules/finance/routes.ts L203-214 (hook de módulo que deja request.scope) y los 20 call sites de transactionService (L311, L392, L429, L466, L502, L546, L803, L853, L920, L1027, L1070, L1609, L1657 entre otros)
    - el-templo-api/src/modules/finance/routes.ts L1391-1413 (analog A: assertTenant inline)
    - el-templo-api/src/modules/finance/coach-load-routes.ts L249 (hook con FINANCE_LOAD_ROLES, coach incluido) y sus call sites (L500, L574, L582, L631, L666, L715, L818, L845, L1015)
    - el-templo-api/src/modules/finance/coach-load-routes.ts L984-997 (analog B: `const ctx = assertTenant(...)` cuando el ctx se usa más de una vez)
  </read_first>
  <action>
    Cada call site de `transactionService.*` pasa como primer argumento
    `assertTenant(request.scope, "<etiqueta>")` con una etiqueta que nombre la ruta
    (`finance.transactions.create`, `finance.transactions.void`, `finance.transactions.validate`,
    `finance.transactions.observe`, `finance.transactions.correct`, `finance.transactions.list`,
    `finance.transactions.summary`, `finance.transactions.export`, `finance.pending-tray`,
    `finance.pending-tray.export`, `finance.movements-history`, `finance.movements-history.export`,
    `coach-load.alta`, `coach-load.misc`, `coach-load.pay-plan`, `coach-load.mis-cargas`, etc.).
    Cuando un handler llama a más de un método del service, resolver el ctx UNA vez con
    `const ctx = assertTenant(request.scope, "<etiqueta>");` y reusarlo (analog B).
    No agregar try/catch nuevos: `assertTenant` lanza `AppError(403, TENANT_UNRESOLVED)` y
    `handleServiceError` ya lo mapea.
    Prohibidos `request.scope.tenantId!` y `?? 1`.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "assertTenant(request.scope" src/modules/finance/routes.ts` devuelve 20 o más
    - `grep -c "assertTenant(request.scope" src/modules/finance/coach-load-routes.ts` devuelve 8 o más
    - `grep -nE "tenantId!|\?\? 1" src/modules/finance/routes.ts src/modules/finance/coach-load-routes.ts` no devuelve líneas
    - `git diff -- el-templo-api/src/modules/finance/routes.ts` no muestra cambios en códigos de status ni en mensajes de error existentes
  </acceptance_criteria>
  <done>Las 38 rutas de finance resuelven el gimnasio en el borde, de forma visible en el call site.</done>
</task>

<task type="auto">
  <name>Task 3: call sites de producción ajenos (members, programs, subscriptions)</name>
  <files>el-templo-api/src/modules/members/routes.ts, el-templo-api/src/modules/programs/enrollment-service.ts, el-templo-api/src/modules/programs/routes.ts, el-templo-api/src/modules/subscriptions/service.ts</files>
  <read_first>
    - el-templo-api/src/modules/members/routes.ts L1563 (getFinancialHistory) y L1636 (getOutstandingConcepts) — el archivo ya viene tocado por los planes 172-04 y 172-07
    - el-templo-api/src/modules/programs/enrollment-service.ts L420-440 (enrollAddon llama a this.transactionService.create) y el constructor L87-95
    - el-templo-api/src/modules/programs/routes.ts (call sites de enrollAddon)
    - el-templo-api/src/modules/subscriptions/service.ts L558, L1557, L1786, L4288 (llamadas a transactionService: create, resolveCashRegister ×2, voidInTx) — todos esos métodos ya tienen ctx del plan 172-07
  </read_first>
  <action>
    `members/routes.ts`: los 2 call sites pasan
    `assertTenant(request.scope, "members.financial-history")` y
    `assertTenant(request.scope, "members.outstanding-concepts")`.
    `programs/enrollment-service.ts`: `ctx: TenantContext` como primer parámetro de
    `enrollAddon` (y de cualquier método intermedio que marque tsc), pasándolo a
    `this.transactionService.create`; sus call sites en `programs/routes.ts` usan
    `assertTenant(request.scope, "programs.enroll-addon")`.
    `subscriptions/service.ts`: pasar en L558, L1557, L1786 y L4288 el `ctx` que esos
    métodos ya reciben — no construir uno nuevo.
    Prohibidos `tenantId!` y `?? 1`.
    `tsc` va a seguir marcando los call sites de test hasta que termine el Task 4: eso es
    esperado, este task cierra solo la superficie de producción.
    Registrar en el SUMMARY el conteo de call sites actualizados POR ARCHIVO.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit 2>&1 | grep -v "^test/" | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec tsc --noEmit 2>&1 | grep -v "^test/" | grep -c "error TS"` devuelve 0 (cero errores fuera de `test/`)
    - `grep -nE "tenantId!|\?\? 1" src/modules/members/routes.ts src/modules/programs/enrollment-service.ts src/modules/programs/routes.ts src/modules/subscriptions/service.ts` no devuelve líneas
    - `grep -c "assertTenant(request.scope" src/modules/members/routes.ts` devuelve 2 o más
    - `grep -c "ctx: TenantContext" src/modules/programs/enrollment-service.ts` devuelve 1 o más
    - El SUMMARY lista, archivo por archivo, cuántos call sites se actualizaron (4 filas, una por archivo)
  </acceptance_criteria>
  <done>Ningún módulo ajeno llama a TransactionService sin gimnasio; el código de producción compila entero.</done>
</task>

<task type="auto">
  <name>Task 4: call sites de test de finance, programas y branch-access</name>
  <files>el-templo-api/test/finance/transaction-service.test.ts, el-templo-api/test/finance/validate-caja.test.ts, el-templo-api/test/finance/validation-regression.test.ts, el-templo-api/test/finance/validation-state.test.ts, el-templo-api/test/finance/summary-by-kind.test.ts, el-templo-api/test/finance/movement-service.test.ts, el-templo-api/test/branch-access.test.ts, el-templo-api/test/programs/admin-addons.test.ts</files>
  <read_first>
    - el-templo-api/test/fixtures/second-tenant.ts L119 (TENANT_TEMPLO)
    - el-templo-api/test/finance/transaction-service.test.ts (el archivo con más llamadas directas al service)
    - la salida de `pnpm exec tsc --noEmit` al terminar el Task 3: es el inventario exacto de call sites de test pendientes
  </read_first>
  <action>
    Agregar `{ tenantId: TENANT_TEMPLO }` como PRIMER argumento en cada llamada directa a
    métodos de `TransactionService` (y a `enrollAddon` donde aparezca), importando
    `TENANT_TEMPLO` desde `test/fixtures/second-tenant` con la ruta relativa que
    corresponda. No cambiar ninguna expectativa ni hardcodear ids de `users` (resolver
    `admin@test.com` como ya hacen esos archivos).
    Este task NO agrega `tenantWhere`/`tenantValues` a las queries directas de esos tests
    — eso es de los planes 172-13/172-14. Acá solo viajan argumentos de ctx.
    Cerrar con `tsc` verde y una corrida dirigida del test de transacciones.
    Registrar en el SUMMARY el conteo de call sites actualizados POR ARCHIVO.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit && pnpm exec vitest run test/finance/transaction-service.test.ts --hookTimeout=250000</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec tsc --noEmit` sale con código 0
    - `test/finance/transaction-service.test.ts` pasa entero sin cambios en sus `expect(...)`
    - `grep -rn "TENANT_TEMPLO" test/finance/transaction-service.test.ts` devuelve al menos 1 línea
    - `git diff -- el-templo-api/test | grep -c "^[-+].*expect("` devuelve 0
    - El SUMMARY lista, archivo por archivo, cuántos call sites se actualizaron (8 filas, una por archivo)
  </acceptance_criteria>
  <done>Cero call sites de TransactionService sin gimnasio, incluidos los de los tests.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                       | Description                                                                                           |
| ---------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| handler de ruta → TransactionService           | acá se cruza de `number` o `null` a `number`: es el único punto donde puede colarse un `!`            |
| TransactionService → SubscriptionCanceller     | un void dispara una cancelación en otro módulo: si el ctx no viaja, la cancelación queda sin gimnasio |
| enrollment-service → transactionService.create | cobro disparado desde programas, fuera del módulo finance                                             |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                       | Disposition | Mitigation Plan                                                                                                     |
| ----------- | ---------------------- | ----------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------- |
| T-172-08-01 | Elevation of Privilege | narrowing de request.scope en 30+ call sites    | mitigate    | `assertTenant` es el único puente permitido; grep de `tenantId!` y `?? 1` en criterios de aceptación de los 3 tasks |
| T-172-08-02 | Tampering              | cadena void → \_cancelSubscription              | mitigate    | el tipo `SubscriptionCanceller` exige el ctx: un canceller sin gimnasio no compila                                  |
| T-172-08-03 | Repudiation            | etiqueta del assertTenant                       | mitigate    | cada call site pasa una etiqueta que nombra la ruta, para que el 403 TENANT_UNRESOLVED sea accionable en el log     |
| T-172-08-04 | Tampering              | tests que llaman services con tenant equivocado | mitigate    | `TENANT_TEMPLO` importado del fixture, nunca un `1` a mano; ids de users resueltos por email                        |
| T-172-SC    | Tampering              | instalación de paquetes                         | accept      | este plan no instala nada                                                                                           |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit` verde (es LA prueba de este plan: no queda camino sin gimnasio).
- `test/finance/transaction-service.test.ts` verde sin tocar expectativas.
- Cero `tenantId!` / `?? 1` en los archivos tocados.
- El SUMMARY documenta el conteo de call sites migrados por archivo en los Tasks 2, 3 y 4.
</verification>

<success_criteria>
El compilador garantiza que ninguna operación de cobro, anulación, validación,
observación o corrección puede ejecutarse sin un gimnasio resuelto server-side.
</success_criteria>

<output>
Create `.planning/phases/172-adopci-n-1-piloto-finance/172-08-SUMMARY.md` when done
</output>
