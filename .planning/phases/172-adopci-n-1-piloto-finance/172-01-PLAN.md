---
phase: 172-adopci-n-1-piloto-finance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [ADO-01]
must_haves:
  truths:
    - "Existe un worktree limpio en /home/franco/projects/et-172 sobre feat/172-adopcion-finance creado desde origin/master"
    - "El fix CR-CAJA está en origin/master Y desplegado en staging antes de arrancar la migración (D-13), para que el snapshot ANTES de 172-05 no quede contaminado (D-12)"
    - "La base del worktree arranca verde: tsc sin errores y lint:tenant sin violaciones nuevas"
  artifacts:
    - path: "/home/franco/projects/et-172/el-templo-api/.env"
      provides: "credenciales locales de MySQL para typecheck/tests dirigidos (no viaja por git)"
    - path: "/home/franco/projects/et-172/el-templo-api/tenant-lint-allowlist.json"
      provides: "allowlist base con 501 entradas — la línea de partida del ratchet"
  key_links:
    - from: "/home/franco/projects/et-172"
      to: "origin/master"
      via: "git worktree add -b feat/172-adopcion-finance"
      pattern: "feat/172-adopcion-finance"
---

<objective>
Montar la base de ejecución de la fase 172 y cerrar el prerequisito D-13.

Purpose: el checkout principal (`/home/franco/projects/el-templo`) está ~403 commits
detrás de `origin/master`, en una rama ajena y COMPARTIDO con otra sesión. Ninguno de
los artefactos que esta fase consume (`src/modules/shared/tenant.ts`, `src/db/sentinel/`,
`test/tenant-manifest.ts`, `test/fixtures/second-tenant.ts`) existe ahí. Todos los planes
siguientes asumen que este worktree ya existe y arranca verde.

Output: worktree `/home/franco/projects/et-172` en `feat/172-adopcion-finance`, con
`.env` copiado, dependencias instaladas y baseline verde registrado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/172-adopci-n-1-piloto-finance/172-CONTEXT.md
@.planning/phases/172-adopci-n-1-piloto-finance/172-PATTERNS.md
@.claude/skills/el-templo-change-control/SKILL.md

**Regla de rutas para TODA la fase 172:** las rutas de código de los planes
(`el-templo-api/...`) son relativas a `/home/franco/projects/et-172`. Los documentos de
planning (`.planning/...`, incluidos los SUMMARY) se leen y escriben en el checkout
principal `/home/franco/projects/el-templo`. **Nunca** se edita código en el checkout
principal.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirmar que CR-CAJA está en master Y DESPLEGADO EN STAGING (D-13)</name>
  <what-built>Nada todavía — este es el gate de secuencia que fijó Franco en D-13.</what-built>
  <how-to-verify>
    1. Correr `git fetch origin && git log --oneline -25 origin/master` y
       `git log --oneline -25 origin/staging` en `/home/franco/projects/el-templo`.
    2. Confirmar que el fix "la caja del cobro sigue la sede del SOCIO" (toca
       `el-templo-api/src/modules/finance/coach-load-routes.ts` y
       `el-templo-api/src/modules/subscriptions/service.ts` — los dos archivos que esta
       fase reescribe) está mergeado a master **y** presente en `origin/staging`.
    3. Confirmar que ese commit de staging **ya está desplegado y corriendo en el servidor
       de staging**: `git log` prueba el merge, NO el deploy. Evidencia aceptada, en este
       orden de preferencia:
       a. El endpoint de salud/versión de la API de staging devuelve un SHA/versión que
          incluye el commit de CR-CAJA (`curl -s <staging-api>/api/health` o el endpoint de
          versión que exista, pegando la salida).
       b. Franco confirma explícitamente que el deploy de staging con CR-CAJA terminó verde
          (pipeline de deploy, no solo el merge).
       c. Franco cobra en staging con un socio de una sede distinta a la del operador y ve
          la caja de la sede del SOCIO: el comportamiento nuevo, en vivo.
    4. Si NO está desplegado en staging: la fase 172 no arranca. Motivo: el plan 172-05
       captura el snapshot "ANTES" (D-12) contra staging. Si CR-CAJA se deploya DESPUÉS de
       esa captura, el diff "antes/después" del plan 172-22 mezcla el cambio de caja con la
       migración de tenancy y deja de ser señal — el criterio 4 de la fase ("mismos
       números") queda invalidado.
    5. Si NO está mergeado a master: idem, terminar CR-CAJA y shippearlo primero.
  </how-to-verify>
  <action>
    Presentar al usuario la salida de `git log --oneline -25 origin/master`, la de
    `git log --oneline -25 origin/staging` y, si el endpoint existe, la respuesta de
    salud/versión de la API de staging. Pedir la confirmación explícita de las DOS
    condiciones por separado: (1) CR-CAJA mergeado a master, (2) CR-CAJA **corriendo** en
    staging. No crear el worktree ni tocar una línea de código sin las dos.
    Registrar en el SUMMARY qué evidencia de deploy se aceptó (a, b o c) y su salida.
  </action>
  <done>Franco confirmó que CR-CAJA está en master Y desplegado en staging (o frenó la fase), con la evidencia de deploy registrada en el SUMMARY.</done>
  <resume-signal>Escribí "cr-caja en master y en staging" para seguir, "falta deploy staging" o "falta cr-caja" para frenar la fase</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Crear el worktree et-172 y dejarlo verde</name>
  <files>ninguno versionado (crea /home/franco/projects/et-172)</files>
  <read_first>
    - .claude/skills/el-templo-change-control/SKILL.md (patrón de worktree por fase, staging-first, git add explícito)
    - .planning/phases/172-adopci-n-1-piloto-finance/172-PATTERNS.md (sección "ADVERTENCIA DE BASE DE CÓDIGO" y "Trampa de worktree")
  </read_first>
  <action>
    Desde `/home/franco/projects/el-templo`: `git fetch origin`, después
    `git worktree add /home/franco/projects/et-172 -b feat/172-adopcion-finance origin/master`.
    Copiar los archivos de entorno gitignoreados desde un worktree hermano vivo
    (`/home/franco/projects/et-170-sentinel`): `el-templo-api/.env` y, si existe,
    `el-templo-api/.env.development`. Correr `pnpm install` (SOLO instalación del
    lockfile existente — está PROHIBIDO agregar o actualizar dependencias en esta fase).
    Verificar la base: `pnpm exec tsc --noEmit` en `el-templo-api` y `pnpm lint:tenant`.
    Registrar en el SUMMARY el conteo de entradas de la allowlist al arrancar (debe ser
    501) y el head del worktree (debe ser `29e61c8b` o posterior si CR-CAJA entró después).
    NO tocar el checkout principal ni ninguna otra rama.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit && pnpm lint:tenant && node -e "const a=require('./tenant-lint-allowlist.json');const l=Array.isArray(a)?a:a.entries;console.log('entradas',l.length)"</automated>
  </verify>
  <acceptance_criteria>
    - `git -C /home/franco/projects/et-172 rev-parse --abbrev-ref HEAD` imprime `feat/172-adopcion-finance`
    - `git -C /home/franco/projects/et-172 merge-base --is-ancestor origin/master HEAD` sale con código 0
    - Existe `/home/franco/projects/et-172/el-templo-api/.env` y contiene una línea que empieza con `DB_` o `DATABASE_`
    - Existen `/home/franco/projects/et-172/el-templo-api/src/modules/shared/tenant.ts`, `src/db/sentinel/install.ts`, `test/tenant-manifest.ts` y `test/fixtures/second-tenant.ts`
    - `pnpm exec tsc --noEmit` sale con código 0
    - `pnpm lint:tenant` sale con código 0
    - `git -C /home/franco/projects/el-templo status --porcelain` no muestra archivos nuevos bajo `el-templo-api/`
  </acceptance_criteria>
  <done>Worktree operativo, typecheck y lint de tenancy verdes, checkout principal intacto.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                                                                 |
| -------------------------------------- | ------------------------------------------------------------------------------------------- |
| checkout compartido → worktree de fase | otra sesión puede estar editando el checkout principal; escribir ahí destruye trabajo ajeno |
| lockfile → node_modules                | `pnpm install` materializa código de terceros en el worktree                                |

## STRIDE Threat Register

| Threat ID   | Category  | Component                                            | Disposition | Mitigation Plan                                                                                                                                                                                   |
| ----------- | --------- | ---------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-172-01-01 | Tampering | checkout principal `/home/franco/projects/el-templo` | mitigate    | todo el código se edita SOLO en `/home/franco/projects/et-172`; criterio de aceptación verifica `git status --porcelain` limpio en el principal                                                   |
| T-172-01-02 | Tampering | base de la rama                                      | mitigate    | la rama se crea desde `origin/master` recién fetcheado y se verifica con `merge-base --is-ancestor`; una base vieja borraría las 4 capas de tenancy                                               |
| T-172-SC    | Tampering | pnpm install                                         | mitigate    | solo `pnpm install` del lockfile existente; CERO dependencias nuevas o actualizadas en toda la fase 172 (si apareciera una, gate de legitimidad + checkpoint humano bloqueante antes de instalar) |

</threat_model>

<verification>
- CR-CAJA mergeado a master **y** corriendo en staging, con evidencia de deploy registrada.
- Worktree en la rama y base correctas, con `.env` presente.
- `pnpm exec tsc --noEmit` y `pnpm lint:tenant` verdes ANTES de tocar una sola línea.
- Checkout principal sin cambios.
</verification>

<success_criteria>
La fase puede ejecutarse: existe una base de código con las 4 capas de tenancy (helpers,
sentinel, lint, manifiesto+fixtures) y con CR-CAJA adentro, verde de arranque; y staging
ya corre CR-CAJA, así que el snapshot "antes" de 172-05 mide solo la migración de tenancy.
</success_criteria>

<output>
Create `.planning/phases/172-adopci-n-1-piloto-finance/172-01-SUMMARY.md` when done
</output>
