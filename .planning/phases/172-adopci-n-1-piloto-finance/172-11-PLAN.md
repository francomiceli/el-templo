---
phase: 172-adopci-n-1-piloto-finance
plan: 11
type: execute
wave: 6
depends_on: ["172-09"]
files_modified:
  - el-templo-api/src/modules/finance/routes.ts
  - el-templo-api/src/modules/finance/coach-load-routes.ts
autonomous: true
requirements: [ADO-01]
must_haves:
  truths:
    - "Las closures de scope de finance (resolveCajaCountry, enforceCajaScope, enforceRowScope) filtran por gimnasio antes de decidir el 404 (D-09)"
    - "Las queries directas de los handlers de finance sobre cajas, transacciones y sedes filtran por gimnasio (D-06)"
    - "coach-load resuelve socio, sede, suscripción y plan solo dentro del gimnasio del coach"
  artifacts:
    - path: "el-templo-api/src/modules/finance/routes.ts"
      provides: "closures de scope y queries de handler scopeadas"
      contains: "tenantWhere(schema.cashRegisters"
    - path: "el-templo-api/src/modules/finance/coach-load-routes.ts"
      provides: "resolución de socio/sede/suscripción/plan scopeada"
      contains: "tenantWhere(schema.subscriptions"
  key_links:
    - from: "el-templo-api/src/modules/finance/routes.ts"
      to: "enforceCajaScope / resolveCajaCountry"
      via: "el ctx del handler entra a la closure como primer parámetro"
      pattern: "resolveCajaCountry\\(\\s*ctx"
---

<objective>
Migrar las queries que viven en los DOS archivos de rutas de finance y no en un service:
las closures de scope del plugin (`resolveCajaCountry`, `enforceCajaScope`,
`enforceRowScope`, routes.ts L126-197) y las lecturas directas de los handlers.

Purpose: son el punto ciego del módulo. Hoy consultan `cash_registers` y
`financial_transactions` sin scope y no tienen dónde recibir un ctx: cuando el sentinel
pase a throw, revientan. Además son las que deciden el 404 de "caja de otro país", así
que su filtro es el que hace cumplir el contrato D-09 (404/vacío, nunca 403).
Output: los dos archivos de rutas sin ningún acceso a base sin gimnasio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/172-adopci-n-1-piloto-finance/172-CONTEXT.md
@.planning/phases/172-adopci-n-1-piloto-finance/172-PATTERNS.md

Working dir: `/home/franco/projects/et-172`. La allowlist no se toca (plan 172-21).
</context>

<tasks>

<task type="auto">
  <name>Task 1: closures de scope de routes.ts con ctx explícito</name>
  <files>el-templo-api/src/modules/finance/routes.ts</files>
  <read_first>
    - el-templo-api/src/modules/finance/routes.ts L126-197 (resolveCajaCountry, enforceCajaScope con el comentario de fase 139 que ya explica el 404 para no filtrar existencia, y enforceRowScope: 4 accesos a cash_registers, 3 a financial_transactions, 3 a branches)
    - el-templo-api/src/modules/finance/routes.ts L203-214 (hook de módulo: request.scope ya está disponible en todo handler)
    - el-templo-api/src/modules/shared/tenant.ts (assertTenant y su AppError 403 TENANT_UNRESOLVED, que handleServiceError ya mapea)
  </read_first>
  <action>
    Agregar `ctx: TenantContext` como PRIMER parámetro de las tres closures
    (`resolveCajaCountry`, `enforceCajaScope`, `enforceRowScope`) y pasar desde cada
    handler el ctx que ya resuelve con `assertTenant`. Adentro, `tenantWhere` como primer
    término en las queries sobre `schema.cashRegisters`, `schema.financialTransactions` y
    `schema.branches`.
    El contrato de respuesta NO cambia: si la fila no matchea por tenant, la closure cae en
    su rama actual de "no encontrado" y devuelve el tuple `{ code: 404, ... }` que ya
    devuelve para cross-country. Eso ES el contrato D-09 (el recurso de otro gimnasio es
    indistinguible de uno inexistente) — no agregar ningún 403 nuevo.
    Ampliar el comentario de la fase 139 arriba de `enforceCajaScope` con una línea que
    diga que ahora el filtro es tenant + country, en ese orden.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '120,200p' src/modules/finance/routes.ts | grep -c "tenantWhere("` devuelve 3 o más
    - `sed -n '120,200p' src/modules/finance/routes.ts | grep -c "code: 403"` devuelve 0
    - `grep -nE "tenantId!|\?\? 1" src/modules/finance/routes.ts` no devuelve líneas
    - `pnpm exec tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>El guardián de caja del módulo decide con el gimnasio adentro del filtro y sigue respondiendo 404.</done>
</task>

<task type="auto">
  <name>Task 2: queries directas de los handlers de routes.ts</name>
  <files>el-templo-api/src/modules/finance/routes.ts</files>
  <read_first>
    - el-templo-api/src/modules/finance/routes.ts L290-420 (POST /transactions con 5 accesos a branches y POST /transactions/:id/void con 4 a financial_transactions y 4 a branches)
    - el-templo-api/src/modules/finance/routes.ts (resto de handlers: buscar con `grep -n "fastify.db" src/modules/finance/routes.ts` todos los accesos directos que queden)
  </read_first>
  <action>
    Recorrer TODOS los accesos directos a base que queden en el archivo (los que usan
    `fastify.db` en vez de un service) y agregarles `tenantWhere(<tabla>, ctx)` como primer
    término, tanto para tablas strict (`financial_transactions`, `cash_registers`) como
    para `branches` (D-06: el archivo tiene que quedar sin NINGUNA entrada de allowlist,
    incluida la de `branches`).
    El ctx sale del `assertTenant` que cada handler ya resuelve; si un handler todavía no
    lo resuelve, agregar `const ctx = assertTenant(request.scope, "<etiqueta>");` al
    principio del try.
    Verificar con `grep -n "fastify.db" src/modules/finance/routes.ts` que no queda ningún
    acceso sin `tenantWhere` en las 6 líneas siguientes.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tenantWhere(schema.branches" src/modules/finance/routes.ts` devuelve 3 o más
    - `grep -c "tenantWhere(schema.financialTransactions" src/modules/finance/routes.ts` devuelve 2 o más
    - Cada línea con `fastify.db` en el archivo tiene un `tenantWhere(` dentro de las 8 líneas siguientes (verificar con `grep -n -A8 "fastify.db" src/modules/finance/routes.ts`)
    - `pnpm exec tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>`finance/routes.ts` queda sin un solo acceso a base sin gimnasio.</done>
</task>

<task type="auto">
  <name>Task 3: queries directas de coach-load-routes.ts + lint de los dos archivos</name>
  <files>el-templo-api/src/modules/finance/coach-load-routes.ts</files>
  <read_first>
    - el-templo-api/src/modules/finance/coach-load-routes.ts L249-460 (hook con FINANCE_LOAD_ROLES y las resoluciones de socio/sede/suscripción/plan: 3 accesos a users, 3 a branches, 7 a subscriptions, 3 a subscription_plans)
    - el-templo-api/src/modules/finance/coach-load-routes.ts L974-1003 (el ÚNICO bloque ya migrado del archivo: `const ctx = assertTenant(...)` + tenantWhere sobre cash_registers)
  </read_first>
  <action>
    Agregar `tenantWhere` a todas las lecturas directas del archivo: `schema.users`,
    `schema.branches`, `schema.subscriptions`, `schema.subscriptionPlans` y cualquier
    acceso a tablas strict que quede. El coach es el rol MENOS privilegiado que toca
    finance: si acá se filtra, se filtra en el borde más barato de atacar.
    Reusar el `const ctx = assertTenant(request.scope, "coach-load.<acción>")` de cada
    handler; no resolver el ctx dos veces en el mismo handler.
    Cerrar generando `/tmp/allowlist-172-11.json` = allowlist sin las entradas de
    `src/modules/finance/routes.ts` (cash_registers, financial_transactions, branches) y de
    `src/modules/finance/coach-load-routes.ts` (branches, subscription_plans,
    subscriptions, users) — 7 entradas — y corriendo el lint contra esa copia.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit && pnpm exec tsx src/db/scripts/lint-tenant.ts --allowlist=/tmp/allowlist-172-11.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tenantWhere(schema.subscriptions" src/modules/finance/coach-load-routes.ts` devuelve 3 o más
    - `grep -c "tenantWhere(schema.users" src/modules/finance/coach-load-routes.ts` devuelve 1 o más
    - `/tmp/allowlist-172-11.json` tiene 7 entradas menos que `tenant-lint-allowlist.json` y el lint contra esa copia sale con código 0
    - `git -C /home/franco/projects/et-172 status --porcelain el-templo-api/tenant-lint-allowlist.json` no devuelve nada
    - `pnpm exec tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>Los dos archivos de rutas del módulo quedan limpios: cero entradas de allowlist pendientes para ellos.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                         | Description                                                 |
| ---------------------------------------------------------------- | ----------------------------------------------------------- |
| coach (rol mínimo de finance) → /api/admin/finance/coach-load/\* | el borde menos privilegiado con acceso a plata              |
| params del cliente → closures de scope                           | `cajaId` y `transactionId` llegan por path y deciden un 404 |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                               | Disposition | Mitigation Plan                                                                                                                             |
| ----------- | ---------------------- | ------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| T-172-11-01 | Information Disclosure | resolveCajaCountry / enforceCajaScope / enforceRowScope | mitigate    | el filtro de tenant entra ANTES de la decisión de scope de país; la respuesta sigue siendo 404 para no filtrar existencia                   |
| T-172-11-02 | Information Disclosure | resolución de socio/suscripción/plan en coach-load      | mitigate    | `tenantWhere` sobre users, subscriptions, subscription_plans y branches: el coach no puede autocompletar datos de un socio de otro gimnasio |
| T-172-11-03 | Tampering              | handlers que escriben con branchId del body             | mitigate    | la sede se valida con `tenantWhere(schema.branches, ctx)` antes de usarse en el cobro                                                       |
| T-172-SC    | Tampering              | instalación de paquetes                                 | accept      | este plan no instala nada                                                                                                                   |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit` verde.
- Lint verde contra la allowlist sin las 7 entradas de los dos archivos de rutas.
- Cero `code: 403` nuevos en las closures de scope.
</verification>

<success_criteria>
Los dos archivos de rutas de finance no tienen ningún acceso a base sin gimnasio, y el
contrato 404/vacío queda garantizado desde el guardián del módulo.
</success_criteria>

<output>
Create `.planning/phases/172-adopci-n-1-piloto-finance/172-11-SUMMARY.md` when done
</output>
