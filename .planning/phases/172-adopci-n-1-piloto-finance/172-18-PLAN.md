---
phase: 172-adopci-n-1-piloto-finance
plan: 18
type: execute
wave: 10
depends_on: ["172-17"]
files_modified:
  - el-templo-api/test/tenancy/iso-03-finance-transacciones.test.ts
autonomous: true
requirements: [ISO-03]
must_haves:
  truths:
    - "Las 13 rutas de transacciones, bandeja de pendientes y historial de movimientos tienen caso de aislamiento con control positivo"
    - "Cobrar, validar, observar, corregir y anular una transacción del otro gimnasio devuelve 404 y no modifica la fila"
    - "Los exports no filtran ni una fila del otro gimnasio"
  artifacts:
    - path: "el-templo-api/test/tenancy/iso-03-finance-transacciones.test.ts"
      provides: "batería ISO-03 para transactions, pending-tray y movements-history"
      min_lines: 300
  key_links:
    - from: "el-templo-api/test/tenancy/iso-03-finance-transacciones.test.ts"
      to: "test/fixtures/finance-gimnasio-dos.ts"
      via: "sembrarFinanzasGimnasioDos + tenantDeLaFila"
      pattern: "sembrarFinanzasGimnasioDos"
---

<objective>
Segundo archivo de la batería ISO-03: las 13 rutas de transacciones, bandeja de
pendientes, historial de movimientos y sus exports.

Purpose: son las rutas donde vive la plata que el staff toca todos los días y las que más
superficie de lectura tienen (listados con filtros libres del cliente). Un export que
filtre es la fuga más silenciosa del sistema.
Output: archivo verde con caso + control por ruta.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/172-adopci-n-1-piloto-finance/172-CONTEXT.md
@.planning/phases/172-adopci-n-1-piloto-finance/172-PATTERNS.md

Working dir: `/home/franco/projects/et-172`.
Contrato D-09: 404 / lista vacía, nunca 403.
</context>

<tasks>

<task type="auto">
  <name>Task 1: lecturas y exports (7 rutas GET)</name>
  <files>el-templo-api/test/tenancy/iso-03-finance-transacciones.test.ts</files>
  <read_first>
    - el-templo-api/test/tenancy/iso-03-finance-cajas.test.ts (la plantilla que dejó el plan 172-17: ciclo de vida, forma de los describe, mensajes de rojo)
    - el-templo-api/test/fixtures/finance-gimnasio-dos.ts (sembrarFinanzasGimnasioDos y tenantDeLaFila)
    - el-templo-api/test/tenant-manifest.ts L272-283 (GET /movements-history, /movements-history/export, /pending-tray, /pending-tray/export, /transactions, /transactions/export, /transactions/pending-misc/:memberId, /transactions/summary)
    - el-templo-api/src/modules/finance/routes.ts (query params de cada listado y forma de los exports)
  </read_first>
  <action>
    Crear `test/tenancy/iso-03-finance-transacciones.test.ts` con el mismo ciclo de vida y
    la misma forma de `describe` que `iso-03-finance-cajas.test.ts`.
    Un `describe` por ruta GET (7: transactions, transactions/export, transactions/summary,
    transactions/pending-misc/:memberId, pending-tray, pending-tray/export,
    movements-history — más movements-history/export en el Task 2 si conviene por tamaño),
    cada uno con:
    1. **Aislamiento:** el staff del gimnasio 2 llama la ruta con filtros ANCHOS (rango de
       fechas amplio, sin filtro de sede) y la respuesta no trae ninguna fila del gimnasio 1;
       para `/transactions/summary`, los totales tienen que ser los del gimnasio 2 (probar
       con montos distinguibles sembrados en cada gimnasio: si el resumen suma los dos, el
       número lo delata).
    2. **Control positivo:** el mismo actor SÍ ve sus propias filas.
    Para `pending-misc/:memberId`, pedir por el id de un socio del gimnasio 1 con el token
    del gimnasio 2 → lista vacía o 404, nunca las filas del socio ajeno.
    Los exports se afirman por CONTENIDO (ninguna fila ajena en el cuerpo), no solo por
    status.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit && pnpm exec vitest run test/tenancy/iso-03-finance-transacciones.test.ts --hookTimeout=250000</automated>
  </verify>
  <acceptance_criteria>
    - Al menos 7 `describe` de rutas GET, cada uno con `it` de aislamiento y `it` de control
    - El caso de `/transactions/summary` compara TOTALES contra montos sembrados distinguibles por gimnasio
    - Los casos de export afirman sobre el contenido devuelto, no solo sobre el status
    - `grep -c "toBe(403)" test/tenancy/iso-03-finance-transacciones.test.ts` devuelve 0
    - El archivo pasa entero
  </acceptance_criteria>
  <done>Ningún listado, resumen o export de transacciones puede mostrar plata del otro gimnasio.</done>
</task>

<task type="auto">
  <name>Task 2: escrituras sobre transacciones (6 rutas POST)</name>
  <files>el-templo-api/test/tenancy/iso-03-finance-transacciones.test.ts</files>
  <read_first>
    - el-templo-api/test/tenant-manifest.ts L306-321 (POST /transactions, /transactions/:id/correct, /transactions/:id/observe, /transactions/:id/validate, /transactions/:id/void, y movements-history/export)
    - el-templo-api/src/modules/finance/routes.ts (bodies esperados por cada POST y roles: FINANCE_WRITE_ROLES / FINANCE_VOID_ROLES / FINANCE_ADJUSTMENT_ROLES)
    - el-templo-api/test/fixtures/finance-gimnasio-dos.ts (transactionId del gimnasio 2 y tenantDeLaFila)
  </read_first>
  <action>
    Un `describe` por ruta de escritura. Cada uno con:
    1. **Aislamiento:** el staff del gimnasio 2 —con el rol MÍNIMO real que puede usar esa
       ruta (D-10)— intenta validar / observar / corregir / anular la transacción DEL
       GIMNASIO 1 por id → 404, y después se relee esa transacción con `tenantDeLaFila` +
       una consulta de su estado para probar que NO cambió (`validationStatus`, `voidedAt`
       y el saldo asociado siguen como estaban). El status HTTP solo no alcanza.
    2. **Control positivo:** la misma operación sobre la transacción PROPIA funciona.
    Para `POST /transactions`: crear un cobro como staff del gimnasio 2 apuntando a un
    `memberId` / `branchId` / `cajaId` DEL GIMNASIO 1 → tiene que fallar (404 sobre el
    recurso ajeno), y crear uno propio tiene que quedar con `tenant_id = TENANT_DOS` en
    `financial_transactions`, en su `transaction_links` y en la fila de `balances` que
    genera — las tres tablas verificadas con `tenantDeLaFila`.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit && pnpm exec vitest run test/tenancy/iso-03-finance-transacciones.test.ts --hookTimeout=250000</automated>
  </verify>
  <acceptance_criteria>
    - El archivo cubre las 13 rutas del grupo (lista ruta por ruta en el SUMMARY)
    - Al menos un caso verifica el `tenant_id` real de las TRES tablas que escribe un cobro (financial_transactions, transaction_links, balances)
    - Al menos un caso prueba que la transacción del gimnasio 1 quedó sin cambios después del intento ajeno
    - `grep -c "toBe(403)" test/tenancy/iso-03-finance-transacciones.test.ts` devuelve 0
    - El archivo pasa entero
  </acceptance_criteria>
  <done>Ninguna escritura sobre transacciones ajenas prospera, y el cobro propio queda estampado en las 3 tablas.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                 | Description                                       |
| -------------------------------------------------------- | ------------------------------------------------- |
| staff del gimnasio 2 con JWT válido → POST /transactions | puede elegir memberId, branchId y cajaId por body |
| filtros de listado y export → agregados de plata         | el cliente controla el rango; el gimnasio no      |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                             | Disposition | Mitigation Plan                                                                                                |
| ----------- | ---------------------- | ----------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------- |
| T-172-18-01 | Information Disclosure | listados, resumen y exports                           | mitigate    | casos con filtros anchos y montos distinguibles por gimnasio: si el filtro de tenant falla, el total lo delata |
| T-172-18-02 | Tampering              | validate/observe/correct/void sobre transacción ajena | mitigate    | 404 + relectura de la fila objetivo para probar que no cambió                                                  |
| T-172-18-03 | Elevation of Privilege | cobro apuntando a socio/sede/caja del otro gimnasio   | mitigate    | caso explícito que exige fallo, y verificación del tenant_id real en las 3 tablas del cobro propio             |
| T-172-SC    | Tampering              | instalación de paquetes                               | accept      | este plan no instala nada                                                                                      |

</threat_model>

<verification>
- Archivo verde con las 13 rutas cubiertas.
- Cero `403` esperados.
- Evidencia leída de la base en los casos de escritura.
</verification>

<success_criteria>
El corazón transaccional de finance queda probado ruta por ruta contra un segundo gimnasio.
</success_criteria>

<output>
Create `.planning/phases/172-adopci-n-1-piloto-finance/172-18-SUMMARY.md` when done
</output>
