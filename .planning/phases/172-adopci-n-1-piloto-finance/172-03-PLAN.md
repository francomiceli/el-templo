---
phase: 172-adopci-n-1-piloto-finance
plan: 03
type: execute
wave: 2
depends_on: ["172-01"]
files_modified:
  - el-templo-api/src/modules/reports/service.ts
  - el-templo-api/src/modules/reports/routes.ts
autonomous: true
requirements: [ADO-01]
must_haves:
  truths:
    - "El historial de cobros, el reporte de deuda y la gestión de deuda solo ven filas del gimnasio del staff logueado"
    - "Los helpers privados que devuelven fragmentos SQL reciben el ctx y estampan el filtro de tenant adentro del fragmento"
    - "El INSERT/UPDATE de debt_management estampa tenant_id por tenantValues, nunca por payload"
  artifacts:
    - path: "el-templo-api/src/modules/reports/service.ts"
      provides: "getChargeHistory / getOutstandingBalances / getExpiredMembers / updateDebtManagement scopeados, incluidos los 6 helpers de SQL"
      contains: "tenantWhere(schema.balances"
  key_links:
    - from: "el-templo-api/src/modules/reports/routes.ts"
      to: "assertTenant"
      via: "call sites de charge-history, outstanding-balances, expired-members, debt-management y sus exports"
      pattern: "assertTenant\\(request\\.scope"
    - from: "el-templo-api/src/modules/reports/service.ts"
      to: "debt_management"
      via: "tenantValues en el insert de gestión de deuda"
      pattern: "tenantValues\\(ctx"
---

<objective>
Migrar los 11 accesos de `reports/service.ts` a las 4 tablas strict que toca
(`financial_transactions`, `transaction_links`, `balances`, `debt_management`), incluida
la parte difícil: 6 helpers privados que devuelven fragmentos `SQL`/`SQL[]` en vez de
ejecutar la query.

Purpose: es el archivo ajeno con más accesos (4 entradas de allowlist sobre tablas
strict) y el único que rompe la receta "ctx primero en el método que hace la query" —
el ctx tiene que viajar hasta el constructor del fragmento. Resolverlo acá deja el
patrón escrito para las fases 173-175.
Output: reports scopeado, con el ctx entrando por el borde (`assertTenant`) en cada
handler afectado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/172-adopci-n-1-piloto-finance/172-CONTEXT.md
@.planning/phases/172-adopci-n-1-piloto-finance/172-PATTERNS.md

Working dir: `/home/franco/projects/et-172`. `tenant-lint-allowlist.json` NO se edita
acá (dueño: plan 172-21); la verificación usa una copia filtrada en `/tmp`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: ctx hasta los helpers de SQL de deuda (buildOutstanding*, effectiveDebtStatusSQL, buildDebtOriginTxSubquery)</name>
  <files>el-templo-api/src/modules/reports/service.ts</files>
  <read_first>
    - el-templo-api/src/modules/shared/tenant.ts (firmas de tenantWhere/tenantValues/assertTenant; convención "tenantWhere primer término del and" y, en `sql` crudo, `WHERE tenant_id = ${ctx.tenantId}`)
    - el-templo-api/src/modules/reports/service.ts L760-1010 (buildDebtOriginTxSubquery L770 sobre transaction_links, effectiveDebtStatusSQL L816, buildOutstandingBaseConds L826, buildOutstandingStatusConds L907, buildOutstandingOrderBy L928, selectOutstandingRows L955 con el .from(schema.balances) de L990)
    - el-templo-api/src/modules/reports/service.ts L1126-1320 (getOutstandingBalances y sus 3 queries sobre balances en L1150, L1201, L1261)
  </read_first>
  <action>
    Agregar `ctx: TenantContext` como PRIMER parámetro de los 6 helpers privados
    (`buildDebtOriginTxSubquery`, `effectiveDebtStatusSQL`, `buildOutstandingBaseConds`,
    `buildOutstandingStatusConds`, `buildOutstandingOrderBy`, `selectOutstandingRows`) y
    del público `getOutstandingBalances`, propagándolo en cada llamada interna.
    En los helpers que devuelven `SQL[]` el filtro entra como PRIMER elemento del array
    devuelto: `tenantWhere(schema.balances, ctx)`. En los que devuelven un `SQL` armado a
    mano (subquery de `transaction_links` en L776, `effectiveDebtStatusSQL`), el filtro va
    escrito dentro del propio fragmento con la convención lockeada de `sql` crudo:
    `AND tenant_id = ${ctx.tenantId}` sobre el alias correspondiente (el sentinel busca el
    literal `tenant_id` en la zona de predicado del statement final).
    Las 3 queries de `getOutstandingBalances` (L1150, L1201, L1261) y la de
    `selectOutstandingRows` (L990) llevan `tenantWhere(schema.balances, ctx)` como primer
    término. Import por path directo desde `../shared/tenant`.
    Documentar en un comentario corto arriba de `buildOutstandingBaseConds` POR QUÉ el ctx
    entra al helper (el fragmento se compone en otra query: si el filtro no viaja adentro
    del fragmento, la query final queda sin scope) — ese comentario es la plantilla que
    copian las fases 173-175.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tenantWhere(schema.balances" src/modules/reports/service.ts` devuelve 4 o más
    - `grep -c "ctx.tenantId" src/modules/reports/service.ts` devuelve 1 o más (fragmentos de `sql` crudo)
    - `grep -nE "tenantId!|\?\? 1" src/modules/reports/service.ts` no devuelve líneas
    - `pnpm exec tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>El reporte de deuda no puede componer un fragmento SQL sin filtro de gimnasio.</done>
</task>

<task type="auto">
  <name>Task 2: charge history, expired members y gestión de deuda + call sites de ruta</name>
  <files>el-templo-api/src/modules/reports/service.ts, el-templo-api/src/modules/reports/routes.ts</files>
  <read_first>
    - el-templo-api/src/modules/reports/service.ts L450-545 (getChargeHistory con la query de L469 y el `sql` crudo de L510 sobre `financial_transactions ft`)
    - el-templo-api/src/modules/reports/service.ts L1319-1520 (getExpiredMembers y el `sql` crudo de L1497 sobre `financial_transactions fx`)
    - el-templo-api/src/modules/reports/service.ts L2342-2530 (exportChargeHistory L2353, exportOutstandingBalances L2388, updateDebtManagement L2414 con .from(schema.balances) L2424, .insert(schema.debtManagement) L2451 y .from(schema.debtManagement) L2476, buildChargeConditions L2531)
    - el-templo-api/src/modules/reports/routes.ts (call sites L145 charge-history, L320 outstanding-balances, L383 expired-members, L506 export charge-history, L734 export outstanding, L813 updateDebtManagement)
    - el-templo-api/src/modules/finance/routes.ts L1391-1413 (analog del assertTenant inline en el call site)
  </read_first>
  <action>
    Agregar `ctx: TenantContext` como PRIMER parámetro de `getChargeHistory`,
    `buildChargeConditions`, `getExpiredMembers`, `updateDebtManagement`,
    `exportChargeHistory` y `exportOutstandingBalances`, propagando a los delegados.
    Queries: `tenantWhere(schema.financialTransactions, ctx)` en L469 y en las condiciones
    que arma `buildChargeConditions`; `AND tenant_id = ${ctx.tenantId}` dentro de los `sql`
    crudos de L510 (alias `ft`) y L1497 (alias `fx`);
    `tenantWhere(schema.balances, ctx)` en L2424; `tenantWhere(schema.debtManagement, ctx)`
    en L2476 y en el UPDATE de gestión de deuda si existe; el INSERT de L2451 pasa por
    `tenantValues(ctx, { ... })` con el tenant DESPUÉS del spread (mitigación de
    mass-assignment 169-01) y SIN `as const` extra (hallazgo 169-07: `tenantValues` no
    ensancha literales, los enums de Drizzle compilan igual).
    En `reports/routes.ts` cada uno de los 6 call sites pasa
    `assertTenant(request.scope, "reports.<ruta>")` como primer argumento (etiquetas
    `reports.charge-history`, `reports.outstanding-balances`, `reports.expired-members`,
    `reports.export-charge-history`, `reports.export-outstanding`, `reports.debt-management`).
    Después generar con node `/tmp/allowlist-172-03.json` = allowlist sin las 4 entradas de
    `src/modules/reports/service.ts` sobre `balances`, `debt_management`,
    `financial_transactions` y `transaction_links`, y correr el lint contra esa copia.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit && pnpm exec tsx src/db/scripts/lint-tenant.ts --allowlist=/tmp/allowlist-172-03.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tenantValues(ctx" src/modules/reports/service.ts` devuelve 1 o más
    - `grep -c "tenantWhere(schema.debtManagement" src/modules/reports/service.ts` devuelve 1 o más
    - `grep -c "assertTenant(request.scope" src/modules/reports/routes.ts` devuelve 6 o más
    - `/tmp/allowlist-172-03.json` tiene exactamente 4 entradas menos que `tenant-lint-allowlist.json` y el lint contra esa copia sale con código 0
    - `git -C /home/franco/projects/et-172 status --porcelain el-templo-api/tenant-lint-allowlist.json` no devuelve nada
    - `pnpm exec tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>Historial de cobros, vencidos y gestión de deuda scopeados; las 4 entradas de allowlist de reports quedan muertas.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                      | Description                                                                      |
| --------------------------------------------- | -------------------------------------------------------------------------------- |
| staff del gimnasio A → /api/admin/reports/\*  | filtros de sede/estado vienen del cliente; el gimnasio sale del server           |
| helper privado que devuelve SQL → query final | un fragmento sin filtro se compone en una query que parece scopeada y no lo está |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                                                      | Disposition | Mitigation Plan                                                                                                                                |
| ----------- | ---------------------- | ------------------------------------------------------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| T-172-03-01 | Information Disclosure | buildOutstandingBaseConds / effectiveDebtStatusSQL / buildDebtOriginTxSubquery | mitigate    | el ctx entra al helper y el filtro se escribe DENTRO del fragmento devuelto; comentario explicando el porqué como plantilla para 173-175       |
| T-172-03-02 | Tampering              | updateDebtManagement (INSERT/UPDATE sobre debt_management)                     | mitigate    | `tenantValues(ctx, {...})` con tenant después del spread + `tenantWhere` en la lectura previa: no se puede gestionar la deuda de otro gimnasio |
| T-172-03-03 | Information Disclosure | `sql` crudo de getChargeHistory (alias ft) y getExpiredMembers (alias fx)      | mitigate    | `AND tenant_id = ${ctx.tenantId}` dentro del fragmento; el sentinel exige el literal en la zona de predicado                                   |
| T-172-SC    | Tampering              | instalación de paquetes                                                        | accept      | este plan no instala nada                                                                                                                      |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit` verde.
- Lint verde contra la allowlist filtrada (4 entradas menos).
- Cero `tenantId!` / `?? 1` en los dos archivos.
</verification>

<success_criteria>
Los tres reportes de plata de `reports` (cobros, deuda, vencidos) y la gestión de deuda
quedan aislados por gimnasio, con el patrón "ctx dentro del fragmento SQL" documentado.
</success_criteria>

<output>
Create `.planning/phases/172-adopci-n-1-piloto-finance/172-03-SUMMARY.md` when done
</output>
