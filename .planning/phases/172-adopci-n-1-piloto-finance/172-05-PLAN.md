---
phase: 172-adopci-n-1-piloto-finance
plan: 05
type: execute
wave: 2
depends_on: ["172-01"]
files_modified:
  - el-templo-api/src/scripts/snapshot-finance-endpoints.ts
autonomous: false
requirements: [ADO-01]
must_haves:
  truths:
    - "Existe un script versionado que golpea los agregadores de finance con un rango de fechas FIJO y guarda el JSON normalizado"
    - "Existe el snapshot ANTES del deploy de la fase en staging (y DESPUÉS del deploy de CR-CAJA, D-13), guardado fuera del repo"
    - "Dos corridas seguidas contra los mismos datos dan un diff vacío (el script es determinístico)"
  artifacts:
    - path: "el-templo-api/src/scripts/snapshot-finance-endpoints.ts"
      provides: "captura y diff de los agregadores de finance"
      contains: "transactions/summary"
    - path: "$HOME/.el-templo-snapshots/172/antes.json"
      provides: "línea de base de los números del staff antes de la migración"
  key_links:
    - from: "el-templo-api/src/scripts/snapshot-finance-endpoints.ts"
      to: "GET /api/admin/finance/*"
      via: "fetch con token de staging"
      pattern: "/api/admin/finance/"
---

<objective>
Escribir la herramienta de D-12 ("mismos números") y capturar la línea de base en staging
ANTES de que la fase toque nada.

Purpose: el criterio 4 de la fase es "el staff ve los mismos números". Sin una foto
tomada antes del deploy, la comparación después es imposible. El script queda versionado
para que las fases 173-175 lo reusen tal cual.
Output: script + `antes.json` capturado y guardado fuera del repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/172-adopci-n-1-piloto-finance/172-CONTEXT.md
@.planning/phases/172-adopci-n-1-piloto-finance/172-PATTERNS.md

Working dir: `/home/franco/projects/et-172`. Los snapshots contienen plata real: NO se
commitean ni se guardan bajo `.planning/`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: script snapshot-finance-endpoints.ts</name>
  <files>el-templo-api/src/scripts/snapshot-finance-endpoints.ts</files>
  <read_first>
    - el-templo-api/src/scripts/dry-run-reassign-multibranch.ts L1-40 (forma del script a demanda: docblock con usage local y de servidor, NO cableado a ningún pipeline)
    - el-templo-api/test/tenant-manifest.ts L250-321 (nombres exactos de las rutas finance del manifiesto)
    - el-templo-api/src/modules/finance/routes.ts (query params que aceptan /transactions/summary, /cash-registers/balances, /pending-tray, /movements-history)
  </read_first>
  <action>
    Crear `src/scripts/snapshot-finance-endpoints.ts`: script a demanda, sin cablear a
    ningún pipeline, con dos modos por argv — `--out=<ruta>` (captura) y
    `--diff=<antes.json> <despues.json>` (comparación, exit 1 si difieren).
    Configuración por env: `SNAPSHOT_BASE_URL` (p. ej. la URL de la API de staging) y
    `SNAPSHOT_TOKEN` (JWT de un usuario admin/owner). Si falta cualquiera de los dos, sale
    con código 2 y el mensaje de usage — mismo contrato de "error de USO" que
    `failTenantArg`.
    Endpoints a golpear con GET, todos con el MISMO rango fijo histórico
    `dateFrom=2026-01-01` y `dateTo=2026-06-30` (rango cerrado, datos quietos):
    `/api/admin/finance/transactions/summary`, `/api/admin/finance/cash-registers/balances`,
    `/api/admin/finance/pending-tray`, `/api/admin/finance/movements-history`,
    `/api/admin/finance/transactions`, `/api/admin/finance/cost-centers/all`,
    `/api/admin/reports/outstanding-balances`.
    Normalización obligatoria para que el diff sea señal y no ruido: ordenar los arrays de
    resultados por `id` (o por la primera clave estable disponible), borrar campos
    volátiles (`generatedAt`, `requestId`, cualquier timestamp de respuesta) y serializar
    con claves ordenadas alfabéticamente e indentación de 2. Guardar un objeto
    `{ capturadoEn, baseUrl, rango, endpoints: { "<método> <path>": { status, body } } }`.
    El modo `--diff` compara los `endpoints` (no `capturadoEn`) e imprime la lista de
    endpoints que difieren con el primer path JSON divergente.
    Docblock arriba: por qué existe (D-12), por qué el rango es fijo, y que las fases
    173-175 lo reusan cambiando solo la lista de endpoints.
    Logging con `console` está bien acá: es un script CLI, no código de request (la regla
    de Pino aplica al server).
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit && pnpm exec tsx src/scripts/snapshot-finance-endpoints.ts; test $? -eq 2</automated>
  </verify>
  <acceptance_criteria>
    - Sin `SNAPSHOT_BASE_URL`/`SNAPSHOT_TOKEN` el script sale con código 2 e imprime el usage
    - `grep -c "/api/admin/finance/" src/scripts/snapshot-finance-endpoints.ts` devuelve 6 o más
    - `grep -c "2026-01-01" src/scripts/snapshot-finance-endpoints.ts` devuelve 1 o más
    - El script no está referenciado desde `package.json`, ni desde el runner de migraciones, ni desde el deploy (`grep -rn "snapshot-finance-endpoints" --include=*.json --include=*.yml .` solo lo encuentra en sí mismo)
    - `pnpm exec tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>Herramienta determinística lista para capturar y comparar los números del staff.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: capturar la línea de base en staging (ANTES del deploy de la fase)</name>
  <what-built>El script `src/scripts/snapshot-finance-endpoints.ts`, que captura los agregadores de finance con rango fijo y los normaliza para diff.</what-built>
  <how-to-verify>
    1. Conseguir un JWT de admin/owner de STAGING (login normal en el admin de staging) y
       la URL de la API de staging.
    2. Correr desde `/home/franco/projects/et-172/el-templo-api`:
       `SNAPSHOT_BASE_URL=<url> SNAPSHOT_TOKEN=<jwt> pnpm exec tsx src/scripts/snapshot-finance-endpoints.ts --out=$HOME/.el-templo-snapshots/172/antes.json`
    3. Correrlo una segunda vez a `/tmp/control.json` y diffear contra `antes.json`:
       `pnpm exec tsx src/scripts/snapshot-finance-endpoints.ts --diff=$HOME/.el-templo-snapshots/172/antes.json /tmp/control.json`
       Tiene que dar diff vacío — si no, el script todavía tiene ruido (timestamps, orden).
    4. Confirmar que `antes.json` tiene los 7 endpoints con status 200 y cuerpos no vacíos.
    5. IMPORTANTE: esta captura tiene que ser sobre el staging SIN el código de la fase 172
       y CON CR-CAJA ya desplegado (gate del plan 172-01 Task 1). Si CR-CAJA se deploya
       DESPUÉS de esta captura, el diff del plan 172-22 mezcla el cambio de caja con la
       migración de tenancy y deja de ser señal: en ese caso hay que recapturar la baseline
       después del deploy de CR-CAJA.
  </how-to-verify>
  <action>
    Pedirle a Franco el token de admin/owner de staging y la URL de la API, correr la
    captura a `$HOME/.el-templo-snapshots/172/antes.json`, correr la segunda captura de
    control y el `--diff`, y reportar el resultado. Sin el OK y el token no se sigue.
  </action>
  <done>`antes.json` capturado sobre el staging SIN el código de la fase, con diff de control vacío.</done>
  <resume-signal>Escribí "baseline capturada" (o pegá el error si algún endpoint no respondió 200)</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                              |
| ----------------------------- | ------------------------------------------------------------------------ |
| script local → API de staging | viaja un JWT de admin por env; el snapshot contiene plata real de socios |

## STRIDE Threat Register

| Threat ID   | Category               | Component                  | Disposition | Mitigation Plan                                                                                                 |
| ----------- | ---------------------- | -------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------- |
| T-172-05-01 | Information Disclosure | archivos de snapshot       | mitigate    | se guardan en `$HOME/.el-templo-snapshots/172/`, fuera del repo y fuera de `.planning/`; nunca se commitean     |
| T-172-05-02 | Information Disclosure | `SNAPSHOT_TOKEN`           | mitigate    | el token entra por env en la línea de comando del operador, no se persiste en el archivo de salida ni se loguea |
| T-172-05-03 | Denial of Service      | corridas contra producción | accept      | script a demanda, solo GETs, sin cableado a pipelines; el operador elige la baseUrl                             |
| T-172-SC    | Tampering              | instalación de paquetes    | accept      | usa `fetch` nativo de Node; cero dependencias nuevas                                                            |

</threat_model>

<verification>
- Script sin env → exit 2 con usage.
- Dos corridas seguidas contra staging → diff vacío.
- `antes.json` existe con los 7 endpoints en 200.
</verification>

<success_criteria>
Queda tomada, fuera del repo, la foto de los números del staff previa a la migración, con
una herramienta determinística y reusable por las fases 173-175.
</success_criteria>

<output>
Create `.planning/phases/172-adopci-n-1-piloto-finance/172-05-SUMMARY.md` when done
</output>
