---
phase: 172-adopci-n-1-piloto-finance
plan: 14
type: execute
wave: 8
depends_on: ["172-12", "172-11"]
files_modified:
  - el-templo-api/test/finance/coach-load.test.ts
  - el-templo-api/test/finance/coach-load-alta.test.ts
  - el-templo-api/test/finance/coach-load-pricing-gate.test.ts
  - el-templo-api/test/finance/movement-service.test.ts
  - el-templo-api/test/finance/transaction-service.test.ts
  - el-templo-api/test/finance/transactions-api.test.ts
  - el-templo-api/test/finance/summary-by-kind.test.ts
  - el-templo-api/test/finance/summary-sanity.test.ts
  - el-templo-api/test/finance/pending-tray.test.ts
  - el-templo-api/test/finance/mov-egresos-history.test.ts
  - el-templo-api/test/finance/validation-regression.test.ts
  - el-templo-api/test/finance/validation-state.test.ts
autonomous: true
requirements: [ADO-01]
must_haves:
  truths:
    - "Con finance en modo strict, los 12 archivos de test de transacciones y coach-load pasan sin tocar expectativas"
    - "Toda query directa de esos tests sobre las 6 tablas strict lleva tenant_id explícito"
  artifacts:
    - path: "el-templo-api/test/finance/coach-load.test.ts"
      provides: "seeds y aserciones de coach-load con tenant explícito"
      contains: "TENANT_TEMPLO"
  key_links:
    - from: "el-templo-api/test/finance"
      to: "sentinel en modo throw"
      via: "tenant_id presente en la zona de predicado de cada query de test"
      pattern: "TENANT_TEMPLO"
---

<objective>
Dejar listo para el throw el grupo más pesado de tests de finance: transacciones,
coach-load, bandeja, movimientos/egresos, resumen y validación (~180 queries directas
sobre las 6 tablas strict).

Purpose: sin esto, el plan del switch (172-21) encuentra la suite roja y no puede
distinguir un bug de migración de una query de fixture sin scope.
Output: 12 archivos verdes con strict encendido en caliente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/172-adopci-n-1-piloto-finance/172-CONTEXT.md
@.planning/phases/172-adopci-n-1-piloto-finance/172-PATTERNS.md

Working dir: `/home/franco/projects/et-172`. Tablas strict: `financial_transactions`,
`transaction_links`, `balances`, `cash_registers`, `cost_centers`, `debt_management`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: coach-load, transacciones y bandeja</name>
  <files>el-templo-api/test/finance/coach-load.test.ts, el-templo-api/test/finance/coach-load-alta.test.ts, el-templo-api/test/finance/coach-load-pricing-gate.test.ts, el-templo-api/test/finance/transaction-service.test.ts, el-templo-api/test/finance/transactions-api.test.ts, el-templo-api/test/finance/pending-tray.test.ts</files>
  <read_first>
    - el-templo-api/test/fixtures/second-tenant.ts L119 (TENANT_TEMPLO = 1) y L60-80 (por qué el 4º argumento de ensureEfectivoCaja no es opcional en la práctica)
    - el-templo-api/test/finance/coach-load.test.ts (el más pesado del grupo: ~24 queries directas)
    - el-templo-api/test/finance/transaction-service.test.ts (llamadas directas al service ya migradas por el plan 172-08 + queries de aserción)
    - el-templo-api/src/db/tenant-tables.ts L465-515 (dónde se enciende el strict en caliente para verificar, y que hay que revertirlo)
  </read_first>
  <action>
    Inventariar primero con
    `grep -nE "\.(from|into|insert|update|delete)\(\s*(schema\.)?(financialTransactions|transactionLinks|balances|cashRegisters|costCenters|debtManagement)|FROM (financial_transactions|transaction_links|balances|cash_registers|cost_centers|debt_management)" <archivos>`
    y anotar el conteo por archivo en el SUMMARY.
    Después, en cada sitio: `tenantWhere(schema.X, { tenantId: TENANT_TEMPLO })` sumado al
    `and(...)` de las lecturas, `tenantValues({ tenantId: TENANT_TEMPLO }, { ... })` en las
    escrituras, y `AND tenant_id = 1` en el predicado de los `sql` crudos. Importar
    `TENANT_TEMPLO` desde `../fixtures/second-tenant`.
    Prohibido cambiar expectativas y prohibido hardcodear ids de `users` (resolver
    `admin@test.com`).
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Ninguna línea con `.from(schema.financialTransactions)` / `schema.balances` / `schema.cashRegisters` / `schema.transactionLinks` en esos 6 archivos queda sin `tenantWhere` o `tenant_id` en las 8 líneas siguientes
    - `git diff -- el-templo-api/test/finance | grep -c "^[-+].*expect("` devuelve 0
    - `pnpm exec tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>Los tests de cobro y coach-load ya no dependen del default de tenant_id.</done>
</task>

<task type="auto">
  <name>Task 2: movimientos, resumen y validación + verificación con strict en caliente</name>
  <files>el-templo-api/test/finance/movement-service.test.ts, el-templo-api/test/finance/mov-egresos-history.test.ts, el-templo-api/test/finance/summary-by-kind.test.ts, el-templo-api/test/finance/summary-sanity.test.ts, el-templo-api/test/finance/validation-regression.test.ts, el-templo-api/test/finance/validation-state.test.ts</files>
  <read_first>
    - el-templo-api/test/finance/movement-service.test.ts (~20 queries directas, más llamadas a cashRegisterService.getBalance ya migradas)
    - el-templo-api/test/finance/summary-sanity.test.ts y summary-by-kind.test.ts (aserciones sobre los agregadores que compara el snapshot D-12)
    - el-templo-api/test/tenancy/con-05-sentinel.test.ts L280-390 (cómo se lee un throw del sentinel: hay que recorrer la cadena de `cause`)
  </read_first>
  <action>
    Mismo tratamiento en los 6 archivos restantes del grupo.
    Después, verificación en caliente: agregar TEMPORALMENTE la entrada `finance` con las 6
    tablas en `TENANT_STRICT_MODULES` (`src/db/tenant-tables.ts`), correr los 12 archivos
    del plan y REVERTIR (`git checkout -- src/db/tenant-tables.ts`) antes de commitear. Si
    algún archivo tira `DrizzleQueryError` con `TenantSentinelError` en `cause`, el mensaje
    trae el SQL exacto: arreglar esa query, nunca sacar la tabla de la lista strict.
    El commit de este plan NO incluye `src/db/tenant-tables.ts`.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit && pnpm exec vitest run test/finance --hookTimeout=900000</automated>
  </verify>
  <acceptance_criteria>
    - Con la entrada `finance` puesta a mano en `TENANT_STRICT_MODULES`, `pnpm exec vitest run test/finance --hookTimeout=900000` pasa entero (salida pegada en el SUMMARY)
    - `git -C /home/franco/projects/et-172 status --porcelain el-templo-api/src/db/tenant-tables.ts` no devuelve nada al terminar
    - `git diff -- el-templo-api/test/finance | grep -c "^[-+].*expect("` devuelve 0
    - `pnpm exec tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>Todo `test/finance/` sobrevive al modo throw sin cambiar una sola expectativa.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                                      |
| -------------------------------- | -------------------------------------------------------------------------------- |
| fixtures de test → tablas strict | los seeds escriben plata: si siembran en el tenant equivocado, los tests mienten |

## STRIDE Threat Register

| Threat ID   | Category    | Component                                  | Disposition | Mitigation Plan                                                                                                      |
| ----------- | ----------- | ------------------------------------------ | ----------- | -------------------------------------------------------------------------------------------------------------------- |
| T-172-14-01 | Spoofing    | seeds de coach-load y transacciones        | mitigate    | `tenantValues({ tenantId: TENANT_TEMPLO }, ...)` explícito en vez del default de columna                             |
| T-172-14-02 | Repudiation | falso verde por aserción sin scope         | mitigate    | las aserciones también filtran por tenant: una fila del gimnasio 2 ya no puede satisfacer un `expect` del gimnasio 1 |
| T-172-14-03 | Tampering   | apagar el sentinel para "arreglar" un rojo | mitigate    | prohibido explícitamente: se arregla la query, nunca se saca la tabla de la lista strict                             |
| T-172-SC    | Tampering   | instalación de paquetes                    | accept      | este plan no instala nada                                                                                            |

</threat_model>

<verification>
- `test/finance` entero verde con strict encendido en caliente.
- Sonda revertida.
- Cero expectativas modificadas.
</verification>

<success_criteria>
El grupo de tests más pesado del módulo deja de ser un bloqueante para el switch.
</success_criteria>

<output>
Create `.planning/phases/172-adopci-n-1-piloto-finance/172-14-SUMMARY.md` when done
</output>
