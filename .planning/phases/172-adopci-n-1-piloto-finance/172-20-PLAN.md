---
phase: 172-adopci-n-1-piloto-finance
plan: 20
type: execute
wave: 14
depends_on: ["172-18", "172-19"]
files_modified:
  - el-templo-api/test/tenancy/iso-03-cobertura.test.ts
  - el-templo-api/test/tenancy/iso-03-finance-cajas.test.ts
  - el-templo-api/test/tenancy/iso-03-finance-transacciones.test.ts
  - el-templo-api/test/tenancy/iso-03-finance-coach-load.test.ts
autonomous: true
requirements: [ISO-03]
must_haves:
  truths:
    - "Una ruta finance tenant-scoped nueva sin caso de aislamiento deja el CI rojo"
    - "Un caso que apunta a una ruta que ya no existe deja el CI rojo"
    - "El criterio de qué es 'ruta finance' sale del manifiesto, no de una lista escrita a mano"
  artifacts:
    - path: "el-templo-api/test/tenancy/iso-03-cobertura.test.ts"
      provides: "gate fail-closed bidireccional de la batería ISO-03"
      contains: "TENANT_MANIFEST"
  key_links:
    - from: "el-templo-api/test/tenancy/iso-03-cobertura.test.ts"
      to: "el-templo-api/test/tenant-manifest.ts"
      via: "derivación de las rutas finance tenant-scoped por prefijo + excepciones nombradas"
      pattern: "/api/admin/finance/"
---

<objective>
Cerrar ISO-03 con el gate de cobertura fail-closed que pidió D-08: toda ruta finance
`tenant-scoped` del manifiesto tiene que tener caso en la batería, y todo caso tiene que
apuntar a una ruta que existe.

Purpose: sin el gate, la batería envejece en silencio — se agrega una ruta de finance y
nadie se entera de que quedó sin probar. Es la misma mecánica del gate ISO-01 de la fase
171, que ya cazó dos rutas sin clasificar.
Output: gate verde y bidireccional, con la cabecera de la batería apuntando al doc de la
receta.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/172-adopci-n-1-piloto-finance/172-CONTEXT.md
@.planning/phases/172-adopci-n-1-piloto-finance/172-PATTERNS.md

Working dir: `/home/franco/projects/et-172`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: normalizar los nombres de describe a la clave del manifiesto</name>
  <files>el-templo-api/test/tenancy/iso-03-finance-cajas.test.ts, el-templo-api/test/tenancy/iso-03-finance-transacciones.test.ts, el-templo-api/test/tenancy/iso-03-finance-coach-load.test.ts</files>
  <read_first>
    - el-templo-api/test/tenant-manifest.ts L155-175 (forma de TENANT_MANIFEST: clave `"<MÉTODO> <url>"` y `{ categoria }`) y L250-321 (las 38 claves finance)
    - los tres archivos de la batería (nombres actuales de los describe)
  </read_first>
  <action>
    Ajustar el nombre de cada `describe` de ruta para que contenga LITERAL la clave del
    manifiesto (`"<MÉTODO> <url completa>"`, p. ej.
    `POST /api/admin/finance/transactions/:id/void`), manteniendo la prosa adelante con el
    separador ` — `. Es lo que hace posible un gate por texto sin duplicar la lista de
    rutas en un registro paralelo que se desincroniza.
    No cambiar ningún `it`, ningún `expect` ni la lógica: solo los strings de `describe`.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Cada `describe` de ruta contiene una clave que existe textualmente en `test/tenant-manifest.ts`
    - `git diff -- el-templo-api/test/tenancy | grep -c "^[-+].*expect("` devuelve 0
    - `pnpm exec tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>Los nombres de la batería son la fuente de verdad de qué ruta cubre cada caso.</done>
</task>

<task type="auto">
  <name>Task 2: gate bidireccional iso-03-cobertura.test.ts</name>
  <files>el-templo-api/test/tenancy/iso-03-cobertura.test.ts</files>
  <read_first>
    - el-templo-api/test/tenancy/iso-01-manifiesto.test.ts L110-266 (las 3 piezas a copiar: constante de baseline con docblock que explica que moverla es una decisión; `expect(lista).toEqual([])` con mensaje que NOMBRA lo que falta y dice QUÉ HACER y POR QUÉ IMPORTA; bidireccionalidad faltantes/fantasmas)
    - el-templo-api/test/tenant-manifest.ts (TENANT_MANIFEST y la categoría `tenant-scoped`)
    - .planning/phases/172-adopci-n-1-piloto-finance/172-PATTERNS.md sección 10 (por qué el criterio recomendado es prefijo `/api/admin/finance/` + lista NOMBRADA de excepciones — `GET /api/admin/analytics/advanced-finance` no matchea el prefijo)
  </read_first>
  <action>
    Crear `test/tenancy/iso-03-cobertura.test.ts` con:
    - `RUTAS_FINANCE`: derivadas de `TENANT_MANIFEST` = claves con `categoria === "tenant-scoped"`
      cuyo path arranca con `/api/admin/finance/`, MÁS una constante exportada
      `EXCEPCIONES_NOMBRADAS` con las rutas finance que no matchean el prefijo (hoy:
      `GET /api/admin/analytics/advanced-finance`), cada una con un comentario de por qué
      está en la lista.
    - `CASOS_ESCRITOS`: leer con `fs.readFileSync` los tres archivos
      `iso-03-finance-*.test.ts` y extraer las claves de ruta que aparecen en sus
      `describe`. Leer el fuente, NO importar los archivos (importarlos los ejecutaría).
    - `it("toda ruta finance tenant-scoped tiene caso de aislamiento")`:
      `expect(faltantes).toEqual([])` con mensaje que liste las rutas sin caso, diga QUÉ
      HACER (escribir el caso + control en el archivo de la familia que corresponda) y POR
      QUÉ IMPORTA (ISO-03 es el gate del milestone para onboardear el tenant 2).
    - `it("todo caso apunta a una ruta que existe en el manifiesto")`:
      `expect(fantasmas).toEqual([])` — casos que nombran rutas borradas o mal escritas.
    - Una constante `CASOS_BASELINE = 38` con docblock explicando que bajarla es una
      DECISIÓN DE DISEÑO, no un ajuste, y qué significa moverla.
    La cabecera del archivo apunta a `.docs/saas-multitenancy/07-receta-adopcion.md` (lo
    escribe el plan 172-23) y al requisito ISO-03.
    Este gate NO toca la base: tiene que correr sin provisioning (como iso-01).
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit && pnpm exec vitest run test/tenancy/iso-03-cobertura.test.ts --hookTimeout=250000</automated>
  </verify>
  <acceptance_criteria>
    - El gate pasa en verde con la batería actual y reporta 38 rutas cubiertas
    - Al comentar temporalmente un `describe` de una ruta, el gate se pone ROJO nombrando esa ruta (probarlo y revertir; dejar la evidencia en el SUMMARY)
    - Al renombrar temporalmente una ruta a una que no existe en el manifiesto, el gate se pone ROJO por `fantasmas` (probarlo y revertir)
    - `grep -c "EXCEPCIONES_NOMBRADAS" test/tenancy/iso-03-cobertura.test.ts` devuelve 1 o más
    - `git -C /home/franco/projects/et-172 status --porcelain el-templo-api/test/tenancy` no muestra restos de las pruebas de rojo
  </acceptance_criteria>
  <done>La batería no puede envejecer en silencio: ruta nueva sin caso = CI rojo.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                    | Description                                                             |
| ------------------------------------------- | ----------------------------------------------------------------------- |
| ruta finance nueva → batería de aislamiento | el riesgo no es el código de hoy sino el que se agregue mañana sin caso |

## STRIDE Threat Register

| Threat ID   | Category    | Component                                   | Disposition | Mitigation Plan                                                                                                          |
| ----------- | ----------- | ------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------ |
| T-172-20-01 | Repudiation | cobertura que envejece                      | mitigate    | gate fail-closed derivado del manifiesto: ruta sin caso = rojo, probado encendiendo el rojo a propósito                  |
| T-172-20-02 | Tampering   | bajar el baseline para "arreglar" el rojo   | mitigate    | `CASOS_BASELINE` con docblock que declara que moverlo es una decisión de diseño, igual que `ENTRADAS_BASELINE` de iso-01 |
| T-172-20-03 | Spoofing    | caso que dice cubrir una ruta que no existe | mitigate    | chequeo bidireccional de fantasmas                                                                                       |
| T-172-SC    | Tampering   | instalación de paquetes                     | accept      | este plan no instala nada                                                                                                |

</threat_model>

<verification>
- Gate verde con 38 rutas.
- Rojo demostrado en las dos direcciones (faltante y fantasma), y revertido.
</verification>

<success_criteria>
ISO-03 queda con cobertura verificable y auto-defendida para las fases 173-175.
</success_criteria>

<output>
Create `.planning/phases/172-adopci-n-1-piloto-finance/172-20-SUMMARY.md` when done
</output>
