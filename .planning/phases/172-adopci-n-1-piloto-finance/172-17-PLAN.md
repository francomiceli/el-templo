---
phase: 172-adopci-n-1-piloto-finance
plan: 17
type: execute
wave: 9
depends_on: ["172-13", "172-14", "172-15", "172-16"]
files_modified:
  - el-templo-api/test/fixtures/finance-gimnasio-dos.ts
  - el-templo-api/test/tenancy/iso-03-finance-cajas.test.ts
autonomous: true
requirements: [ISO-03]
must_haves:
  truths:
    - "Existe un fixture reusable que siembra caja, centro de costo, cuenta bancaria, transacción, link y saldo del gimnasio 2"
    - "Las 14 rutas de cajas y centros de costo tienen caso de aislamiento con su control positivo"
    - "El staff del gimnasio 2 recibe 404 o lista vacía sobre recursos del gimnasio 1, nunca 403"
  artifacts:
    - path: "el-templo-api/test/fixtures/finance-gimnasio-dos.ts"
      provides: "seeding de finanzas del gimnasio 2 + utilidades de evidencia (tenant_id real leído de la base)"
      exports: ["sembrarFinanzasGimnasioDos", "tenantDeLaFila"]
    - path: "el-templo-api/test/tenancy/iso-03-finance-cajas.test.ts"
      provides: "batería ISO-03 para cash-registers y cost-centers (14 rutas)"
      min_lines: 300
  key_links:
    - from: "el-templo-api/test/tenancy/iso-03-finance-cajas.test.ts"
      to: "test/fixtures/second-tenant.ts"
      via: "seedSecondTenant + limpiarSegundoGimnasio + TENANT_DOS"
      pattern: "seedSecondTenant"
---

<objective>
Arrancar la batería ISO-03: el fixture de finanzas del gimnasio 2 (reusable por los
planes 172-18 y 172-19) y los casos de las 14 rutas de cajas y centros de costo.

Purpose: ISO-03 es el requisito que el milestone usa como gate para onboardear el tenant 2. Esta batería es además la PLANTILLA que copian las fases 173-175, así que su forma
importa tanto como su cobertura.
Output: fixture + primer archivo de la batería, verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/172-adopci-n-1-piloto-finance/172-CONTEXT.md
@.planning/phases/172-adopci-n-1-piloto-finance/172-PATTERNS.md

Working dir: `/home/franco/projects/et-172`.
Contrato de respuesta cross-tenant (D-09, para TODO el milestone): GET by-id → 404,
listas → sin filas del otro gimnasio, escrituras sobre recursos ajenos → 404. NUNCA 403.
</context>

<tasks>

<task type="auto">
  <name>Task 1: fixture sembrarFinanzasGimnasioDos + utilidades de evidencia</name>
  <files>el-templo-api/test/fixtures/finance-gimnasio-dos.ts</files>
  <read_first>
    - el-templo-api/test/fixtures/second-tenant.ts (handle SegundoGimnasio L155-167: tenantId, branchId, activityId, adminId, adminToken, coachId, coachToken, socios, planId, scheduleId; L60-80 dice que la caja NO viene sembrada y deja la receta `ensureEfectivoCaja(app, gym2.branchId, "ARS", TENANT_DOS)`; L341-401 limpiarSegundoGimnasio FK-ordenado, que ya borra cash_registers)
    - el-templo-api/test/helpers.ts L321-350 (firma real de ensureEfectivoCaja: app, branchId, currency = "ARS", tenantId = 1 — el 4º argumento NO es opcional en la práctica) y createStaffUser
    - el-templo-api/test/tenancy/iso-02-fixtures.test.ts L90-165 (utilidades `consultar` y `tenantDeLaFila` con `sql.raw` sobre una UNIÓN CERRADA de literales, jamás un string libre)
    - el-templo-api/src/modules/finance/transaction-service.ts (forma de una transacción válida: kind, direction, validationStatus, links y saldo)
  </read_first>
  <action>
    Crear `test/fixtures/finance-gimnasio-dos.ts` que exporte:
    - `sembrarFinanzasGimnasioDos(app, gym2)`: crea para TENANT_DOS una caja de efectivo
      (vía `ensureEfectivoCaja(app, gym2.branchId, "ARS", TENANT_DOS)` — el 4º argumento
      SIEMPRE explícito), un centro de costo, una cuenta bancaria, y una transacción con su
      link y su fila de `balances`, todo estampado con `tenantValues({ tenantId: TENANT_DOS }, ...)`.
      Devuelve los ids (`cajaId`, `costCenterId`, `bankAccountId`, `transactionId`,
      `balanceId`) para que los casos los usen como "recurso del otro gimnasio".
    - `tenantDeLaFila(app, tabla, id)`: lee el `tenant_id` REAL de la base con `sql.raw`
      sobre una unión cerrada de literales = las 6 tablas strict
      (`financial_transactions`, `transaction_links`, `balances`, `cash_registers`,
      `cost_centers`, `debt_management`). Nunca acepta un `string` libre.
    - `sembrarFinanzasTemplo(app, ...)` si hace falta un recurso equivalente del gimnasio 1
      para los controles positivos, reusando los helpers existentes.
    Docblock arriba explicando el orden obligado (`cleanAllTestData` → `seedSecondTenant` →
    `sembrarFinanzasGimnasioDos`) y que `limpiarSegundoGimnasio` ya borra `cash_registers`
    del gimnasio 2 (no hace falta limpieza extra).
    No inventar ids de tenant nuevos: usar `TENANT_DOS` (90671) y `TENANT_TEMPLO` (1).
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TENANT_DOS" test/fixtures/finance-gimnasio-dos.ts` devuelve 3 o más
    - `grep -c "ensureEfectivoCaja(" test/fixtures/finance-gimnasio-dos.ts` devuelve 1 y la llamada pasa 4 argumentos
    - `grep -n "sql.raw" test/fixtures/finance-gimnasio-dos.ts` muestra el uso sobre un tipo unión de literales (no `string`)
    - `pnpm exec tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>Hay un gimnasio 2 con finanzas propias y una forma confiable de leer el tenant real de cada fila.</done>
</task>

<task type="auto">
  <name>Task 2: casos de LECTURA de cajas y centros de costo (6 rutas GET)</name>
  <files>el-templo-api/test/tenancy/iso-03-finance-cajas.test.ts</files>
  <read_first>
    - el-templo-api/test/tenancy/iso-02-fixtures.test.ts L196-240 (ciclo de vida obligado: beforeAll createTestApp, beforeEach cleanAllTestData → seedSecondTenant, afterAll cleanAllTestData + limpiarSegundoGimnasio + app.close; y el molde de mensaje de rojo con "qué significa y a qué archivo ir")
    - el-templo-api/test/tenancy/con-03-write-paths-tenant-id.test.ts L618-712 (un describe por ruta, nombre "«qué hace» — MÉTODO url", caso + control)
    - el-templo-api/test/tenant-manifest.ts L250-321 (las 38 rutas finance; en este archivo van GET /cash-registers, /cash-registers/balances, /cash-registers/balances/export, /cost-centers, /cost-centers/all, y el detalle por id si existe)
    - .planning/phases/172-adopci-n-1-piloto-finance/172-PATTERNS.md sección 10 (roles por ruta: FINANCE_READ_ROLES excluye coach; ADMIN_ROLES = admin/owner para el ABM)
  </read_first>
  <action>
    Crear `test/tenancy/iso-03-finance-cajas.test.ts` con la cabecera que apunta al doc
    `.docs/saas-multitenancy/07-receta-adopcion.md` (lo escribe el plan 172-23) y al
    requisito ISO-03.
    Un `describe` por ruta GET del grupo cajas/centros, cada uno con DOS `it`:
    1. **Aislamiento:** el staff del gimnasio 2 (`gym2.adminToken`, o el rol MÍNIMO real que
       puede usar esa ruta según D-10) llama la ruta y la respuesta NO contiene ningún id
       del gimnasio 1 (ni caja, ni centro, ni cuenta, ni transacción). Para el detalle por
       id de un recurso del gimnasio 1: status 404 (NUNCA 403, NUNCA 200).
    2. **Control positivo:** el mismo actor SÍ ve su propio recurso (el sembrado por
       `sembrarFinanzasGimnasioDos`). Sin este control, un 404 por seeding roto pasaría
       como aislamiento.
    Todo `expect` lleva segundo argumento explicando qué significa el valor equivocado y a
    qué archivo ir (molde de iso-02).
    Ciclo de vida exactamente como iso-02: `cleanAllTestData` ANTES de `seedSecondTenant`
    (el orden no es cosmético), y `afterAll` con `cleanAllTestData` +
    `limpiarSegundoGimnasio` + `app.close()` — obligatorio porque los archivos comparten
    base (`isolate: false`) y `branches` no está en `TABLES_TO_CLEAN`.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit && pnpm exec vitest run test/tenancy/iso-03-finance-cajas.test.ts --hookTimeout=250000</automated>
  </verify>
  <acceptance_criteria>
    - El archivo tiene al menos 6 `describe` nombrados `"<qué hace> — GET /api/admin/finance/..."`
    - Cada `describe` tiene un `it` de aislamiento y un `it` de control
    - `grep -c "toBe(403)" test/tenancy/iso-03-finance-cajas.test.ts` devuelve 0
    - `grep -c "toBe(404)" test/tenancy/iso-03-finance-cajas.test.ts` devuelve 1 o más
    - El archivo pasa entero
  </acceptance_criteria>
  <done>Ninguna lectura de cajas o centros de costo cruza gimnasios, y está probado ruta por ruta.</done>
</task>

<task type="auto">
  <name>Task 3: casos de ESCRITURA de cajas y centros de costo (8 rutas POST/PATCH)</name>
  <files>el-templo-api/test/tenancy/iso-03-finance-cajas.test.ts</files>
  <read_first>
    - el-templo-api/test/tenant-manifest.ts L284-306 (PATCH /cash-registers/:id, PATCH /cost-centers/:id, POST /cash-registers, /cash-registers/:id/close, /cash-registers/:id/reactivate, /cash-registers/efectivo, POST /cost-centers, /cost-centers/:id/deactivate, /cost-centers/:id/reactivate)
    - el-templo-api/src/modules/finance/routes.ts (los handlers de esas rutas, para saber qué body espera cada una)
    - el-templo-api/test/fixtures/finance-gimnasio-dos.ts (el fixture del Task 1)
  </read_first>
  <action>
    Agregar los `describe` de escritura. Cada uno con:
    1. **Aislamiento:** el staff del gimnasio 2 intenta modificar/cerrar/reactivar/desactivar
       el recurso DEL GIMNASIO 1 por id → 404, y —evidencia dura— la fila del gimnasio 1
       queda intacta: releer con `tenantDeLaFila` y comparar el campo que se intentó cambiar
       contra su valor original (no alcanza con mirar el status HTTP).
    2. **Control positivo:** el mismo actor SÍ puede hacer la operación sobre su propio
       recurso, y la fila resultante tiene `tenant_id = TENANT_DOS` leído de la base.
    Para las altas (`POST /cost-centers`, `POST /cash-registers`, `/cash-registers/efectivo`):
    crear como staff del gimnasio 2 y verificar con `tenantDeLaFila` que la fila nueva
    quedó estampada con `TENANT_DOS` aunque el body mande un `tenantId` distinto — ese es
    el caso de mass-assignment (T-169-02) aplicado a finance.
    Cerrar corriendo el archivo entero.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit && pnpm exec vitest run test/tenancy/iso-03-finance-cajas.test.ts --hookTimeout=250000</automated>
  </verify>
  <acceptance_criteria>
    - El archivo cubre las 14 rutas del grupo cajas/centros del manifiesto (lista en el SUMMARY, ruta por ruta)
    - Al menos un caso manda `tenantId` en el body y verifica con `tenantDeLaFila` que la fila quedó en `TENANT_DOS`
    - `grep -c "toBe(403)" test/tenancy/iso-03-finance-cajas.test.ts` devuelve 0
    - El archivo pasa entero con `--hookTimeout=250000`
    - `pnpm exec tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>Ninguna escritura sobre cajas o centros de costo puede tocar el gimnasio ajeno, probado con evidencia leída de la base.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                          | Description                                                              |
| ------------------------------------------------- | ------------------------------------------------------------------------ |
| staff autenticado del gimnasio 2 → API de finance | JWT válido, gimnasio ajeno: es exactamente el atacante que modela ISO-03 |
| body con tenantId → INSERT                        | mass-assignment: el cliente intenta elegir gimnasio                      |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                       | Disposition | Mitigation Plan                                                                                                 |
| ----------- | ---------------------- | ----------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------- |
| T-172-17-01 | Information Disclosure | GET de cajas/centros con token de otro gimnasio | mitigate    | caso por ruta que exige lista sin filas ajenas y 404 en el detalle; control positivo que descarta falsos verdes |
| T-172-17-02 | Tampering              | PATCH/close/reactivate sobre recurso ajeno      | mitigate    | además del 404, se relee la fila objetivo y se compara contra su valor original                                 |
| T-172-17-03 | Elevation of Privilege | tenantId en el body del alta                    | mitigate    | caso explícito que verifica el `tenant_id` REAL de la fila creada                                               |
| T-172-17-04 | Repudiation            | falso verde por seeding roto                    | mitigate    | cada caso de aislamiento va acompañado de su control positivo                                                   |
| T-172-SC    | Tampering              | instalación de paquetes                         | accept      | este plan no instala nada                                                                                       |

</threat_model>

<verification>
- `iso-03-finance-cajas.test.ts` verde con el fixture nuevo.
- Cero `403` esperados en la batería (contrato D-09).
- Evidencia leída de la base, no de la respuesta HTTP.
</verification>

<success_criteria>
Las 14 rutas de cajas y centros de costo quedan probadas ruta por ruta contra un segundo
gimnasio real, con la forma que van a copiar las fases 173-175.
</success_criteria>

<output>
Create `.planning/phases/172-adopci-n-1-piloto-finance/172-17-SUMMARY.md` when done
</output>
