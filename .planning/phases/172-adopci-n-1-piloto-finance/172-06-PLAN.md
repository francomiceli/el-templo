---
phase: 172-adopci-n-1-piloto-finance
plan: 06
type: execute
wave: 2
depends_on: ["172-01"]
files_modified:
  - el-templo-api/src/modules/finance/cash-register-service.ts
  - el-templo-api/src/modules/finance/routes.ts
  - el-templo-api/src/modules/finance/coach-load-routes.ts
  - el-templo-api/test/finance/cost-centers.test.ts
  - el-templo-api/test/finance/cost-centers-abm.test.ts
  - el-templo-api/test/finance/bank-accounts.test.ts
  - el-templo-api/test/finance/cash-balances.test.ts
autonomous: true
requirements: [ADO-01]
must_haves:
  truths:
    - "El ABM de centros de costo, cuentas bancarias y cajas de efectivo solo ve y escribe filas del gimnasio del staff logueado (D-01)"
    - "Todo INSERT sobre cash_registers y cost_centers estampa el tenant por tenantValues, nunca por payload"
    - "createEfectivoCaja queda migrado de verdad: su SELECT sobre cash_registers filtra por tenant (D-06)"
  artifacts:
    - path: "el-templo-api/src/modules/finance/cash-register-service.ts"
      provides: "cluster ABM (centros de costo, cuentas bancarias, cajas) con ctx primero y helpers en todo acceso"
      contains: "tenantWhere(schema.cashRegisters"
  key_links:
    - from: "el-templo-api/src/modules/finance/routes.ts"
      to: "cashRegisterService"
      via: "assertTenant en cada call site del ABM"
      pattern: "assertTenant\\(request\\.scope"
---

<objective>
Migrar el cluster ABM de `cash-register-service.ts` — centros de costo, cuentas
bancarias, cajas de efectivo y listado de cajas con saldo — que solo se llama desde
rutas y por lo tanto se puede cerrar entero (firma + queries) sin arrastrar a
`transaction-service`.

Purpose: es la mitad del archivo más caliente del módulo y la única que no está en el
ciclo de dependencias transaction-service ↔ subscriptions. Cerrarla acá saca trabajo del
camino crítico y valida la receta antes de los planes grandes.
Fuera de este plan (cluster B, plan 172-09): `resolveCashRegister`, `getBalance` y
`getPeriodMovement`, que se llaman desde `transaction-service`/`movement-service`.
Output: ABM de finance aislado por gimnasio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/172-adopci-n-1-piloto-finance/172-CONTEXT.md
@.planning/phases/172-adopci-n-1-piloto-finance/172-PATTERNS.md

Working dir: `/home/franco/projects/et-172`. La allowlist no se toca (plan 172-21).
`cash-register-service.ts` queda con entradas vivas hasta el plan 172-09: es esperado.
</context>

<tasks>

<task type="auto">
  <name>Task 1: centros de costo (8 métodos) con ctx y helpers</name>
  <files>el-templo-api/src/modules/finance/cash-register-service.ts, el-templo-api/src/modules/finance/routes.ts, el-templo-api/test/finance/cost-centers.test.ts, el-templo-api/test/finance/cost-centers-abm.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/shared/tenant.ts (firmas lockeadas; assertTenant único puente; tenantValues pone el tenant DESPUÉS del spread)
    - el-templo-api/src/modules/finance/cash-register-service.ts L431-620 (listActiveCostCenters L431, getCostCenterRow L462, assertUniqueName L490, createCostCenter L522, renameCostCenter L545, deactivateCostCenter L568, reactivateCostCenter L587, listAllCostCenters L603)
    - el-templo-api/src/modules/finance/cash-register-service.ts L782-803 (analog canónico de firma y de tenantWhere ya escrito en este mismo archivo)
    - el-templo-api/src/modules/finance/routes.ts L1211-1345 (call sites de cost-centers) y L203-214 (hook de módulo que ya deja request.scope disponible)
    - el-templo-api/test/finance/cost-centers.test.ts y cost-centers-abm.test.ts (call sites directos al service)
  </read_first>
  <action>
    `ctx: TenantContext` como PRIMER parámetro de los 8 métodos de centros de costo.
    Cada SELECT lleva `tenantWhere(schema.costCenters, ctx)` como PRIMER término del
    `and(...)`; `assertUniqueName` filtra por tenant ANTES de comparar el nombre (dos
    gimnasios pueden tener un centro con el mismo nombre — sin el filtro, el alta del
    gimnasio B choca contra el nombre del A); `createCostCenter` inserta con
    `tenantValues(ctx, { ... })`.
    En `routes.ts`, los 7 call sites del ABM de cost-centers pasan
    `assertTenant(request.scope, "finance.cost-centers.<acción>")` como primer argumento
    (patrón inline de L1391-1413; si el ctx se usa más de una vez en el handler, la
    variante con `const ctx = assertTenant(...)` de coach-load-routes L984).
    En los dos tests, los call sites directos al service pasan
    `{ tenantId: TENANT_TEMPLO }` importando `TENANT_TEMPLO` de `../fixtures/second-tenant`.
    No cambiar ninguna expectativa de esos tests: si una se pone roja, es un bug del cambio.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tenantWhere(schema.costCenters" src/modules/finance/cash-register-service.ts` devuelve 7 o más
    - `grep -c "tenantValues(ctx" src/modules/finance/cash-register-service.ts` devuelve 1 o más
    - `grep -nE "tenantId!|\?\? 1" src/modules/finance/cash-register-service.ts` no devuelve líneas
    - `pnpm exec tsc --noEmit` sale con código 0
    - Ninguna línea de `test/finance/cost-centers.test.ts` ni de `cost-centers-abm.test.ts` cambia un `expect(...)` (solo se agregan argumentos de ctx)
  </acceptance_criteria>
  <done>El ABM de centros de costo no cruza gimnasios ni colisiona nombres entre ellos.</done>
</task>

<task type="auto">
  <name>Task 2: cuentas bancarias y cajas de efectivo con ctx y helpers</name>
  <files>el-templo-api/src/modules/finance/cash-register-service.ts, el-templo-api/src/modules/finance/routes.ts, el-templo-api/src/modules/finance/coach-load-routes.ts, el-templo-api/test/finance/bank-accounts.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/finance/cash-register-service.ts L140-222 (listActiveBankAccounts L140, assertChosenBankAccount L173), L674-720 (getBankAccountRow), L719-1000 (createBankAccount L719, createEfectivoCaja L782 con el SELECT de cash_registers de L818-829 y el INSERT de L837, updateBankAccount L879, closeBankAccount L943, reactivateBankAccount L959, listBankAccounts L975)
    - el-templo-api/src/modules/finance/routes.ts L1376-1500 (call sites de bank accounts y de /cash-registers/efectivo)
    - el-templo-api/src/modules/finance/coach-load-routes.ts L360-375 y L930-945 (call sites de assertChosenBankAccount y listActiveBankAccounts)
    - .planning/phases/172-adopci-n-1-piloto-finance/172-PATTERNS.md (riesgo 3: "el método tiene ctx" ≠ "el método está migrado")
  </read_first>
  <action>
    `ctx: TenantContext` como PRIMER parámetro de `listActiveBankAccounts`,
    `assertChosenBankAccount`, `getBankAccountRow`, `createBankAccount`, `updateBankAccount`,
    `closeBankAccount`, `reactivateBankAccount` y `listBankAccounts`. `createEfectivoCaja`
    YA tiene el ctx: lo que falta ahí es `tenantWhere(schema.cashRegisters, ctx)` en el
    SELECT de L818-829 y `tenantValues(ctx, { ... })` en el INSERT de L837.
    Todas las queries sobre `schema.cashRegisters` de estos métodos llevan
    `tenantWhere(schema.cashRegisters, ctx)` como PRIMER término. Los INSERT/UPDATE pasan
    por `tenantValues` sin agregar `as const` (los enums de Drizzle —`type: "efectivo"`,
    `currency`— compilan igual, hallazgo 169-07).
    Call sites: en `routes.ts` los 7 del ABM bancario/cajas y en `coach-load-routes.ts`
    los 2 (`assertChosenBankAccount` L369 y `listActiveBankAccounts` L938) pasan
    `assertTenant(request.scope, "finance.bank-accounts.<acción>")` /
    `assertTenant(request.scope, "coach-load.bank-accounts")`.
    En `test/finance/bank-accounts.test.ts` los call sites directos reciben
    `{ tenantId: TENANT_TEMPLO }`.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tenantWhere(schema.cashRegisters" src/modules/finance/cash-register-service.ts` devuelve 8 o más
    - `sed -n '810,845p' src/modules/finance/cash-register-service.ts` muestra `tenantWhere(schema.cashRegisters` en el SELECT y `tenantValues(ctx` en el INSERT
    - `grep -c "assertTenant(request.scope" src/modules/finance/coach-load-routes.ts` devuelve 3 o más
    - `pnpm exec tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>Cuentas bancarias y cajas de efectivo aisladas; la trampa de `createEfectivoCaja` queda cerrada.</done>
</task>

<task type="auto">
  <name>Task 3: listActiveCajasWithBalance y getPeriodMovement + prueba dirigida</name>
  <files>el-templo-api/src/modules/finance/cash-register-service.ts, el-templo-api/src/modules/finance/routes.ts, el-templo-api/test/finance/cash-balances.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/finance/cash-register-service.ts L318-430 (getPeriodMovement L318 sobre cash_registers + financial_transactions, listActiveCajasWithBalance L374 sobre cash_registers + branches)
    - el-templo-api/src/modules/finance/routes.ts L1166 y L1518 (los 2 call sites de listActiveCajasWithBalance)
    - el-templo-api/test/finance/cash-balances.test.ts (assertions de saldo por caja)
  </read_first>
  <action>
    `ctx: TenantContext` primero en `listActiveCajasWithBalance` y `getPeriodMovement`.
    En `listActiveCajasWithBalance`: `tenantWhere(schema.cashRegisters, ctx)` primer
    término y ADEMÁS `tenantWhere(schema.branches, ctx)` en el join (D-06: adentro del
    módulo, archivos limpios — `branches` no entra a strict pero se scopea igual).
    En `getPeriodMovement`: `tenantWhere` sobre `cashRegisters` y sobre
    `financialTransactions` (las dos son strict y las dos aparecen en el statement).
    Call sites en `routes.ts` con `assertTenant(request.scope, "finance.cash-registers.balances")`.
    Correr dirigido el archivo de test de saldos para probar que los números no cambian:
    `pnpm exec vitest run test/finance/cash-balances.test.ts --hookTimeout=250000`
    (un archivo por vez; el provisioning de MySQL por worker tarda ~100 s). Está permitido
    por el alcance de esta fase: es verificación dirigida, no el suite completo.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit && pnpm exec vitest run test/finance/cash-balances.test.ts --hookTimeout=250000</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tenantWhere(schema.branches" src/modules/finance/cash-register-service.ts` devuelve 2 o más
    - `test/finance/cash-balances.test.ts` pasa entero, sin haber cambiado ninguna expectativa
    - `pnpm exec tsc --noEmit` sale con código 0
    - `git -C /home/franco/projects/et-172 status --porcelain el-templo-api/tenant-lint-allowlist.json` no devuelve nada
  </acceptance_criteria>
  <done>Saldos por caja scopeados y probados contra la suite real de saldos.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                     | Description                                                  |
| ------------------------------------------------------------ | ------------------------------------------------------------ |
| admin/owner del gimnasio A → ABM de cajas y centros de costo | ids de caja/centro/cuenta llegan por path o body del cliente |
| body del cliente → INSERT en cash_registers / cost_centers   | mass-assignment: un `tenantId` en el body no puede ganar     |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                                            | Disposition | Mitigation Plan                                                                                                                                            |
| ----------- | ---------------------- | -------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-172-06-01 | Information Disclosure | GET /cash-registers, /cost-centers, /cash-registers/balances         | mitigate    | `tenantWhere` primer término en cada SELECT: la caja del gimnasio B no matchea y el handler cae en su rama not-found (contrato D-09: 404/vacío, nunca 403) |
| T-172-06-02 | Tampering              | PATCH /cash-registers/:id, PATCH /cost-centers/:id, close/reactivate | mitigate    | el SELECT previo scopeado no encuentra la fila ajena → 404 antes de escribir                                                                               |
| T-172-06-03 | Elevation of Privilege | INSERT con tenantId venido del body                                  | mitigate    | `tenantValues(ctx, {...})` pisa cualquier tenantId spreadeado (169-01)                                                                                     |
| T-172-06-04 | Spoofing               | colisión de nombres de centro de costo entre gimnasios               | mitigate    | `assertUniqueName` filtra por tenant antes de comparar: el gimnasio B no aprende los nombres del A por el 409                                              |
| T-172-SC    | Tampering              | instalación de paquetes                                              | accept      | este plan no instala nada                                                                                                                                  |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit` verde.
- `test/finance/cash-balances.test.ts` verde sin tocar expectativas.
- Cero `tenantId!` / `?? 1` en los archivos tocados.
</verification>

<success_criteria>
El ABM de cajas, cuentas y centros de costo queda aislado por gimnasio y la trampa
documentada de `createEfectivoCaja` (ctx en la firma sin filtro en la query) queda cerrada.
</success_criteria>

<output>
Create `.planning/phases/172-adopci-n-1-piloto-finance/172-06-SUMMARY.md` when done
</output>
