---
phase: 172-adopci-n-1-piloto-finance
plan: 10
type: execute
wave: 6
depends_on: ["172-09"]
files_modified:
  - el-templo-api/src/modules/finance/transaction-service.ts
autonomous: true
requirements: [ADO-01]
must_haves:
  truths:
    - "Cobrar, anular, validar, observar y corregir solo alcanza filas del gimnasio del staff que ejecuta"
    - "Todo INSERT de transacción, link y saldo estampa el tenant por tenantValues"
    - "Una transacción de otro gimnasio es indistinguible de una inexistente (404), no 403 (D-09)"
  artifacts:
    - path: "el-templo-api/src/modules/finance/transaction-service.ts"
      provides: "caminos de escritura scopeados por helpers"
      contains: "tenantValues(ctx"
  key_links:
    - from: "el-templo-api/src/modules/finance/transaction-service.ts"
      to: "financial_transactions / transaction_links / balances"
      via: "tenantWhere primer término y tenantValues en cada write"
      pattern: "tenantWhere\\(schema\\.financialTransactions"
---

<objective>
Migrar las queries de los caminos de ESCRITURA de `transaction-service.ts`: `create`,
`void`/`voidPair`/`voidInTx`/`_void`, `validate`, `observe`, `correct`,
`findByIdempotencyKey` y `getById`.

Purpose: es el archivo con más accesos del repo (más de 200 referencias a tablas strict)
y el que ejecuta el cobro real. Se parte en dos planes (escrituras acá, lecturas en
172-12) porque una sola pasada por 2172 líneas con 200 sitios no entra en un solo
contexto sin degradar.
Las firmas ya tienen `ctx` desde el plan 172-08: este plan NO cambia ninguna firma.
Output: los caminos de escritura del cobro, aislados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/172-adopci-n-1-piloto-finance/172-CONTEXT.md
@.planning/phases/172-adopci-n-1-piloto-finance/172-PATTERNS.md

Working dir: `/home/franco/projects/et-172`. La allowlist no se toca (dueño: plan 172-21);
`transaction-service.ts` sale de ella recién cuando termine el plan 172-12.
</context>

<tasks>

<task type="auto">
  <name>Task 1: create, findByIdempotencyKey y getById</name>
  <files>el-templo-api/src/modules/finance/transaction-service.ts</files>
  <read_first>
    - el-templo-api/src/modules/shared/tenant.ts (tenantValues pone el tenant DESPUÉS del spread; no ensancha tipos literales — los enums `direction`, `kind`, `validationStatus` compilan sin `as const`)
    - el-templo-api/src/modules/finance/transaction-service.ts L158-375 (create: 6 accesos a financial_transactions, 3 a transaction_links, 3 a balances, más users/branches/subscriptions/program_enrollments)
    - el-templo-api/src/modules/finance/transaction-service.ts L955-1005 (findByIdempotencyKey y getById)
    - el-templo-api/src/modules/finance/cash-register-service.ts L782-830 (analog ya migrado en el módulo)
  </read_first>
  <action>
    En cada query de esos 3 métodos: `tenantWhere(<tabla>, ctx)` como PRIMER término del
    `and(...)` para `schema.financialTransactions`, `schema.transactionLinks` y
    `schema.balances`, y también para las tablas NO strict que este archivo toca
    (`schema.users`, `schema.branches`, `schema.subscriptions`,
    `schema.programEnrollments`, `schema.subscriptionPlans`, `schema.userStatusHistory`) —
    D-06 exige que el archivo quede sin ninguna entrada de allowlist, no solo sin las de
    tablas strict.
    Todo `insert(...).values(...)` pasa por `tenantValues(ctx, { ... })` sin agregar
    `as const`. En los `sql` crudos, si los hubiera, el filtro se escribe como
    `tenant_id = ${ctx.tenantId}` dentro del predicado.
    No cambiar lógica: ni condiciones de negocio, ni orden de operaciones, ni mensajes de
    error. Si una query legítimamente NO debe scopearse, NO se agrega entrada a la
    allowlist: se escribe la exención `tenant-safe: <motivo>` en un comentario de BLOQUE
    aparte (nunca anidado en un `/** */`) anclado al sitio del acceso — en finance no se
    espera ninguna.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '158,375p' src/modules/finance/transaction-service.ts | grep -c "tenantWhere("` devuelve 8 o más
    - `grep -c "tenantValues(ctx" src/modules/finance/transaction-service.ts` devuelve 3 o más
    - `sed -n '955,1005p' src/modules/finance/transaction-service.ts | grep -c "tenantWhere("` devuelve 3 o más
    - `git diff -- el-templo-api/src/modules/finance/transaction-service.ts | grep -c "^[-+].*throw new"` devuelve 0 (no se tocó ningún error)
    - `pnpm exec tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>El alta de cobro y las búsquedas por id/idempotencia no pueden cruzar gimnasios.</done>
</task>

<task type="auto">
  <name>Task 2: void, voidPair, voidInTx, _void y correct</name>
  <files>el-templo-api/src/modules/finance/transaction-service.ts</files>
  <read_first>
    - el-templo-api/src/modules/finance/transaction-service.ts L375-634 (void L375, voidPair L408, voidInTx L435, _void L459 con la llamada al canceller en L522: 6 accesos a financial_transactions, 4 a transaction_links, 5 a users, 1 a user_status_history)
    - el-templo-api/src/modules/finance/transaction-service.ts L826-955 (correct: 2 accesos a financial_transactions, 5 a transaction_links)
  </read_first>
  <action>
    Mismo tratamiento: `tenantWhere` primer término en cada SELECT/UPDATE sobre
    `financial_transactions`, `transaction_links`, `balances`, `users` y
    `user_status_history`; `tenantValues(ctx, { ... })` en los INSERT (la transacción de
    corrección y los links nuevos).
    Ojo con el UPDATE de anulación: el `where` por `id` DEBE llevar además el
    `tenantWhere` — sin él, anular por id ajeno queda posible aunque el SELECT previo
    estuviera scopeado (defensa en profundidad, no solo el read).
    Todo lo que corre dentro de un `TxHandle` sigue usando el `tx` recibido.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '375,640p' src/modules/finance/transaction-service.ts | grep -c "tenantWhere("` devuelve 10 o más
    - `sed -n '826,955p' src/modules/finance/transaction-service.ts | grep -c "tenantWhere("` devuelve 5 o más
    - Todo `.update(schema.financialTransactions)` del rango tiene `tenantWhere` en su `where` (revisar con `grep -n -A6 ".update(schema.financialTransactions)" src/modules/finance/transaction-service.ts`)
    - `pnpm exec tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>Anular y corregir exige que la transacción sea del propio gimnasio, en la lectura y en la escritura.</done>
</task>

<task type="auto">
  <name>Task 3: validate y observe + pruebas dirigidas de validación</name>
  <files>el-templo-api/src/modules/finance/transaction-service.ts</files>
  <read_first>
    - el-templo-api/src/modules/finance/transaction-service.ts L634-826 (validate L634 con 6 accesos a financial_transactions, 5 a cash_registers y 2 a transaction_links; observe L750)
    - el-templo-api/test/finance/validation-state.test.ts y test/finance/validate-caja.test.ts (qué prueban hoy: máquina de estados de validación y elección de caja)
  </read_first>
  <action>
    `tenantWhere` primer término en cada query de `validate` y `observe`, incluidas las 5
    sobre `cash_registers` (validar contra una caja de otro gimnasio es el camino más
    directo a corromper un arqueo) y el UPDATE de estado de validación.
    Cerrar corriendo dirigidos los dos archivos de test de validación, que son los que
    ejercitan esta máquina de estados.
  </action>
  <verify>
    <automated>cd /home/franco/projects/et-172/el-templo-api && pnpm exec tsc --noEmit && pnpm exec vitest run test/finance/validation-state.test.ts --hookTimeout=250000 && pnpm exec vitest run test/finance/validate-caja.test.ts --hookTimeout=250000</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '634,826p' src/modules/finance/transaction-service.ts | grep -c "tenantWhere("` devuelve 8 o más
    - `test/finance/validation-state.test.ts` y `test/finance/validate-caja.test.ts` pasan enteros sin cambios en sus `expect(...)`
    - `pnpm exec tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>Validación y observación de cobros aisladas, probadas contra la suite real de validación.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                             | Description                                               |
| ---------------------------------------------------- | --------------------------------------------------------- |
| body/params del staff → create/void/validate/correct | ids de transacción, caja, socio y sede llegan del cliente |
| INSERT de transacción → financial_transactions       | mass-assignment: el body se spreadea hasta el values      |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                                       | Disposition | Mitigation Plan                                                                                                          |
| ----------- | ---------------------- | --------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------ |
| T-172-10-01 | Tampering              | UPDATE de anulación y de estado de validación                   | mitigate    | `tenantWhere` también en el `where` del UPDATE, no solo en el SELECT previo: defensa en profundidad                      |
| T-172-10-02 | Elevation of Privilege | INSERT en financial_transactions / transaction_links / balances | mitigate    | `tenantValues(ctx, {...})` con el tenant después del spread                                                              |
| T-172-10-03 | Information Disclosure | getById / findByIdempotencyKey de una transacción ajena         | mitigate    | `tenantWhere` hace que la fila no matchee y el método devuelve null → el handler responde 404 (contrato D-09, nunca 403) |
| T-172-10-04 | Tampering              | validate contra una caja de otro gimnasio                       | mitigate    | `tenantWhere(schema.cashRegisters, ctx)` en las 5 queries de caja de `validate`                                          |
| T-172-SC    | Tampering              | instalación de paquetes                                         | accept      | este plan no instala nada                                                                                                |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit` verde.
- `validation-state` y `validate-caja` verdes sin tocar expectativas.
- Cero entradas nuevas en la allowlist (el archivo no se toca).
</verification>

<success_criteria>
Ningún camino de escritura de plata en `transaction-service.ts` puede leer o escribir una
fila de otro gimnasio.
</success_criteria>

<output>
Create `.planning/phases/172-adopci-n-1-piloto-finance/172-10-SUMMARY.md` when done
</output>
