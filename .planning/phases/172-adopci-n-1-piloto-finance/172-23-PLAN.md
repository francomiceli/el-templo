---
phase: 172-adopci-n-1-piloto-finance
plan: 23
type: execute
wave: 17
depends_on: ["172-22"]
files_modified: []
autonomous: true
requirements: [ADO-01, ISO-03]
must_haves:
  truths:
    - "Existe la receta de adopción escrita con lo aprendido en el piloto, lista para que 173-175 la ejecuten sin re-litigar"
    - "La receta nombra las trampas concretas que aparecieron en finance, no generalidades"
  artifacts:
    - path: "/home/franco/projects/el-templo/.docs/saas-multitenancy/07-receta-adopcion.md"
      provides: "receta repetible de adopción por módulo"
      min_lines: 120
  key_links:
    - from: "el-templo-api/test/tenancy/iso-03-cobertura.test.ts"
      to: ".docs/saas-multitenancy/07-receta-adopcion.md"
      via: "cabecera de la batería que apunta al doc"
      pattern: "07-receta-adopcion"
---

<objective>
Escribir `.docs/saas-multitenancy/07-receta-adopcion.md` (D-11): la receta completa de
adopción de un módulo, destilada de lo que realmente pasó en el piloto de finance.

Purpose: la fase 172 no vale solo por finance — vale porque convierte las 5 capas en un
procedimiento repetible. Las fases 173-175 cargan este doc como referencia canónica y no
vuelven a discutir el orden ni la forma.
Output: doc nuevo en el checkout principal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/172-adopci-n-1-piloto-finance/172-CONTEXT.md
@.planning/phases/172-adopci-n-1-piloto-finance/172-PATTERNS.md

**⚠️ `.docs/` NO está versionado.** Vive solo en el checkout principal
`/home/franco/projects/el-templo/.docs/`; un worktree no la trae. Este doc se ESCRIBE en
el checkout principal y NO entra en ningún commit. Es la única excepción a "no tocar el
checkout principal" de esta fase, y es un archivo untracked: no se hace `git add`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: escribir la receta con lo aprendido</name>
  <files>/home/franco/projects/el-templo/.docs/saas-multitenancy/07-receta-adopcion.md</files>
  <read_first>
    - /home/franco/projects/el-templo/.docs/saas-multitenancy/06-estrategia-migracion.md (analog de formato: cabecera con fecha y estado, secciones numeradas)
    - /home/franco/projects/el-templo/.docs/saas-multitenancy/03-diseno-tenant-db-layer.md §3 (las 5 capas: la adopción es la capa 5 sobre las 4 construidas)
    - .planning/phases/172-adopci-n-1-piloto-finance/172-01-SUMMARY.md a 172-22-SUMMARY.md (TODOS: de ahí sale el "lo aprendido" real, no el plan teórico)
  </read_first>
  <action>
    Escribir el doc con esta estructura, llenándola con hechos del piloto (números y
    nombres de archivo concretos, no prosa genérica):
    1. **Cuándo un módulo está listo para adoptarse** — precondiciones: las 4 capas en
       master, manifiesto actualizado, fixtures 2-tenant disponibles.
    2. **El orden que funciona y por qué** — inventario (allowlist + `SENTINEL_INVENTORY=1`)
       → cirugía mínima en archivos ajenos → plomería de firmas → migración de queries →
       endurecimiento de tests → batería ISO-03 → gate de cobertura → switch strict → verificación
       de números. Explicar por qué el switch va ÚLTIMO (D-03: la suite nunca queda roja).
    3. **Cómo romper los ciclos de dependencia entre services** — el caso real
       transaction-service ↔ subscriptions: se corta dando ctx primero al llamador que NO
       depende del otro, y separando "plomería de firmas" de "migración de queries".
    4. **Las trampas que costaron tiempo en el piloto** — (a) un método con `ctx` en la
       firma NO está migrado (`createEfectivoCaja`); (b) los helpers privados que devuelven
       fragmentos `SQL` necesitan el filtro ADENTRO del fragmento (`reports/service.ts`,
       `buildListConditions`); (c) las closures de rutas también hacen queries
       (`resolveCajaCountry`); (d) los ~250 accesos directos de los TESTS también los juzga
       el sentinel, incluida la limpieza global de `cleanAllTestData` (que se resuelve con
       exención `tenant-safe` en el SQL, no con allowlist); (e) el throw llega envuelto en
       `DrizzleQueryError.cause`; (f) las rutas públicas sin `request.scope` derivan el ctx
       de una fila del server (`auth/routes.ts` con la sede).
    5. **La forma de la batería ISO-03** — un archivo por familia de rutas, un `describe`
       por ruta con el nombre = clave del manifiesto, caso de aislamiento + control
       positivo, evidencia leída de la base (`tenantDeLaFila`), rol mínimo real (D-10),
       contrato 404/vacío (D-09) y el gate de cobertura fail-closed bidireccional.
    6. **Checklist de cierre copiable** — allowlist en cero para el módulo, entrada en
       `TENANT_STRICT_MODULES`, gate de forma actualizado, demo fail-closed con salida real,
       CI verde con suite completo, diff de snapshot vacío, UAT del staff.
    7. **Costo real del piloto** — cantidad de planes, archivos tocados, entradas de
       allowlist borradas (47), rutas cubiertas (38), y qué se subestimó (el trabajo sobre
       `test/`), para que 173-175 dimensionen mejor.
    Cabecera con fecha y estado, igual que `06-estrategia-migracion.md`.
  </action>
  <verify>
    <automated>test -f /home/franco/projects/el-templo/.docs/saas-multitenancy/07-receta-adopcion.md && wc -l /home/franco/projects/el-templo/.docs/saas-multitenancy/07-receta-adopcion.md</automated>
  </verify>
  <acceptance_criteria>
    - El archivo existe en `/home/franco/projects/el-templo/.docs/saas-multitenancy/` y tiene 120 líneas o más
    - Tiene las 7 secciones numeradas de la estructura pedida
    - Nombra al menos 6 archivos concretos del repo como ejemplo (transaction-service.ts, reports/service.ts, cash-register-service.ts, helpers.ts, tenant-tables.ts, iso-03-cobertura.test.ts)
    - Incluye los números reales del piloto: 47 entradas borradas, 38 rutas cubiertas, cantidad de planes
    - `git -C /home/franco/projects/el-templo status --porcelain .docs/` no muestra el archivo como staged (es untracked y así queda)
  </acceptance_criteria>
  <done>La receta está escrita con lo que de verdad pasó, no con lo que se planeó.</done>
</task>

<task type="auto">
  <name>Task 2: cerrar los cabos sueltos de la fase</name>
  <files>ninguno versionado</files>
  <read_first>
    - .planning/phases/172-adopci-n-1-piloto-finance/172-21-SUMMARY.md y 172-22-SUMMARY.md
    - .claude/skills/el-templo-change-control/SKILL.md (qué queda pendiente entre staging y master)
  </read_first>
  <action>
    Dejar registrado en el SUMMARY del plan, para que quede en el estado del proyecto:
    - Estado de la rama `feat/172-adopcion-finance` (en staging, pendiente de tren a master).
    - Que el worktree `/home/franco/projects/et-172` queda vivo hasta el UAT en producción.
    - Que la fase NO aplicó migraciones (tope de numeración sin cambios; la próxima fase
      sigue reservando desde donde indique el estado del milestone).
    - Los pendientes heredados: `aura_balances`/`aura_transactions` siguen fuera de strict
      (gamification), el resto de analytics/reports/subscriptions/members/coach se migra en
      173-175, y el `DEFAULT 1` de `tenant_id` sigue en pie.
    - Punteros: el doc 07 recién escrito y el gate `iso-03-cobertura.test.ts` como el lugar
      donde se agregan las rutas nuevas.
    No tocar código.
  </action>
  <verify>
    <automated>test -f /home/franco/projects/el-templo/.planning/phases/172-adopci-n-1-piloto-finance/172-23-SUMMARY.md || echo "el SUMMARY lo escribe este plan al cerrar"</automated>
  </verify>
  <acceptance_criteria>
    - El SUMMARY lista el estado de la rama, del worktree y los 4 pendientes heredados
    - El SUMMARY nombra explícitamente que no hubo migraciones en la fase
    - No hay cambios de código en el diff de este plan
  </acceptance_criteria>
  <done>La fase queda cerrada con el estado y los pendientes escritos donde el próximo agente los va a leer.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                      | Description                                                          |
| --------------------------------------------- | -------------------------------------------------------------------- |
| doc no versionado → conocimiento del proyecto | `.docs/` vive solo en un checkout: si se pierde, se pierde la receta |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                             | Disposition | Mitigation Plan                                                                                         |
| ----------- | ---------------------- | ----------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------- |
| T-172-23-01 | Repudiation            | receta escrita como teoría en vez de como experiencia | mitigate    | el doc se escribe DESPUÉS del cierre, leyendo los 22 SUMMARY, y exige nombrar archivos y números reales |
| T-172-23-02 | Information Disclosure | doc con datos de socios                               | accept      | el doc es de procedimiento; no incluye datos ni credenciales                                            |
| T-172-SC    | Tampering              | instalación de paquetes                               | accept      | este plan no instala nada                                                                               |

</threat_model>

<verification>
- Doc creado con las 7 secciones y los números reales del piloto.
- SUMMARY con estado y pendientes de la fase.
</verification>

<success_criteria>
Las fases 173-175 pueden ejecutar la adopción de su módulo sin volver a discutir el orden,
la forma ni las trampas.
</success_criteria>

<output>
Create `.planning/phases/172-adopci-n-1-piloto-finance/172-23-SUMMARY.md` when done
</output>
