---
phase: 151-registrar-cobro-pagos-cobros
plan: 03
type: execute
wave: 2
depends_on: [151-02]
files_modified:
  - el-templo-admin/src/pages/CobrosPage.vue
  - el-templo-admin/src/router/routes.ts
  - el-templo-admin/src/config/templo-config.ts
autonomous: true
requirements: [COBRO-01, COBRO-02, COBRO-03]

must_haves:
  truths:
    - "The route is /cobros; /pagos redirects to /cobros; role landing lands on /cobros"
    - "Nav and page title read 'Cobros', not 'Pagos'"
    - "The load flow is 4 separate steps (Socio / ¿Qué se cobra? / ¿Cómo se paga? / Resumen), one thing per screen, not nested accordions"
    - "Desktop shows the active step left + an accumulated summary panel right; mobile collapses the summary to a compact header"
    - "The portada shows a 'Registrar cobro' CTA above a historical listado grouped by day with fecha + hora per row"
    - "All existing PoS behavior (typeahead, DNI dedup, plan grid, Zero, FixedSchedulePicker, per-method pricing, idempotency-per-attempt) is preserved"
  artifacts:
    - path: "el-templo-admin/src/pages/CobrosPage.vue"
      provides: "the renamed + restructured wizard page with portada CTA + listado"
      contains: "Registrar cobro"
    - path: "el-templo-admin/src/router/routes.ts"
      provides: "/cobros route + /pagos redirect + landing update"
      contains: "cobros"
    - path: "el-templo-admin/src/config/templo-config.ts"
      provides: "nav item label 'Cobros' + path /cobros"
      contains: "Cobros"
  key_links:
    - from: "CobrosPage.vue desktop right panel AND step 4"
      to: "CobroResumen.vue"
      via: "shared component mounted twice"
      pattern: "CobroResumen"
    - from: "CobrosPage.vue confirm (step 4)"
      to: "useFinanceLoadApi payPlan/miscCharge/altaConPlan"
      via: "step-2 association selects which endpoint"
      pattern: "payPlan|miscCharge|altaConPlan"
---

<objective>
Rename the Pagos PoS to Cobros and rebuild it as a 4-step wizard on a single route: portada (CTA + historical listado grouped by day with fecha+hora) → Socio → ¿Qué se cobra? → ¿Cómo se paga? → Resumen. Desktop is two-column (active step + accumulated `CobroResumen` panel); mobile collapses the summary and shows a `Paso n de 4` progress bar. All existing functional behavior is redistributed into steps, not rewritten. This plan does NOT add the bank-account selector — Plan 04 adds it into the step-3 that this plan builds.

Purpose: COBRO-01 (rename incl. route), COBRO-02 (steps not accordions), COBRO-03 (fecha+hora listado, CTA above).
Output: `CobrosPage.vue` (renamed from `PagosPage.vue`) + router + nav updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/151-registrar-cobro-pagos-cobros/151-CONTEXT.md
@.planning/phases/151-registrar-cobro-pagos-cobros/151-PATTERNS.md
@.planning/phases/151-registrar-cobro-pagos-cobros/151-UI-SPEC.md

<interfaces>
<!-- Router (el-templo-admin/src/router/routes.ts) -->
- `landingForRole()` (~16-26) has `return '/pagos'` (~25) for coach/gestion/recepcion; static fallback `{ path: '', redirect: '/pagos' }` (~43).
- Route record (~112-114): `{ path: 'pagos', component: () => import('pages/PagosPage.vue'), meta: {...} }`.

<!-- Nav config (el-templo-admin/src/config/templo-config.ts) -->

- `PAGOS_ROLES = ['coach','gestion','recepcion','admin','owner']` (~45).
- Nav item (~107): `{ path: '/pagos', label: 'Pagos', icon: 'point_of_sale', roles: PAGOS_ROLES }`.

<!-- CobroResumen.vue (Plan 02) props -->

- `socio?`, `queSecobra?`, `comoPaga?`, `total?`, `currency?`, `debtWarning?`. Mount it in the desktop right panel AND as the step-4 body.

<!-- PagosPage.vue reusable blocks — line refs to redistribute (do NOT rewrite behavior) -->

- Imports ~397-417 (keep createLogger, formatPrice, the 3 composables, FixedSchedulePicker, useAuthStore).
- Idempotency key per attempt ~488-493, 851-855, 912-926: lazy on first Confirmar tap, reuse on retry, regenerate only after acknowledged success. `resetChargeFields()` ~773-780 and `onSucursalChange` ~623 null the key on deliberate target change.
- Confirm dispatch ~839-927: `if renew → payPlan / else misc → miscCharge / else → altaConPlan`. The step-2 association IS the old "mode".
- Socio typeahead ~47-78 tmpl / ~724-770 onMemberSearch (q-select use-input, input-debounce 300, status badge #option slot, membersApi.searchMembers(val,15)).
- New-student mini-form + DNI dedup on-blur ~112-136 tmpl / ~587-616 (membersApi.checkDuplicates({dni})).
- Debt notice banner POS-01 ~97-108 (q-banner bg-warning text-dark, guard autocompletar.outstanding>0).
- Plan grid by tier + Zero + FixedSchedulePicker ~216-289 tmpl / ~640-721 (plansByTier, selectPlan, getBasePriceFor(plan,method,zero) ~662-665, watch amount auto-calc ~716-721).
- Payment-method buttons ~292-308 / paymentOptions ~495-499.
- Listado ~338-374 tmpl / ~939-966 (financeApi.listMyLoads(); formatTime ~962-966).

<!-- UI-SPEC bindings (151-UI-SPEC.md — APPROVED, binding) -->

- Typography: 4 sizes (24/20/16/14), 2 weights (400/600); NO 12px, `text-caption` forbidden — replace with 14px `text-subtitle2` forced 400 + `text-grey-7`.
- Color: accent (`--q-primary`/`text-primary`) reserved for CTA/Continuar/Confirmar/active-step-indicator/selected-plan/selected-method/text-buttons only. Never hard-code terracotta hex (staging marker). Debt/partial/pending use `warning`; Zero/success/Activa use `positive`; errors `negative`.
- Progress header: numbered steps `1 Socio · 2 ¿Qué se cobra? · 3 ¿Cómo se paga? · 4 Resumen` desktop; `Paso n de 4` + thin `q-linear-progress size="4px"` accent + current label on mobile. Back arrow left edge, icon-only mobile with `aria-label="Volver"`. QStepper explicitly rejected.
- Step transitions: horizontal slide 200ms ease-out (forward from right, back from left) via Vue `<transition>`; respect `prefers-reduced-motion: reduce` → plain fade/none.
- Two-column: desktop `$q.screen.gt.sm`, `xl` (32px) gap, left step body max 560px, right summary panel 320px sticky on `#e9e2d6` (use the secondary-surface class, not raw hex). Mobile → compact sticky summary header (socio + running total), tappable to expand.
- Copy strings: portada title `Cobros`; CTA `Registrar cobro`; step advance `Continuar`; confirm `Confirmar · {monto}` (fallback `Confirmar`); listado empty heading `Todavía no registraste cobros`, body `Cuando registres un cobro, va a aparecer acá con su fecha y hora.`; abandon-flow dialog `Si salís ahora, se pierden los datos cargados.` buttons `Salir`(negative)/`Seguir cargando`(primary flat), triggered on back-from-step-1 / route-leave / explicit close only when the form has data. Day-group labels `Hoy`/`Ayer`/`{ddd d MMM}` es-AR; each row `HH:mm`.
  </interfaces>

@el-templo-admin/src/pages/PagosPage.vue
@el-templo-admin/src/router/routes.ts
@el-templo-admin/src/config/templo-config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename PagosPage → CobrosPage + route/redirect/landing + nav label (COBRO-01)</name>
  <files>el-templo-admin/src/pages/CobrosPage.vue, el-templo-admin/src/router/routes.ts, el-templo-admin/src/config/templo-config.ts</files>
  <read_first>
    - el-templo-admin/src/router/routes.ts (landingForRole ~16-26, fallback ~43, route record ~112-114)
    - el-templo-admin/src/config/templo-config.ts (PAGOS_ROLES ~45, nav item ~107)
    - el-templo-admin/src/pages/PagosPage.vue (top of file — the page title/header where "Pagos"/"Mis cargas" strings live)
  </read_first>
  <action>
    Rename the file with `git mv el-templo-admin/src/pages/PagosPage.vue el-templo-admin/src/pages/CobrosPage.vue` (preserve history). In `routes.ts`: change the route record to `{ path: 'cobros', component: () => import('pages/CobrosPage.vue'), meta: {...unchanged} }` and add `{ path: 'pagos', redirect: '/cobros' }` so old links resolve. Change `landingForRole()`'s `return '/pagos'` → `return '/cobros'` and the static fallback `{ path: '', redirect: '/pagos' }` → `{ path: '', redirect: '/cobros' }` (keep the fallback comment/reasoning). In `templo-config.ts`: change the nav item to `{ path: '/cobros', label: 'Cobros', icon: 'point_of_sale', roles: PAGOS_ROLES }`. Constant `PAGOS_ROLES` may stay (rename is discretionary, D-09) — leave it to avoid churn, or rename to `COBROS_ROLES` consistently across both files if renamed. In `CobrosPage.vue` update user-facing "Pagos" strings to "Cobros" and the page/logger namespace as needed (e.g. `createLogger('cobros')`). Do not change functional behavior in this task beyond strings.
  </action>
  <verify>
    <automated>cd el-templo-admin && grep -rn "PagosPage" src/ | grep -v "node_modules" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `test -f el-templo-admin/src/pages/CobrosPage.vue && ! test -f el-templo-admin/src/pages/PagosPage.vue` (renamed, old gone).
    - `grep -rn "PagosPage" el-templo-admin/src/` returns 0 matches (no dangling import).
    - `grep -n "path: 'cobros'\|path: 'pagos'" el-templo-admin/src/router/routes.ts` shows the /cobros record and the /pagos redirect.
    - `grep -n "/cobros" el-templo-admin/src/config/templo-config.ts` shows the nav item with label `'Cobros'`.
    - `grep -rn "'/pagos'" el-templo-admin/src/router/routes.ts` shows only the redirect source, no landing/fallback still returning /pagos.
  </acceptance_criteria>
  <done>Route, redirect, landing, nav label and file name are all Cobros; no dangling PagosPage reference.</done>
</task>

<task type="auto">
  <name>Task 2: Wizard shell — step state + progress header + transitions + two-column scaffolding (COBRO-02)</name>
  <files>el-templo-admin/src/pages/CobrosPage.vue</files>
  <read_first>
    - el-templo-admin/src/pages/CobrosPage.vue (whole file — current template/script layout to scaffold around)
    - el-templo-admin/src/components/caja/CobroResumen.vue (Plan 02 — props to mount)
    - .planning/phases/151-registrar-cobro-pagos-cobros/151-UI-SPEC.md (Flow Layout & Interaction Contract — binding: progress header, transitions, two-column)
  </read_first>
  <action>
    Build the wizard SHELL only — the existing form logic stays in the script untouched (its redistribution into the shells is Task 3). Add `currentStep` reactive state (0=portada, 1..4=steps) held in the component (no child routes, no store — refresh-safe within the component's life). Build the progress header (inline section or small local component): desktop numbered `1 Socio · 2 ¿Qué se cobra? · 3 ¿Cómo se paga? · 4 Resumen` with accent-filled current number + check on completed + `text-grey-6` future; mobile `Paso {n} de 4` + `q-linear-progress size="4px"` accent + current step label. Back arrow at header left edge (icon-only mobile with `aria-label="Volver"`; desktop adds `Volver` label); step-1 back returns to portada. Guard back-from-step-1 / route-leave (`onBeforeRouteLeave`) / explicit close with the abandon-flow `q-dialog` — `Si salís ahora, se pierden los datos cargados.`, buttons `Salir` (negative) / `Seguir cargando` (primary flat) — only when the form has data.

    Wrap the step body in a Vue `<transition>` doing a 200ms ease-out horizontal slide (forward=new step from the right, back=from the left), falling back to plain fade/none under `prefers-reduced-motion: reduce`; no layout shift of the progress header during transitions. Desktop (`$q.screen.gt.sm`): two-column layout with `xl` (32px) gap — left = active step body (max 560px), right = sticky `CobroResumen` panel (320px, secondary-surface class) mounted with the accumulated-state props (may render `—` placeholders until Task 3 wires real state). Mobile: compact sticky summary header (socio + running total) tappable to expand the full summary. Step bodies can be empty scaffolds (headings `Socio` / `¿Qué se cobra?` / `¿Cómo se paga?` / `Resumen` in `text-h5` + a disabled `Continuar`) — Task 3 fills them. Use only brand color classes (no hard-coded hex); `createLogger`, never console.

  </action>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit 2>&1 | grep -i "CobrosPage" || echo "no CobrosPage type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "currentStep\|q-linear-progress" el-templo-admin/src/pages/CobrosPage.vue` shows the step state and the mobile progress bar.
    - `grep -n "CobroResumen" el-templo-admin/src/pages/CobrosPage.vue` shows the shared summary mounted (desktop panel and/or step 4).
    - `grep -n "<transition" el-templo-admin/src/pages/CobrosPage.vue` shows the step transition wrapper; `grep -n "prefers-reduced-motion" el-templo-admin/src/pages/CobrosPage.vue` shows the reduced-motion fallback.
    - `grep -n "aria-label=\"Volver\"" el-templo-admin/src/pages/CobrosPage.vue` present (accessible back button).
    - `grep -n "Si salís ahora" el-templo-admin/src/pages/CobrosPage.vue` shows the abandon-flow dialog copy.
    - `grep -c "#[0-9a-fA-F]\{6\}" el-templo-admin/src/pages/CobrosPage.vue` equals 0 (no hard-coded hex).
    - `npx vue-tsc --noEmit` reports no CobrosPage type errors.
  </acceptance_criteria>
  <done>The wizard shell exists: step state, progress header (desktop+mobile), slide transitions with reduced-motion fallback, two-column desktop / collapsed-summary mobile, back navigation + abandon guard — step bodies still skeletal.</done>
</task>

<task type="auto">
  <name>Task 3: Redistribute the existing PoS logic into the step shells, idempotency preserved (COBRO-02)</name>
  <files>el-templo-admin/src/pages/CobrosPage.vue</files>
  <read_first>
    - el-templo-admin/src/pages/CobrosPage.vue (the shell from Task 2 + ALL the legacy blocks: socio typeahead ~47-78/~724-770, mini-form + DNI dedup ~112-136/~587-616, debt banner ~97-108, plan grid + Zero + FixedSchedulePicker ~216-289/~640-721, payment buttons ~292-308/~495-499, confirm dispatch ~839-927, idempotency ~488-493/~851-855/~912-926, resetChargeFields ~773-780, onSucursalChange ~623)
    - el-templo-admin/src/components/scheduling/FixedSchedulePicker.vue (moves into step 2 unchanged)
    - .planning/phases/151-registrar-cobro-pagos-cobros/151-UI-SPEC.md (Per-step content contract — binding)
  </read_first>
  <action>
    Move the existing functional blocks into the step shells — behavior is redistributed, NOT rewritten. Step 1 = socio typeahead + new-student mini-form + DNI dedup on-blur + debt notice banner (POS-01) below the selected/created socio (banner copy unchanged, 14px Label instead of text-caption). Step 2 = the "¿a qué se asocia?" question surfacing the 3 associations (D-01, no mode toggle): (a) renovar plan vigente (readonly plan name + amount), (b) asignar plan nuevo (plan grid by tier + Zero toggle + FixedSchedulePicker for fixed plans), (c) cobro suelto (concepto + Motivo select obligatorio). If the socio has no renewable plan, show `Asignarle un plan` inline. Step 3 = payment-method buttons + `Monto` (currency suffix) + partial-payment warning `El alumno quedará deudor por {monto}.`; DO NOT add the bank-account selector here (Plan 04 does). Step 4 = `CobroResumen` read-only + `Confirmar · {monto}` (fallback `Confirmar`).

    The step-2 association replaces the old `mode`; the confirm dispatch chooses `payPlan`/`miscCharge`/`altaConPlan` by association exactly as the old `if renew/misc/alta` branch. PRESERVE the idempotency-key-per-attempt semantics EXACTLY (lazy `crypto.randomUUID()` on first Confirmar tap, reuse on retry, regenerate only after acknowledged success; null it when the socio/plan target changes across steps — carry the resetChargeFields/onSucursalChange nulling into step navigation). Wire the accumulated state into the `CobroResumen` mounts (desktop panel live-updates per step; mobile compact header shows socio + running total). `Continuar` enables per step completion (step 1: socio selected or valid mini-form; step 2: association chosen with required fields; step 3: method + valid amount). Remove the old accordion/mode-toggle template remnants. Keep `getBasePriceFor`/amount auto-calc and per-method pricing untouched; success/error `$q.notify` uses the UI-SPEC copy (`Cobro registrado — pendiente de validación`, `No se pudo registrar el cobro. Reintentá.`); replace any `text-caption` with 14px Label; `createLogger`, never console.

  </action>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit 2>&1 | grep -i "CobrosPage" || echo "no CobrosPage type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "currentIdempotencyKey\|crypto.randomUUID" el-templo-admin/src/pages/CobrosPage.vue` shows idempotency-per-attempt preserved.
    - `grep -n "payPlan\|miscCharge\|altaConPlan" el-templo-admin/src/pages/CobrosPage.vue` shows the 3-endpoint dispatch retained.
    - `grep -n "FixedSchedulePicker" el-templo-admin/src/pages/CobrosPage.vue` shows the picker mounted in step 2.
    - `grep -n "checkDuplicates\|onDniBlur" el-templo-admin/src/pages/CobrosPage.vue` shows DNI dedup preserved in step 1.
    - The old mode toggle is gone: no `Pago de plan`/`Alta + plan`/`Cobro suelto` toggle group remains as an upfront mode selector (grep for the old toggle labels shows them only as step-2 association options, if at all).
    - `grep -c "text-caption" el-templo-admin/src/pages/CobrosPage.vue` equals 0.
    - `npx vue-tsc --noEmit` reports no CobrosPage type errors.
  </acceptance_criteria>
  <done>All prior PoS behavior lives inside the 4 steps with idempotency-per-attempt semantics intact, the accumulated summary live-updates, and the accordion/mode-toggle layout is gone.</done>
</task>

<task type="auto">
  <name>Task 4: Portada CTA + historical listado grouped by day with fecha + hora (COBRO-03)</name>
  <files>el-templo-admin/src/pages/CobrosPage.vue</files>
  <read_first>
    - el-templo-admin/src/pages/CobrosPage.vue (listado block ~338-374 tmpl / ~939-966 script incl. formatTime; the portada/step-0 state from Task 2)
    - el-templo-admin/src/utils/format-price.ts (formatPrice)
    - .planning/phases/151-registrar-cobro-pagos-cobros/151-UI-SPEC.md (Listado + Copywriting sections)
  </read_first>
  <action>
    Build the portada (step 0): a full-width-up-to-560px accent `Registrar cobro` CTA (size `lg`) at the TOP that enters the wizard (sets currentStep=1), and below it the historical listado. Keep the data source unchanged (`financeApi.listMyLoads()` — endpoint returns the last 50 historical loads, not today). Change the title from "Mis cargas de hoy" to `Cobros`. Group rows by day using sticky day-group labels `Hoy` / `Ayer` / else `{ddd d MMM}` in `es-AR` (e.g. `mar 1 jul`), computed from each row's `createdAt`. Each row shows the time `HH:mm` (extend/replace `formatTime` with a helper that reads the same timestamp), plus socio + concept + method badge + `Pendiente` badge + amount (`formatPrice`). Timestamps and metadata use 14px Label (`text-subtitle2` forced 400 + `text-grey-7`), never `text-caption`. Empty state: heading `Todavía no registraste cobros`, body `Cuando registres un cobro, va a aparecer acá con su fecha y hora.` Keep the method/Pendiente badge semantic colors.
  </action>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit 2>&1 | grep -i "CobrosPage" || echo "no CobrosPage type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Registrar cobro" el-templo-admin/src/pages/CobrosPage.vue` shows the portada CTA above the listado.
    - `grep -n "Hoy\|Ayer" el-templo-admin/src/pages/CobrosPage.vue` shows day-group labels.
    - `grep -n "Todavía no registraste cobros" el-templo-admin/src/pages/CobrosPage.vue` shows the empty-state copy.
    - `grep -c "Mis cargas de hoy" el-templo-admin/src/pages/CobrosPage.vue` equals 0 (old lying title removed).
    - `grep -c "text-caption" el-templo-admin/src/pages/CobrosPage.vue` equals 0.
    - `npx vue-tsc --noEmit` reports no CobrosPage type errors.
  </acceptance_criteria>
  <done>Portada = CTA above a day-grouped historical listado showing fecha (group) + hora (row), honest title, empty state.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                                              |
| ------------------------- | ------------------------------------------------------------------------ |
| browser route → admin SPA | Route rename/redirect; role landing. No new server surface in this plan. |

## STRIDE Threat Register

| Threat ID | Category  | Component                   | Disposition | Mitigation Plan                                                                                                                                                                                          |
| --------- | --------- | --------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-151-07  | Elevation | /cobros route access        | mitigate    | Route `meta.allowedRoles` (PAGOS_ROLES) unchanged from the /pagos record; the API-side FINANCE_LOAD_ROLES gate remains the real authority (149 D-04). The /pagos→/cobros redirect carries no new access. |
| T-151-08  | Tampering | client-built charge payload | accept      | The wizard forwards the same fields the old page did; server-side derivation/validation (Plan 01 + existing v5.3 invariant) is authoritative. This plan adds no new body field.                          |
| T-151-SC  | Tampering | package installs            | accept      | No new dependencies; reuses existing Quasar components and composables                                                                                                                                   |

</threat_model>

<verification>
- Admin typecheck passes; no dangling PagosPage reference.
- /pagos redirects to /cobros; nav + title read Cobros.
- Load flow is 4 discrete steps with progress header and two-column desktop / collapsed mobile summary.
- Portada CTA sits above a day-grouped listado with fecha+hora; existing behavior + idempotency preserved.
</verification>

<success_criteria>
COBRO-01, COBRO-02, COBRO-03 satisfied: renamed surface incl. route, step-based flow replacing accordions, honest day-grouped listado with fecha+hora and CTA on top — with all prior PoS behavior intact.
</success_criteria>

<output>
Create `.planning/phases/151-registrar-cobro-pagos-cobros/151-03-SUMMARY.md` when done.
</output>
