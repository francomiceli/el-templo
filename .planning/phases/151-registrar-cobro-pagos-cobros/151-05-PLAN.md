---
phase: 151-registrar-cobro-pagos-cobros
plan: 05
type: execute
wave: 4
depends_on: [151-03, 151-04]
files_modified:
  - el-templo-admin/src/pages/CobrosPage.vue
  - el-templo-admin/src/components/caja/CobroResumen.vue
  - el-templo-admin/src/router/index.ts
autonomous: true
gap_closure: true
requirements: [COBRO-01, COBRO-02, COBRO-03]

must_haves:
  truths:
    - "The Sede selector renders for EVERY alta (existing member AND new student), not only inside the new-student mini-form, restoring pre-151 behavior where sucursalId is operator-visible and editable before it is persisted as branchId on the subscription and plan charge."
    - "The resolved sede (branch name) is surfaced read-only in CobroResumen for alta charges, so the operator sees which branch will be written."
    - "A new-student context can no longer reach an unconfirmable dead end: the 'Renovar plan vigente' and 'Cobro suelto' associations are unavailable (disabled + hinted) for a new student, and canContinueStep blocks misc without a selectedMember."
    - "A dedup-adopted existing member (via 'Usar ese alumno') loads autocompletar, so the debt banner, renew pre-fill and charge currency populate as they do for a typeahead-selected member."
    - "Changing the selected plan clears the idempotency key so a lost-success retry after a plan change cannot no-op against the old charge."
    - "Historical rows in the portada listado no longer badge voided charges as 'Pendiente' — voided rows badge 'Anulado'."
    - "The role-denied router fallback lands on /cobros (via landingForRole), not the stale /pagos."
  artifacts:
    - path: "el-templo-admin/src/pages/CobrosPage.vue"
      provides: "Sede selector in step-2 alta mode + dead-end guards + hygiene fixes"
      contains: "onSucursalChange"
    - path: "el-templo-admin/src/components/caja/CobroResumen.vue"
      provides: "optional sede prop + read-only sede row"
      contains: "sede"
    - path: "el-templo-admin/src/router/index.ts"
      provides: "role-denied fallback via landingForRole (no /pagos)"
      contains: "landingForRole"
  key_links:
    - from: "CobrosPage.vue step-2 alta Sede q-select (v-model=sucursalId)"
      to: "assignPlan branchId (CoachAltaInput.branchId = sucursalId)"
      via: "editable selector reachable for existing-member alta"
      pattern: 'v-model="sucursalId"'
    - from: "CobrosPage.vue resumenSede"
      to: "CobroResumen sede prop"
      via: "branch name passed to all CobroResumen mounts for alta"
      pattern: ':sede="resumenSede"'
---

<objective>
Close the two verified gaps from 151-VERIFICATION.md (CR-01 blocker, WR-01 warning) plus the cheap low-risk warnings (WR-02, WR-03, WR-04, WR-05). Frontend-only — no schema, migration, or API surface changes.

CR-01: the Sede (branch) selector, which pre-151 `PagosPage.vue` rendered for every alta (`v-if="mode === 'alta'"`, confirmed via `git show da7a6eda~1:el-templo-admin/src/pages/PagosPage.vue`), now only renders inside the step-1 new-student mini-form (`v-if="showNewStudentForm"`). For an existing-member alta, `sucursalId` silently keeps its default (`authStore.user?.branchId` or `branchOptions[0]`) and is persisted as `branchId` on both the subscription and the plan charge. Restore the selector for all alta flows and surface the resolved sede in `CobroResumen`.

WR-01: new-student + `misc`/`renew` associations reach a permanently-disabled Confirmar (misc) or an empty step-2 body (renew) with no explanation. Disable/hint those associations for a new student and add a member-consistency guard.

Purpose: COBRO-02 (step flow must "work well" without regression / dead ends), COBRO-01 (rename fallout in router fallback), COBRO-03 (listado honesty on voided rows).
Output: corrected `CobrosPage.vue`, `CobroResumen.vue` (new `sede` prop), `router/index.ts` (fallback via `landingForRole`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/151-registrar-cobro-pagos-cobros/151-CONTEXT.md
@.planning/phases/151-registrar-cobro-pagos-cobros/151-VERIFICATION.md
@.planning/phases/151-registrar-cobro-pagos-cobros/151-REVIEW.md
@.planning/phases/151-registrar-cobro-pagos-cobros/151-UI-SPEC.md

<interfaces>
<!-- CobrosPage.vue current state (post 151-03/04) — line refs for the fixes -->

- Sede q-select currently lives ONLY inside the step-1 mini-form: template ~193-211 gated by `v-if="showNewStudentForm"`, bound `v-model="sucursalId"` with `@update:model-value="onSucursalChange"` and a `#prepend` place icon.
- `sucursalId = ref<number | null>(authStore.user?.branchId ?? null)` (~747); `branchOptions` (~748) loaded by `loadBranches()` (~1109) on mount (~1546); `loadBranches` fallbacks `sucursalId` to `branchOptions[0]?.id` (~1112-1114).
- Step-2 alta block: `<template v-else-if="mode === 'alta'">` opens ~330; plan grid loading skeleton ~331-333; `plansByTier` list ~340-389; `FixedSchedulePicker :branch-id="sucursalId ?? 0"` ~395-402. `loadAltaPlans()` (~1244) filters by `sucursalId`; `onSelectAssociation('alta')` (~805) calls `loadBranches()` (if empty) + `loadAltaPlans()`.
- Step-2 association list: `associationOptions` array (~729-743, values `renew`/`alta`/`misc`); q-item loop ~274-297 with `@click="onSelectAssociation(opt.value)"`, `:active="mode === opt.value"`. `onSelectAssociation(m)` (~805-822).
- Gating: `canContinueStep` (~976-1000) case 2 misc `return concepto.value.trim().length > 0 && miscReason.value != null` (NO selectedMember check); `canConfirm` (~941-964) already `if (!selectedMember.value) return false` for non-alta (~956).
- `newStudentValid` (~1181-1187); `resetAltaFields` (~1122-1130); `onUsarExistente` (~1157-1167) sets `selectedMember` + `resetAltaFields()` but does NOT call `loadAutocompletar` (contrast `onMemberSelected` ~1329-1334 which always does).
- `selectPlan(plan)` (~1239-1242) sets `selectedPlan` + clears `scheduleIds` but does NOT clear `currentIdempotencyKey` (contrast `onSucursalChange` ~1169-1174 and `resetChargeFields` ~1320-1327 which both null the key).
- Portada listado badge: `<q-badge color="warning" label="Pendiente" />` hardcoded at ~59, inside the row `q-gutter-xs` div ~54-65. Rows are `TransactionListItem` (`myLoads`), which carries `voidedAt: string | null` (NOT `validationStatus`).
- Summary computeds: `resumenSocio`/`resumenQueSecobra`/`resumenComoPaga`/`resumenCurrency`/`resumenDebtWarning` (~825-852). `CobroResumen` is mounted 3x: mobile expansion-item ~124-131, step-4 body ~564-571, desktop right panel ~594-601.

<!-- CobroResumen.vue props (el-templo-admin/src/components/caja/CobroResumen.vue) -->

- `withDefaults(defineProps<{ socio?; queSecobra?; comoPaga?; total?; currency?; debtWarning? }>(), {...})`. Purely presentational (props only, no API/store/logger). Rows are `q-item` with a `text-subtitle2 text-weight-regular text-grey-7` label + `text-body1` value; `—` shown for empty. Add `sede?: string | null` (default null) and a row that renders only when non-null, mirroring the existing Socio/Qué-se-cobra rows.

<!-- router/index.ts (el-templo-admin/src/router/index.ts) -->

- `landingForRole` already imported (line 3). Role-denied fallback ~56-67 uses a local `defaultPages` map with `coach/gestion/recepcion → '/pagos'` and `return defaultPages[role] || '/pagos'`. Replace with `return landingForRole();` (owner/admin → /alumnos, employees → /cobros — the single source of truth). The `trainingOnly` bounce below (~75-77) stays untouched.

<!-- Pre-151 reference (recoverable) -->

- `git show da7a6eda~1:el-templo-admin/src/pages/PagosPage.vue` — the Sede `q-select` at top with `v-if="mode === 'alta'"` (line 25), label "Sede", `v-model="sucursalId"`, shown for EVERY alta regardless of member origin. This is the behavior to restore (now inside step-2 alta since `mode` is unknown until step 2).
  </interfaces>

@el-templo-admin/src/pages/CobrosPage.vue
@el-templo-admin/src/components/caja/CobroResumen.vue
@el-templo-admin/src/router/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore Sede selector for all alta flows + surface sede in resumen + close new-student dead ends (CR-01, WR-01)</name>
  <files>el-templo-admin/src/pages/CobrosPage.vue, el-templo-admin/src/components/caja/CobroResumen.vue</files>
  <read_first>
    - el-templo-admin/src/pages/CobrosPage.vue (step-1 mini-form Sede ~193-211; step-2 alta block ~330-408; associationOptions ~729-743 + q-item loop ~274-297; onSelectAssociation ~805-822; canContinueStep ~976-1000; canConfirm ~941-964; newStudentValid ~1181-1187; summary computeds ~825-852; the 3 CobroResumen mounts ~124-131, ~564-571, ~594-601; branchOptions/sucursalId ~747-748; loadBranches ~1109-1120)
    - el-templo-admin/src/components/caja/CobroResumen.vue (whole file — props + row markup pattern)
    - .planning/phases/151-registrar-cobro-pagos-cobros/151-VERIFICATION.md (gaps CR-01 + WR-01, the `missing` items)
    - .planning/phases/151-registrar-cobro-pagos-cobros/151-REVIEW.md (CR-01 and WR-01 concrete fixes)
    - the pre-151 markup via `git show da7a6eda~1:el-templo-admin/src/pages/PagosPage.vue` (Sede select for every alta)
  </read_first>
  <action>
    CR-01 — Sede for every alta: Remove the Sede `q-select` from the step-1 `showNewStudentForm` mini-form (~193-211), leaving that mini-form as Nombre/Apellido/DNI + dedup only. Render a single Sede `q-select` inside the step-2 `mode === 'alta'` block, positioned BEFORE the plan-grid loading skeleton (before the `loadingPlans` block ~331), bound `v-model="sucursalId"`, `:options="branchOptions"`, `option-value="id"`, `option-label="name"`, `emit-value`, `map-options`, `outlined`, `label="Sede"`, `@update:model-value="onSucursalChange"`, keeping the `#prepend` place icon. This makes the selector reachable for BOTH existing-member alta (typeahead-selected) and new-student alta, since both pass through step-2 alta mode. There must be exactly ONE `v-model="sucursalId"` q-select in the file after this change. Do not alter `sucursalId`'s default init (~747), `loadBranches` fallback (~1112-1114), `loadAltaPlans` (~1244), or the `FixedSchedulePicker :branch-id="sucursalId ?? 0"` binding.

    CR-01 — surface sede in resumen: In `CobroResumen.vue`, add an optional prop `sede?: string | null` (default `null`) to the `defineProps`/`withDefaults`, and add a new `q-item` row labeled "Sede" (same `text-subtitle2 text-weight-regular text-grey-7` label + `text-body1` value markup as the Socio row) that renders only via `v-if="sede"` (so it is hidden for renew/misc where the branch is server-derived). Place it after the Socio row. In `CobrosPage.vue`, add a computed `resumenSede` returning the branch name for alta only: when `mode.value === 'alta'`, `branchOptions.value.find((b) => b.id === sucursalId.value)?.name ?? null`, else `null`. Pass `:sede="resumenSede"` to all THREE `CobroResumen` mounts (mobile expansion-item ~124-131, step-4 body ~564-571, desktop panel ~594-601).

    WR-01 — close new-student dead ends: Add a computed `isNewStudentContext` = `showNewStudentForm.value && !selectedMember.value`. In the step-2 association q-item loop (~274-297), add `:disable` bound to a helper `isAssociationDisabled(opt.value)` that returns `true` for `renew` and `misc` when `isNewStudentContext.value` is true (a new student has no member id, so only `alta` is valid). Render a hint under those disabled options (e.g. `Solo para socios existentes`). Guard `onSelectAssociation(m)` (~805) to early-return when `isAssociationDisabled(m)` is true so `mode` can never become `renew`/`misc` for a new student. Defensively, add `selectedMember.value != null &&` to the `canContinueStep` case-2 `misc` branch (~989-990) so misc cannot advance without an existing member (renew case already returns false for a new student because `autocompletar` is null). Do NOT touch the alta gating branches.

    Use `createLogger`, never `console`. Do not introduce hard-coded hex colors — use brand/semantic Quasar classes only. Do not change idempotency semantics in this task.

  </action>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit 2>&1 | grep -iE "CobrosPage|CobroResumen" || echo "no type errors in touched files"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'v-model="sucursalId"' el-templo-admin/src/pages/CobrosPage.vue` equals `1` (single Sede selector, no longer duplicated / no longer only in the mini-form).
    - The single `v-model="sucursalId"` q-select is inside the step-2 `mode === 'alta'` block (rendered before the plan grid), NOT inside the `showNewStudentForm` template — verified by reading the surrounding `v-if`/`v-else-if` context. Behavior assertion: starting an alta for a typeahead-selected EXISTING member shows a Sede selector in step 2 before the plan grid.
    - `grep -n 'onSucursalChange' el-templo-admin/src/pages/CobrosPage.vue` shows the moved selector still wired to `@update:model-value="onSucursalChange"`.
    - `grep -n 'sede' el-templo-admin/src/components/caja/CobroResumen.vue` shows the new `sede` prop and a `v-if="sede"` Sede row.
    - `grep -c ':sede="resumenSede"' el-templo-admin/src/pages/CobrosPage.vue` equals `3` (passed to all three CobroResumen mounts).
    - `grep -n 'resumenSede' el-templo-admin/src/pages/CobrosPage.vue` shows the computed returning branch name for alta only (null for renew/misc).
    - `grep -n 'isNewStudentContext\|isAssociationDisabled' el-templo-admin/src/pages/CobrosPage.vue` shows the new-student guard + per-option disable helper.
    - `grep -n ':disable' el-templo-admin/src/pages/CobrosPage.vue` shows the association q-item disable binding. Behavior assertion: with a new student in step 1, the "Renovar plan vigente" and "Cobro suelto" options are disabled/hinted, and only "Asignar plan nuevo" is selectable.
    - The `canContinueStep` case-2 `misc` branch now requires `selectedMember.value != null` (source assertion).
    - `grep -c "#[0-9a-fA-F]\{6\}" el-templo-admin/src/pages/CobrosPage.vue` equals `0` and same for `CobroResumen.vue` (no hard-coded hex).
    - `npx vue-tsc --noEmit` reports no errors in CobrosPage.vue or CobroResumen.vue.
  </acceptance_criteria>
  <done>The Sede selector is reachable and editable for every alta (existing + new student), the chosen branch is shown in CobroResumen, and a new student can no longer select renew/misc or reach a permanently-disabled Confirmar.</done>
</task>

<task type="auto">
  <name>Task 2: Cheap hygiene fixes — dedup autocompletar, idempotency-on-plan-change, honest voided badge, router fallback (WR-02, WR-04, WR-03, WR-05)</name>
  <files>el-templo-admin/src/pages/CobrosPage.vue, el-templo-admin/src/router/index.ts</files>
  <read_first>
    - el-templo-admin/src/pages/CobrosPage.vue (onUsarExistente ~1157-1167; onMemberSelected ~1329-1334; selectPlan ~1239-1242; resetChargeFields ~1320-1327; portada listado badge ~54-65; loadAutocompletar ~1336-1352)
    - el-templo-admin/src/router/index.ts (role-denied fallback ~56-67; landingForRole import line 3)
    - el-templo-admin/src/types/transaction.ts (TransactionListItem ~88-108 — confirm `voidedAt: string | null` exists, `validationStatus` does NOT)
    - .planning/phases/151-registrar-cobro-pagos-cobros/151-REVIEW.md (WR-02, WR-03, WR-04, WR-05 fixes)
  </read_first>
  <action>
    WR-02 — dedup-adopted member loads autocompletar: In `onUsarExistente` (~1157-1167), after setting `selectedMember` and calling `resetAltaFields()`, add `void loadAutocompletar(m.id);` (mirror `onMemberSelected`), so the debt banner (POS-01), renew pre-fill, and charge currency populate for a member adopted via the DNI-dedup "Usar ese alumno" action.

    WR-04 — new target → new idempotency key: In `selectPlan(plan)` (~1239-1242), add `currentIdempotencyKey.value = null;` alongside the existing `selectedPlan`/`scheduleIds` assignments, matching the convention already used by `onSucursalChange` (~1172) and `resetChargeFields` (~1326). This prevents a lost-success retry after a plan change from idempotent-no-op-ing against the previously-charged plan.

    WR-03 — honest voided badge (frontend-only, cheap): In the portada listado row badges (~54-65), replace the unconditional `<q-badge color="warning" label="Pendiente" />` with a conditional: when `ticket.voidedAt != null` render `<q-badge color="negative" label="Anulado" />`, else render `<q-badge color="warning" label="Pendiente" />`. `voidedAt` is already present on `TransactionListItem`. Note in the SUMMARY that the Validado-vs-Pendiente distinction is NOT addressed here because it requires exposing `validationStatus` on the list endpoint (backend), which is out of this frontend-only gap-closure scope; the voided badge removes the worst misrepresentation (a voided charge showing "Pendiente").

    WR-05 — router fallback via landingForRole: In `router/index.ts` role-denied branch (~56-67), delete the local `defaultPages` map and its `return defaultPages[role] || '/pagos'`, and instead `return landingForRole();` (already imported, owner/admin → /alumnos, coach/gestion/recepcion → /cobros). Leave the `trainingOnly` bounce (~75-77) and every other guard untouched.

    Use `createLogger`, never `console`. No hard-coded hex.

  </action>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit 2>&1 | grep -iE "CobrosPage|router/index" || echo "no type errors in touched files"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'loadAutocompletar' el-templo-admin/src/pages/CobrosPage.vue` shows a call inside `onUsarExistente` (source assertion: the function body now invokes `loadAutocompletar(m.id)`).
    - The `selectPlan` function body contains `currentIdempotencyKey.value = null;` (source assertion).
    - `grep -n 'Anulado' el-templo-admin/src/pages/CobrosPage.vue` shows the voided badge; the "Pendiente" badge is now conditional on `voidedAt == null` (source assertion — no longer unconditional).
    - `grep -c 'defaultPages' el-templo-admin/src/router/index.ts` equals `0` (map removed).
    - `grep -c "'/pagos'" el-templo-admin/src/router/index.ts` equals `0` (no stale /pagos fallback remains).
    - `grep -n 'return landingForRole()' el-templo-admin/src/router/index.ts` shows the role-denied fallback now delegating to landingForRole.
    - `npx vue-tsc --noEmit` reports no errors in CobrosPage.vue or router/index.ts.
  </acceptance_criteria>
  <done>Dedup-adopted members load their plan/debt context, a plan change starts a fresh idempotency attempt, voided historical rows badge "Anulado", and the router fallback lands on /cobros via the single-source landingForRole.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                                                                                                                                                              |
| -------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| operator (admin SPA) → API | The operator-chosen `branchId` (sucursalId) crosses to `/coach-load/alta` and is persisted as `branch_id` on the subscription AND the plan-charge ledger row — a financial/membership attribution field. |
| browser route → admin SPA  | Role-denied fallback destination; no new server surface.                                                                                                                                                 |

## STRIDE Threat Register

| Threat ID | Category                | Component                                     | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --------- | ----------------------- | --------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-151-09  | Repudiation / Tampering | branchId attribution on subscription + charge | mitigate    | The blocker being fixed: an unshown, silently-defaulted `sucursalId` writes a wrong branch on financial records with no operator visibility. Task 1 restores the editable Sede selector for every alta and surfaces the resolved branch read-only in CobroResumen (operator awareness before Confirmar). Server still derives/validates the rest of the payload (v5.3 invariant); `branchId` remains a legitimately operator-chosen field per phase 148 design. |
| T-151-10  | Tampering               | client-built charge payload                   | accept      | The wizard forwards the same fields the pre-151 page did; server-side derivation/validation (Plan 01 + v5.3 `additionalProperties:false`) is authoritative. This plan adds no new body field — it only makes an existing one (branchId) visible/editable again.                                                                                                                                                                                                 |
| T-151-11  | Elevation               | role-denied router fallback (/cobros)         | accept      | Client-side redirect only; API-side `FINANCE_LOAD_ROLES` gate is the real authority (149 D-04). Routing to landingForRole carries no new access.                                                                                                                                                                                                                                                                                                                |
| T-151-SC  | Tampering               | package installs                              | accept      | No new dependencies; reuses existing Quasar components and composables.                                                                                                                                                                                                                                                                                                                                                                                         |

</threat_model>

<verification>
- Admin typecheck passes for CobrosPage.vue, CobroResumen.vue, router/index.ts.
- Exactly one Sede selector, reachable in step-2 alta for existing-member AND new-student altas; resolved branch shown in CobroResumen for alta.
- New-student context: renew/misc associations disabled+hinted; no reachable permanently-disabled Confirmar.
- Dedup-adopted member loads autocompletar; plan change clears the idempotency key; voided rows badge "Anulado"; role-denied fallback lands /cobros.
</verification>

<success_criteria>
CR-01 and WR-01 closed (COBRO-02 "funcionando bien" restored without the branch-attribution regression or dead-end states); WR-02/WR-03/WR-04/WR-05 folded in as cheap frontend-only hygiene. No backend, schema, or migration changes; existing 151-01..04 plans/summaries untouched.
</success_criteria>

<output>
Create `.planning/phases/151-registrar-cobro-pagos-cobros/151-05-SUMMARY.md` when done.
</output>
