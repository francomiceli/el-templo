---
phase: 151-registrar-cobro-pagos-cobros
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-admin/src/composables/useFinanceLoadApi.ts
  - el-templo-admin/src/components/caja/CobroResumen.vue
autonomous: true
requirements: [COBRO-02, COBRO-04]

must_haves:
  truths:
    - "The three PoS composable inputs accept an optional bankAccountId forwarded in the request body"
    - "useFinanceLoadApi exposes listBankAccounts(currency?) hitting the coach-reachable endpoint"
    - "A single presentational CobroResumen component renders socio / qué se cobra / cómo se paga / Total"
  artifacts:
    - path: "el-templo-admin/src/composables/useFinanceLoadApi.ts"
      provides: "bankAccountId field on the 3 inputs + listBankAccounts method"
      contains: "listBankAccounts"
    - path: "el-templo-admin/src/components/caja/CobroResumen.vue"
      provides: "shared accumulated-summary component (desktop right panel AND step 4)"
      min_lines: 30
  key_links:
    - from: "useFinanceLoadApi.listBankAccounts"
      to: "GET /admin/finance/coach-load/bank-accounts"
      via: "api.get with optional currency param"
      pattern: "coach-load/bank-accounts"
---

<objective>
Build the two dependency-free pieces the wizard needs: (1) extend `useFinanceLoadApi` with the `bankAccountId` field on the three PoS inputs plus a `listBankAccounts(currency?)` method pointed at the new coach-reachable endpoint, and (2) create the shared `CobroResumen.vue` presentational component that the wizard's desktop right panel AND step-4 both mount (single source of truth per UI-SPEC).

Purpose: Interface-first — define the contracts the wizard (Plan 03) and bank-selector (Plan 04) consume, so those plans build against fixed types instead of exploring.
Output: extended composable + new `CobroResumen.vue`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/151-registrar-cobro-pagos-cobros/151-CONTEXT.md
@.planning/phases/151-registrar-cobro-pagos-cobros/151-PATTERNS.md
@.planning/phases/151-registrar-cobro-pagos-cobros/151-UI-SPEC.md

<interfaces>
<!-- From el-templo-admin/src/composables/useFinanceLoadApi.ts -->
- `CoachPayPlanInput` (~28), `CoachMiscChargeInput` (~38), `CoachAltaInput` (~63) — each currently forwards its fields into the POST body (like `miscReason`, `scheduleIds`).
- `getAutocompletar(userId)` (~136-150) is the GET shape to mirror: `const { data } = await api.get(...)`; `catch (err: unknown)` → `extractError(err, msg)`; `loading`/`error` refs; `finally { loading.value = false }`.
- Composable returns an object (~245-254) listing the methods — new method must be added there.
- CLAUDE.md convention: composables expose `cleanup()`, NO `onUnmounted` inside; never `console.*` (use the composable's error ref / caller logs).

<!-- The endpoint Plan 01 creates -->

- `GET /admin/finance/coach-load/bank-accounts?currency=ARS` → `{ accounts: Array<{ id: number; name: string; currency: string }> }`, reachable by coach/recepcion (FINANCE_LOAD_ROLES).

<!-- UI-SPEC contract for CobroResumen (§Two-column, §Step 4) -->

- Pure presentational, props only: `socio`, `queSecobra`, `comoPaga`, `total`, `currency`. Renders q-list/q-item rows. Empty rows show `—`. Total uses Heading 20/600 (`text-h6`). Uses `formatPrice(total, currency)`. No API calls, no logger. Typography: 4 sizes (24/20/16/14), 2 weights (400/600); NO 12px (`text-caption` forbidden) — muted micro-copy uses 14px `text-subtitle2` forced weight 400 + `text-grey-7`.
  </interfaces>

@el-templo-admin/src/composables/useFinanceLoadApi.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend useFinanceLoadApi with bankAccountId field + listBankAccounts</name>
  <files>el-templo-admin/src/composables/useFinanceLoadApi.ts</files>
  <read_first>
    - el-templo-admin/src/composables/useFinanceLoadApi.ts (whole file — the 3 input interfaces, getAutocompletar shape, the return object, the file header conventions)
  </read_first>
  <action>
    Add an optional `bankAccountId?: number` field to `CoachPayPlanInput`, `CoachMiscChargeInput`, and `CoachAltaInput`. The existing `payPlan`/`miscCharge`/`altaConPlan` functions already spread the input into the POST body, so no change is needed to how they forward it (confirm the body is built from the input object; if any builds the body field-by-field, add `bankAccountId` there).

    Add `async function listBankAccounts(currency?: string): Promise<{ accounts: Array<{ id: number; name: string; currency: string }> }>` mirroring `getAutocompletar`: set `loading.value = true; error.value = null;` in a try, call `api.get('/admin/finance/coach-load/bank-accounts', { params: currency ? { currency } : {} })`, return `data`; `catch (err: unknown)` → `error.value = extractError(err, 'Error cargando las cuentas bancarias'); throw err;`; `finally { loading.value = false; }`. Add `listBankAccounts` to the composable's returned object. Do NOT reuse `useTransactionsApi.listBankAccounts` (it hits the admin-only `/cash-registers` route a coach cannot reach).

  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm tsc --noEmit -p tsconfig.json 2>/dev/null || npx vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bankAccountId" el-templo-admin/src/composables/useFinanceLoadApi.ts` ≥ 3 (all three input interfaces).
    - `grep -n "listBankAccounts" el-templo-admin/src/composables/useFinanceLoadApi.ts` shows both the function definition and its inclusion in the return object.
    - `grep -n "coach-load/bank-accounts" el-templo-admin/src/composables/useFinanceLoadApi.ts` shows the endpoint path.
    - Admin typecheck exits 0.
  </acceptance_criteria>
  <done>The three inputs carry bankAccountId and listBankAccounts is exposed and hits the coach endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared CobroResumen.vue presentational summary component</name>
  <files>el-templo-admin/src/components/caja/CobroResumen.vue</files>
  <read_first>
    - el-templo-admin/src/pages/PagosPage.vue (ticket/summary block ~353-374 and readonly plan field ~164-179 for the row style to reproduce)
    - el-templo-admin/src/components/caja/CuentaBancariaFormDialog.vue (an existing caja component for the SFC structure/import conventions in this folder)
    - el-templo-admin/src/utils/format-price.ts (formatPrice signature)
    - el-templo-admin/src/css/quasar.variables.scss (brand classes — no hard-coded hex)
  </read_first>
  <action>
    Create a `<script setup lang="ts">` SFC that takes props: `socio?: string | null`, `queSecobra?: string | null`, `comoPaga?: string | null`, `total?: number | null`, `currency?: string` (default `'ARS'`). Render a `q-list` of `q-item` rows: `Socio`, `Qué se cobra`, `Cómo se paga` (each showing the prop value or `—` when empty), and a final `Total` row rendering `formatPrice(total ?? 0, currency)` styled `text-h6` (Heading 20/600). Row labels use Body 16px (`text-body1`); empty placeholders and any secondary text use `text-subtitle2` forced weight 400 + `text-grey-7`. NEVER `text-caption`/12px. Purely presentational: no API calls, no `createLogger`, no store. Optionally accept a `debtWarning?: string | null` prop to render a small `warning`-colored chip (the summary-panel debt indicator per UI-SPEC step 1) — render nothing when null. Use only Quasar brand classes for color, never a hard-coded terracotta hex (staging marker relies on `--q-primary`).
  </action>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit 2>&1 | grep -i "CobroResumen" || echo "no CobroResumen type errors"</automated>
  </verify>
  <acceptance_criteria>
    - File `el-templo-admin/src/components/caja/CobroResumen.vue` exists with `<script setup lang="ts">` and defined props including `total` and `currency`.
    - `grep -c "text-caption" el-templo-admin/src/components/caja/CobroResumen.vue` equals 0.
    - `grep -n "formatPrice" el-templo-admin/src/components/caja/CobroResumen.vue` shows the total uses it.
    - `grep -n "text-h6" el-templo-admin/src/components/caja/CobroResumen.vue` shows the Total row uses Heading 20/600.
    - No hard-coded `#` hex color in the component (`grep -c "#[0-9a-fA-F]\{6\}" ...` equals 0).
  </acceptance_criteria>
  <done>A reusable presentational summary exists that the wizard mounts twice (desktop panel + step 4).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                                                  |
| -------------------------- | -------------------------------------------------------------------------------------------- |
| admin SPA → coach-load API | The composable forwards bankAccountId; the authoritative validation is server-side (Plan 01) |

## STRIDE Threat Register

| Threat ID | Category               | Component                                 | Disposition | Mitigation Plan                                                                                                                                      |
| --------- | ---------------------- | ----------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-151-05  | Tampering              | bankAccountId forwarded by the composable | accept      | The client value is untrusted by design; server-side `assertChosenBankAccount` (Plan 01) is the authority. The composable adds no client-side trust. |
| T-151-06  | Information disclosure | listBankAccounts response                 | accept      | Endpoint returns only `{id,name,currency}` (no balances); gated by FINANCE_LOAD_ROLES server-side (Plan 01).                                         |
| T-151-SC  | Tampering              | package installs                          | accept      | No new dependencies added; reuses existing composable + Quasar components                                                                            |

</threat_model>

<verification>
- Admin typecheck passes.
- Composable exposes listBankAccounts and the 3 inputs carry bankAccountId.
- CobroResumen renders the four summary rows with the UI-SPEC typography (no 12px).
</verification>

<success_criteria>
The wizard's data-layer contract (bankAccountId + listBankAccounts) and its shared summary component exist and typecheck, ready for Plans 03/04 to consume.
</success_criteria>

<output>
Create `.planning/phases/151-registrar-cobro-pagos-cobros/151-02-SUMMARY.md` when done.
</output>
