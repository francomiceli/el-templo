---
phase: 151-registrar-cobro-pagos-cobros
plan: 04
type: execute
wave: 3
depends_on: [151-01, 151-02, 151-03]
files_modified:
  - el-templo-admin/src/pages/CobrosPage.vue
autonomous: true
requirements: [COBRO-04]

must_haves:
  truths:
    - "In step 3, choosing Transferencia or Tarjeta reveals a bank-account selector; Efectivo never shows it"
    - "The selector lists only active bank accounts of the charge currency"
    - "Selecting an account sends bankAccountId in the confirm payload; transfer/card cannot finish without one"
    - "admin/owner see a '+ Nueva cuenta' action (even when accounts exist) opening CuentaBancariaFormDialog with the charge currency preselected"
    - "profe/recepción with no accounts see a notice and cannot finish by transfer/card, but Efectivo stays available"
  artifacts:
    - path: "el-templo-admin/src/pages/CobrosPage.vue"
      provides: "step-3 bank-account selector + quick-create wiring + confirm sends bankAccountId"
      contains: "listBankAccounts"
  key_links:
    - from: "CobrosPage.vue step 3 selector"
      to: "useFinanceLoadApi.listBankAccounts(chargeCurrency)"
      via: "load active bank accounts on entering step 3 / method change"
      pattern: "listBankAccounts"
    - from: "CobrosPage.vue '+ Nueva cuenta'"
      to: "CuentaBancariaFormDialog"
      via: "dialog with charge currency preselected, gated on admin/owner"
      pattern: "CuentaBancariaFormDialog"
---

<objective>
Add the COBRO-04 frontend: a bank-account selector in step 3 shown only for Transferencia/Tarjeta, filtered to active bank accounts of the charge currency, that blocks finishing without a selection and sends `bankAccountId` in the confirm payload. admin/owner get an inline `+ Nueva cuenta` quick-create (reusing phase-150's `CuentaBancariaFormDialog`); profe/recepción with no accounts get a notice and can only finish by Efectivo.

Purpose: Complete COBRO-04 on the client, on top of the step-3 built in Plan 03, using the composable + endpoint from Plans 01/02. Server-side validation is the real authority; the UI only guides and hides the create button.
Output: step-3 selector + states + quick-create + confirm-payload wiring in `CobrosPage.vue`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/151-registrar-cobro-pagos-cobros/151-CONTEXT.md
@.planning/phases/151-registrar-cobro-pagos-cobros/151-PATTERNS.md
@.planning/phases/151-registrar-cobro-pagos-cobros/151-UI-SPEC.md

<interfaces>
<!-- useFinanceLoadApi (Plan 02) -->
- `listBankAccounts(currency?) : Promise<{ accounts: Array<{ id:number; name:string; currency:string }> }>`.
- `bankAccountId?: number` field is already on `CoachPayPlanInput` / `CoachMiscChargeInput` / `CoachAltaInput`; the composable forwards it in the body.

<!-- CuentaBancariaFormDialog.vue (phase 150) — reuse for quick-create -->

- Mounted with props `modelValue` (v-model open), `selectedCountry`, `isOwner`, optional `account`; emits `saved`. On `saved`, re-fetch `listBankAccounts` and select the new account. Preselect the charge currency in the dialog.

<!-- Auth (useAuthStore) -->

- Use the store already imported in CobrosPage to derive admin/owner (isOwner / ADMIN role) — this gates ONLY whether the `+ Nueva cuenta` button renders. The real gate is the API `ADMIN_ROLES` check on the create route (149 D-04).

<!-- UI-SPEC bank-account selector states (§Bank-account selector states — locked) -->

- Accounts exist (any role): q-select of active bank accounts of the charge currency, label `Cuenta banco`. `+ Nueva cuenta` shown even when accounts exist (convenience) as a trailing dropdown action AND an inline text button — rendered only for admin/owner.
- No accounts of the charge currency, admin/owner: empty state `No hay cuentas de esta moneda. Creá una para continuar.` + `Crear cuenta` button opening the dialog with currency preselected.
- No accounts, profe/recepción: `Todavía no hay cuentas bancarias cargadas. Pedile al dueño que cargue una para cobrar por transferencia o tarjeta. Podés cobrar en efectivo.` (warning) + disable finishing by transfer/card; Efectivo stays available.
- Block copy when transfer/card w/o account: `Elegí una cuenta bancaria para cobrar por transferencia o tarjeta.`
- Typography/color contract from 151-UI-SPEC.md: no `text-caption`/12px; warning notices `bg-warning text-dark`; `+ Nueva cuenta` is a text button (accent). No hard-coded hex.
  </interfaces>

@el-templo-admin/src/pages/CobrosPage.vue
@el-templo-admin/src/components/caja/CuentaBancariaFormDialog.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bank-account selector + states in step 3, block finish + send bankAccountId</name>
  <files>el-templo-admin/src/pages/CobrosPage.vue</files>
  <read_first>
    - el-templo-admin/src/pages/CobrosPage.vue (step 3 built in Plan 03 — payment-method buttons, Monto, the confirm dispatch and idempotency handling; the charge-currency source per association)
    - el-templo-admin/src/components/caja/BandejaPendientesTab.vue (cajaOptions computed ~557-570 and label ~572-574 — mirror the banco+currency+active filter and `Cuenta banco` label)
    - .planning/phases/151-registrar-cobro-pagos-cobros/151-UI-SPEC.md (Bank-account selector states — locked)
  </read_first>
  <action>
    In step 3, add reactive state `bankAccounts` and `selectedBankAccountId`. When the page enters step 3 (or the payment method changes to transfer/card, or the charge currency changes) call `financeApi.listBankAccounts(chargeCurrency)` and store the result; clear `selectedBankAccountId` when the currency changes so a stale-currency account can't leak. Render a `q-select` (label `Cuenta banco`) of the returned accounts ONLY when `paymentMethod ∈ {transfer, card}` — Efectivo never renders it. When accounts exist, populate options `{ label: name, value: id }`. Gate `Continuar`/`Confirmar` in step 3: for transfer/card, disable finishing unless `selectedBankAccountId` is set; surface the block message `Elegí una cuenta bancaria para cobrar por transferencia o tarjeta.` (as helper/hint, not a blocking error until an attempted advance). In the confirm dispatch (all 3 endpoints), include `bankAccountId: selectedBankAccountId` in the body ONLY when method is transfer/card; never for Efectivo. Empty-currency-accounts state: if admin/owner → show `No hay cuentas de esta moneda. Creá una para continuar.` (handled with the quick-create button in Task 2); if profe/recepción → show the warning notice `Todavía no hay cuentas bancarias cargadas...` and keep Efectivo selectable while disabling transfer/card finish. Use `bg-warning text-dark` for notices; no `text-caption`; no hard-coded hex; `createLogger`, never console.
  </action>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit 2>&1 | grep -i "CobrosPage" || echo "no CobrosPage type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "listBankAccounts\|selectedBankAccountId\|Cuenta banco" el-templo-admin/src/pages/CobrosPage.vue` shows the selector wiring.
    - `grep -n "Elegí una cuenta bancaria" el-templo-admin/src/pages/CobrosPage.vue` shows the block copy.
    - `grep -n "bankAccountId" el-templo-admin/src/pages/CobrosPage.vue` shows it added to the confirm payload for transfer/card.
    - `grep -c "text-caption" el-templo-admin/src/pages/CobrosPage.vue` equals 0.
    - `npx vue-tsc --noEmit` reports no CobrosPage type errors.
  </acceptance_criteria>
  <done>Step 3 shows a currency-filtered bank selector for transfer/card only, blocks finishing without one, and sends bankAccountId; Efectivo path is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Inline quick-create via CuentaBancariaFormDialog, admin/owner-gated (D-08)</name>
  <files>el-templo-admin/src/pages/CobrosPage.vue</files>
  <read_first>
    - el-templo-admin/src/pages/CobrosPage.vue (the selector + states from Task 1; the useAuthStore usage for role/isOwner)
    - el-templo-admin/src/components/caja/CuentaBancariaFormDialog.vue (props modelValue/selectedCountry/isOwner/account, `saved` emit, how currency/country is set)
  </read_first>
  <action>
    Import and mount `CuentaBancariaFormDialog` in `CobrosPage.vue`. Add a `showCuentaDialog` ref. Render a `+ Nueva cuenta` text button (accent) next to the selector AND expose it as a trailing action in the q-select dropdown — but render both ONLY for admin/owner (derive from `useAuthStore`; the dialog's own `isOwner` prop still passes through). In the admin/owner empty-currency state, render the `Crear cuenta` button opening the same dialog. Open the dialog with the charge currency preselected (and the appropriate country/currency prop the dialog expects). On the dialog's `saved` emit, close it, re-call `financeApi.listBankAccounts(chargeCurrency)`, and auto-select the newly created account. For profe/recepción, the create button is NOT rendered (matches the notice from Task 1) — the real security gate is the API `ADMIN_ROLES` check on the create route (149 D-04); the UI only hides the button. No `console.*`; errors via `createLogger('cobros').error(...)` → `$q.notify({ type:'negative', ... })`.
  </action>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit 2>&1 | grep -i "CobrosPage" || echo "no CobrosPage type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "CuentaBancariaFormDialog" el-templo-admin/src/pages/CobrosPage.vue` shows import + mount.
    - `grep -n "Nueva cuenta\|Crear cuenta" el-templo-admin/src/pages/CobrosPage.vue` shows the quick-create trigger(s).
    - `grep -n "@saved\|saved" el-templo-admin/src/pages/CobrosPage.vue` shows the saved handler that refetches + auto-selects.
    - The create button rendering is guarded by an admin/owner condition (grep shows an `isOwner`/role guard around the button, e.g. `v-if=`).
    - `npx vue-tsc --noEmit` reports no CobrosPage type errors.
  </acceptance_criteria>
  <done>admin/owner can create a bank account inline (currency preselected) and have it auto-selected; profe/recepción never see the button and fall back to Efectivo.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                              | Description                                                                                   |
| ----------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| admin SPA → coach-load API / cash-register create API | UI sends bankAccountId; UI hides the create button. The authoritative checks are server-side. |

## STRIDE Threat Register

| Threat ID | Category       | Component                                  | Disposition | Mitigation Plan                                                                                                                                                                               |
| --------- | -------------- | ------------------------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-151-04  | Elevation      | quick-create '+ Nueva cuenta'              | mitigate    | The button is hidden for non-admin/owner, but the real gate is the API `ADMIN_ROLES` check on the phase-150 create route (149 D-04); hiding the button is UX only, not the security boundary. |
| T-151-09  | Tampering/IDOR | selectedBankAccountId in confirm payload   | mitigate    | The chosen id is re-validated server-side by `assertChosenBankAccount` (Plan 01, banco+active+currency); a tampered/foreign id → 400. UI filtering is convenience, not trust.                 |
| T-151-10  | Business-logic | transfer/card finishing without an account | mitigate    | UI disables finishing; the server independently rejects transfer/card without a valid bankAccountId (Plan 01), so a bypassed UI still fails 400.                                              |
| T-151-SC  | Tampering      | package installs                           | accept      | No new dependencies; reuses phase-150 dialog + existing composable                                                                                                                            |

</threat_model>

<verification>
- Admin typecheck passes.
- Selector appears only for transfer/card, filtered to active accounts of the charge currency.
- Finishing by transfer/card is blocked without a selected account; bankAccountId is sent on confirm.
- admin/owner see quick-create (currency preselected, auto-select on save); profe/recepción see the notice and keep Efectivo.
</verification>

<success_criteria>
COBRO-04 frontend complete: transfer/card require a selected bank account, quick-create is admin/owner-only inline, and the payload carries bankAccountId — backed by the server-side authority from Plan 01.
</success_criteria>

<output>
Create `.planning/phases/151-registrar-cobro-pagos-cobros/151-04-SUMMARY.md` when done.
</output>
