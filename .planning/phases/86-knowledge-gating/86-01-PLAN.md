---
phase: 86-knowledge-gating
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-bot/test/fixtures/pb1-e1a-baseline.txt
  - el-templo-bot/test/fixtures/pb1-e1a-baseline.ts
autonomous: true
requirements: [KGATE-05]
must_haves:
  truths:
    - "A text file exists containing the full rendered PB1.E1A system prompt as produced by the CURRENT (pre-refactor) code"
    - "A TypeScript constant BASELINE_CHARS exports the character count of that fixture for use by the regression test"
    - "Re-running the capture script against current source reproduces the committed fixture byte-for-byte (deterministic)"
  artifacts:
    - path: "el-templo-bot/test/fixtures/pb1-e1a-baseline.txt"
      provides: "Frozen pre-refactor PB1.E1A rendered system prompt"
      min_lines: 100
    - path: "el-templo-bot/test/fixtures/pb1-e1a-baseline.ts"
      provides: "BASELINE_CHARS constant for prompt-size regression test"
      exports: ["BASELINE_CHARS"]
  key_links:
    - from: "el-templo-bot/test/fixtures/pb1-e1a-baseline.ts"
      to: "el-templo-bot/test/fixtures/pb1-e1a-baseline.txt"
      via: "BASELINE_CHARS = char count of fixture file"
      pattern: "BASELINE_CHARS\\s*="
---

<objective>
Capture the pre-refactor PB1.E1A rendered system prompt as a frozen fixture, along with a TypeScript constant holding its character count. This baseline is the anchor for the KGATE-05 ≥35% reduction assertion in Plan 03.

Purpose: Lock the "before" size number BEFORE any refactor touches knowledge.ts. If we captured the baseline after refactoring, the number would be meaningless.
Output: Two committed fixtures — the raw prompt text and a TS constant re-exporting its length.
</objective>

<execution_context>
@/Users/bores/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bores/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-knowledge-gating/86-CONTEXT.md

@el-templo-bot/src/ai/system-prompt.ts
@el-templo-bot/src/ai/knowledge.ts
@el-templo-bot/src/playbooks/definitions.ts
@el-templo-bot/src/state/machine.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture pre-refactor PB1.E1A baseline fixture</name>
  <files>
    el-templo-bot/test/fixtures/pb1-e1a-baseline.txt
    el-templo-bot/test/fixtures/pb1-e1a-baseline.ts
  </files>
  <action>
Create a one-shot capture script (inline in the task, not committed as a permanent file) that imports `getSystemPrompt` from `el-templo-bot/src/ai/system-prompt.ts` and calls it with the synthetic PB1.E1A lead input:

```ts
getSystemPrompt({
  clientState: "lead",
  activePlaybook: "PB1",
  currentStage: "E1A",
  // currentAvatar intentionally undefined — PB1 pre-detection state
  // profileContext intentionally undefined — no DB lookup
});
```

Run the script (via `tsx` or equivalent — use the bot's existing tooling) and write the raw returned string to `el-templo-bot/test/fixtures/pb1-e1a-baseline.txt` (no trimming, no normalization, byte-exact).

Then create `el-templo-bot/test/fixtures/pb1-e1a-baseline.ts` with:

```ts
/**
 * Character count of the pre-refactor PB1.E1A rendered system prompt,
 * captured at phase 86 baseline time. Used by the prompt-size regression
 * test (KGATE-05) to assert the refactored prompt is ≥35% smaller.
 *
 * DO NOT edit this number by hand. If the baseline fixture is regenerated
 * against pre-refactor source, recompute this value from the new file.
 */
export const BASELINE_CHARS = <exact-length-of-.txt-file>;
```

Replace `<exact-length-of-.txt-file>` with the actual character count of the .txt file (use `fs.readFileSync(path, 'utf8').length`, NOT byte size — the regression test will compare string lengths).

Important:

- Run the capture against CURRENT source (pre-refactor). If any of knowledge.ts / system-prompt.ts have uncommitted changes from this phase's work, abort.
- Do NOT commit the capture script itself — it's one-shot. The .txt + .ts fixtures are the permanent artifacts.
- Verify the .txt starts with "Soy _Mica_" and contains the full 12 knowledge sections (includes "Estrategias de Retencion" AND "App (DeportNet)" — these are the sections that will be gated out by plan 02).

The prompt-size test in plan 03 will: render the refactored PB1.E1A prompt, measure its length, and assert `length <= BASELINE_CHARS * 0.65`.
</action>
<verify>
<automated>cd el-templo-bot &amp;&amp; node -e "const fs=require('fs'); const txt=fs.readFileSync('test/fixtures/pb1-e1a-baseline.txt','utf8'); const mod=require('./test/fixtures/pb1-e1a-baseline.ts'); if(txt.length !== mod.BASELINE_CHARS) { console.error('MISMATCH', txt.length, mod.BASELINE_CHARS); process.exit(1); } if(!txt.startsWith('Soy _Mica_')) process.exit(2); if(!txt.includes('Estrategias de Retencion')) process.exit(3); if(!txt.includes('App (DeportNet)') &amp;&amp; !txt.includes('Ayuda con la App')) process.exit(4); console.log('OK', txt.length);"</automated>
<manual>Confirm fixture contains PB1 stage content (should include "Playbook activo: PB1" or similar marker from system-prompt.ts)</manual>
<sampling_rate>run after task commits, before plan 02 begins</sampling_rate>
</verify>
<done>

- `test/fixtures/pb1-e1a-baseline.txt` exists with pre-refactor full rendered prompt (includes retention, app-help, policies sections)
- `test/fixtures/pb1-e1a-baseline.ts` exports `BASELINE_CHARS` matching the .txt length exactly
- `BASELINE_CHARS` is a realistic number (roughly 15,000–25,000 range based on QT17 audit; treat anything below 10k or above 40k as suspicious and re-run capture)
- Both files committed
  </done>
  </task>

</tasks>

<verification>
1. .txt and .ts are in sync (automated check above).
2. .txt is recognizably a full v5.2-shaped prompt (contains all 12 knowledge sections).
3. No edits to `el-templo-bot/src/**` in this plan's diff — only fixture additions.
</verification>

<success_criteria>

- BASELINE_CHARS is locked and committed.
- Plan 03 can import `BASELINE_CHARS` from `test/fixtures/pb1-e1a-baseline.ts` without any further computation.
- The fixture captures pre-refactor state, giving KGATE-05 a meaningful reference point.
  </success_criteria>

<output>
After completion, create `.planning/phases/86-knowledge-gating/86-01-SUMMARY.md` with:
- BASELINE_CHARS value
- Sections observed in fixture (confirm all 12)
- Any surprises (e.g., unexpected prompt length)
</output>
