---
phase: 86-knowledge-gating
plan: 02
type: execute
wave: 2
depends_on: [86-01]
files_modified:
  - el-templo-bot/src/ai/knowledge.ts
  - el-templo-bot/src/ai/system-prompt.ts
autonomous: true
requirements: [KGATE-01, KGATE-02, KGATE-03, KGATE-04, KGATE-06]
must_haves:
  truths:
    - "getBusinessKnowledge() with no argument returns the full 12-section knowledge string (backward compat)"
    - "getBusinessKnowledge('lead') returns ONLY discovery-tagged sections: Que es El Templo, Planes y Precios (base), Reglas Zero, Horarios por Sede, Clase de Prueba, Tecnicas de Venta, Objeciones de venta, Reglas de Oro"
    - "getBusinessKnowledge('lead') excludes: ROM, Mejora de plan, App (DeportNet), Politicas, Objeciones de retención, Estrategias de Retencion"
    - "getBusinessKnowledge('trial' | 'active_member' | 'inactive_member' | 'expired_member') returns the full set (identical content to no-arg call)"
    - "Output preserves the original 1–12 section order (filtered, not reordered)"
    - "system-prompt.ts passes clientState through to getBusinessKnowledge — zero rewrite of the base prompt text"
    - "Planes y Precios is split into 'Planes y Precios' (discovery) and 'Mejora de plan' (full-only) — content preserved, no text lost"
    - "Manejo de Objeciones is split into 'Objeciones de venta' (discovery) and 'Objeciones de retención' (full-only) — all 8 current objections distributed across the two, none lost"
  artifacts:
    - path: "el-templo-bot/src/ai/knowledge.ts"
      provides: "Tagged section array + state-gated getBusinessKnowledge(clientState?)"
      contains: "tags"
    - path: "el-templo-bot/src/ai/system-prompt.ts"
      provides: "Call site passes clientState through"
      contains: "getBusinessKnowledge(options"
  key_links:
    - from: "el-templo-bot/src/ai/system-prompt.ts"
      to: "el-templo-bot/src/ai/knowledge.ts::getBusinessKnowledge"
      via: "getBusinessKnowledge(options?.clientState)"
      pattern: "getBusinessKnowledge\\(options\\?\\.clientState\\)"
    - from: "el-templo-bot/src/ai/knowledge.ts"
      to: "el-templo-bot/src/state/machine.ts::ClientState"
      via: "import type { ClientState }"
      pattern: "import\\s+type\\s+\\{\\s*ClientState"
---

<objective>
Refactor `knowledge.ts` from an inline `sections.push(...)` array into a structured `SECTIONS: ReadonlyArray<{ title, body, tags }>` table, split two sections along the discovery/member boundary, tag discovery-relevant sections, and make `getBusinessKnowledge` accept an optional `ClientState` that filters to discovery-only for `'lead'`. Update `system-prompt.ts` with a one-line call-site change.

Purpose: Structural fix for QT17 price leakage and attention dilution — PB1 leads stop seeing retention, upgrade, and member-policy content that dilutes the discovery focus. Non-lead states are unchanged (zero regression).
Output: Tagged section structure in knowledge.ts, gated output for leads, backward-compat for everyone else.
</objective>

<execution_context>
@/Users/bores/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bores/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-knowledge-gating/86-CONTEXT.md
@.planning/phases/86-knowledge-gating/86-01-SUMMARY.md

@el-templo-bot/src/ai/knowledge.ts
@el-templo-bot/src/ai/system-prompt.ts
@el-templo-bot/src/state/machine.ts
@el-templo-bot/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor knowledge.ts to tagged section array with splits + state filter</name>
  <files>el-templo-bot/src/ai/knowledge.ts</files>
  <action>
Replace the current inline `sections.push(...)` body of `getBusinessKnowledge` with a module-level ordered array of tagged section objects, then derive output by filtering.

**1. Add type + import at the top of the file (below existing header comment):**

```ts
import type { ClientState } from "../state/machine.js";

/** Tag vocabulary is intentionally narrow for v5.3.1. Future tags
 *  (member-only, admin, locale) can be added without renaming. A section
 *  is "discovery-relevant" iff it carries the 'discovery' tag. */
type SectionTag = "discovery";

interface KnowledgeSection {
  /** Human-readable title used only for debugging / tests. Not rendered. */
  title: string;
  /** Full formatted body including its own *bold* heading and content. */
  body: string;
  /** Tags. Presence of 'discovery' = include for `clientState === 'lead'`. */
  tags: ReadonlyArray<SectionTag>;
}
```

**2. Construct a module-level `const SECTIONS: ReadonlyArray<KnowledgeSection>` BELOW the existing format helpers (formatPrice, formatPlan, formatSchedule, formatCreditCardPlans).** Preserve the existing 12-section order, but with the two splits below. Each entry's `body` is the exact string that was previously `sections.push(...)`'d.

Section order + tags:

1. `Que es El Templo` — tags: `['discovery']` (body: unchanged from current section 1)
2. `ROM` — tags: `[]` (body: unchanged current section 2)
3. `Planes y Precios` — tags: `['discovery']` — body is current section 3 BUT with the `*Mejora de plan:*\n${UPGRADE_PATHS}` block REMOVED (ends after the credit-card-plans block)
4. `Mejora de plan` — tags: `[]` — NEW section body: `*Mejora de plan*\n\n${UPGRADE_PATHS}` (reuses the existing UPGRADE_PATHS constant verbatim)
5. `Reglas Zero` — tags: `['discovery']` (unchanged)
6. `Horarios por Sede` — tags: `['discovery']` (unchanged)
7. `Clase de Prueba` — tags: `['discovery']` (unchanged)
8. `App (DeportNet)` — tags: `[]` (unchanged)
9. `Politicas` — tags: `[]` (unchanged)
10. `Tecnicas de Venta` — tags: `['discovery']` (unchanged)
11. `Objeciones de venta` — tags: `['discovery']` — NEW body: current `*Manejo de Objeciones*` heading text + objection blocks 1, 2, 3, 4, 5, 6, 7 (caro, tiempo, miedo, pensarlo, otro lado, lejos, pagar por clase) extracted from the existing `OBJECTION_HANDLING` constant. Title the rendered block `*Objeciones de venta*` (not `*Manejo de Objeciones*`).
12. `Objeciones de retención` — tags: `[]` — NEW body: title `*Objeciones de retención*` + objection block 8 (no me convencio / tengo dudas) from current OBJECTION_HANDLING.
13. `Estrategias de Retencion` — tags: `[]` (unchanged from current section 11)
14. `Reglas de Oro` — tags: `['discovery']` (unchanged — universal, carries discovery tag so leads still see it)

**Critical: handling the OBJECTION_HANDLING split.** The existing `OBJECTION_HANDLING` constant is one big template literal with numbered objections 1–8. Rather than mutating it, inline the split inside the SECTIONS array OR split `OBJECTION_HANDLING` into two new constants `OBJECTIONS_SALES` (1–7) and `OBJECTIONS_RETENTION` (8) at the source. Prefer the latter for readability. Mica's existing content must be preserved verbatim — same wording, same numbering within each sub-list is fine (retention can renumber 8 → 1, or keep 8; keep 8 to minimize content diff).

**Critical: handling the Planes y Precios split.** The existing section-3 template literal ends with `*Mejora de plan:*\n${UPGRADE_PATHS}`. Remove those two lines from the section 3 body. `UPGRADE_PATHS` constant stays where it is (it is still referenced by the new `Mejora de plan` section).

**3. Rewrite the `getBusinessKnowledge` function:**

```ts
export function getBusinessKnowledge(clientState?: ClientState): string {
  const selected =
    clientState === "lead"
      ? SECTIONS.filter((s) => s.tags.includes("discovery"))
      : SECTIONS;
  return selected.map((s) => s.body).join("\n\n");
}
```

Notes:

- `clientState` undefined/null → full set (backward compat, KGATE-04).
- Any non-'lead' ClientState → full set (KGATE-03).
- Unknown runtime string → TypeScript prevents it; at runtime the `=== 'lead'` check fails and we fall through to full set (defensive safety net per CONTEXT).
- Return type stays `string`. Order preserved by filter-in-place.

**4. Update the file's top docstring** to reflect: sections now carry tags, `clientState === 'lead'` returns discovery-only subset, all other states and no-arg calls return the full (now 14-entry) set. Mention the two splits.

**Constraints:**

- Do NOT change the wording of any existing section body (apart from the two splits: removing "_Mejora de plan:_" line from section 3, and partitioning OBJECTION_HANDLING into sales/retention).
- Do NOT add a method description section (deferred to Phase 87 per CONTEXT `<deferred>`).
- Do NOT consolidate Boarding Pass mentions (deferred to Phase 87).
- Do NOT introduce `any`. `unknown` + narrowing or proper types only (project CLAUDE.md rule).
- Do NOT use `console.log` (project CLAUDE.md rule).
- `SECTIONS` can be module-private (not exported) — tests will go through `getBusinessKnowledge` only. If a future test needs introspection, export then.
  </action>
  <verify>
  <automated>cd el-templo-bot &amp;&amp; pnpm tsc --noEmit &amp;&amp; node -e "const {getBusinessKnowledge} = require('./dist/ai/knowledge.js') || require('./src/ai/knowledge.ts'); const full = getBusinessKnowledge(); const lead = getBusinessKnowledge('lead'); const trial = getBusinessKnowledge('trial'); if (lead.length >= full.length) { console.error('lead not smaller'); process.exit(1); } if (trial !== full) { console.error('trial should equal full'); process.exit(2); } if (lead.includes('Estrategias de Retencion')) { console.error('lead leaked retention'); process.exit(3); } if (lead.includes('Ayuda con la App')) { console.error('lead leaked app'); process.exit(4); } if (lead.includes('Mejora de plan')) { console.error('lead leaked upgrade paths'); process.exit(5); } if (!lead.includes('Planes Flex')) { console.error('lead missing base pricing'); process.exit(6); } if (!full.includes('Estrategias de Retencion')) { console.error('full lost retention'); process.exit(7); } console.log('OK full', full.length, 'lead', lead.length);"</automated>
  <sampling_rate>run after task commits, before Task 2 begins</sampling_rate>
  </verify>
  <done>
- `getBusinessKnowledge()` returns full 14-entry (post-split) content
- `getBusinessKnowledge('lead')` returns only discovery-tagged sections
- `getBusinessKnowledge('trial' | 'active_member' | 'inactive_member' | 'expired_member')` returns the full set (string equality with no-arg call)
- `tsc --noEmit` passes (strict mode, no `any`)
- No other files in `el-templo-bot/src/` are modified
  </done>
  </task>

<task type="auto">
  <name>Task 2: Wire clientState through system-prompt.ts call site</name>
  <files>el-templo-bot/src/ai/system-prompt.ts</files>
  <action>
Change the single call site at line ~215 in `getSystemPrompt`:

Before:

```ts
${getBusinessKnowledge()}
```

After:

```ts
${getBusinessKnowledge(options?.clientState)}
```

That is the ONLY source change in this task.

Do NOT:

- Rewrite the base prompt text
- Add any other parameter plumbing
- Modify STATE_SECTIONS, AVATAR_TONE_GUIDES, playbook rendering, or profile-detection directive
- Touch resolver.ts, advance.ts, definitions.ts, handler.ts, or any other file (hard constraint per ROADMAP)

Per KGATE-06: minimal change. One character-range diff.

Rationale: `options?.clientState` is already the typed `ClientState | undefined` field that the handler passes in. When the handler doesn't set it (e.g., background tasks without a resolved state), `undefined` flows through and `getBusinessKnowledge` returns the full set (KGATE-04).
</action>
<verify>
<automated>cd el-templo-bot &amp;&amp; pnpm tsc --noEmit &amp;&amp; grep -n "getBusinessKnowledge(options?.clientState)" src/ai/system-prompt.ts &amp;&amp; ! grep -n "getBusinessKnowledge()" src/ai/system-prompt.ts</automated>
<manual>Diff on system-prompt.ts should be a single-line change inside the base template literal.</manual>
<sampling_rate>run after task commits</sampling_rate>
</verify>
<done>

- `src/ai/system-prompt.ts` line ~215 now reads `${getBusinessKnowledge(options?.clientState)}`
- No other lines in the file changed
- `pnpm tsc --noEmit` passes
  </done>
  </task>

</tasks>

<verification>
1. `cd el-templo-bot && pnpm tsc --noEmit` — clean
2. `cd el-templo-bot && pnpm lint` — clean (no `any`, no `console.log`)
3. The full existing test suite (502 tests per QREG-01) is NOT yet re-run in this plan — that is phase 88's job. But `cd el-templo-api && pnpm test` should still be green because this phase only touches bot files and existing bot tests call `getBusinessKnowledge()` with no args (the backward-compat path).
4. Optional spot check: `cd el-templo-bot && pnpm vitest run knowledge.test.ts` — existing knowledge tests should pass unchanged (they hit the no-arg path).
</verification>

<success_criteria>

- KGATE-01 satisfied: `getBusinessKnowledge(clientState)` signature accepts ClientState
- KGATE-02 satisfied: PB1 leads (clientState='lead') get only discovery sections
- KGATE-03 satisfied: trial/active/inactive/expired get full set
- KGATE-04 satisfied: no-arg call returns full set (unchanged behavior)
- KGATE-06 satisfied: system-prompt.ts diff is a single-line change
- Zero regression on existing knowledge.test.ts assertions
  </success_criteria>

<output>
After completion, create `.planning/phases/86-knowledge-gating/86-02-SUMMARY.md` with:
- Final SECTIONS count (should be 14 post-split)
- Discovery-tagged section count (should be 8: Que es, Planes y Precios base, Reglas Zero, Horarios, Clase de Prueba, Tecnicas de Venta, Objeciones de venta, Reglas de Oro)
- Observed length of `getBusinessKnowledge('lead')` vs `getBusinessKnowledge()` (absolute chars + %)
- Any non-trivial decisions made in Claude's discretion zone (e.g., whether SECTIONS was exported, helper names chosen)
</output>
