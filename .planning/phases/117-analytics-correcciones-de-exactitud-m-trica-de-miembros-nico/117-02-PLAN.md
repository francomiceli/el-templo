---
phase: 117-analytics-correcciones-de-exactitud-m-trica-de-miembros-nico
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/schema/user-status-history.ts
  - el-templo-api/src/db/schema/index.ts
  - el-templo-api/src/db/migrations/0128_create_user_status_history.sql
  - el-templo-api/src/db/migrations/0129_backfill_user_status_history.sql
  - el-templo-api/src/modules/subscriptions/service.ts
  - el-templo-api/test/subscriptions/user-status-history.test.ts
autonomous: false
requirements: [D-10]
must_haves:
  truths:
    - "Existe la tabla user_status_history que registra transiciones forward de users.status"
    - "Cuando recomputeUserStatus cambia el status de un user, se inserta una fila (from_status, to_status, changed_at)"
    - "El backfill aproximado pobló historial inicial desde users.created_at + primera subscriptions.created_at, idempotente"
  artifacts:
    - path: "el-templo-api/src/db/schema/user-status-history.ts"
      provides: "Drizzle schema de la tabla nueva (FK user_id CASCADE, índice por user_id, from/to status via userStatusEnum)"
      contains: "user_status_history"
    - path: "el-templo-api/src/db/migrations/0128_create_user_status_history.sql"
      provides: "DDL CREATE TABLE + fila en _migrations"
      contains: "CREATE TABLE"
    - path: "el-templo-api/src/db/migrations/0129_backfill_user_status_history.sql"
      provides: "Backfill aproximado idempotente"
      contains: "WHERE NOT EXISTS"
  key_links:
    - from: "el-templo-api/src/modules/subscriptions/service.ts recomputeUserStatus"
      to: "user_status_history"
      via: "INSERT forward-only tras detectar cambio de status"
      pattern: "user_status_history"
    - from: "el-templo-api/src/db/schema/index.ts"
      to: "user-status-history.ts"
      via: "export *"
      pattern: "user-status-history"
---

<objective>
Crear la tabla `user_status_history` como FUNDACIÓN (se consume en Fase 118: funnel +
retención). Registrar transiciones forward de `users.status` vía hook en
`recomputeUserStatus` + backfill aproximado hacia atrás. NO se consume en esta fase.

Purpose: La conversión freemium→prueba→activo y la retención por cohortes de Fase 118
necesitan historial de status que hoy no existe. Empezar a acumularlo ya, sin esperar.

Output: schema Drizzle + migración DDL (0128) + migración de backfill (0129) + hook
forward-only en recomputeUserStatus + tests de integración.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-analytics-correcciones-de-exactitud-m-trica-de-miembros-nico/117-CONTEXT.md
@.planning/phases/117-analytics-correcciones-de-exactitud-m-trica-de-miembros-nico/117-PATTERNS.md

<interfaces>
refresh-tokens.ts (analog exacto de tabla nueva): mysqlTable con int autoincrement id,
int user_id references users.id onDelete cascade notNull, timestamp createdAt defaultNow,
index idx_<tabla>_user_id. Registrada en db/schema/index.ts vía `export * from "./refresh-tokens"`.

userStatusEnum (db/schema/users.ts:49): mysqlEnum("status", ['freemium','prueba','activo','inactivo']).
recomputeUserStatus (subscriptions/service.ts:4099-4190): UPDATE users SET u.status = CASE ...
ejecuta dentro de un tx; el SET evalúa LEFT-TO-RIGHT (lección Phase 114). El hook debe
leer el status ACTUAL antes del UPDATE y comparar contra el nuevo para insertar solo si cambió.
Precedente migración datos prod: 0127_fix_legacy_plan_duration_days.sql (idempotente, WHERE guards).
Migración tabla nueva: 0125_create_refresh_tokens.sql (CREATE TABLE sin IF NOT EXISTS).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema Drizzle + migración DDL 0128</name>
  <files>el-templo-api/src/db/schema/user-status-history.ts, el-templo-api/src/db/schema/index.ts, el-templo-api/src/db/migrations/0128_create_user_status_history.sql</files>
  <read_first>
    - el-templo-api/src/db/schema/refresh-tokens.ts (analog exacto de tabla nueva — copiar estructura)
    - el-templo-api/src/db/schema/index.ts:50-56 (cómo se registran schemas; agregar export *)
    - el-templo-api/src/db/schema/users.ts:49-58 (userStatusEnum — importar para from/to status)
    - el-templo-api/src/db/migrations/0125_create_refresh_tokens.sql (analog DDL — CREATE TABLE sin IF NOT EXISTS, header con fase)
  </read_first>
  <action>
    Crear `db/schema/user-status-history.ts` copiando la estructura de refresh-tokens.ts:
    columnas id (int autoincrement PK), user_id (int FK users.id onDelete cascade notNull),
    from_status (userStatusEnum, nullable — la primera transición no tiene origen),
    to_status (userStatusEnum notNull), source (varchar corto: 'recompute' | 'backfill' | 'admin' — opcional, default 'recompute', para distinguir orígenes), changed_at (timestamp defaultNow notNull).
    Índice idx_user_status_history_user_id sobre user_id; índice adicional sobre (user_id, changed_at) para las queries de cohortes de Fase 118. Importar userStatusEnum de "./users".
    Registrar en db/schema/index.ts con `export * from "./user-status-history"`.
    Escribir A MANO `db/migrations/0128_create_user_status_history.sql`: CREATE TABLE (sin IF NOT EXISTS, patrón del proyecto) con las mismas columnas/índices/FK. CRÍTICO: NUNCA `;` dentro de comentarios `--` (el runner splitea por `;` antes de strippear comentarios — rompe la migración entera). Header con rationale de Fase 117 D-10. NO usar drizzle-kit migrate; la fila en _migrations la inserta el runner del proyecto (pnpm db:migrate).
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm tsc --noEmit && grep -L ";" /dev/null; grep -n "^--" src/db/migrations/0128_create_user_status_history.sql | grep ";" && echo "FAIL: semicolon in comment" || echo "OK no semicolon in comments"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "user_status_history" el-templo-api/src/db/schema/index.ts` >= 1.
    - `grep -n "CREATE TABLE" el-templo-api/src/db/migrations/0128_create_user_status_history.sql` presente.
    - Ningún `--` comment de la migración contiene `;` (gate del verify).
    - `cd el-templo-api && pnpm tsc --noEmit` sin errores.
  </acceptance_criteria>
  <done>Schema y migración DDL existen, tsc limpio, sin `;` en comentarios SQL.</done>
</task>

<task type="auto">
  <name>Task 2: Hook forward-only en recomputeUserStatus + backfill 0129 + tests</name>
  <files>el-templo-api/src/modules/subscriptions/service.ts, el-templo-api/src/db/migrations/0129_backfill_user_status_history.sql, el-templo-api/test/subscriptions/user-status-history.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/subscriptions/service.ts:4099-4191 (recomputeUserStatus completo — el UPDATE termina en :4188-4189 WHERE u.id)
    - el-templo-api/src/db/migrations/0127_fix_legacy_plan_duration_days.sql (analog backfill idempotente con WHERE guards)
    - el-templo-api/src/db/schema/user-status-history.ts (la tabla del Task 1)
  </read_first>
  <action>
    Hook forward-only en recomputeUserStatus: ANTES del UPDATE, leer el status actual del user (SELECT u.status). Después del UPDATE, leer el nuevo status. Si cambió, INSERT en user_status_history (from_status = anterior, to_status = nuevo, source='recompute', changed_at NOW()), usando el mismo tx para que rollee con la transacción. Forward-only = solo se registra cuando el valor efectivamente cambia (no insertar si from==to). Usar this.log para trazas, nunca console.log. Cuidado con el orden LEFT-TO-RIGHT documentado en :4107-4114 — la lectura del nuevo status va después del execute del UPDATE.
    Escribir A MANO `0129_backfill_user_status_history.sql`: backfill aproximado idempotente. Para cada user, insertar el historial reconstruido: alta inicial desde users.created_at (to_status = el primer estado plausible) y la transición a 'activo' aproximada con la primera subscriptions.created_at del user (source='backfill'). Usar `INSERT ... SELECT ... WHERE NOT EXISTS (...)` para idempotencia (re-correr = 0 filas, patrón 0127/111-06). CRÍTICO: NUNCA `;` dentro de comentarios `--`. El backfill NO debe corromper datos existentes (solo INSERT, nunca UPDATE/DELETE sobre users/subscriptions). Commitear junto al schema.
    Tests en `test/subscriptions/user-status-history.test.ts` contra MySQL real (reloj real, no fake timers): (a) al crear una sub que activa al user, recomputeUserStatus inserta una fila to_status='activo'; (b) re-ejecutar recomputeUserStatus sin cambio de status NO inserta fila duplicada (forward-only); (c) rollback del tx elimina la fila de historial. Registrar user_status_history en TABLES_TO_CLEAN de helpers.ts.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm vitest run test/subscriptions/user-status-history.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "user_status_history" el-templo-api/src/modules/subscriptions/service.ts` >= 1 (hook).
    - `grep -n "WHERE NOT EXISTS" el-templo-api/src/db/migrations/0129_backfill_user_status_history.sql` presente (idempotencia).
    - `grep -n "^--" el-templo-api/src/db/migrations/0129_backfill_user_status_history.sql | grep ";"` no devuelve nada.
    - El backfill 0129 no contiene UPDATE ni DELETE sobre users/subscriptions (solo INSERT).
    - Los 3 tests pasan; user_status_history está en TABLES_TO_CLEAN.
    - `cd el-templo-api && pnpm tsc --noEmit` limpio.
  </acceptance_criteria>
  <done>Hook forward-only registra transiciones; backfill idempotente; 3 tests verdes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Migraciones 0128 (DDL user_status_history) y 0129 (backfill idempotente) escritas y aplicadas a la DB local vía `pnpm db:migrate`. El hook forward-only acumula transiciones desde ya.</what-built>
  <how-to-verify>
    1. `cd el-templo-api && pnpm db:migrate` localmente y confirmar que 0128 y 0129 aparecen en la tabla `_migrations` (SELECT name FROM _migrations WHERE name LIKE '012%').
    2. `SELECT COUNT(*) FROM user_status_history` devuelve filas tras el backfill.
    3. Re-correr `pnpm db:migrate` es no-op (idempotente, 0 filas nuevas en una segunda corrida manual del backfill).
    4. Flujo staging-first ESTRICTO: el operador corre las migraciones en staging y luego, tras sanity-check, aprueba prod. NO mergear a master sin aprobación. NO pushear sin confirmación explícita.
  </how-to-verify>
  <resume-signal>Escribir "aprobado" tras verificar local (y registrar el pendiente de staging/prod como blocker en STATE), o describir problemas.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                                                         |
| ----------------------------------------- | --------------------------------------------------------------------------------------------------- |
| migración 0129 → DB prod                  | Backfill toca datos existentes; debe ser solo INSERT, idempotente, no corromper users/subscriptions |
| recomputeUserStatus → user_status_history | Escritura dentro del tx de subscripción; debe rollear con él                                        |

## STRIDE Threat Register

| Threat ID | Category                   | Component                | Disposition | Mitigation Plan                                                                                                                                                      |
| --------- | -------------------------- | ------------------------ | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-117-03  | Tampering / Data Integrity | migración 0129 backfill  | mitigate    | INSERT ... SELECT ... WHERE NOT EXISTS (idempotente); prohibido UPDATE/DELETE sobre users/subscriptions; verificado por grep gate + checkpoint humano staging-first. |
| T-117-04  | Repudiation                | hook recomputeUserStatus | mitigate    | Inserta dentro del mismo tx que la transición → atomicidad; rollback elimina la fila (test c).                                                                       |
| T-117-SC  | Tampering                  | npm/pip/cargo installs   | mitigate    | No se instalan dependencias. Si surge necesidad, checkpoint blocking-human (regla de memoria).                                                                       |

Sin paquetes nuevos → sin gate de legitimidad de paquetes.
</threat_model>

<verification>
- `cd el-templo-api && pnpm vitest run test/subscriptions/user-status-history.test.ts` verde.
- `cd el-templo-api && pnpm tsc --noEmit` y `pnpm lint` limpios.
- Migración 0129 idempotente verificada por checkpoint humano local.
</verification>

<success_criteria>

- Tabla user_status_history creada (D-10), registrada en schema index.
- Hook forward-only acumula transiciones desde ya.
- Backfill aproximado idempotente, sin corromper datos.
- Tests de integración del hook + rollback.
  </success_criteria>

<output>
Create `.planning/phases/117-analytics-correcciones-de-exactitud-m-trica-de-miembros-nico/117-02-SUMMARY.md` when done
</output>
