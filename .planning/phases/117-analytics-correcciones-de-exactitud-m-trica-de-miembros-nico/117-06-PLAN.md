---
phase: 117-analytics-correcciones-de-exactitud-m-trica-de-miembros-nico
plan: 06
type: execute
wave: 5
depends_on: ["117-01", "117-03", "117-04", "117-05"]
files_modified:
  - el-templo-admin/src/types/analytics.ts
  - el-templo-admin/src/composables/useAnalyticsApi.ts
  - el-templo-admin/src/components/analytics/AsistenciaTab.vue
  - el-templo-admin/src/components/analytics/MiembrosTab.vue
  - el-templo-admin/src/components/analytics/FinanzasTab.vue
autonomous: false
requirements: [D-11, D-12, D-13, D-14, D-15, D-16, D-17]
must_haves:
  truths:
    - "La tab de Asistencias muestra el KPI de miembros únicos 7/14/30 como número destacado (D-11)"
    - "La tab de Asistencias muestra el conteo de activos por segmento + lista nominal en_riesgo/ghost con botón WhatsApp (D-12)"
    - "Cuando se filtra una sede con ratio de check-in <50%, se muestra un warning visual de representatividad (D-13)"
    - "La tab de Miembros muestra vencidos con buckets y daysOverdue real, tasa de renovación 7/14/30, flag ya pagó, y segmento para priorizar (D-14, D-15, D-16, D-17)"
    - "Los tipos del frontend reflejan revenue separado por moneda (ARS/EUR nunca sumados)"
  artifacts:
    - path: "el-templo-admin/src/components/analytics/AsistenciaTab.vue"
      provides: "KPI únicos 7/14/30 + segmentos + listas engagement + warning ratio <50%"
    - path: "el-templo-admin/src/components/analytics/MiembrosTab.vue"
      provides: "Vencidos con buckets + renewal rate + flags + priorización por segmento"
  key_links:
    - from: "useAnalyticsApi.ts"
      to: "/admin/analytics/* endpoints nuevos"
      via: "api.get"
      pattern: "analytics"
    - from: "MiembrosTab.vue"
      to: "wa.me deep-link"
      via: "openWhatsApp"
      pattern: "wa.me"
---

<objective>
Frontend admin de la fase: render del KPI de miembros únicos 7/14/30 (D-11), conteo de
activos por segmento + listas nominales en_riesgo/ghost con WhatsApp (D-12), warning
visual de ratio de check-in <50% por sede (D-13), panel de Vencimientos/Renovaciones
completo (D-14/15/16), y alineación de tipos a revenue por moneda (D-17). Consume los
endpoints de los Planes 01/03/04/05.

Purpose: Que recepción vea en la UI los números correctos + las worklists operativas.

Output: tipos espejo, composable, AsistenciaTab, MiembrosTab, FinanzasTab actualizados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-analytics-correcciones-de-exactitud-m-trica-de-miembros-nico/117-CONTEXT.md
@.planning/phases/117-analytics-correcciones-de-exactitud-m-trica-de-miembros-nico/117-PATTERNS.md

<interfaces>
Backend contratos (de Planes 01/03/04/05) que el front debe espejar en src/types/analytics.ts:
- uniqueMembers: { last7, last14, last30 } (Plan 03)
- checkInAdoptionByBranch: [{ branchId, branchName, confirmados, conCheckin, ratio }] (Plan 03)
- countActiveBySegment: conteo por los 6 segmentos; EngagementMember nominal con phone (Plan 04)
- AttentionMember extendido: type 'expiring'|'overdue', daysOverdue real, yaPago, segment (Plan 05)
- renewalRate: { last7, last14, last30 } (Plan 05)
- revenue por moneda { ARS, EUR } (Plan 01)
useAnalyticsApi.ts: getKpis/getMemberAnalytics/getAttendanceAnalytics/getFinancialAnalytics ya existen (api.get '/admin/analytics/*'). Agregar getters para los endpoints nuevos.
AsistenciaTab.vue:24-34 = patrón de stat card destacada (no-show) → reutilizar para únicos.
MiembrosTab.vue:93-108 = chip estado (ya soporta type 'overdue' con `${daysOverdue} dias de mora`); :301 openWhatsApp (wa.me). No duplicar badges de segmento — AlumnosPage ya los tiene.
Logging frontend: createLogger() de src/utils/logger.ts. Nunca console.log. Sin `any`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tipos espejo + composable (endpoints nuevos + revenue por moneda)</name>
  <files>el-templo-admin/src/types/analytics.ts, el-templo-admin/src/composables/useAnalyticsApi.ts</files>
  <read_first>
    - el-templo-admin/src/types/analytics.ts (espejo de tipos backend — AttentionMember:23, FinancialAnalytics:72)
    - el-templo-admin/src/composables/useAnalyticsApi.ts:18-110 (buildParams, getters existentes, cleanup)
    - el-templo-api/src/modules/analytics/types.ts (contrato backend resultante de Planes 01/03/04/05 — fuente de verdad)
  </read_first>
  <action>
    Actualizar src/types/analytics.ts para espejar: UniqueMembersMetric, CheckInAdoptionRow, SegmentCounts, EngagementMember, RenewalRate; AttentionMember.type a 'expiring'|'overdue' + daysOverdue/yaPago/segment; FinancialAnalytics revenue separado por moneda { ARS, EUR } (revenueTrend, revenueByMethod, revenueByBranch, monthlyRevenue del KPI). Sin `any`.
    Agregar getters en useAnalyticsApi.ts para los endpoints nuevos (únicos/ratio/engagement) reutilizando buildParams y el patrón api.get '/admin/analytics/...'. Exponerlos en el return. Mantener cleanup().
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm tsc --noEmit && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ARS" el-templo-admin/src/types/analytics.ts` aumentó (revenue por moneda).
    - `grep -c "overdue" el-templo-admin/src/types/analytics.ts` >= 1.
    - useAnalyticsApi expone getters nuevos (grep de los nombres en el return).
    - Sin `any`: `grep -n ": any" el-templo-admin/src/types/analytics.ts` vacío.
    - `cd el-templo-admin && pnpm tsc --noEmit` y `pnpm lint` limpios.
  </acceptance_criteria>
  <done>Tipos espejo y composable alineados al contrato backend, tsc/lint limpios.</done>
</task>

<task type="auto">
  <name>Task 2: AsistenciaTab — KPI únicos 7/14/30 + segmentos + listas engagement + warning ratio <50%</name>
  <files>el-templo-admin/src/components/analytics/AsistenciaTab.vue</files>
  <read_first>
    - el-templo-admin/src/components/analytics/AsistenciaTab.vue:1-70 (estructura, stat card de no-show :24-34 como patrón)
    - el-templo-admin/src/components/analytics/MiembrosTab.vue:299-301 (openWhatsApp para reutilizar en listas engagement)
    - el-templo-admin/src/composables/useAnalyticsApi.ts (getters nuevos del Task 1)
  </read_first>
  <action>
    Agregar a AsistenciaTab: stat cards destacadas de miembros únicos 7/14/30 (reusar el patrón de la card de no-show :24-34). Sección de conteo de activos por segmento (reutilizar el estilo de badges de segmento que ya existe en AlumnosPage — NO duplicar la definición, importar/reusar el helper si lo hay). Lista nominal de en_riesgo/ghost con nombre + teléfono + botón WhatsApp (reusar la lógica wa.me de MiembrosTab:301). Warning visual (q-banner o chip negativo) cuando la sede filtrada tiene ratio de check-in <50% (D-13): texto explicando que los números de asistencia/engagement no son representativos para esa sede. Cargar los datos vía los getters del composable. Logging con createLogger(). Sin `any`.
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm tsc --noEmit && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "nico" el-templo-admin/src/components/analytics/AsistenciaTab.vue` (label de únicos) o equivalente presente; el componente renderiza last7/last14/last30.
    - `grep -c "50" el-templo-admin/src/components/analytics/AsistenciaTab.vue` >= 1 (umbral del warning).
    - `grep -c "wa.me" el-templo-admin/src/components/analytics/AsistenciaTab.vue` >= 1 (WhatsApp en listas engagement).
    - Sin `any`; sin console.log (`grep -n "console\." AsistenciaTab.vue` vacío).
    - `cd el-templo-admin && pnpm tsc --noEmit` y `pnpm lint` limpios.
  </acceptance_criteria>
  <done>AsistenciaTab muestra únicos, segmentos, listas engagement y warning de ratio; tsc/lint limpios.</done>
</task>

<task type="auto">
  <name>Task 3: MiembrosTab — vencidos con buckets + renewal rate + flags + priorización; FinanzasTab por moneda</name>
  <files>el-templo-admin/src/components/analytics/MiembrosTab.vue, el-templo-admin/src/components/analytics/FinanzasTab.vue</files>
  <read_first>
    - el-templo-admin/src/components/analytics/MiembrosTab.vue:73-120 (tabla attentionList, chip estado, ya soporta type 'overdue')
    - el-templo-admin/src/components/analytics/MiembrosTab.vue:295-305 (openWhatsApp)
    - el-templo-admin/src/components/analytics/FinanzasTab.vue (render de revenue — debe pasar a por-moneda)
  </read_first>
  <action>
    MiembrosTab: mostrar vencidos con buckets 1-7/8-14/15-30 usando daysOverdue real (el chip ya soporta type 'overdue'). Mostrar la tasa de renovación 7/14/30 (stat cards o fila). Mostrar flag ya pagó/no pagó por miembro (chip/ícono). Mostrar el segmento de engagement por miembro y priorizar visualmente ghost/en_riesgo por-vencer (orden o color). NO mostrar "habló con coach" (D-16 diferido).
    FinanzasTab: actualizar el render de revenue para separar por moneda (ARS/EUR en líneas/series separadas, nunca sumadas) — el contrato backend cambió en Plan 01. Reusar el patrón de outstandingByCurrency que ya separa moneda.
    Logging con createLogger(). Sin `any`. Sin console.log.
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm tsc --noEmit && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "daysOverdue\|renewal\|renovaci" el-templo-admin/src/components/analytics/MiembrosTab.vue` >= 1.
    - `grep -ci "pag" el-templo-admin/src/components/analytics/MiembrosTab.vue` >= 1 (flag ya pagó).
    - FinanzasTab separa ARS/EUR: `grep -c "ARS\|EUR" el-templo-admin/src/components/analytics/FinanzasTab.vue` >= 1.
    - Sin `any`; sin console.log en ambos archivos.
    - `cd el-templo-admin && pnpm tsc --noEmit` y `pnpm lint` limpios.
  </acceptance_criteria>
  <done>MiembrosTab muestra vencidos/renewal/flags/priorización; FinanzasTab separa moneda; tsc/lint limpios.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Frontend admin de analytics actualizado: KPI únicos 7/14/30 + segmentos + listas engagement con WhatsApp + warning de ratio <50% en la tab Asistencias; vencidos con buckets + renewal rate + flags + priorización en la tab Miembros; revenue por moneda en Finanzas.</what-built>
  <how-to-verify>
    1. Correr el admin localmente (apuntando al backend de staging o local con datos): abrir AnaliticasPage → tab Asistencia. Verificar que los KPI de únicos 7/14/30 muestran números, que aparecen los segmentos y las listas en_riesgo/ghost con botón WhatsApp funcional.
    2. Filtrar por una sede de baja adopción (ej. Chapadmalal): confirmar que aparece el warning visual de representatividad (<50%).
    3. Tab Miembros: confirmar vencidos con `N dias de mora`, buckets, tasa de renovación, flag ya pagó/no pagó, y que ghost/en_riesgo por-vencer se ven priorizados.
    4. Tab Finanzas: confirmar que ARS y EUR se muestran separados (nunca un solo número sumado).
    5. Verificar como rol coach/recepción que solo se ven datos de sus sedes (scope).
    NO hacer deploy manual del frontend (regla de memoria). NO pushear sin confirmación.
  </how-to-verify>
  <resume-signal>Escribir "aprobado" tras la verificación visual, o describir problemas.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                   |
| --------------------------------- | ----------------------------------------------------------------------------- |
| admin browser → /api/analytics/\* | El front solo renderiza; el scope/PII se aplica server-side (Planes 03/04/05) |

## STRIDE Threat Register

| Threat ID | Category               | Component                                   | Disposition | Mitigation Plan                                                                                                                                                     |
| --------- | ---------------------- | ------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-117-08  | Information Disclosure | listas con phone/WhatsApp en el front       | mitigate    | El front solo muestra lo que el backend devuelve dentro del scope autorizado; no agrega queries propias. El guard ADMIN_ROLES + scope corta por sede en el backend. |
| T-117-09  | Misrepresentation      | KPI de engagement en sede con baja adopción | mitigate    | Warning visual <50% (D-13) = representatividad honesta; evita decisiones sobre datos no representativos.                                                            |
| T-117-SC  | Tampering              | npm/pip/cargo installs                      | mitigate    | NO instalar dependencias en el admin (vitest/jsdom ausentes — lección 116-04). Si surge necesidad, checkpoint blocking-human.                                       |

Sin paquetes nuevos → sin gate de legitimidad.
</threat_model>

<verification>
- `cd el-templo-admin && pnpm tsc --noEmit` y `pnpm lint` limpios.
- Checkpoint humano de verificación visual + scope.
</verification>

<success_criteria>

- KPI únicos 7/14/30 en Asistencias (D-11).
- Segmentos + listas engagement con WhatsApp (D-12).
- Warning de ratio <50% (D-13).
- Vencidos buckets + renewal + flags + priorización (D-14/15/16).
- Revenue por moneda en el front (D-17).
  </success_criteria>

<output>
Create `.planning/phases/117-analytics-correcciones-de-exactitud-m-trica-de-miembros-nico/117-06-SUMMARY.md` when done
</output>
