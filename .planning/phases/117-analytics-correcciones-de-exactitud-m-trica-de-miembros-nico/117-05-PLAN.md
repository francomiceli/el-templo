---
phase: 117-analytics-correcciones-de-exactitud-m-trica-de-miembros-nico
plan: 05
type: execute
wave: 4
depends_on: ["117-01", "117-04"]
files_modified:
  - el-templo-api/src/modules/analytics/service.ts
  - el-templo-api/src/modules/analytics/types.ts
  - el-templo-api/src/modules/analytics/schemas.ts
  - el-templo-api/test/analytics/analytics.test.ts
autonomous: true
requirements: [D-14, D-15, D-16, D-17, D-18]
must_haves:
  truths:
    - "attentionList incluye vencidos sin renovar en buckets 1-7 / 8-14 / 15-30 días con daysOverdue REAL (ya no siempre null) (D-14)"
    - "El backend expone tasa de renovación 7/14/30: de los que vencieron hace N días, % que renovó (D-15)"
    - "Cada miembro por-vencer/vencido trae su flag ya pagó / no pagó derivado de financial_transactions recientes (D-16, D-18)"
    - "Cada miembro por-vencer/vencido trae su segmento de engagement para priorizar (ghost+por-vencer = alta prioridad) (D-17)"
  artifacts:
    - path: "el-templo-api/src/modules/analytics/service.ts"
      provides: "getAttentionList extendido (overdue buckets + daysOverdue real) + renewalRate 7/14/30 + flags"
      contains: "daysOverdue"
    - path: "el-templo-api/src/modules/analytics/types.ts"
      provides: "AttentionMember.type = 'expiring' | 'overdue'; flags pagó/segmento"
      contains: "overdue"
  key_links:
    - from: "analytics/service.ts getAttentionList"
      to: "financial_transactions (ya pagó)"
      via: "EXISTS transacción reciente inflow plan_charge no anulada"
      pattern: "financialTransactions"
    - from: "analytics/service.ts attentionList"
      to: "member_profiles.segment (cruce engagement)"
      via: "join para priorizar"
      pattern: "segment"
---

<objective>
Completar el panel de Vencimientos y Renovaciones (D-14/15/16): extender `getAttentionList`
con vencidos sin renovar en buckets 1-7/8-14/15-30 (daysOverdue real), tasa de renovación
7/14/30 (operativa), flag ya pagó/no pagó derivado de financial_transactions, y cruce con
el segmento de engagement para priorizar. "Habló con coach" se DIFIERE (D-16). NO se toca
el monolito (solo extensión in-place de getAttentionList + métodos nuevos).

Purpose: Es la worklist semanal de recepción; hoy está a medias (daysOverdue siempre null)
y sin priorización. La priorización por engagement evita malgastar contactos.

Output: getAttentionList extendido + renewalRate, tipos, schema, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-analytics-correcciones-de-exactitud-m-trica-de-miembros-nico/117-CONTEXT.md
@.planning/phases/117-analytics-correcciones-de-exactitud-m-trica-de-miembros-nico/117-PATTERNS.md

<interfaces>
AttentionMember (analytics/types.ts:25-34): { userId, firstName, lastName, planName, phone, type: 'expiring', daysUntilExpiry, daysOverdue: null }. type debe pasar a 'expiring' | 'overdue'; daysOverdue deja de ser siempre null.
getAttentionList (service.ts:434-499): hoy solo "expiring" (subs activas que vencen en 7 días); daysOverdue null; slice(0,20).
financial_transactions (db/schema/financial-transactions.ts): member_id, kind ('plan_charge','debt_settlement',...), direction ('inflow'|'outflow'), transaction_date (date string), voided_at, branch_id. Filtro canónico revenue: voided_at IS NULL AND kind='plan_charge' AND direction='inflow'.
member_profiles.segment (member-profiles.ts): para el cruce de engagement (D-16). Reutilizar el patrón de Plan 04 (NO recalcular).
subscriptions: status, end_date. "Vencido sin renovar" = end_date pasada AND no existe sub vigente posterior (predicado activeMemberExists falso). "Ya pagó" = EXISTS transacción inflow plan_charge reciente (definir ventana, p.ej. últimos 30 días).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extender getAttentionList con overdue buckets + daysOverdue real + flags pagó + cruce segmento</name>
  <files>el-templo-api/src/modules/analytics/service.ts, el-templo-api/src/modules/analytics/types.ts, el-templo-api/test/analytics/analytics.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/analytics/service.ts:434-499 (getAttentionList — extender)
    - el-templo-api/src/modules/analytics/types.ts:25-42 (AttentionMember, MemberAnalytics)
    - el-templo-api/src/modules/shared/active-member.ts (Plan 01 — para "vencido sin renovar")
    - el-templo-api/src/db/schema/financial-transactions.ts (member_id, kind, direction, voided_at — flag ya pagó)
    - el-templo-api/src/db/schema/member-profiles.ts (segment — cruce engagement)
    - el-templo-admin/src/components/analytics/MiembrosTab.vue:93-108 (el front YA renderiza type 'overdue' con `${daysOverdue} dias de mora` — el contrato debe alimentarlo)
  </read_first>
  <behavior>
    - Vencidos: sembrar subs con end_date hace 3, 10 y 20 días sin renovación; getAttentionList devuelve esas 3 con type='overdue' y daysOverdue = 3/10/20 (real, no null), clasificables en buckets 1-7 / 8-14 / 15-30.
    - daysUntilExpiry vs daysOverdue: un expiring tiene daysUntilExpiry y daysOverdue null; un overdue tiene daysOverdue real y daysUntilExpiry null.
    - Flag ya pagó: un vencido con una transacción inflow plan_charge reciente no anulada → yaPago=true; sin transacción → yaPago=false.
    - Cruce segmento: cada miembro de la lista trae su segment (de member_profiles); un ghost por-vencer queda marcado para priorización.
    - Vencido >30 días NO aparece (fuera de los 3 buckets).
  </behavior>
  <action>
    Cambiar AttentionMember.type a unión 'expiring' | 'overdue'. Agregar campos: daysOverdue (ya existe, ahora se puebla real), yaPago: boolean, segment: MemberSegment | null. Importar MemberSegment de segmentation/types.ts.
    Extender getAttentionList: además del bloque expiring existente, agregar bloque overdue — subs cuya end_date pasó hace 1..30 días Y el user NO está activo (negar activeMemberExists). Computar daysOverdue real (CURDATE() - end_date). Limitar a buckets 1-7/8-14/15-30 (excluir >30). Para cada miembro (expiring y overdue): derivar yaPago vía EXISTS de financial_transactions inflow plan_charge voided_at IS NULL en una ventana reciente (documentar ventana, p.ej. 30 días); join a member_profiles para traer segment. Aplicar scope branchId/country (reusar applyScope o el patrón existente del método). Ordenar/slice priorizando: ghost/en_riesgo por-vencer y mayor mora primero. NO implementar "habló con coach" (D-16 diferido — dejar comment explícito).
    Agregar método renewalRate(filters) (D-15): para ventanas 7/14/30, de los que vencieron hace N días, % que renovó (existe sub vigente posterior). Definir tipo RenewalRate { last7, last14, last30 } en types.ts. Exponerlo dentro de getMemberAnalytics o como dato adicional (decisión del planner; si se agrega a MemberAnalytics, actualizar el schema Fastify).
    Tests en analytics.test.ts contra MySQL real (reloj real): overdue buckets + daysOverdue real, yaPago true/false, cruce segment, renewalRate. Sembrar subs/transactions/member_profiles vía Drizzle.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm vitest run test/analytics/analytics.test.ts -t "attention"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "overdue" el-templo-api/src/modules/analytics/types.ts` >= 1 (type unión).
    - `grep -n "daysOverdue" el-templo-api/src/modules/analytics/service.ts` muestra cálculo real (no solo `daysOverdue: null`).
    - `grep -c "financialTransactions" el-templo-api/src/modules/analytics/service.ts` aumentó (flag ya pagó en getAttentionList).
    - El comment de "habló con coach diferido" (D-16) está presente.
    - Test overdue buckets: 3 miembros con daysOverdue 3/10/20; un miembro a 40 días NO aparece.
    - Test renewalRate devuelve last7/last14/last30.
    - `cd el-templo-api && pnpm tsc --noEmit` limpio.
  </acceptance_criteria>
  <done>attentionList completo (overdue + daysOverdue real + yaPago + segment); renewalRate 7/14/30; tests verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Alinear schema Fastify de member analytics + test de scope del contrato</name>
  <files>el-templo-api/src/modules/analytics/schemas.ts, el-templo-api/test/analytics/analytics.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/analytics/schemas.ts (memberAnalyticsSchema — response)
    - el-templo-api/src/modules/analytics/types.ts (AttentionMember extendido, RenewalRate del Task 1)
    - el-templo-api/src/modules/analytics/routes.ts:68-93 (GET /members)
  </read_first>
  <action>
    Actualizar memberAnalyticsSchema (response) para reflejar los campos nuevos de AttentionMember (type 'expiring'|'overdue', daysOverdue, yaPago, segment) y renewalRate si se agregó a MemberAnalytics. fast-json-stringify STRIPEA campos no declarados — declarar todos los nuevos o usar additionalProperties:true según convención del módulo (lección Phase 106-04/109-02). NO romper el contrato existente (daysUntilExpiry sigue presente).
    Test de contrato + scope en analytics.test.ts: GET /members como coach de sede X devuelve attentionList solo de sus sedes (T-117-01) y los campos nuevos NO se stripean (assert yaPago/segment/daysOverdue presentes en la respuesta serializada).
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm vitest run test/analytics/analytics.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - El response de GET /members incluye yaPago, segment, daysOverdue, type (no stripeados) — assert en test.
    - Test de scope: coach no ve attentionList de otras sedes.
    - `cd el-templo-api && pnpm tsc --noEmit` y `pnpm lint` limpios; suite analytics.test.ts verde.
  </acceptance_criteria>
  <done>Schema alineado sin strip de campos nuevos; tests de contrato y scope verdes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                       | Description                                             |
| -------------------------------------------------------------- | ------------------------------------------------------- |
| admin client → /api/analytics/members                          | attentionList expone phone (PII) de vencidos/por-vencer |
| service → subscriptions/financial_transactions/member_profiles | la lista debe cortar por scope de sede/país             |

## STRIDE Threat Register

| Threat ID | Category                              | Component                              | Disposition | Mitigation Plan                                                                                                       |
| --------- | ------------------------------------- | -------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------- |
| T-117-01  | Information Disclosure / Authz Bypass | getAttentionList overdue               | mitigate    | Scope branchId/country preservado/aplicado en el bloque overdue igual que el expiring; test: coach no ve otras sedes. |
| T-117-07  | Information Disclosure                | flag yaPago vía financial_transactions | accept      | Solo expone un booleano derivado (no montos) dentro del mismo scope ya autorizado por el guard ADMIN_ROLES.           |
| T-117-SC  | Tampering                             | npm/pip/cargo installs                 | mitigate    | No se instalan dependencias. Si surge, checkpoint blocking-human.                                                     |

Sin paquetes nuevos → sin gate de legitimidad.
</threat_model>

<verification>
- `cd el-templo-api && pnpm vitest run test/analytics/analytics.test.ts` verde.
- `cd el-templo-api && pnpm tsc --noEmit` y `pnpm lint` limpios.
</verification>

<success_criteria>

- Vencidos en buckets 1-7/8-14/15-30 con daysOverdue real (D-14).
- Renewal rate 7/14/30 (D-15).
- Flag ya pagó/no pagó + cruce segmento; "habló con coach" diferido (D-16).
- Scope respetado (D-17); tests de integración (D-18).
  </success_criteria>

<output>
Create `.planning/phases/117-analytics-correcciones-de-exactitud-m-trica-de-miembros-nico/117-05-SUMMARY.md` when done
</output>
