---
phase: 117-analytics-correcciones-de-exactitud-m-trica-de-miembros-nico
plan: 04
type: execute
wave: 3
depends_on: ["117-01", "117-03"]
files_modified:
  - el-templo-api/src/modules/analytics/engagement-service.ts
  - el-templo-api/src/modules/analytics/types.ts
  - el-templo-api/src/modules/analytics/routes.ts
  - el-templo-api/src/modules/analytics/schemas.ts
  - el-templo-api/test/analytics/engagement.test.ts
autonomous: true
requirements: [D-09, D-12, D-17, D-18]
must_haves:
  truths:
    - "El backend agrega conteo de ACTIVOS por segmento leyendo member_profiles.segment (NO recalcula segmentos) (D-12, D-09)"
    - "El backend expone lista nominal de en_riesgo y ghost con teléfono/acción WhatsApp, respetando scope (D-17, D-18)"
    - "Los segmentos y umbrales se reutilizan del módulo segmentation existente (6 valores), no se inventan nuevos"
  artifacts:
    - path: "el-templo-api/src/modules/analytics/engagement-service.ts"
      provides: "Domain service: countActiveBySegment + nominalList(en_riesgo|ghost)"
      contains: "memberProfiles.segment"
  key_links:
    - from: "engagement-service.ts"
      to: "member_profiles.segment"
      via: "read-only aggregate (reutiliza segmentation)"
      pattern: "memberProfiles.segment"
    - from: "engagement-service.ts"
      to: "analytics/scope.ts applyScope + shared/active-member.ts"
      via: "import"
      pattern: "applyScope"
---

<objective>
Domain service de engagement (D-12) que REUTILIZA el módulo de segmentación existente:
lee `member_profiles.segment` (6 segmentos ya calculados), solo AGREGA conteos de
activos por segmento + lista nominal de en_riesgo/ghost con teléfono/WhatsApp. NO
inventa segmentos ni umbrales nuevos. Consumido por la tab de Asistencias (front Plan 05).

Purpose: Recepción necesita la worklist de "activos que se van a ir si nadie los toca"
(en_riesgo/ghost), priorizable, sin duplicar la lógica de segmentación.

Output: engagement-service.ts (nuevo), tipos, schemas, rutas, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-analytics-correcciones-de-exactitud-m-trica-de-miembros-nico/117-CONTEXT.md
@.planning/phases/117-analytics-correcciones-de-exactitud-m-trica-de-miembros-nico/117-PATTERNS.md

<interfaces>
member_profiles schema (db/schema/member-profiles.ts): user_id (int FK), segment (memberSegmentEnum, nullable), segment_updated_at. Índice idx_member_profiles_user_id.
memberSegmentEnum (member-profiles.ts:40): 6 valores. MemberSegment type (segmentation/types.ts:10-18): 'nuevo'|'espartano'|'intermitente'|'en_riesgo'|'digital_warrior'|'ghost'.
activeMemberExists(userIdColumn) (shared/active-member.ts — Plan 01): predicado canónico para contar SOLO activos por segmento.
applyScope (analytics/scope.ts — Plan 01): scope branch/país.
Lista nominal analog — getAttentionList (analytics/service.ts:434-499): select userId/firstName/lastName/planName/phone, map a objeto, sort por urgencia, slice. Mismo tipo/forma para las listas de engagement.
Rutas: onRequest ADMIN_ROLES + attachCountryScope; requireBranchAccess optional; filters.country = request.scope.country ?? undefined.
Staleness aceptable: el segmento se recalcula al login del miembro (cooldown 1h) — analytics solo lee, no recalcula (D-12).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EngagementService — conteo de activos por segmento + listas nominales en_riesgo/ghost</name>
  <files>el-templo-api/src/modules/analytics/engagement-service.ts, el-templo-api/src/modules/analytics/types.ts, el-templo-api/test/analytics/engagement.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/segmentation/service.ts:24-28 (DI constructor) y :279-283 (patrón de lectura de member_profiles.segment)
    - el-templo-api/src/modules/segmentation/types.ts:10-18 (los 6 MemberSegment — reutilizar, no redefinir)
    - el-templo-api/src/db/schema/member-profiles.ts:40,53,67 (memberSegmentEnum, userId, segment)
    - el-templo-api/src/modules/analytics/service.ts:434-499 (getAttentionList — patrón de lista nominal con phone)
    - el-templo-api/src/modules/shared/active-member.ts (Plan 01)
    - el-templo-api/src/modules/analytics/scope.ts (Plan 01)
  </read_first>
  <behavior>
    - countActiveBySegment: sembrar users activos con member_profiles.segment variados (espartano/en_riesgo/ghost) + un user no-activo con segment ghost; el método cuenta SOLO activos (activeMemberExists) agrupados por segment. El user no-activo NO se cuenta.
    - nominalList: devuelve solo en_riesgo y ghost activos, cada uno con userId/firstName/lastName/planName/phone; respeta scope (coach de sede X no ve member de sede Y).
    - Test segment null: users activos sin segment calculado no rompen el agregado (se ignoran o caen en una bucket "sin segmento" — definir y documentar).
  </behavior>
  <action>
    Crear `engagement-service.ts` con clase EngagementService (DI db+log). REUTILIZA segmentation: lee member_profiles.segment, NO recalcula nada.
    Método countActiveBySegment(filters): JOIN users↔member_profiles, filtrar por activeMemberExists(users.id) + role='member', agrupar por segment, aplicar applyScope. Devolver conteo por cada uno de los 6 segmentos (default 0). Importar el type MemberSegment de segmentation/types.ts (no redefinir).
    Método getEngagementNominalList(filters): select de en_riesgo y ghost activos (mismo shape que getAttentionList: userId, firstName, lastName, planName, phone), aplicar applyScope, sort/slice razonable. Reutilizar el tipo AttentionMember o un EngagementMember análogo (decisión del planner; mantener phone para WhatsApp).
    Definir tipos SegmentCounts y EngagementMember en analytics/types.ts.
    Tests en engagement.test.ts contra MySQL real: conteo solo-activos por segmento, listas nominales en_riesgo/ghost, scope, edge case segment null. Sembrar member_profiles + subscriptions vía Drizzle.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm vitest run test/analytics/engagement.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "memberProfiles.segment" el-templo-api/src/modules/analytics/engagement-service.ts` >= 1.
    - `grep -c "activeMemberExists" el-templo-api/src/modules/analytics/engagement-service.ts` >= 1 (cuenta solo activos).
    - `grep -c "applyScope" el-templo-api/src/modules/analytics/engagement-service.ts` >= 2.
    - El service NO define un enum de segmentos propio (importa MemberSegment de segmentation/types.ts): `grep -c "espartano" el-templo-api/src/modules/analytics/engagement-service.ts` puede ser 0 o referenciar el import, pero NO un mysqlEnum nuevo.
    - Test "no-activo no se cuenta" pasa.
    - `cd el-templo-api && pnpm tsc --noEmit` limpio.
  </acceptance_criteria>
  <done>EngagementService agrega activos por segmento (reutilizando segmentation) y expone listas nominales en_riesgo/ghost con scope; tests verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Endpoint GET de engagement + autorización por scope</name>
  <files>el-templo-api/src/modules/analytics/routes.ts, el-templo-api/src/modules/analytics/schemas.ts, el-templo-api/test/analytics/engagement.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/analytics/routes.ts:24-148 (plugin, guard, mapeo filters)
    - el-templo-api/src/modules/analytics/schemas.ts (schemas existentes)
    - el-templo-api/src/modules/analytics/engagement-service.ts (Task 1)
  </read_first>
  <action>
    Instanciar EngagementService en el plugin (junto a AnalyticsService/AttendanceMetricsService). Agregar GET /engagement (o /attendance/engagement — decisión del planner, consistente con Plan 03) que devuelva countActiveBySegment + nominalList. Reusar guard onRequest (ADMIN_ROLES + attachCountryScope) + requireBranchAccess optional. Mapear filters branchId + scope.country ?? undefined. try/catch + handleServiceError. Response schema en schemas.ts.
    Test de ruta: coach de sede X recibe nominalList solo de sus sedes (T-117-01, PII por sede); owner sin ?country ve global. La lista expone phone (PII) → confirmar que solo roles autorizados (ADMIN_ROLES) la reciben (ya garantizado por el guard) y que el scope corta por sede.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm vitest run test/analytics/engagement.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "EngagementService" el-templo-api/src/modules/analytics/routes.ts` >= 1.
    - El endpoint está bajo el onRequest hook con ADMIN_ROLES + attachCountryScope.
    - Test de PII por scope: coach no ve teléfonos de otras sedes (assert sobre la nominalList).
    - `cd el-templo-api && pnpm tsc --noEmit` y `pnpm lint` limpios.
  </acceptance_criteria>
  <done>Endpoint de engagement con guard de rol+scope; tests de autorización/PII verdes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                         |
| ---------------------------------------- | --------------------------------------------------- |
| admin client → /api/analytics/engagement | nominalList expone phone + deep-link WhatsApp (PII) |
| service → member_profiles/users          | la lista nominal debe cortar por scope de sede/país |

## STRIDE Threat Register

| Threat ID | Category                              | Component                | Disposition | Mitigation Plan                                                                                                                     |
| --------- | ------------------------------------- | ------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| T-117-01  | Information Disclosure / Authz Bypass | getEngagementNominalList | mitigate    | applyScope en toda query (grep >= 2) + test: coach no ve teléfonos de otras sedes.                                                  |
| T-117-06  | Information Disclosure (PII)          | endpoint /engagement     | mitigate    | Guard onRequest ADMIN_ROLES garantiza que solo staff autorizado recibe phone/WhatsApp; scope corta por sede. Test de PII por scope. |
| T-117-SC  | Tampering                             | npm/pip/cargo installs   | mitigate    | No se instalan dependencias. Si surge, checkpoint blocking-human.                                                                   |

Sin paquetes nuevos → sin gate de legitimidad.
</threat_model>

<verification>
- `cd el-templo-api && pnpm vitest run test/analytics/engagement.test.ts` verde.
- `cd el-templo-api && pnpm tsc --noEmit` y `pnpm lint` limpios.
</verification>

<success_criteria>

- Conteo de activos por segmento reutilizando segmentation (D-12).
- Listas nominales en_riesgo/ghost con phone/WhatsApp y scope (D-12, D-17).
- applyScope reutilizado (D-09); tests de integración + PII por scope (D-18).
  </success_criteria>

<output>
Create `.planning/phases/117-analytics-correcciones-de-exactitud-m-trica-de-miembros-nico/117-04-SUMMARY.md` when done
</output>
