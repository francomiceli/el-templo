---
phase: 117-analytics-correcciones-de-exactitud-m-trica-de-miembros-nico
plan: 03
type: execute
wave: 2
depends_on: ["117-01"]
files_modified:
  - el-templo-api/src/modules/analytics/attendance-metrics-service.ts
  - el-templo-api/src/modules/analytics/types.ts
  - el-templo-api/src/modules/analytics/routes.ts
  - el-templo-api/src/modules/analytics/schemas.ts
  - el-templo-api/test/analytics/attendance-metrics.test.ts
autonomous: true
requirements: [D-09, D-11, D-13, D-17, D-18]
must_haves:
  truths:
    - "El backend expone miembros únicos (COUNT DISTINCT member_id sobre attendance) en ventanas 7/14/30 días, respetando scope branch/país (D-11, D-08, D-09)"
    - "El backend expone, por sede, el ratio de adopción de check-in (bookings confirmados con check-in registrado ÷ total confirmados) (D-13)"
    - "Las queries nuevas usan applyScope y respetan el modelo de scope (owner global / admin-país / coach-recepción-sedes) (D-17, D-18)"
  artifacts:
    - path: "el-templo-api/src/modules/analytics/attendance-metrics-service.ts"
      provides: "Domain service nuevo: uniqueMembers 7/14/30 + checkInAdoptionByBranch"
      contains: "COUNT(DISTINCT"
    - path: "el-templo-api/src/modules/analytics/routes.ts"
      provides: "Endpoint(s) GET nuevos para únicos + ratio check-in"
  key_links:
    - from: "attendance-metrics-service.ts"
      to: "analytics/scope.ts applyScope"
      via: "import applyScope"
      pattern: "applyScope"
    - from: "attendance-metrics-service.ts"
      to: "attendance.member_id"
      via: "COUNT DISTINCT"
      pattern: "memberId"
---

<objective>
Domain service nuevo de métricas de asistencia (D-09): miembros únicos en ventanas
7/14/30 días (D-11, feature comprometida) y ratio de adopción de check-in por sede
(D-13 Parte B). Endpoints nuevos consumidos por la tab de Asistencias del admin (front
en Plan 05). NO se toca el monolito service.ts existente (split = v4.9).

Purpose: KPI de únicos que recepción pidió + exponer el problema operativo de sedes que
reservan pero no pasan lista (Chapadmalal: 20/22 reservan, nadie checkea).

Output: attendance-metrics-service.ts (nuevo), tipos, schemas, rutas, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-analytics-correcciones-de-exactitud-m-trica-de-miembros-nico/117-CONTEXT.md
@.planning/phases/117-analytics-correcciones-de-exactitud-m-trica-de-miembros-nico/117-PATTERNS.md

<interfaces>
applyScope (analytics/scope.ts — creado en Plan 01): aplica branchId/country + join condicional a branches. Importar y reutilizar.
attendance schema (db/schema/attendance.ts): member_id (int FK users), branch_id (int FK), checked_in_at (timestamp). Índices (member_id, checked_in_at) y (branch_id, checked_in_at).
bookings schema: status bookingStatusEnum incluye 'confirmado'; check-in se registra en attendance (no en bookings) — ratio = bookings confirmados que TIENEN fila attendance ÷ total bookings confirmados, por sede. Confirmar la relación bookings↔attendance durante implementación (booking_id en attendance o join por member+fecha+schedule).
AnalyticsService DI: constructor(private db: MySql2Database<typeof schema>, private log: FastifyBaseLogger) — mismo patrón para el service nuevo.
Rutas: onRequest hook con ADMIN_ROLES + attachCountryScope; requireBranchAccess({ from: "query.branchId", optional: true }); try/catch + handleServiceError. filters.country = request.scope.country ?? undefined.
D-08: usar rangos `checked_in_at >= dateFrom AND checked_in_at < dateTo+1` (NO DATE()).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AttendanceMetricsService — únicos 7/14/30 + ratio check-in por sede</name>
  <files>el-templo-api/src/modules/analytics/attendance-metrics-service.ts, el-templo-api/src/modules/analytics/types.ts, el-templo-api/test/analytics/attendance-metrics.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/analytics/service.ts:506-550 (getDailyCheckins — patrón de query attendance + scope condicional)
    - el-templo-api/src/modules/analytics/service.ts:722-767 (getNoShowRate — join bookings↔schedules↔branches + enum 'confirmado')
    - el-templo-api/src/modules/analytics/scope.ts (applyScope, creado en Plan 01)
    - el-templo-api/src/db/schema/attendance.ts (member_id, branch_id, checked_in_at, índices)
    - el-templo-api/src/db/schema/bookings.ts (status, scheduleId, member_id, booking_date — para ratio)
    - el-templo-api/src/modules/segmentation/service.ts:24-28 (patrón DI constructor del service nuevo)
  </read_first>
  <behavior>
    - uniqueMembers: sembrar attendance con N member_id distintos dentro de 7 días y otros fuera de 30; el método devuelve { last7, last14, last30 } con COUNT DISTINCT correcto por ventana, respetando scope (un coach de sede X no ve member_id de sede Y).
    - Test scope: con branchId filtrado, únicos cuenta solo esa sede.
    - checkInAdoptionByBranch: sembrar una sede con 4 bookings confirmados de los cuales 1 tiene attendance (ratio 25%) y otra sede con 4/4 (100%); el método devuelve por sede { branchId, branchName, confirmados, conCheckin, ratio }.
    - Test ratio cero: sede con confirmados>0 y 0 check-ins devuelve ratio 0 (no NaN/divide-by-zero).
  </behavior>
  <action>
    Crear `attendance-metrics-service.ts` con clase AttendanceMetricsService (constructor DI db+log, mismo patrón que AnalyticsService/SegmentationService).
    Método uniqueMembers(filters): COUNT(DISTINCT member_id) sobre attendance para cada ventana (7/14/30 días contados hacia atrás desde hoy), usando rangos checked_in_at >= now-Ndías AND checked_in_at < now+1 (D-08, evita anular índice). Aplicar applyScope para branchId/country. Devolver { last7, last14, last30 }.
    Método checkInAdoptionByBranch(filters): por sede, ratio = bookings status='confirmado' que tienen check-in registrado ÷ total bookings status='confirmado'. Determinar la relación bookings↔attendance al implementar (booking_id en attendance, o LEFT JOIN attendance por member+schedule+fecha). Usar el enum 'confirmado' (NO 'confirmed' — D-04). Aplicar applyScope. Devolver array por sede con { branchId, branchName, confirmados, conCheckin, ratio } (ratio 0..1 o 0..100, definir y documentar; el warning <50% es lógica de frontend en Plan 05).
    Definir los tipos UniqueMembersMetric y CheckInAdoptionRow en analytics/types.ts.
    Tests en attendance-metrics.test.ts contra MySQL real (reloj real). Sembrar attendance/bookings vía Drizzle insert. Registrar limpieza si aplica.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm vitest run test/analytics/attendance-metrics.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "COUNT(DISTINCT" el-templo-api/src/modules/analytics/attendance-metrics-service.ts` >= 1.
    - `grep -c "applyScope" el-templo-api/src/modules/analytics/attendance-metrics-service.ts` >= 2 (únicos + ratio).
    - `grep -n "'confirmado'" el-templo-api/src/modules/analytics/attendance-metrics-service.ts` presente; `grep "'confirmed'"` vacío.
    - El test de ratio cero asserta ratio 0 sin NaN.
    - El test de scope asserta que únicos respeta branchId.
    - `cd el-templo-api && pnpm tsc --noEmit` limpio.
  </acceptance_criteria>
  <done>AttendanceMetricsService expone únicos 7/14/30 y ratio por sede; tests verdes con scope y edge cases.</done>
</task>

<task type="auto">
  <name>Task 2: Endpoints GET para únicos + ratio check-in</name>
  <files>el-templo-api/src/modules/analytics/routes.ts, el-templo-api/src/modules/analytics/schemas.ts, el-templo-api/test/analytics/attendance-metrics.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/analytics/routes.ts:24-148 (estructura del plugin, onRequest hook, requireBranchAccess, mapeo de filters)
    - el-templo-api/src/modules/analytics/schemas.ts (forma de los schemas Fastify existentes — attendanceAnalyticsSchema)
    - el-templo-api/src/modules/analytics/attendance-metrics-service.ts (Task 1)
  </read_first>
  <action>
    Instanciar AttendanceMetricsService junto a AnalyticsService (routes.ts:25). Agregar endpoint(s) GET nuevos bajo el mismo plugin (p.ej. GET /attendance/unique-members y GET /attendance/checkin-adoption, o un GET /attendance/operativo combinado — decisión del planner). Reusar el mismo guard onRequest (ADMIN_ROLES + attachCountryScope) y preHandler requireBranchAccess({ from: "query.branchId", optional: true }). Mapear filters desde request.query.branchId + request.scope.country ?? undefined + dateFrom/dateTo. try/catch con handleServiceError(err, reply, request.log, "..."). Definir los response schemas en schemas.ts (Fastify additionalProperties según convención del módulo).
    Agregar tests de ruta en attendance-metrics.test.ts: un coach autenticado de sede X recibe solo únicos/ratio de sus sedes (T-117-01 — no fuga de otras sedes); un owner sin ?country ve global. Reusar helpers de auth de test/helpers.ts.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm vitest run test/analytics/attendance-metrics.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AttendanceMetricsService" el-templo-api/src/modules/analytics/routes.ts` >= 1.
    - Los endpoints nuevos están bajo el onRequest hook con ADMIN_ROLES + attachCountryScope.
    - Test de scope de ruta: coach de sede X no ve datos de sede Y (assert).
    - `cd el-templo-api && pnpm tsc --noEmit` y `pnpm lint` limpios.
  </acceptance_criteria>
  <done>Endpoints expuestos con guard de rol+scope; tests de autorización verdes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                    | Description                                                           |
| ------------------------------------------- | --------------------------------------------------------------------- |
| admin client → /api/analytics/attendance/\* | branchId query untrusted; scope server-side via request.scope         |
| service → attendance/bookings               | métricas por sede deben filtrar por scope para no exponer otras sedes |

## STRIDE Threat Register

| Threat ID | Category                              | Component                               | Disposition | Mitigation Plan                                                                                                                                                  |
| --------- | ------------------------------------- | --------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-117-01  | Information Disclosure / Authz Bypass | uniqueMembers / checkInAdoptionByBranch | mitigate    | applyScope aplicado en TODA query (verificado por grep >= 2 + test de scope de ruta: coach no ve otras sedes). requireBranchAccess valida el branchId del query. |
| T-117-05  | Denial of Service                     | COUNT DISTINCT sobre attendance         | accept      | Volumen actual ~3.681 checkins; rangos de fecha sobre índice (D-08) mantienen el costo bajo. Reevaluar si escala.                                                |
| T-117-SC  | Tampering                             | npm/pip/cargo installs                  | mitigate    | No se instalan dependencias. Si surge, checkpoint blocking-human.                                                                                                |

Sin paquetes nuevos → sin gate de legitimidad.
</threat_model>

<verification>
- `cd el-templo-api && pnpm vitest run test/analytics/attendance-metrics.test.ts` verde.
- `cd el-templo-api && pnpm tsc --noEmit` y `pnpm lint` limpios.
</verification>

<success_criteria>

- Únicos 7/14/30 con scope (D-11).
- Ratio de adopción de check-in por sede (D-13 Parte B).
- applyScope reutilizado (D-09); scope respetado (D-17); tests de integración (D-18).
  </success_criteria>

<output>
Create `.planning/phases/117-analytics-correcciones-de-exactitud-m-trica-de-miembros-nico/117-03-SUMMARY.md` when done
</output>
