---
phase: 117-analytics-correcciones-de-exactitud-m-trica-de-miembros-nico
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/shared/active-member.ts
  - el-templo-api/src/modules/analytics/scope.ts
  - el-templo-api/src/modules/analytics/service.ts
  - el-templo-api/src/modules/analytics/types.ts
  - el-templo-api/test/analytics/analytics.test.ts
autonomous: true
requirements: [D-01, D-02, D-04, D-05, D-06, D-07, D-08, D-09, D-17, D-18]
must_haves:
  truths:
    - "El KPI de activos muestra el número real (excluye los ~48 fantasmas con sub vencida): cuenta vía EXISTS de sub vigente, no via users.status (D-01, D-02)"
    - "La tasa de no-show usa el enum real 'confirmado' y deja de dar siempre ~100%/0 (D-07)"
    - "Revenue (trend, by method, by branch, sum) NUNCA suma ARS+EUR: cada uno separa por moneda (D-04)"
    - "newInPeriod cuenta solo nuevos ACTIVOS (predicado canónico), no freemium/prueba (D-05); sin cron de reparación (no-op, D-03)"
    - "Plan distribution filtra is_archived y agrupa por (name, country): Flex (AR) y Flex (ES) no se fusionan (D-06)"
    - "applyScope helper existe y absorbe los dos sabores de scope (join siempre / join condicional), respetando el modelo de scope (D-17) y los contratos/tipos compartidos (D-18) sobre el predicado canónico reutilizable (D-08, D-09)"
  artifacts:
    - path: "el-templo-api/src/modules/shared/active-member.ts"
      provides: "Fragmento SQL reusable activeMemberExists(userIdColumn) con el predicado canónico"
      contains: "subscription_status"
    - path: "el-templo-api/src/modules/analytics/scope.ts"
      provides: "applyScope helper extraído de las ~15 repeticiones branchId/country"
    - path: "el-templo-api/src/modules/analytics/service.ts"
      provides: "Métodos corregidos in-place (countActiveMembers, getNoShowRate, countNewMembers, getPlanDistribution, sumRevenue, getRevenueTrend, getRevenueByMethod, getRevenueByBranch)"
  key_links:
    - from: "el-templo-api/src/modules/analytics/service.ts"
      to: "el-templo-api/src/modules/shared/active-member.ts"
      via: "import activeMemberExists"
      pattern: "activeMemberExists"
    - from: "el-templo-api/src/modules/analytics/service.ts"
      to: "subscriptions/service.ts:4119 recomputeUserStatus predicate"
      via: "mismo EXISTS canónico"
      pattern: "subscription_status IN \\('active', ?'paused'\\)"
---

<objective>
Fundaciones de correctitud del módulo de analytics: extraer el predicado canónico de
"activo" a un helper SQL compartido, extraer el helper `applyScope`, y corregir todos
los bugs in-place de `FINDINGS.md` (no-show enum, multi-moneda, trend circular, plan
distribution, perf DATE()). Sin entidades nuevas — reutiliza lo existente.

Purpose: Los números del panel deben ser correctos antes de agregar métricas nuevas.
Todas las métricas downstream (engagement, attendance, finance) dependen del predicado
canónico y de `applyScope`.

Output: `shared/active-member.ts` (nuevo), `analytics/scope.ts` (nuevo), correcciones
in-place en `analytics/service.ts` + tipos, y tests de integración contra MySQL real.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-analytics-correcciones-de-exactitud-m-trica-de-miembros-nico/FINDINGS.md
@.planning/phases/117-analytics-correcciones-de-exactitud-m-trica-de-miembros-nico/117-PATTERNS.md
@.planning/phases/117-analytics-correcciones-de-exactitud-m-trica-de-miembros-nico/117-CONTEXT.md

<interfaces>
Predicado canónico (subscriptions/service.ts:4119-4125, dentro de recomputeUserStatus):
EXISTS (SELECT 1 FROM subscriptions s WHERE s.user_id = <userIdCol>
  AND s.subscription_status IN ('active','paused')
  AND s.start_date <= CURDATE()
  AND (s.end_date IS NULL OR s.end_date >= CURDATE()))

Patrón de separación por moneda (getRevenueByMethod usa este mapeo a objeto literal):

- financial_transactions.currency: varchar(3) NOT NULL DEFAULT 'ARS' (valores 'ARS' | 'EUR')
- Filtro canónico de revenue ya presente: voided_at IS NULL AND kind IN ('plan_charge','debt_settlement') AND direction='inflow'
- getOutstandingByCurrency (service.ts:929) mapea rows a { ARS: 0, EUR: 0 }

AnalyticsFilters (analytics/types.ts:93): { branchId?: number; country?: 'AR'|'ES'; dateFrom?; dateTo? }
request.scope (shared/country-scope.ts): { country: 'AR'|'ES'|null; branchIds: number[]; isOwner: boolean; role; userBranchId }
bookingStatusEnum (db/schema/bookings.ts:16): incluye 'confirmado' y 'no_show' (NO 'confirmed')
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extraer helper canónico de "activo" + helper applyScope</name>
  <files>el-templo-api/src/modules/shared/active-member.ts, el-templo-api/src/modules/analytics/scope.ts, el-templo-api/test/analytics/analytics.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/subscriptions/service.ts:4099-4190 (recomputeUserStatus — fuente del predicado canónico, EXISTS en :4119-4125)
    - el-templo-api/src/modules/analytics/service.ts:191-219 (countActiveMembers — sabor "join siempre")
    - el-templo-api/src/modules/analytics/service.ts:506-550 (getDailyCheckins — sabor "join condicional country !== undefined")
    - el-templo-api/src/modules/analytics/service.ts:722-767 (getNoShowRate — join condicional via schedules)
    - el-templo-api/src/modules/shared/country-scope.ts (forma de request.scope)
    - el-templo-api/src/modules/analytics/service.ts:9-12 (imports de drizzle-orm y schema)
  </read_first>
  <behavior>
    - active-member: activeMemberExists(userIdColumn) devuelve un fragmento SQL`EXISTS (...)` idéntico al predicado de recomputeUserStatus (subscription_status IN ('active','paused'), start_date <= CURDATE(), end_date IS NULL OR >= CURDATE()).
    - Test "activo con sub vigente": un user con sub active y end_date futura → activeMemberExists es true (count 1).
    - Test "fantasma excluido": un user con sub cuya end_date ya pasó pero users.status='activo' → activeMemberExists es false (count 0). Este es el caso de los ~48 fantasmas.
    - applyScope absorbe ambos sabores: cuando country está definido agrega innerJoin a branches + condición branches.country; cuando branchId está definido agrega condición de columna branch. No rompe queries que ya pasan country undefined.
  </behavior>
  <action>
    Crear `shared/active-member.ts`: exportar la función `activeMemberExists(userIdColumn)` que retorna un fragmento `sql` de Drizzle con el EXISTS canónico copiado tal cual de recomputeUserStatus (subscriptions/service.ts:4119-4125). Importar `sql` de "drizzle-orm". NO crear clase ni DI. Header comment citando que la fuente de verdad es recomputeUserStatus y la Fase 117 D-01.
    Crear `analytics/scope.ts`: exportar `applyScope` que encapsula los dos sabores repetidos ~15x. Firma a discreción (D-09) pero debe recibir branchId/country + identificar la columna de branch (branchColumn) y decidir si hacer innerJoin condicional a branches. Documentar con comment los dos sabores que absorbe (join siempre vs join condicional). Importar de "drizzle-orm" y schema.
    En `analytics.test.ts`: agregar los 2 casos de activeMemberExists (activo vigente / fantasma vencido) contra MySQL real, sembrando subscriptions vía Drizzle insert (mismo patrón que tests de Fase 105/116). Usar reloj real (NO vi.useFakeTimers — desincroniza con CURDATE() de MySQL, lección Plan 103-03).
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm vitest run test/analytics/analytics.test.ts -t "activeMemberExists"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "subscription_status" el-templo-api/src/modules/shared/active-member.ts` muestra el predicado.
    - `grep -c "innerJoin" el-templo-api/src/modules/analytics/scope.ts` >= 1 (absorbe el join condicional).
    - El test "fantasma excluido" pasa: cuenta 0 para sub vencida con users.status='activo'.
    - `cd el-templo-api && pnpm tsc --noEmit` sin errores nuevos.
  </acceptance_criteria>
  <done>active-member.ts y scope.ts existen y exportan los helpers; los 2 tests del predicado pasan contra MySQL real.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bug fixes in-place (activos, no-show, multi-moneda, trend, plan distribution, perf) + tipos por moneda</name>
  <files>el-templo-api/src/modules/analytics/service.ts, el-templo-api/src/modules/analytics/types.ts, el-templo-api/test/analytics/analytics.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/analytics/service.ts:191-294 (countActiveMembers, countNewMembers — D-02, D-06)
    - el-templo-api/src/modules/analytics/service.ts:396-432 (getPlanDistribution — D-07)
    - el-templo-api/src/modules/analytics/service.ts:722-767 (getNoShowRate — D-04)
    - el-templo-api/src/modules/analytics/service.ts:773-1012 (getRevenueTrend, getRevenueByMethod, getRevenueByBranch, sumRevenue — D-05)
    - el-templo-api/src/modules/analytics/service.ts:918-969 (getOutstandingByCurrency — patrón de moneda a replicar)
    - el-templo-api/src/modules/analytics/service.ts:506-514, 1014-1057 (getDailyCheckins, computeDailyAvg — DATE() en :513,:1021 — D-08)
    - el-templo-api/src/modules/analytics/types.ts:25-89 (AttentionMember, FinancialAnalytics — tipos a cambiar a por-moneda)
    - el-templo-api/src/db/schema/subscription-plans.ts (columnas is_archived, country/branch para group by name+country — D-07)
  </read_first>
  <behavior>
    - Test no-show con datos reales: sembrar bookings con status 'confirmado' (no-show=0) y 'no_show'; getNoShowRate devuelve la proporción correcta (NO ~100%/0). Este test cierra el bug que sobrevivió por falta de cobertura (D-18).
    - Test multi-moneda: sembrar financial_transactions inflow plan_charge en ARS y EUR; en vista owner (country undefined) el resultado separa { ARS, EUR } y NUNCA los suma en un solo número.
    - Test newInPeriod activos: sembrar un user freemium nuevo y un user activo nuevo en el período; countNewMembers cuenta solo el activo (predicado canónico).
    - Test plan distribution: dos planes con mismo name 'Flex' pero country distinto (ids 1 y 105) aparecen como dos filas separadas; planes is_archived no aparecen.
  </behavior>
  <action>
    D-04: en getNoShowRate (service.ts:731) cambiar `'confirmed'` → `'confirmado'` en el `IN (...)`. El enum real es bookingStatusEnum.
    D-02: en countActiveMembers (service.ts:199-214) reemplazar `eq(schema.users.status, "activo")` por el EXISTS de `activeMemberExists(schema.users.id)` (import del helper del Task 1). Mantener `eq(schema.users.role, "member")`. Borrar el comment obsoleto :195-198 sobre el backfill del Plan 103.
    D-06: en countNewMembers (service.ts:275-279) agregar al array de conditions el predicado `activeMemberExists(schema.users.id)` para contar solo nuevos ACTIVOS, no todos role='member'.
    D-07: en getPlanDistribution (service.ts:400-426) agregar condición que filtre planes archivados (subscription_plans.is_archived = false / 0) y cambiar el groupBy de solo `name` a `(name, country)` proyectando country en el select para que "Flex (AR)" y "Flex (ES)" no se fusionen. Ajustar el tipo de retorno y el map.
    D-05: en sumRevenue, getRevenueTrend, getRevenueByMethod, getRevenueByBranch separar por moneda replicando el patrón de getOutstandingByCurrency (groupBy currency, map a { ARS, EUR }). financial_transactions.currency es la columna. Cambiar los tipos en types.ts: revenueTrend pasa de revenue:number a forma por-moneda; monthlyRevenue del KPI y FinancialAnalytics deben reflejar { ARS, EUR }. Definir el shape exacto de tipos (decisión del planner permitida) y actualizar getKpis / getMonthlyRevenueKpi en consecuencia para no romper el contrato. Mantener el filtro canónico existente (voided_at IS NULL, kind IN plan_charge/debt_settlement, direction inflow).
    D-08: reemplazar `DATE(col) >= x AND DATE(col) <= y` por rangos de medio-intervalo `col >= dateFrom AND col < dateTo+1día` en service.ts:277-278 (createdAt), :513-514 y :1021-1022 (checkedInAt) para no anular el índice. Computar dateTo+1 en JS (Date) o vía DATE_ADD; documentar el cambio en comment.
    Agregar todos los tests de behavior arriba en analytics.test.ts contra MySQL real (reloj real, no fake timers).
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm vitest run test/analytics/analytics.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "'confirmed'" el-templo-api/src/modules/analytics/service.ts` no devuelve nada (typo eliminado).
    - `grep -c "activeMemberExists" el-templo-api/src/modules/analytics/service.ts` >= 2 (countActiveMembers + countNewMembers).
    - `grep -v '^\s*//' el-templo-api/src/modules/analytics/service.ts | grep -c "DATE(" ` no incluye las líneas de checkedInAt/createdAt corregidas (solo quedan DATE() legítimos como en DATE_FORMAT/heatmap si aplica).
    - getPlanDistribution agrupa por name+country: el test de Flex AR/ES devuelve 2 filas.
    - El test de revenue multi-moneda asserta que ARS y EUR son claves separadas y nunca un solo número sumado.
    - `cd el-templo-api && pnpm tsc --noEmit` sin errores; `pnpm lint` limpio.
  </acceptance_criteria>
  <done>Los 6 bugs corregidos in-place; tipos de revenue por moneda; suite analytics.test.ts verde contra MySQL real incluyendo no-show, multi-moneda, nuevos activos y plan distribution.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                                                    |
| ----------------------------- | ---------------------------------------------------------------------------------------------- |
| admin client → /api/analytics | Query params branchId/dateFrom/dateTo (untrusted); scope derivado server-side de request.scope |
| analytics service → MySQL     | Predicado canónico debe filtrar por scope para no filtrar datos de otras sedes/países          |

## STRIDE Threat Register

| Threat ID | Category               | Component                         | Disposition | Mitigation Plan                                                                                                                                          |
| --------- | ---------------------- | --------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-117-01  | Information Disclosure | applyScope helper                 | mitigate    | applyScope debe aplicarse en TODA query nueva; los bug fixes in-place preservan el scope branchId/country existente (no se relaja ningún filtro).        |
| T-117-02  | Tampering              | getRevenueByMethod payment-method | accept      | El type guard literal-union (cash/transfer/card) ya filtra; el filtro canónico kind/direction excluye aura_credit/internal (Plan 105-04).                |
| T-117-SC  | Tampering              | npm/pip/cargo installs            | mitigate    | Este plan NO instala dependencias nuevas. Si el executor necesita una, DETENERSE y pedir aprobación humana (regla de memoria: nunca instalar sin pedir). |

Sin paquetes nuevos en este plan → sin gate de legitimidad. Si surge necesidad de instalar, checkpoint blocking-human.
</threat_model>

<verification>
- `cd el-templo-api && pnpm vitest run test/analytics/analytics.test.ts` verde.
- `cd el-templo-api && pnpm tsc --noEmit` sin errores.
- `cd el-templo-api && pnpm lint` limpio.
- grep gates de acceptance_criteria satisfechos.
</verification>

<success_criteria>

- Helper canónico `activeMemberExists` y `applyScope` existen y se usan.
- 6 bugs de FINDINGS corregidos (D-02/D-04/D-05/D-06/D-07/D-08).
- Tipos de revenue separan moneda (D-05/D-17).
- Tests de integración para activo/no-show/multi-moneda/nuevos-activos/plan-distribution (D-18).
  </success_criteria>

<output>
Create `.planning/phases/117-analytics-correcciones-de-exactitud-m-trica-de-miembros-nico/117-01-SUMMARY.md` when done
</output>
