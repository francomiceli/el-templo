---
phase: 122-ltv-vida-del-cliente
plan: 02
type: execute
wave: 2
depends_on: ["122-01"]
files_modified:
  - el-templo-api/src/modules/analytics/ltv-service.ts
  - el-templo-api/src/modules/analytics/routes.ts
  - el-templo-api/src/modules/analytics/schemas.ts
  - el-templo-api/src/modules/analytics/advanced-finance-service.ts
autonomous: true
requirements: [LTV-01, LTV-02, LTV-03, LTV-04, LTV-05]
must_haves:
  truths:
    - "GET /api/admin/analytics/ltv returns the lifetime headline = 1 ÷ churn mensual, reusing the Phase 121 ChurnService (not recomputed)"
    - "The endpoint returns the Kaplan-Meier survival median (months), treating active customers as censored data"
    - "End of life is defined by the Phase 121 matured-churn event (matured AND not retained); reactivation inherits the 15-day window"
    - "Monetary LTV is computed from REAL payments (financial_transactions), never from list price / ARPU snapshot — both projected and observed"
    - "All figures are separated by currency (ARS/EUR never summed) and broken down by branch, country, plan"
    - "gestión gets 403; admin gets 200; every query is scope-guarded via applyScope"
    - "The legacy ARPU computation is annotated @deprecated but still functions (Finanzas Avanzadas dashboard not broken)"
  artifacts:
    - path: "el-templo-api/src/modules/analytics/ltv-service.ts"
      provides: "LtvService.getLtv: headline + KM median + monetary projected/observed, per-currency, 3-axis breakdowns"
      contains: "class LtvService"
      min_lines: 120
    - path: "el-templo-api/src/modules/analytics/routes.ts"
      provides: "GET /ltv route under requireAdminAnalytics + requireBranchAccess"
      contains: "/ltv"
    - path: "el-templo-api/src/modules/analytics/schemas.ts"
      provides: "ltvSchema (window querystring 1..365 + full LtvAnalytics response declaration)"
      contains: "ltvSchema"
    - path: "el-templo-api/src/modules/analytics/advanced-finance-service.ts"
      provides: "@deprecated D-122-01 annotation on the ARPU computation (behavior unchanged)"
      contains: "@deprecated"
  key_links:
    - from: "el-templo-api/src/modules/analytics/ltv-service.ts"
      to: "el-templo-api/src/modules/analytics/churn-service.ts"
      via: "instantiate ChurnService and read window.churn.percentage for the headline"
      pattern: "ChurnService"
    - from: "el-templo-api/src/modules/analytics/ltv-service.ts"
      to: "el-templo-api/src/modules/analytics/kaplan-meier.ts"
      via: "feed the survival cohort to kaplanMeierMedian"
      pattern: "kaplanMeierMedian"
    - from: "el-templo-api/src/modules/analytics/ltv-service.ts"
      to: "financial_transactions"
      via: "canonical revenue filter, per-customer sum by memberId"
      pattern: "financialTransactions"
    - from: "el-templo-api/src/modules/analytics/routes.ts"
      to: "el-templo-api/src/modules/analytics/ltv-service.ts"
      via: "instantiate LtvService and call getLtv in the /ltv handler"
      pattern: "LtvService"
---

<objective>
Build the LTV metric end-to-end: `LtvService.getLtv` composing the Phase 121 expiry-cohort engine for the survival cohort, reusing `ChurnService` for the `1÷churn` headline, the Plan 01 `kaplanMeierMedian` helper for the robust median, and the canonical real-payment revenue filter for monetary LTV (projected + observed) — all per-currency and broken down by branch/country/plan — exposed at `GET /api/admin/analytics/ltv` under the ADMIN guard, plus the D-122-01 `@deprecated` annotation on the legacy ARPU.

Purpose: delivers LTV-01..05. Chains onto Phase 121 (the churn event IS the end of life — D-122-02/03) and Phase 120 (breakdowns + currency isolation — D-122-09), reusing not duplicating the shipped engine.

Output: `ltv-service.ts`, `GET /ltv` route, `ltvSchema`, ARPU deprecation marker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-ltv-vida-del-cliente/122-CONTEXT.md
@.planning/phases/122-ltv-vida-del-cliente/122-PATTERNS.md
@.planning/phases/122-ltv-vida-del-cliente/122-01-SUMMARY.md
@./CLAUDE.md

<interfaces>
<!-- Contracts the executor needs. Use directly — no exploration. -->

From src/modules/analytics/churn-service.ts (REUSE for the headline — instantiate, do NOT recompute churn):

```typescript
export class ChurnService {
  constructor(
    private db: MySql2Database<typeof schema>,
    private log: FastifyBaseLogger,
  ) {}
  async getChurn(filters: AnalyticsFilters): Promise<ChurnAnalytics>; // result.window.churn is MetricShape; .percentage is the monthly churn %, .n the matured cohort size; result.enGracia surfaced separately
}
```

From src/modules/analytics/expiry-cohort.ts (the SAME predicates churn uses — chain, do NOT redefine end-of-life):

```typescript
export function expiryCohortConditions(from?: string, to?: string): SQL[];
export function lastExpiryPerPersonExpr(from?: string, to?: string): SQL; // one row per person (D-04)
export function retainedExpr(windowDays: number): SQL; // true = renewed within window = continuity (D-122-04)
export function maturedExpr(windowDays: number): SQL; // true = past the window (D-08 maturity gate)
export const RENOVATION_WINDOW_DEFAULT_DAYS = 15; // the gap cutoff LTV inherits (D-122-04)
```

From src/modules/analytics/kaplan-meier.ts (Plan 01 — the robust median):

```typescript
export interface KaplanMeierObservation {
  durationMonths: number;
  event: boolean;
} // event=true closed, false censored
export function kaplanMeierMedian(
  observations: KaplanMeierObservation[],
): number | null;
```

From src/modules/analytics/scope.ts + metric-shape.ts + breakdowns.ts (foundation):

```typescript
export function applyScope(args): { conditions: SQL[] };   // SECURITY: spread .conditions into and(...), append-only
export function metricShape(nominal: number, total: number): MetricShape; // never NaN
export function breakdownSegmentKey(axis, parts): string;
export function deriveDurationTier(...): string | null;    // one-off plans -> null (drop from duration axis)
```

</interfaces>

<key_pattern_refs>

<!-- Exact line ranges from 122-PATTERNS.md — read these source files, do not re-derive -->

- Imports block + DI ctor: churn-service.ts:49-73, :123-127
- getChurn fan-out shape (Promise.all): churn-service.ts:135-152
- Per-person cohort scan (the SURVIVAL cohort source): churn-service.ts:171-184 — select {userId, matured, retained, plus the start/end dates for duration}, where expiryCohortConditions + lastExpiryPerPersonExpr + scopeConditions
- foldChurn closed/censored classification: churn-service.ts:117-121 (closed = matured && !retained; censored = matured && retained OR in-grace)
- Per-segment Map accumulator + breakdownByAxis decomposition: churn-service.ts:320-413 (innerJoin branches + subscriptionPlans, deriveDurationTier drop, breakdownSegmentKey, deterministic localeCompare sort)
- Scope on subscriptions.branchId (cohort): scope.ts:55, churn-service.ts:165-169
- Scope on users.branchId + unconditional branches join (the MONEY query, flavor A): advanced-finance-service.ts:170-214 (cashTrend)
- Canonical revenue filter: advanced-finance-service.ts:177-185 (kind IN(...), direction='inflow', voidedAt IS NULL, transactionDate in [from,to])
- Per-currency Map accumulator + ARS/EUR-only guard: ticket-service.ts:189-202, advanced-finance-service.ts:217-223
- ARPU div-by-zero guard to mirror: advanced-finance-service.ts:148-150
- ARPU computation to DEPRECATE (do NOT delete/change math): advanced-finance-service.ts arpu series :138,:147-152,:157 + activeMemberCount :349-375
- D-09 deprecation precedent (the @deprecated marker style): service.ts (Phase 121 markers on countChurnedMembers/computeRetentionRate/getRenewalRate)
- Route block to mirror: routes.ts:78 (instantiate near churnService), :397-433 (the /churn block with window querystring + handleServiceError)
- Schema to mirror: schemas.ts:584-592 (local churnQuerystring window 1..365), :597-604 (churnMetricShapeSchema), :619-663 (churnSchema), :9-15 (errorSchema)
  </key_pattern_refs>

<financial_transactions_schema>

<!-- src/db/schema/financial-transactions.ts:22-56 — customer link is memberId, NO subscription FK on the row -->

memberId (FK users.id, notNull) · kind enum (plan_charge|debt_settlement|refund|adjustment|advance_payment) · direction enum (inflow|outflow) · amount (int, notNull) · currency varchar(3) default ARS · transactionDate/effectiveDate (date string, notNull) · branchId (FK, notNull) · recordedBy (FK, notNull) · paymentMethod enum (notNull) · voidedAt (timestamp, nullable — canonical filter requires IS NULL)
</financial_transactions_schema>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LtvService — headline, Kaplan-Meier median, monetary projected/observed, per-currency, breakdowns</name>
  <files>el-templo-api/src/modules/analytics/ltv-service.ts</files>
  <read_first>
    - el-templo-api/src/modules/analytics/churn-service.ts (the FULL structural analog — imports :49-73, DI ctor :123-127, fan-out :135-152, cohort scan :171-184, foldChurn :117-121, breakdownByAxis :320-413)
    - el-templo-api/src/modules/analytics/advanced-finance-service.ts (the money analog — cashTrend query :170-214, canonical filter :177-185, per-currency guard :217-223, ARPU guard :148-150)
    - el-templo-api/src/modules/analytics/ticket-service.ts (per-currency Map accumulator + ARS/EUR-only guard :189-202)
    - el-templo-api/src/modules/analytics/kaplan-meier.ts (Plan 01 helper signature)
    - el-templo-api/src/modules/analytics/expiry-cohort.ts (the cohort predicates)
    - el-templo-api/src/modules/analytics/types.ts (the LtvAnalytics contract from Plan 01)
    - el-templo-api/src/modules/analytics/cohorts.ts (dayDiff/monthStart/nextMonth for duration-in-months math)
  </read_first>
  <behavior>
    - Headline (LTV-01/D-122-03): instantiate ChurnService inside getLtv, call getChurn(filters), read window.churn.percentage as monthly churn %. lifetime_headline = 1 / (percentage/100) months. Guard churn 0 -> null (NEVER NaN/Infinity), mirroring the ARPU guard.
    - Survival cohort (LTV-02/03, D-122-02/04): build from the SAME per-person expiry-cohort scan churn uses (churn-service.ts:171-184) — also select the person's first startDate and last endDate. Classify each: closed life = matured && !retained (event=true); censored life = matured && retained OR in-grace (event=false). Duration in months = (endDate − first startDate)/~30 for closed, (today − first startDate)/~30 for censored. Feed {durationMonths, event} to kaplanMeierMedian. Active/in-grace customers are CENSORED, never dropped.
    - Monetary LTV (LTV-04, D-122-07/08): from financial_transactions ONLY (canonical revenue filter — kind plan_charge [+ debt_settlement per the advanced-finance precedent], direction inflow, voidedAt IS NULL, transactionDate in range), NEVER price_regular/list. Projected = lifetime_headline × (real monthly revenue per customer average). Observed (closed lives) = exact SUM of real payments over the closed customer's life; real_monthly_revenue = observed ÷ lifetime. Per-customer grouped by memberId.
    - Per-currency (LTV-05, D-122-09): ARS and EUR NEVER summed — per-currency Map accumulator; subscription currency for the cohort figures, financialTransactions.currency for the money; unknown currency skipped (never summed).
    - Breakdowns (LTV-05): branch / country / plan via breakdownByAxis (duration axis optional — planner discretion, may include).
  </behavior>
  <action>
    Create `LtvService` mirroring `ChurnService`: copy the imports block (churn-service.ts:49-73) adding the `kaplanMeierMedian`/`KaplanMeierObservation` import and the `LtvAnalytics` wire types; same constructor DI (`db`, `log`). Implement `async getLtv(filters: AnalyticsFilters): Promise<LtvAnalytics>` with a `Promise.all` fan-out over: (a) headline via an instantiated `ChurnService.getChurn`; (b) the survival cohort scan (reuse `expiryCohortConditions` + `lastExpiryPerPersonExpr` + `applyScope` on `subscriptions.branchId`, selecting userId, matured, retained, min(startDate), max(endDate)) → classify closed/censored → compute `durationMonths` (use the `cohorts.ts` day-arithmetic helpers, not hand-rolled date math) → `kaplanMeierMedian`; (c) the monetary query (canonical revenue filter on `financialTransactions`, `applyScope` on `users.branchId` with the unconditional `branches` join per advanced-finance-service.ts:170-214, grouped by `memberId` and `currency`). Compose projected and observed LTV per <behavior>. Accumulate everything in per-currency `Map`s (ARS/EUR only, mirror ticket-service.ts:189-202). Implement private `breakdownByAxis` for branch/country/plan exactly like churn-service.ts:320-413 (innerJoin branches+subscriptionPlans, `deriveDurationTier` drop for one-off, `breakdownSegmentKey`, deterministic `.sort((a,b)=>a.key.localeCompare(b.key))`). Effective window = `filters.window ?? RENOVATION_WINDOW_DEFAULT_DAYS`. ALL division guarded (never NaN — `metricShape`/explicit null). No `any`; use `request.log`-style logger only via the DI `log` field; no `console.log`.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit -p tsconfig.json && grep -q "class LtvService" src/modules/analytics/ltv-service.ts && grep -q "kaplanMeierMedian" src/modules/analytics/ltv-service.ts && grep -q "ChurnService" src/modules/analytics/ltv-service.ts && grep -q "financialTransactions" src/modules/analytics/ltv-service.ts</automated>
  </verify>
  <acceptance_criteria>
    - `LtvService.getLtv(filters): Promise<LtvAnalytics>` exists.
    - Headline reuses ChurnService (`grep -c "new ChurnService\|churnService" src/modules/analytics/ltv-service.ts` >= 1) — churn is NOT recomputed inline.
    - Survival median uses `kaplanMeierMedian`; censored persons are classified `event: false` (active/in-grace NOT dropped — visible in the closed/censored classification).
    - Monetary LTV queries `financialTransactions` with the canonical filter (`grep -E "voidedAt|voided_at" src/modules/analytics/ltv-service.ts` finds the IS NULL guard; `grep -iE "priceRegular|price_regular|listPrice" src/modules/analytics/ltv-service.ts` returns NOTHING — D-122-08 list price never used).
    - Per-currency isolation present (`grep -E "ARS|EUR" src/modules/analytics/ltv-service.ts` finds the ARS/EUR-only guard; no single sum across currencies).
    - `applyScope` invoked for every cohort and money query (`grep -c "applyScope" src/modules/analytics/ltv-service.ts` >= 2).
    - `grep -v '^[[:space:]]*\*' src/modules/analytics/ltv-service.ts | grep -c ': any'` returns 0; `grep -c "console.log" src/modules/analytics/ltv-service.ts` returns 0.
    - `pnpm exec tsc --noEmit` passes.
  </acceptance_criteria>
  <done>LtvService delivers headline (1÷churn reusing ChurnService), KM survival median (censored-aware), monetary projected+observed from real payments (never list price), per-currency, branch/country/plan breakdowns; all division guarded; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: GET /ltv route + ltvSchema + ARPU @deprecated annotation (D-122-01)</name>
  <files>el-templo-api/src/modules/analytics/routes.ts, el-templo-api/src/modules/analytics/schemas.ts, el-templo-api/src/modules/analytics/advanced-finance-service.ts</files>
  <read_first>
    - el-templo-api/src/modules/analytics/routes.ts (the /churn block :397-433, churnService instantiation :78)
    - el-templo-api/src/modules/analytics/schemas.ts (churnQuerystring :584-592, churnMetricShapeSchema :597-604, churnSchema :619-663, errorSchema :9-15)
    - el-templo-api/src/modules/analytics/advanced-finance-service.ts (the arpu series :138,:147-152,:157 and activeMemberCount :349-375 to annotate — do NOT change the math)
    - el-templo-api/src/modules/analytics/service.ts (the Phase 121 D-09 @deprecated marker style to copy)
  </read_first>
  <action>
    In `schemas.ts`: add a private `ltvQuerystring` const (copy `churnQuerystring`, `window: { type: "integer", minimum: 1, maximum: 365 }`) WITHOUT mutating the shared `analyticsQuerystring`; add a reusable `ltvMetricShapeSchema` (pattern: `churnMetricShapeSchema`); add `ltvSchema` declaring the FULL 200 response — headline (`["number","null"]`), KM median (`["number","null"]`, D-122-05), the per-currency monetary block (ARS/EUR projected + observed + real monthly revenue, `["number","null"]` where empty), `n`, and the breakdowns array — plus `400/401/403/500: errorSchema`. Declare EVERY field (fast-json-stringify strips undeclared fields). In `routes.ts`: import + instantiate `LtvService` next to `churnService` (:78); register `GET /ltv` mirroring the `/churn` block (:397-433) with `schema: ltvSchema`, `preHandler: [requireAdminAnalytics, requireBranchAccess({ from: "query.branchId", optional: true })]`, build `AnalyticsFilters` from query + `request.scope.country ?? undefined` + `window: request.query.window`, call `ltvService.getLtv(filters)`, and `handleServiceError(err, reply, request.log, "get ltv")` in `catch (err: unknown)`. Declare `window?: number` in the route's `Querystring` generic. In `advanced-finance-service.ts`: annotate the ARPU computation (the `arpu` series + its `activeMemberCount` denominator) with a `@deprecated Phase 122 D-122-01 — replaced by GET /ltv monetary LTV (real-payment based, non-snapshot)` doc-comment + inline marker, copying the Phase 121 D-09 style — leave the math, schema field, type, and callers UNCHANGED so Finanzas Avanzadas keeps working.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit -p tsconfig.json && grep -q "ltvSchema" src/modules/analytics/schemas.ts && grep -q '"/ltv"' src/modules/analytics/routes.ts && grep -q "requireAdminAnalytics" src/modules/analytics/routes.ts && grep -q "D-122-01" src/modules/analytics/advanced-finance-service.ts</automated>
  </verify>
  <acceptance_criteria>
    - `schemas.ts` declares `ltvSchema` with 200 + 400/401/403/500; `window` bounded 1..365 in a LOCAL `ltvQuerystring` (`grep -c "analyticsQuerystring =" src/modules/analytics/schemas.ts` unchanged — shared const not mutated).
    - `routes.ts` registers `GET "/ltv"` with `requireAdminAnalytics` + `requireBranchAccess` preHandler and `handleServiceError(..., "get ltv")` in the catch.
    - `LtvService` is instantiated in `routes.ts` (`grep -c "LtvService" src/modules/analytics/routes.ts` >= 2 — import + new).
    - `advanced-finance-service.ts` contains `@deprecated` + `D-122-01` on the ARPU computation; the `arpu` field, its schema declaration (schemas.ts:446), and its type (types.ts:364) are UNCHANGED (`git diff --stat` shows no edit to those lines beyond the annotation; the arpu math expression is byte-identical).
    - `grep -v '^[[:space:]]*\*' src/modules/analytics/routes.ts | grep -c ': any'` returns 0.
    - `pnpm exec tsc --noEmit` passes.
  </acceptance_criteria>
  <done>GET /ltv is registered under the ADMIN guard with a fully-declared ltvSchema (window 1..365); the legacy ARPU is annotated @deprecated D-122-01 but still functions; tsc clean.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                                                                                               |
| ------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| client → GET /api/admin/analytics/ltv | Untrusted query params (branchId, dateFrom, dateTo, window) cross from an authenticated admin client into the analytics service and reach SQL predicates. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                       | Disposition | Mitigation Plan                                                                                                                                                                                                                                                              |
| --------- | ---------------------- | --------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-122-02  | Elevation of Privilege | GET /ltv                                                        | mitigate    | `requireAdminAnalytics` (ADMIN_ROLES only) — gestión gets 403; same guard as /churn and /renewal (test-covered in Plan 03)                                                                                                                                                   |
| T-122-03  | Information Disclosure | scope leakage across branches/countries                         | mitigate    | Every cohort + money query spreads `applyScope(...).conditions` (subscriptions.branchId for the cohort, users.branchId for the money); `request.scope.country` drives the country filter, never client input; breakdown axes are additive groupBy keys, never access filters |
| T-122-04  | Tampering              | `window` integer injection into SQL fragments                   | mitigate    | `window` validated integer-bounded 1..365 at `ltvSchema` before reaching the SERVICE-controlled `sql.raw(String(n))` inside expiry-cohort.ts; date bounds are bound parameters                                                                                               |
| T-122-05  | Information Disclosure | undeclared response fields leaking internal shape               | mitigate    | `ltvSchema` declares ONLY the intended LtvAnalytics fields; fast-json-stringify strips anything else                                                                                                                                                                         |
| T-122-06  | Denial of Service      | ARPU deprecation breaking the live Finanzas Avanzadas dashboard | accept      | D-122-01 mandates annotate-only; arpu math/schema/type/callers UNCHANGED, so the existing dashboard keeps functioning; physical removal deferred to the admin-UI phase                                                                                                       |
| T-122-SC  | Tampering              | npm/pip/cargo installs                                          | mitigate    | No package installs (reuses existing drizzle/fastify deps); nothing to verify                                                                                                                                                                                                |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit` passes for the api package.
- `LtvService.getLtv` reuses `ChurnService` for the headline, `kaplanMeierMedian` for the median, and `financialTransactions` (canonical filter, never list price) for the money — all per-currency and scope-guarded.
- `GET /ltv` is registered under `requireAdminAnalytics` + `requireBranchAccess`.
- The legacy ARPU carries `@deprecated D-122-01` but its math/schema/type are byte-unchanged.
- Integration coverage (gestión 403, admin 200, headline=1÷churn consistency, censored-not-dropped, observed=exact real sum, per-currency isolation, breakdowns) is delivered by Plan 03 and runs in CI on the staging push (project policy — ask before pushing).
</verification>

<success_criteria>

- LTV-01: headline `1 ÷ churn mensual` reusing the Phase 121 ChurnService, by the standard breakdowns and per currency.
- LTV-02: Kaplan-Meier survival median, active customers censored (not dropped).
- LTV-03: end of life = the Phase 121 matured-churn event; reactivation inherits the 15-day window.
- LTV-04: monetary LTV from real payments — projected AND observed — never ARPU snapshot / list price.
- LTV-05: separated by currency (ARS/EUR never summed), broken down by branch/country/plan.
- D-122-01: ARPU annotated deprecated, still functioning.
  </success_criteria>

<output>
Create `.planning/phases/122-ltv-vida-del-cliente/122-02-SUMMARY.md` when done.
</output>
