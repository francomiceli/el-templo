---
phase: 122-ltv-vida-del-cliente
plan: 03
type: execute
wave: 3
depends_on: ["122-02"]
files_modified:
  - el-templo-api/test/analytics/ltv.test.ts
autonomous: true
requirements: [LTV-01, LTV-02, LTV-03, LTV-04, LTV-05]
must_haves:
  truths:
    - "An integration test proves headline = 1 ÷ churn is consistent with ChurnService for identical filters"
    - "The test proves censored (still-active) customers are NOT dropped from the Kaplan-Meier cohort"
    - "The test proves observed monetary LTV equals the EXACT sum of the closed customer's real payments, and list price is NOT used"
    - "The test proves ARS and EUR are never summed (per-currency isolation)"
    - "The test proves gestión gets 403 and admin gets 200 with the LtvAnalytics wire shape"
  artifacts:
    - path: "el-templo-api/test/analytics/ltv.test.ts"
      provides: "Real-MySQL integration coverage for LTV-01..05 (CI-only)"
      contains: "/ltv"
      min_lines: 150
  key_links:
    - from: "el-templo-api/test/analytics/ltv.test.ts"
      to: "el-templo-api/src/modules/analytics/ltv-service.ts"
      via: "instantiate LtvService + ChurnService and assert cross-metric consistency"
      pattern: "LtvService"
    - from: "el-templo-api/test/analytics/ltv.test.ts"
      to: "financial_transactions"
      via: "seed real payment rows below list price and assert observed LTV uses the real amount"
      pattern: "financialTransactions"
---

<objective>
Cover the LTV endpoint with a real-MySQL integration test mirroring `renewal.test.ts` / `churn.test.ts`: headline=1÷churn consistency with `ChurnService`, Kaplan-Meier censored-not-dropped behavior, monetary observed=exact-real-sum (list price NOT used), per-currency isolation, branch/country/plan breakdowns, and the ADMIN 403/200 wire shape.

Purpose: closes the project test discipline (CLAUDE.md: new API routes MUST have integration tests against real MySQL) and proves LTV-01..05 end-to-end. Split from Plan 02 because the integration test is its own context-heavy concern (seed scaffolding + cross-service assertions + financial_transactions seeding) and touches no source files Plan 02 owns.

Output: `test/analytics/ltv.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-ltv-vida-del-cliente/122-CONTEXT.md
@.planning/phases/122-ltv-vida-del-cliente/122-PATTERNS.md
@.planning/phases/122-ltv-vida-del-cliente/122-02-SUMMARY.md
@./CLAUDE.md

<seed_conventions>

<!-- Phase 120/121 lessons, test-covered. From renewal.test.ts. VIOLATING ANY OF THESE = flaky/broken test. -->

- Members: insert with `passwordHash: "x"` (NOT `password`) — renewal.test.ts:107-114.
- Subscriptions: seed with `priceTypeApplied: "regular"` (notNull) — renewal.test.ts:133-143.
- Dates: derive EVERY startDate/endDate from `CURDATE()` via the `dateOffset(days)` helper — renewal.test.ts:147-161. Maturity/duration assertions fail after ~21:00 AR if you use JS `new Date()` (UTC-vs-local skew flake — see MEMORY). Always CURDATE-derived in SQL.
- Scaffolding: copy `createTestApp()` / `cleanAllTestData(app)` / `beforeEach` from renewal.test.ts.
- ES branch seeding (for the per-currency EUR case): renewal.test.ts:54-85.
- Cross-service assertion: instantiate BOTH services like renewal.test.ts:55-57, 196-206 (renewal.test.ts asserts renewal.n === churn.window.churn.n; the LTV analog asserts the headline derives from churn.window.churn.percentage for identical filters).
- Auth: gestión token → 403, admin → 200 — renewal.test.ts:412-454.
- financial_transactions seeding (notNull columns ALL required): memberId, kind ('plan_charge'), direction ('inflow'), amount, currency, paymentMethod, transactionDate, effectiveDate, branchId, recordedBy, voidedAt (null).
- Tests run in CI ONLY (project policy: never run the suite locally; typecheck local is fine). Ask before pushing to staging so CI runs.
  </seed_conventions>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Integration test ltv.test.ts (LTV-01..05 + auth)</name>
  <files>el-templo-api/test/analytics/ltv.test.ts</files>
  <read_first>
    - el-templo-api/test/analytics/renewal.test.ts (the closest analog — instantiates BOTH services, ES branch seeding :54-85, member inserts :107-114, sub seeding :133-143, dateOffset :147-161, auth 403/200 :412-454)
    - el-templo-api/test/analytics/churn.test.ts (the maturity/censoring seed cases)
    - el-templo-api/test/helpers.ts (createTestApp, cleanAllTestData, auth/request utilities, dateOffset)
    - el-templo-api/src/modules/analytics/ltv-service.ts (the Plan 02 service under test — its getLtv shape)
    - el-templo-api/src/db/schema/financial-transactions.ts (the notNull columns to seed)
  </read_first>
  <action>
    Create `test/analytics/ltv.test.ts` mirroring `renewal.test.ts`. Cover:
    (1) Headline=1÷churn consistency: instantiate BOTH `LtvService` and `ChurnService`, seed a matured cohort, assert the LTV headline equals `1 / (churn.window.churn.percentage/100)` (or null when churn is 0) for identical filters.
    (2) Kaplan-Meier censored-not-dropped: seed a cohort mixing closed (matured AND not renewed) and censored (still-active / renewed-within-window) lives; assert the censored persons remain in the cohort `n` and influence the survival median (NOT discarded) — D-122-05.
    (3) Monetary observed = exact real sum, list price NOT used: seed a closed customer with `financialTransactions` rows whose `amount` is BELOW the plan's list price; assert observed LTV equals the SUM of those real amounts (not the list price) — D-122-08.
    (4) Per-currency isolation: seed AR (ARS) + ES (EUR) customers + payments; assert ARS and EUR figures are reported separately and never summed — D-122-09.
    (5) Breakdowns: assert branch / country / plan breakdown rows carry the `{nominal,percentage,n}` envelope (or the per-currency monetary fields).
    (6) Auth: gestión token → 403; admin token → 200 with the LtvAnalytics wire shape (headline, KM median, per-currency monetary block, breakdowns all present).
    Use `passwordHash:"x"` members, `priceTypeApplied:"regular"` subs, CURDATE-derived `dateOffset` dates, and seed `financialTransactions` with ALL notNull columns. Copy `createTestApp()`/`cleanAllTestData(app)`/`beforeEach`. No `any`; no `console.log`.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit -p tsconfig.json && grep -q "/ltv\|LtvService\|getLtv" test/analytics/ltv.test.ts && grep -q "financialTransactions" test/analytics/ltv.test.ts && grep -q "passwordHash" test/analytics/ltv.test.ts && grep -q "dateOffset" test/analytics/ltv.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ltv.test.ts` instantiates BOTH `LtvService` and `ChurnService` and asserts the headline derives from churn for identical filters.
    - A test seeds censored (still-active) lives and asserts they are NOT dropped from the KM cohort (D-122-05).
    - A test seeds `financialTransactions` amounts below list price and asserts observed LTV uses the real amount, not list price (D-122-08): `grep -iE "passwordHash" test/analytics/ltv.test.ts` present; `grep -c "financialTransactions" test/analytics/ltv.test.ts` >= 1.
    - A test seeds AR + ES and asserts ARS/EUR are never summed (per-currency isolation, D-122-09).
    - A gestión-token request asserts 403 and an admin request asserts 200 with the LtvAnalytics shape.
    - All dates derive from `CURDATE()` via `dateOffset` (`grep -c "new Date()" test/analytics/ltv.test.ts` for seeding is 0 — no JS-clock seeding; TZ-flake safe).
    - `grep -v '^[[:space:]]*\*' test/analytics/ltv.test.ts | grep -c ': any'` returns 0.
    - `pnpm exec tsc --noEmit` passes.
  </acceptance_criteria>
  <done>ltv.test.ts covers LTV-01..05 + auth against real MySQL with TZ-safe CURDATE-derived seeding, financial_transactions seeded with all notNull columns, and cross-service consistency assertions; tsc clean; CI-only.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary    | Description                                                                                                                                                            |
| ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (test-only) | This plan adds an integration test. It does not introduce a production trust boundary; it VERIFIES the boundaries Plan 02 introduced (authz, scope, input validation). |

## STRIDE Threat Register

| Threat ID | Category               | Component                                   | Disposition | Mitigation Plan                                                                                                      |
| --------- | ---------------------- | ------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------- |
| T-122-07  | Elevation of Privilege | /ltv authz regression                       | mitigate    | The test asserts gestión → 403 and admin → 200, locking the `requireAdminAnalytics` guard against regression         |
| T-122-08  | Information Disclosure | per-currency / scope correctness regression | mitigate    | The per-currency-isolation and breakdown tests lock ARS/EUR separation and additive-axis behavior against regression |
| T-122-SC  | Tampering              | npm/pip/cargo installs                      | mitigate    | No package installs (uses existing vitest + drizzle test deps); nothing to verify                                    |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit` passes.
- `ltv.test.ts` covers headline=1÷churn consistency, KM censored-not-dropped, observed=exact-real-sum (list price unused), per-currency isolation, breakdowns, and 403/200 auth.
- Seeding uses `passwordHash`, `priceTypeApplied:"regular"`, CURDATE-derived `dateOffset`, and full-column `financialTransactions` rows.
- The suite runs in CI on the staging push (project policy — never run locally; ask before pushing).
</verification>

<success_criteria>

- LTV-01..05 are proven end-to-end against real MySQL: the headline, the censored-aware Kaplan-Meier median, the real-payment monetary LTV (observed exact, list price unused), per-currency isolation, and the branch/country/plan breakdowns — plus the ADMIN-only access gate.
  </success_criteria>

<output>
Create `.planning/phases/122-ltv-vida-del-cliente/122-03-SUMMARY.md` when done.
</output>
