---
phase: 111-salvaguardas-operativas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/shared/phone.ts
  - el-templo-api/src/modules/shared/index.ts
  - el-templo-api/test/shared/phone.test.ts
  - el-templo-api/src/modules/members/service.ts
  - el-templo-admin/src/utils/phone.ts
autonomous: true
requirements:
  - REQ-9
must_haves:
  truths:
    - "normalizePhone('+54 223 661 4406') returns '2236614406'"
    - "normalizePhone('(0223) 661-4406') returns '2236614406'"
    - "normalizePhone('') returns ''"
    - "Inserting a member with firstName='  Soledad  ' results in 'Soledad' in DB"
    - "Updating a member's lastName to '  Mailland  ' results in 'Mailland' in DB"
  artifacts:
    - path: "el-templo-api/src/modules/shared/phone.ts"
      provides: "normalizePhone helper (last 10 digits, AR convention)"
      exports: ["normalizePhone"]
    - path: "el-templo-api/test/shared/phone.test.ts"
      provides: "Unit tests for normalizePhone"
    - path: "el-templo-admin/src/utils/phone.ts"
      provides: "Frontend mirror of normalizePhone (1:1 copy with sync comment)"
      exports: ["normalizePhone"]
  key_links:
    - from: "el-templo-api/src/modules/members/service.ts"
      to: ".trim() on firstName/lastName in createMember + updateMember"
      via: "input transformation before insert/update"
      pattern: "firstName.trim\\(\\)"
---

<objective>
Establish two pre-existing-but-missing primitives that the rest of phase 111 consumes: `normalizePhone()` helper (REQ-9 + D-24) on both backend and frontend, and `.trim()` of firstName/lastName in members service (REQ-9 + D-26).

Purpose: REQ-4 (check-duplicates) and REQ-5 (autorregister phone block) cannot work without phone normalization. Trim of names prevents repeating the bug where Soledad's account was created with `lastName='Mailland '` (trailing space).
Output: `shared/phone.ts` (backend), `utils/phone.ts` (admin frontend), unit tests, trim applied in members service (create + update).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/111-salvaguardas-operativas/111-SPEC.md
@.planning/phases/111-salvaguardas-operativas/111-CONTEXT.md
@.planning/phases/111-salvaguardas-operativas/111-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Existing analog: pure helper module export -->
From `el-templo-api/src/modules/shared/date-utils.ts:15-19`:
```typescript
export function addDays(dateStr: string, days: number): string {
  const d = new Date(dateStr + "T12:00:00Z");
  d.setUTCDate(d.getUTCDate() + days);
  return d.toISOString().split("T")[0];
}
```

From `el-templo-api/src/modules/shared/index.ts` (barrel — confirm by reading; pattern: re-export each shared utility).

From `el-templo-api/src/modules/members/service.ts:405-449` (createMember insert) and `:454-500` (updateMember update) — apply .trim() to firstName / lastName before passing to `db.insert/update`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: normalizePhone helper (backend) + unit tests</name>
  <files>
    el-templo-api/src/modules/shared/phone.ts (NEW)
    el-templo-api/src/modules/shared/index.ts (modified — add re-export)
    el-templo-api/test/shared/phone.test.ts (NEW)
  </files>
  <read_first>
    - el-templo-api/src/modules/shared/date-utils.ts (pure helper export pattern, JSDoc style)
    - el-templo-api/src/modules/shared/index.ts (barrel re-export pattern)
    - el-templo-api/test/branch-access.test.ts OR el-templo-api/test/country-scope.test.ts (test bootstrap for shared helpers; pick whichever exists at root of test/)
    - el-templo-api/vitest.config.ts (confirm test file naming convention .test.ts)
  </read_first>
  <behavior>
    - normalizePhone("+54 223 661 4406") returns "2236614406"
    - normalizePhone("(0223) 661-4406") returns "2236614406"
    - normalizePhone("2236614406") returns "2236614406"
    - normalizePhone("") returns ""
    - normalizePhone("123") returns "123" (less than 10 digits — pass through, slice(-10) is no-op)
    - normalizePhone("12345678901234567890") returns "1234567890" (last 10 digits only)
    - normalizePhone("abc") returns "" (no digits)
  </behavior>
  <action>
    Create `el-templo-api/src/modules/shared/phone.ts` with EXACTLY this content:

    ```typescript
    /**
     * Normalize a phone string to "last 10 digits" — AR mobile convention.
     * Strips all non-digit characters, then keeps the trailing 10. Empty input → empty string.
     *
     * Used by:
     *  - admin /admin/members/check-duplicates (members/service.ts checkDuplicates)
     *  - autorregistro /auth/register phone duplicate block
     *
     * Mirrored 1:1 in el-templo-admin/src/utils/phone.ts (manual sync — no shared TS package).
     */
    export function normalizePhone(input: string): string {
      return input.replace(/\D/g, "").slice(-10);
    }
    ```

    Update `el-templo-api/src/modules/shared/index.ts` to re-export: read the file first, follow whatever barrel pattern exists (likely `export * from "./phone"` if it uses `export *`, or `export { normalizePhone } from "./phone"` if named).

    Create `el-templo-api/test/shared/phone.test.ts` with vitest tests covering all 7 behavior cases above. Follow the bootstrap of an existing simple shared-helper test (no MySQL needed — pure unit test). If `test/shared/` directory doesn't exist, create it.

    Run `pnpm test --filter=phone` (or equivalent vitest command discovered from package.json) — all 7 cases must pass.

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-api && pnpm test test/shared/phone.test.ts 2>&1 | grep -E "(passed|failed)"</automated>
  </verify>
  <acceptance_criteria>
    - File `el-templo-api/src/modules/shared/phone.ts` exists with `export function normalizePhone(input: string): string`
    - `grep -n "normalizePhone" el-templo-api/src/modules/shared/index.ts` returns at least 1 line (barrel re-export present)
    - File `el-templo-api/test/shared/phone.test.ts` exists with at least 7 test cases (one per behavior listed above)
    - `cd el-templo-api && pnpm test test/shared/phone.test.ts` exits 0 with all tests passing
    - Implementation body is exactly `return input.replace(/\D/g, "").slice(-10);` (no extra logic)
  </acceptance_criteria>
  <done>normalizePhone exists, exported via barrel, all 7 unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: normalizePhone helper (admin frontend mirror)</name>
  <files>
    el-templo-admin/src/utils/phone.ts (NEW)
  </files>
  <read_first>
    - el-templo-admin/src/utils/extract-error.ts (existing utility shape — JSDoc, named export style)
    - el-templo-api/src/modules/shared/phone.ts (the file just created in Task 1 — copy content verbatim)
  </read_first>
  <action>
    Create `el-templo-admin/src/utils/phone.ts` with EXACTLY this content (1:1 mirror of backend per D-25):

    ```typescript
    /**
     * Normalize a phone string to "last 10 digits" — AR mobile convention.
     * Strips all non-digits, then keeps the trailing 10. Empty input → empty string.
     *
     * Mirrors el-templo-api/src/modules/shared/phone.ts (kept in sync manually
     * since admin and api are separate apps without a shared TS package).
     * If you change one, change the other.
     */
    export function normalizePhone(input: string): string {
      return input.replace(/\D/g, "").slice(-10);
    }
    ```

    Skip frontend tests — the function is identical to backend (already covered by backend tests) and admin does not currently have a vitest setup for utils. Per D-25: minimum-effort mirror with sync comment.

  </action>
  <verify>
    <automated>diff <(grep -A2 "export function normalizePhone" /home/franco/projects/el-templo/el-templo-api/src/modules/shared/phone.ts | tail -2) <(grep -A2 "export function normalizePhone" /home/franco/projects/el-templo/el-templo-admin/src/utils/phone.ts | tail -2)</automated>
  </verify>
  <acceptance_criteria>
    - File `el-templo-admin/src/utils/phone.ts` exists
    - `grep -c "input.replace(/\\\\D/g" el-templo-admin/src/utils/phone.ts` returns 1
    - `grep -c "slice(-10)" el-templo-admin/src/utils/phone.ts` returns 1
    - Implementation body matches backend version exactly (diff against backend phone.ts shows only JSDoc differs, body identical)
    - JSDoc contains the string "Mirrors el-templo-api/src/modules/shared/phone.ts" (sync warning preserved)
  </acceptance_criteria>
  <done>Frontend mirror exists with sync comment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Trim firstName/lastName in members service (create + update)</name>
  <files>
    el-templo-api/src/modules/members/service.ts (modified)
  </files>
  <read_first>
    - el-templo-api/src/modules/members/service.ts (specifically lines 405-500, createMember + updateMember)
    - el-templo-api/test/members.test.ts OR el-templo-api/test/members/*.test.ts (test bootstrap, helpers)
    - el-templo-api/test/helpers.ts (auth + request helpers)
  </read_first>
  <behavior>
    - POST /admin/members with body firstName="  Soledad  ", lastName="  Mailland  " → DB row has firstName="Soledad", lastName="Mailland"
    - PATCH /admin/members/:id with body firstName="  Updated " → DB row has firstName="Updated"
    - Empty-after-trim is rejected by existing validation (no behavior change there — just trim before validate-against-schema or before insert; choose the path that keeps current validation logic intact)
    - No regression: existing member tests still pass
  </behavior>
  <action>
    In `el-templo-api/src/modules/members/service.ts`:

    1. Locate `createMember` (around line 405) and find the `db.insert(schema.users).values({...})` block.
    2. Apply `.trim()` to `firstName` and `lastName` before they reach the insert. If the existing code passes `input.firstName` directly, change it to `input.firstName.trim()`. If a destructured `firstName` variable is built first, add a normalization step right after destructuring:
       ```typescript
       const firstName = input.firstName.trim();
       const lastName = input.lastName.trim();
       ```
       Use whichever pattern is cleaner given the current code shape (read first).
    3. Repeat for `updateMember` (around line 454-500): if `input.firstName !== undefined`, apply `.trim()` before update; same for lastName. Be careful: only trim if the field is being updated (don't introduce NULL when caller didn't pass it).
    4. Add NEW tests in `el-templo-api/test/members.test.ts` (or a new file `test/members/trim.test.ts` if the structure suggests per-feature files):
       - Test: createMember with `firstName="  Soledad  "` → DB row has `firstName="Soledad"` (verify via direct Drizzle SELECT after the API call)
       - Test: updateMember with `lastName="  Mailland "` → DB row updated to `lastName="Mailland"`
    5. Run full members test suite. All previously-passing tests must still pass.

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-api && pnpm test test/members 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "firstName.*\\.trim\\(\\)" el-templo-api/src/modules/members/service.ts` returns at least 2 matches (one in createMember, one in updateMember)
    - `grep -nE "lastName.*\\.trim\\(\\)" el-templo-api/src/modules/members/service.ts` returns at least 2 matches
    - At least 2 NEW test cases exist in members test file(s) referencing trailing/leading spaces (grep `"  Soledad  "` OR `"  Mailland "` in `el-templo-api/test/members*` files returns ≥1 match)
    - `cd el-templo-api && pnpm test test/members` exits 0 with all tests passing
    - No regression: all previously-passing tests in members test file still pass
  </acceptance_criteria>
  <done>Trim applied in service layer (create + update), 2 new tests pass, no regressions.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                        |
| ----------------------------------------- | ------------------------------------------------------------------ |
| HTTP request body → admin API             | Untrusted strings (firstName, lastName, phone) cross into DB layer |
| HTTP request body → public /auth/register | Untrusted (less than admin) — same trust profile                   |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                                 | Disposition | Mitigation Plan                                                                                                                                                |
| --------- | ---------------------- | ----------------------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-111-01  | Tampering              | normalizePhone helper output                                                              | accept      | Pure transform, deterministic, no state. No mitigation needed beyond unit-test coverage of edge cases.                                                         |
| T-111-02  | Information Disclosure | normalizePhone could leak structure of phone numbers via timing                           | accept      | Function is constant-time string operations on bounded input length. No timing leak material.                                                                  |
| T-111-03  | Tampering              | Unicode whitespace bypass of `.trim()` (e.g., U+00A0 NBSP, zero-width chars in firstName) | accept      | JS `.trim()` covers all Unicode whitespace per ECMAScript spec — adversarial NBSP-only names yield empty strings caught by existing required-field validation. |
| T-111-04  | Repudiation            | Trimmed-vs-original input not logged                                                      | accept      | Trim is normalization, not censorship — no security-sensitive value lost. Logged value (Pino structured log) reflects what was stored.                         |

</threat_model>

<verification>
- All 3 tasks pass their automated `<verify>` commands
- `pnpm typecheck` (or equivalent) in el-templo-api passes
- `pnpm typecheck` in el-templo-admin passes
- `pnpm lint --filter=el-templo-api -- --no-fix` passes (no new lint warnings)
</verification>

<success_criteria>

- `normalizePhone` exists at backend (`shared/phone.ts`) and admin frontend (`utils/phone.ts`)
- Backend unit tests for `normalizePhone` cover 7 cases — all pass
- `members/service.ts` createMember + updateMember apply `.trim()` to firstName + lastName
- Members test suite passes with at least 2 new trim-coverage tests
- No regressions in existing tests
  </success_criteria>

<output>
After completion, create `.planning/phases/111-salvaguardas-operativas/111-01-SUMMARY.md` with:
- normalizePhone signature + file paths (backend + frontend)
- Trim insertion points (line numbers in members/service.ts)
- Test counts (new + total)
- Any deviations from the plan
</output>
