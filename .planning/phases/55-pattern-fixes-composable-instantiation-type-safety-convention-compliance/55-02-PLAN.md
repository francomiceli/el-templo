---
phase: 55-pattern-fixes-composable-instantiation-type-safety-convention-compliance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-app/src/utils/extract-error.ts
  - el-templo-app/src/stores/useAuthStore.ts
  - el-templo-app/src/stores/useUserStore.ts
  - el-templo-app/src/modules/training/stores/weekStore.ts
  - el-templo-app/src/modules/training/composables/useWeekData.ts
  - el-templo-app/src/modules/training/composables/useWakeLock.ts
  - el-templo-app/src/modules/progression/composables/useProgressionApi.ts
  - el-templo-app/src/pages/LoginPage.vue
  - el-templo-app/src/pages/RegisterPage.vue
  - el-templo-app/src/pages/ReservasPage.vue
  - el-templo-app/src/pages/CheckInPage.vue
autonomous: true
requirements: []

must_haves:
  truths:
    - "No unsafe `as` type assertions for Axios error handling exist in the app codebase"
    - "Error extraction uses proper type narrowing via axios.isAxiosError()"
    - "useWakeLock exposes cleanup() method instead of using onUnmounted internally"
    - "App builds without type errors"
  artifacts:
    - path: "el-templo-app/src/utils/extract-error.ts"
      provides: "extractError utility with axios.isAxiosError type narrowing"
      exports: ["extractError"]
    - path: "el-templo-app/src/modules/training/composables/useWakeLock.ts"
      provides: "Convention-compliant wake lock composable with cleanup()"
      exports: ["useWakeLock"]
  key_links:
    - from: "el-templo-app/src/stores/useAuthStore.ts"
      to: "el-templo-app/src/utils/extract-error.ts"
      via: "import { extractError }"
      pattern: "extractError"
    - from: "el-templo-app/src/modules/training/composables/useWakeLock.ts"
      to: "consumer component"
      via: "cleanup() method in return object"
      pattern: "cleanup"
---

<objective>
Replace 12 unsafe Axios `as` type casts in the member app with proper type narrowing, and fix useWakeLock composable convention violation.

Purpose: The `err as { response?: ... }` pattern bypasses TypeScript's type system. Using `axios.isAxiosError()` provides runtime type narrowing that is both type-safe and semantically correct. The useWakeLock composable violates the project convention of no onUnmounted inside composables (see CLAUDE.md: "Frontend composables: Expose cleanup() method, no onUnmounted inside composables").

Output: extractError utility created for app, 12 unsafe casts replaced, useWakeLock refactored to expose cleanup().
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Admin extractError pattern to replicate in app -->
From el-templo-admin/src/utils/extract-error.ts:
```typescript
import axios from 'axios';

export function extractError(err: unknown, fallback: string): string {
if (axios.isAxiosError(err)) {
const message = err.response?.data?.error ?? err.response?.data?.message;
if (typeof message === 'string') return message;
}
if (err instanceof Error) return err.message;
return fallback;
}

````
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create extractError utility and replace unsafe Axios casts in app</name>
  <files>
    el-templo-app/src/utils/extract-error.ts,
    el-templo-app/src/stores/useAuthStore.ts,
    el-templo-app/src/stores/useUserStore.ts,
    el-templo-app/src/modules/training/stores/weekStore.ts,
    el-templo-app/src/modules/training/composables/useWeekData.ts,
    el-templo-app/src/modules/progression/composables/useProgressionApi.ts,
    el-templo-app/src/pages/LoginPage.vue,
    el-templo-app/src/pages/RegisterPage.vue,
    el-templo-app/src/pages/ReservasPage.vue,
    el-templo-app/src/pages/CheckInPage.vue
  </files>
  <action>
1. Create `el-templo-app/src/utils/extract-error.ts` — same implementation as the admin version:
   ```typescript
   import axios from 'axios';

   /**
    * Extract a user-friendly error message from an Axios error or unknown error.
    * Returns the server-provided message if available, otherwise the fallback.
    */
   export function extractError(err: unknown, fallback: string): string {
     if (axios.isAxiosError(err)) {
       const message = err.response?.data?.error ?? err.response?.data?.message;
       if (typeof message === 'string') return message;
     }
     if (err instanceof Error) return err.message;
     return fallback;
   }
````

2. Replace ALL 12 `const axiosError = err as { response?: { data?: { error?: string } } }` patterns across these files:

   **useAuthStore.ts** (2 instances at lines 55, 88):
   - Replace the `as` cast + `axiosError.response?.data?.error` with `extractError(err, 'Error de autenticacion')`
   - Import extractError from 'src/utils/extract-error'

   **weekStore.ts** (1 instance at line 78):
   - Replace with `extractError(err, 'Error cargando sesiones')`

   **useWeekData.ts** (1 instance at line 70):
   - Replace with `extractError(err, 'Error cargando semana')`

   **useProgressionApi.ts** (2 instances at lines 33, 71):
   - Replace with `extractError(err, 'Error cargando progresion')`

   **LoginPage.vue** (1 instance at line 180):
   - Replace with `extractError(err, 'Error de autenticacion')`

   **RegisterPage.vue** (1 instance at line 184):
   - Replace with `extractError(err, 'Error en registro')`

   **ReservasPage.vue** (3 instances at lines 644, 681, 701):
   - Note: these use `.message` not `.error` in the response — extractError already checks both fields
   - Replace with `extractError(err, 'Error en reservas')`

   **CheckInPage.vue** (1 instance at line 131):
   - Replace with `extractError(err, 'Error en check-in')`

3. Also fix `useUserStore.ts` line 101: `response.data as MemberSubscription`
   - The Axios call at that line should already use generic typing `api.get<MemberSubscription>(...)` — if it does, the `as` cast is redundant and can be removed
   - If the Axios call doesn't have a generic type parameter, add one: `api.get<MemberSubscription>(...)`

For each replacement, preserve the existing error message logic. The pattern is:

- OLD: `const axiosError = err as {...}; errorMsg.value = axiosError.response?.data?.error || 'fallback'`
- NEW: `errorMsg.value = extractError(err, 'fallback')`

Keep the Spanish fallback messages. Adapt the exact fallback text to match what was there before.
</action>
<verify>
<automated>cd el-templo-app && npx vue-tsc --noEmit 2>&1 | tail -5</automated>
</verify>
<done>extractError utility created. All 12 unsafe `as` casts replaced with type-safe extractError calls. No `axiosError = err as` patterns remain in el-templo-app/src/.</done>
</task>

<task type="auto">
  <name>Task 2: Fix useWakeLock composable convention violation</name>
  <files>el-templo-app/src/modules/training/composables/useWakeLock.ts</files>
  <action>
Refactor useWakeLock to follow the project composable convention: expose cleanup() method, no onUnmounted inside composables (CLAUDE.md rule).

Changes:

1. Remove `onMounted` and `onUnmounted` imports from vue
2. Remove the `onMounted(async () => { ... })` block — move `initialize()` to be called by the consumer or make it lazy (call on first `requestWakeLock`)
3. Remove the `onUnmounted(() => { ... })` block
4. Add `initialize()` to the return object so consumers can call it explicitly
5. Add a `cleanup()` function that:
   - Calls `document.removeEventListener('visibilitychange', handleVisibilityChange)`
   - Calls `void releaseWakeLock()`
6. Add `cleanup` to the return object

The consumer (likely a page component using useWakeLock) is responsible for:

- Calling `initialize()` in onMounted
- Calling `cleanup()` in its own onUnmounted or via the component's cleanup chain

Also add `document.addEventListener('visibilitychange', handleVisibilityChange)` inside `initialize()` instead of in the removed onMounted hook, so the event listener is set up when initialize is called.

Final return shape:

```typescript
return {
  isActive,
  isSupported,
  requestWakeLock,
  releaseWakeLock,
  initialize,
  cleanup,
};
```

Then check the consumer of useWakeLock to ensure it calls initialize/cleanup. Search for `useWakeLock` usage in .vue files. If the consumer doesn't call initialize/cleanup, update it to do so using onMounted/onUnmounted at the PAGE level (which is where lifecycle hooks belong per convention).
</action>
<verify>
<automated>cd el-templo-app && npx vue-tsc --noEmit 2>&1 | tail -5</automated>
</verify>
<done>useWakeLock no longer uses onMounted/onUnmounted internally. Exposes initialize() and cleanup(). Consumer page handles lifecycle. App type-checks cleanly.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `cd el-templo-app && npx vue-tsc --noEmit` passes
2. `grep -rn "axiosError = err as" el-templo-app/src/` returns no results
3. `grep -rn "onUnmounted\|onMounted" el-templo-app/src/modules/training/composables/useWakeLock.ts` returns no results
4. `grep -rn "cleanup" el-templo-app/src/modules/training/composables/useWakeLock.ts` shows cleanup function in return
</verification>

<success_criteria>

- extractError utility exists in el-templo-app with same API as admin version
- Zero `as` casts for Axios error handling remain in app codebase
- useWakeLock follows composable convention (cleanup exposed, no lifecycle hooks)
- App type-checks and builds cleanly
  </success_criteria>

<output>
After completion, create `.planning/phases/55-pattern-fixes-composable-instantiation-type-safety-convention-compliance/55-02-SUMMARY.md`
</output>
