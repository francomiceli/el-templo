---
phase: 89-planes-online-infra
plan: 05
type: execute
wave: 3
depends_on: [89-02]
files_modified:
  - el-templo-app/src/modules/goal-plan/types.ts
  - el-templo-app/src/modules/goal-plan/index.ts
  - el-templo-app/src/modules/goal-plan/routes.ts
  - el-templo-app/src/modules/goal-plan/stores/goalPlanStore.ts
  - el-templo-app/src/modules/goal-plan/composables/useGoalPlanApi.ts
  - el-templo-app/src/modules/goal-plan/composables/useGoalPlanSession.ts
  - el-templo-app/src/modules/goal-plan/components/GoalPlanProgressBar.vue
  - el-templo-app/src/modules/goal-plan/components/GoalPlanProgressIndicator.vue
  - el-templo-app/src/modules/goal-plan/pages/GoalPlanSession.vue
  - el-templo-app/src/modules/plan/pages/PlanesPage.vue
  - el-templo-app/src/modules/progression/components/GoalPlanSection.vue
  - el-templo-app/src/modules/progression/composables/useGoalPlanProgress.ts
  - el-templo-app/src/modules/progression/pages/MiTemplo.vue
  - el-templo-app/src/modules/progression/components/SessionCtaCard.vue
  - el-templo-app/src/modules/training/pages/WeeklyView.vue
  - el-templo-app/src/stores/useUserStore.ts
  - el-templo-app/src/boot/modules.ts
autonomous: true
requirements: [MON-02, MON-04, MON-05]

must_haves:
  truths:
    - "personalizada/ module directory is gone, replaced by goal-plan/"
    - "Member app uses goalPlan naming in all code (types, stores, composables, routes)"
    - "DurationPicker page is deleted (per D-29)"
    - "PlanesPage shows 'Planes Por Objetivos' section (not Personalizados)"
    - "WhatsApp CTA includes plan name and weekly price"
    - "Member PlanesPage badge says 'POR OBJETIVOS' (not PERSONALIZADO)"
    - "All API paths use /goal-plans/ (not /personalizadas/)"
  artifacts:
    - path: "el-templo-app/src/modules/goal-plan/types.ts"
      provides: "GoalPlan types replacing Personalizada types"
      contains: "GoalPlanType"
    - path: "el-templo-app/src/modules/goal-plan/composables/useGoalPlanApi.ts"
      provides: "API composable with /goal-plans/ paths"
      contains: "/goal-plans/"
    - path: "el-templo-app/src/modules/plan/pages/PlanesPage.vue"
      provides: "Member plans page with Por Objetivos section and WhatsApp CTA"
      contains: "Por Objetivos"
  key_links:
    - from: "el-templo-app/src/boot/modules.ts"
      to: "el-templo-app/src/modules/goal-plan/index.ts"
      via: "Module registration import"
      pattern: "goal-plan"
    - from: "el-templo-app/src/modules/goal-plan/composables/useGoalPlanApi.ts"
      to: "API /goal-plans/*"
      via: "HTTP requests to renamed API endpoints"
      pattern: "/goal-plans/"
    - from: "el-templo-app/src/modules/plan/pages/PlanesPage.vue"
      to: "WhatsApp API"
      via: "Pre-filled message with plan name and weekly price"
      pattern: "wa.me"
---

<objective>
Rename the member app personalizada module to goal-plan, update all UI text to "Por Objetivos", add weekly price to WhatsApp CTA, and remove DurationPicker.

Purpose: Per D-01/D-04/D-24/D-29, the member app must use the new naming consistently. User-facing text changes from "Personalizada" to "Por Objetivos". WhatsApp CTA gets weekly price for sales conversion.

Output: Renamed goal-plan module, updated PlanesPage with Por Objetivos branding and WhatsApp CTA with weekly price.

NOTE on deployment coordination: This is a local-only monorepo that deploys all 3 apps together atomically. The API route change from `/personalizadas/` to `/goal-plans/` (Plan 02) and the member app route update here deploy together in the same release. No backward compatibility or staged rollout needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/89-planes-online-infra/89-CONTEXT.md
@.planning/phases/89-planes-online-infra/89-UI-SPEC.md
@.planning/phases/89-planes-online-infra/89-02-SUMMARY.md

<interfaces>
<!-- Post Plan 02, API returns: -->

API response for GET /plans now includes:

- planCategory: 'presencial' | 'online_regular' | 'online_goal' | 'online_coach'
- linkedProgramId: number | null
- NO isPersonalizada, isOnline, personalizadaType
- NOTE: NO goalPlanType on plans — per D-07 REVISED, goalPlanType lives on micro_programs only

API goal-plan endpoints (was /personalizadas/\*):

- GET /goal-plans/metadata
- GET /goal-plans/active
- GET /goal-plans/session
- POST /goal-plans/complete
- GET /goal-plans/stats
- GET /goal-plans/archived

From el-templo-app/src/stores/useUserStore.ts:

```typescript
interface MemberSubscription {
  // ... isPersonalizada: boolean (TO BE REMOVED)
  // hasActivePersonalizada already uses program enrollment check (Phase 83)
}
```

From el-templo-app/src/modules/personalizada/types.ts:

- PersonalizadaType, PersonalizadaSession, etc. -> GoalPlanType, GoalPlanSession
- PersonalizadaDuration -> DELETE (D-29)

From el-templo-app/src/boot/modules.ts:

- Imports personalizada module -> must change to goal-plan
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Rename personalizada/ module to goal-plan/ with all files</name>
  <files>
    el-templo-app/src/modules/goal-plan/types.ts,
    el-templo-app/src/modules/goal-plan/index.ts,
    el-templo-app/src/modules/goal-plan/routes.ts,
    el-templo-app/src/modules/goal-plan/stores/goalPlanStore.ts,
    el-templo-app/src/modules/goal-plan/composables/useGoalPlanApi.ts,
    el-templo-app/src/modules/goal-plan/composables/useGoalPlanSession.ts,
    el-templo-app/src/modules/goal-plan/components/GoalPlanProgressBar.vue,
    el-templo-app/src/modules/goal-plan/components/GoalPlanProgressIndicator.vue,
    el-templo-app/src/modules/goal-plan/pages/GoalPlanSession.vue,
    el-templo-app/src/modules/progression/components/GoalPlanSection.vue,
    el-templo-app/src/modules/progression/composables/useGoalPlanProgress.ts,
    el-templo-app/src/modules/progression/pages/MiTemplo.vue,
    el-templo-app/src/modules/progression/components/SessionCtaCard.vue,
    el-templo-app/src/modules/training/pages/WeeklyView.vue,
    el-templo-app/src/stores/useUserStore.ts,
    el-templo-app/src/boot/modules.ts
  </files>
  <read_first>
    - el-templo-app/src/modules/personalizada/types.ts
    - el-templo-app/src/modules/personalizada/index.ts
    - el-templo-app/src/modules/personalizada/routes.ts
    - el-templo-app/src/modules/personalizada/stores/personalizadaStore.ts
    - el-templo-app/src/modules/personalizada/composables/usePersonalizadaApi.ts
    - el-templo-app/src/modules/personalizada/composables/usePersonalizadaSession.ts
    - el-templo-app/src/modules/personalizada/components/PersonalizadaProgressBar.vue
    - el-templo-app/src/modules/personalizada/components/PersonalizadaProgressIndicator.vue
    - el-templo-app/src/modules/personalizada/pages/PersonalizadaSession.vue
    - el-templo-app/src/modules/personalizada/pages/DurationPicker.vue (being DELETED per D-29)
    - el-templo-app/src/modules/progression/components/PersonalizadaSection.vue
    - el-templo-app/src/modules/progression/composables/usePersonalizadaProgress.ts
    - el-templo-app/src/modules/progression/pages/MiTemplo.vue
    - el-templo-app/src/modules/progression/components/SessionCtaCard.vue
    - el-templo-app/src/modules/training/pages/WeeklyView.vue
    - el-templo-app/src/stores/useUserStore.ts
    - el-templo-app/src/boot/modules.ts
  </read_first>
  <action>
**Step 1: Use git mv to preserve file history, then create new directory structure**

```bash
git mv el-templo-app/src/modules/personalizada el-templo-app/src/modules/goal-plan
```

Then rename files inside goal-plan/ as needed (e.g., personalizadaStore.ts -> goalPlanStore.ts, etc.).

DELETE `DurationPicker.vue` — this page is deleted per D-29. Duration picker concept is eliminated entirely.

Also rename the progression module files:

```bash
git mv el-templo-app/src/modules/progression/components/PersonalizadaSection.vue el-templo-app/src/modules/progression/components/GoalPlanSection.vue
git mv el-templo-app/src/modules/progression/composables/usePersonalizadaProgress.ts el-templo-app/src/modules/progression/composables/useGoalPlanProgress.ts
```

**Step 2: Rewrite goal-plan/ module files**

**types.ts** — Rename all types:

- `PersonalizadaType` -> `GoalPlanType`
- `PersonalizadaSession` -> `GoalPlanSession`
- `PersonalizadaStats` -> `GoalPlanStats`
- `PersonalizadaDuration` -> DELETE entirely
- All field names: `personalizadaType` -> `goalPlanType`
- Remove any duration-related types/fields

**index.ts** — Update exports to reference new names.

**routes.ts** — Update route definitions:

- Route paths stay user-friendly (e.g., `/mi-camino/goal-plan` or whatever the current paths are — read first to determine)
- Route names: personalizada -> goalPlan
- Component imports: `./pages/GoalPlanSession.vue`
- REMOVE any route for DurationPicker

**stores/goalPlanStore.ts** (was personalizadaStore.ts):

- Store name: `useGoalPlanStore` (was `usePersonalizadaStore`)
- All state/getter/action names with personalizada -> goalPlan
- Remove duration-related state (selectedDuration, etc.)
- API calls use `/goal-plans/` paths

**composables/useGoalPlanApi.ts** (was usePersonalizadaApi.ts):

- Function name: `useGoalPlanApi`
- ALL API paths: `/personalizadas/` -> `/goal-plans/`
- Method names: personalizada -> goalPlan
- Remove duration parameter from API calls

**composables/useGoalPlanSession.ts** (was usePersonalizadaSession.ts):

- Function name: `useGoalPlanSession`
- All internal references renamed
- Remove duration filtering/selection logic

**Components** (GoalPlanProgressBar, GoalPlanProgressIndicator, GoalPlanSession):

- Rewrite with same logic, all personalizada identifiers -> goalPlan
- Remove any duration display or selection
- Update any user-facing text from "personalizada" to "por objetivos"

**Step 3: Update progression module files**

`GoalPlanSection.vue` (renamed from PersonalizadaSection.vue):

- All component name/props/emits with personalizada -> goalPlan
- User-facing text: any "personalizada" -> "por objetivos"

`useGoalPlanProgress.ts` (renamed from usePersonalizadaProgress.ts):

- Function name: `useGoalPlanProgress`

`MiTemplo.vue`:

- Update imports: GoalPlanSection, useGoalPlanProgress
- All `personalizada` variable names -> `goalPlan`
- Prop passing: `isPersonalizada` prop -> derive from goalPlan context or remove if no longer needed
- `hasActivePersonalizada` computed is already program-enrollment-based (Phase 83). Rename to `hasActiveGoalPlan` if the computed exists by that name.

`SessionCtaCard.vue`:

- Rename `isPersonalizada` prop to `isGoalPlan` (or equivalent based on current usage)
- Update template conditionals and class bindings

**Step 4: Update training module**

`WeeklyView.vue`:

- Rename any `personalizada` references to `goalPlan`

**Step 5: Update useUserStore.ts**

- Remove `isPersonalizada: boolean` from `MemberSubscription` interface (per D-09)
- Add `planCategory: string` to `MemberSubscription` if API returns it
- Rename `hasActivePersonalizada` computed to `hasActiveGoalPlan` if it exists as a ref/computed name (but per Phase 83 D-08, this already uses program enrollment check — just rename the computed name)

**Step 6: Update boot/modules.ts**

- Update import path from `../modules/personalizada` to `../modules/goal-plan`
- Update any module registration names
  </action>
  <verify>
  <automated>cd /home/franco/projects/el-templo/el-templo-app && ls src/modules/goal-plan/ && ! ls src/modules/personalizada/ 2>/dev/null && grep -r "personalizada\|personalizadas\|PersonalizadaType\|isPersonalizada\|DurationPicker" src/modules/ src/stores/ src/boot/ | grep -E "\.(ts|vue):" | grep -v "node_modules" | grep -v ".d.ts" && echo "FOUND OLD REFS" || echo "Clean"</automated>
  </verify>
  <acceptance_criteria> - Directory el-templo-app/src/modules/personalizada/ does NOT exist - Directory el-templo-app/src/modules/goal-plan/ exists with files: types.ts, index.ts, routes.ts, stores/goalPlanStore.ts, composables/useGoalPlanApi.ts, composables/useGoalPlanSession.ts, components/GoalPlanProgressBar.vue, components/GoalPlanProgressIndicator.vue, pages/GoalPlanSession.vue - NO DurationPicker.vue file anywhere in goal-plan/ module - goal-plan/composables/useGoalPlanApi.ts contains `/goal-plans/` API paths (not /personalizadas/) - goal-plan/types.ts exports `GoalPlanType` (not PersonalizadaType) and does NOT export PersonalizadaDuration - PersonalizadaSection.vue does NOT exist, GoalPlanSection.vue exists - usePersonalizadaProgress.ts does NOT exist, useGoalPlanProgress.ts exists - useUserStore.ts does NOT contain `isPersonalizada` in MemberSubscription interface - boot/modules.ts imports from `../modules/goal-plan` (not personalizada) - grep for `personalizada|personalizadas|PersonalizadaType|isPersonalizada|DurationPicker` across src/modules/, src/stores/, src/boot/ returns NO matches
  </acceptance_criteria>
  <done>Entire member app personalizada module renamed to goal-plan using git mv for history preservation. All files, types, stores, composables, components, and routes updated. DurationPicker deleted. No old naming references remain.</done>
  </task>

<task type="auto">
  <name>Task 2: Update PlanesPage — Por Objetivos branding and WhatsApp CTA with weekly price</name>
  <files>el-templo-app/src/modules/plan/pages/PlanesPage.vue</files>
  <read_first>
    - el-templo-app/src/modules/plan/pages/PlanesPage.vue (current plan display with "Personalizados" section)
    - .planning/phases/89-planes-online-infra/89-UI-SPEC.md (interaction contract 6, copywriting contract)
  </read_first>
  <action>
**PlanesPage.vue changes (per D-04, D-23, D-24, UI-SPEC contract 6):**

1. Section title rename:
   - Find the section heading that says "Planes Personalizados" and change to "Planes Por Objetivos"

2. Badge label rename:
   - Find the QBadge that shows "PERSONALIZADO" and change to "POR OBJETIVOS"
   - Keep `amber-8` color (per UI-SPEC)

3. MemberPlan interface update:
   - Replace `isPersonalizada: boolean` with `planCategory: string`
   - Replace `isOnline: boolean` with nothing (derived from planCategory)
   - Remove `personalizadaType: string | null` — per D-07 REVISED, goalPlanType is on programs, not plans. The plan response does not include goalPlanType.
   - Add `linkedProgramId: number | null`

4. Plan filtering:
   - Where plans are filtered by `isPersonalizada` -> filter by `planCategory === 'online_goal'` or `planCategory === 'online_coach'`
   - Where plans check `isOnline` -> check `planCategory !== 'presencial'`

5. Section description (per UI-SPEC copywriting):
   - "Planes diseñados a tu medida para potenciar tu entrenamiento."

6. WhatsApp CTA update (per D-24, UI-SPEC):
   - Add weekly price computation: `Math.round(plan.priceRegular / 4.33)` (priceRegular is ALWAYS the monthly price regardless of plan durationDays — confirmed by product strategy showing "$X ARS/month" pricing)
   - Update pre-filled message template from old format to:
     ```
     Hola! Me interesa el plan ${plan.name} ($${weeklyPrice.toLocaleString()}/semana). Quiero mas info.
     ```
   - Apply to BOTH regular plan CTAs and "por objetivos" plan CTAs
   - WhatsApp URL format: `https://wa.me/5492235820521?text=${encodeURIComponent(message)}`
   - Read the current PlanesPage to find the exact WhatsApp phone number being used (it may be a different number or loaded from config)

7. Enrolled badge text: "Ya estas inscripto" (unchanged per UI-SPEC)

8. Empty state: "No hay planes disponibles" (unchanged per UI-SPEC)
   </action>
   <verify>
   <automated>cd /home/franco/projects/el-templo/el-templo-app && grep -c "Por Objetivos" src/modules/plan/pages/PlanesPage.vue && grep -c "semana" src/modules/plan/pages/PlanesPage.vue && grep "planCategory" src/modules/plan/pages/PlanesPage.vue | head -3</automated>
   </verify>
   <acceptance_criteria> - PlanesPage.vue contains "Planes Por Objetivos" section heading (not "Personalizados") - PlanesPage.vue contains "POR OBJETIVOS" badge label (not "PERSONALIZADO") - PlanesPage.vue contains WhatsApp message with `/semana` (weekly price in CTA) - PlanesPage.vue contains `planCategory` in MemberPlan interface (not isPersonalizada/isOnline) - PlanesPage.vue does NOT contain `isPersonalizada` or `personalizadaType` or `goalPlanType` - PlanesPage.vue contains `Math.round` with `4.33` for weekly price calculation - PlanesPage.vue contains `wa.me` URL for WhatsApp CTA
   </acceptance_criteria>
   <done>Member app PlanesPage shows "Planes Por Objetivos" with "POR OBJETIVOS" badge, weekly price calculation, and updated WhatsApp CTA including plan name and weekly price. All old naming removed.</done>
   </task>

</tasks>

<verification>
- `cd el-templo-app && npx vue-tsc --noEmit 2>&1 | head -30` — typecheck passes (or only pre-existing errors)
- No file in member app src/ references personalizada/personalizadas/PersonalizadaType/DurationPicker
- goal-plan/ module exists with all files
- PlanesPage shows "Por Objetivos" and WhatsApp CTA has weekly price
</verification>

<success_criteria>
Member app fully renamed from personalizada to goal-plan with git mv for history preservation. User-facing text shows "Por Objetivos". DurationPicker eliminated. WhatsApp CTA includes plan name and weekly price for sales conversion. Zero references to old naming remain in member app.
</success_criteria>

<output>
After completion, create `.planning/phases/89-planes-online-infra/89-05-SUMMARY.md`
</output>
