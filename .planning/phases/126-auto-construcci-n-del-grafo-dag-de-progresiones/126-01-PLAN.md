---
phase: 126-auto-construcci-n-del-grafo-dag-de-progresiones
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/schema/exercise-progressions.ts
  - el-templo-api/src/db/schema/index.ts
  - el-templo-api/src/db/migrations/0139_create_exercise_progressions.sql
autonomous: true
requirements: [TREE-04]
must_haves:
  truths:
    - "The exercise_progressions table exists in the test MySQL DB with from/to FKs to exercises and a source enum (auto|manual)"
    - "The Drizzle schema for exercise_progressions is exported from the schema barrel and typechecks"
    - "Re-applying the migration is prevented by the _migrations row (no IF NOT EXISTS guards)"
  artifacts:
    - path: "el-templo-api/src/db/schema/exercise-progressions.ts"
      provides: "Drizzle table definition + source enum for graph edges"
      contains: "exerciseProgressions"
    - path: "el-templo-api/src/db/migrations/0139_create_exercise_progressions.sql"
      provides: "Hand-written additive CREATE TABLE migration"
      contains: "CREATE TABLE exercise_progressions"
  key_links:
    - from: "el-templo-api/src/db/schema/index.ts"
      to: "el-templo-api/src/db/schema/exercise-progressions.ts"
      via: "barrel re-export"
      pattern: "export-star-exercise-progressions"
    - from: "exercise_progressions FKs"
      to: "exercises.id"
      via: "FK constraint"
      pattern: "exercise_progressions_from_exercise_id_exercises_id_fk"
---

<objective>
Create the persistence layer for the progression graph: a new Drizzle table `exercise_progressions` (graph edges) plus its hand-written migration, and apply the migration to the test DB so downstream plans (constructor + tests) run against a real schema.

Purpose: D-03 mandates a regenerable edge table that distinguishes `auto` (SPOM-derived backbone) from `manual` (profe overrides in 128). This plan lays the table and its contract; Plans 02 and 03 build the engines on top.
Output: `exercise-progressions.ts` schema, barrel export, `0139_*.sql` migration committed together, migration applied to test MySQL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-auto-construcci-n-del-grafo-dag-de-progresiones/126-CONTEXT.md
@.planning/phases/126-auto-construcci-n-del-grafo-dag-de-progresiones/126-PATTERNS.md

<interfaces>
Extracted from el-templo-api/src/db/schema/exercises.ts (the FK target):
  exercises.id                  -- int PK autoincrement
  exercises.effort              -- varchar(10) NOT NULL  (EXC | ISO | CON)
  exercises.dificultadLineal    -- int("dificultad_lineal") NOT NULL DEFAULT 1
  exercises.route               -- varchar(20) NOT NULL
  exercises.subfamilyId         -- int("subfamily_id"), FK exercise_subfamilies.id, NULLABLE
  exercises.leverage            -- varchar(50), NULLABLE
  exercises.canonicalExerciseId -- int("canonical_exercise_id"), self-FK, NULLABLE (NULL = canonical row, D-01)

Migration conventions (PATTERNS.md): runner splits on the statement separator before
stripping comments, so no separator char inside comment lines; no IF NOT EXISTS guards;
FK names match Drizzle convention; apply with the project custom runner only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Drizzle schema + barrel export for exercise_progressions</name>
  <files>el-templo-api/src/db/schema/exercise-progressions.ts, el-templo-api/src/db/schema/index.ts</files>
  <read_first>
    - el-templo-api/src/db/schema/exercise-dimension-proposals.ts (exact analog: self-FK to exercises.id, inline mysqlEnum, named indexes, header doc-comment -- PATTERNS.md model section)
    - el-templo-api/src/db/schema/exercises.ts (FK target column names: id, effort, dificultad_lineal, subfamily_id, canonical_exercise_id)
    - el-templo-api/src/db/schema/index.ts (barrel: add export directly after the existing exercise-dimension-proposals export line)
  </read_first>
  <action>
    Create `exercise-progressions.ts` exporting an inline `mysqlEnum` named `exercise_progression_source` with values auto and manual, and a `mysqlTable("exercise_progressions", ...)` named export `exerciseProgressions` with columns: `id` int PK autoincrement; `fromExerciseId` = int("from_exercise_id") referencing exercises.id with onDelete cascade, notNull; `toExerciseId` = int("to_exercise_id") referencing exercises.id with onDelete cascade, notNull; `source` = the enum with default auto, notNull (D-03). Both FKs use the plain arrow form referencing exercises.id (NOT AnyMySqlColumn -- that is only for same-file self-reference per PATTERNS.md note). Add a header doc-comment "Module: exercise-progressions -- phase 126 (v5.1 Nuevo Sistema de Entrenamiento)" stating the cascade-delete rationale (an edge is meaningless without both endpoints). Index callback returns an array with: uniqueIndex named exercise_progressions_edge_uq on (fromExerciseId, toExerciseId) -- this UNIQUE backs the regenerate/dedupe in Plan 02 (D-03); plus index exercise_progressions_from_idx on fromExerciseId, index exercise_progressions_to_idx on toExerciseId, index exercise_progressions_source_idx on source. Then add an `export * from "./exercise-progressions";` line to index.ts immediately after the exercise-dimension-proposals export line. Do NOT run drizzle-kit generate (migration is hand-written in Task 2).
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File exercise-progressions.ts exists exporting exerciseProgressions and exerciseProgressionSource.
    - Source contains mysqlEnum with name exercise_progression_source plus values auto and manual.
    - Source contains uniqueIndex named exercise_progressions_edge_uq on fromExerciseId and toExerciseId.
    - Both FK columns reference exercises.id with onDelete cascade.
    - index.ts re-exports ./exercise-progressions.
    - pnpm tsc --noEmit passes with no error referencing exercise-progressions.
  </acceptance_criteria>
  <done>Schema file + barrel export compile cleanly; table defined with edge UNIQUE, three indexes, two cascading FKs, and source enum defaulting to auto.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 [BLOCKING]: Hand-written migration 0139 + apply to test DB</name>
  <files>el-templo-api/src/db/migrations/0139_create_exercise_progressions.sql</files>
  <read_first>
    - el-templo-api/src/db/migrations/0138_create_exercise_dimension_proposals.sql (exact analog: single additive CREATE TABLE, inline ENUM, named UNIQUE/INDEX, inline CONSTRAINT FK names, header comment style)
    - el-templo-api/src/db/run-migrations.ts (the custom runner -- confirm it splits on the statement separator before stripping comments, tracks applied files in the _migrations table by name)
    - CLAUDE.md Database Changes section (no drizzle-kit migrate; _migrations table is source of truth)
  </read_first>
  <action>
    Write 0139_create_exercise_progressions.sql as a single additive CREATE TABLE exercise_progressions mirroring the Task 1 schema: id INT NOT NULL AUTO_INCREMENT, from_exercise_id INT NOT NULL, to_exercise_id INT NOT NULL, source ENUM('auto','manual') NOT NULL DEFAULT 'auto'; PRIMARY KEY (id); UNIQUE KEY exercise_progressions_edge_uq on (from_exercise_id, to_exercise_id); INDEX exercise_progressions_from_idx on from_exercise_id; INDEX exercise_progressions_to_idx on to_exercise_id; INDEX exercise_progressions_source_idx on source; inline CONSTRAINT exercise_progressions_from_exercise_id_exercises_id_fk FOREIGN KEY (from_exercise_id) REFERENCES exercises(id) ON DELETE CASCADE, and the matching to_exercise_id constraint named exercise_progressions_to_exercise_id_exercises_id_fk. The statement ends with a single separator on its own non-comment line. CRITICAL SAFETY (PATTERNS.md + CLAUDE.md, all load-bearing): never put the statement separator char inside any comment line (the runner splits on it before stripping comments -- a separator inside a comment breaks the whole file); NO statement-level IF NOT EXISTS (replay is prevented by the _migrations row); FK constraint names MUST match Drizzle auto-gen convention exactly so a future db:generate converges. Confirm 0139 is the next free number before writing (0137=124, 0138=125 are the last two -- verify no 0139 exists). After writing, apply ONLY to the test DB via the project custom runner (db:migrate against the test DB env) so the Plan 02/03 integration tests are not false positives -- Drizzle types come from the TS schema regardless of live DB state, but the tests hit real MySQL so the table must actually exist. Commit this .sql file in the SAME commit as the Task 1 schema change (memory rule: executors miss migration SQL).
  </action>
  <verify>
    <automated>cd el-templo-api && node -e "const fs=require('fs');const s=fs.readFileSync('src/db/migrations/0139_create_exercise_progressions.sql','utf8');const bad=s.split('\n').some(l=>l.trim().startsWith('--')&&l.includes(';'));if(bad){console.error('FAIL: separator in comment');process.exit(1)};if(/if\s+not\s+exists/i.test(s)){console.error('FAIL: IF NOT EXISTS');process.exit(1)};if(!/exercise_progressions_from_exercise_id_exercises_id_fk/.test(s)||!/exercise_progressions_to_exercise_id_exercises_id_fk/.test(s)){console.error('FAIL: FK name');process.exit(1)};console.log('MIGRATION_SAFE')"</automated>
  </verify>
  <acceptance_criteria>
    - File 0139_create_exercise_progressions.sql exists and contains CREATE TABLE exercise_progressions.
    - No comment line contains the statement separator character.
    - No IF NOT EXISTS guard present.
    - Both FK constraint names follow the Drizzle convention exercise_progressions_(from|to)_exercise_id_exercises_id_fk.
    - Running the project custom migration runner against the test DB creates the table (a row with this migration name appears in _migrations).
    - The .sql file is committed alongside the Task 1 schema change.
  </acceptance_criteria>
  <done>Migration 0139 written safely (no separator in comments, no IF NOT EXISTS, Drizzle FK names), applied to the test DB via the custom runner, and committed with the schema file.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                          |
| -------------------------------- | ---------------------------------------------------- |
| migration runner -> test/prod DB | DDL applied by the custom runner; no untrusted input |
| schema FK integrity              | edges must reference real exercises rows             |

## STRIDE Threat Register

| Threat ID | Category          | Component                   | Disposition | Mitigation Plan                                                                                                 |
| --------- | ----------------- | --------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------- |
| T-126-01  | Tampering         | exercise_progressions edges | mitigate    | Both FKs ON DELETE CASCADE; orphan edges impossible because endpoints are required NOT NULL FKs to exercises.id |
| T-126-02  | Denial of Service | migration replay            | accept      | Replay prevented by the \_migrations row per project convention; additive CREATE TABLE only, no destructive DDL |
| T-126-SC  | Tampering         | npm/pip/cargo installs      | mitigate    | No new packages installed in this plan (drizzle-orm + mysql2 already present); slopcheck N/A                    |

</threat_model>

<verification>
- pnpm tsc --noEmit passes for el-templo-api.
- Migration runner applies 0139 to the test DB and records it in _migrations.
- The migration safety script reports MIGRATION_SAFE.
</verification>

<success_criteria>

- exercise_progressions table exists in the test DB with two cascading FKs, edge UNIQUE, and the source enum.
- Drizzle schema exported from the barrel, typechecks clean.
- Migration follows all project safety rules and is committed with the schema.
  </success_criteria>

<output>
Create `.planning/phases/126-auto-construcci-n-del-grafo-dag-de-progresiones/126-01-SUMMARY.md` when done
</output>
