---
phase: 126-auto-construcci-n-del-grafo-dag-de-progresiones
plan: 03
type: execute
wave: 2
depends_on: ["126-01"]
files_modified:
  - el-templo-api/src/modules/sessions/progressions/exercise-progression-service.ts
  - el-templo-api/test/exercises/exercise-progression-service.test.ts
autonomous: true
requirements: [TREE-04]
must_haves:
  truths:
    - "getNeighbor(exerciseId, 'up') returns the next-harder exercise of the SAME effort within the same subfamily, ordered by dl (D-04)"
    - "getNeighbor(exerciseId, 'down') returns the next-easier exercise of the SAME effort within the same subfamily"
    - "At the end of a chain getNeighbor returns null and never crosses effort automatically (D-05)"
    - "dl ties are broken deterministically by id so the neighbor is stable (D-05)"
    - "Exercises with NULL subfamily_id resolve no neighbor (excluded from the graph)"
  artifacts:
    - path: "el-templo-api/src/modules/sessions/progressions/exercise-progression-service.ts"
      provides: "ExerciseProgressionService.getNeighbor(exerciseId, direction) runtime adjacency primitive for Phase 131"
      contains: "getNeighbor"
    - path: "el-templo-api/test/exercises/exercise-progression-service.test.ts"
      provides: "Integration test for up/down neighbor, chain-end null, effort-fixed, tiebreak"
      contains: "getNeighbor"
  key_links:
    - from: "ExerciseProgressionService.getNeighbor"
      to: "exercises (subfamily x effort x dl adjacency)"
      via: "Drizzle select filtered by subfamilyId + effort, dl bound, stable id tiebreak"
      pattern: "getNeighbor"
---

<objective>
Expose the runtime primitive `ExerciseProgressionService.getNeighbor(exerciseId, direction)` that resolves the dl-adjacent exercise of the SAME effort within the SAME subfamily -- the "one step easier/harder" lookup Phase 131 consumes. It fixes the contraction (D-04) and returns null at chain ends without crossing effort (D-05).

Purpose: D-04/D-05/D-06. The in-session adjust buttons (131) need a deterministic neighbor that preserves the block's prescribed contraction. This extends the exercise-fallback.ts query idiom rather than reimplementing.
Output: a DI-by-constructor module service in sessions/progressions + its integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/126-auto-construcci-n-del-grafo-dag-de-progresiones/126-CONTEXT.md
@.planning/phases/126-auto-construcci-n-del-grafo-dag-de-progresiones/126-PATTERNS.md
@.planning/phases/126-auto-construcci-n-del-grafo-dag-de-progresiones/126-01-SUMMARY.md
@el-templo-api/src/modules/sessions/fallback/exercise-fallback.ts

<interfaces>
ExerciseCandidate (existing, exercise-fallback.ts lines 34-42 -- reuse as the return shape):
  { id: number; name: string; dificultadLineal: number; contraction: Contraction; position: string | null }

exercises columns this primitive reads:
id, exercise (name), subfamily_id, effort (EXC|ISO|CON), dificultad_lineal

ProgramsService DI shape to copy (src/modules/programs/service.ts lines 33-39):
type DbInstance = MySql2Database<typeof schema>;
class X { constructor(private readonly db: DbInstance, private readonly log?: FastifyBaseLogger) {} }

Primitive contract (consumed by Phase 131):
getNeighbor(exerciseId: number, direction: "up" | "down"): Promise<ExerciseCandidate | null>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ExerciseProgressionService.getNeighbor primitive</name>
  <files>el-templo-api/src/modules/sessions/progressions/exercise-progression-service.ts</files>
  <read_first>
    - el-templo-api/src/modules/sessions/fallback/exercise-fallback.ts (D-06 mandate REUSE/extend: ExerciseCandidate interface lines 34-42, queryExercises Drizzle idiom lines 87-112 with effort + dificultadLineal filter, selectClosest stable-sort lines 388-400, explicit terminal null lines 815-822)
    - el-templo-api/src/modules/programs/service.ts (DI-by-constructor class shape: DbInstance type alias + constructor lines 33-39)
    - el-templo-api/src/db/schema/exercises.ts (column names: subfamily_id, effort, dificultad_lineal, exercise)
    - .planning/phases/126-auto-construcci-n-del-grafo-dag-de-progresiones/126-PATTERNS.md (neighbor section: query idiom + deterministic tiebreak + no-neighbor fallback)
  </read_first>
  <behavior>
    - up: for the target exercise (subfamily S, effort E, dl D), return the exercise in (S, E) with the smallest dl strictly greater than D (next harder). If multiple share that dl, pick the smallest id (D-05).
    - down: return the exercise in (S, E) with the largest dl strictly less than D (next easier); tie -> smallest id.
    - effort fixed: the candidate set is filtered to the SAME effort as the target -- never substitute contraction (D-04). An EXC target never returns a CON neighbor.
    - chain end: if no candidate exists in the requested direction, return null (D-05). Do NOT widen, do NOT cross effort.
    - excluded target: if the target exercise has NULL subfamily_id, return null (not in the graph).
    - missing target: if exerciseId does not exist, return null.
  </behavior>
  <action>
    Create the file under src/modules/sessions/progressions/. Reuse the ExerciseCandidate return type from exercise-fallback.ts (import it; do not redefine). Define type DbInstance = MySql2Database<typeof schema> and an exported class ExerciseProgressionService with constructor(private readonly db: DbInstance, private readonly log?: FastifyBaseLogger), copying the ProgramsService shape. Implement async getNeighbor(exerciseId: number, direction: "up" | "down"): Promise<ExerciseCandidate | null>. Step 1: load the target row (id, subfamilyId, effort, dificultadLineal). If not found OR subfamilyId is NULL, return null. Step 2: query candidates from exercises with and(eq(subfamilyId, target.subfamilyId), eq(effort, target.effort)) and the directional dl bound -- direction up uses gt(dificultadLineal, target.dl) ordered ascending; direction down uses lt(dificultadLineal, target.dl) ordered descending (reuse the exercise-fallback.ts queryExercises Drizzle idiom; same effort + dl-bound filter). Step 3: from the candidate set pick the closest by dl, breaking ties deterministically by id (copy the selectClosest stable-sort idiom: sort by dl distance then by id ascending). If the candidate set is empty, return null explicitly (mirror the fallback's terminal return -- do NOT cross effort, D-05). Map the chosen row into an ExerciseCandidate (id, name from exercise, dificultadLineal, contraction from effort, position null/derived as the fallback does). No any types (CLAUDE.md). Use the injected log for any diagnostics, never console.log (this lives in the server module, not a CLI tool).
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File exercise-progression-service.ts exports class ExerciseProgressionService with constructor(db, log?).
    - getNeighbor signature is (exerciseId: number, direction: "up" | "down") => Promise<ExerciseCandidate | null>.
    - Candidate query filters by the target's subfamilyId AND effort (effort fixed, D-04).
    - up uses gt + ascending; down uses lt + descending; tie broken by id (D-05).
    - Returns null when target missing, target subfamily_id NULL, or no candidate in direction (D-05) -- no effort crossing.
    - ExerciseCandidate imported from exercise-fallback.ts, not redefined; no any type; pnpm tsc --noEmit passes.
  </acceptance_criteria>
  <done>getNeighbor compiles, fixes effort, resolves dl-adjacent same-subfamily neighbor with stable id tiebreak, and returns null at chain ends without crossing effort.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test for getNeighbor</name>
  <files>el-templo-api/test/exercises/exercise-progression-service.test.ts</files>
  <read_first>
    - el-templo-api/test/exercises/bootstrap-dimensions.test.ts (harness analog: createTestApp, MARK + seededIds, seedExercise via $returningId, afterEach FK-ordered cleanup, real clock -- lines 25-63)
    - el-templo-api/src/modules/sessions/progressions/exercise-progression-service.ts (service under test, from Task 1)
    - el-templo-api/test/helpers.ts (createTestApp)
  </read_first>
  <behavior>
    - Test A (up): seed (subfamily S, effort EXC) at dl 1,3,5; getNeighbor(mid, 'up') returns the dl-5 exercise.
    - Test B (down): getNeighbor(mid, 'down') returns the dl-1 exercise.
    - Test C (chain end up): getNeighbor(top, 'up') returns null.
    - Test D (chain end down): getNeighbor(bottom, 'down') returns null.
    - Test E (effort fixed): seed a CON exercise at dl 4 in the SAME subfamily; getNeighbor(EXC-dl-3, 'up') still returns the EXC dl-5 (never the CON), confirming effort is not crossed (D-04).
    - Test F (tiebreak): seed two EXC exercises at dl 5 in S; getNeighbor(dl-3, 'up') deterministically returns the smaller-id one (D-05).
    - Test G (excluded): getNeighbor on an exercise with NULL subfamily_id returns null.
  </behavior>
  <action>
    Copy the harness from bootstrap-dimensions.test.ts (createTestApp beforeAll, unique MARK, seededIds, seedExercise via .$returningId() setting subfamilyId/effort/dificultadLineal/route/name, afterEach FK-ordered cleanup of MARK-tagged rows, afterAll close, real clock no fake timers). Instantiate new ExerciseProgressionService(app.db). Write tests A-G from the behavior block, asserting on the returned ExerciseCandidate.id (or null). For the effort-fixed and excluded cases, seed the extra CON / NULL-subfamily rows explicitly. Do NOT run the suite locally (CLAUDE.md + memory: local gate is tsc only; ask Franco before pushing so CI runs it).
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Test file imports ExerciseProgressionService and createTestApp.
    - Seven tests present covering up, down, both chain ends, effort-fixed, tiebreak, NULL-subfamily exclusion.
    - afterEach cleans only MARK-tagged seeded exercises in FK order; no fake timers.
    - pnpm tsc --noEmit passes (suite runs in CI, not locally).
  </acceptance_criteria>
  <done>Integration test compiles and encodes all seven D-04/D-05 contracts for the neighbor primitive; ready for CI.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                                                   |
| -------------------------------- | --------------------------------------------------------------------------------------------- |
| caller (131) -> getNeighbor      | exerciseId + direction are internal call args, not raw user input; direction is a typed union |
| getNeighbor -> exercises catalog | read-only adjacency query over internal exercise data                                         |

## STRIDE Threat Register

| Threat ID | Category               | Component                     | Disposition | Mitigation Plan                                                                                                                                 |
| --------- | ---------------------- | ----------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| T-126-06  | Tampering              | direction argument            | mitigate    | direction is the typed union "up"                                                                                                               | "down"; no string is interpolated into SQL -- Drizzle parameterizes the dl bound and effort/subfamily eq filters |
| T-126-07  | Information disclosure | unconfirmed/missing exercises | mitigate    | NULL subfamily_id and missing target both resolve to null; no rows outside the graph are exposed                                                |
| T-126-08  | Elevation of privilege | effort crossing               | accept      | getNeighbor is a pure read primitive with no privilege; effort-fix is a correctness invariant (D-04) asserted by Test E, not a security control |
| T-126-SC  | Tampering              | npm/pip/cargo installs        | mitigate    | No new packages; reuses existing drizzle-orm + ExerciseCandidate; slopcheck N/A                                                                 |

</threat_model>

<verification>
- pnpm tsc --noEmit passes for el-templo-api.
- getNeighbor filters by subfamily + fixed effort, directional dl bound, stable id tiebreak, explicit null at chain ends.
- Integration test encodes contracts A-G (run in CI, not locally).
</verification>

<success_criteria>

- ExerciseProgressionService.getNeighbor resolves the dl-adjacent same-effort neighbor within the subfamily, deterministically, returning null at chain ends without crossing effort.
- Return type reuses ExerciseCandidate; no reimplementation of the fallback machinery.
- Tests cover up/down/both ends/effort-fixed/tiebreak/exclusion and compile.
  </success_criteria>

<output>
Create `.planning/phases/126-auto-construcci-n-del-grafo-dag-de-progresiones/126-03-SUMMARY.md` when done
</output>
