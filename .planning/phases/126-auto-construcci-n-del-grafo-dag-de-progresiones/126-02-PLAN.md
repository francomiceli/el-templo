---
phase: 126-auto-construcci-n-del-grafo-dag-de-progresiones
plan: 02
type: execute
wave: 2
depends_on: ["126-01"]
files_modified:
  - el-templo-api/rebuild-progression-graph.ts
  - el-templo-api/test/exercises/rebuild-progression-graph.test.ts
autonomous: true
requirements: [TREE-04]
must_haves:
  truths:
    - "Running the constructor builds the linear backbone: consecutive-by-dl auto edges within each (subfamily x effort) partition, with NO speculative cross-edges (D-02)"
    - "Re-running the constructor regenerates only source=auto edges and leaves source=manual edges untouched (D-03)"
    - "Effort is never crossed: an EXC and a CON exercise in the same subfamily never share an auto edge (D-04)"
    - "Exercises with NULL subfamily_id (unconfirmed) are excluded from the graph; each node is a canonical exercise row (canonical_exercise_id IS NULL) (D-01)"
    - "dl ties within a partition are broken deterministically by a stable tiebreak (id) so the backbone is stable across runs (D-05)"
  artifacts:
    - path: "el-templo-api/rebuild-progression-graph.ts"
      provides: "Exported runRebuildProgressionGraph(db) batch constructor + CLI entry"
      contains: "runRebuildProgressionGraph"
    - path: "el-templo-api/test/exercises/rebuild-progression-graph.test.ts"
      provides: "Integration test for backbone, idempotency, manual-preservation, effort-not-crossed, exclusion"
      contains: "runRebuildProgressionGraph"
  key_links:
    - from: "rebuild-progression-graph.ts"
      to: "exercise_progressions (source=auto)"
      via: "DELETE auto then bulk INSERT inside a transaction"
      pattern: "source.*auto"
    - from: "rebuild-progression-graph.ts"
      to: "exercises (canonical, confirmed)"
      via: "SELECT subfamily_id NOT NULL AND canonical_exercise_id NULL"
      pattern: "subfamily_id"
---

<objective>
Build the deterministic graph constructor `runRebuildProgressionGraph(db)`: read confirmed canonical exercises, partition by (subfamily_id x effort), order by dificultadLineal with a stable id tiebreak, and write the consecutive linear backbone as source=auto edges into exercise_progressions -- regenerating only auto edges and preserving manual ones.

Purpose: D-02 + D-03. 126 auto-derives ONLY the linear chains (one per subfamily x effort); cross-edges are profe work in 128. Re-running must be safe and never clobber manual overrides.
Output: `rebuild-progression-graph.ts` (exported run function + CLI entry, mirroring bootstrap-dimensions.ts) and its integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/126-auto-construcci-n-del-grafo-dag-de-progresiones/126-CONTEXT.md
@.planning/phases/126-auto-construcci-n-del-grafo-dag-de-progresiones/126-PATTERNS.md
@.planning/phases/126-auto-construcci-n-del-grafo-dag-de-progresiones/126-01-SUMMARY.md

<interfaces>
exercise_progressions (created in Plan 01):
  id, from_exercise_id (FK exercises.id), to_exercise_id (FK exercises.id), source ENUM('auto','manual') DEFAULT 'auto'
  UNIQUE exercise_progressions_edge_uq (from_exercise_id, to_exercise_id)

exercises columns this constructor reads:
id, subfamily_id (NULL = unconfirmed -> exclude), effort (EXC|ISO|CON), dificultad_lineal, canonical_exercise_id (NULL = canonical row, D-01)

Constructor contract (downstream-stable):
export async function runRebuildProgressionGraph<TSchema extends Record<string, unknown>>(
db: MySql2Database<TSchema>
): Promise<void>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: runRebuildProgressionGraph constructor</name>
  <files>el-templo-api/rebuild-progression-graph.ts</files>
  <read_first>
    - el-templo-api/bootstrap-dimensions.ts (EXACT shape: exported run<TSchema>(db), read-report-before-mutate, db.execute(sql``) catalog read, narrow-rows-without-any helpers, CLI entry, console.log carve-out for standalone tools -- lines 1-40 header + 165-234 runBootstrap)
    - el-templo-api/src/modules/programs/service.ts (transaction idiom: this.db.transaction(async (tx) => {...}) around lines 50-51)
    - el-templo-api/src/db/schema/exercises.ts (column names)
    - el-templo-api/src/db/schema/exercise-progressions.ts (the edge table just created in Plan 01)
    - .planning/phases/126-auto-construcci-n-del-grafo-dag-de-progresiones/126-PATTERNS.md (constructor section: read-report -> transform -> scoped write)
  </read_first>
  <behavior>
    - Backbone: given a (subfamily x effort) partition with exercises at dl 1,3,5 (ids a,b,c), produce exactly 2 auto edges a->b and b->c, in dl order. A single-exercise partition produces 0 edges.
    - Effort not crossed: EXC and CON exercises in the same subfamily are partitioned separately; no edge connects them.
    - Exclusion: exercises with NULL subfamily_id are skipped entirely (not confirmed). Operate on canonical rows only (canonical_exercise_id IS NULL, D-01).
    - dl tiebreak: two exercises at the same dl in one partition are ordered deterministically by id (smaller id first), so the chain is stable across runs (D-05).
    - Idempotent: running twice produces the identical auto edge set, no duplicates (the edge UNIQUE backs this).
    - Manual-preserving: a pre-existing source=manual edge survives a rebuild untouched (D-03).
  </behavior>
  <action>
    Mirror bootstrap-dimensions.ts. Export `runRebuildProgressionGraph<TSchema>(db: MySql2Database<TSchema>): Promise<void>`. Step 1 READ (read-only report before mutate): SELECT id, subfamily_id AS subfamilyId, effort, dificultad_lineal AS dl FROM exercises WHERE subfamily_id IS NOT NULL AND canonical_exercise_id IS NULL (canonical + confirmed only, D-01/Discretion). Narrow the mysql2 result into typed rows with a helper that avoids any (CLAUDE.md TS rule -- copy the readCatalogRows narrowing idiom from bootstrap-dimensions.ts). Print to-process counts before mutating. Step 2 TRANSFORM (pure, in-memory, deterministic, NO inference -- D-02): group rows by the composite key subfamilyId + "|" + effort; within each group sort by dl ascending, tiebreak by id ascending (D-05); emit consecutive edges (from = element[i], to = element[i+1]) for each adjacent pair. Step 3 WRITE inside this.db.transaction: DELETE FROM exercise_progressions WHERE source = 'auto' (scope to auto ONLY -- never touch manual, D-03), then bulk INSERT the recomputed edges with source='auto'. The edge UNIQUE guarantees no duplicates. Add a header doc-comment explaining: derives ONLY the linear backbone per (subfamily x effort) (D-02), regenerates only auto edges (D-03), console.log is acceptable here because this is a standalone CLI maintenance tool (same carve-out as bootstrap-dimensions.ts -- NOT the API server). Add a CLI entry (dotenv config + createSingleConnection + call run + close) guarded so importing the module for the test does not run it.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - rebuild-progression-graph.ts exports runRebuildProgressionGraph with the generic-db signature.
    - The READ query filters subfamily_id IS NOT NULL AND canonical_exercise_id IS NULL.
    - The TRANSFORM groups by subfamilyId + effort and sorts by dl then id.
    - The WRITE is wrapped in db.transaction and the DELETE is scoped to source = 'auto'.
    - INSERT rows set source to 'auto'.
    - No use of the any type (CLAUDE.md rule); pnpm tsc --noEmit passes.
  </acceptance_criteria>
  <done>Constructor compiles, partitions canonical+confirmed exercises by (subfamily x effort), emits consecutive dl-ordered edges with stable id tiebreak, and atomically replaces only the auto backbone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test for the constructor</name>
  <files>el-templo-api/test/exercises/rebuild-progression-graph.test.ts</files>
  <read_first>
    - el-templo-api/test/exercises/bootstrap-dimensions.test.ts (EXACT analog: createTestApp, MARK + seededIds, seedExercise via $returningId, afterEach deletes edges THEN exercises in FK order, real clock no fake timers -- lines 25-63)
    - el-templo-api/test/helpers.ts (createTestApp signature)
    - el-templo-api/rebuild-progression-graph.ts (the function under test, from Task 1)
    - el-templo-api/src/db/schema/exercise-progressions.ts (to seed a manual edge and assert auto edges)
  </read_first>
  <behavior>
    - Test A (backbone): seed 3 exercises in one (subfamily, effort) at dl 1,3,5; run; assert exactly 2 auto edges in dl order (a->b, b->c).
    - Test B (idempotency): run twice; assert the auto edge set is identical and no duplicates.
    - Test C (manual preserved): insert a source=manual edge; run; assert the manual edge still exists untouched (D-03).
    - Test D (effort not crossed): seed an EXC and a CON exercise in the same subfamily; run; assert no edge connects across effort (D-04).
    - Test E (exclusion): seed an exercise with NULL subfamily_id; run; assert it appears in no edge.
    - Test F (tiebreak determinism): seed two exercises at the same dl in one partition; run twice; assert identical edge orientation across runs (D-05).
  </behavior>
  <action>
    Copy the harness from bootstrap-dimensions.test.ts: createTestApp in beforeAll, a unique MARK, a seededIds array, a seedExercise helper that inserts into schema.exercises with .$returningId() (set subfamilyId, effort, dificultadLineal, route, exercise name with the MARK; canonicalExerciseId left NULL so the row is canonical). Track inserted edge ids too. afterEach: delete this test's exercise_progressions rows (by from/to in seededIds) THEN the seeded exercises (FK order). afterAll closes the app. Use a real clock (NO fake timers -- they desync from MySQL timestamps, per the analog header). Import and call runRebuildProgressionGraph(app.db). Write the six tests A-F from the behavior block, asserting on the auto edge rows queried back from exercise_progressions. For Test C seed the manual edge via a direct insert into schema.exerciseProgressions with source 'manual'. Do NOT run the suite locally (CLAUDE.md + memory: local gate is tsc only; ask Franco before pushing so CI runs it).
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Test file imports runRebuildProgressionGraph and createTestApp.
    - Six tests present covering backbone, idempotency, manual-preservation, effort-not-crossed, exclusion, tiebreak determinism.
    - afterEach cleans edges before exercises (FK order) and only this test's MARK-tagged rows.
    - No fake timers used.
    - pnpm tsc --noEmit passes (suite itself runs in CI, not locally).
  </acceptance_criteria>
  <done>Integration test compiles and encodes all six D-02/D-03/D-04/D-05 contracts; ready to run in CI.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                     |
| ------------------------------------ | --------------------------------------------------------------- |
| catalog (exercises) -> constructor   | reads only confirmed canonical rows; no external/user input     |
| constructor -> exercise_progressions | writes only source=auto; manual edges out of scope of the write |

## STRIDE Threat Register

| Threat ID | Category               | Component                   | Disposition | Mitigation Plan                                                                                                                       |
| --------- | ---------------------- | --------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| T-126-03  | Tampering              | manual edges during rebuild | mitigate    | DELETE is scoped WHERE source='auto'; integration Test C asserts manual edges survive (D-03)                                          |
| T-126-04  | Tampering              | edge endpoints              | mitigate    | Constructor only emits edges between rows it read from exercises; FK CASCADE (Plan 01) prevents orphans                               |
| T-126-05  | Information disclosure | unconfirmed exercises       | accept      | NULL subfamily_id rows excluded by the READ filter; no untrusted input reaches the constructor (internal batch over internal catalog) |
| T-126-SC  | Tampering              | npm/pip/cargo installs      | mitigate    | No new packages (drizzle-orm + mysql2 already present); slopcheck N/A                                                                 |

</threat_model>

<verification>
- pnpm tsc --noEmit passes for el-templo-api.
- Constructor READ filters confirmed canonical exercises; WRITE scoped to source=auto inside a transaction.
- Integration test encodes contracts A-F (run in CI, not locally).
</verification>

<success_criteria>

- runRebuildProgressionGraph builds the deterministic linear backbone per (subfamily x effort), regenerable and manual-preserving.
- Effort is never crossed; unconfirmed exercises excluded; dl ties broken stably by id.
- Tests cover every contract and compile.
  </success_criteria>

<output>
Create `.planning/phases/126-auto-construcci-n-del-grafo-dag-de-progresiones/126-02-SUMMARY.md` when done
</output>
