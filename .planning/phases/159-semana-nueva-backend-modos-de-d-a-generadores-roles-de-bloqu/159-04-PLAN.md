---
phase: 159-semana-nueva-backend-modos-de-d-a-generadores-roles-de-bloqu
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/schema/session-week-regime.ts
  - el-templo-api/src/db/schema/index.ts
  - el-templo-api/src/db/tenant-tables.ts
  - el-templo-api/src/db/migrations/0202_session_week_regime.sql
  - el-templo-api/src/db/migrations/0203_backfill_regime_w12_w26.sql
  - el-templo-api/src/db/scripts/0203_regime_dryrun.sql
  - el-templo-api/test/migrations/0202-session-week-regime.test.ts
autonomous: true
requirements: [SEM-05]
must_haves:
  truths:
    - "D-18: ancla semana→fecha/régimen + retro-etiquetado de días combos/técnica desde W12 usando las firmas de detección, como metadata SIN tocar filas históricas"
    - "Existe la tabla session_week_regime con tenant_id NOT NULL DEFAULT 1 y unique (tenant_id, week, day) con tenant_id primero"
    - "session_week_regime tiene una fila por (week, day) etiquetado para W12–W26 con inferred_mode combos/tecnica"
    - "La tabla `sessions` NO cambia de conteo de filas antes/después del backfill (histórico intacto)"
    - "session_week_regime está clasificada en GYM_OWNED_TABLES (el test fail-closed de tenancy queda verde)"
  artifacts:
    - path: "el-templo-api/src/db/schema/session-week-regime.ts"
      provides: "schema Drizzle del ancla, gym-owned, con tenantIdColumn()"
      contains: "tenantIdColumn"
    - path: "el-templo-api/src/db/migrations/0202_session_week_regime.sql"
      provides: "DDL de la tabla del ancla, primera tabla gym-owned que nace con tenant_id"
      contains: "CREATE TABLE"
    - path: "el-templo-api/src/db/migrations/0203_backfill_regime_w12_w26.sql"
      provides: "retro-etiquetado W12–W26 @data-only idempotente, sin tocar sessions"
      contains: "INSERT"
  key_links:
    - from: "session-week-regime.ts"
      to: "tenant-tables.ts GYM_OWNED_TABLES"
      via: "alta obligatoria (test fail-closed)"
      pattern: "session_week_regime"
    - from: "0203 backfill"
      to: "filas literales W12–W26"
      via: "INSERT … SELECT … WHERE NOT EXISTS (idempotente), sin UPDATE sobre sessions"
      pattern: "WHERE NOT EXISTS"
---

<objective>
Crear el ANCLA histórica semana→régimen (SEM-05, corpus IA): una tabla nueva `session_week_regime` (gym-owned, con tenant_id) que persiste qué día de cada semana fue combos y cuál técnica, retro-etiquetada W12–W26 con las firmas de detección — como METADATA, SIN tocar ni una fila del histórico de `sessions`.

Purpose: el histórico de `sessions` dice `'regular'` en días que en realidad fueron combos/técnica (el coach lo hackeaba sobre `regular`). Ese régimen NO es derivable del `session_mode` histórico → hay que persistirlo. La mitad "fecha" ya es determinística (`WEEK_ONE_MONDAY` en `shared/week-dates.ts`); falta la mitad "régimen". Es la PRIMERA tabla gym-owned que nace con `tenant_id` en master → dispara el gate fail-closed de clasificación de tablas.

Output: schema + export + clasificación de tenancy + migración DDL 0202 + backfill 0203 (@data-only) + dry-run + test de migración. Independiente de los generadores (Wave 1, puede correr en paralelo con el plan 01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

REGLA OPERATIVA CRÍTICA: branchear desde `origin/master`, NO desde el checkout compartido (está en 0196, NO refleja master). RE-VERIFICAR la numeración de migraciones en `origin/master` ANTES de escribir SQL: `git ls-tree --name-only origin/master el-templo-api/src/db/migrations/` — el research verificó que master tope es 0201 (hueco en 0200, lo ocupa el tren v6.0) y ninguna rama llega a 0202. Si el tren v6.0 ya mergeó a master, RE-VERIFICAR y renumerar (0202+ podría estar tomado). `git add` por ruta explícita. Tests SOLO en foreground con timeout amplio (PROHIBIDO `run_in_background` y `pgrep`).

Skill `el-templo-db-migrations` (autoridad equivalente): NUNCA `drizzle-kit migrate` (la tabla `_migrations` es la única fuente de verdad). NUNCA un `;` dentro de un comentario `--` (el runner splitea por `;` antes de stripear comentarios — rompió CI en la 0119). El `.sql` va en el MISMO commit que el schema. Datos de prod SIEMPRE por migración, jamás por seed. Sentencias idempotentes (`WHERE NOT EXISTS`).
</execution_context>

<context>
@.planning/phases/159-semana-nueva-backend-modos-de-d-a-generadores-roles-de-bloqu/159-RESEARCH.md
@.planning/phases/159-semana-nueva-backend-modos-de-d-a-generadores-roles-de-bloqu/159-PATTERNS.md

<interfaces>
<!-- Moldes de origin/master. -->

Schema molde — db/schema/day-modes.ts (misma familia: config semana/día, gym-owned, unique por tenant):
mysqlTable("day_modes", { id, tenantId: tenantIdColumn(), dayOfWeek: int, sessionMode: varchar(10) },
(t) => [ uniqueIndex("uq_day_modes_tenant_day_of_week").on(t.tenantId, t.dayOfWeek) ])

Enums reusables — db/schema/weekly-rotator.ts:5-6:
dayEnum = mysqlEnum('day', ['lunes','martes','miercoles','jueves','viernes','sabado'])
(⚠ M6: el primer arg de mysqlEnum ES el nombre físico de la columna)

Contrato de tenancy — db/schema/tenant-column.ts (fuente única):
tenantIdColumn() = int("tenant_id").notNull().default(1).references(() => tenants.id)

Clasificación fail-closed — db/tenant-tables.ts:51-64:
GYM_OWNED_TABLES = [ ... "activities", ... ] // orden alfabético;
session_week_regime va entre "sessions" y "spom_rules". El test test/db/tenant-tables.test.ts
deja la suite en rojo si la tabla nueva no se clasifica.

Export del schema — db/schema/index.ts: patrón `export * from "./session-week-regime";`

DDL molde — 0189_tv_screen.sql (última CREATE TABLE de master; header hand-written + numeración + advertencia del `;`):
backticks, PRIMARY KEY al final, KEY/UNIQUE KEY/CONSTRAINT nombrados.
Composición 159: `tenant_id` int NOT NULL DEFAULT 1, CONSTRAINT fk_session_week_regime_tenant FK (tenant_id) REFERENCES tenants(id),
UNIQUE KEY uq_session_week_regime_tenant_week_day (tenant_id, week, day) // tenant_id PRIMERO.

Backfill molde — 0172\_...sql: `@data-only` en la PRIMERA línea; `INSERT … SELECT … WHERE NOT EXISTS (SELECT 1 …)` idempotente.
Dry-run molde — src/db/scripts/0183_backfill_lost_leads_dryrun.sql (SQL read-only).
Fechas — shared/week-dates.ts: WEEK_ONE_MONDAY=2026-02-23, dateToWeekNumber, dateToDayName (fuente única, no duplicar).

Test de migración molde — test/migrations/0196-tenant-unique-contracts.test.ts:
NO le pregunta a `_migrations`; interroga INFORMATION_SCHEMA (nombres de índice, orden de columnas, NON_UNIQUE) + datos reales.

Datos del retro-etiquetado (RESEARCH Hallazgo 8 + CONTEXT §evidencia_prod):
W12–W20 (firmas del discovery): días de combos detectados = W12jue, W13mié, W14jue, W15mié, W16jue, W17mié, W18jue, W19mié, W20mié; el día complementario de cada semana = técnica.
W21–W26 (SSH prod 2026-08-13, verbatim):
W21: mié=combos, jue=tecnica | W22: mié=tecnica, jue=combos | W23: mié=combos, jue=tecnica
W24: mié=tecnica, jue=combos | W25: mié=tecnica, jue=combos | W26: mié=tecnica, jue=combos
Falsos positivos a excluir por nombre de formato: Ladder / Ladder corta. NO cruzar format_name↔format_params (Pitfall 8).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: schema session-week-regime.ts + export + clasificación tenancy + DDL 0202</name>
  <read_first>
    - el-templo-api/src/db/schema/day-modes.ts (molde de schema gym-owned con unique por tenant)
    - el-templo-api/src/db/schema/tenant-column.ts (tenantIdColumn — contrato único)
    - el-templo-api/src/db/schema/weekly-rotator.ts:5-6 (dayEnum reusable)
    - el-templo-api/src/db/schema/index.ts (patrón de export)
    - el-templo-api/src/db/tenant-tables.ts:51-64 (GYM_OWNED_TABLES, orden alfabético)
    - el-templo-api/src/db/migrations/0189_tv_screen.sql (molde de header + DDL)
    - VERIFICAR primero: `git ls-tree --name-only origin/master el-templo-api/src/db/migrations/ | tail`
  </read_first>
  <action>
    Crear `db/schema/session-week-regime.ts` con `mysqlTable("session_week_regime", { id: autoincrement PK, tenantId: tenantIdColumn(), week: int NOT NULL, day: dayEnum (reusar el de weekly-rotator o declarar mysqlEnum('day', [...]) con el mismo listado), inferredMode: varchar('inferred_mode', {length:10}) NOT NULL, source: varchar('source', {length:20}) NOT NULL, confidence: (decimal o int para porcentaje) NULL, evidence: json NULL, createdAt: timestamp default CURRENT_TIMESTAMP }, (t) => [ uniqueIndex("uq_session_week_regime_tenant_week_day").on(t.tenantId, t.week, t.day) ])`. Comentar la columna tenantId con la referencia a tenant-column.ts (patrón de day-modes.ts). `inferred_mode` toma 'combos'|'tecnica'|'regular'|'rom'; `source` toma 'signature'|'generator'|'manual'.
    Agregar `export * from "./session-week-regime";` en `db/schema/index.ts` (patrón existente).
    Agregar `"session_week_regime"` a `GYM_OWNED_TABLES` en `db/tenant-tables.ts`, en orden alfabético (entre "sessions" y "spom_rules"). Es la razón por la que el test fail-closed de tablas queda verde.
    Crear la migración DDL hand-written `0202_session_week_regime.sql` (RE-VERIFICAR que 0202 sigue libre en origin/master antes). Header con las 4 declaraciones del molde 0189 (propósito; hand-written NUNCA drizzle-kit; numeración verificada en master Y staging = 0202 porque el hueco 0200 lo ocupa el tren v6.0 y ninguna rama llega a 0202; la advertencia de que un comentario NUNCA lleva `;`). DDL con backticks, `tenant_id` int NOT NULL DEFAULT 1, `CONSTRAINT fk_session_week_regime_tenant FOREIGN KEY (tenant_id) REFERENCES tenants(id)`, y `UNIQUE KEY uq_session_week_regime_tenant_week_day (tenant_id, week, day)` con tenant_id PRIMERO. PRIMARY KEY al final.
    NUNCA usar `drizzle-kit migrate`; el runner custom es la fuente de verdad.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit && pnpm vitest run test/db/tenant-tables.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Existe `db/schema/session-week-regime.ts` con `tenantIdColumn()` y la unique `(tenantId, week, day)`.
    - `db/schema/index.ts` exporta el módulo nuevo (grep).
    - `grep -c "session_week_regime" el-templo-api/src/db/tenant-tables.ts` >= 1 y está en GYM_OWNED_TABLES.
    - `0202_session_week_regime.sql` existe, empieza con el header hand-written, tiene `tenant_id ... DEFAULT 1`, la FK a tenants y la unique con tenant_id primero.
    - Ningún comentario `--` de la migración contiene `;` (grep `grep -n '\-\-.*;' 0202_session_week_regime.sql` vacío).
    - `pnpm exec tsc --noEmit` verde; `test/db/tenant-tables.test.ts` verde.
  </acceptance_criteria>
  <done>La tabla del ancla existe en schema + DDL, exportada, clasificada gym-owned; el gate de tablas queda verde; tsc verde.</done>
</task>

<task type="auto">
  <name>Task 2: backfill 0203 (@data-only) + dry-run + test de migración (histórico intacto)</name>
  <read_first>
    - el-templo-api/src/db/migrations/0172_formats_combos_stretching_ruta_fullbody.sql (@data-only + idempotencia)
    - el-templo-api/src/db/migrations/0183_backfill_lost_leads.sql (backfill retroactivo + dry-run separado)
    - el-templo-api/src/db/scripts/0183_backfill_lost_leads_dryrun.sql (molde de dry-run)
    - el-templo-api/test/migrations/0196-tenant-unique-contracts.test.ts (molde de test que interroga INFORMATION_SCHEMA + datos reales, NO _migrations)
    - .planning/phases/159-.../159-RESEARCH.md (Hallazgo 8: firmas + tabla W21–W26) y 159-CONTEXT.md (§evidencia_prod)
  </read_first>
  <action>
    Crear `0203_backfill_regime_w12_w26.sql` con `-- @data-only` en la PRIMERA línea. Insertar las filas del régimen W12–W26 como LITERALES calculados (source='signature' para W12–W20, source='manual' para W21–W26 que vienen del relevamiento SSH), con `inferred_mode`, `week`, `day` (nombre de día en español coincidente con el dayEnum), `tenant_id`=1, `confidence` y `evidence` opcional. Cada INSERT idempotente: `INSERT INTO session_week_regime (...) SELECT ... WHERE NOT EXISTS (SELECT 1 FROM session_week_regime WHERE tenant_id=1 AND week=? AND day=?)`. Encodear la tabla W21–W26 VERBATIM del CONTEXT (W21 mié=combos/jue=tecnica; W22 mié=tecnica/jue=combos; W23 mié=combos/jue=tecnica; W24–W26 mié=tecnica/jue=combos) y las W12–W20 de las firmas del research (combos días: W12jue, W13mié, W14jue, W15mié, W16jue, W17mié, W18jue, W19mié, W20mié; el complementario = técnica). Excluir Ladder/Ladder corta como falsos positivos; NO cruzar format_name↔format_params (Pitfall 8).
    ⚠ El backfill SOLO inserta en la tabla nueva; NO ejecuta ningún UPDATE/DELETE sobre `sessions` (D-18, histórico intacto). NO hace falta tabla de backup (no muta datos existentes). NUNCA un `;` en comentarios `--`.
    Crear `src/db/scripts/0203_regime_dryrun.sql`: SQL READ-ONLY que corre las firmas de detección contra `sessions`/`session_blocks` de prod (Complex×3 con reps 1–3 = combos; Flow Guiado final = técnica) y lista el régimen inferido por (week, day) — es material de verificación humana ANTES de aplicar la migración (SSH lo corre Franco, no el ejecutor).
    Crear `test/migrations/0202-session-week-regime.test.ts` sobre el molde de 0196: (a) la unique `uq_session_week_regime_tenant_week_day` existe con `SEQ_IN_INDEX=1` en `tenant_id` (INFORMATION_SCHEMA); (b) el conteo de filas de `sessions` es IDÉNTICO antes y después de aplicar la migración (D-18); (c) las filas W21–W26 insertadas coinciden con la tabla del CONTEXT. El test NO le pregunta a `_migrations`.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit && pnpm vitest run test/migrations/0202-session-week-regime.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `0203_backfill_regime_w12_w26.sql` empieza con `-- @data-only`, usa `WHERE NOT EXISTS` en cada INSERT, y NO contiene ningún `UPDATE`/`DELETE` sobre `sessions` (grep `grep -in "sessions" 0203_backfill_regime_w12_w26.sql` sin sentencias de mutación).
    - Ningún comentario `--` contiene `;`.
    - `src/db/scripts/0203_regime_dryrun.sql` existe y es read-only (sin INSERT/UPDATE/DELETE).
    - `test/migrations/0202-session-week-regime.test.ts` existe y pasa: assert de unique con tenant_id en SEQ_IN_INDEX=1, assert de conteo de `sessions` invariante, assert de las filas W21–W26.
    - tsc verde; el test de migración verde en foreground.
  </acceptance_criteria>
  <done>El backfill W12–W26 puebla el ancla de forma idempotente sin tocar `sessions`; dry-run disponible para verificación humana; test de migración verde (histórico intacto probado).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                    | Description                                                                                                 |
| ------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| migración → DB de prod                      | El backfill escribe datos derivados de firmas; debe ser idempotente y no mutar histórico                    |
| tabla nueva gym-owned → acceso multi-tenant | session_week_regime lleva tenant_id; lecturas futuras deben respetar el patrón de tenancy vigente en master |

## STRIDE Threat Register

| Threat ID | Category               | Component                                        | Disposition | Mitigation Plan                                                                                                                                      |
| --------- | ---------------------- | ------------------------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-159-08  | Tampering              | Backfill que muta filas históricas de `sessions` | mitigate    | El backfill SOLO inserta en `session_week_regime`; el test asserta conteo de `sessions` invariante (D-18)                                            |
| T-159-09  | Information Disclosure | Cross-tenant leak en la tabla del ancla          | mitigate    | `tenantIdColumn()` + alta en `GYM_OWNED_TABLES` + unique `(tenant_id, week, day)` con tenant_id primero; los gates CON-03/CON-06 de master la cubren |
| T-159-10  | Tampering              | Re-run del backfill duplica filas                | mitigate    | `INSERT … WHERE NOT EXISTS` idempotente por `(tenant_id, week, day)`                                                                                 |
| T-159-SC  | Tampering              | npm/pip installs                                 | accept      | Esta fase NO instala ni actualiza dependencias                                                                                                       |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit` verde.
- `pnpm vitest run test/db/tenant-tables.test.ts test/migrations/0202-session-week-regime.test.ts` verde en foreground.
- Numeración 0202/0203 re-verificada contra `origin/master` (no contra el checkout local).
- Ningún comentario `--` de las migraciones contiene `;`.
</verification>

<success_criteria>
El ancla session_week_regime existe (schema + DDL 0202 + clasificación gym-owned), retro-etiquetada W12–W26 por el backfill idempotente 0203 sin tocar el histórico de `sessions`; dry-run para verificación humana; tests verdes.
</success_criteria>

<output>
Create `.planning/phases/159-semana-nueva-backend-modos-de-d-a-generadores-roles-de-bloqu/159-04-SUMMARY.md` when done. Registrar el número de migración final usado (por si el tren v6.0 forzó renumeración) y el conjunto exacto de filas W12–W26 insertadas.
</output>
