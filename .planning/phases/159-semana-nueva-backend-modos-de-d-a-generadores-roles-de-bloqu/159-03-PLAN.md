---
phase: 159-semana-nueva-backend-modos-de-d-a-generadores-roles-de-bloqu
plan: 03
type: execute
wave: 3
depends_on: [159-01, 159-02]
files_modified:
  - el-templo-api/src/modules/sessions/combos-generator.ts
  - el-templo-api/src/modules/sessions/tecnica-generator.ts
  - el-templo-api/test/unit/combos-generator.test.ts
  - el-templo-api/test/unit/tecnica-generator.test.ts
autonomous: true
requirements: [SEM-02, SEM-03, SEM-04, SEM-06]
must_haves:
  truths:
    - "D-06: los bloques de combo usan el formato 'Combos' ya en prod (mig 0172) y mantienen la forma actual de reps del profe (rounds + reps por ejercicio); NO se introduce un parámetro único de reps"
    - "D-09: el formato por default de técnica es de calidad/skill (For Quality / Accumulate X / Cluster), editable por el coach"
    - "generateCombosSession devuelve sessionMode='combos' con 4 bloques en orden INITIUM → COMBOS_I → COMBOS_II → STRETCHING"
    - "COMBOS_I usa una ruta de tren_superior, COMBOS_II una ruta de tren_inferior, ambos con formato 'Combos'"
    - "generateTecnicaSession devuelve sessionMode='tecnica' con INITIUM → TECNICA_I → TECNICA_II → STRETCHING y TECNICA_I y TECNICA_II comparten la MISMA route"
    - "El bloque STRETCHING es idéntico en los 6 niveles del mismo (week, day)"
  artifacts:
    - path: "el-templo-api/src/modules/sessions/combos-generator.ts"
      provides: "generateCombosSession(db, week, day, levelGroup, memberLevel): DaySession"
      contains: "generateCombosSession"
    - path: "el-templo-api/src/modules/sessions/tecnica-generator.ts"
      provides: "generateTecnicaSession(db, week, day, levelGroup, memberLevel): DaySession"
      contains: "generateTecnicaSession"
    - path: "el-templo-api/test/unit/combos-generator.test.ts"
      provides: "SEM-03 + D-05 + D-11 (determinismo STRETCHING 6 niveles)"
      contains: "COMBOS_I"
    - path: "el-templo-api/test/unit/tecnica-generator.test.ts"
      provides: "SEM-04 + D-08 (misma ruta)"
      contains: "TECNICA_I"
  key_links:
    - from: "combos-generator.ts / tecnica-generator.ts"
      to: "semana-nueva-pipeline.ts (runSemanaNuevaBlockPipeline) + stretching-selection.ts + goal-plans/constants.ts"
      via: "ensamblado de 4 bloques con pipeline compartido"
      pattern: "runSemanaNuevaBlockPipeline|selectStretchingExercises"
    - from: "dayId de combos/tecnica"
      to: "esquema W{week}-{day}-{memberLevel} (MISMO que regular)"
      via: "buildDayIdCandidates de sessions/routes.ts busca por ese patrón"
      pattern: "W\\$\\{week\\}"
---

<objective>
Escribir los dos generadores de sesión: `combos-generator.ts` (INITIUM → COMBOS_I tren superior → COMBOS_II tren inferior → STRETCHING) y `tecnica-generator.ts` (INITIUM → TECNICA_I → TECNICA_II sobre la MISMA ruta → STRETCHING), ambos produciendo los 6 niveles (D-10) y consumiendo el tronco compartido del plan 02. Con sus tests unitarios (SEM-06).

Purpose: son los productores de las sesiones combos/técnica. Comparten ~80% (ensamblado de 4 bloques + STRETCHING + expansión de niveles); la diferencia es la resolución de ruta (dos pools con rol-en-hash vs. una ruta compartida sin rol-en-hash) y el formato forzado ('Combos' vs. formato de calidad). El `dayId` DEBE seguir el esquema regular `W{week}-{day}-{memberLevel}` para que `/sessions/daily`, `/weekly` y la TV los encuentren.

Output: dos generadores + dos test unitarios (DB mockeada); tsc verde y ambos tests verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

REGLA OPERATIVA: branchear desde `origin/master`, releer ahí. `git add` por ruta explícita. Tests SOLO en foreground con timeout amplio (PROHIBIDO `run_in_background` y loops con `pgrep`). Typecheck local sí.
</execution_context>

<context>
@.planning/phases/159-semana-nueva-backend-modos-de-d-a-generadores-roles-de-bloqu/159-RESEARCH.md
@.planning/phases/159-semana-nueva-backend-modos-de-d-a-generadores-roles-de-bloqu/159-PATTERNS.md
@.planning/phases/159-semana-nueva-backend-modos-de-d-a-generadores-roles-de-bloqu/159-02-SUMMARY.md

<interfaces>
<!-- Molde estructural: rom-generator.ts. Tronco compartido: plan 02 (leer su SUMMARY para firmas exactas). -->

rom-generator.ts:79-109 — firma + dayId + INITIUM compartido:
export async function generateRomSession(db, week, day, memberLevel): Promise<DaySession>
const dayId = `W${week}-${day}-${memberLevel}`; // MISMO esquema que regular
const initiumCtx = createInitialContext(week, day, levelGroup, memberLevel, "INITIUM");
const initiumBlock = await runInitiumPipeline(initiumCtx, db);

rom-generator.ts:111-127 — TraceEvent manual (objeto literal, severity/code/where/decision)
rom-generator.ts:157-185 — patrón de degradación de pool fino (severity WARNING + decision.fallback)
rom-generator.ts:205-252 — construcción de BlockPlan y return { ..., sessionMode: "rom" }
⚠ NO copiar `format: { formatId: 0, name: "ROM" }`: 'Combos'/'Stretching' SON filas reales de `formats` (mig 0172).

Lookup del formatId real (molde) — sessions/routes.ts:376-386:
select({name, description}).from(schema.formats) // resolver 'Combos' -> id real
⚠ el combo hoy trae rounds="" (string vacío) en prod — cualquier lectura de formatParams.rounds debe tolerar "".

goal-plans/constants.ts:12-33 — GOAL_PLAN_ROUTE_MAP.tren_superior / .tren_inferior (D-P1)

Expansión de levelGroup → memberLevels (D-10, 6 niveles) — admin/service.ts:719-724:
alfa_delta -> ["alfa","delta","kairos"]; sigma -> ["sigma"]; omega -> ["omega","spartan"]

Del plan 02 (ver 159-02-SUMMARY): runSemanaNuevaBlockPipeline(ctx, spomService, db, {route, forcedFormat}),
resolveRoutePool(pool, hashInput), selectStretchingExercises(db, week, day), simpleHash compartido.

Molde de tests unitarios — test/unit/rom-generator.test.ts:
:1-83 docblock + mocks de runInitiumPipeline y createInitialContext (vi.mock)
:267-278 createMockDb (select().from().where() encadenado)
:280-305 forma de los casos: import dinámico dentro del it, cast Parameters<typeof fn>[0], asserts por rol
:441 caso "graceful fallback when pool is thin"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: combos-generator.ts + test unitario (SEM-02, SEM-03, D-05, D-11)</name>
  <read_first>
    - el-templo-api/src/modules/sessions/rom-generator.ts (molde completo, 252 líneas)
    - el-templo-api/src/modules/sessions/pipeline/semana-nueva-pipeline.ts (creado en plan 02)
    - el-templo-api/src/modules/sessions/pipeline/utils/stretching-selection.ts (creado en plan 02)
    - el-templo-api/src/modules/goal-plans/constants.ts (GOAL_PLAN_ROUTE_MAP)
    - el-templo-api/test/unit/rom-generator.test.ts (molde de test)
    - .planning/phases/159-.../159-02-SUMMARY.md (firmas exactas del tronco compartido)
  </read_first>
  <behavior>
    - Test 1: `generateCombosSession(db, 21, "miercoles", "alfa_delta", "alfa")` devuelve `sessionMode: "combos"`, `blocks.length === 4`, roles en orden `["INITIUM","COMBOS_I","COMBOS_II","STRETCHING"]`.
    - Test 2: la `route` de COMBOS_I pertenece a `GOAL_PLAN_ROUTE_MAP.tren_superior` y la de COMBOS_II a `.tren_inferior` (D-05).
    - Test 3: los bloques COMBOS_I y COMBOS_II tienen `format.name === "Combos"` con `format.formatId !== 0` (id real).
    - Test 4 (anti Pitfall 1, obligatorio): generar los 6 niveles del mismo (week, day) — alfa, delta, kairos, sigma, omega, spartan — y assertear que los `exerciseId` del bloque STRETCHING son IDÉNTICOS en las 6 sesiones.
    - Test 5: degradación con pool fino no rompe (molde "graceful fallback").
    - Test 6: `validateSession` no produce severidad ERROR sobre la sesión generada (assert sobre `session.trace`).
  </behavior>
  <action>
    Escribir PRIMERO `test/unit/combos-generator.test.ts` (RED) sobre el molde de `rom-generator.test.ts`: mockear `runInitiumPipeline` y `createInitialContext` con `vi.mock`, usar `createMockDb` con un pool de ejercicios que cubra las rutas superior/inferior, y cubrir los 6 comportamientos de arriba. Para el Test 4, llamar 6 veces al generador (una por memberLevel) y comparar los exerciseId del bloque STRETCHING.
    Luego implementar `combos-generator.ts` sobre el molde de `rom-generator.ts` (GREEN). Firma: `generateCombosSession(db, week, day, levelGroup: LevelGroup, memberLevel: ExerciseLevel): Promise<DaySession>` (recibe levelGroup y los 6 niveles, NO el par alfa/delta de ROM). `dayId = `W${week}-${day}-${memberLevel}``. Ensamblado: (1) INITIUM vía `runInitiumPipeline`; (2) para role en `["COMBOS_I","COMBOS_II"]`: resolver la ruta con `resolveRoutePool(pool, `${week}-${day}-${role}`)` donde el pool es `tren_superior` para COMBOS_I y `tren_inferior` para COMBOS_II (constante de módulo exportada `COMBOS_ROUTE_POOLS`), y correr `runSemanaNuevaBlockPipeline` con `forcedFormat` = fila REAL 'Combos' (resolver el formatId con una query por nombre al arrancar el día); (3) STRETCHING vía `selectStretchingExercises(db, week, day)` ensamblado a mano con formato 'Stretching' (id real, NO 0). Return con `sessionMode: "combos"`, `goalPlanType: null`.
    3 ejercicios por combo (D-P7), editable — no hardcodear una cantidad rígida si el pipeline ya la deriva; documentar la elección en el trace.
    Trace con códigos `COMBOS_*`. Nunca `console.log` (CLAUDE.md #1). Sin `any`; `catch (err: unknown)` con `instanceof Error`.
    DRY: si el ensamblado de 4 bloques + STRETCHING + expansión de niveles se puede compartir con tecnica-generator (Task 2), extraer un helper común (el tronco). Preferible extraer a duplicar (CLAUDE.md #8).
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit && pnpm vitest run test/unit/combos-generator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test/unit/combos-generator.test.ts` existe y pasa, incluyendo el test de STRETCHING idéntico en los 6 niveles.
    - `generateCombosSession` retorna `sessionMode: "combos"`, 4 bloques, orden INITIUM→COMBOS_I→COMBOS_II→STRETCHING.
    - `grep -c "formatId: 0\|formatId:0" el-templo-api/src/modules/sessions/combos-generator.ts` == 0 (usa ids reales).
    - `grep -c "Math.random\|runBlockPipeline\|resolveRotator" el-templo-api/src/modules/sessions/combos-generator.ts` == 0.
    - tsc verde; el test verde en foreground.
  </acceptance_criteria>
  <done>combos-generator produce las sesiones de combos de 6 niveles con STRETCHING determinístico y formato 'Combos' real; test unitario verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tecnica-generator.ts + test unitario (SEM-04, D-08, D-09)</name>
  <read_first>
    - el-templo-api/src/modules/sessions/combos-generator.ts (creado en Task 1 — tronco a reusar)
    - el-templo-api/src/modules/sessions/pipeline/semana-nueva-pipeline.ts
    - el-templo-api/test/unit/combos-generator.test.ts (molde del test recién escrito)
    - .planning/phases/159-.../159-RESEARCH.md (Hallazgo 6, D-08: hashInput de técnica EXCLUYE el rol)
  </read_first>
  <behavior>
    - Test 1: `generateTecnicaSession(db, 21, "jueves", "alfa_delta", "alfa")` devuelve `sessionMode: "tecnica"`, 4 bloques, orden `["INITIUM","TECNICA_I","TECNICA_II","STRETCHING"]`.
    - Test 2 (D-08, clave): TECNICA_I.route === TECNICA_II.route (la MISMA ruta para ambos bloques).
    - Test 3 (D-09): el formato forzado de TECNICA_I/II es un formato de calidad (For Quality / Cluster / Accumulate X), no 'Combos'.
    - Test 4: STRETCHING idéntico en los 6 niveles del mismo (week, day).
    - Test 5: `validateSession` sin severidad ERROR.
  </behavior>
  <action>
    Escribir PRIMERO `test/unit/tecnica-generator.test.ts` (RED) sobre el molde del test de combos. El Test 2 es el diferenciador: assertear que TECNICA_I y TECNICA_II caen en la misma `route`.
    Luego implementar `tecnica-generator.ts` (GREEN), reusando el tronco compartido de Task 1. Firma análoga: `generateTecnicaSession(db, week, day, levelGroup, memberLevel): Promise<DaySession>`. Diferencia crítica vs. combos: resolver UNA sola ruta para ambos bloques con `resolveRoutePool(pool, `${week}-${day}`)` — hashInput SIN el rol (D-08) — y pasar esa misma ruta a TECNICA_I y TECNICA_II. El pool de ruta de técnica: usar el conjunto de rutas de skill apropiado (arrancar del catálogo de rutas de fuerza disponible; si no hay una lista curada específica, usar la unión `tren_superior` + `tren_inferior` o el pool que el research indique, documentando la elección en el trace y en el SUMMARY). `forcedFormat` = formato de calidad (resolver por nombre 'For Quality' con id real; el coach lo edita, D-09). STRETCHING igual que combos. Return `sessionMode: "tecnica"`, `goalPlanType: null`.
    Reusar el helper de tronco de Task 1 (no duplicar el ensamblado). Trace con códigos `TECNICA_*`. Nunca `console.log`. Sin `any`.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit && pnpm vitest run test/unit/tecnica-generator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test/unit/tecnica-generator.test.ts` existe y pasa, incluyendo la aserción TECNICA_I.route === TECNICA_II.route.
    - `generateTecnicaSession` retorna `sessionMode: "tecnica"`, 4 bloques, orden INITIUM→TECNICA_I→TECNICA_II→STRETCHING.
    - El hashInput de resolución de ruta de técnica NO incluye el rol (grep del patrón `${week}-${day}` sin `-${role}` en la resolución compartida de técnica).
    - `grep -c "formatId: 0\|Math.random" el-templo-api/src/modules/sessions/tecnica-generator.ts` == 0.
    - tsc verde; el test verde en foreground.
  </acceptance_criteria>
  <done>tecnica-generator produce sesiones de técnica con TECNICA_I y TECNICA_II sobre la misma ruta y formato de calidad; STRETCHING determinístico; test unitario verde.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                            |
| -------------------------- | ---------------------------------------------------------------------- |
| generador server-side → DB | Roles, rutas y formato los fija el generador; nunca vienen del payload |

## STRIDE Threat Register

| Threat ID | Category               | Component                                     | Disposition | Mitigation Plan                                                                                                                               |
| --------- | ---------------------- | --------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| T-159-06  | Tampering              | Rol/ruta/formato inyectados                   | accept      | Se resuelven server-side (hash + listas curadas + lookup de formats por nombre); el payload no los controla                                   |
| T-159-07  | Information Disclosure | Lectura de `formats`/`exercises` cross-tenant | accept      | Catálogos globales (formats, exercises) no son gym-owned; la sesión resultante se guarda con el tenant vigente en `saveSession` (sin cambios) |
| T-159-SC  | Tampering              | npm/pip installs                              | accept      | Esta fase NO instala ni actualiza dependencias                                                                                                |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit` verde.
- `pnpm vitest run test/unit/combos-generator.test.ts test/unit/tecnica-generator.test.ts` verde en foreground.
- Regresión: `pnpm vitest run test/unit/rom-generator.test.ts` sigue verde (no se tocó rom-generator).
</verification>

<success_criteria>
Los dos generadores producen las sesiones combos/técnica de 6 niveles con la estructura de bloques correcta, STRETCHING determinístico idéntico en los 6 niveles, formatos reales; tests unitarios verdes; tsc verde.
</success_criteria>

<output>
Create `.planning/phases/159-semana-nueva-backend-modos-de-d-a-generadores-roles-de-bloqu/159-03-SUMMARY.md` when done. Documentar las firmas exactas de `generateCombosSession` y `generateTecnicaSession` (el plan 05 las importa) y el pool de ruta elegido para técnica.
</output>
