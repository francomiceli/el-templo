---
phase: 159-semana-nueva-backend-modos-de-d-a-generadores-roles-de-bloqu
plan: 06
type: execute
wave: 2
depends_on: ["159-04"]
files_modified:
  - el-templo-api/src/modules/scheduling/service.ts
  - el-templo-api/src/db/seed-production.ts
  - el-templo-api/src/db/migrations/0204_rename_calistenia_general.sql
  - el-templo-api/src/modules/tv/class-day.ts
  - el-templo-api/test/scheduling/derived-class-label.test.ts
autonomous: true
requirements: [SEM-13]
must_haves:
  truths:
    - "D-15: el nombre de la clase en horarios/app se deriva de la sesión generada del día (combos→'Combos', tecnica→'Técnica', regular→'General'); no un mapeo fijo por día de la semana"
    - "D-16: la etiqueta genérica 'Calistenia' se renombra a 'General' (nombre por default cuando el día es regular)"
    - "D-17: sin crear actividades nuevas en activities, sin tocar reservas/cupos/gating; solo la etiqueta visible, global a todas las sedes"
    - "getWeeklySchedule devuelve 'Combos' / 'Técnica' / 'General' según la sesión aprobada del día, sin N+1"
    - "La derivación solo aplica a la actividad genérica (no ROM, no isSpecial)"
    - "La fila activities 'Calistenia' pasó a 'General' y los dos get-or-create buscan 'General' (no duplica al sembrar una sede)"
    - "resolveClassDay (TV) lee el modo de sessions.session_mode con fallback a day_modes"
  artifacts:
    - path: "el-templo-api/src/modules/scheduling/service.ts"
      provides: "etiqueta de clase derivada de la sesión + literal 'General' en seedDefaultSchedules"
      contains: "General"
    - path: "el-templo-api/src/db/migrations/0204_rename_calistenia_general.sql"
      provides: "rename @data-only idempotente de la fila activities"
      contains: "UPDATE activities"
    - path: "el-templo-api/src/modules/tv/class-day.ts"
      provides: "modo del día leído de la sesión (no de day_modes)"
      contains: "sessionMode"
    - path: "el-templo-api/test/scheduling/derived-class-label.test.ts"
      provides: "SEM-13(b)(c): etiqueta derivada + no-duplicación al sembrar sede"
      contains: "General"
  key_links:
    - from: "scheduling/service.ts getWeeklySchedule"
      to: "sessions(week, session_mode, goal_plan_type IS NULL, status='approved')"
      via: "una query por semana (anti N+1) → Map modo por día"
      pattern: "selectDistinct|session_mode|sessionMode"
    - from: "migración 0204 (rename dato)"
      to: "scheduling/service.ts:792 y seed-production.ts:100-124 (get-or-create por literal)"
      via: "MISMO commit — si no, la próxima sede duplica la actividad"
      pattern: "General"
---

<objective>
Que el nombre de la clase en horarios/app se DERIVE de la sesión generada del día (`combos`→"Combos", `tecnica`→"Técnica", si no→"General") como read-model de solo lectura (D-15), renombrar la actividad genérica "Calistenia"→"General" (D-16, fila de `activities` + los dos get-or-create, mismo commit), y corregir la TV para que lea el modo de la sesión y no de `day_modes` (D-P3). Todo backend.

Purpose: el modo lo elige el profe día a día → la etiqueta se actualiza sola, sin mapeo fijo por día de la semana. D-17 prohíbe tocar reservas/cupos/gating: la derivación va SOLO en el read-model `getWeeklySchedule` y solo para la actividad genérica (un slot ROM o `isSpecial` NO se renombra). "Calistenia" es una FILA de `activities` (no un string de UI) referenciada por analytics/ratings/reports/attendance/bookings: si se renombra el dato sin actualizar los dos get-or-create por literal, la próxima sede nueva crea una actividad DUPLICADA (Pitfall 4).

Output: derivación + rename + fix TV + test de integración; tsc verde y el test verde. Depende de 159-04 SOLO por numeración de migraciones (0204 asume que 0202/0203 los toma 159-04): Wave 2, después de 159-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

REGLA OPERATIVA: branchear desde `origin/master`, releer ahí. NUMERACIÓN DE MIGRACIONES: este plan es Wave 2 y `depends_on: ["159-04"]` porque 159-04 reserva 0202/0203 y esta migración es 0204 — 159-04 DEBE haber corrido antes. RE-VERIFICAR igual la numeración contra origin/master (0204 libre) y renumerar si el tren v6.0 mergeó a master. `git add` por ruta explícita. Tests SOLO en foreground con timeout amplio (PROHIBIDO `run_in_background`/`pgrep`).

⚠️ DOBLE PUSH (D-P3): el trabajo de TV vive en staging (`et-tv2`/`feat/tv-login-staging`) Y en master (`et-tv-master`) como HISTORIAS SEPARADAS. El fix de `tv/class-day.ts` de esta fase, al llegar a master, requiere DOBLE PUSH (cada cambio de TV se aplica en las dos historias). Declararlo en el SUMMARY para que el orquestador/Franco lo tenga presente al shippear.

Skill `el-templo-db-migrations`: `@data-only` en la 1ª línea; idempotente (`WHERE`); NUNCA `;` en comentarios `--`; SQL en el MISMO commit que las ediciones de código; NUNCA `drizzle-kit migrate`.
</execution_context>

<context>
@.planning/phases/159-semana-nueva-backend-modos-de-d-a-generadores-roles-de-bloqu/159-RESEARCH.md
@.planning/phases/159-semana-nueva-backend-modos-de-d-a-generadores-roles-de-bloqu/159-PATTERNS.md

<interfaces>
<!-- origin/master. -->

scheduling/service.ts:169-197 — query de slots de getWeeklySchedule:
select({ id, activityId, activityName: activities.name, activityMaxCapacity, isSpecial: activities.isSpecial, dayOfWeek, ... })
scheduling/service.ts:207-275 — patrón anti-N+1: Sets/Maps precargados ANTES del loop (holidayDates, bookingCountMap)
scheduling/service.ts:277-324 — loop de slots: slotDate = addDays(weekStartDate, row.dayOfWeek-1);
slots.push({ id, activityName: row.activityName, ..., isSpecial: row.isSpecial })
⚠ HAY UN SEGUNDO proyector con activityName en el MISMO archivo (:960-993) — verificar si necesita la misma derivación.

Derivación recomendada (RESEARCH Code Examples), UNA query por semana (anti Pitfall 5):
selectDistinct({ day: sessions.day, mode: sessions.sessionMode }).from(sessions)
.where(and(eq(sessions.week, week), eq(sessions.status,"approved"), isNull(sessions.goalPlanType)))
const modeByDay = new Map(rows.map(r => [r.day, r.mode]));
const DERIVED_LABEL = { combos: "Combos", tecnica: "Técnica" };
const derived = isGenericActivity ? DERIVED_LABEL[modeByDay.get(dayName) ?? ""] : undefined;
activityName: derived ?? row.activityName // solo si NO es ROM y NO isSpecial

Get-or-create por literal (Pitfall 4) — scheduling/service.ts:792-805 y seed-production.ts:100-124:
where(eq(schema.activities.name, "Calistenia")) // tres ocurrencias en seed-production: select, insert, re-select
⚠ seed-production.ts usa console.log (excepción histórica de seeds) — NO replicar console.log en servicios.

Rename migración molde — 0172\_...sql (@data-only + idempotente):
→ equivalente: UPDATE activities SET name='General' WHERE name='Calistenia'; (no-op en replay)

TV — tv/class-day.ts:118-163 resolveClassDay:
paso 1 lee day_modes; paso 2 ya hace select de sessions con goal_plan_type IS NULL (week, day, approved).
→ agregar `sessionMode: schema.sessions.sessionMode` al select del paso 2 y derivar `mode` de ahí, day_modes como fallback. CERO queries nuevas.
helpers de fecha: shared/week-dates.ts (WEEK_ONE_MONDAY, dateToWeekNumber, dateToDayName) — fuente única.

Test molde — test/scheduling/155-horarios.test.ts:1-40 (docblock de casos + helpers cleanAllTestData, dateOffsetStr).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: etiqueta derivada en getWeeklySchedule (anti-N+1) + literal 'General' en seedDefaultSchedules</name>
  <read_first>
    - el-templo-api/src/modules/scheduling/service.ts (getWeeklySchedule :169-331; segundo proyector :960-993; seedDefaultSchedules :792-805)
    - el-templo-api/src/modules/shared/week-dates.ts (dateToWeekNumber, dateToDayName)
  </read_first>
  <action>
    En `getWeeklySchedule`: ANTES del loop de slots, agregar UNA sola query por semana (anti Pitfall 5, igual que bookingCountMap): `selectDistinct({ day, mode: sessions.sessionMode }).from(sessions).where(and(eq(week), eq(status,"approved"), isNull(goalPlanType)))` → `Map<day, mode>`. Dentro del loop, derivar la etiqueta SOLO cuando el slot es la actividad genérica (nombre 'General'/'Calistenia') y NO es ROM y NO es `isSpecial`: `combos`→"Combos", `tecnica`→"Técnica", cualquier otro→el `activityName` original. NO renombrar slots ROM ni actividades especiales. NO tocar reservas, cupos ni gating (D-17): solo el campo `activityName` del slot view.
    Verificar el SEGUNDO proyector con `activityName` (:960-993): si sirve a horarios/app con la misma actividad genérica, aplicar la misma derivación; si es otro contexto (p.ej. una vista que no debe derivar), documentar por qué se deja.
    En `seedDefaultSchedules` (:792-805): cambiar el literal de get-or-create de `"Calistenia"` a `"General"` (select e insert). Es la mitad de código del rename (la otra mitad es seed-production.ts en Task 2, y la migración en Task 2) — DEBE ir en el mismo commit que la migración 0204.
    Sin `any`; nunca `console.log` en el servicio.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - getWeeklySchedule carga el modo por día en UN Map antes del loop (grep `selectDistinct` o el select de sessions con `goalPlanType`/`isNull` fuera del loop; sin query de modo dentro del `for` de slots).
    - La derivación aplica solo a la actividad genérica y respeta `isSpecial` (grep de la guarda `isSpecial`).
    - `grep -c "Calistenia" el-templo-api/src/modules/scheduling/service.ts` == 0 (el literal de seedDefaultSchedules pasó a 'General').
    - `pnpm exec tsc --noEmit` verde.
  </acceptance_criteria>
  <done>La etiqueta de clase se deriva de la sesión aprobada del día con una query por semana, solo para la actividad genérica; el get-or-create de scheduling usa 'General'; tsc verde.</done>
</task>

<task type="auto">
  <name>Task 2: rename 'Calistenia'→'General' (migración 0204 + seed-production) + fix TV + test</name>
  <read_first>
    - el-templo-api/src/db/seed-production.ts:100-124 (get-or-create por literal 'Calistenia', 3 ocurrencias)
    - el-templo-api/src/db/migrations/0172_formats_combos_stretching_ruta_fullbody.sql (molde @data-only idempotente)
    - el-templo-api/src/modules/tv/class-day.ts:118-163 (resolveClassDay)
    - el-templo-api/test/scheduling/155-horarios.test.ts:1-40 (molde de test)
    - RE-VERIFICAR: `git ls-tree --name-only origin/master el-templo-api/src/db/migrations/ | tail` (0204 libre; 159-04 ya tomó 0202/0203)
  </read_first>
  <action>
    Crear `0204_rename_calistenia_general.sql` con `-- @data-only` en la 1ª línea + header (propósito; hand-written; numeración 0204 verificada, 0202/0203 los toma 159-04; advertencia del `;`). Sentencia idempotente: `UPDATE activities SET name = 'General' WHERE name = 'Calistenia';` (no-op en replay). Efecto retroactivo aceptado (reports/analytics históricos mostrarán "General" — declararlo en el header y en el SUMMARY; A2 pendiente de confirmación de Franco). NUNCA un `;` en un comentario `--`.
    En `seed-production.ts:100-124`: cambiar las TRES ocurrencias del literal `"Calistenia"` a `"General"` (select, insert, re-select). NO replicar el `console.log` en código de servicio (seed es excepción histórica, se mantiene su console.log). Esta edición + la de scheduling/service.ts (Task 1) + la migración 0204 van en el MISMO commit (Pitfall 4).
    En `tv/class-day.ts`: agregar `sessionMode: schema.sessions.sessionMode` al select del paso 2 (la query de sessions con `goal_plan_type IS NULL` ya existe) y derivar `mode` desde esa fila (combos/tecnica/rom/regular), usando `day_modes` como fallback. CERO queries nuevas.
    Crear `test/scheduling/derived-class-label.test.ts` (integración vs MySQL real) sobre el molde de 155-horarios: (a) con una sesión aprobada `session_mode='combos'` para (week, day), `getWeeklySchedule` devuelve "Combos" para el slot genérico de ese día; `tecnica`→"Técnica"; sin sesión combos/técnica→"General"; (b) un slot `isSpecial` NO se renombra aunque el día sea combos; (c) sembrar una sede nueva (seedDefaultSchedules) tras el rename NO crea una segunda fila 'General'/'Calistenia' (no-duplicación). Usuarios por email, nunca ids hardcodeados.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit && pnpm vitest run test/scheduling/derived-class-label.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `0204_rename_calistenia_general.sql` empieza con `-- @data-only`, contiene `UPDATE activities SET name = 'General' WHERE name = 'Calistenia'`, ningún comentario `--` con `;`.
    - `grep -c "Calistenia" el-templo-api/src/db/seed-production.ts` == 0 (las 3 ocurrencias pasaron a 'General').
    - `tv/class-day.ts` selecciona `sessionMode` de `sessions` y deriva `mode` de ahí (grep `sessionMode`).
    - `test/scheduling/derived-class-label.test.ts` existe y pasa: casos Combos/Técnica/General, guarda isSpecial, y no-duplicación al sembrar.
    - tsc verde; el test verde en foreground.
  </acceptance_criteria>
  <done>La actividad genérica se renombra a 'General' (dato + los dos get-or-create + migración, mismo commit) sin duplicar; la TV lee el modo de la sesión; test de integración verde. DOBLE PUSH de la TV anotado en el SUMMARY.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                      |
| --------------------------------- | -------------------------------------------------------------------------------- |
| read-model del horario → cliente  | La etiqueta derivada es solo-lectura; no cambia reservas/cupos/gating            |
| migración @data-only → DB de prod | El rename muta una fila de `activities` viva referenciada por muchas superficies |

## STRIDE Threat Register

| Threat ID | Category          | Component                                                                      | Disposition | Mitigation Plan                                                                                                                           |
| --------- | ----------------- | ------------------------------------------------------------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| T-159-14  | Tampering         | Rename del dato sin actualizar los get-or-create → duplicación de la actividad | mitigate    | La migración 0204 + las dos ediciones de get-or-create ('General') van en el MISMO commit; el test de no-duplicación al sembrar lo prueba |
| T-159-15  | Denial of Service | N+1 en la etiqueta derivada del horario (timeout de prod conocido)             | mitigate    | Una sola query por semana cargada en un Map antes del loop de slots (patrón bookingCountMap)                                              |
| T-159-16  | Tampering         | Derivación que renombra un slot ROM o especial                                 | mitigate    | La derivación solo aplica a la actividad genérica y respeta la guarda `isSpecial`                                                         |
| T-159-SC  | Tampering         | npm/pip installs                                                               | accept      | Esta fase NO instala ni actualiza dependencias                                                                                            |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit` verde.
- `pnpm vitest run test/scheduling/derived-class-label.test.ts` verde en foreground.
- Migración 0204 con `@data-only`, idempotente, sin `;` en comentarios; re-verificada contra origin/master (0202/0203 reservados por 159-04).
- D-17 respetado: reservas/cupos/gating sin cambios.
</verification>

<success_criteria>
La etiqueta de clase se deriva de la sesión (Combos/Técnica/General) sin N+1 y solo para la actividad genérica; 'Calistenia'→'General' en dato + los dos get-or-create + migración (mismo commit), sin duplicar; la TV lee el modo de la sesión con fallback a day_modes; test de integración verde.
</success_criteria>

<output>
Create `.planning/phases/159-semana-nueva-backend-modos-de-d-a-generadores-roles-de-bloqu/159-06-SUMMARY.md` when done. ANOTAR: (1) el DOBLE PUSH del fix de tv/class-day.ts (staging + master, historias separadas); (2) el efecto retroactivo del rename en reports históricos (A2, confirmar con Franco); (3) el número de migración final si hubo renumeración.
</output>
</content>
