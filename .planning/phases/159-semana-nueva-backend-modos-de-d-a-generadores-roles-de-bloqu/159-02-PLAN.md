---
phase: 159-semana-nueva-backend-modos-de-d-a-generadores-roles-de-bloqu
plan: 02
type: execute
wave: 2
depends_on: [159-01]
files_modified:
  - el-templo-api/src/modules/sessions/pipeline/semana-nueva-pipeline.ts
  - el-templo-api/src/modules/sessions/pipeline/utils/stretching-selection.ts
  - el-templo-api/test/unit/stretching-selection.test.ts
autonomous: true
requirements: [SEM-03, SEM-04, SEM-06]
must_haves:
  truths:
    - "D-12: el bloque STRETCHING reutiliza el pool y la lógica de selección de movilidad de ROM (mobility_related), no reinventa la maquinaria"
    - "D-13: el generador elige ~4 ejercicios de movilidad (estilo INITIUM), editables por el coach; no es bloque fijo curado"
    - "La resolución de ruta es determinística por (week, day[, role]) sin Math.random"
    - "El bloque STRETCHING es idéntico para dos llamadas con el mismo (week, day) y NO depende de memberLevel"
    - "COMBOS_I resuelve sobre GOAL_PLAN_ROUTE_MAP.tren_superior, COMBOS_II sobre .tren_inferior; TECNICA_I y TECNICA_II resuelven sobre la MISMA ruta"
  artifacts:
    - path: "el-templo-api/src/modules/sessions/pipeline/semana-nueva-pipeline.ts"
      provides: "runSemanaNuevaBlockPipeline: Stage 1 reemplazado (ruta inyectada), stages 2-7 reusados, formato forzado"
      contains: "runSemanaNuevaBlockPipeline"
    - path: "el-templo-api/src/modules/sessions/pipeline/utils/stretching-selection.ts"
      provides: "selección determinística de ~4 ejercicios de movilidad, función pura de (week, day)"
      contains: "simpleHash"
    - path: "el-templo-api/test/unit/stretching-selection.test.ts"
      provides: "test de determinismo del bloque STRETCHING (anti Pitfall 1)"
      contains: "toEqual"
  key_links:
    - from: "semana-nueva-pipeline.ts"
      to: "pipeline/stage-2..7 + goal-plans/constants.ts GOAL_PLAN_ROUTE_MAP"
      via: "import de stages reusados + resolución de ruta por simpleHash"
      pattern: "resolveSpom|deriveBudget|GOAL_PLAN_ROUTE_MAP"
    - from: "stretching-selection.ts"
      to: "simpleHash(`${week}-${day}-STRETCHING-${i}`)"
      via: "selección pura sin Math.random ni memberLevel"
      pattern: "simpleHash"
---

<objective>
Construir el TRONCO COMPARTIDO de los generadores nuevos: (1) `semana-nueva-pipeline.ts`, un orquestador de bloque que reemplaza SOLO el Stage 1 (resolución de ruta inyectada) y reusa los stages 2-7 del pipeline principal (molde exacto: `goal-plan-pipeline.ts`, D-P6); (2) `stretching-selection.ts`, la selección determinística de ~4 ejercicios de movilidad para el bloque STRETCHING, función PURA de `(week, day)` (anti Pitfall 1). Los generadores combos/tecnica (plan 03) consumen ambos.

Purpose: los roles nuevos NO pueden pasar por `runBlockPipeline`/Stage 1 (`stage-1-rotator.ts:57` tiene `default: throw "Unknown block role"`). El `weekly_rotator` no tiene columnas para ellos. La salida: reusar la selección de ejercicios por dificultad de los 6 niveles (stages 2-7, ~600 líneas ya calibradas) inyectando la ruta desde una lista curada. El STRETCHING se genera 6 veces (1 por nivel) y DEBE ser idéntico en los 6, o el PDF/TV/app muestran filas divergentes.

Output: pipeline + selección de estiramiento + test de determinismo; `pnpm exec tsc --noEmit` verde y el test verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

REGLA OPERATIVA: branchear desde `origin/master`, releer ahí. `git add` por ruta explícita. Tests SOLO en foreground con timeout amplio (PROHIBIDO `run_in_background` y loops con `pgrep`). Typecheck local sí; suite completo en CI.
</execution_context>

<context>
@.planning/phases/159-semana-nueva-backend-modos-de-d-a-generadores-roles-de-bloqu/159-RESEARCH.md
@.planning/phases/159-semana-nueva-backend-modos-de-d-a-generadores-roles-de-bloqu/159-PATTERNS.md
@.planning/phases/159-semana-nueva-backend-modos-de-d-a-generadores-roles-de-bloqu/159-01-SUMMARY.md

<interfaces>
<!-- Molde canónico: goal-plan-pipeline.ts (origin/master). Copiar la forma, no explorar. -->

goal-plan-pipeline.ts:22-38 — imports de stages reusados (copiar literal):
import { resolveSpom } from "./stage-2-spom";
import { deriveBudget } from "./stage-3-budget";
import { deriveContraction } from "./stage-4-contraction";
import { selectFormat } from "./stage-5-format";
import { selectExercises } from "./stage-6-exercises";
import { generatePrescriptions } from "./stage-7-prescription";
import { runInitiumPipeline } from "./initium-pipeline";

goal-plan-pipeline.ts:44-50 — simpleHash (función pura, DRY: extraer a un util compartido):
function simpleHash(input): number { hash=0; for i: hash=(hash + charCodeAt(i)\*(i+1))|0; return Math.abs(hash) }

goal-plan-pipeline.ts:214-251 — cuerpo del pipeline:
if (ctx.role === "INITIUM") return runInitiumPipeline(...);
...stage 5 con forcedFormat: if (options?.forcedFormat) ctx5 = {...ctx4, format: options.forcedFormat} else selectFormat(...)
ctx6 = await selectExercises(ctx5, db); finalCtx = generatePrescriptions(ctx6);
catch: createTraceEvent("PIPELINE_ERROR","ERROR",...) + re-throw

pipeline/index.ts:28-36 — BlockPipelineOptions YA soporta forcedFormat (NO extender):
interface BlockPipelineOptions { forcedFormat?: {formatId:number; name:string}; excludeFormatNames?: string[] }

goal-plans/constants.ts:12-33 — GOAL_PLAN_ROUTE_MAP (usar tal cual, D-P1):
tren_superior: ["HS","HSPU","PHS","OAPU","PLPU","MU","OAP","OAR","BL","HD/ID","MN/RP","FL","FLR","TTB"]
tren_inferior: ["SU","SS","PS","QC","DS","NC","HT"]

Analog de estructura de stretching (COPIAR menos la línea 68):
pipeline/utils/mobility-selection.ts:34-83 — query pool: where(eq(schema.exercises.pattern,'MOVILIDAD'))
fallback: if (pool.length===0) pool = allMobility
:68 -> pool[Math.floor(Math.random()*pool.length)] // PROHIBIDO copiar (Pitfall 1)
MOBILITY_DEFAULTS: CON_REPS / ISO_SECONDS; prescripción por effort ('ISO' -> seconds, else reps)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: semana-nueva-pipeline.ts — orquestador de bloque con Stage 1 reemplazado</name>
  <read_first>
    - el-templo-api/src/modules/sessions/pipeline/goal-plan-pipeline.ts (molde canónico completo — imports :22-38, simpleHash :44-50, resolveGoalPlanRoute :52-88, cuerpo :214-251, catch :364-374)
    - el-templo-api/src/modules/sessions/pipeline/index.ts (BlockPipelineOptions :28-36 — forcedFormat ya existe)
    - el-templo-api/src/modules/goal-plans/constants.ts (GOAL_PLAN_ROUTE_MAP :12-33)
    - el-templo-api/src/modules/sessions/pipeline/context.ts (createInitialContext, appendTrace, createTraceEvent)
  </read_first>
  <action>
    Crear `semana-nueva-pipeline.ts` copiando la estructura de `goal-plan-pipeline.ts`. Exportar `runSemanaNuevaBlockPipeline(initialContext, spomService, db, options)` donde `options` incluye la `route` ya resuelta (string) y el `forcedFormat` ({formatId, name}). El cuerpo: si `role === "INITIUM"` delega a `runInitiumPipeline`; de lo contrario inyecta la `route` en el contexto (reemplazo de Stage 1), corre `resolveSpom → deriveBudget → deriveContraction → (forcedFormat ? saltear selectFormat : selectFormat) → selectExercises → generatePrescriptions`, ensambla el `BlockPlan` con `blockId/role/route/pattern/intensity/repsBudget/format/formatParams/exercises/trace`, y en el catch agrega el trace `PIPELINE_ERROR` (severity ERROR) y re-lanza.
    DRY (CLAUDE.md #8): extraer `simpleHash` a un util compartido (p.ej. `pipeline/utils/deterministic-hash.ts`) y que TANTO este archivo como el generador lo importen, en vez de duplicar el cuerpo. Si extraer rompe algo del `goal-plan-pipeline.ts` existente, dejar `simpleHash` duplicado byte-a-byte con el mismo cuerpo y un comentario que apunte a la fuente (NO reescribir el algoritmo). NO tocar `goal-plan-pipeline.ts`.
    Exportar además un helper de resolución de ruta reutilizable por los generadores: `resolveRoutePool(pool: string[], hashInput: string): string` = `pool[simpleHash(hashInput) % pool.length]`. El hashInput lo arma el generador (plan 03): para COMBOS incluye el rol (`${week}-${day}-${role}`), para TECNICA EXCLUYE el rol (`${week}-${day}`) para que I y II caigan en la misma ruta (D-08).
    Trace con códigos de dominio `SEMANA_NUEVA_*` (nunca `console.log`). Usar el logger Pino si se necesita loguear (CLAUDE.md #1). Sin `any`.
    Los combos usan `forcedFormat` = fila REAL 'Combos' de `formats` (NO formatId:0); el formatId real lo resuelve el generador con una query por nombre y lo pasa en options.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Existe `el-templo-api/src/modules/sessions/pipeline/semana-nueva-pipeline.ts` con `export` de `runSemanaNuevaBlockPipeline`.
    - Importa `resolveSpom`, `deriveBudget`, `deriveContraction`, `selectFormat`, `selectExercises`, `generatePrescriptions`, `runInitiumPipeline` del pipeline principal (grep confirma los 7 imports).
    - NO llama a `runBlockPipeline` ni a `resolveRotator`/`stage-1-rotator` (grep NO debe encontrarlos).
    - NO usa `Math.random` (grep vacío en el archivo nuevo).
    - `goal-plan-pipeline.ts` sin cambios (git diff vacío en ese archivo).
    - `pnpm exec tsc --noEmit` en 0 errores.
  </acceptance_criteria>
  <done>runSemanaNuevaBlockPipeline reusa stages 2-7 con ruta inyectada y formato forzado, sin pasar por el rotator; simpleHash compartido; tsc verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: stretching-selection.ts determinística + test de determinismo</name>
  <read_first>
    - el-templo-api/src/modules/sessions/pipeline/utils/mobility-selection.ts (estructura completa :34-83 — query del pool, fallback, prescripción por effort; línea 68 PROHIBIDA)
    - el-templo-api/src/modules/sessions/pipeline/goal-plan-pipeline.ts:44-50 (simpleHash — fuente del determinismo)
    - el-templo-api/test/unit/rom-generator.test.ts (createMockDb :267-278 — molde del mock de DB encadenado select().from().where())
  </read_first>
  <behavior>
    - Test 1: `selectStretchingExercises(db, week, day)` llamado dos veces con el MISMO (week, day) devuelve el mismo arreglo de exerciseId (determinismo puro).
    - Test 2: la firma NO recibe `memberLevel` — dos "niveles" (dos llamadas idénticas) producen exactamente los mismos ejercicios (anti Pitfall 1: los 6 niveles no deben divergir).
    - Test 3: devuelve ~4 ejercicios (D-13) sin duplicados; si el pool tiene < 4 devuelve todos los disponibles sin repetir.
    - Test 4: NO invoca Math.random (verificable: mockear Math.random con vi.fn y assertear que no se llamó, o inspección estática por grep en acceptance).
  </behavior>
  <action>
    Crear `stretching-selection.ts` copiando la query del pool y la prescripción por `effort` ('ISO' → seconds, else reps con MOBILITY_DEFAULTS) de `mobility-selection.ts`, PERO reemplazando la selección aleatoria (línea 68) por una selección PURA: ordenar el pool por `id` ascendente y elegir 4 ejercicios con `simpleHash(`${week}-${day}-STRETCHING-${i}`) % ordered.length` para i=0,1,2,… deduplicando por id (avanzar i hasta juntar 4 o agotar el pool). La función NO recibe `memberLevel` — su firma es `selectStretchingExercises(db, week, day)`. Importar `simpleHash` del util compartido creado en Task 1. Devolver `ExercisePrescription[]` (o el tipo que consuma el generador) con `exerciseType: 'mobility'`.
    Prohibido: `Math.random`, cualquier dependencia de `memberLevel`, y copiar `shuffleArray` de `rom-generator.ts:61-68`.
    Escribir PRIMERO el test `test/unit/stretching-selection.test.ts` (RED), con el `createMockDb` de `rom-generator.test.ts` devolviendo un pool fijo de ejercicios de movilidad; luego implementar hasta que pase (GREEN).
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit && pnpm vitest run test/unit/stretching-selection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test/unit/stretching-selection.test.ts` existe y pasa; incluye al menos una aserción `toEqual` que compara dos invocaciones con el mismo (week, day).
    - `grep -c "Math.random" el-templo-api/src/modules/sessions/pipeline/utils/stretching-selection.ts` == 0.
    - `grep -c "memberLevel\|member_level" el-templo-api/src/modules/sessions/pipeline/utils/stretching-selection.ts` == 0 (la firma no lo recibe).
    - La función devuelve <= 4 ejercicios sin ids duplicados.
    - tsc verde; el test verde en foreground.
  </acceptance_criteria>
  <done>stretching-selection.ts es función pura de (week, day), sin Math.random ni memberLevel, con test de determinismo verde.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                        |
| -------------------------- | ------------------------------------------------------------------ |
| generador server-side → DB | Las rutas y el rol los fija el generador; nunca vienen del payload |

## STRIDE Threat Register

| Threat ID | Category          | Component                                         | Disposition | Mitigation Plan                                                                                                                        |
| --------- | ----------------- | ------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| T-159-04  | Tampering         | Rol/ruta inyectados desde el cliente              | accept      | Los roles y la ruta se resuelven server-side (hash determinístico sobre listas curadas); el pipeline nunca lee rol ni ruta del payload |
| T-159-05  | Denial of Service | Bucle de selección de STRETCHING con pool pequeño | mitigate    | La selección corta al juntar 4 o agotar el pool (índice acotado por `ordered.length`), sin bucle infinito                              |
| T-159-SC  | Tampering         | npm/pip installs                                  | accept      | Esta fase NO instala ni actualiza dependencias                                                                                         |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit` verde.
- `pnpm vitest run test/unit/stretching-selection.test.ts` verde en foreground.
- `goal-plan-pipeline.ts` sin cambios; `stage-1-rotator.ts` sin cambios.
</verification>

<success_criteria>
El tronco compartido existe: runSemanaNuevaBlockPipeline reusa stages 2-7 con ruta inyectada + formato forzado; stretching-selection es pura de (week, day) con test de determinismo verde; sin Math.random; tsc verde.
</success_criteria>

<output>
Create `.planning/phases/159-semana-nueva-backend-modos-de-d-a-generadores-roles-de-bloqu/159-02-SUMMARY.md` when done. Documentar el nombre/firma exactos de `runSemanaNuevaBlockPipeline`, `resolveRoutePool`, `selectStretchingExercises` y la ubicación de `simpleHash` compartido — el plan 03 los consume.
</output>
