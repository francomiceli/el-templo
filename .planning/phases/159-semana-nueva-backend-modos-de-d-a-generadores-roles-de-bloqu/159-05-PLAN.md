---
phase: 159-semana-nueva-backend-modos-de-d-a-generadores-roles-de-bloqu
plan: 05
type: execute
wave: 4
depends_on: [159-01, 159-03]
files_modified:
  - el-templo-api/src/modules/admin/service.ts
  - el-templo-api/test/sessions/generate-modes.test.ts
autonomous: true
requirements: [SEM-01, SEM-12, SEM-13]
must_haves:
  truths:
    - "D-03: el generador enruta por el modo elegido (combos→combos-generator, tecnica→tecnica-generator); el backend de /generate acepta el modo por día en el request"
    - "D-14: en días regulares las opciones del bloque DEUTEROS pasan de I/II a A/B en el badge del API (libera I/II para COMBOS/TECNICA); rename de labels de presentación, no de datos"
    - "POST /admin/generate con dayModes={miercoles:'combos'} genera una sesión con session_mode='combos' y roles COMBOS_I/II/STRETCHING persistidos"
    - "POST /admin/generate con dayModes que fuerza 'tecnica' enruta a generateTecnicaSession"
    - "Un dayModes con un valor fuera del enum devuelve 400 (validación del body)"
    - "combos/tecnica generan los 6 niveles en los 3 level groups (D-10)"
    - "El badge del listado usa DA/DB (o el label decidido) para DEUTEROS_1/DEUTEROS_2"
  artifacts:
    - path: "el-templo-api/src/modules/admin/service.ts"
      provides: "ruteo por modo en generateWeek (combos/tecnica, 6 niveles) + badge DEUTEROS"
      contains: "generateCombosSession"
    - path: "el-templo-api/test/sessions/generate-modes.test.ts"
      provides: "integración de /generate con dayModes + persistencia de modo/roles + 400 en modo inválido"
      contains: "combos"
  key_links:
    - from: "admin/service.ts generateWeek"
      to: "combos-generator.ts / tecnica-generator.ts"
      via: "import dinámico y ruteo por dayMode del request"
      pattern: "generateCombosSession|generateTecnicaSession"
    - from: "request.dayModes[day]"
      to: "sessions.session_mode persistido"
      via: "mode = request.dayModes[day] ?? day_modes[day] ?? 'regular'"
      pattern: "dayModes"
---

<objective>
Cablear el RUTEO por modo en `AdminService.generateWeek()`: aceptar el modo por día desde el request (`dayModes`, que pisa el default de `day_modes`), y enrutar `combos`→`generateCombosSession` / `tecnica`→`generateTecnicaSession`, ambos con los 6 niveles en los 3 level groups (D-10). Más el rename del badge DEUTEROS_1/2 (SEM-12, mitad backend). Con el test de integración real contra MySQL (SEM-06).

Purpose: es el punto donde el profe elige el tipo de sesión por día (D-02, probado por prod: el régimen alterna semana a semana). `day_modes` queda SOLO como default del ROM del sábado — NUNCA se escribe el modo del request en `day_modes` (tiene UNIQUE `(tenant_id, day_of_week)` y reinterpretaría semanas pasadas/futuras). El badge D1/D2 → DA/DB es la única mitad del rename DEUTEROS A/B que entra en la 159 (D-P5; PDF y centralización = SEM-11/fase 160).

Output: ruteo + badge + test de integración; tsc verde y el test verde en CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

REGLA OPERATIVA: branchear desde `origin/master`, releer ahí. `git add` por ruta explícita. Tests SOLO en foreground con timeout amplio (PROHIBIDO `run_in_background` y loops con `pgrep`). Los usuarios se resuelven por email (`registerUser`/`getAuthToken`), NUNCA con ids hardcodeados. El suite completo corre en CI; typecheck local sí.
</execution_context>

<context>
@.planning/phases/159-semana-nueva-backend-modos-de-d-a-generadores-roles-de-bloqu/159-RESEARCH.md
@.planning/phases/159-semana-nueva-backend-modos-de-d-a-generadores-roles-de-bloqu/159-PATTERNS.md
@.planning/phases/159-semana-nueva-backend-modos-de-d-a-generadores-roles-de-bloqu/159-01-SUMMARY.md
@.planning/phases/159-semana-nueva-backend-modos-de-d-a-generadores-roles-de-bloqu/159-03-SUMMARY.md

<interfaces>
<!-- origin/master + salidas de los planes 01/03. -->

admin/service.ts:629-699 — generateWeek(week, {days, levelGroups, regenerate}); rama ROM a extender:
const dayModeRows = await this.db.select().from(schema.dayModes);
const dayModeMap = new Map(dayModeRows.map(r => [r.dayOfWeek, r.sessionMode]));
for (const day of days) {
const dayNumber = DAY_NAME_TO_NUMBER[day];
const dayMode = dayNumber ? dayModeMap.get(dayNumber) || "regular" : "regular";
if (dayMode === "rom") { for memberLevel of ["alfa","delta"] { ...generateRomSession... } continue; }
// ... loop regular de levelGroups ...
}
import dinámico: const { generateRomSession } = await import("../sessions/rom-generator.js");

Forma recomendada del ruteo (RESEARCH Code Examples):
const requestModes = options.dayModes ?? {};
const dayMode = requestModes[day] ?? (dayNumber ? dayModeMap.get(dayNumber) ?? "regular" : "regular");
if (dayMode === "combos" || dayMode === "tecnica") { /_ 6 niveles en 3 level groups _/ continue; }

Expansión levelGroup → memberLevels (admin/service.ts:719-724):
alfa_delta -> ["alfa","delta","kairos"]; sigma -> ["sigma"]; omega -> ["omega","spartan"]

Manejo de error por dayId (admin/service.ts:692-696): catch (err: unknown) { failed++; warnings.push(`${dayId} (MODE): ${msg}`) }

Badge DEUTEROS (admin/service.ts:166-177) — único rename de la 159:
let label = b.role.charAt(0);
if (b.role === "DEUTEROS_1") label = "D1"; else if (b.role === "DEUTEROS_2") label = "D2";
// → cambiar a DA/DB (o el label acordado). Los literales 'DEUTEROS I'/'II' del PDF NO son de esta fase (SEM-11).

Firmas de los generadores (del plan 03, ver 159-03-SUMMARY):
generateCombosSession(db, week, day, levelGroup, memberLevel): Promise<DaySession>
generateTecnicaSession(db, week, day, levelGroup, memberLevel): Promise<DaySession>

Test de integración molde — test/sessions/sessions.test.ts:1-56:
createTestApp, registerUser, getAuthToken; app.inject({method, url, headers, payload})
el endpoint es POST /admin/generate detrás de canAccessTraining (usar un usuario con rol adecuado).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ruteo por modo en generateWeek + badge DEUTEROS</name>
  <read_first>
    - el-templo-api/src/modules/admin/service.ts (generateWeek :629-699; rama ROM; expansión de niveles :719-724; badge :166-177)
    - el-templo-api/src/modules/sessions/combos-generator.ts y tecnica-generator.ts (firmas, del plan 03)
    - .planning/phases/159-.../159-03-SUMMARY.md (firmas exactas de los generadores)
  </read_first>
  <action>
    En `generateWeek`: leer `const requestModes = options.dayModes ?? {}` y resolver el modo por día como `requestModes[day] ?? (dayNumber ? dayModeMap.get(dayNumber) ?? "regular" : "regular")`. Mantener `days: string[]` tal cual (NO convertir en array de objetos). Actualizar la firma de `options` para aceptar `dayModes?: Record<string, string>` (o el tipo del schema del plan 01).
    Agregar la rama `if (dayMode === "combos" || dayMode === "tecnica")`: iterar los `levelGroups` y expandir a memberLevels (D-10, patrón :719-724), y por cada nivel construir `dayId = `W${week}-${day}-${memberLevel}``, respetar skip/regenerate igual que la rama ROM, e invocar `generateCombosSession` o `generateTecnicaSession` según el modo (import dinámico con extensión `.js`, igual que rom-generator). `await sessionService.saveSession(session)`. `continue` para saltar el loop regular. NUNCA escribir el modo del request en `day_modes` (D-02 lo prohíbe: reinterpretaría semanas pasadas/futuras).
    Manejo de error por dayId con `catch (err: unknown)` + `instanceof Error`, acumulando en `warnings` (`${dayId} (COMBOS/TECNICA): ${msg}`), sin abortar la semana.
    Badge DEUTEROS (:166-177): cambiar `label = "D1"`/`"D2"` a `"DA"`/`"DB"` (rename A/B, mitad backend de SEM-12; D-P5). NO tocar los literales `'DEUTEROS I'`/`'DEUTEROS II'` del PDF (SEM-11, fase 160).
    Nunca `console.log`; usar el logger Pino. Sin `any`.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "generateCombosSession\|generateTecnicaSession" el-templo-api/src/modules/admin/service.ts` >= 2 (ambos generadores enrutados).
    - La rama combos/tecnica expande a 6 niveles (grep de la expansión `["alfa","delta","kairos"]` reutilizada o compartida).
    - `generateWeek` NO escribe en `schema.dayModes` (grep `grep -n "insert\|update" ...dayModes` sin escrituras nuevas).
    - El badge usa `"DA"`/`"DB"` para DEUTEROS_1/DEUTEROS_2.
    - `pnpm exec tsc --noEmit` en 0 errores.
  </acceptance_criteria>
  <done>generateWeek enruta combos/tecnica por el modo del request con los 6 niveles, sin escribir day_modes; el badge DEUTEROS es DA/DB; tsc verde.</done>
</task>

<task type="auto">
  <name>Task 2: test de integración de /generate (SEM-01, SEM-12, SEM-13a, D-10)</name>
  <read_first>
    - el-templo-api/test/sessions/sessions.test.ts:1-56 (bootstrap con helpers reales)
    - el-templo-api/test/helpers.ts (createTestApp, registerUser, getAuthToken, cleanAllTestData)
    - el-templo-api/src/modules/admin/routes.ts (POST /admin/generate detrás de canAccessTraining — el test necesita un usuario con acceso de entrenamiento)
  </read_first>
  <behavior>
    - Test 1: `POST /admin/generate` con `{ week, days:["miercoles"], dayModes:{miercoles:"combos"}, levelGroups:[...] }` responde OK y persiste sesiones con `session_mode='combos'` y bloques con roles COMBOS_I, COMBOS_II, STRETCHING (leer de `sessions`/`session_blocks`).
    - Test 2: con `dayModes:{jueves:"tecnica"}` persiste `session_mode='tecnica'` con TECNICA_I, TECNICA_II, STRETCHING.
    - Test 3: los roles nuevos persisten en `session_blocks.role` sin truncar (varchar(20)).
    - Test 4: `dayModes:{miercoles:"noexiste"}` devuelve 400 (el enum del body rechaza el modo inválido — la columna varchar no valida, el schema es la única defensa).
    - Test 5 (D-10): una generación combos produce los 6 niveles en los 3 level groups.
    - Test 6 (regresión): `dayModes` ausente sigue generando `regular` como antes.
  </behavior>
  <action>
    Crear `test/sessions/generate-modes.test.ts` sobre el molde de `sessions.test.ts`, contra MySQL real (`eltemplo_test`), usando `createTestApp` + `registerUser` + `getAuthToken` (usuarios por email, NUNCA ids hardcodeados). Cubrir los 6 comportamientos. Para verificar la persistencia, consultar `sessions` y `session_blocks` con Drizzle tras el POST. Para el Test 4, assertear `res.statusCode === 400`. Limpiar datos entre casos con los helpers (`cleanAllTestData` si aplica).
    Este test corre en CI (regla del proyecto: no correr el suite completo local). El ejecutor puede correr SOLO este archivo en foreground con timeout amplio para validar localmente si tiene MySQL de test; si no, dejar el gate a CI.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm vitest run test/sessions/generate-modes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test/sessions/generate-modes.test.ts` existe con los 6 casos.
    - El caso de modo inválido asserta `statusCode === 400`.
    - Los casos combos/tecnica assertean `session_mode` y los roles persistidos leyendo la DB.
    - El test NO hardcodea ids de `users` (resuelve por email).
    - El test corre SOLO en foreground (sin `run_in_background`, sin loops de `pgrep`).
  </acceptance_criteria>
  <done>Test de integración de /generate cubre ruteo por modo, persistencia de modo/roles, 6 niveles y 400 en modo inválido; verde en CI.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                                            |
| -------------------------------------- | ---------------------------------------------------------------------- |
| cliente (admin) → POST /admin/generate | `dayModes` decide qué session_mode se persiste (columna varchar libre) |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                               | Disposition | Mitigation Plan                                                                                                                                                                                                        |
| --------- | ---------------------- | --------------------------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-159-11  | Tampering              | `session_mode` arbitrario vía dayModes                                                  | mitigate    | El enum del body (plan 01) rechaza modos no reconocidos; el test 4 asserta 400. El ruteo solo reconoce regular/rom/combos/tecnica; cualquier otro cae a regular por el `?? "regular"` pero el body ya lo rechazó antes |
| T-159-12  | Tampering              | Escritura del modo del request en `day_modes` (reinterpretaría semanas pasadas/futuras) | mitigate    | `generateWeek` NUNCA escribe en `day_modes`; el request solo pisa el modo en memoria para esa generación                                                                                                               |
| T-159-13  | Elevation of Privilege | Acceso a `/generate`                                                                    | accept      | Heredado de `canAccessTraining`; sin líneas de auth nuevas; sin rutas HTTP nuevas (ISO-01 = 370 intacto)                                                                                                               |
| T-159-SC  | Tampering              | npm/pip installs                                                                        | accept      | Esta fase NO instala ni actualiza dependencias                                                                                                                                                                         |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit` verde.
- `pnpm vitest run test/sessions/generate-modes.test.ts` verde (CI / foreground).
- Regresión: `regular`/`rom` generan igual que antes (test 6).
- ISO-01 intacto: no se agregaron rutas HTTP.
</verification>

<success_criteria>
generateWeek enruta combos/tecnica por el modo del request con los 6 niveles sin tocar day_modes; el badge DEUTEROS es DA/DB; el test de integración prueba persistencia de modo/roles y 400 en modo inválido; tsc verde.
</success_criteria>

<output>
Create `.planning/phases/159-semana-nueva-backend-modos-de-d-a-generadores-roles-de-bloqu/159-05-SUMMARY.md` when done.
</output>
