---
phase: 143-profesor-por-clase-puntuaci-n-post-clase-presencial
plan: 02
type: execute
wave: 2
depends_on: [143-01]
files_modified:
  - el-templo-api/src/modules/ratings/service.ts
  - el-templo-api/src/modules/ratings/routes.ts
  - el-templo-api/src/modules/ratings/schemas.ts
  - el-templo-api/src/modules/ratings/types.ts
  - el-templo-api/src/modules/ratings/index.ts
  - el-templo-api/src/app.ts
  - el-templo-api/test/ratings/ratings.test.ts
autonomous: true
requirements: [PROF-ROSTER, PROF-RATING, PROF-OWNERVIEW]
must_haves:
  truths:
    - "El owner asigna un profe a (sucursal, semana, día, turno) y se persiste de inmediato"
    - "Al puntuar, el coachId se resuelve del roster por (branchId, sessionDate→dayOfWeek, slot por startTime<12:00)"
    - "Si no hay profe asignado a ese slot, no se acepta el rating (sin huérfanos)"
    - "Un miembro no puede puntuar dos veces la misma clase (one-shot server-side)"
    - "El endpoint pending devuelve solo la última clase presencial sin puntuar dentro de 48h con profe asignado (D-P3/D-P4 server-side)"
    - "Solo el owner lee las puntuaciones; el coach no ve nada (ni su promedio)"
    - "El coach solo puede asignarse roster dentro de sus sucursales (user_branches)"
  artifacts:
    - path: "el-templo-api/src/modules/ratings/service.ts"
      provides: "RatingsService con upsertRosterAssignment, getCoachesForBranch, getPendingRating, submitRating (atribución), getOwnerRatings"
      contains: "class RatingsService"
    - path: "el-templo-api/src/modules/ratings/routes.ts"
      provides: "Plugins admin (roster + owner view) y member (pending + submit)"
      exports: ["ratingsAdminRoutes", "ratingsMemberRoutes"]
    - path: "el-templo-api/test/ratings/ratings.test.ts"
      provides: "Tests de atribución, pending, one-shot, no-coach, owner-only, average"
      contains: "createTestApp"
  key_links:
    - from: "el-templo-api/src/app.ts"
      to: "ratingsAdminRoutes + ratingsMemberRoutes"
      via: "app.register con prefix /api/admin/ratings y /api/members/ratings"
      pattern: "register\\(ratings(Admin|Member)Routes"
    - from: "el-templo-api/src/modules/ratings/service.ts"
      to: "schema.classCoachAssignments"
      via: "query de atribución por (branchId, weekStartDate, dayOfWeek, slot)"
      pattern: "classCoachAssignments"
---

<objective>
Construir el módulo API `ratings` con los tres flujos desacoplados de la fase: (1) escritura del **roster** (owner asigna profe por slot/semana, D-A1) + listado de coaches por sucursal; (2) **pending + submit del rating** del miembro — `pending` devuelve la última clase presencial sin puntuar dentro de 48h con profe asignado (D-P3/D-P4 server-side), y `submit` resuelve el `coachId` del roster por atribución determinística (D-Q1) con guardas one-shot (D-P2) y no-orphan (D-Q3); (3) **vista del owner** (promedio por profe + recientes, D-O1, owner-only D-M3). Más tests de integración.

Purpose: es el corazón backend de la fase. El roster es la única fuente de atribución; las reglas del pop-up (48h, última clase, one-shot) se enforzan server-side (no solo cliente); la vista respeta el aislamiento de privacidad (coach no ve nada).
Output: módulo `ratings` completo registrado en app.ts + suite de tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/143-profesor-por-clase-puntuaci-n-post-clase-presencial/143-CONTEXT.md
@.planning/phases/143-profesor-por-clase-puntuaci-n-post-clase-presencial/143-PATTERNS.md
@.planning/phases/143-profesor-por-clase-puntuaci-n-post-clase-presencial/143-01-SUMMARY.md

<interfaces>
<!-- Contracts del Plan 01 (ya creados) + analogs del módulo coach/attendance. -->

schema (Plan 01):
classCoachAssignments: { id, branchId, weekStartDate(string), dayOfWeek(tinyint), slot('morning'|'afternoon'), coachId, createdAt, updatedAt }
uniqueIndex (branchId, weekStartDate, dayOfWeek, slot)
coachRatings: { id, coachId, memberId, branchId, scheduleId?, sessionDate(string), stars(tinyint 1-5), comment?(<=500), createdAt }

attendance (existente): { memberId, branchId, scheduleId?, sessionDate(string), checkedInAt, status:'confirmado', source:'qr'|'manual' }
→ señal de "clase presencial completada"; join a schedules→activities para actividad/startTime (turno) del slot.

Service skeleton (coach/service.ts):
import { MySql2Database } from "drizzle-orm/mysql2"
import { eq, and, sql, desc, type SQL } from "drizzle-orm"
import \* as schema from "../../db/schema"
export class RatingsService { constructor(private readonly db: MySql2Database<typeof schema>) {} }
// promedio: .groupBy(...) + sql<string>`AVG(${schema.coachRatings.stars})` — prefijar columnas (reference_drizzle_select_unqualified_columns)

Errores: BadRequestError de "../shared/errors" (NO Error crudo). Routes los convierten con handleServiceError de "../shared/error-handler".

Permissions (shared/permissions.ts):
OWNER_ROLES = ["owner"] // vista de ratings (D-M3, D-O1)
TRAINING_ROLES = ["coach","owner"] // editar Horarios → escribir roster
Guard (coach/routes.ts l.24-33): onRequest → authenticate → role check 403 → attachCountryScope.
Member plugin (attendance routes l.193-212): onRequest sólo authenticate; GET/POST<{...}> → service.method(request.user.userId, ...) → reply.

Branch-scoped write (scheduling routes l.199-203): preHandler:[requireBranchAccess({from:"body.branchId"})] de "../shared/branch-access".

Turno (D-A1, LOCKED): slot = startTime < "12:00" ? "morning" : "afternoon" (mismo split que ReservasPage morningSlots/afternoonSlots, comparación string HH:MM).
Date helpers: "../shared/date-utils" (todayInTz, getWeekRange) tz-aware; usar para sessionDate→dayOfWeek y semana (lunes ISO), NO Date JS crudo.

app.ts l.202-205 (analog de registro):
await app.register(ratingsAdminRoutes, { prefix: "/api/admin/ratings" })
await app.register(ratingsMemberRoutes, { prefix: "/api/members/ratings" })

Tests (test/coach/outstanding-balances.test.ts l.16-69): createTestApp, cleanAllTestData, createStaffUser, getAuthToken, registerUser; seed via app.db.insert(schema.X).values({...}).$returningId().
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear módulo ratings (service + types + schemas + routes + index) y registrarlo en app.ts</name>
  <read_first>
    - el-templo-api/src/modules/coach/service.ts (analog: clase service, scope, AVG/groupBy, columnas prefijadas)
    - el-templo-api/src/modules/coach/routes.ts (analog: guard onRequest + role check + attachCountryScope)
    - el-templo-api/src/modules/attendance/routes.ts (analog: split admin/member plugin, member plugin l.193-212, handleServiceError)
    - el-templo-api/src/modules/attendance/service.ts (analog: join attendance→schedules→activities, helpers tz, BadRequestError)
    - el-templo-api/src/modules/coach/schemas.ts y coach/types.ts y coach/index.ts (analogs)
    - el-templo-api/src/modules/shared/branch-access.ts (requireBranchAccess)
    - el-templo-api/src/modules/shared/permissions.ts (OWNER_ROLES, TRAINING_ROLES)
    - el-templo-api/src/modules/shared/date-utils.ts (todayInTz, getWeekRange — leer firmas)
    - el-templo-api/src/app.ts (l.160-220, bloque de registro)
    - el-templo-api/src/db/schema/class-coach-assignments.ts y coach-ratings.ts (Plan 01)
    - .planning/phases/143-.../143-PATTERNS.md (secciones service.ts / routes.ts / schemas.ts)
    - .planning/phases/143-.../143-CONTEXT.md (D-A1, D-M3, D-O1, D-P2, D-P3, D-P4, D-Q1, D-Q3)
  </read_first>
  <action>
    Crear directorio `el-templo-api/src/modules/ratings/` con:

    `types.ts` — interfaces TS (sin `any`): `RosterAssignmentInput { branchId; weekStartDate; dayOfWeek; slot: "morning"|"afternoon"; coachId }`, `RosterWeekRow`, `CoachOption { id; firstName; lastName }`, `PendingRating { sessionDate: string; branchId: number; scheduleId: number; activityName: string; dayOfWeek: number } | null` (lo que el pop-up necesita: clase a puntuar SIN datos del profe, D-A3), `SubmitRatingInput { sessionDate: string; scheduleId: number; stars: number; comment?: string }`, `OwnerRatingsResult { perCoach: {...}[]; recent: {...}[] }`.

    `schemas.ts` — Fastify JSON schemas (forma de coach/schemas.ts): `rosterWeekQuerySchema` (querystring branchId+weekStart), `assignCoachBodySchema` (body branchId/weekStartDate/dayOfWeek/slot enum/coachId), `coachesForBranchQuerySchema`, `submitRatingBodySchema` (body `sessionDate`, `scheduleId`, `stars: {type:"integer", minimum:1, maximum:5}`, `comment: {type:"string", maxLength:500}` opcional), `pendingRatingSchema` (sin params, response shape).

    `service.ts` — `export class RatingsService` (constructor `db: MySql2Database<typeof schema>`). Métodos:
    - `getCoachesForBranch(branchId)`: select users role='coach' que tengan fila en `user_branches` para esa sucursal; devuelve `{id, firstName, lastName}`.
    - `getRosterWeek(branchId, weekStartDate)`: select de `class_coach_assignments` para esa semana/sucursal con nombre del coach (join users); devuelve filas por (dayOfWeek, slot).
    - `upsertRosterAssignment(input)`: persiste inmediato (UI sin botón Save). Insert con `ON DUPLICATE KEY UPDATE coach_id=...` sobre la unique natural-key, o select-then-insert/update. Throw `BadRequestError` si el coach no pertenece a la sucursal (validar contra user_branches) o si dayOfWeek/slot fuera de rango.
    - `getPendingRating(memberId)` (D-P3/D-P4, server-side): buscar la asistencia presencial CONFIRMADA más reciente del miembro (`attendance` join `schedules`→`activities`) cuya `checkedInAt`/`sessionDate` esté dentro de las **últimas 48h** (D-P3, ventana tz-aware) y que (a) NO tenga ya una fila en `coach_ratings` para `(memberId, sessionDate, scheduleId)` (one-shot D-P2) y (b) tenga un profe asignado en el roster para esa clase (resolver `(branchId, weekStartDate, dayOfWeek, slot)` — si no hay roster, NO devolver, D-Q3). Ordenar `desc` por checkedInAt y devolver **solo la última** (D-P4, sin cola). Devolver `PendingRating | null` SIN exponer coachId/nombre/foto (D-A3): solo `{sessionDate, branchId, scheduleId, activityName, dayOfWeek}`.
    - `submitRating(memberId, input)`: resuelve `coachId` consultando `class_coach_assignments` por `(branchId, weekStartDate(lunes ISO de sessionDate), dayOfWeek(de sessionDate, tz-aware), slot(startTime del schedule<"12:00"?"morning":"afternoon"))`. Si NO hay fila → throw `BadRequestError("No hay profe asignado a esta clase")` (D-Q3). Guard one-shot (D-P2): si ya existe fila en `coach_ratings` para `(memberId, sessionDate, scheduleId)` → throw `BadRequestError("Ya puntuaste esta clase")`. Validar que el miembro realmente asistió (existe `attendance` confirmado del memberId en esa sessionDate/branch/schedule) antes de insertar — defensa server-side. Validar ventana 48h también en submit (no confiar en el cliente). Insert en `coach_ratings`.
    - `getOwnerRatings(scope)`: por-coach `AVG(coachRatings.stars)` + `COUNT(*)` con `.groupBy(coachRatings.coachId, users.firstName, users.lastName)` (columnas prefijadas, reference_drizzle_select_unqualified_columns) + lista de recientes (stars, comment, sessionDate, activity/day, coach). Respetar scope de país (igual que coach/service: owner sin restricción).

    `routes.ts` — DOS plugins exportados:
    - `ratingsAdminRoutes`: guard onRequest → authenticate → role check (TRAINING_ROLES para escribir roster / leer coaches; la vista del owner usa OWNER_ROLES — gate por-ruta o segundo check dentro del handler) → attachCountryScope. Rutas: `GET /coaches?branchId` (TRAINING_ROLES), `GET /roster?branchId&weekStart` (TRAINING_ROLES), `POST /roster` con `preHandler:[requireBranchAccess({from:"body.branchId"})]` (coach solo asigna en sus sucursales), `GET /` (lista owner-only — verificar `request.user.role==="owner"` y 403 si no). Cada handler: `try { ... return reply.code(...).send(...) } catch (err:unknown) { handleServiceError(err, reply, request.log, "label") }`.
    - `ratingsMemberRoutes`: guard onRequest → solo authenticate. `GET /pending` → `ratingsService.getPendingRating(request.user.userId)` → reply.send(result). `POST /` → `ratingsService.submitRating(request.user.userId, request.body)` → reply.code(201).

    `index.ts` — `export * from "./routes";`.

    `app.ts` — importar los plugins y registrar: `await app.register(ratingsAdminRoutes, { prefix: "/api/admin/ratings" })` y `await app.register(ratingsMemberRoutes, { prefix: "/api/members/ratings" })`, junto al bloque coach/attendance (l.202-205).

    Logging: Pino (`request.log`), nunca console.log. Sin `any`; `catch (err: unknown)` + narrowing.

  </action>
  <verify>
    <automated>cd el-templo-api && pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class RatingsService" el-templo-api/src/modules/ratings/service.ts` = 1.
    - service.ts contiene los 6 métodos: `getCoachesForBranch`, `getRosterWeek`, `upsertRosterAssignment`, `getPendingRating`, `submitRating`, `getOwnerRatings`.
    - `getPendingRating` NO devuelve coachId/nombre del profe (D-A3): `grep -A20 "getPendingRating" el-templo-api/src/modules/ratings/service.ts | grep -c "coachId\|coachName\|photoUrl"` = 0 en el shape de retorno.
    - `submitRating` referencia `schema.classCoachAssignments` (atribución) y lanza `BadRequestError` cuando no hay roster (no-orphan, D-Q3).
    - routes.ts exporta `ratingsAdminRoutes` y `ratingsMemberRoutes`; el member plugin tiene `GET /pending` y `POST /`; el POST /roster usa `requireBranchAccess({from:"body.branchId"})`; la ruta de lista owner-only verifica `owner`.
    - `grep -c "register(ratingsAdminRoutes" el-templo-api/src/app.ts` = 1 y lo mismo para member, con prefijos `/api/admin/ratings` y `/api/members/ratings`.
    - `submitRatingBodySchema` define `stars` integer min 1 max 5 y `comment` maxLength 500.
    - `pnpm typecheck` y `pnpm lint` pasan.
    - Ningún `console.` ni `: any` en los archivos nuevos: `grep -rn "console\.\|: any" el-templo-api/src/modules/ratings/` no devuelve nada.
  </acceptance_criteria>
  <done>Módulo ratings completo (incluye pending), tipado, registrado en app.ts; typecheck + lint verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests de integración del módulo ratings</name>
  <read_first>
    - el-templo-api/test/coach/outstanding-balances.test.ts (analog: harness, seed, getAuthToken)
    - el-templo-api/test/helpers.ts (createTestApp, cleanAllTestData, createStaffUser, getAuthToken, registerUser)
    - el-templo-api/src/modules/ratings/service.ts y routes.ts (recién creados)
    - .planning/phases/143-.../143-PATTERNS.md (sección test/ratings)
    - .planning/phases/143-.../143-CONTEXT.md (D-P2, D-P3, D-P4, D-Q3, D-M3, D-O1)
  </read_first>
  <behavior>
    - Roster upsert: asignar un coach a (branch, week, day, slot) persiste; reasignar el mismo slot reemplaza (no duplica) por la unique key.
    - Atribución: un rating con sessionDate resuelve el coachId del roster vigente esa semana por (dayOfWeek, slot derivado de startTime<12:00). Cambiar el roster de OTRA semana no afecta la atribución de una clase pasada.
    - Pending (D-P3/D-P4): con asistencia confirmada hace <48h y profe asignado → pending devuelve esa clase (sin coachId). Asistencia hace >48h → pending null. Si hay 2 sin puntuar → devuelve solo la última. Si ya fue puntuada → pending null (D-P2). Si no hay profe en el roster → pending null (D-Q3).
    - One-shot (D-P2): segundo submit del mismo miembro para la misma clase → 400 "Ya puntuaste".
    - No-coach (D-Q3): submit para un slot sin roster asignado → 400, no se inserta fila en coach_ratings.
    - Owner-only (D-M3): GET de la vista de ratings con token de member → 401/403; con coach → 403; con owner → 200.
    - Branch scope: coach intentando asignar roster en sucursal fuera de su user_branches → 403 (BRANCH_OUT_OF_SCOPE).
    - Average (D-O1): tras N ratings, getOwnerRatings devuelve el AVG correcto por coach y el count.
  </behavior>
  <action>
    Crear `el-templo-api/test/ratings/ratings.test.ts` (Vitest contra la DB de test por worker). Copiar el harness de `test/coach/outstanding-balances.test.ts`: `import { createTestApp, cleanAllTestData, createStaffUser, getAuthToken, registerUser } from "../helpers"`, `import * as schema from "../../src/db/schema"`. Seed de branches/coach (con fila user_branches)/member/schedule/attendance vía `app.db.insert(...).$returningId()`. Cubrir los casos del bloque <behavior> con `app.inject(...)` golpeando `/api/admin/ratings/*` y `/api/members/ratings` (`GET /pending`, `POST /`). NO ejecutar el suite completo localmente (corre en CI al pushear); typecheck local sí.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Existe `el-templo-api/test/ratings/ratings.test.ts` con tests para: atribución, pending (48h + última + ya-puntuada + sin-profe), one-shot (D-P2), no-coach (D-Q3), owner-only 403 (D-M3), branch-scope 403, average (D-O1).
    - `grep -c "createTestApp" el-templo-api/test/ratings/ratings.test.ts` ≥ 1.
    - `grep -c "it(\|test(" el-templo-api/test/ratings/ratings.test.ts` ≥ 8.
    - `cd el-templo-api && pnpm typecheck` pasa (el suite corre en CI, no localmente).
  </acceptance_criteria>
  <done>Suite de tests cubre atribución + pending + las guardas de seguridad + owner-only + average; typecheck verde.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                     |
| --------------------------------- | --------------------------------------------------------------- |
| member app → /api/members/ratings | input no confiable del miembro (stars, comment, clase)          |
| admin web → /api/admin/ratings    | escritura del roster (coach/owner) + lectura de ratings (owner) |
| coach → roster write              | coach solo dentro de sus sucursales                             |

## STRIDE Threat Register

| Threat ID | Category               | Component                                          | Disposition | Mitigation Plan                                                                                                                      |
| --------- | ---------------------- | -------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| T-143-04  | Information Disclosure | GET /api/admin/ratings (vista owner)               | mitigate    | role check `owner` server-side (D-M3); test owner-only 403 para member/coach. El coach nunca ve promedios ni individuales            |
| T-143-05  | Elevation of Privilege | POST /api/admin/ratings/roster                     | mitigate    | `requireBranchAccess({from:"body.branchId"})` valida coach vs user_branches; test branch-scope 403                                   |
| T-143-06  | Integrity              | submitRating doble voto                            | mitigate    | guard one-shot server-side sobre (memberId, sessionDate, scheduleId) (D-P2); test                                                    |
| T-143-07  | Integrity              | rating huérfano (sin profe)                        | mitigate    | submitRating y pending rechazan si no hay fila de roster (D-Q3); test                                                                |
| T-143-08  | Spoofing               | miembro puntúa clase que no asistió o fuera de 48h | mitigate    | submitRating exige attendance confirmado del memberId para esa sessionDate/branch/schedule + revalida ventana 48h server-side (D-P3) |
| T-143-09  | Tampering              | stars fuera de 1–5 / comment > 500                 | mitigate    | JSON schema body (`minimum:1, maximum:5`, `maxLength:500`) + validación service                                                      |
| T-143-16  | Information Disclosure | pending filtra identidad del profe                 | mitigate    | getPendingRating devuelve solo actividad/día/clase, nunca coachId/nombre/foto (D-A3); criterio de aceptación grep                    |

</threat_model>

<verification>
- `pnpm typecheck` + `pnpm lint` verdes en el-templo-api.
- Endpoints registrados bajo `/api/admin/ratings` y `/api/members/ratings` (incl. `GET /pending`).
- Tests de integración presentes cubriendo atribución, pending, one-shot, no-coach, owner-only, branch-scope y average.
</verification>

<success_criteria>
El owner asigna roster (persistencia inmediata, scope por sucursal), el `pending` devuelve solo la última clase sin puntuar dentro de 48h con profe (sin exponer al profe), el `submit` resuelve el coach por atribución determinística con guardas one-shot/no-orphan/asistencia/48h, y la vista de ratings es owner-only con promedio por profe — todo cubierto por tests.
</success_criteria>

<output>
Crear `.planning/phases/143-profesor-por-clase-puntuaci-n-post-clase-presencial/143-02-SUMMARY.md` al terminar.
</output>
