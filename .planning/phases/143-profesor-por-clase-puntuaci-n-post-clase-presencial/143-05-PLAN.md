---
phase: 143-profesor-por-clase-puntuaci-n-post-clase-presencial
plan: 05
type: execute
wave: 3
depends_on: [143-02]
files_modified:
  - el-templo-app/src/composables/useRatingsApi.ts
  - el-templo-app/src/components/RatingPromptDialog.vue
  - el-templo-app/src/layouts/MainLayout.vue
autonomous: true
requirements: [PROF-RATING]
must_haves:
  truths:
    - "Al volver a la app tras una clase presencial completada con profe asignado, el miembro ve un pop-up para puntuar la clase"
    - "El pop-up es salteable; si se saltea, esa clase no se vuelve a pedir (one-shot)"
    - "El pop-up nunca muestra nada del profe (ni nombre ni foto); se arma alrededor de la clase (actividad/día)"
    - "El miembro puntúa 1-5 estrellas (Terracotta) + comentario opcional y se persiste vía el endpoint de submit"
  artifacts:
    - path: "el-templo-app/src/composables/useRatingsApi.ts"
      provides: "Cliente API member: getPendingRating, submitRating, cleanup()"
      contains: "useRatingsApi"
    - path: "el-templo-app/src/components/RatingPromptDialog.vue"
      provides: "Pop-up de puntuación auto-disparado (Surface 2)"
      contains: "Enviar puntuación"
  key_links:
    - from: "el-templo-app/src/components/RatingPromptDialog.vue"
      to: "useRatingsApi.getPendingRating + submitRating"
      via: "shouldShow() consume pending; CTA llama submit"
      pattern: "getPendingRating|submitRating"
    - from: "el-templo-app/src/layouts/MainLayout.vue"
      to: "RatingPromptDialog"
      via: "montaje del componente junto a PushPermissionDialog"
      pattern: "RatingPromptDialog"
---

<objective>
Construir la **Surface 2**: el pop-up de puntuación en la app del miembro (`RatingPromptDialog.vue`), auto-disparado al volver a la app tras una clase presencial completada con profe asignado. Estilo Uber: estrellas 1–5 Terracotta + comentario opcional, **class-framed** (nunca muestra al profe, D-A3), **salteable** (D-P1) y **one-shot** (D-P2). El composable `useRatingsApi` (member) consume `GET /pending` (que ya filtra 48h/última/sin-profe server-side, Plan 02) y postea el rating. Montaje en `MainLayout`.

Purpose: cerrar el loop del feedback del miembro respetando el contrato visual aprobado (143-UI-SPEC Surface 2) y las reglas de privacidad. La lógica pesada (qué clase, 48h, sin-profe) vive en el server; el dialog solo presenta lo que `pending` devuelve y marca la clase como resuelta al saltear/enviar.
Output: composable + RatingPromptDialog + montaje en MainLayout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/143-profesor-por-clase-puntuaci-n-post-clase-presencial/143-CONTEXT.md
@.planning/phases/143-profesor-por-clase-puntuaci-n-post-clase-presencial/143-PATTERNS.md
@.planning/phases/143-profesor-por-clase-puntuaci-n-post-clase-presencial/143-UI-SPEC.md
@.planning/phases/143-profesor-por-clase-puntuaci-n-post-clase-presencial/143-02-SUMMARY.md

<interfaces>
<!-- Endpoints del Plan 02 + analogs de la app del miembro. -->

API (Plan 02):
GET /api/members/ratings/pending → PendingRating | null
PendingRating = { sessionDate, branchId, scheduleId, activityName, dayOfWeek } // SIN datos del profe (D-A3)
POST /api/members/ratings body { sessionDate, scheduleId, stars(1-5), comment? } → 201

useAttendanceApi.ts (analog del composable member):
import { api } from 'src/boot/axios'
export interface X {...}
export function useAttendanceApi() { async checkIn(...){...} ; function cleanup(){/_ no-op _/} ; return { checkIn, getHistory, cleanup } }
// CLAUDE.md: composables exponen cleanup(), sin onUnmounted dentro

PushPermissionDialog.vue (analog ESTRUCTURAL exacto de Surface 2):
<q-dialog v-model="show" persistent> → AQUÍ persistent SE QUITA (D-P1 salteable, UI-SPEC persistent="false")

  <script setup>: show ref, requesting→submitting ref, shouldShow() gate, evaluate(), watch(authStore.isAuthenticated, ..., {immediate:true})
  one-shot via Preferences (ASKED_KEY versionado) → aquí keyear POR CLASE (sessionDate+scheduleId) para marcar resuelta
  createLogger('PushPermissionDialog') → createLogger('RatingPromptDialog')
  err instanceof Error handling

MainLayout.vue:
  l.135 <PushPermissionDialog /> dentro del template (q-footer cierra antes)
  l.146 import PushPermissionDialog from 'src/components/PushPermissionDialog.vue'
  → agregar <RatingPromptDialog /> al lado + su import

UI-SPEC Surface 2 (LOCKED):
  QDialog persistent=false (centrado, QCard, patrón PushPermissionDialog)
  Title: "¿Cómo estuvo tu clase de {Actividad}?" (activityName + día, sin profe)
  QRating size="2.5em" color="primary" icon="star" icon-selected="star" empty=star_border, sin medias estrellas; CTA disabled hasta ≥1 estrella
  QInput type="textarea" autogrow max ~280, label "Comentario (opcional)" (siempre, D-M2)
  Helper caption "Tu opinión es anónima y nos ayuda a mejorar las clases."
  CTA primario color="primary" "Enviar puntuación"; secundario flat "Ahora no" (cierra + marca resuelta)
  submitting → CTA loading; success → cerrar + toast positivo "¡Gracias por tu puntuación!"; error → mantener abierto + toast negativo "No pudimos guardar tu puntuación. Probá de nuevo en un momento."
  Estrellas Terracotta vía color="primary"/$primary SCSS — NUNCA hex hardcodeado.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Composable useRatingsApi (member)</name>
  <read_first>
    - el-templo-app/src/composables/useAttendanceApi.ts (analog: forma lean + cleanup() no-op)
    - .planning/phases/143-.../143-02-SUMMARY.md (shape de PendingRating + submit)
    - .planning/phases/143-.../143-PATTERNS.md (sección useRatingsApi member)
    - CLAUDE.md (composables exponen cleanup(), sin onUnmounted dentro; sin any; sin console)
  </read_first>
  <action>
    Crear `el-templo-app/src/composables/useRatingsApi.ts` copiando la forma de `useAttendanceApi.ts`: `import { api } from 'src/boot/axios'`, interfaces exportadas (sin `any`): `PendingRating { sessionDate: string; branchId: number; scheduleId: number; activityName: string; dayOfWeek: number }`, `SubmitRatingInput { sessionDate: string; scheduleId: number; stars: number; comment?: string }`. Métodos: `getPendingRating(): Promise<PendingRating | null>` → `await api.get('/members/ratings/pending')` (devuelve `data` que puede ser null), `submitRating(input): Promise<void>` → `await api.post('/members/ratings', input)`. Exponer `cleanup()` no-op (convención CLAUDE.md). Sin `console.log`; `catch (err: unknown)` donde aplique.
  </action>
  <verify>
    <automated>cd el-templo-app && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `el-templo-app/src/composables/useRatingsApi.ts` exporta `useRatingsApi` con `getPendingRating`, `submitRating`, `cleanup`.
    - `PendingRating` NO incluye campo de profe (sin `coachId`/`coachName`/`photoUrl`): `grep -c "coachId\|coachName\|photoUrl" el-templo-app/src/composables/useRatingsApi.ts` = 0.
    - Golpea `/members/ratings/pending` y `/members/ratings`.
    - `cd el-templo-app && pnpm typecheck` pasa. Sin `console.` ni `: any`.
  </acceptance_criteria>
  <done>Composable member creado con pending+submit+cleanup; typecheck verde.</done>
</task>

<task type="auto">
  <name>Task 2: RatingPromptDialog.vue (Surface 2) + montaje en MainLayout</name>
  <read_first>
    - el-templo-app/src/components/PushPermissionDialog.vue (analog estructural completo: dialog, script setup, shouldShow, watch isAuthenticated, Preferences one-shot, estilos)
    - el-templo-app/src/composables/useRatingsApi.ts (creado en Task 1)
    - el-templo-app/src/layouts/MainLayout.vue (l.134-146: montaje + import de PushPermissionDialog)
    - .planning/phases/143-.../143-UI-SPEC.md (Surface 2 + Copywriting + Color + States)
    - .planning/phases/143-.../143-CONTEXT.md (D-P1, D-P2, D-A3, D-M2)
  </read_first>
  <action>
    Crear `el-templo-app/src/components/RatingPromptDialog.vue` copiando el patrón de `PushPermissionDialog.vue`:
    - Template: `<q-dialog v-model="show">` SIN `persistent` (D-P1 salteable). `<q-card>` → QRating centrado (`size="2.5em"`, `color="primary"`, `icon="star"`, `icon-selected="star"`, sin medias) bindeado a un ref `stars` → `QInput type="textarea" autogrow` (max ~280) `label="Comentario (opcional)"` → helper caption "Tu opinión es anónima y nos ayuda a mejorar las clases." → `<q-card-actions>` con CTA primario `color="primary"` "Enviar puntuación" (`:disable="stars < 1"`, `:loading="submitting"`) + botón flat "Ahora no". Título "¿Cómo estuvo tu clase de {{ pending?.activityName }}?" (actividad + día; NUNCA datos del profe, D-A3).
    - `<script setup lang="ts">`: refs `show`, `submitting`, `stars`, `comment`, `pending` (PendingRating|null). `createLogger('RatingPromptDialog')`. Usar `useRatingsApi()`. `shouldShow()`: llamar `getPendingRating()`; si devuelve null → false; si la clase ya está marcada resuelta en `Preferences` (key por `sessionDate-scheduleId`) → false; si no → guardar `pending` y true. `evaluate()` setea `show`. `watch(() => authStore.isAuthenticated, (a)=>{ if(a) void evaluate() }, {immediate:true})` para disparar al login/resume. One-shot (D-P2): al saltear ("Ahora no") o al enviar OK → marcar la clase resuelta en `Preferences` (key versionado `rating_resolved_v1_{sessionDate}_{scheduleId}`) para no volver a pedirla. Submit: `submitting=true`, `await submitRating({sessionDate, scheduleId, stars, comment})`; success → marcar resuelta, cerrar, `$q.notify({type:'positive', message:'¡Gracias por tu puntuación!'})`; error (`catch err:unknown`, `instanceof Error`) → mantener abierto, `$q.notify({type:'negative', message:'No pudimos guardar tu puntuación. Probá de nuevo en un momento.'})`. NO usar `persistent`. Estrellas Terracotta vía `color="primary"`/SCSS `$primary`, nunca hex hardcodeado.
    - Estilos: reusar el patrón SCSS de PushPermissionDialog (importar brand SCSS, no hardcodear la paleta).

    Montar en `el-templo-app/src/layouts/MainLayout.vue`: agregar `<RatingPromptDialog />` junto al `<PushPermissionDialog />` existente (l.135) y el import `import RatingPromptDialog from 'src/components/RatingPromptDialog.vue'` junto al de PushPermissionDialog (l.146). El dialog gestiona su propia visibilidad (sin estado del padre).

    Sin `console.log` (usar `createLogger`); sin `any`.
  </action>
  <verify>
    <automated>cd el-templo-app && pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `RatingPromptDialog.vue` contiene `QRating` con `color="primary"` y CTA "Enviar puntuación" (`grep -c "Enviar puntuación" el-templo-app/src/components/RatingPromptDialog.vue` ≥ 1).
    - El `<q-dialog>` NO tiene `persistent` (D-P1): `grep "q-dialog" el-templo-app/src/components/RatingPromptDialog.vue` sin `persistent`.
    - El componente NO renderiza datos del profe: `grep -c "coachName\|photoUrl\|profe.*nombre" el-templo-app/src/components/RatingPromptDialog.vue` = 0.
    - Usa `getPendingRating` y `submitRating` del composable; marca la clase resuelta vía `Preferences` al saltear/enviar (one-shot D-P2).
    - MainLayout monta `<RatingPromptDialog />` y lo importa: `grep -c "RatingPromptDialog" el-templo-app/src/layouts/MainLayout.vue` ≥ 2.
    - `cd el-templo-app && pnpm typecheck && pnpm lint` pasan. Sin `console.` ni `: any`.
  </acceptance_criteria>
  <done>Pop-up de puntuación class-framed, salteable, one-shot, Terracotta, montado en MainLayout; typecheck+lint verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RatingPromptDialog → /api/members/ratings | la UI presenta lo que pending devuelve; las reglas (48h, última, one-shot, sin-profe, asistencia) se enforzan server-side (Plan 02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-17 | Information Disclosure | el pop-up filtra identidad del profe | mitigate | el dialog solo consume `pending` (sin coachId/nombre/foto, D-A3); criterio de aceptación grep sobre el componente y el composable |
| T-143-18 | Integrity | bypass del one-shot/48h desde el cliente | mitigate | el cliente marca resuelta en Preferences pero el server revalida one-shot (D-P2), ventana 48h (D-P3) y asistencia en submitRating (Plan 02 T-143-06/08); el cliente no es la frontera |
| T-143-19 | Tampering | stars/comment manipulados en el request | mitigate | validación de schema + service en el endpoint (Plan 02 T-143-09); `:disable` del CTA es solo UX |
</threat_model>

<verification>
- `pnpm typecheck` + `pnpm lint` verdes en el-templo-app.
- El pop-up se arma alrededor de la clase (actividad/día), nunca muestra al profe.
- Salteable (sin persistent) + one-shot (Preferences por clase) + Terracotta + CTA "Enviar puntuación".
- Montado en MainLayout junto a PushPermissionDialog.
</verification>

<success_criteria>
Al volver a la app tras una clase presencial con profe asignado, el miembro ve un pop-up class-framed para puntuar 1–5 estrellas + comentario opcional; puede saltearlo (no se re-pide) o enviar (toast de gracias), sin ver nunca al profe.
</success_criteria>

<output>
Crear `.planning/phases/143-profesor-por-clase-puntuaci-n-post-clase-presencial/143-05-SUMMARY.md` al terminar.
</output>
