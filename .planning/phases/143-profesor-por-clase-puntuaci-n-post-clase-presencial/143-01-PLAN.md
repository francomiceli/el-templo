---
phase: 143-profesor-por-clase-puntuaci-n-post-clase-presencial
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/schema/class-coach-assignments.ts
  - el-templo-api/src/db/schema/coach-ratings.ts
  - el-templo-api/src/db/schema/index.ts
  - el-templo-api/src/db/migrations/0152_class_coach_roster_and_ratings.sql
autonomous: true
requirements: [PROF-DATA]
must_haves:
  truths:
    - "Existe una tabla de roster semanal que asigna UN profe por (sucursal, semana, día, turno)"
    - "Existe una tabla append-only de puntuaciones (1-5 estrellas + comentario opcional) atribuidas a un profe"
    - "La migración 0152 está aplicada y su SQL commiteado junto al cambio de schema"
  artifacts:
    - path: "el-templo-api/src/db/schema/class-coach-assignments.ts"
      provides: "Modelo del roster semanal con clave natural única (branchId, weekStartDate, dayOfWeek, slot)"
      contains: "class_coach_assignments"
    - path: "el-templo-api/src/db/schema/coach-ratings.ts"
      provides: "Modelo append-only de puntuaciones por profe/clase"
      contains: "coach_ratings"
    - path: "el-templo-api/src/db/migrations/0152_class_coach_roster_and_ratings.sql"
      provides: "DDL aditivo de ambas tablas"
      contains: "CREATE TABLE class_coach_assignments"
  key_links:
    - from: "el-templo-api/src/db/schema/index.ts"
      to: "class-coach-assignments + coach-ratings"
      via: "export *"
      pattern: "export \\* from \"./(class-coach-assignments|coach-ratings)\""
---

<objective>
Crear la capa de persistencia de la fase 143: el **roster semanal** (`class_coach_assignments`) que asigna un profe por `(sucursal, semana, día, turno)` — la única fuente de atribución del puntaje (D-A1, D-Q1) — y la tabla append-only `coach_ratings` (estrellas 1–5 + comentario opcional, D-M1/D-M2). Generar y aplicar la migración 0152.

Purpose: sin estas dos tablas ningún flujo posterior (asignación owner, atribución del rating, vista del owner) puede existir. Es el cimiento que bloquea las demás olas.
Output: dos schema files Drizzle, el barrel actualizado, y el SQL de migración 0152 commiteado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/143-profesor-por-clase-puntuaci-n-post-clase-presencial/143-CONTEXT.md
@.planning/phases/143-profesor-por-clase-puntuaci-n-post-clase-presencial/143-PATTERNS.md

<interfaces>
<!-- Schema analogs the executor builds against. No exploration needed. -->

schedules.ts — mysqlTable shape + index block (the roster mirrors this):
branchId int("branch*id").references(()=>branches.id).notNull()
dayOfWeek tinyint("day_of_week").notNull() // 1=Monday..6=Saturday (ISO)
startTime varchar("start_time",{length:5}) // HH:MM
(table)=>[ index("idx*...").on(...) ]

- schedulesRelations = relations(schedules, ({one,many})=>({ branch: one(branches,{fields,references}), activity: one(activities,...) }))

user-branches.ts — composite unique key style:
(table)=>[ uniqueIndex("user_branch_unique").on(table.userId, table.branchId), index(...) ]

attendance.ts — append-only event log + enum + date(mode:"string"):
attendanceSourceEnum = mysqlEnum("attendance_source", ["qr","manual"])
sessionDate date("session_date",{mode:"string"}).notNull()
createdAt timestamp("created_at").defaultNow().notNull()
memberId/branchId/scheduleId int().references(()=>...).notNull()

migration 0142 — additive CREATE TABLE, FK constraint naming convention, INDEX, ENUM literals must match Drizzle mysqlEnum exactly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear schema files class-coach-assignments.ts y coach-ratings.ts + barrel</name>
  <read_first>
    - el-templo-api/src/db/schema/schedules.ts (analog: mysqlTable + index + relations)
    - el-templo-api/src/db/schema/user-branches.ts (analog: uniqueIndex composite key)
    - el-templo-api/src/db/schema/attendance.ts (analog: append-log + mysqlEnum + date mode:"string")
    - el-templo-api/src/db/schema/index.ts (barrel a extender)
    - .planning/phases/143-profesor-por-clase-puntuaci-n-post-clase-presencial/143-PATTERNS.md (secciones "class-coach-assignments.ts" y "coach-ratings.ts")
    - referencia: reference_drizzle_enum_column_name (mysqlEnum 1er arg = nombre físico de columna)
  </read_first>
  <action>
    Crear `el-templo-api/src/db/schema/class-coach-assignments.ts` exportando `classCoachAssignments = mysqlTable("class_coach_assignments", {...})` con columnas: `id` (PK autoincrement), `branchId` int("branch_id") FK→branches.id notNull, `weekStartDate` date("week_start_date",{mode:"string"}) notNull (lunes de la semana ISO, conserva histórico para atribuir ratings pasados — NO mutar una fila "actual"), `dayOfWeek` tinyint("day_of_week") notNull (1=Lun..6=Sáb), `slot` mysqlEnum("slot",["morning","afternoon"]) notNull (turno derivado por startTime<"12:00", D-A1), `coachId` int("coach_id") FK→users.id notNull, `createdAt`/`updatedAt` timestamps (defaultNow + onUpdateNow como schedules.ts). Bloque `(table)=>[ uniqueIndex("class_coach_assignment_unique").on(table.branchId, table.weekStartDate, table.dayOfWeek, table.slot), index("idx_class_coach_assignments_coach").on(table.coachId) ]` — la unique enforza UN profe por slot/semana a nivel DB (D-A2). Añadir bloque `relations(...)` con `branch: one(branches)` y `coach: one(users)`.

    Crear `el-templo-api/src/db/schema/coach-ratings.ts` exportando `coachRatings = mysqlTable("coach_ratings", {...})` (append-only, espejo de attendance.ts) con columnas: `id` PK, `coachId` int("coach_id") FK→users.id notNull (profe atribuido desde el roster), `memberId` int("member_id") FK→users.id notNull, `branchId` int("branch_id") FK→branches.id notNull, `scheduleId` int("schedule_id") FK→schedules.id (nullable, para resolver actividad/día), `sessionDate` date("session_date",{mode:"string"}) notNull, `stars` tinyint("stars") notNull (1–5, D-M1), `comment` varchar("comment",{length:500}) (nullable, opcional, D-M2), `createdAt` timestamp defaultNow notNull. SIN unique sobre la clave natural (append log); índices: `index("idx_coach_ratings_coach").on(table.coachId)` (promedio por profe, D-O1) y `index("idx_coach_ratings_member_session").on(table.memberId, table.sessionDate)` (guard one-shot D-P2). Añadir `relations(...)` con `coach: one(users)`, `member: one(users)`, `branch: one(branches)`, `schedule: one(schedules)`. OJO: dos FKs a `users` requieren `relationName` distintos en el bloque de relations.

    Editar `el-templo-api/src/db/schema/index.ts`: agregar al final `export * from "./class-coach-assignments";` y `export * from "./coach-ratings";`.

    Sin `console.log`. Sin `any`. Tipado estricto (CLAUDE.md).

  </action>
  <verify>
    <automated>cd el-templo-api && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class_coach_assignments" el-templo-api/src/db/schema/class-coach-assignments.ts` ≥ 1 y contiene `uniqueIndex("class_coach_assignment_unique")`.
    - `grep "mysqlEnum(\"slot\"" el-templo-api/src/db/schema/class-coach-assignments.ts` muestra valores `["morning", "afternoon"]` exactos.
    - `coach-ratings.ts` contiene `stars` tinyint notNull y `comment` varchar length 500 nullable; NO contiene `uniqueIndex`.
    - `grep -c "export \* from \"./class-coach-assignments\"" el-templo-api/src/db/schema/index.ts` = 1 y lo mismo para coach-ratings.
    - `cd el-templo-api && pnpm typecheck` pasa sin errores.
  </acceptance_criteria>
  <done>Ambos schema files existen, tipados, exportados en el barrel; typecheck verde.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: [BLOCKING] Generar y aplicar migración 0152 + commitear el SQL</name>
  <read_first>
    - el-templo-api/src/db/migrations/0142_create_exercise_adjustments.sql (analog: DDL aditivo, FK naming, ENUM, invariante de comentarios sin `;`)
    - el-templo-api/src/db/schema/class-coach-assignments.ts (recién creado)
    - el-templo-api/src/db/schema/coach-ratings.ts (recién creado)
    - .planning/phases/143-.../143-PATTERNS.md (sección "0152_*.sql")
    - CLAUDE.md (Database Changes: pnpm db:generate + pnpm db:migrate, runner custom, NUNCA drizzle-kit migrate/push)
    - referencia: feedback_no_semicolon_in_sql_comments (el runner splitea por `;` antes de strippear `--`)
  </read_first>
  <action>
    Generar el SQL con `pnpm db:generate` (drizzle-kit generate). Si genera un archivo con nombre distinto, renombrarlo/consolidar a `el-templo-api/src/db/migrations/0152_class_coach_roster_and_ratings.sql` (siguiente número tras 0151, verificado). Si `db:generate` pide prompts interactivos por ambigüedad, escribir el SQL a mano siguiendo exactamente la estructura de 0142: dos `CREATE TABLE` aditivos (`class_coach_assignments` con UNIQUE `class_coach_assignment_unique (branch_id, week_start_date, day_of_week, slot)`, `slot ENUM('morning','afternoon') NOT NULL`, e índice `idx_class_coach_assignments_coach (coach_id)`, FKs a `branches(id)` y `users(id)`; `coach_ratings` con `stars TINYINT NOT NULL`, `comment VARCHAR(500) NULL`, `created_at TIMESTAMP NOT NULL DEFAULT (now())`, índices `idx_coach_ratings_coach (coach_id)` y `idx_coach_ratings_member_session (member_id, session_date)`, FKs a `users(id)` ×2, `branches(id)`, `schedules(id)`). Los literales del `ENUM('morning','afternoon')` DEBEN coincidir carácter por carácter con el `mysqlEnum("slot",["morning","afternoon"])` del schema o CI falla con "Unknown column" (tsc no lo cacha). NINGÚN `;` dentro de líneas de comentario `--`.

    Aplicar con `pnpm db:migrate` (runner custom `src/db/run-migrations.ts`; el `_migrations` table es la fuente de verdad). NUNCA `drizzle-kit migrate` ni `drizzle-kit push`.

    Commitear el archivo SQL de migración JUNTO al cambio de schema del Task 1 (executors suelen olvidar el SQL — es criterio de aceptación explícito).

  </action>
  <verify>
    <automated>ls el-templo-api/src/db/migrations/0152_*.sql && grep -v '^--' el-templo-api/src/db/migrations/0152_*.sql | grep -c "CREATE TABLE"</automated>
  </verify>
  <acceptance_criteria>
    - Existe exactamente un archivo `el-templo-api/src/db/migrations/0152_*.sql`.
    - `grep -v '^--' <archivo> | grep -c "CREATE TABLE"` = 2 (ambas tablas).
    - `grep -c "ENUM('morning','afternoon')" <archivo>` ≥ 1 y coincide con el mysqlEnum del schema.
    - Ninguna línea que empiece con `--` contiene `;` (verificar: `grep '^--' <archivo> | grep ';'` no devuelve nada).
    - `pnpm db:migrate` corrió sin error y la fila correspondiente quedó en `_migrations`.
    - El SQL queda staged/commiteado junto a los schema files (NO `git add -A`; stagear por ruta).
  </acceptance_criteria>
  <done>Migración 0152 aplicada vía runner custom, SQL commiteado junto al schema, ambas tablas creadas.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                        |
| --------------------- | -------------------------------------------------- |
| migration runner → DB | DDL aditivo; sin DELETE/UPDATE de datos existentes |

## STRIDE Threat Register

| Threat ID | Category  | Component                   | Disposition | Mitigation Plan                                                                                                     |
| --------- | --------- | --------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------- |
| T-143-01  | Tampering | migración 0152 (ENUM drift) | mitigate    | `ENUM('morning','afternoon')` debe coincidir con `mysqlEnum("slot",[...])`; criterio de aceptación grep-verificable |
| T-143-02  | Tampering | comentarios SQL con `;`     | mitigate    | invariante: ningún `;` en líneas `--` (runner splitea antes de strippear); criterio de aceptación                   |
| T-143-03  | Integrity | doble profe por slot/semana | mitigate    | `uniqueIndex` natural-key en DB (D-A2) impide duplicados a nivel motor                                              |
| T-143-SC  | Tampering | npm/pip/cargo installs      | accept      | esta fase NO instala dependencias nuevas (reutiliza role coach + user_branches existentes)                          |

</threat_model>

<verification>
- `pnpm typecheck` verde en el-templo-api.
- Migración 0152 aplicada (fila en `_migrations`), ambas tablas existen en `eltemplo` local.
- SQL de migración commiteado junto a los schema files.
</verification>

<success_criteria>
Las tablas `class_coach_assignments` y `coach_ratings` existen con sus índices y FKs; el barrel las exporta; la migración 0152 está aplicada y commiteada.
</success_criteria>

<output>
Crear `.planning/phases/143-profesor-por-clase-puntuaci-n-post-clase-presencial/143-01-SUMMARY.md` al terminar.
</output>
