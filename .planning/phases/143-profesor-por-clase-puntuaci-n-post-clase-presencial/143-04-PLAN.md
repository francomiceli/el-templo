---
phase: 143-profesor-por-clase-puntuaci-n-post-clase-presencial
plan: 04
type: execute
wave: 3
depends_on: [143-02]
files_modified:
  - el-templo-admin/src/composables/useRatingsApi.ts
  - el-templo-admin/src/pages/HorariosPage.vue
  - el-templo-admin/src/pages/PuntuacionesPage.vue
  - el-templo-admin/src/router/routes.ts
autonomous: true
requirements: [PROF-ROSTER, PROF-OWNERVIEW]
must_haves:
  truths:
    - "El owner asigna un profe por (día, turno) en la semana/sucursal activa de Horarios, persistiendo de inmediato"
    - "El selector de coach se filtra a coaches de la sucursal seleccionada y muestra 'Sin profe asignado' cuando vacío"
    - "Existe una página owner-only con promedio por profe (estrellas read-only) + puntajes/comentarios recientes"
  artifacts:
    - path: "el-templo-admin/src/composables/useRatingsApi.ts"
      provides: "Cliente API admin: getRosterWeek, assignCoach, getCoachesForBranch, getOwnerRatings"
      contains: "useRatingsApi"
    - path: "el-templo-admin/src/pages/PuntuacionesPage.vue"
      provides: "Vista owner-only de puntuaciones (Surface 3)"
      contains: "Puntuaciones de profes"
  key_links:
    - from: "el-templo-admin/src/pages/HorariosPage.vue"
      to: "useRatingsApi.assignCoach"
      via: "onChange del QSelect de coach por (día, turno)"
      pattern: "assignCoach"
    - from: "el-templo-admin/src/router/routes.ts"
      to: "PuntuacionesPage.vue"
      via: "ruta con meta.allowedRoles owner-only"
      pattern: "PuntuacionesPage"
---

<objective>
Construir las dos superficies admin de la fase: **Surface 1** — grilla de asignación de profe por `(día, turno)` dentro de la vista semanal existente de `HorariosPage` (reusa selector de sucursal + navegación de semana, NO agrega un segundo picker), persistencia inmediata (D-A1, sin botón Save); y **Surface 3** — `PuntuacionesPage.vue` owner-only con promedio por profe (estrellas read-only) + recientes (D-O1, D-M3). Más el composable `useRatingsApi` y la ruta.

Purpose: dar al owner la UI para asignar el roster (fuente de atribución) y para leer las puntuaciones, respetando el contrato visual aprobado (143-UI-SPEC: Terracotta, QRating, copy en español, owner-only).
Output: composable + HorariosPage extendido + PuntuacionesPage + ruta registrada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/143-profesor-por-clase-puntuaci-n-post-clase-presencial/143-CONTEXT.md
@.planning/phases/143-profesor-por-clase-puntuaci-n-post-clase-presencial/143-PATTERNS.md
@.planning/phases/143-profesor-por-clase-puntuaci-n-post-clase-presencial/143-UI-SPEC.md
@.planning/phases/143-profesor-por-clase-puntuaci-n-post-clase-presencial/143-02-SUMMARY.md

<interfaces>
<!-- Endpoints del Plan 02 + analogs de admin. -->

API (Plan 02):
GET /api/admin/ratings/coaches?branchId → CoachOption[] {id, firstName, lastName}
GET /api/admin/ratings/roster?branchId&weekStart → RosterWeekRow[] por (dayOfWeek, slot)
POST /api/admin/ratings/roster body {branchId, weekStartDate, dayOfWeek, slot, coachId}
GET /api/admin/ratings (owner-only) → { perCoach:[{coachId,name,avg,count}], recent:[{stars,comment,activity,day,date}] }

useCoachApi.ts (analog del composable admin):
import { api } from 'src/boot/axios'; import { extractError } from 'src/utils/extract-error'
loading/error refs; async get/post('/admin/...'); catch(err:unknown){ error.value=extractError(err,'...'); throw err } finally { loading.value=false }

HorariosPage.vue (ya existe — extender, NO reemplazar):
selectedBranchId (l.324), branchTimezone, weekStartDate (getMondayInTz, l.333), weekDays computed (l.373, {dayOfWeek, date}), weekRangeLabel (l.403), isMobile=$q.screen.lt.sm (l.356), selectedDay (l.362), createLogger('HorariosPage') (l.317), $q.notify({type:'positive'|'negative',...}) pattern, loadingGrid wrapper try/catch(err:unknown)+log.error.
Turno: morning = startTime < '12:00', afternoon >= '12:00' (igual que ReservasPage).

DeudasPage.vue (analog de Surface 3): <q-page padding>, text-h5 title, QTable :rows/:columns/:loading/flat bordered/:no-data-label, <script setup>, createLogger, columns array, reload() en onMounted.

router/routes.ts: entradas con `meta: { allowedRoles: [...] as AdminRole[] }`. horarios usa ['coach','admin','owner','gestion','recepcion']; la nueva PuntuacionesPage owner-only → ['owner'].

UI-SPEC (LOCKED):
Surface 1: QSelect coaches por (día,turno), label firstName lastName, placeholder "Sin profe asignado", toast "Profe asignado" / error "No se pudo asignar el profe. Reintentá.", sin botón Save. Mobile: dos QSelect "Profe — Mañana"/"Profe — Tarde" para el día seleccionado.
Surface 3: title "Puntuaciones de profes", QRating readonly + numérico para el promedio, labels "Promedio"/"Puntuaciones", empty "Todavía no hay puntuaciones" + body. Estrellas Terracotta (color="primary"), nunca hardcodear hex.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Composable useRatingsApi (admin) + extender HorariosPage con la grilla de roster (Surface 1)</name>
  <read_first>
    - el-templo-admin/src/composables/useCoachApi.ts (analog del composable)
    - el-templo-admin/src/pages/HorariosPage.vue (l.300-640: selectedBranchId, weekStartDate, weekDays, weekRangeLabel, isMobile, selectedDay, notify, loadingGrid)
    - el-templo-app/src/pages/ReservasPage.vue (l.926-928: split morning/afternoon por startTime<'12:00')
    - .planning/phases/143-.../143-UI-SPEC.md (Surface 1 + Copywriting + Color)
    - .planning/phases/143-.../143-CONTEXT.md (D-A1, D-A2)
    - .planning/phases/143-.../143-PATTERNS.md (secciones useRatingsApi + HorariosPage)
  </read_first>
  <action>
    Crear `el-templo-admin/src/composables/useRatingsApi.ts` copiando la forma de `useCoachApi.ts` (refs `loading`/`error`, `extractError`, `throw err` en catch, `finally`). Interfaces exportadas (sin `any`): `CoachOption {id; firstName; lastName}`, `RosterAssignment {dayOfWeek; slot:'morning'|'afternoon'; coachId; coachName?}`, `OwnerRatings` (ver Task 2). Métodos: `getCoachesForBranch(branchId)` → GET `/admin/ratings/coaches`, `getRosterWeek(branchId, weekStart)` → GET `/admin/ratings/roster`, `assignCoach(payload)` → POST `/admin/ratings/roster`, `getOwnerRatings()` → GET `/admin/ratings`.

    Extender `HorariosPage.vue` (NO reemplazar): agregar una grilla "Profe" por `(día, turno)` para la semana/sucursal activa, reusando `selectedBranchId`, `weekStartDate`, `weekDays` y la navegación de semana existentes — NO agregar un segundo selector de sucursal/semana. Por cada día visible mostrar dos QSelect ("Mañana" / "Tarde") de coaches (cargados con `getCoachesForBranch(selectedBranchId)`, label `firstName lastName`, placeholder "Sin profe asignado"). El turno deriva de `startTime < '12:00'` (mismo criterio que ReservasPage). Cargar el roster de la semana con `getRosterWeek` al cambiar branch/semana (envolver en wrapper con `loading` ref + try/catch(err:unknown) + `log.error`). En `@update:model-value` de cada QSelect → llamar `assignCoach({branchId, weekStartDate, dayOfWeek, slot, coachId})` de inmediato y mostrar `$q.notify({type:'positive', message:'Profe asignado'})`; en error `$q.notify({type:'negative', message:'No se pudo asignar el profe. Reintentá.'})`. Desktop: fila Profe Mañana/Tarde por día sobre/junto a las celdas de slots. Mobile (`isMobile`): dos QSelect para el `selectedDay`. Reusar `createLogger('HorariosPage')` existente; sin `console.log`; sin `any`.

  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `el-templo-admin/src/composables/useRatingsApi.ts` exporta `useRatingsApi` con `getCoachesForBranch`, `getRosterWeek`, `assignCoach`, `getOwnerRatings`.
    - HorariosPage referencia `assignCoach` y NO agrega un segundo selector de sucursal: `grep -c "assignCoach" el-templo-admin/src/pages/HorariosPage.vue` ≥ 1.
    - El placeholder "Sin profe asignado" y el toast "Profe asignado" aparecen en HorariosPage.
    - El turno usa el split `< '12:00'` (no se inventa otro criterio).
    - `cd el-templo-admin && pnpm typecheck` pasa.
    - Sin `console.` ni `: any` en los archivos tocados.
  </acceptance_criteria>
  <done>useRatingsApi creado; HorariosPage asigna profe por (día,turno) con persistencia inmediata y toasts; typecheck verde.</done>
</task>

<task type="auto">
  <name>Task 2: PuntuacionesPage owner-only (Surface 3) + ruta</name>
  <read_first>
    - el-templo-admin/src/pages/DeudasPage.vue (l.1-113: estructura QTable + script setup + createLogger + reload onMounted)
    - el-templo-admin/src/composables/useRatingsApi.ts (creado en Task 1: getOwnerRatings)
    - el-templo-admin/src/router/routes.ts (l.73-97: patrón meta.allowedRoles)
    - .planning/phases/143-.../143-UI-SPEC.md (Surface 3 + Copywriting + Color + empty state)
    - .planning/phases/143-.../143-CONTEXT.md (D-O1, D-M3)
  </read_first>
  <action>
    Crear `el-templo-admin/src/pages/PuntuacionesPage.vue` (copia estructural de DeudasPage): `<q-page padding>`, título `text-h5` "Puntuaciones de profes", `<script setup lang="ts">` con `useRatingsApi`, `createLogger('PuntuacionesPage')`, `reload()` en `onMounted` que llama `getOwnerRatings()`. Render: (a) tabla/lista resumen por coach → nombre del profe + promedio como `QRating` `readonly` (`color="primary"` Terracotta, `size` chico) + valor numérico (ej. "4.6") + count, columnas etiquetadas "Promedio" y "Puntuaciones"; (b) debajo, lista (`QList`/`QItem` o `QTable`) de puntajes recientes: estrellas, comentario opcional, clase (actividad/día), fecha — sin filtros, sin export (diferido). Empty state: heading "Todavía no hay puntuaciones" + body "Cuando los miembros puntúen sus clases presenciales, vas a ver acá el promedio por profe y los comentarios recientes." Loading: `QInnerLoading`/skeleton. Error: toast estándar + retry en reload. Estrellas Terracotta vía `color="primary"`, nunca hex hardcodeado. Definir la interfaz `OwnerRatings` en useRatingsApi (perCoach + recent) y consumirla aquí (sin `any`).

    Registrar la ruta en `el-templo-admin/src/router/routes.ts`: nueva entrada `{ path: 'puntuaciones', component: () => import('pages/PuntuacionesPage.vue'), meta: { allowedRoles: ['owner'] as AdminRole[] } }` (owner-only, D-M3). Verificar cómo se agrega el ítem al sidebar/nav de admin y agregar el enlace owner-only si el patrón lo requiere (mismo gating de roles).

  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `el-templo-admin/src/pages/PuntuacionesPage.vue` contiene el título "Puntuaciones de profes" y el empty state "Todavía no hay puntuaciones".
    - Usa `QRating` con `readonly` y `color="primary"` para el promedio (no hex hardcodeado).
    - `routes.ts` tiene la entrada `puntuaciones` con `meta.allowedRoles: ['owner']` (owner-only): `grep -A2 "puntuaciones" el-templo-admin/src/router/routes.ts | grep -c "'owner'"` ≥ 1.
    - `cd el-templo-admin && pnpm typecheck && pnpm lint` pasan.
    - Sin `console.` ni `: any`.
  </acceptance_criteria>
  <done>PuntuacionesPage owner-only con promedio (QRating read-only) + recientes + empty state; ruta registrada; typecheck+lint verdes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                                                 |
| ------------------------------ | ------------------------------------------------------------------------------------------- |
| admin web → /api/admin/ratings | la UI no es la frontera de seguridad; el server enforza owner-only y branch-scope (Plan 02) |

## STRIDE Threat Register

| Threat ID | Category               | Component                                       | Disposition | Mitigation Plan                                                                                                                                    |
| --------- | ---------------------- | ----------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-143-13  | Information Disclosure | ruta /puntuaciones visible a no-owners          | mitigate    | `meta.allowedRoles: ['owner']` en router + el endpoint GET /api/admin/ratings es owner-only server-side (Plan 02 T-143-04). Defensa en profundidad |
| T-143-14  | Elevation of Privilege | coach asignando roster en sucursal ajena vía UI | mitigate    | el server valida con requireBranchAccess (Plan 02); la UI solo ofrece coaches de la sucursal seleccionada                                          |
| T-143-15  | Information Disclosure | coach viendo su promedio                        | mitigate    | no hay UI de ratings para coach; vista owner-only (D-M3)                                                                                           |

</threat_model>

<verification>
- `pnpm typecheck` + `pnpm lint` verdes en el-templo-admin.
- HorariosPage asigna profe por (día,turno) con persistencia inmediata y toasts; reusa selector/semana existentes.
- PuntuacionesPage owner-only renderiza promedio (QRating read-only) + recientes + empty state, ruta con meta owner.
</verification>

<success_criteria>
El owner asigna el roster desde Horarios (sin botón Save, toasts de confirmación) y consulta las puntuaciones en una página owner-only con promedio por profe y comentarios recientes, respetando el contrato visual aprobado.
</success_criteria>

<output>
Crear `.planning/phases/143-profesor-por-clase-puntuaci-n-post-clase-presencial/143-04-SUMMARY.md` al terminar.
</output>
