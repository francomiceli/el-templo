---
phase: 150-cuentas-bancarias-flexibles
plan: 03
type: execute
wave: 3
depends_on: ["150-02"]
files_modified:
  - el-templo-api/src/modules/finance/routes.ts
  - el-templo-api/test/finance/bank-accounts.test.ts
autonomous: true
requirements: [CTA-01, CTA-02]
must_haves:
  truths:
    - "POST/PATCH/GET/close/reactivate /cash-registers responden bajo /api/admin/finance con guard admin/owner"
    - "gestion y coach reciben 403 en los endpoints de escritura del ABM"
    - "El seed 'Retiros' es visible para el flujo de egresos tras la migración"
    - "Un PATCH con currency distinta NO cambia la moneda almacenada (D-04)"
  artifacts:
    - path: "el-templo-api/src/modules/finance/routes.ts"
      provides: "5 endpoints ABM de cuentas bancarias con guard ADMIN_ROLES"
      contains: "cash-registers"
    - path: "el-templo-api/test/finance/bank-accounts.test.ts"
      provides: "Tests de integración del ABM (create/close/reactivate/guard/seed/moneda-inmutable)"
      contains: "bank-accounts"
  key_links:
    - from: "el-templo-api/src/modules/finance/routes.ts"
      to: "cashRegisterService"
      via: "handlers llaman createBankAccount/updateBankAccount/closeBankAccount/reactivateBankAccount/listBankAccounts"
      pattern: "BankAccount"
    - from: "handlers de escritura del ABM"
      to: "ADMIN_ROLES"
      via: "guard en-handler 403"
      pattern: "ADMIN_ROLES"
---

<objective>
Exponer el CRUD de cuentas bancarias como endpoints HTTP bajo `/api/admin/finance` con guard admin/owner (D-12) y cubrirlos con tests de integración obligatorios contra MySQL real.

Purpose: Conecta el service (plan 02) con el frontend (planes 04/05) y prueba CTA-01/CTA-02 end-to-end en la API, incluyendo el guard de autorización (la seguridad real vive en la API, 149 D-04) y la inmutabilidad de la moneda (D-04).
Output: 5 endpoints ABM + suite de integración bank-accounts.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/150-cuentas-bancarias-flexibles/150-CONTEXT.md
@.planning/phases/150-cuentas-bancarias-flexibles/150-PATTERNS.md
@.planning/phases/150-cuentas-bancarias-flexibles/150-02-SUMMARY.md

<interfaces>
<!-- Endpoints ABM (prefijo del plugin financeRoutes = /api/admin/finance) -->
POST   /cash-registers           → createBankAccount   (createBankAccountSchema)   → 201 { account: BankAccountRow }
PATCH  /cash-registers/:id       → updateBankAccount   (updateBankAccountSchema)   → 200 { account: BankAccountRow }
POST   /cash-registers/:id/close → closeBankAccount    (closeBankAccountSchema)    → 200 { account, balance }
POST   /cash-registers/:id/reactivate → reactivateBankAccount (reactivateBankAccountSchema) → 200 { account }
GET    /cash-registers           → listBankAccounts    → 200 { accounts: BankAccountRow[] }

Guard: ADMIN_ROLES = ["admin","owner"] (permissions.ts:23). El hook de módulo ya gatea FINANCE_READ_ROLES en onRequest (routes.ts:190-201); el ABM aplica guard stricter ADMIN_ROLES en-handler.
Recordatorio D-04: `currency` es fija post-creación — `updateBankAccountSchema` NO la acepta y el service nunca la muta.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar los 5 endpoints ABM en routes.ts con guard admin/owner</name>
  <files>el-templo-api/src/modules/finance/routes.ts</files>
  <read_first>
    - el-templo-api/src/modules/finance/routes.ts (analog escritura: POST /expenses líneas 608-647; analog lectura: GET /cost-centers líneas 1148-1176; hook FINANCE_READ_ROLES líneas 190-201; patrón guard en-handler líneas 613-620; imports schemas líneas 28-48; imports permissions líneas 49-54; handleServiceError línea 27 uso 644)
    - el-templo-api/src/modules/shared/permissions.ts (ADMIN_ROLES línea 23)
    - el-templo-api/src/modules/finance/cash-register-service.ts (métodos del plan 02)
  </read_first>
  <action>
    Registrar dentro del plugin `financeRoutes` los 5 endpoints listados en `<interfaces>` (prefijo real `/api/admin/finance`). Importar los 4 schemas nuevos de `./schemas` (bloque de imports líneas 28-48) y `ADMIN_ROLES` de `../shared/permissions` (bloque líneas 49-54) si no está ya. Cada handler de escritura (POST create, PATCH update, POST close, POST reactivate) empieza con el guard en-handler stricter: si `request.user.role` NO está en `ADMIN_ROLES` → `reply.code(403).send({ error: "Acceso denegado", message: "..." })` (patrón líneas 613-620) — usar ADMIN_ROLES, NO FINANCE_VOID_ROLES (que incluye gestion). Estructura del handler igual a POST /expenses: guard → instanciar/obtener `cashRegisterService` → llamar al método del service → `reply.code(201).send({ account })` en create (200 en el resto) → `catch (err: unknown) { handleServiceError(err, reply, request.log, "<contexto>"); }`. Adjuntar el schema correspondiente en la opción `schema` de cada ruta. Las cuentas banco son branchId=null (country-agnostic) → NO aplicar el filtro `resolveCajaCountry`; el guard admin/owner ya cubre el acceso (PATTERNS nota country-scope).
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - routes.ts registra las 5 rutas: POST `/cash-registers`, PATCH `/cash-registers/:id`, POST `/cash-registers/:id/close`, POST `/cash-registers/:id/reactivate`, GET `/cash-registers`
    - Cada handler de escritura chequea `ADMIN_ROLES` y responde 403 si el rol no pertenece
    - Ningún handler del ABM usa FINANCE_VOID_ROLES
    - Cada ruta adjunta su schema (`createBankAccountSchema`/`updateBankAccountSchema`/`closeBankAccountSchema`/`reactivateBankAccountSchema`)
    - Errores canalizados por `handleServiceError`
    - `pnpm build` verde
  </acceptance_criteria>
  <done>Los 5 endpoints ABM existen bajo /api/admin/finance con guard ADMIN_ROLES en escritura y schemas adjuntos; tsc verde.</done>
</task>

<task type="auto">
  <name>Task 2: Tests de integración bank-accounts.test.ts</name>
  <files>el-templo-api/test/finance/bank-accounts.test.ts</files>
  <read_first>
    - el-templo-api/test/finance/cost-centers.test.ts (harness idéntico: createTestApp, getAuthToken, createStaffUser, newCaja helper líneas 39-51; tokens por rol líneas 18-51)
    - el-templo-api/src/modules/finance/routes.ts (endpoints del Task 1)
  </read_first>
  <action>
    Crear la suite siguiendo el harness de `cost-centers.test.ts` (`describe/it/beforeAll/afterAll/expect` de vitest, `createTestApp`, `getAuthToken`, `createStaffUser`, `import * as schema`, tokens `ownerToken`/`gestionToken`/`coachToken`). Casos obligatorios: (1) crear cuenta con los 3 obligatorios (bankName+accountHolder+currency) + alias → 201 y `account` con name derivado "Banco · alias" (D-03). (2) crear sin cbuCvu NI accountAlias → 400 (regla uno-de-dos D-02). (3) crear solo con cbuCvu (sin alias) → 201 y name con fallback últimos 4. (4) PATCH que completa campos (p.ej. taxId/cbuCvu) → 200 y name recalculado. (5) POST /:id/close → 200, luego GET confirma la cuenta con `isActive=false` presente en el listado del ABM pero ausente de las cajas activas operativas. (6) POST /:id/reactivate → 200 y `isActive=true`. (7) guard: `gestionToken` y `coachToken` en POST create → 403; `ownerToken` → 201 (D-12 admin/owner-only). (8) seed: consultar cost_centers y afirmar que 'Retiros' (AR) existe tras la migración 0163. (9) MONEDA FIJA (D-04): crear una cuenta ARS, hacer PATCH intentando setear `currency:'EUR'` (más otro campo válido para que el body no quede vacío), luego GET/leer la cuenta y afirmar que `currency` sigue siendo 'ARS' — la moneda NO cambió. El schema puede rechazar el `currency` extra con 400 (additionalProperties:false) o el service ignorarlo: el assert duro es que la moneda ALMACENADA permanece 'ARS'. Sin `any`; usar el helper `newCaja` si hace falta preparar datos.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - El archivo `test/finance/bank-accounts.test.ts` existe con al menos 9 casos `it(...)`
    - Cubre: crear con 3 obligatorios (201), crear sin CBU/CVU-ni-Alias (400), nombre derivado y fallback, editar, cerrar, reactivar, guard gestion/coach→403 y owner→201, seed 'Retiros' presente
    - Incluye un caso que asegura que un PATCH con `currency` distinta NO cambia la moneda almacenada (D-04)
    - `pnpm build` compila los tests sin errores (la ejecución vitest corre en CI contra MySQL real)
    - Sin `any`
  </acceptance_criteria>
  <done>Suite de integración del ABM creada cubriendo CRUD, regla uno-de-dos, ciclo de vida, guard admin/owner, seed 'Retiros' y moneda inmutable (D-04); compila para CI.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                |
| ---------------------------------------- | -------------------------------------------------------------------------- |
| cliente → API admin (/api/admin/finance) | Requests autenticados de distintos roles; el ABM debe ser admin/owner-only |
| :id en la URL → recurso caja             | IDOR: un actor podría intentar cerrar/editar una caja arbitraria           |

## STRIDE Threat Register

| Threat ID | Category  | Component                      | Disposition | Mitigation Plan                                                                                                                               |
| --------- | --------- | ------------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| T-150-07  | Elevation | endpoints de escritura del ABM | mitigate    | Guard en-handler con ADMIN_ROLES (no FINANCE_VOID_ROLES); test afirma gestion/coach→403. La seguridad real vive en la API (149 D-04)          |
| T-150-08  | Tampering | PATCH/close/reactivate /:id    | mitigate    | El service (plan 02) valida existencia + type='banco' (NotFoundError); el schema fuerza `id` integer ≥1; test cubre el ciclo                  |
| T-150-09  | Spoofing  | hook onRequest del módulo      | mitigate    | Autenticación previa por el hook FINANCE_READ_ROLES (routes.ts:190-201) antes del guard stricter del ABM                                      |
| T-150-18  | Tampering | PATCH currency                 | mitigate    | `updateBankAccountSchema` rechaza `currency` (additionalProperties:false) y el service no la muta; test afirma que la moneda no cambia (D-04) |

Sin instalación de paquetes. Los tests corren en CI (no localmente, per convención del proyecto); verify local = tsc.
</threat_model>

<verification>
- `pnpm build` verde (routes + tests compilan)
- 5 endpoints registrados con guard ADMIN_ROLES
- Suite bank-accounts.test.ts con >=9 casos cubriendo CRUD/guard/seed/moneda-inmutable
- CI ejecuta los tests de integración contra MySQL real
</verification>

<success_criteria>

- Endpoints ABM CTA-01/CTA-02 funcionando bajo /api/admin/finance con autorización correcta
- Tests de integración obligatorios presentes (CLAUDE.md), incluyendo la garantía de moneda fija (D-04)
  </success_criteria>

<output>
Create `.planning/phases/150-cuentas-bancarias-flexibles/150-03-SUMMARY.md` when done
</output>
</output>
