---
phase: 150-cuentas-bancarias-flexibles
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [CTA-01, CTA-02, CTA-03]
files_modified:
  - el-templo-api/src/modules/finance/routes.ts
  - el-templo-api/test/finance/bank-accounts.test.ts
  - el-templo-admin/src/types/transaction.ts
  - el-templo-admin/src/components/caja/CuentaBancariaFormDialog.vue
  - el-templo-admin/src/components/caja/CuentasTab.vue
  - el-templo-admin/src/components/caja/RegistrarMovEgresoDialog.vue

must_haves:
  truths:
    - "El admin puede abrir 'Editar' sobre una cuenta banco legacy (bankName/accountHolder NULL, p.ej. Banco ARS/Banco EUR) sin que el dialog crashee"
    - "GET /cash-registers responde 403 para gestion/recepcion (no-owner sin rol admin) — el ABM de lectura es admin/owner-only en los 5 endpoints (D-12)"
    - "El botón 'Registrar retiro' solo se renderiza para el owner; cuando el prefill de caja no se encuentra, se notifica en vez de fallar en silencio"
  artifacts:
    - path: "el-templo-api/src/modules/finance/routes.ts"
      provides: "Guard ADMIN_ROLES en-handler en GET /cash-registers"
      contains: "ADMIN_ROLES as readonly string[]"
    - path: "el-templo-api/test/finance/bank-accounts.test.ts"
      provides: "Tests RBAC para GET y PATCH/close/reactivate (gestion → 403)"
      contains: "GET /cash-registers"
    - path: "el-templo-admin/src/types/transaction.ts"
      provides: "BankAccount.bankName/accountHolder como string | null (espejo del backend)"
      contains: "bankName: string | null"
    - path: "el-templo-admin/src/components/caja/CuentaBancariaFormDialog.vue"
      provides: "onShow() con fallback ?? '' para bankName/accountHolder"
      contains: "acc.bankName ?? ''"
    - path: "el-templo-admin/src/components/caja/CuentasTab.vue"
      provides: "Botón 'Registrar retiro' gateado por v-if isOwner"
      contains: 'v-if="isOwner"'
    - path: "el-templo-admin/src/components/caja/RegistrarMovEgresoDialog.vue"
      provides: "Notificación cuando prefillCajaId no está en la lista de cajas cargada"
      contains: "$q.notify"
  key_links:
    - from: "el-templo-api/src/modules/finance/routes.ts (GET /cash-registers)"
      to: "ADMIN_ROLES"
      via: "guard en-handler 403 antes de listBankAccounts()"
      pattern: "ADMIN_ROLES as readonly string\\[\\]"
    - from: "el-templo-admin/src/components/caja/CuentaBancariaFormDialog.vue (onShow)"
      to: "form.bankName/form.accountHolder"
      via: "asignación con coalescencia null → ''"
      pattern: "acc\\.(bankName|accountHolder) \\?\\? ''"
---

<objective>
Cerrar los 3 hallazgos Critical (CR-01, CR-02, CR-03) que la verificación de fase confirmó como fallidos (1/4 must-haves). Son fixes quirúrgicos sobre código ya ejecutado en los planes 150-01..150-05 — NO se re-implementa el ABM, solo se corrigen: (1) el desajuste de nullability que crashea la edición de cuentas legacy, (2) el guard de autorización faltante en GET /cash-registers, y (3) el gate de rol del botón de retiro más la notificación de prefill fallido.

Purpose: Restaurar los truths CTA-01 (faceta edición), CTA-02 (garantía admin/owner-only) y CTA-03 (retiro no induce mala imputación) para que la re-verificación pase.
Output: 6 archivos modificados con fixes puntuales + tests RBAC nuevos en bank-accounts.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/150-cuentas-bancarias-flexibles/150-CONTEXT.md
@.planning/phases/150-cuentas-bancarias-flexibles/150-VERIFICATION.md
@.planning/phases/150-cuentas-bancarias-flexibles/150-REVIEW.md

<interfaces>
<!-- Contratos clave que el executor necesita — extraídos del código actual. Usarlos directamente. -->

Backend BankAccountRow (el-templo-api/src/modules/finance/types.ts) declara nullable:

```typescript
bankName: string | null;
accountHolder: string | null;
```

Frontend BankAccount (el-templo-admin/src/types/transaction.ts:499-513) MIENTE sobre nullability:

```typescript
export interface BankAccount {
  ...
  bankName: string;      // línea 504 — debe ser string | null
  accountHolder: string; // línea 505 — debe ser string | null
  ...
}
```

Patrón del guard admin/owner (routes.ts:1203-1208, replicado en POST/PATCH/close/reactivate):

```typescript
if (!(ADMIN_ROLES as readonly string[]).includes(request.user.role)) {
  return reply.code(403).send({
    error: "Acceso denegado",
    message: "No tienes permiso para administrar cuentas bancarias",
  });
}
```

`ADMIN_ROLES` ya está importado en routes.ts (línea 58).

GET /cash-registers actual SIN guard (routes.ts:1289-1297):

```typescript
fastify.get("/cash-registers", async (request, reply) => {
  try {
    const accounts = await cashRegisterService.listBankAccounts();
    ...
```

Prefill de caja en RegistrarMovEgresoDialog.vue (onShow, líneas 264-267):

```typescript
if (props.prefillCajaId !== undefined) {
  const caja = cajas.value.find(
    (c) => c.cashRegisterId === props.prefillCajaId,
  );
  if (caja) egreso.cajaId = caja.cashRegisterId;
}
```

`$q` ya está disponible en ese componente (se usa en loadCajas línea 215).

Harness de tests (bank-accounts.test.ts): `app.inject`, tokens `ownerToken`/`gestionToken`/`coachToken` ya creados en beforeAll; helper `createAccount(token, payload)` existente. Base URL de rutas: `/api/admin/finance/cash-registers`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard ADMIN_ROLES en GET /cash-registers + tests RBAC (CR-02, seguridad)</name>
  <files>el-templo-api/src/modules/finance/routes.ts, el-templo-api/test/finance/bank-accounts.test.ts</files>
  <read_first>
    - el-templo-api/src/modules/finance/routes.ts (leer líneas 1190-1297: el bloque de comentario del ABM que declara admin/owner-only, los 4 handlers de escritura con el guard, y el GET sin guard en 1289)
    - el-templo-api/test/finance/bank-accounts.test.ts (leer completo: harness beforeAll con tokens owner/gestion/coach, helper createAccount, patrón app.inject y el describe "D-12: guard admin/owner en escritura" líneas 264-296 como plantilla)
    - .planning/phases/150-cuentas-bancarias-flexibles/150-REVIEW.md (CR-02 y WR-06 con los fixes concretos propuestos)
  </read_first>
  <action>
    Agregar el guard admin/owner en-handler al inicio del handler `GET /cash-registers` (routes.ts:1289), ANTES de llamar `cashRegisterService.listBankAccounts()`. Usar exactamente el mismo patrón que los 4 endpoints de escritura hermanos: chequear `!(ADMIN_ROLES as readonly string[]).includes(request.user.role)` y retornar `reply.code(403).send({ error: "Acceso denegado", message: "No tienes permiso para administrar cuentas bancarias" })`. `ADMIN_ROLES` ya está importado (línea 58) — no agregar imports. El GET queda protegido tanto por el hook de módulo `FINANCE_READ_ROLES` como por este guard estricto, cerrando la fuga a los roles gestion/recepcion que sí pasan el hook de lectura pero NO son admin/owner.

    En bank-accounts.test.ts, agregar tests RBAC (WR-06) reusando `gestionToken`/`coachToken` y `app.inject` sobre `/api/admin/finance/cash-registers`:
    - GET con gestionToken → espera 403 (este es el test que habría detectado CR-02).
    - GET con coachToken → espera 403.
    - GET con ownerToken → espera 200 (control positivo; el array `accounts` está presente).
    - PATCH `/:id` con gestionToken → espera 403.
    - POST `/:id/close` con gestionToken → espera 403.
    - POST `/:id/reactivate` con gestionToken → espera 403.
    Para los PATCH/close/reactivate usar un id de cuenta creado con `createAccount(ownerToken, ...)` en el mismo bloque o el patrón de creación ya usado en los describes existentes. Agrupar en un describe nuevo (p.ej. "D-12: guard admin/owner en lectura y ciclo de vida"). No romper el describe existente de escritura.

  </action>
  <verify>
    <automated>cd el-templo-api && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ADMIN_ROLES as readonly string" el-templo-api/src/modules/finance/routes.ts` muestra el guard dentro del bloque del handler GET /cash-registers (entre la línea del `fastify.get("/cash-registers"` y la llamada a `listBankAccounts()`).
    - El handler GET retorna 403 con `{ error: "Acceso denegado", ... }` cuando `request.user.role` no está en ADMIN_ROLES, antes de cualquier query.
    - bank-accounts.test.ts contiene al menos: un `it` con `method: "GET"` sobre `/cash-registers` y `gestionToken` que espera `statusCode` 403; un `it` GET con `ownerToken` que espera 200; y tests 403 con gestionToken para PATCH, close y reactivate.
    - `cd el-templo-api && pnpm build` compila sin errores de tipos y sin `any`.
  </acceptance_criteria>
  <done>GET /cash-registers rechaza gestion/recepcion con 403 (admin/owner-only en los 5 endpoints, D-12) y hay cobertura de tests RBAC para GET y para las 3 escrituras restantes.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror de nullability + fallback en la edición de cuentas legacy (CR-01)</name>
  <files>el-templo-admin/src/types/transaction.ts, el-templo-admin/src/components/caja/CuentaBancariaFormDialog.vue</files>
  <read_first>
    - el-templo-admin/src/types/transaction.ts (leer la interface `BankAccount` líneas 499-513 — los campos `bankName`/`accountHolder` en 504-505 declarados no-nulos)
    - el-templo-admin/src/components/caja/CuentaBancariaFormDialog.vue (leer líneas 138-191: el reactive `form`, el computed `canSubmit` línea 151-153 que hace `form.bankName.trim()`, y `onShow()` líneas 180-191 que asigna `form.bankName = acc.bankName` sin fallback)
    - .planning/phases/150-cuentas-bancarias-flexibles/150-REVIEW.md (CR-01 con el fix propuesto)
  </read_first>
  <action>
    En transaction.ts, cambiar la interface `BankAccount` para que `bankName` y `accountHolder` sean `string | null` (líneas 504-505), espejando exactamente `BankAccountRow` del backend (`el-templo-api/src/modules/finance/types.ts`). Motivo: las 2 cajas banco preexistentes de fase 138 (Banco ARS/Banco EUR) tienen esas columnas en NULL tras la migración 0163, y `listBankAccounts()` devuelve TODAS las filas `type='banco'`, así que el listado del ABM las incluye.

    En CuentaBancariaFormDialog.vue `onShow()`, asignar con coalescencia null: `form.bankName = acc.bankName ?? ''` y `form.accountHolder = acc.accountHolder ?? ''` (líneas 183-184). Así el computed `canSubmit` (que llama `.trim()` sobre `form.bankName`/`form.accountHolder`) opera siempre sobre string, no sobre null. El resto de `onShow()` ya usa `?? ''` para los campos opcionales — replicar ese patrón. Verificar que `form.bankName`/`form.accountHolder` siguen siendo `string` (no `string | null`) en el reactive `form` para que el submit y las reglas no cambien de tipo.

    No tocar la regla uno-de-dos ni el submit: el fix es solo saneo de entrada en edición. El comportamiento esperado tras el fix: abrir 'Editar' sobre Banco ARS/Banco EUR muestra los campos vacíos (editables) sin crash, y `canSubmit` queda en false hasta que el usuario complete Banco + Titular + un identificador (comportamiento correcto de completar una cuenta legacy incompleta).

  </action>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit 2>&1 | grep -E "transaction.ts|CuentaBancariaFormDialog.vue" || echo "sin errores nuevos en archivos de fase"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "bankName: string | null" el-templo-admin/src/types/transaction.ts` encuentra la línea dentro de la interface `BankAccount`.
    - `grep -n "accountHolder: string | null" el-templo-admin/src/types/transaction.ts` encuentra la línea dentro de la interface `BankAccount`.
    - `grep -n "acc.bankName ?? ''" el-templo-admin/src/components/caja/CuentaBancariaFormDialog.vue` y `grep -n "acc.accountHolder ?? ''" ...` encuentran las asignaciones en onShow().
    - `npx vue-tsc --noEmit` no introduce errores nuevos en transaction.ts ni CuentaBancariaFormDialog.vue (errores preexistentes fuera de scope son aceptables).
    - Comportamiento: al abrir 'Editar' sobre una cuenta con bankName/accountHolder null, `form.bankName.trim()` no lanza TypeError (el valor es '').
  </acceptance_criteria>
  <done>Editar una cuenta banco legacy (bankName/accountHolder NULL) abre el dialog con campos vacíos editables sin crash; el tipo frontend refleja la nullability del backend.</done>
</task>

<task type="auto">
  <name>Task 3: Gate isOwner del botón 'Registrar retiro' + notify de prefill fallido (CR-03)</name>
  <files>el-templo-admin/src/components/caja/CuentasTab.vue, el-templo-admin/src/components/caja/RegistrarMovEgresoDialog.vue</files>
  <read_first>
    - el-templo-admin/src/components/caja/CuentasTab.vue (leer líneas 38-64: el botón 'Registrar retiro' 44-53 sin gate; línea 121-128: props `isOwner` declarada; función `openRetiro` 243-246)
    - el-templo-admin/src/components/caja/RegistrarMovEgresoDialog.vue (leer líneas 195-272: `cajas`, `loadCajas`, y `onShow()` 256-272 con el prefill de caja 264-267 que hace no-op silencioso si no encuentra la caja; `$q` ya usado en línea 215)
    - .planning/phases/150-cuentas-bancarias-flexibles/150-VERIFICATION.md (gap CR-03: caja banco excluida para no-owner por `branchId===null` skip + enforceCajaScope 404)
    - .planning/phases/150-cuentas-bancarias-flexibles/150-REVIEW.md (CR-03 fix propuesto: opción (a) gate + notify)
  </read_first>
  <action>
    En CuentasTab.vue, agregar `v-if="isOwner"` al botón `<q-btn>` de 'Registrar retiro' (líneas 44-53, el que tiene `icon="payments"` y `@click="openRetiro(...)"`). La prop `isOwner` ya está declarada (línea 123) y ya se pasa a los dialogs hijos — solo falta usarla como gate de esta acción. Motivo: el flujo de retiro sobre cuentas banco (branchId=null) solo funciona para role='owner'; `scope.isOwner` es true únicamente para 'owner' (country-scope.ts:80). Para un admin no-owner, `GET /cash-registers/balances` excluye las cajas banco (`if (c.branchId === null) continue`) y `enforceCajaScope` devuelve 404 — mostrar el botón induce mala imputación. Gatearlo por owner alinea la UI con la capacidad real del backend (CTA-03 = owner-only en esta fase).

    En RegistrarMovEgresoDialog.vue `onShow()`, en el bloque de prefill de caja (líneas 264-267): cuando `props.prefillCajaId !== undefined` pero `cajas.value.find(...)` NO encuentra la caja objetivo, emitir `$q.notify` con `type: 'warning'` y un mensaje del estilo "La caja indicada no está disponible en tu vista; seleccioná una caja manualmente." en vez del no-op silencioso actual. Mantener la asignación `egreso.cajaId = caja.cashRegisterId` cuando sí se encuentra. Este notify solo dispara cuando se pidió prefill (retiro) y falló — el egreso normal (sin prefillCajaId) no lo activa. `$q` ya está disponible en el componente.

    No cambiar el backend en esta fase (no ampliar enforceCajaScope): la decisión de CONTEXT/REVIEW es cerrar CR-03 con la opción mínima (a) gate + notify, documentando CTA-03 como owner-only. No tocar `openRetiro` salvo que sea necesario.

  </action>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit 2>&1 | grep -E "CuentasTab.vue|RegistrarMovEgresoDialog.vue" || echo "sin errores nuevos en archivos de fase"</automated>
  </verify>
  <acceptance_criteria>
    - En CuentasTab.vue, el `<q-btn>` con `icon="payments"` y `@click="openRetiro(...)"` tiene `v-if="isOwner"`. Verificable: el bloque del botón de retiro (líneas ~44-53) contiene `v-if="isOwner"`.
    - `grep -n "isOwner" el-templo-admin/src/components/caja/CuentasTab.vue` muestra el uso como `v-if` en el botón de retiro (no solo en el pase de props a hijos).
    - En RegistrarMovEgresoDialog.vue onShow(), la rama de prefill de caja llama `$q.notify` con `type: 'warning'` cuando `props.prefillCajaId !== undefined` y no hay caja coincidente. Verificable: `grep -n "warning" RegistrarMovEgresoDialog.vue` dentro del bloque de prefill.
    - `npx vue-tsc --noEmit` no introduce errores nuevos en los 2 archivos (preexistentes fuera de scope aceptables).
  </acceptance_criteria>
  <done>El botón 'Registrar retiro' solo aparece para el owner; si el prefill de caja no encuentra la caja objetivo, el usuario recibe un aviso en vez de un no-op silencioso.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                                                                                                                                                                                          |
| --------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| cliente admin → API finance | Un usuario staff autenticado (owner/admin/gestion/recepcion/coach) invoca endpoints del módulo finance directamente, saltando el nav del frontend. La seguridad real vive en la API (149 D-04); el nav solo esconde. |

## STRIDE Threat Register

| Threat ID   | Category                   | Component                                              | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                          |
| ----------- | -------------------------- | ------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-150-06-01 | Information Disclosure     | `GET /cash-registers` (routes.ts:1289)                 | mitigate    | Agregar guard en-handler `ADMIN_ROLES` (Task 1). Sin él, gestion/recepcion (que pasan el hook `FINANCE_READ_ROLES`) leen CBU/CVU/alias/CUIT/número de cuenta y saldo firme de cuentas banco country-agnostic vía API directa, violando D-12 y el invariante de fase 141 (cajas branch-less → owner-only para no-owners). |
| T-150-06-02 | Elevation of Privilege     | ABM lectura/ciclo de vida (GET/PATCH/close/reactivate) | mitigate    | Tests RBAC (Task 1) que aseveran 403 para gestion en GET y en las 3 escrituras — previenen regresión del guard. La escritura ya estaba mitigada en-handler; el gap era la ausencia de cobertura que dejó pasar CR-02.                                                                                                    |
| T-150-06-03 | Tampering (mis-imputación) | Flujo 'Registrar retiro' para admin no-owner           | mitigate    | Gate `v-if="isOwner"` + notify de prefill fallido (Task 3). Sin gate, un admin no-owner completa el retiro del dueño contra una caja efectivo de sucursal equivocada (dinero mal imputado) porque la caja banco objetivo está ausente de su selector.                                                                    |

Nota: no hay tareas de instalación de paquetes (npm/pip/cargo) en este plan — no aplica el gate de legitimidad de paquetes ni el threat T-{phase}-SC.
</threat_model>

<verification>
- Backend compila: `cd el-templo-api && pnpm build` sin errores.
- Frontend sin errores nuevos de tipos en los 4 archivos de fase: `cd el-templo-admin && npx vue-tsc --noEmit` (errores preexistentes fuera de scope son aceptables, per la verificación original).
- Tests de integración (corren en CI, no localmente — convención del proyecto): bank-accounts.test.ts incluye los casos RBAC nuevos de GET/PATCH/close/reactivate → 403 para gestion.
- Gaps cerrados: CR-01 (editar cuenta legacy no crashea), CR-02 (GET admin/owner-only), CR-03 (botón retiro owner-only + notify).
</verification>

<success_criteria>

- Editar una cuenta banco legacy (bankName/accountHolder NULL) abre el dialog sin TypeError.
- `GET /cash-registers` devuelve 403 para gestion/recepcion, con test RBAC que lo cubre.
- El botón 'Registrar retiro' solo se renderiza para el owner; prefill fallido notifica en vez de no-opear.
- `pnpm build` (API) verde; `vue-tsc` sin errores nuevos en archivos de fase 150; sin `any`, sin `console.*`.
  </success_criteria>

<output>
Create `.planning/phases/150-cuentas-bancarias-flexibles/150-06-SUMMARY.md` when done.
</output>
