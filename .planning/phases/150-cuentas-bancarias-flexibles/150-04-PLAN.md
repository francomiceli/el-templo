---
phase: 150-cuentas-bancarias-flexibles
plan: 04
type: execute
wave: 4
depends_on: ["150-03"]
files_modified:
  - el-templo-admin/src/types/transaction.ts
  - el-templo-admin/src/composables/useTransactionsApi.ts
  - el-templo-admin/src/components/caja/CuentaBancariaFormDialog.vue
autonomous: true
requirements: [CTA-01]
must_haves:
  truths:
    - "El admin puede abrir un dialog para crear/editar una cuenta bancaria con los campos flexibles"
    - "El form exige solo Banco + Titular + Moneda y al menos CBU/CVU o Alias (espeja el backend)"
    - "El form no pide 'Nombre' (se deriva en el backend)"
    - "En modo edición el selector de moneda está deshabilitado (moneda fija post-creación, D-04)"
  artifacts:
    - path: "el-templo-admin/src/components/caja/CuentaBancariaFormDialog.vue"
      provides: "Dialog reutilizable de alta/edición de cuenta bancaria"
      contains: "q-dialog"
      min_lines: 80
    - path: "el-templo-admin/src/composables/useTransactionsApi.ts"
      provides: "Métodos API del ABM de cuentas bancarias"
      contains: "createBankAccount"
    - path: "el-templo-admin/src/types/transaction.ts"
      provides: "Tipos BankAccount/CreateBankAccountInput/UpdateBankAccountInput"
      contains: "BankAccount"
  key_links:
    - from: "el-templo-admin/src/components/caja/CuentaBancariaFormDialog.vue"
      to: "useTransactionsApi.createBankAccount"
      via: "submit del form"
      pattern: "createBankAccount|updateBankAccount"
    - from: "el-templo-admin/src/composables/useTransactionsApi.ts"
      to: "/admin/finance/cash-registers"
      via: "api.post/patch"
      pattern: "cash-registers"
---

<objective>
Construir la capa de datos del frontend del ABM (tipos + métodos de `useTransactionsApi`) y el `CuentaBancariaFormDialog.vue` reutilizable de alta/edición de cuentas bancarias.

Purpose: Habilita CTA-01 en la UI con un form flexible (3 obligatorios, sin campo Nombre, moneda fija al editar) diseñado como componente reutilizable que la fase 151 (COBRO-04) montará inline en el flujo de cobro (D-11).
Output: Tipos frontend, 5 métodos API, y el dialog de form reutilizable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/150-cuentas-bancarias-flexibles/150-CONTEXT.md
@.planning/phases/150-cuentas-bancarias-flexibles/150-PATTERNS.md
@.planning/phases/150-cuentas-bancarias-flexibles/150-03-SUMMARY.md

<interfaces>
<!-- Endpoints backend disponibles tras el plan 03 (prefijo axios base ya incluye /api) -->
POST   /admin/finance/cash-registers            → { account: BankAccount }
PATCH  /admin/finance/cash-registers/:id        → { account: BankAccount }
POST   /admin/finance/cash-registers/:id/close  → { account, balance }
POST   /admin/finance/cash-registers/:id/reactivate → { account }
GET    /admin/finance/cash-registers            → { accounts: BankAccount[] }

BankAccount { id:number; name:string; currency:'ARS'|'EUR'; isActive:boolean; bankName:string; accountHolder:string; taxId:string|null; cbuCvu:string|null; accountAlias:string|null; accountNumber:string|null; balance:number }
→ `balance` = saldo firme (nunca suma pendiente, CAJA-03).
Campos obligatorios del form (D-02): bankName, accountHolder, currency + (cbuCvu O accountAlias). Moneda default ARS (D-04) y FIJA post-creación (no editable en modo edición). Sin campo Nombre (D-03).
El PATCH de update NO envía `currency` (moneda fija; `UpdateBankAccountInput` no la incluye).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tipos frontend + métodos API del ABM en useTransactionsApi</name>
  <files>el-templo-admin/src/types/transaction.ts, el-templo-admin/src/composables/useTransactionsApi.ts</files>
  <read_first>
    - el-templo-admin/src/types/transaction.ts (analog: CostCenter, CajaSaldoRow, RegisterExpenseInput)
    - el-templo-admin/src/composables/useTransactionsApi.ts (analog: registerExpense líneas 274-286, getCostCenters líneas 294-308; import api línea 7, extractError línea 8; return object líneas 560-594)
  </read_first>
  <action>
    En `types/transaction.ts` agregar interfaces exportadas sin `any`: `BankAccount` (forma de `<interfaces>`), `CreateBankAccountInput` (bankName: string; accountHolder: string; currency: 'ARS'|'EUR'; cbuCvu?: string|null; accountAlias?: string|null; taxId?: string|null; accountNumber?: string|null), `UpdateBankAccountInput` (los mismos campos que Create pero todos opcionales, SIN `currency` — moneda fija post-creación, D-04). En `useTransactionsApi.ts` agregar 5 métodos siguiendo el patrón exacto de `registerExpense` (set loading/error, try `api.post/patch/get` tipado, catch `extractError` + throw, finally reset loading): `listBankAccounts(): Promise<{ accounts: BankAccount[] }>` (GET), `createBankAccount(input): Promise<{ account: BankAccount }>` (POST), `updateBankAccount(id, input): Promise<{ account: BankAccount }>` (PATCH, `input: UpdateBankAccountInput` sin currency), `closeBankAccount(id): Promise<{ account: BankAccount; balance: number }>` (POST /:id/close), `reactivateBankAccount(id): Promise<{ account: BankAccount }>` (POST /:id/reactivate). Rutas exactas bajo `/admin/finance/cash-registers`. Agregar los 5 al objeto `return {}` del composable. Mensajes de `extractError` en español.
  </action>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - transaction.ts exporta `BankAccount`, `CreateBankAccountInput`, `UpdateBankAccountInput` sin `any`
    - `UpdateBankAccountInput` NO incluye `currency` (moneda fija post-creación, D-04)
    - useTransactionsApi expone y retorna `listBankAccounts`, `createBankAccount`, `updateBankAccount`, `closeBankAccount`, `reactivateBankAccount`
    - Las rutas usan `/admin/finance/cash-registers` (POST/PATCH/GET) con los sufijos /close y /reactivate
    - Cada método usa `extractError` en catch y resetea loading en finally
    - `vue-tsc --noEmit` sin errores
  </acceptance_criteria>
  <done>Tipos frontend (Update sin currency) y 5 métodos API del ABM listos y tipados; vue-tsc verde.</done>
</task>

<task type="auto">
  <name>Task 2: CuentaBancariaFormDialog.vue reutilizable (alta/edición)</name>
  <files>el-templo-admin/src/components/caja/CuentaBancariaFormDialog.vue</files>
  <read_first>
    - el-templo-admin/src/components/caja/RegistrarMovEgresoDialog.vue (patrón dialog completo: show computed líneas 177-180; props/emits líneas 162-171; reactive form + computed canSubmit líneas 270-321; submit try/catch+notify líneas 355-375; resetAll @hide líneas 377-388; onUnmounted cleanup líneas 390-392)
    - el-templo-admin/src/composables/useTransactionsApi.ts (métodos del Task 1)
  </read_first>
  <action>
    Crear el SFC reutilizable siguiendo el patrón de `RegistrarMovEgresoDialog.vue`. Props: `{ modelValue: boolean; selectedCountry: 'AR'|'ES'; isOwner: boolean; account?: BankAccount | null }` (cuando `account` está presente = modo edición; ausente = alta). Emits: `update:modelValue`, `saved`. `show` computed get/set sobre `modelValue`. Estado `reactive` del form con: bankName, accountHolder, currency (default 'ARS', select ARS/EUR — D-04), cbuCvu, accountAlias, taxId, accountNumber. SIN campo "Nombre" (derivado en backend, D-03). Un `computed isEditMode = computed(() => !!props.account)`. El select de moneda se renderiza SIEMPRE, pero en modo edición (`isEditMode` true) va `:disable="true"` / read-only (la moneda es FIJA post-creación, invariante `cash_registers.currency`, D-04) — mostrar la moneda de la cuenta sin permitir cambiarla. `computed canSubmit`: bankName y accountHolder no vacíos, currency presente, y (cbuCvu no vacío O accountAlias no vacío) — espejo de la regla uno-de-dos del backend (D-02). Marcar Banco/Titular/Moneda como obligatorios en la UI (asterisco/rules Quasar). Validación de formato liviana (CBU/CVU numérico ~22 díg., CUIT ~11 díg.) SIN bloquear cuentas del exterior (Claude's Discretion). En modo edición, prellenar el form desde `props.account` en `onShow`/watch. submit: si hay `account` → `updateBankAccount(account.id, input)` donde `input` es `UpdateBankAccountInput` que NO incluye `currency` (moneda fija, D-04); si no → `createBankAccount(input)` (incluye currency); luego `$q.notify` positive → `show.value=false` → `emit('saved')`; catch `extractError` + notify negative. `resetAll` en `@hide`. `onUnmounted(() => transactionsApi.cleanup())` en el SFC (NUNCA dentro del composable). `createLogger()` nunca console. Sin `any`.
  </action>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `el-templo-admin/src/components/caja/CuentaBancariaFormDialog.vue` existe con `q-dialog v-model` sobre `show`
    - Props incluyen `modelValue`, `selectedCountry`, `isOwner`, `account?`; emits incluyen `saved`
    - El form NO tiene un campo "Nombre" (grep sin match de un input de name visible)
    - `canSubmit` exige bankName+accountHolder+currency y (cbuCvu O accountAlias)
    - Moneda default 'ARS' con opciones ARS/EUR en modo alta; en modo edición (`account` presente) el select de moneda está deshabilitado/read-only (D-04)
    - El `input` del PATCH de update NO incluye `currency`
    - submit ramifica create vs update según `account`
    - `onUnmounted` presente en el SFC llamando cleanup; sin `console.*`; sin `any`
    - `vue-tsc --noEmit` sin errores
  </acceptance_criteria>
  <done>Dialog de alta/edición de cuenta bancaria reutilizable con validación uno-de-dos espejada, sin campo Nombre y moneda deshabilitada al editar (D-04); vue-tsc verde.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary       | Description                                                                                                           |
| -------------- | --------------------------------------------------------------------------------------------------------------------- |
| UI admin → API | El form envía datos bancarios; la validación del cliente es UX/defensa-en-profundidad, la autoritativa vive en la API |

## STRIDE Threat Register

| Threat ID | Category               | Component                            | Disposition | Mitigation Plan                                                                                                               |
| --------- | ---------------------- | ------------------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------------- |
| T-150-10  | Elevation              | acceso al form desde el frontend     | accept      | El nav/superficie es admin/owner-only (fase 149) pero la seguridad real la impone la API (plan 03); esconder en UI es solo UX |
| T-150-11  | Tampering              | validación uno-de-dos en `canSubmit` | mitigate    | El cliente valida por UX, pero el backend re-valida en el service (plan 02); no se confía en el gate del frontend             |
| T-150-12  | Information Disclosure | extractError en notify               | mitigate    | `extractError` mapea a mensajes en español sin exponer stack/internals; `createLogger` en vez de console                      |
| T-150-16  | Tampering              | moneda en modo edición               | mitigate    | Select de moneda deshabilitado al editar + el PATCH no incluye `currency`; el backend re-enforcea la inmutabilidad (D-04)     |

Sin instalación de paquetes.
</threat_model>

<verification>
- `vue-tsc --noEmit` verde en el-templo-admin
- 5 métodos API expuestos por el composable (`UpdateBankAccountInput` sin currency)
- Dialog reutilizable con validación uno-de-dos, sin campo Nombre y moneda fija al editar
</verification>

<success_criteria>

- Capa de datos frontend del ABM lista (tipos + API)
- Form dialog de alta/edición reutilizable (CTA-01), con moneda fija post-creación (D-04), listo para reuso en fase 151
  </success_criteria>

<output>
Create `.planning/phases/150-cuentas-bancarias-flexibles/150-04-SUMMARY.md` when done
</output>
</output>
