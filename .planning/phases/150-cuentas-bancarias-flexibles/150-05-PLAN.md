---
phase: 150-cuentas-bancarias-flexibles
plan: 05
type: execute
wave: 5
depends_on: ["150-04"]
files_modified:
  - el-templo-admin/src/components/caja/CuentasTab.vue
  - el-templo-admin/src/constants/caja.ts
  - el-templo-admin/src/pages/CajaPage.vue
  - el-templo-admin/src/components/caja/RegistrarMovEgresoDialog.vue
autonomous: false
requirements: [CTA-01, CTA-02, CTA-03]
must_haves:
  truths:
    - "El admin ve un tab 'Cuentas' en CajaPage con el listado de cuentas bancarias (activas y cerradas atenuadas)"
    - "El admin puede crear, editar, cerrar y reactivar cuentas desde el tab"
    - "Cerrar una cuenta con saldo distinto de 0 muestra un warning pero deja cerrar"
    - "El admin puede registrar un retiro del dueño con centro 'Retiros' preseleccionado desde una fila del ABM"
  artifacts:
    - path: "el-templo-admin/src/components/caja/CuentasTab.vue"
      provides: "Listado ABM de cuentas con acciones editar/cerrar/reactivar/retiro"
      contains: "listBankAccounts"
      min_lines: 90
    - path: "el-templo-admin/src/pages/CajaPage.vue"
      provides: "Tab 'Cuentas' montado en el hub de Caja"
      contains: "CuentasTab"
    - path: "el-templo-admin/src/components/caja/RegistrarMovEgresoDialog.vue"
      provides: "Prefill de retiro (centro 'Retiros' + caja)"
      contains: "prefill"
  key_links:
    - from: "el-templo-admin/src/components/caja/CuentasTab.vue"
      to: "CuentaBancariaFormDialog"
      via: "montaje del dialog de alta/edición"
      pattern: "CuentaBancariaFormDialog"
    - from: "el-templo-admin/src/components/caja/CuentasTab.vue"
      to: "RegistrarMovEgresoDialog"
      via: "acción 'Registrar retiro' con prefill 'Retiros'"
      pattern: "Retiros|prefill"
    - from: "el-templo-admin/src/pages/CajaPage.vue"
      to: "CAJA_TABS.cuentas"
      via: "q-tab + q-tab-panel"
      pattern: "cuentas"
---

<objective>
Ensamblar la superficie del ABM: el tab "Cuentas" dentro de `CajaPage.vue` con el listado y acciones (crear/editar/cerrar/reactivar), y el prefill de "Registrar retiro" en `RegistrarMovEgresoDialog.vue` (D-10).

Purpose: Cierra CTA-01 (crear desde la UI), CTA-02 (cerrar/reactivar con warning de saldo D-06 y cerradas atenuadas D-07) y CTA-03 (retiro del dueño con centro 'Retiros' preseleccionado), todo dentro de Caja (D-11).
Output: Tab "Cuentas" funcional + retiro prellenado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/150-cuentas-bancarias-flexibles/150-CONTEXT.md
@.planning/phases/150-cuentas-bancarias-flexibles/150-PATTERNS.md
@.planning/phases/150-cuentas-bancarias-flexibles/150-04-SUMMARY.md

<interfaces>
<!-- Del plan 04 -->
Componente reutilizable: CuentaBancariaFormDialog.vue → props { modelValue; selectedCountry; isOwner; account? }, emit 'saved'
Composable: useTransactionsApi → listBankAccounts / createBankAccount / updateBankAccount / closeBankAccount(id)→{account,balance} / reactivateBankAccount
BankAccount { id; name; currency; isActive; bankName; accountHolder; taxId; cbuCvu; accountAlias; accountNumber; balance }

<!-- Del dialog de egresos existente -->

RegistrarMovEgresoDialog.vue ya carga todas las cajas activas (líneas 199-213) y centros de costo (líneas 228-248); preselección por name (patrón líneas 237-239).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prefill de retiro en RegistrarMovEgresoDialog.vue</name>
  <files>el-templo-admin/src/components/caja/RegistrarMovEgresoDialog.vue</files>
  <read_first>
    - el-templo-admin/src/components/caja/RegistrarMovEgresoDialog.vue (carga de cajas líneas 199-213; carga de centros líneas 228-248; preselección por name líneas 237-239; props/emits líneas 162-171; onShow/resetAll líneas 377-388)
  </read_first>
  <action>
    Agregar props opcionales de prefill sin romper el uso actual: `prefillTab?: 'egreso'`, `prefillCajaId?: number`, `prefillCostCenterName?: string`. Al mostrarse (onShow, tras cargar cajas y centros), si `prefillCostCenterName` está presente buscar `data.find((c) => c.name === prefillCostCenterName)` y fijar `egreso.costCenterId` a ese id (reusando el patrón de preselección de líneas 237-239); si `prefillCajaId` está presente fijar `egreso.cajaId` a ese id; si `prefillTab==='egreso'` abrir en la pestaña de egreso. Sin prefill, comportamiento idéntico al actual. El centro 'Retiros' aparecerá automáticamente en la lista tras el seed 0163. Sin `any`; sin console.
  </action>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - RegistrarMovEgresoDialog.vue declara props `prefillTab?`, `prefillCajaId?`, `prefillCostCenterName?`
    - Con `prefillCostCenterName='Retiros'` el dialog preselecciona el centro por name en onShow
    - Con `prefillCajaId` preselecciona la caja
    - Sin props de prefill, el comportamiento previo no cambia (defaults preservados)
    - `vue-tsc --noEmit` verde
  </acceptance_criteria>
  <done>El dialog de egresos acepta prefill de tab/caja/centro y preselecciona 'Retiros' cuando se lo lanza como retiro; vue-tsc verde.</done>
</task>

<task type="auto">
  <name>Task 2: CuentasTab.vue (listado ABM) + wiring del tab en CajaPage/constants</name>
  <files>el-templo-admin/src/components/caja/CuentasTab.vue, el-templo-admin/src/constants/caja.ts, el-templo-admin/src/pages/CajaPage.vue</files>
  <read_first>
    - el-templo-admin/src/components/caja/SaldosPorCajaTab.vue (analog listado: defineProps líneas 87-90; createLogger/useQuasar/useTransactionsApi líneas 76-94; loadX try/catch+notify líneas 155-168; onMounted+watch+onUnmounted líneas 203-212)
    - el-templo-admin/src/components/caja/MovEgresosTab.vue (patrón q-table si se prefiere sobre cards)
    - el-templo-admin/src/pages/CajaPage.vue (wiring de tabs líneas 23-65; imports script líneas 73-77)
    - el-templo-admin/src/constants/caja.ts (CAJA_TABS / CAJA_TAB_NAMES)
    - el-templo-admin/src/components/caja/CuentaBancariaFormDialog.vue (del plan 04)
  </read_first>
  <action>
    Crear `CuentasTab.vue` siguiendo `SaldosPorCajaTab.vue`: `defineProps<{ selectedCountry: 'AR'|'ES'; isOwner: boolean }>()`, `createLogger`+`useQuasar`+`useTransactionsApi`, `load()` que llama `listBankAccounts()` con try/catch + `$q.notify`, `onMounted(load)` + `watch(() => props.selectedCountry, load)` + `onUnmounted(() => transactionsApi.cleanup())` (onUnmounted SIEMPRE en el SFC). Render (q-table o cards): mostrar name, bankName, accountHolder, currency, balance y estado; las cerradas (isActive=false) atenuadas (D-07). Acciones por fila: (a) Editar → abre `CuentaBancariaFormDialog` con `:account`; (b) header "Nueva cuenta" → abre el dialog sin account; en `@saved` recargar `load()`; (c) Cerrar (solo activas) → confirmar con `$q.dialog`; si `balance !== 0` incluir en el mensaje el saldo actual y la advertencia "los saldos dejarán de reflejar la realidad si no se transfiere antes" pero permitir confirmar (D-06) → `closeBankAccount(id)` → recargar; (d) Reactivar (solo cerradas) → `reactivateBankAccount(id)` → recargar; (e) "Registrar retiro" por fila → montar `RegistrarMovEgresoDialog` con `:prefill-cost-center-name="'Retiros'"` y `:prefill-caja-id="row.id"` (D-10). Ubicación del botón "Registrar retiro": por fila del ABM (Claude's Discretion). Importar y registrar ambos dialogs. En `constants/caja.ts` agregar `cuentas` a `CAJA_TABS` y `CAJA_TAB_NAMES`. En `CajaPage.vue` agregar `<q-tab :name="CAJA_TABS.cuentas" label="Cuentas" icon="account_balance" />` y su `<q-tab-panel>` con `<CuentasTab :selected-country="selectedCountry" :is-owner="isOwner" />`, e importar el componente en el `<script setup>`. Orden del tab: NO optimizar (fase 152 reordena, Claude's Discretion). Sin `any`; `createLogger` nunca console.
  </action>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `CuentasTab.vue` existe, llama `listBankAccounts()` y renderiza activas + cerradas (atenuadas)
    - Tiene acciones editar / cerrar / reactivar / "Registrar retiro" y "Nueva cuenta"
    - Cerrar con `balance !== 0` muestra el saldo y la advertencia pero permite confirmar (D-06)
    - "Registrar retiro" monta RegistrarMovEgresoDialog con `prefill-cost-center-name='Retiros'` y `prefill-caja-id`
    - `constants/caja.ts` incluye `cuentas` en CAJA_TABS y CAJA_TAB_NAMES
    - `CajaPage.vue` renderiza el q-tab "Cuentas" y el panel con `<CuentasTab>`
    - `onUnmounted` en el SFC llamando cleanup; sin `console.*`; sin `any`
    - `vue-tsc --noEmit` y `pnpm lint` verdes
  </acceptance_criteria>
  <done>El tab "Cuentas" está montado en Caja con listado, ABM completo (crear/editar/cerrar/reactivar con warning D-06) y acción de retiro prellenado; vue-tsc y lint verdes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verificación visual del ABM de cuentas y del retiro</name>
  <what-built>El tab "Cuentas" en Caja (CajaPage) con el ABM de cuentas bancarias flexibles y el botón "Registrar retiro" prellenado. Backend, migración 0163 y tests ya aplicados en waves previas.</what-built>
  <how-to-verify>
    1. Ingresar al admin como owner y abrir Caja → tab "Cuentas".
    2. Crear una cuenta: Banco + Titular + Moneda (ARS por default) + Alias → guardar. Confirmar que aparece en el listado con un nombre visible derivado (p.ej. "Galicia · templo.gym") y que NO se pidió un campo "Nombre".
    3. Intentar crear una cuenta sin CBU/CVU ni Alias → el botón de guardar debe estar deshabilitado (regla uno-de-dos).
    4. Editar Galicia/Mercado Pago y completar CUIT/CBU → guardar y verificar que el nombre se mantiene coherente.
    5. Cerrar una cuenta con saldo distinto de 0 → confirmar que aparece el warning con el saldo y permite cerrar; la cuenta queda atenuada en el listado.
    6. Reactivar esa cuenta → vuelve a estado activo.
    7. Desde una fila, "Registrar retiro" → el dialog de egreso abre con centro "Retiros" y la cuenta preseleccionados; registrar un retiro y confirmar que impacta el saldo.
    8. (RBAC) Con un usuario empleado (gestion/coach) confirmar que Caja/Cuentas no es accesible.
  </how-to-verify>
  <resume-signal>Escribí "approved" o describí los problemas encontrados.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary       | Description                                                                            |
| -------------- | -------------------------------------------------------------------------------------- |
| UI admin → API | Acciones de cerrar/reactivar/retiro cruzan a endpoints admin/owner; la UI solo esconde |

## STRIDE Threat Register

| Threat ID | Category    | Component                               | Disposition | Mitigation Plan                                                                                                                          |
| --------- | ----------- | --------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| T-150-13  | Elevation   | acceso al tab "Cuentas"                 | accept      | Gating de nav admin/owner (fase 149) como UX; la autorización real la imponen los endpoints (plan 03), verificada en UAT paso 8          |
| T-150-14  | Repudiation | confirmación de cierre con saldo≠0      | mitigate    | El `$q.dialog` de confirmación muestra el saldo y la advertencia antes de cerrar (D-06); baja lógica conserva historial                  |
| T-150-15  | Tampering   | retiro preseleccionado ('Retiros'/caja) | mitigate    | El prefill solo preselecciona valores; el egreso pasa por `registerExpense` que valida centro activo y caja en el backend (fase 137/139) |

Sin instalación de paquetes. Checkpoint humano de verificación visual/funcional al cierre (no auto-avanzable).
</threat_model>

<verification>
- `vue-tsc --noEmit` y `pnpm lint` verdes en el-templo-admin
- Tab "Cuentas" visible en Caja con ABM completo
- Cierre con warning de saldo (D-06), cerradas atenuadas (D-07)
- Retiro prellenado con centro 'Retiros' (CTA-03)
- Checkpoint humano aprobado
</verification>

<success_criteria>

- CTA-01: crear cuenta bancaria flexible desde la UI (3 obligatorios)
- CTA-02: cerrar/reactivar con baja lógica conservando historial
- CTA-03: registrar retiro del dueño con centro 'Retiros' impactando el saldo
  </success_criteria>

<output>
Create `.planning/phases/150-cuentas-bancarias-flexibles/150-05-SUMMARY.md` when done
</output>
