---
phase: 150-cuentas-bancarias-flexibles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/schema/cash-registers.ts
  - el-templo-api/src/modules/finance/types.ts
  - el-templo-api/src/db/migrations/0163_bank_accounts_and_retiros.sql
autonomous: true
requirements: [CTA-01, CTA-03]
must_haves:
  truths:
    - "La tabla cash_registers tiene 6 columnas bancarias nullable (bank_name, account_holder, tax_id, cbu_cvu, account_alias, account_number)"
    - "El centro de costo 'Retiros' existe en cost_centers (país AR) tras aplicar la migración"
    - "La migración es idempotente: reaplicarla no duplica el seed ni falla"
    - "UpdateBankAccountInput NO incluye currency (moneda fija post-creación, D-04)"
  artifacts:
    - path: "el-templo-api/src/db/schema/cash-registers.ts"
      provides: "Columnas bancarias nullable en el modelo Drizzle"
      contains: "bank_name"
    - path: "el-templo-api/src/db/migrations/0163_bank_accounts_and_retiros.sql"
      provides: "ALTER de columnas bancarias + seed idempotente 'Retiros'"
      contains: "ADD COLUMN"
    - path: "el-templo-api/src/modules/finance/types.ts"
      provides: "Tipos CreateBankAccountInput/UpdateBankAccountInput/BankAccountRow"
      contains: "CreateBankAccountInput"
  key_links:
    - from: "el-templo-api/src/db/schema/cash-registers.ts"
      to: "el-templo-api/src/db/migrations/0163_bank_accounts_and_retiros.sql"
      via: "nombres de columna byte-for-byte"
      pattern: "bank_name|account_holder|tax_id|cbu_cvu|account_alias|account_number"
---

<objective>
Extender el modelo `cash_registers` con las columnas bancarias flexibles (D-01) y sembrar el centro de costo "Retiros" (D-09), estableciendo la base de datos y los contratos de tipos sobre los que se construye el ABM de cuentas bancarias y el retiro del dueño.

Purpose: Levanta el diferido CAJA-F1 de v5.3 (las cuentas banco hoy son seeds sin campos bancarios) y habilita CTA-01 (crear cuenta con campos flexibles) y CTA-03 (retiro como egreso con centro "Retiros").
Output: Schema Drizzle extendido, tipos de dominio nuevos, y la migración 0163 aplicada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/150-cuentas-bancarias-flexibles/150-CONTEXT.md
@.planning/phases/150-cuentas-bancarias-flexibles/150-PATTERNS.md

<interfaces>
<!-- Nombres de columna que la migración 0163 y el schema Drizzle DEBEN compartir byte-for-byte -->
Columnas nuevas (todas nullable, AFTER `currency`):
- SQL `bank_name` varchar(100)      → Drizzle `bankName`
- SQL `account_holder` varchar(120) → Drizzle `accountHolder`
- SQL `tax_id` varchar(20)          → Drizzle `taxId`        (CUIT)
- SQL `cbu_cvu` varchar(34)         → Drizzle `cbuCvu`
- SQL `account_alias` varchar(60)   → Drizzle `accountAlias`
- SQL `account_number` varchar(50)  → Drizzle `accountNumber`

`name` (varchar 100 NOT NULL) NO cambia: sigue existiendo y se autogenera en el service (D-03, plan 02).
`currency` (invariante existente) NO cambia y es FIJA post-creación (D-04): NO forma parte de `UpdateBankAccountInput`.
`balance` en `BankAccountRow` = saldo FIRME (`firmeBalance` de `getBalance`), NUNCA la suma con `pendienteAmount` (invariante CAJA-03).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender schema cash-registers.ts y agregar tipos de dominio</name>
  <files>el-templo-api/src/db/schema/cash-registers.ts, el-templo-api/src/modules/finance/types.ts</files>
  <read_first>
    - el-templo-api/src/db/schema/cash-registers.ts (estado actual: mysqlTable con id/name/type/branchId/currency/openingBalance/cutoffDate/isActive/createdAt/updatedAt en líneas 29-54)
    - el-templo-api/src/db/schema/cost-centers.ts (patrón de columna varchar nullable, línea 19)
    - el-templo-api/src/modules/finance/types.ts (analog: CostCenterItem, RegisterExpenseInput, CajaSaldoRow)
    - el-templo-api/src/modules/finance/cash-register-service.ts (getBalance líneas 150-228: devuelve firmeBalance + pendienteAmount, NUNCA sumados — CAJA-03)
  </read_first>
  <action>
    Agregar las 6 columnas bancarias nullable dentro del objeto `mysqlTable("cash_registers", {...})`, usando el helper `varchar(<col>, { length: N })` SIN `.notNull()`, con estos pares nombre-Drizzle → nombre-SQL exactos: `bankName`→"bank_name" (100), `accountHolder`→"account_holder" (120), `taxId`→"tax_id" (20), `cbuCvu`→"cbu_cvu" (34), `accountAlias`→"account_alias" (60), `accountNumber`→"account_number" (50). NO tocar `name` (sigue NOT NULL, se deriva en el service). NO agregar índices nuevos. En types.ts agregar tres interfaces exportadas sin `any`: `CreateBankAccountInput` (bankName: string; accountHolder: string; currency: string; cbuCvu?: string | null; accountAlias?: string | null; taxId?: string | null; accountNumber?: string | null), `UpdateBankAccountInput` (los mismos campos que Create pero todos opcionales, EXCEPTO `currency` que se OMITE por completo — la moneda es FIJA post-creación, invariante existente de `cash_registers.currency`, D-04), y `BankAccountRow` (forma de lectura del ABM: id, name, currency, isActive, bankName, accountHolder, taxId, cbuCvu, accountAlias, accountNumber, balance: number donde `balance` = saldo FIRME (`firmeBalance` de `getBalance`), NUNCA la suma con `pendienteAmount` — CAJA-03). Estos tipos son el contrato que consumen el service (plan 02) y los routes (plan 03).
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'bank_name\|account_holder\|tax_id\|cbu_cvu\|account_alias\|account_number' el-templo-api/src/db/schema/cash-registers.ts` devuelve 6
    - Las 6 columnas nuevas NO tienen `.notNull()` (son nullable)
    - `name` sigue con `.notNull()` intacto
    - types.ts exporta `CreateBankAccountInput`, `UpdateBankAccountInput`, `BankAccountRow` sin usar `any`
    - `UpdateBankAccountInput` NO incluye la propiedad `currency` (moneda fija post-creación, D-04)
    - El comentario/tipo de `BankAccountRow.balance` deja explícito que es el saldo firme (`firmeBalance`), no la suma con el pendiente
    - `pnpm build` (tsc) compila sin errores
  </acceptance_criteria>
  <done>El schema Drizzle expone las 6 columnas bancarias nullable con nombres SQL exactos y types.ts exporta los 3 tipos de dominio (UpdateBankAccountInput sin currency, balance=firme); tsc verde.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Escribir y aplicar la migración 0163 (ALTER + seed Retiros)</name>
  <files>el-templo-api/src/db/migrations/0163_bank_accounts_and_retiros.sql</files>
  <read_first>
    - el-templo-api/src/db/migrations/0161_cost_centers.sql (patrón ALTER ADD COLUMN + seed INSERT...SELECT...FROM DUAL WHERE NOT EXISTS)
    - el-templo-api/src/db/migrations/0160_seed_banco_cuentas.sql (patrón guard NOT EXISTS con derived table para evitar error 1093)
    - el-templo-api/src/db/schema/cost-centers.ts (columnas name/country/is_active del target del seed)
  </read_first>
  <action>
    PRIMERO reverificar el número de migración: listar `el-templo-api/src/db/migrations/` — la última es 0162_created_member_id.sql, así que la nueva es 0163 (si el tren avanzó, usar el siguiente). Escribir el SQL A MANO (NO usar `db:generate` — está roto por drift pre-existente `sessions.goal_plan_type`). Dos bloques separados por `;`: (1) un `ALTER TABLE cash_registers ADD COLUMN ... NULL` por cada una de las 6 columnas (bank_name varchar(100), account_holder varchar(120), tax_id varchar(20), cbu_cvu varchar(34), account_alias varchar(60), account_number varchar(50)), todas `AFTER currency` (o encadenadas). (2) seed idempotente del centro de costo "Retiros" con `INSERT INTO cost_centers (name, country, is_active) SELECT 'Retiros', 'AR', true FROM DUAL WHERE NOT EXISTS (SELECT 1 FROM cost_centers cc WHERE cc.name = 'Retiros' AND cc.country = 'AR')` (país AR per D-09 / Claude's Discretion). REGLA DURA: NUNCA poner `;` dentro de comentarios `--` (el runner splittea por `;` antes de strippear comentarios). Aplicar con `pnpm db:migrate` (runner custom, trackea en `_migrations`). NUNCA `drizzle-kit push` ni `drizzle-kit migrate`. Los nombres de columna deben coincidir byte-for-byte con el schema de Task 1. Commitear el SQL junto al cambio de schema.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm db:migrate && node -e "process.exit(0)"</automated>
  </verify>
  <acceptance_criteria>
    - El archivo se llama `0163_bank_accounts_and_retiros.sql` (o el siguiente número si 0163 ya existe al ejecutar)
    - `pnpm db:migrate` aplica sin error y registra la migración en `_migrations`
    - Reejecutar `pnpm db:migrate` no reaplica (idempotencia del runner) y el seed no duplica 'Retiros'
    - Query `SELECT COUNT(*) FROM cost_centers WHERE name='Retiros' AND country='AR'` devuelve 1
    - Query `SHOW COLUMNS FROM cash_registers` incluye las 6 columnas nuevas como nullable
    - Ningún comentario `--` en el SQL contiene `;`
  </acceptance_criteria>
  <done>La migración 0163 está aplicada: cash_registers tiene las columnas bancarias y cost_centers tiene 'Retiros' (AR); reaplicable sin duplicar.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                                                               |
| ------------------------------ | --------------------------------------------------------------------------------------------------------- |
| migración → base de datos prod | El SQL viaja a prod con el tren staging→master; un DDL mal formado o un `;` en comentario rompe el deploy |

## STRIDE Threat Register

| Threat ID | Category          | Component                   | Disposition | Mitigation Plan                                                                                                                                                              |
| --------- | ----------------- | --------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-150-01  | Tampering         | migración 0163 (DDL)        | mitigate    | Escrita a mano siguiendo 0161/0160; nombres byte-for-byte con el schema; sin `;` en comentarios; aplicada con el runner custom (no drizzle-kit) que trackea en `_migrations` |
| T-150-02  | Denial of Service | seed 'Retiros'              | mitigate    | INSERT idempotente con guard NOT EXISTS (derived table) — reaplicar no duplica ni lanza error 1093                                                                           |
| T-150-03  | Elevation         | columnas bancarias nullable | accept      | Solo definición de datos; el control de acceso vive en los endpoints (plan 03). Sin PII sensible más allá de datos bancarios ya presentes en seeds                           |

No hay instalación de paquetes (npm/pip/cargo) en esta fase: extensión brownfield sobre dependencias existentes. Fila T-150-SC no aplica.
</threat_model>

<verification>
- `pnpm build` (tsc) verde en el-templo-api
- Migración 0163 aplicada y trackeada; reaplicable sin efectos
- 6 columnas bancarias visibles en `SHOW COLUMNS FROM cash_registers`
- 'Retiros' presente exactamente una vez en cost_centers (AR)
- `UpdateBankAccountInput` sin `currency`; `BankAccountRow.balance` = firme
</verification>

<success_criteria>

- cash_registers extendida con 6 columnas bancarias nullable de nombres exactos
- Centro de costo 'Retiros' sembrado idempotentemente (AR)
- Tipos de dominio CreateBankAccountInput/UpdateBankAccountInput/BankAccountRow exportados (Update sin currency, balance=firme)
  </success_criteria>

<output>
Create `.planning/phases/150-cuentas-bancarias-flexibles/150-01-SUMMARY.md` when done
</output>
</output>
