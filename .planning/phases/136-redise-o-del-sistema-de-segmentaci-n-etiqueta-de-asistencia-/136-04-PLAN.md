---
phase: 136-redise-o-del-sistema-de-segmentaci-n-etiqueta-de-asistencia-
plan: 04
type: execute
wave: 2
depends_on: ["136-01"]
files_modified:
  - el-templo-api/src/modules/members/service.ts
  - el-templo-api/src/modules/members/schemas.ts
  - el-templo-app/src/stores/useUserStore.ts
  - el-templo-app/src/modules/progression/components/SegmentGreeting.vue
autonomous: true
requirements: [D-01]
must_haves:
  truths:
    - "El filtro de segmento en la lista de members acepta los 4 valores nuevos y filtra correctamente sobre member_profiles"
    - "El schema de validación del query param `segment` de members rechaza los valores viejos y acepta solo optima/regular/alerta/ausente"
    - "La subquery de segmento de la lista de members lee la columna member_segment y devuelve uno de los 4 valores o NULL"
    - "El member app no rompe al recibir un segment de los 4 nuevos valores ni con segment NULL"
  artifacts:
    - path: "el-templo-api/src/modules/members/service.ts"
      provides: "filtro + subquery de segment sobre member_segment con el enum nuevo"
    - path: "el-templo-api/src/modules/members/schemas.ts"
      provides: "enum del query param segment con los 4 valores nuevos"
    - path: "el-templo-app/src/stores/useUserStore.ts"
      provides: "MemberSegment de 4 valores en el member app"
  key_links:
    - from: "el-templo-api/src/modules/members/schemas.ts"
      to: "el-templo-api/src/modules/members/service.ts"
      via: "el enum validado del query param alimenta el filtro WHERE mp.member_segment = ?"
      pattern: "segment"
    - from: "el-templo-api/src/modules/members/service.ts"
      to: "member_profiles.member_segment"
      via: "subquery + filtro WHERE mp.member_segment = ?"
      pattern: "member_segment"
    - from: "el-templo-app/src/modules/progression/components/SegmentGreeting.vue"
      to: "el-templo-app/src/stores/useUserStore.ts"
      via: "import MemberSegment + SEGMENT_GREETINGS record"
      pattern: "MemberSegment"
---

<objective>
Propagar el enum de 4 bandas a `members/service.ts` (filtro + subquery de la lista de alumnos), al `members/schemas.ts` (enum de validación del query param `segment`) y al member app (`useUserStore` + `SegmentGreeting`), que tipan `MemberSegment` y lo usan como clave de records.

Purpose: La lista de alumnos filtra y muestra el segmento; el schema de Fastify valida el query param `segment` contra un enum — si queda con los 6 valores viejos, el admin (plan 05) que envíe `?segment=alerta` recibe **400** antes de llegar al handler. El member app tipa el segment en el store y lo usa como clave en SEGMENT_GREETINGS — un valor fuera del record rompería el render. El enum debe quedar alineado en los tres lados.
Output: `members/service.ts`, `members/schemas.ts`, `el-templo-app/src/stores/useUserStore.ts`, `SegmentGreeting.vue`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-redise-o-del-sistema-de-segmentaci-n-etiqueta-de-asistencia-/136-CONTEXT.md
@./CLAUDE.md
@.planning/phases/136-redise-o-del-sistema-de-segmentaci-n-etiqueta-de-asistencia-/136-01-SUMMARY.md

<interfaces>
<!-- Del plan 01 -->
MemberSegment = "optima" | "regular" | "alerta" | "ausente"
<!-- columna física DB: member_segment (sin cambio de nombre) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Actualizar enum de schemas.ts + filtro + subquery de segment en members/service.ts</name>
  <read_first>
    - el-templo-api/src/modules/members/schemas.ts:163-172 (enum del query param `segment` — actualmente los 6 valores viejos)
    - el-templo-api/src/modules/members/service.ts:76 (destructuring segment), :158-165 (filtro segment), :246-302 (segmentSubquery), :372 (map de r.segment)
    - el-templo-api/src/modules/segmentation/types.ts (MemberSegment nuevo)
  </read_first>
  <action>
    En `members/schemas.ts` (líneas ~163-172): cambiar el `enum` del query param `segment` de `["nuevo","espartano","intermitente","en_riesgo","digital_warrior","ghost"]` a `["optima","regular","alerta","ausente"]` (D-01). Sin esto, un request `?segment=alerta` del admin (plan 05) es rechazado con 400 por Fastify antes del handler — BLOCKER de runtime que tsc no detecta (es un string en un literal de schema).

    En `members/service.ts`: verificar/actualizar el tipo del parámetro `segment` del filtro a `MemberSegment` (4 valores) importado de `../segmentation/types` (si está tipado localmente como string, alinearlo). La SQL del filtro (`WHERE mp.user_id = users.id AND mp.member_segment = ${segment}`) y la `segmentSubquery` (`SELECT mp.member_segment FROM member_profiles mp ...`) NO cambian su forma — usan el nombre de columna físico `member_segment` que se mantiene. Confirmar que el cast/typing de `r.segment` (línea ~372) no asume los valores viejos. Sin `any`.

  </action>
  <verify>
    <automated>cd el-templo-api && grep -A6 'segment:' src/modules/members/schemas.ts | grep -qE '"(optima|alerta|ausente)"' && ! grep -qE '"(nuevo|espartano|intermitente|en_riesgo|digital_warrior|ghost)"' src/modules/members/schemas.ts && grep -q "member_segment" src/modules/members/service.ts && pnpm tsc --noEmit 2>&1 | grep -iE "members/service|members/schemas" || echo "members API OK"</automated>
  </verify>
  <done>El enum del query param `segment` en schemas.ts usa los 4 valores nuevos (sin valores viejos); el filtro y la subquery operan sobre member_segment con el tipo MemberSegment nuevo; sin referencias a valores viejos en typings.</done>
</task>

<task type="auto">
  <name>Task 2: Actualizar MemberSegment en el member app (store + SegmentGreeting)</name>
  <read_first>
    - el-templo-app/src/stores/useUserStore.ts:11-17 (MemberSegment type), :160 (segment computed)
    - el-templo-app/src/modules/progression/components/SegmentGreeting.vue:19-46 (SEGMENT_GREETINGS Record<MemberSegment, string>)
    - el-templo-app/src/modules/progression/pages/MiTemplo.vue (consume userStore.segment, pasa a ProgramCtaCard/SegmentGreeting)
  </read_first>
  <action>
    En `useUserStore.ts`: reescribir el type `MemberSegment` a `"optima" | "regular" | "alerta" | "ausente"` (D-01). `UserProfile.segment` sigue `MemberSegment | null`. En `SegmentGreeting.vue`: reescribir el record `SEGMENT_GREETINGS` a las 4 claves nuevas (el saludo actual es uniforme "Hola," para todas — mantener ese copy, una entrada por banda) y mantener el fallback `?? 'Hola,'` para `null`. Confirmar que `ProgramCtaCard.vue` solo tipa `MemberSegment | null` sin asumir valores específicos (si itera valores viejos, alinearlo; si solo chequea null, no cambia). Usar `createLogger()` si hiciera falta loguear (nunca console). Sin `any`.

    NOTA de compatibilidad: el member app en producción (Play Store) puede recibir `segment` NULL para miembros <1 mes / sin plan — el fallback `?? 'Hola,'` ya lo cubre. No se requiere build de tienda en esta fase (solo código).

  </action>
  <verify>
    <automated>cd el-templo-app && grep -q "'optima'" src/stores/useUserStore.ts && grep -q "optima:" src/modules/progression/components/SegmentGreeting.vue && pnpm tsc --noEmit 2>&1 | grep -iE "useUserStore|SegmentGreeting" || echo "app segment OK"</automated>
  </verify>
  <done>MemberSegment en el app = 4 valores; SEGMENT_GREETINGS con las 4 claves + fallback para null; typecheck del app limpio para los archivos tocados.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                         |
| ---------------------------- | ------------------------------------------------------------------- |
| admin → GET /members (lista) | Filtro por segmento sobre datos de miembros (admin/recepción scope) |
| member app → GET /me         | El miembro recibe su propio segment (no de otros)                   |

## STRIDE Threat Register

| Threat ID | Category               | Component                         | Disposition | Mitigation Plan                                                                                                                                                                                                  |
| --------- | ---------------------- | --------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-136-09  | Information Disclosure | filtro de segment en members list | accept      | El filtro ya respeta el scope de sede/rol existente del módulo members; no se modifica el alcance, solo el enum.                                                                                                 |
| T-136-10  | Tampering              | segment NULL en el member app     | mitigate    | El fallback `?? 'Hola,'` evita crash con NULL o valores inesperados; el render degrada graceful.                                                                                                                 |
| T-136-17  | Tampering              | enum del query param `segment`    | mitigate    | El JSON schema de Fastify valida `segment` contra los 4 valores; un valor fuera del enum es rechazado (400) antes del handler. Alinear el enum con el front (plan 05) evita el 400 espurio en filtros legítimos. |
| T-136-SC  | Tampering              | npm installs                      | mitigate    | No se instalan paquetes en esta fase.                                                                                                                                                                            |

</threat_model>

<verification>
- `pnpm tsc --noEmit` limpio en api (members) y app (store + SegmentGreeting).
- El enum del query param `segment` en members/schemas.ts usa los 4 valores nuevos.
- El member app no asume valores de segmento viejos como claves de record.
- La subquery/filtro de members usan member_segment con el tipo nuevo.
</verification>

<success_criteria>

- members/schemas.ts valida los 4 valores; members/service.ts filtra/devuelve con el enum nuevo; el member app tipa y renderiza los 4 valores + NULL sin romper.
  </success_criteria>

<output>
Create `.planning/phases/136-redise-o-del-sistema-de-segmentaci-n-etiqueta-de-asistencia-/136-04-SUMMARY.md` when done
</output>
</content>
