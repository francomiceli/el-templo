---
phase: 136-redise-o-del-sistema-de-segmentaci-n-etiqueta-de-asistencia-
plan: 06
type: execute
wave: 3
depends_on: ["136-01", "136-05"]
files_modified:
  - el-templo-api/src/modules/attendance/service.ts
  - el-templo-api/src/modules/scheduling/service.ts
  - el-templo-api/src/modules/scheduling/types.ts
  - el-templo-admin/src/types/scheduling.ts
  - el-templo-admin/src/types/attendance.ts
  - el-templo-admin/src/components/scheduling/MemberTags.vue
  - el-templo-admin/src/components/scheduling/SlotDetailDialog.vue
autonomous: true
requirements: [D-05, D-06, D-12]
must_haves:
  truths:
    - "Los endpoints de slot (getSlotAttendance / getSlotDetail) devuelven una etiqueta de Antigüedad calculada al vuelo desde users.createdAt"
    - "La Antigüedad clasifica: [0,1) meses=Nuevo, [1,3)=1-3m, [3,6)=3-6m, [6,∞)=+6m"
    - "MemberTags muestra SOLO Antigüedad + Asistencia (el chip de avatar A-K ya no aparece en Horarios)"
    - "La lista de asistencia de SlotDetailDialog ya no muestra el badge QR/Manual (cambio previo incluido en esta fase)"
  artifacts:
    - path: "el-templo-api/src/modules/attendance/service.ts"
      provides: "getSlotAttendance con seniority calculado desde users.createdAt"
    - path: "el-templo-api/src/modules/scheduling/service.ts"
      provides: "getSlotDetail con seniority calculado desde users.createdAt"
    - path: "el-templo-admin/src/components/scheduling/MemberTags.vue"
      provides: "chips de Antigüedad + Asistencia (sin avatar)"
  key_links:
    - from: "el-templo-api/src/modules/attendance/service.ts"
      to: "users.createdAt"
      via: "cálculo al vuelo de antigüedad en meses"
      pattern: "createdAt"
    - from: "el-templo-admin/src/components/scheduling/MemberTags.vue"
      to: "el-templo-admin/src/types/member.ts"
      via: "SEGMENT_LABELS/COLORS para Asistencia"
      pattern: "SEGMENT_LABELS"
---

<objective>
Agregar la dimensión NUEVA de **Antigüedad** (Nuevo 0-1m / 1-3m / 3-6m / +6m) calculada AL VUELO desde `users.createdAt` en los endpoints de slot de Horarios (D-05, D-06), y reescribir `MemberTags.vue` para mostrar SOLO Antigüedad + Asistencia, quitando el chip de avatar A-K de Horarios (D-12). Incluir el cambio ya aplicado (sin commitear) que removió el badge QR/Manual de la lista de asistencia.

Purpose: Es la única superficie donde se muestra la Antigüedad (solo Horarios). No se persiste ni necesita columna/enum/cron — se deriva en la query. El par chips que el usuario quiere ver por alumno es Antigüedad + Asistencia, reemplazando el par segmento+avatar.
Output: `attendance/service.ts`, `scheduling/service.ts`, `scheduling/types.ts`, `types/scheduling.ts`, `types/attendance.ts`, `MemberTags.vue`, `SlotDetailDialog.vue`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-redise-o-del-sistema-de-segmentaci-n-etiqueta-de-asistencia-/136-CONTEXT.md
@./CLAUDE.md
@.planning/phases/136-redise-o-del-sistema-de-segmentaci-n-etiqueta-de-asistencia-/136-01-SUMMARY.md

<interfaces>
<!-- Del plan 01: la Asistencia (segment) sigue viniendo de member_profiles.segment con los 4 valores. -->
MemberSegment = "optima" | "regular" | "alerta" | "ausente"

<!-- Nuevo campo a exponer en los slot endpoints (calculado al vuelo) -->

seniority: "nuevo" | "1-3m" | "3-6m" | "6m+" // o null si createdAt ausente (defensivo)
// Bordes en meses desde users.createdAt (D-06): [0,1)=nuevo, [1,3)=1-3m, [3,6)=3-6m, [6,∞)=6m+

<!-- Display de Antigüedad (admin) -->

SENIORITY_LABELS: nuevo→"Nuevo", "1-3m"→"1-3 meses", "3-6m"→"3-6 meses", "6m+"→"+6 meses"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exponer Antigüedad al vuelo en getSlotAttendance + getSlotDetail (backend)</name>
  <read_first>
    - el-templo-api/src/modules/attendance/service.ts:360-489 (getSlotAttendance: bookingRows/attendanceRows leftJoin member_profiles, memberMap)
    - el-templo-api/src/modules/scheduling/service.ts:288-357 (getSlotDetail: bookingRows + map a BookingRecord)
    - el-templo-api/src/modules/scheduling/types.ts:55-74 (BookingRecord: segment/avatarType)
    - el-templo-api/src/db/schema/users.ts (columna createdAt) — confirmar tipo timestamp
  </read_first>
  <action>
    En ambos endpoints: SELECT adicional de `users.createdAt` (ya hacen innerJoin con users). Calcular `seniority` al vuelo (D-06) desde `createdAt`: meses transcurridos = diferencia en días / 30.44 (o usar TIMESTAMPDIFF(MONTH, createdAt, NOW()) en SQL y mapear en JS); clasificar `[0,1)→"nuevo"`, `[1,3)→"1-3m"`, `[3,6)→"3-6m"`, `[6,∞)→"6m+"`. Preferir cálculo en JS sobre el `createdAt` ya traído (evita SQL extra y es testeable). Exponer el campo nuevo `seniority` en cada miembro/booking del resultado.

    D-12 / Specifics: DEJAR DE EXPONER `avatarType` en estos endpoints de slot si ya no lo consume nadie en Horarios (el avatar deja de mostrarse en SlotDetailDialog). Quitar `avatarType` del SELECT, de los tipos inline (getSlotAttendance) y de `BookingRecord` (scheduling/types.ts). El `segment` (Asistencia) se MANTIENE — ahora trae los 4 valores nuevos via member_profiles. Sin `any`; usar tipos explícitos para el union de seniority.

    NOTA: el avatar NO se elimina del sistema (otras pantallas lo siguen usando) — solo se deja de proyectar en los slot endpoints.

  </action>
  <verify>
    <automated>cd el-templo-api && grep -q "seniority" src/modules/attendance/service.ts && grep -q "seniority" src/modules/scheduling/service.ts && grep -q "seniority" src/modules/scheduling/types.ts && ! grep -q "avatarType" src/modules/scheduling/types.ts && pnpm tsc --noEmit 2>&1 | grep -iE "attendance/service|scheduling/service|scheduling/types" || echo "slot endpoints OK"</automated>
  </verify>
  <done>getSlotAttendance y getSlotDetail devuelven `seniority` calculado al vuelo desde createdAt con los 4 bordes; `avatarType` removido de los slot endpoints y de BookingRecord; segment (Asistencia) preservado.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend Horarios — MemberTags (Antigüedad + Asistencia), tipos slot, quitar avatar + badge QR/Manual</name>
  <read_first>
    - el-templo-admin/src/components/scheduling/MemberTags.vue (componente completo a reescribir)
    - el-templo-admin/src/components/scheduling/SlotDetailDialog.vue:130-265 (usos de MemberTags + lista de asistencia; el diff actual ya quitó el badge QR/Manual)
    - el-templo-admin/src/types/scheduling.ts:104-105 (segment/avatarType del booking slot)
    - el-templo-admin/src/types/attendance.ts:44-45 (segment/avatarType del attendance member)
    - el-templo-admin/src/types/member.ts (SEGMENT_LABELS/COLORS, tras plan 05)
  </read_first>
  <action>
    En `types/scheduling.ts` y `types/attendance.ts`: reemplazar el campo `avatarType: string | null` por `seniority: 'nuevo' | '1-3m' | '3-6m' | '6m+' | null` en los shapes de booking/member de slot (alinear con el backend del Task 1). Mantener `segment`.

    Reescribir `MemberTags.vue` (D-12): props pasan a `{ segment: string | null; seniority: string | null }` (quitar `avatarType`). Renderizar SOLO dos chips: (1) Antigüedad — un `q-badge` con `SENIORITY_LABELS` (definir el record local o en types/member: nuevo→"Nuevo", "1-3m"→"1-3 meses", "3-6m"→"3-6 meses", "6m+"→"+6 meses") y un color neutro (ej. grey-7 outline o azul suave); (2) Asistencia — `q-badge` con `SEGMENT_LABELS`/`SEGMENT_COLORS` (los 4 valores nuevos). Eliminar el badge de avatar y el import de `AVATAR_LABELS`. D-07: si `segment` es null (miembro <1 mes / sin plan) NO renderizar el chip de Asistencia, solo Antigüedad.

    En `SlotDetailDialog.vue`: actualizar los 4 usos de `<MemberTags>` para pasar `:seniority="booking.seniority"`/`:seniority="member.seniority"` en vez de `:avatar-type`. Mantener el cambio ya aplicado que removió el badge QR/Manual de la lista de asistencia (incluirlo/commitearlo en esta fase). Sin `console.*`; sin `any`.

  </action>
  <verify>
    <automated>cd el-templo-admin && grep -q "seniority" src/components/scheduling/MemberTags.vue && ! grep -q "AVATAR_LABELS" src/components/scheduling/MemberTags.vue && ! grep -q "avatar-type" src/components/scheduling/SlotDetailDialog.vue && pnpm tsc --noEmit 2>&1 | grep -iE "MemberTags|SlotDetailDialog|types/scheduling|types/attendance" || echo "horarios frontend OK"</automated>
  </verify>
  <done>MemberTags muestra Antigüedad + Asistencia sin avatar; SlotDetailDialog pasa seniority y ya no referencia avatar-type; tipos slot con seniority en vez de avatarType; badge QR/Manual removido; typecheck limpio.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                       |
| --------------------------------- | --------------------------------------------------------------------------------- |
| admin/coach → GET slot (Horarios) | Roster del slot con Antigüedad + Asistencia de miembros (datos de comportamiento) |

## STRIDE Threat Register

| Threat ID | Category               | Component                           | Disposition | Mitigation Plan                                                                                                                                                                                                                     |
| --------- | ---------------------- | ----------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-136-13  | Information Disclosure | seniority/segment en slot endpoints | accept      | Los slot endpoints ya están bajo el guard de roles staff (coach/admin/recepción) y son específicos del slot que el staff gestiona; agregar Antigüedad no amplía el público. Antigüedad se deriva de createdAt (no es PII sensible). |
| T-136-14  | Information Disclosure | avatarType removido de slot         | mitigate    | Reducir el dato proyectado (quitar avatar) disminuye la superficie expuesta en Horarios; el avatar sigue solo donde se necesita.                                                                                                    |
| T-136-15  | Tampering              | cálculo de seniority al vuelo       | mitigate    | El cálculo es server-side desde createdAt confiable (no input del cliente); el cliente solo renderiza el union resultante.                                                                                                          |
| T-136-SC  | Tampering              | npm installs                        | mitigate    | No se instalan paquetes en esta fase.                                                                                                                                                                                               |

</threat_model>

<verification>
- `pnpm tsc --noEmit` limpio en api (attendance/scheduling) y admin (MemberTags/SlotDetailDialog/types).
- Los slot endpoints devuelven seniority con los 4 bordes y ya no devuelven avatarType.
- MemberTags muestra solo Antigüedad + Asistencia; sin chip de avatar; sin badge QR/Manual en la lista.
- Asistencia null (D-07) → no se renderiza el chip de Asistencia, solo Antigüedad.
</verification>

<success_criteria>

- Antigüedad calculada al vuelo y mostrada solo en Horarios junto a la Asistencia; avatar y badge QR/Manual fuera de la lista de Horarios.
  </success_criteria>

<output>
Create `.planning/phases/136-redise-o-del-sistema-de-segmentaci-n-etiqueta-de-asistencia-/136-06-SUMMARY.md` when done
</output>
