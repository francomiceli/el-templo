---
phase: 136-redise-o-del-sistema-de-segmentaci-n-etiqueta-de-asistencia-
plan: 07
type: execute
wave: 3
depends_on: ["136-01", "136-05"]
files_modified:
  - el-templo-api/src/modules/settings/routes.ts
  - el-templo-api/src/modules/settings/service.ts
  - el-templo-api/src/modules/settings/index.ts
  - el-templo-api/src/modules/settings/schemas.ts
  - el-templo-api/src/app.ts
  - el-templo-api/src/db/schema/system-settings.ts
  - el-templo-admin/src/composables/useSettingsApi.ts
  - el-templo-admin/src/pages/ConfiguracionPage.vue
  - el-templo-admin/src/router/routes.ts
  - el-templo-admin/src/layouts/AdminLayout.vue
autonomous: true
requirements: [D-03]
must_haves:
  truths:
    - "El módulo settings de la API (routes + service + index) queda eliminado y desregistrado de app.ts"
    - "Los endpoints GET/PUT /api/admin/settings/segments dejan de existir"
    - "El composable useSettingsApi.ts y la página ConfiguracionPage.vue quedan eliminados"
    - "La ruta /configuracion y su item de nav en AdminLayout quedan eliminados"
    - "La tabla/schema system_settings se PRESERVA (la usa streaks/service.ts)"
    - "pnpm tsc --noEmit queda limpio en api y admin tras eliminar a los consumidores muertos de SegmentThresholds/SEGMENT_SETTINGS_KEYS/SEGMENT_DEFAULTS"
  artifacts:
    - path: "el-templo-api/src/app.ts"
      provides: "sin import ni register de settingsRoutes"
    - path: "el-templo-admin/src/router/routes.ts"
      provides: "sin ruta /configuracion"
    - path: "el-templo-admin/src/layouts/AdminLayout.vue"
      provides: "sin item de nav a /configuracion"
  key_links:
    - from: "el-templo-api/src/app.ts"
      to: "el-templo-api/src/modules/settings"
      via: "register(settingsRoutes) ELIMINADO (módulo borrado)"
      pattern: "settingsRoutes"
    - from: "el-templo-admin/src/pages/ConfiguracionPage.vue"
      to: "el-templo-admin/src/composables/useSettingsApi.ts"
      via: "ambos eliminados (consumían SegmentThresholds, borrado en 136-05)"
      pattern: "useSettingsApi"
---

<objective>
Eliminar por completo el subsistema de **settings de thresholds de segmentación**, que D-03 deja muerto (cortes ahora fijos en código). El plan 01 borra los símbolos `SEGMENT_SETTINGS_KEYS`/`SEGMENT_DEFAULTS`/`SegmentThresholds`/`getThresholds` de `segmentation/types.ts`+`service.ts`, y el plan 05 borra `SegmentThresholds` de `types/member.ts` (admin) — eso deja a sus CONSUMIDORES (el módulo `settings` de la API + el composable/página de configuración del admin) referenciando símbolos inexistentes → **rompen el typecheck** si no se eliminan. Este plan los elimina.

Purpose: BLOCKER 1 + BLOCKER 2 del plan-check. El subsistema entero (API: `settings/routes.ts`+`service.ts`+`index.ts`, 2 endpoints `GET/PUT /admin/settings/segments`, 2 métodos; admin: `useSettingsApi.ts`+`ConfiguracionPage.vue`, una sola card "Segmentación") existe únicamente para tunear los cortes de % que ahora son constantes fijas (D-03). No hay otra funcionalidad que preservar — `SettingsService` no se inyecta en ningún otro módulo y la página/composable no tienen otros consumidores. La tabla `system_settings` se PRESERVA porque `streaks/service.ts` la usa con sus propias claves.
Output: módulo `settings` de la API eliminado + desregistrado de `app.ts`; `useSettingsApi.ts` + `ConfiguracionPage.vue` + su ruta + su item de nav eliminados; comentario de `system-settings.ts` desreferenciado de `SettingsService`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-redise-o-del-sistema-de-segmentaci-n-etiqueta-de-asistencia-/136-CONTEXT.md
@./CLAUDE.md
@.planning/phases/136-redise-o-del-sistema-de-segmentaci-n-etiqueta-de-asistencia-/136-01-SUMMARY.md
@.planning/phases/136-redise-o-del-sistema-de-segmentaci-n-etiqueta-de-asistencia-/136-05-SUMMARY.md

<interfaces>
<!-- De plan 01 (wave 1): segmentation/types.ts ya NO exporta -->
SEGMENT_SETTINGS_KEYS, SEGMENT_DEFAULTS, SegmentThresholds  // ELIMINADOS
// segmentation/service.ts ya NO tiene getThresholds()

<!-- De plan 05 (wave 2): admin types/member.ts ya NO exporta -->

SegmentThresholds // ELIMINADO (este plan borra a sus consumidores: useSettingsApi.ts + ConfiguracionPage.vue)

<!-- Lo que ESTE plan elimina -->

// API:
// src/modules/settings/{routes,service,index}.ts → BORRADOS
// app.ts: import + register(settingsRoutes) → ELIMINADOS
// Admin:
// composables/useSettingsApi.ts → BORRADO
// pages/ConfiguracionPage.vue → BORRADO
// router/routes.ts: ruta /configuracion → ELIMINADA
// layouts/AdminLayout.vue: item de nav → ELIMINADO

<!-- PRESERVAR -->

// src/db/schema/system-settings.ts → SE MANTIENE (lo usa streaks/service.ts). Solo se ajusta un comentario.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eliminar el módulo settings de la API y desregistrarlo de app.ts</name>
  <read_first>
    - el-templo-api/src/modules/settings/routes.ts (2 endpoints /segments — único contenido; importa SegmentThresholds, ya borrado por plan 01)
    - el-templo-api/src/modules/settings/service.ts (getSegmentThresholds/updateSegmentThresholds; importa SEGMENT_SETTINGS_KEYS/SEGMENT_DEFAULTS/SegmentThresholds, ya borrados)
    - el-templo-api/src/modules/settings/index.ts (re-exporta settingsRoutes + SettingsService)
    - el-templo-api/src/modules/settings/schemas.ts (stub vacío "Future settings schemas will be added here" — nadie lo importa)
    - el-templo-api/src/app.ts:42 (import settingsRoutes), :213-216 (comentario "Settings routes ..." + register con prefix /api/admin/settings)
    - el-templo-api/src/db/schema/system-settings.ts:12 (comentario JSDoc que nombra "SettingsService")
  </read_first>
  <action>
    Eliminar el módulo settings entero (es 100% segment-thresholds, sin otra funcionalidad; `SettingsService` no se inyecta en ningún otro módulo — grep confirmado):
    - `rm el-templo-api/src/modules/settings/routes.ts`
    - `rm el-templo-api/src/modules/settings/service.ts`
    - `rm el-templo-api/src/modules/settings/index.ts`
    - `rm el-templo-api/src/modules/settings/schemas.ts` (stub huérfano "Future settings schemas..." — nadie lo importa; borrarlo deja el directorio realmente vacío)
    (con los 4 archivos borrados, eliminar el directorio `src/modules/settings/` que queda vacío).

    En `app.ts`: eliminar la línea de import `import { settingsRoutes } from "./modules/settings";` (línea ~42) y el bloque de registro completo `// Settings routes (...)` + `await app.register(settingsRoutes, { prefix: "/api/admin/settings" });` (líneas ~213-216). NO tocar el resto de registros.

    En `db/schema/system-settings.ts` (línea ~12): actualizar el comentario JSDoc que dice "typed access through the SettingsService" — reemplazar por algo neutral (p.ej. "Key-value store de configuración del sistema (usado por streaks).") para no dejar referencia a un service eliminado. NO eliminar ni alterar la tabla/columnas — `streaks/service.ts` la usa. Sin `any`.

  </action>
  <verify>
    <automated>cd el-templo-api && test ! -d src/modules/settings && ! grep -q "settingsRoutes" src/app.ts && grep -q "system_settings\|systemSettings" src/db/schema/system-settings.ts && pnpm tsc --noEmit 2>&1 | grep -iE "settings|app.ts" || echo "api settings module removed, tsc clean"</automated>
  </verify>
  <done>El módulo settings (routes+service+index) está eliminado; app.ts sin import ni register de settingsRoutes; el schema system-settings.ts intacto salvo el comentario desreferenciado; typecheck de api limpio.</done>
</task>

<task type="auto">
  <name>Task 2: Eliminar useSettingsApi + ConfiguracionPage + su ruta + su item de nav (admin)</name>
  <read_first>
    - el-templo-admin/src/composables/useSettingsApi.ts (importa SegmentThresholds de src/types/member — ya borrado por plan 05; único consumidor = ConfiguracionPage)
    - el-templo-admin/src/pages/ConfiguracionPage.vue (única card "Segmentacion"; importa SegmentThresholds + useSettingsApi)
    - el-templo-admin/src/router/routes.ts:153-157 (ruta path 'configuracion' → ConfiguracionPage.vue)
    - el-templo-admin/src/layouts/AdminLayout.vue:161-166 (q-item to="/configuracion" "Configuracion")
  </read_first>
  <action>
    Eliminar la página de configuración y su composable (ambos existen solo para los thresholds de segmentación, sin otros consumidores — grep confirmado):
    - `rm el-templo-admin/src/composables/useSettingsApi.ts`
    - `rm el-templo-admin/src/pages/ConfiguracionPage.vue`

    En `router/routes.ts`: eliminar el bloque de ruta completo `{ path: 'configuracion', component: () => import('pages/ConfiguracionPage.vue'), meta: { allowedRoles: ['admin', 'owner'] as AdminRole[] } }` (líneas ~153-157). Cuidar las comas del array de children para no dejar coma colgante ni romper el literal.

    En `layouts/AdminLayout.vue`: eliminar el `<q-item clickable v-ripple to="/configuracion"> ... Configuracion ... </q-item>` completo (líneas ~161-166). El resto del nav (Usuarios arriba, Notificaciones abajo) no cambia. Sin `console.*`; sin `any`.

  </action>
  <verify>
    <automated>cd el-templo-admin && test ! -f src/composables/useSettingsApi.ts && test ! -f src/pages/ConfiguracionPage.vue && ! grep -q "configuracion\|ConfiguracionPage" src/router/routes.ts && ! grep -q "/configuracion" src/layouts/AdminLayout.vue && pnpm tsc --noEmit 2>&1 | grep -iE "ConfiguracionPage|useSettingsApi|routes.ts|AdminLayout" || echo "admin config removed, tsc clean"</automated>
  </verify>
  <done>useSettingsApi.ts y ConfiguracionPage.vue eliminados; la ruta /configuracion y el item de nav quitados; sin referencias colgando; typecheck de admin limpio.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                      |
| ------------------------------------ | ---------------------------------------------------------------- |
| admin → /api/admin/settings/segments | Endpoints ADMIN_ROLES que tuneaban los cortes de % (se ELIMINAN) |
| admin SPA → /configuracion           | Página de configuración de thresholds (se ELIMINA)               |

## STRIDE Threat Register

| Threat ID | Category               | Component                          | Disposition | Mitigation Plan                                                                                                                                    |
| --------- | ---------------------- | ---------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-136-19  | Tampering              | system_settings (tabla compartida) | mitigate    | Solo se elimina el MÓDULO settings; la tabla/schema `system_settings` se preserva intacta porque `streaks/service.ts` la consume con otras claves. |
| T-136-20  | Denial of Service      | ruta /configuracion huérfana       | mitigate    | Se elimina la ruta del router y el item de nav juntos; no queda link a una página inexistente.                                                     |
| T-136-21  | Information Disclosure | endpoints /segments residuales     | accept      | Los endpoints desaparecen del registro de Fastify; cualquier request a /api/admin/settings/segments devuelve 404 (comportamiento esperado).        |
| T-136-SC  | Tampering              | npm installs                       | mitigate    | No se instalan paquetes en esta fase.                                                                                                              |

</threat_model>

<verification>
- `pnpm tsc --noEmit` limpio en api (sin módulo settings) y admin (sin ConfiguracionPage/useSettingsApi).
- `src/modules/settings/{routes,service,index}.ts` eliminados; app.ts sin import ni register de settingsRoutes.
- `useSettingsApi.ts` y `ConfiguracionPage.vue` eliminados; ruta /configuracion y nav item quitados.
- `system_settings` (schema + tabla) intacta; solo se ajusta un comentario JSDoc.
- Validación cruzada: `grep -rn "SegmentThresholds\|SEGMENT_SETTINGS_KEYS\|SEGMENT_DEFAULTS\|settingsRoutes\|useSettingsApi\|ConfiguracionPage" el-templo-api/src el-templo-admin/src` devuelve vacío.
</verification>

<success_criteria>

- El subsistema de settings de thresholds queda completamente eliminado (API + admin), sin romper el typecheck.
- `system_settings` se preserva para streaks.
- Sin rutas/nav/endpoints huérfanos apuntando a lo eliminado.
  </success_criteria>

<output>
Create `.planning/phases/136-redise-o-del-sistema-de-segmentaci-n-etiqueta-de-asistencia-/136-07-SUMMARY.md` when done
</output>
</content>
