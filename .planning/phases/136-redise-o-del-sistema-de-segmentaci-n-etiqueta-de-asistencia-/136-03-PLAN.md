---
phase: 136-redise-o-del-sistema-de-segmentaci-n-etiqueta-de-asistencia-
plan: 03
type: execute
wave: 2
depends_on: ["136-01"]
files_modified:
  - el-templo-api/src/modules/analytics/types.ts
  - el-templo-api/src/modules/analytics/engagement-service.ts
  - el-templo-api/src/modules/analytics/service.ts
  - el-templo-api/src/modules/analytics/routes.ts
  - el-templo-api/src/modules/analytics/schemas.ts
  - el-templo-api/test/analytics/engagement.test.ts
  - el-templo-api/test/analytics/analytics.test.ts
autonomous: true
requirements: [D-01]
must_haves:
  truths:
    - "countActiveBySegment agrupa miembros activos en las 4 bandas nuevas + sinSegmento"
    - "getEngagementNominalList devuelve la worklist de seguimiento sobre alerta + ausente (antes en_riesgo + ghost)"
    - "Los schemas de validación response/request de analytics aceptan solo los 4 valores nuevos + sinSegmento"
    - "getAttentionList prioriza ausente antes que alerta en la attention-list (antes ghost antes que en_riesgo)"
  artifacts:
    - path: "el-templo-api/src/modules/analytics/engagement-service.ts"
      provides: "SEGMENT_KEYS + countActiveBySegment + getEngagementNominalList sobre 4 bandas"
    - path: "el-templo-api/src/modules/analytics/types.ts"
      provides: "SegmentCounts (4 bandas + sinSegmento) + EngagementMember.segment (alerta|ausente)"
    - path: "el-templo-api/src/modules/analytics/service.ts"
      provides: "getAttentionList.priorityRank remapeado a ausente/alerta"
  key_links:
    - from: "el-templo-api/src/modules/analytics/engagement-service.ts"
      to: "el-templo-api/src/modules/segmentation/types.ts"
      via: "import MemberSegment (4 valores)"
      pattern: "MemberSegment"
    - from: "el-templo-api/src/modules/analytics/engagement-service.ts"
      to: "member_profiles.segment"
      via: "IN ('alerta','ausente') en getEngagementNominalList"
      pattern: "alerta.*ausente"
    - from: "el-templo-api/src/modules/analytics/service.ts"
      to: "member_profiles.segment"
      via: "priorityRank ordena por ausente/alerta en la attention-list"
      pattern: "ausente"
---

<objective>
Propagar el nuevo enum de 4 bandas a analytics: contadores por segmento, worklist de seguimiento (que pasa de `en_riesgo`+`ghost` a `alerta`+`ausente`), el orden de prioridad de la attention-list (`getAttentionList.priorityRank`) y los schemas de validación.

Purpose: Analytics LEE `member_profiles.segment` (nunca recalcula). El cambio de enum rompe SEGMENT_KEYS, SegmentCounts, EngagementMember.segment, los enums de schemas Y el `priorityRank` de la attention-list (que compara contra los strings muertos `"ghost"`/`"en_riesgo"` — tras la migración nunca matchean → regresión silenciosa de orden, además del shape inválido de `/engagement`).
Output: `analytics/types.ts`, `engagement-service.ts`, `analytics/service.ts`, `analytics/routes.ts`, `analytics/schemas.ts` + tests (engagement + analytics).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-redise-o-del-sistema-de-segmentaci-n-etiqueta-de-asistencia-/136-CONTEXT.md
@./CLAUDE.md
@.planning/phases/136-redise-o-del-sistema-de-segmentaci-n-etiqueta-de-asistencia-/136-01-SUMMARY.md

<interfaces>
<!-- Del plan 01 -->
MemberSegment = "optima" | "regular" | "alerta" | "ausente"

<!-- Shape objetivo de SegmentCounts (analytics/types.ts) -->

interface SegmentCounts { optima: number; regular: number; alerta: number; ausente: number; sinSegmento: number; }

<!-- Worklist de seguimiento: alerta + ausente reemplazan en_riesgo + ghost -->

EngagementMember.segment: "alerta" | "ausente"

<!-- Prioridad de attention-list (getAttentionList): ausente reemplaza ghost (urgencia 0), alerta reemplaza en_riesgo (urgencia 1) -->

AttentionMember.segment: MemberSegment | null
priorityRank: ausente → 0, alerta → 1, resto → 2
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Propagar 4 bandas a analytics/types.ts + engagement-service.ts</name>
  <read_first>
    - el-templo-api/src/modules/analytics/types.ts:170-210 (SegmentCounts, EngagementMember, comentarios "6 segments" en :172-181, EngagementMember.segment en :208)
    - el-templo-api/src/modules/analytics/engagement-service.ts (SEGMENT_KEYS:44-51, countActiveBySegment:68-142, getEngagementNominalList:153-230)
    - el-templo-api/src/modules/segmentation/types.ts (MemberSegment nuevo, tras plan 01)
  </read_first>
  <action>
    En `analytics/types.ts`: reescribir `SegmentCounts` a `{ optima, regular, alerta, ausente, sinSegmento }` (D-01). Cambiar `EngagementMember.segment` (línea ~208) de `"en_riesgo" | "ghost"` a `"alerta" | "ausente"`. Actualizar comentarios que nombren los 6 segmentos viejos (incluidos los bloques de :172-181 que hablan de "6 segments").

    En `engagement-service.ts`: actualizar `SEGMENT_KEYS` al array de 4 valores. En `countActiveBySegment`: inicializar el objeto `counts` con las 4 bandas + sinSegmento (eliminar nuevo/espartano/intermitente/en_riesgo/digital_warrior/ghost). En `getEngagementNominalList`: cambiar el predicado SQL `IN ('en_riesgo','ghost')` a `IN ('alerta','ausente')`, el filtro de narrowing `r.segment === "en_riesgo" || r.segment === "ghost"` a `alerta`/`ausente`, y el mapa `urgency` a `{ ausente: 0, alerta: 1 }` (ausente = mayor urgencia). Mantener el `applyScope`/`activeMemberExists` sin cambios (D-17 de fase previa: no fugar PII entre sedes). Sin `any`.

  </action>
  <verify>
    <automated>cd el-templo-api && grep -q "IN ('alerta','ausente')" src/modules/analytics/engagement-service.ts && grep -q "optima" src/modules/analytics/types.ts && pnpm tsc --noEmit 2>&1 | grep -iE "analytics/types|engagement-service" || echo "analytics core OK"</automated>
  </verify>
  <done>SegmentCounts y SEGMENT_KEYS en 4 bandas; getEngagementNominalList sobre alerta/ausente con urgency invertida (ausente primero); scope intacto.</done>
</task>

<task type="auto">
  <name>Task 2: Remapear getAttentionList.priorityRank + comentarios stale (analytics/service.ts + routes.ts)</name>
  <read_first>
    - el-templo-api/src/modules/analytics/service.ts:660-695 (getAttentionList: priorityRank usa "ghost"/"en_riesgo", + comentario ":670-671" "ghost/en_riesgo first")
    - el-templo-api/src/modules/analytics/service.ts:515-520 (comentario JSDoc que nombra "ghost/en_riesgo")
    - el-templo-api/src/modules/analytics/routes.ts:276-280 (comentario "worklist en_riesgo/ghost")
    - el-templo-api/src/modules/analytics/types.ts:79-80 (AttentionMember.segment: MemberSegment | null — tras Task 1, MemberSegment = 4 valores)
  </read_first>
  <action>
    En `analytics/service.ts` `getAttentionList.priorityRank` (líneas ~672-676): remapear los literales muertos `m.segment === "ghost"` → `m.segment === "ausente"` (return 0) y `m.segment === "en_riesgo"` → `m.segment === "alerta"` (return 1), resto return 2 (D-01). Sin esto, tras la migración la attention-list pierde el orden por segmento (los strings viejos nunca matchean `MemberSegment | null` de 4 valores → regresión silenciosa). Actualizar el comentario de :670-671 ("Prioritize: ghost/en_riesgo first") a "Prioritize: ausente/alerta first". Actualizar el comentario JSDoc de :515-520 que nombra "ghost/en_riesgo" a "ausente/alerta" (LOW 7, cosmético).

    En `analytics/routes.ts` (líneas ~276-277): actualizar el comentario `// GET /engagement — ... worklist en_riesgo/ghost` a "worklist alerta/ausente" (LOW 7, cosmético). Sin `any`.

  </action>
  <verify>
    <automated>cd el-templo-api && ! grep -qE '"(ghost|en_riesgo)"' src/modules/analytics/service.ts && grep -q '"ausente"' src/modules/analytics/service.ts && pnpm tsc --noEmit 2>&1 | grep -iE "analytics/service|analytics/routes" || echo "attention-list remap OK"</automated>
  </verify>
  <done>priorityRank ordena por ausente (0) / alerta (1) / resto (2); sin literales "ghost"/"en_riesgo" en service.ts; comentarios de service.ts y routes.ts actualizados a la taxonomía nueva.</done>
</task>

<task type="auto">
  <name>Task 3: Actualizar enums de analytics/schemas.ts + tests de engagement y attention-list</name>
  <read_first>
    - el-templo-api/src/modules/analytics/schemas.ts:123-131 (segment enum en response), :269-275 (SegmentCounts response props), :288 (EngagementMember.segment enum)
    - el-templo-api/test/analytics/engagement.test.ts (suite a actualizar)
    - el-templo-api/test/analytics/analytics.test.ts (suite de attention-list: setSegment :577, casos "ghost"/"en_riesgo" en :682-700 y :791-811)
    - el-templo-api/test/helpers.ts
  </read_first>
  <action>
    En `analytics/schemas.ts`: (1) actualizar el enum `segment` de la línea ~123-131 a `["optima","regular","alerta","ausente"]`; (2) reemplazar las props de SegmentCounts del response (~269-275) por `optima/regular/alerta/ausente/sinSegmento` (eliminar las 6 viejas); (3) actualizar el enum de `EngagementMember.segment` (~288) a `["alerta","ausente"]`. Verificar que no quede ninguna prop huérfana de los segmentos viejos en otros response schemas de este archivo (grep dentro del archivo).

    Reescribir `test/analytics/engagement.test.ts`: ajustar los asserts de `countActiveBySegment` a las 4 bandas + sinSegmento, y los de `getEngagementNominalList` a la worklist alerta/ausente (incluyendo orden de urgencia ausente-antes-de-alerta). Usar `test/helpers.ts` y el patrón de seed contra `eltemplo_test`. Atender el flake conocido de TZ en analytics (seed con fechas consistentes UTC-vs-local — ver memoria analytics_test_tz_flake) si los casos dependen de ventanas de fecha.

    Actualizar `test/analytics/analytics.test.ts`: los casos de attention-list que setean `setSegment(ghost.id, "ghost")` (:687/:700) y `segment: "en_riesgo"` (:791-792/:811) deben pasar a `"ausente"` y `"alerta"` respectivamente. AGREGAR (o reforzar) un assert que verifique el ORDEN de la attention-list: un miembro `ausente` que expira/está en mora aparece ANTES que uno `alerta` con la misma urgencia (cubre el remap de `priorityRank` del Task 2). Mantener el resto de la suite intacta.

  </action>
  <verify>
    <automated>cd el-templo-api && ! grep -qE "\"(nuevo|espartano|intermitente|en_riesgo|digital_warrior|ghost)\"" src/modules/analytics/schemas.ts && ! grep -qE "\"(en_riesgo|ghost)\"" test/analytics/analytics.test.ts && pnpm tsc --noEmit 2>&1 | grep -iE "analytics/schemas|engagement.test|analytics.test" || echo "schemas + tests OK"</automated>
  </verify>
  <done>Los enums de schemas usan solo los 4 valores (+ sinSegmento en counts, alerta/ausente en nominal list); sin props/enum de segmentos viejos; engagement.test.ts reescrito; analytics.test.ts remapeado a ausente/alerta con assert de orden de attention-list.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                       | Description                                               |
| ---------------------------------------------- | --------------------------------------------------------- |
| admin/coach → GET /admin/analytics/engagement  | Expone counts + worklist nominal con phone (PII) por sede |
| admin/coach → GET /admin/analytics (attention) | Attention-list con phone (PII) priorizada por segmento    |

## STRIDE Threat Register

| Threat ID | Category               | Component                                | Disposition | Mitigation Plan                                                                                                                                                     |
| --------- | ---------------------- | ---------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-136-07  | Information Disclosure | getEngagementNominalList (phone PII)     | mitigate    | Se conserva `applyScope` sobre `users.branchId` y el guard ADMIN_ROLES upstream — un coach de sede X no ve teléfonos de sede Y. El cambio es solo el enum filtrado. |
| T-136-08  | Tampering              | enum segment en schemas response/request | mitigate    | El JSON schema de Fastify valida el shape de salida; props huérfanas de segmentos viejos se eliminan para que no se filtren campos fantasma.                        |
| T-136-16  | Tampering              | priorityRank con strings muertos         | mitigate    | El remap a ausente/alerta restaura el orden de la attention-list; el test de orden detecta una futura regresión silenciosa por literales fuera del union.           |
| T-136-SC  | Tampering              | npm installs                             | mitigate    | No se instalan paquetes en esta fase.                                                                                                                               |

</threat_model>

<verification>
- `pnpm tsc --noEmit` limpio para analytics.
- SegmentCounts/SEGMENT_KEYS/schemas en 4 bandas; worklist sobre alerta/ausente.
- `getAttentionList.priorityRank` ordena por ausente/alerta; sin literales "ghost"/"en_riesgo" en service.ts.
- Comentarios de service.ts/routes.ts sin "ghost/en_riesgo".
- Sin referencias a los 6 estados viejos en types/service/schemas.
</verification>

<success_criteria>

- countActiveBySegment, getEngagementNominalList y getAttentionList.priorityRank operan sobre las 4 bandas; schemas validan los 4 valores; tests (engagement + attention-list) actualizados con assert de orden; scope/PII preservados.
  </success_criteria>

<output>
Create `.planning/phases/136-redise-o-del-sistema-de-segmentaci-n-etiqueta-de-asistencia-/136-03-SUMMARY.md` when done
</output>
</content>
