---
phase: 136-redise-o-del-sistema-de-segmentaci-n-etiqueta-de-asistencia-
plan: 05
type: execute
wave: 2
depends_on: ["136-01"]
files_modified:
  - el-templo-admin/src/types/member.ts
  - el-templo-admin/src/types/analytics.ts
  - el-templo-admin/src/pages/AlumnosPage.vue
  - el-templo-admin/src/pages/AlumnoDetailPage.vue
  - el-templo-admin/src/pages/NotificacionesPage.vue
  - el-templo-admin/src/components/analytics/MiembrosTab.vue
autonomous: true
requirements: [D-01]
must_haves:
  truths:
    - "El admin muestra los 4 estados de Asistencia con labels Óptima/Regular/Alerta/Ausente y colores green/amber/orange/red"
    - "El filtro de Segmento en AlumnosPage ofrece exactamente las 4 bandas nuevas"
    - "La card de segmentación en AlumnoDetailPage renderiza el estado nuevo con su label/color"
    - "El targeting de segmentos en NotificacionesPage ofrece las 4 bandas nuevas"
    - "La attention-list de MiembrosTab prioriza ausente antes que alerta (SEGMENT_PRIORITY remapeado)"
    - "SegmentThresholds eliminado de types/member.ts (los consumidores de thresholds se borran en 136-07)"
  artifacts:
    - path: "el-templo-admin/src/types/member.ts"
      provides: "MemberSegment + SEGMENT_LABELS/COLORS/DESCRIPTIONS (4 bandas), SIN SegmentThresholds"
    - path: "el-templo-admin/src/types/analytics.ts"
      provides: "SegmentCounts (4 bandas + sinSegmento) + EngagementMember.segment (alerta|ausente)"
    - path: "el-templo-admin/src/components/analytics/MiembrosTab.vue"
      provides: "SEGMENT_PRIORITY remapeado a { ausente: 0, alerta: 1 }"
  key_links:
    - from: "el-templo-admin/src/pages/AlumnosPage.vue"
      to: "el-templo-admin/src/types/member.ts"
      via: "SEGMENT_LABELS/COLORS + segmentFilterOptions"
      pattern: "SEGMENT_LABELS"
    - from: "el-templo-admin/src/pages/AlumnoDetailPage.vue"
      to: "el-templo-admin/src/types/member.ts"
      via: "SEGMENT_LABELS/COLORS"
      pattern: "SEGMENT_COLORS"
    - from: "el-templo-admin/src/components/analytics/MiembrosTab.vue"
      to: "el-templo-admin/src/types/analytics.ts"
      via: "AttentionMember.segment + SEGMENT_PRIORITY"
      pattern: "SEGMENT_PRIORITY"
---

<objective>
Propagar el enum de 4 bandas al frontend admin: tipos de display (labels/colors/descriptions), tipos de analytics, el filtro+columna de AlumnosPage, la card de segmentación de AlumnoDetailPage, el targeting de NotificacionesPage y el orden de prioridad de la attention-list de MiembrosTab. Eliminar `SegmentThresholds` de `types/member.ts` (de forma INCONDICIONAL — el plan 136-07 borra los consumidores de thresholds).

Purpose: El admin tipifica `MemberSegment` y usa `SEGMENT_LABELS`/`SEGMENT_COLORS` como records indexados por el enum. `MiembrosTab.vue` ordena la attention-list con un `SEGMENT_PRIORITY={ghost,en_riesgo}` muerto que tras el cambio nunca matchea (MEDIUM: regresión silenciosa de orden en el front). Sin esto, el admin muestra opciones de segmentos inexistentes y desordena la worklist.
Output: `types/member.ts`, `types/analytics.ts`, `AlumnosPage.vue`, `AlumnoDetailPage.vue`, `NotificacionesPage.vue`, `MiembrosTab.vue`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-redise-o-del-sistema-de-segmentaci-n-etiqueta-de-asistencia-/136-CONTEXT.md
@./CLAUDE.md
@.planning/phases/136-redise-o-del-sistema-de-segmentaci-n-etiqueta-de-asistencia-/136-01-SUMMARY.md

<interfaces>
<!-- Del plan 01 (display canónico, Claude's Discretion D-69) -->
MemberSegment = "optima" | "regular" | "alerta" | "ausente"
SEGMENT_LABELS:  optima→"Óptima", regular→"Regular", alerta→"Alerta", ausente→"Ausente"
SEGMENT_COLORS:  optima→"green",  regular→"amber",  alerta→"orange", ausente→"red"

<!-- analytics (alineado con plan 03 backend) -->

SegmentCounts { optima; regular; alerta; ausente; sinSegmento }
EngagementMember.segment: "alerta" | "ausente"
SEGMENT_PRIORITY (MiembrosTab, alineado con priorityRank del plan 03): { ausente: 0, alerta: 1 }

<!-- COORDINACIÓN con 136-07 (settings UI):
     `SegmentThresholds` vive en types/member.ts y lo importan useSettingsApi.ts + ConfiguracionPage.vue.
     Este plan (05) ES el dueño exclusivo de types/member.ts y BORRA SegmentThresholds aquí (incondicional).
     El plan 136-07 (wave 3, depends_on: 05) elimina los CONSUMIDORES (useSettingsApi + card de ConfiguracionPage)
     y NO toca types/member.ts → sin solapamiento de escritura. 07 corre DESPUÉS de 05. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescribir tipos de display (member.ts) y analytics.ts a 4 bandas + borrar SegmentThresholds</name>
  <read_first>
    - el-templo-admin/src/types/member.ts:10-59 (MemberSegment, SEGMENT_LABELS/COLORS/DESCRIPTIONS, SegmentThresholds)
    - el-templo-admin/src/types/analytics.ts:154-187 (SegmentCounts, EngagementMember, EngagementResponse)
  </read_first>
  <action>
    En `types/member.ts`: reescribir `MemberSegment` a los 4 valores. Reescribir `SEGMENT_LABELS` (Óptima/Regular/Alerta/Ausente), `SEGMENT_COLORS` (green/amber/orange/red, D-69) y `SEGMENT_DESCRIPTIONS` (texto breve por banda: optima="Usa ≥75% de su cupo de clases"; regular="Usa 50-74% de su cupo"; alerta="Usa menos de la mitad de su cupo — requiere observación"; ausente="Sin asistencia en la ventana — detona seguimiento"). ELIMINAR `SegmentThresholds` de forma INCONDICIONAL (el backend ya no expone thresholds configurables; los únicos consumidores —useSettingsApi.ts + ConfiguracionPage.vue— se borran en el plan 136-07, que depende de este). Mantener `MemberListItem.segment: MemberSegment | null` y el comentario de Phase 79 actualizado.

    En `types/analytics.ts`: reescribir `SegmentCounts` a `{ optima, regular, alerta, ausente, sinSegmento }` y `EngagementMember.segment` a `'alerta' | 'ausente'` (alineado con el backend del plan 03). Actualizar comentarios.

  </action>
  <verify>
    <automated>cd el-templo-admin && grep -q "optima: 'Óptima'\|optima: \"Óptima\"" src/types/member.ts; ! grep -q "SegmentThresholds" src/types/member.ts && grep -q "ausente" src/types/analytics.ts && pnpm tsc --noEmit 2>&1 | grep -iE "types/member|types/analytics" || echo "admin types OK"</automated>
  </verify>
  <done>MemberSegment + records de display en 4 bandas; SegmentThresholds eliminado de types/member.ts; SegmentCounts/EngagementMember en analytics alineados con backend; sin restos de los 6 valores.</done>
</task>

<task type="auto">
  <name>Task 2: Actualizar AlumnosPage (filtro + columna), AlumnoDetailPage (card), NotificacionesPage (targeting), MiembrosTab (SEGMENT_PRIORITY)</name>
  <read_first>
    - el-templo-admin/src/pages/AlumnosPage.vue:136-138 (filtro), :216-222 (columna), :446-456 (segmentFilterOptions), :623-628 (segmentLabel/segmentColor)
    - el-templo-admin/src/pages/AlumnoDetailPage.vue:64-74 (badge segment), :346-360 (card Segmentación)
    - el-templo-admin/src/pages/NotificacionesPage.vue:289-296 (segments array de targeting)
    - el-templo-admin/src/components/analytics/MiembrosTab.vue:214-226 (SEGMENT_PRIORITY={ghost:0,en_riesgo:1} + prioritizedAttentionList)
  </read_first>
  <action>
    En `AlumnosPage.vue`: reescribir `segmentFilterOptions` a `[{Todos,null},{Óptima,optima},{Regular,regular},{Alerta,alerta},{Ausente,ausente}]` (D-01). `segmentLabel`/`segmentColor` ya delegan en `SEGMENT_LABELS`/`SEGMENT_COLORS` — quedan correctos al cambiar el record. El header de columna "Segmento" puede mantenerse o renombrarse a "Asistencia" (Claude's Discretion — recomendado "Asistencia" para reflejar la nueva semántica). El filtro `filters.segment: MemberSegment | null` sigue igual.

    En `AlumnoDetailPage.vue`: las dos zonas (badge en header + card "Segmentación") usan `SEGMENT_COLORS`/`SEGMENT_LABELS` indexados por `memberProfile.segment as MemberSegment` — quedan correctas al cambiar el record. Renombrar el título de la card "Segmentacion" a "Asistencia" (Claude's Discretion, coherencia). Confirmar que no haya literales de segmentos viejos hardcodeados.

    En `NotificacionesPage.vue`: reescribir el array `segments` de targeting a las 4 bandas `[{optima,'Óptima'},{regular,'Regular'},{alerta,'Alerta'},{ausente,'Ausente'}]`. `sendForm.segmentIds` sigue `string[]`. Sin `console.*`; sin `any`.

    En `MiembrosTab.vue` (líneas ~214-226): remapear `SEGMENT_PRIORITY` de `{ ghost: 0, en_riesgo: 1 }` a `{ ausente: 0, alerta: 1 }` (ausente = mayor prioridad, alineado con `priorityRank` del backend en plan 03). Actualizar el comentario de :211-213 ("Ghost / en_riesgo about to expire ...") a "Ausente / alerta about to expire ...". `prioritizedAttentionList` no cambia su lógica (el fallback `?? 2` se mantiene). Sin esto, la worklist del admin pierde el orden por segmento tras la migración (los strings viejos nunca matchean).

  </action>
  <verify>
    <automated>cd el-templo-admin && ! grep -qE "value: '(nuevo|espartano|intermitente|en_riesgo|digital_warrior|ghost)'" src/pages/AlumnosPage.vue src/pages/NotificacionesPage.vue && grep -q "ausente: 0" src/components/analytics/MiembrosTab.vue && ! grep -qE "\b(ghost|en_riesgo):" src/components/analytics/MiembrosTab.vue && pnpm tsc --noEmit 2>&1 | grep -iE "AlumnosPage|AlumnoDetailPage|NotificacionesPage|MiembrosTab" || echo "admin pages OK"</automated>
  </verify>
  <done>Filtro de AlumnosPage, card de AlumnoDetailPage y targeting de NotificacionesPage ofrecen/renderizan solo las 4 bandas; SEGMENT_PRIORITY en MiembrosTab remapeado a ausente/alerta; sin opciones de segmentos viejos; typecheck limpio.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                       | Description                                                                   |
| ---------------------------------------------- | ----------------------------------------------------------------------------- |
| admin UI → datos de comportamiento de miembros | Filtro/targeting/visualización de la etiqueta de Asistencia (admin/recepción) |

## STRIDE Threat Register

| Threat ID | Category               | Component                              | Disposition | Mitigation Plan                                                                                                                                                                                                     |
| --------- | ---------------------- | -------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-136-11  | Information Disclosure | targeting de NotificacionesPage        | mitigate    | El targeting solo envía a través del endpoint protegido (ADMIN_ROLES) del plan 02; el frontend solo arma el payload. El enum del front debe coincidir con el del schema backend para no permitir valores inválidos. |
| T-136-12  | Tampering              | segmentFilterOptions / segments array  | accept      | Valores fijos en código del frontend; el backend revalida (sendSegmentSchema, members filter). El front no es la autoridad.                                                                                         |
| T-136-18  | Tampering              | SEGMENT_PRIORITY muerto en MiembrosTab | mitigate    | El remap a ausente/alerta restaura el orden de la worklist del admin; sin él los strings viejos caen al fallback y la prioridad por segmento se pierde.                                                             |
| T-136-SC  | Tampering              | npm installs                           | mitigate    | No se instalan paquetes en esta fase.                                                                                                                                                                               |

</threat_model>

<verification>
- `pnpm tsc --noEmit` limpio en el-templo-admin para los 6 archivos.
- Ningún `value:` de segmento viejo en los selects de AlumnosPage/NotificacionesPage.
- SEGMENT_LABELS/COLORS/DESCRIPTIONS y SegmentCounts en 4 bandas.
- SegmentThresholds eliminado de types/member.ts.
- SEGMENT_PRIORITY en MiembrosTab usa { ausente: 0, alerta: 1 }.
</verification>

<success_criteria>

- El admin filtra, muestra y targetea exclusivamente con las 4 bandas Óptima/Regular/Alerta/Ausente y sus colores; la worklist prioriza ausente/alerta; tipos alineados con el backend; SegmentThresholds borrado (consumidores en 136-07).
  </success_criteria>

<output>
Create `.planning/phases/136-redise-o-del-sistema-de-segmentaci-n-etiqueta-de-asistencia-/136-05-SUMMARY.md` when done
</output>
</content>
