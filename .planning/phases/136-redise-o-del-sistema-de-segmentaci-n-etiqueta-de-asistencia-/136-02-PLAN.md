---
phase: 136-redise-o-del-sistema-de-segmentaci-n-etiqueta-de-asistencia-
plan: 02
type: execute
wave: 2
depends_on: ["136-01"]
files_modified:
  - el-templo-api/src/modules/notifications/types.ts
  - el-templo-api/src/jobs/notification-cron.ts
  - el-templo-api/src/modules/notifications/routes.ts
  - el-templo-api/test/notifications.test.ts
autonomous: true
requirements: [D-10, D-11]
must_haves:
  truths:
    - "Una transición a alerta dispara segment_transition_en_riesgo ('Tu práctica te espera')"
    - "Una transición a ausente dispara segment_transition_ghost ('El Templo no cierra')"
    - "Una recuperación desde alerta/ausente a optima/regular dispara segment_transition_recovery"
    - "Una transición a optima dispara segment_transition_espartano"
    - "Un miembro que sigue ausente recibe el reintento mensual ghost_monthly_reattempt reusando ghost_reattempt_count/last_ghost_reattempt_at"
    - "El cron nocturno de las 3AM sigue activo y recalcula segmentos"
    - "El copy y los template_key de TEMPLATE_SEEDS no cambian"
  artifacts:
    - path: "el-templo-api/src/jobs/notification-cron.ts"
      provides: "getTransitionTemplateKey reconectado a los 4 estados nuevos"
    - path: "el-templo-api/src/modules/notifications/types.ts"
      provides: "SEGMENT_TRANSITION_TEMPLATES reconectado (TEMPLATE_SEEDS intacto)"
  key_links:
    - from: "el-templo-api/src/jobs/notification-cron.ts"
      to: "el-templo-api/src/modules/notifications/types.ts"
      via: "SEGMENT_TRANSITION_TEMPLATES + template keys"
      pattern: "SEGMENT_TRANSITION_TEMPLATES"
    - from: "el-templo-api/src/jobs/notification-cron.ts"
      to: "el-templo-api/src/modules/segmentation/service.ts"
      via: "calculateSegment (firma nueva MemberSegment | null)"
      pattern: "calculateSegment"
---

<objective>
Reconectar los disparadores de las 5 notis push a los nuevos estados de Asistencia, PRESERVANDO el copy y los template_key existentes (D-10), y mantener el cron nocturno de recálculo de las 3AM (D-11) tras el cambio de firma de `calculateSegment`.

Purpose: Las notis están vivas en prod (verificado 2026-06-22). El cambio de enum rompe `getTransitionTemplateKey`, el batch que llama `calculateSegment` y `sendSegmentSchema`. Hay que recablear sin cambiar mensajes.
Output: `notifications/types.ts` (solo SEGMENT_TRANSITION_TEMPLATES), `notification-cron.ts` (getTransitionTemplateKey + batch), `notifications/routes.ts` (sendSegmentSchema) + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-redise-o-del-sistema-de-segmentaci-n-etiqueta-de-asistencia-/136-CONTEXT.md
@./CLAUDE.md
@.planning/phases/136-redise-o-del-sistema-de-segmentaci-n-etiqueta-de-asistencia-/136-01-SUMMARY.md

<interfaces>
<!-- Del plan 01 (wave 1) -->
MemberSegment = "optima" | "regular" | "alerta" | "ausente"
calculateSegment(userId, opts?): Promise<MemberSegment | null>   // ya NO acepta opts.isFrequencyGoldenCase

<!-- Mapeo de reconexión (D-10) — los template_key NO cambian, solo el disparador -->

alerta ← (antes en_riesgo) → "segment_transition_en_riesgo" ("Tu práctica te espera")
ausente ← (antes ghost) → "segment_transition_ghost" ("El Templo no cierra") + ghost_monthly_reattempt
recuperación a optima/regular desde alerta/ausente → "segment_transition_recovery" ("¡Bienvenido de vuelta!")
optima ← (antes espartano) → "segment_transition_espartano" ("¡Semana increíble!")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconectar SEGMENT_TRANSITION_TEMPLATES + getTransitionTemplateKey a los 4 estados</name>
  <read_first>
    - el-templo-api/src/modules/notifications/types.ts:52-117 (SEGMENT_TRANSITION_TEMPLATES + TEMPLATE_SEEDS)
    - el-templo-api/src/jobs/notification-cron.ts:39-67 (getTransitionTemplateKey)
    - el-templo-api/src/jobs/notification-cron.ts:202-417 (batch 3AM: llama calculateSegment + golden case + ghost reattempt)
    - el-templo-api/src/modules/segmentation/service.ts (firma nueva de calculateSegment, tras plan 01)
  </read_first>
  <action>
    En `notifications/types.ts`: actualizar las CLAVES de `SEGMENT_TRANSITION_TEMPLATES` a la nueva taxonomía manteniendo los VALORES (template_key) intactos (D-10): `any_to_alerta → "segment_transition_en_riesgo"`, `alerta_to_ausente → "segment_transition_ghost"`, `recovery_to_active → "segment_transition_recovery"`, `any_to_optima → "segment_transition_espartano"`. NO tocar `TEMPLATE_SEEDS` (mismo copy/título/route/categoría/female variants).

    En `notification-cron.ts` `getTransitionTemplateKey`: reescribir las condiciones a los 4 estados — (1) newSegment === "alerta" && old !== "alerta" → en_riesgo template; (2) newSegment === "ausente" && old === "alerta" → ghost template; (3) (old === "alerta" || old === "ausente") && (new === "optima" || new === "regular") → recovery template; (4) newSegment === "optima" && old !== "optima" → espartano template (D-10). Como `MemberSegment` ahora puede ser NULL, manejar `newSegment` posiblemente NULL: si calculateSegment retorna NULL (miembro <1 mes o sin plan), NO disparar transición (saltar el update de notificación; persistir NULL es válido).

    En el batch 3AM: ajustar la llamada a `calculateSegment(profile.userId)` quitando `{ isFrequencyGoldenCase: ... }` (el parámetro ya no existe). ELIMINAR el bloque de pre-fetch del golden case y el import de `FrequencyService` si quedan huérfanos. Reapuntar el ghost-monthly-reattempt: la condición `newSegment === "ghost" && old === "ghost"` pasa a `newSegment === "ausente" && oldSegment === "ausente"`, reusando `ghost_reattempt_count`/`last_ghost_reattempt_at` y el template `ghost_monthly_reattempt` sin cambios (D-10). El cron schedule de las 3AM y el resto de jobs (morning/weekly/renewal) NO cambian (D-11). Sin `console.log` (usar `log`); sin `any`.

  </action>
  <verify>
    <automated>cd el-templo-api && grep -q "segment_transition_en_riesgo" src/modules/notifications/types.ts && grep -q "El Templo no cierra" src/modules/notifications/types.ts && ! grep -q "isFrequencyGoldenCase" src/jobs/notification-cron.ts && pnpm tsc --noEmit 2>&1 | grep -iE "notification-cron|notifications/types" || echo "notif rewire OK"</automated>
  </verify>
  <done>SEGMENT_TRANSITION_TEMPLATES reconectado a alerta/ausente/optima manteniendo los 4 template_key; TEMPLATE_SEEDS intacto; getTransitionTemplateKey usa los 4 estados; batch sin golden case; ghost-reattempt apunta a ausente; cron 3AM intacto.</done>
</task>

<task type="auto">
  <name>Task 2: Actualizar sendSegmentSchema + tipo MemberSegment local en routes + tests</name>
  <read_first>
    - el-templo-api/src/modules/notifications/routes.ts:19-26 (tipo MemberSegment local), :142-163 (sendSegmentSchema enum)
    - el-templo-api/src/modules/notifications/routes.ts:403-465 (handler send-segment, filtra por inArray(memberProfiles.segment, segmentIds))
    - el-templo-api/test/notifications.test.ts (suite a actualizar: targeting por segmento + transiciones)
    - el-templo-api/test/helpers.ts
  </read_first>
  <action>
    En `notifications/routes.ts`: actualizar el tipo `MemberSegment` local (líneas 19-26) a los 4 valores `optima`/`regular`/`alerta`/`ausente` — o mejor, importarlo de `../segmentation/types` para DRY (eliminar la duplicación). Actualizar el `enum` de `segmentIds.items` en `sendSegmentSchema` (líneas ~156-161) a los 4 valores. El handler `send-segment` no cambia su lógica (sigue filtrando `inArray(memberProfiles.segment, segmentIds)`). Sin `any`.

    Actualizar `test/notifications.test.ts`: reescribir los casos que referencian segmentos viejos (`en_riesgo`/`ghost`/`espartano`) a los nuevos disparadores — transición a alerta → en_riesgo template, transición a ausente → ghost template, recovery a optima/regular, transición a optima → espartano template, y el ghost_monthly_reattempt sobre `ausente`. Verificar que el copy de las plantillas NO cambió (assertions sobre title/body de TEMPLATE_SEEDS si las hay). Usar `test/helpers.ts`.

  </action>
  <verify>
    <automated>cd el-templo-api && grep -E "optima|alerta|ausente|regular" src/modules/notifications/routes.ts | grep -q enum -i; pnpm tsc --noEmit 2>&1 | grep -iE "notifications/routes|notifications.test" || echo "routes + tests typecheck OK"</automated>
  </verify>
  <done>sendSegmentSchema y el tipo local usan los 4 valores; el handler intacto; notifications.test.ts reescrito a los nuevos disparadores con assertions de copy preservado.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                       | Description                                                                      |
| ---------------------------------------------- | -------------------------------------------------------------------------------- |
| admin → POST /notifications/admin/send-segment | Targeting de push por segmento (ADMIN_ROLES); dato de comportamiento de miembros |
| cron 3AM → push queue                          | Dispara notis según transición de segmento                                       |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                | Disposition | Mitigation Plan                                                                                                                      |
| --------- | ---------------------- | -------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| T-136-04  | Elevation of Privilege | send-segment endpoint                                    | mitigate    | El endpoint ya está bajo el guard ADMIN_ROLES (D-15); no se relaja. El cambio es solo el enum de validación.                         |
| T-136-05  | Tampering              | enum de sendSegmentSchema                                | mitigate    | El schema JSON valida `segmentIds` contra los 4 valores exactos; un valor fuera del enum es rechazado por Fastify antes del handler. |
| T-136-06  | Information Disclosure | notis a miembros equivocados por mal mapeo de transición | mitigate    | Tests de transición cubren el mapeo alerta/ausente/optima; el copy preservado evita confusión de mensajería.                         |
| T-136-SC  | Tampering              | npm installs                                             | mitigate    | No se instalan paquetes en esta fase.                                                                                                |

</threat_model>

<verification>
- `pnpm tsc --noEmit` limpio para notifications + cron.
- SEGMENT_TRANSITION_TEMPLATES mantiene los 4 template_key originales; TEMPLATE_SEEDS sin cambios.
- No quedan referencias a en_riesgo/ghost/espartano como ESTADOS (solo como nombres de template_key) ni a isFrequencyGoldenCase.
- Cron de las 3AM presente e intacto en su schedule.
</verification>

<success_criteria>

- Disparadores reconectados a alerta/ausente/optima/regular con los template_key y copy intactos.
- ghost_monthly_reattempt sobre ausente reusando los campos existentes.
- sendSegmentSchema con los 4 valores; tests actualizados.
  </success_criteria>

<output>
Create `.planning/phases/136-redise-o-del-sistema-de-segmentaci-n-etiqueta-de-asistencia-/136-02-SUMMARY.md` when done
</output>
