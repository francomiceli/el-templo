---
phase: 136-redise-o-del-sistema-de-segmentaci-n-etiqueta-de-asistencia-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/schema/member-profiles.ts
  - el-templo-api/src/db/migrations/0151_attendance_label_enum.sql
  - el-templo-api/src/modules/segmentation/types.ts
  - el-templo-api/src/modules/segmentation/service.ts
  - el-templo-api/test/segmentation/segmentation.test.ts
  - el-templo-api/test/segmentation/golden-case.test.ts
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-07, D-08, D-09]
must_haves:
  truths:
    - "El motor clasifica a un miembro con plan activo y ≥1 mes de antigüedad en exactamente una de optima/regular/alerta/ausente según su % de uso en 28 días"
    - "Un miembro con < 1 mes de antigüedad queda con segment NULL (sin etiqueta de Asistencia)"
    - "Un miembro sin plan activo (sin classesPerWeek) queda con segment NULL"
    - "≥75% → optima; 50-74% → regular; 1-49% → alerta; 0% → ausente; >100% → optima"
    - "La migración deja todas las filas existentes de member_segment en NULL (recálculo en limpio)"
  artifacts:
    - path: "el-templo-api/src/db/migrations/0151_attendance_label_enum.sql"
      provides: "ALTER del enum member_segment a las 4 bandas nuevas + reset a NULL"
      contains: "member_segment"
    - path: "el-templo-api/src/modules/segmentation/service.ts"
      provides: "calculateSegment reescrito a las 4 bandas de %"
    - path: "el-templo-api/src/modules/segmentation/types.ts"
      provides: "MemberSegment union de 4 valores + cortes fijos en código"
  key_links:
    - from: "el-templo-api/src/db/schema/member-profiles.ts"
      to: "el-templo-api/src/db/migrations/0151_attendance_label_enum.sql"
      via: "mysqlEnum 1er-arg = nombre de columna física member_segment"
      pattern: "member_segment"
    - from: "el-templo-api/src/modules/segmentation/service.ts"
      to: "el-templo-api/src/modules/segmentation/types.ts"
      via: "import MemberSegment + cortes fijos"
      pattern: "MemberSegment"
---

<objective>
Reescribir el motor de segmentación a la nueva etiqueta de **Asistencia** de 4 bandas (`optima`/`regular`/`alerta`/`ausente`) basadas 100% en el % de uso de la membresía sobre la ventana móvil de 28 días, alterar el enum `member_segment` en la DB y resetear los datos existentes a NULL (recálculo en limpio).

Purpose: Es el cambio FUNDACIONAL del que dependen todos los consumidores de `member_profiles.segment` (analytics, notificaciones, members, app, frontend, Horarios). El enum nuevo debe existir antes de propagar.
Output: Schema Drizzle + migración SQL + `segmentation/types.ts` + `segmentation/service.ts` reescritos + tests de segmentación reescritos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-redise-o-del-sistema-de-segmentaci-n-etiqueta-de-asistencia-/136-CONTEXT.md
@./CLAUDE.md

<interfaces>
<!-- Contratos que los planes de wave 2 consumirán. El enum nuevo es la fuente única. -->

Nuevo `MemberSegment` (segmentation/types.ts) — REEMPLAZA al de 6 valores:

```typescript
export type MemberSegment = "optima" | "regular" | "alerta" | "ausente";
```

Enum DB (member-profiles.ts) — el 1er arg ES el nombre físico de columna y DEBE coincidir con el ALTER de la migración:

```typescript
export const memberSegmentEnum = mysqlEnum("member_segment", [
  "optima",
  "regular",
  "alerta",
  "ausente",
]);
```

Cortes fijos en código (D-03, NO via system_settings):

- optima: attendancePct >= 75
- regular: 50 <= attendancePct < 75
- alerta: 1 <= attendancePct < 50 (>=1 y <50; valores fraccionarios entre 0 y 1 caen en alerta por ser >0)
- ausente: attendancePct == 0
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reescribir tipos del motor a 4 bandas + cortes fijos</name>
  <read_first>
    - el-templo-api/src/modules/segmentation/types.ts (fuente a reescribir)
    - el-templo-api/src/modules/segmentation/service.ts (consumidores de los tipos: getThresholds, SEGMENT_SETTINGS_KEYS, SEGMENT_DEFAULTS)
  </read_first>
  <behavior>
    - El union MemberSegment tiene exactamente 4 valores: optima, regular, alerta, ausente (D-01).
    - SEGMENT_LABELS mapea: optima→"Óptima", regular→"Regular", alerta→"Alerta", ausente→"Ausente".
    - SEGMENT_COLORS mapea (Claude's Discretion D-69): optima→"green", regular→"amber", alerta→"orange", ausente→"red".
    - SEGMENT_VALUES contiene los 4 valores en orden optima, regular, alerta, ausente.
    - Las constantes de corte (ej. ATTENDANCE_OPTIMA_PCT=75, ATTENDANCE_REGULAR_PCT=50, ATTENDANCE_ALERTA_PCT=1) viven en types.ts como constantes fijas (D-03), NO en system_settings.
    - WINDOW_DAYS sigue siendo 28 (D-02). Se conserva una constante de ventana fija.
  </behavior>
  <action>
    Reescribir `MemberSegment` al union de 4 valores `optima`/`regular`/`alerta`/`ausente` (D-01). Reescribir `SEGMENT_LABELS`, `SEGMENT_COLORS`, `SEGMENT_VALUES` a esos 4. Eliminar `SEGMENT_SETTINGS_KEYS`, `SEGMENT_DEFAULTS`, `SegmentThresholds` y `FREQUENCY_ZERO_VISIT_WINDOW_DAYS` (D-03: cortes fijos, ya no configurables). Introducir constantes exportadas de corte fijo: `ATTENDANCE_OPTIMA_PCT = 75`, `ATTENDANCE_REGULAR_PCT = 50`, `ATTENDANCE_ALERTA_PCT = 1`, y `ATTENDANCE_WINDOW_DAYS = 28` (D-02, D-03). Eliminar `enRiesgoWeeks`, `ghostWeeks`, `nuevoDays` y derivados de inactividad-por-semanas (D-01: desaparecen). Sin tipos `any`.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm tsc --noEmit 2>&1 | grep -i "segmentation/types" || echo "types OK"</automated>
  </verify>
  <done>types.ts exporta MemberSegment de 4 valores + constantes de corte fijas; no quedan referencias a SEGMENT_SETTINGS_KEYS/SEGMENT_DEFAULTS en types.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reescribir calculateSegment a las 4 bandas + reescribir tests</name>
  <read_first>
    - el-templo-api/src/modules/segmentation/service.ts (motor a reescribir — reutilizar steps 8-11 de cálculo de % en ventana 28d)
    - el-templo-api/src/modules/segmentation/types.ts (constantes de corte tras Task 1)
    - el-templo-api/test/segmentation/segmentation.test.ts (suite a reescribir)
    - el-templo-api/test/segmentation/golden-case.test.ts (probablemente eliminar — desaparece el golden case)
    - el-templo-api/test/helpers.ts (utilidades de auth/request/seed contra MySQL real)
  </read_first>
  <behavior>
    - calculateSegment(userId) devuelve `MemberSegment | null`.
    - Miembro con < 1 mes desde users.createdAt → retorna NULL (D-07: sin etiqueta en el primer mes; segment queda sin calcular).
    - Miembro sin subscription active/paused o sin classesPerWeek → retorna NULL (D-08: sin denominador).
    - Con plan activo y ≥1 mes: attendancePct = attendanceCount / (classesPerWeek × 28/7) × 100 sobre la ventana de 28 días.
    - attendancePct >= 75 → "optima"; 50 <= pct < 75 → "regular"; 0 < pct < 50 → "alerta"; pct == 0 → "ausente" (D-02, D-03, D-04: >100% sigue optima por ser >=75).
    - Desaparecen las ramas de nuevo, ghost, en_riesgo-por-inactividad, digital_warrior y el golden case (D-01).
    - calculateAndUpdate persiste segment (incluido NULL) en member_profiles respetando el cooldown de 1h existente. recordLogin se mantiene sin cambios.
  </behavior>
  <action>
    Reescribir `calculateSegment` para que: (1) elimine `getThresholds` (cortes fijos desde constantes de types.ts), (2) elimine las ramas nuevo/ghost/en_riesgo-inactividad/digital_warrior/isFrequencyGoldenCase y el parámetro `opts.isFrequencyGoldenCase`, (3) retorne NULL para < 1 mes de antigüedad (D-07) y para sin plan activo/sin classesPerWeek (D-08), (4) reutilice el cálculo de `attendancePct` en ventana de 28 días (steps 8-11 actuales) y clasifique con las constantes `ATTENDANCE_OPTIMA_PCT`/`ATTENDANCE_REGULAR_PCT`/`ATTENDANCE_ALERTA_PCT`. Cambiar la firma de retorno a `Promise<MemberSegment | null>`. Ajustar `calculateAndUpdate` para persistir NULL cuando corresponda (el cooldown se evalúa, pero NULL es un resultado válido a guardar). Eliminar `DIGITAL_WARRIOR_MIN_APP_USAGE` y `isFrequencyGoldenCase`. Quitar el import de `FrequencyService` si quedara huérfano (se limpia en el plan de notificaciones). Sin `console.log`; usar `this.log`. Sin `any`.

    Reescribir `test/segmentation/segmentation.test.ts` a las 4 bandas: casos de ≥75/50-74/1-49/0%, caso < 1 mes → NULL, caso sin plan activo → NULL, caso >100% → optima. Usar el patrón de seed/auth de `test/helpers.ts` contra `eltemplo_test`. ELIMINAR `test/segmentation/golden-case.test.ts` (desaparece el golden case, D-01).

  </action>
  <verify>
    <automated>cd el-templo-api && test ! -f test/segmentation/golden-case.test.ts && pnpm tsc --noEmit 2>&1 | grep -iE "segmentation/service|segmentation.test" || echo "engine + tests typecheck OK, golden-case removed"</automated>
  </verify>
  <done>calculateSegment retorna una de las 4 bandas o NULL; golden-case.test.ts eliminado; segmentation.test.ts reescrito a 4 bandas; typecheck del módulo limpio. (Los tests corren en CI al pushear, no localmente.)</done>
</task>

<task type="auto">
  <name>Task 3: Alterar enum member_segment + migración con reset a NULL</name>
  <read_first>
    - el-templo-api/src/db/schema/member-profiles.ts:40-47 (memberSegmentEnum a actualizar)
    - el-templo-api/src/db/migrations/0064_rename_nuevo_guerrero.sql (patrón de ALTER de este enum)
    - CLAUDE.md sección "Database Changes" (flujo db:generate + commit del .sql; nunca drizzle-kit push/migrate)
  </read_first>
  <action>
    Actualizar `memberSegmentEnum` en `member-profiles.ts` a `mysqlEnum("member_segment", ["optima","regular","alerta","ausente"])` (D-01) — el 1er arg `member_segment` ES el nombre físico de columna y DEBE coincidir con el ALTER de la migración (memoria reference_drizzle_enum_column_name: drift rompe CI con "Unknown column").

    Crear la migración SQL `0151_attendance_label_enum.sql` (siguiente número tras 0150) que, en orden: (1) `UPDATE member_profiles SET member_segment = NULL` para limpiar todas las filas (D-09: recálculo en limpio, NO mapeo viejo→nuevo); (2) `ALTER TABLE member_profiles MODIFY COLUMN member_segment enum('optima','regular','alerta','ausente') DEFAULT NULL`; (3) opcionalmente limpiar los settings huérfanos de % configurables con `DELETE FROM system_settings WHERE setting_key IN ('segment.espartano_pct','segment.intermitente_pct','segment.frequency_zero_visit_window_days', ...)` (D-03). NUNCA usar `;` dentro de comentarios `--` (el runner splittea por `;` antes de strippear `--`, rompe la migración entera). Preferir `pnpm db:generate` para el ALTER del enum y luego editar el .sql para anteponer el reset a NULL + el DELETE de settings; COMMITEAR el archivo .sql (los ejecutores suelen olvidarlo).

  </action>
  <verify>
    <automated>cd el-templo-api && grep -q "enum('optima','regular','alerta','ausente')" src/db/migrations/0151_attendance_label_enum.sql && grep -q "member_segment = NULL" src/db/migrations/0151_attendance_label_enum.sql && grep -v '^--' src/db/migrations/0151_attendance_label_enum.sql | grep -c "optima" && echo "migration OK"</automated>
  </verify>
  <done>El schema Drizzle y el ALTER de la migración coinciden en `member_segment` con los 4 valores; la migración resetea las filas a NULL antes del ALTER; el .sql está creado y commiteado; sin `;` dentro de comentarios.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                |
| ------------------------------------ | -------------------------------------------------------------------------- |
| cron/batch → member_profiles.segment | El motor escribe la etiqueta de comportamiento; dato sensible de miembro   |
| migración → datos en prod            | El reset a NULL es destructivo de los segmentos viejos (intencional, D-09) |

## STRIDE Threat Register

| Threat ID | Category               | Component                                            | Disposition | Mitigation Plan                                                                                                                                                                    |
| --------- | ---------------------- | ---------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-136-01  | Tampering              | migración 0151 (reset a NULL)                        | accept      | D-09 decide recálculo en limpio; el cron 3AM + recálculo on-login repueblan. No hay pérdida de dato de negocio (el segmento es derivado, recalculable desde attendance/createdAt). |
| T-136-02  | Information Disclosure | calculateSegment retorna NULL para <1 mes / sin plan | mitigate    | NULL es el contrato explícito (D-07/D-08); evita exponer una etiqueta injusta. Los consumidores ya manejan NULL (sinSegmento).                                                     |
| T-136-03  | Denial of Service      | ALTER enum + UPDATE masivo en prod                   | accept      | Tabla member_profiles acotada (miembros activos); ALTER de enum en MySQL reescribe la tabla pero el volumen es bajo. Se aplica vía pipeline en ventana de deploy.                  |
| T-136-SC  | Tampering              | npm/pip/cargo installs                               | mitigate    | Esta fase NO instala paquetes nuevos. Si surge la necesidad, gate humano bloqueante + RESEARCH Package Legitimacy Audit.                                                           |

</threat_model>

<verification>
- `pnpm tsc --noEmit` limpio en el-templo-api para el módulo de segmentación.
- El enum en schema y migración coinciden exactamente (4 valores, mismo nombre de columna `member_segment`).
- La migración resetea a NULL antes del ALTER y no tiene `;` en comentarios.
- golden-case.test.ts eliminado; segmentation.test.ts reescrito a 4 bandas.
</verification>

<success_criteria>

- MemberSegment = 4 bandas; calculateSegment clasifica por % en 28d con cortes fijos 75/50/1 y retorna NULL para <1 mes y sin plan.
- Migración 0151 creada y commiteada, schema↔SQL en sync.
- Tests de segmentación reescritos; golden-case eliminado.
  </success_criteria>

<output>
Create `.planning/phases/136-redise-o-del-sistema-de-segmentaci-n-etiqueta-de-asistencia-/136-01-SUMMARY.md` when done
</output>
