---
phase: 155-horarios
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/schema/activities.ts
  - el-templo-api/src/db/migrations/0167_activity_max_capacity.sql
  - el-templo-api/src/modules/scheduling/service.ts
  - el-templo-api/src/modules/scheduling/booking-service.ts
autonomous: true
requirements: [HOR-01, HOR-03]
must_haves:
  truths:
    - "Dos slots de actividades DISTINTAS en la misma sucursal/día/hora conviven (no hay error de solape)"
    - "Dos slots de la MISMA actividad solapados siguen siendo rechazados (409) con mensaje que nombra la actividad"
    - "Cambiar la actividad de un slot para que coincida con otra clase simultánea de la misma actividad se rechaza (gap de updateScheduleActivity cerrado)"
    - "El cupo efectivo de un slot = activity.maxCapacity ?? branch.maxCapacity ?? 22, resuelto en un único helper"
    - "Una actividad con max_capacity NULL hereda el cupo de la sucursal sin cambio de comportamiento"
  artifacts:
    - path: "el-templo-api/src/db/schema/activities.ts"
      provides: "Columna maxCapacity int nullable"
      contains: "maxCapacity"
    - path: "el-templo-api/src/db/migrations/0167_activity_max_capacity.sql"
      provides: "ALTER activities ADD max_capacity NULL"
      contains: "ADD COLUMN `max_capacity`"
    - path: "el-templo-api/src/modules/scheduling/service.ts"
      provides: "Overlap re-scopeado por actividad en createSchedule + updateScheduleActivity; cupo efectivo en getWeeklyGrid/getSlotDetail"
    - path: "el-templo-api/src/modules/scheduling/booking-service.ts"
      provides: "getEffectiveCapacity(branchId, activityId) usado en los 4 call sites"
  key_links:
    - from: "service.ts createSchedule/updateScheduleActivity"
      to: "schema.schedules.activityId"
      via: "filtro extra en la query de overlap"
      pattern: "schedules\\.activityId"
    - from: "booking-service.ts getEffectiveCapacity"
      to: "activities.maxCapacity / branches.maxCapacity"
      via: "resolución de cupo efectivo por slot"
      pattern: "maxCapacity"
---

<objective>
Backend de scheduling para clases simultáneas (HOR-01) y capacidad por actividad (HOR-03).

Purpose: Habilitar que musculación conviva con actividades especiales en la misma sucursal/hora (re-scope del overlap por actividad, D-01) y que cada actividad tenga su propio cupo con fallback a la sucursal (D-05/D-06/D-07). Cimiento del resto de la fase: el schema y el cupo efectivo por slot son consumidos por el ABM (155-02) y por la grilla del admin/member app (155-04).

Output: Columna `activities.max_capacity` nullable + migración 0167; overlap distinguiendo actividad en `createSchedule` y en el gap de `updateScheduleActivity`; helper único de cupo efectivo aplicado en los 4 call sites de reservas y en las lecturas de grilla/detalle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-horarios/155-CONTEXT.md
@.planning/phases/155-horarios/155-PATTERNS.md

<interfaces>
<!-- Contratos existentes que el executor consume directamente — sin exploración. -->

booking-service.ts — helper privado a envolver (líneas ~1757-1764):
private async getBranchCapacity(branchId: number): Promise<number>
→ SELECT branches.maxCapacity WHERE id = branchId; return maxCapacity ?? 22

getScheduleSlotRaw ya devuelve activityId (línea ~1775) — disponible en scheduleRow/sched en los 4 call sites.

service.ts createSchedule overlap (líneas ~93-116): query sobre schedules con
and(eq(branchId), eq(dayOfWeek), eq(isActive,true), lt(startTime,endTime), gt(endTime,startTime)).limit(1)
El activity row ya se valida antes (líneas ~72-78) — ese select puede traer también name y maxCapacity.

service.ts updateScheduleActivity (líneas ~513-545): cambia activityId SIN check de solape (gap, hallazgo 5 de PATTERNS).

service.ts getWeeklyGrid: joinea activities (~189-192); hoy lee branch.maxCapacity una vez (~158) y lo aplica a todos los slots (~274-275, incluye isFull).
service.ts getSlotDetail: resuelve branch?.maxCapacity ?? 22 (~299-304).

Última migración: 0166_seed_pricing_card_surcharge.sql → la nueva es 0167.
branches.ts:24 → maxCapacity: int("max_capacity").default(22).notNull() (analog; la nueva es nullable, sin default).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Columna activities.max_capacity nullable + migración 0167</name>
  <files>el-templo-api/src/db/schema/activities.ts, el-templo-api/src/db/migrations/0167_activity_max_capacity.sql</files>
  <read_first>
    - el-templo-api/src/db/schema/activities.ts (archivo a modificar)
    - el-templo-api/src/db/schema/branches.ts (analog: maxCapacity int, línea 24)
    - el-templo-api/src/db/migrations/0166_seed_pricing_card_surcharge.sql (numeración previa)
    - el-templo-api/src/db/migrations/0162_created_member_id.sql (patrón hand-written)
  </read_first>
  <action>
    En `activities.ts`, agregar la columna `maxCapacity: int("max_capacity")` (nullable, SIN default, SIN notNull) después de `isActive` (D-05). El nombre de columna del primer arg (`max_capacity`) debe coincidir exacto con la migración o CI falla con Unknown column.

    Crear `0167_activity_max_capacity.sql` hand-written (db:generate está roto por drift; `_migrations` es la fuente de verdad). Header con fase (155 / HOR-03 / D-05), justificación de hand-written y semántica NULL=hereda sucursal, sin backfill. `ALTER TABLE activities ADD COLUMN max_capacity int NULL AFTER is_active`. PROHIBIDO usar `;` dentro de comentarios SQL (el runner splitea por `;` antes de strippear `--`). Commitear el .sql junto al schema.

  </action>
  <acceptance_criteria>
    - `activities.ts` exporta la tabla con `maxCapacity` mapeada a columna `max_capacity`, tipo int, nullable (sin `.notNull()` ni `.default(...)`).
    - Existe `0167_activity_max_capacity.sql` con `ADD COLUMN` `max_capacity` `int` `NULL`.
    - Ningún comentario del .sql contiene el carácter `;`.
    - `cd el-templo-api && pnpm exec tsc --noEmit` pasa.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit && grep -q 'max_capacity' src/db/migrations/0167_activity_max_capacity.sql && ! grep -E '^\s*--.*;' src/db/migrations/0167_activity_max_capacity.sql</automated>
  </verify>
  <done>Schema tiene la columna nullable, migración 0167 existe sin `;` en comentarios, tsc verde.</done>
</task>

<task type="auto">
  <name>Task 2: Overlap re-scopeado por actividad (createSchedule + gap de updateScheduleActivity)</name>
  <files>el-templo-api/src/modules/scheduling/service.ts</files>
  <read_first>
    - el-templo-api/src/modules/scheduling/service.ts (createSchedule ~45-120; updateScheduleActivity ~513-545)
    - el-templo-api/src/modules/scheduling/schemas.ts (mensajes de error en español sin tildes)
  </read_first>
  <action>
    D-01: En la query de overlap de `createSchedule` (~93-116), agregar `eq(schema.schedules.activityId, activityId)` dentro del `and(...)`. Preservar exactamente la semántica de intervalos `[start,end)` (los `lt(startTime,endTime)`/`gt(endTime,startTime)` no se tocan — back-to-back sigue permitido). Ajustar el mensaje del `ConflictError` para nombrar la actividad (traer `name` en el select de validación de actividad de ~72-78, o en el select de overlap). Mensaje en español SIN tildes (patrón: "Ya existe un horario ... de esta actividad que se solapa en esta sede y dia").

    Cerrar el gap de hallazgo 5: en `updateScheduleActivity` (~513-545), antes del `update`, correr el MISMO check de solape re-scopeado por `(branchId, dayOfWeek, activityId=nuevo, isActive)` usando el `startTime`/`endTime` del slot existente y EXCLUYENDO el propio `scheduleId` (`ne(schedules.id, scheduleId)`). Si hay solape con otra clase de la actividad destino, lanzar `ConflictError`. Reusar la misma forma de query (extraer un método privado compartido si reduce duplicación — a discreción, DRY).

  </action>
  <acceptance_criteria>
    - La query de overlap de `createSchedule` incluye un filtro por `activityId`.
    - `updateScheduleActivity` ejecuta un check de solape re-scopeado por la actividad destino que excluye el propio slot (`ne(...id, scheduleId)`) antes del update.
    - El mensaje de conflicto nombra la actividad y no contiene tildes.
    - La semántica `[start,end)` (comparaciones `lt`/`gt`) queda intacta.
    - `pnpm exec tsc --noEmit` pasa.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit && grep -c 'schedules.activityId' src/modules/scheduling/service.ts | grep -qv '^0$'</automated>
  </verify>
  <done>Overlap distingue actividad en createSchedule y en updateScheduleActivity; tests de comportamiento se cubren en 155-02.</done>
</task>

<task type="auto">
  <name>Task 3: Cupo efectivo — helper único (booking-service) + grilla/detalle (service)</name>
  <files>el-templo-api/src/modules/scheduling/booking-service.ts, el-templo-api/src/modules/scheduling/service.ts</files>
  <read_first>
    - el-templo-api/src/modules/scheduling/booking-service.ts (getBranchCapacity ~1757-1764; call sites ~278/600/1088/1255; getScheduleSlotRaw ~1775)
    - el-templo-api/src/modules/scheduling/service.ts (getWeeklyGrid ~147-286; getSlotDetail ~299-304)
    - el-templo-api/src/db/schema/branches.ts (maxCapacity fallback)
  </read_first>
  <action>
    D-06/D-07: En `booking-service.ts`, reemplazar `getBranchCapacity(branchId)` por un helper único `getEffectiveCapacity(branchId, activityId)` que resuelva `activity.maxCapacity ?? branch.maxCapacity ?? 22` (una query que traiga ambos, o dos selects; naming a discreción). Reemplazar 1:1 en los 4 call sites: reserve (~278), adminAddBooking (~600), findNextAvailableDate (~1088), generación de bookings de plan fijo (~1255). Todos ya tienen `activityId` a mano vía `scheduleRow`/`sched` (getScheduleSlotRaw devuelve activityId); en el call site ~1255 verificar que el select de `sched` incluya `activityId`, y agregarlo si falta. El check sigue siendo POR SLOT (`activeCount >= cupo`) — D-07 se cumple solo cambiando la fuente del número, NO se agrega techo agregado por sucursal.

    En `service.ts`: en `getWeeklyGrid`, agregar `activityMaxCapacity: schema.activities.maxCapacity` al select (el join a activities ya existe, ~189-192) y computar el cupo por slot como `row.activityMaxCapacity ?? branch.maxCapacity` reemplazando el `maxCapacity` único de branch (~274-275, incluido `isFull`). En `getSlotDetail` (~299-304) aplicar la misma resolución efectiva.

  </action>
  <acceptance_criteria>
    - No queda ninguna llamada a `getBranchCapacity` en booking-service.ts (reemplazadas por el helper efectivo en los 4 call sites).
    - El helper resuelve `activity.maxCapacity ?? branch.maxCapacity ?? 22`.
    - `getWeeklyGrid` selecciona `activities.maxCapacity` y computa cupo/`isFull` por slot con `?? branch.maxCapacity`.
    - `getSlotDetail` resuelve cupo efectivo del slot.
    - No se introduce ningún techo agregado por sucursal (el check permanece per-slot).
    - `pnpm exec tsc --noEmit` pasa.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit && ! grep -q 'getBranchCapacity' src/modules/scheduling/booking-service.ts</automated>
  </verify>
  <done>Cupo efectivo por slot resuelto en un helper único (4 call sites) y en las lecturas de grilla/detalle; sin techo agregado.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                |
| ------------------------------------ | -------------------------------------------------------------------------- |
| staff (admin/profe) → API scheduling | Body de createSchedule/updateScheduleActivity: branchId, activityId, horas |
| socio → API booking                  | Reserva contra el cupo del slot                                            |

## STRIDE Threat Register

| Threat ID | Category               | Component                                        | Disposition | Mitigation Plan                                                                                                                                                                                                                                           |
| --------- | ---------------------- | ------------------------------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-155-01  | Tampering              | overlap de createSchedule/updateScheduleActivity | mitigate    | El filtro por activityId conserva la invariante "no dos slots de la misma actividad solapados"; el gap de updateScheduleActivity se cierra con el mismo check (excluyendo el propio slot) para que no se pueda crear un solape misma-actividad silencioso |
| T-155-02  | Elevation of Privilege | rutas /schedules                                 | accept      | El guard módulo-level existente (authenticate + ALL_STAFF_ROLES + requireBranchAccess en POST /schedules) NO se modifica (Horarios es categoría libre, 149 D-04)                                                                                          |
| T-155-03  | Denial of Service      | cupo efectivo por slot                           | mitigate    | Fallback `?? branch.maxCapacity ?? 22` evita cupo undefined/NaN; NULL en actividad hereda la sucursal sin abrir cupo infinito                                                                                                                             |
| T-155-SC  | Tampering              | npm installs                                     | accept      | Esta fase NO instala ni actualiza dependencias (solo schema/lógica). Sin superficie de supply chain nueva.                                                                                                                                                |

</threat_model>

<verification>
- `cd el-templo-api && pnpm exec tsc --noEmit` verde.
- Migración 0167 presente, sin `;` en comentarios, aplicable con `pnpm db:migrate` (runner propio; NUNCA drizzle-kit migrate/push).
- Overlap distingue actividad; el gap de updateScheduleActivity queda cerrado.
- `getBranchCapacity` eliminado de booking-service; cupo efectivo por slot en los 4 call sites + grilla + detalle.
- Los tests de integración que prueban este comportamiento se crean en 155-02 (suite corre en CI).
</verification>

<success_criteria>

- Dos actividades distintas pueden ocupar la misma sucursal/día/hora; dos slots de la misma actividad solapados siguen rechazándose.
- Cada slot resuelve su cupo efectivo (actividad ?? sucursal) en un único helper.
- tsc verde; migración 0167 lista para el tren.
  </success_criteria>

<output>
Create `.planning/phases/155-horarios/155-01-SUMMARY.md` when done
</output>
