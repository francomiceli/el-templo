---
phase: 155-horarios
plan: 04
type: execute
wave: 4
depends_on: [155-03, 155-01]
files_modified:
  - el-templo-admin/src/pages/HorariosPage.vue
autonomous: false
requirements: [HOR-01, HOR-02]
must_haves:
  truths:
    - "La grilla del admin muestra N clases simultáneas en la misma celda (la segunda ya no pisa a la primera)"
    - "El click en un slot puntual (detalle/borrado) opera sobre ESE slot, no sobre 'el slot de la celda'"
    - "El click en una celda vacía abre CreateSlotDialog prefilleado con sucursal seleccionada, día y hora de la celda"
    - "El cupo/ocupación mostrado por slot usa el cupo efectivo servido por la API (getWeeklyGrid)"
    - "La vista de horarios del member app sigue mostrando N clases por franja con el cupo correcto (verify-only, sin cambios de código)"
  artifacts:
    - path: "el-templo-admin/src/pages/HorariosPage.vue"
      provides: "slotMap Map<string, WeeklySlotView[]>, render apilado, onCellClick por slot, click-para-crear con prefill"
  key_links:
    - from: "HorariosPage celda vacía onCellClick"
      to: "CreateSlotDialog prop initial"
      via: "prefill { branchId, dayOfWeek, startTime }"
      pattern: "initial"
    - from: "HorariosPage render de celda"
      to: "slotMap por franja"
      via: "iteración sobre el array de slots"
      pattern: "WeeklySlotView\\[\\]"
---

<objective>
Grilla del admin con clases simultáneas + crear clase desde el slot (HOR-01 / HOR-02, D-02/D-03), y verificación de la vista del member app.

Purpose: Cerrar la parte visible de simultaneidad: hoy `slotMap` keyea por `${startTime}-${dayOfWeek}` y la segunda clase simultánea PISA a la primera (hallazgo 1 de PATTERNS). Se migra a N slots por celda, el click desambigua por slot puntual, y la celda vacía abre el dialog prefilleado (reusa el prop `initial` de 155-03). El member app ya renderiza listas → verify-only.

Output: `HorariosPage.vue` con `Map<string, WeeklySlotView[]>`, render apilado, `onCellClick` por slot y click-para-crear; checkpoint humano de verificación visual (admin + member app).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-horarios/155-CONTEXT.md
@.planning/phases/155-horarios/155-PATTERNS.md
@.planning/phases/155-horarios/155-03-SUMMARY.md

<interfaces>
<!-- Estructuras reales del admin a migrar (líneas aproximadas de PATTERNS). -->

HorariosPage.vue:
slotKey(startTime, dayOfWeek) = `${startTime}-${dayOfWeek}` (~552).
slotMap = computed Map<string, WeeklySlotView> — hoy map.set PISA simultáneas (~557-563).
getCellSlot(time, day): WeeklySlotView | undefined (~574).
cellClass(...) por slot único (~582-595).
Render desktop de celda (~301-336): activityName / ocupación / FERIADO / CANCELADA.
onCellClick (~855-883): if(!slot) return (~857); modo delete-selection (~862-874); abre SlotDetailDialog por slot.
Vista mobile selectedDaySlots (~609-613) YA es lista, PERO su handler onMobileSlotClick (~628-632) descarta el slot concreto y llama onCellClick(slot.startTime, slot.dayOfWeek, info.date) — con 2 simultáneas la re-derivación por (time,day) resuelve al slot equivocado (blocker del plan-checker): migrarlo junto con el refactor.
La celda conoce row.time y day.dayOfWeek/day.date (~308).
CreateSlotDialog se embebe acá; el prop `initial` se agregó en 155-03.
Celda vacía: .grid-cell--empty tiene cursor: default (~1003-1010).

Member app (verify-only): el-templo-app/src/pages/ReservasPage.vue
selectedDaySlots (~967-976) ya es lista por día; cupo mostrado = slot.maxCapacity - slot.bookedCount (~1084)
→ se corrige solo al servir cupo efectivo desde la API. Sin cambios de código.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HorariosPage — N slots por celda + click por slot + click-para-crear</name>
  <files>el-templo-admin/src/pages/HorariosPage.vue</files>
  <read_first>
    - el-templo-admin/src/pages/HorariosPage.vue (slotMap ~552-576, cellClass ~582-595, render celda ~301-336, onCellClick ~855-883, onMobileSlotClick ~628-632 + gate isMobile `$q.screen.lt.sm` ~128/451, grid-cell--empty ~1003-1010)
    - el-templo-admin/src/components/scheduling/CreateSlotDialog.vue (prop initial de 155-03)
  </read_first>
  <action>
    D-02: Migrar `slotMap` de `Map<string, WeeklySlotView>` a `Map<string, WeeklySlotView[]>` (en vez de `map.set` que pisa, hacer push al array de la key). Renombrar/ajustar `getCellSlot` → `getCellSlots(time, day): WeeklySlotView[]`. El render de celda desktop (~301-336) itera el array y apila las clases simultáneas (activityName/ocupación/FERIADO/CANCELADA por slot) — layout apilado vs columnas a discreción. `cellClass` (~582-595) pasa a colorear por slot apilado (o un agregado de celda, a discreción). La vista mobile (`selectedDaySlots`) ya es lista → no requiere cambios.

    Click por slot: `onCellClick` (~855-883) hoy resuelve un único slot por celda; con N slots, el click de detalle/borrado debe venir del sub-elemento del slot puntual (propagar el `WeeklySlotView` clickeado, no la celda), incluido el modo delete-selection (~862-874) que elige por slot. TAMBIÉN migrar `onMobileSlotClick` (~628-632): ya tiene el `WeeklySlotView` concreto en mano (viene del `v-for` de `selectedDaySlots` con `:key="slot.id"`) pero hoy lo descarta y llama `onCellClick(slot.startTime, slot.dayOfWeek, info.date)` — debe pasar el slot concreto (p.ej. por `scheduleId`) al nuevo entry point slot-aware, sin round-trip por (time, day). Con dos simultáneas, la lista mobile debe abrir/borrar EXACTAMENTE el slot tocado (blocker señalado por el plan-checker: aplica a detalle Y a delete-selection).

    D-03 (click-para-crear): cuando la celda esté vacía (sin slots), el click abre `CreateSlotDialog` pasando `initial = { branchId: selectedBranchId, dayOfWeek: day.dayOfWeek, startTime: row.time }` (la celda conoce esos valores, ~308). Quitar el `cursor: default` de `.grid-cell--empty` y dar un affordance de "click para crear" (a discreción). El botón global "Crear horario" se mantiene sin prefill.

    Logger `createLogger('HorariosPage')`, nunca console. Sin `any` (usar `WeeklySlotView`).

  </action>
  <acceptance_criteria>
    - `slotMap` es `Map<string, WeeklySlotView[]>`; ningún slot simultáneo se pierde (no hay `map.set` que sobreescriba una franja con dos actividades).
    - El render de celda itera sobre el array y muestra las N clases; el cupo/ocupación por slot viene de los datos de la API (cupo efectivo).
    - El detalle/borrado se dispara sobre el slot puntual clickeado (no sobre "el slot de la celda"), TANTO en la grilla desktop COMO en la lista mobile: `onMobileSlotClick` pasa el slot concreto (grep de `onCellClick(slot.startTime` en HorariosPage.vue devuelve 0 matches).
    - El click en celda vacía abre `CreateSlotDialog` con `initial` (branchId seleccionado, día, hora); el botón global sigue funcionando.
    - `cd el-templo-admin && pnpm lint` pasa; sin `any`.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-admin && pnpm lint && grep -q 'WeeklySlotView\[\]' src/pages/HorariosPage.vue && grep -q 'initial' src/pages/HorariosPage.vue</automated>
  </verify>
  <done>La grilla muestra clases simultáneas, desambigua el click por slot y crea horarios desde celdas vacías con prefill.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verificación visual de simultaneidad + cupo (admin + member app)</name>
  <what-built>
    Grilla del admin con clases simultáneas (N por celda), click-para-crear desde celda vacía con prefill, y cupo por actividad end-to-end (schema/API/UI). Vista del member app sirviendo el cupo efectivo por slot.
  </what-built>
  <how-to-verify>
    1. Admin → Horarios: crear dos clases de actividades DISTINTAS a la misma hora en la misma sucursal (ej. Musculación 10-11 y Funcional 10-11). Ambas deben aparecer apiladas en la misma celda (ninguna desaparece).
    2. Intentar crear una segunda clase de la MISMA actividad solapada → debe rechazarse con mensaje que nombra la actividad.
    3. Click en una celda VACÍA → se abre "Crear horario" con sucursal/día/hora ya precargados y editables. Desde ese dialog, crear una actividad nueva inline con un Cupo (ej. 8).
    4. Click en una de las clases apiladas → abre el detalle/borrado de ESA clase (no de la otra simultánea).
    5. Tab Actividades: editar una actividad y setear/limpiar el campo Cupo (vacío = hereda la sucursal).
    6. Member app → Reservas: en un día con dos clases a la misma hora, deben verse las dos cards; el cupo mostrado debe reflejar el cupo de la actividad (menor al de la sucursal si se configuró).
    7. VISTA MOBILE del admin (achicar la ventana a ancho de teléfono o usar el device toolbar): en el listado por día con dos clases simultáneas, tocar cada una — debe abrir el detalle de ESA clase (no la otra); probar también el modo "Eliminar horario" desde mobile y verificar que selecciona el slot correcto.
  </how-to-verify>
  <resume-signal>Escribí "approved" o describí los problemas visuales/funcionales encontrados.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                               |
| -------------------------------- | --------------------------------------------------------- |
| admin (browser) → API /schedules | Prefill de creación desde celda vacía (branchId/día/hora) |

## STRIDE Threat Register

| Threat ID | Category    | Component                        | Disposition | Mitigation Plan                                                                                                                                                     |
| --------- | ----------- | -------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-155-09  | Tampering   | prefill de CreateSlotDialog      | mitigate    | El prefill solo precarga campos editables; la validación de solape/branch access es server-side (createSchedule + requireBranchAccess), el cliente no decide        |
| T-155-10  | Repudiation | click por slot (detalle/borrado) | mitigate    | La desambiguación por slot puntual evita borrar/editar la clase equivocada al haber simultáneas; el borrado sigue validado server-side (falla con bookings activas) |
| T-155-SC  | Tampering   | npm installs                     | accept      | Esta fase NO instala/actualiza dependencias.                                                                                                                        |

</threat_model>

<verification>
- `cd el-templo-admin && pnpm lint` verde.
- slotMap soporta N slots; sin pérdida de clases simultáneas; click por slot; click-para-crear con prefill.
- Checkpoint humano aprueba la simultaneidad visual (admin + member app) y el cupo end-to-end.
</verification>

<success_criteria>

- Musculación y una actividad especial conviven visualmente en la misma celda/hora.
- El admin crea horarios y actividades (con cupo) directamente desde el slot.
- El member app muestra N clases por franja con el cupo efectivo.
  </success_criteria>

<output>
Create `.planning/phases/155-horarios/155-04-SUMMARY.md` when done
</output>
