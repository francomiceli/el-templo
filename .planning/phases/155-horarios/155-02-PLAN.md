---
phase: 155-horarios
plan: 02
type: execute
wave: 2
depends_on: [155-01]
files_modified:
  - el-templo-api/src/modules/scheduling/activity-service.ts
  - el-templo-api/src/modules/scheduling/schemas.ts
  - el-templo-api/src/modules/scheduling/types.ts
  - el-templo-api/src/modules/scheduling/routes.ts
  - el-templo-api/test/scheduling/155-horarios.test.ts
autonomous: true
requirements: [HOR-01, HOR-03]
must_haves:
  truths:
    - "POST /activities acepta maxCapacity y lo persiste; GET/POST/PUT lo devuelven en la respuesta"
    - "PUT /activities/:id con maxCapacity=null limpia el cupo (vuelve a heredar la sucursal)"
    - "Crear un slot solapado de actividad DISTINTA responde 201; de la MISMA actividad responde 409"
    - "updateScheduleActivity que provoca solape misma-actividad responde 409"
    - "Reservar en un slot cuya actividad tiene maxCapacity < cupo sucursal cae a lista_espera en el menor; actividad NULL hereda la sucursal"
  artifacts:
    - path: "el-templo-api/src/modules/scheduling/activity-service.ts"
      provides: "createActivity/updateActivity aceptan maxCapacity"
      contains: "maxCapacity"
    - path: "el-templo-api/src/modules/scheduling/schemas.ts"
      provides: "maxCapacity en activityRecordSchema, createActivitySchema, updateActivitySchema"
      contains: "maxCapacity"
    - path: "el-templo-api/test/scheduling/155-horarios.test.ts"
      provides: "Cobertura de overlap re-scopeado, cupo efectivo y ABM de cupo"
      contains: "155"
  key_links:
    - from: "routes.ts POST/PUT /activities"
      to: "activityService.createActivity/updateActivity"
      via: "request.body.maxCapacity"
      pattern: "maxCapacity"
    - from: "schemas.ts activityRecordSchema"
      to: "response del cliente"
      via: "propiedad declarada (fast-json-stringify strippea lo no declarado)"
      pattern: "maxCapacity"
---

<objective>
ABM de cupo por actividad (HOR-03 / D-08) + suite de integración que prueba todo el backend de la fase (simultaneidad HOR-01 y cupo efectivo HOR-03).

Purpose: Exponer `maxCapacity` en el CRUD de actividades para que el admin defina el cupo, y garantizar por test el comportamiento del re-scope de solape y del cupo efectivo introducidos en 155-01 (los tests corren en CI, no localmente).

Output: `maxCapacity` en service/schema/types/routes de actividades + `155-horarios.test.ts` con los casos de CONTEXT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-horarios/155-CONTEXT.md
@.planning/phases/155-horarios/155-PATTERNS.md
@.planning/phases/155-horarios/155-01-SUMMARY.md

<interfaces>
<!-- Firmas reales verificadas — el executor las usa directamente. -->

activity-service.ts createActivity es POSICIONAL:
async createActivity(name: string, description?: string): Promise<ActivityRecord>
→ hace insert values({ name, description }). Agregar 3er param maxCapacity y sumarlo al insert.
updateActivity(activityId, data): patrón de update parcial `if (data.x !== undefined) updateData.x = data.x`.

routes.ts POST /activities: Body generic { name; description? }; el handler llama
activityService.createActivity(request.body.name, request.body.description) → agregar request.body.maxCapacity.
routes.ts PUT /activities/:activityId: Body generic { name?; description?; isActive? }; el handler pasa request.body ENTERO a updateActivity → agregar maxCapacity al generic.

schemas.ts:
activityRecordSchema (línea ~22) — response; fast-json-stringify STRIPPEA lo no declarado (comentario ~177-180). Agregar maxCapacity o el campo no llega al cliente.
createActivitySchema (~130), updateActivitySchema (~158) — body properties.
weeklySlotViewSchema (~57) y getSlotDetail response (~267) YA declaran maxCapacity: { type: "integer" } → no tocar.

types.ts ActivityRecord (~20-27): id/name/description/isActive/createdAt/updatedAt → sumar maxCapacity: number | null.

Test analog: el-templo-api/test/scheduling/schedule-activity-crud.test.ts
(createTestApp + getAuthToken("admin@test.com","adminpass123") + cleanAllTestData en beforeEach

- fake timers pinneados + branch seeded no-virtual + helpers createActivity/postSchedule).
  Seeding de bookings/lista_espera: ver test/scheduling/scheduling.test.ts.
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: maxCapacity en el ABM de actividades (service + schema + types + routes)</name>
  <files>el-templo-api/src/modules/scheduling/activity-service.ts, el-templo-api/src/modules/scheduling/schemas.ts, el-templo-api/src/modules/scheduling/types.ts, el-templo-api/src/modules/scheduling/routes.ts</files>
  <read_first>
    - el-templo-api/src/modules/scheduling/activity-service.ts (createActivity ~24-56, updateActivity ~75-160)
    - el-templo-api/src/modules/scheduling/schemas.ts (activityRecordSchema ~22, createActivitySchema ~130, updateActivitySchema ~158)
    - el-templo-api/src/modules/scheduling/types.ts (ActivityRecord ~20-27)
    - el-templo-api/src/modules/scheduling/routes.ts (POST/PUT /activities ~123-160)
  </read_first>
  <behavior>
    - createActivity con maxCapacity=10 → persiste 10 y lo devuelve en el record.
    - createActivity sin maxCapacity → persiste NULL (hereda sucursal).
    - updateActivity con maxCapacity=null → limpia el cupo (columna a NULL).
    - updateActivity sin la key maxCapacity → no toca el cupo existente (patrón !== undefined).
    - Respuesta del cliente incluye maxCapacity (no strippeado).
  </behavior>
  <action>
    D-08. `activity-service.ts`: extender `createActivity` con un 3er parámetro `maxCapacity?: number | null` y sumarlo al `insert.values({ ... maxCapacity: maxCapacity ?? null })`. En `updateActivity`, agregar `if (data.maxCapacity !== undefined) updateData.maxCapacity = data.maxCapacity;` al bloque de update parcial (soporta `null` explícito para limpiar y volver a heredar).

    `schemas.ts`: agregar `maxCapacity: { type: ["integer", "null"], minimum: 1, maximum: 500 }` a las body properties de `createActivitySchema` y `updateActivitySchema` (validación D-08: entero positivo, techo razonable). Agregar `maxCapacity: { type: ["integer", "null"] }` a `activityRecordSchema` (si falta, fast-json-stringify lo strippea y el cliente lo recibe vacío).

    `types.ts`: sumar `maxCapacity: number | null` a `ActivityRecord`.

    `routes.ts`: en POST /activities sumar `maxCapacity?: number | null` al Body generic y pasar `request.body.maxCapacity` como 3er arg a `createActivity`; en PUT /activities sumar `maxCapacity?: number | null` al Body generic (el handler ya pasa `request.body` entero a `updateActivity`). NO cambiar los guards (Horarios categoría libre, 149 D-04).

  </action>
  <acceptance_criteria>
    - `ActivityRecord` (types.ts) tiene `maxCapacity: number | null`.
    - `activityRecordSchema`, `createActivitySchema`, `updateActivitySchema` declaran `maxCapacity`; los body schemas con `minimum: 1` y `maximum` finito.
    - `createActivity` acepta un tercer arg de cupo; `updateActivity` soporta `maxCapacity: null` explícito vía patrón `!== undefined`.
    - POST/PUT routes propagan maxCapacity al service; guards intactos.
    - `pnpm exec tsc --noEmit` pasa.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit && grep -c 'maxCapacity' src/modules/scheduling/schemas.ts | grep -qv '^0$'</automated>
  </verify>
  <done>El CRUD de actividades acepta, persiste, limpia y devuelve maxCapacity con validación server-side.</done>
</task>

<task type="auto">
  <name>Task 2: Suite de integración 155 (overlap re-scopeado, cupo efectivo, ABM cupo)</name>
  <files>el-templo-api/test/scheduling/155-horarios.test.ts</files>
  <read_first>
    - el-templo-api/test/scheduling/schedule-activity-crud.test.ts (analog estructural: app/auth/clean/timers/helpers)
    - el-templo-api/test/scheduling/scheduling.test.ts (seeding de bookings + lista_espera)
    - el-templo-api/test/helpers.ts (auth/request utilities)
  </read_first>
  <action>
    Crear `155-horarios.test.ts` copiando la estructura del analog (createTestApp + getAuthToken admin + cleanAllTestData en beforeEach + fake timers pinneados + branch seeded no-virtual + helpers createActivity/postSchedule). Casos (según CONTEXT §Established Patterns):

    Simultaneidad (D-01):
    - Solape de la MISMA actividad → 409 (adaptar el caso existente "rechaza overlap activo").
    - Solape de actividades DISTINTAS a la misma hora → 201 (caso nuevo, espejo con segunda actividad).
    - Back-to-back de la misma actividad (10-11 y 11-12) → 201 (semántica [start,end) intacta).
    - updateScheduleActivity que cambia la actividad de un slot para coincidir/solapar con otra clase de la actividad destino → 409 (gap cerrado en 155-01).

    Cupo efectivo (D-05/D-06/D-07):
    - Actividad con maxCapacity=1 en un slot: reservar 2 socios → el 2º cae a `lista_espera` (bloquea en el menor, aunque branch.maxCapacity sea mayor).
    - Actividad con maxCapacity NULL → hereda `branch.maxCapacity` (comportamiento previo).

    ABM (D-08):
    - POST /activities con maxCapacity lo persiste y lo devuelve.
    - PUT /activities/:id con maxCapacity=null limpia el cupo (la actividad vuelve a heredar la sucursal).

    Logging con `request.log`/`app.log` según el harness; sin `console`. NO correr el suite localmente (corre en CI); el gate local es tsc.

  </action>
  <acceptance_criteria>
    - Existe `test/scheduling/155-horarios.test.ts` con casos para: overlap misma-actividad (409), overlap distinta-actividad (201), back-to-back (201), gap de updateScheduleActivity (409), cupo actividad<sucursal (lista_espera en el menor), cupo NULL hereda, ABM persist + PUT null clears.
    - `cd el-templo-api && pnpm exec tsc --noEmit` compila el archivo de test.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit && test -f test/scheduling/155-horarios.test.ts && grep -Eqi 'lista_espera|maxCapacity' test/scheduling/155-horarios.test.ts</automated>
  </verify>
  <done>Suite de integración cubre simultaneidad, cupo efectivo y ABM de cupo; compila con tsc; corre en CI al pushear.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                | Description                                    |
| ----------------------- | ---------------------------------------------- |
| staff → API /activities | Body con name/description/isActive/maxCapacity |

## STRIDE Threat Register

| Threat ID | Category               | Component                        | Disposition | Mitigation Plan                                                                                                                       |
| --------- | ---------------------- | -------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| T-155-04  | Tampering              | POST/PUT /activities maxCapacity | mitigate    | JSON schema `{ type: ["integer","null"], minimum: 1, maximum: 500 }` rechaza cupo negativo/cero/absurdo server-side antes del service |
| T-155-05  | Information Disclosure | activityRecordSchema response    | accept      | maxCapacity es dato operativo no sensible; se declara en el response schema solo para que llegue al cliente                           |
| T-155-06  | Elevation of Privilege | guards de /activities            | accept      | Guard módulo-level existente (ALL_STAFF_ROLES) sin cambios — Horarios es categoría libre (149 D-04)                                   |
| T-155-SC  | Tampering              | npm installs                     | accept      | Esta fase NO instala/actualiza dependencias.                                                                                          |

</threat_model>

<verification>
- `cd el-templo-api && pnpm exec tsc --noEmit` verde (incluye el nuevo test).
- maxCapacity presente en service/schema/types/routes; guards intactos.
- Suite `155-horarios.test.ts` cubre los casos de CONTEXT; corre en CI.
</verification>

<success_criteria>

- El admin puede definir/limpiar el cupo de una actividad vía API con validación server-side.
- El comportamiento de simultaneidad y cupo efectivo de 155-01 queda garantizado por tests de integración.
  </success_criteria>

<output>
Create `.planning/phases/155-horarios/155-02-SUMMARY.md` when done
</output>
