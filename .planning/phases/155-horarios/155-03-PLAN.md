---
phase: 155-horarios
plan: 03
type: execute
wave: 3
depends_on: [155-02]
files_modified:
  - el-templo-admin/src/types/scheduling.ts
  - el-templo-admin/src/composables/useSchedulingApi.ts
  - el-templo-admin/src/components/scheduling/ActivitiesDialog.vue
  - el-templo-admin/src/components/scheduling/CreateSlotDialog.vue
autonomous: true
requirements: [HOR-02, HOR-03]
must_haves:
  truths:
    - "El ABM de actividades (ActivitiesDialog) tiene un campo Cupo con hint 'vacío = hereda el cupo de la sucursal' y persiste maxCapacity"
    - "CreateSlotDialog acepta un prop de prefill (branchId/día/hora) que precarga el form al abrir, dejando los campos editables"
    - "Desde CreateSlotDialog se puede crear una actividad nueva inline (nombre + descripción opcional + cupo) reusando createActivity, y el slot queda con esa actividad seleccionada"
    - "El campo Cupo valida entero positivo y admite vacío (null)"
  artifacts:
    - path: "el-templo-admin/src/types/scheduling.ts"
      provides: "ActivityRecord con maxCapacity"
      contains: "maxCapacity"
    - path: "el-templo-admin/src/composables/useSchedulingApi.ts"
      provides: "createActivity/updateActivity con maxCapacity"
      contains: "maxCapacity"
    - path: "el-templo-admin/src/components/scheduling/CreateSlotDialog.vue"
      provides: "Prop de prefill + creación de actividad inline + campo cupo"
    - path: "el-templo-admin/src/components/scheduling/ActivitiesDialog.vue"
      provides: "Campo Cupo en el form de actividad"
      contains: "maxCapacity"
  key_links:
    - from: "CreateSlotDialog actividad inline"
      to: "useSchedulingApi.createActivity"
      via: "opción 'crear nueva actividad' en el q-select"
      pattern: "createActivity"
    - from: "ActivitiesDialog form"
      to: "useSchedulingApi.updateActivity/createActivity"
      via: "campo maxCapacity"
      pattern: "maxCapacity"
---

<objective>
Dialogs del admin: cupo por actividad en el ABM (HOR-03 / D-08), prefill del dialog de crear horario y creación de actividad inline desde el slot (HOR-02 / D-03/D-04).

Purpose: Exponer en la UI del admin el cupo por actividad y generalizar el "test de profe" a "crear clase/actividad desde el slot": el dialog de crear horario gana un prop de prefill (consumido por la grilla en 155-04) y la capacidad de crear una actividad al vuelo. Reusa el endpoint `POST /activities` (ya soporta maxCapacity tras 155-02) — cero backend nuevo.

Output: `maxCapacity` en el composable/types del admin; campo Cupo en `ActivitiesDialog`; prop de prefill + actividad inline + campo cupo en `CreateSlotDialog`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-horarios/155-CONTEXT.md
@.planning/phases/155-horarios/155-PATTERNS.md
@.planning/phases/155-horarios/155-02-SUMMARY.md

<interfaces>
<!-- Estructuras reales verificadas del admin. -->

CreateSlotDialog.vue props actuales:
defineProps<{ show: boolean; branches: BranchOption[]; defaultBranchId: number | null }>()
form: SlotForm { branchId; dayOfWeek: DayOfWeek|null; startTime; endTime; activityId }
q-select "Actividad" con v-model form.activityId, :options="activeActivityOptions", emit-value map-options.
resetForm() se dispara en watch(() => props.show); hoy prefillea branchId: props.defaultBranchId.
onSubmit maneja error inline con extractError (mensaje en errorMessage, no toast).

ActivitiesDialog.vue form (activityForm { name; description }): q-input Nombre + q-input Descripcion + botón save/add.
startEditActivity carga name/description al form; onSaveActivity llama createActivity/updateActivity.
Patrón de creación existente: schedulingApi.createActivity({ name, description }) + $q.notify positive.

useSchedulingApi.ts createActivity(data: { name; description? }) → POST /admin/scheduling/activities.
updateActivity(data) análogo. Loading/error/extractError ya encapsulados.

types/scheduling.ts ActivityRecord: id/name/description/isActive/createdAt/updatedAt (espeja API).

Logger: createLogger('ComponentName'); nunca console. Gate admin: pnpm lint.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: maxCapacity en types + composable + campo Cupo en ActivitiesDialog</name>
  <files>el-templo-admin/src/types/scheduling.ts, el-templo-admin/src/composables/useSchedulingApi.ts, el-templo-admin/src/components/scheduling/ActivitiesDialog.vue</files>
  <read_first>
    - el-templo-admin/src/types/scheduling.ts (ActivityRecord ~15)
    - el-templo-admin/src/composables/useSchedulingApi.ts (createActivity ~26-41, updateActivity ~59-77)
    - el-templo-admin/src/components/scheduling/ActivitiesDialog.vue (form ~72-94, startEditActivity ~151, onSaveActivity ~161-189)
  </read_first>
  <action>
    D-08 (frontend). `types/scheduling.ts`: sumar `maxCapacity: number | null` a `ActivityRecord` (espeja la API tras 155-02). `useSchedulingApi.ts`: sumar `maxCapacity?: number | null` a los data types de `createActivity` y `updateActivity`.

    `ActivitiesDialog.vue`: agregar `maxCapacity` al `activityForm` (valor `number | null`, inicial null). Agregar un `q-input type="number"` "Cupo" con hint "vacío = hereda el cupo de la sucursal" (dense outlined, patrón de los inputs existentes). En `startEditActivity` cargar `maxCapacity` de la actividad al form. En `onSaveActivity` pasar `maxCapacity` (normalizando string vacío → null; entero positivo) a create/update. Validación: entero positivo o vacío. Logger `createLogger`, no console.

  </action>
  <acceptance_criteria>
    - `ActivityRecord` (admin) incluye `maxCapacity: number | null`.
    - `createActivity`/`updateActivity` del composable aceptan `maxCapacity?: number | null`.
    - `ActivitiesDialog` renderiza un input Cupo con el hint indicado, lo carga al editar y lo envía al guardar (vacío→null).
    - `cd el-templo-admin && pnpm lint` pasa.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-admin && pnpm lint && grep -q 'maxCapacity' src/components/scheduling/ActivitiesDialog.vue</automated>
  </verify>
  <done>El ABM de actividades del admin edita y persiste el cupo con hint de herencia.</done>
</task>

<task type="auto">
  <name>Task 2: CreateSlotDialog — prop de prefill + actividad inline + campo cupo</name>
  <files>el-templo-admin/src/components/scheduling/CreateSlotDialog.vue</files>
  <read_first>
    - el-templo-admin/src/components/scheduling/CreateSlotDialog.vue (props ~94-121, q-select actividad ~54-63, resetForm ~173-182, onSubmit ~208-215)
    - el-templo-admin/src/components/scheduling/ActivitiesDialog.vue (patrón createActivity inline ~171-176)
  </read_first>
  <action>
    D-03 (prefill): agregar un prop opcional `initial?: { branchId?: number | null; dayOfWeek?: DayOfWeek; startTime?: string; endTime?: string }`. En `resetForm` (que ya corre en `watch(() => props.show)`), cuando `props.initial` esté presente y `show` pase a true, precargar `form.branchId/dayOfWeek/startTime/endTime` desde `initial` (fallback al comportamiento actual: `branchId: props.defaultBranchId`). Los campos quedan EDITABLES (prefill, no lock).

    D-04 (actividad inline): en el q-select "Actividad" (~54-63) agregar una opción "＋ Crear nueva actividad" (o botón adyacente) que despliegue un mini-form inline: nombre (requerido), descripción (opcional) y cupo (D-08, `type="number"`, hint "vacío = hereda el cupo de la sucursal"). Al confirmar, llamar `schedulingApi.createActivity({ name, description, maxCapacity })` (patrón de ActivitiesDialog), setear `form.activityId` con el id devuelto, y refrescar `activities`. Manejo del error 409 (nombre duplicado) inline con `extractError` en `errorMessage` (no toast, para no perder el contexto del form), patrón de `onSubmit`. Logger `createLogger`, no console.

  </action>
  <acceptance_criteria>
    - `CreateSlotDialog` acepta un prop `initial` de prefill consumido en `resetForm` al abrir; sin `initial` conserva el comportamiento actual.
    - Los campos precargados permanecen editables.
    - Existe un affordance inline para crear actividad (nombre + descripción opcional + cupo) que llama `createActivity` y deja `form.activityId` seteado con el id nuevo.
    - Error 409 de nombre duplicado se muestra inline (no toast).
    - `cd el-templo-admin && pnpm lint` pasa.
  </acceptance_criteria>
  <verify>
    <automated>cd el-templo-admin && pnpm lint && grep -q 'initial' src/components/scheduling/CreateSlotDialog.vue && grep -q 'createActivity' src/components/scheduling/CreateSlotDialog.vue</automated>
  </verify>
  <done>El dialog de crear horario precarga desde un prefill y permite crear una actividad con cupo sin salir del flujo.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                      | Description                                  |
| --------------------------------------------- | -------------------------------------------- |
| admin (browser) → API /activities, /schedules | Inputs del form de cupo y de creación inline |

## STRIDE Threat Register

| Threat ID | Category               | Component           | Disposition | Mitigation Plan                                                                                                                                                                              |
| --------- | ---------------------- | ------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-155-07  | Tampering              | campo Cupo (client) | mitigate    | Validación client-side (entero positivo o vacío) como UX; la validación autoritativa vive en el JSON schema del API (155-02, minimum 1 / maximum 500) — el cliente no es la fuente de verdad |
| T-155-08  | Information Disclosure | error 409 inline    | accept      | El mensaje de nombre duplicado no expone datos sensibles; se muestra tal cual del API                                                                                                        |
| T-155-SC  | Tampering              | npm installs        | accept      | Esta fase NO instala/actualiza dependencias.                                                                                                                                                 |

</threat_model>

<verification>
- `cd el-templo-admin && pnpm lint` verde.
- ABM de actividades con campo Cupo (hint de herencia), edición carga el cupo.
- CreateSlotDialog acepta prefill (consumido por 155-04) y crea actividad inline con cupo reusando createActivity.
</verification>

<success_criteria>

- El admin define el cupo de una actividad desde la UI.
- El dialog de crear horario está listo para abrirse prefilleado desde la grilla y permite crear la actividad al vuelo.
  </success_criteria>

<output>
Create `.planning/phases/155-horarios/155-03-SUMMARY.md` when done
</output>
