---
phase: 161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement
plan: 03
type: execute
wave: 2
depends_on: ["161-01"]
files_modified:
  - el-templo-api/src/modules/attendance/service.ts
  - el-templo-api/src/jobs/mark-no-shows.ts
  - el-templo-api/test/attendance/especial-consumption.test.ts
autonomous: true
requirements: [GATE-02]
must_haves:
  truths:
    - "Un check-in (QR / self / coach) a una actividad especial decrementa classesRemaining de la sub especial, NO del presencial"
    - "Un check-in a una actividad regular decrementa la sub no-especial (comportamiento actual intacto)"
    - "El undo de check-in restaura la clase a la sub correcta según la actividad"
    - "El job de no-shows decrementa la sub correcta por actividad (especial vs regular)"
  artifacts:
    - path: "el-templo-api/test/attendance/especial-consumption.test.ts"
      provides: "cobertura GATE-02"
      min_lines: 60
  key_links:
    - from: "el-templo-api/src/modules/attendance/service.ts"
      to: "pickSubscriptionForActivity"
      via: "routing de decremento por actividad en los 4 puntos + undo"
      pattern: "pickSubscriptionForActivity"
    - from: "el-templo-api/src/jobs/mark-no-shows.ts"
      to: "pickSubscriptionForActivity"
      via: "decremento por (member, actividad especial)"
      pattern: "pickSubscriptionForActivity"
---

<objective>
Rutear el consumo de clases por actividad (GATE-02): en los 4 puntos de decremento de attendance (QR, self, coach, undo) y en el job de no-shows, elegir la suscripción correcta según si la actividad es especial, usando `pickSubscriptionForActivity` en lugar del `getMemberSubscription` singular.

Purpose: Que asistir a una especial descuente del pase y asistir a una regular descuente del presencial — sin cruces.
Output: attendance/service.ts + mark-no-shows.ts ruteados + tests de integración.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-CONTEXT.md
@.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-RESEARCH.md
@.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-PATTERNS.md
@.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-01-SUMMARY.md

<interfaces>
Del Plan 01: `pickSubscriptionForActivity(userId, isSpecialActivity)`. Callsites del singular a reemplazar (verificados 2026-07-14):
- `attendance/service.ts`: `getMemberSubscription(memberId)` en :89 (QR check-in), :412 (self check-in), :659 (coach), :772 (undo/restore). El decremento es `sql\`${classesRemaining} - 1\`` con guard `> 0` (:240-256, :415-426, :695-709); el undo suma +1 (:772-779).
- `jobs/mark-no-shows.ts`: selecciona sub en :86 y decrementa `- ${deduct}` en :105 (agrupado por member).
- El booking tiene `scheduleId`; `schedule → activityId → activities.is_special` resuelve si la actividad es especial. Podés resolver `isSpecial` con un join propio (schedules innerJoin activities) por scheduleId — NO dependés del getScheduleSlotRaw del Plan 05 (evita acoplar planes de la misma ola).
- Schema: `activities.isSpecial` (Plan 01), tabla `bookings` (memberId, scheduleId, status), `schedules` (activityId).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Routing de decremento en los 4 puntos de attendance</name>
  <read_first>
    - el-templo-api/src/modules/attendance/service.ts (los 4 puntos: :89/:412/:659/:772 y sus decrementos)
    - el-templo-api/src/db/schema/activities.ts (isSpecial), el-templo-api/src/db/schema/schedules.ts (activityId)
  </read_first>
  <behavior>
    - Check-in QR a especial: classesRemaining de la sub especial baja 1; la presencial no cambia.
    - Check-in a regular: la sub no-especial baja 1 (igual que hoy).
    - Undo de check-in a especial: restaura +1 a la sub especial.
  </behavior>
  <action>
    En cada uno de los 4 puntos (:89, :412, :659, :772), antes de decrementar, resolvé `isSpecialActivity` para el `scheduleId` del booking (join `schedules` innerJoin `activities` por scheduleId → `activities.is_special`; helper local privado si se repite). Reemplazá `getMemberSubscription(memberId)` por `pickSubscriptionForActivity(memberId, isSpecialActivity)`. Mantené el patrón de decremento existente (`sql` con guard `> 0`) y el undo (+1) apuntando a la sub elegida (`eq(subscriptions.id, subscription.id)`). Si `pickSubscriptionForActivity` devuelve null (staff sin sub, etc.), mantené el comportamiento defensivo actual (no decrementar). Sin `any`; logging con `request.log`/`app.log`, nunca console.
  </action>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pickSubscriptionForActivity" el-templo-api/src/modules/attendance/service.ts` ≥ 4.
    - `grep -c "getMemberSubscription(" el-templo-api/src/modules/attendance/service.ts` para consumo de clases = 0 en los 4 puntos de decremento (pueden quedar usos no relacionados a decremento — documentar).
    - `grep -E "is_special|isSpecial" el-templo-api/src/modules/attendance/service.ts` matchea la resolución de la actividad.
    - `npx tsc --noEmit` pasa.
  </acceptance_criteria>
  <done>Los 4 puntos de attendance rutean el decremento por actividad; tsc verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Routing de decremento en el job de no-shows</name>
  <read_first>
    - el-templo-api/src/jobs/mark-no-shows.ts (selección de sub :86 y decremento :105)
    - el-templo-api/src/modules/attendance/service.ts (patrón de resolución isSpecial de la Task 1)
    - el-templo-api/src/jobs/auto-resume-pauses.ts (:18 import + :40 `new SubscriptionService(...)` — analog de cómo un job instancia `SubscriptionService`; reusá este patrón para llamar `pickSubscriptionForActivity` desde el job en vez de duplicar la selección con una query cruda)
  </read_first>
  <behavior>
    - Un no-show sobre una reserva de actividad especial descuenta del pase; un no-show sobre una regular descuenta del presencial.
  </behavior>
  <action>
    En `mark-no-shows.ts`, agrupá los no-shows por (member, es-actividad-especial) resolviendo `is_special` por el `scheduleId` de cada booking (mismo join que Task 1). Instanciá `SubscriptionService` como lo hace `auto-resume-pauses.ts` (:18 import, :40 `new SubscriptionService(...)`) y usá `pickSubscriptionForActivity(memberId, isSpecialActivity)` en lugar de duplicar la selección con una query cruda; decrementá `- ${deduct}` sobre la sub elegida con el guard `> 0`. Mantené la idempotencia/estructura del batch existente. Sin `any`; logging por el logger del job, nunca console.
  </action>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "pickSubscriptionForActivity" el-templo-api/src/jobs/mark-no-shows.ts` matchea.
    - `grep -E "is_special|isSpecial" el-templo-api/src/jobs/mark-no-shows.ts` matchea la resolución de actividad.
    - `npx tsc --noEmit` pasa.
  </acceptance_criteria>
  <done>El job de no-shows decrementa la sub correcta por actividad; tsc verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests de integración de consumo (GATE-02)</name>
  <read_first>
    - el-templo-api/test/subscriptions/class-tracking.test.ts (patrón de asertar classesRemaining)
    - el-templo-api/test/helpers.ts (auth/request, check-in)
    - el-templo-api/test/subscriptions/_helpers.ts (createPlan/createMember/assignPlan)
  </read_first>
  <action>
    Creá `el-templo-api/test/attendance/especial-consumption.test.ts`. Setup: un socio con presencial (classesRemaining X) + pase especial (classesRemaining 2), una actividad especial con slot de sábado y una regular. Casos:
    (1) GATE-02: check-in a la especial → classesRemaining del pase = 1, presencial sin cambios.
    (2) check-in a la regular → presencial baja 1, pase sin cambios.
    (3) undo del check-in a la especial → pase vuelve a 2.
    (4) no-show sobre una reserva de especial (ejecutar el job / la ruta que dispara mark-no-shows) → pase baja, presencial intacto.
    Corré sólo este archivo.
  </action>
  <verify>
    <automated>cd el-templo-api && npx vitest run test/attendance/especial-consumption.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - El archivo existe con ≥ 4 casos (check-in especial, check-in regular, undo, no-show).
    - `npx vitest run test/attendance/especial-consumption.test.ts` pasa.
    - El caso (1) asierta explícitamente que classesRemaining del presencial NO cambió tras el check-in a especial.
  </acceptance_criteria>
  <done>especial-consumption.test.ts verde cubriendo GATE-02.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                                                                    |
| --------------------------- | ---------------------------------------------------------------------------------------------- |
| booking → decremento de sub | El scheduleId determina la sub a decrementar; elegir la equivocada rompe el gating y el budget |

## STRIDE Threat Register

| Threat ID | Category    | Component                | Disposition | Mitigation Plan                                                                                                                            |
| --------- | ----------- | ------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| T-161-06  | Tampering   | decremento por actividad | mitigate    | resolver `is_special` server-side por scheduleId→activity; nunca confiar en input del cliente; test que verifica que el presencial no baja |
| T-161-07  | Repudiation | undo / no-show           | mitigate    | restaurar/decrementar sobre la sub elegida por actividad (eq por subscription.id); guard `> 0`                                             |
| T-161-SC  | Tampering   | npm installs             | accept      | sin paquetes nuevos                                                                                                                        |

</threat_model>

<verification>
- `npx tsc --noEmit` verde.
- `npx vitest run test/attendance/especial-consumption.test.ts` verde.
- class-tracking.test.ts sigue verde en CI (no-regresión del consumo regular).
</verification>

<success_criteria>

- Consumo ruteado por actividad en check-in (QR/self/coach), undo y no-shows.
- Cero cruce entre pase y presencial en el decremento.
  </success_criteria>

<output>
Create `.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-03-SUMMARY.md` when done
</output>
