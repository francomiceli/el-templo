---
phase: 161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement
plan: 07
type: execute
wave: 3
depends_on: ["161-02", "161-05"]
files_modified:
  - el-templo-admin/src/components/scheduling/ActivitiesDialog.vue
  - el-templo-admin/src/types/scheduling.ts
  - el-templo-admin/src/components/AssignPlanDialog.vue
  - el-templo-admin/src/components/MemberSubscriptionTab.vue
  - el-templo-admin/src/types/subscription.ts
autonomous: false
requirements: [ACT-01, PASE-02, PASE-03]
must_haves:
  truths:
    - "El ABM de actividades tiene un toggle 'Actividad especial (requiere pase)' que persiste el flag"
    - "AssignPlanDialog puede filtrar y vender los planes especiales; el filtro 'online' ya no incluye especiales"
    - "MemberSubscriptionTab permite vender el pase (categoría especial) y renovarlo pasando subscriptionId"
    - "El booking manual admin de una especial sin pase muestra una advertencia confirmable (D-07)"
  artifacts:
    - path: "el-templo-admin/src/components/scheduling/ActivitiesDialog.vue"
      provides: "toggle isSpecial en el ABM"
      contains: "isSpecial"
  key_links:
    - from: "el-templo-admin/src/components/AssignPlanDialog.vue"
      to: "categoryFilter especial"
      via: "filteredPlans incluye 'especial' y excluye especial de 'online'"
      pattern: "especial"
---

<objective>
Cerrar la superficie admin del núcleo: toggle "especial" en el ABM de actividades (ACT-01 UI), venta del pase por categoría en AssignPlanDialog/MemberSubscriptionTab (PASE-02/03) con renovación por subscriptionId, y la advertencia confirmable del booking manual de una especial sin pase (D-07 UI). Cierra con un checkpoint de verificación humana (el admin no tiene e2e — VALIDATION.md).

Purpose: Que Nacho/gestión puedan marcar actividades especiales y vender/renovar el pase desde el admin.
Output: 4 componentes/tipos del admin + verificación manual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-CONTEXT.md
@.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-RESEARCH.md
@.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-PATTERNS.md
@.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-02-SUMMARY.md
@.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-05-SUMMARY.md

<interfaces>
Verificado 2026-07-14:
- `ActivitiesDialog.vue`: `q-input` de `maxCapacity` en :85-95 e interfaz `activityForm` en :146-150 — replicá ese patrón con un `q-toggle` "Actividad especial (requiere pase)" y `isSpecial: boolean` en el form.
- `src/types/scheduling.ts`: `ActivityRecord` — agregá `isSpecial: boolean`.
- `AssignPlanDialog.vue`: `filteredPlans` en :1250-1259; hoy `categoryFilter` es `'presencial' | 'online'` y el filtro `'online'` = `p.planCategory !== 'presencial'` (INCLUYE especiales). Agregá tercer valor `'especial'` (muestra sólo especiales) y refiná `'online'` para excluir especial.
- `src/types/subscription.ts`: union `PlanCategory` — agregá `especial`.
- `MemberSubscriptionTab.vue`: sección/CTA de venta por categoría; la renovación debe pasar `subscriptionId` (schema del Plan 02) para renovar el pase de forma inequívoca.
- Booking manual admin: donde el admin dispara `adminAddBooking`; mostrar `q-dialog`/confirm si la actividad es especial y el member no tiene pase.
- Reglas del repo: `createLogger()` nunca console; sin `any`; Pinia composition API; CI NO typechequea el admin → correr `npx vue-tsc --noEmit` local.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Toggle 'especial' en el ABM de actividades (ACT-01 UI)</name>
  <read_first>
    - el-templo-admin/src/components/scheduling/ActivitiesDialog.vue (q-input maxCapacity :85-95, form iface :146-150)
    - el-templo-admin/src/types/scheduling.ts (ActivityRecord)
  </read_first>
  <action>
    En `ActivitiesDialog.vue` agregá un `q-toggle` con label "Actividad especial (requiere pase)" ligado a `activityForm.isSpecial`, con el mismo layout que el campo de cupo; incluí `isSpecial: boolean` en el ref del form y en su interfaz, y enviá/recibí el flag en el submit y en la carga de edición (mapeo desde `ActivityRecord.isSpecial`). En `src/types/scheduling.ts` agregá `isSpecial: boolean` a `ActivityRecord`. Sin `any`; `createLogger()` para cualquier log.
  </action>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "isSpecial" el-templo-admin/src/components/scheduling/ActivitiesDialog.vue` matchea el toggle y el form.
    - `grep "isSpecial" el-templo-admin/src/types/scheduling.ts` matchea en ActivityRecord.
    - `grep -i "especial" el-templo-admin/src/components/scheduling/ActivitiesDialog.vue` matchea el label del toggle.
    - `npx vue-tsc --noEmit` no agrega errores nuevos atribuibles a este cambio.
  </acceptance_criteria>
  <done>Toggle de actividad especial en el ABM, persistente; vue-tsc sin regresión del cambio.</done>
</task>

<task type="auto">
  <name>Task 2: Venta/renovación del pase + advertencia D-07 (PASE-02/03)</name>
  <read_first>
    - el-templo-admin/src/components/AssignPlanDialog.vue (filteredPlans :1250-1259)
    - el-templo-admin/src/types/subscription.ts (PlanCategory)
    - el-templo-admin/src/components/MemberSubscriptionTab.vue (venta/renovación)
  </read_first>
  <action>
    En `src/types/subscription.ts` agregá `especial` a la union `PlanCategory`. En `AssignPlanDialog.vue` agregá el tercer valor `'especial'` a `categoryFilter` (muestra sólo planCategory==='especial') y refiná la rama `'online'` para excluir especiales (`p.planCategory !== 'presencial' && p.planCategory !== 'especial'`). En `MemberSubscriptionTab.vue` agregá la sección/CTA para vender el pase (abrir AssignPlanDialog con categoryFilter='especial') y en la renovación del pase pasá el `subscriptionId` de la sub especial en el body de renew (contrato del Plan 02). Agregá la advertencia confirmable (D-07): al reservar manualmente una actividad especial para un member sin pase, mostrá un `q-dialog`/confirm antes de disparar el booking admin. Sin `any`; `createLogger()`; Pinia composition API.
  </action>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "especial" el-templo-admin/src/types/subscription.ts` matchea la union.
    - `grep -c "especial" el-templo-admin/src/components/AssignPlanDialog.vue` ≥ 2 (nuevo valor + refinamiento del filtro 'online').
    - `grep -E "subscriptionId|especial" el-templo-admin/src/components/MemberSubscriptionTab.vue` matchea la venta y el paso de subscriptionId en renew.
    - `npx vue-tsc --noEmit` no agrega errores nuevos atribuibles a este cambio.
  </acceptance_criteria>
  <done>Venta/renovación del pase y advertencia D-07 en el admin; vue-tsc sin regresión del cambio.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificación humana del admin (ABM + venta del pase)</name>
  <action>PAUSA para verificación humana — no hay e2e del admin (VALIDATION.md manual-only). El ejecutor detiene y espera la señal del usuario tras completar los pasos de how-to-verify.</action>
  <read_first>
    - .planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-VALIDATION.md (Manual-Only Verifications)
  </read_first>
  <acceptance_criteria>
    - El usuario responde "approved" tras validar el toggle del ABM, la venta del pase Socio (con y sin presencial) y Externo, la renovación por subscriptionId, y la advertencia confirmable del booking manual.
  </acceptance_criteria>
  <what-built>
    Toggle "Actividad especial" en el ABM de actividades, venta del pase Socio/Externo en AssignPlanDialog/MemberSubscriptionTab, renovación por subscriptionId, y advertencia confirmable del booking manual de una especial sin pase.
  </what-built>
  <how-to-verify>
    1. En Horarios → Actividades, creá/editá una actividad y activá el toggle "Actividad especial (requiere pase)"; guardá y reabrí para confirmar que el flag persiste.
    2. En la ficha de un socio con presencial activo, vendé el pase Socio ($10.000) → debe aparecer como sub en paralelo. Intentá vender el pase Socio a un usuario sin presencial → debe rechazar con el mensaje de "presencial activo".
    3. En la ficha de un freemium/externo, vendé el pase Externo ($20.000) como única sub.
    4. Renová el pase desde la ficha → debe renovar el pase (no el presencial) y resetear a 2 clases.
    5. Reservá manualmente una actividad especial para un member sin pase → debe aparecer la advertencia confirmable antes de confirmar.
  </how-to-verify>
  <resume-signal>Escribí "approved" o describí los problemas encontrados</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary       | Description                                                                                                           |
| -------------- | --------------------------------------------------------------------------------------------------------------------- |
| admin UI → API | El admin sólo dispara acciones; toda la validación (presencial activo, gating, budget) es server-side en Planes 02/06 |

## STRIDE Threat Register

| Threat ID | Category               | Component              | Disposition | Mitigation Plan                                                                                                      |
| --------- | ---------------------- | ---------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------- |
| T-161-16  | Elevation of Privilege | venta/renovación admin | mitigate    | la validación de reglas vive server-side (Plan 02); el admin sólo pasa categoría/subscriptionId; no confiar en la UI |
| T-161-17  | Repudiation            | booking manual D-07    | accept      | el bypass de staff es intencional y auditado por rol; la advertencia confirmable reduce error humano                 |
| T-161-SC  | Tampering              | npm installs           | accept      | sin paquetes nuevos (sólo componentes/tipos existentes)                                                              |

</threat_model>

<verification>
- `npx vue-tsc --noEmit` sin errores nuevos (CI no typechequea el admin — correr local).
- Checkpoint humano aprobado (VALIDATION.md manual-only).
</verification>

<success_criteria>

- Toggle de actividad especial persistente en el ABM.
- Venta del pase Socio/Externo y renovación por subscriptionId desde el admin.
- Advertencia confirmable del booking manual sin pase.
  </success_criteria>

<output>
Create `.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-07-SUMMARY.md` when done
</output>
