---
phase: 161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/schema/subscription-plans.ts
  - el-templo-api/src/db/schema/activities.ts
  - el-templo-api/src/db/migrations/0179_especial_pass_core.sql
  - el-templo-api/src/modules/subscriptions/types.ts
  - el-templo-api/src/modules/subscriptions/schemas.ts
  - el-templo-api/src/modules/shared/errors.ts
  - el-templo-api/src/modules/subscriptions/service.ts
autonomous: true
requirements: [PASE-01]
must_haves:
  truths:
    - "La categoría de plan 'especial' existe en enum, tipos y whitelists JSON-schema (tsc + CI verdes)"
    - "Existen 2 planes AR: 'Actividades con Aura — Socio' (10000, requires_presencial=1) y '— Externo' (20000, requires_presencial=0), ambos con monthly_class_budget=2, duration_days=30, classes_per_week NULL"
    - "activities.is_special existe (default false); subscription_plans tiene monthly_class_budget y requires_presencial"
    - "PassRequiredError con code PASS_REQUIRED existe siguiendo el patrón CoverageExpiredError"
    - "pickSubscriptionForActivity(userId, isSpecialActivity) devuelve la sub de la categoría correcta (especial vs no-especial) o null"
  artifacts:
    - path: "el-templo-api/src/db/migrations/0179_especial_pass_core.sql"
      provides: "enum MODIFY + 3 columnas + seed 2 planes"
      contains: "monthly_class_budget"
    - path: "el-templo-api/src/modules/shared/errors.ts"
      provides: "PassRequiredError"
      contains: "PASS_REQUIRED"
    - path: "el-templo-api/src/modules/subscriptions/types.ts"
      provides: "categoryGroup + PlanCategory especial"
      contains: "categoryGroup"
  key_links:
    - from: "el-templo-api/src/modules/subscriptions/service.ts"
      to: "getMemberSubscriptions"
      via: "pickSubscriptionForActivity filtra por categoryGroup"
      pattern: "pickSubscriptionForActivity"
    - from: "el-templo-api/src/db/schema/subscription-plans.ts"
      to: "0179_especial_pass_core.sql"
      via: "enum planCategory byte-for-byte igual al MODIFY"
      pattern: "especial"
---

<objective>
Establecer los contratos compartidos de la fase 161: schema + migración 0179 (categoría `especial`, columna de budget explícito, flag `is_special` en actividades, discriminador `requires_presencial`, seed de los 2 planes AR), el error tipado `PassRequiredError`, el helper de grupo de categoría de 3 valores, y el helper de routing `pickSubscriptionForActivity` que los planes downstream (booking, attendance, no-shows) consumen.

Purpose: Todo lo demás depende de estos contratos. Se definen primero para que los ejecutores de las olas 2-3 los usen sin explorar.
Output: Schema Drizzle actualizado, migración 0179 aplicada en la DB local, tipos/whitelists/error/helpers exportados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-CONTEXT.md
@.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-RESEARCH.md
@.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-PATTERNS.md
@.claude/skills/el-templo-db-migrations/SKILL.md

<interfaces>
Estado actual verificado (2026-07-14):
- `planCategoryEnum` (subscription-plans.ts:22-27): `["presencial","online_regular","online_goal","online_coach"]`.
- `bookingModeEnum`: `["fixed","flexible"]`. `planTierEnum`: `["flex","foundation","performance","other"]`.
- `activities.ts`: col precedente `maxCapacity: int("max_capacity")` (nullable, sin default) en :20; ya importa `boolean` en :7.
- `subscription-plans.ts` columnas NOT NULL relevantes para el seed: name, planTier, bookingMode, planCategory, priceRegular, priceZero, durationDays, multiBranch, isTrial, isGroup, isActive, isArchived, country, currency, grantsAllPrograms. Nullable: description, priceCreditCard, classesPerWeek, groupMaxMembers, linkedProgramId.
- `types.ts`: `PlanCategory` union en :25-29; `isOnlinePlan(category)` en :31-33 = `category !== "presencial"`.
- `schemas.ts`: 3 whitelists `enum: ["presencial","online_regular","online_goal","online_coach"]` en :35, :181, :223.
- `errors.ts`: `CoverageExpiredError extends BadRequestError` con `readonly code = "COVERAGE_EXPIRED"` en :49-57; `AppError.code` opcional en :16.
- `getMemberSubscriptions(userId)` (service.ts:963-991) devuelve `SubscriptionDetail[]` con `planCategory` y `status`; `getMemberSubscription` singular en :885 (`.limit(1)`, NO usar para routing).
- Máximo de migración aplicado = `0178_referrals_ab_copy_test.sql`. Siguiente libre = **0179** (re-verificar con `ls el-templo-api/src/db/migrations/*.sql | sort | tail -3` antes de generar).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema Drizzle + migración 0179 (enum, columnas, seed de 2 planes)</name>
  <read_first>
    - el-templo-api/src/db/schema/subscription-plans.ts (estado del enum y columnas)
    - el-templo-api/src/db/schema/activities.ts (patrón maxCapacity nullable)
    - el-templo-api/src/db/migrations/0091_multi_currency_and_country_scope.sql (patrón seed INSERT IGNORE + índice único name,country + docstring)
    - .claude/skills/el-templo-db-migrations/SKILL.md (numeración, semicolon trap, enum append-last byte-for-byte)
  </read_first>
  <action>
    Re-verificá el número libre con `ls el-templo-api/src/db/migrations/*.sql | sort | tail -3`; usá el máximo+1 (esperado 0179, tomá el siguiente libre si otra rama lo tomó).

    En `subscription-plans.ts`: agregá `"especial"` AL FINAL del array de `planCategoryEnum` (append-last, orden byte-for-byte con el SQL). Agregá dos columnas nullable dentro de `mysqlTable`: `monthlyClassBudget: int("monthly_class_budget")` (NULL para planes no-especiales) y `requiresPresencial: boolean("requires_presencial").default(false).notNull()`.

    En `activities.ts`: agregá `isSpecial: boolean("is_special").default(false).notNull()` (patrón de la col `maxCapacity`, con comentario que documente cero cambio de comportamiento por default).

    Creá `el-templo-api/src/db/migrations/0179_especial_pass_core.sql` con, en este orden, statements separados por `;`:
    (a) `ALTER TABLE subscription_plans MODIFY plan_category enum('presencial','online_regular','online_goal','online_coach','especial') NOT NULL;` — la lista debe ser byte-for-byte igual al enum del schema.
    (b) `ALTER TABLE subscription_plans ADD COLUMN monthly_class_budget INT NULL;`
    (c) `ALTER TABLE subscription_plans ADD COLUMN requires_presencial BOOLEAN NOT NULL DEFAULT 0;`
    (d) `ALTER TABLE activities ADD COLUMN is_special BOOLEAN NOT NULL DEFAULT 0;`
    (e) Un `INSERT IGNORE INTO subscription_plans (...) VALUES (...)` por los 2 planes AR, copiando la forma exacta de la 0091 (índice único ux_subscription_plans_name_country ya existe en prod → dedupe por name,country). Valores: name='Actividades con Aura — Socio'/'— Externo', plan_tier='other', booking_mode='flexible', plan_category='especial', linked_program_id=NULL, price_regular=10000/20000, price_zero=10000/20000, price_credit_card=NULL, duration_days=30, classes_per_week=NULL, monthly_class_budget=2, requires_presencial=1(Socio)/0(Externo), multi_branch=1, is_trial=0, is_group=0, group_max_members=NULL, is_active=1, is_archived=0, country='AR', currency='ARS', grants_all_programs=0, created_at=NOW(), updated_at=NOW().

    Docstring de cabecera enumerando secciones (D-12, D-03, D-14). CRÍTICO: NUNCA un `;` dentro de un comentario `--` (el runner splitea por `;` antes de stripear comentarios — incidente 0119). Usá `--> statement-breakpoint` si necesitás prosa entre statements. Committeá el SQL en el MISMO commit que el schema.

  </action>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "especial" el-templo-api/src/db/schema/subscription-plans.ts` ≥ 1 y `especial` es el último valor del array del enum.
    - `grep -E "monthly_class_budget|requires_presencial" el-templo-api/src/db/schema/subscription-plans.ts` matchea ambas columnas.
    - `grep "is_special" el-templo-api/src/db/schema/activities.ts` matchea.
    - El archivo `0179_especial_pass_core.sql` existe y su MODIFY del enum coincide byte-for-byte con el array del schema.
    - `grep -n ";" el-templo-api/src/db/migrations/0179_especial_pass_core.sql | grep "\-\-"` no devuelve ninguna línea (sin `;` en comentarios).
    - `grep -c "INSERT IGNORE" el-templo-api/src/db/migrations/0179_especial_pass_core.sql` ≥ 1 con los 2 planes (Socio 10000, Externo 20000).
    - `npx tsc --noEmit` pasa.
  </acceptance_criteria>
  <done>Schema y migración 0179 escritos y committeados juntos; tsc verde.</done>
</task>

<task type="auto">
  <name>Task 2 [BLOCKING]: Aplicar migración 0179 en la DB local</name>
  <read_first>
    - el-templo-api/src/db/migrations/0179_especial_pass_core.sql (la migración recién creada)
    - .claude/skills/el-templo-db-migrations/SKILL.md (runner custom, PROHIBIDO drizzle-kit)
  </read_first>
  <action>
    Aplicá la migración con el runner custom del repo: `cd el-templo-api && pnpm db:migrate` (ejecuta `src/db/run-migrations.ts`, trackea en la tabla `_migrations`). PROHIBIDO `drizzle-kit migrate` y `drizzle-kit push`. Esta tarea es BLOCKING y va DESPUÉS de escribir schema+SQL y ANTES de los tests: sin la migración aplicada, tsc pasa igual (los tipos vienen del schema TS, no de la DB) y la verificación daría falso positivo.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm db:migrate 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - El runner reporta 0179 como aplicada (aparece en la tabla `_migrations`).
    - Consulta de verificación: `SHOW COLUMNS FROM subscription_plans LIKE 'monthly_class_budget'` y `LIKE 'requires_presencial'` devuelven fila; `SHOW COLUMNS FROM activities LIKE 'is_special'` devuelve fila; `SELECT name, price_regular, monthly_class_budget, requires_presencial FROM subscription_plans WHERE plan_category='especial'` devuelve las 2 filas con budget=2.
  </acceptance_criteria>
  <done>Migración 0179 aplicada; columnas y 2 planes especiales presentes en la DB local.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Contratos compartidos — categoryGroup, whitelists, PassRequiredError, pickSubscriptionForActivity</name>
  <read_first>
    - el-templo-api/src/modules/subscriptions/types.ts (isOnlinePlan, PlanCategory union)
    - el-templo-api/src/modules/subscriptions/schemas.ts (3 whitelists en :35,:181,:223)
    - el-templo-api/src/modules/shared/errors.ts (CoverageExpiredError patrón)
    - el-templo-api/src/modules/subscriptions/service.ts (getMemberSubscriptions :963-991, getMemberSubscription :885)
  </read_first>
  <behavior>
    - categoryGroup('especial') === 'especial'; categoryGroup('presencial') === 'presencial'; categoryGroup('online_regular') === 'online'; categoryGroup('online_coach') === 'online'.
    - pickSubscriptionForActivity(userId, true) devuelve la sub de planCategory 'especial' (status active/paused/scheduled) priorizando la que cubre hoy, o null si no hay.
    - pickSubscriptionForActivity(userId, false) devuelve la sub de categoría no-especial (presencial u online, status active/paused/scheduled) con el mismo criterio cubre-hoy>active>paused>scheduled, o null.
    - Una sub 'scheduled' que arranca mañana ES elegible (mismo comportamiento que getMemberSubscription singular; sostiene el bloque coverage-from de reserve() en booking-service.ts:104-116).
    - PassRequiredError.code === 'PASS_REQUIRED' e instanceof BadRequestError.
  </behavior>
  <action>
    En `types.ts`: agregá `"especial"` a la union `PlanCategory`. Agregá `export type CategoryGroup = "presencial" | "online" | "especial"` y `export function categoryGroup(c: PlanCategory): CategoryGroup` que devuelve 'presencial' si c==='presencial', 'especial' si c==='especial', 'online' en otro caso. NO cambiés `isOnlinePlan` (se refina en el Plan 02 al reescribir el conflicto de assign) — pero corré `grep -rn "isOnlinePlan" el-templo-api/src/` y dejá un comentario/nota de los callsites (member-routes.ts:194-195, service.ts:251,1249) para el Plan 02.

    En `schemas.ts`: agregá `"especial"` a las 3 whitelists `enum` (:35, :181, :223 — re-verificá con grep, las líneas pueden haber corrido). Sin esto tsc pasa pero CI rompe contra MySQL real.

    En `errors.ts`: agregá `PassRequiredError extends BadRequestError` con `readonly code = "PASS_REQUIRED"` y mensaje default "Necesitás el pase de actividades para reservar esta clase" (espejo exacto de CoverageExpiredError:49-57).

    En `subscriptions/service.ts`: agregá `async pickSubscriptionForActivity(userId: number, isSpecialActivity: boolean): Promise<SubscriptionDetail | null>` que llama `getMemberSubscriptions(userId)` (el plural YA incluye status active/paused/**scheduled** — service.ts:1012-1014), filtra por `categoryGroup(s.planCategory) === 'especial'` cuando `isSpecialActivity`, o `!== 'especial'` cuando no. Ordená y elegí con el MISMO criterio que `getMemberSubscription` singular (:943-952): primero las subs que cubren hoy (`startDate <= today`), dentro de esas active>paused>scheduled, luego por `startDate`; devolvé la primera o null. CRÍTICO: NO excluyas `scheduled` — el bloque coverage-from de `reserve()` (booking-service.ts:104-116) permite reservar con una sub `scheduled` que arranca mañana, y el singular la incluye; excluirla acá sería una regresión de reserva dentro de ventana. NUNCA uses `getMemberSubscription` singular para esto (Pitfall 1). Sin `any`; catch con `err: unknown`.

  </action>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "especial" el-templo-api/src/modules/subscriptions/schemas.ts` ≥ 3.
    - `grep "categoryGroup" el-templo-api/src/modules/subscriptions/types.ts` matchea la función y el type CategoryGroup.
    - `grep "PASS_REQUIRED" el-templo-api/src/modules/shared/errors.ts` matchea.
    - `grep "pickSubscriptionForActivity" el-templo-api/src/modules/subscriptions/service.ts` matchea la definición.
    - `npx tsc --noEmit` pasa sin errores.
  </acceptance_criteria>
  <done>Tipos, whitelists, error y helper de routing exportados; tsc verde.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary            | Description                                                                                          |
| ------------------- | ---------------------------------------------------------------------------------------------------- |
| migración → DB prod | El SQL corre en prod por el pipeline; un `;` en comentario o enum desalineado rompe el deploy entero |
| schema TS ↔ DB real | tsc verde no garantiza DB correcta; whitelists/enum desincronizados pasan tsc y rompen CI            |

## STRIDE Threat Register

| Threat ID | Category          | Component              | Disposition | Mitigation Plan                                                                                                      |
| --------- | ----------------- | ---------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------- |
| T-161-01  | Tampering         | migración 0179         | mitigate    | enum MODIFY byte-for-byte con el schema; sin `;` en comentarios; INSERT IGNORE idempotente (dedupe por name,country) |
| T-161-02  | Denial of Service | deploy pipeline        | mitigate    | aplicar y verificar la migración local (Task 2 BLOCKING) antes de mergear; tsc + CI verdes                           |
| T-161-SC  | Tampering         | npm/pip/cargo installs | accept      | esta fase NO instala paquetes (RESEARCH: cero deps nuevas); sin checkpoint de legitimidad requerido                  |

</threat_model>

<verification>
- `npx tsc --noEmit` verde.
- Migración 0179 aplicada y verificada en DB local (2 planes especial, 3 columnas nuevas).
- `grep -rn "isOnlinePlan" src/` documentado para el Plan 02.
</verification>

<success_criteria>

- Enum `especial`, columnas `monthly_class_budget`/`requires_presencial`/`is_special` en schema + DB.
- 2 planes especiales AR seedeados con precios y discriminador correctos.
- `PassRequiredError`, `categoryGroup`, `pickSubscriptionForActivity` exportados y tipados sin `any`.
  </success_criteria>

<output>
Create `.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-01-SUMMARY.md` when done
</output>
