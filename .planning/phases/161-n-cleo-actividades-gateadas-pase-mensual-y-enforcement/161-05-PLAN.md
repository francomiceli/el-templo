---
phase: 161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement
plan: 05
type: execute
wave: 2
depends_on: ["161-01"]
files_modified:
  - el-templo-api/src/modules/scheduling/activity-service.ts
  - el-templo-api/src/modules/scheduling/routes.ts
  - el-templo-api/src/modules/scheduling/schemas.ts
  - el-templo-api/src/modules/scheduling/booking-service.ts
  - el-templo-api/test/scheduling/schedule-activity-crud.test.ts
autonomous: true
requirements: [ACT-01, ACT-02]
must_haves:
  truths:
    - "El CRUD de actividades acepta y persiste el flag is_special al crear y al editar"
    - "GET /activities devuelve isSpecial para que el admin lo edite y la fase 162 pinte la grilla"
    - "getScheduleSlotRaw(scheduleId) devuelve isSpecial (JOIN activities) — contrato que consume el gating del Plan 06"
  artifacts:
    - path: "el-templo-api/test/scheduling/schedule-activity-crud.test.ts"
      provides: "cobertura ACT-01 (persistencia del flag)"
      min_lines: 40
  key_links:
    - from: "el-templo-api/src/modules/scheduling/booking-service.ts"
      to: "activities.is_special"
      via: "getScheduleSlotRaw JOIN activities → isSpecial"
      pattern: "isSpecial"
    - from: "el-templo-api/src/modules/scheduling/routes.ts"
      to: "activity-service"
      via: "POST/PUT /activities persisten is_special"
      pattern: "isSpecial"
---

<objective>
Exponer el flag `is_special` de actividades end-to-end del lado API: el CRUD de actividades (POST/PUT /activities + schemas) lo acepta y persiste, GET lo devuelve, y `getScheduleSlotRaw` lo trae vía JOIN para que el gating de reserva (Plan 06) lo consuma. Las 3 actividades y sus slots de sábado los carga Nacho por el ABM existente (D-12) — acá solo habilitamos el flag.

Purpose: ACT-01 (marcar actividad especial) y ACT-02 (las actividades existen con el flag) del lado backend.
Output: activity-service + rutas + schemas + getScheduleSlotRaw + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-CONTEXT.md
@.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-RESEARCH.md
@.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-PATTERNS.md
@.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-01-SUMMARY.md

<interfaces>
Verificado 2026-07-14:
- `scheduling/routes.ts`: `POST /activities` (:125-143) con Body `{ name, description?, maxCapacity? }`; `GET /activities` (:145); `PUT /activities/:activityId` (:151-) con Body `{ ..., maxCapacity? }`.
- `scheduling/activity-service.ts`: lógica de create/update/list de actividades.
- `scheduling/schemas.ts`: JSON-schemas de create/update/list de actividades (buscar los que validan `maxCapacity`).
- `scheduling/booking-service.ts`: `getScheduleSlotRaw(scheduleId)` (:1895) ya trae `activityId`; extenderlo con JOIN a `activities` para traer `isSpecial`.
- Schema `activities.isSpecial` (Plan 01, default false notNull).
- Patrón de la col precedente: `maxCapacity` recorre exactamente este mismo camino (schema → activity-service → routes body → schemas → GET list). Replicá ese recorrido para `isSpecial`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CRUD de actividades acepta y persiste is_special</name>
  <read_first>
    - el-templo-api/src/modules/scheduling/activity-service.ts (create/update/list)
    - el-templo-api/src/modules/scheduling/routes.ts (POST/PUT/GET /activities :125-165)
    - el-templo-api/src/modules/scheduling/schemas.ts (schemas de actividad con maxCapacity)
  </read_first>
  <behavior>
    - POST /activities con isSpecial=true persiste is_special=1.
    - PUT /activities/:id con isSpecial=false actualiza el flag.
    - GET /activities devuelve isSpecial por actividad.
  </behavior>
  <action>
    Siguiendo exactamente el recorrido de `maxCapacity`: en `activity-service.ts` extendé create/update para aceptar y persistir `isSpecial` (default false si no viene). En `routes.ts` agregá `isSpecial?: boolean` a los Body de POST y PUT /activities y pasalo al service; asegurá que GET /activities lo devuelva. En `schemas.ts` agregá `isSpecial: { type: "boolean" }` (opcional) a los schemas de create/update y al item del response de list. Sin `any`; logging con `request.log`.
  </action>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isSpecial" el-templo-api/src/modules/scheduling/activity-service.ts` ≥ 1.
    - `grep "isSpecial" el-templo-api/src/modules/scheduling/routes.ts` matchea en POST y PUT.
    - `grep "isSpecial" el-templo-api/src/modules/scheduling/schemas.ts` matchea en create/update y list response.
    - `npx tsc --noEmit` pasa.
  </acceptance_criteria>
  <done>El CRUD de actividades persiste y devuelve is_special; tsc verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: getScheduleSlotRaw trae isSpecial + tests del CRUD del flag</name>
  <read_first>
    - el-templo-api/src/modules/scheduling/booking-service.ts (getScheduleSlotRaw :1895)
    - el-templo-api/test/scheduling/schedule-activity-crud.test.ts si existe, o un test de scheduling análogo
    - el-templo-api/test/helpers.ts (auth/request)
  </read_first>
  <behavior>
    - getScheduleSlotRaw(scheduleId) de un slot cuya actividad es especial devuelve isSpecial=true; de una regular, false.
    - El flag ida-y-vuelta por el CRUD conserva su valor.
  </behavior>
  <action>
    Extendé `getScheduleSlotRaw` (:1895) para hacer JOIN a `activities` por `activityId` y agregar `isSpecial: activities.isSpecial` al objeto devuelto (y a su tipo de retorno). Este es el contrato que consume el gating del Plan 06.

    Creá/extendé `el-templo-api/test/scheduling/schedule-activity-crud.test.ts`: crear una actividad con isSpecial=true (POST), verificar GET devuelve isSpecial=true, editar a false (PUT) y verificar. Si es viable en el test, verificá también que un slot sobre una actividad especial reporta isSpecial en el flujo de reserva/consulta. Corré sólo este archivo.

  </action>
  <verify>
    <automated>cd el-templo-api && npx vitest run test/scheduling/schedule-activity-crud.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep "isSpecial" el-templo-api/src/modules/scheduling/booking-service.ts` matchea dentro de getScheduleSlotRaw (JOIN activities).
    - El test existe y cubre POST(true)→GET→PUT(false).
    - `npx vitest run test/scheduling/schedule-activity-crud.test.ts` pasa.
  </acceptance_criteria>
  <done>getScheduleSlotRaw expone isSpecial; test del CRUD del flag verde.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                                                               |
| ---------------------- | --------------------------------------------------------------------------------------------------------- |
| admin → CRUD actividad | El flag is_special define el gating; sólo staff autenticado debe poder editarlo (auth de rutas existente) |
| schedule → activity    | El gating downstream depende de que getScheduleSlotRaw resuelva isSpecial correctamente por JOIN          |

## STRIDE Threat Register

| Threat ID | Category               | Component            | Disposition | Mitigation Plan                                                                                                |
| --------- | ---------------------- | -------------------- | ----------- | -------------------------------------------------------------------------------------------------------------- |
| T-161-10  | Elevation of Privilege | POST/PUT /activities | mitigate    | reusar la auth/rol de las rutas de actividad existentes; is_special es dato de dominio editable sólo por staff |
| T-161-11  | Tampering              | getScheduleSlotRaw   | mitigate    | resolver isSpecial server-side por JOIN activities (no del request); contrato tipado para el Plan 06           |
| T-161-SC  | Tampering              | npm installs         | accept      | sin paquetes nuevos                                                                                            |

</threat_model>

<verification>
- `npx tsc --noEmit` verde.
- `npx vitest run test/scheduling/schedule-activity-crud.test.ts` verde.
</verification>

<success_criteria>

- Flag is_special editable/persistente por el CRUD y visible en GET.
- getScheduleSlotRaw expone isSpecial (contrato del gating).
  </success_criteria>

<output>
Create `.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-05-SUMMARY.md` when done
</output>
