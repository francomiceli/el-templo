---
phase: 161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement
plan: 02
type: execute
wave: 2
depends_on: ["161-01"]
files_modified:
  - el-templo-api/src/modules/subscriptions/service.ts
  - el-templo-api/src/modules/subscriptions/routes.ts
  - el-templo-api/src/modules/subscriptions/schemas.ts
  - el-templo-api/test/subscriptions/especial-pass.test.ts
autonomous: true
requirements: [PASE-01, PASE-02, PASE-03, PASE-04]
must_haves:
  truths:
    - "assignPlan permite un pase especial en paralelo a un plan presencial (no choca con presencial ni online)"
    - "assignPlan/renew del pase Socio (requires_presencial=1) rechaza si el usuario no tiene presencial active/paused; el pase Externo no exige presencial"
    - "El budget de la sub especial se setea = plan.monthlyClassBudget (2), no derivado de classesPerWeek (NULL)"
    - "Pagar un pase especial NO cualifica vínculos de referido ni aplica descuento de referido (D-09)"
    - "renewSubscription puede renovar la sub especial de forma inequívoca vía subscriptionId explícito"
  artifacts:
    - path: "el-templo-api/test/subscriptions/especial-pass.test.ts"
      provides: "cobertura PASE-02/03/04, D-01, D-09"
      min_lines: 80
  key_links:
    - from: "el-templo-api/src/modules/subscriptions/service.ts"
      to: "requiresPresencial"
      via: "validación de presencial activo en assign + renew del pase Socio"
      pattern: "requiresPresencial"
    - from: "el-templo-api/src/modules/subscriptions/service.ts"
      to: "categoryGroup"
      via: "conflicto de assign por grupo de 3 valores"
      pattern: "categoryGroup"
---

<objective>
Habilitar la venta y renovación del pase especial en `assignPlan`/`renewSubscription`/`changePlan` sin regresiones: conflicto de categoría por grupo de 3 valores, budget explícito, validación "presencial activo" del pase Socio (D-01), exclusión de referidos (D-09), y discriminador de qué sub renovar.

Purpose: Que gestión pueda vender el pase Socio (a socios activos) y el Externo (única sub) reusando el flujo de cobro/deuda existente.
Output: Servicio de suscripciones extendido a 3 categorías + tests de integración.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-CONTEXT.md
@.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-RESEARCH.md
@.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-PATTERNS.md
@.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-01-SUMMARY.md

<interfaces>
Del Plan 01 (foundation) ya existen: `categoryGroup(c)`, `PlanCategory` con `especial`, columnas `monthlyClassBudget`/`requiresPresencial` en el plan, `pickSubscriptionForActivity`. Callsites a auditar (verificados 2026-07-14):
- Conflicto de categoría en `assignPlan`: `service.ts:1249` `const planIsOnline = isOnlinePlan(plan.planCategory)` + el `.where` que compara `ne/eq(..., "presencial")` (:1249-1277).
- Budget derivado: `service.ts:1398` (assign) y `:3910` (renew): `plan.classesPerWeek !== null ? Math.ceil(plan.durationDays/7)*plan.classesPerWeek : null`.
- Referral callsites (3): `qualifyReferralOnCharge`+`computePriceWithReferralDiscount` en assign `:1377-1378`, changePlan `:3128-3129`, renew `:3626-3627`.
- `renewSubscription` en `:3791`; elige currentSub con `.limit(1)` active-first SIN filtro de categoría.
- `renewSubscriptionSchema` (schemas.ts:391): body con paymentMethod/amountReceived/etc.; params solo userId.
- `getMemberSubscriptions(userId)` :963 trae planCategory+status. NO tocar `recomputeUserStatus` (:~5455, D-08).
- Test helpers: `test/subscriptions/_helpers.ts` (`createPlan`/`createMember`/`assignPlan` con overrides `planCategory`,`monthlyClassBudget`) y `test/subscriptions/dual-subscription.test.ts` (estructura beforeAll/afterAll/beforeEach + cleanAllTestData).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: assignPlan — conflicto por grupo, budget explícito, validación presencial (D-01), guard de referidos (D-09)</name>
  <read_first>
    - el-templo-api/src/modules/subscriptions/service.ts (assignPlan :1150-1400, incl. conflicto :1249-1277, budget :1398, referral :1377)
    - el-templo-api/src/modules/subscriptions/types.ts (categoryGroup, isOnlinePlan)
    - el-templo-api/src/modules/subscriptions/_helpers.ts si existe, o el-templo-api/test/subscriptions/_helpers.ts
  </read_first>
  <behavior>
    - Asignar un pase especial a un socio con presencial active NO lanza conflicto y crea la sub con classesRemaining=2, classesBudget=2.
    - Asignar el pase Socio (requires_presencial=1) a un usuario SIN presencial active/paused lanza BadRequestError con mensaje que menciona "presencial activo".
    - Asignar el pase Externo (requires_presencial=0) a un usuario sin presencial funciona.
    - Un usuario no puede tener 2 subs del mismo grupo (2 especiales) activas a la vez.
    - Pagar el pase NO llama qualifyReferralOnCharge ni aplica descuento de referido.
  </behavior>
  <action>
    Reescribí el conflicto de `assignPlan` (:1249-1277) para comparar por `categoryGroup(plan.planCategory)` en lugar del binario `isOnlinePlan`: una sub active/paused/scheduled permitida por grupo (presencial / online / especial), de modo que especial NO choque con presencial ni con online. Actualizá los callsites de `isOnlinePlan` detectados en el Plan 01 (service.ts:251, member-routes.ts:194-195) sólo si su semántica se rompe con `especial` — documentá la decisión por callsite en el summary.

    Budget explícito (:1398): cuando `plan.classesPerWeek === null` usá `plan.monthlyClassBudget ?? null` en lugar de `null`; `classesBudget` = ese mismo valor (patrón :1475). Los planes online siguen NULL.

    Validación D-01: tras validar el plan y ANTES de insertar la sub, si `plan.requiresPresencial === true`, llamá `getMemberSubscriptions(userId)` y verificá que exista una sub con `planCategory === 'presencial'` y status 'active' o 'paused'; si no, lanzá `BadRequestError("El pase Socio requiere un plan presencial activo. Ofrecé el pase Externo.")`.

    Guard D-09: envolvé el par `qualifyReferralOnCharge` + `computePriceWithReferralDiscount` de assign (:1377-1378) con `if (plan.planCategory !== 'especial')`. Cuando es especial, `pricePaid` queda sin descuento de referido.

    Sin `any`; `catch (err: unknown)` con `instanceof Error`. NO tocar `recomputeUserStatus`.

  </action>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "categoryGroup" el-templo-api/src/modules/subscriptions/service.ts` matchea el nuevo conflicto de assign.
    - `grep "requiresPresencial" el-templo-api/src/modules/subscriptions/service.ts` matchea la validación D-01.
    - `grep -n "planCategory !== 'especial'\|planCategory !== \"especial\"" el-templo-api/src/modules/subscriptions/service.ts` matchea al menos el guard de assign.
    - `npx tsc --noEmit` pasa.
  </acceptance_criteria>
  <done>assignPlan soporta el pase especial con las 3 reglas (conflicto por grupo, budget explícito, D-01, D-09); tsc verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: renewSubscription + changePlan — discriminador de sub, budget, D-01 en renew, guard D-09</name>
  <read_first>
    - el-templo-api/src/modules/subscriptions/service.ts (renewSubscription :3791-3912, changePlan :2977/:3128/:3626)
    - el-templo-api/src/modules/subscriptions/schemas.ts (renewSubscriptionSchema :391)
    - el-templo-api/src/modules/subscriptions/routes.ts (POST /members/:userId/subscription/renew :558-569)
  </read_first>
  <behavior>
    - renewSubscription con subscriptionId explícito renueva ESA sub (validando que pertenece al userId).
    - Renovar el pase Socio (requires_presencial=1) sin presencial active/paused lanza BadRequestError; el Externo renueva sin exigir presencial.
    - La renovación de una sub especial setea classesRemaining=2 (monthlyClassBudget), no derivado.
    - Renovar/cambiar un pase especial NO cualifica ni descuenta referido.
    - Renovar sin subscriptionId mantiene el comportamiento actual (backward compat, elige currentSub .limit(1)).
  </behavior>
  <action>
    En `renewSubscriptionSchema` (schemas.ts) agregá al body la propiedad opcional `subscriptionId: { type: "integer" }` (no required, backward compat). En `routes.ts` pasá `request.body.subscriptionId` a `renewSubscription`.

    En `renewSubscription` (:3791) agregá un parámetro opcional `subscriptionId?: number`: si viene, seleccioná esa sub tras validar `subscription.userId === userId` (lanzá NotFoundError si no coincide); si no viene, mantené la selección actual `.limit(1)`. Esto resuelve la ambigüedad presencial+pase (Open Q 2 → decisión: subscriptionId explícito).

    Budget en renew (:3910): mismo patrón que Task 1 — `plan.classesPerWeek === null ? (plan.monthlyClassBudget ?? null) : derivado`.

    D-01 en renew: si el plan de la sub renovada tiene `requiresPresencial === true`, corré la misma validación de presencial active/paused vía `getMemberSubscriptions`; si falla, lanzá el mismo BadRequestError que en assign.

    Guard D-09: envolvé los pares referral de renew (:3626-3627) y changePlan (:3128-3129) con `if (plan.planCategory !== 'especial')` (usá la variable de plan correcta en cada callsite). NO tocar `recomputeUserStatus`.

  </action>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "subscriptionId" el-templo-api/src/modules/subscriptions/schemas.ts` matchea el nuevo campo del renew schema.
    - `grep -c "planCategory !== 'especial'\|planCategory !== \"especial\"" el-templo-api/src/modules/subscriptions/service.ts` ≥ 3 (assign + changePlan + renew).
    - `grep "requiresPresencial" el-templo-api/src/modules/subscriptions/service.ts` aparece también en el bloque de renew.
    - `npx tsc --noEmit` pasa.
  </acceptance_criteria>
  <done>renew/changePlan soportan el pase con discriminador, budget, D-01 y D-09; tsc verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests de integración especial-pass (PASE-02/03/04, D-01, D-09)</name>
  <read_first>
    - el-templo-api/test/subscriptions/dual-subscription.test.ts (estructura beforeAll/afterAll/beforeEach + cleanAllTestData)
    - el-templo-api/test/subscriptions/_helpers.ts (createPlan/createMember/assignPlan con overrides)
    - el-templo-api/test/helpers.ts (auth/request)
  </read_first>
  <action>
    Creá `el-templo-api/test/subscriptions/especial-pass.test.ts` copiando la estructura de dual-subscription.test.ts. Cubrí:
    (1) PASE-02: socio con presencial active + asignar pase Socio → 2 subs en paralelo, classesRemaining del pase = 2.
    (2) PASE-02 negativo (D-01): asignar pase Socio a usuario sin presencial → 400 con mensaje de "presencial activo".
    (3) PASE-03: freemium/externo sin presencial + asignar pase Externo → sub única creada OK.
    (4) PASE-04: renovar el pase por subscriptionId → extiende la sub especial (no la presencial) y resetea classesRemaining a 2.
    (5) D-09: pagar el pase NO flippea un vínculo de referido pending→qualified ni aplica descuento (asertar precio pagado = precio del plan sin descuento).
    Creá los planes especiales de test vía `createPlan` con overrides `planCategory:'especial'`, `monthlyClassBudget:2`, `requiresPresencial` según corresponda (o usá los 2 planes seedeados por 0179 si el helper los expone). Corré sólo este archivo (regla del repo: no suite completo local).
  </action>
  <verify>
    <automated>cd el-templo-api && npx vitest run test/subscriptions/especial-pass.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - El archivo `test/subscriptions/especial-pass.test.ts` existe con ≥ 5 casos (PASE-02 positivo/negativo, PASE-03, PASE-04, D-09).
    - `npx vitest run test/subscriptions/especial-pass.test.ts` pasa (todos verdes).
    - El caso D-01 asierta status 400 y mensaje con "presencial activo".
    - El caso D-09 asierta que el precio pagado del pase no recibió descuento de referido.
  </acceptance_criteria>
  <done>especial-pass.test.ts verde cubriendo PASE-02/03/04, D-01, D-09.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                                                           |
| ---------------------------- | ----------------------------------------------------------------------------------------------------- |
| admin/gestión → assign/renew | Input de venta cruza acá; el discriminador Socio↔Externo debe venir del dato del plan, no del cliente |
| referidos ↔ pase             | El pase no debe contaminar el sistema de referidos (D-09)                                             |

## STRIDE Threat Register

| Threat ID | Category               | Component             | Disposition | Mitigation Plan                                                                                            |
| --------- | ---------------------- | --------------------- | ----------- | ---------------------------------------------------------------------------------------------------------- |
| T-161-03  | Elevation of Privilege | assignPlan pase Socio | mitigate    | validación server-side de presencial active/paused por `requiresPresencial` del plan (no flag del cliente) |
| T-161-04  | Tampering              | renewSubscription     | mitigate    | validar `subscription.userId === userId` antes de renovar por subscriptionId; evita renovar sub ajena      |
| T-161-05  | Repudiation            | guard de referidos    | mitigate    | guard `planCategory !== 'especial'` en los 3 callsites (assign/changePlan/renew); test D-09                |
| T-161-SC  | Tampering              | npm installs          | accept      | sin paquetes nuevos en esta fase                                                                           |

</threat_model>

<verification>
- `npx tsc --noEmit` verde.
- `npx vitest run test/subscriptions/especial-pass.test.ts` verde.
- dual-subscription.test.ts y class-tracking.test.ts siguen verdes en CI (no-regresión).
</verification>

<success_criteria>

- Pase Socio y Externo vendibles/renovables con las reglas D-01/D-09 y budget explícito.
- Discriminador de renovación por subscriptionId sin romper callers existentes.
  </success_criteria>

<output>
Create `.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-02-SUMMARY.md` when done
</output>
