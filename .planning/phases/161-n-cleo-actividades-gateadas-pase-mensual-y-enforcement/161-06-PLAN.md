---
phase: 161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement
plan: 06
type: execute
wave: 3
depends_on: ["161-01", "161-05"]
files_modified:
  - el-templo-api/src/modules/scheduling/booking-service.ts
  - el-templo-api/src/modules/scheduling/routes.ts
  - el-templo-api/test/scheduling/especial-gating.test.ts
autonomous: true
requirements: [GATE-01, GATE-03, GATE-04]
must_haves:
  truths:
    - "Un member sin pase con saldo que reserva una actividad especial recibe 400 con code PASS_REQUIRED (GATE-01/03)"
    - "El acceso a clases regulares del socio no cambia en nada (GATE-03)"
    - "Un externo con solo pase NO puede reservar actividades regulares (GATE-04)"
    - "Con 2 reservas futuras pendientes de especiales no se puede comprometer una 3ª; cancelar libera el cupo (D-04)"
    - "Las especiales son reservables dentro del período del pase (ventana extendida, D-06); la lista de espera aplica igual"
    - "Staff (admin/gestión) puede reservar una especial para alguien sin pase (bypass con aviso, D-07)"
  artifacts:
    - path: "el-templo-api/test/scheduling/especial-gating.test.ts"
      provides: "cobertura GATE-01/03/04, D-04, D-06, D-07"
      min_lines: 90
  key_links:
    - from: "el-templo-api/src/modules/scheduling/booking-service.ts"
      to: "PassRequiredError"
      via: "gating en reserve() para member sin pase en actividad especial"
      pattern: "PassRequiredError"
    - from: "el-templo-api/src/modules/scheduling/routes.ts"
      to: "PASS_REQUIRED"
      via: "surface del code en el catch de /reserve"
      pattern: "PASS_REQUIRED"
---

<objective>
Aplicar el enforcement de gating en `BookingService.reserve()` (y el booking manual admin): rechazar con `PassRequiredError` la reserva de una especial por un member sin pase (GATE-01/03), bloquear al externo-solo-pase de reservar regulares (GATE-04), contar reservas futuras pendientes contra el budget del pase (D-04), extender la ventana de anticipación para especiales (D-06), y permitir el bypass de staff con aviso (D-07). Surfacear el code PASS_REQUIRED en la ruta.

Purpose: Núcleo del enforcement — el gating duro server-side de las actividades especiales.
Output: reserve() + adminAddBooking + routes.ts + tests de integración de gating.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-CONTEXT.md
@.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-RESEARCH.md
@.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-PATTERNS.md
@.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-01-SUMMARY.md
@.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-05-SUMMARY.md

<interfaces>
Del Plan 01: `PassRequiredError` (code PASS_REQUIRED), `pickSubscriptionForActivity(userId, isSpecialActivity)`. Del Plan 05: `getScheduleSlotRaw(scheduleId)` ahora trae `isSpecial`.
Verificado 2026-07-14 en `booking-service.ts`:
- `reserve(...)` en :60; `getScheduleSlotRaw(scheduleId)` en :66; `assertDateWithinWindow(..., MEMBER_BOOKING_WINDOW_DAYS)` en :79-82 (constante `MEMBER_BOOKING_WINDOW_DAYS=2` en :42; `assertDateWithinWindow` parametriza windowDays en :1508).
- `getMemberSubscription(memberId)` singular en :87 (a reemplazar por `pickSubscriptionForActivity` cuando la actividad importa).
- Budget check actual en :152-160: sólo `subscription.classesRemaining <= 0`. Para D-04 hay que ADEMÁS contar reservas futuras pendientes de especiales del member (bookings status 'reservado'/'lista_espera' sobre actividades especiales, fecha futura) y bloquear cuando `count >= classesRemaining`.
- Resolución de rol del actor: `const actorRole = actor?.role ?? "member"` en :130; checks `if (actorRole === "member" && ...)` en :142,:185.
- `adminAddBooking(...)` en :528, usa `getScheduleSlotRaw` :536 y `getMemberSubscription` :550.
- Analog para contar bookings en ventana: `countBonusBookings(...)` en :1813.
- `scheduling/routes.ts`: catch de `/reserve` con `if (err instanceof CoverageExpiredError) return reply.code(400).send({ error, message, code: "COVERAGE_EXPIRED" })` en :813-825 (`handleServiceError` NO serializa `code`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gating en reserve() — GATE-01/03/04, ventana D-06, budget D-04</name>
  <read_first>
    - el-templo-api/src/modules/scheduling/booking-service.ts (reserve :60-215, assertDateWithinWindow :1508, countBonusBookings :1813)
    - el-templo-api/src/modules/shared/errors.ts (PassRequiredError)
    - el-templo-api/src/db/schema/bookings.ts (status enum: reservado/qr_escaneado/confirmado/cancelado/lista_espera/no_show)
  </read_first>
  <behavior>
    - member reserva especial sin pase (o sin saldo) → lanza PassRequiredError.
    - member con pase reserva especial dentro del período del pase → OK aunque falten más de 2 días.
    - member reserva 2 especiales futuras → la 3ª lanza error de saldo; cancelar una libera el cupo.
    - externo-solo-pase reserva una regular → BadRequestError ("tu pase solo habilita las actividades especiales").
    - socio con presencial (sin pase) reserva una regular → OK (comportamiento intacto).
  </behavior>
  <action>
    En `reserve()`, tras `getScheduleSlotRaw` leé `isSpecialActivity = scheduleRow.isSpecial`. Reemplazá `getMemberSubscription(memberId)` (:87) por `sub = await this.subscriptionService.pickSubscriptionForActivity(memberId, isSpecialActivity)` cuando la actividad importa (mantené el fallback de "no tenés suscripción" para el caso regular).

    GATE-01/03: si `isSpecialActivity && actorRole === "member" && !sub` → `throw new PassRequiredError()`.

    GATE-04: si `!isSpecialActivity && actorRole === "member"` y el member tiene sub especial-only (via `getMemberSubscriptions`: hay especial active/paused y NO hay presencial/online active/paused) → `throw new BadRequestError("Tu pase solo habilita las actividades especiales")`.

    D-06: cuando `isSpecialActivity`, pasá a `assertDateWithinWindow` un `windowDays` extendido = días restantes hasta el fin del período del pase (derivá de `sub.endDate`/vencimiento del pase) en lugar de `MEMBER_BOOKING_WINDOW_DAYS`. El resto de `assertDateWithinWindow` (feriado, día, excepción) y la capacidad/lista de espera quedan iguales.

    D-04: cuando `isSpecialActivity`, ADEMÁS del check `classesRemaining <= 0`, contá las reservas futuras pendientes del member sobre actividades especiales (bookings status en ('reservado','lista_espera'), fecha ≥ hoy, join a actividades especiales) — usá el patrón de `countBonusBookings` como analog — y si `count >= sub.classesRemaining` lanzá `BadRequestError` de saldo comprometido. Cancelar una reserva (flujo existente) libera el cupo automáticamente porque baja el count.

    Sin `any`; `catch (err: unknown)`. NO cambiés el orden de validación para actividades regulares (el gating nuevo va entre sub y budget).

  </action>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "PassRequiredError" el-templo-api/src/modules/scheduling/booking-service.ts` matchea en reserve().
    - `grep "pickSubscriptionForActivity" el-templo-api/src/modules/scheduling/booking-service.ts` matchea.
    - `grep -E "isSpecial" el-templo-api/src/modules/scheduling/booking-service.ts` matchea el uso de la ventana extendida y el conteo D-04.
    - `npx tsc --noEmit` pasa.
  </acceptance_criteria>
  <done>reserve() gatea especiales (GATE-01/03/04), extiende ventana (D-06) y cuenta reservas futuras (D-04); tsc verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bypass staff en booking manual (D-07) + surface de PASS_REQUIRED en la ruta</name>
  <read_first>
    - el-templo-api/src/modules/scheduling/booking-service.ts (adminAddBooking :528-560, patrón staff-bypass :142/:185)
    - el-templo-api/src/modules/scheduling/routes.ts (catch de /reserve :813-825)
  </read_first>
  <behavior>
    - adminAddBooking de una especial para un member sin pase NO lanza PassRequiredError (staff bypass); el gating duro es sólo para actorRole === 'member'.
    - La ruta /reserve, ante PassRequiredError, responde 400 con code "PASS_REQUIRED".
  </behavior>
  <action>
    En `adminAddBooking` (:528), aplicá el mismo patrón staff-bypass por rol server-derived: el gating de pase (PassRequiredError / GATE-04) sólo aplica si `actorRole === "member"`. Para staff, la reserva de una especial sin pase procede (el aviso confirmable es UI del Plan 07). Reusá `pickSubscriptionForActivity`/`getScheduleSlotRaw` como en reserve().

    En `scheduling/routes.ts`, en el catch de `/reserve`, agregá un branch `if (err instanceof PassRequiredError) return reply.code(400).send({ error: "Solicitud invalida", message: err.message, code: "PASS_REQUIRED" })` (espejo exacto del branch de CoverageExpiredError; importá PassRequiredError). Verificá si el endpoint de booking admin necesita el mismo surface y agregalo si aplica.

  </action>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "PASS_REQUIRED" el-templo-api/src/modules/scheduling/routes.ts` matchea el nuevo branch del catch.
    - `grep -A3 "adminAddBooking" el-templo-api/src/modules/scheduling/booking-service.ts` muestra el gating condicionado a actorRole === 'member' (staff bypass).
    - `npx tsc --noEmit` pasa.
  </acceptance_criteria>
  <done>Staff bypass en adminAddBooking + code PASS_REQUIRED surfaced en /reserve; tsc verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests de integración de gating (GATE-01/03/04, D-04, D-06, D-07)</name>
  <read_first>
    - el-templo-api/test/subscriptions/class-tracking.test.ts (patrón de reserva/booking en tests)
    - el-templo-api/test/helpers.ts (auth/request, roles staff vs member)
    - el-templo-api/test/subscriptions/_helpers.ts (createPlan/assignPlan especial)
  </read_first>
  <action>
    Creá `el-templo-api/test/scheduling/especial-gating.test.ts`. Setup: actividad especial con slot de sábado + actividad regular; un socio con presencial (+opcional pase), un externo con solo pase, un member sin pase. Casos:
    (1) GATE-01/03: member sin pase reserva especial → 400 code=PASS_REQUIRED.
    (2) GATE-03: el mismo socio reserva una regular → OK (acceso regular intacto).
    (3) GATE-04: externo-solo-pase reserva una regular → 400 con mensaje de pase-solo-especiales.
    (4) D-04: member con pase reserva 2 especiales futuras → OK; la 3ª → 400 saldo; cancelar una → la 3ª ahora OK.
    (5) D-06: reserva de especial a más de 2 días pero dentro del período del pase → OK.
    (6) D-07: staff reserva especial para member sin pase vía booking admin → OK (sin PASS_REQUIRED).
    Corré sólo este archivo.
  </action>
  <verify>
    <automated>cd el-templo-api && npx vitest run test/scheduling/especial-gating.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - El archivo existe con ≥ 6 casos (GATE-01/03, GATE-03 regular, GATE-04, D-04, D-06, D-07).
    - `npx vitest run test/scheduling/especial-gating.test.ts` pasa.
    - El caso (1) asierta status 400 y `code === "PASS_REQUIRED"`.
    - El caso (4) asierta que cancelar libera el cupo comprometido.
  </acceptance_criteria>
  <done>especial-gating.test.ts verde cubriendo GATE-01/03/04, D-04, D-06, D-07.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                                                                                    |
| --------------------- | -------------------------------------------------------------------------------------------------------------- |
| member → /reserve     | Input no confiable; el gating debe resolverse server-side por scheduleId→activity, nunca por flags del cliente |
| staff → booking admin | El bypass debe derivar del rol server-side, no de un parámetro del request                                     |

## STRIDE Threat Register

| Threat ID | Category               | Component            | Disposition | Mitigation Plan                                                                                       |
| --------- | ---------------------- | -------------------- | ----------- | ----------------------------------------------------------------------------------------------------- |
| T-161-12  | Spoofing               | reserve() gating     | mitigate    | `isSpecial` desde getScheduleSlotRaw (JOIN), `actorRole` server-derived; nunca del body               |
| T-161-13  | Elevation of Privilege | GATE-04              | mitigate    | bloqueo server-side de especial-only en actividades regulares vía getMemberSubscriptions              |
| T-161-14  | Tampering              | D-04 conteo de saldo | mitigate    | contar reservas futuras pendientes en la query (no confiar en classesRemaining sola); cancelar libera |
| T-161-15  | Elevation of Privilege | staff bypass D-07    | mitigate    | bypass sólo si actorRole !== 'member' (server-derived); aviso confirmable en UI (Plan 07)             |
| T-161-SC  | Tampering              | npm installs         | accept      | sin paquetes nuevos                                                                                   |

</threat_model>

<verification>
- `npx tsc --noEmit` verde.
- `npx vitest run test/scheduling/especial-gating.test.ts` verde.
- Tests de booking existentes verdes en CI (no-regresión del flujo regular).
</verification>

<success_criteria>

- Gating duro de especiales para members; bypass de staff; ventana extendida; conteo de reservas futuras; code PASS_REQUIRED en la ruta.
  </success_criteria>

<output>
Create `.planning/phases/161-n-cleo-actividades-gateadas-pase-mensual-y-enforcement/161-06-SUMMARY.md` when done
</output>
