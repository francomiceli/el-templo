---
phase: 60-plan-configuration
plan: 03
type: execute
wave: 2
depends_on: [60-01]
files_modified:
  - el-templo-admin/src/pages/PlanesPage.vue
  - el-templo-admin/src/components/AssignPlanDialog.vue
  - el-templo-admin/src/components/MemberSubscriptionTab.vue
  - el-templo-admin/src/composables/useSubscriptionsApi.ts
  - el-templo-admin/src/composables/useSettingsApi.ts
  - el-templo-admin/src/types/subscription.ts
  - el-templo-admin/src/types/settings.ts
autonomous: true
requirements: [PLANS-01, PLANS-02, PLANS-03, PLANS-04, PLANS-05]

must_haves:
  truths:
    - "PlanesPage shows grace period card at top with editable input and save button (superadmin only)"
    - "AssignPlanDialog shows day checkboxes (Lun-Sab) when assigning a fixed-mode plan"
    - "MemberSubscriptionTab shows class usage: plan name, classes used this week, total remaining, assigned days, expiry date"
    - "Grace period changes persist after save and page reload"
  artifacts:
    - path: "el-templo-admin/src/pages/PlanesPage.vue"
      provides: "Grace period settings card at top of plans page"
      contains: "gracePeriodDays"
    - path: "el-templo-admin/src/components/AssignPlanDialog.vue"
      provides: "Fixed-day selector step in subscription assignment"
      contains: "fixedDays"
    - path: "el-templo-admin/src/components/MemberSubscriptionTab.vue"
      provides: "Class usage display section in subscription card"
      contains: "classesRemaining"
    - path: "el-templo-admin/src/composables/useSettingsApi.ts"
      provides: "API composable for settings endpoints"
      exports: ["useSettingsApi"]
  key_links:
    - from: "el-templo-admin/src/pages/PlanesPage.vue"
      to: "/api/settings/grace-period"
      via: "useSettingsApi composable"
      pattern: "useSettingsApi.*grace"
    - from: "el-templo-admin/src/components/AssignPlanDialog.vue"
      to: "/api/subscriptions/:userId/assign"
      via: "fixedDays in assign payload"
      pattern: "fixedDays"
    - from: "el-templo-admin/src/components/MemberSubscriptionTab.vue"
      to: "/api/subscriptions/:userId/class-usage"
      via: "useSubscriptionsApi composable"
      pattern: "classUsage|getClassUsage"
---

<objective>
Wire the admin UI to the Plan 01 API changes: grace period settings card on PlanesPage, fixed-day selector in AssignPlanDialog, and class usage display in MemberSubscriptionTab. All data flows through the API — no new backend logic.

Purpose: Makes plan configuration features visible and usable to admin staff. Without this, the backend enforcement from Plans 01-02 is invisible.
Output: Updated PlanesPage, AssignPlanDialog, MemberSubscriptionTab, new useSettingsApi composable, updated types.
</objective>

<execution_context>
@/home/franco/.claude/get-shit-done/workflows/execute-plan.md
@/home/franco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-plan-configuration/60-CONTEXT.md
@.planning/phases/60-plan-configuration/60-01-SUMMARY.md

<interfaces>
<!-- API endpoints from Plan 01 that this plan consumes -->

GET /api/settings/grace-period
Response: { gracePeriodDays: number }

PUT /api/settings/grace-period
Body: { gracePeriodDays: number }
Response: { gracePeriodDays: number }

POST /api/subscriptions/:userId/assign (updated)
Body: { ...existing fields, fixedDays?: number[] }
Response: SubscriptionDetail (with classesRemaining, fixedDays, graceCheckInsAfterExpiry)

GET /api/subscriptions/:userId/class-usage
Response: { classesRemaining: number | null, classesUsedThisWeek: number, weeklyLimit: number | null, fixedDays: number[] | null, bookingMode: string }

<!-- Existing admin types -->

From el-templo-admin/src/types/subscription.ts:

```typescript
export interface PlanListItem {
  id: number;
  name: string;
  bookingMode: BookingMode;
  classesPerWeek: number | null; /* ... */
}
export interface SubscriptionDetail {
  id: number;
  userId: number;
  planId: number;
  planName: string;
  status: SubscriptionStatus;
  startDate: string;
  endDate: string | null;
  /* ... */
}
export interface AssignPlanInput {
  planId: number;
  branchId: number;
  startDate: string;
  priceTypeApplied: PriceType; /* ... */
}
```

From el-templo-admin/src/composables/useSubscriptionsApi.ts:

```typescript
export function useSubscriptionsApi() {
  return {
    getPlans,
    getMemberSubscription,
    getMemberSubscriptionHistory,
    assignPlan,
    pauseSubscription,
    resumeSubscription,
    cancelSubscription,
    getPricingPreview,
    createPlan,
    updatePlan,
    deactivatePlan,
    bulkMigratePlan,
  };
}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types, API composables, and grace period card on PlanesPage</name>
  <files>
    el-templo-admin/src/types/subscription.ts,
    el-templo-admin/src/types/settings.ts,
    el-templo-admin/src/composables/useSettingsApi.ts,
    el-templo-admin/src/composables/useSubscriptionsApi.ts,
    el-templo-admin/src/pages/PlanesPage.vue
  </files>
  <action>
    Update el-templo-admin/src/types/subscription.ts:
    - Add to SubscriptionDetail: classesRemaining: number | null, fixedDays: number[] | null, graceCheckInsAfterExpiry: number
    - Add to AssignPlanInput: fixedDays?: number[]
    - Add new ClassUsageInfo interface: { classesRemaining: number | null, classesUsedThisWeek: number, weeklyLimit: number | null, fixedDays: number[] | null, bookingMode: BookingMode }

    Create el-templo-admin/src/types/settings.ts:
    - Export interface GracePeriodSetting { gracePeriodDays: number }

    Create el-templo-admin/src/composables/useSettingsApi.ts:
    - Follow same pattern as useSubscriptionsApi
    - getGracePeriod(): Promise<GracePeriodSetting> — GET /api/settings/grace-period
    - setGracePeriod(days: number): Promise<GracePeriodSetting> — PUT /api/settings/grace-period with body { gracePeriodDays: days }

    Update el-templo-admin/src/composables/useSubscriptionsApi.ts:
    - Add getClassUsage(userId: number): Promise<ClassUsageInfo> — GET /api/subscriptions/${userId}/class-usage

    Update el-templo-admin/src/pages/PlanesPage.vue:
    - Add a q-card BEFORE the QTable, wrapped in a v-if for superadmin visibility (for now, always show — role gating comes in Phase 66)
    - Card content: "Periodo de Gracia" title, text explaining "Dias de gracia despues del vencimiento de la suscripcion", q-input type="number" v-model="gracePeriodDays" with suffix "dias", q-btn "Guardar" that calls saveGracePeriod()
    - Load grace period on mount via useSettingsApi().getGracePeriod()
    - Save with useSettingsApi().setGracePeriod(gracePeriodDays)
    - Show success/error toast on save
    - Style: q-card flat bordered with q-card-section, max-width 400px, q-mb-lg above the table

    DAY_LABELS constant for fixed-day UI (shared): { 1: 'Lun', 2: 'Mar', 3: 'Mie', 4: 'Jue', 5: 'Vie', 6: 'Sab' }
    Add to types/subscription.ts or as a shared constant.

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    PlanesPage shows grace period card at top with number input and save button. Grace period loads on mount and saves on click. useSettingsApi and useSubscriptionsApi updated with new methods. Types updated with new fields. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: AssignPlanDialog fixed-day selector + MemberSubscriptionTab class usage</name>
  <files>
    el-templo-admin/src/components/AssignPlanDialog.vue,
    el-templo-admin/src/components/MemberSubscriptionTab.vue
  </files>
  <action>
    Update AssignPlanDialog.vue:
    - After step 2 (Precio y Opciones), add a conditional step that shows ONLY when selectedPlan.bookingMode === 'fixed'. This step shows 6 checkboxes for weekdays (Lun, Mar, Mie, Jue, Vie, Sab) corresponding to ISO days [1,2,3,4,5,6].
    - The stepper should be: Step 1 (Select Plan) -> Step 2 (Pricing) -> Step 2.5 (Fixed Days, conditional) -> Step 3 (Confirm).
    - Implementation approach: use a computed stepCount and dynamic step names. When bookingMode is flexible, skip the fixed-days step entirely.
    - Add fixedDays: number[] to assignForm state (default empty array).
    - When plan is fixed mode, validate at least 1 day selected before allowing proceed.
    - In the confirm step, show selected days (e.g., "Dias asignados: Lun, Mie, Vie").
    - In onConfirm, include fixedDays in the assign payload when bookingMode is 'fixed'.
    - Styling: 6 q-checkbox components in a row with q-gutter-sm, each labeled with day abbreviation.

    Update MemberSubscriptionTab.vue:
    - In the active subscription card, add a new section between the dates row and pricing row:
    - "Clases" section showing:
      a. "Clases esta semana: {classesUsedThisWeek} / {weeklyLimit}" (or "Sin limite" if weeklyLimit is null)
      b. "Clases restantes: {classesRemaining}" (or "Sin limite" if classesRemaining is null)
      c. If fixedDays is not null: "Dias asignados: Lun, Mie, Vie" (translated from number array)
    - Load class usage via useSubscriptionsApi().getClassUsage(props.userId) alongside loadSubscription
    - Only show the classes section if the subscription has classesPerWeek (i.e., classUsage data exists and weeklyLimit is not null OR fixedDays is not null)
    - Use the DAY_LABELS constant from types for translation

    Both components use createLogger for error logging and useQuasar for toasts per project conventions.

  </action>
  <verify>
    <automated>cd /home/franco/projects/el-templo/el-templo-admin && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    AssignPlanDialog shows day checkboxes when assigning a fixed-mode plan, includes fixedDays in payload. MemberSubscriptionTab shows class usage section with weekly count, remaining classes, and assigned days. TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<verification>
- TypeScript compiles: `cd el-templo-admin && npx tsc --noEmit`
- PlanesPage: Grace period card visible, loads value, saves changes
- AssignPlanDialog: Fixed-day checkboxes appear for fixed-mode plans, hidden for flexible
- AssignPlanDialog: fixedDays sent in assign request body
- MemberSubscriptionTab: Class usage section shows weekly/remaining/days info
- MemberSubscriptionTab: Section hidden when no class tracking data
</verification>

<success_criteria>

- Grace period card on PlanesPage with load/save functionality
- Fixed-day selector conditionally shown in AssignPlanDialog for fixed-mode plans
- Class usage display in MemberSubscriptionTab (weekly count, remaining, assigned days)
- All admin TypeScript compiles cleanly
- API composables properly wired to new endpoints
  </success_criteria>

<output>
After completion, create `.planning/phases/60-plan-configuration/60-03-SUMMARY.md`
</output>
