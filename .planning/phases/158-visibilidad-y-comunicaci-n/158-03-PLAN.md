---
phase: 158-visibilidad-y-comunicaci-n
plan: 03
type: execute
wave: 2
depends_on: ["158-01"]
files_modified:
  - el-templo-app/src/pages/MisReferidosPage.vue
  - el-templo-app/src/router/routes.ts
  - el-templo-app/src/pages/ProfilePage.vue
autonomous: true
requirements: [VIS-01]
must_haves:
  truths:
    - "Desde ProfilePage el socio abre una pantalla 'Mis referidos' que muestra su codigo con boton Compartir (share nativo + fallback copiar), el descuento vigente con desglose pedagogico, y la lista simetrica de vinculos con chips Pendiente/Activo/Suspendido"
    - "El bloque de codigo + Compartir se muestra siempre, incluso con cero vinculos (es la herramienta de difusion)"
    - "Todos los numeros (% por vinculo, tope, %) vienen del server; nada hardcodeado"
    - "El link compartido contiene solo el codigo publico: https://app.eltemplo.org/register?ref=CODE"
  artifacts:
    - path: "el-templo-app/src/pages/MisReferidosPage.vue"
      provides: "pantalla Mis referidos (fetch GET /members/referrals, share, estados)"
      min_lines: 80
    - path: "el-templo-app/src/router/routes.ts"
      provides: "ruta /mis-referidos hija de MainLayout"
      contains: "mis-referidos"
    - path: "el-templo-app/src/pages/ProfilePage.vue"
      provides: "item de entrada 'Mis referidos' en el settings-card"
      contains: "mis-referidos"
  key_links:
    - from: "el-templo-app/src/pages/ProfilePage.vue"
      to: "/mis-referidos"
      via: "router.push en un settings-card__item clickable"
      pattern: "mis-referidos"
    - from: "el-templo-app/src/pages/MisReferidosPage.vue"
      to: "/members/referrals"
      via: "api.get en el fetch inicial"
      pattern: "members/referrals"
---

<objective>
La pantalla "Mis referidos" (VIS-01, D-26/D-27/D-28/D-29) de la app del socio, segun el contrato aprobado UI-SPEC S1/S2: codigo + share nativo (esta pantalla ES la herramienta de difusion del codigo de 157), descuento vigente con desglose pedagogico leido de config, y la lista simetrica de vinculos con chips de estado derivado. Consume `GET /members/referrals` del plan 01. Entrada desde ProfilePage.

Purpose: hoy el codigo de referido de 157 no tiene ninguna UI. Sin esta pantalla el socio no puede ver su descuento ni difundir su codigo.
Output: `MisReferidosPage.vue` (NEW) + ruta `/mis-referidos` + item de entrada en `ProfilePage.vue`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/158-visibilidad-y-comunicaci-n/158-CONTEXT.md
@.planning/phases/158-visibilidad-y-comunicaci-n/158-PATTERNS.md
@.planning/phases/158-visibilidad-y-comunicaci-n/158-UI-SPEC.md

<interfaces>
<!-- Shape de la respuesta de GET /members/referrals (owner: plan 01, referrals/types.ts). Espejar en el <script setup>. -->

ReferralLinkView: { userId: number; fullName: string; state: "pending" | "active" | "suspended" }
ReferralDiscountView: { percent: number; activeCount: number; perLinkPercent: number; capPercent: number }
ReferralsResponse: {
referralCode: string
discount: ReferralDiscountView
referred: ReferralLinkView[] // "Trajiste a"
referredBy: ReferralLinkView | null // "Te trajo"
}

Constante de difusion (planning decision resuelta): base publica = https://app.eltemplo.org (el WEB build de el-templo-app, ver boot/deep-links.ts). RegisterPage lee route.query.ref (fase 157). shareUrl = `https://app.eltemplo.org/register?ref=${referralCode}`.

Share nativo (dynamic import, molde bar-challenge/pages/Resultado.vue:207-234):
const { Share } = await import('@capacitor/share') // v8.0.1 YA en package.json — NO instalar nada
await Share.share({ title, text, url: shareUrl })
Fallback (el asset es un LINK, no imagen -> NO usar download): import { copyToClipboard } from 'quasar' + $q.notify warning.

Chips (UI-SPEC Color): Pendiente -> color="info"; Activo -> color="positive"; Suspendido -> color="warning" (+ caption "se reactiva si vuelve").
Fetch stack (ProfilePage.vue:200-214): api de 'src/boot/axios', createLogger de 'src/utils/logger', extractError de 'src/utils/extract-error', TemploLoader de 'src/components/TemploLoader.vue'.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MisReferidosPage.vue (pantalla completa S1)</name>
  <read_first>
    - el-templo-app/src/pages/ProfilePage.vue (familia de cards dark .info-card/.settings-card/.section-title y el bloque <style>; patron de fetch :163-166,200-214 con api/createLogger/extractError/TemploLoader)
    - el-templo-app/src/modules/bar-challenge/pages/Resultado.vue (:207-234 flujo de share nativo con dynamic import y try/catch de fallback)
    - el-templo-app/src/boot/deep-links.ts (base publica https://app.eltemplo.org)
    - el-templo-app/src/css/quasar.variables.scss (tokens $primary terracotta, $info/$positive/$warning; regla sin azul)
    - .planning/phases/158-visibilidad-y-comunicaci-n/158-UI-SPEC.md (contrato S1: bloques 1-3, estados, copy exacto)
  </read_first>
  <action>
    Crear `el-templo-app/src/pages/MisReferidosPage.vue` reusando 1:1 la familia de cards dark de ProfilePage (.info-card/.section-title — no introducir patron visual nuevo). En onMounted hacer `api.get<ReferralsResponse>('/members/referrals')` con el stack de fetch de ProfilePage (createLogger, extractError, TemploLoader). Estructura top-to-bottom de UI-SPEC S1: (Bloque 1 — SIEMPRE visible, incluso con cero vinculos) card con icono card_giftcard terracotta, eyebrow "TU CODIGO", el valor del codigo prominente (700, 24px, letter-spacing 0.1em) y un q-btn primary "Compartir mi codigo"; el handler de share hace dynamic import de @capacitor/share (NO instalar — ya esta en package.json) con `Share.share({ title: "Sumate a El Templo", text: "Entrena conmigo en El Templo. Usa mi codigo y los dos empezamos a pagar menos: " + shareUrl, url: shareUrl })` donde `shareUrl = "https://app.eltemplo.org/register?ref=" + referralCode`; en el catch, fallback con `copyToClipboard(shareUrl)` de 'quasar' + `$q.notify` warning con el copy exacto de UI-SPEC ("No pudimos abrir el menu de compartir. Copiamos el link para que lo pegues donde quieras."). (Bloque 2 — descuento) si `discount.percent > 0`: headline "Estas pagando {percent}% menos" (numero terracotta 700/24px) + linea de desglose "{activeCount} vinculos activos x {perLinkPercent}% = {percent}% (tope {capPercent}%)" leyendo TODOS los numeros de la respuesta (nunca hardcodear); si `percent === 0`: headline "Todavia no tenes descuento activo". (Bloque 3 — vinculos simetricos) dos secciones con .section-title: "Trajiste a" (referred[]) y "Te trajo" (referredBy, si no es null); renderizar una seccion solo si tiene >=1 vinculo; cada fila = fullName (14px/400) + q-chip a la derecha con el estado derivado y color por mapeo (info/positive/warning); las filas Suspendido muestran un caption 12px "se reactiva si vuelve". Estados de pagina: Loading = TemploLoader centrado; Loaded cero vinculos = Bloque 1 prominente + Bloque 2 con headline 0% + estado vacio en Bloque 3 (heading "Todavia no trajiste a nadie" + body de UI-SPEC); Load error = $q.notify negative + boton inline "Reintentar" que re-fetchea. Regla dura: el codigo + Compartir NUNCA se esconde detras del estado vacio. Voseo rioplatense (es-AR), signos y acentos correctos.
  </action>
  <verify>
    <automated>cd el-templo-app && npx vue-tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - El archivo el-templo-app/src/pages/MisReferidosPage.vue existe
    - `grep -n "members/referrals" el-templo-app/src/pages/MisReferidosPage.vue` presente (fetch)
    - `grep -n "app.eltemplo.org/register?ref=" el-templo-app/src/pages/MisReferidosPage.vue` presente (shareUrl con solo el codigo)
    - `grep -n "@capacitor/share" el-templo-app/src/pages/MisReferidosPage.vue` presente via dynamic import; NO hay linea de instalacion de dependencia
    - `grep -n "copyToClipboard" el-templo-app/src/pages/MisReferidosPage.vue` presente (fallback)
    - `grep -c "perLinkPercent\|capPercent\|activeCount" el-templo-app/src/pages/MisReferidosPage.vue` >= 3 (desglose leido de la respuesta, no hardcodeado)
    - Los tres colores de chip info/positive/warning aparecen mapeados a Pendiente/Activo/Suspendido
    - `vue-tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Pantalla operativa: codigo+share siempre visible, descuento con desglose desde server, vinculos simetricos con chips y estado vacio segun contrato.</done>
</task>

<task type="auto">
  <name>Task 2: Ruta /mis-referidos + item de entrada en ProfilePage</name>
  <read_first>
    - el-templo-app/src/router/routes.ts (:42-46 la ruta hija /change-password de MainLayout — molde exacto)
    - el-templo-app/src/pages/ProfilePage.vue (:68-82 el item "Cambiar contrasena" dentro del settings-card de "Ajustes" — molde a clonar)
  </read_first>
  <action>
    (1) En router/routes.ts agregar como hija del layout protegido (junto a change-password) la ruta `{ path: 'mis-referidos', name: 'mis-referidos', component: () => import('pages/MisReferidosPage.vue') }`. (2) En ProfilePage.vue, dentro del settings-card de "Ajustes", clonar el item de "Cambiar contrasena": un `settings-card__item settings-card__item--clickable` con `@click="$router.push('/mis-referidos')"`, icono `card_giftcard` (color primary), label "Mis referidos" y el `chevron_right` de cierre; reusar el `settings-card__divider` existente para separar. Sin patron visual nuevo.
  </action>
  <verify>
    <automated>cd el-templo-app && npx vue-tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "mis-referidos" el-templo-app/src/router/routes.ts` muestra la ruta con el import de MisReferidosPage.vue
    - `grep -n "mis-referidos" el-templo-app/src/pages/ProfilePage.vue` muestra el router.push del item nuevo
    - `grep -n "Mis referidos" el-templo-app/src/pages/ProfilePage.vue` presente (label)
    - `grep -n "card_giftcard" el-templo-app/src/pages/ProfilePage.vue` presente (icono)
    - `vue-tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>La ruta /mis-referidos resuelve MisReferidosPage y ProfilePage tiene el item de entrada que navega a ella.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary          | Description                                                             |
| ----------------- | ----------------------------------------------------------------------- |
| link compartido   | solo debe contener el codigo publico de referido, nada mas              |
| numeros mostrados | el % y el desglose los computa el server, el cliente no debe recalcular |

## STRIDE Threat Register

| Threat ID | Category               | Component              | Disposition | Mitigation Plan                                                                                                            |
| --------- | ---------------------- | ---------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------- |
| T-158-07  | Information Disclosure | shareUrl               | mitigate    | la URL solo lleva ?ref={referralCode} (codigo publico, el mismo param que RegisterPage ya lee); sin token ni id de usuario |
| T-158-08  | Tampering              | desglose del descuento | mitigate    | todos los numeros vienen de GET /members/referrals; el cliente no hace matematica de descuento (sin hardcode 10/40)        |
| T-158-SC  | Tampering              | supply chain           | mitigate    | @capacitor/share v8.0.1 y copyToClipboard (quasar) YA instalados; el plan NO instala ni actualiza dependencias             |

</threat_model>

<verification>
- `vue-tsc --noEmit` limpio en el-templo-app.
- Pantalla accesible desde ProfilePage; codigo+share siempre visible; desglose desde server; chips por estado derivado.
- shareUrl = https://app.eltemplo.org/register?ref=CODE (solo el codigo).
</verification>

<success_criteria>

- El socio ve su codigo, lo comparte (nativo o fallback copiar), entiende su descuento vigente (desglose pedagogico) y ve sus vinculos con estado en ambas direcciones.
- Cero vinculos: la herramienta de difusion (codigo+share) sigue prominente.
  </success_criteria>

<output>
Create `.planning/phases/158-visibilidad-y-comunicaci-n/158-03-SUMMARY.md` when done
</output>
