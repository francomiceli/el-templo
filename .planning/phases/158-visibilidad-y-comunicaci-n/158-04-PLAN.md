---
phase: 158-visibilidad-y-comunicaci-n
plan: 04
type: execute
wave: 2
depends_on: ["158-01"]
files_modified:
  - el-templo-admin/src/composables/useMembersApi.ts
  - el-templo-admin/src/components/MemberReferralsTab.vue
  - el-templo-admin/src/pages/AlumnoDetailPage.vue
autonomous: true
requirements: [VIS-03]
must_haves:
  truths:
    - "En la ficha del alumno, gestion ve una seccion 'Referidos' con quien lo trajo (link a esa ficha) y a quienes trajo, cada vinculo con su chip de estado derivado"
    - "El estado por vinculo usa el mismo criterio derivado (deriveCoveredUntil) que la app, nunca users.status"
    - "Cuando el alumno no tiene referidos, se muestra la nota vacia; los errores de carga usan el toast del modulo"
  artifacts:
    - path: "el-templo-admin/src/components/MemberReferralsTab.vue"
      provides: "seccion Referidos de la ficha (fetch getReferrals, dos subsecciones, chips, links)"
      min_lines: 50
    - path: "el-templo-admin/src/composables/useMembersApi.ts"
      provides: "getReferrals(userId) -> GET /admin/members/:id/referrals"
      contains: "getReferrals"
    - path: "el-templo-admin/src/pages/AlumnoDetailPage.vue"
      provides: "tab/panel Referidos que renderiza MemberReferralsTab"
      contains: "MemberReferralsTab"
  key_links:
    - from: "el-templo-admin/src/pages/AlumnoDetailPage.vue"
      to: "MemberReferralsTab.vue"
      via: "q-tab-panel name=referidos"
      pattern: "MemberReferralsTab"
    - from: "el-templo-admin/src/composables/useMembersApi.ts"
      to: "/admin/members/:id/referrals"
      via: "api.get en getReferrals"
      pattern: "admin/members"
---

<objective>
La seccion "Referidos" en la ficha del alumno del admin (VIS-03, D-34), segun UI-SPEC S3: quien lo trajo (con link a esa ficha) + a quienes trajo + el chip de estado derivado por vinculo — el mismo estado que ve la app. Es donde gestion consulta caso a caso. Consume `GET /admin/members/:id/referrals` del plan 01. El dashboard/listado GLOBAL queda diferido (D-35, fuera de scope).

Purpose: gestion necesita ver, desde la ficha, quien trajo a quien y en que estado esta cada vinculo para resolver dudas del socio.
Output: `getReferrals` en el composable + `MemberReferralsTab.vue` (NEW) + tab en `AlumnoDetailPage.vue`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/158-visibilidad-y-comunicaci-n/158-CONTEXT.md
@.planning/phases/158-visibilidad-y-comunicaci-n/158-PATTERNS.md
@.planning/phases/158-visibilidad-y-comunicaci-n/158-UI-SPEC.md

<interfaces>
<!-- Shape de GET /admin/members/:id/referrals (owner: plan 01 — devuelve el mismo ReferralOverview). Espejar en TS. -->

MemberReferralLink: { userId: number; fullName: string; state: "pending" | "active" | "suspended" }
MemberReferralsResponse: {
referralCode: string
discount: { percent: number; activeCount: number; perLinkPercent: number; capPercent: number }
referred: MemberReferralLink[] // "Trajo a"
referredBy: MemberReferralLink | null // "Lo trajo"
}
// La ficha S3 usa solo referred + referredBy (referralCode/discount se ignoran en esta superficie).

Analog composable (useMembersApi.ts:328-341 getNotes): async getReferrals(userId: number) { const { data } = await api.get<MemberReferralsResponse>(`/admin/members/${userId}/referrals`); return data } y exponerlo en el return del composable.
Chips (misma semantica que S1): Pendiente -> info, Activo -> positive, Suspendido -> warning (+ caption "se reactiva si vuelve"). Superficie light (cream/white). Link de nombre = terracotta ($primary), navega a la ficha de ese miembro.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: getReferrals en useMembersApi + MemberReferralsTab.vue</name>
  <read_first>
    - el-templo-admin/src/composables/useMembersApi.ts (:328-341 getNotes — molde exacto de la funcion nueva + como se expone en el return)
    - el-templo-admin/src/components/MemberSubscriptionTab.vue (:1-40 estructura de tab: spinner q-spinner-dots color=primary centrado durante loading, luego contenido; extractError para el toast de error)
    - .planning/phases/158-visibilidad-y-comunicaci-n/158-UI-SPEC.md (contrato S3: subsecciones "Lo trajo"/"Trajo a", chips, empty note, states)
    - el-templo-admin/src/css/quasar.variables.scss (tokens $primary terracotta, $info/$positive/$warning)
  </read_first>
  <action>
    (1) En useMembersApi.ts agregar `async function getReferrals(userId: number): Promise<MemberReferralsResponse>` clonando getNotes (`api.get<MemberReferralsResponse>('/admin/members/' + userId + '/referrals')`) y exponerlo en el objeto de retorno del composable junto a getNotes/getPlans. Definir el tipo MemberReferralsResponse (con MemberReferralLink) en el mismo archivo o en el modulo de tipos del composable, espejando el bloque `<interfaces>`. (2) Crear `MemberReferralsTab.vue` que recibe `props: { userId: number }`, en onMounted llama getReferrals(userId), muestra q-spinner-dots color=primary centrado durante loading (molde MemberSubscriptionTab), y renderiza dos subsecciones: "Lo trajo" (referredBy, si no es null — nombre completo como link terracotta que hace router.push a la ficha de ese userId) y "Trajo a" (referred[] — cada nombre link a su ficha). Renderizar una subseccion solo si tiene >=1 vinculo. Cada fila lleva un q-chip con el estado derivado y color por mapeo (info/positive/warning); las filas Suspendido muestran caption "se reactiva si vuelve". Si el alumno no tiene ningun referido (referredBy null y referred vacio): una unica linea text-caption "Este alumno no tiene referidos." En error de carga: toast negativo via extractError (patron del modulo), sin UI de error inline nueva. Superficie light. Voseo/es-AR, acentos correctos.
  </action>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "getReferrals" el-templo-admin/src/composables/useMembersApi.ts` muestra la funcion y su exposicion en el return
    - `grep -n "admin/members" el-templo-admin/src/composables/useMembersApi.ts` cubre la ruta `/admin/members/${userId}/referrals`
    - El archivo el-templo-admin/src/components/MemberReferralsTab.vue existe y declara prop userId
    - `grep -c "info\|positive\|warning" el-templo-admin/src/components/MemberReferralsTab.vue` cubre los tres colores de chip
    - `grep -n "Este alumno no tiene referidos" el-templo-admin/src/components/MemberReferralsTab.vue` presente (empty note)
    - El nombre "Lo trajo" navega a la ficha (`grep -n "router.push\|push(" el-templo-admin/src/components/MemberReferralsTab.vue` presente)
    - `vue-tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>El composable expone getReferrals y el tab renderiza ambas subsecciones con chips, links a ficha y empty note.</done>
</task>

<task type="auto">
  <name>Task 2: Tab "Referidos" en AlumnoDetailPage</name>
  <read_first>
    - el-templo-admin/src/pages/AlumnoDetailPage.vue (:340-386 patron q-tab/q-tab-panel; :797-804 zona de imports de los otros *Tab)
    - el-templo-admin/src/components/MemberReferralsTab.vue (prop userId — creado en Task 1)
  </read_first>
  <action>
    En AlumnoDetailPage.vue agregar tras el tab "Perfil" un `<q-tab name="referidos" label="Referidos" />` y el panel correspondiente `<q-tab-panel name="referidos"><MemberReferralsTab :user-id="memberProfile.id" /></q-tab-panel>` (usar la misma prop de id de miembro que ya usan los demas paneles). Importar MemberReferralsTab junto a los otros *Tab en la zona de imports. Alternativa permitida por UI-SPEC: foldear como card al fondo del tab "perfil" en vez de tab propio — el contrato visual aplica igual; elegir tab propio salvo que el archivo lo dificulte.
  </action>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "name=\"referidos\"" el-templo-admin/src/pages/AlumnoDetailPage.vue` muestra el q-tab y el q-tab-panel
    - `grep -n "MemberReferralsTab" el-templo-admin/src/pages/AlumnoDetailPage.vue` muestra el import y el uso en el panel con :user-id
    - `vue-tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>La ficha del alumno tiene la seccion Referidos operativa, alimentada por MemberReferralsTab.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                                                 |
| ----------------------------------------- | ------------------------------------------------------------------------------------------- |
| admin -> GET /admin/members/:id/referrals | la autorizacion la impone el backend (guard ADMIN_ROLES del plan 01); el front solo consume |
| estado mostrado                           | debe reflejar el mismo criterio derivado del server, no recalcular en el cliente            |

## STRIDE Threat Register

| Threat ID | Category               | Component                  | Disposition | Mitigation Plan                                                                                                                                     |
| --------- | ---------------------- | -------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-158-09  | Elevation of Privilege | consumo del endpoint admin | accept      | el guard de rol vive en el backend (plan 01, T-158-02); el admin ya esta autenticado con rol para entrar a la ficha; el front no reimplementa authz |
| T-158-10  | Tampering              | chip de estado             | mitigate    | el state viene del server (deriveCoveredUntil); el cliente no deriva estado desde users.status                                                      |
| T-158-SC  | Tampering              | supply chain               | accept      | este plan NO instala paquetes (npm) — sin superficie nueva                                                                                          |

</threat_model>

<verification>
- `vue-tsc --noEmit` limpio en el-templo-admin.
- La ficha muestra la seccion Referidos con ambas subsecciones, chips por estado y empty note.

## Puesta en marcha (OPERACIONAL — post-deploy, NO tareas de codigo; D-36/D-37)

Checklist para despues de que las 4 planes esten en staging/master y el deploy pase verde (requiere OK de Franco, ver reglas de SSH/deploy en MEMORY):

1. Verificar smoke del deploy (enum 0177 aplicada en prod por el pipeline de migraciones).
2. Correr `backfill-referral-codes.ts --apply` en prod (D-25 de la 157 / D-36) — genera los codigos faltantes de los socios existentes. Requiere OK explicito de Franco para SSH.
3. Anuncio unico del programa via `queueAdHocNotification` categoria `anuncios` (D-37), copy de UI-SPEC S4 ("Nuevo: programa de referidos — trae a quien quieras entrenar y los dos pagan menos. Mira tu codigo en Mi perfil."). SIN banners ni onboarding in-app.
   </verification>

<success_criteria>

- Gestion ve, desde la ficha del alumno, quien lo trajo y a quienes trajo, con el estado derivado de cada vinculo y links de navegacion a las fichas relacionadas.
- El alumno sin referidos muestra la nota vacia; los errores usan el toast del modulo.
  </success_criteria>

<output>
Create `.planning/phases/158-visibilidad-y-comunicaci-n/158-04-SUMMARY.md` when done
</output>
