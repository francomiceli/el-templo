---
phase: 158-visibilidad-y-comunicaci-n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/referrals/types.ts
  - el-templo-api/src/modules/referrals/service.ts
  - el-templo-api/src/modules/referrals/routes.ts
  - el-templo-api/src/app.ts
  - el-templo-api/src/modules/members/routes.ts
  - el-templo-api/test/referrals/member-endpoint.test.ts
  - el-templo-api/test/referrals/admin-referrals-endpoint.test.ts
autonomous: true
requirements: [VIS-01, VIS-03]
must_haves:
  truths:
    - "Un socio autenticado obtiene su propio código (lazy-generado si falta), su descuento vigente con desglose, y la lista de vínculos con estado derivado, sin ver los vínculos de otro socio"
    - "discount.percent se obtiene LLAMANDO a computeReferralDiscountPercent (D-30: reuso, no reimplementación) — cero drift de mecánica posible"
    - "Gestión (rol admin) obtiene los referidos de cualquier alumno desde su ficha; un no-admin autenticado recibe 403"
    - "El estado por vínculo se deriva con deriveCoveredUntil de la contraparte, nunca con users.status; los vínculos revoked no aparecen"
  artifacts:
    - path: "el-templo-api/src/modules/referrals/service.ts"
      provides: "getReferralOverview(userId) — composición pura sobre generateReferralCode + getReferralConfig + computeReferralDiscountPercent + deriveCoveredUntil"
      contains: "getReferralOverview"
    - path: "el-templo-api/src/modules/referrals/routes.ts"
      provides: "GET /members/referrals member-scoped (id server-derived)"
      exports: ["referralMemberRoutes"]
    - path: "el-templo-api/src/modules/members/routes.ts"
      provides: "GET /admin/members/:id/referrals con guard ADMIN_ROLES"
      contains: "referrals"
    - path: "el-templo-api/test/referrals/member-endpoint.test.ts"
      provides: "cobertura de código lazy, estados derivados, desglose desde config, IDOR"
  key_links:
    - from: "el-templo-api/src/app.ts"
      to: "referralMemberRoutes"
      via: "app.register con prefix /api/members/referrals"
      pattern: "referralMemberRoutes"
    - from: "el-templo-api/src/modules/referrals/service.ts getReferralOverview"
      to: "computeReferralDiscountPercent"
      via: "reuso directo para discount.percent (D-30)"
      pattern: "computeReferralDiscountPercent"
    - from: "el-templo-api/src/modules/referrals/service.ts getReferralOverview"
      to: "deriveCoveredUntil"
      via: "estado derivado por contraparte (solo para los estados por vínculo)"
      pattern: "deriveCoveredUntil"
---

<objective>
Backend de lectura del sistema de referidos: un método de servicio `getReferralOverview` que compone lo que la fase 157 ya expone (código lazy, config de %/tope, % vigente vía `computeReferralDiscountPercent`, estado activo por `deriveCoveredUntil`) en un shape único, y dos rutas que lo sirven — la del socio (member self-scoped) y la de la ficha del admin. Es la fuente de datos de las pantallas de los planes 03 (app) y 04 (admin).

Purpose: VIS-01 (datos de "Mis referidos") y VIS-03 (datos de la ficha admin) necesitan un endpoint. Sin este plan las pantallas no tienen de dónde leer.
Output: `getReferralOverview` + `referrals/routes.ts` (NEW) + registro en `app.ts` + ruta admin en `members/routes.ts` + tests de integración.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/158-visibilidad-y-comunicaci-n/158-CONTEXT.md
@.planning/phases/158-visibilidad-y-comunicaci-n/158-PATTERNS.md
@.planning/phases/157-n-cleo-transaccional-de-referidos/157-VERIFICATION.md

<interfaces>
<!-- Contrato de respuesta que este plan CREA. Los planes 03 (app) y 04 (admin) lo espejan. -->
<!-- Owner: el-templo-api/src/modules/referrals/types.ts -->

ReferralLinkView:

- userId: number
- fullName: string // nombre + apellido de la contraparte (D-29: se conocen)
- state: "pending" | "active" | "suspended"

ReferralDiscountView:

- percent: number // = await this.computeReferralDiscountPercent(userId) — REUSO D-30, NO reimplementar la fórmula
- activeCount: number // vínculos con state === "active" (ambas direcciones), derivado de las listas
- perLinkPercent: number // desde getReferralConfig().percentPerLink (fallback 10)
- capPercent: number // desde getReferralConfig().maxPercentCap (fallback 40)

ReferralOverview:

- referralCode: string // generateReferralCode(userId) — lazy si falta
- discount: ReferralDiscountView
- referred: ReferralLinkView[] // a quiénes trajo este usuario ("Trajiste a" / admin "Trajo a")
- referredBy: ReferralLinkView | null // quién lo trajo ("Te trajo" / admin "Lo trajo")

Firmas fase 157 reusadas (referrals/service.ts, NO modificar):

- generateReferralCode(userId: number): Promise<string> // lazy + idempotente
- getReferralConfig(): Promise<{ percentPerLink: number; maxPercentCap: number }>
- computeReferralDiscountPercent(userId: number): Promise<number> // canónico del cobro — getReferralOverview LO LLAMA para discount.percent (D-30); el test de paridad queda como red de seguridad
- deriveCoveredUntil(db, userId): Promise<string | null> // exportado desde subscriptions/service — "YYYY-MM-DD" o null

Mapeo de estado por vínculo (D-28):

- referrals.status === "pending" -> "pending"
- "qualified" + contraparte cubierta (coveredUntil >= today) -> "active"
- "qualified" + contraparte vencida (coveredUntil < today | null) -> "suspended"
- "revoked" -> EXCLUIR de la lista
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: getReferralOverview en ReferralService + tipos de retorno</name>
  <read_first>
    - el-templo-api/src/modules/referrals/service.ts (métodos existentes: generateReferralCode, getReferralConfig, computeReferralDiscountPercent — reusar tal cual, computeReferralDiscountPercent se LLAMA para discount.percent; deriveCoveredUntil se importa desde subscriptions/service)
    - el-templo-api/src/modules/referrals/types.ts (ReferralConfig existente — agregar los tipos nuevos junto a él)
    - el-templo-api/src/db/schema/referrals.ts (columnas referrerId/referredId/status; status enum pending/qualified/revoked)
    - el-templo-api/src/db/schema/users.ts (firstName/lastName para el fullName de la contraparte)
  </read_first>
  <action>
    Agregar a `referrals/types.ts` las interfaces `ReferralLinkView`, `ReferralDiscountView` y `ReferralOverview` con el shape exacto del bloque `<interfaces>`. Agregar a `ReferralService` (referrals/service.ts) el método `async getReferralOverview(userId: number): Promise<ReferralOverview>`. Implementación: (1) `const referralCode = await this.generateReferralCode(userId)` (lazy). (2) SELECT de `referrals` donde `referrerId = userId OR referredId = userId` y `status != 'revoked'`, con JOIN a `users` para traer firstName+lastName de la contraparte (el otro extremo del vínculo). (3) Para cada vínculo calcular `state` — el loop propio existe SOLO para derivar los estados por vínculo (`ReferralLinkView[]`), NO para la matemática del descuento: si `status='pending'` -> "pending"; si `status='qualified'`, resolver `deriveCoveredUntil(this.db, counterpartyId)` y comparar con `today` (`new Date().toISOString().split("T")[0]`, mismo criterio que computeReferralDiscountPercent :176-185) -> cubierta="active", vencida/null="suspended". Separar en `referred` (vínculos donde `referrerId === userId`) y `referredBy` (el único vínculo donde `referredId === userId`, o null). (4) `discount.percent = await this.computeReferralDiscountPercent(userId)` — REUSO obligatorio (D-30 dice literalmente "reusando computeReferralDiscountPercent"); PROHIBIDO reimplementar `Math.min(activeCount * pct, cap)` en getReferralOverview. (5) `const { percentPerLink, maxPercentCap } = await this.getReferralConfig()`; `activeCount` = total de vínculos con `state==="active"` en ambas listas (derivado de la lista del paso 3). Devolver `{ referralCode, discount: { percent, activeCount, perLinkPercent: percentPerLink, capPercent: maxPercentCap }, referred, referredBy }`. NO tocar computeReferralDiscountPercent ni qualifyFirstPayment (mecánica de 157 congelada). `fullName` = `[firstName, lastName].filter(Boolean).join(" ").trim()`.
  </action>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "getReferralOverview" el-templo-api/src/modules/referrals/service.ts` devuelve la firma `async getReferralOverview(userId: number): Promise<ReferralOverview>`
    - El cuerpo de getReferralOverview contiene `this.computeReferralDiscountPercent(userId)` (reuso D-30) y NO contiene un `Math.min` propio para el percent
    - `grep -c "deriveCoveredUntil" el-templo-api/src/modules/referrals/service.ts` >= 2 (el uso preexistente de computeReferralDiscountPercent + el nuevo de getReferralOverview para los estados)
    - `grep -n "ReferralOverview\|ReferralLinkView\|ReferralDiscountView" el-templo-api/src/modules/referrals/types.ts` muestra las 3 interfaces exportadas
    - No aparece `users.status` dentro de getReferralOverview (`grep -n "users.status" el-templo-api/src/modules/referrals/service.ts` sin coincidencias nuevas)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>getReferralOverview compila, obtiene el % llamando a computeReferralDiscountPercent (sin fórmula duplicada), deriva los estados por vínculo con deriveCoveredUntil, excluye revoked y expone código+descuento+ambos lados del vínculo.</done>
</task>

<task type="auto">
  <name>Task 2: Ruta member GET /members/referrals + registro + ruta admin en ficha</name>
  <read_first>
    - el-templo-api/src/modules/notifications/routes.ts (:158-206 patrón member-facing con onRequest:[fastify.authenticate] y userId de request.user; :254-291 bloque admin con guard ADMIN_ROLES + reply.code(403))
    - el-templo-api/src/modules/subscriptions/member-routes.ts (member self-scoped, id server-derived, IDOR-safe)
    - el-templo-api/src/app.ts (:172-220 bloque de registros /api/members/* y /api/admin/*; ubicar el register nuevo junto a memberSubscriptionRoutes :180)
    - el-templo-api/src/modules/members/routes.ts (prefijo real del módulo y molde de una ruta admin con :id — p.ej. la de notes; import de ADMIN_ROLES)
  </read_first>
  <action>
    Crear `el-templo-api/src/modules/referrals/routes.ts` exportando `export const referralMemberRoutes: FastifyPluginAsync`. Instancia `new ReferralService(fastify.db, fastify.log)`. Registra `fastify.get("/", { onRequest: [fastify.authenticate] }, ...)` que toma `const { userId } = request.user` (NUNCA de params/body — IDOR) y devuelve `await service.getReferralOverview(userId)`. En `app.ts` registrar `await app.register(referralMemberRoutes, { prefix: "/api/members/referrals" })` junto a los otros `/api/members/*` (per D-30, convención member-facing). En `members/routes.ts` agregar la ruta admin de la ficha (D-34): `GET /:id/referrals` (resultante `/api/admin/members/:id/referrals`) con el guard admin exacto del molde — leer `const { role } = request.user`; si `!(ADMIN_ROLES as readonly string[]).includes(role)` devolver `reply.code(403).send({ error: "Acceso denegado" })`; parsear `:id` a number y devolver `await new ReferralService(request.server.db, request.server.log).getReferralOverview(id)`. Reusar la instancia/patrón de servicio que ya use ese archivo si difiere.
  </action>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "referralMemberRoutes" el-templo-api/src/app.ts` muestra el register con `prefix: "/api/members/referrals"`
    - `grep -n "export const referralMemberRoutes" el-templo-api/src/modules/referrals/routes.ts` presente
    - En referrals/routes.ts, el handler usa `request.user` para el userId y NO lee userId de `request.params`/`request.body` (`grep -n "request.user" el-templo-api/src/modules/referrals/routes.ts` presente)
    - `grep -n "referrals" el-templo-api/src/modules/members/routes.ts` muestra la ruta admin nueva con `:id/referrals`
    - La ruta admin contiene el guard `403` (`grep -n "403\|ADMIN_ROLES" el-templo-api/src/modules/members/routes.ts` cubre el bloque nuevo)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Ambas rutas montadas: la del socio devuelve su overview autoscopeado, la del admin la de cualquier alumno tras el guard de rol.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests de integración de ambos endpoints</name>
  <read_first>
    - el-templo-api/test/subscriptions/coverage-endpoint.test.ts (:1-75 molde exacto: createTestApp, getAuthToken, cleanAllTestData, insertSub :54-70 para montar cobertura de contraparte)
    - el-templo-api/test/referrals/qualification.test.ts (helpers de setup de vínculos referrals ya existentes en test/referrals/)
    - el-templo-api/test/helpers.ts (auth/request utilities, roles disponibles)
    - el-templo-api/src/modules/referrals/service.ts (computeReferralDiscountPercent — para el assert de paridad)
  </read_first>
  <behavior>
    - member-endpoint: sin código previo, GET /members/referrals genera y devuelve un referralCode no vacío (lazy)
    - member-endpoint: vínculo pending -> state "pending"; qualified con contraparte cubierta (insertar sub activa) -> "active"; qualified con contraparte vencida -> "suspended"; revoked NO aparece
    - member-endpoint: discount.perLinkPercent y capPercent salen de config (no hardcodeados); discount.percent === computeReferralDiscountPercent(userId) (paridad — red de seguridad del reuso D-30)
    - member-endpoint: IDOR — el socio A no ve los vínculos del socio B (id server-derived; el token de A solo devuelve lo de A)
    - admin-endpoint: token admin -> 200 con { referred, referredBy } del alumno objetivo; token de socio no-admin -> 403
  </behavior>
  <action>
    Crear `test/referrals/member-endpoint.test.ts` y `test/referrals/admin-referrals-endpoint.test.ts` clonando el harness de `coverage-endpoint.test.ts` (createTestApp/getAuthToken/cleanAllTestData) y los helpers de setup de vínculos de `test/referrals/`. Montar cobertura de contraparte con subs directas al estilo `insertSub` para forzar active vs suspended. Cubrir cada bullet de `<behavior>`. El assert de paridad compara `res.body.discount.percent` con el retorno de `computeReferralDiscountPercent` para el mismo userId — como getReferralOverview lo llama directamente (D-30), este assert es la red de seguridad contra regresiones futuras. El caso admin verifica 200 con rol admin y 403 con rol de socio.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm test test/referrals/member-endpoint.test.ts test/referrals/admin-referrals-endpoint.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Los dos archivos de test existen bajo `el-templo-api/test/referrals/`
    - `grep -c "it(\|test(" el-templo-api/test/referrals/member-endpoint.test.ts` >= 4 (lazy code, tres estados, config, IDOR — al menos 4 casos)
    - member-endpoint contiene un assert de paridad contra `computeReferralDiscountPercent` (`grep -n "computeReferralDiscountPercent" el-templo-api/test/referrals/member-endpoint.test.ts` presente)
    - admin-referrals-endpoint contiene un caso que espera status 403 (`grep -n "403" el-templo-api/test/referrals/admin-referrals-endpoint.test.ts` presente)
    - `pnpm test` de ambos archivos: verde (o el ejecutor confirma verde en CI si el runner local no está disponible)
  </acceptance_criteria>
  <done>Los dos endpoints tienen cobertura de estados derivados, desglose desde config, paridad de descuento, IDOR y guard admin.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                             | Description                                        |
| ---------------------------------------------------- | -------------------------------------------------- |
| socio autenticado → GET /members/referrals           | el userId debe derivarse del token, no del request |
| admin autenticado → GET /admin/members/:id/referrals | solo roles admin pueden leer datos de otro alumno  |

## STRIDE Threat Register

| Threat ID | Category               | Component                        | Disposition | Mitigation Plan                                                                                                           |
| --------- | ---------------------- | -------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------- |
| T-158-01  | Information Disclosure | GET /members/referrals           | mitigate    | `const { userId } = request.user` — nunca leer userId de params/body; test IDOR explícito (socio A no ve a B)             |
| T-158-02  | Elevation of Privilege | GET /admin/members/:id/referrals | mitigate    | guard `ADMIN_ROLES` con `reply.code(403)`; test 403 para rol socio                                                        |
| T-158-03  | Information Disclosure | fullName de contraparte          | accept      | exposición intencional (D-29: las partes del vínculo se conocen en el gym); solo dentro de los vínculos propios del socio |
| T-158-SC  | Tampering              | supply chain                     | accept      | este plan NO instala paquetes (npm) — sin superficie nueva                                                                |

</threat_model>

<verification>
- `npx tsc --noEmit` limpio en el-templo-api.
- Los dos endpoints existen, autoscopeados (member) y guardados por rol (admin).
- discount.percent viene de computeReferralDiscountPercent (reuso, sin fórmula duplicada).
- Tests de referrals nuevos verdes; assert de paridad con computeReferralDiscountPercent presente.
</verification>

<success_criteria>

- Un socio autenticado recibe su overview completo (código lazy + descuento con desglose + vínculos con estado derivado) sin poder leer los de otro.
- Gestión lee los referidos de cualquier alumno desde la ficha tras el guard de rol; no-admin recibe 403.
- El % del overview no puede divergir del cómputo del cobro: es la MISMA función (paridad testeada como red de seguridad).
  </success_criteria>

<output>
Create `.planning/phases/158-visibilidad-y-comunicaci-n/158-01-SUMMARY.md` when done
</output>
