---
phase: 158-visibilidad-y-comunicaci-n
plan: 02
type: execute
wave: 2
depends_on: ["158-01"]
files_modified:
  - el-templo-api/src/db/schema/notifications.ts
  - el-templo-api/src/db/migrations/0177_referidos_notification_category.sql
  - el-templo-api/src/modules/notifications/types.ts
  - el-templo-api/src/modules/referrals/service.ts
  - el-templo-api/src/modules/subscriptions/service.ts
  - el-templo-api/test/referrals/activation-notification.test.ts
autonomous: true
requirements: [VIS-02]
must_haves:
  truths:
    - "Al flippear un vínculo pending→qualified por un primer pago real (pricePaid>0), se encola UNA notificación 'referral_link_activated' AL REFERIDOR con el nombre del referido interpolado"
    - "El referido NO recibe push; un re-cobro del mismo socio no vuelve a notificar (solo el flip real dispara)"
    - "Un fallo de la notificación NUNCA rompe el cobro (best-effort try/catch + log.warn)"
    - "La categoría 'referidos' existe en el enum de ambas tablas y cada usuario tiene su preferencia opt-out backfilleada, idempotente"
  artifacts:
    - path: "el-templo-api/src/db/migrations/0177_referidos_notification_category.sql"
      provides: "ALTER enum x2 (append 'referidos' ultimo) + backfill NOT EXISTS de preferencias"
      contains: "referidos"
    - path: "el-templo-api/src/modules/notifications/types.ts"
      provides: "union + NOTIFICATION_CATEGORIES + seed referral_link_activated"
      contains: "referral_link_activated"
    - path: "el-templo-api/src/modules/subscriptions/service.ts"
      provides: "hook best-effort en qualifyReferralOnCharge"
      contains: "queueNotification"
  key_links:
    - from: "el-templo-api/src/modules/subscriptions/service.ts qualifyReferralOnCharge"
      to: "NotificationService.queueNotification"
      via: "encolado al referrerId tras el flip"
      pattern: "queueNotification"
    - from: "el-templo-api/src/modules/referrals/service.ts qualifyFirstPayment"
      to: "hook de notificacion"
      via: "retorno del vinculo flippeado (referrerId + nombre del referido)"
      pattern: "qualifyFirstPayment"
---

<objective>
La notificacion de "vinculo activado" (VIS-02, D-31/D-32/D-33): categoria nueva `referidos` (migracion de enum + seed de template + backfill de preferencias), y el disparo best-effort al referidor en el momento exacto del flip a `qualified`, sin romper jamas el cobro. `qualifyFirstPayment` se augmenta para devolver el vinculo que efectivamente flippeo (referidor + nombre del referido) — la mecanica del flip (UPDATE guardado `WHERE status='pending'`) NO cambia, solo se expone que flippeo para poder notificar solo una vez.

Purpose: sin este plan el socio no se entera de que su referido pago; el estado en pantalla existe pero falta el push que trae al socio de vuelta.
Output: migracion 0177 + enum/seed + `qualifyFirstPayment` con retorno + hook en `qualifyReferralOnCharge` + test de integracion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/158-visibilidad-y-comunicaci-n/158-CONTEXT.md
@.planning/phases/158-visibilidad-y-comunicaci-n/158-PATTERNS.md
@.planning/phases/158-visibilidad-y-comunicaci-n/158-UI-SPEC.md
@.claude/skills/el-templo-db-migrations/SKILL.md

<interfaces>
<!-- Firmas reusadas/augmentadas -->

qualifyFirstPayment (referrals/service.ts) — HOY: async qualifyFirstPayment(payerUserId: number): Promise<void>
AUGMENTAR a: Promise<{ referrerId: number; referredFirstName: string } | null>
Semantica NUEVA: SELECT del vinculo pending del que payer es referido (join a users para firstName del payer) ANTES del UPDATE guardado; si no hay pending -> return null; si hay -> hacer el UPDATE existente (WHERE referredId=? AND status='pending') y return { referrerId, referredFirstName }.
La clausula del UPDATE NO cambia (mecanica de 157 congelada); solo se agrega el SELECT previo y el valor de retorno.

queueNotification (notifications/service.ts:226) — reusar tal cual:
queueNotification(input: { userId: number; templateKey: string; scheduledAt?: Date; titleOverride?: string; bodyOverride?: string; routeOverride?: string }): Promise<number>
Ya es defensivo: devuelve -1 si no hay template / preferencia off / sin device-token. El {Nombre} NO lo interpola ningun engine — va por bodyOverride.

Seed a agregar en TEMPLATE_SEEDS (notifications/types.ts, copy exacto UI-SPEC S4):
templateKey: "referral_link_activated"
category: "referidos"
title: "Tu referido pago!" (con signos correctos) / titleFemale: variante femenina
body / bodyFemale: "{Nombre} pago su primer plan. Ya tenes tu descuento activo." (con signos/acentos correctos)
route: "/mis-referidos"

Hook actual (subscriptions/service.ts:404-412):
private async qualifyReferralOnCharge(payerUserId, pricePaid): Promise<void>
if (pricePaid <= 0) return
await new ReferralService(this.db, this.log).qualifyFirstPayment(payerUserId)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Migracion 0177 + enum de categoria + seed del template</name>
  <read_first>
    - el-templo-api/src/db/migrations/0158_planes_notification_category.sql (ANALOG EXACTO — clonar estructura completa, planes -> referidos)
    - el-templo-api/src/db/schema/notifications.ts (notificationCategoryEnum linea 16-22; columna fisica notification_category en notification_templates y notification_preferences)
    - el-templo-api/src/modules/notifications/types.ts (union NotificationCategory, const NOTIFICATION_CATEGORIES, array TEMPLATE_SEEDS)
    - .claude/skills/el-templo-db-migrations/SKILL.md (regla: hand-written, _migrations es fuente de verdad, NUNCA drizzle-kit migrate/push, cero punto-y-coma en comentarios, append enum ultimo byte-for-byte)
  </read_first>
  <action>
    (1) En db/schema/notifications.ts apendar "referidos" AL FINAL del array de notificationCategoryEnum (tras "planes") — preservar el orden byte-for-byte de los valores existentes. La columna fisica es `notification_category` (primer arg del mysqlEnum), NO `category`. (2) En notifications/types.ts: agregar el literal "referidos" a la union NotificationCategory, agregarlo al final del const array NOTIFICATION_CATEGORIES, y agregar al final de TEMPLATE_SEEDS el seed con templateKey "referral_link_activated", category "referidos", route "/mis-referidos" y el copy de titulo/cuerpo (masculino y femenino) exacto de UI-SPEC S4. NO tocar seedTemplates() — ya itera TEMPLATE_SEEDS con INSERT IGNORE y corre en startup via notification-cron. (3) Crear db/migrations/0177_referidos_notification_category.sql clonando 0158: un ALTER TABLE notification_templates MODIFY COLUMN notification_category enum(...,'planes','referidos') NOT NULL, el mismo ALTER sobre notification_preferences, y el backfill INSERT ... SELECT ... WHERE NOT EXISTS de una preferencia enabled categoria referidos por cada usuario. Cero punto-y-coma dentro de comentarios de linea (el runner splitea por punto-y-coma antes de stripear comentarios). Confirmar que 0177 es el proximo libre (ultimo = 0176_referrals_core.sql). (4) Aplicar local con `pnpm db:migrate` (runner custom, NO drizzle-kit) DESPUES de los cambios de schema y ANTES de la verificacion. (5) El .sql se commitea en el MISMO commit que el cambio de schema.
  </action>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - notificationCategoryEnum en schema/notifications.ts termina con "referidos" como ultimo valor
    - `grep -n "referidos" el-templo-api/src/modules/notifications/types.ts` muestra el literal en la union y en NOTIFICATION_CATEGORIES
    - `grep -n "referral_link_activated" el-templo-api/src/modules/notifications/types.ts` muestra el seed nuevo con route "/mis-referidos"
    - El .sql existe y sus dos ALTER terminan la lista del enum con ...,'planes','referidos'
    - Ningun comentario de linea del .sql contiene punto-y-coma
    - `pnpm db:migrate` corre limpio y registra 0177 en la tabla _migrations
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Categoria referidos presente en enum de ambas tablas + template seedeable + preferencias backfilleadas idempotentes; migracion 0177 aplicada local y commiteada junto al schema.</done>
</task>

<task type="auto">
  <name>Task 2: qualifyFirstPayment devuelve el flip + hook best-effort al referidor</name>
  <read_first>
    - el-templo-api/src/modules/referrals/service.ts (qualifyFirstPayment linea 196-206 — UPDATE guardado; schema referrals para referrerId/referredId/status; users para firstName del payer)
    - el-templo-api/src/modules/subscriptions/service.ts (qualifyReferralOnCharge linea 404-412 — punto exacto del flip, invocado desde 4 charge-paths)
    - el-templo-api/src/modules/notifications/service.ts (queueNotification linea 226-310 — firma, comportamiento defensivo -1, bodyOverride)
  </read_first>
  <action>
    (1) Augmentar `qualifyFirstPayment(payerUserId)` en referrals/service.ts para que devuelva `{ referrerId: number; referredFirstName: string } | null`: antes del UPDATE existente, SELECT del vinculo `WHERE referredId = payerUserId AND status = 'pending'` con join/lookup del firstName del payer en users; si no hay vinculo pending -> `return null` (no hubo flip, no notificar); si hay -> ejecutar el UPDATE existente SIN cambiar su clausula y `return { referrerId, referredFirstName }`. Esto NO altera la mecanica del flip (157 congelada), solo agrega el SELECT previo y el retorno. (2) En subscriptions/service.ts, dentro de `qualifyReferralOnCharge` (mantener el guard `pricePaid <= 0` intacto), capturar el retorno de qualifyFirstPayment; si es no-null, encolar la notificacion AL REFERIDOR (D-31: nunca al referido) con `new NotificationService(this.db, this.log).queueNotification({ userId: referrerId, templateKey: "referral_link_activated", bodyOverride: \`${referredFirstName} pago su primer plan. Ya tenes tu descuento activo.\` })` (usar el copy con acentos correctos de UI-SPEC). Envolver TODO el encolado en try/catch: en el catch, `this.log.warn({ err, referrerId }, "referral activation notification failed (best-effort)")` y seguir — un fallo NUNCA relanza ni rompe el cobro (D-33). El hook vive DENTRO de qualifyReferralOnCharge (no en cada una de las 4 call-sites). El titulo lo resuelve queueNotification por genero del referidor (title/titleFemale del template) — solo se overridea el body para interpolar {Nombre}.
  </action>
  <verify>
    <automated>cd el-templo-api && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - La firma de qualifyFirstPayment devuelve `Promise<{ referrerId: number; referredFirstName: string } | null>` (`grep -n "qualifyFirstPayment" el-templo-api/src/modules/referrals/service.ts`)
    - La clausula del UPDATE sigue siendo `status = 'pending'` sobre referredId (no cambio de mecanica)
    - `grep -n "queueNotification" el-templo-api/src/modules/subscriptions/service.ts` presente dentro de qualifyReferralOnCharge
    - `grep -n "referral_link_activated" el-templo-api/src/modules/subscriptions/service.ts` presente
    - El encolado esta envuelto en try/catch con log.warn (`grep -n "best-effort" el-templo-api/src/modules/subscriptions/service.ts` presente)
    - El userId encolado es el referrerId, no el payer (verificable por lectura del handler)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>El flip real devuelve el referidor + nombre del referido; el hook encola una vez al referidor, best-effort, sin poder romper el cobro.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test de integracion de la notificacion de activacion</name>
  <read_first>
    - el-templo-api/test/referrals/qualification.test.ts (flip a qualified — reusar setup de vinculos y del cobro)
    - el-templo-api/test/subscriptions/coverage-endpoint.test.ts (harness createTestApp/cleanAllTestData/insertSub)
    - el-templo-api/test/helpers.ts (auth/request utilities, seed de device tokens si existe)
    - el-templo-api/src/modules/notifications/service.ts (queueNotification: requiere template seedeado + preferencia enabled + device token del referidor, si no devuelve -1)
  </read_first>
  <behavior>
    - Con template referral_link_activated seedeado y device token del referidor: un primer pago (pricePaid>0) del referido encola EXACTAMENTE una fila en pending_notifications para el referrerId, con el body conteniendo el firstName del referido
    - El referido NO recibe fila alguna
    - Re-cobrar al mismo referido (segundo pago) NO encola una segunda fila (el flip ya ocurrio, qualifyFirstPayment devuelve null)
    - Best-effort: si la notificacion falla/skippea (p.ej. referidor sin device token -> queueNotification devuelve -1), el cobro igual se completa sin error
  </behavior>
  <action>
    Crear test/referrals/activation-notification.test.ts clonando el harness de qualification.test.ts. Seedear el template (llamar seedTemplates o insertar la fila referral_link_activated con categoria referidos tras aplicar la migracion en el test DB) y un device token para el referidor. Montar un vinculo pending, ejecutar el primer cobro del referido con pricePaid>0 y asertar una unica fila en pending_notifications para el referrerId con el firstName del referido en el body. Asertar cero filas para el referido. Ejecutar un segundo cobro y asertar que no se agrega una nueva fila. Agregar el caso best-effort: referidor sin device token -> el cobro se completa sin throw y no hay fila (skip -1).
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm test test/referrals/activation-notification.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - El archivo test/referrals/activation-notification.test.ts existe
    - `grep -c "it(\|test(" el-templo-api/test/referrals/activation-notification.test.ts` >= 3 (encola-al-referidor, no-al-referido/no-dup, best-effort)
    - Un caso asserta que el referido no recibe notificacion
    - Un caso asserta que el re-cobro no duplica
    - Un caso asserta que el cobro no rompe ante fallo/skip de la notificacion
    - `pnpm test` del archivo: verde (o el ejecutor confirma verde en CI si el runner local no esta disponible)
  </acceptance_criteria>
  <done>La notificacion de activacion esta cubierta: encola una vez al referidor, nunca al referido, no duplica en re-cobro, y es best-effort ante fallos.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                               | Description                                                       |
| ------------------------------------------------------ | ----------------------------------------------------------------- |
| cobro (transaccion de plata) -> cola de notificaciones | un fallo de push no debe abortar la transaccion del cobro         |
| destinatario del push                                  | solo el referidor recibe; el nombre del referido viaja en el body |

## STRIDE Threat Register

| Threat ID | Category               | Component                                    | Disposition | Mitigation Plan                                                                                                                                           |
| --------- | ---------------------- | -------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-158-04  | Denial of Service      | qualifyReferralOnCharge -> queueNotification | mitigate    | try/catch envolvente + log.warn; un fallo de la cola nunca relanza ni rompe el cobro (D-33); test best-effort explicito                                   |
| T-158-05  | Information Disclosure | destinatario / body del push                 | mitigate    | encolar SOLO al referrerId (nunca al referido); el nombre del referido es conocido por el referidor (partes del vinculo), sin datos sensibles adicionales |
| T-158-06  | Tampering              | migracion de enum 0177                       | mitigate    | append "referidos" ultimo byte-for-byte en ambas tablas; backfill NOT EXISTS idempotente; runner custom con \_migrations como fuente de verdad            |
| T-158-SC  | Tampering              | supply chain                                 | accept      | este plan NO instala paquetes (npm) — sin superficie nueva                                                                                                |

</threat_model>

<verification>
- `npx tsc --noEmit` limpio en el-templo-api.
- Migracion 0177 aplicada local (`pnpm db:migrate`), enum con "referidos" ultimo en ambas tablas, cero punto-y-coma en comentarios.
- qualifyFirstPayment devuelve el flip; hook encola al referidor best-effort dentro de qualifyReferralOnCharge.
- Test de activacion verde: una notificacion al referidor, ninguna al referido, sin duplicado, best-effort.
</verification>

<success_criteria>

- Cuando un referido paga su primer plan, el referidor recibe la notificacion "vinculo activado" con el nombre del referido.
- El referido no recibe push; el re-cobro no re-notifica.
- Ningun fallo de notificacion puede romper un cobro.
- La categoria referidos queda opt-out granular como el resto.
  </success_criteria>

<output>
Create `.planning/phases/158-visibilidad-y-comunicaci-n/158-02-SUMMARY.md` when done
</output>
