---
phase: 124-estructura-de-datos-de-las-3-dimensiones-saneo
plan: 02
type: execute
wave: 2
depends_on: ["124-01"]
files_modified:
  - el-templo-api/saneo-exercises.ts
  - el-templo-api/test/exercises/saneo-exercises.test.ts
autonomous: true
requirements: [TREE-05]
user_setup: []

must_haves:
  truths:
    - "Los dupes EXACTOS (mismo exercise + dificultadLineal + route + effort) quedan resueltos por soft-merge: cada dupe apunta a su canónico (MIN id) vía canonical_exercise_id, sin borrar ninguna fila"
    - "Los ejercicios con ruta vacía/placeholder quedan marcados route_pending=true (detección, NO asignación de ruta — D-08)"
    - "El script es idempotente: re-correrlo no cambia nada (guardas IS NULL / NOT marcado)"
    - "El mismo ejercicio en distinto dificultadLineal NO se trata como dupe (se preserva como escalón distinto de la cadena — D-06)"
    - "Ninguna fila de exercises se borra ni se reasigna su id (FKs históricas de session_prescriptions/program_content_blocks intactas)"
  artifacts:
    - path: "el-templo-api/saneo-exercises.ts"
      provides: "Script CLI one-off de detección + soft-merge de dupes + marcado de ruta-pendiente (analog backfill-gender.ts)"
      contains: "createSingleConnection"
      min_lines: 60
    - path: "el-templo-api/test/exercises/saneo-exercises.test.ts"
      provides: "Integration test contra MySQL real: dupes exactos colapsan, escalones distintos se preservan, idempotencia"
      contains: "canonical_exercise_id"
  key_links:
    - from: "el-templo-api/saneo-exercises.ts"
      to: "exercises.canonical_exercise_id"
      via: "UPDATE guardado por canonical_exercise_id IS NULL (soft-merge D-07)"
      pattern: "canonical_exercise_id IS NULL"
    - from: "el-templo-api/saneo-exercises.ts"
      to: "exercises.route_pending"
      via: "UPDATE route_pending=1 donde route vacío/placeholder y route_pending=0"
      pattern: "route_pending"
---

<objective>
SANEAR el catálogo de ejercicios sobre los campos creados en el Plan 01: (1) resolver duplicados EXACTOS por soft-merge con puntero canónico (sin deletes), y (2) DETECTAR y MARCAR los ejercicios sin ruta válida. Cubre **TREE-05**. Backend-first.

Purpose: El bootstrap LLM (125), el grafo DAG (126) y el ajuste in-session (131) deben construir sobre un catálogo confiable. Los dupes exactos serían nodos basura en el grafo; los "sin ruta" no pueden entrar al árbol. Este plan deja ambos resueltos a nivel estructural.

Output:

- `saneo-exercises.ts` — script CLI one-off (corre vía `npx tsx`, analog `backfill-gender.ts`) que detecta + reporta + escribe punteros canónicos + marca route_pending, todo idempotente.
- Integration test contra MySQL real que prueba el colapso de dupes exactos, la preservación de escalones distintos (D-06), el marcado de ruta, y la idempotencia.

Decisiones LOCKED honradas:

- D-06: dupe = SOLO exacto (mismo `exercise` + `dificultadLineal` + `route` + `effort`). Mismo ejercicio en distinto dl = escalón distinto, NO dupe.
- D-07: soft-merge por puntero (`canonical_exercise_id` = MIN(id) del grupo), CERO deletes (FKs históricas).
- D-08: ruta faltante → SOLO detectar + marcar `route_pending`; NO borrar ni inventar ruta (la asignación real es 125/128).
- D-11/D-12: idempotente (guardas IS NULL / NOT marcado), reversible (rollback = null punteros + route_pending=0, ya cubierto por drop de columnas del Plan 01).

NO incluye: asignar/inventar rutas, fusionar nombres de sub-familia, poblar leverage/subfamily (eso es 125). `position` NO se toca (queda legacy — D-11).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-estructura-de-datos-de-las-3-dimensiones-saneo/124-CONTEXT.md
@.planning/phases/124-estructura-de-datos-de-las-3-dimensiones-saneo/124-PATTERNS.md
@.planning/phases/124-estructura-de-datos-de-las-3-dimensiones-saneo/124-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Contratos del Plan 01 (deben existir antes de este plan) + analogs. Usar directamente. -->

Columnas nuevas (Plan 01, schema exercises):

- subfamily_id INT NULL (FK exercise_subfamilies.id)
- leverage VARCHAR(50) NULL
- canonical_exercise_id INT NULL (self-FK exercises.id) — destino del soft-merge
- route_pending TINYINT(1) NOT NULL DEFAULT 0 — destino del marcado de ruta
  Campos para detectar dupe exacto (D-06): exercise, dificultad_lineal, route, effort

Script CLI analog (backfill-gender.ts):

- import "dotenv/config";
- import { createSingleConnection } from "./src/db/index"; // src/db/index.ts línea 23
- import { exercises } from "./src/db/schema";
- import { sql } from "drizzle-orm";
- async function main(): Promise<void> { const { db, connection } = await createSingleConnection(); try { ... } finally { await connection.end(); } }
- main().catch((err: unknown) => { const message = err instanceof Error ? err.message : String(err); console.error(...); process.exit(1); });
- console.log permitido SOLO en este tipo de script CLI one-off (NO en el server). catch(err: unknown)+instanceof Error obligatorio. Sin `any`.

Test helpers: el-templo-api/test/helpers.ts (auth/request utils, DB eltemplo_test). Los tests corren en CI contra MySQL real — NO correr el suite localmente (política del proyecto); gate local = tsc.

FK-preservation (por qué soft-merge): session-prescriptions.ts (exercise_id NOT NULL), micro-programs.ts (program_content_blocks.exercise_id references exercises.id). NUNCA DELETE/reasignar exercises.id.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Escribir el integration test del saneo (dupes exactos, escalones distintos, marcado de ruta, idempotencia)</name>
  <files>el-templo-api/test/exercises/saneo-exercises.test.ts</files>
  <read_first>
    - el-templo-api/test/helpers.ts (utilidades de DB de test, conexión a eltemplo_test)
    - el-templo-api/backfill-gender.ts (analog del script que se va a testear: entrypoint, UPDATE guardado)
    - el-templo-api/src/db/schema/exercises.ts (columnas resultantes del Plan 01)
    - .planning/phases/124-estructura-de-datos-de-las-3-dimensiones-saneo/124-PATTERNS.md (sección saneo-detect.ts)
  </read_first>
  <behavior>
    - Test A (soft-merge dupe exacto): insertar 2 filas con MISMO exercise+dificultad_lineal+route+effort. Tras correr el saneo, la de id mayor tiene `canonical_exercise_id` = id de la menor (MIN); la menor (canónico) tiene `canonical_exercise_id` NULL. Ambas filas SIGUEN existiendo (count sin cambios).
    - Test B (escalón distinto NO es dupe — D-06): insertar 2 filas mismo exercise+route+effort pero distinto `dificultad_lineal`. Tras el saneo, ambas tienen `canonical_exercise_id` NULL (no se colapsan).
    - Test C (marcado de ruta — D-08): insertar fila con route vacío/placeholder. Tras el saneo, `route_pending` = 1. Insertar fila con route válida → `route_pending` permanece 0.
    - Test D (idempotencia): correr el saneo dos veces seguidas; el segundo run no cambia ningún `canonical_exercise_id` ni `route_pending` ya escrito (guardas IS NULL / ya-marcado).
    - Test E (FKs intactas): ninguna fila de exercises borrada; el count de exercises antes == después.
  </behavior>
  <action>
    Crear el directorio `el-templo-api/test/exercises/` si no existe (`mkdir -p el-templo-api/test/exercises`) ANTES de escribir el archivo. Crear `el-templo-api/test/exercises/saneo-exercises.test.ts` siguiendo el patrón de los tests existentes en `test/` (vitest, helpers de DB contra eltemplo_test). El test debe importar y ejecutar la función exportada del saneo (ver Task 2: el script debe exportar una función `runSaneo(db)` además del entrypoint CLI, para ser testeable sin spawnear proceso). Seed de filas de exercises directamente vía Drizzle insert (NO vía API). Cubrir los 5 comportamientos del bloque <behavior>. Usar reloj real (no vi.useFakeTimers — desincroniza de MySQL). Limpiar las filas sembradas en afterEach/afterAll. Sin `any` (catch err:unknown + instanceof Error donde aplique).

    NOTA: este test se escribe ANTES de la implementación (RED). El executor debe verificar que falla por la ausencia de `runSaneo` / lógica, NO por error de setup. Por política del proyecto el suite completo se corre en CI, no localmente — el gate local es `tsc --noEmit`; el RED se confirma por tsc/import error o corriendo solo este archivo si el entorno de DB de test está disponible.

  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test/exercises/saneo-exercises.test.ts` existe y cubre los 5 comportamientos (A–E) con asserts explícitos sobre `canonical_exercise_id`, `route_pending` y count de filas.
    - El test referencia `runSaneo` (la función exportada que la Task 2 implementa).
    - `grep -c "canonical_exercise_id\|route_pending" test/exercises/saneo-exercises.test.ts` >= 2.
    - `tsc --noEmit` no reporta errores en el archivo de test (los tipos resuelven). El fallo de runtime esperado (RED) es por lógica ausente, no por compilación.
    - Sin `any`; reloj real.
  </acceptance_criteria>
  <done>
    Existe un integration test que codifica los criterios de saneo (soft-merge exacto, no-colapso de escalones, marcado de ruta, idempotencia, no-delete) y compila contra el schema del Plan 01.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar saneo-exercises.ts (detección + soft-merge + marcado, idempotente)</name>
  <files>el-templo-api/saneo-exercises.ts</files>
  <read_first>
    - el-templo-api/backfill-gender.ts (analog completo: dotenv, createSingleConnection, UPDATE guardado por IS NULL, entrypoint main().catch, console.log permitido)
    - el-templo-api/src/db/index.ts (export createSingleConnection, línea 23)
    - el-templo-api/test/exercises/saneo-exercises.test.ts (el contrato a satisfacer — Task 1)
    - .planning/phases/124-estructura-de-datos-de-las-3-dimensiones-saneo/124-PATTERNS.md (UPDATE correlacionado de soft-merge + nota de FK-preservation)
  </read_first>
  <action>
    Crear `el-templo-api/saneo-exercises.ts` siguiendo el analog `backfill-gender.ts`. Exportar una función `runSaneo(db)` (recibe la instancia Drizzle) que contiene toda la lógica, y un entrypoint CLI (`main()` que abre `createSingleConnection()`, llama `runSaneo(db)` en try/finally con `connection.end()`, y `main().catch(...)` con `err: unknown` + `instanceof Error` + `process.exit(1)`). Esto hace el script testeable (Task 1) y ejecutable vía `npx tsx saneo-exercises.ts`.

    `runSaneo(db)` hace, en orden, todo idempotente:
    1. DETECCIÓN + REPORTE (console.log — permitido en script CLI one-off): contar grupos de dupes exactos (GROUP BY exercise, dificultad_lineal, route, effort HAVING COUNT(*) > 1) y contar ejercicios con ruta vacía/placeholder. Imprimir los conteos antes de mutar (para que un humano pueda inspeccionar en una corrida de dry-read si se desea).
    2. SOFT-MERGE (D-07): UPDATE correlacionado que setea `canonical_exercise_id` = MIN(id) del grupo de dupe exacto para cada fila del grupo distinta del MIN, GUARDADO por `canonical_exercise_id IS NULL` (idempotente). CERO DELETE / cero reasignación de id. El criterio de dupe exacto es EXACTAMENTE `exercise` + `dificultad_lineal` + `route` + `effort` (D-06) — mismo ejercicio en distinto dificultad_lineal NO entra al grupo.
    3. MARCADO DE RUTA (D-08): UPDATE `route_pending = 1` donde la route esté vacía/placeholder, GUARDADO por `route_pending = 0` (idempotente). NO asigna ni inventa route; SOLO marca. El valor exacto de "ruta vacía/placeholder" debe determinarse leyendo los datos (route es NOT NULL, así que será cadena vacía o un placeholder); usar una condición robusta (p.ej. `route = '' OR route IS NULL` y/o el placeholder real si existe — documentar en comentario). Si hay ambigüedad sobre el placeholder real en prod, marcar solo el caso vacío y dejar comentario TODO para confirmar contra prod en ejecución; NO bloquear.

    Usar `db.execute(sql\`...\`)` para los UPDATE correlacionados (Drizzle query builder no expresa bien el self-join correlacionado — analog backfill-gender.ts). `catch (err: unknown)` + `instanceof Error`. Sin `any`. NO tocar `position`, `effort`, `level`, `dificultad_lineal`, `route` (solo lectura para detección).

  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `saneo-exercises.ts` exporta `runSaneo` y tiene un entrypoint CLI (`main().catch`).
    - `tsc --noEmit` sale 0.
    - El UPDATE de soft-merge está guardado por `canonical_exercise_id IS NULL` y usa MIN(id) como canónico; el criterio de grupo es exercise+dificultad_lineal+route+effort (los 4, D-06).
    - El UPDATE de marcado está guardado por `route_pending = 0` y NO escribe en la columna `route`.
    - Cero `DELETE` y cero reasignación de `id` sobre exercises (`grep -ci "DELETE\|SET[[:space:]]*id[[:space:]]*=" saneo-exercises.ts` == 0; el patrón mira cláusulas SET, no WHERE).
    - Sin `any`; `catch (err: unknown)` + `instanceof Error` en el entrypoint.
    - Los tests de la Task 1 quedan en estado GREEN-able (la lógica satisface A–E). El executor confirma vía CI, no corriendo el suite local.
  </acceptance_criteria>
  <done>
    El saneo detecta dupes exactos y rutas faltantes, escribe punteros canónicos + marca route_pending de forma idempotente y sin borrar filas, satisfaciendo el test del contrato. TREE-05 cubierto a nivel estructural.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                      | Description                                                                                                |
| ------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| script saneo → prod DB (~1.493 filas)                         | UPDATE masivo sobre exercises; un criterio de dupe mal escrito colapsaría escalones legítimos de la cadena |
| exercises.id ↔ session_prescriptions / program_content_blocks | FKs históricas NOT NULL; un DELETE las orfanaría                                                           |

## STRIDE Threat Register

| Threat ID | Category               | Component              | Disposition | Mitigation Plan                                                                                                                                |
| --------- | ---------------------- | ---------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| T-124-04  | Tampering              | soft-merge UPDATE      | mitigate    | Criterio de dupe EXACTO de 4 campos (D-06) testeado (Test B: escalón distinto NO colapsa); cero DELETE/reasignación de id (Test E + grep gate) |
| T-124-05  | Denial of Service      | re-corrida del script  | mitigate    | Guardas IS NULL / route_pending=0 → idempotente (Test D); reportar conteos antes de mutar permite inspección humana                            |
| T-124-06  | Information Disclosure | console.log del script | accept      | Script CLI one-off (analog backfill-gender.ts); console.log permitido SOLO aquí, no en el server; no imprime secretos                          |
| T-124-SC  | Tampering              | npm/pip/cargo installs | mitigate    | N/A — no instala paquetes (drizzle-orm/tsx ya presentes)                                                                                       |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit` en el-templo-api sale 0.
- El integration test cubre A–E (soft-merge exacto, no-colapso de escalón, marcado de ruta, idempotencia, no-delete) y corre en CI contra MySQL real.
- El script no contiene DELETE ni reasignación de exercises.id.
- `position`/`effort`/`route`/`dificultad_lineal` no se escriben.
</verification>

<success_criteria>

- TREE-05: dupes exactos resueltos por soft-merge (puntero canónico, sin deletes); ejercicios sin ruta detectados y marcados; criterio explícito y documentado (D-06).
- El saneo es idempotente y reversible (rollback cubierto por el drop de columnas del Plan 01).
- Las FKs históricas (session_prescriptions / program_content_blocks) quedan intactas.
  </success_criteria>

<output>
Create `.planning/phases/124-estructura-de-datos-de-las-3-dimensiones-saneo/124-02-SUMMARY.md` when done
</output>
