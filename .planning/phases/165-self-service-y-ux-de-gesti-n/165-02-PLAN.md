---
phase: 165-self-service-y-ux-de-gesti-n
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/members/service.ts
  - el-templo-api/src/modules/members/schemas.ts
  - el-templo-api/src/modules/members/routes.ts
  - el-templo-api/test/convert-freemium-to-trial.test.ts
  - el-templo-admin/src/components/TrialMemberFormDialog.vue
autonomous: true
requirements: [SELF-02]

must_haves:
  truths:
    - "Convertir un freemium sin teléfono a prueba (admin) sin enviar phone → 409 accionable"
    - "Si el freemium tiene phone o se envía phone en el body, la conversión procede y persiste el phone normalizado"
    - "El alta de lead de prueba desde TrialMemberFormDialog exige teléfono (verificado, ya satisfecho)"
  artifacts:
    - path: "el-templo-api/src/modules/members/service.ts"
      provides: "guard de teléfono en convertFreemiumToTrial (409) + persistencia en la tx"
      contains: "convertFreemiumToTrial"
    - path: "el-templo-api/src/modules/members/schemas.ts"
      provides: "phone opcional en convertToTrialSchema"
      contains: "convertToTrialSchema"
  key_links:
    - from: "members/routes.ts convert-to-trial handler"
      to: "convertFreemiumToTrial"
      via: "pasa request.body.phone al service"
      pattern: "phone: request.body.phone"
---

<objective>
Cerrar el teléfono obligatorio del lado admin: `convertFreemiumToTrial` (promover un self-registered freemium a `status='prueba'`) rechaza con 409 accionable si el lead no tiene teléfono y ninguno viene en el body; si viene, lo normaliza y persiste en la misma tx. `createTrialMember` y `TrialMemberFormDialog.vue` ya exigen teléfono (Pattern Map): se VERIFICAN y se documentan como satisfechos (sin editar salvo que estén rotos).

Purpose: SELF-02 — ninguna alta de SP desde el admin puede quedar sin teléfono del lead.
Output: guard en convertFreemiumToTrial + schema/route, test de integración, verificación del alta directa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-self-service-y-ux-de-gesti-n/165-CONTEXT.md
@.planning/phases/165-self-service-y-ux-de-gesti-n/165-PATTERNS.md
@.planning/phases/163-m-quina-de-estados-autom-tica-del-lead/163-03-SUMMARY.md

<interfaces>
<!-- Contratos que el executor usa directamente. -->

members/service.ts:
- `createTrialMember` (~816-846): YA hace guard de phone con `normalizePhone` + `ConflictError`. Analog a replicar.
- `convertFreemiumToTrial` (~1010-1086): user select ~1014-1022 (agregar `phone: schema.users.phone`), freemium guard ~1027-1031, tx UPDATE ~1057-1079. `normalizePhone` YA importado en este archivo (usado ~819).
- `ConvertFreemiumToTrialServiceInput`: agregar `phone?: string`. Ya recibe `{ branchId, createdBy }`.

members/schemas.ts:
- `createTrialMemberSchema` (~315-331): phone required `{ type:"string", minLength:1, maxLength:30 }` — referencia.
- `convertToTrialSchema` (~342-362): `required: ["branchId"]`, `additionalProperties:false` — agregar `phone` OPCIONAL, NO tocar `required`.

members/routes.ts convert-to-trial handler (790-804): actualmente pasa solo `{ branchId, createdBy }` → agregar `phone: request.body.phone`.

TrialMemberFormDialog.vue (el-templo-admin): phone `q-input` (~23-48) YA tiene `:rules="[requiredRule('Teléfono')]"`, onSubmit (~141-164) trim+send. Backend createTrialMemberSchema YA exige phone. Solo VERIFICAR.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard de teléfono en convertFreemiumToTrial + schema + route (D-02)</name>
  <read_first>
    - el-templo-api/src/modules/members/service.ts (createTrialMember guard 816-846, convertFreemiumToTrial 1010-1086)
    - el-templo-api/src/modules/members/schemas.ts (createTrialMemberSchema 315-331, convertToTrialSchema 342-362)
    - el-templo-api/src/modules/members/routes.ts (convert-to-trial handler 790-804)
    - el-templo-api/src/modules/shared/phone.ts (normalizePhone)
  </read_first>
  <behavior>
    - convert-to-trial de un freemium SIN users.phone y SIN phone en el body → ConflictError 409 con mensaje accionable ("Cargale el teléfono al lead antes de convertirlo a sesión de prueba"). No muta estado.
    - convert-to-trial con phone en el body (o freemium que ya tiene phone) → procede: status→'prueba', leadStatusSource→'auto' (163) y, si vino phone, users.phone queda normalizado (últimos 10 dígitos) escrito en la MISMA tx.
    - El body con phone fuera de rango (>30) es rechazado por AJV antes del handler.
  </behavior>
  <action>
    En members/service.ts: agregar `phone?: string` a `ConvertFreemiumToTrialServiceInput`; sumar `phone: schema.users.phone` al select del user (~1014-1022). Tras el freemium guard, agregar: si `!user.phone && !input.phone?.trim()` → `throw new ConflictError("Cargale el teléfono al lead antes de convertirlo a sesión de prueba")` (espeja el gate de createTrialMember, usa `ConflictError` ya importado). Si `input.phone?.trim()`, calcular `phone = normalizePhone(input.phone)` y agregar `...(phone ? { phone } : {})` al `.set()` de la tx UPDATE existente (~1057-1079) — sin round-trip extra.
    En members/schemas.ts: agregar `phone: { type: "string", minLength: 1, maxLength: 30 }` OPCIONAL a `convertToTrialSchema.properties`; mantener `required: ["branchId"]` y `additionalProperties: false`.
    En members/routes.ts: en el convert-to-trial handler, agregar `phone: request.body.phone` al objeto pasado a `convertFreemiumToTrial`.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm tsc --noEmit</automated>
    Acceptance: `grep -q 'phone' src/modules/members/schemas.ts` en la zona de convertToTrialSchema y `grep -q 'convertToTrialSchema' src/modules/members/schemas.ts`; typecheck limpio.
  </verify>
  <done>convertFreemiumToTrial exige/persiste phone; schema y route lo propagan; tsc verde.</done>
</task>

<task type="auto">
  <name>Task 2: Test de integración convert 409/persistencia + verificación del alta directa (D-02)</name>
  <read_first>
    - el-templo-api/test/convert-freemium-to-trial.test.ts (scaffold y casos existentes)
    - el-templo-api/test/helpers.ts (createEligibleFreemium 500-526, registerUser 81-123)
    - el-templo-admin/src/components/TrialMemberFormDialog.vue (q-input phone 23-48, onSubmit 141-164)
  </read_first>
  <action>
    Extender `convert-freemium-to-trial.test.ts`: (a) freemium sin phone → convert sin `phone` en el body → 409; (b) mismo freemium → convert CON `phone` en el body → 200 y DB-assert `users.phone` persistido normalizado; reusar scaffold/helpers existentes, fecha LOCAL. Solo agregar casos; no tocar los existentes salvo consecuencia directa.
    VERIFICAR (sin editar salvo que falle): que `TrialMemberFormDialog.vue` tiene la regla `requiredRule('Teléfono')` activa en el q-input de teléfono y que `createTrialMemberSchema` exige phone; correr eslint del archivo del admin como gate. Documentar en el SUMMARY que D-02 alta directa está satisfecho por diseño (sin cambio) o, si se encontró roto, describir el fix acotado.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm vitest run test/convert-freemium-to-trial.test.ts && cd ../el-templo-admin && pnpm exec eslint src/components/TrialMemberFormDialog.vue</automated>
  </verify>
  <done>Tests de convert (409 + persistencia) verdes; TrialMemberFormDialog verificado como ya-satisfecho (o fix documentado); eslint del archivo limpio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin → POST /admin/members/:id/convert-to-trial | staff CAJA_ROLES envía `phone` no confiable en el body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-04 | Tampering | `phone` del body de convert-to-trial | mitigate | AJV `minLength:1 maxLength:30` + `additionalProperties:false`; `normalizePhone` antes de persistir; escrito solo en `users.phone` |
| T-165-05 | Elevation of Privilege | convert-to-trial | accept | ruta ya bajo `requireBranchAccess` + rol admin (heredado); el guard de phone es 409 previo, no altera autorización |
| T-165-SC | Tampering | dependencias | mitigate | sin dependencias nuevas |
</threat_model>

<verification>
- `cd el-templo-api && pnpm tsc --noEmit` limpio.
- `pnpm vitest run test/convert-freemium-to-trial.test.ts` verde.
- `cd el-templo-admin && pnpm exec eslint src/components/TrialMemberFormDialog.vue` limpio.
- Sin migraciones nuevas.
</verification>

<success_criteria>
- convert-to-trial sin teléfono → 409 accionable; con teléfono → 200 + persistido.
- Alta directa (TrialMemberFormDialog + createTrialMember) verificada como satisfactoria.
- Tests verdes; tsc/eslint limpios; nada pusheado; staging por ruta explícita.
</success_criteria>

<output>
Create `.planning/phases/165-self-service-y-ux-de-gesti-n/165-02-SUMMARY.md` when done
</output>
