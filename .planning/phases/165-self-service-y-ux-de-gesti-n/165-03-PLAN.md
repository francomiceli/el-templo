---
phase: 165-self-service-y-ux-de-gesti-n
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/reports/service.ts
  - el-templo-api/src/modules/reports/types.ts
  - el-templo-api/src/modules/reports/schemas.ts
  - el-templo-api/test/reports-trial-sessions.test.ts
  - el-templo-admin/src/components/reports/TrialSessionsReport.vue
  - el-templo-admin/src/composables/useReportsApi.ts
autonomous: true
requirements: [SELF-04]

must_haves:
  truths:
    - "Cada fila del reporte de Sesiones de Prueba incluye el teléfono del lead (o vacío para leads legacy sin teléfono)"
    - "El export CSV del reporte incluye la columna Teléfono"
    - "El reporte muestra el teléfono como link wa.me y una acción por fila 'Ver ficha' → /alumnos/:userId"
  artifacts:
    - path: "el-templo-api/src/modules/reports/service.ts"
      provides: "u.phone en el SELECT del page query + celda CSV Teléfono"
      contains: "phone"
    - path: "el-templo-admin/src/components/reports/TrialSessionsReport.vue"
      provides: "columna Teléfono con wa.me + acción Ver ficha"
      contains: "wa.me"
  key_links:
    - from: "reports/service.ts getTrialSessionsReport"
      to: "users.phone"
      via: "u.phone AS phone en el SELECT + mapTrialSessionRow"
      pattern: "phone"
    - from: "TrialSessionsReport.vue fila"
      to: "/alumnos/:userId"
      via: "router-link Ver ficha"
      pattern: "alumnos/"
---

<objective>
Extender el reporte de Sesiones de Prueba con el teléfono del lead (columna + link wa.me + CSV) y una acción por fila "Ver ficha" hacia `/alumnos/:userId`, para acortar el camino de recupero/conversión (SELF-04, D-06/D-07). Sigue exactamente la forma de extensión de columnas de 164 (`reschedules`/`lead_status_source`) en los seis touch-points. No construye pantallas nuevas: `/alumnos/:userId` ya hospeda edición de estado + asignación de plan.

Purpose: SELF-04 — gestión tiene teléfono a mano (insumo del recupero segmentado por Asistió) y un salto directo a la ficha del lead.
Output: columna phone en backend+CSV, tipos/schemas, UI con wa.me y Ver ficha.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-self-service-y-ux-de-gesti-n/165-CONTEXT.md
@.planning/phases/165-self-service-y-ux-de-gesti-n/165-PATTERNS.md
@.planning/phases/164-reprogramaci-n-y-reporte/164-03-SUMMARY.md

<interfaces>
<!-- Forma exacta a espejar: la extensión reschedules/lead_status_source de 164 (mismos seis touch-points). -->

reports/service.ts (getTrialSessionsReport):
- Raw SQL SELECT del page query (~1540-1585): `u` es el alias de users → agregar `u.phone AS phone`.
- Result-type inline ×2 (~1518-1538 y ~1587-1608): agregar `phone: string | null`.
- `mapTrialSessionRow` param type (~1842-1863) y return (~1906-1929): agregar `phone: r.phone` (junto a `reschedules: Number(r.reschedules)` / `leadStatusSource: r.lead_status_source ?? null`).
- `exportTrialSessions` CSV (~1633-1693): agregar `"Teléfono"` a `headers` (~1642-1658) y `row.phone ?? ""` a `cells` (~1671-1687) en la MISMA posición ordinal. Mantener `csvEscape` RFC-4180.
- La count query NO cambia (phone no se filtra ni ordena).

reports/types.ts: `TrialSessionsRow` (~212-263) → agregar `phone: string | null;` junto a reschedules/leadStatusSource (~255-262).
reports/schemas.ts: `trialSessionsRowSchema.properties` (~493-546) → agregar `phone: { type: ["string", "null"] },` (espeja `leadNotes` ~533).

el-templo-admin/src/composables/useReportsApi.ts: `TrialSessionsRowClient` (~51-77) → agregar `phone: string | null;` (junto a reschedules ~74 / leadStatusSource ~76).

TrialSessionsReport.vue:
- `columns` array (~592+): agregar `{ name: 'phone', label: 'Teléfono', field: 'phone', align: 'left' }`.
- La celda de nombre del lead (~128-138) YA es `router-link :to="`/alumnos/${props.row.userId}`"` — analog para "Ver ficha".
- wa.me pattern (de SesionesDePruebaDialog.vue 281-284):
```typescript
function openWhatsapp(phone: string): void {
  const cleaned = phone.replace(/[^0-9]/g, '');
  window.open(`https://wa.me/${cleaned}`, '_blank');
}
```

reports-trial-sessions.test.ts: helper `insertTrialLead`/`seedLead` ya siembra `phone` (~171,198); patrón de assertion de `reschedules` (~920-983).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend — u.phone en el reporte + CSV + tipos/schemas + test (D-06)</name>
  <read_first>
    - el-templo-api/src/modules/reports/service.ts (getTrialSessionsReport SELECT ~1540-1608, mapTrialSessionRow ~1842-1929, exportTrialSessions ~1633-1693)
    - el-templo-api/src/modules/reports/types.ts (TrialSessionsRow ~212-263)
    - el-templo-api/src/modules/reports/schemas.ts (trialSessionsRowSchema ~493-546)
    - el-templo-api/test/reports-trial-sessions.test.ts (seedLead/insertTrialLead ~171-198, assertion reschedules ~920-983)
    - .planning/phases/164-reprogramaci-n-y-reporte/164-03-SUMMARY.md (forma de la extensión de columnas)
  </read_first>
  <behavior>
    - GET /api/admin/reports/trial-sessions → cada fila incluye `phone` (string del lead o null si el user no tiene teléfono).
    - El CSV del reporte tiene la columna "Teléfono" en su posición, escapada RFC-4180.
    - No cambia el conteo ni el filtrado (phone es display+CSV).
  </behavior>
  <action>
    En reports/service.ts: agregar `u.phone AS phone` al SELECT del page query; agregar `phone: string | null` a los DOS result-types inline y al param type de `mapTrialSessionRow`; agregar `phone: r.phone` al return de `mapTrialSessionRow`; agregar `"Teléfono"` a los `headers` del CSV y `row.phone ?? ""` a los `cells` en la misma posición ordinal. NO tocar la count query.
    En reports/types.ts: agregar `phone: string | null;` a `TrialSessionsRow` con doc comment (junto a reschedules/leadStatusSource).
    En reports/schemas.ts: agregar `phone: { type: ["string", "null"] },` a `trialSessionsRowSchema.properties`.
    En el test: agregar aserción de que la fila del reporte trae `phone` (sembrar un lead con phone conocido y verificar el valor; opcional un lead sin phone → null). Reusar el helper de seed existente; fecha LOCAL.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm tsc --noEmit && pnpm vitest run test/reports-trial-sessions.test.ts</automated>
  </verify>
  <done>El reporte (JSON + CSV) trae phone; tipos/schemas actualizados; test verde; tsc limpio.</done>
</task>

<task type="auto">
  <name>Task 2: Admin UI — columna Teléfono con wa.me + acción "Ver ficha" (D-06/D-07)</name>
  <read_first>
    - el-templo-admin/src/components/reports/TrialSessionsReport.vue (columns ~592+, celda lead router-link ~128-138, slots #body-cell-*)
    - el-templo-admin/src/components/scheduling/SesionesDePruebaDialog.vue (openWhatsapp 281-284)
    - el-templo-admin/src/composables/useReportsApi.ts (TrialSessionsRowClient 51-77)
  </read_first>
  <action>
    En useReportsApi.ts: agregar `phone: string | null;` a `TrialSessionsRowClient` (junto a reschedules/leadStatusSource).
    En TrialSessionsReport.vue: agregar la columna `{ name: 'phone', label: 'Teléfono', field: 'phone', align: 'left' }` al array `columns`; agregar un slot `#body-cell-phone` que, cuando `props.row.phone`, renderiza un link wa.me (patrón `openWhatsapp`/`replace(/[^0-9]/g,'')` → `https://wa.me/${cleaned}`), y cuando no hay teléfono muestra `—` (celda vacía sin link, para leads legacy). Agregar la acción "Ver ficha" por fila (slot `#body-cell-acciones` o reusar la celda de lead) como `q-btn`/icono con `:to="`/alumnos/${props.row.userId}`"` etiquetado "Ver ficha". Sin pantallas nuevas. Strings en español.
  </action>
  <verify>
    <automated>cd el-templo-admin && pnpm exec eslint src/components/reports/TrialSessionsReport.vue src/composables/useReportsApi.ts</automated>
    Acceptance: `grep -q 'wa.me' src/components/reports/TrialSessionsReport.vue && grep -q "alumnos/" src/components/reports/TrialSessionsReport.vue`.
  </verify>
  <done>El reporte del admin muestra Teléfono con wa.me (o vacío) y una acción Ver ficha → /alumnos/:userId; eslint limpio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin report viewer → GET /reports/trial-sessions | staff con report-scope ve teléfonos de leads (dato personal) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-06 | Information Disclosure | teléfono en el reporte + CSV | accept | el teléfono ya es visible en otras vistas admin (ficha del lead, SesionesDePruebaDialog) bajo CAJA_ROLES/country-scope; el reporte hereda ese scope, sin cambio de política de acceso |
| T-165-07 | Injection | valor phone en link wa.me / CSV | mitigate | wa.me limpia con `replace(/[^0-9]/g,'')`; CSV usa `csvEscape` RFC-4180 existente |
| T-165-SC | Tampering | dependencias | mitigate | sin dependencias nuevas |
</threat_model>

<verification>
- `cd el-templo-api && pnpm tsc --noEmit` limpio y `pnpm vitest run test/reports-trial-sessions.test.ts` verde.
- `cd el-templo-admin && pnpm exec eslint src/components/reports/TrialSessionsReport.vue src/composables/useReportsApi.ts` limpio.
- Sin migraciones nuevas.
</verification>

<success_criteria>
- Reporte de SP: columna Teléfono (wa.me o vacío) + CSV + acción Ver ficha.
- Tests verdes; tsc/eslint limpios; nada pusheado; staging por ruta explícita.
</success_criteria>

<output>
Create `.planning/phases/165-self-service-y-ux-de-gesti-n/165-03-SUMMARY.md` when done
</output>
