---
phase: 165-self-service-y-ux-de-gesti-n
plan: 05
type: execute
wave: 2
depends_on: [165-01, 165-03]
files_modified:
  - el-templo-api/test/self-service-trial-e2e.test.ts
autonomous: true
requirements: [SELF-01]

must_haves:
  truths:
    - "Un test E2E recorre el funnel completo: POST /register (freemium) → GET trial-eligibility (elegible) → POST reserve-trial (promueve a prueba, booking is_trial source self_service, lead en_seguimiento source auto) → el lead aparece en GET /api/admin/reports/trial-sessions"
    - "Los negativos clave están cubiertos: sub activa NO elegible; segunda prueba NO permitida (una por vida); cancelación self-service revierte prueba→freemium; sin teléfono → 400 PHONE_REQUIRED; con teléfono → 201 + persistido"
    - "Todo lo que el recorrido encuentre roto en el funnel queda CORREGIDO en esta fase (deviation autorizada y documentada)"
  artifacts:
    - path: "el-templo-api/test/self-service-trial-e2e.test.ts"
      provides: "test de regresión del milestone (funnel 119 + writes 163 + phone 165)"
      contains: "reserve-trial"
  key_links:
    - from: "self-service-trial-e2e.test.ts"
      to: "POST /register → reserve-trial → reports/trial-sessions"
      via: "un único walk de integración con DB-asserts multi-tabla"
      pattern: "trial-sessions"
---

<objective>
Escribir el test de integración E2E que es el corazón de la fase (SELF-01 / D-01) y también el test de regresión del milestone: valida que el funnel self-service de Phase 119, los writes de la máquina de estados de Phase 163 (reset/source) y el teléfono obligatorio de Phase 165 conviven. El plan tiene PRESUPUESTO EXPLÍCITO para CORREGIR lo que el recorrido encuentre roto (deviation rule permitida y documentada) — ese es el punto de SELF-01.

Depende de Plan 01 (`phoneRequired` en eligibility + `PHONE_REQUIRED`) y Plan 03 (`phone` en el reporte), por eso corre en Wave 2.

Purpose: SELF-01 — el flujo self-service queda verificado punta a punta por test y corregido donde falle.
Output: `test/self-service-trial-e2e.test.ts` verde + cualquier fix del funnel documentado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-self-service-y-ux-de-gesti-n/165-CONTEXT.md
@.planning/phases/165-self-service-y-ux-de-gesti-n/165-PATTERNS.md
@.planning/phases/163-m-quina-de-estados-autom-tica-del-lead/163-03-SUMMARY.md
@.planning/phases/165-self-service-y-ux-de-gesti-n/165-01-PLAN.md
@.planning/phases/165-self-service-y-ux-de-gesti-n/165-03-PLAN.md

<interfaces>
<!-- Fuentes a coser en un único funnel walk (copiar scaffolds verbatim, NO reinventar). -->

Scaffolds a copiar:
- el-templo-api/test/scheduling-reserve-trial.test.ts: beforeAll/beforeEach (43-95) — fake timers a un miércoles, createTestApp(), admin token via getAuthToken(app,"admin@test.com","adminpass123"), resolver branch ids TEST/ONLINE, recrear activity+schedule por test. Bloque de asserts multi-tabla del reserve (136-185). Sub activa → no elegible (323-357). Segunda prueba → 409 (203-218).
- el-templo-api/test/scheduling-trial-eligibility.test.ts: estilo de assert (107-114) — incluir el nuevo `phoneRequired`.
- el-templo-api/test/scheduling-cancel-trial.test.ts: patrón de cancelación self-service revierte prueba→freemium.
- el-templo-api/test/reports-trial-sessions.test.ts: setup del reporte (255-297), llamada + find de fila (314) — incluir el nuevo `phone`.
- el-templo-api/test/helpers.ts: registerUser (81-123, genera dni/phone únicos); createEligibleFreemium (500-526, inserta con createdAt viejo — usar SOLO para el bypass de frescura de 3 días en un negativo); dateOffsetStr (37-41); createStaffUser (para el token admin/owner del reporte).

Puntos de assert del happy path tras reserve-trial (DB): `users.status='prueba'`, `users.leadStatus='en_seguimiento'`, `users.leadStatusSource='auto'` (163), `userStatusHistory.source='self_service'`, `bookings.isTrial=1 source='self_service'`.

Regla de fecha: helper LOCAL (CURDATE es ART) — preferir el helper local de 163/164 (ver test/expire-lost-leads.test.ts) sobre offsets UTC.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir el E2E funnel (register → eligibility → reserve → reporte) + negativos</name>
  <read_first>
    - el-templo-api/test/scheduling-reserve-trial.test.ts (scaffold 43-95, asserts 136-185, sub activa 323-357, segunda prueba 203-218)
    - el-templo-api/test/scheduling-trial-eligibility.test.ts (assert style 107-114)
    - el-templo-api/test/scheduling-cancel-trial.test.ts (cancel revierte prueba→freemium)
    - el-templo-api/test/reports-trial-sessions.test.ts (setup 255-297, row find 314)
    - el-templo-api/test/helpers.ts (registerUser 81-123, createEligibleFreemium 500-526, dateOffsetStr 37-41, createStaffUser)
    - el-templo-api/test/expire-lost-leads.test.ts (helper de fecha LOCAL)
  </read_first>
  <action>
    Crear `test/self-service-trial-e2e.test.ts` cosiendo los scaffolds anteriores en un único walk:
    (1) Happy path: `POST /register` → status freemium; `GET /api/members/scheduling/trial-eligibility` → `eligible:true, alreadyBooked:false, phoneRequired:<según perfil>`; `POST /api/members/scheduling/reserve-trial` → 201 + DB-asserts multi-tabla (status prueba, leadStatus en_seguimiento, leadStatusSource auto, userStatusHistory.source self_service, bookings.isTrial=1 source self_service); `GET /api/admin/reports/trial-sessions` (token owner/admin via createStaffUser) → el lead aparece y su fila trae `phone`.
    (2) Negativos: sub activa → no elegible; segunda prueba → 409 (una por vida); cancel self-service → revierte prueba→freemium; phone negativo → freemium sin phone reserva sin `phone` en el body → 400 `code:"PHONE_REQUIRED"`, y con `phone` → 201 + `users.phone` persistido.
    Usar helper de fecha LOCAL (no offset UTC). Escribir el test de forma que, si el funnel real está roto, FALLE de forma diagnóstica (mensajes claros por paso). NO relajar aserciones para "pasar": si algo falla, se corrige en Task 2.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm exec eslint test/self-service-trial-e2e.test.ts && pnpm tsc --noEmit</automated>
    Acceptance: el archivo existe, compila y lintea (aún puede fallar en runtime — eso lo resuelve Task 2).
  </verify>
  <done>test/self-service-trial-e2e.test.ts escrito con happy path + 5 negativos; compila y lintea.</done>
</task>

<task type="auto">
  <name>Task 2: Correr el E2E y corregir lo que el funnel encuentre roto (deviation autorizada)</name>
  <read_first>
    - el-templo-api/src/modules/scheduling/trials-service.ts (reserveTrialSelfService, cancelTrialSelfService ~498, getTrialEligibility)
    - el-templo-api/src/modules/reports/service.ts (getTrialSessionsReport)
    - el-templo-api/src/modules/auth/routes.ts (POST /register ~47, status freemium ~194)
  </read_first>
  <action>
    Correr `pnpm vitest run test/self-service-trial-e2e.test.ts` y llevarlo a verde. Este plan tiene PRESUPUESTO EXPLÍCITO para corregir el funnel: si el recorrido revela un bug real (promoción no atómica, source/leadStatus mal seteados, el lead no aparece en el reporte, cancelación que no revierte, elegibilidad incorrecta, phone no persistido, etc.), CORREGIRLO en el archivo fuente correspondiente (deviation rule permitida). Documentar cada fix en el SUMMARY (síntoma → causa → archivo → verificación), distinguiendo bug-en-producto vs. ajuste-de-test. Si el fallo es del test (aserción o fecha mal escrita), arreglar el test. NO relajar una aserción para ocultar un bug de producto.
    Alcance del fix: acotado al funnel self-service (register/eligibility/reserve/cancel/reporte). Si aparece un bug fuera de ese alcance, registrarlo en `.planning` (deferred) y notar en el SUMMARY en vez de expandir el scope.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm vitest run test/self-service-trial-e2e.test.ts && pnpm tsc --noEmit</automated>
  </verify>
  <done>El E2E pasa verde; cualquier fix del funnel queda documentado en el SUMMARY (bug de producto vs. ajuste de test); tsc limpio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → API real | test de integración contra eltemplo_test; sin superficie de ataque nueva |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-09 | Tampering | fixes del funnel (deviation) | mitigate | cada fix acotado al funnel self-service, documentado en el SUMMARY, cubierto por el propio E2E; sin relajar aserciones para ocultar bugs |
| T-165-SC | Tampering | dependencias | mitigate | sin dependencias nuevas; reusa helpers de test existentes |
</threat_model>

<verification>
- `cd el-templo-api && pnpm vitest run test/self-service-trial-e2e.test.ts` verde.
- `pnpm tsc --noEmit` limpio.
- Si Task 2 tocó código fuente, correr también los tests del módulo afectado (p.ej. `pnpm vitest run test/scheduling-reserve-trial.test.ts`).
- Sin migraciones nuevas.
</verification>

<success_criteria>
- El funnel self-service queda verificado punta a punta por un E2E verde (happy path + 5 negativos).
- Cualquier ruptura del funnel corregida y documentada.
- tsc limpio; nada pusheado; staging por ruta explícita. UAT visual queda como human verification del milestone.
</success_criteria>

<output>
Create `.planning/phases/165-self-service-y-ux-de-gesti-n/165-05-SUMMARY.md` when done
</output>
