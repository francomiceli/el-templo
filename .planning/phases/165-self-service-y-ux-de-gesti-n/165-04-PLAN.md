---
phase: 165-self-service-y-ux-de-gesti-n
plan: 04
type: execute
wave: 2
depends_on: [165-01]
files_modified:
  - el-templo-app/src/composables/useSchedulingApi.ts
  - el-templo-app/src/pages/ReservasPage.vue
autonomous: true
requirements: [SELF-03]

must_haves:
  truths:
    - "Cuando eligibility.phoneRequired es true, el diálogo de confirmación de reserva de prueba pide el teléfono (input tel, no vacío)"
    - "El teléfono ingresado viaja en el body del reserve-trial y se persiste (backend Plan 01)"
    - "El registro (signup) NO cambia — el teléfono se pide recién al reservar la prueba"
  artifacts:
    - path: "el-templo-app/src/composables/useSchedulingApi.ts"
      provides: "TrialEligibility.phoneRequired + reserveTrial con phone opcional"
      contains: "phoneRequired"
    - path: "el-templo-app/src/pages/ReservasPage.vue"
      provides: "q-input de teléfono condicional en el diálogo de reserva de prueba"
      contains: "phoneRequired"
  key_links:
    - from: "ReservasPage.vue confirmTrialReserve"
      to: "useSchedulingApi.reserveTrial"
      via: "pasa trialDialog.phone.trim() || undefined como 4º arg"
      pattern: "reserveTrial"
---

<objective>
Cerrar SELF-03 del lado de la member app: cuando la eligibility devuelve `phoneRequired`, el diálogo de confirmación de reserva de sesión de prueba en `ReservasPage.vue` muestra un input de teléfono requerido (teclado tel, validación no-vacío) que viaja en el body del reserve-trial. El registro NO cambia (cero fricción extra en signup). Depende del backend del Plan 01 (`phoneRequired` en eligibility + `phone` opcional en reserve-trial + code `PHONE_REQUIRED`).

Purpose: SELF-03 — el freemium sin teléfono lo carga en el momento de reservar, no antes.
Output: composable con phoneRequired + reserveTrial(phone), diálogo con input condicional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-self-service-y-ux-de-gesti-n/165-CONTEXT.md
@.planning/phases/165-self-service-y-ux-de-gesti-n/165-PATTERNS.md
@.planning/phases/165-self-service-y-ux-de-gesti-n/165-01-PLAN.md

<interfaces>
<!-- Contratos producidos por Plan 01 (backend) que este plan consume. -->

Backend (Plan 01): GET /members/scheduling/trial-eligibility ahora retorna `phoneRequired: boolean`; POST /members/scheduling/reserve-trial acepta `phone?: string` opcional en el body; sin phone y sin perfil → 400 `{ code: "PHONE_REQUIRED" }`.

el-templo-app/src/composables/useSchedulingApi.ts:
- `TrialEligibility` interface (~39-52): agregar `phoneRequired: boolean` (tras `alreadyBooked`).
- `reserveTrial` (~110-121): agregar param `phone?: string` e incluirlo en el body solo si viene:
```typescript
async function reserveTrial(scheduleId: number, date: string, branchId: number, phone?: string) {
  const response = await api.post<BookingRecord>(
    '/members/scheduling/reserve-trial',
    { scheduleId, date, branchId, ...(phone ? { phone } : {}) },
    { signal: getSignal() },
  )
  return response.data
}
```

el-templo-app/src/pages/ReservasPage.vue:
- Diálogo de confirmación de prueba (markup ~636-655): hoy solo muestra `trialDialog.message` + Confirmar/Cancelar.
- `trialEligibility` ref (~879); `trialDialog` state (~915-921); `confirmTrialReserve` (~1495-1514) con catch `extractError` (~1504-1510).
- No hay un q-input de teléfono con reglas en este archivo para espejar → usar regla inline `:rules="[(v) => !!v?.trim() || 'Ingresá tu teléfono']"`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useSchedulingApi — phoneRequired + reserveTrial(phone) (D-04/D-05)</name>
  <read_first>
    - el-templo-app/src/composables/useSchedulingApi.ts (TrialEligibility 39-52, getTrialEligibility 103-109, reserveTrial 110-121)
  </read_first>
  <action>
    Agregar `phoneRequired: boolean` a la interface `TrialEligibility` (tras `alreadyBooked`). Modificar `reserveTrial` para aceptar un 4º parámetro `phone?: string` e incluirlo en el body del POST solo cuando esté presente (`...(phone ? { phone } : {})`). No tocar `getTrialEligibility` (el tipo ampliado ya expone phoneRequired).
  </action>
  <verify>
    <automated>cd el-templo-app && pnpm exec eslint src/composables/useSchedulingApi.ts</automated>
    Acceptance: `grep -q 'phoneRequired' src/composables/useSchedulingApi.ts`.
  </verify>
  <done>TrialEligibility expone phoneRequired; reserveTrial acepta y envía phone opcional; eslint limpio.</done>
</task>

<task type="auto">
  <name>Task 2: ReservasPage — input de teléfono condicional en el diálogo de reserva (D-05)</name>
  <read_first>
    - el-templo-app/src/pages/ReservasPage.vue (diálogo prueba 636-655, trialEligibility ref 879, trialDialog state 915-921, confirmTrialReserve 1495-1514)
  </read_first>
  <action>
    En el diálogo de confirmación de reserva de prueba (636-655): agregar un `q-input` de teléfono que se muestre SOLO cuando `trialEligibility?.phoneRequired` sea true, con `type="tel"`, `inputmode="tel"`, label en español (p.ej. "Teléfono") y regla no-vacío `:rules="[(v) => !!v?.trim() || 'Ingresá tu teléfono']"`. Agregar `phone: ''` al state `trialDialog` (915-921) y bindear `v-model="trialDialog.phone"`. En `confirmTrialReserve` (~1499) pasar `trialDialog.value.phone.trim() || undefined` como 4º arg a `reserveTrial`. Mantener el catch `extractError` existente (ya surfacea el mensaje del backend, incluido PHONE_REQUIRED). El registro/signup NO se toca.
    Si el diálogo requiere que el botón Confirmar respete la validación del input, deshabilitarlo/validar cuando phoneRequired y el teléfono esté vacío (seguir el patrón de validación del diálogo si existe; si no, guard simple en confirmTrialReserve).
  </action>
  <verify>
    <automated>cd el-templo-app && pnpm exec eslint src/pages/ReservasPage.vue</automated>
    Acceptance: `grep -q 'phoneRequired' src/pages/ReservasPage.vue` y `grep -q 'trialDialog' src/pages/ReservasPage.vue` con el phone bindeado.
  </verify>
  <done>El diálogo pide teléfono cuando phoneRequired y lo envía en el reserve; signup intacto; eslint limpio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| member app UI → reserve-trial | el usuario ingresa su propio teléfono en el diálogo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-08 | Tampering | teléfono ingresado en el diálogo | mitigate | validación no-vacío en el front; la validación/normalización real es server-side (Plan 01 — AJV + normalizePhone). El front no es la frontera de confianza |
| T-165-SC | Tampering | dependencias | mitigate | sin dependencias nuevas; solo usa componentes Quasar existentes |
</threat_model>

<verification>
- `cd el-templo-app && pnpm exec eslint src/composables/useSchedulingApi.ts src/pages/ReservasPage.vue` limpio.
- Nota: si eslint falla por deps faltantes en el worktree, correr `pnpm install --frozen-lockfile` en el-templo-app (NO agregar deps nuevas).
- Verificación visual (UAT app real en staging) queda como human verification del milestone — fuera de este plan.
</verification>

<success_criteria>
- El diálogo de reserva de prueba pide teléfono cuando el perfil no lo tiene y lo envía en el body.
- El registro no cambia. eslint limpio; nada pusheado; staging por ruta explícita.
</success_criteria>

<output>
Create `.planning/phases/165-self-service-y-ux-de-gesti-n/165-04-SUMMARY.md` when done
</output>
