---
phase: 165-self-service-y-ux-de-gesti-n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/modules/shared/errors.ts
  - el-templo-api/src/modules/scheduling/trials-service.ts
  - el-templo-api/src/modules/scheduling/schemas.ts
  - el-templo-api/src/modules/scheduling/routes.ts
  - el-templo-api/test/scheduling-reserve-trial.test.ts
  - el-templo-api/test/scheduling-trial-eligibility.test.ts
autonomous: true
requirements: [SELF-02, SELF-03]

must_haves:
  truths:
    - "Un freemium sin teléfono que reserva prueba sin enviar phone → 400 code PHONE_REQUIRED"
    - "Si envía phone, se persiste en users.phone (normalizado laxo) dentro de la misma tx y la prueba se crea (201)"
    - "GET trial-eligibility expone phoneRequired:boolean (true si el perfil no tiene teléfono)"
    - "bookTrial (alta admin de SP a lead existente) sin teléfono → 409 con mensaje accionable"
  artifacts:
    - path: "el-templo-api/src/modules/shared/errors.ts"
      provides: "PhoneRequiredError (code PHONE_REQUIRED, extends BadRequestError → 400)"
      contains: "PHONE_REQUIRED"
    - path: "el-templo-api/src/modules/scheduling/trials-service.ts"
      provides: "phone opcional en reserveTrialSelfService + phoneRequired en getTrialEligibility + guard 409 en bookTrial"
      contains: "phoneRequired"
    - path: "el-templo-api/src/modules/scheduling/routes.ts"
      provides: "surface explícito de PhoneRequiredError → { code: PHONE_REQUIRED }"
      contains: "PhoneRequiredError"
  key_links:
    - from: "el-templo-api/src/modules/scheduling/routes.ts"
      to: "PhoneRequiredError"
      via: "instanceof branch antes de handleServiceError"
      pattern: "instanceof PhoneRequiredError"
    - from: "trials-service reserveTrialSelfService"
      to: "users.phone"
      via: "write dentro de la tx que flipea status a prueba"
      pattern: "phone"
---

<objective>
Cablear el teléfono obligatorio en el backend de scheduling: la reserva self-service exige teléfono (nuevo error tipado `PHONE_REQUIRED`, captura opcional en el body que se persiste atómicamente), la eligibility informa `phoneRequired` para que la app lo pida de antemano, y el alta admin de SP a un lead existente (`bookTrial`) rechaza con 409 accionable si el lead no tiene teléfono.

Purpose: Ningún alta/reserva de sesión de prueba puede quedar sin teléfono del lead (insumo del recupero segmentado). Fundación para el diálogo de la app (Plan 04) y el E2E (Plan 05).
Output: `PhoneRequiredError`, `reserveTrialSelfService` con phone, `getTrialEligibility.phoneRequired`, `bookTrial` 409, schemas/route actualizados, tests verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-self-service-y-ux-de-gesti-n/165-CONTEXT.md
@.planning/phases/165-self-service-y-ux-de-gesti-n/165-PATTERNS.md
@.planning/phases/163-m-quina-de-estados-autom-tica-del-lead/163-03-SUMMARY.md

<interfaces>
<!-- Contratos que el executor usa directamente — no explorar el codebase para esto. -->

Analog error a copiar — el-templo-api/src/modules/shared/errors.ts (66-74):
```typescript
export class PassRequiredError extends BadRequestError {
  readonly code = "PASS_REQUIRED";
  constructor(message = "Necesitás el pase de actividades para reservar esta clase") {
    super(message);
  }
}
```
Nota base: AppError.code NO se auto-serializa (errors.ts:8-23) — la ruta debe echar `code` explícitamente.

trials-service.ts (interfaces ~70-93):
- `ReserveTrialSelfServiceInput` (~70-74): { userId, scheduleId, bookingDate/date, branchId } → agregar `phone?: string`.
- `TrialEligibility` (~80-93): { eligible, alreadyBooked, booking? } → agregar `phoneRequired: boolean`.
- `reserveTrialSelfService` (~202): el user select (~214-222) y la tx UPDATE users (~306-316) que flipea status→'prueba', leadStatus→'en_seguimiento', leadStatusSource→'auto'.
- `getTrialEligibility` (~385): user select (~386-393); retornos de eligible/no-eligible (~472-478).
- `bookTrial` (~599): userRow select (~618-625), status guard `ConflictError` (~627-631), tx a partir de ~672.

Normalización laxo (D-04, NO validador estricto) — el-templo-api/src/modules/shared/phone.ts:
```typescript
export function normalizePhone(input: string): string { return input.replace(/\D/g, "").slice(-10); }
```

Route surface analog — scheduling/routes.ts (~874-880):
```typescript
if (err instanceof PassRequiredError) {
  return reply.code(400).send({ error: "Solicitud invalida", message: err.message, code: "PASS_REQUIRED" });
}
```
`/reserve-trial` handler ~889-905; imports de `../shared/errors` ~35-36.

Schemas — scheduling/schemas.ts: `reserveTrialSchema.body` (~864-887, `additionalProperties:false`), `trialEligibilitySchema.response.200` (~897-920).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PhoneRequiredError + reserveTrialSelfService acepta/persiste phone + schema + surface de ruta (D-04)</name>
  <read_first>
    - el-templo-api/src/modules/shared/errors.ts (clases 49-83, patrón PassRequiredError)
    - el-templo-api/src/modules/scheduling/trials-service.ts (ReserveTrialSelfServiceInput ~70, reserveTrialSelfService ~202-360: select de user, guards de status/branch, tx UPDATE users)
    - el-templo-api/src/modules/shared/phone.ts (normalizePhone)
    - el-templo-api/src/modules/scheduling/schemas.ts (reserveTrialSchema ~864-887)
    - el-templo-api/src/modules/scheduling/routes.ts (surface PassRequiredError ~857-882, handler /reserve-trial ~889-905)
  </read_first>
  <behavior>
    - reserve-trial de un freemium SIN users.phone y SIN phone en el body → PhoneRequiredError → HTTP 400 con `code: "PHONE_REQUIRED"`.
    - reserve-trial con phone en el body → 201; users.phone queda con el valor normalizado (últimos 10 dígitos) escrito en la MISMA tx que flipea status→'prueba'.
    - reserve-trial de un freemium que YA tiene users.phone → 201 sin exigir phone (el body phone se ignora o pisa laxo — no re-validar formato).
    - El body con `phone` de longitud fuera de rango (vacío / >30) es rechazado por el schema AJV (400) antes del handler.
  </behavior>
  <action>
    En errors.ts: agregar `PhoneRequiredError extends BadRequestError` con `readonly code = "PHONE_REQUIRED"` y mensaje default en español accionable (p.ej. "Necesitamos tu teléfono para reservar la sesión de prueba"), copiando verbatim la forma de `PassRequiredError`.
    En trials-service.ts: agregar `phone?: string` a `ReserveTrialSelfServiceInput`; importar `PhoneRequiredError` desde `../shared/errors` y `normalizePhone` desde `../shared/phone`. Sumar `phone: schema.users.phone` al select del user. ANTES de la tx: si `!user.phone && !input.phone?.trim()` → `throw new PhoneRequiredError()`. Calcular `phoneToPersist = input.phone?.trim() ? normalizePhone(input.phone) : null` (solo cuando el user no tenía phone). Dentro del `tx.update(schema.users).set({...})` existente (junto a status/leadStatus/leadStatusSource/createdBy/branchId) agregar el spread `...(phoneToPersist ? { phone: phoneToPersist } : {})` — SIN query extra ni segundo round-trip (patrón atomic write inside tx, 163-03).
    En scheduling/schemas.ts: agregar a `reserveTrialSchema.body.properties` un `phone: { type: "string", minLength: 1, maxLength: 30 }` OPCIONAL (no en `required`); mantener `additionalProperties: false`. `maxLength: 30` espeja `createTrialMemberSchema.phone`.
    En scheduling/routes.ts: importar `PhoneRequiredError`; agregar al generic `Body` del handler `/reserve-trial` el `phone?: string`; agregar una rama `if (err instanceof PhoneRequiredError)` que responde 400 con `{ error: "Solicitud invalida", message: err.message, code: "PHONE_REQUIRED" }`, ubicada ANTES de `handleServiceError`, copiando la rama de `PassRequiredError`.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm tsc --noEmit</automated>
    Acceptance: `grep -q 'PHONE_REQUIRED' src/modules/shared/errors.ts && grep -q 'PhoneRequiredError' src/modules/scheduling/routes.ts` (ambos presentes); typecheck limpio.
  </verify>
  <done>PhoneRequiredError existe; reserveTrialSelfService exige/persiste phone atómicamente; schema acepta phone opcional; la ruta echa code PHONE_REQUIRED; tsc verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: getTrialEligibility.phoneRequired + bookTrial 409 sin teléfono (D-04/D-03)</name>
  <read_first>
    - el-templo-api/src/modules/scheduling/trials-service.ts (TrialEligibility ~80-93, getTrialEligibility ~385-478, bookTrial ~599-680 incl. userRow select y status guard)
    - el-templo-api/src/modules/scheduling/schemas.ts (trialEligibilitySchema.response.200 ~897-920)
  </read_first>
  <behavior>
    - getTrialEligibility de un user sin phone → objeto retornado incluye `phoneRequired: true`; con phone → `phoneRequired: false`. El campo aparece en TODOS los caminos de retorno (elegible y no elegible).
    - bookTrial (admin agenda SP a lead existente) cuyo lead NO tiene users.phone → ConflictError 409 con mensaje que dice qué hacer (ir a la ficha y cargar el teléfono). La reserva NO se crea.
    - bookTrial de un lead CON phone → comportamiento intacto (sin regresión del guard una-prueba-por-vida ni del reset 163).
  </behavior>
  <action>
    En getTrialEligibility: agregar `phoneRequired: boolean` a la interface `TrialEligibility`; sumar `phone: schema.users.phone` al select del user; agregar `phoneRequired: !user.phone` a CADA objeto de retorno (elegible, no-freemium, ya-reservado, etc.). En scheduling/schemas.ts: agregar `phoneRequired: { type: "boolean" }` a `trialEligibilitySchema.response.200.properties` y a su `required` junto a `eligible`/`alreadyBooked`.
    En bookTrial: sumar `phone: schema.users.phone` al `userRow` select; tras los guards de status/branch existentes y ANTES de abrir la tx, agregar: si `!userRow.phone` → `throw new ConflictError("Cargale el teléfono al lead en su ficha antes de agendar la sesión de prueba")`. Usar `ConflictError` (409), NO `PhoneRequiredError` — el admin trata 409 como error de cliente esperado (extract-error, sin ruido Sentry). La reprogramación (164) queda exenta: no pasa por bookTrial.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm tsc --noEmit</automated>
    Acceptance: `grep -c 'phoneRequired' src/modules/scheduling/trials-service.ts` ≥ 3 (interface + al menos 2 retornos); typecheck limpio.
  </verify>
  <done>Eligibility expone phoneRequired en todos los retornos + schema; bookTrial lanza 409 accionable sin teléfono; tsc verde.</done>
</task>

<task type="auto">
  <name>Task 3: Tests de integración — PHONE_REQUIRED, persistencia, phoneRequired, bookTrial 409</name>
  <read_first>
    - el-templo-api/test/scheduling-reserve-trial.test.ts (beforeAll/beforeEach 43-95, seeding de sub activa 323-357, segunda prueba 203-218)
    - el-templo-api/test/scheduling-trial-eligibility.test.ts (assertion style 107-114)
    - el-templo-api/test/helpers.ts (registerUser 81-123, createEligibleFreemium 500-526, dateOffsetStr 37-41)
    - el-templo-api/test/expire-lost-leads.test.ts (referencia de helper de fecha LOCAL — CURDATE es ART)
  </read_first>
  <action>
    Extender `scheduling-reserve-trial.test.ts`: (a) freemium sin phone, reserve sin `phone` en el body → 400 con `body.code === "PHONE_REQUIRED"`; (b) mismo freemium, reserve CON `phone` → 201 y DB-assert que `users.phone` quedó persistido (normalizado). Reusar el scaffold/beforeEach existente; fecha LOCAL (no el offset UTC si el helper local existe).
    Extender `scheduling-trial-eligibility.test.ts`: assert que la respuesta incluye `phoneRequired` (true para user sin phone, false tras persistir/cuando tiene phone).
    Agregar cobertura de bookTrial 409 sin teléfono: si ya existe un test de bookTrial reusar su scaffold; si no, un caso mínimo (lead `status='prueba'` sin phone → bookTrial → 409). Solo agregar/ajustar tests; no tocar otros casos salvo consecuencia directa.
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm vitest run test/scheduling-reserve-trial.test.ts test/scheduling-trial-eligibility.test.ts</automated>
  </verify>
  <done>Los tests nuevos (PHONE_REQUIRED negativo, persistencia, phoneRequired, bookTrial 409) pasan verdes contra eltemplo_test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| member app → POST /reserve-trial | usuario autenticado envía `phone` no confiable en el body |
| admin → bookTrial | staff con CAJA_ROLES agenda SP; el lead objetivo puede no tener phone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-01 | Tampering | `phone` del body de reserve-trial | mitigate | AJV `minLength:1 maxLength:30` + `additionalProperties:false`; `normalizePhone` (solo dígitos) antes de persistir; el valor SOLO se escribe en `users.phone`, nunca se usa para otra decisión |
| T-165-02 | Information Disclosure | respuesta PHONE_REQUIRED / eligibility | mitigate | el error/`phoneRequired` no incluye datos de otros users; scope de user propio (token) |
| T-165-03 | Elevation of Privilege | bookTrial (admin) | accept | ruta admin ya bajo guard CAJA_ROLES + country-scope (heredado); el guard de phone es 409 previo a la tx, no cambia autorización |
| T-165-SC | Tampering | dependencias | mitigate | sin dependencias nuevas (normalizePhone/errors ya existen) |
</threat_model>

<verification>
- `cd el-templo-api && pnpm tsc --noEmit` limpio.
- `pnpm vitest run test/scheduling-reserve-trial.test.ts test/scheduling-trial-eligibility.test.ts` verde.
- Sin migraciones nuevas (phone ya existe en users; 0182/0183 aplicadas por 163).
</verification>

<success_criteria>
- reserve-trial sin teléfono → 400 code PHONE_REQUIRED; con teléfono → 201 + users.phone persistido.
- eligibility expone phoneRequired en todos los retornos.
- bookTrial sin teléfono → 409 accionable.
- Tests de integración verdes; tsc limpio; nada pusheado; staging por ruta explícita.
</success_criteria>

<output>
Create `.planning/phases/165-self-service-y-ux-de-gesti-n/165-01-SUMMARY.md` when done
</output>
