---
phase: 119-campa-a-de-sesi-n-de-prueba-freemium-reserva-self-service-si
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/package.json
  - el-templo-api/src/modules/email/service.ts
  - el-templo-api/src/modules/campaigns/templates.ts
  - el-templo-api/src/modules/campaigns/types.ts
  - el-templo-api/.env.example
  - el-templo-web/public/email/.gitkeep
autonomous: false
requirements: [D-12, D-13, D-14, D-16, D-23, D-27, D-17]
user_setup:
  - service: resend
    why: "Campaign email sending in production (D-17)"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard → API keys (set in prod .env)"
      - name: CAMPAIGN_EMAIL_FROM
        source: "Verified subdomain sender, e.g. El Templo <hola@send.eltemplo.org>"
    dashboard_config:
      - task: "Verify sending domain/subdomain send.eltemplo.org (SPF/DKIM, coexists with Google Workspace)"
        location: "Resend Dashboard → Domains + eltemplo.org DNS"
must_haves:
  truths:
    - "MJML is installed (approved dep D-23) and compiles the campaign template to bulletproof table HTML"
    - "EmailService has a sendCampaignBatch method that degrades silently without RESEND_API_KEY"
    - "Campaign template renders all slots: pixel, hero, headline, body, dual CTA, sedes table, unsubscribe footer"
    - "Email is fully readable/actionable with images OFF (CTAs are VML/text, not images)"
    - "All email images are self-hosted under el-templo-web/public/email/ (NO CDN)"
  artifacts:
    - path: "el-templo-api/src/modules/campaigns/templates.ts"
      provides: "trialCampaignHtml(vars) + TRIAL_CAMPAIGN_SUBJECT via MJML"
      contains: "trialCampaignHtml"
    - path: "el-templo-api/src/modules/email/service.ts"
      provides: "sendCampaignBatch batch method"
      contains: "sendCampaignBatch"
  key_links:
    - from: "el-templo-api/src/modules/email/service.ts"
      to: "resend.batch.send"
      via: "batch with idempotencyKey"
      pattern: "batch"
---

<objective>
Build the reusable email infrastructure: install MJML (D-23, the one approved new dep), add a `sendCampaignBatch` method to the existing `EmailService` (extend — never `new Resend()` in the campaigns module, Pitfall 3), and author the responsive campaign template (`templates.ts`) per the UI-SPEC Email Layout Contract (D-13/D-14/D-16/D-27). Declare the new Resend env vars and create the self-hosted email-image directory (D-27, NO CDN). A blocking human checkpoint verifies the MJML install and confirms the prod Resend domain/API-key setup plan (D-17).

Purpose: Wave 4 (campaign send) renders this template and calls this batch method. The email is the PRIMARY surface — it must be bulletproof across Gmail/Apple Mail/Outlook and fully actionable with images off.
Output: MJML installed, EmailService batch method, campaign template with all slots, env vars declared, image dir created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/119-campa-a-de-sesi-n-de-prueba-freemium-reserva-self-service-si/119-CONTEXT.md
@.planning/phases/119-campa-a-de-sesi-n-de-prueba-freemium-reserva-self-service-si/119-PATTERNS.md
@.planning/phases/119-campa-a-de-sesi-n-de-prueba-freemium-reserva-self-service-si/119-UI-SPEC.md
@.planning/phases/119-campa-a-de-sesi-n-de-prueba-freemium-reserva-self-service-si/119-RESEARCH.md

<interfaces>
- EmailService analog: el-templo-api/src/modules/email/service.ts:22-46 — `new Resend(apiKey)` + graceful-degradation guard `if (!apiKey) { this.log.info("RESEND_API_KEY not configured, skipping ..."); return; }` (lines 27-34). Constructor takes (log). from = `El Templo <noreply@eltemplo.org>`.
- Template analog: el-templo-api/src/modules/email/templates.ts — "function returns HTML string + exported subject const" shape.
- Resend batch: `resend.batch.send(messages, { idempotencyKey })` — ≤100/req, ≤50 recipients/email. CAMPAIGN_FROM = subdomain sender (D-17).
- UI-SPEC § "Email Layout Contract" is the structural authority: pixel first → hero (self-hosted logo.png + hero image, max-width 160/600px, alt text) → headline (Georgia 28/700 #3D3732) → body (Arial 16/1.5) → dual CTA (Terracotta #C07A56 primary + WhatsApp #25D366) → sedes table (name + branches.address) → footer (Olive Stone 12px + unsubscribe). Web-safe fonts only (Georgia/Arial), all CSS inline, VML <v:roundrect> for Outlook, 600px container, dark-mode media query with solid fallback.
- WhatsApp numbers: AR 5492235820521, ES 34680774331 (from el-templo-app/src/utils/whatsapp.ts and el-templo-web/data/sedes.ts).
- WARM brand palette (UI-SPEC): Marble Cream #F2EDE5, Terracotta #C07A56, Deep Charcoal #3D3732, Olive Stone #8A8472, WhatsApp #25D366. NO blue/navy (the orchestrator "Navy/Bronze" reference is STALE).
- Template var slots: { headline, subheadline, body, trackingPixelUrl, ctaAppUrl, whatsappUrl, sedes: {name,address,mapsUrl}[], unsubscribeUrl }.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Plan to install MJML (D-23, approved dep) as a build dependency in el-templo-api, and the prod Resend setup plan (D-17: set RESEND_API_KEY in prod + verify send.eltemplo.org domain).</what-built>
  <how-to-verify>
    1. Confirm MJML is the intended package and version is acceptable (it is the ONLY new dep this phase; D-23 pre-approved it explicitly as an exception to the no-new-deps rule).
    2. Confirm the sending subdomain choice for `CAMPAIGN_EMAIL_FROM` (RESEARCH recommends `send.eltemplo.org`; the exact subdomain + from address is the user's DNS decision, A2).
    3. Confirm that prod Resend API-key setting + domain verification will happen as part of Plan 07 infra (not blocking dev — degrades silently without the key).
  </how-to-verify>
  <resume-signal>Type "approved" with the chosen subdomain/from, or specify a different package/version/from.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Install MJML + EmailService.sendCampaignBatch</name>
  <read_first>
    - el-templo-api/src/modules/email/service.ts (extend — copy the graceful-degradation guard 27-34, reuse the constructor/log)
    - el-templo-api/.env.example lines 34-39 (RESEND_API_KEY + NOTIFICATION_EMAILs)
    - .planning/phases/119-.../119-PATTERNS.md § "src/modules/email/service.ts — batch method"
  </read_first>
  <action>Install MJML as a dependency in el-templo-api (D-23 — only after the checkpoint above is approved). Add `async sendCampaignBatch(messages: { to: string; subject: string; html: string }[], idempotencyKey: string): Promise<void>` to `EmailService`, reusing the existing `if (!apiKey) { log; return }` degradation guard, then `new Resend(apiKey)` and `resend.batch.send(messages.map(m => ({ from: CAMPAIGN_FROM, ...m })), { idempotencyKey })`. Define `CAMPAIGN_FROM` from `process.env.CAMPAIGN_EMAIL_FROM` (fallback to the existing noreply from). Add `CAMPAIGN_EMAIL_FROM` (+ note RESEND_API_KEY prod requirement) to `.env.example`. No `any`; `catch (err: unknown)`; Pino log, never console.log. Do NOT instantiate Resend in the campaigns module.</action>
  <verify>
    <automated>cd el-templo-api && pnpm typecheck && grep -q "sendCampaignBatch" src/modules/email/service.ts && grep -q "mjml" package.json && grep -q "CAMPAIGN_EMAIL_FROM" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `mjml` appears in el-templo-api/package.json dependencies (or devDependencies).
    - `sendCampaignBatch` defined in email/service.ts and uses `resend.batch.send` with an `idempotencyKey` argument.
    - The `if (!apiKey)` guard returns early (silent degradation) — `grep -A3 "RESEND_API_KEY" src/modules/email/service.ts` shows the skip path in sendCampaignBatch.
    - `CAMPAIGN_EMAIL_FROM` present in .env.example.
    - `pnpm typecheck` passes.
  </acceptance_criteria>
  <done>MJML installed; EmailService.sendCampaignBatch added with idempotency + degradation; env var declared.</done>
</task>

<task type="auto">
  <name>Task 2: MJML campaign template + self-hosted image dir</name>
  <read_first>
    - el-templo-api/src/modules/email/templates.ts (shape: exported subject const + function returning HTML)
    - .planning/phases/119-.../119-UI-SPEC.md § "Email Layout Contract" + § "Copywriting Contract" (slots, structure, CTAs, fonts, NO-CDN, images-off requirement)
    - el-templo-web/data/sedes.ts (sede name+address+mapsUrl shape for the sedes table)
    - .planning/phases/119-.../119-PATTERNS.md § "templates.ts (NEW)"
  </read_first>
  <action>Create `el-templo-api/src/modules/campaigns/templates.ts` exporting `TRIAL_CAMPAIGN_SUBJECT` (placeholder — copy is user-supplied per UI-SPEC; use a clear slot/placeholder, do NOT invent final copy) and `trialCampaignHtml(vars)` where vars = { headline, subheadline, body, trackingPixelUrl, ctaAppUrl, whatsappUrl, sedes: BranchAddress[], unsubscribeUrl }. Build the MJML document per the UI-SPEC vertical structure: tracking pixel first (1x1 GIF img, alt="", display:block), hero (logo from https://eltemplo.org/email/logo.png + hero image, alt text), headline (Georgia 28/700 #3D3732), body, dual CTA (Terracotta #C07A56 primary "Reservá en la app" → ctaAppUrl with VML roundrect fallback; WhatsApp #25D366 "Agendá por WhatsApp" → whatsappUrl), sedes table (name + address + optional "Cómo llegar" → mapsUrl), footer (Olive Stone 12px + "¿No querés recibir más novedades? Darte de baja" → unsubscribeUrl). Compile MJML → HTML at render time. Web-safe fonts only (Georgia/Arial), NO @font-face/Google Fonts, all images self-hosted under eltemplo.org/email/ (NO CDN). The email MUST be fully actionable with images off (CTAs are VML/text). Add `el-templo-api/src/modules/campaigns/types.ts` with `BranchAddress` and the vars interface. Create `el-templo-web/public/email/.gitkeep` (image assets — logo/hero — are user-supplied art; placeholder dir so the path exists). Use the WARM palette only — NO blue.</action>
  <verify>
    <automated>cd el-templo-api && pnpm typecheck && node -e "const {trialCampaignHtml}=require('./src/modules/campaigns/templates.ts'); /* compile check via ts-node not required; rely on typecheck */ process.exit(0)" 2>/dev/null; grep -q "trialCampaignHtml" src/modules/campaigns/templates.ts && grep -qi "v:roundrect\|roundrect" src/modules/campaigns/templates.ts && grep -q "25D366" src/modules/campaigns/templates.ts && grep -q "C07A56" src/modules/campaigns/templates.ts && ! grep -qiE "#(2c3e5c|navy)|fonts.googleapis|@font-face" src/modules/campaigns/templates.ts</automated>
  </verify>
  <acceptance_criteria>
    - `trialCampaignHtml` and `TRIAL_CAMPAIGN_SUBJECT` exported from templates.ts; `BranchAddress` exported from types.ts.
    - Template contains the Terracotta primary CTA (`C07A56`), WhatsApp green CTA (`25D366`), and a VML roundrect fallback for Outlook.
    - Template contains the tracking pixel slot (`trackingPixelUrl`), sedes table (iterates `sedes`), and unsubscribe footer (`unsubscribeUrl`).
    - NO blue/navy, NO Google Fonts, NO @font-face (the negative grep passes — exit 0).
    - All image `src` URLs point at `eltemplo.org/email/` (self-hosted) — `grep -c "eltemplo.org/email" templates.ts` >= 1; no third-party CDN host appears.
    - `el-templo-web/public/email/.gitkeep` exists.
    - `pnpm typecheck` passes.
  </acceptance_criteria>
  <done>MJML campaign template renders all UI-SPEC slots, bulletproof + images-off safe, warm brand, self-hosted images.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                                            |
| ----------------------------- | -------------------------------------------------------------------------------------- |
| API → Resend (external)       | Email transport; API key is a secret; over-send risk                                   |
| email HTML → recipient client | Untrusted rendering env (Gmail/Outlook/Apple Mail); image proxies, dark-mode inversion |

## STRIDE Threat Register

| Threat ID   | Category               | Component          | Disposition | Mitigation Plan                                                                                                                                                                         |
| ----------- | ---------------------- | ------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-119-02-01 | Denial of Service      | sendCampaignBatch  | mitigate    | idempotencyKey on resend.batch.send prevents duplicate sends on retry (D-12); silent degradation without API key prevents accidental dev sends                                          |
| T-119-02-02 | Information Disclosure | email images / CDN | mitigate    | All images self-hosted under eltemplo.org (D-27, NO CDN) — no third-party host receives recipient request leakage                                                                       |
| T-119-02-03 | Tampering              | MJML dependency    | accept      | MJML pre-approved by user (D-23) as explicit exception; mature maintained package. Blocking checkpoint confirms install before adding.                                                  |
| T-119-02-SC | Tampering              | npm install (mjml) | mitigate    | Single approved package (D-23). Blocking human checkpoint (Task 0) gates the install; verify on npmjs.com/package/mjml before proceeding. Legitimacy checkpoint is not auto-approvable. |

</threat_model>

<verification>
- Checkpoint approved before MJML install.
- `pnpm typecheck` green.
- Template renders all slots, no blue, no CDN/Google Fonts, VML present.
- sendCampaignBatch degrades silently without key + uses idempotency.
</verification>

<success_criteria>

- MJML installed (post-approval) and compiling the template.
- EmailService.sendCampaignBatch ready for Wave 4.
- Campaign template matches the UI-SPEC Email Layout Contract.
- Self-hosted image dir created; Resend env vars declared.
  </success_criteria>

<output>
Create `.planning/phases/119-campa-a-de-sesi-n-de-prueba-freemium-reserva-self-service-si/119-02-SUMMARY.md` when done.
</output>
