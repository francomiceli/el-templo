---
phase: 119-campa-a-de-sesi-n-de-prueba-freemium-reserva-self-service-si
plan: 03
type: execute
wave: 2
depends_on: ["119-01"]
files_modified:
  - el-templo-api/src/modules/scheduling/trials-service.ts
  - el-templo-api/src/modules/scheduling/booking-service.ts
  - el-templo-api/src/modules/scheduling/routes.ts
  - el-templo-api/src/modules/scheduling/schemas.ts
  - el-templo-api/test/scheduling-reserve-trial.test.ts
  - el-templo-api/test/scheduling-trial-eligibility.test.ts
autonomous: true
requirements: [D-01, D-02, D-03, D-05, D-06, D-07, D-20, D-21, D-26]
must_haves:
  truths:
    - "POST /members/scheduling/reserve-trial promotes freemium→prueba + inserts user_status_history + inserts trial booking atomically (D-01/D-26)"
    - "reserve-trial validates eligibility server-side (freemium + no sub + no prior trial) and ignores the email token (D-21)"
    - "reserve-trial sets the user's chosen physical branch and source='self_service', createdBy=null (D-02/D-06)"
    - "Trial booking window is 30 days; members' window stays at +2 days (D-05)"
    - "Trial bookings cannot be cancelled from the app (D-03)"
    - "GET /members/scheduling/trial-eligibility returns { eligible, alreadyBooked, booking? } (D-20)"
    - "Trial does not consume schedule capacity (D-07, regression)"
  artifacts:
    - path: "el-templo-api/src/modules/scheduling/trials-service.ts"
      provides: "reserveTrialSelfService promote-and-book + trial eligibility predicate"
      contains: "reserveTrialSelfService"
    - path: "el-templo-api/src/modules/scheduling/routes.ts"
      provides: "reserve-trial + trial-eligibility member routes"
      contains: "reserve-trial"
  key_links:
    - from: "el-templo-api/src/modules/scheduling/routes.ts"
      to: "trials-service.reserveTrialSelfService"
      via: "POST handler"
      pattern: "reserveTrialSelfService"
    - from: "trials-service.ts"
      to: "schema.userStatusHistory"
      via: "tx insert in promote-and-book"
      pattern: "userStatusHistory"
---

<objective>
Expose the self-service trial reservation safely. Add `reserveTrialSelfService` to `trials-service.ts` that atomically promotes a freemium user to `status='prueba'` (replicating `convertFreemiumToTrial`), writes `user_status_history`, sets the chosen physical branch, and inserts the trial booking (`source='self_service'`, `createdBy=null`) — all in one transaction (D-01/D-02/D-26). Branch the booking window to 30 days for trials (D-05). Block cancelling trial bookings (D-03). Add `GET /members/scheduling/trial-eligibility` (D-20) since `/me` does not expose status. All authorization is server-side by user state — the email token never authorizes (D-21).

Purpose: This is the backend the member app (Wave 3) calls. The 70% of booking mechanics that already exist (one-per-lifetime guard, capacity exclusion) is reused, not duplicated.
Output: reserveTrialSelfService + eligibility predicate + 30d window branch + cancel guard + 2 member routes + schemas; GREEN tests (verified in CI).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/119-campa-a-de-sesi-n-de-prueba-freemium-reserva-self-service-si/119-CONTEXT.md
@.planning/phases/119-campa-a-de-sesi-n-de-prueba-freemium-reserva-self-service-si/119-PATTERNS.md
@.planning/phases/119-campa-a-de-sesi-n-de-prueba-freemium-reserva-self-service-si/119-RESEARCH.md

<interfaces>
- ATOMIC PROMOTE+BOOK blueprint: el-templo-api/src/modules/members/service.ts:863-885 (convertFreemiumToTrial): inside `db.transaction`, UPDATE users SET status='prueba', leadStatus='en_seguimiento' + INSERT userStatusHistory { fromStatus, toStatus:'prueba', source }. The 409 guards (status!=='freemium', branch.isVirtual) run BEFORE the tx.
- ONE-PER-LIFETIME guard + cancelled-row reactivation: trials-service.ts:115-209 (bookTrial). Guard checks no non-cancelled is_trial booking (149-169); reactivate-or-insert avoids 500 on a previously cancelled exact slot+date row (173-208).
- CRITICAL (RESEARCH Pattern 1): bookTrial today (lines 136, 143) hard-requires status==='prueba' AND user.branchId===schedule.branchId BEFORE booking. Do NOT call bookTrial as-is from freemium. Either add a promote-and-book mode OR build reserveTrialSelfService reusing ONLY the guard (149-169) + reactivation (173-208) inside its own promotion tx. Recommended: reserveTrialSelfService standalone.
- 30d WINDOW: booking-service.ts:69-74 hardcodes `const maxDate = addDays(today, 2)`. Branch by an isTrial/windowDays param → `addDays(today, isTrial ? 30 : 2)`. KEEP isWithinBookingWindow (77-79), dayOfWeek (81-87), holiday (89-110) checks intact (Pitfall 2). reserve-trial must NOT hit the subscription check (112-117) — that's why it is a separate endpoint, not /reserve.
- ROUTE shape: scheduling/routes.ts member plugin starts line 654 (second addHook onRequest authenticate). /reserve POST 708-721; /branches GET 766-793 already filters active+non-virtual+user-country (reuse for D-06). Use handleServiceError(err, reply, request.log, "...").
- ELIGIBILITY predicate (D-20) = campaign audience (D-08) minus email/unsubscribe: status==='freemium' + NO active/paused/scheduled sub + NO non-cancelled is_trial booking. Returns { eligible, alreadyBooked, booking?: { date, branchName, ... } }.
- Errors: ConflictError/NotFoundError/BadRequestError from modules/shared/errors. No `any`, catch(err: unknown).
- Test scaffolds (made GREEN here, run in CI): test/scheduling-reserve-trial.test.ts (D-01/D-03/D-05/D-21), test/scheduling-trial-eligibility.test.ts (D-20). Fixture createEligibleFreemium added in Plan 01.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 30d window branch + reserveTrialSelfService + cancel guard</name>
  <read_first>
    - el-templo-api/src/modules/scheduling/booking-service.ts lines 63-120 (window/day/holiday/sub checks)
    - el-templo-api/src/modules/scheduling/trials-service.ts lines 92-220 (constructor, bookTrial guard + reactivation)
    - el-templo-api/src/modules/members/service.ts:817-892 (convertFreemiumToTrial — the EXACT promote-and-book blueprint)
    - el-templo-api/src/db/schema/{users,bookings,user-status-history,subscriptions}.ts (column names/enums)
    - .planning/phases/119-.../119-PATTERNS.md § "trials-service.ts + reserve-trial" and § "booking-service.ts — 30-day window"
  </read_first>
  <behavior>
    - reserveTrialSelfService(userId, { scheduleId, date, branchId }):
      - 409 if user.status !== 'freemium'; 409 if chosen branch isVirtual; 409 if a non-cancelled is_trial booking already exists (one-per-lifetime); 409 if an active/paused/scheduled sub exists.
      - On success: in ONE db.transaction → UPDATE users SET status='prueba', leadStatus='en_seguimiento', createdBy=null, branchId=<chosen physical branch>; INSERT userStatusHistory { fromStatus:'freemium', toStatus:'prueba', source:'self_service' }; INSERT (or reactivate cancelled) booking { memberId:userId, scheduleId, bookingDate:date, status:'reservado', isTrial:true, source:'self_service' }.
      - Booking date validated against the 30-day window, dayOfWeek, holiday, and not-past checks (NOT the subscription check).
    - Cancelling a booking where isTrial=true is rejected from the member cancel path (D-03).
    - The email token is never read here — authorization is purely server-side state (D-21).
  </behavior>
  <action>Branch the booking-window validation in booking-service.ts to accept an `isTrial`/`windowDays` parameter so the date-range check becomes `addDays(today, isTrial ? 30 : 2)` while keeping isWithinBookingWindow/dayOfWeek/holiday checks intact and skipping the subscription check for the trial path. Add `reserveTrialSelfService` to trials-service.ts following the convertFreemiumToTrial blueprint combined with the booking insert in ONE transaction (reuse the one-per-lifetime guard and cancelled-row reactivation; do NOT call bookTrial as-is). Add a guard in the member cancel path that rejects cancelling an is_trial booking with a clear message (D-03). Verify capacity exclusion (countActiveBookings filters is_trial=false) is untouched (D-07). No `any`; ConflictError/BadRequestError; Pino log.</action>
  <verify>
    <automated>cd el-templo-api && pnpm typecheck</automated>
    <ci_note>CI (post-push to staging) runs: pnpm test -- test/scheduling-reserve-trial.test.ts. Per project rule (MEMORY: tests run in CI, not local), the executor runs ONLY typecheck locally and lets CI run the suite.</ci_note>
  </verify>
  <acceptance_criteria>
    - `reserveTrialSelfService` defined in trials-service.ts and performs UPDATE users + INSERT userStatusHistory + INSERT booking inside a single `db.transaction`.
    - booking-service window check uses an isTrial/windowDays branch (grep shows `30` and `2` both reachable; the subscription check is NOT on the trial path).
    - scheduling-reserve-trial.test.ts passes (in CI): asserts freemium→prueba + history row + trial booking created atomically (D-01); rejects non-freemium / with-sub / with-prior-trial server-side (D-21); rejects cancel of is_trial (D-03); allows date up to +30d but rejects +31d, and members still limited to +2d (D-05).
    - `pnpm typecheck` passes locally.
  </acceptance_criteria>
  <done>Atomic self-service promote-and-book + 30d window + cancel guard implemented; reserve-trial test GREEN in CI; typecheck green locally.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: reserve-trial + trial-eligibility routes + schemas</name>
  <read_first>
    - el-templo-api/src/modules/scheduling/routes.ts lines 654-793 (member plugin, /reserve 708-721, /branches 766-793)
    - el-templo-api/src/modules/scheduling/schemas.ts (existing *Schema consts shape)
    - .planning/phases/119-.../119-PATTERNS.md § "routes.ts — reserve-trial + trial-eligibility"
    - el-templo-api/test/scheduling-trial-eligibility.test.ts (scaffold to make GREEN)
  </read_first>
  <behavior>
    - POST /members/scheduling/reserve-trial { scheduleId:number, date:string, branchId:number } → 201 with the booking result; delegates to reserveTrialSelfService(request.user.id, body); errors via handleServiceError.
    - GET /members/scheduling/trial-eligibility → { eligible:boolean, alreadyBooked:boolean, booking?:{ date, branchName, branchAddress?, ... } } using the eligibility predicate. Auth required (member JWT); reflects the 3 ReservasPage states.
  </behavior>
  <action>Add `reserveTrialSchema` and `trialEligibilitySchema` to scheduling/schemas.ts (Fastify validation, additionalProperties:false on the body). Register both routes inside the existing member plugin (auth already applied by the section-level onRequest hook at 654), copying the /reserve handler shape (708-721) + handleServiceError. The eligibility handler calls a `getTrialEligibility(userId)` method (add to trials-service.ts reusing the same predicate as reserveTrialSelfService's guards, plus the already-booked lookup returning the booking's date + branch name/address). No `any`; catch(err: unknown).</action>
  <verify>
    <automated>cd el-templo-api && pnpm typecheck</automated>
    <ci_note>CI (post-push to staging) runs: pnpm test -- test/scheduling-trial-eligibility.test.ts. Executor runs ONLY typecheck locally (MEMORY: tests in CI).</ci_note>
  </verify>
  <acceptance_criteria>
    - `grep -c "reserve-trial" el-templo-api/src/modules/scheduling/routes.ts` >= 1 and `grep -q "trial-eligibility" routes.ts` succeeds.
    - reserveTrialSchema + trialEligibilitySchema exist in schemas.ts with additionalProperties:false on the reserve-trial body.
    - scheduling-trial-eligibility.test.ts passes (in CI): eligible freemium → { eligible:true, alreadyBooked:false }; after reserving → { eligible:false, alreadyBooked:true, booking }; non-freemium / with-sub → { eligible:false } (D-20).
    - `pnpm typecheck` passes locally.
  </acceptance_criteria>
  <done>Two member routes live with validation; eligibility reflects 3 states; eligibility test GREEN in CI.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                    | Description                                                                |
| ------------------------------------------- | -------------------------------------------------------------------------- |
| email link / token → app → reserve-trial    | Token arrives from an untrusted email link; must NOT grant the reservation |
| member client → reserve-trial / eligibility | Authenticated member JWT; user state is the authorization source of truth  |

## STRIDE Threat Register

| Threat ID   | Category               | Component                  | Disposition | Mitigation Plan                                                                                                                                   |
| ----------- | ---------------------- | -------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-119-03-01 | Elevation of Privilege | reserve-trial              | mitigate    | D-21: token never authorizes; reserveTrialSelfService revalidates status==='freemium' + no sub + one-per-lifetime guard server-side inside the tx |
| T-119-03-02 | Tampering              | trial booking window       | mitigate    | 30d window applies ONLY to the isTrial path; members' +2d unchanged; dayOfWeek/holiday/past checks retained so trials cannot book invalid slots   |
| T-119-03-03 | Tampering              | trial cancel               | mitigate    | Member cancel path rejects is_trial bookings (D-03); one-per-lifetime guard prevents re-reserve abuse                                             |
| T-119-03-04 | Information Disclosure | trial-eligibility          | mitigate    | Endpoint requires member JWT; returns only the requesting user's own status/booking; no enumeration of other users                                |
| T-119-03-05 | Tampering              | audience/eligibility query | mitigate    | Drizzle parameterized queries (no string concat)                                                                                                  |
| T-119-03-SC | Tampering              | npm installs               | accept      | No package installs in this plan.                                                                                                                 |

</threat_model>

<verification>
- `pnpm typecheck` green locally.
- scheduling-reserve-trial.test.ts + scheduling-trial-eligibility.test.ts GREEN in CI (run after push to staging — MEMORY: tests in CI, not local).
- Capacity exclusion (D-07) regression-checked.
</verification>

<success_criteria>

- Atomic freemium→prueba + trial booking via reserve-trial (D-01/D-02/D-26).
- 30d trial window without breaking members' +2d (D-05).
- Trial not cancelable (D-03); does not consume capacity (D-07).
- Eligibility endpoint drives the 3 app states (D-20); token never authorizes (D-21).
  </success_criteria>

<output>
Create `.planning/phases/119-campa-a-de-sesi-n-de-prueba-freemium-reserva-self-service-si/119-03-SUMMARY.md` when done.
</output>
</content>
