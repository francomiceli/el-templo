---
phase: 119-campa-a-de-sesi-n-de-prueba-freemium-reserva-self-service-si
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - el-templo-api/src/db/schema/branches.ts
  - el-templo-api/src/db/schema/bookings.ts
  - el-templo-api/src/db/schema/campaigns.ts
  - el-templo-api/src/db/schema/index.ts
  - el-templo-api/src/db/migrations/0132_add_branches_address.sql
  - el-templo-api/src/db/migrations/0133_add_bookings_source.sql
  - el-templo-api/src/db/migrations/0134_create_campaign_tables.sql
  - el-templo-api/test/helpers.ts
  - el-templo-api/test/scheduling-reserve-trial.test.ts
  - el-templo-api/test/scheduling-trial-eligibility.test.ts
  - el-templo-api/test/campaign-token.test.ts
  - el-templo-api/test/campaigns-audience.test.ts
  - el-templo-api/test/campaigns-tracking.test.ts
  - el-templo-api/test/campaigns-send.test.ts
  - el-templo-api/test/campaigns-funnel.test.ts
  - el-templo-api/test/campaigns-template.test.ts
autonomous: true
requirements: [D-02, D-13, D-24, D-12, D-15, D-16, D-18, D-23]
must_haves:
  truths:
    - "branches table has a nullable address column backfilled with the 8 canonical sede addresses"
    - "bookings table has a nullable source column ('self_service'|'admin'|NULL)"
    - "4 campaign tables exist: campaigns, campaign_sends, campaign_events, campaign_unsubscribes"
    - "campaign_sends has UNIQUE(campaign_id, user_id) for audience idempotency"
    - "campaign_unsubscribes has UNIQUE(email) for suppression"
    - "All 3 migrations applied locally via pnpm db:migrate and tracked in _migrations"
    - "Failing test scaffolds exist for every Wave 0 gap (8 scaffolds incl. the email-template render test)"
  artifacts:
    - path: "el-templo-api/src/db/schema/campaigns.ts"
      provides: "campaign/sends/events/unsubscribes Drizzle tables + relations"
      contains: "campaignSends"
    - path: "el-templo-api/src/db/migrations/0134_create_campaign_tables.sql"
      provides: "campaign tables DDL"
      contains: "campaign_sends"
    - path: "el-templo-api/src/db/migrations/0132_add_branches_address.sql"
      provides: "branches.address column + backfill"
      contains: "ADD COLUMN address"
  key_links:
    - from: "el-templo-api/src/db/schema/index.ts"
      to: "campaigns.ts"
      via: "re-export"
      pattern: "campaigns"
---

<objective>
Lay the schema foundation for Phase 119: add `branches.address` (D-24, source of the email's sede addresses D-13), add `bookings.source` (D-02, funnel attribution D-18), and create the 4 reusable campaign tables (D-12: campaigns/sends/events/unsubscribes, D-15 unsubscribe suppression, D-18 tracking events). Write all 3 migrations with the project's custom runner, apply locally, and scaffold every Wave 0 failing test (incl. the MJML email-template render test D-16/D-23) so downstream waves have a Nyquist-valid harness.

Purpose: Everything in Waves 2-4 reads against this schema. Tests run against real MySQL (`eltemplo_test`), so the schema MUST be applied before any integration test runs — build/typecheck alone would be a false positive.
Output: 3 schema files modified/created, schema index updated, 3 committed `.sql` migrations applied locally, 8 failing test scaffolds + extended test fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-campa-a-de-sesi-n-de-prueba-freemium-reserva-self-service-si/119-CONTEXT.md
@.planning/phases/119-campa-a-de-sesi-n-de-prueba-freemium-reserva-self-service-si/119-PATTERNS.md
@.planning/phases/119-campa-a-de-sesi-n-de-prueba-freemium-reserva-self-service-si/119-RESEARCH.md

<interfaces>
<!-- Canonical analogs the executor copies. Extracted; no exploration needed. -->
- Foundation-table analog: el-templo-api/src/db/schema/user-status-history.ts (mysqlTable, int autoincrement PK, FK .references(() => users.id, { onDelete: "cascade" }), index([...]) array form, varchar(16) soft-enum, relations() export, header comment).
- Migration analog: el-templo-api/src/db/migrations/0128_create_user_status_history.sql (hand-written, NO `IF NOT EXISTS`, Drizzle-convention FK names, NO `;` inside `--` comments, header comment block).
- branches.ts current columns: name/code/timezone/country(varchar 2)/maxCapacity/isVirtual. Add `address` AFTER `country` (line 19).
- bookings.ts: add `source` AFTER `isTrial` (line 40).
- Canonical sede addresses live in el-templo-web/data/sedes.ts (8 sedes: 7 Mar del Plata + Barcelona "Eixample, Av. Diagonal 368"). Map each branch by name/code to its address string for the backfill UPDATEs.
- Test helpers: el-templo-api/test/helpers.ts exports createTestApp, getAuthToken, createTestMember, createTestPlan, assignTestPlan, createStaffUser, cleanAllTestData. TABLES_TO_CLEAN must gain the 4 new campaign tables.
- Migration numbering: current tip is 0131; new files start at 0132.
- EMAIL-TEMPLATE scaffold target (Plan 02): `el-templo-api/src/modules/campaigns/templates.ts` exports `trialCampaignHtml(vars)` + `TRIAL_CAMPAIGN_SUBJECT`; vars = { headline, subheadline, body, trackingPixelUrl, ctaAppUrl, whatsappUrl, sedes: {name,address,mapsUrl}[], unsubscribeUrl }. The scaffold `campaigns-template.test.ts` (made GREEN by Plan 02) renders the compiled MJML HTML and asserts the key rules: VML roundrect present for the Outlook CTA fallback, warm palette / no blue (no `2c3e5c`/`navy`), no `@font-face`/Google Fonts CDN, and `alt` attributes present on images. This is a pure render assertion (no DB) — it can `import { trialCampaignHtml } from "../src/modules/campaigns/templates"` once Plan 02 lands.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema columns + campaign tables + index wiring</name>
  <read_first>
    - el-templo-api/src/db/schema/branches.ts (target — add address)
    - el-templo-api/src/db/schema/bookings.ts (target — add source)
    - el-templo-api/src/db/schema/user-status-history.ts (foundation-table analog: imports, FK style, index array form, relations, header comment)
    - el-templo-api/src/db/schema/index.ts (target — re-export campaigns)
    - .planning/phases/119-.../119-PATTERNS.md § "campaigns.ts (NEW)" and § "branches.ts" / "bookings.ts" (exact column shapes)
    - .planning/phases/119-.../119-RESEARCH.md § "Schema de Campañas Reutilizable" (column lists for all 4 tables)
  </read_first>
  <behavior>
    - branches.address: `varchar("address", { length: 255 })` nullable.
    - bookings.source: `varchar("source", { length: 16 })` nullable.
    - campaigns: id, name, subject, status varchar(16) default 'draft' ('draft'|'sending'|'sent'), createdBy FK users.id, country varchar(2) nullable, createdAt, sentAt nullable.
    - campaign_sends: id, campaignId FK→campaigns cascade NOT NULL, userId FK→users cascade NOT NULL, email varchar(255) snapshot NOT NULL, status varchar(16) default 'pending', resendMessageId varchar(64) nullable, sentAt nullable, createdAt; uniqueIndex("uniq_campaign_user") on (campaignId, userId).
    - campaign_events: id, sendId FK→campaign_sends cascade NOT NULL, type varchar(16) ('open'|'click'|'bounce'), metadata varchar/json nullable, createdAt; index on (sendId, type). Forward-only.
    - campaign_unsubscribes: id, userId FK→users cascade nullable, email varchar(255) NOT NULL, campaignId FK→campaigns nullable, createdAt; uniqueIndex on email.
    - relations() exported for campaigns (sends), campaignSends (campaign, user, events).
  </behavior>
  <action>Create `el-templo-api/src/db/schema/campaigns.ts` with the 4 tables above mirroring user-status-history.ts conventions (no `any`, header comment documenting purpose + index rationale per D-12/D-15/D-18). Add `address` to branches.ts and `source` to bookings.ts as nullable columns. Re-export campaigns from `src/db/schema/index.ts` (append like the aura/member modules at lines ~29-40). Use `varchar(16)` soft-enums (no DB enum) consistent with user-status-history `source`. Do NOT generate migrations here — Task 2 owns that.</action>
  <verify>
    <automated>cd el-templo-api && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd el-templo-api && pnpm typecheck` passes.
    - `grep -c "export const campaign" el-templo-api/src/db/schema/campaigns.ts` returns 4 (campaigns, campaignSends, campaignEvents, campaignUnsubscribes).
    - `grep -q "address" el-templo-api/src/db/schema/branches.ts` and `grep -q "source" el-templo-api/src/db/schema/bookings.ts` both succeed.
    - `grep -q "campaigns" el-templo-api/src/db/schema/index.ts` succeeds.
    - `grep -q "uniqueIndex" el-templo-api/src/db/schema/campaigns.ts` succeeds (audience + unsubscribe idempotency).
  </acceptance_criteria>
  <done>4 campaign tables defined + 2 columns added + re-exported, typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Write, commit, and apply 3 migrations</name>
  <read_first>
    - el-templo-api/src/db/migrations/0128_create_user_status_history.sql (verbatim conventions: header comment, no IF NOT EXISTS, FK naming, NO `;` in comments)
    - el-templo-web/data/sedes.ts (canonical address strings for the branches.address backfill)
    - CLAUDE.md § "Database Changes" (runner is custom; never drizzle-kit migrate)
    - .planning/phases/119-.../119-PATTERNS.md § "Migrations" (ALTER pattern, numbering, FK convention)
  </read_first>
  <action>Create 3 hand-written SQL migrations (numbering after tip 0131): `0132_add_branches_address.sql` (ALTER TABLE branches ADD COLUMN address VARCHAR(255) NULL, then backfill UPDATEs per sede — match each branch by name/code to its sedes.ts address string; the 8 sedes are 7 Mar del Plata + Barcelona "Eixample, Av. Diagonal 368"); `0133_add_bookings_source.sql` (ALTER TABLE bookings ADD COLUMN source VARCHAR(16) NULL — existing rows stay NULL = historical admin); `0134_create_campaign_tables.sql` (CREATE TABLE campaigns/campaign_sends/campaign_events/campaign_unsubscribes WITHOUT `IF NOT EXISTS`, Drizzle-convention FK names like `campaign_sends_user_id_users_id_fk`, the uniqueIndex on (campaign_id,user_id), uniqueIndex on email, index(send_id,type)). HARD RULE: never a `;` inside any `--` comment line (runner splits on `;` before stripping comments). Apply locally with `cd el-templo-api && pnpm db:migrate`. COMMIT all 3 `.sql` files — the executor must not forget the .sql files.</action>
  <verify>
    <automated>cd el-templo-api && pnpm db:migrate && node -e "const m=require('fs').readFileSync('src/db/migrations/0134_create_campaign_tables.sql','utf8').split('\n').filter(l=>l.trim().startsWith('--')); if(m.some(l=>l.includes(';'))){console.error('SEMICOLON IN COMMENT');process.exit(1)} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm db:migrate` exits 0 and reports 0132/0133/0134 applied (or already applied).
    - No `--` comment line in any of the 3 .sql files contains `;` (the node guard prints `ok` and exits 0).
    - `_migrations` table contains rows with name 0132_add_branches_address, 0133_add_bookings_source, 0134_create_campaign_tables (verify: `cd el-templo-api && node -e "..."` querying _migrations, or via db:migrate idempotent re-run reporting "already applied").
    - All 3 .sql files are tracked by git (`git status --porcelain el-templo-api/src/db/migrations/0132*.sql 0133*.sql 0134*.sql` shows them added, not ??-ignored).
    - branches.address is populated for all 8 physical sedes (none NULL among non-virtual branches).
  </acceptance_criteria>
  <done>3 migrations committed + applied locally + tracked in _migrations; backfill populated; no `;`-in-comment hazard.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 test scaffolds + fixtures</name>
  <read_first>
    - el-templo-api/test/helpers.ts (createTestMember, createStaffUser, assignTestPlan, cleanAllTestData, TABLES_TO_CLEAN)
    - el-templo-api/test/scheduling.test.ts (existing scheduling test patterns: createTestApp, getAuthToken, request shape)
    - .planning/phases/119-.../119-RESEARCH.md § "Phase Requirements → Test Map" and § "Wave 0 Gaps" (file→requirement mapping)
  </read_first>
  <action>Add the 4 campaign tables to TABLES_TO_CLEAN in `test/helpers.ts` (FK ordering: campaign_events → campaign_sends → campaign_unsubscribes → campaigns, before users), and add a `createEligibleFreemium(app)` fixture helper (creates a user with status='freemium', valid email, created_at older than 3 days, no sub, no trial) plus a `createTestCampaign`/`createTestSend` helper. Create the 8 failing test scaffolds listed below, each with `describe`/`it.todo` or a single failing assertion that references its D-XX requirement so Nyquist gating is satisfied: `scheduling-reserve-trial.test.ts` (D-01/D-03/D-05/D-21), `scheduling-trial-eligibility.test.ts` (D-20), `campaign-token.test.ts` (D-04/D-21 unit), `campaigns-audience.test.ts` (D-08/09/10), `campaigns-tracking.test.ts` (D-15/D-18), `campaigns-send.test.ts` (D-12, Resend mocked), `campaigns-funnel.test.ts` (D-18/D-19), and `campaigns-template.test.ts` (D-16/D-23 — pure render test of `trialCampaignHtml` from Plan 02: asserts VML roundrect for the Outlook CTA fallback, warm palette / no blue, no `@font-face`/Google Fonts CDN, and `alt` attributes on images; use `it.todo`/RED until Plan 02 lands the template). Each scaffold MUST start RED (the implementing wave makes it GREEN). Use `catch (err: unknown)`, no `any`, `createLogger`/Pino never console.log.</action>
  <verify>
    <automated>cd el-templo-api && pnpm typecheck && ls test/scheduling-reserve-trial.test.ts test/scheduling-trial-eligibility.test.ts test/campaign-token.test.ts test/campaigns-audience.test.ts test/campaigns-tracking.test.ts test/campaigns-send.test.ts test/campaigns-funnel.test.ts test/campaigns-template.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 8 test files exist (the `ls` lists all 8 without error).
    - `pnpm typecheck` passes (scaffolds compile, no `any`).
    - `grep -c "campaign" el-templo-api/test/helpers.ts` >= 4 (TABLES_TO_CLEAN entries) and `createEligibleFreemium` is defined in helpers.ts.
    - Each scaffold file references its D-XX requirement in a comment or test name (grep for the D-IDs); `campaigns-template.test.ts` references D-16/D-23.
  </acceptance_criteria>
  <done>8 RED test scaffolds (incl. campaigns-template.test.ts) + extended fixtures committed; typecheck green; cleanup ordering safe.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                                                 |
| ---------------------------- | ------------------------------------------------------------------------------------------- |
| migration runner → DB        | DDL executed by custom runner; malformed SQL (e.g. `;` in comment) corrupts the whole batch |
| schema → downstream services | Column shapes/nullability become the contract every later wave depends on                   |

## STRIDE Threat Register

| Threat ID   | Category    | Component               | Disposition | Mitigation Plan                                                                                                                               |
| ----------- | ----------- | ----------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| T-119-01-01 | Tampering   | migration `.sql` files  | mitigate    | NO `;` inside `--` comments (runner splits on `;`); node guard in verify; hand-written DDL no `IF NOT EXISTS` per project pattern             |
| T-119-01-02 | Tampering   | campaign_unsubscribes   | mitigate    | uniqueIndex(email) enforces suppression idempotency (D-15) — duplicate unsub rows cannot dilute the NOT EXISTS audience filter                |
| T-119-01-03 | Repudiation | campaign_sends audience | mitigate    | uniqueIndex(campaign_id,user_id) prevents double-enrolling a user → idempotent send (D-12)                                                    |
| T-119-01-SC | Tampering   | npm/pip/cargo installs  | accept      | No package installs in this plan (D-12: no new deps; MJML approved but added in Plan 02). RESEARCH Package Legitimacy Audit: no new packages. |

</threat_model>

<verification>
- `pnpm typecheck` green across all 3 tasks.
- `pnpm db:migrate` applies 0132/0133/0134; re-run is idempotent.
- No `;` in any migration `--` comment.
- 8 test scaffolds exist and compile (incl. campaigns-template.test.ts).
</verification>

<success_criteria>

- branches.address + bookings.source columns live and applied.
- 4 campaign tables created with correct unique/index constraints.
- 3 .sql migrations committed and tracked in `_migrations`.
- Wave 0 test harness scaffolded RED for downstream waves (8 scaffolds incl. email-template render).
  </success_criteria>

<output>
Create `.planning/phases/119-campa-a-de-sesi-n-de-prueba-freemium-reserva-self-service-si/119-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
