---
phase: 119-campa-a-de-sesi-n-de-prueba-freemium-reserva-self-service-si
plan: 07
type: execute
wave: 4
depends_on: ["119-02", "119-04", "119-05", "119-06"]
files_modified:
  - el-templo-api/.env.example
autonomous: false
requirements: [D-17, D-11, D-13, D-25]
must_haves:
  truths:
    - "Resend sending domain/subdomain (send.eltemplo.org) is verified (SPF/DKIM) and coexists with Google Workspace (D-17)"
    - "RESEND_API_KEY is set in prod so campaign sends actually deliver (D-17)"
    - "The .well-known files are reachable at https://eltemplo.org/.well-known/ so App Links/Universal Links verify (D-25)"
    - "A real trial campaign is created with user-supplied copy and the email image assets are in place (D-13)"
    - "The first campaign send is gated behind the recipient-count confirmation and is reviewed before firing (D-11)"
  artifacts:
    - path: "el-templo-api/.env.example"
      provides: "documented prod Resend vars for the campaign sender"
      contains: "CAMPAIGN_EMAIL_FROM"
  key_links:
    - from: "Resend domain"
      to: "send.eltemplo.org DNS"
      via: "SPF/DKIM records"
      pattern: "send.eltemplo.org"
---

<objective>
Land the production infrastructure and fire the campaign. This plan is mostly human-required external setup (D-17): verify the Resend sending subdomain (SPF/DKIM, coexisting with Google Workspace), set `RESEND_API_KEY` in prod, confirm the `.well-known` deep-link files are reachable on `eltemplo.org` (D-25), supply the email copy + image assets (D-13), then create and send the first real campaign behind the recipient-count confirmation (D-11). Claude automates everything it can (env doc, reachability checks, campaign creation via API); the DNS/dashboard steps and the final send approval are human gates.

Purpose: Without the verified domain + prod API key the campaign degrades silently (no delivery). Without reachable .well-known files the deep links fall back to web. This plan flips the system live.
Output: documented env, verified domain, reachable .well-known, a created campaign, and an approved first send.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/119-campa-a-de-sesi-n-de-prueba-freemium-reserva-self-service-si/119-CONTEXT.md
@.planning/phases/119-campa-a-de-sesi-n-de-prueba-freemium-reserva-self-service-si/119-RESEARCH.md
@.planning/phases/119-campa-a-de-sesi-n-de-prueba-freemium-reserva-self-service-si/119-UI-SPEC.md

<interfaces>
- RESEARCH § "Email System over Resend": Resend requires SPF + DKIM (DMARC recommended). Coexistence with Google Workspace → use a SUBDOMAIN (send.eltemplo.org) with its own SPF + a TXT (Envelope From) + an MX for bounces. DMARC: strict DKIM / relaxed SPF coexists with Workspace.
- CAMPAIGN_EMAIL_FROM example: El Templo <hola@send.eltemplo.org>. RESEND_API_KEY set in prod .env (degrades silently if absent).
- Deep-link .well-known (Plan 05) must be served at https://eltemplo.org/.well-known/assetlinks.json and https://eltemplo.org/.well-known/apple-app-site-association (Content-Type application/json, no redirect).
- Email images (Plan 02 created the dir el-templo-web/public/email/): logo.png + hero image are user-supplied art; must be deployed so https://eltemplo.org/email/... resolves (NO CDN, D-27).
- Campaign creation + send via the Plan 04 admin endpoints: POST create campaign (name, subject, copy slots), GET /api/campaigns/admin/eligible-count, POST /api/campaigns/admin/:id/send. The send is irreversible (D-11).
- Member app store rebuild (bump minor) carries the deep-link native config to the stores — this is the feature bump noted in ROADMAP; per MEMORY do NOT do unsolicited store/manual deploys — surface it as a follow-up.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document prod env + verify .well-known reachability</name>
  <read_first>
    - el-templo-api/.env.example (current RESEND_API_KEY block, lines 34-39)
    - .planning/phases/119-.../119-RESEARCH.md § "Email System over Resend" (subdomain/SPF/DKIM)
  </read_first>
  <action>Finalize the `.env.example` documentation for the campaign sender: ensure `RESEND_API_KEY` notes it MUST be set in prod and `CAMPAIGN_EMAIL_FROM` documents the verified subdomain sender (e.g. El Templo <hola@send.eltemplo.org>). Add a comment pointing to the Resend domain-verification requirement (D-17). Check that the deep-link `.well-known` files are reachable on the deployed web app: curl https://eltemplo.org/.well-known/assetlinks.json and https://eltemplo.org/.well-known/apple-app-site-association and confirm HTTP 200 + JSON content-type (if not yet deployed, record as a deploy-dependent follow-up — do NOT perform an unsolicited deploy per MEMORY). No `any`.</action>
  <verify>
    <automated>cd el-templo-api && grep -q "CAMPAIGN_EMAIL_FROM" .env.example && grep -qi "resend" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - .env.example documents RESEND_API_KEY (prod-required) + CAMPAIGN_EMAIL_FROM with the subdomain sender example.
    - The .well-known reachability result is recorded in the SUMMARY (HTTP status for both files); if not 200, a clear deploy-dependent follow-up is noted (no unsolicited deploy performed).
  </acceptance_criteria>
  <done>Env documented; .well-known reachability checked and recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Resend production setup that has no CLI/API path for El Templo: verifying the sending subdomain in the Resend dashboard via DNS, and setting the prod RESEND_API_KEY.</what-built>
  <how-to-verify>
    1. In the Resend dashboard, add and verify the sending subdomain (recommended send.eltemplo.org): create the SPF TXT, DKIM CNAME/TXT, the Envelope-From TXT, and the MX record on eltemplo.org DNS (these coexist with Google Workspace because the subdomain has its own SPF). Wait for Resend to report "Verified".
    2. Set RESEND_API_KEY in the prod environment (.env / secret store) and CAMPAIGN_EMAIL_FROM to the verified subdomain sender.
    3. Send a single test email from Resend (or a 1-recipient test campaign to your own address) and confirm it lands in the inbox (not spam) with correct SPF/DKIM/DMARC alignment.
  </how-to-verify>
  <resume-signal>Type "verified" once Resend reports the domain verified and a test email inboxes, or describe the DNS/verification blocker.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>The email copy + image assets that only the user can supply (D-13 / UI-SPEC: user gives raw info, Claude shapes brand voice).</what-built>
  <how-to-verify>
    1. Provide the email subject + headline + body copy (Claude will shape it into brand voice if you give raw info). The structure is fixed by the UI-SPEC; only the copy text is needed.
    2. Provide the logo.png + hero image for el-templo-web/public/email/ (self-hosted, NO CDN) and confirm they will be deployed so https://eltemplo.org/email/... resolves.
    3. Confirm the WhatsApp numbers + preloaded message for the secondary CTA (AR 5492235820521 / ES 34680774331 already known — confirm or override).
  </how-to-verify>
  <resume-signal>Paste the copy + confirm the image assets/WhatsApp numbers, or hand over raw info for Claude to shape.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The first real trial campaign created with the supplied copy, ready to send to the eligible freemium audience behind the recipient-count confirmation (D-11, irreversible mass send).</what-built>
  <how-to-verify>
    1. In /campanias, confirm the campaign is created with the final copy/subject and the eligible recipient count looks right (cross-check with expectations for freemium-no-sub-no-trial-not-unsubscribed-registered>3d).
    2. Send a test/preview to your own inbox first; confirm the email renders correctly across at least Gmail + Apple Mail (and ideally Outlook), is fully readable with images off, both CTAs work (app deep link + WhatsApp), the sedes addresses show, and the unsubscribe link works.
    3. Only then trigger "Enviar campaña" and confirm the recipient-count dialog ("no se puede deshacer") before firing. After send, confirm the funnel begins populating (enviado, then opens/clicks over time).
  </how-to-verify>
  <resume-signal>Type "sent" after the campaign fires and the funnel shows "enviado", or describe what looked wrong in the preview.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                                |
| ----------------------------- | -------------------------------------------------------------------------- |
| eltemplo.org DNS → Resend     | Domain ownership proves sending authority; misconfig → spam/spoof exposure |
| prod env → RESEND_API_KEY     | Secret enabling real sends; over-send / leak risk                          |
| eltemplo.org .well-known → OS | App↔domain association for deep links                                      |

## STRIDE Threat Register

| Threat ID   | Category               | Component         | Disposition | Mitigation Plan                                                                                                                    |
| ----------- | ---------------------- | ----------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| T-119-07-01 | Spoofing               | sending domain    | mitigate    | SPF + DKIM + DMARC on send.eltemplo.org subdomain (D-17); verified before sending so mail isn't spoofable/spammed                  |
| T-119-07-02 | Denial of Service      | first mass send   | mitigate    | Human-verify checkpoint + recipient-count confirmation (D-11) before firing; test-to-self preview first; idempotent send (Plan 04) |
| T-119-07-03 | Information Disclosure | API key           | accept      | Set via prod secret store, never committed; .env.example documents the var name only                                               |
| T-119-07-04 | Tampering              | .well-known files | mitigate    | Served from eltemplo.org over HTTPS with correct content-type; reachability verified before relying on App Links                   |
| T-119-07-SC | Tampering              | npm installs      | accept      | No package installs in this plan.                                                                                                  |

</threat_model>

<verification>
- .env documents the prod Resend vars.
- Resend subdomain verified + test email inboxes (checkpoint).
- .well-known reachable (200 + JSON) or follow-up recorded.
- First campaign previewed cross-client, then sent behind the confirmation; funnel shows "enviado".
</verification>

<success_criteria>

- Resend domain verified + prod API key set (D-17) → real delivery.
- Deep-link .well-known reachable so App Links/Universal Links verify (D-25).
- First trial campaign created with user copy + self-hosted images (D-13) and sent behind the irreversible-send confirmation (D-11).
  </success_criteria>

<output>
Create `.planning/phases/119-campa-a-de-sesi-n-de-prueba-freemium-reserva-self-service-si/119-07-SUMMARY.md` when done.
</output>
