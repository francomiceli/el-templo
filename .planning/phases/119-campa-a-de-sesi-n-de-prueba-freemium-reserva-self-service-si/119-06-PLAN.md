---
phase: 119-campa-a-de-sesi-n-de-prueba-freemium-reserva-self-service-si
plan: 06
type: execute
wave: 3
depends_on: ["119-04"]
files_modified:
  - el-templo-admin/src/composables/useCampaignsApi.ts
  - el-templo-admin/src/components/campaigns/CampaignFunnel.vue
  - el-templo-admin/src/pages/CampaniasPage.vue
  - el-templo-admin/src/router/routes.ts
  - el-templo-admin/src/layouts/AdminLayout.vue
autonomous: false
requirements: [D-18, D-19, D-11, D-12]
must_haves:
  truths:
    - "Admin has a dedicated Campañas section at /campanias (NOT inside analytics/Reportes) (D-19)"
    - "AdminLayout has a Campañas nav q-item (icon campaign), owner/admin-gated, consistent with who can send (D-19)"
    - "A 'Nueva campaña' button opens a creation dialog that POSTs to /api/campaigns/admin and creates a draft (D-12)"
    - "Campaign list shows name, status badge, sent date, recipient count; row click opens funnel detail (D-19)"
    - "Per-campaign funnel renders 6 stages: enviado → abierto → click → reservó → asistió → convirtió (D-18)"
    - "The 'abierto' stage carries an Apple-Mail-Privacy approximate caveat banner (D-18)"
    - "The send action shows a confirmation dialog with recipient count before the irreversible mass send (D-11)"
  artifacts:
    - path: "el-templo-admin/src/pages/CampaniasPage.vue"
      provides: "Campañas page: header + filters + tabs + list + create dialog + funnel detail"
      contains: "Campañas"
    - path: "el-templo-admin/src/components/campaigns/CampaignFunnel.vue"
      provides: "6-stage funnel chart + approximate caveat"
      contains: "Apple Mail"
    - path: "el-templo-admin/src/composables/useCampaignsApi.ts"
      provides: "listCampaigns / getCampaignFunnel / createCampaign / sendCampaign / getEligibleCount"
      contains: "getCampaignFunnel"
  key_links:
    - from: "el-templo-admin/src/router/routes.ts"
      to: "CampaniasPage.vue"
      via: "path: 'campanias'"
      pattern: "campanias"
    - from: "el-templo-admin/src/layouts/AdminLayout.vue"
      to: "/campanias"
      via: "q-item nav entry (owner/admin gated)"
      pattern: "campanias"
    - from: "el-templo-admin/src/pages/CampaniasPage.vue"
      to: "useCampaignsApi"
      via: "composable calls"
      pattern: "useCampaignsApi"
---

<objective>
Build the dedicated admin "Campañas" section (D-19): a new `CampaniasPage.vue` + route `path: 'campanias'`, a nav entry in `AdminLayout.vue`, a `useCampaignsApi` composable, and a `CampaignFunnel.vue` component visualizing the 6-stage funnel (D-18) with the Apple-Mail-Privacy approximate caveat. The page can create a draft campaign via a "Nueva campaña" dialog (POST /api/campaigns/admin, D-12) and send it behind a recipient-count confirmation (D-11, irreversible mass send). Follows the `ReportesPage.vue` / phase-117–118 admin pattern — no reinvention.

Purpose: This is the campaign control/observability home for the reusable system. It consumes the Plan 04 admin endpoints (create/list/send/funnel/eligible-count) and is how a campaign is created in the UI — making the end-to-end goal (create + send a campaign) reachable from the admin.
Output: composable, funnel component, page (list + create dialog + funnel + send confirmation), route, nav entry. Blocking checkpoint verifies the section renders + create + send gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/119-campa-a-de-sesi-n-de-prueba-freemium-reserva-self-service-si/119-CONTEXT.md
@.planning/phases/119-campa-a-de-sesi-n-de-prueba-freemium-reserva-self-service-si/119-PATTERNS.md
@.planning/phases/119-campa-a-de-sesi-n-de-prueba-freemium-reserva-self-service-si/119-UI-SPEC.md

<interfaces>
- PAGE analog: el-templo-admin/src/pages/ReportesPage.vue — header text-h5 + text-caption text-grey-7 (6-13), country(owner-only)+branch filters (18-45), q-tabs active-color="primary" indicator-color="primary" (50-66), q-tab-panels (68). Icons campaign / trending_up.
- FUNNEL analog: el-templo-admin/src/components/analytics/FunnelTab.vue — props-driven loading/data/skeleton/empty (46-55), chart.js card (58+), q-banner bg-orange-2 text-orange-10 caveat banner (21-28). NOTE: FunnelTab gates behind a comingSoon flag (8-17) — the campaign funnel does NOT need that gate; it shows live per-campaign data.
- COMPOSABLE analog: el-templo-admin/src/composables/useAnalyticsApi.ts — import { api } from 'src/boot/axios' + extractError, buildParams(filters) (24-32), ref-based loading/error. Methods to add: listCampaigns(), getCampaignFunnel(id), createCampaign(payload), sendCampaign(id), getEligibleCount().
- ROUTE analog: el-templo-admin/src/router/routes.ts lines 84-90 (analiticas/reportes) → add { path: 'campanias', component: () => import('pages/CampaniasPage.vue') }.
- NAV analog: el-templo-admin/src/layouts/AdminLayout.vue lines 84-95 — the `/analiticas` and `/reportes` q-items in the "Gestion" group. `isAdminRole` computed (line 209) = `['admin','owner'].includes(userRole.value)` — this is exactly "who can send campaigns" (owner/admin). Add a `<q-item v-if="isAdminRole" clickable v-ripple to="/campanias">` with `<q-icon name="campaign" />` and label "Campañas" right after the Reportes q-item (line 95). Do NOT add a new role computed — reuse isAdminRole.
- Backend endpoints (Plan 04): POST /api/campaigns/admin (create draft, body {name,subject,country?,heroImageUrl?,copySlots}), GET /api/campaigns/admin (list), GET /api/campaigns/admin/:id/funnel (6 stages), POST /api/campaigns/admin/:id/send, GET /api/campaigns/admin/eligible-count.
- UI-SPEC § "Admin Campañas Section Contract" is layout authority; § "Copywriting Contract": send confirmation Title "Enviar campaña" / Body "Vas a enviar este email a {N} personas. Esta acción no se puede deshacer. ¿Continuar?" / Confirm "Enviar a {N}"; "abierto" caveat "Aproximado — Apple Mail Privacy puede inflar las aperturas. El click es la métrica confiable." Status badges: draft grey / sending warning / sent positive.
- Funnel stages (D-18): enviado → abierto → click → reservó → asistió → convirtió; first + convirtió bars in $primary. WARM palette, NO blue.
- chart.js / vue-chartjs already installed (used by FunnelTab). No `any`; createLogger never console.log.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useCampaignsApi + CampaignFunnel component</name>
  <read_first>
    - el-templo-admin/src/composables/useAnalyticsApi.ts (api import, extractError, buildParams, ref state)
    - el-templo-admin/src/components/analytics/FunnelTab.vue (props/loading/skeleton/empty, chart.js card, caveat banner)
    - .planning/phases/119-.../119-UI-SPEC.md § "Admin Campañas Section Contract" + § "Copywriting Contract"
    - .planning/phases/119-.../119-PATTERNS.md § "useCampaignsApi.ts" + § "CampaignFunnel.vue"
  </read_first>
  <action>Create useCampaignsApi.ts mirroring useAnalyticsApi.ts (api + extractError + buildParams + ref loading/error): listCampaigns() → GET /api/campaigns/admin; getCampaignFunnel(id) → GET /api/campaigns/admin/:id/funnel; createCampaign(payload) → POST /api/campaigns/admin (payload = {name, subject, country?, heroImageUrl?, copySlots}); sendCampaign(id) → POST /api/campaigns/admin/:id/send; getEligibleCount() → GET /api/campaigns/admin/eligible-count. Create components/campaigns/CampaignFunnel.vue mirroring FunnelTab.vue (props-driven loading/data/skeleton/empty + chart.js bar) rendering the 6 stages enviado→abierto→click→reservó→asistió→convirtió (first + convirtió in $primary), WITHOUT the comingSoon gate. Add the orange caveat banner near the "abierto" stage with the exact UI-SPEC copy. WARM palette, NO blue. No `any`; createLogger.</action>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit && grep -q "getCampaignFunnel" src/composables/useCampaignsApi.ts && grep -q "createCampaign" src/composables/useCampaignsApi.ts && grep -q "sendCampaign" src/composables/useCampaignsApi.ts && grep -qi "Apple Mail" src/components/campaigns/CampaignFunnel.vue</automated>
  </verify>
  <acceptance_criteria>
    - useCampaignsApi.ts exports listCampaigns/getCampaignFunnel/createCampaign/sendCampaign/getEligibleCount hitting the Plan 04 endpoints.
    - CampaignFunnel.vue renders the 6 stages and includes the Apple-Mail-Privacy caveat banner with the exact UI-SPEC copy; no comingSoon gate.
    - NO blue/navy color used (warm palette).
    - `npx vue-tsc --noEmit` passes (no `any`).
  </acceptance_criteria>
  <done>Composable (incl. createCampaign) + funnel component implemented; vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: CampaniasPage + route + nav entry + create dialog + send confirmation</name>
  <read_first>
    - el-templo-admin/src/pages/ReportesPage.vue (header, country/branch filters, q-tabs/q-tab-panels skeleton)
    - el-templo-admin/src/router/routes.ts lines 84-90 (analiticas/reportes route pattern)
    - el-templo-admin/src/layouts/AdminLayout.vue lines 84-95 + line 209 (analiticas/reportes q-items, isAdminRole computed)
    - el-templo-admin/src/composables/useCampaignsApi.ts (from Task 1)
    - .planning/phases/119-.../119-UI-SPEC.md § "Admin Campañas Section Contract" + § "Copywriting Contract"
    - .planning/phases/119-.../119-PATTERNS.md § "CampaniasPage.vue (NEW) + route"
  </read_first>
  <action>Create CampaniasPage.vue copying the ReportesPage.vue skeleton: header text-h5 "Campañas" + text-caption text-grey-7 "Envíos masivos y seguimiento de conversión"; country (owner-only) + branch filters; q-tabs (active-color="primary", indicator-color="primary") with a "Campañas" list tab (q-table or card list: name, status badge draft-grey/sending-warning/sent-positive, sent date, recipient count) and a per-campaign funnel detail view embedding CampaignFunnel.vue. Add a "Nueva campaña" primary q-btn that opens a q-dialog creation form (fields: name, subject, optional country, optional heroImageUrl, copySlots = headline/body/CTA labels per UI-SPEC) and on confirm calls createCampaign(payload) then refreshes the list (the new campaign appears in 'draft'). Row click → load getCampaignFunnel(id). "Enviar campaña" primary q-btn opens a q-dialog confirmation showing the recipient count (call getEligibleCount) with the exact UI-SPEC copy (Title "Enviar campaña", Body "Vas a enviar este email a {N} personas. Esta acción no se puede deshacer. ¿Continuar?", Confirm "Enviar a {N}") before calling sendCampaign(id) (D-11, irreversible). Add the route { path: 'campanias', component: () => import('pages/CampaniasPage.vue') } to router/routes.ts mirroring analiticas/reportes. Add a nav q-item in AdminLayout.vue right after the Reportes q-item (line 95): `<q-item v-if="isAdminRole" clickable v-ripple to="/campanias">` with `<q-icon name="campaign" />` + label "Campañas" — reuse the existing isAdminRole computed (owner/admin = who can send), do NOT add a new role computed. WARM palette, NO blue. No `any`; createLogger.</action>
  <verify>
    <automated>cd el-templo-admin && npx vue-tsc --noEmit && grep -q "campanias" src/router/routes.ts && grep -q "campanias" src/layouts/AdminLayout.vue && grep -q "useCampaignsApi" src/pages/CampaniasPage.vue && grep -q "createCampaign" src/pages/CampaniasPage.vue && grep -q "no se puede deshacer" src/pages/CampaniasPage.vue</automated>
  </verify>
  <acceptance_criteria>
    - Route path 'campanias' added pointing at CampaniasPage.vue (mirrors analiticas/reportes).
    - AdminLayout.vue has a `<q-item v-if="isAdminRole" ... to="/campanias">` with icon `campaign` and label "Campañas", placed with the Gestion nav group and gated to owner/admin (same gate as send).
    - "Nueva campaña" button opens a creation dialog that calls createCampaign and creates a draft campaign (D-12); the new draft shows in the list afterward.
    - CampaniasPage shows the list (name/status badge/sent date/recipient count) + funnel detail via CampaignFunnel.vue.
    - Send button opens a confirmation dialog showing the recipient count with the exact UI-SPEC copy before calling sendCampaign (D-11).
    - Section lives standalone at /campanias, NOT under analytics/Reportes (D-19).
    - `npx vue-tsc --noEmit` passes.
  </acceptance_criteria>
  <done>CampaniasPage + route + nav entry + create dialog + send confirmation gate implemented; vue-tsc clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Dedicated admin Campañas section: nav entry, list of campaigns, "Nueva campaña" create dialog, per-campaign 6-stage funnel with the Apple-Mail-Privacy caveat, and the irreversible-send confirmation dialog.</what-built>
  <how-to-verify>
    1. Confirm the "Campañas" entry appears in the admin left nav (icon campaign) for an owner/admin and is absent for lower roles; navigate to /campanias.
    2. Confirm it is a standalone section (not under Analíticas/Reportes) with the header + country/branch filters.
    3. Click "Nueva campaña": fill name/subject/copy, confirm it creates a draft campaign that then appears in the list with a grey "draft" badge.
    4. Confirm the campaign list shows name, status badge (draft grey / sending warning / sent positive), sent date, and recipient count.
    5. Open a campaign's funnel: confirm the 6 stages (enviado → abierto → click → reservó → asistió → convirtió) render and the "abierto" caveat banner appears.
    6. Click "Enviar campaña": confirm the dialog shows the recipient count and the "no se puede deshacer" copy; cancel without sending.
    7. Confirm the warm brand (terracotta/cream), NO blue.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what renders/behaves wrong.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                              |
| --------------------------------------- | ---------------------------------------------------------------------------------------- |
| admin client → campaign admin endpoints | Owner/admin JWT + country scope; create writes a draft, send is irreversible mass action |

## STRIDE Threat Register

| Threat ID   | Category               | Component             | Disposition | Mitigation Plan                                                                                                                                                                      |
| ----------- | ---------------------- | --------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-119-06-01 | Denial of Service      | send action           | mitigate    | Confirmation dialog with recipient count gates the irreversible send (D-11); backend send is admin/owner-only + idempotent (Plan 04)                                                 |
| T-119-06-02 | Repudiation            | misleading metrics    | mitigate    | "abierto" labeled approximate (Apple Mail Privacy caveat) so opens are not over-trusted (D-18)                                                                                       |
| T-119-06-03 | Information Disclosure | funnel data           | accept      | Admin endpoints enforce role + country scope server-side (Plan 04); UI shows only scoped data                                                                                        |
| T-119-06-04 | Elevation of Privilege | Campañas nav + create | mitigate    | Nav q-item + page gated by isAdminRole (owner/admin) — same gate as send; backend re-checks role/country on POST /admin (Plan 04). UI gating is convenience, server is the authority |
| T-119-06-SC | Tampering              | npm installs          | accept      | No package installs (chart.js/vue-chartjs already in deps).                                                                                                                          |

</threat_model>

<verification>
- `npx vue-tsc --noEmit` green (both tasks).
- Checkpoint: nav entry visible to owner/admin, section renders, create dialog makes a draft, funnel + caveat, send confirmation gate.
</verification>

<success_criteria>

- Dedicated /campanias section (D-19) reachable from an owner/admin-gated nav entry, with list + per-campaign funnel (D-18).
- "Nueva campaña" create dialog persists a draft via POST /api/campaigns/admin (D-12).
- "abierto" approximate caveat present (D-18).
- Irreversible send guarded by recipient-count confirmation (D-11).
  </success_criteria>

<output>
Create `.planning/phases/119-campa-a-de-sesi-n-de-prueba-freemium-reserva-self-service-si/119-06-SUMMARY.md` when done.
</output>
</content>
