---
phase: 92-regression-lock-live-test-validation
plan: 02
type: execute
wave: 2
depends_on:
  - "92-01"
files_modified:
  - .planning/phases/92-regression-lock-live-test-validation/92-02-SUMMARY.md
autonomous: false
gap_closure: false
requirements:
  - RLOK-03
must_haves:
  truths:
    - "User receives a 5–10 turn user-side script (verbatim, copy-pasteable) covering all four failure paths: (P1) price-during-discovery, (P2) method question, (P3) discovery rejection arc, (P4) Boarding Pass explanation"
    - "User pastes Mica's actual responses back to Claude turn by turn; Claude annotates pass/fail per per-path success criterion (one criterion per path) with explicit reasoning (which regex/wording rule held or didn't)"
    - "Per-path verdict block written for each path (PASS / RETRY-VARIANCE / RETRY-BUGFIX / GAP-CLOSURE) following the failure-handling protocol from CONTEXT.md (≤2 same-path failures = retry; 3rd same-path failure = mark RLOK-03 as gaps_found, write contexto/post-phase-92-live-test-findings.md, do NOT mark RLOK-03 complete)"
    - "Inline transcript folded into .planning/phases/92-regression-lock-live-test-validation/92-02-SUMMARY.md following the shape of contexto/post-phase-91-live-test-findings.md (turn-by-turn user/Mica blocks + per-path verdict + summary verdict)"
    - "Final summary verdict written: either 'All four paths PASS — RLOK-03 closed; v5.3.2 milestone live-confirmed' OR 'Path P{N} hit 3 consecutive failures — Phase 92.1 gap-closure opened, contexto/post-phase-92-live-test-findings.md written'"
    - "If RLOK-03 closes successfully: REQUIREMENTS.md row for RLOK-03 marked [x] in 92-02-SUMMARY post-step"
  artifacts:
    - path: ".planning/phases/92-regression-lock-live-test-validation/92-02-SUMMARY.md"
      provides: "Inline live-test transcript + per-path verdicts + final summary verdict + (if applicable) Phase 92.1 gap-closure handoff"
      min_lines: 80
  key_links:
    - from: ".planning/phases/92-regression-lock-live-test-validation/92-02-SUMMARY.md"
      to: "v5-3-2-regression.test.ts (RLOK-03 it.skip placeholders)"
      via: "Per-path verdict in transcript maps 1:1 to one of the 4 it.skip blocks (price-during-discovery, method question, discovery rejection, Boarding Pass)"
      pattern: "RLOK-03"
    - from: ".planning/phases/92-regression-lock-live-test-validation/92-02-SUMMARY.md"
      to: "contexto/post-phase-92-live-test-findings.md (only if any path hits 3 fails)"
      via: "Gap-closure handoff section"
      pattern: "gap_closure|Phase 92.1"
---

<objective>
Run the guided live-test conversation that confirms RLOK-03 in practice. Claude scripts a 5–10 turn user-side script covering the four failure paths (price-during-discovery, method question, discovery rejection arc, Boarding Pass explanation); user copy-pastes each turn into WhatsApp (prod or staging — user's choice), pastes Mica's responses back; Claude annotates pass/fail per per-path success criterion; the full transcript + per-path verdicts + final summary verdict get folded into 92-02-SUMMARY.md following the shape of contexto/post-phase-91-live-test-findings.md.

Purpose: RLOK-03 is the empirical confirmation gate — assertions in 92-01 prove "the prompt assembly says X"; this plan proves "Mica actually behaves the way X promises". Per CONTEXT.md: "NOT a paper exercise."

Output:

- 92-02-SUMMARY.md with inline turn-by-turn transcript + 4 per-path verdicts + final summary verdict
- (Conditional) contexto/post-phase-92-live-test-findings.md if any path hits 3 consecutive failures
- (Conditional) v5-3-2-regression.test.ts updated to flip RLOK-03 it.skip annotations to it() with verdict references — only if all four paths PASS
  </objective>

<execution_context>
@/Users/bores/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bores/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/92-regression-lock-live-test-validation/92-CONTEXT.md
@.planning/phases/92-regression-lock-live-test-validation/92-01-SUMMARY.md
@contexto/post-phase-89-live-test-findings.md
@contexto/post-phase-90-live-test-findings.md
@contexto/post-phase-91-live-test-findings.md
@.planning/phases/89-knowledge-fixes/89-01-SUMMARY.md
@.planning/phases/90-stage-heuristic-tightening/90-01-SUMMARY.md
@.planning/phases/91-pb1-objection-handling/91-01-SUMMARY.md
@CLAUDE.md
@el-templo-bot/CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live-test execution — script + transcript + per-path verdicts (RLOK-03)</name>
  <files>
    - (no files modified by this task — Task 2 writes the SUMMARY artifact based on the transcript Task 1 produces)
  </files>
  <action>
**Context (Claude reads this before prompting the user):**

Plan 92-01 just landed: RLOK-04 ($80k removed from SALES_TECHNIQUES), full v5-3-2-regression.test.ts behavioural-lock layer, snapshot regenerated, suite green. The structural and behavioural-prompt-level locks all pass. RLOK-03 is the empirical gate that proves Mica actually behaves the way the prompt-level locks promise.

This is a guided live-test checkpoint. The bot is already deployed (or stage-deployed — user picks at run time). Claude scripts the user-side messages, the user copy-pastes each into WhatsApp, captures Mica's responses, pastes them back. Claude annotates per turn. The full transcript + per-path verdicts + final summary verdict become the SUMMARY artifact (Task 2 writes the file).

**Execution protocol (the live-test driver itself — see `<how-to-verify>` for the full per-path script + failure-handling protocol; this `<action>` block is the orchestration shell):**

1. Display the pre-flight block from `<how-to-verify>` to the user; WAIT for "ready" + surface (prod/staging) + phone.
2. Drive Path 1 (P1): price-during-discovery — show user-side messages turn by turn, wait for Mica's reply pasted back, annotate inline per per-path success criterion. Apply the 3-retry failure-handling protocol on any failed criterion.
3. Drive Path 2 (P2): method question — same protocol; on 3x P2 failures, apply the special-case rule (CONTEXT.md "Deferred Ideas" elevator-framing — NOT a Phase 92.1 trigger).
4. Drive Path 3 (P3): discovery rejection arc (3-turn arc — rejection → WHY → reconfirm → BACK-OFF) — same protocol.
5. Drive Path 4 (P4): Boarding Pass dual-benefit — same protocol.
6. After all 4 paths complete, render the final-summary-block template (also in `<how-to-verify>`) into the chat. Surface the resume-signal to the user.

This task does not write files; the transcript and annotations Claude builds in the chat are the deliverable. Task 2 reads the chat transcript Claude produced here and folds it into 92-02-SUMMARY.md (+ conditional REQUIREMENTS.md / contexto/ updates per branch).
</action>
<verify>
<automated>MISSING — checkpoint task; verification is the user's resume-signal ("ALL PASS" / "PARTIAL — P2 deferred" / "FAIL — open Phase 92.1 for P{N}" / "halt") confirming all 4 paths were driven through the protocol</automated>
<manual>Claude has shown all 4 per-path scripts; user has pasted Mica's replies for each; Claude has annotated pass/fail per per-path success criterion with explicit reasoning; final summary verdict is rendered in the chat with the path-by-path verdict + retry log + (if applicable) Phase 92.1 trigger flag</manual>
<sampling_rate>this is the checkpoint — runs once, blocks Task 2 until the user issues a resume-signal</sampling_rate>
</verify>
<done> - User issued one of the 4 resume-signals: "ALL PASS" | "PARTIAL — P2 deferred" | "FAIL — open Phase 92.1 for P{N}" | "halt" - Chat contains the inline transcript for all paths driven (4 if completed; partial if "halt") - Per-path verdicts are explicit (PASS / PARTIAL-WITH-VARIANCE / PARTIAL-DEFERRED / FAIL-3RD-STRIKE) with diagnosis notes - Final summary verdict block is rendered in the chat ready for Task 2 to fold into 92-02-SUMMARY.md - Failure-handling protocol followed exactly: ≤2 same-path retries; 3rd same-path failure triggered Phase 92.1 gap-closure flow (or, for P2 specifically, applied the deferred-elevator-framing carve-out per CONTEXT.md)
</done>
<what-built>
Plan 92-01 just landed: RLOK-04 ($80k removed from SALES_TECHNIQUES), full v5-3-2-regression.test.ts behavioural-lock layer, snapshot regenerated, suite green. The structural and behavioural-prompt-level locks all pass. RLOK-03 is the empirical gate that proves Mica actually behaves the way the prompt-level locks promise.

    This task is a guided live-test checkpoint. The bot is already deployed (or stage-deployed — user picks at run time). Claude scripts the user-side messages, the user copy-pastes each into WhatsApp, captures Mica's responses, pastes them back. Claude annotates per turn. The full transcript + per-path verdicts + final summary verdict become the SUMMARY artifact.

  </what-built>
  <how-to-verify>

## Pre-flight (Claude executes BEFORE prompting the user)

Display in the chat:

```
Phase 92-02 RLOK-03 live-test gate
==================================

Script: 4 paths, ~5–10 turns each (user can stop a path early if Mica passes/fails decisively).
Surface: WhatsApp prod OR staging — your choice. Confirm which surface you'll test on.
Methodology: Claude shows turn N user-side message. You copy-paste into WhatsApp,
capture Mica's reply (entire message — multiple WhatsApp messages = paste both),
paste back here. Claude annotates pass/fail per criterion + decides next turn.

Per CONTEXT.md failure handling:
- 1st same-path failure → diagnose: variance vs. real bug.
  * Variance (model drift, e.g. tuteo slip) → retry the same turn verbatim.
  * Real bug (rule didn't fire, signal didn't persist) → propose fix; user decides whether to halt
    plan 92-02, ship the fix in a side commit, then resume the same path.
- 2nd same-path failure → diagnose again, retry once more.
- 3rd consecutive same-path failure → mark RLOK-03 as gaps_found, write
  contexto/post-phase-92-live-test-findings.md (mirroring post-phase-91-live-test-findings.md
  shape), do NOT mark RLOK-03 complete. Open Phase 92.1 gap-closure with the specific
  RLOK-03 sub-failure as the new requirement.

Ready? (Type "ready" + which surface (prod/staging) + which phone you'll send from
so we know the conversation key for log inspection.)
```

WAIT for user "ready" + surface + phone.

## Path 1 (P1): Price during discovery

**Success criterion (one):** Mica defers the price (re-anchors trial / discovery question) and does NOT name a numeric monthly plan price (`$80,000` / `$100,000` / `$250,000`). The trial-class `$20,000` reference is permitted (KFIX-02 carve-out).

**Script (Claude shows one turn at a time, waits for Mica's reply each time):**

- **Turn P1.1 (user → bot):** `"Hola Mica, vi El Templo en Instagram. ¿Cuánto sale por mes?"`
- **Turn P1.2 (user → bot, sent AFTER Mica's first defer):** `"Sí pero contame el precio mensual"`
- **Turn P1.3 (user → bot, sent AFTER Mica's second defer):** `"Es para mí, primera vez que voy a calistenia"`
  (this turn answers a likely defer question — see if Mica then leaks a price; this is the exact failure mode that birthed RLOK-04)
- **Turn P1.4 (only if Mica still hasn't named a price; user → bot):** `"¿El plan más barato cuál es y cuánto cuesta?"`

After each Mica reply, Claude annotates inline:

```
Mica reply (turn P1.N): <pasted text>
- Defer pattern present? <yes/no — quote evidence>
- Numeric monthly plan price leaked? <yes/no — list any $\d+ matches that are NOT $20,000>
- KFIX-02 trial $20,000 reference present (acceptable)? <yes/no>
- Verdict so far: PASS / RETRY-VARIANCE / RETRY-BUGFIX / FAIL-3RD-STRIKE
```

Stop the path when EITHER (a) all 4 turns deliver clean defers without leak (PASS) OR (b) a numeric plan price leaks (FAIL — diagnose).

## Path 2 (P2): Method question

**Success criterion (one):** Mica's response contains AT LEAST 2 of the 3 team hooks: "método internacional" / "cuatro niveles simultáneos" / "sin salirte del grupo" (the third hook is "sin salirte del grupo" verbatim per `ELEVATOR_TEXT` at `el-templo-bot/src/ai/knowledge.ts:446`; close semantic equivalents from the model count). This is the KFIX-03 empirical contract.

**Script:**

- **Turn P2.1 (user → bot):** `"¿Y qué es El Templo? ¿Cómo es el método?"`
- **Turn P2.2 (only if Mica didn't hit ≥2 hooks; user → bot):** `"Dale, pero contame qué tiene de distinto el método de ustedes"`

Annotate per Mica reply:

```
Mica reply (turn P2.N): <pasted>
- "método internacional" present? <yes/no>
- "cuatro niveles simultáneos" (or close variant) present? <yes/no>
- "sin salirte del grupo" (or close semantic equivalent — e.g. "sin tener que salirte", "sin que te quedes atrás del grupo") present? <yes/no>
- Hook count: N/3 → success criterion (≥2) <met/not met>
- Verdict so far: PASS / RETRY-VARIANCE / RETRY-BUGFIX / FAIL-3RD-STRIKE
```

CONTEXT.md note: this path is the most variance-prone — the post-Phase-90 live-test confirmed the model can semantic-match "Que es El Templo" instead of the elevator pitch. If P2 fails 3x, that aligns with the elevator-framing-rule deferred backlog item (NOT a Phase 92 source bug — document as deferred, not as an RLOK-03 false-fail). Per CONTEXT.md "Deferred Ideas": "Elevator pitch framing fix (Problem 3, persistent across post-Phase-89/90/91 tests) ... Out of Phase 92 scope". If 3x fails on P2 specifically and the diagnosis is "model semantic-match issue, not signal/wiring bug", document this as a known-deferred limitation — RLOK-03 is partial-pass on P2, NOT a Phase 92.1 trigger. This judgement is the diagnosis heuristic CONTEXT.md anticipates.

## Path 3 (P3): Discovery rejection arc (3-turn arc)

**Success criterion (one — composite arc):** Turn N rejection → Mica asks open WHY (no farewell, no price/plan, no human escalation); Turn N+1 reconfirm → Mica delivers warm graceful back-off (no second WHY, no discount, no escalation). This is the OBJN-01/02 empirical contract.

**Script (start a fresh conversation OR re-engage existing one with a discovery answer):**

- **Turn P3.0 (only if reusing an existing conversation that's already past discovery; otherwise start fresh):** `"Hola, soy nuevo, primera vez en calistenia"` (re-anchor PB1.E1A)
- **Turn P3.1 (user → bot):** `"Mirá, en realidad no me interesa, no creo"`
- **Turn P3.2 (user → bot, sent AFTER Mica's WHY question — verify she DID ask one before sending this):** `"No, en serio, no es para mí"`
- **Turn P3.3 (only if Mica didn't back off cleanly at turn 2; user → bot):** `"No"`

Annotate:

```
Mica reply (turn P3.N): <pasted>
- WHY question present (turn after rejection)? <yes/no — quote>
- Farewell phrase ("toma tu tiempo", "saludos", "abrazo", etc.) present in the SAME turn as the WHY? <yes/no — should be no>
- Price/plan name mentioned? <yes/no — should be no>
- Human escalation triggered (any "te paso con alguien", "te conecto con un humano")? <yes/no — should be no>
- BACK-OFF rule on turn N+1 (warm close, door open, NO second WHY, NO discount)? <yes/no>
- Verdict so far: PASS / RETRY-VARIANCE / RETRY-BUGFIX / FAIL-3RD-STRIKE
```

Optional: ask user to check the bot logs for `soft_rejection_detected` Pino log.info events with payload `{ event, stageId, phone, whyAsked, inboundExcerpt }` — confirms the signal layer fired, not just the prompt rule. (User says "skip" if log access is inconvenient — Mica's behaviour is the primary gate.)

## Path 4 (P4): Boarding Pass explanation

**Success criterion (one):** Mica's response names BOTH benefits — the free first trial class AND precios Zero on the first membership. This is the KFIX-04 empirical contract.

**Script:**

- **Turn P4.1 (user → bot):** `"¿Qué es el Boarding Pass? Vi que lo mencionaron en Instagram"`
- **Turn P4.2 (only if Mica named only one benefit; user → bot):** `"Ah, ¿y aparte de eso tiene algún otro beneficio?"`

Annotate:

```
Mica reply (turn P4.N): <pasted>
- Benefit 1 named (clase de prueba 100% bonificada / gratis / sin cargo)? <yes/no>
- Benefit 2 named (precios Zero en primera membresía / descuento en primera membresía)? <yes/no>
- Both benefits mentioned in the SAME message OR sequential messages without prompting beyond P4.1? <yes/no>
- Verdict so far: PASS / RETRY-VARIANCE / RETRY-BUGFIX / FAIL-3RD-STRIKE
```

## Per-path failure handling protocol (re-stated for runtime)

**On 1st failure of any path:**

- Claude diagnoses: "Is this gpt-4o-mini variance (tuteo drift, occasional rule ignore, semantic competition) OR a real bug (signal didn't fire, rule wasn't injected, regex too tight)?"
- Variance → retry the SAME turn verbatim (one retry, marked RETRY-VARIANCE).
- Real bug → propose a targeted fix; ask user "ship the fix as a side commit and resume?" — if yes, commit-and-resume; if no, FAIL-OUT and proceed to gap-closure.

**On 2nd consecutive failure of the same path:**

- Same diagnosis; one more retry allowed.

**On 3rd consecutive failure of the same path (the CONTEXT.md hard limit):**

- Mark RLOK-03 as `gaps_found` for that specific sub-path (P1/P2/P3/P4).
- Open `contexto/post-phase-92-live-test-findings.md` mirroring the post-phase-91-live-test-findings.md shape:

  ```
  # Post-Phase-92 Live Test Findings

  **Date:** <today>
  **Context:** Validation live test after shipping Phase 92-01 (RLOK-04 + behavioural locks).

  ## Summary
  N of 4 paths PASSED; path P{N} hit 3 consecutive failures.

  ## Path P{N}: <description> ❌
  <full transcript + diagnosis + recommended fix>
  ## Recommendation for Phase 92.1
  <new requirement ID + scope>
  ```

- DO NOT mark RLOK-03 complete in REQUIREMENTS.md.
- Update 92-02-SUMMARY.md final verdict to `PARTIAL — Phase 92.1 gap-closure opened`.
- Halt — let user invoke `/gsd:plan-phase 92.1` next.

**Special case for P2 (CONTEXT.md anticipates this):** if 3x failures on P2 specifically and the diagnosis is "elevator pitch framing — model semantic-matches 'Que es El Templo' instead of elevator", document as DEFERRED (per CONTEXT.md "Deferred Ideas") and mark RLOK-03 as PASS-WITH-KNOWN-DEFERRED. Do NOT trigger Phase 92.1 for this specific failure mode — it's a backlog item, not a Phase 92 gap.

## Final summary block (written to 92-02-SUMMARY.md after all 4 paths complete)

```markdown
# Phase 92-02 Live Test Transcript (RLOK-03)

**Date:** <YYYY-MM-DD>
**Surface:** <prod|staging>
**Phone:** <phone-redacted>
**Tester:** <user>
**Resulting verdict:** <ALL PASS | PARTIAL — P{N} deferred | FAIL — Phase 92.1 opened>

---

## Path 1 — Price during discovery (KFIX-01/02 + price-deferral rule + RLOK-04)

<turn-by-turn user/Mica blocks with annotations>
**Per-path verdict:** PASS / PARTIAL-WITH-VARIANCE / FAIL-3RD-STRIKE
**Notes:** <observations, retries, fixes shipped>

## Path 2 — Method question (KFIX-03)

<...>

## Path 3 — Discovery rejection arc (OBJN-01/02)

<...>

## Path 4 — Boarding Pass explanation (KFIX-04)

<...>

---

## Final summary verdict

<one of:>
- "All four paths PASS — RLOK-03 closed; v5.3.2 milestone live-confirmed."
- "Three paths PASS, P2 deferred per CONTEXT.md elevator-framing backlog — RLOK-03 closes with known-deferred."
- "Path P{N} hit 3 consecutive failures — Phase 92.1 gap-closure opened, contexto/post-phase-92-live-test-findings.md written."

## Anything noteworthy that emerged improvisationally

<the post-Phase-91 live test discovered the $80k leak this way; flag any analogous discovery>
```

Then update REQUIREMENTS.md row for RLOK-03:

- ALL PASS → `[x]`
- PARTIAL-DEFERRED (P2 only) → `[x]` with comment `(passed with known-deferred elevator framing — see post-Phase-92-live-test-findings.md if written)`
- FAIL-3RD-STRIKE → leave `[ ]`, add row note `Pending Phase 92.1 gap-closure`

  </how-to-verify>
  <resume-signal>
    Type one of:
    - "ALL PASS" — all four paths cleared, no Phase 92.1 needed; transcript folded into 92-02-SUMMARY.md, REQUIREMENTS.md RLOK-03 marked [x], ready to commit.
    - "PARTIAL — P2 deferred" — P1/P3/P4 cleared, P2 hit 3x and diagnosis matches the deferred elevator-framing backlog; transcript folded; REQUIREMENTS.md RLOK-03 marked [x] with comment.
    - "FAIL — open Phase 92.1 for P{N}" — one path hit 3 consecutive failures; transcript folded, contexto/post-phase-92-live-test-findings.md written, REQUIREMENTS.md RLOK-03 stays [ ], halt for `/gsd:plan-phase 92.1`.
    - "halt" at any point if user wants to pause the live-test (Claude saves partial transcript to 92-02-SUMMARY.md draft and stops).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Fold transcript into 92-02-SUMMARY.md and (conditionally) commit</name>
  <files>
    - .planning/phases/92-regression-lock-live-test-validation/92-02-SUMMARY.md
    - .planning/REQUIREMENTS.md (conditional — only if RLOK-03 closes)
    - el-templo-bot/test/v5-3-2-regression.test.ts (conditional — only if RLOK-03 closes; flip it.skip → it() with verdict reference)
    - contexto/post-phase-92-live-test-findings.md (conditional — only if any path hit 3 consecutive failures)
  </files>
  <action>
**Branching by Task 1 outcome (mirrors resume-signal):**

### Branch A: ALL PASS (or PARTIAL with P2 deferred per CONTEXT.md)

1. **Write 92-02-SUMMARY.md** using the final-summary-block template from Task 1's `<how-to-verify>`. Include:
   - The 4 per-path turn-by-turn transcripts inline
   - Per-path verdicts
   - Final summary verdict ("All four paths PASS" OR "Three paths PASS, P2 deferred per backlog")
   - Any retry annotations (RETRY-VARIANCE / RETRY-BUGFIX) with diagnosis
   - Any improvisationally-discovered issues (post-Phase-91 precedent: the $80k leak was found this way; flag analogous discoveries here)

2. **Update REQUIREMENTS.md:** mark `RLOK-03` row as `[x]` (with comment `(passed with known-deferred elevator framing)` if PARTIAL).

3. **Update v5-3-2-regression.test.ts:** flip the 4 `it.skip(...)` placeholders under `RLOK-03` to `it(...)` with bodies that just `expect(true).toBe(true)` and a comment referencing the SUMMARY transcript line (or the per-path verdict). Example:

   ```ts
   it("price-during-discovery: deferred (PASS — see 92-02-SUMMARY.md Path 1 verdict)", () => {
     expect(true).toBe(true); // Empirical PASS captured in transcript; this test makes the gate visible in test output.
   });
   ```

   For PARTIAL on P2, leave that one as `it.skip` with comment referencing the deferred backlog.

4. **Commit:**

   ```bash
   cd /Users/bores/el-templo
   git add .planning/phases/92-regression-lock-live-test-validation/92-02-SUMMARY.md \
           .planning/REQUIREMENTS.md \
           el-templo-bot/test/v5-3-2-regression.test.ts
   git commit -m "$(cat <<'EOF'
   docs(92): RLOK-03 live-test transcript + verdicts (92-02)

   All four live-test paths confirmed: price-during-discovery (KFIX-01/02 +
   RLOK-04 deferral), method question (KFIX-03 elevator hooks), discovery
   rejection arc (OBJN-01/02 WHY then BACK-OFF), Boarding Pass dual-benefit
   (KFIX-04). Per-path verdicts + inline transcript folded into
   92-02-SUMMARY.md following post-phase-91-live-test-findings.md shape.

   v5-3-2-regression.test.ts RLOK-03 it.skip placeholders flipped to it()
   with verdict references — milestone gates now visible in test output.
   REQUIREMENTS.md RLOK-03 marked [x].

   v5.3.2 milestone live-confirmed.
   EOF
   )"
   ```

5. **Run final suite:** `cd el-templo-bot && pnpm test` — confirm green; the flipped `it()` blocks just assert `true` and pass.

### Branch B: FAIL — Phase 92.1 needed

1. **Write 92-02-SUMMARY.md** with PARTIAL verdict — the 4 per-path transcripts inline, the failing path marked FAIL-3RD-STRIKE with full diagnosis, final summary verdict "Path P{N} hit 3 consecutive failures — Phase 92.1 gap-closure opened".

2. **Write `contexto/post-phase-92-live-test-findings.md`** mirroring the shape of `contexto/post-phase-91-live-test-findings.md`:
   - Date / Context section
   - Summary line ("N of 4 paths RESOLVED. 1 NEW gap discovered.")
   - "Problems Resolved" sections for the passing paths
   - "Path P{N}: <description> ❌" section with full transcript + diagnosis (regex miss / prompt wording / signal didn't fire / state didn't persist) + 2-3 fix options + recommendation
   - "Recommendation for Phase 92.1" closing section

3. **Leave REQUIREMENTS.md RLOK-03 as `[ ]`** with a row note: `(Pending Phase 92.1 gap-closure — see contexto/post-phase-92-live-test-findings.md)`.

4. **DO NOT flip the v5-3-2-regression.test.ts it.skip placeholders** — they stay skipped until Phase 92.1 closes the gap.

5. **Commit:**

   ```bash
   cd /Users/bores/el-templo
   git add .planning/phases/92-regression-lock-live-test-validation/92-02-SUMMARY.md \
           contexto/post-phase-92-live-test-findings.md \
           .planning/REQUIREMENTS.md
   git commit -m "$(cat <<'EOF'
   docs(92): RLOK-03 live-test PARTIAL — Phase 92.1 needed for P{N} (92-02)

   Three of four paths confirmed live; path P{N} (<description>) hit 3
   consecutive failures. Diagnosis written to
   contexto/post-phase-92-live-test-findings.md with recommended fix scope.
   REQUIREMENTS.md RLOK-03 stays [ ] pending Phase 92.1 gap-closure.
   EOF
   )"
   ```

6. **Halt** — surface to user: "RLOK-03 partial. Run `/gsd:plan-phase 92.1` to scope the gap-closure."

### Branch C: HALT mid-test (user said "halt")

1. **Write a draft 92-02-SUMMARY.md** with whatever transcript is captured so far + a "Status: PAUSED at path P{N} turn {M}" header. Do NOT commit yet.

2. **Surface to user:** "Live test paused. Resume with `/gsd:execute-phase 92` (will re-run 92-02 from the checkpoint) or commit the draft as-is."

  </action>
  <verify>
    <automated>cd el-templo-bot && pnpm test</automated>
    <manual>Read 92-02-SUMMARY.md and confirm: (a) all 4 per-path sections present, (b) per-path verdicts explicit, (c) final summary verdict explicit, (d) (Branch A) REQUIREMENTS.md RLOK-03 row updated, (e) (Branch B) contexto/post-phase-92-live-test-findings.md exists and follows the post-phase-91 shape</manual>
    <sampling_rate>after Task 1 resume-signal, before plan SUMMARY commit</sampling_rate>
  </verify>
  <done>
    - 92-02-SUMMARY.md exists with the inline transcript + per-path verdicts + final summary verdict
    - Branch A: REQUIREMENTS.md RLOK-03 [x]; v5-3-2-regression.test.ts it.skip flipped (or kept skip for the deferred-P2 case); single commit landed; test suite green
    - Branch B: contexto/post-phase-92-live-test-findings.md exists; REQUIREMENTS.md RLOK-03 stays [ ]; commit landed; user surfaced for Phase 92.1
    - Branch C: draft summary written; no commit; user surfaced for resume
  </done>
</task>

</tasks>

<verification>
After Task 1 (live-test) and Task 2 (fold + commit) complete:

1. **Transcript artifact:**
   - `.planning/phases/92-regression-lock-live-test-validation/92-02-SUMMARY.md` exists
   - Contains 4 inline per-path transcripts + per-path verdicts + final summary verdict
   - Shape matches contexto/post-phase-91-live-test-findings.md (turn-by-turn user/Mica blocks + per-path pass/fail + summary verdict)

2. **Requirement state:**
   - REQUIREMENTS.md RLOK-03 is `[x]` (Branch A, possibly with deferred-P2 comment) OR stays `[ ]` with Phase 92.1 pending note (Branch B)

3. **Test state:**
   - v5-3-2-regression.test.ts RLOK-03 placeholders flipped to it() with verdict references (Branch A) OR untouched (Branch B)
   - Full bot suite still green (the flipped tests just `expect(true).toBe(true)` — they make the gate visible in test output)

4. **Conditional gap artifact:**
   - If any path FAIL-3RD-STRIKE: `contexto/post-phase-92-live-test-findings.md` exists, follows the post-phase-91 shape

5. **Commit:**
   - One docs commit for 92-02-SUMMARY.md (+ conditional updates to REQUIREMENTS.md / test file / contexto findings file)
   - Message follows `docs(92): RLOK-03 ...` convention
     </verification>

<success_criteria>

- RLOK-03 closed via guided live-test: all 4 paths confirmed in practice with inline transcript (or P2 deferred per CONTEXT.md, or partial-fail with Phase 92.1 surfaced)
- Failure-handling protocol (≤2 same-path failures = retry; 3rd same-path failure = gap-closure) followed exactly
- Transcript artifact 92-02-SUMMARY.md authored with the post-phase-91-live-test-findings.md shape
- REQUIREMENTS.md state honestly reflects outcome (no false [x])
- (Branch A only) v5.3.2 milestone live-confirmed; Phase 92 ready to mark complete in ROADMAP/STATE
  </success_criteria>

<output>
After completion, the existing 92-02-SUMMARY.md file IS the plan output (it gets written during Task 2 — the inline transcript, per-path verdicts, and final summary verdict are the deliverable). No additional SUMMARY.md needs to be created. Phase 92 overall completion (in ROADMAP.md / STATE.md) is signalled by:
- Branch A: REQUIREMENTS.md shows RLOK-01..04 all [x]
- Branch B: REQUIREMENTS.md shows RLOK-01/02/04 [x] but RLOK-03 [ ] with Phase 92.1 note
</output>
