---
phase: 116-refresh-tokens-auth
plan: 05
type: execute
wave: 3
depends_on: ["116-02"]
files_modified:
  - el-templo-api/test/helpers.ts
  - el-templo-api/test/auth/refresh-tokens.test.ts
autonomous: true
requirements: [Req14]
must_haves:
  truths:
    - "refresh_tokens está en TABLES_TO_CLEAN para aislamiento inter-test"
    - "Test cubre refresh exitoso + rotación (viejo revoked_at + replaced_by_id apunta al nuevo, sliding 30d)"
    - "Test cubre reuse detection: refresh rotado reusado → 401 + toda la familia del user revocada"
    - "Test cubre revocación en logout (idempotente), change-password (revoca todos menos device actual), delete-account"
    - "Test verifica acceso dual: token legacy (7d) y accessToken (30m) ambos pasan /auth/me (Req 8)"
  artifacts:
    - path: "el-templo-api/test/helpers.ts"
      provides: "refresh_tokens agregado a TABLES_TO_CLEAN"
      contains: "refreshTokens"
    - path: "el-templo-api/test/auth/refresh-tokens.test.ts"
      provides: "suite de integración de refresh/rotación/reuse/revocación/dual-access"
      contains: "/auth/refresh"
  key_links:
    - from: "el-templo-api/test/auth/refresh-tokens.test.ts"
      to: "POST /api/auth/refresh, /logout, /me/change-password, /me/delete-account"
      via: "app.inject contra eltemplo_test"
      pattern: "app.inject"
    - from: "el-templo-api/test/auth/refresh-tokens.test.ts"
      to: "schema.refreshTokens"
      via: "query directa para verificar revoked_at/replaced_by_id/expires_at"
      pattern: "refreshTokens"
---

<objective>
Escribir la suite de tests de integración (Req 14) contra `eltemplo_test` que prueba los flujos críticos de refresh tokens: refresh exitoso + rotación, reuse detection con revocación de familia, revocación en logout (idempotente), change-password (revoca todos menos emite par nuevo para el device actual), delete-account, y verificación dual de access (token legacy + accessToken ambos pasan `/auth/me`, Req 8). Agrega `refresh_tokens` a `TABLES_TO_CLEAN` para aislamiento inter-test.

Purpose: Es el cierre verificable de la fase del lado API — convierte el contrato de Plans 01/02 en garantías reproducibles (`pnpm test` verde). Depende de que los endpoints existan (Plan 02) y la migración 0125 esté aplicada en eltemplo_test (checkpoint blocking de Plan 02).
Output: helpers con cleanup de refresh_tokens + suite `test/auth/refresh-tokens.test.ts` verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-refresh-tokens-auth/116-SPEC.md
@.planning/phases/116-refresh-tokens-auth/116-CONTEXT.md
@CLAUDE.md
@.planning/phases/116-refresh-tokens-auth/116-02-SUMMARY.md

<interfaces>
<!-- Contrato del API a testear (LOCKED, de Plans 01/02). -->

POST /api/auth/login -> 200 { token, accessToken, refreshToken, user, ... }
POST /api/auth/register -> 200/201 { token, accessToken, refreshToken, user, ... }
POST /api/auth/refresh -> body { refreshToken } -> 200 { accessToken, refreshToken } | 401
POST /api/auth/logout -> body { refreshToken } -> 200 siempre (idempotente, D-04)
POST /api/auth/me/change-password -> body { currentPassword, newPassword } (Bearer access) -> 200 { message, accessToken, refreshToken } (D-01)
POST /api/auth/me/delete-account -> (Bearer access) revoca refresh internamente (D-05)
GET /api/auth/me -> (Bearer access | token legacy) -> 200

Helpers existentes (test/helpers.ts):

- createTestApp(), getAuthToken(app, email, password) [devuelve token legacy], registerTestUser(...) [defaults dni/phone/firstName/lastName].
- TABLES_TO_CLEAN (línea 144) + cleanup en bucle (línea 208). Incluye schema.memberLogins (analog FK->users) — agregar schema.refreshTokens junto a las leaf tables que referencian users.
- Import `* as schema from "../src/db/schema"` y drizzle `eq`/`sql` ya disponibles.

Schema refresh_tokens (Plan 01): refreshTokens con userId, tokenHash, expiresAt, revokedAt, replacedById, createdAt.
NOTA: los tests reciben el refresh PLANO en el response; para verificar estado en DB se consulta por user_id (o por hash sha256 del plano) — el plano no está en DB.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar refresh_tokens a TABLES_TO_CLEAN</name>
  <read_first>
    - el-templo-api/test/helpers.ts (líneas 144-210 — TABLES_TO_CLEAN + bucle de truncate; orden FK-aware)
    - el-templo-api/src/db/schema/refresh-tokens.ts (Plan 01 — export refreshTokens)
  </read_first>
  <action>
    En `el-templo-api/test/helpers.ts` agregar `schema.refreshTokens` al array `TABLES_TO_CLEAN`, ubicado junto a `schema.memberLogins` (ambas son leaf tables que referencian `users` con FK; deben limpiarse antes que `users`). El self-FK `replaced_by_id` con `ON DELETE SET NULL` no impide el truncate/delete, pero respetar el orden existente FK-aware. No tocar nada más del helper.
  </action>
  <verify>
    <automated>cd el-templo-api && grep -c "schema.refreshTokens" test/helpers.ts ; pnpm tsc --noEmit 2>&1 | grep -v '^#' | grep -ci 'helpers.ts'</automated>
  </verify>
  <acceptance_criteria>
    - `schema.refreshTokens` aparece en TABLES_TO_CLEAN (primer comando >=1)
    - tsc limpio para helpers.ts (segundo comando = 0)
  </acceptance_criteria>
  <done>refresh_tokens se limpia entre tests; helper type-check limpio.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Suite de integración refresh/rotación/reuse/revocación/dual-access</name>
  <read_first>
    - el-templo-api/test/auth/auth.test.ts (patrón describe/it + app.inject + JSON.parse del body)
    - el-templo-api/test/auth/register.test.ts (patrón de registro + cleanup en beforeEach/afterAll)
    - el-templo-api/test/helpers.ts (createTestApp, registerTestUser, getAuthToken, cleanup)
    - el-templo-api/src/modules/auth/refresh-token-service.ts (Plan 01 — comportamiento de rotate/reuse)
    - 116-SPEC.md Req 2,3,4,6,7,8,12 (acceptance criteria por flujo)
    - 116-CONTEXT.md D-01 (change-password), D-04 (logout idempotente), D-05 (delete-account)
  </read_first>
  <behavior>
    - Refresh exitoso + rotación (Req 2,6): login → /auth/refresh con el refreshToken → 200 con accessToken+refreshToken nuevos; query DB: el refresh viejo tiene revoked_at != null y replaced_by_id apuntando al nuevo; el nuevo tiene expires_at ≈ now+30d (sliding). El accessToken nuevo pasa GET /auth/me (200).
    - Reuse detection (Req 3): login → refresh una vez (obtiene token B, revoca A) → reusar A (ya rotado) → 401; query DB muestra TODOS los refresh del user con revoked_at seteado (familia revocada).
    - Logout idempotente (Req 4, D-04): login → /auth/logout con refreshToken → 200; refresh con ese token → 401; segundo /auth/logout con el mismo (ya revocado) → 200 igual (idempotente).
    - Change-password (Req 12, D-01): login en dos sesiones A y B (dos /auth/login → dos refresh) → change-password desde A (Bearer access de A) → 200 con { message, accessToken, refreshToken } nuevo para A; refresh desde B → 401 (revocado); el par nuevo de A funciona en /auth/refresh.
    - Delete-account (D-05): login → delete-account (Bearer access) → tras eso, refresh con el refreshToken del user → 401 (todos revocados).
    - Dual access (Req 8): login → usar el `token` legacy (7d) en GET /auth/me → 200; usar el `accessToken` (30m) en GET /auth/me → 200. Ambos verifican.
  </behavior>
  <action>
    Crear `el-templo-api/test/auth/refresh-tokens.test.ts` siguiendo el patrón de `auth.test.ts` (createTestApp en beforeAll, cleanup en beforeEach/afterEach según el patrón del archivo, app.close en afterAll, app.inject + JSON.parse). Implementar un `describe` con `it`s por cada flujo de `<behavior>`. Para verificación de estado en DB, importar `* as schema` y `eq`/`isNotNull`/`isNull` de drizzle y consultar `schema.refreshTokens` filtrando por `userId` (obtener el userId del registro/login). Para verificar rotación, consultar la fila por hash: importar `createHash` de `node:crypto` y buscar por `tokenHash = sha256(plano)`, asertando `revokedAt != null` y `replacedById != null`. Para sliding 30d, asertar que `expires_at` del nuevo cae dentro de una ventana razonable (now+30d ± algunos minutos). Usar `registerTestUser`/`getAuthToken` de helpers para fixtures; el password de cambio usa el del fixture. NO usar `console.*`; sin `any` (tipar bodies con interfaces locales o narrowing de `unknown`). Asegurar aislamiento: cada `it` registra su propio user (emails únicos vía el patrón de helpers).
  </action>
  <verify>
    <automated>cd el-templo-api && pnpm test test/auth/refresh-tokens.test.ts 2>&1 | tail -25 ; grep -c ": any" test/auth/refresh-tokens.test.ts ; grep -c "console\." test/auth/refresh-tokens.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test test/auth/refresh-tokens.test.ts` pasa todos los `it` (refresh+rotación, reuse, logout idempotente, change-password, delete-account, dual-access)
    - Rotación verificada con query DB: viejo revoked_at + replaced_by_id; nuevo expires_at ≈ now+30d
    - Reuse detection verificada: reuso → 401 + familia completa revocada
    - Dual access verificado: token legacy y accessToken ambos pasan /auth/me (Req 8)
    - Sin `any` ni `console.` (segundo y tercer comandos = 0)
  </acceptance_criteria>
  <done>Suite de integración verde cubriendo los 6 flujos críticos + dual-access.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                              |
| ---------------------------- | -------------------------------------------------------- |
| test runner -> eltemplo_test | Suite contra MySQL real; aislamiento vía TABLES_TO_CLEAN |
| test -> /auth/\* endpoints   | Verifica las fronteras de sesión del API end-to-end      |

## STRIDE Threat Register

| Threat ID | Category               | Component              | Disposition | Mitigation Plan                                                                                            |
| --------- | ---------------------- | ---------------------- | ----------- | ---------------------------------------------------------------------------------------------------------- |
| T-116-19  | Spoofing/Replay        | reuse detection        | mitigate    | Test asserta 401 + familia revocada al reusar refresh rotado (Task 2)                                      |
| T-116-20  | Repudiation            | rotación trazable      | mitigate    | Test verifica replaced_by_id + revoked_at en DB (Task 2)                                                   |
| T-116-21  | Information Disclosure | logout no-leak         | mitigate    | Test asserta 200 idempotente sin revelar existencia del token (D-04, Task 2)                               |
| T-116-22  | Tampering              | aislamiento inter-test | mitigate    | refresh_tokens en TABLES_TO_CLEAN evita contaminación cross-test (Task 1)                                  |
| T-116-SC  | Tampering              | npm installs           | mitigate    | NO se agregan dependencias (vitest/argon2/drizzle ya presentes); si falta algo → checkpoint blocking-human |

</threat_model>

<verification>
- `refresh_tokens` en TABLES_TO_CLEAN; cleanup inter-test correcto.
- `pnpm test test/auth/refresh-tokens.test.ts` verde: rotación, reuse, logout idempotente, change-password, delete-account, dual-access.
- Verificación de estado DB para rotación (replaced_by_id, revoked_at) y sliding 30d.
- Sin `console.*`, sin `any`, sin dependencias nuevas.
</verification>

<success_criteria>

- `pnpm test` pasa con la suite nueva verde (Req 14).
- Los 6 flujos críticos + dual-access verificados contra eltemplo_test (no falso verde por solo type-check — la migración 0125 ya fue aplicada en Plan 02).
  </success_criteria>

<output>
Create `.planning/phases/116-refresh-tokens-auth/116-05-SUMMARY.md` when done
</output>
