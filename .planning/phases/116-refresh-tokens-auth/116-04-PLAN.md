---
phase: 116-refresh-tokens-auth
plan: 04
type: execute
wave: 3
depends_on: ["116-02"]
files_modified:
  - el-templo-admin/src/boot/axios.ts
  - el-templo-admin/src/stores/useAuthStore.ts
  - el-templo-admin/src/boot/__tests__/axios-refresh-lock.test.ts
autonomous: true
requirements: [Req10]
must_haves:
  truths:
    - "El admin usa keys adminAccessToken + adminRefreshToken y lee adminToken legacy como access hasta que expire (D-03)"
    - "login() del authStore admin guarda adminAccessToken+adminRefreshToken; logout() borra las 3 keys; checkAuth() lee adminAccessToken con fallback a adminToken (sin esto el refreshToken nunca se persiste y el interceptor desloguea al primer 401)"
    - "El primer refresh exitoso o re-login borra el adminToken legacy (cleanup diferido) (D-03)"
    - "5 requests 401 concurrentes en el admin disparan exactamente UN /auth/refresh; los 5 se reintentan (Req10 acceptance)"
    - "/auth/refresh whitelisteado: su 401 setea __authRedirected + limpia + window.location /login, sin loop (D-02)"
  artifacts:
    - path: "el-templo-admin/src/boot/axios.ts"
      provides: "interceptor 401 con lock anti-storm (Promise module-scope) + retry + dual-key localStorage"
      contains: "refreshPromise"
    - path: "el-templo-admin/src/stores/useAuthStore.ts"
      provides: "login persiste ambos tokens; logout borra las 3 keys; checkAuth lee adminAccessToken con fallback"
      contains: "adminAccessToken"
    - path: "el-templo-admin/src/boot/__tests__/axios-refresh-lock.test.ts"
      provides: "unit test del lock con 5 requests 401 concurrentes contra el bundle admin"
      contains: "refresh"
  key_links:
    - from: "el-templo-admin/src/boot/axios.ts"
      to: "POST /auth/refresh"
      via: "única llamada por lock compartido module-scope"
      pattern: "auth/refresh"
    - from: "el-templo-admin/src/stores/useAuthStore.ts"
      to: "localStorage adminAccessToken/adminRefreshToken"
      via: "login persiste ambos tokens, logout borra las 3 keys"
      pattern: "adminAccessToken"
    - from: "el-templo-admin/src/boot/axios.ts"
      to: "localStorage adminAccessToken/adminRefreshToken"
      via: "dual-key storage + lectura adminToken legacy"
      pattern: "adminAccessToken"
---

<objective>
Replicar en el admin (Quasar web-only) el mismo refactor de lock anti-refresh-storm que la member app, adaptado a su entorno: `localStorage`, `window.location` (no router de Capacitor), flag `__authRedirected` ya existente para filtrado de Sentry, y keys `adminAccessToken` + `adminRefreshToken` con lectura del `adminToken` legacy diferida (D-02 + D-03). Incluye además la actualización del `authStore` admin (`login`/`logout`/`checkAuth`) para que persista y lea ambos tokens — sin esto el `refreshToken` del response nunca se guarda y el interceptor desloguea al primer 401. Incluye el unit test equivalente del lock contra el bundle admin (acceptance Req 10).

Purpose: El admin se redeploya inmediato (no Play Store), pero igual aplica la migración soft de storage para no deslogear sesiones activas al actualizar. Implementación por-app (D-02): mismo algoritmo, NO package compartido cross-repo.
Output: authStore admin que persiste ambos tokens + interceptor admin con lock + dual-key storage + cleanup diferido + unit test verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-refresh-tokens-auth/116-SPEC.md
@.planning/phases/116-refresh-tokens-auth/116-CONTEXT.md
@CLAUDE.md
@.planning/phases/116-refresh-tokens-auth/116-02-SUMMARY.md

<interfaces>
<!-- Contrato del API consumido (LOCKED, de Plans 01/02). -->

POST /api/auth/refresh -> body { refreshToken } -> 200 { accessToken, refreshToken } | 401
POST /api/auth/login -> 200 { token, accessToken, refreshToken, user, ... }

Estado actual (a refactorizar) — el-templo-admin/src/boot/axios.ts:

- TOKEN_KEY = 'adminToken' (localStorage, web-only, sin Capacitor).
- request interceptor: agrega Bearer desde localStorage.getItem('adminToken').
- response interceptor 401: setea (error as {**authRedirected?:boolean}).**authRedirected = true (filtrado Sentry — PRESERVAR), localStorage.removeItem('adminToken'), si window.location.pathname !== '/login' → window.location.href = '/login'. Sin retry.

Estado actual — el-templo-admin/src/stores/useAuthStore.ts (Pinia setup store):

- `token = ref(localStorage.getItem('adminToken'))`.
- `login()`: lee `{ data }` de `/auth/login`, valida `data.user.role` contra ADMIN_ROLES, hace `token.value = data.token` + `localStorage.setItem('adminToken', data.token)`.
- `logout()`: `token.value = null` + `localStorage.removeItem('adminToken')`.
- `checkAuth()`: `localStorage.getItem('adminToken')` → si existe llama `/auth/me`, valida role, setea `user.value` + `token.value`.
- Estos handlers son los ÚNICOS lugares que persisten/leen el token tras autenticar — si no se actualizan, el `refreshToken` del nuevo contrato se descarta y la fase no funciona.

D-03 (admin storage soft migration):

- Lectura access: `adminAccessToken` nuevo, o `adminToken` legacy como fallback.
- Escritura: `adminAccessToken` + `adminRefreshToken` en primer refresh/re-login; `adminToken` borrado diferido en ese set.

D-02 (lock, mismo algoritmo que la app):

- Promise compartida module-scope; un solo /auth/refresh por oleada; encolar+reintentar; whitelist /auth/refresh (su 401 → \_\_authRedirected + clear + redirect, sin loop).
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: authStore admin (login/logout/checkAuth) + Interceptor admin con lock anti-storm + dual-key + cleanup diferido</name>
  <read_first>
    - el-templo-admin/src/stores/useAuthStore.ts (login/logout/checkAuth actuales — solo manejan adminToken)
    - el-templo-admin/src/boot/axios.ts (interceptor actual — preservar __authRedirected líneas 36-38)
    - el-templo-admin/src/utils/logger.ts (createLogger — warn, no error)
    - 116-CONTEXT.md D-02 + D-03 (keys admin, lock, cleanup diferido)
    - 116-SPEC.md Req 10
  </read_first>
  <behavior>
    - authStore: `login()` guarda `adminAccessToken` (= data.accessToken) + `adminRefreshToken` (= data.refreshToken) en localStorage y setea `token.value = data.accessToken`; `logout()` borra las 3 keys (adminToken legacy + ambas nuevas); `checkAuth()` lee `adminAccessToken` con fallback a `adminToken` legacy.
    - Lectura access (interceptor): `adminAccessToken` nuevo, o `adminToken` legacy como fallback.
    - request interceptor agrega `Bearer <access>` con esa lectura dual.
    - 5 requests 401 concurrentes → un único POST /auth/refresh; los 5 reintentados con el access nuevo.
    - Tras refresh exitoso: escribir `adminAccessToken` + `adminRefreshToken` y BORRAR `adminToken` legacy (cleanup diferido D-03).
    - 401 desde `/auth/refresh` (whitelist) → setear `__authRedirected = true`, limpiar las 3 keys, `window.location.href = '/login'` (si no estamos ya en /login), NO re-disparar refresh (loop prevention).
    - Refresh fallido o sin refreshToken disponible → __authRedirected + clear + redirect (comportamiento actual).
  </behavior>
  <action>
    PARTE A — `el-templo-admin/src/stores/useAuthStore.ts` (BLOCKER: sin esto el refreshToken nunca se guarda):
    - `login()`: el response ahora es `{ token, accessToken, refreshToken, user }`. Tras validar `data.user.role` contra ADMIN_ROLES (mantener), reemplazar `token.value = data.token` + `localStorage.setItem('adminToken', data.token)` por: `token.value = data.accessToken`, `localStorage.setItem('adminAccessToken', data.accessToken)`, `localStorage.setItem('adminRefreshToken', data.refreshToken)`. Conservar el manejo de error existente.
    - `logout()`: reemplazar `localStorage.removeItem('adminToken')` por borrar las 3 keys (`adminToken` legacy + `adminAccessToken` + `adminRefreshToken`); mantener `token.value = null` + `user.value = null`.
    - `checkAuth()`: cambiar `const storedToken = localStorage.getItem('adminToken')` a leer `adminAccessToken` con fallback al legacy: `const storedToken = localStorage.getItem('adminAccessToken') ?? localStorage.getItem('adminToken')`. El resto (llamada a /auth/me, validación de role, set de user/token) sin cambios. El interceptor refrescará el access vencido de forma transparente, así que checkAuth no necesita lógica de refresh propia.
    - Inicializar el `token` ref con el mismo fallback: `ref(localStorage.getItem('adminAccessToken') ?? localStorage.getItem('adminToken'))`.
    Sin `any` (tipar `data` con la forma del response; el store ya usa narrowing en el catch). Sin `console.*`.

    PARTE B — `el-templo-admin/src/boot/axios.ts`:
    Definir constantes `ACCESS_KEY='adminAccessToken'`, `REFRESH_KEY='adminRefreshToken'`, `LEGACY_KEY='adminToken'`. Helpers `getAccess()` (lee ACCESS_KEY o LEGACY_KEY), `getRefresh()`, `setTokens(access, refresh)` (escribe ambas nuevas + `localStorage.removeItem(LEGACY_KEY)` — cleanup diferido), `clearAll()` (borra las 3). En module scope `let refreshPromise: Promise<string | null> | null = null` (lock D-02). Helper `runRefresh()`: reusa `refreshPromise` si existe; si no, llama `POST /auth/refresh` con `{ refreshToken: getRefresh() }` vía una instancia axios cruda (sin interceptor, evita reentrada), en éxito `setTokens(...)` y resuelve el access nuevo, en error resuelve null, `finally` resetea `refreshPromise=null`. Migrar el request interceptor a `getAccess()`. Reescribir el response interceptor 401: (a) si `error.config.url` matchea `/auth/refresh` → marcar `__authRedirected`, `clearAll()`, redirect, reject (whitelist); (b) si `config.__isRetry` → mismo clear+redirect, reject; (c) si `!getRefresh()` → clear+redirect, reject; (d) caso normal → `const newAccess = await runRefresh()`; si newAccess → `config.__isRetry=true`, `config.headers.Authorization='Bearer '+newAccess`, `return api(config)`; si null → marcar `__authRedirected`, clear, redirect, reject. PRESERVAR el patrón `__authRedirected` para el filtrado de Sentry. Loguear fallos con `createLogger().warn`. TypeScript sin `any` (tipar `__isRetry`/`__authRedirected` vía augmentación de `InternalAxiosRequestConfig`/error). Sin `console.*`.

  </action>
  <verify>
    <automated>cd el-templo-admin && grep -c "refreshPromise" src/boot/axios.ts ; grep -c "adminAccessToken" src/boot/axios.ts ; grep -c "adminAccessToken" src/stores/useAuthStore.ts ; grep -c "adminRefreshToken" src/stores/useAuthStore.ts ; grep -c "__authRedirected" src/boot/axios.ts ; grep -c "console\." src/boot/axios.ts src/stores/useAuthStore.ts ; grep -c ": any" src/boot/axios.ts src/stores/useAuthStore.ts</automated>
  </verify>
  <acceptance_criteria>
    - authStore `login()` guarda `adminAccessToken` + `adminRefreshToken` en localStorage y setea `token.value = data.accessToken` (tercer y cuarto comandos >=1)
    - authStore `logout()` borra las 3 keys (adminToken legacy + adminAccessToken + adminRefreshToken)
    - authStore `checkAuth()` lee `adminAccessToken` con fallback a `adminToken` legacy
    - `refreshPromise` module-scope (lock D-02); un único /auth/refresh por oleada (primer comando >=1)
    - Keys `adminAccessToken`/`adminRefreshToken` usadas en el interceptor; `adminToken` legacy leído como fallback y borrado diferido en setTokens (segundo comando >=1)
    - `__authRedirected` preservado para filtrado Sentry (quinto comando >=1)
    - Whitelist /auth/refresh: su 401 hace clear+redirect sin re-disparar
    - Sin `console.` ni `any` en axios.ts ni authStore (últimos dos comandos = 0)
  </acceptance_criteria>
  <done>authStore admin persiste ambos tokens y lee con fallback; interceptor admin con lock + dual-key + cleanup diferido + __authRedirected preservado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit test del lock contra el bundle admin (5 requests 401 concurrentes)</name>
  <read_first>
    - el-templo-admin/src/boot/axios.ts (Task 1)
    - el-templo-admin/ (config vitest — ver si existe vitest.config.* o test setup; replicar el patrón de la plan 03 del app)
    - el-templo-app/src/boot/__tests__/axios-refresh-lock.test.ts (analog — mismo test, adaptar storage a localStorage y redirect a window.location)
    - 116-SPEC.md Req 10 (acceptance: mismo test que Req 9 contra admin)
  </read_first>
  <behavior>
    - 5 requests concurrentes reciben 401 → exactamente 1 POST /auth/refresh (contador mockeado), los 5 reintentados con el access nuevo.
    - 401 desde /auth/refresh → no dispara otro refresh, ejecuta __authRedirected + clear + redirect (loop prevention).
  </behavior>
  <action>
    Crear `el-templo-admin/src/boot/__tests__/axios-refresh-lock.test.ts` (vitest). Mockear `window.location` (jsdom) y `localStorage`; mockear el adapter de axios / la instancia cruda de refresh para contar las llamadas a `/auth/refresh`. Test 1: disparar 5 requests concurrentes que devuelven 401 → assert que `/auth/refresh` se llamó exactamente 1 vez y que los 5 se reintentan con el access nuevo. Test 2: 401 desde `/auth/refresh` → assert que NO se dispara otro refresh, que `__authRedirected` queda seteado, y que se limpian las keys. Si hace falta config vitest, agregar la config mínima necesaria SIN instalar dependencias nuevas. Sin `console.*`/`any`.
    NOTA importante: el admin NO tiene vitest instalado de forma garantizada. NO instalar dependencias bajo ninguna circunstancia (constraint del usuario, precedente axios supply chain). La verificación del runner está gobernada por el `<verify>` de abajo — si vitest no está presente, NO correr `npx vitest` directo (daría un error de módulo ambiguo); seguir la instrucción condicional del `<verify>` y abrir el checkpoint blocking-human.
  </action>
  <verify>
    <automated>cd el-templo-admin && if [ -x node_modules/.bin/vitest ]; then ./node_modules/.bin/vitest run src/boot/__tests__/axios-refresh-lock.test.ts 2>&1 | grep -ciE "pass|✓" ; else echo "VITEST_MISSING"; fi ; grep -c ": any" src/boot/__tests__/axios-refresh-lock.test.ts</automated>
    <conditional-checkpoint>
      Si el primer comando del verify imprime `VITEST_MISSING` (no existe `node_modules/.bin/vitest` en el admin): NO instalar vitest/jsdom (el usuario prohíbe agregar/actualizar dependencias sin aprobación — precedente axios supply chain). En su lugar, ABRIR un checkpoint blocking-human informando que el test del lock admin requiere vitest+jsdom y pidiendo aprobación explícita para instalarlos (o decisión de skipear el unit test admin y verificar el lock solo por el test de la member app, que comparte el algoritmo). NO ejecutar `npx vitest run` (descargaría/ejecutaría un paquete sin aprobación y daría error de resolución de módulo).
    </conditional-checkpoint>
  </verify>
  <acceptance_criteria>
    - Si vitest está presente: test de 5 requests 401 concurrentes → exactamente 1 /auth/refresh, los 5 reintentados (verde — primer comando >=1) + test de loop prevention verde
    - Si vitest NO está presente (VITEST_MISSING): el executor abrió un checkpoint blocking-human pidiendo aprobación de instalación, sin correr `npx vitest` ni instalar nada
    - Sin `any` (último comando = 0)
    - Ninguna dependencia nueva instalada sin aprobación explícita del usuario
  </acceptance_criteria>
  <done>Unit test del lock admin escrito; ejecutado si vitest está presente, o checkpoint blocking-human si falta (sin instalar deps).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                    | Description                              |
| ------------------------------------------- | ---------------------------------------- |
| localStorage -> admin app                   | Tokens en reposo en el browser; web-only |
| admin -> API /auth/refresh                  | Refresh plano sale hacia el API          |
| oleada de 401 concurrentes -> /auth/refresh | Posible storm sin lock                   |

## STRIDE Threat Register

| Threat ID | Category               | Component                   | Disposition | Mitigation Plan                                                                                                                             |
| --------- | ---------------------- | --------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| T-116-15  | Denial of Service      | refresh storm (admin)       | mitigate    | Lock Promise module-scope: 1 /auth/refresh por oleada (Task 1 + test Task 2)                                                                |
| T-116-16  | Elevation of Privilege | loop /auth/refresh (admin)  | mitigate    | Whitelist: 401 del refresh → \_\_authRedirected + clear + redirect, no re-dispara (Task 1/2)                                                |
| T-116-17  | Information Disclosure | logs + Sentry               | mitigate    | \_\_authRedirected preservado para filtrado; createLogger().warn no .error                                                                  |
| T-116-18  | Spoofing               | tokens en localStorage      | accept      | localStorage como hoy; secure storage out-of-scope (SPEC); admin web detrás de auth                                                         |
| T-116-SC  | Tampering              | npm installs (vitest/jsdom) | mitigate    | NO se agregan dependencias; si falta vitest → checkpoint blocking-human (Task 2 verify conditional), NUNCA `npx vitest` ni install autónomo |

</threat_model>

<verification>
- authStore admin login/logout/checkAuth persisten/leen adminAccessToken+adminRefreshToken con fallback a adminToken legacy.
- Interceptor admin con lock module-scope: unit test 5 requests 401 concurrentes → 1 /auth/refresh, todos reintentados (Req 10), SOLO si vitest presente; si no, checkpoint blocking-human.
- Dual-key `adminAccessToken`/`adminRefreshToken` + lectura `adminToken` legacy + cleanup diferido (D-03).
- `__authRedirected` preservado; whitelist /auth/refresh probado.
- Sin `console.*`, sin `any`, sin dependencias nuevas instaladas sin aprobación.
</verification>

<success_criteria>

- El refreshToken del response admin se persiste (authStore.login usa adminRefreshToken) — el interceptor encuentra getRefresh() != null.
- Admin nunca desloguea a una sesión activa con refresh válido.
- Lock anti-storm verificado por unit test (acceptance Req 10) o checkpoint si falta vitest.
- Migración soft de storage admin sin deslogueos al redeploy (D-03).
  </success_criteria>

<output>
Create `.planning/phases/116-refresh-tokens-auth/116-04-SUMMARY.md` when done
</output>
</content>
