/**
 * OpenAI Provider Implementation
 *
 * TODO: Implement AiProvider interface using the openai npm package.
 * See digital-initiatives/whatsapp-agent-renovafacil/message_processor.py for pattern.
 *
 * Key points:
 * - Use function calling (tools parameter) for bot actions
 * - Map ToolDefinition[] to OpenAI's tool format
 * - Parse tool_calls from response
 * - Handle rate limits with retries
 */

export {};

// TODO: export class OpenAiProvider implements AiProvider { ... }
