# El Templo API - Production Environment (AWS)
# Copy to el-templo-api/.env.production and fill in values

# Server Configuration
PORT=3000
NODE_ENV=production

# Database (MySQL on same EC2)
DB_HOST=localhost
DB_PORT=3306
DB_USER=eltemplo
DB_PASSWORD=YOUR_SECURE_DB_PASSWORD_HERE
DB_NAME=eltemplo

# Frontend URLs (for CORS)
FRONTEND_URL=https://app.eltemplo.org
ADMIN_URL=https://admin.eltemplo.org

# JWT Secret - GENERATE A NEW ONE!
# Run: openssl rand -base64 64
JWT_SECRET=GENERATE_WITH_openssl_rand_-base64_64

# Logging
LOG_LEVEL=info
